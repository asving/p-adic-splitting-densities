# NODE C.128 (`lawBS2_pair_or_triple`, re-signed A-C.5) — RESCHEDULE: needs `TouchCert`

**Verdict: RESCHEDULE, not BLOCKED-on-mathematics.** The narrowed statement is proved at
corpus grade (`lean/notes/openmath/LAWBS2_PROOF_2026-08-16.md`, 4 adversarial passes CLEAN,
battery `verification/openmath/bs2_lawbs2_cert.py` GREEN 666/0). What stops the Lean
transcription today is a **stub-gate pre-check failure**, recorded here so the next agent does
not re-derive it.

## The blocking fact

The signed conclusion is

```
TouchCert T hπ μ₂ ((μ₂ - 2) + (2 * T.e₂ * tstar + 1) / (T.e₂ * T.f₂))
  ↔ (¬ (ringChar (ResidueField O) ∣ Nat.choose μ₂ 2)
     ∨ ¬ (ringChar (ResidueField O) ∣ Nat.choose μ₂ 3))
```

`TouchCert` is **NODE C.74's `def`** (`leanspec/Leanspec/ChapC.lean:3302`):

```
def TouchCert (T : TowerDatum F H₀ hpin) (hπ : Irreducible π) (μ₂ j : ℕ) : Prop :=
  dv2Hgt (T.levelDatum hπ)
      (shadowDev T ((composedKey T) ^ μ₂) j - dev (composedKey T) ((composedKey T) ^ μ₂) j)
    = (T.theta μ₂ j : ℕ∞)
```

There is **no `leanfinal/Uniformity/ChapC/C74.lean`** and no declaration named `TouchCert`
anywhere under `leanfinal/Uniformity/` (checked 2026-08-16 by `grep -rn TouchCert leanfinal/`;
the only hit is a prose mention in `C71.lean`'s docstring). C.128 cannot be *stated* in the
landed corpus, let alone proved.

**RESCHEDULE: needs `TouchCert` (NODE C.74's def).** Landing just the `def` is a one-line
node — its ingredients `dv2Hgt` (C.11), `shadowDev` (C.71), `dev` (B.02), `composedKey` (C.43),
`TowerDatum.theta` (C.71), `levelDatum` (C.44) are **all landed** — so the unblocking unit is
small and should be scheduled before C.128 is re-attempted. (C.74's three companion `axiom`s
`shadow_attained_of_certificate` / `shadow_persistence` / … are NOT needed by C.128; only the
`def`.)

## The three real proof obstacles behind it (found while scoping, not yet attacked)

Once `TouchCert` exists, C.128 is still materially harder than C.127's `lawEW_pin` was. Three
named gaps, in the order they will bite:

**1. No `hcomp`. The trinomial shape of `composedKey T` must be DERIVED.**
C.127's three clauses were handed `hcomp : composedKey T = Φ′^{f₂} + C(c₂π^{v₂})x^{i₂}Φ′^{f₂−1}
+ C(c₀π^{v₀})` as a *hypothesis* — the node is "D19-safe" precisely because it never touches
`stageLiftO`. C.128's binder list has no such hypothesis; the shape must come out of C.43's
body

```
composedKey T = F.key ^ (e₂f₂)
  − Σ_{t < f₂} F.stageLiftO H₀ hpin ((f₂ − t)·u₂) (−ψ₂.coeff t · stageLetter ^ wrapExp T t)
      * F.key ^ (e₂ t)
```

together with `he₁ : e₁ = 2`, `hf₂ : f₂ = 2`, `hcasc`, `hslot`, `hψt`, `T.hψ0`, `hf₁`. The
proof note states the target explicitly as (G2)/(G3) and asserts that at the Lean carrier the
on-side heights and the `x`-freeness of `c₀` are *automatic*, but the derivation is not
written anywhere. **This is the largest single piece and deserves its own node** (an
"unfolding of `composedKey` at `e₂ = 1, f₂ = 2`" lemma; note C.43's own `example` unfolding
checks at `f₂ = 1, 2` are the seed). Binder derivation supplied by A-C.5 (verbatim, so the
next agent need not redo it): `he₁ + hf₁` give `D′ = 2`, `F.hcop` then makes `h` ODD,
`F.hpure` gives one-sidedness of slope `h/2`; `hf₂ + hcasc + htlt` force `T.e₂ = 1` and
`t* = 1`; `hslot` gives `i₂ = 1`; `hψt` and `T.hψ0` give the two entry units `ĉ₂, ĉ₀`;
`T.hfloor` gives `δ ≥ 1`.

**2. The key is NOT binomial, so C.129 (a) does not apply.**
The proof note's frame class (G1) is `Φ′ = x² + κ₁x − π^hω` with `v(κ₁) ≥ (h+1)/2` — the
binomial subclass `κ₁ = 0` is the certified sub-case, but the theorem is proved for the whole
one-sided class, and at the Lean carrier `F.key` is a general monic one-sided key. C.129's
`binomKey_mod_monomial` (and hence C.127's `stageHeight_rep_monomial`) is stated at
`X^D − C w` and **cannot be reused**. What IS reusable from the wave-12 pin unit, unchanged:
`KeyFrame.stageHeight_zero`, `KeyFrame.stageHeight_eq_inf_range`,
`KeyFrame.min_stageHeight_le_add` (the ultrametric inequality — the note's §5 `(0,0)`-slot
bound "the height is the min of the two deposits and only rises when they collide" is exactly
this lemma), `dvSupp_eq_of_slots`, `dev_reass`, `seedQuot_coeff`/`seedRem_coeff`. What must be
NEW: the degree-2 wrap dictionary `x² = Φ′ + (π^hω − κ₁x)`, `x³ = (x−κ₁)Φ′ + …`,
`x⁴ = Φ′² + (2π^hω + κ₁² − 2κ₁x)Φ′ + …` (the note's §4 displays, exact), plus the weight
calculus LEMMA 3 (3a)–(3d).

**3. The census is a multinomial expansion, not one division.**
C.127's pin rested on ONE monic division (`seed_division_census`, landed). C.128 needs the
note's §2 wrap-seed sum over all `(k,l,r)` with `k+l+r = μ₂`, the §3 weight grading (depth `j`
sits `jδ` above the pure power's own weight), the §4 three-branch survival argument
(`l ∈ {2,3,4}`, `r = 0`), and LEMMA 4's chain values `R_{μ₂−1}(Y^{2μ₂−2}) = 1` and
`R_{μ₂−1}(Y^{2μ₂−1}) = Y − (μ₂−1)P`. Landed material that helps: `devQ_add`, `devQ_mul_pow`,
`devQ_mul_pow_of_lt`, `devQ_eq_zero_of_lt`, `biRead_eval_eq_map`, `shadowDev_of_ingrid`
(C.127 Parts 1–2) — but note the in-grid transfer `shadowDev_of_ingrid` **does not apply**
here: the pure power is over-grid, which is the whole point of the law.

**4. One small bridge, easy but not landed.** `ringChar (ResidueField O) ∣ n ↔ ¬ IsUnit (n : O)`
for `n : ℕ` (the note's "an integer is a unit of `O` iff `p ∤ n`", used in both directions and
in both mixed and equal characteristic).

## Two arithmetic facts already checked, so the next agent can skip them

* **The coordinate index is `μ₂ − 1`.** `hf₂ : T.f₂ = 2` + `htlt : tstar < T.f₂` +
  `hcasc : T.e₂ * (T.f₂ − tstar) = 1` force `T.e₂ = 1` and `tstar = 1` (a `Nat.eq_one_of_mul…`
  / `omega` argument), and then the stub's index
  `(μ₂ − 2) + (2·T.e₂·tstar + 1)/(T.e₂·T.f₂) = (μ₂ − 2) + 3/2 = (μ₂ − 2) + 1 = μ₂ − 1`
  for `μ₂ ≥ 3` (ℕ-division). This matches the note's `j* := μ₂ − 1`.
* **`hslot` says `i₂ = 1`.** With `tstar = 1` and `T.f₂ = 2`, `hslot` reads
  `F.slotIdx T.u₂ = 1`, i.e. the `t*`-entry carries `x`-power exactly `1`.

## Scope fence that must NOT be widened

`he₁ : F.e₁ = 2` and `hf₂ : T.f₂ = 2` are load-bearing: without them the statement is REFUTED
on two constructed PARI-valid axes (`e₁ = 4`, empty top band; `e₁ = 3`, unit-dependent merged
digit) — `verification/openmath/bs2_lawbs2_cert.py`, BS2-STUBGAP, and A-C.5's warning block.
The `f₂ ≥ 3, D′ = 2` slice is MEASURED-OPEN (note §7.3); widening `hf₂` needs a new
certification leg first.

---

*Recorded by Chapter-C wave 12 agent 2, after landing C.129 clause (b) and C.127's
`lawEW_pin`. No Lean was written for C.128 — the pre-check failed at `TouchCert`.*
