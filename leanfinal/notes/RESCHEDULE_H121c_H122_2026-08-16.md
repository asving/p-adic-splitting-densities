# BLOCKED record — NODES H.121 (clause iii) and H.122, Chapter-H §17 fleet wave 2, agent 1

**Date.** 2026-08-16. **Verdict.** H.121a and H.121b **LANDED** (commits `2c009424`,
`4a8f848f`); **H.121c BLOCKED** — dependency not landed; **H.122 BLOCKED** — dependency not
landed **AND** the blueprint's stated proof route is refuted at the stated constants
(finding below). Neither node was weakened, and no stand-in was committed.

---

## 1. H.121 clause (iii) — the β leg. BLOCKED: needs H.116b.

Signed statement (`leanspec/Leanspec/ChapH.lean`, `uCluster_beta_leg`; blueprint §17.3 NODE
H.121):

```
(Nat.card {c : ClusterState O m N // IsBetaState π c ∧ ClusterUndecided O π m N c} : ℝ)
  ≤ (N : ℝ) ^ m * (residueCard O : ℝ) ^ (m * (N - 1)) * ((residueCard O : ℝ) ^ (N - 2))⁻¹
```

**At `m = 2` the clause is trivial** — the β bucket is EMPTY. `HasChildAt` carries `2 ≤ μ`
(H.109) and H.118's `betaChild_mult_lt` forces `μ < m`, so at `m = 2` no β state has a child
and H.111 (b)'s third conjunct fails. The count is `0`.

**At `m = 3` the clause is not reachable from what is landed.** The blueprint's PROOF field
routes it through *"partition the β-undecided set by genre (H.116 (ii))"*, i.e. through
`betaExtract_fiber_card` — **H.116b, which is not landed** (H.116 clause (i),
`betaChild_spec`, is; `H116bR.lean` currently carries only §1–§2 of the re-planting
construction, commit `8dbbdd9b`). Per the wave's standing rule this is
**RESCHEDULE: needs H.116b**.

No crude substitute exists. The trivial bound `#(β ∧ undec) ≤ census = Q^(m(N−1))` is weaker
than the target exactly when `N^m < Q^(N−2)`, which is the whole asymptotic content of the
clause (at `q = 2, m = 3` the target first bites at `N = 15`; at `q = 3, m = 3`, at `N = 8`).
So the genre fibration is load-bearing, not a convenience.

**Two further dependencies the rescheduler should price in** (H.116b alone will not finish
this node):

1. `betaExtract_fiber_card` is deliberately `∃ F` only — the genre census `census_g` is
   TRACK V and the blueprint explicitly does **not** owe it at H.116. The pricing route that
   avoids `census_g` is: `#(genre g) = F_g · Π T_l ≤ Q^(m(N−1))`, then the composed count
   `F_g · [Π T_l − Π (T_l − u_l)] ≤ #(genre g) · Σ_l û_l` by H.63's `prod_sub_prod_le_sum`.
   That route needs the child rate `û_l` at degree `μ = 2`.
2. That child rate is the `m = 2` species — H.122 at `m = 2`, or §5's landed `m = 2` closed
   forms (H.26/H.28) as the blueprint's own alternative. Using H.122 would be circular
   (H.122 is ONE theorem over `2 ≤ m ≤ 3`), so the §5 route is the one that closes.

## 2. H.122 — the cluster rate species. BLOCKED, and the route is REFUTED.

Signed statement: `RateSpecies (residueCard O) 1 1 1 (uClusterNorm O π m)` for `2 ≤ m ≤ 3`,
i.e. `û(N) ≤ N · Q^(−(N−1))` for `N ≥ 1`.

It is blocked first because it consumes H.121 (iii) (§1). But it does not become unblocked
when H.121 (iii) lands, and that is the finding:

### FINDING F-H17.2 — H.121 (i)+(ii)+(iii) do not entail H.122, at any `N`

Normalize H.121 (i) by the census `Q^(m(N−1))`. The split is an EQUALITY into three
NONNEGATIVE summands:

```
û(N)  =  ĥead(N)  +  α̂(N)  +  β̂(N),      all three ≥ 0.
```

H.122's conclusion is `û(N) ≤ N · Q^(−(N−1))`, so any derivation from the three legs must in
particular have the β summand inside that budget:

```
β̂(N)  ≤  N · Q^(−(N−1))            (what the conclusion needs of the β cell)
```

Clause (iii), normalized, supplies only

```
β̂(N)  ≤  N^m · Q^(−(N−2))  =  Q · N^m · Q^(−(N−1))     (what clause (iii) gives)
```

and `Q · N^m > N` for **every** `Q ≥ 2`, `m ≥ 2`, `N ≥ 1`. The envelope therefore never
certifies even the β cell alone against H.122's TOTAL budget, let alone against the slack
left after the head and α cells. The gap is not an artifact of a lazy head/α estimate: the
inequality above uses nothing about the other two summands beyond nonnegativity.

**Numeric certificate** (conservative reference model `hexU`, `q = 2`, `m = 3`; the same
transcription H.119/H.121a/H.121b gate):

| `N` | true `β̂` | clause (iii) envelope on `β̂` | H.122's total budget `N·Q^(−(N−1))` |
| --- | --- | --- | --- |
| 3 | 0 | 13.5 | 0.75 |
| 5 | 0.0039 | 15.6 | 0.3125 |
| 6 | 0.0064 | 13.5 | 0.1875 |
| 7 | 0.0035 | 10.7 | 0.1094 |

The envelope exceeds the budget by a factor ~100 at every cell — indeed it exceeds `1`, the
trivial bound `β̂ ≤ 1`, at all of them. (The blueprint's own TEETH note records that the
battery measures `K′ ≤ 0.0004` against this envelope; the statement then sets `K′ = 1` and
`B′ = 0` at `c′ = 1`, discarding all of that measured slack.)

### The route note's `rate_close` leg fails for a second, independent reason

H.122's ROUTE NOTE offers *"one `rate_close` application at the repaired structure"* (after
PA-H17.1). The landed `rate_close` (H.71) returns

```
∃ K, 0 ≤ K ∧ ∀ D, RateSpecies Q K (m + L.B' + 1) (L.c' + 1) (u D),   with K = 1 + L.K'
```

so at clause (iii)'s legs `(K′, B′, c′) = (1, 0, 1)` it returns `RateSpecies Q 2 (m+1) 2` —
that is `(K, B, c) = (2, m+1, 2)`, not `(1, 1, 1)`. H.66's `RateSpecies.mono` moves constants
UPWARD only (`K ≤ K'`, `B ≤ B'`, `c ≤ c'`), so `(1,1,1)` is strictly sharper than anything
`rate_close` can hand back at these legs. PA-H17.1 repairs the RANGE (finding F-H17.1); it
does not touch the constants.

### What the conclusion actually needs, and what is true

Replaying the six steps locally with the EXACT head (H.113: `ĥead(N) = Q^(−(N−1))` on the
nose) and the α leg bounded by the induction hypothesis plus H.30 (`Σ_{k≥1} (Q−1) Q^(−k·γ)
≤ 1` at `γ = clusterC m ≥ 1`) gives

```
û(N)  ≤  (1 + (N − m)) · Q^(−(N−1))  +  β̂(N),
```

so the β cell's budget is `(m − 1) · Q^(−(N−1))`. H.122 at `(1,1,1)` therefore needs a β
envelope of the shape `β̂(N) ≤ K'' · Q^(−(N−1))` with `K'' ≤ m − 1` and **no polynomial
factor in `N`** — sharper than clause (iii) in the polynomial degree AND in the offset. That
is a statement-level change to H.121 (iii) (or a fourth clause), which this agent does not
own.

Two facts worth carrying into the re-plan, both checked — and the first is now **LANDED in
Lean**, at `leanfinal/Uniformity/ChapH/H122m2.lean` (commit `2720c2f3`), sorry-free with a
Lean-core footprint:

* **At `m = 2` the constants are exactly right and the node is unconditional.** The β bucket
  is empty (§1), so `β̂ ≡ 0`, the display above reads `û(N) ≤ (N − 1)·Q^(−(N−1))`, and
  `(1,1,1)` closes with room. The whole of H.122's gap sits at `m = 3`.
  `H122m2.lean` proves `not_isBetaState_two`, the `m = 2` recursion as an EQUALITY
  (`uCluster_two_recursion`), the battery's `P1(i)` tie `uCluster O π 2 = uTwo (residueCard O)`
  (`uCluster_two_eq_uTwo`), and `RateSpecies (residueCard O) 1 1 1 (uClusterNorm O π 2)`
  (`uClusterNorm_rateSpecies_two`). **It is explicitly NOT the signed H.122** — the signed
  statement quantifies over `2 ≤ m ≤ 3` and remains BLOCKED. The tie is also the bridge
  §1's item 2 asks for: it lets H.121 (iii)'s degree descent consume H.26/H.28 at `μ = 2`
  without touching H.122.
* **The measured β mass is comfortably inside the needed budget.** At `q = 2, m = 3` the true
  `β̂` is `0.0064` at `N = 6` against a needed `(m−1)·Q^(−(N−1)) = 0.0625`, and `0.0035` at
  `N = 7` against `0.0313`. So `(1,1,1)` is not in danger — only the ENVELOPE that H.121 (iii)
  states is.

## 3. Disposition

* **H.121c** — RESCHEDULE behind H.116b (`betaExtract_fiber_card`), and price in the two
  further dependencies of §1.
* **H.122** — RESCHEDULE behind H.121c, and **re-adjudicate H.121 (iii)'s envelope first**:
  as stated it cannot close H.122 at `(1,1,1)`, by F-H17.2. Owner decision needed on whether
  H.121 (iii) is re-signed with a sharper envelope, or H.122 is re-signed at the constants
  `rate_close` actually yields.
* Landed this wave and available to both: `uCluster_split` (H.121a,
  `leanfinal/Uniformity/ChapH/H121.lean`) and `uCluster_alpha_leg` (H.121b,
  `leanfinal/Uniformity/ChapH/H121b.lean`), both sorry-free with Lean-core footprints and
  both verified to inhabit the frozen stub signatures verbatim; plus the unconditional
  `m = 2` half of H.122 and the `P1(i)` tie (`leanfinal/Uniformity/ChapH/H122m2.lean`).

⚠ S-1 throughout: every count named here is the CONSERVATIVE complement `u_{m,1}`, never the
semantic `undecidedCount` (finding F-2).

---

## 4. DISPOSITION APPENDED 2026-08-16 by unit A-H.6 — READ THIS BEFORE §2's LAST PARAGRAPH

**F-H17.2 is UPHELD and DISCHARGED at the statement level** (blueprint AMENDMENT **A-H.6**,
`blueprint/CHAP-H_general_induction.md`). But **§2's proposed repair — `β̂(N) ≤ K''·Q^{−(N−1)}`
with `K'' ≤ m − 1` and NO polynomial factor in `N` — is itself REFUTED.** Do not build on it.

* **The refutation.** `ratio(N) := β̂(N)·Q^{N−1}` is not bounded; it grows LINEARLY. First
  failures of `ratio ≤ m − 1 = 2`: `q = 2` at **`N = 32`** (`ratio = 4518967813/2147483648 =
  2.1043…`), `q = 3` at **`N = 30`** (`ratio = 441064751910830/205891132094649 = 2.1422…`).
  Certificate: `verification/AH6_beta_envelope_check.py` (54/54, exit 0, exact `Fraction`
  arithmetic; the reader imported verbatim from `OM2_genindb_battery.py`).
* **Why §2's derivation reached it.** It priced the α leg with H.30 (`Σ ≤ 1`), which is TIGHT at
  `c = 1` (`m = 2`) but crude at `clusterC 3 = 3`, where the true value is `1/(Q²+Q+1) ≤ 1/7`.
  Fixing the α pricing — not shrinking the β cell — is what buys the room.
* **What A-H.6 signed instead.** H.121 (iii) at `(K′, B′, c′) = (1/3, 1, 1)`:
  `#(β ∧ undec)(N) ≤ (1/3)·N·Q^{m(N−1)}·Q^{−(N−1)}`. True with a `> 4.3×` margin at every
  tested `(q, N)`, and it IMPLIES the frozen envelope at every `N` (a strengthening; H.122 is
  its only consumer). Two nodes added: **H.121d** (the content-weighted β census
  `Σ_{β} Q^{D} ≤ (1/3)Q^{m(N−1)}`; sharp value `Q/(Q²+Q+1) ≤ 2/7`, derived in closed form from
  the three β polygon shapes at `m = 3`) and **H.30b** (`Σ_{k≥1}(Q−1)Q^{−ck} ≤ 1/3` for
  `c ≥ 2`).
* **H.122's route.** `rate_close` is now CLOSED as refuted for these constants (this record's
  own second leg, promoted to the node), and **PA-H17.1 is no longer a prerequisite**. The signed
  route is the local replay, split on `m`: `m = 2` cites the landed
  `uClusterNorm_rateSpecies_two`; `m = 3` runs `û(N) ≤ [1 + (N−3)⁺/3 + N/3]·Q^{−(N−1)} ≤
  N·Q^{−(N−1)}`. ⚠ Clause (iii) does NOT close `m = 2` — see A-H.6 §4's fence.
* **H.121c's reschedule reason is UNCHANGED**: still behind H.116b (`betaExtract_fiber_card`),
  now also behind H.121d. §1's two priced dependencies are both adjudicated in A-H.6 §3: the
  genre census `census_g` CANCELS out of the union bound (TRACK V not owed), and the `μ = 2`
  child rate is taken from H.28 / the landed `m = 2` species, never from the signed H.122.

---

## 5. DISPOSITION APPENDED 2026-08-16 by unit A-H.7 — H.116b IS NOW FOUR NODES

**§1's and §4's reschedule reason for H.121c is UNCHANGED in substance** — it still waits on
`betaExtract_fiber_card` (H.116b) and on H.121d. What changed is that H.116b is no longer a
single assignable node.

* **The blocker was adjudicated, not removed.** `leanfinal/notes/BLOCKED_H116b_2026-08-16.md`
  reported H.116b BLOCKED (true but four node-sized jobs, one of them a research step) and
  recommended a re-split. **Blueprint AMENDMENT A-H.7**
  (`blueprint/CHAP-H_general_induction.md`) signs that re-split:
  **H.116b1** (the `Finset`-indexed multi-child peel + the cofactor-inherits clause),
  **H.116b2** (multiplicity ADDS + the genre/contents/reads of a planted product),
  **H.116b3** (the `¬ IsCSState` transport at the ChapB polygon API),
  **H.116b4** (THE COUNT, at the gauge-resultant route), with **H.116b itself demoted to the
  ASSEMBLY node** that consumes them (A-H.7 §8). `betaExtract_fiber_card` is BYTE-UNCHANGED.
  Land them in the order **b2 → b1 → b3 → b4 → assembly**; leanspec stubs are in
  `leanspec/Leanspec/ChapH.lean` (`lake build Leanspec.ChapH` green) and the 24 DAG rows are in
  `spec/DAG_BLUEPRINT_H.tsv`.
* **What the rescheduler must price now.** H.121c's H.116b dependency is really a dependency on
  **four** nodes, one of which (**H.116b4**) is *"not a transcription"* — its layer-2 step is a
  Smith-normal-form statement about the Sylvester lattice of the planted factors. Do not put
  H.121c in a wave that assumes H.116b is one agent-sized job.
* **⚠ FENCE A-H.7/F1 carries into this note.** The peel-shift-REPLANT route to the count is
  CLOSED — refuted with witnesses by `OM2_h116b_replant_cert.py`'s checks **D3** (two exact
  planted presentations of the same class, shifted by the same `Δ`, land in DIFFERENT classes)
  and **E3** (the round trip is not the identity), and surjectivity fails for the same reason.
  Assign no agent to it, at H.121c or anywhere else.
* **The single-child shortcut does NOT rescue H.121c.** A-H.7 §9 records
  `betaExtract_fiber_card_single` as OPTIONAL and NOT scheduled: at `m = 3` the fact that
  `|L| = 1` follows only from the degree-sum bound `Σ_p μ_p ≤ m`, which is **H.116b1's
  conclusion** (A-H.7 §3 adjudicates that it does NOT come from H.108), and the variant still
  needs H.116b3. Electing it avoids b4 and nothing else.
* **Unchanged by A-H.7:** §1's two further dependencies as A-H.6 §3 adjudicated them
  (`census_g` CANCELS out of the union bound — A-H.7 §8 step 4 preserves that by leaving `G`
  and `Nat.card 𝒬` uncomputed; and the `μ = 2` child rate comes from H.28 / the landed `m = 2`
  species, never from the signed H.122); §2's finding F-H17.2 and its A-H.6 disposition; H.122's
  signed route.
