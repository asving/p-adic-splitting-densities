# UNIT NOTE — CERTAIN-NODES BATCH B (CN-05 … CN-11), 2026-08-14

Formalization batch from `spec/CERTAIN_NODES_2026-08-14.md`: the general-`n` density/census
API cluster over the landed `decidedSeq`/`possibleSeq`/`decidedDensity`/`genuineDensity`
machinery (`Uniformity/Density/GenuineDensity.lean`). Order run: CN-05 → CN-06 (edge),
CN-07 → CN-08 (edge), CN-09, CN-10, CN-11 (independent). All seven LANDED, none BLOCKED.

New file (root import added, no landed file edited):
`leanfinal/Uniformity/Density/DensityAPI.lean`, namespace `Uniformity.Density`.

## Node → declaration map

| node | declaration | line | status |
|---|---|---|---|
| CN-05 | `genuineDensity_eq_zero_of_forall_ne` | `DensityAPI.lean:69` | LANDED |
| CN-05 helper | `decidedDensity_eq_zero_of_forall_ne` | `DensityAPI.lean:86` | LANDED |
| CN-06 | `genuineDensity_eq_zero_of_degree_ne` | `DensityAPI.lean:106` | LANDED |
| CN-07 | `proj_eq_iff_dvd` | `DensityAPI.lean:124` | LANDED |
| CN-08 | `decidedAt_of_congr` | `DensityAPI.lean:140` | LANDED |
| CN-09 | `decidedDensity_ge_of_subset` | `DensityAPI.lean:160` | LANDED |
| CN-10 | `genuineDensity_le_of_superset` | `DensityAPI.lean:176` | LANDED |
| CN-11 helper | `gapSeq_antitone_general` | `DensityAPI.lean:201` | LANDED |
| CN-11 | `drainage_of_undecided_comp` | `DensityAPI.lean:213` | LANDED |

Every signature is byte-identical to its spec entry (types, binder order, implicit/explicit
marks, hypothesis names) — verified by diff against `spec/CERTAIN_NODES_2026-08-14.md`.

## Proof routes taken

* **CN-05**: `∀ a, typeOf (monicPoly a) ≠ σ` ⟹ `possibleSet O n σ N = ∅` at every `N` (direct
  unfold of `PossibleAt`) ⟹ `possibleSeq O n σ = fun _ => 0` ⟹ `⨅ N, 0 = 0` (`ciInf_const`).
  Helper: `0 ≤ decidedDensity ≤ genuineDensity = 0` via `decidedDensity_le_genuineDensity` +
  `decidedDensity_nonneg`.
* **CN-06**: reduces to CN-05 — `typeOf (monicPoly a) = σ` would force
  `σ.degree = (typeOf (monicPoly a)).degree = (monicPoly a).natDegree = n` (`typeOf_degree`,
  `monicPoly_natDegree`), contradicting `σ.degree ≠ n`.
* **CN-07**: both directions of `Ideal.Quotient.eq` applied pointwise, exactly generalizing the
  landed one-directional `Drainage.lean:639 congAt_of_proj_eq` (`n = 2`) to an iff at every `n`;
  reuses the already-general `Drainage.lean:635 mem_maximalIdeal_pow_iff_dvd` verbatim (no
  re-proof). Direction convention needed the anticipated `dvd_sub_comm` flip on both branches.
* **CN-08**: one line — `DecidedAt` unfolds to exactly CN-07's RHS after `.symm`.
* **CN-09** / **CN-10**: `Nat.card_le_card_of_injective (Set.inclusion h) (Set.inclusion_injective h)`
  transported through `decidedSeq_le_decidedDensity` / `genuineDensity_le_possibleSeq`, mirroring
  the landed single-class `Gates.lean:325 decidedDensity_ge_of_decided` but for an arbitrary
  subset/superset `T`.
* **CN-11**: verbatim replay of `Drainage.lean:838 drainage_two` with the hard-coded subsequence
  `2 * M` and its bound `undecidedSeq_tendsto_zero` abstracted to an arbitrary `φ` and hypothesis
  `h`. `gapSeq_antitone_general` (helper) is the general-`n` `Antitone (gapSeq O n σ)`, built from
  the already-general `possibleSeq_antitone` / `decidedSeq_mono`; named `_general` per the spec's
  RISK note to avoid touching the landed `n = 2`-only `gapSeq_antitone` (`Drainage.lean:829`) —
  merge left to the orchestrator (strict generalization, safe but a landed-statement change).

No new axioms, no re-proved landed lemmas (all consumed by name: `typeOf_degree`,
`monicPoly_natDegree`, `monicPoly_monic`, `mem_maximalIdeal_pow_iff_dvd`, `decidedSeq_le_decidedDensity`,
`genuineDensity_le_possibleSeq`, `possibleSeq_antitone`, `decidedSeq_mono`, `gapSeq_nonneg`,
`gapSeq_le_undecidedSeq`, `decidedDensity_le_genuineDensity`, `decidedDensity_nonneg`).

## `#print axioms` (every contract + helper)

All nine print exactly the Lean-core footprint:

```
'Uniformity.Density.genuineDensity_eq_zero_of_forall_ne' depends on axioms: [propext, Classical.choice, Quot.sound]
'Uniformity.Density.decidedDensity_eq_zero_of_forall_ne' depends on axioms: [propext, Classical.choice, Quot.sound]
'Uniformity.Density.genuineDensity_eq_zero_of_degree_ne' depends on axioms: [propext, Classical.choice, Quot.sound]
'Uniformity.Density.proj_eq_iff_dvd' depends on axioms: [propext, Classical.choice, Quot.sound]
'Uniformity.Density.decidedAt_of_congr' depends on axioms: [propext, Classical.choice, Quot.sound]
'Uniformity.Density.decidedDensity_ge_of_subset' depends on axioms: [propext, Classical.choice, Quot.sound]
'Uniformity.Density.genuineDensity_le_of_superset' depends on axioms: [propext, Classical.choice, Quot.sound]
'Uniformity.Density.gapSeq_antitone_general' depends on axioms: [propext, Classical.choice, Quot.sound]
'Uniformity.Density.drainage_of_undecided_comp' depends on axioms: [propext, Classical.choice, Quot.sound]
```

## Build status

* `lake env lean Uniformity/Density/DensityAPI.lean` — clean (one benign
  `linter.unusedSectionVars` warning on `genuineDensity_eq_zero_of_forall_ne`'s
  `[Finite (ResidueField O)]`, matching the same false-positive pattern already present 18×
  in the landed `Drainage.lean` — the instance is genuinely consumed through `genuineDensity`'s
  own signature; not touched, not blocking).
* `lake build Uniformity` (root target, whole library) — green (8573/8573 jobs at last check;
  job count includes concurrent sibling batches' new files also present in this shared repo).
* `grep sorry Uniformity/Density/DensityAPI.lean` — no matches.

## Root import

`Uniformity.lean` gained one line: `import Uniformity.Density.DensityAPI` (appended after
`Uniformity.Density.Drainage`). No other file touched.
