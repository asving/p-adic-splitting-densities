# UNIT NOTE — UNIFORMITY-P5: strong Hensel + `typeOf` invariances, 2026-08-14

Infrastructure nodes for the formalization program, started ahead of the blueprint sweep because
their statements were already precise in `spec/HYPOTHESIS_LEDGER.md` (HYP.04, HYP.05, HYP.06) and
every later chapter wants them. Per the Q4 review-boundary policy these are INTERMEDIATE nodes
(consumed-or-discharging); faithfulness paragraphs are still shipped below, one per new statement.

New files, both in `leanfinal` (new files only + root import, no edits to landed files):

* `Uniformity/StrongHensel.lean` — HYP.04.
* `Uniformity/Density/TypeOfInvariance.lean` — HYP.05, HYP.06.

**Informal proofs are written here BEFORE the Lean** (§§1–3); §4 records what landed, §5 the
faithfulness paragraphs, §6 the axiom footprints, §7 what the `n = 3` consumer still lacks.

---

## 1. HYP.04 — STRONG HENSEL (the mathematics, written before the Lean)

### 1.1 The statement the consumer needs

`leancheck/notes/N3_CHECK_2026-08-13.md` §9.4 names the blocking tool exactly:

> `F` monic with `v(F(x₀)) > 2·v(F′(x₀)) = 2w`, `w ≥ 1` ⟹ `∃ r, F(r) = 0 ∧ v(r − x₀) > w`.

Three observations shape the statement actually proved.

1. **Monicity is not used.** The Newton iteration below only ever evaluates `F` and `F′`; nothing
   asks for a leading coefficient. So the theorem is stated for an arbitrary `F : O[X]`. (This is
   what makes it also the *non-monic* Hensel lemma §9.4 asks for in its last bullet: strong Hensel
   and non-monic-Hensel-at-a-simple-residual-root are the same statement, and the general-`F` form
   supplies both.)
2. **`w ≥ 1` is not used either.** At `w = 0` the statement degenerates to ordinary Hensel; the
   proof is uniform in `w`, so no hypothesis `w ≥ 1` appears. The consumer may instantiate at any
   `w`.
3. **The sharp distance is `k − w`, not just `> w`.** The iteration lands `v(r − x₀) ≥ k − w` where
   `k = v(F(x₀))`; since `k > 2w` this implies `v(r − x₀) > w`, the ledger's form. The sharp form
   is what a *recursion* wants (each round must know how many digits it gained), so it is the one
   proved, with the ledger form as a corollary.

Valuations are handled as divisibility by powers of a uniformizer `π` — that is the form the
counting code uses and it avoids `ℕ∞` arithmetic inside the induction. `v(F′(x₀)) = w` is written
as the equivalent `∃ u : Oˣ, F′(x₀) = π ^ w * u` (exactness matters: the iteration needs the
derivative's valuation to be *stable*, not merely bounded). An `addVal` wrapper is supplied for
consumers that prefer the valuation language.

**THEOREM (strong Hensel).** Let `O` be a complete DVR, `π` a uniformizer, `F ∈ O[X]`, `x₀ ∈ O`,
`w k : ℕ` with

* `F′(x₀) = π^w · u` for some unit `u`,
* `π^k ∣ F(x₀)`,
* `2w < k`.

Then there is `r ∈ O` with `F(r) = 0` and `π^(k−w) ∣ r − x₀`; moreover `r` is the *only* root of
`F` in the disc `π^(w+1) ∣ s − x₀`.

### 1.2 The step

Everything rests on the exact second-order Taylor identity, which mathlib supplies over any
commutative ring (`Polynomial.exists_mul_sq_add_linear_part_eq_eval_add`):

    ∀ x δ, ∃ c ∈ O,  F(x + δ) = F(x) + F′(x)·δ + c·δ² .                        (T)

and on the elementary divisibility `(x − y) ∣ F′(x) − F′(y)` (`Polynomial.sub_dvd_eval_sub`).

**STEP LEMMA.** Suppose `j ≥ 1`, `F′(x) = π^w·u` with `u` a unit, and `π^(2w+j) ∣ F(x)`. Write
`F(x) = π^(2w+j)·a` and put

    δ := −π^(w+j)·a·u⁻¹ ,      y := x + δ .

Then (i) `π^(w+j) ∣ y − x`; (ii) `F′(y) = π^w·u′` for some unit `u′`; (iii) `π^(2w+2j) ∣ F(y)`.

*Proof.* (i) is the definition of `δ`.

(iii) By (T), `F(y) = F(x) + F′(x)δ + cδ²`. The first two terms cancel exactly:
`F′(x)·δ = π^w u ·(−π^(w+j) a u⁻¹) = −π^(2w+j) a = −F(x)`, using `u·u⁻¹ = 1`. So `F(y) = c·δ²`, and
`δ² = π^(2w+2j)·(a u⁻¹)²`, giving the divisibility. **This is the quadratic-convergence
bookkeeping**: the level index `j` doubles.

(ii) `δ ∣ F′(y) − F′(x)`, so `F′(y) = π^w u + π^(w+j)·s = π^w·(u + π^j·s)` for some `s ∈ O`. Since
`j ≥ 1`, `π^j s ∈ 𝔪`; and `u + m` with `u` a unit and `m ∈ 𝔪` is a unit in a local ring (if
`u + m ∈ 𝔪` then `u ∈ 𝔪`, contradicting that `u` is a unit). ∎

Note `2j ≥ j + 1`, so the hypothesis `j ≥ 1` is reproduced and the index strictly increases.

### 1.3 The iteration and the limit

Starting from `j₀ := k − 2w ≥ 1` (so `2w + j₀ = k`), iterate the step. Writing `xₙ`, `jₙ` for the
`n`-th state, `jₙ = 2ⁿ·j₀ ≥ n + 1`, and

    π^(w + jₙ) ∣ x_{n+1} − xₙ ,        π^(2w + jₙ) ∣ F(xₙ) .

*Cauchy.* For `m ≤ n`, `π^m ∣ xₙ − x_m`: telescope, and for every `i ≥ m` the increment
`x_{i+1} − x_i` is divisible by `π^(w+j_i)` with `w + j_i ≥ j_i ≥ i + 1 > m`.

*Limit.* `𝔪 = (π)`, so `𝔪^m = (π^m)` and the display above says `x_m ≡ xₙ [SMOD 𝔪^m]`.
`IsPrecomplete` (half of `IsAdicComplete`, which is in the standing bundle) produces `r` with
`π^n ∣ r − xₙ` for every `n`.

*`F(r) = 0`.* `F(r) − F(xₙ)` is divisible by `r − xₙ`, hence by `π^n`; and `π^n ∣ π^(2w+jₙ) ∣ F(xₙ)`
because `2w + jₙ ≥ n`. So `π^n ∣ F(r)` for every `n`, i.e. `F(r) ∈ ⋂ₙ 𝔪^n = 0` by `IsHausdorff`
(the other half of `IsAdicComplete`).

*Distance.* The same telescoping with the constant bound gives `π^(w+j₀) ∣ xₙ − x₀` for every `n`;
take `n = w + j₀` and add `π^n ∣ r − xₙ` to get `π^(w+j₀) ∣ r − x₀`, and `w + j₀ = k − w`. ∎

### 1.4 Uniqueness in the disc `v(s − x₀) > w`

Let `F(r) = F(s) = 0` with `π^(w+1) ∣ r − x₀` and `π^(w+1) ∣ s − x₀`. Then `π^(w+1) ∣ s − r`. As in
step (ii), `F′(r) = π^w·(u + π·t)` for some `t`, and `u + π t` is a unit. Apply (T) at `r` with
`δ = s − r`:

    0 = F(s) = F(r) + F′(r)(s − r) + c(s − r)² = (s − r)·(F′(r) + c(s − r)) ,

and `F′(r) + c(s − r) = π^w·(u + πt + π·c·b)` (writing `s − r = π^(w+1) b`) is `π^w` times a unit,
hence nonzero. `O` is a domain, so `s = r`. ∎

**Why the disc must be `> w` and not `≥ w`.** Two distinct roots at distance exactly `w` from `x₀`
are perfectly possible (`F = X² − π^(2w)` at `x₀ = 0` has both `±π^w`), and there `v(F′(x₀)) = ∞`
so the hypothesis fails anyway; the point is that the uniqueness radius is *open* at `w`.

---

## 2. HYP.05 — `typeOf` SHIFT INVARIANCE (the mathematics, written before the Lean)

**THEOREM.** For `c ∈ O` and any `F ∈ O[X]`, `typeOf (F.comp (X + C c)) = typeOf F`.

The honest route (no root-counting, no residue-field bookkeeping) is: *translation is a ring
automorphism of `O[X]`, and the `(e,f)` data of a monic irreducible is read off an algebra that the
automorphism transports.* In three steps.

**(a) The automorphism.** `τ_c : O[X] ≃ₐ[O] O[X]`, `p ↦ p(X + c)`, is mathlib's
`Polynomial.algEquivAevalXAddC c`, with inverse `τ_{−c}`. It preserves degree
(`natDegree_taylor`), preserves monicity, and — being a ring isomorphism — preserves
irreducibility and products.

**(b) Factorizations transport.** If `F = ∏ gᵢ` is *the* monic factorization of a monic `F`, then
`∏ τ_c(gᵢ) = τ_c(F)` is a factorization of `τ_c F` into monic irreducibles, hence *the* one, by the
uniqueness already proved in `TypeOf.lean` (`monicFactorization_unique`). So

    monicFactors (τ_c F) = (monicFactors F).map τ_c .

**(c) `efPair` is invariant.** For monic irreducible `g` of degree `d`, `τ_c g` has the same degree,
so it is enough that `inertiaDegOf (τ_c g) = inertiaDegOf g`, i.e. that the two sets `normValues`
coincide. They do, because the `O`-algebras

    A = O[X]/(g)   and   B = O[X]/(τ_c g)

are isomorphic: `X ↦ X − c` induces `B ≃ₐ[O] A` (well-defined because `g(X)` evaluated at
`root(τ_c g) − c` is `(τ_c g)(root) = 0`), with inverse induced by `X ↦ X + c`. Under an
`O`-algebra isomorphism the norm is preserved (`Algebra.norm_eq_of_algEquiv`), so the two sets of
norm-valuations are literally equal, and the `sSup`-of-common-divisors definition returns the same
number. ∎

This is a *plain* invariance: no side condition, no hypothesis on `c` beyond `c ∈ O`, no
separability, no residue characteristic.

---

## 3. HYP.06 — `typeOf` SCALE RELATION (the mathematics, written before the Lean)

### 3.1 The true statement (transcribed, not guessed)

The ledger row says only "Under `X = πᵐY`, the normalized polynomial has the splitting-type
relation asserted by the recursion", and points at `N3_CHECK` §9.4, whose table row reads

> single side of INTEGRAL slope `m` | rescale `X = π^m Y`, recurse on the residual cubic | needs
> typeOf SCALE-invariance

So the assertion the recursion consumes is: **the rescaled polynomial has the same type**. It is
*not* a plain invariance in the sense of §2, because the substitution is only legal under a
side condition — `F(π^m Y)` must be `π^(mn)` times something *integral and monic*, which is exactly
the Newton-polygon condition "every root has valuation ≥ m". The clean way to state it without
mentioning polygons is to run the substitution BACKWARDS, where no side condition is needed:

**DEFINITION (unscale).** For `h ∈ O[Y]` monic of degree `d` and `m : ℕ`, put

    (unscale π m h)(X) := π^(m d) · h(X / π^m) = ∑_{i ≤ d} h_i · π^(m(d−i)) · X^i .

All exponents `m(d−i)` are `≥ 0`, so `unscale π m h ∈ O[X]` always, and it is monic of degree `d`.
The forward direction is its partial inverse: if `F` is monic of degree `n` and
`F(π^m Y) = π^(mn)·G(Y)` with `G ∈ O[Y]` monic, then `F = unscale π m G`.

**THEOREM (scale relation).** For `h ∈ O[Y]` monic and `m : ℕ`,
`typeOf (unscale π m h) = typeOf h`. Equivalently, in the forward form the recursion uses: if `F`
is monic of degree `n` and `G` is monic with `F(π^m Y) = π^(mn) G(Y)`, then
`typeOf F = typeOf G`.

### 3.2 Why it is true, and why it is NOT the same proof as §2

Write `d = deg h`, `A = O[X]/(unscale π m h)`, `B = O[Y]/(h)`, and `x`, `y` for the two roots. The
substitution sends `x ↦ π^m y`, so there is an `O`-algebra map

    φ : A → B ,  x ↦ π^m y      (well defined: (unscale π m h)(π^m Y) = π^(m d)·h(Y)) .

But `φ` is **not** an isomorphism: `A = O[x] = O[π^m y] ⊆ O[y] = B` is a strictly smaller order in
the same field (example: `h = Y² − πu`, `m = 1`, `unscale = X² − π³u`; a rank computation shows the
two `O`-algebras are not isomorphic). So `Algebra.norm_eq_of_algEquiv` does not apply and §2's
argument does not transfer. Two facts replace it.

**FACT 1 (norms agree along `φ`).** For every `z ∈ A`, `N_{A/O}(z) = N_{B/O}(φ z)`.

*Proof, matrix form, elementary.* `A` has `O`-basis `(x^i)_{i<d}` and `B` has `O`-basis
`(y^i)_{i<d}` (`AdjoinRoot.powerBasis'`, both polynomials monic of degree `d`). Let `M` be the
matrix of multiplication by `z` in the first basis and `N` that of multiplication by `φ z` in the
second, so `z·x^j = ∑ᵢ M_{ij} x^i` and `(φ z)·y^j = ∑ᵢ N_{ij} y^i`. Apply `φ` to the first identity
and use `φ(x^i) = π^(m i) y^i`:

    (φ z)·π^(m j) y^j = ∑ᵢ M_{ij} π^(m i) y^i ,   while   π^(m j)·(φ z)·y^j = ∑ᵢ π^(m j) N_{ij} y^i .

Comparing coordinates in the basis `(y^i)`: `π^(m j)·N_{ij} = π^(m i)·M_{ij}` for all `i, j`, i.e.
`D·N = M·D` with `D = diag(π^(m i))`. Taking determinants, `det D · det N = det M · det D`, and
`det D = π^(m d(d−1)/2) ≠ 0`; cancel in the domain `O`. ∎

**FACT 2 (the two gcds agree).** `inertiaDegOf (unscale π m h) = inertiaDegOf h`.

*Proof.* By Fact 1 and injectivity of `φ` — `φ` is injective because `π^(m(d−1))·B ⊆ φ(A)` and
`B` is torsion-free, or directly from the basis relation — `normValues(unscale π m h) ⊆
normValues(h)`. Hence every common divisor of `normValues(h)` is a common divisor of
`normValues(unscale π m h)`: `normDivisors(h) ⊆ normDivisors(unscale π m h)`, so
`inertiaDegOf h ≤ inertiaDegOf (unscale π m h)`.

For the other direction, let `k` be a common divisor of `normValues(unscale π m h)` and let
`0 ≠ v ∈ B`. Since `π^(m(d−1))·y^i = π^(m(d−1−i))·(π^(m i) y^i) ∈ φ(A)` for every `i < d`, we get
`π^(m(d−1))·v = φ(z)` for some `z ∈ A`, `z ≠ 0`. Then

    v(N_B(π^(m(d−1)) v)) = v(N_B(π^(m(d−1)))) + v(N_B(v)) = m(d−1)d + v(N_B(v)) ,

and by Fact 1 the left-hand side is `v(N_A(z)) ∈ normValues(unscale π m h)`, while
`m(d−1)d = v(N_A(π^(m(d−1))))` is also in `normValues(unscale π m h)` (or is `0`). So `k` divides
both, hence `k ∣ v(N_B(v))`. Thus `normDivisors(unscale π m h) ⊆ normDivisors(h)` and the two
`sSup`s agree. ∎

**FACT 3 (factors transport).** `unscale π m` is multiplicative on monic polynomials
(`unscale (h₁h₂) = unscale h₁ · unscale h₂`, because degrees add), and for monic `h`,
`Irreducible (unscale π m h) ↔ Irreducible h`. The irreducibility transfer goes through the
fraction field `K`: `Monic.irreducible_iff_irreducible_map_fraction_map` (valid because a DVR is
integrally closed) reduces both sides to irreducibility over `K`, and over `K` the map
`h ↦ unscale π m h` is "multiply by the unit `π^(md)`, then apply the `K`-algebra automorphism
`Y ↦ X/π^m`", both of which preserve irreducibility.

Combining Facts 1–3 exactly as in §2(b)–(c) gives `typeOf (unscale π m h) = typeOf h`. ∎

---

## 4. WHAT LANDED

Two new files in `leanfinal` (new files only; `Uniformity.lean` gained two import lines) and one
in `leancheck`. **Zero `sorry`s, zero `axiom` commands, every footprint the Lean core triple.**

### 4.1 `leanfinal/Uniformity/StrongHensel.lean` (HYP.04)

| name | statement (abbreviated) | rung |
|---|---|---|
| `Hensel.isUnit_add_of_mem_maximalIdeal` | unit + `𝔪` is a unit | any local ring |
| `Hensel.newton_step` | `F'(x)=π^w u`, `π^(2w+j) ∣ F(x)`, `j ≥ 1` ⟹ ∃ `y`, `π^(w+j) ∣ y−x`, `F'(y)=π^w u'`, `π^(2w+2j) ∣ F(y)` | general DVR (no completeness) |
| `Hensel.NState` (+ `next`, `iter`, `iter_lvl`, `lt_iter_lvl`, `iter_dist`) | the iteration and its Cauchy estimate | general DVR |
| `Hensel.maximalIdeal_pow_eq_span`, `smodEq_of_dvd`, `dvd_of_smodEq`, `exists_unit_mul_pow_of_addVal_eq`, `pow_dvd_iff_le_addVal` | the divisibility ↔ `addVal` ↔ `SModEq` dictionary | general DVR |
| **`Hensel.root_unique_of_strongHensel`** | two roots in `π^(w+1) ∣ · − x₀` coincide | general DVR (**no completeness**) |
| **`Hensel.exists_root_of_strongHensel`** | `F'(x₀)=π^w u`, `π^k ∣ F(x₀)`, `2w < k` ⟹ `∃ r`, `F(r)=0`, `π^(k−w) ∣ r − x₀` | complete DVR |
| **`Hensel.strongHensel`** | existence at distance `k − w` **and** uniqueness in radius `> w` | complete DVR |
| **`Hensel.strongHensel_addVal`** | the same in the ledger's wording: `v(F(x₀)) > 2·v(F'(x₀)) = 2w` ⟹ `∃! r` at `v(r − x₀) > w` | complete DVR |
| `Hensel.gatePoly`, `gate_der`, `gate_val`, `gate_eval_ne_zero`, **`gate_fires`** | non-vacuity: `(X − π^(2w+1))(X + π^w)` at `x₀ = 0`, every `w`; the theorem returns exactly `π^(2w+1)` | complete DVR |

Rung reached: **the strongest one on the ladder** (general complete DVR bundle, not `ℤ_[p]`), and
strictly stronger than the ledger's HYP.04 in three ways — no monicity, no `w ≥ 1`, sharp distance
`k − w` rather than `> w` — plus uniqueness, which HYP.04 did not ask for.

### 4.2 `leanfinal/Uniformity/Density/TypeOfInvariance.lean` (HYP.05, HYP.06)

| name | statement | rung |
|---|---|---|
| `normValues_of_algEquiv`, `normDivisors_of_algEquiv`, `inertiaDegOf_of_algEquiv` | isomorphic root algebras ⟹ equal `inertiaDegOf` | general DVR |
| `shift`, `shift_apply/_monic/_natDegree/_irreducible`, `aeval_shift`, `shiftRootEquiv` | translation as an `O`-algebra automorphism, and the induced root-algebra isomorphism | any comm ring |
| `efPair_shift`, `monicFactors_shift` | `efPair` and the factorization transport | general DVR |
| **`typeOf_shift`** | `typeOf (F.comp (X + C c)) = typeOf F`, `F` monic, any `c` | general DVR, **unconditional** |
| `scaleHom`, `scaleHom_root`, `rootBasis`, `scaleRootBasis`, `scaleHom_basis` | the comparison map `O[X]/(scaleRoots h s) → O[X]/(h)` and the two power bases | any comm ring |
| **`repr_scaleHom`** | the comparison map is `diag(s^i)` in those bases | any comm ring |
| `scaleHom_injective` | injectivity, `s ≠ 0` | domain |
| **`norm_scaleHom`** | `N(φ z) = N(z)` — FACT 1 | domain |
| `algebraMap_mul_ne_zero`, `exists_scaleHom_eq`, `norm_algebraMap_rootBasis` | `π^(m(d−1))·B ⊆ φ(A)` and the scalar norm | domain / DVR |
| `normValues_scaleRoots_subset`, **`normDivisors_scaleRoots_subset`**, `inertiaDegOf_scaleRoots`, `efPair_scaleRoots` | the two gcds agree — FACT 2 | general DVR |
| `scaleRootsMulEquiv`, **`irreducible_scaleRoots_iff`** | irreducibility transfer — FACT 3 | general DVR (via Gauss over `Frac O`) |
| `prod_map_scaleRoots`, `monicFactors_scaleRoots` | factorizations transport | general DVR |
| **`typeOf_scaleRoots`** | `typeOf (scaleRoots F (π^m)) = typeOf F`, `F` monic | general DVR, **unconditional** |
| `eq_scaleRoots_of_comp` | `F(π^m Y) = π^(m·n)·G(Y)` ⟺ `F = scaleRoots G (π^m)` (coefficientwise) | general DVR |
| **`typeOf_scale`** | the recursion's phrasing: `F.comp (C (π^m) * X) = C (π^(m·deg F)) * G`, `G` monic, `deg G = deg F` ⟹ `typeOf F = typeOf G` | general DVR |
| `scaleRoots_X_sq_sub_C`, **`typeOf_scale_gate`** | non-vacuity: `typeOf (X² − π³) = typeOf (X² − π)` | general DVR |

Rung reached: **the strongest one on the ladder** for both. Neither degenerates: `typeOf_shift`
has no side condition at all, and `typeOf_scaleRoots` has none either once the substitution is run
in the always-integral direction.

### 4.3 `leancheck/UniformityCheck/P5Consume.lean` (target 4, consumption wiring)

Four one-line consumers, one per row of the `N3_CHECK` §9.4 table:
`typeOf_ram3_of_eisenstein_shift` (recentred Eisenstein certificate — the row that was blocked on
shift invariance), `typeOf_cubic_scale` (depth reduction — blocked on scale invariance),
`exists_cubic_root_strong` and `exists_cubic_root_strong_addVal` (the two/three-sides row —
blocked on strong Hensel). All the mathematics stays in `leanfinal`; these only specialise to
`monicPoly a` at `n = 3`.

## 5. FAITHFULNESS PARAGRAPHS

**`Hensel.strongHensel` / `strongHensel_addVal`.** `v(a) = w` is written `∃ u : Oˣ, a = π^w·u`,
which for a DVR is *equivalent* to `addVal O a = w` (`exists_unit_mul_pow_of_addVal_eq` proves one
direction; `addVal_def'` the other), and `v(a) ≥ k` is written `π^k ∣ a`, equivalent by
`addVal_le_iff_dvd` (`pow_dvd_iff_le_addVal`). So the divisibility form is a faithful transcription
of the valuation statement, and `strongHensel_addVal` is the valuation statement itself, with the
`F(x₀) = 0` corner (`v = ⊤`) handled by returning `r = x₀`. The conclusion's `π^(k−w) ∣ r − x₀` is
*stronger* than HYP.04's `v(r − x₀) > w` whenever `k > 2w + 1`; both are stated. The uniqueness
clause quantifies over the OPEN disc `v(s − x₀) > w`, not the closed one — the closed disc genuinely
contains several roots in general (`X² − π^(2w)` at `x₀ = 0`), so this is not a weakening of a true
statement but the true statement. `F` is arbitrary, not monic: monicity is nowhere used, and
dropping it makes the theorem *also* the non-monic Hensel lemma that `N3_CHECK` §9.4's last bullet
identifies with strong Hensel.

**`Hensel.gate_fires`.** A non-vacuity certificate, not a mathematical claim about the target
problem: it exhibits, for every `w` and over every base in the bundle, a polynomial and a point
satisfying the hypotheses with `F(x₀) ≠ 0` and `v(F'(x₀)) = w` **exactly**, and checks that the
theorem's output is the intended root `π^(2w+1)` rather than the other root `−π^w`. Read it as the
answer to "could the hypotheses be unsatisfiable for `w ≥ 1`?" — no.

**`typeOf_shift`.** The statement is about `F.comp (X + C c)`, i.e. `F(X + c)`, with `c` ranging
over all of `O` (integral translations only — a non-integral translation does not preserve `O[X]`
and is not claimed). `F` is required monic because `typeOf` is junk (`⟨0⟩`) off the monic locus,
by design (`TypeOf.lean`); the hypothesis is therefore not a restriction but a well-definedness
condition. The proof does **not** go through roots or the residue field: it transports the monic
factorization along a ring automorphism and matches `(e,f)` through an isomorphism of the two root
algebras. Consequently it inherits `typeOf`'s own trust boundary (HYP.01, the identification of the
gcd-defined `(e,f)` with the classical one) and adds nothing to it: whatever `typeOf` means, this
theorem says the translate has the same value.

**`typeOf_scaleRoots`.** `Polynomial.scaleRoots h s` is mathlib's, with
`(scaleRoots h s).coeff i = h.coeff i · s^(deg h − i)`; at `s = π^m` this is exactly
`π^(m·deg h)·h(X/π^m)`, the "unscale" of §3.1. The theorem has **no side condition** because the
unscale direction never leaves `O[X]`. The reader should check that this is the substitution the
recursion wants and not its inverse: `eq_scaleRoots_of_comp` is the bridge, and it is a pure
coefficient computation — `F.comp (C (π^m) * X) = C (π^(m·n)) * G` iff `F = scaleRoots G (π^m)`,
given `deg G = deg F`. The degree hypothesis in `typeOf_scale` is not automatic *from the stated
equation alone* in Lean (it is a consequence of monicity plus `π^m ≠ 0`, which we did not
formalise), so it is carried explicitly; a consumer supplying it from `monicPoly_natDegree` pays
nothing (`typeOf_cubic_scale` does exactly that).

**`norm_scaleHom`.** The load-bearing new mathematics of §3. The claim is that the `O`-algebra norm
is unchanged along `φ : O[X]/(scaleRoots h s) → O[X]/(h)`, a map that is **not** an isomorphism —
the source is the smaller order `O[π^m y]`. It is proved by the determinant identity `N·D = D·M`
(`D = diag(s^i)`), which is a statement about the multiplication matrices in the two *power* bases
and uses nothing about `h` beyond monicity. The reader should check that the two bases really are
the power bases (`rootBasis_apply`, `scaleRootBasis_apply` state it), since the identity is false
for arbitrary bases.

**`normDivisors_scaleRoots_subset`.** This is where the *gcd* form of `inertiaDegOf` earns its keep:
the two `normValues` sets are genuinely different (the bigger order has more norms), and only their
gcds agree. The proof needs `0 < deg h`, which is supplied at every use site because the factors
`typeOf` reads are irreducible.

**`typeOf_scale_gate`.** Non-vacuity certificate. `X² − π³` and `X² − π` are not equal, are not
translates of one another, and are exactly the pair on which the pre-2026-08-13 `sInf` form of
`inertiaDegOf` disagreed with the truth (`TypeOf.lean` module docstring). The gate checks that the
current gcd form assigns them the same type, as the mathematics requires.

**`P5Consume` (leancheck).** Each consumer is definitionally the general theorem instantiated at
`monicPoly a`; no new mathematical content and no new hypotheses beyond `monicPoly_monic` and
`monicPoly_natDegree`. They exist so that the recursion's call sites do not have to re-derive
monicity and degree each time.

## 6. AXIOM FOOTPRINTS

`lake env lean` on each file prints (`AxCheck` blocks at the foot of each file):

```
'Uniformity.Hensel.newton_step'                    : [propext, Classical.choice, Quot.sound]
'Uniformity.Hensel.exists_root_of_strongHensel'    : [propext, Classical.choice, Quot.sound]
'Uniformity.Hensel.root_unique_of_strongHensel'    : [propext, Classical.choice, Quot.sound]
'Uniformity.Hensel.strongHensel'                   : [propext, Classical.choice, Quot.sound]
'Uniformity.Hensel.strongHensel_addVal'            : [propext, Classical.choice, Quot.sound]
'Uniformity.Hensel.gate_fires'                     : [propext, Classical.choice, Quot.sound]

'Uniformity.Density.typeOf_shift'                  : [propext, Classical.choice, Quot.sound]
'Uniformity.Density.norm_scaleHom'                 : [propext, Classical.choice, Quot.sound]
'Uniformity.Density.typeOf_scaleRoots'             : [propext, Classical.choice, Quot.sound]
'Uniformity.Density.typeOf_scale'                  : [propext, Classical.choice, Quot.sound]
'Uniformity.Density.typeOf_scale_gate'             : [propext, Classical.choice, Quot.sound]

'UniformityCheck.typeOf_ram3_of_eisenstein_shift'  : [propext, Classical.choice, Quot.sound]
'UniformityCheck.typeOf_cubic_scale'               : [propext, Classical.choice, Quot.sound]
'UniformityCheck.exists_cubic_root_strong'         : [propext, Classical.choice, Quot.sound]
'UniformityCheck.exists_cubic_root_strong_addVal'  : [propext, Classical.choice, Quot.sound]
```

`grep -rn sorry` over both new `leanfinal` files and the new `leancheck` file: **0**.
`lake build` from `leanfinal/`: green, 8 570 jobs.

## 7. WHAT THE `n = 3` CONSUMER STILL LACKS

P5 removes the three *tool* blockages named in `N3_CHECK` §9.4. It does **not** finish triple-root
drainage. Sharply, what remains:

1. **The Newton-polygon case analysis itself.** §9.4's table is a case split on the polygon of the
   recentred cubic `(B₀, B₁, B₂, 1)`. Nothing in the repo reads that polygon: there is no lemma
   taking `(B₀, B₁, B₂)` to "one side of integral slope `m`" / "one side of non-integral slope" /
   "two or three sides", and in particular no lemma that *produces* the point `x₀` at which
   `v(F(x₀)) > 2 v(F'(x₀))` from the two-or-three-sides hypothesis. Strong Hensel is the tool for
   that row; the hypothesis-manufacturing lemma is missing.
2. **HYP.07, the non-integral-slope `CertRam3`** — still absent, as §12 recorded.
3. **Level bookkeeping across the extraction.** `typeOf_scale` is a statement about polynomials.
   The recursion runs on level-`N` *classes*, and the extraction `X = π^m Y` changes the level
   (roughly `N ↦ N − 3m`). The correspondence of classes — the statement that would let
   `undecidedSeq` at level `N` be bounded by `undecidedSeq` at the reduced level — is not proved.
   This is the "each round consumes three digits of level and pins the centre" clause of §9.4's
   recursion `u(N) ≤ q^(−N/2) + q^(−5)·u(N−3)`, i.e. **HYP.08**, and it remains underived.
4. **HYP.09, the drainage conclusion**, therefore remains open; `drainage_three_of_triple` still has
   its single named hypothesis.
5. **HYP.10** (exactly five cubic types) is untouched by this unit.

The honest summary: after P5 the triple-root branch is blocked on *bookkeeping and case analysis*,
not on missing algebra. That is a different kind of gap from the one §9.4 diagnosed, and a smaller
one.
