# NODE C.128 (`lawBS2_pair_or_triple`, re-signed A-C.5) — PARTIAL LANDING, capstone BLOCKED

**Supersedes `leanfinal/notes/RESCHEDULE_C128_2026-08-16.md`** on its blocking fact only: that
record's `RESCHEDULE: needs TouchCert` is CURED — NODE C.74's `def TouchCert` is landed
(`leanfinal/Uniformity/ChapC/C74.lean`, commit `3d18396a`, `#print axioms` Lean core). C.128 can
now be *stated* in the landed corpus. Its three named proof obstacles are partly discharged and
partly still open; this record replaces the RESCHEDULE's obstacle list with the current one.

Landed file: `leanfinal/Uniformity/ChapC/C128.lean` (twenty public declarations plus six
private helpers, sorry-free, `#print axioms` = Lean core on every public one; `lake build Uniformity.ChapC.C128` green, 8627 jobs,
zero warnings on the module). Proof source: `lean/notes/openmath/LAWBS2_PROOF_2026-08-16.md`.

## What is LANDED (and what each piece was for)

| piece | declaration(s) | note reference |
|---|---|---|
| the unit bridge | `isUnit_natCast_iff` | §0/§5 ("an integer is a unit of `O` iff `p ∤ n`") |
| the trinomial chain (LEMMA 4) | `triKey`, `triKey_monic`, `triKey_natDegree`, `triKey_degree`, `devQ_C_mul`, `devQ_triKey_eq_zero_of_natDegree_lt` (4a), `devQ_triKey_X_pow_even` (4b), `devQ_triKey_X_pow_odd` (4c) | §1 LEMMA 4 |
| the degree-2 wrap dictionary | `X_sq_eq_triKey_sub`, `X_cube_eq_triKey`, `X_four_eq_triKey` | §4's three displays |
| the trinomial shape of `Φ₂` (the missing `hcomp`) | `stageLiftO_of_f1`, `KeyFrame.slotIdx_mul_left`, `entryCoef`, `onSide_of_slot_one`, `composedKey_trinomial`, `key_eq_triKey`, `stageCoord_ne_zero_of_f1`, `isUnit_entryCoef` | §0 (G1)–(G3) at the Lean carrier |

Two of these answer the RESCHEDULE record's obstacles outright:

* **Obstacle 1 ("No `hcomp`. The trinomial shape must be DERIVED", judged "the largest single
  piece, deserves its own node") is DISCHARGED.** `composedKey_trinomial` derives, from C.43's
  body through C.14a's `stageLiftO`, at `e₁ = 2`, `f₁ = 1`, `f₂ = 2`, `e₂ = 1`, `hslot`:

  ```
  composedKey T = F.key ^ 2
      + C (entryCoef T 1 * π ^ ((T.u₂ - F.h) / 2)) * X * F.key
      + C (entryCoef T 0 * π ^ T.u₂)
  ```

  with `onSide_of_slot_one` supplying (G3) — `u₂ = e₁·((u₂−h)/2) + 1·h` (the `ℕ`-division is
  exact because `hslot` forces `h ≡ u₂ (mod 2)` and `T.hfloor` forces `2h < u₂`) and
  `e₁·u₂ = f₂·u₂` — and `isUnit_entryCoef` supplying (G2)'s `ĉ₂, ĉ₀ ∈ O^×` (via C.14a's
  `sum_stageCoord` at `f₁ = 1`, C.19's `stageLetter_ne_zero`, and `resLift_spec`). So the whole
  C.127-style `hcomp` + `hu₂` + `hv₀` + `hc₂` + `hc₀` package is available at C.128's OWN signed
  binders, with `i₂ = 1`, `v₂ = (u₂−h)/2`, `v₀ = u₂`. No new node is needed for it.
* **Obstacle 4 (the `ringChar ∣ n ↔ ¬ IsUnit (n : O)` bridge) is DISCHARGED**
  (`isUnit_natCast_iff`).

Obstacle 2's reusable/NEW split is confirmed and half-executed: C.129's `binomKey_mod_monomial`
is indeed unusable here (it is stated at the binomial key `X^D − C w`; `F.key` is a general
one-sided monic quadratic), and the replacement — the degree-2 wrap dictionary — is landed,
generic in the ring, at the same `triKey` carrier the chain uses, with `key_eq_triKey` the
bridge that makes `Φ′` an instance of it.

## What is BLOCKED, with the boundary named

**The capstone `lawBS2_pair_or_triple` is NOT landed and NOT axiomatized.** The boundary is the
note's §2 closing display: everything upstream of

```
Δ_j = red( R_j( T̂ ) )      for every j < μ₂
```

is landed (here or in C.127); everything downstream is open. Concretely, two pieces remain, and
each is a node's worth of work:

**(B1) the wrap-seed sum (§2) + the weight calculus (§3).** The multinomial expansion
`K^{μ₂} = Σ_{k+l+r=μ₂} C(μ₂;k,l,r)·P^l c₀^r·Y^{2k+l}`, its development against the (landed) wrap
dictionary into the seed sum `T̂`, and LEMMA 3(3a)–(3d): the weight `wt(λx^aY^b) = 2v(λ) + ah +
bu₂`, `K` weight-homogeneous of weight `E₂`, monic division respecting the grading
(`wt(R_j(V)) ≥ wt(V) − jE₂`), `red` never lowering weight, and `pin = wt` on reduced slot
vectors. The consequence the census needs is "**wrap depth `j` sits exactly `jδ` above `μ₂E₂`**",
hence "the Θ-layer is depth-1 only". Nothing landed carries a grading of this kind; C.127's
`shadowDev_of_ingrid` explicitly does not apply (the pure power is OVER-grid — that is the
content of the law).

**(B2) the top-coordinate census (§4) + the heights (§5).** The three-branch survival argument
(`r ≥ 1` impossible; `r = 0` forces `l ∈ {2,3,4}`), the exact dict
`{(0,1): b₂ĉ₂²π^{2v₂}, (1,0): Mĉ₂³π^{3v₂}, (0,0): b₄ĉ₂⁴π^{4v₂} − κ₁b₃ĉ₂³π^{3v₂}}`, the three
slot heights, and the congruence swap `p ∣ b₂ ⇒ M ≡ b₃ (mod p)`. Its chain-value input is landed
(LEMMA 4 above: the PAIR branch's `Y − (μ₂−1)P` is exactly `devQ_triKey_X_pow_odd` at
`n = μ₂−1`, which is where the mixed digit `M = b₃ − (μ₂−1)b₂` comes from); its grading input is
(B1).

**Recommended scheduling.** (B1) as its own node (frame-generic: a weight/grading calculus for
`devQ` at a weight-homogeneous key, useful beyond B-S2 — LAW E-W's §3 wants the same object);
(B2) then becomes the C.128 capstone proper, consuming (B1) + this file's Parts 1–3.

## Fences that must NOT be widened

`he₁ : F.e₁ = 2` and `hf₂ : T.f₂ = 2` are load-bearing. Without them the signed statement is
REFUTED on two constructed PARI-valid axes: `e₁ = 4` (`Φ′ = x⁴ − 5`/ℤ₅ — the top band is EMPTY,
`TouchCert` false while the RHS holds at `μ₂ ∈ {3,4}`) and `e₁ = 3` (`Φ′ = x³ − 13`/ℤ₁₃, `ĉ₀ = 2`
— the census is a single unit-DEPENDENT merged digit, dead, while `13 ∤ b₂ = 3`). Machine record:
`verification/openmath/bs2_lawbs2_cert.py`, BS2-STUBGAP, GREEN 666/0. The `f₂ ≥ 3, D′ = 2` slice
is MEASURED-OPEN (note §7.3) and needs a new certification leg before any widening.

Every landed statement in `C128.lean` is stated at the pinned geography for this reason;
`triKey`, the chain lemmas and the wrap dictionary are ring-generic, but they say nothing about
`Φ₂` on their own — `composedKey_trinomial` and `key_eq_triKey` are the only bridges, and both
carry `he₁`/`hf₁`(/`hf₂`, `he₂`, `hslot`).

---

*Recorded by Chapter-C wave 13 agent 2, 2026-08-16, after landing C.74's `def` and C.128's
Parts 1–3. Commits: `3d18396a` (C.74), `e57dfc08`, `a7d19359`, `f393ce5e`, `a7ddcde6`,
`45638dc2` (C.128).*
