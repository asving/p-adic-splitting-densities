# CN Batch A — 2026-08-14

Fleet: sonnet, formalization batch A (`spec/CERTAIN_NODES_2026-08-14.md`). Contracts: CN-01,
CN-02, CN-03, CN-23, CN-14, CN-18 (the edge chain CN-02 → CN-03 → {CN-14, CN-18}, plus the two
independent nodes CN-01 and CN-23). All six LANDED, exactly as specified — no signature was
weakened, renamed, or given extra hypotheses.

## Node → declaration → file map

| node | declaration | file |
|---|---|---|
| CN-01 | `Uniformity.Density.exists_monicPoly_eq` (+ helper `monicPoly_coeff_lt`) | `Uniformity/Density/Transport.lean` |
| CN-02 | `Uniformity.Density.monicFactors_mul` | `Uniformity/Density/TypeOfAlgebra.lean` |
| CN-03 | `Uniformity.Density.typeOf_mul` | `Uniformity/Density/TypeOfAlgebra.lean` |
| CN-23 | `Uniformity.FactorizationType.degree_mk_add` | `Uniformity/Density/TypeOfAlgebra.lean` |
| CN-14 | `Uniformity.Density.typeOf_prod_X_sub_C` | `Uniformity/Density/TypeOfAlgebra.lean` |
| CN-18 | `Uniformity.Density.typeOf_of_residual_coprime` | `Uniformity/Density/TypeOfAlgebra.lean` |

Both files import cleanly from landed API only (`Density.LocalData`, `Density.TypeOf`,
`Uniformity.HenselFactorization`) and are wired into the root `Uniformity.lean`.

## `#print axioms` output (every contract declaration)

```
'Uniformity.Density.monicPoly_coeff_lt' depends on axioms: [propext, Classical.choice, Quot.sound]
'Uniformity.Density.exists_monicPoly_eq' depends on axioms: [propext, Classical.choice, Quot.sound]
'Uniformity.Density.monicFactors_mul' depends on axioms: [propext, Classical.choice, Quot.sound]
'Uniformity.Density.typeOf_mul' depends on axioms: [propext, Classical.choice, Quot.sound]
'Uniformity.FactorizationType.degree_mk_add' depends on axioms: [propext, Quot.sound]
'Uniformity.Density.typeOf_prod_X_sub_C' depends on axioms: [propext, Classical.choice, Quot.sound]
'Uniformity.Density.typeOf_of_residual_coprime' depends on axioms: [propext, Classical.choice, Quot.sound]
```

All within the allowed Lean-core triple `{propext, Classical.choice, Quot.sound}`; no new axioms
anywhere in the batch.

## Notes on the proofs

* **CN-01** (`Transport.lean`): witness `a i := f.coeff i`; the below-degree coefficients match
  by direct computation on `monicPoly`'s definition (`Finset.sum_eq_single`), the degree-`n`
  coefficient matches because both sides are monic of the same `natDegree`
  (`Monic.coeff_natDegree`), and above-degree coefficients both vanish
  (`coeff_eq_zero_of_natDegree_lt`).
* **CN-02/CN-03** (`TypeOfAlgebra.lean`): exactly as specified in SOURCE — `monicFactors_eq`
  applied to the multiset union, `Multiset.prod_add`; `typeOf_mul` is one `rw` chain through
  `typeOf_data` and `Multiset.map_add`. No coprimality or separability hypothesis added to
  `monicFactors_mul`, per the node's explicit RISK warning.
* **CN-23**: pure `Multiset.map_add` / `Multiset.sum_add` unfold of `FactorizationType.degree`;
  no `Add` instance introduced on `FactorizationType` (REJECTED R14 respected).
* **CN-14**: induction on `n` via `Fin.prod_univ_castSucc` (peel the last linear factor); the
  `n = 0` base case is the empty product `1`, whose `IsMonicFactorization` witness is the empty
  multiset `0` (`Multiset.notMem_zero`, `Multiset.prod_zero`); the successor step composes CN-03
  with the landed `typeOf_of_natDegree_one` and `Multiset.replicate_add`/`replicate_one` for the
  multiset bookkeeping.
* **CN-18**: a direct weld of the landed `Hensel.exists_monic_factorization_dvr` with CN-03's
  conclusion via `hfeq ▸ typeOf_mul hg hh`; no new mathematics, exactly as the node's RISK line
  anticipated ("None mathematically. Bookkeeping only").

## Build status

* `leanfinal`: `lake build` green (8573 jobs), zero `sorry` in this batch's declarations (or
  anywhere in the tree at time of landing).
* `leancheck`: rebuilt against the updated `leanfinal` and green (transitive dependency only —
  this batch does not touch `UniformityCheck`).

## Blocked

None. All six contracted signatures elaborated and were proved exactly as stated.

## Commits

* `CN-A: CN-01 — exists_monicPoly_eq, monicPoly is onto the monic degree-n polynomials`
* `CN-A: CN-02/CN-03/CN-23/CN-14/CN-18 — the typeOf peel law, degree bookkeeping, and the split/order-0 leaves`
