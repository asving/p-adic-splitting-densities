# BLOCKED record — NODE C.34, `exists_dv_residual_dissection`

**Unit:** chapter-C fleet, NODE C.34 (DAG frontier after C.33's partial).
**Landing file:** `leanfinal/Uniformity/ChapC/C34.lean` (PARTIAL — see "What survives").
**Signed statement:** `leanspec/Leanspec/ChapC.lean`, A-C.1 §5 block (the `(λ, r)`-block
clause) — **byte-frozen, untouched by this unit.** Statement transcribed byte-exact and
compiling (diff-verified against the spec): skeleton commit `cdf53b30`; the frontier-split
form quoted below: commit `e93bb724`.

This record follows the `C33_BLOCKED_2026-08-17.md` convention: it documents that the
node's signed THEOREM is **not reachable from the landed engine**, quotes the failing
goals verbatim, and specifies the missing mechanisms. It is evidence, not a node.

## The mandatory vacuity audit came FIRST — verdict: no A-C.7 instance

Per the unit charge, all hypotheses were audited against the five-instance A-C.7 registry
(C.111 `hnode`, C.94 self-loop, C.113 `hne`, C.118 `Visible₂` clause 1, C.118 clause 2)
before any proving. Verdict: **no instance**; the full per-hypothesis audit is the
landing file's header. Two audit claims were strong enough to machine-check and are
LANDED as theorems (`KeyFrame.stageHeight_ne_top`, `dvSideSet_nonempty` — ⊤ heights occur
only at zero blocks; the complement clause's ∀-domain never empties on monic
competitors), and the fence claim is landed as `one_le_dvSideDeg_of_dvd_dvResPoly`
(`hdvd` is unsatisfiable on the C.66 low-degree collapse stratum).

## What is blocked, and what is NOT claimed

BLOCKED: the signed PROOF route of NODE C.34 (`blueprint/CHAP-C_tower_grammar.md` §5,
"B.48's graded-coprime lift at the `dv`-carrier"), for BOTH residual frontier legs
(existence of the block pair for general `R`; separation of block pairs).

**NOT claimed:** that the statement is false. `exists_dv_residual_dissection` is the
residual-primary refinement of the theorem of the polygon at the level carrier ([GN15]
Thm 2.3 / [FGMN] arXiv-v3 Thm 6.6 index the factors by `(λ, ψ)` resp. `(λ, L)` — i.e.
the SOURCES already deliver the per-residual-factor refinement whose grouped-by-slope
form is C.33). It is expected TRUE as signed; no counterexample is offered and none is
expected.

## The frontier, isolated (landed packaging)

The landed Lean-core theorem `exists_dv_residual_dissection_of_frontier` proves the FULL
signed conclusion from exactly two Props, and the landed
`block_pair_self_of_dvResPoly_eq_pow` discharges the first on the single-class stratum
(`R = L.r ^ m` — `EFF.HE6.20` item 1, `fS = g`, `g' = 1`). So the entire remaining
content of the signed statement is the two goals below.

## The failing goals, verbatim

Reproduce: `git show e93bb724:leanfinal/Uniformity/ChapC/C34.lean |
sed 's/^    sorry$/    done/' > /tmp/c34_goals.lean && cd leanfinal &&
lake env lean /tmp/c34_goals.lean`. Output at the pin (common context: the full C.34
hypothesis pack `hπ`, `[IsAdicComplete (IsLocalRing.maximalIdeal O) O]`, `hg : g.Monic`,
`hpure : IsDvPure F g L.u L.ℓ`, `hne`, `hp : dvHgt … = ↑M₀`,
`hdvd : L.r ∣ dvResPoly F H₀ hpin g L.u L.ℓ hne M₀ hp`):

**FRONTIER 1 (`hex`) — the dv-graded coprime Hensel lift:**

```
⊢ ∃ fS g',
    g = fS * g' ∧
      HasLabel L fS ∧
        g'.Monic ∧
          ∀ (hne' : (dvSideSet F g' L.u L.ℓ).Nonempty) (M₀' : ℕ)
            (hp' : dvHgt F g' (dvSideMin F g' L.u L.ℓ hne') = ↑M₀'), ¬L.r ∣ dvResPoly F H₀ hpin g' L.u L.ℓ hne' M₀' hp'
```

**FRONTIER 2 (`hsep`) — same-slope block separation:**

```
⊢ ∀ (fS g' fS' g'' : Polynomial O),
    g = fS * g' →
      HasLabel L fS →
        g'.Monic →
          (∀ (hne' : (dvSideSet F g' L.u L.ℓ).Nonempty) (M₀' : ℕ)
              (hp' : dvHgt F g' (dvSideMin F g' L.u L.ℓ hne') = ↑M₀'),
              ¬L.r ∣ dvResPoly F H₀ hpin g' L.u L.ℓ hne' M₀' hp') →
            g = fS' * g'' →
              HasLabel L fS' →
                g''.Monic →
                  (∀ (hne' : (dvSideSet F g'' L.u L.ℓ).Nonempty) (M₀' : ℕ)
                      (hp' : dvHgt F g'' (dvSideMin F g'' L.u L.ℓ hne') = ↑M₀'),
                      ¬L.r ∣ dvResPoly F H₀ hpin g'' L.u L.ℓ hne' M₀' hp') →
                    fS' = fS
```

## Obstruction inventory — FRONTIER 1 (existence, general `R`)

1. **The signed route's engine does not exist at the `dv`-carrier.** The route is
   "B.48's graded-coprime lift (`GradedCoprime`, `exists_graded_solve`-pattern) at the
   `dv`-carrier". The landed B.44–B.48 machinery is `IsKey φ`-fenced throughout, and
   `IsKey F.key` is FALSE at `F.h ≥ 1` (reduction `x^{D′}`, not irreducible — C01
   header). This is `C33_BLOCKED_2026-08-17.md` obstruction 2 verbatim: no `dv`-graded
   coprimality carrier, no `dv` solve/limit lemma is landed anywhere in
   `Uniformity/ChapC/` (the only completeness-consuming ChapC nodes are
   C04/C60/C94/C109*/C110, none Hensel-shaped). C.34 needs the ONE-SLOPE version
   (residual Bézout over `F.stageField` between `r^{m_r}` and the coprime complement) —
   missing mechanism 1 of the C.33 record, not even the harder vertex version.
2. **The lift's bookkeeping twin is also unlanded.** The node's ⚠ flags
   `dvResPoly_mul_of_pure` (B.35's level-2 twin, needed by C.37 too) as a RE-PLAN
   expectation; nothing of that name or shape exists. The landed C.66 cite
   (`fgmn_residual_mul`) yields residual multiplicativity only GIVEN both factors
   `IsDvPure` with `D′ ∣ natDegree` — the lift must PRODUCE the factors' purity
   (B.57's step-1 squeeze at the `dv`-carrier), not consume it, so the cite cannot
   substitute for the engine.
3. **What IS landed:** the single-class case (`R = r^m`) needs no engine and is proved
   (`block_pair_self_of_dvResPoly_eq_pow`, Lean-core), and the general case's residual
   split `R = r^{m_r}·s` is available in `K[Z]` — the gap is exactly the lift.

## Obstruction inventory — FRONTIER 2 (separation)

1. **The blueprint's tool does not fire.** The route names
   `monic_factorization_unique` + residual coprimality.
   `Hensel.monic_factorization_unique` (`Uniformity/HenselFactorization.lean`) requires
   both factorizations to reduce to the same **coprime** pair over `ResidueField O`; at
   `F.h ≥ 1` distinct `dv`-pure factors generically share the divisor `x` mod `𝔪`
   (C33_BLOCKED uniqueness obstruction 1) — the coprimality hypothesis is structurally
   unavailable at the residue field, and no `dv`-graded uniqueness analogue exists.
2. **The complements' purity is unprovable from the landed engine.** Any residual-level
   comparison of `(fS, g')` with `(fS', g'')` runs through residual multiplicativity on
   each product. The landed cite C.66 `fgmn_residual_mul` demands `IsDvPure` + degree
   divisibility + positivity for BOTH factors; `HasLabel` supplies this for `fS`, `fS'`,
   but NOTHING supplies purity of `g'`, `g''` — that is `dv`-polygon additivity /
   purity-of-factors-of-pure ([FGMN] Thm 2.8 at the level carrier), landed nowhere and
   explicitly NOT part of C.66's signed same-slope clause (C33_BLOCKED uniqueness
   obstructions 2–3). Two further seams even if it were granted: C.66 is
   `{O : Type}`-monomorphic while the node is `{O : Type*}`, and C.66's floor
   `ℓ * ((e₁f₁)·h) < u` vs `LevelDatum.hκ`'s `ℓ * (e₁f₁) * h < u` needs an assoc bridge.
3. **The owner-signed A-C.8 cite pair does not cover this.** `exists_dvDissection` /
   `fgmn_dvDissection_factor_eq` (signed 2026-08-18, certification in progress, NOT yet
   landed) quantify over `DvDissection` structures — the grouped-by-SLOPE dissection.
   For pure `g` the slope dissection is the single factor `g` itself; C.34's block split
   is a refinement WITHIN one slope, about which the pair says nothing (A-C.8.2's scope
   fence states this explicitly: "imports no residual-primary dissection (C.34)").
4. **Even granting residual comparison,** UFD arithmetic in `K[Z]` pins only the
   residuals (`m = m'`, complements' residuals associate); factor EQUALITY `fS' = fS`
   is then precisely the uniqueness leg of the missing one-slope `dv`-Hensel.

## What survives, and is landed (commits `cdf53b30`, `4b2a436c`, `e93bb724`)

All Lean-core (`#print axioms = [propext, Classical.choice, Quot.sound]`), in
`C34.lean`:

* `KeyFrame.stageHeight_ne_top` — audit leg 1: ⊤ level heights only at zero blocks.
* `dvSideSet_nonempty` — audit leg 2: nonzero polynomials have nonempty `dv` sides at
  every `0 < ℓ` direction (closes the C.118-pattern escape through the complement
  clause; also reusable wherever an `hne` witness must be manufactured).
* `one_le_dvSideDeg_of_dvd_dvResPoly` — the `hdvd` fence: divisibility by `L.r` forces
  positive residual side degree (kills the C.66 low-degree collapse stratum). Proved by
  monic-`r` degree arithmetic; consumes NO stage-field `Field` instance.
* `natDegree_pos_of_one_le_dvSideDeg` — `HasLabel`'s positivity bookkeeping.
* `not_dvd_dvResPoly_of_natDegree_eq_zero` — the constant-complement law.
* `hasLabel_self_of_dvResPoly_eq_pow` — the `R = r^m` self-label.
* `block_pair_self_of_dvResPoly_eq_pow` — FRONTIER 1 discharged on the single-class
  stratum (`EFF.HE6.20` item 1).
* `exists_dv_residual_dissection_of_frontier` — the packaging: the FULL signed
  conclusion from FRONTIER 1 + FRONTIER 2 alone (uniqueness's second component is
  monic cancellation, supplied). C.33's `dvDissection_unique_of_factor_eq` pattern.

## The missing mechanisms, specified (for the orchestrator)

1. **`dv`-graded ONE-SLOPE Hensel with residual Bézout over `F.stageField`** — exactly
   missing mechanism 1 of `C33_BLOCKED_2026-08-17.md` (Newton step + limit through
   `IsAdicComplete` + purity/perturbation calculus at `dvHgt`). Its existence leg
   discharges FRONTIER 1; its uniqueness leg (coprime residuals ⟹ unique monic lift)
   discharges FRONTIER 2 step 4. Landing it serves C.33 and C.34 simultaneously.
2. **`dv`-polygon additivity / purity-of-factors** ([FGMN] Thm 2.8 at the level
   carrier) — needed for FRONTIER 2 steps 1–2 (and already recorded as C.33's cheaper
   uniqueness path). Either as theorems (heavy: the order-1 twin is B39a, ~700 lines,
   `IsKey`-fenced) or as a gate-(b) cite extension — an OWNER decision.
3. **Cite alternative for the whole node:** the SOURCES' theorem ([GN15] Thm 2.3 /
   [FGMN] Thm 6.6) is already residual-primary-indexed, so a C.34-shaped gate-(b)
   import in the A-C.8 style (consumer-normal-form: the signed block clause itself)
   would close both frontiers at once. A-C.8's scope fence makes clear this would be a
   NEW owner-gated proposal, not an extension-by-interpretation of the signed pair.

## Disposition

Per the unit charge (BLOCKED-not-weakened, no new axioms, no `sorry` in the final
landing): the signed theorem remains OPEN in `leanspec/Leanspec/ChapC.lean` (stub axiom,
as before this unit); the landing file carries the eight Lean-core declarations above,
sorry-free. Consumers of C.34's existence/uniqueness (C.35's `blockFactor_spec`, C.36,
C.61, …) must keep threading the leanspec stub until a mechanism above lands or the
owner extends the gate-(b) queue.
