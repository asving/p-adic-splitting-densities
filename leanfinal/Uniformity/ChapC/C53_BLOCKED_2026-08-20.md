# BLOCKED record — NODE C.53, `towerLocus_fibration`

**Unit:** chapter-C fleet, NODES C.35 + C.53 (2026-08-20).
**Landing file:** `leanfinal/Uniformity/ChapC/C53.lean` (PARTIAL — the signed DEF and the
ENTIRE counting half of the node LANDED; the signed theorem BLOCKED).
**Signed declarations:** `leanspec/Leanspec/ChapC.lean` lines 2011–2027 — **byte-frozen and
untouched.** `towerFreeCount` was transcribed and `diff`-verified byte-identical against lines
2011–2014 before any proof work.

Convention as in `C33_BLOCKED_2026-08-17.md` / `C34_BLOCKED_2026-08-18.md` /
`C52_BLOCKED_2026-08-18.md`: this documents that the signed THEOREM is not reachable from the
landed engine, quotes every failing goal verbatim, and classifies each block. **Evidence, not a
node.** No statement weakened, no axiom added, zero `sorry` in the landing.

## The mandatory vacuity audit came FIRST — verdict: no A-C.7 instance

The full audit is the landing file's header, committed before any proving per the charge.
Verdict: **no instance.** The risk site is `towerLocus`'s ∀-quantified pin block
(`∀ (hne) (M₀) (hp), dvResPoly … = towerLabel ^ μ₂`), a C.113-shaped pattern; it BINDS
on-locus because `IsDvPure F f T.u₂ T.e₂` is a CONJUNCT of the same membership and its first
component `0 ∈ dvSideSet F f T.u₂ T.e₂` inhabits `hne`, while `DvOnSide`'s second component
`dvHgt ≠ ⊤` supplies a finite pin at `dvSideMin`, inhabiting `M₀`/`hp`. `hμ₂ : 0 < μ₂`
restricts rather than rescues — the count identity is in fact TRUE at `μ₂ = 0` too (both sides
`1`). The frontier Props were audited as refutation targets in their own right (iffs over the
same class, no one-sided guard, domain never empty since `proj` is surjective, and not
trivially true).

## What is blocked, and what is NOT claimed

BLOCKED: the signed `towerLocus_fibration`. Its remaining content is EXACTLY two Props,
`TowerBudgetIff` (leg 1) and `TriangularDigitRead` (leg 2); the counting half is landed and
sharp.

**NOT claimed:** that the statement is false. `towerLocus_fibration` is the expected
count law of `EFF.GENTOW1.16` in its R2a-corrected (clipped) form. No counterexample is offered
and none is expected. The clip itself — the one place the corpus was WRONG — is now a landed
theorem rather than a caveat (see `card_budgetBox` below).

## The three failing goals, verbatim

Reproduce (from the repo root, with `Uniformity.ChapC.C53` built — `lake build
Uniformity.ChapC.C53`):

```sh
cat > /tmp/c53_goals.lean <<'EOF'
import Uniformity.ChapC.C53
namespace Uniformity.Density.Tower
open Uniformity Uniformity.Density Uniformity.Density.Leaf IsLocalRing IsDiscreteValuationRing
variable {O : Type*} [CommRing O] [IsDomain O] [IsDiscreteValuationRing O] {π : O}

-- GOAL 1: the whole frontier, as one Prop
theorem goal_C53 {F : KeyFrame O π} {H₀ : ℕ} {hpin : F.Pin H₀}
    (T : TowerDatum F H₀ hpin) (hπ : Irreducible π) (hh : 1 ≤ F.h)
    [IsAdicComplete (IsLocalRing.maximalIdeal O) O] [Finite (ResidueField O)]
    (μ₂ N : ℕ) (hμ₂ : 0 < μ₂) :
    Nat.card {c : Coeff O (μ₂ * T.D₂) N //
        ∃ a : Fin (μ₂ * T.D₂) → O,
          proj O (μ₂ * T.D₂) N a = c ∧ monicPoly a ∈ towerLocus T μ₂}
      = residueCard O ^ towerFreeCount T μ₂ N := by
  refine towerLocus_fibration_of_frontier hπ T μ₂ N ?_
  unfold BudgetBoxRead
  done

-- GOAL 2: leg 1 alone (leg 2 granted)
theorem goal_C53_leg1 {F : KeyFrame O π} {H₀ : ℕ} {hpin : F.Pin H₀}
    (T : TowerDatum F H₀ hpin) (hπ : Irreducible π) (hh : 1 ≤ F.h)
    [IsAdicComplete (IsLocalRing.maximalIdeal O) O] [Finite (ResidueField O)]
    (μ₂ N : ℕ) (hμ₂ : 0 < μ₂) (h2 : TriangularDigitRead T μ₂ N) :
    Nat.card {c : Coeff O (μ₂ * T.D₂) N //
        ∃ a : Fin (μ₂ * T.D₂) → O,
          proj O (μ₂ * T.D₂) N a = c ∧ monicPoly a ∈ towerLocus T μ₂}
      = residueCard O ^ towerFreeCount T μ₂ N := by
  refine towerLocus_fibration_of_frontier hπ T μ₂ N (budgetBoxRead_of_legs T μ₂ N ?_ h2)
  unfold TowerBudgetIff
  done

-- GOAL 3: leg 2 alone (leg 1 granted)
theorem goal_C53_leg2 {F : KeyFrame O π} {H₀ : ℕ} {hpin : F.Pin H₀}
    (T : TowerDatum F H₀ hpin) (hπ : Irreducible π) (hh : 1 ≤ F.h)
    [IsAdicComplete (IsLocalRing.maximalIdeal O) O] [Finite (ResidueField O)]
    (μ₂ N : ℕ) (hμ₂ : 0 < μ₂) (h1 : TowerBudgetIff T μ₂) :
    Nat.card {c : Coeff O (μ₂ * T.D₂) N //
        ∃ a : Fin (μ₂ * T.D₂) → O,
          proj O (μ₂ * T.D₂) N a = c ∧ monicPoly a ∈ towerLocus T μ₂}
      = residueCard O ^ towerFreeCount T μ₂ N := by
  refine towerLocus_fibration_of_frontier hπ T μ₂ N (budgetBoxRead_of_legs T μ₂ N h1 ?_)
  unfold TriangularDigitRead
  done
end Uniformity.Density.Tower
EOF
cd leanfinal && lake env lean /tmp/c53_goals.lean
```

### GOAL 1 — the frontier `BudgetBoxRead`, unfolded

```
/tmp/c35c53_goals.lean:33:2: error: unsolved goals
O : Type u_1
inst✝⁴ : CommRing O
inst✝³ : IsDomain O
inst✝² : IsDiscreteValuationRing O
π : O
F : KeyFrame O π
H₀ : ℕ
hpin : F.Pin H₀
T : TowerDatum F H₀ hpin
hπ : Irreducible π
hh : 1 ≤ F.h
inst✝¹ : IsAdicComplete (IsLocalRing.maximalIdeal O) O
inst✝ : Finite (ResidueField O)
μ₂ N : ℕ
hμ₂ : 0 < μ₂
⊢ ∀ (c : Coeff O (μ₂ * T.D₂) N),
    (∃ a, proj O (μ₂ * T.D₂) N a = c ∧ monicPoly a ∈ towerLocus T μ₂) ↔
      ∃ a, proj O (μ₂ * T.D₂) N a = c ∧ ∀ (i : Fin (μ₂ * T.D₂)), ↑(budgetSlot T μ₂ ↑i) ≤ (addVal O) (a i)
```

### GOAL 2 — leg 1 (`TowerBudgetIff`), unfolded: C.52's signed characterization

```
/tmp/c35c53_goals.lean:47:2: error: unsolved goals
O : Type u_1
inst✝⁴ : CommRing O
inst✝³ : IsDomain O
inst✝² : IsDiscreteValuationRing O
π : O
F : KeyFrame O π
H₀ : ℕ
hpin : F.Pin H₀
T : TowerDatum F H₀ hpin
hπ : Irreducible π
hh : 1 ≤ F.h
inst✝¹ : IsAdicComplete (IsLocalRing.maximalIdeal O) O
inst✝ : Finite (ResidueField O)
μ₂ N : ℕ
hμ₂ : 0 < μ₂
h2 : TriangularDigitRead T μ₂ N
⊢ ∀ (f : Polynomial O),
    f.Monic →
      f.natDegree = μ₂ * T.D₂ →
        (f ∈ towerLocus T μ₂ ↔
          ∀ (j a b : ℕ),
            j < μ₂ →
              a < F.e₁ * F.f₁ →
                b < T.e₂ * T.f₂ →
                  ↑(budgetFloor T μ₂ j a b) ≤ (addVal O) ((dev F.key (dev (composedKey T) f j) b).coeff a))
```

### GOAL 3 — leg 2 (`TriangularDigitRead`), unfolded: the unipotent digit dictionary

```
/tmp/c35c53_goals.lean:59:2: error: unsolved goals
O : Type u_1
inst✝⁴ : CommRing O
inst✝³ : IsDomain O
inst✝² : IsDiscreteValuationRing O
π : O
F : KeyFrame O π
H₀ : ℕ
hpin : F.Pin H₀
T : TowerDatum F H₀ hpin
hπ : Irreducible π
hh : 1 ≤ F.h
inst✝¹ : IsAdicComplete (IsLocalRing.maximalIdeal O) O
inst✝ : Finite (ResidueField O)
μ₂ N : ℕ
hμ₂ : 0 < μ₂
h1 : TowerBudgetIff T μ₂
⊢ ∀ (c : Coeff O (μ₂ * T.D₂) N),
    (∃ a,
        proj O (μ₂ * T.D₂) N a = c ∧
          ∀ (j a' b : ℕ),
            j < μ₂ →
              a' < F.e₁ * F.f₁ →
                b < T.e₂ * T.f₂ →
                  ↑(budgetFloor T μ₂ j a' b) ≤
                    (addVal O) ((dev F.key (dev (composedKey T) (monicPoly a) j) b).coeff a')) ↔
      ∃ a, proj O (μ₂ * T.D₂) N a = c ∧ ∀ (i : Fin (μ₂ * T.D₂)), ↑(budgetSlot T μ₂ ↑i) ≤ (addVal O) (a i)
```

## Classification of each block

The charge's three categories: **(i)** unreachable from the currently landed engine;
**(ii)** reachable, but needing a lemma nobody has written; **(iii)** blocked on one of the
documented frontiers (the `dv`-level Hensel engine being the recurring one).

| goal | category | mechanism |
|---|---|---|
| **GOAL 1** (frontier) | **(iii)**, inherited | It is the conjunction of GOALS 2 and 3 (`budgetBoxRead_of_legs`, landed). It is recorded separately only so a consumer can thread ONE Prop. Its status is the worse of the two below, i.e. **(iii)**. |
| **GOAL 2** (leg 1 = C.52) | **(iii)** — blocked on the documented `dv`-graded product/purity frontier | This is *verbatim* C.52's signed `towerLocus_iff_budget`, whose four proof legs are BLOCKED at `C52_BLOCKED_2026-08-18.md`. The mechanism there, restated: `towerLocus` membership contains `IsDvPure F f T.u₂ T.e₂` and `dvResPoly … = towerLabel ^ μ₂`, and reaching either from coefficient floors needs (a) `DvAbove` closure under multiplication, (b) `IsDvPure` closure for full-degree factors, (c) normalized `dvResPoly` multiplicativity or an exact power theorem for a test key. A repository-wide search finds NO `dvSupp`, `IsDvPure`, or `dvResPoly` multiplication theorem. C.47's `composedKey_isTestKey` supplies the test-key hood only (monicity, degree, lattice digits, exact single-key heights) — not `IsTestKey → IsDvPure`, not closure under powers. The one adjacent ALLOWLISTED cite, C.66's `fgmn_residual_mul`, assumes both factors already `IsDvPure` with `D′ ∣ natDegree` and concludes only up to an unspecified nonzero scalar, so it neither creates the power's purity nor yields the exact equation `dvResPoly = towerLabel ^ μ₂`. **This is the same missing mechanism as `C33_BLOCKED_2026-08-17.md`'s and `C34_BLOCKED_2026-08-18.md`'s** — the order-one `suppVal` engine (B.33/B.35/B.39) is `IsKey φ`-fenced and `IsKey F.key` is FALSE at `F.h ≥ 1`, so it cannot be applied at the `dv` carrier. |
| **GOAL 3** (leg 2 = unipotence) | **(ii)** — reachable, but the lemma is unwritten | This one is NOT blocked on the Hensel engine. It is pure two-key development bookkeeping at a fixed window: the digit `c_{j,a,b}` is the coefficient at degree `j·D₂ + b·D′ + a` plus an `O`-combination of strictly HIGHER-degree coefficients (unipotent in the x-degree filtration), so the change of coordinates is a bijection of the level-`N` box onto itself and transports the per-slot floors. Nothing about polygons, purity, residuals, completeness, or Hensel lifting enters. The needed ingredients are all landed: B.02's `dev`, B.03's `degree_dev_lt`, B.05's `sum_dev_eq`, B.06's `dev_unique`, and — as a worked template for exactly this style of argument — `C110.lean` clause 2's `dev_sub_C` two-key binomial, whose header explicitly calls itself "the honest replacement for a 'translation is unitriangular' hand-wave: no matrix". **Nobody has written the general two-key dictionary**; obstruction 4 of `C52_BLOCKED_2026-08-18.md` names this very file as its intended supplier, and this unit did not supply it (it is a self-contained sub-unit of roughly C.110-clause-2 size). Recommended as the next actionable increment in §6: it is the only piece of C.52+C.53 that does NOT wait on the `dv`-graded engine. |

## What survives, and is landed (commits `bc39e128`, `509a20a2`)

All Lean-core (`#print axioms = [propext, Classical.choice, Quot.sound]`), in `C53.lean`,
zero `sorry`:

* `towerFreeCount` — the signed DEF, byte-frozen. The R2a CLIP is the `ℕ`-subtraction,
  definitionally.
* `budgetSlot` — the slot decoding `i ↦ (i / D₂, i % D₂ % D′, i % D₂ / D′)`. Since
  `D₂ = D′ · (e₂f₂)` (C.42), the `n = μ₂D₂` scalar coefficient coordinates are in bijection
  with the slot triples `(j, a, b)`, `j < μ₂`, `a < D′`, `b < e₂f₂`.
* `sum_budgetSlot` — flattening the signed triple sum along that decoding reproduces
  `towerFreeCount` exactly (two applications of a new `sum_range_div_mod`, then
  `Finset.sum_comm`).
* **`card_budgetBox` — the ENTIRE counting half of the node.** The level-`N` classes admitting
  a lift whose slot-`i` entry has valuation `≥ budgetSlot T μ₂ i` number exactly
  `q ^ towerFreeCount T μ₂ N`, at EVERY finite window `N` — **including windows below some
  slot's floor**, where that slot contributes the single zero class (`q ^ 0 = 1`) rather than a
  negative exponent. This is the node's TEETH discharged: the R2a counter-instance (the clip
  active at small `N`) is a THEOREM here, not a caveat, exactly as the blueprint's TEETH line
  demands ("the clip is definitionally active in the formula").
* `BudgetBoxRead`, `TowerBudgetIff`, `TriangularDigitRead` — the frontier and its two legs.
* `budgetBoxRead_of_legs` — the frontier splits into exactly leg 1 + leg 2 (`monicPoly_monic`
  and `monicPoly_natDegree` supply C.52's two side conditions for free).
* `towerLocus_fibration_of_frontier` — the FULL signed conclusion from the frontier alone. The
  signed `hh` and `hμ₂` are not consumed by the reduction, which is itself part of the R2a
  finding: the count law is uniform in them, so it is the CLIP and not a positivity side
  condition that makes the formula correct at small `N`.

## Overlap with the recent §6/§10 landings — checked, and how it was resolved

* **`C128.lean` / `C128b1.lean` / `C128b2.lean`** (LAW B-S2, the P-BINOM cascade
  pair-or-triple attainment law): read before transcription. They carry
  `composedKey_trinomial` and pinned-parameter attainment identities; none is the general
  two-key triangular dictionary, and none mentions `Coeff`, `proj`, `monicPoly` or `Nat.card`.
  **Resolution:** nothing there is consumable here, and nothing here re-proves anything there.
  (This confirms obstruction 4 of the C.52 record, which had already noted that
  `composedKey_trinomial` "is a specialized pinned-parameter identity, not that general
  dictionary".)
* **`C52.lean`**: `budgetFloor` is consumed **by name**, not re-declared. Its two landed step
  legs (`budgetFloor_le_iff`, `dev_pow_self`) were not duplicated. Its four BLOCKED legs are
  precisely leg 1's suppliers and were **not re-attempted** — the route is documented dead at
  the `dv`-graded product law and re-walking it is forbidden by the charge.
* **`C110.lean`**: `ht_conservative_card` is the level-1 twin of `card_budgetBox`, but its
  floor function is hard-wired to the conservative shape `(a − i/d)·k + 1`, so it CANNOT be
  instantiated at `budgetFloor`. Its three valuation-counting helpers
  (`card_res_ge_of_le`, `card_res_ge_of_ge`, `card_res_ge`) are exactly what was needed and are
  `private` there, hence not importable. **Resolution: reproduced verbatim as `private` lemmas
  of `C53.lean`, with the duplication DISCLOSED in the file header**, rather than editing
  another unit's landed file (the charge scopes `git add` to this unit's own files).
  **RE-PLAN candidate for the orchestrator:** promote the cluster to
  `Uniformity/Density/LocalData.lean` and have both C.110 and C.53 consume it — this removes
  the only duplication introduced by this unit.

## Disposition

The signed `towerLocus_fibration` remains OPEN in `leanspec/Leanspec/ChapC.lean` (stub axiom,
as before this unit). Consumers in §11 should consume `towerLocus_fibration_of_frontier` and
thread `BudgetBoxRead` — or, better, thread the two legs, since leg 2 is **category (ii)** and
therefore closable now, independently of the `dv`-graded engine that gates everything else in
§5/§6.
