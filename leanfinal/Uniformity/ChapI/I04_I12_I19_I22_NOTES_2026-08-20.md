# Chapter I `[note]`-node dispositions — I.04, I.12, I.19, I.22

**Unit:** CHAPTER-I OPENER, 2026-08-20.  **House style:** `Uniformity/ChapC/C34_BLOCKED_2026-08-18.md`.

Per `blueprint/CHAP-I_capstone_conditionality.md` §0.2: *"Note-nodes (provenance/resolution/audit
rows marked `[note]`) have **no Lean file** — they live in this chapter and in the DAG only."*
The §11 disposition table confirms the count: *"`[note]` nodes with no Lean file — **8** — I.04,
I.11, I.12, I.13, I.14, I.19, I.20, I.22 — transcribed as docstring sections, nothing declared."*

**So the deliverable for each node below is this record plus DAG rows, and NOTHING in Lean.**  All
four are hereby recorded as **NO NEW LEAN — DISCHARGED BY THE NAMED EXISTING DECLARATION(S)**,
except I.22, which is discharged by a human-audit assignment and by construction has no
declaration (see its entry).  Every name below was verified by `rg` against
`leanfinal/Uniformity/` at this commit; the `file:line` is the DECLARATION site, not a mention.

---

## NODE I.04 `[note]` — `A2_n` IS LANDED `DrainageAt n` (no new Lean) — **VERIFIED**

**Verdict: DISCHARGED. No new Lean. Chapter I declares NO parallel drainage `Prop`.**

| what the node claims | the declaration | verified at |
|---|---|---|
| `A2_n`'s formal carrier exists and is landed | `Uniformity.Density.DrainageAt (n : ℕ) : Prop` | `Uniformity/Density/Statement.lean:118` |
| PROVED at `n = 1` | `Uniformity.Density.drainage_one` | `Uniformity/Density/Gates.lean:125` |
| PROVED at `n = 2` | `Uniformity.Density.drainage_two` | `Uniformity/Density/Drainage.lean:838` |

The blueprint's own line-number claim (*"`Statement.lean:118`"*) is **exact at HEAD** — the `rg`
hit is `Statement.lean:118:def DrainageAt (n : ℕ) : Prop :=`.  Both proof sites conclude
`UndecidedVanishes O n σ` for the two degrees, which is `DrainageAt`'s body under its bundle
quantifier.

**Open at `n ≥ 3`** — unchanged by this unit.

**WHY NO NEW LEAN IS THE HONEST ANSWER (honesty I-4).**  A chapter-I `A2` carrier would
**double-count the leg**: `TotalMassOne` is the drainage leg in disguise (`Statement.lean`'s own
words in the `TotalMassOne` docstring, read at this commit), and the disposition table gives it no
row of its own.  Declaring `def A2At (n : ℕ) : Prop := DrainageAt n` would add a name and no
content, and would make the open surface look one obligation larger than it is.

**Boundary reached and NOT crossed.**  The node's remaining clause — *"the structure field `a2`
(I.10) binds the landed name directly"* — is **I.10 content and I.10 is FROZEN** (the
tower-instantiation freeze; the critical path).  This unit records the boundary and stops: the
`a2` field is not written, not typed, and not `#check`ed here.

**DAG rows owed** (blueprint's own list, for the orchestrator's `dag_build`):
`BP.I.04 → lean:DrainageAt`, `BP.I.04 → HYP.27`, `COND:A2_n → BP.I.04`.

---

## NODE I.12 `[note]` — `COND:A2_n` RESOLVED — **VERIFIED**

**Verdict: DISCHARGED. No new Lean.  The orphan was a NAME-FORM MISS, not a missing row.**

`A2_n` **is** ledger row `HYP.27`'s content; its formal carrier is the same landed
`Uniformity.Density.DrainageAt n` (`Uniformity/Density/Statement.lean:118`) recorded under I.04.
The 0c DAG matcher missed it because `HYP.27`'s title is *"`A2` drainage"* with no `_n` suffix.

**Status by degree, as resolved:**

| degree | status | declaration / mechanism | verified at |
|---|---|---|---|
| `n = 1` | PROVED | `drainage_one` | `Uniformity/Density/Gates.lean:125` |
| `n = 2` | PROVED | `drainage_two` | `Uniformity/Density/Drainage.lean:838` |
| `n = 3` | OPEN | the `HYP.08`/`HYP.09` frontier | — |
| `n ≥ 4` | OPEN | via H.98 ⇐ `InductionPackage` (see I.19) | — |

**Why this is a resolution and not a discharge.**  `COND:A2_n` was a DAG orphan — a Display-A
conjunct matching no ledger row.  Resolving it means *tying it to a row and a carrier*, which is
bookkeeping; it moves no mathematics.  The `n ≥ 3` obligation is exactly as open after this note
as before it.

**DAG rows owed:** `COND:A2_n → HYP.27`, `COND:A2_n → lean:DrainageAt` (via `BP.I.12`).

---

## NODE I.19 `[note]` — THE PACKAGE ROUTE (H.98 consumed, not re-proved) — **VERIFIED**

**Verdict: DISCHARGED. No new Lean, and a re-export is FORBIDDEN, not merely unnecessary** —
the one-public-declaration discipline: *"a verbatim re-export adds a name, not content."*

The four names the node consumes, all verified as landed declarations:

| role | declaration | verified at |
|---|---|---|
| the rate route to the `a2` fields (H.98) | `Uniformity.Density.drainage_of_package : (∀ n, InductionPackage n) → ∀ n, DrainageAt n` | `Uniformity/ChapH/H98.lean:148` |
| the package form of I.17 (H.98) | `Uniformity.Density.uniformity_of_package` (`UniformityStatementDecided → (∀ n, InductionPackage n) → UniformityStatement`) | `Uniformity/ChapH/H98.lean:168` |
| base case, **UNCONDITIONAL** (H.96, a chapter-G payoff) | `Uniformity.Density.package_two : InductionPackage 2` | `Uniformity/ChapH/H96.lean:61` |
| base case, H.97's **ADOPTED** form | `Uniformity.Density.package_three_of_rate` | `Uniformity/ChapH/H97.lean:77` |
| the predicate itself | `Uniformity.Density.InductionPackage (n : ℕ) : Prop` | `Uniformity/ChapH/H95.lean:61` |

**A `rg` FINDING WORTH RECORDING — `package_three_of_drainage` IS NOT LANDED, and the grep that
suggests otherwise is a trap.**  A naive `rg` for the name hits `Uniformity/ChapH/H97.lean:26`.
That hit is **inside H97's module docstring**, in a fenced code block quoting the VOIDED committed
signature under the heading *"⚠ BLUEPRINT DEFECT DECLARED AND CURED IN PLACE —
`package_three_of_drainage` IS NOT LANDED."*  H97's own docstring on the adopted theorem repeats
it: *"The superseded `package_three_of_drainage` is not landed: drainage is a limit, and
`InductionPackage` demands a rate (`EFF.GENIND.149`)."*  The blueprint's instruction — it *"is
DECLARED UNPROVABLE by H.97's own ⚠ and must not be assigned"* — is therefore **live and
correctly reflected in the tree**.  Anyone auditing by grep alone would mis-read the tree here.

**⛔ THE GENIND.B FENCE APPLIES IN FULL (honesty I-2), and this unit did not approach it.**  No
node here or anywhere composes `InductionPackage n` for `n ≥ 4`.  H.98 is `GENIND.B` **Step 5
only** (`P(n)` for all `n` ⟹ the capstone); the reduction's inductive step
(`(IH) + [GENIND-H(n)] ⟹ P(n)`) has **no node in any chapter**, and its content is exactly
Display A's hypothesis block.  A node claiming `GENIND.B` must be returned.

**DAG rows owed:** ties to `BP.H.95`–`BP.H.98` and `HYP.27`/`HYP.30`.

---

## NODE I.22 `[note]` — THE CORE-SET AUDIT LEDGER (A-1 … A-7) — **RECORDED**

**Verdict: NO NEW LEAN, AND NO DECLARATION TO NAME — by construction.**  This node's content is
an assignment of **human** audit rows.  Per Part V item 2, cite/def-faithfulness rows stay
**gate-(b)-adjacent human reads**; the rest are absorbed into the standing fresh-eyes cadence.
**No audit row is a fleet node**, so no agent — including this one — may mark one done.

The ledger, transcribed with its `when` column, which is the operative part:

| id | row(s) | object | class | when |
|---|---|---|---|---|
| A-1 | HYP.01, HYP.105 | `typeOf` faithfulness — review I.21's statement against the classical `(e,f)` multiset | gate-(b)-adjacent (def-faithfulness), human | **before any node hypothesizes `TypeOfFaithful`**; refreshed each fresh-eyes pass |
| A-2 | HYP.12 | `(e,f)` ordering/multiset normalization vs the classical convention | one clause of A-4 | with A-4 |
| A-3 | HYP.17 | [AGNPRW]/[GN15]/[FGMN]/Kuhlmann publisher numbering | gate-(b)-adjacent (cite), bounded lookup | **before C.94/C.66/C.92 sign** — load-bearing since Q1 cites by number |
| A-4 | HYP.98 | the core set: the types, `UniformityStatement`, the two densities, the cites, changed-definition faithfulness paragraphs | RECURRING fresh-eyes | at chapter milestones |
| A-5 | HYP.111 (def half) | what `decidedDensity` counts | rides A-4 | with A-4 |
| A-6 | (I.10) | the field list of `CapstoneHypotheses` vs frozen Display A — one-to-one, nothing added, nothing dropped | fresh-eyes, decorrelated arm | at the cross-read + each gate-(a) event |
| A-7 | HYP.91 | OB-a…OB-d representation vs CHAP-H's committed text | bounded, one pass | at the H stub stage |

**TWO ROWS THIS UNIT TOUCHES, AND EXACTLY HOW.**

* **A-1 is now LIVE-BUT-UNRUN, and this unit is why.**  Its trigger is *"before any node
  hypothesizes `TypeOfFaithful`"*, and I.21 (`Uniformity/ChapI/I21.lean`, this unit) **lands that
  name**.  Landing the carrier is not hypothesizing it — no declaration in this unit takes
  `TypeOfFaithful` as a hypothesis — so the trigger has **not** fired.  But the name now exists,
  so **the first consumer fires A-1**, and A-1 has not run.  Recorded as the standing
  precondition on I.21's first consumer.  I.21's own header additionally records that the carrier
  is `True`-bodied, so such a hypothesis would be **vacuous** until the body lands.
* **A-6's finding is REAFFIRMED, and its re-fire condition is UNMET.**  The leanspec's A-6 row
  records the 0e gate result: the `CapstoneHypotheses` field list is **NOT one-to-one** with
  Display A — defects I-D4 (`𝒲` dropped), I-D7/I-D8 (`jd0`/`genhnBox2` placeholders, no `w1`),
  I-D9 (`a1` vacuous), I-D10 (`a0` over-demanded at `n = 0`), and A-I.1 ADDS **I-D13** (the
  fields are refutable, so the block is currently UNINHABITED).  A-I.1 closes I-D4 and I-D9,
  rules I-D7/I-D8, and **leaves I-D10 open**.  The row's re-fire condition is *"after the
  I.10a/I.10b sockets are typed"* — **those sockets are FROZEN and were not touched by this
  unit**, so A-6 cannot re-fire yet.
  * I-D9 is now **closed in `leanfinal` too**, not only in the spec: `Uniformity/ChapI/I03.lean`
    (this unit) lands A-I.1's re-signed enumerating `MenuLawAt` with the refutation of the old
    form preserved as `MenuLawAtVoided` and the re-sign certified.
  * I-D10 is now **recorded in `leanfinal`**: `Uniformity/ChapI/I02.lean` (this unit) carries it
    in its header and makes it machine-visible through the `Iff.rfl` literal-slice pin.  It is
    **not repaired** — the repair is a field change at I.10, owner-gated under Part V gate (a).

**DAG rows owed:** `A-*` tie to their `HYP` rows via `BP.I.22`.

---

## What this record does NOT do

* It does **not** touch I.10 / I.10a / I.10b, and it does not type, write or `#check` any field of
  `CapstoneHypotheses`.
* It does **not** mark any A-* audit row done; no audit row is a fleet node.
* It declares **no axiom**, imports **no cite** (the allowlist stays at exactly six), and asserts
  **no capstone statement**.
* It resolves **no** open mathematics.  I.04 and I.12 leave drainage open at `n ≥ 3`; I.19 leaves
  the `n ≥ 4` inductive step with no node in any chapter (the GENIND.B fence).
