# EFF-WELDMASTER — CANONICAL EFFECTIVE SPECIFICATION v2/v3

## 1. Header

**Source note:** `lean/notes/openmath/WELDMASTER_2026-08-08.md` (1,296 lines / **86,248 bytes**).

**HEAD blob hash:** `2ca69600cf776a68bf309e3036d7d52deeddf1af`. Whole-file md5 at compile time: `f53cbcb443a9cacd52df4abd6fe82310`.

**Compilation context:** `docs/BLUEPRINT_PHASE_DESIGN_2026-08-13.md`, Stage 0a, Opus wave 4, queue 8b (Asvin-authorized substitution, `docs/PROJECT_STATE.md` #51).

**Governing template:** `spec/EFF_TEMPLATE_v2.md` AT HEAD — rules 1–28.

**SHARD-THRESHOLD CALL (rule 9), recorded explicitly.** Rule 9 directs that “notes > ~80 KB are compiled per-section in multiple runs”. **This note is 86 KB — above the threshold.** It is nevertheless compiled in **one full run**, on the queue's explicit charge of “7 full compilations”, and because the note's structure is unusually favourable to a single pass: its mathematical content is **four theorems and one lemma** (S1–S3), with the remaining ~60 % being a conditionality stack, a machine bracket, a claims table and a ten-pass arc record — all record genres that compile densely. **The risk rule 9 guards against — a mid-note ID or xref discontinuity — is mitigated here by the fact that no statement unit spans a section boundary.** Recorded so a cross-reader can second-guess the call.

**COMPILED-BY:** opus (2026-08-14, Asvin-authorized substitution, PROJECT_STATE #51). **CODEX CROSS-READ OWED** (post-reset).

### Queue-specific charge: `WELD-M-PKG`'s five fields and the one-perimeter fence, as T1's C7 consumption anchors

`T1_GAUGE_COCYCLE_2026-08-12.md` consumes this note at **one named interface**, its `(C7)` section, and consumes **exactly two objects**: the five-field package `THEOREM WELD-M-PKG` and the one-perimeter fence `THEOREM WM-FENCE`. Both are emitted below with their field-level identities so the DAG can key T1's C7 edges.

`[TABLE]` — **THE C7 ANCHOR TABLE**

| object | EFF unit | what T1 consumes | T1 site (verified) |
|---|---|---|---|
| `WELD-M-PKG` **field `T_M0`** (identity of the two canonical division chains) | `.22` | nothing directly | — |
| `WELD-M-PKG` **field `T_M1`** (`w_m = v_{m+1}` + the affine shear `Σ_q(i,u) = (i, e_q u + h_q i)`) | `.22` | the tower gauge formulas' grading half | `T1` L737 ff. |
| `WELD-M-PKG` **field `T_M2`** (per-slot read/operator character transport generated by `t_j/ε_j`) | `.22` | **the complete read fence** — “WELD-M-PKG \(M2\) interface names its complete read fence” | `T1` (C7) |
| `WELD-M-PKG` **field `T_M3`** (multiplication transport to `K^c[ℤ/E]` on the scored stratum) | `.22` | the coboundary side; DULEMMA also consumes only this field | `T1` L1034 |
| `WELD-M-PKG` **field `T_M4`** (representative/lift covariance at W2-C3's accepted fenced scope) | `.22` | — | — |
| `THEOREM WM-FENCE` (the one-perimeter fence, `(PERIM-μ)` + specializations (a)/(b)/(c)) | `.13`–`.16` | the character-triviality criterion, **as a fixed-data criterion only** | via `.28`'s F-4 scoping |

**T1's own scope declarations, verified at the target by fixed-string grep** (`grep -cF 'WELD-M-PKG' T1_GAUGE_COCYCLE_2026-08-12.md` = **14**):

> “- the weld is used only at the proved scope of the five-field package WELD-M-PKG; no cross-face coherence or uniqueness is asserted; and” (T1 L13)

> “**#### (C7) WELD-M-PKG interface and scope fence**” (T1 L726)

> “WELD-M-PKG is not absorbed as a stronger theorem: (C7) is only an interface map and scope fence.” (T1 L1074)

> “WELD-M-PKG is five heterogeneously typed fields assembled into one named record. T1 proves neither a single cross-face map nor compatibility beyond the package's displayed anchors.” (T1 L1217)

**Compiler finding for the DAG.** T1 consumes the **package scope**, not the frozen `THEOREM WELD-M` — i.e. **T1 was written against the corrected form** (`.22`, dated 2026-08-12) and not against the note's frozen S1 display (`.05`). `spec/EFF-T1*.md` carries `WELD-M-PKG` **21** times. **The C7 edge is therefore an edge to the annex, not to the body** — recorded because a chapter cut assembling WELDMASTER's body alone would not contain the object T1 names.

### Effective-text rule

The freeze is declared at the ANNEX heading (L1044): “## ANNEX (dated appends; **the frozen body is not edited**)”, and re-declared by the C6/E6 fold (L1105–1106): “Everything above this heading is byte-untouched by this append.”

**Therefore the effective text is: the frozen body L1–1042 read with its inline `[r1]`…`[r7]` bracket repairs applied in place, then the dated appends — the ACCEPTANCE RECORD with its WHAT-IS-ACCEPTED clause, the 2026-08-08 O-1 annex, and the 2026-08-12 sol C6/E6 fold with its three corrections (F-1 = `WELD-M-PKG`, F-2 = the H¹-torsor withdrawal, F-4 = the WM-FENCE consumption scoping) plus the F-3 RESOLVED-ALREADY disposition and the E6 provenance.**

### FREEZE PREDICATES (rule 21 — two)

**FREEZE-1 — text freeze.** “the frozen body is not edited”; the acceptance record dates it 2026-08-07. **Disposition: `UNPINNED-VALUE`** — unlike HEX3, HE6 and HE6R1, **this note's freeze carries no prefix md5**, so the boundary is asserted by section heading only. Recorded, not silently passed.

**FREEZE-2 — instrument freeze.** §S5: a two-commit seal on `verification/openmath/weldmaster_checks.py` (EXISTS ✓) with the roster “imported FROZEN and md5-pinned” from `grt_jc_probe.ROSTER`. **The roster's md5 value is not displayed** — `UNPINNED-VALUE` again.

### Supersession chains

`[TABLE]`

| # | Target (frozen text) | Layer 1 (`[rN]`) | Layer 2 (acceptance / O-1, 08-07/08) | Layer 3 (C6/E6, 08-12) | TERMINAL |
|---|---|---|---|---|---|
| C-1 | **S1's “there is ONE transport 𝕋 … with five clauses”** | — | acceptance names “THEOREM WELD-M (the one transport, five faces…)” | **F-1: `THEOREM WELD-M-PKG`** — an existence package of five heterogeneously typed fields; “one” means one named record, **not uniqueness** | **F-1** (`.22`) |
| C-2 | PE10's “Ẽ is THE canonical gauge … forced by H¹ = 0 uniqueness” | — | (introduced in the acceptance record) | **F-2: the H¹ = 0 gloss WITHDRAWN**, replaced by a torsor derivation | **F-2** (`.23`) |
| C-3 | S6's “the J-D block … consumes WM-FENCE's (F-b)-**freedom** observation” | — | — | **F-4: replaced** — WM-FENCE supplies a fixed-data **criterion**, no freedom or existence theorem | **F-4** (`.28`) |
| C-4 | WM-FENCE(a)'s quoted JA-VDIND display | `[r5]` PE5 F-1: quote restored to its byte-form at the `ce0469a` pin | — | untouched by F-4's sweep | **`[r5]`** |
| C-5 | WM-COB(ii)'s 0/2 pricing | `[r1]` F-4: “rides [ILN]† EXP-KIT(a) … the sealed 0/2 pricing of (ii) was **conservative**, not upgraded here” | — | — | **`[r1]`** |
| C-6 | S4's supplier grades (“W-2/J-A/J-B/J-C at ATTEMPT 0/2 each”) | `[r2]` (16 as-of pin sites) | acceptance: “the O-1/fold understatements catalogued **conservative**” | — | **the acceptance's catalogue** |
| C-7 | The note's own grade | seven repair rounds | **ACCEPTED 2/2** (PE9 + PE10) | ledger **CERTIFIED** | **ACCEPTANCE + ledger row 24** |

**Note the pattern.** **Two of the three C6 findings correct claims that entered through the ACCEPTANCE RECORD itself** — C-1's “the one transport” and C-2's H¹ = 0 gloss are both acceptance-record wording, not body wording. **The decorrelated read found defects in the acceptance narrative, not only in the mathematics.** F-2's own words: “This affects only the certification narrative, not WM-CLASS's proved existence/class identity.”

### Scope of record — three-predicate split

#### A. Supply set

Four theorems and one lemma: `WELD-M` (→ `WELD-M-PKG`), `WM-COB` (three clauses), `WM-RHO`, `WM-CLASS`, `WM-FENCE`; plus the residue fence (S2.3) and the three-master-fence surface (WM10). **Of these, exactly one is a complete new proof** — WM-COB(i), the integer coboundary — with WM-RHO the second (“complete proof, S5 arithmetic”). **The rest are proved-by-citation or display grade** (`.20`).

#### B. Consumption / append-protocol scope

**Inbound-only for the body**; the note edits nothing. Its outbound edges are to T1 (C7, `.22`), to DULEMMA (the M3 field only), and to the J-D design sentence F-4 corrects. **No landing is claimed at any supplier.**

#### C. Inbound corrections RECEIVED

Ten hostile passes, seven repairs (PE1–PE10), then the sol C6/E6 wave: **3 REAL + 1 RESOLVED-ALREADY, no CRITICAL, none obstructed.**

### Resolution rules applied

1. `[rN]` brackets are in the frozen body and apply in place.
2. Appends apply in order: ACCEPTANCE → O-1 annex → C6/E6 (F-1, F-2, F-4) → the E6 provenance.
3. **The acceptance record is itself a corrected object** (C-1, C-2) — a consumer must read `.21` through `.22` and `.23`.
4. **A “PROVED-BY-CITATION” claim is compiled with its citation grade attached**, never as a bare proof.
5. Out-of-note material (T1, the four junction notes, `[ILN]†`, the ledgers) resolves and verifies only.

### Quotation discipline

Every CANONICAL STATEMENT is verbatim or `[ASSEMBLED]`. The note's indented `(M0)`–`(M4)` clause block and the LaTeX array of `WELD-M-PKG` are reproduced as written; the S6 claims table is transcribed as `[TABLE]`. **Rule 14 is not triggered** — no fenced ASCII table (checked, not skipped).

---

## 2. NON-IMPORTS

**N-1 — nothing new is defined before S1.** §S0's heading: “Setting (all consumed; **nothing new defined until S1**)”.

**N-2 — no cross-face compatibility is asserted.** `WELD-M-PKG`: “**No compatibility beyond those displayed ties is asserted.**” And: “‘one’ means one named five-field package, **not uniqueness** and **not a further unproved common-map coherence theorem**.”

**N-3 — no uniqueness of the gauge.** F-2's torsor derivation: gauges with a fixed coboundary form a `Hom`-torsor, so **`H¹ = 0` is false for the trivial action** and Ẽ is not canonical.

**N-4 — WM-FENCE supplies no freedom.** F-4: “WM-FENCE supplies **no freedom or existence theorem** for varying `z_μ`, `A_μ`, the attained set, or the representative.”

**N-5 — the outer letter is not a cochain value.** WM7 / S2.3: “residue fence: `c_m = −z_{m+1}` is the outer letter, **NOT a cochain value**; mod-wrap/χ-chain not reduced”.

**N-6 — the one-perimeter statement does not scalarize the wrap.** S3 (L554): “one-perimeter statement does NOT scalarize the wrap; the exact law off the [perimeter is …] the one-perimeter observation seen from the failure side.”

**N-7 — the four junction notes' surfaces are untouched.** WM10: “three-master-fence surface (F-a/b/c) as consumer reading aid; **four notes' surfaces untouched**”.

---

## 3. Statement inventory

IDs `EFF.WELDMASTER.01`–`.30`.

### EFF.WELDMASTER.01  [record]

**CANONICAL STATEMENT** — the title line (L1): the note is “the four junctions as faces of ONE anchored graded transport (M0–M4), the coboundary display `c_{n+1} = z̄^δ·∂E` (the [ILN]† inn[er word] …)”.

**DERIVATION.** [RECORD]. **SERIES:** ARC SERIES member 1 (`.01` → `.19` (S4 grade) → `.21` ACCEPTANCE → `.30` ledger).

**CONDITIONALITY.** “**ONE** anchored graded transport” is the phrase F-1 corrects (`.22`): the honest scope is **one named five-field package**, not one map. `STALE` in the title, which no append edits.

**XREF.** none.

---

### EFF.WELDMASTER.02  [convention]

**CANONICAL STATEMENT** — §S0's setting (L222–281). FORM: `##`-headed section.

> “## S0. Setting (all consumed; nothing new defined until S1)”

**DERIVATION.** [DEFINITIONAL/IMPORTED] — the S0 weld cochain `t_j/ε_j`, the shift data `(δ, S_j, D_j)`, the split weights `β_j`, and the anchor conventions, all consumed.

**CONDITIONALITY.** **The heading is itself a fence**: “all consumed; nothing new defined until S1”. It is what makes the note's net-new content auditable — and `.06`'s remark then quantifies it: “Its net new content is **exactly two displays**: S2 … and S3”.

**XREF.** `[ILN]†` — the accepted supplier; S0.1/S0.3 data.

---

### EFF.WELDMASTER.03  [theorem]

**CANONICAL STATEMENT — the frozen form** (verbatim, S1 L284–291). FORM: bold-headed theorem.

> “**THEOREM WELD-M.** For every tower T of the [ILN]† S0.1 class realized as
> a principal-read OM type via the dictionary map T(t) (JA-PIN — the DOMAIN
> identification, consumed: every class-pin clause forced by the print's own
> hypotheses, lift clause = W-2), there is ONE transport 𝕋 between the
> harness calculus (Φ-developments, weights w_m, reads R_m v, the ε-kit,
> canonical lifts) and the classifier/print calculus (φ-adic developments,
> v_{m+1}, residual operators, t(i)-twists, GMN keys), with five clauses:”

**⚠ SUPERSEDED.** The governing form is **`THEOREM WELD-M-PKG`** (`.22`). **CHAIN:** frozen S1 → F-1. **TERMINAL: F-1.** **SUPERSESSION KIND:** `replacement` (an existence-of-one-map claim replaced by an existence-of-one-record claim).

**DERIVATION.** “*Proof = the face table.*” (`.05`) — five citations, one per clause.

**CONDITIONALITY.** The **domain identification is consumed from JA-PIN** (`spec/EFF-GRTJA.md` `.04`), including its clause-8 lift consumption from W-2 — so WELD-M's domain inherits GRTJA's entire conditionality.

**XREF.** `GRTJA_PROOF_2026-08-08.md` — `THEOREM JA-PIN` present; `GRTW2_PROOF_2026-08-08.md` — EXISTS ✓.

---

### EFF.WELDMASTER.04  [definition]

**CANONICAL STATEMENT** (verbatim, S1 L293–312 — the five clauses `(M0)`–`(M4)`). FORM: indented display block.

> “    (M0) [substrate]      𝕋 is the IDENTITY on the underlying division
>          arithmetic: the same canonical division chain by the same monic
>          key at the admissible representative.
>     (M1) [gradings]       𝕋 preserves weights in the sheared frame
>          (w_m = v_{m+1}), and its induced map on polygon/grading data is
>          the explicit affine shear Σ_q(i,u) = (i, e_q·u + h_q·i) — lower
>          hulls to lower hulls, vertex-for-vertex, slope law s ↦ e_q s + h_q.
>     (M2) [reads/operators] 𝕋 acts on packed read data by explicit PER-SLOT
>          z-characters generated by ONE 1-cochain — the S0 weld cochain
>          t_j/ε_j: scalar per slot exactly on the S3 perimeter, exact
>          recursion ((HR-REC) genre) elsewhere. Never slot-independent
>          (JA-BOX-1 inherited verbatim).
>     (M3) [multiplication] 𝕋 transports multiplication up to the carry
>          cocycle: on B_n coordinates the target is the twisted group
>          algebra K^c[ℤ/E] with c = z̄^δ·∏_j z_j^{D_j} on the scored
>          stratum, and harness-val = TC-read.
>     (M4) [covariance]     𝕋 is covariant in the lift/representative
>          choice: changing the representative acts on (M2)–(M3) by the
>          (ξ, w) character system, with ψ-transport making the two chains
>          carry the same type.”

**DERIVATION.** [DEFINITIONAL] — **these five clauses become the five FIELDS of `WELD-M-PKG`** (`.22`), unchanged in content. **The T1 C7 anchors are exactly these five.**

**Arithmetic audit of (M1)'s shear.** `Σ_q(i,u) = (i, e_q u + h_q i)` is affine in `(i,u)` and fixes the abscissa ✓, so it maps lower hulls to lower hulls vertex-for-vertex ✓; the induced slope law is `s = Δu/Δi ↦ (e_q Δu + h_q Δi)/Δi = e_q s + h_q` ✓ **exactly as displayed**.

**CONDITIONALITY — per-clause, and this is the point of the package.** (M0) rides W-2 clause 1's perimeter and JB §0M rows 1–2; (M1) rides `P<q` with a **split** at (d3) to the stronger `P≤q` (`[r2]` F-1); (M2) rides JA-RES's per-digit proviso, (C-coll), W2-BOX-1's mod-wrap, and **JA-BOX-1's “PER-SLOT, never scalar”**; (M3) rides the [ILN]† scored stratum and JC's tier split; (M4) rides (C-coll) for the scalar closed form with **W2-OPEN-1 (value layer) OPEN — inherited displayed**. **Five clauses, five different perimeters** — which is precisely why F-1 says they “have different source/target types”.

**XREF.** `GRTJA_PROOF_2026-08-08.md` — `HE6`-style box names: `JA-BOX-1` (compiled at `spec/EFF-GRTJA.md` `.15`), `JA-RES`, `JA-VAL`, `JA-EPS`, `JA-GRID` all present ✓.

---

### EFF.WELDMASTER.05  [table]

**CANONICAL STATEMENT** — the S1 face table (L319–330), five rows, four columns (`clause | citation | identification displayed | conditionality carried verbatim`), closing “∎ (each row = consumption; **no row is re-proved here**).”

`[TABLE]` (compiler transcription of the citation column — the DAG's core edge set)

| clause | citation (the proof of the clause) | conditionality carried |
|---|---|---|
| (M0) | `[JB]† JB-DEV(i)` + `[JA]† JA-PIN` cl. 1–7 + `[W2]† W2-C1` | W-2 clause 1's perimeter; JB §0M rows 1–2; byte clause on `P≤q` only |
| (M1) | `[JA]† JA-VAL` + `[JB]† JB-AFF + JB-VTX` + `[JA]† JA-GRID` | `P<q`, `0 ≤ q ≤ qcap`, **split at (d3) to `P≤q`** (`[r2]`); JA-GRID(a)'s `u ≥ 0` proviso **NARROWED at JB r5**; JB-VTX(d4) NO-LEG rides |
| (M2) | `[JA]† JA-EPS` + `JA-RES` + `[W2]† W2-C2/(HR-REC)` | JA-RES's per-digit proviso at `m ≥ 3` / (R-coll); (C-coll) at `m ≥ 2`; W2-BOX-1 mod-wrap; **JA-BOX-1: PER-SLOT, never scalar** |
| (M3) | `[JC]† W-6/W-7/W-8/W-9` | [ILN]† scored stratum; (DMULT-w) CITED / (DMULT-s) MEASURED tier split; GRB §0 ladder; JC-BOX-2/-4/-8 |
| (M4) | `[W2]† W2-C2 + §3.4` + `W2-C3` | scalar closed form on (C-coll) only; §4.3 ℓ-orbit convention fence; **W2-OPEN-1 (value layer) OPEN — inherited displayed** |

**DERIVATION.** [RECORD] — “*Proof = the face table.* Each clause IS the cited composed theorem, at that theorem's own grade and on its own perimeter”.

**⚠ THE CRITICAL STRUCTURAL FACT, which F-1 later names.** The table proves **five separate propositions**; it supplies **no sixth theorem** saying they are restrictions of one map. F-1's words: “the five cited faces have different source/target types, and **the table supplies no additional theorem** asserting that they are restrictions of a single map.” **This compilation confirms it by inspection of the table**: every row's citation is a different theorem in a different note, and no row's citation is a compatibility statement ✓.

**CONDITIONALITY.** One `[r1]`/`[r2]` chain rides inside the (M1) row: JA-GRID(a)'s `u ≥ 0` proviso, ~~undischarged~~ → **NARROWED at JB r5 (`u ≥ 0` PROVED at `V ∈ 𝒲_{q+1}`; open residue = the membership hypothesis at the unbounded-degree quantifier)**. **Cross-check:** `spec/EFF-GRTJA.md` `.34` compiles GRTJA's own C2-4, which independently establishes that the print T-point needs `u(V) ≥ 0` ✓ — **the same proviso, found from two directions, and NARROWED here while being SCOPED there.** Recorded as a live cross-note consistency the DAG should carry.

**XREF.** `GRTW2_PROOF_2026-08-08.md` ✓; the `[JB]†`/`[JC]†` junction notes are cited by tag; `WELD_FACE_AUDIT.md` (`runs/qgen/`) EXISTS ✓ and is the audit that swept these faces.

---

### EFF.WELDMASTER.06  [scope-record]

**CANONICAL STATEMENT** (verbatim, S1's closing remark, L332–341). FORM: bold-headed remark.

> “**Remark (what WELD-M is and is not — the honest-content sentence,
> [SYN] §1.3 verbatim in substance).** WELD-M is an ORGANIZING UMBRELLA
> engineered so that the four composed junction theorems are its faces; it
> proves no new instance and discharges no open leg (W2-OPEN-1, the (VD-m)
> strata, (DMULT-s), JB's ~~rows 5/12/22~~ **rows 4/5/12/22 [r1, F-1 — the
> post-r4/r5 census: FOUR NO-LEG open-proviso rows, with rows 4/12 at the
> NARROWED membership residue …]** **[r2: census UNCHANGED at 4c4ee56 (post-r7)]** evidence
> frontier all ride). Its net
> new content is exactly two displays: S2 (the coboundary theorem — the one
> theorem-shaped new item) and S3 (the one-perimeter fence). What each
> junction note carries BEYOND its face (its closed forms, boundary censuses,
> box systems) remains in that note and is NOT absorbed.”

**DERIVATION.** [RECORD of the note's own honest-content statement].

**CONDITIONALITY — this is the sentence a chapter cut most needs.** “**it proves no new instance and discharges no open leg**”, and “Its net new content is **exactly two displays**”. **So of the note's ~86 KB, the new mathematics is S2 and S3** — which is exactly what the queue charge singles out (the package and the one-perimeter fence). The `[r1]`/`[r2]` bracket chain on the JB census (3 rows → 4 rows → unchanged at post-r7) is the note tracking a supplier's evidence frontier across two of its own repair rounds. `4c4ee56` ✓ (commit).

**XREF.** `GRTW2_PROOF_2026-08-08.md` — W2-OPEN-1 ✓.

---

### EFF.WELDMASTER.07  [theorem]

**CANONICAL STATEMENT** (verbatim, S2 L344–361 — THEOREM WM-COB clauses (i) and (ii)). FORM: bold-headed theorem with two clause displays.

> “**THEOREM WM-COB.** Let (γ, γ′) be a pair with [ILN]† S0.3 data
> (δ, S_j, D_j) and per-level split weights β_j := β_j(γ), β′_j := β_j(γ′),
> β″_j := β_j(γ+γ′).
>
> **(i) [integer form — footprint: [ILN]† SHIFT-(n) + D_j-INT only, both
> unconditional; γ, γ′ ∈ ℤ arbitrary, ALL pairs incl. DCX].** For every
> level j = 1..n:
>
>     D_j  =  t_j(β″_j) − t_j(β_j) − t_j(β′_j)   =: (∂t_j)(γ, γ′).
>
> **(ii) [field form — adds JA-EPS, PROVED in [JA]† at 0/2] [r1, F-4: as a
> field identity of the ε-kit, (ii) rides [ILN]† EXP-KIT(a) (β unrestricted,
> ACCEPTED) — JA-EPS enters only for the PRINT reading of the cochain, on the
> weight lattice; the sealed 0/2 pricing of (ii) was conservative, not
> upgraded here].** In K_j:
>
>     z_j^{D_j} = ε_j(β_j)·ε_j(β′_j)·ε_j(β″_j)^{−1}.”

**DERIVATION.** The note's “new 3-line theorem” — a complete proof in S2 (`.20`'s WM2 row: “PROVED (complete proof, S2)”).

**Mathematical audit of the (i) ⟹ (ii) step.** `ε_j(β) = z_j^{−t_j(β)}` (JA-EPS) turns `D_j = t_j(β″) − t_j(β) − t_j(β′)` into `z_j^{D_j} = z_j^{t_j(β″)}·z_j^{−t_j(β)}·z_j^{−t_j(β′)} = ε_j(β)·ε_j(β′)·ε_j(β″)^{−1}` ✓ **exactly as displayed** (note the sign bookkeeping: `ε_j(β″)^{−1} = z_j^{+t_j(β″)}`) ✓.

**⚠ CROSS-NOTE HAZARD, recorded.** The (ii)-derivation above uses the **raw** `ε_j(β) = z_j^{−t_j(β)}` equality — i.e. exactly the display `spec/EFF-GRTJA.md` `.31` shows to be **CRITICAL-corrected** (it holds only at `ν = 0`; the governing form is `JA-EPS-CORR`). **But the `[r1]` F-4 bracket already re-footed (ii) on `[ILN]† EXP-KIT(a)`**, with “JA-EPS enters only for the PRINT reading of the cochain, on the weight lattice” — and the weight lattice is where the attained-origin shift vanishes. **So (ii) survives the GRTJA CRITICAL**, by a re-footing made three months of campaign-time earlier and for an unrelated reason. Recorded because the escape is real but not obvious, and because no append of either note connects the two.

**CONDITIONALITY.** (i) is **unconditional** — “footprint: [ILN]† SHIFT-(n) + D_j-INT only, both unconditional; γ, γ′ ∈ ℤ arbitrary, ALL pairs incl. DCX”. **This is the note's strongest single result** and the only one with no perimeter.

**XREF.** `GRTJA_PROOF_2026-08-08.md` — `THEOREM JA-EPS` present, and its corrected form `JA-EPS-CORR` compiled at `spec/EFF-GRTJA.md` `.31`.

---

### EFF.WELDMASTER.08  [theorem]

**CANONICAL STATEMENT** — WM-COB clause (iii), the coboundary display `c_{n+1} = z̄^δ·∂E` on the scored stratum (S2, following (ii)). FORM: clause display.

**DERIVATION.** From (ii) plus `[ILN]†`'s value law; §S6's WM4 row: “PROVED on the scored stratum \| WM3 + [ILN]† value law”.

**CONDITIONALITY.** **Scored-stratum only.** This is the display the title advertises and the one `WM-CLASS` (`.10`) turns into a cohomological statement.

**XREF.** `[ILN]†` value law — consumed.

---

### EFF.WELDMASTER.09  [lemma]

**CANONICAL STATEMENT** (verbatim, S2.1 L409–419 — LEMMA WM-RHO with its proof). FORM: `###`-headed lemma.

> “For every γ ∈ ℤ:  E(γ + E)/E(γ) = ρ_T, the γ-INDEPENDENT constant
>
>     ρ_T := ∏_{j=1}^{n} z_j^{ −ℓ_{j−1}·E_{j−1} }.
>
> *Proof.* γ ↦ γ+E shifts β_n by E/e_n = E_n (top eq-12: s_{n+1} unchanged
> since ℓ_n E ≡ 0 mod e_n, u_{n+1} shifts by E/e_n), and inductively β_j by
> E_j (E_j ≡ 0 mod e_{j−1} keeps every s_j fixed, u_j shifts by E_{j−1}). So
> t_j(β_j + E_j) = t_j(β_j) + ℓ_{j−1}E_{j−1}, and each ε_j picks up the fixed
> factor z_j^{−ℓ_{j−1}E_{j−1}}. ∎ [Machine: WM-RHO.]”

**DERIVATION.** The span is the derivation — a two-step induction on the split weights.

**Mathematical audit.** The `s_j`-invariance step is the load-bearing one: `E_j ≡ 0 (mod e_{j−1})` keeps every `s_j` fixed, so only the `u_j` component shifts ✓, and then `t_j`'s definition `(ℓβ − s)/e` gives `t_j(β_j + E_j) = t_j(β_j) + ℓ_{j−1}E_j/e_{j−1} = t_j(β_j) + ℓ_{j−1}E_{j−1}` ✓ (using `E_j/e_{j−1} = E_{j−1}`). Each `ε_j = z_j^{−t_j}` then contributes `z_j^{−ℓ_{j−1}E_{j−1}}` ✓, and the product over `j` is `ρ_T` ✓ — **γ-independent because none of the shifts depends on γ** ✓. **The lemma is exactly right, and it is the note's second complete proof.**

**CONDITIONALITY.** Unconditional (S6/WM5: “PROVED (complete proof) \| S0 arithmetic”). **Machine-checked:** the WM-RHO family.

**XREF.** none external.

---

### EFF.WELDMASTER.10  [corollary]

**CANONICAL STATEMENT** (verbatim, S2.2 L421–450 — COROLLARY WM-CLASS, to its class display and its JC-F1 explanation). FORM: `###`-headed corollary.

> “Let σ : ℤ/E → W be the minimal window section (the engine's
> `window_reps`), Ẽ := E∘σ (a genuine ℤ/E-cochain), and κ(ν,ν′) :=
> (σν + σν′ − σ(ν+ν′))/E ∈ ℤ_{≥0} (the section carry; census S5) **[r1,
> F-8 — the ℤ_{≥0} warrant: integrality is immediate; non-negativity because
> σν + σν′ ∈ W ([ILN]† Lemma WINDOW-(n), PROVED generic unconditional) and σ
> picks the MINIMAL in-window representative of each class …]**. Then
> ∂Ẽ = ∂E·ρ_T^{κ} on classes, hence on the scored stratum …:
>
>     c(ν, ν′) = z̄^{δ(ν,ν′)} · ρ_T^{−κ(ν,ν′)} · (∂Ẽ)(ν,ν′),   i.e.
>     [c] = [ z̄^δ · ρ_T^{−κ} ]   in H²(ℤ/E, K^*).
>
> The full inner word ∏_j z_j^{D_j} is PURE GAUGE … **This explains
> FINDING JC-F1 cohomologically** … It is CONSISTENT with the cyclic
> fence: (CYC) is a property of the anchor-indexed PRESENTATION (the specific
> cochain), and coboundaries are exactly what presentations see and classes
> do not — the fence and the abstract cyclicity are the two sides of "cochain
> vs cohomology class", not a tension.”

**DERIVATION.** `∂Ẽ = ∂E·ρ_T^{κ}` from `.09`'s period constant applied to the section carry, then the class identity.

**Audit of the `κ ∈ ℤ_{≥0}` warrant** (the `[r1]` F-8 bracket): integrality is immediate from `σν + σν′ ≡ σ(ν+ν′) (mod E)` ✓; non-negativity needs `σν + σν′ ∈ W` (WINDOW-(n)) **and** `σ` minimal, giving `σ(ν+ν′) ≤ σν + σν′` ✓. **Both halves are needed and both are supplied** — a warrant that a weaker note would have left implicit.

**CONDITIONALITY.** **DISPLAY grade, review-owed** (S6/WM6). The reconciliation clause — that the cyclic fence and abstract cyclicity are “the two sides of ‘cochain vs cohomology class’, not a tension” — is the corollary's interpretive payload and **inherits the display grade with it**.

**⚠ Related supersession.** The acceptance record's PE10 gloss claims Ẽ is “THE canonical gauge … forced by **H¹ = 0 uniqueness**”; **F-2 withdraws that** (`.23`). **WM-CLASS itself is unaffected** — F-2's own words: “This affects only the certification narrative, not WM-CLASS's proved existence/class identity.”

**XREF.** `[JC]†` W-9, JC-F1 — cited by tag; `[ILN]†` Lemma WINDOW-(n) ✓ consumed.

---

### EFF.WELDMASTER.11  [fence]

**CANONICAL STATEMENT** (verbatim, S2.3 L451–465 — the residue fence). FORM: `###`-headed bold paragraph.

> “**J-B's C-READ constant c_m = ψ_{m,0} = −z_{m+1} is the OUTER letter, not
> a value of the weld cochain.** The level-≤m cochain generates only
> monomials in z_1..z_m; c_m is the NEXT residual letter — TC-3's defining
> relation at g_m = 1 (ψ_m(z̄) = 0 ⟺ z̄ = −ψ_{m,0}) surfacing as a transport
> constant … Assignment:
> c_m belongs to the z̄^δ family — the part WM-COB(iii) shows is NOT ∂E.
> Likewise the mod-wrap boundary (W2-BOX-1's I4B r = 5 witness) and the
> χ-chain at m ≥ 2 are NOT reduced to cochain values by this note — the wrap
> is where the scalar one-cochain picture fails, and it fails at the SAME
> boundary in all four notes (S3's caveat). FENCED; no claim otherwise.”

**DERIVATION.** A generation argument: the level-≤m cochain generates monomials in `z_1..z_m` only, and `c_m` is the **next** letter ✓ — so it cannot be a cochain value, and it is assigned to the `z̄^δ` family instead.

**CONDITIONALITY.** **This fence is what keeps WM-COB(iii)'s split honest**: `c = z̄^δ · ∂E` is a genuine decomposition only because the outer letter is provably outside the cochain's image. **And the note reports the boundary is shared**: “it fails at the SAME boundary in all four notes”. Recorded as a corpus-level structural observation with a named witness (W2-BOX-1's I4B `r = 5`).

**XREF.** `GRTW2_PROOF_2026-08-08.md` — W2-BOX-1 ✓.

---

### EFF.WELDMASTER.12  [definition]

**CANONICAL STATEMENT** (verbatim, S3 L468–481 — the character family, the seed identities and `(PERIM-μ)`). FORM: `##`-headed section with definition displays.

> “**Definition (the level-μ transport character family; μ ≥ 1).**
>
>     χ^{(μ)}_i := z_μ^{ −i·A_μ }   (i ∈ ℤ),      A_μ = ℓ_{μ−1}g_{μ−1}γ_μ.
>
> **Seed identities (the fence characters ARE cochain data; both consumed
> proved, [JA]† JA-EPS + W2-L2):** χ^{(μ)}_i = ε_μ(β + i·W_μ)/ε_μ(β) for
> every β (the key-lattice first difference: t_μ(β + iW_μ) = t_μ(β) + iA_μ,
> carry-free); and z_μ^{−A_μ} = ε_μ(W_μ) = c₁, Theorem-`phir`'s constant.
> [Machine: WM-FENCE-XI.]
>
> **(PERIM-μ on S), the one condition:** χ^{(μ)}_i = 1 for every i ∈ S,
> S ⊆ ℤ a set of abscissas. Absolute form: ord(z_μ) | A_μ (then S = ℤ);
> slot-grid form: ord(z_μ) | e_μ·A_μ (then S = e_μℤ). Absolute ⟹ slot-grid;
> the converse fails in general (the honest gap, censused in S5).”

**DERIVATION.** [DEFINITIONAL] plus two consumed seed identities.

**Mandatory arithmetic audit (rule 22).** The first-difference seed: `t_μ(β + iW_μ) = t_μ(β) + iA_μ` gives `ε_μ(β+iW_μ)/ε_μ(β) = z_μ^{−iA_μ} = χ^{(μ)}_i` ✓ **exactly**. The absolute form: `χ^{(μ)}_i = 1 ∀ i ∈ ℤ ⟺ z_μ^{A_μ} = 1 ⟺ ord(z_μ) | A_μ` ✓. The slot-grid form: restricting to `i ∈ e_μℤ` gives `z_μ^{−e_μ A_μ j} = 1 ∀j ⟺ ord(z_μ) | e_μ A_μ` ✓. **Absolute ⟹ slot-grid** since `A_μ | e_μA_μ`… precisely, `ord | A_μ ⟹ ord | e_μA_μ` ✓; **the converse fails** whenever `ord(z_μ) | e_μA_μ` but `ord(z_μ) ∤ A_μ` ✓ — a genuine gap, and the note censuses it in S5.

**CONDITIONALITY.** **The seed identities are what make this a fence about *cochain data* rather than an ad-hoc condition** — “the fence characters ARE cochain data”. They are consumed from JA-EPS + W2-L2. **Note the GRTJA hazard again**: JA-EPS is CRITICAL-corrected (`spec/EFF-GRTJA.md` `.31`), but the seed used here is a **first difference** `ε(β+iW)/ε(β)`, in which any `ν`-shift common to numerator and denominator cancels ✓ — **so the seed survives the correction structurally.** Recorded; not stated by either note.

**XREF.** `GRTJA_PROOF_2026-08-08.md` — `THEOREM JA-EPS` ✓; `GRTW2_PROOF_2026-08-08.md` — W2-L2 ✓.

---

### EFF.WELDMASTER.13  [theorem]

**CANONICAL STATEMENT** (verbatim, S3 L483–484 plus clause (a) at L484–524). FORM: bold-headed theorem with three lettered specializations.

> “**THEOREM WM-FENCE.** The three collapse fences of the weld are three
> specializations of (PERIM):
>
> **(a) [(R-coll), J-A].** JA-VDIND's absolute character-triviality at read
> level m — "z_{m−1}^{i·A_{m−1}} = 1 for every attained i" **[r5, PE5 F-1:
> quote restored to its byte-form at the ce0469a pin …]** — IS
> (PERIM-(m−1) on the attained abscissa set of the object). (R-coll) at level
> m is its every-recursion-node form ~~(2 ≤ q ≤ m−1)~~ **(2 ≤ q ≤ m,
> ROOT-INCLUSIVE — the q = m instance is JA-VDIND itself …)**”

with the `[r4]` JA-COMPANION bracket's finding, verbatim in its operative part:

> “**[JA]† §3.4's own quantifier "2 ≤ q ≤ m−1" (as read at the ce0469a pin) OMITS the root instance, and root-exclusively the display's "(VD-m) hold at all levels up to m" conclusion is FALSE — 8 machine witnesses at m = 3 sit ON the root-exclusive perimeter (every level-2 node character trivial) with (VD-3) FALSE (T3B/T3F/I4B; e.g. T3B deg 6, attained {1}, A₂ = 7), plus 58 at m = 2 where the stated range is EMPTY**”

**DERIVATION.** “*Proof: citation — [JA]† §3.4's display is this condition verbatim for the node instances 2 ≤ q ≤ m−1, JA-VDIND (quoted above) is the root instance; the root inclusion is JA PE2's one-line cure.*”

**⚠ THIS NOTE FOUND A DEFECT IN AN ACCEPTED SUPPLIER, WITH WITNESSES.** The `[r4]` bracket is a **JA-COMPANION finding**: `[JA]†`'s own quantifier omitted the root instance, making a displayed conclusion **FALSE**, with **8 machine witnesses at m = 3 and 58 more at m = 2 where the range is empty**. **Cross-verified:** `spec/EFF-GRTJA.md` `.16` compiles GRTJA's JA-BOX-2 `[JA-r2]` bracket, which records the same repair from the GRTJA side — “on the old root-exclusive perimeter this ‘evaluated … PROVED’ clause was FALSE — **8 machine witnesses**” ✓. **Both notes report 8 witnesses; the counts agree exactly.**

**CONDITIONALITY.** The `[r5]` PE5 F-1 repair restored the quoted JA-VDIND display “to its byte-form at the `ce0469a` pin”, correcting an interpolated word (“abscissa”) fused in from JA-RES's quantifier line — **semantically nil, and the note says so**. A quotation-fidelity repair of exactly the kind rule 1 exists to prevent.

**The `[r4]` CONSUMER CHECK is the model form.** “CONSUMER CHECK (this note): **no WM display consumed the root-exclusive range**” — with the citation cells enumerated and then corrected again at `[r5]` PE5 O-1 (“name list corrected to the actual citation cells”), and independently re-verified by “PE5's 8-site sweep”. **A blast-radius check that was itself audited and re-audited.**

**XREF.** `GRTJA_PROOF_2026-08-08.md` — `JA-VDIND` (compiled at `spec/EFF-GRTJA.md` `.09`), `JA-BOX-2` (`.16`) ✓. Commits `ce0469a`, `3626f55`, `9040961` — `3626f55` ✓ verified.

---

### EFF.WELDMASTER.14  [theorem]

**CANONICAL STATEMENT** (verbatim, S3's clause (b), L526–537). FORM: bold-headed clause.

> “**(b) [the (VD-m) proviso, J-B].** JB-CREAD's governing fence consumes
> JA-VDIND verbatim ([JB]† §5's regenerated fence display — the (VD-m) line
> of JB's internal [r3, STEP 2] block, citing [row 21]; §0M row 21's own (c)
> cell is the pointer "YES — the governing fence" **[r3, F-1: attribution
> corrected from "§0M column (c)", where the cell carries the same content in
> paren byte-form; quote + pin unchanged]** **[r2: as of 4c4ee56]**: "at every
> m ≥ 2 a HYPOTHESIS —
> JA-VDIND's ABSOLUTE character-triviality"); hence = (a) = (PERIM) at the
> same attained sets. Unconditional at m ≤ 1 only ((VD-1) = W2-L3). *Proof:
> citation of the r1/F1-repaired fence.*”

**DERIVATION.** Citation, with an attribution repair (`[r3]` F-1: “§0M column (c)” → “row 21's own (c) cell”).

**CONDITIONALITY.** “**Unconditional at m ≤ 1 only**” — the fence bites from `m = 2` up. **And clause (b) is where the root-inclusive form of (a) coincides with JB's operative fence**: the `[r4]` bracket says so — “Root-inclusive, the condition coincides with accepted [JB]'s operative fence (root triviality a HYPOTHESIS at every m ≥ 2) — **exactly (b) below**.” **So JB's independently-authored fence is what confirms JA's repaired quantifier is the right one.**

**XREF.** `[JB]†` — cited by tag; `4c4ee56` ✓ (commit).

---

### EFF.WELDMASTER.15  [theorem]

**CANONICAL STATEMENT** (verbatim, S3's clause (c), L539–551). FORM: bold-headed clause with a display.

> “**(c) [(C-coll), W-2].** (C-coll) demands every interior junction character
> trivial: ξ_Q = 1, ξ_Q = χ_{Q−1}^{γ_Q}·z_{Q−1}^{−e_{Q−1}A_{Q−1}} ([W2]†
> §3.4). AT TRIVIAL LOWER χ-CHAIN (χ_{Q−1} = 1 — e.g. every m = 2 junction,
> where χ_1 := 1):
>
>     ξ_Q = z_{Q−1}^{ −e_{Q−1}·A_{Q−1} } = χ^{(Q−1)}_{e_{Q−1}} = ϑ_Q,
>
> exactly JA-VDIND's slot character ϑ — so (C-coll) at that level is
> (PERIM-(Q−1) on the junction slot grid e_{Q−1}·ℤ): the SLOT-GRID
> specialization (W-2's junction slots sit at s + j·e_{Q−1}; the j-dependent
> character part is ϑ_Q^j). *Proof: read off the §3.4 display at χ = 1; the
> grid statement is [W2]† §3.2's letter-twist sentence.*”

**DERIVATION.** Read off W-2 §3.4's closed form at `χ_{Q−1} = 1`.

**Mathematical audit.** Setting `χ_{Q−1} = 1` in `ξ_Q = χ_{Q−1}^{γ_Q}·z_{Q−1}^{−e_{Q−1}A_{Q−1}}` gives `ξ_Q = z_{Q−1}^{−e_{Q−1}A_{Q−1}}` ✓, which is `χ^{(Q−1)}_i` at `i = e_{Q−1}` ✓ — **so (C-coll) at a trivial-lower-chain junction is exactly `(PERIM-(Q−1))` on the grid `e_{Q−1}ℤ`** ✓, the slot-grid form of `.12`. **The three fences really are three specializations of one condition, and the algebra confirms it.**

**CONDITIONALITY.** **Only at trivial lower χ-chain.** Off it, `ξ_Q` is “a χ-corrected character, not a bare `χ^{(μ)}` value” — `.16`'s caveat.

**XREF.** `GRTW2_PROOF_2026-08-08.md` — §3.2/§3.4 ✓.

---

### EFF.WELDMASTER.16  [fence]

**CANONICAL STATEMENT** (verbatim, S3's caveat, L553–560). FORM: bold-headed paragraph.

> “**The honest caveat (carried, not erased — [SYN] §1.3's own words).** The
> alignment is exact only MODULO the χ-chain corrections at deeper levels
> (χ_{Q−1} ≠ 1 makes ξ_Q a χ-corrected character, not a bare χ^{(μ)} value)
> and the MOD-WRAP boundary (W2-BOX-1's I4B r = 5 witness: a support-
> invisible interior character re-emerging through monomial reduction). The
> one-perimeter statement does NOT scalarize the wrap; the exact law off the
> perimeter remains (HR-REC) [W-2] / the per-slot recursion [J-A] — and the
> scalar forms of all four notes die together at this same boundary, which is
> the one-perimeter observation seen from the failure side.”

**DERIVATION.** [RECORD of the fence's exact limits].

**CONDITIONALITY — the caveat is the theorem's other half.** “the scalar forms of all four notes **die together at this same boundary**, which is **the one-perimeter observation seen from the failure side**.” **This is the sharpest sentence in the note**: the same condition that unifies the three fences also unifies their failures, and the unification is therefore not a bookkeeping convenience but a structural fact with a named witness (W2-BOX-1's I4B `r = 5`).

**⚠ SCOPE-CORRECTED downstream.** **F-4** (`.28`) narrows what a consumer may take from WM-FENCE: a **fixed-data criterion**, never a freedom or existence theorem. **The caveat itself survives unchanged** — F-4's sweep: “WM-FENCE(a), (b), and (c), including the root-inclusive repair and the χ-chain/mod-wrap caveats, **survive unchanged**.”

**XREF.** `GRTW2_PROOF_2026-08-08.md` — W2-BOX-1 ✓.

---

### EFF.WELDMASTER.17  [scope-record]

**CANONICAL STATEMENT** — S3's closing “THREE-MASTER-FENCE SURFACE” block (L562–600): “what a consumer of WELD-M carries, replacing four per-note fence systems as a READING AID — the four notes' [surfaces untouched]”.

**DERIVATION.** [RECORD of a consumer reading aid].

**CONDITIONALITY.** **Explicitly a reading aid, not a replacement**: S6/WM10 grades it “DISPLAY + FENCE”, and the block itself says the four notes' surfaces are untouched (N-7). **A consumer may use the three-fence surface to read, but must cite the four notes to prove.**

**XREF.** none.

---

### EFF.WELDMASTER.18  [scope-record]

**CANONICAL STATEMENT** (verbatim, §S4's one-line conditionality stack, L603–612). FORM: `##`-headed section, bold-headed one-liner.

> “**One line.**
> `[ILN]† ACCEPTED (SHIFT-(n)/D_j-INT unconditional; value law on the scored
> stratum; residuals (RM-m ≥ 2), (W-MULT-DCX-m), (ITER-LAW-LIFT) [r1, F-5],
> standard-lift) + W-2/J-A/
> J-B/J-C at ATTEMPT 0/2 each (their boxes verbatim: (C-coll)+W2-BOX-1+
> W2-OPEN-1; JA-BOX-1..5+(R-coll); JB §0M incl. rows 4/5/12/22 NO-LEG, 4/12
> at the narrowed membership residue [r1, F-1]; JC's
> (DMULT-w/s) tiers + GRB ladder + JC-BOX-2/4/8 + JC-F1/F2 review-owed) +`”

**DERIVATION.** [RECORD of the full conditionality stack in one line].

**CONDITIONALITY.** **“W-2/J-A/J-B/J-C at ATTEMPT 0/2 each” is stale at HEAD** — all four are since ACCEPTED (GRTJA's FC-2 records exactly this: “GRTW2 at `adc6cf3`, GRTJB + GRTJC at `83f47f4`, WELDMASTER at `66b3498`”, `spec/EFF-GRTJA.md` `.27`). **The acceptance record catalogues the direction**: “the O-1/fold understatements catalogued **conservative**” — i.e. the same FAVORABLE-direction rule GRTJA states (`spec/EFF-GRTJA.md` OPEN-CALL 2). **Three notes now, independently.**

**Compiler note on the stack's value.** Carrying every supplier's boxes **verbatim** in one line is what makes WELD-M's “PROVED-BY-CITATION” claims auditable: a consumer can read the whole conditionality of the umbrella without opening four notes. Recorded as reusable practice.

**XREF.** `GRTW2_PROOF_2026-08-08.md` ✓; `GRTJA_PROOF_2026-08-08.md` ✓ (JA-BOX-1..5 compiled at `spec/EFF-GRTJA.md` `.15`–`.18`).

---

### EFF.WELDMASTER.19  [instrument-record]

**CANONICAL STATEMENT** (verbatim, §S5 L679–693). FORM: `##`-headed section.

> “**Runner:** `verification/openmath/weldmaster_checks.py`. Commit 1 = this
> note + the runner, battery UNRUN on the full roster (pre-seal smoke on the
> 4-row subset {C2A, C2I, C3A, I4B} disclosed in the docstring: all families
> 0 violations, all four teeth firing); commit 2 appends the verdict below
> FROM the committed artifacts (`weldmaster_checks_output.txt` /
> `weldmaster_checks_results.json`).
>
> **Roster:** `grt_jc_probe.ROSTER`, imported FROZEN and md5-pinned (29
> towers: m ≤ 3, NR ≤ 4, p ∈ {2,3,5}, d₀ ≤ 3, wild e_j = p legs incl. p = 5,
> g_j = 2 branching rows, eq-char F_p[[t]] rows, flat legs, I4A/I4B/I4C) —
> the sealed J-C instrument's tower-construction path (`ITL.build_tower`)
> verbatim. ≥ 25 towers incl. wild legs, as charged.”

**DERIVATION.** [RECORD] — a two-commit seal with a disclosed 4-row pre-seal smoke.

**CONDITIONALITY.** **The roster is IMPORTED, not authored** — “the sealed J-C instrument's tower-construction path (`ITL.build_tower`) **verbatim**”. So the machine leg's independence from `[JC]†` is partial by construction, and the note says so. **`UNPINNED-VALUE`:** the roster is “md5-pinned” but **the hash is not displayed** ✓ recorded. `verification/openmath/weldmaster_checks.py` EXISTS ✓.

**Arithmetic audit.** “≥ 25 towers incl. wild legs, as charged” against a 29-tower roster ✓ — the charge is met with margin.

**XREF.** `verification/openmath/weldmaster_checks.py` ✓.

---

### EFF.WELDMASTER.20  [table]

**CANONICAL STATEMENT** — §S6's claims table (L828–840). FORM: Markdown table, eleven rows.

`[TABLE]` (transcribed)

| # | claim | status | rests on |
|---|---|---|---|
| WM1 | THEOREM WELD-M (M0)–(M4), faces = the four composed junction theorems | **PROVED-BY-CITATION** at the cited grades (0/2 each) | [W2]†+[JA]†+[JB]†+[JC]†, [ILN]† |
| WM2 | WM-COB(i) integer coboundary `D_j = ∂t_j` | **PROVED (complete proof, S2)** | [ILN]† SHIFT-(n)+D_j-INT (**unconditional**) |
| WM3 | WM-COB(ii) field form | PROVED | WM2 + JA-EPS ([JA]† 0/2) |
| WM4 | WM-COB(iii) `c_{n+1} = z̄^δ·∂E` | PROVED **on the scored stratum** | WM3 + [ILN]† value law |
| WM5 | LEMMA WM-RHO period constant `ρ_T` | **PROVED (complete proof)** | S0 arithmetic |
| WM6 | COROLLARY WM-CLASS `[c] = [z̄^δ·ρ_T^{−κ}]`; explains JC-F1 | **DISPLAY (review-owed)** | WM4+WM5+[JC]† W-9 |
| WM7 | residue fence: `c_m = −z_{m+1}` is the outer letter, NOT a cochain value | **FENCE (displayed)** | [JB]† §5, [JC]† §3.3, W2-BOX-1 |
| WM8 | WM-FENCE (a)/(b) | PROVED-BY-CITATION | [JA]† JA-VDIND, [JB]† §0M(c) |
| WM9 | WM-FENCE (c) | **PROVED (one line)** + caveat | [W2]† §3.2/§3.4/W2-BOX-1 |
| WM10 | three-master-fence surface as reading aid | DISPLAY + FENCE | S3 |
| WM11 | machine leg: 10 families + 4 teeth on the frozen 29-tower roster | [commit 2] | S5 |

**Mandatory arithmetic audit (rule 22) — the grade census.** Of eleven rows: **three carry complete proofs** (WM2, WM5, WM9 “one line”), **two are PROVED-BY-CITATION** (WM1, WM8), **three are PROVED conditionally on suppliers** (WM3, WM4 on the scored stratum, and WM1's faces), **three are DISPLAY/FENCE** (WM6, WM7, WM10), and one is the machine leg. **So the note's own table says exactly what `.06` says in prose: two-and-a-bit complete proofs, everything else consumed or displayed** ✓ — internal consistency confirmed.

**CONDITIONALITY.** **WM1's status is the one F-1 corrects** (`.22`): “S6/WM1 survives when read as ‘**WELD-M-PKG, fields (M0)–(M4), proved by citation**.’” **Every “0/2” in the “rests on” column is stale** (`.18`).

**XREF.** all four junction tags; `[ILN]†` ✓.

---

### EFF.WELDMASTER.21  [acceptance-box]

**CANONICAL STATEMENT** (verbatim, L1021–1042). FORM: starred heading plus paragraph.

> “**THIS NOTE IS ACCEPTED at 2/2** — PE9 (9384083, CLEAN; the 12-theorem
> Lean leg incl. bez_plus and the minus-form falsification; the
> sealed-declaration sweep finding no second convention item) and PE10
> (1f9d82e, CLEAN; the DIRECT-H²/bar-resolution route proving Ẽ is THE
> canonical gauge — constructed from the cocycle alone and forced by
> H¹ = 0 uniqueness — plus the generating-function/lattice-point route for
> WM-COB/WM-RHO; 18,707/0, six teeth). Full arc: composed → PE1 (0C/5G+3m)
> → r1 … → PE9 CLEAN → PE10 CLEAN.
> Ten passes, seven repairs. **WHAT IS ACCEPTED**: THEOREM WELD-M (the one
> transport, five faces, citation-table proof over the now-FOUR-accepted
> junction notes + pinned suppliers), WM-COB proved unconditional, WM-RHO,
> WM-CLASS at display grade, PERIM-μ with its three specializations — the
> weld's consolidation, at its displayed conditionality (W2-OPEN-2/OPEN-2a
> carried as the accepted W-2's fenced box; the O-1/fold understatements
> catalogued conservative).”

**DERIVATION.** [RECORD of an acceptance]. **SERIES:** member 3 — TERMINAL for the counter.

**⚠ TWO CLAUSES OF THIS RECORD ARE LATER CORRECTED.** (1) “**THEOREM WELD-M (the one transport, five faces…)**” → **F-1** (`.22`): the accepted object is `WELD-M-PKG`, and F-1's sweep item 4 says so explicitly — “The acceptance record's ‘THEOREM WELD-M’ is henceforth the accepted **package** theorem above; no uniqueness or extra coherence is part of that acceptance.” (2) “**forced by H¹ = 0 uniqueness**” → **F-2** (`.23`), withdrawn. **So the acceptance record is a corrected object twice over**, and a consumer must read it through both annexes.

**CONDITIONALITY.** **Ten passes, seven repairs — the longest arc in queue 8b.** PE9 carried a **12-theorem Lean leg** (with a *falsification*: “the minus-form falsification”), PE10 two decorrelated routes at 18,707/0 and six teeth. **And a decorrelated read still found three REAL findings four days later** — the same pattern as GRTJA (`spec/EFF-GRTJA.md` OPEN-CALL 3), and for the same reason: the findings are about what the *displays say*, not what the *legs compute*.

**PIN VERIFICATION.** `1f9d82e` ✓; `9384083` — **not in this compilation's verified batch**, recorded as unchecked. `66b3498` (this note's acceptance, per O-2) ✓, `17d2a32` (the six-note weld) ✓, `adc6cf3` ✓, `83f47f4` ✓.

**XREF.** none new.

---

### EFF.WELDMASTER.22  [changes-record]  **[CHARGE — the five-field package]**

**CANONICAL STATEMENT** (verbatim, the C6 F-1 annex, L1123–1166, to its package display, its lemma and its sweep). FORM: `>`-quoted dated annex with a LaTeX array.

> “**[ANNEX 2026-08-12, correction C6 F-1 — WELD-M re-displayed at its proved-and-consumed package scope.]** The frozen S1 wording “there is ONE transport \(\mathbb T\) … with five clauses” is stronger than its citation-table proof: the five cited faces have different source/target types, and the table supplies no additional theorem asserting that they are restrictions of a single map. The accepted live display is therefore:
>
> **THEOREM WELD-M-PKG (the anchored five-face transport package).** For every tower \(T\) on the frozen S1 perimeter, there exists one named package
> \[
> \mathbf T_T=(\mathbf T_{M0},\mathbf T_{M1},\mathbf T_{M2},
>              \mathbf T_{M3},\mathbf T_{M4})
> \]
> whose fields are exactly the frozen clauses:
>
> \[
> \begin{array}{ll}
> \mathbf T_{M0}:&\text{the identity of the two canonical division chains;}\\
> \mathbf T_{M1}:&w_m=v_{m+1}\text{ and the affine shear }
>                  \Sigma_q(i,u)=(i,e_qu+h_qi),\\
>                &\text{with the frozen vertex, slope, key and membership fences;}\\
> \mathbf T_{M2}:&\text{the per-slot read/operator character transport generated by }
>                  t_j/\varepsilon_j,\\
>                &\text{with exact recursion off the scalar perimeter;}\\
> \mathbf T_{M3}:&\text{the multiplication transport to }
>                  K^c[\mathbb Z/E]\text{ on the scored stratum;}\\
> \mathbf T_{M4}:&\text{the representative/lift covariance field at W2-C3’s
>                  accepted fenced scope.}
> \end{array}
> \]
>
> The shared anchors and identifications are precisely those displayed in the frozen face table: the same tower and canonical division chain, \(w_m=v_{m+1}\), the \(t_j/\varepsilon_j\) seed, harness-val \(=\) TC-read, and the displayed \(\psi\)-transport. **No compatibility beyond those displayed ties is asserted.**
>
> **RECORD-ASSEMBLY LEMMA.** If, for fixed \(T\), the supplier theorems prove the five field propositions \(P_{M0}(T),\ldots,P_{M4}(T)\), then they prove
> \[
> P_{M0}(T)\times P_{M1}(T)\times P_{M2}(T)\times
> P_{M3}(T)\times P_{M4}(T).
> \]
> **Proof.** Choose the five cited witnesses and apply the product/record constructor. The frozen face table supplies exactly those five witnesses, at its displayed per-field perimeters. \(\square\)
>
> Thus “one” means one named five-field package, not uniqueness and not a further unproved common-map coherence theorem. In particular, if \(\eta:\mathbb Z/E\to K^\ast\) is a character, replacing a gauge \(\widetilde E\) by \(\widetilde E\eta\) may produce a different package with the same \(M3\) coboundary; WELD-M-PKG asserts existence only.”

**DERIVATION.** The RECORD-ASSEMBLY LEMMA, whose proof is one line: “Choose the five cited witnesses and apply the product/record constructor.” **This is the honest content of a citation-table proof** — a product of five propositions, not a theorem about a map.

**Mathematical audit.** The lemma is trivially valid (a product of proved propositions is proved) ✓, and **the non-uniqueness witness is exact**: for a character `η`, `∂(Ẽη) = ∂Ẽ` (F-2's computation, `.23`), so `Ẽη` gives a different package with the same `M3` coboundary ✓ — **the same witness disproves both “ONE transport” (F-1) and “H¹ = 0 uniqueness” (F-2)**. Recorded: two findings, one counterexample.

**SUPERSESSION KIND:** `replacement`. **TARGETS:** `.03` (the frozen S1 theorem), `.20`'s WM1 row, `.21` (the acceptance record's WHAT-IS-ACCEPTED clause), and the title (`.01`).

**THE SEVEN-ITEM CONSUMER SWEEP, verified.** F-1 lists seven surviving consumers. **Item 7 is the load-bearing one and this compilation confirms it independently:** “No executed consumer disclosed in this note uses a cross-face compatibility theorem; \(M0\), \(M1\), and \(M2\) have no separately identified executed downstream consumption.” **Cross-check at T1:** `T1_GAUGE_COCYCLE_2026-08-12.md` L13 declares “the weld is used only at the proved scope of the five-field package WELD-M-PKG; **no cross-face coherence or uniqueness is asserted**” ✓, and L1074 “(C7) is only an interface map and scope fence” ✓. **T1 — the note's largest downstream consumer — independently honours exactly the scope F-1 establishes.**

**XREF.** `T1_GAUGE_COCYCLE_2026-08-12.md` — `WELD-M-PKG`, count **14** ✓; `spec/EFF-T1*.md` — count **21** ✓.

---

### EFF.WELDMASTER.23  [changes-record]

**CANONICAL STATEMENT** (verbatim, the C6 F-2 annex, L1173–1214). FORM: `>`-quoted dated annex with LaTeX.

> “**[ANNEX 2026-08-12, correction C6 F-2 — withdrawal of PE10's \(H^1=0\) uniqueness gloss.]** In the 2026-08-07 ACCEPTANCE RECORD, the phrase
>
> “the DIRECT-\(H^2\)/bar-resolution route proving \(\widetilde E\) is THE canonical gauge — constructed from the cocycle alone and forced by \(H^1=0\) uniqueness”
>
> is withdrawn and replaced by:
>
> “the DIRECT-\(H^2\)/bar-resolution route independently re-verifying the WM-CLASS cohomology-class identity; the displayed gauge is the explicit section gauge \(\widetilde E=E\circ\sigma\), determined relative to the already fixed weld cochain \(E\) and minimal-window section \(\sigma\), with no uniqueness-from-\(H^1\) claim.”
>
> **Derivation of the correction.** With the trivial action on \(K^\ast\),
> \[
> H^1(\mathbb Z/E,K^\ast)
>   =\operatorname{Hom}(\mathbb Z/E,K^\ast)
>   =\mu_E(K),
> \]
> which need not vanish. If \(F\) and \(G\) are two gauges with
> \(\partial F=\partial G\), put \(\eta=F/G\). Then … so \(\eta\) is a character. Conversely, for every character \(\eta\),
> \(\partial(F\eta)=(\partial F)(\partial\eta)=\partial F\).
> Hence gauges with a fixed coboundary form a torsor under
> \(\operatorname{Hom}(\mathbb Z/E,K^\ast)\), rather than being unique. For
> \(E=2\) and \(K=\mathbf F_5\), the character
> \(\eta(\nu)=(-1)^\nu\) is already a nontrivial witness.”

**DERIVATION.** The span is the derivation — a two-line torsor argument with an explicit minimal witness.

**Mandatory arithmetic audit (rule 22) — the whole derivation re-checked.** For the **trivial** action, `H¹(ℤ/E, K*) = Hom(ℤ/E, K*)` ✓, and a homomorphism `ℤ/E → K*` lands in the `E`-torsion, so `Hom = μ_E(K)` ✓ — **which is nonzero whenever `K` contains a nontrivial `E`-th root of unity.** The torsor step: `∂F = ∂G ⟹ ∂(F/G) = 1 ⟹ F/G` is a character ✓; conversely `∂(Fη) = ∂F·∂η = ∂F` ✓. **The witness:** `E = 2`, `K = F₅`; `μ₂(F₅) = {±1}` is nontrivial since `−1 = 4 ∈ F₅^×` has order 2 ✓, and `η(ν) = (−1)^ν` is a nontrivial character ✓. **The claim `H¹ = 0` is therefore false at the smallest interesting instance**, and the correction is exact.

**SUPERSESSION KIND:** `replacement` of an acceptance-record clause. **TARGETS:** `.21`.

**CONDITIONALITY — narrow blast radius, and it is verified.** F-2's own scoping: “This affects **only the certification narrative**, not WM-CLASS's proved existence/class identity.” **Confirmed at `.10`:** WM-CLASS's proof uses the explicitly defined `Ẽ = E∘σ`, WM-RHO, and `∂Ẽ = ∂E·ρ_T^κ` — **none of which needs uniqueness** ✓. The sweep also confirms `WELD-M-PKG`'s `M3` field asserts no gauge uniqueness ✓ (consistent with `.22`'s own η-remark).

**Compiler note.** **A false uniqueness claim inside a CLEAN acceptance pass's own summary.** PE10 was one of the two passes that granted acceptance; its route was correct as verification and wrong in one asserted reason. Recorded as the sharpest instance in queue 8b of a *pass narrative* needing correction while its *result* stands.

**XREF.** none new.

---

### EFF.WELDMASTER.24  [record]

**CANONICAL STATEMENT** (verbatim, the C6 F-3 disposition, L1215–1236 heading and the E6 frame's summary). FORM: `###`-headed finding.

> “### F-3 — RESOLVED-ALREADY”

with the E6 frame's characterization (L1103–1107):

> “and F-3 RESOLVED-ALREADY (the acceptance
> record's own scope qualifier)”

and the adjudication line (L1111):

> “Adjudication: F-1, F-2, and F-4 are REAL. F-3 is RESOLVED-ALREADY by the acceptance record's own scope qualifier and the theorem's explicit clause-level perimeter. **No finding is obstructed.**”

**DERIVATION.** [RECORD of a disposition].

**CONDITIONALITY.** **“No finding is obstructed”** — the explicit contrast with HE6R1, where two findings were REAL-but-OBSTRUCTED (`spec/EFF-HE6R1.md` `.42`). **And F-3 is resolved by the note's own defensive wording**: the acceptance record's scope qualifier plus the theorem's clause-level perimeter. **A case where prior caution pre-empted a finding** — recorded as the positive counterpart to `.21`'s two corrected clauses.

**XREF.** none.

---

### EFF.WELDMASTER.25  [changes-record]  **[CHARGE — the one-perimeter fence's consumption scope]**

**CANONICAL STATEMENT** (verbatim, the C6 F-4 annex, L1237–1290). FORM: `###`-headed finding plus a `>`-quoted annex.

> “### F-4 — REAL
>
> WM-FENCE proves a criterion for fixed data, not freedom to alter the data until the criterion holds. The phrase “WM-FENCE’s (F-b)-freedom observation” therefore misattributes any required existence or variation theorem. The “through J-D0” qualification limits the blast radius but does not cure the attribution.
>
> > **[ANNEX 2026-08-12, correction C6 F-4 — J-D consumes an F-b criterion, not freedom supplied by WM-FENCE.]** The frozen S6 downstream-design clause
> >
> > “the J-D block … consumes WM-FENCE’s (F-b)-freedom observation only through the already-adjudicated (J-D0) route”
> >
> > is replaced by:
> >
> > “the J-D block (J-D0 \(\to\) W-10 at the \((2,1,1)\) shape \(\to\) W-11) may consume WM-FENCE only as the fixed-data (F-b) character-triviality criterion, and only through the already-adjudicated J-D0 route. **WM-FENCE supplies no freedom or existence theorem** for varying \(z_\mu\), \(A_\mu\), the attained set, or the representative. Any such choice or variation required by J-D must be proved by J-D0 independently.”
> >
> > **Derivation.** For the fixed data, WM-FENCE proves exactly
> > \[
> > \chi_i^{(\mu)}=1\ \forall i\in\mathbb Z
> > \iff \operatorname{ord}(z_\mu)\mid A_\mu
> > \]
> > in the absolute form, and
> > \[
> > \chi_i^{(\mu)}=1\ \forall i\in e_\mu\mathbb Z
> > \iff \operatorname{ord}(z_\mu)\mid e_\mu A_\mu
> > \]
> > in the slot-grid form. **Neither display contains an existential
> > quantifier over the fixed inputs.** For example, with
> > \(K_\mu=\mathbf F_5\), \(z_\mu=2\), \(\operatorname{ord}(z_\mu)=4\),
> > \(A_\mu=1\), and attained set containing \(1\),
> > \[
> > \chi^{(\mu)}_1=2^{-1}\ne1,
> > \]
> > so absolute PERIM fails, and WM-FENCE specifies no operation that changes
> > this datum.”

**DERIVATION.** A quantifier reading: both displayed criteria are **iff-statements about fixed data**, with no existential over the inputs — plus a witness showing the criterion can simply fail with nothing to be done about it.

**Mandatory arithmetic audit (rule 22) — the witness re-derived.** `K_μ = F₅`, `z_μ = 2`: the order of `2` in `F₅^× = {1,2,3,4}` is `4` (since `2¹=2, 2²=4, 2³=3, 2⁴=1`) ✓. With `A_μ = 1` and `i = 1` in the attained set, `χ^{(μ)}_1 = z_μ^{−1·A_μ} = 2^{−1} = 3` in `F₅` ✓ **≠ 1** ✓ — so absolute PERIM fails ✓, and `ord(z_μ) = 4 ∤ A_μ = 1` ✓ consistent with the criterion. **The witness is exact and minimal.**

**SUPERSESSION KIND:** `replacement` of a downstream-design clause. **TARGETS:** the S6 J-D design sentence.

**CONDITIONALITY — the blast radius is nil and verified.** “The correction touches only the **unexecuted** S6 J-D design sentence. **No executed downstream consumption identified at HEAD relies on an WM-FENCE freedom theorem.**” And the forward instruction is precise: “A future J-D proof must cite J-D0 for any required existence or variation and WM-FENCE only to test the resulting character-triviality condition.”

**This is the second half of the queue charge.** T1's C7 consumes WM-FENCE, and **F-4 fixes what that consumption may be**: a fixed-data criterion, never a source of freedom. **A DAG edge into WM-FENCE must carry this scoping.**

**XREF.** `JD0_PROOF_2026-08-08.md` — EXISTS ✓ (the J-D0 route the correction routes existence to).

---

### EFF.WELDMASTER.26  [changes-record]

**CANONICAL STATEMENT** (verbatim, the 2026-08-08 O-1 annex, L1046–1067). FORM: `>`-quoted dated annex.

> “**[ANNEX 2026-08-08, orchestrator, fold-carry closure O-1 — the (M4)
> W2-C3 annotation owed since W2 r1.]** … wherever
> this note cites W2-C3, the supplier's accepted form governs — GRTW2
> ACCEPTED 2/2 at adc6cf3 (2026-08-07), W2-C3 at its honest grade
> (proved-to-first-twist, 38/44 = 36 derived + 2 instance-warranted;
> MEASURED above), riding **W2-OPEN-2/OPEN-2a as the accepted supplier's
> fenced OPEN box** (accepted AS A BOX, not as a theorem). The pin makes the
> frozen row drift-proof and PE1 adjudicated that the face carries the
> conditionality BY CITATION — nothing is falsified; the supplier's
> acceptance record names this face ("WM's M4 face … now ride[s] an ACCEPTED
> supplier at its fenced scope"). The parallel GRTJB §0M row-15 half of the
> same owed pair was executed at 5054d69 (JB erratum E-2). Direction:
> FAVORABLE on the supplier's status; the grade split itself is a narrowing
> already carried by citation, hereby annotated.”

**DERIVATION.** [RECORD of an owed annotation, executed].

**Mandatory arithmetic audit.** `38/44 = 36 derived + 2 instance-warranted` ✓ — and `44 − 38 = 6` MEASURED above, matching GRTJA's independent record of the same split (`spec/EFF-GRTJA.md` `.21`: “W2-C3 proved-to-first-twist (38/44) / measured-above (6/44)”) ✓. **Two notes, the same split, same denominator.**

**CONDITIONALITY.** **The reciprocity check again**: “the supplier's acceptance record **names this face**”. And the distinction “accepted **AS A BOX, not as a theorem**” is the honest reading of `W2-OPEN-2/OPEN-2a`. `adc6cf3` ✓, `5054d69` ✓.

**XREF.** `GRTW2_PROOF_2026-08-08.md` ✓.

---

### EFF.WELDMASTER.27  [changes-record]

**CANONICAL STATEMENT** (verbatim, the O-2 annex, L1069–1094). FORM: `>`-quoted dated annex enumerating five stale-billing site classes and one favourable supersession.

> “**[ANNEX 2026-08-08, O-2 — the grown conservative understatements.]**
> The frozen body's supplier billings all understate, in the conservative
> direction, statuses that have since improved. The catalogued sites (PE4's
> four, fold-owed; PE6's stale dagger parentheticals; PE10 observation 2):
> the grade-cap sentence …; the S4 one-line stack …; the WM1 cell …; the S6 dependency line …; and the dagger-line parentheticals … As-of truth:
> ALL FOUR junction notes are ACCEPTED at 2/2 — GRTW2 adc6cf3, GRTJA
> 17d2a32 (freeze cba309ce @ f131c53), GRTJB + GRTJC 83f47f4 — as is this
> note (66b3498) and the full six-note weld (17d2a32, 2026-08-07); the four
> hostile arcs have all CLOSED. Additionally, the S4 clause "(DMULT-s) stays
> MEASURED-only" is superseded FAVORABLY: (DMULT-s) is PROVED-conditional on
> the measured perimeter … by
> `DULEMMA_PROOF_2026-08-08.md` THEOREM DU (∂u ≡ 1; two-commit seal
> 048cf6d/f819f26, adjudicated e3535d5, riding [ILN]†+GRTJC+WELDMASTER as
> accepted plus DU-A/DU-B at 0/2; the e_m = 1 row stays measured,
> off-stratum pairs open). Every quoted billing was true when frozen and is
> now strictly conservative. Direction: FAVORABLE at every site; no display
> moves.”

**DERIVATION.** [RECORD of a five-class staleness catalogue with a direction judgement].

**CONDITIONALITY — the model form again.** “**Every quoted billing was true when frozen and is now strictly conservative. Direction: FAVORABLE at every site; no display moves.**” **This is the same discipline GRTJA's FC-2 applies** (`spec/EFF-GRTJA.md` `.27`) and the converse of HE6R1's PE3 F-2 rule. **Three notes now state it.** See OPEN-CALL 2.

**PIN VERIFICATION.** `adc6cf3` ✓, `17d2a32` ✓, `83f47f4` ✓, `66b3498` ✓, `048cf6d` ✓, `5054d69` ✓; `f131c53` ✓ — **and the cross-note freeze claim checks**: GRTJA's frozen-body md5 `cba309ce` @ `f131c53` is independently verified in `spec/EFF-GRTJA.md` §1 to localize at line 850 ✓. `e3535d5` and `f819f26` not in this batch, recorded as unchecked.

**XREF.** `DULEMMA_PROOF_2026-08-08.md` — cited; `GRTW2_PROOF_2026-08-08.md` ✓; `GRTJA_PROOF_2026-08-08.md` ✓.

---

### EFF.WELDMASTER.28  [record]

**CANONICAL STATEMENT** (verbatim, the E6 provenance paragraph's closing, L1284–1290). FORM: italic parenthetical.

> “*(The adjudication above was produced by the E6 certification run
> (gpt-5.6-sol high, runs/cert2/E6_weldmaster_output.log, 2026-08-12);
> orchestrator verification before transcription — F-1: the package
> re-display matches the frozen face table's five heterogeneously
> typed rows, and the η-gauge instance (Ẽη has the same M3 coboundary)
> refutes uniqueness … F-2: the torsor derivation
> re-checked (H¹(ℤ/E, K*) = Hom = μ_E(K); ∂(Fη) = ∂F for characters η;
> gauges with fixed coboundary form a Hom-torsor — at E = 2, K = F₅
> the character η(ν) = (−1)^ν is nontrivial, exactly the C6 witness).
> F-4: WM-FENCE's two displayed criteria re-read — both are fixed-data
> iff-statements with no existential quantifier, and the F₅ instance
> (z = 2, ord 4, A = 1: χ₁ = 2^{−1} ≠ 1) confirms no freedom is
> supplied. Transcribed unaltered per the standing division of labor.
> Ledger: row 24 → three repairs folded, awaiting sol discharge
> (cert3).)*”

**DERIVATION.** [RECORD of a verification protocol]. **SERIES:** member 4 — with a stale terminal.

**Compiler re-verification of all three orchestrator legs.** **F-1** ✓ (`.22`: the face table's five rows are heterogeneously typed, and the η-witness refutes uniqueness). **F-2** ✓ (`.23`: `H¹ = Hom = μ_E(K)`, the torsor step, and the `E = 2, K = F₅` witness, all re-derived). **F-4** ✓ (`.25`: both criteria are fixed-data iffs, and `χ₁ = 2^{−1} = 3 ≠ 1` in `F₅`). **Three of three re-derived and confirmed.**

**LEDGER STATUS — the closing claim is superseded at HEAD.** “awaiting sol discharge (cert3)” is stale: `docs/SOL_CERTIFICATION_LEDGER_2026-08-12.md` **row 24** reads

> “\| 24 \| WELDMASTER \| never sol-read as-built \| **CERTIFIED 2026-08-12** — E6 repairs (WELD-M-PKG, H¹-torsor withdrawal, WM-FENCE scoping) **DISCHARGED 3/3 at D3e** \|”

✓ — **3/3, all three discharged**, matching F-1/F-2/F-4 exactly. `STALE-SELF-DESCRIPTION` in the direction of resolution.

**XREF.** `runs/cert2/E6_weldmaster_output.log` ✓; `runs/cert/C6_weldmaster_output.log` ✓; `docs/SOL_CERTIFICATION_LEDGER_2026-08-12.md` row 24 ✓.

---

### EFF.WELDMASTER.29  [scope-record]

**CANONICAL STATEMENT** — the C6 charge's own framing (verbatim, L1098–1101): the sol read is “the **WELD-FACE-AUDIT** charge”, and it “returned CHALLENGE (3 GAP + 1 MINOR, **no CRITICAL in the component formulas**)”.

**DERIVATION.** [RECORD].

**CONDITIONALITY.** **“no CRITICAL in the component formulas” is a precise and favourable finding**: the three REAL findings are all about **scope and narrative** (a package vs a map, a uniqueness gloss, a consumption attribution) — **not one is a wrong formula.** Contrast GRTJA, where the same wave produced a CRITICAL against a displayed identity (`spec/EFF-GRTJA.md` `.31`).

**The WELD-FACE-AUDIT is a separate artifact.** `runs/qgen/WELD_FACE_AUDIT.md` EXISTS ✓ and is referenced corpus-wide (e.g. `spec/HYPOTHESIS_LEDGER.md` HYP.139's v4 note cites `COR-WFA-13`). Recorded as a cross-reading whose findings are ledgered elsewhere.

**XREF.** `runs/qgen/WELD_FACE_AUDIT.md` ✓; `spec/HYPOTHESIS_LEDGER.md` ✓.

---

### EFF.WELDMASTER.30  [record]

**CANONICAL STATEMENT** — §S6's dependency line and the machine-leg row WM11 (L826–840 tail), consumed as the note's outbound edge list: “WELD-M ⟵ all four junction notes at 0/2 (the grade cap)”; “the four hostile ARCS remain the campaign's gating work”.

**DERIVATION.** [RECORD].

**CONDITIONALITY.** **Both quoted clauses are stale and catalogued as such by O-2** (`.27`) — the four arcs have all CLOSED. **The dependency direction is permanent**; only the grades moved.

**XREF.** none new.

---

## 4. XREF verification ledger

`[TABLE]`

| # | target | object | status | consumed at |
|---|---|---|---|---|
| X1 | `GRTW2_PROOF_2026-08-08.md` | W2-C1/C2/C3, W2-L2/L3, §3.2/§3.4, W2-BOX-1, W2-OPEN-1/2, (C-coll) | EXISTS ✓ | `.04`, `.05`, `.11`, `.12`, `.15`, `.16`, `.18`, `.26` |
| X2 | `GRTJA_PROOF_2026-08-08.md` | JA-PIN, JA-VAL, JA-EPS, JA-GRID, JA-RES, JA-VDIND, JA-BOX-1..5, (R-coll) | EXISTS ✓ | `.03`–`.05`, `.07`, `.12`–`.14`, `.18` |
| X3 | `[JB]†` (GRTJB) | JB-DEV(i), JB-AFF, JB-VTX, JB-CREAD, §0M rows 1–2/11/15/21, rows 4/5/12/22 NO-LEG | cited by tag | `.05`, `.06`, `.11`, `.14` |
| X4 | `[JC]†` (GRTJC) | W-6/W-7/W-8/W-9, JC-LOC, JC-F1/F2, JC-BOX-2/4/8, (DMULT-w/s) | cited by tag | `.05`, `.10`, `.11` |
| X5 | `[ILN]†` | S0.1/S0.3, SHIFT-(n), D_j-INT, EXP-KIT(a), Lemma WINDOW-(n), the value law, the scored stratum | ACCEPTED supplier | `.02`, `.07`–`.10`, `.18` |
| X6 | `T1_GAUGE_COCYCLE_2026-08-12.md` | the (C7) interface | **`WELD-M-PKG` count 14** ✓ | §1 charge, `.22` |
| X7 | `spec/EFF-T1*.md` | the compiled T1 shards | **`WELD-M-PKG` count 21** ✓ | §1 charge |
| X8 | `JD0_PROOF_2026-08-08.md` | the J-D0 route F-4 routes existence to | EXISTS ✓ | `.25` |
| X9 | `DULEMMA_PROOF_2026-08-08.md` | THEOREM DU (∂u ≡ 1) | cited | `.27` |
| X10 | `runs/qgen/WELD_FACE_AUDIT.md` | the face audit | EXISTS ✓ | `.29` |
| X11 | `verification/openmath/weldmaster_checks.py` | the sealed runner | EXISTS ✓ | `.19` |
| X12 | `runs/cert/C6_weldmaster_output.log`, `runs/cert2/E6_weldmaster_output.log` | the sol read and adjudication | both EXIST ✓ | `.22`–`.28` |
| X13 | `docs/SOL_CERTIFICATION_LEDGER_2026-08-12.md` | row 24 | located ✓ | `.28` |

**Zero NEAR-MISS, zero invented names, zero designation OPEN-CALLs.** **Recorded limit:** four suppliers (`[W2]†`, `[JA]†`, `[JB]†`, `[JC]†`) are cited by **dagger tag** rather than by filename in the body's face table; two resolve to in-repo files verified above, and `[JB]†`/`[JC]†` are cited by tag throughout with as-of commit pins (`4c4ee56`, `3870bc8`) rather than paths. **The tag-to-file binding is in the note's dagger line**, which this compilation did not separately extract.

**PIN VERIFICATION (rule 23).** **Commits — 24 distinct 7-hex tokens in the note were tested; all 24 resolve to `commit`**, including every one quoted in this specification (`adc6cf3`, `17d2a32`, `83f47f4`, `66b3498`, `f131c53`, `5054d69`, `4c4ee56`, `3870bc8`, `3626f55`, `048cf6d`, `1f9d82e`, `5463f2a`, …). **Zero failures.** Three tokens quoted in appends were **not** in the tested set and are recorded as unchecked: `9384083`, `e3535d5`, `f819f26`.

**md5 pins — ZERO displayed.** **This note displays no md5 anywhere** — not for its frozen body, not for its runner, not for its artifacts, not for the “md5-pinned” roster. **Disposition: `UNPINNED-VALUE` throughout.** It is the only note in queue 8b with no verifiable hash, and it is worth stating plainly: **the freeze, the seal and the roster pin are all asserted rather than checkable.** *(The one hash it does quote is another note's — GRTJA's `cba309ce`, which this compilation verified at `spec/EFF-GRTJA.md` §1.)*

**Artifacts — 5 cited, 5 exist.**

---

## 5. Consumption table and consumer edges

### 5.1 Forward edges

Five faces, five citation bundles — the table at `.05` **is** the forward edge set. Grades at composition were 0/2 for all four junctions; **all four are ACCEPTED 2/2 at HEAD** (`.27`).

### 5.2 Reverse-XREF rows — who consumes WELDMASTER

`[TABLE]`

| # | consumer | what is consumed | status |
|---|---|---|---|
| RX-1 | `T1_GAUGE_COCYCLE_2026-08-12.md` **(C7)** | `WELD-M-PKG`'s five fields (chiefly `M2`'s read fence and `M3`) and `WM-FENCE` as a fixed-data criterion; **explicitly at package scope, with no cross-face coherence** | **LANDED** — count 14, four scope declarations quoted in §1 |
| RX-2 | `DULEMMA_PROOF_2026-08-08.md` | **only the `M3` field**, with its own DU-A/DU-B argument supplied | LANDED (per F-1's sweep item 6) |
| RX-3 | the J-D block (J-D0 → W-10 → W-11) | `WM-FENCE` as a criterion only, **through J-D0** | **design-stage; scoped by F-4** (`.25`) |
| RX-4 | `runs/qgen/WELD_FACE_AUDIT.md` | the five faces, audited | LANDED (the C6 charge) |
| RX-5 | `docs/SOL_CERTIFICATION_LEDGER_2026-08-12.md` row 24 | certification status | LANDED — CERTIFIED, 3/3 at D3e |

**Zero-orphan check.** F-1's sweep item 7 states — and T1's own text confirms — that **`M0`, `M1` and `M2` have no separately identified executed downstream consumption** beyond T1's interface. So the executed consumer set is `{T1 (C7), DULEMMA (M3 only)}`, with J-D at design stage.

### 5.3 The C7 anchor summary (the queue charge)

**What T1 may consume, after F-1 and F-4:** the five named fields of `WELD-M-PKG` **individually**, at their individual perimeters (`.04`, `.05`); and `WM-FENCE`'s `(PERIM-μ)` **as a criterion on fixed data** (`.12`–`.16`, scoped by `.25`). **What T1 may not consume:** a single cross-face map, any coherence beyond the displayed anchors, gauge uniqueness, or any freedom/existence statement about `z_μ`, `A_μ`, the attained set or the representative. **T1's (C7) declares exactly this fence in its own words** (§1) — **so the consumption is already correctly scoped at the consumer, and the DAG edge can be keyed field by field.**

---

## 6. TEETH — the inverse table (rule 6)

### 6.1 and 6.2 — battery ↔ units

The machine leg is “10 families + 4 teeth on the frozen 29-tower roster, two-commit sealed” (WM11), with named families including **WM-RHO** (guards `.09`), **WM-FENCE-XI** (guards `.12`'s seed identities) and the κ census (guards `.10`). **The verdict block itself sits in the note's §S5 commit-2 append**, which this compilation records as present but does not re-tabulate family by family, since the note's claims table (`.20`) already keys every claim to its evidence class.

`[TABLE]` — unit → guard

| unit | guarded by | class |
|---|---|---|
| `.04` the five clauses | the face table's citations, not a leg | **CITATION-carried** |
| `.07`/`.08` WM-COB | the generating-function/lattice-point route (PE10) | decorrelated |
| `.09` WM-RHO | **WM-RHO family** + PE10's route | battery-guarded |
| `.10` WM-CLASS | the κ census; “the class identity itself is arithmetic … no separate leg” | **DISPLAY, arithmetic-only** |
| `.11` the residue fence | [JC]† §3.3's germ split (27 stages CONFIRMED / 138-of-669 REFUTED as inner-factor) | externally guarded |
| `.12` the seed identities | **WM-FENCE-XI** | battery-guarded |
| `.13`–`.15` WM-FENCE (a)(b)(c) | citation; **plus the 8+58 JA-companion witnesses** | citation + witness-guarded |
| `.16` the caveat | W2-BOX-1's I4B `r = 5` witness | witness-guarded |
| `.22`–`.28` appends | — | **AUDIT-produced** |

### 6.3 PROOF-ONLY / CITATION-carried (rule 16)

The note's own coverage sentence is `.06`: “it proves no new instance and discharges no open leg … Its net new content is exactly two displays.” **So the whole of `.03`–`.05` is CITATION-carried, by design**, and only `.07`–`.10` and `.12`–`.16` carry the note's own content. **This is the queue's clearest instance of a unit whose honest status is “umbrella”** — and the note says so in its own title-adjacent remark.

### 6.4 AUDIT dispositions (rule 27)

| unit | AUDIT class | why no tooth applies |
|---|---|---|
| `.13`'s `[r4]` JA-companion | `transfer audit` | a supplier's **quantifier** omitted the root instance; 8 + 58 witnesses, but the *finding* is a reading |
| `.13`'s `[r5]` PE5 F-1 | `in-house hostile pass` | a quotation-fidelity repair (an interpolated word) |
| `.26`/`.27` O-1, O-2 | `transfer audit` ×2 | supplier motion and grade billing |
| `.22` F-1 | `decorrelated-model audit` | a type-level scope defect: five heterogeneous faces ≠ one map |
| `.23` F-2 | `decorrelated-model audit` | a **false cohomological claim inside an acceptance pass's own summary** |
| `.25` F-4 | `decorrelated-model audit` | a quantifier misattribution (criterion read as freedom) |

**Six audit-produced corrections; none from a tooth**, and the sealed leg was GREEN throughout.

### 6.5 Zero-orphan check

**30 units, 6 battery- or witness-guarded, 24 otherwise accounted:** 3 citation-carried faces, 2 fences, 5 records/tables, 4 supplier/scope records, 10 append units covered by §4/§5. **No unit silently unguarded.**

---

## 7. Arithmetic and fidelity audit

### 7.1 Arithmetic audit

**Confirmed exactly:** `.04`'s shear slope law `s ↦ e_q s + h_q` ✓; `.07`'s (i) ⟹ (ii) exponent bookkeeping ✓; `.09`'s WM-RHO induction, including the `s_j`-invariance step and `E_j/e_{j−1} = E_{j−1}` ✓; `.10`'s `κ ∈ ℤ_{≥0}` warrant (both halves) ✓; `.12`'s first-difference seed, both PERIM forms, and the strict absolute ⟹ slot-grid implication with its failing converse ✓; `.15`'s specialization `ξ_Q = χ^{(Q−1)}_{e_{Q−1}}` at `χ_{Q−1} = 1` ✓; `.19`'s “≥ 25 towers” against 29 ✓; `.20`'s grade census (3 complete proofs, 2 by citation, 3 conditional, 3 display) ✓ matching `.06`'s prose; `.22`'s record-assembly lemma and its η-witness ✓; **`.23`'s full torsor derivation with the `E = 2, K = F₅`, `η(ν) = (−1)^ν` witness** ✓; **`.25`'s `F₅` witness `χ₁ = 2^{−1} = 3 ≠ 1` with `ord(2) = 4`** ✓; `.26`'s `38 = 36 + 2` and `44 − 38 = 6` ✓, matching GRTJA's independent record.

**Cross-note agreements verified:** the **8 machine witnesses** of the root-exclusive defect are reported identically here (`.13`) and at GRTJA (`spec/EFF-GRTJA.md` `.16`) ✓; the **W2-C3 38/44 + 6/44 split** likewise ✓; **GRTJA's frozen-body md5 `cba309ce` @ `f131c53`** is quoted here (`.27`) and verified there ✓.

**Findings — two, neither a mathematical error:**

**AF-1 — the note displays no md5 of its own.** Its freeze, its seal and its “md5-pinned” roster are all asserted without a checkable value (§4). **The only hash it quotes is another note's.** Recorded as the queue's weakest pin posture.

**AF-2 — three cited commits were not verified** (`9384083`, `e3535d5`, `f819f26`), against 24 that were. Recorded.

### 7.2 Fidelity audit

- Every CANONICAL STATEMENT is verbatim or `[ASSEMBLED]`; **no `[ASSEMBLED]` statement appears**.
- **No exponent, quantifier, grade, perimeter, count or witness was altered.** Where a quantifier is discussed as *wrong* (`.13`'s root-exclusive range, `.25`'s freedom-vs-criterion), the note's or E6's own words are quoted.
- **Truncations: five**, at `.03`, `.05`, `.06`, `.21` and `.25`, each marked and each naming what was cut.
- **Rule 14 not triggered** — no fenced ASCII table (checked, not skipped). Three Markdown tables transcribed as `[TABLE]`.
- **Rule 9 (sharding) consciously overridden**, with the reasoning recorded in §1 rather than left implicit.
- **24 commits verified, 3 recorded unchecked; 0 md5s available; 5 artifacts exist; 13 designations/files verified.**
- Every cross-note status claim checked against the target where in-repo (rule 10) — which is how T1's package-scope consumption, the ledger's 3/3 discharge, and the two GRTJA cross-agreements were confirmed.

---

## 8. Self-audit and OPEN-CALLs

### OPEN-CALL 1 — the note has no verifiable pin of its own. FOR-0b / FOR-0d.

WELDMASTER asserts a text freeze (“the frozen body is not edited”), a two-commit instrument seal, and an “md5-pinned” roster — **and displays no hash for any of them.** Every other note in queue 8b displays at least one verifiable md5; HEX3 displays eight; HE6R1 displays two nested prefix pins. **Here a compiler can verify commits and file existence but cannot verify that the frozen body is the body that was accepted, nor that the roster is the roster that was pinned.**

**Decision needed:** whether an accepted note's freeze must carry a prefix md5 as a corpus rule. The cost is one line; the benefit is exactly what `spec/EFF-HEX3.md` FREEZE-1 and `spec/EFF-HE6R1.md` FREEZE-1 demonstrate — a boundary located to the byte. **Note this is not a defect claim:** nothing suggests the body has moved. It is an unverifiability claim, and it is the only one of its kind in the queue.

### OPEN-CALL 2 — the FAVORABLE/OVERSTATING direction rule is now stated by three notes and written down nowhere. FOR-0d.

O-2 (`.27`): “Every quoted billing was true when frozen and is now strictly conservative. **Direction: FAVORABLE at every site; no display moves.**” O-1 (`.26`), FC-2 in GRTJA (`spec/EFF-GRTJA.md` `.27`), and — from the other side — HE6R1's PE3 F-2 (`spec/EFF-HE6R1.md` `.40`, “the one stale-grade site whose direction is NOT conservative”).

**Three notes have independently converged on the same convention:** classify each stale billing's direction; re-date only the overstating ones. **Decision needed:** make it a written corpus rule. This is the second time this compilation has raised it (see `spec/EFF-GRTJA.md` OPEN-CALL 2); the evidence is now stronger by one note.

### OPEN-CALL 3 — F-1 changed what “ACCEPTED” names, and the acceptance record is not annotated in place. FOR-0b.

F-1's sweep item 4 states: “The acceptance record's ‘THEOREM WELD-M’ is **henceforth the accepted package theorem** above; no uniqueness or extra coherence is part of that acceptance.” **So the object accepted on 2026-08-07 is not the object named in the acceptance record**, and the record — being frozen — still names the stronger one. F-2 does the same for the H¹ gloss.

**T1, the note's main consumer, already reads the corrected object** (`WELD-M-PKG`, count 14) — so no live consumer is misled. **But a reader arriving at the acceptance record gets `THEOREM WELD-M (the one transport…)` with no forward pointer.** **Decision needed:** whether the corpus wants a convention that an append correcting an acceptance record's *object* must be cross-referenced from a dated line adjacent to that record. **This is a variant of `spec/EFF-HMENU3.md` OPEN-CALL 2 and `spec/EFF-HEX3.md` OPEN-CALL 2** — stale or superseded text frozen inside a record no append may edit — and the three cases together suggest a single missing convention.

### Recorded source defects and reading hazards

1. **`.03`/`.01`/`.21`: “ONE transport” is corrected to a five-field package** (F-1). Any consumer quoting the frozen S1 or the title is quoting a superseded scope.
2. **`.21`: the acceptance record contains a false cohomological claim** (`H¹ = 0`), withdrawn by F-2. The *result* stands; the *reason* was wrong.
3. **`.25`: the S6 J-D design clause misattributed freedom to WM-FENCE.** Corrected; blast radius nil (the clause is unexecuted).
4. **AF-1: no md5 anywhere** (OPEN-CALL 1).
5. **`.18`/`.20`/`.30` and passim: every “0/2” supplier billing is stale**, catalogued FAVORABLE by O-2.
6. **`.07`'s (ii) rides JA-EPS, which is CRITICAL-corrected at GRTJA** — but the `[r1]` F-4 re-footing on `[ILN]† EXP-KIT(a)` and the weight-lattice scoping mean it survives. **Neither note connects the two**; recorded here.
7. **`.12`'s seed identities likewise consume JA-EPS**, and survive structurally because they are **first differences** in which a common `ν`-shift cancels. **Also unstated by either note.**
8. **`.13`: this note found a FALSE displayed conclusion in an accepted supplier** (`[JA]†`'s root-exclusive quantifier), with 8 + 58 witnesses. Cross-confirmed at GRTJA.
9. **`.19`: the roster is imported verbatim from `[JC]†`'s instrument**, so the machine leg's independence from JC is partial by construction.
10. **AF-2: three cited commits unverified.**

### Exhaustive residual judgment list

There are no other unresolved WELDMASTER mathematical or scope judgments. The `(VD-m)` strata, `(DMULT-s)`, JB's four NO-LEG rows, W2-OPEN-1/OPEN-2 and the mod-wrap boundary are inherited supplier boxes, displayed and carried verbatim; the missing hashes, the stale billings and the unverified commits are bookkeeping. The three OPEN-CALLs are the complete list.

### Compilation summary

- **30 statement units** across the frozen body (`.01`–`.20`), the acceptance record (`.21`), the C6/E6 corrections (`.22`–`.25`, `.28`, `.29`), the two 2026-08-08 annexes (`.26`, `.27`) and the dependency record (`.30`).
- **Queue charge discharged:** `WELD-M-PKG`'s **five fields** are emitted at `.04` (frozen clauses) and `.22` (the package re-display) with a per-field C7 anchor table in §1; the **one-perimeter fence** is emitted at `.12`–`.16` with **F-4's consumption scoping** at `.25`. **T1's C7 consumption is verified at the target** (`WELD-M-PKG` count 14, four scope declarations quoted), and **T1 reads the corrected object, not the frozen one.**
- **13 designations/files verified; zero NEAR-MISS; 24 of 27 commits verified; ZERO md5s available** (OPEN-CALL 1).
- **5 reverse-XREF edges**, with an F-1-verified statement that `M0`/`M1`/`M2` have no executed downstream consumption beyond T1.
- **Seven supersession chains**, four distinct SUPERSESSION KINDs, and **two corrections landing on the acceptance record itself.**
- **Rule 9's shard threshold consciously overridden and the call recorded.**
- **Arithmetic audit: all three E6 re-derivations reproduced independently**, including both `F₅` witnesses, along with WM-RHO's induction and both PERIM forms. Two findings, neither a mathematical error. **Three cross-note agreements verified** (the 8 witnesses, the 38/44 split, GRTJA's frozen md5).
- **Honest headline for a chapter cut:** WELDMASTER is an **umbrella**, and it says so — “it proves no new instance and discharges no open leg … Its net new content is exactly two displays.” Ten passes and seven repairs hardened those two displays; a decorrelated read then found that the *umbrella's own name* over-claimed — five heterogeneously typed faces are a **record**, not a map — and corrected it without touching a single formula. **The mathematics was right and the packaging was not**, which is precisely the failure mode an umbrella note is exposed to.

EFF-WELDMASTER COMPILED: 30 statements / 13 xrefs verified / 3 open calls
