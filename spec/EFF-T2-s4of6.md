# EFF-T2-s4of6 — CANONICAL EFFECTIVE-SPECIFICATION SHARD

**COMPILED-BY:** gpt-5.6-sol high (2026-08-18), cross-reading and repairing the 2026-08-14 Opus draft against the current template and terminal source record.

## 1. Shard header

**Source note:** `lean/notes/openmath/T2_SIGMA_LADDER_MASTER_2026-08-12.md`

**HOME range:** source lines 3569–4018 inclusive (r8 repairs 2–4 and counter; r9 in full; post-pass rider block 1; the r10 header and repairs 1–2). The destination section of an edit, not the physical location of the edit directive, determines HOME; directives aimed at base S0–S8 resolve `EFF.T2.s1of6.<nn>` units and are routed, not re-emitted (§7).

**HEAD blob hash:** `2b74ad0f35f41e3109f1ba779eb8eb266a8ba0fc` (`git hash-object` and `git rev-parse HEAD:<path>` agree).

**Last source-changing commit:** `66062771a0496cc1105b618fc98523a0437b79b0` (verified as a Git `commit`; the commit adds the dated consumption record without superseding the accepted text).

**Compilation context:** `spec/EFF_TEMPLATE_v2.md`; temporary IDs `EFF.T2.s4of6.<nn>`. Conventions follow `spec/EFF-T2-s1of6.md`; the HOME routing audit follows `spec/EFF-T1-s2of3.md`.

### Governing effective-text record

The front matter says:

> “**Status:** ACCEPTED note of record — the hostile arc is closed at 2/2 and the accepted text is byte-frozen; THE ACCEPTANCE RECORD AT THE END OF THIS NOTE GOVERNS (before acceptance the governing sections were the highest-numbered r-section and the post-pass rider blocks); the pass record, the folded riders, the pin-sweep result, the counter state, the four standing conditionalities, and the `(GRADE)` cap that still holds the S3.1/S3.2 composed instances at HE3's HEAD grade are recorded in that block.”

Two acceptance-record findings govern this range specifically. On its GAP:

> “the print/coherent frame conflation at the HE6 family index, caught by the first model-diverse pass (r9 — (IDX-TWIST)); the level-one test package and `(ACCOUNT)` without pinned suppliers (r10 — three new HE6 spans)”

On the rider it contains:

> “Per the campaign's minors-only precedent … a 0 CRITICAL + 0 GAP pass is a clean pass and its minor folds as a post-pass rider without a grade event or a counter reset.”

Every pin count and counter state set in this range (32 → 35; “RESET to 0 of 2”; “STANDS AT 1 OF 2”) is superseded to **37 pins, taxonomy \(20+4+13\), counter 2 OF 2**.

The terminal dated record is explicitly non-superseding:

> “NO r-section, no grade event, no counter reset, and NO supersession: not one sentence of the accepted text is withdrawn or replaced.”

`[ASSEMBLED from the same dated record]` It leaves the frozen T2 text, accepted `2 OF 2` counter, two-open-obligation ledger, HE3-grade cap, and 37-pin `20+4+13` inventory unchanged.

It nevertheless governs the precision with which r10's HE6 append sweep is reported: the 2026-08-12 F-1 lands on a consumed clause whose corrected form T2 already uses, but moves no conclusion and earns no new pin; the distinct post-PE4 Correction 2 beta guard is consumed and, by r11, pinned as `HE6-BETA-GUARD`, while the 2026-08-12 F-2 remains proof-internal.

### Freeze predicates

**FREEZE PREDICATES — accepted-text/status freeze:** “With this record the note is **BYTE-FROZEN**.” Pin: acceptance record, source line 4505.

**FREEZE PREDICATES — acceptance-scope enumeration:** “That sweep was run whole at **37** pins” and “re-resolved to the same \(20+4+13=37\) inventory”; the same record fixes the two open obligations and HE3 grade cap. Pins: source lines 4494–4503.

**FREEZE PREDICATES — consumption-record fence:** “NO r-section, no grade event, no counter reset, and NO supersession”. Pin: source line 4513.

`[TABLE — compiler freeze ledger]`

| Predicate | Governing evidence | Effect in this shard |
|---|---|---|
| Accepted-text/status freeze | acceptance record: “the note is ACCEPTED AT 2 OF 2” and “the accepted text above is byte-frozen” | No locally homed theorem text or accepted status may be reopened by the dated record. |
| Acceptance-scope freeze | acceptance record's exhaustive enumeration: 37 pins, \(20+4+13\), two open obligations, `(RISE)`/`(SEC-RANK)` standing, HE3 `(GRADE)` cap | Historical r8–r10 counters and inventories are not effective state. |
| Consumption-record fence | dated record's “NO supersession” sentence | Its F1–F4 rulings refine `.15`'s protocol report only; they do not create a repair round. |

### Scope split

**ABSORPTION/supply SET.** Unchanged as to membership. This range changes the *supplier map*: r10 gives the level-one test package and `(ACCOUNT)` pinned suppliers for the first time. The governing statement of the defect and of its cure shape is r10's own:

> “Accepted in full, and cured by pinning rather than by amending either crediting sentence. The two effective-text sentences the report quotes … are TRUE of the pin table this repair installs and false of the one it replaces. The defect is the table's, not the claim's, so no sentence of S1.7, S3.1 or S3.2 is amended.”

No theorem-statement change is made anywhere in this range; r8, r9 and r10 each book that explicitly.

**APPEND/consumption PROTOCOL scope.** One protocol event of record occurs here and it is source-side only: the r9+rider pass ran the **full pin gate end to end for the first time**:

> “It then ran the full 32-pin gate end to end on the final text for the first time — 15 quoted pairs with all 30 delimiter lines at `grep -cFx` = 1, 4 first-only, 13 description-only, zero `T2V2PIN` lines in the six sources, zero source edits — and fourteen supersession landmarks across r1–r9 and the rider, including the `(MP1′)` retirement chain, with no orphan, no double-render and no ambiguous insertion point.”

That is a gate run on the *sources*, not a downstream consumption check; downstream checks remain queued by the acceptance record.

**INBOUND-CORRECTION scope.** The HE6 dated corrections are foreign-source inputs. The governing r11 repair says: “**Accepted in full, and cured by pinning rather than by amending any sentence.**” r10 classifies them, r11 corrects the beta-guard classification, and the terminal dated record adjudicates F1–F4 without changing the frozen T2 text. These inbound corrections alter the effective pin/consumption account in `.11`, `.12`, and `.15`; they do not enlarge T2's theorem statement.

### Resolution rules applied

1. The acceptance record governs status; counter states, pin counts and taxonomies stated in this range are historical.
2. A directive homed in base S0–S8 is routed (§7), with the same two exceptions declared in shard 3: (i) reading directives, adjudications, declinations and verification records whose destination is a `(PINS)` row or a pin name; (ii) a multi-leg derivation with quoted source legs whose shard-1 destination unit compresses it to a single clause. Exception (ii) is used at .03, .04, .05 and .06. **No statement, display, clause or instance box of shard 1 is re-emitted.**
3. Intra-shard supersession is applied and only the replacement is stated as effective: r9 repair 1(a)–(d) supersedes r8 repair 1(a)'s step sentence, r8 repair 1(b)'s height ground and corroboration clause, and S1.8C's family-member sentence; r9 repair 2 supersedes both counts of the r8 landmark census; Rider 1 qualifies r9's `(IDX-TWIST)` display; r10 repair 2 qualifies Rider 1's covering direction.
4. Cross-shard supersession is applied in both directions. **Downward:** pin count 35 → 36 (r11) → 37 (r12); taxonomy 18+4+13 → \(20+4+13\); the cured-consumption census eight → nine → ten; the five Source-pin lines re-set at r11 repair 1(e) and r12 repair 1(f); r11's beta-guard correction is itself pinned; r12 completes the five-source append sweep. **Upward, into shard 3:** r9 repair 1 supersedes two passages of the r8 `(RISE)` discharge that shard 3 `.03` quotes. This shard records the merge target; it does not mutate another shard.
5. `(IDX-TWIST)` is a derived identity of the level-one instance, never a clause of the theorem: “The one new display, (IDX-TWIST), is a derived identity of the level-one instance, not a clause of the theorem.”
6. Rider blocks are not repair rounds: they fold a minors-only pass, set counter state, and reset nothing.
7. Cross-note XREF counting follows the current template: each distinct fixed-string target designation is counted once, not each quoted evidence line. All 17 designations in §3.A were re-run at compilation time and returned exactly 1. Quote-line and master-landmark evidence is recorded separately and is not added to the XREF total.
8. The r10 counter (4019) begins outside this range and is compiled in shard 5.
9. `FORM`, `SUPERSESSION KIND`, `TARGETS`, and `CHAIN` fields are compiler metadata. They do not restate or extend source claims.

### Quotation and table discipline

Source statements are verbatim excerpts or marked `[ASSEMBLED]`; abridgement inside a quoted block is marked with an ellipsis. Source lines are reproduced byte-exactly including any leading `> ` or leading spaces. Compiler ledgers are separately marked.

---

## 2. Statement inventory

### EFF.T2.s4of6.01  [changes-record]

**CANONICAL STATEMENT:** `[ASSEMBLED — the r8 round record (counter; the r8 header is homed at EFF.T2.s3of6.08)]`

**FORM:** bold-headed paragraph.

> “r8 folds repairs for the single GAP (G-1P7), both minors (m-1P7, m-2P7) and both recorded nits of the r7 hostile pass of record. Its substantive content is one object, and it is an assembly order rather than a new claim: the level-one discharge of (RISE) now exhibits the instance's refine step BEFORE reading either clause off it.”

> “**No theorem-statement change is made by this round**, in the exact sense S7 counts: no display is amended, no hypothesis is added to or removed from HE7.A's suite, no case is added or dropped, and no conclusion is touched. (RISE) and (HT) read exactly as r7 set them; (RISE) remains STIPULATED, on r4 repair 3's `(SEC-RANK)` footing, and (HT) remains DERIVED from it by the min-rule display. r7's own statement change — (RISE) joining the root-preserving continuation assertions — stands as r7 booked it.”

> “**Two named obligations remain open**, both at level one: `(LB1)` for clause-4 block construction, and `(MP1)` for the clause-5 peel at a recentered key. (RISE) is a carrier hypothesis, not a ledger obligation”

> “**The clean-pass counter is RESET to 0 of 2.**”

**DERIVATION:** `[ROUND RECORD]` The round's own classification test is S7's: a statement change is an amendment to a display, the hypothesis suite, the case list or a conclusion. r8 changes none of those — it changes a *discharge*, one citation and four items of bookkeeping — so it books no statement change while r7, which added (RISE) to the suite, books one.

**CONDITIONALITY:** Historical as to counter. Its landmark census is **corrected inside this shard** by r9 repair 2 (“Twelve master-internal landmark strings, on eleven lines” → “on ten lines”; “Nine source lines” → “Eleven source lines”); the lists themselves “are themselves correct and unchanged”. The obligation set survives to acceptance.

**RESOLUTION TRACE:** **statement lines:** 3655–3667; **proof lines:** r8 header 3513–3519 (shard 3, `.08`); **correction sites:** census 3767–3789 and terminal acceptance 4479–4505.

**TEETH:** S7 acceptance gate; the r7 hostile pass of record; the round's own landmark census as an audit tooth.

---

### EFF.T2.s4of6.02  [changes-record]

**CANONICAL STATEMENT:** `[ASSEMBLED — what the three S1 data do and do not derive, and the removal of the last two (REF-HT) mis-citations]`

**FORM:** bold-headed paragraph.

**SUPERSESSION KIND:** wording-rider.

**TARGETS:** `EFF.T2.s1of6.19`, `EFF.T2.s1of6.25`, `EFF.T2.s3of6.02`.

**CHAIN:** r6 three-data binding claim → r7 `(RISE)`+`(HT)` architecture → r8 once-for-all implication/binding correction → **TERMINAL:** r8 wording as preserved through acceptance.

The four r6 sentences describing the binding as derived from three S1 data are booked together:

> “Four live sentences, all r6 round prose, make the same now-incomplete claim; r7 booked one of them. They are booked here together and on one uniform footing — each is r6's own record of r6, is left standing as such, and is superseded in substance by the (RISE)+(HT) architecture”

> “**What the correction is, once for all four.** The three S1 data derive the min-rule DISPLAY — \(d(\Phi(\rho))=\lambda\) on the class by `(SIDE-COUNT)`, \(d(L_k(s)(\rho))=k\) by `(SLOT-V)` + `(LIFT)`, hence \(d(\Phi_{\mathrm{new}}(\rho))=\min(\lambda,k)\le\lambda\) at \(k\ne\lambda\) — and that display is an IMPLICATION, converting (RISE) into (HT). It binds nothing on its own, because it is silent on whether any value rises: the three data are as true at \(k\ne\lambda\), where the class does not rise, as at \(k=\lambda\). The binding needs a fourth datum, the stipulated (RISE), which r7 displays and discharges per instance.”

The lead-in is corrected so that only (RISE) is stipulated:

> “The recentering at this branch is a REFINE step, and this master stipulates of a refine step ONE thing: that it raise the key's value on the class it refines. That its increment then sits at the side's own slope is not a second stipulation but a consequence of the first, derived below. Both are displayed, because both are consumed downstream:”

Reading directive 2 is re-cited to the support it actually has:

> “The passage's configuration is a repeated \(K\)-rational residual root at an integer slope; that is clause 3's product-\(1\) branch by S1.8's own case split, since a \(K\)-rational residual root has \(\deg r'=1\) and an integer \(\lambda=u'/e'\) in lowest terms forces \(e'=1\), so \(e'\deg r'=1\) — the only transition of `(RANK)` that recenters a key. Neither clause of `(REF-HT)` is used here: (RISE) and (HT) are about the step this branch performs, not about which branch is taken.”

> “**Consequential booking.** r7 repair 4's closing clause … is superseded on that clause alone: after (b) above, `(REF-HT)` governs neither the match nor the directive, and the rest of r7 repair 4's paragraph … stands unchanged.”

**DERIVATION:** `[BOOKKEEPING CORRECTION WITH A SUBSTANTIVE CORE]` The core is the once-for-all correction: an implication is not a binding. The three S1 data are symmetric in \(k\) and therefore cannot select \(k=\lambda\); only a premise asserting a rise can, and that premise is (RISE). The two citation cures follow from the same accounting: the lead-in must not present (HT) as stipulated, and reading directive 2's proposition (which branch is taken) is decided by S1.8's case split and the lowest-terms convention, not by either clause of `(REF-HT)`.

**CONDITIONALITY:** All four r6 sentences stand byte-unchanged as r6's own record; only their substance is superseded. After this unit, **no clause of `(REF-HT)` is a premise anywhere in S1.8C** — the completion of the withdrawal r8 repair 1(d) began.

**RESOLUTION TRACE:** **statement lines:** r8 repair 2, 3569–3595, and repair 4(a)–(b), 3619–3651; **proof lines:** the same two repair spans' implication/binding and branch-arithmetic arguments; **correction sites:** r6 landmarks 2922, 2934, 3287, 3289 and superseded strings 3321, 3105, 3431. Destination: base S1.8/S1.8C → `EFF.T2.s1of6.19`, `.25`.

**TEETH:** S7 Pass 1 theorem gate; the non-circularity audit tooth; the orphan check on withdrawn supports.

---

### EFF.T2.s4of6.03  [changes-record]

**CANONICAL STATEMENT:** `[VERBATIM — the HE6 leg of the level-one (RISE) discharge, with its caveat routed (r8 repair 3); attaches to the discharge map at EFF.T2.s3of6.03]`

**FORM:** bold-headed paragraph.

**SUPERSESSION KIND:** license.

**TARGETS:** `EFF.T2.s1of6.19`, `EFF.T2.s3of6.03`.

**CHAIN:** HE3-only support for a S3.1/S3.2 scope → r8 HE6 closing-paragraph licence with caveat routed → **TERMINAL:** joint HE3/HE6 discharge support, unchanged at acceptance.

> “**The S3.2 half of the scope.** S3.1 is HE3 and S3.2 is HE6, whose family is enlarged beyond HE3's, so the stage-α reading above has to be licensed for HE6's classes before the discharge may be scoped to both. It is, in the closing paragraph of the pinned proof unit of THEOREM HE6.A: `**This is HE3's LEMMA HE3-4 in full generality**, so HE3's THEOREM HE3.A` (`HE6_PROOF_2026-08-08.md`:663), `assembly (§S6.3 there: base read + stage-α + stage-CS + embedded genres) may` (664), `be run with HE3-4 replaced by THEOREM HE6.A — with the one caveat of §S7 for` (665), `histories containing an ℓ ≥ 2 side with a repeated residual factor.` (666) — the same quoted pair S1.8C already leans on, naming **stage-α** explicitly. The caveat is not consumed by this discharge: an \(\ell\ge2\) side with a repeated residual factor is \(e'\deg r'\ge2\), the product-\(\ge2\) child jump of clause 3, whereas the step under discharge is the product-\(1\) refine step, \(e'\deg r'=1\); S1.8C records the same routing for the frame discharge, and S3.2's own text sends that configuration to the level-two construction.”

**DERIVATION:** `[CITATION COMPLETENESS]` The defect was scope-versus-citation: the discharge was scoped to “(S3.1/S3.2)” but cited only `HE3-A-PROOF`. The licence exists inside a pin already on S3.2's line, so “this is a citation completeness defect, not a missing pin”. The caveat is disposed of by branch arithmetic — \(\ell\ge2\) with a repeated residual factor is exactly \(e'\deg r'\ge2\), which is not the branch under discharge — and the same routing is already recorded twice elsewhere (S1.8C, and S3.2's own continuation sentence).

**CONDITIONALITY:** The licence is for HE6's enlarged classes at the product-\(1\) step only. Nothing behind the §S7 caveat is consumed anywhere in the note.

**RESOLUTION TRACE:** **statement lines:** r8 repair 3, 3599–3615; **proof lines:** HE6 closing transfer paragraph 663–666 plus the branch arithmetic at 3607–3613; **correction sites:** superseded anchor at 3353. Destination: `EFF.T2.s1of6.19`, with the earlier discharge state at `EFF.T2.s3of6.03`.

**TEETH:** the level-one `(RISE)` discharge tooth; the standing S7 consumption check (the licence lies inside `HE6-PROOF`).

---

### EFF.T2.s4of6.04  [lemma]

**CANONICAL STATEMENT:** `[ASSEMBLED — the print/coherent seam and the derivation of (IDX-TWIST) (r9 repair 1(a)), with Rider 1's lift-choice qualifier and r10 repair 2's height qualification applied. The (IDX-TWIST) display itself is homed at base S1.8 (shard 1, EFF.T2.s1of6.20); what is homed here is its derivation, its bijection argument and its two qualifiers.]`

**FORM:** bold-headed paragraph.

**SUPERSESSION KIND:** replacement.

**TARGETS:** `EFF.T2.s1of6.20`, `EFF.T2.s1of6.25`, `EFF.T2.s3of6.03`, `EFF.T2.s3of6.04`.

**CHAIN:** r8 equal-index identification → r9 fixed-unit relabelling `(IDX-TWIST)` → Rider 1 lift-choice qualifier → r10 \(\kappa>D'h\) qualifier → **TERMINAL:** the qualified derivation in this unit.

The seam:

> “a residue in the RECIPE'S OWN frame, which is the print frame: line 204, quoted above, fixes \(B_0\) by \(\operatorname{res}(B_0(\theta)/\varpi(\theta)^{\kappa})\), the \(\varpi\)-read, and not by the coherent \(n(\kappa)\)-read of `(SLOT-R)`. This master's `(HE6-SEAM)` is the conversion between the two, \(\operatorname{res}(A(\xi)/\varpi(\xi)^{k})=\iota_\xi(\gamma_k(A))\eta_\xi^{-q(k)}\), and at \(\xi=\theta\) — a \((T1)/(T2)\) point, being the point at which the recipe states its own condition and at which `(GAMMA)` is written — the embedding \(\iota_\theta\) is the identity inclusion of \(K=\mathbf F_Q(\eta)\): it is a \(k_0\)-embedding by `(RES-COMP)`, \(k_0=\mathbf F_Q\) by S1.1, and \(\eta_\theta\mapsto\eta_\theta\) by S3.1's \(\iota_\xi(\eta_\theta)=\eta_\xi\) read at \(\xi=\theta\), so it is the identity on \(K=k_0(\eta)\). Hence \(-\sigma=\gamma_\kappa(B_0)\eta^{-q(\kappa)}\), so \(\operatorname{dig}(B_0)=\gamma_\kappa(B_0)=-\sigma\eta^{q(\kappa)}\)”

The non-triviality of the twist:

> “The print label and the coherent digit are the same element of \(K\) only when \(\eta^{q(\kappa)}=1\), and no hypothesis of this master or of its sources supplies that: `(Q-DEF)` posits \(q\) precisely because the twist can be nontrivial, and the minus sign of `(HE6-SEAM)` is declared mandatory at S1.2.”

The bijection, which is what makes the relabelling lossless:

> “**Nothing is lost by the mismatch, because the relabelling is a bijection.** \(\eta\in K^\times\) and \(q(\kappa)\in\mathbf Z\), so \(\eta^{q(\kappa)}\) is ONE fixed element of \(K^\times\), depending on \(\kappa\) alone and on neither the polynomial read nor the label — that is the universality of the RIDER's twist, the clause `HE6-LIFT-1L` is pinned for. Multiplication by it is a bijection of \(K\) fixing \(0\), and of \(K^\times\) onto itself. The family's labels at \((\ell,d)=(1,1)\) run over exactly \(K^\times\), because the recipe quantifies over EVERY monic irreducible \(r\in K[Z]\) with \(r(0)\ne0\) … and \(r=Z-\sigma\) is such an \(r\) exactly for \(\sigma\in K^\times\); this master's digits at a full height run over all of \(K\) (S1.4, "every digit \(c\in K\) has an integral lift"), with \(L_\kappa(0)=0\) the empty recentering, which is no transition. … What r8 asserted member-by-member at equal index NAMES is true after the relabelling and false before it.”

The exhibited step, with nothing asserted of it:

> “whichever member of the family the read forms, it is \(\Phi'-\Lambda\) with \(\Lambda:=-B_0\) integral, \(\deg\Lambda<D'=D\), \(h(\Lambda)=\kappa\) and \(\operatorname{dig}(\Lambda)\in K^\times\), and the recipe's standing \(\kappa>D'h=T\) places it inside `(WINDOW)` before any clause of `(REF-HT)` is read. **No height and no digit is asserted of that member here, and neither is needed to read the clause that follows.**”

S1.8C's family-member sentence, in its effective form:

> “At \((\ell,d)=(1,1)\) the recentered key IS a member of the family, and the member it is, is \(\Psi_{\lambda,\,Z-s\eta^{-q(\lambda)}}=\Phi'+B_0\) with \(B_0=-L_\lambda(s)\): the family indexes its members by the PRINT residue of \(B_0\) … this master indexes its recenterings by the COHERENT digit of the increment (`(SLOT-R)`), and the two differ by the fixed unit \(\eta^{q(\lambda)}\in K^\times\) of `(HE6-SEAM)` … The relabelling is a bijection of \(K^\times\), so the family covers every recentering of this master at that height, member for member; what it does not do is match them at equal index names.”

**DERIVATION:** `[SEAM COMPUTATION]` One conversion and one identification: `(HE6-SEAM)` converts the recipe's \(\varpi\)-residue into the coherent digit, and \(\iota_\theta=\mathrm{id}\) at the recipe's own point (three cited grounds: `(RES-COMP)`, S1.1's \(k_0=\mathbf F_Q\), S3.1's \(\iota_\xi(\eta_\theta)=\eta_\xi\)). The bijection argument then needs only that \(\eta^{q(\kappa)}\) is one fixed element of \(K^\times\) depending on \(\kappa\) alone — the universality clause `HE6-LIFT-1L` is pinned for — plus the two quantifier ranges (family labels over \(K^\times\); master digits over \(K\), with \(0\) the empty recentering).

**CONDITIONALITY:** Two qualifiers apply, both homed in this shard (`.06`): the equality is exact **at the compatible lift choice** and holds modulo increments of height \(>\kappa\) at an arbitrary permitted one, with the choice-independent content being the pair \(h(-B_0)=\kappa\), \(\operatorname{dig}(-B_0)=\sigma\eta^{q(\kappa)}\); and the covering direction is stated at **\(\kappa>D'h\)**, DEFINITION HE6-1's own standing hypothesis, not at mere fullness. `(IDX-TWIST)` is a derived identity of the level-one instance and no clause of the theorem. The terminal F-1 record confirms that the foreign `HE6-SLOT-SEAM` bare display is corrected, but this unit already uses the normalized `(GAMMA)`/`(HE6-SEAM)` form; no conclusion or pin moves.

**RESOLUTION TRACE:** **statement lines:** r9 repair 1(a), 3689–3711, and 1(d), 3749–3755; **proof lines:** the seam/bijection argument in those spans, using HE6 130–165, 201–205, and 215–238; **correction sites:** r8 landmarks 3537 and 3085, Rider 1 at 3829–3831, r10 repair 2 at 3999–4013, terminal F-1 adjudication 4511–4540. Destination: `EFF.T2.s1of6.20`, `.25`.

**TEETH:** the print/coherent-frame mutant; the HE6 seam/sign gate; the lift-choice qualifier tooth; the covering-height tooth.

---

### EFF.T2.s4of6.05  [lemma]

**CANONICAL STATEMENT:** `[ASSEMBLED — the effective level-one derivation of the step's height AND digit from (RISE) (r9 repair 1(b)), superseding the r8 height ground quoted at EFF.T2.s3of6.03]`

**FORM:** display.

**SUPERSESSION KIND:** replacement.

**TARGETS:** `EFF.T2.s1of6.19`, `EFF.T2.s3of6.03`.

**CHAIN:** r8 height-only min-rule argument → r9 coherent height-and-digit derivation → r12 ultrametric completion of the lift-freedom rider → **TERMINAL:** the assembled derivation below.

> “At any index \(\kappa\ne\lambda\) the display leaves every root of the class at \(d=\min(\lambda,\kappa)\le\lambda\) … Hence \(\kappa=\lambda\). At \(\kappa=\lambda\) the display is silent, the two values being equal; there the same reading continues one notch finer, at the residues, and fixes the step's DIGIT as well — without consulting the family's print label.”

The coherent label read, quoted from strictly inside `HE3-A-PROOF`:

> “`note's frame, where the label of ρ is (λ, r) with r ∈ K[Z] read via the` (`HE3_PROOF_2026-08-08.md`:1114), `F_Q-embedding ι_ρ : K → k̄, η_θ ↦ η_ρ, and β_ρ := res(y_ρ/n(λ)(ρ)) at` (1115); the argument \(y_\rho\) is the key's own value, as the equivariance display inside the same span shows at \(g=\operatorname{id}\), `    β_{gρ} = res(Φ′(gρ)^{ℓ}/n(u)(gρ)) = ḡ(β_ρ)   (ℓ = 1: n(λ) read).` (1131, quoted with its four leading spaces), giving \(\beta_\rho=\operatorname{res}(\Phi(\rho)/n(\lambda)(\rho))\) at \(\ell=1\) with \(\Phi=\Phi'\) by `(HE3-INSTANCE)`; and the label condition itself is \(\iota_\rho(r)(\beta_\rho)=0\), `ι_{gρ}(r)(β_{gρ}) = ḡ(ι_ρ(r)(β_ρ)) = ḡ(0) = 0, and gρ carries the same` (1135)”

The forced digit:

> \[
> \boxed{
> d\bigl((\Phi-\Lambda)(\rho)\bigr)>\lambda
> \iff
> \iota_\rho\bigl(s-\operatorname{dig}(\Lambda)\bigr)=0
> \iff
> \operatorname{dig}(\Lambda)=s ,}
> \]

> “\(\iota_\rho\) being an injection of fields. The passage's rise therefore forces both readings at once: \(h(\Lambda)=\lambda\) and \(\operatorname{dig}(\Lambda)=s\), with \(s\) this master's own coherent residual root of `(RES-FACT)` — the digit at which S1.8 forms \(\Phi-L_\lambda(s)\) — while the family's print label at that same member is \(s\eta^{-q(\lambda)}\) by (IDX-TWIST) and plays no part in the derivation.”

The two riders that close it:

> “FIRST, the derived digit is automatically in \(K^\times\): a zero increment cannot raise \(d(\Phi(\rho))=\lambda\), and S1.2 gives \(\operatorname{dig}(A)\in K^\times\) at every nonzero slot. SECOND, the lift freedom is immaterial. \(\Lambda\) is the increment the instance's construction actually formed; \(L_\lambda(s)\) is the carrier's designated lift at the same height and digit (S1.4). Their difference \(E=\Lambda-L_\lambda(s)\) has \(\deg E<D\) and, read at the fixed height \(\lambda\), digit \(s-s=0\) … so \(E\) cannot have \(h(E)=\lambda\), since S1.2 gives a nonzero slot a digit in \(K^\times\); **[r12 repair 4(d), homed in shard 5:]** “nor can it have \(h(E)<\lambda\), because \(h(\Lambda)=\lambda\) and \(h(L_\lambda(s))=\lambda\) give \(d(\Lambda(\rho))=d(L_\lambda(s)(\rho))=\lambda\) by `(SLOT-V)`, whence \(d(E(\rho))\ge\lambda\) by the ultrametric inequality for \(d\) (S1.1), while a nonzero \(E\) of degree \(<D\) has \(d(E(\rho))=h(E)\) by `(SLOT-V)` again”; hence \(E=0\) or \(h(E)>\lambda\), and then \(d(E(\rho))>\lambda\) by `(SLOT-V)`. From \((\Phi-L_\lambda(s))(\rho)=(\Phi-\Lambda)(\rho)+E(\rho)\), both clauses hold at this master's own displayed step and not merely at a companion of it.”

The corroboration ground, in its effective frame:

> “so the member of DEFINITION HE6-1's family that the read uses at that node is the one whose COHERENT digit is the node's own \(s\) — its print label being \(s\eta^{-q(\lambda)}\) and not \(s\), by (IDX-TWIST) — and its increment sits at \(d=\lambda\) by the recipe above.”

**DERIVATION:** `[RESIDUE-LEVEL REFINEMENT OF THE MIN-RULE]` Above the value seam the min-rule display fixes the height; at \(k=\lambda\) it is silent, and the argument descends one level, to residues. Two reads in the same coherent frame — the label condition \(\iota_\rho(r)(\beta_\rho)=0\) at \(r=Z-s\), and `(SLOT-R)` on the increment — subtract to give the boxed equivalence, because \(d(n(\lambda)(\rho))=\lambda\) makes the quotient value-nonnegative with residue vanishing exactly when the value is positive. The two riders remove the only two degrees of freedom left: the digit's membership in \(K^\times\), and the difference between the instance's increment and the carrier's designated lift.

**CONDITIONALITY:** **One clause of the lift-freedom rider is completed downstream:** r12 repair 4(d) (shard 5, `.13`) inserts the step ruling out \(h(E)<\lambda\), quoted inline above in its effective position; it uses the ultrametric law for \(d\) that r12 repair 4(b) displays in S1.1. The derivation runs at the instance and instantiates the master's generality in \(k\) and in the digit rather than transferring the theorem's (HT) clause: “the direction of use is the safe one — the master's generality in \(k\), and in the digit, is INSTANTIATED here, not transferred.” It requires \(\rho\in\operatorname{Pt}\) and \(\deg\Lambda<D\) for `(SLOT-R)`. The premise remains (RISE), stipulated.

**RESOLUTION TRACE:** **statement lines:** r9 repair 1(b)–(c), 3713–3747; **proof lines:** HE3 coherent-label proof 1114–1135; **correction sites:** r8 height/corroboration landmark 3545 and r12 lift-freedom completion 4421. Destination: `EFF.T2.s1of6.19`; prior state: `EFF.T2.s3of6.03`.

**TEETH:** the print/coherent-frame mutant (this is its cure); the level-one `(RISE)` discharge tooth; the lift-choice tooth.

---

### EFF.T2.s4of6.06  [changes-record]

**CANONICAL STATEMENT:** `[ASSEMBLED — the two qualifiers on (IDX-TWIST): Rider 1's lift-choice clause and r10 repair 2's covering-height correction]`

**FORM:** bold-headed paragraph.

**SUPERSESSION KIND:** wording-rider.

**TARGETS:** `EFF.T2.s1of6.20`, `EFF.T2.s1of6.25`, `EFF.T2.s3of6.04`, `EFF.T2.s4of6.04`.

**CHAIN:** unqualified r9 polynomial equality/covering claim → Rider 1 compatible-versus-arbitrary lift split → r10 standing-height correction → **TERMINAL:** exact covering at \(\kappa>D'h\), with the converse modulo higher-height increments.

Rider 1's inserted clause:

> “**Which lift the equality is read at.** (IDX-TWIST) is an equality of polynomials AT the compatible choice \(B_0=-L_\kappa(\sigma\eta^{q(\kappa)})\), and that choice is one DEFINITION HE6-1 permits: \(-L_\kappa(\sigma\eta^{q(\kappa)})\) has \(\deg<D=D'\) and \(h=\kappa\), hence \(d(B_0)=\kappa\), by S1.4's `(LIFT)`, and its print residue is \(\gamma_\kappa\bigl(-L_\kappa(\sigma\eta^{q(\kappa)})\bigr)\eta^{-q(\kappa)}=-\sigma\eta^{q(\kappa)}\eta^{-q(\kappa)}=-\sigma=c_0\) by the same `(HE6-SEAM)` read at \(\xi=\theta\) used just above — so the recipe's three conditions at line 204 are met, and the master's own recentering \(\Phi'-L_\kappa(\sigma\eta^{q(\kappa)})\) IS a member of the family at the label \(r=Z-\sigma\). For an ARBITRARY (LIFT)-permitted choice, line 204 fixes only the height and the print residue: two permitted \(B_0\) differ by an \(E\) with \(\deg E<D'\) and \(d(E)\ge\kappa\) whose digit at \(\kappa\) is \(\gamma_\kappa(B_0)-\gamma_\kappa(B_0')=0\) … so \(E=0\) or \(h(E)>\kappa\) … Read at an arbitrary permitted choice, (IDX-TWIST) therefore holds MODULO increments of height \(>\kappa\), and what is choice-independent is exactly the pair \(h(-B_0)=\kappa\), \(\operatorname{dig}(-B_0)=\sigma\eta^{q(\kappa)}\) that the seam read above produced.”

with its two directions distinguished:

> “The COVERING direction — every nonempty recentering \(\Phi'-L_\kappa(c)\) of this master at a height \(\kappa>D'h\) … is a member of the family, at the label \(r=Z-c\eta^{-q(\kappa)}\) and the permitted choice \(B_0=-L_\kappa(c)\) — is EXACT, by the permittedness just checked, and it is the direction S1.8C consumes. The CONVERSE reading — that an arbitrarily chosen member is literally the designated recentering — holds up to an increment of height \(>\kappa\), which is to say it holds exactly in the height-and-digit data that every consumer of it reads.”

r10 repair 2's height correction, quoted in the form it inserts:

> “at a height \(\kappa>D'h\) — DEFINITION HE6-1's own standing hypothesis (`HE6_PROOF_2026-08-08.md`:202), strictly stronger than fullness, since \(\kappa>D'h>(D'-1)h\) makes \(\kappa\) full by S1.4's uniform sufficient bound while a full \(\kappa\) in the band \((D'-1)h\le\kappa\le D'h\) indexes no member of the family — is a member of the family”

The four “member for member” sites are classified, none amended:

> “Line 3677 … and line 3755 … both state the COVERING direction, which the qualifier certifies as exact; they stand unchanged. Lines 3709 and 3797 state the identity of SETS …, and they are the two sentences the qualifier's closing clause scopes: exact at the compatible choice, and up to an increment of height \(>\kappa\) at an arbitrary permitted one.”

**DERIVATION:** `[QUALIFIER, NOT REPAIR]` DEFINITION HE6-1 constrains \(B_0\) by a height and a *print residue* only, while `(LIFT)` designates a lift by a height and a *digit*; the seam therefore yields the choice-independent pair, and polynomial equality only at the compatible choice. Permittedness of that choice is checked by computing its print residue back through `(HE6-SEAM)`. The height correction is a strict-inequality comparison: \(\kappa>D'h>(D'-1)h\) implies full, but a full \(\kappa\) in \([(D'-1)h,\;D'h]\) indexes no family member, so “full” was too weak a hypothesis for the covering claim.

**CONDITIONALITY:** Nothing downstream narrows: `(WINDOW)` puts every side slope strictly above \(T=D'h\), and S1.8C — the covering direction's only consumer — reads it at \(\lambda>T=D'h\). Both qualifiers are counter-neutral: Rider 1 folds a minors-only pass and resets nothing; r10 repair 2 is a minor inside a gap-bearing round. The level-one `(RISE)` discharge is explicitly **unchanged** by the lift qualifier, since it “consults neither the family's print label nor the recipe's lift”.

**RESOLUTION TRACE:** **statement lines:** Rider 1, 3821–3839, and r10 repair 2, 3999–4015; **proof lines:** permitted-choice and height comparison arguments in the same spans; **correction sites:** `(IDX-TWIST)` line 3706 and Rider phrase 3831. Destination: `EFF.T2.s1of6.20`.

**TEETH:** the lift-choice qualifier tooth; the covering-height tooth; the consumer-census check (six consumption sites of `(IDX-TWIST)`, all choice-independent).

---

### EFF.T2.s4of6.07  [run-record]

**CANONICAL STATEMENT:** `[ASSEMBLED — the r9 round record: provenance, the pass's certification list, and the counter]`

**FORM:** bold-headed paragraph.

> “*(Opus/Claude repair arm, against the r8 hostile pass of record `runs/comp13/T2SOL_output.log` — this note's FIRST model-diverse pass (gpt-5.6-sol high) on the current architecture, returning 0 CRITICAL + 1 GAP + 1 minor; counter resets.)*”

> “**What the GAP is, and what it is not.** The pass certified the master core end to end and said so clause by clause: "`(ACCOUNT)` correctly yields clause 1 and `(SIDE-COUNT)`"; "`(RES-DEG)` plus the defect-free fundamental identity gives the termwise orbit count and exact \((e,f)\)"; "`(SIDE-PROD)`, `(LABEL-PROD)`, `(MID-PEEL)`, terminal reads, child jumps, and linear refinements form a complete recursive analysis under the stated block/continuation hypotheses"; "The clause-5 trigger analysis covers both key-changing transitions, while transition 6 returns key-free residual blocks by definition"; "`(RANK)=(\deg F,\mu,\sigma_{\mathcal C})` decreases correctly in every transition, with `(SEC-RANK)` honestly explicit"; "`(LB1)` and `(MP1)` remain fenced; I found no fence leakage"; and "S1.8B's level-two transport and composed-key \(W+w\) routing are coherent. The HE6-PROOF \(\ell\ge2\) caveat is correctly routed to the product-\(\ge2\) child branch."”

> “the abstract theorem remains true because `(RISE)` is an explicit carrier hypothesis; the instance discharge is the gap”

> “**No theorem-statement change is made by this round.** … The one new display, (IDX-TWIST), is a derived identity of the level-one instance, not a clause of the theorem. Two sentences of the r8 counter survive verbatim and are not booked: "DEFINITION HE6-1's family at \(\ell=1\) IS the family of HE3's recenterings" is a statement about the family as a SET, which (IDX-TWIST) confirms, and "its recipe puts the \((\ell,d)=(1,1)\) member's increment at the member's own index" is a statement about HEIGHTS, which the twist does not touch. What was false was only the matching of index names”

> “The finding is credited to that pass in full: it caught a print/coherent frame conflation that five same-arm passes did not.”

> “**The clean-pass counter is RESET to 0 of 2.**”

**DERIVATION:** `[ROUND RECORD]` The round's scope discipline is the certification list: everything the pass certified is named and left untouched, and the repair is confined to the one identification refuted. The credit sentence is a methodological record — the first model-diverse pass caught what five same-arm passes did not — which the acceptance record later generalises into its two-arm requirement.

**CONDITIONALITY:** Historical as to counter. Its own census (“Six master-internal landmark strings, on four lines … Five source lines … Two span delimiters”) is confirmed at compilation (§3). The three pins this round consumes were already on both level-one lines, so no Source-pin line is amended.

**RESOLUTION TRACE:** **statement lines:** r9 header 3671–3677 and counter 3793–3805; **proof lines:** pass artifact `runs/comp13/T2SOL_output.log` (existence verified); **correction sites:** r8 census correction 3767–3789, carried at `.01`.

**TEETH:** S7 acceptance gate; the r8 hostile pass of record (the first model-diverse one); the print/coherent mutant this round installs the cure for.

---

### EFF.T2.s4of6.08  [fence]

**CANONICAL STATEMENT:** `[VERBATIM — the frame-authority fence: what supplies the transport law, and what is cited without being consumed (r9 repair 1(e))]`

**FORM:** bold-headed paragraph.

> “**(e) The frame authority, and what is NOT cited.** The transport law consumed above is this master's own `(HE6-SEAM)`, whose supplier pin is `HE6-SLOT-SEAM` (LEMMA HE6-0″, adjudicated at r4 repair 1 for exactly the two clauses `(SLOT-V)` and the \(\varpi\)-read conversion, with the `[r2]` sign correction this master reproduces). No new pin and no new source is needed for it. The gauge-cocycle note T1 (`T1_GAUGE_COCYCLE_2026-08-12.md`, ACCEPTED 2/2 on 2026-08-13) books the same SHAPE of error at its own seam — its honesty ledger records that "raw print/canonical equality is iff \(z^\nu=1\), not iff \(\nu=0\)" — and that sentence is recorded here as methodological precedent ONLY: T1 is not among this master's six source files, no pin covers it, and nothing above consumes it. S0.2's no-unpinned-material clause is therefore respected in the strict sense it states.”

**DERIVATION:** `[NON-IMPORT DECLARATION]` Two claims, each with its own ground: the transport law is already pinned (so the r9 repair adds no source dependency), and the cross-note observation is precedent rather than premise (so S0.2's no-unpinned-material clause is not breached by naming a note outside the six sources). The distinction is the same one r12 repair 3 later generalises: “Consumption, not citation, is the gated relation.”

**CONDITIONALITY:** T1 is not a source of this note and carries no pin here. Anything a later reader wants from T1 must be re-derived or newly pinned; the precedent status of the quoted sentence gives it no evidential force in T2. The two clauses read at `HE6-SLOT-SEAM` are understood in the normalized post-F-1 form fixed by T2's own `(GAMMA)`/`(HE6-SEAM)` displays and the terminal dated record.

**RESOLUTION TRACE:** **statement lines:** r9 repair 1(e), 3757–3763; **proof lines:** the same paragraph's pin-versus-precedent argument and T1's uniquely verified honesty-ledger string; **correction sites:** terminal HE6 F-1 adjudication 4511–4540. T1 is named, not consumed.

**TEETH:** the standing S7 consumption sweep (a named-but-not-consumed unit must be classified, not silently left); the print/coherent-frame mutant.

---

### EFF.T2.s4of6.09  [acceptance-box]

**CANONICAL STATEMENT:** `[ASSEMBLED — post-pass rider block 1: the fold rule, the landmark convention, and the counter state it sets]`

**FORM:** bold-headed paragraph.

**SUPERSESSION KIND:** counter re-reading.

**TARGETS:** `EFF.T2.s4of6.07`.

**CHAIN:** r9 reset to 0/2 → Rider 1 clean pass, counter 1/2 without reset → r10 reset → terminal acceptance 2/2 → **TERMINAL:** acceptance record.

> “**Dated rider (2026-08-13 — the single minor of the gpt-5.6-sol high hostile pass on the r9 effective text, `runs/comp13/T2SOL2_output.log`, returning 0 CRITICAL + 0 GAP + 1 minor. Folded per the campaign's minors-only precedent — T4's post-pass gloss-direction rider, T5's two post-pass-1 locator riders, and T1's five post-pass-1 riders, each folded on a 0C + 0G pass and each subsequently confirmed to 2/2. This note carries no earlier rider precedent, every previous round having been gap-or-worse; the T1 acceptance-fold rider style is followed here. NO r-section, no grade event, NO counter reset.)** The rider QUALIFIES one display and does nothing else.”

> “**Landmark convention (rider).** Counts are taken over the note AS IT STOOD BEFORE THIS BLOCK — lines 1–3805, i.e. through the last line of the r9 counter — because each landmark is necessarily quoted a second time here.”

> “This rider folds the single MINOR of a hostile pass that returned **0 CRITICAL + 0 GAP + 1 minor**. S7's reset rule is stated of gap-or-worse repairs only ("A gap-or-worse repair resets the clean count"), and a minors-only pass is a clean pass whose minors fold as post-pass riders (T4, T5, T1). Folding riders is not a repair round: **the clean-pass counter is NOT reset and STANDS AT 1 OF 2.** Pass of record: the gpt-5.6-sol high hostile pass on the r9 effective text, `runs/comp13/T2SOL2_output.log`, 2026-08-13 — this note's second model-diverse pass and its first clean one. Still owed at the S7 acceptance gate: the Opus/Claude-arm confirmation pass, the second of the two consecutive clean passes, taken over the r9 effective text PLUS this rider.”

**DERIVATION:** `[COUNTER-STATE RECORD]` The fold rule is read off S7's own wording: the reset rule is stated of gap-or-worse repairs, so a minors-only pass is a clean pass and folding its minor is not a repair round. The campaign precedent (T4, T5, T1) is cited as the practice, and the note records that it has no earlier rider precedent of its own. The landmark bound advances to 3805 because the rider necessarily re-quotes each landmark.

**CONDITIONALITY:** The 1-of-2 counter is superseded: the confirmation pass this block awaits (`T2P8`, r10's pass of record) returned 0C+1G+1m, so the counter resets again at r10 and reaches 2/2 only at post-pass rider 3. The qualifier this rider installs is itself qualified by r10 repair 2 (`.06`).

**RESOLUTION TRACE:** **statement lines:** Rider 1 and its counter, 3809–3843; **proof lines:** pass artifact `runs/comp13/T2SOL2_output.log` plus the fold-rule argument in 3841–3843; **correction sites:** r10 repair 2, 3999–4015, and terminal acceptance 4479–4505.

**TEETH:** S7 acceptance gate; the minors-only fold precedent; the consumer census of `(IDX-TWIST)`.

---

### EFF.T2.s4of6.10  [run-record]

**CANONICAL STATEMENT:** `[ASSEMBLED — the r10 round record: provenance and the first end-to-end pin-gate run]`

**FORM:** bold-headed paragraph.

> “*(Opus/Claude repair arm, against the Opus-arm confirmation pass of record `runs/comp13/T2P8_output.log`, taken over the r9 effective text PLUS the post-pass rider and returning 0 CRITICAL + 1 GAP + 1 minor; counter resets.)*”

> “**What the pass verified, and what this round does not touch.** The pass re-derived the rider's three claims from the byte-level source and upheld all three: the compatible choice \(B_0=-L_\kappa(\sigma\eta^{q(\kappa)})\) is `(LIFT)`-permitted, read against `HE6_PROOF_2026-08-08.md`:201–205; two permitted choices differ by height \(>\kappa\); and every downstream consumer of (IDX-TWIST) is choice-independent, the consumer census being complete at six sites (3709, 3739, 3747, 3755, 3759, 3797) across the rider's two classifying paragraphs. It then ran the full 32-pin gate end to end on the final text for the first time — 15 quoted pairs with all 30 delimiter lines at `grep -cFx` = 1, 4 first-only, 13 description-only, zero `T2V2PIN` lines in the six sources, zero source edits — and fourteen supersession landmarks across r1–r9 and the rider, including the `(MP1′)` retirement chain, with no orphan, no double-render and no ambiguous insertion point. None of that is touched below. The GAP is not in the master core, the r9 coherent-frame rebuild, or the rider's mathematics; it is in the pin table, at the two level-one instance records.”

**DERIVATION:** `[ROUND RECORD / GATE RESULT]` The gate run is the note's own S7 pin gate executed in full for the first time, and its result is stated in the gate's own four terms (pair resolution, taxonomy, synthetic-marker count, source-edit count). The round's scope follows from where the gate failed: not in the mathematics but in the pin table's coverage of two instance records.

**CONDITIONALITY:** The gate figures are at 32 pins and are superseded to 37 with taxonomy \(20+4+13\); the acceptance record's pass 2 re-runs the same gate whole at 37. The counter reset is superseded to 2/2.

**RESOLUTION TRACE:** **statement lines:** r10 header 3847–3853; **proof lines:** pass artifact `runs/comp13/T2P8_output.log`; **correction sites:** r10 counter 4019–4031 (shard-5 HOME) and terminal acceptance 4479–4505.

**TEETH:** S7 pin gate (this is the tooth firing); the standing consumption sweep; the acceptance gate.

---

### EFF.T2.s4of6.11  [table]

**CANONICAL STATEMENT:** `[TABLE — the three new HE6 pins of r10, with their delimiters and S0.2 adjudications; delimiter lines verbatim and re-verified at compilation]`

**FORM:** display.

**SUPERSESSION KIND:** inventory completion.

**TARGETS:** `EFF.T2.s1of6.03`, `EFF.T2.s1of6.33`, `EFF.T2.s1of6.34`, `EFF.T2.s1of6.35`, `EFF.T2.s1of6.36`, `EFF.T2.s1of6.37`.

**CHAIN:** 32-pin inventory → r10 adds three HE6 spans (35) → r11 adds `HE6-BETA-GUARD` (36) → r12 adds one quoted-pair pin (37) → **TERMINAL:** acceptance inventory \(20+4+13=37\).

| Pin | Named unit | First delimiter | Last delimiter |
|---|---|---|---|
| `HE6-TEST-PACKAGE` | the seven consecutive statement-display units LEMMA HE6-0′ through LEMMA HE6-5, HE6 §S1 (244–287) | `> **LEMMA HE6-0′ (the local forcing — HE3-0′ REPAIRED at ℓ ≥ 2).** If a root` | `> Galois-stable, at every f₁ (no f₁ = 1 restriction).` |
| `HE6-ROOT-LABEL` | COROLLARY HE6-2″, the bolded unit closing HE6 §S4 (517–521) | `**Corollary HE6-2″ (HE3-0(iii), re-proved in this convention).** Taking` | `of ρ; it is well defined because R_λ is a fixed element of K[Z].` |
| `HE6-ACCOUNT` | HE6 §S5, the proof unit of LEMMA HE6-3 and LEMMA HE6-4, carrying (RES) and (†) (525–604) | `## S5. LEMMA HE6-3 (slope counts, (SEP)-free) AND LEMMA HE6-4 (nonemptiness)` | `what replaces HE3-BOX-6's base change.` |

The three S0.2 adjudications, each recorded at its pin:

> “Condition 1: each of the seven is a "named statement display" — the unit type r3 repair 2 added to S0.2's list — and each occurs exactly once in that type; the same lemmas' proof units (§S3.2, §S3.3, §S4, §S5, §S6.1) are units of a different type, exactly as `HE3-A-STMT` and `HE3-A-PROOF` are two pins over one theorem.”

> “Condition 1: the designation "HE6-2″" is borne by exactly one unit, this one; its only other occurrence in the file, HE6:645, is a citation inside the `HE6-PROOF` span. S0.2's outermost-unit rule resolves the pin to the outermost unit bearing that designation … Condition 4: the unit runs from its bolded opening to the end of its statement and ends immediately before the next unit of equal or higher rank, `## S5.` at 525; the horizontal rule at 523 is a boundary marker and not unit content”

> “Condition 4: the span contains (RES) at 529, the generic-letter construction at 533–538, the ξ-side and ρ-side bullets at 540–547, the identity (†) at 551, parts (a) and (b) of LEMMA HE6-3's proof, the (SEP)-counterexample remark, the complete proof of LEMMA HE6-4 with its ∎, and the closing "why this is not the tautology it looks like" remark”

Two reading directives are attached at the new pins:

> “The span contains LEMMA HE6-2's statement guard "when δ has denominator dividing ℓ", which the source's own dated append re-scopes: `**Correction 2 (PE4 F-2 — LEMMA HE6-2's β-definedness guard).** The` (`HE6_PROOF_2026-08-08.md`:1036) directs that the guard be read as "when δ = κ". **The 2026-08-09 post-PE4 correction governs, and is itself pinned, as `HE6-BETA-GUARD` (r11).** This master consumes from LEMMA HE6-2 only the dichotomy \(\varepsilon(\xi)>0\iff\delta=\kappa\) and \(\iota_\xi(r)(\beta)=0\) … which the correction leaves byte-unchanged”

> “The span contains the inference inside LEMMA HE6-3(a)'s proof that the source's own dated append replaces: `**Dated correction (2026-08-12, post-acceptance challenge F-2 — completion of LEMMA HE6-3(a)).** In the proof of LEMMA HE6-3(a), replace the inference` (`HE6_PROOF_2026-08-08.md`:1115) … **The 2026-08-12 correction governs.** Nothing this master consumes from the span is touched by it: the correction operates strictly below (†) at HE6:551, and this master derives its own clause 1 … from `(ACCOUNT)` in S2's proof rather than consuming LEMMA HE6-3(a). Recorded at the same pin: the phrase "that instance's pinned proof" in S1.7's closing sentence denotes this span together with `HE6-PROOF`.”

**DERIVATION:** `[PIN DERIVATION]` Each pin is derived from an itemised consumption (`.12`) and closed by a delimiter pair, with the S0.2 condition that was in doubt adjudicated explicitly: the multi-unit run for `HE6-TEST-PACKAGE` (the form r3 repair 1(b) and r4 repair 5 declared), the outermost-unit and horizontal-rule rules for `HE6-ROOT-LABEL`, and the `##`-unit boundary rule for `HE6-ACCOUNT`. All six delimiter lines re-verified at 1 at exactly the recorded line numbers (§3, part B).

**CONDITIONALITY:** The pin count this round sets (35) is superseded to 37. The two reading directives have different effective dispositions. The beta-definedness correction is **consumed material**: r11 pins it separately as `HE6-BETA-GUARD`, cites it at `HE6-TEST-PACKAGE`, and records its transitive use inside `HE6-ACCOUNT`. The 2026-08-12 HE6-3(a) correction remains an unpinned reading directive because it changes only proof-internal material below (\(\dagger\)) that this master does not consume. The distinction is the consumption test, not the common form “reading directive”.

**RESOLUTION TRACE:** **statement lines:** r10 repair 1(b), 3879–3914; **proof lines:** HE6 span/delimiter evidence 244–287, 517–521, 525–604; **correction sites:** r11 4051–4088 and 4142, acceptance 4501, non-superseding record 4513. Destination: `EFF.T2.s1of6.03`; later beta-guard row remains shard-5 HOME.

**TEETH:** S7 pin gate conditions 1–4; the pin-note directive coherence audit; the standing consumption sweep.

---

### EFF.T2.s4of6.12  [table]

**CANONICAL STATEMENT:** `[TABLE / ASSEMBLED — the itemised level-one HE6 consumption map of r10 repair 1(a), with r11's governing beta-guard pin and transitive route applied: which pinned unit supplies each S1.7 clause and (ACCOUNT)]`

**FORM:** display.

**SUPERSESSION KIND:** inventory completion.

**TARGETS:** `EFF.T2.s1of6.03`, `EFF.T2.s1of6.33`, `EFF.T2.s1of6.34`, `EFF.T2.s1of6.35`, `EFF.T2.s1of6.36`, `EFF.T2.s1of6.37`.

**CHAIN:** unpinned HE6 level-one consumptions → r10 three-span supplier map → r11 direct/transitive `HE6-BETA-GUARD` coverage → r12 final Source-pin reset → **TERMINAL:** accepted 37-pin supplier map.

| Master demand | HE6 supplier (statement display unless noted) | Pin |
|---|---|---|
| S1.7 clause 1 — roots in the intended disk carrying \(r'\)'s label; S3.2's “ξ-side” | LEMMA HE6-1, `> **LEMMA HE6-1 (the ξ-side: EVERY root of a test polynomial carries the` (250) | `HE6-TEST-PACKAGE` |
| S1.7 clause 2 — generic value attained off the class, rising strictly on it | LEMMA HE6-2 (255) and LEMMA HE6-2′ (266), with the former's \(\beta\)-guard re-scoped by post-PE4 Correction 2 | `HE6-TEST-PACKAGE`, read under `HE6-BETA-GUARD` |
| S1.7 clause 3 — resultant symmetry makes the class nonempty | LEMMA HE6-4 (280); the argument at (RES), `    (RES)   Σ_{ρ : f(ρ)=0} dv(g(ρ)) = e₁·v(±Res(f,g)) = Σ_{ξ : g(ξ)=0} dv(f(ξ)),` (529) | `HE6-TEST-PACKAGE`, `HE6-ACCOUNT` |
| S1.7 clause 4 — disjointness and exhaustion; the side total \(n_\lambda=D'L_\lambda\) | item 1 of THEOREM HE6.A's proof (644–648, already inside `HE6-PROOF`) with its premise COROLLARY HE6-2″ (517); LEMMA HE6-3 (271) | `HE6-PROOF`, `HE6-ROOT-LABEL`, `HE6-TEST-PACKAGE` |
| S1.7 clause 5 — Galois equivariance of labels | LEMMA HE6-5 (284) | `HE6-TEST-PACKAGE` |
| S1.7 clause 6 — local invariants divisible by \(e_{\mathcal C}e'\), \(f_{\mathcal C}\deg r'\) (“local forcing”) | LEMMA HE6-0′ (244) | `HE6-TEST-PACKAGE` |
| S1.7 clause 7 and \(\deg\Psi_{\lambda,r'}=De'\deg r'\) — integrality | DEFINITION HE6-1, with lift legality from LEMMA HE6-1L | `HE6-FAMILY`, `HE6-LIFT-1L` (already pinned) |
| `(ACCOUNT)` — the boxed side-accounting identity | (†), `    (†)   Σ_λ n_λ·min(λ, κ) = D′·h_F(κ) = D′·Σ_λ L_λ·min(λ, κ),` (551), with the root-to-slope passage at 545–547; the proof invokes LEMMA HE6-2 at arbitrary roots | `HE6-ACCOUNT`, with the LEMMA HE6-2 read under `HE6-BETA-GUARD` |
| S1.7's closing sentence — “those arguments remain inside that instance's pinned proof” | the generic test (533), the ξ-side (540) and ρ-side (545) evaluations, resultant symmetry (529) | `HE6-ACCOUNT` (with `HE6-PROOF`), and `HE6-BETA-GUARD` for the transitive LEMMA HE6-2 read |

The `(ACCOUNT)` identification is stated with its own translation:

> “asserted there for EVERY rational \(\kappa>D'h\) — wider than the master's "\(\kappa>T\) outside the finite set of critical values". The master's left side sums over roots and (†)'s over slopes; the passage between them is displayed in the same proof … that is \(\sum_\rho\min(\lambda_\rho,\kappa)=\sum_\lambda n_\lambda\min(\lambda,\kappa)\) after dividing by \(\ell d_0\). With \(d=dv\), \(D=D'\), \(T=D'h\) and \(w_\rho=dv(\Phi'(\rho))\) by `(HE3-INSTANCE)`/`(HE6-INSTANCE)`, the two displays together are this master's `(ACCOUNT)` verbatim.”

and the two senses of “ξ-side/ρ-side” are disambiguated:

> “HE6 uses the words "ξ-side" and "ρ-side" in two senses — as the title of LEMMA HE6-1, which is S3.2's sense at item 1, and as the two sides of the resultant identity (RES), which is this sentence's sense … Both senses now resolve into pins, the first into `HE6-TEST-PACKAGE` and the second into `HE6-ACCOUNT`.”

**DERIVATION:** `[CONSUMPTION ITEMISATION]` The defect was that the level-one records credited HE6 with the test package and `(ACCOUNT)` while “Every one of those four ranges is disjoint from every one of the five pin spans”, so “the only side-accounting identity the master proof uses has, at both level-one instance records, no pinned supplier at all”. The cure itemises each demand, names its supplier line, and pins it. Four grounds of the finding are answered one by one: S0.2's no-unpinned-material clause, S5 ledger item 3, S1.7's closing sentence, and the standing S7 check.

**CONDITIONALITY:** No sentence of S1.7, S3.1 or S3.2 is amended — the two crediting sentences (base line 889 and r1 repair 2's line 1454) “are TRUE of the pin table this repair installs and false of the one it replaces”, and both are left standing. The `(ACCOUNT)` supplier is wider than the master's use, not narrower. Rider 2 widens r11's “only two sites” census: there are two direct master citation sites plus the transitive route through the LEMMA HE6-2 invocations inside `HE6-ACCOUNT`; it adds no pin because those invocations already lie in that span, while their external correction is covered by `HE6-BETA-GUARD`. The consumption reaches a recentered level-one key only through S1.8C's (vi)/(vii) transport, which is why the pins also join the three level-two Source-pin lines.

**RESOLUTION TRACE:** **statement lines:** r10 repair 1(a), 3867–3877, and 1(d), 3922–3927; **proof lines:** HE6 supplier displays/proof spans 130–138, 201–287, 517–604, 639–666; **correction sites:** r11 4072–4088 and 4140–4142, Rider 2 at 4166–4196, r12 at 4206–4349, acceptance 4501. Destination: `EFF.T2.s1of6.33`–`.37`.

**TEETH:** S7 pin gate; the standing consumption sweep; the `(ACCOUNT)` supplier gate; the HE6 test-package gate.

---

### EFF.T2.s4of6.13  [fence]

**CANONICAL STATEMENT:** `[VERBATIM — r10 repair 1(c): what the repair declines to pin, with the ground for each declination]`

**FORM:** bold-headed paragraph.

> “- **LEMMA HE6-0** (the disk criterion, HE6:240–242), the unit immediately preceding the new span. This master names it only to fix a boundary … and consumes no disk criterion: what S1.7 clause 1 reads as "its roots lie in the intended disk" is LEMMA HE6-1's own conclusion that every root of \(\Psi_{\kappa,r}\) satisfies (T1)/(T2), inside the new span. Where HE6-0 is used, it is used inside HE6's own arguments … which is the source's business and not a master-side consumption.
> - **THEOREM HE6.A's statement display** (HE6:289–301). … This master names THEOREM HE6.A at two sites only: the `(PINS)` row description "the complete proof unit of THEOREM HE6.A", which names it to fix a boundary, and S4.1's grade line "HE6 is accepted \(2/2\)". S3.2's conclusion display \(|S_{\lambda,r}|=D'\ell\deg r\), \((e,f)=(e_1\ell,f_1\deg r)\) is this master's OWN clause-2 output at the instance, derived in S2's proof from `(SIDE-COUNT)`, `(RES-DEG)` and the test package — not consumed from HE6.A. Pinning it would cover no consumption. The asymmetry with HE3 is real and is adjudicated here rather than cured: `HE3-A-STMT` is pinned because S3.1's instance substitution reads THEOREM HE3.A's stated data, whereas S3.2's corresponding display is derived by this master and is not read from HE6.A.
> - **§S3.3** (the proof of LEMMA HE6-1), **§S4's two evaluation proofs**, and **§S6.1** (the proof of LEMMA HE6-5). What S3.2's line 889 credits to "HE6's ξ-side, evaluation formulas … and equivariance" are those lemmas' ASSERTIONS, now inside `HE6-TEST-PACKAGE`; this master consumes none of their proofs and reproves none of them.”

**DERIVATION:** `[SIGNED NON-APPLICABILITY]` Each declination applies the consumption test: a unit is pinned when the master reads a claim from it, not when the master names it. Boundary-fixing citations (LEMMA HE6-0, the `(PINS)` row description) and grade lines are naming, not consumption; a display the master derives itself (S3.2's conclusion) is not consumed from the source that also states it; and a lemma's proof is not consumed when only its assertion is read. The HE3/HE6 asymmetry is adjudicated on exactly this test rather than cured by adding a pin.

**CONDITIONALITY:** The declinations are scoped per consumption, as at shard 2 `.03`: declining LEMMA HE6-0 as a supplier of the disk criterion is not a statement that HE6-0 is unused inside HE6's own arguments. Should a later round read a claim from any declined unit, the declination would have to be revisited.

**RESOLUTION TRACE:** r10 repair 1(c) (3916–3920). Destination: the `(PINS)` table's coverage claim → shard 1, `.03`; the two crediting sentences (889, 1454) are located and left unamended.

**TEETH:** the standing S7 consumption sweep (a declination must be signed, not silent); S7 pin gate condition 1.

---

### EFF.T2.s4of6.14  [table]

**CANONICAL STATEMENT:** `[TABLE — r10's inventory bookkeeping (repair 1(e))]`

**FORM:** display.

**SUPERSESSION KIND:** inventory completion.

**TARGETS:** `EFF.T2.s1of6.03`, `EFF.T2.s1of6.42`.

**CHAIN:** r10 35 pins / 18+4+13 / eight cures → r11 36 / 19+4+13 / nine → r12 and acceptance 37 / 20+4+13 / ten → **TERMINAL:** acceptance record.

| Quantity | Value set at r10 | Status |
|---|---|---|
| Pin count | 35 — “Pin arithmetic for the gate: 26 (r2) → 27 (r3 repair 1 splits `HETOW-LABELS`) → 29 (r4 repair 13 adds two) → 32 (r5 adds three) → **35** (r10 adds three).” | **Superseded** to 37 (r11 +1, r12 +1) |
| Taxonomy | “Totals: 18 pairs + 4 first-only + 13 description-only = 35.” | **Superseded** to \(20+4+13=37\) |
| Quoted-pair roster | “… `HE6-PROOF` (r6); and `HE6-TEST-PACKAGE`, `HE6-ROOT-LABEL`, `HE6-ACCOUNT` (r10) — **18 of 35**.” | Superseded as a count; the roster's membership stands |
| Cured-consumption census | “**Eight** consumptions have been cured this way.” — five at r4/r5, three at r10 | **Superseded** to nine (r11) and ten (r12/acceptance) |
| r4 repair 13's count clause | “so the count of cured consumptions reads two of five; the r9 + rider pass then found the r5 sweep incomplete in turn, and three further consumptions were cured at r10, so the count now reads two of eight.” | Superseded to “two of ten” |
| Synthetic markers / source edits | zero / zero | Effective; confirmed at acceptance |

**ARITHMETIC AUDIT:** r10: \(18+4+13=35\). Terminal inventory: the r11 and r12 additions are both quoted pairs, so \((18+2)+4+13=20+4+13=37\), agreeing with acceptance. Pin-chain cross-check: \(26+1+2+3+3+1+1=37\). Cured-consumption cross-check: the five pre-r10 cures plus three at r10 plus one at r11 plus one at r12 give \(5+3+1+1=10\).

A self-correction is recorded with the census:

> “(The superseded sentence also miscounted its own list, reading "Four" against the five cures it then names. The list is the authority; the corrected total counts those five together with this round's three.)”

**DERIVATION:** `[BOOKKEEPING]` Each figure is derived from the round's own three additions, and the arithmetic chain is stated so the gate can re-derive it rather than trust a total. The census correction applies the note's standing rule that an enumeration governs its own count.

**CONDITIONALITY:** Every count here is superseded downstream. The rule the round applies — list over total — is not.

**RESOLUTION TRACE:** **statement lines:** r10 repair 1(e), 3929–3965; **proof lines:** the arithmetic chain and enumerated roster in the same span; **correction sites:** eleven earlier landmarks at 2900 (×2), 3123, 3391, 2791, 2799, 2858, 2859, 3449, 3455, 3461, followed by r11 4090–4138, r12 4285–4309, acceptance 4501. Destination: `EFF.T2.s1of6.03`, `.42`.

**TEETH:** S7 pin gate; the pin-sweep leg of the acceptance gate.

---

### EFF.T2.s4of6.15  [table]

**CANONICAL STATEMENT:** `[TABLE / ASSEMBLED — the r10 re-run of the standing consumption sweep (repair 1(g)), with r11, r12, and the terminal dated F1–F4 consumption record applied]`

**FORM:** display.

**SUPERSESSION KIND:** inventory completion.

**TARGETS:** `EFF.T2.s1of6.42`, `EFF.T2.s4of6.11`, `EFF.T2.s4of6.12`.

**CHAIN:** r10 HE6-only append sweep → r11 beta-guard asymmetry correction → r12 five-source append sweep → terminal dated F1–F4 consumption record → **TERMINAL:** the effective protocol classification below, without text supersession.

| Class examined | Disposition |
|---|---|
| HE6 §S5 | pinned entire, as `HE6-ACCOUNT` |
| HE6 §S4 | COROLLARY HE6-2″ pinned as `HE6-ROOT-LABEL`; the two evaluation proofs not consumed |
| HE6 §S3.3, §S6.1 | not consumed; what is consumed is each lemma's statement display, all seven pinned as `HE6-TEST-PACKAGE` |
| r5's six residues (LEMMA HETOW-3, LEMMA GENTOW-4.1, LEMMA HE7-13, DEFINITION HE6-1, GENHN-3(a), HE3's `[r1-N1]`) | unchanged |
| HE6 post-PE4 Correction 2 | Consumed as the well-posedness guard on LEMMA HE6-2; separately pinned by r11 as `HE6-BETA-GUARD`, directly at `HE6-TEST-PACKAGE` and transitively through `HE6-ACCOUNT`. |
| HE6 2026-08-12 F-2 | Proof-internal below (\(\dagger\)); remains an unpinned reading directive at `HE6-ACCOUNT` because no consumed conclusion moves. |
| HE6 2026-08-12 F-1 | Lands on the consumed conversion clause inside `HE6-SLOT-SEAM`, contrary to r11's loose “touch nothing” phrase; T2 is immune because its own `(GAMMA)`/`(HE6-SEAM)` form is already the corrected normalized form, independently supported through `HE3-HE3-1L`. No new pin is owed. |
| HE6 post-PE4 Correction 1 and 2026-08-12 F-3/F-4 | Touch units or proof material this master declines or does not read; no consumed assertion moves. |
| Boundary-fixing and declining quotations (HE6:668 at master 3118; the GENTOW4 TOWERRAT2-C annex heading at master 2564) | “remain declinations, not consumptions, and are unaffected” |

> “This enumeration is recorded, not asserted complete; the gate re-runs it.”

**DERIVATION:** `[SWEEP RECORD]` The sweep is the standing S7 check (installed r4 repair 13) re-run at the two level-one records, in answer to the same check's own earlier incompleteness. Its new element is the **dated-append class**: each HE6 append is tested for whether it changes a claim T2 actually reads. r11 applies that test asymmetrically to the beta guard and HE6-3(a), Rider 2 adds the transitive account route, r12 applies it to the five non-HE6 sources, and the terminal dated record sharpens F-1 from “touches nothing” to “touches a consumed clause whose corrected form T2 already carries” without changing the disposition.

**CONDITIONALITY:** The r10 enumeration is declared incomplete by construction. Its dated-append leg is HE6-only; r12 sweeps the other five sources, and acceptance records the resulting 37-pin gate. The 2026-08-14 F1–F4 block is expressly a **consumption record with NO supersession**: it corrects the precision of the r11 F-1 description but changes no accepted text, pin, span, fence, obligation, Source-pin line, or count.

**RESOLUTION TRACE:** **statement lines:** r10 repair 1(g), 3991–3995; **proof lines:** the HE6 append classifications at 3892–3914 and the supplier spans named in `.11`/`.12`; **correction sites:** r11 4142, Rider 2 at 4166–4196, r12 sweep 4329–4349, acceptance 4501, terminal record 4511–4542.

**TEETH:** the standing S7 consumption sweep; the pin-note directive coherence audit.

---

## 3. XREF verification ledger

**Method.** On 2026-08-18 each distinct foreign target designation used as a supplier or load-bearing inbound correction by a local statement was searched as a fixed string in its named source. The 17 unique designations in part A are the XREF total. Quote-line evidence, internal supersession landmarks, negative NON-IMPORT referents, the named-but-not-consumed T1 precedent, and artifact/path checks are verified separately and are not added to that total. A fixed-string miss or ambiguity would be an `OPEN-CALL`; none occurred.

### A. Fixed-string target designations (counted XREFs)

`[TABLE — compiler verification ledger]`

| XREF | Target designation searched | File | Count | Line |
|---|---|---|---:|---:|
| X01 | `### S6.3 PROOF OF THEOREM HE3.A` | `HE3_PROOF_2026-08-08.md` | 1 | 1092 |
| X02 | `> **DEFINITION HE6-1 (THE ENLARGED TEST FAMILY).**` | `HE6_PROOF_2026-08-08.md` | 1 | 201 |
| X03 | `### S6.2 PROOF OF THEOREM HE6.A` | HE6 | 1 | 639 |
| X04 | `> **LEMMA HE6-0″ [r1, R5/F6] (THE SLOT LEMMA at (T1)/(T2) POINTS` | HE6 | 1 | 215 |
| X05 | `> **LEMMA HE6-1L [r1; display corrected [r2], PE2 F-1]` | HE6 | 1 | 130 |
| X06 | `> **LEMMA HE6-0′ (the local forcing — HE3-0′ REPAIRED at ℓ ≥ 2).` | HE6 | 1 | 244 |
| X07 | `> **LEMMA HE6-1 (the ξ-side: EVERY root of a test polynomial carries the` | HE6 | 1 | 250 |
| X08 | `> **LEMMA HE6-2 (the ρ-side evaluation: the family SEPARATES labels).` | HE6 | 1 | 255 |
| X09 | `> **LEMMA HE6-2′ (the f-side evaluation at a test point).` | HE6 | 1 | 266 |
| X10 | `> **LEMMA HE6-3 (slope counts, (SEP)-FREE).` | HE6 | 1 | 271 |
| X11 | `> **LEMMA HE6-4 (NONEMPTINESS — the engine).` | HE6 | 1 | 280 |
| X12 | `> **LEMMA HE6-5 (Galois equivariance of labels — HE3's PE1 finding 6,` | HE6 | 1 | 284 |
| X13 | `**Corollary HE6-2″ (HE3-0(iii), re-proved in this convention).**` | HE6 | 1 | 517 |
| X14 | `## S5. LEMMA HE6-3 (slope counts, (SEP)-free) AND LEMMA HE6-4 (nonemptiness)` | HE6 | 1 | 525 |
| X15 | `**Correction 2 (PE4 F-2 — LEMMA HE6-2's β-definedness guard).**` | HE6 | 1 | 1036 |
| X16 | `> **LEMMA HE3-1L [r1, F1 — THE LETTER LIFT AT LEVEL 1, back-propagated` | HE3 | 1 | 498 |
| X17 | `**Dated correction (2026-08-12, post-acceptance challenge F-1 — normalized slot coefficients in LEMMA HE6-0″).** The displayed definition` | HE6 | 1 | 1061 |

All 17 designations returned exactly **1**. X01 supports the HE3 residue-level derivation; X02–X05 identify the already-pinned HE6 family/proof/seam/lift suppliers; X06–X12 are the seven statement units collected by `HE6-TEST-PACKAGE`; X13–X14 are the two further r10 spans; X15 is the later governing reading-directive pin; X16 is the independent normalized-coefficient supplier used by the terminal F-1 adjudication; X17 is the F-1 correction that makes that adjudication necessary.

**ARITHMETIC AUDIT:** 17 rows × fixed-string count 1 = **17 counted XREFs**. The 33 quote-evidence lines, 18 internal landmark searches, negative NON-IMPORT referents, 1 named-not-consumed precedent, and artifact checks below are distinct verification classes and do not enter that total.

### B. Source lines quoted in this range (exact full-line match, count 1, at the recorded line number)

| XREF | Use | File | Line no. | Line searched |
|---|---|---|---:|---|
| W01–W04 | `HE6-PROOF` closing transfer paragraph (the HE6 leg of the discharge, and S1.8C's) | HE6 | 663–666 | `**This is HE3's LEMMA HE3-4 in full generality**, so HE3's THEOREM HE3.A` · `assembly (§S6.3 there: base read + stage-α + stage-CS + embedded genres) may` · `be run with HE3-4 replaced by THEOREM HE6.A — with the one caveat of §S7 for` · `histories containing an ℓ ≥ 2 side with a repeated residual factor.` |
| W05 | coherent label read | HE3 | 1114 | `note's frame, where the label of ρ is (λ, r) with r ∈ K[Z] read via the` |
| W06 | \(\beta_\rho\) definition | HE3 | 1115 | `F_Q-embedding ι_ρ : K → k̄, η_θ ↦ η_ρ, and β_ρ := res(y_ρ/n(λ)(ρ)) at` |
| W07 | equivariance display (four leading spaces preserved) | HE3 | 1131 | `    β_{gρ} = res(Φ′(gρ)^{ℓ}/n(u)(gρ)) = ḡ(β_ρ)   (ℓ = 1: n(λ) read).` |
| W08 | label condition | HE3 | 1135 | `ι_{gρ}(r)(β_{gρ}) = ḡ(ι_ρ(r)(β_ρ)) = ḡ(0) = 0, and gρ carries the same` |
| W09 | `HE6-FAMILY` label quantifier | HE6 | 203 | `> irreducible with r(0) ≠ 0. Choose, by (LIFT), B_t ∈ O[x] with` |
| W10 | DEFINITION HE6-1's standing \(\kappa>D'h\) | HE6 | 202 | `> terms with κ > D′h, and let r = Z^{d} + Σ_{t<d}c_tZ^t ∈ K[Z] be monic` |
| W11 | `HE6-TEST-PACKAGE` first delimiter / clause 6 | HE6 | 244 | `> **LEMMA HE6-0′ (the local forcing — HE3-0′ REPAIRED at ℓ ≥ 2).** If a root` |
| W12 | clause 1 | HE6 | 250 | `> **LEMMA HE6-1 (the ξ-side: EVERY root of a test polynomial carries the` |
| W13 | clause 2 | HE6 | 255 | `> **LEMMA HE6-2 (the ρ-side evaluation: the family SEPARATES labels).** Let ξ` |
| W14 | clause 2 | HE6 | 266 | `> **LEMMA HE6-2′ (the f-side evaluation at a test point).** For any` |
| W15 | slope count | HE6 | 271 | `> **LEMMA HE6-3 (slope counts, (SEP)-FREE).** With` |
| W16 | clause 3 | HE6 | 280 | `> **LEMMA HE6-4 (NONEMPTINESS — the engine).** For every slope λ of P(F) and` |
| W17 | clause 5 | HE6 | 284 | `> **LEMMA HE6-5 (Galois equivariance of labels — HE3's PE1 finding 6,` |
| W18 | `HE6-TEST-PACKAGE` last delimiter | HE6 | 287 | `> Galois-stable, at every f₁ (no f₁ = 1 restriction).` |
| W19 | `HE6-ROOT-LABEL` first delimiter | HE6 | 517 | `**Corollary HE6-2″ (HE3-0(iii), re-proved in this convention).** Taking` |
| W20 | `HE6-ROOT-LABEL` last delimiter | HE6 | 521 | `of ρ; it is well defined because R_λ is a fixed element of K[Z].` |
| W21 | `HE6-ACCOUNT` first delimiter | HE6 | 525 | `## S5. LEMMA HE6-3 (slope counts, (SEP)-free) AND LEMMA HE6-4 (nonemptiness)` |
| W22 | resultant symmetry (RES) | HE6 | 529 | `    (RES)   Σ_{ρ : f(ρ)=0} dv(g(ρ)) = e₁·v(±Res(f,g)) = Σ_{ξ : g(ξ)=0} dv(f(ξ)),` |
| W23 | generic-letter construction | HE6 | 533 | `*Proof of LEMMA HE6-3.* Fix once an irreducible r₀ ∈ K[Z] of degree` |
| W24 | ξ-side evaluation | HE6 | 540 | `* ξ-side: by LEMMA HE6-1 every root ξ of Ψ_κ has dv(Φ′(ξ)) = κ and` |
| W25 | ρ-side evaluation | HE6 | 545 | `* ρ-side: by LEMMA HE6-2 (with r₀ never a label's factor)` |
| W26 | root-to-slope passage | HE6 | 546 | `  dv(Ψ_κ(ρ)) = ℓd₀·min(λ_ρ, κ) for every root ρ of f, so the ρ-side is` |
| W27 | root-to-slope passage | HE6 | 547 | `  ℓd₀·Σ_λ n_λ min(λ, κ).` |
| W28 | the accounting identity (†) | HE6 | 551 | `    (†)   Σ_λ n_λ·min(λ, κ) = D′·h_F(κ) = D′·Σ_λ L_λ·min(λ, κ),` |
| W29 | `HE6-ACCOUNT` last delimiter | HE6 | 604 | `what replaces HE3-BOX-6's base change.` |
| W30 | pin-note append (β-guard) | HE6 | 1036 | `**Correction 2 (PE4 F-2 — LEMMA HE6-2's β-definedness guard).** The` |
| W31 | pin-note append (HE6-3(a)) | HE6 | 1115 | `**Dated correction (2026-08-12, post-acceptance challenge F-2 — completion of LEMMA HE6-3(a)).** In the proof of LEMMA HE6-3(a), replace the inference` |
| W32–W33 | `HE3-A-PROOF` delimiters (re-verified) | HE3 | 1092, 1205 | `### S6.3 PROOF OF THEOREM HE3.A` · `leaf's history, so THEOREM HE3.A is proved. ∎` |

All 33 lines returned exactly **1**, each at the line number the note records.

**ARITHMETIC AUDIT:** W01–W33 represent 33 exact full lines; \(33\times1=33\) successful unique-line checks. Grouped rows W01–W04 and W32–W33 contribute four and two lines respectively and are not single checks.

### C. Master-internal landmarks (fixed-string, under the round's own bound)

| Landmark | Bound | Line | Count |
|---|---|---:|---:|
| `Repair 1 binds the height from the master's own` (r6 lead-in) | 3509 | 2922 | 1 |
| `The binding is derivable at the master's own generality from three data already displayed in S1` | 3509 | 2934 | 1 |
| ``derived from `(SIDE-COUNT)`, `(SLOT-V)` and `(LIFT)` rather than named`` | 3509 | 3287 | 1 |
| `binds a parameter that clause 3 and S1.8 already quantified over without naming a value` | 3509 | 3289 | 1 |
| `The recentering at this branch is a REFINE step, and this master stipulates of a refine step both` | 3509 | 3321 | 1 |
| ``The passage's configuration is a repeated \(K\)-rational residual root at an integer slope; by `` | 3509 | 3105 | 1 |
| `still governs reading directive 2, downstream of the match.` | 3509 | 3431 | 1 |
| `Twelve master-internal landmark strings, on eleven lines` | 3667 | 3665 | 1 |
| `Nine source lines` | 3667 | 3665 | 1 |
| `At \((\ell,d)=(1,1)\) and \(r=Z-s\) that reads` (r8 step sentence) | 3667 | 3537 | 1 |
| `At \((\ell,d)=(1,1)\) the member \(\Psi_{\lambda,Z-s}=\Phi'+B_0\) IS the recentered key` | 3667 | 3085 | 1 |
| `> \tag{IDX-TWIST}` (whole line) | 3805 | 3706 | 1 |
| `SECOND, the lift freedom is immaterial.` | 3805 | 3739 | 1 |
| `member for member` (census) | 3805 | 3677, 3709, 3755, 3797 | 4 |
| `at a full height \(\kappa\) is a member of the family` | 3843 | 3831 | 1 |
| `The 32 pins are the following existing-text pairs` | 3843 | 2900 | 1 |
| `Four consumptions have been cured this way.` | 3843 | 2791 | 1 |
| `so the count of cured consumptions reads two of five.` | 3843 | 2799 | 1 |

The rider's own “member for member” census — four occurrences at 3677, 3709, 3755 and 3797, two stating the covering direction and two the set identity — is reproduced exactly.

**ARITHMETIC AUDIT:** 18 landmark queries were run: 17 expected count 1 and one expected count 4, for \(17+4=21\) located occurrences. All 18 query results equal their recorded expectations.

### D. Negative foreign referents (verified, not counted as consuming XREFs)

`[TABLE — NON-IMPORT referent checks]`

| Referent searched in HE6 | Count | Line | Disposition |
|---|---:|---:|---|
| `> **LEMMA HE6-0 (the disk criterion).` | 1 | 240 | boundary/source-internal use only; T2 does not consume its disk criterion |
| `> **THEOREM HE6.A (HE3-BOX-6 CLOSED).` | 1 | 289 | T2 derives the corresponding conclusion itself |
| `### S3.3 LEMMA HE6-1 (the ξ-side)` | 1 | 426 | proof not consumed; statement display is inside `HE6-TEST-PACKAGE` |
| `## S4. LEMMA HE6-2 AND HE6-2′ (the two evaluation displays)` | 1 | 487 | evaluation proofs not consumed |
| `### S6.1 LEMMA HE6-5 (Galois equivariance — HE3's finding 6 closed)` | 1 | 610 | proof not consumed; statement display is inside `HE6-TEST-PACKAGE` |

**ARITHMETIC AUDIT:** 5 negative-referent rows × count 1 = 5 uniquely resolved NON-IMPORT referents; none enters the 17-XREF consuming/inbound total.

### E. Cross-note reference named but not consumed

| Reference | Check | Result |
|---|---|---|
| `T1_GAUGE_COCYCLE_2026-08-12.md` | file exists; fixed string `raw print/canonical equality is iff` | count 1; **not consumed**, per `.08` |

### F. Artifact and provenance checks

`[TABLE — compiler existence/type checks; not counted as XREFs]`

| Item | Check | Result |
|---|---|---|
| `runs/comp13/T2SOL_output.log` | path exists | present |
| `runs/comp13/T2SOL2_output.log` | path exists | present |
| `runs/comp13/T2P8_output.log` | path exists | present |
| source commit `66062771a0496cc1105b618fc98523a0437b79b0` | `git cat-file -t` | `commit` |
| source blob | `git hash-object` versus `git rev-parse HEAD:<path>` | both `2b74ad0f35f41e3109f1ba779eb8eb266a8ba0fc` |

**XREF result:** **17 fixed-string target designations verified**; 33 quote-evidence lines and 18 master-internal landmark checks separately confirmed; negative NON-IMPORT referents classified separately; 1 cross-note precedent classified as named-not-consumed; 3 artifacts and 1 commit checked. **0 unverifiable designations; 0 compiler `OPEN-CALL`s.**

---

## 4. Local effective supply/consumption ledger

`[TABLE — compiler ledger, not source wording]`

| Item homed here | Effective status | Surviving condition/fence | Consumption-protocol status |
|---|---|---|---|
| r8 residual corrections (.01, .02) | Effective; four r6 sentences booked | No `(REF-HT)` clause is a premise in S1.8C | n/a |
| HE6 leg of the level-one discharge (.03) | Effective | §S7 caveat routed to the product-\(\ge2\) branch | Inside `HE6-PROOF`; no new pin |
| `(IDX-TWIST)` derivation (.04) | Effective | Exact at the compatible lift; covering direction at \(\kappa>D'h\) | No new pin; `(HE6-SEAM)`'s supplier already pinned |
| Height+digit derivation (.05) | Effective; supersedes the r8 ground | Premise remains stipulated (RISE) | Inside `HE3-A-PROOF` |
| The two qualifiers (.06) | Effective | Choice-independent content is the height–digit pair | n/a |
| r9/r10 round records (.07, .10) | Historical as to counter | 32-pin gate result superseded by the 37-pin sweep | Source-side gate only |
| Frame-authority fence (.08) | Effective | T1 named, never consumed | Sweep must keep it classified |
| Rider block 1 (.09) | Historical as to counter (1 of 2) | Fold rule effective | n/a |
| Three new HE6 pins (.11) | Effective; later inventory is 37 | Beta guard is consumed and separately pinned; HE6-3(a) correction remains a declination | Pin sweep run whole at acceptance |
| Level-one consumption map (.12) | Effective with r11 overlay | Direct beta-guard sites plus transitive `HE6-ACCOUNT` route; reaches \(\Phi''\) only via S1.8C (vi)/(vii) | Source-pin lines re-set at r11/r12 |
| Declinations (.13) | Effective | Scoped per consumption | Revisit if a later round reads from a declined unit |
| Bookkeeping (.14) | Counts superseded; rules effective | List governs total | Same |
| Sweep record (.15) | Effective protocol record, terminally adjudicated | F-1 touches a consumed clause already carried in corrected form; no new pin | r12 completes other sources; gate re-runs whole |

**Reverse-XREF audit.** No unit homed here names a downstream consumer note/designation pair. The one cross-note name (T1) is a precedent citation, not a consumer edge, and is recorded as such rather than promoted to an XREF row.

---

## 5. NON-IMPORTS

`[TABLE — negative perimeter declared by material homed in this range]`

| NON-IMPORT | Effective prohibition |
|---|---|
| Print label as digit | Do not read DEFINITION HE6-1's family index as this master's coherent digit; they differ by the fixed unit \(\eta^{q(\kappa)}\), and equality holds only if \(\eta^{q(\kappa)}=1\), which nothing supplies. |
| Index-name matching | Do not match family members to recenterings at equal index names; match them through `(IDX-TWIST)`. |
| `(IDX-TWIST)` as a theorem clause | Do not treat `(IDX-TWIST)` as a hypothesis or conclusion of HE7.A; it is a derived identity of the level-one instance. |
| Arbitrary-lift equality | Do not read `(IDX-TWIST)` as a polynomial identity at an arbitrary permitted lift; at an arbitrary choice it holds modulo increments of height \(>\kappa\). |
| Fullness for covering | Do not state the covering direction at “a full height”; DEFINITION HE6-1 requires \(\kappa>D'h\), and a full \(\kappa\) in \([(D'-1)h,\,D'h]\) indexes no member. |
| Three-data binding | Do not say the height is bound by `(SIDE-COUNT)`, `(SLOT-V)` and `(LIFT)`: those three derive the implication only; the binding needs (RISE). |
| `(REF-HT)` for branch selection | Do not cite either clause of `(REF-HT)` for which branch a state takes; that is S1.8's case split plus the lowest-terms convention. |
| HE6 §S7 caveat | Do not consume anything behind the \(\ell\ge2\)-with-repeated-residual caveat; that configuration is the product-\(\ge2\) branch. |
| T1 as a source | Do not consume `T1_GAUGE_COCYCLE_2026-08-12.md`: it is outside the six source files, covered by no pin, and cited as methodological precedent only. |
| HE6-0 as disk criterion | Do not credit LEMMA HE6-0 with S1.7 clause 1; the disk membership read is LEMMA HE6-1's own conclusion. |
| HE6.A statement as supplier | Do not read S3.2's conclusion display from THEOREM HE6.A; it is this master's own clause-2 output at the instance. |
| Lemma proofs at level one | Do not import HE6 §S3.3, §S4's evaluation proofs or §S6.1; only the statement displays are consumed. |
| Reading-directive form as a disposition | Do not infer that a dated correction is unconsumed merely because it is recorded as a reading directive: `HE6-BETA-GUARD` is consumed and pinned, while HE6's 2026-08-12 F-2 is proof-internal and unpinned. Apply the consumption test. |
| r11's loose F-1 phrase | Do not repeat “F-1 touches nothing this master consumes” as exact. F-1 lands on the consumed seam conversion, but T2 already carries the corrected normalized form and no conclusion or pin changes. |
| Superseded counts | Do not cite 32 or 35 pins, 15/18-pair taxonomies, or the “eight cured consumptions” census as effective; the accepted figures are 37, \(20+4+13\), and ten. |
| Rider counter states | Do not cite “STANDS AT 1 OF 2” as status; the accepted counter is 2/2. |

---

## 6. TEETH inverse table

`[TABLE — compiler inverse]`

| Tooth / hostile demand | Disposition in effective text | Local units carrying the response |
|---|---|---|
| Print/coherent frame mutant (the r8-pass GAP) | Seam displayed; family reindexed; discharge rebuilt so the label is never read | .04, .05 |
| HE6 seam and sign gate | `(HE6-SEAM)` used as the conversion, its supplier already pinned | .04, .08 |
| Lift-choice mutant | Compatible choice checked permitted; arbitrary choice bounded above \(\kappa\) | .06 |
| Covering-height mutant | Covering stated at \(\kappa>D'h\), not at fullness | .06 |
| Level-one `(RISE)` discharge tooth | Both halves of the (S3.1/S3.2) scope cited; caveat routed | .03, .05 |
| Non-circularity audit | Assembly order preserved through the rebuild; no `(REF-HT)` clause premised in S1.8C | .02, .05 |
| S7 pin gate, conditions 1–4 | Three new pins with adjudicated conditions; first end-to-end gate run recorded | .10, .11 |
| `(ACCOUNT)` supplier gate | The only side-accounting identity now has a pinned supplier | .12 |
| HE6 test-package gate | Each of the seven S1.7 clauses mapped to a pinned display | .12 |
| Standing S7 consumption sweep | Re-run with a new dated-append leg; declinations signed | .13, .15 |
| Pin-note directive coherence | Two new reading directives at the new pins | .11 |
| Inbound-correction consumption test | Beta guard pinned; HE6-3(a) declined; F-1 precision corrected without a new pin | .11, .12, .15 |
| S7 acceptance gate | Two resets and one non-reset recorded; superseded to 2/2 | .01, .07, .09, .10 |

**Forward check:** all 15 statement units have a nonempty `TEETH` field and appear in at least one inverse row.

**Inverse check:** every inverse row names at least one local unit.

**Zero-orphan result:** 0 statement teeth orphaned; 0 inverse teeth orphaned; 0 of 0 numbered battery rows (T2 owes no battery).

---

## 7. HOME routing audit

`[TABLE — compiler routing audit; every block of the charged physical range accounted for]`

| Physical span | Effective operation | Destination HOME / disposition |
|---|---|---|
| 3569–3595, r8 repair 2 | Four r6 “three S1 data” sentences booked, with the correction stated once | **Local: .02** |
| 3599–3615, r8 repair 3 | HE6 leg inserted into the level-one discharge, caveat routed | **Local: .03**; destination base S1.8 → shard 1 `.19` |
| 3619–3651, r8 repair 4 | (a) lead-in corrected; (b) reading directive 2 re-cited; consequential booking of r7 repair 4's closing clause | Base S1.8 / S1.8C → shard 1 (.19, .25); **local: .02** |
| 3655–3667, r8 counter | No-statement-change booking; obligations; census | **Local: .01** |
| 3671–3677, r9 header | Arm, first model-diverse pass, protocol, certification list, GAP framing | **Local: .07** |
| 3681–3763, r9 repair 1 | (a) seam and `(IDX-TWIST)`; (b) height+digit derivation; (c) corroboration reframed; (d) S1.8C family-member sentence; (e) frame authority; non-circularity restated | Base S1.8 / S1.8C → shard 1 (.19, .20, .25); **local: .04, .05, .08** |
| 3767–3789, r9 repair 2 | r8's two census counts corrected | **Local: .01** (conditionality) |
| 3793–3805, r9 counter | Credit to the model-diverse pass; no statement change; two r8 sentences left standing | **Local: .07** |
| 3809–3819, rider header | Fold rule, scope of the rider, landmark convention | **Local: .09** |
| 3821–3839, Rider 1.1 | Lift-choice qualifier inserted after the `(IDX-TWIST)` display; four “member for member” sites classified; S1.8C confirmed exact | Base S1.8 → shard 1 `.20`; **local: .06** |
| 3841–3843, rider counter state | Counter NOT reset; stands at 1 of 2 | **Local: .09** |
| 3847–3853, r10 header | Arm, pass of record, protocol, first end-to-end pin-gate run | **Local: .10** |
| 3857–3877, r10 repair 1(a) | Itemised level-one HE6 consumption map | **Local: .12** |
| 3879–3914, r10 repair 1(b) | Three new pins, their S0.2 adjudications and two reading directives | **Local: .11**; `(PINS)` rows → shard 1 `.03` |
| 3916–3920, r10 repair 1(c) | Three declinations with grounds | **Local: .13** |
| 3922–3927, r10 repair 1(d) | The finding's four grounds answered | **Local: .12** (derivation) |
| 3929–3965, r10 repair 1(e) | Pin count 32→35; taxonomy; cured-consumption census | **Local: .14**; counts → shard 1 `.03`, `.42` |
| 3967–3989, r10 repair 1(f) | Five Source-pin lines re-set | Base S3.1–S3.5 → shard 1 (.33–.37); superseded at r11/r12; ground recorded in .12 |
| 3991–3995, r10 repair 1(g) | Consumption sweep re-run, with the dated-append leg | **Local: .15** |
| 3999–4015, r10 repair 2 | Rider's covering direction qualified to \(\kappa>D'h\) | **Local: .06**; destination base S1.8 → shard 1 `.20` |

---

## 8. BOUNDARY SELF-AUDIT

| Unit group | Boundary reason |
|---|---|
| .01, .02 | The round record is separated from its substantive correction because the correction (an implication is not a binding) outlives the round's bookkeeping and is cited later; the counter is superseded and the correction is not. |
| .03 | The HE6 leg is a citation-completeness unit attaching to a discharge homed in shard 3; keeping it separate makes the cross-shard attachment explicit instead of silently rewriting shard 3. |
| .04, .05 | Split at the seam/derivation boundary: `.04` establishes what the family's index means, `.05` derives the step's height and digit without using it. That independence is the whole point of the r9 rebuild, and merging them would hide it. |
| .06 | Both qualifiers attach to the same display and were installed by two different pass types (a rider and a repair); they are one unit because a consumer must read them together. |
| .07, .10 | Each is a pass/round record rather than the mathematical cure it triggers. They remain separate so provenance, protocol, and historical counter state are not mistaken for theorem content or pin-table rows. |
| .08 | The frame-authority fence is separated because it is a NON-IMPORT with a named external note — the only cross-note citation in the whole shard. |
| .09 | A rider block sets counter state and folds a pass; it is a status record, not a repair, and must not be merged with either neighbouring round. |
| .11, .12, .13 | Three faces of one repair, kept apart because they answer different gate questions: what is pinned, what each pin supplies, and what is declined. |
| .14, .15 | Inventory arithmetic (superseded) is kept apart from the protocol sweep (effective and later completed/adjudicated). The former answers “how many”; the latter answers “what was actually consumed”. |

**Leading edge.** Line 3568 is blank; line 3569 opens `## r8 repair 2`. The r8 round is split across the edge by design: its header and repair 1 are compiled in shard 3 `.08`/`.03`, its repairs 2–4 and counter here. Both halves name each other.

**Trailing edge.** Line 4017 is `---` and 4018 is blank; line 4019 opens `## r10 counter`. The r10 round is likewise split: header and repairs 1–2 here, counter in shard 5. No statement unit is physically truncated.

---

## 9. SHARD NOTES

- **Cross-shard supersessions applied (downward):** the acceptance record's counter (2/2), pin inventory (37), taxonomy (\(20+4+13\)) and cured-consumption census (ten); r11 repair 1(e) and r12 repair 1(f)'s re-settings of the five Source-pin lines that r10 repair 1(f) sets here; r11's `HE6-BETA-GUARD`, which pins the same PE4 Correction 2 that `.11` records as a reading directive at `HE6-TEST-PACKAGE`; r12's five-source dated-append sweep, which extends `.15`'s HE6-only leg.
- **Cross-shard supersession applied (upward, into shard 3):** r9 repair 1(a)–(b) supersedes two passages of the r8 `(RISE)` discharge quoted at shard 3 `.03`. This shard flags that merge-time replacement and supplies the terminal derivations in `.04`/`.05`; it does not silently rewrite the neighboring shard. The level-two bullet, assembly order, and non-circularity statement remain effective there.
- **Intra-shard supersessions applied:** r9 repair 1 over r8 repair 1 (three sentences) and over S1.8C's family-member sentence; r9 repair 2 over the r8 census; Rider 1 over the unqualified `(IDX-TWIST)`; r10 repair 2 over Rider 1's covering phrase. Only replacement text is stated as effective.
- **Exception (ii) accounting.** Four units (`.03`, `.04`, `.05`, `.06`) carry derivations whose destination is a base section compiled in shard 1 (`.19`, `.20`, `.25`). Shard 1 carries the statements and compresses these derivations to single clauses; the merge should attach them as `DERIVATION` content, not as parallel statements. No shard-1 statement, display, clause or instance box is re-emitted here.
- **Merge flags:** (i) shard 1 `.20` states `(IDX-TWIST)` and both qualifiers in compressed form but carries neither the \(\iota_\theta=\mathrm{id}\) argument nor the bijection/covering argument — supplied here as `.04`/`.06`. (ii) Shard 1 `.19` does not carry the digit-forcing derivation — supplied here as `.05`. (iii) The level-one HE6 consumption map (`.12`) has no shard-1 counterpart at all; shard 1 `.34` names the pins but not the clause-by-clause supplier assignment.
- **A recorded self-correction of the note, carried forward:** r10's census sentence notes that the superseded S0.2 sentence “miscounted its own list, reading ‘Four’ against the five cures it then names”. The compiled census in `.14` uses the list, not the stale total, and the same discipline (list governs total) is what r9 repair 2 applies to the r8 landmark census.
- **Verification independence:** all 17 foreign target designations returned fixed-string count 1; all 33 quote-evidence lines retained their recorded line numbers; the T1 precedent quote returned 1; the three named pass artifacts exist; and the source blob and commit pins resolve.
- **Terminal inbound-correction overlay:** r11's `HE6-BETA-GUARD`, Rider 2's transitive `HE6-ACCOUNT` route, r12's five-source sweep, and the 2026-08-14 F1–F4 record govern `.11`, `.12`, and `.15`. The last is non-superseding but corrects the precision of r11's F-1 description.
- **Protocol versus absorption:** the locally homed gate event is source-side (the 32-pin gate run end to end), not a downstream T2-consumer check. Acceptance later re-runs the source gate at 37 and queues downstream checks; no such downstream check is claimed here.

---

## 10. SELF-AUDIT

| Check | Result |
|---|---|
| Statement inventory | 15 temporary-ID units, all with HOME in source lines 3569–4018 |
| Required fields | 15/15 have `CANONICAL STATEMENT`, `FORM`, `DERIVATION`, `CONDITIONALITY`, `RESOLUTION TRACE`, and `TEETH`; every superseding unit also has `SUPERSESSION KIND`, `TARGETS`, and a terminal `CHAIN` |
| XREF verification | 17 distinct fixed-string target designations at count 1; 33 quote-evidence lines and 18 internal landmark checks separately confirmed; T1 precedent classified named-not-consumed |
| Compiler open calls | 0 |
| Source open obligations | `(LB1)` and `(MP1)` only, unchanged by every round in this range; `(RISE)` remains a stipulated carrier hypothesis; no obligation is opened or closed here |
| Supersession | Intra-shard (r9 over r8; r10 over the rider) and cross-shard (both directions) applied; superseded counts and passages recorded as historical only |
| Protocol/supply split | Source-side pin gate separated from queued downstream consumption checks |
| Tables | Source enumerations as `[TABLE]`; compiler ledgers marked; no source table prosified |
| Fidelity | Quotations verbatim or explicitly `[ASSEMBLED]`, abridgements marked with ellipses; source lines byte-exact including leading `> ` and leading spaces |
| NON-IMPORTS | Present, 16 rows, including the beta-guard/F-1 correction perimeter imposed by later governing sections |
| Teeth inverse | Both directions checked; zero orphans |
| HOME routing | All 20 physical blocks accounted for; 15 homed locally, the rest routed (one upward into shard 3) |
| Shard edges | Both edges split a round (r8 with shard 3, r10 with shard 5); each half names the other; no unit truncated |
| Padding discipline | Exception (ii) used four times with destinations named; no shard-1 statement re-emitted |

EFF-T2-s4of6 COMPILED: 15 statements / 17 xrefs verified / 0 open calls
