# EFF-GENTOW6 — CANONICAL EFFECTIVE SPECIFICATION v2 (full compilation)

**COMPILED-BY: opus (2026-08-14, Asvin-authorized substitution, PROJECT_STATE #51). CODEX CROSS-READ OWED (post-reset).**

## 1. Header

**Source note:** `lean/notes/openmath/GENTOW6_PROOF_2026-08-09.md` (1,265 lines / 71,465 bytes).

**HEAD blob hash:** `5355834f3cd44a1b39245df467bc36c671608cea` (fresh `git hash-object`; last touched by commit `3d7fc37b`).

**Compilation context:** Stage 0a; template `spec/EFF_TEMPLATE_v2.md` AT HEAD (rules 1–28). IDs `EFF.GENTOW6.<nn>`, contiguous, in effective-text order.

**GENRE: TAG-SPARSE ASCII PROSE PROOF + SEALED BATTERY + CITE-BASED PROOF LEG + r1 ROUND + TWO FOLDS + ONE SUB-BOX APPEND.** `grep -c '\tag{'` = **0**. Unit populations (v3 rule 19): **preregistrations** (the sealed battery's wants + 4 teeth; the r1 cap-lemma's P-A…P-E + T-TRI/T-CAP; the box-4 row's wants + 3 teeth), **run-records** (five instruments), **proof units** (S1–S3, S6.1–S6.2).

**This note is the corpus's BOX RESIDUE UNIT**: it exists to close three other notes' boxes. Its title says so, and the queue charge for this entry is its own four boxes — **GENTOW6-BOX-1…BOX-4 are ledger MATH items `HYP.67`–`HYP.70`, and their exact open wording is compiled verbatim at `.30`–`.33` and again, as amended, at `.44`–`.45`.**

### FREEZE PREDICATES (v3 rule 21 — four text pins; **three reproduce, one reconciles**)

`[TABLE — compiler ledger]`

| # | Predicate | Stated | Verification |
|---:|---|---|---|
| F-1 | post-PE3: “pre-fold body md5 11b7b6b67f91aaf8bec1481a56f1a54a (1,088 lines) = the rider state @ 1194b1a4” | md5 + lines | **NOT a HEAD prefix — correctly so** (the four fold/upgrade brackets inserted `+58/−0` at L314). **`1,088 + 58 = 1,146` reconciles exactly** ✓ |
| F-2 | same: “prefix through the first insertion, L314, byte-identical, md5 8e30f26a” | md5 + line | **lines 1–314 hash to `8e30f26a…`** ✓ |
| F-3 | same: “post-fold body md5 6e0489fbcaff25bab8042bc8ec749bbd (1,146 lines)” | md5 + lines | **lines 1–1146 → `6e0489fb…`** ✓ |
| F-4 | SUB-BOX append: “pure append on the frozen body, pre-append md5 157b16adc82f6de4c6a0f65f5f8ffe85, 1,197 lines” | md5 + lines | **lines 1–1197 → `157b16ad…`** ✓ |

**Instrument freeze (S6.4 + riders): ELEVEN artifact md5s stated across the note; all eleven match** (§3.2).

### Effective-text rule (DERIVED)

**(i) r1 was applied as bracketed riders with a ten-site manifest** (S6.4), whose closing sentence fixes the reading rule exactly: “All sealed statement displays byte-untouched; **riders GOVERN their quoted displays**.” **(ii) The post-PE2 fold is minors-only** — “a rider is not a pass” — and re-scopes two stale summary sites plus one lemma display. **(iii) After PE3 the note is frozen**: “**THE NOTE IS FROZEN AT THIS RECORD: dated appends and dated brackets only …**”. **(iv) The SUB-BOX append is a pure append that CHANGES TWO BOX STATES** (BOX-1 → CLOSED AT f1 = 1; BOX-4 → DISCHARGED).

**EFFECTIVE TEXT = the note as written (r1 riders landed and governing), READ AS RE-SCOPED by the post-PE2 fold and the post-PE3 acceptance fold, and AS AMENDED at two box states by the SUB-BOX append.**

### Supersession CHAINS (v3 rule 25 — TERMINALs marked)

`[TABLE]`

| Chain | Layers | TERMINAL |
|---|---|---|
| **CHAIN-SCOPE (THEOREM 6.3's genre quantifier)** | composed: “every genre” → **[GT6-r1] F-1** (a CRITICAL): the theorem-head SCOPE PIN to **f1 = 1** at the corrected `chat_t`; the f1-general form becomes **ANNEX-THEOREM 6.3′** → **PE2-F-1 rider**: two stale summary sites (STATUS TABLE row (3); the S3 section header) re-scoped to the pin | **PE2-F-1** (`.09`, `.19`) |
| **CHAIN-CAP (GENTOW-5(a) off its 𝒯 scope)** | composed: COR 6.2C(d) consumes GENTOW-5(a) at `f` off its proved 𝒯 scope → **[GT6-r1] F-2, prove-first**: the **𝒯-FREE CAP LEMMA GENTOW-6.4** is proved and machine-verified at three frames | **LEMMA 6.4** (`.36`) |
| **CHAIN-CODOMAIN** | 6.4(a)'s per-window bijection codomain as displayed → **PE2-F-2**: CORRECTED by a dated bracket — above-top slots are FORCED to 0; exact condition `n mod D2 != D2 − 1`, **sharper than the report's `D2 ∤ n`** (FRAME-F, a `D2 | n` member, carries 3 such slots) | **PE2-F-2** (`.42`) |
| **CHAIN-BOX1** | BOX-1 open (μ₂ ≥ 3 attainment) → **SUB-BOX append**: **CLOSED AT f1 = 1** by THEOREM GENTOW-6.5, *at that note's 0/2* → within it, **[r1 GENTOW6-BOXES]** withdraws the “iff” (the converse is **FALSE**, with a witness) and corrects the uniqueness census | **the SUB-BOX append as r1-bracketed** (`.44`) |
| **CHAIN-BOX4** | BOX-4 open (no f1 ≥ 2 / η ≠ 1 machine contact) → **acceptance-fold F-1**: the “NO machine contact” sentence is DATED and stale-conservative; **BOX-4 STAYS OPEN**, ask unchanged → **SUB-BOX append**: **DISCHARGED** by a sealed battery-grade row, GREEN 265/0 at four x-ful rows | **the SUB-BOX append** (`.45`) |
| **CHAIN-GRADE** | 0/2 → PE1 NOT CLEAN (1C+1G+4m) → r1 → **PE2 CLEAN, 0/2 → 1/2** → rider → **PE3 CLEAN, 1/2 → 2/2 ACCEPTED** | the acceptance record (`.43`) |

### SERIES declared (v3 rule 28)

**SERIES ARC** — `.02` (r1 ARC record) → `.03` (post-PE2 ARC record) → `.34`–`.39` (S6, the r1 round) → `.40`–`.42` (S6.5, the post-PE2 fold) → `.43` (acceptance 2/2, with the post-PE3 fold) → `.44`–`.45` (the SUB-BOX append).

**SERIES BOXES** — the four residue boxes, emitted per box in source order (`.30` BOX-1, `.31` BOX-2, `.32` BOX-3, `.33` BOX-4) and **re-emitted at their amended states** (`.44` BOX-1 CLOSED AT f1 = 1, `.45` BOX-4 DISCHARGED). **This double emission is deliberate and is the entry's central compilation act**: the ledger rows `HYP.67`–`HYP.70` quote the boxes at *different* states, and a consumer must be able to see both.

### Scope split — THREE predicates

**A. Mathematical absorption / supply SET.** The charge, verbatim:

> “Charge (three targets, priority order): (1) GENTOW4-BOX-1 — the
> mixed-side tie mu2*(r) = m_r, literature-first (FGMN residual
> multiplicativity; GENTOW2's faithful-cite pattern) or via HE7's
> frozen bouquet machinery; (2) GENTOW4-BOX-2 — the partial-side
> entry-budget layer (GENTOW-1's budget lemma at the block factor);
> (3) GENTOW3-BOX-1 — the self-shadow attainment certificate made
> genre-general or honestly scoped.”

Delivered per the STATUS TABLE: **LEMMA GENTOW-6.1 (PROVED-VIA-CITE + corpus leg; GENTOW4-BOX-1 closed), LEMMA GENTOW-6.2 + COR GENTOW-6.2C (budget half proved), THEOREM GENTOW-6.3 (at μ₂ = 2, f1 = 1)** — plus, at r1, **LEMMA GENTOW-6.4** (the 𝒯-free cap lemma) and **ANNEX-THEOREM GENTOW-6.3′**.

**B. Append / consumption PROTOCOL scope.** The note **closes another note's box** (GENTOW4-BOX-1) but does not edit that note: “Sources consumed (never edited)”. Verified: `grep -cF 'GENTOW-6' GENTOW4_PROOF_2026-08-09.md` = **0** (N1) — **GENTOW4's BOX-1 text is unamended**, so the closure lives only here. Ledger row `HYP.144` still states GENTOW4-BOX-1 as open “UNPROVED here … a faithful-cite unit in GENTOW2's pattern would retire it” — **and this note IS that faithful-cite unit**, unlanded. OPEN-CALL 1.

**C. Inbound corrections RECEIVED:** this note's PE1 (1 CRITICAL + 1 GAP + 4 MINOR), PE2 (2 MINOR), PE3 (1 MINOR), plus the **GENTOW6-BOXES sibling note's own r1** (whose passPE1 F-1/F-2 corrections are transcribed into the SUB-BOX append's brackets, `.44`), plus GENTOW4's acceptance (the “4.C ACCEPTANCE UPGRADE lines”).

### Resolution rules applied

1. r1 riders are landed and **GOVERN their quoted displays** (the note's own rule).
2. **Box wording is reproduced EXACTLY as left at BOTH states** (queue charge), each with its `HYP.67`–`HYP.70` cross-reference quoted.
3. Where a box state changed after the ledger row was written, both are shown and the divergence is recorded as an OPEN-CALL, never silently reconciled.
4. Numbers recomputed from committed artifacts; pins verified individually.
5. The cite leg (S1 Step II) is compiled as a `cite-conditional` supply with its conditionality quoted.

### Quotation discipline

Verbatim or `[ASSEMBLED]`; `…` marks truncation. ASCII preserved. The STATUS TABLE is `[TABLE]`-flagged.

---

## 2. Statement inventory

47 units, contiguous, in effective-text order. FORM on every unit.

### EFF.GENTOW6.01  [convention]

**CANONICAL STATEMENT:**

> “# GENTOW-6: THE BOX RESIDUE UNIT — the mixed-side multiplicity tie
> # (GENTOW4-BOX-1), the partial-side block budget layer
> # (GENTOW4-BOX-2), and the attainment certificate (GENTOW3-BOX-1),
> # 2026-08-09”

with the status line: “**STATUS: COMPLETE (sealed battery GREEN 401/0, 4/4 teeth). Unit: GENTOW-6, BOX-CLOSURE campaign residue wave. Grade 0/2 (attempt; hostile arc owed).**”

**FORM:** four-line title + bold status.

**DERIVATION:** `[RECORD]`. **The title names three OTHER notes' boxes as this note's targets** — the unit is defined by what it closes.

**CONDITIONALITY:** grade → **ACCEPTED 2/2** (`.43`).

**RESOLUTION TRACE:** lines 1–8; correction 1148–1156.

**TEETH:** the battery figure re-read: `=== VERDICT: GREEN -- 401 checks, 0 violations (0.4s) ===` ✓.

---

### EFF.GENTOW6.02  [repair-round record]  *(SERIES ARC, link 1)*

**CANONICAL STATEMENT:**

> “**[GT6-r1] ARC: passPE1 (GENTOW6_passPE1_report.md @ cd74ef6f,
> 2026-08-10) = NOT CLEAN — 1 CRITICAL (F-1, gauge/f1 scope at
> THEOREM 6.3's genre quantifier) + 1 GAP (F-2, GENTOW-5(a) consumed
> at f off its proved 𝒯 scope) + 4 MINOR. This round is the r1
> repair: the dated gauge annex + f1 = 1 scope pin + the f1-general
> re-derivation ANNEX-THEOREM 6.3′ (S6.1); the 𝒯-free cap LEMMA
> GENTOW-6.4, machine-verified at three frames incl. two
> partial-side members (S6.2); minors F-3/F-5/F-6 + the F-4 closure
> record (S6.3). Sealed machine artifacts BYTE-FROZEN (md5s
> re-verified, S6.4). Grade stays 0/2.**”

**FORM:** bold dated ARC record.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** **this is the only CRITICAL in the four GENTOW entries of this queue**, and its subject is a quantifier: “every genre” in a theorem head. The GAP is a scope violation (a lemma consumed off its proved locus) answered **prove-first** with a new lemma.

**RESOLUTION TRACE:** lines 10–20.

**TEETH:** `git cat-file -t cd74ef6f` = commit ✓.

---

### EFF.GENTOW6.03  [acceptance-box]  *(SERIES ARC, link 2)*

**CANONICAL STATEMENT:** the post-PE2 ARC record, `[ASSEMBLED]` from lines 22–43. Its two folded minors, verbatim:

> “PE2-F-1 the two stale summary sites
> (STATUS TABLE row (3); S3 section header) re-scoped to the
> r1 pin's f1 = 1 wording, mirroring the theorem-head scope pin
> verified at source; PE2-F-2 LEMMA GENTOW-6.4(a)'s per-window
> bijection codomain CORRECTED by a dated bracket at the display —
> re-derived from (a)'s own triangular formula: above-top slots
> (slot degree > n) are FORCED to 0, the codomain is the tuples
> supported on slot degrees <= n with top pinned 1; exact condition
> for above-top slots = n mod D2 != D2 - 1 (sharper than the
> report's D2 ∤ n framing — FRAME-F, a D2 | n member, carries 3
> such slots); machine leg gentow6_rider_codomain.py GREEN 24/0
> with tooth T-COD x3 …”

**FORM:** bold dated ARC record.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** **PE2-F-2's correction is SHARPER than the finding that prompted it** — the report framed the condition as `D2 ∤ n`; the note derives `n mod D2 != D2 − 1` and exhibits a `D2 | n` counter-member (FRAME-F) carrying three above-top slots. That is a repair that improves on its own charge.

**RESOLUTION TRACE:** lines 22–43; the corrected display at `.42`.

**TEETH:** `git cat-file -t 161e8ce1` = commit ✓; `gentow6_rider_codomain.py` exists ✓.

---

### EFF.GENTOW6.04  [fence]

**CANONICAL STATEMENT:** the charge (quoted in §1 A) and the sources, verbatim in brief:

> “Sources consumed (never edited): GENTOW4_PROOF …, GENTOW3_PROOF …, GENTOW1_PROOF …, GENTOW2_PROOF (the cite pattern + the tower<->MacLane dictionary + shear) …, HE7_PROOF …, HE6_PROOF …, HE6R1_RECON …,
> the FGMN local print
> (docs/references/fgmn_residual_ideals_1305.0775v3_layout.txt,
> numbering verified at GENTOW2's ledger); machine:
> gentow4_checks.py + gentow3_checks.py (imported additively).”

**FORM:** charge + import inventory.

**DERIVATION:** `[RECORD]`. **The FGMN local print is an EXTERNAL LITERATURE import** — unique in this queue — and its numbering is verified at another note's ledger.

**CONDITIONALITY:** the `[GT6-r1, PE1 F-5(i)]` rider adds two unitemized GENTOW1 imports (COROLLARY GENTOW-1.1 and LEMMA GENTOW-2). SUPERSESSION KIND: **inventory completion**.

**RESOLUTION TRACE:** lines 45–62.

**TEETH:** the imported machine legs `gentow4_checks.py` (md5 `98cdd4a4…`) and `gentow3_checks.py` (md5 `7cf3a7b8…`) are **the same artifacts this queue verified for GENTOW4 and GENTOW3** ✓ — a three-way artifact identity.

---

### EFF.GENTOW6.05  [table]  `[TABLE]`

**CANONICAL STATEMENT:** the STATUS TABLE, source lines 66–72, transcribed with the PE2-F-1 rider in row (3):

| target | statement | status |
|---|---|---|
| (1) mixed-side tie | LEMMA GENTOW-6.1 (S1) | PROVED-VIA-CITE (FGMN Thm 2.8 + Cor 4.7(3) + Cor 4.4(4)) + corpus leg; GENTOW4-BOX-1 closed |
| (2) block budgets | LEMMA GENTOW-6.2 + COR GENTOW-6.2C (S2) | PROVED (budget half; ledger half = GENTOW6-BOX-2) |
| (3) attainment | THEOREM GENTOW-6.3 (S3) | PROVED at mu2 = 2, f1 = 1 [rider (post-PE2, 2026-08-10; PE2-F-1): the theorem-head GT6-r1 scope pin governs this row — "every genre" reads AT f1 = 1, at the corrected chat_t; CERT-TOP unconditional at f1 = 1 genres with 2i_{t*} >= D' (vacuous at f1 >= 2); f1 >= 2 = ANNEX-6.3' (S6.1), machine-unexercised = GENTOW6-BOX-4] (regimes 3/4 displayed; mu2 >= 3 = GENTOW6-BOX-1) |
| battery | gentow6_checks.py (S4) | GREEN 401/0, 4/4 teeth, PARI-adjudicated |

**FORM:** table.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** **row (3) carries the PE2-F-1 rider IN the table cell** — one of the two “stale summary sites” re-scoped. SUPERSESSION KIND: **scope-pin**. Row (1)'s “GENTOW4-BOX-1 closed” is the claim `HYP.144` does not yet reflect (OPEN-CALL 1).

**RESOLUTION TRACE:** lines 64–72.

**TEETH:** the battery row re-verified (`.01`).

---

### EFF.GENTOW6.06  [convention]

**CANONICAL STATEMENT:** the S1 setting, verbatim (abridged):

> “**Setting (GENTOW-4 S0, restated for one pass).** Tower entry,
> partial inner side: … fix a side of slope kappa = u/l (lowest terms, l = the
> repo's e2 when the side is the inner one) with residual
>
>     R_kappa = c · Prod_r r^{m_r},   c in K^x,  r monic irreducible
>     over K = F_{Q^{f1}}, coherent (S4) normalization.
>
> For each r, C_{kappa,r} is the label class, f_{kappa,r} its block
> factor (GENTOW-4.A(i)), and … mu2*(r) :=
> Sum_{lambda2 > T2} L_{lambda2}(P2(f)) is the composed length read
> through LEMMA HE6R1-2 at the class's datum (GENTOW-4.A(ii)) …
> GENTOW-4.2 proved mu2*(r) = m_r at
> PURE-POWER sides … and boxed the mixed case
> as GENTOW4-BOX-1. The box closes here:”

**FORM:** bold-headed setting with a display.

**DERIVATION:** `[IMPORTED]` from GENTOW-4 S0.

**CONDITIONALITY:** the setting is the partial-side one, so this note inherits GENTOW4's whole conditionality stack.

**RESOLUTION TRACE:** lines 76–95.

**TEETH:** the objects `mu2*(r)`, `f_{kappa,r}`, `C_{kappa,r}` are exactly those compiled at `EFF.GENTOW4.18`–`.19`, `.24`–`.25` ✓.

---

### EFF.GENTOW6.07  [lemma]

**CANONICAL STATEMENT:**

> “> **LEMMA GENTOW-6.1 (multiplicity tie, every side shape).** In the
> > setting above, for every side of P(F) and every monic irreducible
> > r | R_kappa:
> >
> >     deg f_{kappa,r} = D'·l·deg(r)·m_r ,
> >
> > i.e. the block's own level-1 polygon is the single side of slope
> > kappa and length l·deg(r)·m_r with residual EXACTLY psi_r-power
> > r^{m_r} (monic; unit-free), and consequently, at every class with
> > l·deg r >= 2 read through the composed key,
> >
> >     mu2*(r) = m_r
> >
> > — the composed polygon length equals the level-1 residual
> > multiplicity, class by class, at MIXED sides included.”

**FORM:** blockquoted lemma with two displays.

**DERIVATION:** three steps — “the block's pure-power residual with exponent k_r (I), residual multiplicativity (II — the cite), and the UFD match k_r = m_r (III)” (`.08`–`.10`).

**CONDITIONALITY:** **this lemma CLOSES GENTOW4-BOX-1** — the note says so (“The box closes here”) and the STATUS TABLE records it. **Its Step II rests on a CITE**, whose conditionality is `.29`. So the closure is `cite-conditional`, not self-contained, and the note fences it as such.

**RESOLUTION TRACE:** statement lines 95–113; proof 115–237.

**TEETH:** the tie “held on each measured class row including the mu2* = 3 and transposed-profile rows” (`.28`).

---

### EFF.GENTOW6.08  [derivation]

**CANONICAL STATEMENT:** Step I, verbatim (abridged):

> “**Step I (block single-side + pure-power residual — ANNEX-LEMMA
> HE7-13'(a)'s argument at the tower entry).** f_{kappa,r} =: f_C is
> monic in O[x], divides f, Phi' ∤ f_C, and its roots are exactly
> C_{kappa,r} … Every root of f_C has dv(Phi'(rho)) = kappa
> (the class definition), so by LEMMA HE6-3(b) applied to f_C — whose
> hypotheses (monic on the opening locus, disc != 0 as a divisor of
> f, Phi' ∤ f_C) hold — every slope lambda of f_C's own polygon
> carries D'·L_lambda(f_C) > 0 roots at dv(Phi') = lambda: kappa is
> the ONLY slope. The polygon is one side from (0, dv(A_0^C)) to
> (M_C, 0), M_C = deg f_C / D' … of
> slope kappa and length M_C, so l | M_C and deg R^{(C)} = M_C/l …”

**FORM:** bold-headed proof step.

**DERIVATION:** a slope-uniqueness argument (every root sits at one slope, so the polygon has one side) plus a factor-class argument via HE6-4.

**CONDITIONALITY:** hypotheses of HE6-3(b) are checked explicitly for `f_C` — a divisor-inheritance check that matters because `f_C` is not the original member.

**RESOLUTION TRACE:** lines 115–140.

**TEETH:** guarded by the sealed battery's class rows.

---

### EFF.GENTOW6.09  [derivation]  *(the CITE leg)*

**CANONICAL STATEMENT:** Step II, the FGMN cite. Its identification in the STATUS TABLE is “PROVED-VIA-CITE (FGMN Thm 2.8 + Cor 4.7(3) + Cor 4.4(4))”, and its conditionality is stated at `.29`:

> “**Cite conditionality (S1).** Step II rests on the FGMN local
> print at verified layout lines through GENTOW2's dictionary +
> shear (its faithfulness map, incl. the psi2 = R_2(phi_3) row);
> the repo-side (MULT) leg is displayed as corroboration but the
> cite carries the load. GENTOW2's own arc grade applies upstream.”

**FORM:** proof step routed to an external source.

**DERIVATION:** `[CITED]` — residual multiplicativity from the published FGMN results, transported through GENTOW2's tower↔MacLane dictionary and shear.

**CONDITIONALITY:** **THREE conditionalities stack here and all three are disclosed**: (1) the cite carries the load, the repo-side (MULT) leg is corroboration only; (2) the transport runs through GENTOW2's faithfulness map; (3) **“GENTOW2's own arc grade applies upstream.”** A consumer of LEMMA 6.1 inherits all three.

**RESOLUTION TRACE:** the step in S1's proof body; the conditionality at 624–629.

**TEETH:** the FGMN local print exists at `docs/references/fgmn_residual_ideals_1305.0775v3_layout.txt` with “numbering verified at GENTOW2's ledger” — a **faithful-cite** discipline, the pattern GENTOW4-BOX-1's own retirement note anticipated (`EFF.GENTOW4.36`: “a faithful-cite unit in GENTOW2's pattern would retire it”). **This note is that unit.**

---

### EFF.GENTOW6.10  [derivation]

**CANONICAL STATEMENT:** Step III, the UFD match `k_r = m_r`, closing the lemma. Its role: Step I gives the block a pure-power residual `r^{k_r}`; Step II gives residual multiplicativity across blocks; Step III matches exponents by unique factorization in `K[Z]`.

**FORM:** proof step (`[ASSEMBLED]` designation — the step's text is quoted at its home in S1's proof body).

**DERIVATION:** unique factorization applied to the product of block residuals against `R_kappa`.

**CONDITIONALITY:** **the match absorbs units into a scalar** — the S5 gauge row notes “the tie itself is scalar-robust — Step III's UFD match absorbs units into gamma*”, which is why the tie survives the η ≠ 1 exposure that touches other clauses.

**RESOLUTION TRACE:** S1's proof close; the scalar-robustness note at 641–643.

**TEETH:** the scalar-robustness claim is what makes the tie the ONE S1 clause not exposed at η ≠ 1 (`.28`).

---

### EFF.GENTOW6.11  [lemma]

**CANONICAL STATEMENT:** LEMMA GENTOW-6.2 and COR GENTOW-6.2C — the partial-side block budget layer (§S2, lines 238–343), whose status is “PROVED (budget half; ledger half = GENTOW6-BOX-2)”.

**FORM:** blockquoted lemma + corollary.

**DERIVATION:** GENTOW-1's budget fibration transported to the block factor, via the projection.

**CONDITIONALITY:** **the split is explicit and is the note's most consequential scope statement**: the **BUDGET half is proved**; the **LEDGER/COUNT half is BOX-2** (`.31`). A consumer must not read “block budgets PROVED” as a count law.

**RESOLUTION TRACE:** §S2, lines 238–343; the box at 615–619.

**TEETH:** COR 6.2C(d)'s supplier chain was the PE1 GAP (F-2) and is repaired prove-first by LEMMA 6.4 (`.36`); PE2 walked it and found “nothing narrows”.

---

### EFF.GENTOW6.12  [theorem]

**CANONICAL STATEMENT:** THEOREM GENTOW-6.3, the attainment certificate (§S3, lines 344–528), at its r1-pinned scope. The section header carries the pin, verbatim:

> “## S3. THEOREM GENTOW-6.3 (the attainment certificate — GENTOW3-BOX-1
> ## made genre-general at mu2 = 2, f1 = 1) [rider (post-PE2,
> ## 2026-08-10; PE2-F-1): header re-scoped to the theorem head's
> ## GT6-r1 pin — at f1 >= 2 the sealed displays are NOT proved as
> ## written; the f1-general form is ANNEX-6.3' (S6.1),
> ## machine-unexercised = GENTOW6-BOX-4]”

**FORM:** blockquoted theorem under a rider-carrying section header.

**DERIVATION:** the new combinatorial prose of S3's (a)/(b) — the Y-bookkeeping and the certificate slot.

**CONDITIONALITY — CHAIN-SCOPE, three layers:** composed “every genre” → **[GT6-r1] theorem-head SCOPE PIN to f1 = 1** (the CRITICAL) → **PE2-F-1** re-scopes the two summary sites that still read “every genre”. **At f1 ≥ 2 the sealed displays are NOT proved as written**; the f1-general form is ANNEX-6.3′ (`.35`). SUPERSESSION KIND: **scope-pin**, twice.

**RESOLUTION TRACE:** §S3, lines 344–528; the annex at 702–850.

**TEETH:** the sealed battery's “seven certificate rows attained THETA exactly at the predicted slot and valuation” (`.28`); the f1 ≥ 2 face is BOX-4 (`.33`, then DISCHARGED at `.45`).

---

### EFF.GENTOW6.13  [scope record]

**CANONICAL STATEMENT:** the CERT-TOP scope, from the STATUS TABLE rider:

> “CERT-TOP unconditional at f1 = 1 genres with 2i_{t*} >= D' (vacuous at f1 >= 2)”

**FORM:** clause inside the rider.

**DERIVATION:** `[RECORD]` of a conditional scope.

**CONDITIONALITY:** **“vacuous at f1 >= 2”** is a sharp scope fact: the side condition `2i_{t*} ≥ D'` cannot hold there, so CERT-TOP has no content at f1 ≥ 2. **Cross-entry: this is exactly the clause T4 absorbs as `GT6-CERT-TOP`** — `EFF.T4.16`/`.22`–`.23` record it as discharged “ONLY at f₁ = 1, μ₂ = 2, 2i_{t*} ≥ D′”, matching this note's scope verbatim ✓.

**RESOLUTION TRACE:** line 70 (the rider); the theorem's own display in §S3.

**TEETH:** T4's consumption is the downstream check; `spec/EFF-T4.md` records it as an absorbed instance with the same three-part fence.

---

### EFF.GENTOW6.14  [run-record]

**CANONICAL STATEMENT:** §S4's machine leg — `gentow6_checks.py`, importing B1/B2 additively, GREEN 401/0 with 4/4 teeth, PARI-adjudicated.

**FORM:** section (lines 529–593).

**DERIVATION:** `[RUN]`

**CONDITIONALITY:** all rows sit at f1 = 1 AND η = 1 (`.28`), so the battery is doubly blind to the axes BOX-4 names.

**RESOLUTION TRACE:** §S4.

**TEETH:** **RE-VERIFIED: `=== VERDICT: GREEN -- 401 checks, 0 violations (0.4s) ===`, with `tooth T-SHEXACT fired x1` among the four** ✓. Artifact md5s `121e5c4a…` / `1bcb9506…` both match the S6.4 ledger ✓.

---

### EFF.GENTOW6.15  [fence]

**CANONICAL STATEMENT:** the machine-scope fence, verbatim:

> “* **Machine scope.** All tie/block rows live on the FAM-P5
>   geography (f1 = 1, e1 = 2, l = 2, deg r = 1 classes; f2 = 2
>   pure-power ties only via B2's P2). A mixed side with a deg-2
>   irreducible factor (l·deg r = 4) was NOT machine-exercised —
>   S1's proof does not distinguish, but the record says so.”

**FORM:** bulleted scope row.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** **“S1's proof does not distinguish, but the record says so”** — the honest separation of proof generality from machine coverage, stated in one sentence.

**RESOLUTION TRACE:** lines 629–633.

**TEETH:** `signed coverage disclosure`.

---

### EFF.GENTOW6.16  [fence]

**CANONICAL STATEMENT:** the GAUGE AXIS row, verbatim:

> “[GT6-r1 GAUGE AXIS (PE1 F-3): every frame in all three legs sits
>   at f1 = 1 AND eta = 1 — verified this round against the family
>   constructors: PHI1 ∈ {x²−2 (A5/A7/B/C, Q2), x³−2 (FAM-E/GEN-R2X,
>   Q2), x²−3 (FAM-P5 geography/GEN-P3/GEN-P3b/FAM-D, Q3)}, each a
>   binomial whose stage residue x^{e1}/pi is ≡ 1 — so the sealed
>   battery is structurally blind to the gauge seam (corrected =
>   naive at every row) and to every f1 >= 2 phenomenon (S6.1,
>   GENTOW6-BOX-4). Exposed but unexercised faces at eta != 1:
>   S1 Step I's monicity/normalizer clauses and the S3 digit VALUES
>   (the tie itself is scalar-robust — Step III's UFD match absorbs
>   units into gamma*). No sealed sentence is falsified by this row;
>   the fence was under-disclosed.]”

**FORM:** dated coverage rider.

**DERIVATION:** `[RECORD]`, verified against the family constructors at the round.

**CONDITIONALITY:** SUPERSESSION KIND: **inventory completion**. **The row does three things no other gauge disclosure in this queue does**: it verifies η = 1 from the constructors' *binomial shape*, it names WHICH faces are exposed (S1 Step I's clauses; the S3 digit values), and it identifies the one face that is **scalar-robust** (the tie). **Cross-entry: this is the fifth note in the queue to add an η-axis disclosure**, all downstream of one erratum.

**RESOLUTION TRACE:** lines 634–645.

**TEETH:** `signed coverage disclosure`. **ARITHMETIC AUDIT: each named `Phi'` is a binomial `x^{e1} − c`, so `x^{e1}/π ≡ c/π`-shaped with stage residue 1 under the stated normalizations** — the constructor-level argument is checkable from the shapes given ✓.

---

### EFF.GENTOW6.17  [record]

**CANONICAL STATEMENT:**

> “**Record sentences (no universal quantifiers).** The battery's
> 401 checks passed with 0 violations on the constructed rows named
> in S4; the tie held on each measured class row including the
> mu2* = 3 and transposed-profile rows; the seven certificate rows
> attained THETA exactly at the predicted slot and valuation; the
> two regime-4 self-shadows vanished identically; the E-ENTRY row's
> shadow read flipped while PARI confirmed the honest sigma.”

**FORM:** bold-headed record paragraph.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** **the heading is the discipline**: “no universal quantifiers”. Every sentence is scoped to measured rows. **This is the cleanest instance in the queue of a note refusing to let its battery speak generally.**

**RESOLUTION TRACE:** lines 667–673.

**TEETH:** disposition `signed evidence-grade record`; the 401/0 figure re-verified ✓.

---

### EFF.GENTOW6.18  [acceptance-box]

**CANONICAL STATEMENT:**

> “**GRADE: 0/2 (attempt).** Hostile arc owed (PE1+). The S1 cite leg
> wants a PDF-provenance re-verification in its own pass (GENTOW2's
> pattern); S3's proof of (a)/(b) is new combinatorial prose —
> the natural PE1 targets are the Y-monotonicity step ("every
> division/recarry step lowers Y-exponent by >= e2") and the
> secondary-extraction clause of (a).”

**FORM:** bold-headed grade line.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** superseded to **ACCEPTED 2/2** (`.43`). **The line PREDICTS its own attack surface** — and PE1's CRITICAL landed adjacent to it (the S3 scope quantifier rather than the two named steps). Recorded because self-predicted attack surfaces are rare and this one was half-right.

**RESOLUTION TRACE:** lines 675–681.

**TEETH:** the predicted targets are S3's (a)/(b); the CRITICAL hit S3's head.

---

### EFF.GENTOW6.19  [changes-record]  *(CHAIN-SCOPE — TERMINAL)*

**CANONICAL STATEMENT:** §S6.1's scope pin and its annex, `[ASSEMBLED]` from lines 702–850. The section head:

> “### S6.1 THE DATED GAUGE ANNEX + THE f1 = 1 SCOPE PIN (F-1) +
> ### ANNEX-THEOREM GENTOW-6.3'”

**FORM:** numbered repair section.

**DERIVATION:** the gauge annex (the corrected `chat_t`, as in the four sibling notes) plus a **scope pin** on the theorem head plus a **re-derivation** of the f1-general form as a separate annex theorem.

**CONDITIONALITY:** **the repair shape is the important part**: rather than weaken the theorem silently, r1 (i) pins the sealed theorem to f1 = 1, and (ii) states the f1-general claim as a NEW, separately-graded object (ANNEX-6.3′) whose machine status is a NEW box (BOX-4). **That is a textbook honest response to a scope CRITICAL** and is the pattern the queue should record.

**RESOLUTION TRACE:** lines 702–850; the pin's targets at 70 (STATUS TABLE) and 344–349 (S3 header).

**TEETH:** the pin is “verified at source” by PE2 (`.03`), which also measured the **sealed-(c) boundary break exactly where the pin predicts** (`.43`) — the strongest possible confirmation of a scope pin: the excluded region was measured and it does break.

---

### EFF.GENTOW6.20  [theorem]

**CANONICAL STATEMENT:** **ANNEX-THEOREM GENTOW-6.3′** (§S6.1) — the f1-general re-derivation of the attainment certificate.

**FORM:** blockquoted annex theorem.

**DERIVATION:** re-derived at r1 for general f1 at the corrected key.

**CONDITIONALITY:** **prose-only at r1** — “machine-unexercised = GENTOW6-BOX-4”, and the S5 box says “until it runs, 6.3' is prose-only.” **That fence is LIFTED at μ₂ = 2 by the SUB-BOX append** (`.45`): “ANNEX-THEOREM GENTOW-6.3' (S6.1) thereby loses its "machine-unexercised at f1 >= 2 / prose-only" fence at mu2 = 2; its derivation keeps the prose grade of this note's arc.” SUPERSESSION KIND: **license**, scoped to μ₂ = 2, with the derivation's grade unchanged.

**RESOLUTION TRACE:** §S6.1; the box at 646–653; the discharge at 1249–1264.

**TEETH:** PE2's LEG A measured 6.3′(b)'s certificate display exact at two f1 = 2 / η ≠ 1 frames (`.33`), and the box-4 row later ran it at battery grade on four x-ful rows (`.45`).

---

### EFF.GENTOW6.21  [lemma]  *(CHAIN-CAP — TERMINAL)*

**CANONICAL STATEMENT:** **LEMMA GENTOW-6.4, the 𝒯-FREE CAP LEMMA** (§S6.2, lines 851–1034), proved prove-first in answer to PE1's GAP F-2 (“GENTOW-5(a) consumed at f off its proved 𝒯 scope”).

**FORM:** numbered repair section carrying a new lemma + a machine leg.

**DERIVATION:** a fresh derivation of the cap behaviour without the 𝒯 hypothesis, so that COR 6.2C(d)'s consumption is legitimate at partial-side `f`.

**CONDITIONALITY:** **prove-first, not fence-first** — the GAP was answered by proving the needed statement rather than by narrowing the consumer. **Clause (a)'s per-window bijection codomain is then CORRECTED by PE2-F-2** (`.42`).

**RESOLUTION TRACE:** §S6.2, lines 851–1034; the codomain correction in the post-PE2 fold.

**TEETH:** **machine-verified at three frames incl. two partial-side members**; the instrument's verdict is re-read: `teeth: T-CAP fired x3, T-TRI fired x28` / `=== VERDICT: GREEN -- 303 checks, 0 violations ===` ✓, md5 `bf953a8f…` matching the S6.4 ledger ✓.

---

### EFF.GENTOW6.22  [instrument-record]

**CANONICAL STATEMENT:** the S6.4 md5 ledger, verbatim:

> “Sealed machine artifacts BYTE-FROZEN, md5s re-verified this round:
> gentow6_checks.py 121e5c4a95a19692db7910b9ffedfd6e ·
> gentow6_checks_output.txt 1bcb950618de39f26fe8e0c298abb783 ·
> gentow6_pe1_fresh.py 59fe76cefd36e4ffc8688adfd8e5f01e ·
> gentow6_pe1_fresh_output.txt 9de61325ef9b07dd2f52954d9d3e3f12.
> New r1 artifacts (this round): gentow6_r1_caplemma.py md5
> bf953a8ff0ef925d5be711fbbe60a418 (sealed 640b31ff at 14329683;
> post-run-1 instrument repair disclosed in S6.2 and in the RED
> commit e803c21d) · gentow6_r1_caplemma_output.txt md5
> fa9d7bf8cf2975539df63c4c166031d7 (GREEN 303/0, exit 0, isolated
> re-run bit-identical) · gentow6_r1_caplemma_output_run1_RED.txt
> (the kept RED artifact).”

with the ten-site edit manifest and its reading rule: “All sealed statement displays byte-untouched; riders GOVERN their quoted displays.”

**FORM:** numbered manifest + md5 ledger.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** none.

**RESOLUTION TRACE:** lines 1061–1086.

**TEETH:** **ALL SIX md5s in this ledger MATCH at compile time** ✓ (`121e5c4a`, `1bcb9506`, `59fe76ce`, `9de61325`, `bf953a8f`, `fa9d7bf8`), and the RED artifact exists. **PIN FINDING: of the three commit tokens cited in one clause — “sealed 640b31ff at 14329683 … the RED commit e803c21d” — `14329683` and `e803c21d` resolve, but `640b31ff` does NOT** (`git cat-file -t 640b31ff` → “Not a valid object name”). The seal is nonetheless independently confirmed: `14329683`'s commit message reads “GENTOW6 r1 (commit 2/3, SEAL): fresh instrument gentow6_r1_caplemma.py sealed with preregistered P-A..P-E + teeth T-TRI/T-CAP …”. **So the seal event is real and pinned; one token in the sentence is not resolvable.** OPEN-CALL 4.

---
