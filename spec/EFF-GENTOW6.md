# EFF-GENTOW6 — CANONICAL EFFECTIVE SPECIFICATION v2 (full compilation)

**COMPILED-BY: opus (2026-08-14, Asvin-authorized substitution, PROJECT_STATE #51). CODEX CROSS-READ OWED (post-reset).**

## 1. Header

**Source note:** `lean/notes/openmath/GENTOW6_PROOF_2026-08-09.md` (1,265 lines / 71,465 bytes).

**HEAD blob hash:** `5355834f3cd44a1b39245df467bc36c671608cea` (fresh `git hash-object`; last touched by commit `3d7fc37b`).

**Compilation context:** Stage 0a; template `spec/EFF_TEMPLATE_v2.md` AT HEAD (rules 1–28). IDs `EFF.GENTOW6.<nn>`, contiguous, in effective-text order.

**GENRE: TAG-SPARSE ASCII PROSE PROOF + SEALED BATTERY + CITE-BASED PROOF LEG + r1 ROUND + TWO FOLDS + ONE SUB-BOX APPEND.** `grep -c '\tag{'` = **0**. Unit populations (v3 rule 19): **preregistrations** (the sealed battery's wants + 4 teeth; the r1 cap-lemma's P-A…P-E + T-TRI/T-CAP; the box-4 row's wants + 3 teeth), **run-records** (five instruments), **proof units** (S1–S3, S6.1–S6.2).

**This note is the corpus's BOX RESIDUE UNIT**: it exists to close three other notes' boxes. Its title says so, and the queue charge for this entry is its own four boxes — **GENTOW6-BOX-1…BOX-4 are ledger MATH items `HYP.67`–`HYP.70`, and their exact open wording is compiled verbatim at `.23`–`.25`/`.27` and again, as amended, at `.34`–`.35`.**

### FREEZE PREDICATES (v3 rule 21 — four text pins; **three reproduce, one reconciles**)

`[TABLE — compiler ledger]`

| # | Predicate | Stated | Verification |
|---:|---|---|---|
| F-1 | post-PE3: “pre-fold body md5 11b7b6b67f91aaf8bec1481a56f1a54a (1,088 lines) = the rider state @ 1194b1a4” | md5 + lines | **NOT a HEAD prefix — correctly so** (the four fold/upgrade brackets inserted `+58/−0` at L314). **`1,088 + 58 = 1,146` reconciles exactly** ✓ |
| F-2 | same: “prefix through the first insertion, L314, byte-identical, md5 8e30f26a” | md5 + line | **lines 1–314 hash to `8e30f26a…`** ✓ |
| F-3 | same: “post-fold body md5 6e0489fbcaff25bab8042bc8ec749bbd (1,146 lines)” | md5 + lines | **lines 1–1146 → `6e0489fb…`** ✓ |
| F-4 | SUB-BOX append: “pure append on the frozen body, pre-append md5 157b16adc82f6de4c6a0f65f5f8ffe85, 1,197 lines” | md5 + lines | **lines 1–1197 → `157b16ad…`** ✓ |

**Instrument freeze (S6.4 + riders): ELEVEN artifact md5s stated across the note; all eleven match** (§3.2).

### Effective-text rule (DERIVED)

**(i) r1 was applied as bracketed riders with a ten-site manifest** (S6.4), whose closing sentence fixes the reading rule exactly: “All sealed statement displays byte-untouched; **riders GOVERN their quoted displays**.” **(ii) The post-PE2 fold is minors-only** — “a rider is not a pass” — and re-scopes two stale summary sites plus one lemma display. **(iii) After PE3 the note is frozen**: “**THE NOTE IS FROZEN AT THIS RECORD: dated appends and dated brackets only …**”. **(iv) The SUB-BOX append is a pure append that CHANGES TWO BOX STATES** (BOX-1 → CLOSED AT f1 = 1; BOX-4 → DISCHARGED).

**EFFECTIVE TEXT = the note as written (r1 riders landed and governing), READ AS RE-SCOPED by the post-PE2 fold and the post-PE3 acceptance fold, and AS AMENDED at two box states by the SUB-BOX append.**

### Supersession CHAINS (v3 rule 25 — TERMINALs marked)

`[TABLE]`

| Chain | Layers | TERMINAL |
|---|---|---|
| **CHAIN-SCOPE (THEOREM 6.3's genre quantifier)** | composed: “every genre” → **[GT6-r1] F-1** (a CRITICAL): the theorem-head SCOPE PIN to **f1 = 1** at the corrected `chat_t`; the f1-general form becomes **ANNEX-THEOREM 6.3′** → **PE2-F-1 rider**: two stale summary sites (STATUS TABLE row (3); the S3 section header) re-scoped to the pin | **PE2-F-1** (`.30`) |
| **CHAIN-CAP (GENTOW-5(a) off its 𝒯 scope)** | composed: COR 6.2C(d) consumes GENTOW-5(a) at `f` off its proved 𝒯 scope → **[GT6-r1] F-2, prove-first**: the **𝒯-FREE CAP LEMMA GENTOW-6.4** is proved and machine-verified at three frames | **LEMMA 6.4** (`.21`) |
| **CHAIN-CODOMAIN** | 6.4(a)'s per-window bijection codomain as displayed → **PE2-F-2**: CORRECTED by a dated bracket — above-top slots are FORCED to 0; exact condition `n mod D2 != D2 − 1`, **sharper than the report's `D2 ∤ n`** (FRAME-F, a `D2 | n` member, carries 3 such slots) | **PE2-F-2** (`.31`) |
| **CHAIN-BOX1** | BOX-1 open (μ₂ ≥ 3 attainment) → **SUB-BOX append**: **CLOSED AT f1 = 1** by THEOREM GENTOW-6.5, *at that note's 0/2* → within it, **[r1 GENTOW6-BOXES]** withdraws the “iff” (the converse is **FALSE**, with a witness) and corrects the uniqueness census | **the SUB-BOX append as r1-bracketed** (`.34`) |
| **CHAIN-BOX4** | BOX-4 open (no f1 ≥ 2 / η ≠ 1 machine contact) → **acceptance-fold F-1**: the “NO machine contact” sentence is DATED and stale-conservative; **BOX-4 STAYS OPEN**, ask unchanged → **SUB-BOX append**: **DISCHARGED** by a sealed battery-grade row, GREEN 265/0 at four x-ful rows | **the SUB-BOX append** (`.35`) |
| **CHAIN-GRADE** | 0/2 → PE1 NOT CLEAN (1C+1G+4m) → r1 → **PE2 CLEAN, 0/2 → 1/2** → rider → **PE3 CLEAN, 1/2 → 2/2 ACCEPTED** | the acceptance record (`.32`) |

### SERIES declared (v3 rule 28)

**SERIES ARC** — `.02` (r1 ARC record) → `.03` (post-PE2 ARC record) → `.19`–`.22`, `.28` (S6, the r1 round) → `.29`–`.31` (S6.5, the post-PE2 fold) → `.32` (acceptance 2/2, with the post-PE3 fold) → `.33`–`.35` (the SUB-BOX append).

**SERIES BOXES** — the four residue boxes, emitted per box in source order (`.23` BOX-1, `.24` BOX-2, `.25` BOX-3, `.27` BOX-4) and **re-emitted at their amended states** (`.34` BOX-1 CLOSED AT f1 = 1, `.35` BOX-4 DISCHARGED). **This double emission is deliberate and is the entry's central compilation act**: the ledger rows `HYP.67`–`HYP.70` quote the boxes at *different* states, and a consumer must be able to see both.

### Scope split — THREE predicates

**A. Mathematical absorption / supply SET.** The charge, verbatim:

> “Charge (three targets, priority order): (1) GENTOW4-BOX-1 — the
> mixed-side tie mu2*(r) = m_r, literature-first (FGMN residual
> multiplicativity; GENTOW2's faithful-cite pattern) or via HE7's
> frozen bouquet machinery; (2) GENTOW4-BOX-2 — the partial-side
> entry-budget layer (GENTOW-1's budget lemma at the block factor);
> (3) GENTOW3-BOX-1 — the self-shadow attainment certificate made
> genre-general or honestly scoped.”

Delivered per the STATUS TABLE: **LEMMA GENTOW-6.1 (PROVED-VIA-CITE + corpus leg; GENTOW4-BOX-1 closed), LEMMA GENTOW-6.2 + COR GENTOW-6.2C (budget half proved), THEOREM GENTOW-6.3 (at μ₂ = 2, f1 = 1)** — plus, at r1, **LEMMA GENTOW-6.4** (the 𝒯-free cap lemma) and **ANNEX-THEOREM GENTOW-6.3′**.

**B. Append / consumption PROTOCOL scope.** The note **closes another note's box** (GENTOW4-BOX-1) but does not edit that note: “Sources consumed (never edited)”. Verified: `grep -cF 'GENTOW-6' GENTOW4_PROOF_2026-08-09.md` = **0** (N1) — **GENTOW4's BOX-1 text is unamended**, so the closure lives only here. Ledger row `HYP.144` still states GENTOW4-BOX-1 as open “UNPROVED here … a faithful-cite unit in GENTOW2's pattern would retire it” — **and this note IS that faithful-cite unit**, unlanded. OPEN-CALL 1.

**C. Inbound corrections RECEIVED:** this note's PE1 (1 CRITICAL + 1 GAP + 4 MINOR), PE2 (2 MINOR), PE3 (1 MINOR), plus the **GENTOW6-BOXES sibling note's own r1** (whose passPE1 F-1/F-2 corrections are transcribed into the SUB-BOX append's brackets, `.34`), plus GENTOW4's acceptance (the “4.C ACCEPTANCE UPGRADE lines”).

### Resolution rules applied

1. r1 riders are landed and **GOVERN their quoted displays** (the note's own rule).
2. **Box wording is reproduced EXACTLY as left at BOTH states** (queue charge), each with its `HYP.67`–`HYP.70` cross-reference quoted.
3. Where a box state changed after the ledger row was written, both are shown and the divergence is recorded as an OPEN-CALL, never silently reconciled.
4. Numbers recomputed from committed artifacts; pins verified individually.
5. The cite leg (S1 Step II) is compiled as a `cite-conditional` supply with its conditionality quoted.

### Quotation discipline

Verbatim or `[ASSEMBLED]`; `…` marks truncation. ASCII preserved. The STATUS TABLE is `[TABLE]`-flagged.

---

## 2. Statement inventory

35 units, contiguous, in effective-text order. FORM on every unit.

### EFF.GENTOW6.01  [convention]

**CANONICAL STATEMENT:**

> “# GENTOW-6: THE BOX RESIDUE UNIT — the mixed-side multiplicity tie
> # (GENTOW4-BOX-1), the partial-side block budget layer
> # (GENTOW4-BOX-2), and the attainment certificate (GENTOW3-BOX-1),
> # 2026-08-09”

with the status line: “**STATUS: COMPLETE (sealed battery GREEN 401/0, 4/4 teeth). Unit: GENTOW-6, BOX-CLOSURE campaign residue wave. Grade 0/2 (attempt; hostile arc owed).**”

**FORM:** four-line title + bold status.

**DERIVATION:** `[RECORD]`. **The title names three OTHER notes' boxes as this note's targets** — the unit is defined by what it closes.

**CONDITIONALITY:** grade → **ACCEPTED 2/2** (`.32`).

**RESOLUTION TRACE:** lines 1–8; correction 1148–1156.

**TEETH:** the battery figure re-read: `=== VERDICT: GREEN -- 401 checks, 0 violations (0.4s) ===` ✓.

---

### EFF.GENTOW6.02  [repair-round record]  *(SERIES ARC, link 1)*

**CANONICAL STATEMENT:**

> “**[GT6-r1] ARC: passPE1 (GENTOW6_passPE1_report.md @ cd74ef6f,
> 2026-08-10) = NOT CLEAN — 1 CRITICAL (F-1, gauge/f1 scope at
> THEOREM 6.3's genre quantifier) + 1 GAP (F-2, GENTOW-5(a) consumed
> at f off its proved 𝒯 scope) + 4 MINOR. This round is the r1
> repair: the dated gauge annex + f1 = 1 scope pin + the f1-general
> re-derivation ANNEX-THEOREM 6.3′ (S6.1); the 𝒯-free cap LEMMA
> GENTOW-6.4, machine-verified at three frames incl. two
> partial-side members (S6.2); minors F-3/F-5/F-6 + the F-4 closure
> record (S6.3). Sealed machine artifacts BYTE-FROZEN (md5s
> re-verified, S6.4). Grade stays 0/2.**”

**FORM:** bold dated ARC record.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** **this is the only CRITICAL in the four GENTOW entries of this queue**, and its subject is a quantifier: “every genre” in a theorem head. The GAP is a scope violation (a lemma consumed off its proved locus) answered **prove-first** with a new lemma.

**RESOLUTION TRACE:** lines 10–20.

**TEETH:** `git cat-file -t cd74ef6f` = commit ✓.

---

### EFF.GENTOW6.03  [acceptance-box]  *(SERIES ARC, link 2)*

**CANONICAL STATEMENT:** the post-PE2 ARC record, `[ASSEMBLED]` from lines 22–43. Its two folded minors, verbatim:

> “PE2-F-1 the two stale summary sites
> (STATUS TABLE row (3); S3 section header) re-scoped to the
> r1 pin's f1 = 1 wording, mirroring the theorem-head scope pin
> verified at source; PE2-F-2 LEMMA GENTOW-6.4(a)'s per-window
> bijection codomain CORRECTED by a dated bracket at the display —
> re-derived from (a)'s own triangular formula: above-top slots
> (slot degree > n) are FORCED to 0, the codomain is the tuples
> supported on slot degrees <= n with top pinned 1; exact condition
> for above-top slots = n mod D2 != D2 - 1 (sharper than the
> report's D2 ∤ n framing — FRAME-F, a D2 | n member, carries 3
> such slots); machine leg gentow6_rider_codomain.py GREEN 24/0
> with tooth T-COD x3 …”

**FORM:** bold dated ARC record.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** **PE2-F-2's correction is SHARPER than the finding that prompted it** — the report framed the condition as `D2 ∤ n`; the note derives `n mod D2 != D2 − 1` and exhibits a `D2 | n` counter-member (FRAME-F) carrying three above-top slots. That is a repair that improves on its own charge.

**RESOLUTION TRACE:** lines 22–43; the corrected display at `.31`.

**TEETH:** `git cat-file -t 161e8ce1` = commit ✓; `gentow6_rider_codomain.py` exists ✓.

---

### EFF.GENTOW6.04  [fence]

**CANONICAL STATEMENT:** the charge (quoted in §1 A) and the sources, verbatim in brief:

> “Sources consumed (never edited): GENTOW4_PROOF …, GENTOW3_PROOF …, GENTOW1_PROOF …, GENTOW2_PROOF (the cite pattern + the tower<->MacLane dictionary + shear) …, HE7_PROOF …, HE6_PROOF …, HE6R1_RECON …,
> the FGMN local print
> (docs/references/fgmn_residual_ideals_1305.0775v3_layout.txt,
> numbering verified at GENTOW2's ledger); machine:
> gentow4_checks.py + gentow3_checks.py (imported additively).”

**FORM:** charge + import inventory.

**DERIVATION:** `[RECORD]`. **The FGMN local print is an EXTERNAL LITERATURE import** — unique in this queue — and its numbering is verified at another note's ledger.

**CONDITIONALITY:** the `[GT6-r1, PE1 F-5(i)]` rider adds two unitemized GENTOW1 imports (COROLLARY GENTOW-1.1 and LEMMA GENTOW-2). SUPERSESSION KIND: **inventory completion**.

**RESOLUTION TRACE:** lines 45–62.

**TEETH:** the imported machine legs `gentow4_checks.py` (md5 `98cdd4a4…`) and `gentow3_checks.py` (md5 `7cf3a7b8…`) are **the same artifacts this queue verified for GENTOW4 and GENTOW3** ✓ — a three-way artifact identity.

---

### EFF.GENTOW6.05  [table]  `[TABLE]`

**CANONICAL STATEMENT:** the STATUS TABLE, source lines 66–72, transcribed with the PE2-F-1 rider in row (3):

| target | statement | status |
|---|---|---|
| (1) mixed-side tie | LEMMA GENTOW-6.1 (S1) | PROVED-VIA-CITE (FGMN Thm 2.8 + Cor 4.7(3) + Cor 4.4(4)) + corpus leg; GENTOW4-BOX-1 closed |
| (2) block budgets | LEMMA GENTOW-6.2 + COR GENTOW-6.2C (S2) | PROVED (budget half; ledger half = GENTOW6-BOX-2) |
| (3) attainment | THEOREM GENTOW-6.3 (S3) | PROVED at mu2 = 2, f1 = 1 [rider (post-PE2, 2026-08-10; PE2-F-1): the theorem-head GT6-r1 scope pin governs this row — "every genre" reads AT f1 = 1, at the corrected chat_t; CERT-TOP unconditional at f1 = 1 genres with 2i_{t*} >= D' (vacuous at f1 >= 2); f1 >= 2 = ANNEX-6.3' (S6.1), machine-unexercised = GENTOW6-BOX-4] (regimes 3/4 displayed; mu2 >= 3 = GENTOW6-BOX-1) |
| battery | gentow6_checks.py (S4) | GREEN 401/0, 4/4 teeth, PARI-adjudicated |

**FORM:** table.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** **row (3) carries the PE2-F-1 rider IN the table cell** — one of the two “stale summary sites” re-scoped. SUPERSESSION KIND: **scope-pin**. Row (1)'s “GENTOW4-BOX-1 closed” is the claim `HYP.144` does not yet reflect (OPEN-CALL 1).

**RESOLUTION TRACE:** lines 64–72.

**TEETH:** the battery row re-verified (`.01`).

---

### EFF.GENTOW6.06  [convention]

**CANONICAL STATEMENT:** the S1 setting, verbatim (abridged):

> “**Setting (GENTOW-4 S0, restated for one pass).** Tower entry,
> partial inner side: … fix a side of slope kappa = u/l (lowest terms, l = the
> repo's e2 when the side is the inner one) with residual
>
>     R_kappa = c · Prod_r r^{m_r},   c in K^x,  r monic irreducible
>     over K = F_{Q^{f1}}, coherent (S4) normalization.
>
> For each r, C_{kappa,r} is the label class, f_{kappa,r} its block
> factor (GENTOW-4.A(i)), and … mu2*(r) :=
> Sum_{lambda2 > T2} L_{lambda2}(P2(f)) is the composed length read
> through LEMMA HE6R1-2 at the class's datum (GENTOW-4.A(ii)) …
> GENTOW-4.2 proved mu2*(r) = m_r at
> PURE-POWER sides … and boxed the mixed case
> as GENTOW4-BOX-1. The box closes here:”

**FORM:** bold-headed setting with a display.

**DERIVATION:** `[IMPORTED]` from GENTOW-4 S0.

**CONDITIONALITY:** the setting is the partial-side one, so this note inherits GENTOW4's whole conditionality stack.

**RESOLUTION TRACE:** lines 76–95.

**TEETH:** the objects `mu2*(r)`, `f_{kappa,r}`, `C_{kappa,r}` are exactly those compiled at `EFF.GENTOW4.18`–`.19`, `.24`–`.25` ✓.

---

### EFF.GENTOW6.07  [lemma]

**CANONICAL STATEMENT:**

> “> **LEMMA GENTOW-6.1 (multiplicity tie, every side shape).** In the
> > setting above, for every side of P(F) and every monic irreducible
> > r | R_kappa:
> >
> >     deg f_{kappa,r} = D'·l·deg(r)·m_r ,
> >
> > i.e. the block's own level-1 polygon is the single side of slope
> > kappa and length l·deg(r)·m_r with residual EXACTLY psi_r-power
> > r^{m_r} (monic; unit-free), and consequently, at every class with
> > l·deg r >= 2 read through the composed key,
> >
> >     mu2*(r) = m_r
> >
> > — the composed polygon length equals the level-1 residual
> > multiplicity, class by class, at MIXED sides included.”

**FORM:** blockquoted lemma with two displays.

**DERIVATION:** three steps — “the block's pure-power residual with exponent k_r (I), residual multiplicativity (II — the cite), and the UFD match k_r = m_r (III)” (`.08`–`.10`).

**CONDITIONALITY:** **this lemma CLOSES GENTOW4-BOX-1** — the note says so (“The box closes here”) and the STATUS TABLE records it. **Its Step II rests on a CITE**, whose conditionality is `.29`. So the closure is `cite-conditional`, not self-contained, and the note fences it as such.

**RESOLUTION TRACE:** statement lines 95–113; proof 115–237.

**TEETH:** the tie “held on each measured class row including the mu2* = 3 and transposed-profile rows” (`.28`).

---

### EFF.GENTOW6.08  [derivation]

**CANONICAL STATEMENT:** Step I, verbatim (abridged):

> “**Step I (block single-side + pure-power residual — ANNEX-LEMMA
> HE7-13'(a)'s argument at the tower entry).** f_{kappa,r} =: f_C is
> monic in O[x], divides f, Phi' ∤ f_C, and its roots are exactly
> C_{kappa,r} … Every root of f_C has dv(Phi'(rho)) = kappa
> (the class definition), so by LEMMA HE6-3(b) applied to f_C — whose
> hypotheses (monic on the opening locus, disc != 0 as a divisor of
> f, Phi' ∤ f_C) hold — every slope lambda of f_C's own polygon
> carries D'·L_lambda(f_C) > 0 roots at dv(Phi') = lambda: kappa is
> the ONLY slope. The polygon is one side from (0, dv(A_0^C)) to
> (M_C, 0), M_C = deg f_C / D' … of
> slope kappa and length M_C, so l | M_C and deg R^{(C)} = M_C/l …”

**FORM:** bold-headed proof step.

**DERIVATION:** a slope-uniqueness argument (every root sits at one slope, so the polygon has one side) plus a factor-class argument via HE6-4.

**CONDITIONALITY:** hypotheses of HE6-3(b) are checked explicitly for `f_C` — a divisor-inheritance check that matters because `f_C` is not the original member.

**RESOLUTION TRACE:** lines 115–140.

**TEETH:** guarded by the sealed battery's class rows.

---

### EFF.GENTOW6.09  [derivation]  *(the CITE leg)*

**CANONICAL STATEMENT:** Step II, the FGMN cite. Its identification in the STATUS TABLE is “PROVED-VIA-CITE (FGMN Thm 2.8 + Cor 4.7(3) + Cor 4.4(4))”, and its conditionality is stated at `.29`:

> “**Cite conditionality (S1).** Step II rests on the FGMN local
> print at verified layout lines through GENTOW2's dictionary +
> shear (its faithfulness map, incl. the psi2 = R_2(phi_3) row);
> the repo-side (MULT) leg is displayed as corroboration but the
> cite carries the load. GENTOW2's own arc grade applies upstream.”

**FORM:** proof step routed to an external source.

**DERIVATION:** `[CITED]` — residual multiplicativity from the published FGMN results, transported through GENTOW2's tower↔MacLane dictionary and shear.

**CONDITIONALITY:** **THREE conditionalities stack here and all three are disclosed**: (1) the cite carries the load, the repo-side (MULT) leg is corroboration only; (2) the transport runs through GENTOW2's faithfulness map; (3) **“GENTOW2's own arc grade applies upstream.”** A consumer of LEMMA 6.1 inherits all three.

**RESOLUTION TRACE:** the step in S1's proof body; the conditionality at 624–629.

**TEETH:** the FGMN local print exists at `docs/references/fgmn_residual_ideals_1305.0775v3_layout.txt` with “numbering verified at GENTOW2's ledger” — a **faithful-cite** discipline, the pattern GENTOW4-BOX-1's own retirement note anticipated (`EFF.GENTOW4.36`: “a faithful-cite unit in GENTOW2's pattern would retire it”). **This note is that unit.**

---

### EFF.GENTOW6.10  [derivation]

**CANONICAL STATEMENT:** Step III, the UFD match `k_r = m_r`, closing the lemma. Its role: Step I gives the block a pure-power residual `r^{k_r}`; Step II gives residual multiplicativity across blocks; Step III matches exponents by unique factorization in `K[Z]`.

**FORM:** proof step (`[ASSEMBLED]` designation — the step's text is quoted at its home in S1's proof body).

**DERIVATION:** unique factorization applied to the product of block residuals against `R_kappa`.

**CONDITIONALITY:** **the match absorbs units into a scalar** — the S5 gauge row notes “the tie itself is scalar-robust — Step III's UFD match absorbs units into gamma*”, which is why the tie survives the η ≠ 1 exposure that touches other clauses.

**RESOLUTION TRACE:** S1's proof close; the scalar-robustness note at 641–643.

**TEETH:** the scalar-robustness claim is what makes the tie the ONE S1 clause not exposed at η ≠ 1 (`.28`).

---

### EFF.GENTOW6.11  [lemma]

**CANONICAL STATEMENT:** LEMMA GENTOW-6.2 and COR GENTOW-6.2C — the partial-side block budget layer (§S2, lines 238–343), whose status is “PROVED (budget half; ledger half = GENTOW6-BOX-2)”.

**FORM:** blockquoted lemma + corollary.

**DERIVATION:** GENTOW-1's budget fibration transported to the block factor, via the projection.

**CONDITIONALITY:** **the split is explicit and is the note's most consequential scope statement**: the **BUDGET half is proved**; the **LEDGER/COUNT half is BOX-2** (`.31`). A consumer must not read “block budgets PROVED” as a count law.

**RESOLUTION TRACE:** §S2, lines 238–343; the box at 615–619.

**TEETH:** COR 6.2C(d)'s supplier chain was the PE1 GAP (F-2) and is repaired prove-first by LEMMA 6.4 (`.21`); PE2 walked it and found “nothing narrows”.

---

### EFF.GENTOW6.12  [theorem]

**CANONICAL STATEMENT:** THEOREM GENTOW-6.3, the attainment certificate (§S3, lines 344–528), at its r1-pinned scope. The section header carries the pin, verbatim:

> “## S3. THEOREM GENTOW-6.3 (the attainment certificate — GENTOW3-BOX-1
> ## made genre-general at mu2 = 2, f1 = 1) [rider (post-PE2,
> ## 2026-08-10; PE2-F-1): header re-scoped to the theorem head's
> ## GT6-r1 pin — at f1 >= 2 the sealed displays are NOT proved as
> ## written; the f1-general form is ANNEX-6.3' (S6.1),
> ## machine-unexercised = GENTOW6-BOX-4]”

**FORM:** blockquoted theorem under a rider-carrying section header.

**DERIVATION:** the new combinatorial prose of S3's (a)/(b) — the Y-bookkeeping and the certificate slot.

**CONDITIONALITY — CHAIN-SCOPE, three layers:** composed “every genre” → **[GT6-r1] theorem-head SCOPE PIN to f1 = 1** (the CRITICAL) → **PE2-F-1** re-scopes the two summary sites that still read “every genre”. **At f1 ≥ 2 the sealed displays are NOT proved as written**; the f1-general form is ANNEX-6.3′ (`.35`). SUPERSESSION KIND: **scope-pin**, twice.

**RESOLUTION TRACE:** §S3, lines 344–528; the annex at 702–850.

**TEETH:** the sealed battery's “seven certificate rows attained THETA exactly at the predicted slot and valuation” (`.17`); the f1 ≥ 2 face is BOX-4 (`.27`, then DISCHARGED at `.35`).

---

### EFF.GENTOW6.13  [scope record]

**CANONICAL STATEMENT:** the CERT-TOP scope, from the STATUS TABLE rider:

> “CERT-TOP unconditional at f1 = 1 genres with 2i_{t*} >= D' (vacuous at f1 >= 2)”

**FORM:** clause inside the rider.

**DERIVATION:** `[RECORD]` of a conditional scope.

**CONDITIONALITY:** **“vacuous at f1 >= 2”** is a sharp scope fact: the side condition `2i_{t*} ≥ D'` cannot hold there, so CERT-TOP has no content at f1 ≥ 2. **Cross-entry: this is exactly the clause T4 absorbs as `GT6-CERT-TOP`** — `EFF.T4.16`/`.22`–`.23` record it as discharged “ONLY at f₁ = 1, μ₂ = 2, 2i_{t*} ≥ D′”, matching this note's scope verbatim ✓.

**RESOLUTION TRACE:** line 70 (the rider); the theorem's own display in §S3.

**TEETH:** T4's consumption is the downstream check; `spec/EFF-T4.md` records it as an absorbed instance with the same three-part fence.

---

### EFF.GENTOW6.14  [run-record]

**CANONICAL STATEMENT:** §S4's machine leg — `gentow6_checks.py`, importing B1/B2 additively, GREEN 401/0 with 4/4 teeth, PARI-adjudicated.

**FORM:** section (lines 529–593).

**DERIVATION:** `[RUN]`

**CONDITIONALITY:** all rows sit at f1 = 1 AND η = 1 (`.28`), so the battery is doubly blind to the axes BOX-4 names.

**RESOLUTION TRACE:** §S4.

**TEETH:** **RE-VERIFIED: `=== VERDICT: GREEN -- 401 checks, 0 violations (0.4s) ===`, with `tooth T-SHEXACT fired x1` among the four** ✓. Artifact md5s `121e5c4a…` / `1bcb9506…` both match the S6.4 ledger ✓.

---

### EFF.GENTOW6.15  [fence]

**CANONICAL STATEMENT:** the machine-scope fence, verbatim:

> “* **Machine scope.** All tie/block rows live on the FAM-P5
>   geography (f1 = 1, e1 = 2, l = 2, deg r = 1 classes; f2 = 2
>   pure-power ties only via B2's P2). A mixed side with a deg-2
>   irreducible factor (l·deg r = 4) was NOT machine-exercised —
>   S1's proof does not distinguish, but the record says so.”

**FORM:** bulleted scope row.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** **“S1's proof does not distinguish, but the record says so”** — the honest separation of proof generality from machine coverage, stated in one sentence.

**RESOLUTION TRACE:** lines 629–633.

**TEETH:** `signed coverage disclosure`.

---

### EFF.GENTOW6.16  [fence]

**CANONICAL STATEMENT:** the GAUGE AXIS row, verbatim:

> “[GT6-r1 GAUGE AXIS (PE1 F-3): every frame in all three legs sits
>   at f1 = 1 AND eta = 1 — verified this round against the family
>   constructors: PHI1 ∈ {x²−2 (A5/A7/B/C, Q2), x³−2 (FAM-E/GEN-R2X,
>   Q2), x²−3 (FAM-P5 geography/GEN-P3/GEN-P3b/FAM-D, Q3)}, each a
>   binomial whose stage residue x^{e1}/pi is ≡ 1 — so the sealed
>   battery is structurally blind to the gauge seam (corrected =
>   naive at every row) and to every f1 >= 2 phenomenon (S6.1,
>   GENTOW6-BOX-4). Exposed but unexercised faces at eta != 1:
>   S1 Step I's monicity/normalizer clauses and the S3 digit VALUES
>   (the tie itself is scalar-robust — Step III's UFD match absorbs
>   units into gamma*). No sealed sentence is falsified by this row;
>   the fence was under-disclosed.]”

**FORM:** dated coverage rider.

**DERIVATION:** `[RECORD]`, verified against the family constructors at the round.

**CONDITIONALITY:** SUPERSESSION KIND: **inventory completion**. **The row does three things no other gauge disclosure in this queue does**: it verifies η = 1 from the constructors' *binomial shape*, it names WHICH faces are exposed (S1 Step I's clauses; the S3 digit values), and it identifies the one face that is **scalar-robust** (the tie). **Cross-entry: this is the fifth note in the queue to add an η-axis disclosure**, all downstream of one erratum.

**RESOLUTION TRACE:** lines 634–645.

**TEETH:** `signed coverage disclosure`. **ARITHMETIC AUDIT: each named `Phi'` is a binomial `x^{e1} − c`, so `x^{e1}/π ≡ c/π`-shaped with stage residue 1 under the stated normalizations** — the constructor-level argument is checkable from the shapes given ✓.

---

### EFF.GENTOW6.17  [record]

**CANONICAL STATEMENT:**

> “**Record sentences (no universal quantifiers).** The battery's
> 401 checks passed with 0 violations on the constructed rows named
> in S4; the tie held on each measured class row including the
> mu2* = 3 and transposed-profile rows; the seven certificate rows
> attained THETA exactly at the predicted slot and valuation; the
> two regime-4 self-shadows vanished identically; the E-ENTRY row's
> shadow read flipped while PARI confirmed the honest sigma.”

**FORM:** bold-headed record paragraph.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** **the heading is the discipline**: “no universal quantifiers”. Every sentence is scoped to measured rows. **This is the cleanest instance in the queue of a note refusing to let its battery speak generally.**

**RESOLUTION TRACE:** lines 667–673.

**TEETH:** disposition `signed evidence-grade record`; the 401/0 figure re-verified ✓.

---

### EFF.GENTOW6.18  [acceptance-box]

**CANONICAL STATEMENT:**

> “**GRADE: 0/2 (attempt).** Hostile arc owed (PE1+). The S1 cite leg
> wants a PDF-provenance re-verification in its own pass (GENTOW2's
> pattern); S3's proof of (a)/(b) is new combinatorial prose —
> the natural PE1 targets are the Y-monotonicity step ("every
> division/recarry step lowers Y-exponent by >= e2") and the
> secondary-extraction clause of (a).”

**FORM:** bold-headed grade line.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** superseded to **ACCEPTED 2/2** (`.32`). **The line PREDICTS its own attack surface** — and PE1's CRITICAL landed adjacent to it (the S3 scope quantifier rather than the two named steps). Recorded because self-predicted attack surfaces are rare and this one was half-right.

**RESOLUTION TRACE:** lines 675–681.

**TEETH:** the predicted targets are S3's (a)/(b); the CRITICAL hit S3's head.

---

### EFF.GENTOW6.19  [changes-record]  *(CHAIN-SCOPE — TERMINAL)*

**CANONICAL STATEMENT:** §S6.1's scope pin and its annex, `[ASSEMBLED]` from lines 702–850. The section head:

> “### S6.1 THE DATED GAUGE ANNEX + THE f1 = 1 SCOPE PIN (F-1) +
> ### ANNEX-THEOREM GENTOW-6.3'”

**FORM:** numbered repair section.

**DERIVATION:** the gauge annex (the corrected `chat_t`, as in the four sibling notes) plus a **scope pin** on the theorem head plus a **re-derivation** of the f1-general form as a separate annex theorem.

**CONDITIONALITY:** **the repair shape is the important part**: rather than weaken the theorem silently, r1 (i) pins the sealed theorem to f1 = 1, and (ii) states the f1-general claim as a NEW, separately-graded object (ANNEX-6.3′) whose machine status is a NEW box (BOX-4). **That is a textbook honest response to a scope CRITICAL** and is the pattern the queue should record.

**RESOLUTION TRACE:** lines 702–850; the pin's targets at 70 (STATUS TABLE) and 344–349 (S3 header).

**TEETH:** the pin is “verified at source” by PE2 (`.03`), which also measured the **sealed-(c) boundary break exactly where the pin predicts** (`.32`) — the strongest possible confirmation of a scope pin: the excluded region was measured and it does break.

---

### EFF.GENTOW6.20  [theorem]

**CANONICAL STATEMENT:** **ANNEX-THEOREM GENTOW-6.3′** (§S6.1) — the f1-general re-derivation of the attainment certificate.

**FORM:** blockquoted annex theorem.

**DERIVATION:** re-derived at r1 for general f1 at the corrected key.

**CONDITIONALITY:** **prose-only at r1** — “machine-unexercised = GENTOW6-BOX-4”, and the S5 box says “until it runs, 6.3' is prose-only.” **That fence is LIFTED at μ₂ = 2 by the SUB-BOX append** (`.35`): “ANNEX-THEOREM GENTOW-6.3' (S6.1) thereby loses its "machine-unexercised at f1 >= 2 / prose-only" fence at mu2 = 2; its derivation keeps the prose grade of this note's arc.” SUPERSESSION KIND: **license**, scoped to μ₂ = 2, with the derivation's grade unchanged.

**RESOLUTION TRACE:** §S6.1; the box at 646–653; the discharge at 1249–1264.

**TEETH:** PE2's LEG A measured 6.3′(b)'s certificate display exact at two f1 = 2 / η ≠ 1 frames (`.33`), and the box-4 row later ran it at battery grade on four x-ful rows (`.45`).

---

### EFF.GENTOW6.21  [lemma]  *(CHAIN-CAP — TERMINAL)*

**CANONICAL STATEMENT:** **LEMMA GENTOW-6.4, the 𝒯-FREE CAP LEMMA** (§S6.2, lines 851–1034), proved prove-first in answer to PE1's GAP F-2 (“GENTOW-5(a) consumed at f off its proved 𝒯 scope”).

**FORM:** numbered repair section carrying a new lemma + a machine leg.

**DERIVATION:** a fresh derivation of the cap behaviour without the 𝒯 hypothesis, so that COR 6.2C(d)'s consumption is legitimate at partial-side `f`.

**CONDITIONALITY:** **prove-first, not fence-first** — the GAP was answered by proving the needed statement rather than by narrowing the consumer. **Clause (a)'s per-window bijection codomain is then CORRECTED by PE2-F-2** (`.42`).

**RESOLUTION TRACE:** §S6.2, lines 851–1034; the codomain correction in the post-PE2 fold.

**TEETH:** **machine-verified at three frames incl. two partial-side members**; the instrument's verdict is re-read: `teeth: T-CAP fired x3, T-TRI fired x28` / `=== VERDICT: GREEN -- 303 checks, 0 violations ===` ✓, md5 `bf953a8f…` matching the S6.4 ledger ✓.

---

### EFF.GENTOW6.22  [instrument-record]

**CANONICAL STATEMENT:** the S6.4 md5 ledger, verbatim:

> “Sealed machine artifacts BYTE-FROZEN, md5s re-verified this round:
> gentow6_checks.py 121e5c4a95a19692db7910b9ffedfd6e ·
> gentow6_checks_output.txt 1bcb950618de39f26fe8e0c298abb783 ·
> gentow6_pe1_fresh.py 59fe76cefd36e4ffc8688adfd8e5f01e ·
> gentow6_pe1_fresh_output.txt 9de61325ef9b07dd2f52954d9d3e3f12.
> New r1 artifacts (this round): gentow6_r1_caplemma.py md5
> bf953a8ff0ef925d5be711fbbe60a418 (sealed 640b31ff at 14329683;
> post-run-1 instrument repair disclosed in S6.2 and in the RED
> commit e803c21d) · gentow6_r1_caplemma_output.txt md5
> fa9d7bf8cf2975539df63c4c166031d7 (GREEN 303/0, exit 0, isolated
> re-run bit-identical) · gentow6_r1_caplemma_output_run1_RED.txt
> (the kept RED artifact).”

with the ten-site edit manifest and its reading rule: “All sealed statement displays byte-untouched; riders GOVERN their quoted displays.”

**FORM:** numbered manifest + md5 ledger.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** none.

**RESOLUTION TRACE:** lines 1061–1086.

**TEETH:** **ALL SIX md5s in this ledger MATCH at compile time** ✓ (`121e5c4a`, `1bcb9506`, `59fe76ce`, `9de61325`, `bf953a8f`, `fa9d7bf8`), and the RED artifact exists. **PIN FINDING: of the three commit tokens cited in one clause — “sealed 640b31ff at 14329683 … the RED commit e803c21d” — `14329683` and `e803c21d` resolve, but `640b31ff` does NOT** (`git cat-file -t 640b31ff` → “Not a valid object name”). The seal is nonetheless independently confirmed: `14329683`'s commit message reads “GENTOW6 r1 (commit 2/3, SEAL): fresh instrument gentow6_r1_caplemma.py sealed with preregistered P-A..P-E + teeth T-TRI/T-CAP …”. **So the seal event is real and pinned; one token in the sentence is not resolvable.** OPEN-CALL 4.

---
### EFF.GENTOW6.23  [fence]  *(SERIES BOXES; ledger HYP.67)* — QUEUE-CHARGED

**CANONICAL STATEMENT — reproduced EXACTLY as left:**

> “* **GENTOW6-BOX-1 (mu2 >= 3 attainment).** THEOREM 6.3 is scoped
>   to mu2 = 2. At mu2 >= 3 the source layer has k-fold products
>   (multinomial coefficients != 1 off the diagonal appear at the
>   top pair only through (t*,...,t*), coefficient 1 again — the
>   diagonal argument LOOKS uniform) but the Y-bookkeeping of (a)
>   needs the k-extraction ledger re-derived; not claimed.”

**FORM:** bulleted box.

**DERIVATION:** `[RECORD]` of a non-claim, with the tempting-but-unverified diagonal argument named (“LOOKS uniform”).

**CONDITIONALITY:** **AMENDED by the SUB-BOX append to “CLOSED AT f1 = 1”** (`.34`) — and the append also **REFUTES this box's own parenthetical**: “This box's own parenthetical ("(t*,...,t*), coefficient 1 again") is REFUTED: the top branch is the diagonal PAIR with coefficient binom(mu2,2)”. SUPERSESSION KIND: **replacement** (of the parenthetical) + **license** (the closure at f1 = 1, at another note's 0/2).

**LEDGER CROSS-REFERENCE — `spec/HYPOTHESIS_LEDGER.md` `HYP.67  \`GENTOW6-BOX-1\``:**

> “STATEMENT: Lower-coordinate classification at μ₂≥3, including f₁≥2 compound face, is incomplete.
> CONSUMED BY: **RE-HOMED off `[H-c]` (COR-WFA-08).** … it is GENTOW6’s own μ₂≥3 attainment-classification residue (the Y-bookkeeping k-extraction ledger). GENTOW6 L610–615 (re-verified at L610): “GENTOW6-BOX-1 (mu2 >= 3 attainment). THEOREM 6.3 is scoped to mu2 = 2 … the Y-bookkeeping of (a) needs the k-extraction ledger re-derived; not claimed.” … Real home: the GENTOW6/T4 attainment layer (note GENTOW6 L1201–1204 records BOX-1 **CLOSED AT f1 = 1**, leaving the f₁≥2 face).
> CLASS: hypothesis-box
> PROPOSED DISPOSITION: MATH — retained.”

**The ledger row is CURRENT**: it quotes the box verbatim from L610–615 AND records the L1201–1204 closure, “leaving the f₁≥2 face” ✓. **Both line ranges verify against this note** (the box at 609–615; the closure heading at 1199–1202) ✓.

**RESOLUTION TRACE:** lines 609–615; amendment 1204–1248; ledger `HYP.67`.

**TEETH:** `signed non-claim`, then a conditional closure. The residual after the append is named there: “NOT delivered: the j < j* classification at mu2 >= 3 … and the f1 >= 2 x mu2 >= 3 compound”.

---

### EFF.GENTOW6.24  [fence]  *(SERIES BOXES; ledger HYP.68)* — QUEUE-CHARGED

**CANONICAL STATEMENT — reproduced EXACTLY as left:**

> “* **GENTOW6-BOX-2 (the count LEDGER at partial sides).** From S2:
>   the product-measure count of the f-locus per window (pricing
>   (f_S, g) ↦ f_S·g) — open already at FULL sides (GENTOW-1's
>   count-law remark), not a partial-side novelty.”

**FORM:** bulleted box.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** **OPEN at HEAD; unchanged by every later append.** The box's own framing is the important part: the gap is **not a partial-side novelty** — it is open at full sides too.

**LEDGER CROSS-REFERENCE — `HYP.68  \`GENTOW6-BOX-2\``:**

> “STATEMENT: Product-measure count is open already at FULL sides; budget half is proved and only ledger/count half remains.
> CONSUMED BY: **RE-HOMED off `[H-c]` (COR-WFA-09).** Zero weld-layer content. GENTOW6 L616–619 (re-verified at L616, L618): “GENTOW6-BOX-2 (the count LEDGER at partial sides) … open already at FULL sides (GENTOW-1’s count-law remark)”. The consumer, if any, is **the tower count layer**, not `[H-c]` — it belongs beside HYP.144’s GENTOW4-BOX-2 open COUNT layer, which is the same μ≥3 count-layer obligation family as HYP.139 and HYP.142.
> CLASS: hypothesis-box
> PROPOSED DISPOSITION: MATH — retained; consumer re-homed, reachability as a tower-leg residue not settled by this audit.”

**The ledger's WHERE (L616–619) verifies against this note** ✓, and **the row places BOX-2 in a FOUR-MEMBER count-layer family** (HYP.139, HYP.142, HYP.144's GENTOW4-BOX-2, and this) — the same family `EFF.GENTOW4.37` records from the other side.

**RESOLUTION TRACE:** lines 616–619; ledger `HYP.68`.

**TEETH:** cross-entry: `EFF.GENTOW1.26`'s count-law remark is the “open already at FULL sides” referent, and this queue compiled it independently as a non-executed counting unit ✓ — **the box's framing checks out against its own cited source.**

---

### EFF.GENTOW6.25  [fence]  *(SERIES BOXES; ledger HYP.69)* — QUEUE-CHARGED

**CANONICAL STATEMENT — reproduced EXACTLY as left:**

> “* **GENTOW6-BOX-3 (regime-3 classification).** Which non-x-free,
>   no-top-overflow members diverge, and exact THETA-attainment via
>   entry drivers: displayed only (E-ENTRY witness; the pair-sum
>   digit display of the quadrichotomy clause (3)); no theorem.”

**FORM:** bulleted box.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** **OPEN at HEAD.** “displayed only … no theorem” is the sharpest form of a non-claim: a witness and a display exist, a theorem does not.

**LEDGER CROSS-REFERENCE — `HYP.69  \`GENTOW6-BOX-3\``:**

> “STATEMENT: Entry-driven/non-top-overflow cases lack exhaustive classification.
> CONSUMED BY: **RE-HOMED off “W12 value partition” (COR-WFA-10).** Zero weld-layer content; **W12 has no value partition consuming GENTOW6**, so the v3 attribution was a mis-attribution. Real home: GENTOW6’s own regime-3 (entry-driven / non-top-overflow) display. GENTOW6 L620–623 (re-verified at L620, L622): “GENTOW6-BOX-3 (regime-3 classification). Which non-x-free, no-top-overflow members diverge, and exact THETA-attainment via entry drivers: displayed only … no theorem.”
> CLASS: hypothesis-box
> PROPOSED DISPOSITION: MATH — retained; consumer re-homed, reachability not settled by this audit.”

**WHERE (L620–623) verifies** ✓. **Cross-entry: this box is the level-2 analogue of `EFF.GENTOW3.28`'s scope remark (2)** (entry-digit-driven overflows, displayed but with no witness battery) — the same gap genre at two levels, boxed in two notes.

**RESOLUTION TRACE:** lines 620–623; ledger `HYP.69`.

**TEETH:** the E-ENTRY witness is a battery row (`.17`: “the E-ENTRY row's shadow read flipped while PARI confirmed the honest sigma”) — a measured instance, not a classification.

---

### EFF.GENTOW6.26  [fence]

**CANONICAL STATEMENT:**

> “* **Cite conditionality (S1).** Step II rests on the FGMN local
>   print at verified layout lines through GENTOW2's dictionary +
>   shear (its faithfulness map, incl. the psi2 = R_2(phi_3) row);
>   the repo-side (MULT) leg is displayed as corroboration but the
>   cite carries the load. GENTOW2's own arc grade applies upstream.”

**FORM:** bulleted conditionality row.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** **this row is what makes LEMMA 6.1's box closure `cite-conditional`** (`.09`). Three stacked dependencies, all named: the external print, GENTOW2's faithfulness map, and GENTOW2's arc grade.

**RESOLUTION TRACE:** lines 624–629.

**TEETH:** the FGMN local print exists at the cited path; “numbering verified at GENTOW2's ledger” is the faithful-cite discipline. **Disposition `cite-conditional supply`** — and the grade-box's own PE1 forecast (`.18`) anticipated that “The S1 cite leg wants a PDF-provenance re-verification in its own pass”.

---

### EFF.GENTOW6.27  [fence]  *(SERIES BOXES; ledger HYP.70)* — QUEUE-CHARGED

**CANONICAL STATEMENT — reproduced EXACTLY as left, with its acceptance-fold dating bracket:**

> “* **GENTOW6-BOX-4 (the f1 >= 2 / eta != 1 machine leg).** [GT6-r1]
>   ANNEX-THEOREM GENTOW-6.3' (S6.1) and the corrected-lift readings
>   above have NO machine contact anywhere in this note's arc: no
>   f1 >= 2 frame, no eta != 1 frame, exists in any leg (sealed or
>   fresh). First x-ful-digit certificate row (an f1 >= 2 genre with
>   some c_t·eta^{W(t)} not in F_Q) = the box's ask; until it runs,
>   6.3' is prose-only.”

> “  [acceptance fold (post-PE3) 2026-08-10, F-1 — the "NO machine
>   contact" sentence above DATED: true at its [GT6-r1] writing,
>   stale at HEAD in the conservative direction. Since r1 the arc
>   has verifier-leg machine contact on both named axes, verified
>   at the committed instruments themselves: gentow6_pe2_fresh.py
>   (md5 061639c3, @ 55fb9074) LEG A runs the first f1 = 2 /
>   eta != 1 frames (Q2, Phi' = x⁴ + 2x² + 4: e1 = 2, f1 = 2,
>   eta = res(x²/2) with eta² + eta + 1 = 0, K = F_4; corrected
>   lifts for c_0 = eta and c_0 = 1 + eta; 6.3'(b')'s certificate
>   display exact at both, divergence at THETA_0 = 37 on the
>   floor), and gentow6_pe3_fresh.py (md5 0ef76baa) runs the first
>   eta != 1 / f1 = 1 gauge-live frames … The
>   sentence understates evidence; no conclusion rested on it.
>   GENTOW6-BOX-4 STAYS OPEN, its ask unchanged and still unrun:
>   a SEALED BATTERY-grade x-ful-digit certificate row — the
>   verifier legs above are hostile-arc instruments exercising the
>   6.3'(b') display, not rows of this note's sealed apparatus —
>   so 6.3' keeps its prose-only grading in the note's own terms.]”

**FORM:** bulleted box + a dated acceptance-fold bracket.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY — TWO layers, and the second is a MODEL of scope discipline.** The fold DATES the stale sentence, supplies the new evidence, and then **refuses to close the box**: the verifier legs are hostile-arc instruments, not sealed rows, so the ask is unchanged. **Then the SUB-BOX append DISCHARGES it** with an actual sealed row (`.35`). SUPERSESSION KIND: **as-of scoping**, then **replacement** (open → DISCHARGED).

**LEDGER CROSS-REFERENCE — `HYP.70  \`GENTOW6-BOX-4\` residue`:**

> “WHERE: T4 §S7 L533–535/L2105–2109; `GENTOW6_PROOF_2026-08-09.md` L1249–1255.
> STATEMENT: Sealed-row ask is DISCHARGED at HEAD, GREEN 265/0 at four x-ful rows including f₁=2; only theorem-grade all-f₁≥2 widening remains unsealed.
> CONSUMED BY: a widened theorem, not the discharged box.
> CLASS: theorem-strength-residue
> PROPOSED DISPOSITION: MATH — no OPEN-CALL; keep battery discharge distinct from widening.”

**The ledger row is CURRENT and reclassified**: it quotes the DISCHARGED state (not the open one), pins it to L1249–1255 ✓, and — uniquely among the four — changes the CLASS from `hypothesis-box` to **`theorem-strength-residue`**. **The ledger's own discipline (“keep battery discharge distinct from widening”) matches the note's** (“its derivation keeps the prose grade of this note's arc”).

**RESOLUTION TRACE:** lines 646–666; the discharge 1249–1264; ledger `HYP.70`.

**TEETH:** **both cited instrument md5s verified**: `gentow6_pe2_fresh.py` = `061639c3…` ✓, `gentow6_pe3_fresh.py` = `0ef76baa…` ✓; `git cat-file -t 55fb9074` = commit ✓.

---

### EFF.GENTOW6.28  [changes-record]

**CANONICAL STATEMENT:** §S6.3 — the minor repairs F-3/F-5/F-6 and the **F-4 closure record** (lines 1035–1060).

**FORM:** numbered repair section.

**DERIVATION:** `[RECORD]` of four minor dispositions, one of which (F-4) is a closure record rather than an edit.

**CONDITIONALITY:** F-3's disposition is the GAUGE AXIS row (`.16`); F-5(i) is the sources rider (`.04`); F-5(ii) is the S4 seal-pin rider; F-6 is COR 6.1C(c)'s re-fence.

**RESOLUTION TRACE:** lines 1035–1060; the manifest's site list at 1063–1070.

**TEETH:** the ten-site manifest (`.22`) accounts for every r1 edit including these.

---

### EFF.GENTOW6.29  [acceptance-box]  *(SERIES ARC, link 3)*

**CANONICAL STATEMENT:** §S6.5's fold record (lines 1087–1147) — the post-PE2 minors-only fold, whose two dispositions are `.30` and `.31`, and whose closing rule is “grade 1/2 (PE2's verdict; a rider is not a pass); PE3 = the acceptance attempt.”

**FORM:** numbered fold record.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** the counter is superseded to 2/2 (`.32`).

**RESOLUTION TRACE:** lines 1087–1147.

**TEETH:** “Sealed artifacts BYTE-FROZEN (six S6.4 md5s re-verified this fold)” — all six verified here ✓.

---

### EFF.GENTOW6.30  [changes-record]  *(CHAIN-SCOPE — TERMINAL)*

**CANONICAL STATEMENT:** PE2-F-1, verbatim from the ARC record:

> “PE2-F-1 the two stale summary sites
> (STATUS TABLE row (3); S3 section header) re-scoped to the
> r1 pin's f1 = 1 wording, mirroring the theorem-head scope pin
> verified at source”

**FORM:** folded minor, landed as riders at two sites.

**DERIVATION:** a consistency sweep: the theorem head was pinned at r1, but two SUMMARY sites still carried the old quantifier.

**CONDITIONALITY:** SUPERSESSION KIND: **scope-pin**, applied to summary surfaces. **TARGETS:** `.05` (STATUS TABLE row 3) and `.12` (the S3 section header). **This is the recurring corpus defect** — a scope pin landed at the statement but not at its summaries — and PE2 caught it here as HETOW's PE6 and GENTOW1's PE5 caught the analogous ones.

**RESOLUTION TRACE:** the riders at lines 70 and 344–349.

**TEETH:** **both rider sites verified present in the effective text** ✓ (`.05`, `.12` quote them).

---

### EFF.GENTOW6.31  [changes-record]  *(CHAIN-CODOMAIN — TERMINAL)*

**CANONICAL STATEMENT:** PE2-F-2, verbatim from the ARC record (quoted in full at `.03`). Its core:

> “LEMMA GENTOW-6.4(a)'s per-window bijection codomain CORRECTED by a dated bracket at the display —
> re-derived from (a)'s own triangular formula: above-top slots
> (slot degree > n) are FORCED to 0, the codomain is the tuples
> supported on slot degrees <= n with top pinned 1; exact condition
> for above-top slots = n mod D2 != D2 - 1 (sharper than the
> report's D2 ∤ n framing — FRAME-F, a D2 | n member, carries 3
> such slots); machine leg gentow6_rider_codomain.py GREEN 24/0
> with tooth T-COD x3; COR 6.2C(d)'s chain consumes only (a)'s
> triangularity + (b)'s band-1 congruence — walked, unchanged.”

**FORM:** folded minor, landed as a dated bracket at the display.

**DERIVATION:** re-derived from the lemma's own triangular formula.

**CONDITIONALITY:** SUPERSESSION KIND: **replacement** of a codomain. **The correction is SHARPER than the finding** (`n mod D2 != D2 − 1` vs `D2 ∤ n`) and comes with a counter-member (FRAME-F, `D2 | n`, three above-top slots). **The consumer walk found nothing narrows.**

**RESOLUTION TRACE:** lines 30–40; the bracket at the 6.4(a) display in §S6.2.

**TEETH:** **ARITHMETIC AUDIT of the sharper condition:** slot degrees run over a residue system mod `D2`; an above-top slot exists iff the largest slot degree `≤ n` leaves room, i.e. iff `n mod D2 != D2 − 1` ✓ — and at `D2 | n` (so `n mod D2 = 0 ≠ D2 − 1` for `D2 ≥ 2`) above-top slots DO exist, which is exactly the FRAME-F counter-member ✓. **The note's condition is correct and the report's was not.** `gentow6_rider_codomain.py` exists ✓.

---

### EFF.GENTOW6.32  [acceptance-box]  *(SERIES ARC, link 4; CHAIN-GRADE)*

**CANONICAL STATEMENT:**

> “**GENTOW6 IS ACCEPTED 2/2** — passPE2 CLEAN (0 CRITICAL + 0 GAP +
> 2 MINOR, GENTOW6_passPE2_report.md @ 161e8ce1; counter 0/2 -> 1/2)
> followed by passPE3 CLEAN (0 CRITICAL + 0 GAP + 1 MINOR,
> GENTOW6_passPE3_report.md @ ed5ed51f; counter 1/2 -> 2/2) …
> **THE NOTE IS FROZEN AT THIS RECORD …**”

with the arc's two decisive measurements, verbatim:

> “-> PE2 CLEAN 2m @ 161e8ce1 (first f1 = 2 /
> eta != 1 frames anywhere in this arc; the sealed-(c) boundary
> break predicted by the r1 pin measured from OUTSIDE) … -> PE3 CLEAN 1m @ ed5ed51f
> (fresh route GREEN 61/0, 6/6 teeth: the FIRST gauge-live tie
> anywhere — Q3, Phi' = x² − 6, eta = 2, R_coh(f) = (T+1)²(T²+1)
> exact at the hand-derived wrap, with T-GAUGE killing the
> eta-blind read — plus the rider codomain at above-top counts 1
> and 5 and the first eta != 1 certificate row at the
> (0, e2f2−1) corner, height 21 = THETA_0 exact; run-1 RED kept, a
> verifier-side prereg slip).”

and the freeze ledger (quoted in §1, F-1…F-3).

**FORM:** bold-headed acceptance record.

**DERIVATION:** `[RECORD]` of two consecutive CLEAN passes.

**CONDITIONALITY:** none on the counter.

**RESOLUTION TRACE:** lines 1148–1197.

**TEETH:** **“the sealed-(c) boundary break predicted by the r1 pin measured from OUTSIDE” is the strongest scope-pin confirmation in the queue** — the r1 CRITICAL repair predicted that the sealed display fails at f1 ≥ 2, and PE2 measured exactly that break at the first f1 = 2 frames. **All eight commits in the arc resolve** (`cd74ef6f`, `3b6bf5ce`, `161e8ce1`, `e7a6b8c2`, `1194b1a4`, `ed5ed51f`, `15965106`, and the seal pair `59ea785`/`5fb8fd7`) ✓. **The freeze ledger fully reconciles** (§1): `1,088 + 58 = 1,146` ✓, L314 prefix ✓, 1,146 ✓.

---

### EFF.GENTOW6.33  [record]

**CANONICAL STATEMENT:**

> “### Dated append (2026-08-10, GENTOW6 SUB-BOX unit; pure append on
> ### the frozen body, pre-append md5 157b16adc82f6de4c6a0f65f5f8ffe85,
> ### 1,197 lines): GENTOW6-BOX-1 CLOSED AT f1 = 1 + GENTOW6-BOX-4
> ### DISCHARGED”

**FORM:** three-line dated append heading that IS the record.

**DERIVATION:** `[RECORD]`

**CONDITIONALITY:** **the heading announces two box-state changes**, which is why the SERIES BOXES double emission is necessary.

**RESOLUTION TRACE:** lines 1199–1202.

**TEETH:** **FREEZE F-4 verified: lines 1–1197 hash to `157b16ad…`** ✓ — both the md5 and the 1,197-line count.

---

### EFF.GENTOW6.34  [changes-record]  *(SERIES BOXES — BOX-1 amended state; CHAIN-BOX1 — TERMINAL)* — QUEUE-CHARGED

**CANONICAL STATEMENT — the amended BOX-1 state, reproduced EXACTLY as left (abridged only inside the r1 brackets, which are quoted separately below):**

> “**GENTOW6-BOX-1 (mu2 >= 3 attainment): CLOSED AT f1 = 1** by
> THEOREM GENTOW-6.5 (GENTOW6_BOXES_2026-08-10.md S2, at that note's
> 0/2 attempt grade, hostile arc owed there): the box's k-extraction
> ledger is clause (a) (the Y-ceiling B* + 1 = e2f2(mu2-2) + 2e2t*
> + 1 over every branch, with the unique equality case …), and the
> attainment claim is (b1) at the TOP coordinate j* = (mu2-2) +
> floor((2e2t*+1)/(e2f2)) — the height-THETA_{j*} graded digit at
> the certificate slot is binom(mu2,2)*chat_{t*}^2*pi^{2a_{t*}}*
> (wrap unit): attainment on the floor iff p does not divide
> binom(mu2,2), with (b2) the honest cancellation clause when
> p | binom(mu2,2).”

**The two r1 brackets from the sibling note's own repair round, verbatim** (these carry the operative corrections):

> “[r1 dated bracket, 2026-08-10, GENTOW6-BOXES r1 per passPE1 F-1 (3ae577e8):
> "unique" holds exactly when e2(f2 - t*) >= 2; at e2(f2 - t*) = 1
> the equality census also carries k >= 3 all-t* branches whenever
> k i_{t*} >= (k-1) D', each landing at x-slot < a* at weight >=
> mu2 E2 + 2 delta — the boxes note's S2/Step III carry the
> corrected census and the witness frame CE3]”

> “[r1 dated bracket, 2026-08-10, GENTOW6-BOXES r1
> per passPE1 F-2 (3ae577e8): the "iff" is WITHDRAWN — the theorem
> proves the IF direction only. (b2) cancels the graded digit at
> the CERTIFICATE slot and prices no other slot of coordinate j*,
> and the converse is FALSE, not merely unproved: at BP3 (Q3,
> Phi' = x^2 - 3, Phi2 = Phi'^2 - 3x Phi' - 27, m = 3,
> p = 3 | binom(3,2)) the certificate slot (0,1)@j2 = 27 cancels in
> grade (height 9 > THETA_2 = 7) yet slot (1,0)@j2 = 135 carries a
> graded floor digit 5, a 3-unit: pin = THETA_2, attainment ON the
> floor with p | binom(m,2) — while FR5X at m = 4, p = 2 fails
> attainment (pin 65 > THETA_2 = 61): the converse is
> genre-dependent both ways. Read: attainment on the floor IF
> p does not divide binom(mu2,2). Machine leg
> gentow6_boxes_pe1_fresh.py (md5 fae8bc99), re-run bit-identical
> at the r1 round; PROJECT_STATE #22 carries the same correction.]”

and the residual, verbatim:

> “NOT delivered: the j < j* classification at
> mu2 >= 3 (displayed genre arithmetic — BOX-3's geography one m
> up), and the f1 >= 2 x mu2 >= 3 compound (ANNEX-REMARK 6.5'
> there, prose only).”

**FORM:** bold-headed box-state amendment with two nested dated brackets from a sibling note's repair round.

**DERIVATION:** `[IMPORTED]` from `GENTOW6_BOXES_2026-08-10.md` S2's THEOREM GENTOW-6.5, **at that note's 0/2 attempt grade**.

**CONDITIONALITY — THREE stacked, all disclosed:**
1. **The closure is at f1 = 1 only**, and rides a note at **0/2 with a hostile arc owed there**. So GENTOW6 (2/2) closes its own box by importing a 0/2 result — the grade does not propagate upward.
2. **The “iff” is WITHDRAWN**: only the IF direction is proved, and **the converse is FALSE with a displayed witness (BP3)**, plus a contrasting failure (FR5X at m = 4, p = 2). SUPERSESSION KIND: **replacement**.
3. **Two faces are NOT delivered**: the `j < j*` classification at μ₂ ≥ 3, and the f1 ≥ 2 × μ₂ ≥ 3 compound.
**Additionally, this append REFUTES the original box's parenthetical** (`.23`).

**RESOLUTION TRACE:** lines 1204–1248; the original box at 609–615; ledger `HYP.67`'s “leaving the f₁≥2 face”.

**TEETH:** **`gentow6_boxes_pe1_fresh.py` md5 = `fae8bc99…` ✓** (matches). `git cat-file -t 3ae577e8` = commit ✓; `2ae94ded` (seal) and `a646c7c9` (verdict) both resolve ✓; the boxes-note machine leg is “sealed GREEN 459/0 (4 preregistered teeth at exact kill counts; run-1 RED kept + disclosed)”. **ARITHMETIC AUDIT of the BP3 witness:** at `m = 3`, `binom(3,2) = 3` and `p = 3 | 3` ✓ (so (b2)'s cancellation applies), yet attainment holds via a different slot — **the converse-falsifying configuration is arithmetically consistent as displayed**. At FR5X, `m = 4`, `binom(4,2) = 6`, `p = 2 | 6` ✓ and attainment FAILS (pin 65 > Θ₂ = 61) — **the two witnesses together show genre-dependence in both directions**, exactly as claimed.

---

### EFF.GENTOW6.35  [changes-record]  *(SERIES BOXES — BOX-4 amended state; CHAIN-BOX4 — TERMINAL)* — QUEUE-CHARGED

**CANONICAL STATEMENT — the amended BOX-4 state, reproduced EXACTLY as left:**

> “**GENTOW6-BOX-4 (the f1 >= 2 x-ful-digit sealed battery row):
> DISCHARGED.** The box's ask — a SEALED BATTERY-grade x-ful-digit
> certificate row, which the PE2/PE3 verifier legs were explicitly
> not — has run: gentow6_box4_row.py (two-commit seal 90b23648,
> preregistered wants + 3 teeth at exact kill counts, dual R/E
> routes + PARI, 40-member perturbation sweep; run-1 RED = one gp
> format literal, kept + disclosed) GREEN 265/0 at FOUR x-ful rows:
> PE2's two frames at battery grade plus the NEW u2 = 11 pair
> (Phi2 = Phi'^2 - 16x^3 and Phi'^2 - 16x^3 - 32x, c_0 = eta and
> 1 + eta at Q2, Phi' = x^4 + 2x^2 + 4), certificate digits
> 64/64/256/256 at v = 2(a_0 - r*h), pins = THETA_0 = 37/37/47/47
> exactly on the floor, ShC_1 empty at each row, and the
> f1 = 1-evaluation mutant killed at each row. ANNEX-THEOREM
> GENTOW-6.3' (S6.1) thereby loses its "machine-unexercised at
> f1 >= 2 / prose-only" fence at mu2 = 2; its derivation keeps the
> prose grade of this note's arc. Artifacts: runner md5 9d6e16cc,
> output md5 2d8d617a, verdict commit follows this append.”

**FORM:** bold-headed box discharge.

**DERIVATION:** `[RUN]` at sealed-battery grade — precisely the ask the acceptance fold refused to accept verifier legs for (`.27`).

**CONDITIONALITY:** SUPERSESSION KIND: **replacement** (open → DISCHARGED), **scoped to μ₂ = 2**: “loses its … fence at mu2 = 2”. **The derivation's GRADE is explicitly unchanged**: “its derivation keeps the prose grade of this note's arc.” **So the discharge is a MACHINE discharge, not a proof upgrade** — the distinction the ledger row `HYP.70` also insists on (“keep battery discharge distinct from widening”).

**RESOLUTION TRACE:** lines 1249–1264 (the note's final lines); the pre-discharge state at 646–666; ledger `HYP.70`.

**TEETH:** **BOTH ARTIFACT md5s VERIFIED**: `gentow6_box4_row.py` = `9d6e16cc…` ✓ and `gentow6_box4_row_output.txt` = `2d8d617a…` ✓; the output's tail reads `teeth kills: [('T-F1EVAL', 4), ('T-SLOT', 4), ('T-VAL2', 4)]` / `RESULT: 265 checks, 0 violations, teeth OK` ✓ — **the 265/0 figure and the three teeth at exact kill counts (4 each, one per x-ful row) reproduce**, and `T-F1EVAL`'s four kills are exactly the “f1 = 1-evaluation mutant killed at each row” claim ✓. `git cat-file -t 90b23648` = commit ✓; the kept RED artifact exists ✓. **ARITHMETIC AUDIT: four rows, four kills per tooth = one kill per row per tooth** ✓; the certificate digits `64/64/256/256` and pins `37/37/47/47` pair the two PE2 frames (37) with the new u2 = 11 pair (47) ✓ — consistent with `EFF.GENTOW6.27`'s PE2 frame at `THETA_0 = 37`.

---

## 3. XREF verification ledger

### 3.1 Designation XREFs

| ID | Target | Check | count | Role |
|---|---|---|---:|---|
| X01 | `spec/HYPOTHESIS_LEDGER.md` | `HYP.67  \`GENTOW6-BOX-1\`` | 1 | ledger home; WHERE L610–615 **and** the L1201–1204 closure — **both verified** |
| X02 | same | `HYP.68  \`GENTOW6-BOX-2\`` | 1 | WHERE L616–619 ✓; places BOX-2 in the four-row count-layer family |
| X03 | same | `HYP.69  \`GENTOW6-BOX-3\`` | 1 | WHERE L620–623 ✓; consumer re-homed off a mis-attribution |
| X04 | same | `HYP.70  \`GENTOW6-BOX-4\` residue` | 1 | WHERE L1249–1255 ✓; **class reclassified to `theorem-strength-residue`, state DISCHARGED** |
| X05 | same | `HYP.144  \`GENTOW4-BOX-1/-2/-4\`` | 1 | the box this note closes — **still states BOX-1 open** (OPEN-CALL 1) |
| X06 | `GENTOW6_BOXES_2026-08-10.md` | THEOREM GENTOW-6.5, S2 | — | the closure's source, at 0/2 |
| X07 | `docs/references/fgmn_residual_ideals_1305.0775v3_layout.txt` | the FGMN local print | — | the S1 cite's external source |
| X08 | `GENTOW4_PROOF_2026-08-09.md`, `GENTOW3_PROOF_2026-08-09.md`, `GENTOW1_PROOF_2026-08-09.md`, `GENTOW2_PROOF_2026-08-09.md`, `HE7_PROOF`, `HE6_PROOF`, `HE6R1_RECON` | all exist | — | the seven consumed notes |

### 3.2 Commit and artifact pins (v3 rule 23) — **eleven artifact md5s, all matched; one commit token unresolvable**

| ID | Pin | Verified |
|---|---|---|
| P-01 | commits `cd74ef6f`, `161e8ce1`, `ed5ed51f`, `3b6bf5ce`, `e7a6b8c2`, `1194b1a4`, `15965106`, `59ea785`, `5fb8fd7`, `55fb9074`, `3ae577e8`, `2ae94ded`, `a646c7c9`, `90b23648`, `e803c21d`, `14329683`, `f0874ff9` | **ALL SEVENTEEN resolve** |
| P-02 | **`640b31ff`** (cited as “sealed 640b31ff at 14329683”) | **DOES NOT RESOLVE** — `git cat-file -t` → “Not a valid object name”. The seal event is independently confirmed by `14329683`'s commit message. **OPEN-CALL 4** |
| P-03 | `gentow6_checks.py` `121e5c4a…`, output `1bcb9506…` | BOTH MATCH; `GREEN -- 401 checks, 0 violations` |
| P-04 | `gentow6_pe1_fresh.py` `59fe76ce…`, output `9de61325…` | BOTH MATCH |
| P-05 | `gentow6_r1_caplemma.py` `bf953a8f…`, output `fa9d7bf8…` | BOTH MATCH; `GREEN -- 303 checks, 0 violations`, teeth `T-CAP x3, T-TRI x28` |
| P-06 | `gentow6_pe2_fresh.py` `061639c3…` | MATCHES |
| P-07 | `gentow6_pe3_fresh.py` `0ef76baa…` | MATCHES |
| P-08 | `gentow6_boxes_pe1_fresh.py` `fae8bc99…` | MATCHES |
| P-09 | `gentow6_box4_row.py` `9d6e16cc…`, output `2d8d617a…` | BOTH MATCH; `RESULT: 265 checks, 0 violations, teeth OK` |
| P-10 | `gentow6_rider_codomain.py` | EXISTS |
| P-11 | the four text freezes | **ALL RECONCILED** (§1) |
| P-12 | imported legs `gentow4_checks.py` `98cdd4a4…`, `gentow3_checks.py` `7cf3a7b8…` | BOTH MATCH — **same artifacts verified for GENTOW4/GENTOW3 in this queue** |

### 3.3 Verified NEGATIVE edge

| ID | Check | count | Meaning |
|---|---|---:|---|
| N1 | `grep -cF 'GENTOW-6' GENTOW4_PROOF_2026-08-09.md` | **0** | GENTOW4's BOX-1 text is unamended; the closure lives only here (OPEN-CALL 1) |

---

## 4. Effective supply / consumption ledger

| Obligation | Effective supply | Surviving fence | Protocol status |
|---|---|---|---|
| GENTOW4-BOX-1 (mixed-side tie) | `.07` (LEMMA 6.1) | **cite-conditional** (`.26`): FGMN print + GENTOW2's map + GENTOW2's arc grade | **CLOSED here**; **UNLANDED on GENTOW4** (N1), ledger `HYP.144` still open |
| GENTOW4-BOX-2 (block budgets) | `.11` (LEMMA 6.2 + COR 6.2C) | budget half only; **ledger/count half = BOX-2** | **HALF-DELIVERED**; the count half joins a four-row family |
| GENTOW3-BOX-1 (attainment certificate) | `.12` (THEOREM 6.3) | **μ₂ = 2, f1 = 1** after the r1 pin; f1-general = ANNEX-6.3′ | **SCOPED, not genre-general** — the charge's own alternative (“or honestly scoped”) |
| COR 6.2C(d)'s cap consumption | `.21` (LEMMA 6.4) | codomain corrected (`.31`) | **REPAIRED prove-first** |
| ANNEX-6.3′ at f1 ≥ 2 | `.20` | prose grade unchanged | **machine fence LIFTED at μ₂ = 2** by the box-4 row (`.35`) |
| GENTOW6-BOX-1 (own, μ₂ ≥ 3) | `.34` | **f1 = 1 only, at a 0/2 note; “iff” withdrawn** | **CLOSED AT f1 = 1**, conditionally |
| GENTOW6-BOX-2/-3 | not supplied | `.24`, `.25` | **OPEN** |

---

## 5. NON-IMPORTS

**NI-1 — no consumed note is edited.** `.04`; verified (N1).
**NI-2 — the repo-side (MULT) leg is corroboration, not load.** `.26`: “the cite carries the load”.
**NI-3 — μ₂ ≥ 3 attainment is “not claimed” at composition.** `.23`.
**NI-4 — the count LEDGER is not supplied.** `.24`.
**NI-5 — regime 3 is “displayed only … no theorem”.** `.25`.
**NI-6 — no universal quantifiers in the record sentences.** `.17`.
**NI-7 — at f1 ≥ 2 the sealed displays are NOT proved as written.** `.12`.
**NI-8 — verifier legs are not sealed rows.** `.27`: the box stays open despite new machine contact, because the ask names sealed-battery grade.
**NI-9 — the box-4 discharge is a MACHINE discharge, not a proof upgrade.** `.35`: “its derivation keeps the prose grade”.
**NI-10 — the BOX-1 closure does not upgrade its source's grade.** `.34`: “at that note's 0/2 attempt grade, hostile arc owed there”.

Accordingly **no edge is created** from this note to a genre-general attainment theorem at f1 ≥ 2, to μ₂ ≥ 3 lower-coordinate classification, to a partial-side count law, to a regime-3 classification, or to any upgrade of the GENTOW6-BOXES note's grade.

---

## 6. TEETH inverse inventory

### 6.1 Obligation → guarded units

| # | Row | Guarded units | Disposition |
|---:|---|---|---|
| 1 | the sealed battery (401/0, 4/4 teeth) | `.07`, `.11`, `.12`, `.14` | executable regression; **all rows at f1 = 1 AND η = 1** (`.16`) |
| 2 | the tie on measured class rows (incl. μ₂* = 3, transposed) | `.07`, `.17` | executable regression, no universal quantifier |
| 3 | the seven certificate rows at Θ | `.12`, `.17` | executable regression |
| 4 | the two regime-4 self-shadows | `.17` | executable regression (vanish identically) |
| 5 | the E-ENTRY row | `.17`, `.25` | executable regression — **a measured instance, not a classification** |
| 6 | the r1 cap-lemma leg (303/0, T-CAP ×3, T-TRI ×28) | `.21` | executable regression at three frames incl. two partial-side members |
| 7 | the rider codomain leg (24/0, T-COD ×3) | `.31` | executable regression |
| 8 | PE2's LEG A (first f1 = 2 / η ≠ 1 frames) | `.27`, `.32` | **AUDIT / decorrelated frame** — measured the predicted sealed-(c) break |
| 9 | PE3's fresh route (61/0, 6/6 teeth; first gauge-live tie) | `.32` | **AUDIT / decorrelated frame**, with T-GAUGE killing the η-blind read |
| 10 | the boxes-note leg (459/0, 4 teeth) | `.34` | **IMPORTED run-record at 0/2** |
| 11 | the box-4 sealed row (265/0, 3 teeth ×4 kills) | `.35` | executable regression at **sealed-battery grade** — the discharge |
| 12 | the S1 cite | `.09`, `.26` | **cite-conditional**, three stacked dependencies |
| 13 | GENTOW6-BOX-1 | `.23`, `.34` | **CLOSED AT f1 = 1, conditionally** (HYP.67) |
| 14 | GENTOW6-BOX-2 | `.24` | **OPEN** (HYP.68) |
| 15 | GENTOW6-BOX-3 | `.25` | **OPEN** (HYP.69) |
| 16 | GENTOW6-BOX-4 | `.27`, `.35` | **DISCHARGED** (HYP.70) |
| 17 | the machine-scope + gauge-axis fences | `.15`, `.16` | **signed coverage disclosures** |

**Zero-orphan check: 17 obligations, all mapped, 0 orphans.**

### 6.2 Arithmetic audit summary (v3 rule 22)

- **401/0** ✓, **303/0** with `T-CAP ×3, T-TRI ×28` ✓, **265/0** with three teeth at 4 kills each ✓, **24/0** ✓ (all re-read from artifacts)
- **four rows × one kill per tooth = 4 kills** ✓ (`.35`)
- **the codomain condition** `n mod D2 != D2 − 1` derived and the `D2 | n` counter-member checked ✓ (`.31`)
- **BP3**: `binom(3,2) = 3`, `p = 3 | 3`, attainment nonetheless ✓; **FR5X**: `binom(4,2) = 6`, `p = 2 | 6`, attainment fails ✓ — both directions consistent (`.34`)
- **the certificate rows**: digits `64/64/256/256`, pins `37/37/47/47`, pairing the two PE2 frames with the u2 = 11 pair ✓
- **the freezes**: `1,088 + 58 = 1,146` ✓, L314 ✓, 1,146 ✓, 1,197 ✓
- **eleven artifact md5s** ✓; **seventeen commits** ✓; **one commit token unresolvable** (P-02)

**Zero arithmetic discrepancies found.**

---

## 7. BOUNDARY SELF-AUDIT

### 7.1 The rule applied

Zero `\tag{`; smallest cuttable span. **Boxes are units, and box STATES are units** — the entry's defining choice.

### 7.2 Boundaries drawn, arguable ones both ways

1. **The four boxes → four units at their composed state (`.23`–`.27`) PLUS two units at their amended states (`.34`, `.35`).** *Alternative:* four units, each carrying its amendment in CONDITIONALITY. *Chosen* because **the ledger rows quote the boxes at DIFFERENT states** — `HYP.67` quotes the composed text and notes the closure; `HYP.70` quotes only the DISCHARGED state and reclassifies the row's CLASS. A single-unit-per-box emission would force one state to be primary and the other subordinate, and a chapter cut would silently pick one. **Double emission with explicit SERIES membership is the only way both are cuttable.** *Cost:* two box IDs appear twice in the inventory; the SERIES declaration in §1 says so.
2. **The box units emitted after their §S5 neighbours (`.15`–`.18`) rather than strictly interleaved.** *Chosen* to keep SERIES BOXES contiguous; the RESOLUTION TRACE of each box gives its true source lines (609–653), so source order is recoverable.
3. **`.09` (the cite step) and `.26` (the cite conditionality) kept apart.** *Chosen* because the conditionality is a §S5 record row with its own downstream force (it is what makes the closure conditional), while `.09` is a proof step.
4. **`.19` (the scope pin) and `.20` (ANNEX-6.3′) split.** *Chosen* because the pin is a supersession and the annex is a new theorem with its own grade and its own box.
5. **`.34` kept whole rather than split into closure / bracket-1 / bracket-2.** *Arguable* — the two r1 brackets are a sibling note's corrections. *Chosen* because the closure is unintelligible without them: the “iff” it states is withdrawn by one of them.

### 7.3 Merges deliberately NOT made

- `.23`/`.34` and `.27`/`.35` kept as separate units (the double emission), per §7.2 item 1.
- `.30`/`.31` kept apart: two minors with different KINDs (scope-pin vs replacement) and different targets.
- `.15`/`.16` kept apart: machine scope is a geography row; the gauge axis is a dated F-3 disposition.

### 7.4 Rules that fought this genre

- **Rule 28 (SERIES) is what makes the box double emission legible**; without a declared series the two states would look like duplicate units.
- **Rule 23 found the entry's one defect**: an unresolvable commit token inside an otherwise perfect eleven-md5 ledger.
- **Rule 18 (KIND) carried the BOX-4 arc**: `as-of scoping` (the dating) then `replacement` (the discharge) — two acts that a flat supersession field would merge.
- **Rule 22** confirmed the sharper codomain condition and both BP3/FR5X witnesses.

---

## 8. Deferred reconciliation obligations

1. **The GENTOW4-BOX-1 closure is unlanded on GENTOW4 and on `HYP.144` — FOR-0c/ledger.** This note IS the faithful-cite unit `HYP.144`'s disposition calls for, and both GENTOW4 (N1) and the ledger row still carry the box as open.
2. **The BOX-1 closure rides a 0/2 note — FOR-0c.** `GENTOW6_BOXES_2026-08-10.md` has a hostile arc owed; its own r1 already corrected two of the closure's clauses.
3. **`640b31ff` does not resolve — FOR-0b.** One token in the S6.4 seal citation; the seal itself is confirmed by `14329683`.
4. **`HYP.67`'s STATEMENT line and the note's amended state differ in emphasis — FOR-0d/ledger.** The ledger's STATEMENT reads “Lower-coordinate classification at μ₂≥3, including f₁≥2 compound face, is incomplete”, which matches the SUB-BOX append's “NOT delivered” list better than it matches the composed box text it quotes. Not an error; worth a one-line refresh.

---

## 9. Self-audit and OPEN-CALLS

### 9.1 Resolved compilation calls

- **All four boxes reproduced EXACTLY as left, at BOTH states where they changed**, each with its `HYP.67`–`HYP.70` row quoted and every ledger WHERE line-range verified against the note.
- Four text freezes reconciled (one via `1,088 + 58 = 1,146`); **eleven artifact md5s matched**; seventeen commits resolve; **one token found unresolvable and reported rather than smoothed**.
- Six CHAINs with TERMINALs, including two whose terminal is a box-state change.
- The cite leg is compiled as `cite-conditional` with all three stacked dependencies named, so LEMMA 6.1's closure is never presented as self-contained.
- The r1 CRITICAL's repair shape (pin the sealed theorem, restate the general claim as a separately-graded annex, open a new machine box) is recorded as the queue's model response to a scope CRITICAL — and PE2's measurement of the predicted break confirms it.

### 9.2 OPEN-CALLS

**OPEN-CALL 1 — the GENTOW4-BOX-1 closure is unlanded, and the ledger row disagrees with this note. FOR-0c/ledger — NEEDS ASVIN.**
`.07`/`.05` record “GENTOW4-BOX-1 closed”; `HYP.144` still states it “UNPROVED here … a faithful-cite unit in GENTOW2's pattern would retire it”, and GENTOW4 itself carries no reference to this note (N1). **This note is the unit that disposition asks for.** Three actions are implied and none is a compiler decision: land a dated append on GENTOW4, refresh `HYP.144`, and decide whether a cite-conditional closure at another note's 0/2 counts as retirement.

**OPEN-CALL 2 — the BOX-1 closure's grade chain crosses 0/2. FOR-0c.**
`.34` imports THEOREM GENTOW-6.5 “at that note's 0/2 attempt grade, hostile arc owed there”, and that note's own r1 has already withdrawn an “iff” inside the imported statement. **A consumer of “GENTOW6-BOX-1 CLOSED AT f1 = 1” inherits a 0/2 dependency**, and this compilation does not grade other notes.

**OPEN-CALL 3 — is a machine discharge a box closure? FOR-0d.**
`.35` DISCHARGES BOX-4 by running the sealed row the box asked for, while stating that “its derivation keeps the prose grade of this note's arc”, and `HYP.70` reclassifies the row to `theorem-strength-residue` with “keep battery discharge distinct from widening”. **Both are right and they point in different directions**: the box is discharged, the theorem is not widened. The corpus convention for what “DISCHARGED” licenses downstream is not fixed by either text.

**OPEN-CALL 4 — `640b31ff` is not a valid object. FOR-0b.**
S6.4 reads “sealed 640b31ff at 14329683”. `14329683` resolves and its message confirms the seal of `gentow6_r1_caplemma.py`; `e803c21d` (the RED commit) resolves; **`640b31ff` does not.** The seal is therefore real and pinned; one token is wrong or refers to an object outside this repository. A one-line correction would close it.

**OPEN-CALL 5 — the S1 cite wants its own provenance pass. FOR-0d.**
`.18` forecast it (“The S1 cite leg wants a PDF-provenance re-verification in its own pass (GENTOW2's pattern)”) and `.26` states the three stacked dependencies. **No such pass is recorded in the arc** — PE1/PE2/PE3 attacked scope, cap and codomain. Since the tie's closure rests on the cite, the forecast pass remains owed.

### 9.3 Fidelity audit

- Every CANONICAL STATEMENT is verbatim, or `[ASSEMBLED]` (six units: `.03`, `.04`, `.06`, `.08`, `.10`, `.32`), with `…` at every truncation.
- **The four boxes are reproduced without abridgement at both states**, per the queue charge; the two nested sibling-note r1 brackets inside `.34` are quoted in full.
- ASCII preserved; the STATUS TABLE emitted as a table.
- No quantifier, box state, coefficient or pin altered. Compiler-computed values appear only in TEETH fields, marked recomputed.
- 8 designation XREFs, 12 pin classes, 1 negative edge verified; **one pin reported unresolvable; no designation invented.**

EFF-GENTOW6 COMPILED: 35 statements / 8 xrefs verified / 5 open calls
