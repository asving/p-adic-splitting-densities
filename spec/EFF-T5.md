# EFF-T5 — CANONICAL EFFECTIVE SPECIFICATION v2

## 1. Header

**Source note:** `lean/notes/openmath/T5_WFRAME_2026-08-12.md`

**HEAD blob hash:** `966f19bbce7bd0cadcaee8944ef7ce470373a69d`

**Compilation context:** `docs/BLUEPRINT_PHASE_DESIGN_2026-08-13.md`, Revision 2, Stage 0a.

**Governing template:** `spec/EFF_TEMPLATE_v2.md` (all rules; full compilation).

**COMPILED-BY:** opus (2026-08-14, Asvin-authorized substitution, PROJECT_STATE #51). CODEX CROSS-READ OWED (post-reset).

### Effective-text rule

Derived, per template rule 6. Every round of this note opens with an explicit apply-and-replace instruction, and the rounds compose in order.

r1:

> “Apply the following replacements. All unlisted text remains unchanged.”

r2:

> “Apply after r1. All unlisted text remains unchanged.”

r3:

> “Apply after r2. All unlisted text remains unchanged. This repair records the unresolved evidence state; it does not claim repository verification or acceptance.”

r4:

> “Apply after r3. All unlisted text remains unchanged.”

r5:

> “Apply after r4. All unlisted text remains unchanged.”

The acceptance record then certifies the post-r5 text with its two folded riders:

> “Arc: composed (comp4 T5C) → r1–r3 (sol-arm passes P1–P4 with
> repairs) → r4 (Fable arm: companion scoping + the eight-item
> evidence census independently verified, incl. a bit-identical
> instrument re-run) → r5 (frame refresh to T1 r1–r10, intrinsic/
> avatar re-scopes, locator-compliant census) → PASS 1: Fable-arm
> hostile, VERDICT CLEAN (0C + 0G + 2m, folded as riders per the
> minors-only precedent) → PASS 2: sol confirmation (gpt-5.6-sol
> high, runs/comp12/T5P7_output.log), VERDICT CLEAN (0C + 0G + 0m).”

**Therefore the effective text is the base with r1–r5 applied in order, plus the two post-pass-1 dated riders (m-1, m-2) folded.** Note that r5's replacements are stated against *r1's replacement text* in two places (its item 2 targets “r1's item 4 (the I5 rewrite)”), so the assembly is genuinely nested, not a flat overlay.

### Supersession chain inside the note

`[TABLE]`

| Span | base | r1 | r2 | r3 | r4 | r5 | rider | Governing text |
|---|---|---|---|---|---|---|---|---|
| Composition-pass / status lines | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — | **r5** |
| Frame of record | “with r1–r3” | — | — | — | — | “with r1–r9” | m-2: “r1–r10” | **rider m-2** |
| Tower supplier line | ✓ | ✓ | — | — | — | — | — | **r1** |
| §2 from `### LEMMA W-FRAME-LAW` to end of §2 | ✓ | replaced in full | — | — | — | — | — | **r1** |
| I1 final sentence | ✓ | ✓ | — | — | — | — | — | **r1** |
| I3 (heading + body) | ✓ | — | — | — | — | replaced in full | — | **r5** |
| I4 (heading + body) | ✓ | — | — | — | — | replaced in full | — | **r5** |
| I5 head → paragraph before LW3 | ✓ | replaced | — | — | — | — | — | **r1** |
| I5's `where`-block (w_i, θ_i) | — | ✓ | replaced | — | — | — | — | **r2** |
| I5's closing paragraph (companion scope) | — | ✓ | — | — | replaced (G1) | — | — | **r4** |
| §3 opening paragraph | ✓ | — | — | replaced | — | — | — | **r3** |
| §4 opening → sentence before (WF-chain) | ✓ | replaced | — | — | — | — | — | **r1** |
| Honesty item 1 | ✓ | — | ✓ | — | — | — | — | **r2** |
| Honesty items 2, 3 | ✓ | ✓ | — | — | — | — | — | **r1** |
| Honesty items 13, 14 | — | — | — | — | — | appended | — | **r5** |
| Pass 1 item 1 | ✓ | — | ✓ | — | — | — | — | **r2** |
| Pass 1 item 2 | ✓ | ✓ | — | — | — | — | — | **r1** |
| Pass 2 items 1, 4 | ✓ | — | — | ✓ | — | — | — | **r3** |
| Pass 2 item 3 | ✓ | ✓ | — | — | — | — | — | **r1** |
| Pass 2 item 5 | ✓ | ✓ | — | — | — | — | — | **r1** |
| Acceptance gate + §7 close | ✓ | — | — | replaced | — | — | — | **r3** |
| “Current verification disposition” body | — | — | — | created | replaced | replaced | — | **r5** |
| Census entry (8) “artifact ends” | — | — | — | — | created | replaced (F5) | — | **r5** |
| r5 locator table entry 6 span | — | — | — | — | — | L2299–2312 | m-1: L2299–2313 | **rider m-1** |

### Scope of record — mandatory two-predicate split

#### A. Mathematical absorption/supply set

The note's own charge:

> “This note gives the cross-frame unit one canonical definition and gives \(\psi^{(w)}\) one home. The former per-note riders become instance records below; they are neither independent definitions nor general laws inferred from measurements.”

and its fence:

> “No absorbed statement changes. GENTOW2 and GENTOW5 remain frozen.”

The mathematical supply set is therefore: **one definition triple ((WF-w), (WF-twist), (WF-psi)), one law in two scoped forms ((WF-law\(_2\)) unconditional and (WF-law\(_i\)) under \(\mathcal W_{\le i}\)), its realized-residual corollary in the same two scopes, one corrected twist index (WF-index), and five instance records I1–I5.** Nothing is absorbed *out of* a source in the T3/T4 sense — this is a **naming-and-scoping** unit, not a statement-compression unit; the closing sentence of §7 says so:

> “No new arithmetic theorem is required. The remaining work is an
> independent repository verification of the definition, supplier range,
> cumulative dependency chain, corrected twist index, and instance
> records, followed by two consecutive clean hostile passes.”

#### B. Consumption/append-protocol scope

The acceptance record states, without truncation:

> “Consumption checks and the absorbed notes' dated appends ride the next queue (the T3/S-2 pattern). Standing conditionalities unchanged and displayed:
> 𝒲_{≤i} at i ≥ 3, OPEN-2 at i = 1, [WFRAME OPEN-1]/[OPEN-3].”

So the protocol scope is: **five instance sources, ZERO dated appends landed.** Verified negatively — `grep -cF 'WF-w'` = 0 in each of `T1_GAUGE_COCYCLE_2026-08-12.md`, `GENTOW2_PROOF_2026-08-09.md`, `GENTOW5_PROOF_2026-08-09.md`, `GENTOW5_WI_2026-08-10.md` (XREF rows N1–N4), and no file in the repo other than the note itself contains `WF-law`, `WF-realized`, or `DEFINITION W-FRAME` (row N5).

### Resolution rules applied

1. Rounds compose in order r1 → r5; a later round wins on any span an earlier round also touched. The 24 collisions are tabulated above.
2. **r5's item 2 and item 5 target r1's and r4's replacement text, not the base.** The assembly is nested; a flat base-overlay reading produces the wrong I5 and the wrong verification disposition.
3. **The base §4 sentence at L299, “The certified WI note discharges these conjuncts in its tower range.”, is SUPERSEDED** by r1's replacement (L616): “Certification of the WI companion does not discharge these conjuncts from the frozen GENTOW5 statements.” The stale sentence remains physically in the file; `spec/HYPOTHESIS_LEDGER.md` HYP.100 records the pair as a corpus-consistency item (“Stale text says WI discharges the conjuncts; governing text says it does not”, disposition “strike/append stale sentence”). The compiler resolves it by supersession and does not strike it.
4. The base (WF-law) — “At every certified recipe rung \(2\le i<r\)” with no hypothesis — is fully superseded by r1's two-scope split. **No unconditional \(i\ge3\) law exists in the effective text.**
5. I5's heading is **“conditional `[GENTOW5-W(i)]` supplier”** (r1), not the base's “certified `[GENTOW5-W(i)]`”. The *certification* is a fact of the record; the *supplier reading* is conditional. r4-G1 supplies the full argument (EFF.T5.27).
6. The frame of record is **T1 with r1–r10** (rider m-2), superseding r5's “r1–r9” and the base's “r1–r3”.
7. Every round's “counter resets” and every pre-acceptance status line are historical after the acceptance record.
8. r2 is declared notation-only: “This repair changes notation only.” It creates no new mathematical unit; it bridges \(\bar k_i=\bar\kappa_i\).
9. The r3 census-verification requirement is *met* as of r5 in the per-entry locator form r3 demanded; the two clean passes then landed. Both halves of r3's gate are therefore discharged (EFF.T5.39, .45).
10. Compile-time re-verification is performed on every locator this note asserts: all 16 named commits, both object-addressed pin spans, and all three instrument md5s (§4.5).

### Quotation and prosification discipline

All source displays and tables are reproduced as quotations or marked `[ASSEMBLED]`; every source table is marked `[TABLE]`; no display or table is prosified; truncation carries an explicit ellipsis; terminal punctuation inside quoted displays is preserved. `[IMPORTED]`, `[DEFINITIONAL]`, `[ASSUMED]`, `[RECORD]`, `[SUPERSEDED]` are compiler metadata.

---

## 2. NON-IMPORTS

**N-1 — no avatar identification.** §1:

> “It does not identify \(w_i\) with a separately normalized uniformizer avatar.”

and honesty item 8:

> “The avatars \(z_1,z_1^2,z_2^2,z_2\) are measured instances. LW3 shows that an avatar can differ from intrinsic \(w_i\).”

The PE6 avatar–intrinsic identification is explicitly a NON-IMPORT: r5's I3 says the identification “rides Thm 4.2/eq (16) — row 23, DESCRIPTIVE — not a proved leg.”

**N-2 — the withdrawn “\(w\)-blind” argument is not revived.** §2:

> “Per-grade fixedness alone does not prove (WF-law). In particular, this note does not revive S11.2’s withdrawn “\(w\)-blind” argument.”

**N-3 — the companion's unconditional self-description is not adopted.** r4-G1:

> “Accordingly this note consumes
> the companion's pinned display only within the explicit cumulative
> hypothesis \(\mathcal W_{\le i}\) displayed above — a consumption
> every reading supports — and does not adopt the companion's
> unconditional self-description.”

and it explicitly declines to inherit the companion's own re-verification obligation:

> “Whether that conditional
> step, and the induction closing the conjuncts from the empty base,
> survives on the companion's full footprint is the companion's own
> re-verification obligation; it is NOT performed by the companion as
> written and NOT assumed here.”

**N-4 — no \(i=1\) instance of the definition triple.** r5 item 2:

> “- **\(i=1\): NONE.** No supplier discharges
>   (C3-ladder-nonvanishing) at \(i=1\), and no \(i=1\) instance of
>   (WF-w), (WF-twist), or (WF-psi) is asserted.”

**N-5 — the \(\vartheta\)-membership discharge is not adjudicated.** r5 item 2:

> “Whether S11.2's S2.1
> monomial-quotient computation discharges that hypothesis is not
> adjudicated here.”

**N-6 — no intrinsic reading of I4.** r5's I4:

> “An INTRINSIC evaluation \(\psi_3^{(w_2)}(Y)=0\) at this frame would
> additionally need either (a) a direct canonical-frame measurement
> of \(w_2\) — an IDY-style closure as at PE6's LD1/GA1/GA3 —
> together with a canonical-frame read of the class, or (b) a proved
> (non-descriptive) avatar–intrinsic identification at this frame.
> Neither is in the PE3 record.”

**N-7 — no intrinsic \(w_2\ne1\) at LD3.** r5's I3: “LD3 has NO direct canonical row: only the avatar measurement exists there, so intrinsic \(w_2\ne1\) is NOT asserted at LD3.”

**N-8 — splitting types are not concluded.** §1: “Turning factor data into a \(p\)-adic splitting type still requires the surrounding tower theorem.” Honesty item 11: “Unit twisting preserves factor patterns, not literal coefficients or roots. Splitting conclusions require the tower theorem.”

**N-9 — the measurement census is not coverage.** Honesty item 10: “The six PE6 primes, seventh-prime PE3 check, and LW3 are regression records, not exhaustive coverage.”

**N-10 — no source is rewritten.** Honesty item 12: “No accepted or byte-frozen source is rewritten.”

Accordingly **no edge is created** from T5 to any avatar-identity law, to S11.2's withdrawn scope walk, to the companion's unconditional verdict, to an \(i=1\) frame instance, or to a splitting-type conclusion.

---

## 3. Statement inventory

45 units, contiguous, in effective-text order.

### EFF.T5.01  [scope-record]

**CANONICAL STATEMENT — the governing metadata block** `[ASSEMBLED: r5's three lines + r1's tower-supplier line + rider m-2's frame refresh; the base RCT-unit line survives]`:

> “**RCT unit:** S-4-T5-w-frame”

> “**Composition pass:** r5, attempt 0/2, 2026-08-12  
> **Status:** repaired candidate; two clean hostile passes owed; the §7 span-pin ledger is locator-compliant as of r5 (exact per-entry commits; entry 1 re-verified at the current T1 commit)  
> **Frame of record:** T1, LEMMA GAUGE-COCYCLE, with r1–r9”

with the frame line further refreshed by rider m-2:

> “The frame of record is T1 with r1–r10
> (fb991138 — a 27-line pure append to C7's M2/M3 rows, immaterial to
> every T5 pin per the pass's own audit)”

and the tower-supplier line as replaced by r1:

> “**Tower supplier:** GENTOW2 unconditionally at \(i=2\); for \(i\ge3\), the explicit cumulative hypothesis \(\mathcal W_{\le i}\), whose newest conjunct is `[GENTOW5-W(i)]`, read with GENTOW5 S12.1 and the dated \(\psi_j^{(\omega_{j-1})}\) corrigendum”

**DERIVATION:** `[RECORD]` The base's tower-supplier line read “certified `[GENTOW5-W(i)]`”; r1 replaced it with the cumulative-hypothesis form. The frame advanced base r1–r3 → r5's r1–r9 → m-2's r1–r10.

**CONDITIONALITY:** the status line's “two clean hostile passes owed” is `[SUPERSEDED]` by EFF.T5.45. **The frame pin is now STALE by two T1 rounds** — see EFF.T5.09's staleness flag and §8 item 2.

**RESOLUTION TRACE:** base metadata → r1 (tower supplier) → r2, r3, r4, r5 (pass/status) → rider m-2 (frame). Commit `fb991138` verified to exist (XREF C16).

**TEETH:** §7 Pass 1(2), Pass 2(3).

---

### EFF.T5.02  [scope-record]

**CANONICAL STATEMENT:**

> “This note gives the cross-frame unit one canonical definition and gives \(\psi^{(w)}\) one home. The former per-note riders become instance records below; they are neither independent definitions nor general laws inferred from measurements.”

> “No absorbed statement changes. GENTOW2 and GENTOW5 remain frozen.”

**DERIVATION:** `[SCOPE RECORD]` This is the unit's charge and the reason its supply set contains no new arithmetic theorem (§7 close). The second sentence is the freeze fence that makes T5 a *naming* unit.

**CONDITIONALITY:** none.

**RESOLUTION TRACE:** base preamble; unchanged by r1–r5.

**TEETH:** §7 Pass 1(5); Pass 2(2).

---

### EFF.T5.03  [convention]

**CANONICAL STATEMENT:**

> “At tower level \(i\), fix the T1 data
>
> \[
> K_i,\qquad
> N_i:=\widehat n_i(u_{i+1}),\qquad
> R_{i+1,\beta},
> \]
>
> and
>
> \[
> \psi_{i+1}(y)
> =
> y^{f_{i+1}}-\sum_{t<f_{i+1}}c_ty^t
> \in K_i[y].
> \]
>
> Put
>
> \[
> \bar\kappa_i:=\frac{u_{i+1}}{e(\mu_i)}.
> \]”

**DERIVATION:** `[IMPORTED]` T1 data, imported wholesale. XREF X01 locates T1's (C3) family; the normalized slope \(\bar\kappa_i\) is bridged to the companion's \(\bar k_i\) by r2 (EFF.T5.26).

**CONDITIONALITY:** a tower level \(i\) is fixed.

**RESOLUTION TRACE:** base §1 head; unchanged by r1–r5.

**TEETH:** §7 Pass 1(1).

---

### EFF.T5.04  [definition]

**CANONICAL STATEMENT:**

> “### DEFINITION W-FRAME
>
> The cross-frame unit is
>
> \[
> \boxed{
> w_i:=
> R_{i+1,\bar\kappa_i}
> \bigl(\widehat n_i(u_{i+1})\bigr)
> \in K_i^\times.
> }
> \tag{WF-w}
> \]”

**DERIVATION:** `[DEFINITIONAL]` The definition itself is a naming act; what is NOT definitional is the membership `\in K_i^\times`. r5 item 2 makes that an explicit carried obligation at every site: “the membership \(w_i\in K_i^\times\) displayed in (WF-w)” is one of the enumerated (C3-ladder-nonvanishing) sites. See EFF.T5.09.

**CONDITIONALITY:** \(w_i\in K_i^\times\) is asserted only at sites carrying (C3-ladder-nonvanishing) — and, at T1's HEAD, a second clause T5 does not carry (staleness flag, EFF.T5.09).

**RESOLUTION TRACE:** base §1; unchanged by r1–r5, but *gated* by r5 item 2.

**TEETH:** §7 Pass 1(1) as replaced by r2.

---

### EFF.T5.05  [definition]

**CANONICAL STATEMENT:**

> “For a monic \(P\in K_i[y]\) of degree \(f\), define
>
> \[
> \boxed{
> P^{(w_i)}(y):=w_i^fP(y/w_i).
> }
> \tag{WF-twist}
> \]”

**DERIVATION:** `[DEFINITIONAL]` Unit substitution on a monic polynomial. Well-formedness requires \(w_i\ne0\) (division by \(w_i\)) — carried by EFF.T5.09.

**CONDITIONALITY:** \(P\) monic of degree \(f\); (C3-ladder-nonvanishing) at the site.

**RESOLUTION TRACE:** base §1; unchanged.

**TEETH:** §7 Pass 1(1).

---

### EFF.T5.06  [definition]

**CANONICAL STATEMENT:**

> “Thus
>
> \[
> \boxed{
> \psi_{i+1}^{(w_i)}(y)
> =
> w_i^{f_{i+1}}\psi_{i+1}(y/w_i)
> =
> y^{f_{i+1}}
> -\sum_{t<f_{i+1}}c_tw_i^{f_{i+1}-t}y^t.
> }
> \tag{WF-psi}
> \]
>
> These are T1’s \(w\)-inclusive canonical definitions.”

**DERIVATION:** `[COMPUTATION]` Substitute EFF.T5.03's \(\psi_{i+1}\) into EFF.T5.05 with \(f=f_{i+1}\): \(w_i^{f_{i+1}}\bigl((y/w_i)^{f_{i+1}}-\sum_t c_t(y/w_i)^t\bigr)=y^{f_{i+1}}-\sum_t c_tw_i^{f_{i+1}-t}y^t\). The r4 census independently confirms the same display in T1: “(C3-three-reads) boxes the canonical residual \(\psi_{i+1}^{(w_i)}(y):=w_i^{f_{i+1}}\psi_{i+1}(y/w_i)\) = (WF-psi)” (XREF X04).

**CONDITIONALITY:** as EFF.T5.05.

**RESOLUTION TRACE:** base §1; unchanged.

**TEETH:** §7 Pass 1(1); Pass 1(6).

---

### EFF.T5.07  [fence]

**CANONICAL STATEMENT:**

> ““Cross-frame” means that \(w_i\) converts the multiplicative residual frame to the canonical residual frame. It does not identify \(w_i\) with a separately normalized uniformizer avatar.”

**DERIVATION:** `[FENCE]` This is the note's foundational avatar separation (NON-IMPORT N-1). Its live counterexample is LW3, where \(w_{\rm av}=2\ne w_3=1\) (EFF.T5.28).

**CONDITIONALITY:** none.

**RESOLUTION TRACE:** base §1; unchanged. Ledger row HYP.66 (`WFRAME OPEN-3`) carries it downstream.

**TEETH:** §7 Pass 1(5); Pass 2(6).

---

### EFF.T5.08  [lemma]

**CANONICAL STATEMENT:**

> “Unit substitution preserves monicity, degree, irreducibility, nonzero constant term, and irreducible-factor degrees and multiplicities. Turning factor data into a \(p\)-adic splitting type still requires the surrounding tower theorem.”

**DERIVATION:** `[IMPORTED — and gated]` The preservation list is T1's §2.3 factor-pattern conclusion. r5's entry-1 re-verification narrows exactly this: T1 r9's R9-4 records that “preservation of irreducible-factor degrees/multiplicities is T1's §2.3 factor-pattern conclusion, fenced by (C3-ladder-nonvanishing), NOT an assertion of the pinned B span”. So the preservation list is conditional on the same site obligation as \(w_i\in K_i^\times\).

**CONDITIONALITY:** (C3-ladder-nonvanishing) at the site (EFF.T5.09); the tower theorem for splitting types (NON-IMPORT N-8).

**RESOLUTION TRACE:** base §1; text unchanged, but r5 item 2 lists “the unit-substitution paragraph above” among the carrying sites, and r5's entry-1 delta re-scopes its T1 source.

**TEETH:** §7 Pass 1(1).

---

### EFF.T5.09  [hypothesis]

**CANONICAL STATEMENT — the (C3-ladder-nonvanishing) carry (r5 item 2):**

> “### The (C3-ladder-nonvanishing) carry (T1 r8, R8-2)
>
> T1 r8 makes nonvanishing of the base ladder read an independent
> site obligation:
>
> \[
> \boxed{
> R_{i+1,\bar\kappa_i}
> \bigl(\widehat n_i(u_{i+1})\bigr)
> \ne0.
> }
> \tag{C3-ladder-nonvanishing}
> \]
>
> It does not follow from ladder multiplicativity, graded scalar
> covariance, the grade-zero residue rule, or (C3-zero-read); without
> it, neither division by \(w_i\) nor the unit-substitution argument
> is asserted. Accordingly, EVERY site of this note that divides by
> \(w_i\) or substitutes it carries (C3-ladder-nonvanishing)
> explicitly: the membership \(w_i\in K_i^\times\) displayed in
> (WF-w); the twists (WF-twist) and (WF-psi); the unit-substitution
> paragraph above; (WF-realized\(_2\)) and (WF-realized\(_i\)); the
> instance displays of I2, I4, and I5; and every
> \(\psi_j^{(\omega_{j-1})}\) leg of (WF-index). The named suppliers,
> by level:
>
> - **\(i=2\): LEMMA GENTOW2-B\('\)(3)/B\(''\).** B\('\)(3) is the
>   accepted per-grade-unit clause (each
>   \(u(\beta_t)=R_{3,\beta_t}(\widehat n_2(u_3(f_3-t)))\) a unit),
>   and B\(''\) pins \(w=R_{3,\bar\kappa}(\widehat n_2(u_3))\in
>   K_2^\times\) as a single slot-independent unit. This is the
>   discharge T1 r8 itself names at the accepted GENTOW2 site.
> - **\(i\ge3\): S11.2's proved fixed-unit list.** GENTOW5_PROOF
>   S11.2 (at `b0e5c648`, the proved list at L1904–1908) proves,
>   independently of \(\mathcal W_{\le i}\), per-grade FIXEDNESS of
>   \(u_i(\beta)\) — "each grade's unit is a fixed
>   \(K_i^\times\)-value". The \(s=1\) grade of that family is
>   exactly the base ladder read: at \(t=f_{i+1}-1\),
>   \(u_i(\beta_t)=R_{i+1,\bar\kappa_i}(\widehat n_i(u_{i+1}))\), the
>   defining read of (WF-w). A fixed \(K_i^\times\)-value is in
>   particular nonzero, so (C3-ladder-nonvanishing) is discharged at
>   every permitted \(i\ge3\) site — on S11.2's PROVED side of the
>   fence, while the single-\(w\) geometric law itself stays behind
>   \(\mathcal W_{\le i}\).
> - **\(i=1\): NONE.** No supplier discharges
>   (C3-ladder-nonvanishing) at \(i=1\), and no \(i=1\) instance of
>   (WF-w), (WF-twist), or (WF-psi) is asserted. The \(j=2\) leg of
>   (WF-index) is scoped accordingly: \(\psi_2\) carries
>   \(\omega_1=\) S11.3's fixed letter-power unit, which is a unit by
>   its own displayed letter-power form — NOT via
>   (C3-ladder-nonvanishing) applied to a displayed \(w_1\). Its
>   identification with \(w_1\) is exactly WFRAME OPEN-2; the
>   \(i=1\) display stays open with it.”

**DERIVATION:** `[IMPORTED OBLIGATION + per-level discharge argument]` The obligation is T1 r8's R8-2 (XREF X05, `\tag{C3-ladder-nonvanishing}`, count 1 in T1). The \(i\ge3\) discharge is a genuine derivation, not a citation: it identifies the \(s=1\) grade of S11.2's proved per-grade fixedness family with (WF-w)'s defining read, then observes that a fixed \(K_i^\times\)-value is nonzero. This deliberately routes around \(\mathcal W_{\le i}\) — the nonvanishing sits on S11.2's PROVED side while the geometric law stays behind the hypothesis.

**CONDITIONALITY / ⚠ STALENESS FLAG (load-bearing).** T5's frame pin is T1 **r1–r10** (EFF.T5.01). At T1's HEAD the gate for \(w_i\in K_i^\times\) is **two-clause**, per T1 r12's R12-3 (XREF X07, count 1):

> “after R11-1 the gate is two-clause — (C3-read-codomain) places \(w_i\in K_i\), (C3-ladder-nonvanishing) makes it nonzero (ledger item 22, §4.2 check 9).”

and T1's superseded honesty item 19 now reads:

> “The status \(w_i\in K_i^\times\) is asserted only at sites carrying BOTH (C3-read-codomain) and (C3-ladder-nonvanishing) — at \(i=2\) supplied by the accepted GENTOW2 package (S5.1 (R-FGMN) for the codomain, LEMMA GENTOW2-B\('\)/B\(''\) for nonvanishing), at \(i\ge3\) by an explicitly cited level-general analogue carrying both clauses.”

**T5 carries only the nonvanishing clause.** `spec/HYPOTHESIS_LEDGER.md` HYP.99 records exactly this (“T5 pins T1 through r10 … while r12 adds two-clause w_i∈K_i× gate”), class `audit-work-item`, disposition “cross-note HEAD audit”. The compiler does NOT add the missing clause — that is a note-owner repair (§8 item 2, OPEN-CALL 1).

**RESOLUTION TRACE:** r5 item 2, inserted into §1 “immediately after the paragraph ending "Turning factor data into a \(p\)-adic splitting type still requires the surrounding tower theorem."”. New in r5.

**TEETH:** §7 Pass 1(1), Pass 1(2), Pass 2(3).

---

### EFF.T5.10  [hypothesis]

**CANONICAL STATEMENT:**

> “Separately, the T1 r9 fence is carried at the telescope: this
> note's \(\Theta_i(t)=\vartheta_{i,s_t}^{-1}\) displays invert
> \(\vartheta\), and per T1 r9 the membership
> \(\vartheta_{i,s}\in K_i^\times\) is proved at \(i\in\{1,2\}\)
> only. Every \(i\ge3\) use of \(\Theta_i(t)\) in this note carries
> the named hypothesis \(\mathrm{(H\!-\!VARTHETA\!-\!RES)}_i\)
> alongside \(\mathcal W_{\le i}\). Whether S11.2's S2.1
> monomial-quotient computation discharges that hypothesis is not
> adjudicated here.”

**DERIVATION:** `[IMPORTED FENCE]` T1 r9's R9-2 fences (C2-vartheta)'s \(K_i^\times\) membership behind \(\mathrm{(H\!-\!VARTHETA\!-\!RES)}_i\) at \(i\ge3\). XREF X06 verifies the hypothesis name in T1 (19 occurrences); XREF X08 verifies `R9-2`. T1's own honesty item 21 states it: “\(\vartheta_{i,s}\in K_i^\times\) is proved at \(i\in\{1,2\}\) only.”

**CONDITIONALITY:** at \(i\ge3\), every \(\Theta_i(t)\) display carries \(\mathrm{(H\!-\!VARTHETA\!-\!RES)}_i\) **in addition to** \(\mathcal W_{\le i}\). This is a SECOND conditional stack on the same displays and must not be conflated with \(\mathcal W_{\le i}\).

**RESOLUTION TRACE:** r5 item 2, closing paragraph. New in r5; honesty item 13 (EFF.T5.35) restates it. Ledger rows in the `### T1/T3/T5 supplier conditions` section carry it (RX-row).

**TEETH:** §7 Pass 1(1), Pass 2(3).

---

### EFF.T5.11  [convention]

**CANONICAL STATEMENT:**

> “For \(t<f_{i+1}\), put
>
> \[
> s_t:=f_{i+1}-t,
> \qquad
> \beta_t:=s_t\bar\kappa_i,
> \]
>
> and use T1’s two telescope orientations
>
> \[
> \vartheta_{i,s}
> =
> \operatorname{res}\!\left(
> \frac{\widehat n_i(u_{i+1})^s}
>      {\widehat n_i(su_{i+1})}
> \right),
> \qquad
> \Theta_i(t):=\vartheta_{i,s_t}^{-1}.
> \]
>
> Define
>
> \[
> u_i(\beta_t)
> :=
> R_{i+1,\beta_t}
> \bigl(\widehat n_i(s_tu_{i+1})\bigr).
> \]”

**DERIVATION:** `[IMPORTED]` T1's telescope pair. The r4 census confirms the T1 side (XREF X03, `\tag{C3-Theta}`): “(C3-Theta) boxes \(\Theta_i(t)=\vartheta_{i,f_{i+1}-t}^{-1} =\operatorname{res}(\widehat n_i((f_{i+1}-t)u_{i+1}) /\widehat n_i(u_{i+1})^{f_{i+1}-t})\)”.

**CONDITIONALITY:** \(t<f_{i+1}\); the inverse \(\Theta_i(t)\) at \(i\ge3\) carries EFF.T5.10's fence.

**RESOLUTION TRACE:** base §2 head (r1's replacement span begins at `### LEMMA W-FRAME-LAW`, so this survives).

**TEETH:** §7 Pass 1(6) (the \(\vartheta\)-vs-\(\Theta\) orientation lint).

---

### EFF.T5.12  [lemma]

**CANONICAL STATEMENT:**

> “### LEMMA W-FRAME-LAW
>
> At the base rung \(i=2\), unconditionally,
>
> \[
> \boxed{
> u_2(\beta_t)
> =
> \Theta_2(t)w_2^{f_3-t}
> =
> \vartheta_{2,f_3-t}^{-1}w_2^{f_3-t}
> \qquad(t<f_3).
> }
> \tag{WF-law\(_2\)}
> \]”

**DERIVATION:** `[IMPORTED — accepted GENTOW2]` Supplied by LEMMA GENTOW2-B″, object-addressed at `33f86204` L729–738. The r4 census quotes the source (XREF X09/C03): “**LEMMA GENTOW2-B″.** For each slot t < f₃, with w := u(κ̄) = R_{3,κ̄}(n̂₂(u₃)): (6) u(β_t) = ϑ(t) · w^{f₃−t}. Consequently … R_{3,α}(Φ₃) = … = w^{f₃}·P(y/w) …”, with the orientation note “(B″'s single-
argument \(\vartheta(t)\) is this note's \(\Theta_2(t)\), the
inverse-telescope orientation)”.

**CONDITIONALITY:** **unconditional** at \(i=2\) — no \(\mathcal W\) hypothesis. The site obligation (C3-ladder-nonvanishing) is discharged here by GENTOW2-B′(3)/B″ (EFF.T5.09, EFF.T5.23).

**RESOLUTION TRACE:** base (WF-law) — a single unscoped law “At every certified recipe rung \(2\le i<r\)” — is `[SUPERSEDED]` in full by r1's two-scope split. r1 governs.

**TEETH:** §7 Pass 1(2) as replaced by r1; Pass 2(3).

---

### EFF.T5.13  [definition]

**CANONICAL STATEMENT:**

> “For every permitted rung \(3\le i<r\), put
>
> \[
> \mathcal W_{\le i}
> :=
> \bigwedge_{3\le q\le i}
> [\mathrm{GENTOW5-W}(q)].
> \]”

**DERIVATION:** `[IMPORTED]` This is S12.1's cumulative condition, verified in GENTOW5_PROOF (XREF X12): the r4 census quotes “\(\mathcal W_{\le n}:= \bigwedge_{3\le q\le n}[{\rm GENTOW5\!-\!W}(q)]\)”. r4-G1 fixes the empty base, displaying inside its conditional step “\mathcal W_{\le2}\ \text{the empty conjunction}”.

**CONDITIONALITY:** \(3\le i<r\); \(\mathcal W_{\le2}\) is empty.

**RESOLUTION TRACE:** r1's §2 replacement (and, for the \(n\) form, r1's §4 replacement). New in r1 at this position.

**TEETH:** §7 Pass 1(2), Pass 1(3), Pass 2(5).

---

### EFF.T5.14  [lemma]

**CANONICAL STATEMENT:**

> “The higher-rung statement retains this explicit hypothesis:
>
> \[
> \boxed{
> \mathcal W_{\le i}
> \Longrightarrow
> \left[
> u_i(\beta_t)
> =
> \Theta_i(t)w_i^{s_t}
> =
> \vartheta_{i,s_t}^{-1}w_i^{s_t}
> \qquad(t<f_{i+1})
> \right].
> }
> \tag{WF-law\(_i\)}
> \]”

**DERIVATION:** `[CONDITIONAL IMPORT]` The consequent is the companion's pinned display (XREF C02, `b0e5c648` L1912–1913, re-verified byte-literally at compile time). The implication form — rather than the bare law — is r1's repair and r4-G1's argument (EFF.T5.27): the companion's own unconditional verdict rests on a scope walk that GENTOW5 S12.1 has since WITHDRAWN.

**CONDITIONALITY:** \(\mathcal W_{\le i}\); **additionally** \(\mathrm{(H\!-\!VARTHETA\!-\!RES)}_i\) at every \(i\ge3\) use of \(\Theta_i(t)\) (EFF.T5.10); **additionally** (C3-ladder-nonvanishing) at every \(w_i\) site (EFF.T5.09, one clause short of T1 HEAD). Ledger row HYP.63 carries \(\mathcal W_{\le i}\) with disposition “CARRY — WI proves only the induction step, not induction from empty base.”

**RESOLUTION TRACE:** base (WF-law) `[SUPERSEDED]` → r1's §2 replacement. r1 governs.

**TEETH:** §7 Pass 1(2), Pass 2(3), Pass 2(5).

---

### EFF.T5.15  [record]

**CANONICAL STATEMENT:**

> “The suppliers are:
>
> - \(i=2\): LEMMA GENTOW2-B\(^{\prime\prime}\), unconditionally;
> - \(3\le i<r\): the final conjunct `[GENTOW5-W(i)]` of the cumulative hypothesis \(\mathcal W_{\le i}\).
>
> At \(i\ge3\), the new rung hypothesis is `[GENTOW5-W(i)]`; the earlier conjuncts remain the ambient induction history. The existence or certification of the companion note does not remove these hypotheses from the statement.”

**DERIVATION:** `[RECORD]` The final sentence is the r1 repair's core: certification is an event in the record, not a discharge of a hypothesis inside a frozen statement. The base said the opposite in effect (“The certified companion closes that induction from the unconditional \(i=2\) rung.”), which is `[SUPERSEDED]`.

**CONDITIONALITY:** none beyond EFF.T5.14's.

**RESOLUTION TRACE:** base §2 supplier list → r1's §2 replacement.

**TEETH:** §7 Pass 1(2), Pass 2(2), Pass 2(5).

---

### EFF.T5.16  [fence]

**CANONICAL STATEMENT:**

> “Per-grade fixedness alone does not prove (WF-law). In particular, this note does not revive S11.2’s withdrawn “\(w\)-blind” argument.”

**DERIVATION:** `[FENCE]` This is the fence that keeps EFF.T5.09's \(i\ge3\) nonvanishing discharge from over-reaching: S11.2's per-grade fixedness is used ONLY for nonvanishing of one grade, never for the single-\(w\) geometric law. EFF.T5.09 states the boundary explicitly (“on S11.2's PROVED side of the fence, while the single-\(w\) geometric law itself stays behind \(\mathcal W_{\le i}\)”).

**CONDITIONALITY:** none.

**RESOLUTION TRACE:** base §2, preserved verbatim inside r1's §2 replacement.

**TEETH:** §7 Pass 1(6), Pass 2(6).

---

### EFF.T5.17  [fence]

**CANONICAL STATEMENT:**

> “No \(i=1\) version is asserted. S11.3 supplies a fixed letter-power unit there, but its identification with the displayed \(w_1\) remains open.”

**DERIVATION:** `[FENCE]` This is `WFRAME OPEN-2` (EFF.T5.36), reinforced by EFF.T5.09's \(i=1\): NONE bullet. Ledger row HYP.65 carries it: “CONSUMED BY: entire i=1 stratum, presently excluded.”

**CONDITIONALITY:** none.

**RESOLUTION TRACE:** base §2, preserved verbatim inside r1's §2 replacement.

**TEETH:** §7 Pass 1(2), Pass 2(3).

---

### EFF.T5.18  [lemma]

**CANONICAL STATEMENT:**

> “### Realized-residual corollary
>
> Whenever the relevant (WF-law) equality is available, the coherent recipe coefficient is
>
> \[
> \Gamma_t=c_t\vartheta_{i,s_t},
> \]
>
> and therefore
>
> \[
> \Gamma_tu_i(\beta_t)
> =
> c_t\vartheta_{i,s_t}
> \vartheta_{i,s_t}^{-1}w_i^{s_t}
> =
> c_tw_i^{s_t}.
> \]
>
> Thus the base rung is unconditional:
>
> \[
> \boxed{
> R_{\nu_2}(\Phi_3)
> =
> \psi_3^{(w_2)}.
> }
> \tag{WF-realized\(_2\)}
> \]”

**DERIVATION:** `[PROVED — one cancellation]` Substituting (WF-law) into \(\Gamma_tu_i(\beta_t)\) cancels \(\vartheta_{i,s_t}\vartheta_{i,s_t}^{-1}=1\), leaving \(c_tw_i^{s_t}\) — exactly the coefficient of \(y^t\) in (WF-psi) (EFF.T5.06, with \(s_t=f_{i+1}-t\)). The r4 census confirms both T1 inputs (XREF X02/X04): the body has “\(\Gamma_t=c_t\vartheta_{i,f_{i+1}-t}\) and "so the \(\vartheta/\Theta\) factors cancel"”. Step justifications: \(\Gamma_t\) `by XREF X02` (T1 (C3) body); the cancellation `by EFF.T5.11`'s definition \(\Theta_i(t)=\vartheta_{i,s_t}^{-1}`; the identification of the result with \(\psi^{(w)}\) `by EFF.T5.06`; the \(i=2\) unconditionality `by EFF.T5.12`.

**CONDITIONALITY:** “Whenever the relevant (WF-law) equality is available” — r1's added guard; unconditional only at \(i=2\). Plus (C3-ladder-nonvanishing) (EFF.T5.09 lists (WF-realized\(_2\)) as a carrying site).

**RESOLUTION TRACE:** base corollary (which asserted (WF-realized) “at every certified rung \(i\ge2\)”) `[SUPERSEDED]` → r1's §2 replacement, which splits it into the unconditional base and the conditional higher rung.

**TEETH:** §7 Pass 1(2), Pass 2(2).

---

### EFF.T5.19  [lemma]

**CANONICAL STATEMENT:**

> “For every permitted \(i\ge3\), the realized residual retains the cumulative scope
>
> \[
> \boxed{
> \mathcal W_{\le i}
> \Longrightarrow
> \text{\rm(WF-law)}_i
> \Longrightarrow
> R_{\nu_i}(\Phi_{i+1})
> =
> \psi_{i+1}^{(w_i)}.
> }
> \tag{WF-realized\(_i\)}
> \]”

**DERIVATION:** `[CONDITIONAL]` Same cancellation as EFF.T5.18, run inside the implication. The two-arrow display is the note's own way of keeping the hypothesis attached through the corollary.

**CONDITIONALITY:** \(\mathcal W_{\le i}\); plus \(\mathrm{(H\!-\!VARTHETA\!-\!RES)}_i\) (EFF.T5.10) and (C3-ladder-nonvanishing) (EFF.T5.09).

**RESOLUTION TRACE:** r1's §2 replacement. New in r1.

**TEETH:** §7 Pass 1(2), Pass 2(5).

---

### EFF.T5.20  [record]

**CANONICAL STATEMENT:**

> “T1 supplies the dictionary and the final cancellation. GENTOW2-B\(^{\prime\prime}\) supplies the unconditional \(i=2\) B-law. At \(i\ge3\), `[GENTOW5-W(i)]` supplies the sitewise B-law only within the displayed cumulative hypothesis.”

**DERIVATION:** `[RECORD — supplier attribution]` The three-way split is what §7 Pass 2(2) checks (“Verify that T1 supplies only the dictionary/cancellation and that each B-law names its supplier”). T1's own A7 re-scope confirms the narrow T1 role (XREF X10): “T1 only **supplies the gauge algebra to** the preceding B-law display. The residual obligation is the accepted GENTOW2-B″ per-grade-unit theorem … but it does not prove the supplier identity itself. Thus the B-law is no longer labeled 'absorbed verbatim.'” — a quote r5's entry-1 delta then marks partly superseded (EFF.T5.43).

**CONDITIONALITY:** none.

**RESOLUTION TRACE:** base §2 tail → r1's §2 replacement (the \(i\ge3\) sentence gains “only within the displayed cumulative hypothesis”).

**TEETH:** §7 Pass 2(2).

---

### EFF.T5.21  [fence]

**CANONICAL STATEMENT:**

> “## 3. Instance records and span pins
>
> An instance imports only its pinned display. It does not enlarge the
> containing theorem or turn a measurement into a symbolic formula.
>
> The locators below are claimed span pins, not completed verification
> records. A locator becomes verified only when a hostile pass checks it
> against the repository object at an exact commit hash or immutable
> ledger identifier and records the result in the verification ledger of
> §7. Failure of the workspace launcher does not count as verification,
> and agreement among the supplied excerpts does not substitute for an
> independent repository check.”

**DERIVATION:** `[FENCE — evidence protocol]` r3's addition, written against the r1/r2 rounds in which “Repository span-pin verification was again unavailable because the workspace executor could not launch without `bwrap`”. It is the gate r4 and r5 then discharge.

**CONDITIONALITY:** none. The gate is met as of r5 (EFF.T5.40).

**RESOLUTION TRACE:** base §3 opening (two sentences) → r3's replacement (adds the four-sentence locator paragraph).

**TEETH:** §7 Pass 2(1) as replaced by r3.

---

### EFF.T5.22  [instance-record]

**CANONICAL STATEMENT:**

> “### I1 — T1 canonical frame
>
> **Span pin:** T1, (C3-B-law), (C3-canonical-read), and (C3-three-reads), read with T1 r2’s A7 re-scope.
>
> T1 supplies
>
> \[
> \Gamma_t=c_t\vartheta_{i,s_t},
> \qquad
> \Theta_i(t)=\vartheta_{i,s_t}^{-1},
> \]
>
> and the cancellation leading to (WF-realized).”

with r1's replaced final sentence:

> “Its supplier fence leaves the unconditional base law to GENTOW2 and every higher-rung law to the explicit cumulative hypothesis \(\mathcal W_{\le i}\).”

**DERIVATION:** `[INSTANCE]` All three T1 designations verified: XREF X01 `\tag{C3-B-law}` (1), X11 `\tag{C3-canonical-read}` (3), X04 `\tag{C3-three-reads}` (1), plus X03 `\tag{C3-Theta}` (1) and X10 the A7 row (3). Independently re-verified by the note's own census entry 1 and again at r5 against T1's then-current commit `c2c77b54` (EFF.T5.43).

**CONDITIONALITY:** the pin is read “with T1 r2's A7 re-scope”; r5's entry-1 delta records that the quoted A7 sentence is itself SUPERSEDED by T1 r8's expanded replacement, and that the three-reads/factor-pattern conclusions now sit behind (C3-ladder-nonvanishing).

**RESOLUTION TRACE:** base I1 → r1 item 3 (final sentence). r1 governs the closing sentence.

**TEETH:** §7 Pass 1(1) as replaced by r2; Pass 2(1), Pass 2(2).

---

### EFF.T5.23  [instance-record]

**CANONICAL STATEMENT:**

> “### I2 — GENTOW2 symbolic rung
>
> **Span pin:** accepted GENTOW2, LEMMA GENTOW2-B\(^{\prime\prime}\),
>
> \[
> u_2(\beta_t)
> =
> \Theta_2(t)w_2^{f_3-t},
> \qquad
> w_2
> =
> R_{3,\bar\kappa_2}
> \bigl(\widehat n_2(u_3)\bigr),
> \]
>
> together with the terminal rescaling
>
> \[
> \psi_3^{(w_2)}(y)=w_2^{f_3}\psi_3(y/w_2).
> \]
>
> This is the unconditional base rung.”

with r5's appended site obligation:

> “Site obligation: the terminal rescaling divides by \(w_2\);
> (C3-ladder-nonvanishing) is discharged here by
> GENTOW2-B\('\)(3)/B\(''\) (\(w\in K_2^\times\)).”

**DERIVATION:** `[INSTANCE — object-addressed]` The pin is object-addressed at `33f86204` L729–738 and re-verified byte-literally at compile time (XREF C03): the span opens `**LEMMA GENTOW2-B″.** For each slot t < f₃, with w := u(κ̄) = R_{3,κ̄}(n̂₂(u₃)):`. Its ledger identity is `statement_ledger_baseline.json` entry `GENTOW2_PROOF_2026-08-09.md/statements/3`, md5 `d4c8b8854e7b00e517eee10c7d7a042d`, with the note's own mechanism caveat retained: “the ledger hashes the NORMALIZED statement text; a raw `sed -n '729,738p' | md5sum` of the byte span gives `8978f8ba…` — not a mismatch.”

**CONDITIONALITY:** unconditional as a law; the terminal rescaling carries (C3-ladder-nonvanishing), discharged here (and, at T1 HEAD, also (C3-read-codomain) — not carried; EFF.T5.09's staleness flag).

**RESOLUTION TRACE:** base I2 + r5 item 2's append. r5 governs the closing paragraph.

**TEETH:** §7 Pass 1(2), Pass 2(1).

---

### EFF.T5.24  [instance-record]

**CANONICAL STATEMENT — I3 as replaced in full by r5 (F2):**

> “### I3 — GENTOW2-PE6
>
> **Span pin:** GENTOW2-PE6, the six-prime/four-frame record
> (`GENTOW2_passPE6_report.md` @ `deba21bd`, census entry 3).
>
> The record carries two different strengths, scoped separately.
>
> **Intrinsic \(w_2\ne1\) — PROVED at the three IDY-closed frames:**
>
> \[
> \boxed{
> w_2\ne1
> \quad\text{(intrinsic, in the sense of (WF-w)) at LD1, GA1, GA3.}
> }
> \]
>
> At those frames PE6 measured \(u(\beta_0)\) DIRECTLY as its own
> canonical-frame ratio (LD1: \(1\); GA1: \(1\); GA3: \(z_2\)) and
> \(\vartheta(0)\) independently (re-meeting the decode pin \(z_1\)
> and the PE5 pin \(z_2^2\)). The unconditional \(i=2\) law — B\(''\)
> display (6) at \(t=0\), with \(f_3=2\) at these frames:
> \(u(\beta_0)=\vartheta(0)\,w_2^2\) — then pins the intrinsic value:
> \(w_2^2=u(\beta_0)/\vartheta(0)\ne1\) at each of the three frames
> (the measured pairs \(1\ne z_1\), \(1\ne z_2^2\), \(z_2\ne z_2^2\),
> each an instance of the recorded letter values \(\ne1\)), whence
> \(w_2\ne1\). The IDY closure rows additionally tie
> \(w_{\rm av}^2=w_2^2\) there — a consistency closure, as the PE6
> report itself labels them.
>
> **Avatar \(\ne1\) — MEASURED at all six primes of the four
> frames,** with recorded avatars \(z_1\) (LD1), \(z_1^2\) (LD3),
> \(z_2^2\) (GA1), \(z_2\) (GA3). The directly measured object is the
> avatar
> \(\operatorname{res}((\widehat n_2(u_3)/\pi_3^{u_3})(\xi))\); its
> identification with the intrinsic read (WF-w) rides Thm 4.2/eq (16)
> — row 23, DESCRIPTIVE — not a proved leg. LD3 has NO direct
> canonical row: only the avatar measurement exists there, so
> intrinsic \(w_2\ne1\) is NOT asserted at LD3.
>
> **LW3 caveat (displayed because it prices the avatar leg):** LW3 is
> the first measured geography where the avatar differs from the
> intrinsic unit (\(w_{\rm av}=2\ne w_3=1\)); an avatar measurement
> alone never upgrades to an intrinsic (WF-w) value without a closed
> identification.
>
> These records refute a universal \(w=1\) assertion — intrinsically
> at LD1/GA1/GA3, avatar-wise at all six primes. The avatar
> expressions remain frame-local measurements; no general avatar
> formula is inferred.”

**DERIVATION:** `[PROVED at three frames; MEASURED at six]` The intrinsic leg is a genuine derivation, not a measurement report: at an IDY-closed frame both \(u(\beta_0)\) and \(\vartheta(0)\) are measured independently in the canonical frame, so the *unconditional* \(i=2\) law (EFF.T5.12) inverts to \(w_2^2=u(\beta_0)/\vartheta(0)\); the three measured pairs \(1\ne z_1\), \(1\ne z_2^2\), \(z_2\ne z_2^2\) are each \(\ne1\), whence \(w_2\ne1\). The avatar leg is measurement only, with the identification riding a DESCRIPTIVE row. Source quotes verified: XREFs X13 (`w-avatar = z`), X14 (`Results (run-2 GREEN 68 checks / 0 violations)`), X15 (the IDY rows), X16 (`avatar = R_{3,κ̄}(n̂₂(u₃)) — riding Thm 4.2/eq (16) (row 23,`), X17 (`u(β₀) measured DIRECTLY as its own canonical-frame ratio (LD1: 1;`).

**SUPERSEDED PREDECESSOR.** The base I3 boxed \(\boxed{w_2\ne1}\) with no intrinsic/avatar split, over all six primes — an intrinsic upgrade of an avatar measurement. That is the r5 finding F2 and the box is `[SUPERSEDED]`.

**CONDITIONALITY:** intrinsic at LD1/GA1/GA3 ONLY; avatar-keyed elsewhere; the avatar–intrinsic identification is DESCRIPTIVE (ledger row HYP.66, whose disposition records the gap “is now measured to GAP at LW3”).

**RESOLUTION TRACE:** base I3 → r5 item 3, full replacement. r5 governs.

**TEETH:** §7 Pass 1(5), Pass 1(6), Pass 2(4).

---

### EFF.T5.25  [instance-record]

**CANONICAL STATEMENT — I4 as replaced in full by r5 (F3):**

> “### I4 — GENTOW5-PE3
>
> **Span pin:** GENTOW5-PE3, the seventh-prime row
> (`GENTOW5_passPE3_report.md` @ `39c36aa2`, census entry 4).
>
> The pinned closure is avatar-coherent, not intrinsic:
>
> \[
> \boxed{\psi_3^{(w_{\rm av})}(Y_{\rm av})=0,}
> \]
>
> where \(w_{\rm av}=z_2\ne1\) is the avatar measured at the seventh
> prime (the first carrier-(8,4) geometry) and \(Y_{\rm av}\) is the
> FGMN class avatar — the report's own keying ("Y = the FGMN class
> avatar"). Both keys are measured nonzero letters, so the displayed
> twist is well-formed at the avatar key; the
> (C3-ladder-nonvanishing) question attaches to the intrinsic read,
> which this row does not evaluate. The avatar-mediation caveat is
> census entry 3's, verbatim: the identification of \(w_{\rm av}\)
> with the intrinsic (WF-w) value rides Thm 4.2/eq (16) (row 23,
> DESCRIPTIVE), and LW3 shows that leg can gap.
>
> An INTRINSIC evaluation \(\psi_3^{(w_2)}(Y)=0\) at this frame would
> additionally need either (a) a direct canonical-frame measurement
> of \(w_2\) — an IDY-style closure as at PE6's LD1/GA1/GA3 —
> together with a canonical-frame read of the class, or (b) a proved
> (non-descriptive) avatar–intrinsic identification at this frame.
> Neither is in the PE3 record.
>
> As re-keyed, this remains an evaluation instance of the
> avatar-coherent form of (WF-psi), not another definition of the
> twist; the untwisted tooth \(\psi_3(Y_{\rm av})\ne0\) fired at the
> same frame, pricing the re-scope on the nose.”

**DERIVATION:** `[INSTANCE — re-keyed]` The PE3 record is avatar-keyed on both sides: the report's own text (XREF X18) reads “**w MEASURED at a 7th prime, first carrier-(8,4) geometry: w = z2 != 1**; **PSIW: psi_3^{(w)}(Y) = 0 — THEOREM GENTOW5-B(a) AS** [re-scoped]”, and the class key is glossed in the report as `(Y = the FGMN class avatar)` (XREF X19; the phrase is line-wrapped in the PE3 source between `FGMN` and `class`, so it matches only modulo that wrap). The untwisted tooth is XREF X20, “**NOSE TOOTH FIRED: psi_3(Y) != 0**”.

**SUPERSEDED PREDECESSOR.** The base I4 boxed \(\boxed{\psi_3^{(w_2)}(Y)=0.}\) — an intrinsic key on an avatar-keyed record. That is the r5 finding F3; the box is `[SUPERSEDED]`.

**CONDITIONALITY:** avatar key throughout; no intrinsic evaluation is claimed; (C3-ladder-nonvanishing) is explicitly *not* the operative question at this row.

**RESOLUTION TRACE:** base I4 → r5 item 4, full replacement. r5 governs.

**TEETH:** §7 Pass 1(5), Pass 1(6), Pass 2(4).

---

### EFF.T5.26  [instance-record]

**CANONICAL STATEMENT — I5's head, as replaced by r1 and then r2** `[ASSEMBLED: r1's item-4 rewrite with r2's item-2 replacement of its \`where\`-block]`:

> “### I5 — conditional `[GENTOW5-W(i)]` supplier
>
> **Statement pin:** companion note §S1, matching GENTOW5_PROOF S11.2 at `b0e5c648`, lines 1912–1913.
>
> For every permitted \(i\ge3\), the pinned equality is consumed here only with its cumulative hypothesis:
>
> \[
> \boxed{
> \mathcal W_{\le i}
> \Longrightarrow
> \left[
> u_i(\beta_t)
> =
> \theta_i(t)w_i^{f_{i+1}-t}
> \right],
> }
> \]”

then r2's notation bridge in place of the base `where`-block:

> “The companion’s symbol \(\bar k_i\) and this note’s canonical symbol
> \(\bar\kappa_i\) denote the same normalized slope. Explicitly, throughout
> this instance pin,
>
> \[
> \boxed{
> \bar k_i=\bar\kappa_i
> :=
> \frac{u_{i+1}}{e(\mu_i)}.
> }
> \]
>
> Accordingly, the supplier’s displayed definition is read as
>
> \[
> w_i
> =
> R_{i+1,\bar k_i}
> \bigl(\widehat n_i(u_{i+1})\bigr)
> =
> R_{i+1,\bar\kappa_i}
> \bigl(\widehat n_i(u_{i+1})\bigr),
> \]
>
> which is exactly (WF-w), and
>
> \[
> \theta_i(t)
> =
> \Theta_i(t)
> =
> \vartheta_{i,f_{i+1}-t}^{-1}.
> \]
>
> The first expression for \(w_i\) preserves the supplier’s notation; the
> second is the same expression in the canonical notation of this note.
> No second definition of \(w_i\) is introduced.”

**DERIVATION:** `[CONDITIONAL IMPORT + notation bridge]` The pin is object-addressed and re-verified byte-literally at compile time (XREF C02): `git show b0e5c648:…/GENTOW5_PROOF_2026-08-09.md` lines 1912–1913 read exactly

```
      [GENTOW5-W(i)]   u_i(beta_t) = theta_i(t) * w_i^{f_{i+1}-t},
                       w_i = R_{i+1,kbar_i}(nhat_i(u_{i+1})),
```

which is why the r2 bridge is needed at all: the supplier writes `kbar_i`, T5 writes \(\bar\kappa_i\). r2 declares itself notation-only: “This repair changes notation only. It leaves all r1 scope restrictions, cumulative hypotheses, supplier ranges, corrected twist indices, and instance records unchanged.”

**SUPERSEDED PREDECESSOR.** The base I5 heading read “certified `[GENTOW5-W(i)]`” and asserted the bare equality plus “The companion certifies this for every permitted \(i\ge3\).” Both are `[SUPERSEDED]` by r1's implication form.

**CONDITIONALITY:** \(\mathcal W_{\le i}\), displayed inside the box.

**RESOLUTION TRACE:** base I5 → r1 item 4 (head through the paragraph before LW3) → r2 item 2 (the `where`-block). Both govern their spans; r1 explicitly retains the rest: “Retain the existing LW3 paragraph and the remainder of I5 unchanged.”

**TEETH:** §7 Pass 1(1) as replaced by r2; Pass 2(1), Pass 2(3).

---

### EFF.T5.27  [scope-record]

**CANONICAL STATEMENT — the r4-G1 companion re-reading (this is the note's single hardest scope judgment):**

> “The companion note itself DOES claim the promotion: its title and
> verdict read "[GENTOW5-W(i)] is PROVED at every level i >= 3", and
> its S4 asserts that the S3(a) algebra "runs UNCONDITIONALLY at
> i >= 3" with the fence's conditional list "DISCHARGED: those clauses
> now rest on this note's lemma instead of an open hypothesis." Those
> claims consume THEOREM GENTOW5-B clause (b) as an unconditional
> w-blind fact — at index \(i-1\) in the companion's proof step (iv)
> ("a w-blind clause, unconditional per the S11.2 scope walk") and at
> index \(i\) in its r1 nonemptiness derivation ("S11.2 expressly
> places the repo-side P-membership clause outside the
> [GENTOW5-W(i)] fence and classifies it as w-blind") — citing the
> S11.2 scope walk at `b0e5c648` L1925–1928. GENTOW5's later S12.1
> append expressly WITHDRAWS that scope walk ("its subsequent
> assertion that exact values, repo-side residue classes,
> P-membership, clause (c), and clause (e) remain unconditional is
> WITHDRAWN") and places ALL of clause (b) at index \(n\ge3\),
> P-membership included, behind \(\mathcal W_{\le n}\).
>
> Read under S12.1, the companion's written computation can be
> consumed as at most the conditional step
>
> \[
> \mathcal W_{\le i-1}
> \Longrightarrow
> [\mathrm{GENTOW5\text{-}W}(i)]
> \qquad(i\ge3;\ \mathcal W_{\le2}\ \text{the empty conjunction}),
> \]
>
> since its step-(iv) clause-(b) leg sits at index \(i-1\); its r1
> nonemptiness derivation does not survive as written (its
> clause-(b)-at-index-\(i\) leg now sits behind \(\mathcal W_{\le i}\),
> which would be circular there), so S1's "with \(P_{i+1}\) nonempty"
> reverts to a recorded setting assumption. Whether that conditional
> step, and the induction closing the conjuncts from the empty base,
> survives on the companion's full footprint is the companion's own
> re-verification obligation; it is NOT performed by the companion as
> written and NOT assumed here. Timeline of record: the companion was
> CERTIFIED 2/2 at 09:49 (runs/cert/R10_wi_pass3_output.log) — a pass
> whose own no-circularity walk reads clause (b) as "unconditional,
> w-blind" — while the S12.1 append was derived 11:31–11:34
> (runs/cert2/progress.txt, E12) and folded at 11:40 (commit
> `fa05591f`); the certification is a fact of the record, but its
> scope reading is superseded by S12.1. Accordingly this note consumes
> the companion's pinned display only within the explicit cumulative
> hypothesis \(\mathcal W_{\le i}\) displayed above — a consumption
> every reading supports — and does not adopt the companion's
> unconditional self-description.”

**DERIVATION:** `[SCOPE ADJUDICATION — four legs]`

Leg 1: the companion's self-description is quoted, verified — XREF X21 (`PROVED at every level`, count 1 in `GENTOW5_WI_2026-08-10.md`) and X22 (`runs UNCONDITIONALLY at`, count 1).

Leg 2: those claims rest on S11.2's scope walk at `b0e5c648` L1925–1928, which the r4 census independently read: “L1925–1928 = the scope walk ("… the repo-side residue classes and P-membership clauses, (c)'s budgets, (d), (e) — w-blind there")”.

Leg 3: S12.1 withdraws exactly that scope walk — XREF X23 (`is WITHDRAWN`, count 2 in GENTOW5_PROOF) — and places all of clause (b) at \(n\ge3\) behind \(\mathcal W_{\le n}\).

Leg 4: index arithmetic. The companion's step (iv) uses clause (b) at index \(i-1\), so it survives as \(\mathcal W_{\le i-1}\Rightarrow[\mathrm{GENTOW5\text{-}W}(i)]\); its r1 nonemptiness derivation uses clause (b) at index \(i\), which under S12.1 sits behind \(\mathcal W_{\le i}\) — the very thing being derived — hence circular, hence withdrawn as a derivation and demoted to a recorded setting assumption.

Timeline verified: commit `fa05591f` exists (XREF C12); the certification/append ordering (09:49 vs 11:31–11:40) is the note's own record, evidenced by `runs/cert/R10_wi_pass3_output.log` and `runs/cert2/progress.txt`.

**SUPERSEDED PREDECESSOR.** r1's closing sentence for I5 — “The companion records the required sitewise conjunct for each permitted \(i\ge3\); it does not promote the displayed implication to a hypothesis-free law.” — is `[SUPERSEDED]` by r4-G1. The r1 sentence was *too weak in the wrong direction*: it implied the companion made no promotion claim, when in fact it does and the claim is superseded by S12.1.

**CONDITIONALITY:** T5 consumes only \(\mathcal W_{\le i}\Rightarrow[\dots]\). The stronger conditional step \(\mathcal W_{\le i-1}\Rightarrow[\mathrm{GENTOW5\text{-}W}(i)]\) — which, with the empty base \(\mathcal W_{\le2}\), would close the induction and discharge every conjunct — is **explicitly NOT assumed here**. Ledger row HYP.63 records the same verdict: “CARRY — WI proves only the induction step, not induction from empty base.”

**RESOLUTION TRACE:** base I5 tail → r1 item 4's closing sentence → r4 item 2 (G1), full replacement. r4 governs.

**TEETH:** §7 Pass 1(2), Pass 2(2), Pass 2(5).

---

### EFF.T5.28  [regression-record]

**CANONICAL STATEMENT — I5's retained LW3 record, with r5's appended site obligation:**

> “Its LW3 instance records
>
> \[
> w_3=1,\qquad
> u_3(\beta_1)=1,\qquad
> u_3(\beta_0)=2=\theta_3(0)w_3^2,
> \qquad
> \psi_4^{(w_3)}(Y)=0.
> \]
>
> The same frame has
>
> \[
> w_{\mathrm{av}}=2\ne w_3.
> \]
>
> Thus the avatar is not substituted for (WF-w). LW3 also kills the
> theta-free formula \(u_i(\beta_t)=w_i^{f_{i+1}-t}\).”

> “Site obligation: \(\psi_4^{(w_3)}\) divides by \(w_3\);
> (C3-ladder-nonvanishing) at \(i=3\) is supplied by S11.2's proved
> fixed-unit list, and the measured \(w_3=1\ne0\) confirms it live at
> LW3.”

**DERIVATION:** `[INHERITED EVIDENCE — independently reproduced]` The rows are read off the sealed WI instrument artifact, and the r4 census re-ran the instrument in isolation: “a fresh isolated run of `verification/openmath/gentow5_wi_checks.py` today exited 0 with "== SUMMARY: 60 checks, 0 violations ==" and output BIT-IDENTICAL to the committed artifact (md5 `b0a8c3c4b5857e90929fff9c340ae870`; wall time 0.8 s) — the census is reproduced, not inferred from any note's summary.” The artifact rows quoted there are “MEASURED w* = 1”, “ok: P-D3' LAW-I1 norm(Y) = 2 tr(Y)^2”, “ok: P-D3' LAW-I2 psi_4^{(w*)}(Y) = 0 (single-w display)”, “ok: P-D4' TOOTH psi_4^{(w')}(Y) != 0”, “MEASURED w_av = 2 (avatar; row-23 record)”, “ok: P-D7' TOOTH naive twin VERDICT KILL: (e,f) != (8,2) (measured N-EF: 1 16 1)”.

Two negative results are load-bearing: \(w_{\rm av}=2\ne w_3=1\) is the measured avatar–intrinsic gap (`WFRAME OPEN-3`), and \(\theta_3(0)=2\ne1\) kills the theta-free law \(u_i(\beta_t)=w_i^{f_{i+1}-t}\) (which at \(w_3=1\) would force \(u_3(\beta_0)=1\), against the measured \(2\)).

**CONDITIONALITY:** a regression record, not coverage (honesty item 10).

**RESOLUTION TRACE:** base I5 LW3 paragraph, retained verbatim by r1 (“Retain the existing LW3 paragraph and the remainder of I5 unchanged.”), with r5 item 2's site-obligation append.

**TEETH:** §7 Pass 1(6), Pass 2(4), Pass 2(6).

---

### EFF.T5.29  [hypothesis]

**CANONICAL STATEMENT — §4's opening, as replaced by r1:**

> “For \(n\ge3\), retain S12.1’s cumulative hypothesis
>
> \[
> \mathcal W_{\le n}
> :=
> \bigwedge_{3\le q\le n}
> [\mathrm{GENTOW5-W}(q)].
> \]
>
> Certification of the WI companion does not discharge these conjuncts from the frozen GENTOW5 statements. Both the higher-rung supplier law and every downstream conclusion retain their explicit cumulative hypothesis.”

**DERIVATION:** `[IMPORTED HYPOTHESIS]` The definition is S12.1's, verified in GENTOW5_PROOF (XREF X12); the non-discharge sentence is r1's repair, whose full argument arrives at r4-G1 (EFF.T5.27).

**⚠ SUPERSEDED-BUT-PHYSICALLY-PRESENT PREDECESSOR (load-bearing).** The base sentence it replaces says the OPPOSITE:

> “The certified WI note discharges these conjuncts in its tower range.”

That sentence is at T5 L299 and **is still in the file**, because r1 supplied a replacement rather than a deletion. `spec/HYPOTHESIS_LEDGER.md` HYP.100 (`contradictory T5 sentence`, RX-row) records the pair: “WHERE: T5 stale L299 versus governing L616. STATEMENT: Stale text says WI discharges the conjuncts; governing text says it does not. CONSUMED BY: extraction of HYP.63 status. CLASS: corpus-consistency-item. PROPOSED DISPOSITION: NODE — strike/append stale sentence.” **The effective text is L616.** A consumer that reads the note linearly without applying r1 gets the wrong answer on the single most consequential question in the note.

**CONDITIONALITY:** \(\mathcal W_{\le n}\) at \(n\ge3\), undischarged.

**RESOLUTION TRACE:** base §4 opening (L290–299) → r1 item 5, replacing “the opening of §4 through the sentence before `(WF-chain)`”. r1 governs.

**TEETH:** §7 Pass 1(3), Pass 2(5).

---

### EFF.T5.30  [record]

**CANONICAL STATEMENT:**

> “The proof dependency in frozen GENTOW5 remains
>
> \[
> \boxed{
> \mathcal W_{\le n}
> \Longrightarrow (a)@n
> \Longrightarrow \mathrm{Corollary\ 6.4}
> \Longrightarrow (b)@n
> \Longrightarrow \mathrm{S11.1.1/S11.1.3}
> \Longrightarrow (c)@n
> \Longrightarrow (e)@n
> \Longrightarrow\text{the next automatic floor}.
> }
> \tag{WF-chain}
> \]”

**DERIVATION:** `[IMPORTED]` The r4 census verified the chain against S12.1 (XREF X12, X24): the source displays “[GENTOW5-W(n)] ⟹ (a)@n ⟹ Corollary 6.4 ⟹ (b)@n ⟹ S11.1.1/S11.1.3 ⟹ (c)@n ⟹ (e)@n ⟹ the next automatic floor”, read “per the corrigendum, "under the ambient hypothesis 𝒲_{≤n} as a whole" — the form (WF-chain) displays”. **Note the deliberate difference:** the source's antecedent is the single conjunct `[GENTOW5-W(n)]`; T5's display promotes it to the cumulative \(\mathcal W_{\le n}\), on the corrigendum's own instruction. This is the one place T5's display is *stronger in hypothesis* than the raw source line, and the census records why.

**CONDITIONALITY:** \(\mathcal W_{\le n}\) governs every arrow.

**RESOLUTION TRACE:** base (WF-chain) → r1 item 5 re-displays it identically (only the surrounding prose changed). The display itself is unchanged from base.

**TEETH:** §7 Pass 1(3).

---

### EFF.T5.31  [scope-record]

**CANONICAL STATEMENT:**

> “Consequently:
>
> 1. Clause (a)’s realized residual, admissibility, key-polynomial membership, prime/irreducibility certificate, and non-equivalence consume \(\mathcal W_{\le n}\).
>
> 2. Clause (b)’s one-sidedness, corrected residual powers, exact values, repo-side residue classes, and \(P\)-membership consume \(\mathcal W_{\le n}\).
>
> 3. Clause (c), including sufficiency, deeper-read ascent, fibration, node pins, and the field-floor conclusion, consumes \(\mathcal W_{\le n}\). S11.1.2’s numerical \(\rho\)-ladder remains unconditional floor arithmetic.
>
> 4. Clause (e)’s automatic next-floor conclusion consumes \(\mathcal W_{\le n}\). A floor inequality explicitly included in the fixed datum remains independently available.”

**DERIVATION:** `[RECORD]` These four rows are the arrow-by-arrow reading of (WF-chain). Two carve-outs are explicit and must not be lost: S11.1.2's numerical \(\rho\)-ladder (unconditional floor arithmetic) and an explicitly-included floor inequality in the fixed datum. Everything else consumes \(\mathcal W_{\le n}\).

**CONDITIONALITY:** \(\mathcal W_{\le n}\) on all four items except the two named carve-outs.

**RESOLUTION TRACE:** base §4; outside r1's replacement span (which ends at the sentence before (WF-chain)), so base text governs unchanged.

**TEETH:** §7 Pass 1(3), Pass 2(5).

---

### EFF.T5.32  [definition]

**CANONICAL STATEMENT:**

> “The clause-(b) residual powers use the corrected index
>
> \[
> \boxed{
> R_{j-1}(\Phi_{n+1})
> =
> \bigl(\psi_j^{(\omega_{j-1})}\bigr)^{
> l_{j+1}\cdots l_{n+1}}
> \qquad(2\le j\le n+1),
> }
> \tag{WF-index}
> \]
>
> where
>
> \[
> \omega_1
> =
> \text{S11.3's fixed letter-power unit for }\psi_2,
> \qquad
> \omega_2=w_2,
> \qquad
> \omega_q=w_q\quad(q\ge3).
> \]”

**DERIVATION:** `[IMPORTED — the dated corrigendum]` Verified by the r4 census against GENTOW5_PROOF's dated corrigendum (XREF X25): “read every ψ_j^{(ω_j)} in S12.1's clause (b) as ψ_j^{(ω_{j−1})} with ω₁ = the S11.3 unit”; “ψ₂ carries S11.3's fixed letter-power unit, ψ₃ carries w₂, and ψ_j carries w_{j−1} for j ≥ 4”; and the reason the shift was forced: “The display as first written demanded w_{n+1} at the top term, exceeding the assumed 𝒲_{≤n}”. That last sentence is the whole point of the correction — an off-by-one that would have required a conjunct outside the assumed hypothesis.

**CONDITIONALITY:** \(2\le j\le n+1\); every \(\psi_j^{(\omega_{j-1})}\) leg carries (C3-ladder-nonvanishing) per EFF.T5.09, and the \(j=2\) leg is scoped to \(\omega_1\)'s own letter-power form, NOT to a displayed \(w_1\).

**RESOLUTION TRACE:** base §4; explicitly left unchanged by r1 (“This repair leaves (WF-index), including the top twist \(w_n\), unchanged.”).

**TEETH:** §7 Pass 1(4), Pass 2(6).

---

### EFF.T5.33  [fence]

**CANONICAL STATEMENT:**

> “Thus:
>
> - \(\psi_2\) carries \(\omega_1\);
> - \(\psi_3\) carries \(w_2\);
> - \(\psi_j\) carries \(w_{j-1}\) for \(j\ge4\).
>
> The top term consumes \(w_n\), never \(w_{n+1}\). The outer \(\psi_1\) leg remains separately accepted.”

> “The \(i=2\) rung is unconditional. The \(i=1\) residual remains S11.3’s fixed-letter-power statement. The unaffected scope listed in S12.1 remains unaffected.”

**DERIVATION:** `[FENCE]` The “never \(w_{n+1}\)” clause is the corrigendum's operative constraint (EFF.T5.32's derivation). The two boundary statements are verified against S12.1 by the r4 census (XREF X12): “The i = 2 rung remains unconditional by the landed GENTOW2 law; the i = 1 residual remains exactly S11.3's fixed-letter-power statement”.

**CONDITIONALITY:** none — these are the scope boundaries themselves.

**RESOLUTION TRACE:** base §4 tail; unchanged by r1–r5.

**TEETH:** §7 Pass 1(4), Pass 2(3).

---

### EFF.T5.34  [fence]

**CANONICAL STATEMENT — honesty ledger items 1–7** `[ASSEMBLED: base items 4–7 with r2's item 1 and r1's items 2–3 substituted in place]`:

> “1. Every \(w_i\) and \(\psi_{i+1}^{(w_i)}\) here means (WF-w) and
>    (WF-psi). In I5 the supplier notation is explicitly bridged by
>
>    \[
>    \bar k_i=\bar\kappa_i:=\frac{u_{i+1}}{e(\mu_i)}.
>    \]
>
>    Thus the pinned supplier expression is literally the canonical
>    definition after notation identification; riders do not redefine it.”

> “2. (WF-law\(_2\)) and (WF-realized\(_2\)) are unconditional. For every permitted \(i\ge3\), the asserted scope is
>
>    \[
>    \mathcal W_{\le i}
>    \Longrightarrow
>    \text{\rm(WF-law)}_i
>    \Longrightarrow
>    \text{\rm(WF-realized)}_i.
>    \]
>
>    No \(i=1\) version is asserted.
>
> 3. Downstream GENTOW5 clauses at \(n\ge3\) retain the dependency \(\mathcal W_{\le n}\). Certification of a companion does not discharge or erase that hypothesis or any dependency arrow.”

> “4. Exact values, residue classes, \(P\)-membership, clauses (c) and (e) are not made unconditional by per-grade fixedness. S12.1’s withdrawal governs.
>
> 5. Clause (b) uses \(\psi_j^{(\omega_{j-1})}\), not the superseded \(\psi_j^{(\omega_j)}\).
>
> 6. The recipe uses \(\vartheta_{i,s}\); the B-law uses its inverse \(\Theta_i(t)\).
>
> 7. There is no universal \(w=1\) claim. PE6 already records \(w_2\ne1\) at six primes. What remains open is a structural or closed-form criterion for \(w_i\), including when it equals \(1\).”

**DERIVATION:** `[FENCE]` Item 1 ↔ EFF.T5.04/.06/.26; items 2–3 ↔ EFF.T5.12/.14/.18/.19/.29; item 4 ↔ EFF.T5.16/.31; item 5 ↔ EFF.T5.32; item 6 ↔ EFF.T5.11; item 7 ↔ EFF.T5.24 and `WFRAME OPEN-1`.

**⚠ INTERNAL TENSION (recorded, not resolved).** Item 7's “PE6 already records \(w_2\ne1\) at six primes” is the pre-r5 reading. r5's F2 re-scope (EFF.T5.24) proves **intrinsic** \(w_2\ne1\) at three frames only and records the other measurements as **avatar-keyed**; r5's new item 14 states the corrected scope. r5 did not restate item 7. OPEN-CALL 2 (§9).

**CONDITIONALITY:** none; these are the note's own fences.

**RESOLUTION TRACE:** base items 1–7 → r2 item 3 (item 1) → r1 item 6 (items 2, 3). Items 4–7 base-unchanged.

**TEETH:** §7 Pass 1(5), Pass 1(6), Pass 2(5), Pass 2(6).

---

### EFF.T5.35  [fence]

**CANONICAL STATEMENT — honesty ledger items 8–14** `[ASSEMBLED: base items 8–12 with r5's appended items 13–14]`:

> “8. The avatars \(z_1,z_1^2,z_2^2,z_2\) are measured instances. LW3 shows that an avatar can differ from intrinsic \(w_i\).
>
> 9. There is no theta-free law. LW3’s \(\theta_3(0)=2\) is a live counterexample.
>
> 10. The six PE6 primes, seventh-prime PE3 check, and LW3 are regression records, not exhaustive coverage.
>
> 11. Unit twisting preserves factor patterns, not literal coefficients or roots. Splitting conclusions require the tower theorem.
>
> 12. No accepted or byte-frozen source is rewritten.”

> “13. \(w_i\in K_i^\times\), division by \(w_i\), and
>     unit-substitution transport are asserted only at sites carrying
>     (C3-ladder-nonvanishing) (T1 r8): supplied at \(i=2\) by
>     GENTOW2-B\('\)(3)/B\(''\), at \(i\ge3\) by S11.2's proved
>     fixed-unit list, and at \(i=1\) by NOTHING — the \(i=1\)
>     display is scoped to WFRAME OPEN-2. At \(i\ge3\),
>     \(\Theta_i(t)\) additionally carries
>     \(\mathrm{(H\!-\!VARTHETA\!-\!RES)}_i\) (T1 r9).
>
> 14. I3's intrinsic \(w_2\ne1\) is PROVED at the IDY-closed frames
>     LD1/GA1/GA3 only; at LD3 and in I4 the records are
>     avatar-keyed, with the avatar–intrinsic identification riding
>     the descriptive row 23.”

**DERIVATION:** `[FENCE]` Item 8 ↔ EFF.T5.07/.28; item 9 ↔ EFF.T5.28 (the \(\theta_3(0)=2\) counterexample); item 10 ↔ EFF.T5.24/.25/.28; item 11 ↔ EFF.T5.08; item 12 ↔ EFF.T5.02; item 13 ↔ EFF.T5.09/.10; item 14 ↔ EFF.T5.24/.25. Items 13–14 are r5's additions and carry the same staleness flag as EFF.T5.09 (one-clause gate vs T1 HEAD's two-clause gate).

**CONDITIONALITY:** none.

**RESOLUTION TRACE:** base items 8–12 unchanged; r5 item 2 appends 13–14.

**TEETH:** §7 Pass 1(5), Pass 1(6), Pass 2(4), Pass 2(6).

---

### EFF.T5.36  [open-obligation]

**CANONICAL STATEMENT:**

> “**WFRAME OPEN-1 — structural value law.** Find a closed formula or intrinsic criterion for \(w_i\), including a criterion for \(w_i=1\).
>
> **WFRAME OPEN-2 — outer tie.** Identify S11.3’s fixed letter-power unit with the displayed \(w_1\), or retain them as distinct normalizations.
>
> **WFRAME OPEN-3 — avatar bookkeeping.** Determine the exact gap between a uniformizer avatar and the intrinsic read (WF-w). LW3 proves the gap can be nontrivial.”

**DERIVATION:** `[OPEN OBLIGATIONS]` All three are carried downstream in `spec/HYPOTHESIS_LEDGER.md` as rows HYP.64, HYP.65, HYP.66 (reverse-XREFs RX-3–RX-5), each with `WHERE: T5_WFRAME_2026-08-12.md` and a line cite. HYP.64 records “CONSUMED BY: no current capstone leg”; HYP.65 “CONSUMED BY: entire i=1 stratum, presently excluded”; HYP.66 records that the gap “is now MEASURED, not merely known”.

**CONDITIONALITY:** these are the standing open items, re-displayed at acceptance (EFF.T5.45).

**RESOLUTION TRACE:** base §6; unchanged by r1–r5.

**TEETH:** §7 Pass 1(5), Pass 2(6).

---

### EFF.T5.37  [battery-frame]

**CANONICAL STATEMENT — Pass 1** `[ASSEMBLED: base items 3–6 with r2's item 1 and r1's item 2 substituted in place]`:

> “A repair resets the counter.”

> “1. Check (WF-w) and (WF-psi) literally against T1 C3, and check that I5
>    retains the explicit bridge
>    \(\bar k_i=\bar\kappa_i=u_{i+1}/e(\mu_i)\), so its supplier pin is
>    exactly (WF-w) in the canonical notation.”

> “2. Check the unconditional \(i=2\) law against GENTOW2-B\(^{\prime\prime}\); check that every permitted \(i\ge3\) occurrence retains \(\mathcal W_{\le i}\) and uses `[GENTOW5-W(i)]` only as its newest conjunct; reject any \(i=1\) promotion.”

> “3. Walk every arrow of (WF-chain) against S12.1.
> 4. Instantiate (WF-index) at \(j=2,3,4,n+1\); the top must use \(w_n\).
> 5. Reject any universal inference from the avatar or seven-prime records.
> 6. Lint all \(w=1\), avatar-\(w\), and theta-free claims against PE6 and LW3.”

**DERIVATION:** `[BATTERY FRAME]` Item 1 gained r2's notation-bridge clause; item 2 gained r1's cumulative-hypothesis clause.

**CONDITIONALITY:** none.

**RESOLUTION TRACE:** base §7 Pass 1 → r2 item 4 (item 1) → r1 item 7 (item 2).

**TEETH:** this IS the battery; see §6.

---

### EFF.T5.38  [battery-frame]

**CANONICAL STATEMENT — Pass 2** `[ASSEMBLED: base items 2, 6 with r3's items 1, 4 and r1's items 3, 5 substituted in place]`:

> “1. Recheck every claimed span pin against the repository object and
>    record, for each pin, the exact commit hash or immutable ledger
>    identifier, the file or ledger entry, the checked span, and a
>    pass/fail result. A hash or locator merely copied from this note is
>    not an independent verification.”

> “2. Verify that T1 supplies only the dictionary/cancellation and that each B-law names its supplier.”

> “3. Check the three boundary regimes: \(i=1\) open tie, \(i=2\) unconditional, and \(i\ge3\) conditional under the cumulative history \(\mathcal W_{\le i}\).”

> “4. Reproduce and record the measurement census from its repository
>    artifacts: the six PE6 primes/four frames, the PE3 seventh-prime
>    check, WI run 2's 60/60 result, and the retained run 1 record. Record
>    the exact hash or immutable ledger identifier for every artifact
>    consumed; do not infer the census from this note's summary.”

> “5. Ensure that neither (WF-law), (WF-realized), nor any conclusion behind S12.1’s chain is labeled unconditional merely because a companion note reports successful verification of the required conjuncts.”

> “6. Lint for unquoted positive occurrences of the superseded \(\psi_j^{(\omega_j)}\), theta-free power laws, universal \(w=1\), or a general avatar identity.”

**DERIVATION:** `[BATTERY FRAME]` r3 hardened items 1 and 4 into per-entry, do-not-trust-this-note obligations; r1 hardened items 3 and 5 into cumulative-hypothesis obligations. Item 5's final form is the direct battery encoding of EFF.T5.27's verdict.

**CONDITIONALITY:** none.

**RESOLUTION TRACE:** base §7 Pass 2 → r1 item 7 (items 3, 5) → r3 items 3, 4 (items 1, 4).

**TEETH:** this IS the battery; see §6.

---

### EFF.T5.39  [acceptance-box]

**CANONICAL STATEMENT — the acceptance gate and the eight-item ledger (r3):**

> “The acceptance gate is
>
> \[
> \boxed{
> \text{two consecutive clean hostile passes}
> +
> \text{a complete independently verified span-pin ledger}.
> }
> \]
>
> A complete ledger must contain successful repository checks for:
>
> 1. T1 C3, including (C3-B-law), (C3-canonical-read), and
>    (C3-three-reads), with T1 r2's A7 re-scope;
> 2. LEMMA GENTOW2-B\(^{\prime\prime}\);
> 3. GENTOW2-PE6;
> 4. GENTOW5-PE3;
> 5. GENTOW5 S12.1;
> 6. the dated \(\psi_j^{(\omega_{j-1})}\) corrigendum;
> 7. companion note §S1 and GENTOW5_PROOF S11.2, whose claimed locator is
>    commit `b0e5c648`, lines 1912–1913;
> 8. WI run 1 and WI run 2, including an independently reproduced 60/60
>    census.
>
> For each entry the ledger must record the exact commit hash or immutable
> ledger identifier, the repository path or ledger entry, the checked
> span, and the result. An unverified, missing, or inaccessible entry
> leaves the gate unmet.”

**DERIVATION:** `[ACCEPTANCE FRAME]` The base gate was

> \[
> \boxed{
> \text{two consecutive clean hostile passes}
> +
> \text{all instance span pins verified}.
> }
> \]

and r3 replaced the second conjunct with the eight-item, per-entry-locator ledger. Both conjuncts are now met — the ledger at r4/r5 (EFF.T5.40–.43), the passes at acceptance (EFF.T5.45).

**CONDITIONALITY:** none.

**RESOLUTION TRACE:** base §7 gate → r3 item 5, full replacement. r3 governs.

**TEETH:** §7 in full; see also §7 of this specification.

---

### EFF.T5.40  [record]

**CANONICAL STATEMENT — the verification disposition, as replaced by r5:**

> “### Current verification disposition”

> “As of r5, every entry of the eight-item ledger above carries an
> independent repository check AND a locator-compliant record: the r5
> locator table below gives, per entry, the repository path, the
> exact commit hash (computed by `git log -1 --format=%H -- <path>`,
> or the immutable git object where the pin is object-addressed), and
> the checked span. Entry 1 (T1) is re-verified against T1 AT ITS
> CURRENT commit, with the r8/r9 deltas recorded. The acceptance
> gate's evidence component is met in the per-entry form r3 demanded;
> the two consecutive clean hostile passes remain owed (the
> r5-precipitating pass found 0C + 4G + 1m, and the counter resets
> with these repairs).”

**DERIVATION:** `[RECORD]` Three-generation supersession chain: r3 created the section reporting a total block (“the workspace executor again failed before any repository command could run because no `bwrap` launcher was available”; “no repository span pin was independently checked”); r4 replaced the body with “As of r4, every entry … carries an independent repository check”; r5 replaced it again with the locator-compliant form. **The r3 disposition is `[SUPERSEDED]` in every bullet and must not be quoted as HEAD state:**

> “- no repository span pin was independently checked;
> - the claimed `b0e5c648`, lines 1912–1913 locator was not independently
>   confirmed;
> - the PE6, PE3, and WI measurement records were not independently
>   reproduced;
> - the WI 60/60 census was not independently confirmed;
> - no hostile-pass credit is awarded.” The heading is retained; only the body was replaced.

**CONDITIONALITY:** the “two consecutive clean hostile passes remain owed” clause is `[SUPERSEDED]` by EFF.T5.45.

**RESOLUTION TRACE:** r3 item 5 (creates) → r4 item 3 (replaces body) → r5 item 5 (replaces body). r5 governs.

**TEETH:** §7 Pass 2(1), Pass 2(4).

---

### EFF.T5.41  [table]

**CANONICAL STATEMENT — the r4 eight-entry census-verification record.**

> “Each entry: the note's claim → the independently checked source
> quote/value → verdict. Checks performed in-repo on 2026-08-12
> (working tree at `runs/comp11` state; git objects addressed by hash).”

`[TABLE]` (the note displays this as eight numbered prose entries; transcribed as a verdict table with each entry's decisive source quote, no reordering and no verdict changed)

| # | Ledger item / instance | Decisive source quote (verbatim from the census) | Verdict |
|---|---|---|---|
| 1 | T1 C3 rows + r2 A7 re-scope (I1) | “(C3-Theta) boxes \(\Theta_i(t)=\vartheta_{i,f_{i+1}-t}^{-1}…\)”; “(C3-B-law) boxes \(u(\beta_t)=\Theta_i(t)w_i^{f_{i+1}-t}\) gated by "At every site where the stated ladder-multiplicativity obligation is discharged"”; “(C3-three-reads) boxes the canonical residual \(\psi_{i+1}^{(w_i)}(y):=w_i^{f_{i+1}}\psi_{i+1}(y/w_i)\) = (WF-psi)”; A7 verbatim “T1 only **supplies the gauge algebra to** the preceding B-law display. … Thus the B-law is no longer labeled 'absorbed verbatim.'” | **VERIFIED** |
| 2 | LEMMA GENTOW2-B″ (I2) | `git show 33f86204:…GENTOW2_PROOF_2026-08-09.md` L729–738: “**LEMMA GENTOW2-B″.** For each slot t < f₃, with w := u(κ̄) = R_{3,κ̄}(n̂₂(u₃)): (6) u(β_t) = ϑ(t) · w^{f₃−t}. Consequently … R_{3,α}(Φ₃) = … = w^{f₃}·P(y/w) …”; ledger md5 `d4c8b8854e7b00e517eee10c7d7a042d`, md5-8 `d4c8b885` MATCHES; `statement_ledger.py --check`: “CHECK: 178 baseline statements; 0 changed, 0 deleted, 2 added.” / “GREEN: every baseline statement byte-stable (normalized).” | **VERIFIED** |
| 3 | GENTOW2-PE6 (I3) | “w-avatar = z₁ (LD1), z₁² (LD3), z₂² (GA1), z₂ (GA3) — ≠ 1 at all 6 primes of the 4 frames”; “Results (run-2 GREEN 68 checks / 0 violations)”; verdict “CLEAN — 0 CRITICAL, 0 GAP, 1 MINOR”; retained disclosure “the postregistered identification: avatar = R_{3,κ̄}(n̂₂(u₃)) — riding Thm 4.2/eq (16) (row 23," DESCRIPTIVE")”; PE6 also measured u(β₀) “DIRECTLY as its own canonical-frame ratio” | **VERIFIED** (avatar-mediated, exactly as I3 states) |
| 4 | GENTOW5-PE3 (I4) | “**w MEASURED at a 7th prime, first carrier-(8,4) geometry: w = z2 != 1**; **PSIW: psi_3^{(w)}(Y) = 0 — THEOREM GENTOW5-B(a) AS** [re-scoped]”; tooth “**NOSE TOOTH FIRED: psi_3(Y) != 0**” | **VERIFIED** |
| 5 | GENTOW5 S12.1 | “\(\mathcal W_{\le n}:= \bigwedge_{3\le q\le n}[{\rm GENTOW5\!-\!W}(q)]\)”; “its subsequent assertion that exact values, repo-side residue classes, P-membership, clause (c), and clause (e) remain unconditional is WITHDRAWN”; the chain display; “The i = 2 rung remains unconditional by the landed GENTOW2 law; the i = 1 residual remains exactly S11.3's fixed-letter-power statement” | **VERIFIED** |
| 6 | The dated \(\psi_j^{(\omega_{j-1})}\) corrigendum | “read every ψ_j^{(ω_j)} in S12.1's clause (b) as ψ_j^{(ω_{j−1})} with ω₁ = the S11.3 unit”; “ψ₂ carries S11.3's fixed letter-power unit, ψ₃ carries w₂, and ψ_j carries w_{j−1} for j ≥ 4”; “The display as first written demanded w_{n+1} at the top term, exceeding the assumed 𝒲_{≤n}” | **VERIFIED** |
| 7 | Companion §S1 ↔ S11.2 @ `b0e5c648` L1912–1913 (I5) | L1912–1913 read exactly “`[GENTOW5-W(i)]   u_i(beta_t) = theta_i(t) * w_i^{f_{i+1}-t},`” / “`w_i = R_{i+1,kbar_i}(nhat_i(u_{i+1})),`”; also L1904–1908 the S11.2 proved list and L1925–1928 the scope walk “… the repo-side residue classes and P-membership clauses, (c)'s budgets, (d), (e) — w-blind there” — “the sentence S12.1 has since withdrawn (item 5 above), which is G1's mechanism” | **VERIFIED** |
| 8 | WI instrument + run 1 + run 2 (I5's LW3) | `gentow5_wi_checks.py` = `c8c5d129a91837d5f6b2a5902601a4aa`; `gentow5_wi_checks_output.txt` = `b0a8c3c4b5857e90929fff9c340ae870`; `gentow5_wi_checks_output_run1_RED.txt` = `598d6704fc4348767a0a6f5f57705f73` — “all three md5-8 pins MATCH”; fresh isolated re-run “exited 0 with "== SUMMARY: 60 checks, 0 violations ==" and output BIT-IDENTICAL to the committed artifact … wall time 0.8 s” | **VERIFIED + REPRODUCED** |

with entry 8's phrasing as corrected by r5 (F5):

> “Run-1's SUMMARY line reads "== SUMMARY: 58 checks, 5 violations =="
> and is FOLLOWED by the five VIOLATION detail lines (P-D1 nfeltval
> ladder; P-D3 LAW-1; P-D3 LAW-3; P-D4 TOOTH; P-D6 TOOTH nose) — the
> artifact ends with that detail block, not with the summary line.
> Run-2's artifact does end at its summary line
> "== SUMMARY: 60 checks, 0 violations ==". Both re-confirmed by
> direct tail today.”

and the census close:

> “No MISMATCH was found at any ledger entry. This r4 preserves every
> r1 scope restriction, the r2 notation bridge, and r3's evidence
> gate; it changes the note's description of the companion (item 2)
> and records the completed evidence census (items 3–4).”

**DERIVATION:** `[MACHINE RECORD — independently re-verified at compile time]` **2026-08-14 re-verification:** all three md5 digests recomputed and MATCH exactly (`c8c5d129a91837d5f6b2a5902601a4aa`, `b0a8c3c4b5857e90929fff9c340ae870`, `598d6704fc4348767a0a6f5f57705f73`); the two object-addressed pin spans re-read from git and byte-matched (`b0e5c648` L1912–1913 and `33f86204` L729–738, XREFs C02 and C03); all sixteen commits named anywhere in the note verified to exist as git commit objects (§4.5).

**CONDITIONALITY:** entry 3 is verified *as avatar-mediated*, which is precisely what r5's F2 then re-scoped in I3. Entry 1's A7 quote is partly superseded — see EFF.T5.43.

**RESOLUTION TRACE:** r4 item 4 (creates), r5 item 6 (F5, entry 8's phrasing). r5 governs entry 8's last sentence.

**TEETH:** §7 Pass 2(1), Pass 2(4).

---

### EFF.T5.42  [table]

**CANONICAL STATEMENT — the r5 per-entry locator table:**

> “#### r5 locator table (per-entry exact commits) and entry-1 re-verification
>
> Computed 2026-08-12 at repo HEAD
> `c2c77b546647ae6de2693a0961d245e1b0b978fd`.”

`[TABLE]` (transcribed from the note's eight numbered entries; spans and hashes reproduced exactly)

| # | Repository path | Commit / object | Span |
|---|---|---|---|
| 1 | `lean/notes/openmath/T1_GAUGE_COCYCLE_2026-08-12.md` | `c2c77b546647ae6de2693a0961d245e1b0b978fd` (the r9 fold; = HEAD) | “the §1.1 (C2)/(C3) displays, §2.3, and the A7 row, in the r1–r9 effective text” |
| 2 | `lean/notes/openmath/GENTOW2_PROOF_2026-08-09.md` | current commit `9f29a28b9904df17c286c0c89246a6540159b010`; pin object-addressed at `33f86204d4b502edf55d2f5246eaa4175fd7abda` | L729–738 (immutable); “the pinned span is byte-identical at the current commit” |
| 3 | `lean/notes/openmath/GENTOW2_passPE6_report.md` | `deba21bd87f7d047761c4eb218070d67fc26da2d` | — |
| 4 | `lean/notes/openmath/GENTOW5_passPE3_report.md` | `39c36aa2e0387e7820701f41eb3a2fca6ef1a7eb` | — |
| 5 | `lean/notes/openmath/GENTOW5_PROOF_2026-08-09.md` | `cb010522aff06defbf0f4f9b69bb5da4ce61f0e8` | L2035–2133 (“header at L2035; the "Unaffected scope" close at L2133 — re-verified today, replacing r4's working-tree line cite”) |
| 6 | same file | same commit | L2299–2312 → **L2299–2313 per rider m-1** |
| 7 | companion `lean/notes/openmath/GENTOW5_WI_2026-08-10.md` @ `72eadc1d9cd3fab3126bdfc2735dad26fd75f0fd`; source `GENTOW5_PROOF_2026-08-09.md` object-addressed at `b0e5c648b2287d940ee404d0042168d5542c4a0f` | — | L1912–1913 (“immutable; the full hash of r4's short `b0e5c648`”) |
| 8 | `verification/openmath/gentow5_wi_checks.py` @ `4c95940959b501ac70655d45a5424a2b19f31c08`; run-2 artifact `…_output.txt` @ `ec30256cafe301b5087c81eb465a4e3e0e391800`; run-1 RED artifact `…_output_run1_RED.txt` @ `4c95940959b501ac70655d45a5424a2b19f31c08`; seal commit `07fa3676d5e1964d1ff11aa033149cc245457e77` | — | — |

with rider m-1's correction to entry 6, quoted:

> “(m-1) The r5 locator table's entry 6 span is L2299–2313 (the named
> closing sentence "Awaiting sol re-confirmation in the next queue."
> sits at L2313 at commit cb010522; the r5 line excluded its own
> endpoint by one).”

**DERIVATION:** `[MACHINE RECORD — independently re-verified]` **2026-08-14:** all eleven distinct hashes in this table were confirmed to be git commit objects, as were the five further commits named elsewhere in the note (`fa05591f`, `fb991138`, `e765aa9c`, `1950f588`, `0013cd60`) — sixteen for sixteen. The rider m-1 endpoint sentence `Awaiting sol re-confirmation in the next queue.` is verified present in `GENTOW5_PROOF_2026-08-09.md` at count 1 (XREF X26).

**CONDITIONALITY:** entry 1's commit is labelled “= HEAD”; rider m-2 dates that gloss to its check window (see EFF.T5.44) — at compile time HEAD is far later, and T1 has advanced to r13.

**RESOLUTION TRACE:** r5 item 5 (creates), rider m-1 (entry 6 span). The rider governs entry 6.

**TEETH:** §7 Pass 2(1).

---

### EFF.T5.43  [record]

**CANONICAL STATEMENT — the entry-1 re-verification and its r8/r9 deltas:**

> “**Entry-1 re-verification at `c2c77b54` (current), with the r8/r9
> deltas.** Timeline of the mutation r5 repairs: T1 r7 folded 21:29
> (`0013cd60`), r8 folded 21:54 (`1950f588`), this note's r4 census
> folded 22:21 (`e765aa9c`), T1 r9 folded 22:38 (`c2c77b54`) — so
> entry 1's source mutated between check and fold, and again after.
> Re-checked at `c2c77b54`: the (C3-Theta) box, the (C3-B-law) box
> with its site-gate sentence ("At every site where the stated
> ladder-multiplicativity obligation is discharged"), the
> \(\Gamma_t=c_t\vartheta_{i,f_{i+1}-t}\) display with "so the
> \(\vartheta/\Theta\) factors cancel", and the (C3-three-reads) box
> (= (WF-psi)) all stand as quoted at r4. Deltas: (r8) the census's
> quoted A7 sentence "The residual obligation is the accepted
> GENTOW2-B″ per-grade-unit theorem …" is SUPERSEDED in the effective
> text by r8's expanded replacement, which names GENTOW2-B′/B″, the
> slot-grade identity, graded scalar covariance, and the nonvanishing
> clause \(R_{i+1,\bar\kappa_i}(\widehat n_i(u_{i+1}))\ne0\); the
> flanking sentences ("T1 only supplies the gauge algebra to the
> preceding B-law display"; "… the B-law is no longer labeled
> 'absorbed verbatim'") survive. The three-reads and factor-pattern
> conclusions now sit behind the explicit site obligation
> (C3-ladder-nonvanishing) (R8-2) — carried into this note by r5
> item 2. (r9) (C2-vartheta)'s \(K_i^\times\) membership is fenced
> behind \(\mathrm{(H\!-\!VARTHETA\!-\!RES)}_i\) at \(i\ge3\) (R9-2)
> — carried at this note's \(\Theta_i(t)\) displays by r5 item 2; the
> A7 SPAN PIN 2 consequence list is re-scoped (preservation of
> irreducible-factor degrees/multiplicities is T1's §2.3
> factor-pattern conclusion, fenced by (C3-ladder-nonvanishing), NOT
> an assertion of the pinned B span) (R9-4); and the md5
> pin-normalization convention is now documented in T1's §3 (R9-5),
> retro-grounding this census's entry-2 mechanism note. Verdict:
> entry 1 re-VERIFIED at the current commit, conditional on the r5
> item-1/2 carries now displayed in this note.”

**DERIVATION:** `[MACHINE RECORD]` This is the note's own moving-target audit: T1 mutated between the r4 check and the r4 fold, and again afterwards, so r5 re-ran entry 1 against the then-current T1 and recorded the two deltas as carries (r5 item 2 = EFF.T5.09 and EFF.T5.10). All four T1 round designations are verified at compile time: `R8-2` (25 in T1), `R9-2` (2), `R9-4` (3), `R9-5` (4) — XREFs X27–X30 — and all four commits exist (§4.5).

**⚠ THE AUDIT'S OWN LESSON, UNAPPLIED ONE ROUND LATER.** This unit exists because T1 moved under the note. T1 then moved twice more (r12, r13) — see EFF.T5.09's staleness flag and OPEN-CALL 1.

**CONDITIONALITY:** “entry 1 re-VERIFIED at the current commit, conditional on the r5 item-1/2 carries now displayed in this note” — the verdict is explicitly conditional on the carries, and “current” means `c2c77b54`.

**RESOLUTION TRACE:** r5 item 5, appended to the census. New in r5.

**TEETH:** §7 Pass 2(1).

---

### EFF.T5.44  [changes-record]

**CANONICAL STATEMENT — the two folded post-pass-1 riders:**

> “**Dated riders (2026-08-12, post-pass-1 — the two minors of the clean
> Fable pass, folded per the minors-only precedent, no grade event).**
> (m-1) The r5 locator table's entry 6 span is L2299–2313 (the named
> closing sentence "Awaiting sol re-confirmation in the next queue."
> sits at L2313 at commit cb010522; the r5 line excluded its own
> endpoint by one). (m-2) The frame of record is T1 with r1–r10
> (fb991138 — a 27-line pure append to C7's M2/M3 rows, immaterial to
> every T5 pin per the pass's own audit); the census entry-1 "= HEAD"
> gloss is dated to its check window (22:38–22:49). Pass of record:
> the Fable-arm hostile pass returned VERDICT: CLEAN | 0C + 0G + 2m —
> clean pass 1 of 2.”

**DERIVATION:** `[RIDERS — folded]` Both are off-by-one/staleness corrections of exactly the kind the note's own §7 Pass 2(1) demands. m-1 fixes an endpoint; m-2 advances the frame pin one T1 round and time-stamps the “= HEAD” gloss. Commit `fb991138` verified to exist (XREF C16).

**CONDITIONALITY:** the riders are reading instructions on EFF.T5.42 and EFF.T5.01, not new hypotheses. m-2's characterization of r10 as “immaterial to every T5 pin” is the pass's audit, not a re-derivation by this compiler.

**RESOLUTION TRACE:** post-r5 dated riders, before the acceptance record.

**TEETH:** §7 Pass 2(1); the pass of record is PASS 1 of EFF.T5.45.

---

### EFF.T5.45  [acceptance-box]

**CANONICAL STATEMENT:**

> “## ACCEPTANCE RECORD (2026-08-12): T5 ACCEPTED 2/2 (model-diverse pair)
>
> Arc: composed (comp4 T5C) → r1–r3 (sol-arm passes P1–P4 with
> repairs) → r4 (Fable arm: companion scoping + the eight-item
> evidence census independently verified, incl. a bit-identical
> instrument re-run) → r5 (frame refresh to T1 r1–r10, intrinsic/
> avatar re-scopes, locator-compliant census) → PASS 1: Fable-arm
> hostile, VERDICT CLEAN (0C + 0G + 2m, folded as riders per the
> minors-only precedent) → PASS 2: sol confirmation (gpt-5.6-sol
> high, runs/comp12/T5P7_output.log), VERDICT CLEAN (0C + 0G + 0m).
> Two consecutive clean fresh-context passes on model-diverse arms:
> **THE w-FRAME NOTE IS ACCEPTED at 2/2.** Consumption checks and the
> absorbed notes' dated appends ride the next queue (the T3/S-2
> pattern). Standing conditionalities unchanged and displayed:
> 𝒲_{≤i} at i ≥ 3, OPEN-2 at i = 1, [WFRAME OPEN-1]/[OPEN-3].”

**DERIVATION:** `[ACCEPTANCE RECORD]` Both halves of the r3 gate (EFF.T5.39) are met: the span-pin ledger at r4/r5, the two clean passes here, on model-diverse arms. Evidence pointer: `runs/comp12/T5P7_output.log`.

**CONDITIONALITY:** the three re-displayed standing conditionalities are part of the accepted statement: \(\mathcal W_{\le i}\) at \(i\ge3\) (EFF.T5.13/.14/.29, ledger HYP.63), `WFRAME OPEN-2` at \(i=1\) (EFF.T5.17/.36, ledger HYP.65), and `WFRAME OPEN-1`/`OPEN-3` (EFF.T5.36, ledger HYP.64/HYP.66). **Not re-displayed but equally live:** \(\mathrm{(H\!-\!VARTHETA\!-\!RES)}_i\) at \(i\ge3\) (EFF.T5.10) and the (C3-ladder-nonvanishing) carry (EFF.T5.09), the latter now one clause short of its own frame supplier. Protocol scope: zero dated appends landed (N1–N5).

**RESOLUTION TRACE:** final section; supersedes every status line and every “counter resets”.

**TEETH:** §7 of this specification.

---

## 4. XREF verification ledger

All designations grep-verified at compile time (2026-08-14) with fixed-string matching against the working tree; counts recorded. Commit objects verified with `git cat-file -t`.

### 4.1 T1 designations (`lean/notes/openmath/T1_GAUGE_COCYCLE_2026-08-12.md`)

| ID | Verified designation | count | Role |
|---|---|---:|---|
| X01 | `\tag{C3-B-law}` | 1 | I1 span pin; the B-law display T1 supplies gauge algebra to |
| X02 | `T1 only **supplies the gauge algebra to**` | 3 | the A7 re-scope sentence quoted by census entry 1 |
| X03 | `\tag{C3-Theta}` | 1 | \(\Theta_i(t)=\vartheta_{i,f_{i+1}-t}^{-1}\) |
| X04 | `\tag{C3-three-reads}` | 1 | the canonical residual = (WF-psi) |
| X05 | `\tag{C3-ladder-nonvanishing}` | 1 | T1 r8's independent site obligation (EFF.T5.09) |
| X06 | `H\!-\!VARTHETA\!-\!RES` | 19 | T1 r9's \(\vartheta\)-membership fence (EFF.T5.10). **NOTE:** the unescaped form `(H-VARTHETA-RES)` has count **0** in T1; the escaped form is the only verifiable designation |
| X07 | `R11-1` | 10 | the round that made the \(w_i\in K_i^\times\) gate two-clause |
| X08 | `R9-2` | 2 | the (C2-vartheta) fence round |
| X10 | `A7 — GENTOW2` | 3 | the A7 row (I1's re-scope pin) |
| X11 | `\tag{C3-canonical-read}` | 3 | I1 span pin |
| X27 | `R8-2` | 25 | the (C3-ladder-nonvanishing) round |
| X28 | `R9-4` | 3 | the A7 SPAN PIN 2 re-scope |
| X29 | `R9-5` | 4 | the md5 pin-normalization convention |
| X30 | `\tag{C3-read-codomain}` | 1 | **the second gate clause T5 does NOT carry** (OPEN-CALL 1) |
| X31 | `\tag{C3-zero-read}` | 1 | named in EFF.T5.09's non-derivability list |
| X32 | `C3-ladder-multiplicativity` | 16 | named in T1's superseded honesty item 2 |
| X33 | `C3-graded-scalar-covariance` | 16 | named in EFF.T5.09's non-derivability list |
| X34 | `## r13` | 1 | T1's current last round — two rounds past T5's pin |

### 4.2 GENTOW2 designations

| ID | Target and verified designation | count | Role |
|---|---|---:|---|
| X09 | `GENTOW2_PROOF_2026-08-09.md` — `GENTOW2-B″` | 11 | the \(i=2\) supplier |
| X12a | same — `B′(3)` | 11 | the per-grade-unit clause discharging (C3-ladder-nonvanishing) at \(i=2\) |
| X13 | `GENTOW2_passPE6_report.md` — `w-avatar = z` | 3 | the four recorded avatars |
| X14 | same — `Results (run-2 GREEN 68 checks / 0 violations)` | 1 | PE6 run-2 result |
| X15 | same — `IDY` | 2 | the IDY closure rows underwriting I3's intrinsic leg |
| X16 | same — `avatar = R_{3,κ̄}(n̂₂(u₃)) — riding Thm 4.2/eq (16) (row 23,` | 1 | the DESCRIPTIVE identification (NON-IMPORT N-1) |
| X17 | same — `u(β₀) measured DIRECTLY as its own canonical-frame ratio (LD1: 1;` | 1 | the direct canonical-frame rows (I3's intrinsic leg) |

### 4.3 GENTOW5 designations

| ID | Target and verified designation | count | Role |
|---|---|---:|---|
| X12 | `GENTOW5_PROOF_2026-08-09.md` — `S12.1` | 4 | the cumulative-hypothesis append |
| X23 | same — `is WITHDRAWN` | 2 | the withdrawal of S11.2's scope walk (EFF.T5.27 leg 3) |
| X24 | same — `the next automatic floor` | 1 | (WF-chain)'s terminal arrow |
| X25 | same — `S11.3` | 8 | the \(i=1\) fixed-letter-power statement |
| X26 | same — `Awaiting sol re-confirmation in the next queue.` | 1 | rider m-1's L2313 endpoint sentence |
| X35 | same — `S11.2` | 7 | the proved fixed-unit list + the withdrawn scope walk |
| X36 | same — `Corollary 6.4` | 2 | (WF-chain) node |
| X18 | `GENTOW5_passPE3_report.md` — `carrier-(8,4)` | 1 | the seventh-prime geometry |
| X19 | same — `FGMN` | 3 | the class-avatar keying of I4; the gloss `(Y = the FGMN class avatar)` is line-wrapped in the PE3 source between the words `FGMN` and `class` |
| X20 | same — `NOSE TOOTH FIRED` | 1 | the untwisted tooth \(\psi_3(Y)\ne0\) |
| X21 | `GENTOW5_WI_2026-08-10.md` — `PROVED at every level` | 1 | the companion's promotion claim (EFF.T5.27 leg 1) |
| X22 | same — `runs UNCONDITIONALLY at` | 1 | the companion's S4 assertion (EFF.T5.27 leg 1) |
| X37 | same — `## S1` | 1 | I5's statement pin section |

### 4.4 Verified NEGATIVE edges (no dated append has landed)

| ID | Check | count | Meaning |
|---|---|---:|---|
| N1 | `grep -cF 'WF-w' T1_GAUGE_COCYCLE_2026-08-12.md` | 0 | I1's source carries no T5 append |
| N2 | `grep -cF 'WF-w' GENTOW2_PROOF_2026-08-09.md` | 0 | I2's source carries no T5 append |
| N3 | `grep -cF 'WF-w' GENTOW5_PROOF_2026-08-09.md` | 0 | I4/I5's source carries no T5 append |
| N4 | `grep -cF 'WF-w' GENTOW5_WI_2026-08-10.md` | 0 | the companion carries no T5 append |
| N5 | repo-wide `grep -rlF` for `WF-law` / `WF-realized` / `DEFINITION W-FRAME` | 1 file | only `T5_WFRAME_2026-08-12.md` itself — **no consumer anywhere in the repo yet** |

### 4.5 Locator re-verification (compile time, 2026-08-14)

`[TABLE]` Every commit hash the note names, checked with `git cat-file -t`.

| ID | Hash | type | Role in the note |
|---|---|---|---|
| C01 | `c2c77b546647ae6de2693a0961d245e1b0b978fd` | commit | locator entry 1; T1 r9 fold |
| C02 | `b0e5c648b2287d940ee404d0042168d5542c4a0f` | commit | I5's object-addressed pin; **L1912–1913 re-read and byte-matched** |
| C03 | `33f86204d4b502edf55d2f5246eaa4175fd7abda` | commit | I2's object-addressed pin; **L729–738 re-read and byte-matched** |
| C04 | `9f29a28b9904df17c286c0c89246a6540159b010` | commit | GENTOW2 current commit at r5 |
| C05 | `deba21bd87f7d047761c4eb218070d67fc26da2d` | commit | PE6 report |
| C06 | `39c36aa2e0387e7820701f41eb3a2fca6ef1a7eb` | commit | PE3 report |
| C07 | `cb010522aff06defbf0f4f9b69bb5da4ce61f0e8` | commit | GENTOW5 S12.1 + corrigendum |
| C08 | `72eadc1d9cd3fab3126bdfc2735dad26fd75f0fd` | commit | the WI companion |
| C09 | `4c95940959b501ac70655d45a5424a2b19f31c08` | commit | WI instrument + run-1 RED artifact |
| C10 | `ec30256cafe301b5087c81eb465a4e3e0e391800` | commit | WI run-2 artifact |
| C11 | `07fa3676d5e1964d1ff11aa033149cc245457e77` | commit | the seal commit (instrument sealed UNRUN) |
| C12 | `fa05591f` | commit | the S12.1 fold at 11:40 (EFF.T5.27 timeline) |
| C13 | `e765aa9c` | commit | this note's r4 census fold, 22:21 |
| C14 | `1950f588` | commit | T1 r8 fold, 21:54 |
| C15 | `0013cd60` | commit | T1 r7 fold, 21:29 |
| C16 | `fb991138` | commit | T1 r10 (rider m-2's frame refresh) |

`[TABLE]` Instrument digests recomputed with `md5sum`.

| Artifact | Recomputed md5 | Note's md5-8 pin | Verdict |
|---|---|---|---|
| `verification/openmath/gentow5_wi_checks.py` | `c8c5d129a91837d5f6b2a5902601a4aa` | `c8c5d129` | MATCH |
| `verification/openmath/gentow5_wi_checks_output.txt` | `b0a8c3c4b5857e90929fff9c340ae870` | `b0a8c3c4` | MATCH |
| `verification/openmath/gentow5_wi_checks_output_run1_RED.txt` | `598d6704fc4348767a0a6f5f57705f73` | `598d6704` | MATCH |

### 4.6 Reverse-XREFs: consumers of T5

| ID | Consumer file and verified designation | count | Supplied edge |
|---|---|---:|---|
| RX-1 | `spec/HYPOTHESIS_LEDGER.md` — `HYP.63  \`[GENTOW5-W(i)]\` / 𝒲_(≤i)` | 1 | EFF.T5.13/.14/.29; disposition “CARRY — WI proves only the induction step, not induction from empty base” — the ledger's independent agreement with EFF.T5.27 |
| RX-2 | same — `### T1/T3/T5 supplier conditions` | 1 | the ledger section homing EFF.T5.09/.10 |
| RX-3 | same — `HYP.64  \`WFRAME OPEN-1\`` | 1 | EFF.T5.36 OPEN-1 |
| RX-4 | same — `HYP.65  \`WFRAME OPEN-2\`` | 1 | EFF.T5.36 OPEN-2; “CONSUMED BY: entire i=1 stratum, presently excluded” |
| RX-5 | same — `HYP.66  \`WFRAME OPEN-3\`` | 1 | EFF.T5.36 OPEN-3; the gap “is now MEASURED, not merely known” |
| RX-6 | same — `HYP.99  T3/T5 frame staleness` | 1 | **the two-clause-gate staleness of EFF.T5.09** |
| RX-7 | same — `HYP.100  contradictory T5 sentence` | 1 | **the L299-vs-L616 supersession of EFF.T5.29** |
| RX-8 | `docs/in-progress/LEAN_SCOPING_consumption_map_2026-08-12.md` — `**(d) Arc grade.** **Attempt 0/2, r1–r3 folded, pass 4 NOT CLEAN — 0C + 2G + 0m; repairs owed` | 1 | the Lean-scoping read of T5 (**STALE**: pre-r4/r5, pre-acceptance) |

---

## 5. Effective supply/consumption ledger

`[TABLE]`

| Source | Effective T5 consumption | Surviving fence | Protocol status |
|---|---|---|---|
| T1 (C3-B-law)/(C3-canonical-read)/(C3-three-reads)/(C3-Theta), with A7 | I1 = EFF.T5.22; supplies \(\Gamma_t\), \(\Theta_i(t)\), the cancellation, and (WF-psi) | T1 supplies gauge algebra ONLY; the B-law identity is not T1's; the factor-pattern conclusion is fenced by (C3-ladder-nonvanishing) | **CONSUMED; append OUTSTANDING** (N1). Frame pin STALE by two rounds (OPEN-CALL 1) |
| T1 r8 (C3-ladder-nonvanishing) | EFF.T5.09, carried at nine enumerated sites | discharge at \(i=2\)/\(i\ge3\); NONE at \(i=1\) | **CARRIED — but one clause short of T1 HEAD's two-clause gate** |
| T1 r9 \(\mathrm{(H\!-\!VARTHETA\!-\!RES)}_i\) | EFF.T5.10, carried at every \(i\ge3\) \(\Theta_i(t)\) | discharge not adjudicated (N-5) | **CARRIED as an open hypothesis** |
| LEMMA GENTOW2-B″ | I2 = EFF.T5.23; supplies (WF-law\(_2\)) | none — this is the unconditional rung | **CONSUMED; append OUTSTANDING** (N2) |
| GENTOW2-PE6 | I3 = EFF.T5.24 | intrinsic at LD1/GA1/GA3 only; avatar elsewhere; row 23 DESCRIPTIVE | **CONSUMED at measured strength; append OUTSTANDING** |
| GENTOW5-PE3 | I4 = EFF.T5.25 | avatar-keyed on both keys; no intrinsic evaluation | **CONSUMED as avatar-coherent; append OUTSTANDING** |
| GENTOW5 S12.1 + the dated corrigendum | EFF.T5.29, .30, .31, .32, .33 | \(\mathcal W_{\le n}\) on every arrow; two named carve-outs | **CONSUMED; append OUTSTANDING** (N3) |
| GENTOW5 S11.2 proved fixed-unit list | EFF.T5.09's \(i\ge3\) nonvanishing discharge | PROVED side only; the single-\(w\) law stays behind \(\mathcal W_{\le i}\) | **CONSUMED on the proved side** |
| GENTOW5 S11.2 scope walk (L1925–1928) | — | **WITHDRAWN by S12.1** | **NOT CONSUMED — NON-IMPORT** |
| `[GENTOW5-W(i)]` companion (§S1) | I5 = EFF.T5.26, scoped by EFF.T5.27 | consumed ONLY as \(\mathcal W_{\le i}\Rightarrow[\dots]\); the companion's unconditional verdict is not adopted | **CONSUMED CONDITIONALLY; append OUTSTANDING** (N4) |
| WI instrument + LW3 rows | EFF.T5.28 | regression records, not coverage | **CONSUMED as inherited, independently reproduced evidence** |

---

## 6. TEETH inverse inventory

Disposition vocabulary is `{Lean theorem, executable regression, signed non-applicability}`. T5's Pass-1 rows are statement/scope attacks (propositions in the formalization); its Pass-2 rows are locator and census obligations, of which the census rows ARE executable (and were executed, bit-identically, at r4).

| # | Battery row (abbreviated) | Guarded EFF units | Disposition |
|---|---|---|---|
| 1 | Pass 1(1) — (WF-w)/(WF-psi) literal against T1 C3 **+ the \(\bar k_i=\bar\kappa_i\) bridge in I5** | EFF.T5.03, .04, .05, .06, .08, .09, .11, .22, .26 | Lean theorem (definitional) |
| 2 | Pass 1(2) — \(i=2\) unconditional vs GENTOW2-B″; every \(i\ge3\) retains \(\mathcal W_{\le i}\); reject \(i=1\) promotion | EFF.T5.01, .12, .13, .14, .15, .17, .18, .19, .23, .27 | Lean theorem |
| 3 | Pass 1(3) — walk every (WF-chain) arrow against S12.1 | EFF.T5.29, .30, .31 | Lean theorem |
| 4 | Pass 1(4) — instantiate (WF-index) at \(j=2,3,4,n+1\); top must use \(w_n\) | EFF.T5.32, .33 | Lean theorem |
| 5 | Pass 1(5) — reject universal inference from avatar or seven-prime records | EFF.T5.02, .07, .24, .25, .34, .35, .36 | **signed non-applicability** |
| 6 | Pass 1(6) — lint \(w=1\), avatar-\(w\), theta-free claims against PE6 and LW3 | EFF.T5.06, .11, .16, .24, .25, .28, .34, .35 | executable regression (INHERITED — PE6 + WI instruments) |
| 7 | Pass 2(1) — per-pin repository recheck with exact hash, span, pass/fail; a copied locator is not verification | EFF.T5.21, .22, .23, .26, .41, .42, .43, .44 | executable regression (`git show` / `git cat-file` / `md5sum`; re-run clean at compile time, §4.5) |
| 8 | Pass 2(2) — T1 supplies only dictionary/cancellation; each B-law names its supplier | EFF.T5.02, .18, .20, .22, .27 | Lean theorem |
| 9 | Pass 2(3) — the three boundary regimes: \(i=1\) open, \(i=2\) unconditional, \(i\ge3\) conditional | EFF.T5.01, .09, .10, .12, .14, .17, .26, .33 | Lean theorem (three separate statements) |
| 10 | Pass 2(4) — reproduce the census from repository artifacts: six PE6 primes/four frames, PE3 seventh prime, WI run 2's 60/60, run 1 retained | EFF.T5.24, .25, .28, .40, .41 | executable regression (INHERITED — reproduced bit-identically at r4) |
| 11 | Pass 2(5) — nothing behind S12.1's chain is labeled unconditional merely because a companion reports verification | EFF.T5.13, .14, .15, .19, .27, .29, .31, .34 | Lean theorem |
| 12 | Pass 2(6) — lint for the superseded \(\psi_j^{(\omega_j)}\), theta-free power laws, universal \(w=1\), general avatar identity | EFF.T5.07, .28, .32, .34, .35, .36 | **signed non-applicability** |

**Zero-orphan check:** 12 of 12 battery rows are mapped (Pass 1's 6 items + Pass 2's 6 items); 0 orphan rows.

**Reverse direction — units with no tooth:** EFF.T5.37, .38, .39, .45 are the battery frame and the acceptance record themselves; EFF.T5.42/.43 are guarded by row 7. Every other unit (41 of 45) appears in at least one row.

---

## 7. Acceptance and evidence fence

EFF.T5.39's gate is two-conjunct:

> \[
> \boxed{
> \text{two consecutive clean hostile passes}
> +
> \text{a complete independently verified span-pin ledger}.
> }
> \]

Both are met. The ledger conjunct was discharged at r4 (eight entries, one bit-identical instrument re-run) and brought to per-entry locator compliance at r5; the pass conjunct at EFF.T5.45 (Fable-arm 0C+0G+2m with both minors folded as riders, then gpt-5.6-sol high 0C+0G+0m, `runs/comp12/T5P7_output.log`).

**What this note's evidence IS:** first-party locator verification (16 commits, 2 object-addressed spans, 3 md5 digests — all re-confirmed at compile time, §4.5) plus one genuinely independent instrument reproduction (`gentow5_wi_checks.py`, exit 0, 60/60, output bit-identical).

**What it is NOT:** coverage. Honesty item 10 governs — “The six PE6 primes, seventh-prime PE3 check, and LW3 are regression records, not exhaustive coverage.” And the mathematical content behind \(\mathcal W_{\le i}\) is not evidenced at all: the census verifies that the companion *says* what T5 quotes, not that the companion's induction closes (EFF.T5.27, ledger HYP.63).

---

## 8. Deferred reconciliation obligations

1. **Five dated absorption appends — protocol.** Run the consumption checks and land the dated appends in T1, GENTOW2, GENTOW5, the WI companion, and the two PE reports. Verified not yet done (N1–N5); the acceptance record schedules it (“ride the next queue (the T3/S-2 pattern)”). Action item.

2. **The two-clause gate — cross-note HEAD audit.** EFF.T5.09 carries only (C3-ladder-nonvanishing); T1 r12's R12-3 makes the gate for \(w_i\in K_i^\times\) two-clause, adding (C3-read-codomain). T5's frame pin (r1–r10) predates it. Ledger HYP.99 owns this row (`audit-work-item`, “cross-note HEAD audit”). See OPEN-CALL 1 — this is the one place where a downstream formalizer following EFF-T5 literally would assert a unit membership on one clause of a two-clause gate.

3. **Strike or annotate the stale L299 sentence — corpus consistency.** Ledger HYP.100's proposed disposition is “strike/append stale sentence.” EFF.T5.29 records the supersession; the physical strike is a note-owner edit.

4. **`docs/in-progress/LEAN_SCOPING_consumption_map_2026-08-12.md` is stale on T5** (RX-8): its arc grade reads “Attempt 0/2, r1–r3 folded, pass 4 NOT CLEAN — 0C + 2G + 0m; repairs owed”, predating r4, r5, and the acceptance. Refresh owed. (The same map is stale on T4 — see `spec/EFF-T4.md` §8 item 2 — so this is one refresh, not two.)

5. **Entry-1 locator drift.** EFF.T5.42's entry 1 pins T1 at `c2c77b54` and glosses it “= HEAD”; rider m-2 dates that gloss to 22:38–22:49 on 2026-08-12. At compile time T1 is at r13 and the gloss is false as an unqualified statement. Recorded so a mechanical locator-checker does not report a fresh MISMATCH: the pin is correct *as dated*, and the rider already dates it.

---

## 9. Self-audit

### Resolved compilation calls

- Effective text derived: base + r1–r5 in order + both folded riders; 24 same-span collisions tabulated with the governing round named for each.
- The nesting is recorded explicitly: r5's item 2 replaces text inside r1's item 4, and r5's item 5 replaces text inside r4's item 3. A flat base-overlay assembly yields the wrong I5 and the wrong verification disposition.
- **The L299/L616 contradiction is resolved by supersession, not by choice** — r1 replaces the span containing L299, so L616 governs. The stale sentence is flagged as physically present (EFF.T5.29, §8 item 3), matching ledger HYP.100 independently.
- I5's heading is compiled as **conditional**, per r1, with r4-G1's four-leg argument reproduced in full (EFF.T5.27) — including the index arithmetic that demotes the companion's nonemptiness derivation to a recorded setting assumption.
- The base I3 and I4 boxes are classified `[SUPERSEDED]`, and the intrinsic/avatar split is carried into the supply ledger and the teeth table.
- The r3 “no repository span pin was independently checked” disposition is classified `[SUPERSEDED]` — a live trap, since it reads as a HEAD statement if quoted out of the round order.
- All 16 named commits, both object-addressed pin spans, and all three instrument md5s independently re-verified at compile time; 37 designations grep-verified with counts.
- Five verified negative edges, including the repo-wide N5: **no file other than the note itself contains `WF-law`, `WF-realized`, or `DEFINITION W-FRAME`.**
- Eight reverse-XREFs emitted and verified, including the two ledger rows (HYP.99, HYP.100) that independently corroborate this compilation's two hardest findings.
- Battery fully consumed: 12 rows, 0 orphans, 41 of 45 units guarded.
- No table prosified: both census tables, the locator table, and the supersession chain are tables.

### Fidelity audit (compile-time, mechanical)

- **233 quoted segments extracted** from this specification and tested for verbatim containment in the source note under a normalization removing only presentational reflow (whitespace, display delimiters, emphasis markers, blockquote prefixes). Script: `scratchpad/verb2.py`. **209 verbatim.**
- The 24 non-matches are exclusively (a) **cross-file quotations** — 16 from `spec/HYPOTHESIS_LEDGER.md` and 2 from `T1_GAUGE_COCYCLE_2026-08-12.md`, each independently grep-verified at count 1 in its own file, plus the PE3 gloss `(Y = the FGMN class avatar)` which matches its own source modulo one line wrap — and (b) explicitly marked `[ASSEMBLED]` composites (EFF.T5.01, .26, .34, .35, .37, .38), whose components were verified individually.
- Five quotation repairs were made during the audit: one LaTeX-for-Unicode substitution inside a ledger quote, one hyphen-across-linebreak rejoin, one display re-quoted as a display rather than prosified, one bullet list restored as a bullet list rather than slash-joined, and one `\(…\)` wrapper removed from a fragment that carries none in the source.
- Two source displays are reproduced as `[TABLE]`s that the note wrote as numbered prose entries (EFF.T5.41, .42); both are transcribed without reordering, without verdict changes, and with the note's own wording in every cell.
- No quantifier, index, scope, hypothesis, or verdict was altered anywhere.

### OPEN-CALLS

**OPEN-CALL 1 — the \(w_i\in K_i^\times\) gate is carried with one of its two clauses. FOR-0d — NEEDS ASVIN.**

EFF.T5.09 is the note's most load-bearing carry: it is what licenses \(w_i\in K_i^\times\), division by \(w_i\), (WF-twist), (WF-psi), both (WF-realized) forms, the instance displays of I2/I4/I5, and every \(\psi_j^{(\omega_{j-1})}\) leg of (WF-index). It carries exactly one obligation, (C3-ladder-nonvanishing).

T1 r12's R12-3 (verified, XREF X07/X30) says one clause is not enough:

> “after R11-1 the gate is two-clause — (C3-read-codomain) places \(w_i\in K_i\), (C3-ladder-nonvanishing) makes it nonzero”

and T1's superseded honesty item 19:

> “The status \(w_i\in K_i^\times\) is asserted only at sites carrying BOTH (C3-read-codomain) and (C3-ladder-nonvanishing) — at \(i=2\) supplied by the accepted GENTOW2 package (S5.1 (R-FGMN) for the codomain, LEMMA GENTOW2-B\('\)/B\(''\) for nonvanishing), at \(i\ge3\) by an explicitly cited level-general analogue carrying both clauses.”

T5 was accepted 2/2 against a T1 frame (r1–r10) that predates R11-1/R12-3. The compiler will not add the missing clause — that changes what T5 asserts — nor will it silently emit a one-clause gate as if complete. Note the asymmetry in difficulty: at \(i=2\) T1 itself names the codomain supplier (GENTOW2 S5.1 (R-FGMN)), so that leg looks cheap; at \(i\ge3\) T1 requires “an explicitly cited level-general analogue carrying both clauses”, and EFF.T5.09's \(i\ge3\) discharge argument (S11.2's proved fixed-unit list) is a **nonvanishing** argument — it says a fixed \(K_i^\times\)-value is nonzero — so whether it also supplies the codomain clause is exactly the undecided question. Ledger HYP.99 owns the row. Phase 0d must decide whether T5 needs a repair round or whether the S11.2 fixed-unit list discharges both clauses at once.

**OPEN-CALL 2 — honesty item 7 was not re-scoped by r5's F2. FOR-0b (note owner).**

Item 7 reads “PE6 already records \(w_2\ne1\) at six primes”, which is the pre-r5 conflation r5's F2 exists to fix: after r5, intrinsic \(w_2\ne1\) is PROVED at three frames and the other records are avatar-keyed. r5 added item 14 stating the corrected scope but did not restate item 7, so the ledger now contains both readings. No consumer is misled if items 7 and 14 are read together, and EFF.T5.34 records the tension inline — but two ledger items of the same document disagree on the strength of the same measurement. **Proposed disposition:** a minors-only rider aligning item 7's wording with item 14. Needs the note owner.

**OPEN-CALL 3 — (WF-chain)'s antecedent is strengthened relative to its source line. FOR-0b.**

GENTOW5 S12.1 displays the chain with antecedent `[GENTOW5-W(n)]` (the single newest conjunct); T5's (WF-chain) displays \(\mathcal W_{\le n}\) (the whole cumulative conjunction). The r4 census records the license — “read, per the corrigendum, "under the ambient hypothesis 𝒲_{≤n} as a whole" — the form (WF-chain) displays” — so the promotion is the corrigendum's instruction, not an invention, and it is *conservative* (a stronger hypothesis). But the displayed T5 formula is not the displayed S12.1 formula, and §7 Pass 1(3) instructs a checker to “Walk every arrow of (WF-chain) against S12.1” — a literal walk finds an antecedent mismatch at arrow 0. Recorded so the cross-read does not re-derive it as a finding. Whether (WF-chain) should carry the corrigendum cite inline is a note-owner call.

### Exhaustive residual judgment list

There are no other unresolved T5 mathematical or scope judgments. \(\mathcal W_{\le i}\), `WFRAME OPEN-1/2/3`, and \(\mathrm{(H\!-\!VARTHETA\!-\!RES)}_i\) are the note's own displayed standing conditionalities, not compiler ambiguities; the missing appends, the stale L299 sentence, the stale consumption map, and the dated “= HEAD” gloss are classified in §8 as bookkeeping.

EFF-T5 COMPILED: 45 statements / 56 xrefs verified / 3 open calls
