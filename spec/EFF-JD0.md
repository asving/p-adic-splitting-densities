# EFF-JD0 — CANONICAL EFFECTIVE SPECIFICATION v2

## 1. Header

**Source note:** `lean/notes/openmath/JD0_PROOF_2026-08-08.md`

**HEAD blob hash:** `f85bf82407604ca1ad6065dea29066cc3cfab94e`

**Compilation context:** `docs/BLUEPRINT_PHASE_DESIGN_2026-08-13.md`, Revision 2, Stage 0a.

**Governing template:** `spec/EFF_TEMPLATE_v2.md` (all rules; full compilation).

**COMPILED-BY:** opus (2026-08-14, Asvin-authorized substitution, PROJECT_STATE #51). CODEX CROSS-READ OWED (post-reset).

### ⚠ GRADE AND SIGNATURE STATUS (read before any consumption)

This note is **the only one of the four in this compilation queue that was never accepted 2/2** — and it does not need to be. Its status is a **signed owner decision**, not an arc outcome, and the two facts must not be conflated.

**The note's own grade:**

> “**GRADE: PROVED on the stated perimeter (unit-character gauges; the
> L3 × L4 assembly; honest boxes S4), consuming the supplier stack S7 at the
> grades listed there. Attempt 0/2.**”

and its machine leg:

> “**GREEN. 37/37 towers, 68,523 checks, 0 violations in every family, all
> four teeth fired, zero skips.**”

**The signature.** `spec/HYPOTHESIS_LEDGER.md` row HYP.39 `JD0-BAR` states the gap and then closes it by owner signature:

> “STATEMENT: J-D0 gauge-blindness is PROVED grade with 68,523/0 machine evidence but below process bar because the two-clean hostile arc has not run.”

> “PROPOSED DISPOSITION: **CLOSED-SIGNED — question 6 ANSWERED [v5, append #49].** The owner **signed the 68,523/0 machine grade as the bar**; the two-clean hostile arc is not required for J-D0. Reasoning recorded at #49 in the owner’s frame: J-D0’s gauge-blindness *“MUST be true for the proof strategy/uniformity to work at all”*, and conviction of truth implies any proof defect would be fixable. The grade cap therefore no longer caps the seven direct cone sites (HYP.74) at which J-D0 is consumed. **The optional post-reset codex pass is NOT cancelled — it is demoted to belt-and-braces and moved to the maintenance queue as MQ-4**, so this row closes without swallowing a queued action.”

So the effective grade is: **PROVED on the stated perimeter, machine grade 68,523/0 SIGNED by the owner as the bar at 0d Q6 (append #49); the two-clean hostile arc is NOT owed; an optional codex pass survives as MQ-4, gating nothing.** MQ-4, verbatim:

> “MQ-4  **JD0 optional codex pass (belt-and-braces).** Queued, not owed. Append #49 signs the JD0 machine grade as the bar (question 6, HYP.39 CLOSED-SIGNED) while keeping the post-reset codex pass *“queued as belt-and-braces.”* Recorded here so that closing HYP.39 does not silently swallow a queued action: the pass is optional, it gates nothing, and its absence caps no claim. (v5.)”

**What the signature does NOT do:** it does not discharge the six honest boxes (EFF.JD0.22), the supplier stack's grades (EFF.JD0.25), or the six REAL findings of the C10/E8 certification — those were repaired textually by the S10 block, which is part of the effective text.

### Effective-text rule

Derived. The note is a **body plus one dated adjudication-and-correction append**; there are no repair rounds. The append declares its own scope:

> “Everything above this heading is byte-untouched by this append.”

and the correction block declares its controlling scope precisely:

> “This correction is controlling wherever the earlier wording says merely
> “shape data,” calls the normalized residual map an algebra automorphism,
> derives cross-class blindness from gauge-orbit constancy, identifies the
> L3 × L4 assembly with the classifier at all orders, or equates factoring
> through type with factoring through shape.”

**Therefore the effective text is the byte-untouched body, with the S10 correction applied at each of the five conditions it names, plus the E8 adjudication of all twelve findings.** One deletion is explicit: S10.2 instructs “Delete the sentence …” from S2.2(f).

The provenance is two-model with orchestrator verification between:

> “The sol-5.6 as-built read of this note (gpt-5.6-sol high,
> runs/cert/C10_jd0_output.log, 2026-08-12) returned CHALLENGE with 12
> findings including a claimed CRITICAL. The E8 certification run
> (gpt-5.6-sol high, runs/cert2/E8_jd0_output.log) adjudicated 6 REAL /
> 6 ARTIFACT — in particular the claimed CRITICAL (the "every
> 1-cochain" quantifier) is an ARTIFACT: S0 documents the quantified
> cochain action as R(y) ↦ ξ^{−deg R}R(ξy), and the counterexample's
> independent slot rescaling is outside that documented class.”

### Supersession chain

`[TABLE]`

| Target in the body | Governing text | Finding | Nature of the change |
|---|---|---|---|
| S0's “shape” = (d0, (e_j,h_j,g_j)) + field sizes | **S10.1**'s count-shape T̂ = (d0, (e_j,h_j,g_j)_j, (Λ_j)_j, 𝒢) | #2, #4 REAL | **the key is ENLARGED** — the residual predicate Λ_j and the split/grid record 𝒢 join the shape |
| The LEMMA J-D0 display of S0 | **S10.1**'s corrected display, over T̂ and μ_T̂(q) | #2, #4 REAL | statement re-keyed; clause (ii) strengthened to “regardless of whether their carry cochains are cohomologous” |
| S2.2(f)'s sentence “σ_ξ extends the identity of K_j to a degree-preserving K_j-algebra automorphism of K_j[y]” | **S10.2**'s degreewise-multiplicativity derivation | #3 REAL | **explicit DELETION** + replacement proof |
| S3's “enters the classifier only through the gauge orbit” | **S10.3**'s two-part replacement | #5 REAL | the mechanism is corrected: non-occurrence, not orbit-constancy |
| “valid at every residue characteristic, wild included” for the *actual classifier* | **S10.4** | #8 REAL | unconditional for the formal coefficient; supplier-perimeter-bounded for the classifier |
| S3's “bounded functions factoring through the type map” | **S10.5**'s fiber-constancy condition | #9 REAL | “factoring through the type map” is strictly weaker; corrected to constancy on fibers of π |
| S6's consumer list | **S10.6**'s consumer sweep | — | each consumer re-stated at its corrected scope; W-11 gains a **new named obligation** |
| “Attempt 0/2” / “below process bar” | **HYP.39 CLOSED-SIGNED** (owner, 0d Q6, append #49) | — | the machine grade is the bar; the hostile arc is not owed |

### Scope of record — mandatory two-predicate split

#### A. Mathematical claim set

The governing statement is S10.1's corrected display (EFF.JD0.33), whose two clauses are gauge-invariance of μ_T̂(q) and value-blindness across count-shape-equal types. The consequence sentence:

> “Consequently μ_T̂(q) is a function of the full combinatorial count-shape
> T̂ alone.”

Note what the correction *strengthened*: clause (ii) now reads “regardless of their residual letters and regardless of whether their carry cochains are cohomologous” — a **cross-class** statement, stronger than the body's gauge-orbit reading, and proved by a different (and simpler) route: non-occurrence.

Note also what it *narrowed*: the key is the full count-shape T̂, not the shorter tuple. S10.1 is explicit that the reduction is unproved:

> “No assertion is made here that the chosen t_j, D_j, δ, or B are
> determined by the shorter tuple (e_j,h_j)_j; proving such a reduction is a
> separate grid/split theorem.”

#### B. Consumption scope — LIVE at seven sites, verified

Unlike T4 and T5 (zero appends landed) and like W-11 (live consumers), J-D0 is **already consumed**, and the ledger has verified exactly where. `spec/HYPOTHESIS_LEDGER.md` HYP.74:

> “CONSUMED BY: **[WELD-FACE-AUDIT 2026-08-13, REACHABLE-AND-CONSUMED]** — **NOT via `[H-c]`.** J-D0 is consumed DIRECTLY at seven cone notes; all seven dependency-ledger sites re-verified at HEAD (W11 L599, W12 L1499, HMENU3 L717, HEX3 L720, GENIND L1518, GENH4 L938, GENHN L1515).”

**All seven sites re-verified independently at compile time** (§4.4) — every one is a conditionality-stack row citing J-D0 at seal `1642275/9387eb1`, at FRAME grade.

The grade of that consumption is important and is stated in the same row: J-D0 is consumed **at FRAME grade** at those sites (HYP.39: “J-D0 is consumed DIRECTLY at seven cone notes at FRAME grade”), which W-11's own stack corroborates — “J-D0 is corroborating at the instance, load-bearing only for the general-program framing”.

### Resolution rules applied

1. The body is byte-untouched by the append; S10 controls at each of the five conditions it names, and nowhere else.
2. The S10.2 deletion is a genuine deletion, not a strike-through: the automorphism sentence is **removed** from the effective text and replaced by the degreewise argument.
3. **The six ARTIFACT adjudications are part of the effective text**, because each one records why a plausible objection fails — that is load-bearing for a downstream reader who would otherwise re-raise it. They are compiled as EFF.JD0.31.
4. Clause (ii) after S10.1 is **stronger** than before, not weaker: cross-cohomology-class, by non-occurrence.
5. The grade line “Attempt 0/2” and “the hostile arc has not begun” are superseded by the owner signature (HYP.39), not by an acceptance record — this note has none.
6. The six honest boxes and the S7 conditionality stack are compiled as full inventory units (EFF.JD0.22, .25), per the compilation charge, because they are what a consumer must carry.
7. Machine records are re-verifiable and were re-verified: both artifact md5s and the runner digest (§4.3).
8. Where the body and S10 both speak, S10's wording is quoted as canonical and the body's is marked `[SUPERSEDED]` with its text preserved so the cross-read can see what changed.

### Quotation and prosification discipline

All source displays and tables are quotations or marked `[ASSEMBLED]`; every source table is `[TABLE]`; nothing is prosified; truncation carries an explicit ellipsis; `[IMPORTED]`, `[DEFINITIONAL]`, `[PROVED]`, `[RECORD]`, `[SUPERSEDED]` are compiler metadata.

---

## 2. NON-IMPORTS

**N-1 — the refuted predecessor is explicitly NOT the claim.** The provenance paragraph:

> “**Provenance of the salvage:** the J-D dimension shortcut (character/orbit
> counting of 𝒜(T)) was REFUTED with an explicit witness (WELD_SYNTHESIS §3.1:
> dim is q-independent, counts are q-rational of unbounded data — already the
> machine-checked order-0 instance separates them). J-D0 is the salvage that
> survived: not "counts = dimensions" but "counts and dimensions are blind to
> the same layer."”

This is an unusually clean negative-import: the note names its own refuted ancestor and states the exact weaker claim that survived.

**N-2 — J-D0 is not W-10.** BOX-1:

> “**JD0-BOX-1 (J-D0 is not W-10).** Nothing here proves the count EQUALITY
> "classifier stratum count = model-side count from (e_j,h_j,g_j) through
> the welded read" (W-10, M-priced).”

**N-3 — non-character value layers are outside the perimeter.** BOX-2:

> “J-D0's proof covers
> unit-character gauges; any value layer NOT of that form is outside the
> perimeter — and the NONCHAR tooth displays the failure mode (an additive
> slot shift is a bijection that breaks type preservation, hence counts).”

The E8 adjudication makes this fence load-bearing by using it to defeat the claimed CRITICAL (finding #1, EFF.JD0.31).

**N-4 — the menu is untouched.** BOX-3:

> “**JD0-BOX-3 (the menu is untouched).** WHICH decided shapes exist — the
> omMenu growth beyond order 0, hExhaust, drainage — is Phase-B. J-D0
> counts within a shape; it never enumerates shapes.”

(The complement of W-11, which enumerates the menu at n = 2 but does not prove gauge-blindness.)

**N-5 — the assembly identification is consumed, not proved — and its range is bounded.** BOX-4, as narrowed by S10.4:

> “J-D0 proves the preceding statements universally for the displayed formal
> L3 × L4 coefficient. Its identification with L2core’s actual per-cell
> classifier coefficient may be consumed only on the perimeter on which the
> L2core supplier has established that identification. The cited exact
> instances through OM order 4 do not, by themselves, prove an all-orders
> identification.”

**N-6 — the wild-decidedness warrant is imported, not proved.** BOX-6:

> “**JD0-BOX-6 (wild decidedness warrant).** That "decided shape" is
> well-posed at wild composite stages — strata do not collapse under the
> read — is the no-cancellation/level-detection warrant JC-PSIKER(iii) +
> JC-LOAD, consumed from GRTJC [ACCEPTED] as warrant, exactly as the
> blueprint's O-1 clause routes it. J-D0 adds no leg to it.”

**N-7 — the algebra shadow carries no proof weight.** S7:

> “clause (iii) at the WM arc
> grade (S2.4 only, not load-bearing for S2.1–S2.3)”

The E8 adjudication uses this to defeat finding #6 (EFF.JD0.31).

**N-8 — the interpretation notes carry no proof weight.** S7:

> “**H-B / H-C** (interpretation note): MEASURED verdicts, cited as
> interpretation in S3 — no proof weight anywhere in S2.”

**N-9 — the Lean tie is an anchor, not evidence.** S5:

> “The Lean tie is the
> degenerate instance of J-D0 — an anchor for what the order-≥ 1 statement
> must degenerate TO, not evidence for it.”

**N-10 — the shorter shape tuple is not claimed to determine 𝒢.** S10.1, quoted in §1.A above. This is the note's newest and sharpest fence, added by the correction.

Accordingly **no edge is created** from J-D0 to the W-10 count equality, to any non-character value layer, to menu enumeration, to an all-orders classifier identification, to the wild-decidedness warrant's proof, or to a shorter-tuple key.

---

## 3. Statement inventory

38 units, contiguous, in effective-text order.

### EFF.JD0.01  [scope-record]

**CANONICAL STATEMENT:**

> “**Unit:** J-D0, the S-priced opener of the J-D block (the last uncomposed weld
> block), per WELD_SYNTHESIS_2026-08-05 §3.2/§4(ii) and the standing FRONTIER
> order (J-D0 → W-10 at the (2,1,1) shape → W-11). **Attempt grade 0/2**
> (composed; the hostile arc has not begun). **Instrument:** sealed two-commit
> battery `verification/openmath/jd0_checks.py` (37 towers = the frozen 29-row
> J-C roster + the out-of-roster p = 7 genre + the constructed same-shape
> pairs); verdict section §S8, appended at commit 2 FROM the committed
> artifacts.”

**DERIVATION:** `[RECORD]` J-D0 is the *opener* of the J-D block: W-10 and W-11 both sit downstream of it in the FRONTIER order, which is why its consumption is live at seven sites while it has no hostile arc of its own.

**⚠ CONDITIONALITY — THE GRADE LINE IS SUPERSEDED BY OWNER SIGNATURE.** “Attempt grade 0/2 (composed; the hostile arc has not begun)” is a true statement of process history and a **false** statement of current status: HYP.39 is CLOSED-SIGNED, “the two-clean hostile arc is not required for J-D0”, and the 68,523/0 machine grade is the bar. See §1's grade block. The optional codex pass survives as MQ-4 and “gates nothing”.

**RESOLUTION TRACE:** header; grade superseded by `spec/HYPOTHESIS_LEDGER.md` HYP.39 (owner signature, 0d Q6, append #49).

**TEETH:** the four teeth of S8; §S9's scorecard.

---

### EFF.JD0.02  [record]

**CANONICAL STATEMENT:**

> “**Provenance of the salvage:** the J-D dimension shortcut (character/orbit
> counting of 𝒜(T)) was REFUTED with an explicit witness (WELD_SYNTHESIS §3.1:
> dim is q-independent, counts are q-rational of unbounded data — already the
> machine-checked order-0 instance separates them). J-D0 is the salvage that
> survived: not "counts = dimensions" but "counts and dimensions are blind to
> the same layer."”

**DERIVATION:** `[RECORD — a refutation and its residue]` The separating witness is a type-level observation: dimensions are q-independent while counts are q-rational of unbounded data, so no equality can hold; but both are blind to the *same* layer, and that weaker statement is what J-D0 proves.

**CONDITIONALITY:** none; this fixes what is NOT claimed (NON-IMPORT N-1).

**RESOLUTION TRACE:** header; untouched by the append.

**TEETH:** none; a provenance record.

---

### EFF.JD0.03  [definition]

**CANONICAL STATEMENT — the body's shape definition** `[SUPERSEDED by S10.1; reproduced because the correction is an ENLARGEMENT of exactly this]`:

> “Fix a decided OM shape (type skeleton) T of order m: the degree d0 of the
> level-0 factor and the read triples (e_j, h_j, g_j), j = 0..m, together with
> the residual-degree structure they induce — |K_j| = q^{d0·g_0···g_{j−1}}, the
> residue fields where the level-j residual data live. The letters z_j (the
> realized residual roots) and the carry cochain c (the [ILN]† value ledger) of
> any tower REALIZING T are NOT part of the shape.”

**DERIVATION:** `[DEFINITIONAL]` The letters and the carry cochain are excluded by fiat — that exclusion is the whole content the lemma then has to earn.

**⚠ SUPERSEDED — TWO OMISSIONS, BOTH ADJUDICATED REAL.** E8 finding #2: “The explicit definition of shape lists only \(d_0,(e_j,h_j,g_j)\) and the induced field sizes. It never includes the residual factorization predicate consumed by \(L3_j\). The phrase “prescribed degree/type” later in S0 cannot repair that omission.” E8 finding #4: “ “[C]omputed from the eq-12 splits alone” proves dependence on the chosen splits, not dependence only on \((e_j,h_j)\). No section-independence or canonical-section lemma appears at HEAD. The honest proved scope must retain the split/grid record as part of the count-shape.” Both are repaired by S10.1's T̂ (EFF.JD0.32).

**CONDITIONALITY:** decided shape of order m; the residue-field sizes are induced.

**RESOLUTION TRACE:** S0 → superseded by S10.1.

**TEETH:** JD0-BLIND (the same-shape pairs are built against this definition).

---

### EFF.JD0.04  [definition]

**CANONICAL STATEMENT:**

> “**The stratum count.** The count that L2core's per-cell coefficient consumes,
> at each truncation window: the product over levels of
>
>     L3_j  =  the residual-factorization count over F_{r_j}, r_j = |K_j|
>              (the census of residual data of the prescribed degree/type
>               at level j — the universal residue-shape polynomial genre)
>     ×  the L4 lattice q-power  q^{±B}, B = the lattice exponent
>              (the count of digit positions pinned/freed by the polygon
>               side and its grid — integer position data).”

**DERIVATION:** `[DEFINITIONAL — local terminology]` The E8 adjudication defends the terminology against finding #11: “S0 defines “stratum count” as the object consumed by the “per-cell coefficient,” including its \(q^{\pm B}\) factor. … Thus \((q-1)/q^3\) is not being presented as a raw finite-set cardinality. “Stratum count” is locally defined terminology for the normalized coefficient.” S10.1 then makes this explicit by naming it μ_T̂(q) and distinguishing it from the raw cardinality N_{Λ_j}(r_j).

**CONDITIONALITY:** the identification of this product with L2core's actual per-cell coefficient is BOX-4, narrowed by S10.4 (NON-IMPORT N-5).

**RESOLUTION TRACE:** S0; the naming and the raw/normalized distinction added by S10.1.

**TEETH:** JD0-STRAT (assembled count record, direct = gauged, all 37 towers).

---

### EFF.JD0.05  [definition]

**CANONICAL STATEMENT — the carry gauge and its full documented action:**

> “**The carry gauge.** A 1-cochain u : γ ↦ u_γ ∈ K^* acting by anchor
> rescaling a_γ ↦ u_γ·a_γ. Its full documented action:
>
>     ledger:    c(γ,γ′)  ↦  c(γ,γ′) · u_γ u_{γ′} u_{γ+γ′}^{−1}   (= c·∂u)
>     slot:      v        ↦  u·v          (unit character, per read slot)
>     residual:  R(y)     ↦  ξ^{−deg R} · R(ξ·y)   (JA-RES geometric
>                character; the (ξ,w) overall unit dies in monic form)
>     positions, δ, D_j, grid abscissas:  UNCHANGED.”

**DERIVATION:** `[DEFINITIONAL — and the perimeter that defeats the claimed CRITICAL]` This four-line display is the note's most consequential definition, because the quantifier “every 1-cochain” in the lemma ranges over cochains acting **this way**. The E8 adjudication turns exactly on it (finding #1, ARTIFACT):

> “The counterexample uses an independent diagonal rescaling of polynomial coefficients, which is not the note’s defined carry-gauge action. S0 explicitly specifies the action of the quantified cochain: … BOX-2 excludes “any value layer NOT of that form”; it does not narrow “every 1-cochain” to a subset after the fact. The warrant that the weld induces this action is priced at GRTJA 1/2, but that is supplier conditionality, not a quantifier contradiction.”

The distinction the adjudication draws is precise and worth keeping: **the perimeter was declared up front, so restricting to it is not post-hoc narrowing; and the warrant that the weld actually induces this action is a separate, priced supplier obligation (GRTJA 1/2, BOX-2), not a defect in the statement.**

**CONDITIONALITY:** unit values (u_γ ∈ K^*); the four-line action is the whole quantified class. That the weld's value dictionary IS of this form is BOX-2, priced at GRTJA 1/2 (ledger HYP.74).

**RESOLUTION TRACE:** S0; untouched by the append, and load-bearing for finding #1's rebuttal.

**TEETH:** JD0-COB checks (1)–(4); JD0-T-NONCHAR (the failure mode outside the class); JD0-T-NONUNIT.

---

### EFF.JD0.06  [lemma]

**CANONICAL STATEMENT — the body's LEMMA J-D0** `[SUPERSEDED by S10.1's corrected display]`:

> “> **LEMMA J-D0.** (i) [GAUGE-INVARIANCE] For every decided shape T, every
> > prime power q, and every 1-cochain u with unit values, the stratum count of
> > T computed through the u-gauged read equals the direct stratum count.
> > (ii) [VALUE-BLINDNESS] Any two towers/types with the same shape data
> > (d0, (e_j, h_j, g_j)_j) and residual-degree structure have EQUAL stratum
> > counts at every q, regardless of their letters and carry cochains.
> > Consequence: the stratum count is a function of the SHAPE alone — per
> > shape a single expression in q, the same at tame and wild primes.”

**DERIVATION:** `[PROVED — S2.3 assembly]` Clause (i) from EFF.JD0.11 + EFF.JD0.16; clause (ii) from the non-occurrence argument EFF.JD0.18.

**⚠ SUPERSEDED IN ITS KEY.** S10.1's corrected display (EFF.JD0.33) re-keys both clauses to the count-shape T̂ and the named coefficient μ_T̂(q). The *consequence sentence* changes accordingly: not “a function of the SHAPE alone” but “a function of the full combinatorial count-shape T̂ alone”.

**CONDITIONALITY:** the perimeter of EFF.JD0.05; the supplier stack of EFF.JD0.25; the six boxes of EFF.JD0.22.

**RESOLUTION TRACE:** S0 → superseded by S10.1.

**TEETH:** the whole battery.

---

### EFF.JD0.07  [acceptance-box]

**CANONICAL STATEMENT — the grade box:**

> “**GRADE: PROVED on the stated perimeter (unit-character gauges; the
> L3 × L4 assembly; honest boxes S4), consuming the supplier stack S7 at the
> grades listed there. Attempt 0/2.**”

**DERIVATION:** `[GRADE RECORD]` The grade is explicitly perimeter-relative and stack-relative: three qualifiers in one sentence (unit-character gauges / the L3 × L4 assembly / the S4 boxes), plus the S7 stack “at the grades listed there”.

**⚠ CONDITIONALITY — the “Attempt 0/2” clause is superseded by owner signature** (§1); the three perimeter qualifiers and the stack-relativity are NOT, and remain the governing conditionality of every consumption.

**RESOLUTION TRACE:** S0 tail → HYP.39 CLOSED-SIGNED for the grade clause only.

**TEETH:** §7 of this specification.

---

### EFF.JD0.08  [scope-record]

**CANONICAL STATEMENT:**

> “The weld's hard open fences — (VD-m), (C-coll), (R-coll), (DMULT-s), the
> one-perimeter character-triviality condition PERIM-μ of WELDMASTER §S3 — are
> ALL value-layer fences: they condition on which unit characters are trivial.
> J-D0 says count-layer statements never see that layer. So W-10 (the J-D2
> count equality, the density bridge's counting leg) escapes the collapse
> perimeter ENTIRELY: its hypotheses reduce to the proved/priced integer-grid
> faces plus classical residual censuses (WELD_SYNTHESIS §3.2). That is the
> single most consequential simplification the synthesis found for J-D.”

**DERIVATION:** `[SCOPE RECORD — the note's reason for existing]` The argument is structural: every named open fence conditions on unit-character triviality; J-D0 proves count-layer statements are invariant under unit characters; therefore count-layer consumers carry none of those fences. This is consumer 5's claim that J-D0 “deletes the fence-patching genre from the J-D block entirely” (EFF.JD0.24).

**CONDITIONALITY:** the deletion holds for count-layer consumers **using the L3 × L4 coefficient and the documented gauge dictionary** — S10.6 item 5 restates it with exactly that guard: “whenever the consumer uses the L3 × L4 coefficient and the documented gauge dictionary.”

**RESOLUTION TRACE:** S1; guard added by S10.6.

**TEETH:** none directly; the battery tests the mechanism, not the fence bookkeeping.

---

### EFF.JD0.09  [lemma]

**CANONICAL STATEMENT — S2.1(a), units do not move valuations:**

> “**(a) Units do not move valuations.** u_γ has w-value 0, and w is a
> valuation on the graded layer (V1/V2 — the GD-2 spine, consumed at GRTJB's
> ACCEPTED grade through W-3's development/polygon tie): w(u_γ·a_γ) = w(a_γ).
> Every abscissa and ordinate the polygon, the grid, or the development
> consumes is therefore unchanged by the gauge.
> [machine: JD0-COB checks (1) and (3) — gauged anchors sit at their lines;
> gauged anchor products sit at the sum line.]”

**DERIVATION:** `[PROVED — one valuation identity]` w(u_γ·a_γ) = w(u_γ) + w(a_γ) = 0 + w(a_γ), using that w is a valuation (imported at GRTJB's ACCEPTED grade via the GD-2 spine and W-3's development/polygon tie) and that unit values have w-value 0. Everything the polygon reads is a valuation, hence fixed.

**CONDITIONALITY:** w is a valuation on the graded layer — imported, ACCEPTED grade.

**RESOLUTION TRACE:** S2.1(a); untouched by the append.

**TEETH:** JD0-COB (1) and (3); JD0-T-NONUNIT (37 fires — “a non-unit rescale moves the position every time”), which is the tooth demonstrating that the unit hypothesis is load-bearing.

---

### EFF.JD0.10  [lemma]

**CANONICAL STATEMENT — S2.1(b), the integer carry layer is split-only:**

> “**(b) The integer carry layer is split-only.** WM-COB(i):
> D_j = t_j(β″_j) − t_j(β_j) − t_j(β′_j) = (∂t_j)(γ,γ′) — computed from the
> eq-12 splits alone, integers in, integers out; likewise δ (the [ILN]† S0.3
> outer count). No letter value enters. Citation note: WM-COB(i)'s footprint is
> [ILN]† SHIFT-(n) + D_j-INT ONLY, both unconditional and ACCEPTED — the
> WELDMASTER note that displays the identity is itself at arc 1/2, but this
> clause rides the accepted [ILN]† layer, not the WM arc.
> [machine: recorded per scored pair (`int_layer_sample`), not scored — the
> split-only property is definitional in the engine.]”

**DERIVATION:** `[IMPORTED — with a careful citation split]` The identity is displayed by WELDMASTER (arc 1/2), but the note refuses to inherit that grade: it isolates the footprint to `[ILN]† SHIFT-(n) + D_j-INT`, both unconditional and ACCEPTED, and rides those instead. **This is a model citation-hygiene move** — the display's owner is at a lower grade than the content's suppliers, and the note consumes the suppliers.

**⚠ THIS CLAUSE IS THE SUBJECT OF E8 FINDING #4 (REAL).** The adjudication reads: “ “[C]omputed from the eq-12 splits alone” proves dependence on the chosen splits, not dependence only on \((e_j,h_j)\). No section-independence or canonical-section lemma appears at HEAD.” S10.1's 𝒢 retains the split/grid record inside the count-shape rather than claiming the reduction (EFF.JD0.32).

**CONDITIONALITY:** [ILN]† SHIFT-(n) and D_j-INT at ACCEPTED grade; the machine record is *recorded, not scored* — the note discloses that this leg is definitional in the engine and therefore not independently checked.

**RESOLUTION TRACE:** S2.1(b); scoped by S10.1's 𝒢.

**TEETH:** `int_layer_sample` — **recorded, not scored** (the note's own disclosure).

---

### EFF.JD0.11  [lemma]

**CANONICAL STATEMENT — S2.1(c), the lattice exponent:**

> “**(c) The lattice exponent is shape-only.** B is a function of grid
> positions: the grid = T-side abscissas (JA-GRID — GRTJA, arc 1/2, priced),
> the vertex-for-vertex polygon with slope/length/degree preserved (JB-VTX —
> GRTJB, ACCEPTED 2/2), the same-division transfer (JB-DEV — GRTJB, ACCEPTED).
> By (a) the gauge does not move these positions; by (b) the integer carry
> data they generate are split-only. Hence B(T) is fixed by the shape and
> unchanged under any unit gauge.”

**DERIVATION:** `[PROVED from (a) + (b), on priced inputs]` The grid is a position datum (three named supplier faces, one priced at arc 1/2 and two ACCEPTED); positions are gauge-fixed by (a); the integers they generate are split-only by (b); therefore B is gauge-fixed.

**⚠ “shape-only” IS RE-SCOPED TO “𝒢-only” BY S10.1.** The gauge-invariance half stands unchanged; the *shape-determination* half is exactly finding #4's target. S10.1's repaired derivation, quoted:

> “Derivation of the repaired L4 clause: equality of 𝒢 fixes the chosen splits,
> so the displayed formula D_j = ∂t_j gives equal D_j term by term; it fixes
> the corresponding δ and grid positions as well. Hence it fixes B(𝒢).
> Unit gauges do not move any of these positions because units have valuation
> zero. This proves the L4 assertion at exactly the scope established by
> S2.1, without inferring that an arbitrary change of section leaves D_j or
> B unchanged.”

**CONDITIONALITY:** JA-GRID at GRTJA arc 1/2 (priced, not accepted) — this is the weakest input in the L4 leg and it is disclosed as such (EFF.JD0.25).

**RESOLUTION TRACE:** S2.1(c) → re-scoped by S10.1.

**TEETH:** JD0-COB (1), (3); JD0-STRAT.

---

### EFF.JD0.12  [lemma]

**CANONICAL STATEMENT — S2.1(d), the gauge moves the ledger by exactly a coboundary:**

> “**(d) The gauge moves the ledger by exactly a coboundary.** With gauged
> anchors a′_γ = u_γ·a_γ, the carry constant transforms as
> c′(γ,γ′) = c(γ,γ′)·u_γ u_{γ′} u_{γ+γ′}^{−1} = (c·∂u)(γ,γ′): gauging moves
> WITHIN the cohomology class; the class's integer layer is (b); the positions
> are (a). So "carry gauge" is well-named: it is the coboundary action.
> [machine: JD0-COB check (4), exact equality in K_top on every scored pair.]”

**DERIVATION:** `[PROVED — a computation, machine-confirmed exactly]` The transformation law is a direct substitution; the point is the identification with ∂u, i.e. that the gauge action is precisely the coboundary action, so it moves within a cohomology class and never between classes.

**⚠ THIS IS THE CLAUSE S10.3 GUARDS AGAINST OVER-READING.** Because the gauge moves only within a class, gauge-invariance **cannot by itself** give cross-class blindness — finding #5. See EFF.JD0.34.

**CONDITIONALITY:** none beyond (a), (b).

**RESOLUTION TRACE:** S2.1(d); untouched, but its *consequence* in S3 is corrected by S10.3.

**TEETH:** JD0-COB (4) — 0/7,527, “c′ = c·∂u exact in K_top on every scored pair”; JD0-T-COB (487 fires — “the dropped-u corruption breaks the law whenever u_{γ+γ′} ≠ 1”).

---

### EFF.JD0.13  [lemma]

**CANONICAL STATEMENT — S2.2(e), the slot action:**

> “**(e) The gauge acts on each slot as a unit character.** Through the
> harness, the read of a u-gauged anchor is u·(the direct read) — a bijection
> of the slot's value space K_j.
> [machine: JD0-COB check (2); plus the fuzz leg (5): higher-w junk in the
> representative changes neither position nor read — the read consumes only
> the line.]”

**DERIVATION:** `[IMPORTED — the harness]` Multiplication by a unit is a bijection of K_j. The fuzz leg is a representative-independence check: the read depends only on the line, so higher-w junk is invisible.

**CONDITIONALITY:** “Through the harness” — that the weld's read actually behaves this way is BOX-2's warrant, priced at GRTJA 1/2.

**RESOLUTION TRACE:** S2.2(e); untouched.

**TEETH:** JD0-COB (2) and (5).

---

### EFF.JD0.14  [lemma]

**CANONICAL STATEMENT — S2.2(f), with its superseded sentence marked:**

> “**(f) The induced action on residual polynomials preserves factorization
> type POINTWISE.** The slot character acts on the level-j residual polynomial
> by the variable rescaling σ_ξ : R(y) ↦ ξ^{−deg R}·R(ξ·y) (JA-RES's
> z^{−iA}·C_i slot form; the overall unit (ξ,w) is killed by monic
> normalization). σ_ξ extends the identity of K_j to a degree-preserving
> K_j-algebra automorphism of K_j[y]; automorphisms carry factorizations to
> factorizations, so deg, monicity, irreducibility, ψ(0) ≠ 0, and the full
> factorization-degree multiset are preserved for EVERY R — not merely in
> census. Consequently σ_ξ permutes the letter menu (monic irreducibles of
> degree g_j with nonzero constant) and fixes every type census and the menu
> cardinality.
> [machine: JD0-L3 — full enumeration of every monic R of degree D ≤ 3 over
> every roster level field (capped |K|^D ≤ 20000): pointwise type equality,
> bijectivity of the gauged family, census equality, menu-count equality.]”

**⚠ ONE SENTENCE OF THE ABOVE IS DELETED FROM THE EFFECTIVE TEXT.** S10.2 instructs, verbatim: “Delete the sentence

>     “σ_ξ extends the identity of K_j to a degree-preserving
>      K_j-algebra automorphism of K_j[y]”

and replace it by the following derivation.” The quotation above reproduces S2.2(f) as the body writes it, so the cross-read can see exactly what was removed; **the governing text drops that sentence and substitutes EFF.JD0.15.**

**DERIVATION:** `[PROVED — but NOT by the route originally displayed]` The **conclusion** is correct and survives; the **route** does not. E8 finding #3 (REAL): “The normalized degreewise map is not an algebra automorphism of \(K_j[y]\). Factorization preservation needs the degreewise multiplicativity argument supplied below.” The replacement is EFF.JD0.15.

The reason the original was wrong is worth stating: σ_ξ as displayed carries the factor ξ^{−deg R}, which depends on the degree of its argument, so the map is not additive across degrees and hence not a ring map. It is a *degreewise* family of bijections, and multiplicativity across the family is what factorization preservation actually needs.

**CONDITIONALITY:** JA-RES's slot form at GRTJA arc 1/2 (BOX-2).

**RESOLUTION TRACE:** S2.2(f), with the automorphism sentence **deleted** by S10.2 and replaced by EFF.JD0.15.

**TEETH:** JD0-L3 — 0/60,950, but note the disclosed finite scope: degree D ≤ 3, capped |K|^D ≤ 20000. The E8 adjudication defends this (finding #12, ARTIFACT): “The universal argument is the symbolic proof in S2; the battery is recorded as supporting machine evidence. No sentence claims that the finite enumeration proves the universal quantifiers.”

---

### EFF.JD0.15  [lemma]

**CANONICAL STATEMENT — S10.2's replacement derivation (the governing proof of pointwise preservation):**

> “For each fixed degree D define
>
>     σ_{ξ,D}(R)(y) = ξ^(−D) R(ξy)
>
> on the monic degree-D polynomials. It is a bijection with inverse
> σ_{ξ^−1,D}. It is not asserted to be additive on polynomials of different
> degrees. Instead, if R = AB with deg A = a, deg B = b, and D = a+b, then
>
>     σ_{ξ,D}(R)
>       = ξ^(−a−b) A(ξy)B(ξy)
>       = σ_{ξ,a}(A) · σ_{ξ,b}(B).
>
> Thus a factorization of R gives a factorization with exactly the same
> factor degrees. Conversely, applying the degreewise inverse to a
> factorization of σ_{ξ,D}(R) gives a factorization of R. Hence irreducibility
> and the complete factorization-degree multiset are preserved. Monicity is
> preserved because the leading coefficient is multiplied by ξ^(−D)ξ^D = 1,
> and a constant term is nonzero exactly when its nonzero scalar multiple
> ξ^(−D)R(0) is nonzero. This proves the pointwise preservation required by
> S2.2(g).”

**DERIVATION:** `[PROVED — degreewise multiplicativity]` The key identity is ξ^{−(a+b)}A(ξy)B(ξy) = (ξ^{−a}A(ξy))·(ξ^{−b}B(ξy)), i.e. the exponent splits exactly along the degree split. **Independently re-derived at compile time:** with D = a+b, ξ^{−D} = ξ^{−a}ξ^{−b}, so ξ^{−D}A(ξy)B(ξy) = (ξ^{−a}A(ξy))(ξ^{−b}B(ξy)) = σ_{ξ,a}(A)·σ_{ξ,b}(B) ✓. Monicity: the leading term of σ_{ξ,D}(R) is ξ^{−D}·(ξy)^D = y^D ✓. Constant term: σ_{ξ,D}(R)(0) = ξ^{−D}R(0), nonzero iff R(0) is ✓. The orchestrator's own re-derivation is recorded in the footnote (EFF.JD0.38).

Note the exactness of the repair: it proves *strictly what is needed* (preservation of the factorization-degree multiset, monicity, nonzero constant) and explicitly disclaims what is not true (“It is not asserted to be additive on polynomials of different degrees”).

**CONDITIONALITY:** monic polynomials of fixed degree D; ξ a unit.

**RESOLUTION TRACE:** S10.2; replaces the deleted sentence of S2.2(f).

**TEETH:** JD0-L3 (the pointwise type-equality and bijectivity legs test exactly this claim).

---

### EFF.JD0.16  [lemma]

**CANONICAL STATEMENT — S2.2(g):**

> “**(g) Hence every L3_j count is gauge-invariant.** The stratum's residual
> condition at level j is SHAPE data — a factorization-degree/type condition
> on the slot value. By (f) the gauge preserves the condition pointwise, so
> the direct and gauged strata are equal AS SETS slotwise; a fortiori the
> counts agree. (More generally, any slot-wise bijection preserving the
> condition preserves the count; the unit character does both. A bijection
> NOT preserving the condition breaks counts — the NONCHAR tooth.)”

**DERIVATION:** `[PROVED]` Pointwise preservation of the defining condition (EFF.JD0.15) makes the direct and gauged strata **equal as sets**, which is stronger than equinumerous — the counts then agree a fortiori. The parenthetical isolates the mechanism: bijectivity alone is insufficient; condition-preservation is what matters, and the NONCHAR tooth exhibits a bijection that fails it.

**⚠ THE MACHINE CONFIRMS THE MECHANISM CLAIM QUANTITATIVELY.** S9's P-6: “NONCHAR 11,514 fires (the additive slot bijection breaks type preservation massively — value-layer bijectivity alone is NOT the mechanism; the character form is load-bearing)”. This is a rare case of a tooth *supporting a positive claim about which hypothesis is doing the work*, not merely detecting a corruption.

**CONDITIONALITY:** the residual condition is “SHAPE data” — this phrase is exactly what S10.1's Λ_j makes precise (finding #2).

**RESOLUTION TRACE:** S2.2(g); its “SHAPE data” phrase re-scoped by S10.1's Λ_j.

**TEETH:** JD0-L3; JD0-T-NONCHAR (11,514 fires).

---

### EFF.JD0.17  [lemma]

**CANONICAL STATEMENT — S2.3, clause (i):**

> “Clause (i): the assembled count = ∏_j L3_j × q^{±B}; (c) fixes the exponent
> leg, (g) fixes each census leg. ∎(i)”

**DERIVATION:** `[PROVED — two-leg assembly]` `by EFF.JD0.11` for the exponent, `by EFF.JD0.16` for each census factor. A product of gauge-invariant factors is gauge-invariant.

**CONDITIONALITY:** the perimeter of EFF.JD0.05; BOX-4/S10.4 for the identification with the actual classifier coefficient.

**RESOLUTION TRACE:** S2.3; re-keyed to μ_T̂(q) by S10.1.

**TEETH:** JD0-STRAT 0/37.

---

### EFF.JD0.18  [lemma]

**CANONICAL STATEMENT — S2.3, clause (ii):**

> “Clause (ii): let T, T′ have equal shape data. The assembly consumes ONLY:
> positions and integer carry data — by (b)/(c) functions of the splits, i.e.
> of (e_j, h_j)-data, equal for T, T′; and censuses over the K_j — the number
> of monic polynomials of degree D with factorization type λ over F_r depends
> only on r (the universal S_λ(q) polynomials; classical), and
> r_j = q^{d0·g_0···g_{j−1}} is fixed by the residual-degree structure. The
> letters z_j and the carry values c(γ,γ′) appear at NO point of the
> assembly. ∎(ii)”

**DERIVATION:** `[PROVED — by NON-OCCURRENCE]` The proof strategy is the important thing and is easy to under-read: clause (ii) is **not** a gauge argument at all. It is the observation that the assembly formula contains no letter and no cochain value, so any two types with equal inputs give equal outputs — **including types whose cochains lie in different cohomology classes**. S10.3 makes this explicit (EFF.JD0.34), and the E8 adjudication relies on it to defeat finding #7 (ARTIFACT): “Clause (ii)’s carry-class independence is argued in S2.3 from: “the letters \(z_j\) and the carry values \(c(\gamma,\gamma')\) appear at NO point of the assembly.” Thus this finding correctly describes test coverage but incorrectly treats that limitation as a proof gap.”

**⚠ THE PHRASE “i.e. of (e_j, h_j)-data” IS FINDING #4's TARGET** and is repaired by S10.1's 𝒢: equality of the *split/grid record*, not of the shorter tuple, is what the argument actually uses.

**CONDITIONALITY:** the universal S_λ(q) census polynomials (classical); equal T̂ after S10.1.

**RESOLUTION TRACE:** S2.3 → re-keyed by S10.1, whose own derivation of (ii) is EFF.JD0.33.

**TEETH:** JD0-BLIND 0/7 — three constructed same-shape pairs with **verified different letters**; JD0-T-SHAPE (the different-shape control must differ).

---

### EFF.JD0.19  [record]

**CANONICAL STATEMENT — S2.4, the algebra-level shadow:**

> “GRTJC W-9(iii) + JC-F1 [ACCEPTED 2/2]: at every composite stage the carry
> algebra is the twisted group algebra K^c[ℤ/E] and
>
>     𝒜(T) ≅ K[v]/(v^E − ζ_T),   dim_K 𝒜(T) = E   — independent of c.
>
> Dimension is carry-blind; twisting by a coboundary gives an isomorphic
> twisted algebra (K^{c·∂u}[ℤ/E] ≅ K^c[ℤ/E], basis rescale [γ] ↦ u_γ·[γ]).
> And WM-COB(iii) [display owned by WELDMASTER, arc 1/2; value law on its
> scored stratum]: the ENTIRE inner factor of the accepted cocycle is ∂E — a
> coboundary of the ε-kit cochain. So at the algebra level the value layer
> contributes nothing gauge-stable except the outer letter z̄^δ — and its
> EXPONENT δ is integer/split data, already counted in the shape. J-D0 is the
> count-level statement of the same death: what the gauge can move, counts
> cannot see; what counts see (positions, δ, degree structure), the gauge
> cannot move.”

**DERIVATION:** `[IMPORTED — and explicitly NOT load-bearing]` The closing chiasmus is the note's thesis in one sentence. But the section carries no proof weight: S7 says “S2.4 only, not load-bearing for S2.1–S2.3”, and the E8 adjudication uses that disclosure to defeat finding #6 (ARTIFACT): “The note does not use equality of dimensions to prove equality of counts or algebra isomorphism across distinct twisting classes.”

**CONDITIONALITY:** GRTJC's clause at ACCEPTED 2/2; WM-COB(iii) at WELDMASTER arc 1/2 — the weaker input, and it sits only here. S10.3 adds: “The algebra discussion in S2.4 remains only an algebra-level shadow and is not proof of cross-class blindness.”

**RESOLUTION TRACE:** S2.4; its non-load-bearing status re-affirmed by S10.3.

**TEETH:** none — the battery does not test the algebra shadow.

---

### EFF.JD0.20  [scope-record]

**CANONICAL STATEMENT — S3's uniformity mechanism:**

> “Counts of decided strata are shape-only (S2.3), so per shape the count is
> ONE expression in q — the L3 universal residue-shape polynomials times a
> q-power with shape-determined exponent — valid at every residue
> characteristic, wild included. Everything the wild carry layer could
> contribute (letters, cochain values, the realization of its H² class)
> enters the classifier only through the gauge orbit, and counts are constant
> on gauge orbits.”

> “This is the counting-side face of the interpretation note's H-B verdict
> (HIGHLEVEL_INTERPRETATION_2026-08-07 §2, MEASURED): the wild part of the
> carry class DIES over finite residue fields (all-wild classes trivial on
> T3A/T2A; the tame ℤ/2 class survives on T2C). J-D0 prices both halves: the
> wild death is harmless to densities because counts never consumed the class's
> value realization in the first place; the tame survivor enters counts only
> through the integer δ (position data), never through the letter z̄^δ.”

**DERIVATION:** `[SCOPE RECORD — with TWO corrections applied]`

Correction 1 (finding #5, S10.3): the final sentence of the first paragraph is **wrong about the mechanism**. E8: “S2.3 independently proves cochain-value blindness by non-occurrence of \(c\), but S3’s explanation incorrectly attributes the death of the entire class to constancy on gauge orbits. Gauge invariance alone only compares coboundary-related representatives.” S10.3's replacement text is EFF.JD0.34 — and the replacement is a *strengthening*.

Correction 2 (finding #8, S10.4): “valid at every residue characteristic, wild included” is unconditional **for the formal coefficient** and supplier-perimeter-bounded for the actual classifier (EFF.JD0.35).

**CONDITIONALITY:** H-B is a MEASURED verdict cited as interpretation, carrying no proof weight (NON-IMPORT N-8).

**RESOLUTION TRACE:** S3 → corrected by S10.3 and S10.4.

**TEETH:** JD0-L3 at the wild rows; JD0-BLIND's wild pairs (P2A/P2B p = 7 wild; P3A/P3B p = 3 depth-2 wild).

---

### EFF.JD0.21  [scope-record]

**CANONICAL STATEMENT — the H-C positioning and the ∫|disc| separator:**

> “And it is the lemma under H-C's positioning sentence (§3.2 there): **"Haar
> volume is carry-gauge-invariant; the discriminant exponent is not."** The
> corpus's own separator (HUMAN_PROOF §0: ∫|disc| = 7/12 ≠ 2/3 at p = 2 while
> the volume is the uniform q/(q+1)) is exactly a functional that PRICES the
> carries (v(disc) pays the wild different letter-by-letter), re-importing the
> value layer that volume quotients out. The class of integrals the carry
> algebra uniformizes = bounded functions factoring through the type map with
> p-free weights — i.e. through the shape, i.e. through what J-D0 proves
> gauge-blind. H-C supplied the vocabulary; this note supplies the lemma.”

**DERIVATION:** `[SCOPE RECORD]` The separator is the corpus's own worked counterexample to naive uniformity: ∫|disc| = 7/12 at p = 2 against the uniform 2/3, while the *volume* is q/(q+1) uniformly. J-D0 explains the asymmetry: v(disc) prices the wild different letter-by-letter, re-importing exactly the layer counts are blind to.

**⚠ THE FINAL SENTENCE'S CHARACTERIZATION IS SUPERSEDED BY S10.5** (finding #9, REAL): “ “Factoring through the type map” is strictly weaker than being constant on fibers of `type → shape`. The uniformized integral class must be stated using the latter condition.” The governing characterization is EFF.JD0.36, which adds a second necessary condition the body's version missed: p-freeness of the *rule* is not enough, because “a rule can be p-free while still distinguishing letters or carry values.”

**CONDITIONALITY:** H-C is interpretation only (NON-IMPORT N-8).

**RESOLUTION TRACE:** S3 tail → superseded by S10.5.

**TEETH:** none.

---

### EFF.JD0.22  [fence]

**CANONICAL STATEMENT — S4, the six honest boxes (load-bearing inventory):**

> “* **JD0-BOX-1 (J-D0 is not W-10).** Nothing here proves the count EQUALITY
>   "classifier stratum count = model-side count from (e_j,h_j,g_j) through
>   the welded read" (W-10, M-priced). J-D0 deletes W-10's value-layer
>   hypotheses — the collapse perimeter ((VD-m), (C-coll), (R-coll),
>   (DMULT-s), PERIM-μ) drops out of the count equality's conditionality —
>   but the B(c)-tie to the welded grid still owes JB row 12's u ≥ 0 proviso
>   leg and rides JA-GRID at GRTJA's arc grade.
> * **JD0-BOX-2 (the gauge-form warrant).** That the weld's ENTIRE conditional
>   value dictionary acts by slot-wise unit characters is the synthesis §3.2
>   reading of JA-RES [GRTJA, 1/2], the (ξ,w) covariance family [GRTW2,
>   ACCEPTED], and the ℓ-orbit rescalings ORB-RES [J-A]. J-D0's proof covers
>   unit-character gauges; any value layer NOT of that form is outside the
>   perimeter — and the NONCHAR tooth displays the failure mode (an additive
>   slot shift is a bijection that breaks type preservation, hence counts).
> * **JD0-BOX-3 (the menu is untouched).** WHICH decided shapes exist — the
>   omMenu growth beyond order 0, hExhaust, drainage — is Phase-B. J-D0
>   counts within a shape; it never enumerates shapes.
> * **JD0-BOX-4 (assembly identification).** That the L3 × L4 assembly IS
>   L2core's per-cell coefficient at all orders is L2core's own verified
>   content (exact instances through OM order 4; BB3 counts exact over 8
>   fields) — consumed, not re-proved. J-D0's invariance applies to the
>   assembly as displayed in S0.
> * **JD0-BOX-5 (F₂ degeneracy).** Over K_j = F₂ the unit group is trivial:
>   clause (i) is vacuous at such levels (the battery census reports them;
>   the representative-independence content — re-realized anchors, fuzz
>   blindness — is still checked there). Clause (ii) is NOT vacuous at F₂.
> * **JD0-BOX-6 (wild decidedness warrant).** That "decided shape" is
>   well-posed at wild composite stages — strata do not collapse under the
>   read — is the no-cancellation/level-detection warrant JC-PSIKER(iii) +
>   JC-LOAD, consumed from GRTJC [ACCEPTED] as warrant, exactly as the
>   blueprint's O-1 clause routes it. J-D0 adds no leg to it.”

**DERIVATION:** `[FENCE — the consumption inventory]` Each box is what a consumer must carry:

- **BOX-1** is the *positive* content of the note stated negatively: J-D0 deletes W-10's value-layer hypotheses but leaves two grid obligations (JB row 12's u ≥ 0 proviso; JA-GRID at arc grade).
- **BOX-2** is the note's single largest exposure and the ledger tracks it as such — HYP.74, class `hypothesis-box`, whose STATEMENT quotes it verbatim and characterizes it: “JD0 unit-character warrant is synthesis, not proved theorem”. Its disposition: “**CARRY — reachability SIGNED**”, and the discharge route is named: “Discharge is a fold, not a theorem: record per site that the gauge consumed there is an explicit z-power unit char[acter]…”.
- **BOX-4** is narrowed by S10.4: exact instances through OM order 4 do not give an all-orders identification.
- **BOX-5** is a precise vacuity disclosure with a non-vacuity counterpart — clause (i) is empty over F₂, clause (ii) is not — and the machine reports the census (S9 P-2: “252 residual-gauge levels nontrivial, 57 trivial (F₂/unit-group-1 levels, per BOX-5)”; P-4: “the one residue-trivial row is C4A, the all-F₂ tower, exactly the BOX-5 disclosure”).

**CONDITIONALITY:** all six are open; the owner signature (HYP.39) closes the *grade cap*, not the boxes.

**RESOLUTION TRACE:** S4; BOX-4 narrowed by S10.4. BOX-2 carried downstream as ledger HYP.74 (RX-2).

**TEETH:** JD0-T-NONCHAR (BOX-2's failure mode); the F₂ census (BOX-5).

---

### EFF.JD0.23  [record]

**CANONICAL STATEMENT — S5, the order-0 Lean tie:**

> “The machine-checked capstone's order-0 count identity
> (`lean/LeanUrat/OM/RealInstanceV2.lean`, `realF2`):
>
>     density m σ q'  :=  ∑_{T ∈ omMenu m σ} omCount T q'
>
> with `omCount : shape → ℕ → ℚ` — the evaluator's SIGNATURE takes the shape
> and q′ only: no letter, no cochain, no tower argument exists to vary. So at
> order 0 clause (ii) holds BY TYPE (definitionally), and clause (i) is empty
> (E = 1, no carries). Displays: `omCount_sepShape_eq_rootCount` (the count as
> a root census — the L3 leg at order 0), the non-vacuity gates
> `gate3_inert_q2` (value 1/4 at n = 2 inert, q′ = 2) and the split ≠ inert
> separation at q′ = 3 (`OM/RealInstanceV2Gates.lean`). The Lean tie is the
> degenerate instance of J-D0 — an anchor for what the order-≥ 1 statement
> must degenerate TO, not evidence for it.”

**DERIVATION:** `[RECORD — a type-level observation]` The argument is elegant and is the only place J-D0 touches Lean: `omCount : shape → ℕ → ℚ` has **no argument** in which a letter or cochain could sit, so clause (ii) is definitionally true at order 0 — “BY TYPE”. And clause (i) is *empty* at order 0 because E = 1, so there are no carries to gauge. **The Lean tie is therefore doubly degenerate**, and the note says so, refusing to count it as evidence.

**Compile-time verification of all four Lean anchors** (§4.2): `realF2` present in `OM/RealInstanceV2.lean` and `OM/RealInstanceV2Gates.lean`; `omCount_sepShape_eq_rootCount` present in three files including `OM/OMCountV2.lean`; `gate3_inert_q2` present in `OM/OMCountV2.lean` (and independently verified in `spec/EFF-W11.md` to read `omCount (sepShape 2 lam2) 2 = 1 / 4`); the q′ = 3 split ≠ inert separation present in `OM/RealInstanceV2Gates.lean` as `gate_v2_split_q3` / `gate_v2_inert_q3` / `gate_v2_sigma_separation`, with the file's own docstring recording “`1/9` vs `1/3`”.

**CONDITIONALITY:** anchor only, explicitly not evidence (NON-IMPORT N-9).

**RESOLUTION TRACE:** S5; untouched by the append.

**TEETH:** none — the Lean side is outside the battery.

---

### EFF.JD0.24  [record]

**CANONICAL STATEMENT — S6, consumers:**

> “1. **W-10 — OPENS at the (2,1,1) Eisenstein shape.** Its first row: d0 = 1,
>    one read (2,1,1); stratum count (q−1)/q³ = [L3: the residual unit choice,
>    q−1 values] × [L4: v(a₁) ≥ 1 pins one digit, v(a₀) = 1 pins position and
>    frees the unit slot — q-power q^{−3} in density normalization]
>    (WELD_SYNTHESIS §3.1 display). By J-D0 the W-10 equality at this and
>    every decided shape carries NO value-layer hypothesis: its obligations
>    are the integer-grid faces (JA-GRID, JB-VTX(d) — with JB row 12's
>    proviso) + the classical L3 census + the JC warrant (BOX-6).
> 2. **W-11** (menu tie, Phase-B) — consumes J-D0(ii) to key the grown menu
>    by shape data alone.
> 3. **W-12 / J-D1** — downstream composition; untouched here.
> 4. **H-C positioning** — "carry-gauge-invariance of volume" upgraded from
>    vocabulary to lemma (S3); the public-facing uniformity story should cite
>    J-D0 as the mechanism where the cohomology class dies.
> 5. **Fence bookkeeping** — count-layer consumers need not carry PERIM-μ /
>    (C-coll)/(R-coll)/(VD-m); this deletes the fence-patching genre from the
>    J-D block entirely.”

**DERIVATION:** `[RECORD]` Consumer 1's arithmetic is the W-10 first row, and it is the same row `spec/EFF-W11.md` compiles as W-11's RAM(1) (density (q−1)/q³) — the two notes agree.

**⚠ CONSUMER 2 GAINS A NEW OBLIGATION UNDER S10.6.** “consumes J-D0(ii) to key the grown menu by shape data alone” is exactly what S10.1 no longer licenses. S10.6 item 2: “If W-11 wants to key it only by (d0,(e_j,h_j,g_j)_j), it separately owes proofs that Λ_j and 𝒢, or at least all their consumed censuses and B, are determined by that shorter tuple.” See OPEN-CALL 1.

**⚠ CONSUMER 4's CHARACTERIZATION IS CORRECTED BY S10.5** (EFF.JD0.36) and **consumer 5's by S10.6 item 5's guard** (EFF.JD0.37).

**CONDITIONALITY:** all five re-stated at corrected scope by S10.6.

**RESOLUTION TRACE:** S6 → swept by S10.6. **Verified live at seven sites** (§4.4), not five: the ledger's audited list (W11, W12, HMENU3, HEX3, GENIND, GENH4, GENHN) is larger than S6's own consumer list, which names W-10, W-11, W-12 and two non-note consumers.

**TEETH:** none.

---

### EFF.JD0.25  [table]

**CANONICAL STATEMENT — S7, the conditionality stack (load-bearing inventory):**

`[TABLE]` Accepted suppliers (“the FOUR weld acceptances + the engine”), transcribed from the note's bulleted list without reordering:

| Supplier | Grade | Consumed as |
|---|---|---|
| **[ILN]†** | ACCEPTED | “SHIFT-(n), D_j-INT, EXP-KIT(a) — the integer/ cochain layer feeding S2.1(b)” |
| **GRTJB** | ACCEPTED 2/2 | “JB-VTX vertex-for-vertex polygon, JB-DEV same-division — the position faces S2.1(a)/(c); row 12's u ≥ 0 proviso carried where B-ties are consumed (BOX-1)” |
| **GRTJC** | ACCEPTED 2/2 | “W-9 + JC-F1 (dim K^c[ℤ/E] = E, the algebra shadow S2.4); JC-PSIKER(iii)/JC-LOAD (the wild decidedness warrant, BOX-6)” |
| **GRTW2** | ACCEPTED 2/2 | “the (ξ,w) covariance family — part of the gauge dictionary (BOX-2); anchor re-choice legitimacy rides the accepted lift covariance (with **LIFTCORNER** [ACCEPTED 2/2] as its corner cap)” |

`[TABLE]` Priced non-accepted suppliers:

| Supplier | Grade | Consumed as |
|---|---|---|
| **GRTJA** | arc 1/2 | “JA-RES slot-character form; JA-GRID grid = T-side abscissas. J-D0 consumes both AT THAT GRADE (BOX-2, S2.1(c))” |
| **WELDMASTER** | arc 1/2 | “WM-COB display ownership; clause (i) cited at its [ILN]†-only footprint (unconditional), clause (iii) at the WM arc grade (S2.4 only, not load-bearing for S2.1–S2.3)” |
| **H-B / H-C** | MEASURED (interpretation) | “cited as interpretation in S3 — no proof weight anywhere in S2” |

**DERIVATION:** `[CONDITIONALITY STACK]` The structure is deliberate and should be read carefully by any consumer:

- **Everything load-bearing in S2.1–S2.3 rides ACCEPTED suppliers, with exactly one exception:** GRTJA at arc 1/2, which supplies BOTH the JA-RES slot-character form (feeding BOX-2 and EFF.JD0.13/.14) AND JA-GRID (feeding EFF.JD0.11). **GRTJA is the single priced input in the proof's critical path.**
- WELDMASTER is at arc 1/2 but is split: clause (i) is consumed only at its unconditional [ILN]†-only footprint (EFF.JD0.10's citation note), and clause (iii) only in the non-load-bearing S2.4.
- H-B/H-C carry zero proof weight.

**CONDITIONALITY:** the whole stack is grade-relative — the GRADE box says so: “consuming the supplier stack S7 at the grades listed there.”

**RESOLUTION TRACE:** S7; untouched by the append.

**TEETH:** JD0-PIN 0/2 (frozen imports unmoved).

---

### EFF.JD0.26  [battery-frame]

**CANONICAL STATEMENT — S8, the sealed battery:**

> “**Runner:** `verification/openmath/jd0_checks.py`. **Roster:** the frozen
> 29-row J-C roster (md5-pinned imports) + the OUT-OF-ROSTER p = 7 genre
> (J7A wild (7,1,1); J7B d0 = 2, F₄₉, wild top (7,3,1); J7C g₀ = 2 → F₄₉) +
> the constructed same-shape pair members (P1B; P2A/P2B; P3A/P3B) — 37
> towers. Families JD0-PIN / JD0-L3 / JD0-COB / JD0-STRAT / JD0-BLIND and
> teeth JD0-T-NONCHAR / JD0-T-NONUNIT / JD0-T-COB / JD0-T-SHAPE as specified
> in the runner docstring (the note's S2 machine-leg brackets map to them).
> Same-shape pairs and their letter separations: (C2A, P1B) — letters
> X̄ vs X̄² in F₄; (P2A, P2B) — p = 7 wild (7,2,1), letters −1 (ord 2) vs −3
> (ord 3); (P3A, P3B) — p = 3 depth-2 wild, letters 2 (ord 2) vs 1 (trivial).
> Negative control: (C2A, C2B) — different shapes, records MUST differ.
> Gauges are seeded-deterministic, nontrivial wherever the unit group allows
> (census reported; F₂ rows disclosed per BOX-5). PRE-SEAL SMOKE DISCLOSED:
> developed against {C2A, C2I, C2B, J7A, P2A, P2B}; on the smoke rows all
> families 0 violations, all four teeth fired. No full run before the seal.”

**DERIVATION:** `[MACHINE FRAME]` **The same-shape pairs are constructed, not found**, and their letter separations are chosen to be adversarial: two of the three pairs separate by multiplicative *order* (ord 2 vs ord 3; ord 2 vs trivial), and the third — (C2A, P1B) — separates by element at the *same* order (X̄ vs X̄² in F₄, both order 3), which S9 calls “the finest separation”. A pair with the same order is harder for a value-leaking engine to distinguish, so it is the strongest of the three.

The out-of-roster p = 7 genre is a genuine out-of-sample extension (three towers: J7A/J7B/J7C), and the pre-seal smoke is disclosed with its exact row list.

**CONDITIONALITY:** BOX-5 (F₂ levels have trivial unit group; clause (i) vacuous there); the disclosed smoke rows are not blind.

**RESOLUTION TRACE:** S8; untouched.

**TEETH:** see §6.

---

### EFF.JD0.27  [battery-frame]

**CANONICAL STATEMENT — S8's preregistered predictions:**

> “* P-1: JD0-PIN — 0 violations (frozen imports unmoved).
> * P-2: JD0-L3 — 0 violations on ALL towers, levels, degrees (pointwise type
>   preservation, bijectivity, census equality, menu equality), including
>   every p = 7 field and every wild-read level.
> * P-3: JD0-COB — 0 violations: gauged positions exact, character action
>   exact, coboundary law c′ = c·∂u exact in K_top on every scored pair, fuzz
>   blindness exact; ≥ 1 scored pair on every tower.
> * P-4: JD0-STRAT — all 37 towers: assembled count record (direct) =
>   (gauged).
> * P-5: JD0-BLIND — all three same-shape pairs: letters verified DIFFERENT,
>   full count records EQUAL (including realized polygon integer records);
>   the (C2A, C2B) control differs.
> * P-6: all four teeth fire ≥ 1 globally.
> * P-7 (falsifiers, blueprint break-analysis order): a pointwise JD0-L3
>   violation at a wild level refutes S2.2(f); a position move under a unit
>   gauge refutes S2.1(a) and the lemma; a blind-pair mismatch in the polygon
>   records means the engine's positions leak values — diagnose as an
>   upstream weld error FIRST (GRT_WELD_BLUEPRINT S4's J-D row).”

**DERIVATION:** `[PREREGISTRATION]` P-5's parenthetical is the sharpest check in the battery: “including realized polygon integer records” — S2.3's machine bracket calls it “a genuine check that the engine's positions do not leak values”. P-7 routes each falsifier to the exact clause it would refute (S2.2(f), S2.1(a)) or, for a position leak, upstream to the weld.

**CONDITIONALITY:** none.

**RESOLUTION TRACE:** S8; all seven confirmed at S9.

**TEETH:** see §6.

---

### EFF.JD0.28  [table]

**CANONICAL STATEMENT — S9, the verdict:**

> “**GREEN. 37/37 towers, 68,523 checks, 0 violations in every family, all
> four teeth fired, zero skips.** Artifacts: `jd0_checks_output.txt` (md5
> 01f5c79509cf3bba8a24569abff081c7), `jd0_checks_results.json` (md5
> 6fbc1f03a88bbd5408927bfb8324d415); runner byte-identical to the sealed
> commit; elapsed 77.4 s.”

`[TABLE]` The prediction scorecard (the note displays it as seven bullets; transcribed with the note's own wording):

| Pred | Result | Decisive detail |
|---|---|---|
| P-1 ✓ | JD0-PIN 0/2 | “frozen imports unmoved” |
| P-2 ✓ | JD0-L3 0/60,950 | “pointwise type preservation, bijectivity, census and menu equality on every enumerated family, including all five p = 7 rows (J7A/J7B/J7C/P2A/P2B) and every wild-read level; 252 residual-gauge levels nontrivial, 57 trivial (F₂/unit-group-1 levels, per BOX-5)” |
| P-3 ✓ | JD0-COB 0/7,527 | “gauged positions exact, character action exact, c′ = c·∂u exact in K_top on every scored pair (≥ 6 pairs per tower, no realize/pair skips), fuzz blindness exact” |
| P-4 ✓ | JD0-STRAT 0/37 | “assembled count record (direct) = (gauged) on all 37 towers; 36/37 with residue-nontrivial gauges (the one residue-trivial row is C4A, the all-F₂ tower, exactly the BOX-5 disclosure)” |
| P-5 ✓ | JD0-BLIND 0/7 | “all three same-shape pairs: letters verified DIFFERENT from the artifacts (C2A/P1B: z₁ = X̄ vs X̄² = X̄+1, both ord 3 — same order, different element, the finest separation; P2A/P2B: ord 2 vs ord 3; P3A/P3B: ord 2 vs the TRIVIAL letter), full stratum-count records EQUAL across each pair including the realized polygon integer records” |
| P-6 ✓ | teeth | “NONCHAR 11,514 fires (the additive slot bijection breaks type preservation massively — value-layer bijectivity alone is NOT the mechanism; the character form is load-bearing), NONUNIT 37 (one per tower: a non-unit rescale moves the position every time), COB 487 (the dropped-u corruption breaks the law whenever u_{γ+γ′} ≠ 1), SHAPE 1 (the C2A/C2B control pair's records differ)” |
| P-7 ✓ | — | “no falsifier fired; nothing to diagnose upstream” |

**DERIVATION:** `[MACHINE RECORD — re-verified at compile time]` **2026-08-14:** both artifact md5s recomputed and MATCH — `jd0_checks_output.txt` = `01f5c79509cf3bba8a24569abff081c7`, `jd0_checks_results.json` = `6fbc1f03a88bbd5408927bfb8324d415`. The runner `jd0_checks.py` is also independently md5-pinned by a *downstream* note: W-11's frozen-import list carries `jd0_checks d3a5e13d`, and the current digest is `d3a5e13df97d3768573800960a0ec509` — MATCH, i.e. the runner is unmoved and a second note is watching it.

Arithmetic check of the total: 2 + 60,950 + 7,527 + 37 + 7 = 68,523 ✓ — the five family counts sum exactly to the headline check count.

**⚠ THE TEETH ARE UNUSUALLY INFORMATIVE HERE.** NONUNIT firing exactly 37 (once per tower) confirms the unit hypothesis is load-bearing at every tower; NONCHAR firing 11,514 times confirms that mere bijectivity is not the mechanism; SHAPE firing exactly 1 confirms the control pair behaves as a control and nothing else does.

**CONDITIONALITY:** the finite enumeration scope is disclosed (D ≤ 3, |K|^D ≤ 20000) and defended as evidence rather than proof (E8 finding #12, ARTIFACT).

**RESOLUTION TRACE:** S9, appended at commit 2 from the committed artifacts.

**TEETH:** this IS the teeth record.

---

### EFF.JD0.29  [record]

**CANONICAL STATEMENT — S9's consequence paragraph:**

> “**Consequence for the grade box:** the lemma stands as composed — PROVED on
> the stated perimeter with the S4 boxes, battery GREEN, attempt 0/2. The
> J-D block's next unit (W-10 at the (2,1,1) shape) may consume S2 with the
> value-layer fences deleted per S1/S6.1.”

**DERIVATION:** `[RECORD]` The operative permission is the second sentence: W-10 may consume S2 with the value-layer fences deleted.

**CONDITIONALITY:** “attempt 0/2” superseded by owner signature (§1). The fence-deletion permission is guarded by S10.6 item 5's condition (EFF.JD0.37).

**RESOLUTION TRACE:** S9 tail → grade superseded by HYP.39.

**TEETH:** §7.

---

### EFF.JD0.30  [changes-record]

**CANONICAL STATEMENT — the dated adjudication header:**

> “The sol-5.6 as-built read of this note (gpt-5.6-sol high,
> runs/cert/C10_jd0_output.log, 2026-08-12) returned CHALLENGE with 12
> findings including a claimed CRITICAL. The E8 certification run
> (gpt-5.6-sol high, runs/cert2/E8_jd0_output.log) adjudicated 6 REAL /
> 6 ARTIFACT — in particular the claimed CRITICAL (the "every
> 1-cochain" quantifier) is an ARTIFACT: S0 documents the quantified
> cochain action as R(y) ↦ ξ^{−deg R}R(ξy), and the counterexample's
> independent slot rescaling is outside that documented class. The six
> REAL findings are repaired by the single S10 correction block below
> (the full count-shape T̂ with Λ_j and 𝒢; the corrected LEMMA J-D0
> display; degreewise multiplicativity replacing the automorphism
> claim; the cohomology-class correction; classifier-identification
> scope; consumer sweep). Everything above this heading is
> byte-untouched by this append. The E8 adjudication, transcribed
> unaltered:”

with the adjudication's own framing sentence:

> “The displayed theorem needs six repairs. The other six findings misread either the stated perimeter or the role of the battery/algebra shadow.”

**DERIVATION:** `[CERTIFICATION RECORD]` A CHALLENGE verdict with a claimed CRITICAL, resolved 6/6 by a second run. **The claimed CRITICAL was defeated on the perimeter declared in S0** — which is why EFF.JD0.05's four-line action display is load-bearing far beyond its size.

**CONDITIONALITY:** the six REAL findings are repaired *textually* by S10; they were real defects in the displayed theorem and are now fixed.

**RESOLUTION TRACE:** the dated append; the body is byte-untouched.

**TEETH:** none.

---

### EFF.JD0.31  [table]

**CANONICAL STATEMENT — the twelve-finding adjudication** `[TABLE]` (transcribed from the note's numbered list; each verdict and its reasoning in the adjudication's own wording, abbreviated only where marked):

| # | Verdict | Reasoning (verbatim, abbreviated where marked) | Repaired by |
|---|---|---|---|
| 1 | **ARTIFACT** (the claimed CRITICAL) | “The counterexample uses an independent diagonal rescaling of polynomial coefficients, which is not the note’s defined carry-gauge action. … BOX-2 excludes “any value layer NOT of that form”; it does not narrow “every 1-cochain” to a subset after the fact. The warrant that the weld induces this action is priced at GRTJA 1/2, but that is supplier conditionality, not a quantifier contradiction.” | — |
| 2 | **REAL** | “The explicit definition of shape lists only \(d_0,(e_j,h_j,g_j)\) and the induced field sizes. It never includes the residual factorization predicate consumed by \(L3_j\). The phrase “prescribed degree/type” later in S0 cannot repair that omission.” | S10.1 (Λ_j) |
| 3 | **REAL** | “The normalized degreewise map is not an algebra automorphism of \(K_j[y]\). Factorization preservation needs the degreewise multiplicativity argument supplied below.” | S10.2 |
| 4 | **REAL** | “ “[C]omputed from the eq-12 splits alone” proves dependence on the chosen splits, not dependence only on \((e_j,h_j)\). No section-independence or canonical-section lemma appears at HEAD. The honest proved scope must retain the split/grid record as part of the count-shape.” | S10.1 (𝒢) |
| 5 | **REAL** | “S2.3 independently proves cochain-value blindness by non-occurrence of \(c\), but S3’s explanation incorrectly attributes the death of the entire class to constancy on gauge orbits. Gauge invariance alone only compares coboundary-related representatives.” | S10.3 |
| 6 | **ARTIFACT** | S2.4 is explicitly called “The algebra-level shadow” and S7 expressly says “S2.4 only, not load-bearing for S2.1–S2.3.” … “The note does not use equality of dimensions to prove equality of counts or algebra isomorphism across distinct twisting classes.” | — |
| 7 | **ARTIFACT** | “The battery accurately says that its blind pairs have different letters; it does not say that they realize distinct cohomology classes. Clause (ii)’s carry-class independence is argued in S2.3 from: “the letters \(z_j\) and the carry values \(c(\gamma,\gamma')\) appear at NO point of the assembly.” Thus this finding correctly describes test coverage but incorrectly treats that limitation as a proof gap.” | — |
| 8 | **REAL** | “BOX-4 cites exact verification through OM order 4 while the surrounding prose claims classifier identification “at all orders.” A finite verified range cannot supply the universal identification. The formal-product theorem remains universal; its transfer to the actual classifier must be limited to the supplier’s established perimeter.” | S10.4 |
| 9 | **REAL** | “ “Factoring through the type map” is strictly weaker than being constant on fibers of `type → shape`. The uniformized integral class must be stated using the latter condition.” | S10.5 |
| 10 | **ARTIFACT** | “JD0-T-SHAPE concerns the particular registered pair `(C2A,C2B)`, whose records were expected to differ. The note never claims that the count function is injective on all shapes. Calling this one fixture a “different-shape control” does not assert the converse of shape-blindness.” | — |
| 11 | **ARTIFACT** | “S0 defines “stratum count” as the object consumed by the “per-cell coefficient,” including its \(q^{\pm B}\) factor. S6.1 then explicitly identifies \(q^{-3}\) as: “q-power \(q^{-3}\) in density normalization.” Thus \((q-1)/q^3\) is not being presented as a raw finite-set cardinality. “Stratum count” is locally defined terminology for the normalized coefficient.” | — |
| 12 | **ARTIFACT** | The finite scope is disclosed verbatim: “degree \(D\le3\) … capped \(|K|^D\le20000\)” and “37/37 towers, 68,523 checks.” … “The universal argument is the symbolic proof in S2; the battery is recorded as supporting machine evidence. No sentence claims that the finite enumeration proves the universal quantifiers.” | — |

**DERIVATION:** `[ADJUDICATION RECORD]` The six ARTIFACT rows are compiled because each is a **standing rebuttal**: a downstream reader encountering the same objection can be routed to the row rather than re-litigating. Four of the six turn on the note having declared something up front (the gauge action, the shadow's non-load-bearing status, the local definition of “stratum count”, the finite enumeration scope) — an unusually strong return on disclosure discipline.

**CONDITIONALITY:** the ARTIFACT verdicts rest on the note's declared perimeter; if a consumer widens the perimeter, finding #1 in particular becomes live again.

**RESOLUTION TRACE:** the dated append, transcribed unaltered from `runs/cert2/E8_jd0_output.log`.

**TEETH:** finding #10 concerns JD0-T-SHAPE; #12 concerns JD0-L3's scope.

---

### EFF.JD0.32  [definition]

**CANONICAL STATEMENT — S10.1, the count-shape (the governing key):**

> “For each level j, let Λ_j denote the complete LETTER-FREE residual census
> condition consumed by L3_j: its total polynomial degree, its full
> factorization-degree multiset with multiplicities, and any nonzero-constant
> or distinguished-factor DEGREE conditions. Λ_j contains no specified
> residual root or coefficient value.
>
> Let 𝒢 denote the complete discrete lattice datum consumed by L4: the chosen
> eq-12 splits t_j, their induced integers
>
>     D_j = t_j(β″_j) − t_j(β_j) − t_j(β′_j),
>
> the corresponding δ-data, and the side/grid positions from which B is
> computed. No assertion is made here that the chosen t_j, D_j, δ, or B are
> determined by the shorter tuple (e_j,h_j)_j; proving such a reduction is a
> separate grid/split theorem.
>
> The COUNT-SHAPE is therefore
>
>     T̂ = (d0, (e_j,h_j,g_j)_j, (Λ_j)_j, 𝒢).
>
> Its residual fields still have
>
>     r_j = |K_j| = q^(d0·g_0···g_{j−1}).
>
> The letters z_j and all carry-cochain values c(γ,γ′) are not part of T̂.
>
> For the normalization fixed by L2core, write
>
>     μ_T̂(q) = q^(±B(𝒢)) · ∏_j N_{Λ_j}(r_j),
>
> where N_{Λ_j}(r_j) is the finite-field census satisfying Λ_j. This μ_T̂(q)
> is the “stratum count” or normalized per-cell coefficient used throughout
> this note; N_{Λ_j}(r_j) is the raw residual cardinality.”

**DERIVATION:** `[DEFINITIONAL — the repair of findings #2 and #4]` Two additions, each carefully bounded:

- **Λ_j** repairs #2 by naming the residual predicate explicitly, and is fenced to be letter-free: “Λ_j contains no specified residual root or coefficient value.” That fence is what preserves clause (ii) — enlarging the key would be self-defeating if the enlargement admitted letters.
- **𝒢** repairs #4 by retaining the split/grid record, and is accompanied by the sharpest new fence in the note: the reduction to (e_j, h_j) is **not** asserted and is named as a separate theorem still owed.

The raw/normalized distinction (μ_T̂ vs N_{Λ_j}) is what makes finding #11's rebuttal precise.

**CONDITIONALITY:** the normalization is “fixed by L2core”; its identification with the actual classifier coefficient is S10.4.

**RESOLUTION TRACE:** S10.1; supersedes EFF.JD0.03's shape definition and re-keys EFF.JD0.04, .06, .11, .16, .18.

**TEETH:** JD0-BLIND (built against the *old* key — see OPEN-CALL 2).

---

### EFF.JD0.33  [lemma]

**CANONICAL STATEMENT — S10.1's corrected LEMMA J-D0 (the governing display):**

> “> **LEMMA J-D0 (corrected display).**
> > (i) [GAUGE-INVARIANCE] For every decided count-shape T̂, every prime
> > power q, and every unit-valued carry gauge having the documented S0
> > residual action
> >
> >     R(y) ↦ ξ^(−deg R) R(ξy),
> >
> > the normalized coefficient μ_T̂(q) computed through the gauged read
> > equals the direct coefficient.
> >
> > (ii) [VALUE-BLINDNESS] Any two realized decided types with the same
> > count-shape T̂ have equal normalized coefficients at every q, regardless
> > of their residual letters and regardless of whether their carry
> > cochains are cohomologous.
> >
> > Consequently μ_T̂(q) is a function of the full combinatorial count-shape
> > T̂ alone.”

with S10.1's derivation of clause (ii):

> “Derivation of clause (ii): equality of T̂ gives the same fields K_j, the
> same predicates Λ_j, and the same lattice exponent B(𝒢). Therefore every
> factor N_{Λ_j}(r_j) and the q-power agree. The displayed formula for
> μ_T̂(q) contains neither z_j nor c(γ,γ′), so equality holds even for carry
> cochains in distinct cohomology classes. ∎”

**DERIVATION:** `[PROVED — the governing statement]` Clause (i) is EFF.JD0.11 (repaired form) + EFF.JD0.16; clause (ii) is pure non-occurrence, as derived above.

**⚠ CLAUSE (ii) IS STRICTLY STRONGER THAN THE BODY'S.** The body said “regardless of their letters and carry cochains”; the corrected display says “regardless of whether their carry cochains are cohomologous” — an explicit **cross-class** claim. The strengthening costs nothing because the proof was never a gauge argument (EFF.JD0.18), which is exactly S10.3's point.

**⚠ CLAUSE (i)'s QUANTIFIER IS NOW SELF-CONTAINED.** It displays the residual action inline rather than referring to S0, which closes the reading that produced the claimed CRITICAL.

**CONDITIONALITY:** the documented residual action; the count-shape key T̂ (not the shorter tuple); the S4 boxes and the S7 stack.

**RESOLUTION TRACE:** S10.1; supersedes EFF.JD0.06.

**TEETH:** JD0-STRAT (clause (i)); JD0-BLIND (clause (ii), at the old key — OPEN-CALL 2).

---

### EFF.JD0.34  [lemma]

**CANONICAL STATEMENT — S10.3, the cohomology-class correction:**

> “Gauge invariance and cochain-value blindness have different proofs.
> Clause (i) compares presentations inside one coboundary orbit:
> c and c·∂u. It makes no assertion that distinct cohomology classes are
> gauge-related. Clause (ii) compares distinct classes because the coefficient
>
>     μ_T̂(q) = q^(±B(𝒢)) · ∏_j N_{Λ_j}(r_j)
>
> contains no carry-cochain value at all. Accordingly, replace the S3 claim
> that the entire value realization “enters the classifier only through the
> gauge orbit” by:
>
>     Unit gauges do not change the coefficient within a coboundary orbit;
>     more strongly, the L3 × L4 formula is constant across different
>     cohomology classes because no value of c occurs in that formula.
>
> The algebra discussion in S2.4 remains only an algebra-level shadow and is
> not proof of cross-class blindness.”

**DERIVATION:** `[CORRECTION — a strengthening disguised as a repair]` The body's S3 sentence was *logically insufficient* (gauge-orbit constancy compares only coboundary-related representatives, so it cannot reach across classes) while the *conclusion* it was defending is true for an entirely different and stronger reason (non-occurrence). The replacement text says both halves in order: within-orbit invariance, then “more strongly” the cross-class constancy.

**Compile-time check of the logic:** the coefficient displayed is q^(±B(𝒢))·∏_j N_{Λ_j}(r_j); B depends on 𝒢, N on Λ_j and r_j, r_j on q and the g's — no symbol in the formula ranges over cochain values, so constancy across classes is immediate ✓.

**CONDITIONALITY:** none. The correction is unconditional.

**RESOLUTION TRACE:** S10.3; supersedes the final sentence of EFF.JD0.20's first paragraph.

**TEETH:** JD0-BLIND tests letters, not classes — the note's own finding-#7 rebuttal says so (EFF.JD0.31 row 7).

---

### EFF.JD0.35  [scope-record]

**CANONICAL STATEMENT — S10.4, classifier-identification scope:**

> “J-D0 proves the preceding statements universally for the displayed formal
> L3 × L4 coefficient. Its identification with L2core’s actual per-cell
> classifier coefficient may be consumed only on the perimeter on which the
> L2core supplier has established that identification. The cited exact
> instances through OM order 4 do not, by themselves, prove an all-orders
> identification.
>
> Therefore “valid at every residue characteristic, wild included” is
> unconditional for the formal coefficient μ_T̂(q). The same conclusion for
> the actual classifier coefficient is unconditional through the supplier’s
> proved/verified range and is conditional, at any further order, on a
> general L2core assembly-identification theorem.”

**DERIVATION:** `[SCOPE CORRECTION]` The split is clean and should be carried by every consumer: **two statements, two scopes.** The formal coefficient statement is unconditional and all-orders; the classifier statement is unconditional only through L2core's verified range (exact instances through OM order 4) and conditional beyond it on a theorem that does not exist.

**⚠ THIS IS THE SHARPEST LIVE CONDITIONALITY IN THE NOTE** for a downstream consumer working above OM order 4 — and it interacts with EFF.JD0.08's fence-deletion claim, which S10.6 item 5 guards accordingly.

**CONDITIONALITY:** above OM order 4, the classifier reading is conditional on an unproved general L2core assembly-identification theorem.

**RESOLUTION TRACE:** S10.4; narrows BOX-4 and EFF.JD0.20's second clause.

**TEETH:** none — L2core's verification is outside this battery (BOX-4: “consumed, not re-proved”).

---

### EFF.JD0.36  [lemma]

**CANONICAL STATEMENT — S10.5, the corrected integral class:**

> “Let
>
>     π : decided type → count-shape
>
> forget the residual letters and carry-cochain values. A bounded function f
> is uniformized by J-D0 precisely when it is constant on every fiber of π;
> equivalently, when
>
>     f = w ∘ π
>
> for a weight w on count-shapes. Requiring the displayed rule for f to have
> no explicit p-dependent coefficient is not sufficient: a rule can be
> p-free while still distinguishing letters or carry values.
>
> For f = w ∘ π with p-free w, grouping the integral by fibers gives
>
>     ∫ f = ∑_T̂ w(T̂) μ_T̂(q),
>
> so J-D0 supplies the carry-value blindness of the mass terms. This is the
> correct scope of the H-C positioning sentence.”

**DERIVATION:** `[PROVED — a fibration argument]` Constancy on fibers of π is equivalent to factoring as w ∘ π; grouping ∫f by fibers then gives ∑_T̂ w(T̂)μ_T̂(q), in which J-D0 controls exactly the mass terms μ_T̂(q) and nothing else.

The sentence that repairs finding #9 is the middle one, and it identifies a real trap: **p-freeness of the displayed rule is not sufficient**, because a p-free rule can still read a letter. The body's characterization (“bounded functions factoring through the type map with p-free weights”) conflated the two.

**CONDITIONALITY:** w must be p-free AND f must be constant on π-fibers; both are needed.

**RESOLUTION TRACE:** S10.5; supersedes EFF.JD0.21's final characterization.

**TEETH:** none.

---

### EFF.JD0.37  [scope-record]

**CANONICAL STATEMENT — S10.6, the consumer sweep:**

> “1. **W-10 at the (2,1,1) Eisenstein shape survives.** Its residual
>    condition Λ, its eq-12 split/grid record 𝒢, and hence B are fixed in
>    that row. The value-layer fences remain absent because neither its
>    letter nor its carry-cochain values occur in μ_T̂(q). BOX-1’s outstanding
>    grid/proviso obligations remain exactly as stated.
>
> 2. **W-11 survives with the corrected key.** The grown menu may be keyed by
>    the full count-shape T̂. If W-11 wants to key it only by
>    (d0,(e_j,h_j,g_j)_j), it separately owes proofs that Λ_j and 𝒢, or at
>    least all their consumed censuses and B, are determined by that shorter
>    tuple. No letter or carry-cochain value must be added to the key.
>
> 3. **W-12 / J-D1 survives.** It consumes the same coefficient equality,
>    now at its exact proved scope.
>
> 4. **H-C survives with the correction in S10.5.** Its admissible weights
>    are the p-free weights constant on fibers of type → count-shape.
>
> 5. **Fence bookkeeping survives.** PERIM-μ, (C-coll), (R-coll), and (VD-m)
>    remain unnecessary for count-layer consumers whenever the consumer uses
>    the L3 × L4 coefficient and the documented gauge dictionary. The repair
>    does not promote the finite L2core checks into an all-orders
>    assembly-identification theorem.”

**DERIVATION:** `[CONSUMER SWEEP]` All five survive, but two acquire explicit new content:

- **Item 2 creates a NEW NAMED OBLIGATION for W-11** — a Λ/𝒢-reduction proof if W-11 keys its menu by the shorter tuple. The E8 footnote flags this as the sweep's honest cost: “the honest new obligation noted (W-11's shorter menu key owes a Λ/𝒢-reduction proof if it drops the full count-shape)”. See OPEN-CALL 1.
- **Item 5 adds the guard** that makes EFF.JD0.08's fence-deletion claim precise (“whenever the consumer uses the L3 × L4 coefficient and the documented gauge dictionary”) and blocks the over-read S10.4 warns against.

**CONDITIONALITY:** each item's conditionality is stated inline.

**RESOLUTION TRACE:** S10.6; sweeps EFF.JD0.24.

**TEETH:** none.

---

### EFF.JD0.38  [record]

**CANONICAL STATEMENT — the E8 orchestrator-verification footnote:**

> “*(The adjudication above was produced by the E8 certification run
> (gpt-5.6-sol high, runs/cert2/E8_jd0_output.log, 2026-08-12);
> orchestrator verification before transcription — the ARTIFACT
> rebuttals checked against S0's documented action, S2.4's
> "algebra-level shadow" disclosure, and S0's stratum-count
> definition; S10.2's degreewise identity re-derived
> (ξ^{−a−b}A(ξy)B(ξy) = σ_{ξ,a}(A)·σ_{ξ,b}(B); leading coefficient
> ξ^{−D}ξ^{D} = 1; nonzero constant preserved); S10.1's class-blindness
> checked (μ_T̂(q) = q^{±B(𝒢)}·Π N_{Λ_j}(r_j) contains neither letters
> nor carry values); the L4-clause derivation checked (equal 𝒢 fixes
> each D_j = ∂t_j, δ, and B — no cross-section claim); the honest new
> obligation noted (W-11's shorter menu key owes a Λ/𝒢-reduction proof
> if it drops the full count-shape). Transcribed unaltered per the
> standing division of labor. Ledger: row 28 → six repairs folded,
> awaiting sol discharge (cert3).)*”

**DERIVATION:** `[VERIFICATION RECORD]` The two-model discipline: sol adjudicated and derived, the orchestrator re-checked each item before transcription, then transcribed unaltered. **The compiler independently re-checked all four listed re-derivations** and they hold (EFF.JD0.15, .32, .33, .34).

**⚠ AN OPEN PROCESS ITEM IS RECORDED HERE, NOT ELSEWHERE:** “Ledger: row 28 → six repairs folded, **awaiting sol discharge (cert3)**.” The S10 repairs have been folded but not yet re-certified by a fresh sol run. This is the note's only outstanding process obligation, and it sits alongside MQ-4's optional codex pass.

**CONDITIONALITY:** cert3 discharge outstanding.

**RESOLUTION TRACE:** the append's closing footnote; the last line of the note.

**TEETH:** none.

---

## 4. XREF verification ledger

All designations grep-verified at compile time (2026-08-14); commits verified with `git cat-file -t`; digests recomputed with `md5sum`.

### 4.1 Commit and digest locators

| ID | Locator | Verification | Role |
|---|---|---|---|
| C01 | `1642275` | commit | the seal commit (runner sealed unrun) |
| C02 | `9387eb1` | commit | the verdict commit — the pin all seven consumers cite |
| D01 | `verification/openmath/jd0_checks_output.txt` md5 `01f5c79509cf3bba8a24569abff081c7` | recomputed → `01f5c79509cf3bba8a24569abff081c7` | **MATCH** |
| D02 | `verification/openmath/jd0_checks_results.json` md5 `6fbc1f03a88bbd5408927bfb8324d415` | recomputed → `6fbc1f03a88bbd5408927bfb8324d415` | **MATCH** |
| D03 | `verification/openmath/jd0_checks.py` | recomputed → `d3a5e13df97d3768573800960a0ec509` | **MATCH** against the md5-8 pin `d3a5e13d` carried by W-11's frozen-import list — an independent downstream watcher on this runner |

### 4.2 Lean anchors (S5's degenerate instance)

| ID | Verified designation | Location | Role |
|---|---|---|---|
| L01 | `realF2` | `OM/RealInstanceV2.lean`, `OM/RealInstanceV2Gates.lean` | the real instance carrying `density` |
| L02 | `omCount_sepShape_eq_rootCount` | `OM/OMCountV2.lean`, `OM/RealInstanceV2.lean`, `OM/RealInstanceV2Gates.lean` | “the count as a root census — the L3 leg at order 0” |
| L03 | `gate3_inert_q2` | `OM/OMCountV2.lean` | the non-vacuity gate, value 1/4 at n = 2 inert, q′ = 2 |
| L04 | `gate_v2_split_q3` / `gate_v2_inert_q3` / `gate_v2_sigma_separation` | `OM/RealInstanceV2Gates.lean` (lines 120, 127, 136) | the split ≠ inert separation at q′ = 3; the file's own docstring records “`1/9` vs `1/3`” |
| L05 | `omCount` | `OM/OMCountV2.lean` and 40 further files | the evaluator whose SIGNATURE is the whole S5 argument — no letter or cochain argument exists |
| L06 | `density` | `OM/RealInstanceV2.lean`, `OM/OMCountV2.lean`, `OM/RealInstanceV2Gates.lean` | `density m σ q' := ∑_{T ∈ omMenu m σ} omCount T q'` |

### 4.3 Ledger rows (the signature and the reachability audit)

| ID | Verified designation in `spec/HYPOTHESIS_LEDGER.md` | count | Role |
|---|---|---:|---|
| G01 | `HYP.39  \`JD0-BAR\`` | 1 | **the grade cap and its owner signature** — CLOSED-SIGNED at Q6, append #49 |
| G02 | `the owner **signed the 68,523/0 machine grade as the bar**` (within HYP.39's disposition) | 1 | the signature itself |
| G03 | `HYP.74  \`JD0-BOX-2\`` | 1 | BOX-2 carried downstream; “CARRY — reachability SIGNED”; REACHABLE-AND-CONSUMED at seven sites |
| G04 | `MQ-4  **JD0 optional codex pass (belt-and-braces).**` | 1 | the surviving optional pass — “Queued, not owed … it gates nothing, and its absence caps no claim” |

### 4.4 Reverse-XREFs: the seven verified consumption sites

Every site named by the ledger's weld-face audit was re-read at its cited line at compile time. All seven are conditionality-stack rows citing J-D0 at seal `1642275/9387eb1`.

| ID | Consumer note | Line | Text at that line |
|---|---|---:|---|
| RX-1 | `lean/notes/openmath/W11_PROOF_2026-08-08.md` | 599 | `* **LEMMA J-D0** — PROVED on its perimeter, seal 1642275/**9387eb1**;` |
| RX-2 | `lean/notes/openmath/W12_PROOF_2026-08-08.md` | 1499 | `* **LEMMA J-D0** — PROVED on its perimeter, seal 1642275 / verdict` |
| RX-3 | `lean/notes/openmath/HMENU3_PROOF_2026-08-08.md` | 717 | `* **LEMMA J-D0** @ 1642275/9387eb1 — the gauge-blindness frame` |
| RX-4 | `lean/notes/openmath/HEX3_PROOF_2026-08-08.md` | 720 | `* **LEMMA J-D0** @ 1642275/9387eb1 — the gauge-blindness frame behind` |
| RX-5 | `lean/notes/openmath/GENIND_PROOF_2026-08-08.md` | 1518 | `* **LEMMA J-D0** @ 1642275/9387eb1 — the count-gauge-blindness frame` |
| RX-6 | `lean/notes/openmath/GENH4_PROOF_2026-08-08.md` | 938 | `* **GRTJC W-6..W-9 + JC-LOAD/W-8 (ACCEPTED 2/2 @ 83f47f4), J-D0 @` |
| RX-7 | `lean/notes/openmath/GENHN_PROOF_2026-08-08.md` | 1515 | `* **J-D0** @ 9387eb1 (proved): letter/twist-blindness of counts.` |

**Two sites additionally declare BOX-2's perimeter INHERITED**, per HYP.74; W-11's is quoted in `spec/EFF-W11.md` (its S9 J-D0 row: “J-D0 is corroborating at the instance, load-bearing only for the
  general-program framing (S4.3 inherits its BOX-2 gauge-form perimeter).”) and is independently compiled there as EFF.W11.37.

### 4.5 Cross-spec edges (this compilation queue)

| ID | Edge | Verification |
|---|---|---|
| S01 | `spec/EFF-W11.md` EFF.W11.37 ↔ RX-1 | the same J-D0 conditionality row, compiled from both ends; both agree J-D0 is a FRAME at the instance |
| S02 | `spec/EFF-W11.md` EFF.W11.16 (the residual census) cites J-D0's value-blindness as “the frame” | W-11: “J-D0's value-blindness surfacing at
the branching level (the mechanism cited from pin 9387eb1; the counts here
are proved directly, J-D0 is the frame)” |
| S03 | EFF.JD0.24 consumer 1 ↔ `spec/EFF-W11.md` EFF.W11.15 | the same (2,1,1) row: J-D0's “(q−1)/q³” = W-11's RAM(1) “density (q−1)/q³” — the two notes' arithmetic agrees |
| S04 | EFF.JD0.37 item 2 ↔ `spec/EFF-W11.md` EFF.W11.12 | **the new obligation lands on a note already compiled in this queue**: W-11's C7-F2 keys its menus by shape families, and S10.6 item 2 says a shorter-tuple key owes a Λ/𝒢-reduction proof. See OPEN-CALL 1. |

---

## 5. Effective supply/consumption ledger

`[TABLE]`

| Supplier / consumer | Effective J-D0 relation | Surviving fence | Status |
|---|---|---|---|
| **[ILN]†** (SHIFT-(n), D_j-INT, EXP-KIT(a)) | CONSUMED — the integer/cochain layer of EFF.JD0.10 | unconditional, ACCEPTED | **CONSUMED at ACCEPTED grade** |
| **GRTJB** (JB-VTX, JB-DEV) | CONSUMED — the position faces of EFF.JD0.09/.11 | row 12's u ≥ 0 proviso carried where B-ties are consumed (BOX-1) | **CONSUMED at ACCEPTED 2/2** |
| **GRTJC** (W-9 + JC-F1; JC-PSIKER(iii)/JC-LOAD) | CONSUMED — the algebra shadow (non-load-bearing) and the wild-decidedness warrant | BOX-6: “J-D0 adds no leg to it” | **CONSUMED at ACCEPTED 2/2** |
| **GRTW2** + **LIFTCORNER** | CONSUMED — the (ξ,w) covariance family in the gauge dictionary | BOX-2 | **CONSUMED at ACCEPTED 2/2** |
| **GRTJA** (JA-RES slot-character form; JA-GRID) | CONSUMED **at arc 1/2** — both the slot-character form and the grid | BOX-2; **the single priced input in the critical path** | **CONSUMED AT ARC GRADE — the note's largest exposure** |
| **WELDMASTER** (WM-COB) | clause (i) at its [ILN]†-only footprint (unconditional); clause (iii) at arc 1/2 in S2.4 only | “not load-bearing for S2.1–S2.3” | **SPLIT CONSUMPTION — clause (i) unconditional, clause (iii) non-load-bearing** |
| **H-B / H-C** | CITED as interpretation in S3 | “no proof weight anywhere in S2” | **NOT CONSUMED as proof** |
| **L2core assembly identification** | CONSUMED, not re-proved (BOX-4) | **narrowed by S10.4**: exact through OM order 4; conditional beyond | **CONSUMED on the supplier's perimeter only** |
| **The J-D dimension shortcut** | — | REFUTED with an explicit witness | **NOT CONSUMED — refuted ancestor (N-1)** |
| **W-10** (downstream) | SUPPLIES the value-layer fence deletion at every decided shape | BOX-1's grid/proviso obligations remain | **LIVE consumer (S10.6 item 1)** |
| **W-11** (downstream) | SUPPLIES clause (ii) as the menu key | **NEW OBLIGATION**: a shorter-tuple key owes a Λ/𝒢-reduction proof (S10.6 item 2) | **LIVE consumer; obligation OPEN (OPEN-CALL 1)** |
| **W-12/J-D1, HMENU3, HEX3, GENIND, GENH4, GENHN** | SUPPLIES the gauge-blindness FRAME at seal `1642275/9387eb1` | consumed at FRAME grade (HYP.39/HYP.74) | **LIVE — seven sites verified (§4.4)** |
| **The Lean order-0 evaluator** | J-D0(ii) holds BY TYPE at order 0; (i) is empty | “an anchor … not evidence for it” | **ANCHOR ONLY (N-9)** |

---

## 6. TEETH inverse inventory

Disposition vocabulary is `{Lean theorem, executable regression, signed non-applicability}`. J-D0's battery is first-party, sealed two-commit, and re-verifiable; one leg is explicitly **recorded, not scored**, and the note says so.

| # | Battery row / prediction | Guarded EFF units | Disposition |
|---|---|---|---|
| 1 | JD0-PIN / P-1 (0/2) — frozen imports unmoved | EFF.JD0.25, .26 | executable regression (md5 pins; the runner's own digest re-verified MATCH against W-11's independent pin) |
| 2 | JD0-L3 / P-2 (0/60,950) — pointwise type preservation, bijectivity, census and menu equality, all p = 7 rows, every wild level | EFF.JD0.14, .15, .16 | executable regression; **finite scope disclosed** (D ≤ 3, |K|^D ≤ 20000) and defended as evidence not proof (finding #12) |
| 3 | JD0-COB (1),(3) / P-3 — gauged positions exact | EFF.JD0.09, .11 | executable regression |
| 4 | JD0-COB (2),(5) / P-3 — character action exact; fuzz blindness | EFF.JD0.13 | executable regression |
| 5 | JD0-COB (4) / P-3 (0/7,527) — c′ = c·∂u exact in K_top on every scored pair | EFF.JD0.12 | executable regression |
| 6 | JD0-STRAT / P-4 (0/37) — assembled count record direct = gauged | EFF.JD0.04, .17, .33(i) | executable regression |
| 7 | JD0-BLIND / P-5 (0/7) — three same-shape pairs, letters verified DIFFERENT, records EQUAL incl. realized polygon integers | EFF.JD0.18, .33(ii) | executable regression; **tests letters, NOT cohomology classes** (finding #7) |
| 8 | JD0-T-NONCHAR (11,514) — the additive slot bijection breaks type preservation | EFF.JD0.05, .16, .22 (BOX-2) | executable regression — and the **positive** evidence that the character form, not bijectivity, is the mechanism |
| 9 | JD0-T-NONUNIT (37, one per tower) — a non-unit rescale moves the position | EFF.JD0.09 | executable regression — confirms the unit hypothesis is load-bearing at every tower |
| 10 | JD0-T-COB (487) — the dropped-u corruption breaks the law whenever u_{γ+γ′} ≠ 1 | EFF.JD0.12 | executable regression |
| 11 | JD0-T-SHAPE (1) — the (C2A,C2B) control pair's records differ | EFF.JD0.18 | **signed non-applicability** — “The note never claims that the count function is injective on all shapes” (finding #10) |
| 12 | `int_layer_sample` — the integer-layer record | EFF.JD0.10 | **signed non-applicability** — the note's own disclosure: “recorded per scored pair …, **not scored** — the split-only property is definitional in the engine” |
| 13 | P-7 falsifier ordering (L3 violation → S2.2(f); position move → S2.1(a); blind-pair polygon mismatch → upstream weld) | EFF.JD0.09, .14, .18 | signed non-applicability (a diagnostic protocol) |
| 14 | BOX-5 F₂ census (252 nontrivial / 57 trivial residual-gauge levels; C4A the one residue-trivial tower) | EFF.JD0.22 (BOX-5) | executable regression (the vacuity disclosure is machine-reported, not asserted) |

**Zero-orphan check:** 14 of 14 battery obligations mapped (5 check families expanded into their 7 scored legs + 4 teeth + the unscored integer-layer record + the falsifier protocol + the F₂ census); 0 orphan rows.

**Arithmetic check:** the five family counts sum to the headline total — 2 + 60,950 + 7,527 + 37 + 7 = **68,523** ✓.

**Reverse direction — units with no tooth:** EFF.JD0.19 (the algebra shadow, explicitly non-load-bearing), .20, .21, .23 (the Lean anchor, outside the battery), .35, .36 and all the record/fence units. Every mathematical unit of S2 (EFF.JD0.09–.18) and both clauses of the corrected display (EFF.JD0.33) are guarded.

---

## 7. Acceptance and evidence fence

**There is no acceptance record.** J-D0's status rests on three distinct things, which must not be merged:

**1. A perimeter-relative proof.** “PROVED on the stated perimeter (unit-character gauges; the L3 × L4 assembly; honest boxes S4), consuming the supplier stack S7 at the grades listed there.” The perimeter is real and the stack has one arc-grade input in its critical path (GRTJA at 1/2, supplying both JA-RES and JA-GRID).

**2. A GREEN sealed battery: 68,523 checks, 0 violations, 37 towers, four teeth fired, zero skips.** Both artifact md5s re-verified MATCH at compile time; the runner digest re-verified MATCH against an *independent downstream pin* carried by W-11. The teeth are informative rather than merely present: NONUNIT fires once per tower, NONCHAR fires 11,514 times, and the note draws the right conclusion from that count — “value-layer bijectivity alone is NOT the
  mechanism; the character form is load-bearing”

**3. An owner signature in place of a hostile arc.** HYP.39 CLOSED-SIGNED at 0d Q6 (append #49): “The owner **signed the 68,523/0 machine grade as the bar**; the two-clean hostile arc is not required for J-D0.” The recorded reasoning is a judgment about necessity, not about verification — J-D0's gauge-blindness *“MUST be true for the proof strategy/uniformity to work at all”* — and the optional codex pass survives as MQ-4, gating nothing.

**What the evidence does NOT establish.** (a) The JD0-L3 enumeration is finite and disclosed: degree D ≤ 3, |K|^D ≤ 20000; the universal claim is the symbolic proof, not the enumeration (finding #12). (b) JD0-BLIND separates **letters**, not cohomology classes; cross-class blindness is proved by non-occurrence, not tested (finding #7). (c) The integer-layer leg is recorded, not scored. (d) Five towers were pre-seal smoke rows (disclosed). (e) The Lean tie is doubly degenerate and is explicitly not evidence.

**One process obligation is open**, recorded only in the append's closing footnote: “Ledger: row 28 → six repairs folded, **awaiting sol discharge (cert3)**.” The S10 corrections have been folded but not re-certified.

---

## 8. Deferred reconciliation obligations

1. **cert3 discharge of the six folded repairs.** The E8 footnote records the S10 block as “six repairs folded, awaiting sol discharge (cert3)”. This is the note's only outstanding process item beyond MQ-4's optional pass. Action item.

2. **MQ-4, the optional codex pass.** Explicitly “Queued, not owed … it gates nothing, and its absence caps no claim.” Recorded so that HYP.39's closure is not read as cancelling it.

3. **BOX-2's per-site discharge is a fold, not a theorem.** Ledger HYP.74's disposition names the route: “Discharge is a fold, not a theorem: record per site that the gauge consumed there is an explicit z-power unit char[acter]…”. Seven sites are identified (§4.4); the per-site records are not yet written. Bookkeeping, but it is the discharge path for the note's largest exposure.

4. **W-11's menu key (cross-spec).** S10.6 item 2 creates a named obligation on a note already compiled in this queue. See OPEN-CALL 1 — recorded here as well because it is a *cross-spec* reconciliation, not solely a J-D0 judgment.

5. **The Λ/𝒢-reduction theorem itself.** S10.1: “proving such a reduction is a separate grid/split theorem.” No such theorem exists at HEAD. It is not owed by J-D0 — J-D0 correctly declines to claim it — but every consumer wanting the shorter key owes it. Named so it does not vanish between notes.

---

## 9. Self-audit

### Resolved compilation calls

- The effective text is derived under a body-plus-one-append rule, with the append's five controlling conditions enumerated and each mapped to its governing S10 subsection.
- **The grade is compiled from the ledger, not from the note.** The note says “Attempt 0/2”; the governing status is HYP.39 CLOSED-SIGNED with the 68,523/0 machine grade signed as the bar at 0d Q6, MQ-4 preserved. Both are quoted; neither is conflated with the other, and §1 carries the split at the top where a consumer cannot miss it.
- The S10.2 **deletion** is compiled as a deletion (the sentence is marked `[SUPERSEDED SENTENCE — DELETED by S10.2]` inside EFF.JD0.14 rather than silently dropped), because a reader of the body alone would otherwise re-derive a false route to a true conclusion.
- The six **ARTIFACT** adjudications are compiled as a unit (EFF.JD0.31), not discarded: each is a standing rebuttal, and four of the six turn on up-front disclosure.
- Clause (ii)'s correction is identified as a **strengthening** (cross-class, by non-occurrence), not a weakening — the single most under-readable feature of the append.
- The count-shape enlargement is compiled with both of its fences: Λ_j is letter-free, and the reduction to the shorter tuple is explicitly not asserted.
- All four listed orchestrator re-derivations were **independently re-checked**: the degreewise identity ξ^{−(a+b)}A(ξy)B(ξy) = σ_{ξ,a}(A)·σ_{ξ,b}(B), the monicity and constant-term claims, the non-occurrence of letters/cochains in μ_T̂(q), and the L4 derivation's no-cross-section scope.
- The battery arithmetic was re-checked: 2 + 60,950 + 7,527 + 37 + 7 = 68,523 ✓.
- Both artifact md5s and the runner digest re-verified MATCH; the runner digest cross-checks against W-11's independent frozen-import pin.
- **All seven consumption sites re-read at their cited lines** and confirmed; the ledger's audited list is larger than the note's own S6 consumer list, and both are recorded.
- Four cross-spec edges to `spec/EFF-W11.md` emitted and checked, including the one place where J-D0's correction creates an obligation on W-11.
- Battery fully consumed: 14 obligations, 0 orphans, including the one leg the note itself marks unscored.
- No table prosified: the adjudication, the conditionality stack, the scorecard, the seven-site inventory, and the supersession chain are tables.

### Fidelity audit (compile-time, mechanical)

- Quoted segments extracted from this specification and tested for verbatim containment in the source note under a normalization removing only presentational reflow. Script: `scratchpad/verb2.py`.
- **200 quoted segments checked, ~176 verbatim.** The non-matches are exclusively **cross-file quotations**, each independently grep-verified at count ≥ 1 in its own file: 10 from `spec/HYPOTHESIS_LEDGER.md` (HYP.39's STATEMENT and CLOSED-SIGNED disposition, HYP.74's CONSUMED-BY and disposition, MQ-4, and the FRAME-grade line), 4 from `lean/notes/openmath/W11_PROOF_2026-08-08.md`, 1 from `lean/LeanUrat/OM/RealInstanceV2Gates.lean` (`\`1/9\` vs \`1/3\``), and the S10.2 deletion instruction quoted from the append.
- Six quotation repairs were made during the audit: one compiler paraphrase that had been set in quotation marks was replaced by the note's actual wording; one adjudication quote restored to its bracketed `[C]omputed` form; three line-wrap rejoins restored to the source's wrapping; and one tuple restored to the source's internal spacing `(e_j, h_j, g_j)_j`.
- No quantifier, index, verdict, grade, count, or supplier grade was altered anywhere.

### OPEN-CALLS

**OPEN-CALL 1 — S10.6 item 2 creates an obligation on W-11 that W-11 does not carry. FOR-0d — NEEDS ASVIN. (Cross-spec: `spec/EFF-W11.md`.)**

S10.6 item 2 states:

> “**W-11 survives with the corrected key.** The grown menu may be keyed by
> the full count-shape T̂. If W-11 wants to key it only by
> (d0,(e_j,h_j,g_j)_j), it separately owes proofs that Λ_j and 𝒢, or at
> least all their consumed censuses and B, are determined by that shorter
> tuple. No letter or carry-cochain value must be added to the key.”

and the E8 footnote flags it as the sweep's cost: “the honest new obligation noted (W-11's shorter menu key owes a Λ/𝒢-reduction proof if it drops the full count-shape)”.

W-11 keys its menus by **shape families** — `spec/EFF-W11.md` EFF.W11.12 (C7-F2's (iv′)) defines M_split, M_inert, M_ram by the shape data of its table (i), i.e. by parameters (h), (w₁,w₂), (k) and the SEP rows, with `omCount` the S2.5 laws. It carries no Λ/𝒢-reduction proof, and neither note cites the other's correction: J-D0's S10 append is dated 2026-08-12, W-11's C7-F2 append is dated 2026-08-12, and the two were derived by separate certification runs (E8 and E9 respectively).

Whether this is a real gap or a non-issue turns on a question neither note answers: **at n = 2, is W-11's shape data enough to determine Λ_j and 𝒢?** There is a plausible affirmative — W-11 works on the three-point grid {0,1,2} where, in its own words, “all
  polygons computed outright on the finite grid {0,1,2}”, so the split/grid record may well be forced. But *plausible* is not *proved*, and the compiler cannot supply the argument. Note also that S10.6 item 2's obligation is stated for the general menu key, while W-11 is an n = 2 instance — so the honest reading may be that W-11 owes only the n = 2 case, which is likely cheap. Phase 0d must decide whether to (a) discharge the n = 2 case explicitly in W-11, (b) re-key W-11's menu by the full count-shape, or (c) record that J-D0's obligation is vacuous at n = 2 with a one-line argument. Recording, not deciding.

**OPEN-CALL 2 — JD0-BLIND tests the OLD key, and no one re-scored it. FOR-0b (note owner).**

The three same-shape pairs of EFF.JD0.26 were constructed against the body's shape definition (EFF.JD0.03) — “same shape data
(d0, (e_j, h_j, g_j)_j) and residual-degree structure”. After S10.1, the governing clause (ii) quantifies over pairs with the same **count-shape T̂**, which additionally fixes Λ_j and 𝒢.

The direction of the change is favourable — T̂-equality is *stronger* than shape-equality, so a pair that is T̂-equal is automatically shape-equal, and the constructed pairs might not be T̂-equal, in which case the test would be checking something weaker than the theorem now claims. But nothing in the append re-examines the pairs, and P-5's wording (“all three same-shape pairs”) is not re-scoped. The finding-#7 rebuttal touches the classes question but not the key question.

Concretely: if (P2A, P2B) happen to differ in their eq-12 split records, then their equal count records are evidence for something *broader* than clause (ii) — which would be good news — but the battery would no longer be a direct test of the stated clause. Conversely if they are T̂-equal, the test is exactly on point and a one-line note would say so. **Proposed disposition:** a minors-only rider recording, per pair, whether Λ_j and 𝒢 agree. Needs the note owner; the artifacts are committed and the check is cheap.

**OPEN-CALL 3 — the fence-deletion claim (S1) and S10.4's classifier scope interact, and the interaction is stated in only one direction. FOR-0b.**

EFF.JD0.08 claims W-10 “escapes the collapse perimeter ENTIRELY”. S10.6 item 5 guards the general fence-deletion claim (“whenever the consumer uses the L3 × L4 coefficient and the documented gauge dictionary”) and adds “The repair does not promote the finite L2core checks into an all-orders assembly-identification theorem.” But S10.4 establishes something sharper: for the **actual classifier** coefficient above OM order 4, the conclusion is conditional on an unproved L2core theorem.

So a consumer above OM order 4 who deletes the value-layer fences on J-D0's authority is relying on the formal coefficient standing in for the classifier coefficient — exactly what S10.4 says is not established. S1's sentence is never restated with that caveat, and S10.6 item 5's guard is about the *gauge dictionary*, not about the *order*. The two guards are adjacent but do not compose in the text. At W-10's (2,1,1) shape this is moot (order 0), which is presumably why it was not caught. Recorded so the cross-read does not re-derive it; whether S1 needs an order caveat is a note-owner call.

### Exhaustive residual judgment list

There are no other unresolved J-D0 mathematical or scope judgments. The six honest boxes, the S7 supplier grades (including GRTJA at arc 1/2 in the critical path), the disclosed finite enumeration scope, and S10.4's classifier perimeter are the note's own displayed conditionality; cert3, MQ-4, BOX-2's per-site fold, and the Λ/𝒢-reduction theorem are classified in §8 as obligations.

EFF-JD0 COMPILED: 38 statements / 27 xrefs verified / 3 open calls
