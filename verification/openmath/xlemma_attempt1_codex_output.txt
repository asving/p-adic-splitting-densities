Reading additional input from stdin...
OpenAI Codex v0.145.0
--------
workdir: /data/users/asvin/math-and-lean/p-adic-splitting-densities
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: high
reasoning summaries: none
session id: 019fed8e-6e3d-7b21-9a51-0eb2549078ca
--------
user
X-LEMMA PROOF ATTEMPT (the universal stage-recentering lemma) — you are a
mathematician in a fresh context. Everything needed is pasted below,
self-contained; you cannot read files.

CONTEXT: a prior probe (also pasted, at the end) established that GENHN-4
(the junction-consumption step) is NOT derivable from the WELDMASTER faces
(M0)-(M4) + WM-COB + J-D0 alone: layers 2-3 are recoverable, but layer 1
(the graded kills) requires exactly one missing lemma, called X below. The
probe characterized X precisely:

  X (the universal stage-recentering lemma), two inseparable halves:
  (i) PERIMETER RE-INSTANTIATION: an arbitrary stage-alpha event at
      (e1, f1, mu, kappa) is identified with a scored (M3) B_n-stratum
      inside (M2)'s slot fences (what (M0) needs before it can identify
      division chains);
  (ii) THE ADDITIVE ACTION: under recentering Phi' -> Phi' + w, the
      transform acts as T -> T + s coefficient-for-coefficient with
      matching z^{fl}-twists in both carry products and residual assembly;
      the coefficient identity res_{(mu-j)kappa}(A_j^new) = [T^j] R(T+s),
      INCLUDING the collision/mod-wrap cases.

YOUR CHARGE: PROVE X. Owner guidance: this lemma "simplifies the proof a
lot"; it matches an already-wanted elementary carry display (GENHN-BOX-2),
so it fits the program's intuition; attempt the full proof. Deliverables,
in order:
1. A precise statement of X (both halves), in the pasted materials'
   notation, with every hypothesis explicit.
2. THE PROOF, complete: every step justified from the pasted statements or
   from standard facts (name them precisely — standard literature is
   citable). If a step resists, do NOT paper over it: display the exact
   resisting case and downgrade the deliverable to a partial proof + the
   named residual, honestly.
3. A MACHINE-VERIFICATION PLAN: 3+ concrete frames (small explicit
   parameters), the predicted exact values of the X-identity at each
   (numbers, not shapes), and 2+ planted falsifiers (mutations that must
   fail). The program verifies numerically before trusting.
4. End with exactly one line: OUTCOME: PROVED | PARTIAL | OBSTRUCTED.

THE MATERIALS (from the probe's own self-contained brief, then the probe's
findings):

# SG-2 PROBE BRIEF (RCT unit S-7-SG2-probe, CODEX arm) — 2026-08-10

You are a research mathematician doing an adversarial feasibility probe in
p-adic Okutsu–Montes/Newton-polygon combinatorics. You see ONLY this brief
(you cannot read files). All statements below are pasted verbatim from a
frozen, verified corpus, with (note, commit, line) pins.

## 0. THE QUESTION

A large verified corpus proves splitting-density laws via a "weld": four
junction theorems tying a harness calculus (Φ-developments, weights, reads)
to a classifier/print calculus. WELDMASTER packages the four junctions as
five clauses (M0)–(M4) of ONE transport 𝕋 plus a coboundary theorem WM-COB.
Separately, LEMMA J-D0 proves count-layer gauge/value blindness. The lemma
LEMMA GENHN-4 (refine transfer) currently consumes COROLLARY W-9 (a face of
junction note GRTJC) DIRECTLY as its graded organizing frame (recorded as
GENHN-BOX-2). PROBE QUESTION (exactly this, no more):

  Is LEMMA GENHN-4 re-derivable from WELDMASTER's clauses (M0)–(M4) +
  WM-COB + LEMMA J-D0 ALONE (plus GENHN's own internal lemmas GENHN-2,
  GENHN-3 and classical facts) — i.e. does the weld's one-face consumption
  through WELDMASTER's stated clauses suffice, WITHOUT any direct citation
  into the junction notes (GRTJA/GRTJB/GRTJC/GRTW2) beyond what the clause
  texts themselves state?

You do NOT decide anything about retiring notes or formalization scope.
Your deliverable is the mathematical verdict only.

## 1. THE TARGET — LEMMA GENHN-4, verbatim
(GENHN_PROOF_2026-08-08.md @ 59c1966d, L865–921)

Setting: a stage-α event at dv-slope κ (the stage polygon one side
(0, μκ)–(μ, 0), integer κ > current floor S′, residual (T − s)^μ,
s ∈ K^×). Recenter Φ′ ↦ Φ′_new := Φ′ + w, w := −ŝ·n(κ)-lift (the
canonical digit lift; convention-keyed as always).

*The computation, in three layers.*
1. **Leading graded layer (the kills).** Pass to gr R = the twisted
   algebra K^c[ℤ/e₁-graded lines] (COROLLARY W-9 @ accepted pin,
   consumed HERE as the organizing frame — disclosed; GENHN-2 makes
   the passage pointwise-faithful: one K-slot per height, slot-min =
   valuation). In gr, the development transforms by the substitution
   Φ̄′ ↦ Φ̄′ + s̄ on the residual polynomial: the new residual at the
   node heights is R(T + s) where R(T) = (T − s)^μ, ALL of whose
   coefficients below degree μ vanish — the μ pins die. The twist
   powers z^{fl} (S4's cocycle: n(κ)^t = ẑ^{fl_t}n(tκ)) enter both
   the residual assembly and the carry products IDENTICALLY, so the
   graded identity holds coefficient for coefficient over K.
   [...instance reconciliation prose elided...] Both
   characteristics: the identity R(T+s) = T^μ is binomial-free as
   stated (it is a substitution identity, not an expansion).
2. **Correction layer (everything else lands deeper and in-window).**
   The non-graded corrections — products w^{i−j}A_i and the x-degree
   overflow of w·A_i reduced mod Φ′ — have dv strictly greater than
   the pinned heights: dv(w^{i−j}A_i-correction) ≥ (i−j)κ + (μ−i)κ +
   1 = (μ−j)κ + 1 (each factor exceeds its side height only in the
   correction terms; the exact-height products ARE layer 1). By
   LEMMA GENHN-3(c)(d) they land at in-window positions and move
   upward only.
3. **Bijectivity.** The map on digit spaces is triangular (each new
   digit = old digit + strictly-lower-data), injective, image inside
   the floored node {dv(A_j) > (μ−j)κ}; onto by cardinality: the
   refine slice fixes the μ pinned digits and frees exactly the
   slots above the node floors (slot strings are key-independent —
   GENHN-2), the same count as the node. Letters: |K| − 1 choices of
   s per step. ∎

Grade note (verbatim): "At μ = 2 the identity was verified pointwise-
elementarily by GENH4 [...]. At general μ, layer 1 CONSUMES the W-9 graded
frame (accepted 2/2) rather than re-deriving the carry digit-by-digit".

GENHN-BOX-2 (verbatim, L1331–1339): "LEMMA GENHN-4's layer 1 at μ ≥ 3
consumes COROLLARY W-9's twisted algebra as the carry bookkeeping (unlike
GENH4, which re-proved its μ = 2 instances elementarily). [...] An
elementary general-μ carry display (the GENH4-S5 computation at general
(e₁, f₁, μ)) would retire this box; until then the count laws at μ ≥ 3
stand on one accepted weld face."

## 2. ALLOWED INPUT A — WELDMASTER (fdf1d995), THEOREM WELD-M, L284–311

**THEOREM WELD-M.** For every tower T of the [ILN]† S0.1 class realized as
a principal-read OM type via the dictionary map T(t) (JA-PIN — the DOMAIN
identification, consumed: every class-pin clause forced by the print's own
hypotheses, lift clause = W-2), there is ONE transport 𝕋 between the
harness calculus (Φ-developments, weights w_m, reads R_m v, the ε-kit,
canonical lifts) and the classifier/print calculus (φ-adic developments,
v_{m+1}, residual operators, t(i)-twists, GMN keys), with five clauses:

    (M0) [substrate]      𝕋 is the IDENTITY on the underlying division
         arithmetic: the same canonical division chain by the same monic
         key at the admissible representative.
    (M1) [gradings]       𝕋 preserves weights in the sheared frame
         (w_m = v_{m+1}), and its induced map on polygon/grading data is
         the explicit affine shear Σ_q(i,u) = (i, e_q·u + h_q·i) — lower
         hulls to lower hulls, vertex-for-vertex, slope law s ↦ e_q s + h_q.
    (M2) [reads/operators] 𝕋 acts on packed read data by explicit PER-SLOT
         z-characters generated by ONE 1-cochain — the S0 weld cochain
         t_j/ε_j: scalar per slot exactly on the S3 perimeter, exact
         recursion ((HR-REC) genre) elsewhere. Never slot-independent
         (JA-BOX-1 inherited verbatim).
    (M3) [multiplication] 𝕋 transports multiplication up to the carry
         cocycle: on B_n coordinates the target is the twisted group
         algebra K^c[ℤ/E] with c = z̄^δ·∏_j z_j^{D_j} on the scored
         stratum, and harness-val = TC-read.
    (M4) [covariance]     𝕋 is covariant in the lift/representative
         choice: changing the representative acts on (M2)–(M3) by the
         (ξ, w) character system, with ψ-transport making the two chains
         carry the same type.

Perimeter/conditionality carried by the faces (from the note's face table,
condensed): (M0) W-2 clause 1's perimeter; byte clause on `P≤q` only.
(M1) shared-key perimeter `P<q` (one layer at `P≤q`); a u ≥ 0 membership
residue open at the unbounded-degree quantifier. (M2) per-digit proviso at
m ≥ 3 / (R-coll); (C-coll) at m ≥ 2; mod-wrap box; PER-SLOT, never scalar.
(M3) [ILN]† scored stratum; (DMULT-w) CITED / (DMULT-s) MEASURED tier
split. (M4) scalar closed form on (C-coll) only; W2-OPEN-1 (value layer)
OPEN. The note's own remark: "WELD-M is an ORGANIZING UMBRELLA [...] What
each junction note carries BEYOND its face (its closed forms, boundary
censuses, box systems) remains in that note and is NOT absorbed."

## 3. ALLOWED INPUT B — THEOREM WM-COB (same note, L344–369)

**THEOREM WM-COB.** Let (γ, γ′) be a pair with [ILN]† S0.3 data
(δ, S_j, D_j) and per-level split weights β_j := β_j(γ), β′_j := β_j(γ′),
β″_j := β_j(γ+γ′).
(i) [integer form; unconditional; ALL pairs] For every level j = 1..n:
    D_j = t_j(β″_j) − t_j(β_j) − t_j(β′_j) =: (∂t_j)(γ, γ′).
(ii) [field form] In K_j:  z_j^{D_j} = ε_j(β_j)·ε_j(β′_j)·ε_j(β″_j)^{−1}.
(iii) [composite display; scored stratum] With ∂E(γ,γ′) :=
E(γ)E(γ′)/E(γ+γ′):  c_{n+1}(γ, γ′) = z̄^δ·(∂E)(γ, γ′).
"THE ENTIRE INNER FACTOR OF THE ACCEPTED COCYCLE IS A COBOUNDARY [...] The
outer letter z̄^δ is NOT a coboundary of this cochain."

(THEOREM WM-FENCE, the note's third statement, is the value-layer
one-perimeter fence — character-triviality specializations (R-coll)/
(VD-m)/(C-coll). Available if you need it, but it is value-layer.)

## 4. ALLOWED INPUT C — LEMMA J-D0 (JD0_PROOF @ 9387eb1a, L40–57)

The carry gauge: a 1-cochain u : γ ↦ u_γ ∈ K^* acting by anchor rescaling
a_γ ↦ u_γ·a_γ; action: ledger c(γ,γ′) ↦ c·∂u; slot v ↦ u·v; residual
R(y) ↦ ξ^{−deg R}·R(ξ·y); positions, δ, D_j, grid abscissas UNCHANGED.

**LEMMA J-D0.** (i) [GAUGE-INVARIANCE] For every decided shape T, every
prime power q, and every 1-cochain u with unit values, the stratum count of
T computed through the u-gauged read equals the direct stratum count.
(ii) [VALUE-BLINDNESS] Any two towers/types with the same shape data
(d0, (e_j, h_j, g_j)_j) and residual-degree structure have EQUAL stratum
counts at every q, regardless of their letters and carry cochains.
Consequence: the stratum count is a function of the SHAPE alone.
[Proved; perimeter: unit-character gauges; the L3 × L4 count assembly.]

## 5. ALLOWED INPUTS D — GENHN's OWN lemmas (not weld faces; @ 59c1966d)

**LEMMA GENHN-2** (L699–717): A = Σ_{i<D′} a_i x^i, a_i ∈ O. Then
dv(A(θ)) = min_i (e₁·v(a_i) + i·h) EXACTLY, for every root θ of Φ′ and
every lift; min's residue class mod e₁ is i·h mod e₁ (distinct for distinct
i mod e₁, gcd(h,e₁) = 1); within-class ties assemble Σ_t res(a_{i+e₁t})·η^t
≠ 0 by F_Q-independence of {1, η, ..., η^{f₁−1}}. Each A_j carries exactly
ONE K-digit slot per integer dv-height; formal slot-min IS the valuation:
"the graded read transcribed pointwise." (Note: "W-9's dim_K 𝒜(T) = E = e₁
is this statement's graded shadow (cited as frame)" — GENHN-2 is proved
elementarily, NOT via the weld.)

The twist paragraph (L731–738, verbatim core): normalizers n(m) :=
x^{i(m)}π^{a(m)}, i(m)·h + e₁·a(m) = m; products wrap through θ^{e₁} =
(unit lift)·π^h: n(κ)^t = ẑ^{fl}·n(tκ) with explicit integer fl = the wrap
count — "W-9's cocycle, literal." Side residuals assembled with coherent
normalizers are z-power-twisted slot digits; "counts are twist-blind
(J-D0: (digits) ↦ (twisted digits) is a type-preserving bijection),
LETTERS are twisted."

**LEMMA GENHN-3** (L741–763): (a) a computed dv-value is lift-stable iff
< e₁N; (b) class-i slot string ends at e₁(N−1) + ih (ragged); (c) every
digit consulted by a readable event sits strictly inside its class's
window (congruence argument); (d) along chains every update determines new
digits at height d from old digits at heights ≤ d: carries move upward
only. [All proved elementarily in the note.]

## 6. REFERENCE ONLY — what GENHN-4 currently consumes (NOT an allowed
input except insofar as (M3)'s clause text carries it)

**COROLLARY W-9** (GRTJC @ 63091cef, L1777–1787): Given W-6, W-7, W-8:
(i) c_{m+1} descends to ℤ/E × ℤ/E → K^* and is a 2-cocycle (= associativity
of gr(w)'s multiplication); (ii) its y-degree part δ satisfies the integer
carry-cocycle identity; (iii) 𝒜(T) is the twisted group algebra K^c[ℤ/E]
of the abscissa-coset group over the carry cocycle; (iv) the E-fold orbit
product ζ_T := ∏ c(1,k) is the K-scalar tying anchor product to π-power.

The recorded scent (HE3_PROOF @ its pin, §S0, verbatim): "GENHN.A(iv)/
LEMMA GENHN-4 is NOT consumed [by HE3], hence the W-9 graded weld face
(GENHN-BOX-2) is NOT inherited: GENHN-4 is the *digit-space bijection*
used for COUNT laws; this unit needs only the *valuations and residues* of
a recentered development."

## 7. YOUR CHARGE

ATTEMPT THE DERIVATION of LEMMA GENHN-4 (all three layers) from inputs
A + B + C + D + classical facts only. Rules:
- W-9 may be used ONLY through (M3)'s clause text ("on B_n coordinates the
  target is the twisted group algebra K^c[ℤ/E] with c = z̄^δ·∏_j z_j^{D_j}
  on the scored stratum, and harness-val = TC-read") with its scored-
  stratum perimeter — not through GRTJC's internal content.
- No input may be cited that itself consumes GENHN-4 (circularity).
- Perimeter accounting is mandatory: GENHN-4 quantifies over ALL stage-α
  events at general (e₁, f₁, μ). Say explicitly whether (M3)'s scored
  stratum + (M2)'s per-slot fences cover that range, or whether the
  perimeter mismatch is itself the obstruction.
- Note that GENHN-4 is used at STAGE level (the stage-graded algebra of a
  stage-α event, slope κ, residual (T−s)^μ) — if (M0)–(M4) speak only of
  the tower/type transport at the base weld and not of the stage-internal
  graded frame, say precisely which re-instantiation would be needed.

OUTPUT FORMAT (strict):
1. VERDICT: one of DERIVABLE / NOT-DERIVABLE / NEEDS-X. If NEEDS-X, state
   X as a precise mathematical statement (the exact missing content).
2. PER-LAYER verdicts (layer 1 graded kills; layer 2 correction
   strictness; layer 3 bijectivity+count), one line each, each naming the
   inputs (M0..M4, WM-COB, J-D0, GENHN-2, GENHN-3) actually used.
3. If DERIVABLE: the full proof sketch, numbered steps, each step naming
   its input label; flag the single most falsifiable step (one that a
   machine could spot-check on a concrete stage-α event).
4. If NOT-DERIVABLE or NEEDS-X: the EXACT obstruction — which GENHN-4
   content is not present in the weld's faces (e.g. the stage-level
   twisted-algebra structure vs the tower-level K^c[ℤ/E]; the fl-twist
   cancellation in residual assembly vs carry products; the cardinality
   count of freed slots) — stated so that a follow-up unit could target
   exactly it.
5. CHECKED-CLEAN list: which parts of GENHN-4 ARE clearly recoverable from
   the allowed inputs, so the residue is minimal.
No deference: the corpus authors expect you to try to BREAK the hoped-for
derivation. An honest NOT-DERIVABLE with a sharp obstruction is worth more
than an optimistic sketch.
=== THE PROBE'S FULL FINDINGS (verbatim) ===
# SG-2 PROBE — GENHN-4 vs WELDMASTER (M0)–(M4) + J-D0 (CODEX ARM)

**RCT unit S-7-SG2-probe** (docs/PROVER_RCT_PROTOCOL_2026-08-10.md §3 +
Amendment 1; assignment ledger row: frontier, first-hex b, CODEX).
**Blueprint:** docs/SIMPLIFICATION_BLUEPRINT_2026-08-10.md §3 SG-2, as
re-scoped by Plan revision 1 R.4 (F23/F36): this probe concludes at most
"GENHN-4 is / is not re-derivable from WELDMASTER M0–M4 + JD0"; it claims
NOTHING about retiring junction notes, and it is BRANCH-NEUTRAL between
the two declared R.4 payoff branches (Asvin's choice).

**Run of record.** Brief: `sg2_probe_brief_2026-08-10.md` (233 lines,
sealed commit 628feeb0 — GENHN-4 verbatim @ 59c1966d L865–921; WELD-M
@ fdf1d995 L284–311 + face conditionality; WM-COB L344–369; J-D0
@ 9387eb1a L49–57 + gauge action; GENHN-2/GENHN-3 as allowed non-weld
inputs; W-9 marked REFERENCE-ONLY, usable solely through (M3)'s clause
text). ONE foreground call, `codex exec --skip-git-repo-check
-c model_reasoning_effort=high -`, exit 0, wall 113 s, 13,037 tokens;
raw output `verification/openmath/sg2_probe_codex_output.txt`
(md5 ceb96150, commit c0dc1e8e).

---

## THE FEASIBILITY VERDICT (branch-neutral, R.4-scoped)

**NEEDS-X.** On this probe's trace, GENHN-4 is NOT re-derivable from
WELDMASTER (M0)–(M4) + WM-COB + J-D0 as those clauses are stated: the
weld's one-face consumption does not suffice for layer 1 (the graded
kills). Two of GENHN-4's three layers ARE recoverable without any direct
junction-note citation (layer 2 from GENHN-2 + GENHN-3(c)(d) + classical
filtered arithmetic; layer 3 from GENHN-2 + GENHN-3(d) + J-D0 +
triangular-map cardinality, CONDITIONAL on layer 1). The residue X is
stated precisely below. This verdict informs, and does not make, the R.4
branch decision; a whole-path retirement claim would additionally
require the corpus-wide face-citation audit named at R.4 and the branch
choice — neither is attempted here.

**X (the exact missing content, as returned):** a universal
stage-recentering lemma — for a stage-α event at data (e₁, f₁, μ, κ),
(i) the stage filtration is identified with an (M3) scored B_n-stratum
with the relevant slots inside (M2)'s perimeter, and (ii) the
recentering Φ′ ↦ Φ′ + w induces, in coherent normalized graded
coordinates, res_{(μ−j)κ}(A_j^new) = [T^j] R(T+s) for 0 ≤ j ≤ μ —
equivalently, the cocycle factors from the products w^{i−j}A_i agree
term-by-term with the twist factors assembling the stage residual
polynomial — including collision/mod-wrap cases beyond WELD-M's
scored/perimeter subrange.

## THE OBSTRUCTION (two inseparable layer-1 gaps, per the output)

1. **Perimeter re-instantiation is absent from the faces.** GENHN-4
   ranges over stage-α events at arbitrary (e₁, f₁, μ); (M3) speaks on
   the [ILN]† scored stratum and (M2) carries level/collision/mod-wrap
   fences. No clause of (M0)–(M4) asserts that a stage event determines
   an admissible principal-read type in WELD-M's domain, identifies
   stage node coordinates with B_n-coordinates, is scored, or meets
   (M2)'s slot conditions. (M0) identifies division chains only AFTER
   such an instantiation.
2. **The additive recentering action is not a stated face.** (M3) gives
   the ambient twisted algebra K^c[ℤ/E] but not how Φ′ ↦ Φ′ + w becomes
   T ↦ T + s coefficient-for-coefficient with matching z^{fl}-factors in
   both the carry products and the residual assembly. (M4)'s covariance
   is a character-system action, not the additive-translation formula.
   WM-COB gauges the inner factor ∏_j z_j^{D_j} but leaves z̄^δ and does
   not identify the residual variable. J-D0 is count-layer blindness and
   cannot substitute for the event-level coefficient identity
   R(T+s) = T^μ.

## CHECKED-CLEAN (recoverable from the allowed inputs; 8 items returned)

GENHN-2's slot-per-height read with slot-min = valuation; the normalizer
wrap powers as recorded (compatibility at general μ NOT included);
layer-2 (+1)-strictness once leading terms vanish; GENHN-3(c)(d) window
placement + upward-only carries; triangularity ⟹ injectivity; equal
free-slot cardinalities ⟹ surjectivity; J-D0 twist-blindness of the
resulting counts; the letter factor |K|−1 from s ∈ K^×.

## LAB-TECH RECORD (transcription only; no content altered)

* The verdict, obstruction, and checked-clean sections above transcribe
  the CODEX output's items 1/2/4/5 with notation matched to the corpus
  (LaTeX → Unicode); the raw output is committed unedited and is the
  artifact of record. The output carries no item "3" — the format
  reserved 3 for a DERIVABLE sketch, which was not claimed.
* **No machine spot-check owed:** the unit's machine leg fires only on a
  claimed derivation (feasible-with-sketch); the returned verdict is
  NEEDS-X, so no sealed spot-check instrument was built. The X-statement
  is itself machine-checkable (res_{(μ−j)κ}(A_j^new) = [T^j]R(T+s) on
  concrete stage-α events) and is the natural sealed battery IF a
  follow-up unit attempts X.
* Consistency note (tech observation, not adjudication): the NEEDS-X
  geography matches GENHN-BOX-2's own record — "an elementary general-μ
  carry display (the GENH4-S5 computation at general (e₁, f₁, μ)) would
  retire this box" — i.e. the probe's X is the box's known wanted
  display PLUS the perimeter-instantiation clause the box does not name.
* Declared conditionality (R.4/F36): this verdict reads the statements
  at the S-0 ledger pins (628feeb0's brief quotes; ledger baseline
  7e2b38df) and under the standing axiom policy; it is CODEX-arm
  composed and awaits the standard arc treatment if consumed further.

## RCT §4 outcome data (recorded at unit close)

(a) rounds: single call, no retry. (b) first-pass profile: n/a (probe,
not a proof note). (c) instrument: none owed (see above). (e) cost:
113 s wall, 13,037 tokens, one call. (f) qualitative: the arm consumed a
233-line fully-pasted brief and returned a structured, perimeter-aware
obstruction that separates the three layers and names the two missing
face contents precisely; it neither overclaimed derivability nor padded
the checked-clean list beyond the allowed inputs.
warning: Codex could not find bubblewrap on PATH. Install bubblewrap with your OS package manager. See the sandbox prerequisites: https://developers.openai.com/codex/concepts/sandboxing#prerequisites. Codex will use the bundled bubblewrap in the meantime.
codex
I’ll push the elementary carry computation as far as the supplied axioms permit, and I’ll separate a genuine algebraic proof from any perimeter assertion that the pasted statements do not define strongly enough to prove.
codex
## 1. Precise statement of X

Let \(K\) be the residue field, and let

\[
(e_1,f_1,\mu,\kappa),\qquad e_1,f_1,\mu\ge 1,\quad \kappa>S'
\]

be the data of a readable stage-\(\alpha\) event. Let \(\Phi'\) be its monic admissible representative and

\[
F=\sum_{i=0}^{\mu} A_i(\Phi')^i,\qquad
d_i=(\mu-i)\kappa ,
\]

its relevant \(\Phi'\)-development. Assume:

1. \(dv(A_i)\ge d_i\), with a coefficient of valuation \(>d_i\) assigned zero residue at height \(d_i\).
2. The stage polygon is the single side from \((0,\mu\kappa)\) to \((\mu,0)\).
3. Its coherently normalized side residual polynomial is

   \[
   R(T)=\sum_{i=0}^{\mu}r_iT^i=(T-s)^\mu,
   \qquad s\in K^\times .
   \]
4. The normalizers are

   \[
   n(m)=x^{i(m)}\pi^{a(m)},\qquad i(m)h+e_1a(m)=m,
   \]

   with \(0\le i(m)<e_1\), and the initial form of \(n(m)\) spans the graded line of height \(m\).
5. Write

   \[
   \operatorname{in}(n(a))\operatorname{in}(n(b))
     =c(a,b)\operatorname{in}(n(a+b)),
   \]

   where grades are interpreted in the relevant \(B_n\)-coordinate group, including reduction modulo \(E\). Put

   \[
   n(\kappa)^t=q_t\,n(t\kappa),\qquad
   q_t=\bar z^{\,fl_t}.
   \]

   Thus \(q_0=q_1=1\).
6. If

   \[
   \operatorname{in}_{d_i}(A_i)=a_i\operatorname{in}(n(d_i)),
   \]

   define the coherently normalized stage residue by

   \[
   r_i=\operatorname{res}_{d_i}(A_i)
      :=\lambda_{\mu,i}a_i,\qquad
   \lambda_{\mu,i}:=
      q_i\,c(d_i,i\kappa).
   \]

   This explicitly incorporates the \(z^{fl}\)-twist used in residual assembly.
7. Let \(\widehat s\) be the canonical lift of \(s\), set

   \[
   w=-\widehat s\,n(\kappa)+(\text{terms of }dv>\kappa),
   \qquad
   \Phi'_{\rm new}=\Phi'+w,
   \]

   and let \(A_j^{\rm new}\) be the coefficients obtained by the canonical division chain for the \(\Phi'_{\rm new}\)-development.

Then X asserts both of the following.

**X(i), perimeter re-instantiation.** Every such stage event admits an instance of the WELD-M dictionary: its canonical division chain is the chain in (M0); its node-height slots are the corresponding (M2) slots; its graded coordinates form an (M3) scored \(B_n\)-stratum; and every slot used in the recentering lies within the (M2) perimeter, including all \((R\text{-coll})\), \((C\text{-coll})\), and mod-wrap cases.

**X(ii), additive action.** For every \(0\le j\le\mu\),

\[
\boxed{\quad
\operatorname{res}_{(\mu-j)\kappa}(A_j^{\rm new})
 =[T^j]R(T+s).
\quad}
\]

Equivalently, in raw line coordinates,

\[
\boxed{\quad
a_j^{\rm new}
 =\lambda_{\mu,j}^{-1}
   \sum_{i=j}^{\mu}
   \binom{i}{j}\lambda_{\mu,i}a_i s^{\,i-j}.
\quad}
\]

The formula includes coincident grades and reductions modulo \(E\): all such effects are represented by \(c\), \(q_t=\bar z^{fl_t}\), and \(\lambda_{\mu,i}\).

Since \(R(T)=(T-s)^\mu\), it follows that

\[
R(T+s)=T^\mu,
\]

so all residues below degree \(\mu\) vanish.

## 2. Proof attempt

### The cocycle carry identity

Assume first that X(i) supplies the scored stage-level twisted algebra. Because it is the associated graded algebra of commutative division arithmetic, its multiplication is associative and commutative. Consequently \(c\) satisfies the normalized cocycle identity

\[
c(a,b)c(a+b,d)=c(b,d)c(a,b+d).
\]

This is the standard equivalence between associativity of a twisted group algebra and the 2-cocycle identity.

Fix \(j\le i\), and put \(r=i-j\). The exact-height part of \((-w)^rA_i\) is

\[
s^r n(\kappa)^r\cdot a_i n(d_i)
 =s^ra_iq_r c(r\kappa,d_i)n(d_j),
\]

because \(d_i+r\kappa=d_j\).

When this contribution is assembled as the degree-\(j\) residual coefficient, it acquires the factor

\[
\lambda_{\mu,j}=q_jc(d_j,j\kappa).
\]

The crucial carry identity is therefore

\[
\lambda_{\mu,j}q_r c(r\kappa,d_i)
   =\lambda_{\mu,i}. \tag{1}
\]

Indeed, the left-hand side is the scalar obtained by writing

\[
\bigl(n(\kappa)^r n(d_i)\bigr)n(\kappa)^j
\]

in the basis \(n(\mu\kappa)\), whereas the right-hand side is the scalar obtained from

\[
n(d_i)n(\kappa)^i.
\]

These are the same product because \(i=r+j\), and multiplication is associative and commutative. Expanding the two products gives (1) explicitly.

This proof uses the full cocycle, including the outer \(\bar z^\delta\). It does not attempt to gauge that factor away. WM-COB is therefore unnecessary for (1); it only explains why the inner factors \(\prod z_j^{D_j}\) form a coboundary.

### Coefficient transformation

In a commutative ring the binomial theorem gives

\[
(\Phi')^i
 =(\Phi'_{\rm new}-w)^i
 =\sum_{j=0}^{i}\binom{i}{j}
   (\Phi'_{\rm new})^j(-w)^{i-j}.
\]

Hence the exact-height part of the new coefficient at position \(j\) is the sum over \(i\ge j\) of the exact-height parts of

\[
\binom{i}{j}(-w)^{i-j}A_i.
\]

Canonical reductions needed to restore coefficient degree may create carries. Under (M0), the associated-graded calculation uses the same division chain; GENHN-3(d) says those carries move upward only. Terms containing either the higher-\(dv\) part of \(w\) or a higher-\(dv\) part of \(A_i\) have valuation at least

\[
(i-j)\kappa+d_i+1=d_j+1,
\]

so they do not affect the residue at \(d_j\). This is the standard initial-form rule for a multiplicative filtration.

Using (1), the normalized residue contributed by index \(i\) is

\[
\binom{i}{j}s^{i-j}\lambda_{\mu,i}a_i
 =\binom{i}{j}s^{i-j}r_i.
\]

Therefore

\[
\operatorname{res}_{d_j}(A_j^{\rm new})
 =\sum_{i=j}^{\mu}\binom{i}{j}r_i s^{i-j}
 =[T^j]R(T+s).
\]

This proves X(ii), conditional on the stage-level instantiation in X(i).

The proof also covers:

- **Collisions:** terms landing in the same line are added in that line. If they cancel, their sum has zero initial form and the valuation rises, exactly as the coefficient formula predicts.
- **Mod-wrap:** every wrap is included in \(c\) and \(q_t\); identity (1) follows from associativity after reduction modulo \(E\).
- **Positive characteristic:** no division by factorials occurs. The displayed binomial coefficients are their images in \(K\), and

  \[
  ((T+s)-s)^\mu=T^\mu
  \]

  is a polynomial-substitution identity in every characteristic.

Thus the additive carry calculation itself is complete once the event is licensed to use the relevant twisted graded algebra.

### The resisting half: X(i)

X(i) is not provable from the pasted statements.

WELD-M begins with a tower in the \([ILN]^\dagger\) S0.1 class already realized as a principal-read OM type. Clause (M3) then applies only on its scored \(B_n\)-stratum, while (M2) retains explicit collision and mod-wrap fences.

By contrast, the hypotheses defining the stage event supply only

\[
(e_1,f_1,\mu,\kappa),\quad
\Phi',\quad
dv(A_i)\ge(\mu-i)\kappa,\quad
R(T)=(T-s)^\mu.
\]

No supplied implication establishes that:

1. this stage event is a new WELD-M domain object;
2. its node coordinates are \(B_n\)-coordinates;
3. the resulting stratum is scored; or
4. its collision/mod-wrap slots satisfy every (M2) proviso.

Clause (M0) identifies division chains only after such an instance exists; it cannot create the instance. Neither WM-COB nor J-D0 changes domains or slot perimeters.

This is also a formal independence obstruction. One may enlarge the collection of stage events by one event not declared scored, while leaving the class of WELD-M towers and every assertion (M0)–(M4), WM-COB, J-D0, GENHN-2, and GENHN-3 unchanged. All supplied statements remain true, but X(i) is false for the added event. Hence X(i) requires an additional bridge theorem.

The first concrete resisting case is already

\[
e_1=2,\quad f_1=1,\quad \mu=4,
\]

with \(\kappa\) odd. Then \(n(\kappa)^2\) wraps modulo \(e_1\), and the \(i=3\) and \(i=4\) contributions to lower new coefficients require mod-wrap multiplication. The pasted faces do not assert that an arbitrary such stage event lies inside the applicable (M2)/(M3) perimeter.

The named residual is therefore:

> **X-PERIM:** Every readable stage-\(\alpha\) event is functorially realized as a scored WELD-M \(B_n\)-stratum, with all node, collision, and mod-wrap slots inside the (M2) perimeter and with (M0) identifying its canonical redivision chain.

Once X-PERIM is supplied, the calculation above proves X(ii) and hence all graded kills.

## 3. Machine-verification plan

Use the explicit normalizer model

\[
i(m)=m\bmod e_1,\qquad
c(a,b)=z^{\left\lfloor
(i(a)+i(b))/e_1
\right\rfloor},
\qquad
q_t=z^{\lfloor t\,i(\kappa)/e_1\rfloor}.
\]

For every frame, the verifier should:

1. check the cocycle identity for all grades occurring;
2. compute \(\lambda_{\mu,i}=q_i c(d_i,i\kappa)\);
3. convert normalized residuals \(r_i\) to raw digits \(a_i=r_i/\lambda_{\mu,i}\);
4. perform the twisted binomial convolution;
5. compare \(\lambda_{\mu,j}a_j^{\rm new}\) with \([T^j]R(T+s)\).

### Frame 1: ordinary, characteristic \(5\)

\[
K=\mathbf F_5,\quad e_1=f_1=1,\quad
\mu=3,\quad\kappa=1,\quad s=2,\quad z=1.
\]

Here

\[
R(T)=(T-2)^3
 =2+2T+4T^2+T^3.
\]

Thus

\[
(r_0,r_1,r_2,r_3)=(2,2,4,1),
\qquad
(\lambda_0,\lambda_1,\lambda_2,\lambda_3)=(1,1,1,1).
\]

Predicted identity:

\[
\bigl(\operatorname{res}_{3}(A_0^{\rm new}),
\operatorname{res}_{2}(A_1^{\rm new}),
\operatorname{res}_{1}(A_2^{\rm new}),
\operatorname{res}_{0}(A_3^{\rm new})\bigr)
=(0,0,0,1).
\]

### Frame 2: repeated mod-wrap

\[
K=\mathbf F_7,\quad e_1=2,\quad f_1=1,\quad
\mu=4,\quad\kappa=1,\quad s=2,\quad z=3.
\]

One obtains

\[
R(T)=(T-2)^4
 =2+3T+3T^2+6T^3+T^4,
\]

\[
(\lambda_0,\lambda_1,\lambda_2,\lambda_3,\lambda_4)
 =(1,3,3,2,2),
\]

and raw digits

\[
(a_0,a_1,a_2,a_3,a_4)=(2,1,1,3,4).
\]

The twisted convolution must give

\[
(a_0^{\rm new},a_1^{\rm new},a_2^{\rm new},
  a_3^{\rm new},a_4^{\rm new})
=(0,0,0,0,4).
\]

After residual normalization, the exact X-values are

\[
(0,0,0,0,1).
\]

### Frame 3: collision and characteristic-\(3\) binomial collapse

Let

\[
K=\mathbf F_9=\mathbf F_3[\alpha]/(\alpha^2+1),
\quad e_1=3,\quad f_1=2,\quad
\mu=3,\quad\kappa=2,\quad s=1,\quad z=2.
\]

All displayed values lie in the prime subfield. Here

\[
R(T)=(T-1)^3=T^3-1=2+T^3,
\]

\[
(\lambda_0,\lambda_1,\lambda_2,\lambda_3)
=(1,2,1,1),
\qquad
(a_0,a_1,a_2,a_3)=(2,0,0,1).
\]

The exact X-values are again

\[
(0,0,0,1).
\]

This simultaneously checks a wrap from \(2+2\) modulo \(3\) and the vanishing of \(\binom31,\binom32\) in characteristic \(3\).

### Planted falsifiers

1. **Delete the carry twists in Frame 2.** Ordinary convolution of the raw vector \((2,1,1,3,4)\) gives

   \[
   (5,1,3,0,4).
   \]

   Normalizing afterward gives

   \[
   (5,3,2,0,1)\ne(0,0,0,0,1).
   \]

   The checker must reject this mutation.

2. **Reverse the recentering sign in Frame 1.** Replacing \(w=-\widehat s n(\kappa)\) by \(w=+\widehat s n(\kappa)\) produces \(R(T-s)\), namely

   \[
   (T-4)^3=(T+1)^3
     =1+3T+3T^2+T^3
   \]

   in \(\mathbf F_5[T]\). The checker must obtain \((1,3,3,1)\), not \((0,0,0,1)\).

These frames verify X(ii). A verifier for X(i) cannot be built from the pasted material: it additionally needs executable definitions of the \([ILN]^\dagger\) domain predicate, the scoring predicate, the stage-to-\(B_n\) map, and every (M2) collision/mod-wrap fence. It should fail closed when those predicates are unavailable.

OUTCOME: PARTIAL
tokens used
13,754
## 1. Precise statement of X

Let \(K\) be the residue field, and let

\[
(e_1,f_1,\mu,\kappa),\qquad e_1,f_1,\mu\ge 1,\quad \kappa>S'
\]

be the data of a readable stage-\(\alpha\) event. Let \(\Phi'\) be its monic admissible representative and

\[
F=\sum_{i=0}^{\mu} A_i(\Phi')^i,\qquad
d_i=(\mu-i)\kappa ,
\]

its relevant \(\Phi'\)-development. Assume:

1. \(dv(A_i)\ge d_i\), with a coefficient of valuation \(>d_i\) assigned zero residue at height \(d_i\).
2. The stage polygon is the single side from \((0,\mu\kappa)\) to \((\mu,0)\).
3. Its coherently normalized side residual polynomial is

   \[
   R(T)=\sum_{i=0}^{\mu}r_iT^i=(T-s)^\mu,
   \qquad s\in K^\times .
   \]
4. The normalizers are

   \[
   n(m)=x^{i(m)}\pi^{a(m)},\qquad i(m)h+e_1a(m)=m,
   \]

   with \(0\le i(m)<e_1\), and the initial form of \(n(m)\) spans the graded line of height \(m\).
5. Write

   \[
   \operatorname{in}(n(a))\operatorname{in}(n(b))
     =c(a,b)\operatorname{in}(n(a+b)),
   \]

   where grades are interpreted in the relevant \(B_n\)-coordinate group, including reduction modulo \(E\). Put

   \[
   n(\kappa)^t=q_t\,n(t\kappa),\qquad
   q_t=\bar z^{\,fl_t}.
   \]

   Thus \(q_0=q_1=1\).
6. If

   \[
   \operatorname{in}_{d_i}(A_i)=a_i\operatorname{in}(n(d_i)),
   \]

   define the coherently normalized stage residue by

   \[
   r_i=\operatorname{res}_{d_i}(A_i)
      :=\lambda_{\mu,i}a_i,\qquad
   \lambda_{\mu,i}:=
      q_i\,c(d_i,i\kappa).
   \]

   This explicitly incorporates the \(z^{fl}\)-twist used in residual assembly.
7. Let \(\widehat s\) be the canonical lift of \(s\), set

   \[
   w=-\widehat s\,n(\kappa)+(\text{terms of }dv>\kappa),
   \qquad
   \Phi'_{\rm new}=\Phi'+w,
   \]

   and let \(A_j^{\rm new}\) be the coefficients obtained by the canonical division chain for the \(\Phi'_{\rm new}\)-development.

Then X asserts both of the following.

**X(i), perimeter re-instantiation.** Every such stage event admits an instance of the WELD-M dictionary: its canonical division chain is the chain in (M0); its node-height slots are the corresponding (M2) slots; its graded coordinates form an (M3) scored \(B_n\)-stratum; and every slot used in the recentering lies within the (M2) perimeter, including all \((R\text{-coll})\), \((C\text{-coll})\), and mod-wrap cases.

**X(ii), additive action.** For every \(0\le j\le\mu\),

\[
\boxed{\quad
\operatorname{res}_{(\mu-j)\kappa}(A_j^{\rm new})
 =[T^j]R(T+s).
\quad}
\]

Equivalently, in raw line coordinates,

\[
\boxed{\quad
a_j^{\rm new}
 =\lambda_{\mu,j}^{-1}
   \sum_{i=j}^{\mu}
   \binom{i}{j}\lambda_{\mu,i}a_i s^{\,i-j}.
\quad}
\]

The formula includes coincident grades and reductions modulo \(E\): all such effects are represented by \(c\), \(q_t=\bar z^{fl_t}\), and \(\lambda_{\mu,i}\).

Since \(R(T)=(T-s)^\mu\), it follows that

\[
R(T+s)=T^\mu,
\]

so all residues below degree \(\mu\) vanish.

## 2. Proof attempt

### The cocycle carry identity

Assume first that X(i) supplies the scored stage-level twisted algebra. Because it is the associated graded algebra of commutative division arithmetic, its multiplication is associative and commutative. Consequently \(c\) satisfies the normalized cocycle identity

\[
c(a,b)c(a+b,d)=c(b,d)c(a,b+d).
\]

This is the standard equivalence between associativity of a twisted group algebra and the 2-cocycle identity.

Fix \(j\le i\), and put \(r=i-j\). The exact-height part of \((-w)^rA_i\) is

\[
s^r n(\kappa)^r\cdot a_i n(d_i)
 =s^ra_iq_r c(r\kappa,d_i)n(d_j),
\]

because \(d_i+r\kappa=d_j\).

When this contribution is assembled as the degree-\(j\) residual coefficient, it acquires the factor

\[
\lambda_{\mu,j}=q_jc(d_j,j\kappa).
\]

The crucial carry identity is therefore

\[
\lambda_{\mu,j}q_r c(r\kappa,d_i)
   =\lambda_{\mu,i}. \tag{1}
\]

Indeed, the left-hand side is the scalar obtained by writing

\[
\bigl(n(\kappa)^r n(d_i)\bigr)n(\kappa)^j
\]

in the basis \(n(\mu\kappa)\), whereas the right-hand side is the scalar obtained from

\[
n(d_i)n(\kappa)^i.
\]

These are the same product because \(i=r+j\), and multiplication is associative and commutative. Expanding the two products gives (1) explicitly.

This proof uses the full cocycle, including the outer \(\bar z^\delta\). It does not attempt to gauge that factor away. WM-COB is therefore unnecessary for (1); it only explains why the inner factors \(\prod z_j^{D_j}\) form a coboundary.

### Coefficient transformation

In a commutative ring the binomial theorem gives

\[
(\Phi')^i
 =(\Phi'_{\rm new}-w)^i
 =\sum_{j=0}^{i}\binom{i}{j}
   (\Phi'_{\rm new})^j(-w)^{i-j}.
\]

Hence the exact-height part of the new coefficient at position \(j\) is the sum over \(i\ge j\) of the exact-height parts of

\[
\binom{i}{j}(-w)^{i-j}A_i.
\]

Canonical reductions needed to restore coefficient degree may create carries. Under (M0), the associated-graded calculation uses the same division chain; GENHN-3(d) says those carries move upward only. Terms containing either the higher-\(dv\) part of \(w\) or a higher-\(dv\) part of \(A_i\) have valuation at least

\[
(i-j)\kappa+d_i+1=d_j+1,
\]

so they do not affect the residue at \(d_j\). This is the standard initial-form rule for a multiplicative filtration.

Using (1), the normalized residue contributed by index \(i\) is

\[
\binom{i}{j}s^{i-j}\lambda_{\mu,i}a_i
 =\binom{i}{j}s^{i-j}r_i.
\]

Therefore

\[
\operatorname{res}_{d_j}(A_j^{\rm new})
 =\sum_{i=j}^{\mu}\binom{i}{j}r_i s^{i-j}
 =[T^j]R(T+s).
\]

This proves X(ii), conditional on the stage-level instantiation in X(i).

The proof also covers:

- **Collisions:** terms landing in the same line are added in that line. If they cancel, their sum has zero initial form and the valuation rises, exactly as the coefficient formula predicts.
- **Mod-wrap:** every wrap is included in \(c\) and \(q_t\); identity (1) follows from associativity after reduction modulo \(E\).
- **Positive characteristic:** no division by factorials occurs. The displayed binomial coefficients are their images in \(K\), and

  \[
  ((T+s)-s)^\mu=T^\mu
  \]

  is a polynomial-substitution identity in every characteristic.

Thus the additive carry calculation itself is complete once the event is licensed to use the relevant twisted graded algebra.

### The resisting half: X(i)

X(i) is not provable from the pasted statements.

WELD-M begins with a tower in the \([ILN]^\dagger\) S0.1 class already realized as a principal-read OM type. Clause (M3) then applies only on its scored \(B_n\)-stratum, while (M2) retains explicit collision and mod-wrap fences.

By contrast, the hypotheses defining the stage event supply only

\[
(e_1,f_1,\mu,\kappa),\quad
\Phi',\quad
dv(A_i)\ge(\mu-i)\kappa,\quad
R(T)=(T-s)^\mu.
\]

No supplied implication establishes that:

1. this stage event is a new WELD-M domain object;
2. its node coordinates are \(B_n\)-coordinates;
3. the resulting stratum is scored; or
4. its collision/mod-wrap slots satisfy every (M2) proviso.

Clause (M0) identifies division chains only after such an instance exists; it cannot create the instance. Neither WM-COB nor J-D0 changes domains or slot perimeters.

This is also a formal independence obstruction. One may enlarge the collection of stage events by one event not declared scored, while leaving the class of WELD-M towers and every assertion (M0)–(M4), WM-COB, J-D0, GENHN-2, and GENHN-3 unchanged. All supplied statements remain true, but X(i) is false for the added event. Hence X(i) requires an additional bridge theorem.

The first concrete resisting case is already

\[
e_1=2,\quad f_1=1,\quad \mu=4,
\]

with \(\kappa\) odd. Then \(n(\kappa)^2\) wraps modulo \(e_1\), and the \(i=3\) and \(i=4\) contributions to lower new coefficients require mod-wrap multiplication. The pasted faces do not assert that an arbitrary such stage event lies inside the applicable (M2)/(M3) perimeter.

The named residual is therefore:

> **X-PERIM:** Every readable stage-\(\alpha\) event is functorially realized as a scored WELD-M \(B_n\)-stratum, with all node, collision, and mod-wrap slots inside the (M2) perimeter and with (M0) identifying its canonical redivision chain.

Once X-PERIM is supplied, the calculation above proves X(ii) and hence all graded kills.

## 3. Machine-verification plan

Use the explicit normalizer model

\[
i(m)=m\bmod e_1,\qquad
c(a,b)=z^{\left\lfloor
(i(a)+i(b))/e_1
\right\rfloor},
\qquad
q_t=z^{\lfloor t\,i(\kappa)/e_1\rfloor}.
\]

For every frame, the verifier should:

1. check the cocycle identity for all grades occurring;
2. compute \(\lambda_{\mu,i}=q_i c(d_i,i\kappa)\);
3. convert normalized residuals \(r_i\) to raw digits \(a_i=r_i/\lambda_{\mu,i}\);
4. perform the twisted binomial convolution;
5. compare \(\lambda_{\mu,j}a_j^{\rm new}\) with \([T^j]R(T+s)\).

### Frame 1: ordinary, characteristic \(5\)

\[
K=\mathbf F_5,\quad e_1=f_1=1,\quad
\mu=3,\quad\kappa=1,\quad s=2,\quad z=1.
\]

Here

\[
R(T)=(T-2)^3
 =2+2T+4T^2+T^3.
\]

Thus

\[
(r_0,r_1,r_2,r_3)=(2,2,4,1),
\qquad
(\lambda_0,\lambda_1,\lambda_2,\lambda_3)=(1,1,1,1).
\]

Predicted identity:

\[
\bigl(\operatorname{res}_{3}(A_0^{\rm new}),
\operatorname{res}_{2}(A_1^{\rm new}),
\operatorname{res}_{1}(A_2^{\rm new}),
\operatorname{res}_{0}(A_3^{\rm new})\bigr)
=(0,0,0,1).
\]

### Frame 2: repeated mod-wrap

\[
K=\mathbf F_7,\quad e_1=2,\quad f_1=1,\quad
\mu=4,\quad\kappa=1,\quad s=2,\quad z=3.
\]

One obtains

\[
R(T)=(T-2)^4
 =2+3T+3T^2+6T^3+T^4,
\]

\[
(\lambda_0,\lambda_1,\lambda_2,\lambda_3,\lambda_4)
 =(1,3,3,2,2),
\]

and raw digits

\[
(a_0,a_1,a_2,a_3,a_4)=(2,1,1,3,4).
\]

The twisted convolution must give

\[
(a_0^{\rm new},a_1^{\rm new},a_2^{\rm new},
  a_3^{\rm new},a_4^{\rm new})
=(0,0,0,0,4).
\]

After residual normalization, the exact X-values are

\[
(0,0,0,0,1).
\]

### Frame 3: collision and characteristic-\(3\) binomial collapse

Let

\[
K=\mathbf F_9=\mathbf F_3[\alpha]/(\alpha^2+1),
\quad e_1=3,\quad f_1=2,\quad
\mu=3,\quad\kappa=2,\quad s=1,\quad z=2.
\]

All displayed values lie in the prime subfield. Here

\[
R(T)=(T-1)^3=T^3-1=2+T^3,
\]

\[
(\lambda_0,\lambda_1,\lambda_2,\lambda_3)
=(1,2,1,1),
\qquad
(a_0,a_1,a_2,a_3)=(2,0,0,1).
\]

The exact X-values are again

\[
(0,0,0,1).
\]

This simultaneously checks a wrap from \(2+2\) modulo \(3\) and the vanishing of \(\binom31,\binom32\) in characteristic \(3\).

### Planted falsifiers

1. **Delete the carry twists in Frame 2.** Ordinary convolution of the raw vector \((2,1,1,3,4)\) gives

   \[
   (5,1,3,0,4).
   \]

   Normalizing afterward gives

   \[
   (5,3,2,0,1)\ne(0,0,0,0,1).
   \]

   The checker must reject this mutation.

2. **Reverse the recentering sign in Frame 1.** Replacing \(w=-\widehat s n(\kappa)\) by \(w=+\widehat s n(\kappa)\) produces \(R(T-s)\), namely

   \[
   (T-4)^3=(T+1)^3
     =1+3T+3T^2+T^3
   \]

   in \(\mathbf F_5[T]\). The checker must obtain \((1,3,3,1)\), not \((0,0,0,1)\).

These frames verify X(ii). A verifier for X(i) cannot be built from the pasted material: it additionally needs executable definitions of the \([ILN]^\dagger\) domain predicate, the scoring predicate, the stage-to-\(B_n\) map, and every (M2) collision/mod-wrap fence. It should fail closed when those predicates are unavailable.

OUTCOME: PARTIAL
