1. **Critical — the new cubic publicity overstates what “density” is machine-checked to mean.** The root blurb, the map headline, the cubic group, and the paper entry all present `genuineDensity_three_exact` as a theorem about the Haar probability of the actual splitting-type event. In the formal source, however, `genuineDensity` is the infimum/limit of the proportions of finite coefficient boxes *consistent with* the type, and `GenuineDensity.lean` explicitly says that identifying this limit with the Haar measure of the type locus is **not formalized**. The Lean theorem proves the five formulas for that outer-box limit. The public draft must not silently identify this with the Haar probability introduced earlier in the exposition.

2. **Critical — “prove the theorem” and “inhabit `CapstoneHypotheses`” are not shown to be the same task.** The stage-6 role paragraph calls them “the same task,” and the paper/map repeatedly call the result a “reduction.” The displayed theorem proves only `(∀ n, CapstoneHypotheses n) → UniformityStatement`; no converse is displayed or reported. This is a sufficient-condition assembly, not an equivalence or an exact reduction.

3. **Critical — the claim that the eleven fields “itemize exactly what remains unproved” is false against the designated project-state ledger.** The formal source itself records `w1` as a named debt with **no field at all**. It also records four `True`-bodied carriers, including `TypeOfFaithful`, whereas the stage-6 field status omits `TypeOfFaithful`. Project state §4 also names the WLE per-level exporter under `deepTwist`; the public open lists mention only deep witnesses. Thus the root blurb’s “itemized exactly,” the capstone node’s “everything still owed,” the paper’s “eleven fields itemize exactly,” and the closing claim that the “exact distance” is formalized all overclaim completeness.

4. **Critical — the placeholder count is internally inconsistent.** The stage says “four of its fields are still placeholder vocabulary (literally `True`)”; its own field explanation then identifies `jd0` and `genhnBox2` as literally `True` plus `windowPinning`, `genhnHE`, and `genhnTow1` as carriers whose bodies are still `True`: five displayed fields. The formal ledger separately lists four `True`-bodied carriers, the fourth being the omitted `TypeOfFaithful`, in addition to the two literal-`True` fields. The paper and amber update repeat “four fields,” conflating direct fields with underlying carrier bodies.

5. **Critical — the draft reports three compiled countermodels, but there were only two.** A-I.6 and A-I.7 were forced by compiled Lean refutations. The draft’s own A-I.8 detail correctly says A-I.8 was a structural/source-faithfulness stop-line, “not a machine refutation.” Therefore “three machine-forced repairs,” “each triggered by a compiled countermodel,” the corpus oneliner “repaired three times ... by compiled countermodels,” and the proved-node title “three compiled countermodels” are false.

6. **The stated `compile-is-the-validator` trichotomy contradicts the campaign account.** Stage 6 and the paper say a signed statement may be proved, refuted by a compiled countermodel, or amended by a statement change “that such a countermodel forces, and nothing else.” A-I.8 is then described as an amendment forced by a non-machine structural faithfulness finding. The advertised rule excludes an amendment the same section celebrates.

7. **The new section is numbered “6” even though the map already has a stage 6.** It appears after existing stages 6 through 12, so the page now runs 1, 2, 3, 4, 5, 6, 7, ..., 12, 6. All new cross-references to “stage 6” are ambiguous: they can mean either “A finite alphabet of patterns” or “The second formalization.” This also breaks the “each numbered stage” reading instructions and makes the stage chronology misleading.

8. **The new stage violates the page’s promise that every result exposes the exact Lean statement.** `CapstoneHypotheses` is expressly a “field skeleton” with `⋯`; `genuineDensity_three_exact`, `theoremA`, `blockFrontier_of_context`, and `s2Mu3_calculus_nonempty` omit section variables/typeclass assumptions needed to read their scopes; the amendment, trusted-base, and depth-4 nodes show repository paths or comments rather than formal statements. This omission is material: for example, the displayed depth-2 theorem contains undefined `h2` and `hq`, and the displayed cubic theorem hides the complete-DVR and finite-residue-field assumptions.

9. **The `proved` badge on “The amendment loop” is dishonest under the page’s own legend.** The node is a historical/process bundle containing two Lean refutations, a third non-machine structural adjudication, and claims about amendment effects. There is no single machine-checked result matching its title, and the title’s “three compiled countermodels” is false. Counting this as one of the six newly “proved” selected results compounds the problem.

10. **The `proved` badge on “The trusted base shrinks” is likewise not a machine-checked theorem matching the node.** One retired axiom has a byte-identical Lean theorem, but the node also asserts allowlist completeness, citation provenance, audit completion, and the fate of two proposed citations. Those are repository/audit facts, not consequences of the displayed Lean kernel statement. The badge and Hensel-group count implicitly certify more than Lean certifies.

11. **The `conditional` badge on `blockFrontier_of_context` uses a different meaning from the legend.** The theorem is not parameterized by a displayed mathematical hypothesis corresponding to the dissection result; it consumes declared literature axioms through its dependency graph. Labeling an axiom-dependent theorem “conditional” while reserving `cited axiom` for other nodes visually lowers its trust cost. At minimum, the badge scheme must distinguish “theorem depending on audited axioms” from a theorem conditional on an explicit argument.

12. **The layout undercounts the new trusted base.** The final tally still says “4 cited axioms,” while stage 6 announces a separate six-axiom allowlist. Those six are hidden inside a green `proved` node and contribute no `cited axiom` badges. A skimming reader therefore sees four cited items, not the honest global picture of four selected legacy cites plus six declared non-core axiom constants in the second library.

13. **“Everything else it has landed prints Lean core” is false as written.** The six cited axioms propagate into downstream theorem footprints; the same stage explicitly says `blockFrontier_of_context` prints Lean core plus the dissection cite. What is supported is that the allowlist contains six non-core declarations and specifically identified results (the cubic theorem, Theorem A, the depth-2 calculus, and the capstone assembly implication) have Lean-core-only footprints.

14. **“Six audited literature citations, each ... with a faithfulness record” is not supported by the supplied project state.** Project state §3 confirms a six-name declared allowlist, but it also says the C.94/`agnprw_termination` faithfulness entry and `CITE_REVIEW_LIST` row are pending authoring. The draft may claim six declared cites; it cannot claim all six audit records are complete without resolving that source-of-truth conflict. “Six citations” is also imprecise: these are six declared axiom constants, including two legs of one dissection result and a composite cite, not necessarily six independent bibliographic citations.

15. **The site-root blurb overstates the ring scope.** It says the cubic theorem holds “uniformly over every complete discretely-valued coefficient ring at once.” The formal theorem assumes a commutative domain, a discrete valuation ring, adic completeness, and a **finite residue field**. Omitting the finite-residue-field restriction changes the quantified class and is a genuine scope overclaim.

16. **The depth-2 calculus is presented as supplying the capstone’s `deepTwist` field when it does not inhabit that field.** The open field requires arising witnesses at depth `r ≥ 4` plus the exporter named in project state §4. A single calculus at the pinned depth-2 S2 tower is a prototype/input toward that campaign, not a partial inhabitant of a universally quantified structure field. “The first live supplier for the field” and “where the first one became a theorem” invite the stronger, incorrect reading.

17. **“The quadratic theorem is now a proved special case inside the same corpus” is mathematically inaccurate.** A degree-2 theorem is not a special case of the degree-3 theorem; the cubic proof consumes separately proved degree-2 values. The intended fact seems to be that the second library also contains those quadratic inputs, but the present wording suggests logical specialization from the cubic result.

18. **The headline “every statement the informal corpus rests on was first signed” overclaims corpus coverage.** The same stage admits that several vocabularies are still literal `True`, while the formal ledger says `w1` has no field at all and some obligations are forbidden to sign until their vocabulary exists. Moreover, 491 of the 1,240 census entries are definitions or hypothesis-carrying interfaces, not proved propositions. “1,240 signed statements” and “every statement” obscure both facts.

19. **“At full strength” and “exact distance” are given dominant headline emphasis despite the later disclaimer that the implication is only presumptively related to the intended final proof.** The conclusion `UniformityStatement` has the intended quantifier order, but the hypothesis carrier still contains vacuous bodies and omits named debts. Calling the assembly “the theorem stated and assembled” before saying “this proves no mathematics” encourages the false skim-level inference that the capstone itself has been assembled/proved.

20. **“Not provably empty” is presented as though it were a certified positive fact.** What the records establish is that two *retired* carrier shapes were refuted and that no current inhabitation is claimed. Absence of a known refutation of the current structure is not a machine-checkable consistency/nonemptiness result. A fresh reader cannot tell whether “not provably” means “Lean proves `¬¬ Nonempty`,” “a model was built,” or merely “the campaign has no refutation.”

21. **The paper’s emphatic pre-existing status section is now stale and directly contradicts the new milestone.** It says “Exact status, nothing more claimed anywhere on this page,” then says degree 3 is only constructed and matched to recorded values. The new post-date line says cubics are unconditional, and the new milestone later calls them proved. A fresh reader meets the obsolete status claim long before the correction in the milestone log.

22. **The paper’s “general uniformity theorem is still not proved — at any grade” is ambiguous against the immediately preceding milestone history.** Earlier entries say the general theorem’s prose corpus is accepted-text-complete at the hostile bar modulo four named sub-boxes. The new sentence can only be reconciled by distinguishing the stronger all-complete-DVR `UniformityStatement` of the second library from the earlier per-prime/prose target, but that scope distinction is not made at the point of the categorical “at any grade” claim.

23. **The draft conflates two different conditional results.** The map’s main theorem paragraph first advertises the legacy per-prime, two-clause regularity-gate theorem and then introduces the second library’s `CapstoneHypotheses` implication; the site-root blurb replaces the former description with the latter without explaining that these are separate certificates with different hypotheses and scopes. “The same uniformity” makes them sound like one continuously strengthened theorem even though the milestone later insists the developments are separate.

24. **The current honest boundary is structurally buried under an obsolete boundary.** The amber panel still opens with many paragraphs asserting that only degree 2 is unconditional and that the Lean boundary did not move, and only its final dated paragraph supplies the August 28 state. This conflicts with the page-top promise that the amber panel is “the exact certification boundary” and makes the current degree-3/capstone status depend on reading through a long historical block.

25. **Several stage-6 explanations presuppose private campaign vocabulary rather than explaining it to a sophisticated new reader.** The capstone group uses “socket,” “live indices,” “same-realization record,” `mp1`, `a0`/`a1`/`a2`, “drainage,” and “owner review row” before giving a mathematical description of the obligations. The engine/calculus groups similarly rely on “dv-graded read,” “normalized twist,” “support line,” “side lift,” “block frontier,” “payoff rows,” “S2 probe tower,” “μ₃ chain,” “corrected triple,” “star fired,” “formal controls,” and the “level-2 twin of Theorem M.” These names may be useful provenance, but they do not presently translate the nodes into standalone mathematics as the reading guide promises.

26. **The new source/provenance claims are not auditable from the public page.** The six-cite node and the campaign nodes give internal paths such as `leanfinal/...`, `runs/wave-c/...`, and `docs/...` without identifying or linking a public repository/root. A fresh public reader cannot inspect the claimed axiom allowlist, faithfulness records, countermodels, or adversarial verdicts, despite those artifacts being used to justify badges and exact historical claims.

27. **The phrase “wild case OPEN” needs general-degree scope at the same point it appears.** It now sits in the same headline paragraph as an unconditional cubic theorem covering wild characteristics 2 and 3. The two facts are compatible only if “wild case” means the conjecture in unrestricted degree, but the unqualified wording reads as a direct contradiction.

28. **The paper post-date phrase “a certified architecture for every degree” remains undefined and is now especially liable to overreading.** In the updated context it sits beside “unconditionally ... cubics” and before the reader learns that the general capstone carrier is uninhabited, contains vacuous bodies, omits named debts, and proves only a one-way implication. It should not serve as a summary badge for the general-degree status without an explicit certificate grade.

FIX-THEN-PUSH — fix findings 1–28; in particular, do not push until the Haar-density scope, one-way capstone status, omitted/placeholder debt ledger, false three-countermodel claim, duplicate stage number, and badge/citation accounting are corrected.
