# verdict_IEC — UNIT IEC: `IfaceExportedCore` DISCHARGED (CEX's R1) — the windows engine landed, the F1 residue scoped to ONE inequality

**Date:** 2026-08-31.  **Status: COMPLETE.**
`leanfinal/Uniformity/ChapI/I10IfaceCore.lean` (NEW, ~900 lines, namespace
`Uniformity.Density.IFC5`) — 0 errors, 0 warnings, 0 `sorry`, all **19** printed
footprints exactly Lean core `{propext, Classical.choice, Quot.sound}`.  Verified
per-file (`timeout 580 lake env lean Uniformity/ChapI/I10IfaceCore.lean`, clean),
per-target (`lake build Uniformity.ChapI.I10IfaceCore` — Build completed successfully,
8976 jobs), aggregator `Uniformity/ChapI.lean` exit 0 after the one import-line edit
(only landed-file edit).  Cite ledger: **EMPTY**.  IFCG54.lean untouched.  No git ops.

## STATUS FIRST

| charge | status | exact result |
|---|---|---|
| 1. SCOPE | **DONE** | per-field engine map below; the corpus's UZL/WFR/CN1/ChapB hull machinery lives on chapter-B/C height types — the `ladderSuppVal` windows engine had to be built fresh (only E13's endpoint facts reused); the arising side pins NOTHING steep-shaped today (BlockViewEq = data fields; CanonicalThresholdAt = the KEY's (WINDOW) law) |
| 2. BUILD + fire | **★ R1 DISCHARGED at every (STEEP) pair, generic in (C, B)** | ★ `he7aPackageExported_of_steep : SteepBlock B → HE7APackageExported C B` (Lean-core, zero sorry); LB1G's genre inhabited (`exists_realizedExport_of_steep`); ★ `ifaceExportedCore_of_arising` (the charge's named target at its honest minimal restriction); supplier level: ★ `ladderPackageExportedSupplier_of_steep` |
| 3. remainder | **NAMED, minimal — and ONE brief correction** | R1′ = `LadderSteepSupplier` (steep at arising pairs — the F1 frontier, now ONE pointwise inequality); R2 unchanged (CEX's full-strength clauses); lb1 = `hpart` + R3, **NOT R3 alone** (see below); R3 = owner re-signs, flagged NOT enacted |

## The discharge (charge 2) — what was built

**`SteepBlock B` (NEW STATEMENT, review):** `∀ j < μ, ((μ−j)·T : ℕ) < B.devHgt j` — the
development heights clear the slope-`T` line through the monic top strictly.  This is
the ONE block-local law the discharge needs.

**The windows engine (fresh, Lean-core):**
* `sideArg/winMin/winMax` — the extremal argmin window reads off `B.devHgt`
  (`mem_sideArg` ↔ E.11's `onLadderSide`; attainment via `Finset.exists_mem_eq_inf`,
  finiteness from `devHgt μ = 0`).
* LOCAL laws (no hull chaining): `winLen_dvd` — the two-point support equation +
  coprimality force `ℓ ∣ (winMax − winMin)`; `side_node_of_steep` — every genuine
  two-abscissa side has `ℓ·T < u`, purely from the right endpoint's argmin property
  against abscissa `μ` + (STEEP) at the left endpoint.
* ★ `peel_step` — at a hull-supported right endpoint `k > 0` (`k = winMin` of some
  pair), a coprime pair exists whose extremal window ends at `k` and starts strictly
  left (internals: ℚ-valued slope minimizer over the finite abscissas left of `k`,
  gcd-reduction, three-way argmin comparison transferred to ℤ).  **Machine-caught
  simplification: the peel needs NO steepness** — descent is forced by the entry
  window's own strictness; first-attempt compile.
* ★ `tiles_of_entry` — strong induction on the right endpoint: the genuine windows of
  finitely many coprime pairs TILE `[0, k]` exactly (widths sum to `k`).  Also
  steep-free; first-attempt compile.
* `winMin_zero_one` — (STEEP)'s first entry point: the flat pair `(0,1)` pins the
  argmin to `{μ}`, starting the peel at the monic top.

**Assembly:** the tiled windows give `sides`/`len`/`hlen_sum = Σ len = μ` — the fields
`IfaceExportedCore` genuinely constrains; `hside_node` from `side_node_of_steep`; the
residual/count fields fill synthetically at CEX's pattern, LEGAL because `winLen_dvd`
gives `ℓ ∣ len` (one linear factor of multiplicity `len/ℓ` satisfies `hresdeg`; `hforce`
saturates since `ℓ ≤ len`).  R2 (full-strength residual clauses) is untouched, exactly
as CEX scoped it.

**Rigidity/redundancy teeth:** `IfaceExportedCore.win_forced` — the core's extremality
clause makes the per-side witness pair UNIQUE (`len` pinned to `winMax − winMin`), so
the relation is rigid, not merely inhabited; `exported_node_forced` — at a (STEEP)
block the export relation REPROVES E.12's `hside_node` on ANY consumer interface;
`exported_len_dvd` — structure for the E.57 funnel's constructor.

**GATE (executed nonvacuity):** a full concrete `SlotCarrier` over `ℚ` (constant height
1, digit = constant coefficient — all ten carrier laws discharged) with the block
`Φ = X`, `F = X² + 1`, `μ = 2`, `T = 0` is proved (STEEP), and
`HE7APackageExported gateSlot gateBlock` FIRES via the discharge — the premise is not
provably empty, and the peel's `⊤`-branch is exercised (`A 1 = 0` interior).

## The remainder (charge 3), and the brief's lb1 claim corrected

1. **R1′ = `LadderSteepSupplier n` (OPEN, the honest remainder):** every arising
   degree-`n` configuration's block is (STEEP).  This is exactly the F1 `BlockFrontier`
   frontier (`BLOCKERS_PLAN_2026-08-26.md`, "genuinely new mathematics") — but scoped by
   this unit from "build the whole 1,500–3,000-line exporter" down to ONE pointwise
   height inequality per block.  Nothing landed pins it: `BlockViewEq` (C130vw) pins
   only the block's data fields; `CanonicalThresholdAt` (C130in) is the (WINDOW) law
   about the KEY's height at the point, not the development coefficients'.  In
   `ifaceExportedCore_of_arising` the config binder is deliberately `_hcfg`-unconsumed —
   machine-visible honesty, mirroring CEX's disclosure pattern.
2. **R2** (unchanged): the full-strength export clauses (residuals through `C.dig`,
   pinned counts, rank carrier).
3. ⚠ **lb1 — PROMINENT correction of the unit brief.**  The brief said the lb1 leg
   "would then rest on the owner re-sign (R3) alone."  FALSE as stated: firing CEX's
   `lb1CarrierExported_of_partitionLegs` requires `hpart` — partition legs at ALL
   core-exported triggering interfaces — and `PartitionLeg I` (E57p) demands a genuine
   coprime FACTORIZATION of `B.F` per residual class (HE7.96(a), the C.63/C.69
   C-side content).  The polygon engine cannot produce factorizations.  Honest post-IEC
   boundary: **package leg = R1′ + R3 alone** (that part of the brief's claim is now
   true); **lb1 leg = `hpart` (E.57 funnel) + R3**.  Joint wiring pin landed:
   `exportedTwins_of_steep_partitionLegs` (steep + hpart ⟹ both exported twins).  The
   forced-window/dvd teeth are the structure `hpart`'s eventual constructor consumes.
4. **R3 (owner, flagged NOT enacted):** re-sign the capstone's `package` leg to
   `LadderPackageExportedSupplier` (typed here) and `lb1` to the exported twin.

## Files

* `leanfinal/Uniformity/ChapI/I10IfaceCore.lean` — NEW (the unit's landing).
* `leanfinal/Uniformity/ChapI.lean` — one aggregator import line added.
* New statements flagged for standing review: `SteepBlock`, `LadderSteepSupplier`,
  `LadderPackageExportedSupplier` (+ definitional engine reads `sideVal`/`sideArg`/
  `winMin`/`winMax`, gate artifacts `gateSlot`/`gateBlock`).  No signed name touched.
