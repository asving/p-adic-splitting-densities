# Verdict — UNIT CHA: the unattacked CapstoneHypotheses fields (2026-08-28)

## Re-grounding

The capstone `UniformityStatement` reduces to inhabiting `CapstoneHypotheses n`
(`leanfinal/Uniformity/ChapI/I10_I15_I18.lean`, post-A-I.6/A-I.7).  The ladder/deepTwist
legs are under campaign elsewhere; CHA's charge was the REST: `a0`/`a1` at `n ≥ 3`,
`jd0`/`genhnBox2` + the `w1` debt, and the four I-D12 `True` bodies
(`WindowPinningAt`/`GenhnHEAt`/`GenhnTow1At`/`TypeOfFaithful`).  Per field: re-base the
2026-08-26 supply-map row against today's corpus, land what's reachable in a new
`IFC6.lean`, decompose the rest.

## What landed

`leanfinal/Uniformity/ChapI/IFC6.lean` — NEW file, namespace `Uniformity.Density.IFC6`.
Verified: `timeout 580 lake env lean Uniformity/ChapI/IFC6.lean` exit 0, ZERO
errors/warnings/`sorry`; all six AxCheck-footer footprints Lean-core
(`mu3_stage_sigma_decided` is even `Classical.choice`-free).  Nothing imported by any
landed file; no landed/leanspec file touched.  Supply map extended with the dated
`[CHA 2026-08-28]` section (full per-field re-base).

★ **THE HEADLINE: the `a1` field is CLOSED at every degree** —
`IFC6.menuLawAt_all (n : ℕ) : MenuLawAt n`, Lean-core, signed statement byte-unchanged.
The map priced this as the multi-node MENU-FAMILY campaign; both halves were already
landed, unconnected: CN-22's covering-menu construction
(`Density/CoveringMenus.lean`, `exists_coveringMenu`) builds its menu from `n` ALONE (the
`O`-dependence of its statement is shape, not content) — `IFC6.exists_uniform_coveringMenu`
re-derives it with `S` hoisted out of the `∀ O` binder and filtered to exact degree `n`
(the filter leg is `typeOf_degree`); and the A1-family half is free at every `n` via
I.03's own gate-(6) `r = 0` label cells (`IFC6.menuLawAt_of_uniformMenu`, generic in `S`).
R8 respected: the menu stays existentially bound, never named.

⚠ REVIEW FLAG (standing queue, CHA row): I.03's audit item (3) believed `MenuLawAt` "is
not provable in general" — that belief is REFUTED with the statement unchanged (the
H0LEG/A-I.4 genre: a carrier's content shown derivable).  Sound and disclosed-by-design:
the A-I.1 carrier states menu + shape + labels ONLY (its own ⚠ SIGNATURE NOTE); the
weight-correctness tie stays at HYP.23/32/35, untouched.  Consequence: `a1` imposes NO
residual obligation on `CapstoneHypotheses n` at any `n`; MENU-FAMILY retires.

## Per-field table

| field | status | exact citation / remainder |
|---|---|---|
| `a1 : MenuLawAt n` | **SUPPLIED (all `n`)** | `IFC6.menuLawAt_all`; suppliers `IFC6.exists_uniform_coveringMenu` + `IFC6.menuLawAt_of_uniformMenu`; consumes CN-22 (`efPair_pos_of_mem`, `efPair_mul_le_natDegree`, `typeOf_degree`) + I.03's label-cell pattern.  Review flag above. |
| `a0 : DecidedSliceAt 3` | **DECOMPOSED + reduction socket landed** | `IFC6.decidedSliceAt_three_of_values : CubicValueLaw c3split → … → CubicValueLaw c3ram → DecidedSliceAt 3` (Lean-core).  Every structural link is landed — enumeration `G53.typeOf_three_cases`, off-menu zero `G54.genuineDensity_three_eq_zero`, tie `H97b.genuineDensity_three_eq_decidedDensity` (new since the map) — so **A0-CUBIC's remainder = exactly the five `IFC6.CubicValueLaw` instances** (the HM3.D rationals, today only G61's honesty-ridered transcription + G77's `q=2` literals; the `≥` halves exist at `G61.lowers_three`, nowhere near sharp).  Counting campaign, multi-node. |
| `a0` at `n ≥ 4` | **DECOMPOSED (unchanged)** | The main exact-count program; no `c4*` types / `typeOf_four_cases` exist.  Hensel engine irrelevant: C133mh*/C134* have ZERO density-layer vocabulary (grep-verified). |
| `jd0 : True` / `genhnBox2` / `w1` debt | **DECOMPOSED (unchanged)** | A-I.1 ruling stands (`leanspec/ChapI.lean:339,443-457`); `IFC0.ArisingWeldSiteSupply` has ZERO consumers; `ValueDictSite`/`CarrySite` occur tree-wide in parameter position only.  Blocker = WELD-SITE-SUPPLY node 1, the SITE EXPORTER (weld analogue of CC-12 views) from realized stage/chain objects; design-review-first.  No landing possible; socket already exists. |
| `windowPinning : WindowPinningAt n` | **DECOMPOSED, row CORRECTED** | WIN0 carrier stands, `Arises` still free.  Corrections: H89w's consulted-height arithmetic is GENERAL-`e₁` (map said `e₁=2`-only — that fence is H.89's per-genre ledger); the reader rule is a STANDING chapter-H decision not to formalize (§16 item 3), so resolution starts with reversing a recorded decision (owner-adjacent), then the canonical `Arises`, then the pinning proof. |
| `genhnHE : GenhnHEAt n` (`n ≥ 6`) | **PARTIAL (μ=3 slice packaged)** | NEW `IFC6.mu3_stage_sigma_decided`: every positive mass-3 stage type transports at every genre to one of the five `StageLeaf3` dictionary values (repackages `H73w.stageType3_complete` through `stageSigma3`).  Remainder: (a) the STAGE-LEAF BRIDGE — nothing ties an actual polynomial stage to a `StageLeaf3` (only `StageLeaf` consumer is `H09w.stageIfaceF` → `StageInterface`); (b) `μ ≥ 4` dictionary (HYP.143).  No amendment draftable until (a). |
| `genhnTow1 : GenhnTow1At n` (`n ≥ 8`) | **DECOMPOSED (unchanged), next node named** | C93 in leanfinal is still `tower_first_live`(+sharp) only; the census block `C93Census` lives ONLY in signed leanspec ChapC.  Node 1 = transcribe it to leanfinal (30–60 min class); then the census-conjunction body; then the `n ≥ 8` family (HYP.67–71/78–80/142/143/144-BOX-2), research-open. |
| `TypeOfFaithful` (I.21; NOT a field) | **DECOMPOSED (unchanged)** | Structural: the signed `disc ≠ 0` locus needs the B.56 factor-field valuation-ring carrier; at `d ≥ 2` leaves `AdjoinRoot g` is never a DVR, so `typeOf_eq_classical_of_isDiscreteValuationRing`/`typeOf_inert_faithful` cannot reach it.  Zero `typeOf` content in the new wave (C61 `tier1_typeOf` = frame dictionary, not faithfulness).  Terminal fate: discharge node (B.56 carrier, multi-node) or gate-(b) cite — note `docs/references/FGMN_residual_ideals_2015_authorpdf.pdf` just arrived as the natural cite source.  Human audit A-1 must precede any consumer. |

## Amendment drafts

NONE warranted: no I-D12 body's real statement is supplied by landed material (window:
guard open; HE: bridge absent; Tow1: census untranscribed; TypeOf: structural).  `a1`
needs no amendment at all — the field already binds `MenuLawAt n` and `menuLawAt_all`
supplies it directly at any future construction site.

## Stale-text notices (comment-level, owner/orchestrator pass — NOT touched by CHA)

* `I10_I15_I18.lean:49` "`a0`/`a1` are open at `n ≥ 3`" — stale twice (`menuLawAt_three`
  landed 2026-08-26; `a1` now closed at all `n`).
* I.03 audit item (3) "not provable in general" — refuted (review flag above).
* Supply-map DAG line "Chapter G menu + A1 label family ──> a1" — retired by the
  `[CHA 2026-08-28]` section.

## Verification record

* `cd leanfinal && timeout 580 ~/.elan/bin/lake env lean Uniformity/ChapI/IFC6.lean` —
  exit 0, no output except the six `#print axioms` lines, all
  `[propext, Classical.choice, Quot.sound]` or smaller.
* IFC6 is imported by nothing; AxChk baseline and all landed footprints untouched.
* Corpus negatives grep-verified (no density vocabulary in C133mh*/C134*; no site-type
  constructors; no canonical `Arises`; no `StageLeaf4`; no `C93Census` in leanfinal).
