# UNIT RIS — IN PROGRESS (scoping COMPLETE, route DECIDED; skeleton next) 2026-08-30

**Charge:** discharge `ResidualInertiaSupply` (IFCG35 §1, the ONE open leaf leg = B-BOX-1
at the x-frame) in `leanfinal/Uniformity/ChapI/IFCG37.lean`; wire the leaf laws at every
d + ER4's type22/type41 premises; recompute the endgame set.

## The exact target (verbatim, IFCG35.lean:149–158)

```lean
def ResidualInertiaSupply : Prop :=
  ∀ (O : Type) [CommRing O] [IsDomain O] [IsDiscreteValuationRing O]
    [IsAdicComplete (maximalIdeal O) O] [Finite (ResidueField O)],
  ∀ π : O, Irreducible π →
  ∀ u ℓ : ℕ, 0 < u → 0 < ℓ → Nat.Coprime u ℓ →
  ∀ g : Polynomial O, g.Monic → IsPure X g u ℓ →
  ∀ (hne : (sideSet X g u ℓ).Nonempty) (H₀ : ℕ),
    npHgt X g (sideMin X g u ℓ hne) = (H₀ : ℕ∞) →
    Irreducible (resPoly π X g u ℓ hne H₀) →
    ∀ g' ∈ monicFactors g, sideDeg X g u ℓ hne ∣ inertiaDegOf g'
```

`inertiaDegOf = sSup (normDivisors)` (gcd of norm valuations, Density/TypeOf.lean), so the
content is: d divides EVERY norm valuation of every monic factor.

## Mechanism candidates — RESOLVED

- **(c) B.63 / BBox1CiteStatement: DEAD as a discharge.** `BBox1CiteStatement`
  (ChapB/BBox1CiteStatement.lean) is the general-φ form of the supply but is *stated, not
  asserted* (unsigned cite proposal, [FGMN] 6.6/[GN15] 2.3). B.63's
  `typeOf_of_separable_residuals` CONSUMES the box as its `hperim` hypothesis — it cannot
  supply it. B.52 (`key_natDegree_dvd_inertiaDegOf`, PROVED Lean-core) gives only the KEY
  degree (=1 at the x-frame). So RIS is a genuine proof, not a transcription.
- **(a) IFCG32 norm route: LIVE, decided.** `normValues_descent` + the packaged gcd trick
  `dvd_inertiaDegOf_of_mem_normDivisors` give `d ∣ inertiaDegOf G` from ONE hypothesis:
  `hGmap : G.map (algebraMap O (AdjoinRoot ψ̃)) = tauProd hψ̃ C` (C monic, deg > 0) — G
  base-changes to the full conjugate orbit of one factor at the UNRAMIFIED key ψ̃ := a
  monic lift of the irreducible residual ψ (deg d). Only LEG A needed (no ascent/CRT).
- (b) ramified adjoin: not needed.

## THE PROOF PLAN (all interface reads done; lemma names verified in-repo)

**Step I (reduction to g irreducible).** New lemma `irreducible_of_pure_of_resPoly_irreducible`:
monic + IsPure + resPoly irreducible ⟹ Irreducible g. Any monic split g = a·b (monic WLOG
via unit leading coeffs) is pure on both sides with side data additive (B.57
`isPure_of_monic_factor`), so `resPoly_mul_of_pure` (B.35c, signed contract, landed) splits
the irreducible residual into two positive-degree factors — contradiction; degree-0 side
forces the factor = 1. Hence `monicFactors g = {g}` (`monicFactors_eq`) and the goal is
`d ∣ inertiaDegOf g`. (Same lemma reused over O' at linear residuals in Step IV.)

**Step II (transport O → O' := AdjoinRoot ψ̃).** Everything is generated by `dev` (iterated
%ₘ//ₘ — commutes with any ring map at the canonical key X), `gaussVal` (coeff addVals —
preserved by any hom h with Irreducible (h π): IFCG32's `addVal_map_of_irreducible`,
public), `digAt` (choice digit — transports along `IsLocalRing.ResidueField.map h` once
IsLocalHom h is derived from addVal preservation). One general transport section (h : O →+*
O'', Irreducible (h π)) gives: npHgt/suppVal/OnSide/sideSet/sideMin/sideDeg/IsPure equal,
and the digit polynomial R (B63's `resPoly_eq_map_of_natDegree_one` idiom, private — copied
at φ = X) maps by ε := ResidueField.map h. Serves BOTH h = algebraMap O O' and h = tau_β.

**Step III (the split residual).** R := digit polynomial of resPoly over ResidueField O
(monic — B63's hRmonic derivation; irreducible via `irreducible_map_algebraMap_iff` copy);
ψ̃ := monic coefficientwise lift, IsKey (reduction = R irreducible), natDegree d.
`keyRoots hψ̃` (IFCG29): card d, nodup, in ResidueField O'; R.map ε = ψ̃-residual splits as
∏_{β ∈ keyRoots.toFinset}(X − C β) (mathlib
`prod_multiset_X_sub_C_of_monic_of_roots_card_eq`).

**Step IV (dissection + orbit).** Copy B63's private `exists_pinned_dissection` (public
ingredients: B41b `exists_graded_factorization`, B39b, B.30/B.35 pins) — over O', dissect
g.map along the d linear residual factors: g.map = ∏_β G_β, each monic pure deg ℓ,
resPoly = (y−ιβ). Each G_β and each C.map(tau_β) irreducible by Step I (linear residual =
irreducible; tau-equivariance of resPoly from Step II at h = tau_β + `resTau_apply_of_mem`).
C := G_{β₀} (β₀ = residue of adjoined root, `residue_root_mem_keyRoots`). Matching:
C.map(tau_β) is a monic irreducible divisor of ∏ G_γ (via `map_tau_map_algebraMap`) ⟹
equals some G_γ (prime-divides-product + monic-associates-eq); residuals pin γ = β. Hence
`tauProd hψ̃ C = ∏ G_β = g.map` — hGmap.

**Step V (fire).** Instances on O' via B53b `keyIsDomain` + IFCG27's three (exact letI
cascade of IFCG32's `inertiaScaling_all`). `normValues_descent` ⟹ d ∣ every norm value ⟹
`dvd_inertiaDegOf_of_mem_normDivisors` ⟹ d ∣ inertiaDegOf g. (0 < d from hirr: degree-0
resPoly is a unit or 0, both non-irreducible.)

**Step VI (wiring).** `residualInertiaSupply : ResidualInertiaSupply`; leaf laws ∀ e' d via
IFCG35's `residualLeafLaw_of_supply`; ER4: `E1IrrLeafDecision` (IFCG34:941) fired (it is
the (2,2) instance — check its exact shape vs the supply; `e1IrrLeafDecision_of_no41` or
direct); type22/type41 `coneRemainderCount_four_*_of_leaf` premises discharged; endgame
recompute per IFCG35 §"surviving open set" (items 1–3, 5 remain; item 4 = RIS CLOSED).

## Honest risk register

- Transport section is the new-code mass (~300–400 lines); choice-based `digAt` needs the
  cancel-the-witness idiom; `dvd ↔ addVal ≥ k` bridge needed.
- resPoly comparisons need H₀ pins aligned (heights pinned to u·sideDeg by
  `height_eq_of_pure`, as in B63's block proof — patterns cribbable verbatim).
- ER4's E1IrrLeafDecision statement not yet read in full — integration test pending.
- Fallback (sanctioned): honest partial = land Steps I–V for g itself
  (`dvd_inertiaDegOf_of_pure_irr_resPoly`) + exact named remainder for any unlanded leg.

## Status (updated after the core landed, 2026-08-30)

- [x] Target statement pinned (verbatim above)
- [x] Route decided: IFCG32 LEG-A descent at the unramified residual key; B.63 ruled out
- [x] All interface reads done
- [x] Skeleton compiled first-pass (all statements elaborate; supply-from-core wiring green)
- [x] ★★★ THE CORE LANDED, ZERO SORRIES, LEAN CORE ONLY:
      `IFCG37.sideDeg_dvd_inertiaDegOf` (B-BOX-1 at the x-frame, PROVED),
      `IFCG37.residualInertiaSupply : IFCG35.ResidualInertiaSupply`,
      `IFCG37.residualLeafLaw_all : ∀ e' d, 0 < e' → 0 < d → ResidualLeafLaw e' d`
      — all three `#print axioms` = {propext, Classical.choice, Quot.sound}; the B.42
      cite does NOT enter this chain. Aggregator import added (after IFCG36's line;
      IFCG36.lean untouched).
- [ ] ER4 wiring: E1IrrLeafDecision from the (2,2) leaf law + type22/type41 rows
- [ ] Endgame recompute + final verdict rewrite
