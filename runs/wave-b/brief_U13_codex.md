# Unit U13 (codex HIGH) — the S2SourceFrontier inhabitation plan (the funnel campaign)

You are a design unit in the p-adic-splitting-densities campaign (a Fable predecessor died
silently with no output — you own this). Repo root:
/data/users/asvin/math-and-lean/p-adic-splitting-densities. Lean project: leanfinal/
(mathlib v4.31.0, built; `lake env lean <file>` from leanfinal/; NEVER bare `lake build`).

## Why this is the funnel
The 2026-08-24 campaign reduced the chapter-I socket path AND the FGMNCalculus class
discharge to ONE named bundle: `S2SourceFrontier`
(leanfinal/Uniformity/ChapC/C130s17.lean — read its exact fields). Conditional
constructors already prove: S2SourceFrontier → full ChainRealization →
Nonempty (FGMNCalculus (2,1,5)) → RealizedInput → both I.10 socket applications
(C130s18.lean). Inhabiting it also bears on the GENTOW2 HOLDs (OPEN-DICT-2/4;
C.100's OPEN-EVAL-ISO and C.102's OPEN-LETTERS are adjacent).

## Read (all)
1. docs/PROJECT_STATE.md; runs/wave-b/verdict_CC17.md (the FILLED/BLOCKED field table);
   C130s17.lean's `S2SourceFrontier` definition.
2. docs/in-progress/LEVEL_GENERAL_DESIGN_2026-08-24.md (U12) — §7 heavy-math cost, the
   `CompletedRootAmbient`/`MacLaneChainSource` signatures (scratch/U12_check.lean), and
   the false-dictionary fence (CC's L/node points are NOT K̄_v/θ).
3. docs/in-progress/CHAIN_CARRIER_DESIGN_2026-08-24.md §4.3/§9 (FGMNSourceData/Laws);
   FGMNCALCULUS_FIELDLIST_2026-08-24.md §8 (OPEN-DICT-1..4);
   FGMN_ADJUDICATION_2026-08-24.md (U9's dictionary).
4. What mathlib already has: search seriously (rg in the mathlib cache under
   leanfinal/.lake + `lake env lean` probes) for Gauss/extension valuations on
   polynomial rings, `Valuation` machinery, `AdjoinRoot` valuations, Newton polygons.
   The S2 case is CONCRETE (depth 2, small parameters) — a concrete construction may
   bypass general MacLane theory entirely.
5. The FGMN paper §§1–4 (docs/references/FGMN_residual_ideals_2015_authorpdf.pdf) —
   what the source objects are (inductive valuations μ₀,μ₁,μ₂; residual operators; letters).

## The design question (end-to-end criterion)
Is `S2SourceFrontier` inhabitable by a CONCRETE depth-2 construction — explicit μ-values
as functions on developments over the S2 tower, explicit residual operators via the
landed dev/twistRead machinery, verified by computation — WITHOUT general MacLane theory?
If yes: the node plan is that construction (each bundle field = 1–2 nodes, 30–60 min,
named landed inputs). If genuinely no for some field (e.g. a ∀-polynomial law needing an
inductive argument): name the field, the inductive core, and size it honestly.

## Deliverables
1. docs/in-progress/S2_SOURCE_PLAN_2026-08-24.md — decided approach + per-field analysis
   + node table (U11 §10 style). Write it INCREMENTALLY (section by section to disk).
2. leanfinal/scratch/U13_probe.lean — elaboration/computation probes backing the
   load-bearing feasibility claims (no sorry, no axiom; exit 0).
3. runs/wave-b/verdict_U13.md — summary.
Do NOT edit leanspec/, leanfinal/Uniformity/, roll-ups; do NOT git commit.
