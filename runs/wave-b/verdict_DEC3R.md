# DEC3R verdict — μ₃ / Route B

**VERDICT: SUPPLY.**  The requested campaign blueprint and a green machine probe are landed as
the three permitted deliverables.  The μ₃ construction is scoped to 29 nodes (each 30–60
minutes), with every node marked `REUSE-WITH-NUMERALS` or `GENUINELY-NEW` and wired directly to
the already-landed `fgmn_model_calculus_nonempty` endpoint
(`leanfinal/Uniformity/ChapC/C130pk.lean:253-258`).  No new packaging is proposed.

## Decided

- The correct target is
  `FGMNSourceData (s2DepthTwo h2 hq) (s2DepthTwoKeyChain h2 hq) 2 1 21`.
  The witness stores `(e₃,f₃,u₃)=(2,1,21)`
  (`leanfinal/Uniformity/ChapC/C97.lean:321-345`), and the probe checks `20<21`, rejects `20<5`,
  and computes the next degree `8`.
- OPEN-DICT-2 is decided in favor of full-current clearing `e(μ₃)α`.  FGMN Definition 3.12
  uses `e(μ_i)α`, so preceding-group membership becomes `e_i`-divisibility.  At S2 this is
  evenness; consequently `Coprime 21 2` and `¬PrevGrade 21` hold simultaneously.  The current
  consumer premise `hprev : PrevGrade u'` is therefore source-faithfully impossible.
- The narrow repair is `hprev : 2 ≤ f' → PrevGrade u'` (or an equivalent `f'=1` base theorem).
  The probe proves the literal ratio identity for `f'=1` using cancellation and no graded
  multiplication.  The consumer audit covers C.99, both C.89 paths, and confirms that the
  genuine source law `Rgr_pow` is unchanged
  (`leanfinal/Uniformity/ChapC/C99r.lean:118-170`;
  `leanfinal/Uniformity/ChapC/C107ac14.lean:110-213`;
  `leanfinal/Uniformity/ChapC/C130la.lean:217-247`).
- A second, independent blocker was found.  The current `chainNorm 2 21` equals `keyAt 2`, so
  its strict degree premise is false.  The source uses `n̂₂`; the landed normalizer definition
  identifies this with `towerNorm 1` (`leanfinal/Uniformity/ChapC/C83.lean:182-187`).  The probe
  computes its S2 realization as `16·keyAt 1`, of degree `2<4`.  The campaign therefore includes
  a coordinated `chainNormBelow`/`thetaRatio`/consumer re-index amendment, not a weakened degree
  claim.

## Supplied artifacts and remaining boundary

`leanfinal/scratch/DEC3_probe.lean` elaborates with
`lake env lean scratch/DEC3_probe.lean` from `leanfinal/` (exit 0).  It contains no `sorry` or
axiom declaration and machine-checks all load-bearing numerals, clearing arithmetic, both
normalizer computations, the base ratio proof, the raw/class normalizer signature match, and
the literal Route-B packaging signature.

The only blocking open inside the 29-node construction is `OPEN-RP1-TRANSPORT`: before defining
the μ₃ graded coefficient, the implementation must prove that its landed μ₂ read realizes the
epsilon-normalized coefficient printed in FGMN Definitions 3.10 and 3.12–3.13 and Theorem 4.1.
The broader `OPEN-DICT-3-MU3` field-transport theorem remains a faithfulness boundary, not a
reason to add packaging.  `OPEN-LETTERS`, C.90's fixed-triple truncation family, and C.103's
explicit `c`/`hliftdeg`/`hadm` supplies are recorded but are not dependencies of the depth-2
`gentow5w_two` endpoint.

Accordingly, **SUPPLY** means the blueprint is ready for implementation, not that the μ₃ model
or GENTOW conclusion is already landed.  The current corpus cannot fire that conclusion until
the two signed consumer amendments and the 27 model-construction nodes are discharged.
