# WELD-FACE-AUDIT — census row S-4, WIDENED (COR-41) — 2026-08-13

**Unit.** `WELD-FACE-AUDIT` = census `docs/TIGHTNESS_CENSUS_2026-08-11.md` §2
row **S-4** (`HYP.73`), the corpus-wide face-citation audit named at
SIMPLIFICATION_BLUEPRINT Plan revision R.4/F23–F36 and declared NOT-attempted
by `SG2_PROBE_2026-08-10.md`. **Charter width:** the four census-assigned rows
`HYP.74, 75, 76, 77` PLUS the widened set `HYP.21, 22, 67, 68, 69, 71`
(orchestrator default per the ledger's COR-41 / 0d owner question 2).
**Mode:** AUDIT ONLY — no note is edited, no grade is upgraded or downgraded,
no repair is attempted. Every verdict below is sourced to a corpus quote.

**Method.** (1) Fixed the capstone path as the census's own cone:
W-12.D ∘ GENIND.B ∘ GENH4 ∘ GENHN ∘ HEX3 ∘ HE6 ∘ HE7 ∘ HE3 ∘ HE6R1 ∘ HETOW ∘
GENTOW1–6, plus W11/W12/HMENU3 upstream (20 note files). (2) Grepped all 20
for every weld-layer label — note names (`GRTW2|GRTJA|GRTJB|GRTJC|WELDMASTER|
SIGMALAW|LIFTCORNER`), face labels (`W-6..W-9|JC-LOAD|JC-PSIKER|WM-COB|WELD-M|
[ILN]|JB-VTX|JB-DEV|JA-GRID|JA-RES`), the value-layer fences (`VD-m|(C-coll)|
(R-coll)|PERIM-μ|DMULT-s|DMULT-w`), `W2-OPEN-1/-2|W2-BOX-1`, `[W12-H]`,
`J-D1`, `J-D0`. (3) Read every hit in context plus every cone note's own
CONDITIONALITY STACK / dependency-ledger section and "One line" summary.
(4) Cross-read `JD0_PROOF` (all 673 lines), `W12_PROOF` §S4.2/§S5,
`GRTJC_PROOF` §5 + JC-BOX-3, `GRTW2_PROOF` §5.3, `SG2_PROBE_2026-08-10`,
`T4_CERTIFICATE_WITNESS_2026-08-12` §S7, `T1_GAUGE_COCYCLE_2026-08-12` (C7).

---

## VERDICT UP FRONT

**The capstone path consumes exactly TWO weld faces, and only ONE of them sits
inside a count law.** They are (a) **LEMMA J-D0** (count gauge-blindness),
consumed FRAME-grade at seven cone notes with each site supplying its own
type-preserving bijection inline; and (b) **COROLLARY W-9** (GRTJC's twisted
group algebra), consumed at **LEMMA GENHN-4 layer 1 at μ ≥ 3** — GENHN's own
words: *"the ONE weld face inside a count law, disclosed (GENHN-BOX-2)"*
(`GENHN_PROOF_2026-08-08.md:1512–1513`).

**Of the ten chartered rows: 1 REACHABLE-AND-CONSUMED, 0 CORROBORATIVE-ONLY,
9 NOT-REACHED.** The entire J-D1 value package — `(VD-m)`, `(C-coll)`,
`(R-coll)`, `PERIM-μ`, `DMULT-S`, `W2-VALUE` — and all four `JB-NOLEG` rows are
consumed by NOTHING on the capstone path. Their only consumption site is
`[W12-H]`, the **route W-12 priced but did not take**: the corpus's actual
order-≥2 layer is GENIND's `(CS-1..3)` grammar discharged by GENH4/GENHN,
which re-prove the needed content elementarily.

**The charter's stated premise about the preview is REFUTED.** The brief says
"currently NONE of the weld rows is in the preview". In fact `spec/HYPOTHESIS_LEDGER.md`
Display A (L1113–1137) carries **six of the ten** as explicit conjuncts:

> `∧ JB-NOLEG(rows 4,5,12,22)†`
> `∧ [H-c]† = JD1ValuePackage[ VD-m, C-coll, R-coll, PERIM-μ, DMULT-S†, JD0-BOX-2†, W2-VALUE† ]`

The preview therefore has the weld carry **exactly inverted**: it carries the
unreachable value-layer package and **omits the one face that is actually
consumed** (W-9 / GENHN-BOX-2) together with that face's own live residue
(`HYP.139`, the W-1 transport at ATTEMPT grade).

---

## §1. WHAT THE COUNT SPINE ACTUALLY CITES (the reachability substrate)

Weld-note citation counts across the 20 cone notes (note-name labels):
GENH4 4 · GENHN 10 · GENIND 7 · GENTOW2 3 · HE3 2 · HEX3 1 · W11 4 · W12 11 ·
**GENTOW1/3/4/5/6, GENTOW5_WI, GENTOW6_BOXES, HE6, HE6R1, HE7, HETOW, HMENU3: 0.**

Every one of those hits was read. The notes' own dependency ledgers settle the
question in their own vocabulary:

| Note | Its own declaration (quoted) |
|---|---|
| **W11** | *"J-D0 is corroborating at the instance, load-bearing only for the general-program framing (S4.3 inherits its BOX-2 gauge-form perimeter)"* (L602–603); weld notes *"cited for the TEMPLATE faces only … S2's n = 2 proofs are self-contained elementary arithmetic + Hensel"* (L605–608); *"JB row-12 u ≥ 0 proviso (GRTJB @ 83f47f4): **not consumed**"* (L391); *"DULEMMA / (DMULT-s) … **NOT consumed anywhere above**"* (L383–384) |
| **W12** | *"The six ACCEPTED weld notes … consumed **ONLY** in §S5's composition display and as [W12-H]'s named inputs (S4.2); **NO S2/S3 proof depends on them**"* (L1506–1509) |
| **HMENU3** | *"**NOT consumed:** the weld notes ([W12-H] untouched — all n = 3 reads are order-1)"* (L728); *"**No axiom, no weld face**"* (L734) |
| **HEX3** | *"**NOT consumed:** the weld notes ([W12-H] untouched …)"* (L727); *"**No axiom, no weld face, no oracle**"* (L734) |
| **GENIND** | *"**GRTJC W-6..W-9** … consumed **ONLY in S7's display** … **No count law of S2–S6 consumes them**"* (L1520–1522); *"WELDMASTER / DULEMMA / [W12-H] … **named in S7.1's (CS-1..3) pricing only**"* (L1523–1524) |
| **GENH4** | *"every count-load-bearing input is RE-PROVED elementarily at this stage … so **no weld face sits inside any count law below**"* (L53–58); *"**No axiom, no weld face inside any count law**"* (L951–952) |
| **GENHN** | *"the slot geometry and no-cancellation are RE-PROVED elementarily below (GENHN-2 …), so **no weld face sits inside the count laws**; W-9's cocycle IS the carry bookkeeping of GENHN-4, where the graded-algebra frame is consumed as the organizing computation"* (L96–101); ledger: *"consumed at LEMMA GENHN-4 layer 1 (μ ≥ 3) — **the ONE weld face inside a count law**, disclosed (GENHN-BOX-2); elsewhere frame-only"* (L1512–1514) |
| **HE3** (σ spine) | *"**GENHN.A(iv)/LEMMA GENHN-4 is NOT consumed**, hence **the W-9 graded weld face (GENHN-BOX-2) is NOT inherited** … (c) SIGMALAW, JC-LOAD, DULEMMA: not consumed"* (L78–88); *"the W-9 weld face (GENHN-BOX-2) is **absent from the σ chain** — it survives only inside GENHN's COUNT laws"* (L1293–1294) |
| **HE6 / HE7** | *"GENHN-4 is not consumed, so the W-9 …"* (HE6 L68); *"(d) GENHN-4 (refine transfer), hence no W-9 …"* (HE7 L77) |
| **GENTOW1** | the ONE tower hit: *"at general mu2 layer 1 consumes the composed graded frame **exactly as GENHN-4 layer 1 consumed W-9's** — GENHN-BOX-2's disclosure pattern one level up"* (L376–378) |
| **GENTOW5** | RETIRES that: *"the frame needed is not an abstract twisted algebra at all — it is gr of the leaf FIELD … **no W-9-style weld face is consumed**"* (L152–157); *"GENTOW-BOX-1 is RETIRED as a mathematical box … **no W-9-analogue weld face**"* (L1308–1311) |
| **GENTOW2** | 3 hits, all FGMN cite-set bookkeeping (L109, L423, L835) — no weld consumption |

**Consequence for the widened rows.** `VD-m`, `(C-coll)`, `(R-coll)`,
`PERIM-μ`, `W2-OPEN-1/-2`, `W2-BOX-1` appear in **zero** cone notes. The
corpus-wide grep for those labels returns only the weld notes themselves,
their pass reports, `W10_PROOF` (out of cone, census O-11), `OPEN2ATTACK`,
`sg2_probe_brief`, and **W12** — where they occur exactly twice: once in the
W-12.A display saying they are ABSENT (*"with NO value-layer hypothesis
((VD-m), (C-coll), (R-coll), (DMULT-s), PERIM-μ all absent, J-D0 frame)"*,
L218–220) and once as `[H-c]`'s content in §S4.2/§S5.

---

## §2. PER-ROW VERDICT TABLE

Legend — verdict: **R&C** = REACHABLE-AND-CONSUMED · **CORR** =
CORROBORATIVE-ONLY (droppable without breaking the chain) · **NR** =
NOT-REACHED (dead branch). Discharge class for R&C rows: **CARRY** (bounded
transcription/packaging; no new mathematics) vs **MATH**.

| Row | Consumption site(s) actually found | Verdict | Discharge / class | Load-bearing quote |
|---|---|---|---|---|
| **HYP.74** `JD0-BOX-2` (gauge-form warrant) | J-D0 is consumed at **7** cone notes (W11 L599, W12 L1499, HMENU3 L717, HEX3 L720, GENIND L1518, GENH4 L938, GENHN L1515). Two of them declare BOX-2's perimeter INHERITED. | **R&C** (frame grade) | **CARRY** — not dischargeable from the weld notes as they stand (BOX-2 is by its own words a *reading*, not a theorem), but every spine site supplies the needed bijection inline, so a fold that records "site X's gauge is an explicit z-power unit character, verified in-note" strikes the inheritance without new mathematics. The general lemma the census S-5 proposes ("every value-dictionary entry the corpus consumes is a unit character") is **not needed on the path**. | W11 L602–603: *"J-D0 is corroborating at the instance, load-bearing only for the general-program framing (**S4.3 inherits its BOX-2 gauge-form perimeter**)"*; W12 L1502–1504: *"J-D0 is corroborating at the instances and load-bearing for the general framing (**its BOX perimeter inherited**)"*; JD0 L219–225: *"That the weld's ENTIRE conditional value dictionary acts by slot-wise unit characters is the synthesis §3.2 **reading** of JA-RES [GRTJA, 1/2], the (ξ,w) covariance family [GRTW2, ACCEPTED], and … ORB-RES"* |
| **HYP.75** `JB-NOLEG` rows 4/5/12/22 | **None on the path.** Row 12 reaches only via (i) `JD0-BOX-1`'s B(c)-tie → **W-10** (census O-11, out of cone) and (ii) `[W12-H](H-b)` (route not taken). W11 S4.3's owed-input list (L438–441) is `W11-BOX-1`'s *program*, itself unproved and superseded by GENIND/GENH4/GENHN. J-D0's own L4 leg consumes **JB-VTX/JB-DEV**, the ACCEPTED faces — **not** the NO-LEG rows. | **NR** | n/a — record OUT; drop from preview Display A. | W11 L391–393: *"**JB row-12 u ≥ 0 proviso** (GRTJB @ 83f47f4): **not consumed** — all polygons computed outright on the finite grid {0,1,2}"*; JD0 L212–218 (BOX-1): *"the B(c)-tie to the welded grid **still owes JB row 12's u ≥ 0 proviso leg**"* — i.e. owed by W-10, not by the spine |
| **HYP.76** `W2-VALUE` (W2-OPEN-1/-2, W2-BOX-1) | **None.** Zero occurrences of `W2-OPEN-1/-2` or `W2-BOX-1` in any of the 20 cone notes. GRTW2 reaches the spine only through W11's "template faces" list and W12 §S5's display; `JD0-BOX-2` cites GRTW2's **ACCEPTED** (ξ,w) covariance, not the open value layer. Every cone note that mentions the seam says it is off: *"the W-2/LIFTCORNER seam NOT consumed"* (GENH4 L69, L870; GENHN L120; HEX3 L503). | **NR** | n/a — record OUT; drop from preview. | GRTW2 L881–889 (W2-OPEN-1): *"Status: **instance-confirmed, not proved.** … That analysis is the W-2 → W-5 seam (the correction-tree junction J-B) and is **NOT claimed here**"* |
| **HYP.77** `DMULT-S` (exact ω ≡ 1 tier) | **None.** The one reachable GRTJC face, COROLLARY W-9, rides the **citable** tier. W11 states DULEMMA/(DMULT-s) not consumed anywhere. `JC-COB`'s sharp form and `R1-COB`'s exact predicate — the only (DMULT-s) consumers — have no cone consumer. | **NR** | n/a — record OUT; drop from preview. Note the census I-4 fold (2026-08-12) already PDF-verified the (DMULT-w) cite, so the reachable tier is cite-VERIFIED. | GRTJC L1985–1990: *"If **(DMULT-w)** failed at composite order, (COORD-B)(2), (MULT-B), §§2.7–3.4, **W-8 and W-9 fail together**; if only **(DMULT-s)** failed (ω ≢ 1), JC-COB's sharp values and R1-COB's exact predicate fail while **the structural clauses stand**"*; JC-F1 [r1]: *"**THROUGH, general m**, on **(DMULT-w)** + JC-IND"* (L1826–1827) |
| **HYP.21** `[H-b]` / JB row 12 | Only `[W12-H](H-b)` (W12 L1141–1143) and `W11-BOX-1`'s S4.3 program (HYP.146). Both off the taken route. | **NR** | n/a — record OUT; drop from Display A (it is inside the `JB-NOLEG` conjunct). | W12 L1508–1509: *"consumed **ONLY** in §S5's composition display and as **[W12-H]'s named inputs** (S4.2); **NO S2/S3 proof depends on them**"* |
| **HYP.22** `[H-c]` / J-D1 value package | Only W12 §S4.2 `[W12-H]` + §S5's composition display. The actual (A0)/(A1) suppliers at n ≥ 4 are **GENH4.C** and **GENHN.C** (W12's own [r4] FIRING CHECK table, L437–447), and both declare no weld face inside a count law. | **NR** | n/a — record OUT; drop the whole `[H-c]` conjunct from Display A, replacing it with the reachable face (see §5). | W12 L1213–1216 (W12-BOX-1): *"**No count law at composite stages is claimed**; the route is priced in S4.2 ((H-a)–(H-d))"*; GENIND L1523–1524: *"WELDMASTER / DULEMMA / **[W12-H]** … named in S7.1's (CS-1..3) pricing **only**"*; GENIND L860: *"the reason **the n ≤ 3 program never consulted [W12-H]**"* |
| **HYP.67** `GENTOW6-BOX-1` | Zero weld-layer content. It is GENTOW6's own μ₂ ≥ 3 attainment-classification residue (the Y-bookkeeping k-extraction ledger). Ledger's *"CONSUMED BY: potentially [H-c]"* is a **mis-attribution**: `[H-c]` is by definition *"the six weld notes' displayed boxes + the Σ-LAW residue"* (W12 L1146–1148); GENTOW6 is not a weld note and cites none. | **NR** *as a weld-face residue* (out of this audit's remit; may be in-cone as a tower-leg residue under HYP.82/HYP.144) | n/a for this audit — re-home CONSUMED BY. | GENTOW6 L610–615: *"**GENTOW6-BOX-1 (mu2 >= 3 attainment).** THEOREM 6.3 is scoped to mu2 = 2 … the Y-bookkeeping of (a) needs the k-extraction ledger re-derived; **not claimed**"* |
| **HYP.68** `GENTOW6-BOX-2` | Zero weld-layer content. The partial-side product-measure **count** ledger, *"open already at FULL sides (GENTOW-1's count-law remark)"*. Consumer, if any, is the tower count layer, not `[H-c]`. | **NR** *as a weld-face residue* | n/a — re-home CONSUMED BY (it belongs beside HYP.144's GENTOW4-BOX-2 open count layer). | GENTOW6 L616–619: *"**GENTOW6-BOX-2 (the count LEDGER at partial sides).** … the product-measure count of the f-locus per window … **open already at FULL sides**, not a partial-side novelty"* |
| **HYP.69** `GENTOW6-BOX-3` | Zero weld-layer content. Regime-3 (entry-driven / non-top-overflow) classification, *"displayed only … no theorem"*. Ledger's *"potentially W12 value partition"* is a mis-attribution — W12 has no value partition consuming GENTOW6. | **NR** *as a weld-face residue* | n/a — re-home CONSUMED BY. | GENTOW6 L620–623: *"Which non-x-free, no-top-overflow members diverge, and exact THETA-attainment via entry drivers: **displayed only** … **no theorem**"* |
| **HYP.71** `GENTOW3-BOX-1` | Zero weld-layer content. The attainment/non-cancellation certificate; T4 records the original ask is *"replaced, not magically solved, by the explicit witness ω_j"*. | **NR** *as a weld-face residue* | n/a — re-home CONSUMED BY (GENTOW-6.3 / CERT-TOP, not `[H-c]`). | GENTOW3 L449–456: *"a genre-general non-vanishing proof, and battery witnesses for entry-digit-driven overflows … are **NOT delivered**"*; T4 §S7 L538–540: *"the original genre-general non-cancellation ask is **replaced, not magically solved**, by the explicit witness ω_j"* |

**Counts: R&C 1 · CORR 0 · NR 9.**

### 2b. Two faces the charter names that are not chartered rows

Recorded because the charter asks what the path consumes, not only what the ten
rows say.

| Face | Verdict | Note |
|---|---|---|
| **COROLLARY W-9 / `GENHN-BOX-2`** (GRTJC's twisted algebra at GENHN-4 layer 1, μ ≥ 3) | **R&C — the only weld face inside a count law** | **Has no ledger row of its own.** Only its sub-residue `HYP.139` (the W-1 transport) is indexed. See §4 for the discharge analysis. |
| **`WELD-M-PKG` (five fields M0–M4) / WELDMASTER** | **NR** | Consumed on the spine as **naming only**: GENHN L112 *"the (CS-1..3) pricing grammar"*; GENIND L1523 *"named in S7.1's (CS-1..3) pricing only"*; GENH4 L938–941 *"the MECHANISM frame … no count law consumes them"*. `HYP.59`'s narrow-interface disposition is correct and needs no widening. T1 C7 confirms from the other side: *"T1 organizes five named fields around one tower cocycle; **it proves no cross-face coherence theorem**"* (`T1_GAUGE_COCYCLE_2026-08-12.md` C7-scope). |
| **SIGMALAW (Σ-LAW), LIFTCORNER, GRTJA, DULEMMA** | **NR** | GENHN L1522–1523: *"**SIGMALAW** @ a27ed68 … named at S8(b)'s letter-layer composition only; **no count consumption**"*; HE3 L88: *"SIGMALAW, JC-LOAD, DULEMMA: **not consumed**"*; W12 L1511–1512: *"**DULEMMA** … **NOT consumed** by any count law (S5 display only)"*. GRTJA (arc 1/2) reaches only *inside* J-D0's own L4 leg (JA-GRID) and BOX-2 (JA-RES) — i.e. it rides HYP.74, not the spine directly. |

---

## §3. THE JD0-INTERFACE LEAK SWEEP

**Rule applied.** A count-side consumption must route through J-D0's
gauge-blindness (counts are a function of the shape / count-shape alone), not
through a letter-layer fact used as a premise. A *leak* is a count-side site
whose conclusion needs a letter-layer identity that J-D0 structurally cannot
supply. J-D0's own proof names the failure mode: the NONCHAR tooth —
*"an additive slot shift is a bijection that breaks type preservation, hence
counts"* (`JD0_PROOF` L232). Sites were checked at every occurrence of `J-D0`
in the cone (25 hits) plus every count-side occurrence of a twist/letter
identity.

### Result: **1 live leak, disclosed. 1 leak retired inside the corpus. 2 latent, inside boxed non-theorems. 10 clean routings.**

**LEAK-1 (LIVE, disclosed) — `LEMMA GENHN-4` layer 1 at μ ≥ 3.**
GENHN-4 is a **count** lemma (a digit-space bijection giving the refine
transfer). Its layer 1 does not route through J-D0; it imports the W-9 graded
frame and a letter-layer cocycle identity as premises:

> *"Pass to gr R = the twisted algebra K^c[ℤ/e₁-graded lines] (**COROLLARY W-9
> @ accepted pin, consumed HERE as the organizing frame** — disclosed …). …
> **The twist powers z^{fl} (S4's cocycle: n(κ)^t = ẑ^{fl_t}n(tκ)) enter both
> the residual assembly and the carry products IDENTICALLY**, so the graded
> identity holds coefficient for coefficient over K"* (`GENHN_PROOF` L871–885)

The recentering it studies (Φ′ ↦ Φ′ + w) is an **additive** shift — precisely
the NONCHAR failure mode, so J-D0's perimeter cannot cover it even in
principle. The Codex SG-2 probe independently reached the same boundary:

> *"**J-D0 is count-layer blindness and cannot substitute for the event-level
> coefficient identity R(T+s) = T^μ**"* (`SG2_PROBE_2026-08-10.md` L60–62)

**Status: correctly disclosed** at `GENHN-BOX-2` — *"the count laws at μ ≥ 3
stand on one accepted weld face"* (L1331–1338). This is not a hidden leak; it
is the audit's one confirmed weld-face consumption, and it is exactly where
the census's DAG summary said it would be.

**LEAK-2 (RETIRED inside the corpus) — `GENTOW-2` layer 1 at general μ₂
(tower leg).** GENTOW1 opened the same leak one level up: *"at general mu2
layer 1 consumes the composed graded frame **exactly as GENHN-4 layer 1
consumed W-9's** — GENHN-BOX-2's disclosure pattern one level up"*
(`GENTOW1_PROOF` L376–378). GENTOW5 closes it without any weld face:
*"the frame needed is not an abstract twisted algebra at all — it is gr of the
leaf FIELD, which is a graded division ring for free … Everything below is
elementary given GENHN-2'; **no W-9-style weld face is consumed**"*
(`GENTOW5_PROOF` L152–157). **Net: the tower leg carries no weld face.**

**LATENT-1 — `CANDIDATE THEOREM GENIND-5`'s stage recentering.** GENIND S7.3
computes the stage-refine carry as *"**LITERALLY the cocycle multiplication of
W-9**: odd·odd dv-slots landing on even slots twisted by s"* (L1291–1293).
This is letter-layer input to a count bijection — but GENIND-5 is explicitly
*"NOT proved — boxed at the displayed obstruction"* (L1258), so it is not a
consumption. Flagged because any future discharge of GENIND-5 inherits LEAK-1's
geography.

**LATENT-2 — GENIND `(OB-b)`.** *"the general step needs the graded-read
identification (**W-8/JC-LOAD consumed as warrant** — accepted 2/2, but its
consumption here as a POINTWISE digit statement on (α₁, α₀, β₁, β₀)
coordinates is **a new transcription that must be written and machine-checked,
not cited**)"* (L1307–1312). An explicitly undischarged transcription
obligation attached to a weld face, sitting inside GENIND's boxed obstruction
rather than in a fired count law. Not a leak today; a named one if the box is
ever discharged by citation rather than by GENH4/GENHN-style re-proof.

**CLEAN (10 sites, all routing correctly through J-D0 with the
type-preserving bijection exhibited inline, not merely cited).**
W11 L206–208 · W12 L574, L776 · HMENU3 L424 · GENIND L645, L700, L1201 ·
GENH4 L370–371 (*"Counts are twist-blind ((S₁, S₀) ↦ (S₁, z̄S₀) is a
type-preserving bijection — J-D0's mechanism, literal at order 2); LETTERS are
twisted"*) · GENHN L415, L736 (*"counts are twist-blind (J-D0: (digits) ↦
(twisted digits) is a type-preserving bijection), LETTERS are twisted"*),
L1076. At every one of these the note supplies the bijection itself and cites
J-D0 as the mechanism's name — which is why W11/W12 grade J-D0 "corroborating
at the instance". None imports a letter-layer fact as a premise.

**Sub-finding (not a leak, but a scope note).** J-D0's corrected statement
(§S10.1, from the E8 certification run) keys blindness to the **count-shape**
`T̂ = (d0, (e_j,h_j,g_j)_j, (Λ_j)_j, 𝒢)`, and explicitly declines to reduce
`𝒢`/`B` to the shorter tuple: *"No assertion is made here that the chosen t_j,
D_j, δ, or B are determined by the shorter tuple (e_j,h_j)_j; proving such a
reduction is a separate grid/split theorem"* (L488–492). The spine's sites do
not key by the shorter tuple — they key by their own explicit slot geometries
— so the corrected scope is respected. `W-11`'s menu key is the one place the
corrected note flags an obligation (*"If W-11 wants to key it only by
(d0,(e_j,h_j,g_j)_j), it separately owes proofs that Λ_j and 𝒢 … are
determined by that shorter tuple"*, L640–645); that is Phase-B menu growth
(`JD0-BOX-3`), not a fired count law.

---

## §4. DISCHARGE ANALYSIS FOR THE ONE REACHABLE FACE

**Question.** Is `GENHN-BOX-2` (the W-9 graded face at GENHN-4 layer 1, μ ≥ 3)
dischargeable from the accepted weld notes as they stand?

**From the WELD notes alone: NO — and this is already on record.** The SG-2
probe's verdict is `NEEDS-X`: *"GENHN-4 is NOT re-derivable from WELDMASTER
(M0)–(M4) + WM-COB + J-D0 as those clauses are stated: the weld's one-face
consumption does not suffice for layer 1 (the graded kills)"*
(`SG2_PROBE_2026-08-10.md` L26–29), with two named obstructions: perimeter
re-instantiation (M0–M4 never assert a stage event lands in WELD-M's domain)
and the additive recentering action (*"(M4)'s covariance is a character-system
action, not the additive-translation formula"*).

**From the ACCEPTED CORPUS (not the weld layer): a named route exists, and it
is a CARRY-class fold, not new mathematics.** Three displays already in
accepted notes compose to replace the W-9 import:

1. **GENHN's own [r2] erratum** already re-aims the frame away from the
   twisted algebra and onto a valued field's graded ring:
   *"S6.1 layer 1's graded frame **re-aims at gr(O_L)** (one K-line per
   dv-height, W-9's twisted algebra; R ⊆ O_L and every graded read of an
   evaluated element happens in gr(O_L))"* (`GENHN_PROOF` L515–517), with the
   forcing chain establishing L a complete DVR field at L500–512.
2. **LEMMA GENTOW5-A0** proves the needed frame property elementarily and
   without any weld input: *"gr(L2) is a commutative graded ring … every
   nonzero homogeneous element is invertible … [a][b] = [ab] is dv2(a) +
   dv2(b), a valuation identity on a **FIELD** (no zero divisors to break
   it)"* (`GENTOW5_PROOF` L162–178). Its stated point is exactly LEAK-1's
   content: *"**pointwise, at a root, there is no carry problem.** Carries are
   a phenomenon of the slot GRID … evaluation at x0 is blind to them"* (L179–183).
3. **LEMMA GENTOW5-A1** supplies the coefficient-for-coefficient dictionary
   (the missing SG-2 item X(ii)) with the twist factors made explicit and
   x0-free: `c_j = ι(γ_j)·θ_{μ₂−j}^{−1}` with `θ_t` telescoping through the
   ladder cocycle `τ(a,b)` (L211–248).

**Non-circularity check (done).** GENTOW5-A1's proof leans on *"GENHN's S4
level-1 cocycle (accepted; the ITER-LAW z-letter)"*. At level 1 that input is
derived in GENHN itself from the normalizer arithmetic —
*"Products wrap through θ^{e₁} = (unit lift)·π^h: n(κ)^t = ẑ^{fl}·n(tκ) with
the explicit integer fl = the wrap count — W-9's cocycle, literal"*
(`GENHN_PROOF` L732–735) — i.e. GENHN **derives** the cocycle and only
**identifies** it with W-9's. So the A0/A1 route at level 1 does not
re-consume the face it is replacing.

**Verdict.** `GENHN-BOX-2` is **dischargeable**, by the named supplying
displays GENTOW5-A0 + GENTOW5-A1 + GENHN's [r2] gr(O_L) re-aim + GENHN-2's
pointwise-faithful slot passage. **Class: CARRY** — the owed object is one
fold unit that rewrites GENHN-4 layer 1 in the A0/A1 idiom and strikes the
box; no new mathematics is required. Two caveats, stated honestly: (a) the
route is this audit's synthesis, not a corpus display — no accepted note
asserts the discharge; (b) GENTOW5 is itself at grade 0/2 (hostile arc owed),
so the fold inherits that arc. Independent corroboration that the target is
the right one: the census's O-1 X-lemma attempt already **DELIVERED X(ii)**
(the additive action T ↦ T+s) at attempt grade.

**The residue that does NOT discharge with it.** `HYP.139` — the **W-1
transport** (harness w, 𝑅 → FGMN objects), which GRTJC records as still
ATTEMPT: *"Transport of the harness w and 𝑅 to the FGMN objects is still W-1
(ATTEMPT) — and the corpus's own march-level dictionary for that transport,
TR-3′-GEN, is OPEN at general order"* (`GRTJC_PROOF` L1990–1993). This rides
under the W-9 face and is class **MATH**. Note that discharging GENHN-BOX-2 by
the A0/A1 route would **also retire HYP.139 from the cone**, since the
replacement frame is gr(O_L), not an FGMN object — a second reason the fold
unit is high-value.

---

## §5. LEDGER-FOLD BLOCK (exact corrections for the next HYPOTHESIS_LEDGER fold)

Prefix tag for this fold: **COR-WFA-01 … COR-WFA-13**. Twelve row corrections
(one of them a new row) + one preview edit. Format matches the ledger's own
row block and its `- HYP.NN corrected: …` correction list.

### 5a. Correction-list lines (append to the corrections section)

```
- HYP.73 CLOSED: WELD-FACE-AUDIT executed at the WIDENED charter (HYP.74-77 + HYP.21/22/67/68/69/71);
  record runs/qgen/WELD_FACE_AUDIT.md; verdicts 1 R&C / 0 CORR / 9 NR (COR-WFA-01).
- HYP.74 corrected: CONSUMED BY re-sourced from "[H-c]" to the seven direct spine sites; reachability
  SIGNED REACHABLE; disposition CARRY retained with the discharge route named (COR-WFA-02).
- HYP.75 re-dispositioned CARRY -> NODE: audit signs NON-REACHABILITY on all four JB rows (COR-WFA-03).
- HYP.76 re-dispositioned CARRY -> NODE: audit signs NON-REACHABILITY; zero cone consumers (COR-WFA-04).
- HYP.77 re-dispositioned CARRY -> NODE: audit signs NON-REACHABILITY; W-9 rides (DMULT-w) (COR-WFA-05).
- HYP.21 re-dispositioned CARRY -> NODE: reachable only via [W12-H] and W11-BOX-1's superseded
  S4.3 program; needs-Asvin question 2 answered by this audit (COR-WFA-06).
- HYP.22 re-dispositioned CARRY -> NODE: the [H-c] aggregate is unreachable; its children 74/76/77/83-86
  are individually NR except 74, which is re-parented off [H-c] (COR-WFA-07).
- HYP.67 corrected: CONSUMED BY "potentially [H-c]" -> GENTOW6/T4 attainment layer; not a weld
  residue and outside the weld-face audit's remit (COR-WFA-08).
- HYP.68 corrected: CONSUMED BY "potentially [H-c]" -> the tower count layer beside HYP.144's
  GENTOW4-BOX-2 open count layer (COR-WFA-09).
- HYP.69 corrected: CONSUMED BY "potentially W12 value partition" -> GENTOW6 regime-3 display;
  W12 has no value partition consuming GENTOW6 (COR-WFA-10).
- HYP.71 corrected: CONSUMED BY "potentially [H-c]" -> GENTOW-6.3/CERT-TOP; T4 records the ask
  replaced by the explicit witness omega_j (COR-WFA-11).
- HYP.148 ADDED: GENHN-BOX-2 / the W-9 graded face - the ONE weld face inside a count law, previously
  indexed only through its sub-residue HYP.139 (COR-WFA-12).
- HYP.139 corrected: consumer chain confirmed; note that a GENTOW5-A0/A1 fold of HYP.148 would
  retire HYP.139 from the cone with it (COR-WFA-13).
- Capstone-conditionality preview: CHANGED (first change since v3). See 5c.
```

### 5b. Row blocks (replace/insert verbatim)

```
HYP.74  `JD0-BOX-2`
WHERE: census S-5; `lean/notes/openmath/JD0_PROOF_2026-08-08.md` S4 L219-225.
STATEMENT: JD0 unit-character warrant is synthesis, not proved theorem: "That the weld's ENTIRE
conditional value dictionary acts by slot-wise unit characters is the synthesis §3.2 reading of
JA-RES [GRTJA, 1/2], the (ξ,w) covariance family [GRTW2, ACCEPTED], and the ℓ-orbit rescalings
ORB-RES [J-A]."
CONSUMED BY: [WELD-FACE-AUDIT 2026-08-13, REACHABLE-AND-CONSUMED] NOT via [H-c]. J-D0 is consumed
DIRECTLY at seven cone notes (W11 L599, W12 L1499, HMENU3 L717, HEX3 L720, GENIND L1518, GENH4 L938,
GENHN L1515); two declare BOX-2's perimeter inherited (W11 L602-603 "S4.3 inherits its BOX-2
gauge-form perimeter"; W12 L1502-1504 "its BOX perimeter inherited"). At all ten counted routing
sites the consuming note exhibits the type-preserving bijection itself, so J-D0 enters at FRAME
grade, not as an imported theorem.
CLASS: hypothesis-box
PROPOSED DISPOSITION: CARRY - reachability SIGNED. Discharge is a fold, not a theorem: record per
site that the gauge consumed there is an explicit z-power unit character verified in-note, which
strikes the inheritance. The general lemma census S-5 proposed ("every value-dictionary entry the
corpus consumes is a unit character") is NOT needed on the capstone path.

HYP.75  `JB-NOLEG`
WHERE: census S-6; `GRTJB_PROOF_2026-08-08.md` §0M.
STATEMENT: Open no-machine-leg cells are row 4 GRID, row 5 RES, row 12 d4/u>=0, and row 22 v=0.
CONSUMED BY: [WELD-FACE-AUDIT 2026-08-13, NOT-REACHED] No capstone-path consumer. W11 L391-393:
"JB row-12 u >= 0 proviso (GRTJB @ 83f47f4): not consumed - all polygons computed outright on the
finite grid {0,1,2}". Row 12 reaches only (i) JD0-BOX-1's B(c)-tie -> W-10 (census O-11, out of
cone) and (ii) [W12-H](H-b), the route W-12 priced but did not take. W11 S4.3's owed-input list is
W11-BOX-1's unproved program (HYP.146), superseded by GENIND/GENH4/GENHN. J-D0's own L4 leg consumes
JB-VTX/JB-DEV, the ACCEPTED faces, not the NO-LEG rows.
CLASS: named-obligation
PROPOSED DISPOSITION: NODE - record OUT of the cone; drop from preview Display A. Retain as a
GRTJB-internal frontier row.

HYP.76  `W2-VALUE`
WHERE: census S-7; `GRTW2_PROOF_2026-08-08.md` §5.3 L881-889, §5.2, §3.5.
STATEMENT: W2 value formula and two side conditions hold on every claimed face; GRTW2's own grade
is "instance-confirmed, not proved" (16 towers / 684 groups).
CONSUMED BY: [WELD-FACE-AUDIT 2026-08-13, NOT-REACHED] Zero occurrences of W2-OPEN-1/-2 or W2-BOX-1
in any of the 20 cone notes. GRTW2 reaches the spine only through W11's "template faces" list and
W12 §S5's composition display; JD0-BOX-2 cites GRTW2's ACCEPTED (ξ,w) covariance, not the open
value layer. Four cone notes state the seam is off (GENH4 L69/L870, GENHN L120, HEX3 L503:
"the W-2/LIFTCORNER seam NOT consumed").
CLASS: hypothesis-box
PROPOSED DISPOSITION: NODE - record OUT of the cone; drop from preview Display A.

HYP.77  `DMULT-S`
WHERE: census S-8; `GRTJC_PROOF_2026-08-08.md` JC-BOX-3 [r2,G-1] L1985-1990, L1220.
STATEMENT: Exact ω=1 multiplicity tier satisfies claimed formula beyond measured cases.
CONSUMED BY: [WELD-FACE-AUDIT 2026-08-13, NOT-REACHED] The one reachable GRTJC face, COROLLARY W-9,
rides the CITABLE tier: "if only (DMULT-s) failed (ω != 1), JC-COB's sharp values and R1-COB's exact
predicate fail while the structural clauses stand" (L1987-1989); JC-F1 [r1] "THROUGH, general m, on
(DMULT-w) + JC-IND". W11 L383-384: "DULEMMA / (DMULT-s) ... NOT consumed anywhere above". The
(DMULT-w) cite is PDF-VERIFIED (census I-4 fold, 2026-08-12).
CLASS: named-obligation
PROPOSED DISPOSITION: NODE - record OUT of the cone; drop from preview Display A.

HYP.21  `[H-b]` / JB row 12
WHERE: `W12_PROOF_2026-08-08.md`, §4.2 L1141-1143.
STATEMENT: JB row-12's u>=0 membership proviso holds on the unbounded grid.
CONSUMED BY: [WELD-FACE-AUDIT 2026-08-13, NOT-REACHED] W12's own ledger, L1506-1509: the six weld
notes are "consumed ONLY in §S5's composition display and as [W12-H]'s named inputs (S4.2); NO S2/S3
proof depends on them". The n>=4 route actually taken is GENIND's (CS-1..3) discharged by
GENH4/GENHN; GENIND L860 records "the n <= 3 program never consulted [W12-H]".
CLASS: hypothesis-box
PROPOSED DISPOSITION: NODE - record OUT of the cone (needs-Asvin question 2 answered by the audit);
represented for GRTJB-internal purposes by HYP.75.

HYP.22  `[H-c]` / J-D1 value package
WHERE: `W12_PROOF_2026-08-08.md`, §§4.2 L1146-1148, 5.
STATEMENT: J-D1 is conditional on the six weld notes' boxes plus Σ-law, including VD-m, C-coll,
R-coll, DMULT-S, and PERIM-μ.
CONSUMED BY: [WELD-FACE-AUDIT 2026-08-13, NOT-REACHED] Only [W12-H] and W12 §S5's display. The
actual (A0)/(A1) suppliers at n>=4 are GENH4.C and GENHN.C per W12's own [r4] FIRING CHECK table
(L437-447), and both declare no weld face inside a count law (GENH4 L951-952; GENHN L96-101).
W12-BOX-1: "No count law at composite stages is claimed; the route is priced in S4.2".
CLASS: hypothesis-box
PROPOSED DISPOSITION: NODE - record OUT of the cone; drop the whole [H-c] conjunct from preview
Display A. Children 83-86 (VD-m, C-coll, R-coll, PERIM-μ) inherit NOT-REACHED; child 74 is
re-parented off [H-c] and carried on its own (R&C) footing; children 76/77 are NOT-REACHED.

HYP.148  `GENHN-BOX-2` / the W-9 graded face
WHERE: `lean/notes/openmath/GENHN_PROOF_2026-08-08.md` S6.1 L865-921, BOX L1331-1338, ledger
L1512-1514; `SG2_PROBE_2026-08-10.md`.
STATEMENT: LEMMA GENHN-4's layer 1 at μ>=3 consumes COROLLARY W-9's twisted algebra as the carry
bookkeeping rather than re-deriving it: "the count laws at μ>=3 stand on one accepted weld face".
This is the ONE weld face inside a count law on the whole capstone path.
CONSUMED BY: GENHN.A(iv) at μ>=3 -> the composite-stage count laws -> GENIND.B/W-12.D at n>=4;
NOT inherited by the σ chain (HE3 L78-88, L1293-1294) and NOT by the tower leg (GENTOW5 L152-157,
L1308-1311, which retires the analogue).
CLASS: conditional-theorem-leg
PROPOSED DISPOSITION: CARRY, dischargeable by a named fold (class CARRY, not MATH). Route:
GENHN's own [r2] erratum re-aiming layer 1's frame at gr(O_L) (L515-517) + LEMMA GENTOW5-A0
("gr of a valued field is a graded division ring", GENTOW5_PROOF L162-183) + LEMMA GENTOW5-A1 (the
coefficient-for-coefficient dictionary, L211-248) + GENHN-2's pointwise-faithful slot passage.
Non-circular at level 1: GENHN derives its own S4 cocycle from the normalizer arithmetic (L732-735)
and only identifies it with W-9's. Caveats: the route is the audit's synthesis, asserted by no
accepted note; GENTOW5 is at grade 0/2. Compensating instrument already standing: GN-REFINE3
(pointwise re-division transport at every μ=3 stage-α event). Independent corroboration that the
missing content is the right target: SG-2's X(ii) was DELIVERED at attempt grade (census O-1).
```

### 5c. Preview edit (Display A and Display B share the hypothesis block)

Replace

```
  ∧ JB-NOLEG(rows 4,5,12,22)†
  ∧ [H-c]† = JD1ValuePackage[
       VD-m, C-coll, R-coll, PERIM-μ,
       DMULT-S†, JD0-BOX-2†, W2-VALUE† ]
```

by

```
  ∧ JD0-BOX-2†            -- the unit-character gauge-form perimeter, inherited FRAME-grade
  ∧ (GENHN-BOX-2 if n≥6)  -- the W-9 graded face inside GENHN-4 layer 1 at μ≥3; carries HYP.139 (W-1 transport)
```

**Rationale, stated for the fold record.** (i) `JB-NOLEG` and the `[H-c]`
package have no capstone-path consumer (§2); carrying them overstates the
conditionality. (ii) `JD0-BOX-2` survives because two spine notes declare its
perimeter inherited in their own words — it is the only member of the deleted
block that is reachable. (iii) `GENHN-BOX-2` must be ADDED: it is the one weld
face inside a count law and it is currently invisible in the preview, present
only obliquely through `HYP.139`. Its `n ≥ 6` guard matches μ ≥ 3's first live
degree, the same guard `[GENHN-HE(μ≥3)]` already carries. (iv) The
`(H-e)-window-pinning` conjunct should have its **supplier re-sourced** in the
ledger prose: W12's (H-e) text sources the pinning to "the (H-a)/(H-b)-leg",
but on the taken route it is supplied elementarily by `LEMMA GENHN-3`
(caps/raggedness/consultation, GENHN L741-770) and GENIND-2's consulted-height
bound. The conjunct itself does not move; only its stated supplier does.
No other conjunct of Display A or B changes. `AllOInterfaces[… J-D0 …]` is
unchanged and correct.

---

## §6. THE CENSUS'S UNDERLYING QUESTION, ANSWERED

**How much of the weld layer does the capstone path actually consume, and is
any weld residue a genuine carry into the conditionality display?** Almost
none of it, and yes — exactly one, and it is not the one currently carried.
Of the eight weld-layer texts (11,942 lines: LIFTCORNER, GRTW2, GRTJA, GRTJB,
GRTJC, WELDMASTER, JD0, SIGMALAW), the capstone path consumes **two faces**:
`LEMMA J-D0` at frame grade, cited by name at ten sites that each exhibit the
type-preserving bijection themselves (which is why W11 and W12 both grade it
"corroborating at the instance"); and `COROLLARY W-9`'s twisted group algebra
at `LEMMA GENHN-4` layer 1 for μ ≥ 3, which GENHN itself calls "the ONE weld
face inside a count law". Everything else the weld layer proves — the whole
value/letter dictionary, the four JB no-leg rows, the W-2 transported value
layer, the exact (DMULT-s) tier, the Σ-LAW, the five-field WELD-M-PKG,
LIFTCORNER's corner, DULEMMA — is consumed by **nothing** on the path: the
notes that could have consumed it (GENH4, GENHN, GENIND, HMENU3, HEX3, W11,
W12 §S2/S3) each re-prove the needed instances elementarily and say so in
their own dependency ledgers, and the σ chain and the tower leg carry strictly
fewer faces still (HE3 declines GENHN-4 outright; GENTOW5 retires the tower's
W-9 analogue by replacing the twisted algebra with gr of the leaf field). The
`[H-c]` value package survives in the corpus only as an input to `[W12-H]` —
the route W-12 **priced but did not take** — so carrying it in the preview
conditionalizes the theorem on a branch no proof walks. The genuine carry is
`GENHN-BOX-2` plus its sub-residue `HYP.139` (the W-1 transport, still
ATTEMPT), and it is presently **absent** from both displays. The good news is
that this single genuine carry is a CARRY-class fold rather than open
mathematics: GENHN's own [r2] erratum already re-aims the frame at gr(O_L),
GENTOW5-A0/A1 already prove the elementary substitute one level up, and the
SG-2 probe's missing X(ii) was delivered at attempt grade — so one fold unit
would take the weld layer's contribution to the capstone's conditionality from
one face down to zero, and would retire `HYP.139` from the cone with it.

---

**Record rule.** Every verdict above cites its note + line at HEAD; grades are
the sources' own vocabulary; no row upgrades or downgrades a source grade. The
one synthesis this audit contributes — the GENTOW5-A0/A1 discharge route for
`GENHN-BOX-2` — is labelled as the audit's own, asserted by no accepted note,
and is offered as a named unit, not as a discharge.
