# TIGHTNESS CENSUS — 2026-08-11

**Charge (Asvin 2026-08-10, verbatim intent):** "the math proof really should be
tight completely, not even a small lemma can be left unproven because everything
could collapse! either cited to standard literature (minus my paper) or proven!"
**Scope swept:** the 26 accepted texts (corpus map:
docs/SIMPLIFICATION_BLUEPRINT_2026-08-10.md §1) + the five newer notes
(GENTOW6_BOXES, GENTOW5_WI, WFRAME, T1_GAUGE_COCYCLE, SG2_PROBE, all
…_2026-08-10.md in lean/notes/openmath/) + PROJECT_STATE dated appends #14–#24 +
PROVER_RCT_PROTOCOL_2026-08-10.md (ledger + Amendments 1–2) + the X-lemma
attempt-1 record (BRIDGE_ADJUDICATIONS append 2026-08-10 22:53;
verification/openmath/xlemma_attempt1_*). **Method:** fence-vocabulary grep
(OPEN/BOX/fence/owed/conditional/placeholder/hypothesis/cited) over all 31
texts + every note's STATUS header + every HONEST BOX / HONEST RESIDUE /
CONDITIONALITY STACK section read in full + the capstone conditionalities.

**THE CONE.** "IN-CONE" = consumed, directly or transitively, by the uniformity
theorem's proof: W-12.D ∘ GENIND.B ∘ GENH4 ∘ GENHN (T(b)′ repaired) ∘ HEX3 ∘
HE6 ∘ HE7 ∘ HE3 ∘ HE6R1 ∘ HETOW ∘ GENTOW1–6, fed by the weld through TWO faces
(J-D0 count-blindness; the W-9 graded face via GENHN-4 — blueprint §1 DAG
summary; HE3 §S0 records the σ-chain inherits NEITHER W-9 nor GENHN-4), plus
the full-density tie's drainage input. Palindromy is NOT in the cone: Asvin's
Amendment 2 (PROVER_RCT_PROTOCOL L130–134) dropped palindromicity from both
stage-two targets; the theorem claims one R_τ ∈ ℚ(q) per splitting type at
every prime with Σ_τ R_τ = 1 = the Haar density. **The corpus-wide
face-citation audit named at Plan revision R.4 has NOT run** (SG2_PROBE
declares this); every weld-layer classification below that depends on it is
graded IN-CONE-SUSPECT, never optimistically OUT.

**Verdict up front.** 33 census rows: 5 IN-CONE (2 are arcs at 1/2 finishing,
1 is a verified cite needing only its stage-two declaration, 2 are audit-grade
units), 8 IN-CONE-SUSPECT (the sub-box refinement family + the weld value
layer, both decidable by one audit unit each), 20 OUT-OF-CONE (compression
track, displays, refinements, process debt, and the Lean-capstone stage-two
items listed separately). **No unproven mathematical statement is consumed by
the theorem at acceptance grade**; the in-cone opens are two 1/2-arcs, one
cite re-verification, one sub-bar process grade, and one audit.

---

## §1. IN-CONE (must be proven or cited; discharge queue §5)

| # | Item | Grade | Cone evidence | Discharge route | Effort |
|---|---|---|---|---|---|
| I-1 | **DRAIN-N4** — the n ≥ 4 drainage rate (informal hExhaust-n): GENIND.C proves the simple-slice vanishing (accepted 2/2); the FULL-complement / CS-drain all-window rate is ANNEX-LEMMA GENIND-C2, annex arc at **1/2** (GENIND_PROOF S8 GENIND-BOX-4 L1368–1374; PROJECT_STATE #21 the gap, #22 "annex pass 2 CLEAN → 1/2"). n = 2 is W11's theorem, n = 3 is HEX3.B — both accepted. | conditional (annex 1/2) | Σ_τ R_τ = 1 (the full-density tie) consumes drainage at every n; W-12.D's (hExhaust-n) hypothesis. | PROVE — one more clean annex pass; already in the standing queue (append #21 "repair owed" → #22 1/2). | one-liner (arc completion) |
| I-2 | **WI-LEMMA** — [GENTOW5-W(i)], the single-w monomial form of the B″-transport at levels i ≥ 3 (fence: GENTOW5_PROOF S11.2 L1888–1933, "the honest-sorry analogue"). PROVED in GENTOW5_WI_2026-08-10.md; arc at **1/2** (PE1 CLEAN; PE2 in flight, skeleton committed). Conditional on it at i ≥ 3: GENTOW5-B(a)'s ψ^{(w)} realized-residual display + the (a)-certificate legs (KP membership, prime, non-equivalence) + (b)'s FGMN-side residual displays; NOT conditional: σ, values, degrees, budgets, (c)/(d)/(e) (the S11.2 walk; WFRAME §5 OPEN-1). | attempt-consumed (1/2) | The level-r grammar (GENTOW5-B) is load-bearing for depth ≥ 4 towers, first live n = 32; the theorem quantifies over ALL n. | PROVE — finish PE2 (in flight), then the WFRAME OPEN-1 fence drops by its own as-of clause. | one-liner (arc completion) |
| I-3 | **FGMN-MAIN** — FGMN Thm 6.3 + Cor 6.4 (one-sided N_i(F) at every level, ψ-power residuals) + the supporting S6.1 rows 1–22: the corpus's ONE load-bearing literature cite (blueprint §4), consumed at GENTOW2-A (item 6α) and re-consumed by GENTOW5-C/GENTOW6 S1 through GENTOW2's dictionary. PDF-verified at the displayed numbering by the two-leg method (GENTOW2_PROOF S6.1 table; arXiv fetch bit-identical). Standard literature (Fernández–Guàrdia–Montes–Nart), NOT 2212.00294. | cite-VERIFIED | GENTOW2 → GENTOW5/GENTOW6 → the n ≥ 8 tower leg. | CITE — already verified; stage two owes the declaration: axiom + AXIOM_FAITHFULNESS entry + semantic-guardian pass (blueprint §4 names it the one new axiom candidate). | unit (stage-two process) |
| I-4 | **FGMN-WELD** — GRTJC's FGMN citations: Thm 4.2 consumed only through the (DMULT-w) citable tier on B = gr(w)[in(π)⁻¹] (JC-BOX-3 [r1] L1960–1990), Thm 4.8/Cor 4.9 (Δ/(ψ_m) ≅ K_{m+1}), Cor 2.7 (abscissa additivity, JC-SPAN). WELDMASTER S4 records "FGMN numbering re-verification owed" (L578, L613) — no later discharge fold found in appends #14–#24 (append #14's "external cites … owed" is the last mention; GENTOW2's S6.1 verified only its OWN quotes). | cite-UNVERIFIED | W-9 is proved GIVEN W-6..W-8 (GRTJC §10 table); W-6..W-8 consume (DMULT-w); GENHN-4's layer-1 carry bookkeeping at μ ≥ 3 consumes COROLLARY W-9 (GENHN-BOX-2) → count laws at μ ≥ 3. | CITE — PDF-verify the GRTJC quotes at displayed numbering by GENTOW2's S6.1 two-leg method (same FGMN print, already fetched). | unit |
| I-5 | **JD0-BAR** — J-D0 (count gauge-blindness) is PROVED grade, machine 68,523/0, but **sub-bar**: never through the 2-clean hostile arc (blueprint §1.1 "the one exception, flagged where consumed"). | process-grade (sub-bar) | BOTH weld faces the count spine consumes ride it: JD0 count-blindness directly; GENHN-4's layer 3 cites J-D0 (SG2_PROBE checked-clean list). | PROVE (process) — run the 2-clean hostile arc on JD0_PROOF, or Asvin signs off the machine grade as the bar. | unit (arc) |

---

## §2. IN-CONE-SUSPECT (walk not on record; audit decides; never optimistically OUT)

**Family A — the sub-box refinement residue** (appends #20/#23 grade these
"count-grade refinements, not correctness conditions on the tie", but the
lineage is GENTOW3(iv)-sharpness feeding count displays; the consumption walk
from the R_τ laws to these refinements is not on record either way).

| # | Item | Grade | Evidence | Route if in | Effort |
|---|---|---|---|---|---|
| S-1 | **S8.1-MEMBER** — the ABOVE-floor divergence classification (E-ENTRY vs key-power diverge-vs-not, one genre): the one honest open of the sub-box family (append #23). The exactly-Θ member half is CLOSED by LEMMA GENTOW-6.7E (GENTOW6_BOXES S8.1 r2 block, accepted 2/2; sealed leg 441/0). RCT unit S8.1-member-half: pending, CODEX (protocol ledger L92). | open (displayed obstruction) | GENTOW6_BOXES L1272–1283; append #22. | PROVE — the pending RCT unit; the 6.7E genre-geometry pair is the constructed foothold. | hard-unit |
| S-2 | **P-BINOM** — THEOREM GENTOW-6.5(b) is IF-only: at p \| binom(m,2) floor attainment is per-genre undecided (BP3 attains, FR5X fails — append #22's withdrawn iff); + the j < j* lower-coordinate classification at μ₂ ≥ 3 (S3 display only; "BOX-3's geography one m up", GENTOW6_BOXES S6). | open (displayed dichotomy) | GENTOW6_BOXES S6 L544–553. | PROVE — genre arithmetic extending 6.7/6.7E; natural rider on S-1's unit. | unit |
| S-3 | **COMPOUND-6.5′** — the f₁ ≥ 2 × μ₂ ≥ 3 attainment compound: ANNEX-REMARK 6.5′, prose only, machine-uncontacted ("a NEW honest fence since BOX-4's leg is μ₂ = 2", GENTOW6_BOXES S6 L554–556). | fenced (prose, no machine) | ibid. | PROVE + machine row — extend the BOX-4 sealed battery one μ₂ up. | unit |

**Family B — the weld value/letter layer** (JD0 proves counts are letter-blind
and HE3 §S0 keeps the σ-chain weld-minimal, so these are LIKELY out; the R.4
face-citation audit that would prove it has not run).

| # | Item | Grade | Evidence | Route if in | Effort |
|---|---|---|---|---|---|
| S-4 | **WELD-FACE-AUDIT** — the corpus-wide face-citation audit itself (grep the 26 texts against the S-0 ledger labels for weld-face consumption): named at Plan revision R.4/F23-F36, explicitly NOT attempted (SG2_PROBE header). It is the decider for S-5…S-8 and the SG-2 branch input. | owed audit | SIMPLIFICATION_BLUEPRINT R.4; SG2_PROBE L32–35. | AUDIT — one session, mechanical grep + walk; each hit becomes a census row. | unit |
| S-5 | **JD0-BOX-2** — the gauge-form warrant: that the weld's ENTIRE conditional value dictionary acts by slot-wise unit characters is a synthesis READING of JA-RES + GRTW2's (ξ,w) covariance + ORB-RES, not a theorem; a non-unit-character value layer would sit outside J-D0's blindness perimeter (JD0_PROOF S4 L219–226; the NONCHAR tooth displays the failure mode). | fenced (warrant = reading) | ibid. | PROVE — state and prove "every value-dictionary entry the corpus consumes is a unit character" as a lemma over the S-0 ledger's value-layer rows (rides S-4's enumeration). | unit |
| S-6 | **JB-NOLEG** — GRTJB §0M's four-row NO-LEG open-proviso frontier (rows 4/5/12/22; row 12 = the u ≥ 0 proviso DISCHARGED at deg f < deg Φ_{q+1}, open residue = MEMBERSHIP at the unbounded-degree quantifier — WELDMASTER S4 [r1,F-1] quote; row 4 = the grid/attained-abscissa half). JD0-BOX-1 names row 12 riding only the B(c)-tie, a W-10 leg (W-10 is out, O-13). | fenced (proviso rows) | GRTJB L3159; WELDMASTER S4; JD0 S4. | If S-4 finds a spine consumer: prove the membership residue; else record OUT. | unit/row after S-4 |
| S-7 | **W2-VALUE** — W2-OPEN-1 (the transported value layer: "instance-confirmed, not proved", 16 towers / 684 groups, GRTW2 §5.3) + W2-OPEN-2 (the chain-level bridging box, §5.2) + W2-BOX-1/(C-coll) (the boundary witness, §3.5). | open (displayed lemmas) | GRTW2 L444, L617, L881. | Same as S-6: S-4 decides; letter layer enters σ only through canonical frames (blueprint DAG). | hard-unit if in |
| S-8 | **DMULT-S** — the (DMULT-s) EXACT tier (ω ≡ 1) is MEASURED, not cited, and carries only JC-COB's sharp form (JC-BOX-3 [r2,G-1]); the citable tier (DMULT-w) suffices for W-9. | measured-only tier | GRTJC L1985–1990. | If S-4 finds a sharp-form consumer on the spine: prove or cite; else OUT. | unit if in |
