# SYNTHESIS PASS 8 (2026-08-08, wallclock 2026-08-03) — the wave-16/17 close: the acceptance window

Charge: the /goal's standing synthesis clause; successor to `SYNTHESIS_PASS7_2026-08-08.md`;
owed since the r33/r34 displays ("SYNTHESIS PASS 8 OWED at the wave-15/16 close"), executed
at the wave-17 close. Sources: PROJECT_STATE (r36 checkpoint tail); ROOT §§0–3 + the §40/§41
folds + the REVISION-34/35 standing paragraphs; the ledger tail from "ITER-LAW ACCEPTED"
through "ACCEPTANCE RE-ANCHORED at REVISION 35 + r36b / VC36+VC36b+VC36c" (f89e60c);
SYNTHESIS_PASS7 (the executed queue); ITERLAW S0 + acceptance bracket; H7FORGE; DITERSUP
§§S4–S6; BANKED_SORRY; MATH_COMPLETION_TREE DELTA 4; REMAINING_CENSUS (TV rows).

HEADLINE. The window this pass maps (waves 15–17) is the corpus's first ACCEPTANCE window:
nothing in it opened a new front — it CLOSED the four fronts synthesis pass 7 dispatched.
The strata family reached FOUR accepted members (TR-3′-GEN proved on the three-component
union); ITER-LAW went from open lemma to **ACCEPTED-on-class** (harness tower class,
standard-lift, with all three residuals standing); the corpus's banked-sorry count went
1 → 0 (Lean-core, statement byte-unchanged, capstone footprints unchanged); (H7) acquired
its first compiled non-vacuous decision point (discharging nothing); and the entire
junction/supplier program (DITERSUP J1–J3b/JS1–JS3/P1) is now PRICED with every gate
BLOCKED-ON-APPLICATION — i.e. blocked on ONE Asvin decision, the FGMN three-token repair.
The map's center of gravity has moved: the critical path to the remaining (H1) mathematics
now runs THROUGH the Asvin gate (S3-vii), and the deeper strata (the only probe-first
[M] frontier with a working mechanism in hand) get their wave-18 entry from the accepted
ITER-LAW's carry mechanism (S3-i). This pass also finds one cross-corpus unification
worth a named check (S3-vi: the FGMN double-scale defect vs the V10/V9 steep-read
inconsistency — plausibly ONE root cause) and one cheap Lean collapse (S3-iv:
HullStability's hull layer vs the Scaffold `L6e_covers` blocker).

---

## S1 — THE LEDGER OF THE WINDOW (waves 15–17, compressed, honest)

1. **TR3-S4 ACCEPTED** (cd611af; 4-pass/2-revision arc, model-diverse; F2c verifier-owned
   from-note implementation discharged IN-PIPELINE, 1,108 checks/0 fail at off-roster
   p ∈ {5,7} with 3 mutation controls RED). With accepted TR3-S1/S2/S3: **TR-3′-GEN is
   PROVED on the THREE-component union {all order-2} ∪ {order-3 all-e_j = 1,
   PRINT-UNCONDITIONAL} ∪ {order-3 e₀ = 1, e₁ ≥ 2, e₂ = 1}** — the strata family at FOUR
   accepted members. CONDITIONALITY: component 3 rides (H-ℓ) + the (KP_i)+[T] consumptions
   at S6.2 + the PIN-GEN-i attribution display (Lemma HV3-E2 DERIVED from the pinned
   print); deeper strata (e₀ ≥ 2 stacked; e₂ ≥ 2; orders ≥ 4) OPEN, probe-first, NO
   candidate sealed.
2. **ITER-LAW ACCEPTED** (adjudication 33373cb; arc PC1 NOT-CLEAN 1C/1G → PE1 CLEAN
   (F2c 89,056/0) → errata round 7566ccd (S0 scope erratum + COR-4 mixed-block supplement
   18,396/0) → PC2 CLEAN + PE2 CLEAN, 2-clean model-diverse on the amended text; sealed
   falsifier byte-untouched, re-run 35,232/0 byte-exact). SCOPE AS ACCEPTED: proved on the
   harness tower class (standard-lift; a STRICT SUPERSET of DITER_RESTATE §S2's pinned
   E₂ = e₀e₁ > 1 setting; coincides with the §S2 display on the nose restricted to E₂ > 1;
   the E₂ = 1 leg a harmless COR-3-covered extension outside the statement of record).
   RESIDUALS, ALL THREE, at every use: **(ITER-LAW-LIFT)** open exactly at g₀ = 1 ∧ δ₁ = 1;
   the **gr(w₂)** semantics wrapper rides the accepted GRB retarget consumer-side (NOT
   consumed by the theorem); **orders ≥ 3 untouched**. This closed the (H1)(iii) current
   (pass-7 F4 numbering) end-to-end.
3. **THE BANKED SORRY RETIRED** (15372d1): `npVertices_stable_of_hull_preserved` PROVED,
   Lean-core, STATEMENT BYTE-UNCHANGED, via the new `OM/HullStability.lean` (~1740 lines;
   survivor-corner law `dropCollinear_corner` + flattest-chord validity + glue induction +
   two-hull agreement; BPLL §0 kernels copied verbatim with attribution to keep the
   Scaffold closure off the capstone path). Falsifier 0/4000 + 0/4000 PRE-proof; full
   build green (8698 jobs); `AxChk_baseline` exit 0, ZERO sorryAx in the census, all
   capstone footprints unchanged; `certLevel_stabilizes` now carries only the declared
   NAMED CITE axiom. **Corpus banked-sorry count: 0.** CONDITIONALITY: none (this is the
   window's one unconditional machine-checked upgrade; AxChk is the ground truth).
4. **H7 FORGE — the first compiled non-vacuous (H7) decision point** (037ac6f): sorry-free
   5-site depth-1 instance, 15 decls Lean-core; non-diagonal `branchCellOf` certified 3
   ways; sides = 2 with real SideSplit; (JC-multi) DECIDED TRUE (1·2² = 4); PIN-1
   slope-keyed ε PROVED + pooled form COMPILED-REFUTED; PIN-2 the first contentful
   `KLocCounts` instantiation (1·4 = 2·2). CONDITIONALITY: depth-1 (the HK23/HK25
   transplant is the named residual), node literals synthetic-but-lawful; **NOTHING here
   discharges (H7)** — the row stays [M].
5. **DITERSUP PRICED** (2a25ed9, display-only): `SideReads′`/`ReadsOf′`/
   `DIterJunctionSupplier′` displays (three σ.wPrev tokens; clauses (iii)–(vi)
   byte-identical) + the D2 carrier interface with the gr(w₂) tie displayed as MISSING;
   gates J1(M)/J2(L)/J3a(M, ITER-LAW-FREE via D-REAL degeneration at e₀ = 1)/J3b(L)/
   JS1–JS3(S/S/M)/P1(M)/P0(L). CONDITIONALITY: **EVERY gate BLOCKED-ON-APPLICATION** —
   the three-token repair is Asvin-gated (P0), NOT applied; old-clause records byte-frozen;
   J3b may cite ITER-LAW ACCEPTED-on-class (standard-lift instances only) but still needs
   the tie layer.
6. **FGMN DIAGNOSIS ACCEPTED** (ec64503, the 14-pass/10-revision arc, certified inside the
   anchor at VC34): the print's glue step FIRES at row A; the compiled parity/landing walls
   are TRANSCRIPTION ARTIFACTS of the double-scaled SideReads clauses (i)/(ii); the
   THREE-token diff of record (Defs.lean:285/287/292, σ.w → σ.wPrev). CONDITIONALITY: the
   APPLICATION is the Asvin-gated unit's deliverable (REVISION-9 scope cut: adjudicated on
   the actual scratch-worktree diff + a post-application hostile pass); nothing applied.
7. **THE ANCHOR CASCADE**: acceptance stood at REVISION 33/VC35+VC35b through the window;
   VC36 ran NOT-CONFIRMED 1C/3G on REVISION 34 (F1/F2 fold-lag on the then-in-flight
   ITER-LAW arc, cured by the acceptance; F3/F4 genuine closing-site qualifier omissions,
   cured by r36 errata) → r36 fold = REVISION 35 (the acceptance sweep + the census
   re-sweep 36 → 35; the executing unit died on API 529s and was completed by an audited
   recovery unit, db298d3) → VC36b NOT-CONFIRMED 0C/2G (all four remedies CONFIRMED cured;
   2 new qualifier-propagation gaps at acceptance-citation sites) → r36b errata →
   **VC36c CONFIRMED 0C/0G (f89e60c)** — the SEVENTEENTH re-anchoring: **acceptance at
   REVISION 35 + r36b / VC36+VC36b+VC36c**. The certified perimeter now contains items
   1–6 above. Roster: NINE [M] rows, unchanged all window.
8. **Sorry census of record: 35 repo-wide** (34 code-level tokens in `LeanUrat/` + 1
   tracked scratch probe; B2D 0; OM tree 0; the −1 vs r27 is exactly the retired banked
   sorry). Breakdown: MovesV 10 · MovesU 7 · Scaffold 6 · HC1 5 · MovesT 4 · HC2 2.

---

## S2 — THE HONEST DISTANCE TO THE GOAL

The goal of record: ONE fixed R_τ ∈ ℚ(q) per splitting type computing the density at ALL
primes including wild, formalized, conditional ONLY on published literature
(arXiv:2212.00294 excluded; palindromy/htameFE out of scope by the 2026-07-22 directive).
Two ledgers, kept separate.

### S2(a) — MATHEMATICAL opens (the NINE §3.1 [M] rows + the standing Phase-B items)

Format per item: what it is → what would DISCHARGE it → what literature could carry it
(a published-statement conditionality is GOAL-COMPATIBLE; a campaign conjecture is NOT) →
price (S/M/L/blocked).

**(H1) — the graded-dictionary family at OM orders ≥ 2** (four displayed members + the
strata/ITER currents). The row's live residuals after this window:
- *(a) (GR-B) orders ≥ 2* — the composed anchored-march residue-structure package.
  Discharge: write the composed march (the per-stratum TR3-S1..S4 arcs are its accepted
  instances). Literature: modeled statement-for-statement on printed FGMN Thm 4.2 (both
  pairings) via the LITUNIT — the march is transcription + composition against print, the
  genre HV3/HV3-E2 already executed ("derived from the pinned print"). Price: L
  (composition), with the literature pins already on file.
- *(b) GD-1 beyond order 1* (representative admissibility, OL-2a-1). Literature: FGMN
  Lemma 5.3 carries the properness-proviso LEG only ([T] row); the admissibility family
  itself is open. Price: M/L.
- *(c) GD-6 conformance instances at orders ≥ 2* — routes on record, not proofs. Engine
  conformance: NOT literature-carryable. Price: M.
- *(d) the order-≥ 2 KEY1 block* (read-order-quantified). Price: M/L.
- *Strata current*: TR-3′-GEN accepted on the three-component union (conditions in S1.1);
  OPEN beyond it: e₀ ≥ 2 stacked, e₂ ≥ 2, orders ≥ 4 — probe-first, no candidate sealed.
  Discharge route: S3(i)'s level-3 cocycle probe. Price: M probe → L compose per success.
- *(ITER-LAW-LIFT)* — general lawful (non-standard) lifts, open exactly at g₀ = 1 ∧ δ₁ = 1.
  Discharge: S3(iii)'s degeneration attempt (at g₀ = 1 the level-1 extension is trivial —
  the corner is degeneration-shaped). Not literature. Price: S/M probe-first.
- *the gr(w₂) carrier tie* (DITER §S3's displayed-missing tie layer; also J3b's
  prerequisite). Discharge: S3(ii)'s design note — state the tie as a consumer of the
  ACCEPTED ITER-LAW's ANCHOR-VAL/PAIR-VAL + the accepted GRB retarget, not a fresh
  mechanism. Not literature. Price: M.

**(FRESH)** — O-9's joint-with-parent level-determinacy separation clause (c). Numerically
unviolated over 8.2M boxes; K1 consumer face discharged at the v4 chain (compiled
supplier); the residual is the §B2-DEF non-ledger general supplier — theorem-strength.
Not literature. Price: L (no cheap artifact; background until a mechanism appears).

**(ADM)** — O-9 census admissibility at FULL attainment. CLASS-LAT is now Lean-core
machine-checked at every order (the F-ADM-3 re-key); displayed opens = (B-PIN) + the
(C-JCT) fan-out audit (now on the honest carrier). Not literature. Price: M (audit-genre).

**(UB-X)** — the clause-(UB) residue: (a) per-leaf O_δ re-base audit ([T]-shaped);
(b) δ-stable position classes / the (K3-δ) fragment consumed by clause (R); (c) solve +
aggregation commute with unramified base change. The RB-DELTA re-scope is DECISION-READY
(the walk is on file, consumer-visible verdict); condition (5) = Asvin sign-off is the
only open gate. Price: M, mostly audit + the gated decision.

**(PACK)** — D-11's clause 4, the actual-pack correspondence + four row-laws. Routes:
(a) n = 3 transcription + booking (L, background), (b) the O-14d construction. Engine
bookkeeping: not literature. Price: L, unstaffed background.

**(H6)** — the terminal-seam group (T-DEC)/(T-READ)/(T-VERD) + D-11 item (vii). The k′ = 0
seam is now THEOREM-BACKED (`BasePolygonLengthLaw` proved every prime →
`terminalPolySem_kp0_unconditional`). **C2 STAYS BLOCKED at k′ ≥ 1 — still no mechanism**
(the III-G3a/G4/G6 typeclass wall); the sanctioned unit remains the GDOrder1
typeclass-repair DESIGN NOTE (carried three passes now — see S4 disposition). The −∞
corner and the (T-DEC)/(T-VERD) construction routes: background. Price: C2 blocked;
rest M.

**(H7) = (I-τ)/K-LOC** — the sibling-interface conformance pair. Now the best-instrumented
[M] row: vacuous-by-type at every pre-forge compiled instance, first-contact survival at
six real m ≥ 2 sites, two measured pins, and (this window) the first compiled decision
point with the pin-corrected faces decided TRUE and the pooled strengthening
compiled-FALSE. Named residuals: the HK23/HK25 depth-2 transplant (M); `ITauShadow`
(Tier-S, M); H7-W3a engine-tied semantics (M/L). Engine conformance: not literature.

**(H8′)** — the K-CUT residual interfaces: (i) GRAFT completeness/bijectivity (blocked;
the optional n2 bijectivity check STILL unexecuted, third consecutive carry), (ii) MASS-ID
(blocked-on-owners), (iii) POOL-DICT (decided; blocker COMPILED — blocked-on-owners
[2r]/CL-8 REL.2 + (K-CUT-g)). Price: blocked (no agents without a new mechanism).

**(E-N)** — O-1thr Thm 3(c)'s exhaustion caveat. Dormant by design; witness-backed; only
mover = a future CU-4 full walk. Price: blocked/dormant.

**Standing Phase-B items outside the roster** (the density-capstone chain, parked behind
the math-first criterion but part of the FULL formalized goal): `htameFE` (palindromy —
OUT OF SCOPE by directive, carried as the capstone's honest hypothesis, certified
non-trivial); `hExhaust`/drainage at general n (discharged unconditionally at n = 2 by
`hExhaust_n2`; open beyond); the wild self-loop `h_node` legs (`SelfLoopHNode`, menu
growth beyond order 0); order-≥ 2 classifier faithfulness (the Lean OM tower, standing
obligation 2). Literature status: the Montes-paper axioms (`om_leaf_faithful`, the GMN
index cite, Serre DVR) are the on-record GOAL-COMPATIBLE conditionality pattern; drainage
and the self-loop resummation are engine content, not citable.

### S2(b) — LEAN-SIDE opens (exact, per the r36 census of record)

**The 35-sorry census** (34 code-level tokens in `LeanUrat/`, strict non-comment unit,
+ 1 tracked scratch probe `scratch_S7_escalation.lean`; B2D 0; OM tree 0; NONE banked;
NONE on the capstone path — `AxChk_baseline` prints zero sorryAx):
- **MovesV 10 · MovesU 7 · MovesT 4** (21): the parked wave-4/bridge-era instantiation
  debt of the `theoremU` interface campaign — E-phase opens behind named blockers
  (RealizationComplete/IFK faces, the `hfin : Finite (Skeleton n)` ratification, wave-D
  carrier duties, the parked items 22–29). Status relative to the goal: NOT on the
  math-first critical path; ON the eventual formalization path (the goal is a complete
  FORMALIZED proof) — they re-activate when the ROOT-era mathematics drains and the
  bridge instantiation resumes. Parked, correctly.
- **Scaffold 6**: the live Lean mirror of the ROOT movements — `O12/Semantic.lean`
  `L6e_covers` (blocked on the finite lower-convex-hull layer shared with un-landed
  II-M6 — see S3(iv): HullStability plausibly supplies this NOW), `MeasureFloor/SumOne`
  (I-F2, statement-fenced), `DictIII/Window` (III-T12, awaits III-T11b/T13),
  `DictIII/Devid` (III-G16 FALSE-as-stated, compiled countermodel, statement adjudication
  owed), `DictIII/CU1` (W3 row, fenced), `ValueSide/Transfer` (BP_IV T7,
  dedicated-campaign row). These ARE goal-path Lean (the ROOT composition's mirror).
- **HC1 5** (V9_K1nonrec ×4 + CL04_addrDichotomy) + **HC2 2**: the honest HK-52/HK-11
  reopenings at the steep-read seam + the K1-cone remainder. See S3(vi): plausibly
  re-shaped or retired by the FGMN application ripple — do NOT spend prover effort on
  them before the application decision.

**Task clusters**: TV_F2–F6 (the ratBurdens seam chain) sits behind the named Phase-B row
`RealizationComplete` (= M18 MarkGraft, an IFK face) — blocked, correctly parked.
TV_E1–E7: the E-cluster is sorry-free (TV_E7's LedgerIV assembly PROVED); the residual
is the `hfin` ratification row (V7_livC), a parked sign-off item, not a proof gap.

**Gated programs**: the FGMN application unit (P0, L, Asvin-gated — the three-token
σ.w → σ.wPrev diff + the primed-mirror set `ReadsOf′`/`TruncRunFrom′`/`GlueJunction′`/
`readsOf_glue′`/`ftie_extends′` + mandatory gate re-runs, adjudicated on the actual
scratch-worktree diff + a post-application hostile pass); behind it the whole DITERSUP
gate ladder (P1 → JS2/JS1 → J1 → J3a → JS3 → J2; J3b additionally needs the gr(w₂) tie
layer). P1 (the D2 carrier layer) and JS1/JS2 statements can PRECEDE the application
(no dependency) — queued in S4.

---

## S3 — UNIFYING IDEAS (the pass's core duty)

**(i) ACCEPTED ITER-LAW + the carry algebra D = F[u]/(u^e − z̄) as the uniform deeper-strata
induction — ACCEPT as the probe-first candidate (not as a claimed route).**
The four accepted strata arcs (TR3-S1/S2/S3/S4) each re-derived, per stratum, the same
two ingredients: an eq-(12) carry bookkeeping at each level and a Bézout-normalized unit
(ε₁, then the TWIST-3 law θ₂ = ℓ₁g₁γ₂ measured EXACT at order-3 e₁ ≥ 2). ITER-LAW's
accepted content is exactly the level-2 composition law for these units — the
two-constant cocycle c(γ,γ′) = z̄^{δ₂}·z₁^{(s₁(γ)+s₁(γ′)−s₁(γ+γ′)+ℓ₀γ₂δ₂)/e₀}, proved by
CARRY-1 at BOTH levels + the two-line Bézout kernel — and the MOVES pass-7 carry algebra
D = F[u]/(u^e − z̄) is the same mechanism in structural form (the u-relations are the
carry identities; ε and z̄^δ are images of the u-generator). The natural candidate:
**ITER-LAW-r** — a level-indexed cocycle on r-level harness towers, constants
(s_{r−1}-carry + ℓ_{r−2}γ_r δ_r)/e_{r−2}, with the per-stratum transports of TR3-S1..S4 as
its instances and the twist exponents θ_j = ℓ_{j−1}g_{j−1}γ_j as its unit part. If it
measures exact, ONE induction replaces the remaining per-stratum arcs (e₀ ≥ 2 stacked,
e₂ ≥ 2, orders ≥ 4 all-e_j-mixed). HONESTY FENCES: the accepted ITER-LAW says NOTHING at
orders ≥ 3 (residual 3); the [M]-row protocol binds — probe-first, sealed preregistration,
no candidate is "sealed" until the harness extends to 3-level towers and the composed law
survives 0-violation; the (ITER-LAW-LIFT) residual (g₀ = 1 ∧ δ₁ = 1) propagates to every
level. RETIRES on success: the (H1) strata current's whole open perimeter collapses to
one composed theorem + per-level instantiations. Wave-18 queue #2.

**(ii) Anchor-monomial transport + PAIR-VAL as ONE transport lemma — REFUTE as stated;
ACCEPT the narrower gr(w₂)-tie version.** As a single lemma covering "the remaining
TWIST/DIG legs" this fails today on two facts: the two mechanisms live in different
formalisms (the MOVES carry-algebra/(z+c)^a anchor language vs the harness eq12/ε₁
calculus), and the MOVES §B2-FINAL consolidation — the home the DIG legs live in — was
NOT accepted (pass 8: 5 crit/2 gap, structural; per its own record the definitive
write-up is a dedicated future unit). A unification lemma would have to bridge an
unaccepted formalism, importing its risk. What DOES unify NOW: the **gr(w₂) carrier tie**
(DITER §S3's displayed-missing layer, J3b's prerequisite) should be DESIGNED as a
consumer of the accepted ITER-LAW's L6 ANCHOR-VAL + L7 PAIR-VAL plus the accepted GRB
retarget — the tie is precisely "the graded piece of the level-2 weight filtration is
the carry algebra's graded piece", which is PAIR-VAL's content read functorially. Concrete
deliverable: a tie-layer design note stating `DIterCarrier.grTie` with ITER-LAW
(ACCEPTED-on-class, standard-lift, three residuals displayed) as its only nontrivial
input. RETIRES: the tie-layer design question; unblocks J3b's non-application
prerequisite. Wave-18 queue #4.

**(iii) The D-REAL degeneration trick at OTHER gates — ACCEPT, narrowly, at exactly two
targets.** The trick (J3a): at e₀ = 1 the carry algebra degenerates (D = F[u]/(u − z̄) ≅ F),
so the digit-algebra content trivializes and the gate consumes NO ITER-LAW. Where else it
bites: **(a) (ITER-LAW-LIFT) at its own open corner g₀ = 1 ∧ δ₁ = 1** — at g₀ = 1 the
level-1 extension is trivial (K₁ = K₀, z₁ a scalar), so a general lawful lift differs
from the standard lift by scalar-valued data; the cocycle comparison becomes a
scalar/unit computation — the corner is degeneration-shaped, and a targeted S/M
probe-first attempt is warranted. **(b) JS2 itself** (the degeneration collapse gate) —
already priced S, can precede the application. REFUTED elsewhere: (H6) C2 is a typeclass
wall, (H7) is conformance/type-vacuity, (H8′) is blocked on owners — none is a valuation
degeneration. RETIRES on success: the last residual INSIDE the accepted ITER-LAW's order-2
scope. Wave-18 queue #5.

**(iv) The HullStability survivor-corner method at the classifier's npVertices-adjacent
opens — ACCEPT for the Scaffold hull blocker; REFUTE for order-≥ 2 faithfulness.**
The order-≥ 2 tower's open is the citation/architecture layer (the per-digit read cutoffs
= the arising-key-restricted GMN cite + the fixed-ambient-field design), NOT hull
geometry — `childResidualExt_reduce_stable_of_cuts` already reduced it there; nothing for
the corner law to do. But the Scaffold census's `L6e_covers` (II-M9's coverage leg,
`O12/Semantic.lean:1607`) is blocked on EXACTLY "the finite lower-convex-hull layer
(shared with un-landed II-M6)" — and `OM/HullStability.lean` now proves ~1740 lines of
that layer (valid-line constructions, chord attainment, bracket covers, two-hull
agreement) in plain Finset (ℕ × ℕ) geometry. Concrete unit: audit the exact hull
statements `L6e_covers`/II-M6 need against HullStability's §B–§F kernels; if they match,
re-point (import direction: Scaffold may import OM, not conversely — checked no-cycle at
the BPLL copy) and retire 1 of the 6 Scaffold sorries. Price S/M. Wave-18 queue #6.

**(v) One literature statement carrying several [M] rows — PARTIAL-ACCEPT, and an honest
negative finding.** The positive half: (H1)(a)'s order-≥ 2 march is FGMN-pinned
statement-for-statement (Thm 4.2 both pairings, Lemma 5.3 for the properness leg), and
the window PROVED the transcription genre works at acceptance grade (HV3-THM and HV3-E2
both "derived from the pinned print"; PIN-GEN-i discharged with a fresh column-verified
extraction). So the (H1) print side is literature-carried ALREADY — what remains is
OUR-side composition, not citation. The negative half, which the queue should respect:
**every other [M] row is engine-conformance or campaign-internal** — (FRESH), (ADM),
(PACK), (H6)'s seam group, (H7), (H8′) state facts about the project's OWN engine
presentation, and no published statement can carry them by construction. Consequence:
the literature-only conditionality class is reachable ONLY by proving the conformance
rows (or restructuring them away); no citation shortcut exists. RETIRES: the recurring
temptation to hunt for an FGMN/GMN cite for the conformance rows — stop looking.

**(vi) NEW (not on the candidate list): the FGMN double-scale defect and the V10/V9
steep-read inconsistency are plausibly ONE root cause — CHECK AT APPLICATION TIME.**
The accepted FGMN diagnosis: SideReads clauses (i)/(ii) double-scale the stride weight
(σ.w where the print keys the parent valuation σ.wPrev), producing compiled walls at
e ≥ 2 reads. The V10 finding (2026-07-28h): HistoryCoherent's recorded read-transition is
INCONSISTENT at steep reads (e·g ≥ 2), making the K1 kernel's steep legs
vacuous-by-inconsistency — the SAME e ≥ 2 read seam, the SAME weight-keying layer, found
independently five days earlier. If the three-token repair cures (or re-keys) the V10
inconsistency, the HC1 V9_K1nonrec ×4 sorries (the HK-52/HK-11 reopenings) and the Q1
HistoryCoherent sign-off cluster collapse into the SAME Asvin-gated application ripple.
NOT ASSERTED — the claim is checkable only on the actual application diff; the
application unit's charge should carry an explicit rider: "audit whether the σ.wPrev
re-key resolves, re-keys, or leaves the V10 steep-read inconsistency and the V9 sorry
family." RETIRES on confirmation: 4 HC1 sorries + a standing sign-off item, unifying two
obstruction records into one repair. Rider on the Asvin-batch item (queue #1).

**(vii) The window's structural finding: the critical path runs through ONE decision.**
Not a new lemma — a dispatch fact the queue must encode. After this window, (H1)'s live
non-strata currents (junction/supplier program, J1–J3b), the SITE-EXH residual chain, the
gr(w₂) tie's consumption site, and possibly the V9/V10 family (S3-vi) ALL sit behind the
FGMN application gate (P0). Everything else in wave 18 is chosen to be parallel to that
gate (probes, design notes, carrier statements, hull audit). The Asvin batch is not one
queue item among thirteen — it is the schedule's articulation point.

---

## S4 — THE WAVE-18 QUEUE (hardest-first; genre · price · falsifier/spec gate · GOAL-relevance)

1. **The Asvin decision batch** (standing, zero-cost to us, THE articulation point —
   S3-vii). Four items, unchanged: the FGMN three-token application (P0; TOP, fully
   verified diagnosis; deliverable = scratch-worktree diff + post-application hostile
   pass; NEW RIDER per S3-vi: the V10/V9 steep-read unification audit);
   TowerMoveClosureL two-layer re-ratification; fence-(vii) ROUTE (a) ratify-or-reopen
   (incl. RECSL hσt rider + the executed F-ADM-3 re-key); Route-B RB-DELTA (decision-ready
   per the walk). GOAL: unblocks every DITERSUP gate + the (H1) junction current.
   **ASVIN-GATED.**
2. **The deeper-strata probe: ITER-LAW-r / level-3 cocycle** (numerics, M; S3-i).
   Falsifier-FIRST: extend the harness to 3-level towers (standard lift), preregister the
   composed-cocycle law (constants + θ_j = ℓ_{j−1}g_{j−1}γ_j unit part) SEALED pre-run,
   measure on e₂ ≥ 2 and e₀ ≥ 2 stacked strata + mixed blocks (the COR-4 lesson: include
   mixed blocks in the FIRST battery). Spec gate: an exact 0-violation law on ≥ 10⁴
   samples with mutation controls before ANY compose unit charters; on failure the resid
   is itself the finding. GOAL: the only open [M] frontier with a working mechanism —
   collapses the per-stratum arc treadmill.
3. **(H7) next decision: the HK23/HK25 depth-2 transplant onto the forge** (Lean, M).
   Coherent 2-node continuation chains under `forgeLedger` (the gates prove such
   histories exist over F4; transplant to the forge's ZMod 2 box). Spec gate: the sides-2
   site upgraded from ledger-data hull to REALIZED continuations, all decls Lean-core,
   nothing claimed discharged ((H7) stays [M]; H7-W3a remains the Tier-M obligation).
   GOAL: the named residual of the row's first decision point.
4. **The gr(w₂) tie-layer design note** (design, M; S3-ii; DITER §S3's missing layer).
   Statement `DIterCarrier.grTie` consuming ONLY: ITER-LAW ACCEPTED-on-class
   (standard-lift; residuals (ITER-LAW-LIFT) at g₀ = 1 ∧ δ₁ = 1, gr(w₂) wrapper
   consumer-side, orders ≥ 3 untouched — displayed at every use) + the accepted GRB
   retarget. Spec gate: Codex plan-review before drafting (the plan-level-adversarial
   rule); no new mechanism admitted. GOAL: J3b's only non-application prerequisite.
5. **(ITER-LAW-LIFT) corner attempt at g₀ = 1 ∧ δ₁ = 1 via degeneration** (math, S/M;
   S3-iii). Probe-first: measure general-lawful-lift vs standard-lift cocycle deltas at
   the trivial-extension corner (z₁ scalar); attempt only on an exact measured law.
   GOAL: closes the last in-scope residual of the accepted ITER-LAW.
6. **The `L6e_covers` hull-layer re-point audit** (Lean, S/M; S3-iv). Map II-M9/II-M6's
   needed hull statements onto `OM/HullStability` §B–§F; re-point if exact; spec gate:
   statement byte-unchanged, AxChk clean, no new import cycle. GOAL: −1 Scaffold sorry on
   the ROOT-mirror path; first reuse dividend of the banked-sorry unit.
7. **P1 + JS1/JS2 statements** (Lean statements, M + S/S; application-independent per
   DITERSUP §S5). Land `DIterCarrier`/`IterLaw`/`RowNPattern` etc. as reviewed statements
   + the two cheap sanity gates. Spec gate: every displayed definition is a NEW-statement
   site under the trust boundary — statement review owed, no proof claims. GOAL: shortens
   the post-application ladder to J1 → J3a → JS3 → J2.
8. **The r37 fold + VC37 cadence** (doc, S): fold THIS pass as the dispatch of record
   (the f89e60c close already displays "synthesis pass 8 in flight"); census baseline
   corrections per the r36b erratum precedent. Blocker: none.
9. **Optional S batch — final disposition** (third consecutive carry; per the pass-5/6
   dropped-again rule these now get named dispositions, not another carry): (a) hsc
   two-instance sign witness — fold into any HC1-touching unit or DECLINE at r37;
   (b) GRAFT n2 bijectivity check — fold into #6's Lean session or DECLINE; (c) the C2
   GDOrder1 typeclass-repair design note — KEEP (sole sanctioned C2 unit, no agents
   until a mechanism).
10. **Background/BLOCKED (no agents without a new mechanism)**: (H8′)(i)/(ii) proper;
    (PACK) route-a; (E-N); C2 beyond the design note; (FRESH)'s general supplier; the
    declined forges (stay declined); the parked bridge-era sorry clusters (MovesV/U/T —
    re-activate only at the bridge-instantiation resume; S3-vi may retire the HC1 four
    earlier via #1's rider).

Qualifier discipline rider on every wave-18 artifact (the VC36b lesson — the defect class
survived even into acceptance citations): acceptance citations are PASTED from the
ledger's defined-term block, never re-typed from memory; the ITER-LAW citation ALWAYS
carries class + all three residuals; the strata union ALWAYS carries component 3's
conditions; the H7 forge ALWAYS carries depth-1 + synthetic-but-lawful.

---

## S5 — WHAT THIS PASS CHANGED (deltas vs the pass-7 map)

Pass 7 mapped a window that OPENED a source question (junc_bundle instance-false → the
FGMN charter) and queued four hard attempts; this pass maps the window that CLOSED them:
its queue went 10/10 executed with zero dropped-again items (the (UB-X) walk's named
dispatch landed; the optional batch alone carried, now force-dispositioned at S4-9), and
every non-Asvin item converted — ITER-LAW open-lemma → ACCEPTED-on-class (three residuals
standing), strata family 2 → 4 accepted members with print-unconditional order-3
all-e_j = 1, banked sorries 1 → 0, (H7) no-decision-point → first compiled decision
point, junction program unpriced → fully priced-and-gated. The map's shape changed in
three ways: (1) the critical path now has an ARTICULATION POINT — one Asvin decision
(P0) gates the junction/supplier current, the SITE-EXH residual, the tie layer's
consumption site, and plausibly the V9/V10 sorry family (the new S3-vi unification
candidate); (2) the deeper-strata frontier acquired a MECHANISM (the accepted carry/
cocycle route) and hence a legitimate probe-first entry (ITER-LAW-r), where pass 7 had
"probe-first, no candidate"; (3) the literature question is now SETTLED negatively for
the conformance rows (S3-v): only (H1)'s print side is citation-carried — the rest of
the distance is proof or restructure, and the queue stops hunting cites for it.

— Synthesis pass 8, 2026-08-08 (wallclock 2026-08-03).
