# SYNTHESIS PASS 5 (2026-08-08) — post-hardest-first synthesis + queue

Charge: the /goal's standing synthesis clause. Sources: PROJECT_STATE r23; ROOT REV 22
(§3.1 NINE rows, §28); SYNTHESIS_PASS4; HARDEST_FIRST_ORDER; ledger tail
355c1eb→5a10256; B2D state; GRB R11.5; B-M2 REV 15.

HEADLINE. Hardest-first Tiers 1–3 ALL EXECUTED, verified (probe 355c1eb; V9 d632305;
TD arc→9a10109; CL5 942becd; GR suppliers a5911fd; strata 8db1b00); Tier 4 background
(U10 closed; rest open). All five architectural risks resolved WITHOUT core
re-architecture: SITE-EXH needed only an ADDITIVE carrier; V9 = vacuity-rescope; the TD
failures were transcription seams, not math; D-ITER caught by measurement.

## F1 — countermodel-first is the proven engine; make it the entry gate

Every hard hole that CLOSED did so only after a compiled refutation pinned the exact
statement: TD-3 v1→v2→v3 PROVED (N-TD1 gating each round); V9 e′≥2 closed-as-question
by `readsOf_e2_s0_zero_empty`; D-ITER's cyclic form killed by measurement; TR3-S1 rode
a green falsifier. Action: countermodel/instance-first = the ENTRY protocol for
remaining [M]-row campaigns. Targets, cheapest first: (i) huni/SlotUniformLaw — decide
it at the compiled tdL r=1 instance (near-free; if FALSE, the huni face needs
restatement before supplier work); (ii) ADM-U0 triangular repair (known defect,
authority on file) before ADM-U6a/U6b; (iii) (H8′)(iii) POOL-DICT attempt (pass-4 F2a,
never ran).

## F2 — the GRADED-READ shape (supplier + vacuity scoping) transfers to REC-SL

REC-SL proper (the IsReadLift-at-(1,1)/(SL-top)/(SL-dev) digit legs — B-M2's one
untouched open) lives at recentering configurations, and the window compiled exactly
that supplier layer: IsRecentering clause 1 forces σ.e = 1 (CL5's legality discovery),
`node_hψ0` at a recentering IS the c̃≠0 pin, `child_e`/`child_h` + `hspecRec` tie
frames to reads (GR0b). Action: ONE attempt-grade unit — derive the three digit legs
history-level from GR0b, steep face fenced to kp_step per `readHyps_e_eq_one`. Similar
≠ same: digit laws are not graded identities; only the supplier layer is shared;
outcome = suppliers or compiled obstruction.

## F3 — the narrowed readsOf debt: a consumer-need question, not a proof hole

The landing CONTENT is f-tied-witnessed on the ReadsOfFrom face (`gate_ftie_chain`,
`landingKey_ν₁rcRoot`); only the ReadsOf-SPECIES antecedent is uninstantiable.
Statement-level: a general same-f From→ReadsOf converse is DEAD — C-e is a real tower
whose recentering hosts nowhere on its own f (the extension exists because the converse
fails); the free-f converse = OnTruncatedCarrier's pre-E-3 weakness. Live glue route =
junc_bundle (D-1 wall) + a ROOT-FRAME-restricted converse (plausibly cheap: base pin
restored; junc_bundle supplies the interior SideReads). Action: one B-M2 walk — does
the (K0) recentering-branch consumer accept the truncated-carrier witness? If yes,
restate the debt at ReadsOfFrom + no-consumer disposition of the species form; else
both surviving routes funnel into F5.

## F4 — TR3-S2: the measured TWIST-LAW makes stratum (ii) the TR3-S1 genre

TR-3′-GEN(ii) now has exact shape (C^F_j = z1^(l0·g0·h0·s_j)·c^G_{s_j}, 0/3,640) + a
green falsifier — the finite-algebra-leaf precondition TR3-S1 cashed. ONE compose unit
threading the slot-keyed character; if accepted, TR-3′-GEN closes as a two-stratum
THEOREM — GRB open lemma #1 GONE; display the TR-3-ORD-X-on-(ii) consequence inside
the unit. The D-ITER RESTATEMENT (two-constant ITER-LAW; cyclic fenced) is separate
hygiene, must precede any DIterJunctionSupplier work — do NOT merge (character law ≠
digit-algebra iteration).

## F5 — three opens funnel into ONE missing artifact class: a deep compiled instance

junc_bundle's merged gate (a deg f > deg Φ₁ run), the readsOf direct-witness route, and
GR-3×2/GR-4's gap countermodel (a p≠2 stage forge; ambient F4 blocks it) all wait on
one deep compiled object. Two species: run forge vs stage forge. Charter ONE forge
campaign, named targets, AFTER F3's walk and the GR decision adjudicate need.

## F6 — pass-4 execution audit + dead weight

EXECUTED: queue 1–5 (VC22; D-11 bar met; TR3-S1 ACCEPTED; §B2-DEF → B2D, TD front
sorry-free + TransDeep supplier compiled; P(O-8b/M14) ACCEPT → (H4a) RETIRED, NINE
rows). DROPPED AGAIN (pass-4 F5's own failure mode): POOL-DICT, TerminalPolySem k′=0,
TR-4 family, (ADM) track B. Fix: ONE named small-units dispatch, not queue tails. Dead
weight: TDDefs/GRDefs E-phase headers read campaign-wide sorry ledgers (actual: 3 GR
sorries); the r22 "wave 1 next" pointer stale; B2D MANIFEST sync owed — rides VC25.

## REVISED PRIORITY QUEUE (value/cost; blockers)

1. **VC25 on REVISION 22 (→ r24 cadence)** + F6 hygiene in-batch. Blocker: none.
2. **Asvin zero-cost batch**: TowerMoveClosureL re-ratification; Route-B re-scope;
   review queue. Blocker: Asvin.
3. **TR3-S2 (F4)** — cheapest genuine math; closes a GRB open lemma outright.
4. **GR-3×2/GR-4 disposition** — owner call (frame pins into ReadHyps vs history-keyed
   graded_read) + p≠2 countermodel pricing; ends B2D at 0 sorries or a displayed
   interface change. Blocker: §4 statement-change call (B-M2 §S4 consumer).
5. **REC-SL supplier question (F2)** — attempt-grade on GR0b.
6. **B-M2 readsOf-debt consumer walk (F3)** — possible discharge by restatement.
7. **Countermodel-first batch (F1)**: huni@tdL → ADM-U0 → POOL-DICT.
8. **Small-units dispatch (F6)**: TR-4 harness family; TerminalPolySem k′=0; leakFree
   general-instance face.
9. **D-ITER restatement** (hygiene; precedes 11).
10. **Deep-instance forge (F5)** — after 4 and 6 adjudicate need.
11. **DIterJunctionSupplier formalization** — blocked on 9; priced with 10.
12. Background/blocked: (H6) C2 typeclass repair (no agents without a new mechanism);
    (H7) faces; (H8′) remaining interfaces; (E-N) witness-backed; deeper-order strata
    (numerics). F8/F9 duties stay folded in the next dispatch templates.

— Synthesis pass 5, 2026-08-08.
