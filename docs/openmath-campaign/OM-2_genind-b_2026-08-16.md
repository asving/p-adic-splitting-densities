# OM-2 — GENIND.B, THE INDUCTIVE STEP `P(<n) ⟹ P(n)`: OUTLINE + CERTIFICATION

**Unit:** OM-2 of `docs/OPENMATH_CAMPAIGN_2026-08-16.md` (wave 1). **Ledger row:** T-1, the
terminal open item of `blueprint/CHAP-I_capstone_conditionality.md` §3.9.
**Mandate fence (verbatim, CHAP-H NODE H.98):** *"The reduction's inductive step has **no node**
in chapter H … **A node claiming `GENIND.B` must be returned.**"* This unit claims nothing: the
deliverable is the certified skeleton a future proving campaign runs on.
**Author:** OM-2 (fable, max reasoning), 2026-08-16.
**Battery:** `verification/openmath/OM2_genindb_battery.py` (exact-rational, exhaustive at its
cells, `q ∈ {2,3}`, BOTH characteristics: `ℤ/q^N` and `𝔽_q[t]/t^N`).

---

## 0. The target, stated exactly

**T-1 (CHAP-I §3.9, verbatim):** *"THE INDUCTIVE STEP. `(IH) + [GENIND-H(n)] ⟹ P(n)` at
`n ≥ 4` — the content of Display A's hypothesis block as a single implication. NO NODE ANYWHERE
(honesty I-2, the GENIND.B fence). … its Lean-facing shadow is exactly "supply
`InductionPackage n` at every `n`" (H.98's `hp`)."*

**Corpus form** (`spec/EFF-GENIND.md` `.13`–`.15`): fix `n ≥ 2`; the package
`P(k) := {history-resolved exact menu (HM3.A-form) + finite (A1)-admissible aggregate menu +
(A0) + exact-complement (A2)}`, with `(A2)` read WITH the `(A2-RATE)` species pin (`.151`) and
`(CS-EXACT)` on the box (`.14`). Assume `(IH) = P(k)` for `2 ≤ k < n` and `[GENIND-H(n)]`
(the clause family `(CS-1)`/`(CS-1Q)`/`(CS-2)`/`(CS-3)`/`(CS-EXACT)` for every composite-stage
genre realizable at degree `≤ n`; at `n ≥ 8` widened by `[GENHN-HE(μ ≥ 3)] + [GENHN-TOW-1]`,
`.176`). Then `P(n)`. The proof is §S6 Steps 1–4; Step 5 (fire `W-12.D`) is landed as H.98 and
is out of this unit's scope.

**Landed Lean shadow** (`leanfinal/Uniformity/ChapH/H95.lean`, verbatim):

```lean
def InductionPackage (n : ℕ) : Prop :=
  ∃ (K : ℝ) (B c : ℕ), 0 ≤ K ∧
    ∀ (O : Type) [CommRing O] [IsDomain O] [IsDiscreteValuationRing O]
      [IsAdicComplete (IsLocalRing.maximalIdeal O) O] [Finite (IsLocalRing.ResidueField O)],
      (∃ S : Finset FactorizationType, CoveringMenu O n S) ∧
        Induction.RateSpecies (residueCard O) K B c (undecidedSeq O n)
```

**STRUCTURAL OBSERVATION S-0 (load-bearing for the whole outline).** The menu clause of
`InductionPackage n` is a LANDED THEOREM at every degree:
`Uniformity.Density.exists_coveringMenu` (CN-22, `Uniformity/Density/CoveringMenus.lean:59`)
proves `∃ S : Finset FactorizationType, CoveringMenu O n S` unconditionally. Therefore

> **the entire Lean-facing content of T-1 is the RATE clause:**
> `∃ K B c` uniform in `O` with
> `Induction.RateSpecies (residueCard O) K B c (undecidedSeq O n)` — i.e. `(A2-RATE)` at
> degree `n` for the FULL space, `undecidedSeq O n M ≤ K·M^B·q^{−(M−c)}`.

The corpus's other two members are NOT dropped by this observation; they moved:
* **(A0)** and the exact menu WEIGHTS (the value layer) are the other capstone hypothesis,
  `UniformityStatementDecided` = H.98's `hdec` — a SEPARATE track (rows HYP.24/25/26 + the
  per-degree value program). The blueprint's own signature note at H.95 records the split.
* **(CS-EXACT)** is separated from the rate by `R6.2` (`EFF.GENIND.170`): only the
  exact-complement IDENTITY consumer needs it; the rate consumer needs monotonicity + the union
  bound (landed H.63). The identity consumer lives on the value track.

So the outline below runs **two tracks through the same five steps**:
**TRACK R** (rate/drainage — what `hp : ∀ n, InductionPackage n` needs) and
**TRACK V** (value/menu — what `hdec : UniformityStatementDecided` needs). T-1 proper is
TRACK R; an outline that silently dropped TRACK V would misstate GENIND.B, whose `P(n)` carries
all four members.

**STRUCTURAL OBSERVATION S-1 (the two complements — a distinction the battery made exact).**
The corpus's recursion object `u_{m,d}` is the **CONSERVATIVE** complement (the reader exits
UNDECIDED on `v(a₀) ≥ N`, deciding on visible data only); `leanfinal`'s `undecidedSeq` counts
the **SEMANTIC** undecided set (`UndecidedAt` = no `σ` has all lifts of that type). These are
NOT equal: the battery exhibits **conservative ⊋ semantic from `(n, N) = (2, 3)` on** (finding
F-2, §3: reader 12 vs the landed `undecidedCount_two_eq` value `q^N = 8` at `q = 2, N = 3`).
The safe direction is a theorem shape (a conservative certificate is a semantic one:
reader-decided ⟹ `DecidedAt`), so `undecidedCount O n N ≤ (conservative count)` and every
TRACK-R consumer is fine — but the future campaign's terminal bridge is an INEQUALITY, never
an identity, and any node identifying the two objects must be REFUTED on F-2's witnesses.
(Chapter H's §16 item 6 warned against identifying a *different* pair — full-space vs cluster;
F-2 adds the conservative-vs-semantic axis with explicit witnesses.)

**Base cases (landed, cited for grounding):** `package_two : InductionPackage 2` (H.96,
unconditional, from chapter G's exact `n = 2` law) and
`package_three_of_rate (hrate : …) : InductionPackage 3` (H.97; `hrate` is OPEN-MATH HYP.08,
**owned by unit OM-1** — not this unit's; note `hrate` is stated about `undecidedSeq`, the
SEMANTIC object, so per S-1 it follows from a conservative-side rate plus the certificate
bridge — the same two-part shape as the general step's 4h below).

---

## 1. THE OUTLINE — the step decomposition, with per-step status

Statuses: **LANDED**(decl) = machine-checked in `leanfinal` (Lean-core axioms);
**CONTRACT**(node) = committed blueprint node, Lean pending; **CITE** = gate-(b) literature
axiom, owner signature pending; **REDUCES-TO**(row/debt) = named OPEN-MATH row or named
transcription debt; **CERTIFIED-OPEN**(N-k) = genuinely new content, certified by this unit's
battery (§3); **REFUTED** would mark a step whose displayed form the battery killed (§3 records
one *near-miss* finding, F-1, at Step 1e — a statement-level correction, not a refutation of the
corpus's own text).

### Step 0 — the package and the species pin (what "P(n)" means)

| # | claim | status |
|---|---|---|
| 0a | `P(n)` with the `(A2-RATE)` fourth member (`.13` + `.151`) | **LANDED** `InductionPackage` (H.95), `RateSpecies` (H.65). Deliberate weakenings recorded at H.95's signature note + CHAP-H §16 item 2: no (A0) member, no exactness member, no (A1)-shape member. |
| 0b | a bare `o(1)` is not a rate (`.149`, CODEX F3) — the fence that shaped 0a | **LANDED** as design: `RateSpecies` demands the species. The H.97 story (drainage-as-hypothesis REJECTED, rate-as-hypothesis ADOPTED) is the worked example. |
| 0c | bases `P(2)`, `P(3)` | **LANDED** H.96; H.97 modulo `hrate` = **REDUCES-TO**(HYP.08, unit OM-1). |
| 0d | window-1 boundary `u(1) = 1` (`ANNEX R R3`, `.160`) | **LANDED** H.22(ii)/`alphaRange_window_one` (H.71); certified again on true data (the `m = 3, N = 6` cell's boundary genre extracts a child at window `M_l = 1`, composed correctly — §3 RESULTS, PART 1). |

### Step 1 — level 0: the degree-`n` problem is a disjoint union over level-0 configurations of products of cluster systems and simple branches (`.54`)

The corpus text: *"By W12-S2.1 the degree-n problem is the disjoint union over level-0
configurations of products of cluster systems `(m_i, d_i)` and simple branches. Configurations
with every `m_i·d_i < n` are products of strictly-lower-degree data: (IH) supplies each factor's
package; LEMMA GENIND-0 multiplies the menus, W-12.A's level-0 censuses supply `C₀(q)`; drains
compose by "some factor drains". The only configurations not covered by (IH) are the full
clusters `m·d = n, m ≥ 2`."*

| # | claim | status |
|---|---|---|
| 1a | existence + uniqueness of the level-0 monic Hensel factorization over complete `O` | **LANDED** `Uniformity.Hensel.exists_monic_factorization_finset` (HYP.03 DISCHARGED 2026-08-14), `monic_factorization_unique`. |
| 1b | the CLASS-level (mod `π^N`) fibration: `Coeff O n N` stratifies by residue factorization; per stratum the factor-classes map is a **bijection** onto the product of factor state spaces, with the count identity | **CERTIFIED-OPEN (N-1a)** — no landed node, no blueprint node, no ledger row states it (it sits inside the owed `W12-S2.1` transcription, CHAP-H honesty H-12). Battery PART 2 checks (i)–(ii): exhaustive at `n ∈ {3,4}`, `q ∈ {2,3}`, both characteristics. |
| 1c | `typeOf` multiplies across the split | **LANDED** `typeOf_mul` (kernel). The order-0 leaf-law weld *at every degree* = **CONTRACT** CHAP-B B.67–B.74 (HYP.03's recorded residual). |
| 1d | menus multiply with σ-labels (`LEMMA GENIND-0`, strengthened predicate) | **LANDED** H.93/H.94 (`A1Cell.prod`, `prod_σ_degree` — OPEN-CALL 3 closed Lean-side). The general dominance-region split of GENIND-0's ν-clause is declared OPEN at H.94 (§16 item 6) — consumed only at multi-child visibility floors, chapter C/F territory. |
| 1e | decidedness/drain composition: a stratum class is decided **iff** every factor class is; undecided iff some factor is | **CERTIFIED-OPEN (N-1b)** with the defeat-infeasibility lemma CC-1 (§3): multiset cancellation + the 1b bijection. **Finding F-1 (statement-level, new):** the naive per-TYPE census composition is FALSE — distinct factor-type combinations can collide in the frame type (exhibited in the battery: `split⊎inert = inert⊎split` collisions) — so the composition must be stated on the decided/undecided PREDICATE (where it is exact) or as a census SUM over combinations, never as a free-product labeling. The corpus's own sentence ("drains compose by *some factor drains*") is the safe form; a transcription that upgraded it to a per-type product census would be REFUTED. This is exactly an A-H.2-class trap, caught at outline time. |
| 1f | quantitative composition: stratum undecided mass ≤ union bound over factors | **LANDED** H.63 `prod_sub_prod_le_sum` (bound direction, no exactness hypothesis); the IDENTITY direction (TRACK V / (A2)-exactness) needs exact child `u`'s = (IH) member 4 + `(CS-EXACT)` = **REDUCES-TO**(HYP.35) at stages. |
| 1g | level-0 censuses (`THEOREM W-12.A`) | **REDUCES-TO**(the owed `W12` transcription unit — CHAP-H honesty H-12; NOT a ledger row — **flagged**, see §4 item D-1). The census *identities* at `n ≤ 4` are re-checked numerically in Battery PART 2(i). |

### Step 2 — the full cluster `m·d = n`: the menu (`.55`)

| # | claim | status |
|---|---|---|
| 2a | **the depth-0 partition** (`LEMMA GENIND-1(i)`): every cluster state is exactly one of DRAIN / DEC / α(k) / β / CS, and the α-locus law is `(Q−1)·Q^{m(N−1) − k·m(m+1)/2}` | **CERTIFIED-OPEN (N-2a)** — the event grammar has NO node by design (CHAP-H honesty H-3: *"the event grammar, fracture permanence and the refine taxonomy are geometric, and there is no node for them"*), and no OPEN-MATH row owns it (HYP.121 is only its all-`O` generality flag, itself dispositioned onto the ℕ-level H.93/H.94, which do NOT contain the grammar). Leaf-level suppliers: the order-1 side dissection is now **CITE** `[GN15 Thm 2.3]` via CHAP-B B.42 (A-F.10: the proof route is machine-refuted; owner signature pending) + CHAP-B contracts B.48/B.58/B.63–B.74; the `ℓ = 1, m ≥ 2, d ≥ 2` leaf region is conditional on `B-BOX-1` (A-F.7). Ground instances landed: `n = 2` (ChapG `typeOf_three_cases`), `n = 3` (leancheck `N3MBox`/`N3Recur` four-case split). Battery PART 1 (a)–(c): partition + locus law, `m ∈ {2,3,4}`, both characteristics. |
| 2b | **α preserves the cluster** (`GENIND-1(ii)` + `GENIND-2(a)`): the α(k)-recentering is a bijection of the z-slice onto the node `D_k`, the shear transports the read, ghost fibres are constant `= Q^{k·c(m)}`, the update matrix is triangular with UNIT pivots (`ANNEX R R2`) | arithmetic identities **LANDED** H.15/H.16/H.17 (`alphaExp`, `k·clusterC m + m·W = alphaExp m W k`); the abstract pivot lemma **LANDED** H.60 `TriangularUnitPivot.bijective` (+ H.62, the `ZMod 4` non-surjective triangular toy — the failure mode exhibited); the O-level instantiation (the recentering IS such a map on `Coeff`-states) = **CERTIFIED-OPEN (N-2b)**, Battery PART 1(d): exhaustive ghost-fiber histogram — every fiber exactly `Q^{k·c(m)}`, onto, both characteristics. |
| 2c | **the α-bracket** (`GENIND-2(b)`): `Σ_{Σk_i = μ} Π(Q−1)Q^{c(m)k_i} = (Q−1)Q^{(c(m)+1)μ−1}`, realized-history window condition `mμ ≤ N−1` | **LANDED** H.18/H.20 (`alphaBracket_closed`/`_eq`), H.21 (mass-normalized audit). Window condition carried per amendment A-H.2. |
| 2d | **β-fracture transport** (`LEMMA GENIND-3`, terminal `[r3]`-capped display): the genre fibers over `Π_l [(μ_l, d) system at window M_l]`, `M_l = N − D_l`, `D_l = μ_l k_l + Σ_b deg_b·min(s_b, k_l) = min_j(v(a_j) + j·k_l)`; Hensel split; read equivalence; children enter with the β3 history mechanism; fracture permanence (`GENIND-1(iii)`) | **CERTIFIED-OPEN (N-2c)**. No node (part of the grammar, H-3); the capped-window formula has a THREE-layer correction history (`.24`: sealed uncapped → `[r3]` capped → `[r4]` regime inventory) — precisely the class of display a transcription must re-verify, so the battery re-derives it: PART 1(e)–(f) checks the per-genre composed counts `census_g·[ΠT_l − Π(T_l − u_l)]` against exhaustive enumeration at `m = 3` (and the constant-fiber division exactness is itself the R2 onto-check), both characteristics. |
| 2e | **CS bucket**: stages open at `4 ≤ e·μ·deg ψ`, hence `m ≥ 4`; stage menus supplied by `(CS-1)+(CS-2)` | criterion arithmetic **LANDED** H.03/H.04 (`eμγ = 4` has exactly `(2,1,2)`, `(1,2,2)`); the read-side criterion (zero CS below `m = 4`; every `m = 4` CS obeys it) **certified** Battery PART 1(g), both characteristics. Stage menus = fields of **LANDED-carrier** H.09 `StageInterface` = **REDUCES-TO**(HYP.32 `(CS-1)`, HYP.33 `(CS-2)`; embedded/mixed obstructed per `.199` = HYP.36; `μ ≥ 3` = HYP.81; `n ≥ 8` = HYP.82). |
| 2f | **mixed CS genres** (`GENIND-6(a)–(c)`): the split is factor-generic; fibration onto with constant `q`-power fiber via unit pivots | pivot lemma **LANDED** H.60; the per-genre affine ledgers are COMPRESSED in the corpus (`.42`) and the embedded/mixed instances are exactly the HYP.36 obstruction = **REDUCES-TO**(HYP.32/HYP.36). First live at `n = 6` (`.32`, §S14 C6) — empty for `n ∈ {4, 5}`. |
| 2g | aggregation into the finite `(A1)` menu with σ-labels (= `(hMenu-n)`) | closure **LANDED** H.93/H.94; the menu CONSTRUCTION from the grammar = TRACK V, **REDUCES-TO**(HYP.26, owner H/C) riding on N-2a. TRACK R needs none of it (S-0: `exists_coveringMenu` suffices). |

### Step 3 — (A0): the σ-decision at leaves (`.56`) — TRACK V, plus the decidability half TRACK R consumes

| # | claim | status |
|---|---|---|
| 3a | order-1 leaves: Ore at the terminal frame + window coherence | **REDUCES-TO**(HYP.24 decision half at order 1, HYP.25) = CHAP-B contracts B.48/B.58/B.79–B.82 + the B.42 **CITE**; window coherence = `GENIND-2(a)`, its arithmetic landed (2b/2c). |
| 3b | CS leaves: `(CS-3) = (H-e)` | **REDUCES-TO**(HYP.24 order-≥2 half: chapter C §10 certificates + chapter E ladder; `μ ≥ 3` = HYP.81). Carrier landed: `StageInterface.hsigma` (μ = 2 degree half); `stageSigma` defined for μ = 2 leaf labels only (H.73). *"TEETH: NONE, at any degree, anywhere in this note"* (`.65`) — the least-guarded clause of the whole surface. |
| 3c | branchwise σ-composition (products; mixed via `GENIND-6(d)`) | **LANDED** `typeOf_mul` + H.76 `efPair_forced_of_dvd` (the μ = 2 `(e,f)`-forcing chain); composition mechanism = N-1b/N-2c above; fresh decorrelated oracle leg in Battery PART 2(iv) (PARI `factorpadic` at `ℤ_2`/`ℤ_3` — reader-decided σ vs oracle, exact match required). |
| 3d | **the decidability split this outline makes explicit** (compiler note, not a corpus sentence): TRACK R consumes only *that* leaf strata are semantically decided (`DecidedAt` = all lifts share a type) — certificates, not values; TRACK V additionally consumes *which* σ. A future campaign that conflates them will misroute HYP.24's two halves. | recorded; the battery's reader implements exactly the certificate half. |

### Step 4 — (A2): the drainage rate (`.57` + the annex stack) — TRACK R's core

Corpus inventory: `r(N)` = conservative drains + window-boundary (tail) families + the CS
stages' ragged bands + CS-internal residues; summed: `r(N)/q^{nN} → 0` at the `(A2-RATE)`
species.

| # | claim | status |
|---|---|---|
| 4a | the bucket inventory (four families, exhaustive) | rides N-2a's grammar (**CERTIFIED-OPEN**); at `m ≤ 3` buckets 2–4 are empty (certified, PART 1(g): zero CS events below `m = 4` — the GT-CRIT negative half reproduced, both characteristics; ragged bands are a stage-side notion with no cluster-level analogue to test here); at `n = 4, 5` the corpus's GENH4 battery covers it (not re-run here). |
| 4b | **the simple part's rate** (`ANNEX-THEOREM GENIND.C′`, `.153`): lexicographic induction on (degree, window) closes `u^{simple}/Q^{m(N−1)} ≤ K N^B Q^{−(N−c)}` | abstract closure **LANDED** H.71 `rate_close` (A-H.2-repaired signature, PROVED, Lean-core; split H.71a/b/c) + pricing H.30–H.37 + ground species H.23–H.28 (`(K,B,c) = (1,1,1)` at `m = 2`, H.28). **The INSTANTIATION** — the true normalized cluster complements inhabit `RecursionLegs Q m (m(m+1)/2)` — is **CERTIFIED-OPEN (N-3)**, Battery PART 3: every field checked on exhaustive true data at `m ∈ {2,3}`, both characteristics, plus the A-H.2 defeat replays (CC-3). |
| 4c | **the β-legs** (`ANNEX-LEMMA GENIND-C1`, `.152`): route prefactor `ρ_g ≤ Q^{−(D_l−1)}`; union bound; GROWING/BOUNDED regime split; polynomial genre count | two regimes **LANDED** H.67 `rate_growing` / H.68 `rate_bounded`; union bound **LANDED** H.63; prefactor + genre count enter as `RecursionLegs`' `K'/B'/c'` data — their instantiation is inside N-3. |
| 4d | boundary tails + ragged bands: routed through (A2) as `W-12 S3.4` displays; confinement by `LEMMA GENHN-3` at the discharged (`f₁ = 1`) genres | **REDUCES-TO**: `W-12 S3.4` = the owed W12 transcription (D-1); GENHN-3's cap clauses are only PARTIALLY transcribed — CHAP-I orchestrator item O-2 (honesty I-11(ii)) = HYP.24's pinning half; the `e₁ = 2` pinning instance = **CONTRACT** H.89. |
| 4e | **the CS-internal rate** (`ANNEX-LEMMA GENIND-C2Q`, `.198`, under `(CS-1Q.a–b)`, `.197`): stage-drain contribution ≤ `K N^B Q^{−a(N−1)−H/2+O(1)}`, height sum converges (`b_S − a ≥ 1/2`) | the algebra **LANDED** H.72 `rate_lossPriced` (proved FROM `StageInterface.hwin`/`hprice` — A-H.1/D4's adopted Candidate A); the per-genre hypotheses `(CS-1Q.a–b)` = **REDUCES-TO**(HYP.36 — CARRY; embedded/mixed affine ledgers unverified per `.199`; the two standalone quartic genres are exempt with `Δ_G = 0`, laws landed H.38/H.42, the genre-E `StageInterface` instance booked, genre-F instance owed §16 item 14); stage-drain input over `K` = GENIND.C′ over the stage field (4b's machinery at `|K| = Q^γ`); `μ ≥ 3` = HYP.81; `n ≥ 8` = HYP.82; the rate row itself = HYP.30. |
| 4f | **well-founded descent** (`GENIND-C2D` `.201` + the lexicographic measure `.202` — the repair of the refuted `M★ < N` inference) | **LANDED** H.70 `undecidedSet_preimage_subset`/`undecidedSeq_antitone` (with the source's compressed conservative-read citation DISCHARGED — in `leanfinal` monotonicity is definitional via `coeffFactor_proj`) + H.71's `Nat.strong_induction_on` structure. The cluster-level C2D (same argument on cluster states) has no separate node — flagged in §4 as N-3's first sub-node. |
| 4g | mixed drains: inclusion–exclusion over (children's `u`'s exact by (IH) member 4) × (stage drains exact by `(CS-EXACT)`) | identity direction = TRACK V, **REDUCES-TO**(HYP.35 + the (IH) exactness member — NOT in the landed `InductionPackage`, recorded asymmetry §4 D-3); bound direction **LANDED** H.63 (`R6.2`'s separation, machine-audited in the corpus and re-audited at `.58`'s compiler block). |
| 4h | **the total assembly**, two halves per S-1: (bridge-a) reader-decided ⟹ `DecidedAt`, so `undecidedCount O n N ≤ (conservative count)`; (bridge-b) the conservative count obeys the level-0 composition (Step 1) of the cluster rates (4b–4e) ⟹ `RateSpecies` at degree `n`, `(K,B,c)` uniform in `O` | **CERTIFIED-OPEN (N-1c + the bridge)** — no node, no row. (bridge-a) is the certificate layer: exactly ChapG's `CertFamily` pattern + CHAP-B's leaf laws, per leaf shape; its `n = 3` instance is H.97's `hrate` (OM-1). (bridge-b) certified: Battery PART 2(iii) — the per-stratum counts obey `census·[ΠT − Π(T−u)]` with the child tables computed INDEPENDENTLY at their own windows (the frame VERDICT is the composition by construction, so its semantic content is carried by the bijection (i,ii), the oracle (iv) and CC-1's lemma — Codex pass findings 1/3, adopted). The `O`-uniformity of the constants is structural in the blueprint's ∀`O` statement forms (HYP.87's pattern) once the per-`(m,d)` constants are; recorded, not certified (no numeric content). |

### Step 5 — fire `W-12.D` (`.60`) — OUT OF SCOPE, landed

**LANDED** H.98 `drainage_of_package` + `uniformity_of_package` (+ `UniformityStatement.ofDecided`,
kernel). Named here only so the outline is the whole of GENIND.B.

---

## 2. THE DEPENDENCY MAP (sub-step → landed node / row / new)

TRACK R (= T-1's Lean shadow), in firing order for a future campaign:

```
InductionPackage n
 ├─ menu clause ........................ LANDED exists_coveringMenu (CN-22)          [done]
 └─ rate clause  RateSpecies q K B c (undecidedSeq O n)
     ├─ Step 1 level-0 transport ....... N-1a + N-1b + N-1c (NEW, certified §3)
     │    ├─ Hensel factorization ...... LANDED exists_monic_factorization_finset
     │    ├─ typeOf_mul weld ........... LANDED (kernel) + CONTRACT B.67–B.74
     │    ├─ union bound ............... LANDED H.63
     │    └─ W-12.A censuses ........... DEBT D-1 (W12 transcription unit)
     ├─ Step 2/4 cluster grammar ....... N-2a + N-2b + N-2c (NEW, certified §3)
     │    ├─ order-1 dissection ........ CITE B.42 [GN15 Thm 2.3] + CONTRACT B.48/B.58
     │    ├─ leaf laws ................. CONTRACT B.63–B.74 (+ B-BOX-1 at ℓ=1,m≥2,d≥2)
     │    ├─ α arithmetic .............. LANDED H.13–H.22
     │    ├─ unit-pivot lemma .......... LANDED H.60 (+H.62)
     │    └─ CS criterion .............. LANDED H.03/H.04
     ├─ Step 4b recursion closure ...... LANDED H.71 rate_close (+H.30–H.37, H.22)
     │    └─ instantiation ............. N-3 (NEW, certified §3)
     ├─ Step 4c β-legs ................. LANDED H.63/H.67/H.68 (constants inside N-3)
     ├─ Step 4d tails/ragged ........... DEBT D-1 (S3.4) + OPEN HYP.24-pinning (O-2) + CONTRACT H.89
     ├─ Step 4e CS-internal rate ....... LANDED H.72 ⇐ OPEN HYP.36 (per genre; E/F exempt Δ_G=0)
     │    ├─ stage menus/brackets ...... OPEN HYP.32 / HYP.33 (carrier H.09)
     │    ├─ μ ≥ 3 ..................... OPEN HYP.81 (μ=3 proof exists, NO OWNER — item O-1)
     │    └─ n ≥ 8 ..................... OPEN HYP.82 (+ the n≥8 box family)
     ├─ Step 4f descent ................ LANDED H.70 + H.71 structure
     └─ bases .......................... LANDED H.96 · H.97 ⇐ OPEN HYP.08 (unit OM-1)
```

TRACK V (GENIND.B's other two members; NOT T-1's Lean shadow but consumed by H.98's `hdec`):
Step 2g menu weights = HYP.26; Step 3a/3b σ-values = HYP.24/HYP.25 (+ HYP.81); Step 4g
exactness = HYP.35; per-degree value assembly = HYP.11/28/29 at `n = 3` (unit OM-1) and the
count layer (unit OM-3). **This unit adds no work there beyond the map.**

Named debts that are NOT ledger rows (flagged so the reduction is honest):
* **D-1 — the `W12` transcription unit** (CHAP-H honesty H-12): `W12-S2.1` (Step 1's
  decomposition), `THEOREM W-12.A` (every entry law chapter H audits), `S3.4` (Step 4d's
  routing). Owed, unblocked, unowned.
* **D-2 — the B.42 cite gate** (CHAP-B A-F.10): the order-1 dissection under N-2a now rests on
  a literature cite awaiting the owner's signature; CHAP-B's §8+ layer is gated on it.
* **D-3 — the exactness asymmetry**: the landed `InductionPackage` does not carry the (IH)'s
  exactness member, so Step 4g's identity direction cannot even be STATED against H.95 as-is;
  a future TRACK V campaign needs a package extension (or per-degree exact laws as at `n = 2`,
  G.36/G.37). Recorded at H.95's signature note; repeated here because Step 4g consumes it.
* **D-4 — the cluster-level C2D**: H.70 is the FULL-SPACE monotonicity; the recursion at 4b/4e
  consumes the CLUSTER-state version (same proof, different carrier). First sub-node of N-3.

---

## 3. CERTIFICATION RECORDS (the battery) AND REFUTATION ATTEMPTS

**What "certified" means here (scoped, per the campaign charter and the Codex pass §3.4
finding 17):** CERTIFIED-OPEN = exact-rational numeric evidence on exhaustive finite cells
PLUS survived constructed-counterexample attacks. It is NOT a proof and licenses nothing: a
finite battery at `q ∈ {2,3}` cannot certify an all-DVR theorem family. What it certifies is
that the STATEMENT, as this note phrases it, survives the attack classes that killed its
predecessors (the A-H.2 class) — the charter's admission standard for future proof effort.

`verification/openmath/OM2_genindb_battery.py` — exact integer/rational arithmetic throughout;
exhaustive (never sampled) except the PARI oracle's lift probes; both coefficient rings
`ℤ/q^N` (mixed characteristic) and `𝔽_q[t]/t^N` (equal characteristic) at `q ∈ {2,3}` — the
corpus's own machine legs were `d = 1`, prime `q`, mixed characteristic only (GENIND-BOX-3/-6),
so the equal-characteristic leg is genuinely additive and is where a `q = 2` coincidence trap
would surface. Cells and verdicts below; the script prints a per-check ledger and exits nonzero
on any failure.

### 3.1 N-1 (Step 1, the level-0 transport) — PART 2 of the battery

Precise statements certified (self-contained):

> **N-1a (count fibration).** Fix `q`, `n`, `N ≥ 1`, `O/π^N ∈ {ℤ/q^N, 𝔽_q[t]/t^N}`. For each
> monic degree-`n` residue factorization `f̄ = Π_i P̄_i^{m_i}` (P̄_i distinct irreducible over
> `𝔽_q`), the classes `c ∈ Coeff O n N` with residue `f̄` are in bijection with tuples of
> monic factor-classes `(g_i mod π^N)`, `ḡ_i = P̄_i^{m_i}`, via Hensel factorization; the
> bijection is realized by (lift, factor, reduce) and inverted by multiplication, with NO
> precision loss (the level-0 resultants are units).

> **N-1b (decidedness composition).** Under N-1a's bijection, `c` is decided (all lifts share
> one `typeOf`) **iff** every factor class `g_i` is decided; when decided,
> `σ(c) = ⊎_i σ(g_i)`. Consequently `undecided(stratum) = census × [Π_i T_i − Π_i (T_i − u_i)]`.

> **N-1c (rate composition).** Normalizing N-1b by the stratum totals and summing strata:
> `undecidedSeq O n N` = Σ over configurations of (config weight) × (union-composed cluster
> fractions) — so the degree-`n` rate follows from per-cluster rates + the level-0 weights.

Refutation attempt CC-1 (the constructed-counterexample discipline): the simultaneous-defeat
equations for N-1b demand factor-possible-type sets `T_A, T_B` (each of size ≥ 2, realized
independently) with ALL pairwise unions equal. Multiset cancellation
(`τ ⊎ υ₁ = τ ⊎ υ₂ ⟹ υ₁ = υ₂`) makes the system infeasible — **infeasibility is the lemma**
(the future Lean node is one `Multiset.add_right_cancel` application), PROVIDED all
combinations are realized, which is N-1a's bijection (Hensel product onto). The battery scans
all 2-ELEMENT type-set pairs at per-factor degree ≤ 3 and confirms zero solutions — and
2-element pairs SUFFICE (Codex pass finding 2's precision, adopted): a defeat with larger
possible-type sets restricts to a defeat on any 2-element subsets, and a defeat across
`r ≥ 3` factors restricts to `r = 2` by freezing the other factors at fixed types, so the
pair scan is complete for the bounded degrees. Finding F-1
(the per-type collision `split⊎inert = inert⊎split`) is exhibited as the nearby TRUE defeat of
the WRONG statement — the trap the future node's docstring must carry.

### 3.2 N-2 (Steps 2/4, the cluster grammar) — PART 1 of the battery

> **N-2a (depth-0 partition + locus laws).** For the `(m, d=1, N)` cluster system over
> `O/π^N` (states `(a_0..a_{m−1})`, `v(a_j) ≥ 1`, `Q = q`): the conservative read's five-case
> classification (DRAIN `v(a_0) ≥ N` / DEC all-residuals-separable / α(k) single-side integer
> slope with residual `(y−z)^m` / β fracture / CS `e·deg ψ ≥ 2` repeated) is total and
> disjoint; `#DRAIN = Q^{(m−1)(N−1)}`; `#α(k) = (Q−1)Q^{m(N−1)−k·m(m+1)/2}` for exactly the
> realized `1 ≤ k`, `mk ≤ N−1`; zero CS at `m ≤ 3`; every `m = 4` CS event obeys
> `4 ≤ e·μ·deg ψ ≤ m`.

> **N-2b (α-transport).** The α(k)-recentering `x ↦ π^k(y + z)` followed by the `π^{mk}`-scale
> maps the α(k, z)-slice ONTO the full window-`(N−mk)` state space with constant fiber
> `Q^{k·c(m)}`, `c(m) = m(m−1)/2`.

> **N-2c (β-transport, capped window).** A β-state's children extract at windows
> `M_l = N − D_l`, `D_l = min_j(v(a_j) + j·k_l)`; per genre (partition type + slopes + windows)
> the state count fibers with CONSTANT fiber over `Π_l [(μ_l, d) states at M_l]`, and the
> frame's conservative verdict is (undecided ⟺ some child undecided), so per genre
> `#undecided = census_g · [Π T_l − Π(T_l − u_l)]` with the child `u`-tables computed
> INDEPENDENTLY at their own windows.

Refutation attempts CC-2 (A-H.2-class attacks, run exhaustively at every cell):
(i) partition defeat — a state in two buckets or none; (ii) fiber defeat — a non-constant α or
β fiber (the R2 unit-pivot failure signature; H.62's toy shows the signature is REAL in
triangular-without-unit-pivot maps, so a hit would refute the O-level instantiation);
(iii) window defeat — an α(k) event with `mk > N−1` (the A-H.2 (α-0) scenario on true data);
(iv) characteristic defeat — any cellwise count differing between `ℤ/q^N` and `𝔽_q[t]/t^N`.

### 3.3 N-3 (Step 4b, the RecursionLegs instantiation) — PART 3 of the battery

> **N-3.** For `m ∈ {2,3}`, `d = 1`: define `û(N) := u_{m,d}(N)/Q^{m(N−1)}` from the TRUE
> exhaustive reader counts, and the three legs from the reader's own buckets (head := drain
> mass, alpha := α-bucket mass, beta := β-bucket mass, all normalized). Then every field of the
> landed `RecursionLegs Q m c û` holds with the intended structure slope `c = m(m+1)/2`
> (H.71's A-H.2 adjudication: the NORMALIZED α-discount), with `hsplit` an EQUALITY, `halpha`
> an equality at `m = 2` (no β) and the windowed range exactly the realized `k`'s; and the
> conclusion `RateSpecies Q K B c'` holds at the species constants (`(1,1,1)` at `m = 2`
> exactly as `.150`; measured minimal constants recorded at `m = 3`).

Refutation attempts CC-3: (i) replay A-H.2's (α-0): the UNWINDOWED `halpha` at `N = m(k+1)`
would read `û(0)` — on true data window 0 does not exist, so within the corpus's
positive-window convention the windowed form is forced, not optional (recorded as the
faithfulness check of the A-H.2 repair; a re-based interface with shifted windows would be a
different, non-corpus formulation — Codex pass finding 16's precision); (ii) `hmc : m < c` at the
intended instantiation `c = m(m+1)/2` ⟺ `m ≥ 2` — checked for `m = 2..12`; the boundary
`c = m` (A-H.2's second refutation family) is confirmed UNREACHABLE by true instantiations;
(iii) the sharpest-constant probe: search for a `(K,B,c)` DEFEAT — an `N` where the species
bound fails at the recorded constants.

### RESULTS (full run, 2026-08-16; log `verification/openmath/logs/OM2_full_20260816_*.log`)

**612 checks, 612 passed, 0 failed** (fast mode: 381/381; full mode adds the deeper windows
and the complete oracle plan; the final count includes the 17 checks added by the Codex-pass
repairs of §3.4 — the explicit Hensel roundtrip, the PART-2 both-characteristics census
equality, the per-`(k,z)` α-slice totals, the `hdesc`/`n₀` field checks, the exact
ℕ-truncation `hbeta` convention, and the F-2 exactness leg). Cell inventory:

* PART 1 — 28 cells × 2 rings, all exhaustive: `q = 2`: `m = 2, N ≤ 6` · `m = 3, N ≤ 6` ·
  `m = 4, N ≤ 4`; `q = 3`: `m = 2, N ≤ 5` · `m = 3, N ≤ 4` · `m = 4, N ≤ 3` (largest cells
  32,768 and 19,683 states; the `m = 3, N = 6` cell exercises TWO live β genres, including
  the `k = 2` child at the boundary window `M_l = 1` — the R3 window-1 rider's site, composed
  correctly). Per cell: partition totality, drain count, α-locus law + window scan,
  α-fibre histogram (every fibre exactly `Q^{k·c(m)}`, onto), the recursion identity, the
  per-genre β composition (constant fibres, onto, `census·[ΠT − Π(T−u)]` with independent
  child tables), the CS criterion, **and cellwise equality of the ENTIRE census between
  `ℤ/q^N` and `𝔽_q[t]/t^N`** — to this unit's knowledge the first equal-characteristic
  NUMERIC leg this argument has had in the repo (the corpus's own coverage disclosures:
  GENIND-BOX-6, oracle mixed-characteristic only; GENIND-BOX-3, battery `d = 1` prime `q`;
  the `verification/` layer is `ℤ_p`-based).
* PART 1(i), the decorrelated tie: my reader's `u₂` table equals ChapG's landed
  `hex3R`/`uTwo` closed form at every cell, and **`u₃` equals ChapG's landed `hex3U`
  (`ChapG/G66.lean`) at every cell** (`q = 2, N ≤ 5`; `q = 3, N ≤ 4`) — an independent
  implementation reproducing the landed arithmetic layer letter-for-letter.
* PART 2 — 12 cells × 2 rings, exhaustive frame enumeration (`n = 2: N ≤ 3`; `n = 3`:
  `q = 2, N ≤ 3`, `q = 3, N = 2`; `n = 4`: `q = 2, N ≤ 3`, `q = 3, N = 2`; largest
  `3^8 = 6561` classes): stratum counts, the Hensel class bijection (distinct factor-tuple
  count = predicted product, roundtrip exact), the composed undecided law per stratum, and
  the both-ring count equality. PARI oracle (`quartic_oracle.full_type`, `ℤ_2`/`ℤ_3`):
  **every reader-DECIDED σ matches the oracle on 2 independent lifts per tested class**
  (12 tie checks across cells, `0` mismatches); ambiguity witnesses found on the majority of
  reader-UNDECIDED classes, with the misses explained by F-2 (below) and probe statistics.
* PART 3 — `RecursionLegs` (the landed H.71 A-H.2 signature) on the TRUE exhaustive tables at
  `(q, m) ∈ {2,3} × {2,3}`: `hsplit` and `hhead` hold as EQUALITIES; **`halpha` holds as an
  EQUALITY with the windowed range and the structure slope `c = m(m+1)/2`** (the A-H.2
  adjudication's intended instantiation, confirmed on true data); `hbeta` holds with measured
  `K' ≤ 0.0004` at `(B', c') = (0, 1)`; the species conclusion `RateSpecies` holds at
  `(K, B, c) = (1, 1, 1)` on true data at both degrees; the `m = 2` closed form ties
  `EFF.GENIND.150` exactly.
* PART 4 — the CC-1 defeat scan (all factor-type-set pairs, degrees ≤ 3): **zero solutions**,
  as multiset cancellation predicts; the F-1 collision exhibit confirmed (4 combinations,
  3 frame types).

**Refutation-attempt outcomes (the constructed-counterexample discipline):**

* **CC-1 (N-1b): INFEASIBLE, with the infeasibility a one-line lemma** (multiset
  cancellation `Multiset.add_right_cancel` + the Hensel product bijection N-1a). Scan
  confirms empty.
* **CC-2 (N-2a/b/c): all defeat scans empty at every cell** — no doubly/un-classified state,
  no non-constant α or β fibre (the R2 unit-pivot failure signature never fires on the true
  ledgers — consistent with H.60's monic-pivot mechanism), no out-of-window α event, no
  characteristic-dependent count.
* **CC-3 (N-3): the A-H.2 attacks do not re-open** — the windowed `halpha` is forced by the
  true data within the positive-window convention (the unwindowed form would read the
  nonexistent `u(0)` at every tested multiple of `m`), and the boundary `c = m` is unreachable (`m < m(m+1)/2` ⟺ `m ≥ 2`, checked
  `m ≤ 12`).

**Finding F-1 (a refuted transcription, with repair).** The per-TYPE census of a level-0
product does NOT compose freely: distinct factor-type combinations collide in the frame type
(`split ⊎ inert = inert ⊎ split`; 4 combinations, 3 frame types). The DECIDED/UNDECIDED
predicate composes exactly (CC-1); a future node stating N-1b must be phrased on the
predicate, or as a census SUM over combinations — the free-product phrasing is REFUTED.

**Finding F-2 (a refuted identification, with repair — the unit's main new fact).** The
conservative complement STRICTLY exceeds the semantic undecided set from `(n, N) = (2, 3)`
on. Witnesses, machine-checked and hand-proved:

* `n = 2, N = 3`: reader-undecided `= q·hex3R(q,3)` (12 at `q = 2`; 45 at `q = 3`) vs the
  LANDED semantic law `undecidedCount O 2 N = q^N` (8; 27). *Hand proof of the gap family:*
  a state with `v(a₀) ≥ 3` and `v(a₁) = 1` is reader-drained, but every lift has Newton
  points `(0, v₀ ≥ 3), (1, 1), (2, 0)` with `2·1 < 3 ≤ v₀`, so `(1,1)` is a hull vertex and
  every lift splits as two linear factors — semantically DECIDED (type `{(1,1),(1,1)}`).
  Gap count `q·(q−1)·q` matches exactly (4; 18).
* `n = 3, q = 2, N = 2`: exactly 4 of the 16 reader-undecided classes are semantically
  decided (regression check P2(vi)): e.g. `(a₀, a₁, a₂) = (0, 2, 0)` — every lift has points
  `(0, v₀ ≥ 2), (1, 1), (3, 0)`; `(1,1)` is below every chord (`2v₀/3 > 1`), giving a linear
  piece plus an `e = 2` Ore piece: type `{(1,1),(2,1)}` for every lift. The polygon argument
  is the proof; the depth-3-exhaustive oracle scan corroborates it (a finite lift scan alone
  cannot decide all lifts — Codex pass finding 14). "Exactly 4" is rigorous: the other 12
  reader-undecided classes all carry two-type witnesses (two explicit lifts with distinct
  oracle types — check P2(vi)-exactness), and the 4 are decided by the polygon argument (the
  two shapes above; `(1,1,1)` and `(3,1,3)` recenter onto the `(0,2,0)` shape).

*Repair (already folded into the outline):* S-0/S-1's two-object discipline and 4h's two-part
bridge. Nothing in the corpus is refuted — its object is NAMED conservative — but any
transcription identifying `u_{m,d}`-assembled counts with `undecidedCount O n N` (or citing
the `n = 2` exact law `q^N` for the conservative object) dies on these witnesses. The
`hex3R`/`hex3U`/`uTwo` layer is the conservative object's; `G.36`'s `q^N` is the semantic
object's; both are true and they are different functions.

### 3.4 The adversarial verification pass (Codex, fresh context, 2026-08-16)

Per the standing verifier discipline, the note + battery were handed to Codex with the
quote-and-classify charge (fix nothing). Verdict: **no CRITICAL ERROR**; the two F-2 hand
proofs, the F-1 collision, the multiset-cancellation argument, and the α-slope/`halpha`
equality claim were each returned **Clean**; 18 JUSTIFICATION GAPS. Dispositions:

* **Adopted as battery repairs (re-run green, +17 checks):** finding 4 (the roundtrip was
  claimed but not executed — now an explicit product-and-compare assert), finding 5 (PART 2
  had no both-ring comparison — now `P2(vii)`), finding 6 (α-slices per `(k,z)` — now in
  `P1(d)`), finding 8 (`hdesc`/`n₀` unchecked — now checked), finding 9 (`hbeta`'s `W = 1`
  bound used a looser factor than Lean's ℕ-truncation — now exact), finding 12 ("exactly 4"
  needed the other 12 classes' two-type witnesses — now `P2(vi)`-exactness).
* **Adopted as note wording repairs:** findings 1/3 (PART 2(iii)'s verdict-side is
  compositional by construction; the tested content is the per-stratum aggregation against
  independent child tables + the bijection + the oracle), finding 2 (the CC-1 scan is over
  2-element pairs, which SUFFICE by restriction — argument recorded), finding 7 (dropped
  "ragged" from the `m ≤ 3` claim), findings 10/14 (the `n = 2` semantic side is the landed
  law, not battery-computed; the oracle scan corroborates, the polygon argument proves),
  finding 16 ("forced" scoped to the positive-window convention), findings 17/18 (the
  "certified" scope box at the head of §3).
* **Noted, no change:** finding 11/13/15 (Clean), and the closing confirmation that F-1's
  collision is genuine while cancellation still gives exact decidedness composition.

---

## 4. PROPOSED BLUEPRINT DECOMPOSITION for the future proving campaign (sketches, landed vocabulary)

The N-nodes below are the genuinely-new surface. They are deliberately phrased so the fleet
consumes ONLY landed names (`Coeff`, `proj`, `typeOf`, `monicFactors`, `DecidedAt`,
`undecidedSet`, `coeffFactor`, `RecursionLegs`, `RateSpecies`, `StageInterface`) — a fleet
problem, not a re-architecture. Environments per CHAP-H §0.1.

**N-1 cluster (4–6 nodes, ENV-H3).**
```lean
-- N1.1 [def] the level-0 stratum of a residue factorization (Finset of Coeff O n N)
-- N1.2 [theorem] the Hensel class bijection + count identity
theorem card_levelZeroStratum … : Nat.card {c : Coeff O n N // resFactor … } = …
-- N1.3 [theorem] decided-composition (⟺) via Multiset cancellation + typeOf_mul
theorem decidedAt_prod_iff … : DecidedAt O n (σ₁ + σ₂) N (mulClass c₁ c₂) ↔ …
-- N1.4 [theorem] the stratum undecided count = census × (ΠT − Π(T−u))  (fires H.63)
```
Risk note: N1.3's ⟸ direction must quantify over ALL frame types (finding F-1's collision);
state it on the predicate, never on per-type censuses.

**N-2 cluster (the hard one; ~10–14 nodes, ENV-H3 + a new `ClusterState` carrier).**
```lean
-- N2.0 [def] ClusterState O m N  (the (m,1,N) system: Fin m → residue-zero classes)
-- N2.1 [def+lemma] depth0Read : ClusterState → Bucket; partition theorem (Battery N-2a)
-- N2.2 [lemma] card_drain, card_alphaSlice (the locus laws)
-- N2.3 [theorem] alphaShear : αSlice k z ≃ ClusterState O m (N − m*k) fibrewise,
--        card_ghostFiber = Q^(k * clusterC m)   (fires H.60 for onto)
-- N2.4 [theorem] betaExtract (capped window M_l, product fibration, verdict composition)
-- N2.5 [lemma] cs_criterion (fires H.03/H.04)
```
Supplier gates a fleet agent must respect: B.42's cite signature (D-2), `B-BOX-1` at
`ℓ = 1, m ≥ 2, d ≥ 2` leaves (return `BLOCKED: B-BOX-1`), `HYP.36` at embedded/mixed stage
genres (return `BLOCKED: HYP.36`).

**N-3 cluster (4–5 nodes, ENV-H1/H3 seam).**
```lean
-- N3.0 [lemma] cluster-level C2D (H.70's argument on ClusterState)      [debt D-4]
-- N3.1 [def] uCluster O m N / legs from N2.1's buckets  (the CONSERVATIVE object, S-1)
-- N3.2 [theorem] legsOfCluster : RecursionLegs q m (m*(m+1)/2) (uClusterNorm …)
--        (battery PART 3: hsplit/hhead/halpha are EQUALITIES on true data — state them so)
-- N3.3 [theorem] cluster RateSpecies (fires H.71 rate_close)
-- N3.4 [theorem] the bridge (S-1, INEQUALITY only):
--        undecidedCount O n N ≤ assembled conservative count
--        (per leaf shape: reader-decided ⟹ DecidedAt — the CertFamily pattern; F-2's
--         witnesses REFUTE any equality form) ; then N-1c assembles InductionPackage n
--        from per-cluster species + StageInterface data per genre.
```
The terminal composed statement the campaign should aim at (T-1's honest conditional Lean
form — the reduction, never the theorem):
```lean
theorem inductionStep (n : ℕ) (ih : ∀ k, 2 ≤ k → k < n → InductionPackage k)
    (hstage : /- per-genre StageInterface data + CS decidability for every composite-stage
                genre realizable at degree ≤ n; at n ≥ 8 the HYP.81/82 conjuncts -/) :
    InductionPackage n
```

---

## 5. HONEST FINAL VERDICT

**What fraction of GENIND.B's inductive step is already landed or reduced.** The §1 tables
carry 30 sub-claim rows across Steps 0–4 (4 + 7 + 7 + 4 + 8). By PRIMARY status (several rows
are mixed — a landed abstract half with an open instantiation half — and are counted at their
open half when the open half is load-bearing):

* **LANDED (machine-checked, Lean-core): 12 rows** — the package/species/base scaffolding
  (0a, 0b, 0c-modulo-HYP.08, 0d), the Hensel and menu-product mechanisms (1a, 1c, 1d,
  1f-bound), the α-bracket arithmetic (2c), the σ-composition mechanism (3c), and the
  abstract rate calculus (4c's H.63/H.67/H.68, 4f's H.70/H.71-structure). By volume this is
  MOST of the step's arithmetic content — the annex stack (`GENIND.C′`, `C1`, `C2Q`, `C2D`,
  the union bound, the unit pivot) is fully machine-checked at its abstract signatures.
* **REDUCES-TO named rows / contracts / cites / debts: 10 rows** — the CS clause family
  (2e, 2f, 4e → HYP.32/33/35/36/81/82 with carrier H.09; the criterion arithmetic inside 2e
  is landed H.03/H.04), the value layer (2g, 3a, 3b → HYP.24/25/26), the boundary/pinning
  leg (4d → HYP.24-pinning + O-2 + H.89 + debt D-1), the census debt (1g → D-1), the
  mixed-exactness leg (4g → HYP.35, bound half landed H.63), and 3d (the decidability/value
  routing note). Plus the two named non-row debts D-1 (the W12 transcription unit) and D-2
  (the B.42 cite), flagged.
* **GENUINELY NEW (no owner anywhere): 8 rows in 3 theorem families** — N-1 (the level-0
  class transport: 1b, 1e, 4h), N-2 (the cluster event grammar over `O`: 2a,
  2b-instantiation, 2d, 4a — the single irreducible core, exactly what CHAP-H honesty H-3
  left node-less), and N-3 (the `RecursionLegs` instantiation + the S-1 bridge:
  4b-instantiation). All eight are now CERTIFIED-OPEN: exhaustively certified at
  `q ∈ {2,3}`, both characteristics, degrees/multiplicities through 4, with an oracle σ-tie
  and all constructed-counterexample attacks infeasible (§3).

**The irreducibly new mathematics, in one sentence:** the `O`-level INSTANTIATION LAYER — the
depth-0 five-case read with its two transports (α shear with constant unit-pivot fibres; β
capped-window extraction with product fibration), the level-0 class fibration, and the
resulting `RecursionLegs` instantiation with the certificate bridge to `undecidedSeq` — i.e.
the geometry the corpus proved at schema grade with three-layer-deep display corrections, and
which no chapter, ledger row, or contract owns. Everything else in Steps 1–4 is either landed
arithmetic or a named reduction.

**What this unit changed about the target's shape:** (i) S-0 — T-1's Lean content is the rate
clause alone (`exists_coveringMenu` already discharges the menu clause at every degree);
(ii) S-1/F-2 — the conservative and semantic complements are DIFFERENT functions from
`(n, N) = (2, 3)` on, with hand-proved witnesses, so the terminal bridge is an inequality and
two tempting transcription strengthenings (F-1's free per-type census, F-2's
conservative-=-semantic identification) are REFUTED with repairs recorded; (iii) the future
campaign is a fleet problem over the ~15 sketched N-nodes of §4 phrased entirely in landed
vocabulary, gated by exactly the known reductions (HYP.36/81/82 at stages, B.42's signature,
D-1's W12 unit) — not a re-architecture.

**Fence compliance:** no step of GENIND.B is claimed proved here; the composite-stage clauses
remain OPEN per genre exactly as `GENIND-BOX-1` prices them; this note adds no axiom, no Lean
declaration, and no new conditionality — it adds certifications, two refuted strengthenings,
and a typed skeleton.
