# OM-9 — the two weld conjuncts: HYP.74 (`JD0-BOX-2`) and HYP.148+139 (`GENHN-BOX-2` / W-1)

**Unit:** OPEN-MATH campaign (docs/OPENMATH_CAMPAIGN_2026-08-16.md), unit OM-9, Fable-max arm.
**Rows:** HYP.74 (`JD0-BOX-2`, CARRY), HYP.148 (`GENHN-BOX-2`, CARRY), HYP.139 (`W-1`
transport, MATH). **Status: CLOSED 2026-08-16.** Headline: chapter F's §4+§5 are the first
chapter-F landings in `leanfinal` (16 files; the two Display-A carriers + the W-1 carrier at
gate signatures; 9 of 15 axiom stubs PROVED; 29 theorems, all Lean-core); the certification
battery is 95 GREEN / 0 RED with 8/8 planted mutants caught; one fresh certified-then-proved
theorem (`CarryCocycle.c_comm` — every carry cocycle is symmetric); all three rows'
dispositions UNCHANGED (CARRY / CARRY / MATH), residuals pinned sharper than before; the
WELD-ZERO fence verified untouched at close.

**SCOPE FENCE, restated before any work (chapter F honesty F-2).** HYP.148's discharge arc
is WELD-ZERO (HYP.149): **IN-PROGRESS at clean-pass 1 of 2**, pass 2 of 2 reassigned to a
Fable-max agent and IN FLIGHT with no landing commit at HEAD. This unit does NOT anticipate
that pass, does NOT transcribe from `lean/notes/openmath/WELDZERO_2026-08-14.md` (no
effective spec exists), and does NOT touch WZ-1/WZ-2/WZ-B or the gr(O_L) frame. Everything
below narrows HYP.148 from the ABSTRACT side only: W-9's conclusions as algebra, at the
landed carrier vocabulary. HYP.148 stays CARRY and HYP.139 stays MATH at full strength
throughout; nothing here licenses a bucket move.

---

## 0. The row texts and the carrier vocabulary (sources, quoted)

**HYP.74 (`spec/HYPOTHESIS_LEDGER.md`):** "JD0 unit-character warrant is synthesis, not
proved theorem: 'That the weld's ENTIRE conditional value dictionary acts by slot-wise unit
characters is the synthesis §3.2 reading of JA-RES [GRTJA, 1/2], the (ξ,w) covariance family
[GRTW2, ACCEPTED], and the ℓ-orbit rescalings ORB-RES [J-A].'" Disposition CARRY,
reachability SIGNED at seven cone sites (W11 L599, W12 L1499, HMENU3 L717, HEX3 L720,
GENIND L1518, GENH4 L938, GENHN L1515), FRAME grade; "Discharge is a fold, not a theorem:
record per site that the gauge consumed there is an explicit z-power unit character verified
in-note, which strikes the inheritance."

**The perimeter display (`spec/EFF-JD0.md`, EFF.JD0.05, verbatim):**

> "**The carry gauge.** A 1-cochain u : γ ↦ u_γ ∈ K^* acting by anchor rescaling
> a_γ ↦ u_γ·a_γ. Its full documented action:
>
>     ledger:    c(γ,γ′)  ↦  c(γ,γ′) · u_γ u_{γ′} u_{γ+γ′}^{−1}   (= c·∂u)
>     slot:      v        ↦  u·v          (unit character, per read slot)
>     residual:  R(y)     ↦  ξ^{−deg R} · R(ξ·y)   (JA-RES geometric character;
>                the (ξ,w) overall unit dies in monic form)
>     positions, δ, D_j, grid abscissas:  UNCHANGED."

**HYP.139 (`spec/HYPOTHESIS_LEDGER.md`):** class MATH; GRTJC L1990–1993: "Transport of the
harness w and 𝑅 to the FGMN objects is still W-1 (ATTEMPT) — and the corpus's own march-level
dictionary for that transport, TR-3′-GEN, is OPEN at general order." "This row is the live
residue that does NOT discharge with HYP.148 by a transcription fold."

**W-1's transport law (`spec/EFF-GRTJC.md`, EFF.GRTJC.69, verbatim):** "(IN-3)'s transport
clause is 𝑅_λ = c_λ·R^{GMN}_λ with c_λ a LINE-WISE unit (W-1). Substituting,
𝑅_{λ+μ}(fg) = (c_{λ+μ}/(c_λc_μ))·y^δ·𝑅_λ(f)·𝑅_μ(g), so ω(λ,μ) = c_{λ+μ}/(c_λc_μ) — the
2-coboundary of the line-wise unit system. That ω depends only on the lines is what the
transport gives; **nothing gives ω ≡ 1.**"

**HYP.148 (`spec/HYPOTHESIS_LEDGER.md`):** "GRTJC W-6..W-9 + JC-LOAD/W-8 are consumed at
LEMMA GENHN-4 layer 1 (μ ≥ 3) — the ONE weld face inside a count law, disclosed
(GENHN-BOX-2)"; CARRY at full strength; carries HYP.139 as sub-residue; first live n = 6.

**W-9 (`spec/EFF-GRTJC.md`, EFF.GRTJC.89, verbatim):** "(i) c_{m+1} descends to a map
ℤ/E × ℤ/E → K^* and **is a 2-cocycle**: c(a,b)c(a+b,c) = c(b,c)c(a,b+c) …; (ii) its
y-degree part δ satisfies the integer cocycle identity … (the {0,1}-valued carry cocycle);
(iii) 𝒜(T) is the **twisted group algebra** K^{c}[ℤ/E] …; (iv) [NOT consumed at layer 1]."

**Landed carriers (gate-verified, `leanspec/Leanspec/ChapF.lean`, stage 0e 2026-08-16):**
`JD0Box2 site := ∃ u : Γ → Kˣ, site.slotAct = slotScale u ∧ site.ledgerAct = gaugeCobdry u`
(F.04); `GenhnBox2 s := 3 ≤ s.μ → ∃ (h : NeZero s.E) (cc : CarryCocycle s.E K), ∀ a b,
(cc.c a b : K) = s.carry a b` (F.11); `W1Transport Rh RG := ∃ c : ι → Kˣ, ∀ l,
Rh l = (c l : K) * RG l` (F.12, F-D2 respelling). Chapter F has landed NOTHING in
`leanfinal` before this unit; chapter I consumes the three carriers as Display-A conjunct
fields (CHAP-I I.08/I.09, §3 rows HYP.74/139/148).

---

## 1. OUTLINE (mandatory step 1) — proof steps per leg, each with its certification plan

### LEG A — HYP.74, the `JD0-BOX-2` side (prove what the corpus carries; pin the residual)

* **A-1 (mechanism, provable).** The four-line action's slot leg is a bijection and stratum
  counts are blind to it wherever the stratum is carried onto itself (F.02's
  `card_image_slotScale`, stubbed as `axiom` at stage 0e). CERTIFY: exhaustive count checks
  at q = 2 on F₄ and q = 3 on F₃/F₉; NONCHAR constructed-counterexample control (the
  additive slot shift — a bijection OUTSIDE the class that leaves the stratum). PROVE.
* **A-2 (mechanism, provable).** The ledger leg `c ↦ c·∂u` is an action by coboundaries:
  ∂u is a 2-cocycle, and gauging preserves the 2-cocycle identity — the perimeter's
  consistency with the F.07 carry-cocycle class (fresh but elementary; certify then prove
  as an F.04-adjacent witness). CERTIFY: exhaustive over enumerated cocycle tables at
  E ∈ {2,3}, both primes. PROVE.
* **A-3 (residual leg conclusions, provable).** `resTwist` degree/monicity/root census
  (F.03's three stubs), WZ-BOX-7-compliant (conclusions, never type-preservation). CERTIFY:
  exhaustive small-degree polynomials, both primes. PROVE.
* **A-4 (the fold route's site-independent half, provable).** A per-site STRIKE
  (`JD0SiteStrike`, F.05) whose ledger leg is also exhibited witnesses `JD0Box2` for its
  site — HYP.74's discharge fold, as a theorem shape. PROVE (definitional-adjacent).
* **A-5 (the F₂ degeneracy, provable).** F.06's vacuity disclosure as theorem. PROVE.
* **A-6 (the residual, pinned — NOT provable here).** (a) That the weld's actual value
  dictionary IS of the four-line form: synthesis, priced at GRTJA 1/2 — stays CARRY
  (CERTIFIED-OPEN; no in-cone route). (b) The seven per-site strike RECORDS: instances need
  chapter H/C site objects (GC-13) — REDUCES-TO those landings. Verdict target for the row:
  CARRY unchanged, mechanism side PROVED, residual = (a)+(b) exactly.

### LEG B — HYP.139, `W1Transport` at the landed carrier vocabulary (MATH-classed; proof attempt)

* **B-1 (characterization, provable).** At the carrier vocabulary the transport is an orbit
  relation: `W1Transport Rh RG ↔ ∃ c, Rh = slotScale c RG` (the two surviving weld conjuncts
  share the F.01 action), and over a field it is EQUIVALENT to equality of vanishing loci:
  `W1Transport Rh RG ↔ ∀ l, (Rh l = 0 ↔ RG l = 0)`. CERTIFY: exhaustive over all pairs at
  |ι| = 3, K ∈ {F₃, F₄} (+ sampled F₉); count formula cross-check. PROVE (an F.12-adjacent
  witness file).
* **B-2 (transport algebra, provable).** refl/symm/trans + count-blindness of transported
  families (via A-1). CERTIFY + PROVE.
* **B-3 (the DMULT-w mechanism shadow, provable).** A line-wise unit system c on an additive
  index induces ω(l,m) = c(l+m)·(c_l c_m)⁻¹, a 2-coboundary; ω satisfies exactly F.07's
  Kˣ-cocycle identity, and "nothing gives ω ≡ 1" — ω ≡ 1 iff c is a homomorphism. CERTIFY:
  exhaustive at ι = ℤ/6, both primes, with the exact count of ω ≡ 1 systems predicted
  (= #Hom(ℤ/6, Kˣ) · |Kˣ| tables? — pin the formula in the script); involution separator
  control (ω vs ω⁻¹ coincide at F₃, separate at F₄/F₅ — the F-gate's separator model).
  PROVE the cocycle law; exhibit the ω ≢ 1 witness by `decide`.
* **B-4 (the pin — what stays MATH).** The carrier's ∃ is the ANCHOR SHADOW of W-1: the
  open mathematics (TR-3′-GEN at general order; the compatibility of the unit system with
  the march-level dictionary) does NOT live in the carrier's existential — B-1 shows the
  carrier is decided by vanishing loci alone. HYP.139 therefore stays MATH at full
  strength; what this unit adds is the proved carrier-level layer chapter I's `w1` field
  actually types against, plus the machine-checked boundary of what that field can carry.

### LEG C — HYP.148 narrowed from the abstract side (the fenced arc untouched)

* **C-1 (W-9(i) abstract half, provable).** cocycle ⟹ associativity of the twisted
  convolution (F.09's stub). CERTIFY: at E = 2 the exact BICONDITIONAL over all 81 tables
  at F₄ (associativity holds iff the cocycle identity holds — both directions numerically);
  E = 3 enumerated cocycles, both primes; a planted non-cocycle breaks associativity
  (constructed counterexample). PROVE.
* **C-2 (W-9(iii) count form, provable).** finrank = E (F.10a stub), Nat.card = |K|^E
  (F.13 stub). CERTIFY (gate values re-fired + E = 3). PROVE.
* **C-3 (JC-F1's power basis, provable).** `v^E = (∏_{k=1}^{E−1} c(1,k)) • single 0 1`
  (F.10b stub). Supporting fresh-but-elementary lemmas, certified BEFORE proving:
  `c(0,b) = c(0,0) = c(a,0)` (degenerate rows are constant) and `c(1,k) = c(k,1)` (first
  row/column symmetry, from the telescoped cocycle identity); stretch: full symmetry
  `c(a,b) = c(b,a)` (every carry cocycle is symmetric — consistent with JC-F1's cyclic
  presentation, which forces commutativity). CERTIFY: exhaustive over ALL cocycle tables at
  E ∈ {2,3,4}, |Kˣ| ∈ {2,3,4}. PROVE.
* **C-4 (the face, pinned — NOT provable here).** That GENHN-4 layer 1's carry bookkeeping
  IS an instance of this shape is the CARRIED face; its candidate discharge is the fenced
  WELD-ZERO arc (pass 2 of 2 IN FLIGHT). After C-1..C-3 the abstract clauses (i)–(iii) are
  theorems, so the carried content of HYP.148 narrows to EXACTLY the instantiation claim
  (the `∃ cc` matching the site's carry table) plus HYP.139's transport — nothing else.
  Verdict target: CARRY unchanged, perimeter narrowed, fence intact.

**Statuses used below:** PROVED / CERTIFIED-OPEN / REFUTED(+repair) / REDUCES-TO / CITE-CANDIDATE.

---

## 2. CERTIFICATION (step 2) — `verification/openmath/om9_weld_cert.py`

**95 GREEN / 0 RED; mutants 8/8 caught** (two are the documented involution-separator
expected-miss-at-F₃ / caught-at-F₄ pairs — the F-gate's separator model carried to the new
legs). Exact arithmetic over the gate's table fields (F₄: θ² = θ+1; F₉: θ² = −1) plus
F₂/F₃/F₅. Runtime ~3 s. Output: `om9_weld_cert_output.txt`, results JSON alongside.

**LEG A (HYP.74).** A1: slotScale bijective + stratum-count-blind for ALL gauges/families at
F₄ and F₃; NONCHAR control: the additive shift is a bijection whose image is DISJOINT from
the stratum (outside the four-line class, exactly J-D0's documented failure mode); zero-letter
mutant caught. A2: **the count law |Z²(ℤ/E, Kˣ)| = |Kˣ|^E confirmed exactly at six (K,E)
pairs** (brute force), and gaugeCobdry maps every cocycle to a cocycle under every gauge
(the perimeter's consistency — then proved, F04w); single-entry-flip mutant caught. A3:
resTwist degree/leading-coefficient/root-census exhaustive at F₄ and F₉ (deg ≤ 2, all ξ);
the wrong-normalization ξ^{+deg} mutant caught at F₄, invisible at F₃ (involution regime —
documented, mirrors A-W.1/F-D3's Kˣ-inverse wall). A4/A5: z-power strikes unit-valued; F₂
triviality.

**LEG B (HYP.139).** B1: W1Transport ⟺ equal vanishing loci, brute-force over ALL 729 (F₃)
and 4096 (F₄) pairs at |ι| = 3; **transportable-pair count = ((q−1)²+1)³ exact** (125 of
729; 1000 of 4096). B2: refl/symm/trans exhaustive at F₃. B3: ω = c(l+m)/(c_l c_m) is a
2-cocycle for ALL (Kˣ)^{ℤ/6} systems at F₃/F₄/F₅; **#{ω ≡ 1} = #Hom(ℤ/6, Kˣ) = gcd(6, q−1)
exact** (2/64 at F₃, 3/729 at F₄, 2/4096 at F₅) — "nothing gives ω ≡ 1" with the refusal
count displayed; the ω-vs-ω⁻¹ separator: coincide at F₃ (all units involutions), separated
at F₄/F₅ — mutant pair caught/expected-missed accordingly. B4: concrete ω ≠ 1 witness.

**LEG C (HYP.148, abstract side only).** C1: **associativity ⟺ cocycle identity as an exact
biconditional over ALL unit tables** at (F₄,2): 9 = 9 of 81; (F₃,2): 4 = 4; (F₃,3): 8 = 8 of
512; (F₅,2): 16 = 16; full-function associativity on all 27 cocycles at (F₄,3); planted
non-cocycle breaks associativity at a singles triple (caught). C2: cardinality q^E. C3: over
EVERY enumerated cocycle at E ∈ {2,3,4} × |Kˣ| ∈ {2,3,4} (brute where ≤ 2M tables, else the
∂u·wrap(ζ) generated family, itself verified and count-matched): degenerate rows constant;
first-row symmetry c(1,k) = c(k,1); **FULL symmetry c(a,b) = c(b,a)** (the new structural
fact, then proved); the power basis v^E = (∏ c(1,k))·single 0 1 under BOTH the right
(npowRec) and left (JC-F1) recursions; asymmetric-table mutant caught. C4: the {0,1} integer
carry census at E = 2 (4 solutions) and E = 3 (6), standard wrap present.

## 3. LEAN LANDINGS (step 3) — `leanfinal/Uniformity/ChapF/` (first chapter-F landings)

Sixteen files, every declaration at the stage-0e gate's signed signature (A-W.1 spellings),
each `lake build Uniformity.ChapF.F<nn>` green; **all 29 proved theorems Lean-core**
(`#print axioms` ⊆ {propext, Classical.choice, Quot.sound}); zero `sorry`; no roll-up/root
edits (orchestrator's pass, OM-7/C.93 precedent). Registered as blueprint AMENDMENT A-W.2.

| leg | files | content | status |
|---|---|---|---|
| carriers | F01, F04, F05, F07, F11, F12 | `gaugeCobdry`/`slotScale`; `ValueDictSite`/`JD0Box2`; `JD0SiteStrike`; `CarryCocycle`; `CarrySite`/`GenhnBox2`; `W1Transport` | LANDED (definitional, byte-exact) |
| HYP.74 proved layer | F02, F03, F06, F04w | count-blindness; resTwist ×3; F₂ vacuity; strike⟹perimeter + gauged-cocycle preservation + action law | **PROVED** (4 former axiom stubs + 4 witness theorems) |
| HYP.148 abstract layer | F08, F09, F10a, F10b, F13 | twisted algebra + unit laws; **assoc from cocycle**; finrank = E; **power basis at the signed ∏ c(1,k)**; card = q^E; `c_zero_left/right`, `c_one_comm`, **`c_comm`** | **PROVED** (5 former axiom stubs + helpers + 1 fresh theorem) |
| HYP.139 proved layer | F12w | **`w1Transport_iff_zero_iff`** (carrier ⟺ equal vanishing loci); orbit identification with slotScale; refl/symm/trans; `of_ne_zero`; `lineCobdry` cocycle law + ω ≡ 1 iff hom + ω ≠ 1 witness | **PROVED** |

Commits: `5c186ca2` (skeleton), `c4d75160` (battery), `badbb9cb` (carriers + F.02/03/06 +
F.07 lemmas), `aac26996` (W-9 layer F.08–F.13), `1c7688ea` (witness files), `f91a4600`
(`c_comm`), plus the docs commit closing this unit.

## 4. PER-STEP VERDICTS, LEDGER EFFECT, CHAP-I NOTES

| outline step | verdict |
|---|---|
| A-1 slot mechanism | **PROVED** (F.02 + F.30-model controls) |
| A-2 ledger-leg consistency | **PROVED** (`gaugeCobdry_isCocycle`, fresh, certified first) |
| A-3 residual conclusions | **PROVED** (F.03 ×3) |
| A-4 strike ⟹ perimeter | **PROVED** (`JD0SiteStrike.jd0Box2`) |
| A-5 F₂ vacuity | **PROVED** (F.06) |
| A-6a four-line-form warrant | **CERTIFIED-OPEN** (synthesis, GRTJA 1/2 — stays CARRY; no in-cone route) |
| A-6b seven per-site strikes | **REDUCES-TO** chapter H/C site landings (GC-13); one-lemma discharge each once sites exist |
| B-1 characterization | **PROVED** (`w1Transport_iff_zero_iff` + orbit form) |
| B-2 transport algebra | **PROVED** (refl/symm/trans + `of_ne_zero`) |
| B-3 DMULT-w mechanism | **PROVED** (`lineCobdry_cocycle`, `lineCobdry_eq_one_iff`, ω ≠ 1 witness) |
| B-4 the MATH pin | **CERTIFIED-OPEN** — HYP.139 stays MATH; its content is provably NOT in the carrier (see below) |
| C-1 assoc ⟸ cocycle | **PROVED** (F.09; biconditional certified both directions) |
| C-2 count forms | **PROVED** (F.10a, F.13) |
| C-3 power basis + symmetry | **PROVED** (F.10b; `c_one_comm`; fresh `c_comm`) |
| C-4 the face | **CERTIFIED-OPEN** — stays CARRY; carried content narrowed to the bare instantiation claim + HYP.139 |

**Ledger effect (ARC NOTEs appended, NO bucket moves):** HYP.74 CARRY unchanged — residual
pinned to (synthesis warrant) + (seven strike records). HYP.139 MATH unchanged — with the
new machine-checked boundary: the carrier's ∃ is decided by vanishing loci, so no
carrier-level instantiation can ever discharge this row; a discharge claim must point at
site-level mathematics or the WELD-ZERO arc passing. HYP.148 CARRY unchanged — narrowed:
everything downstream of "the site's carry table IS a `CarryCocycle`" is now a Lean theorem.
HYP.149 untouched. **CHAP-I:** ADDENDUM (unit OM-9) after OM-7's — I.08/I.09's late-binding
targets now exist; L-1/L-2 can bind real names at F freeze; the `w1` field must carry the
strength note so a typed instantiation is never mistaken for a discharge.

**Fresh mathematics introduced (all certified before proving, all elementary):**
`gaugeCobdry_isCocycle` (gauging preserves the 2-cocycle class); `CarryCocycle.c_zero_left/
c_zero_right/c_one_comm`; **`CarryCocycle.c_comm`** — every carry cocycle over `ZMod E` is
symmetric (via coefficient extraction of v^{m+n} = v^m·v^n; consistent with — and an
abstract echo of — JC-F1's cyclic presentation; does not touch JC-BOX-5's anchor-indexed
fence); `w1Transport_iff_zero_iff`; `lineCobdry_cocycle`/`lineCobdry_eq_one_iff`. The
certified count laws |Z²| = |Kˣ|^E and #{ω ≡ 1} = gcd are script-level teeth (classical
group cohomology of cyclic groups; not landed in Lean — not needed by any row).

## 5. WELD-ZERO FENCE STATEMENT (what remains fenced behind pass 2)

Verified at close as at open: `HYP.149` is IN-PROGRESS at **clean-pass 1 of 2**; pass 2 of 2
is reassigned to a Fable-max agent and IN FLIGHT with **no landing commit in the tree**; the
unit note `lean/notes/openmath/WELDZERO_2026-08-14.md` still reads "composed, attempt 0/2,
hostile arc owed". **This unit consumed nothing from that arc**: no WZ lemma (WZ-1/WZ-2/WZ-B),
no gr(O_L) frame, no wrap-floor formula, no transcription from the WELDZERO note (which has
no effective spec). Behind the fence remain, exactly as before: (i) the DISCHARGE of HYP.148
(striking `GenhnBox2` as a carried conjunct) and (ii) the retirement of HYP.139 as its side
effect — both fire only via §8's pre-authorized amendment after the ledger books a passed
arc (the 0b fold's authority, owner-gated per CHAP-I O-3). Nothing in this unit's landings
would need to change if the arc passes (the abstract theorems are unconditional) or if it
fails (the carriers stand at ledger strength either way) — the work is fence-independent by
construction.

**Leanspec stub lifecycle (flagged for the orchestrator, not executed here):** 9 proved
theorem rows + the landed definitional declarations are ready for
`leanspec/Leanspec/ChapF.lean`'s stub retirement to the `example : <type> := <name>` form
(the file's RESUME item (iii)); the ChapF roll-up + `Uniformity.lean` root line are the
fleet/orchestrator's regeneration pass.
