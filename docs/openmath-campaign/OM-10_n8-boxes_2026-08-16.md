# OM-10 — the n ≥ 8 box family (HYP.67–71, HYP.78–80, HYP.142, HYP.143, HYP.144-BOX-2) — unit note, 2026-08-16

**Charge.** The terminal residue of HYP.81/HYP.82 after OM-1..OM-9: the ten-row `n ≥ 8`
open family. OM-7 verified the family leaks nothing below n = 8 (its §2.7 no-leak check,
row-by-row against CHAP-I §3). Mandate (owner directive, verbatim intent): (1) FIRST a
high-level outline for the WHOLE family, grouped by mechanism, not by number; (2) each
step CERTIFIED before proving (exact-rational/PARI scripts in `verification/`, constructed
counterexamples with simultaneous-defeat equations); (3) only certified steps get proof
effort. Statuses: PROVED / CERTIFIED-OPEN / REFUTED(+repair) / REDUCES-TO(<row>).
Fences honored: WELD-ZERO arc at clean-pass 1 of 2 — nothing consumed from
`lean/notes/openmath/WELDZERO_2026-08-14.md`; I.10 consumption rules (A-E.2/A-D.2 forms
only); HYP.112's candidate fence on every GROUP-E object; never weaken, never push.

**Verdict summary** (details §3; certifications §2; outline §1):

| row | verdict |
|---|---|
| HYP.67 `GENTOW6-BOX-1` (μ₂ ≥ 3 attainment, f₁ ≥ 2 face) | **CARRY, narrowed** — f₁ = 1 half CLOSED at source (6.5); the f₁ ≥ 2 face gets its FIRST machine contact here (GROUP C: 6 frames GREEN over 3 keys, D′ ∈ {2,4}, η ≠ 1 included); the naive 6.5 laws hold VERBATIM at the measured t* = 0, f₂ = 1 geographies; residue = the 6.3′ full-exposure theorem + t* > 0 / f₂ ≥ 2 geographies + j < j* |
| HYP.68 `GENTOW6-BOX-2` (count ledger, partial sides) | **REDUCES-TO(GENTOW-6.6) + record reconciled** — the boxed pricing EXISTS as THEOREM GENTOW-6.6(a)–(d) (BOXES note, 0/2); first direct window-enumeration check of the ledger identity GREEN (LEG D); residual = 6.6's hostile arc (process) + the aggregation seam (out of family) |
| HYP.69 `GENTOW6-BOX-3` (regime-3 classification) | **CARRY, narrowed to the above-floor member half** — key-power half + exactly-Θ member half CLOSED at source (6.7/6.7E); residue = HYP.78's surface exactly (E-S3 re-verification GREEN at a fresh regime-3 genre) |
| HYP.70 `GENTOW6-BOX-4` residue (all-f₁ ≥ 2 widening) | **CARRY, narrowed** — REDUCES-TO the 6.3′ full-exposure calculation; one sealed row reproduced fresh (F423m2: cert 256, pin 47 bit-equal) + TWO new x-ful f₁ = 2 rows at a new key shape (D′ = 2, e₁ = 1) GREEN; the widening stays open at theorem grade |
| HYP.71 `GENTOW3-BOX-1` (nonvanishing + entry-driven witnesses) | **CARRY, conjunct (b) REFUTED(+repair) at the measured geography** — the asked-for witnesses are INFEASIBLE at all-in-grid genres: LAW E-W (certified 54/54, tight per T-E-BOUND) pins every in-locus member's discrepancy STRICTLY above Θ; repair = re-scope (iv)-sharpness to certificate-carrying geographies; conjunct (a) genre-general nonvanishing stays open (HYP.112 fence) |
| HYP.78 `S8.1-MEMBER` (above-floor divergence membership) | **CARRY, narrowed** — at the single-crossing all-in-grid geography the classification is COMPLETE and linear (LAW E-W: diverges iff a crossing entry exists; height = Θ + excess; unit-c independence E-S4 GREEN); fresh diverge-vs-not pair inside one genre reproduced; the general (multi-branch) case stays the census's hard-unit, now LOCALIZED to multi-crossing geographies |
| HYP.79 `P-BINOM` (p ∣ binom(m,2) classification) | **narrowed by two certified laws** — single-slot geography: attainment iff p ∤ binom(μ₂,2), certified 9/9 frames incl. three fresh (X5/P36/P38), corollary-shape from 6.5(a)+(b) at their grade; cascade geography: LAW B-S2 pair-or-triple, 7/7, with the naive any-k candidate REFUTED at B4 (binom(4,4) = 1 a unit yet pin = Θ+1 — kept); lower-coordinate cases stay open |
| HYP.80 `COMPOUND-6.5′` (f₁ ≥ 2 × μ₂ ≥ 3) | **CERTIFIED-OPEN, first machine contact** — the prose-only compound now has 3 GREEN μ₂ = 3 frames incl. BOX-4's own sealed key one μ₂ up (F423m3: cert 768 at Θ exactly); census S-3's "no machine" clause discharged; theorem-grade classification open (rides HYP.67's face) |
| HYP.142 `GENHN-ERR-1` dual disposition | **RECONCILED — the GENHN record governs, NO escalation to MATH** — the lemma HE6R1 owed EXISTS (LEMMA GENHN-COUNT-RERUN, 2026-08-12); transcription-confirmation PERFORMED (D-S1, verbatim vs `runs/cert4/P2_genhn_count_output.log`); decorrelated F₉ leg GREEN (first odd-characteristic f₁ = 2 count contact) with the S-node tooth killed |
| HYP.143 `HE3-BOX-6` ramified branch (ii) | **CERTIFIED-OPEN, narrowed + first machine contact** — branch (ii) machine-contacted for the FIRST time (41 rows: μ = 4, ℓ = 2 sides, both d-shapes, (e₁,f₁) ∈ {(2,1),(1,2)}, both characteristics; class sizes = D′ℓd on every row, the box's failure mode EXCLUDED on every row); the repair route's instruments CERTIFIED (separation A-S3, anchor polygons A-S4, count inversion A-S5); residual = the slot lemma at Ψ's roots + the test-side evaluation display (the box's named further step, not executed) |
| HYP.144-BOX-2 (GENTOW4 partial-side COUNT layer) | **REDUCES-TO(GENTOW-6.6 iteration)** — the two-class pricing + iteration clause covers the bouquet form; instantiation hypotheses (H1)–(H3) certified at a GENTOW4-style frame (D-S3); residual = the sibling-data box hypothesis at theorem grade + 6.6's grade |

---

## §1. HIGH-LEVEL OUTLINE (mandated order) — the family grouped by MECHANISM

### 1.1 The ledger rows, verbatim (spec/HYPOTHESIS_LEDGER.md at HEAD)

> HYP.67  `GENTOW6-BOX-1`
> WHERE: `T4_CERTIFICATE_WITNESS_2026-08-12.md`, §S7 L526–539; upstream GENTOW6.
> STATEMENT: Lower-coordinate classification at μ₂≥3, including f₁≥2 compound face, is incomplete.
> CLASS: hypothesis-box
> PROPOSED DISPOSITION: MATH — retained. [...] Real home: the GENTOW6/T4 attainment layer
> (note GENTOW6 L1201–1204 records BOX-1 **CLOSED AT f1 = 1**, leaving the f₁≥2 face).

> HYP.68  `GENTOW6-BOX-2`
> WHERE: T4 §S7; upstream GENTOW6.
> STATEMENT: Product-measure count is open already at FULL sides; budget half is proved and
> only ledger/count half remains.
> CLASS: hypothesis-box
> PROPOSED DISPOSITION: MATH — retained; consumer re-homed [beside HYP.144's GENTOW4-BOX-2
> open COUNT layer], reachability as a tower-leg residue not settled by this audit.

> HYP.69  `GENTOW6-BOX-3`
> WHERE: T4 §S7; upstream GENTOW6.
> STATEMENT: Entry-driven/non-top-overflow cases lack exhaustive classification.
> CLASS: hypothesis-box
> PROPOSED DISPOSITION: MATH — retained; consumer re-homed [GENTOW6's own regime-3 display].

> HYP.70  `GENTOW6-BOX-4` residue
> WHERE: T4 §S7 L533–535/L2105–2109; `GENTOW6_PROOF_2026-08-09.md` L1249–1255.
> STATEMENT: Sealed-row ask is DISCHARGED at HEAD, GREEN 265/0 at four x-ful rows including
> f₁=2; only theorem-grade all-f₁≥2 widening remains unsealed.
> CLASS: theorem-strength-residue
> PROPOSED DISPOSITION: MATH — no OPEN-CALL; keep battery discharge distinct from widening.

> HYP.71  `GENTOW3-BOX-1`
> WHERE: T4 §S7; upstream GENTOW3.
> STATEMENT: Neither genre-general nonvanishing nor battery witnesses for entry-driven
> overflow with self-power below D′ are delivered.
> CLASS: hypothesis-box
> PROPOSED DISPOSITION: MATH — retained; consumer re-homed [GENTOW-6.3 / CERT-TOP]. Read
> with HYP.112's fence: a candidate implies neither ω_j≠0, touched j, nor threshold attainment.

> HYP.78  `S8.1-MEMBER`
> WHERE: census S-1.
> STATEMENT: Above-floor divergence cases have complete membership classification.
> CONSUMED BY: drainage/value decomposition.
> CLASS: named-obligation
> PROPOSED DISPOSITION: MATH — pending RCT hard unit; no OPEN-CALL/HYP.73 dependency.

> HYP.79  `P-BINOM`
> WHERE: census S-2.
> STATEMENT: If p divides binom(m,2), floor attainment and lower-coordinate cases are classified.
> CONSUMED BY: compound value branch.
> CLASS: named-obligation
> PROPOSED DISPOSITION: MATH — rider on HYP.78; no OPEN-CALL.

> HYP.80  `COMPOUND-6.5′`
> WHERE: census S-3.
> STATEMENT: f₁≥2, μ₂≥3 compound case has required classification and measure law.
> CONSUMED BY: compound genre under [H-c].
> CLASS: named-obligation
> PROPOSED DISPOSITION: MATH — proof+machine unit; no OPEN-CALL/HYP.73 dependency.

> HYP.142  `GENHN-ERR-1` dual disposition
> WHERE: `lean/notes/openmath/GENHN_PROOF_2026-08-08.md` L2593, L2624; `HE6R1_RECON_2026-08-08.md`
> L926, L1031.
> STATEMENT: Two un-reconciled records at HEAD. Settled side: "**GENHN-ERR-1 is settled.** The
> ordinary μ=2 stage law is the (2,K)-law at B=D′h, not at the old numeric node S=e₁h" (L2593),
> with "a final transcription-confirmation rides the next queue" (L2624). Un-retracted side:
> "Repairing the completion statement without weakening it **requires a new lemma** re-running
> GENHN's f₁≥2 count argument on the corrected region" (HE6R1 L926), and the dated re-display
> heads "R7 finding 1 closed under the owner's directive; **finding 2 routed to a proof unit**" (L1031).
> CLASS: corpus-consistency-item
> PROPOSED DISPOSITION: NODE — reconcile the two records and say which governs; escalates to
> MATH if the HE6R1 reading governs, since that reading owes a new count lemma.

> HYP.143  `HE3-BOX-6` ramified branch (ii)
> WHERE: `lean/notes/openmath/HE3_PROOF_2026-08-08.md` L1334–1361, L1694.
> STATEMENT: "**HE3-BOX-6 (THE ONE SURVIVING GAP** [...] it localizes to μ≥4, so μ=3 is
> unconditional)." LEMMA HE3-4 branch (ii) has no test key at a non-integer dv height, and the
> substitute ramified base change O ↝ O[Π]/(Π^ℓ−π) is "stated too cheaply: **Φ′ need not remain
> irreducible over O₂**", so the SLOT LEMMA and HE3-1's norm computation are unavailable there.
> Post-R8: "**Only the ramified branch (ii) remains in HE3-BOX-6.**" The repair route
> (higher-degree test polynomials at fractional heights, i.e. the higher-order Montes
> construction) is "IDENTIFIED REPAIR ROUTE (**not executed here**)".
> CLASS: hypothesis-box
> PROPOSED DISPOSITION: MATH — named repair route, not executed. Supplier-side only: it
> refines HYP.81 rather than adding a conjunct.

> HYP.144  `GENTOW4-BOX-1/-2/-4`  [THIS UNIT'S SLICE: BOX-2 ONLY — CHAP-I splits the row:
> BOX-1 DISCHARGED-BY C.67 (cite-conditional), BOX-4 NOT-ON-CAPSTONE-PATH (grade cap)]
> STATEMENT (BOX-2 clause): BOX-2 (partial-side entry budgets) — the budget fibration is
> proved only on the FULL-side locus 𝒯; "the item-(4) **COUNT layer stays open**".
> CLASS: hypothesis-box
> PROPOSED DISPOSITION: MATH — BOX-2's open COUNT layer is the binding residue.

**Non-textual leg per load-bearing formula (extraction-corruption rule):** every formula
quoted above is re-derived or re-measured by `verification/openmath/om10_cert.py` (§2) —
the Θ_j floors, the j*/b* coordinates, the binom(μ₂,2) certificate digit, the D′ℓd class
size, the (2,K)-law node B = D′h, and the 6.6 ledger constant c_J are each recomputed
from definitions in exact arithmetic, independent of the quoted text.

### 1.2 The five mechanism groups

The ten rows decompose by MECHANISM into five groups. Two observations drive the grouping:
(i) six of the rows are residues of ONE instrument — the GENTOW6 shadow ledger (the
two-variable division of a level-2 member by the key development K2, scored by graded
digits against the Θ_j floors) — cut along three axes (μ₂ ≥ 3, f₁ ≥ 2, entry drivers);
(ii) the remaining rows are one class-size law (HE3-4's fractional-slope branch), one
count-pricing theorem (Sylvester/resultant), and one record reconciliation.

* **GROUP A — the fractional-slope class-size law** (HYP.143). Mechanism: LEMMA HE3-4's
  branch (ii) — at a side of dv-slope λ = u/ℓ (lowest terms, ℓ ≥ 2) with residual degree
  d_λ ≥ 2, the class of each irreducible residual factor r must have |S_r| = D′·ℓ·deg r.
  Live only at μ ≥ 4 ∧ D′ ≥ 2, i.e. n ≥ 8 (OM-7's Lean `branch_ii_needs_mu_four` +
  `mu4_lives_at_eight`).
* **GROUP B — the μ₂ ≥ 3 top-coordinate classification** (HYP.79 + HYP.67's p ∣ binom
  slice). Mechanism: THEOREM GENTOW-6.5(b)'s certificate digit binom(μ₂,2)·ĉ_{t*}²·π^{2a}
  dies in grade exactly when p ∣ binom(μ₂,2); the classification of attainment then moves
  to the OTHER coordinate-j* slots (the k-diagonal extraction branches, k ≥ 3, with
  binom(μ₂,k)-type digits — in-band only at the e₂(f₂−t*) = 1 geography).
* **GROUP C — the f₁ ≥ 2 compound** (HYP.67's f₁ ≥ 2 face, HYP.80, HYP.70). Mechanism:
  the same shadow ledger at keys with f₁ ≥ 2 (ANNEX 6.3′'s corrected lift) — prose-only,
  machine-uncontacted at HEAD ("a NEW honest fence since BOX-4's leg is μ₂ = 2").
* **GROUP D — the count layer** (HYP.68, HYP.144-BOX-2, HYP.142). Mechanism: window
  counts. Three sub-mechanisms: the multiplication pricing (Sylvester Jacobian, THEOREM
  GENTOW-6.6), its partial-side bouquet instantiation (GENTOW4's locus), and the μ = 2
  node law at the corrected node B = D′h (LEMMA GENHN-COUNT-RERUN vs HE6R1's obstruction
  record).
* **GROUP E — the entry-driven certificates** (HYP.71, HYP.69, HYP.78). Mechanism: the
  extraction ledger's ENTRY branches (member digits C_j crossing lifts, overflow iff
  a + i_t ≥ D′) as certificate carriers — witnesses, the χ-criterion's residue, and the
  above-floor membership question.

### 1.3 Step decomposition (statuses in §3)

**GROUP A** (HYP.143):
* **A-S1** (arith): reachability — branch (ii) needs μ ≥ 4 ∧ D′ ≥ 2 ⟹ n ≥ 8. Already
  PROVED in Lean by OM-7 (`H73w.lean`); consumed here as the family fence, no new work.
* **A-S2** (construction + oracle): FIRST machine contact with branch (ii) — μ = 4
  members at decided leaves with an ℓ = 2 side, in all four cells
  {two linear labels d = 1+1} × {one irreducible quadratic label d = 2} ×
  {(e₁,f₁) = (2,1), (1,2)}, mixed AND equal characteristic. Check |S_r| = D′ℓd per label
  (elementary value-group/residue-degree certificates + PARI decorrelation) and that the
  box's displayed failure mode ("one class empty while the other carries two Galois
  orbits") does NOT occur.
* **A-S3** (the higher-order anchor): the test-polynomial separation. Ψ_s := Φ′^ℓ − C_{u,s}
  (C a letter carrier at the INTEGER height u = ℓλ). Certify: (i) per irreducible factor
  g of f, v(Res(g, Ψ_s)) jumps above the generic value exactly at the label s of g;
  (ii) Ψ_s's own stage polygon is one side of slope λ with a single label (gcd(u,ℓ) = 1
  makes the binomial resolvent Y^ℓ − c single-sided with lattice-primitive slope).
* **A-S4** (proof attempt): the (T1)/(T2) half of "the slot lemma at Ψ's roots" — every
  root ξ of Ψ_s has dv(Φ′(ξ)) = λ > D′h, hence satisfies (T1)/(T2) by LEMMA HE3-0's
  argument verbatim; so HE6-0″ gives dv(A_j(ξ)) = dv(A_j(θ)) at Ψ's roots.
* **A-S5** (the residual gap, named): the fractional-height count inversion — HE3-3's
  analogue for the family {Ψ_{s,κ}}: the two-sided sample identity
  Σ_ρ dv(Ψ_s-read at ρ) = Σ_ξ dv(f-read at ξ) with the polygon inversion recovering
  |S_r|. Certify numerically; proof is the genuine further step (the box's own words).
* **A-S6** (named, out of scope): the μ ≥ 4 stage-CS O_L re-carrier (HE3 §S1 [r1,F7]).

**GROUP B** (HYP.79; rider on HYP.67's lower coordinates):
* **B-S1** (proof target): the single-slot geography law — at genres where coordinate j*
  carries ONLY the certificate slot (the f₂ = 1/t* = 0 geography: X/P3 frames),
  p ∣ binom(μ₂,2) ⟹ NO floor attainment at j* (the coordinate's one graded digit dies;
  6.5(a) empties every other slot). Certify at fresh frames (X5; P36), then prove from
  6.5(a)+(b2)'s ledger.
* **B-S2** (examples-first law): the k-cascade at the e₂(f₂−t*) = 1 geography — measure
  the B/BP-geometry at μ₂ = 3,4,5,6 and both primes; read off the law "attainment is
  decided by the first in-band k-diagonal branch whose binom(μ₂,k)·(digit) is a p-unit"
  (Kummer-governed); status target CERTIFIED-OPEN as a certified law candidate.
* **B-S3** (rider): the j < j* cascade (HYP.67's lower-coordinate face at f₁ = 1) —
  re-verify S3's display slots at the new frames; the classification stays open.

**GROUP C** (HYP.67 f₁ ≥ 2 face, HYP.80, HYP.70):
* **C-S1**: FIRST machine contact of the f₁ ≥ 2 × μ₂ ≥ 3 compound — dual-route shadow
  (route R ≡ route E bit-equality as the internal check) at f₁ = 2 keys (both e₁ = 1 and
  the e₁ = 2, f₁ = 2 mixed shape if reachable), μ₂ = 3: measure the ceiling clause (a),
  the top-slot certificate (b1) at the corrected ĉ, and the Θ_j floors.
* **C-S2** (HYP.70): one NEW x-ful f₁ = 2 row beyond BOX-4's four sealed ones (fresh key
  or fresh c₀), measured at the same wants; the theorem-grade widening = ANNEX 6.3′'s
  full-exposure calculation, REDUCES-TO C-S1's geography.
* **C-S3**: statuses — CERTIFIED-OPEN expected; a REFUTED(+repair) verdict if the naive
  f₁ = 1 displays fail verbatim at f₁ ≥ 2 (which the T4 record predicts for SOME displays:
  "are NOT proved as written").

**GROUP D** (HYP.68, HYP.144-BOX-2, HYP.142):
* **D-S1** (HYP.142): the reconciliation. (i) Timeline: HE6R1's L926 obstruction record
  (R7 finding 2, A4 adjudication) ROUTES the repair to a scheduled proof unit (its own
  tail: "the count re-run on the corrected node floor ... is hereby a scheduled proof
  unit ... it simultaneously settles GENHN-ERR-1"); GENHN's dated proof completion
  (2026-08-12, LEMMA GENHN-COUNT-RERUN + machine certificate) IS that unit's landing, and
  its consumer sweep item 5 restores HE6R1's completion clause with no wording change.
  So the two records AGREE at HEAD: the GENHN record governs BECAUSE the HE6R1 record's
  own demand is satisfied by it. NO escalation to MATH. (ii) Perform the residual
  "final transcription-confirmation" (GENHN L2624): verbatim check of the transcribed
  lemma against `runs/cert4/P2_genhn_count_output.log`. (iii) Decorrelated machine leg:
  the corrected-node count law at F₉ (first odd-characteristic f₁ = 2 contact), fresh
  implementation, with the S-node mutant tooth.
* **D-S2** (HYP.68): the ledger reconciliation — the row's "ledger/count half remains"
  is supplied at HEAD by THEOREM GENTOW-6.6(a)–(d) (BOXES note S7) at 0/2 grade; the
  full-side clause is 6.6's own record ("at a one-class locus ... the ledger is
  GENTOW-1(b) + its R2a clip verbatim", GENTOW-1 ACCEPTED 2/2). Certify: first direct
  window-enumeration check of 6.6(c)'s identity #(m(B_S×B_g) mod π^N) =
  q^{−c}·#(B_S mod π^N)·#(B_g mod π^N) and the q^c fiber clause, plus the c ± 1 tooth.
  Status: REDUCES-TO(GENTOW-6.6) with residual = the hostile arc (process) + the
  aggregation seam (GENH5-DENS geography, outside this family).
* **D-S3** (HYP.144-BOX-2): the GENTOW4 partial-side instantiation — certify that
  GENTOW4-style partial-side loci (products of class boxes × sibling data) satisfy
  (H1)–(H3) at a concrete frame and that the counted locus obeys 6.6(d)'s display;
  status REDUCES-TO(GENTOW-6.6 iteration) with the named residual (the sibling-data box
  hypotheses at theorem grade, per-genre).

**GROUP E** (HYP.71, HYP.69, HYP.78):
* **E-S1** (HYP.71(b)): DELIVER the missing witnesses — genres with 2i_{t*} < D′ (key
  self-power in-grid in x) where an ENTRY-driven overflow branch attains Θ with a nonzero
  graded digit; exact ledger + dual-route shadow measurement, ≥ 2 primes.
* **E-S2** (HYP.71(a)): verify ω_j ≠ 0 at each witness; the genre-general nonvanishing
  stays open (T4: "replaced, not magically solved, by the explicit witness ω_j").
* **E-S3** (HYP.69): re-verify the 6.7 χ-criterion + 6.7E coverage at a fresh regime-3
  genre; record the residue map (remaining surface = HYP.78 + GROUP B's μ₂ ≥ 3 lower
  coordinates — nothing else).
* **E-S4** (HYP.78): (i) certify the decision-procedure form (the shadow of any member is
  an exact finite object; divergence-vs-not is decided by finite arithmetic); reproduce
  the E-ENTRY vs key-power obstruction pair fresh; (ii) ATTACK a member-level congruence
  criterion (leading-entry-digit congruence) by constructing a defeating member if one
  exists; a surviving criterion is a law candidate, a defeat sharpens the obstruction.

### 1.4 What this unit does NOT claim

No GENTOW6/GENTOW3/GENTOW4/GENHN/HE3/HE6R1 sealed display is edited (dated ledger ARC
NOTEs + this note only). No box is declared discharged unless the discharge is complete
and unconditional. Lean landings only where the vocabulary reaches cheaply (the family is
corpus-side genre arithmetic; OM-7's H73w already owns the family's Lean-shaped fences).
GROUP A's A-S5 remains open mathematics after this unit — named, certified, not proved.

---

## §2. CERTIFICATIONS

Two scripts, committed with outputs:
* `verification/openmath/om10_cert.py` → `om10_cert_output.txt` — **VERDICT GREEN,
  569 checks, 0 violations, 9/9 planted teeth killed** (md5 script `8d92751a…`, output
  `c1da3252…`).
* `verification/openmath/om10_f9_count.py` → `om10_f9_full_output.txt` — **VERDICT GREEN**
  on all three runs (smoke 9⁶, mixed 9⁶, full 9⁸ = 43,046,721 members exhaustive; output
  md5 `c7e73491…`); teeth T-F9-Q, T-F9-SNODE, T-F9-SEAL all killed.

### 2.1 LEG A — HYP.143 branch (ii): the FIRST machine contact (88 checks)

Branch (ii) of LEMMA HE3-4 was **machine-unreachable at μ = 3, first live at μ = 4**
(HE3-BOX-5 priority 1); no machine leg existed at HEAD (HE3-BOX-3). This leg constructs
the box's exact geography — a decided-leaf stage polygon with ONE side of fractional
dv-slope λ = 5/2 (ℓ = 2), length 4, separable residual of degree 2 — in all four cells:

| row | O | key (e₁,f₁) | residual | law prediction | verdict |
|---|---|---|---|---|---|
| A1a | ℤ₅ | x²−5 (2,1) | (Z−1)(Z−2) | σ = {(4,1),(4,1)}, \|S_r\| = 4 = D′ℓd each | ✓ PARI + elementary |
| A1b | ℤ₅ | x²−5 (2,1) | Z²−2 irred | σ = {(4,2)}, \|S_r\| = 8 = D′ℓd | ✓ PARI + elementary |
| A2a | ℤ₃ | x²−18 (1,2) | (Z−1)(Z+1) | σ = {(2,2),(2,2)} | ✓ PARI + elementary |
| A2b | ℤ₃ | x²−18 (1,2) | Z²−ω, ω ∈ F₉ non-square | σ = {(2,4)} | ✓ PARI + elementary |
| A3a | F₃[[t]] | x²−t (2,1) | Z²+2 = (Z−1)(Z+1) | two (4,1) classes | ✓ construction-certified |
| A3b | F₃[[t]] | x²−t (2,1) | Z²+1 irred | (4,2) octic | ✓ construction-certified |

The elementary certificates are value-group forcing (the exact relation Φ′(ρ)^ℓ = c·π^u·ρ
forces v(Φ′(ρ)) = u/(ℓe₁) with the case analysis script-checked over (1/4)ℤ) + residue
forcing (β_ρ² = the residual constant) + the exhaustive divisor census ({(e,f) : ef ≤ deg,
ℓe₁ ∣ e, d·f₁ ∣ f} is a singleton at each row). PARI (factorpadic-200 + idealprimedec, the
pinned w12 GP_FUN convention) decorrelates the mixed-characteristic rows.

* **A1c — the failure-mode sweep** (the box's displayed worry: "the degree count alone
  permits one class to be empty while the other carries two Galois orbits"): 35 perturbed
  A1a-leaf members (all four slot axes, unit multipliers swept), PARI σ stable at
  {(4,1),(4,1)} on every member, AND the unanchored resultant census reads both labels
  NONEMPTY on every member (T_s ≥ 42 at labels, = 40 exactly at non-labels) — nonempty ×
  degree-split forces \|S_1\| = \|S_2\| = 4. **The failure mode is excluded on every
  measured row.** Tooth T-A-EMPTY: an UNDECIDED-leaf probe ((Φ′²−25x)² + 2·5⁶, residual
  (Z−1)²) shows the census SEES an empty label (T_2 = 40 flat) — the 4/4 verdicts are
  informative, not blind.
* **A-S3 — the higher-order test-polynomial separation** (the box's IDENTIFIED REPAIR
  ROUTE, instrumented for the first time): Ψ_{s,κ} = Φ′² − s·25x − (letter at κ). Exact
  integer resultants give the two-sided reads (44, 48) at labels and flat (40, 40) at
  non-labels, on the exact product AND on a perturbed member.
* **A-S5 — the count inversion at fractional heights**: the κ-slope T(7) − T(6) reads
  \|S_s\| = 4 exactly at both labels (exact product + perturbed member) — the polygon
  inversion mechanism of HE3-3, transported to the fractional side, WORKS as measured.
* **A-S4 — the anchor property**: Ψ_s's own x-Newton polygon is a SINGLE side at the key
  valuation (8/8 checks) — so every root of Ψ_s is in LEMMA HE3-0's entry situation; the
  remaining unproved content of "the slot lemma at Ψ's roots" is the slot READ at those
  points (HE6-0″'s analogue) plus the test-side evaluation display, not the root geometry.
* Teeth: T-A-DPRIME (the mutant law \|S_r\| = D′d — the ℓ-rescale forgotten — refuted by
  every measured σ), T-A-SIG (flipped-label mutant killed by the exact relation).

### 2.2 LEG B — the P-BINOM classification (123 checks + 2 teeth)

Fresh implementation of the GENTOW6 shadow ledger (dual routes R/E, bit-compared on every
member — 39 DUAL checks across all legs). Six ANCHOR frames reproduce the sealed
`gentow6_box1_mu3.py` record bit-exactly (X3 top slot {(0,1): 12288}, X4 {(0,1): 24576},
A53 {(0,1): 48}, P33 {(0,1): 972}, P34 {(0,1): 1944}, P35 {(0,1): 3240}) — the fresh
implementation is tied to the sealed instrument before any new frame is trusted.

* **Single-slot geography** (f₂ = 1, t* = 0 — X/A5/P3): three FRESH frames X5 (μ₂ = 5,
  p = 2), P36 (μ₂ = 6, p = 3), P38 (μ₂ = 8, p = 3). The top coordinate carries ONLY the
  certificate slot on all 9 frames; its digit's valuation = 2a_{t*} + v_p(binom(μ₂,2))
  on every frame; **floor attainment iff p ∤ binom(μ₂,2)** — 9/9 (X3 att / X4 no / X5 no /
  P33 no / P34 no / P35 att / P36 no / P38 att / A53 att).
* **Cascade geography** (e₂(f₂−t*) = 1 — B/BP, the BP3 genre of the GENTOW6_PROOF r1
  bracket): measured at μ₂ = 3, 4, 5 (B) and 3, 4, 5, 6 (BP). BP3 reproduces the r1
  record exactly ({(0,1): 27 dead in grade, (1,0): 135 with graded floor digit}).
  **LAW B-S2 (read off the table, then asserted 7/7):** the height-Θ_{j*} graded layer at
  the top coordinate is carried by the PAIR slot (digit ~ binom(μ₂,2)) and the TRIPLE slot
  (digit ~ binom(μ₂,3)) ONLY; attainment ⟺ p ∤ binom(μ₂,2) ∨ p ∤ binom(μ₂,3).
  **The naive "any in-band k-diagonal" candidate (this unit's own outline B-S2) is
  REFUTED at B4**: binom(4,4) = 1 is a unit, yet pin(ShC_3) = 8 = Θ + 1 — k ≥ 4 branches
  land strictly above the floor (B5 confirms at μ₂ = 5). Refutation kept; the repaired
  pair-or-triple law is the certified object.
* Teeth: T-B-COEF (μ₂-for-binom mutant killed at X4 by the exact valuation), T-B-ALLM
  (always-attains mutant killed at all 5 p ∣ binom frames).

### 2.3 LEG C — the f₁ ≥ 2 compound: FIRST machine contact (63 checks + 1 tooth)

ANNEX-REMARK 6.5′ is prose-only and machine-uncontacted at HEAD ("a NEW honest fence
since BOX-4's leg is μ₂ = 2"). Three f₁ = 2 geographies, μ₂ ∈ {2, 3} each:

| frame | key | (e₁,f₁), D′ | axis value |
|---|---|---|---|
| F23m2/m3 | x²−18 over ℤ₃, Φ2 = Φ′²−81x | (1,2), 2 | K = F₉, **η = √2 ≠ 1** (the gauge axis GENTOW4-r1 flagged as machine-blind) |
| F22m2/m3 | x²+2x+4 over ℤ₂, Φ2 = Φ′²−16x | (1,2), 2 | K = F₄ |
| F423m2/m3 | x⁴+2x²+4 over ℤ₂, Φ2 = Φ′²−16x³ | (2,2), 4 | **BOX-4's own sealed key**; m2 = the sealed row reproduced fresh (cert 256, pin 47 = Θ₀ ✓), m3 = 6.5′'s direct object |

RESULT: at these geographies (t* = 0, f₂ = 1) **the f₁ = 1 displays of 6.5 survive
verbatim**: ceiling (ShC_j = ∅ above j*) 6/6, certificate slot (2i_{t*}−D′, 1) with
v_p = 2a_{t*} + v_p(binom(μ₂,2)) 6/6, attainment iff p ∤ binom(μ₂,2) 6/6, floors 15/15,
perturbation rows 30/30. The certificate digit at the compound F423m3 (f₁ = 2 × μ₂ = 3,
the 6.5′ object itself): slot (2,1) value 768 = 2⁸·3, v = 8 = 2·4 + v₂(3), attaining
Θ₁ = 47 exactly. Tooth T-C-VAL killed. **Scope honesty:** first contact covers the
t* = 0, f₂ = 1 slice; f₂ ≥ 2 / t* > 0 compound geographies (where the corrected-lift ĉ
wrap enters with force) remain unmeasured — the 6.3′ full-exposure calculation stays the
open theorem-grade object.

### 2.4 LEG E — entry-driven certificates (238 checks + 2 teeth)

* **W-geography** (HYP.71(b)'s ask): W1 = (ℚ₂, Φ′ = x³−2, Φ2 = Φ′³ + 8xΦ′² + 1024) and
  W2 = (ℚ₃, x³−3, Φ′³ + 27xΦ′² + 2·3¹⁰): f₂ = 3, u₂ = 10, T = {0, 2}, i₂ = 1, i₀ = 0 —
  ALL key pairs in-grid (i_t + i_{t′} ≤ 2 < 3 = D′), residuals Z³+Z²+1 / Z³+Z²+2
  irreducible. The pure power Φ2² has EMPTY shadow (measured — the clean geography), so
  any Θ-attainment must be entry-driven, which is exactly the witness ask.
  - **LAW E-W (hard-checked 54/54 crossing reads, both primes):** for an in-budget
    single-entry member f = Φ2² + c·p^α x^a Φ′^b · Φ2^{j′}: a coordinate-0 entry never
    crosses (run-1 finding: the j′ = 0 slots never enter the division's quotient — the
    naive "a + i_t ≥ D′" predicate is only sufficient at j′ ≥ 1); a crossing entry
    (a + i₂ ≥ D′, j′ = 1) produces discrepancy at EVERY coordinate with
    **mindiff_j = Θ_j + (entry height − (μ₂−j′)·E₂)** — the branch transports the entry's
    excess over the side verbatim.
  - **The infeasibility record (E-INFEAS, both genres):** the in-locus floor is STRICT
    (entry height ≥ side + 1), so by LAW E-W the sweep's minimal gap is 1 — **no in-locus
    member attains Θ**; the sweep (54 crossing + 135 non-crossing in-budget rows)
    confirms: min gap = 1 at both W1 and W2. Tooth T-E-BOUND: the ON-side entry (α = 6,
    a = 2, b = 1 at W1 — off the leaf locus by exactly the boundary) attains Θ at BOTH
    coordinates exactly — the law is TIGHT; the infeasibility is the locus fence, not
    slack in Θ.
  - Two-entry linearity (E-LIN) and unit-c independence (E-S4) both GREEN: at this
    geography the discrepancy is a linear function of the total entry digit ledger — the
    above-floor membership classification is COMPLETE here (diverge iff a crossing entry
    monomial exists; height by LAW E-W).
* **R-geography** (HYP.69 / E-S3): fresh regime-3 genres R5 = (ℚ₅, x³−5, Φ′² + 25xΦ′ +
  625x²) and R2 = (ℚ₂, same shape): s* = 1, I = {3}, χ = 2c̄₀c̄₁. THEOREM GENTOW-6.7's
  dichotomy verified fresh: R5 (χ = 2 ≠ 0): pin(ShC_1) = 18 = Θ₁ (b1 attainment); R2
  (χ = 0 in F₂): pin = 21 > 18 (b2). **First p = 5 regime-3 row ever; tooth T-E-CHI: the
  mutant χ without the (2−δ_{tt′}) factor predicts attainment at R2 — killed: the
  multiplicity factor is load-bearing at p = 2.**

### 2.5 LEG D — the count layer (11 checks + 1 tooth)

First DIRECT window-enumeration checks of THEOREM GENTOW-6.6(c)'s ledger identity
#(m(B_S×B_g) mod π^N) = q^{−c}·#(B_S mod π^N)·#(B_g mod π^N) and the q^c fiber clause,
at three shapes over ℤ₃ (deg1×deg1; deg2×deg1 with the evaluation-constant (H2); the
two-class residual-separation shape — the 6.6(d)/GENTOW4 partial-side mechanism in
miniature: distinct residue classes force the constant). All GREEN; tooth T-D-CJ: both
c ± 1 mutants fail the identity. Plus the c_J closed-form integer identity
(μ₂*m′D₂E₂/(e₁e₂) = μ₂*m′f₁f₂²e₂u₂) on a 648-tuple grid, 0 bad.

### 2.6 LEG F — HYP.142's decorrelated leg (`om10_f9_count.py`)

The corrected-node (2,K)-law (LEMMA GENHN-COUNT-RERUN) at K = F₉ — the FIRST
odd-characteristic f₁ = 2 count contact (the existing certificate
`genhn_f2_count_check.py` is F₄-only). Fresh field, fresh implementation, predictor's
residual censuses ENUMERATED over K×K (non-textual leg). Results:
* smoke (genre (1,2,2), B = D′h = 2, cap 6; 531,441 members): measured = predicted on all
  13 keys.
* full (cap 7; 9⁸ = 43,046,721 members, exhaustive): measured = predicted on all 23 keys;
  **each ALPHA(κ=3, s) slice splits RAM 52,488 / UND 6,561 across all 8 letters — the
  exact clause whose sealed S-node version was refuted at F₄ (0/1024 vs 768/256),
  reproduced at the fresh field**; tooth T-F9-SEAL (the sealed-error pattern, alpha
  slices lumped to UND) killed.
* mixed (genre (2,2,2), B = 4, cap 9): measured = predicted (RAM/UND band); tooth
  T-F9-SNODE (the S = e₁h window is a different object) killed. Tooth T-F9-Q (the law at
  |K| → Q) killed at the smoke run.

### 2.7 D-S1 — the HYP.142 transcription-confirmation (performed in-session)

GENHN L2624's residual ("a final transcription-confirmation rides the next queue") is
DISCHARGED: the dated-proof-completion block in `GENHN_PROOF_2026-08-08.md` (header
`### Dated proof completion (2026-08-12 — GENHN-COUNT-RERUN…` through the end of proof,
9,431 chars — certificate interpretation + LEMMA GENHN-COUNT-RERUN (CR-1)–(CR-6) + proof
+ consumer sweep) matches the composition source
`runs/cert4/P2_genhn_count_output.log` **verbatim** (whitespace-normalized character
identity; check script inline in the session, both files at HEAD).

---

## §3. STATUSES AND RESIDUES (per row)

### 3.1 HYP.143 `HE3-BOX-6` ramified branch (ii) — GROUP A

| step | status |
|---|---|
| A-S1 reachability (μ ≥ 4 ∧ D′ ≥ 2 ⟹ n ≥ 8) | **PROVED** (OM-7's `H73w.lean`, cross-referenced; no new work) |
| A-S2 the class-size law \|S_r\| = D′ℓd at branch (ii) | **CERTIFIED-OPEN** — first machine contact ever: 41 measured rows over 4 cells × 2 characteristics; law holds on every row; the box's failure mode excluded on every row |
| A-S3 the test-polynomial separation | **CERTIFIED-OPEN** — the repair route's instrument works as displayed (label reads exact, both anchors) |
| A-S4 the anchor property of Ψ_s | **CERTIFIED-OPEN** — single-side polygons measured 8/8; the general proof = HE3-0 at Ψ's roots, named |
| A-S5 the fractional count inversion | **CERTIFIED-OPEN** — the κ-slope reads \|S_s\| exactly at the measured frames; the proof (the slot lemma at Ψ's roots + the test-side evaluation display) is the box's named "genuine further step", NOT executed here |
| A-S6 the μ ≥ 4 stage-CS O_L re-carrier | named, untouched (HE3 §S1 [r1,F7]) |

**Row verdict: MATH retained (CARRY as HYP.81's supplier), materially narrowed.** What
changed: branch (ii) had NO machine contact ("machine-unreachable at μ = 3, first live at
μ = 4" — and no μ = 4 row existed); it now has 41 rows across every cell of its geography,
the failure mode it was boxed FOR is excluded on every row, and the repair route's two
instruments (separation + inversion) are certified working. What did NOT change: no proof
of \|S_r\| = D′ℓd at branch (ii) exists; HE3.B at μ ≥ 4 remains conditional exactly as
before. Residual, exactly: prove the slot lemma at Ψ's roots (HE6-0″'s analogue at
degree-D′ℓ test polynomials) + the test-side evaluation display, then run HE3-4's direct
argument at the fractional side — the certified instruments say every displayed step of
that route computes what it should.

### 3.2 HYP.79 `P-BINOM` — GROUP B

| step | status |
|---|---|
| B-S1 single-slot geography law | **CERTIFIED (9/9 frames) + corollary-shape derivation** — at f₂ = 1, t* = 0 the top coordinate carries only the certificate slot (measured on all 9; by 6.5(a)'s equality census, at e₂(f₂−t*) = e₂ ≥ 2 no division step stays in-band, so only the zero-step s*-branches reach j*, and T = {0} makes the slot unique); its graded digit is binom(μ₂,2)·ĉ²·(unit), so p ∣ binom(μ₂,2) kills the floor. Consumes 6.5(a)/(b) at the BOXES note's 0/2 grade — hence NOT claimed PROVED outright |
| B-S2 cascade law (pair-or-triple) | **CERTIFIED-OPEN (7/7, two primes)** — attainment ⟺ p ∤ binom(μ₂,2) ∨ p ∤ binom(μ₂,3); the any-k candidate REFUTED at B4/B5 (kept verbatim in §2.2) |
| B-S3 lower coordinates (j < j*) | **OPEN** (unchanged: the S3-display geography; HYP.67's rider) |

**Row verdict: MATH retained, narrowed by two certified laws.** The row's ask ("floor
attainment and lower-coordinate cases are classified" at p ∣ binom(m,2)) is now: floor
attainment CLASSIFIED at the two measured geography families (single-slot: never;
cascade: by the triple binomial), certified not proved; lower coordinates open.

### 3.3 HYP.67 / HYP.80 / HYP.70 — GROUP C (the f₁ ≥ 2 compound)

**HYP.67 verdict: MATH retained, narrowed.** The f₁ = 1 half is CLOSED at source (6.5).
The f₁ ≥ 2 face — the row's open face — now has its first machine geography: the naive
6.5 displays survive verbatim at t* = 0, f₂ = 1 (6 frames, both D′ ∈ {2,4}, η ≠ 1
included). Residual: theorem-grade proof at f₁ ≥ 2 (the 6.3′ full-exposure calculation)
+ the unmeasured t* > 0 / f₂ ≥ 2 compound geographies + the j < j* classification.

**HYP.80 verdict: MATH retained; the "machine-uncontacted" clause of its census row is
DISCHARGED** (census S-3 graded it "fenced (prose, no machine)"; F423m3/F23m3/F22m3 are
the machine rows it asked for — "extend the BOX-4 sealed battery one μ₂ up" is exactly
F423m3). The classification/measure-law ask stays open at theorem grade.

**HYP.70 verdict: MATH retained; REDUCES-TO(6.3′ full exposure).** The battery half was
already discharged (four sealed rows); this unit adds fresh-implementation reproduction
of a sealed row (F423m2: cert 256, pin 47 bit-equal) plus two NEW f₁ = 2 x-ful rows at a
different key shape (D′ = 2, e₁ = 1: F22m2/F23m2) — widening evidence, not the widening
theorem. The theorem-grade all-f₁ ≥ 2 statement remains exactly the 6.3′ calculation.

### 3.4 HYP.71 / HYP.69 / HYP.78 — GROUP E

**HYP.71 verdict: MATH retained; conjunct (b) REFUTED(+repair) at the measured
geography.** The asked-for battery witnesses (entry-driven Θ-attainment where the key
self-power stays below D′ in x) are INFEASIBLE at the all-in-grid geography: LAW E-W
forces mindiff = Θ + (entry excess), and the locus floor makes the excess ≥ 1 — Θ is
never attained by an in-locus member (T-E-BOUND: the boundary entry attains exactly, so
the obstruction is the locus fence, not slack). REPAIR (dated amendment, source
statements untouched): GENTOW-3(iv)'s sharpness should be read as scoped to
certificate-carrying geographies (CERT-TOP regimes + LAW B-S2's positive cascade cases);
at all-in-grid genres the Θ floor is strictly slack by exactly the locus margin.
Conjunct (a) (genre-general nonvanishing / deciding ω_j's vanishing outside CERT-TOP)
stays open with HYP.112's fence. Scope honesty: the infeasibility is certified at two
genre shapes (W1/W2), μ₂ = 2, single- and two-entry members, in-budget grid exhaustive
per slot; LAW E-W's proof at the K2-division ledger is a named open (one careful page).

**HYP.69 verdict: MATH retained, narrowed to exactly HYP.78's surface.** 6.7 (key-power
half) + 6.7E (exactly-Θ member half) closed at source; this unit re-verifies the
χ-criterion at TWO fresh regime-3 genres including the first p = 5 row and the first
χ-mutant tooth. Remaining surface: the above-floor member half (= HYP.78) and the
μ₂ ≥ 3 lower coordinates (= GROUP B's open slice). Nothing else.

**HYP.78 verdict: MATH retained (the census hard-unit), narrowed and LOCALIZED.** At
single-crossing all-in-grid geographies the above-floor membership classification is
COMPLETE (LAW E-W is the classification: diverge iff a crossing entry monomial exists,
at height Θ + excess, linearly in the ledger — unit-c independence and two-entry
linearity measured). The genre-level obstruction (S8.1's measured diverge-vs-not pair)
is reproduced FRESH inside W1 and W2. What remains hard is exactly the multi-crossing /
free-digit-mixing geography — the classification obstruction now has a named location.

### 3.5 HYP.68 / HYP.144-BOX-2 / HYP.142 — GROUP D

**HYP.68 verdict: MATH retained; REDUCES-TO(GENTOW-6.6).** The row's STATEMENT ("only
ledger/count half remains") predates the BOXES append: THEOREM GENTOW-6.6(a)–(d)
delivers exactly the asked pricing ((f_S,g) ↦ f_S·g priced by the Sylvester Jacobian,
window ledger with q^{−c_J}), at the BOXES note's 0/2 grade; the full-side clause is
6.6's own record (one-class ledger = GENTOW-1(b) + R2a verbatim, GENTOW1 ACCEPTED 2/2).
This unit adds the first direct enumeration checks of (c) + the two-class mechanism +
the c ± 1 teeth. Residual: 6.6's hostile arc (process, not math) and the aggregation
seam (GENH5-DENS geography — outside this row per 6.6's fence (i)).

**HYP.144-BOX-2 verdict: MATH retained; REDUCES-TO(GENTOW-6.6(d) + [r2] iteration).**
The partial-side COUNT layer's shape (products of class boxes × sibling data) is exactly
6.6(d)'s two-class instantiation iterated ([r2]'s per-step re-establishment + the
[rider] N₀-domination line); the named residual is the (H1)/(H2)/(H3) instantiation at
GENTOW4's actual class boxes and sibling data (per-genre, with c_J constants summing per
class pair) — certified in miniature at D2d, not executed at a real GENTOW4 frame.

**HYP.142 verdict: NODE — RECONCILED; NO escalation to MATH.** The adjudication the row
asks for: **the two records AGREE at HEAD, and the GENHN record governs BECAUSE the
HE6R1 record's own demand is satisfied by it.** Timeline, verbatim-pinned: (i) HE6R1's
obstruction record (R7 finding 2, A4 adjudication, 2026-08-12) rules "no honest repair
can be derived from the supplied note alone" and its tail SCHEDULES the repair: "the
count re-run on the corrected node floor … is hereby a scheduled proof unit (numerical
certification first, then the proof; it simultaneously settles GENHN-ERR-1 and restores
the completion statement's … clause with no wording change)". (ii) GENHN's dated proof
completion (2026-08-12, LEMMA GENHN-COUNT-RERUN, machine certificate
`genhn_f2_count_check.py`, orchestrator verification) IS that scheduled unit's landing;
its consumer sweep item 5 restores HE6R1's completion clause with no wording change —
exactly as scheduled. So the "un-retracted side" was never a counter-record: it is the
demand; the settled side is its fulfillment. The escalation clause does not fire: the
new-count-lemma the HE6R1 reading owes EXISTS. This unit adds: (iii) the residual GENHN
L2624 named ("a final transcription-confirmation rides the next queue") — PERFORMED,
verbatim match against `runs/cert4/P2_genhn_count_output.log` (§2.7); (iv) the
decorrelated F₉ leg (§2.6): first odd-characteristic contact, 43M-member exhaustive
window, the refuted clause's geography reproduced, three teeth. Remaining residual on
the LEMMA (not on this row's ask): its own grade (composed by the P2 certification run,
orchestrator-verified, machine-certified at F₄ + now F₉; no 2-clean hostile arc on
record) — a process item riding GENHN's record, named here for honesty.

### 3.6 Lean-landing adjudication (the "where cheap" clause)

**No new Lean landings this unit — recorded reasoning:** the family's Lean surface at
HEAD is (i) `tower_first_live` (C.93) PROVED by OM-7; (ii) the H73w fences
(`branch_ii_needs_mu_four`, `mu4_lives_at_eight`) covering HYP.143's reachability,
PROVED by OM-7; (iii) the signed axiom-stubs `cap_free`/`attainment_certificate`/
`attainment_certificate'` (C.95) and `box1_attainment_if` (C.96) in
`leanspec/Leanspec/ChapC.lean` [signed A-C.1] — these are EXACTLY HYP.67/70/79's Lean
shapes, and proving their bodies requires the full `TowerDatum`/`shadowDev`/`dv2Hgt`
ledger (§9's transcription surface, defect D1 territory) — the C fleet's chapter work,
not a unit's cheap landing. A carrier-free arithmetic shell (e.g. the divisor-census
one-liners of LEG A) would be vacuous decoration and is declined per the repo's
vacuity discipline. LAW B-S2 and LAW E-W have no signed home; creating one is blueprint
amendment work for chapter C's §9, flagged for the orchestrator as an OPTION (a dated
A-C amendment adding two law nodes at the certified statements), not performed here.

---

## §4. LEDGER-EFFECT NOTES (ARC NOTEs appended at the rows; dispositions unchanged)

* **HYP.67, HYP.70, HYP.80**: MATH retained; ARC NOTEs record the first f₁ ≥ 2 machine
  geographies (LEG C) + the reduction map (6.3′ full exposure is the single theorem-grade
  residual behind all three).
* **HYP.68, HYP.144**: MATH retained; ARC NOTEs record the REDUCES-TO(GENTOW-6.6)
  adjudication + the first direct enumeration checks (LEG D). HYP.144's note scoped to
  BOX-2 (BOX-1/BOX-4 untouched — CHAP-I's split governs).
* **HYP.69, HYP.71, HYP.78**: MATH retained; ARC NOTEs record the fresh χ-verification,
  LAW E-W + the witness-infeasibility refutation(+repair), and the localization of the
  S8.1 obstruction.
* **HYP.79**: MATH retained; ARC NOTE records the two certified laws + the B4 refutation.
* **HYP.142**: NODE; ARC NOTE records the reconciliation verdict (GENHN governs, demand
  satisfied, no escalation), the performed transcription-confirmation, and the F₉ leg.
* **HYP.143**: MATH retained; ARC NOTE records the first machine contact (41 rows, all
  cells), the failure-mode exclusion, and the certified repair-route instruments.
* **Fences verified untouched**: nothing consumed from `WELDZERO_2026-08-14.md`; no
  sealed display of GENTOW3/4/6, GENHN, HE3, HE6R1 edited; HYP.112's fence restated at
  every GROUP-E object (no candidate here implies ω_j ≠ 0, touched j, or attainment —
  indeed the unit's E-verdict is the opposite: non-attainment).

## Commits
* `74edb57c` — skeleton: charge, verbatim rows, the five-group outline, step decomposition.
* `cc3cdada` — LEG A GREEN (branch (ii) first contact).
* `b9ed277a` — battery GREEN 561/0 (legs B/C/E/D + teeth).
* `07b86ddf` — LEG F script (F₉ smoke + mixed GREEN).
* `a09e7080` — F₉ full run GREEN (43M members, T-F9-SEAL killed).
* `ad4d8f5d` — battery final GREEN 569/0 (AS4 anchor checks).
* `ba4262f9` — note §§2–4 + verdict table final.
* `16c7a937` — ledger ARC NOTEs at all ten rows (dispositions unchanged).
* `da0b01b8` — roster row RUNNING → DONE (the campaign's last unit).
* (this commit) — commit-ledger touch-up.
