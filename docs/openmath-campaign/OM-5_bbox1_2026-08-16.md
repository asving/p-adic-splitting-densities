# OM-5 — `B-BOX-1` (the residue leg of the class-size count) — unit note, 2026-08-16

**Charge.** Chapter B's sole open box, `B-BOX-1`: the residue-degree LOWER bound at
`d ≥ 2` leaves — B.61's explicit hypothesis

```
(B-BOX-1)   ∀ g' ∈ monicFactors g,  m * d ∣ inertiaDegOf g'
```

for `g` monic `(u,ℓ)`-pure at an order-1 key `φ` of degree `m`, `Nat.Coprime u ℓ`, `0 < u`,
with residual polynomial a unit times an irreducible `ψ` of degree `d = sideDeg` — whose scope
amendment A-F.7 widened to include the `ℓ = 1, m ≥ 2, d ≥ 2` integral-slope region (CHAP-I
dated addendum 2026-08-15). Consumers: B.61 (and through it B.71, B.79), and one level up
C.60's `C-BOX-1` (DECISION C-D1 Tier 1's residue leg), which inherits this box's repair route.
Mandate: decide PROVED / CERTIFIED-OPEN / REFUTED(+repair) / REDUCES-TO / CITE-CANDIDATE, with
per-step certification (exact numerics, both primes, both characteristics, constructed-
counterexample attacks) before any proof or cite draft.

**Statuses used:** PROVED / CERTIFIED-OPEN / REFUTED(+repair) / REDUCES-TO(<node>) /
CITE-CANDIDATE (published Montes-literature source states it → gate-(b) draft at the B.42
standard instead of a proof).

**Verdict summary (details in §3; certifications in §2):**

| item | verdict |
|---|---|
| `B-BOX-1` (B.61's `hBOX`, full scope: `d ≥ 2`, any `(u,ℓ)` coprime, any key degree `m`) | **CITE-CANDIDATE** — the content is the leaf dichotomy's `(e,f)` clause, stated in the published Montes literature at COVERS-ALL-O scope ([FGMN] Thm 6.6 + [GN15] Thm 2.3 & eq. (2.1); GMN Thm 1.19 + Cor 1.20 as the ℚ_p-scope display), read through two classical-valuation-theory glue legs that are NOT Montes-specific (§3.2); gate-(b) draft at §5, composite-cite honesty disclosed |
| in-repo proof from LANDED pieces (`typeOf_inert_faithful`, `TypeOfFaithful*`, Quarry cluster) | **STRUCTURALLY UNAVAILABLE — new finding (§3.1): at every `d ≥ 2` leaf the order `AdjoinRoot g'` has residue field `resField φ` of degree `m < m·d`, hence is NEVER a DVR — the box's region lies entirely OFF the monogenic-maximal locus, the only locus the landed HYP.01 machinery reaches** |
| repair route 1 (`EFF.HE3.68` LEMMA HE3-4U, unramified base change) | corpus-internal, model-composed, machine-audited arithmetic — **NOT a published citable argument**; its conclusion is subsumed by the published leaf dichotomy; its *mechanism* is certified numerically on an instance (§2.4) |
| repair route 2 (GMN Cor 1.20's residue tower / `EFF.HE6.29` root label) | = the box's own content; the published form IS the cite of the row above |
| the axiom draft's residual-irreducibility hypothesis | **necessary** — constructed counterexample (§2.3): multiplicity-2 residual `(y−1)²` defeats `hBOX`; the draft carries `Irreducible resPoly` |
| relation to C-D1's tiers | `B-BOX-1` = Tier 1's residue leg at level 1; the cited [GN15] Thm 2.3 is stated for ANY MacLane chain length, so ONE published source covers `C-BOX-1`'s level-2 residue leg too — but the level-2 axiom instance needs its own signing in C's vocabulary (§4) |
| LANDED this unit | `BBox1CiteStatement` (the signable `Prop`, no axiom, no `sorry`, Lean-core footprint) + `typeOf_leaf_of_cite` (machine-checked: the cite discharges B.61's `hBOX` — signature alone completes the `d ≥ 2` leaf rows), `leanfinal/Uniformity/ChapB/BBox1CiteStatement.lean` |

---

## §1. HIGH-LEVEL OUTLINE (from the sources, in the mandated order)

### 1.1 What the box says, and what it does not

`inertiaDegOf g'` (`leanfinal/Uniformity/Density/TypeOf.lean:193`) is the gcd (as
`sSup (normDivisors g')`) of the set of positive valuations `addVal O (Algebra.norm O x)`,
`x ≠ 0` ranging over the ORDER `AdjoinRoot g' = O[x]/(g')` — not over the maximal order
(CHAP-B H-7: "`inertiaDegOf` is a gcd over the **order** … this chapter never assumes the two
agree"). The box asks `m·d` to divide that gcd for every monic irreducible factor `g'` of the
leaf `g`. Since the additive set of norm-valuations generates a subgroup `f·ℤ` of `ℤ`, the box
is equivalently: **`m·d` divides `addVal O (norm x)` for every nonzero `x` in the order.**

The chapter proves, unconditionally (CHAP-B §7, all landed-vocabulary contracts):

* **B.52** — `m ∣ inertiaDegOf g` at every `d` (the `m` half: `AdjoinRoot g` is local with
  residue field `resField φ` of degree `m`, and the length mechanism B.51 gives
  `m ∣ v(N x)` for all `x`);
* **B.54** — `inertiaDegOf g ∣ m·d` (the upper bound, from the two witnesses
  `v(N(φ(α))) = m·u·d` and `v(N(π)) = ℓ·m·d` plus `Nat.Coprime u ℓ`);
* **B.55** — the bracket, collapsing at `d = 1` (B.58's unconditional leaf theorem);
* **B.60** — the `ℓ = 1, m = 1` slice, via `scaleRoots` extraction + landed `InertLeaf`.

The box is the residual gap `m·d ∣ …` at `d ≥ 2`, where the bracket `m ∣ f' ∣ m·d` does not
pin `f'`. H-7's own diagnosis of why B.52's mechanism stops at `m`: "the lower bound needs the
residual root to be realised in the residue field **of the leaf** [= of the factor field's
valuation ring] … and that is a statement about the valuation ring of `K(α)`, not about
`AdjoinRoot g`."

### 1.2 The classical decomposition of the box's content

Fix a factor `g'` (under B.61's hypotheses the published theorem forces `monicFactors g = {g}`,
but the decomposition below works per-factor). Write `L := K[x]/(g')`, `K = Frac O`, `n' = deg g'`.

* **(A) The leaf dichotomy at multiplicity 1** *(published Montes literature, COVERS-ALL-O)*:
  under B.61's hypotheses `g` itself is irreducible, and the extension `L/K` has
  `e(L/K) = ℓ`, `f(L/K) = m·d`. Sources with verbatim displays already on file
  (`docs/CITE_SCOPE_RESOLUTION_2026-08-13.md` NS-2, verdict **COVERS-ALL-O**): [GN15] Thm 2.3
  ("if `ord_ψ(R_λ(g)) = 1`, then `g_{λ,ψ}` is irreducible in `O_v[x]`") + eq. (2.1)
  (`e(F) = e₀⋯e_r`, `f(F) = f₀⋯f_r` — at order 1: `e = ℓ`, `f = m·d`); [FGMN] Thm 6.6 (same
  display, "proper key polynomial" hypothesis — covers the whole `IsKey` range including
  `φ̄ = x̄`, per the A-3 disposition in CHAP-I's B.42 faithfulness entry, which this draft
  inherits verbatim); GMN Thm 1.19 + Cor 1.20 ("`f(L/K)` is divisible by `m·deg ψ_i`; … if
  `a_i = 1` then `G_i` is irreducible, `f(L/K) = m·deg ψ_i`, `e(L/K) = e`") as the ℚ_p-scope
  secondary display (GMN's standing setting is a finite extension of ℚ_p — NOT equal
  characteristic; [FGMN]/[GN15] are the load-bearing legs, exactly as in the B.42 entry).
* **(B) The maximal-order carrier** *(classical valuation theory, not Montes-specific)*: `O`
  complete ⟹ the integral closure `O_L` of `O` in `L` is a DVR, finite free of rank `n'`
  over `O`, with residue field `k_L` of degree `f(L/K)` — Serre, *Corps Locaux*, Ch. II
  (complete case), also Neukirch ANT Ch. II. *(Exact proposition numbers flagged for lookup
  at signature time — see §5 caveats; the repo rule "never trust a number from memory".)*
* **(C) The norm-valuation identity** *(classical)*: for `x ∈ L^×`,
  `v(N_{L/K}(x)) = f(L/K) · w(x)`, `w` the normalized valuation of `L`. Equivalent published
  form: the unique extension of `v` to `L` is `(1/n')·v ∘ N_{L/K}` (Neukirch ANT II, the
  henselian uniqueness theorem), combined with the fundamental identity `e·f = n'` for
  complete discrete valuations. **In-repo corroboration of exactly this mechanism:** landed
  `Uniformity.Density.exists_ramification_data` + `inertiaDegOf_eq_inertiaDeg'`
  (`TypeOfFaithful.lean`) prove `(C)` *shape-for-shape* for any `A` with
  `[IsDiscreteValuationRing A]` — the normal form `x = unit·ϖ^j` gives `v(N x) = j·v(N ϖ)`
  and the fundamental identity (Quarry backport
  `Ideal.ramificationIdx_mul_inertiaDeg_eq_finrank_of_isLocalRing`) pins `v(N ϖ) = f`. What is
  missing in Lean is only the CARRIER `O_L` of (B), never the identity (C).
* **(D) Norm compatibility order → field** *(mathlib-tier)*: for `x` in the order,
  `Algebra.norm O x` maps to `N_{L/K}(x)` under `O → K` (norm commutes with the fraction-field
  base change of the free module `AdjoinRoot g'`; `Algebra.norm_localization`-family at the pin).

(A)+(B)+(C)+(D) ⟹ for `x ≠ 0` in the order: `addVal O (norm x) = v(N_{L/K} x) = (m·d)·w(x)`,
so `m·d` divides every norm-valuation, i.e. `m·d ∣ inertiaDegOf g'`. **Direction check (the
order-vs-field reading, D-3):** the order's norm-value set is a SUBSET of the field's, and a
gcd over a subset is a multiple of the gcd over the superset — so the box's divisibility
direction is exactly the direction that survives the order reading. (The other direction —
`inertiaDegOf g' ∣ m·d` — is landed-tier B.54 and needs no field.)

### 1.3 The two named repair routes, from the sources

* **Route 1** (`EFF.HE3.68`, R8-2, LEMMA HE3-4U, verbatim): "Let λ be an integer slope and
  let r ∈ K[Z] be a separable irreducible factor of R_λ of degree d. Then |S_r| = D′d." Proof
  mechanism (audited arithmetic at `EFF.HE3.33`, "Every step reproduces"): unramified base
  change of degree `c := f₁m` with `d ∣ m`; `Φ′` splits into `f₁` conjugate keys of degree
  `e₁`; the image of `r` splits into `d` linear factors; per component `e₁·d` roots; summing
  over `f₁` components gives `f₁e₁d = D′d`. **Status as a cite: NOT PUBLISHED.** HE3-4U lives
  in `HE3_PROOF_2026-08-08.md` (a corpus note, model-composed, grade TERMINAL 1/2 per CHAP-B
  H-4) — gate (b) requires a published source, and the published leaf dichotomy (A) already
  states the conclusion, so route 1 contributes *certification* value (§2.4 runs its mechanism
  numerically), not a citation.
* **Route 2** (GMN Cor 1.20's residue tower / `EFF.HE6.29` Cor HE6-2″ `[PIN HE6-ROOT-LABEL]`):
  the residue class of `φ(θ)^ℓ/π^u` in `k_L` is a root of `ψ`, so `k_L ⊇ resField φ[Y]/(ψ)`
  of degree `m·d` — "the corpus's proof of exactly `hBOX`, through the resultant account that
  H-2 puts out of scope" (B.61 SOURCE field). This IS the box's content; its published form is
  (A), and its in-repo form is B.62's sketch steps 3–5. Route 2 therefore collapses into the
  cite of §1.2(A) plus the glue (B)–(D).

### 1.4 Where the box sits in DECISION C-D1's three tiers

C-D1 Tier 1 (the closure-free sandwich at level 2) has two legs: the ramified divisibility
leg (`e₁ℓ ∣ e'`, closure-free at every parameter) and the residue leg (`f₁d_r ∣ f'`),
"closure-free where B.53's mechanism reaches (`f₁d_r = 1` …) and … otherwise conditional on
`C-BOX-1` … B-BOX-1's level-2 analogue". Tier 2 (proper-block count in read form) and Tier 3
(the mixed-side multiplicity tie, already `[cite:FGMN]`-tagged) do not touch the box.
**`B-BOX-1` is exactly Tier 1's residue leg one level down.** Since [GN15] Thm 2.3 is stated
for an arbitrary inductive valuation `µ` (any chain length `r` — NS-2: "it is simultaneously
the order-1 and the order-`r` statement") and eq. (2.1) gives `(e,f)` at every order, the SAME
published statement covers `C-BOX-1`'s residue leg; only the Lean vocabulary of the level-2
axiom instance differs (`CBox1Side`'s `KeyFrame`/`LevelDatum` vs B.61's polygon binders).

---

## §2. CERTIFICATIONS (exact numerics; both primes, both characteristics)

Battery: `verification/om5_bbox1_cert.gp` (PARI/GP 2.17.4; the only exact engine available on
this node this session — the repo's sympy/cypari2 venv is not loadable here, and GP subsumes
both uses: exact integer/`F_p[t]` resultants for the norm-valuations, `factorpadic`/`nfinit`/
`idealprimedec` as the trusted oracle). **RESULT: 128 checks, 0 failures (`ALL GREEN`),
reproducible with `gp -q verification/om5_bbox1_cert.gp` (seeded, `setrand(42)`).**

### 2.1 CERT-A — the leaf `(e,f)` at `d ≥ 2`: instances of (A), mixed characteristic

Eight constructed leaves, each verified: irreducible over ℚ and over ℚ_p (factorpadic), a
single prime above `p` with `e = ℓ` and `f = m·d` (idealprimedec):

| row | p | g | (ℓ,m,d) | measured (e,f) | order gcd | v_p(index) |
|---|---|---|---|---|---|---|
| A1 (widened) | 2 | `φ₄² + 2φ₄ + 4x`, `φ₄ = x²+x+1` | (1,2,2) | (1,4) | 4 | 2 |
| A2 (widened) | 3 | `(x²+1)² − 9(1+x)` | (1,2,2) | (1,4) | 4 | 2 |
| A3 | 3 | `(x²+1)⁴ − 9(1+x)` | (2,2,2) | (2,4) | 4 | 4 |
| A4 (wild p∣ℓ) | 2 | `x⁴ + 2x² + 4` | (2,1,2) | (2,2) | 2 | 2 |
| A5 | 2 | `x² + 2x + 4` | (1,1,2) | (1,2) | 2 | 1 |
| A6 (widened, d=3) | 2 | `φ₄³ + 8x` | (1,2,3) | (1,6) | 6 | 6 |
| A7 (wild p∣ℓ) | 3 | `x⁶ + 3x³ + 18` | (3,1,2) | (3,2) | 2 | 3 |
| A8 (p∣d) | 3 | `x³ − 9x + 27` | (1,1,3) | (1,3) | 3 | 3 |

Every row also checks the two structured witnesses exactly: `v(N(φ(α))) = m·u·d` and
`v(N(π)) = ℓ·m·d` (B.53/B.54's displays; their gcd is already `m·d` since `gcd(u,ℓ) = 1`,
which makes the gcd measurement deterministic, with the 80 random samples only able to
refine downward).

### 2.2 CERT-B — `inertiaDegOf` semantics: gcd of order-level norm-valuations = `m·d`

The gcd is measured in the EXACT Lean semantics: `inertiaDegOf` = gcd of
`{addVal O (Algebra.norm O x)}` over the ORDER `O[x]/(g)`, and for `x = h(α)` the norm is
`±Res(g,h)`. Mixed characteristic: column "order gcd" above. Equal characteristic
(`O = F_p[[t]]`, `t`-valuations of `F_p[t]`-resultants):

| row | p | g | (ℓ,m,d) | order gcd | irreducibility certificate |
|---|---|---|---|---|---|
| EC1 | 2 | `x² + tx + t²` | (1,1,2) | 2 | unit-root residual `y²+y+1` irreducible /F₂ (elementary Hensel) |
| EC2 | 3 | `x² + tx + 2t²` | (1,1,2) | 2 | same, `y²+y+2` /F₃ |
| EC3 (widened) | 2 | `φ₄² + tφ₄ + t²x` | (1,2,2) | 4 | CERT-D (base-change split) |
| EC4 (wild p∣ℓ) | 2 | `x⁴ + tx² + t²` | (2,1,2) | 2 | hand case-analysis: a deg-2 factor forces either a root of EC1's irreducible `z²+tz+t²` or `√t ∈ F₂((t))` (odd valuation) — both impossible |
| EC5 (wild p∣ℓ) | 3 | `x⁶ + tx³ + 2t²` | (3,1,2) | 2 | polygon + residual (theorem-side prediction; gcd is the measured leg) |
| EC6 (p∣d) | 3 | `x³ + 2t²x + t³` | (1,1,3) | 3 | unit-root residual `y³−y+1` irreducible /F₃ |

The order/field gap on display: at EC1 the ORDER's residue field is `F₂` (degree 1), yet the
measured gcd is `2` — the box's content is real and invisible to the order's residue field.

### 2.3 CERT-C — constructed-counterexample attacks (defeat equations)

* **C(i) — multiplicity `≥ 2` defeats `hBOX`** (⟹ the axiom draft MUST carry
  `Irreducible resPoly`): `g = (x²−3)²` over ℚ₃ is `(1,2)`-pure at `X`, `sideDeg = 2`,
  `resPoly = (y−1)²` (measured: `factormod` exponent 2). Its factor `x²−3` has measured
  `inertiaDegOf = 1`, and `m·d = 2 ∤ 1`. Same at `p = 5` and in equal characteristic
  (`(x²−t)²` over F₃). Note Cor 1.20's *divisibility* clause (`m·deg ψ_i ∣ f`) survives —
  `1 ∣ 1`; what dies at multiplicity ≥ 2 is `d = sideDeg`. This is HE6-BOX-1's territory and
  the two boxes stay unconflated (B.61's TEETH warning).
* **C(ii) — the order is a STRICT suborder at every `d ≥ 2` instance**: `v_p(index) > 0`
  measured on all eight A-rows (last column of §2.1) — the numeric exhibit of §3.1's
  structural finding.
* **C(iii) — dropping `Nat.Coprime u ℓ` defeats the read** (HE6-T-BADKEY's genre, minimal
  instance): `x² − 9` over ℚ₃ is "(2,2)-pure"-shaped but splits; each factor has
  `inertiaDegOf = 1`, defeating the `m·d = 2` prediction. The hypothesis is load-bearing.
* **C(iv) — the general defeat system is INFEASIBLE given the published theorem** (the
  constructed-counterexamples-over-sweeps discipline): a defeat needs a monic irreducible
  factor `g'` of a leaf `g` with `m·d ∤ inertiaDegOf g'`. By landed B.57, `g'` is
  `(u,ℓ)`-pure with `sideDeg d' ≤ d` and residual dividing `unit·ψ` up to the B.48
  bookkeeping; with `ψ` irreducible of degree `d = deg resPoly`, `d' ∈ {0, d}` and `d' > 0`
  for positive degree, so `g'` has full degree and `g' = g` — the defeat reduces to
  `f(g) ≠ m·d` for the irreducible leaf itself, which is the negation of published (A). So
  a counterexample to the axiom IS a counterexample to [FGMN] Thm 6.6/[GN15] Thm 2.3:
  infeasible short of refuting the literature. (Numerically corroborated by §§2.1–2.2.)
* **C(v) — bonus, found by the battery itself**: the first A6 candidate used residual
  `y³ + y + ω`, which is REDUCIBLE over F₄ (`ω²` is a root: `1 + ω² + ω = 0`) — a hand-check
  error caught by the oracle. The corrected row keeps `y³ + ω` (the cubing map on F₄ has
  image `{0,1}`). The failed candidate is retained in the battery as a dissection
  cross-check: two primes above 2, `f`-multiset `{2,4} = {m·1, m·2}`, both `e = 1` — the
  B.48/B.63 per-residual-factor read confirmed on a mixed residual.

### 2.4 CERT-D — route 1's mechanism (HE3-4U) executed on an `m ≥ 2` instance

At EC3 (`g = φ₄² + tφ₄ + t²x` over `F₂[[t]]`, the widened region): unramified base change
`F₂ → F₄` splits `φ̄₄ = (x+ω)(x+ω²)`; the two mod-`t` blocks `(x+ω)², (x+ω²)²` are coprime,
and a linear Hensel lift (fixed mod-`t` Bezout, 24 `t`-digits, final error `O(t²⁵)`,
machine-checked) produces `g = G₁·G₂` over `F₄[[t]]`. Each component `Gᵢ` is a degree-2 leaf
at a LINEAR key over `F₄((t))`: measured `v(c₀) = 2`, `v(c₁) ≥ 1`, component residual
irreducible over F₄ (no root among all 4 elements), and the component order's measured
norm-valuation gcd is `2` — so `f(Gᵢ/F₄((t))) = 2`, and the split-sum over the `f₁ = m = 2`
conjugate components gives `f = 2·2 = 4 = m·d` over `F₂((t))`, matching EC3's directly
measured gcd `4`. This is HE3-4U's mechanism (`"Φ′ = ∏ Φ′_a … summing over all f₁
components"`) run end-to-end on an instance — certification of the ROUTE, not a proof and
not a citation (the lemma is corpus-internal, unpublished; see §3.3).

---

## §3. ANALYSIS AND VERDICT

### 3.1 New structural finding: the box's region lies entirely OFF the monogenic-maximal locus

**Claim.** Let `g'` be monic, `(u,ℓ)`-pure at an order-1 key `φ` of degree `m`, with
`ḡ' = φ̄^{ℓ'd'}` (`ℓ'd' ≥ 1`, `u ≥ 1`), and let `A := AdjoinRoot g' = O[x]/(g')`. Then
`A` is local with maximal ideal `(π, φ(α))` and residue field `A/(π, φ(α)) ≅ resField φ`,
of `ResidueField O`-degree `m`.

*Proof.* `A/πA ≅ (ResidueField O)[x]/(φ̄^{ℓ'd'})`, a local ring with nilpotent maximal ideal
`(φ̄)`; so `A` is local with maximal ideal `𝔪 = (π, φ(α))`, and
`A/𝔪 ≅ (ResidueField O)[x]/(φ̄) = resField φ`, a field of degree `m`. ∎ (This is B.49's
landed mechanism; nothing new.)

**Consequence.** Suppose `d ≥ 2` (any factor `g'` of a `d ≥ 2` leaf that still has
`d' ≥ 2` — under B.61's hypotheses, `g' = g` itself). If `A` were a DVR (= the
monogenic-maximal locus of `TypeOfFaithful.lean`), then the classical `(e,f)` of `𝔪` would
be read off `A` itself: `f = [A/𝔪 : ResidueField O] = m` and, by the fundamental identity
(Quarry), `e = ℓ'd'`. But the classical pair of the extension `L/K` is `(ℓ', m·d')` by (A).
A DVR order IS the maximal order, so the two readings would coincide, forcing `m = m·d'`,
i.e. `d' = 1` — contradiction. **Hence at `d ≥ 2` the order `AdjoinRoot g'` is never a DVR,
and the landed HYP.01 machinery (`typeOf_eq_classical_of_isDiscreteValuationRing`,
`typeOf_inert_faithful` — both keyed to `[IsDiscreteValuationRing (AdjoinRoot g)]` or to
`Irreducible ḡ`, and `ḡ = φ̄^{ℓd}` is never irreducible at `ℓd ≥ 2`) can NEVER fire on the
box's region.** No assembly of landed pieces proves any instance of `B-BOX-1` at `d ≥ 2`:
the box genuinely requires the field-side carrier (B.62's object) or an axiom. This
*confirms* H-7's sizing and CHAP-I I.21's stub note ("typing needs the factor-field
valuation-ring carrier — the same object B-BOX-1's repair route would build") by a proof
rather than an impression, and it upgrades the "order/field gap" from a caution to a
structural theorem of the region: **on the box's entire region, order ≠ maximal order.**
(Numeric exhibit: CERT-C(ii).)

### 3.2 Verdict: CITE-CANDIDATE, as a composite cite with disclosed glue

The KEY QUESTION of the charge — is the box's content a stated theorem of the Montes
literature at COVERS-ALL-O scope? — has answer: **YES for its mathematical core (A)**, with
the SAME sources and scope verdict already resolved for NS-2 (COVERS-ALL-O,
`docs/CITE_SCOPE_RESOLUTION_2026-08-13.md`), **plus two glue legs (B)/(C) that are classical
valuation theory rather than Montes literature**, and one mathlib-tier leg (D). The axiom
must be stated in landed vocabulary (`inertiaDegOf`, the order-level gcd), because the
field-level statement is not typeable in `leanfinal` (no carrier — CHAP-I I.21). So the
gate-(b) draft at §5 is a **composite cite**: primary [FGMN] Thm 6.6 + [GN15] Thm 2.3 &
eq. (2.1) for (A); Serre/Neukirch for (B)+(C); (D) absorbed as mathlib-checkable. This is the
same honesty shape as B.42's draft (whose dev/npHgt dictionary is likewise repo-side glue
audited in the faithfulness entry, and whose [FGMN]-properness co-cite this draft inherits),
with ONE more ingredient class (classical valuation theory) explicitly named. If the owner
prefers a pure-single-theorem cite discipline, the alternative is REDUCES-TO(B.62): carry the
box until the spectral-norm (or integral-closure) carrier unit fires — §3.1 shows there is no
third route through landed vocabulary.

### 3.3 Repair routes, re-graded after certification

* Route 1 (HE3-4U): NOT citable (unpublished); mechanism CERTIFIED on an instance (§2.4);
  remains the natural *in-repo proof design* for the `ℓ = 1` region if the owner ever wants
  the cite retired — but it needs the H-6-excluded base-change + descent machinery, sized
  B.62-tier by A-F.7.
* Route 2 (root label): = the content; published form = the cite; in-repo form = B.62,
  UNCHANGED as the chapter's named repair (opus-tier, norm-to-`addVal` bridge RISK as
  recorded). §3.1 adds: any in-repo proof MUST build the carrier (or an `addVal`-on-`L`
  substitute); there is no shortcut through the order.

---

## §4. LEDGER AND CONSUMER EFFECTS (if the owner signs §5)

* **No new ledger row** — A-F.7's addendum already ruled the box "adds no new ledger row";
  the cite enters the owner's gate-(b) queue as the FOURTH literature cite (after B.42's
  [GN15]/[FGMN], HYP.15's C.94 = [AGNPRW] Thm 5.2, HYP.38's C.66/C.92 = FGMN). CHAP-I §3.11
  census: CITE (gate (b)) count 3 → 4 on signature.
* **B.61** consumes the axiom as `hBOX` verbatim (the axiom's conclusion is B.61's binder
  under B.61's other hypotheses PLUS `hirr : Irreducible resPoly` — B.61's signature must
  supply `hirr` at its call sites, which its consumers B.71/B.79 already carry through B.63's
  separable-residual splits; NOTE: B.61's own frozen signature does NOT carry `hirr` and must
  NOT silently gain it — the axiom is applied at instantiation sites, not spliced into B.61).
* **B.71 / B.79** (and B.63's `hperim` general branch): conditionality relabels from
  OPEN-MATH `B-BOX-1` to gate-(b)-cite — same honesty class as the B.42 inheritance (H-14
  pattern: the axiom name appears in downstream `#print axioms`).
* **C-BOX-1 (C.60)**: the same published source covers the level-2 residue leg ([GN15]
  Thm 2.3 at any chain length); the level-2 axiom instance is a SEPARATE signable statement
  in C's vocabulary — flagged to CHAP-C's owner, not drafted here (C.60's repair-route field
  should gain a pointer to this note).
* **CHAP-I §3's OPEN-MATH rows** for the `ℓ ≥ 2, d ≥ 2` and `ℓ = 1, m ≥ 2, d ≥ 2` regions:
  re-gate on the signature instead of on open mathematics (the I.20 stage display's S3 row
  and the 2026-08-15 addendum's routing both point at this box).
* **HYP.01 / HYP.12** ([CORE-SET] neighbours): UNTOUCHED — the axiom is stated in the
  gcd/order vocabulary and does not identify the two APIs (GC-3 respected); §3.1 in fact
  sharpens the audit surface for HYP.01's eventual review (the disc ≠ 0 locus at `d ≥ 2`
  leaves has order ⊊ maximal order, always).

---

## §5. THE GATE-(b) CITE DRAFT (for CHAP-I's queue, at the B.42 standard)

**COMPOSED AND APPENDED**: `blueprint/CHAP-I_capstone_conditionality.md`, dated addendum
"(2026-08-16, OM-5 — GATE-(b) CITE-CANDIDATE: the leaf dichotomy for `B-BOX-1`)" — the full
faithfulness draft (composite-cite disclosure; [FGMN] Thm 6.6 primary + [GN15] Thm 2.3 &
eq. (2.1) co-primary + GMN Cor 1.20 ℚ_p-scope secondary; glue legs (B)/(C) with
FLAGGED-FOR-LOOKUP display numbers; (D) = `Algebra.norm_localization` verified present at the
pin; clause mapping; `hirr`-necessity from CERT-C(i); consumer effects; queue mechanics
4 (+1) → 5 (+1) with the merge option into B.42's signature event).

**THE SIGNABLE ARTIFACT IS LANDED IN LEAN** (beyond the B.42 standard, whose statement lived
only in the addendum): `Uniformity.Density.Leaf.BBox1CiteStatement`
(`leanfinal/Uniformity/ChapB/BBox1CiteStatement.lean`) — a closed `Prop`, **no axiom, no
`sorry`**, compiled at the pin with Lean-core footprint — plus the machine-checked wiring
`typeOf_leaf_of_cite : BBox1CiteStatement → (B.61's conclusion under the leaf hypotheses +
hirr)`. Signature alone (adding `axiom bBox1_cite : BBox1CiteStatement` + the
`AXIOM_FAITHFULNESS.md` entry) completes the `d ≥ 2` leaf rows; until then nothing changes.
NOTE for the orchestrator: the file is not registered in `ChapB.lean`'s import list (0c
artifact this unit does not own) — `lake env lean` covers it; add the import at the next
aggregation pass.

---

## §6. COMMIT TRAIL

* `51d01fbb` — skeleton (outline, decomposition (A)–(D), §3.1 structural finding,
  certification plan).
* `6feb5a0d` — `verification/om5_bbox1_cert.gp`, 128 checks, 0 failures.
* `28a23e89` — `leanfinal/Uniformity/ChapB/BBox1CiteStatement.lean` (the signable `Prop` +
  the wiring theorem; `lake env lean` green, Lean-core footprint).
* (this commit) — note finalized; CHAP-I dated addendum appended.
