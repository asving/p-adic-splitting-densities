# OWNER REVIEW QUEUE — 2026-08-26

Consolidated secondary-review queue, compiled by unit RQ from four sources: (1)
`docs/CITE_REVIEW_LIST.md` (the queue's existing spine — referenced, not duplicated,
below); (2) all `runs/wave-b/verdict_*.md` files (106 at sweep time — one, `verdict_PK12.md`,
landed mid-sweep on 2026-08-26 and is folded in below), grepped for
review/flagged/owner/human and read in full context; (3) the dated amendment records
`[A-C.10..16]`, `[A-E.7]`, `[A-I.2]`, `[SG-0]`, `[PK-*]` in `leanspec/` and
`leanfinal/Uniformity/`; (4) `docs/in-progress/PACKAGING_ROUTE_2026-08-25.md` §6's
review-list rows (U15). Coverage stats and method are in
`runs/wave-b/verdict_RQ.md`.

**How to use this document.** Three tiers, ordered by how directly they match the
owner's two standing gates (main Lean statement changes; literature-cite imports —
`signoff-scope-2026-08-24`/`-08-15` memory entries). Tier 1 is the owner's stated
primary role (cite spot-checks). Tier 2 is re-signs of already-signed statements
(should be skimmable — most are honest repairs of machine-refuted forms, already
enacted, not asking permission but flagging what changed). Tier 3 is net-new general
lemma banks the fleet wrote, flagged by the units themselves as worth a look because
they are new mathematical content rather than transcription. Every row names the exact
file/declaration and where to look; nothing here has been re-derived or re-checked by
this unit — RQ only located, deduplicated, and organized what other units already
flagged.

---

## Tier 1 — Cite transcriptions (the owner's primary role)

| # | cite | Lean name(s) | landed at | quoted-clause location to spot-check | status |
|---|---|---|---|---|---|
| 1 | [AGNPRW] Thm 5.6 (OM descent terminates) | `agnprw_termination : NS7TerminationStatementR` + NEW binding `NS7Termination` (I.01), discharged via `ns7Termination_of_cite` | `leanfinal/Uniformity/ChapC/C94.lean` + `ChapI/I01.lean` | C94.lean module docstring (the executed print-read) | OPEN — review the I.01 BINDING only (the redraft grammar itself was audited at A-C.6) |
| 2 | FGMN, *Residual ideals of MacLane valuations*, J. Algebra 427 (2015): Thm 2.8 + Cor 4.12(3) + Cor 4.9(3) + Def 5.4 | `fgmn_dv_exact_mul` (axiom; C66b) | `leanfinal/Uniformity/ChapC/C66b.lean` | C66b.lean module docstring (quotes + the corpus↔paper dictionary: `IsDvPure`↔Def 5.4, `dvResPoly`↔R_r up to twist) | OPEN — review the dictionary + spot-check quotes against the journal text. **New consumers since the row opened:** `C123r.lean` (`s2_composedKeyPow_mem` etc., via C.131ac's `composedKey_pow_mem_towerLocus`) and `U-HEART`/`UASM`'s chain at `C131af.lean` — same axiom, no new dictionary content, listed here so the spot-check covers every use site |
| 3 | FGMN Cor 4.12(2) grade-anchoring vs. the S2 parity twist (U14/A-C.13, 2026-08-25) | `FGMNSourceData.PrevGrade`, `FGMNSourceLaws.graded_mul`'s restored `PrevGrade β'` premise, `FGMNCalculus.Rgr_mul` | `leanfinal/Uniformity/ChapC/C130fg.lean`, leanspec `ChapC.lean` NODE C.92, `C130pk.lean`, `C130la.lean`, `C99r.lean` | `docs/in-progress/COR412_ADJUDICATION_2026-08-25.md` §3–4 (Def 3.12/3.13 + Cor 4.12(2) proof, "because `s_r(β)=0`") | **ENACTED as A-C.13** (see Tier 2 row 4) — still needs the owner's spot-check of the two quoted clauses (Def 3.13's line parameterization; the Cor 4.12(2) proof line) against the journal text, per the original CITE_REVIEW_LIST row |
| 4 | (not yet declared) FGMN's order-2 theorem of the residual polynomial, same J. Algebra 427 (2015) paper | none — flagged only | would land near `C78.lean`'s dv-purity chain (CC-16/`FGMNCalculus` path) | `runs/wave-b/verdict_C78.md` lines 62–65 | **FUTURE Tier-1 item** — explicitly flagged "print-read + owner review-list row required — NOT declared by this unit"; nothing to check yet, recorded so it isn't missed when it lands |
| 5 | (decision point, not yet made) C.63 `classSize_separable` | none | would land at the C.27→C.59→C.61→{C.62,C.63}→{C.49,C.69} chain (`runs/wave-b/verdict_AC15C27.md` §"6-row chain") | `runs/wave-b/verdict_DEC1.md` row "Genuinely MISSING objects" | **FUTURE Tier-1 item** — DEC1 named the fork explicitly: "landing = prove, or cite-axiom behind the owner gate"; currently a leanspec axiom only, blocked on the C.34 dissection frontier either way |
| 6 | **A-I.6 (2026-08-28) — CAPSTONE-SURFACE AMENDMENT, not a cite: the signed `CapstoneHypotheses.ladder` field REBOUND to the live-range record** (machine-forced statement change; supersedes the Tier-2 LVT row's "future A-I.6, NOT enacted" clause) | `CapstoneHypotheses.ladder`'s result record: `Ladder.LadderSupply.{0,0,uW,uG,uKt,uL} C B G Kt L N v ρ q` → `IFC5.LadderSupplyLive.{uW,uG,uKt,uL} C B G Kt L N v ρ q n` (first three fields byte-identical to E.24's; ONLY `vartheta` gains the `CanonicalLadderLiveAt` premise); the two live decls MOVED byte-for-byte from `IFC5.lean` to NEW `Uniformity/ChapI/I10LadderLive.lean` (namespace unchanged) | `leanspec/Leanspec/ChapI.lean` (A-I.6 STATE UPDATE + field + gate item (4)) + `leanfinal/Uniformity/ChapI/I10_I15_I18.lean` (byte-identical twin) + `IFC0.lean` (pin replaced; `LadderField`/suppliers/split byte-frozen as the refuted tension record) | the forcing refutation: `leanfinal/scratch/AI6_probe.lean` (`AI6Probe.ladderField_four_refuted : ¬ IFC0.LadderField 4`, `capstoneHypotheses_four_refuted` — pre-rebind form archived at commit `ce301df1`, now pinned to the retired verbatim copy `CapstoneHypothesesUnbounded`), all Lean-core, exit 0; adjudication `runs/wave-c/verdict_AI6.md`; enactment `runs/wave-c/verdict_AI6E.md` | **ENACTED (unit AI6E)** — the owner's highest-priority read: the signed field's OLD bytes were machine-proved EMPTY at degree 4, so no rider was possible; the rebind weakens a hypothesis (STRENGTHENS the conditional capstone); field list + I.15–I.18 statements byte-unchanged; `∀`-degree inhabitation stays OPEN |
| 7 | **A-I.7 (2026-08-28) — CAPSTONE-SURFACE AMENDMENT, not a cite: the `ladder` field's `mp1` leg REBOUND to the same-realization live carrier** (machine-forced statement change; the GN15 payload `GNCitePayload` is a STATEMENT CARRIER one-to-one with Guàrdia–Nart, Contemp. Math. 637 (2015), Thm 2.3's exact hypothesis list — NO cite consumed) | `CapstoneHypotheses.ladder`'s result record: `IFC5.LadderSupplyLive.{uW,uG,uKt,uL} C B G Kt L N v ρ q n` → `IFC5.LadderSupplyLive₂.{…} C B G Kt L N v ρ q n` (`package`/`lb1`/`vartheta` byte-identical; ONLY `mp1` becomes `MP1CarrierLive` = MidPeelEmission demanded only at successors exported by a same-witness `RecenterStep`); four export decls (`MP1StepCore`/`GNCitePayload`/`RecenterStep`/`MP1CarrierLive`) PROMOTED byte-for-byte from `scratch/REX_probe.lean` into NEW `Uniformity/ChapI/I10RecenterLive.lean`; S2 landed prefix + tangent-class impossibility in NEW `I10RecenterLiveS2.lean` | `leanspec/Leanspec/ChapI.lean` (A-I.7 STATE UPDATE + field + gate item (3)) + `leanfinal/Uniformity/ChapI/I10_I15_I18.lean` (byte-identical twin) + `IFC0.lean` (pin retargeted; everything else byte-frozen) | the forcing refutation: `leanfinal/scratch/REX_probe.lean` (`REXProbe.s2_unrestricted_mp1_false : ¬ Ladder.MP1Carrier C2 B2`, `s2_ladderSupplyLive_false`, `capstoneHypotheses_four_refuted_via_mp1` — pre-rebind form archived at commit `05f067b7`, now pinned to the retired verbatim copy `CapstoneHypothesesUnrestrictedMP1`), all Lean-core, exit 0; adjudication `runs/wave-c/verdict_REX.md`; enactment `runs/wave-c/verdict_AI7E.md` | **ENACTED (unit AI7E)** — the A-I.6 record's remaining unrestricted leg was machine-proved EMPTY at degree 4 (S2's own `B.F = Φ+1`, `Λ = −1`), so no rider was possible; the rebind weakens a hypothesis (STRENGTHENS the conditional capstone); field list + I.15–I.18 statements byte-unchanged; supersession pins prove OLD ⟹ NEW; the S2 `RecenterStep` instance stays the NAMED OPEN `S2-RECENTER-EXPORT` |
| 8 | **A-I.8 (2026-08-28) — CAPSTONE-SURFACE AMENDMENT, not a cite: `GNCitePayload` RETIRED from the live `RecenterStep`; the `mp1` leg goes cite-free via the DIRECT key route** (statement change forced by an HONESTY stop-line, NOT a machine refutation: DWR proved the payload shape structurally defective — the GN-selected factor can never literally equal the recentered key, so NO source-faithful occurrence exists, for ANY tower; the Prop stayed formally inhabitable only by source-unfaithful instances) | `CapstoneHypotheses.ladder`'s result record: `IFC5.LadderSupplyLive₂.{…} C B G Kt L N v ρ q n` → `IFC5.LadderSupplyLive₃.{…} C B G Kt L N v ρ q n` (`package`/`lb1`/`vartheta` byte-identical; ONLY `mp1` becomes `MP1CarrierLiveDirect` = MidPeelEmission demanded only at successors exported by a `RecenterStepDirect`: the GN payload replaced by `DirectKeyPayload` — same-chain `FGMNSourceData` + `source.keyPolynomial B'.Φ` + the universal `key_irreducible` law, to be filled at deep instances by direct `IsMuKey → Irreducible` theorems (D3-16/D4-15), NEVER by a literature axiom — plus DWR's four anti-splicing bindings `quot.F = B'.F`, `B'.T = B.T`, `quot.T = B.T`, `B.F = B'.Φ * B'.F`); five decls PROMOTED from `scratch/A8D_probe.lean` (declaration bytes identical; one docstring word `draft`→`live`) into NEW `Uniformity/ChapI/I10RecenterDirect.lean`; NOTE the honest asymmetry: the new `mp1` leg is DISCHARGED OUTRIGHT by the generic compiler `mp1CarrierLiveDirect_of_steps`, so the field's remaining open content is `package`/`lb1`/`vartheta` and the mathematical content moved to producing `RecenterStepDirect` occurrences (C4d/C5d) | `leanspec/Leanspec/ChapI.lean` (A-I.8 STATE UPDATE + field + gate item (3)) + `leanfinal/Uniformity/ChapI/I10_I15_I18.lean` (byte-identical twin) + `IFC0.lean` (pin retargeted; everything else byte-frozen) + `I10RecenterLiveS2.lean` (declarations byte-frozen, dated re-scope note) | the forcing stop-line: `runs/wave-c/verdict_DWR.md` (G9 RED — GN index alignment structurally impossible); the adjudication: `runs/wave-c/verdict_A8D.md` ((β) chosen; leg-by-leg table NO ROW BLOCKED; shapes machine-verified in `leanfinal/scratch/A8D_probe.lean`, Lean-core, exit 0, pre-enactment state archived at commit `3d545e6e`); enactment `runs/wave-c/verdict_AI8E.md` | **ENACTED (unit AI8E)** — supersession pins prove OLD ⟹ NEW at payload level (`directKeyPayload_of_gnCitePayload`, via `FGMNSourceLaws.key_irreducible`) and record level (`ladderSupplyLive₃_of_live₂`); NO step-level adapter exists (recorded, `I10RecenterDirect.lean` §3); rebind weakens a hypothesis (STRENGTHENS the conditional capstone); field list + I.15–I.18 statements byte-unchanged; the NAMED OPEN `S2-RECENTER-EXPORT` re-scoped to the direct fields (GN-only rows deleted) |

| 9 | **(staged, NOT declared) the deep-wild OM tower step** — the Montes/Okutsu induction correspondence: GMN, *Newton polygons of higher order in algebraic number theory*, Trans. AMS 364 (2012) (Theorem of the Polygon + Theorem of the Residual Polynomial, exact numbering to be pinned at print-read) + the standing FGMN J. Algebra 427 (2015) dictionary — the gate-(b) cite for `CellTypeScalingAt D` at `D ≥ 2` (DTR's named remainder, `runs/wave-c/verdict_DTR.md`) | none declared — TWO staged surfaces, owner signs ONE: `DeepWildTypeScalingStatement` (raw, IFCG48 §1) or `∀ D, 2 ≤ D → FactorCorrespondenceAt D` (correspondence, IFCG48 §5 — RECOMMENDED: strictly smaller trusted content, all `(e,f)`/type bookkeeping machine-checked; the Lean-core reduction `deepWildTypeScaling_of_correspondence` recovers the raw surface) | would land in `leanfinal/Uniformity/ChapI/IFCG48.lean` (both Props typed against the landed layer; consumers wired; whole-file `#print axioms` = Lean core) | `IFCG48.lean` module docstring: the faithfulness DRAFT — sources to print-read, the corpus↔source dictionary, and THREE scope caveats (esp. caveat 2: the multiplicity/inseparability fringe applies to BOTH surfaces and may force an explicit separability hypothesis — a statement-shape decision at signing) | **STAGED (unit CTS/CTS2, 2026-08-30)** — print-read + exact theorem numbering + caveat adjudication required at declaration; consumers already fire from either surface (`transport_decidedAt_iff_of_cite`/`_of_correspondence`, the `D = 2` dd supplies) |

**Not re-derived here, flagged for completeness:** `docs/AXIOM_FAITHFULNESS.md` carries
four older entries still marked "DRAFT — PENDING ASVIN REVIEW" (dated 2026-07-30/31,
predating this queue's protocol and CITE_REVIEW_LIST): `AX-CELLREC`
(`PadicMeasure.AX_cellRecursion`), `AX-INDEX`
(`OM.Classifier.omReadValuation_lt_of_certLevel_fkeyed`), `AX-LEAF`
(`OM.OmLeafFaithful.om_leaf_faithful`), and `AX-GMN-INDEX` (not yet declared as a Lean
axiom — additionally gated on two unbuilt definitions). These are out of this sweep's
four named sources; if still open they belong in a future pass over that file.

---

## Tier 2 — Amendments to signed statements (newest first)

Each row: the old (pre-amendment) form, the new (re-signed) form, and where the machine
evidence (refutation + repaired-form proof) lives.

| amendment | node / declaration | old form | new form | machine evidence | unit / date |
|---|---|---|---|---|---|
| A-C.24″ | C.38 `dv2ResPolyAnch_radical_eq` → `dv2ResPolyTw_radical_eq` + C.39 `dv2ResPolyAnch_scalar` → `dv2ResPolyTw_scalar`, over the NEW C.38a″ twisted-carrier cluster `twistExp`/`dv2ResPolyTw` (`leanspec/Leanspec/ChapC.lean` NODES C.38a″/C.38/C.39 — full numbered record at C.39; pre-amendment text archived at commit `48927e49`; the C.38a′ anchored cluster + `γgAnch`/`pinHeight` byte-untouched and consumed; blueprint untouched per A-C.21/22/23/24′ precedent). **SUPERSEDES the A-C.24′ row below — round 4 of the C.39 saga closes it: the SOURCE is vindicated, the transcription had dropped the carrier's twist** | the A-C.24′ rows put EFF.HE6R1.39's constant per-side scalar (and C.38's radical/degree tie) on the UNTWISTED anchored carrier `dv2ResPolyAnch` — but the source asserts its display between its OWN residual polynomials, which carry DEFINITION HE7-3's per-slot twist exponents ε_t = (s(m₁−tu₂) + t·s(u₂) − s(m₁))/ℓ (EFF.HE7.08's fixed-n₂ convention: "changing the choice multiplies its coefficients by explicit powers of β"; EFF.HE7.30 Step 2's τ_t assembly twist; tooth HE7-T-BADTWIST machine-load-bearing); over the untwisted carrier the honest law is F14C's slot-indexed family `root^{c₁(M₂′−t·u₂, c_g)}`, and the A-C.24′ constant is REFUTED-IN-MECHANISM at any β ≠ 1 frame with a parity-varying slot line: battery Part D (the 𝔽₄ frame, β of order 3) — the enacted constant FAILS at exactly the 15 odd-c_z complements of 127 while the family holds 127/127 (S2's β = 1 and the source's decided ℓ = 1 branch are both degenerate, which is why rounds 1–3's batteries could not see it); the radical half is likewise not slot-twist invariant (radical exhibit: the family weight moves the linear residual's root by a β-power) | both rows re-signed over the SOURCE-faithful TWISTED carrier `dv2ResPolyTw` (coefficient t = `root^{ε_t}` · the anchored coefficient; `twistExp` = HE7-3's exponents in exact cocycle-sum form): C.39's display scalar byte-kept from the source (PE3 F-1's `γ̂_g · root^{c₁(pinHeight, c_g)}`), hypothesis blocks byte-identical to A-C.24′'s; NO stationarity hypothesis added — ID1 (`C136f14d.twistExp_cocycle_transfer`, Lean-core) converts F14C's family into the constant law exactly, machine bridge `C136f14d.dv2ResPolyTw_scalar_of_anch_family`; cite-free (the SAME EFF.HE6R1.39 conclusion over the carrier the source itself defines); consumer note: C.79 (the μ₂* = 2 decision table, blueprint-OPEN) must consume the TWISTED object — root/irreducibility structure is not preserved between the carriers, degree-only consumers are indifferent (`natDegree_dv2ResPolyTw_eq`) | A24C source archaeology with the exact HE7-3/HE7.30/HE7.08 quotes (`runs/wave-c/verdict_A24C.md` §(d)); the landed twisted-carrier bank `leanfinal/Uniformity/ChapC/C136f14d.lean` (12/12 Lean-core: `twistExp`, ID1/ID2, `dv2ResPolyTw`, the scalar transfer, the classical product law, the Tw↔Anch bridge, the F1.5 adapter); battery Part D (`verification/a24b_anchor_battery.py`: family 127/127, enacted constant 15/127 failures, source law over the twisted reads 127/127); ZERO consumers of the two removed axioms (A24F pre-edit STOP-gate rg, only self-declarations + comments hit); leanspec `ChapC.lean` re-elaborates exit 0; **statement flags carried from A24C (trust boundary, new statement shapes): `twistExp`, `dv2ResPolyTw`, and the A-C.24″ axiom texts themselves**; honest remainder UNCHANGED from F14C: the digit-split convolution supplier (§2.1) — the re-signed C.39 stays an axiom until it lands, but its exact conclusion is now the Lean-core CONDITIONAL theorem `C136f14e.dv2ResPolyTw_scalar_of_conv_split` (`leanfinal/Uniformity/ChapC/C136f14e.lean`, 5/5 Lean-core, first-attempt): supplier `hconv` (F14C §2.1's byte-shape at the block/complement pair) + C.36/C.37-genre bookkeeping legs ⇒ the byte-kept display; F14C §2.2's stationarity obstruction confirmed DISCHARGED by ID1/ID2 in the composition | A24F, 2026-08-29 (verdict_A24F.md; draft verdict_A24C.md) |
| A-C.24′ | C.38 `dv2ResPoly_radical_eq` → `dv2ResPolyAnch_radical_eq` + C.39 `dv2ResPoly_scalar` → `dv2ResPolyAnch_scalar`, over the NEW C.38a′ carrier cluster `dv2FullReadPoly`/`dv2FullRead`/`dv2ResPolyAnch` + `γgAnch` (`leanspec/Leanspec/ChapC.lean` NODES C.38a′/C.38/C.39 — full numbered record at C.39; pre-amendment text archived at commit `dea24337`; the bare `dv2ResPoly`/`dv2Res`/`γg`/`pinHeight` defs and C.104's `repoRead := dv2Res` byte-untouched; blueprint untouched per A-C.21/22/23 precedent) | the A-C.1 rows put the source conclusion (PE3 F-1's `γ_g · root^{c₁(pinHeight, c_g)}` scalar and the radical/degree tie) on the BARE per-slot own-height carrier `dv2ResPoly` — refuted by two independent machine channels: (1) root-power (`C136l2e1.dv2Res_mul`: the normalized product law is CLEAN, so the signed root factor forces `root^cocycle = 1`, unsuppliable); (2) off-side junk (`C136f14.dv2ResPoly_scalar_shape_digit_kill`: unguarded lattice coefficients force a false digit-vanishing transfer under ANY constant scalar; live block×block instances in the A24B battery Part B′, bare 1 vs anchored 0); the A24B battery also REALIZED the on-side anchor-shift risk — 135/135 μ-value ties shift the level-1 anchor (δ = 1, injection genre), so no normalized-γg law survives on the bare carrier either | both rows re-signed over the GUARDED/ANCHORED classical carrier `dv2ResPolyAnch` (C.25's fixed-grade shape one level up: coefficient t = anchored absolute read `dv2FullRead` at line height `M₂ − t·u₂`; off-side junk-0 guard + reduction stability are THEOREMS in `C136f14b.lean`, incl. the unconditional absorber `dv2FullRead_modByMonic`); C.39's scalar is the SOURCE's own shape byte-unchanged except `γg ↦ γgAnch` (the pin-anchored complement read); premises: GC-1 pin binders `(M₂, hp₂)`/`(M₂', hp₂')` added, `hcop : Nat.Coprime u₂ ℓ₂` added (engine-honest: the guard's span exactness), A-C.22 floor retained; cite-free (no new axiom SOURCE — same EFF.HE6R1.39 conclusion, repaired carrier; A24A §1: the source's normalizers ARE pin-anchored) | A24A refutation channels + carrier diagnosis (`runs/wave-c/verdict_A24A.md`, `C136f14.lean` Lean-core ×7); A24B battery `verification/a24b_anchor_battery.py` (12/12 landed-value oracles, 8128 digit pairs, 120+15 = 135/135 tie shifts absorbed, carry law 8128/8128) + the landed carrier bank `C136f14b.lean` (15/15 Lean-core); zero landed consumers of the two removed axioms (A24B pre-verified + A24E re-verified by rg); leanspec `ChapC.lean` re-elaborates exit 0; **statement flags carried from A24B §3 (trust boundary, new statement shapes): `dv2ResPolyAnch`, `dv2FullReadPoly_eq_shift`, `dv2FullRead_modByMonic`**; honest remainder: the re-signed C.39 is NOT yet a theorem — needs the digit-split convolution assembly + the carry law (`Dv2FullReadCarryLawStatement`); L2E4 (verdict_L2E4.md) adjudicated the bare unweighted convolution unsuppliable, so the assembly must be the anchored/weighted form — the F1.4/F1.5 rows stay open pending those two pieces | A24E, 2026-08-28 (verdict_A24E.md; draft verdict_A24B.md §4, basis verdict_A24A.md) |
| A-C.23 | C.56(i) `refine_invariants` (`leanspec/Leanspec/ChapC.lean` NODE C.56 — full numbered record at the node; pre-amendment text archived at commit `523c8a7b`; blueprint NODE C.56 deliberately untouched per A-C.21/22 precedent) | the conclusion's third conjunct `IsTestKey (T.levelDatum hπ) (composedKey T - k2DigitLift T s lam)` — an elaboration OVERCLAIM of the source `EFF.GENTOW1.28` (which claims only the perturbation-stable package, never exact-zero test-family membership); unprovable-AND-unrefutable under the A-C.3 opaque `k2Coord` (true only on a pathological `Classical.choice` realization), FALSE under the faithful power-basis read (s2Tower: `e₂=2, E₂=10, lam=11, s=1, μ₂=0`) | the source-faithful M4 package: `dev F.key Φ₂⁺ (e₂·f₂) = 1` ∧ `IsDvPure F Φ₂⁺ u₂ e₂` ∧ ∃-pinned side-min height `f₂·u₂` + `dvResPoly = towerLabel T` (all byte-unchanged from `Φ₂`'s own reads); conjuncts 1–2 and EVERY premise byte-unchanged; adjudicated over the `IsMuKey` alternative by the end-to-end criterion (consumers read only `dv2Pin`; the "hence"-chain consumes HasLabel-shaped data; no landed route to `IsMuKey Φ₂⁺`); **the re-signed statement is a LANDED THEOREM** `Uniformity.Density.Tower.refine_invariants` (`leanfinal/Uniformity/ChapC/C150rb5.lean`, Lean-core) — C.56(i) is no longer an open axiom obligation | RB3R (codex adversarial pass 5/5 CONFIRMED, no CRITICAL; `runs/wave-c/verdict_RB3R.md` §2: every lift term at `F.key`-abscissa `towerSolve u₂ e₂ lam + e₂t`, nonzero offset ⟺ `e₂ ∤ lam`; vacuity escape closed by `μ₂=0`/`f=1`); conjunct + refutation record `C150rb4.lean` (unchanged); repaired-form proof `C150rb5.lean` (436 lines: MH.6/M4 at `x := Φ₂`, `y := −lift`, + the NEW grade floor `dvSupp_k2DigitLift_ge : W(lift) ≥ lam` closing the EFF.GENTOW1.27 `a₀ ≥ 0` audit from `T.hfloor`; per-file exit 0, footprints Lean-core); leanspec `ChapC.lean` re-elaborates exit 0; census ripple `C135c93.lean` row (2) BLOCKED→LANDED with live `#check @refine_invariants` (per-file exit 0); chapter roll-up `lake build Uniformity.ChapC` exit 0 (8909 jobs) | A23E, 2026-08-28 (verdict_A23E.md; basis verdict_RB3R.md, supersedes C93T row (2)) |
| A-C.22 | C.37 `dv2Supp_translation`, C.38 `dv2ResPoly_radical_eq`, C.39 `dv2ResPoly_scalar`, C.64 `mult₂_readable` — the F1.3–F1.6 signed floors (`leanspec/Leanspec/ChapC.lean`, 4 hunks: full record at C.37, pointer records at the other three) | `(hseam : ℓ₂ * L.seam < u₂)` — the seam floor, an `L.ℓ`-factor WEAKER than the landed generic discharge's premise (they coincide iff `L.ℓ = 1`) | `(hseam : ℓ₂ * (L.ℓ * L.seam) < u₂)` — the ENGINE-HONEST floor above the test-key grade `L.ℓ·L.seam` (= `C131aa.dvSupp_testKey`'s key grade); conclusions BYTE-UNCHANGED; out of scope, deliberately: C.65's ∀-floor (a premise of the landed F1.7 proof `C134f12c.dv2_length_sum` — weak premise = stronger proved theorem), C.68's binder (no landed engine forces it yet), C.118's guarded congruence (PROVED at the seam form in `C118b.lean`) | `C134dv2g.lean` Part 7 (P3): the engine consumes the OUTER floor `ℓ₂·KΨ < u₂` at `KΨ = L.ℓ·L.seam`, and its slot-1 sharp carry bound is TIGHT (`C132nv3b`), so weak→engine is unprovable; S2/μ₃ margins (`C130np4.seam_s2Tower`/`ell_mul_seam_s2Tower`: `L.seam=5`, `L.ℓ·L.seam=10`; outer pair `(u₂,ℓ₂)=(21,2)` per `C132nv4`/`C132rp*`): engine `2·(2·5)=20<21` vs pre-A-C.22 `10<21`; the generic implication is FALSE at `(ℓ₂,u₂)=(2,11)` (`10<11` but `20≮11`); no consumer threads a strengthening (repo-wide name search, verdict_P1U §P3 — the four names have zero callers); leanspec `ChapC.lean` re-elaborates exit 0 (40s); `C134dv2g.lean` (P3)-record ripple comment-only, per-file exit 0 + direct importer `C134p1.lean` exit 0, footprints unchanged | A22E, 2026-08-28 (verdict_A22E.md; draft verdict_P1U.md §P3) |
| VAUD-ϑ live-range (SUPERSESSION — no signed statement changed; the `CapstoneHypotheses.ladder` rebinding = future numbered A-I.6, OWNER-GATED, not enacted) | NEW `leanfinal/Uniformity/ChapI/IFC5.lean` (`CanonicalLadderLiveAt`, `LadderVarthetaSupplierLive`, `LadderSupplyLive`, `LadderFieldLive`, `ladderFieldLive_iff_suppliers`, `ladderFieldLive_of_suppliers`, + 2 supersession pins `ladderVarthetaSupplierLive_of_unbounded`/`ladderFieldLive_of_ladderField`); comment-level LVT record in `leanspec/Leanspec/ChapI.lean` after the A-I.5 block | IFC0 §1's `LadderVarthetaSupplier`/`LadderField` demand `VarthetaRes` at EVERY `i ≥ 3` (E.24's unbounded `vartheta` field, which the signed I.10 `ladder` field consumes), but the I.10a guard's `GaugeFamilyViewEq` pins the supplied `(v, ρ, q)` only at `GaugeLive core.r i` (`1 ≤ i < core.r`) — the unbounded demand is NOT guard-derivable, and adding an HVR-shaped guard premise is FORBIDDEN (D.62 trap fence) | fourth supplier restricted to `∀ i ≥ 3, CanonicalLadderLiveAt … n i → VarthetaRes … i`, where `CanonicalLadderLiveAt` re-exhibits a realization witnessing the SAME external socket views (slot/block/gauge) with `GaugeLive core.r i` — liveness and family views on ONE witness; IFC0 §1 BYTE-UNTOUCHED as the tension record (GCW-6 pattern; retired as a campaign target); suppliers 1–3 consumed unchanged; `LadderFieldLive` is the campaign target | VAUD certificates (`leanfinal/scratch/VAUD_probe.lean`, Lean-core, exit 0): `gauge_view_offRange_kernel_replacement` (guard view invariant under arbitrary dependent `(v,ρ)` + `q` replacement at every off-range index), `vartheta_sitewise_negative` (concrete FALSE sitewise `HVarthetaRes`), `s2_three_not_gaugeLive` (landed depth-2 S2 socket's first demanded index `3` off range); IFC5's 8 declarations all Lean-core (`lake env lean Uniformity/ChapI/IFC5.lean` exit 0, zero `sorry`); the supersession pins prove OLD ⟹ NEW (range restriction only, no strengthening smuggled); leanspec `ChapI.lean` re-elaborates exit 0 (comment-only record) | LVT, 2026-08-28 (verdict_LVT.md; basis verdict_VAUD.md + `LADDER_SUPPLY_2026-08-27.md` `[VAUD]`/`[LVT]` sections) |
| A-I.5 | NEW NODES I.10c `GC13Wiring` (structure) / I.10d `WiredGentowDemand` (def) + an interpretive rider on the byte-unchanged `deepTwist` field (`leanspec/Leanspec/ChapI.lean`); leanfinal twins + μ₃ consumption route in NEW `leanfinal/Uniformity/ChapI/IFC3.lean` | GC-13's read dictionary (`w_i = R_{i+1,κ̄_i}(n̂_i(u_{i+1}))`, D.44's note) had NO carrier — IFC1 displayed `hread`/`htheta` only as open hypothesis SHAPES, and FGCW certified the wiring UNDERDETERMINED at the landed structures (`towerRead` a free field; both truth values realized); DT0's per-level `GentowW` demand was the flat `DeepGentowSupplier` conclusion, refutable outright at any zero-read depth-`≥4` witness (GCW-6 tension) | `GC13Wiring` SIGNED verbatim per GCW-0's decision record (sitewise scalar-read relation at the abstract D.44 surface; grade `e'·(s·u')` = OPEN-DICT-2 full-current; normalizer `chainNormBelow r`); the DT0 demand amended to the WIRED SITEWISE form `WiredGentowDemand = GC13Wiring ∧ GentowW` (a per-site STRENGTHENING; supplier gains the wiring leg per P2 — the `CanonicalDeepTwistConfig` guard-extension fork REJECTED, socket + `CapstoneHypotheses` byte-unchanged); the UNIVERSAL wired supplier deliberately NOT signed — BLOCKED-BECAUSE on the missing `WleCleared` per-level calculus/receiver exporter (every site-quantified form defective; `f' = 0` junk-site vacuity is the machine anchor) | IFC3's 10 declarations all Lean-core (`lake env lean Uniformity/ChapI/IFC3.lean` exit 0): the plug fires byte-shape (`s2Mu3_gentowWWindow_of_wiring` = IFC2's `hread` binder superseded by `hwire.read_norm 1 le_rfl le_rfl`), the law forces the S2 read normalization (`s2_wiring_forces_read_one`), the anti-cook tooth `s2_wiring_refuted_at_landed_exports` (law FALSE at the landed zero-read exports — blueprint falsifier 1 survives signing), the fabricability fence `gc13Wiring_vacuous_of_f'_zero` + junk-site collapse `wiredGentowDemand_iff_of_f'_zero`; leanspec `ChapI.lean` exit 0 with the two new `#axiom_core` census guards passing and gate items (18)–(20) executed; ⚠ `s2_gentowW_of_read_one` cited ONLY as the fenced freedom half (NOT SUPPLY) | GCW1, 2026-08-27 (verdict_GCW1.md; decision basis verdict_GCW0.md + orchestrator F2=P2; VAUD's parallel vartheta range-restriction finding NOTED, not enacted — owner-gated, IFC0 untouched by both units) |
| A-C.21 | C.92 `thetaRatio`; C.99 `gentow2_Bpp`/`theta_letter_valued`; C.89 `GENTOW5W`/`gentow5w_two`/`gentow5w_one_shape`; C.90 `hres` display (`leanspec/Leanspec/ChapC.lean`, 7 hunks) | the B-1/`hprev`/conclusion grades read the normalizer's printed INDEX as its grade (`hnorm : ExactGrade (d·u')`, `hprev : PrevGrade u'`, `Rgr ((f'-t)·u')`) — the source's `κ̄ = u₃/e(μ₂)`, `β_t = u₃d/e(μ₂)` (GENTOW2_PROOF S5.2 ll.324-325, 711, 740-744) are PRE-clearing Γ-rationals, so the printed numeral is off the full-current coordinate by the factor `e'` | all family grades cleared: `hnorm : ExactGrade (e'·(d·u'))`, `hprev : PrevGrade (e'·u')` (= the source's own l.748 "lies in Γ₂" peel premise, now TRUE at honest grades), `thetaRatio`/`GENTOW5W`/one_shape/C.90-`hres` `Rgr` reads at `e'·(…·u')`; pre-A-C.21 leanfinal family RETAINED VERBATIM (dated supersession comments) because the do-not-edit record `C132sg2.lean` elaborates against it | MSG2's refutation `C132sg2.s2Mu3_gentow5w_two_hnorm_refuted` (grade 42 = 2·21 EVEN vs odd 21; `C132rp9` + `C132rp10b.s2Hgt₃_corr`; depth-1 analogue 10 = 2·5 vs printed 5); amended family re-proved Lean-core in NEW `C132sg3.lean` (`gentow2_Bpp_cleared`, `gentow5w_two_cleared`, one-shape conditional, C.90 twin); **the payoff: `C132sg3.s2Mu3_gentow5w_two` — the signed `GENTOW5W` at `(2,1,21)` UNCONDITIONAL (campaign exit gate 4 closed)** + `s2Mu3_gentow5w_old_refuted` (the pre-A-C.21 carrier is FALSE at the honest instance); flagged NOT enacted: C.100/C.101's `chainNormBelow 2 β`-at-cleared-β and `hlift`/`hunit` grades carry the same defect genre (OPEN-EVAL-ISO cluster, follow-up amendment) | ANORM, 2026-08-27 (verdict_ANORM.md) |
| A-I.4 | I.01 `NS7Termination` BODY (`leanspec/Leanspec/ChapI.lean`) | frozen complete-DVR bundle followed by the literal `True` matrix (signed/landed drift against I.01) | ONLY the body matrix re-signed at the landed A-C.6 `NS7TerminationStatementR` shape: uniformizer + irreducibility, monic squarefree `f`, infinite `DescentState` history, block divisibility and `DescentStepR` at every successor imply `False`; name/bundle/`Prop` kind and every other signed declaration byte-unchanged. **Tier-1-adjacent cite row for the owner's read:** this is the I.01 binding for C.94's allowlisted `agnprw_termination : NS7TerminationStatementR`; landed `ns7Termination_of_cite` is the cite-consuming discharge | leanspec §4 gate item (14): `NS7Termination ↔ Tower.NS7TerminationStatementR := Iff.rfl` plus explicit `CapstoneHypotheses.ns7` projection; `#axiom_core` gates for both carriers pass; `lake env lean Leanspec/ChapI.lean` exit 0 | NS7R, 2026-08-27 (CHFD stop-the-line signed/landed reconciliation; Tier 1 row 1 linkage) |
| A-C.20 | C.34 `exists_dv_residual_dissection`; C.35 `blockFactor` / landed `BlockFrontier` | load-bearing block candidates used naked `HasLabel`; `blockFactor` maximized over the defective `D′ ∤ deg` stratum | every C.34 block clause and the C.35 selector/frontier maximality quantifier use `HasLabel L fS ∧ (F.e₁*F.f₁) ∣ fS.natDegree`; pre-amendment selector retained only as named `blockFactorLeaky` for historical refutation rows | `verification/dv_hensel_cert.py` Sec. 5 (2832 checks, 0 FAIL): concrete ℤ₂ polynomial has two unpinned labelled splits; repaired `C34.exists_dv_residual_dissection_of_frontier` and `C35.blockFactor_spec_of_frontier` re-elaborate Lean-core; `scratch/AMND_check.lean` pins the amended types | AMND, 2026-08-26 (MHENS stop-the-line finding; `HENSEL_ENGINE_2026-08-26.md` §7) |
| A-C.19 | C.74 `shadow_persistence` | no degree restriction on `g`; `g=(composedKey T)^μ₂` makes every budget digit below `μ₂` vanish while `TouchCert` pins the contradictory exact height | insert `(hdeg : g.natDegree < μ₂ * T.D₂)` before `hbudget` | `verification/rb3_bridge_cert.py` (210 PASS / 0 FAIL) + `RB3_BRIDGE_PROOF_2026-08-26.md` §1; repaired closure elaborates in `scratch/DEC4_check.lean` and `scratch/AMND_check.lean` | AMND, 2026-08-26 (MBRIDGE stop-the-line finding) |
| A-C.18 | C.92 `chainNormBelow` / `thetaRatio`; C.99–C.101 and C.89/C.90 consumers | source `n̂₂` was read as current-index `chainNorm 2`; at grade 21 this equals `keyAt 2`, falsifying strict degree `< keyAt 2` | add `chainNormBelow I r k := chainNorm I (r-1) k`; re-index `thetaRatio`, B-1 supplies, B′/B formulas, `GENTOW5W`, both C.89 paths, and C.90's coupled recipe | `scratch/DEC3_probe.lean`: `towerNorm 1 21` realizes `16·keyAt 1`, degree `2<4`, while index 2 equals `keyAt 2`; `scratch/AMND_check.lean` pins the successor-index equation; all landed consumers re-proved Lean-core | AMND, 2026-08-26 (DEC3R/MU3_CAMPAIGN §4) |
| A-C.17 | C.99 `gentow2_Bpp`; both C.89 paths | unconditional `hprev : I.PrevGrade u'`, impossible at the honest μ₃ target `f'=1,u'=21` under full-current clearing | `(hprev : 2 ≤ f' → I.PrevGrade u')`; landed engines split `f'=1` (cancellation only) from `2≤f'` (existing graded multiplication induction) | `scratch/DEC3_probe.lean`: `Coprime 21 2 ∧ ¬ PrevGrade 21` and `ratio_power_one`; amended `C99r.gentow2_Bpp`, `C107ac14.gentow5w_two`, and conditional `gentow5w_one_shape_of_letter_tie` all Lean-core | AMND, 2026-08-26 (DEC3R/MU3_CAMPAIGN §3) |
| A-I.3 | I.10a `CanonicalLadderConfig` / I.10b `CanonicalDeepTwistConfig` socket BODIES (`leanspec/Leanspec/ChapI.lean`), + the Stage-2 leanfinal landing of I.10/I.15–I.18 (`leanfinal/Uniformity/ChapI/I10_I15_I18.lean`) | both bodies the literal `True` placeholder (A-I.1's blocked-until-resolution state), so the guarded Display-A fields were equivalent to the unguarded REFUTED forms and `CapstoneHypotheses n` was PROVABLY uninhabited — I.15–I.18 fired from `False` (defect I-D13, leanspec §4 gate item (10)) | bodies TYPED at chapter C's enacted PK-12 carriers: I.10a := `C130s18.CanonicalLadderConfigData.{0, uG, uKt, uL}`, I.10b := `CanonicalLadderConfig … ∧ ∃ dom dvr, C130s18.DeepTwistRealizationData.{0, uG, uKt, uL} …` (frozen refinement shape kept; ambient universe pinned to `0` — forced, a `Prop` can't quantify universes and a new socket universe would un-elaborate the byte-frozen field list; disclosed in-file). Field list, `UniformityStatement`, I.15–I.18 statements BYTE-UNCHANGED. **I-D13 LIFTED** (block now OPEN — not provably empty, not claimed inhabited). ⚠ **This IS the owner-gate-(a) narrowing A-I.1 pre-flagged**: the guarded fields are now strictly weaker than the 0e reading (weaker hypothesis ⟹ stronger I.15–I.18) | leanspec §4 gate: item (10) STRUCK with text preserved (no longer elaborates — the point), item (11) retired to its surviving weakening direction, NEW items (15) `Iff.rfl` body pins + (16)/(17) S2 non-vacuity teeth (`C130sg.s2Frontier_ladder_socket`/`_deepTwist_socket`, conditional on exactly `w : S2LevelOneThreshold` + `IsAdicComplete`); non-swallowing fence checked against C130s18's mechanized audit (`arenaAgreement_not_uniform`/`embeddedValue_not_uniform`); I.15–I.18 landed REAL in leanfinal, Lean-core footprints (`verdict_RSGN.md`) | RSGN, 2026-08-26 (re-sign target set by PK12's "Notes for later nodes" + PACKAGING_ROUTE §4.4) |
| A-C.16 | C.91 `gentow5_selffeed` | naked `hj` absent (live-range fence missing) | `(hj : j < μ)` restored | `C150rb2.gentow5_selffeed_false` (naked form FALSE: `j=μ`,`u=0` satisfies `hOnSide`, forces `ℓE<0`); repaired form PROVED as `gentow5_selffeed_resigned` (floor telescoping) | RB2, 2026-08-25 |
| A-C.15 | C.69 `ClassSizeSupplyData.read_form` | quoted the naked C.64 law with no riders | gains `HasLabel L (blockFactor L f)` + `(F.e₁*F.f₁) ∣ (blockFactor L f).natDegree` riders | `C64.lean`'s `blockDegEq_false` (3=2 at `(s2Frame,L₀,g₀)`/ℤ_[2]); repaired shape = C.35's landed `hasLabel_natDegree_eq_of_dvd`, proved as `blockDeg_eq_resigned` | AC15C27, 2026-08-25 (DEC1's stop-the-line finding; A-C.10 had fixed the standalone axiom but missed this bundle field) |
| A-C.14 | C.89 `gentow5w_two`/`gentow5w_one_shape`, C.90 `gentow5_key_certificate` | no C.99 supply cascade; C.90 missing `hr3`, the `ψ'`↔`Φnext` recipe coupling, and `hadm`'s 3rd conjunct | four supply binders (`hprev`,`hnorm`,`hnormdeg`,`hnormz`) added to both C.89 axioms; C.90 gains `hr3 : 3 ≤ r`, `(c, hψc, hres)` recipe-coupling binders, and `hadm`'s `coeff 0 ≠ 0` conjunct | `C107ac14.lean`: `gentow5w_two` and `gentow5_key_certificate` LANDED Lean-core; `gentow5w_one_shape` still HOLD (OPEN-LETTERS) with a conditional form landed; pre-repair instantiation `ψ' := X^f'` is shown to derive `False` at `f'≥2` | AC14, 2026-08-25 (predicted verbatim by GENTOW2_ADJUDICATION's "cascade flag") |
| A-C.13 | C.92 `FGMNCalculus`/`FGMNSourceLaws.graded_mul`/`Rgr_mul`, C.99 `gentow2_Bpp` | U9/A-C.11 had removed the `β∈Γ_{r-1}` premise as "automatic after clearing" | `PrevGrade : ℕ → Prop` field restored; `graded_mul`/`Rgr_mul` regain `S.PrevGrade β' →`; `gentow2_Bpp` gains explicit `(hprev : I.PrevGrade u')` | `docs/in-progress/COR412_ADJUDICATION_2026-08-25.md` (print-read: Cor 4.12(2) proof line "because `s_r(β)=0`"); RP23's tooth machine-refutes the unrestricted plain law at odd×odd; 5 files rebuilt green (`C130fg/C130la/C130pk/C99r.lean` + leanspec), all `#print axioms` Lean-core | AC13, 2026-08-25, correcting U9 Q3 |
| A-I.2 | I.10b `CanonicalDeepTwistConfig`/`deepTwist`, E.63 `DeepTwistConjunct` | unbounded `∀ i, 3 ≤ i → …` (E.63); I.10 sockets had no finite-tower-depth binder | (a) interpretive rider: `K_t` is the terminal common receiver, not literal per-level `K_i` membership; (b) `DeepTwistConjunct r`/`DeepTwistConjunctLive` bounded to `3 ≤ i ∧ i < r`, with an `r` binder threaded through I.10b/E.63/the I.24 core registry | `leanfinal/Uniformity/ChapI/I10FreezeV2.lean` (`DeepTwistConjunctLive`, `deepTwistConjunctLive_iff` — an `Iff.rfl` anti-drift pin); `lake build Uniformity.ChapI` green (8686 jobs) | F3 (proposal, 2026-08-24) → enacted in `leanspec/Leanspec/ChapI.lean:625,657,734-743` + `ChapE.lean:1406` |
| A-C.12 | C.99 `gentow2_Bpp`/`theta_letter_valued`, C.101 `gentow2_B`, C.103 `gentow2_A`, C.85 `theta_dictionary` | C.99/101/103 omitted source-stated supply data (B-1 normalizer families, recipe data, admissibility antecedent); `theta_dictionary` asserted an untied `θ(s+t)` cocycle | RE-SIGN (a) on C.99/101/103 (supply hypotheses added; `gentow2_Bpp` machine-certified provable from the class fields alone); `theta_dictionary` RE-SIGN with height-tied quantifiers (still open); C.100/C.102/C.88 stay HOLD (need OPEN-EVAL-ISO/OPEN-DICT-2/OPEN-LETTERS) | `docs/in-progress/GENTOW2_ADJUDICATION_2026-08-24.md` §0 verdict table + §9 diff; `leanfinal/scratch/GSW_check.lean` (`gentow2_Bpp_resigned_provable`, Lean-core) | GSW, 2026-08-24 |
| A-C.11 | leanspec NODE C.92 `FGMNCalculus` (whole class body) | provisional class body (U7 §9's open questions Q1–Q7) | exact replacement per U9's 7 decisions: class = calculus of the next augmented valuation (Q1); `Rgr`=coeff-0 of the *polynomial*-valued `R_{r+1,β}` (Q2); `PrevGrade` removed as redundant (Q3 — **later reversed by A-C.13**); class kept live at `r=0` (Q4); Gauss cite pinned (Q5); C.100 re-scoped to `deg g < D₂`, `hfree` deleted (Q6); C.90 stays OPEN-LEVEL-GENERAL, no fake field added (Q7) | `docs/in-progress/FGMN_ADJUDICATION_2026-08-24.md` §1 verdict table + §11 unified diff | U9, 2026-08-24 |
| A-C.10 | 5 axioms: `slot2_exact` (C.27), `blockDeg_eq` (C.64), the C.67 mult-tie bundle, and two `C.80`-family tower clauses | naked forms with no label/divisibility/purity riders | riders restored (`hx : IsPure X g F.h F.e₁` on C.27; `HasLabel`+divisibility riders on C.64/C.67/C.80's two clauses); each dropped conjunct survives only as a separate floor-level theorem | `ChapC/C27.lean:slot2_exact_false`, `C64.lean:blockDegEq_false` (3=2), `C67.lean:multiplicityTie_false` (3=2), `C80.lean:partialProjection_false`/`tiePurePower_false` (5=4) — all machine-refuted at concrete probe frames over ℤ_[2] | 2026-08-24 (5 leanspec sites, `ChapC.lean:879,2286,2331,3549,3624`) |
| A-E.7 | E.57 `BlockSplitTarget` | `hpart : True` | `hpart : Nonempty (PartitionLeg I)` | landed `PartitionLeg` + proof `block_split_of_partitionLeg`, `leanfinal/Uniformity/ChapE/E57p.lean`; `lake build Leanspec.ChapE` green (9240 jobs); field-list diffed byte-for-byte, empty diff | AE7, 2026-08-24 |
| SG-0 / PK-1..6 | S2 `ChainRealization`/`FGMNSourceData`/`FGMNSourceLaws` carrier + `fgmnCalculusOf`, ~12 files (`C130fg/in/k2/th/pt/cr/ar/vw/pk/s17/s18/sg.lean`) | `ChainRealization` carried `(e' f' u')` params + `fgmn`/`fgmnLaws`/`grade_compat`/`letter_compat` fields typed at the refuted depth-two shape `(2,1,5)`; `FGMNCalculus` had NO inhabitant anywhere | **FULLY ENACTED 2026-08-26 (unit PK12, superseding U15's proposal and SG01's conditional discharge)**: `ChainRealization` retyped to the split node (params/fields removed, `node : SplitNodePointSource …` added); `C130pk.fgmn_model_calculus_nonempty` (factored map) + ★ `C130sg.s2_calculus_discharge : Nonempty (FGMNCalculus (s2DepthOne h2 hq) 2 1 5)` — **UNCONDITIONAL**, no `w` witness needed any more (SG01's version was conditional on one datum `w`) | `lake build Uniformity.ChapC` (8804 jobs) + `Uniformity.ChapI` (8710 jobs) + leanspec `ChapC`/`ChapI` (9278 jobs) all green; 12 touched/new files' `#print axioms` all Lean-core; both non-swallowing grep-teeth pass (exit 1) | U15 (design, 2026-08-25) → PK-1/PK-2 (folding in PK-3..6) enacted by unit **PK12**, 2026-08-26 |

**Open review-list rows carried forward from PACKAGING_ROUTE §6 (U15), not yet amendments — read alongside the table above:**
1. Depth-2 parameter: every mention of the S2 depth-two class at `(2,1,5)` (CC-17's typing, `LegacyFGMN`, SG-1's `calculusNonempty` target) should really be `(2,1,21)` — confirmed by 3 independent legs, but not yet re-signed.
2. The `hprev`/`hcop` clearing tension: `I.PrevGrade u'` (A-C.13) is unsatisfiable at any evenness-realized instance passing `hcop` — `¬S2PrevGrade 5 ∧ ¬S2PrevGrade 21`, machine-pinned. Which clearing the future μ₃ operators use (OPEN-DICT-2) must be decided before Route-B. A candidate unblock: fence `hprev` by `2 ≤ f'` (A-C.15-style re-sign).
3. Leanspec NODE C.90's `gentow5_key_certificate` binds ONE `(e',f',u')` triple across all truncations `i ≤ r`, but each truncation's honest data differ — satisfiable only for uniform-data towers; needs a per-level family at its next re-sign.
4. `gentow5w_one_shape`'s remaining holds: `htie` (OPEN-LETTERS) and `hprev` — depth-1 consumer cannot fire until both move.

---

## Tier 3 — New general statements (reusable lemma banks), grouped by file

These are net-new mathematical content the fleet flagged itself ("Flagged for human
review — trust boundary — new statements") because Lean-checked ≠ correctly-modeled;
Lean verifies the proof, not that the statement says what the unit thinks it says.

| file | unit | flagged declarations | one-line what-it-is |
|---|---|---|---|
| `leanfinal/Uniformity/ChapI/IFC3.lean` | GCW1 (2026-08-27) | `GC13Wiring` + `WiredGentowDemand` (both SIGNED at A-I.5 — Tier 2 row above), `gc13Wiring_vacuous_of_f'_zero`, `gentowWWindow_of_wiring`, `s2Mu3_gentowWWindow_of_wiring`, `s2_wiring_forces_read_one`, `s2_wiring_refuted_at_landed_exports`, `wiredGentowDemand_iff_of_f'_zero` | the signed GC-13 read/normalizer law, the wired sitewise DT0 demand, the `hread`→law translation upgrade, and the fences (law vacuous at `f' = 0`; law refuted at the landed zero-read witness) — statement carriers + bookkeeping, no open mathematics proved |
| `leanfinal/Uniformity/ChapC/C130nv2.lean` | NV2 | `stageHeight_le_modByMonic`/`_eq_min_mod_div`, `stageHeight_dev_mul_eq`, `le_line_dev_term`/`lt_line_dev_term`, `dvSupp_add_le_dvSupp_mul` | the one-step MacLane expansion (`dv(w)=min(dv(w%ₘΦ′), dv(w/ₘΦ′)+dv(Φ′))`) + both-direction convolution-term estimates + the ≥-half of the level-two product law |
| `leanfinal/Uniformity/ChapC/C130nv3.lean` | NV3 | `stageHeight_dev_endpoint_le_of_surv` (generic, conditional on `hsurv`), `s2_stageHeight_mul_modByMonic_le` (S2 survival core, unconditional), `s2_dvSupp_mul`, `s2_mul_modByMonic` | the "survival core": does the unique lowest convolution term survive residual cancellation. Generic case genuinely open (needs `hresirr`, a reducible-residual counterexample is given); S2 case proved unconditionally by parity case-split |
| `leanfinal/Uniformity/ChapC/C130rp2.lean` | RP23 | `twistRead_add_of_le`, `twistRead_eq_zero_of_lt`, `dev_mul_endpoint_split` | generic-frame, strict-admissibility read/development lemmas (not S2-specific) |
| `leanfinal/Uniformity/ChapC/C130rp4.lean` | RP4 | `dev_mul_conv_split`, `le_stageHeight_finsetSum`, `twistRead_finsetSum_of_le`, `ceil_le_dvHgt_of_le_dvSupp`, `le_stageHeight_mul_modByMonic`, `s2_dev_mul_one` | convolution/finite-sum height bounds feeding the graded residual machinery |
| `leanfinal/Uniformity/ChapC/C130rp6.lean` | RP56 | `S2PrevGrade` (evenness realization — carried as a def until OPEN-DICT-2 proves the divisibility test), `exists_slotOnGrade_of_exact`, `s2GradedRes_ne_zero_of_exact`, 3 assembly theorems | the S2 realization of A-C.13's new `PrevGrade` field, plus the graded-residual nonvanishing/assembly bank |
| `leanfinal/Uniformity/ChapC/C130rp8.lean` | RP78 | `s2GradeOf`, `s2NormRes` (certified `= ⌊s(g)/e₂⌋`), `s2_graded_scalar_nonzero` (+worker/fills), 2 endpoint pins, a degree law | the normalized-residual scalar and its nonvanishing |
| `leanfinal/Uniformity/ChapC/C130rp9.lean` | RP910 | `s2GradedRes_natTrailingDegree_mul`, `dvSideMin_mul_of_exact`, `s2NormRes_mul`, FD-0/A-C.11 shape pins | Cor 4.12(3)-shaped exact multiplicativity at the normalized residual |
| `leanfinal/Uniformity/ChapC/C130kp0.lean` | KP0 | 5 predicate definitions (incl. "prime-ideal-through-divisibility" rendering), the graded-piece characterization, the odd-carry engine | new predicates characterizing the S2 key polynomial's minimality shape |
| `leanfinal/Uniformity/ChapC/C130kp1.lean` | KP12 | the backward translation + fences, carried-key characterization, KP-2's statement, the naked-KP-2 refutation, the named-gap discharge | KP-2's μ-minimality-at-recipe-degree bank |
| `leanfinal/Uniformity/ChapC/C130kp3.lean` | KP34 | KP-3's honest hypothesis set, the trichotomy teeth, `s2_key_criterion`, "the crown", the pin's `keyAt 1` anchoring | the S2 key-polynomial irreducibility criterion (trichotomy form) |
| `leanfinal/Uniformity/ChapC/C130kp5.lean` | KP56 | `s2Key_eisenstein_irreducible`, `s2Key_irreducible_map_fractionRing`, the `…cX` generalization bank, `s2NormRes_composedKey`, `s2KeyPoly_composedKey` | Eisenstein-criterion irreducibility for the S2 key polynomial, ported to the composed/tower setting |
| `leanfinal/Uniformity/ChapC/C130fd0.lean` | FD01 | `s2_key_irreducible` (**published Lemma 1.8's conclusion, new direct proof at KP-0's predicates, no cite**), `g₈` adapter statements, both depth-two refutation statements, `s2SourceLaws_depthTwo_unsatisfiable`, `s2DepthOne`/`s2DepthOne_fld_eq` truncation typing, the vacuous discharge of field 6 at `r=1`, 2 FD-1 statements | the S2 depth-one `FGMNSourceData` instance + the machine-refutation that depth-TWO is unsatisfiable at the landed operators (which forced SG-0's anchor workaround, Tier 2 row) |
| `leanfinal/Uniformity/ChapC/C130sg.lean` | SG01 (2026-08-25), superseded by PK12 (2026-08-26) | `S2LevelOneThreshold` + its disclosure tooth, `s2SplitNode`/`s2Frontier` instance statements, `s2Frontier_legacyEvaluation_isEmpty`, `s2LegacyFGMN_landed_empty`, `S2LevelOneThreshold.ofSplitNode`, the `LegacyFGMN` quarantine typing | SG01's FIRST inhabitant of the S2 split-node frontier was conditional on one datum `w`; PK12 (Tier 2 SG-0/PK row) replaced the relevant stage with ★ `s2AnchorCalculus`/`s2_calculus_discharge` — **UNCONDITIONAL**. Both emptiness theorems above are retained statement-verbatim and still worth a look |
| `leanfinal/Uniformity/ChapC/C130pk.lean`, `C130fg.lean`, `C130s17.lean`, `C130s18.lean` (+ 8 more) | PK12 (2026-08-26) | `fgmnCalculusOf`/`fgmn_model_calculus_nonempty` (factored packaging map), ★ `s2_calculus_discharge` (see above), the retyped `ChainRealization`/`RealizedInput`/`SplitNodePointSource` carrier shapes, C130s18's re-premised socket bodies (`∃ (E : Type uE) (fE : Field E) …` replacing the `(e' f' u')` existential) | the full packaging-route enactment (Tier 2 row) — flagged here because 12 files' worth of new/retyped general statements landed in one unit; `runs/wave-b/verdict_PK12.md` §"Sites the diff didn't cover" also records 4 minimal-repair judgment calls (named instance binders, universe pinning, `C130st.lean` left un-retyped by import-direction necessity) worth a skim |
| `leanfinal/Uniformity/ChapC/C107ac14.lean` | AC14 | (covered under Tier 2 A-C.14; landing proofs, not new statements) | — |
| `leanfinal/scratch/U15_check.lean` (superseded, no longer elaborates) | U15 | `fgmnCalculusOf`, `s2AnchorCalculus`/`s2_calculus_discharge`, `ChainRealizationV2`/`V2b`/`RealizedInputV2`, 4 floor/PrevGrade numeral pins | the proposed PK-1..3 retyped shapes, pre-registered before enactment. PK12's own verdict notes this scratch file "will no longer elaborate against the enacted corpus" — historical record only, cross-check against the landed row above rather than this file |
| `leanfinal/Uniformity/ChapC/C84d.lean` | TDW | (module docstring's "⚠ mandatory-review block") | the telescope/junk-value conventions for `TowerDatum.deepTower`'s off-range indices (rungs outside `{1,2}`); `hπ` is carried but unused, flagged as the same pattern C.97 already established |

---

*Compiled 2026-08-26 by unit RQ. Read-only sweep — no files other than this one and
`runs/wave-b/verdict_RQ.md` were written; nothing was committed.*
