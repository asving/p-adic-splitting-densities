# THE HARDEST-FIRST DISPATCH ORDER (Asvin directive, 2026-08-08 / wallclock 2026-08-02)

Directive: "structure the order of filling in the lean proof so that you do
the hardest things first, the ones that might cause the deepest changes to
the architecture of the proof." Ranking = architectural blast radius (what
must change if the item is false/unprovable as stated); numerics-first
wherever truth is uncertain (the standing concrete-trace discipline).

## Tier 1 — core-corpus redesign risk (probe FIRST)
1. SITE-EXH ADJUDICATION PROBE: attempt the closed FiredLift datatype at the
   engine layer; verdict = closable-as-is / needs-extension (the C-e fence
   exhibit already shows real towers unrecordable in lawful full runs — the
   run architecture itself is in question).
2. V9 e′≥2 RULING: the parked FALSE-SUSPECT legs (HK52_stretchGate compiled
   obstruction; the ν_{i+1}.e stretch). Countermodel or repair; decides
   whether HC2's K1 vertex-pin keying changes.

## Tier 2 — campaign re-architecture risk
3. B2D hard holes NOW: TD-0/TD-3 (DigitsProdLaw at the skeletal ledger — can
   fail at shallow heights; failure ⇒ TransDeep unsatisfiable ⇒ (FRESH)
   re-architecture) + CL5 (C6_alphabetCard.lean:129 fullness — new math;
   failure ⇒ counting-face re-price).
4. GR seam suppliers: hscale/hψ0 (GR-0's two displayed hypotheses) — does the
   history-level supplier exist? No ⇒ GRADED-READ is permanently a row.

## Tier 3 — dictionary re-route risk (falsifier-FIRST, before any proof)
5. Harness extensions: (a) the corrected-ǫ twist law at e₀ ≥ 2 vs GMN
   (untested stratum); (b) the D-ITER iterated composite beyond the last-read
   layer (T10's gap). RED here re-routes (H1)(a) — find out for the cost of
   a numerics run.

## Tier 4 — bounded/local (background only; never jumps the queue)
H6-C2 GDOrder1 typeclass repair · (ADM) track · (H8′) interfaces · readsOf
recentering witness · U10/III-U3b (in flight) · TR-4 family · mop-ups.

Standing rule going forward: any new unit is slotted by blast radius before
dispatch; a Tier-1/2 RED verdict PAUSES lower-tier dispatching until the
architecture question is adjudicated.
