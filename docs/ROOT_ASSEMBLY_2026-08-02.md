# ROOT ASSEMBLY — the uniformity theorem, composed from the leaves (unit ROOT, 2026-08-02)

**Phase C spine.** This is the document the end-to-end verifiers read: the uniformity
theorem stated in Asvin's scaffold form, its proof as a LINEAR COMPOSITION of the
Phase-B leaves, and the COMPLETE conditionality ledger. It is built at the wave-10
state of the campaign (ledger `lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md` §"WAVE 10
+ PASS-2 ADJUDICATIONS", HEAD d730669) from `docs/MATH_COMPLETION_TREE_2026-08-01.md`
(+ DELTAs 1–3, whose corrected readings govern), the per-leaf attempt/verify records in
`lean/notes/openmath/`, and the literature unit `lean/notes/openmath/LITUNIT_2026-08-01.md`.

**Honesty headline (read first).** The theorem below is NOT yet a verified complete
proof, and this document does not claim it is. **REVISION 1 applied** (appendix §7,
2026-08-02): the Phase C end-to-end pass 1 (VC1, Codex, fresh context;
`lean/notes/openmath/VC1_root.jsonl`) returned **COMPOSITION-GAPS** (blockers 1–8,
advisories 9–11); every finding is dispositioned in §7 and repaired in place below, and
statuses are synced to the 2026-08-02 harvest (ledger
`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md` final sections, commit c43b13b).
**REVISION 2 applied** (appendix §8): the end-to-end pass 2 (VC2, Codex, fresh
context; `lean/notes/openmath/VC2_root.jsonl`) returned **COMPOSITION-GAPS** (5
blockers V2-1/2/4/5/6, 2 advisories V2-3/7); every finding is dispositioned in §8 and
repaired in place below — headline repairs: the clause-(R) self-loop formula is now
quantified e ≥ 2 with the forcing proof DISPLAYED (the (SL≥2) display at Step 5), and
(H4) is SPLIT into (H4a) = (R1)–(R4) (consumed by clause (R) too, not (UB)-only) +
(H4b) = (UB-X). **REVISION 3 applied** (appendix §9): the end-to-end pass 3 (VC3,
Fable, fresh context — the model-diverse pass;
`lean/notes/openmath/VC3_root_fable_verify.md`) returned **COMPOSITION-GAPS** (4
blockers B1–B4, 4 advisories A5–A8 — ALL attribution/ledger-integrity; VC3 explicitly
CONFIRMED the composition's mathematics: acyclicity edge-by-edge incl. 17 ≺ 18 ≺ 18b,
the (ROOT-C)-hypotheses ↔ §3.1-[M]-rows bijection, the (SL≥2) display, the (S3)
split); every finding is dispositioned in §9 and repaired in place below, and
statuses are synced to the 2026-08-02-night harvest (ledger 7b28562: **VERIFIED = 6**).
**REVISION 4 applied** (appendix §10): the end-to-end pass 4 (VC4, Codex, fresh
context; `lean/notes/openmath/VC4_root.jsonl`) returned **COMPOSITION-GAPS** (4
blockers + 1 advisory, findings VC4-1..VC4-5 — again all attribution/ledger
integrity; VC4's own pass-list re-confirmed acyclicity incl. 17 ≺ 18 ≺ 18b, (SL≥2),
and the repaired Step-19 Σ_τ R_τ = 1 derivation); every finding is dispositioned in
§10 and repaired in place below — headline repairs: a NEW §3.1 [M] row **(PACK)**
(D-11's open clause 4, the actual-pack correspondence) carried as hypothesis
**(H5)**; clause (SQ)'s attribution corrected DOWNWARD (unconditional — a favorable
honesty fix); the (ADM) row rewritten to its single operative FULL-attainment form
(O-9 r4); and statuses synced to the confirming-passes harvest (**VERIFIED = 7**).
**REVISION 5 applied** (appendix §11): the end-to-end pass 5 (VC5, Codex, fresh
context; `lean/notes/openmath/VC5_root.jsonl`) returned **COMPOSITION-GAPS** (2
blockers + 1 advisory, findings VC5-1..VC5-3 — the two blockers ONE genre: rows this
document itself classified [T] are OPEN PROPOSITIONS consumed on clause (R)'s
mandatory path, and under the ledger's own classification rule placing them in [T]
does not supply them); repaired by PROMOTING them — CU-2t's three terminal rows
(T-DEC)/(T-READ)/(T-VERD) and D-11's widened-charge item (vii) — to a NEW §3.1 [M]
row group carried as hypothesis **(ROOT-C)(H6)**, and statuses synced to the
O-9/CU-2t-verified harvest (ledger 6a7593b: **VERIFIED = 9**).
**REVISION 6 applied** (appendix §12): the end-to-end pass 6 (VC6, Fable, fresh
context — the second model-diverse pass;
`lean/notes/openmath/VC6_root_fable_verify.md`) returned **COMPOSITION-GAPS** (3
blockers F-1/F-2/F-3 + 6 advisories A-4..A-9 — the blockers ALL the VC5 genre walked
to completion: open propositions consumed on clause (R)'s mandatory path still
classified [T]; VC6's own pass-list re-confirmed acyclicity over all 20 steps, the
NINE-row bijection, both unconditional clauses, the Σ_τ R_τ = 1 derivation, (SL≥2),
and the leaf-#8 interlock); every finding is dispositioned in §12 and repaired in
place — headline repairs: **(H1) ENLARGED to the graded-dictionary group** (GD-1's
(KP)/(KPirr) clauses beyond order 1 and the consumed GD-6 conformance instances join
(GR-B) — findings F-2/F-3), a NEW hypothesis **(H7)** = the (I-τ)/K-LOC
sibling-interface engine-stratum conformance pair (the LAST of O-11 r2's declared
D-a/D-b/D-c triple adjudicated hypothesis-grade — finding F-1), and the **T-U
§U.SQ.6 correspondence table EXECUTED** (NEW §3.4 — the standing A8 → VC5-3 → A-9
audit debt converted from asserted to displayed).
**REVISION 7 applied** (appendix §13): the end-to-end pass 7 (VC7, Codex, fresh
context; `lean/notes/openmath/VC7_root_report.md`) returned **COMPOSITION-GAPS**
(2 blockers + 3 advisories — the blockers BOTH attribution/warrant findings at the
two REVISION-6 A-6 adjudications VC6 had flagged forward; VC7's findings 6–11
CONFIRMED the composition: the §3.4 T-U tables genuine incl. the three flagged
adjudications, the (H1) enlargement + (H7) sound, the TEN-row bijection correct,
the walk proving (ROOT-C) quantifier-for-quantifier, acyclicity passing); every
finding is dispositioned in §13 and repaired in place — headline repairs: **(H1)
gains the displayed FOURTH member (d)** (the degree-g > 1 KEY1 generalization,
which Step 6's all-site theorems consume at order-≥ 2 reads — finding 1) and
**(H5) = (PACK) is ENLARGED to carry the three pack row-laws as explicit clauses
(PACK-i/ii/iii)** (rsh_interp / r_bdd / wsh_ok at the 𝔅_n rows — NOT inferable
from the correspondence alone; finding 2).
**REVISION 8 applied** (appendix §14): the end-to-end pass 8 (VC8, Codex, fresh
context; `lean/notes/openmath/VC8_root_report.md`) returned **COMPOSITION-GAPS**
(1 blocker + 1 advisory — the NARROWEST residue of the eight passes, both findings
at the REVISION-7 repairs themselves: the (H1)(d) display's "key has degree > 1"
quantifier rested on an UNPROVEN identification of key degree with read order,
and several summaries lumped all three PACK row-laws into "K2/evaluation glue";
VC8 confirmed all three r8 advisory repairs, the TEN-row count, the quantifier
walk, acyclicity, and no regression on the VC7-confirmed items); both findings
are dispositioned in §14 and repaired in place — headline repairs: **(H1)(d)
REQUANTIFIED to READ ORDER** (the member is now the order-≥ 2 KEY1 block:
membership by OM order ≥ 2 alone, the current key's degree irrelevant —
degree-1-key e·g = 1 refinement chains INCLUDED — so (d) covers exactly the
complement of the proved order-≤ 1 slice and total coverage consumes NO
key-degree/order equivalence — finding VC8-1) and the **PACK K2/K3
sub-attribution synced** (PACK-i/ii enter at Step 18's K2 evaluation glue;
PACK-iii is the membership input to Step 18's K3 evaluation ring-hom, with D-6 —
finding VC8-2).
**ACCEPTANCE at passes 9–10 (2026-08-03) + REVISION 9 applied** (appendix §15):
the end-to-end passes 9 and 10 (VC9, Codex, fresh context,
`lean/notes/openmath/VC9_root_report.md`; VC10, Fable, fresh context — the
model-diverse pass, `lean/notes/openmath/VC10_fable_report.md`) BOTH returned
**COMPOSITION-SOUND with ZERO blockers** on the IDENTICAL REVISION-8 text — the
tree §4 Root criterion's ≥ 2 consecutive clean model-diverse passes is MET:
**the composition is ACCEPTED at (ROOT-C)'s displayed conditional scope**
(milestone addendum, `docs/PROJECT_STATE.md` 2026-08-03). Acceptance certifies
the composition — the walk, the ten-row bijection, the per-clause attribution,
acyclicity — NOT the discharge of §3's ledger: the theorem as proved remains the
conditional (ROOT-C). VC10's three ADVISORIES — display hygiene only — are
dispositioned in §15 and repaired in place at REVISION 9 (post-acceptance: no
[M] member, no step content, no clause attribution changed; the
consecutive-clean counter is TWO and STAYS TWO — advisories do not reset it).
What exists is: an acyclic composition (acyclicity now holds edge-by-edge: the Step-1 edge
REMOVED by re-pointing to Dfloor's self-contained Appendix A, the D-11/O-11 cycle
STRATIFIED as Step 17 ≺ Step 18 ≺ Step 18b) in which every step is a named leaf with an
on-file proof, NINE of the leaves VERIFIED through the campaign's two-pass
model-diverse bar (O-13, O-12, Dfloor, GD23, CU-1, O-10, O4T, O-9, CU-2t — the last
two adjudicated at the 6a7593b harvest after O-9's clean confirming pass 5 and the
CU-2t R4 spot-check, REVISION 5 status sync), the rest PROVED-UNVERIFIED with
their pending passes NAMED, and the genuinely open mathematics confined to §3.1: the
two heavy items — **(GR-B)** at OM orders ≥ 2 and **(FRESH)** — both pinned to printed
literature statements and priced at a single lemma layer [since REVISION 6, findings
F-2/F-3, (GR-B) HEADS the enlarged **(H1) graded-dictionary group**, whose displayed
members add GD-1's (KP)/(KPirr) clauses beyond order 1 and the consumed GD-6
run-decomposition-conformance instances — and, since REVISION 7, finding VC7-1,
the order-≥ 2 KEY1 block as member (d) (REQUANTIFIED to read order at REVISION 8,
finding VC8-1)], plus the smaller displayed
rows ((R1)–(R4) with the clause-(UB) residue (UB-X), (E-N) [D-12r, formerly listed
here, RECLASSIFIED [M] → the §4 literature-pin class at REVISION 14 (r15), executing
VC16 finding 2 — its residue is exactly the (HEN-LIFT)/(OM-SAT) pins], — added at
REVISION 2 — (ADM), O-9's census-admissibility hypothesis (FULL attainment since O-9
r4; automatic at order 0 only), — added at REVISION 4, finding VC4-3 — **(PACK)**,
the actual-pack correspondence (ENLARGED at REVISION 7, finding VC7-2, to carry
the three pack row-laws PACK-i/ii/iii, and AGAIN at REVISION 10, r11, the fourth,
(PACK-iv)), — added at REVISION 5, findings VC5-1/VC5-2 —
the **(H6) terminal-seam engine-conformance group** ((T-DEC)/(T-READ)/(T-VERD) +
D-11 item (vii)), — added at REVISION 6, finding F-1 — **(H7)**, the (I-τ)/K-LOC
sibling-interface conformance pair, and — added at REVISION 10, r11 — **(H8)** =
(K-CUT), the shallow-cut decomposition kernel, and **(H9)** = (NSF), the
ns-routing interface row [this headline enumeration run to the twelve-row scope
at REVISION 12 — the global enumeration audit; run to the ELEVEN-row scope at
REVISION 14 (r15) — the D-12r move]). **THE THEOREM AS
PRESENTLY PROVED is the conditional form (ROOT-C) displayed in §1**; the unconditional
(ROOT) is the TARGET, quantifier-for-quantifier NOT yet what the steps prove. §3 is the
complete list of everything the root currently rests on beyond literature; §4 is the
literature roster (the goal's conditionality class); §5 is the numerics corroboration
index (flags, never substitutes). The tree's §4 acceptance criterion applies verbatim:
PROVED-UNVERIFIED is never consumed by an acceptance claim, and this assembly becomes
THE COMPLETE PROOF only when §3's ledger empties into VERIFIED + LITERATURE-audited.

**SUSPENSION + REVISION 10 (r11) — the standing of 2026-08-04 (superseded the
acceptance language above as status; itself SUPERSEDED as status by the
RE-ACCEPTANCE + REVISION 13 paragraph below — kept as the revision record,
incl. its then-true "(NSF-KEY) KNOWN FALSE AT HEAD" display).** The Phase-C acceptance was **SUSPENDED**
(2026-08-03, stop-the-line; ledger entry at 44ce0a7; `docs/PROJECT_STATE.md`
addendum): the owed D-11 pass 3 (`D11_owedpass_2026-08-03.md`) returned CRITICAL,
and two MANDATORY steps (17, 18b) consumed the refuted layers AS PROVED. **REVISION
10 (§16, unit ROOT r11)** executes the repair design
(`lean/notes/openmath/R11_REPAIR_DESIGN_V5_2026-08-04.md`, Unit B): Steps 17/18b now
consume the REPAIRED true-scope D-11 leaf (`D11_rev4_2026-08-04.md` at REVISION 3,
twice hostile-passed: r4 pass 1 CRITICAL → folded; r4 pass 2 **GAPS-ONLY, all
design-§7 obligations adjudicated VALID as proofs**), and every premise the audit
found unpriced is a displayed [M] member: **(PACK-iv)** joins (H5) as its FOURTH
row-law, and TWO NEW rows enter §3.1 — **(H8) = (K-CUT)** and **(H9) = (NSF)**
(whose member (NSF-KEY) is KNOWN FALSE AT HEAD, retiring only by the design's
sign-off-gated Unit C). The §3.1 roster is now **TWELVE rows ↔ (H1)–(H9)**. The
suspension's defect is CURED at composition level — nothing refuted remains
consumed-as-proved — but **the acceptance is NOT re-earned: the consecutive-clean
counter is ZERO of 2**, and the end-to-end bar (two consecutive clean model-diverse
passes on identical text, fresh verifiers) must be re-run on THIS REVISION-10 text.
Until it passes, every acceptance statement in this document is historical record,
not current status.

**REVISION 11 applied (r12, appendix §17).** The end-to-end pass 11 (VC11, fresh
context; `lean/notes/openmath/VC11_report.md`) RAN on the REVISION-10 text and
returned **COMPOSITION-GAPS** (4 blockers + 1 advisory — ALL r11 RIPPLE-SYNC
defects: displays the r11 edits should have carried forward but did not — Step 19's
evaluation/(UB) summaries and final ∎ stopped at (H7)/PACK-i-iii, Step 16's claimed
exact (UB) scope was stale against §1, §3.4's item 12 routed the shallow-cut
content to a verified layer against the (H8) row, the 6(iii)–(ix)+7 residue list
omitted (H9), Step 18b's Consumes summary omitted (ADM) — while the TARGETED r11
repairs themselves were CONFIRMED: Step 17's per-clause measures, Step 18b at
(RE)/(PACK-iv), the twelve-row §3.1 ↔ (H1)–(H9) bijection, (NSF-KEY) marked KNOWN
FALSE AT HEAD with gated retirement, (0)/(SQ) unconditional, 17 ≺ 18 ≺ 18b). Every
finding is dispositioned at §17 and repaired in place — PURE DISPLAY SYNCS: no [M]
member, no step proof content, no leaf pointer changed. The standing is UNCHANGED:
the acceptance is NOT re-earned, **the consecutive-clean counter is ZERO of 2**,
and the bar re-runs (VC12) on THIS REVISION-11 text (charge at §17).

**REVISION 12 applied (r13, appendix §18).** The end-to-end pass 12 (VC12, fresh
context; `lean/notes/openmath/VC12_report.md`) RAN on the REVISION-11 text and
returned **COMPOSITION-SOUND, ZERO findings** — the consecutive-clean counter
reached ONE of 2. The model-diverse pass 13 (VC13, Fable, fresh context;
`lean/notes/openmath/VC13_fable_report.md`) then RAN on the same REVISION-11 text
and returned **COMPOSITION-GAPS** (2 blockers + 3 advisories — ALL the
stale-hypothesis-enumeration genre that survived the r11/r12 sync rounds: §1's own
(ROOT-C) display of (H5) stopped at PACK-i/ii/iii; Step 18's δ-stable note quoted
§1's clause-(R) set four groups short with a REVISION-4-era "five-hypothesis"
count; Step 17's Consumes omitted its own consumed (H9)/(PACK-iv); the (H8)
provenance label pointed at an O-11 "owed-pass" no §3.2 row recorded; the (AGR)
residue sentence elided CU-4's displayed [M] conditionality — while VC13 CONFIRMED
the walk, the twelve-row bijection, the seams, acyclicity, and (NSF-KEY)'s
never-consumed-as-true discipline). **The counter RESETS to ZERO of 2.** Every
finding is dispositioned at §18 and repaired in place — pure display syncs — and,
to KILL THE CLASS, §18 records a GLOBAL ENUMERATION AUDIT: every operative
(§§0–6 + §3.4) display that enumerates hypothesis groups/members or counts them
was checked against §1's current display and synced; VC14 re-runs the bar on THIS
REVISION-12 text (charge at §18).

**RE-ACCEPTANCE at passes 14–15 + REVISION 13 applied (r14, appendix §19) —
superseded as CURRENT standing by the REVISION 14 paragraph below.** VC14 (Codex,
fresh context; `lean/notes/openmath/VC14_report.md`)
and VC15 (Fable, fresh context — the model-diverse pass;
`lean/notes/openmath/VC15_fable_report.md`) BOTH RAN on the IDENTICAL REVISION-12
text and BOTH returned **COMPOSITION-SOUND** (VC14 clean, zero regressions, the §18
enumeration-audit claim tested and held; VC15 ZERO blockers with an explicit null
result on the blocker hunt, 4 display-hygiene advisories): the tree §4 Root
criterion (≥ 2 consecutive clean model-diverse passes, fresh verifiers) is MET
AGAIN — **the composition is RE-ACCEPTED at (ROOT-C)'s twelve-row displayed
conditional scope**, closing the suspension arc (refutation caught → leaf repaired
at true scope → composition re-pointed → bar re-earned). REVISION 13 (§19) is the
post-acceptance hygiene + discharge-sync batch (the r10/REVISION-9 precedent:
acceptance earned on the pre-batch text, every edit recorded): VC15's four
advisories repaired in place; the (H9) row synced to the EXECUTED Unit C —
**(NSF-KEY) is RETIRED** (six (NSF-KEY-RET) artifacts on file; compiled witness
H9-U1), the row's open member now (NSF-INT) with its (K1-CANON) premise; the
D-12r row rewritten to its dual-accepted reduction (both cites pinned-confirmed;
reclassification VC-gated); the (H8) = (K-CUT) row re-scoped to its landed 11-unit
Lean development (REDUCED, not discharged — the fan-out's hostile pass owed); the
mid-campaign (E-N) rewrite (commit 89176a4) formally ADOPTED with its process gap
noted. The §3.1 roster stays TWELVE rows ↔ (H1)–(H9). HONEST COUNTER STATEMENT:
the acceptance was EARNED on the REVISION-12 text; REVISION 13 is display/status
sync but IS a text change — a confirming end-to-end pass (**VC16**) is OWED to
re-anchor the accepted text on the REVISION-13 wording; until it lands, the
acceptance citation points at the REVISION-12 text (charge at §19).

**VC16 + REVISION 14 applied (r15, appendix §20) — superseded as CURRENT
standing by the REVISION 15 paragraph below.** VC16
(the confirming end-to-end pass, fresh context;
`lean/notes/openmath/VC16_report.md`) RAN on the REVISION-13 text and returned
**COMPOSITION-SOUND** — zero blockers, advisories only — re-anchoring the
acceptance on the REVISION-13 wording (the §19 charge met: the four advisory
repairs, the (H9)/(H8)/(E-N) syncs, and the REWRITTEN D-12r row's faithfulness
all verified; finding 1 notes Step 4's exact grade reads as PROVED-UNVERIFIED
under §0's vocabulary until its audit lands). Its finding 2 ADJUDICATES the
D-12r reclassification **WARRANTED**: no D-12r-specific open mathematics remains
that must stay [M] — the residue is confined to the (HEN-LIFT)/(OM-SAT)
literature pins. REVISION 14 (§20, unit ROOT r15) EXECUTES the move: the D-12r
row leaves §3.1 for §4 as a literature-pin entry (pins on file: the three-part
(HEN-LIFT) at Neukirch II §4 (4.6) p. 129 / the displayed companion uniqueness
lemma / Serre LF I §6 Prop 15 pp. 17–18; (OM-SAT) riding the §4 GMN entry at the
TAMS-confirmed Cor 3.8 + Def 3.9), with the dependency-decomposition routing
preserved and every consuming step's conditionality re-pointed
("perimeter-conditional on the §4 D-12r pin"). The §3.1 roster is now **ELEVEN
rows ↔ (H1)–(H9)** with **(H3) = {(ADM), (E-N)}**; VC16 finding 2's three §4
roster-sync items are executed honestly at §20 (GMN pagination: CONFIRMED for
the (OM-SAT) slice only, the LIT-1-wide session still owed; the Neukirch pin:
numbers CONFIRMED, Mathlib probe RAN — coprime-factor Hensel ABSENT, the pin
stands as a paper cite; Serre LF I §6 Prop 15 added to the consumed-loci
enumeration). HONEST COUNTER STATEMENT: the acceptance was EARNED on the
REVISION-12 text and RE-ANCHORED by VC16 on REVISION 13; REVISION 14 executes a
VC16-warranted reclassification but IS a text change — a confirming end-to-end
pass (**VC17**) is OWED on the REVISION-14 wording (charge at §20).

**VC17 + REVISION 15 applied (r16, appendix §21) — the CURRENT standing.** VC17
(the confirming end-to-end pass, fresh context;
`lean/notes/openmath/VC17_report.md`) RAN on the REVISION-14 text and returned
**COMPOSITION-GAPS** — ONE blocker + ONE advisory, both display-conformance at
the r15 edits themselves, with ALL other charged checks PASSING (the D-12r
residue confined to (HEN-LIFT)/(OM-SAT), the ELEVEN-row ↔ (H1)–(H9) bijection
with (H3) = {(ADM), (E-N)}, the operative-clause attributions, the conditional
walk, acyclicity incl. 17 ≺ 18 ≺ 18b, no REVISION-12/13 regression). REVISION 15
(§21, unit ROOT r16) repairs both in place: Step 12's (τ-irr) saturation
description now locally displays "perimeter-conditional on the §4 D-12r pin
(entry 10)" — the one former consumption site the r15-5 audit had passed on the
central-enumeration ground alone, now matching Steps 2/3/11/16/19 — and Step 4's
grade is rendered in the EXACT §0 word, **PROVED-UNVERIFIED** (pending upgrade:
the §3.2 M17/RegPFinite faithfulness-audit row; the machine-checked-Lean-core
fact and Step 5's independent route stay as displayed notes, not grade words).
No [M] member, step proof content, leaf pointer, or clause attribution changed;
the §3.1 roster stays ELEVEN rows. HONEST COUNTER STATEMENT: the acceptance was
EARNED on the REVISION-12 text, RE-ANCHORED on REVISION 13 by VC16; REVISION 15
is a two-sentence display repair but IS a text change — the confirming
end-to-end pass (**VC18**) is OWED on the REVISION-15 wording (charge at §21).

**Status vocabulary** (tree §0, compressed): **VERIFIED** = survived ≥ 2 consecutive
clean adversarial passes in fresh contexts, at least two models (VERIFIED-DUAL), or
machine-checked in Lean with a faithfulness audit (VERIFIED-LEAN). **PROVED-UNVERIFIED**
= proof on file, pending pass named. **OPEN** = no proof. **LITERATURE** = external
citation carries it, faithfulness status stated. A reduction is not a proof.

---

## 1. THE THEOREM (the scaffold form)

Fix n ≥ 2. A **splitting type** τ of degree n is a multiset of pairs (e_i, f_i) with
Σ e_i f_i = n — the ramification indices and residue degrees of the irreducible
ℚ_p-factors. (The tree writes σ for the same object; τ = σ throughout.) Monic degree-n
polynomials over ℤ_p are identified with ℤ_p^n by coefficients, carrying Haar
probability μ. decided_τ(k) counts the residue classes mod p^k ALL of whose monic lifts
are separable of type τ; undec(k) is the measure of the classes not decided for any
type. Let ℛ := ℚ[q, 1/q, 1/(q^c − 1) : c ≥ 1] ⊂ ℚ(q) — the cyclotomic-type ring, whose
elements have no poles at any real q ≥ 2, hence at any prime power. (Variable
convention [REVISION 13, VC15 advisory 4(b)]: q and t name the SAME single formal
variable — t = q, ℚ(t) = ℚ(q); some displays below write t where the solve's
formal variable is emphasized as an interpolation variable. No second variable
exists anywhere in the document.)

> **(ROOT) THE UNIFORMITY THEOREM — THE TARGET.** (Displayed as the campaign's goal;
> NOT what the §2 steps presently prove — that is (ROOT-C) below, the honest
> quantifier-for-quantifier form.) For each n and each splitting type τ of degree n
> there is a SINGLE rational function R_τ ∈ ℛ ⊂ ℚ(t) such that at EVERY prime p —
> including every wild prime p ≤ n — the following hold.
>
> **(0) Cylinder-limit density.** The type-τ locus L_τ = {f monic separable of type τ}
> is Borel; the decided-τ cylinder unions are nested increasing in k; and
>
>     α(n, τ; p) := μ(L_τ) = lim_{k→∞} decided_τ(k) / p^{nk}.
>
> **(SQ) Level-k squeeze.** At every finite level k, two-sidedly,
>
>     decided_τ(k)/p^{nk}  ≤  α(n, τ; p)  ≤  decided_τ(k)/p^{nk} + undec(k),
>
> and undec(k) → 0 (drainage) — so the finite-level counts converge to the density
> with an explicit bracket at every k.
>
> **(R) R_τ evaluation.** R_τ is the τ-entry of the unique solution of a finite linear
> system (I − K(q))·C = b(q) over ℚ(q) — branch = convolution over coprime children,
> descend = strictly smaller child, self-loop of size e (**e ≥ 2 always** — no size-1
> self-loop exists in the system; the forcing is the (SL≥2) display at Step 5
> [REVISION 2, finding V2-6]) = geometric resummation 1/(1 − q^{−(e(e+1)/2 − 1)})
> (a unit of ℛ, the exponent being e(e+1)/2 − 1 ≥ 2) — a system whose state
> menu, coefficients, and pivots carry NO
> dependence on p; every denominator produced anywhere in the solve is cyclotomic-type
> (a unit of ℛ), so the specialization is pole-free at every prime power; and
>
>     α(n, τ; p) = R_τ(p),      Σ_τ R_τ = 1  identically in ℚ(t).
>
> **(UB) Unramified base.** The SAME R_τ works over every unramified base: over the
> degree-δ unramified extension of ℚ_p the type-τ density is R_τ(p^δ).

> **(ROOT-C) THE COMPOSED THEOREM (what §2 proves, stated exactly).** Assume the §3.1
> open-mathematics rows as hypotheses:
> **(H1)** the graded-dictionary open family at OM orders ≥ 2 [ENLARGED at
> REVISION 6, findings F-2/F-3, from its former single member; member (d) added at
> REVISION 7, finding VC7-1]: (a) (GR-B), the
> anchored-march residue-structure package; (b) GD-1's (KP)/(KPirr)
> representative-admissibility clauses beyond order 1 (= O-2a's open lemma OL-2a-1);
> (c) the GD-6 run-decomposition-conformance instances at orders ≥ 2 that Step 10's
> conditionality line consumes; (d) the order-≥ 2 KEY1 block [REQUANTIFIED to
> READ ORDER at REVISION 8, finding VC8-1] — the key-compliance block KEY1 proves
> at order ≤ 1, asserted at EVERY realized site of Step 6's machine at OM order
> ≥ 2, IRRESPECTIVE of the current key's degree (order-≥ 2 sites whose key
> remains degree 1 — e.g. e·g = 1 refinement chains — INCLUDED), which Step 6's
> ALL-SITE theorems consume there — exactly the complement of the proved
> order-≤ 1 slice; same proved-≤1/open-≥2 shape, one group, displayed
> at the §3.1 row; **(H2)** (FRESH); **(H3)** (E-N) and (ADM) (O-9's
> census-admissibility hypothesis,
> FULL attainment — automatic at order 0 only; at r ≥ 1 a displayed p-free
> per-datum decidable check — §3.1) [(ADM) added at REVISION 2, finding V2-5; its
> "automatic at orders ≤ 1" phrasing corrected at REVISION 4, finding VC4-2, per
> the O-9 r3/r4 rescopes; **D-12r (the TB-CAP (τ-irr) perimeter), formerly this
> group's third member, RECLASSIFIED [M] → the §4 literature-pin class at
> REVISION 14, r15, executing VC16 finding 2** — the TB-CAP perimeter remains a
> conditionality of every consuming step, now LITERATURE-grade
> ("perimeter-conditional on the §4 D-12r pin"), no longer a hypothesis row of
> this display];
> **(H4a)** (R1)–(R4) [SPLIT out of the former (H4) at REVISION 2, finding V2-2:
> NOT clause-(UB)-only — clause (R)'s value chain consumes it too, through Step 18's
> K3-c (the base-changed β-legs at q ↦ q^δ, δ > 1; the O-11 r2 declared row D-c)];
> **(H4b)** the clause-(UB) residue (UB-X) (the per-leaf O_δ
> re-base audit + position-class exhaustion + aggregation commutation — §3.1),
> whose position-class-stability fragment **(K3-δ)** (K3-c's realized δ > 1 pools
> are δ-stable position classes — (UB-X)(b) restricted to those pools) is ALSO
> consumed by clause (R) [REVISION 4, finding VC4-1];
> **(H5)** (PACK), the actual-pack correspondence PLUS the pack row-laws (D-11's
> open clause 4: the concrete assembled pack's rows equal the intended 𝔅_n
> instantiation's entry-for-entry — AND, ENLARGED at REVISION 7, finding VC7-2,
> the three named row-laws at the 𝔅_n rows as explicit clauses: **(PACK-i)**
> rsh_interp, **(PACK-ii)** r_bdd, **(PACK-iii)** wsh_ok — AND, ENLARGED AGAIN at
> REVISION 10, r11, the FOURTH row-law **(PACK-iv)** sem_alloc, the semantic
> verdict-row form at the intended member (the premise D-11 r4's Theorem M2 names
> as (RE)) — all displayed at the §3.1
> row — consumed with the sub-attribution exact [synced at REVISION 8,
> finding VC8-2; (PACK-iv) carried into THIS display at REVISION 12, finding
> VC13-1 — the r11/r12 syncs had run the §3.1 row, Step 19, and the ∎ to four
> members while this display stopped at three]: PACK-i/ii at Step 18's K2
> evaluation glue, PACK-iii the membership
> input to Step 18's K3 evaluation ring-hom (with D-6), and PACK-iv at its CLOSED
> two-edge set — Step 18b DIRECT (Theorem M2's (RE) instantiation) + Step 17's E1
> delivery display of (ROW-Σ) (whose (RE) hypothesis at the consumed member IS
> this clause) — explicit because
> entry-for-entry equality transfers a law only if a
> displayed theorem supplies it on the 𝔅_n side, and none is on file — §3.1)
> [added at REVISION 4, finding VC4-3];
> **(H6)** the terminal-seam engine-conformance obligation group (§3.1):
> CU-2t's three terminal rows **(T-DEC)/(T-READ)/(T-VERD)** (first displayed at
> CUC §9.4; (T-READ) graded into the GD-2/GD-5 read-semantics-conformance family
> at CU-2t R4) PLUS D-11's widened-charge item **(vii)** (general-n H.6 in-fence
> conformance of the built tables) — consumed by clause (R) at Step 11's route
> (CU-2t's §9.5 terminal-verdict conditionality, which Step 18 consumes through
> D-a/K4-CYL), through the terminal-seam leaf labels riding O5triple's counting
> into Step 18's K1 ("CU leaf labels", via Steps 11/12 → Step 17's M3/Theorem-E
> leg — the same K1 route the (FRESH) row displays; leg added at REVISION 6,
> finding A-5) and, for item (vii), at Step 18's K3-a [group added at REVISION 5,
> findings VC5-1/VC5-2: open propositions consumed on the mandatory path cannot
> sit in [T] under the ledger's own classification rule];
> **(H7)** the sibling-interface engine-stratum conformance pair **(I-τ)/K-LOC**
> (§3.1): (I-τ) — the engine's realized site strata present as O-10's adapted
> cells; K-LOC (= M07 OL-3, an exactly-stated OPEN lemma) — continuation reads
> of branch j = OM reads of f_j up to cell-constant normalization. Consumed by
> clause (R) at Step 15's application-to-engine-strata line and Step 18's K1
> ("(I-τ)→GD"); clause (UB) inherits the same content through the transported
> chain [added at REVISION 6, finding F-1: the LAST of O-11 r2's declared
> D-a/D-b/D-c interface rows adjudicated hypothesis-grade, by the same rule as
> (H6); the own-row-vs-(H6)-membership adjudication is displayed at the §3.1
> row and §12];
> **(H8)** (K-CUT), the shallow-cut decomposition kernel (§3.1) [NEW row at
> REVISION 10, r11, from finding G-2 of the UN-BANKED O-11 hostile pass of
> 2026-08-03 (`O11_owedpass_2026-08-03.md`, verdict CRITICAL, not banked as
> O-11's pass 3 — recorded at the §3.2 O-11 row; provenance disambiguated at
> REVISION 12, finding VC13-4)]: exactly-one shallow
> decomposition with exhaustive-exclusive read ownership and the
> weight-preserving product law μ(fiber T) = W(Ŝ)·∏_i weight(D_i) — M04 Lemma
> 3.3's hypothesis, consumed inside Step 18's K1 regrouping; an OPEN KERNEL of
> T-6, previously displayed NOWHERE in this document;
> **(H9)** (NSF), the ns-routing interface row (§3.1) [NEW row at REVISION 10,
> r11]: two members retiring SEPARATELY — **(NSF-INT)**, the interface
> implication chain pin → predicate → corpus (with its TWO displayed premises,
> the §1.4 marks-vocabulary premise and **(K1-CANON)**, housed in the member's
> statement), and **(NSF-KEY)**, as-built keying conformance, **RETIRED**
> [synced at REVISION 13, r14, per the H9 lead's adjudication] (Unit C executed
> at the 2026-08-05 sign-off's dispatch: all six (NSF-KEY-RET) artifacts on
> file — ledger "UNIT C EXECUTED" (commit 322ec54) + "H9 LEAD ADJUDICATION"
> entries; holds at HEAD by construction of (†7) over the guarded carrier;
> compiled-proposition record H9-U1, `Scaffold/HDischarge/H9/NsfKeyProp.lean`,
> Lean-core); the row's OPEN member is (NSF-INT) (its two displayed premises
> housed in-member; the (K1-CANON) discharge owed); enters Step 18's K1/K4
> chain and Step 17's M3 leg (via Steps 11/12).
> Then for each n ≥ 2 and each splitting type τ of degree n there is a single
> R_τ ∈ ℛ such that clauses (0), (SQ), (R) hold at EVERY prime p including every wild
> p ≤ n, and clause (UB) holds. Hypothesis attribution, exact [REVISION 2, finding
> V2-2; corrected at REVISION 4, findings VC4-1/VC4-3/VC4-4]: clause (0) rests on
> Step 1 alone (unconditional); clause (SQ) — under this section's SEMANTIC
> definitions of decided_τ(k) and undec(k) — rests on Step 1's bracket (Corollaries
> BRACKET/SEM-DRAIN) + Step 13's semantic drainage layer, both unconditional at
> every prime including wild: **it consumes NO §3.1 row** [attribution corrected
> DOWNWARD at REVISION 4, finding VC4-4 — the former display over-attributed
> (H1)–(H3), including (ADM) and the conditional classifier-transfer route, to
> (SQ); the classifier-side counting machinery rides the evaluation squeeze inside
> clause (R)'s scope instead]; clause (R) consumes (H1)–(H3) + (H4a) + (H5) + the
> (K3-δ) fragment of (H4b) + (H6) + (H7) + (H8) + (H9) [(H6) added at REVISION 5,
> findings
> VC5-1/VC5-2; (H7) added at REVISION 6, finding F-1 — Step 18's K1 consumes
> O-10's product law at the engine's strata only through it; (H1)'s group
> ENLARGED at REVISION 6, findings F-2/F-3, so the "(H1)" here now carries the
> GD-1/GD-6 members too; (H8)/(H9) added at REVISION 10, r11 — (H8) inside Step
> 18's K1 regrouping (Lemma 3.3's cut hypothesis), (H9) at Step 18's K1/K4 label
> routing and Step 17's M3 leg];
> NO clause other than (UB) consumes (H4b) BEYOND its (K3-δ) fragment; clause (UB)
> consumes
> **(H1)–(H3) + (H4a) + (H4b) + (H5) + (H6) + (H7) + (H8) + (H9)** [(H8)/(H9)
> added at REVISION 10, r11: the transported K1/K4 chain at q ↦ q^δ consumes the
> same cut-decomposition and ns-routing content; (H6) added at REVISION 5: the
> transported evaluation chain at q ↦ q^δ consumes the same terminal-verdict and
> fence-conformance content; attribution COMPLETED at REVISION 3, finding B1: the
> former display read "(H4a) + (H4b)" as (UB)'s full set — but (H1) enters (UB)
> through Steps 8/16 (the GD layer, (GR-B)-conditional at orders ≥ 2), (H2)/(H3)
> through Step 16's consumption of Step 14's census ((FRESH)/(ADM)-conditional at
> orders ≥ 2) and through the (UB-X)(a)-transported dictionary/CU/drainage layers
> ((E-N); the same transported layers also carry the §4 D-12r pin's TB-CAP
> perimeter — literature-conditional, not an (H3) member since REVISION 14, r15);
> only (H4b) is (UB)-exclusive; (H7) added at REVISION 6 — the
> transported product-law chain at q ↦ q^δ consumes the same (I-τ)/K-LOC
> interface].
> Without (H4b), what is proved of (UB) is exactly the Step-16 scope: the GD/census
> layers hold over an arbitrary absolutely-unramified complete DVR (consuming neither
> (H4a) nor (H4b), while retaining their own (H1)–(H3) conditionality at orders ≥ 2 —
> finding B1), and — still under (H4a) — the alphabet law (F_{p^δ}-linearity +
> a_δ = δ·a₁) at δ-stable position classes. (The former "Without (H4)" sentence
> asserted the alphabet law hypothesis-free — false as written, since the law is
> (R1)–(R4)-conditional; corrected here.)
> **Proof status of the implication itself:** it is the §2 composition — nine leaves
> VERIFIED (the count synced at REVISION 6, finding A-8: a REVISION-5 sync miss had
> left "seven" here against the headline/§3.2/§11 NINE), the rest PROVED-UNVERIFIED
> with named pending passes (§3.2) and displayed
> transcription debts (§3.3). The [V]/[T] rows are AUDIT debts on the proof of this
> implication, not additional mathematical hypotheses of its statement; the
> implication is therefore itself PROVED-UNVERIFIED (never consumed by an acceptance
> claim) until §3.2/§3.3 empty and the end-to-end passes land. (ROOT) = (ROOT-C) +
> discharge of (H1) (the full group — four members since REVISION 7), (H2), (H3),
> (H4a), (H4b), (H5) (incl. the REVISION-7 PACK-i/ii/iii clauses AND the
> REVISION-10 (PACK-iv)), (H6), (H7), (H8), (H9) (ONE open member since
> REVISION 13 — (NSF-INT), whose (K1-CANON) discharge is owed; (NSF-KEY)
> RETIRED at the executed Unit C).

**Wild-prime scope, explicit.** The content is at p ≤ n (p | n!), where factorization
is governed by higher-order Okutsu–Montes (OM) Newton-polygon towers whose combinatorics
a priori depend on p; p > n is residue-field combinatorics. The paper's original tame
route (S_n-equivariant resolution) FAILS at p ≤ n and is used nowhere below: every step
computes the wild densities directly.

**Relation to the tree's root.** (0)+(SQ) = (U-GEN)(i)/(ii) + the D-1 bridge; (R) =
(U-GEN)(iii) + D-6 + D-2; the operative claim is (U-GEN), and (U-GEN) ⇒ (U-DENS) via
(0) (the converse is not claimed — DELTA-1 §D1.5(a)). "Every prime" (as opposed to
"every prime outside an explicit finite effectively-bounded set") is carried by K-13 +
O-12. The projective normalization and the palindromic functional equation are ANNEX
(A-1/A-2, out of the critical path by standing directive).

**Verified instances (anchors, not proofs of the general theorem):** n = 2 is
VERIFIED-LEAN at every prime including wild p = 2 (`OM/UniformCapstone.montes_uniform_n2`,
Lean-core footprint: value + bracket + drainage machine-checked); n = 3 numerically
exact at the gate standard (RESUM-n3, 65/65, PARI 0 mismatches — with the G-3 scope
caveat [REVISION 13, VC15 advisory 3]: the §3.2 O-11 row's un-banked 2026-08-03
pass records the "65/65 END-TO-END rederivation of the assembled pipeline" reading
as UNSUPPORTED beyond the five displayed identities; the 65/65 agreement stands as
a flag either way — flags are never substitutes); the density tie
numerically confirmed through n ≤ 5 including deep-wild (5,5) (N2). O-11's falsifier
re-derived the true n = 2 triple (p/(2(p+1)), p/(2(p+1)), 1/(p+1)) from the REAL
assembled pack identically in ℚ(q).

---

## 2. THE PROOF (a linear composition of the leaves)

Each step names: the leaf/node (tree id) · its exact statement (compressed, faithful) ·
its verification status with the pass records · what it consumes. The steps are in
logical order; no step consumes a later one (the O-2/O-3 cycle was decomposed at
DELTA-2 §D2.3 — the K-RUN-C composition replaced the joint induction; the D-11/O-11
cycle VC1 found in the original walk is stratified at REVISION 1 as Steps 17/18/18b,
and the Step-1 forward edge is re-pointed — see §7). Standing
preconditions throughout (tree §2.4): f monic separable; trees range over ns-free
carriers; statements are per-f (selected-f) except where D-15 owns box quantification.

### Movement I — the measure floor and the symbolic engine

**Step 1 (D-1 MEAS-BRIDGE + D-2 SUM-ONE + D-4 BASE-CASES — the assembly floor;
"Dfloor").** Statement: (D-1) L_τ is Borel; level-k classes are cylinders of mass
p^{−nk}; decided unions nest; ∪_k decided_τ(k) = L_τ up to Haar-null; continuity from
below gives clause (0) — proved for both decided conventions in circulation, clause
(iv) EXACT in the strict convention; plus Corollaries BRACKET / SEM-DRAIN /
SEM-DRAIN-RATE = the measure content of (SQ) on the SEMANTIC side, unconditional at
every prime including wild. (D-2) every monic separable f has exactly one type
(unique factorization + defectlessness); the nonseparable locus is Haar-null — proved
SELF-CONTAINED inside this leaf (Dfloor Appendix A, via LIT-8c resultant/Vandermonde;
part of the VERIFIED leaf-#3 perimeter). [REVISION 1, finding 1: the original text
attributed ns-null to T-6's (ns-null) clause, a Step-2 node — a silent forward edge.
The dependency is RE-POINTED to Appendix A, which is what the verified leaf actually
proves; T-6's (ns-null) is the SAME fact proved independently at the T-layer (Step 2)
and is NOT consumed here. The edge is removed, not waived.] Hence **Σ_τ α(n,τ;p) = 1
at every p**, plus the interpolation principle (a rational function agreeing with a
constant at infinitely many arguments equals it). [REVISION 3, finding B3: the former
text concluded "Σ_τ R_τ = 1 in ℚ(t)" INSIDE this statement — beyond the leaf's
declared perimeter (R_τ is not even definable at Step 1, and α = R_τ needs Steps
18/18b), a textual forward edge contradicting this step's own Consumes-line. Dfloor's
VERIFIED statement supplies exactly Σ_τ α = 1 + the principle; the identity
Σ_τ R_τ = 1 is CONCLUDED at Step 19, inside clause (R)'s conditional scope.] (D-4)
the degree-1/0 floor: type {(1,1)} density 1 decided at
k = 0; empty-convolution unit conventions (C0)–(C2).
Status: **VERIFIED — leaf #3** (pass 1 Codex V22 CLEAN SOUND, 0 criticals, commit
604621f; pass 2 fresh Fable, model-diverse, 0 criticals, commit bdc996a; adjudicated at
ledger dbccbc1; falsifier `verification/openmath/dfloor_checks.py` ALL PASS). Three
minor gaps ride the next doc touch. Displayed residue: the **BASE-1(f) fence** — the
classifier-side (algorithmic) half at n = 1 is fenced OUT to the classifier steps below.
Consumes: LIT-2 (defectlessness; the K-11/M16 audited e·f bridge), LIT-8c
(resultant/Vandermonde — carries Appendix A's ns-null), standard measure theory with
displayed statements (Folland-grade; §4 roster). NOTHING from any later step.
Delivers to the root: clause (0), the bracket shell of (SQ), and the Σ_τ α = 1
identity + interpolation principle feeding (R)'s Σ_τ R_τ = 1 (concluded at Step 19 —
REVISION 3, finding B3).

**Step 2 (T-1..T-9 + T-U — the MOVES core: the symbolic engine).** Statement: the
carry-algebra stage tower and move ledger (T-1), composition of per-move digit/threshold
ledgers along read histories (T-2), the per-shape-prefix mass laws SUM/EVENT on the
branch tree T_can(f) (T-3), the finite p-free species/letter catalogue (T-4), the
height-resummation domains (T-5), tree assembly + (ns-null) + TB-CAP (T-6), the
per-species symbolic value tables + obligations ledger (T-7s, the symbolic half of the
T-7 split), the finite block-triangular solve with spectral escape over ℚ(q) (T-8), the
exhaustion index table (T-9), and THEOREM U — the squeeze closer, conditional on its
displayed kernel ledger §U.SQ.6 (T-U).
Status: **VERIFIED-DUAL, every node** (passes named per node in the tree §2.2;
2026-07-26/28), WITH the DELTA-1 re-scopings governing: T-1 is verified AS A THEOREM
ABOUT THE SYMBOLIC ENGINE (the semantic "each OM read" clause routes through the
dictionary steps below — §D1.5(e)); T-8 is verified for the formal ℚ(q) solve +
(REG-p)-conditional specialization (§D1.5(f)); T-7's value-correctness event
([1v]-FULL) is RE-OWNED by O-11 (D-9); T-6's TB-CAP is perimeter-conditional on the
§4 D-12r literature pin [re-pointed at REVISION 14, r15 — formerly the §3.1 (H3) row].
Twenty-two sealed numeric gates ride this layer (§5).
Consumes: B-1..B-7, the June skeleton, with grades DISPLAYED HERE so the dependency
universe is closed inside this document [REVISION 1, finding 3]: **B-1** (Hensel
residue-splitting measure-exact) PROVED-UNVERIFIED (June document; 2000/2000
unit-resultant checks) — and the MOVES layer re-proves the split it needs inside
T-1..T-3; **B-2** (residue shape counts are universal polynomials) PROVED-UNVERIFIED at
document level with the counting core VERIFIED-LEAN (`L3.lean`/`L3Gauss.lean`/
`L3Squarefree.lean`, Lean-core); **B-3** (Newton-cell volume) core VERIFIED-LEAN
(`L4.lean` `cellVolume_eq`) + PROVED-UNVERIFIED document; **B-4** (order-r residual
equidistribution) order 1 = B-3 (VERIFIED-LEAN), order r PROVED-UNVERIFIED
(`M6_lemma.md`; its modern descendant is O-9's OL-B, discharged to GD-3); **B-5**
(Vandermonde pushforward) PROVED-UNVERIFIED (`R1_complete.md`); **B-6** (termination +
finite shape menu) PROVED-UNVERIFIED (`L5fix_complete.md`; modern face = O-13, Step 3);
**B-7** (finite linear solve) PROVED-UNVERIFIED (`L6M4_complete.md`; modern VERIFIED
counterpart = T-8 + K-13). The B-layer verification rounds are a §3.2 ledger row. B-8
is SUPERSEDED by this layer and cited nowhere.
Delivers: the entire p-free symbolic pipeline behind clause (R) — menu, tables, solve,
squeeze shell.

**Step 3 (O-13 + D-12 — skeleton/menu finiteness).** Statement: run-collapse is
well-defined; every realized T_can(f) (infinite branches included) collapses to a
finite tree (≤ 6n² letter blocks per branch); the collapsed shapes form the finite
p-FREE set Sk(n), |Sk(n)| ≤ (n+2)^{7n³(n²+3n+16)}. This survives the K-3 caterpillar
countermodel because unbounded recentering runs collapse to one node with a depth
parameter, never new shapes.
Status: **VERIFIED at honest scope — leaf #1** (pass 1 Codex V8 SOUND-WITH-GAPS → rev 2;
pass 2 fresh Fable, model-diverse, package-only context: all six gaps non-consumed
cosmetics, enumerator re-implemented from the text alone reproducing
|S_n| = 1, 15, 53, 197, 509, 1460 exactly; ledger "O-13 REACHES VERIFIED, 2026-07-31
night"). Lean: `skeleton_finite n` at every n (`SkeletonFinite.lean`). Displayed
residue: **D-12r** — the completeness clause (realized trees spell inside Sk(n)) rides
TB-CAP's D4R.0-K(c) verdict pin, a SEPARATE node (since REVISION 14, r15, the §4
D-12r literature pin — residue exactly (HEN-LIFT)/(OM-SAT); formerly the §3.1 (H3)
row). VAL-DIV is a labeled conjecture,
consumed by nothing here.
Consumes: T-4's letter finiteness (VERIFIED-DUAL), the B-6 depth mechanism
(PROVED-UNVERIFIED, grade displayed at Step 2's consumption list; §3.2 B-layer row).
Delivers: finiteness of the solve's state menu in clause (R), and K-OB-A's roster
finiteness (CU-5).

### Movement II — the solve's regularity (pole-freeness at every prime power)

**Step 4 (K-13 — (REG-p) finiteness; Lean-formalized).** Statement: the set of primes
where the pinned rational data degenerate is finite, exactly characterized, effectively
bounded; Theorem U fires at a cofinite set. Status [grade conformed to the §0
vocabulary at REVISION 13, VC15 advisory 2 — the O-14a′/VC10-advisory-1 precedent;
rendered in the EXACT §0 word at REVISION 15, r16, VC17 advisory 2]:
**PROVED-UNVERIFIED — the pending upgrade is the named §3.2 row M17/RegPFinite
faithfulness-audit record, whose landing upgrades the grade to VERIFIED-LEAN**;
displayed notes, NOT grade words: dual provenance + machine-checked Lean-core,
and Step 5's independent route contains the impact (M17 Theorems A/B/C + Corollary U;
`MovesU/RegPFinite.lean`, 14 theorems, Lean-core, per-declaration `#print axioms`
checked; dual provenance = the Fable proof `M17-regp-density_fable.md` + an
independent Codex twin from the neutral brief, agreeing on every load-bearing step
— `lean/notes/M17_FORMALIZED_2026-07-31.md`; N6 25/25). §0's VERIFIED-LEAN
requires "machine-checked in Lean WITH a faithfulness audit", and NO
Lean-statement-vs-mathematical-statement audit record is on file for this leaf
(contrast the K-12 row, expressly held pending exactly such an audit) — the owed
audit is the NEW §3.2 row **M17/RegPFinite faithfulness-audit record**; impact
contained: Step 5's stronger emptiness-at-𝔅_n result independently covers the
specialization clause. Consumes: T-8's formal solve. Delivers: (R) at all but
finitely many primes — upgraded to ALL primes by Step 5.

**Step 5 (O-12 + D-6 — pole-free / (REG-p)-emptiness at the intended solve family
𝔅_n).** Statement: at the abstract solve family 𝔅_n (the §2.1-defined intended
instance), for all three on-record kernel organizations, EVERY block determinant and
EVERY assembled denominator — table coefficients, height resummations, normalizations,
the final R_τ — is cyclotomic-type (a unit of ℛ), hence nonvanishing at every prime
power ≥ 2: the failing set is EMPTY (Theorems 1 + 3 + 4 + Lemma RR; substochasticity +
termination margins ≥ 3/4, 7/8, 6/7 ⇒ spectral radius < 1). Includes the semantic mass
lemma base L1–L6′/Fact F that Step 17 consumes.
Status: **VERIFIED at honest scope — leaf #2** (pass 1 Codex V19 no criticals; pass 2
fresh Fable ZERO criticals, 2 non-load-bearing gaps; adjudicated at ledger commit
95a46d2; falsifier 18,120 prime powers 0 findings, extended by D-11's run to 303/303
primes < 2000). The honest scope: zeros/poles at 𝔅_n — VALUE correctness is NOT part of
this claim (that is D-11 + O-11); the extension to the eventual general-n pack is gated
by D-11 (instance pin) + the contingent OL-O12-1 (fires only if a future pack departs
from 𝔅_n's organizations). Residue: **OL-O12-2** — mathematical half CLOSED at the D-11
unit; what remains is a finite transcription/data pass, zero mathematics.
**(B-INST) — the instance correspondence, stated exactly [REVISION 1, finding 9].**
What identifies the realized packs with 𝔅_n, per n: the pack consumed by Steps 17/18
is BY CONSTRUCTION the 𝔅_n member assembled from T-4's menu, T-7s's tables, and T-8's
block organization (tree §2.1's definition of the intended instance) — the O-12
theorems apply to it verbatim PROVIDED its kernel organization is one of the three
on-record, and OL-O12-1 (contingent, currently unfired) is the displayed escape if a
future pack departs. NO supplied theorem identifies "every general-n realized pack"
with 𝔅_n beyond this by-construction pin: at n = 3 the identification of the complete
realized degree-3 table as an (O3) member of 𝔅₃ is the OL-O12-2 data pass (§3.3, owed);
at general n it is D-11's instance pin (Step 17) + the same finite transcription
discipline, which is exactly why D-11 sits on the mandatory path.
**(SL≥2) — every self-loop has size e ≥ 2; the e = 1 corner is VACUOUS [REVISION 2,
finding V2-6].** The §1 clause-(R) denominator 1/(1 − q^{−(e(e+1)/2 − 1)}) degenerates
at e = 1 (exponent 0, denominator 0); it never fires there, by the solve structure —
proof displayed, each step a proved O-12 clause (`O12_phaseB_verifybrief_rev4.md`
§§2.2–2.4, Thm 1, Cor D):
(a) *self-loops live inside one block.* Every INTER-block booking descends strictly —
hand-off targets have multiplicity m < e resp. μ < e (Lemma L4(i)/(ii)) — so the block
solve is triangular with the within-block (S-loop) return as the ONLY diagonal entry:
a self-loop of the system IS a block-e loop, and no cycle through distinct blocks
exists.
(b) *block 1 has no loop.* K_1 = (0), Φ_1 = 1, E(1) = {1} (O-12 §§2.2–2.3; Theorem 1
is stated "for e ∈ {2, …, n} (block 1 has K_1 = 0, Φ_1 = 1)"): a size-1 aligned
cluster is a monic linear factor, decided immediately (β_1 = 1 — also the base case
of D-11 M2's induction); and multiplicity-1 hand-off atoms TERMINATE as simple Hensel
factors, absorbed into the composition datum as fixed data — they never open a
continuation state, so nothing ever re-enters block 1.
(c) *hence e ≥ 2 at every self-loop*, so E = e(e+1)/2 ≥ 3, the exponents E − 1 ≥ 2 and
E ≥ 3 are ≥ 1, and every solve denominator u ∈ {1 − q^{1−E}, 1 − q^{−E}} is a unit of
ℛ (Theorem 1(3); Corollary D's margins Φ ≥ 3/4, 7/8, 6/7 — the Step-17 (r1) numbers —
are computed exactly on "E ≥ 3 for e ≥ 2 … (and Φ_1 = 1)").
Consumer audit: the formula's only §2 consumers are Steps 5/17/18 (pole-freeness,
margins, spectral escape), all of which consume it THROUGH O-12's e ≥ 2-scoped
theorems — no consumer ever instantiates e = 1. The bug was this document's
unquantified §1 display (now quantified), not a leaf gap.
Consumes: T-8's block structure, T-3/T-6 event structure (as structure only, never
value correctness — the V6-finding-19 circularity is scoped OUT).
Delivers: the pole-free clause of (R); with Step 4, "every prime" in the root.

### Movement III — the classifier ↔ semantics dictionary (the campaign's center)

The chain touches the actual p-adic factorization semantics exactly here. The pieces
were decomposed at DELTA-2 (the K-RUN-C composition CU-1..CU-7 replacing the O-3 ⊕
O-2b joint induction; OB1/OB2/BNS/K-OB-C all EVICTED from the mandatory path — DELTA-2
§D2.4, DELTA-3 §D3.1: ZERO root-side BNS touch-points remain).

**Step 6 (O-1thr package: Theorem 1 = D-14, Theorem 2 = O-1(thr), Theorem 3 = D-15 —
per-site read locality + the cylinder law).** Statement: (Thm 1/D-14) for every
menu-LAWFUL site (H, ν), whether the canonical run on f reads (H, ν) is determined by
f's digits below the computable modulus L(H, ν) (the read ceiling) — the noncircular
quantifier form; (Thm 2/O-1thr) the realized-site two-lift tier with window closure
(K2); (Thm 3/D-15) decided ns-free verdict classes are EXACT finite unions of level-t
cylinders with the per-tree fiber scaling #Φ_N(T) = mass(T)·p^{nN} (the CYL law; owns
ALL box-level lift quantification). Proved at rev 3/4 over the sloped/sheared
convention pin (the depth-2 falsifier REFUTED the unsheared reading — 7,472 phantom
violations — and confirmed the sheared machine exhaustively, 41,720 instances, 0
violations); VAL1/KEY1/DIV1/ROOT-M/WGEO proved outright at order ≤ 1.
**Order-≥ 2 conditionality [REVISION 7, finding VC7-1; REQUANTIFIED at REVISION 8,
finding VC8-1]:** the theorems' ALL-SITE scope consumes, at EVERY realized site of
OM order ≥ 2 — membership by READ ORDER alone, irrespective of the current key's
degree, order-≥ 2 sites whose key remains degree 1 (e·g = 1 refinement chains)
INCLUDED [the former "(degree-g > 1 keys)" parenthesis identified the order-≥ 2
reads with sites whose key has degree > 1, an UNPROVEN identification — the VC8
blocker] — the
order-≥ 2 KEY1 block: the key-compliance block Lemma WGEO's (V1)/(V2) inputs
carry beyond the proved order-≤ 1 slice. That statement is OPEN, and is since
REVISION 7 the displayed §3.1 member **(H1)(d)**, consumed HERE (the former
display priced it only as "the (H1) campaign perimeter", which named no member —
the VC7 blocker); the KEY1 proved outright at this step is the order-≤ 1 clause
alone, so (d) covers exactly the complement of the proved slice in this step's
site range — total coverage consumes no key-degree/order equivalence.
Status: **PROVED-UNVERIFIED** — rev 4 landed (commit 5613274; V24's REAL≡ circularity
repaired structurally: (M6c) requantified, non-ns-halt now a conclusion); pending pass
**P(O-1thr r4)** (fresh hostile pass on rev 4, owed). Displayed caveat **(E-N)** (Thm
3(c) exhaustion) is OPEN and touches CU-4.
Consumes: T-1/T-2's carry algebra (symbolic scope); the graded-presentation dictionary
= **C.1.5**, whose supplier is now DISPLAYED [REVISION 1, finding 5]: C.1.5 ("downsets
are literal digit systems") is T-2's ONE deferred proof obligation, displayed at T-2's
dual acceptance (tree §2.2) and consumed exactly here — at the mathematical machine
O-1thr's proofs run over, the dictionary is DEFINITIONAL (O-1thr §4 residue (S-1): the
clause-by-clause correspondence is displayed, and its remaining content is the S-1
machine-transcription audit, a §3.3 row that now names C.1.5 explicitly); MacLane 1936
(V1) via the LITUNIT pin; at order-≥ 2 sites, the §3.1 member **(H1)(d)** (the
order-≥ 2 KEY1 block — read-order membership, key degree irrelevant) [REVISION 7,
finding VC7-1; requantified at REVISION 8, finding VC8-1].
Delivers: D-14 + D-15 to CU-1/CU-3/CU-4 and Step 17's per-box σ-constancy; the K-4/K-6
repairs route through D-15.

**Step 7 (O-2a — the upstream dictionary half: invariants, order placement,
normalization crossing, admissibility + the D-5 carrier design).** Statement: the
correspondence Θ, typed per fixed separable f from realized runs to GMN types:
(accE, accF) = (e₁⋯e_r, f₀⋯f_r) with saturation ⇔ ω = 1; certified increment nodes map
injectively to GMN orders; raw-frame side reads transport to the v_r-normalized frame;
the canonical key lifts form one GMN-admissible representative family; Theorem 2(B)/(D)
= the emitted-record ↔ GMN-datum tie; OL-6 = run decomposition + chain binding at every
level.
Status: **PROVED-UNVERIFIED** — rev 5 (commit b337195), core THRICE confirmed (V10 →
pass-2 → V25 arc; V25 on rev 4: 0 criticals, 1 minimality gap → OL-3-min trim executed);
pending pass **P(O-2a r5)**. Displayed residue: the SIX exactly-stated open lemmas
**OL-2a-1..6 = the GD-1..GD-6 families**, mapped ONE-FOR-ONE [REVISION 1, finding 5's
coverage charge]: OL-2a-1 = GD-1 (representative admissibility — since REVISION 6,
finding F-2, the (KP)/(KPirr) clauses beyond order 1 are §3.1 (H1)(b),
hypothesis-grade; the properness-proviso leg keeps its pinned §3.3 (KP)/(KPirr)
row); OL-2a-2 = GD-2 (frame/valuation tie — head CLOSED at Step 8 order ≤ 1; orders
≥ 2 = (GR-B) = (H1)(a), §3.1); OL-2a-3 = GD-3 (residual dictionary — min core CLOSED
at Step 8; FULL form at orders ≥ 2 = (GR-B), consumed by the value side); OL-2a-4 =
GD-4 (engine bookkeeping — §3.3 row); OL-2a-5 = GD-5 (base-read conformance — §3.3
S-1 row; its KEY1 mathematical face is since REVISION 7, finding
VC7-1, the §3.1 (H1)(d) member — since REVISION 8, finding VC8-1, the order-≥ 2
KEY1 block — the audit half staying [T]); OL-2a-6 = GD-6
(run-decomposition conformance — since REVISION 6, finding
F-3, the instances Step 10 consumes at orders ≥ 2 are §3.1 (H1)(c); the
bookkeeping/route remainder keeps the §3.3 row, with GD-4). Every
residual has exactly one ledger home; nothing is covered twice or zero times.
Consumes: LIT-1 (GMN) via the pinned citations; the LITUNIT pins; **D-5's
carrier/equality design — supplier DISPLAYED [REVISION 1, finding 5]:** this is NOT an
external input. The tree's D-5 (WELLPOSED) carrier/equality clause is INCORPORATED as
proved content of O-2a rev 5 itself (§§1–2: the two discrete carriers, the map Θ, the
consistency predicate, Lemma R — the design the V10 → pass-2 → V25 arc audited; its
verification debt is the same P(O-2a r5)). The tree's wider D-5 semantic-invariance
clause is carried on the mandatory path by each consuming leaf's own displayed carrier
section (O-13's run-collapse well-definedness at Step 3; O-1thr's §1 machine carriers
at Step 6; CU-1's declared pins at Step 10), not consumed as a separate node — §3.3
records this disposition.
Delivers: CU-2's content (no-phantom = Theorem 2(D) ∘ ι), CU-1's base and step
vocabulary.

**Step 8 (GD23 — the GD dictionary head, GD-2 ⊕ GD-3: frame/valuation tie, development
identity, residual dictionary, over an ARBITRARY complete DVR).** Statement: (GD-2)
u_{i+1} = v_{i+1} on low-degree nonzero B; w multiplicative (MacLane); w_{i+1}(B) =
min_k(w_{i+1}(C_k) + k·γ̂_i); window polygon = N_{i+1}(f) vertex-for-vertex; emitted
record (e,h,ℓ,s,u) verbatim. (GD-3, min core) ord_{ψ̂_hom}(in(f)) = ord_ψ(R_λ(f));
(full form) R^nrm = c·R_λ(f) with slot-wise residue-field bijections — the anchored
march, restated at rev 3 in BOTH printed FGMN Thm-4.2 pairings with the exact match
verified clause-for-clause against the printed source. Stated over an arbitrary
absolutely-unramified complete DVR (killing most of D-3 by scope discipline).
Status: **VERIFIED at honest scope — leaf #4** (arc: V26 UNSOUND with order-≤1 core
confirmed → rev 2 against the LITUNIT pins → V28 SOUND-WITH-GAPS 0-critical → rev 3
(notation split + properness proviso) → pass 2 fresh Fable, 0 criticals, 9
justification gaps, commit 4d109b9 → [the wave-10 VERIFIED adjudication at d730669 was
CORRECTED AS PREMATURE at the ledger, commit 110f308] → rev 4 repairing all 9 gaps,
cores byte-stable (commit f9878d1) → GAPS-CLOSED check → **ADJUDICATED VERIFIED at the
2026-08-02 harvest** (ledger final section, commit c43b13b) [REVISION 1 status sync]).
The
honest scope IS the conditionality: order ≤ 1 proved outright; **(GR-B) at orders ≥ 2
is a DECLARED open input** (the anchored-march residue-structure package, pinned to
printed FGMN Thm 4.2 + Cor 4.4/4.8/4.9/5.6 + Prop 5.14/Cor 5.15 + MacLane Thm
10.2/Lemmas 10.1, 11.1–11.2/Thm 12.1/Cor 12.2); the (KP)/(KPirr) clauses beyond order
1 and the properness proviso at e·g = 1 refinement reads are owned by GD-1 [the
beyond-order-1 clauses are since REVISION 6, finding F-2, the §3.1 (H1)(b) member —
hypothesis-grade, no longer a [T] filing; the properness leg stays pinned at §3.3];
engine
conformance (u = w for the implemented engine) is a declared non-claim (GD-4/GD-6).
Consumes: MacLane 1936 + GMN + FGMN via LITUNIT (every flag resolved MATCH; §4).
Delivers: the GD-2/GD-3 instances consumed by CU-1's step, O-9's census, O-11's K3.

**Step 9 (O-14a′ — RunRealizerExists, the upstream realizer supply).** Statement: every
coherent history's designated realizer exists (interior half from re-keyed coherence;
the (S6b′) monomial-shape clause + last-node landing flank closed at the O14ap arc).
Status: **PROVED-UNVERIFIED, pending pass named — the §3.2 row P(O-14a′ r3)**
[grade display conformed to the §0 status vocabulary at REVISION 9, VC10 advisory 1
— the former "TWICE-STABLE across V11/V15" display was not one of the four declared
grades]: core Theorems 1–3 stable across the V11 and V15 passes (rev 3 on file —
exactly the record the §3.2 row displays); the consumed instance RRE(p, ZMod p) is
FORMALIZED Lean-core (`MovesD/R7_runRealizer.lean`, `runRealizerExists_zmod`); the
CUC §0 "VERIFIED homes (no pass condition)" registry line is cited as PROVENANCE,
not as a grade — the V11/V15 models/cleanliness are not displayed in-document, so
no VERIFIED claim is made here. The landing-flank countermodel search ran and
produced no negation (positive gates only).
Consumes [list added at REVISION 1, finding 5]: T-1/T-2's MacLane-frame vocabulary
(symbolic scope, Step 2); the coherence predicate `HistoryCoherent` — the "re-keyed
coherence" of the statement — which is the O-3 clause-3 re-key EXECUTED at DELTA-2
§D2.1 line 1 (governance on record, not an open input); MacLane 1936 via the LITUNIT
pins (§4) — **exact loci [REVISION 2, finding V2-7]: Def 4.1 (key polynomials) + Thm
4.2 (augmented values), the (V1) frame-semantics pin (LITUNIT §4, verdict MATCH)** —
consumed as the intended semantics of T-1/T-2's MacLane-frame vocabulary, the same
pins Steps 2/6 ride; NO further MacLane theorem enters O-14a′ (its core Theorems 1–3
are self-contained per the V11/V15 arc, and the consumed Lean instance rests on Lean
core only). The (S6b′) clause and the landing flank are INTERNAL — closed inside the
O14ap arc itself (rev 3), not consumed from elsewhere. The consumed instance
RRE(p, ZMod p) rests on Lean core only. The rev-3 honesty ledger's open flags
(OL-1/OL-2/(F1)) ride the NON-consumed tower strengthenings (Prop 5.4.2 sharpness,
Conjecture 5.4.C) and enter nothing here.
Delivers: CU-3's nonempty-locus witness.

**Step 10 (CU-1 — no-missing + interior adequacy + state binding; THE ONE NEW PROOF of
the K-RUN-C composition).** Statement: one ordinary induction on chain length, N-free:
for every Cons_f chain 𝐇 with N\*(𝐇) ≤ N, (i) the canonical run on f at working level
N realizes 𝐇; (ii) the engine's frame state after the realized prefix equals the stage
data of Θ(𝐇) (the OL-6 binding). Interior adequacy = per-site SPAN totality inside the
same step; ADEQ split interior/terminal with terminals routed to O5triple's counting;
no box-keyed universal statement anywhere.
Status: **VERIFIED — leaf #5** [REVISION 3 status sync, ledger 7b28562]
(arc: V27 UNSOUND, core repairable → rev 2 structural repairs (N-free induction,
interior/terminal split, construction-before-recovery, declared pins) → V29
SOUND-WITH-GAPS 0-critical → rev 3 (consumption accounting only; induction
byte-stable) → pass 2 fresh Fable, 0 criticals, 7 consumption-accounting/display gaps,
commit 252f34c → [the wave-10 VERIFIED adjudication at d730669 was CORRECTED AS
PREMATURE at the ledger, commit 110f308] → rev 4 light repairs, all 7 gaps, cores
§§1–3 byte-identical (commit 8ab8ca6) → gap-check: GAPS-REMAIN → rev 5 (commit
6299a4a, the single GAPS-REMAIN item repaired, cores byte-identical) → r5 gap-check
**GAPS-CLOSED, 0 criticals / 0 gaps** (the F-4 Lemma-R scope-note residual verified
repaired; §3/§4/SURJ spot-checks clean; commit 5329a4e) → **ADJUDICATED VERIFIED
(leaf #5) at the 2026-08-02-night harvest**, ledger 7b28562). The mathematical core
(N-freeness, creation-first, split-exhaustiveness) was confirmed by the pass-2
report; the r5 gap-check closed the accounting/display residue. Primary gate: the
depth-3 falsifier ran BEFORE rev 1 and SURVIVED.
Conditionality carried: the step consumes GD-2/GD-3(min)/GD-6 instances at orders ≥ 2
through **(H1)** [routing display synced at REVISION 6, finding F-3: the former
"through (GR-B)" routed the GD-6 conformance instances through a row scoped
GD-2/GD-3-only — the (H1) group now displays them as its member (c), so this line's
routing is supplied row-for-row], plus the declared pins **(P-ROOT)/(P-KEY)/(FR≡)**
((FR≡)'s
refutable order-2 content = the gate's coherent-convention finding; adjudicated
convention-pin [T], §3.3 pins row — REVISION 6, finding A-6(iv)).
Consumes: Steps 6–9 (D-14, OL-6, OL-2a-2-min/3, SPAN/(M6a)/(M6c)/VTX, REAL≡, the
realizer supply).
Delivers: the no-missing direction of realization faithfulness (K-RUN's heart); K-14
(I0)/(I1)/(I3-rc); the (FRESH) consumption site.

**Step 11 (CU-2/CU-2t/CU-3/CU-4/CU-5/CU-7 — the rest of the K-RUN-C composition;
"CUC").** Statements (per DELTA-2 §D2.3): CU-2 no-phantom = O-2a Theorem 2(D) ∘ ι;
CU-2t the terminal seam ((t-i)/(t-ii) as ONE lemma from O-2a Thm 2(B) saturation +
Cor 1.20/3.8 + TB-CAP windows); CU-3 nonempty locus (coherent form) = O-14a′ + D-14 +
(O6) — **(O6)'s supplier DISPLAYED [REVISION 1, finding 5]:** (O6) = the O3design §1
canonicalizations (branch-letter keying + deep-marker pooling), a DEFINITIONAL
bookkeeping convention for census-side counting with NO proof content (CUC §3(c)
states exactly this; the O3design record passed the V16/V21 arc) — a §3.3 pin row,
not an undefined leaf; CU-4 acceptance, selected-f (F4-S soundness at every N; F4-C
completeness at
N ≥ N\*, TB-CAP (τ-irr) perimeter-conditional); CU-5 the satellites K-OB-A/B/E; CU-7
the O5triple interface discharge ((I-1)(a,b,c) + (I-2), with the attribution correction:
the content is single-polynomial and consumes NO K-DICT-OB1).
Status: **PROVED-UNVERIFIED as a consolidation record** — wave 8 confirmed 6/7
re-labels mapped to proved homes (commit a9f294c); **CU-2t is PROVED at R3** (CUC §9,
unit D3CU2t) conditional on P(O-2a r5) + TB-CAP's D-12r perimeter + the GD residue
**including the THREE NEW GD-4 terminal rows (T-DEC)/(T-READ)/(T-VERD)** [count
AMENDED 2 → 3 at REVISION 4, folding the CU-2t R3 §9.5 touch (3); PROMOTED to the
§3.1 [M] group (H6) at REVISION 5, finding VC5-2 — with (T-READ) re-graded into the
GD-2/GD-5 conformance family at CU-2t R4] — genuinely new
unproved mandatory-path obligations first displayed at CUC §9.4, folded into GD-4 (no
new open FAMILY) but growing the residue by three rows; the residue-count history is
stated plainly per the R3's own discipline: rev 1 billed ZERO new open inputs
(OVERSTATED, retracted at R2 per the CUC F4 correction), R2 billed TWO, and the
honest count is THREE — the R2 itself undercounted by one, the (RC-read) supply row
(T-READ) (the CU2tpass G1 finding; §3.3 GD-4/GD-6 row synced). [REVISION 3 status sync:
CUC pass 1 RAN — **UNSOUND** (b9d1d70: 1 critical,
§9.3(iii)'s Θ^τ injectivity constant in ν under an unconstrained hypothesis row, + 9
gaps incl. the (T-DEC)/(T-VERD) residue-growth contradiction; the §6.1 audit SOUND;
clauses (i)/(ii)-main survive) → **CU-2t R2 LANDED** (unit of record 567814d;
52d4eed's staging sweep captured an intermediate state: the ν-dependence critical +
its root, the undefined record-consistency predicate, repaired via the displayed
Definition RC, §§9.2a/9.3 — Θ^τ now carries ν's announced verdict pair, injectivity
restated at two displayed granularities; CU-2 rescoped to the continuing part 𝐇°;
surviving cores byte-stable; full disposition table CUC §10).] [REVISION 4 status
sync: the R2 hostile re-pass RAN — **SOUND-WITH-GAPS, 0 criticals** (4190ca4: the F1
ν-dependence repair CONFIRMED at both injectivity granularities, Definition RC
non-circular as displayed; 4 gaps — G1 the missing (T-READ) third row, G2 a (T-DEC)
row-internal contradiction, G3 the unrouted W3-carrier/2(D)-scope dichotomy, G4 (c1)
unquoted) → **CU-2t R3 LANDED** (81589e9, light: (T-READ) displayed with per-half
consumption at the §9.4 tie — the read-entry half consumes (T-READ), the verdict
half survives on (T-VERD) + clause (ii) alone; (T-DEC) SPLIT (T-DEC-dec)/(T-DEC-cor)
— corner bookings fenced to the Corollary at its own conditionality; pricing
corrected to THREE rows with the 0 → 2 → 3 history said plainly; the W3
carrier finding + 2(D)-scope dichotomy ROUTED to the O-2a owner at the P(O-2a r5)
charge — §9.5 touch (5); verified cores byte-stable); the fresh hostile pass on the
R3 record is the in-flight debt (§3.2).] [REVISION 5 status sync: the R3 pass (unit
CU2tp) RAN — **SOUND-WITH-GAPS, 0 criticals, both R3 logic repairs VERIFIED** (2
pricing-prose gaps + 1 minor) → **CU-2t R4 LANDED** (unit of record b9f05db: G-alpha
— **(T-READ) RE-GRADED honestly out of the GD-4 "engine bookkeeping" label**: its
(RC-read) content is read-semantics CONFORMANCE, mathematics-grade, graded in the
GD-2/GD-5 family with the per-case decomposition displayed (at k′ ≥ 1 an OL-2-min
instance + the genuinely new terminal-BINDING clause, which stays GD-4; at k′ = 0 no
sibling anywhere in the OL residue); G-beta — the false "meanwhile EXECUTED"
fold-state note rewritten to the verified repo state; M-gamma — the §9.1 a₀ ≥ 2
carrier display; cores byte-stable) → R4 spot-check CLEAN (unit CU2tc, d14cd8c: 3/3
repaired sites verified, hunk map proves cores byte-stable, no further full pass
owed per the CU2tp disposition) → **CU-2t ADJUDICATED VERIFIED at scope (leaf #9)**
at the 6a7593b harvest — the terminal seam, conditional per §9.5 (the three terminal
rows + the pinned literature); the three rows are since REVISION 5 the §3.1 (H6)
group (finding VC5-2).] [REVISION 6, finding A-4 — this step's TWO conditionality
renderings reconciled, the earlier one marked: the R3-era sentence above ("PROVED at
R3 ... conditional on P(O-2a r5) + TB-CAP's D-12r perimeter + the GD residue
including the THREE NEW GD-4 terminal rows") prices NEW and INHERITED conditionality
TOGETHER and is SUPERSEDED AS A CONDITIONALITY DISPLAY by the §9.5/leaf-#9 form,
which prices the leaf's NEW conditionality exactly (the three terminal rows + the
pinned literature). The R3 sentence's remaining items are INHERITED debts, each at
its own ledger home — P(O-2a r5) the §3.2 [V] row; TB-CAP's perimeter the §4 D-12r
literature pin [re-pointed at REVISION 14, r15 — formerly the §3.1 (H3) row];
"the GD residue" the O-2a OL residue at its §3.1 (H1)-group/(H6) and
§3.3 homes — and its "including" is NOT a ⊇ license: no un-enumerated GD residue is
consumed via CU-2t beyond those displayed homes.] The quarantined Corollary
CU-2t-deep (the
GD-8 exact-key
corner) is conditional on the unpinned (REP-IRR) literature pull and is consumed by
NOTHING on the mandatory path.
Consumes (mathematical inputs): Steps 6–10 + (O6) as displayed above. The CUC §0
pending-verdict registry is METADATA — the live key the P(·) grades resolve through,
never a mathematical supplier [wording fixed at REVISION 1, finding 5].
Delivers: no-phantom + terminal verdicts + acceptance = with Step 10 the full
realization-faithfulness package (the old O-3), serving K-4/K-6's repairs, O-4's
OL1-ii, O-5, O-7's (X1)/(X4).

**Step 12 (O5triple — O-5 halt/assembly + the D-7 bijection triple).** Statement: at an
ns-free leaf of a realized run the halting verdict is the true factorization datum
((τ-hen) via LIT-3 coprime Hensel; (τ-irr) saturated leaf via OM saturation through the
dictionary — the (τ-irr) leg's TB-CAP dependence is perimeter-conditional on the
§4 D-12r pin (entry 10) [locally re-pointed at REVISION 15, r16, per VC17
blocker 1: the r15-5 audit had confirmed this step as already-conformant because
its consumption is priced at the §4 entry's enumeration, but the r15 claim was
that EVERY former consumption site displays the conditionality locally — Step 12
now matches Steps 2/3/11/16/19]); Theorem B derives D-7b LEAF-COVER from injectivity + degree checksums
alone (NO no-missing consumed — the F2′-as-assembly note, verified); D-7a
LEAF-DISJOINT and D-7c LEAF-MULT close the triple; Theorem E: any two monic separable
lifts of a decided class have EQUAL splitting types (the multiset is engine data,
lift-independent) — the σ-constancy Step 17 needs.
Status: **PROVED-UNVERIFIED** — rev 3 (commit 6fe1b2d), V23: NO criticals → rev-3
repairs 0-critical; pending pass **P(O5t r3)**, PLUS the five §2 flags
(GMN-COH)/(R-0)/(R-1)/(R-2)/(R-3) — GD-1/GD-7/GD-8 duties, NOT covered by the executed
LITUNIT. The abstract counting core is Lean-formalized (`MovesU/O5CountingB.lean`).
Consumes: Steps 7/8/10/11 via CU-7's interface; LIT-3; GMN Def 3.10/eq. (37) +
Cor 4.19 + LIT-2 defectlessness.
Delivers: correct semantic leaf labels for O-11's K1 (Step 18) and D-11's M3 (Step 17);
type(g) = typemult(T) for every MONIC SEPARABLE lift g of the decided class —
Theorem E's own scope [RESCOPED at REVISION 10, r11, edit E-M3, per the D-11 r4
REVISION-2 gap-6 record: the former "for every lift g" was BROADER than the
premise displays (ns-null justifies only a.e., not all-lifts); the nonseparable
locus is Haar-null (T-6 (ns-null)), so every mass/counting consumer is unaffected,
and the D-11 r4 §4A walk certifies no mandatory consumer reads the unrescoped
breadth (its sites 1–6 are M3-free/label-blind; site 7 consumes mass-mediated at
separable scope)].

**Step 13 (O-4 + O4T — drainage: undec(k) → 0 at every prime, wild included).**
Statement, in two layers: (SEMANTIC, unconditional) the M05/V2-verified core — Lemma A
(drainage ⇔ a.e.-decidedness), Lemma C (discriminant tail bound, wild term a finite
additive shift n·v_p(n)), Lemma D (Krasner local constancy at modulus 2v+1, explicit
constant, wild-valid), Theorem E (the semantic classifier drains at rate
~p^{−N/(2(n−1))} at every prime) — plus Dfloor's SEM-DRAIN making the measure content
exact. (TRANSFER, the canonical classifier) O4T: TR-0 (a.e.-finite decision level ⇒
env → 0), FLOOR (any sound stable classifier is bounded below by the semantic one),
TR-Q (quantitative transfer under a nondecreasing UNBOUNDED majorant), and THEOREM N3:
at 2 ≤ n ≤ 3 the canonical order-≤1 classifier terminates, is sound and stable, with
the SHARP read ceiling L(f) ≤ v_p(disc f) + 1 at every prime including wild — hence
pointwise OL1 and drainage with explicit envelope, UNCONDITIONAL at n ≤ 3 (no CU/GD≥2/
BNS consumed). General n: TR-OM is CONDITIONAL on exactly the displayed seam
**(AGR) = OL1-ii** — the canonical run on each separable f terminates below a finite
N(f) with the certification predicate accepting there — assembled from CU-2 + CU-4 +
GMN Cor 4.19 (the DELTA-1 §D1.4 adjudication: this qualitative route is the MANDATORY
one; rates/O-6/O-7/O-8a are route R-QNT, struck from the root).
Status [REVISION 1 status sync]: M05 core **VERIFIED at the V2-upheld scope** (items
5–8); O4T **REPAIRED AT REV 3, AWAITING ONE FULL CLEAN PASS** (arc: V30 UNSOUND →
rev-2 repairs → Fable pass 2 UNSOUND, one localized critical: Corollary N3-BRACKET's
terminal "=" false for N ≥ 2 by the level-normalization factor p^{n(N−1)}, plus 4 gaps
(import inventory short: Galois descent + char-0 separability) → **rev 3** (commit
bb4326c): the terminal "=" → "≤" with the exact p^{n(N−1)} normalization identity
displayed and the verifier's 8-vs-32 refutation shown, the density form kept as the
identity-bearing statement (§4 consumed only the inequality — nothing downstream
consumed the false clause); import inventory COMPLETED to eight named Facts — Facts
**SEP** (char-0 separability of irreducibles) and **DES** (Galois descent/fixed field)
added as in-line-proved imports with consumption-site cite tags, six-vs-eight
reconciled (EF/GRD each bundle two of V30's items); the every-lift-separability chain
displayed in N3's proof; cores byte-stable (diff-verified). The repair was CHECKED
CLEAN at the 2026-08-02 harvest (ledger, c43b13b), but one full clean hostile pass is
still OWED before any VERIFIED grade; the mathematical core has now survived
line-by-line audit twice. [REVISION 3 status sync: pass 3 (the acceptance pass) RAN —
**SOUND-WITH-GAPS, ZERO criticals** (ed413ae; harness independently re-run, 2.53M
classes, ceiling sharpness confirmed; all 11 charges PASS), two one-line gaps (the
(I1) base-case "≥ 1" criterion display — the consumed form verified correct — and the
"one external import" undercount vs M05 Lemmas B/D) → **r4 (the two one-liners)** —
**LANDED at revision close** (3956500: the (I1) criterion corrected "≥ 1" → "> 0"
with the verifier's derivation displayed; the external-import count corrected to
three with exact consumption sites; cores byte-stable) — **+ one confirming pass to
the bar** (ledger 7b28562); the core has now survived line-by-line audit three
times.] [REVISION 4 status sync: the confirming pass (pass 4, acceptance) RAN —
**SOUND, CLEAN PASS: 0 criticals / 0 gaps** (8602e97: all 12 charges + acceptance
items pass; the core independently re-derived including both CEIL corners; harness
re-run digit-for-digit vs the sealed JSON) → **O4T ADJUDICATED VERIFIED (leaf #7)**
at the confirming-passes harvest (ledger 85670f7); the "REPAIRED AT REV 3, AWAITING
ONE FULL CLEAN PASS" header above is SUPERSEDED — the M05 core's V2-upheld grade
stands unchanged.]
Consumes: Steps 1/10/11 + GMN Cor 4.19 + the O4T import inventory (§4).
Delivers: the undec(k) → 0 clause of (SQ) = the SEMANTIC layer — unconditional at
every n and every prime (Theorem E + Lemma A/C/D, with Step 1's SEM-DRAIN making the
measure content exact); since §1's undec(k) is DEFINED semantically, this is all
clause (SQ) consumes [attribution corrected DOWNWARD at REVISION 4, finding VC4-4 —
the former line's "(AGR)-conditional at general n" priced the TRANSFER layer into
(SQ)]. The TRANSFER layer (the canonical classifier's own termination + drainage:
unconditional at n ≤ 3 by Theorem N3; conditional on (AGR) (= CU-2 + CU-4 + Cor
4.19, all on file above) at general n) serves the classifier-side counting reading
consumed by Step 19's squeeze machinery inside clause (R)'s conditional scope, and
the algorithmic-decidability strengthening — NOT clause (SQ)'s statement.

### Movement IV — the value side (the tables evaluate correctly)

**Step 14 (O-9 — the order-r stratum census).** Statement: for a type of order r the
order-(r+1) stratum census over O/π^N is one ℕ-valued polynomial in q per stratum,
independent of p, O, δ (census field degree d = f₀⋯f_r — the DELTA-3 §D3.2 index fix
governs). Level-1 (order-1 strata, every n, every prime power, every complete DVR) is
PROVED (M08 Theorem 2, 46-strata machine check). Order ≥ 2: REDUCED to exactly TWO
pre-existing named opens — **(GR-B)** (enters as the anchored-march residue structure;
CEN-W conditional on it alone) and **(FRESH)** (the joint-with-parent separation
clause; CEN-J = THE census). TYPE-MARCH (+ the F₃ type-breaking countermodel) shows the
march is what MAKES the census polynomial: GD-3-FULL is a theorem-level dependency of
the value side. OL-A's ledger (A1/TRI/LED/(DET)) proved outright; OL-B discharged to
GD-3 per DELTA-2.
Status [REVISION 1, finding 4 + status sync]: **REPAIRED AT REV 2, RE-PASS OWED.**
Verify pass 1 NOT ACCEPTED (commit c17777f; 2 criticals: the census exponent E′'s
left-tail ceiling REFUTED by machine countermodel (256 vs 64 — overcount q^d per
integral left-tail slot), and JUNCTION-PIN's PROVED label unsupported; the verifier's
own sound-list: TYPE-MARCH + F₃, A1/TRI/LED, (DET), CEN-W's (GR-B)-only conditionality
— the reduction's SHAPE survives). The pass-1 verdict made the then-current [V] filing
DISHONEST (finding 4 is accepted: a refuted exponent is a missing proof, not a
verification debt). **The repair has since LANDED — rev 2, commit 01c21e6:** E′'s left
tail made STRICT (floor(line)+1; the countermodel now predicted exactly, 64 = 64), the
K5 predict-from-ledger falsifier added (11/11 cells exact; the rev-0 formula refuted
at every integral-left-tail case), E″ displayed and validated against M08 at r = 0, an
**(ADM)** admissibility hypothesis displayed (an explicit hypothesis on CEN-W/CEN-J,
now a §3.1 [M] row carried by (ROOT-C)(H3) [REVISION 2, finding V2-5]; the r2's
"automatic at orders ≤ 1" claim is SUPERSEDED — twice rescoped, see the r3/r4 syncs
below; the single operative definition is the §3.1 row's FULL-attainment form
[REVISION 4, finding VC4-2]), and JUNCTION-PIN's
PROVED label WITHDRAWN — honestly rescoped as OPEN, absorbed into **(FRESH) as its
clause (c)** (the telescope consequence is proved GIVEN the clause). The sound-listed
core is byte-stable. With the repair on file the row is again honestly [V]:
re-pass owed. [REVISION 3 status sync: the re-pass (pass 2) RAN — **UNSOUND** (1
critical, 3 gaps; af2293e): both pass-1 repairs (strict left tail, junction-pin
rescope) verified SOUND, but the rev-2 (ADM) display rests on a FALSE class-vs-value
attainment equivalence (r = 1 countermodel: e₁ = 3, h₁ = 2, weights {0,2,4}, on-line
β = 1 unattained — empty stratum, positive formula; the automaticity claim refuted at
that reading); K5 + harness records verified log-exact → **r3 with the VALUE-attained
(ADM) reading** (ledger 7b28562) — **LANDED at revision close** (460ea59: automaticity
rescoped r = 0 automatic / r = 1 displayed decidable inequality / general r ≥ 1
withdrawn; LED-top(ii) image-subspace fix; K6 falsifier 0 = 0 exact); pass 3 owed.
The §3.1 (ADM) row carries the matching note.] [REVISION 4 status sync: pass 3 on r3
RAN — **UNSOUND, 1 critical** (b7df5c2: the attainment ladder goes one rung deeper —
VALUE-attainment (dim G_{β_k} ≥ 1) is insufficient at PARTIALLY attained slots; the
count needs FULL attainment dim G_{β_k} = d; machine countermodel from the note's own
LED-top(ii) type: r = 1, d = 2, CEN-W display 73728 vs truth 49152 at q = 2, ratio
(q+1)/q per partial slot — every prior value gate ran at d = 1 where the notions
coincide; all four charged r3 repairs + the census layer verified clean) → **r4 with
(ADM) at FULL attainment LANDED** (unit of record 35faac7, draft captured at
e8e1839/031784c: dim_{F_q} G_{β_k} = s(β_k+1) − s(β_k) = d, still p-free per-datum
decidable; the r = 1 inequality re-derived for the FULL predicate
β_k ≥ h₁((h₁^{−1}β_k mod e₁) + (f₁−1)e₁), collapsing to r3's at f₁ = 1; the r3
"(GR-B) bijection of F_{r+1}" gloss WITHDRAWN — read injective on G_β with image V_β,
onto ⟺ FULL, propagated to §§5.1/6.3/6.4; automaticity: r = 0 STILL automatic
(dim = f₀ = d); K7 falsifier ALL PASS — the countermodel refuted + the FIRST d ≥ 2
VALUE gate exact, census q^14(q²−1)² at p ∈ {2,3}); **pass 4 in flight**. The §3.1
(ADM) row is rewritten to this single operative form at REVISION 4, finding VC4-2.]
[REVISION 5 status sync: pass 4 on r4 RAN — **SOUND, 0 criticals** (the FULL-
attainment (ADM) form verified as displayed; two gloss-survival display sites: the
r4-withdrawn "(GR-B) per-slot bijection" gloss surviving byte-carried in two summary
lines) → **r5** (9335057: the withdrawn gloss purged from both sites — §0 OL-A(iii)
re-pointed at the LINE clause with the onto ⟺ FULL-attainment qualifier, §1 (P5)
re-scoped to §5.1's onto-ness at fully attained β; every other byte verbatim rev 4,
diff-audited) → pass 5 (confirming) **CLEAN: 0 criticals / 0 gaps** (825378d: both
sites verified fixed against §5.1; K7 re-executed byte-identical; census layer
clean) → **O-9 ADJUDICATED VERIFIED (leaf #8)** at the 6a7593b harvest — the order-r
census layer at its honest scope (CEN-W/CEN-J conditional on (GR-B) + (FRESH) +
(ADM)-FULL, all displayed; the FULL-attainment ladder closed). The §3.1 rows are
unchanged by the adjudication — the [M] conditionality IS the verified scope.]
Consumes: Step 8 (GD-3), GMN Prop 2.7/Lemma 2.2/Defs 2.19–2.21 via pins.
Delivers: the transition weights of T-7s's tables as proven counts — K3's census half
for Step 18.

**Step 15 (O-10 — sibling independence at adapted cells; K-COUNT).** Statement, sharper
than charged: in factor coordinates over ℤ_p, the fibers of the level-N multiplication
map over a ρ-separated polydisc are constant and nonempty at EVERY N ≥ τ, of size
p^{Σ min(e_i, N−τ)} (Smith profile), = p^ρ exactly in the D-8 guard range N ≥ τ + e_max;
Theorem 2 = the adapted-cell (SIB) product law, UNCONDITIONAL in factor coordinates;
K-JAC = Lemma 3; OL-4/D-8r RESOLVED (no small-N anomaly). Application to the engine's
realized site strata is conditional on the two displayed interfaces **K-LOC** (= M07
OL-3) and **(I-τ)** — since REVISION 6 (finding F-1) the §3.1 [M] row **(H7)**: the
former "both priced into the GD families" had NO displayed [M] landing (no GD row
carried read-locality/normalization content), so the pair is now hypothesis-grade
with this step and Step 18's K1 as its displayed consumption sites.
Status: **VERIFIED at scope — leaf #6** [REVISION 3 status sync, ledger 7b28562]
(commit 3e9f199; pass 1 CLEAN, ec5e9a1: 0 criticals, 0 load-bearing gaps; pass 2
model-diverse SOUND-WITH-GAPS, 5296528 — all five charged proofs check, Lemmas 1/2
verified incl. the adversarial minor case, harness 68/0 reproduced; 2 justification
gaps: the D-8 value-vs-law split collapses definitionally (fix rides the next doc
touch) + one undisplayed box-count transport cite → **ADJUDICATED VERIFIED (leaf #6)
at scope** at the 2026-08-02-night harvest; the two gaps were repaired at r2, landed
at revision close (3956500) — VERIFIED stands per the wave-14 ledger, framing fixed).
Falsifier: `verification/o10_kcount_harness.py` 68 PASS / 0 FAIL, predictions sealed
pre-run.
Consumes: LIT-8c (Sylvester/resultant); GMN stage factorization **K-FACT — pinned
[REVISION 1, finding 8]: K-FACT = GMN Thms 1.15/1.19 + Thms 3.1/3.7** (the recorded
perimeter, `docs/GMN_citations.md`; the O-10 §8 honesty ledger consumes exactly this
citation set — all four loci already in the §4 GMN pin list).
Delivers: K1's product law for Step 18; T-6's (SIB-STEP) at the statement layer.

**Step 16 (O-8b + D-3 — the unramified-base package; clause (UB)).** Statement: the
root's "same R_τ at q = p^δ" clause. Route (adjudicated at GD dispatch): the GD
families and O-9 are stated over an ARBITRARY absolutely-unramified complete DVR from
the start (Steps 8/14 — GD23's generality audit V26-confirmed: no proof uses
completeness, perfectness, or a Teichmüller section), collapsing D-3 to per-leaf scope
hygiene PLUS the alphabet slice O-8b: at δ-stable position classes the re-based slot
map is F_{p^δ}-linear and a_δ = δ·a₁ — M14's conditional proof over the displayed
(R1)–(R4) (the T-1 stage laws re-proved verbatim over O_δ = W(F_{p^δ})).
Status: GD23's arbitrary-DVR scope **VERIFIED** (inside leaf #4); O-8b
**PROVED-UNVERIFIED conditional on (R1)–(R4)** (M14's conditional proof is ON FILE;
the (R1)–(R4) base itself is the unwritten-but-expected-mechanical §3.1 row); pending
pass **P(O-8b/M14)**, now a named §3.2 row [REVISION 3, finding B2 — it was the one
consumed PROVED-UNVERIFIED proof with no ledger row naming its pass]; N8 numerics
exhaustive (199,723 alphabets, 17 fields, 0 violations).
**Honest scope of what this step proves [REVISION 1, finding 7]:** (i) the GD/census
layers (Steps 8, 14) hold over an arbitrary absolutely-unramified complete DVR —
proved/stated at that generality from the start; (ii) the alphabet law
(F_{p^δ}-linearity of the re-based slot map + a_δ = δ·a₁) AT δ-STABLE POSITION CLASSES,
conditional on (R1)–(R4). This does NOT yet derive the theorem's global clause (UB):
missing are exactly (a) the per-leaf O_δ re-base audit for the leaves stated over ℚ_p
(the dictionary/CU/drainage layers — the "per-leaf scope hygiene" of the D-3 collapse,
declared but unwritten), (b) that δ-stable position classes exhaust the classifier's
realized ledger at every δ, and (c) that the full solve + type aggregation commute
with unramified base change. (a)–(c) = the displayed row **(UB-X)** in §3.1; clause
(UB) of (ROOT-C) is conditional on **(H1)–(H3)** (inherited through this step's
Consumes-list: Step 8 is (GR-B)-conditional at orders ≥ 2, Step 14's census is
(FRESH)/(ADM)-conditional at orders ≥ 2, and the (UB-X)(a)-transported
dictionary/CU/drainage layers carry (E-N) plus the §4 D-12r pin's TB-CAP perimeter
[the latter literature-conditional since REVISION 14, r15 — no longer an (H3)
member] — none of these evaporates over O_δ)
**+ (H4a) = (R1)–(R4) + (H4b) = (UB-X) + (H5) = (PACK)** (the actual-pack
correspondence — since REVISION 7, finding VC7-2, incl. its PACK-i/ii/iii row-law
clauses, AND since REVISION 10, r11, its FOURTH row-law **(PACK-iv)**, entering the
transported evaluation chain at its closed edge set: Step 18b's direct (RE)
consumption + Step 17's (ROW-Σ) delivery display, both riding to q ↦ q^δ —
inherited through the transported evaluation chain — the value tie
at q^δ runs through Steps 17/18/18b's pack consumption; added at REVISION 4, finding
VC4-3) **+ (H6)** (the terminal-seam engine-conformance group, inherited through the
same transported chain — the terminal-verdict and in-fence content does not
evaporate over O_δ; added at REVISION 5, findings VC5-1/VC5-2) **+ (H7)** (the
(I-τ)/K-LOC sibling-interface pair, inherited the same way — the transported
product-law chain still enters through K1's engine-stratum receipt; added at
REVISION 6, finding F-1) **+ (H8) = (K-CUT) + (H9) = (NSF)** (inherited through
the transported K1/K4 chain at q ↦ q^δ — the cut-decomposition content Step 18's
K1 regrouping consumes (M04 Lemma 3.3's hypothesis) and the ns-routing content of
the K1/K4 label chain do not evaporate over O_δ; added at REVISION 10, r11; this
step's display synced at REVISION 11, r12 — finding VC11-2), exactly as §1 now
displays [attribution
completed at REVISION 3, finding B1; run to the twelve-row scope at REVISION 11]. [REVISION 2, finding V2-2: (R1)–(R4) is NOT
(UB)-only — it also enters
clause (R) through Step 18's K3-c; the split (H4a)/(H4b) records this.]
Consumes [list added at REVISION 1, finding 5]: Step 8 (the arbitrary-DVR GD
statements + the V26-confirmed generality audit), Step 14 (the census over any
complete DVR), Step 2 (the T-1 stage laws as the re-proof template for (R1)–(R4)),
M14's conditional proof (the O-8b attempt on file), and the §3.1 rows
(R1)–(R4)/(UB-X). N8 is a flag, never a substitute.
Delivers: clause (UB) at the displayed conditional scope.

**Step 17 (D-11 first tranche — MASS-ID M1/M1′/M3: the pack side; the instance pin).**
[REVISION 1, findings 2/11: D-11 is STRATIFIED. Its M2 clause consumed (S2) = the
O-11 composition datum while O-11 consumed D-11's pack side — a genuine cycle as
originally displayed. The honest order is **D-11 {M1, M1′, M3} ≺ O-11 ≺ D-11 M2**, and
the steps now follow it: this step carries ONLY the first tranche; M2 is Step 18b.]
Statement [REWRITTEN at REVISION 10, r11, edit E1 — the r4 true-scope form; the
consumed leaf is `D11_rev4_2026-08-04.md` (REVISION 3), whose reference measures
are named PER CLAUSE]: (M1, one-step) every family-(i)/(ii)/(iii) solve entry of
𝔅_n at every prime power IS the Haar mass of its continuation event CONDITIONAL
on the block state law C_e; family-(v) atoms at their per-occurrence (a′) readings
(MEASURE-SPECIFIC: ambient boxes C_e/R_e/□(P) incl. Lemma (BOX-VOL)'s relaxed-box
reading of the L6a factor; the CONDITIONAL ratio P(R_e | C_e) = q₀^{−(E−e)};
per-cell singleton residuals; the (O3) scalings NOT masses — their whole mass
content is (a)'s resummed-event bullet); families (iv)/(vi)/(vii) excluded
IN-STATEMENT ((a″) — family-(iv) verdict rows under arbitrary α carry no per-entry
mass identity). Σ = 1 holds at the displayed ONE-STEP STRATA (M1(d)); member-row
normalization at (RE)/(PACK-iv) members is **(ROW-Σ), a PROVED lemma** (the leaf's
§2A.2 theorem, adjudicated AS A PROOF at the r4 passes): solved totals
Σ_σ β_e(σ)(q₀) = 1, and the per-booking verdict-row totals are the Corollary-D
termination margins Φ^{(O1)}/Φ^{(O2′)}/Φ^{(O3)} — NO booking's displayed row total
is 1. Multi-step disjointness holds at prefix-free families (**(b-PF)**, proved);
grammar exhaustiveness = **(b-EX), a PROVED lemma at its route-(a) scope**
(quantified over the EXPLICITLY RESUMMED row terms — the (O3) kernel resummation
and the (O3)-scaled exit rows; solved-value legs are non-booked continuation
values, excluded); the infinite loop-0 word and the finite-time Z_e encounters per
**(LOOP-∞)** (null + disjoint, exact decomposition). Per-block a.s. exit is
UNCONDITIONAL; multi-block a.s. termination lives at Lemma M2.0's
(S1-m)+(DET) conditionality with the (O-1)–(O-5) + (O-GLOB) write-outs PROVED at
that displayed billing (Step 18b's layer). All of M1 at this scope is
UNCONDITIONAL over the VERIFIED O-12 lemma base (L1–L6′). (M1′) the (O2)
verdict-row coarseness finding (gate object, not a density carrier). (M3) row
labels are true types AT SEPARABLE-LIFT SCOPE: σ-constancy across one continuation
event's cylinder for every MONIC SEPARABLE lift (Theorem E's own scope; ns-locus
Haar-null, so mass/counting consumers unaffected) ← O5triple Theorem E + D-15
(per-box lift uniformity) — NO ∼_OM/OB2 consumption (the DELTA-3 §D3.1 audit,
re-confirmed constructively by this unit; zero root-side BNS touch-points); the
routing of these labels into the COUNTED corpus is priced at **(H9) = (NSF)**
(§3.1: (NSF-INT) for the pin → predicate → corpus chain incl. its displayed
(K1-CANON) premise — the row's residual price; (NSF-KEY), the as-built keying,
is RETIRED as of REVISION 13, r14 — Unit C executed, artifacts at the §3.1 row).
OL-O12-2's mathematical half CLOSED here.
Status [REVISION 1 status sync]: **PROVED-UNVERIFIED, r2 owed** (attempt landed,
commit 0cae7d8; wave-10 adjudication: "THEOREM M1 PROVED UNCONDITIONALLY over the
VERIFIED O-12 lemma base"; verify pass 1 NOT ACCEPTED as billed, commit 2587c70 — 1
critical: M1(c)'s MULTI-BLOCK a.s.-termination billing consumes open (S1) while
ledgered unconditional (per-block survives unconditional; contained), + 3 gaps; M1(a,
b,d), the (O3) resummation, and the uniqueness pivot SURVIVE → D-11 r2 owed).
[In-flight at REVISION 1 close: **r2 LANDED**, commit 566b967 — the critical contained
exactly as specified (M1(c) rescoped to per-block unconditional content; multi-block
a.s. termination MOVED to Lemma M2.0 at M2's (S1) layer; the (O2′) balance leg
DERIVED; a THIRD seam **(S3)** = the projective assembly weight DECLARED, owner annex
A-1, monic scope pinned on M2's statement); pass 2 launched.] [REVISION 3 status
sync: pass 2 (V31) RAN — 2 criticals, both CONTAINED at **r3** (8a0786c: M1(a)
rescoped to families (i)–(iii) with displayed exclusions; the completed-node
overclaim WITHDRAWN — status honestly LAYERED, clause 4 = the actual-pack
correspondence OPEN as cited; Lemma M2.0 rewritten over countable symbolic
addresses); pass 3 owed.] [REVISION 4, finding VC4-3: the OPEN clause 4 is now a
LEDGER ROW — the §3.1 [M] row **(PACK)**, carried by (ROOT-C)(H5): what Steps 18/18b
consume of this step is the M1/M1′/M3 layers at their displayed conditionality PLUS
that correspondence, and the correspondence has no proof on file — it cannot ride
the §3.2 D-11 [V] row (a pass-debt class requires a proof on file), so it is priced
as hypothesis-grade open mathematics.] [REVISION 10 status sync, r11, edit E2: the
owed pass 3 RAN — **CRITICAL** (`D11_owedpass_2026-08-03.md`: 6 criticals/3 gaps —
M1's conditioning scope, the family-(v) factor-as-mass claim, the §5/§7 billing,
M1(b)'s multi-step disjointness, M2's universal scope, M3(b)'s silent clause-4
consumption) — the STOP-THE-LINE event that SUSPENDED the Phase-C acceptance
(ledger 44ce0a7). Repaired at TRUE SCOPE per the five-round repair design
(`R11_REPAIR_DESIGN_V5_2026-08-04.md`) as **r4** = `D11_rev4_2026-08-04.md`, then
hostile-passed TWICE: r4 pass 1 (`D11r4_pass1_2026-08-04.md`) CRITICAL — folded at
the leaf's REVISION 2; r4 pass 2 (`D11r4_pass2_2026-08-04.md`) **GAPS-ONLY: no
critical remains, all six pass-1 repairs adjudicated VALID, and the design-§7
obligations ((b-EX) at route (a), (O-1)–(O-5), (O-GLOB), (ROW-Σ)) adjudicated AS
PROOFS**; its three bookkeeping gaps folded at the leaf's REVISION 3. This step
consumes the REVISION-3 text; the leaf's own clean bar (a confirming pass on that
text) rides the §3.2 D-11 row.]
Falsifier: N6-regp-ext2000, 303/303 primes < 2000, all EMPTY, margin sharp at 6/7.
Consumes: Step 5's lemma base (as the mass statements they are), Step 6 (D-15), Step
12 (Thm E), T-3/T-6 mass laws; the §3.1 [M] rows this step's own Statement/Delivers
display — **(H9) = (NSF)** (the M3 label-routing price) and **(PACK-iv)** ((H5),
entering through the (ROW-Σ) delivery display's (RE) hypothesis at the consumed
member) [summary synced at REVISION 12, finding VC13-3, per the post-VC11-5
convention: Consumes summaries carry the consumed [M] rows]. NOTHING from Steps
18/18b.
Delivers [synced at REVISION 10, r11]: the pack-side mass laws AT the r4 per-clause
scope + true row labels (separable-lift scope, at the (H9) price) that Step 18's
K1/K2 consume; **(ROW-Σ)** as a PROVED lemma in this tranche — per the leaf's §2A.4
closure walk this Statement display is (ROW-Σ)'s ONLY delivery edge (no consuming
proof line on the mandatory path reads a member-row normalization, relative to the
displayed residue (EDGE-RES)), and THROUGH this edge (ROW-Σ)'s (RE) hypothesis at
the consumed member = **(PACK-iv)** ((H5)) enters this tranche; D-9k's escape input
at the pack; the (r1) uniqueness pivot Step 18b re-uses.

**Step 18 (O-11 — SERIES-TIE: per-τ tree series = R_τ).** Statement — displayed
exactly, since Step 19's attribution rides on it [REVISION 1, finding 11]:

    (O-11)  for every type τ: the σ-graded tree series of T_can, assembled at the
            intended 𝔅_n instantiation, EQUALS the block-solve value R_τ in ℚ(q).

This is the SERIES = SOLVE-VALUE tie ONLY — it does not by itself say any Haar density
equals anything (that is Step 18b). Proof: the M04 Theorem-4 walk (least fixed point,
Bekić triangularization, Neumann under spectral escape; proved self-contained)
re-priced at the D-9 corrected dependency set: K1 ← O-10 (guarded) + (I-τ)→GD [the
(I-τ)/K-LOC interface = §3.1 (H7) since REVISION 6, finding F-1 — without it K1 does
not receive O-10's product law at the engine's strata] + CU
leaf labels [which carry the terminal-seam (H6) content and (FRESH) — the A-5 route
leg] + the Lemma-3.3 shallow-cut regrouping, whose cut hypothesis is **(H8) =
(K-CUT)** (§3.1) [NEW at REVISION 10, r11, from finding G-2 of the un-banked O-11
hostile pass of 2026-08-03 (`O11_owedpass_2026-08-03.md` — recorded at the §3.2
O-11 row; provenance disambiguated at REVISION 12, finding VC13-4): the
exactly-one-decomposition / read-ownership / weight-preservation kernel that M04
Lemma 3.3 consumes — an open kernel of T-6 previously displayed nowhere in this
document] + the ns-ROUTING of K1's labeled corpus at **(H9) = (NSF)** (§3.1) [NEW
at REVISION 10, r11: the (H9) TARGET — at any instantiation satisfying the
interface pin (q-4), every K1/K4-labeled box lies in the decided-ns-free counted
corpus and D-15's exact-classification package applies with no a.e. weakening —
holds at the INTENDED/PINNED instantiation at the row's priced members — the
OPEN price since REVISION 13, r14, is (NSF-INT) (incl. its displayed (K1-CANON)
premise); (NSF-KEY), the as-built keying face, is RETIRED (Unit C executed —
the guarded carrier landed at HEAD, the M02 V₀ exact-square poison dead; §3.1
row artifacts + compiled witness H9-U1), so the former "must NEVER be read as
covering the HEAD Lean wiring" rider is DISCHARGED — the residual display rule:
not dischargeable while (NSF-INT) stands priced; consumption is two-channel per the D-11
r4 §4A audit: (ch-1) K1's exact combinatorics is label-semantics-blind, (ch-2) the
semantic tie is mass-mediated at Step 19's squeeze; (CH-COV) executed CLEAN at the
walked closure, corpus-relative residue (CH-RES)]; K2 ← O-12 at 𝔅_n + D-11 PACK
SIDE (= Step 17's tranche, never M2) + the
**(PACK)** correspondence-plus-row-laws (§3.1, = (ROOT-C)(H5): that the concrete
assembled pack IS
the intended 𝔅_n instantiation entry-for-entry — Step 17's OPEN clause 4, consumed
here and at Step 18b as the identification the tie evaluates through [REVISION 4,
finding VC4-3] — AND, since REVISION 7, finding VC7-2, the three pack row-laws
**(PACK-i) rsh_interp / (PACK-ii) r_bdd / (PACK-iii) wsh_ok**, consumed by this
step's restated D-11 entry item (v), sub-attribution exact [synced at REVISION 8,
finding VC8-2]: (PACK-i)/(PACK-ii) at THIS K2 evaluation glue (the Rsh → Rval
conversion and the [0,1] bound behind ENNReal.ofReal); (PACK-iii) at K3 below
(the evalAt regular-subring membership, with D-6); the FOURTH row-law
**(PACK-iv)** sem_alloc (since REVISION 10, r11) is NOT consumed at this step —
its closed two-edge set is Step 18b DIRECT + Step 17's (ROW-Σ) delivery display
[noted at REVISION 12, the global enumeration audit: (H5) has FOUR members
i/ii/iii/iv per §1/§3.1; this step consumes exactly three]: they
are EXPLICIT (H5) clauses because entry-for-entry equality transfers a law to the
concrete pack only if a displayed theorem establishes it at the corresponding
𝔅_n rows, and no consulted record displays one — the correspondence alone does
NOT supply them); K3 =
[1v]-FULL value correctness ASSEMBLED from T-7s + O-9's census + O-8b
(arbitrary-DVR), with **(PACK-iii) wsh_ok** the membership input to K3's
evaluation ring-hom evalAt (with D-6) [REVISION 8, finding VC8-2];
K4 ← Dfloor/D-15/CU mass semantics — D-15's supplier scope is "T decided ns-free",
so K4's in-scope invocation at the labeled corpus rides **(H9) = (NSF)** exactly as
at K1 [(NSF-INT)(ii)'s K4 leg is proved at the interface pin in the D-11 r4 §4B
leaf proof; the as-built HEAD keying — (NSF-KEY) — RETIRED at REVISION 13, r14
(Unit C executed; formerly the failing coincidence, REVISION 10, r11); the open
residue here is (NSF-INT) incl. (K1-CANON)]. Every consumption displayed (the unit's 20-row
table). δ-stable interface note [REVISION 3, finding A5; PRICED at REVISION 4,
finding VC4-1]: O-8b's law is declared AT
δ-STABLE position classes only (Step 16), and that K3-c's realized δ > 1 pools are
δ-stable position classes is a match ASSERTED at the D-c interface (O-11 r2/r3's
declared row) with NO proof or [V]/[T] row supplying it. It is therefore no longer
carried as a presumption inside (H4a): the fragment is NAMED **(K3-δ)** = (UB-X)(b)
restricted to K3-c's realized pools, and clause (R)'s displayed attribution now
CONSUMES it ((H1)–(H3) + (H4a) + the (K3-δ) fragment of (H4b) + (H5) + (H6) +
(H7) + (H8) + (H9) — §1's full clause-(R) set [this "— §1" quote synced at
REVISION 12, finding VC13-2: the former parenthetical stopped at (K3-δ),
understating §1 by four groups, and called (ROOT-C) "the joint five-hypothesis
theorem" — a REVISION-4-era count against the current (H1)–(H9) roster]; the
theorem's hypothesis roster is unchanged by the NAMING since (K3-δ) ⊆ (H4b) —
the naming moves nothing in or out). A direct match display (or a re-scope)
rides the next O-11/O-8b touch and would return clause (R) to (H4a)-only at this
interface.
H.6 fence display [unit TREE-GAPS, audit gap G-4]: K3-a consumes T-5's H-DOMAINS
package at its VERIFIED-DUAL perimeter, whose named open kernels are the tree T-5
row's six H.6 fences (XHD-d-EX(∂), M4b-T-AUD, U-R-COUNT, XHD-u-X, H-LIST-GEN, E-POS —
roster with owners/refuters at `ASSEMBLY_PLAN_2026-07-26.md` REV 15/16). The tree's
"six fences gate [1v]-FULL" line is SUPERSEDED AS ROUTED by the O-11 r2/r3
restructure (K3-a, the G-5 repair): NO fence is consumed here as an open general
lemma — the ROOT-consumed residue is the SINGLE condition that the built per-n tables
sit INSIDE the fenced perimeter, proved nowhere at general n (V-n3 178/178 / H-n3
151/151 are sealed n = 3 evidence), = D-11's restated-charge item (vii).
**[REVISION 5, finding VC5-1 — the routing below is SUPERSEDED:]** the former display
("carried at the §3.3 widened-charge row ... No (ROOT-C) hypothesis is affected: the
condition rides D-11's PENDING tier-1 grade (Steps 17/18), not (H1)–(H4b)") is
RETRACTED as the VC5 blocker: the condition is an OPEN PROPOSITION consumed here on
clause (R)'s mandatory path (Step 18's K3-a requires it), expressly "proved nowhere
at general n" — under the ledger's own classification rule it cannot sit in [T].
Item (vii) is PROMOTED to the §3.1 [M] group **(H6)** (with CU-2t's three terminal
rows), consumed by clause (R); the §3.3 widened-charge row keeps items (v)/(vi)
only. The escalation clause survives inside (H6): if a built pack ever NEEDS fenced
territory (sharpest: H-LIST-GEN's beyond-(TRI) partitions), the fence kernel so
touched becomes ADDITIONAL consumed open mathematics beyond (H6)'s conformance
condition. [Concurrency sync, REVISION 4 — historical: (H5) = (PACK) is D-11's
clause 4, a DIFFERENT D-11 clause from item (vii); the R4 flag "whether (vii) too
should be promoted is flagged to the next VC pass" is RESOLVED here — VC5 promoted
it.] Full adjudication + the fence-by-fence mapping table: tree DELTA 4 §D4.2.
Status [REVISION 1 status sync]: **PROVED-UNVERIFIED (ASSEMBLED), r2 owed** — commit
3fc8f52; tier-1 residue exactly **{(GR-B), (FRESH), CU-2t, D-11 first tranche}**;
verify pass 1 NOT ACCEPTED AS STATED (commit a7ede72 — 1 critical: (M6b)(iii) consumed
at K4-CYL/row 4 but priced into no tier, so the "exactly FOUR" residue display is
unsupported as written, + 8 gaps incl. reductions asserted by pricing-citation; the
equivalence directions and the walk itself CLEAN; drainage verifiably unconsumed) →
O-11 r2 owed. [In-flight at REVISION 1 close: **r2 LANDED**, commit 1bd8bd1 — the
"exactly FOUR" display withdrawn, restated as the tier-1 four + THREE DECLARED
interface rows with ledger homes HERE: **D-a** = (M6b)(iii)+(ii−) → the §3.3
GD-4/GD-6 row; **D-b** = (I-τ)/K-LOC → the §3.3 interface row; **D-c** = (R1)–(R4) →
the §3.1 [M] row (= (ROOT-C)(H4a) since REVISION 2: through K3-c this row conditions
clause (R) itself, not only (UB) — finding V2-2); V1 corrected to the ENGINE-PROXY
form; NEW supplement
`o11_v2_oracle_pin.py` (10/10, engine ≡ oracle as rational functions) closes the
executed-vs-sealed gap; pass 2 launched. The r2's §9 owed ROOT touches are EXECUTED
in this document — see §7.] [REVISION 3 status sync: pass 2 (V32) RAN — billing
precision items → **r3** (7aef1b9: tier 2 SPLIT into hypothesis-grade rows H-1
((ADM), consumed at K3-b) / H-2 (D-12r — since REVISION 14, r15, the §4
literature pin, no longer hypothesis-grade) / H-3 (the O5triple five flags) / H-4
(LIT-1), the "nothing in tier 2 adds an open lemma" claim WITHDRAWN; §7.1 oracle-pin
premises displayed; walk/cores byte-stable); pass 3 owed.]
Falsifier ran FIRST: `verification/openmath/o11_seriestie_check.py`
including Finding F1's test at the real-pack proxy (the n = 2 solve reproduces the
true triple identically in ℚ(q); n = 3 five types symbolic; monotone bracket
exhaustive p = 2, 3, 5; the pass-1 record corrects the count to 37/37 live).
Consumes: Steps 1, 2 (DIRECT [added at REVISION 13, VC15 advisory 1, the
VC11-5/VC13-3 summary convention]: K3's assembly line consumes T-7s — "K3 =
[1v]-FULL value correctness ASSEMBLED from T-7s + O-9's census + O-8b" — and K3-a
consumes T-5's H-DOMAINS package at its VERIFIED-DUAL perimeter; neither rides
through Steps 5/17, whose own Consumes-lines take only T-8/T-3/T-6; the open
residue of those layers is priced on the displayed K3/K3-a lines at (H1)/(H6)(vii);
2 ≺ 18, acyclicity unaffected), 5, 6, 10–12, 14, 15, 16, 17 — all EARLIER. NOT
consumed: D-11 M2 (Step 18b) — the cycle is gone by stratification, not by fiat.
Delivers: the series = R_τ half of clause (R)'s evaluation.

**Step 18b (D-11 second tranche — MASS-ID M2: the density IS the series).** [NEW STEP
at REVISION 1, findings 2/11.] Statement:

    (D-11 M2)  for every type τ: α(n, τ; p) — the Haar density of clause (0) — equals
               the σ-graded tree series of T_can; equivalently, the solved values are
               the real conditional densities.

Proof [REWRITTEN at REVISION 10, r11, edit E3]: the D-11 **r4** M2 argument
(`D11_rev4_2026-08-04.md` §3, REVISION 3 — the twice-passed repaired leaf) **at its
(RE) scope**: Theorem M2 (r4) is a theorem about members satisfying the re-entrant
booking-profile hypothesis (RE) — every block booked (O1)/(O2′)/(O3), never (O2);
no allocation datum; family-(iv) rows the canonical 0/1-coefficient assembly with
only the displayed (O2′)/(O3) adjustments — with arbitrary-α members EXCLUDED
IN-STATEMENT (the counterexample class of finding C6 of the D-11 owed pass — pass 3,
`D11_owedpass_2026-08-03.md`, the §3.2 D-11 row [pointer disambiguated at REVISION
12, the global enumeration audit]; for them only O-12's pole/zero
theorem speaks). At the CONSUMED member, (RE)+row-form is PRICED as **(PACK-iv)**
((H5)'s FOURTH row-law, NEW at REVISION 10; the former definitional-discharge
display — that O-12's §1 booking sentence pins the consumed member's profile — is
WITHDRAWN, design §3.3). The repaired proof step consumes M1(a) at families
(ii)/(iii) ONLY (the r4 rescope; never family (iv)), and M2's exhaustiveness note
consumes M1(b)/(d) + (b-PF)/(b-EX)/(LOOP-∞) at their proved r4 scopes. Seams as
before: **(S1)** = the GD-3-FULL/O-9 residual-descent hand-off ← Step 14 (+ its
(GR-B)/(FRESH)/(ADM) conditionality, §3.1 — the census is consumed at its full
displayed hypothesis set [(ADM) added at REVISION 4, finding VC4-5]); **(S2)** =
the O-11 composition datum ← Step 18. (r1) is
the uniqueness pivot (Step 17). With Step 18 this closes the chain
α = (tree series) = R_τ — as an identification of the CONCRETE pack's densities with
the solve values, conditional also on **(PACK)** (§3.1, (ROOT-C)(H5)) through the
Step 17/18 consumption [REVISION 4, finding VC4-3]: the concrete pack's conformance
to the member is (H5)'s correspondence clause, and the member's booking/row-form
profile is (PACK-iv) — consumed HERE directly (Theorem M2's (RE) instantiation)
and, per the leaf's §2A.4 closed edge walk, at exactly ONE other place: Step 17's
(ROW-Σ) delivery display. NOTHING of M2 flows into Steps 17/18 (acyclicity intact:
(ROW-Σ) is M2-free at Step 17's layer).
**Scope: MONIC — and the third seam (S3) is NOT consumed here [REVISION 2, finding
V2-1].** M2's stated conclusion is about the monic ensemble — exactly clause (0)'s μ
(monic degree-n ≅ ℤ_p^n), which is all this walk needs. D-11 r2's third seam **(S3)**
(the mass reading of the projective assembly weight (q−1)/(q^{n+1}−1), owner annex
A-1) conditions ONLY the PROJECTIVE reading of M2's conclusion, per the r2's own
scoping ("M2 as stated (monic ensemble) does not consume it"; its §3 seam display and
Theorem M2's bracket) — and no §2 step consumes that reading: the projective
normalization is ANNEX by standing directive (§1 "Relation to the tree's root").
Step 5's pole-free claim covers the weight as a RATIONAL FUNCTION ((g2) of the O-12
grammar); its mass reading enters nowhere on the mandatory path. The proof above
therefore names (S1), (S2), (r1) and nothing else — correctly, not by omission.
Status [REVISION 10 status sync, r11]: **PROVED-UNVERIFIED at the r4 two-pass
record** — same leaf arc as Step 17's E2 display: pass 3 CRITICAL (M2 false at its
displayed universal scope, the C6 finding — the suspension trigger) → r4 restated
AT (RE) with the proof step re-derived from the verified §2.3(iv) grammar display →
r4 pass 1 validated "M2's (RE)/(PACK-iv) scoping" → r4 pass 2 GAPS-ONLY, no-churn
confirmed on M2; the leaf's confirming clean pass rides the §3.2 D-11 row. Lemma
M2.0 (multi-block a.s. termination) is PROVED at its displayed (S1-m)+(DET)
billing via the loop-extended addresses + (O-1)–(O-5) + (O-GLOB), adjudicated as
proofs at the same passes.
Consumes: Steps 14, 17, 18 — all earlier; the (GR-B)/(FRESH)/(ADM) rows through
(S1) [(ADM) restored to this summary at REVISION 11, r12 — advisory VC11-5: the
proof paragraph above already consumes the census at its FULL displayed set];
(PACK-iv) ((H5)) directly.
Delivers: the α = series identification; with Step 18, the α(n,τ;p) = R_τ(p)
evaluation clause of (R).

### Movement V — the closer

**Step 19 (T-U + K-13 + O-12 — the squeeze fires at every prime).** THEOREM U
(VERIFIED-DUAL, conditional on its displayed §U.SQ.6 kernel ledger — whose
coincidence with the Steps 1–18b roster plus the recorded evictions/annex items is
since REVISION 6 DISPLAYED ROW-BY-ROW at §3.4 [the A8 → VC5-3 → VC6 A-9 standing
audit debt, EXECUTED: per-item pointers §U.SQ.6 → step/eviction/annex, with the
Lean-bridge kernel rows cross-walked; VC7 CONFIRMED the table (findings 5/6) and
the §3.3 audit row is RETIRED at REVISION 7, kept as tombstone]) takes: the bracket
at every finite level (Step 1
+ D-15 + T-6 — of these, clause (SQ)'s SEMANTIC bracket is Step 1's ALONE,
unconditional; the D-15/T-6 legs serve the classifier-side counting tie inside the
evaluation chain, at clause (R)'s conditionality — T-6's TB-CAP leg
perimeter-conditional on the §4 D-12r pin [re-pointed at REVISION 14, r15]
[re-attributed at REVISION 4,
finding VC4-4 — this supersedes the REVISION-3 A6(ii) phrasing "the (SQ) bracket
consumes T-6"]), drainage
(Step 13 — the SEMANTIC layer, unconditional; the classifier-transfer layer likewise
rides the evaluation squeeze, not (SQ) — finding VC4-4), the evaluation chain —
**attribution exact [REVISION 1, finding 11]: Step
18 delivers series = R_τ (O-11); Step 18b delivers α = series (D-11 M2); TOGETHER they
deliver α = R_τ — neither alone does; the chain carries (H4a) = (R1)–(R4) through
Step 18's K3-c (the base-changed β-legs) [REVISION 2, finding V2-2], the (K3-δ)
fragment of (H4b) at the same interface [REVISION 4, finding VC4-1], (H5) =
(PACK) through Steps 17/18/18b's pack consumption [REVISION 4, finding VC4-3;
since REVISION 7, finding VC7-2, incl. the row-law legs — PACK-i/ii at K2's
evaluation glue, PACK-iii at K3's evaluation ring-hom (with D-6); sub-attribution
synced at REVISION 8, finding VC8-2; since REVISION 10, r11, incl. PACK-iv at its
closed edge set — Step 18b direct ((RE)) + Step 17's (ROW-Σ) delivery display],
(H6) = the terminal-seam engine-conformance group through Step 18's D-a/K4-CYL
consumption of CU-2t's terminal verdicts (Step 11's route), through the CU-leaf-label
leg into K1 (the A-5 route leg, REVISION 6), and K3-a's in-fence
condition [REVISION 5, findings VC5-1/VC5-2], (H7) = the (I-τ)/K-LOC pair
through Step 18's K1 receipt of O-10's product law at the engine's strata
[REVISION 6, finding F-1], (H8) = (K-CUT) through Step 18's K1 regrouping (M04
Lemma 3.3's exactly-one weight-preserving cut hypothesis) [REVISION 10, r11], and
(H9) = (NSF) through Step 18's K1/K4 label routing and Step 17's M3 leg (via
Steps 11/12) [REVISION 10, r11; this closer synced at REVISION 11, r12 — finding
VC11-1]** —
and the
pole-free specialization at every prime power (Steps 4 + 5 + 17; every self-loop
denominator at e ≥ 2 by Step 5's (SL≥2) display [REVISION 2, finding V2-6]) — and
squeezes: decided_τ(k)/p^{nk} → R_τ(p) = α(n,τ;p)
at EVERY prime including wild. **Σ_τ R_τ = 1, concluded HERE [REVISION 3, finding
B3]:** Step 1 supplies Σ_τ α(n,τ;p) = 1 at every p plus the interpolation principle;
Steps 18 + 18b supply α(n,τ;p) = R_τ(p) at every p (conditional on (H1)–(H3) + (H4a)
+ (H5) + (K3-δ) + (H6) + (H7) + (H8) + (H9) [the last two since REVISION 10, r11;
this summary synced at REVISION 11, r12 — finding VC11-1],
already inside clause (R)'s displayed scope); hence Σ_τ R_τ agrees with 1 at
infinitely many prime powers, and Σ_τ R_τ = 1 in ℚ(t) — no new hypothesis; the former
attribution "from Step 1" over-credited the VERIFIED leaf beyond its declared
statement. Clause (UB) from Step 16
at its displayed conditional scope ((H1)–(H3) + (H4a) + (H4b) + (H5) + (H6) + (H7)
+ (H8) + (H9) —
completed at
REVISION 3, finding B1, at REVISION 4, finding VC4-3, at REVISION 5, findings
VC5-1/VC5-2, at REVISION 6, findings F-1/F-2/F-3, and at REVISION 10, r11, for
(H8)/(H9) through the transported K1/K4 chain [synced at REVISION 11, r12 —
findings VC11-1/VC11-2]). This is exactly (0) + (SQ)
+ (R) + (UB-conditional): **(ROOT-C)**, quantifier-for-quantifier. ∎ (conditional
exactly on the (ROOT-C) hypotheses (H1) (the enlarged group — four members since
REVISION 7), (H2), (H3),
(H4a), (H4b), (H5) (incl. PACK-i/ii/iii since REVISION 7 AND PACK-iv since
REVISION 10, r11), (H6), (H7), (H8), (H9) —
attribution
per clause displayed at §1, with clauses (0) and (SQ) UNCONDITIONAL [REVISION 4,
finding VC4-4]; the §3.2/§3.3 rows are audit debts on
the composition, displayed, not hypotheses)

**Composition audit trail (rewritten at REVISION 1).** Acyclicity, edge-checked: Step
1 consumes no later node (the former T-6 attribution re-pointed to its own Appendix A
— finding 1); Steps 2–5 are symbolic/regularity and consume only earlier steps (B-1..
B-7 grades displayed at Step 2 — finding 3); Steps 6–13 form the DELTA-2 recut order
(the O-2/O-3 cycle decomposed into O-2a upstream + the CU composition; OB1/OB2
evicted), with every formerly implicit input now displayed (C.1.5, D-5, (O6), Step-9
Consumes — finding 5); Steps 14–18b consume only earlier steps, with the former
D-11/O-11 cycle stratified as 17 ≺ 18 ≺ 18b (finding 2); Step 19 consumes 1–18b. The
V6 completeness review (26 findings) and the V5/V16/V21 architectural rejections were
each dispositioned in the tree's DELTAs; every countermodel on record is explained by
the repaired statements and none touches the root (tree §2.3 consistency note); VC1's
composition findings are dispositioned in §7.

---

## 3. THE CONDITIONALITY LEDGER (everything the root rests on beyond literature)

The complete honest list, per the wave-10 residue enumeration (ledger §"WAVE 10 +
PASS-2 ADJUDICATIONS": "THE WHOLE MANDATORY-PATH RESIDUE now = {(GR-B) orders ≥ 2
(FGMN-pinned, transcription), (FRESH), the O-9 repair, O4T pass 2, D-11/O-11
verification cycles, ROOT ASSEMBLY}" — quoted as of wave 10; the rows below are SYNCED
to the 2026-08-02 harvest at REVISION 1), EXPANDED to every named pin/flag a reader of
§2 encounters. Character classes: **[V]** verification-pass debt (a proof is on file; a
named fresh-context hostile pass is owed — no new mathematics expected unless the pass
finds some); **[T]** transcription debt (content pinned to printed sources or existing
proofs; writing/auditing owed, no new mechanism expected); **[M]** genuine open
mathematics (no proof on file). The CUC §0 pending-verdict registry
(`lean/notes/openmath/CU_cluster_phaseB.md` §0) is the live key for the P(·) items.

### 3.1 Genuine open mathematics [M] — the honest mathematical distance

| item | statement | owner | notes |
|---|---|---|---|
| **(H1) — the graded-dictionary open family at OM orders ≥ 2** [ENLARGED at REVISION 6, findings F-2/F-3, from the former single-member "(GR-B)" row; member (d) ADDED at REVISION 7, finding VC7-1, and REQUANTIFIED to READ ORDER at REVISION 8, finding VC8-1 — the group has FOUR displayed members] | **(a) (GR-B)**: the anchored-march residue-structure package (GD-2/GD-3's one open input) — the frame tie + residual-dictionary march at orders ≥ 2, modeled on printed FGMN Thm 4.2 (both pairings), EXCLUDING the properness-proviso leg (the e·g = 1 side condition), whose SOLE home is the pinned §3.3 (KP)/(KPirr) [T] row at FGMN Lemma 5.3 [one-home cleanup at REVISION 7, finding VC7-4: the former "with the properness proviso" wording gave that leg two classifications]. **(b) GD-1 beyond order 1** (= O-2a's exactly-stated open lemma OL-2a-1): the (KP)/(KPirr) representative-admissibility clauses beyond order 1 — the canonical key lifts form one GMN-admissible representative family at every order (the properness-proviso LEG stays [T] at the §3.3 (KP)/(KPirr) row, pinned to FGMN Lemma 5.3). **(c) GD-6 conformance instances at orders ≥ 2** (= OL-2a-6's consumed face): the run-decomposition-conformance instances Step 10's conditionality line consumes — held on proof-by-construction ROUTES on record, which are not proofs (the same evidentiary status VC5 ruled [M] for (T-DEC)/(T-VERD)). **(d) the order-≥ 2 KEY1 block** [ADDED at REVISION 7, finding VC7-1 — formerly only gestured at by the §3.3 S-1 row's "campaign perimeter" line, which named no member; REQUANTIFIED to READ ORDER at REVISION 8, finding VC8-1 — the REVISION-7 form quantified over realized sites "whose current key has degree > 1", parenthetically identified with the order-≥ 2 reads: an UNPROVEN identification (e·g = 1 refinement reads keep degree-1 keys at higher order, and the proved order-≤ 1 slice does not cover a higher-order site merely because its key stays degree 1), and its title conflated the KEY's degree with the residual factor ψ's degree g]: KEY1's conclusion block asserted at EVERY realized site of Step 6's machine at OM ORDER ≥ 2 — membership by read order alone, IRRESPECTIVE of the current key's degree, explicitly INCLUDING order-≥ 2 sites whose key remains degree 1 (e.g. e·g = 1 refinement chains) — matching Step 6's all-site use exactly: for the STANDARD key lift Φ_{i+1} assigned at such a site with read side (e, h) and residual factor ψ of degree g ≥ 1 (g is ψ's degree, unconstrained — the key's degree is irrelevant to membership; ord_ψ(R_i) = μ): (d1) in(Φ_{i+1}) = u·ψ̂_hom in gr(w_{i+1}) with u an invertible homogeneous element and ψ̂_hom irreducible; (d2) the degree block — ψ̂_hom ∤ in(B) for every nonzero B with deg B < deg Φ_{i+1}; (d3) ord_{ψ̂_hom}(in f) = ord_ψ(R_i(f)) = μ. This is exactly O-1thr's (V1)/(V2) key-compliance block beyond its proved order-≤ 1 slice — the block Lemma WGEO consumes at those sites. **Coverage arithmetic, displayed [VC8-1]:** the PROVED slice is exactly the order-≤ 1 sites (the GD23 dictionary + O-1thr's (V1)/(V2), proved at order ≤ 1), so (d) covers precisely the COMPLEMENT of the proved slice in Step 6's site range — order ≤ 1 proved, order ≥ 2 assumed here — and total coverage of the all-site theorems consumes NO key-degree/order equivalence; the §3.3 S-1 row's "same method" note prices the intended proof ROUTE (KEY1's weighted-Gauss/homogenization argument), not a proof | (a) GD-1/GD23 arc (verify passes: the GD23 verification cycle continues at orders ≥ 2); (b) GD-1 owner (O-2a Step 7); (c) GD-6 owner (with GD-4, Step 7); (d) O-1thr/GD-1 arc (Step 6; GD23's Theorem KEY-g is the proved j = 0 instance at any d₀) | order ≤ 1 PROVED over an arbitrary complete DVR for all four faces (leaf #4; O-2a's core; CU-1's order-≤1 scope; (d)'s proved slice = KEY1 itself at Step 6 + GD23's KEY-g at the order-0 key for any d₀); the wave-10 ledger grades the ≥ 2 layer "FGMN-pinned, transcription" — every (a)-target is pinned to a printed theorem (LITUNIT §7), but the composed march at ≥ 2 is unwritten, and (b)/(c)/(d) carry NO pin and no proof on file: honestly [M] until written and passed [(b)/(c) were the F-2/F-3 blockers, (d) the VC7-1 blocker — same proved-≤1/open-≥2 shape, previously misfiled [T] or gestured at]. Enters: (a) Steps 10 (CU-1's step), 14 (CEN-W), 16, 18 (K3), 18b ((S1)); (b) Steps 10–12 (O-2a's admissible-representative-family clause: Θ's typing, Theorem 2(B)/(D) feeding CU-2/CU-2t; Step 12's representative-lookup flag duty); (c) Step 10 (the displayed conditionality line); (d) Step 6 (Theorems 1–3's all-site scope at order-≥ 2 reads — the WGEO-consumed block), propagating with Step 6's deliveries (D-14/D-15 → CU-1/CU-3/CU-4, Steps 10–12, Step 17's per-box σ-constancy) — all inside clauses (R)/(UB) |
| **(FRESH)** | the joint-with-parent level-determinacy separation clause (O-9's named open), priced at the GD-2/CL-13 vertex-law layer = CU-1's step content; **since the O-9 rev-2 repair it carries clause (c) = the former JUNCTION-PIN** (PROVED label withdrawn at 01c21e6; the telescope consequence is proved GIVEN the clause) [REVISION 1, finding 4] | O-9/CU-1 layer | enters Step 10 (CU-1's step — the GD-2/CL-13 vertex-law layer where it is priced; Step 10's Delivers-line names the consumption site, so clause **(R)** inherits it through Step 18's K1/CU leaf labels — the REVISION-3 A6(iii) phrasing "clause (SQ) inherits it" is SUPERSEDED at REVISION 4, finding VC4-4: the semantic (SQ) is unconditional and consumes no CU content) and Step 14's joint census CEN-J, hence Step 18's K3, and Step 16/clause (UB) through the same census (REVISION 3, finding B1). Numerically unviolated (8.2M boxes) |
| **(R1)–(R4)** (O-8b's base) **= (ROOT-C)(H4a)** [re-scoped at REVISION 2, finding V2-2] | the T-1 stage-tower laws re-proved verbatim over O_δ = W(F_{p^δ}) | O-8b (M14's conditional proof closes given them) | expected mechanical ("nobody has written it" — tree O-8b); N8 exhaustive. Borderline [M]/[T]; kept here for honesty. Enters Step 16 AND Step 18 (O-11 K3-c — declared row D-c at O-11 r2): NOT clause-(UB)-only — through K3-c it conditions clause (R)'s α = R_τ evaluation wherever verdict rows consume base-changed β-legs (δ > 1 pools). Audit note [REVISION 3, finding A5; PRICED at REVISION 4, finding VC4-1]: O-8b's law is declared at δ-STABLE position classes; that K3-c's realized δ > 1 pools are of that kind is asserted at the D-c interface with no supplying row — so the presumption is NO LONGER carried inside (H4a): the fragment is named **(K3-δ)** (= (UB-X)(b) restricted to K3-c's realized pools, displayed on the (UB-X) row) and clause (R)'s attribution consumes it explicitly; the match display (or a re-scope) rides the next O-11/O-8b touch and would retire (K3-δ) from clause (R) |
| **(ADM)** — O-9's census admissibility [NEW row at REVISION 2, finding V2-5; REWRITTEN to the single operative form at REVISION 4, finding VC4-2] | **THE OPERATIVE DEFINITION (O-9 r4, FULL attainment):** every on-line lattice slot's β_k is FULLY attained by the ledger — dim_{F_q} G_{β_k} = s(β_k+1) − s(β_k) = d, equivalently #{𝐣 : wt(𝐣) ≡ β_k (mod e), wt(𝐣) ≤ β_k} = d — the explicit hypothesis on CEN-W/CEN-J (O-9 r4, unit of record 35faac7). Two earlier displays are REFUTED and superseded, kept here so no reader can consume them: the r2 CLASS form "β_k mod e ∈ {wt(𝐣) mod e}" (refuted by K6's r = 1 countermodel, 256/26244) and the r3 VALUE form "G_{β_k} ≠ 0" (refuted at d ≥ 2 by the pass-3 countermodel, 73728 vs 49152, ratio (q+1)/q per partially-attained slot) | O-9 (Step 14) | p-free and per-datum DECIDABLE (a finite lattice check; at r = 1 the displayed inequality β_k ≥ h₁((h₁^{−1}β_k mod e₁) + (f₁−1)e₁), collapsing to the r3 form at f₁ = 1), so borderline [M]/[T] like (R1)–(R4); kept [M] for honesty. **Automaticity, exact:** automatic at r = 0 ONLY (dim = f₀ = d on nonnegative on-line data); NOT automatic at r ≥ 1 (two explicit r = 1 countermodels on file); every older "automatic at orders ≤ 1" phrasing in this document is superseded by this row [finding VC4-2's charge]. Enters Step 14 (CEN-W/CEN-J), hence Step 18's K3 census half (K3-b) AND Step 18b through seam (S1) [added at REVISION 4, finding VC4-5], AND Step 16/clause (UB) through Step 16's consumption of the census over any complete DVR [REVISION 3, finding B1]; carried by (ROOT-C)(H3); consumed by clauses (R)/(UB) only — NOT (SQ) [REVISION 4, finding VC4-4]. Unattained-vertex data have census 0 (the padding value), so the hypothesis prices attainment, not positivity. Falsifier state: K6 (class refuted, 0 = 0 exact) + K7 (value-vs-FULL dichotomy — countermodel refuted, first d ≥ 2 VALUE gate exact: census q^14(q²−1)² at p ∈ {2,3}). O-9 pass 4 in flight; the [M] classification is unchanged |
| **(UB-X)** — the clause-(UB) residue [NEW at REVISION 1, finding 7] | (a) the per-leaf O_δ re-base audit for the ℚ_p-stated leaves (the "per-leaf scope hygiene" of the D-3 collapse, declared at the GD dispatch but unwritten); (b) δ-stable position classes exhaust the realized ledger at every δ — **whose restriction to Step 18 K3-c's realized δ > 1 pools is the named fragment (K3-δ), consumed by clause (R) [REVISION 4, finding VC4-1]**; (c) full solve + type aggregation commute with unramified base change | D-3/O-8b (Step 16; the (K3-δ) fragment also Step 18) | until written, clause (UB) is proved ONLY at the Step-16 displayed scope; (ROOT-C) makes this explicit as hypothesis **(H4b)** (renamed from (H4) at the REVISION-2 split, finding V2-2 — since REVISION 4 the genuinely clause-(UB)-only part is (H4b) MINUS its (K3-δ) fragment: clause (R)'s attribution includes (K3-δ) until the δ-stable match display lands at the O-11/O-8b interface). N8 numerically supports the law it scopes |
| **(PACK)** — the actual-pack correspondence + the pack row-laws **= (ROOT-C)(H5)** [NEW row at REVISION 4, finding VC4-3; ENLARGED at REVISION 7, finding VC7-2; ENLARGED AGAIN at REVISION 10, r11, edit E4 — the FOURTH row-law (PACK-iv), an ADDITION to the roster, never a clarification] | D-11's clause 4, OPEN as the D-11 r3 record cites it (8a0786c, pass-2 P3: the completed-node overclaim withdrawn): the CONCRETE assembled pack — the realized tables/rows whose mass laws Step 17's M1 covers and whose root/entrance rows M2's (R-read) layer excludes in-statement — equals the intended 𝔅_n instantiation ENTRY-FOR-ENTRY (= OL-O12-2's remaining DATA half, `O12_phaseB_attempt_rev4.md` §3). **PLUS [ENLARGED at REVISION 7, finding VC7-2] the three named row-laws, stated AT THE 𝔅_n ROWS (hence, through the correspondence, holding at the concrete pack's rows): (PACK-i) rsh_interp** — the interpolation law (RatBurdens' interpolation face): for every row σ and every prime power q₀, the evaluation at q₀ of the symbolic shape series Rsh(vmap σ) equals Rval(vmap σ)(q₀) — the Rsh → Rval conversion Step 18's K2 evaluation glue consumes; **(PACK-ii) r_bdd** — the bound: Rval(vmap σ)(q₀) ∈ [0,1] at every prime power q₀ (what makes ENNReal.ofReal faithful there — with (PACK-i) the K2-evaluation-glue pair); **(PACK-iii) wsh_ok** — regular-subring membership: every row's weight/shape series lies in the regular subring on which the evaluation ring-hom evalAt is defined (with D-6, the membership input to Step 18's K3 evaluation ring-hom — NOT K2's glue; sub-attribution made uniform at REVISION 8, finding VC8-2); **AND (PACK-iv) sem_alloc — semantic verdict-row form at the intended member** [NEW at REVISION 10, r11]: at every block e of the intended 𝔅_n instantiation, the booking is re-entrant ((O1)/(O2′)/(O3)) and the family-(iv) source/verdict rows are the canonical semantic assembly — every verdict-row entry equals Σ_{F exit} m(F)·Σ_{comp_F(σ⃗)=σ} ∏_i β_{μ_i}(σ_i)(q^{D_i}) — plus, at (O2′), the displayed κ₁ re-feed leg and, at (O3), the displayed family-(v) exit scaling, and nothing else — with m(F) the member's family-(ii)/(iii) mass entry and the β's its family-(vi) legs; the member carries no allocation adjustment beyond these. [The premise D-11 r4's Theorem M2 names as **(RE)**; (S1)/(S2) do NOT imply it (finding C6 of the D-11 owed pass — pass 3, `D11_owedpass_2026-08-03.md`, the §3.2 D-11 row). CONSUMED AT exactly TWO displayed sites — the D-11 r4 §2A.4 closure walk's CLOSED two-element set (relative to its displayed residue (EDGE-RES)), which REPLACES the design's former "untraced cover / possibly within 17/18" clause: **Step 18b DIRECT** (Theorem M2's (RE) instantiation) AND **Step 17's E1 delivery display of (ROW-Σ)** (the proved member-row normalization theorem — its (RE) hypothesis at the consumed member IS this clause; (ROW-Σ) itself is M2-free, so the 17 ≺ 18 ≺ 18b order is untouched). [Terminology reconciled, REVISION 13, VC15 advisory 4(a): "CONSUMED AT" here prices the leaf's closed EDGE SET — the two displayed edges through which (RE) content can reach the mandatory path — while Step 17's Delivers-line statement "no consuming proof line on the mandatory path reads a member-row normalization (relative to (EDGE-RES))" speaks of PROOF-LINE consumption: the E1 edge is a delivery DISPLAY, priced conservatively as a consumption site precisely because it is an edge, not because any mandatory proof line reads through it. Both statements are true together — the edge-set pricing over-prices, never under-prices.] Step 18's K2/K3 glue consumes PACK-i/ii/iii, and Step 17's M1 excludes family (iv) in-statement ((a″)). RETIREMENT ROUTES (design §3.3): (route-a), n = 3 — the OL-O12-2 transcription-and-correspondence pass EXTENDED by the named per-row booking-profile duty (verify each sealed block's booking against the (O3) forms and each row against §2.3(iv)'s canonical assembly + the displayed scaling); (route-b), general n — the O-14d instance population constructing the member IN the (O1) booking, the profile then holding by construction.] The first three are EXPLICIT clauses, NOT consequences of the correspondence: entry-for-entry equality transfers a law to the concrete pack only if an earlier displayed theorem establishes that law at the corresponding 𝔅_n rows, and NO consulted record displays one — Steps 2/5 state none of the three by name nor an explicit implication to them (the VC7 finding-2 warrant charge; the REVISION-6 A-6(ii) "priced inside the correspondence" inference is WITHDRAWN as invalid). Steps 18 (item (v)'s row-law consumption, sub-attributed [REVISION 8, finding VC8-2]: PACK-i/ii at K2's evaluation glue; PACK-iii at K3's evaluation ring-hom, with D-6; PACK-iv at Step 18b direct + Step 17's E1 (ROW-Σ) delivery display ONLY [REVISION 10, r11 — the leaf's closed edge set]) and 18b consume Step 17 AS this correspondence-plus-laws; no proof is on file, so it cannot ride the §3.2 D-11 [V] row | D-11/O-12 (Steps 17, 18, 18b; the n = 3 instance = the §3.3 OL-O12-2 data pass); the row-law clauses: the D-11 owner with O-14d/D-11 pack construction (O-11 r3 §5 row 20) | per D-11 r3 the content is a finite per-n data comparison ("finite"/"engineering" ≠ "proved" — the pass-2 P3 charge), so borderline [M]/[T]; kept [M] for honesty until executed and passed — the row-law clauses included (properties to be established at 𝔅_n, not data checks). Enters clause (R) (Steps 17 → 18/18b: the α = R_τ evaluation runs THROUGH the identification and through PACK-i/ii at K2's glue and PACK-iii at K3's ring-hom) and clause (UB) (the same chain at q ↦ q^δ). NOT (SQ)/(0). Numerics: the n = 2 real-pack triple re-derivation (O-11's falsifier) and RESUM-n3 65/65 are its executed instances at n ≤ 3 — the O-11 record's "checked at RESUM-n3" covers the row-laws' instances too [G-3 caveat, REVISION 13, VC15 advisory 3: that O-11 record's END-TO-END reading of RESUM-n3 is challenged by its own un-banked 2026-08-03 pass (finding G-3, the §3.2 O-11 row) — supported beyond doubt only at the five displayed identities; the coverage sentence here inherits that scope] — flags, not the general-n proof. Related but DISTINCT: D-11's widened-charge item (vii) (H.6-fence conformance of the built tables) — the REVISION-4 flag "whether it too should be promoted to hypothesis grade" was RESOLVED at REVISION 5, finding VC5-1: promoted, into the (H6) group below |
| **(T-DEC)/(T-READ)/(T-VERD) + D-11 item (vii)** — the terminal-seam engine-conformance group **= (ROOT-C)(H6)** [NEW row group at REVISION 5, findings VC5-1/VC5-2] | (a) CU-2t's THREE terminal rows (first displayed CUC §9.4; split/amended at R3, 81589e9): **(T-DEC)** — emission discipline at decided sites ((T-DEC-dec)) + the corner fence ((T-DEC-cor), at the Corollary's own conditionality); **(T-READ)** — the emitted shape/position fields equal the (c1)-shape polygon-side datum (the (RC-read) supply; NO displayed machine-side home on file, purely owed); **(T-VERD)** — terminal verdict-field conformance. (b) **D-11 widened-charge item (vii)** — general-n H.6 in-fence conformance of the built tables (the surviving live condition of the tree T-5 row's "six fences gate [1v]-FULL"; the six fence kernels themselves stay off the mandatory path — if a built pack ever NEEDS fenced territory, the kernel so touched becomes ADDITIONAL open mathematics beyond this row). All four sat in §3.3 [T] through REVISION 4; VC5 applied the ledger's OWN classification rule — [T] requires content pinned to a printed source or an existing proof, and these are OPEN PROPOSITIONS consumed on clause (R)'s mandatory path with no proof on file — so they are [M], hypothesis-grade like (PACK) | GD-4/GD-2-family owners per CU-2t R4's grading: (T-READ) re-graded OUT of the "engine bookkeeping" GD-4 label into the GD-2/GD-5 read-semantics-conformance family (at k′ ≥ 1 an OL-2-min instance + the terminal-BINDING clause, which stays GD-4; at k′ = 0 no sibling in the OL residue — a proof-by-construction route cannot discharge it); (T-DEC)/(T-VERD) GD-4 (proof-by-construction routes on record); item (vii): the D-11 owner (Steps 17/18) | enters clause (R) at Step 11's route — CU-2t's §9.5 terminal-verdict conditionality (CU-2t is VERIFIED at scope MODULO exactly these rows), consumed by Step 18 through D-a/K4-CYL — through the terminal-seam leaf labels riding O5triple's counting (Steps 11/12 → Step 17's M3/Thm-E leg) into Step 18's K1 ("CU leaf labels" — the same K1 route the (FRESH) row displays; leg added at REVISION 6, finding A-5) — and, for item (vii), at Step 18's K3-a; clause (UB) inherits the same content through the transported chain at q ↦ q^δ; NOT (0)/(SQ). Falsifier state: the CU-2t sealed gates + V-n3 178/178 / H-n3 151/151 (sealed n = 3 evidence for (vii)) — flags, not the general-n proofs |
| **(I-τ)/K-LOC — the sibling-interface engine-stratum conformance pair = (ROOT-C)(H7)** [NEW row at REVISION 6, finding F-1 — the LAST of O-11 r2's declared D-a/D-b/D-c interface triple adjudicated: D-a → (H6) at VC5-2, D-c → (H4a)/(K3-δ) at V2-2/VC4-1, and this row is D-b] | **(I-τ)**: the engine's realized site strata present as O-10's ρ-separated adapted cells (the adapted-cell → engine-stratum presentation Theorem 2 needs). **(K-LOC)** (= M07 OL-3, an exactly-stated OPEN lemma — the document's own naming): continuation reads of branch j on the stratum = OM reads of f_j up to cell-constant normalization. Without the pair, Step 18's K1 does not receive O-10's product law at the engine's strata — triple-displayed consumption (Step 15's application line; Step 18's K1 "(I-τ)→GD"; the former §3.3 row's own owner column). **Adjudication own-row-vs-(H6), displayed [the F-1 charge]:** the pair is engine-conformance GENRE (kin to (H6), and K-LOC's read-semantics content is (T-READ)'s GD-2/GD-5 family), but its SEAM is the sibling/product-law interface (K1, Steps 15 → 18), not the terminal seam; its owners are O-10/GD-family (not the CU-2t/D-11 owners); and (H6)'s membership is PINNED by the leaf-#9 interlock (CU-2t VERIFIED at scope modulo exactly its three rows + item (vii)) — so a SIBLING row, not an (H6) member | O-10 consumers / GD-family presentation (Steps 15, 18); discharge = the K-LOC lemma (M07 OL-3) + the (I-τ) presentation write-up | the former disposition "both priced into the GD families" (Step 15) had NO displayed [M] landing — no GD row carried read-locality/normalization content — so under the ledger's own rule the pair could not sit in [T]: no pin, no proof on file, consumed on clause (R)'s mandatory path. Enters Step 15 (application to realized strata) and Step 18 (K1), hence clauses (R)/(UB) only; NOT (0)/(SQ). Falsifier state: O-10's harness 68/0 + U6/IFK gates exercise the adapted-cell law itself — flags for the pair's instances, not the general proof |
| **(H8) = (K-CUT) — the shallow-cut decomposition kernel** [NEW row at REVISION 10, r11, from finding G-2 of the UN-BANKED O-11 hostile pass of 2026-08-03 (`O11_owedpass_2026-08-03.md`, verdict CRITICAL, findings G-1/G-2/G-3, NOT banked as O-11's pass 3 — the run is recorded at the §3.2 O-11 row; provenance disambiguated at REVISION 12, finding VC13-4)] | For every n ≥ 2, every prime p, and every splitting type σ of degree n, every complete type-σ canonical tree T admits EXACTLY ONE decomposition into a shallow shape Ŝ plus per-branch block derivations (D_1, …, D_r) — exhaustive-exclusive read ownership at the first-split cut — and the decomposition is weight-preserving, μ(fiber T) = W(Ŝ)·∏_i weight(D_i), as M04 Lemma 3.3's hypothesis consumes it at Step 18's K1. Status [RE-SCOPED at REVISION 13, r14, executing the H8-R2 draft (`Scaffold/HDischarge/H8/R2RootSync.lean` §D; firing conditions verified there — waves 0–2 landed green + H8-R1's adjudication on file, plus B3/C1 beyond)]: **REDUCED, NOT discharged** (blueprint `lean/blueprints/HDISCHARGE_H8.md`; all 8 Lean units + 2 documentary units LANDED, green + Lean-core verified at R2RootSync §A). PROVED Lean-core (`lean/LeanUrat/Scaffold/HDischarge/H8/`): (K-CUT-1) level (1a) — first-entrance uniqueness (`firstEntAbove_unique`) + exhaustive-exclusive read ownership with the (BDY) boundary law (`read_cut_exhaustive`, `shallow_block_disjoint`, `block_block_disjoint`, `bdy_display`) — killing the G-2 falsifier channels ("overlapping ownership, an unowned read, or two different shallow decompositions") at the ownership-partition level; (K-CUT-1) level (1b) FORWARD half — the concrete (Ŝ,(D_ε)) extraction + cut-map injectivity (`cutOf`, `cut_injective`) + conformance + countability transport; (K-CUT-2) count-form (`siteExp_cut`, `fiber_count_cut`); the (†)-regroup of (K-CUT-3) incl. M04 Lemma 3.3 as `seriesSum_cut_conv`; the positive gate (`toy_cut_display`). The formal bridge to the M04 consumer EXISTS as the CONDITIONAL theorem `kcut_reduce` (Step 18 K1's exact form); its binder rows are the row's ENTIRE remaining content, each typed, [M]-grade, UNPINNED: (K-CUT-s) entrance-predicate cell-data supply [owner: CTS-M(i)/(iii)/CL-13, (H1)-adjacent census rows; enters `kcut_reduce` only at its under-typed `supplyDec` face], (K-CUT-g) graft completeness = `graft`/`graft_cut`/`cut_graft` [owner: the D-14 + CU-3 + REL.2 arc; charge (vi) ADJUDICATED at H8-R1: (vi-a) FAIL ⇒ NO retirement, the serving map NOT re-pointable as demonstrated; (vi-b-2) NO], (K-CUT-m) measured mass tie = `mass_eq` + the `W`/`weight` rows [owners: (H7)/sibjc TREE-EXP rows + [2r]/CL-8 + O-1/O-3 count-tie], plus the [3t]↦`VTree` realization + verdict-keying rows (`real`/`verdict_key` — TREE-N/[3t] seam territory). HONESTY GATE (displayed for the VC pass): the H8 Lean units are machine-checked, but NO unit-level hostile pass has run on the prover fan-out — the blueprint's own "next gate"; discharge of this row AWAITS that pass and the seam owners; nothing here is claimed discharged. Pre-r14 history: an OPEN KERNEL of T-6 displayed NOWHERE before r11 (`rg CUT-WD` = 0 hits at r10) — an open proposition consumed on clause (R)'s mandatory path cannot ride an unexecuted adjudication promise (the VC5/VC6/F-1 genre rule). One-home adjudication: NOT (H6) (its seam is the first-split cut inside K1, not the terminal seam; (H6)'s membership is pinned by the leaf-#9 interlock) and NOT an (H7) member (the K1 seam class matches but the owners differ — T-6/O-1thr/CU-3 vs O-10/GD-family — and this content carries its OWN retirement route): a sibling row, per the F-1 precedent | T-6/O-1thr/CU-3 arc; RETIREMENT ROUTE, ADJUDICATED [REVISION 13, r14]: O-11 §8 charge (vi) RAN at H8-R1 (`Scaffold/HDischarge/H8/R1Adjudication.lean`, Codex fresh-context) — **(vi-a) FAIL**: D-14 + CU-3 do NOT demonstrably imply (K-CUT-1/2/g); per the PRE-REGISTERED disposition rule, NO retirement into [V] debts — the row PERSISTS at the reduced scope displayed; the O-11 r3 serving map "CUT-WD ← D-14 + CU-3" may NOT be re-pointed as demonstrated. Next gates: the unit-level hostile pass on the H8 fan-out + the three seam-field owners | Enters Step 18 (K1's Lemma-3.3 regrouping), hence clause (R); clause (UB) inherits through the transported chain at q ↦ q^δ. NOT (0)/(SQ). Consumption sites UNCHANGED by the re-scope |
| **(H9) = (NSF) — the ns-routing interface row** [NEW row at REVISION 10, r11; TWO members retiring SEPARATELY — neither retirement discharges the other; (NSF-KEY) RETIRED at REVISION 13, r14, per the H9 lead's ledger adjudication — the row's open member is (NSF-INT)] | THE TARGET (the row's conditional statement): at any instantiation satisfying (q-4)'s interface pin (the ClassifierSpec docstring: `some σ` = complete finite realizable tree, all leaves (τ-irr)/(τ-hen), certified at level N), every K1/K4-labeled box lies in the decided-ns-free counted corpus and D-15's exact-classification package applies with no a.e. weakening — the round-3 attempt to PROVE this by chaining displayed quotes was refuted and stands WITHDRAWN; the two priced members: **(NSF-INT) — the interface implication chain (pin → predicate → corpus).** At every consumed site of Step 18's K1/K4 chain: (i) irr/hen-terminal completeness excludes ns-marks — a complete finite realizable tree with all leaves (τ-irr)/(τ-hen) carries no ns-marks (in particular the Hensel payload carries none) — so every box labeled under (q-4)'s pin fibers a tree that is decided ns-free in (q-1)'s sense; and (ii) M04's "complete-finite-realizable" scope coincides with the guarded TREE-N corpus ((q-3)'s all-leaves-(τ-irr)/(τ-hen), `hdet`-guarded trees) at every consumed site. The interface quotes (q-1)–(q-6) are CONTEXT, not proof. **Displayed premises housed IN this member (one-home; the D-11 r4 passes surfaced them):** the §1.4 marks-vocabulary premise (a mark is the RECORD of an NsHalts event — §1.4's sole generation rule; claim (i)'s premise) and **(K1-CANON)** — at every consumed Step-18 site, K1's "canonical tree" denotes (q-4)'s pinned notion; equivalently (the interface-lemma form), every tree at which the mandatory path invokes K1 satisfies the all-leaves-(τ-irr)/(τ-hen) guard [the r4 source-inheritance inference — that K1's bare quantifier carries the guard FROM its cited sources — is WITHDRAWN (r4 pass-1 gap 5): a theorem's quantifier does not inherit its source lemmas' restriction, and the `hdet` guard scopes one machine-checked candidate, not K1]. The D-11 r4 §4B leaf proof (artifact I-1) proves (i) from the §1.4 displays + the marks-vocabulary premise and (ii) site-by-site over the closed I-2 list {K1, K4} — the K4 leg at the pin directly, the K1 leg UNDER (K1-CANON). **(NSF-KEY) — as-built keying conformance [ONE proposition] — RETIRED [REVISION 13, r14; executed status].** At the wired instance: for every n, p, level N, box f, and σ — if the wired classifier labels f (`bridgeCanonical … N f = some σ`) then f fibers a carried tree of the σ-carrier satisfying `MovesT.NsFree` (no ns-marked chain) at threshold ≤ N. RETIREMENT PROVENANCE (historical — the pre-Unit-C record, re-labeled at r14, kept so the repair's necessity stays visible): the member was **KNOWN FALSE AT HEAD** from the M02 record — the exact-square poison V₀ (ns-marked, self-realized, carried, because the PRE-REPAIR `bridgeTree` carried no ns-free conjunct) was labeled "totally split" by `bridgeCanonical`; c_{V₀}(N) = p^{N−2} against (SCALE)'s forced p²-per-level growth — `count_tie` as then typed REFUTED, `canonical_stable` refuted at the intended instance, `VPSound` unsatisfiable; an obligation certain to fire, which fired. EXECUTED (Unit C, the sanctioned mechanism — M02's "Minimal repair" — landed at HEAD under the on-record sign-off): the carrier is now the GUARDED form, verbatim `{V // V.typemult = σ.1 ∧ RealizedSelf n p V ∧ MovesT.NsFree V}` (`BridgeD3_treeCarrier.lean:94–96`; commits 322ec54 + dfdb558); V₀ DEAD (`bridgeVt_nsFree` compiled, consuming wiring recompiled); `count_tie` sorry-free at the guarded corpus (the hdet scope); `bridgeCanonical_stable` re-proved green; the three statement texts byte-identical (re-scoped THROUGH the carrier only). THE SIX (NSF-KEY-RET) ARTIFACTS, all on file: R-0 Asvin sign-off (ledger verbatim) · R-1 the conjunct diff + the closed ripple enumeration (`UNITC_ripple.md`) · R-2 byte-identical statement texts · R-3 the three per-statement validations · R-4 `lake build` green (8579 jobs) + AxChk census identical (740 footprints, 0 sorryAx) · R-5 fresh-context Codex diff confirmation (`UNITC_diff_confirm.md`, VERDICT: CONFIRMED). Adjudication of record: ledger entries "UNIT C EXECUTED" + "H9 LEAD ADJUDICATION"; compiled-proposition witness **H9-U1** = `Scaffold/HDischarge/H9/NsfKeyProp.lean` (landed green, Lean-core — a WITNESS of the retired state, carrying no retirement weight of its own). Bounds inherited from the adjudication: no carrier non-vacuity claim (compiled existence of a carried tree stays OPEN at the realization seam); the BridgeD16/TreePin.vt_surj completeness residue stands (Phase-B queue). One-home argument: both members are the ns-routing face of the SAME classifier interface — (NSF-INT) the interface/paper side, (NSF-KEY) the as-built/Lean side; same seam (K1/N2 label routing at Step 18), same owners, same consumption chain (the VC7-2 one-home precedent) | the O-1thr/M04/M02 classifier-interface arc. RETIREMENT, exact: **(NSF-INT)** by (NSF-INT-RET) — I-1 (the §4B leaf proof, ON FILE) + I-2 (the closed site list {K1 (M04 §5.5 (K1) + Lemma 3.3), K4 (M04 §5.5 (K4) + D-15)}, closed by the §4A walk relative to (CH-RES)) + I-3 (the r4 hostile pass's ACCEPT of §4B as a proof — GRANTED at the r4 passes: pass 2 adjudicated the conditional argument VALID) **+ a separate discharge of (K1-CANON)** (leaf-external — the M04-side pin display or the interface-lemma form; r4 pass-2 gap 3: I-3's accept of a CONDITIONAL proof verifies only (K1-CANON) ⇒ the K1 scope conclusion and cannot itself establish (K1-CANON)) — until that discharge, (NSF-INT) stands PRICED; **(NSF-KEY)** RETIRED by Unit C per (NSF-KEY-RET) — EXECUTED, the six artifacts R-0..R-5 on file (never by any leaf text; H9-U1's compiled proposition is corroboration, not a second retirement route) [synced at REVISION 13, r14] | Enters Step 18's K1/K4 chain and Step 17's M3 leg (via Steps 11/12), hence clause (R); clause (UB) inherits through the transported chain. NOT (0)/(SQ). Two-channel consumption (D-11 r4 §4A, outcome CLEAN at the walked closure, residue (CH-RES)): (ch-1) label-blind exact combinatorics; (ch-2) mass-mediated semantic tie at Step 19's squeeze. Display rule [updated at REVISION 13, r14 — the former "must NEVER be displayed as covering the HEAD wiring while (NSF-KEY) is unretired" rider is DISCHARGED by the retirement]: the target's as-built keying FACE now holds at HEAD ((NSF-KEY) retired; compiled witness H9-U1); the target must NOT be displayed as discharged while (NSF-INT) stands priced (its (K1-CANON) premise owed) |
| **(E-N)** [REWRITTEN at the (H3) discharge adjudication — HDISCHARGE_H3 unit EN-U5, VC-gated, machine clock 2026-08-01 (directive window 2026-08-05): EN-U1 consumption walk = RESCOPE-CLEAN + EN-U2 gate G-EN-1 = HIT, so the §3.4 criterion's rescope wording lands WITH route (b)'s absorption verification forced into this display and route (a) permanently fenced] | O-1thr Theorem 3(c)'s exhaustion caveat | O-1thr package | **OPEN, consumed nowhere unrestricted; scoped count + drainage absorption displayed.** CONSUMPTION (unchanged; completed at REVISION 9, VC10 advisory 2): touches CU-4 through Thm 3(c) (risk row R2 of the O3adj display) — AND enters Step 16/clause (UB) through the (UB-X)(a)-transported dictionary/CU/drainage layers, the SAME CU-4/Thm 3(c) content transported at q ↦ q^δ: the §1 B1 attribution's displayed entry. Beyond that CU-4 touch and its clause-(UB) transport, NO other mandatory-path site consumes it — and BOTH displayed touches are pricing at thr-scoped objects, not consumption of the unrestricted reading. THE RESCOPE (unit EN-U1, `lean/notes/openmath/H3_en_walk.md`, verdict RESCOPE-CLEAN): the 12-site per-site table (CU-4's six displayed legs S1–S3/C1–C3 — the walk's superset of the blueprint's five-way supply grouping, walk §1 — plus the Thm-3 fiber-clause entry, O3adj R2, the Step-16 transport, the squeeze's input-(vii) sum, the (SQ) bracket, the drainage absorption) confirms every finite-N counting/acceptance object on the mandatory path is thr-scoped BY ITS DISPLAYED DEFINITION OR HYPOTHESIS — the scoped count is the assembly's decided_σ(N) := Σ_{T̂ : type(T̂)=σ, thr(T̂) ≤ N} C_T̂(q)·q^{nN−A(T̂)} (input (vii), quoted at walk §4.1); CU-4's legs consume acceptance objects under the hypothesis N ≥ N\*(𝐇), a thr-majorant (CEIL-UB direction); the (SQ) bracket consumes no tree object at all; and both envelope displays on file define undecided-at-N as the complement of the DETECTED-at-N class (walk duty W-3, first horn), so drainage absorption of the thr > N decided boxes is DEFINITIONAL given the connector (EN-W1) ("detected at N ⇒ the box's canonical tree has thr ≤ N" — CU-4/CU-2t window bookkeeping consumed at its displayed conditionality, incl. the §4 D-12r pin's TB-CAP perimeter at leg C3 [re-pointed at REVISION 14, r15] and (AGR) at general n; (EN-W1) points the OPPOSITE direction to this caveat and does not consume it). THE FORCED ABSORPTION VERIFICATION (unit EN-U2, `verification/h3_en_falsifier.py`; log `verification/logs/h3_en_falsifier_2026-08-01.log` + JSON `verification/results/h3_en_falsifier_results.json`; PARI-factorpadic oracle gates PASS per config; gate G-EN-1 = **HIT**): decided ns-free decodes with thr(T) > N exist at ALL 19 probed configs (p ∈ {2,3}, n ∈ {2,3}, N ≤ 5 — the (3,3) family probed to N = 4) — 11,177 hit boxes, 1354 recorded with realized-chain records, margins N − thr down to −9 — so the thr ≤ N clause is NOT redundant (the unrestricted reading counts strictly more boxes: witnessed numerically at n ≤ 3 by the exact machine, route (a) DEAD as stated and PERMANENTLY FENCED per §3.4 of the blueprint), and the architecture's absorption prediction was CHECKED on the hits: operationally each hit is undetected-at-N BY DEFINITION (thr > N means its verdict windows exceed N; the capped-at-N walk halts at its first read with ceiling > N), hence sits in U^can_N and drains at Step 13's displayed price (Theorem N3 unconditional at n ≤ 3; (AGR)-conditional at general n); numerically ALL 1354/1354 recorded hits carry a tree-record witness (a level-N-equal lift realizing a DIFFERENT tree, or going ns — the level-N data does not pin the tree), confirming tree-undecidedness at N; and 77/420 of the n = 2 recorded hits are census-TYPE-decided at N, so the W-3 semantic(type)/operational(tree) readings DIVERGE on concrete boxes — the caveat is NON-VACUOUS, a semantically defined envelope WOULD have broken absorption there, and route (b) survives precisely because the quoted corpus displays define the envelopes operationally. STATUS: the caveat remains TRUE and OPEN as a display (now witness-backed, no longer merely unpriced); the rescope is a DISPLAY CHANGE — (E-N) prices nothing on the mandatory path beyond the CU-4 touch and its clause-(UB) transport displayed above. CARRIED RISKS (walk §6, restated): CU-4's own end-to-end walk is UNWALKED and conditional on P(O-1thr r4) + P(O-2a r5) + P(CU-1 r2) — EN-U1 walked the (E-N)-relevant faces only and must not be cited as CU-4's full walk; if a future CU-4 walk finds detection WITHOUT window containment, the absorption site re-opens as a window-bookkeeping defect, not as an (E-N) consumption |

**D-12r relocated [REVISION 14, r15 — executing VC16 finding 2]:** TB-CAP's
(τ-irr) completeness perimeter (the D4R.0-K(c) verdict-pin residual) is NO LONGER
[M]: the dual-accepted reduction confines its residue to the two literature cites
(HEN-LIFT)/(OM-SAT), both pinned-confirmed on file, the Hensel uniqueness gap is
handled by the displayed companion lemma, TB-CAP(irr) is PROVED at [3t] from the
accepted §C rev-14 Theorem C, and every sibling dependency is decomposed into its
existing priced home — so under this ledger's own definitions no D-12r-specific
open mathematics remains. It now lives in **§4** as the D-12r literature-pin
entry (the [T]/LITERATURE class), consumption sites and dependency-decomposition
routing preserved there verbatim; (H3) = {(ADM), (E-N)} since this revision. The
REVISION-13 rewrite's provenance record is §19's r14-6 row; this tombstone keeps
the one-home trail.

**(AGR) relocated [REVISION 1, finding 10]:** the drainage agreement seam
(= OL1-ii at general n: canonical termination + acceptance below a finite N(f)) is
ASSEMBLED from CU-2 + CU-4 + GMN Cor 4.19 — all on file — so under this ledger's own
definitions it is NOT [M]: no new mathematics is missing. It now lives in §3.2 as the
inherited-[V] row it is; general-n drainage stays conditional until those passes land
(n ≤ 3 unconditional, O4T Thm N3).

### 3.2 Verification-pass debt [V] — proofs on file, passes owed (named)

| item | what is owed | owner |
|---|---|---|
| **P(O-2a r5)** | fresh hostile pass on O-2a rev 5 (core thrice confirmed; V25 arc). The pass charge now ALSO carries the CU-2t R3 delivery [REVISION 4, CU-2t §9.5 touch (5)]: the W3 carrier finding + the 2(D)-scope dichotomy — decide whether Theorem 2(D) as printed asserts continuing-part or full-history 𝔈-membership at terminal-carrying emissions, and repair 2(D) first if the latter | O-2a (Step 7) |
| **P(O-1thr r4)** | fresh hostile pass on O-1thr rev 4 (V24's criticals repaired; sloped/sheared pin stands) | O-1thr (Step 6) |
| **P(O5t r3)** | fresh hostile pass on O5triple rev 3 (V23 0-critical arc) | O5triple (Step 12) |
| **P(O-8b/M14)** [NEW row at REVISION 3, finding B2] | fresh hostile pass on M14's conditional O-8b proof (on file; the alphabet law at δ-stable position classes over (R1)–(R4)) — the one consumed PROVED-UNVERIFIED proof whose pending pass was named nowhere; it sits on the mandatory path of clause (R) (Step 18's K3-c), not just (UB) | O-8b/M14 (Steps 16, 18) |
| **P(O-14a′ r3)** [NEW row at REVISION 9, VC10 advisory 1 — grade-display conformance, post-acceptance] | the O-14a′ (RunRealizerExists) verification record, displayed in the §0 vocabulary. ON RECORD, exactly: core Theorems 1–3 STABLE across the V11 and V15 passes (rev 3 on file); the consumed instance RRE(p, ZMod p) FORMALIZED Lean-core (`MovesD/R7_runRealizer.lean`, `runRealizerExists_zmod`); the CUC §0 "VERIFIED homes (no pass condition)" registry line as provenance. OWED: the in-document display of the V11/V15 arc's models and pass-cleanliness (or one fresh named hostile pass) — until then the leaf is graded **PROVED-UNVERIFIED with this named pass row**, NOT VERIFIED: the §0 bar (≥ 2 consecutive clean passes, ≥ 2 models) is not checkable from this document's four corners. This resolves the VC10-noted asymmetry precedent (O-2a's core is thrice confirmed yet carries P(O-2a r5)): every consumed PROVED-UNVERIFIED leaf now carries a named §3.2 pass row, O-14a′ included — the §§13/14 invariant is checkable in-document | O-14a′ (Step 9) |
| **D-11 verification cycle (at r4 REVISION 3)** [synced at REVISIONS 1/3; clause 4 carved out at REVISION 4, finding VC4-3; the pass-3-owed disposition RETIRED at REVISION 10, r11 — replaced by the two-pass r4 record] | pass 1 NOT ACCEPTED as billed (2587c70) → r2 (566b967) → pass 2: 2 criticals, both CONTAINED at r3 (8a0786c) → **pass 3 RAN: CRITICAL** (`D11_owedpass_2026-08-03.md`, 6 crit/3 gap — the STOP-THE-LINE event, ledger 44ce0a7) → the five-round repair design (`R11_REPAIR_DESIGN_V5_2026-08-04.md`) → **r4** (`D11_rev4_2026-08-04.md`: true-scope repairs + the obligations (b-EX)/(O-1)–(O-5)/(O-GLOB)/(ROW-Σ) written out + the (CH-COV)/§2A.4 audits + the §4B (NSF-INT) leaf proof) → **r4 pass 1: CRITICAL** (`D11r4_pass1_2026-08-04.md`, 3 crit/3 gap; folded at the leaf's REVISION 2) → **r4 pass 2: GAPS-ONLY** (`D11r4_pass2_2026-08-04.md`: NO critical remains; all six pass-1 repairs adjudicated VALID; the design-§7 obligations adjudicated AS PROOFS; 3 bookkeeping gaps folded at the leaf's REVISION 3). OWED NOW: one confirming clean pass on the REVISION-3 text toward the leaf's own two-consecutive-clean bar (pass 2's gaps were folded AFTER it, so the leaf bar is not yet met). **Scope of this [V] row, exact:** it prices the passes on D-11's ON-FILE proof layers (M1 families/M1′/M3/M2-at-(RE)/M2.0/(ROW-Σ)/(b-EX)/(O-GLOB) at their displayed conditionality) ONLY — clause 4 (the actual-pack correspondence) and the (RE) profile of the consumed member ((PACK-iv)) have NO proof on file and are NOT covered here: they are the §3.1 [M] row (PACK) = (ROOT-C)(H5); the (H9) members likewise sit in §3.1, never here | D-11 (Steps 17/18b) |
| **O-11 verification cycle (at r3)** [synced at REVISIONS 1/3; the 2026-08-03 un-banked run RECORDED at REVISION 12, finding VC13-4] | pass 1 NOT ACCEPTED AS STATED (a7ede72) → r2 (1bd8bd1, D-a/D-b/D-c declared + oracle-pin supplement) → pass 2 (V32): billing precision → **r3** (7aef1b9: tier 2 split into hypothesis-grade rows H-1..H-4; walk/cores byte-stable) → **an O-11 hostile pass RAN (2026-08-03, the owed-pass campaign; `lean/notes/openmath/O11_owedpass_2026-08-03.md`): verdict CRITICAL — adjudicated NOT BANKED at the report itself ("O-11's r3 counter does NOT advance toward its bar on this pass")**; its three findings: **G-1** (CRITICAL — Theorem O11 as displayed proves only the n ≥ 2 restriction of the advertised for-every-n target row; n = 0, 1 untreated), **G-2** (GAP — the CUT-WD serving map ← D-14 + CU-3 is a correspondence claim, not a demonstrated implication: overlapping ownership, an unowned read, or two different shallow decompositions are unexcluded — THE finding priced as the §3.1 **(H8) = (K-CUT)** row at REVISION 10, r11), **G-3** (GAP — the "RESUM-n3 65/65 end-to-end" rederivation claim unsupported beyond the five displayed identities). This row's former "pass 3 owed" tail did not record that run, leaving the (H8) row's "owed-pass G-2" provenance irreconcilable in-document (finding VC13-4); the BANKED pass 3 — one advancing the r3 counter, on text repaired for G-1/G-2/G-3 — stays owed | O-11 (Step 18) |
| **(AGR) inherited [V]** [relocated from §3.1 at REVISION 1, finding 10; residue sentence widened at REVISION 12, finding VC13-5] | the drainage agreement seam, ASSEMBLED from CU-2 + CU-4 + GMN Cor 4.19 (all on file); its residue = the pass debts of CU-2/CU-4 (this table) + the Cor 4.19 literature row (§4) + the [M] conditionalities CU-4's own consumption displays — the **D-12r** TB-CAP (τ-irr) perimeter (Step 11 grades CU-4 "TB-CAP (τ-irr) perimeter-conditional"; since REVISION 14, r15, priced at the §4 D-12r literature pin — no longer [M]) and **(E-N)**'s CU-4 touch through Thm 3(c) (the (E-N) row's displayed entry, a §3.1 (H3) member riding its own row) — both already priced at their own named homes, so nothing is hidden — and nothing else [the former "exactly the pass debts + the literature row — nothing else" elided CU-4's displayed §3.1 conditionality; membership home synced at REVISION 14] | O-4/O4T TR-OM (Step 13) |
| **Dfloor 3 minor gaps** | ID reduced-rep, ADD induction coprimality, TRANSFER sum-regularity — ride the next doc touch | Dfloor (Step 1) |
| **O-10 2 minor gaps** [added at REVISION 3 status sync] | the D-8 value-vs-law framing (collapses definitionally) + the box-count transport cite — found at the VERIFYING pass 2; **repaired at O-10 r2, landed at revision close** (3956500: the D-8 separation dropped as definitional with the existence range displayed; the (BOX) transport display + M07 §2.1 cite row; cores byte-stable). Per the wave-14 ledger: VERIFIED (leaf #6) STANDS, framing fixed; the r2 diff-confirmation rides the next harvest | O-10 (Step 15) |
| **B-layer verification rounds** [row added at REVISION 1, finding 3; B-4 added at REVISION 2, finding V2-4; **B-3 (doc level) added at REVISION 3, finding B4**] | B-1, B-2 (doc level), **B-3 (doc level)**, **B-4 (order-r layer)**, B-5, B-6, B-7 are PROVED-UNVERIFIED June documents (grades displayed at Step 2); the tree §4 protocol already schedules their verification-only rounds; B-3's core and B-2's counting core are VERIFIED-LEAN (B-3's doc level has the same status shape as B-2's — Lean-verified core + PROVED-UNVERIFIED June document — and is now listed like it); B-4's order-1 instance = B-3's VERIFIED-LEAN core, and its order-r content (`M6_lemma.md`) has as modern face O-9's OL-B, discharged to GD-3 — on the modern path the orders-≥ 2 content rides §3.1's (GR-B), so this row prices only the June-document verification rounds; B-8 SUPERSEDED, cited nowhere | B-layer (Steps 2, 3) |
| **K-12 faithfulness-audit record** | the Lean `undec`/`hExhaustP` vs mathematical-classifier audit (DELTA-1 §D1.5(g)); grade held pending | K-12/n = 2 anchor |
| **M17/RegPFinite faithfulness-audit record** [NEW row at REVISION 13, VC15 advisory 2 — grade-display conformance, the O-14a′/K-12 precedent] | the Lean-statement-vs-mathematical-statement audit for Step 4's (REG-p) finiteness leaf: the `MovesU/RegPFinite.lean` declarations (`regP_iff_pool_avoids`/`regP_failing_eq`/`regP_failing_finite`/`pow_hits_finite`/`UCarriers.detHyp` + companions, 14 theorems Lean-core) vs M17's mathematical Theorems A/B/C + Corollary U. ON RECORD: dual provenance (Fable proof + independent Codex twin from a neutral brief, load-bearing steps agreeing; `lean/notes/M17_FORMALIZED_2026-07-31.md` incl. its honesty ledger — Theorem C and the cardinality bound NOT formalized; the vacuity note on supplied pinned families). OWED: the faithfulness-audit record itself (the K-12 genre). Until it lands, Step 4's grade is **PROVED-UNVERIFIED** (the exact §0 word, rendered at Step 4 at REVISION 15, r16, VC17 advisory 2), NOT VERIFIED-LEAN — §0's definition requires the audit; Step 5's independent emptiness-at-𝔅_n result contains the impact | K-13/M17 (Step 4) |
| **End-to-end passes on THIS document** [synced at REVISIONS 1/3/4/5/6/7/8/9] | passes 1–8 RAN, each COMPOSITION-GAPS → each repaired by its revision (§§7–14): VC1 (Codex) → REVISION 1; VC2 (Codex) → REVISION 2; VC3 (Fable, model-diverse) → REVISION 3 — VC3 confirmed the composition's MATHEMATICS; VC4 (Codex) → REVISION 4 — again all attribution/ledger integrity (a new hypothesis-grade row surfaced ((PACK)), one attribution weakened favorably ((SQ) unconditional)); VC5 (Codex) → REVISION 5 — two blockers of ONE genre (consumed [T] rows are open propositions: the (H6) promotion) + the standing T-U advisory, with the eight-row mapping, both unconditional clauses, the (ADM) operative form, the (PACK)/(K3-δ) propagation, acyclicity, and the Σ_τ R_τ = 1 derivation all PASSING; VC6 (Fable, model-diverse) → REVISION 6 — 3 blockers, the VC5 genre WALKED TO COMPLETION (the last consumed-open-in-[T] rows: (H1) enlarged by GD-1-beyond-1 + the GD-6 instances; (H7) = (I-τ)/K-LOC) + 6 advisories (incl. A-9: the T-U §U.SQ.6 table now EXECUTED at §3.4), with the nine-row bijection, per-clause attribution, acyclicity, Σ_τ R_τ = 1, (SL≥2), and the leaf-#8 interlock all PASSING; VC7 (Codex) → REVISION 7 — 2 blockers, BOTH attribution/warrant findings at REVISION-6 A-6 adjudications (Step 6's all-order KEY1 consumption had no displayed (H1) member → member (d) added; (PACK)'s correspondence could not warrant the three pack row-laws → the (H5) enlargement PACK-i/ii/iii) + 3 advisories, with VC7's findings 6–11 CONFIRMING the composition (the §3.4 T-U tables genuine incl. the three flagged adjudications; the (H1) enlargement + (H7) sound; the TEN-row bijection correct with per-clause attribution consistent; the walk proving (ROOT-C) quantifier-for-quantifier; acyclicity PASSING); VC8 (Codex) → REVISION 8 — 1 blocker + 1 advisory, the NARROWEST residue of the eight passes, BOTH at the REVISION-7 repairs themselves ((H1)(d)'s "key has degree > 1" quantifier rested on an UNPROVEN identification with the order-≥ 2 reads → the member REQUANTIFIED to read order; the PACK K2/K3 sub-attribution inconsistent across summaries → synced, PACK-i/ii at K2's glue, PACK-iii at K3's ring-hom with D-6), with all three r8 advisory repairs, the TEN-row count, the quantifier walk (Σ_τ R_τ = 1 properly delayed to Step 19), acyclicity incl. 17 ≺ 18 ≺ 18b, and no regression on the §3.4 tables/(H7)/bijection/pack-law non-inference all PASSING. The criterion: ≥ 2 consecutive CLEAN end-to-end passes, ≥ 2 models, verifiers who saw none of the per-leaf rounds (tree §4 Root criterion); after pass 8 the consecutive-clean counter stood at ZERO, with VC9 next (charge at §14). **ACCEPTANCE [synced at REVISION 9]:** VC9 (Codex) and VC10 (Fable — the model-diverse pass) RAN on the IDENTICAL REVISION-8 text and BOTH returned **COMPOSITION-SOUND, ZERO blockers** (`lean/notes/openmath/VC9_root_report.md`, `lean/notes/openmath/VC10_fable_report.md`) — the criterion is MET (two consecutive clean passes, two models, fresh verifiers): the composition is **ACCEPTED at (ROOT-C)'s displayed conditional scope**, and the consecutive-clean counter is **TWO**. VC10's three ADVISORIES (display hygiene only) are dispositioned at §15 (REVISION 9, post-acceptance) — advisories do NOT reset the counter; it STAYS TWO. **SUSPENSION + REVISION 10 [r11, 2026-08-04 — the CURRENT standing]:** the acceptance was **SUSPENDED** at the stop-the-line (ledger 44ce0a7): the owed D-11 pass 3 returned CRITICAL and Steps 17/18b consumed the refuted layers AS PROVED. REVISION 10 (§16) CURES the composition-level defect (the repaired r4 leaf consumed at true scope; every missing premise a displayed [M] member; TWELVE rows ↔ (H1)–(H9)) — but a suspension cure is a TEXT CHANGE: **the consecutive-clean counter is ZERO of 2**, the acceptance is NOT in force, and it is re-earned ONLY by two fresh consecutive clean model-diverse end-to-end passes on the IDENTICAL REVISION-10 text (fresh verifiers, tree §4 Root criterion). **VC11 [synced at REVISION 11, r12]:** the pass RAN on the REVISION-10 text (`lean/notes/openmath/VC11_report.md`) — **COMPOSITION-GAPS** (4 blockers + 1 advisory, ALL r11 RIPPLE-SYNC defects: Steps 16/19's scope displays + the final ∎ stale at (H7)/PACK-i-iii, §3.4 item 12's routing contradicting the (H8) row, the 6(iii)–(ix)+7 residue list omitting (H9), Step 18b's Consumes summary omitting (ADM)) — with the TARGETED r11 repairs CONFIRMED (Step 17's per-clause measures, Step 18b at (RE)/(PACK-iv), the twelve-row bijection, (NSF-KEY) known-false-at-HEAD with gated retirement, (0)/(SQ) unconditional, 17 ≺ 18 ≺ 18b) → REVISION 11 (§17), pure display syncs; a text change: **the counter STAYS ZERO of 2**. **VC12 + VC13 [synced at REVISION 12, r13]:** VC12 RAN on the REVISION-11 text (`lean/notes/openmath/VC12_report.md`) — **COMPOSITION-SOUND, ZERO findings**: the counter reached ONE of 2; VC13 (Fable — the model-diverse pass) RAN on the same REVISION-11 text (`lean/notes/openmath/VC13_fable_report.md`) — **COMPOSITION-GAPS** (2 blockers + 3 advisories, ALL the stale-hypothesis-enumeration genre: §1's (H5) display at PACK-i-iii only, Step 18's stale "— §1" clause-(R) quote + five-hypothesis count, Step 17's Consumes omitting (H9)/(PACK-iv), the (H8) "owed-pass G-2" provenance irreconcilable with the §3.2 O-11 row, the (AGR) residue eliding CU-4's [M] conditionality — the walk/bijection/seams/acyclicity/(NSF-KEY) discipline all CONFIRMED) → REVISION 12 (§18): pure display syncs + the GLOBAL ENUMERATION AUDIT (every operative hypothesis-enumeration display checked against §1 and synced); **the counter RESETS to ZERO of 2**; VC14 next (charge at §18). **VC14 + VC15 — RE-ACCEPTANCE [synced at REVISION 13, r14]:** VC14 (Codex, fresh context; `lean/notes/openmath/VC14_report.md`) RAN on the REVISION-12 text — **COMPOSITION-SOUND, clean** (the enumeration-audit claim TESTED and held; zero regressions): counter ONE of 2; VC15 (Fable, fresh context — the model-diverse, deciding pass; `lean/notes/openmath/VC15_fable_report.md`) RAN on the same REVISION-12 text — **COMPOSITION-SOUND, ZERO blockers** (explicit null result on the blocker hunt; 4 advisories, display hygiene) — the bar is MET (two consecutive clean model-diverse passes, fresh verifiers): **the composition is RE-ACCEPTED at (ROOT-C)'s twelve-row displayed conditional scope** (ledger "RE-ACCEPTANCE: THE BAR IS MET AT TWELVE ROWS"). REVISION 13 (r14, §19) is the post-acceptance display/status-sync batch (the r10/REVISION-9 precedent); it is a TEXT CHANGE, so a confirming end-to-end pass **VC16 is OWED to re-anchor the accepted text on the REVISION-13 wording** — until it lands, the acceptance citation points at the REVISION-12 text. **VC16 [synced at REVISION 14, r15]:** the confirming pass RAN on the REVISION-13 text (`lean/notes/openmath/VC16_report.md`) — **COMPOSITION-SOUND, zero blockers** (advisories only: the Step-4 grade reads as PROVED-UNVERIFIED under §0's vocabulary until its audit lands; the D-12r §4 roster sync owed at the move; plus five confirmations incl. the (NSF-KEY) retirement, the K-CUT re-scope, the structural invariants, and no REVISION-12 regression) — **the acceptance RE-ANCHORS on the REVISION-13 text**, and finding 2 adjudicates the D-12r reclassification WARRANTED. REVISION 14 (r15, §20) EXECUTES the move (D-12r [M] → the §4 literature pin; ELEVEN rows ↔ (H1)–(H9)); a text change: the confirming pass **VC17 is OWED on the REVISION-14 wording** — until it lands, the acceptance citation points at the REVISION-13 text | Phase C |

**GD23 row RETIRED [REVISION 1 status sync]:** the former "GD23 gap-9 light repairs"
row is DISCHARGED — rev 4 repaired all 9 gaps (f9878d1, cores byte-stable), the
gap-check ran clean, and GD23 was ADJUDICATED **VERIFIED (leaf #4)** at the 2026-08-02
harvest (c43b13b). No GD23 pass debt remains; its (GR-B) conditionality stays in §3.1.

**CU-1 and O-10 rows RETIRED [REVISION 3 status sync]:** the former "CU-1 r5" and
"O-10 pass 2" rows are DISCHARGED at the 2026-08-02-night harvest (ledger 7b28562) —
CU-1's r5 gap-check ran GAPS-CLOSED (0/0, commit 5329a4e) → **VERIFIED (leaf #5)**;
O-10's model-diverse pass 2 ran SOUND-WITH-GAPS with the mathematics airtight
(5296528) → **VERIFIED (leaf #6) at scope** (its two minor gaps ride as the doc-touch
row above). No CU-1/O-10 pass debt remains; CU-1's (GR-B)/pin conditionality and
O-10's K-LOC/(I-τ) interfaces stay where they are (§3.1/§3.3).

**O4T row RETIRED [REVISION 4 status sync]:** the former "O4T r4 + confirming pass"
row is DISCHARGED — the confirming pass (pass 4, acceptance) ran **SOUND, CLEAN: 0
criticals / 0 gaps** (8602e97: core independently re-derived incl. both CEIL corners;
harness re-run digit-for-digit vs the sealed JSON) and O4T was ADJUDICATED
**VERIFIED (leaf #7)** at the confirming-passes harvest (ledger 85670f7) —
**VERIFIED = 7: O-13, O-12, Dfloor, GD23, CU-1, O-10, O4T**. No O4T pass debt
remains; the (AGR) inherited-[V] row and Step 13's displayed conditionality
attribution (VC4-4) stay where they are.

**O-9 and CU-2t rows RETIRED [REVISION 5 status sync]:** the former "O-9 r4 +
pass 4" and "CUC + CU-2t R3 pass" rows are DISCHARGED at the 6a7593b harvest.
**O-9**: pass 4 on r4 SOUND (0 criticals; two gloss-survival display sites) → r5
(9335057, gloss purge only, diff-audited) → pass 5 CLEAN 0/0 (825378d, K7 re-executed
byte-identical) → **VERIFIED (leaf #8)** at its honest scope (CEN-W/CEN-J conditional
on (GR-B) + (FRESH) + (ADM)-FULL, all §3.1 rows — unchanged by the adjudication).
**CU-2t**: R3 pass SOUND-WITH-GAPS 0-critical (both logic repairs verified) → R4
(b9f05db: the (T-READ) re-grade + the fold-state and a₀ ≥ 2 prose repairs) → R4
spot-check CLEAN (d14cd8c, no further pass owed per the verifier's own disposition)
→ **VERIFIED at scope (leaf #9)**, conditional per §9.5 on exactly the three terminal
rows (now the §3.1 (H6) group) + the pinned literature —
**VERIFIED = 9: O-13, O-12, Dfloor, GD23, CU-1, O-10, O4T, O-9, CU-2t**. The CUC
consolidation record's remaining debt is its constituent re-label passes = the named
registry rows above (P(O-2a r5)/P(O-1thr r4)/P(O5t r3)); no separate CUC row remains.

### 3.3 Transcription/audit debt [T] and named pins/flags

| item | content | owner |
|---|---|---|
| **GD-5 residue: the S-1 audit (incl. C.1.5)** | the one-page composition note + machine transcription audit (O-1thr machine ↔ O-2a stage interface F.0–F.2 at order 1) + the order-≥ 2 KEY1 block (same method). **Explicitly includes C.1.5** [REVISION 1, finding 5]: T-2's deferred graded-presentation obligation ("downsets are literal digit systems") — definitional at the mathematical machine (O-1thr §4 (S-1)); its transcription half is THIS audit. **KEY1-generalization adjudication [REVISION 6, finding A-6(i); SUPERSEDED at REVISION 7, finding VC7-1]:** proof on file — NO (unchanged); but the REVISION-6 consumption ruling ("consumed by NO order-≤ 1 mandatory step ... the item's mathematical face lives in the GD order-≥ 2 campaign perimeter priced by (H1)") is WITHDRAWN as insufficient — VC7 ruled that clearing the order-≤ 1 steps does not address Step 6's ALL-SITE theorems at higher OM orders, and "the (H1) campaign perimeter" named NO member assuming the statement: it was a consumed open proposition without an [M] home. The mathematical face is NOW the displayed §3.1 member **(H1)(d)** (stated there to match Step 6's use — the WGEO-consumed key-compliance block at EVERY order-≥ 2 site, key degree irrelevant; REQUANTIFIED to read order at REVISION 8, finding VC8-1), consumed at Step 6's order-≥ 2 conditionality line; this row's [T] content is the transcription/composition audit alone (unchanged) | GD-5 (D2.6 step 2) / T-2 → Step 6 |
| **D-5 disposition** [row added at REVISION 1, finding 5] | the tree's D-5 (WELLPOSED) carrier/equality design = O-2a rev 5 §§1–2 (two carriers, Θ, consistency predicate, Lemma R) — PROVED CONTENT of that leaf, verification debt = P(O-2a r5); the wider semantic-invariance clause is carried by each consuming leaf's own displayed carrier section (Steps 3, 6, 10), consumed nowhere as a separate node | O-2a (Step 7) |
| **(O6) canonicalizations** [row added at REVISION 1, finding 5] | O3design §1: branch-letter keying + deep-marker pooling — DEFINITIONAL census-side bookkeeping, no proof content (CUC §3(c)); V16/V21 arc on the O3design record | O3design → CU-3 (Step 11) |
| **GD-4 / GD-6 engine-bookkeeping units** | emission/record discipline (W1–W5, (c0), AUX band; K-OB-E's GMN Def-3.9 arithmetic is the one semantic clause) and run-decomposition conformance; proof-by-construction routes on record; CU-2t's terminal rows — RE-BILLED at the CU-2t R2 (567814d, CUC F4) and AMENDED at the CU-2t R3 (81589e9, CU2tpass G1; folded here at REVISION 4 per the R3's §9.5 touch (3)) as **THREE genuinely NEW unproved mandatory-path GD-4 obligations: (T-DEC) (split at R3 into (T-DEC-dec) emission discipline at decided sites + (T-DEC-cor), the corner fence), (T-READ) (emitted shape/position fields = the (c1)-shape polygon-side datum — the (RC-read) supply; NO displayed machine-side home on file, purely owed), and (T-VERD)** (in no printed clause list before CUC §9.4 displayed them; no new open family, but the residue GREW by these three rows — the count history 0 → 2 → 3 across rev1/R2/R3 is stated plainly: rev 1's "zero new inputs" was retracted at R2, and R2's TWO undercounted by one); GD-4's owner brief absorbs the §9.4 scope note at its next revision (the three rows + the §9.2a inventory-closure question + (T-DEC-cor)'s exhaustiveness clause, the last jointly with GD-8); **enters Step 18 through D-15's (M6b)(iii)/(ii−) clauses** (O-11 K4-CYL — declared row D-a at O-11 r2). **[REVISION 5, finding VC5-2: the three terminal rows are PROMOTED OUT of this [T] row** to the §3.1 [M] group (H6) — under the ledger's own classification rule they are open propositions consumed on clause (R)'s mandatory path, not transcription debts; (T-READ) carries CU-2t R4's GD-2/GD-5-family re-grade. This row KEEPS the engine-bookkeeping content (W1–W5, (c0), AUX band, K-OB-E's semantic clause) and the owner-brief absorption duty; its Step-18 D-a entry line now points at the (H6) row for the promoted content. **[REVISION 6, finding F-3 — two further repairs: (i)** the GD-6 run-decomposition-conformance INSTANCES consumed at orders ≥ 2 (Step 10's conditionality line) are PROMOTED OUT of this row to §3.1 (H1)(c) — "proof-by-construction routes on record" are routes, not proofs, the same evidentiary status under which (T-DEC)/(T-VERD) were promoted at VC5-2; this row keeps only GD-6's UNCONSUMED bookkeeping/route remainder. **(ii)** the D-a consumption ambiguity resolved: what Step 18's D-a/K4-CYL consumes of this area's OPEN content is exactly the (H6) row — the KEPT bookkeeping content (W1–W5, (c0), AUX band, K-OB-E's clause) is consumed at the CU/GD construction level where its proof-by-construction records live, by NO mandatory §2 step as an open general lemma; if a future pass finds a mandatory consumption of the kept content, the VC5/VC6 genre rule applies and that item promotes] | GD-4/GD-6 owners |
| **GD-7 flags: (GMN-COH)/(R-0)/(R-1)/(R-2)/(R-3)** | O5triple's five citation-grade flags (sprouted-type dissection, flow coherence, order-0 anchor, ADEQ read-back, representative lookup) — one GMN §2.1/§3/§4.1 reading session covers them; NOT covered by the executed LITUNIT. **Pins note [REVISION 6, finding A-7]:** the section-granularity pin is a CLAIMED range, unexecuted — [T] here is honest only with the discharge made a NAMED acceptance precondition, so it is: this row (with the LIT-1 pagination row it shares a session with) must be EXECUTED — per-flag verbatim extraction lines at the printed source, LITUNIT-style MATCH verdicts — before the §6 clean-pass acceptance bar; partial per-flag loci already on record (O5triple attempt): (GMN-COH) ← the sprouted-type cluster identity under eq. (37) (the eq.-(37) locus itself is §4-pinned), (R-1) ← the representative's "of type t" shape (GMN §3 region), (R-2) ← the exact-key polygon convention at order r (order-1 falsifier-covered, O2a F5 871 samples); (R-0)/(R-3) loci owed at the session. Given this project's two-false-axioms history, no flag is consumable from the range pin alone | GD-7/literature unit (Step 12) |
| **GD-8 order ≥ 2** | the exact-key polygon seam beyond order 1 (order-1 proved, falsifier-covered); expected verbatim over GD-2's frame tie. **Consumption scope [REVISION 6, finding A-6(iii)]:** consumed by NO mandatory §2 step — its only named consumers are the QUARANTINED Corollary CU-2t-deep (displayed non-mandatory at Step 11) and GD-8's share of Step 12's five-flag duty, which is the flags' citation-grade reading (the GD-7 row's session), not the ≥ 2 seam itself; O-9's VERIFIED reduction names exactly TWO ≥ 2 opens ((GR-B)/(FRESH)) — the census does not consume this seam — and Step 10 consumes GD-2/GD-3(min)/GD-6 (the (H1) group), not GD-8. The ≥ 2 write-up belongs to the GD order-≥ 2 campaign; recorded here LC-COH-style (its "(R1)–(R4) shape" stays honestly open, unconsumed) | GD-8 |
| **(KP)/(KPirr): the properness-proviso leg** [row RESCOPED at REVISION 6, finding F-2] | properness — a genuine side condition only at e·g = 1 refinement reads — with the FGMN Lemma-5.3 discharge DISPLAYED: this leg is pinned to a printed source, so it keeps [T]. **The beyond-order-1 (KP)/(KPirr) representative-admissibility clauses are PROMOTED OUT of this row** to §3.1 (H1)(b): they carried no pin and no proof on file while consumed at Steps 10–12 through O-2a's admissible-representative-family clause (= OL-2a-1, an exactly-stated open lemma) — the same-shape GD-2/GD-3 residue was already (H1), and the asymmetric classification was the F-2 blocker | GD-1 |
| **Declared pins (P-ROOT)/(P-KEY)/(FR≡)** | CU-1's three declared pins replacing silent cross-brief glue; (FR≡) has refutable order-2 content = the gate's coherent-convention finding. **(FR≡) adjudication [REVISION 6, finding A-6(iv)] — why refutable content is [T] here:** a declared CONVENTION pin is the Step-6 sloped/sheared genre — CU-1's proofs are STATED OVER the pinned convention and the leaf is VERIFIED with the pin declared, so no open proposition is consumed: what is owed is the transcription audit that every consumer reads the SAME convention (this row's [T] content). The order-2 content being refutable makes the pin FALSIFIABLE, and the gates ran COHERENT (the coherent-convention finding — a flag supporting the pin, never carrying it); a future refutation would be a fence event on CU-1's declared perimeter, not a silent edge | CU-1 (Step 10) |
| **Sloped/sheared convention pin** | O-1thr's window-height convention (the depth-2 falsifier forced it; 41,720/0 vs 7,472 violations unsheared) | O-1thr (Step 6) |
| **(I-τ) + K-LOC interface** [PROMOTED at REVISION 6, finding F-1 — tombstone] | the adapted-cell → engine-stratum presentation for O-10's Theorem 2 (K-LOC = M07 OL-3: continuation reads of branch j = OM reads of f_j up to cell-constant normalization) — **now the §3.1 [M] row (H7)**: an exactly-stated open lemma + an unproved presentation, consumed on clause (R)'s mandatory path (Steps 15/18 K1) with no pin and no supplying row, could not sit in [T] under the ledger's own rule; the LAST of O-11 r2's D-a/D-b/D-c triple adjudicated. This tombstone keeps the invariant's one-home-per-pin trail; content and owners at the (H7) row | → §3.1 (H7) |
| **(S1)/(S2) seams of D-11 M2** | (S1) = GD-3-FULL/O-9 hand-off ← Step 14 (+ (GR-B)); (S2) = the O-11 composition datum ← Step 18. Since REVISION 1 (finding 2) the step order 17 ≺ 18 ≺ 18b makes both seams STRUCTURALLY earlier — the acyclicity is in the walk itself, and the seams discharge when Steps 14/18's passes land. D-11 r2 (566b967) declares a THIRD seam **(S3)** = the projective assembly weight's MASS reading, owner annex A-1 — POSITIONED at REVISION 2 (finding V2-1): (S3) conditions ONLY the projective reading of M2's conclusion (the r2's own scoping), which NO §2 step consumes — Step 18b consumes M2 at monic scope, and the projective normalization is annex by standing directive; (S3) is therefore an ANNEX A-1 obligation, not mandatory-path residue, and appears in no (ROOT-C) hypothesis | D-11 (Step 18b, non-consuming) / annex A-1 |
| **OL-O12-1 contingent-escape flag** [row added at REVISION 3, finding A7] | Step 5's (B-INST) escape: fires ONLY if a future pack departs from 𝔅_n's three on-record kernel organizations — currently UNFIRED and consumed by NOTHING; homed here per the §7 closing standard (every named pin/flag has exactly one ledger home). If it ever fires, the O-12 pole-freeness theorems need re-derivation at the new organization | O-12 (Step 5) |
| **OL-O12-2 data pass** | the finite transcription/correspondence pass identifying the complete degree-3 table as an (O3) member of 𝔅₃ (math half closed at D-11). Since REVISION 4 (finding VC4-3) its GENERAL-n face is hypothesis-grade: the §3.1 (PACK) row (= (ROOT-C)(H5), D-11's open clause 4) — this row keeps only the executed-instance duty at n = 3 | O-12/D-11 |
| **T-U §U.SQ.6 correspondence audit** [row added at REVISION 3, finding A8] | Step 19 consumes THEOREM U conditional on its displayed §U.SQ.6 kernel ledger, discharged by the claim that the ledger's items coincide with the Steps 1–18b roster + the recorded evictions/re-routings (e.g. the R-QNT kernels struck at DELTA-1 §D1.4) + the annex items; the ROW-BY-ROW correspondence was ASSERTED, not displayed, through REVISION 5. [RE-CONFIRMED as the recorded [T] audit row at REVISION 5, advisory VC5-3.] **[EXECUTED at REVISION 6, finding A-9 — the third consecutive recording forced the display: the per-item pointer table (§U.SQ.6 items 1–21 → step/eviction/annex, with the Lean-bridge kernel rows of `REMAINING_CENSUS_2026-08-01.md` §2.3 / `MovesU/BridgeKernels.lean` cross-walked) is now §3.4.** Residual duty of this row: VC7 confirms the table (three of its dispositions are NEW adjudications made at the table and are flagged there: items 4/13c's consumer-audit evictions and the items-15–21 empirical-roster disposition); the row RETIRES on that confirmation. **[CONFIRMED at VC7 (findings 5/6): the table's warrants ruled compositionally genuine and the three NEW adjudications sound — two corrections applied at REVISION 7 (the 6(iii)–(ix)+7 residue list, advisory VC7-3; the items-15–21 gate-preserving wording, advisory VC7-5). This row is RETIRED as of REVISION 7, kept as tombstone per the one-home trail]**] | T-U (Step 19) / Phase C → §3.4 |
| **BASE-1(f) fence** | Dfloor's classifier-side n = 1 clause, fenced to the classifier steps (served by Steps 6–13's package) | Dfloor/CU |
| **LC-COH** | the displayed open note from the O-3 clause-3 re-key — CONSUMED BY NOTHING (kept for honesty) | tree D2.1 line 1 |
| **(REP-IRR)** | the unpinned literature pull behind the QUARANTINED Corollary CU-2t-deep (GD-8 corner) — consumed by nothing mandatory; flagged to the literature unit | CU-2t/literature unit |
| **W3 carrier note** | the τ-irr seam object is a PAIR, not an 𝔈-concatenation (CU-2t display; convergent with CU-1's split) | CU-2t |
| **LIT-1 printed-TAMS pagination** | the standing GMN confirmation duty (LITERATURE-CONDITIONAL until confirmed); one session with the printed TAMS covers it + the GD-7 flags. **PARTIALLY DISCHARGED [REVISION 14, r15 — the VC16 finding-2 roster sync]:** the (OM-SAT) slice is CONFIRMED on file (unit D12R-U3, `lean/notes/openmath/H3_omsat_brief.md` §1 — six loci VERBATIM against the printed TAMS: Cor 1.20 p. 374, Cor 3.8 pp. 398–399, Def 3.9 p. 399, Def 3.10 + eq. (37) p. 399, Lemma 3.11 p. 400, the base-field convention p. 366; citation line EXACT; printed = arXiv v2 numbering, no drift), and the brief contributes a located-page map for the REST of the §4 item-1 pin list (NOT verbatim-checked). The remaining loci + the GD-7 flags still owe the session — this row stays until it runs | literature unit |
| **LIT-3 numbers + Mathlib probe** | Neukirch II §4 Prop 4.6 / Serre LF I §6 numbers to confirm at declaration; probe `HenselianLocalRing` first. **DISCHARGED AS STATED [REVISION 14, r15 — the VC16 finding-2 roster sync]:** numbers CONFIRMED on file (unit D12R-U1, `lean/notes/openmath/H3_henlift_brief.md` — Neukirch (4.6) "Hensel's Lemma" printed p. 129, index cross-checked; the printed (4.6) carries NO uniqueness clause, disposed via the brief's §2 displayed companion lemma; Serre LF I §6 Prop 15 printed pp. 17–18); the Mathlib probe RAN, answer **ABSENT** (`HenselianLocalRing` supplies root-lifting only — NO coprime-factor lifting) — the pin STANDS AS A PAPER CITE (§4 item 5), and the optional Lean materialization (D12R-U2a/b/c) owes its OWN axiom-policy loop (citation + faithfulness entry + guardian audit) if/when it lands | literature unit (Step 12) |
| **LIT-8 per-use-site transcriptions** | LIT-8a..8e split (DELTA-1 §D1.5(d)); exact numbering owed at each declaration site (O4T's inventory did this for its own sites; Dfloor displays statements) | per consumer |
| **O-11 → D-11 widened-charge items (v)–(vii)** [row added at unit TREE-GAPS, audit gap G-4 — REVISION 1's §7 note "(5) ... noted pending acceptance" promoted to an operative row] | O-11 r2/r3's restated D-11 entry WIDENED the WAVE-8 charge: (v) the pack's carried laws rsh_interp/r_bdd/wsh_ok; (vi) the DVR scope-hygiene audit of K3-c; **(vii) general-n H.6 in-fence conformance of the built tables** — the surviving live condition of the tree T-5 row's "six H.6 fences gate [1v]-FULL" (Step 18's fence display; full adjudication + fence-by-fence mapping: tree DELTA 4 §D4.2): the six fences stay the H-DOMAINS perimeter's open kernels (owners/refuters unchanged, off the mandatory path); what Step 18's K3-a consumes is ONLY this conformance, proved nowhere at general n (V-n3/H-n3 are sealed n = 3 evidence). **[REVISION 5, finding VC5-1: item (vii) PROMOTED to §3.1** — the (H6) group, ending the "kept [T]" disposition: VC5 ruled the condition an undischarged mathematical premise on clause (R) (Step 18's K3-a consumes it; "proved nowhere at general n" is this row's own text), analogous to (PACK) — the escalation happened by classification-rule ruling, not by owner rejection or perimeter exit. This row KEEPS items (v) (the pack's carried laws rsh_interp/r_bdd/wsh_ok) and (vi) (the DVR scope-hygiene audit of K3-c) at their [T] grade; acceptance of the widened charge still owed by the D-11 owner. **Item-(v) adjudication [REVISION 6, finding A-6(ii); RESOLVED at REVISION 7, finding VC7-2 — the flag FIRED]:** proof on file — NONE DISPLAYED in the D-11/O-11 records this document consults (unchanged); consumed where — Step 18's restated D-11 entry item (v), sub-attributed [REVISION 8, finding VC8-2]: PACK-i/ii at K2's evaluation glue, PACK-iii at K3's evaluation ring-hom (with D-6). The REVISION-6 pricing ("a pack equal to the intended 𝔅_n instantiation entry-for-entry carries its 𝔅_n-side laws ... item (v) therefore rides (H5)") is WITHDRAWN as an invalid inference: entry-for-entry equality transfers the laws only if an earlier displayed theorem establishes them at the corresponding 𝔅_n rows, and NO displayed theorem does — the parenthetical "the T-layer/O-12 displayed statements at 𝔅_n" named no statement stating rsh_interp/r_bdd/wsh_ok or implying them (Steps 2/5 state neither — VC7's warrant charge). The honest disposition is the (H5) ENLARGEMENT: the three laws are now EXPLICIT clauses **(PACK-i)/(PACK-ii)/(PACK-iii)** of the §3.1 (PACK) row — still no separate open row, but carried as displayed hypothesis content, never as a correspondence corollary; this row keeps only the owner's acceptance-of-charge duty] | D-11 (Steps 17/18) / O-11 r3 §0 |
| **O-14a/b/c engine-residue dispositions** [row added at unit TREE-GAPS, audit gaps G-1/G-2/G-3] | tree D-10's O-14 split, homed: **O-14a** guarded vertex-transport law — CLOSED at the e′ = 1 sub-perimeter, Lean twin `lean/LeanUrat/HC2/HK11a_vertexTransport.lean` (σV-vertex law, Lean-core); unguarded form stays REFUTED (HK52_stretchGate); ≥ 2 disposition = sign-off item 28 (the PARKED row above); consumed by NO §2 step (engine-internal — the Lean bridge's V9/HK cone; GD-2's clause-(c) HK52 trap is the ≥ 2 countermodel, not a consumption). **O-14b** cl19 repaired carrier ladder (post-K-7: the O1–O3 semantic ladder at a real carrier pack) — OPEN; K-7's "repaired carrier form" pointer lands HERE; sole consumer = the Lean-bridge slot wiring (queue item 22, `REMAINING_CENSUS_2026-08-01.md`; theoremU's slot bare-Prop, SAFE), PARKED by the 2026-07-31 math-first directive (fe3cda5). **O-14c** hStateNe — OPEN, expected derivable; = `BridgePre.hStateNe`, a named ratified Lean-bridge premise (BRIDGE_ADJUDICATIONS Q3); standing disposition = the IB-B2 derivation attempt (row drops if it closes). None of the three is consumed by any §2 step; none appears in any (ROOT-C) hypothesis (display sweep at unit TREE-GAPS, incl. the REVISION-4 (H5)) | Lean side, post-math (tree DELTA 4 §§D4.3/D4.4) |
| **Item-27/28 Lean statement repairs + O-14d instance population** | PARKED engineering (Lean statement fences; the wave-D carrier population) — outside the math criterion by DELTA-1 §D-10 | Lean side, post-math |

**What is NOT on the ledger (evicted from the mandatory path, recorded):** O-2b/OB1
(trace correspondence), OB2/CU-6 (σ-invariance under ∼_OM — the DELTA-3 audit: D-11
needs per-box lift uniformity instead), LIT-5/BNS entirely (zero root-side touch-points),
K-OB-C (Okutsu proximity), **K-OB-D (certificate-firing at O-5's threshold — evicted by
containment: sole consumer O-7; named here at unit TREE-GAPS, audit A-3 — the last IFK
REV-1 obligation without an explicit disposition line)**, D-16 (δ-tier execution
transport), O-6/O-7/O-8a (track count/weight charge/linear rates), **and `cl3_tails`
(X2-TAILS measure/cone tail estimate — the tree O-8 row's SEPARATE open estimate, owner
§H-DOMAINS family; its OWN eviction row at unit TREE-GAPS, audit G-5: quantitative-rate
content of the X.2 pricing family, consumed by nothing count-native — the root's
undecided-mass control is drainage (Step 13), never tail rates; tree DELTA 4 §D4.5)** —
ALL route R-QNT, needed only if explicit rates are
elected. htameFE/palindromy + the projective bridge are ANNEX (A-1/A-2).

**Closed/refuted tree ids — content homes (tree-§4 criterion-4 display; added at unit
TREE-GAPS, audit A-1/A-4/A-5).** T-11/JOINT-D1 → Step 2 inside T-1's perimeter
(D1-marginal machine-checked, `OM/DevBijection.lean`) · K-1 ksub / K-2 w17ii / K-15 →
CLOSED Lean-interface rows, nothing left for the composition to consume · K-3 →
refuted (caterpillar countermodel cited at Step 3); repaired math = O-6, route R-QNT ·
K-5 → refuted; the per-site true statement = O-1 (Step 6) · K-7 cl19_rep → refuted;
repaired carrier form = O-14b (the §3.3 O-14 row above) · K-8 → adapted-cell math =
O-10 (Step 15) · K-9 cl6 → level 1 at Step 14 (M08 Thm 2); order ≥ 2 = O-9 · K-10
cl15_align → Steps 7/8 + the §4 GMN Thm 4.18 pin · K-14 duties: (I0)/(I1)/(I3-rc) →
Step 10 + the D2.3 consumer walk; **(I2) level coherence → definitional at the
statement layer against T-3 §D4-R's D4R.4/L12 stabilization level (N(P̂) = max
per-prefix resolution level, finite by L8; M18 fable §6.2's displayed warrant) — its
instance-build face rides the O-14d/D-11 population; not a missing consumer-walk
edge (audit A-4)**.

### 3.4 The T-U §U.SQ.6 correspondence table [EXECUTED at REVISION 6, finding A-9]

Step 19 consumes THEOREM U (T-U, §U-SQUEEZE SQ.4) at its VERIFIED-DUAL grade,
conditional on the note's own displayed distance-to-done ledger **§U.SQ.6**
(`lean/notes/MOVES_2026-07-24.md` §SQ.6, items 1–21 over the capstone ledger
CL-1..CL-19; frozen 2026-07-24). Through REVISION 5 the claim "§U.SQ.6 ⊆ the Steps
1–18b roster + recorded evictions/annex" was ASSERTED (the A8/VC5-3/A-9 audit row).
This section DISPLAYS it. Sources read for the execution: the §SQ.6 list itself; the
kernel-ledger materialization `lean/notes/REMAINING_CENSUS_2026-08-01.md` §2.3 (the
BridgePre/Ctor/BK/seam rows) and `lean/LeanUrat/MovesU/BridgeKernels.lean` (whose
docstring pins every field to the frozen ledger display). Reading rule: "→ Step k"
means the item's mathematical content is supplied by that step's declared statement
at its displayed conditionality (open residue priced by the named §3.1 rows);
"EVICTED" means the recorded R-QNT/annex disposition applies (§3.3's eviction
paragraph); NEW adjudications made at this table (no prior recorded disposition) are
marked **[NEW — VC7]** (VC7 RAN and CONFIRMED all three — items 4/13c at finding 6,
items 15–21 at finding 5 as a consumption ruling; the markers stay as provenance,
and the two VC7 advisory corrections are applied in place below [REVISION 7,
findings VC7-3/VC7-5]).

**Table A — §U.SQ.6 items 1–21 → step / eviction / annex.**

| §U.SQ.6 item | content (compressed) | ROOT home + warrant |
|---|---|---|
| 1. CL-1 ESCAPE(E0), per-pool q₀ = p^δ | active-block solvability / spectral escape | → Steps 4/5 (O-12 Thms 1/3/4 + Lemma RR: margins ⇒ spectral radius < 1, every denominator a unit of ℛ at every prime power; K-13 the cofinite cap) — Step 18's walk consumes it as "Neumann under spectral escape" through O-12 at 𝔅_n; margins re-read at Step 17 (r1) |
| 2. CL-2 WEIGHT-CHARGE = X.1b (+ s(n)) | envelope weight-charge pricing | EVICTED — tree O-7, route R-QNT (DELTA-1 §D1.4; §3.3 eviction list); consumed by nothing count-native (census §2.4's cl2_route stays OPEN-MATH, owner [5], off-path) |
| 3. CL-3 X.2 PROGRESS ((X2-AFF)/(X2-BRIDGE)/(X2-CAP)) + (X2-TAILS) | coercivity/rate dichotomy | EVICTED — tree O-8a (linear rates), route R-QNT; (X2-TAILS) = `cl3_tails`, its OWN recorded eviction (TREE-GAPS G-5, §3.3): undecided-mass control is drainage (Step 13), never tail rates |
| 4. (X1a-ALIGN) (ALIGN-inc)/(ALIGN-rec) | the X.1a dictionary's proof duty | EVICTED by consumer audit **[NEW — VC7]**: its displayed consumers are X.2's constants + the depth ⟹ disc envelope — both the struck rate family (items 3/13c); the mandatory path's discriminant control is Step 13's Lemma C (M05-verified, self-contained). The X-n3 probe's recorded refutation of an X.1b reading (155,648 violations, adjudicated 2026-07-30) touches only this off-path family |
| 5. CL-4 X.3 termination a.e. | a.e. decidedness | → Step 13 (Lemma A: drainage ⇔ a.e.-decidedness + Theorem E at every prime, SEMANTIC layer unconditional; canonical-classifier transfer at general n = (AGR), the §3.2 inherited-[V] row; n ≤ 3 unconditional by Thm N3) |
| 6(i). CL-5 XHD = M4a-X (+ (ENT-U)) | the height-domains package | → Step 2 (T-5 H-DOMAINS at its VERIFIED-DUAL perimeter), consumed by Step 18's K3-a at exactly that perimeter; the package's open kernels are item 14's six fences — see item 14 |
| 6(ii). CL-5 D4R.0 | marked CLOSED in-list (ledger echo, not distance) | → Step 2 (T-3 §D4-R, dual-verified derivation; (a)/(b) discharged); the K-residual (c) = VP routes to item 10 |
| 6(iii)–(ix) + 7. the CTS-M family (PART / REP / MEAS / HMC / ACT / INIT+ENT-COUNT / COMP-typing) + CTS-M(iv)-POLY (CL-6) | the per-species value/mass semantic laws | governed by the recorded re-scopes (DELTA-1 §D1.5(e): T-1/T-7 semantic clauses route through the dictionary; D-9: [1v]-FULL re-owned by O-11): symbolic halves → Step 2 (T-7s/T-3); mass laws → Step 17 (M1/M1′/M3 over O-12's L1–L6′ base, Step 5 — at M1's r4 per-clause scope [REVISION 10, r11, edit E6]: families (i)–(iii) conditional on C_e, family-(v) per-occurrence readings, families (iv)/(vi)/(vii) excluded at (a″); M3 at separable-lift scope with the (H9) routing price) with measurability/cylinder structure → Steps 1/6 (D-1, D-15) and leaf labels → Step 12; the COUNT laws (CL-6's polynomiality = the census) → Step 14 (M08/O-9) and Step 18's K3 assembly; COMP-typing → Steps 7/10 (Θ's typing; OL-6 binding); ACT's (REG-p)-separateness note → Steps 4/5 + the D1.5(f) T-8 re-scope. Open residue priced at §3.1 [residue list CORRECTED at REVISION 7, finding VC7-3 — the routes through Steps 6/11/12/17 also inherit D-12r and (E-N), i.e. the rest of (H3); the table's reading rule (step pointers inherit each step's declared conditionality) already prevented a hidden hypothesis, but the former list ((H1), (FRESH), (ADM), (PACK), (H6)) was inaccurate as a purported exhaustive residue]: (H1) group, (H2) = (FRESH), the FULL (H3) ((ADM) + (E-N); the routes ALSO inherit the §4 D-12r pin's TB-CAP perimeter, literature-conditional since its REVISION-14 reclassification out of (H3)), **(H4a) + (K3-δ)** [RUN TO CLOSURE at REVISION 9, VC10 advisory 3 — this row's own route through Step 18's K3 inherits them at K3-c's base-changed β-legs (the δ > 1 pools), exactly as row 9 already names explicitly; the VC7-3 correction completed], (PACK), (H6), **(H9)** [added at REVISION 11, r12 — finding VC11-4: this row's own M3 pointer names the (H9) routing price (M3 at separable-lift scope), so the route through Step 17's M3 leg (via Steps 11/12) inherits (H9) = (NSF) and the purported closed list must carry it] — the list is now CLOSED under the table's own reading rule (step pointers inherit each step's declared conditionality; every step route this row names has been traced): no CTS-M face is consumed outside these homes |
| 8. CL-7 TRACK-COUNT + L(n)/D(n) + positive-genuine-increment | p-uniform decided-family bounds | EVICTED — tree O-6, route R-QNT (§3.3 eviction list; K-3's repaired math = O-6, likewise struck); the count-native walk needs only per-level finiteness (inside Step 1's cylinder count) + the p-free menu bound (Step 3). Lean face `BK.slice_bound` carried, permanently fenced (census §2.3) |
| 9. CL-8 REL.1 / REL.2(a)(b)(d)(e) / REL.3 | the relative-measure ledger | → Steps 15 (O-10's adapted-cell product law — the (SIB) mass statements), 17 (M1 mass laws), and the β(q^δ)-typing leg (e) → Step 16 + Step 18's K3-c = (H4a) + (K3-δ) (§3.1); the PCI/height-elimination package (REL.2(a)/(b)/(d) + XRB) is CL-9's RS.0(β), proved in [3] = the VERIFIED-DUAL T-5/T-8 layer (Step 2) |
| 10. CL-10 (SIB)/(SIB-STEP) + (JC-multi); VP; VP-SOUND; TREE-EXP/TREE-N | the tree-assembly package | (SIB)/(SIB-STEP)/(JC-multi) → Step 15 (Thm 2 unconditional in factor coordinates; T-6's (SIB-STEP) at the statement layer per Step 15's Delivers; engine-stratum application = (H7), §3.1). VP = the D4R.0-K(c) verdict pin → Step 6 (D-15, the pin's home) with TB-CAP per clause: (τ-hen) proved, (τ-irr) = the §4 D-12r literature pin [re-pointed at REVISION 14, r15 — formerly the (H3) row], (ns-null) → Step 1's Appendix A + T-6 (Step 2). VP-SOUND's two cites → Step 12 (LIT-3 coprime Hensel + GMN Cor 3.8/Def 3.9 — both §4-pinned). TREE-EXP/TREE-N → Step 2 (T-3/T-6) + Step 6 (D-14's per-site read locality = the transfer kernel's repaired form) |
| 11. CL-11 no-equal-e-feedback + DEG-CONS | degree conservation / well-foundedness | DISCHARGED: PROVED at all n Lean-core (M09 `KsubGeneral.lean`, census §2.3) + Step 5's (SL≥2)/L4 strict descent + Step 17's M1 degree checksums (Step 12 Thm B consumes checksums only) |
| 12. CL-17 shallow assembly (routing + unique shallow cut + W_Ŝ regrouping; (ii) production) | the RS.1 interface | RE-ADJUDICATED at REVISION 11, r12 — finding VC11-3 (the former routing sent the cut content to the verified Step-2 layer while the (H8) row displays it as OPEN; both could not be true): the shallow-CUT content — routing + EXACTLY-ONE shallow decomposition with exhaustive-exclusive read ownership + the weight-preserving W_Ŝ regrouping — is HYPOTHESIS-GRADE → **§3.1 (H8) = (K-CUT)** (an OPEN kernel of T-6, M04 Lemma 3.3's hypothesis, consumed inside Step 18's K1 regrouping — clause (R); clause (UB) through the transported chain); Step 2 supplies only the SOLVE face (T-8's block-triangular solve + T-5 domains, VERIFIED-DUAL — no cut-decomposition claim); the production burden is PROVED Lean-core at every chain (`w17ii_pin`, census §2.3 — the banked burden retired) |
| 13. CL-18 (J-RAT); CL-19 (JC-INV) | per-cell rational tables; joint-cell invariance | (J-RAT) → Step 2 (T-7s tables) + Step 14 (census polynomials) + Step 17 (M1 r4: family-(i)/(ii)/(iii) entries ARE masses CONDITIONAL on C_e, families (iv)/(vi)/(vii) excluded at (a″) [re-scoped at REVISION 10, r11, edit E6]); its general-n table BUILD = Step 18's K3 + (H5)/(PACK). (JC-INV) = `cl19_rep`: REFUTED AS STATED (K-7 → refuted, §3.3 closed/refuted roster; M11's 4 ≠ 0 countermodel at n = 2) — the mandatory path never consumes it: D-11 needs per-box lift uniformity instead (D-15, Step 6; DELTA-3 §D3.1, zero ∼_OM/OB2 touch-points); the repaired carrier form = O-14b, §3.3, consumed by NO §2 step |
| 13a. CL-13 CTS-M(i) SYNTAX | the ONE fixed finite p-independent state set | → Steps 2/3 (T-4's letter catalogue + O-13/D-12's p-free Sk(n); `cl13_wf` DISCHARGED — `menuWFT_holds`, census §2.3) |
| 13b. CL-14 M1 + M5 | marked CLOSED in-list (ledger echo) | → Steps 2/3 (the finite branch-shape catalogue); residuals route per the list itself (VP → item 10; SP-FIN/SP-OUT → item 13a) |
| 13c. CL-15 X.1a per-species GMN index dictionary | envelope-constants dictionary | EVICTED by consumer audit **[NEW — VC7]**: CL-15's own text names its consumers as X.2's constants + the depth ⟹ disc envelope — the R-QNT family (items 2/3/4); distinct from item 4's proof duty but same disposition; the count-native discriminant bound is Step 13's Lemma C |
| 13d. CL-16 M4b height-translation constancy + M4b-T equivariance (π_v) | count-closure at fixed template | → Step 2 (T-5's interface at its VERIFIED-DUAL perimeter); the beyond-perimeter audit face = M4b-T-AUD, one of item 14's six fences (off-path) |
| 14. the six H.6 fences (XHD-d-EX(∂), M4b-T-AUD, U-R-COUNT, XHD-u-X, H-LIST-GEN, E-POS) | the H-DOMAINS perimeter's open kernels | → Step 18's K3-a fence display + §3.1 (H6) item (vii): NO fence is consumed as an open general lemma; the consumed residue is exactly in-fence CONFORMANCE ((H6)(vii)) with the displayed escalation clause; roster/owners at tree DELTA 4 §D4.2 — the fences stay off the mandatory path |
| 15–21. event blockers / unrun censuses (STATE-probe-2; X-n3 + STALL-probe; ESCAPE-probe (a)/(b); TRACK-probe; RESUM-n3 + (ORC-1); SIB deg-≥5, H-n3 re-seal, HMC-probe, n = 5 VALUE, REL-n4; gate SQUEEZE-n3) | the plan-§7 empirical roster | NOT mathematical kernels — tree §0's standing rule (numerics are FLAGS, never substitutes) means Step 19 consumes NO census; their successors are the §5 matrix + the per-leaf countermodel-first falsifiers **[disposition NEW at this table — VC7]**. Executed members: RESUM-n3 65/65 (item 19); STATE-probe-2, H-n3 151/151, V-n3 178/178 (items 15/20, §5's sealed T-layer gates); X-n3 layers ran (item 16 — output: the recorded X.1b-reading refutation, R-QNT content); N6-regp/ext2000 = the escape/margin gates (item 17's modern face, 25/25 + 303/303); N1/N2 = the squeeze/density ties to n ≤ 5 (item 21's class, executed more broadly than SQUEEZE-n3's spec). Never-run members (TRACK-probe, STALL-probe, HMC-probe, REL-n4, SIB deg-≥5) gate only R-QNT/fence content — off-path; root criterion 3 (§6: the §5 matrix re-runs green at acceptance) is the standing empirical bar. **Scope of this ruling, sharpened [REVISION 7, finding VC7-5]:** the disposition is a CONSUMPTION ruling ONLY — none of items 15–21 is a (ROOT-C) hypothesis — and NOT a retirement of any acceptance gate: the §5 rerun acceptance-gate obligation (root criterion 3) stands in full |

**Table B — the Lean-bridge kernel rows (census §2.3 / `BridgeKernels.lean`),
cross-walked.** These are the frozen ledger's Lean materialization ("the BK rows
below are the ledger CONTENT" — census §2.2); every row lands on a home above.

| Lean row | §U.SQ.6 face | ROOT home | tree-verified status (census §2.3) |
|---|---|---|---|
| `BridgePre.hdet` | (REG-p)/(r1)'s symbolic determinant face (SQ.4's own hypothesis schema, not a numbered item) | Steps 4/5 (+ Step 2's D1.5(f) (REG-p)-conditional T-8 re-scope) | ratified premise; `RegPin.detHyp` derives it from any (REG-p) prime |
| `BridgePre.hStateNe` | per-block roster nonemptiness (§S-RESUM vocabulary) | §3.3 O-14c row — consumed by NO §2 step; IB-B2 derivation attempt standing | ratified premise (drops if the derivation closes) |
| Ctor.`transfer` | item 10's TREE-N stability face | Step 6 — D-14's per-site noncircular form IS the repaired mathematics; the refuted form is the BOX-level reading O-1thr's requantification removed | REFUTED at the canonical instance as typed (M03 Eisenstein family) — fence event 25, Lean-side per-site guard re-type owed (§3.3 parked-engineering class) |
| BK.`slice_bound` | item 8 (CL-7) | EVICTED (O-6, R-QNT) | carried, permanently fenced |
| BK.`series_tie` | the [3t] solve-side seam | Step 18 (O-11 SERIES-TIE — the same statement; proved by the M04 walk re-priced at D-9) | OPEN-MATH REDUCED (M04) — the Lean row awaits the O-11 chain's formalization; the MATH home is Step 18 |
| BK.`env_tendsto` | item 5 (CL-4) | Step 13 | M05 PARTIAL-VERIFIED (semantic core proved, every prime incl. wild); residual = the classifier tie = (AGR), §3.2 |
| BK.`vp_sound` | item 10's VP-SOUND | Step 12 + Step 6 (D-15's ns-free verdict classes) | REFUTED AS BUILT (M06: finite-level ns-verdict without the ns-free clause) — the refutation is exactly why D-15/T-6 and the §2 standing preconditions are ns-free-scoped; Lean repair = fence event 24's NsFree clause |
| BK.`sibjc` | item 10's (SIB)/(JC-multi) | Step 15 + (H7) (the adaptedness presentation is precisely what the Lean type omits) | PLAUSIBLY FALSE AS TYPED (M07: CellData lacks an adaptedness law) — fence event 27; the countermodel targets the UNGUARDED type, not O-10's adapted-cell theorem |
| BK.`cl6` | item 7 (CTS-M(iv)-POLY) | Step 14 (M08/O-9 census) + §3.1 (GR-B)/(FRESH)/(ADM) | n = 2 POSITIVE (compiled witness); general n OPEN-MATH = exactly the census layer's displayed conditionality |
| BK.`cl11_ksub` | item 11 | PROVED at all n (M09, Lean-core) | discharged |
| BK.`cl17` | item 12's production burden | PROVED at every chain (`w17ii_pin`, Lean-core) | discharged |
| BK.`cl19_rep` | item 13's (JC-INV) | REFUTED as stated; O-14b (§3.3, parked); mandatory path = per-box uniformity (Step 6) | refuted (M11); queue item 22's carrier re-type parked (math-first directive) |
| seam.`count_tie` | CL-10's TREE-N/[3t] count row | Step 6 (D-15's CYL law — the ns-free-scoped form) + Step 2 (T-3/T-6 mass laws) + Step 17 (M1) | REFUTED AS TYPED (M02: bridgeTree lacks the ns-free clause; exact-square ×p family) — fence event 24; post-repair REDUCES to the CYL law = D-15's displayed content |
| `cl13_wf` | item 13a's well-foundedness face | Step 3 | DISCHARGED (`menuWFT_holds`) |
| (`bridge_slice_finite`) | former slice_finite row | Step 3's finiteness at the instance | PROVED 2026-07-30 (left the pack; Lean-core probe theorems) |

**Verdict of the executed audit.** Every §U.SQ.6 item and every Lean-bridge kernel
row lands on a §2 step at its declared statement, a recorded eviction/annex
disposition, or a §3.1 row already carried by (ROOT-C) — NO item names mathematics
outside the walk. [The concluding claim "no new hypothesis surfaced" is CORRECTED
at REVISION 11, r12 — finding VC11-3: it was true of the REVISION-6 execution but
is STALE since REVISION 10, r11 — item 12's shallow-cut content is now priced as
**(H8) = (K-CUT)** and item 6(iii)–(ix)+7's M3 route carries **(H9) = (NSF)**;
both are §3.1 rows carried by (ROOT-C), so the landing invariant above still
holds, but the table DID surface hypothesis-grade content once re-adjudicated at
the r11 scope.] The compiled refutations in the
row set (M02/M03/M06/M07/M11) all target UNSCOPED Lean typings whose mathematical
repairs are the ROOT's own displayed scopings (ns-free preconditions, the per-site
quantifier form, adapted cells, per-box lift uniformity) — none touches a consumed
statement. Three dispositions were NEW at this table (items 4/13c by consumer audit;
items 15–21's roster disposition) and were VC7 charge items — **VC7 CONFIRMED them**
(items 4/13c at finding 6: only the evicted R-QNT family consumes X.1a/X.1b/CL-15,
with Step 13 supplying independent count-native discriminant control; items 15–21
at finding 5, as a consumption ruling — wording sharpened per advisory VC7-5, and
the 6(iii)–(ix)+7 residue list corrected per advisory VC7-3); everything else
restates a recorded adjudication (DELTA-1 §§D1.4/D1.5, DELTA-2 §D2.3, DELTA-3
§D3.1, D-9, TREE-GAPS G-4/G-5) with its pointer displayed.

---

## 4. LITERATURE ROSTER (the goal's conditionality class)

Every external citation the assembled proof consumes, with its pin status. Discipline:
tree §4 rule 5 (verbatim published-text transcription + faithfulness entry + guardian
audit + sign-off before any axiom-grade use); the LITUNIT
(`lean/notes/openmath/LITUNIT_2026-08-01.md`) resolved every GD23 flag MATCH with
verbatim quotes at named extraction lines.

1. **[GMN] Guàrdia–Montes–Nart**, *Newton polygons of higher order in algebraic number
   theory*, Trans. AMS 364 (2012) 361–416 (= arXiv:0807.2620v2). THE machinery import.
   Pinned loci (via `docs/GMN_citations.md` + LITUNIT): Lemma 2.2(2); Prop 2.7(1)(3)(4);
   Lemma 2.17(2)(3); Def 1.8; Thm 1.15/1.19/Cor 1.20; Thm 2.11 + Defs 2.19–2.21;
   Thm 3.1/3.7, Cor 3.3/3.8, Defs 3.9/3.10, eq. (37); Lemma 3.11(3); Thm 4.18 (theorem
   of the index — declared, guardian-audited Lean axiom, read-set-restricted) +
   Cor 4.19 (termination); Defs 4.11/4.12/4.15. **LIT-4/(OM-SAT) rides this entry by
   id [added at unit TREE-GAPS, audit A-2]:** a realized OM branch with residual
   multiplicity 1 at its last read certifies an irreducible factor with e = e₁⋯e_r,
   f = f₀⋯f_r (Cor 3.8 + Def 3.9; order-1 instance Cor 1.20 — the pins Steps 11/12
   already consume); audited scope **"K = ℚ_p exactly"**, consumed only through the
   dictionary node O-2; its K_δ (δ > 1) re-scope duty is exactly (UB-X)(a) content
   (the per-leaf O_δ re-base audit, §3.1) — named here rather than ridden
   generically. Status: LITERATURE-CONDITIONAL
   (printed-TAMS pagination CONFIRMED for the (OM-SAT) slice [REVISION 14, r15]:
   Cor 1.20 / Cor 3.8 / Def 3.9 / Def 3.10 + eq. (37) / Lemma 3.11 / the base-field
   convention, verbatim at printed pp. 374/398–399/399/399/400/366 — unit D12R-U3,
   `lean/notes/openmath/H3_omsat_brief.md` §1, no arXiv-v2 numbering drift; the
   REMAINING pinned loci still owe the LIT-1/GD-7 printed-source session — §3.3,
   which now holds the brief's located-page map as input). Consumed by Steps 7, 8, 11
   (CU-2t consumes Cor 1.20/3.8 directly — added at REVISION 3, finding A6(i)), 12,
   13, 14, and 15 (K-FACT = Thms 1.15/1.19 + 3.1/3.7 — Step 15's pinned perimeter,
   all four loci above) [Step 15 added at REVISION 2, finding V2-7].
2. **[ML] MacLane**, *A construction for absolute values in polynomial rings*, Trans.
   AMS 40 (1936) 363–395. Pinned (LITUNIT, re-typeset caveat displayed): Thm 4.2 (the
   (V1) multiplicativity), Def 4.1, Thm 10.2, Lemma 10.1, Lemmas 11.1–11.2, Thm 12.1,
   Cor 12.2 (commensurable hypothesis automatic here). Consumed by Steps 6, 8, and 9
   (Step 9's loci: Def 4.1 + Thm 4.2 — the (V1) frame-semantics pin, LITUNIT §4
   verdict MATCH; consumed as the intended semantics of T-1/T-2's frame vocabulary,
   no further MacLane theorem — Step 9 added at REVISION 2, finding V2-7) (and
   (GR-B)'s pinned targets).
3. **[FGMN] Fernández–Guàrdia–Montes–Nart**, *Residual ideals of MacLane valuations*,
   J. Algebra 427 (2015) 30–75 (= arXiv:1305.0775). Pinned: Thm 4.2 (BOTH printed
   pairings; exact match verified clause-for-clause at GD23 r3), Cor 4.4, Thm 4.8,
   Cor 4.9, Cor 5.6, Thm 5.8, Cor 5.12, Prop 5.14/Cor 5.15, Lemma 5.3 (properness).
   Consumed by Step 8 and (GR-B)'s target statements.
4. **[Serre LF] Serre**, *Corps Locaux / Local Fields* (GTM 67). Ch. II §2 Prop 3
   (integral closure is a DVR — the declared axiom `AX_integralClosure_dvr`, guardian
   audit PASSED, V3: FAITHFUL/EXACT); Ch. II §2–§3 (unique valuation extension;
   Σe_if_i = [L:K] defectlessness); Ch. III §6 Prop 13 (wild different bound, June
   L5fix); **Ch. I §6 Prop 15, printed pp. 17–18 (B_f = A[X]/(f) a DVR with maximal
   ideal m·B_f and residue field k[X]/(f̄) when f̄ is irreducible — the (HL-c)
   read-off of the D-12r pin, CONFIRMED at unit D12R-U1,
   `lean/notes/openmath/H3_henlift_brief.md` §3; added to this consumed-loci
   enumeration at REVISION 14, r15, per VC16 finding 2)**. Consumed by Steps 1, 12,
   13 (Facts EXT/EF), B-6, and the item-10 D-12r pin ((HL-c)).
5. **[Neukirch] Neukirch**, *Algebraic Number Theory* (Grundlehren 322). Thm II.4.8
   (valuation extension; the M16-precedent high-confidence pin); II §4 (4.6)
   "Hensel's Lemma" = LIT-3 coprime-Hensel — **numbers CONFIRMED at printed p. 129
   [REVISION 14, r15 — the honest state per VC16 finding 2]:** unit D12R-U1
   (`lean/notes/openmath/H3_henlift_brief.md`) confirmed the statement + page (index
   cross-check "Hensel's lemma 129") and RAN the Mathlib probe — answer **ABSENT**
   (`HenselianLocalRing` is root-lifting only; no coprime-factor lifting), so the pin
   STANDS AS A PAPER CITE; the printed (4.6) states EXISTENCE only — the uniqueness
   clause is ABSENT and is supplied by the brief's §2 displayed companion lemma (the
   (HL-b) disposition, never a silent strengthening); the optional Lean
   materialization (D12R-U2a/b/c) owes its own axiom-policy loop (citation +
   faithfulness entry + guardian audit) when it lands; II §6 region (fundamental
   identity). Consumed by Steps 12, 13, and the item-10 D-12r pin ((HL-a)/(HL-b)).
6. **[Lang] Lang**, *Algebra* (rev. 3rd ed., GTM 211), Ch. IV §8
   (resultant/discriminant; number flagged for page verification) — O4T Fact D.
   **[Cohen] Cohen**, *A Course in Computational Algebraic Number Theory* (GTM 138)
   §3.3 (coefficient-side resultant — also what the harnesses compute). Step 13.
   **O4T import inventory — COMPLETE at rev 3 [REVISION 1, finding 8]:** the eight
   named Facts beyond NP/SF/HEN are EXT, EF, D, GRD, UCT, HRG (rev 2, sources as
   displayed in the O4T record) **+ SEP (char-0 separability of irreducibles) + DES
   (Galois descent/fixed field)** — the pass-2 "Galois descent + one more" pair, both
   added at rev 3 (commit bb4326c) as IN-LINE-PROVED imports with consumption-site
   cite tags (standard field theory; no new external citation is consumed — the
   proofs are in the leaf). Six-vs-eight reconciled: EF and GRD each bundle two of
   V30's items.
7. **[LIT-8 split, per DELTA-1 §D1.5(d)]** LIT-8a Krasner + the 2v+1 polish constant
   (inside VERIFIED M05 Lemma D); LIT-8b Newton–Hensel; LIT-8c Vandermonde/resultant
   identities (Bourbaki *Algèbre* IV §6 / Lang); LIT-8d Igusa §7.4 change of variables;
   LIT-8e Gauss/Möbius counts. Each LITERATURE-CANDIDATE, transcribed at its use site.
   Consumed by Steps 1, 13, 15 and the B-layer.
8. **Standard graduate measure theory with displayed statements** (Dfloor's audit:
   Folland *Real Analysis* Thm 1.14 (premeasure extension), Thm 2.37 (product/Tonelli),
   Tychonoff/Carathéodory/π–λ). Step 1. **[Serre CA] Serre**, *A Course in Arithmetic*
   (the n = 2 split criterion used in Dfloor's unconditional instance).
9. **In-project Lean-audited axiom set** (not literature but axiom-grade trust,
   `docs/AXIOM_FAITHFULNESS.md`): the GMN index theorem (item 1's Thm 4.18 declaration)
   and — measure route ONLY, NOT consumed by this count-native assembly — the
   Denef–Igusa cell recursion (`PadicMeasure.AX_cellRecursion`).
10. **[D-12r] — TB-CAP's (τ-irr) completeness perimeter: the D4R.0-K(c)
    verdict-pin residual** [MOVED HERE from §3.1 [M] at REVISION 14, r15,
    executing VC16 finding 2 — the reclassification adjudicated WARRANTED: no
    D-12r-specific open mathematics remains; the residue is exactly the two
    literature pins below. A [T]/LITERATURE-class entry, not open mathematics].
    THE REDUCTION, dual-accepted and displayed (MOVES §T.5(2), T-ASSEMBLY rev 5,
    passes 5+6 clean + fresh-Fable pass — quoted verbatim at HDISCHARGE_H3 §2.1):
    D4R.0-K parts (a)/(b) DISCHARGED (D4R0K_DERIVATION rev 3, dual-verified);
    part (c) is owned by [3t] VP with TB-CAP per clause ((hen) proved; (irr)
    proved from the accepted corpus — see the TB-CAP(irr) sentence below; (ns)
    refuted-as-worded, replaced by the proved (ns-lump)+(ns-null) pair), and
    "**D4R.0-K(c) is therefore NOT completely discharged: what remains is exactly
    {VP-SOUND's cites}**" — the §T.2 status block names the set's TWO members:
    **(HEN-LIFT)** (coprime-factor Hensel lifting package, three-part) and
    **(OM-SAT)** (realized-branch irreducibility read-off). THE PINS, confirmed
    on file: (HL-a) existence = **[Neukirch]** II §4 (4.6), printed p. 129,
    CONFIRMED (item 5 above); (HL-b) uniqueness = printed clause ABSENT in (4.6)
    — DISPOSED via the displayed companion lemma carried by the brief
    (`lean/notes/openmath/H3_henlift_brief.md` §2, never a silent strengthening);
    (HL-c) read-off = **[Serre LF]** I §6 Prop 15, printed pp. 17–18, CONFIRMED
    (item 4 above; Neukirch frame pinned as alternate); (OM-SAT) = GMN Cor 3.8 +
    Def 3.9 — **rides the §4 GMN entry (item 1) by id**, TAMS pagination PASSED
    at printed pp. 374/398–400 (`lean/notes/openmath/H3_omsat_brief.md` §1;
    consumed only through the dictionary node O-2, whose conditionality it
    inherits — the item-1 rider). Mathlib probe RAN, answer ABSENT (root-lifting
    only; no coprime-factor lifting) — the optional Lean upgrade D12R-U2a/b/c is
    a genuine unit, NOT load-bearing for the door, owing its own axiom-policy
    loop when it lands. **TB-CAP(irr), the precise sentence [Codex pass-1
    finding 3]:** TB-CAP(irr) is PROVED at [3t] from the accepted §C rev-14
    Theorem C — a citation into the dual-accepted corpus, not an open
    hypothesis; this entry never carried §C's perimeter as its content.
    CONSUMED (perimeter-conditional wherever TB-CAP is consumed, the language
    now "perimeter-conditional on the §4 D-12r pin"): Steps 2 (T-6's declared
    TB-CAP conditionality), 3, 11, 12, and Step 19 — where T-6's leg serves the
    classifier-side counting tie inside the evaluation squeeze, at clause (R)'s
    conditionality — AND Step 16/clause (UB) through the (UB-X)(a)-transported
    dictionary/CU/drainage layers, the §1 B1 attribution's displayed entry
    [REVISION 9, VC10 advisory 2]; the REVISION-3 A6(ii) phrasing "the (SQ)
    bracket consumes T-6" is SUPERSEDED at REVISION 4, finding VC4-4.
    DEPENDENCY DECOMPOSITION, preserved verbatim (the H3 lead's routing table,
    HDISCHARGE_H3 §2.1 — why the OTHER conditions riding the [3t] acceptance
    record are NOT this entry's residual, each at its own priced home:
    (SIB)/(SIB-STEP)/(JC-multi) → Step 15 + (H7); X.3/CL-4 → the drainage chain
    (Steps 13/19, TREE-EXP/TREE-N X.3-free per MOVES §T.0); [2r] REL.2 →
    (H4b)/(UB-X)(a); the (τ-ns) (z-3) identification → D4R.0-K(b), discharged,
    with the residual application inside the (OM-SAT) cite; the O-2 dictionary
    identification → the §3.1 (H1) rows + the item-1 rider (priced there, never
    re-priced here); P(O5t r3) (Step 12's owed pass, which consumes the
    read-offs) → the §3.2 row. This entry's move emptied the former §3.1 row
    without absorbing any sibling row's debt. Status: LITERATURE-CONDITIONAL at
    its two pins (both pinned-confirmed); fully retires when the pins clear the
    standing §4/§3.3 literature discipline (LIT-1's remaining session does NOT
    gate this entry — its (OM-SAT) slice is already verbatim-confirmed).

**Explicitly NOT consumed:** [BNS] Bauch–Nart–Stainsby LMS JCM 16 (2013) — route
R-QNT only (DELTA-3: zero root-side touch-points); [DCD] Del Corso–Dvornicich — annex
A-2 only (the 2026-07-21 retirement stands); the companion 2212.00294 (excluded by
standing goal directive).

---

## 5. NUMERICS CORROBORATION INDEX (flags, never substitutes — tree §0)

Standing matrix (tree §5) + the Phase-B falsifiers, keyed to §2's steps. Every leaf
with a runnable falsifier RAN it before acceptance (countermodel-first discipline).

| suite / falsifier | what it corroborates (step) | result |
|---|---|---|
| n = 2 Lean capstone + `verification/census_n2_uniform.py` | THE ROOT at n = 2, machine-checked + external census (anchor) | exact at p = 2, 3 |
| RESUM-n3 | n = 3 densities at the gate standard (Steps 2, 18) — G-3 caveat [REVISION 13, VC15 advisory 3]: the "what it corroborates" reading is scoped by the §3.2 O-11 row's recorded finding G-3 (the "65/65 END-TO-END rederivation of the assembled pipeline" claim is unsupported beyond the FIVE displayed identities); the Step-18 corroboration is at those identities, not a demonstrated end-to-end walk | 65/65 exact, PARI 0 mismatches (flag; G-3-scoped) |
| N1 exhaust / N2 density-tie | bracket + drainage; the root numerically to n ≤ 5 incl. (5,5) (Steps 13, 19) | CONFIRMED (max ratio 0.858 < 1; sandwich ~1e-5) |
| `dfloor_checks.py` | Step 1 (incl. the T-F n = 3 interpolation certificate) | ALL PASS |
| O-13 enumerator reproduction | Step 3 (verifier re-implemented from text alone) | \|S_n\| = 1, 15, 53, 197, 509, 1460 exact |
| N6-regp + ext2000 | Steps 4, 5, 17 ((REG-p) emptiness, margins) | 25/25 gates; 303/303 primes < 2000, margin sharp 6/7 |
| O-1thr depth-2 falsifier (T-E/T-F harnesses) | Step 6's sheared convention | 41,720 instances 0 violations (7,472 violations of the UNsheared reading — the pin is load-bearing) |
| GD23 (L3+) harness | Step 8's order-1 exact-key seam | 871 forced samples, 0 findings |
| U6 / IFK-U6-FIRST + the depth-3 falsifier (CU-1's gate) | Steps 10–11 (no-phantom, counting tie, equal fibers) | 174/174 length-2 strata T = T̂, 369 chains, 0 phantoms, PARI 1600/1600; depth-3 gate SURVIVED pre-rev-1 |
| `O4T_transfer_check.py` | Step 13 (n ≤ 3 ceiling sharpness, envelope) | 2,534,851 exhaustive classes, 5 configs incl. wild, ALL PASS, max ceiling slack 0 |
| `o9_order2_census_check.py` (M08-T6) | Step 14 (order-2 census polynomiality) | 8,220,994 boxes, 1,128,288 order-2 instances, K1–K4+T2 0 violations, 18 cross-p cells, ε = 0 |
| `o10_kcount_harness.py` | Step 15 (K-COUNT fiber law; sealed predictions) | 68 PASS / 0 FAIL |
| N8 eq2 | Step 16 (the alphabet law) | 199,723 alphabets / 17 fields, 0 violations |
| `o11_seriestie_check.py` | Step 18 (incl. Finding F1's test at the assembled-pack proxy) | 38/38 billed; the O-11 pass-1 record corrects the live count to 37/37 (one test = discriminant proxy) — flag stands either way; n = 2 true triple identical in ℚ(q) |
| N3/N4/N5/N7 + the 22 sealed T-layer gates (D4-n3 404M boxes, SIB 32/32, T-n3, M-n3-V2, H-n3 151/151, V-n3 178/178, STATE-probe-2, …) | the §2.2 layer (Step 2) | ALL PASSING |

Matrix synthesis (adjudications ledger): no Fable-vs-Codex-vs-numerics conflict
anywhere; every refutation triangulated; every surviving conjecture numerically
supported. Root criterion 3 (tree §4): this matrix re-runs green at acceptance time.

---

## 6. Change discipline and next actions

This document is Phase C's spine. Update rules: it re-states the tree + DELTAs and the
wave-10 ledger; where they move (DELTA 4 at the next fold), a dated appendix here
records the delta — the body above stays byte-stable per the tree's own discipline.
Acceptance path (tree §4 Root): (1) empty §3.1 and §3.2 into VERIFIED (the [M] rows
via the GD campaign's order-≥ 2 arc; the [V] rows via the named passes); (2) ≥ 2
consecutive clean END-TO-END passes on this document by fresh verifiers, ≥ 2 models;
(3) the §5 matrix re-runs green; (4) the completeness audit: every tree node id appears
here or in the recorded eviction list (§3.3's final paragraph + the tree's DELTAs).

Immediate dispatch order (updated at REVISION 1; the O-9 E′ and O4T N3-BRACKET
repairs have LANDED): the D-11 and O-11 r2s (+ their re-passes); CU-1 r5 (in flight);
the O4T full clean pass; the CUC/CU-2t pass; the three registry passes
P(O-2a r5)/P(O-1thr r4)/P(O5t r3); O-10's pass 2; the (UB-X)/(R1)–(R4) write-up; then
the GD order-≥ 2 campaign for (GR-B)/(FRESH) — the last genuine mathematics between
this assembly and the theorem. [Superseded event: the pass-2 slot RAN as VC2 and
returned COMPOSITION-GAPS — dispositioned at §8 (REVISION 2); the next end-to-end
pass (VC3, fresh verifier) runs on THIS twice-revised document, and the ≥ 2
consecutive-clean counter is still at zero.] [Updated at REVISION 3: VC3 RAN —
COMPOSITION-GAPS, all attribution/ledger integrity, the mathematics CONFIRMED —
dispositioned at §9. Dispatch now: **VC4 (Codex) on this thrice-revised document**
toward the two-consecutive-clean bar; the wave-14 repairs LANDED at revision close
(460ea59/567814d/3956500: O-9 r3, CU-2t R2, O4T r4 + O-10 r2) — their confirming
passes (O-9 pass 3, the CU-2t R2 re-pass, O4T's acceptance pass) are the in-flight
debts, with the D-11/O-11 pass 3s; owed: P(O-2a r5)/P(O-1thr r4)/P(O5t r3),
P(O-8b/M14) (§3.2, new), the (UB-X)/(R1)–(R4) write-up; then the GD order-≥ 2
campaign for (GR-B)/(FRESH).] [Updated at REVISION 4: VC4 RAN — COMPOSITION-GAPS,
all attribution/ledger integrity again — dispositioned at §10; the confirming-pass
round resolved O4T → VERIFIED (leaf #7) but sent O-9 and CU-2t one more rung
(pass 3 UNSOUND → r4 FULL-attainment; R2 pass 0-critical → R3 three-row pricing);
the REG leg ran ZERO-REGRESSIONS (044a6d9, all 18 suites — root criterion 3 evidence
at HEAD, accepted) and tree DELTA 4 landed (unit TREE-GAPS: audit gaps G-1..G-5
homed/adjudicated, its ROOT touches executed in this document concurrently with this
revision). Dispatch now: **VC5 on this four-times-revised document**; in flight:
O-9 pass 4, the CU-2t R3 pass, the D-11/O-11 pass 3s; owed:
P(O-2a r5) (now carrying the CU-2t W3/2(D) delivery)/P(O-1thr r4)/P(O5t r3)/
P(O-8b/M14), the (UB-X)/(R1)–(R4) write-up + the (K3-δ) match display, the (PACK)
data-pass execution (the new (H5)); then the GD order-≥ 2 campaign for
(GR-B)/(FRESH).] [Updated at REVISION 5: VC5 RAN — COMPOSITION-GAPS, two blockers of
ONE genre (consumed [T] rows are open propositions) + the standing T-U advisory —
dispositioned at §11; the harvest resolved O-9 → VERIFIED (leaf #8) and CU-2t →
VERIFIED at scope (leaf #9): **VERIFIED = 9**, and the promoted content = the NEW
§3.1 (H6) group. Dispatch now: **VC6 on this five-times-revised document** (fresh
verifier; model diversity favors Fable for the eventual two-model clean bar); in
flight: the D-11/O-11 pass 3s; owed: P(O-2a r5)/P(O-1thr r4)/P(O5t r3)/P(O-8b/M14),
the (UB-X)/(R1)–(R4) write-up + the (K3-δ) match display, the (PACK) data pass
((H5)), the (H6) conformance-unit write-ups (the O-3 conformance routes for
(T-DEC)/(T-READ)/(T-VERD); the D-11 owner's item-(vii) general-n display); then the
GD order-≥ 2 campaign for (GR-B)/(FRESH) — the heavy remainder.] [Updated at
REVISION 6: VC6 RAN — COMPOSITION-GAPS, 3 blockers + 6 advisories, the blockers the
VC5 genre WALKED TO COMPLETION (the last consumed-open-in-[T] rows) — dispositioned
at §12; NO status syncs (a composition-only pass; VERIFIED = 9 stands). Dispatch
now: **VC7 on this six-times-revised document** (fresh verifier; the §12 charge
includes confirming the NEW §3.4 table); in flight: the D-11/O-11 pass 3s; owed:
P(O-2a r5)/P(O-1thr r4)/P(O5t r3)/P(O-8b/M14), the (UB-X)/(R1)–(R4) write-up + the
(K3-δ) match display, the (PACK) data pass ((H5)), the (H6) conformance-unit
write-ups, the NEW (H7) discharge unit (the K-LOC = M07 OL-3 lemma + the (I-τ)
presentation write-up), the GD-7 flag reading session (now a named acceptance
precondition — A-7); then the GD order-≥ 2 campaign for the ENLARGED (H1) group
((GR-B) + GD-1-beyond-1 + the GD-6 instances) and (FRESH) — the heavy remainder.]
[Updated at REVISION 7: VC7 RAN — COMPOSITION-GAPS, 2 blockers + 3 advisories,
the blockers BOTH attribution/warrant findings (Step 6's all-order KEY1
consumption without a displayed [M] member → the NEW (H1)(d); the
(PACK)-correspondence law-transfer inference → the (H5) PACK-i/ii/iii
enlargement), and findings 6–11 CONFIRMING the composition incl. the §3.4 tables
— dispositioned at §13; NO status syncs (composition-only; VERIFIED = 9 stands);
the §3.3 T-U audit row RETIRED on VC7's table confirmation. Dispatch now: **VC8
on this seven-times-revised document** (fresh verifier; charge at §13: verify the
two enlargements + the three advisory fixes — VC7's confirmations need no
re-litigation but must not regress); in flight: the D-11/O-11 pass 3s; owed:
P(O-2a r5)/P(O-1thr r4)/P(O5t r3)/P(O-8b/M14), the (UB-X)/(R1)–(R4) write-up +
the (K3-δ) match display, the (PACK) data pass + the three row-law displays at
𝔅_n ((H5), now incl. PACK-i/ii/iii), the (H6) conformance-unit write-ups, the
(H7) discharge unit, the GD-7 flag reading session; then the GD order-≥ 2
campaign for the ENLARGED (H1) group (now also (d), the degree-g > 1 KEY1
generalization) and (FRESH) — the heavy remainder.] [Updated at REVISION 8: VC8
RAN — COMPOSITION-GAPS, 1 blocker + 1 advisory, the narrowest residue of the
eight passes, both at the REVISION-7 repairs themselves ((H1)(d)'s key-degree
quantifier → REQUANTIFIED to read order, finding VC8-1; the PACK K2/K3
sub-attribution → synced, finding VC8-2), with the three r8 advisory repairs,
the TEN-row count, the walk, and acyclicity all CONFIRMED — dispositioned at
§14; NO status syncs (composition-only; VERIFIED = 9 stands). Dispatch now:
**VC9 on this eight-times-revised document** (fresh verifier; charge at §14:
the (H1)(d) requantification + the PACK sub-attribution sync + no regression);
in flight: the D-11/O-11 pass 3s; owed: P(O-2a r5)/P(O-1thr r4)/P(O5t r3)/
P(O-8b/M14), the (UB-X)/(R1)–(R4) write-up + the (K3-δ) match display, the
(PACK) data pass + the three row-law displays at 𝔅_n, the (H6) conformance-unit
write-ups, the (H7) discharge unit, the GD-7 flag reading session; then the GD
order-≥ 2 campaign for the ENLARGED (H1) group (member (d) now the order-≥ 2
KEY1 block — read-order membership) and (FRESH) — the heavy remainder.]
[Updated at REVISION 9 — ACCEPTANCE: VC9 (Codex) and VC10 (Fable, the
model-diverse pass) RAN on the identical REVISION-8 text and BOTH returned
COMPOSITION-SOUND with ZERO blockers — acceptance-path criterion (2) above is
MET (two consecutive clean end-to-end passes, two models, fresh verifiers;
counter = TWO) and the composition is ACCEPTED at (ROOT-C)'s displayed
conditional scope. VC10's three advisories (display hygiene only) are
dispositioned at §15 and repaired in place — post-acceptance hygiene: no [M]
member, step content, or clause attribution changed; the counter STAYS TWO.
The remaining acceptance-path criteria toward THE COMPLETE PROOF are (1), (3),
(4): (1) empty §3.1/§3.2 into VERIFIED — owed: the D-11/O-11 pass 3s (in
flight), P(O-2a r5)/P(O-1thr r4)/P(O5t r3)/P(O-8b/M14) + the NEW P(O-14a′ r3)
(the REVISION-9 grade-display row), the (UB-X)/(R1)–(R4) write-up + the (K3-δ)
match display, the (PACK) data pass + the three row-law displays at 𝔅_n, the
(H6) conformance-unit write-ups, the (H7) discharge unit, the GD-7 flag
reading session; then the GD order-≥ 2 campaign for the ENLARGED (H1) group
and (FRESH) — the heavy remainder; (3) the §5 matrix re-runs green at final
acceptance; (4) the completeness audit.] [Updated at REVISION 10 — SUSPENSION
CURED, ACCEPTANCE NOT RE-EARNED: the D-11 pass 3 ran CRITICAL (the stop-the-line,
ledger 44ce0a7 — acceptance SUSPENDED per `docs/PROJECT_STATE.md`); the five-round
repair design (`R11_REPAIR_DESIGN_V5_2026-08-04.md`) executed as Unit A (the D-11
r4 leaf, twice hostile-passed: pass 1 CRITICAL folded, pass 2 GAPS-ONLY with all
design-§7 obligations VALID as proofs) and Unit B (THIS document's r11 = §16:
Steps 17/18b re-pointed to the repaired true-scope leaf; (PACK-iv) into (H5);
NEW rows (H8) = (K-CUT) and (H9) = (NSF); E-M3 at Step 12; TWELVE rows ↔
(H1)–(H9)). Dispatch now: **VC-next — the fresh end-to-end bar on this
REVISION-10 text, counter ZERO of 2** (charge at §16: verify the r11 edits, the
twelve-row invariant, no regression); in flight/owed BEYOND the bar: the D-11
confirming clean pass on the leaf's REVISION-3 text + the O-11 pass 3, Unit C =
the sign-off-gated (NSF-KEY) execution ((NSF-KEY-RET) R-0..R-5), the (K1-CANON)
discharge (M04-side pin display or interface lemma), the (H8) retirement
adjudication (O-11 §8 charge (vi)), the (PACK) data pass now INCLUDING
(route-a)'s booking-profile duty, and the standing queue above ((H6)/(H7)
units, registry passes, (UB-X)/(R1)–(R4), the GD order-≥ 2 campaign).]
[Updated at REVISION 11 (r12): VC11 RAN on the REVISION-10 text —
COMPOSITION-GAPS, 4 blockers + 1 advisory, ALL r11 RIPPLE-SYNC defects (the
targeted r11 repairs themselves CONFIRMED) — dispositioned at §17 and repaired
in place as pure display syncs: Steps 16/19's evaluation/(UB) scope summaries +
the final ∎ run to the twelve-row (H1)–(H9) scope with (H5) at PACK-i/ii/iii/iv,
§3.4 item 12 re-adjudicated to (H8) = (K-CUT) with the stale "no new hypothesis"
concluding claim corrected, the 6(iii)–(ix)+7 residue list closed with (H9),
Step 18b's Consumes summary restored to (GR-B)/(FRESH)/(ADM). No [M] member,
step proof content, or leaf pointer changed; NO status syncs (VERIFIED = 9
stands). Dispatch now: **VC12 — the fresh end-to-end bar on this REVISION-11
text, counter ZERO of 2** (charge at §17: verify the five syncs + no regression;
the r11 repairs need not be re-litigated but must not regress); in flight/owed
BEYOND the bar: unchanged from the REVISION-10 list above.]
[Updated at REVISION 12 (r13): VC12 RAN on the REVISION-11 text — COMPOSITION-SOUND,
ZERO findings (counter ONE of 2); VC13 (Fable, the model-diverse pass) RAN on the
same text — COMPOSITION-GAPS, 2 blockers + 3 advisories, ALL the
stale-hypothesis-enumeration genre (the walk, twelve-row bijection, seams,
acyclicity, and (NSF-KEY) discipline CONFIRMED) — dispositioned at §18 and repaired
in place as pure display syncs, PLUS the class kill: the GLOBAL ENUMERATION AUDIT
(§18's audit record — every operative §§0–6 + §3.4 display enumerating hypothesis
groups/members/counts checked against §1's current (ROOT-C) display and synced).
No [M] member, step proof content, or leaf pointer changed; NO status syncs
(VERIFIED = 9 stands); the §3.2 O-11 row now RECORDS the un-banked 2026-08-03
hostile pass (VC13-4). A text change: **the counter RESETS to ZERO of 2**.
Dispatch now: **VC14 — the fresh end-to-end bar on this REVISION-12 text** (charge
at §18: the five VC13 syncs + the audit's claim "every operative
hypothesis-enumeration display matches §1" + no regression); in flight/owed BEYOND
the bar: unchanged from the REVISION-10 list above.]
[Updated at REVISION 13 — RE-ACCEPTANCE + the post-acceptance batch: VC14 (Codex)
and VC15 (Fable, model-diverse) both returned COMPOSITION-SOUND on the identical
REVISION-12 text — the bar is MET and the composition is RE-ACCEPTED at
(ROOT-C)'s twelve-row scope. REVISION 13 (§19) folds VC15's four advisories +
the discharge syncs ((NSF-KEY) RETIRED at the executed Unit C; D-12r rewritten
at its confirmed cites, reclassification VC-gated; (K-CUT) re-scoped to the
landed H8 development, NOT discharged; (E-N)'s mid-campaign rewrite adopted).
Dispatch now: **VC16 — the confirming end-to-end pass on this REVISION-13 text**
(charge at §19: the four advisory repairs + the four row syncs + no regression;
re-anchors the acceptance on the current wording). In flight/owed BEYOND VC16:
the D-11 confirming clean pass + the banked O-11 pass 3; the (K1-CANON)
discharge (after it, (NSF-INT) retires on I-1+I-2+I-3 and the (H9) row closes);
the H8 fan-out's unit-level hostile pass (the (H8) ratification gate); the H4
ROOT-sync briefs' firing conditions (M1 ×2 / M2 ×1 passes, M4 pass-2 record, M5
second clean, the N-K3 run, Asvin sign-off on the Route-B re-scope); the
M17/RegPFinite faithfulness-audit record (§3.2, new); the standing queue (the
(PACK) data pass incl. route-a's booking-profile duty, (H6)/(H7) units, registry
passes, the GD order-≥ 2 campaign).]
[Updated at REVISION 14 (r15): VC16 RAN on the REVISION-13 text —
COMPOSITION-SOUND, zero blockers (advisories only) — the acceptance RE-ANCHORS
on REVISION 13, and finding 2 adjudicates the D-12r reclassification WARRANTED.
REVISION 14 (§20) EXECUTES the move: D-12r [M] → the §4 literature pin (entry
10), §3.1 = ELEVEN rows ↔ (H1)–(H9) with (H3) = {(ADM), (E-N)}; the §4 roster
synced honestly (item 1's (OM-SAT)-slice pagination CONFIRMED, the LIT-1-wide
session still owed; item 5's Neukirch numbers CONFIRMED with the Mathlib probe
ABSENT — paper cite stands; Serre LF I §6 Prop 15 added to item 4). Dispatch
now: **VC17 — the confirming end-to-end pass on this REVISION-14 text** (charge
at §20). In flight/owed BEYOND VC17: unchanged from the REVISION-13 list above,
MINUS nothing (the D-12r move retires no pass debt) — the (K1-CANON) discharge,
the H8 fan-out hostile pass, the H4 firing conditions, the M17/RegPFinite
audit, the standing queue; PLUS the LIT-1 remaining-loci session (input: the
D12R-U3 located-page map).]
[Updated at REVISION 15 (r16): VC17 RAN on the REVISION-14 text —
COMPOSITION-GAPS, 1 blocker + 1 advisory, BOTH display-conformance at the r15
edits (Step 12 not locally re-pointed at the D-12r perimeter; Step 4's grade
outside the §0 vocabulary), with all other charged checks PASSING (the D-12r
move's faithfulness, the ELEVEN-row bijection, the walk, acyclicity, no
regression) — dispositioned at §21 and repaired in place as pure display syncs.
Dispatch now: **VC18 — the confirming end-to-end pass on this REVISION-15 text**
(charge at §21). In flight/owed BEYOND VC18: unchanged from the REVISION-14
list above.]

— ROOT ASSEMBLY complete, 2026-08-02. Composed at HEAD d730669 + the recorded
in-flight events (O4T pass 2, untracked). Every §2 grade traces to a named commit or
pass record; nothing above upgrades any leaf's status. REVISION 1 (§7) applied after
VC1 at the 2026-08-02 harvest state (c43b13b). REVISION 2 (§8) applied after VC2
(bfa462f dispatch record; no status syncs — VC2 was a composition pass on this
document alone). REVISION 3 (§9) applied after VC3 at the 2026-08-02-night harvest
state (7b28562: VERIFIED = 6). REVISION 4 (§10) applied after VC4 at the
confirming-passes/O-9-r4/CU-2t-R3 harvest state (85670f7/35faac7: VERIFIED = 7),
concurrent with tree DELTA 4's ROOT touches (unit TREE-GAPS). REVISION 5 (§11)
applied after VC5 at the O-9/CU-2t-verified harvest state (6a7593b: VERIFIED = 9).
REVISION 6 (§12) applied after VC6 at the same harvest state (no status syncs —
VC6 was a composition-only pass; VERIFIED = 9 unchanged); it also EXECUTES the
standing T-U §U.SQ.6 audit as the new §3.4. REVISION 7 (§13, unit ROOT r8) applied
after VC7 at the same harvest state (composition-only pass; VERIFIED = 9
unchanged); it ENLARGES (H1) (the NEW member (d), the degree-g > 1 KEY1
generalization) and (H5) (the NEW explicit clauses PACK-i/ii/iii), applies the
three VC7 advisories, and retires the §3.3 T-U audit row on VC7's table
confirmation. REVISION 8 (§14, unit ROOT r9) applied after VC8 at the same
harvest state (composition-only pass; VERIFIED = 9 unchanged); it REQUANTIFIES
(H1)(d) to READ ORDER (the order-≥ 2 KEY1 block — membership by OM order ≥ 2
alone, the current key's degree irrelevant) and syncs the PACK-i/ii/iii K2/K3
sub-attribution (PACK-i/ii at Step 18's K2 evaluation glue; PACK-iii at Step
18's K3 evaluation ring-hom, with D-6). ACCEPTANCE (2026-08-03): VC9 (Codex) +
VC10 (Fable) both returned COMPOSITION-SOUND, ZERO blockers, on the identical
REVISION-8 text — the ≥ 2-consecutive-clean, ≥ 2-model bar (tree §4 Root
criterion) is MET and the composition is ACCEPTED at (ROOT-C)'s displayed
conditional scope. REVISION 9 (§15, unit ROOT r10) applied POST-ACCEPTANCE —
display hygiene only: it repairs VC10's three advisories (the O-14a′
§0-vocabulary grade display + the NEW §3.2 row P(O-14a′ r3); the D-12r/(E-N)
rows' Step-16/clause-(UB) transport entries; Table A's 6(iii)–(ix)+7 residue
list run to closure with (H4a)+(K3-δ)), with no [M] member, no step content,
and no clause attribution changed. [The "ACCEPTANCE STANDS ... counter TWO"
close of that revision is SUPERSEDED by the record below.] SUSPENSION
(2026-08-03, ledger 44ce0a7): the owed D-11 pass 3 returned CRITICAL and Steps
17/18b consumed the refuted layers as proved — the Phase-C acceptance
SUSPENDED. REVISION 10 (§16, unit ROOT r11, 2026-08-04) applied: Steps 17/18b
re-pointed to the twice-passed true-scope D-11 r4 leaf (REVISION 3); (PACK-iv)
added as (H5)'s fourth row-law (consumed at Step 18b direct + Step 17's E1
(ROW-Σ) display only — the leaf's closed edge set); NEW [M] rows (H8) = (K-CUT)
and (H9) = (NSF) ((NSF-KEY) KNOWN FALSE AT HEAD, Unit-C/sign-off-gated);
Step 12's delivery rescoped to separable lifts (E-M3); the §3.1 roster is
TWELVE rows ↔ (H1)–(H9). **The suspension's composition-level defect is CURED;
the ACCEPTANCE is NOT re-earned: the consecutive-clean counter is ZERO of 2**,
and the end-to-end bar re-runs on this REVISION-10 text (charge at §16).
REVISION 11 (§17, unit ROOT r12, VC11 ripple-sync) applied after VC11 returned
COMPOSITION-GAPS on the REVISION-10 text (4 blockers + 1 advisory, ALL r11
ripple-sync defects; the targeted r11 repairs CONFIRMED): Steps 16/19's
evaluation/(UB) scope displays and Step 19's final ∎ synced to (H1)–(H9) with
(H5) at PACK-i/ii/iii/iv; §3.4 item 12 re-adjudicated to (H8) = (K-CUT) and the
table's "no new hypothesis surfaced" claim corrected; the 6(iii)–(ix)+7 residue
list closed with (H9); Step 18b's Consumes summary restored to
(GR-B)/(FRESH)/(ADM). Pure display syncs — no [M] member, step proof content, or
leaf pointer changed; NO status syncs (VERIFIED = 9 stands). **The counter STAYS
ZERO of 2**; VC12 re-runs the bar on this REVISION-11 text (charge at §17).
VC12 RAN on the REVISION-11 text — COMPOSITION-SOUND, ZERO findings: the counter
reached ONE of 2. VC13 (Fable, the model-diverse pass) RAN on the same text —
COMPOSITION-GAPS (2 blockers + 3 advisories, all the stale-hypothesis-enumeration
genre; the composition itself CONFIRMED). REVISION 12 (§18, unit ROOT r13) applied:
the five VC13 findings repaired in place as pure display syncs (§1's (H5) display
run to PACK-i/ii/iii/iv; Step 18's "— §1" clause-(R) quote run to the full
(H1)–(H9) set with the five-hypothesis count retired; Step 17's Consumes carrying
(H9)/(PACK-iv); the §3.2 O-11 row recording the un-banked 2026-08-03 hostile pass
with the (H8) provenance disambiguated; the (AGR) residue widened to CU-4's
displayed [M] conditionalities) PLUS the GLOBAL ENUMERATION AUDIT killing the
class (§18's audit record). No [M] member, step proof content, or leaf pointer
changed; NO status syncs (VERIFIED = 9 stands). **The counter RESETS to ZERO
of 2**; VC14 re-runs the bar on this REVISION-12 text (charge at §18).
RE-ACCEPTANCE (2026-08-05; ledger "RE-ACCEPTANCE: THE BAR IS MET AT TWELVE
ROWS"): VC14 (Codex) clean + VC15 (Fable, the model-diverse pass)
COMPOSITION-SOUND with ZERO blockers, both on the identical REVISION-12 text —
the composition is RE-ACCEPTED at (ROOT-C)'s twelve-row displayed conditional
scope; the suspension arc closes. REVISION 13 (§19, unit ROOT r14) applied
POST-ACCEPTANCE — the hygiene + discharge-sync batch: VC15's four advisories
repaired (Step 18's Consumes gains Step 2; Step 4's grade conformed to §0 with
the NEW §3.2 M17/RegPFinite faithfulness-audit row; the RESUM-n3 billings carry
the G-3 scope caveat; the (PACK-iv) edge-set/proof-line wording reconciled and
the q/t variable convention declared); **(NSF-KEY) RETIRED** (Unit C executed,
commits 322ec54/dfdb558; the six (NSF-KEY-RET) artifacts on file; compiled
witness H9-U1) — the (H9) row's open member is now (NSF-INT) (its (K1-CANON)
premise owed); the D-12r row rewritten at its confirmed literature pins
(reclassification [M] → §4 VC-gated); the (H8) = (K-CUT) row re-scoped to the
landed 11-unit Lean development (REDUCED, not discharged; charge (vi-a) FAIL on
record — no retirement); the mid-campaign (E-N) rewrite (commit 89176a4)
formally ADOPTED with the process gap recorded; the H4 ROOT-sync briefs
adjudicated STILL-OWED (firing conditions unmet). The acceptance was EARNED on
the REVISION-12 text; REVISION 13 is a text change: **a confirming end-to-end
pass (VC16) is OWED to re-anchor the accepted text on the REVISION-13 wording**
(charge at §19). VC16 RAN on the REVISION-13 text — **COMPOSITION-SOUND, zero
blockers** (`lean/notes/openmath/VC16_report.md`): the acceptance RE-ANCHORS on
REVISION 13, and finding 2 adjudicates the D-12r reclassification WARRANTED.
REVISION 14 (§20, unit ROOT r15) applied: the D-12r row MOVED from §3.1 [M] to
the §4 literature-pin class (entry 10 — (HEN-LIFT) at Neukirch II §4 (4.6)
p. 129 / the displayed companion uniqueness lemma / Serre LF I §6 Prop 15
pp. 17–18; (OM-SAT) riding the §4 GMN entry at the TAMS-confirmed Cor 3.8 +
Def 3.9; dependency-decomposition routing preserved); consuming steps re-pointed
to "perimeter-conditional on the §4 D-12r pin"; the §3.1 roster is **ELEVEN rows
↔ (H1)–(H9)** with (H3) = {(ADM), (E-N)}; the §4 roster synced honestly per VC16
finding 2 (items 1/4/5 + the §3.3 LIT-1/LIT-3 rows). No step proof content, no
leaf pointer, no clause attribution changed. REVISION 14 is a text change: **a
confirming end-to-end pass (VC17) is OWED on the REVISION-14 wording** (charge
at §20). VC17 RAN on the REVISION-14 text — **COMPOSITION-GAPS, 1 blocker + 1
advisory, both display-conformance at the r15 edits** (all other charged checks
PASS; `lean/notes/openmath/VC17_report.md`). REVISION 15 (§21, unit ROOT r16)
applied: Step 12's (τ-irr) description locally re-pointed to
"perimeter-conditional on the §4 D-12r pin (entry 10)" — matching Steps
2/3/11/16/19 — with the §20 r15-5 already-conformant confirmation marked
RESOLVED; Step 4's grade rendered in the exact §0 word, PROVED-UNVERIFIED
(pending: the §3.2 M17/RegPFinite faithfulness-audit row; the Lean-core and
Step-5 facts kept as notes). Pure display syncs — no [M] member, step proof
content, leaf pointer, or clause attribution changed; ELEVEN rows stand.
REVISION 15 is a text change: **the confirming end-to-end pass (VC18) is OWED
on the REVISION-15 wording** (charge at §21).

---

## 7. REVISION 1 (2026-08-02, unit ROOT-R2) — disposition of VC1, the Phase C end-to-end pass 1

**The pass.** VC1 = the first end-to-end verification of this document (Codex, fresh
context, dependency-graph charge: "a named input counts only if an earlier step
supplies it, a ledger row classifies it, or the literature roster carries it";
`lean/notes/openmath/VC1_root.jsonl`). Verdict: **COMPOSITION-GAPS (findings 1–8
blockers, 9–11 advisories)** — "the assembly is not composition-sound as written...
forward edges, a genuine D-11/O-11 cycle, several unbound inputs, and an incomplete
literature inventory." Every finding was accepted and repaired IN PLACE (the body
above is edited, against the §6 byte-stability default, per the standing repair
directive); this section is the audit trail. The step-by-step walk's FAIL/GAP verdicts
(Steps 1–3, 6, 7, 9, 11, 14, 16–19) are subsumed by the findings below.

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| 1 | BLOCKER | Step 1 silently consumes T-6 (ns-null), supplied only at Step 2 | ACCEPTED; the edge is REMOVED, not waived: the dependency is re-pointed to Dfloor's own Appendix A (self-contained ns-null via LIT-8c, inside the VERIFIED leaf-#3 perimeter — the fact the leaf actually proves); T-6's (ns-null) is declared NOT consumed | Step 1 |
| 2 | BLOCKER | D-11 M2 consumes (S2) = O-11's datum while O-11 consumes D-11's pack side — a genuine cycle | ACCEPTED; STRATIFIED exactly as the verifier proposed: D-11 {M1, M1′, M3} = Step 17 ≺ O-11 = Step 18 ≺ D-11 M2 = NEW Step 18b; both results displayed; no mutual dependency remains | Steps 17/18/18b, audit trail |
| 3 | BLOCKER | B-1..B-7 outside the document's dependency universe | ACCEPTED; grades inlined at Step 2 from tree §2.1 (B-2/B-3 cores VERIFIED-LEAN; B-1/B-2doc/B-4/B-5/B-6/B-7 PROVED-UNVERIFIED; B-8 SUPERSEDED, uncited); §3.2 B-layer row added | Step 2, Step 3, §3.2 |
| 4 | BLOCKER | O-9's refuted census exponent misfiled as [V] ("a proof is on file" was false) | ACCEPTED for the composed state; since then the rev-2 repair LANDED (01c21e6: E′ strict left tail, countermodel now predicted exactly; JUNCTION-PIN withdrawn → (FRESH)(c)) — the row is [V] again, re-pass owed | Step 14, §3.1 (FRESH), §3.2 |
| 5 | BLOCKER | unbound inputs: C.1.5 (Step 6), D-5 (Step 7), (O6) (Step 11); Steps 9/16 lack Consumes lists | ACCEPTED; all suppliers displayed: C.1.5 = T-2's deferred obligation → the S-1 audit row; D-5's carrier design = O-2a rev 5 §§1–2 (proved content, debt = P(O-2a r5)); (O6) = O3design §1 definitional canonicalizations; Consumes lists ADDED to Steps 9 and 16; the registry re-worded as metadata | Steps 6/7/9/11/16, §3.3 |
| 6 | BLOCKER | displayed theorem unconditional; steps prove a conditional implication | ACCEPTED; (ROOT) re-badged as THE TARGET; **(ROOT-C)** — hypotheses (H1)–(H4) = exactly the §3.1 [M] rows — displayed as THE theorem the composition proves, with the [V]/[T] rows displayed as audit debts on the implication's proof, not statement hypotheses; Step 19 closes at (ROOT-C) | §1, Step 19 |
| 7 | BLOCKER | universal (UB) stronger than the Step-16 support (δ-stable classes, conditional (R1)–(R4)) | ACCEPTED; the exhaustion debt DISPLAYED as new §3.1 row **(UB-X)** (per-leaf O_δ audit; class exhaustion; aggregation/base-change commutation); Step 16 restated at honest scope; (ROOT-C)(H4) carries it | §1, Step 16, §3.1 |
| 8 | BLOCKER | literature roster incomplete: O4T's "Galois descent + one more"; K-FACT's GMN source unpinned | ACCEPTED; the missing pair NOW EXISTS — O4T rev 3 (bb4326c) added Facts **SEP** + **DES** as in-line-proved imports (inventory complete at eight named Facts, six-vs-eight reconciled); **K-FACT = GMN Thms 1.15/1.19 + 3.1/3.7** (`docs/GMN_citations.md`), all four loci already §4-pinned | Step 13, Step 15, §4 item 6 |
| 9 | ADVISORY | general-n 𝔅_n instance correspondence insufficiently explicit | ACCEPTED; **(B-INST)** displayed: the consumed pack is BY CONSTRUCTION the 𝔅_n member (three on-record organizations; OL-O12-1 the contingent escape); no theorem claims more; degree-3 realized-table identification = the OL-O12-2 data pass | Step 5 |
| 10 | ADVISORY | (AGR) mislocated in [M] | ACCEPTED; relocated to §3.2 as an inherited-[V] row (assembled from CU-2 + CU-4 + Cor 4.19, all on file) with a §3.1 tombstone note | §3.1, §3.2 |
| 11 | ADVISORY | Step 19 conflates O-11 (series = R_τ) with D-11 M2 (density = series) | ACCEPTED; both results displayed as labeled statements; Step 19's attribution now names both tranches and states neither alone delivers α = R_τ | Steps 18/18b/19 |

**Status syncs applied with the repairs** (all at the 2026-08-02 harvest, ledger
c43b13b): **GD23 = VERIFIED, leaf #4** (rev 4 gap repairs f9878d1 + gaps-closed check;
the premature d730669 adjudication superseded); **O4T = repaired at rev 3, awaiting
one full clean pass**; **CU-1 = rev 5 in flight** (rev 4 landed 8ab8ca6; gap-check
GAPS-REMAIN; NOT VERIFIED — the headline's leaf count corrected 5 → 4); **O-9 =
repaired at rev 2** (01c21e6), re-pass owed; **D-11/O-11 pass 1s = NOT ACCEPTED**
(2587c70 / a7ede72), r2s owed. Nothing in this revision upgrades any leaf; it corrects
this document's own composition and bookkeeping.

**What VC1 pass 2 should check first:** the three structural repairs (Step 1's
Appendix-A re-point; the 17 ≺ 18 ≺ 18b stratification; (ROOT-C) vs the Step-19 close),
then the §3 completeness standard ("every named pin/flag has exactly one ledger
home"), which findings 3/5 showed is the failure-prone invariant.

**Concurrent in-flight events recorded at REVISION 1 close** (landed by parallel
units while this revision was being written; statuses in the body are AS OF the
c43b13b harvest, with bracketed in-flight notes at the affected rows): **D-11 r2**
(566b967 — the pass-1 critical contained as specified; (S3) declared) and **O-11 r2**
(1bd8bd1 — D-a/D-b/D-c declared; oracle-pin supplement 10/10) landed, pass 2s
launched; **CU-1 r5** landed (6299a4a — the GAPS-REMAIN item repaired); the
PROJECT_STATE refresh landed (3896eb0). O-11 r2's §9 owed ROOT touches are EXECUTED
here: (1) Step 18 names D-a/D-b/D-c with engine-proxy language; (2) the §3.3
GD-4/GD-6 row gains its Step-18 entry line; (3) the §3.1 (R1)–(R4) row gains "hence
Step 18"; (4) the (I-τ)+K-LOC row already listed Step 18; (5) D-11's widened charge
items (v)–(vii) noted pending acceptance; (6) the 37/37 falsifier-count correction
displayed (§5 + Step 18). Bookkeeping note: commit 4e03512's staging sweep captured
an intermediate state of THIS revision alongside the O-11/D-11 fold; this commit is
the revision's unit of record.

— REVISION 1 complete. The body above is the operative composition; VC1 pass 2 runs
on this document as a whole.

---

## 8. REVISION 2 (2026-08-02+, unit ROOT) — disposition of VC2, the Phase C end-to-end pass 2

**The pass.** VC2 = the second end-to-end verification (Codex, fresh context, same
dependency-graph charge as VC1, run on the REVISION-1 document;
`lean/notes/openmath/VC2_root.jsonl`). Verdict: **COMPOSITION-GAPS** — 5 blockers +
2 advisories, numbered V2-1..V2-7 in report order. VC2 also CONFIRMED the three
REVISION-1 structural repairs: "the two theorem statements are honestly
distinguished", "the original D-11/O-11 cycle and Step-1 forward edge are repaired"
(V2-3 is the explicit ADVISORY confirming the Appendix-A re-point compositionally
valid). Every finding is dispositioned below and repaired in place; this section is
the audit trail. Repairs are at named sites only; the §2 walk, both theorem
statements' mathematical content, and all leaf grades are otherwise byte-stable —
nothing here upgrades any leaf.

**Headline finding (V2-6, the one REAL mathematical bug in this document):** the §1
clause-(R) display quantified the self-loop resummation 1/(1 − q^{−(e(e+1)/2−1)})
over unrestricted e — at e = 1 the denominator is 1 − q⁰ = 0. Adjudication against
the solve structure (O-12 rev 4 §§2.2–2.4 + Thm 1 + Cor D): **e ≥ 2 is FORCED at
every self-loop** — inter-block bookings descend strictly (Lemma L4: m, μ < e), so
self-loops are within-block only, and block 1 has NO loop (K_1 = (0), Φ_1 = 1,
E(1) = {1}; Theorem 1 is stated "for e ∈ {2, …, n}"; multiplicity-1 atoms terminate
as simple Hensel factors). The e = 1 corner is VACUOUS; the leaf was never wrong —
the ROOT display was. Proof now DISPLAYED as **(SL≥2)** at Step 5; §1 quantified.

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| V2-1 | BLOCKER | Step 18b names only (S1)/(S2)/(r1) while D-11 r2 declares seam (S3), sent to annex A-1 — an unpositioned annex edge; the acyclic composition not closed | ACCEPTED as a display gap; RESOLVED by the r2's own scoping, now displayed: (S3) = the projective assembly weight's MASS reading conditions ONLY the projective reading of M2's conclusion ("M2 as stated (monic ensemble) does not consume it" — D-11 r2 §3 + Theorem M2's bracket); Step 18b consumes M2 at MONIC scope = clause (0)'s ensemble, so NO §2 step consumes (S3); it is an annex A-1 obligation, in no (ROOT-C) hypothesis. Step 18b's scope display added; §3.3 row repositioned. The dependency graph closes without the edge | Step 18b, §3.3 |
| V2-2 | BLOCKER | (H4) declared "for clause (UB) only", but Step 18's K3-c consumes O-8b/(R1)–(R4) (declared row D-c), so clause (R) depends on part of H4; and the "Without (H4)" sentence asserted the (R1)–(R4)-conditional alphabet law hypothesis-free | ACCEPTED — a genuine leakage; (H4) SPLIT: **(H4a)** = (R1)–(R4), consumed by clause (R) (Step 18 K3-c, base-changed β-legs) AND clause (UB) (Step 16); **(H4b)** = (UB-X), clause-(UB)-only. Per-clause hypothesis attribution displayed in (ROOT-C); the "Without (H4)" sentence corrected (alphabet law now stated under (H4a)); Step 16/18/19 + §3.1 rows synced | §1, Steps 16/18/19, §3.1 |
| V2-3 | ADVISORY | Step-1 Appendix-A re-point compositionally valid (confirmation) | RECORDED; no action — the REVISION-1 finding-1 repair stands verified | Step 1 |
| V2-4 | BLOCKER | B-4 consumed at Step 2 and graded PROVED-UNVERIFIED, but the §3.2 B-layer row omits it — a consumed input with no ledger class | ACCEPTED; B-4 (order-r layer) ADDED to the §3.2 B-layer row with its full grade (order 1 = B-3's VERIFIED-LEAN core; order r = `M6_lemma.md`, modern face O-9's OL-B → GD-3, orders ≥ 2 riding §3.1's (GR-B) on the modern path). Three-class completeness restored | §3.2 |
| V2-5 | BLOCKER | Step 14's (ADM) hypothesis (O-9 r2) has no ledger home — a consumed condition in no class | ACCEPTED; new §3.1 [M] row **(ADM)** (ledger-admissibility of the census datum; PROVED automatic at orders ≤ 1; at orders ≥ 2 open but p-free and per-datum decidable — borderline [M]/[T], kept [M] for honesty); carried by **(ROOT-C)(H3)**; enters Step 14 (CEN-W/CEN-J) hence Step 18's K3 census half. Step 14 + §1 headline synced | §3.1, Step 14, §1 |
| V2-6 | BLOCKER | clause (R)'s self-loop formula unquantified — at e = 1 the denominator vanishes, contradicting ℛ-membership and pole-freeness; no step excluded e = 1 | ACCEPTED — the real bug; adjudicated as displayed in the headline above: **e ≥ 2 forced** (L4 strict descent + K_1 = (0)); proof DISPLAYED as **(SL≥2)** at Step 5 with the consumer audit (Steps 5/17/18 consume only through O-12's e ≥ 2-scoped theorems; no consumer instantiates e = 1); §1 formula quantified with the exponent bound e(e+1)/2 − 1 ≥ 2 | §1, Step 5, Step 19 |
| V2-7 | ADVISORY | roster consumer annotations incomplete: MacLane entry omits Step 9 (and Step 9 unpinned); GMN consumer list omits Step 15 | ACCEPTED; MacLane entry + Step 9 now pin the exact loci (Def 4.1 + Thm 4.2 = the (V1) frame-semantics pin, LITUNIT §4 MATCH; no further MacLane theorem enters O-14a′); GMN entry gains Step 15 (K-FACT = Thms 1.15/1.19 + 3.1/3.7, loci already pinned) | Step 9, §4 items 1/2 |

**The completeness invariant, re-run after these repairs** (the §7 closing charge —
"every named pin/flag has exactly one ledger home" — which V2-4/V2-5 again showed is
the failure-prone one): the (ROOT-C) hypothesis list is now (H1) (GR-B) ≥ 2, (H2)
(FRESH), (H3) D-12r + (E-N) + (ADM), (H4a) (R1)–(R4), (H4b) (UB-X) — exactly the
seven §3.1 [M] rows, none clause-misattributed; (S3) is the one declared seam with a
NON-mandatory home (annex A-1), displayed as such at both its touch points.

**What VC3 (the next end-to-end pass) should check first:** (1) the (SL≥2) display
against O-12 rev 4 (is the three-step forcing airtight, and is the consumer audit
complete?); (2) the (H4a)/(H4b) per-clause attribution in (ROOT-C) against Steps
16/18/19 (does any OTHER §3.1 row leak across clauses the same way?); (3) the (S3)
scoping against D-11 r2 §3 (is the monic/projective split verbatim-faithful?).

**Standing note on status syncs:** REVISION 2 makes NO status changes — VC2 was a
composition-only pass; leaf statuses remain as synced at REVISION 1 (the c43b13b
harvest + the recorded in-flight r2/r5 landings). The in-flight passes (D-11/O-11
pass 2s, CU-1 gap-check, O4T clean pass, O-9 re-pass) resolve at the next harvest
and their rows carry unchanged.

— REVISION 2 complete. The body above is the operative composition; VC3 runs on this
document as a whole.

---

## 9. REVISION 3 (2026-08-02 night, unit ROOT r4) — disposition of VC3, the Phase C end-to-end pass 3

**The pass.** VC3 = the third end-to-end verification and the first MODEL-DIVERSE one
(Claude/Fable, fresh context — VC1/VC2 were Codex; read the ROOT document ONLY, no
leaf records; `lean/notes/openmath/VC3_root_fable_verify.md`). Verdict:
**COMPOSITION-GAPS** — 4 blockers (B1–B4) + 4 advisories (A5–A8). The headline is
what VC3 CONFIRMED: "under all five hypotheses jointly, the composition proves
(ROOT-C) quantifier-for-quantifier from the leaves' declared statements + ledger
rows; every defect found is attribution/ledger integrity, not mathematics missing
beyond the displayed hypotheses." Explicit passes: acyclicity edge-by-edge over all
20 steps (incl. the 17 ≺ 18 ≺ 18b stratification); the completeness invariant
((ROOT-C) hypotheses ↔ the seven §3.1 [M] rows, EXACTLY EQUAL); and all three §8
pre-charged spots — the (SL≥2) display internally sound with its consumer audit, the
(H4a)/(H4b) split consistent as displayed, the (S3) monic/projective split coherent.
Every finding is dispositioned below and repaired in place; per the standing charge,
NO mathematical content changed — every repair is attribution, a ledger row, or a
status sync.

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| B1 | BLOCKER | the "exact" per-clause attribution understates clause (UB): it reads "(H4a) + (H4b)" as (UB)'s full hypothesis set, but (UB) also consumes (H1)–(H3) — through Step 8 ((GR-B) at orders ≥ 2), Step 14's census ((FRESH)/(ADM)), and the (UB-X)(a)-transported layers (D-12r, (E-N)); the (ADM) row's Enters-list omitted Step 16 | ACCEPTED — the V2-2 genre in reverse (consumed but unattributed); (ROOT-C)'s attribution completed: clause (UB) consumes **(H1)–(H3) + (H4a) + (H4b)**, only (H4b) is (UB)-exclusive; Step 16's conditionality restated in full; Step 19's (UB) close synced; the (ADM) and (FRESH) rows' Enters-lists gain Step 16/clause (UB). The JOINT theorem (all five hypotheses) was never affected | §1, Steps 16/19, §3.1 (ADM)/(FRESH) |
| B2 | BLOCKER | O-8b/M14's PROVED-UNVERIFIED conditional proof is consumed (Step 16; Step 18's K3 — hence clause (R)'s mandatory path) with NO ledger row naming its pending pass — the exact V2-4/V2-5 failure class | ACCEPTED; new §3.2 row **P(O-8b/M14)** (fresh hostile pass on the M14 proof, owners Steps 16/18); Step 16's status names the pass | §3.2, Step 16 |
| B3 | BLOCKER | Σ_τ R_τ = 1 credited to Step 1 beyond Dfloor's declared statement (R_τ undefinable in the leaf's perimeter; α = R_τ needs Steps 18/18b) — a textual forward edge inside a VERIFIED perimeter, consumed by Step 19 "from Step 1" | ACCEPTED; repaired exactly as the verifier prescribed, no new hypothesis: Step 1 restated to its declared perimeter (Σ_τ α = 1 at every p + the interpolation principle; Delivers-line synced), and the identity CONCLUDED at Step 19 with the derivation displayed (Σα = 1 [Step 1] + α = R_τ ∀p [Steps 18/18b, (H1)–(H3)+(H4a)] + the principle ⇒ Σ_τ R_τ = 1 in ℚ(t)); the conditionality was already priced inside clause (R) | Step 1, Step 19 |
| B4 | BLOCKER | B-3's document-level PROVED-UNVERIFIED grade (same status shape as B-2's: Lean-verified core + June document) missing from the §3.2 B-layer row — a consumed input with no ledger class (the V2-4 class) | ACCEPTED; **B-3 (doc level)** added to the B-layer row | §3.2 |
| A5 | ADVISORY | undisplayed interface match: O-8b's law holds at δ-STABLE position classes only, but nothing displays that K3-c's δ > 1 pools are δ-stable — if one were not, (UB-X)(b) would leak into clause (R) | ACCEPTED as an honesty display: the presumption is now DISPLAYED at Step 18 and recorded as an audit note on the §3.1 (R1)–(R4) row (the match is asserted at the D-c interface, not proved here); the match display (or a re-scope) rides the next O-11/O-8b touch | Step 18, §3.1 |
| A6 | ADVISORY | three consumer-list gaps: (i) GMN roster omits Step 11 (CU-2t consumes Cor 1.20/3.8 directly); (ii) the D-12r row omits Steps 2 and 19 (T-6's TB-CAP conditionality; the bracket consumes T-6); (iii) the (FRESH) row omits Step 10 while Step 10's Delivers-line names "the (FRESH) consumption site" | ACCEPTED; all three lists completed ((iii) resolved as CONSUMPTION at CU-1's step — the row itself prices (FRESH) "at the GD-2/CL-13 vertex-law layer = CU-1's step content"; clause (SQ)'s (H1)–(H3) attribution already covered it) | §4 item 1, §3.1 D-12r/(FRESH) |
| A7 | ADVISORY | OL-O12-1 (Step 5's contingent escape flag) has no ledger home, against the §7 closing standard | ACCEPTED; §3.3 row added (unfired, consumed by nothing — homed for the invariant) | §3.3 |
| A8 | ADVISORY | Step 19 discharges T-U's §U.SQ.6 kernel ledger by the ASSERTED-not-displayed claim that it equals the Steps 1–18b roster; a fresh reader cannot check it | ACCEPTED at the honest-marking option: Step 19's parenthetical now states the claim is asserted, and a §3.3 audit row owes the row-by-row display table (per-item pointer §U.SQ.6 → step/eviction/annex) | Step 19, §3.3 |

**Status syncs applied with the repairs** (the 2026-08-02-night harvest, ledger
7b28562 — "THE FIVE OWED PASSES" section): **CU-1 = VERIFIED, leaf #5** (r5 gap-check
GAPS-CLOSED 0/0, commit 5329a4e); **O-10 = VERIFIED at scope, leaf #6** (pass 2
model-diverse SOUND-WITH-GAPS, math airtight, 5296528; two minor gaps ride a §3.2
doc-touch row) — **VERIFIED = 6: O-13, O-12, Dfloor, GD23, CU-1, O-10** (headline +
(ROOT-C) proof-status synced 4 → 6; the discharged CU-1/O-10 §3.2 rows retired).
Wave-14 rows synced: **O-9** pass 2 UNSOUND (af2293e: the rev-2 (ADM) class-vs-value
attainment equivalence refuted by an r = 1 countermodel) → **r3 with the
VALUE-attained (ADM) reading** (§3.1 (ADM) row annotated); **CUC/CU-2t** pass 1
UNSOUND (b9d1d70: the Θ^τ ν-blindness critical + 9 gaps) → **CU-2t R2** (Definition
RC displayed; the (T-DEC)/(T-VERD) pricing honesty-corrected — the mandatory-path
residue GREW by two GD-4 rows, folded at Step 11 + the §3.3 GD-4/GD-6 row per the
R2's F4 charge), re-pass owed; **O4T** pass 3 SOUND-WITH-GAPS,
ZERO criticals (ed413ae) → **r4 (two one-liners)** + one confirming pass to
the bar. Also recorded (round-3 events pre-dating VC3, rows previously stale): D-11
pass 2 → **r3** (8a0786c, honestly LAYERED) and O-11 pass 2 (V32) → **r3** (7aef1b9,
tier 2 split into H-1..H-4), pass 3s owed. Nothing in this revision upgrades any
leaf beyond the ledger's own adjudications; it corrects this document's composition
bookkeeping and syncs recorded statuses.

**The completeness invariant, re-run after these repairs** (the failure-prone one
again — B2/B4 are its V2-4/V2-5 class, and B1 is its per-clause face): the (ROOT-C)
hypothesis list is unchanged — (H1) (GR-B) ≥ 2, (H2) (FRESH), (H3) D-12r + (E-N) +
(ADM), (H4a) (R1)–(R4), (H4b) (UB-X) — exactly the seven §3.1 [M] rows; the
per-clause attribution is now: (0) unconditional; (SQ)/(R) ← (H1)–(H3), (R)
additionally ← (H4a); (UB) ← (H1)–(H3) + (H4a) + (H4b); (H4b) (UB)-only. Every
consumed PROVED-UNVERIFIED input now has a named §3.2 pass (O-8b/M14 and B-3-doc were
the last two missing); every named pin/flag has exactly one ledger home (OL-O12-1 was
the last).

**What VC4 (the next end-to-end pass, Codex, toward the ≥ 2-consecutive-clean bar)
should check first:** (1) the B3 repair — does Step 19's displayed Σ_τ R_τ = 1
derivation consume only Steps 1/18/18b at their declared statements, and is Step 1's
restated perimeter now exactly the Dfloor leaf's?; (2) the completed (UB) attribution
against Steps 8/14/16 (is (H1)–(H3) + (H4a) + (H4b) now exact, neither under- nor
over-attributed?); (3) the A5 presumption at Step 18 (is the δ-stable audit note
scoped so that clause (R)'s (H4a)-only attribution is honest as displayed?); (4) the
completeness invariant, per standing §7/§8 experience.

**Concurrent in-flight events recorded at REVISION 3 close** (the wave-14 repair
commits landed by parallel units while this revision was being written; body statuses
are AS OF the 7b28562 harvest, with bracketed landed-at-close notes at the affected
rows): **O-9 r3** (460ea59 — (ADM) redefined VALUE-attained; automaticity rescoped
r = 0 automatic / r = 1 displayed decidable inequality / general r ≥ 1 withdrawn; K6
falsifier 0 = 0 exact at the pass-2 countermodel); **CU-2t R2** (unit of record
567814d — Definition RC; Θ^τ carries ν's announced pair; the +2 GD-4 rows pricing);
**O4T r4 + O-10 r2** (3956500 — display fixes, cores byte-stable; O-10's VERIFIED
stands per the wave-14 ledger). Confirming passes owed: O4T (acceptance), O-9 pass 3,
the CU-2t R2 re-pass. Bookkeeping note (the REVISION-1 4e03512 precedent): commit
9136564's staging sweep captured this revision's in-progress body repairs alongside
the wave-14 ledger fold; THIS commit is the revision's unit of record.

— REVISION 3 complete. The body above is the operative composition; VC4 runs on this
document as a whole, and the ≥ 2 consecutive-clean counter remains at ZERO.

---

## 10. REVISION 4 (2026-08-02+, unit ROOT r5) — disposition of VC4, the Phase C end-to-end pass 4

**The pass.** VC4 = the fourth end-to-end verification (Codex, fresh context, run on
the REVISION-3 document with the §9 priority charges;
`lean/notes/openmath/VC4_root.jsonl`). Verdict: **COMPOSITION-GAPS** — 4 blockers +
1 advisory, numbered VC4-1..VC4-5 in report order. What VC4 PASSED on the document's
own declarations: acyclicity (including Step 1's Appendix-A dependency and
17 ≺ 18 ≺ 18b); the (SL≥2) e = 1 exclusion and the displayed denominators; Step 19's
repaired Σ_τ R_τ = 1 derivation (Step 1 + Steps 18/18b + interpolation — the §9
pre-charged B3 repair); clause (UB)'s displayed joint attribution; and the syntactic
correspondence of the seven [M] rows to the grouped hypotheses (with findings 2/3
blocking the SEMANTIC completeness invariant — both now repaired). As at VC3, every
finding is attribution/ledger integrity; the joint conditional theorem was not
touched. Every finding is dispositioned below and repaired in place; NO mathematical
content changed — every repair is an attribution, a ledger row, or a status sync.

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| VC4-1 | BLOCKER | the δ-stable K3-c interface is assumed, not supplied: Step 18 says O-8b applies only to δ-stable position classes and the stability of K3-c's pools is merely ASSERTED — no (H4a) content or [V]/[T] row supplies it, so the claimed exact attribution "clause (R) ← (H1)–(H3)+(H4a) only" is unproved (under all five hypotheses jointly, (UB-X)(b) covers it) | ACCEPTED; the fragment is PRICED, not presumed: named **(K3-δ)** = (UB-X)(b) restricted to K3-c's realized δ > 1 pools, displayed on the (UB-X) row, and clause (R)'s attribution now consumes it explicitly ((H1)–(H3) + (H4a) + (H5) + (K3-δ)); "only (H4b)'s remainder beyond (K3-δ) is (UB)-exclusive" replaces the former sentence; the joint theorem is unchanged ((K3-δ) ⊆ (H4b)); the direct match display rides the next O-11/O-8b touch and would retire (K3-δ) from clause (R) | §1, Step 18, §3.1 (R1)–(R4)/(UB-X), Step 19 |
| VC4-2 | BLOCKER | (ADM) has two incompatible operative definitions — the §3.1 statement column still displayed the REFUTED class-attainment formula (β_k mod e ∈ {wt(𝐣) mod e}) while the r3 annotation said value-attainment governs; §1 and Step 14 retained the false "automatic at orders ≤ 1" | ACCEPTED; the (ADM) row REWRITTEN to a single operative definition — and synced one rung further to O-9 r4's FULL-attainment form (dim G_{β_k} = d; the r3 VALUE form itself was refuted at d ≥ 2 by O-9 pass 3), with both refuted displays quarantined inside the row as history; §1's (H3) parenthetical and Step 14's rev-2 text corrected to "automatic at r = 0 only"; every stale phrasing marked superseded | §3.1 (ADM), §1, Step 14 |
| VC4-3 | BLOCKER | an OPEN clause is consumed with no [M] hypothesis: Step 17's r3 status says the actual-pack correspondence (clause 4) is OPEN, yet Steps 18/18b consume Step 17 AS that correspondence, and the §3.2 D-11 [V] row cannot classify it (no proof on file) — composition closure and the completeness invariant break | ACCEPTED; new §3.1 [M] row **(PACK)** (D-11's clause 4 = OL-O12-2's remaining data half; borderline [M]/[T], kept [M] for honesty), carried as NEW hypothesis **(ROOT-C)(H5)**, consumed by clauses (R)/(UB) at Steps 17 → 18 (K2)/18b; the §3.2 D-11 row's scope restated (proof-layer passes only, clause 4 carved OUT); Step 17/18/18b/19 + the headline + the OL-O12-2 row synced. DISTINCT from D-11 item (vii) (H.6-fence conformance — the TREE-GAPS §3.3 row; promotion question flagged to VC5) | §3.1 (PACK), §1, Steps 16/17/18/18b/19, §3.2, §3.3 |
| VC4-4 | BLOCKER | clause (SQ)'s attribution is not exact: §1 attributed all of (H1)–(H3) incl. (ADM) to (SQ), but (ADM) enters only the Step-14 census/value chain, and for the §1 SEMANTIC definitions of decided_τ(k)/undec(k), Step 1 supplies the bracket and semantic drainage unconditionally — an over-attribution of (ADM) and the entire conditional classifier-transfer route | ACCEPTED — corrected DOWNWARD (a favorable honesty fix: the hypotheses shrink): clause (SQ) = Step 1's bracket + Step 13's semantic drainage layer, **unconditional at every prime, consuming NO §3.1 row**; the classifier-side counting machinery (D-15/T-6 bracket legs, the O4T transfer layer/(AGR)) re-attributed to the evaluation squeeze inside clause (R)'s scope; the REVISION-3 A6(ii)/(iii) phrasings ("the (SQ) bracket consumes T-6", "clause (SQ) inherits (FRESH)") superseded at the D-12r/(FRESH) rows; Step 13's Delivers-line restated | §1, Steps 13/19, §3.1 D-12r/(FRESH)/(ADM) |
| VC4-5 | ADVISORY | Step 18b's local hypothesis account omits (ADM): it consumes Step 14 through seam (S1) but summarized the inherited open rows as (GR-B)/(FRESH) only — a local consumer-accounting defect (the root-level clause-(R) attribution already included (ADM)) | ACCEPTED; Step 18b's (S1) line restated to the census's FULL displayed hypothesis set (GR-B)/(FRESH)/(ADM); the (ADM) row's Enters-list gains Step 18b via (S1) | Step 18b, §3.1 (ADM) |

**Folds executed with this revision (beyond VC4).**
(1) **The CU-2t R3 three-row GD-4 amendment** (81589e9; ledger amended at 35faac7):
the GD-4 terminal residue is THREE rows (T-DEC, split dec/cor at R3 · T-READ ·
T-VERD), count history 0 → 2 → 3 — folded at Step 11 + the §3.3 GD-4/GD-6 row (the
R3's §9.5 touch (3)); touch (5) (the W3-carrier/2(D)-scope delivery) folded into the
§3.2 P(O-2a r5) row's charge.
(2) **Tree DELTA 4 (unit TREE-GAPS) — landed CONCURRENTLY with this revision, its
ROOT touches executed by that unit in this document** (the [TREE-GAPS] tags): the
Step-18 H.6-fence display (audit gap G-4: SUPERSEDED AS ROUTED into D-11 widened-
charge item (vii), no (ROOT-C) hypothesis affected), the §3.3 widened-charge and
O-14a/b/c rows, and the closed-row/roster items (audit advisories A-1..A-5). This
revision's own Step-18 pending-fold placeholder was removed when that display
landed; the two units cross-reference and do not conflict — (H5)/(PACK) is D-11's
clause 4, item (vii) is a different D-11 clause, and both texts say so.
(3) **The REG leg** (044a6d9, accepted at e8e1839): all 18 standing falsifier/
verification suites re-run at HEAD — ZERO-REGRESSIONS, all diffable outputs
line-for-line identical; root criterion 3 evidence at HEAD (not an acceptance event).

**Status syncs applied with the repairs** (the confirming-passes/O-9-r4/CU-2t-R3
harvest, ledger 85670f7 → 35faac7): **O4T = VERIFIED, leaf #7** (pass 4 acceptance
CLEAN 0/0, 8602e97) — **VERIFIED = 7: O-13, O-12, Dfloor, GD23, CU-1, O-10, O4T**
(headline + (ROOT-C) proof-status synced 6 → 7; the discharged O4T §3.2 row
retired). **O-9**: pass 3 on r3 UNSOUND (b7df5c2: value ≠ FULL attainment at d ≥ 2)
→ **r4 with (ADM) at FULL attainment** (35faac7; K7 falsifier ALL PASS incl. the
first d ≥ 2 value gate) — **pass 4 in flight**; the §3.1 (ADM) row and (ROOT-C)(H3)
carry the r4 form. **CU-2t**: R2 pass SOUND-WITH-GAPS 0-critical (4190ca4) → **R3**
(81589e9, three-row pricing) — fresh pass on R3 owed. D-11/O-11 pass 3s unchanged
(owed). Nothing else moved.

**The completeness invariant, re-run after these repairs** (the standing failure-
prone check — VC4-3 is its missing-row face, VC4-1/VC4-4 its per-clause face): the
(ROOT-C) hypothesis list is now **(H1) (GR-B) ≥ 2, (H2) (FRESH), (H3) D-12r + (E-N)
+ (ADM), (H4a) (R1)–(R4), (H4b) (UB-X) (incl. its named (K3-δ) fragment), (H5)
(PACK)** — exactly the EIGHT §3.1 [M] rows (seven at REVISION 3 + (PACK)), none
clause-misattributed. Per-clause attribution: **(0) unconditional** (Step 1); **(SQ)
unconditional** (Steps 1 + 13-semantic — corrected downward at VC4-4); **(R) ←
(H1)–(H3) + (H4a) + (H5) + (K3-δ)**; **(UB) ← (H1)–(H3) + (H4a) + (H4b) + (H5)**;
only (H4b)-minus-(K3-δ) is (UB)-exclusive. Every consumed PROVED-UNVERIFIED input
has a named §3.2 pass; every consumed OPEN clause is now a §3.1 row ((PACK) was the
last); every named pin/flag has exactly one ledger home (the TREE-GAPS rows homed
the audit's G-1..G-5/A-1..A-5 residue). The one flagged promotion question — D-11
item (vii), [T] vs hypothesis grade — is displayed at its §3.3 row for VC5.

**What VC5 (the next end-to-end pass, toward the ≥ 2-consecutive-clean bar) should
check first:** (1) the VC4-4 downward correction — is clause (SQ) as displayed in §1
(semantic definitions) genuinely carried by Step 1 + Step 13's semantic layer alone,
and is no classifier-transfer content silently needed by the (SQ) statement?; (2)
the (PACK)/(K3-δ)/(H5) additions against Steps 17/18/18b — is the eight-row
bijection exact, and is (K3-δ)'s ⊆-(H4b) containment displayed correctly at both
touch points?; (3) the single-definition (ADM) row against O-9 r4 (is the FULL-
attainment form quoted faithfully, with no stale automaticity phrasing surviving
anywhere — grep "orders ≤ 1"); (4) the TREE-GAPS folds (Step 18's fence display +
the widened-charge row: does item (vii) belong at [T], or is it hypothesis-grade
like clause 4?); (5) the completeness invariant, per standing experience.

**Concurrency and bookkeeping.** This revision was written concurrently with the
TREE-GAPS unit's DELTA 4 ROOT touches in this same document (both landed cleanly at
disjoint sites; each references the other where they meet — Step 18 and the §3.3
rows). Commit 698a629's checkpoint sweep captured in-progress states of both; the
commit closing this revision is its unit of record. Body statuses are AS OF the
85670f7/35faac7 harvest; the in-flight passes (O-9 pass 4, the CU-2t R3 pass, the
D-11/O-11 pass 3s) resolve at the next harvest and their rows carry unchanged.

— REVISION 4 complete. The body above is the operative composition; VC5 runs on
this document as a whole, and the ≥ 2 consecutive-clean counter remains at ZERO.

---

## 11. REVISION 5 (2026-08-03, unit ROOT R6) — disposition of VC5, the Phase C end-to-end pass 5

**The pass.** VC5 = the fifth end-to-end verification (Codex, fresh context, run on
the REVISION-4 document with the §10 priority charges;
`lean/notes/openmath/VC5_root.jsonl`). Verdict: **COMPOSITION-GAPS** — 2 blockers +
1 advisory, numbered VC5-1..VC5-3 in report order. The two blockers are ONE genre,
and it is this document's OWN rule turned on itself: §3's character classes define
[T] as "content pinned to printed sources or existing proofs; writing/auditing owed,
no new mechanism expected" — but four §3.3 rows (CU-2t's three terminal rows and
D-11's widened-charge item (vii)) are OPEN PROPOSITIONS consumed on clause (R)'s
mandatory path, expressly described by their own row text as "genuinely NEW unproved
mandatory-path obligations" resp. "proved nowhere at general n". Placing them in [T]
does not supply them; they are undischarged mathematical premises, analogous to
(PACK) at VC4-3. The repair is the honest one: PROMOTION to a §3.1 [M] row group,
carried as the NEW hypothesis **(ROOT-C)(H6)**. As at VC3/VC4, no mathematical
content changed — the leaves were never wrong; this document's classification of
what it consumes was.

**What VC5 PASSED on the displayed material** (the verifier's own pass-list): the
operative eight-row hypothesis mapping syntactically EXACT (H1 = GR-B; H2 = FRESH;
H3 = D-12r, E-N, ADM; H4a = R1–R4; H4b = UB-X; H5 = PACK); clause (0) unconditional
from Step 1; clause (SQ) unconditional from Step 1's bracket + Step 13's semantic
drainage under the §1 semantic definitions (no classifier-transfer hypothesis
needed — the VC4-4 downward correction VERIFIED); the FULL-attainment (ADM) as the
single operative form (older formulations only in explicitly superseded history);
(PACK)/(K3-δ) correctly propagated through Steps 17–19, (K3-δ) visibly a fragment
of (H4b); acyclicity of the displayed dependency graph including the repaired
Step-1 edge and 17 ≺ 18 ≺ 18b; the Step-19 Σ_τ R_τ = 1 derivation; and no
additional undeclared forward edge in the visible walk.

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| VC5-1 | BLOCKER | D-11 item (vii) consumed but misclassified: Step 18's K3-a requires general-n H.6 in-fence conformance of the built tables, which the document itself says is "proved nowhere at general n", yet it sat in §3.3 as [T] — an undischarged mathematical premise on clause (R), analogous to (PACK); the eight [M] rows were not the complete hypothesis list | ACCEPTED; item (vii) PROMOTED to §3.1 — a member of the NEW row group **(H6)**; Step 18's fence-display routing ("no (ROOT-C) hypothesis is affected") RETRACTED and superseded in place; the §3.3 widened-charge row keeps items (v)/(vi) only; the REVISION-4 promotion flag is resolved (promoted) | §3.1 (H6), §1, Steps 18/19, §3.3 widened-charge row, (PACK) row |
| VC5-2 | BLOCKER | the three GD-4 terminal obligations lack suppliers: Step 18 consumes (T-DEC)/(T-READ)/(T-VERD) through D-a/K4-CYL; §3.3 calls them "genuinely NEW unproved mandatory-path obligations" and (T-READ) has "NO displayed machine-side home on file"; placing them in [T] does not supply them — clause (R) silently relied on unresolved content outside its eight [M] hypotheses | ACCEPTED; the three rows PROMOTED to §3.1 as the terminal-seam members of **(H6)**, with CU-2t R4's grading carried (per-row: (T-READ) GD-2/GD-5-family read-semantics conformance — a proof-by-construction route cannot discharge it; (T-DEC)/(T-VERD) GD-4 with construction routes on record); consumed by clause (R) at Step 11's route (CU-2t's §9.5 conditionality, which Step 18 consumes through D-a/K4-CYL) and by clause (UB) through the transported chain; the §3.3 GD-4/GD-6 row keeps only genuine engine-bookkeeping [T] content | §3.1 (H6), §1, Steps 11/18/19, §3.3 GD-4/GD-6 row |
| VC5-3 | ADVISORY | the T-U closing correspondence remains externally opaque: the §U.SQ.6 → Steps 1–18b row-by-row mapping is asserted, not displayed — openly recorded as a [T] debt, so not itself a hidden edge, but the document alone cannot establish that §U.SQ.6 contains no additional consumption | RECORDED, no reclassification: this is exactly the REVISION-3 A8 audit row doing its job — the row stays the recorded [T] audit debt (annotated as re-confirmed); the display table (per-item pointer §U.SQ.6 → step/eviction/annex) remains owed there and gates acceptance through §3.3's emptying, not through a hypothesis | §3.3 T-U row |

**The (H6) group, stated once** (full display at the §3.1 row): (T-DEC) = emission
discipline at decided sites + the corner fence (split dec/cor at CU-2t R3); (T-READ)
= the emitted shape/position fields equal the (c1)-shape polygon-side datum (the
(RC-read) supply); (T-VERD) = terminal verdict-field conformance; item (vii) =
general-n H.6 in-fence conformance of the built tables. Owners: GD-4/GD-2-family per
CU-2t R4's grading; the D-11 owner for item (vii). Enters clauses (R)/(UB) only.

**Status syncs applied with the repairs** (the 6a7593b harvest — "O-9 + CU-2T
VERIFIED"): **O-9 = VERIFIED, leaf #8** (pass 4 SOUND 0-critical → r5 gloss purge,
9335057 → pass 5 confirming CLEAN 0/0, 825378d, K7 re-executed byte-identical) — the
order-r census layer at its honest scope (CEN-W/CEN-J conditional on (GR-B) +
(FRESH) + (ADM)-FULL, all displayed §3.1 rows, unchanged by the adjudication).
**CU-2t = VERIFIED at scope, leaf #9** (R3 pass 0-critical, both logic repairs
verified → R4, b9f05db: the (T-READ) re-grade + prose repairs → R4 spot-check CLEAN,
d14cd8c, no further pass owed per the verifier's own disposition) — conditional per
§9.5 on exactly the three terminal rows (= the (H6) terminal members) + the pinned
literature. **VERIFIED = 9: O-13, O-12, Dfloor, GD23, CU-1, O-10, O4T, O-9, CU-2t**
(headline synced 7 → 9; the discharged §3.2 rows retired). Note the interlock, said
plainly: CU-2t's VERIFIED grade is exactly what makes the (H6) promotion the RIGHT
repair rather than a leaf reopening — the leaf is verified MODULO the three rows it
displays, and the composition must therefore carry them as hypotheses until the
conformance units land. D-11/O-11 pass 3s unchanged (owed). Nothing else moved.

**The completeness invariant, re-run after these repairs** (the standing
failure-prone check — VC5-1/VC5-2 are its missing-row face, at the [T]/[M] boundary
this time): the (ROOT-C) hypothesis list is now **(H1) (GR-B) ≥ 2, (H2) (FRESH),
(H3) D-12r + (E-N) + (ADM), (H4a) (R1)–(R4), (H4b) (UB-X) (incl. its named (K3-δ)
fragment), (H5) (PACK), (H6) the terminal-seam engine-conformance group
((T-DEC)/(T-READ)/(T-VERD) + item (vii))** — exactly the NINE §3.1 [M] rows/groups
(eight at REVISION 4 + the (H6) group), none clause-misattributed. Per-clause
attribution: **(0) unconditional** (Step 1); **(SQ) unconditional** (Steps 1 +
13-semantic); **(R) ← (H1)–(H3) + (H4a) + (H5) + (K3-δ) + (H6)**; **(UB) ←
(H1)–(H3) + (H4a) + (H4b) + (H5) + (H6)**; only (H4b)-minus-(K3-δ) is
(UB)-exclusive. Every consumed PROVED-UNVERIFIED input has a named §3.2 pass; every
consumed OPEN proposition is now a §3.1 row (the four (H6) members were the last —
and the VC5 genre check is now standing: a [T] row must never carry a consumed open
proposition); every named pin/flag has exactly one ledger home.

**What VC6 (the next end-to-end pass, toward the ≥ 2-consecutive-clean bar; model
diversity favors Fable) should check first:** (1) the (H6) promotion — is the
NINE-row bijection exact, are the four members' consumption routes as displayed
(Step 11 §9.5 / Step 18 D-a/K4-CYL / Step 18 K3-a), and is the (R)/(UB)-only
attribution neither under- nor over-stated (in particular: does the (UB) inheritance
through the transported chain hold row-for-row)?; (2) the VC5 genre, re-audited
document-wide: walk EVERY remaining §3.3 [T] row against the §3 classification rule
— does any other [T] row carry an open proposition consumed on a mandatory path
(candidates the VC5 pass-list did not explicitly clear: the S-1 audit row, the
(I-τ)+K-LOC interface row, the GD-7 flags)?; (3) the T-U §U.SQ.6 row (VC5-3, thrice
recorded): does the opacity gate the acceptance criterion as displayed, and should
the display table be executed before VC7?; (4) the CU-2t/O-9 VERIFIED-at-scope
interlocks — do Step 11's and Step 14's synced statuses consume their leaves at
exactly the adjudicated scopes (nothing upgraded in transit)?; (5) the completeness
invariant, per standing experience.

**Concurrency and bookkeeping.** Body statuses are AS OF the 6a7593b harvest; the
in-flight passes (the D-11/O-11 pass 3s) resolve at the next harvest and their rows
carry unchanged. This revision executes the ROOT-side sync of the 6a7593b
adjudication in the same unit as the VC5 disposition (one fold, no intermediate
state); the commit closing this revision is its unit of record (unit ROOT R6).

— REVISION 5 complete. The body above is the operative composition; VC6 runs on
this document as a whole, and the ≥ 2 consecutive-clean counter remains at ZERO.

---

## 12. REVISION 6 (2026-08-03+, unit ROOT R7) — disposition of VC6, the Phase C end-to-end pass 6

**The pass.** VC6 = the sixth end-to-end verification and the SECOND model-diverse
one (Claude/Fable, fresh context, run on the REVISION-5 document with the §11
priority charges; `lean/notes/openmath/VC6_root_fable_verify.md`). Verdict:
**COMPOSITION-GAPS** — 3 blockers (F-1/F-2/F-3) + 6 advisories (A-4..A-9). The
three blockers are ONE genre, and it is the VC5 genre WALKED TO COMPLETION: the
[T]→[M] classification rule ("a [T] row must never carry a consumed open
proposition") applied at REVISION 5 to D-a's terminal content and item (vii), but
not yet to the sibling declared row D-b ((I-τ)/K-LOC), not to O-2a's GD-1 residue
face, and not to the GD-6 instances Step 10's own conditionality line displays. As
at VC3/VC4/VC5, the composition's MATHEMATICS was re-confirmed — VC6's pass-list:
acyclicity edge-by-edge over all 20 steps (no new forward edge from the REVISION-5
edits), the NINE-row bijection EXACT, per-clause attribution consistent at every
displayed site, Σ_τ R_τ = 1, (SL≥2), both unconditional clauses, the leaf-#8
interlock exact, and the (H6) promotion mechanics correctly executed. Every finding
is dispositioned below and repaired in place; NO mathematical content changed and
NO status moved (a composition-only pass; VERIFIED = 9 stands at the 6a7593b
harvest).

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| F-1 | BLOCKER | declared row D-b ((I-τ)/K-LOC) — the LAST unpromoted member of O-11 r2's D-a/D-b/D-c triple — is a consumed open interface in [T]: K-LOC is the document's own named OPEN lemma (M07 OL-3), consumption triple-displayed (Step 15's application line; Step 18's K1; the row's owner column), and "both priced into the GD families" had NO displayed [M] landing | ACCEPTED; PROMOTED as its OWN row **(H7)** (adjudication displayed at the §3.1 row: engine-conformance GENRE kin to (H6), but a disjoint SEAM (K1/product law vs the terminal seam), disjoint owners (O-10/GD-family vs CU-2t/D-11), and (H6)'s membership is pinned by the leaf-#9 interlock — so a sibling row, not an (H6) member); consumed by clauses (R)/(UB); §1/Steps 15/16/18/19/§3.1/§3.3-tombstone synced | §3.1 (H7), §1, Steps 15/16/18/19, §3.3 |
| F-2 | BLOCKER | GD-1's (KP)/(KPirr) clauses beyond order 1 (= OL-2a-1, an exactly-stated open lemma of consumed O-2a) sat in [T] with a pin covering ONLY the properness leg, while the same-shape GD-2/GD-3 residue was (H1) — asymmetric classification of the proved-≤1/open-≥2 dictionary family | ACCEPTED; PROMOTED into the ENLARGED **(H1) group** as member (b) (most natural home: the graded-dictionary family at orders ≥ 2, per the charge); the §3.3 (KP)/(KPirr) row RESCOPED to the pinned properness-proviso leg only (FGMN Lemma 5.3, keeps [T]); Step 7's OL-2a-1 mapping and Step 8's ownership line re-pointed | §3.1 (H1)(b), §1, Steps 7/8, §3.3 (KP)/(KPirr) row |
| F-3 | BLOCKER | Step 10's consumed "GD-6 instances at orders ≥ 2" had no supplying row: (GR-B)'s row was scoped GD-2/GD-3-only, and GD-6's §3.3 home holds "proof-by-construction routes on record" — routes, not proofs (the exact evidentiary status under which (T-DEC)/(T-VERD) were promoted at VC5-2); the GD-4/GD-6 row's post-promotion D-a consumption display was also ambiguous | ACCEPTED; the consumed GD-6 conformance instances PROMOTED into **(H1)** as member (c) — the option under which (H1)'s row was understating its own content, matching Step 10's "through (GR-B)" routing (now "through (H1)"); the GD-4/GD-6 row keeps only the UNCONSUMED bookkeeping remainder with its consumption scope now displayed (kept content consumed at construction level by no mandatory step; D-a's open content = exactly (H6)) | §3.1 (H1)(c), Step 10, §3.3 GD-4/GD-6 row |
| A-4 | ADVISORY | Step 11 renders CU-2t's conditionality twice, unreconciled: the R3-era "P(O-2a r5) + TB-CAP's D-12r perimeter + the GD residue including the three terminal rows" vs the adjudication's "conditional per §9.5: the three terminal rows + the pinned literature"; "including" invited a ⊇ reading | ACCEPTED; the reconciling display added at Step 11: NEW-vs-INHERITED split (the §9.5 form prices the leaf's NEW conditionality exactly; the R3 sentence's other items are inherited debts at named homes — P(O-2a r5) §3.2, D-12r (H3), the GD residue at its §3.1/(H1)/(H6)/§3.3 homes), the earlier sentence marked superseded-as-display, and "including" declared ⊇-free | Step 11 |
| A-5 | ADVISORY | the (H6) route display omitted the Step-12 → Step-18-K1 "CU leaf labels" leg (terminal labels ride O5triple's counting into K1 — the same route the (FRESH) row displays) | ACCEPTED; the K1/CU-leaf-labels leg added to the (H6) route display at §1, the §3.1 (H6) row, Step 18's K1 line, and Step 19; no clause attribution changes ((R)/(UB) already carry (H6)) | §1, §3.1 (H6), Steps 18/19 |
| A-6 | ADVISORY | four [T] sites displayed neither proof-on-file status nor consumption scope: (i) the S-1 row's degree-g > 1 KEY1 generalization; (ii) widened-charge item (v) (rsh_interp/r_bdd/wsh_ok); (iii) the GD-8 order-≥ 2 row; (iv) (FR≡)'s refutable order-2 content | ACCEPTED; item-(vii)-style adjudication lines added at all four rows: (i) no proof on file, consumed by no order-≤ 1 mandatory step, ≥ 2 face re-scoped to (H1)'s campaign perimeter (LC-COH-style record); (ii) no proof-on-file display exists — the three pack-row laws are PRICED INSIDE (H5)=(PACK)'s correspondence perimeter, flagged to VC7; (iii) consumed by NO mandatory step (quarantined corollary non-mandatory; O-9's verified reduction names exactly TWO ≥ 2 opens; Step 10 consumes the (H1) group, not GD-8) — recorded open, unconsumed; (iv) adjudicated a declared CONVENTION pin of the Step-6 sloped/sheared genre — the leaf is verified OVER the pin, the refutable content makes it falsifiable and the gates ran coherent, [T] = the same-convention transcription audit | §3.3 (four rows) |
| A-7 | ADVISORY | GD-7's five consumed flags are pinned only at section granularity, expressly NOT covered by the executed LITUNIT — [T] only under a generous reading of "pinned", against the two-false-axioms history | ACCEPTED at the named-precondition option: the row now states the discharge (per-flag verbatim extraction lines, LITUNIT-style MATCH verdicts, one session with the LIT-1 pagination duty) is a NAMED precondition of the §6 acceptance bar, displays the partial per-flag loci already on record ((GMN-COH) ← the eq.-(37) sprouted-type identity; (R-1) ← the "of type t" shape; (R-2) ← the order-r exact-key convention), and bars consumption from the range pin alone; the session itself is dispatch (§6) | §3.3 GD-7 row, §6 |
| A-8 | ADVISORY | stale "seven leaves VERIFIED" inside the (ROOT-C) proof-status display (a REVISION-5 sync miss; headline/§3.2/§11 all said NINE) | ACCEPTED; corrected to NINE with the sync-miss noted in place | §1 |
| A-9 | ADVISORY (standing, third consecutive recording) | the T-U §U.SQ.6 row-by-row correspondence remained asserted, not displayed — a fresh verifier structurally cannot certify "no §U.SQ.6 item falls outside the steps" from this document | ACCEPTED; **EXECUTED** — the NEW §3.4 displays the per-item pointer table (§U.SQ.6 items 1–21 → step/eviction/annex) PLUS the Lean-bridge kernel-row cross-walk (census §2.3 / `BridgeKernels.lean`), with warrants cited per row and the three NEW-at-the-table adjudications flagged (items 4/13c consumer-audit evictions; the items-15–21 empirical-roster disposition); Step 19's parenthetical re-pointed; the §3.3 audit row records the execution and retires on VC7's confirmation | §3.4 (NEW), Step 19, §3.3 T-U row |

**The completeness invariant, re-run after these repairs** (the standing
failure-prone check — F-1/F-2/F-3 are its missing-row face at the [T]/[M] boundary,
the VC5 class): the (ROOT-C) hypothesis list is now **(H1) the graded-dictionary
group ((GR-B) + GD-1-beyond-1 + the consumed GD-6 conformance instances — ENLARGED
here), (H2) (FRESH), (H3) D-12r + (E-N) + (ADM), (H4a) (R1)–(R4), (H4b) (UB-X)
(incl. its named (K3-δ) fragment), (H5) (PACK), (H6) the terminal-seam
engine-conformance group ((T-DEC)/(T-READ)/(T-VERD) + item (vii)), (H7) the
(I-τ)/K-LOC sibling-interface pair (NEW here)** — exactly the TEN §3.1 [M]
rows/groups (nine at REVISION 5, with the (H1) row enlarged in place + the (H7) row
added), none clause-misattributed. Per-clause attribution: **(0) unconditional**
(Step 1); **(SQ) unconditional** (Steps 1 + 13-semantic); **(R) ← (H1)–(H3) +
(H4a) + (H5) + (K3-δ) + (H6) + (H7)**; **(UB) ← (H1)–(H3) + (H4a) + (H4b) + (H5) +
(H6) + (H7)**; only (H4b)-minus-(K3-δ) is (UB)-exclusive. Every consumed
PROVED-UNVERIFIED input has a named §3.2 pass; every consumed OPEN proposition is a
§3.1 row — the O-11 r2 declared triple D-a/D-b/D-c is now FULLY adjudicated (D-a →
(H6) at VC5-2, D-b → (H7) here, D-c → (H4a)/(K3-δ) at V2-2/VC4-1), and the O-2a OL
residue's six families each have an explicit consumed-face home ((H1) for
GD-1/GD-2/GD-3/GD-6's open faces; §3.3 scoped rows for GD-4's kept bookkeeping,
GD-5's audit, GD-7's flags, GD-8's unconsumed seam); every named pin/flag has
exactly one ledger home (the (I-τ)/K-LOC tombstone preserves the trail). The T-U
closing edge is no longer asserted-only: §3.4 displays it (three NEW dispositions
flagged to VC7 there).

**What VC7 (the next end-to-end pass, toward the ≥ 2-consecutive-clean bar) should
check first:** (1) the §3.4 table — row-by-row against the frozen §SQ.6 list
(MOVES 13542–13609) and census §2.3: is every mapping faithful to the cited
warrant, and are the three NEW adjudications sound (items 4/13c: do X.1a/(X1a-ALIGN)
really have no consumer outside the struck rate family?; items 15–21: is the
flags-never-substitutes disposition airtight at Step 19)?; (2) the (H1)
enlargement — do Steps 10–12 consume GD-1/GD-6's ≥ 2 faces at exactly the displayed
member scopes, and does any OTHER OL-2a face remain consumed-but-unpriced?; (3) the
(H7) row — is the own-row adjudication argument sound, are Step 15/18-K1's
consumption displays exact, and does (UB)'s inheritance hold for the pair?; (4) the
A-6 adjudications — in particular item (v)'s priced-inside-(H5) argument against
the O-11 r3 restated D-11 entry; (5) the completeness invariant (TEN rows/groups ↔
(H1)–(H7)), per standing experience.

**Concurrency and bookkeeping.** No status syncs ride this revision (VC6 was a
composition-only pass; the D-11/O-11 pass 3s remain the in-flight debts and their
rows carry unchanged). The §3.4 execution consumed two sources OUTSIDE the prior
four-corners set (`lean/notes/MOVES_2026-07-24.md` §SQ.6;
`lean/notes/REMAINING_CENSUS_2026-08-01.md` §2.3 + `MovesU/BridgeKernels.lean`) —
they enter as DISPLAY warrants only, never as suppliers (nothing in §2 consumes
them); the commit closing this revision is its unit of record (unit ROOT R7).

— REVISION 6 complete. The body above is the operative composition; VC7 runs on
this document as a whole, and the ≥ 2 consecutive-clean counter remains at ZERO.

---

## 13. REVISION 7 (r8, VC7 fold, 2026-08-03) — disposition of VC7, the Phase C end-to-end pass 7

**The pass.** VC7 = the seventh end-to-end verification (Codex, fresh context, run
on the REVISION-6 document with the §12 priority charges;
`lean/notes/openmath/VC7_root_report.md`). Verdict: **COMPOSITION-GAPS** — 2
blockers + 3 advisories. Both blockers are attribution/warrant findings at the two
REVISION-6 A-6 adjudications VC6 itself had flagged forward: (i) a consumed
ALL-ORDER proposition whose [M] pricing was a perimeter GESTURE — no (H1) member
displayed the degree-g > 1 KEY1 statement Step 6's all-site theorems consume at
order-≥ 2 reads; and (ii) a law-transfer INFERENCE the displayed (PACK)
correspondence could not warrant — entry-for-entry equality with 𝔅_n transfers
rsh_interp/r_bdd/wsh_ok to the concrete pack only if a displayed theorem supplies
them at the 𝔅_n rows, and none does (Steps 2/5 state none of the three by name nor
an implication to them). The composition itself was CONFIRMED — VC7's findings
6–11: the §3.4 T-U tables compositionally genuine (all 15 Table B rows homed; the
refuted Lean rows not consumed as suppliers; the six H.6 kernels not silently
assumed; the items 4/13c evictions sound), the (H1) enlargement otherwise sound and
the (H7) row sound incl. (UB)'s inheritance, the TEN-row syntactic bijection
correct with per-clause attribution internally consistent, the remaining linear
walk proving (ROOT-C) quantifier-for-quantifier at its displayed conditional scope,
and acyclicity passing (17 ≺ 18 ≺ 18b genuinely stratified; the §3.4 cross-walk
adds no proof dependency). Every finding is dispositioned below and repaired in
place; NO mathematical content changed and NO status moved (a composition-only
pass; VERIFIED = 9 stands at the 6a7593b harvest).

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| VC7-1 | BLOCKER | the A-6(i) KEY1 adjudication does not price the open higher-order content: Step 6 states KEY1 outright only at order ≤ 1 while its theorems are ALL-SITE; establishing non-consumption by order-≤ 1 steps does not address the higher OM orders, and "lives in the (H1) campaign perimeter" names NO member — none of (H1)(a)–(c) assumes the degree-g > 1 statement, so it remained a consumed open proposition without an [M] home | ACCEPTED; the degree-g > 1 KEY1 generalization ADDED as the explicitly displayed FOURTH member **(H1)(d)** (§3.1 — stated precisely, matching Step 6's use: the (d1)–(d3) key-compliance block at degree-g > 1 keys under the standard lift, = O-1thr's (V1)/(V2) block beyond its proved order-≤ 1 slice, the block Lemma WGEO consumes); Step 6 gains the order-≥ 2 conditionality display and the (H1)(d) Consumes entry; the §3.3 S-1 adjudication SUPERSEDED in place (mathematical face → (H1)(d); the row keeps only the transcription/composition audit); §1's (H1) display, Step 7's OL-2a-5 mapping, and the headline synced | §3.1 (H1)(d), §1, Steps 6/7, §3.3 S-1 row |
| VC7-2 | BLOCKER | the A-6(ii) pricing of D-11 item (v) inside (H5) lacks the required warrant: Step 18-K2 consumes rsh_interp/r_bdd/wsh_ok, but (PACK) as stated gives only entry-for-entry correspondence, which transfers the laws only if an earlier displayed theorem establishes them at the corresponding 𝔅_n rows — no such theorem is displayed, so (PACK) had not been shown to supply all three predicates (exactly the flagged contingency) | ACCEPTED — the flag FIRED, resolved by HONEST ENLARGEMENT (not by claiming the inference): **(H5) = (PACK)'s displayed statement now carries the three laws at the 𝔅_n rows as explicit clauses (PACK-i) rsh_interp / (PACK-ii) r_bdd / (PACK-iii) wsh_ok** (§3.1 — each stated: interpolation Rsh → Rval at every prime power; the [0,1] bound; the evalAt regular-subring membership); Step 18's K2 line consumes the enlarged (H5) with the row-law legs displayed; the §3.3 item-(v) adjudication re-pointed (the REVISION-6 correspondence-only inference WITHDRAWN as invalid); §1's (H5) display and Steps 16/19's inheritance lines synced | §3.1 (PACK), §1, Steps 16/18/19, §3.3 widened-charge row |
| VC7-3 | ADVISORY | Table A's combined 6(iii)–(ix)+7 row understates inherited conditionality: its purported exhaustive residue list named only (H1), (FRESH), (ADM), (PACK), (H6), though its own routes through Steps 6/11/12/17 inherit D-12r and (E-N), hence the full (H3); the reading rule prevented a hidden hypothesis, but the list was inaccurate | ACCEPTED; the residue list corrected to name the FULL (H3) ((ADM) + D-12r + (E-N)) with the correction's warrant displayed in the row | §3.4 Table A |
| VC7-4 | ADVISORY | (H1)'s properness attribution not one-home clean: (H1)(a) read "with the properness proviso" while (H1)(b) and the §3.3 (KP)/(KPirr) row assign the properness-proviso leg to [T] pinned at FGMN Lemma 5.3 — the same e·g = 1 condition carried two classifications | ACCEPTED; (H1)(a)'s wording now explicitly EXCLUDES the properness-proviso leg, whose SOLE home is the pinned §3.3 [T] row — the clause has one classification | §3.1 (H1)(a) |
| VC7-5 | ADVISORY | the items-15–21 empirical-roster adjudication is sound only as a proof-consumption ruling, not as retirement of the acceptance gates — the §3.4 wording risked a "no remaining campaign obligation" reading | ACCEPTED; the row's wording sharpened in place: NOT a (ROOT-C) hypothesis, AND not a gate retirement — the §5 rerun acceptance-gate obligation (root criterion 3) stands in full | §3.4 Table A |
| VC7-6..11 | CONFIRMATIONS (findings 6–11) | the §3.4 warrants compositionally genuine (items 4/13c evictions sound; H.6 kernels not silently assumed; refuted Lean rows not consumed; all 15 Table B rows homed); the (H1) enlargement otherwise sound (GD-1 via O-2a at Steps 10–12, GD-6 at Step 10); the (H7) row sound (distinct seam/owners; (UB) inheritance with the (UB-X)(a) re-base audit); the TEN-row bijection + per-clause attribution correct ((0)/(SQ) unconditional; (R)/(UB) as displayed); the walk proves (ROOT-C) quantifier-for-quantifier; acyclicity passes | RECORDED; the §3.3 T-U audit row RETIRED on the table confirmation (tombstone kept; Step 19's parenthetical synced); per the VC8 charge below, these confirmations need no re-litigation but must not regress | §3.3 T-U row, §3.4, Step 19 |

**The completeness invariant, re-run after these repairs** (the standing
failure-prone check; this revision's blockers are its member-level face — a
consumed open proposition must be a DISPLAYED [M] member, not a perimeter gesture
or an unwarranted corollary): the (ROOT-C) hypothesis list is **(H1) the
graded-dictionary group — now FOUR displayed members: (a) (GR-B) (properness-proviso
leg EXCLUDED, single-homed at the §3.3 (KP)/(KPirr) [T] pin — VC7-4), (b)
GD-1-beyond-1, (c) the consumed GD-6 conformance instances, (d) the degree-g > 1
KEY1 generalization (NEW here); (H2) (FRESH); (H3) D-12r + (E-N) + (ADM); (H4a)
(R1)–(R4); (H4b) (UB-X) (incl. its named (K3-δ) fragment); (H5) (PACK) — now the
correspondence PLUS the three explicit row-law clauses (PACK-i/ii/iii) (ENLARGED
here); (H6) the terminal-seam engine-conformance group; (H7) the (I-τ)/K-LOC
sibling-interface pair** — exactly the TEN §3.1 [M] rows/groups, the row COUNT
UNCHANGED from REVISION 6: (H1) and (H5) grew members/clauses IN PLACE, no new row
and no new hypothesis letter. Per-clause attribution unchanged: **(0)
unconditional** (Step 1); **(SQ) unconditional** (Steps 1 + 13-semantic); **(R) ←
(H1)–(H3) + (H4a) + (H5) + (K3-δ) + (H6) + (H7)**; **(UB) ← (H1)–(H3) + (H4a) +
(H4b) + (H5) + (H6) + (H7)**; only (H4b)-minus-(K3-δ) is (UB)-exclusive. The new
members' consumption sites are displayed: (H1)(d) at Step 6 (the all-site theorems
at order-≥ 2 reads, propagating with D-14/D-15's deliveries to Steps 10–12/17);
PACK-i/ii/iii at Step 18's K2/evaluation glue (the restated D-11 entry item (v)) —
both inside clauses (R)/(UB) only. Every consumed PROVED-UNVERIFIED input has a
named §3.2 pass; every consumed OPEN proposition is a displayed §3.1 member (the
two VC7 blockers were the last known consumed-open-without-a-member sites); every
named pin/flag has exactly one ledger home (the properness leg now single-homed —
VC7-4; the retired T-U audit row and the (I-τ)/K-LOC row keep tombstones preserving
the trails).

**The VC8 charge (the next end-to-end pass, toward the ≥ 2-consecutive-clean
bar).** Verify the two enlargements and the advisory fixes: (1) **(H1)(d)** — is
the displayed statement exactly what Step 6's all-site theorems consume at
order-≥ 2 sites (the WGEO/(V1)/(V2) key-compliance block at degree-g > 1 keys under
the standard lift, no more and no less), and is its consumption display complete
(Step 6's conditionality line + the propagation through D-14/D-15's consumers)?;
(2) **the (H5) enlargement** — are PACK-i/ii/iii stated correctly against O-11 r3's
§3.5 glue and §5 row 20 (interpolation, [0,1] bound, evalAt membership, at the 𝔅_n
rows), does Step 18-K2's consumption line now carry them honestly, and does NO text
still lean on the withdrawn correspondence-only inference?; (3) the three advisory
fixes (the 6(iii)–(ix)+7 residue list naming the full (H3); the (H1)(a) one-home
exclusion; the items-15–21 gate-preserving wording); (4) the completeness invariant
(TEN rows/groups ↔ (H1)–(H7); (H1) at FOUR members; (H5) at correspondence + three
clauses), per standing experience. **The confirmations of findings 6–11 need NOT be
re-litigated — but they must not REGRESS:** any edit-induced break in the confirmed
items (the §3.4 tables, the (H7) row, the TEN-row bijection, per-clause
attribution, acyclicity, the quantifier-for-quantifier walk) is a VC8 finding.

**Concurrency and bookkeeping.** No status syncs ride this revision (VC7 was a
composition-only pass; the D-11/O-11 pass 3s remain the in-flight debts and their
rows carry unchanged). No new sources enter the four-corners set — both
enlargements display content already named in consulted records (O-1thr's KEY1 and
the §3.3 S-1 row for (H1)(d); O-11 r3's §5 row 20 / restated D-11 item (v) for
PACK-i/ii/iii); the commit closing this revision is its unit of record (unit ROOT
r8).

— REVISION 7 complete. The body above is the operative composition; VC8 runs on
this document as a whole, and the ≥ 2 consecutive-clean counter remains at ZERO.

---

## 14. REVISION 8 (r9, VC8 fold, 2026-08-03) — disposition of VC8, the Phase C end-to-end pass 8

**The pass.** VC8 = the eighth end-to-end verification (Codex, fresh context, run
on the REVISION-7 document with the §13 charges;
`lean/notes/openmath/VC8_root_report.md`). Verdict: **COMPOSITION-GAPS** — 1
blocker + 1 advisory, the NARROWEST residue of the eight passes, both findings at
the REVISION-7 repairs themselves: (i) the new (H1)(d) member quantified over
realized sites "whose current key has degree > 1", parenthetically identified
with the order-≥ 2 reads — an identification NO earlier statement proves (the
document itself recognizes e·g = 1 refinement reads, where key degree need not
increase; nor does the proved order-≤ 1 slice cover a higher-order site merely
because its key stays degree 1), with a further ambiguity between "degree-g > 1
key" and the residual factor ψ of degree g — so the claimed all-site coverage,
and with it the completeness invariant's semantic face, was unsupported; (ii) the
PACK K2/K3 sub-attribution was not exact — several summaries assigned all three
row-laws to "K2/evaluation glue" while the §3.1 row itself routes wsh_ok to Step
18's K3 evaluation (no clause-level loss: (H5) covers both). Everything else was
CONFIRMED: all three r8 advisory repairs pass (Table A's 6(iii)–(ix)+7 row names
the full (H3); the properness-proviso leg one-home; items 15–21 non-hypothesis
with the §5 acceptance-gate rerun preserved); the TEN-row count syntactically
correct (H1, FRESH, R1–R4, ADM, UB-X, PACK, H6, H7, D-12r, E-N ↔ (H1)–(H7)); the
walk supports the displayed quantifiers and clause scopes ((0)/(SQ)
unconditional; (R)/(UB) carrying their listed hypotheses; Σ_τ R_τ = 1 properly
delayed to Step 19); acyclicity passes incl. 17 ≺ 18 ≺ 18b; NO regression in the
§3.4 T-U tables, (H7), the syntactic bijection, or the pack-law non-inference.
Both findings are dispositioned below and repaired in place; NO mathematical
content changed and NO status moved (a composition-only pass; VERIFIED = 9
stands at the 6a7593b harvest).

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| VC8-1 | BLOCKER | (H1)(d) does not demonstrably cover every Step-6 order-≥ 2 site: it quantified over realized sites "whose current key has degree > 1", parenthetically identified with the order-≥ 2 reads — an UNPROVEN identification (e·g = 1 refinement reads keep degree-1 keys at higher order; the proved order-≤ 1 slice does not cover a higher-order site merely because its key stays degree 1), plus a key-degree vs residual-factor-degree-g conflation — so the claimed all-site coverage and the per-clause completeness invariant remained unsupported | ACCEPTED; the member REQUANTIFIED IN PLACE to READ ORDER: (H1)(d) is now **the order-≥ 2 KEY1 block** — KEY1's (d1)/(d2)/(d3) conclusion block asserted at EVERY realized site of Step 6's machine at OM ORDER ≥ 2, membership by read order alone, IRRESPECTIVE of the current key's degree, explicitly INCLUDING order-≥ 2 sites whose key remains degree 1 (e.g. e·g = 1 refinement chains), for the standard key lift Φ_{i+1} with read side (e, h) and residual factor ψ of degree g ≥ 1 (g is ψ's degree, unconstrained — the key's degree is irrelevant to membership); the COVERAGE ARITHMETIC displayed at the row: the proved slice is exactly order ≤ 1 (GD23 dictionary + O-1thr (V1)/(V2) at order ≤ 1), so (d) covers precisely the COMPLEMENT of the proved slice in Step 6's site range and total coverage consumes NO key-degree/order equivalence; the member renamed at every current-status site ((H1)(d) title, §1's display, Step 6's conditionality + Consumes lines, Step 7's OL-2a-5 mapping, the §3.3 S-1 adjudication, headline/§3.2/§6/footer) — historical revision records (§§7–13) untouched | §3.1 (H1)(d), §1, Steps 6/7, §3.3 S-1 row |
| VC8-2 | ADVISORY | the PACK internal K2/K3 routing not consistently described: several summaries assigned all three row-laws to "K2/evaluation glue" while the §3.1 PACK row itself says PACK-iii is an input to Step 18's K3 evaluation — no clause-level loss ((H5) covers both), but the sub-attribution was not exact | ACCEPTED; made UNIFORM everywhere: **PACK-i (rsh_interp) + PACK-ii (r_bdd) enter at Step 18's K2 evaluation glue** (the Rsh → Rval conversion; the [0,1] bound behind ENNReal.ofReal); **PACK-iii (wsh_ok) is the membership input to Step 18's K3 evaluation ring-hom evalAt (with D-6)** — synced at §1's (H5) display, the §3.1 (PACK) row, Step 18's K2 and K3 lines, Step 19's evaluation-chain attribution, and the §3.3 widened-charge item-(v) adjudication; clause-level home unchanged ((H5), consumed by clauses (R)/(UB)) | §3.1 (PACK), §1, Steps 18/19, §3.3 widened-charge row |
| VC8-C | CONFIRMATIONS | the three r8 advisory repairs pass; the TEN-row count syntactically correct; the walk supports the displayed quantifiers and clause scopes (Σ_τ R_τ = 1 delayed to Step 19); acyclicity incl. 17 ≺ 18 ≺ 18b; no regression on the §3.4 T-U tables, (H7), the syntactic bijection, or the pack-law non-inference | RECORDED; per the VC9 charge below, these need no re-litigation but must not regress | — |

**The completeness invariant, re-run after these repairs** (the standing
failure-prone check; this revision's blocker is its QUANTIFIER face — a displayed
[M] member must cover its consumption site by its OWN quantifier, not through an
unproven identification): the (ROOT-C) hypothesis list is **(H1) the
graded-dictionary group — FOUR displayed members, unchanged in count: (a) (GR-B)
(properness-proviso leg excluded, single-homed at the §3.3 (KP)/(KPirr) [T]
pin), (b) GD-1-beyond-1, (c) the consumed GD-6 conformance instances, (d) the
order-≥ 2 KEY1 block (REQUANTIFIED IN PLACE here — membership by read order,
covering exactly the complement of the proved order-≤ 1 slice in Step 6's site
range, no key-degree/order equivalence consumed); (H2) (FRESH); (H3) D-12r +
(E-N) + (ADM); (H4a) (R1)–(R4); (H4b) (UB-X) (incl. its named (K3-δ) fragment);
(H5) (PACK) — the correspondence plus the three explicit row-law clauses
PACK-i/ii/iii; (H6) the terminal-seam engine-conformance group; (H7) the
(I-τ)/K-LOC sibling-interface pair** — exactly the TEN §3.1 [M] rows/groups,
UNCHANGED from REVISION 7: no new row, no new member, no new hypothesis letter —
(H1)(d) was requantified in place. Per-clause attribution unchanged: **(0)
unconditional** (Step 1); **(SQ) unconditional** (Steps 1 + 13-semantic); **(R)
← (H1)–(H3) + (H4a) + (H5) + (K3-δ) + (H6) + (H7)**; **(UB) ← (H1)–(H3) + (H4a)
+ (H4b) + (H5) + (H6) + (H7)**; only (H4b)-minus-(K3-δ) is (UB)-exclusive. The
corrected members' consumption sites are displayed: (H1)(d) at Step 6's all-site
theorems at EVERY order-≥ 2 site (read-order membership — proved slice order
≤ 1 + assumed complement order ≥ 2 = the full site range, with no unproven
identification in between), propagating with D-14/D-15's deliveries to Steps
10–12/17; PACK-i/ii at Step 18's K2 evaluation glue and PACK-iii at Step 18's
K3 evaluation ring-hom (with D-6) — all inside clauses (R)/(UB) only. Every
consumed PROVED-UNVERIFIED input has a named §3.2 pass; every consumed OPEN
proposition is a displayed §3.1 member whose quantifier covers its consumption
site; every named pin/flag has exactly one ledger home (tombstones preserved).

**The VC9 charge (the next end-to-end pass, toward the ≥ 2-consecutive-clean
bar).** Verify ONLY: (1) **the (H1)(d) requantification** — does the displayed
member now cover Step 6's order-≥ 2 use WITHOUT any unproven identification
(membership by read order alone, key degree irrelevant, degree-1-key e·g = 1
refinement chains included), and is the proved-slice complement arithmetic sound
(the proved slice exactly order ≤ 1; (d) exactly the complement in Step 6's site
range; total coverage consuming no key-degree/order equivalence)?; (2) **the
PACK sub-attribution sync** — PACK-i/ii at Step 18's K2 evaluation glue and
PACK-iii at Step 18's K3 evaluation ring-hom (with D-6), uniform across §1, the
§3.1 (PACK) row, Step 18's K2/K3 lines, Step 19, and the §3.3 widened-charge
row, with NO surviving OPERATIVE summary lumping all three into "K2/evaluation
glue" (the historical revision records §§7–13 quote the former displays as
provenance and are not consumption displays — this section's invariant re-run
is the operative one);
(3) **no regression** on the VC7/VC8-confirmed items (the three r8 advisory
repairs, the §3.4 tables, the (H7) row, the TEN-row bijection, per-clause
attribution, acyclicity, the quantifier-for-quantifier walk, the Step-19
Σ_τ R_τ = 1 derivation, the pack-law non-inference). Any edit-induced break in a
confirmed item is a VC9 finding.

**Concurrency and bookkeeping.** No status syncs ride this revision (VC8 was a
composition-only pass; the D-11/O-11 pass 3s remain the in-flight debts and
their rows carry unchanged). No new sources enter the four-corners set — the
requantified member displays content already named in consulted records
(O-1thr's KEY1/(V1)/(V2) block and Step 6's site range), and the sub-attribution
restates the §3.1 (PACK) row's own K2/K3 routing; the commit closing this
revision is its unit of record (unit ROOT r9).

— REVISION 8 complete. The body above is the operative composition; VC9 runs on
this document as a whole, and the ≥ 2 consecutive-clean counter remains at ZERO.

---

## 15. REVISION 9 (r10, post-acceptance hygiene, 2026-08-03) — the ACCEPTANCE record + disposition of VC10's three advisories

**The passes and the acceptance.** VC9 = the ninth end-to-end verification
(Codex, fresh context; `lean/notes/openmath/VC9_root_report.md`) and VC10 = the
tenth (Fable, fresh context — the model-diverse pass;
`lean/notes/openmath/VC10_fable_report.md`) both RAN on the IDENTICAL REVISION-8
text and both returned **COMPOSITION-SOUND with ZERO blockers**. The tree §4
Root criterion — ≥ 2 consecutive CLEAN end-to-end passes, ≥ 2 models, verifiers
who saw none of the per-leaf rounds — is MET. **THE ACCEPTANCE STANDS: the
composition is ACCEPTED at (ROOT-C)'s displayed conditional scope** (milestone
addendum, `docs/PROJECT_STATE.md` 2026-08-03; nothing in this revision weakens,
re-opens, or re-litigates it). Acceptance certifies the composition — the
linear walk, the ten-row bijection, the per-clause attribution, acyclicity —
NOT the discharge of §3's ledger: the theorem as proved remains the conditional
(ROOT-C), and the acceptance path's remaining criteria ((1) empty §3.1/§3.2,
(3) the §5 matrix re-run, (4) the completeness audit) stand as displayed at §6.

**This revision is DISPLAY HYGIENE ONLY, and the counter is unmoved.** VC10's
three findings were all ADVISORY (its own closing ruling: "display/ledger
imprecision strictly narrower than VC8's residue; none affects the joint
conditional theorem, the ten-row bijection, the per-clause attribution, or the
acyclic walk"). Advisories do NOT reset the consecutive-clean counter: **the
counter is TWO and STAYS TWO.** Verified of this revision's own edits: NO §3.1
[M] member changed (the D-12r/(E-N) edits touch those rows' notes/consumer-list
columns only — statement columns byte-identical; no row added, removed, or
re-membered; the TEN-row roster and the (H1)–(H7) grouping are unchanged); NO
step content changed (the one step touched is Step 9, whose Statement/Consumes/
Delivers lines are byte-identical — only its Status grade display is rewritten);
NO clause attribution changed (§1's (ROOT-C) display, per-clause lists included,
is byte-untouched — the D-12r/(E-N) clause-(UB) entries were ALREADY displayed
there since the REVISION-3 B1 fix; the rows' lists are brought up to §1, not §1
to the rows). The touched surfaces, exhaustively: Step 9's Status line; the NEW
§3.2 row P(O-14a′ r3); the §3.1 D-12r and (E-N) rows' notes columns; the §3.4
Table A "6(iii)–(ix) + 7" residue list; and the sync surfaces (headline, the
§3.2 end-to-end row, §6 dispatch, footer). Historical sections §§7–14 untouched.

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| VC10-A1 | ADVISORY | Step 9 certified O-14a′ as "TWICE-STABLE across V11/V15" — not one of the four declared §0 grades, absent from the nine-leaf VERIFIED roster, no named §3.2 pass row — so the §§13/14 invariant ("every consumed PROVED-UNVERIFIED input has a named §3.2 pass") was checkable there only by trusting the cited CUC §0 registry line; asymmetric with O-2a (core thrice confirmed, still carrying P(O-2a r5)) | ACCEPTED; NEW §3.2 row **P(O-14a′ r3)** records exactly what is ON RECORD (core Theorems 1–3 stable across the V11/V15 passes, rev 3 on file; the consumed instance RRE(p, ZMod p) FORMALIZED Lean-core; the CUC §0 registry line as provenance) and what is OWED (the V11/V15 arc's models/cleanliness display, or one fresh named pass); Step 9's Status line REWRITTEN in the declared vocabulary — **PROVED-UNVERIFIED with the named pass row P(O-14a′ r3)** — NOT promoted to VERIFIED (the V11/V15 models/cleanliness are not displayed in-document, so the §0 VERIFIED bar is not checkable from the four corners). The asymmetry precedent is thereby RESOLVED: every consumed PROVED-UNVERIFIED leaf now carries a named §3.2 pass row, O-14a′ included | Step 9, §3.2 (new row) |
| VC10-A2 | ADVISORY | §1 displays D-12r and (E-N) entering clause (UB) through the (UB-X)(a)-transported dictionary/CU/drainage layers (the B1 attribution), but the D-12r row's Enters-list had no Step-16/(UB) entry and the (E-N) row's "NOT consumed by the mandatory path elsewhere" read as denying it — a residual consumer-list gap of exactly the VC3-A6/B1 genre (the B1 fix reached the (ADM)/(FRESH) rows but not these two) | ACCEPTED; the Step 16/clause-(UB) transport entry ADDED to both rows' consumer lists (mirroring the B1 fix at (ADM)/(FRESH)), and the (E-N) row's "elsewhere" sentence REWORDED to scope explicitly over sites OTHER than the CU-4 touch and its (UB) transport, so it can no longer be read as contradicting §1. NO attribution change: §1's per-clause display already carried both entries — the rows' lists are completed up to it | §3.1 D-12r/(E-N) rows |
| VC10-A3 | ADVISORY | §3.4 Table A row "6(iii)–(ix) + 7": the VC7-3-corrected residue list ((H1) group, (FRESH), the FULL (H3), (PACK), (H6)) was still not exhaustive by the table's own reading rule — the row's own route through Step 18's K3 inherits (H4a) + (K3-δ) at K3-c's base-changed β-legs, which row 9 names explicitly but this list did not | ACCEPTED; the list RUN TO CLOSURE: **(H4a) + (K3-δ)** ADDED with the K3-c base-changed β-legs warrant (the δ > 1 pools — exactly row 9's display), and the row now STATES the list is closed under the table's own reading rule (step pointers inherit each step's declared conditionality; every step route the row names traced). As at VC7-3, the reading rule already prevented a hidden hypothesis — the correction is to the list's exhaustiveness claim only | §3.4 Table A |

**Bookkeeping.** No status syncs ride this revision beyond the O-14a′
grade-display conformance (the same record, no adjudication changed — now
displayed in the declared vocabulary); the D-11/O-11 pass 3s remain the
in-flight [V] debts and their rows carry unchanged; VERIFIED = 9 stands at the
6a7593b harvest. No new sources enter the four-corners set: every fact the new
displays state is quoted from records this document already cites (the O-14a′
rev-3/V11/V15 record and the CUC §0 registry line at Step 9; §1's B1
attribution; row 9's (H4a)+(K3-δ) warrant). The commit closing this revision is
its unit of record (unit ROOT r10).

— REVISION 9 complete. The ACCEPTANCE (VC9 + VC10 on REVISION 8) STANDS; this
revision changes displays only; the ≥ 2 consecutive-clean counter is TWO and
stays TWO. [Historical close — superseded by the §16 suspension record.]

## 16. REVISION 10 (r11 executed, 2026-08-04, unit ROOT r11) — the suspension record: stop-the-line CURED, acceptance NOT re-earned, counter ZERO

**The suspension (what happened).** The Phase-C acceptance (VC9 + VC10 on the
REVISION-8 text, §15) was **SUSPENDED at the 2026-08-03 stop-the-line** (ledger
`BRIDGE_ADJUDICATIONS_2026-07-30.md` entry at 44ce0a7; `docs/PROJECT_STATE.md`
correction addendum): the owed D-11 pass 3 (`D11_owedpass_2026-08-03.md`, Codex,
fresh context) returned **CRITICAL** (6 criticals/3 gaps), and the hostile impact
audit showed two MANDATORY steps of REVISION 9 consuming the refuted layers AS
PROVED — Step 17 ← Theorem M1 (refuted at its announced conditioning scope, at
the family-(v) per-entry mass claim, at its billing, and at M1(b)'s multi-step
disjointness) and Step 18b ← Theorem M2 (FALSE at its displayed universal scope;
the missing premise billed in NO hypothesis row). The VC9/VC10 passes remain
valid AS composition-level audits — what failed was the leaf under its owed
pass, exactly what the owed-pass discipline exists to catch.

**The cure (what this revision consumes and why nothing refuted remains).**
Governing design: `lean/notes/openmath/R11_REPAIR_DESIGN_V5_2026-08-04.md`
(five Codex plan-review rounds; execution-ready per its §9). Unit A produced
the repaired leaf `D11_rev4_2026-08-04.md` — true-scope repairs at every pass-3
finding site plus the written-out obligations ((b-PF)/(b-EX)/(LOOP-∞),
(O-1)–(O-5), (O-GLOB), (ROW-Σ)) and audits ((CH-COV) §4A, the (ROW-Σ) edge walk
§2A.4, the (NSF-INT) leaf proof §4B) — which then passed its own hostile bar:
**r4 pass 1 CRITICAL** (`D11r4_pass1_2026-08-04.md`; folded at the leaf's
REVISION 2), **r4 pass 2 GAPS-ONLY** (`D11r4_pass2_2026-08-04.md`: no critical
remains, all six pass-1 repairs VALID, the design-§7 obligations adjudicated AS
PROOFS; its three bookkeeping gaps folded at the leaf's REVISION 3). THIS
revision (Unit B) re-points ROOT's consumption at that final state:

| edit | disposition |
|---|---|
| E1 (Step 17 Statement) | REWRITTEN to the r4 true-scope form: per-clause reference measures; families (iv)/(vi)/(vii) excluded at (a″); the "Σ = 1 across each row" fragment RESCOPED to cite **(ROW-Σ)** (the leaf's §2A.2 PROVED theorem: solved totals = 1; per-booking verdict-row totals = Φ^{(O1)}/Φ^{(O2′)}/Φ^{(O3)}); (b) via (b-PF) + (b-EX) at its route-(a) scope; (LOOP-∞) exact; M3 at separable-lift scope with the (H9) routing price |
| E2 (Step 17 Status) | the pass-3 CRITICAL record + the r4 two-pass record displayed; the leaf's confirming pass rides §3.2 |
| E3 (Step 18b Proof) | re-pointed to Theorem M2 (r4) AT its (RE) scope; arbitrary-α members excluded in-statement; the definitional discharge WITHDRAWN; (RE) at the consumed member = **(PACK-iv)** |
| E4 ((H5) enlargement) | **(PACK-iv) sem_alloc** added as the FOURTH row-law (an addition, never a clarification); consumption set = the leaf's CLOSED two-element edge set {Step 18b direct; Step 17's E1 (ROW-Σ) delivery display} — the design's "untraced cover" clause replaced by the executed §2A.4 walk (residue (EDGE-RES)) |
| E5 (new rows) | **(H8) = (K-CUT)** (the Lemma-3.3 cut kernel, owed-pass G-2; retirement = O-11 §8 charge (vi)) and **(H9) = (NSF)** ((NSF-INT) with its displayed marks-vocabulary + **(K1-CANON)** premises housed in-member; (NSF-KEY) KNOWN FALSE AT HEAD, retiring ONLY by the sign-off-gated Unit C per (NSF-KEY-RET)); §1's (ROOT-C) display, per-clause attribution, Step 18's K1/K4 lines synced |
| E6 (§3.4) | rows 6(iii)–(ix)+7 and 13: "(M1: entries ARE masses)" pointers re-scoped to the r4 per-clause form |
| E7 (§3.2 D-11 row) | pass-3-owed disposition RETIRED → the two-pass r4 record; O-11's row stays owed, unchanged |
| E8 (standing) | header/§1/§3.2/§6/footer read SUSPENDED-then-CURED with the counter at ZERO; no display reads "accepted" as current status |
| E9 ((H9) displays) | Step 18's K1/K4 lines + Step 17's M3 leg carry the (H9) target AT ITS INTERFACE-PIN SCOPE with both members + two-channel attribution + (CH-COV) status; NO display covers the HEAD wiring ((NSF-KEY) unretired — Unit C has NOT run; no executed status is written) |
| E-M3 (Step 12) | the "every lift g" delivery RESCOPED to every MONIC SEPARABLE lift + the ns-null mass note (the leaf's REVISION-2 gap-6/G6 record) |

Nothing refuted is consumed-as-proved anywhere: Step 17/18b consume the
twice-passed repaired layers at their displayed scopes, and every premise the
audit found unpriced is a displayed [M] member ((PACK-iv), (H8), (H9)).

**The completeness invariant, re-run.** §3.1 has **TWELVE rows ↔ (ROOT-C)'s
(H1)–(H9)**: (H1) the graded-dictionary family · (H2) = (FRESH) · (H3) =
{(ADM), D-12r, (E-N)} (three rows, one group) · (H4a) = (R1)–(R4) · (H4b) =
(UB-X) · (H5) = (PACK) (row-law roster now FOUR: PACK-i/ii/iii/iv, plus the
correspondence clause) · (H6) the terminal-seam group · (H7) = (I-τ)/K-LOC ·
**(H8) = (K-CUT)** · **(H9) = (NSF)** (two members). Ten → twelve; (PACK-iv)
adds a MEMBER, not a row. Per-clause attribution: (0)/(SQ) UNTOUCHED
(unconditional, consuming no §3.1 row); (R) and (UB) each gain (H8) + (H9).

**Acyclicity note.** The stratification Step 17 ≺ Step 18 ≺ Step 18b is
INTACT: (ROW-Σ) is proved at Step 17's layer with NO M2 content (hypotheses:
(PACK-iv), M1(a)(ii)/(iii), M1(d), the O-12 solve displays); (PACK-iv) is
hypothesis-class with no step-dependencies, entering at Step 18b (direct) and
at Step 17's E1 display only; no 18b content flows into 17/18.

**The counter and the bar.** REVISION 10 is a text change after the VC9/VC10
passes, and the acceptance those passes earned is suspended: **the
CONSECUTIVE-CLEAN COUNTER = ZERO.** The acceptance condition is restated in
full: **two consecutive CLEAN end-to-end passes on the IDENTICAL REVISION-10
text, fresh-context verifiers, ≥ 2 models (the model-diverse bar), verifiers
who saw none of the per-leaf rounds** — the tree §4 Root criterion, exactly as
at REVISION 8. Any text change after a pass restarts the counter.

**VC-next charge (the next end-to-end pass).** Verify (i) the r11 edits
themselves — E1/E2/E3/E4/E5/E6/E7/E8/E9/E-M3 above, each against the D-11 r4
leaf's final state (REVISION 3) and design V5; (ii) the TWELVE-row invariant —
the §3.1 roster ↔ (H1)–(H9) bijection, the per-clause attribution ((0)/(SQ)
untouched; (R)/(UB) gaining exactly (H8)/(H9)), and the (PACK-iv)
member-not-row accounting; (iii) NO REGRESSION on the VC9/VC10-confirmed
composition items (the walk, acyclicity incl. 17 ≺ 18 ≺ 18b, the §3.4 tables,
the quantifier-for-quantifier (ROOT-C) reading). Standing charge riders: the
(H9) display must nowhere read as covering the HEAD Lean wiring; no display
may read "accepted" as current status.

**Bookkeeping.** Historical §§7–15 untouched (their acceptance language is
historical record, marked superseded at §15's close and the footer). No leaf
status upgraded: D-11 stays PROVED-UNVERIFIED at its two-pass record (the
confirming pass owed); VERIFIED = 9 stands. Unit C ((NSF-KEY) execution) has
NOT run — it awaits Asvin's sign-off per (NSF-KEY-RET) R-0 (statement fence);
the O-11 pass 3 and the (K1-CANON) discharge remain owed. The commit closing
this revision is its unit of record (unit ROOT r11).

— REVISION 10 complete. The stop-the-line is CURED at composition level; the
acceptance is NOT re-earned; the counter is ZERO of 2 toward the restated bar.

---

## 17. REVISION 11 (r12, VC11 ripple-sync) — disposition of VC11, the Phase C end-to-end pass 11

**The pass.** VC11 (fresh context; `lean/notes/openmath/VC11_report.md`) ran on
the REVISION-10 text and returned **COMPOSITION-GAPS**: FOUR blockers + ONE
advisory, ALL of one genre — **r11 RIPPLE-SYNC defects** (displays the REVISION-10
edits should have carried forward but did not). The TARGETED r11 repairs
themselves were CONFIRMED: Step 17 matches the repaired M1's per-clause measures
and distinguishes solved (ROW-Σ) totals from booking margins; Step 18b is scoped
at (RE) = (PACK-iv); (PACK-iv) is a member (not a thirteenth row) with the closed
two-edge consumption set; §3.1 has twelve rows ↔ (H1)–(H9); (NSF-KEY) is
prominently KNOWN FALSE AT HEAD with gated retirement; (K1-CANON) has one home
inside (NSF-INT); (0)/(SQ) remain unconditional; 17 ≺ 18 ≺ 18b, (H7), and the
twelve-row bijection do not regress.

**Disposition table.**

| finding | grade | content | disposition | edit sites |
|---|---|---|---|---|
| VC11-1 | BLOCKER | Step 19 not updated to the REVISION-10 scope: its evaluation and (UB) summaries stopped at (H7), the final ∎ read "(H1)–(H7)" with (H5) at PACK-i/ii/iii only — the displayed closer silently upgraded (ROOT-C) past Step 18's new premises | ACCEPTED; Step 19's evaluation-chain attribution now carries (H8) = (K-CUT) (Step 18's K1 regrouping, Lemma 3.3's cut hypothesis) and (H9) = (NSF) (Step 18's K1/K4 label routing + Step 17's M3 leg) with PACK-iv at its closed edge set; the Σ_τ R_τ = 1 conditional set, the clause-(UB) scope display, and the final ∎ all read (H1)–(H9) with (H5) at PACK-i/ii/iii/iv — the closer proves the CURRENT (ROOT-C) quantifier-for-quantifier with no silent upgrade | Step 19 (four displays) |
| VC11-2 | BLOCKER | Step 16's claimed exact (UB) scope was stale: (H1)–(H7) + pre-r11 PACK content while asserting "exactly as §1 now displays" — a false claim after r11 | ACCEPTED; Step 16's (UB) scope display now carries (H8) + (H9) via the transported K1/K4 chain at q ↦ q^δ and PACK-iv in the transported evaluation chain (Step 18b direct + Step 17's (ROW-Σ) delivery, riding to q^δ) — the "exactly as §1 now displays" claim is made TRUE, not deleted | Step 16 |
| VC11-3 | BLOCKER | §3.4 Table A item 12 routed the shallow-cut content (routing + unique shallow cut + W_Ŝ regrouping) to the VERIFIED Step-2 layer while the (H8) row displays the same content as an OPEN T-6 kernel — two operative displays that cannot both be true; the table's "no new hypothesis surfaced" concluding claim likewise stale | ACCEPTED; item 12 RE-ADJUDICATED to hypothesis-grade: the cut content routes to **(H8) = (K-CUT)** (open T-6 kernel, M04 Lemma 3.3's hypothesis, consumed inside Step 18's K1 regrouping), Step 2 keeping only the SOLVE face (T-8/T-5, no cut-decomposition claim); the concluding claim corrected for (H8)/(H9) with the landing invariant restated | §3.4 Table A item 12 + verdict paragraph |
| VC11-4 | BLOCKER | the 6(iii)–(ix)+7 residue list was not closed: the row itself routes M3 "with the (H9) routing price" yet its purported CLOSED residue list omitted (H9) — a regression of the previously confirmed T-U correspondence audit | ACCEPTED; **(H9)** added to the row's residue list (the M3 route through Step 17's M3 leg, via Steps 11/12, inherits (NSF)); the list is again CLOSED under the table's own reading rule | §3.4 Table A row 6(iii)–(ix)+7 |
| VC11-5 | ADVISORY | Step 18b's Consumes: summary listed (GR-B)/(FRESH) while its own proof paragraph consumes seam (S1) at (GR-B)/(FRESH)/(ADM) — no hidden premise (the fuller local display governs) but an inconsistent consumer account | ACCEPTED; (ADM) restored to the summary line | Step 18b Consumes line |

All five repairs are **pure display syncs**: no [M] member added or changed, no
step proof content or member statement touched, no leaf pointer moved, historical
§§7–16 untouched.

**The completeness invariant, re-run.** §3.1 has **TWELVE rows ↔ (ROOT-C)'s
(H1)–(H9)**, UNCHANGED from REVISION 10: (H1) the graded-dictionary family (four
members) · (H2) = (FRESH) · (H3) = {(ADM), D-12r, (E-N)} · (H4a) = (R1)–(R4) ·
(H4b) = (UB-X) · (H5) = (PACK) (correspondence clause + row-laws
PACK-i/ii/iii/iv) · (H6) the terminal-seam group · (H7) = (I-τ)/K-LOC · (H8) =
(K-CUT) · (H9) = (NSF) (two members). Per-clause attribution, now displayed at
§1, Step 16, and Step 19 IDENTICALLY: clause (0) unconditional (Step 1 alone);
clause (SQ) unconditional (consumes NO §3.1 row); clause (R) consumes (H1)–(H3)
+ (H4a) + the (K3-δ) fragment of (H4b) + (H5) + (H6) + (H7) + (H8) + (H9);
clause (UB) consumes (H1)–(H3) + (H4a) + (H4b) + (H5) + (H6) + (H7) + (H8) +
(H9). No display anywhere reads a narrower or wider set.

**The counter and the bar.** VC11 was not clean, and REVISION 11 is a text
change: **the CONSECUTIVE-CLEAN COUNTER stays ZERO of 2.** The bar is unchanged:
two consecutive CLEAN end-to-end passes on the IDENTICAL REVISION-11 text,
fresh-context verifiers, ≥ 2 models, verifiers who saw none of the per-leaf
rounds (tree §4 Root criterion).

**VC12 charge (the next end-to-end pass).** Verify (i) the FIVE syncs above —
Step 19's closer against §1's current (ROOT-C) quantifier-for-quantifier; Step
16's (UB) scope against §1 (the "exactly as §1 now displays" claim must be
TRUE); §3.4 item 12's (H8) routing (no verified-layer contradiction remains);
the 6(iii)–(ix)+7 residue list's closure incl. (H9); Step 18b's Consumes/proof
consistency — and (ii) NO REGRESSION: the r11 targeted repairs (Step 17's
per-clause measures, Step 18b at (RE)/(PACK-iv), the twelve-row bijection,
(NSF-KEY)'s known-false-at-HEAD display, (0)/(SQ) unconditional, 17 ≺ 18 ≺ 18b)
need NOT be re-litigated but must not regress. Standing charge riders: the (H9)
display must nowhere read as covering the HEAD Lean wiring; no display may read
"accepted" as current status.

**Bookkeeping.** Historical §§7–16 untouched. NO status syncs: VERIFIED = 9
stands; D-11 stays PROVED-UNVERIFIED at its two-pass record (confirming pass
owed); the O-11 pass 3, Unit C ((NSF-KEY)), and the (K1-CANON) discharge remain
owed. The commit closing this revision is its unit of record (unit ROOT r12).

— REVISION 11 complete. Five ripple-syncs applied, pure display; the acceptance
is NOT re-earned; the counter is ZERO of 2 toward the VC12 bar.

## 18. REVISION 12 (r13, VC13 fold + global enumeration audit)

**The passes.** VC12 (fresh context; `lean/notes/openmath/VC12_report.md`) RAN on
the REVISION-11 text and returned **COMPOSITION-SOUND, ZERO findings** — all five
REVISION-11 syncs verified, no regression: the consecutive-clean counter reached
**ONE of 2**. VC13 (Fable, fresh context — the model-diverse pass;
`lean/notes/openmath/VC13_fable_report.md`) then RAN on the same REVISION-11 text
and returned **COMPOSITION-GAPS**: TWO blockers + THREE advisories, ALL the
STALE-HYPOTHESIS-ENUMERATION genre — displays enumerating or counting the
(ROOT-C) hypothesis roster that the r11/r12 sync rounds should have carried but
did not, now surviving a SECOND sync round. VC13 CONFIRMED everything else it
walked: the Steps 1–19 composition (every consumption a proved-on-record pointer;
no silent unconditional upgrade), the twelve-row §3.1 ↔ (H1)–(H9) bijection with
member statements at consumption strength, (NSF-KEY) nowhere consumed as true,
Step 17/18b's seams ((ROW-Σ) totals, (b-PF)/(b-EX) scopes, (RE) priced as
(PACK-iv) at the closed two-edge set), acyclicity incl. 17 ≺ 18 ≺ 18b, (0)/(SQ)
unconditional, and the suspension/counter status displays.

**Disposition table.**

| finding | grade | content | disposition | edit sites |
|---|---|---|---|---|
| VC13-1 | BLOCKER | §1's (ROOT-C) display of (H5) enumerated only PACK-i/ii/iii — the displayed theorem statement assumed a WEAKER member than Steps 17/18b consume, and the Step-19 closer's hypothesis content was not identical to §1's display at member granularity | ACCEPTED; §1's (H5) member display now carries **(PACK-iv)** sem_alloc with its REVISION-10 (r11) provenance tag and its closed two-edge consumption set (Step 18b DIRECT = Theorem M2's (RE) instantiation + Step 17's E1 delivery display of (ROW-Σ)) — §1's member enumeration is now IDENTICAL (groups and members) to the §3.1 (PACK) row and the Step-19 ∎ | §1 (ROOT-C) (H5) block |
| VC13-2 | BLOCKER | Step 18's δ-stable note quoted §1's clause-(R) attribution as "(H1)–(H3) + (H4a) + (H5) + (K3-δ) — §1", four groups short, and called (ROOT-C) "the joint five-hypothesis theorem" — a REVISION-4-era count | ACCEPTED; the "— §1" quote now reads the FULL current clause-(R) set ((H1)–(H3) + (H4a) + the (K3-δ) fragment of (H4b) + (H5) + (H6) + (H7) + (H8) + (H9)), the five-hypothesis count is RETIRED (the sentence no longer counts: "the theorem's hypothesis roster is unchanged by the NAMING since (K3-δ) ⊆ (H4b)") | Step 18 δ-stable interface note |
| VC13-3 | ADVISORY | Step 17's Consumes summary omitted the [M] rows its own Statement/Delivers display it consuming ((H9), (PACK-iv)) — against the post-VC11-5 convention (Consumes summaries carry consumed [M] rows) | ACCEPTED; (H9) = (NSF) (the M3 label-routing price) and (PACK-iv) ((H5), through (ROW-Σ)'s (RE) hypothesis) added to the Consumes line | Step 17 Consumes line |
| VC13-4 | ADVISORY | (H8)'s provenance label "the O-11 owed-pass G-2" was irreconcilable in-document with the §3.2 O-11 row's bare "pass 3 owed" — the row recorded no such pass | ACCEPTED, resolved HONESTLY: the 2026-08-03 owed-pass campaign DID run an O-11 hostile pass (`lean/notes/openmath/O11_owedpass_2026-08-03.md`, verdict CRITICAL, findings G-1 (n ≥ 2 scope restriction) / G-2 (CUT-WD undemonstrated — the (H8) source) / G-3 (the 65/65 rederivation claim unsupported)) which was adjudicated NOT BANKED as pass 3 (the r3 counter did not advance). The §3.2 O-11 row now RECORDS that run (un-banked, findings listed; the banked pass 3 stays owed), and all three (H8) provenance labels (§1, the §3.1 row, Step 18) point to it unambiguously | §3.2 O-11 row; (H8) labels at §1/§3.1/Step 18 |
| VC13-5 | ADVISORY | the (AGR) inherited-[V] row's residue claim "nothing else" elided CU-4's displayed §3.1 [M] conditionalities (the D-12r TB-CAP (τ-irr) perimeter; (E-N)'s CU-4 touch) | ACCEPTED; the residue sentence now names both (as (H3) members riding their own §3.1 rows — nothing hidden before, but the enumeration was imprecise as self-contained) | §3.2 (AGR) row |

**THE GLOBAL ENUMERATION AUDIT (the class kill).** The VC13 genre — a
hypothesis-group/member enumeration or count somewhere in the operative text,
stale against §1 — has now produced findings at THREE consecutive gapped passes
(VC11, VC13, plus the r11 originals). This revision therefore audited EVERY
display in the OPERATIVE sections (§§0–6 + §3.4; historical §§7–17 excluded by
design) that enumerates hypothesis groups or members or counts them (grep
patterns: "(H1)–(H", "PACK-i", "five-hypothesis", "hypothesis group", "— §1",
"per-clause", "incl. PACK", "twelve", plus a manual walk of §§0/1, Steps 16–19,
and §§3.1/3.2/3.4). **Result: 36 displays checked; 10 fixed** — 8 finding-driven
(the table's edit sites: §1's (H5) display; §1's, the §3.1 row's, and Step 18's
(H8) provenance labels; Step 17's Consumes; Step 18's δ-stable note; the §3.2
O-11 row; the §3.2 (AGR) row) plus 2 audit-driven syncs of the
same class caught by the sweep: (i) the §0 headline's "What exists is" §3.1-row
enumeration (stopped at (H7)/PACK-i-iii since the acceptance era; run to the
twelve-row scope with (H8)/(H9) and (PACK-iv)); (ii) Step 18's K2 (H5) row-law
display (now states the FOUR-member roster and that this step consumes exactly
PACK-i/ii/iii, with PACK-iv's closed two-edge set displayed — previously a
three-law enumeration a reader could take as (H5)'s roster). The sweep also
disambiguated the two "owed-pass C6" pointers (Step 18b + the §3.1 (PACK) row)
to the D-11 owed pass by name — the VC13-4 ambiguity genre, fixed before it
could be found (pointer fixes, outside the 36-display enumeration count). The 26
checked-clean displays: §0's suspension, REVISION-11, and (new) REVISION-12
paragraphs (3); §1's clause-(R)/(UB)/(SQ) attributions, the (H1) four-member
display, and the (ROOT) = (ROOT-C) + discharge closer (5); Step 11's A-4 homes
note (1);
Step 16's (UB) scope ("exactly as §1 now displays" — TRUE) (1); Step 17's
Statement (1);
Step 18's (H9) and K3-a/fence displays (2); Step 18b's proof + Consumes (1);
Step 19's
four attribution displays incl. the ∎ (4); §3.1's (PACK) row (four members) (1);
§3.2's D-11 scope note (1); §3.3's widened-charge row (its
PACK-i/ii/iii enumeration is item-(v)-scoped, correctly THREE laws — (PACK-iv)
is not an O-11 widened-charge item but the D-11 r4 (RE) profile) (1); §3.4's item
6(iii)–(ix)+7 residue list (closed: its named routes inherit neither (H7) nor
(H8)), item 12, item 14, Table B's (H7)/cl6 rows, and the corrected verdict
paragraph (5). **The claim
VC14 will test: every operative hypothesis-enumeration display now matches §1's
current display** (groups (H1)–(H9); (H5) members i/ii/iii/iv; (H3) =
{(ADM), D-12r, (E-N)}; (0)/(SQ) unconditional; clause-(R)/(UB) attributions as
displayed at §1/Step 16/Step 19).

All repairs are **pure display syncs**: no [M] member statement changed, no step
proof content touched, no leaf pointer moved, historical §§7–17 untouched. NO
status syncs: VERIFIED = 9 stands; the D-11 confirming pass, the banked O-11
pass 3, Unit C ((NSF-KEY)), and the (K1-CANON) discharge remain owed.

**The completeness invariant, re-run.** §3.1 has **TWELVE rows ↔ (ROOT-C)'s
(H1)–(H9)**, UNCHANGED from REVISION 10: (H1) the graded-dictionary family (four
members) · (H2) = (FRESH) · (H3) = {(ADM), D-12r, (E-N)} · (H4a) = (R1)–(R4) ·
(H4b) = (UB-X) · (H5) = (PACK) (correspondence clause + row-laws
PACK-i/ii/iii/iv) · (H6) the terminal-seam group · (H7) = (I-τ)/K-LOC · (H8) =
(K-CUT) · (H9) = (NSF) (two members). Per-clause attribution, displayed
IDENTICALLY at §1, Step 16, Step 18's δ-stable note (since this revision), and
Step 19: clause (0) unconditional (Step 1 alone); clause (SQ) unconditional
(consumes NO §3.1 row); clause (R) consumes (H1)–(H3) + (H4a) + the (K3-δ)
fragment of (H4b) + (H5) + (H6) + (H7) + (H8) + (H9); clause (UB) consumes
(H1)–(H3) + (H4a) + (H4b) + (H5) + (H6) + (H7) + (H8) + (H9). No operative
display reads a narrower or wider set, at group OR member granularity.

**The counter and the bar.** VC12 was clean (ONE of 2); VC13 was not, and
REVISION 12 is a text change: **the CONSECUTIVE-CLEAN COUNTER RESETS to ZERO
of 2.** The bar is unchanged: two consecutive CLEAN end-to-end passes on the
IDENTICAL REVISION-12 text, fresh-context verifiers, ≥ 2 models, verifiers who
saw none of the per-leaf rounds (tree §4 Root criterion).

**VC14 charge (the next end-to-end pass).** Verify (i) the FIVE VC13 repairs —
§1's (H5) display against the §3.1 row and the Step-19 ∎ (member-for-member
identity); Step 18's "— §1" quote against §1's clause-(R) attribution (the quote
must be TRUE, with no hypothesis count); Step 17's Consumes against its own
Statement/Delivers; the §3.2 O-11 row's record of the un-banked 2026-08-03 pass
against the (H8) labels (provenance reconstructible from the four corners); the
(AGR) residue sentence against Step 11's CU-4 display and the (E-N) row — and
(ii) THE AUDIT'S CLAIM: hunt for ANY operative (§§0–6 + §3.4) display
enumerating or counting hypothesis groups/members that mismatches §1's current
display — the class is claimed DEAD; one counterexample is a blocker. (iii) NO
REGRESSION on the VC12/VC13-confirmed items (the walk, the twelve-row bijection,
the seams, acyclicity, (0)/(SQ) unconditional, (NSF-KEY) discipline). Standing
charge riders: the (H9) display must nowhere read as covering the HEAD Lean
wiring; no display may read "accepted" as current status.

**Bookkeeping.** Historical §§7–17 untouched. The commit closing this revision
is its unit of record (unit ROOT r13).

— REVISION 12 complete. Five findings folded + the enumeration class killed by
global audit (36 displays checked, 10 fixed); the acceptance is NOT re-earned;
the counter is ZERO of 2 toward the VC14 bar.

## 19. REVISION 13 (r14, post-acceptance batch) — RE-ACCEPTANCE record + VC15's advisories + the discharge syncs

**The passes and the re-acceptance.** VC14 (Codex, fresh context;
`lean/notes/openmath/VC14_report.md`) RAN on the REVISION-12 text and returned
**COMPOSITION-SOUND, clean** — the §18 enumeration-audit claim TESTED and held,
zero regressions: counter ONE of 2. VC15 (Fable, fresh context — the
model-diverse, deciding pass; `lean/notes/openmath/VC15_fable_report.md`) RAN on
the same REVISION-12 text and returned **COMPOSITION-SOUND, ZERO blockers** with
an explicit null result on the blocker hunt (no unhoused consumption, no weak
member, no silent upgrade, no known-false-consumed-as-true, no attribution
mismatch at member granularity, acyclicity intact incl. 17 ≺ 18 ≺ 18b; the n = 2
anchor triple re-verified as the true quadratic densities) and FOUR advisories
(display hygiene only). The tree §4 Root criterion is MET: **the composition is
RE-ACCEPTED at (ROOT-C)'s twelve-row displayed conditional scope** (ledger entry
"RE-ACCEPTANCE: THE BAR IS MET AT TWELVE ROWS", 2026-08-05). Acceptance certifies
the composition, never the discharge of §3's ledger: the theorem as proved
remains the conditional (ROOT-C).

**This revision (unit ROOT r14).** The post-acceptance hygiene + discharge-sync
batch, per the r10/REVISION-9 precedent (acceptance earned on the pre-batch text;
every edit recorded below; the counter statement updated honestly at the close).
Also executed here: D12R-U4 (routed to this batch after the division workflow's
safety classifier correctly BLOCKED its dispatch through a generic prover
template — doc-edit units now auto-route to the revision workflows).

**Disposition table.**

| item | source (finding traced) | grade | disposition | edit sites |
|---|---|---|---|---|
| r14-1 | VC15 advisory 1 (consumption-summary sync — the VC11-5/VC13-3 convention genre) | ADVISORY | ACCEPTED; Step 18's Consumes line gains **Step 2** with the two direct edges displayed (K3 ← T-7s on the step's own K3 assembly line; K3-a ← T-5's H-DOMAINS package), the not-through-5/17 clarification, the open-residue pricing pointer ((H1)/(H6)(vii) on the displayed K3/K3-a lines), and the acyclicity note (2 ≺ 18) | Step 18 Consumes line |
| r14-2 | VC15 advisory 2 (grade-display conformance — the VC10-advisory-1/O-14a′ precedent) | ADVISORY | ACCEPTED at the honest form: NO faithfulness-audit record is on file for `MovesU/RegPFinite.lean` (checked: the M17 note's dual provenance + honesty ledger exist; a Lean-statement-vs-mathematical-statement audit does not) — Step 4's grade DROPS from "PROVED + VERIFIED-LEAN" to "PROVED (dual provenance) + machine-checked Lean-core; VERIFIED-LEAN PENDING", with the NEW named §3.2 row **M17/RegPFinite faithfulness-audit record** (the K-12 genre); impact contained by Step 5's independent emptiness result | Step 4 status; NEW §3.2 row |
| r14-3 | VC15 advisory 3 (flag billing unsynced with the recorded G-3 finding) | ADVISORY | ACCEPTED; the G-3 scope caveat (the "65/65 end-to-end rederivation" reading unsupported beyond the FIVE displayed identities — the §3.2 O-11 row's un-banked 2026-08-03 pass) now RIDES all three RESUM-n3 billings: §1's verified-instances display, the §5 RESUM-n3 row (incl. its "what it corroborates" column), and the (PACK) row's coverage sentence. Numerics stay flags-never-substitutes; nothing compositional rested on the reading | §1; §5 row; §3.1 (PACK) row |
| r14-4 | VC15 advisory 4 ((PACK-iv) terminology tension + the ℛ ⊂ ℚ(q)-vs-ℚ(t) display nit) | ADVISORY | ACCEPTED; (a) the (PACK) row gains the one-sentence reconciliation: "consumed at" prices the closed EDGE SET while Step 17's Delivers speaks of PROOF-LINE consumption relative to (EDGE-RES) — the E1 edge is a delivery display priced conservatively; both true together, over-pricing never under-pricing; (b) the q/t variable convention declared at ℛ's definition (t = q, ONE formal variable; ℚ(t) = ℚ(q)) | §3.1 (PACK) row; §1 ℛ definition |
| r14-5 | (NSF-KEY) discharge — ledger "UNIT C EXECUTED" (commit 322ec54, swept with dfdb558) + "H9 LEAD ADJUDICATION"; edit spec HDISCHARGE_H9.md §1.4 (E-r14-1..6) | STATUS SYNC (the one sanctioned retirement route, executed) | **(NSF-KEY) RETIRED** on ground (a), the six-artifact (NSF-KEY-RET) iff — R-0 Asvin sign-off (ledger verbatim) · R-1 the verbatim `MovesT.NsFree` conjunct diff at HEAD (`BridgeD3_treeCarrier.lean:94–96`) + closed ripple (`UNITC_ripple.md`) · R-2 statement texts byte-identical · R-3 the three validations (count_tie sorry-free at the guarded corpus/hdet scope; `bridgeCanonical_stable` green; V₀ DEAD = `bridgeVt_nsFree`) · R-4 build green (8579 jobs) + AxChk 740 footprints/0 sorryAx, no regression · R-5 Codex diff CONFIRMED (`UNITC_diff_confirm.md`) — with the compiled-proposition witness **H9-U1** (`Scaffold/HDischarge/H9/NsfKeyProp.lean`, landed green, Lean-core; corroboration, not a second route). The member's KNOWN-FALSE record re-labeled RETIREMENT PROVENANCE (historical); the guarded carrier displayed verbatim; the "must NEVER be displayed as covering HEAD wiring" rider DISCHARGED and replaced by "not dischargeable while (NSF-INT) stands priced". **(H9) is NOT closed**: the open member is (NSF-INT), incl. its (K1-CANON) premise (discharge owed; after it, (NSF-INT) retires on I-1+I-2+I-3, all on file, at the spec'd r15). Bounds carried: no carrier non-vacuity claim; the vt_surj/BridgeD16 completeness residue stands | §1 (H9) block + closer; §3.1 (H9) row (four sub-edits); Step 17 M3 leg; Step 18 K1 + K4 displays; §3.2 D-11 row checked (no leak) |
| r14-6 | D-12r row rewrite — D12R-U4 (HDISCHARGE_H3 §2.3), preconditions verified ON FILE: D12R-U1's (HL-a) CONFIRMED (Neukirch II §4 (4.6), p. 129) / (HL-b) DISPOSED (printed clause absent; companion lemma displayed) / (HL-c) CONFIRMED (Serre LF I §6 Prop 15, pp. 17–18) + D12R-U3's TAMS pagination PASSED (GMN Cor 3.8 + Def 3.9, pp. 374/398–400) | STATUS SYNC (VC-gated) | EXECUTED at the VC-gated wording: the row now displays the dual-accepted reduction quote ("D4R.0-K(c) … what remains is exactly {VP-SOUND's cites}" = (HEN-LIFT) + (OM-SAT)), the per-part confirmed pins, the TB-CAP(irr) precise sentence ("PROVED at [3t] from the accepted §C rev-14 Theorem C — citation into the dual-accepted corpus, not an open hypothesis"), and the §2.1 dependency-decomposition routing table (each sibling condition at its own priced home — (H7), drainage, (UB-X)(a), (H1)/O-2, P(O5t r3)). The reclassification [M] → §4 literature class RIDES VC16, which adjudicates the rewritten row's faithfulness; until then the row STANDS in §3.1 — the twelve-row roster is UNCHANGED by this revision | §3.1 D-12r row |
| r14-7 | (H8) = (K-CUT) row re-scope — H8-R2's drafted spec (`Scaffold/HDischarge/H8/R2RootSync.lean` §§A–D), firing conditions VERIFIED there (waves 0–2 green + H8-R1 on file; B3/C1 landed beyond) | STATUS SYNC (no discharge claimed) | EXECUTED: the row's status is **REDUCED, NOT discharged** — the landed-development pointer (8 Lean units + 2 documentary, green, Lean-core: ownership partition killing the G-2 falsifier channels, level-(1b) forward extraction + injectivity, count/series regroups incl. M04 Lemma 3.3 as `seriesSum_cut_conv`, the conditional consumer bridge `kcut_reduce`) with the remaining content TYPED as `kcut_reduce`'s binder rows ((K-CUT-s)/(K-CUT-g)/(K-CUT-m) + realization/verdict-keying, owners displayed); the retirement route ADJUDICATED at H8-R1: charge **(vi-a) FAIL ⇒ NO retirement** (pre-registered rule; serving map not re-pointable); the HONESTY GATE displayed — no unit-level hostile pass has run on the fan-out; **discharge awaits that hostile pass** (the ledger's "ratification-away" gate) | §3.1 (H8) row (status + owner columns) |
| r14-8 | H4's ROOT-sync briefs (`lean/notes/openmath/H4M7_rootsync_brief.md` §§2–4) | STILL-OWED (preconditions unmet — recorded, NOT executed) | The unit's own §0 verdict: NONE of the three briefs is fire-ready — F-(i) unmet (H4-M1 passes ×2 UNRUN; H4-M2 pass ×1 no record), M4's pass-2 verdict ABSENT from its file, M5's second consecutive clean owed, N-K3 NOT RUN against the sealed predictions, Asvin sign-off on the Route-B statement change not requested. NO (H4a)/(K3-δ)/(UB-X) display changes in this batch; the briefs' drafted deltas remain queued behind their displayed firing conditions F-(i)/F-(iii)/F-(iv)/F-B | none (recorded here) |
| r14-9 | EN-U5 regularization — the (E-N) row was rewritten mid-campaign (commit 89176a4, "WIP sweep") WITHOUT a ledgered revision record | PROCESS ADOPTION | The rewrite is hereby formally ADOPTED. What changed (the 89176a4 diff, on record): the row gained the EN-U1 consumption walk (RESCOPE-CLEAN, `H3_en_walk.md` — the 12-site table; every mandatory-path finite-N object thr-scoped by displayed definition/hypothesis) and the EN-U2 forced absorption verification (gate G-EN-1 = HIT: 11,177 thr > N decided boxes at 19 configs, all draining at Step 13's displayed price; route (a) permanently fenced; the caveat NON-VACUOUS at 77/420 diverging boxes) — the consumption display itself UNCHANGED; the caveat stays TRUE and OPEN, now witness-backed. Evidence: `lean/notes/openmath/H3_en_walk.md` + `verification/h3_en_falsifier.py` logs/results. THE PROCESS GAP, noted: a §3.1 row edit landed in a WIP sweep without a revision record — against this document's own change discipline; the division workflow's doc-guard (auto-BLOCK of doc-edit units, the D12R-U4 catch) now prevents recurrence. Content verdict: evidence-backed, honesty-increasing (the row prices LESS ambiguously than before), consistent with the REVISION-12 twelve-row invariant — ADOPTED as displayed | none (adoption of the standing text; record here) |

**The completeness invariant, re-run.** §3.1 has **TWELVE rows ↔ (ROOT-C)'s
(H1)–(H9)**, UNCHANGED by this revision: (H1) the graded-dictionary family (four
members) · (H2) = (FRESH) · (H3) = {(ADM), D-12r, (E-N)} (D-12r rewritten,
reclassification VC-gated, STANDING here) · (H4a) = (R1)–(R4) · (H4b) = (UB-X) ·
(H5) = (PACK) (correspondence + row-laws PACK-i/ii/iii/iv) · (H6) the
terminal-seam group · (H7) = (I-τ)/K-LOC · (H8) = (K-CUT) (REDUCED, open) ·
(H9) = (NSF) — **membership now (NSF-INT)-only-open** ((NSF-KEY) RETIRED at the
executed Unit C; (NSF-INT) stands priced with its (K1-CANON) premise). Per-clause
attribution unchanged and displayed identically at §1, Step 16, Step 18's
δ-stable note, and Step 19: (0) unconditional; (SQ) unconditional; clause (R)
consumes (H1)–(H3) + (H4a) + the (K3-δ) fragment of (H4b) + (H5) + (H6) + (H7) +
(H8) + (H9); clause (UB) consumes (H1)–(H3) + (H4a) + (H4b) + (H5)–(H9). No
step proof content, no leaf pointer, and no clause attribution changed in this
batch; the falsifier ran: `grep -n "KNOWN FALSE AT HEAD"` now hits ONLY
historical/revision-record sections and the (H9) row's re-labeled RETIREMENT
PROVENANCE clause — no operative display.

**The honest acceptance statement (the counter).** The acceptance was EARNED on
the REVISION-12 text: VC14 + VC15, two consecutive clean model-diverse passes,
fresh verifiers — the bar met at the twelve-row scope; nothing in this batch
withdraws it. But acceptance attaches to TEXT, and REVISION 13 is a text change
(advisory repairs + status syncs, incl. two §3.1 row rewrites): **a confirming
end-to-end pass (VC16) is OWED to re-anchor the accepted text on the REVISION-13
wording.** Until VC16 lands clean, the acceptance citation points at the
REVISION-12 text; a VC16 blocker would re-open the composition question in full
(the standing suspension discipline). [Superseded event, recorded at REVISION 14:
VC16 RAN CLEAN — the acceptance re-anchored on this REVISION-13 text; see §20.]

**VC16 charge (the confirming pass).** Verify (i) the four VC15 advisory repairs
(Step 18's Consumes against its own K3/K3-a lines; Step 4's grade against §0's
definition + the new §3.2 row; the three G-3 caveats against the §3.2 O-11 row;
the (PACK-iv) reconciliation sentence against Step 17's Delivers + the q/t
convention); (ii) the (H9) row sync against the six named Unit C artifacts and
the H9-U1 module (the retirement must trace to Unit C per (NSF-KEY-RET), never to
leaf text; the (NSF-INT)/(K1-CANON) residue must stay priced); (iii) the
REWRITTEN D-12r row's faithfulness (this is D12R-U4's own gate: the reduction
quote against MOVES §T.5(2)/§T.2, the pins against the briefs, the
dependency-decomposition routing against the named ledger homes) — a clean VC16
RATIFIES the row's reclassification to the §4 literature class at the NEXT
revision; (iv) the (H8) re-scope against `R2RootSync.lean` §§C–D (the
"REDUCED, not discharged" line, the (vi-a) FAIL disposition, the honesty gate);
(v) NO REGRESSION on the VC14/VC15-confirmed composition (walk, twelve-row
bijection, seams, acyclicity, (0)/(SQ) unconditional). Standing riders: no
display may claim (H9) discharged while (NSF-INT) stands priced; the acceptance
statement must read exactly as this revision displays it.

**Bookkeeping.** Historical §§7–18 untouched. `docs/PROJECT_STATE.md` synced
(E-r14-6). The commit closing this revision is its unit of record (unit ROOT
r14).

— REVISION 13 complete. Four advisories folded + four discharge/status syncs
(one retirement: (NSF-KEY); two VC-gated row rewrites: D-12r, (H8); one
adoption: (E-N)) + one still-owed record (H4 ROOT-sync); the acceptance stands
as earned on REVISION 12; VC16 is owed to re-anchor it on this text.

## 20. REVISION 14 (r15, the D-12r move) — VC16 record + the [M] → §4 reclassification executed

**The pass.** VC16 (the confirming end-to-end pass, fresh context;
`lean/notes/openmath/VC16_report.md`) RAN on the REVISION-13 text and returned
**COMPOSITION-SOUND** — zero blockers, advisories only: (1) Step 4's grade is
directionally correct but not §0-vocabulary-exact (its exact grade reads
PROVED-UNVERIFIED until the M17/RegPFinite audit lands; composition unaffected —
Step 5 independently supplies the all-prime specialization); (2) the D-12r
reclassification is **WARRANTED** on the displayed evidence (the dual-accepted
reduction confines the residue to (HEN-LIFT)/(OM-SAT); the Hensel uniqueness gap
handled by the companion lemma; TB-CAP(irr) supplied by the accepted [3t]
corpus; all sibling dependencies decomposed to existing homes — "no additional
D-12r-specific open mathematics remains that must stay [M]"), with the §4
roster synchronization named as the move's precondition; (3)–(7) the (NSF-KEY)
retirement, the K-CUT re-scope, the other REVISION-13 edits, the structural
invariants (twelve rows, acyclicity, (0)/(SQ) unconditional), and
no-REVISION-12-regression all PASS. **The acceptance RE-ANCHORS on the
REVISION-13 text** (the §19 charge met).

**This revision (unit ROOT r15).** Executes exactly the VC16-warranted move +
finding 2's three roster-sync items, honestly graded. No step proof content, no
leaf pointer, no clause attribution changed.

**Disposition table.**

| item | source | grade | disposition | edit sites |
|---|---|---|---|---|
| r15-1 | VC16 finding 2 (the adjudication) + the §19 r14-6 VC-gate ("a clean VC16 RATIFIES the row's reclassification to the §4 literature class at the NEXT revision") | STATUS SYNC (the gated move, executed) | **D-12r MOVED §3.1 [M] → §4 (entry 10)**: the row deleted from §3.1 (tombstone paragraph kept for the one-home trail); the §4 entry carries the dual-accepted reduction quote, the per-part confirmed pins ((HL-a) Neukirch II §4 (4.6) p. 129; (HL-b) companion-lemma disposition; (HL-c) Serre LF I §6 Prop 15 pp. 17–18; (OM-SAT) riding §4 item 1 by id at the TAMS-confirmed Cor 3.8 + Def 3.9 with the O-2 rider), the TB-CAP(irr) precise sentence, the consumption enumeration (Steps 2/3/11/12/19 + Step 16/clause (UB)) and the FULL dependency-decomposition routing table — each sibling dependency at its existing home, verbatim from the r14 row | §3.1 (row out + tombstone); §4 entry 10 |
| r15-2 | VC16 finding 2 sync item (a) — "§4 item 1 still says the GMN pagination confirmation is owed" | ROSTER SYNC (honest partial) | §4 item 1's status updated to the on-file state: pagination **CONFIRMED for the (OM-SAT) slice ONLY** (unit D12R-U3, `lean/notes/openmath/H3_omsat_brief.md` §1 — six loci verbatim at printed pp. 374/398–399/399/399/400/366, no arXiv-v2 drift); NOT run to full CONFIRMED — the REMAINING pinned loci still owe the LIT-1/GD-7 session (the §3.3 LIT-1 row now records the partial discharge and holds the brief's located-page map as session input) | §4 item 1; §3.3 LIT-1 row |
| r15-3 | VC16 finding 2 sync item (b) — "§4 item 5 still calls the Neukirch pin a candidate with its probe owed" | ROSTER SYNC (honest state) | §4 item 5 updated: numbers **CONFIRMED** ((4.6) "Hensel's Lemma", printed p. 129 — unit D12R-U1, `lean/notes/openmath/H3_henlift_brief.md`); the Mathlib probe **RAN, answer ABSENT** (`HenselianLocalRing` root-lifting only; no coprime-factor lifting) — the pin STANDS AS A PAPER CITE; the printed (4.6) is existence-only, uniqueness disposed via the brief's §2 companion lemma; the optional Lean materialization (D12R-U2a/b/c) owes its own axiom-policy loop when it lands. §3.3 LIT-3 row synced to the same state | §4 item 5; §3.3 LIT-3 row |
| r15-4 | VC16 finding 2 sync item (c) — "Serre LF I §6 Prop. 15 is not presently enumerated as a consumed locus" | ROSTER SYNC | **Serre LF I §6 Prop 15 (printed pp. 17–18) ADDED** to §4 item 4's consumed-loci enumeration, tagged as the (HL-c) read-off of the D-12r pin, with the entry-10 cross-reference | §4 item 4 |
| r15-5 | the VC13-lesson enumeration audit (this revision's ripple) | RIPPLE SYNC | Every operative (H1)–(H9)/(H3)-membership/row-count display checked and synced: §1's (ROOT-C) (H3) display shrunk to {(ADM), (E-N)} with the reclassification bracket (grouping preserved); §1's clause-(UB) B1 attribution parenthetical re-pointed; headline enumeration run to ELEVEN; Steps 2, 3, 11 (the A-4 reconciling display), 16, and 19 re-pointed to "perimeter-conditional on the §4 D-12r pin"; the §3.1 (E-N) row's leg-C3 mention, the §3.2 (AGR) row, §3.4 Table A (the 6(iii)–(ix)+7 residue list and row 10's (τ-irr) pointer), Step 18's O-11 r3 tier record (its H-2 = D-12r naming bracketed to the §4 home), the §3.2 pass-ledger row, §6, and the footer synced. AUDIT COUNT: 19 displays checked — 16 EDITED (header roster enumeration + headline count note; §1's (H3) display + the clause-(UB) B1 parenthetical; Steps 2, 3, 11 (A-4 display), 16, 19; Step 18's tier record; §3.1 row-out/tombstone + the (E-N) leg-C3 mention; §3.2 (AGR) + pass-ledger rows; §3.4 residue list + row 10) and 3 CONFIRMED already-conformant (Step 12's text names no D-12r directly — its consumption is priced at the §4 entry's enumeration [this confirmation was ADJUDICATED INSUFFICIENT by VC17 blocker 1 against the stronger every-site-re-pointed claim — RESOLVED at REVISION 15, r16: Step 12's (τ-irr) description now carries the local "perimeter-conditional on the §4 D-12r pin (entry 10)" sentence]; §1's (ROOT)-discharge sentence and Step 19's final ∎ name hypothesis GROUPS only, membership displayed at §1's (H3) bracket); the §4/§3.3 roster-sync edits counted at r15-1..r15-4; the historical §§7–19 records untouched by design | §§0/1, Steps 2/3/11/16/18/19, §3.1/§3.2/§3.4/§4/§6, footer |

**The completeness invariant, re-run.** §3.1 has **ELEVEN rows ↔ (ROOT-C)'s
(H1)–(H9)**: (H1) the graded-dictionary family (four members) · (H2) = (FRESH) ·
**(H3) = {(ADM), (E-N)} (TWO members — D-12r out to §4 at this revision)** ·
(H4a) = (R1)–(R4) · (H4b) = (UB-X) · (H5) = (PACK) (correspondence + row-laws
PACK-i/ii/iii/iv) · (H6) the terminal-seam group · (H7) = (I-τ)/K-LOC ·
(H8) = (K-CUT) (REDUCED, open) · (H9) = (NSF) ((NSF-INT)-only-open). Per-clause
attribution UNCHANGED IN FORM and displayed identically at §1, Step 16, Step
18's δ-stable note, and Step 19: (0) unconditional; (SQ) unconditional; clause
(R) consumes (H1)–(H3) + (H4a) + the (K3-δ) fragment of (H4b) + (H5)–(H9);
clause (UB) consumes (H1)–(H3) + (H4a) + (H4b) + (H5)–(H9) — with (H3) now the
two-member group, and the former D-12r conditionality carried as
LITERATURE-grade at the §4 pin (consuming steps say "perimeter-conditional on
the §4 D-12r pin"; nothing evaporated — the perimeter is priced, in the goal's
conditionality class instead of the hypothesis ledger). The falsifier ran:
`grep -n "D-12r"` hits in operative sections are now the §4 entry, the §3.1
tombstone, and re-pointed conditionality language only — no operative display
names a §3.1 D-12r row.

**The honest acceptance statement (the counter).** The acceptance was EARNED on
the REVISION-12 text (VC14 + VC15, two consecutive clean model-diverse passes,
fresh verifiers) and RE-ANCHORED on the REVISION-13 text by VC16's clean
confirming pass. REVISION 14 executes a VC16-WARRANTED reclassification — the
verifier adjudicated the move's substance before it was made — but it IS a text
change: **a confirming end-to-end pass (VC17) is OWED to re-anchor the accepted
text on the REVISION-14 wording.** Until VC17 lands clean, the acceptance
citation points at the REVISION-13 text; a VC17 blocker would re-open the
composition question in full (the standing suspension discipline).

**VC17 charge (the confirming pass).** Verify (i) the D-12r move's faithfulness:
the §4 entry 10 against the §19 r14-6 record and the HDISCHARGE_H3 §2.3 spec
(reduction quote, pins, routing table — nothing dropped, nothing strengthened);
(ii) the ELEVEN-row ↔ (H1)–(H9) bijection with (H3) at two members, and that
every operative enumeration display matches §1 (the r15-5 audit's claim);
(iii) the three roster syncs against the briefs (item 1 partial-only — the
LIT-1 session must still show owed; item 5's probe-ABSENT wording; item 4's
Prop 15 addition); (iv) that no consuming step lost its TB-CAP perimeter
pricing (Steps 2/3/11/12/16/19 all display the §4-pin conditionality);
(v) NO REGRESSION on the VC16-confirmed composition (walk, seams, acyclicity,
(0)/(SQ) unconditional, the (H9)/(H8)/(E-N) states). Standing riders: no
display may claim the D-12r perimeter discharged — it is literature-conditional
at the §4 pins, which is a conditionality class, not a discharge; the
acceptance statement must read exactly as this revision displays it.

**Bookkeeping.** Historical §§7–19 untouched. `docs/PROJECT_STATE.md` sync owed
at the next state-file touch. The commit closing this revision is its unit of
record (unit ROOT r15).

— REVISION 14 complete. One warranted reclassification executed (D-12r [M] →
§4, ELEVEN rows) + three honest roster syncs + the enumeration-audit ripple;
the acceptance stands as earned on REVISION 12 and re-anchored on REVISION 13;
VC17 is owed to re-anchor it on this text.

## 21. REVISION 15 (r16, VC17 fold) — the two display-conformance repairs at the r15 edits

**The pass.** VC17 (the confirming end-to-end pass, fresh context;
`lean/notes/openmath/VC17_report.md`) RAN on the REVISION-14 text and returned
**COMPOSITION-GAPS** — ONE blocker + ONE advisory, both display-conformance
findings at the r15 edits themselves. All other charged checks PASS: the D-12r
move's faithfulness (residue confined to (HEN-LIFT)/(OM-SAT), the uniqueness
companion lemma explicit, sibling debts decomposed at their prior homes); §3.1
at exactly ELEVEN rows ↔ (H1)–(H9) with (H3) = {(ADM), (E-N)}; the operative
clause attributions ((0)/(SQ) unconditional; (R)/(UB) carrying the same groups
at §1 and Steps 16/18/19); the remaining twelve-row/former-(H3) enumerations
confined to dated historical records; the walk conditional at its stated scope
with no unconditional (ROOT) upgrade; acyclicity intact incl. 17 ≺ 18 ≺ 18b;
no REVISION-12/13 regression.

**This revision (unit ROOT r16).** Repairs both findings in place — pure
display syncs. No [M] member, step proof content, leaf pointer, or clause
attribution changed; NO status syncs.

**Disposition table.**

| item | source | grade | disposition | edit sites |
|---|---|---|---|---|
| r16-1 | VC17 blocker 1 (Step 12 not locally re-pointed at the D-12r move) | BLOCKER — repaired in place | Step 12's `(τ-irr)` saturation description now carries the honest conditionality sentence locally: "the (τ-irr) leg's TB-CAP dependence is perimeter-conditional on the §4 D-12r pin (entry 10)" — the language Steps 2/3/11/16/19 received at r15. The r15-5 audit had passed Step 12 as already-conformant on the central-§4-enumeration ground alone; VC17 adjudicated that INSUFFICIENT against the stronger every-former-consumption-site-re-pointed claim — the §20 r15-5 acknowledgment ("Step 12's text names no D-12r directly") is KEPT and marked RESOLVED in place | Step 12 statement; §20 r15-5 row (resolution bracket) |
| r16-2 | VC17 advisory 2 (Step 4's grade outside the §0 vocabulary — completing VC16 finding 1 / the REVISION-13 VC15-advisory-2 conformance) | ADVISORY — repaired in place | Step 4's grade rendered in the EXACT §0 word: **PROVED-UNVERIFIED**, with the named §3.2 row **M17/RegPFinite faithfulness-audit record** displayed as the pending upgrade condition (VERIFIED-LEAN on its landing); the machine-checked-Lean-core fact (dual provenance, `MovesU/RegPFinite.lean`, 14 theorems) and the Step-5 independent-route containment stay as displayed NOTES, not grade words — no silent mathematical upgrade or downgrade; the §3.2 row's own grade sentence synced | Step 4 status; §3.2 M17/RegPFinite row |

**The completeness invariant, re-run.** §3.1 has **ELEVEN rows ↔ (ROOT-C)'s
(H1)–(H9)**, UNCHANGED from REVISION 14: (H1) the graded-dictionary family
(four members) · (H2) = (FRESH) · (H3) = {(ADM), (E-N)} (TWO members) ·
(H4a) = (R1)–(R4) · (H4b) = (UB-X) · (H5) = (PACK) (PACK-i/ii/iii/iv) ·
(H6) the terminal-seam group · (H7) = (I-τ)/K-LOC · (H8) = (K-CUT) (REDUCED,
open) · (H9) = (NSF) ((NSF-INT)-only-open). Per-clause attribution UNCHANGED
and displayed identically at §1, Step 16, Step 18's δ-stable note, and Step 19:
(0) unconditional; (SQ) unconditional; clause (R) consumes (H1)–(H3) + (H4a) +
the (K3-δ) fragment of (H4b) + (H5)–(H9); clause (UB) consumes (H1)–(H3) +
(H4a) + (H4b) + (H5)–(H9) — the former D-12r conditionality carried as
LITERATURE-grade at the §4 pin, now displayed locally at EVERY former
consumption site (Steps 2/3/11/12/16/19 — Step 12 closed at r16-1). The
falsifier re-ran: `grep -n "D-12r"` hits in operative sections are the §4
entry, the §3.1 tombstone, and re-pointed conditionality language only.

**The honest acceptance statement (the counter).** The acceptance was EARNED
on the REVISION-12 text (VC14 + VC15, two consecutive clean model-diverse
passes, fresh verifiers) and RE-ANCHORED on the REVISION-13 text by VC16's
clean confirming pass. VC17 was the confirming pass owed on REVISION 14; it
returned COMPOSITION-GAPS, so it does NOT re-anchor — and REVISION 15, which
repairs its two findings, IS a text change: **the confirming end-to-end pass
(VC18) is OWED to re-anchor the accepted text on the REVISION-15 wording.**
Until VC18 lands clean, the acceptance citation points at the REVISION-13
text; a VC18 blocker would re-open the composition question in full (the
standing suspension discipline).

**VC18 charge (the confirming pass).** Verify (i) the two r16 repairs: Step 12
displays the local "perimeter-conditional on the §4 D-12r pin (entry 10)"
sentence at its (τ-irr) description, and Step 4's grade word is exactly
PROVED-UNVERIFIED with the audit row named and the Lean-core/Step-5 facts as
notes only; (ii) EVERY former D-12r consumption site (Steps 2/3/11/12/16/19)
now displays the §4-pin conditionality locally — the claim VC17 refuted at
Step 12; (iii) the §20 r15-5 resolution bracket is honest (acknowledgment
kept, marked resolved, nothing rewritten); (iv) NO REGRESSION on the
VC17-confirmed items (the D-12r faithfulness, the ELEVEN-row bijection, the
walk, seams, acyclicity, (0)/(SQ) unconditional, the (H9)/(H8)/(E-N) states).
Standing riders unchanged: no display may claim the D-12r perimeter
discharged; the acceptance statement must read exactly as this revision
displays it.

**Bookkeeping.** Historical §§7–20 untouched except the sanctioned r15-5
resolution bracket. `docs/PROJECT_STATE.md` sync owed at the next state-file
touch. The commit closing this revision is its unit of record (unit ROOT r16).

— REVISION 15 complete. VC17's blocker + advisory folded as two in-place
display syncs (Step 12 locally re-pointed; Step 4 at the exact §0 grade);
ELEVEN rows unchanged; the acceptance stands as earned on REVISION 12 and
re-anchored on REVISION 13; VC18 is owed to re-anchor it on this text.
