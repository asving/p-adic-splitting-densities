# ROOT ASSEMBLY — the uniformity theorem, composed from the leaves (unit ROOT, 2026-08-02)

**Phase C spine.** This is the document the end-to-end verifiers read: the uniformity
theorem stated in Asvin's scaffold form, its proof as a LINEAR COMPOSITION of the
Phase-B leaves, and the COMPLETE conditionality ledger. It is built at the wave-10
state of the campaign (ledger `lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md` §"WAVE 10
+ PASS-2 ADJUDICATIONS", HEAD d730669) from `docs/MATH_COMPLETION_TREE_2026-08-01.md`
(+ DELTAs 1–3, whose corrected readings govern), the per-leaf attempt/verify records in
`lean/notes/openmath/`, and the literature unit `lean/notes/openmath/LITUNIT_2026-08-01.md`.

**Honesty headline (read first).** The theorem below is NOT yet a verified complete
proof, and this document does not claim it is. **REVISION 1 applied** (appendix §7,
2026-08-02): the Phase C end-to-end pass 1 (VC1, Codex, fresh context;
`lean/notes/openmath/VC1_root.jsonl`) returned **COMPOSITION-GAPS** (blockers 1–8,
advisories 9–11); every finding is dispositioned in §7 and repaired in place below, and
statuses are synced to the 2026-08-02 harvest (ledger
`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md` final sections, commit c43b13b).
**REVISION 2 applied** (appendix §8): the end-to-end pass 2 (VC2, Codex, fresh
context; `lean/notes/openmath/VC2_root.jsonl`) returned **COMPOSITION-GAPS** (5
blockers V2-1/2/4/5/6, 2 advisories V2-3/7); every finding is dispositioned in §8 and
repaired in place below — headline repairs: the clause-(R) self-loop formula is now
quantified e ≥ 2 with the forcing proof DISPLAYED (the (SL≥2) display at Step 5), and
(H4) is SPLIT into (H4a) = (R1)–(R4) (consumed by clause (R) too, not (UB)-only) +
(H4b) = (UB-X). **REVISION 3 applied** (appendix §9): the end-to-end pass 3 (VC3,
Fable, fresh context — the model-diverse pass;
`lean/notes/openmath/VC3_root_fable_verify.md`) returned **COMPOSITION-GAPS** (4
blockers B1–B4, 4 advisories A5–A8 — ALL attribution/ledger-integrity; VC3 explicitly
CONFIRMED the composition's mathematics: acyclicity edge-by-edge incl. 17 ≺ 18 ≺ 18b,
the (ROOT-C)-hypotheses ↔ §3.1-[M]-rows bijection, the (SL≥2) display, the (S3)
split); every finding is dispositioned in §9 and repaired in place below, and
statuses are synced to the 2026-08-02-night harvest (ledger 7b28562: **VERIFIED = 6**).
**REVISION 4 applied** (appendix §10): the end-to-end pass 4 (VC4, Codex, fresh
context; `lean/notes/openmath/VC4_root.jsonl`) returned **COMPOSITION-GAPS** (4
blockers + 1 advisory, findings VC4-1..VC4-5 — again all attribution/ledger
integrity; VC4's own pass-list re-confirmed acyclicity incl. 17 ≺ 18 ≺ 18b, (SL≥2),
and the repaired Step-19 Σ_τ R_τ = 1 derivation); every finding is dispositioned in
§10 and repaired in place below — headline repairs: a NEW §3.1 [M] row **(PACK)**
(D-11's open clause 4, the actual-pack correspondence) carried as hypothesis
**(H5)**; clause (SQ)'s attribution corrected DOWNWARD (unconditional — a favorable
honesty fix); the (ADM) row rewritten to its single operative FULL-attainment form
(O-9 r4); and statuses synced to the confirming-passes harvest (**VERIFIED = 7**).
**REVISION 5 applied** (appendix §11): the end-to-end pass 5 (VC5, Codex, fresh
context; `lean/notes/openmath/VC5_root.jsonl`) returned **COMPOSITION-GAPS** (2
blockers + 1 advisory, findings VC5-1..VC5-3 — the two blockers ONE genre: rows this
document itself classified [T] are OPEN PROPOSITIONS consumed on clause (R)'s
mandatory path, and under the ledger's own classification rule placing them in [T]
does not supply them); repaired by PROMOTING them — CU-2t's three terminal rows
(T-DEC)/(T-READ)/(T-VERD) and D-11's widened-charge item (vii) — to a NEW §3.1 [M]
row group carried as hypothesis **(ROOT-C)(H6)**, and statuses synced to the
O-9/CU-2t-verified harvest (ledger 6a7593b: **VERIFIED = 9**).
**REVISION 6 applied** (appendix §12): the end-to-end pass 6 (VC6, Fable, fresh
context — the second model-diverse pass;
`lean/notes/openmath/VC6_root_fable_verify.md`) returned **COMPOSITION-GAPS** (3
blockers F-1/F-2/F-3 + 6 advisories A-4..A-9 — the blockers ALL the VC5 genre walked
to completion: open propositions consumed on clause (R)'s mandatory path still
classified [T]; VC6's own pass-list re-confirmed acyclicity over all 20 steps, the
NINE-row bijection, both unconditional clauses, the Σ_τ R_τ = 1 derivation, (SL≥2),
and the leaf-#8 interlock); every finding is dispositioned in §12 and repaired in
place — headline repairs: **(H1) ENLARGED to the graded-dictionary group** (GD-1's
(KP)/(KPirr) clauses beyond order 1 and the consumed GD-6 conformance instances join
(GR-B) — findings F-2/F-3), a NEW hypothesis **(H7)** = the (I-τ)/K-LOC
sibling-interface engine-stratum conformance pair (the LAST of O-11 r2's declared
D-a/D-b/D-c triple adjudicated hypothesis-grade — finding F-1), and the **T-U
§U.SQ.6 correspondence table EXECUTED** (NEW §3.4 — the standing A8 → VC5-3 → A-9
audit debt converted from asserted to displayed).
**REVISION 7 applied** (appendix §13): the end-to-end pass 7 (VC7, Codex, fresh
context; `lean/notes/openmath/VC7_root_report.md`) returned **COMPOSITION-GAPS**
(2 blockers + 3 advisories — the blockers BOTH attribution/warrant findings at the
two REVISION-6 A-6 adjudications VC6 had flagged forward; VC7's findings 6–11
CONFIRMED the composition: the §3.4 T-U tables genuine incl. the three flagged
adjudications, the (H1) enlargement + (H7) sound, the TEN-row bijection correct,
the walk proving (ROOT-C) quantifier-for-quantifier, acyclicity passing); every
finding is dispositioned in §13 and repaired in place — headline repairs: **(H1)
gains the displayed FOURTH member (d)** (the degree-g > 1 KEY1 generalization,
which Step 6's all-site theorems consume at order-≥ 2 reads — finding 1) and
**(H5) = (PACK) is ENLARGED to carry the three pack row-laws as explicit clauses
(PACK-i/ii/iii)** (rsh_interp / r_bdd / wsh_ok at the 𝔅_n rows — NOT inferable
from the correspondence alone; finding 2).
**REVISION 8 applied** (appendix §14): the end-to-end pass 8 (VC8, Codex, fresh
context; `lean/notes/openmath/VC8_root_report.md`) returned **COMPOSITION-GAPS**
(1 blocker + 1 advisory — the NARROWEST residue of the eight passes, both findings
at the REVISION-7 repairs themselves: the (H1)(d) display's "key has degree > 1"
quantifier rested on an UNPROVEN identification of key degree with read order,
and several summaries lumped all three PACK row-laws into "K2/evaluation glue";
VC8 confirmed all three r8 advisory repairs, the TEN-row count, the quantifier
walk, acyclicity, and no regression on the VC7-confirmed items); both findings
are dispositioned in §14 and repaired in place — headline repairs: **(H1)(d)
REQUANTIFIED to READ ORDER** (the member is now the order-≥ 2 KEY1 block:
membership by OM order ≥ 2 alone, the current key's degree irrelevant —
degree-1-key e·g = 1 refinement chains INCLUDED — so (d) covers exactly the
complement of the proved order-≤ 1 slice and total coverage consumes NO
key-degree/order equivalence — finding VC8-1) and the **PACK K2/K3
sub-attribution synced** (PACK-i/ii enter at Step 18's K2 evaluation glue;
PACK-iii is the membership input to Step 18's K3 evaluation ring-hom, with D-6 —
finding VC8-2).
**ACCEPTANCE at passes 9–10 (2026-08-03) + REVISION 9 applied** (appendix §15):
the end-to-end passes 9 and 10 (VC9, Codex, fresh context,
`lean/notes/openmath/VC9_root_report.md`; VC10, Fable, fresh context — the
model-diverse pass, `lean/notes/openmath/VC10_fable_report.md`) BOTH returned
**COMPOSITION-SOUND with ZERO blockers** on the IDENTICAL REVISION-8 text — the
tree §4 Root criterion's ≥ 2 consecutive clean model-diverse passes is MET:
**the composition is ACCEPTED at (ROOT-C)'s displayed conditional scope**
(milestone addendum, `docs/PROJECT_STATE.md` 2026-08-03). Acceptance certifies
the composition — the walk, the ten-row bijection, the per-clause attribution,
acyclicity — NOT the discharge of §3's ledger: the theorem as proved remains the
conditional (ROOT-C). VC10's three ADVISORIES — display hygiene only — are
dispositioned in §15 and repaired in place at REVISION 9 (post-acceptance: no
[M] member, no step content, no clause attribution changed; the
consecutive-clean counter is TWO and STAYS TWO — advisories do not reset it).
What exists is: an acyclic composition (acyclicity now holds edge-by-edge: the Step-1 edge
REMOVED by re-pointing to Dfloor's self-contained Appendix A, the D-11/O-11 cycle
STRATIFIED as Step 17 ≺ Step 18 ≺ Step 18b) in which every step is a named leaf with an
on-file proof, NINE of the leaves VERIFIED through the campaign's two-pass
model-diverse bar (O-13, O-12, Dfloor, GD23, CU-1, O-10, O4T, O-9, CU-2t — the last
two adjudicated at the 6a7593b harvest after O-9's clean confirming pass 5 and the
CU-2t R4 spot-check, REVISION 5 status sync), the rest PROVED-UNVERIFIED with
their pending passes NAMED, and the genuinely open mathematics confined to §3.1: the
two heavy items — **(GR-B)** at OM orders ≥ 2 and **(FRESH)** — both pinned to printed
literature statements and priced at a single lemma layer [since REVISION 6, findings
F-2/F-3, (GR-B) HEADS the enlarged **(H1) graded-dictionary group**, whose displayed
members add GD-1's (KP)/(KPirr) clauses beyond order 1 and the consumed GD-6
run-decomposition-conformance instances — and, since REVISION 7, finding VC7-1,
the order-≥ 2 KEY1 block as member (d) (REQUANTIFIED to read order at REVISION 8,
finding VC8-1)], plus the smaller displayed
rows ((R1)–(R4) with the clause-(UB) residue (UB-X) [the (R1)–(R4) = (H4a) row
RETIRED at REVISION 22, r23 — F-(iv) fired on the P(O-8b/M14) composite ACCEPT
(passes 3+4 clean, model-diverse); record at the §3.2 (H4a) retirement row; the
(K3-δ) audit note SURVIVES on the (UB-X) row], (E-N) [D-12r, formerly listed
here, RECLASSIFIED [M] → the §4 literature-pin class at REVISION 14 (r15), executing
VC16 finding 2 — its residue is exactly the (HEN-LIFT)/(OM-SAT) pins], — added at
REVISION 2 — (ADM), O-9's census-admissibility hypothesis (FULL attainment since O-9
r4; automatic at order 0 only), — added at REVISION 4, finding VC4-3 — **(PACK)**,
the actual-pack correspondence (ENLARGED at REVISION 7, finding VC7-2, to carry
the three pack row-laws PACK-i/ii/iii, and AGAIN at REVISION 10, r11, the fourth,
(PACK-iv)), — added at REVISION 5, findings VC5-1/VC5-2 —
the **(H6) terminal-seam engine-conformance group** ((T-DEC)/(T-READ)/(T-VERD) +
D-11 item (vii)), — added at REVISION 6, finding F-1 — **(H7)**, the (I-τ)/K-LOC
sibling-interface conformance pair, and — added at REVISION 10, r11 — **(H8)** =
(K-CUT), the shallow-cut decomposition kernel [RETIRED at REVISION 16, r17 — the
round-3 discharge gate: the package machine-proved Lean-core; its three residual
semantic interfaces RE-PRICED at REVISION 17 (r18) as the NEW row (H8′) — the
§3.2 K-CUT retirement row], — added at REVISION 17, r18, executing VC19 —
**(H8′)**, the K-CUT residual-interface row (three members: **GRAFT** the graft
completeness/bijectivity interface, **MASS-ID** the shMass/blkMass measured-mass
identification, **POOL-DICT** the node-level τ/δ pool dictionary +
blockTotal_tie instantiation), and **(H9)** = (NSF), the
ns-routing interface row [RETIRED at REVISION 19, r20 — BOTH members retired:
(NSF-KEY) at REVISION 13, r14 (Unit C executed) and (NSF-INT) at ledger entry
H9-C3 (the (K1-CANON) discharge, warrant note
`K1CANON_discharge_2026-08-02.md`); record at the §3.2 H9 = (NSF) retirement
row, RE-OPEN TRIGGER displayed there (the D-11 leaf bar)] [this headline
enumeration run to the twelve-row scope
at REVISION 12 — the global enumeration audit; to the ELEVEN-row scope at
REVISION 14 (r15) — the D-12r move; to the TEN-row scope at REVISION 16 (r17) —
the (H8) retirement; to the ELEVEN-row scope at REVISION 17 (r18) — the (H8′)
residue re-pricing; to the TEN-row scope at REVISION 19 (r20) — the (H9)
retirement; to the NINE-row scope at REVISION 22 (r23) — the (H4a)
retirement]). **THE THEOREM AS
PRESENTLY PROVED is the conditional form (ROOT-C) displayed in §1**; the unconditional
(ROOT) is the TARGET, quantifier-for-quantifier NOT yet what the steps prove. §3 is the
complete list of everything the root currently rests on beyond literature; §4 is the
literature roster (the goal's conditionality class); §5 is the numerics corroboration
index (flags, never substitutes). The tree's §4 acceptance criterion applies verbatim:
PROVED-UNVERIFIED is never consumed by an acceptance claim, and this assembly becomes
THE COMPLETE PROOF only when §3's ledger empties into VERIFIED + LITERATURE-audited.

**SUSPENSION + REVISION 10 (r11) — the standing of 2026-08-04 (superseded the
acceptance language above as status; itself SUPERSEDED as status by the
RE-ACCEPTANCE + REVISION 13 paragraph below — kept as the revision record,
incl. its then-true "(NSF-KEY) KNOWN FALSE AT HEAD" display).** The Phase-C acceptance was **SUSPENDED**
(2026-08-03, stop-the-line; ledger entry at 44ce0a7; `docs/PROJECT_STATE.md`
addendum): the owed D-11 pass 3 (`D11_owedpass_2026-08-03.md`) returned CRITICAL,
and two MANDATORY steps (17, 18b) consumed the refuted layers AS PROVED. **REVISION
10 (§16, unit ROOT r11)** executes the repair design
(`lean/notes/openmath/R11_REPAIR_DESIGN_V5_2026-08-04.md`, Unit B): Steps 17/18b now
consume the REPAIRED true-scope D-11 leaf (`D11_rev4_2026-08-04.md` at REVISION 3,
twice hostile-passed: r4 pass 1 CRITICAL → folded; r4 pass 2 **GAPS-ONLY, all
design-§7 obligations adjudicated VALID as proofs**), and every premise the audit
found unpriced is a displayed [M] member: **(PACK-iv)** joins (H5) as its FOURTH
row-law, and TWO NEW rows enter §3.1 — **(H8) = (K-CUT)** and **(H9) = (NSF)**
(whose member (NSF-KEY) is KNOWN FALSE AT HEAD, retiring only by the design's
sign-off-gated Unit C). The §3.1 roster is now **TWELVE rows ↔ (H1)–(H9)**. The
suspension's defect is CURED at composition level — nothing refuted remains
consumed-as-proved — but **the acceptance is NOT re-earned: the consecutive-clean
counter is ZERO of 2**, and the end-to-end bar (two consecutive clean model-diverse
passes on identical text, fresh verifiers) must be re-run on THIS REVISION-10 text.
Until it passes, every acceptance statement in this document is historical record,
not current status.

**REVISION 11 applied (r12, appendix §17).** The end-to-end pass 11 (VC11, fresh
context; `lean/notes/openmath/VC11_report.md`) RAN on the REVISION-10 text and
returned **COMPOSITION-GAPS** (4 blockers + 1 advisory — ALL r11 RIPPLE-SYNC
defects: displays the r11 edits should have carried forward but did not — Step 19's
evaluation/(UB) summaries and final ∎ stopped at (H7)/PACK-i-iii, Step 16's claimed
exact (UB) scope was stale against §1, §3.4's item 12 routed the shallow-cut
content to a verified layer against the (H8) row, the 6(iii)–(ix)+7 residue list
omitted (H9), Step 18b's Consumes summary omitted (ADM) — while the TARGETED r11
repairs themselves were CONFIRMED: Step 17's per-clause measures, Step 18b at
(RE)/(PACK-iv), the twelve-row §3.1 ↔ (H1)–(H9) bijection, (NSF-KEY) marked KNOWN
FALSE AT HEAD with gated retirement, (0)/(SQ) unconditional, 17 ≺ 18 ≺ 18b). Every
finding is dispositioned at §17 and repaired in place — PURE DISPLAY SYNCS: no [M]
member, no step proof content, no leaf pointer changed. The standing is UNCHANGED:
the acceptance is NOT re-earned, **the consecutive-clean counter is ZERO of 2**,
and the bar re-runs (VC12) on THIS REVISION-11 text (charge at §17).

**REVISION 12 applied (r13, appendix §18).** The end-to-end pass 12 (VC12, fresh
context; `lean/notes/openmath/VC12_report.md`) RAN on the REVISION-11 text and
returned **COMPOSITION-SOUND, ZERO findings** — the consecutive-clean counter
reached ONE of 2. The model-diverse pass 13 (VC13, Fable, fresh context;
`lean/notes/openmath/VC13_fable_report.md`) then RAN on the same REVISION-11 text
and returned **COMPOSITION-GAPS** (2 blockers + 3 advisories — ALL the
stale-hypothesis-enumeration genre that survived the r11/r12 sync rounds: §1's own
(ROOT-C) display of (H5) stopped at PACK-i/ii/iii; Step 18's δ-stable note quoted
§1's clause-(R) set four groups short with a REVISION-4-era "five-hypothesis"
count; Step 17's Consumes omitted its own consumed (H9)/(PACK-iv); the (H8)
provenance label pointed at an O-11 "owed-pass" no §3.2 row recorded; the (AGR)
residue sentence elided CU-4's displayed [M] conditionality — while VC13 CONFIRMED
the walk, the twelve-row bijection, the seams, acyclicity, and (NSF-KEY)'s
never-consumed-as-true discipline). **The counter RESETS to ZERO of 2.** Every
finding is dispositioned at §18 and repaired in place — pure display syncs — and,
to KILL THE CLASS, §18 records a GLOBAL ENUMERATION AUDIT: every operative
(§§0–6 + §3.4) display that enumerates hypothesis groups/members or counts them
was checked against §1's current display and synced; VC14 re-runs the bar on THIS
REVISION-12 text (charge at §18).

**RE-ACCEPTANCE at passes 14–15 + REVISION 13 applied (r14, appendix §19) —
superseded as CURRENT standing by the REVISION 14 paragraph below.** VC14 (Codex,
fresh context; `lean/notes/openmath/VC14_report.md`)
and VC15 (Fable, fresh context — the model-diverse pass;
`lean/notes/openmath/VC15_fable_report.md`) BOTH RAN on the IDENTICAL REVISION-12
text and BOTH returned **COMPOSITION-SOUND** (VC14 clean, zero regressions, the §18
enumeration-audit claim tested and held; VC15 ZERO blockers with an explicit null
result on the blocker hunt, 4 display-hygiene advisories): the tree §4 Root
criterion (≥ 2 consecutive clean model-diverse passes, fresh verifiers) is MET
AGAIN — **the composition is RE-ACCEPTED at (ROOT-C)'s twelve-row displayed
conditional scope**, closing the suspension arc (refutation caught → leaf repaired
at true scope → composition re-pointed → bar re-earned). REVISION 13 (§19) is the
post-acceptance hygiene + discharge-sync batch (the r10/REVISION-9 precedent:
acceptance earned on the pre-batch text, every edit recorded): VC15's four
advisories repaired in place; the (H9) row synced to the EXECUTED Unit C —
**(NSF-KEY) is RETIRED** (six (NSF-KEY-RET) artifacts on file; compiled witness
H9-U1), the row's open member now (NSF-INT) with its (K1-CANON) premise; the
D-12r row rewritten to its dual-accepted reduction (both cites pinned-confirmed;
reclassification VC-gated); the (H8) = (K-CUT) row re-scoped to its landed 11-unit
Lean development (REDUCED, not discharged — the fan-out's hostile pass owed); the
mid-campaign (E-N) rewrite (commit 89176a4) formally ADOPTED with its process gap
noted. The §3.1 roster stays TWELVE rows ↔ (H1)–(H9). HONEST COUNTER STATEMENT:
the acceptance was EARNED on the REVISION-12 text; REVISION 13 is display/status
sync but IS a text change — a confirming end-to-end pass (**VC16**) is OWED to
re-anchor the accepted text on the REVISION-13 wording; until it lands, the
acceptance citation points at the REVISION-12 text (charge at §19).

**VC16 + REVISION 14 applied (r15, appendix §20) — superseded as CURRENT
standing by the REVISION 15 paragraph below.** VC16
(the confirming end-to-end pass, fresh context;
`lean/notes/openmath/VC16_report.md`) RAN on the REVISION-13 text and returned
**COMPOSITION-SOUND** — zero blockers, advisories only — re-anchoring the
acceptance on the REVISION-13 wording (the §19 charge met: the four advisory
repairs, the (H9)/(H8)/(E-N) syncs, and the REWRITTEN D-12r row's faithfulness
all verified; finding 1 notes Step 4's exact grade reads as PROVED-UNVERIFIED
under §0's vocabulary until its audit lands). Its finding 2 ADJUDICATES the
D-12r reclassification **WARRANTED**: no D-12r-specific open mathematics remains
that must stay [M] — the residue is confined to the (HEN-LIFT)/(OM-SAT)
literature pins. REVISION 14 (§20, unit ROOT r15) EXECUTES the move: the D-12r
row leaves §3.1 for §4 as a literature-pin entry (pins on file: the three-part
(HEN-LIFT) at Neukirch II §4 (4.6) p. 129 / the displayed companion uniqueness
lemma / Serre LF I §6 Prop 15 pp. 17–18; (OM-SAT) riding the §4 GMN entry at the
TAMS-confirmed Cor 3.8 + Def 3.9), with the dependency-decomposition routing
preserved and every consuming step's conditionality re-pointed
("perimeter-conditional on the §4 D-12r pin"). The §3.1 roster is now **ELEVEN
rows ↔ (H1)–(H9)** with **(H3) = {(ADM), (E-N)}**; VC16 finding 2's three §4
roster-sync items are executed honestly at §20 (GMN pagination: CONFIRMED for
the (OM-SAT) slice only, the LIT-1-wide session still owed; the Neukirch pin:
numbers CONFIRMED, Mathlib probe RAN — coprime-factor Hensel ABSENT, the pin
stands as a paper cite; Serre LF I §6 Prop 15 added to the consumed-loci
enumeration). HONEST COUNTER STATEMENT: the acceptance was EARNED on the
REVISION-12 text and RE-ANCHORED by VC16 on REVISION 13; REVISION 14 executes a
VC16-warranted reclassification but IS a text change — a confirming end-to-end
pass (**VC17**) is OWED on the REVISION-14 wording (charge at §20).

**VC17 + REVISION 15 applied (r16, appendix §21) — superseded as CURRENT
standing by the REVISION 16 paragraph below.** VC17
(the confirming end-to-end pass, fresh context;
`lean/notes/openmath/VC17_report.md`) RAN on the REVISION-14 text and returned
**COMPOSITION-GAPS** — ONE blocker + ONE advisory, both display-conformance at
the r15 edits themselves, with ALL other charged checks PASSING (the D-12r
residue confined to (HEN-LIFT)/(OM-SAT), the ELEVEN-row ↔ (H1)–(H9) bijection
with (H3) = {(ADM), (E-N)}, the operative-clause attributions, the conditional
walk, acyclicity incl. 17 ≺ 18 ≺ 18b, no REVISION-12/13 regression). REVISION 15
(§21, unit ROOT r16) repairs both in place: Step 12's (τ-irr) saturation
description now locally displays "perimeter-conditional on the §4 D-12r pin
(entry 10)" — the one former consumption site the r15-5 audit had passed on the
central-enumeration ground alone, now matching Steps 2/3/11/16/19 — and Step 4's
grade is rendered in the EXACT §0 word, **PROVED-UNVERIFIED** (pending upgrade:
the §3.2 M17/RegPFinite faithfulness-audit row; the machine-checked-Lean-core
fact and Step 5's independent route stay as displayed notes, not grade words).
No [M] member, step proof content, leaf pointer, or clause attribution changed;
the §3.1 roster stays ELEVEN rows. HONEST COUNTER STATEMENT: the acceptance was
EARNED on the REVISION-12 text, RE-ANCHORED on REVISION 13 by VC16; REVISION 15
is a two-sentence display repair but IS a text change — the confirming
end-to-end pass (**VC18**) is OWED on the REVISION-15 wording (charge at §21).

**VC18 + REVISION 16 applied (r17, appendix §22) — superseded as CURRENT
standing by the REVISION 17 paragraph below.** VC18
(the confirming end-to-end pass, fresh context;
`lean/notes/openmath/VC18_report.md`) RAN on the REVISION-15 text and returned
**COMPOSITION-SOUND** — zero blockers, ONE advisory (the §3.2 end-to-end-passes
row stale at VC16, repaired at this revision) — **the acceptance RE-ANCHORS on
the REVISION-15 text** (the §21 charge met: both r16 repairs verified, all six
former D-12r consumption sites locally conditional, the ELEVEN-row bijection,
acyclicity, the displayed-scope walk, no regression). REVISION 16 (§22, unit
ROOT r17) then EXECUTES the SECOND post-acceptance row retirement (D-12r at
REVISION 14 was the first): the (K-CUT) discharge gate — the row's own displayed
"discharge awaits ONLY the clean re-pass" (R2RootSync §§A/G standing status) —
**PASSED at the round-3 unit-level pass**
(`lean/notes/openmath/KCUT_pass3_2026-08-06.md`, VERDICT **DISCHARGES**; its
scope line: it discharges the review gate, NOT the remaining semantic owner
rows and NOT the unconditional (ROOT)). **The (H8) = (K-CUT) row RETIRES from
§3.1**: the package (the repaired adjudication display + `KCutMassFactor` +
`KCutK1Tie`, machine-proved Lean-core; the 3-round pass arc) is recorded at the
NEW §3.2 K-CUT retirement row, and the three semantic residues MOVED to their
owning leaves' displayed debts — graft completeness → the D-14 + CU-3 + REL.2
arc; measured-mass identification → the (H7)/sibjc TREE-EXP + [2r]/CL-8 +
O-1/O-3 count-tie owners; the τ/δ pool dictionary → [2r]/CL-8, with the W-leg
at CL-17(ii)/W17ii. The §3.1 roster is now **TEN rows** — the historical
letters kept: **(H1)–(H9) minus the retired (H8)**. HONEST COUNTER STATEMENT:
the acceptance was EARNED on the REVISION-12 text, RE-ANCHORED on REVISION 13
by VC16 and on REVISION 15 by VC18; REVISION 16 IS a text change — the
confirming end-to-end pass (**VC19**) is OWED on the REVISION-16 wording
(charge at §22); until it lands, the acceptance citation points at the
REVISION-15 text.

**VC19 + REVISION 17 applied (r18, appendix §23) — superseded as CURRENT
standing by the REVISION 18 paragraph below.** VC19
(the confirming end-to-end pass, fresh context;
`lean/notes/openmath/VC19_report.md`) RAN on the REVISION-16 text and returned
**COMPOSITION-GAPS** — TWO blockers, ONE defect: **the r17 retirement
OVERSHOT**. The (K-CUT) package is genuinely proved and its §3.2 retirement
record STANDS, but the three residual semantic interfaces were assigned to
owners whose statements do NOT house them (D-14/CU-3 prove read locality and
nonempty realization, not graft completeness/bijectivity; no cited row states
the shMass/blkMass measured-mass identification; no row supplies the
node-level τ/δ pool dictionary or blockTotal_tie's instantiation) — so clause
(R), and clause (UB) by inheritance, silently consumed unpriced content at
Step 18's K1. REVISION 17 (§23, unit ROOT r18) EXECUTES the honest re-pricing:
a NEW SLIMMED §3.1 row **(H8′) — the K-CUT residual-interface row** — houses
the three interfaces as displayed [M] members, stated precisely from the
package's own displays (**(i) GRAFT** — graft completeness/bijectivity, the
charge-(vi-a) FAIL standing as its status; **(ii) MASS-ID** — the
shMass/blkMass measured-mass identification at the consumed member;
**(iii) POOL-DICT** — the node-level τ/δ pool dictionary + blockTotal_tie
instantiation), the former owner labels kept as retirement ROUTES, not homes.
Step 18's K1 and the per-clause attributions now consume the retired package
+ (H8′). The §3.1 roster is **ELEVEN rows — (H1)–(H9) minus the retired (H8),
plus (H8′)**. NET PROGRESS, stated honestly: the former (H8) row assumed the
WHOLE cut decomposition; (H8′) assumes ONLY the three interfaces — the
decomposition/ownership/product/K1-composition core is machine-proved
Lean-core. HONEST COUNTER STATEMENT: the acceptance was EARNED on the
REVISION-12 text, RE-ANCHORED on REVISION 13 by VC16 and on REVISION 15 by
VC18; VC19's blockers re-opened the composition question on the REVISION-16
wording, and REVISION 17 repairs it — the confirming end-to-end pass
(**VC20**) is OWED on the REVISION-17 wording (charge at §23); until it lands,
the acceptance citation points at the REVISION-15 text.

**VC20 + REVISION 18 applied (r19, appendix §24) — superseded as CURRENT
standing by the REVISION 19 paragraph below.**
VC20 (the confirming end-to-end pass, fresh context;
`lean/notes/openmath/VC20_report.md`) RAN on the REVISION-17 text and returned
**COMPOSITION-GAPS** — TWO blockers, BOTH leftover SENTENCES, the (H8′)
substance CONFIRMED: VC20 verified that (H8′)'s three members exactly cover
VC19's missing K1 premises (GRAFT, MASS-ID, POOL-DICT incl. `blockTotal_tie`;
no fourth residue, none weakened, no silent reinstatement of the retired
kernel), that the live attributions agree ((R)/(UB) gain exactly (H8′); (0)/
(SQ) unconditional), that the displayed-scope walk closes at (ROOT-C), that
acyclicity stands (incl. 17 ≺ 18 ≺ 18b), and that no independent regression
exists. The two blockers: (1) §1's retired-(H8) bracket still counted TEN
live rows; (2) the §3.2 K-CUT retirement row still carried the pre-r18
"owner-displayed interfaces" / "priced at its owners" clauses alongside the
r18 correction. REVISION 18 (§24, unit ROOT r19) repairs both in place:
§1's bracket now reads ELEVEN with the (H8′) pointer, and the retirement
row's WHAT-RETIRED sentence is REWRITTEN to one voice — the package is
proved conditional on the three interfaces, PRICED AT (H8′), the owner arcs
retirement ROUTES only. The §3.1 roster stays **ELEVEN rows — (H1)–(H9)
minus the retired (H8), plus (H8′)**. HONEST COUNTER STATEMENT: the
acceptance was EARNED on the REVISION-12 text, RE-ANCHORED on REVISION 13 by
VC16 and on REVISION 15 by VC18; VC19 re-opened the composition question,
REVISION 17 re-priced, VC20 confirmed the substance but blocked on the two
leftover sentences, and REVISION 18 fixes them — the confirming end-to-end
pass (**VC21**) is OWED on the REVISION-18 wording (charge at §24); until it
lands, the acceptance citation points at the REVISION-15 text.

**VC21 + REVISION 19 applied (r20, appendix §25) — superseded as CURRENT
standing by the REVISION 20 paragraph below.**
VC21 (the confirming end-to-end pass, fresh context;
`lean/notes/openmath/VC21_report.md`) RAN on the REVISION-18 text and returned
**COMPOSITION-SOUND, zero blockers** (ONE advisory — the §3.2 end-to-end row's
tail stale at VC19, repaired at this revision) — **the acceptance RE-ANCHORS
on the REVISION-18 text**. REVISION 19 (§25, unit ROOT r20) then EXECUTES the
THIRD post-acceptance row retirement (D-12r at REVISION 14, (H8) at
REVISION 16 the first two): **the (H9) = (NSF) row retires from §3.1**. Ground
(ledger entry H9-C3, commit 9abc3ab): the row's LAST open member (NSF-INT)
retired per (NSF-INT-RET)'s four grounds — I-1 (the D-11 r4 §4B leaf proof) +
I-2 (the closed {K1, K4} site list, (CH-RES)-relative) + I-3 (the r4 pass-2
ACCEPT of §4B as a conditional proof) + the **(K1-CANON) discharge** (the
(ed-1) M04 §5.5 scope pin: K1's quantifier + the post-K4 sum pinned to (q-4)'s
notion, preamble line, Lemma 3.3 untouched; executed under Asvin's standing
statement-change authority, SURFACED for review; warrant note
`lean/notes/openmath/K1CANON_discharge_2026-08-02.md` with the 2-round hostile
pass, substance PASS both rounds) — (NSF-KEY) was already retired at
REVISION 13, r14. The record moves to the NEW **§3.2 H9 = (NSF) retirement
row**; the §3.1 roster is **TEN rows — (H1)–(H9) minus the retired (H8) and
(H9), plus (H8′)**; clauses (R)/(UB) each LOSE (H9). **RE-OPEN TRIGGER,
displayed:** the D-11 leaf bar still owes one confirming clean pass (the §3.2
D-11 row); if that pass finds §4B defective, I-3 re-opens and the (NSF-INT)
retirement SUSPENDS. HONEST COUNTER STATEMENT: the acceptance was EARNED on
the REVISION-12 text, RE-ANCHORED on REVISION 13 by VC16, on REVISION 15 by
VC18, and on REVISION 18 by VC21; REVISION 19 IS a text change — the
confirming end-to-end pass (**VC22**) is OWED on the REVISION-19 wording
(charge at §25); until it lands, the acceptance citation points at the
REVISION-18 text.

**REVISION 20 applied (r21, appendix §26) — superseded as CURRENT standing
by the REVISION 21 paragraph below.**
REVISION 20 (§26, unit ROOT r21) is a **leaf-status/display sync batch** —
NO roster change, NO clause-attribution change, no step proof content or
leaf pointer moved; the §3.1 roster RECOUNTED at **TEN rows** (unchanged
from REVISION 19). What it folds, all per the ledger's records
(`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`): (1) the (H1)(a) leaf
**(GR-B) RE-ACCEPTED** at its REVISION 14 (commit 58534d4; 21-pass arc —
the Def-3.15 extraction-artifact incident caught by the N-1 harness,
corrected, passes 20+21 CLEAN; N-1 bar MET 15/15 GREEN; the corrected
dictionary of record); (2) **B-M2 (LIFT-CONF) ACCEPTED** at its REVISION 14
(commit 416c213; 14-pass arc — the (H1) member-(b) engine-lift conformance
face: actual-pair (K0) on both carrier classes, the hspecRec transport, the
REC-WIRE-G width-law derivation, the H₃rc witness; opens GRADED-READ /
REC-SL / SITE-EXH / the readsOf recentering-branch debt STAND); (3)
**H1-U14 STATED** behind its T8-decided N-1 gate (commit 6df3eae —
`FaithfulTotal` [M]-row + two proved consumers); (4) **H2-U9 LANDED**
(commit 23d2c03 — the (TRANS-DEEP) [M]-row + `fresh_of_transDeep` PROVED;
gate satisfied by the accepted §B2-DEF supersession; `leakFree` bare-Prop
placeholder owed at U10); (5) **III-S4 REPAIRED + H6-A4b LANDED** (commit
f56a536 — `TerminalPolySem` + `cu2t_readForcing_sem` with pos/neg gates;
`terminalDatumD_slope_spec` landed, the A4b external blocker CLEARED). All
five are status-note syncs INSIDE the standing (H1)/(H2)/(H6) rows — every
row stays [M]; nothing is discharged. Review surface for Asvin (ledger +
standing queue; no ROOT section, by design): the `hf` Monic pricing
(III-S4), the (ed-1) M04 §5.5 pin (K1-CANON), the `TerminalPolySem`
supplier. HONEST COUNTER STATEMENT: the acceptance was EARNED on the
REVISION-12 text, RE-ANCHORED on REVISION 13 by VC16, on REVISION 15 by
VC18, and on REVISION 18 by VC21; VC22 has NOT yet run, and REVISION 20 IS
a text change — the confirming end-to-end pass (**VC22**) ROLLS FORWARD and
is OWED on the REVISION-20 wording (charge at §26); until it lands, the
acceptance citation points at the REVISION-18 text.

**VC22 CONFIRMED + REVISION 21 applied (r22, appendix §27) — superseded as
CURRENT standing by the REVISION 22 paragraph below.** VC22 (the confirming end-to-end pass, fresh context; ledger
2026-08-08, "VC22 CONFIRMED — the ROOT acceptance RE-ANCHORS at
REVISION 20") RAN on the REVISION-20 text and returned **COMPOSITION-SOUND,
zero blockers** (the Step 1–19 spine composes end-to-end post-r19/r20/r21;
TEN rows recounted; 8 honesty spot-checks — 5 touched + 3 controls — all
FAITHFUL against artifacts and commits; the §26 audit table reproduced; ONE
metadata-only ADVISORY: the (GR-B) row's 55/2-NEW grep counts read 54/3
under the line-site unit, zero stale operative displays) — **the acceptance
RE-ANCHORS on the REVISION-20 text**. REVISION 21 (§27, unit ROOT r22) is a
status-record batch — NO roster change, NO clause-attribution change, the
§3.1 roster RECOUNTED at **TEN rows** (unchanged): (1) the (H4a) =
(R1)–(R4) F-chain record — F-(i) COMPLETE (H4-M1 ACCEPTED, H4-M2 DONE,
H4-M4 rev 4 Route-B adjudication, H4-M5 textual bar MET, N-K3 ALL GREEN
376/376) and P(O-8b/M14) FIRED at pass 2 on the current texts (Codex,
fresh context) returning **GAPS-ONLY (0 critical / 1 gap** — M14 Lemma
1(b)'s top-space scale display stale against M1's accepted two-case
display**)**, so the (H4a) retirement is **RETIREMENT-READY-PENDING-CLEAN-PASS,
NOT executed** (the consecutive-clean ACCEPT bar; owed: the M14 display
fold + one clean composite pass — then F-(iv) fires per H4M7 §3); (2) the
TR3-S1 stratum-served annotation on the (H1) row (order-2/e₀ = 1 stratum:
TR-3′-GEN(i) a THEOREM, TR-3-ORD-X PROVED mod (KP_i)+[T] there; row stays
[M]); (3) the (UB-X) row's Route-B adjudication status (M4/M5/N-K3 legs
green; NOTHING discharged — the re-scope waits on H4M7 §4's five conditions
incl. Asvin sign-off); (4) §6 in-flight campaign pointers (the B2D Lean
campaign wave E; the H4 pass-report archive). HONEST COUNTER STATEMENT: the
acceptance was EARNED on the REVISION-12 text, RE-ANCHORED on REVISION 13
by VC16, on REVISION 15 by VC18, on REVISION 18 by VC21, and on REVISION 20
by VC22 (CONFIRMED); REVISION 21 IS a text change — the confirming
end-to-end pass (**VC23**) is OWED on the REVISION-21 wording (charge at
§27); until it lands, the acceptance citation points at the REVISION-20
text; **VC24** names the subsequent pass in the standing cadence (model
diversity per the tree §4 bar).

**REVISION 22 applied (r23, appendix §28) — superseded as CURRENT standing
by the REVISION 23 paragraph below.** The
ledger's 2026-08-08 batch (commits 355c1eb→5a10256) lands in ONE revision.
**THE (H4a) RETIREMENT EXECUTES — F-(iv) FIRES:** the P(O-8b/M14) composite
passed CLEAN twice consecutively, model-diverse (pass 3 Codex 0C/0G on M14
REVISION 1, the pass-2 gap folded at c152602/fb59459; pass 4 Fable decider,
nothing classifiable — ACCEPT per H4M7 §2; ledger "P(O-8b/M14) COMPOSITE
ACCEPTED", commit 5a10256), completing the F-chain (F-(i) five records
**[count CORRECTED at REVISION 23, r24 — VC25 gap 2: SIX records at the
declared "records" unit (H4-M1, H4-M2, H4-M4, H4-M5, N-T4, N-K3); the
"five" was an under-count inherited from the ledger's "F-(i) five
records ✔" summary line, which folded N-T4 and N-K3 into one numerics
entry]** · F-(ii) automatic · F-(iii) passes 1–4). Per H4M7 §3 + the r22 display: **the
(R1)–(R4) = (H4a) row LEAVES the §3.1 roster** for the NEW §3.2 (H4a) =
(R1)–(R4) retirement row (the K-CUT/H9 precedent — provenance kept, §3.1
tombstone below the table); the **(K3-δ) audit note SURVIVES, MOVED to the
(UB-X) row** (clause (R) still consumes (K3-δ) until the H4M7 §4 Route-B
re-scope fires — NOT retired here); Step 16/18 K3-c displays upgrade the
(R1)–(R4) leg to PROVED-VERIFIED at the ACCEPT record; clauses (R)/(UB) each
LOSE (H4a). The §3.1 roster is **NINE rows — (H1)–(H9) minus the retired
(H4a), (H8), and (H9), plus (H8′)** (recounted per the r13 rule). The
revision ALSO folds, per the same ledger batch: (1) the **SITE-EXH arc** on
the (H1) row — probe NEEDS-EXTENSION (355c1eb) → Asvin sign-off (49b778e) →
E-1/E-2/E-4 LANDED (f36ab72: `siteExh_elimination'` UNCONDITIONAL at the
as-built engine face, two-layer form; `TowerMoveClosureL` layering
re-ratification PENDING ASVIN) → E-3 glue PROVED (db47e35:
`truncRunFrom_glue` + the f-tie chain `FTiedTruncLanding`/`gate_ftie_chain`
zero-hypothesis; residuals `junc_bundle` (the D-1 wall) +
`DIterJunctionSupplier` (named open, ITER-LAW two-constant fence) + the box
face); (2) **GRADED-READ DISCHARGED at interior coherent [NR] reads**
(a5911fd: `graded_read_of_history`, M-parametric; vacuous-at-steep by the
compiled scoping `readHyps_e_eq_one`; abstract σ.e ≥ 2 frame residue
OPEN-OBSTRUCTED — GR-3 ×2 + GR-4 ×1 frozen) and the **B2D campaign state**
(TD front SORRY-FREE end-to-end incl. TD-3 proved on v3 after the
countermodel arc + the TD-6 supplier `transDeep_of_ledger` MACHINE-CHECKED
filling `fresh_of_transDeep` — the (FRESH) row's (TRANS-DEEP) member now has
a COMPILED ledger-instance supplier, `huni` still displayed; CL5 PROVED;
GR 5/8 proved); (3) the **V9 e′ ≥ 2 VACUITY-RESCOPE ruling** (d632305, §3.3
O-14a row); (4) the **strata probe** (8db1b00: TR-3′-GEN(ii)'s closed-form
twist law ON RECORD 0/3,640; D-ITER's single-ζ′ cyclic presentation FALSE at
the canonical alphabet 22/28 — the measured two-constant ITER-LAW is the
true structure, D-ITER to be RESTATED **[EXECUTED at REVISION 25, r26:
D-ITER RESTATED, 3bcae68 — the ITER-LAW is the statement of record]**) —
(H1)-row residue re-descriptions;
(5) **B-M2 REVISION 15** (38d98f6: post-acceptance annotation fold,
acceptance NOT reset, counter stays 2/2; the four opens re-priced;
`readsOf_landing_K0` debt STANDS NARROWED). The (H1)/(FRESH)/(H6)/(H7)/
(H8′)/(ADM)/(UB-X)/(PACK)/(E-N) rows all STAY [M]. HONEST COUNTER STATEMENT:
the acceptance was EARNED on the REVISION-12 text and last RE-ANCHORED on
REVISION 20 by VC22 (CONFIRMED); **VC23/VC24 (named at r22) NEVER RAN** —
this revision supersedes the REVISION-21 text before either fired, so they
stand as named-but-unrun slots and the owed confirming pass ROLLS FORWARD
onto THIS text at the cadence's next fired number: **VC25 is OWED on the
REVISION-22 wording** (this batch's phase-2 confirming pass; charge at §28),
**VC26** the pass after it (model diversity per the tree §4 bar); until VC25
lands, the acceptance citation points at the REVISION-20 text.

**REVISION 23 applied (r24, appendix §29) — superseded as CURRENT standing
by the REVISION 24 paragraph below.** **VC25
RAN on the REVISION-22 wording and returned NOT-CONFIRMED (1 critical + 3
gaps; report `lean/notes/openmath/VC25_report.md`, archived at ea9f615)** —
the occurrence census, roster recount (NINE rows), and clause recounts all
REPRODUCED, but the (H4a) retirement had NOT been propagated into three
OPERATIVE displays. This revision disposes all four findings: (1) CRITICAL
— §3.4 Table A items 6(iii)–(ix)+7 and 9 and Step 16's honest-scope clause
(ii) rewritten: (K3-δ) stays the LIVE input (housed at the §3.1 (UB-X)
row, clause-(R)-consumed), (H4a) appears there only under explicit
retirement brackets pointing at the §3.2 (H4a) = (R1)–(R4) retirement row
(item 9's dangling "(§3.1)" pointer cured); (2) the F-(i) count corrected
five → **SIX records** (H4-M1, H4-M2, H4-M4, H4-M5, N-T4, N-K3); (3) the
granular F-chain provenance pointers VC25 could not resolve from its
pasted ledger tail were RE-VERIFIED IN-REPO (all four attempt files exist;
commits 80635f0/c152602/fb59459/5a10256 resolve) — an AUDIT-INPUT
artifact, not a document defect, recorded at §29; (4) the stale "ROOT
ASSEMBLY complete, 2026-08-02 … HEAD d730669" close marked HISTORICAL.
The revision ALSO folds this window's ledger events: **GR ROUTE (a)
EXECUTED** (commit 5b7f1ee per `GR34_DISPOSITION_2026-08-08.md` @ 205e3f8:
`ReadHyps` gained the frame-pin fields `hσs`/`hσt` — a fence-(vii)
STATEMENT CHANGE adjudicated under Asvin's standing statement-change
authority (2026-08-05), on ZERO consumers + believed-FALSE at the bound
scope; the 3 frozen GR-3/GR-4 sorries discharged as one-liners, **B2D
census 3 → 0 sorries**, all Lean-core; `graded_read_of_history` — the
consumed face — signature-UNCHANGED; the F5 stage-forge GR leg DECLINED;
**FLAGGED FOR ASVIN at §29**); **TR3-S2 attempt ON RECORD** (330cc6b,
`TR3S2_2026-08-08.md`: TR-3′-GEN(ii) at order 2 proved-informal in the
§3.4-REV-11 form; units-only phrasing REFUTED on (ii); the ORD-INV
state-key bifurcation finding; hostile pass 1 returned CRITICAL 1C/2G —
core compose chain verifies, the universal key-divergence claim is the
critical — REVISION 1 dispatched, bar reset; NO status upgrade beyond
attempt-on-record); the F6 hygiene batch (112b9ac) incl. this document's
stale r22-era "wave 1 next" pointer now marked superseded. NO roster
change: **NINE rows** stand. HONEST COUNTER STATEMENT: VC25 was
NOT-CONFIRMED, so **the acceptance citation STAYS at the REVISION-20 text
(VC22 CONFIRMED)**; the owed confirming pass rolls forward onto THIS text:
**VC26 is OWED on the REVISION-23 wording** (charge at §29; model
diversity per the tree §4 bar).

**REVISION 24 applied (r25, appendix §30) — superseded as CURRENT standing
by the REVISION 25 paragraph below.** **VC26 +
VC26b CONFIRMED — the acceptance RE-ANCHORS on the REVISION-23 text** (ledger
2026-08-08, commit b6121a5): VC26 (full audit of REVISION 23) returned 0
critical / 1 gap — all four VC25 dispositions PASS at their sites, the
whole-operative sweep found ZERO surviving unbracketed (H4a)/(R1)–(R4) live
uses, the full 12-pattern occurrence census reproduced EXACTLY, NINE rows,
clause (R)/(UB) recounts 8/8, the line-anchored falsifier 0 hits — the single
gap an AUDIT-INPUT artifact (the provenance appendix omitted N-T4, which is
not a separate file but the §N NUMERIC-GATE RECORD embedded at
`H4M1_rpack_attempt.md:1091–1186`); VC26b (narrow confirming pass on the
completed appendix, the embedded §N slice verbatim) returned **CONFIRMED
0C/0G**, the auditor additionally re-executing the embedded N-T4 script
independently in-process (13/13 assertions PASS). Reports
`lean/notes/openmath/VC26_report.md` (565aa28) + `VC26b_report.md` (b6121a5).
**The acceptance anchor moves REVISION 20/VC22 → REVISION 23/VC26+VC26b**
(this citation-text update rides r25 per the VC22 precedent). REVISION 24
then folds the wave-8 ledger batch, NO roster change (**NINE rows** stand):
(1) **TR3-S2 ACCEPTED** (6663aa4; passes 2 Codex + 3 Fable consecutive CLEAN,
model-diverse, on its REVISION 1) — with accepted TR3-S1, **TR-3′-GEN CLOSES
AT ORDER 2 as a two-stratum THEOREM** in the S3.4 REV-11 form (GRB open lemma
#1 closed at order 2; deeper orders ≥ 3 OPEN; the units-only record phrasing
PERMANENTLY REFUTED on (ii) via unconditional Lemma WIT; ORD-INV on record;
TR-3-ORD-X-on-(ii) displayed proved conditional on (KP_i)+[T], not promoted);
(2) **REC-SL NARROWED** (suppliers 1adac62 + the KP-STEP [RC] wiring
`RecSLWire` 85e11a4): B-M2's REC-SL open is **NARROWED-TO the two displayed
rows** — the `hsc` StageCore-provenance row at successor-having recentering
frames on abstract carriers (the RECSL-SIGN carrier; discharged on all
engine-built towers) and the `hfence` GR0b recentering-chain fence — and
**CLOSED at every compiled site** (gates positive on H₃rc); (3) the
**readsOf debt RESTATED at the `ReadsOfFrom` face** (F3 walk verdict
(a)/NO-CONSUMER, 878ac17; B-M2 REVISION 16 APPLIED e7a673a) — the species
form is now a FENCE DISPLAY of the dead From→ReadsOf converse, not an
obligation; (4) **the huni face**: v3 REFUTED at the compiled td2
countermodel (375487c) → **HUNI-V2 v4 carrier repair LANDED** (058aaf3:
`TDDefsV4`/`LedgerLawfulV4`/`TDV4_chain`; `SlotUniformLaw` HOLDS at td2V4;
`FreshReadsLaw` DELIVERED; H2 statements byte-unchanged; B2D 0 sorries) —
huni is displayed-and-satisfiable at the v4 carrier, the v3 binder recorded
dead; (5) the **F5 deep-instance forge re-priced against `junc_bundle`
ALONE** (GR leg DECLINED at ROUTE (a); readsOf leg DEAD at no-consumer);
(6) hygiene: the synthesis-pass-5 F1(ii) ADM-U0 queue line adjudicated STALE
(repair already executed at H3 Wave 0, e1cf117, 2026-08-01 — not quoted in
any ROOT operative text; disposition at §30) + the StageCore docstring
doc-debt DISPLAYED (retired-FAITHLESS MovesGr cites; live chain = HC1
T1_baseStage/S9_transStage/S10_recStage — recorded, NOT edited this unit).
The §29 fence-(vii) ASVIN FLAG on GR ROUTE (a) STAYS; §30 records the wave-8
consequence under the same lineage (the RECSL suppliers consume the `hσt`
pin). HONEST COUNTER STATEMENT: the acceptance is anchored on the
REVISION-23 text (VC26+VC26b); REVISION 24 IS a text change — the confirming
end-to-end pass (**VC27**) is OWED on the REVISION-24 wording (charge at
§30; model diversity per the tree §4 bar); until it lands, the acceptance
citation points at the REVISION-23 text.

**REVISION 25 applied (r26, appendix §31) — superseded as CURRENT standing
by the REVISION 26 paragraph below.** **VC27 +
VC27b CONFIRMED — the acceptance RE-ANCHORS on the REVISION-24 text** (ledger
2026-08-08, "VC27 + VC27b CONFIRMED", commit 6aa3b20): VC27 (full audit of
REVISION 24) returned 0 critical / 2 gaps — ALL substantive checks PASSED
(3/3 re-anchor sites; no self-confirmation; TR3-S2 scope discipline; REC-SL's
exact two-row display; the readsOf fence + no-consumer disposition; huni v3
refutation + v4 repair both recorded; forge = junc_bundle alone with reasons;
the §29 ASVIN FLAG standing; the full 17-pattern census arithmetically exact;
NINE rows; clauses 8/8; falsifier 0) — both gaps AUDIT-INPUT appendix defects
(the N-T4 §N slice named but not pasted; the GRB bracket slice empty from a
wrong grep pattern; the recurring genre now a saved orchestrator checklist);
VC27b (the narrow confirming pass on the verbatim slices) returned
**CONFIRMED 0C/0G**. Reports `lean/notes/openmath/VC27_report.md` (5e8dc97) +
`VC27b_report.md` (6aa3b20). **The acceptance anchor moves
REVISION 23/VC26+VC26b → REVISION 24/VC27+VC27b** (this citation-text update
rides r26 per the VC22 precedent). REVISION 25 then folds the wave-9 ledger
batch, NO roster change (**NINE rows** stand): (1) **POOL-DICT DECIDED,
countermodel-first** (d7f7f58, `PoolDictProbe.lean`): the pass-4 F2a
derive-from-wiring route REFUTED by compiled underdetermination — two full
`KCutK1Tie` instances over ONE shared pack differing ONLY in `δOf` (the
`pd_blockSolve1_zero` mechanism), so no law over (C, R, hdet) can pin the
pool dictionary; the (H8′)(iii) wording STANDS as the honest form, now
legitimately blocked-on-owners ([2r]/CL-8 REL.2 + (K-CUT-g)) WITH THE BLOCKER
COMPILED; the τ-leg same-genre, not compiled (the two-equal-state table =
forge-class); (2) **the GRB TR-4 EMPIRICAL debt DISCHARGED** (4761cdd:
harness family TR4a–f, 19,226 samples, 0 violations, preregistered; TR-4
stays PAPER-SECURED on printed Cor 4.4(1)/4.5/4.11 — NO proof-status change;
GRB brackets applied additively); (3) **the CU2t k′=0 charter SPLIT-decided**
(9cb30d1, `CU2tKp0.lean`): literal derivability REFUTED at THREE compiled
gates (saturation; the −∞ leg; the terminal-blind CU2t index) + the honest
𝐇°-keyed reduction **`terminalPolySem_kp0` PROVED** (positive gate fires at
the REAL `gmnDataOrder1` read of X+2 — the first ReadThroughIota-certified
`TerminalPolySem` witness at HEAD), reducing the face to the **NEW NAMED OPEN
ROW `BasePolygonLengthLaw p`** ([M]-discipline; pure polygon geometry, the
OL5-adjacent convexity gap; displayed at the (H6) row's residuals — a
DictIII-side open lemma, NOT a §3.1 roster row); (4) **leakFree TYPED**
(d237149, `B2D/LeakFaceV4.lean`): the typed carrier `LeakFreeCarrierV4` +
the general supplier `ledger_leakFreeV4` (every `LedgerLawfulV4` datum) +
the de-vacuity fence compiled; the TransDeep rows carry a PROVED (L) field;
the H2 `TransDeep.leakFree` FIELD stays bare per the U9 fold; still owed:
the Step-10/K1 classifier consumer face + a nonempty-parent lawful instance
(forge-class); (5) **D-ITER RESTATED** (3bcae68): the **ITER-LAW is the
STATEMENT OF RECORD** (open lemma, instance-true 0/804; the single-ζ′ cyclic
form REFUTED 22/28 and bracketed at all display sites) + the
`DIterJunctionSupplier` consumable spec on record (three mandatory features,
cyclic kernels fenced; queue #11 unblocked on #9); (6) **the FORGE
CONSOLIDATION display**: the forge-class target list now has THREE named
targets — `junc_bundle` (the surviving F5 justification), the
nonempty-parent lawful leakFree instance, the two-equal-state table
(POOL-DICT τ-leg) — the consolidated forge question; chartering decision =
synthesis pass 6 (OWED). The §29 fence-(vii) ASVIN FLAG and its §30 lineage
STAY. HONEST COUNTER STATEMENT: the acceptance is anchored on the
REVISION-24 text (VC27+VC27b); REVISION 25 IS a text change — the confirming
end-to-end pass (**VC28**) is OWED on the REVISION-25 wording (charge at
§31; model diversity per the tree §4 bar); until it lands, the acceptance
citation points at the REVISION-24 text.

**REVISION 26 applied (r27, appendix §32) — superseded as CURRENT standing
by the REVISION 27 paragraph below.** **VC28
CONFIRMED FIRST-PASS — the acceptance RE-ANCHORS on the REVISION-25 text**
(ledger 2026-08-08, "VC28 CONFIRMED FIRST-PASS", commit d66bef2): VC28 (full
audit of REVISION 25) returned **CONFIRMED, 0 critical / 0 gaps — the FIRST
first-pass confirmation of the cadence**: all folds verified at their sites
(anchor citations; POOL-DICT displayed derivation-route-dead with the row
intact; TR-4 empirical-only; BasePolygonLengthLaw displayed NOT rostered,
roster NINE; the leakFree honesty display; the D-ITER open-lemma discipline;
the three-target forge display); the 26-pattern census reproduced exactly
(incl. the VC27/VC27b substring relationship); provenance fully resolved
(the appendix built per the saved vc-audit-appendix-checklist — its first
validation). Report `lean/notes/openmath/VC28_report.md` (d66bef2). **The
acceptance anchor moves REVISION 24/VC27+VC27b → REVISION 25/VC28** (this
citation-text update rides r27 per the VC22 precedent). REVISION 26 then
ADOPTS SYNTHESIS PASS 6 (`docs/SYNTHESIS_PASS6_2026-08-08.md`, 976484d) as
the dispatch of record, NO roster change (**NINE rows** stand): (1) **THE
FORGE DECISION (pass-6 F1)** — the three consolidated targets are three
SPECIES (OM tower runs / B2D ledger data / K-CUT tables; no shared
infrastructure beyond the discipline), priced separately:
**`junc_bundle` CHARTERED ALONE** (price L, staged numerics-first — stage 1
the deep compiled deg f > deg Φ₁ run with REAL digit data, the target tower
preregistered from strata-probe/BM2_TRACE numerics and the clause-(iii)
out-of-carrier residual traced numerically BEFORE the Lean build; stage 2
`gate_glue_junction` fired un-hypothesized; stage 3 the
`DIterJunctionSupplier` sanity gates, ITER-LAW consumed only as the
displayed hypothesis) — the ONLY target with a live consumer chain (the
last SITE-EXH displayed residual; the `DIterJunctionSupplier` substrate;
B-M2's last non-hypothesis-shaped wall); the **nonempty-parent lawful
leakFree instance DECLINED** (disposition displayed at the (FRESH) row:
revisit ONLY if the classifier consumer face, once stated, demands a
nonempty-parent witness for its own non-vacuity) and the **two-equal-state
table DECLINED** (disposition displayed at the (H8′) row: a second leg of a
compiled blocker is not progress); the GR stage-forge stays DECLINED, the
readsOf run-forge stays DEAD; (2) **`BasePolygonLengthLaw p` CHARTERED HIGH
(pass-6 F2, price M)** — it IS the recorded convexity gap, honestly priced
(H-free, D-free pure polygon geometry; route quarried from PROVED kernels,
the genuinely new machinery the negative-prefix telescoping; unlocks
`terminalPolySem_kp0_of_law` stratum-wide); (3) **the F3 collapse map**:
the Step-10/K1 classifier consumer face is now ATTEMPT-GRADE (one
statement-design unit, fired at `td2V4`, fence mandatory); the `hsc` row is
PERMANENTLY hypothesis-shaped (records bundle NO `StageCore`; the round-2
fields determine only σ.R(−1)² = 1 — hypothesis at abstract carriers,
closed at every compiled site; optional S two-instance witness only, folded
into a passing H1 unit); deeper strata are NOT yet finite-algebra (the
ǫ-recurrence can REVIVE at order 3 even on e₀ = 1) — next candidate ORDER 3
all-e_j = 1, entry a preregistered depth-3 probe; junc_bundle is B-M2's
ONLY non-hypothesis-shaped wall; (4) **the (ADM) row status CORRECTED: NOT
BLOCKED** — the adjudicating gate **G-ADM-2** (HDISCHARGE_H3 §1.4: log
dim G_{β_k} vs d at n ≤ 5, p ∈ {2,3,5}) had NEVER RUN; it routes track A
(prove ADM-REAL over realized shape families) vs track B (the U6a/U6b CEN-W
generalization, statement-change-gated) and is the row's countermodel-first
entry (dispatched at wave 10 — preregistration SEALED pre-run at 4d7dba1;
results ride r28, no outcome claimed here); (5) **the sorry-census refresh
(pass-6 F6)**: re-measured at HEAD, **36 sorry sites repo-wide, B2D 0** —
re-verified at r27 under the strict non-comment unit as 35 in `LeanUrat/`
+ 1 in the tracked scratch probe `scratch_S7_escalation.lean`, leaders
V9_K1nonrec ×4 / SlotsG2_relFamily ×4 / TV_G3 ×2 / G1_toyGate ×2 and the
banked classifier sorry all reproduced (`REMAINING_CENSUS_2026-08-01.md`
predates the window and is superseded by this display); (6) housekeeping:
the pass-6 queue is the DISPATCH OF RECORD (wave 10 dispatched — queue
units #2–#6 running concurrently with this fold; results ride r28); the
pass-5 execution audit returned CLEAN (dropped-again count ZERO); the
StageCore docstring doc-debt ASSIGNED to the first landing Lean unit; the
standing Asvin items re-displayed (the `TowerMoveClosureL` two-layer
re-ratification; the fence-(vii) ROUTE (a) ratify-or-reopen incl. the RECSL
`hσt`-pin rider; the H4M7 §4 Route-B re-scope, teed up by the queued (UB-X)
consumer-need walk). The §29 fence-(vii) ASVIN FLAG and its §30 lineage
STAY. HONEST COUNTER STATEMENT: the acceptance is anchored on the
REVISION-25 text (VC28); REVISION 26 IS a text change — the confirming
end-to-end pass (**VC29**) is OWED on the REVISION-26 wording (charge at
§32; model diversity per the tree §4 bar); until it lands, the acceptance
citation points at the REVISION-25 text.

**REVISION 27 applied (r28, appendix §33) — superseded as CURRENT standing
by the REVISION 28 paragraph below.**
REVISION 27 is the **WAVE-10 FOLD BATCH** — the five dispatched units'
ledger-recorded results folded into the standing rows; NO roster change
(**NINE rows** recounted); NO pass ran at this unit (VC29 has NOT yet run;
the acceptance stays anchored on the REVISION-25 text, VC28). The folds:
(1) **`BasePolygonLengthLaw p` is PROVED — for EVERY prime, sorry-free,
Lean-core** (a6fcc4d; `Scaffold/DictIII/BasePolyLength.lean`,
`basePolygonLengthLaw_proved`, statement VERBATIM as displayed at the (H6)
residuals; note `BPLL_2026-08-08.md`): the abstract negative-prefix hull
core `BPLL.npSides_neg_length_sum` (height sign law with two NEW valid-line
constructions + the `dropCollinear` merge invariant + the negative-prefix
telescope), per-side e ∣ length via `Rat.den_dvd`; **the recorded
OL5-adjacent convexity gap was NEVER HIT** (chord-attainment on sides not
needed — convexity enters only via `slope_mono_adjacent` at on-hull
points); compiled gates `gate_hensel` + `gate_eisenstein_sum` green, all
footprints Lean-core. **CONSUMER FIRED: `terminalPolySem_kp0_unconditional`**
— the FIRST unconditional `TerminalPolySem` supplier at an 𝐇°-keyed k′ = 0
seam: **the (H6) (T-READ) k′ = 0 face's one numerical input is now
THEOREM-BACKED**; the BasePolygonLengthLaw open-row display at the (H6)
residuals is RETIRED TO PROVED STATUS (it was never a §3.1 roster row; the
−∞ corner stays the separately-priced open leg; `Kp0GateC` stands; C2 at
k′ ≥ 1 untouched). (2) **FORGE `junc_bundle` STAGE 1 ALL GREEN**
(023a9d7 preregistration SEALED pre-run + 53f5d03; 10/10 preregistered
rows, 4,116 samples, 28 tower instances, first-pass; note
`JUNCFORGE_S1_2026-08-08.md`): the clause-(iii) residual PINNED at nature —
the MEASURED TWO-SCALE LAW (the recorded (a, Ranch, μ) live exactly at the
read-pair scale 336/336; every strictly coarser stage scale reads the
top-vertex key-power monomial 336/336), displayed as a **MEASURED
reduction, NOT proved**: at a read-pair-graded stage clause (iii) is
interface-DERIVABLE from clauses (i)+(ii) — numerics-backed route, no Lean
discharge claimed; the terminal deg f = deg Φ residual = the rank-2
binomial R(B₀) + T^s with the digit coordinate the 𝒟₂-anchor coordinate
(u₀'s class exact on designed anchors 56/56); ITER-LAW falsifier record
EXTENDED 0/840 on run-realized classes (the lemma stays OPEN). **THE D0
STRATUM FLAG (displayed, NOT asserted):** `gate_glue_junction`'s hbundle
instantiates the degenerate f = key stratum where a recentering-shaped
bundle appears UNSATISFIABLE at the unique development — an INSTANCE-FALSE
FLAG on the gate's hypothesis, PENDING the orchestrator adjudication riding
forge stage 2 (one Lean check owed: monic-key development uniqueness); no
gate display changes at this revision. STAGE-2 SPEC ON RECORD (JUNCFORGE_S1
§S5): row A over ℤ₂, the junction stage built at the read pair
(e′,h′) = (2,3) — the first compiled e ≥ 2 junction. The StageCore
docstring debt is CLEARED (the F6 rider: `Moves/DefsCore.lean` synced to
the live HC1 chain, git-diff-verified COMMENT-ONLY). (3) **G-ADM-2
EXECUTED — the (ADM) row routes to TRACK A** (38ac01e; preregistration
sealed 4d7dba1; 198 rows, 13 configs, P1–P7 ALL PASS; note
`GADM2_2026-08-08.md`): the sealed disposition verbatim — RIGHT-END-ONLY
PARTIAL, NO track-B fire, track A LIVE as **(ADM-REAL′)**; the ADM-U7
dichotomy CONFIRMED at scan scope (all 85 partials at the ρ-pinned
right-end slot CEN-J never charges; off-end partials ZERO). **The no-hit
discipline displayed: the scan ADJUDICATES ROUTING ONLY, it proves
NOTHING** — the (ADM) row stays [M] open; next (ADM) unit = the
(ADM-REAL′) attempt per `ADM_U7_TRACKA_ATTEMPT.md`. (4) **the Step-10/K1
classifier consumer face FIRED** (380cc37; `B2D/K1ConsumerFace.lean`, 8
NEW decls, zero statements touched, Lean-core; note
`K1FACE_2026-08-08.md`): **`K1ConsumerFace`** stated (interior on-line
slots' reads factor through off-parent labels; k_s excluded as clause (c)'s
parent charge), the exact-strength pin `k1ConsumerFace_iff_parentBlind`
(the face ⟺ parent-blind descent — exactly the (L) content, the R2-C1 bar
respected consumer-side), supplied through the v4 chain and **FIRED at
`td2V4`** with the mandatory de-vacuity fence `td2V4bad_not_k1ConsumerFace`
compiled — **the (FRESH) residual "Step-10/K1 classifier consumer face" is
DISCHARGED** at the v4 supplier chain + fence. HONESTY: parentBlock = ∅ at
all compiled instances (content = suppliers + fence); the nonempty-parent
instance STAYS DECLINED (its revisit trigger did NOT fire); NO tie claimed
to the Step-18 K1 exact form. (5) **the (H7) first probe DECIDED AT THE
INSTANCE BIN** (2c72c5f; `Scaffold/HDischarge/H7/H7Probe.lean`, 12 thms
Lean-core; note `H7PROBE_2026-08-08.md`): (I-τ) and full K-LOC are
**VACUOUS-BY-TYPE at every compiled instance** in the bin — no
countermodel obtainable, the row's statements STAND unrepaired; the
corpus's only m ≥ 2 site (toyTreeA/tA1) certified card-2 but TRIPLY
DEGENERATE as a decision point; the one compiled-decidable fragment KL-1a
(order-1 count face) HOLDS at all three bin fields (ℤ/3, F4, ℤ/2); OL-2
SHARPENED to the exact inhabitant spec; **the (H7) route of record is now
N-H7-1 NUMERICS FIRST**. (6) housekeeping: **wave 11 IS DISPATCHED**
(forge stage 2 with the D0 adjudication; the (ADM-REAL′) attempt; the
depth-3 probe, in flight from wave 10 with no result yet; the N-H7-1
numerics — results ride r29, nothing from them claimed here). The §29/§30
fence-(vii) ASVIN FLAG lineage STAYS. HONEST COUNTER STATEMENT: the
acceptance is anchored on the REVISION-25 text (VC28); VC29 has NOT yet
run and REVISION 27 IS a text change — the confirming end-to-end pass
(**VC29**) ROLLS FORWARD and is OWED on the REVISION-27 wording (charge at
§33; model diversity per the tree §4 bar); until it lands, the acceptance
citation points at the REVISION-25 text.

**REVISION 28 applied (r29, appendix §34) — superseded as CURRENT standing
by the REVISION 29 paragraph below.** **VC29
CONFIRMED FIRST-PASS — the acceptance RE-ANCHORS on the REVISION-27 text**
(ledger 2026-08-08, "VC29 CONFIRMED FIRST-PASS", commit 3d457cc): VC29
(full audit of REVISION 27) returned **CONFIRMED, 0 critical / 0 gaps —
the SECOND consecutive first-pass confirmation of the cadence** (the
appendix checklist holds twice). Report `lean/notes/openmath/VC29_report.md`
(3d457cc). **The acceptance anchor moves REVISION 25/VC28 → REVISION
27/VC29** (this citation-text update rides r29 per the VC22 precedent);
the LINEAGE, displayed: VC29 was owed on REVISION 26 and REVISION 26 was
properly SUPERSEDED MID-CADENCE by REVISION 27 (the r28 roll-forward per
the REVISION-20 precedent), so the pass ran on the REVISION-27 wording and
NO pass ever ran on the REVISION-26 text. REVISION 28 then folds the
WAVE-11 acceptance/verdict batch; NO roster change (**NINE rows**
recounted): (1) **the `junc_bundle` VERDICT OF RECORD** (forge S2 arc:
036ee8c the six-theorem Lean kernel + the REVISION 1–4b faithfulness
hardening + 09f93c1 arc closure, 5 audit rounds to CLEAN 0C/0G; note
`JUNCFORGE_S2_2026-08-08.md`): the compiled CHAR-2, Φ₂-LANDING,
INTERIOR-Φ₂A-PINNED `junc_bundle` is **INSTANCE-FALSE at row A**
(kernel-proved; the parity wall `juncforge_no_e2_junction` ALONE is
char-free/Φnext-free and explains the C-e escapee AT THE ROW-A READ);
`gate_glue_conditional` is **VACUOUS at its compiled instance** — now a
THEOREM (`juncforge_gate_hbundle_false`, definitional-equality certified,
+ `juncforge_gluePacket_false` the independent leg); `DIterJunctionSupplier`
NOT refuted; the SOURCE-LEVEL question OPEN (the
incompatible-transcription-scales reading — a SideReads repair could
RESTORE the paper-source glue step); **the FGMN transcription-scale
re-derivation design unit CHARTERED** (JUNCFORGE_S2 §R3 — PROPOSAL-ONLY,
statement-change genre, NO application without ROOT adjudication: the
ASVIN-FLAG decision point, displayed in the §29/§30/§33 flag lineage);
`junc_bundle` LEAVES the open-wall list and becomes this displayed verdict
+ charter. (2) **ADM-REAL′ ACCEPTED** (ledger 24e51df; passes 3 (Codex) +
4 (Fable, 24a595c) consecutive CLEAN 0C/0G, model-diverse; note
`ADMREAL_2026-08-08.md` REVISION 2): **(CLASS-d) is a lattice THEOREM at
every order** (Lemma **CLASS-LAT**, unconditional — the (GR-B)
conditionality REMOVED from the track-A chain); NON-END + LOC + W-CAP
stand at full any-type scope (the GMN Lemma 2.14 route); the (ADM) row's
track-A attempt is DISCHARGED to the displayed opens: **(B-PIN)** (the
boundary hypothesis — FULL-at-the-L′-slots conjunct only) + the
**(C-JCT)** fan-out audit + **F-ADM-3** (the CensusCore `wphi`
missing-rescale finding, verified at `CensusCore.lean:155` — EDIT-GATED
design input for the carrier re-key, no edit made). (3) **the depth-3
probe MEASURED + TR3-S3 IN VERIFICATION**: (IDENT-3) EXACT at the sealed
first pass (54c518d — 21,950 samples, 0 violations; C^F3 = c^G3
identically 0/1,656; no ǫ-revival on the all-e_j = 1 stratum at probe
scope; note `STRATA3_2026-08-08.md`); the chartered compose unit TR3-S3
COMPOSED (482150b, preregistered b4a142b — TR-3′-GEN at order 3,
all-e_j = 1, conditional on (H-v3)) and now at REVISION 3 (f05df77) inside
its verification arc: pass 2 Codex CLEAN (pre-revision), pass 3 Fable
GAPS-ONLY, pass 4 Codex GAPS-ONLY, pass 5 running — counter 0/2, **NO
status upgrade claimed here** (the pass-3 verifier's independent 93-sample
external leg on record). (4) **N-H7-1 EXECUTED** (f8aec25;
`h7_kloc_probe.py`, sealed pre-run; note `NH71_2026-08-08.md`): the (H7)
(I-τ)/K-LOC pair **SURVIVES FIRST CONTACT at six REAL m ≥ 2 sites** (139
PASS / 8 FAIL classified / 26 INFO) — with TWO MEASURED RESTATEMENT PINS
for the future Lean forge (PIN 1: ε must be SLOPE-KEYED — the slope-pooled
strengthening REFUTED at S5; PIN 2: the D-8 guard measured as the exact
extraction round-trip boundary); statements still OPEN, nothing proved.
(5) **the JFS2 faithfulness arc CLOSED** (09f93c1; 5 rounds: 4 CRITICAL →
round 5 CLEAN 0C/0G — the note of record now states exactly what the Lean
theorems prove); ops discipline records, one line each: the
vc-audit-appendix checklist VALIDATED twice (VC28 + VC29 first-passes);
the pkill self-match trap saved to memory after three exit-144 self-kills.
(6) housekeeping: **SYNTHESIS PASS 7 becomes OWED** (the wave 10–13 window
created new collapses — CLASS-LAT, the junc_bundle verdict, the BPLL
theorem, the v4 chain — a re-map is due per the /goal cadence); the Asvin
batch re-displayed (the `TowerMoveClosureL` two-layer re-ratification; the
fence-(vii) ROUTE (a) flag lineage — now ALSO carrying the FGMN
transcription-scale charter alongside the RECSL `hσt`-pin rider; the
Route-B re-scope awaiting the (UB-X) walk note). HONEST COUNTER STATEMENT:
the acceptance is anchored on the REVISION-27 text (VC29); REVISION 28 IS
a text change — the confirming end-to-end pass (**VC30**) is OWED on the
REVISION-28 wording (charge at §34; model diversity per the tree §4 bar);
until it lands, the acceptance citation points at the REVISION-27 text;
**never self-confirm** — this unit ran no audit.

**REVISION 29 applied (r30, appendix §35) — superseded as CURRENT standing
by the REVISION 30 paragraph below.** **VC30
+ VC30b CONFIRMED — the acceptance RE-ANCHORS on the REVISION-28 text**
(ledger 2026-08-08 "VC30 + VC30b CONFIRMED", commit 6984d5d). The
r29-ERRATUM LINEAGE, displayed at every operative site per the fold rule:
VC30 (full audit of REVISION 28) returned **1 critical / 0 gaps** — the
critical: the §34 closing line's UNQUALIFIED `junc_bundle` restatement,
the one verdict-tier site that omitted the three restrictions (the
qualifier-propagation defect class's last appearance); every fold, the
38-pattern census, roster NINE, and all discipline checks otherwise
passed (report `lean/notes/openmath/VC30_report.md`, 573bd1b). The
**r29-ERRATUM** applied the three restrictions in a dated
pre-confirmation bracket at that line (573bd1b), and the narrow
confirming pass **VC30b returned CONFIRMED 0C/0G** on the
errata-corrected text (report `VC30b_report.md`, 6984d5d — the
VC26b/VC27b narrow-companion genre). **The acceptance anchor moves
REVISION 27/VC29 → REVISION 28/VC30+VC30b** (the citation-text update
rides r30 per the VC22 precedent); now INSIDE the certified anchor: the
`junc_bundle` verdict of record + the FGMN charter (the Asvin flag), the
ADM-REAL′ acceptance (CLASS-LAT), the N-H7-1 pins, the JFS2 arc, the
strata3 measurement. On the newly re-anchored text REVISION 29 folds ONE
acceptance: **TR3-S3 ACCEPTED** (ledger 96b7f54; note
`TR3S3_2026-08-08.md` REVISION 4; the 2-clean model-diverse bar MET —
pass 7 Codex CLEAN 0C/0G, dc40668, + pass 8 Fable CLEAN 0C/0G, 83870a8,
consecutive; the eight-pass arc with four orchestrator-applied precision
revisions; **THREE independent from-note verifier implementations** — 93
+ 1,564 + 821 checks, 0 failures — the strongest external-leg record of
any accepted note): with accepted TR3-S1 + TR3-S2, **TR-3′-GEN is PROVED
on {all order-2} ∪ {order-3 all-e_j = 1}, CONDITIONAL ON (H-v3)** — the
order-3 ordinate identity v₃ = w₂, ATTRIBUTION ONLY: the finite identity
σ₂(R^F₃(f)) = R_λ₃(f), discrepancy ≡ 1, is UNCONDITIONAL as algebra and
(H-v3) carries the GMN-operator attribution — with **PIN-GEN-i** the
displayed attribution pin (the owed non-textual leg: a fresh
`pdftotext -layout` re-extraction of FGMN at Defs 3.15/3.16/3.18,
hand-verified at the instantiated indices); the (H1) member-(a) display
and the GRB note's S3.4 scope line carry the enlarged union (the GRB
second post-acceptance bracket applied ADDITIVE — that leaf's acceptance
counters NOT reset); **deeper strata stay OPEN, probe-first** (e_j ≥ 2
strata at orders ≥ 3 stay fenced; the sealed UNTESTED (TWIST-3)
candidate displayed — a compose unit fires only on an exact measured
law). NO roster change (**NINE rows** recounted); no clause attribution
moved. Housekeeping: **SYNTHESIS PASS 7 is RUNNING** (in flight — its
report becomes the next dispatch of record WHEN IT LANDS; nothing is
claimed from it here); the Asvin batch re-displayed (the
`TowerMoveClosureL` two-layer re-ratification; the fence-(vii) ROUTE (a)
flag lineage incl. the FGMN transcription-scale charter alongside the
RECSL `hσt`-pin rider; the Route-B re-scope awaiting the (UB-X) walk
note). HONEST COUNTER STATEMENT: the acceptance is anchored on the
REVISION-28 text (VC30+VC30b); REVISION 29 IS a text change — the
confirming end-to-end pass (**VC31**) is OWED on the REVISION-29 wording
(charge at §35; model diversity per the tree §4 bar); until it lands,
the acceptance citation points at the REVISION-28 text; **never
self-confirm** — this unit ran no audit.

**REVISION 30 applied (r31, appendix §36) — superseded as CURRENT standing
by the REVISION 31 paragraph below.** **VC31
+ VC31b + VC31c CONFIRMED — the acceptance RE-ANCHORS on the
(errata-corrected) REVISION-29 text** (ledger 620417b "VC31 + VC31b +
VC31c CONFIRMED"). The VC31 ARC LINEAGE, displayed at every operative
site per the fold rule: VC31 (full audit of REVISION 29) returned **0
critical / 2 gaps** (the 43-pattern census EXACT, NINE rows, every fold
verified; gap 1 = a truncated appendix slice, audit-input — the saved
checklist extended to completeness; gap 2 = the (H1) fence display named
only the e_j ≥ 2 complement) → the dated **r30-ERRATUM** (f9aa7ef) made
the fence name the ENTIRE complement of the proved union (orders ≥ 4
incl. all-e_j = 1 explicit; display only — the proved union was always
exact) → VC31b PASSED both substance checks and raised the dating
observation → the standing **DATING CONVENTION disclosure** (a63b9ef:
window-label dating on artifacts/entries; git commit order = the
authoritative chronology) → **VC31c CONFIRMED 0C/0G** (620417b; the
sequencing claims verified from the machine-recorded commit chain).
**The acceptance anchor moves REVISION 28/VC30+VC30b → REVISION
29/VC31+VC31b+VC31c** (the citation-text update rides r31 per the VC22
precedent); now INSIDE the certified anchor: the TR3-S3 acceptance (the
strata trilogy), the erratum-corrected fence, the dating disclosure
itself. On the newly re-anchored text REVISION 30 folds the WAVE-14
batch: (1) **the FGMN transcription-scale re-derivation PROPOSAL ON
RECORD, NOT applied** (303b11e; note `FGMN_REDERIVATION_2026-08-08.md`) —
the diagnosis: the compiled `SideReads` clauses (i)/(ii) DOUBLE-SCALE the
stride weight vs the print's parent-valuation keying (the window
functional belongs at the parent scale σ.wPrev; the JuncForge parity
wall is the artifact's arithmetic shadow); THE ROW-A SOURCE VERDICT
(defined term, note §S4): the source glue step FIRES at row A; the cure
= a FOUR-TOKEN re-key σ.w → σ.wPrev (verbatim diff at note §S3) —
hostile pass 1 LANDED 0C/4G + REVISION 1 applied (236d85c: any-gam branch repaired, rerun 0/336 with the ≥2-distinct-values support meter; the R1 claim rescoped to the window/support check) [r31-ERRATUM, VC32 gap 1], application ASVIN-GATED (the flag lineage's TOP
item); the JuncForge refutation theorems REMAIN TRUE of the old clause;
**PIN-GEN-i DISCHARGED** as the rider (fresh column-verified `-layout`
extraction at Defs 3.15/3.16/3.18; Def 3.15's prime placement
COLUMN-VERIFIED; the PDF sha256-pinned into `docs/references/`); (2)
**the (H-v3) DISCHARGE ON RECORD, pending verification** (f90d68a; note
`HV3_DISCHARGE_2026-08-08.md`; hostile pass 1 LANDED 0C/2G + REVISION 1 applied (bc34acf: condition (iv) explicit; the D3 claim rescoped) — the conditionality list is now FOUR conditions [r31-ERRATUM, VC32 gap 2]) — **HV3-THM**
(defined term): v₃ = w₂ at e₀ = e₁ = 1 (e₂ free) on THREE displayed
conditions (the pinned-print baseline, accepted TR3-S1, the standing
(H-CHAIN)/RESCALE consumptions); IF it survives its bar, TR-3′-GEN's
order-3 scope becomes UNCONDITIONAL ON PRINT; NO bracket edits yet (the
47 listed (H-v3) sites ride the acceptance); (3) **F-ADM-3 RE-KEY
APPLIED** (ba02d60+8648d99; the standing definition-change authority,
Codex diff confirmation CONFIRM): `wphi` re-keyed to the O-9 §4
top-normalized ledger; **CLASS-LAT MACHINE-CHECKED at every order**
(`classCard_eq_d`, Lean-core) and ADM-U2's `hclass` RETIRED
(`classCount_eq_d` proved, `admFull_iff_maxClassWeight` hypothesis-free);
33-module cone green, capstone fence EMPTY intersection; the R7
acceptance test PASSED (the F-ADM-2 88-type grid → 0 failures); (4)
**the TWIST-3 LAW MEASURED EXACT** (76513f8+48c07b4): the ǫ-recurrence
REVIVES at order-3 e₁ ≥ 2 with EXACTLY the sealed character — θ₂ =
ℓ₁g₁γ₂, 0/1,656 — with the genre's first PARI-certified RAMIFIED
order-3 keys; the honest P4′ SECONDARY-display RED disclosed (post-hoc
explained, harness unamended, exit 1 stands); **TR3-S4 CHARTERED** (the
route on record; verifier-owned implementation required at acceptance);
(5) **the (UB-X) WALK ON FILE** (4924dcb): **RB-DELTA** (defined term)
is **CONSUMER-VISIBLE, not display-only** (the three-site consumption
map; F-B (1)–(4) satisfied; condition (5) = Asvin sign-off + the ROOT owner's acceptance pass, the ONLY open gate [r31-ERRATUM, VC32 gap 3: harmonized to the §36 disposition wording]) —
the Route-B decision paragraph is DECISION-READY and joins the Asvin
batch. NO roster change (**NINE rows** recounted); no clause attribution
moved. Housekeeping: **SYNTHESIS PASS 8 NOT YET OWED** (pass 7's queue
still executing); wave-15 candidates displayed (the TR3-S4 compose; the
held pass-7 items — the ITER-LAW attempt, the (H7) restatement forge,
the `DIterJunctionSupplier` statement, the banked-sorry attempt); the
Asvin batch RESTATED IN FULL (the `TowerMoveClosureL` two-layer
re-ratification; the fence-(vii) ROUTE (a) ratify-or-reopen lineage —
the GR route (a) + the RECSL `hσt`-pin rider + the EXECUTED F-ADM-3
re-key recorded for ratify-or-reopen; the FGMN four-token proposal AT
TOP; the Route-B RB-DELTA decision, decision-ready per the walk).
HONEST COUNTER STATEMENT: the acceptance is anchored on the REVISION-29
text (VC31+VC31b+VC31c); REVISION 30 IS a text change — the confirming
end-to-end pass (**VC32**) is OWED on the REVISION-30 wording (charge at
§36; model diversity per the tree §4 bar); until it lands, the
acceptance citation points at the REVISION-29 text; **never
self-confirm** — this unit ran no audit.

**REVISION 31 applied (r32, appendix §37) — superseded as CURRENT standing
by the REVISION 32 paragraph below.** **VC32
+ VC32b CONFIRMED — the acceptance RE-ANCHORS on the (errata-corrected)
REVISION-30 text** (ledger e32ebef "VC32b CONFIRMED"). The VC32 ARC
LINEAGE, displayed at every operative site per the fold rule: VC32 (full
audit of REVISION 30) returned **0 critical / 3 gaps** — all folds
PASSED (the FGMN proposal-only discipline; no HV3 scope leak; the
F-ADM-3 flag-lineage record; the TWIST-3 honest RED; the UBX gate;
roster NINE; the 51-pattern census EXACT); the gaps were ALL
fold-lag/wording syncs → the dated **r31-ERRATUM** (e3425b1: the FGMN/HV3
pass-1 status displays synced to LANDED + REVISION 1 applied; the
336-SAT claim rescoped to the window/support check; the (UB-X) gate
wording harmonized to the §36 disposition) → **VC32b CONFIRMED 0C/0G**
(e32ebef — the narrow companion on the corrected passages verbatim, the
VC30b/VC31b genre). **The acceptance anchor moves REVISION
29/VC31+VC31b+VC31c → REVISION 30/VC32+VC32b** (the citation-text update
rides r32 per the VC22 precedent); now INSIDE the certified anchor: the
FGMN proposal-on-record with the source-fires verdict, the HV3-THM
discharge-pending display, the APPLIED F-ADM-3 re-key, the TWIST-3 law +
TR3-S4 charter, the UBX walk verdict, the four-item Asvin batch. On the
newly re-anchored text REVISION 31 folds: (1) **HV3 ACCEPTED — (H-v3) is
DISCHARGED** (fa2180e; note `HV3_DISCHARGE_2026-08-08.md` ACCEPTANCE
RECORD): passes 4 (Codex) + 5 (fresh Fable, independence-fenced,
b2e5b20) consecutive CLEAN 0C/0G, model-diverse — the pass-5 verifier
re-fetched the arXiv source independently (byte-identical to the pin),
ran a fresh 1,655-check two-way falsifier at off-roster p ∈ {7,17}, and
ran an ADVERSARIAL CONTROL (type-hood deleted → exactly the step-5 leg
breaks); **HV3-THM stands: v₃ = w₂ at e₀ = e₁ = 1 (e₂ free) on its FOUR
standing conditions** (the pinned-print attribution baseline; accepted
TR3-S1; the standing (H-CHAIN)+RESCALE consumptions; the standing
projective-dictionary consumptions — all standing rows, no bespoke
hypothesis). CONSEQUENCE, executed THIS revision: **TR-3′-GEN is
PRINT-UNCONDITIONAL at {all order-2} ∪ {order-3 all-e_j = 1}** — the
strata trilogy's only non-print hypothesis is gone (nothing becomes
machine-checked or attribution-free that wasn't) — and **the (H-v3)
bracket DISCHARGE SWEEP is EXECUTED** (the r31 display's "47 listed
sites ride the acceptance" duty): every operative (H-v3) conditionality
bracket in `TR3S3_2026-08-08.md`, this document, and
`GRB_ORDER2_2026-08-05.md` gains the additive dated bracket
"DISCHARGED at HV3 acceptance fa2180e — the scope is UNCONDITIONAL ON PRINT per
HV3-THM's four standing conditions" (old bracket text stays; accepted
notes get the post-acceptance-bracket treatment, counters NOT reset;
the site reconciliation vs the D7 census of 47 is displayed at §37);
**PIN-GEN-i stays DISPLAYED** (the FGMN-side attribution pin — its owed
leg was discharged SEPARATELY at 303b11e, the FGMN §S7 rider); the
e₁ ≥ 2 (TWIST-3)/TR3-S4 regime is NOT reached by HV3-THM (e₁ = 1
required) — those displays keep the hypothesis. (2) **FGMN STATUS SYNC —
pass 5 CRITICAL → REVISION 4** (af88f12 + fa2180e): the REVISION-3
mirror enumeration was INCOMPLETE — the verifier's complete DictIII site
list is ADOPTED at REVISION 4 (the `SideClauseR2`/`sideReads_r2` pair;
the whole (†)/`hdag` transfer layer, where the corrected-scale row is
STRICTLY STRONGER than (†) supplies at e ≥ 2 — the twins need RE-DERIVED
readCeil arithmetic; `Window.lean` :554/:557; the R5 tie family); the
application change-set is PRICED UP accordingly in the Asvin-batch
display (no longer four tokens + mirrors: the complete grep-disciplined
DictIII perimeter); §S8's raw-order disclosure moved to the header;
counter 0/2, passes 6+7 decide — **the proposal stays ASVIN-GATED,
nothing applied** [r32-ERRATUM, VC33 gap 1: FGMN pass 6 (c7df512,
0C/2G GAPS-ONLY) landed CONCURRENTLY with this fold's assembly —
c7df512 precedes this revision's commit in the authoritative
chronology — and is NOT folded here; it rides the next fold. The
displays stand: counter 0/2, the proposal ASVIN-GATED]. NO roster change (**NINE rows** recounted); no clause
attribution moved. Housekeeping: **SYNTHESIS PASS 8 NOT YET OWED** (the
FGMN arc still executing [r32-ERRATUM, VC33 gap 1: pass 6 landed
concurrently, rides the next fold]); wave-15 candidates displayed (**TR3-S4 the
lead** — the compose unit on the measured TWIST-3 law; the held pass-7
items — the ITER-LAW attempt, the (H7) restatement forge, the
`DIterJunctionSupplier` statement, the banked-sorry attempt); the Asvin
batch RESTATED IN FULL (the `TowerMoveClosureL` two-layer
re-ratification; the fence-(vii) ROUTE (a) ratify-or-reopen lineage —
the GR route (a) + the RECSL `hσt`-pin rider + the EXECUTED F-ADM-3
re-key recorded for ratify-or-reopen; the FGMN proposal AT TOP, now
priced at the COMPLETE DictIII change-set incl. the re-derived readCeil
arithmetic for the (†)-transfer twins; the Route-B RB-DELTA decision,
decision-ready per the walk). HONEST COUNTER STATEMENT: the acceptance
is anchored on the REVISION-30 text (VC32+VC32b); REVISION 31 IS a text
change — the confirming end-to-end pass (**VC33**) is OWED on the
REVISION-31 wording (charge at §37; model diversity per the tree §4
bar); until it lands, the acceptance citation points at the REVISION-30
text; **never self-confirm** — this unit ran no audit.

**REVISION 32 applied (r33, appendix §38) — superseded as CURRENT standing
by the REVISION 33 paragraph below.** **VC33
+ VC33b + VC33c CONFIRMED — the acceptance RE-ANCHORS on the
(errata-corrected) REVISION-31 text** (ledger 1de8ec2 "VC33c
CONFIRMED"). The VC33 ARC LINEAGE, displayed at every operative site per
the fold rule: VC33 (full audit of REVISION 31) returned **0 critical /
2 gaps** (gap 1 = the FGMN pass-6 concurrency displays; gap 2 =
AUDIT-INPUT — the read-only sandbox failed to launch, the grep-execution
duty transferred) → the dated **r32-ERRATUM** (e7ff234: the six
p6-concurrency disclosure brackets applied; the execution duty
transferred to the narrow pass) → **VC33b 0C/1G WITH execution** (the
12/3/6 discharge-core transcripts CONFIRMED; the gap = three multi-line
brackets missing from the orchestrator's `grep -o` extraction,
audit-input) → **VC33c CONFIRMED 0C/0G** (all six brackets verified in
full context). **The acceptance anchor moves REVISION 30/VC32+VC32b →
REVISION 31/VC33+VC33b+VC33c** (the citation-text update rides r33 per
the VC22 precedent); now INSIDE the certified anchor: the HV3 acceptance
+ the complete (H-v3) discharge sweep (**TR-3′-GEN PRINT-UNCONDITIONAL
at {all order-2} ∪ {order-3 all-e_j = 1}**), the FGMN
REVISION-4-through-7 perimeter-hardening displays, the re-stated Asvin
batch. On the newly re-anchored text REVISION 32 folds: (1) **FGMN
RE-DERIVATION ACCEPTED** (ec64503; note `FGMN_REDERIVATION_2026-08-08.md`
ACCEPTANCE RECORD — the 14-pass, 10-revision arc closes: pass 12 Fable
CLEAN 0C/0G WITH the full execution leg + pass 14 Codex content-CLEAN
0C/0G under the ledger's division-of-labor disposition, model-diverse;
THREE independent executed legs on record, passes 3/9/12). ACCEPTED:
(a) the two-scale transcription DIAGNOSIS (the compiled `SideReads`
double-scales the stride weight vs the print's parent-valuation keying);
(b) the ROW-A SOURCE-FIRES verdict; (c) the THREE-token proposal diff
(HC2/Defs.lean:285/287/292, σ.w → σ.wPrev) + the fixed points — the S1
two-scale law, `juncforge_clauseIII_derivable`, the DITER §S3 supplier
obligations, and the JuncForge/D0 RETAINED SEMANTICS (the four theorems
keep their exact compiled meanings about the OLD clause, byte-frozen
under the new-definition route — load-bearing, INSIDE acceptance);
(d) the PIN-GEN-i discharge; (e) the pinned sources + the battery
record. CONSEQUENCE for (H1): **the glue route's SOURCE-LEVEL QUESTION
is ANSWERED** — the print's glue step FIRES at row A; the compiled
parity/landing walls were TRANSCRIPTION ARTIFACTS of the double-scaled
clause; the route's revival awaits only the gated application. THE
APPLICATION = the ASVIN-GATED application unit's deliverable (the
REVISION-9 scope cut: the change-set is EXPLICITLY OUT OF ACCEPTANCE
SCOPE — a scratch-worktree diff adjudicated at the Asvin gate ON THE
DIFF ITSELF, with a POST-APPLICATION hostile pass as the completeness
test, the strongest available adjudication, not a totality oracle; the
REVISION 4-8 censuses = the reviewer's informative map). The Asvin
batch's TOP item is now a FULLY VERIFIED proposal. (2) **the
DIVISION-OF-LABOR adjudication recorded as standing campaign
discipline** (ledger 3875c49): on this host the model-diverse 2-clean
bar divides — the FABLE leg carries EXECUTION (hash verification,
battery re-runs, compilation, from-note reimplementation), the CODEX
leg adjudicates CONTENT (statements, derivations, arithmetic, honesty
sweeps); an infrastructure-only inability-to-execute finding on a Codex
pass is recorded as a NOTE, not a gap, PROVIDED a Fable-executed clean
on the same revision is on record — a specification of existing
practice (TR3-S3 precedent), no content finding ever downgraded.
(3) **TR3-S4 STATUS — composed + sealed-falsifier ALL GREEN; NOTHING
accepted, NO status upgrade**: the compose note `TR3S4_2026-08-08.md`
on the order-3 e₀ = 1/e₁ ≥ 2/e₂ = 1 stratum — σ₂(R^F₃(f)) =
z̄₂^{θ₂s₀(f)}·R_λ₃(f)(z̄₂^{θ₂}y), θ₂ = ℓ₁g₁γ₂, the measured (TWIST-3)
composed as an identity — with **Lemma HV3-E2 DERIVED from the pinned
print, not hypothesized** (v₃ = w₂ at e₀ = 1, e₁ ≥ 2, from the same
sha256-pinned HigherNewton.tex generic displays at |λ₂| = h₁/e₁; no
bespoke ordinate hypothesis remains; HV3-THM's acceptance does NOT
transfer — the new instantiation is the note's own hostile-pass
obligation); falsifier `tr3s4_check.py` two-commit protocol: seal
7fa8825 → first run **exit 0, 13,679 samples / 0 violations** (79eaee6),
rerun JSON-identical, zero amendments; pass 1 (Codex) **0C/2G
archived** (the S6.2 key-orientation conflation; the missing second
seal hash in S8.3) — **REVISION 1 owed next cycle; counter 0/2**;
TR-3′-GEN on this stratum stays ATTEMPT-GRADE; **the F2c VERIFIER-OWNED
from-note implementation stays the acceptance obligation** (note S8.4).
NO roster change (**NINE rows** recounted); no clause attribution
moved. Housekeeping: the WAVE-15 REMAINDER displayed (the TR3-S4
pipeline — REVISION 1 → hostile passes → the F2c obligation; the
synthesis-7 tail: the ITER-LAW proof attempt, the (H7) restatement
forge on the two N-H7-1 pins, the `DIterJunctionSupplier` statement —
its junction gates now RE-PRICE against the ACCEPTED FGMN diagnosis
(the note's §S6 gate display is now the accepted content, no longer
blocked-on-adjudication), the banked-sorry attempt; the optional S
batch); **SYNTHESIS PASS 8 is OWED at the wave-15 close** (the FGMN arc
that held it has closed). The Asvin batch RESTATED IN FULL (the
`TowerMoveClosureL` two-layer re-ratification; the fence-(vii) ROUTE
(a) ratify-or-reopen lineage — the GR route (a) + the RECSL `hσt`-pin
rider + the EXECUTED F-ADM-3 re-key recorded for ratify-or-reopen;
**the FGMN proposal AT TOP — now FULLY VERIFIED, ACCEPTED at ec64503**,
its application the gated application unit's deliverable per the
REVISION-9 scope cut; the Route-B RB-DELTA decision, decision-ready per
the walk). HONEST COUNTER STATEMENT: the acceptance is anchored on the
REVISION-31 text (VC33+VC33b+VC33c); REVISION 32 IS a text change — the
confirming end-to-end pass (**VC34**) is OWED on the REVISION-32
wording (charge at §38; model diversity per the tree §4 bar); until it
lands, the acceptance citation points at the REVISION-31 text; **never
self-confirm** — this unit ran no audit.

**REVISION 33 applied (r34, appendix §39) — superseded as CURRENT
standing by the REVISION 34 paragraph below.**
**VC34 CONFIRMED FIRST-PASS 0C/0G — the acceptance RE-ANCHORS on the
REVISION-32 text** (ledger f3b1ff8 "VC34 CONFIRMED FIRST-PASS"; report
`lean/notes/openmath/VC34_report.md` — the third first-pass confirmation
of the window: ALL folds verified — the FGMN acceptance display exactly
the REVISION-10 list with no application leak and the wall theorems
byte-frozen; the TR3-S4 un-upgraded status with the F2c obligation
displayed; the division-of-labor discipline; the Asvin batch with its
verified top item; the 59-pattern census exact; roster NINE). **The
acceptance anchor moves REVISION 31/VC33+VC33b+VC33c → REVISION
32/VC34** (the citation-text update rides r34 per the VC22 precedent);
now INSIDE the certified anchor: THE WAVE-14/FGMN MEGA-ARC, CLOSED AND
CERTIFIED END-TO-END — the glue route's source-level question answered
inside the anchor (the print's glue step fires at row A; the compiled
walls were transcription artifacts), the three-token proposal fully
verified at the Asvin gate — plus the division-of-labor discipline and
the TR3-S4 composed-not-accepted status. On the newly re-anchored text
REVISION 33 folds: (1) **TR3-S4 ACCEPTED** (cd611af; note
`TR3S4_2026-08-08.md` ACCEPTANCE RECORD — the 4-pass/2-revision arc
closes: pass 3 Fable CLEAN 0C/0G with BOTH execution legs — the sealed
battery reproduced byte-identical, 13,679 samples / 0 violations, AND
**the F2c VERIFIER-OWNED from-note implementation DISCHARGED
IN-PIPELINE for the first time**: 1,108 checks / 0 failures at
off-roster p ∈ {5,7}, GMN ordinates via the decorrelated geometric
(E4)-Fraction leg, teeth validated by 3 mutation controls, each RED —
plus pass 4 Codex content-CLEAN 0C/0G, model-diverse under the standing
division-of-labor discipline). CONSEQUENCE for (H1): the strata family
stands at FOUR accepted members — with accepted TR3-S1/S2/S3,
**TR-3′-GEN is PROVED on the THREE-component union {all order-2} ∪
{order-3 all-e_j = 1, PRINT-UNCONDITIONAL} ∪ {order-3 e₀ = 1, e₁ ≥ 2,
e₂ = 1, conditional on (H-ℓ) + the (KP_i)+[T] consumptions at S6.2 +
the PIN-GEN-i attribution display, with Lemma HV3-E2 DERIVED from the
pinned print}**; deeper strata (e₀ ≥ 2 stacked; e₂ ≥ 2; orders ≥ 4)
stay OPEN, PROBE-FIRST, NO candidate sealed. (2) **HYGIENE** (the
pass-3 non-classifiable observation): ONE dated supersession bracket
applied in `TWIST3_PROBE_2026-08-08.md` at its KEY0-LIFT orientation
prose — the probe's pre-correction "GMN-lawful = θ₂-twisted ψ₂" label
is SUPERSEDED by the TR3-S4 REVISION-1 corrected orientation (the
GMN-lawful class is the UNTWISTED ψ₂ — R_λ₃(Φ₃) = z̄₂^{−θ₂g₂}·ψ₂; the
twisted T_{c⁻¹}ψ₂ class belongs to the FGMN-normalized lift) —
additive, the probe's measured data untouched. NO roster change
(**NINE rows** recounted); no clause attribution moved. Housekeeping:
the WAVE-15 REMAINDER is THE queue (the synthesis-7 tail: the ITER-LAW
proof attempt; the (H7) restatement forge on the two N-H7-1 pins; the
`DIterJunctionSupplier` statement — re-priced against the accepted
FGMN diagnosis, its §S6 gate display accepted content; the banked-sorry
attempt; the optional S batch); **SYNTHESIS PASS 8 is OWED at its
close**. The Asvin batch RESTATED IN FULL (the `TowerMoveClosureL`
two-layer re-ratification; the fence-(vii) ROUTE (a) ratify-or-reopen
lineage — the GR route (a) + the RECSL `hσt`-pin rider + the EXECUTED
F-ADM-3 re-key recorded for ratify-or-reopen; **the FGMN proposal AT
TOP — FULLY VERIFIED, ACCEPTED at ec64503**, its application the gated
application unit's deliverable per the REVISION-9 scope cut; the
Route-B RB-DELTA decision, decision-ready per the walk). HONEST
COUNTER STATEMENT: the acceptance is anchored on the REVISION-32 text
(VC34); REVISION 33 IS a text change — the confirming end-to-end pass
(**VC35**) is OWED on the REVISION-33 wording (charge at §39; model
diversity per the tree §4 bar); until it lands, the acceptance
citation points at the REVISION-32 text; **never self-confirm** — this
unit ran no audit.

**REVISION 34 applied (r35, appendix §40) — the CURRENT standing.**
**VC35 + VC35b CONFIRMED — the acceptance RE-ANCHORS on the
(errata-corrected) REVISION-33 text** (ledger 345b432 "VC35 + VC35b
CONFIRMED" — the SIXTEENTH audited re-anchoring of the window). The VC35
ARC LINEAGE, displayed per the fold rule: VC35 (full audit of REVISION
33) returned **0 critical / 2 gaps** (gap 1 = the §39 closing summary
abbreviated the third strata component's conditions → the dated
**r34-ERRATUM** d79f295 spells out (H-ℓ) + the (KP_i)+[T] consumptions
at S6.2 + the PIN-GEN-i attribution display at the closing site too;
gap 2 = AUDIT-INPUT — the appendix's TWIST3-bracket slice was EMPTY, a
grep-pattern artifact, checklist rule 5) → **VC35b CONFIRMED 0C/0G**
(the corrected closing passage + the ACTUAL TWIST3 bracket verified in
full context). **The acceptance anchor moves REVISION 32/VC34 →
REVISION 33/VC35+VC35b** (the citation-text update rides r35 per the
VC22 precedent); now INSIDE the certified anchor: the TR3-S4 acceptance
— **the strata family's THREE-component union {all order-2} ∪ {order-3
all-e_j = 1, PRINT-UNCONDITIONAL} ∪ {order-3 e₀ = 1, e₁ ≥ 2, e₂ = 1,
conditional on (H-ℓ) + the (KP_i)+[T] consumptions at S6.2 + the
PIN-GEN-i attribution display, with Lemma HV3-E2 DERIVED from the
pinned print}** — the F2c in-pipeline discharge (1,108 checks with
mutation-control teeth), and the TWIST3 hygiene bracket. On the newly
re-anchored text REVISION 34 folds THE WAVE-16 BATCH (the synthesis-7
tail, EXECUTED; full records at §40): (1) **ITER-LAW PROVED AT ATTEMPT
GRADE on the harness tower class** (standard-lift towers — the class
DITER_RESTATE §S2 pins; the ENTIRE instance base sits inside it; note
`ITERLAW_2026-08-08.md`): the CARRY-1/W-MULT fibred route (L1 EQ12-ADD
both levels, L2 INNER-SHIFT, L3 WINDOW closure, L4 EPS-CLOSED, L5
EPS-MULT, the two-line Bézout Lemma EXP, L6 ANCHOR-VAL, L7 PAIR-VAL
Cases I/II); two-commit seal honored (seal ab7c31c, run 242ba1e);
falsifier ALL GREEN FIRST PASS exit 0 — 35,232 samples / 0 violations,
F1–F10 + CERT, Case-II coverage MET, row-X NOT-CYCLIC 4/4 with EXACT
value-set match; (W-MULT) RECOVERED as a corollary, not consumed;
B0a/B1/D-REAL-degeneration/B0c displayed corollaries. HONEST RESIDUALS:
**(ITER-LAW-LIFT)** general lawful (non-standard) lifts OPEN exactly at
g₀ = 1 ∧ δ₁ = 1; the gr(w₂) semantics wrapper rides the accepted GRB
retarget (consumer-side, NOT consumed by the theorem); orders ≥ 3 NOT
TOUCHED; **ACCEPTANCE COUNTER 0** — hostile passes IN FLIGHT (Codex
content pass + Fable execution/F2c pass, running concurrently with this
fold; AT THE FOLD'S CLOSE both landed — the Fable/F2c leg CLEAN 0C/0G,
the Codex content leg NOT-CLEAN 1C/1G with the errata round owed to the
next unit; counter stays 0; the §40.(i) concurrency disclosure governs);
status-upgrade brackets applied this fold at DITER_RESTATE §S2 /
GRB S5.3 + S8 / PROJECT_STATE. **[r36 STATUS UPGRADE (REVISION 35,
§41): ITER-LAW is ACCEPTED — the hostile-pass arc closed at the
2-clean model-diverse bar on the amended proof note (post-errata
7566ccd): PC1 (Codex content) NOT-CLEAN 1C/1G (acc955a) → PE1 (Fable
execution/F2c) CLEAN (64ac340, from-note reimplementation 89,056
samples / 0 violations + 4/4 mutation controls) → errata round 1
(7566ccd: the S0 scope ERRATUM + the COR-4 mixed-block supplement
`iterlaw_cor4_mixed.py` 18,396/0, sealed falsifier md5-untouched) →
PC2 CLEAN 0C/0G (804b88a) + PE2 CLEAN 0C/0G (c40a1da); adjudicated
33373cb; ACCEPTANCE COUNTER 2/2 — the bar is MET. SCOPE AS ACCEPTED
(supersedes this paragraph's "the class DITER_RESTATE §S2 pins"
phrasing): proved and ACCEPTED on the harness tower class
(standard-lift; a STRICT SUPERSET of DITER_RESTATE §S2's pinned
E₂ = e₀e₁ > 1 setting; coincides with the §S2 display on the nose
restricted to E₂ > 1; the E₂ = 1 leg a harmless COR-3-covered
extension outside the statement of record). RESIDUALS unchanged, ALL
THREE: (ITER-LAW-LIFT) open exactly at g₀ = 1 ∧ δ₁ = 1; the gr(w₂)
semantics wrapper rides the accepted GRB retarget consumer-side;
orders ≥ 3 untouched. The clause-(4) J3b cross-reference upgrades
likewise: J3b MAY now cite ITER-LAW ACCEPTED-on-class (standard-lift
instances only; the gr(w₂) tie layer still a separate prerequisite;
every DITERSUP gate still BLOCKED-ON-APPLICATION).]** (2) **THE
BANKED SORRY RETIRED**
(15372d1; note `BANKED_SORRY_2026-08-08.md`):
`npVertices_stable_of_hull_preserved` PROVED Lean-core, statement
byte-unchanged, via the NEW `OM/HullStability.lean` (~1740 lines,
imports Mathlib + OM/NewtonPolygon ONLY; the BPLL §0 kernels copied
VERBATIM with attribution — the deliberate hygiene copy keeping the
183-module Scaffold closure out from under the capstone path); key
lemmas: the survivor-corner law `dropCollinear_corner`, the
`full_zip_valid` flattest-chord transfer, `glue_aux`/`vert_zip_attain`
chord attainment, the two-hull agreement; falsifier 0/4000 + 0/4000
PRE-PROOF; AxChk: full `lake build` green (8698 jobs), `AxChk_baseline`
exit 0, ZERO `sorryAx` in the 1099-line census, all capstone footprints
UNCHANGED; `certLevel_stabilizes` now `[propext, Classical.choice,
Quot.sound, omReadValuation_lt_of_certLevel_fkeyed]` (the declared
NAMED CITE axiom only). **The corpus's banked-sorry count: 1 → 0.**
(3) **H7 FORGE** (037ac6f; note `H7FORGE_2026-08-08.md`): the FIRST
compiled NON-VACUOUS (H7) decision point — the sorry-free 5-site
depth-1 instance at (p,n,N,m) = (2,2,2,4), the S6 shape, 15 decls ALL
Lean-core; non-diagonal `branchCellOf` certified 3 ways
(`forge_read_nondiagonal`, `forge_states_crosscut`, the STRUCTURAL
`forge_no_diagonal_read`); sides = 2 at fHj with the real SideSplit,
(JC-multi) DECIDED TRUE (1·2² = 4); PIN-1 `forge_kloc_slope_keyed`
PROVED + `forge_kloc_pooled_refuted` COMPILED COUNTERMODEL (the S5
pooled-u refutation's compiled twin); PIN-2 `forge_guard` InGuard +
`forge_klocCounts` = the FIRST contentful `KLocCounts` instantiation
(1·4 = 2·2 exact at every word). HONESTY: depth-1 (the sides-2 site's
own continuations unrealized — the HK23/HK25 transplant is the named
residual); node literals synthetic-but-lawful (the S2
center-distinction genre); `ITauShadow` at the forge NOT attempted
(Tier-S); **NOTHING here discharges (H7)** — this is its first compiled
decision point. (4) **DITERSUP STATEMENT** (2a25ed9; note
`DITERSUP_STMT_2026-08-08.md`; display-only, NO .lean touched):
`SideReads′`/`ReadsOf′`/`DIterJunctionSupplier′` displays (three
σ.wPrev tokens marked; clauses (iii)–(vi) byte-identical); the D2
carrier interface (`DIterCarrier`/`IterLaw`; the gr(w₂) tie displayed
as MISSING); junction gates J1 (M) / J2 (L) / J3a (M, ITER-LAW-FREE via
the D-REAL degeneration at e₀ = 1) / J3b (L) / JS1–JS3 (S/S/M) +
carrier layer P1 (M) + the application P0 (L, Asvin-gated) — **EVERY
gate BLOCKED-ON-APPLICATION** (the three-token repair NOT applied; the
old-clause records byte-frozen). Perimeter finding of record:
`TruncRunFrom` binds `SideReads` at [T-5] (TruncatedRun.lean:230), so
`TruncRunFrom′` joins the primed-mirror set
(`FTiedTruncLanding′`/`ftie_extends′` likewise). CROSS-REFERENCE
(this fold): J3b's blocked-on-ITER-LAW leg MAY now cite ITER-LAW
proved-on-class — the harness tower class, standard-lift instances
only (all compiled ones are) — at attempt grade, acceptance pending.
Housekeeping: **SYNTHESIS PASS 8 is OWED at the wave-16 close** (the
tail executed; the ITER-LAW hostile-pass arc is THE in-flight
verification). NO roster change (**NINE rows** recounted); no clause
attribution moved. The Asvin batch RESTATED IN FULL (the
`TowerMoveClosureL` two-layer re-ratification; the fence-(vii) ROUTE
(a) ratify-or-reopen lineage — the GR route (a) + the RECSL `hσt`-pin
rider + the EXECUTED F-ADM-3 re-key recorded for ratify-or-reopen;
**the FGMN proposal AT TOP — FULLY VERIFIED, ACCEPTED at ec64503**,
its application the gated application unit's deliverable per the
REVISION-9 scope cut; the Route-B RB-DELTA decision, decision-ready
per the walk). HONEST COUNTER STATEMENT: the acceptance is anchored on
the REVISION-33 text (VC35+VC35b); REVISION 34 IS a text change — the
confirming end-to-end pass (**VC36**) is OWED on the REVISION-34
wording (charge at §40; model diversity per the tree §4 bar); until it
lands, the acceptance citation points at the REVISION-33 text; **never
self-confirm** — this unit ran no audit. **[r36 (REVISION 35, §41):
VC36 RAN and returned NOT-CONFIRMED 1C/3G (report `VC36_report.md`,
3cf25ca; charge v2 after the v1 input-cap launch failure — the
frozen-history trim, checklist rule 7). F1 (critical) + F2 (gap) =
FOLD-LAG on the then-in-flight ITER-LAW arc, cured by the acceptance
(33373cb); F3/F4 = GENUINE closing-site qualifier omissions, remedied
by the dated r36 errata at the §40 close. The acceptance citation
STAYS at the REVISION-33 text (VC35+VC35b); **VC36b** — the NARROW
confirming pass on the r36 remedies only — is OWED (charge at §41);
never self-confirm.]**

**Status vocabulary** (tree §0, compressed): **VERIFIED** = survived ≥ 2 consecutive
clean adversarial passes in fresh contexts, at least two models (VERIFIED-DUAL), or
machine-checked in Lean with a faithfulness audit (VERIFIED-LEAN). **PROVED-UNVERIFIED**
= proof on file, pending pass named. **OPEN** = no proof. **LITERATURE** = external
citation carries it, faithfulness status stated. A reduction is not a proof.

---

## 1. THE THEOREM (the scaffold form)

Fix n ≥ 2. A **splitting type** τ of degree n is a multiset of pairs (e_i, f_i) with
Σ e_i f_i = n — the ramification indices and residue degrees of the irreducible
ℚ_p-factors. (The tree writes σ for the same object; τ = σ throughout.) Monic degree-n
polynomials over ℤ_p are identified with ℤ_p^n by coefficients, carrying Haar
probability μ. decided_τ(k) counts the residue classes mod p^k ALL of whose monic lifts
are separable of type τ; undec(k) is the measure of the classes not decided for any
type. Let ℛ := ℚ[q, 1/q, 1/(q^c − 1) : c ≥ 1] ⊂ ℚ(q) — the cyclotomic-type ring, whose
elements have no poles at any real q ≥ 2, hence at any prime power. (Variable
convention [REVISION 13, VC15 advisory 4(b)]: q and t name the SAME single formal
variable — t = q, ℚ(t) = ℚ(q); some displays below write t where the solve's
formal variable is emphasized as an interpolation variable. No second variable
exists anywhere in the document.)

> **(ROOT) THE UNIFORMITY THEOREM — THE TARGET.** (Displayed as the campaign's goal;
> NOT what the §2 steps presently prove — that is (ROOT-C) below, the honest
> quantifier-for-quantifier form.) For each n and each splitting type τ of degree n
> there is a SINGLE rational function R_τ ∈ ℛ ⊂ ℚ(t) such that at EVERY prime p —
> including every wild prime p ≤ n — the following hold.
>
> **(0) Cylinder-limit density.** The type-τ locus L_τ = {f monic separable of type τ}
> is Borel; the decided-τ cylinder unions are nested increasing in k; and
>
>     α(n, τ; p) := μ(L_τ) = lim_{k→∞} decided_τ(k) / p^{nk}.
>
> **(SQ) Level-k squeeze.** At every finite level k, two-sidedly,
>
>     decided_τ(k)/p^{nk}  ≤  α(n, τ; p)  ≤  decided_τ(k)/p^{nk} + undec(k),
>
> and undec(k) → 0 (drainage) — so the finite-level counts converge to the density
> with an explicit bracket at every k.
>
> **(R) R_τ evaluation.** R_τ is the τ-entry of the unique solution of a finite linear
> system (I − K(q))·C = b(q) over ℚ(q) — branch = convolution over coprime children,
> descend = strictly smaller child, self-loop of size e (**e ≥ 2 always** — no size-1
> self-loop exists in the system; the forcing is the (SL≥2) display at Step 5
> [REVISION 2, finding V2-6]) = geometric resummation 1/(1 − q^{−(e(e+1)/2 − 1)})
> (a unit of ℛ, the exponent being e(e+1)/2 − 1 ≥ 2) — a system whose state
> menu, coefficients, and pivots carry NO
> dependence on p; every denominator produced anywhere in the solve is cyclotomic-type
> (a unit of ℛ), so the specialization is pole-free at every prime power; and
>
>     α(n, τ; p) = R_τ(p),      Σ_τ R_τ = 1  identically in ℚ(t).
>
> **(UB) Unramified base.** The SAME R_τ works over every unramified base: over the
> degree-δ unramified extension of ℚ_p the type-τ density is R_τ(p^δ).

> **(ROOT-C) THE COMPOSED THEOREM (what §2 proves, stated exactly).** Assume the §3.1
> open-mathematics rows as hypotheses:
> **(H1)** the graded-dictionary open family at OM orders ≥ 2 [ENLARGED at
> REVISION 6, findings F-2/F-3, from its former single member; member (d) added at
> REVISION 7, finding VC7-1]: (a) (GR-B), the
> anchored-march residue-structure package; (b) GD-1's (KP)/(KPirr)
> representative-admissibility clauses beyond order 1 (= O-2a's open lemma OL-2a-1);
> (c) the GD-6 run-decomposition-conformance instances at orders ≥ 2 that Step 10's
> conditionality line consumes; (d) the order-≥ 2 KEY1 block [REQUANTIFIED to
> READ ORDER at REVISION 8, finding VC8-1] — the key-compliance block KEY1 proves
> at order ≤ 1, asserted at EVERY realized site of Step 6's machine at OM order
> ≥ 2, IRRESPECTIVE of the current key's degree (order-≥ 2 sites whose key
> remains degree 1 — e.g. e·g = 1 refinement chains — INCLUDED), which Step 6's
> ALL-SITE theorems consume there — exactly the complement of the proved
> order-≤ 1 slice; same proved-≤1/open-≥2 shape, one group, displayed
> at the §3.1 row; **(H2)** (FRESH); **(H3)** (E-N) and (ADM) (O-9's
> census-admissibility hypothesis,
> FULL attainment — automatic at order 0 only; at r ≥ 1 a displayed p-free
> per-datum decidable check — §3.1) [(ADM) added at REVISION 2, finding V2-5; its
> "automatic at orders ≤ 1" phrasing corrected at REVISION 4, finding VC4-2, per
> the O-9 r3/r4 rescopes; **D-12r (the TB-CAP (τ-irr) perimeter), formerly this
> group's third member, RECLASSIFIED [M] → the §4 literature-pin class at
> REVISION 14, r15, executing VC16 finding 2** — the TB-CAP perimeter remains a
> conditionality of every consuming step, now LITERATURE-grade
> ("perimeter-conditional on the §4 D-12r pin"), no longer a hypothesis row of
> this display];
> **(H4a)** (R1)–(R4) — **RETIRED at REVISION 22, r23** [the historical letter is
> KEPT; the live roster is (H1)–(H9) MINUS the retired (H4a), (H8), and (H9) PLUS
> the (H8′) row below: NINE §3.1 rows. SPLIT out of the former (H4) at REVISION 2,
> finding V2-2: NOT clause-(UB)-only — clause (R)'s value chain consumed it too,
> through Step 18's K3-c (the base-changed β-legs at q ↦ q^δ, δ > 1; the O-11 r2
> declared row D-c). Retirement ground (blueprint HDISCHARGE_H4 §1 clause F-(iv),
> fired per H4M7 §§2–3): the (R1)–(R4) base is WRITTEN AND PASSED —
> `H4M1_rpack_attempt.md` ((R1)–(R3) over W(F_q), ONE theory, δ absolute; ACCEPTED,
> passes 2+3 clean model-diverse) + `H4M2_dict_attempt.md` ((R4) via the
> factor-correspondence dictionary; DONE, pass 5 CLEAN on its REVISION 4) — and the
> composite P(O-8b/M14) (M14 REVISION 1 + M1 + M2) is **ACCEPTED**: passes 3
> (Codex, 0C/0G) + 4 (Fable decider, nothing classifiable) consecutive clean,
> model-diverse (ledger 5a10256), with N-T4/N-K3 numerics green. Steps 16/18's
> K3-c (R1)–(R4) leg now reads PROVED-VERIFIED at the ACCEPT record; clauses
> (R)/(UB) each LOSE (H4a). The (K3-δ) audit note SURVIVES the retirement, MOVED
> to the (UB-X) row — clause (R) still consumes (K3-δ) until the H4M7 §4 Route-B
> re-scope fires. Displayed bounds carried at the §3.2 retirement row: M1's §0.4
> perimeter fence (recipe-presented towers; the run-production layer stays REL.1's
> wider walk), M2's DictModel grammar/[IN-C]-at-consumer-strength displays, the
> [IN-U] MacLane pin (LITUNIT duty). Record: the §3.2 (H4a) = (R1)–(R4) retirement
> row];
> **(H4b)** the clause-(UB) residue (UB-X) (the per-leaf O_δ
> re-base audit + position-class exhaustion + aggregation commutation — §3.1),
> whose position-class-stability fragment **(K3-δ)** (K3-c's realized δ > 1 pools
> are δ-stable position classes — (UB-X)(b) restricted to those pools) is ALSO
> consumed by clause (R) [REVISION 4, finding VC4-1];
> **(H5)** (PACK), the actual-pack correspondence PLUS the pack row-laws (D-11's
> open clause 4: the concrete assembled pack's rows equal the intended 𝔅_n
> instantiation's entry-for-entry — AND, ENLARGED at REVISION 7, finding VC7-2,
> the three named row-laws at the 𝔅_n rows as explicit clauses: **(PACK-i)**
> rsh_interp, **(PACK-ii)** r_bdd, **(PACK-iii)** wsh_ok — AND, ENLARGED AGAIN at
> REVISION 10, r11, the FOURTH row-law **(PACK-iv)** sem_alloc, the semantic
> verdict-row form at the intended member (the premise D-11 r4's Theorem M2 names
> as (RE)) — all displayed at the §3.1
> row — consumed with the sub-attribution exact [synced at REVISION 8,
> finding VC8-2; (PACK-iv) carried into THIS display at REVISION 12, finding
> VC13-1 — the r11/r12 syncs had run the §3.1 row, Step 19, and the ∎ to four
> members while this display stopped at three]: PACK-i/ii at Step 18's K2
> evaluation glue, PACK-iii the membership
> input to Step 18's K3 evaluation ring-hom (with D-6), and PACK-iv at its CLOSED
> two-edge set — Step 18b DIRECT (Theorem M2's (RE) instantiation) + Step 17's E1
> delivery display of (ROW-Σ) (whose (RE) hypothesis at the consumed member IS
> this clause) — explicit because
> entry-for-entry equality transfers a law only if a
> displayed theorem supplies it on the 𝔅_n side, and none is on file — §3.1)
> [added at REVISION 4, finding VC4-3];
> **(H6)** the terminal-seam engine-conformance obligation group (§3.1):
> CU-2t's three terminal rows **(T-DEC)/(T-READ)/(T-VERD)** (first displayed at
> CUC §9.4; (T-READ) graded into the GD-2/GD-5 read-semantics-conformance family
> at CU-2t R4) PLUS D-11's widened-charge item **(vii)** (general-n H.6 in-fence
> conformance of the built tables) — consumed by clause (R) at Step 11's route
> (CU-2t's §9.5 terminal-verdict conditionality, which Step 18 consumes through
> D-a/K4-CYL), through the terminal-seam leaf labels riding O5triple's counting
> into Step 18's K1 ("CU leaf labels", via Steps 11/12 → Step 17's M3/Theorem-E
> leg — the same K1 route the (FRESH) row displays; leg added at REVISION 6,
> finding A-5) and, for item (vii), at Step 18's K3-a [group added at REVISION 5,
> findings VC5-1/VC5-2: open propositions consumed on the mandatory path cannot
> sit in [T] under the ledger's own classification rule];
> **(H7)** the sibling-interface engine-stratum conformance pair **(I-τ)/K-LOC**
> (§3.1): (I-τ) — the engine's realized site strata present as O-10's adapted
> cells; K-LOC (= M07 OL-3, an exactly-stated OPEN lemma) — continuation reads
> of branch j = OM reads of f_j up to cell-constant normalization. Consumed by
> clause (R) at Step 15's application-to-engine-strata line and Step 18's K1
> ("(I-τ)→GD"); clause (UB) inherits the same content through the transported
> chain [added at REVISION 6, finding F-1: the LAST of O-11 r2's declared
> D-a/D-b/D-c interface rows adjudicated hypothesis-grade, by the same rule as
> (H6); the own-row-vs-(H6)-membership adjudication is displayed at the §3.1
> row and §12];
> **(H8)** (K-CUT), the shallow-cut decomposition kernel — **RETIRED at
> REVISION 16, r17** [the historical letter is KEPT; the live roster is
> (H1)–(H9) MINUS the retired (H4a), (H8), and (H9) PLUS the (H8′) row below:
> NINE §3.1 rows — count corrected at REVISION 18, r19, executing VC20 blocker 1
> (this bracket had lagged at the r17 TEN), re-synced at REVISION 19, r20 (the
> (H9) retirement), and at REVISION 22, r23 (the (H4a) retirement: TEN → NINE). NEW row at REVISION 10,
> r11, from finding G-2 of the UN-BANKED O-11 hostile pass of 2026-08-03
> (`O11_owedpass_2026-08-03.md` — recorded at the §3.2 O-11 row); the content
> was: exactly-one shallow decomposition with exhaustive-exclusive read
> ownership and the weight-preserving product law μ(fiber T) =
> W(Ŝ)·∏_i weight(D_i) — M04 Lemma 3.3's hypothesis, consumed inside Step 18's
> K1 regrouping. Retirement ground: that statement is machine-proved Lean-core
> in its exact-form K1 shape (`kcut_reduce` + `kcut_k1_form` +
> `massFactor_mass_eq`, `Scaffold/HDischarge/H8/`), the 3-round unit-level
> pass arc closed at DISCHARGES (`KCUT_pass3_2026-08-06.md`), and every
> remaining semantic residue (graft; measured-mass identification; the τ/δ
> pool dictionary) is PRICED — since REVISION 17, r18, executing VC19 — as
> the NEW row **(H8′)** below (VC19 found the r17 "already-priced home"
> claim OVERSHOT: no cited owner statement houses the interfaces; the owner
> labels stand as retirement ROUTES only); Step 18's K1 now consumes the
> RETIRED package + (H8′), no separate (H8) hypothesis. Record: the §3.2
> K-CUT retirement row];
> **(H8′)** the K-CUT residual-interface row (§3.1) [NEW row at REVISION 17,
> r18, executing VC19 — the three semantic residues of the retired (K-CUT)
> package, stated from the package's own displays (the §3.2 retirement
> record; `Scaffold/HDischarge/H8/R2RootSync.lean` §D; the round-3 report)]:
> **(i) GRAFT** — the graft completeness/bijectivity interface: the
> `graft`/`graft_cut`/`cut_graft` correspondence is a BIJECTION between full
> deep decompositions and (shallow cut, per-block continuations) pairs, every
> composite realizable (continuation memorylessness the semantic core; the
> H8-R1 charge-(vi-a) FAIL STANDS as this member's status); **(ii) MASS-ID**
> — the measured-mass identification: C2's `shMass`/`blkMass` symbols ARE
> the Haar-measured masses of their events at the consumed member (the
> proved `massFactor_mass_eq` forbids only slot-dependent compensation, not
> mis-identified block weights); **(iii) POOL-DICT** — the node-level τ/δ
> pool dictionary (a typed read-off for C3's pool rows beyond the pinned
> `e_key`; `vBridge` constrained only by keying/injectivity/support) +
> `blockTotal_tie`'s instantiation at the intended member. Enters Step 18's
> K1 WITH the retired package, hence clause (R); clause (UB) inherits
> through the transported chain at q ↦ q^δ;
> **(H9)** (NSF), the ns-routing interface row — **RETIRED at REVISION 19,
> r20** [the historical letter is KEPT; the live roster is (H1)–(H9) MINUS the
> retired (H4a), (H8), and (H9) PLUS the (H8′) row above: NINE §3.1 rows (the
> (H4a) retirement at REVISION 22, r23). NEW row at
> REVISION 10, r11; its two members retired SEPARATELY — neither retirement
> discharged the other: **(NSF-KEY)**, as-built keying conformance, RETIRED at
> REVISION 13, r14 (Unit C executed at the on-record sign-off: all six
> (NSF-KEY-RET) artifacts on file — ledger "UNIT C EXECUTED" (commit 322ec54)
> + "H9 LEAD ADJUDICATION"; compiled-proposition record H9-U1,
> `Scaffold/HDischarge/H9/NsfKeyProp.lean`, Lean-core); **(NSF-INT)**, the
> interface implication chain pin → predicate → corpus, RETIRED at ledger
> entry **H9-C3** (2026-08-02, commit 9abc3ab) per (NSF-INT-RET)'s four
> grounds, ALL on file — I-1 (the D-11 r4 §4B leaf proof) + I-2 (the closed
> {K1, K4} site list, (CH-RES)-relative) + I-3 (the r4 pass-2 ACCEPT of §4B as
> a conditional proof) + the **(K1-CANON) discharge** (the (ed-1) M04 §5.5
> scope pin — K1's quantifier + the post-K4 sum pinned to (q-4)'s notion,
> preamble line, Lemma 3.3 untouched; the standing statement-change authority,
> SURFACED for review; warrant note `K1CANON_discharge_2026-08-02.md` + the
> 2-round hostile pass, substance PASS both rounds). **RE-OPEN TRIGGER,
> displayed:** the D-11 leaf bar still owes one confirming clean pass (the
> §3.2 D-11 row); if that pass finds §4B defective, I-3 re-opens and the
> (NSF-INT) retirement SUSPENDS. Honesty bounds carried at their homes: the
> I-2 closure is (CH-RES)-relative; no carrier non-vacuity claim (realization
> seam); the BridgeD16/TreePin.vt_surj completeness residue stands (Phase-B).
> Step 18's K1/K4 chain and Step 17's M3 leg now consume the retired target
> at the pinned scope. Record: the §3.2 H9 = (NSF) retirement row].
> Then for each n ≥ 2 and each splitting type τ of degree n there is a single
> R_τ ∈ ℛ such that clauses (0), (SQ), (R) hold at EVERY prime p including every wild
> p ≤ n, and clause (UB) holds. Hypothesis attribution, exact [REVISION 2, finding
> V2-2; corrected at REVISION 4, findings VC4-1/VC4-3/VC4-4]: clause (0) rests on
> Step 1 alone (unconditional); clause (SQ) — under this section's SEMANTIC
> definitions of decided_τ(k) and undec(k) — rests on Step 1's bracket (Corollaries
> BRACKET/SEM-DRAIN) + Step 13's semantic drainage layer, both unconditional at
> every prime including wild: **it consumes NO §3.1 row** [attribution corrected
> DOWNWARD at REVISION 4, finding VC4-4 — the former display over-attributed
> (H1)–(H3), including (ADM) and the conditional classifier-transfer route, to
> (SQ); the classifier-side counting machinery rides the evaluation squeeze inside
> clause (R)'s scope instead]; clause (R) consumes (H1)–(H3) + (H5) + the
> (K3-δ) fragment of (H4b) + (H6) + (H7) + (H8′) [**(H4a) RETIRED at
> REVISION 22, r23** — Step 18's K3-c now reads the PROVED (R1)–(R4) package at
> the P(O-8b/M14) ACCEPT record (the §3.2 (H4a) retirement row); the (K3-δ)
> fragment stays consumed, its audit note MOVED to the (UB-X) row; (H6) added at REVISION 5,
> findings
> VC5-1/VC5-2; (H7) added at REVISION 6, finding F-1 — Step 18's K1 consumes
> O-10's product law at the engine's strata only through it; (H1)'s group
> ENLARGED at REVISION 6, findings F-2/F-3, so the "(H1)" here now carries the
> GD-1/GD-6 members too; (H8)/(H9) added at REVISION 10, r11 — (H8) inside Step
> 18's K1 regrouping (Lemma 3.3's cut hypothesis), (H9) at Step 18's K1/K4 label
> routing and Step 17's M3 leg; **(H8) RETIRED at REVISION 16, r17** — K1's cut
> consumption now reads the machine-proved (K-CUT) package + its residual
> interfaces, the §3.2 K-CUT retirement row; **(H8′) ADDED at REVISION 17,
> r18, executing VC19** — the r17 owner homes did NOT house those interfaces,
> which are now the (H8′) members consumed at the same K1 site; **(H9)
> RETIRED at REVISION 19, r20** — Step 18's K1/K4 ns-routing and Step 17's M3
> leg now read the retired (NSF) target at the pinned scope (ledger H9-C3;
> the §3.2 H9 retirement row)];
> NO clause other than (UB) consumes (H4b) BEYOND its (K3-δ) fragment; clause (UB)
> consumes
> **(H1)–(H3) + (H4b) + (H5) + (H6) + (H7) + (H8′)** [**(H4a) RETIRED at
> REVISION 22, r23** — Step 16's alphabet-law leg now rides the PROVED
> (R1)–(R4) package at the P(O-8b/M14) ACCEPT record; (H8)/(H9)
> added at REVISION 10, r11: the transported K1/K4 chain at q ↦ q^δ consumes the
> same cut-decomposition and ns-routing content; **(H8) RETIRED at REVISION 16,
> r17** — the transported cut content rides the same retired machine-proved
> package (the §3.2 K-CUT retirement row); **(H8′) ADDED at REVISION 17, r18,
> executing VC19** — the transported chain consumes the same three residual
> interfaces; **(H9) RETIRED at REVISION 19, r20** — the transported ns-routing
> content rides the same retired (NSF) target at the pinned scope (the §3.2 H9
> retirement row); (H6) added at REVISION 5: the
> transported evaluation chain at q ↦ q^δ consumes the same terminal-verdict and
> fence-conformance content; attribution COMPLETED at REVISION 3, finding B1: the
> former display read "(H4a) + (H4b)" as (UB)'s full set — but (H1) enters (UB)
> through Steps 8/16 (the GD layer, (GR-B)-conditional at orders ≥ 2), (H2)/(H3)
> through Step 16's consumption of Step 14's census ((FRESH)/(ADM)-conditional at
> orders ≥ 2) and through the (UB-X)(a)-transported dictionary/CU/drainage layers
> ((E-N); the same transported layers also carry the §4 D-12r pin's TB-CAP
> perimeter — literature-conditional, not an (H3) member since REVISION 14, r15);
> only (H4b) is (UB)-exclusive; (H7) added at REVISION 6 — the
> transported product-law chain at q ↦ q^δ consumes the same (I-τ)/K-LOC
> interface].
> Without (H4b), what is proved of (UB) is exactly the Step-16 scope: the GD/census
> layers hold over an arbitrary absolutely-unramified complete DVR (consuming neither
> (H4a) nor (H4b), while retaining their own (H1)–(H3) conditionality at orders ≥ 2 —
> finding B1), and — formerly under (H4a), since REVISION 22, r23, at the ACCEPTED
> P(O-8b/M14) record (the retired (R1)–(R4) package) — the alphabet law
> (F_{p^δ}-linearity + a_δ = δ·a₁) at δ-stable position classes. (The former
> "Without (H4)" sentence asserted the alphabet law hypothesis-free — false as
> then written, since the law was (R1)–(R4)-conditional; the base is now proved
> and passed, the δ-stable scope restriction stands — its exhaustion is exactly
> (UB-X)(b)/(K3-δ).)
> **Proof status of the implication itself:** it is the §2 composition — nine leaves
> VERIFIED (the count synced at REVISION 6, finding A-8: a REVISION-5 sync miss had
> left "seven" here against the headline/§3.2/§11 NINE), the rest PROVED-UNVERIFIED
> with named pending passes (§3.2) and displayed
> transcription debts (§3.3). The [V]/[T] rows are AUDIT debts on the proof of this
> implication, not additional mathematical hypotheses of its statement; the
> implication is therefore itself PROVED-UNVERIFIED (never consumed by an acceptance
> claim) until §3.2/§3.3 empty and the end-to-end passes land. (ROOT) = (ROOT-C) +
> discharge of (H1) (the full group — four members since REVISION 7), (H2), (H3),
> (H4b), (H5) (incl. the REVISION-7 PACK-i/ii/iii clauses AND the
> REVISION-10 (PACK-iv)), (H6), (H7), and — since REVISION 17, r18 — **(H8′)**
> (the three K-CUT residual interfaces: GRAFT, MASS-ID, POOL-DICT). (H4a) is
> OFF the discharge distance since REVISION 22, r23 (retired — the (R1)–(R4)
> package WRITTEN AND PASSED at the P(O-8b/M14) ACCEPT, the §3.2 (H4a)
> retirement row; its (K3-δ) audit note survives on the (UB-X) row). (H8) is
> OFF the discharge distance since REVISION 16, r17 (retired — the package
> machine-proved); its three residual interfaces are ON it as (H8′) since
> REVISION 17, r18, executing VC19 (the r17 owner labels priced nothing —
> they stand as retirement ROUTES at the §3.2 K-CUT retirement row). (H9) is
> OFF the discharge distance since REVISION 19, r20 (retired — both members:
> (NSF-KEY) at the executed Unit C, REVISION 13; (NSF-INT) at ledger H9-C3 on
> I-1 + I-2 + I-3 + the (K1-CANON) discharge), subject to the displayed
> RE-OPEN TRIGGER (the D-11 leaf bar, the §3.2 D-11 row).

**Wild-prime scope, explicit.** The content is at p ≤ n (p | n!), where factorization
is governed by higher-order Okutsu–Montes (OM) Newton-polygon towers whose combinatorics
a priori depend on p; p > n is residue-field combinatorics. The paper's original tame
route (S_n-equivariant resolution) FAILS at p ≤ n and is used nowhere below: every step
computes the wild densities directly.

**Relation to the tree's root.** (0)+(SQ) = (U-GEN)(i)/(ii) + the D-1 bridge; (R) =
(U-GEN)(iii) + D-6 + D-2; the operative claim is (U-GEN), and (U-GEN) ⇒ (U-DENS) via
(0) (the converse is not claimed — DELTA-1 §D1.5(a)). "Every prime" (as opposed to
"every prime outside an explicit finite effectively-bounded set") is carried by K-13 +
O-12. The projective normalization and the palindromic functional equation are ANNEX
(A-1/A-2, out of the critical path by standing directive).

**Verified instances (anchors, not proofs of the general theorem):** n = 2 is
VERIFIED-LEAN at every prime including wild p = 2 (`OM/UniformCapstone.montes_uniform_n2`,
Lean-core footprint: value + bracket + drainage machine-checked); n = 3 numerically
exact at the gate standard (RESUM-n3, 65/65, PARI 0 mismatches — with the G-3 scope
caveat [REVISION 13, VC15 advisory 3]: the §3.2 O-11 row's un-banked 2026-08-03
pass records the "65/65 END-TO-END rederivation of the assembled pipeline" reading
as UNSUPPORTED beyond the five displayed identities; the 65/65 agreement stands as
a flag either way — flags are never substitutes); the density tie
numerically confirmed through n ≤ 5 including deep-wild (5,5) (N2). O-11's falsifier
re-derived the true n = 2 triple (p/(2(p+1)), p/(2(p+1)), 1/(p+1)) from the REAL
assembled pack identically in ℚ(q).

---

## 2. THE PROOF (a linear composition of the leaves)

Each step names: the leaf/node (tree id) · its exact statement (compressed, faithful) ·
its verification status with the pass records · what it consumes. The steps are in
logical order; no step consumes a later one (the O-2/O-3 cycle was decomposed at
DELTA-2 §D2.3 — the K-RUN-C composition replaced the joint induction; the D-11/O-11
cycle VC1 found in the original walk is stratified at REVISION 1 as Steps 17/18/18b,
and the Step-1 forward edge is re-pointed — see §7). Standing
preconditions throughout (tree §2.4): f monic separable; trees range over ns-free
carriers; statements are per-f (selected-f) except where D-15 owns box quantification.

### Movement I — the measure floor and the symbolic engine

**Step 1 (D-1 MEAS-BRIDGE + D-2 SUM-ONE + D-4 BASE-CASES — the assembly floor;
"Dfloor").** Statement: (D-1) L_τ is Borel; level-k classes are cylinders of mass
p^{−nk}; decided unions nest; ∪_k decided_τ(k) = L_τ up to Haar-null; continuity from
below gives clause (0) — proved for both decided conventions in circulation, clause
(iv) EXACT in the strict convention; plus Corollaries BRACKET / SEM-DRAIN /
SEM-DRAIN-RATE = the measure content of (SQ) on the SEMANTIC side, unconditional at
every prime including wild. (D-2) every monic separable f has exactly one type
(unique factorization + defectlessness); the nonseparable locus is Haar-null — proved
SELF-CONTAINED inside this leaf (Dfloor Appendix A, via LIT-8c resultant/Vandermonde;
part of the VERIFIED leaf-#3 perimeter). [REVISION 1, finding 1: the original text
attributed ns-null to T-6's (ns-null) clause, a Step-2 node — a silent forward edge.
The dependency is RE-POINTED to Appendix A, which is what the verified leaf actually
proves; T-6's (ns-null) is the SAME fact proved independently at the T-layer (Step 2)
and is NOT consumed here. The edge is removed, not waived.] Hence **Σ_τ α(n,τ;p) = 1
at every p**, plus the interpolation principle (a rational function agreeing with a
constant at infinitely many arguments equals it). [REVISION 3, finding B3: the former
text concluded "Σ_τ R_τ = 1 in ℚ(t)" INSIDE this statement — beyond the leaf's
declared perimeter (R_τ is not even definable at Step 1, and α = R_τ needs Steps
18/18b), a textual forward edge contradicting this step's own Consumes-line. Dfloor's
VERIFIED statement supplies exactly Σ_τ α = 1 + the principle; the identity
Σ_τ R_τ = 1 is CONCLUDED at Step 19, inside clause (R)'s conditional scope.] (D-4)
the degree-1/0 floor: type {(1,1)} density 1 decided at
k = 0; empty-convolution unit conventions (C0)–(C2).
Status: **VERIFIED — leaf #3** (pass 1 Codex V22 CLEAN SOUND, 0 criticals, commit
604621f; pass 2 fresh Fable, model-diverse, 0 criticals, commit bdc996a; adjudicated at
ledger dbccbc1; falsifier `verification/openmath/dfloor_checks.py` ALL PASS). Three
minor gaps ride the next doc touch. Displayed residue: the **BASE-1(f) fence** — the
classifier-side (algorithmic) half at n = 1 is fenced OUT to the classifier steps below.
Consumes: LIT-2 (defectlessness; the K-11/M16 audited e·f bridge), LIT-8c
(resultant/Vandermonde — carries Appendix A's ns-null), standard measure theory with
displayed statements (Folland-grade; §4 roster). NOTHING from any later step.
Delivers to the root: clause (0), the bracket shell of (SQ), and the Σ_τ α = 1
identity + interpolation principle feeding (R)'s Σ_τ R_τ = 1 (concluded at Step 19 —
REVISION 3, finding B3).

**Step 2 (T-1..T-9 + T-U — the MOVES core: the symbolic engine).** Statement: the
carry-algebra stage tower and move ledger (T-1), composition of per-move digit/threshold
ledgers along read histories (T-2), the per-shape-prefix mass laws SUM/EVENT on the
branch tree T_can(f) (T-3), the finite p-free species/letter catalogue (T-4), the
height-resummation domains (T-5), tree assembly + (ns-null) + TB-CAP (T-6), the
per-species symbolic value tables + obligations ledger (T-7s, the symbolic half of the
T-7 split), the finite block-triangular solve with spectral escape over ℚ(q) (T-8), the
exhaustion index table (T-9), and THEOREM U — the squeeze closer, conditional on its
displayed kernel ledger §U.SQ.6 (T-U).
Status: **VERIFIED-DUAL, every node** (passes named per node in the tree §2.2;
2026-07-26/28), WITH the DELTA-1 re-scopings governing: T-1 is verified AS A THEOREM
ABOUT THE SYMBOLIC ENGINE (the semantic "each OM read" clause routes through the
dictionary steps below — §D1.5(e)); T-8 is verified for the formal ℚ(q) solve +
(REG-p)-conditional specialization (§D1.5(f)); T-7's value-correctness event
([1v]-FULL) is RE-OWNED by O-11 (D-9); T-6's TB-CAP is perimeter-conditional on the
§4 D-12r literature pin [re-pointed at REVISION 14, r15 — formerly the §3.1 (H3) row].
Twenty-two sealed numeric gates ride this layer (§5).
Consumes: B-1..B-7, the June skeleton, with grades DISPLAYED HERE so the dependency
universe is closed inside this document [REVISION 1, finding 3]: **B-1** (Hensel
residue-splitting measure-exact) PROVED-UNVERIFIED (June document; 2000/2000
unit-resultant checks) — and the MOVES layer re-proves the split it needs inside
T-1..T-3; **B-2** (residue shape counts are universal polynomials) PROVED-UNVERIFIED at
document level with the counting core VERIFIED-LEAN (`L3.lean`/`L3Gauss.lean`/
`L3Squarefree.lean`, Lean-core); **B-3** (Newton-cell volume) core VERIFIED-LEAN
(`L4.lean` `cellVolume_eq`) + PROVED-UNVERIFIED document; **B-4** (order-r residual
equidistribution) order 1 = B-3 (VERIFIED-LEAN), order r PROVED-UNVERIFIED
(`M6_lemma.md`; its modern descendant is O-9's OL-B, discharged to GD-3); **B-5**
(Vandermonde pushforward) PROVED-UNVERIFIED (`R1_complete.md`); **B-6** (termination +
finite shape menu) PROVED-UNVERIFIED (`L5fix_complete.md`; modern face = O-13, Step 3);
**B-7** (finite linear solve) PROVED-UNVERIFIED (`L6M4_complete.md`; modern VERIFIED
counterpart = T-8 + K-13). The B-layer verification rounds are a §3.2 ledger row. B-8
is SUPERSEDED by this layer and cited nowhere.
Delivers: the entire p-free symbolic pipeline behind clause (R) — menu, tables, solve,
squeeze shell.

**Step 3 (O-13 + D-12 — skeleton/menu finiteness).** Statement: run-collapse is
well-defined; every realized T_can(f) (infinite branches included) collapses to a
finite tree (≤ 6n² letter blocks per branch); the collapsed shapes form the finite
p-FREE set Sk(n), |Sk(n)| ≤ (n+2)^{7n³(n²+3n+16)}. This survives the K-3 caterpillar
countermodel because unbounded recentering runs collapse to one node with a depth
parameter, never new shapes.
Status: **VERIFIED at honest scope — leaf #1** (pass 1 Codex V8 SOUND-WITH-GAPS → rev 2;
pass 2 fresh Fable, model-diverse, package-only context: all six gaps non-consumed
cosmetics, enumerator re-implemented from the text alone reproducing
|S_n| = 1, 15, 53, 197, 509, 1460 exactly; ledger "O-13 REACHES VERIFIED, 2026-07-31
night"). Lean: `skeleton_finite n` at every n (`SkeletonFinite.lean`). Displayed
residue: **D-12r** — the completeness clause (realized trees spell inside Sk(n)) rides
TB-CAP's D4R.0-K(c) verdict pin, a SEPARATE node (since REVISION 14, r15, the §4
D-12r literature pin — residue exactly (HEN-LIFT)/(OM-SAT); formerly the §3.1 (H3)
row). VAL-DIV is a labeled conjecture,
consumed by nothing here.
Consumes: T-4's letter finiteness (VERIFIED-DUAL), the B-6 depth mechanism
(PROVED-UNVERIFIED, grade displayed at Step 2's consumption list; §3.2 B-layer row).
Delivers: finiteness of the solve's state menu in clause (R), and K-OB-A's roster
finiteness (CU-5).

### Movement II — the solve's regularity (pole-freeness at every prime power)

**Step 4 (K-13 — (REG-p) finiteness; Lean-formalized).** Statement: the set of primes
where the pinned rational data degenerate is finite, exactly characterized, effectively
bounded; Theorem U fires at a cofinite set. Status [grade conformed to the §0
vocabulary at REVISION 13, VC15 advisory 2 — the O-14a′/VC10-advisory-1 precedent;
rendered in the EXACT §0 word at REVISION 15, r16, VC17 advisory 2]:
**PROVED-UNVERIFIED — the pending upgrade is the named §3.2 row M17/RegPFinite
faithfulness-audit record, whose landing upgrades the grade to VERIFIED-LEAN**;
displayed notes, NOT grade words: dual provenance + machine-checked Lean-core,
and Step 5's independent route contains the impact (M17 Theorems A/B/C + Corollary U;
`MovesU/RegPFinite.lean`, 14 theorems, Lean-core, per-declaration `#print axioms`
checked; dual provenance = the Fable proof `M17-regp-density_fable.md` + an
independent Codex twin from the neutral brief, agreeing on every load-bearing step
— `lean/notes/M17_FORMALIZED_2026-07-31.md`; N6 25/25). §0's VERIFIED-LEAN
requires "machine-checked in Lean WITH a faithfulness audit", and NO
Lean-statement-vs-mathematical-statement audit record is on file for this leaf
(contrast the K-12 row, expressly held pending exactly such an audit) — the owed
audit is the NEW §3.2 row **M17/RegPFinite faithfulness-audit record**; impact
contained: Step 5's stronger emptiness-at-𝔅_n result independently covers the
specialization clause. Consumes: T-8's formal solve. Delivers: (R) at all but
finitely many primes — upgraded to ALL primes by Step 5.

**Step 5 (O-12 + D-6 — pole-free / (REG-p)-emptiness at the intended solve family
𝔅_n).** Statement: at the abstract solve family 𝔅_n (the §2.1-defined intended
instance), for all three on-record kernel organizations, EVERY block determinant and
EVERY assembled denominator — table coefficients, height resummations, normalizations,
the final R_τ — is cyclotomic-type (a unit of ℛ), hence nonvanishing at every prime
power ≥ 2: the failing set is EMPTY (Theorems 1 + 3 + 4 + Lemma RR; substochasticity +
termination margins ≥ 3/4, 7/8, 6/7 ⇒ spectral radius < 1). Includes the semantic mass
lemma base L1–L6′/Fact F that Step 17 consumes.
Status: **VERIFIED at honest scope — leaf #2** (pass 1 Codex V19 no criticals; pass 2
fresh Fable ZERO criticals, 2 non-load-bearing gaps; adjudicated at ledger commit
95a46d2; falsifier 18,120 prime powers 0 findings, extended by D-11's run to 303/303
primes < 2000). The honest scope: zeros/poles at 𝔅_n — VALUE correctness is NOT part of
this claim (that is D-11 + O-11); the extension to the eventual general-n pack is gated
by D-11 (instance pin) + the contingent OL-O12-1 (fires only if a future pack departs
from 𝔅_n's organizations). Residue: **OL-O12-2** — mathematical half CLOSED at the D-11
unit; what remains is a finite transcription/data pass, zero mathematics.
**(B-INST) — the instance correspondence, stated exactly [REVISION 1, finding 9].**
What identifies the realized packs with 𝔅_n, per n: the pack consumed by Steps 17/18
is BY CONSTRUCTION the 𝔅_n member assembled from T-4's menu, T-7s's tables, and T-8's
block organization (tree §2.1's definition of the intended instance) — the O-12
theorems apply to it verbatim PROVIDED its kernel organization is one of the three
on-record, and OL-O12-1 (contingent, currently unfired) is the displayed escape if a
future pack departs. NO supplied theorem identifies "every general-n realized pack"
with 𝔅_n beyond this by-construction pin: at n = 3 the identification of the complete
realized degree-3 table as an (O3) member of 𝔅₃ is the OL-O12-2 data pass (§3.3, owed);
at general n it is D-11's instance pin (Step 17) + the same finite transcription
discipline, which is exactly why D-11 sits on the mandatory path.
**(SL≥2) — every self-loop has size e ≥ 2; the e = 1 corner is VACUOUS [REVISION 2,
finding V2-6].** The §1 clause-(R) denominator 1/(1 − q^{−(e(e+1)/2 − 1)}) degenerates
at e = 1 (exponent 0, denominator 0); it never fires there, by the solve structure —
proof displayed, each step a proved O-12 clause (`O12_phaseB_verifybrief_rev4.md`
§§2.2–2.4, Thm 1, Cor D):
(a) *self-loops live inside one block.* Every INTER-block booking descends strictly —
hand-off targets have multiplicity m < e resp. μ < e (Lemma L4(i)/(ii)) — so the block
solve is triangular with the within-block (S-loop) return as the ONLY diagonal entry:
a self-loop of the system IS a block-e loop, and no cycle through distinct blocks
exists.
(b) *block 1 has no loop.* K_1 = (0), Φ_1 = 1, E(1) = {1} (O-12 §§2.2–2.3; Theorem 1
is stated "for e ∈ {2, …, n} (block 1 has K_1 = 0, Φ_1 = 1)"): a size-1 aligned
cluster is a monic linear factor, decided immediately (β_1 = 1 — also the base case
of D-11 M2's induction); and multiplicity-1 hand-off atoms TERMINATE as simple Hensel
factors, absorbed into the composition datum as fixed data — they never open a
continuation state, so nothing ever re-enters block 1.
(c) *hence e ≥ 2 at every self-loop*, so E = e(e+1)/2 ≥ 3, the exponents E − 1 ≥ 2 and
E ≥ 3 are ≥ 1, and every solve denominator u ∈ {1 − q^{1−E}, 1 − q^{−E}} is a unit of
ℛ (Theorem 1(3); Corollary D's margins Φ ≥ 3/4, 7/8, 6/7 — the Step-17 (r1) numbers —
are computed exactly on "E ≥ 3 for e ≥ 2 … (and Φ_1 = 1)").
Consumer audit: the formula's only §2 consumers are Steps 5/17/18 (pole-freeness,
margins, spectral escape), all of which consume it THROUGH O-12's e ≥ 2-scoped
theorems — no consumer ever instantiates e = 1. The bug was this document's
unquantified §1 display (now quantified), not a leaf gap.
Consumes: T-8's block structure, T-3/T-6 event structure (as structure only, never
value correctness — the V6-finding-19 circularity is scoped OUT).
Delivers: the pole-free clause of (R); with Step 4, "every prime" in the root.

### Movement III — the classifier ↔ semantics dictionary (the campaign's center)

The chain touches the actual p-adic factorization semantics exactly here. The pieces
were decomposed at DELTA-2 (the K-RUN-C composition CU-1..CU-7 replacing the O-3 ⊕
O-2b joint induction; OB1/OB2/BNS/K-OB-C all EVICTED from the mandatory path — DELTA-2
§D2.4, DELTA-3 §D3.1: ZERO root-side BNS touch-points remain).

**Step 6 (O-1thr package: Theorem 1 = D-14, Theorem 2 = O-1(thr), Theorem 3 = D-15 —
per-site read locality + the cylinder law).** Statement: (Thm 1/D-14) for every
menu-LAWFUL site (H, ν), whether the canonical run on f reads (H, ν) is determined by
f's digits below the computable modulus L(H, ν) (the read ceiling) — the noncircular
quantifier form; (Thm 2/O-1thr) the realized-site two-lift tier with window closure
(K2); (Thm 3/D-15) decided ns-free verdict classes are EXACT finite unions of level-t
cylinders with the per-tree fiber scaling #Φ_N(T) = mass(T)·p^{nN} (the CYL law; owns
ALL box-level lift quantification). Proved at rev 3/4 over the sloped/sheared
convention pin (the depth-2 falsifier REFUTED the unsheared reading — 7,472 phantom
violations — and confirmed the sheared machine exhaustively, 41,720 instances, 0
violations); VAL1/KEY1/DIV1/ROOT-M/WGEO proved outright at order ≤ 1.
**Order-≥ 2 conditionality [REVISION 7, finding VC7-1; REQUANTIFIED at REVISION 8,
finding VC8-1]:** the theorems' ALL-SITE scope consumes, at EVERY realized site of
OM order ≥ 2 — membership by READ ORDER alone, irrespective of the current key's
degree, order-≥ 2 sites whose key remains degree 1 (e·g = 1 refinement chains)
INCLUDED [the former "(degree-g > 1 keys)" parenthesis identified the order-≥ 2
reads with sites whose key has degree > 1, an UNPROVEN identification — the VC8
blocker] — the
order-≥ 2 KEY1 block: the key-compliance block Lemma WGEO's (V1)/(V2) inputs
carry beyond the proved order-≤ 1 slice. That statement is OPEN, and is since
REVISION 7 the displayed §3.1 member **(H1)(d)**, consumed HERE (the former
display priced it only as "the (H1) campaign perimeter", which named no member —
the VC7 blocker); the KEY1 proved outright at this step is the order-≤ 1 clause
alone, so (d) covers exactly the complement of the proved slice in this step's
site range — total coverage consumes no key-degree/order equivalence.
Status: **PROVED-UNVERIFIED** — rev 4 landed (commit 5613274; V24's REAL≡ circularity
repaired structurally: (M6c) requantified, non-ns-halt now a conclusion); pending pass
**P(O-1thr r4)** (fresh hostile pass on rev 4, owed). Displayed caveat **(E-N)** (Thm
3(c) exhaustion) is OPEN and touches CU-4.
Consumes: T-1/T-2's carry algebra (symbolic scope); the graded-presentation dictionary
= **C.1.5**, whose supplier is now DISPLAYED [REVISION 1, finding 5]: C.1.5 ("downsets
are literal digit systems") is T-2's ONE deferred proof obligation, displayed at T-2's
dual acceptance (tree §2.2) and consumed exactly here — at the mathematical machine
O-1thr's proofs run over, the dictionary is DEFINITIONAL (O-1thr §4 residue (S-1): the
clause-by-clause correspondence is displayed, and its remaining content is the S-1
machine-transcription audit, a §3.3 row that now names C.1.5 explicitly); MacLane 1936
(V1) via the LITUNIT pin; at order-≥ 2 sites, the §3.1 member **(H1)(d)** (the
order-≥ 2 KEY1 block — read-order membership, key degree irrelevant) [REVISION 7,
finding VC7-1; requantified at REVISION 8, finding VC8-1].
Delivers: D-14 + D-15 to CU-1/CU-3/CU-4 and Step 17's per-box σ-constancy; the K-4/K-6
repairs route through D-15.

**Step 7 (O-2a — the upstream dictionary half: invariants, order placement,
normalization crossing, admissibility + the D-5 carrier design).** Statement: the
correspondence Θ, typed per fixed separable f from realized runs to GMN types:
(accE, accF) = (e₁⋯e_r, f₀⋯f_r) with saturation ⇔ ω = 1; certified increment nodes map
injectively to GMN orders; raw-frame side reads transport to the v_r-normalized frame;
the canonical key lifts form one GMN-admissible representative family; Theorem 2(B)/(D)
= the emitted-record ↔ GMN-datum tie; OL-6 = run decomposition + chain binding at every
level.
Status: **PROVED-UNVERIFIED** — rev 5 (commit b337195), core THRICE confirmed (V10 →
pass-2 → V25 arc; V25 on rev 4: 0 criticals, 1 minimality gap → OL-3-min trim executed);
pending pass **P(O-2a r5)**. Displayed residue: the SIX exactly-stated open lemmas
**OL-2a-1..6 = the GD-1..GD-6 families**, mapped ONE-FOR-ONE [REVISION 1, finding 5's
coverage charge]: OL-2a-1 = GD-1 (representative admissibility — since REVISION 6,
finding F-2, the (KP)/(KPirr) clauses beyond order 1 are §3.1 (H1)(b),
hypothesis-grade; the properness-proviso leg keeps its pinned §3.3 (KP)/(KPirr)
row); OL-2a-2 = GD-2 (frame/valuation tie — head CLOSED at Step 8 order ≤ 1; orders
≥ 2 = (GR-B) = (H1)(a), §3.1); OL-2a-3 = GD-3 (residual dictionary — min core CLOSED
at Step 8; FULL form at orders ≥ 2 = (GR-B), consumed by the value side); OL-2a-4 =
GD-4 (engine bookkeeping — §3.3 row); OL-2a-5 = GD-5 (base-read conformance — §3.3
S-1 row; its KEY1 mathematical face is since REVISION 7, finding
VC7-1, the §3.1 (H1)(d) member — since REVISION 8, finding VC8-1, the order-≥ 2
KEY1 block — the audit half staying [T]); OL-2a-6 = GD-6
(run-decomposition conformance — since REVISION 6, finding
F-3, the instances Step 10 consumes at orders ≥ 2 are §3.1 (H1)(c); the
bookkeeping/route remainder keeps the §3.3 row, with GD-4). Every
residual has exactly one ledger home; nothing is covered twice or zero times.
Consumes: LIT-1 (GMN) via the pinned citations; the LITUNIT pins; **D-5's
carrier/equality design — supplier DISPLAYED [REVISION 1, finding 5]:** this is NOT an
external input. The tree's D-5 (WELLPOSED) carrier/equality clause is INCORPORATED as
proved content of O-2a rev 5 itself (§§1–2: the two discrete carriers, the map Θ, the
consistency predicate, Lemma R — the design the V10 → pass-2 → V25 arc audited; its
verification debt is the same P(O-2a r5)). The tree's wider D-5 semantic-invariance
clause is carried on the mandatory path by each consuming leaf's own displayed carrier
section (O-13's run-collapse well-definedness at Step 3; O-1thr's §1 machine carriers
at Step 6; CU-1's declared pins at Step 10), not consumed as a separate node — §3.3
records this disposition.
Delivers: CU-2's content (no-phantom = Theorem 2(D) ∘ ι), CU-1's base and step
vocabulary.

**Step 8 (GD23 — the GD dictionary head, GD-2 ⊕ GD-3: frame/valuation tie, development
identity, residual dictionary, over an ARBITRARY complete DVR).** Statement: (GD-2)
u_{i+1} = v_{i+1} on low-degree nonzero B; w multiplicative (MacLane); w_{i+1}(B) =
min_k(w_{i+1}(C_k) + k·γ̂_i); window polygon = N_{i+1}(f) vertex-for-vertex; emitted
record (e,h,ℓ,s,u) verbatim. (GD-3, min core) ord_{ψ̂_hom}(in(f)) = ord_ψ(R_λ(f));
(full form) R^nrm = c·R_λ(f) with slot-wise residue-field bijections — the anchored
march, restated at rev 3 in BOTH printed FGMN Thm-4.2 pairings with the exact match
verified clause-for-clause against the printed source. Stated over an arbitrary
absolutely-unramified complete DVR (killing most of D-3 by scope discipline).
Status: **VERIFIED at honest scope — leaf #4** (arc: V26 UNSOUND with order-≤1 core
confirmed → rev 2 against the LITUNIT pins → V28 SOUND-WITH-GAPS 0-critical → rev 3
(notation split + properness proviso) → pass 2 fresh Fable, 0 criticals, 9
justification gaps, commit 4d109b9 → [the wave-10 VERIFIED adjudication at d730669 was
CORRECTED AS PREMATURE at the ledger, commit 110f308] → rev 4 repairing all 9 gaps,
cores byte-stable (commit f9878d1) → GAPS-CLOSED check → **ADJUDICATED VERIFIED at the
2026-08-02 harvest** (ledger final section, commit c43b13b) [REVISION 1 status sync]).
The
honest scope IS the conditionality: order ≤ 1 proved outright; **(GR-B) at orders ≥ 2
is a DECLARED open input** (the anchored-march residue-structure package, pinned to
printed FGMN Thm 4.2 + Cor 4.4/4.8/4.9/5.6 + Prop 5.14/Cor 5.15 + MacLane Thm
10.2/Lemmas 10.1, 11.1–11.2/Thm 12.1/Cor 12.2); the (KP)/(KPirr) clauses beyond order
1 and the properness proviso at e·g = 1 refinement reads are owned by GD-1 [the
beyond-order-1 clauses are since REVISION 6, finding F-2, the §3.1 (H1)(b) member —
hypothesis-grade, no longer a [T] filing; the properness leg stays pinned at §3.3];
engine
conformance (u = w for the implemented engine) is a declared non-claim (GD-4/GD-6).
Consumes: MacLane 1936 + GMN + FGMN via LITUNIT (every flag resolved MATCH; §4).
Delivers: the GD-2/GD-3 instances consumed by CU-1's step, O-9's census, O-11's K3.

**Step 9 (O-14a′ — RunRealizerExists, the upstream realizer supply).** Statement: every
coherent history's designated realizer exists (interior half from re-keyed coherence;
the (S6b′) monomial-shape clause + last-node landing flank closed at the O14ap arc).
Status: **PROVED-UNVERIFIED, pending pass named — the §3.2 row P(O-14a′ r3)**
[grade display conformed to the §0 status vocabulary at REVISION 9, VC10 advisory 1
— the former "TWICE-STABLE across V11/V15" display was not one of the four declared
grades]: core Theorems 1–3 stable across the V11 and V15 passes (rev 3 on file —
exactly the record the §3.2 row displays); the consumed instance RRE(p, ZMod p) is
FORMALIZED Lean-core (`MovesD/R7_runRealizer.lean`, `runRealizerExists_zmod`); the
CUC §0 "VERIFIED homes (no pass condition)" registry line is cited as PROVENANCE,
not as a grade — the V11/V15 models/cleanliness are not displayed in-document, so
no VERIFIED claim is made here. The landing-flank countermodel search ran and
produced no negation (positive gates only).
Consumes [list added at REVISION 1, finding 5]: T-1/T-2's MacLane-frame vocabulary
(symbolic scope, Step 2); the coherence predicate `HistoryCoherent` — the "re-keyed
coherence" of the statement — which is the O-3 clause-3 re-key EXECUTED at DELTA-2
§D2.1 line 1 (governance on record, not an open input); MacLane 1936 via the LITUNIT
pins (§4) — **exact loci [REVISION 2, finding V2-7]: Def 4.1 (key polynomials) + Thm
4.2 (augmented values), the (V1) frame-semantics pin (LITUNIT §4, verdict MATCH)** —
consumed as the intended semantics of T-1/T-2's MacLane-frame vocabulary, the same
pins Steps 2/6 ride; NO further MacLane theorem enters O-14a′ (its core Theorems 1–3
are self-contained per the V11/V15 arc, and the consumed Lean instance rests on Lean
core only). The (S6b′) clause and the landing flank are INTERNAL — closed inside the
O14ap arc itself (rev 3), not consumed from elsewhere. The consumed instance
RRE(p, ZMod p) rests on Lean core only. The rev-3 honesty ledger's open flags
(OL-1/OL-2/(F1)) ride the NON-consumed tower strengthenings (Prop 5.4.2 sharpness,
Conjecture 5.4.C) and enter nothing here.
Delivers: CU-3's nonempty-locus witness.

**Step 10 (CU-1 — no-missing + interior adequacy + state binding; THE ONE NEW PROOF of
the K-RUN-C composition).** Statement: one ordinary induction on chain length, N-free:
for every Cons_f chain 𝐇 with N\*(𝐇) ≤ N, (i) the canonical run on f at working level
N realizes 𝐇; (ii) the engine's frame state after the realized prefix equals the stage
data of Θ(𝐇) (the OL-6 binding). Interior adequacy = per-site SPAN totality inside the
same step; ADEQ split interior/terminal with terminals routed to O5triple's counting;
no box-keyed universal statement anywhere.
Status: **VERIFIED — leaf #5** [REVISION 3 status sync, ledger 7b28562]
(arc: V27 UNSOUND, core repairable → rev 2 structural repairs (N-free induction,
interior/terminal split, construction-before-recovery, declared pins) → V29
SOUND-WITH-GAPS 0-critical → rev 3 (consumption accounting only; induction
byte-stable) → pass 2 fresh Fable, 0 criticals, 7 consumption-accounting/display gaps,
commit 252f34c → [the wave-10 VERIFIED adjudication at d730669 was CORRECTED AS
PREMATURE at the ledger, commit 110f308] → rev 4 light repairs, all 7 gaps, cores
§§1–3 byte-identical (commit 8ab8ca6) → gap-check: GAPS-REMAIN → rev 5 (commit
6299a4a, the single GAPS-REMAIN item repaired, cores byte-identical) → r5 gap-check
**GAPS-CLOSED, 0 criticals / 0 gaps** (the F-4 Lemma-R scope-note residual verified
repaired; §3/§4/SURJ spot-checks clean; commit 5329a4e) → **ADJUDICATED VERIFIED
(leaf #5) at the 2026-08-02-night harvest**, ledger 7b28562). The mathematical core
(N-freeness, creation-first, split-exhaustiveness) was confirmed by the pass-2
report; the r5 gap-check closed the accounting/display residue. Primary gate: the
depth-3 falsifier ran BEFORE rev 1 and SURVIVED.
Conditionality carried: the step consumes GD-2/GD-3(min)/GD-6 instances at orders ≥ 2
through **(H1)** [routing display synced at REVISION 6, finding F-3: the former
"through (GR-B)" routed the GD-6 conformance instances through a row scoped
GD-2/GD-3-only — the (H1) group now displays them as its member (c), so this line's
routing is supplied row-for-row], plus the declared pins **(P-ROOT)/(P-KEY)/(FR≡)**
((FR≡)'s
refutable order-2 content = the gate's coherent-convention finding; adjudicated
convention-pin [T], §3.3 pins row — REVISION 6, finding A-6(iv)).
Consumes: Steps 6–9 (D-14, OL-6, OL-2a-2-min/3, SPAN/(M6a)/(M6c)/VTX, REAL≡, the
realizer supply).
Delivers: the no-missing direction of realization faithfulness (K-RUN's heart); K-14
(I0)/(I1)/(I3-rc); the (FRESH) consumption site.

**Step 11 (CU-2/CU-2t/CU-3/CU-4/CU-5/CU-7 — the rest of the K-RUN-C composition;
"CUC").** Statements (per DELTA-2 §D2.3): CU-2 no-phantom = O-2a Theorem 2(D) ∘ ι;
CU-2t the terminal seam ((t-i)/(t-ii) as ONE lemma from O-2a Thm 2(B) saturation +
Cor 1.20/3.8 + TB-CAP windows); CU-3 nonempty locus (coherent form) = O-14a′ + D-14 +
(O6) — **(O6)'s supplier DISPLAYED [REVISION 1, finding 5]:** (O6) = the O3design §1
canonicalizations (branch-letter keying + deep-marker pooling), a DEFINITIONAL
bookkeeping convention for census-side counting with NO proof content (CUC §3(c)
states exactly this; the O3design record passed the V16/V21 arc) — a §3.3 pin row,
not an undefined leaf; CU-4 acceptance, selected-f (F4-S soundness at every N; F4-C
completeness at
N ≥ N\*, TB-CAP (τ-irr) perimeter-conditional); CU-5 the satellites K-OB-A/B/E; CU-7
the O5triple interface discharge ((I-1)(a,b,c) + (I-2), with the attribution correction:
the content is single-polynomial and consumes NO K-DICT-OB1).
Status: **PROVED-UNVERIFIED as a consolidation record** — wave 8 confirmed 6/7
re-labels mapped to proved homes (commit a9f294c); **CU-2t is PROVED at R3** (CUC §9,
unit D3CU2t) conditional on P(O-2a r5) + TB-CAP's D-12r perimeter + the GD residue
**including the THREE NEW GD-4 terminal rows (T-DEC)/(T-READ)/(T-VERD)** [count
AMENDED 2 → 3 at REVISION 4, folding the CU-2t R3 §9.5 touch (3); PROMOTED to the
§3.1 [M] group (H6) at REVISION 5, finding VC5-2 — with (T-READ) re-graded into the
GD-2/GD-5 conformance family at CU-2t R4] — genuinely new
unproved mandatory-path obligations first displayed at CUC §9.4, folded into GD-4 (no
new open FAMILY) but growing the residue by three rows; the residue-count history is
stated plainly per the R3's own discipline: rev 1 billed ZERO new open inputs
(OVERSTATED, retracted at R2 per the CUC F4 correction), R2 billed TWO, and the
honest count is THREE — the R2 itself undercounted by one, the (RC-read) supply row
(T-READ) (the CU2tpass G1 finding; §3.3 GD-4/GD-6 row synced). [REVISION 3 status sync:
CUC pass 1 RAN — **UNSOUND** (b9d1d70: 1 critical,
§9.3(iii)'s Θ^τ injectivity constant in ν under an unconstrained hypothesis row, + 9
gaps incl. the (T-DEC)/(T-VERD) residue-growth contradiction; the §6.1 audit SOUND;
clauses (i)/(ii)-main survive) → **CU-2t R2 LANDED** (unit of record 567814d;
52d4eed's staging sweep captured an intermediate state: the ν-dependence critical +
its root, the undefined record-consistency predicate, repaired via the displayed
Definition RC, §§9.2a/9.3 — Θ^τ now carries ν's announced verdict pair, injectivity
restated at two displayed granularities; CU-2 rescoped to the continuing part 𝐇°;
surviving cores byte-stable; full disposition table CUC §10).] [REVISION 4 status
sync: the R2 hostile re-pass RAN — **SOUND-WITH-GAPS, 0 criticals** (4190ca4: the F1
ν-dependence repair CONFIRMED at both injectivity granularities, Definition RC
non-circular as displayed; 4 gaps — G1 the missing (T-READ) third row, G2 a (T-DEC)
row-internal contradiction, G3 the unrouted W3-carrier/2(D)-scope dichotomy, G4 (c1)
unquoted) → **CU-2t R3 LANDED** (81589e9, light: (T-READ) displayed with per-half
consumption at the §9.4 tie — the read-entry half consumes (T-READ), the verdict
half survives on (T-VERD) + clause (ii) alone; (T-DEC) SPLIT (T-DEC-dec)/(T-DEC-cor)
— corner bookings fenced to the Corollary at its own conditionality; pricing
corrected to THREE rows with the 0 → 2 → 3 history said plainly; the W3
carrier finding + 2(D)-scope dichotomy ROUTED to the O-2a owner at the P(O-2a r5)
charge — §9.5 touch (5); verified cores byte-stable); the fresh hostile pass on the
R3 record is the in-flight debt (§3.2).] [REVISION 5 status sync: the R3 pass (unit
CU2tp) RAN — **SOUND-WITH-GAPS, 0 criticals, both R3 logic repairs VERIFIED** (2
pricing-prose gaps + 1 minor) → **CU-2t R4 LANDED** (unit of record b9f05db: G-alpha
— **(T-READ) RE-GRADED honestly out of the GD-4 "engine bookkeeping" label**: its
(RC-read) content is read-semantics CONFORMANCE, mathematics-grade, graded in the
GD-2/GD-5 family with the per-case decomposition displayed (at k′ ≥ 1 an OL-2-min
instance + the genuinely new terminal-BINDING clause, which stays GD-4; at k′ = 0 no
sibling anywhere in the OL residue); G-beta — the false "meanwhile EXECUTED"
fold-state note rewritten to the verified repo state; M-gamma — the §9.1 a₀ ≥ 2
carrier display; cores byte-stable) → R4 spot-check CLEAN (unit CU2tc, d14cd8c: 3/3
repaired sites verified, hunk map proves cores byte-stable, no further full pass
owed per the CU2tp disposition) → **CU-2t ADJUDICATED VERIFIED at scope (leaf #9)**
at the 6a7593b harvest — the terminal seam, conditional per §9.5 (the three terminal
rows + the pinned literature); the three rows are since REVISION 5 the §3.1 (H6)
group (finding VC5-2).] [REVISION 6, finding A-4 — this step's TWO conditionality
renderings reconciled, the earlier one marked: the R3-era sentence above ("PROVED at
R3 ... conditional on P(O-2a r5) + TB-CAP's D-12r perimeter + the GD residue
including the THREE NEW GD-4 terminal rows") prices NEW and INHERITED conditionality
TOGETHER and is SUPERSEDED AS A CONDITIONALITY DISPLAY by the §9.5/leaf-#9 form,
which prices the leaf's NEW conditionality exactly (the three terminal rows + the
pinned literature). The R3 sentence's remaining items are INHERITED debts, each at
its own ledger home — P(O-2a r5) the §3.2 [V] row; TB-CAP's perimeter the §4 D-12r
literature pin [re-pointed at REVISION 14, r15 — formerly the §3.1 (H3) row];
"the GD residue" the O-2a OL residue at its §3.1 (H1)-group/(H6) and
§3.3 homes — and its "including" is NOT a ⊇ license: no un-enumerated GD residue is
consumed via CU-2t beyond those displayed homes.] The quarantined Corollary
CU-2t-deep (the
GD-8 exact-key
corner) is conditional on the unpinned (REP-IRR) literature pull and is consumed by
NOTHING on the mandatory path.
Consumes (mathematical inputs): Steps 6–10 + (O6) as displayed above. The CUC §0
pending-verdict registry is METADATA — the live key the P(·) grades resolve through,
never a mathematical supplier [wording fixed at REVISION 1, finding 5].
Delivers: no-phantom + terminal verdicts + acceptance = with Step 10 the full
realization-faithfulness package (the old O-3), serving K-4/K-6's repairs, O-4's
OL1-ii, O-5, O-7's (X1)/(X4).

**Step 12 (O5triple — O-5 halt/assembly + the D-7 bijection triple).** Statement: at an
ns-free leaf of a realized run the halting verdict is the true factorization datum
((τ-hen) via LIT-3 coprime Hensel; (τ-irr) saturated leaf via OM saturation through the
dictionary — the (τ-irr) leg's TB-CAP dependence is perimeter-conditional on the
§4 D-12r pin (entry 10) [locally re-pointed at REVISION 15, r16, per VC17
blocker 1: the r15-5 audit had confirmed this step as already-conformant because
its consumption is priced at the §4 entry's enumeration, but the r15 claim was
that EVERY former consumption site displays the conditionality locally — Step 12
now matches Steps 2/3/11/16/19]); Theorem B derives D-7b LEAF-COVER from injectivity + degree checksums
alone (NO no-missing consumed — the F2′-as-assembly note, verified); D-7a
LEAF-DISJOINT and D-7c LEAF-MULT close the triple; Theorem E: any two monic separable
lifts of a decided class have EQUAL splitting types (the multiset is engine data,
lift-independent) — the σ-constancy Step 17 needs.
Status: **PROVED-UNVERIFIED** — rev 3 (commit 6fe1b2d), V23: NO criticals → rev-3
repairs 0-critical; pending pass **P(O5t r3)**, PLUS the five §2 flags
(GMN-COH)/(R-0)/(R-1)/(R-2)/(R-3) — GD-1/GD-7/GD-8 duties, NOT covered by the executed
LITUNIT. The abstract counting core is Lean-formalized (`MovesU/O5CountingB.lean`).
Consumes: Steps 7/8/10/11 via CU-7's interface; LIT-3; GMN Def 3.10/eq. (37) +
Cor 4.19 + LIT-2 defectlessness.
Delivers: correct semantic leaf labels for O-11's K1 (Step 18) and D-11's M3 (Step 17);
type(g) = typemult(T) for every MONIC SEPARABLE lift g of the decided class —
Theorem E's own scope [RESCOPED at REVISION 10, r11, edit E-M3, per the D-11 r4
REVISION-2 gap-6 record: the former "for every lift g" was BROADER than the
premise displays (ns-null justifies only a.e., not all-lifts); the nonseparable
locus is Haar-null (T-6 (ns-null)), so every mass/counting consumer is unaffected,
and the D-11 r4 §4A walk certifies no mandatory consumer reads the unrescoped
breadth (its sites 1–6 are M3-free/label-blind; site 7 consumes mass-mediated at
separable scope)].

**Step 13 (O-4 + O4T — drainage: undec(k) → 0 at every prime, wild included).**
Statement, in two layers: (SEMANTIC, unconditional) the M05/V2-verified core — Lemma A
(drainage ⇔ a.e.-decidedness), Lemma C (discriminant tail bound, wild term a finite
additive shift n·v_p(n)), Lemma D (Krasner local constancy at modulus 2v+1, explicit
constant, wild-valid), Theorem E (the semantic classifier drains at rate
~p^{−N/(2(n−1))} at every prime) — plus Dfloor's SEM-DRAIN making the measure content
exact. (TRANSFER, the canonical classifier) O4T: TR-0 (a.e.-finite decision level ⇒
env → 0), FLOOR (any sound stable classifier is bounded below by the semantic one),
TR-Q (quantitative transfer under a nondecreasing UNBOUNDED majorant), and THEOREM N3:
at 2 ≤ n ≤ 3 the canonical order-≤1 classifier terminates, is sound and stable, with
the SHARP read ceiling L(f) ≤ v_p(disc f) + 1 at every prime including wild — hence
pointwise OL1 and drainage with explicit envelope, UNCONDITIONAL at n ≤ 3 (no CU/GD≥2/
BNS consumed). General n: TR-OM is CONDITIONAL on exactly the displayed seam
**(AGR) = OL1-ii** — the canonical run on each separable f terminates below a finite
N(f) with the certification predicate accepting there — assembled from CU-2 + CU-4 +
GMN Cor 4.19 (the DELTA-1 §D1.4 adjudication: this qualitative route is the MANDATORY
one; rates/O-6/O-7/O-8a are route R-QNT, struck from the root).
Status [REVISION 1 status sync]: M05 core **VERIFIED at the V2-upheld scope** (items
5–8); O4T **REPAIRED AT REV 3, AWAITING ONE FULL CLEAN PASS** (arc: V30 UNSOUND →
rev-2 repairs → Fable pass 2 UNSOUND, one localized critical: Corollary N3-BRACKET's
terminal "=" false for N ≥ 2 by the level-normalization factor p^{n(N−1)}, plus 4 gaps
(import inventory short: Galois descent + char-0 separability) → **rev 3** (commit
bb4326c): the terminal "=" → "≤" with the exact p^{n(N−1)} normalization identity
displayed and the verifier's 8-vs-32 refutation shown, the density form kept as the
identity-bearing statement (§4 consumed only the inequality — nothing downstream
consumed the false clause); import inventory COMPLETED to eight named Facts — Facts
**SEP** (char-0 separability of irreducibles) and **DES** (Galois descent/fixed field)
added as in-line-proved imports with consumption-site cite tags, six-vs-eight
reconciled (EF/GRD each bundle two of V30's items); the every-lift-separability chain
displayed in N3's proof; cores byte-stable (diff-verified). The repair was CHECKED
CLEAN at the 2026-08-02 harvest (ledger, c43b13b), but one full clean hostile pass is
still OWED before any VERIFIED grade; the mathematical core has now survived
line-by-line audit twice. [REVISION 3 status sync: pass 3 (the acceptance pass) RAN —
**SOUND-WITH-GAPS, ZERO criticals** (ed413ae; harness independently re-run, 2.53M
classes, ceiling sharpness confirmed; all 11 charges PASS), two one-line gaps (the
(I1) base-case "≥ 1" criterion display — the consumed form verified correct — and the
"one external import" undercount vs M05 Lemmas B/D) → **r4 (the two one-liners)** —
**LANDED at revision close** (3956500: the (I1) criterion corrected "≥ 1" → "> 0"
with the verifier's derivation displayed; the external-import count corrected to
three with exact consumption sites; cores byte-stable) — **+ one confirming pass to
the bar** (ledger 7b28562); the core has now survived line-by-line audit three
times.] [REVISION 4 status sync: the confirming pass (pass 4, acceptance) RAN —
**SOUND, CLEAN PASS: 0 criticals / 0 gaps** (8602e97: all 12 charges + acceptance
items pass; the core independently re-derived including both CEIL corners; harness
re-run digit-for-digit vs the sealed JSON) → **O4T ADJUDICATED VERIFIED (leaf #7)**
at the confirming-passes harvest (ledger 85670f7); the "REPAIRED AT REV 3, AWAITING
ONE FULL CLEAN PASS" header above is SUPERSEDED — the M05 core's V2-upheld grade
stands unchanged.]
Consumes: Steps 1/10/11 + GMN Cor 4.19 + the O4T import inventory (§4).
Delivers: the undec(k) → 0 clause of (SQ) = the SEMANTIC layer — unconditional at
every n and every prime (Theorem E + Lemma A/C/D, with Step 1's SEM-DRAIN making the
measure content exact); since §1's undec(k) is DEFINED semantically, this is all
clause (SQ) consumes [attribution corrected DOWNWARD at REVISION 4, finding VC4-4 —
the former line's "(AGR)-conditional at general n" priced the TRANSFER layer into
(SQ)]. The TRANSFER layer (the canonical classifier's own termination + drainage:
unconditional at n ≤ 3 by Theorem N3; conditional on (AGR) (= CU-2 + CU-4 + Cor
4.19, all on file above) at general n) serves the classifier-side counting reading
consumed by Step 19's squeeze machinery inside clause (R)'s conditional scope, and
the algorithmic-decidability strengthening — NOT clause (SQ)'s statement.

### Movement IV — the value side (the tables evaluate correctly)

**Step 14 (O-9 — the order-r stratum census).** Statement: for a type of order r the
order-(r+1) stratum census over O/π^N is one ℕ-valued polynomial in q per stratum,
independent of p, O, δ (census field degree d = f₀⋯f_r — the DELTA-3 §D3.2 index fix
governs). Level-1 (order-1 strata, every n, every prime power, every complete DVR) is
PROVED (M08 Theorem 2, 46-strata machine check). Order ≥ 2: REDUCED to exactly TWO
pre-existing named opens — **(GR-B)** (enters as the anchored-march residue structure;
CEN-W conditional on it alone) and **(FRESH)** (the joint-with-parent separation
clause; CEN-J = THE census). TYPE-MARCH (+ the F₃ type-breaking countermodel) shows the
march is what MAKES the census polynomial: GD-3-FULL is a theorem-level dependency of
the value side. OL-A's ledger (A1/TRI/LED/(DET)) proved outright; OL-B discharged to
GD-3 per DELTA-2.
Status [REVISION 1, finding 4 + status sync]: **REPAIRED AT REV 2, RE-PASS OWED.**
Verify pass 1 NOT ACCEPTED (commit c17777f; 2 criticals: the census exponent E′'s
left-tail ceiling REFUTED by machine countermodel (256 vs 64 — overcount q^d per
integral left-tail slot), and JUNCTION-PIN's PROVED label unsupported; the verifier's
own sound-list: TYPE-MARCH + F₃, A1/TRI/LED, (DET), CEN-W's (GR-B)-only conditionality
— the reduction's SHAPE survives). The pass-1 verdict made the then-current [V] filing
DISHONEST (finding 4 is accepted: a refuted exponent is a missing proof, not a
verification debt). **The repair has since LANDED — rev 2, commit 01c21e6:** E′'s left
tail made STRICT (floor(line)+1; the countermodel now predicted exactly, 64 = 64), the
K5 predict-from-ledger falsifier added (11/11 cells exact; the rev-0 formula refuted
at every integral-left-tail case), E″ displayed and validated against M08 at r = 0, an
**(ADM)** admissibility hypothesis displayed (an explicit hypothesis on CEN-W/CEN-J,
now a §3.1 [M] row carried by (ROOT-C)(H3) [REVISION 2, finding V2-5]; the r2's
"automatic at orders ≤ 1" claim is SUPERSEDED — twice rescoped, see the r3/r4 syncs
below; the single operative definition is the §3.1 row's FULL-attainment form
[REVISION 4, finding VC4-2]), and JUNCTION-PIN's
PROVED label WITHDRAWN — honestly rescoped as OPEN, absorbed into **(FRESH) as its
clause (c)** (the telescope consequence is proved GIVEN the clause). The sound-listed
core is byte-stable. With the repair on file the row is again honestly [V]:
re-pass owed. [REVISION 3 status sync: the re-pass (pass 2) RAN — **UNSOUND** (1
critical, 3 gaps; af2293e): both pass-1 repairs (strict left tail, junction-pin
rescope) verified SOUND, but the rev-2 (ADM) display rests on a FALSE class-vs-value
attainment equivalence (r = 1 countermodel: e₁ = 3, h₁ = 2, weights {0,2,4}, on-line
β = 1 unattained — empty stratum, positive formula; the automaticity claim refuted at
that reading); K5 + harness records verified log-exact → **r3 with the VALUE-attained
(ADM) reading** (ledger 7b28562) — **LANDED at revision close** (460ea59: automaticity
rescoped r = 0 automatic / r = 1 displayed decidable inequality / general r ≥ 1
withdrawn; LED-top(ii) image-subspace fix; K6 falsifier 0 = 0 exact); pass 3 owed.
The §3.1 (ADM) row carries the matching note.] [REVISION 4 status sync: pass 3 on r3
RAN — **UNSOUND, 1 critical** (b7df5c2: the attainment ladder goes one rung deeper —
VALUE-attainment (dim G_{β_k} ≥ 1) is insufficient at PARTIALLY attained slots; the
count needs FULL attainment dim G_{β_k} = d; machine countermodel from the note's own
LED-top(ii) type: r = 1, d = 2, CEN-W display 73728 vs truth 49152 at q = 2, ratio
(q+1)/q per partial slot — every prior value gate ran at d = 1 where the notions
coincide; all four charged r3 repairs + the census layer verified clean) → **r4 with
(ADM) at FULL attainment LANDED** (unit of record 35faac7, draft captured at
e8e1839/031784c: dim_{F_q} G_{β_k} = s(β_k+1) − s(β_k) = d, still p-free per-datum
decidable; the r = 1 inequality re-derived for the FULL predicate
β_k ≥ h₁((h₁^{−1}β_k mod e₁) + (f₁−1)e₁), collapsing to r3's at f₁ = 1; the r3
"(GR-B) bijection of F_{r+1}" gloss WITHDRAWN — read injective on G_β with image V_β,
onto ⟺ FULL, propagated to §§5.1/6.3/6.4; automaticity: r = 0 STILL automatic
(dim = f₀ = d); K7 falsifier ALL PASS — the countermodel refuted + the FIRST d ≥ 2
VALUE gate exact, census q^14(q²−1)² at p ∈ {2,3}); **pass 4 in flight**. The §3.1
(ADM) row is rewritten to this single operative form at REVISION 4, finding VC4-2.]
[REVISION 5 status sync: pass 4 on r4 RAN — **SOUND, 0 criticals** (the FULL-
attainment (ADM) form verified as displayed; two gloss-survival display sites: the
r4-withdrawn "(GR-B) per-slot bijection" gloss surviving byte-carried in two summary
lines) → **r5** (9335057: the withdrawn gloss purged from both sites — §0 OL-A(iii)
re-pointed at the LINE clause with the onto ⟺ FULL-attainment qualifier, §1 (P5)
re-scoped to §5.1's onto-ness at fully attained β; every other byte verbatim rev 4,
diff-audited) → pass 5 (confirming) **CLEAN: 0 criticals / 0 gaps** (825378d: both
sites verified fixed against §5.1; K7 re-executed byte-identical; census layer
clean) → **O-9 ADJUDICATED VERIFIED (leaf #8)** at the 6a7593b harvest — the order-r
census layer at its honest scope (CEN-W/CEN-J conditional on (GR-B) + (FRESH) +
(ADM)-FULL, all displayed; the FULL-attainment ladder closed). The §3.1 rows are
unchanged by the adjudication — the [M] conditionality IS the verified scope.]
Consumes: Step 8 (GD-3), GMN Prop 2.7/Lemma 2.2/Defs 2.19–2.21 via pins.
Delivers: the transition weights of T-7s's tables as proven counts — K3's census half
for Step 18.

**Step 15 (O-10 — sibling independence at adapted cells; K-COUNT).** Statement, sharper
than charged: in factor coordinates over ℤ_p, the fibers of the level-N multiplication
map over a ρ-separated polydisc are constant and nonempty at EVERY N ≥ τ, of size
p^{Σ min(e_i, N−τ)} (Smith profile), = p^ρ exactly in the D-8 guard range N ≥ τ + e_max;
Theorem 2 = the adapted-cell (SIB) product law, UNCONDITIONAL in factor coordinates;
K-JAC = Lemma 3; OL-4/D-8r RESOLVED (no small-N anomaly). Application to the engine's
realized site strata is conditional on the two displayed interfaces **K-LOC** (= M07
OL-3) and **(I-τ)** — since REVISION 6 (finding F-1) the §3.1 [M] row **(H7)**: the
former "both priced into the GD families" had NO displayed [M] landing (no GD row
carried read-locality/normalization content), so the pair is now hypothesis-grade
with this step and Step 18's K1 as its displayed consumption sites.
Status: **VERIFIED at scope — leaf #6** [REVISION 3 status sync, ledger 7b28562]
(commit 3e9f199; pass 1 CLEAN, ec5e9a1: 0 criticals, 0 load-bearing gaps; pass 2
model-diverse SOUND-WITH-GAPS, 5296528 — all five charged proofs check, Lemmas 1/2
verified incl. the adversarial minor case, harness 68/0 reproduced; 2 justification
gaps: the D-8 value-vs-law split collapses definitionally (fix rides the next doc
touch) + one undisplayed box-count transport cite → **ADJUDICATED VERIFIED (leaf #6)
at scope** at the 2026-08-02-night harvest; the two gaps were repaired at r2, landed
at revision close (3956500) — VERIFIED stands per the wave-14 ledger, framing fixed).
Falsifier: `verification/o10_kcount_harness.py` 68 PASS / 0 FAIL, predictions sealed
pre-run.
Consumes: LIT-8c (Sylvester/resultant); GMN stage factorization **K-FACT — pinned
[REVISION 1, finding 8]: K-FACT = GMN Thms 1.15/1.19 + Thms 3.1/3.7** (the recorded
perimeter, `docs/GMN_citations.md`; the O-10 §8 honesty ledger consumes exactly this
citation set — all four loci already in the §4 GMN pin list).
Delivers: K1's product law for Step 18; T-6's (SIB-STEP) at the statement layer.

**Step 16 (O-8b + D-3 — the unramified-base package; clause (UB)).** Statement: the
root's "same R_τ at q = p^δ" clause. Route (adjudicated at GD dispatch): the GD
families and O-9 are stated over an ARBITRARY absolutely-unramified complete DVR from
the start (Steps 8/14 — GD23's generality audit V26-confirmed: no proof uses
completeness, perfectness, or a Teichmüller section), collapsing D-3 to per-leaf scope
hygiene PLUS the alphabet slice O-8b: at δ-stable position classes the re-based slot
map is F_{p^δ}-linear and a_δ = δ·a₁ — M14's conditional proof over the displayed
(R1)–(R4) (the T-1 stage laws re-proved verbatim over O_δ = W(F_{p^δ})).
Status: GD23's arbitrary-DVR scope **VERIFIED** (inside leaf #4); O-8b
**PROVED-VERIFIED at the P(O-8b/M14) ACCEPT record** [upgraded at REVISION 22,
r23, executing H4M7 §3 item 3 — formerly "PROVED-UNVERIFIED conditional on
(R1)–(R4)": the (R1)–(R4) base is WRITTEN AND PASSED (M1 accepted + M2 done),
M14's Lemma-1(b) display synced at its REVISION 1, and the composite passed
CLEAN twice consecutively, model-diverse (passes 3+4; ledger 5a10256); the
former §3.1 (R1)–(R4) row is RETIRED to the §3.2 (H4a) retirement row, which
carries the composite's own displayed bounds — this upgrade is the (R1)–(R4)
leg ONLY: the δ-stable scope, (K3-δ), and the standing (H1)-group rows are
untouched]; the pass row **P(O-8b/M14)** (§3.2) is DISCHARGED into that
record [REVISION 3, finding B2 named it; REVISION 22 closes it]; N8 numerics
exhaustive (199,723 alphabets, 17 fields, 0 violations).
**Honest scope of what this step proves [REVISION 1, finding 7]:** (i) the GD/census
layers (Steps 8, 14) hold over an arbitrary absolutely-unramified complete DVR —
proved/stated at that generality from the start; (ii) the alphabet law
(F_{p^δ}-linearity of the re-based slot map + a_δ = δ·a₁) AT δ-STABLE POSITION CLASSES,
formerly conditional on (R1)–(R4) **[retirement bracket added at REVISION 23, r24 —
VC25 finding 1: that obligation is DISCHARGED by the retired row's F-chain (the §3.2
(H4a) = (R1)–(R4) retirement row; P(O-8b/M14) ACCEPT, ledger 5a10256) — the
δ-STABLE-POSITION-CLASSES scope of this clause and the (K3-δ) consumption (housed at
the §3.1 (UB-X) row) are UNTOUCHED by the retirement and remain live]**. This does
NOT yet derive the theorem's global clause (UB):
missing are exactly (a) the per-leaf O_δ re-base audit for the leaves stated over ℚ_p
(the dictionary/CU/drainage layers — the "per-leaf scope hygiene" of the D-3 collapse,
declared but unwritten), (b) that δ-stable position classes exhaust the classifier's
realized ledger at every δ, and (c) that the full solve + type aggregation commute
with unramified base change. (a)–(c) = the displayed row **(UB-X)** in §3.1; clause
(UB) of (ROOT-C) is conditional on **(H1)–(H3)** (inherited through this step's
Consumes-list: Step 8 is (GR-B)-conditional at orders ≥ 2, Step 14's census is
(FRESH)/(ADM)-conditional at orders ≥ 2, and the (UB-X)(a)-transported
dictionary/CU/drainage layers carry (E-N) plus the §4 D-12r pin's TB-CAP perimeter
[the latter literature-conditional since REVISION 14, r15 — no longer an (H3)
member] — none of these evaporates over O_δ)
**+ (H4b) = (UB-X)** [the former lead item **(H4a) = (R1)–(R4) RETIRED at
REVISION 22, r23** — this step's alphabet-law leg now rides the PROVED
(R1)–(R4) package at the P(O-8b/M14) ACCEPT record, the §3.2 (H4a) retirement
row] **+ (H5) = (PACK)** (the actual-pack
correspondence — since REVISION 7, finding VC7-2, incl. its PACK-i/ii/iii row-law
clauses, AND since REVISION 10, r11, its FOURTH row-law **(PACK-iv)**, entering the
transported evaluation chain at its closed edge set: Step 18b's direct (RE)
consumption + Step 17's (ROW-Σ) delivery display, both riding to q ↦ q^δ —
inherited through the transported evaluation chain — the value tie
at q^δ runs through Steps 17/18/18b's pack consumption; added at REVISION 4, finding
VC4-3) **+ (H6)** (the terminal-seam engine-conformance group, inherited through the
same transported chain — the terminal-verdict and in-fence content does not
evaporate over O_δ; added at REVISION 5, findings VC5-1/VC5-2) **+ (H7)** (the
(I-τ)/K-LOC sibling-interface pair, inherited the same way — the transported
product-law chain still enters through K1's engine-stratum receipt; added at
REVISION 6, finding F-1) (the formerly-listed **(H9) = (NSF)** — inherited
through the transported K1/K4 chain at q ↦ q^δ, added at REVISION 10, r11;
this step's display synced at REVISION 11, r12 — finding VC11-2 — was RETIRED
at REVISION 19, r20: the transported ns-routing content now rides the retired
(NSF) target at the pinned scope, ledger H9-C3 — the §3.2 H9 retirement row;
the formerly-listed
**(H8) = (K-CUT)** was RETIRED at REVISION 16, r17: the transported
cut-decomposition content (M04 Lemma 3.3's hypothesis at Step 18's K1
regrouping) now rides the retired machine-proved package **+ (H8′)**, the
K-CUT residual-interface row (GRAFT / MASS-ID / POOL-DICT — ADDED at REVISION
17, r18, executing VC19: the r17 owner homes did not house these interfaces) —
the §3.2 K-CUT retirement row), exactly as §1 now
displays [attribution
completed at REVISION 3, finding B1; run to the twelve-row scope at REVISION 11;
to the TEN-row scope at REVISION 16; to the ELEVEN-row scope at REVISION 17;
to the TEN-row scope at REVISION 19, r20 — the (H9) retirement]. [REVISION 2, finding V2-2: (R1)–(R4) is NOT
(UB)-only — it also enters
clause (R) through Step 18's K3-c; the split (H4a)/(H4b) records this.]
Consumes [list added at REVISION 1, finding 5; synced at REVISION 22, r23]:
Step 8 (the arbitrary-DVR GD statements + the V26-confirmed generality audit),
Step 14 (the census over any complete DVR), Step 2 (the T-1 stage laws as the
re-proof template for (R1)–(R4)), the ACCEPTED P(O-8b/M14) composite (M14
REVISION 1 + M1 + M2 — the retired (R1)–(R4) package, §3.2 retirement row),
and the §3.1 row (UB-X). N8 is a flag, never a substitute.
Delivers: clause (UB) at the displayed conditional scope.

**Step 17 (D-11 first tranche — MASS-ID M1/M1′/M3: the pack side; the instance pin).**
[REVISION 1, findings 2/11: D-11 is STRATIFIED. Its M2 clause consumed (S2) = the
O-11 composition datum while O-11 consumed D-11's pack side — a genuine cycle as
originally displayed. The honest order is **D-11 {M1, M1′, M3} ≺ O-11 ≺ D-11 M2**, and
the steps now follow it: this step carries ONLY the first tranche; M2 is Step 18b.]
Statement [REWRITTEN at REVISION 10, r11, edit E1 — the r4 true-scope form; the
consumed leaf is `D11_rev4_2026-08-04.md` (REVISION 3), whose reference measures
are named PER CLAUSE]: (M1, one-step) every family-(i)/(ii)/(iii) solve entry of
𝔅_n at every prime power IS the Haar mass of its continuation event CONDITIONAL
on the block state law C_e; family-(v) atoms at their per-occurrence (a′) readings
(MEASURE-SPECIFIC: ambient boxes C_e/R_e/□(P) incl. Lemma (BOX-VOL)'s relaxed-box
reading of the L6a factor; the CONDITIONAL ratio P(R_e | C_e) = q₀^{−(E−e)};
per-cell singleton residuals; the (O3) scalings NOT masses — their whole mass
content is (a)'s resummed-event bullet); families (iv)/(vi)/(vii) excluded
IN-STATEMENT ((a″) — family-(iv) verdict rows under arbitrary α carry no per-entry
mass identity). Σ = 1 holds at the displayed ONE-STEP STRATA (M1(d)); member-row
normalization at (RE)/(PACK-iv) members is **(ROW-Σ), a PROVED lemma** (the leaf's
§2A.2 theorem, adjudicated AS A PROOF at the r4 passes): solved totals
Σ_σ β_e(σ)(q₀) = 1, and the per-booking verdict-row totals are the Corollary-D
termination margins Φ^{(O1)}/Φ^{(O2′)}/Φ^{(O3)} — NO booking's displayed row total
is 1. Multi-step disjointness holds at prefix-free families (**(b-PF)**, proved);
grammar exhaustiveness = **(b-EX), a PROVED lemma at its route-(a) scope**
(quantified over the EXPLICITLY RESUMMED row terms — the (O3) kernel resummation
and the (O3)-scaled exit rows; solved-value legs are non-booked continuation
values, excluded); the infinite loop-0 word and the finite-time Z_e encounters per
**(LOOP-∞)** (null + disjoint, exact decomposition). Per-block a.s. exit is
UNCONDITIONAL; multi-block a.s. termination lives at Lemma M2.0's
(S1-m)+(DET) conditionality with the (O-1)–(O-5) + (O-GLOB) write-outs PROVED at
that displayed billing (Step 18b's layer). All of M1 at this scope is
UNCONDITIONAL over the VERIFIED O-12 lemma base (L1–L6′). (M1′) the (O2)
verdict-row coarseness finding (gate object, not a density carrier). (M3) row
labels are true types AT SEPARABLE-LIFT SCOPE: σ-constancy across one continuation
event's cylinder for every MONIC SEPARABLE lift (Theorem E's own scope; ns-locus
Haar-null, so mass/counting consumers unaffected) ← O5triple Theorem E + D-15
(per-box lift uniformity) — NO ∼_OM/OB2 consumption (the DELTA-3 §D3.1 audit,
re-confirmed constructively by this unit; zero root-side BNS touch-points); the
routing of these labels into the COUNTED corpus — formerly priced at
**(H9) = (NSF)** — rides the RETIRED (NSF) target at the pinned scope
[RETIRED at REVISION 19, r20: (NSF-INT) at ledger H9-C3 — the pin → predicate
→ corpus chain proved at I-1 (the D-11 r4 §4B leaf proof) with its (K1-CANON)
premise DISCHARGED at the M04 §5.5 (ed-1) scope pin; (NSF-KEY), the as-built
keying, RETIRED at REVISION 13, r14 (Unit C executed) — the §3.2 H9 retirement
row, RE-OPEN TRIGGER = the D-11 leaf bar].
OL-O12-2's mathematical half CLOSED here.
Status [REVISION 1 status sync]: **PROVED-UNVERIFIED, r2 owed** (attempt landed,
commit 0cae7d8; wave-10 adjudication: "THEOREM M1 PROVED UNCONDITIONALLY over the
VERIFIED O-12 lemma base"; verify pass 1 NOT ACCEPTED as billed, commit 2587c70 — 1
critical: M1(c)'s MULTI-BLOCK a.s.-termination billing consumes open (S1) while
ledgered unconditional (per-block survives unconditional; contained), + 3 gaps; M1(a,
b,d), the (O3) resummation, and the uniqueness pivot SURVIVE → D-11 r2 owed).
[In-flight at REVISION 1 close: **r2 LANDED**, commit 566b967 — the critical contained
exactly as specified (M1(c) rescoped to per-block unconditional content; multi-block
a.s. termination MOVED to Lemma M2.0 at M2's (S1) layer; the (O2′) balance leg
DERIVED; a THIRD seam **(S3)** = the projective assembly weight DECLARED, owner annex
A-1, monic scope pinned on M2's statement); pass 2 launched.] [REVISION 3 status
sync: pass 2 (V31) RAN — 2 criticals, both CONTAINED at **r3** (8a0786c: M1(a)
rescoped to families (i)–(iii) with displayed exclusions; the completed-node
overclaim WITHDRAWN — status honestly LAYERED, clause 4 = the actual-pack
correspondence OPEN as cited; Lemma M2.0 rewritten over countable symbolic
addresses); pass 3 owed.] [REVISION 4, finding VC4-3: the OPEN clause 4 is now a
LEDGER ROW — the §3.1 [M] row **(PACK)**, carried by (ROOT-C)(H5): what Steps 18/18b
consume of this step is the M1/M1′/M3 layers at their displayed conditionality PLUS
that correspondence, and the correspondence has no proof on file — it cannot ride
the §3.2 D-11 [V] row (a pass-debt class requires a proof on file), so it is priced
as hypothesis-grade open mathematics.] [REVISION 10 status sync, r11, edit E2: the
owed pass 3 RAN — **CRITICAL** (`D11_owedpass_2026-08-03.md`: 6 criticals/3 gaps —
M1's conditioning scope, the family-(v) factor-as-mass claim, the §5/§7 billing,
M1(b)'s multi-step disjointness, M2's universal scope, M3(b)'s silent clause-4
consumption) — the STOP-THE-LINE event that SUSPENDED the Phase-C acceptance
(ledger 44ce0a7). Repaired at TRUE SCOPE per the five-round repair design
(`R11_REPAIR_DESIGN_V5_2026-08-04.md`) as **r4** = `D11_rev4_2026-08-04.md`, then
hostile-passed TWICE: r4 pass 1 (`D11r4_pass1_2026-08-04.md`) CRITICAL — folded at
the leaf's REVISION 2; r4 pass 2 (`D11r4_pass2_2026-08-04.md`) **GAPS-ONLY: no
critical remains, all six pass-1 repairs adjudicated VALID, and the design-§7
obligations ((b-EX) at route (a), (O-1)–(O-5), (O-GLOB), (ROW-Σ)) adjudicated AS
PROOFS**; its three bookkeeping gaps folded at the leaf's REVISION 3. This step
consumes the REVISION-3 text; the leaf's own clean bar (a confirming pass on that
text) rides the §3.2 D-11 row.]
Falsifier: N6-regp-ext2000, 303/303 primes < 2000, all EMPTY, margin sharp at 6/7.
Consumes: Step 5's lemma base (as the mass statements they are), Step 6 (D-15), Step
12 (Thm E), T-3/T-6 mass laws; the §3.1 [M] rows this step's own Statement/Delivers
display — **(PACK-iv)** ((H5),
entering through the (ROW-Σ) delivery display's (RE) hypothesis at the consumed
member) [summary synced at REVISION 12, finding VC13-3, per the post-VC11-5
convention: Consumes summaries carry the consumed [M] rows; the formerly-listed
**(H9) = (NSF)** (the M3 label-routing price) RETIRED at REVISION 19, r20 —
the routing rides the retired target at the M04 §5.5 pin, the §3.2 H9
retirement row]. NOTHING from Steps
18/18b.
Delivers [synced at REVISION 10, r11]: the pack-side mass laws AT the r4 per-clause
scope + true row labels (separable-lift scope; the former (H9) routing price
RETIRED at REVISION 19, r20 — the M04 §5.5 pin) that Step 18's
K1/K2 consume; **(ROW-Σ)** as a PROVED lemma in this tranche — per the leaf's §2A.4
closure walk this Statement display is (ROW-Σ)'s ONLY delivery edge (no consuming
proof line on the mandatory path reads a member-row normalization, relative to the
displayed residue (EDGE-RES)), and THROUGH this edge (ROW-Σ)'s (RE) hypothesis at
the consumed member = **(PACK-iv)** ((H5)) enters this tranche; D-9k's escape input
at the pack; the (r1) uniqueness pivot Step 18b re-uses.

**Step 18 (O-11 — SERIES-TIE: per-τ tree series = R_τ).** Statement — displayed
exactly, since Step 19's attribution rides on it [REVISION 1, finding 11]:

    (O-11)  for every type τ: the σ-graded tree series of T_can, assembled at the
            intended 𝔅_n instantiation, EQUALS the block-solve value R_τ in ℚ(q).

This is the SERIES = SOLVE-VALUE tie ONLY — it does not by itself say any Haar density
equals anything (that is Step 18b). Proof: the M04 Theorem-4 walk (least fixed point,
Bekić triangularization, Neumann under spectral escape; proved self-contained)
re-priced at the D-9 corrected dependency set: K1 ← O-10 (guarded) + (I-τ)→GD [the
(I-τ)/K-LOC interface = §3.1 (H7) since REVISION 6, finding F-1 — without it K1 does
not receive O-10's product law at the engine's strata] + CU
leaf labels [which carry the terminal-seam (H6) content and (FRESH) — the A-5 route
leg] + the Lemma-3.3 shallow-cut regrouping, whose cut consumption now cites the
**RETIRED (K-CUT) package + (H8′)** (§3.1, the K-CUT residual-interface row) [the former
§3.1 row (H8), NEW at REVISION 10, r11, from finding G-2 of the un-banked O-11
hostile pass of 2026-08-03 (`O11_owedpass_2026-08-03.md` — recorded at the §3.2
O-11 row; provenance disambiguated at REVISION 12, finding VC13-4); RETIRED at
REVISION 16, r17: the exactly-one-decomposition / read-ownership /
weight-preservation kernel that M04 Lemma 3.3 consumes is machine-proved
Lean-core in its exact-form K1 convolution shape — `kcut_reduce` +
`kcut_k1_form` + `massFactor_mass_eq` (`Scaffold/HDischarge/H8/`), the 3-round
pass arc closed at DISCHARGES (`KCUT_pass3_2026-08-06.md`) — CONDITIONAL on the
typed residues, PRICED since REVISION 17, r18 (executing VC19), as the §3.1
row **(H8′)**'s three members: (i) GRAFT — graft completeness/bijectivity
(`graft`/`graft_cut`/`cut_graft`); (ii) MASS-ID — the C2 `shMass`/`blkMass`
measured-mass identification at the consumed member; (iii) POOL-DICT — the
node-level τ/δ pool dictionary + `blockTotal_tie`'s instantiation. The r17
owner labels (the D-14 + CU-3 + REL.2 arc; the (H7)/sibjc TREE-EXP +
[2r]/CL-8 + O-1/O-3 owners; [2r]/CL-8 with the W-leg at CL-17(ii)/W17ii)
stand as retirement ROUTES at the §3.2 K-CUT retirement row, NOT as housing
statements — the VC19 adjudication. No separate (H8) hypothesis remains] + the ns-ROUTING of K1's labeled corpus at the **RETIRED (H9) = (NSF)** target
[formerly the §3.1 (H9) row, NEW
at REVISION 10, r11: the (H9) TARGET — at any instantiation satisfying the
interface pin (q-4), every K1/K4-labeled box lies in the decided-ns-free counted
corpus and D-15's exact-classification package applies with no a.e. weakening —
now holds at the INTENDED/PINNED instantiation with BOTH members retired
[REVISION 19, r20]: (NSF-KEY), the as-built keying face, RETIRED at REVISION
13, r14 (Unit C executed — the guarded carrier landed at HEAD, the M02 V₀
exact-square poison dead; compiled witness H9-U1); (NSF-INT), incl. its
displayed (K1-CANON) premise, RETIRED at ledger H9-C3 — (K1-CANON) DISCHARGED
by the M04 §5.5 (ed-1) scope pin (K1's quantifier + the post-K4 sum pinned to
(q-4)'s notion, preamble line; Lemma 3.3 untouched; the warrant note
`K1CANON_discharge_2026-08-02.md`). Bounds: the site closure is
(CH-RES)-relative; no carrier non-vacuity claim; the vt_surj residue stands.
RE-OPEN TRIGGER: the D-11 leaf bar (the §3.2 D-11 row) — a §4B-defective
finding re-opens I-3 and suspends the (NSF-INT) retirement. The two-channel
consumption record is KEPT per the D-11
r4 §4A audit: (ch-1) K1's exact combinatorics is label-semantics-blind, (ch-2) the
semantic tie is mass-mediated at Step 19's squeeze; (CH-COV) executed CLEAN at the
walked closure, corpus-relative residue (CH-RES); record: the §3.2 H9
retirement row]; K2 ← O-12 at 𝔅_n + D-11 PACK
SIDE (= Step 17's tranche, never M2) + the
**(PACK)** correspondence-plus-row-laws (§3.1, = (ROOT-C)(H5): that the concrete
assembled pack IS
the intended 𝔅_n instantiation entry-for-entry — Step 17's OPEN clause 4, consumed
here and at Step 18b as the identification the tie evaluates through [REVISION 4,
finding VC4-3] — AND, since REVISION 7, finding VC7-2, the three pack row-laws
**(PACK-i) rsh_interp / (PACK-ii) r_bdd / (PACK-iii) wsh_ok**, consumed by this
step's restated D-11 entry item (v), sub-attribution exact [synced at REVISION 8,
finding VC8-2]: (PACK-i)/(PACK-ii) at THIS K2 evaluation glue (the Rsh → Rval
conversion and the [0,1] bound behind ENNReal.ofReal); (PACK-iii) at K3 below
(the evalAt regular-subring membership, with D-6); the FOURTH row-law
**(PACK-iv)** sem_alloc (since REVISION 10, r11) is NOT consumed at this step —
its closed two-edge set is Step 18b DIRECT + Step 17's (ROW-Σ) delivery display
[noted at REVISION 12, the global enumeration audit: (H5) has FOUR members
i/ii/iii/iv per §1/§3.1; this step consumes exactly three]: they
are EXPLICIT (H5) clauses because entry-for-entry equality transfers a law to the
concrete pack only if a displayed theorem establishes it at the corresponding
𝔅_n rows, and no consulted record displays one — the correspondence alone does
NOT supply them); K3 =
[1v]-FULL value correctness ASSEMBLED from T-7s + O-9's census + O-8b
(arbitrary-DVR), with **(PACK-iii) wsh_ok** the membership input to K3's
evaluation ring-hom evalAt (with D-6) [REVISION 8, finding VC8-2];
K4 ← Dfloor/D-15/CU mass semantics — D-15's supplier scope is "T decided ns-free",
so K4's in-scope invocation at the labeled corpus rides the **RETIRED (H9) =
(NSF)** target exactly as
at K1 [(NSF-INT)(ii)'s K4 leg is proved at the interface pin in the D-11 r4 §4B
leaf proof; the as-built HEAD keying — (NSF-KEY) — RETIRED at REVISION 13, r14
(Unit C executed; formerly the failing coincidence, REVISION 10, r11);
(NSF-INT) incl. its (K1-CANON) premise RETIRED at REVISION 19, r20 (ledger
H9-C3 — the M04 §5.5 pin; the §3.2 H9 retirement row, re-open trigger = the
D-11 leaf bar)]. Every consumption displayed (the unit's 20-row
table). δ-stable interface note [REVISION 3, finding A5; PRICED at REVISION 4,
finding VC4-1]: O-8b's law is declared AT
δ-STABLE position classes only (Step 16), and that K3-c's realized δ > 1 pools are
δ-stable position classes is a match ASSERTED at the D-c interface (O-11 r2/r3's
declared row) with NO proof or [V]/[T] row supplying it. It is therefore no longer
carried as a presumption inside (H4a): the fragment is NAMED **(K3-δ)** = (UB-X)(b)
restricted to K3-c's realized pools, and clause (R)'s displayed attribution
CONSUMES it ((H1)–(H3) + the (K3-δ) fragment of (H4b) + (H5) + (H6) +
(H7) + (H8′) — §1's full clause-(R) set [(H4a) dropped from this quote at
REVISION 22, r23 — the (H4a) retirement: K3-c's (R1)–(R4) leg now reads the
PROVED package at the P(O-8b/M14) ACCEPT record, and this audit note's home
is the (UB-X) row; this "— §1" quote synced at
REVISION 12, finding VC13-2: the former parenthetical stopped at (K3-δ),
understating §1 by four groups, and called (ROOT-C) "the joint five-hypothesis
theorem" — a REVISION-4-era count against the current roster; (H8) dropped from
the quote at REVISION 16, r17 — the (K-CUT) retirement; (H8′) added at
REVISION 17, r18 — the residue re-pricing; (H9) dropped from the quote at
REVISION 19, r20 — the (NSF) retirement]; the
theorem's hypothesis roster is unchanged by the NAMING since (K3-δ) ⊆ (H4b) —
the naming moves nothing in or out). A direct match display (or the H4M7 §4
Route-B re-scope, five-condition-gated incl. Asvin sign-off) rides the next
O-11/O-8b touch and would retire (K3-δ) from clause (R) at this interface
[the former "return clause (R) to (H4a)-only" reading is superseded at
REVISION 22, r23: (H4a) is retired, so the return target is the PROVED
package alone].
H.6 fence display [unit TREE-GAPS, audit gap G-4]: K3-a consumes T-5's H-DOMAINS
package at its VERIFIED-DUAL perimeter, whose named open kernels are the tree T-5
row's six H.6 fences (XHD-d-EX(∂), M4b-T-AUD, U-R-COUNT, XHD-u-X, H-LIST-GEN, E-POS —
roster with owners/refuters at `ASSEMBLY_PLAN_2026-07-26.md` REV 15/16). The tree's
"six fences gate [1v]-FULL" line is SUPERSEDED AS ROUTED by the O-11 r2/r3
restructure (K3-a, the G-5 repair): NO fence is consumed here as an open general
lemma — the ROOT-consumed residue is the SINGLE condition that the built per-n tables
sit INSIDE the fenced perimeter, proved nowhere at general n (V-n3 178/178 / H-n3
151/151 are sealed n = 3 evidence), = D-11's restated-charge item (vii).
**[REVISION 5, finding VC5-1 — the routing below is SUPERSEDED:]** the former display
("carried at the §3.3 widened-charge row ... No (ROOT-C) hypothesis is affected: the
condition rides D-11's PENDING tier-1 grade (Steps 17/18), not (H1)–(H4b)") is
RETRACTED as the VC5 blocker: the condition is an OPEN PROPOSITION consumed here on
clause (R)'s mandatory path (Step 18's K3-a requires it), expressly "proved nowhere
at general n" — under the ledger's own classification rule it cannot sit in [T].
Item (vii) is PROMOTED to the §3.1 [M] group **(H6)** (with CU-2t's three terminal
rows), consumed by clause (R); the §3.3 widened-charge row keeps items (v)/(vi)
only. The escalation clause survives inside (H6): if a built pack ever NEEDS fenced
territory (sharpest: H-LIST-GEN's beyond-(TRI) partitions), the fence kernel so
touched becomes ADDITIONAL consumed open mathematics beyond (H6)'s conformance
condition. [Concurrency sync, REVISION 4 — historical: (H5) = (PACK) is D-11's
clause 4, a DIFFERENT D-11 clause from item (vii); the R4 flag "whether (vii) too
should be promoted is flagged to the next VC pass" is RESOLVED here — VC5 promoted
it.] Full adjudication + the fence-by-fence mapping table: tree DELTA 4 §D4.2.
Status [REVISION 1 status sync]: **PROVED-UNVERIFIED (ASSEMBLED), r2 owed** — commit
3fc8f52; tier-1 residue exactly **{(GR-B), (FRESH), CU-2t, D-11 first tranche}**;
verify pass 1 NOT ACCEPTED AS STATED (commit a7ede72 — 1 critical: (M6b)(iii) consumed
at K4-CYL/row 4 but priced into no tier, so the "exactly FOUR" residue display is
unsupported as written, + 8 gaps incl. reductions asserted by pricing-citation; the
equivalence directions and the walk itself CLEAN; drainage verifiably unconsumed) →
O-11 r2 owed. [In-flight at REVISION 1 close: **r2 LANDED**, commit 1bd8bd1 — the
"exactly FOUR" display withdrawn, restated as the tier-1 four + THREE DECLARED
interface rows with ledger homes HERE: **D-a** = (M6b)(iii)+(ii−) → the §3.3
GD-4/GD-6 row; **D-b** = (I-τ)/K-LOC → the §3.3 interface row; **D-c** = (R1)–(R4) →
the §3.1 [M] row (= (ROOT-C)(H4a) since REVISION 2: through K3-c this row conditions
clause (R) itself, not only (UB) — finding V2-2); V1 corrected to the ENGINE-PROXY
form; NEW supplement
`o11_v2_oracle_pin.py` (10/10, engine ≡ oracle as rational functions) closes the
executed-vs-sealed gap; pass 2 launched. The r2's §9 owed ROOT touches are EXECUTED
in this document — see §7.] [REVISION 3 status sync: pass 2 (V32) RAN — billing
precision items → **r3** (7aef1b9: tier 2 SPLIT into hypothesis-grade rows H-1
((ADM), consumed at K3-b) / H-2 (D-12r — since REVISION 14, r15, the §4
literature pin, no longer hypothesis-grade) / H-3 (the O5triple five flags) / H-4
(LIT-1), the "nothing in tier 2 adds an open lemma" claim WITHDRAWN; §7.1 oracle-pin
premises displayed; walk/cores byte-stable); pass 3 owed.]
Falsifier ran FIRST: `verification/openmath/o11_seriestie_check.py`
including Finding F1's test at the real-pack proxy (the n = 2 solve reproduces the
true triple identically in ℚ(q); n = 3 five types symbolic; monotone bracket
exhaustive p = 2, 3, 5; the pass-1 record corrects the count to 37/37 live).
Consumes: Steps 1, 2 (DIRECT [added at REVISION 13, VC15 advisory 1, the
VC11-5/VC13-3 summary convention]: K3's assembly line consumes T-7s — "K3 =
[1v]-FULL value correctness ASSEMBLED from T-7s + O-9's census + O-8b" — and K3-a
consumes T-5's H-DOMAINS package at its VERIFIED-DUAL perimeter; neither rides
through Steps 5/17, whose own Consumes-lines take only T-8/T-3/T-6; the open
residue of those layers is priced on the displayed K3/K3-a lines at (H1)/(H6)(vii);
2 ≺ 18, acyclicity unaffected), 5, 6, 10–12, 14, 15, 16, 17 — all EARLIER. NOT
consumed: D-11 M2 (Step 18b) — the cycle is gone by stratification, not by fiat.
Delivers: the series = R_τ half of clause (R)'s evaluation.

**Step 18b (D-11 second tranche — MASS-ID M2: the density IS the series).** [NEW STEP
at REVISION 1, findings 2/11.] Statement:

    (D-11 M2)  for every type τ: α(n, τ; p) — the Haar density of clause (0) — equals
               the σ-graded tree series of T_can; equivalently, the solved values are
               the real conditional densities.

Proof [REWRITTEN at REVISION 10, r11, edit E3]: the D-11 **r4** M2 argument
(`D11_rev4_2026-08-04.md` §3, REVISION 3 — the twice-passed repaired leaf) **at its
(RE) scope**: Theorem M2 (r4) is a theorem about members satisfying the re-entrant
booking-profile hypothesis (RE) — every block booked (O1)/(O2′)/(O3), never (O2);
no allocation datum; family-(iv) rows the canonical 0/1-coefficient assembly with
only the displayed (O2′)/(O3) adjustments — with arbitrary-α members EXCLUDED
IN-STATEMENT (the counterexample class of finding C6 of the D-11 owed pass — pass 3,
`D11_owedpass_2026-08-03.md`, the §3.2 D-11 row [pointer disambiguated at REVISION
12, the global enumeration audit]; for them only O-12's pole/zero
theorem speaks). At the CONSUMED member, (RE)+row-form is PRICED as **(PACK-iv)**
((H5)'s FOURTH row-law, NEW at REVISION 10; the former definitional-discharge
display — that O-12's §1 booking sentence pins the consumed member's profile — is
WITHDRAWN, design §3.3). The repaired proof step consumes M1(a) at families
(ii)/(iii) ONLY (the r4 rescope; never family (iv)), and M2's exhaustiveness note
consumes M1(b)/(d) + (b-PF)/(b-EX)/(LOOP-∞) at their proved r4 scopes. Seams as
before: **(S1)** = the GD-3-FULL/O-9 residual-descent hand-off ← Step 14 (+ its
(GR-B)/(FRESH)/(ADM) conditionality, §3.1 — the census is consumed at its full
displayed hypothesis set [(ADM) added at REVISION 4, finding VC4-5]); **(S2)** =
the O-11 composition datum ← Step 18. (r1) is
the uniqueness pivot (Step 17). With Step 18 this closes the chain
α = (tree series) = R_τ — as an identification of the CONCRETE pack's densities with
the solve values, conditional also on **(PACK)** (§3.1, (ROOT-C)(H5)) through the
Step 17/18 consumption [REVISION 4, finding VC4-3]: the concrete pack's conformance
to the member is (H5)'s correspondence clause, and the member's booking/row-form
profile is (PACK-iv) — consumed HERE directly (Theorem M2's (RE) instantiation)
and, per the leaf's §2A.4 closed edge walk, at exactly ONE other place: Step 17's
(ROW-Σ) delivery display. NOTHING of M2 flows into Steps 17/18 (acyclicity intact:
(ROW-Σ) is M2-free at Step 17's layer).
**Scope: MONIC — and the third seam (S3) is NOT consumed here [REVISION 2, finding
V2-1].** M2's stated conclusion is about the monic ensemble — exactly clause (0)'s μ
(monic degree-n ≅ ℤ_p^n), which is all this walk needs. D-11 r2's third seam **(S3)**
(the mass reading of the projective assembly weight (q−1)/(q^{n+1}−1), owner annex
A-1) conditions ONLY the PROJECTIVE reading of M2's conclusion, per the r2's own
scoping ("M2 as stated (monic ensemble) does not consume it"; its §3 seam display and
Theorem M2's bracket) — and no §2 step consumes that reading: the projective
normalization is ANNEX by standing directive (§1 "Relation to the tree's root").
Step 5's pole-free claim covers the weight as a RATIONAL FUNCTION ((g2) of the O-12
grammar); its mass reading enters nowhere on the mandatory path. The proof above
therefore names (S1), (S2), (r1) and nothing else — correctly, not by omission.
Status [REVISION 10 status sync, r11]: **PROVED-UNVERIFIED at the r4 two-pass
record** — same leaf arc as Step 17's E2 display: pass 3 CRITICAL (M2 false at its
displayed universal scope, the C6 finding — the suspension trigger) → r4 restated
AT (RE) with the proof step re-derived from the verified §2.3(iv) grammar display →
r4 pass 1 validated "M2's (RE)/(PACK-iv) scoping" → r4 pass 2 GAPS-ONLY, no-churn
confirmed on M2; the leaf's confirming clean pass rides the §3.2 D-11 row. Lemma
M2.0 (multi-block a.s. termination) is PROVED at its displayed (S1-m)+(DET)
billing via the loop-extended addresses + (O-1)–(O-5) + (O-GLOB), adjudicated as
proofs at the same passes.
Consumes: Steps 14, 17, 18 — all earlier; the (GR-B)/(FRESH)/(ADM) rows through
(S1) [(ADM) restored to this summary at REVISION 11, r12 — advisory VC11-5: the
proof paragraph above already consumes the census at its FULL displayed set];
(PACK-iv) ((H5)) directly.
Delivers: the α = series identification; with Step 18, the α(n,τ;p) = R_τ(p)
evaluation clause of (R).

### Movement V — the closer

**Step 19 (T-U + K-13 + O-12 — the squeeze fires at every prime).** THEOREM U
(VERIFIED-DUAL, conditional on its displayed §U.SQ.6 kernel ledger — whose
coincidence with the Steps 1–18b roster plus the recorded evictions/annex items is
since REVISION 6 DISPLAYED ROW-BY-ROW at §3.4 [the A8 → VC5-3 → VC6 A-9 standing
audit debt, EXECUTED: per-item pointers §U.SQ.6 → step/eviction/annex, with the
Lean-bridge kernel rows cross-walked; VC7 CONFIRMED the table (findings 5/6) and
the §3.3 audit row is RETIRED at REVISION 7, kept as tombstone]) takes: the bracket
at every finite level (Step 1
+ D-15 + T-6 — of these, clause (SQ)'s SEMANTIC bracket is Step 1's ALONE,
unconditional; the D-15/T-6 legs serve the classifier-side counting tie inside the
evaluation chain, at clause (R)'s conditionality — T-6's TB-CAP leg
perimeter-conditional on the §4 D-12r pin [re-pointed at REVISION 14, r15]
[re-attributed at REVISION 4,
finding VC4-4 — this supersedes the REVISION-3 A6(ii) phrasing "the (SQ) bracket
consumes T-6"]), drainage
(Step 13 — the SEMANTIC layer, unconditional; the classifier-transfer layer likewise
rides the evaluation squeeze, not (SQ) — finding VC4-4), the evaluation chain —
**attribution exact [REVISION 1, finding 11]: Step
18 delivers series = R_τ (O-11); Step 18b delivers α = series (D-11 M2); TOGETHER they
deliver α = R_τ — neither alone does; the chain carries the RETIRED (H4a) =
(R1)–(R4) target through
Step 18's K3-c (the base-changed β-legs) [REVISION 2, finding V2-2; RETIRED at
REVISION 22, r23 — K3-c now reads the PROVED package at the P(O-8b/M14)
ACCEPT record, the §3.2 (H4a) retirement row], the (K3-δ)
fragment of (H4b) at the same interface [REVISION 4, finding VC4-1], (H5) =
(PACK) through Steps 17/18/18b's pack consumption [REVISION 4, finding VC4-3;
since REVISION 7, finding VC7-2, incl. the row-law legs — PACK-i/ii at K2's
evaluation glue, PACK-iii at K3's evaluation ring-hom (with D-6); sub-attribution
synced at REVISION 8, finding VC8-2; since REVISION 10, r11, incl. PACK-iv at its
closed edge set — Step 18b direct ((RE)) + Step 17's (ROW-Σ) delivery display],
(H6) = the terminal-seam engine-conformance group through Step 18's D-a/K4-CYL
consumption of CU-2t's terminal verdicts (Step 11's route), through the CU-leaf-label
leg into K1 (the A-5 route leg, REVISION 6), and K3-a's in-fence
condition [REVISION 5, findings VC5-1/VC5-2], (H7) = the (I-τ)/K-LOC pair
through Step 18's K1 receipt of O-10's product law at the engine's strata
[REVISION 6, finding F-1], the RETIRED (K-CUT) package + (H8′) through Step 18's K1
regrouping (M04 Lemma 3.3's exactly-one weight-preserving cut hypothesis —
the core machine-proved Lean-core; its three residual interfaces GRAFT /
MASS-ID / POOL-DICT priced as the §3.1 (H8′) row since REVISION 17, r18,
executing VC19; formerly (H8),
RETIRED at REVISION 16, r17 — the §3.2 K-CUT retirement row) [REVISION 10, r11], and
the RETIRED (H9) = (NSF) target through Step 18's K1/K4 label routing and Step
17's M3 leg (via
Steps 11/12) [REVISION 10, r11; this closer synced at REVISION 11, r12 — finding
VC11-1; (H9) RETIRED at REVISION 19, r20 — both members retired, ledger H9-C3,
the §3.2 H9 retirement row]** —
and the
pole-free specialization at every prime power (Steps 4 + 5 + 17; every self-loop
denominator at e ≥ 2 by Step 5's (SL≥2) display [REVISION 2, finding V2-6]) — and
squeezes: decided_τ(k)/p^{nk} → R_τ(p) = α(n,τ;p)
at EVERY prime including wild. **Σ_τ R_τ = 1, concluded HERE [REVISION 3, finding
B3]:** Step 1 supplies Σ_τ α(n,τ;p) = 1 at every p plus the interpolation principle;
Steps 18 + 18b supply α(n,τ;p) = R_τ(p) at every p (conditional on (H1)–(H3)
+ (H5) + (K3-δ) + (H6) + (H7) + (H8′) [(H8)/(H9) entered at REVISION 10, r11;
this summary synced at REVISION 11, r12 — finding VC11-1; (H8) RETIRED at
REVISION 16, r17; (H8′) ADDED at REVISION 17, r18 — the cut content rides the
retired package + the (H8′) residual interfaces; (H9) RETIRED at REVISION 19,
r20 — the ns-routing content rides the retired target at the pinned scope;
(H4a) RETIRED at REVISION 22, r23 — the base-change content rides the PROVED
(R1)–(R4) package at the P(O-8b/M14) ACCEPT record, (K3-δ) still consumed],
already inside clause (R)'s displayed scope); hence Σ_τ R_τ agrees with 1 at
infinitely many prime powers, and Σ_τ R_τ = 1 in ℚ(t) — no new hypothesis; the former
attribution "from Step 1" over-credited the VERIFIED leaf beyond its declared
statement. Clause (UB) from Step 16
at its displayed conditional scope ((H1)–(H3) + (H4b) + (H5) + (H6) + (H7)
+ (H8′) — (H4a) RETIRED out of the set at REVISION 22, r23 (the transported
alphabet-law base rides the PROVED package at the ACCEPT record) —
completed at
REVISION 3, finding B1, at REVISION 4, finding VC4-3, at REVISION 5, findings
VC5-1/VC5-2, at REVISION 6, findings F-1/F-2/F-3, at REVISION 10, r11, for
(H8)/(H9) through the transported K1/K4 chain [synced at REVISION 11, r12 —
findings VC11-1/VC11-2], at REVISION 16, r17, with (H8) RETIRED out of the
set, at REVISION 17, r18, with (H8′) ADDED — the transported cut content
riding the retired package + the (H8′) residual interfaces — and at
REVISION 19, r20, with (H9) RETIRED out of the set: the transported
ns-routing content rides the retired target at the pinned scope).
This is exactly (0) + (SQ)
+ (R) + (UB-conditional): **(ROOT-C)**, quantifier-for-quantifier. ∎ (conditional
exactly on the (ROOT-C) hypotheses (H1) (the enlarged group — four members since
REVISION 7), (H2), (H3),
(H4b), (H5) (incl. PACK-i/ii/iii since REVISION 7 AND PACK-iv since
REVISION 10, r11), (H6), (H7), (H8′) (the K-CUT residual interfaces GRAFT /
MASS-ID / POOL-DICT, ADDED at REVISION 17, r18, executing VC19) — (H4a)
RETIRED at REVISION 22, r23 (the (R1)–(R4) package written and passed at the
P(O-8b/M14) ACCEPT; the §3.2 (H4a) retirement row; its (K3-δ) audit note
survives on the (UB-X) row, still consumed by clause (R)); (H8)
RETIRED at REVISION 16, r17 (its core machine-proved, the §3.2 K-CUT
retirement row; its residues = (H8′)); (H9) RETIRED at REVISION 19, r20
(both members retired, ledger H9-C3; the §3.2 H9 retirement row) —
attribution
per clause displayed at §1, with clauses (0) and (SQ) UNCONDITIONAL [REVISION 4,
finding VC4-4]; the §3.2/§3.3 rows are audit debts on
the composition, displayed, not hypotheses)

**Composition audit trail (rewritten at REVISION 1).** Acyclicity, edge-checked: Step
1 consumes no later node (the former T-6 attribution re-pointed to its own Appendix A
— finding 1); Steps 2–5 are symbolic/regularity and consume only earlier steps (B-1..
B-7 grades displayed at Step 2 — finding 3); Steps 6–13 form the DELTA-2 recut order
(the O-2/O-3 cycle decomposed into O-2a upstream + the CU composition; OB1/OB2
evicted), with every formerly implicit input now displayed (C.1.5, D-5, (O6), Step-9
Consumes — finding 5); Steps 14–18b consume only earlier steps, with the former
D-11/O-11 cycle stratified as 17 ≺ 18 ≺ 18b (finding 2); Step 19 consumes 1–18b. The
V6 completeness review (26 findings) and the V5/V16/V21 architectural rejections were
each dispositioned in the tree's DELTAs; every countermodel on record is explained by
the repaired statements and none touches the root (tree §2.3 consistency note); VC1's
composition findings are dispositioned in §7.

---

## 3. THE CONDITIONALITY LEDGER (everything the root rests on beyond literature)

The complete honest list, per the wave-10 residue enumeration (ledger §"WAVE 10 +
PASS-2 ADJUDICATIONS": "THE WHOLE MANDATORY-PATH RESIDUE now = {(GR-B) orders ≥ 2
(FGMN-pinned, transcription), (FRESH), the O-9 repair, O4T pass 2, D-11/O-11
verification cycles, ROOT ASSEMBLY}" — quoted as of wave 10; the rows below are SYNCED
to the 2026-08-02 harvest at REVISION 1), EXPANDED to every named pin/flag a reader of
§2 encounters. Character classes: **[V]** verification-pass debt (a proof is on file; a
named fresh-context hostile pass is owed — no new mathematics expected unless the pass
finds some); **[T]** transcription debt (content pinned to printed sources or existing
proofs; writing/auditing owed, no new mechanism expected); **[M]** genuine open
mathematics (no proof on file). The CUC §0 pending-verdict registry
(`lean/notes/openmath/CU_cluster_phaseB.md` §0) is the live key for the P(·) items.

### 3.1 Genuine open mathematics [M] — the honest mathematical distance

| item | statement | owner | notes |
|---|---|---|---|
| **(H1) — the graded-dictionary open family at OM orders ≥ 2** [ENLARGED at REVISION 6, findings F-2/F-3, from the former single-member "(GR-B)" row; member (d) ADDED at REVISION 7, finding VC7-1, and REQUANTIFIED to READ ORDER at REVISION 8, finding VC8-1 — the group has FOUR displayed members] | **(a) (GR-B)**: the anchored-march residue-structure package (GD-2/GD-3's one open input) — the frame tie + residual-dictionary march at orders ≥ 2, modeled on printed FGMN Thm 4.2 (both pairings), EXCLUDING the properness-proviso leg (the e·g = 1 side condition), whose SOLE home is the pinned §3.3 (KP)/(KPirr) [T] row at FGMN Lemma 5.3 [one-home cleanup at REVISION 7, finding VC7-4: the former "with the properness proviso" wording gave that leg two classifications]. **(b) GD-1 beyond order 1** (= O-2a's exactly-stated open lemma OL-2a-1): the (KP)/(KPirr) representative-admissibility clauses beyond order 1 — the canonical key lifts form one GMN-admissible representative family at every order (the properness-proviso LEG stays [T] at the §3.3 (KP)/(KPirr) row, pinned to FGMN Lemma 5.3). **(c) GD-6 conformance instances at orders ≥ 2** (= OL-2a-6's consumed face): the run-decomposition-conformance instances Step 10's conditionality line consumes — held on proof-by-construction ROUTES on record, which are not proofs (the same evidentiary status VC5 ruled [M] for (T-DEC)/(T-VERD)). **(d) the order-≥ 2 KEY1 block** [ADDED at REVISION 7, finding VC7-1 — formerly only gestured at by the §3.3 S-1 row's "campaign perimeter" line, which named no member; REQUANTIFIED to READ ORDER at REVISION 8, finding VC8-1 — the REVISION-7 form quantified over realized sites "whose current key has degree > 1", parenthetically identified with the order-≥ 2 reads: an UNPROVEN identification (e·g = 1 refinement reads keep degree-1 keys at higher order, and the proved order-≤ 1 slice does not cover a higher-order site merely because its key stays degree 1), and its title conflated the KEY's degree with the residual factor ψ's degree g]: KEY1's conclusion block asserted at EVERY realized site of Step 6's machine at OM ORDER ≥ 2 — membership by read order alone, IRRESPECTIVE of the current key's degree, explicitly INCLUDING order-≥ 2 sites whose key remains degree 1 (e.g. e·g = 1 refinement chains) — matching Step 6's all-site use exactly: for the STANDARD key lift Φ_{i+1} assigned at such a site with read side (e, h) and residual factor ψ of degree g ≥ 1 (g is ψ's degree, unconstrained — the key's degree is irrelevant to membership; ord_ψ(R_i) = μ): (d1) in(Φ_{i+1}) = u·ψ̂_hom in gr(w_{i+1}) with u an invertible homogeneous element and ψ̂_hom irreducible; (d2) the degree block — ψ̂_hom ∤ in(B) for every nonzero B with deg B < deg Φ_{i+1}; (d3) ord_{ψ̂_hom}(in f) = ord_ψ(R_i(f)) = μ. This is exactly O-1thr's (V1)/(V2) key-compliance block beyond its proved order-≤ 1 slice — the block Lemma WGEO consumes at those sites. **Coverage arithmetic, displayed [VC8-1]:** the PROVED slice is exactly the order-≤ 1 sites (the GD23 dictionary + O-1thr's (V1)/(V2), proved at order ≤ 1), so (d) covers precisely the COMPLEMENT of the proved slice in Step 6's site range — order ≤ 1 proved, order ≥ 2 assumed here — and total coverage of the all-site theorems consumes NO key-degree/order equivalence; the §3.3 S-1 row's "same method" note prices the intended proof ROUTE (KEY1's weighted-Gauss/homogenization argument), not a proof | (a) GD-1/GD23 arc (verify passes: the GD23 verification cycle continues at orders ≥ 2); (b) GD-1 owner (O-2a Step 7); (c) GD-6 owner (with GD-4, Step 7); (d) O-1thr/GD-1 arc (Step 6; GD23's Theorem KEY-g is the proved j = 0 instance at any d₀) | order ≤ 1 PROVED over an arbitrary complete DVR for all four faces (leaf #4; O-2a's core; CU-1's order-≤1 scope; (d)'s proved slice = KEY1 itself at Step 6 + GD23's KEY-g at the order-0 key for any d₀); the wave-10 ledger grades the ≥ 2 layer "FGMN-pinned, transcription" — every (a)-target is pinned to a printed theorem (LITUNIT §7), but the composed march at ≥ 2 is unwritten, and (b)/(c)/(d) carry NO pin and no proof on file: honestly [M] until written and passed [(b)/(c) were the F-2/F-3 blockers, (d) the VC7-1 blocker — same proved-≤1/open-≥2 shape, previously misfiled [T] or gestured at]. Enters: (a) Steps 10 (CU-1's step), 14 (CEN-W), 16, 18 (K3), 18b ((S1)); (b) Steps 10–12 (O-2a's admissible-representative-family clause: Θ's typing, Theorem 2(B)/(D) feeding CU-2/CU-2t; Step 12's representative-lookup flag duty); (c) Step 10 (the displayed conditionality line); (d) Step 6 (Theorems 1–3's all-site scope at order-≥ 2 reads — the WGEO-consumed block), propagating with Step 6's deliveries (D-14/D-15 → CU-1/CU-3/CU-4, Steps 10–12, Step 17's per-box σ-constancy) — all inside clauses (R)/(UB). **[REVISION 20, r21 — the H1-campaign leaf-status sync; the row STAYS [M], NO member discharged (blueprint §0: the row retires only when (a)–(d) all land):** (i) **member (a)'s execution OF RECORD** = the (GR-B) leaf `GRB_ORDER2_2026-08-05.md`, **RE-ACCEPTED at its REVISION 14** (commit 58534d4; 21-pass arc, passes 20 Codex + 21 Fable both CLEAN, model-diverse; the FGMN retarget TR-1..TR-7 executed with the corrected dictionary λ^GMN = −E_i·λ^FGMN of record) — the arc CONTAINS the **Def-3.15 extraction-artifact incident**: the N-1 falsifier harness (15/15 families GREEN, the bar MET) caught the leaf's ǫ-twist display as a corrupted shared pdftotext extraction (true twist ǫ_i(α) = z_i^{ℓ′_i s − ℓ_i u}), corrected at leaf REVISIONS 11–14 under passes 15–19 (the old S3.2 geometric discrepancy law RETIRED as an artifact at the leaf; ROOT never quoted it — the r21 grep gamma_2/discrepancy/ǫ audit found ZERO ROOT displays of the old law); the leaf's residue taxa displayed there: OPEN LEMMAS TR-3′-GEN (two strata) / TR-3-ORD-X (re-based) / D-ITER (E_i > 1) / TR-4's empirical debt; PROVED-BUT-SCOPED TR-3-ORD + D-REAL; EXTERNAL (KP_i)/(H-CHAIN)/[T]; member (a) is now a pinned-and-WRITTEN package riding the leaf's verification record, NOT discharged. (ii) **member (b)'s engine-lift conformance face** = B-M2 (Theorem LIFT-CONF, §2.2(s7); `H1_BM2_2026-08-06.md`) **ACCEPTED at its REVISION 14** (commit 416c213; 14-pass arc, passes 13 Codex + 14 Fable both CLEAN, model-diverse): the compiled perimeter (`Scaffold/HDischarge/H1/{Conformance,RecParamE,RCWitness}.lean`, Lean-core) proves actual-pair (K0)-conformance on BOTH carrier classes — [NR] direct, [RC] via the `k0_actual_of_recentering` hspecRec-transport AND the REC-WIRE-G width-law derivation `recParam_eg_of_recentering` (the fiat unusable by type) — with the compiled H₃rc interior-recentering witness; B-M2's displayed opens STAND: GRADED-READ, REC-SL proper, SITE-EXH, the `readsOf_landing_K0` recentering-branch witness debt — so KP-STEP's application to the fired lifts stays CONDITIONAL (door-1 KP-STEP itself = B-M1, verified 2-clean earlier). (iii) **H1-U14 STATED behind its gate** (commit 6df3eae): `FaithfulTotal` — the TOTAL (FGMN Cor 5.15) faithfulness [M]-row — stated in `H1/SiteBlock.lean` §U14 after its preregistered N-1 family T8 decided POSITIVE (0/2,400 violations; the gate licenses STATING, never asserting; Cor 5.15 checked ǫ-free against the corrected Def-3.15 reading before stating), with two PROVED Lean-core consumers `faithful_at_of_total` / `faithful_total_consume`; the row is displayed + consumed, NEVER proved — its supplier is the (GR-B) leaf's future Lean face]** **[REVISION 21, r22 — the TR3-S1 stratum-served annotation (ledger 2026-08-08: TR3-S1 ACCEPTED, passes 2+3 CLEAN model-diverse; `lean/notes/openmath/TR3S1_2026-08-08.md` REVISION 2; commit 549e8a6 composed, the F1-chartered synthesis-4 unit): on the ORDER-2/e₀ = 1 STRATUM (unramified below), with σ₀/σ₁ constructed explicitly, σ₁(R^F₂(f)) = R_λ(f) EXACTLY — so member (a)'s residue rows TR-3′-GEN and TR-3-ORD-X now carry a SERVED STRATUM: TR-3′-GEN(i) restricted there is a THEOREM ((GR-B-STR)-ladder inputs only) and TR-3-ORD-X there is PROVED mod the inherited (KP_i)+[T]-properness, serving three consumer families on the stratum (GD3-FULL/OL-B's R_λ leg; GD3-MIN Route B + the clause-(4) ord-consumers; B-M2's OL-1(b) TR-3-ORD-X seam — its GRADED-READ/REC-SL seams untouched). NOT served, displayed: orders ≥ 3 (even all-e_j = 1), e₀ ≥ 2, the general σ_j family — neither GRB lemma closes stratum-wide; the row STAYS [M], nothing discharged; falsifiers: fresh N-1 re-run ALL GREEN (33,547 samples) + the NEW preregistered family `verification/openmath/tr3s1_check.py` (TS1a–d, 6,260 samples, 0 violations)]** **[REVISION 22, r23 — the 2026-08-08 campaign batch folded; the row STAYS [M], no member discharged. (i) THE SITE-EXH ARC (member (b)'s B-M2 open, re-priced per B-M2 REVISION 15 — the post-acceptance annotation fold 38d98f6, acceptance NOT reset, clean counter stays 2/2): the adjudication probe returned NEEDS-EXTENSION with the closed `FiredLift` enumeration compiled (355c1eb; elimination definitional on the 3 run-borne carriers, residue = exactly the TowerMoveClosure row; f-tied form refuted-at-nature by C-e) → Asvin SIGN-OFF on the TruncatedRun extension E-1..E-4 (49b778e) → **E-1/E-2/E-4 LANDED** (f36ab72, `H1/TruncatedRun.lean`, 12/12 decls Lean-core: the widened row DISCHARGED at BOTH layers — `siteExh_elimination'` UNCONDITIONAL over the as-built core-free carrier; `TowerMoveClosureL` = the signed core-binding layer DISCHARGED at cored parents, its LAYERING **RE-RATIFICATION PENDING ASVIN** (the standing item: the signed E-1 signature and the junk-admitting FiredLift are jointly unsatisfiable by one carrier)) → **E-3 GLUE PROVED** (db47e35, `H1/GlueRun.lean`, 9/9 Lean-core: `truncRunFrom_glue` purely structural — no composite carry constant enters; the f-tie at honest strength — `FTiedTruncLanding`/`ftie_extends` UNCONDITIONAL with `gate_ftie_chain` firing END-TO-END, the first f-tied deep landing on file; `readsOf_glue` packet-conditional, e ≥ 2-ended runs do not merge — the RG-2 fence honored, no lift). SITE-EXH is thereby **DISCHARGED AT THE ENGINE FACE (two-layer as-built form) + f-tied coverage**; named residuals displayed: `junc_bundle` at choice-built stages (the D-1 wall), `DIterJunctionSupplier` (the NAMED OPEN carrying the measured ITER-LAW — see (iii)), the disjoint box face (`FiredLiftBox`/`BoxKeyBridge`, the M1-track display), the S-1 [T] rider. (ii) **GRADED-READ (member (b)'s B-M2 (i-b) seam) DISCHARGED at interior coherent [NR] reads, M-PARAMETRIC** (a5911fd, B2D GR wave 2a: `graded_read_of_history` — all three (i-b) clauses from `HistoryCoherent` + parent model only, no wiring hypotheses; the supplier verdict corrected the E-gate — `hψ0` supplied at EVERY node, `hscale` proved at interior reads via the `child_e`/`child_h` stage-pair ties); VACUOUS at steep recorded reads by the compiled scoping `readHyps_e_eq_one` (`ReadHyps` forces ν.e = 1 — the displayed pack IS the e = 1/[NR] recording perimeter, kp_step's face); residue: the abstract σ.e ≥ 2 frame legs OPEN-OBSTRUCTED (GR-3 ×2 + GR-4 ×1 frozen sorries; the ψ = φ(z^σ.e) gap countermodel is mathematical, not compilable in-corpus — ambient char 2; interface re-adjudication = a campaign-owner statement-change decision). REC-SL proper UNCHANGED OPEN; the `readsOf_landing_K0` recentering-branch debt STANDS NARROWED (f-tied at the `ReadsOfFrom` face via the ν₁rcRoot root-respeciation; no recentering-SPECIES `ReadsOf` run on file — remaining route: a direct witness, or glue past `junc_bundle` + a From→ReadsOf converse). (iii) THE STRATA PROBE (8db1b00, Tier-3 falsifiers, exit 0 — member (a)'s residue rows re-described): **TR-3′-GEN(ii) now has its exact closed-form shape ON RECORD** — the corrected-eps twist law at e₀ ≥ 2 is the pure slot-keyed z₁-character C^F_j = z₁^(ℓ₀g₀h₀·s_j)·c^G_{s_j} (0/3,640 vs an independent GMN Defs 2.19–2.21 implementation; naive identity dead 1,023/1,023) — an instance-confirmed MEASUREMENT, the lemma stays OPEN; **D-ITER's single-ζ′ CYCLIC presentation is FALSE at the canonical alphabet** (NOT-CYCLIC at 22/28 mixed towers incl. the designed deg-2-z̄ kill-shot row N) — the true structure is the measured two-constant ITER-LAW c = z̄^{d₂}·z₁^{(s₁-defect + ℓ₀γ₂d₂)/e₀} (0/804), D-ITER is to be RESTATED to the iterated twisted form and NO Lean kernel may bake in F′[u]/(u^{e′_b} − ζ′) at composite stages (`DIterJunctionSupplier`'s docstring carries exactly this fence)]** **[REVISION 23, r24 — two member-residue updates. (1) The (ii) GRADED-READ residue RE-PRICED: **GR ROUTE (a) EXECUTED** (5b7f1ee, per `GR34_DISPOSITION_2026-08-08.md` @ 205e3f8) — the abstract σ.e ≥ 2 frame residue is RETIRED as a fence-(vii) STATEMENT CHANGE: `ReadHyps` gained the frame-pin fields `hσs : ν.σ.s = 1` / `hσt : ν.σ.t = 0` and the 3 frozen sorries (GR-3 ×2 + GR-4 ×1) discharged as one-liners from the `_level` forms, all Lean-core; adjudicated under Asvin's standing statement-change authority (2026-08-05) on the decisive facts: ZERO Lean consumers (docstring mentions only; no module imports the two home files; no OM/capstone module imports ANY B2D module) + the statements believed FALSE at the bound scope (the ψ = φ(z^σ.e) gap countermodel, wave-2a record). **B2D census 3 → 0 sorries** (grep-verified); the consumed face `graded_read_of_history` signature-UNCHANGED; the F5 stage-forge GR leg DECLINED (its only GR payoff was refuting statements this change deletes from the interface — the forge re-prices against the junc_bundle/readsOf need only). (2) Member (a)'s TR-3′-GEN(ii) residue: **TR3-S2 attempt ON RECORD** (330cc6b, `TR3S2_2026-08-08.md`) — TR-3′-GEN(ii) at order 2 proved-informal in the §3.4-REV-11 two-stratum form (Lemma A-GEN + the Bézout EXP kernel; v2 falsifiers 11,748 samples 0 violations), the units-only record phrasing REFUTED on (ii), the ORD-INV state-key bifurcation displayed (FGMN-normalized vs GMN-lawful keys diverge on the twist-visible set); hostile pass 1 (1bd75de) returned **CRITICAL 1C/2G** (core compose chain S2–S5 verifies; the critical is the universal key-divergence scope claim) — REVISION 1 dispatched, acceptance bar NOT met: the lemma's row status is UNCHANGED (attempt-on-record only)]** **[REVISION 24, r25 — three member-residue updates; the row STAYS [M], no member discharged. (1) Member (a)'s TR-3′-GEN residue: **TR3-S2 ACCEPTED** (6663aa4; the arc: attempt 330cc6b → pass 1 CRITICAL 1C/2G → REVISION 1 5844572, 9 divergence sites rescoped to the twist-visible criterion, Lemma KEY0 PROVED, Lemma WIT PROVED, falsifier run 3 ALL GREEN 11,768 incl. TS2f 0/20 → passes 2 (Codex, 96d3d9b) + 3 (fresh Fable, independence-fenced, 400f000) consecutive CLEAN 0C/0G, model-diverse; pass 2 independently checked the load-bearing GMN t_1/(18) transcription against the original arXiv — the non-textual leg for the one flagged pin; pass 3 re-ran the falsifier byte-identically + hand-walked a twist-visible F4 instance) — with accepted TR3-S1, **TR-3′-GEN at ORDER 2 = a two-stratum THEOREM in the S3.4 REV-11 form** (GRB open lemma #1 CLOSED at order 2; the GRB note's post-acceptance bracket applied at this unit): the units-only phrasing of the record statement is PERMANENTLY REFUTED on (ii) (unconditional via Lemma WIT) — the record form of TR-3′-GEN is the S3.4 REV-11 form; the ORD-INV state-key bifurcation finding (divergence exactly on the twist-visible set) enters the record as the convention-independence law for deeper-order work; **deeper orders ≥ 3 remain OPEN** (no scope creep — member (a) is NOT discharged; D-ITER/TR-4 residues stand); **TR-3-ORD-X-on-(ii) stands PROVED CONDITIONAL on (KP_i) + [T]-properness** (displayed, not promoted). (2) Member (b)'s REC-SL open (B-M2): **NARROWED + CLOSED AT EVERY COMPILED SITE** — the F2 suppliers (`B2D/RECSL_suppliers.lean`, 1adac62: 2.75/3 legs — (SL-top) unconditional, (SL-dev) data from `ReadHyps`, the residual digit law compiled SHARP to the one sign pin σ.R(−1) = −1 = `StageCore.R_neg`, corpus-underivable from round-2 fields; the assembled IsReadLift-at-(1,1) proved with the pin displayed) + the KP-STEP [RC] wiring (`H1/RecSLWire.lean`, 85e11a4, 6 thms Lean-core: `hlink_nodeLift_of_history` supplies `key1_along_history`'s hlink 4th conjunct `IsNodeLift` at EVERY leg — [NR] from coherence, [RC] via `recsl_isNodeLift_of_history_stageCore`; `kp_step_of_recentering`/`key1SiteBlock_step_of_recentering` fire the consumers at [RC] legs; `key1_along_history_recsl` re-keys the fold, hlink losing IsNodeLift entirely; gates positive on H₃rc: `rc_recSL_hPhi_gate` through the general route matching GlueRun's hand-rolled instance + `rc_hlink_gate` with both rows discharged concretely). THE TWO SURVIVING ROWS, displayed exactly (per RECSL_WIRE §3): **(i) the provenance row `hsc`** — `StageCore` at each successor-having recentering frame (the RECSL-SIGN pin's carrier; σ.R(−1) = −1 via `signLaw_of_stageCore`); discharged on every engine-built tower (T1/S9/S10 all transport `StageCoreL`) and at every compiled site (`score_σ₁rec.core`); open ONLY for abstract recorded histories with no engine provenance; **(ii) the recentering-chain fence `hfence`** — [RC] legs whose predecessor is ALSO recentering are not covered (GR0b-inherited standing fence, not sign-related). The hlink parity ties and (K1)+(K2)/GRADED-READ are untouched, as priced; B-M2 note re-priced at its REVISION 17 (this unit). (3) Member (b)'s `readsOf_landing_K0` recentering-branch debt: **RESTATED AT THE `ReadsOfFrom` FACE — the species form has NO CONSUMER** (F3 walk verdict (a)/NO-CONSUMER, `READSOF_F3_WALK_2026-08-08.md` 878ac17; B-M2 REVISION 16 APPLIED e7a673a, additive, acceptance counter NOT reset, stays 2/2): the walk's compiled facts — zero term-level consumers of the branch; the only compiled consumer slot for (K0)-at-a-recentering content is `kp_step`'s `hΦ : IsNodeLift`, which the species branch's `K0Conformant` conclusion could NEVER feed and the `ReadsOfFrom`-face witness (`landingKey_ν₁rcRoot`) feeds LITERALLY — so the debt of record is the f-tied `ReadsOfFrom`-face coverage (WITNESSED, `gate_ftie_chain`) and the ReadsOf-SPECIES form leaves the opens list as an obligation, re-entering only as a FENCE DISPLAY of the dead From→ReadsOf converse (the C-e exhibit); the F5 run-forge is NOT chartered by this consumer]** **[REVISION 25, r26 — two member-(a) residue updates; the row STAYS [M], no member discharged. (1) **The TR-4 EMPIRICAL debt DISCHARGED** (4761cdd; `verification/openmath/tr4_check.py` + `TR4_HARNESS_2026-08-08.md`): the GRB leaf's displayed evidence gap ("Cor-4.5/4.11 factorization/bijectivity content ↔ NO listed family, paper-secured only" — GRB REV-12 GAP 1 / REV-13 GAP 4) is closed by the preregistered harness family **TR4a–f** (seed 20260817 and all violation-count predictions committed BEFORE running; full N-1 roster A–J × 4 base rings, reading C): coset vanishing both halves (Cor 4.4(1)), factorization-through-H_µ both directions (Cor 4.5), bijectivity with an arbitrary-target anchor realizer + non-circular w₂-only injectivity legs (Cor 4.11) — **19,226 samples, 0 violations, ALL GREEN** (TR4f's 26 exhaustive windows tie the combinatorial sample formula exactly). **TR-4's mathematical status UNCHANGED — PAPER-SECURED** on printed Cor 4.4(1)/4.5/4.11 (a GREEN run is instance evidence, not proof; no proof-status change; ORD-INV not engaged — TR-4 is convention-internal, displayed); GRB note brackets applied additively at R11.5 + R13.4 + the residue-of-record display (acceptance counters NOT reset). (2) **D-ITER RESTATED — the ITER-LAW is the STATEMENT OF RECORD** (3bcae68; `DITER_RESTATE_2026-08-08.md` §S2, countermodel-pinned under the standing authority; pin = the strata probe's B3-adj adjudication): the single-ζ′ cyclic presentation ("𝒟′ = F′[u′]/(u′^{e_b′} − ζ′)") is REFUTED at the canonical dig-consumed alphabet (NOT-CYCLIC at 22/28 mixed towers; two witnessed failure genres) and BRACKETED at all display sites (MOVES §F.2 + GRB S5.3/S8, additive; it survives ONLY as the e_b = 1 D-REAL stratum + the information-free abstract iso); the operative form is the measured two-constant iterated cocycle **(ITER-LAW)** c(γ,γ′) = z̄^{δ₂}·z₁^{(s₁(γ)+s₁(γ′)−s₁(γ+γ′)+ℓ₀γ₂δ₂)/e₀}, δ₂ = ⌊(s₂(γ)+s₂(γ′))/e₁⌋ — an **OPEN LEMMA, instance-true at 0/804** anchor pairs over 52 towers, e₀ = 1 leg theorem-backed (D-REAL), CARRY-1/W-MULT fibred-index proof route displayed; orders ≥ 3 = conjectured template only, no claim. The `DIterJunctionSupplier` CONSUMABLE SPEC is on record (§S3: three mandatory features — the iterated 𝒟₂ carrier, the (ITER-LAW) cocycle with BOTH constants + the fibration term, the three sanity gates incl. the row-N NOT-CYCLIC falsifier target; cyclic kernels fenced; until ITER-LAW is proved, any discharge consumes it only as a displayed hypothesis); queue #11 (the `DIterJunctionSupplier` formalization) UNBLOCKED on its #9 prerequisite, priced with the deep-instance forge]** **[REVISION 26, r27 — the pass-6 adoption at this row; the row STAYS [M], nothing discharged (`docs/SYNTHESIS_PASS6_2026-08-08.md` F1/F3b/F3c/F4, 976484d). (1) **`junc_bundle` CHARTERED** — the F1 forge decision, the ONLY target chartered (price L, staged numerics-first): stage 1 the deep compiled run (deg f > deg Φ₁ with REAL digit data — the corpus's first compiled order-2 digit computation; target tower preregistered from strata-probe/BM2_TRACE numerics; the clause-(iii) out-of-carrier residual — deviation D-1's wall, unprovable and irrefutable from the stage interface at the choice-built σ₁rec — traced numerically BEFORE the Lean build per the concrete-trace rule); stage 2 `gate_glue_junction` fired with the bundle discharged, un-hypothesized; stage 3 the `DIterJunctionSupplier` sanity gates (ITER-LAW consumed only as the displayed hypothesis; cyclic kernels fenced per the §S3 spec). Consumer chain (why this target): the last SITE-EXH displayed residual; the queue-#11 instantiation substrate; B-M2's LAST non-hypothesis-shaped wall — the KP-STEP [RC] collapse leaves B-M2's opens = {hsc, hfence, junc_bundle, the K1/K2 parity ties} (pass-6 F3d). Dispatched at wave 10 (stage 1); results ride r28. (2) **The `hsc` surviving row is PERMANENTLY hypothesis-shaped — the terminal honest state** (pass-6 F3b): `HistoryCoherent`'s records bundle NO `StageCore` (RECSL_WIRE §1), and the round-2 fields determine only σ.R(−1)² = 1 (`ResVal.R_neg_one_sq` — both signs consistent), so NO transport can manufacture the sign at an abstract carrier; hypothesis at abstract carriers, closed at every compiled site — this display supersedes any reading of the r25 narrowing as awaiting a discharge mechanism; optional S hygiene only (PoolDictProbe genre: a compiled two-instance witness differing only at σ.R(−1), moving the row to blocked-with-blocker-compiled), folded into any H1-touching unit, NO standalone dispatch. (3) **Member (a)'s deeper strata (orders ≥ 3) are NOT yet finite-algebra** (pass-6 F3c; TR3S2 §S7): the inner reads are marched operators with their own ǫ-systems and the ǫ-recurrence can REVIVE even on e₀ = 1 strata — the recursion is unwritten; entry per the [M]-row protocol: a preregistered depth-3 probe MEASURING the composed ǫ-recurrence at ORDER 3, all-e_j = 1 (queued at pass-6 #6, dispatched at wave 10), a compose unit ONLY on an exact law; the ITER-LAW proof attempt (e₀ ≥ 2 order 2, the CARRY-1/W-MULT fibred route, falsifiers on file 0/804) is ATTEMPT-GRADE now the statement is of record, slotted AFTER the probe (pass-6 #10)]** **[REVISION 27, r28 — FORGE `junc_bundle` STAGE 1 EXECUTED ALL GREEN; the row STAYS [M], nothing discharged (023a9d7 preregistration SEALED pre-run + 53f5d03; `verification/openmath/juncforge_stage1.py`; note `JUNCFORGE_S1_2026-08-08.md`): 10/10 preregistered rows GREEN first-pass — 4,116 samples, 28 tower instances (7 rows × 4 rings, families D0–D4/DA, deg f > deg Φ₁ with REAL digit data — the chartered deep run; NUMERICS, not Lean). The clause-(iii) out-of-carrier residual (deviation D-1's wall) PINNED at nature: (a) the MEASURED TWO-SCALE LAW — the recorded (a, Ranch, μ) live EXACTLY at the read-pair scale (S(λ_side) = pattern support, 336/336) while every strictly coarser stage scale reads the top-vertex key-power MONOMIAL (zero pattern content, 336/336) — so at a read-pair-graded stage (the corpus's own compiled convention, HK25/U31) clause (iii) is interface-DERIVABLE from clauses (i)+(ii); displayed as a **MEASURED REDUCTION, NOT proved** (numerics are falsifiers, never proofs — the Lean discharge is stage 2's); (b) the terminal deg f = deg Φ residual = the RANK-2 BINOMIAL R(B₀) + T^s, its digit coordinate κ₀ = val(B₀)/val(φ) the 𝒟₂-ANCHOR coordinate (u₀'s class verbatim on designed anchors, 56/56 — the §S3 consumable confirmed at the supplier's own computation site); (c) ITER-LAW 0/840 on RUN-REALIZED classes — the falsifier record EXTENDED, the lemma stays the OPEN LEMMA of record. **THE D0 STRATUM FLAG (pending, NOT asserted):** at f = key the residual is KEY-ONLY and a recentering-shaped bundle (pat₀ ≠ 0, Ranch = z − c, μ = 1) is unsatisfiable at the unique development — `gate_glue_junction`'s hbundle (U31/RCW seam) instantiates EXACTLY this stratum, an INSTANCE-FALSE FLAG on the gate's hypothesis PENDING the orchestrator adjudication riding forge stage 2 (one Lean check owed: monic-key development uniqueness); NO gate display changes at r28 — the honest reading: the wall is not opacity but the degenerate stratum, and the dischargeable junction lives at deg σ₁.Φ < deg f (= `DIterJunctionSupplier`'s own antecedent). STAGE-2 SPEC FIXED (§S5): row A over ℤ₂ (Φ₂ = X² + 4X + 12, f = X² + 4X + 28), the junction stage BUILT AT THE READ PAIR (e′,h′) = (2,3) — the first compiled e ≥ 2 junction; dispatched at wave 11, results ride r29. The StageCore docstring debt (F6 rider) CLEARED: `Moves/DefsCore.lean` docstrings synced to the live HC1 chain (`T1_baseStage`/`S9_transStage`/`S10_recStage`), git-diff-verified COMMENT-ONLY]** **[REVISION 28, r29 — THE `junc_bundle` VERDICT OF RECORD (forge S2 arc closed): the row STAYS [M]; `junc_bundle` LEAVES the open-wall list and becomes a displayed VERDICT + CHARTER (036ee8c the six-theorem kernel `H1/JuncForge.lean`, Lean-core, NEW decls only; + the REVISION 1–4b faithfulness hardening; arc closure 09f93c1 — audit rounds 1–4 CRITICAL, round 5 CLEAN 0C/0G; note `JUNCFORGE_S2_2026-08-08.md`). THE VERDICT: the compiled CHAR-2, Φ₂-LANDING, INTERIOR-Φ₂A-PINNED `junc_bundle` is **INSTANCE-FALSE at row A** (kernel-proved; the §S5 stage-2 discharge is IMPOSSIBLE as transcribed) — the PARITY WALL `juncforge_no_e2_junction` ALONE is char-free/Φnext-free (no e ≥ 2 `SideReads` recording can exist AT THE ROW-A READ — fA = X²+4X+28 against the Φ1A = X+2-keyed frame, any finite F, any Φnext) and EXPLAINS the C-e escapee at that read; the D0 flag is DECIDED IN LEAN: `gate_glue_conditional` VACUOUS at its compiled instance is now a THEOREM (`juncforge_gate_hbundle_false`, the hbundle binder verbatim-negated at definitional equality, + `juncforge_gluePacket_false` the independent structure-field leg); the POSITIVE fragment stands (`juncforge_clauseIII_derivable` — clause (iii) alone derivable at the (2,3) pins; the S1 two-scale law survives); `DIterJunctionSupplier` NOT refuted (its antecedent lives at deg σ₁.Φ < deg f). HONESTY, displayed: this is a TRANSCRIPTION verdict, NOT established falsity of the FGMN source — the incompatible-transcription-scales reading (clause (ii) at the parent scale vs clause (iii) at the graded/read-pair scale) means a `SideReads` repair could RESTORE the paper-source glue step; the SOURCE-LEVEL question is OPEN. **THE CHARTER (ASVIN FLAG — the statement-change decision point):** the FGMN transcription-scale re-derivation design unit (JUNCFORGE_S2 §R3) — re-derive the glue step from the FGMN source at the read-pair scale S1 measured (fixed points: the S1 two-scale law + `juncforge_clauseIII_derivable`; non-textual-leg discipline — /tmp/litunit/fgmn.txt BAD at Def 3.15, fresh pdftotext -layout legs + numeric falsifiers); product = a corrected `SideReads`/`junc_bundle` transcription as PROPOSAL ONLY, NO application without ROOT adjudication — displayed in the §29/§30/§33 flag lineage. ALSO AT THIS ROW — member (a)'s deeper strata (the pass-6 F3c arc): the DEPTH-3 PROBE MEASURED (54c518d, sealed pre-run d8ef8d8; note `STRATA3_2026-08-08.md`) — **(IDENT-3) EXACT** (C^F3 = c^G3 identically, 0/1,656; 21,950 samples, 24 order-3 towers; NO ǫ-revival on the all-e_j = 1 stratum at probe scope; the level-2 Def-3.15 reading discriminated externally for the first time) — and the chartered compose unit **TR3-S3 COMPOSED, IN VERIFICATION** (482150b, preregistered b4a142b: TR-3′-GEN at order 3, all-e_j = 1, conditional on (H-v3); REVISION 3 at f05df77; passes: p2 Codex CLEAN pre-revision, p3 Fable GAPS-ONLY, p4 Codex GAPS-ONLY, p5 running; counter 0/2; the pass-3 verifier's independent 93-sample external leg on record) — **NO status upgrade**: TR-3′-GEN at order 3 stays attempt-grade until its bar is met; e_j ≥ 2 strata stay fenced (the sealed UNTESTED (TWIST-3) candidate rides the note)]** **[REVISION 29, r30 — TR3-S3 ACCEPTED (ledger 96b7f54): member (a)'s residue display gains the ENLARGED SCOPE UNION. The arc: `TR3S3_2026-08-08.md` REVISION 4 — pass 7 Codex CLEAN 0C/0G (dc40668) + pass 8 Fable CLEAN 0C/0G (83870a8), consecutive model-diverse, counter 2/2; eight passes with four orchestrator-applied precision revisions (p2 CLEAN pre-revision → p3 0C/2G → REV2 → p4 0C/1G → REV3 → p5 CLEAN → p6 0C/1G display-inert → REV4 → p7+p8 CLEAN); **THREE independent from-note verifier implementations** (p3: 93 checks; p6: off-roster p = 5, 1,564 checks; p8: p = 7/11 incl. both degeneracy corners + the s₂ > 0 battery, 821 checks — all 0 failures), the strongest external-leg record of any accepted note. THE THEOREM: at order 3 on the all-e_j = 1 stratum, σ₂(R^F₃(f)) = R_λ₃(f) EXACTLY — discrepancy ≡ 1, support equal on both sides, total in nonzero f (no (R1)/(R2) hypothesis consumed); the ǫ-recurrence does NOT revive on this stratum (the theorem form of the probe's measured (IDENT-3)). CONSEQUENCE, with accepted TR3-S1 + TR3-S2: **TR-3′-GEN is PROVED on {all order-2} ∪ {order-3 all-e_j = 1}, CONDITIONAL ON (H-v3)** — attribution only (the (H-v3) order-3 ordinate identity v₃ = w₂, the order-2 Def-2.5 extension; the finite identity itself is UNCONDITIONAL algebra), with **PIN-GEN-i** the displayed attribution pin (the identification of corrected Def 3.15 at level 2 / Def 3.16 at level 3 / Def 3.18 at i = 3 rests on level-generic print displays + computational discrimination; the owed non-textual leg = a fresh `pdftotext -layout` re-extraction at Defs 3.15/3.16/3.18, hand-verified at the instantiated indices) [the leg DISCHARGED at REVISION 30, r31 — PIN-GEN-i display updated at the r31 bracket below] [the (H-v3) conditionality DISCHARGED at HV3 acceptance fa2180e (REVISION 31, r32) — the scope is UNCONDITIONAL ON PRINT per HV3-THM's four standing conditions: **TR-3′-GEN is PRINT-UNCONDITIONAL at {all order-2} ∪ {order-3 all-e_j = 1}**; the r32 bracket below carries the acceptance record]. The GRB S3.4 scope line carries the matching SECOND post-acceptance bracket, ADDITIVE (that leaf's acceptance counters NOT reset). NOT touched: e_j ≥ 2 strata at orders ≥ 3 AND all strata at orders ≥ 4 (including all-e_j = 1) — i.e. the ENTIRE complement of the proved union {all order-2} ∪ {order-3 all-e_j = 1} — stay FENCED and OPEN [r30-ERRATUM, VC31 gap 2: the orders-≥ 4 family named explicitly; the proved union was always exact, this is display only], probe-first (the sealed UNTESTED (TWIST-3) candidate stays sealed — a compose unit fires only on an exact measured law); D-ITER/TR-4 residues stand; member (a) is NOT discharged — the row STAYS [M]]** **[REVISION 30, r31 — the WAVE-14 window at this row: THREE displays, NOTHING accepted, the row STAYS [M]. (1) **THE FGMN RE-DERIVATION PROPOSAL ON RECORD, NOT APPLIED** (303b11e; note `FGMN_REDERIVATION_2026-08-08.md`; harness `fgmn_rederivation_check.py`, 1,093 checks ALL GREEN first pass; the chartered ASVIN-FLAG design unit executed): the [T-5] glue step re-derived from the provenance-pinned print (`docs/references/fgmn_residual_ideals_1305.0775v3.pdf` sha256-pinned + the fresh `-layout` extraction, sha256-pinned; the corrupted shared fgmn.txt consumed NOWHERE). THE DIAGNOSIS: the compiled `SideReads` clauses (i)/(ii) DOUBLE-SCALE the stride weight relative to the print's parent-valuation keying — the window functional belongs at the PARENT scale σ.wPrev (FGMN eq (13)/Cor 4.4(2): polygon/window/digits at µᵢ₋₁), the residual at the CURRENT graded algebra (Thm 4.2 — the two-scale law IS in the print, reproducing the S1 measured law and `juncforge_clauseIII_derivable` as fixed points) — and the JuncForge PARITY WALL is the ARITHMETIC SHADOW of this double-scaling (a true theorem about the compiled clause shape). **THE ROW-A SOURCE VERDICT** (defined term, note §S4 — a SOURCE-plus-numerics verdict, not a Lean theorem): the source glue step FIRES at the row-A fiber — the parity wall and the C-e escapee are TRANSCRIPTION ARTIFACTS, invisible at every compiled instance (all e = 1/identity-regrade, where w = wPrev pointwise). THE CURE = a FOUR-TOKEN re-key `ν.σ.w → ν.σ.wPrev` at clauses (i)/(ii) (verbatim Lean-ready diff at note §S3; clause (iii) and all digit-algebra content untouched); corrected battery 336/336 on the scale-sensitive window/support check (NOT full bundle satisfiability — the Lean construction stays owed) [r31-ERRATUM, VC32 gap 1], compiled scale 0/336 (the wall universal at the compiled scale), D0 NOT resurrected (eq (14) is the source's own degeneracy — `gate_glue_conditional` stays vacuous). STATUS: **PROPOSAL — hostile pass 1 RUNNING; application ASVIN-GATED** (the flag lineage's TOP item; no `.lean` file touched; `SideReads` stands as compiled); **the JuncForge refutation theorems REMAIN TRUE OF THE OLD CLAUSE** — statements unedited, meanings unchanged (they bind the compiled shape and become the mis-scale record if the proposal is adopted) [r32 STATUS SYNC: the hostile arc has since run p1 0C/4G → REVISION 1 (236d85c) → p3 Fable 0C/4G with the EXACT execution leg → REVISION 2 (0170d68: the diff is THREE tokens, Defs.lean 285/287/292 — every four-token display superseded; the RECOMMENDED application mode = a NEW definition `SideReads'` with the JuncForge theorems byte-frozen) → p4 Codex 0C/3G → REVISION 3 (f64307e) → **p5 Fable CRITICAL** (af88f12: REVISION 3's mirror enumeration was INCOMPLETE — the `SideClauseR2`/`sideReads_r2` pair, the (†)/`hdag` transfer layer whose primed twins need RE-DERIVED readCeil arithmetic at e ≥ 2, `Window.lean` :554/:557, the R5 tie family) → **REVISION 4** (fa2180e: the verifier's complete DictIII site list ADOPTED — the application perimeter is now defined by the GREP DISCIPLINE, not a completeness claim; §S8's raw-order disclosure at the header); the change-set PRICED UP accordingly in the Asvin batch; counter 0/2, passes 6+7 decide; the proposal stays ASVIN-GATED, nothing applied] RIDER DISCHARGED: **PIN-GEN-i is DISCHARGED** (note §S7) — the fresh decorrelated `pdftotext -layout` leg under the sha256-pinned PDF: Def 3.15's prime placement COLUMN-VERIFIED (ǫᵢ(α) = zᵢ^{ℓ′ᵢs(α)−ℓᵢu(α)}, the mode-C reading; the shared extraction orphans the prime = the GRB REVISION-11 artifact), genericity hand-verified at the instantiated indices, from-print operator == certified at all 336 deep sites with the misread exponent discriminated on EVERY tower row; residual displayed: single extractor pass at one tool version (the TR3S3/GRB note brackets ride their own next revisions). (2) **THE (H-v3) DISCHARGE ON RECORD — PENDING VERIFICATION** (f90d68a; note `HV3_DISCHARGE_2026-08-08.md`; hostile pass 1 RUNNING; NOTHING upgraded here): **HV3-THM** (defined term, note §D0) = v₃(P) = w₂(P) for every nonzero P ∈ O[x] at e₀ = e₁ = 1 (**e₂ FREE** — the TR3-S3 stratum contained; the derivation reads only level-≤ 2 data), a theorem given ONLY (i) the standing print-attribution baseline for the sha256-pinned `HigherNewton.tex` GENERIC displays — Def 2.5, the H_{r−1} mapping display, Prop 2.6, Prop 2.7(3)(4), re-fetched byte-identical this unit and NOW PINNED at `docs/references/`; the r = 2 instantiation reproduces the accepted TR3-S1 pin VERBATIM (the charter check) — (ii) accepted TR3-S1, (iii) the standing (H-CHAIN)-at-depth-3 + Lemma RESCALE consumptions. IF HV3-THM survives its verification bar, **TR-3′-GEN's order-3 scope becomes UNCONDITIONAL ON PRINT** — the trilogy's only non-print hypothesis gone (nothing becomes machine-checked or attribution-free that wasn't); **NO bracket edits yet** — the 47 listed (H-v3) sites (TR3S3 30 / ROOT 14 / GRB 3) RIDE THE ACCEPTANCE; this display is the pending-verification notice, NOT the discharge; the (TWIST-3) e₁ ≥ 2 regime is untouched by HV3-THM's scope [SUPERSEDED at REVISION 31, r32 — **HV3 ACCEPTED at fa2180e**: passes 4 (Codex) + 5 (fresh Fable, b2e5b20) consecutive CLEAN 0C/0G, model-diverse, with the pass-5 independent arXiv re-fetch, the fresh 1,655-check two-way falsifier at off-roster p ∈ {7,17}, and the ADVERSARIAL CONTROL (type-hood deleted → exactly the step-5 leg breaks); (H-v3) is DISCHARGED — the scope is UNCONDITIONAL ON PRINT per HV3-THM's four standing conditions (the three displayed here + (iv) the projective-dictionary consumptions, made explicit at HV3 REVISION 1); the 47-site bracket sweep EXECUTED at this revision (reconciliation at §37); the e₁ ≥ 2 out-of-scope clause STANDS — the r32 bracket below is current]. (3) member (a)'s deeper strata — **THE TWIST-3 LAW MEASURED EXACT** (seal 76513f8 pre-run + verdict 48c07b4, the two-commit protocol; note `TWIST3_PROBE_2026-08-08.md`; 23,733 samples, seed 20260818, rerun JSON-identical): the ǫ-recurrence **REVIVES at order-3 e₀ = 1/e₁ ∈ {2,3}/e₂ = 1 with EXACTLY the sealed character** — (TWIST-3) C^F3_j = z₂^{θ₂s_j}·c^G3_{s_j}, **θ₂ = ℓ₁g₁γ₂, EXACT 0/1,656** (the precise level shift of TR3-S2's order-2 θ = ℓ₀g₀γ₁); the degenerate alternative IDENT-3-persists REFUTED at ALL 710 twist-visible slots; the KEY0/ORD-INV lift confirmed one level up (the θ₂-twisted ψ₂ key class 24/24, divergence exactly on the 22 twist-visible towers; matched-label ord transport 0/1,656; naive shared-ψ₂ pairing fails 142×); reading-P ǫ₂ refuted 1,277/1,277 with BOTH readings nontrivial; marched increment exact 504/504, marched-ǫ residue ≡ 1; **CERT3 = the genre's FIRST PARI-certified RAMIFIED order-3 keys** ((e,f) = (e₁, d₀g₀g₁g₂), 12/12). HONESTY, displayed: **exit 1** — ONE sealed SECONDARY display clause (the P4′ divergence criterion) went RED 10/24, root-caused POST-HOC (unmonicized + bottom-weighted comparison; the corrected formulas match 24/24 in the labeled post-hoc script ONLY), the coefficient LAW itself green 24/24, ZERO harness amendments — the red stands as committed (the D-ITER precedent). **TR3-S4 CHARTERED** (earned per the F2b protocol — a compose unit fires only on an exact measured law): TR-3′-GEN at order 3 on the e₀ = 1/e₁ ≥ 2/e₂ = 1 stratum, route on record (ǫ₂-Bezout cancellation + the level-3 A-GEN lift + TR3-S1 digits + t₂-absorption + the KEY0/ORD-INV display lemmas); **verifier-owned from-note implementation REQUIRED at acceptance** (the pass-7 F2c standing rule); instances not proofs, (H-v3) displayed (shared-input scope) [r32 NOTE: the HV3-THM discharge does NOT reach this stratum — HV3-THM requires e₁ = 1 and TR3-S4's stratum has e₁ ≥ 2; the order-3 ordinate hypothesis STAYS DISPLAYED here, exactly as the shared-input scope states]. The stratum AFTER that (e₀ ≥ 2 stacked / e₂ ≥ 2 live carry) stays PROBE-FIRST, NO candidate sealed (the mechanism note predicts a non-scalar inner-twisted evaluation — the next probe must MEASURE the shape). Member (a) is NOT discharged; the row STAYS [M]]** **[REVISION 32, r33 — FGMN RE-DERIVATION ACCEPTED (ec64503; note `FGMN_REDERIVATION_2026-08-08.md` ACCEPTANCE RECORD): the 14-pass, 10-revision arc closes — pass 12 (Fable) CLEAN 0C/0G WITH the full execution leg (both sha256 pins + the layout extraction independently regenerated byte-identical; the 1,093-check battery rerun byte-identical; the four JuncForge/D0 fixed-point theorems compiled directly; a from-note 336-site reimplementation exact) + pass 14 (Codex) content-CLEAN 0C/0G under the ledger's division-of-labor disposition — model-diverse, THREE independent executed legs on record (passes 3/9/12). **The glue route's SOURCE-LEVEL QUESTION IS ANSWERED**: the print's glue step FIRES at row A — the compiled parity wall and the C-e escapee were TRANSCRIPTION ARTIFACTS of the double-scaled clause, not source facts. ACCEPTED (the REVISION-9 scope cut binds acceptance to the MATHEMATICS ONLY): (a) the two-scale diagnosis (compiled `SideReads` double-scales the stride weight vs the print's parent-valuation keying); (b) the ROW-A SOURCE-FIRES verdict; (c) the THREE-token proposal diff (HC2/Defs.lean:285/287/292, σ.w → σ.wPrev) + the fixed points — the S1 two-scale law, `juncforge_clauseIII_derivable`, the DITER §S3 supplier obligations, and the JuncForge/D0 RETAINED SEMANTICS (`juncforge_no_e2_junction` / `juncforge_deep_wall` / `juncforge_junc_bundle_false` / `juncforge_D0_instanceFalse` keep their EXACT COMPILED MEANINGS as theorems about the OLD clause, byte-frozen under the new-definition route — a load-bearing fixed point INSIDE acceptance); (d) the PIN-GEN-i discharge; (e) the pinned sources + battery record. THE APPLICATION = the ASVIN-GATED application unit's deliverable: a scratch-worktree diff adjudicated at the Asvin gate ON THE DIFF ITSELF, with a POST-APPLICATION hostile pass as the completeness test (the strongest available adjudication, not a totality oracle); the REVISION 4-8 censuses are the reviewer's INFORMATIVE MAP, retained as the record of the five executed perimeter refutations — the impossibility claim is scoped to those five demonstrated failures. The flag lineage's TOP item is now a FULLY VERIFIED proposal; NO `.lean` file touched — `SideReads` stands as compiled until the gate fires; the route's revival awaits only the gated application. TR3-S4 STATUS at this row's deeper-strata display (NOTHING accepted, NO status upgrade): the compose note LANDED (`TR3S4_2026-08-08.md` — the measured (TWIST-3) law composed as an identity on the e₀ = 1/e₁ ≥ 2/e₂ = 1 stratum, attempt grade), with **Lemma HV3-E2 DERIVED from the pinned print, not hypothesized** (v₃ = w₂ at e₀ = 1, e₁ ≥ 2, from the same sha256-pinned HigherNewton.tex generic displays at |λ₂| = h₁/e₁; no bespoke ordinate hypothesis remains; HV3-THM's acceptance does NOT transfer — the new instantiation is the note's own hostile-pass obligation); falsifier sealed 7fa8825 → first run 13,679 samples / 0 violations (79eaee6), rerun JSON-identical; pass 1 (Codex) 0C/2G archived — REVISION 1 owed, counter 0/2; the F2c VERIFIER-OWNED from-note implementation stays the acceptance obligation. Member (a) is NOT discharged; the row STAYS [M]]** **[REVISION 33, r34 — TR3-S4 ACCEPTED (cd611af; note `TR3S4_2026-08-08.md` ACCEPTANCE RECORD): the 4-pass/2-revision arc closes — pass 3 (Fable) CLEAN 0C/0G WITH BOTH execution legs (the sealed battery reproduced byte-identical, 13,679 samples / 0 violations; **the F2c VERIFIER-OWNED from-note implementation DISCHARGED IN-PIPELINE for the first time** — 1,108 checks / 0 failures at off-roster p ∈ {5,7} on 6 towers, GMN ordinates via the decorrelated geometric (E4)-Fraction leg rather than the hard-coded ordinate reading, teeth validated by 3 mutation controls, each RED) + pass 4 (Codex) content-CLEAN 0C/0G — model-diverse under the standing division-of-labor discipline. The deeper-strata display UPGRADES: with accepted TR3-S1/S2/S3, **TR-3′-GEN is PROVED on the THREE-component union {all order-2} ∪ {order-3 all-e_j = 1, PRINT-UNCONDITIONAL} ∪ {order-3 e₀ = 1, e₁ ≥ 2, e₂ = 1, conditional on (H-ℓ) + the (KP_i)+[T] consumptions at S6.2 + the PIN-GEN-i attribution display, with Lemma HV3-E2 DERIVED from the pinned print, not hypothesized}** — the strata family at FOUR accepted members; deeper strata (e₀ ≥ 2 stacked; e₂ ≥ 2; orders ≥ 4) stay OPEN, PROBE-FIRST, NO candidate sealed (the mechanism note predicts a non-scalar inner-twisted evaluation — the next probe must MEASURE the shape). The upstream HYGIENE bracket applied this fold: `TWIST3_PROBE_2026-08-08.md`'s pre-correction KEY0-LIFT orientation label ("GMN-lawful = θ₂-twisted ψ₂") superseded by the REVISION-1 corrected orientation (the GMN-lawful class is the UNTWISTED ψ₂; the twisted class = the FGMN-normalized lift), ONE dated additive bracket, the probe's measured data untouched. Member (a) is NOT discharged; the row STAYS [M]]** **[REVISION 34, r35 — the (iii) STRATA-PROBE member's ITER-LAW display UPGRADES: **ITER-LAW is PROVED AT ATTEMPT GRADE on the harness tower class** (standard-lift towers — the class DITER_RESTATE §S2 pins; the ENTIRE 0/804 + 0/840 instance base sits inside it; note `ITERLAW_2026-08-08.md`): the CARRY-1/W-MULT fibred route (EQ12-ADD both levels + INNER-SHIFT + WINDOW + EPS-CLOSED/EPS-MULT + the two-line Bézout Lemma EXP + ANCHOR-VAL + PAIR-VAL Cases I/II); two-commit seal honored (seal ab7c31c, run 242ba1e); falsifier ALL GREEN FIRST PASS exit 0 — 35,232 samples / 0 violations (F1–F10 + CERT; Case-II coverage MET; row-X NOT-CYCLIC 4/4 with EXACT value-set match to the row-N tables); (W-MULT) RECOVERED as a corollary, not consumed; B0a/B1/D-REAL-degeneration/B0c displayed corollaries. HONEST RESIDUALS: **(ITER-LAW-LIFT)** general lawful (non-standard) lifts OPEN exactly at g₀ = 1 ∧ δ₁ = 1; the gr(w₂) semantics wrapper rides the accepted GRB retarget (consumer-side, NOT consumed by the theorem); orders ≥ 3 NOT TOUCHED; **ACCEPTANCE COUNTER 0** — hostile passes IN FLIGHT (Codex content + Fable execution/F2c, concurrent with the r35 fold; at the fold's close both landed — Fable/F2c CLEAN 0C/0G, Codex content NOT-CLEAN 1C/1G, errata round owed to the next unit; counter stays 0; the §40.(i) concurrency disclosure governs). Consumer touch-point: DITERSUP's gate J3b MAY now cite ITER-LAW proved-on-class (attempt grade, acceptance pending; standard-lift instances only). Member (a) is NOT discharged; the row STAYS [M]]** **[REVISION 35, r36 — the (iii) member's ITER-LAW is ACCEPTED: the hostile-pass arc closed at the 2-clean model-diverse bar on the amended proof note (post-errata 7566ccd) — PC1 (Codex content) NOT-CLEAN 1C/1G (acc955a) → PE1 (Fable execution/F2c) CLEAN (64ac340: F2c from-note reimplementation 89,056 samples / 0 violations + 4/4 mutation controls detected) → errata round 1 (7566ccd: the S0 scope ERRATUM + the COR-4 mixed-block supplement `iterlaw_cor4_mixed.py` 18,396/0, sealed falsifier md5-untouched) → PC2 CLEAN 0C/0G (804b88a) + PE2 CLEAN 0C/0G (c40a1da); adjudicated 33373cb; ACCEPTANCE COUNTER 2/2. SCOPE AS ACCEPTED (supersedes the r35 bracket's "the class DITER_RESTATE §S2 pins" phrasing): proved and ACCEPTED on the harness tower class (standard-lift; a STRICT SUPERSET of DITER_RESTATE §S2's pinned E₂ = e₀e₁ > 1 setting; coincides with the §S2 display on the nose restricted to E₂ > 1; the E₂ = 1 leg a harmless COR-3-covered extension outside the statement of record). RESIDUALS, ALL THREE: (ITER-LAW-LIFT) open exactly at g₀ = 1 ∧ δ₁ = 1; the gr(w₂) semantics wrapper rides the accepted GRB retarget consumer-side; orders ≥ 3 untouched. Member (a) is STILL NOT discharged; the row STAYS [M] — the accepted result is the (iii)-member's ITER-LAW on the harness class, not the (GR-B) package]** |
| **(FRESH)** | the joint-with-parent level-determinacy separation clause (O-9's named open), priced at the GD-2/CL-13 vertex-law layer = CU-1's step content; **since the O-9 rev-2 repair it carries clause (c) = the former JUNCTION-PIN** (PROVED label withdrawn at 01c21e6; the telescope consequence is proved GIVEN the clause) [REVISION 1, finding 4] | O-9/CU-1 layer | enters Step 10 (CU-1's step — the GD-2/CL-13 vertex-law layer where it is priced; Step 10's Delivers-line names the consumption site, so clause **(R)** inherits it through Step 18's K1/CU leaf labels — the REVISION-3 A6(iii) phrasing "clause (SQ) inherits it" is SUPERSEDED at REVISION 4, finding VC4-4: the semantic (SQ) is unconditional and consumes no CU content) and Step 14's joint census CEN-J, hence Step 18's K3, and Step 16/clause (UB) through the same census (REVISION 3, finding B1). Numerically unviolated (8.2M boxes). **[REVISION 20, r21 — Lean-face sync; the row STAYS [M], nothing discharged: H2-U9 LANDED** (commit 23d2c03, `Scaffold/HDischarge/H2/TransDeep.lean` — H2 wave 3 complete): the **(TRANS-DEEP)** hypothesis row `TransDeep` (the blueprint-§5 (F)/(L)/(J) row at one census-keyed (W, P, J)) STATED — displayed + consumed, NEVER proved; its supplier is the §B2-DEF campaign's future Lean formalization — with two PROVED Lean-core consumers: `fresh_of_transDeep` (the (FRESH-RED) assembly at the census counting shadow — clause (b)'s counting face fires U5's `freshReads_of_digitsProd`) and `freshRowOn_of_transDeep` (the D-keyed BP_IV C5′ `FreshRowOn` binder at U8's packaging). The unit's B2-FINAL gate was SATISFIED by the ACCEPTED **§B2-DEF** supersession (blueprint §1: the B2-FINAL tie is organizational, not logical; §B2-DEF accepted + dual-accepted, pins re-verified at land time). Dual statement gate PASSED (Codex R3 ACCEPT 0 crit/0 gap after two folds — `LeakFreeLaw` DELETED, (L) re-typed bare Prop; Fable leg fresh-context PASS). Honest residue: (L) = `leakFree` is a BARE-Prop UNPINNED placeholder (no faithful typed counting shadow exists at the skeletal carrier — the Codex fold of record); its typed carrier + the Step-10/K1 classifier face are OWED at the U10 chain-keyed twins; `huni : SlotUniformLaw` rides the CONSUMING theorem, never the row ((ADM)-FULL + (GR-B)-line suppliers displayed, not smuggled)]** **[REVISION 22, r23 — the B2D TD campaign folded; the row STAYS [M], nothing discharged: the (TRANS-DEEP) hypothesis row now has a **COMPILED SUPPLIER AT THE LEDGER-INSTANCE SCOPE** — the B2D TD projection layer is SORRY-FREE END TO END (E2/E2-v2/E2-v3 + TD-0..TD-6 + all gates and countermodel records; the statement-hardening arc on file: TD-3 REFUTED as stated at v1 (5371139, the infinite-chart Nat.card seam) → v2 finite-box semantics (eaa1a5f, N-TD1 3/3 MATCH) → round-2 seams refuted (d770cf3, cmL3/cmL4) → **TD-3 PROVED on v3** (a1e14ef, the MOVES D.11 product argument in full, the two seam clauses `member_exists`/`parent_box_disjoint` added) → TD-2/TD-4/TD-5 proved at v2 + **TD-6 PROVED in full** (9a10109)): `transDeep_of_ledger` produces EXACTLY the `TransDeep W P J` datum `fresh_of_transDeep` consumes (§3 signatures verbatim, no consumer signature touched), `ledger_freshRowOn` compiles THROUGH `freshRowOn_of_transDeep` — the interface fill is MACHINE-CHECKED, not asserted — and the firing gate `td_transDeep` is non-vacuous at the designated r = 1 instance (positive fiber counts, `td_devacuify`; the `leakFree` plug verified definitionally by `transDeep_of_lawful_leakFree` = rfl, never True). `huni` STILL rides the consuming theorem, never the row (displayed (ADM) hypothesis, passed through). Honest residue: the supplier is at the LEDGER-STRATUM INSTANCE scope (`LedgerStrataV3` membership) — the general-instance supplier and the Step-10/K1 classifier face stay owed, so the row stays [M]]** **[REVISION 24, r25 — the huni arc; the row STAYS [M], nothing discharged. The displayed `huni : SlotUniformLaw` face was DECIDED at compiled instances (the countermodel-first entry protocol, synthesis-pass-5 F1): **VACUOUS at tdL, REFUTED at the v3-lawful td2 extension** (`HuniProbe.lean`, 375487c — the alphabet seam: `SlotUniformLaw` quantifies codes z < q^{D.d} while the v2/v3 ledger reads ONE `Fin q₀` leaf, forcing `slotDigits = 0` on [q₀, q₀^d); `FreshReadsLaw` fails too, so the CHAIN was re-keyed, not the law re-guarded) → **HUNI-V2, the d-leaf-read (v4) round-4 carrier repair, LANDED** (058aaf3, countermodel-pinned under the standing statement-change authority: `TDDefsV4.lean` — `LedgerStratumDataV4` + `slotRead`, the [R1-G2] field-element-to-code bijection as the base-q₀ code of the d leaves; `LedgerLawfulV4` = the 15 v3 clauses with EXACTLY 5 re-keyed to `slotRead`; `LedgerStrataV4`; `TDV4_chain.lean` — the TD-1..TD-6 v4 twins incl. `ledger_freshRowOnV4`, the LIVE huni face; `HuniV4Probe.lean` acceptance gates: `slotUniform_td2V4` — huni HOLDS at the transported countermodel td2V4; `td2V4_oldPair_uniform` — the exact z = 0/z = 2 refutation pair now counts 1 = 1; `td2V4_freshReads` — `FreshReadsLaw` DELIVERED where `freshReads_td2_false` refuted it). NO in-place signature change: ALL H2 statements (`SlotUniformLaw`/`FreshReadsLaw`/`FreshClauses`/`TransDeep`) byte-UNCHANGED; v1–v3 files byte-unchanged as records; tdL/td2 remain v3-lawful; **B2D 0 sorries incl. the v4 layer**, footprints Lean-core. STATUS: the huni face moves from refuted-as-stated (v3 family — `ledger_freshRowOn` stands as the round-3 record with its DEAD binder) to **displayed-and-satisfiable** (v4 family, `ledger_freshRowOnV4`'s binder over `LedgerStrataV4`, satisfiable at d ≥ 2 by `slotUniform_td2V4`); huni still rides the CONSUMING theorem, never the row — (ADM) + (GR-B)-line content, never supplied by the TD chain. Full repair record: `HUNI_PROBE_2026-08-08.md` §REPAIR RECORD]** **[REVISION 25, r26 — leakFree TYPED: the general-instance face LANDED at the v4 carrier; the row STAYS [M], nothing discharged (d237149, `B2D/LeakFaceV4.lean`, 14 decls, zero sorry, all footprints Lean-core, every existing statement byte-unchanged; note `LEAKFREE_2026-08-08.md`). THE TYPED LAW **`LeakFreeCarrierV4`** = TD-5's accepted (ii.3)/(ii.4) `LeakFreeCarrier` pair PLUS "every read leaf of every interior on-line slot avoids the parent block" — clause (L)'s §5 referent at the d-leaf `slotRead` (a consequence-of-clauses carrier, TD-5 species; NO counting law, the H2-U9 R2-C1 counting-shadow bar respected), with the display face `read_parentBlind`. **General supplier `ledger_leakFreeV4`**: EVERY `LedgerLawfulV4` datum satisfies the typed law. WIRING: the chain's displayed `leakFree` upgraded from rfl-INSPECTED to **DISCHARGED** at both presenting-datum builders AND the family suppliers (through the Exists.choose transport, `TransDeep.leakFree_of_heq`); the fired `TransDeep` rows (`td_transDeep`/`td2_transDeep`/`td2V4_transDeep`) now carry a **PROVED (L) field**. **DE-VACUITY FENCE compiled** (`td2V4bad_not_leakFreeV4` — the law is falsifiable at a raw datum, lawfulness load-bearing). HONESTY: all compiled instances have `parentBlock = ∅` (trivial there; content = supplier + fence; a **nonempty-parent LAWFUL instance = forge-class**, the consolidated F5 list); the H2 `TransDeep.leakFree` FIELD stays bare per the U9 fold of record (no faithful typed counting shadow at the skeletal carrier — displayed, by design); **still owed: the Step-10/K1 classifier consumer face** (the other U10 twin). The (FRESH) general-instance-supplier residue is retired ONLY in its leakFree typed-face component — the full-row residue (the whole §B2-DEF at non-ledger instances) stands]** **[REVISION 26, r27 — the pass-6 dispositions at this row's residuals; nothing discharged (`docs/SYNTHESIS_PASS6_2026-08-08.md` F1/F3a, 976484d). (1) **The nonempty-parent lawful instance is DECLINED at pass 6** (F1, price M): the law is already falsifiable (the `td2V4bad` fence) and generally supplied (`ledger_leakFreeV4`), so a hand-built two-level `LedgerLawfulV4` datum at an occupied parent block has low standalone value; the DISPOSITION OF RECORD, displayed — *revisit ONLY if the classifier consumer face, once stated, demands a nonempty-parent witness for its own non-vacuity*; the r26 forge-class marker on this residual is retired TO this displayed disposition (not silently dropped). (2) **The Step-10/K1 classifier consumer face is now ATTEMPT-GRADE** (pass-6 F3a; was unpriced-shape): `LeakFreeCarrierV4` + the general supplier + the compiled de-vacuity fence provide the exact typed object AND the method (consequence-of-clauses carrier, display face, compiled fence); missing = ONLY the Step-10-side statement (how the classifier census consumes parent-blindness); one statement-design unit, fired at `td2V4`, fence mandatory (price M) — QUEUED at pass-6 #5, dispatched at wave 10. The full §B2-DEF non-ledger general supplier stays the row's theorem-strength residue, untouched]** **[REVISION 27, r28 — the Step-10/K1 classifier consumer face FIRED; the residual DISCHARGED at the v4 supplier chain + fence; the row STAYS [M] (380cc37; `B2D/K1ConsumerFace.lean`, 8 NEW decls, zero statements touched, all Lean-core; note `K1FACE_2026-08-08.md`). THE FACE **`K1ConsumerFace L`** = at every INTERIOR on-line slot k ≠ k_s there EXISTS a label function on the off-parent coordinates through which `slotRead` factors — the CU leaf label is a STRATUM functional, never a realization functional (k_s excluded by design: its read is clause (c)'s parent charge; per the HDISCHARGE_H2 §1/§2.2 consumption map — Step 10's CU leaf labels inherit (FRESH), clause (R) consumes them through Step 18's K1 regrouping). EXACT-STRENGTH PIN `k1ConsumerFace_iff_parentBlind`: the face ⟺ parent-blind descent — Step-10/K1 asks exactly the (L) content, nothing more (the H2-U9 R2-C1 counting-shadow bar respected consumer-side); display `labelFiber_parentBlind` = the K1 regrouping cells are invariant under parent-block rewrites. SUPPLY CHAIN: `k1ConsumerFace_of_leakFreeV4` (the typed carrier's `read_parentBlind` IS the descent) → `k1ConsumerFace_of_lawful_v4` (via `ledger_leakFreeV4`) → family `ledgerStrataV4_k1ConsumerFace`; **FIRED, not obstructed: `td2V4_k1ConsumerFace`**; mandatory de-vacuity fence `td2V4bad_not_k1ConsumerFace` compiled (at the parent-on-read-leaf raw datum NO label exists — identical off-parent restrictions, reads 0 ≠ 1). HONESTY: td2V4's parentBlock = ∅ (trivial route there; the content = suppliers + fence); the nonempty-parent DECLINED disposition STANDS — its revisit trigger did NOT fire (non-vacuity carried by the compiled fence); NO tie claimed to the Step-18 K1 exact form at the K-CUT/pool carrier (`KCutK1Tie` owners [2r]/CL-8 + (K-CUT-g) untouched). The row's remaining residue = the full §B2-DEF non-ledger general supplier (theorem-strength), unchanged]** |
| **(ADM)** — O-9's census admissibility [NEW row at REVISION 2, finding V2-5; REWRITTEN to the single operative form at REVISION 4, finding VC4-2] | **THE OPERATIVE DEFINITION (O-9 r4, FULL attainment):** every on-line lattice slot's β_k is FULLY attained by the ledger — dim_{F_q} G_{β_k} = s(β_k+1) − s(β_k) = d, equivalently #{𝐣 : wt(𝐣) ≡ β_k (mod e), wt(𝐣) ≤ β_k} = d — the explicit hypothesis on CEN-W/CEN-J (O-9 r4, unit of record 35faac7). Two earlier displays are REFUTED and superseded, kept here so no reader can consume them: the r2 CLASS form "β_k mod e ∈ {wt(𝐣) mod e}" (refuted by K6's r = 1 countermodel, 256/26244) and the r3 VALUE form "G_{β_k} ≠ 0" (refuted at d ≥ 2 by the pass-3 countermodel, 73728 vs 49152, ratio (q+1)/q per partially-attained slot) | O-9 (Step 14) | p-free and per-datum DECIDABLE (a finite lattice check; at r = 1 the displayed inequality β_k ≥ h₁((h₁^{−1}β_k mod e₁) + (f₁−1)e₁), collapsing to the r3 form at f₁ = 1), so borderline [M]/[T] like (R1)–(R4); kept [M] for honesty. **Automaticity, exact:** automatic at r = 0 ONLY (dim = f₀ = d on nonnegative on-line data); NOT automatic at r ≥ 1 (two explicit r = 1 countermodels on file); every older "automatic at orders ≤ 1" phrasing in this document is superseded by this row [finding VC4-2's charge]. Enters Step 14 (CEN-W/CEN-J), hence Step 18's K3 census half (K3-b) AND Step 18b through seam (S1) [added at REVISION 4, finding VC4-5], AND Step 16/clause (UB) through Step 16's consumption of the census over any complete DVR [REVISION 3, finding B1]; carried by (ROOT-C)(H3); consumed by clauses (R)/(UB) only — NOT (SQ) [REVISION 4, finding VC4-4]. Unattained-vertex data have census 0 (the padding value), so the hypothesis prices attainment, not positivity. Falsifier state: K6 (class refuted, 0 = 0 exact) + K7 (value-vs-FULL dichotomy — countermodel refuted, first d ≥ 2 VALUE gate exact: census q^14(q²−1)² at p ∈ {2,3}). O-9 pass 4 in flight; the [M] classification is unchanged **[REVISION 26, r27 — STATUS CORRECTED: NOT BLOCKED — the adjudicating gate was NEVER RUN (`docs/SYNTHESIS_PASS6_2026-08-08.md` F4, 976484d). The row's two tracks: track A = prove (ADM-REAL) over realized shape families; track B = the U6a/U6b CEN-W generalization (statement-change-gated, contingency). **G-ADM-2** (HDISCHARGE_H3 §1.4: instrument the order-2 census harness to log dim_{F_q} G_{β_k} vs d at n ≤ 5, p ∈ {2,3,5}) routes between them and had NEVER RUN — it is this row's exact countermodel-first entry; single unblocking artifact = the committed G-ADM-2 log + a one-page track-A/track-B disposition. Dispatched at wave 10 (pass-6 #4; preregistered predictions SEALED pre-run at 4d7dba1); results ride r28 — NO attainment outcome is claimed here. (The ADM-U0 repair was executed pre-pass-5, e1cf117; U6a/U6b are gated on G-ADM-2, NOT on U0)]** **[REVISION 27, r28 — G-ADM-2 EXECUTED: the row ROUTES TO TRACK A; the row STAYS [M], NOTHING PROVED (38ac01e; preregistration sealed pre-run 4d7dba1; 198 rows, 13 configs, P1–P7 ALL PASS; note `GADM2_2026-08-08.md`, sealed artifacts GADM2_DISPOSITION.md + jsonl). THE SEALED DISPOSITION, verbatim: **RIGHT-END-ONLY PARTIAL — no track-B fire; track A live as (ADM-REAL′); ADM-U7 dichotomy CONFIRMED at scan scope.** All 85 partially-attained slots sit at the ONE ρ-pinned right-end slot that CEN-J never charges (the L′ subtraction); off-end partials ZERO across the scan. THE NO-HIT DISCIPLINE, displayed: the scan ADJUDICATES ROUTING ONLY and proves NOTHING — a no-hit run upgrades no hypothesis instance; the row's [M] status and the FULL-attainment operative definition are UNCHANGED. Next (ADM) unit = the **(ADM-REAL′)** attempt per `ADM_U7_TRACKA_ATTEMPT.md` (track A: prove the realized-shape-family form; track B stays the statement-change-gated contingency, NOT fired) — dispatched at wave 11, results ride r29. (Harvest honesty: the wave-10 unit ended mid-battery; the orchestrator completed the write-up from the unit's own sealed artifacts — no content beyond them)]** **[REVISION 28, r29 — **ADM-REAL′ ACCEPTED** (ledger 24e51df; passes 3 (Codex) + 4 (Fable, 24a595c) consecutive CLEAN 0C/0G, model-diverse — the tree §4 bar met at REVISION 2 of the note; note `ADMREAL_2026-08-08.md`; attempt 045b435 → REVISION 1 e05e660/90e1ecb sealed pair → REVISION 2 b2e173a): **(CLASS-d) is a lattice THEOREM at every order — Lemma CLASS-LAT, UNCONDITIONAL** (the (GR-B) conditionality REMOVED from the track-A chain); NON-END + LOC + W-CAP stand at FULL any-type scope (the GMN Lemma 2.14 route — `omji` printed for ANY §2-scope type incl. stationary levels, pins (P-OMJI)/(P-VRPHII), sha256-pinned arXiv-v2 TeX source decorrelated from the shared extraction). **The row's track-A attempt is DISCHARGED to the displayed opens** (the row STAYS [M] on exactly these): (i) **(B-PIN)** — the boundary hypothesis of the consumption corollary (the ρ-pinned k_s slot subtracted via L′; the corollary holds at the FULL-at-the-L′-slots conjunct ONLY, value-attainment at vertices displayed OPEN; the ε = 1-exclusion equivalence a displayed open audit item); (ii) the **(C-JCT)** fan-out audit (the consumption-seam audit that owns (B-PIN)'s discharge); (iii) **F-ADM-3** — the CensusCore `wphi` missing-E_i-rescale finding, verified at `CensusCore.lean:155` (call-path audit: 6 consumer files, ZERO rescale sites, all compiled value-level instances r ≤ 1/degenerate — the inheritance PROSPECTIVE on r-generic decls) — **EDIT-GATED design input for the carrier re-key, NO edit made**. Falsifier record: the sealed REVISION-1 run ALL-PASS exit 0 incl. the NEW stationary leg R6 (31,164 types, 0 fails; 43,536 stationary premise-control failures vs 0 live — the pass-1 critical was real at scale); the pass-4 verifier recomputed the arithmetic exactly. Track B stays the statement-change-gated contingency, NOT fired]** **[REVISION 30, r31 — **F-ADM-3 RE-KEY APPLIED** (seal pair: preregistration ba02d60 + verdict 8648d99; note `FADM3_REKEY_2026-08-08.md`; executed under the STANDING DEFINITION-CHANGE AUTHORITY with the on-record mechanism — Codex diff confirmation **CONFIRM**, all four charge points, diff-based with sandbox disclosed; the executed edit is RECORDED IN THE ASVIN FLAG LINEAGE for ratify-or-reopen): the census carrier `CensusData.wphi` (`Scaffold/ValueSide/CensusCore.lean`) RE-KEYED from the birth values e_i·V_i + h_i to the O-9 §4 TOP-NORMALIZED ledger E_i·(e_i·V_i + h_i), E_i = e_{i+1}⋯e_r (new `Emul`/`wbirth`; the OLD BODY DISPLAYED at the def site with the F-ADM-3 cite) — F-ADM-3's prospective-corruption window is CLOSED: r ≥ 2 instantiations of `ADMFull`/`maxClassWeight` now read the honest lattice. NEW Lean-core theorems: **CLASS-LAT is MACHINE-CHECKED AT EVERY ORDER** (`CensusData.classCard_eq_d` via the abstract `ClassLat` Nat.ModEq triangular kernel `digitSum_inj`/`card_digitBox_class` — the first compiled r-generic theorem on the weight lattice), and **ADM-U2's `hclass` hypothesis is RETIRED**: `HD3.classCount_eq_d` proved, `admFull_iff_maxClassWeight` restated HYPOTHESIS-FREE (a strengthening; the old `hclass` form displayed in its docstring; grep at HEAD: zero downstream consumers had passed `hclass`). Ripple: two one-line r = 1 proof repairs (AdmCarrier/Census via `Emul_last`); AdmGates + BOTH B2D audit files ZERO edits; the full 33-module cone `lake build` GREEN; footprints Lean-core, AxChk census unchanged, zero new sorry; the CAPSTONE FENCE VERIFIED (the RealInstanceV2 import closure ∩ the CensusCore reverse-import cone = EMPTY intersection). Falsifier: the sealed leg R7 (the two-commit seal protocol; run 1 exit 0, log `verification/logs/fadm3_rekey_run1_20260803T042912Z.log`) — the re-keyed carrier == the O-9 ledger POINTWISE on all 12,348 grid types (the note's defined verdict **REKEY-CLEAN**, elementwise clause), and **THE ACCEPTANCE TEST PASSED**: the F-ADM-2 88-type r = 2 failing grid → **0 failures** under re-keyed semantics while the historical coded twin stays == 88 EXACTLY (R3′ kept as the historical reproduction). The row STAYS [M] on its surviving opens: **(B-PIN)** + the **(C-JCT)** fan-out audit (now proceeding on the HONEST carrier); **F-ADM-3 LEAVES the open list** (executed); realized r ≥ 2 numeric coverage still NONEXISTENT (R7 is lattice-level, exactly like R3′/R3″ — the TRI + (P1) pin remains the realized-read discrimination)]** |
| **(UB-X)** — the clause-(UB) residue [NEW at REVISION 1, finding 7] | (a) the per-leaf O_δ re-base audit for the ℚ_p-stated leaves (the "per-leaf scope hygiene" of the D-3 collapse, declared at the GD dispatch but unwritten); (b) δ-stable position classes exhaust the realized ledger at every δ — **whose restriction to Step 18 K3-c's realized δ > 1 pools is the named fragment (K3-δ), consumed by clause (R) [REVISION 4, finding VC4-1]**; (c) full solve + type aggregation commute with unramified base change | D-3/O-8b (Step 16; the (K3-δ) fragment also Step 18) | until written, clause (UB) is proved ONLY at the Step-16 displayed scope; (ROOT-C) makes this explicit as hypothesis **(H4b)** (renamed from (H4) at the REVISION-2 split, finding V2-2 — since REVISION 4 the genuinely clause-(UB)-only part is (H4b) MINUS its (K3-δ) fragment: clause (R)'s attribution includes (K3-δ) until the δ-stable match display lands at the O-11/O-8b interface). N8 numerically supports the law it scopes. **[REVISION 21, r22 — the §3.2-adjudication status for member (b)/(K3-δ), NOTHING discharged:** H4-M4 (`H4M4_k3delta_adjudication.md`, rev 4 after three folds) adjudicated the precision step — member (b) NOT WELL-POSED as worded, TWO routes stated exactly; **RECOMMENDATION: ROUTE B** (the intrinsic per-base ledger law (RB): a_δ(y) = δ·a_sym(letter_δ(y)); branch-robust for the non-escalating branches); H4-M5 (`H4M5_k3delta_attempt.md`) proved (RB) as a displayed conditional on its five §1.3 rows — TEXTUAL BAR MET (passes 6+7 CLEAN, model-diverse) — and the sealed falsifier N-K3 ran ALL GREEN (376/376, commit 5fdf350: intrinsic law EXACT at every probed n = 3 pool incl. per-branch at split pools; NO Route-A refuter under the β-leg-born reading, structurally absent at n = 3; the free-re-base pressure displayed, carrier-conditional; NO M4 branch fired). Per the blueprint (Codex-fold finding 3) and H4M7 §4: a Route-B adoption is a LEDGER STATEMENT CHANGE gated on five conditions INCLUDING Asvin sign-off + the ROOT owner's consumer-need walk — until that re-scope fires, member (b) and (K3-δ) stand AS WORDED and clause (R) keeps consuming (K3-δ)]** **[REVISION 22, r23 — THE (K3-δ) AUDIT NOTE, MOVED HERE from the retired (R1)–(R4) row (F-(iv) executed; the note SURVIVES retirement per the pass-1 ledger pin "retires (H4a) mod (K3-δ)"). The note, verbatim-in-content [REVISION 3, finding A5; PRICED at REVISION 4, finding VC4-1]: O-8b's law is declared at δ-STABLE position classes; that Step 18 K3-c's realized δ > 1 pools are of that kind is asserted at the D-c interface with NO supplying row — the fragment is named **(K3-δ)** (= member (b) restricted to K3-c's realized pools) and clause (R)'s attribution consumes it EXPLICITLY; the match display (or the Route-B re-scope above) rides the next O-11/O-8b touch and would retire (K3-δ) from clause (R). Post-retirement reading: the (R1)–(R4) BASE under the pools is now the PROVED package at the P(O-8b/M14) ACCEPT record (the §3.2 (H4a) retirement row); the δ-stability-of-realized-pools assertion — this note's content — remains OPEN and remains clause (R)'s only surviving (H4)-family consumption beyond (H4b)'s (UB)-exclusive members]** **[REVISION 30, r31 — **THE CONSUMER-NEED WALK ON FILE** (4924dcb; note `UBX_WALK_2026-08-08.md` — the twice-dropped item executed as NAMED DISPATCH per pass-7 queue #7; genre DOC WALK: NOTHING adopted, NOTHING discharged, no Lean edits — the walk is condition (5)'s substance for the Route-B re-scope, H4M7 §4's F-B). VERDICT: **RB-DELTA** (defined term, note §1.3 — the H4M7 §4 five-clause delta, exactly) is **CONSUMER-VISIBLE, NOT display-only**, at all THREE site families of the consumption map (complete under ROOT's exhaustive-attribution warrant): (Site 1) clause (R)/Step-18 K3-c via (K3-δ) — **NEED-EXACT and strictly improving**: the consuming row text reads the realized letter's EXPONENT only (no stability quantifier; (K3-δ) exists to bridge a scope mismatch, not a consumer need), and the open [M] fragment would convert to the (RB) instance, PROVED-conditional at the M5 record (residue = M5's five §1.3 rows; N-K3 376/376 green); (Site 2) clause (UB)/Step 16 — a **supply NARROWING** at member (b)'s ONE consuming site (the coverage step): exponent law + letter identification supplied at every realized position, F_{p^δ}-linearity at realized-not-Stable positions NO LONGER PROMISED — and the walk found NO on-file consumer, prose or compiled, of the dropped conjunct (H4M7's fallback trigger UNFIRED on the written corpus; the honest limit displayed: the clause-(UB) composition is UNWRITTEN, so the finding is a census of existing text, guarded by the mandatory withdrawal display); (Site 3) the Scaffold typed faces — a Prop-content statement change (`h4b_stable`/`h4b_k3delta`) with PRESERVED derivation shape (`stable_implies_k3delta`/`clauseR_of_clauseUB` survive re-bound). The F-B firing-condition ledger reads (1)–(4) SATISFIED on the current record; **condition (5) — Asvin sign-off + the ROOT owner's acceptance pass on the walk — is the ONLY open gate**: the note's §6 is the DECISION-READY paragraph (adopt full-scope RB-DELTA on the census's strength, or decline/narrow to the clause-(R)-only fallback at zero additional proof cost; the current wording is NOT a safe default — member (b) adjudicated NOT WELL-POSED as worded, and the F₉ shape witnesses stability failing where (RB) holds) — **the Route-B gate joins the ASVIN BATCH decision-ready**. Until the gate fires, member (b) and (K3-δ) stand AS WORDED and clause (R) keeps consuming (K3-δ)]** |
| **(PACK)** — the actual-pack correspondence + the pack row-laws **= (ROOT-C)(H5)** [NEW row at REVISION 4, finding VC4-3; ENLARGED at REVISION 7, finding VC7-2; ENLARGED AGAIN at REVISION 10, r11, edit E4 — the FOURTH row-law (PACK-iv), an ADDITION to the roster, never a clarification] | D-11's clause 4, OPEN as the D-11 r3 record cites it (8a0786c, pass-2 P3: the completed-node overclaim withdrawn): the CONCRETE assembled pack — the realized tables/rows whose mass laws Step 17's M1 covers and whose root/entrance rows M2's (R-read) layer excludes in-statement — equals the intended 𝔅_n instantiation ENTRY-FOR-ENTRY (= OL-O12-2's remaining DATA half, `O12_phaseB_attempt_rev4.md` §3). **PLUS [ENLARGED at REVISION 7, finding VC7-2] the three named row-laws, stated AT THE 𝔅_n ROWS (hence, through the correspondence, holding at the concrete pack's rows): (PACK-i) rsh_interp** — the interpolation law (RatBurdens' interpolation face): for every row σ and every prime power q₀, the evaluation at q₀ of the symbolic shape series Rsh(vmap σ) equals Rval(vmap σ)(q₀) — the Rsh → Rval conversion Step 18's K2 evaluation glue consumes; **(PACK-ii) r_bdd** — the bound: Rval(vmap σ)(q₀) ∈ [0,1] at every prime power q₀ (what makes ENNReal.ofReal faithful there — with (PACK-i) the K2-evaluation-glue pair); **(PACK-iii) wsh_ok** — regular-subring membership: every row's weight/shape series lies in the regular subring on which the evaluation ring-hom evalAt is defined (with D-6, the membership input to Step 18's K3 evaluation ring-hom — NOT K2's glue; sub-attribution made uniform at REVISION 8, finding VC8-2); **AND (PACK-iv) sem_alloc — semantic verdict-row form at the intended member** [NEW at REVISION 10, r11]: at every block e of the intended 𝔅_n instantiation, the booking is re-entrant ((O1)/(O2′)/(O3)) and the family-(iv) source/verdict rows are the canonical semantic assembly — every verdict-row entry equals Σ_{F exit} m(F)·Σ_{comp_F(σ⃗)=σ} ∏_i β_{μ_i}(σ_i)(q^{D_i}) — plus, at (O2′), the displayed κ₁ re-feed leg and, at (O3), the displayed family-(v) exit scaling, and nothing else — with m(F) the member's family-(ii)/(iii) mass entry and the β's its family-(vi) legs; the member carries no allocation adjustment beyond these. [The premise D-11 r4's Theorem M2 names as **(RE)**; (S1)/(S2) do NOT imply it (finding C6 of the D-11 owed pass — pass 3, `D11_owedpass_2026-08-03.md`, the §3.2 D-11 row). CONSUMED AT exactly TWO displayed sites — the D-11 r4 §2A.4 closure walk's CLOSED two-element set (relative to its displayed residue (EDGE-RES)), which REPLACES the design's former "untraced cover / possibly within 17/18" clause: **Step 18b DIRECT** (Theorem M2's (RE) instantiation) AND **Step 17's E1 delivery display of (ROW-Σ)** (the proved member-row normalization theorem — its (RE) hypothesis at the consumed member IS this clause; (ROW-Σ) itself is M2-free, so the 17 ≺ 18 ≺ 18b order is untouched). [Terminology reconciled, REVISION 13, VC15 advisory 4(a): "CONSUMED AT" here prices the leaf's closed EDGE SET — the two displayed edges through which (RE) content can reach the mandatory path — while Step 17's Delivers-line statement "no consuming proof line on the mandatory path reads a member-row normalization (relative to (EDGE-RES))" speaks of PROOF-LINE consumption: the E1 edge is a delivery DISPLAY, priced conservatively as a consumption site precisely because it is an edge, not because any mandatory proof line reads through it. Both statements are true together — the edge-set pricing over-prices, never under-prices.] Step 18's K2/K3 glue consumes PACK-i/ii/iii, and Step 17's M1 excludes family (iv) in-statement ((a″)). RETIREMENT ROUTES (design §3.3): (route-a), n = 3 — the OL-O12-2 transcription-and-correspondence pass EXTENDED by the named per-row booking-profile duty (verify each sealed block's booking against the (O3) forms and each row against §2.3(iv)'s canonical assembly + the displayed scaling); (route-b), general n — the O-14d instance population constructing the member IN the (O1) booking, the profile then holding by construction.] The first three are EXPLICIT clauses, NOT consequences of the correspondence: entry-for-entry equality transfers a law to the concrete pack only if an earlier displayed theorem establishes that law at the corresponding 𝔅_n rows, and NO consulted record displays one — Steps 2/5 state none of the three by name nor an explicit implication to them (the VC7 finding-2 warrant charge; the REVISION-6 A-6(ii) "priced inside the correspondence" inference is WITHDRAWN as invalid). Steps 18 (item (v)'s row-law consumption, sub-attributed [REVISION 8, finding VC8-2]: PACK-i/ii at K2's evaluation glue; PACK-iii at K3's evaluation ring-hom, with D-6; PACK-iv at Step 18b direct + Step 17's E1 (ROW-Σ) delivery display ONLY [REVISION 10, r11 — the leaf's closed edge set]) and 18b consume Step 17 AS this correspondence-plus-laws; no proof is on file, so it cannot ride the §3.2 D-11 [V] row | D-11/O-12 (Steps 17, 18, 18b; the n = 3 instance = the §3.3 OL-O12-2 data pass); the row-law clauses: the D-11 owner with O-14d/D-11 pack construction (O-11 r3 §5 row 20) | per D-11 r3 the content is a finite per-n data comparison ("finite"/"engineering" ≠ "proved" — the pass-2 P3 charge), so borderline [M]/[T]; kept [M] for honesty until executed and passed — the row-law clauses included (properties to be established at 𝔅_n, not data checks). Enters clause (R) (Steps 17 → 18/18b: the α = R_τ evaluation runs THROUGH the identification and through PACK-i/ii at K2's glue and PACK-iii at K3's ring-hom) and clause (UB) (the same chain at q ↦ q^δ). NOT (SQ)/(0). Numerics: the n = 2 real-pack triple re-derivation (O-11's falsifier) and RESUM-n3 65/65 are its executed instances at n ≤ 3 — the O-11 record's "checked at RESUM-n3" covers the row-laws' instances too [G-3 caveat, REVISION 13, VC15 advisory 3: that O-11 record's END-TO-END reading of RESUM-n3 is challenged by its own un-banked 2026-08-03 pass (finding G-3, the §3.2 O-11 row) — supported beyond doubt only at the five displayed identities; the coverage sentence here inherits that scope] — flags, not the general-n proof. Related but DISTINCT: D-11's widened-charge item (vii) (H.6-fence conformance of the built tables) — the REVISION-4 flag "whether it too should be promoted to hypothesis grade" was RESOLVED at REVISION 5, finding VC5-1: promoted, into the (H6) group below |
| **(T-DEC)/(T-READ)/(T-VERD) + D-11 item (vii)** — the terminal-seam engine-conformance group **= (ROOT-C)(H6)** [NEW row group at REVISION 5, findings VC5-1/VC5-2] | (a) CU-2t's THREE terminal rows (first displayed CUC §9.4; split/amended at R3, 81589e9): **(T-DEC)** — emission discipline at decided sites ((T-DEC-dec)) + the corner fence ((T-DEC-cor), at the Corollary's own conditionality); **(T-READ)** — the emitted shape/position fields equal the (c1)-shape polygon-side datum (the (RC-read) supply; NO displayed machine-side home on file, purely owed); **(T-VERD)** — terminal verdict-field conformance. (b) **D-11 widened-charge item (vii)** — general-n H.6 in-fence conformance of the built tables (the surviving live condition of the tree T-5 row's "six fences gate [1v]-FULL"; the six fence kernels themselves stay off the mandatory path — if a built pack ever NEEDS fenced territory, the kernel so touched becomes ADDITIONAL open mathematics beyond this row). All four sat in §3.3 [T] through REVISION 4; VC5 applied the ledger's OWN classification rule — [T] requires content pinned to a printed source or an existing proof, and these are OPEN PROPOSITIONS consumed on clause (R)'s mandatory path with no proof on file — so they are [M], hypothesis-grade like (PACK) | GD-4/GD-2-family owners per CU-2t R4's grading: (T-READ) re-graded OUT of the "engine bookkeeping" GD-4 label into the GD-2/GD-5 read-semantics-conformance family (at k′ ≥ 1 an OL-2-min instance + the terminal-BINDING clause, which stays GD-4; at k′ = 0 no sibling in the OL residue — a proof-by-construction route cannot discharge it); (T-DEC)/(T-VERD) GD-4 (proof-by-construction routes on record); item (vii): the D-11 owner (Steps 17/18) | enters clause (R) at Step 11's route — CU-2t's §9.5 terminal-verdict conditionality (CU-2t is VERIFIED at scope MODULO exactly these rows), consumed by Step 18 through D-a/K4-CYL — through the terminal-seam leaf labels riding O5triple's counting (Steps 11/12 → Step 17's M3/Thm-E leg) into Step 18's K1 ("CU leaf labels" — the same K1 route the (FRESH) row displays; leg added at REVISION 6, finding A-5) — and, for item (vii), at Step 18's K3-a; clause (UB) inherits the same content through the transported chain at q ↦ q^δ; NOT (0)/(SQ). Falsifier state: the CU-2t sealed gates + V-n3 178/178 / H-n3 151/151 (sealed n = 3 evidence for (vii)) — flags, not the general-n proofs. **[REVISION 20, r21 — the III-S4 repair + H6-A4b sync; the row STAYS [M], no member discharged:** the external blocker on the H6 blueprint's unit A4b is CLEARED — the `cu2t_readForcing` display, which HAD been refuted (FALSE AS DISPLAYED at the S4Refute countermodel; historical record kept untouched), is REPAIRED at the record's option (b) (commit f56a536, a sanctioned statement repair under the standing statement-change authority): NEW named semantic hypothesis row **`TerminalPolySem`** (`DictIII/CU2t.lean` — ties the emitted principal sides to the terminal-level polygon: per-side nondegeneracy, total horizontal length Σ e·ℓ = the EXACT multiplicity j₀ of some irreducible φ in f̄, saturation j₀ ≥ 1; NO supplier at HEAD — a hypothesis row per the [M]-row discipline, the φ-through-ι key identity the documented OL-1 residue) + NEW theorem **`cu2t_readForcing_sem`** (display rows + `hf : f.Monic` (priced) + the row ⇒ the singleton ℓ = 1 side shape), with BOTH compiled gates (POSITIVE `readForcing_sem_gate_pos`; NEGATIVE `d0_fails_terminalPolySem` — the old countermodel does NOT arm the repair); Codex design round 2 ACCEPT + applied-diff CONFIRMED. DOWNSTREAM: **H6-A4b LANDED** — `terminalDatumD_slope_spec` (`H6/Emission.lean`, verbatim as displayed, consuming A6b's `ReadThroughIota`; per-file gates green, Lean-core). Review surface (Asvin, tracked at the ledger + the standing queue — no ROOT review section exists by design): the `hf` Monic pricing and the `TerminalPolySem` supplier question ride the surfaced-for-review rider]** **[REVISION 25, r26 — the `TerminalPolySem` k′=0 charter SPLIT-decided; the row STAYS [M], no member discharged (9cb30d1, `Scaffold/DictIII/CU2tKp0.lean`, NEW file/decls only, green, all six audited footprints Lean-core; note `CU2T_KP0_2026-08-08.md`). **The literal derivability charter REFUTED at THREE compiled gates**: (A) saturation necessary — f = X+1 undecided seam, `ReadThroughIota` holds on the empty datum yet `TerminalPolySem` fails; (B) the −∞ leg unsupplied — f = X, a junk e = 0 slope-−∞ side passes A6b certification and kills nondegeneracy; (C) the CU2t consumption keying (terminal-first, index 1) is TERMINAL-BLIND — `(Theta H).slopes = []` grants no OL1 constraint, the REAL ι-certified read of X+2 on the DEC-decided `S4Refute.H0` fails the row. **The honest 𝐇°-keyed reduction PROVED**: on `H.nodes = []` seams with the `f(0) ≠ 0` + `p ∣ f(0)` perimeter, **`terminalPolySem_kp0`** reduces the face to the **NEW NAMED OPEN ROW `BasePolygonLengthLaw p`** ([M]-discipline, the `Lit3Hensel`/`OL1` genre; a DictIII-side open lemma DISPLAYED HERE in the residuals — NOT a new §3.1 roster row, the hypothesis-group count is unchanged): the OM base length law ℓ(N⁻(f)) = ord_X(f̄) + side nondegeneracy — H-free, D-free pure Newton-polygon geometry, NOT provable at HEAD (only flat-hull machinery exists; the OL5-adjacent convexity gap), proof route displayed in its docstring. Positive gate **`Kp0Gate.terminalPolySem_kp0_gate_pos`** FIRES at f = X+2 with the REAL `gmnDataOrder1` read — **the first ReadThroughIota-certified `TerminalPolySem` witness at HEAD**. `terminalDatumD_slope_spec` not consumed (slope reads, not side-list mass); the −∞ corner stays the separately-priced TerminalPolySem HONESTY residue; the `TerminalPolySem` supplier question (Asvin rider) STANDS]** **[REVISION 26, r27 — **`BasePolygonLengthLaw p` CHARTERED HIGH at pass 6** (F2, price M; `docs/SYNTHESIS_PASS6_2026-08-08.md`, 976484d): it does not HIDE the recorded convexity gap — it IS the gap, honestly priced: ℓ(N⁻(f)) = ord_X(f̄) + side nondegeneracy at monic f with 0 ≠ f(0) ≡ 0 mod p is H-free, D-free, seam-free pure polygon geometry, and the corpus has no negative-prefix hull machinery beyond `Order0Hull`'s flat case; the route is quarried mostly from PROVED kernels (per-side e ∣ length + 1 ≤ length via zip-consecutive + `npVertices_sorted` + `Rat.den` divisibility; slope monotonicity from the PROVED `npHeight_convexOn`; endpoint pins from `valSupport_facts`), the genuinely NEW machinery = the negative-prefix telescoping to the first zero-height vertex; both perimeter legs countermodel-forced (`Kp0GateA`/`Kp0GateB`), so no statement slack; acceptance = `terminalPolySem_kp0_of_law` firing stratum-wide (the (T-READ) k′=0 face's one numerical input) with the Kp0 gates green; the −∞ corner stays separately priced (the f(0) ≠ 0 perimeter excludes it). QUEUED at pass-6 #3, dispatched at wave 10 — the law remains an OPEN lemma until that unit lands. **C2 STAYS BLOCKED — no new mechanism** (pass-6 F4): the window's re-key mechanism (v4 twins; the CU2tKp0 𝐇°-keying) worked at k′ = 0 empty-nodes keying, but C2's face is k′ ≥ 1 where the terminal-binding + OL2min routing hits the III-G3a/G4/G6 typeclass wall unchanged; honest route = ONE dedicated GDOrder1 typeclass-repair DESIGN NOTE (no agents); (T-DEC)/(T-VERD) construction routes stay on record, background]** **[REVISION 27, r28 — **`BasePolygonLengthLaw p` PROVED for EVERY prime, sorry-free, Lean-core; the open-row display at these residuals is RETIRED TO PROVED STATUS** (a6fcc4d; `Scaffold/DictIII/BasePolyLength.lean`, NEW decls only, statement VERBATIM as displayed; note `BPLL_2026-08-08.md`): `basePolygonLengthLaw_proved` supplies the law ℓ(N⁻(f)) = ord_X(f̄) + side nondegeneracy on the monic / f(0) ≠ 0 / p ∣ f(0) perimeter, built on the reusable abstract core `BPLL.npSides_neg_length_sum` on bare support Finsets (the height sign law with two NEW valid-line constructions — flattest chord into (j₀,0), steepest chord out of (0,v₀); the `dropCollinear` merge invariant `dropCollinear_between` + keeps-last; the negative-prefix `telescope_sum`), per-side arithmetic e ∣ length via `Rat.den_dvd`, Σ e·ℓ telescoping to j₀ = natTrailingDegree f̄. **The recorded OL5-adjacent convexity gap was NEVER HIT** — chord-attainment on sides was not needed (convexity enters only via `slope_mono_adjacent` at on-hull points). Test-before-proving honored (20,000-hull numeric falsifier 0 fails + the collinearity-transitivity ℤ-identity at 100,000 points BEFORE proving); compiled gates `gate_hensel` (against the independently landed `principalData_hensel_a0_one`) + `gate_eisenstein_sum` (X² + 2 over ℤ₂: the ramified e = 2, j₀ = 2 stratum) green; `#print axioms` Lean-core on the law, the abstract core, both gates, the consumer, and all five NEW hull kernels. **CONSUMER FIRED: `terminalPolySem_kp0_unconditional`** = `terminalPolySem_kp0_of_law` + the law at p = 2 — the FIRST unconditional `TerminalPolySem` supplier at an 𝐇°-keyed k′ = 0 seam: **the (T-READ) k′ = 0 face's one numerical input is now THEOREM-BACKED** (the r27 charter's acceptance criterion MET). Perimeter honesty: the −∞ corner stays the separately-priced open leg (the f(0) ≠ 0 perimeter excludes it); perimeter necessity stays countermodel-forced (`Kp0GateA`/`Kp0GateB`); `Kp0GateC` (the CU2t keying refuted) STANDS; the row itself STAYS [M] — C2 (k′ ≥ 1) and the remaining members are untouched by this fold]** |
| **(I-τ)/K-LOC — the sibling-interface engine-stratum conformance pair = (ROOT-C)(H7)** [NEW row at REVISION 6, finding F-1 — the LAST of O-11 r2's declared D-a/D-b/D-c interface triple adjudicated: D-a → (H6) at VC5-2, D-c → (H4a)/(K3-δ) at V2-2/VC4-1, and this row is D-b] | **(I-τ)**: the engine's realized site strata present as O-10's ρ-separated adapted cells (the adapted-cell → engine-stratum presentation Theorem 2 needs). **(K-LOC)** (= M07 OL-3, an exactly-stated OPEN lemma — the document's own naming): continuation reads of branch j on the stratum = OM reads of f_j up to cell-constant normalization. Without the pair, Step 18's K1 does not receive O-10's product law at the engine's strata — triple-displayed consumption (Step 15's application line; Step 18's K1 "(I-τ)→GD"; the former §3.3 row's own owner column). **Adjudication own-row-vs-(H6), displayed [the F-1 charge]:** the pair is engine-conformance GENRE (kin to (H6), and K-LOC's read-semantics content is (T-READ)'s GD-2/GD-5 family), but its SEAM is the sibling/product-law interface (K1, Steps 15 → 18), not the terminal seam; its owners are O-10/GD-family (not the CU-2t/D-11 owners); and (H6)'s membership is PINNED by the leaf-#9 interlock (CU-2t VERIFIED at scope modulo exactly its three rows + item (vii)) — so a SIBLING row, not an (H6) member | O-10 consumers / GD-family presentation (Steps 15, 18); discharge = the K-LOC lemma (M07 OL-3) + the (I-τ) presentation write-up | the former disposition "both priced into the GD families" (Step 15) had NO displayed [M] landing — no GD row carried read-locality/normalization content — so under the ledger's own rule the pair could not sit in [T]: no pin, no proof on file, consumed on clause (R)'s mandatory path. Enters Step 15 (application to realized strata) and Step 18 (K1), hence clauses (R)/(UB) only; NOT (0)/(SQ). Falsifier state: O-10's harness 68/0 + U6/IFK gates exercise the adapted-cell law itself — flags for the pair's instances, not the general proof **[REVISION 27, r28 — the FIRST PROBE executed: DECIDED AT THE INSTANCE BIN, the pair VACUOUS-BY-TYPE at every compiled instance; the row STAYS [M], statements STAND unrepaired (2c72c5f; `Scaffold/HDischarge/H7/H7Probe.lean`, 12 thms, all Lean-core; note `H7PROBE_2026-08-08.md`). VERDICTS: (I-τ) and full K-LOC are VACUOUS-BY-TYPE at all four bin instances (toyStage3/bStageP = `Stage`, one read, no roster; H₃rc = `History`, one chain; td2V4 = digit-window carrier, no factorization) — NO countermodel is obtainable there, confirming pass-6 F4's "no decision point of its own" at compile level. The corpus's ONLY compiled m ≥ 2 sibling site (toyTreeA's tA1) is CERTIFIED card-2 Lean-core (toy_roster_card, toy_sibling_pair/parent) but TRIPLY DEGENERATE as an (H7) decision point: diagonal branchCellOf (sibling reads definitionally equal), sides ≡ 1 (split legs omega-vacuous), and a sorryAx carrier chain (measured, kept OUT of the probe's compiled perimeter). The ONE compiled-decidable fragment — KL-1a's order-1 count face (H7-C2) — HOLDS at every bin field (ℤ/3, F4, ℤ/2; kl1a_decided_*). OL-2 SHARPENED to the exact inhabitant spec: an inhabitant must break BOTH rfl-degeneracies (sides ≥ 2 at a branching site AND non-diagonal branchCellOf) on a sorry-free TreeModel; gate_order0_semantic stays the named cheapest Lean route, blocked on OL-2 — no cheap Lean extension exists. **THE ROUTE OF RECORD IS NOW N-H7-1 NUMERICS FIRST** (h7_kloc_probe.py — the instrumented continuation-reads vs OM-reads comparison at realized strata; dispatched at wave 11, results ride r29)]** **[REVISION 28, r29 — **N-H7-1 EXECUTED: the pair SURVIVES FIRST CONTACT at REAL m ≥ 2 sites**; the row STAYS [M], statements still OPEN, nothing proved (f8aec25; `verification/openmath/h7_kloc_probe.py`, sealed predictions pre-run, seed 20260808; note `NH71_2026-08-08.md`): SIX genuine m ≥ 2 rosters across (p,n) ∈ {(5,4),(2,4),(3,3),(3,5),(3,4),(3,3)} — incl. the blueprint-named wild pair, the m = 3 HEN roster, the genuine-f₀/C₀ face, and the KL-1b SIDE-separation face — battery **139 PASS / 8 FAIL classified / 26 INFO**: K-JAC within-cell constancy GREEN; K-LOC (i) λ-side translation GREEN (sealed δ_j exact, singleton observed-δ sets); K-LOC (ii) fixed-λ vertex units GREEN incl. nontrivial u = 2; (iii′)-core GREEN (cofactor-independence + word bijection + K ≤ τ+ρ+1 tight twice); (I-τ) count/fiber/product laws EXACT everywhere incl. #C₀. **TWO MEASURED RESTATEMENT PINS for the future Lean forge** (the 8 FAILs, fully classified): **PIN 1 — ε must be SLOPE-KEYED**: the slope-pooled unit-transport strengthening is REFUTED at S5 (the flip = the unit of the x²-reduction constant on x-dominated deviations; per-slope-parity singletons — the blueprint's per-slope clause (ii) itself SURVIVES; `KLocSemantic` must key ε by realized slope); **PIN 2 — the D-8 guard is the extraction boundary**: the Lemma-A extraction round-trip fails EXACTLY at pins-uncovered below-guard scans (N-emax < τ; 0 false-positives; in-guard 100% clean) — Lean extraction consumers must stay in the guard range. These pins are RESTATEMENT INPUTS, not repairs — no statement is changed at this display]** **[REVISION 34, r35 — **THE FORGE EXECUTED: the FIRST compiled NON-VACUOUS (H7) decision point**; the row STAYS [M], statements still OPEN, NOTHING discharged (037ac6f; `Scaffold/HDischarge/H7/H7Forge.lean`, 15 decls ALL Lean-core, zero sorryAx; note `H7FORGE_2026-08-08.md`): the sorry-free 5-site depth-1 instance at (p,n,N,m) = (2,2,2,4), the S6 shape — BOTH OL-2 rfl-degeneracies BROKEN: (a) non-diagonal `branchCellOf` certified 3 ways (`forge_read_nondiagonal`, `forge_states_crosscut`, the STRUCTURAL `forge_no_diagonal_read` — no single-map read over ANY cell type presents both sibling states); (b) sides = 2 at the joint site with the real `SideSplit` (hull slopes 1 < 2 tied to recorded slopes; split legs discharged; (JC-multi) DECIDED TRUE, 1·2² = 4). The N-H7-1 pins DECIDED AT THE INSTANCE: PIN 1 — `forge_kloc_slope_keyed` PROVED + `forge_kloc_pooled_refuted` COMPILED COUNTERMODEL (the S5 pooled-u refutation's compiled twin); PIN 2 — `forge_guard` InGuard + `forge_klocCounts` = the FIRST contentful `KLocCounts` instantiation (1·4 = 2·2 exact at every word; no gate stated below the guard). HONESTY: depth-1 (the sides-2 site's own continuations unrealized — the HK23/HK25 transplant is the named residual); node literals synthetic-but-lawful (the S2 center-distinction genre); engine-tied `ITauSemantic`/`KLocSemantic` (H7-W3a) and `ITauShadow` at the forge integers (Tier-S, H7-D1b) remain OPEN — the forge is the surrogate decision point, not the discharge]** |
| **(H8′) — the K-CUT residual-interface row** [NEW row at REVISION 17, r18, executing VC19 — the honest re-pricing of the r17 retirement's three moved residues: VC19 blocker 1 found the cited owner statements do NOT house them (D-14/CU-3 prove read locality and nonempty realization, not graft completeness/bijectivity; no cited row states the shMass/blkMass measured-mass identification; no row supplies the node-level τ/δ pool dictionary or blockTotal_tie's instantiation), so clause (R) — and (UB) by inheritance — was silently consuming unpriced content at Step 18's K1 (VC19 blocker 2). SLIMMED against the former (H8): that row assumed the WHOLE cut decomposition; this row assumes ONLY the three interfaces — the decomposition/ownership/product/K1-composition core is machine-proved Lean-core (`kcut_reduce` + `kcut_k1_form` + `massFactor_mass_eq`, the §3.2 K-CUT retirement row)] | THREE members, stated from the package's own displays (the §3.2 retirement record; `Scaffold/HDischarge/H8/R2RootSync.lean` §D; the round-3 report `KCUT_pass3_2026-08-06.md`): **(i) GRAFT — the graft completeness/bijectivity interface**: the `graft`/`graft_cut`/`cut_graft` correspondence is a BIJECTION between the full deep decompositions at a labeled box and the (shallow cut, per-block continuations) pairs — every full decomposition factors uniquely through its shallow cut, and every composite of realizable continuations onto a realizable cut is realizable (continuation memorylessness is the semantic core). STATUS: the H8-R1 charge-(vi-a) FAIL STANDS as this member's status — the O-11 r3 serving map "CUT-WD ← D-14 + CU-3" is NOT re-pointed as demonstrated. **(ii) MASS-ID — the measured-mass identification at the consumed member**: C2's `shMass`/`blkMass` symbols ARE the Haar-measured masses of their shallow-cut/block-continuation events at the consumed member — the proved factorwise law `massFactor_mass_eq` forbids only slot-dependent compensation and cannot exclude uniformly mis-identified block weights. **(iii) POOL-DICT — the node-level τ/δ pool dictionary + the blockTotal_tie instantiation**: a typed node-level read-off for C3's pool rows beyond the pinned `e_key` (`vBridge` is constrained only by keying/injectivity/support), PLUS `blockTotal_tie` instantiated at the intended member | retirement ROUTES — the natural discharge paths, NOT current homes (the VC19 adjudication): (i) a future graft completeness theorem on the **D-14 + CU-3 + REL.2 arc** (Steps 6/11 and their debts); (ii) the **(H7)/TREE-EXP + [2r]/CL-8 development** with the O-1/O-3 count-tie; (iii) **[2r]/CL-8** + the W-leg at **CL-17(ii)/W17ii** | Enters Step 18's K1 (clause (R)) WITH the retired package — `kcut_k1_form`'s conditional conclusion becomes Step 18 K1's displayed transported-pool convolution exactly under these three interfaces; clause (UB) inherits through the transported chain at q ↦ q^δ. NOT (0)/(SQ). Falsifier state: the package's 3-round hostile-pass arc displays exactly these interfaces as its residue (the round-3 DISCHARGES scope line) — the sealed n ≤ 3 gates exercise their instances: flags, not proofs. **[REVISION 25, r26 — member (iii) POOL-DICT DECIDED, countermodel-first; the row STAYS [M], NOTHING discharged, no statement touched (d7f7f58, `Scaffold/HDischarge/H8/PoolDictProbe.lean`, NEW file/decls only, green, Lean-core, zero sorry; note `POOLDICT_2026-08-08.md`). **The pass-4 F2a derive-from-wiring route REFUTED by compiled underdetermination**: over ONE fixed compiled carrier pack (the REAL n2 ledger instance `pdC : UCarriers 2`, the proved `n2hdet`, one shared `KCutReKeys` pack `pdR` with a real `BlockDeriv` — `e_key` non-vacuous), TWO full `KCutK1Tie` instances `pdTIE 1`/`pdTIE 2` agree on `eOf`/`τOf`/`vBridge` and differ in `δOf` (1 vs 2), the grammar-total tie holding NON-vacuously at all three chain-Sigmas members — mechanism: the NEW lemma `pd_blockSolve1_zero` (the n2 e = 1 solve vanishes at every σ′ ≠ {(1,1)}) + `powSubst δ 0 = 0` ∀δ; display `pooldict_delta_underdetermined`. Since the instances share EVERY ambient object, NO law over (C, R, hdet) — `kcut_reduce` rows, `massFactor`, future wiring — can pin `δOf`: **the pool dictionary is genuinely NEW interface content, not derivable wiring**. HONEST SCOPE: the δ-leg is compiled; the τ-leg is same-genre but NOT compiled (every compiled table carries `State ≡ Unit`, so `τOf` is unique there; a **two-equal-state table = forge-class**, the consolidated F5 list); value-pinning at solve-nonzero instances is instance-luck, not a dictionary. CONSEQUENCE: the (iii) wording above is CONFIRMED as the honest form and is now **legitimately blocked-on-owners ([2r]/CL-8 REL.2 + (K-CUT-g)) WITH THE BLOCKER COMPILED** — the "attempt deriving before treating as blocked" disposition (pass-4 F2a, twice dropped) is EXECUTED and closed]** **[REVISION 26, r27 — the pass-6 dispositions at this row (`docs/SYNTHESIS_PASS6_2026-08-08.md` F1/F4, 976484d). (1) **The two-equal-state table is DECLINED at pass 6**: *declined at pass 6 — a second leg of a compiled blocker is not progress* — the δ-leg already refutes the derivation route and (iii) is already blocked-on-owners WITH THE BLOCKER COMPILED, so a compiled τ-leg twin changes no row text; the r26 display above ("τ-leg same-genre, NOT compiled, forge-class") STANDS as the permanent record under this annotation, its forge-class marker retired to this displayed disposition. (2) Member (i) GRAFT stays BLOCKED (theorem-strength graft completeness on the D-14+CU-3+REL.2 arc; no new mechanism), with a CHEAP ENTRY now displayed: a PoolDictProbe-genre bijectivity check of `graft`/`cut_graft` at the compiled n2 pack — it decides whether the interface is even instance-true (S/M, optional batch, folded into passing units, pass-6 #12); member (ii) MASS-ID stays BLOCKED on owners ((H7)/TREE-EXP + [2r]/CL-8 + the count-tie; artifact = the O-1/O-3 count-tie note); member (iii) is CLOSED as decided — nothing further at this row]** |
| **(E-N)** [REWRITTEN at the (H3) discharge adjudication — HDISCHARGE_H3 unit EN-U5, VC-gated, machine clock 2026-08-01 (directive window 2026-08-05): EN-U1 consumption walk = RESCOPE-CLEAN + EN-U2 gate G-EN-1 = HIT, so the §3.4 criterion's rescope wording lands WITH route (b)'s absorption verification forced into this display and route (a) permanently fenced] | O-1thr Theorem 3(c)'s exhaustion caveat | O-1thr package | **OPEN, consumed nowhere unrestricted; scoped count + drainage absorption displayed.** CONSUMPTION (unchanged; completed at REVISION 9, VC10 advisory 2): touches CU-4 through Thm 3(c) (risk row R2 of the O3adj display) — AND enters Step 16/clause (UB) through the (UB-X)(a)-transported dictionary/CU/drainage layers, the SAME CU-4/Thm 3(c) content transported at q ↦ q^δ: the §1 B1 attribution's displayed entry. Beyond that CU-4 touch and its clause-(UB) transport, NO other mandatory-path site consumes it — and BOTH displayed touches are pricing at thr-scoped objects, not consumption of the unrestricted reading. THE RESCOPE (unit EN-U1, `lean/notes/openmath/H3_en_walk.md`, verdict RESCOPE-CLEAN): the 12-site per-site table (CU-4's six displayed legs S1–S3/C1–C3 — the walk's superset of the blueprint's five-way supply grouping, walk §1 — plus the Thm-3 fiber-clause entry, O3adj R2, the Step-16 transport, the squeeze's input-(vii) sum, the (SQ) bracket, the drainage absorption) confirms every finite-N counting/acceptance object on the mandatory path is thr-scoped BY ITS DISPLAYED DEFINITION OR HYPOTHESIS — the scoped count is the assembly's decided_σ(N) := Σ_{T̂ : type(T̂)=σ, thr(T̂) ≤ N} C_T̂(q)·q^{nN−A(T̂)} (input (vii), quoted at walk §4.1); CU-4's legs consume acceptance objects under the hypothesis N ≥ N\*(𝐇), a thr-majorant (CEIL-UB direction); the (SQ) bracket consumes no tree object at all; and both envelope displays on file define undecided-at-N as the complement of the DETECTED-at-N class (walk duty W-3, first horn), so drainage absorption of the thr > N decided boxes is DEFINITIONAL given the connector (EN-W1) ("detected at N ⇒ the box's canonical tree has thr ≤ N" — CU-4/CU-2t window bookkeeping consumed at its displayed conditionality, incl. the §4 D-12r pin's TB-CAP perimeter at leg C3 [re-pointed at REVISION 14, r15] and (AGR) at general n; (EN-W1) points the OPPOSITE direction to this caveat and does not consume it). THE FORCED ABSORPTION VERIFICATION (unit EN-U2, `verification/h3_en_falsifier.py`; log `verification/logs/h3_en_falsifier_2026-08-01.log` + JSON `verification/results/h3_en_falsifier_results.json`; PARI-factorpadic oracle gates PASS per config; gate G-EN-1 = **HIT**): decided ns-free decodes with thr(T) > N exist at ALL 19 probed configs (p ∈ {2,3}, n ∈ {2,3}, N ≤ 5 — the (3,3) family probed to N = 4) — 11,177 hit boxes, 1354 recorded with realized-chain records, margins N − thr down to −9 — so the thr ≤ N clause is NOT redundant (the unrestricted reading counts strictly more boxes: witnessed numerically at n ≤ 3 by the exact machine, route (a) DEAD as stated and PERMANENTLY FENCED per §3.4 of the blueprint), and the architecture's absorption prediction was CHECKED on the hits: operationally each hit is undetected-at-N BY DEFINITION (thr > N means its verdict windows exceed N; the capped-at-N walk halts at its first read with ceiling > N), hence sits in U^can_N and drains at Step 13's displayed price (Theorem N3 unconditional at n ≤ 3; (AGR)-conditional at general n); numerically ALL 1354/1354 recorded hits carry a tree-record witness (a level-N-equal lift realizing a DIFFERENT tree, or going ns — the level-N data does not pin the tree), confirming tree-undecidedness at N; and 77/420 of the n = 2 recorded hits are census-TYPE-decided at N, so the W-3 semantic(type)/operational(tree) readings DIVERGE on concrete boxes — the caveat is NON-VACUOUS, a semantically defined envelope WOULD have broken absorption there, and route (b) survives precisely because the quoted corpus displays define the envelopes operationally. STATUS: the caveat remains TRUE and OPEN as a display (now witness-backed, no longer merely unpriced); the rescope is a DISPLAY CHANGE — (E-N) prices nothing on the mandatory path beyond the CU-4 touch and its clause-(UB) transport displayed above. CARRIED RISKS (walk §6, restated): CU-4's own end-to-end walk is UNWALKED and conditional on P(O-1thr r4) + P(O-2a r5) + P(CU-1 r2) — EN-U1 walked the (E-N)-relevant faces only and must not be cited as CU-4's full walk; if a future CU-4 walk finds detection WITHOUT window containment, the absorption site re-opens as a window-bookkeeping defect, not as an (E-N) consumption |

**D-12r relocated [REVISION 14, r15 — executing VC16 finding 2]:** TB-CAP's
(τ-irr) completeness perimeter (the D4R.0-K(c) verdict-pin residual) is NO LONGER
[M]: the dual-accepted reduction confines its residue to the two literature cites
(HEN-LIFT)/(OM-SAT), both pinned-confirmed on file, the Hensel uniqueness gap is
handled by the displayed companion lemma, TB-CAP(irr) is PROVED at [3t] from the
accepted §C rev-14 Theorem C, and every sibling dependency is decomposed into its
existing priced home — so under this ledger's own definitions no D-12r-specific
open mathematics remains. It now lives in **§4** as the D-12r literature-pin
entry (the [T]/LITERATURE class), consumption sites and dependency-decomposition
routing preserved there verbatim; (H3) = {(ADM), (E-N)} since this revision. The
REVISION-13 rewrite's provenance record is §19's r14-6 row; this tombstone keeps
the one-home trail.

**(H8) = (K-CUT) retired [REVISION 16, r17 — the round-3 discharge gate]:** the
shallow-cut decomposition kernel (NEW at REVISION 10, r11, from O-11 owed-pass
finding G-2; REDUCED at REVISION 13, r14) is NO LONGER a §3.1 [M] row: its
consumed statement — exactly-one shallow decomposition + exhaustive-exclusive
read ownership + the weight-preserving product μ(fiber T) = W(Ŝ)·∏_i weight(D_i),
as M04 Lemma 3.3's hypothesis enters Step 18's K1 — is machine-proved Lean-core
in the exact-form K1 convolution shape (`kcut_reduce` + `kcut_k1_form` +
`massFactor_mass_eq`, `Scaffold/HDischarge/H8/`), the 3-round unit-level
hostile-pass arc closed at DISCHARGES (`KCUT_pass3_2026-08-06.md`).
[CORRECTED at REVISION 17, r18, executing VC19: the r17 claim that every
remaining binder row was "TYPED and owned by an already-displayed home"
OVERSHOT — the cited owner statements do not house the three interfaces. The
retirement of the PACKAGE stands; the residues are RE-PRICED as the NEW
slimmed row **(H8′)** above (GRAFT / MASS-ID / POOL-DICT), the former owner
labels kept as retirement ROUTES.] Full record — the package pointers, the
pass arc, and the three residues — at the **§3.2 K-CUT retirement row**. The
historical letter (H8) is KEPT: the roster reads **(H1)–(H9) minus the retired
(H4a), (H8), and (H9), plus (H8′): NINE rows** [count synced at REVISION 19, r20 — the
(H9) retirement; and at REVISION 22, r23 — the (H4a) retirement]. The H8-R1 charge-(vi) adjudication STANDS unchanged ((vi-a)
FAIL — the O-11 r3 serving map "CUT-WD ← D-14 + CU-3" is still NOT re-pointed
as demonstrated); this retirement rides the LATER-EARNED ground (the proved
package + the 3-round pass arc, per the ledger's "KCUT ROUND 3" entry), not
that route.

**(H9) = (NSF) retired [REVISION 19, r20 — the THIRD post-acceptance row
retirement]:** the ns-routing interface row (NEW at REVISION 10, r11) is NO
LONGER a §3.1 [M] row: BOTH members are retired — **(NSF-KEY)** at REVISION 13,
r14 (Unit C executed at the on-record sign-off; the six (NSF-KEY-RET) artifacts;
compiled witness H9-U1) and **(NSF-INT)** at ledger entry **H9-C3** (2026-08-02,
commit 9abc3ab) per (NSF-INT-RET)'s four grounds, ALL on file: I-1 (the D-11 r4
§4B leaf proof) + I-2 (the closed {K1, K4} site list, (CH-RES)-relative) + I-3
(the r4 pass-2 ACCEPT of §4B as a conditional proof) + the **(K1-CANON)
discharge** (the (ed-1) M04 §5.5 scope pin — K1's quantifier + the post-K4 sum
pinned to (q-4)'s notion, preamble line, Lemma 3.3 untouched; the standing
statement-change authority, SURFACED for review; warrant note
`lean/notes/openmath/K1CANON_discharge_2026-08-02.md` + the 2-round hostile
pass, substance PASS both rounds). Full record — both members' provenance, the
executed retirement, the bounds — at the **§3.2 H9 = (NSF) retirement row**.
**RE-OPEN TRIGGER, displayed:** the D-11 leaf bar still owes one confirming
clean pass (the §3.2 D-11 row); a §4B-defective finding re-opens I-3 and
SUSPENDS the (NSF-INT) retirement. The historical letter (H9) is KEPT: the
roster reads **(H1)–(H9) minus the retired (H4a), (H8), and (H9), plus (H8′):
NINE rows** [count synced at REVISION 22, r23 — the (H4a) retirement].

**(H4a) = (R1)–(R4) retired [REVISION 22, r23 — the FOURTH post-acceptance row
retirement (D-12r at REVISION 14, (H8) at REVISION 16, (H9) at REVISION 19 the
first three); blueprint HDISCHARGE_H4 §1 clause F-(iv), fired per H4M7 §§2–3]:**
the T-1 stage-tower laws over O_δ = W(F_{p^δ}) (O-8b's base) are NO LONGER a
§3.1 [M] row: the base is **WRITTEN AND PASSED** — `H4M1_rpack_attempt.md`
((R1)–(R3) over W(F_q), ONE theory, δ absolute; ACCEPTED, passes 2+3 CLEAN
model-diverse) + `H4M2_dict_attempt.md` ((R4) via the factor-correspondence
dictionary, `stable_iff_R4`; DONE, pass 5 CLEAN on its REVISION 4) — and the
composite **P(O-8b/M14) is ACCEPTED**: after the M14 REVISION-1 Lemma-1(b)
display fold (the pass-2 gap cured), passes 3 (Codex, 0C/0G) + 4 (Fable
decider, nothing classifiable) ran consecutive CLEAN, model-diverse (ledger
2026-08-08 "P(O-8b/M14) COMPOSITE ACCEPTED", commit 5a10256; H4M7 §2's firing
condition met). The **(K3-δ) audit note SURVIVES this retirement** per the
pass-1 ledger pin — it MOVES to the (UB-X) row above, and clause (R) STILL
consumes (K3-δ) until the H4M7 §4 Route-B re-scope fires (five conditions
incl. Asvin sign-off; nothing about (K3-δ) is discharged here). Steps 16/18's
K3-c displays upgrade the (R1)–(R4) leg to PROVED-VERIFIED at the ACCEPT
record; clauses (R)/(UB) each LOSE (H4a). Full record — the F-chain
provenance, the pass arc, the displayed bounds — at the **§3.2 (H4a) =
(R1)–(R4) retirement row**. The historical letter (H4a) is KEPT: the roster
reads **(H1)–(H9) minus the retired (H4a), (H8), and (H9), plus (H8′): NINE
rows**.

**(AGR) relocated [REVISION 1, finding 10]:** the drainage agreement seam
(= OL1-ii at general n: canonical termination + acceptance below a finite N(f)) is
ASSEMBLED from CU-2 + CU-4 + GMN Cor 4.19 — all on file — so under this ledger's own
definitions it is NOT [M]: no new mathematics is missing. It now lives in §3.2 as the
inherited-[V] row it is; general-n drainage stays conditional until those passes land
(n ≤ 3 unconditional, O4T Thm N3).

### 3.2 Verification-pass debt [V] — proofs on file, passes owed (named)

| item | what is owed | owner |
|---|---|---|
| **P(O-2a r5)** | fresh hostile pass on O-2a rev 5 (core thrice confirmed; V25 arc). The pass charge now ALSO carries the CU-2t R3 delivery [REVISION 4, CU-2t §9.5 touch (5)]: the W3 carrier finding + the 2(D)-scope dichotomy — decide whether Theorem 2(D) as printed asserts continuing-part or full-history 𝔈-membership at terminal-carrying emissions, and repair 2(D) first if the latter | O-2a (Step 7) |
| **P(O-1thr r4)** | fresh hostile pass on O-1thr rev 4 (V24's criticals repaired; sloped/sheared pin stands) | O-1thr (Step 6) |
| **P(O5t r3)** | fresh hostile pass on O5triple rev 3 (V23 0-critical arc) | O5triple (Step 12) |
| **P(O-8b/M14)** [NEW row at REVISION 3, finding B2] | fresh hostile pass on M14's conditional O-8b proof (on file; the alphabet law at δ-stable position classes over (R1)–(R4)) — the one consumed PROVED-UNVERIFIED proof whose pending pass was named nowhere; it sits on the mandatory path of clause (R) (Step 18's K3-c), not just (UB). **[REVISION 21, r22 — the pass record:** FIRED as the composite M14 + H4-M1 + H4-M2 per the H4M7 §2(iii) charge. Pass 1 RAN 2026-08-02 (ledger entry, commit 4aae1b5): CLEAN, 0 critical, all six charge targets walked — but PRE-F-(i) (the component gates were then unmet). Pass 2 RAN at THIS revision (Codex, fresh context, on the CURRENT M1-ACCEPTED/M2-REVISION-4 texts; report `lean/notes/openmath/PO8bM14_pass2_2026-08-02.md`): **GAPS-ONLY, 0 critical / 1 gap** — M14 Lemma 1(b)'s top-space scale display stale against M1's accepted two-case γ* display; targets (b)–(g) otherwise CLEAN. NOT yet ACCEPT (consecutive-clean bar): owed = the M14 Lemma-1(b) sync fold + one clean pass on the folded text — see the §3.1 (R1)–(R4) row's F-chain record]** **[REVISION 22, r23 — THIS ROW IS DISCHARGED: the M14 REVISION-1 fold landed (the pass-2 gap cured; echo audit clean), then pass 3 RAN (Codex, fresh context, on M14 REVISION 1): **CLEAN, 0C/0G**, full walk-record incl. the cured itemization; pass 4 RAN (Fable decider, `PO8bM14_pass4_2026-08-02.md`): **nothing classifiable** — the synced Lemma-1(b) display walked verbatim against M1 §5.3, Lemma 2 re-derived end-to-end, both F₉/δ contrasts re-derived by hand, numerics grep-verified empirical-cover-only. Two consecutive CLEAN composite passes, model-diverse — **ACCEPT per H4M7 §2** (ledger "P(O-8b/M14) COMPOSITE ACCEPTED", commit 5a10256). F-(iv) fired at this revision; the record's home is now the (H4a) = (R1)–(R4) retirement row below]** | O-8b/M14 (Steps 16, 18) |
| **P(O-14a′ r3)** [NEW row at REVISION 9, VC10 advisory 1 — grade-display conformance, post-acceptance] | the O-14a′ (RunRealizerExists) verification record, displayed in the §0 vocabulary. ON RECORD, exactly: core Theorems 1–3 STABLE across the V11 and V15 passes (rev 3 on file); the consumed instance RRE(p, ZMod p) FORMALIZED Lean-core (`MovesD/R7_runRealizer.lean`, `runRealizerExists_zmod`); the CUC §0 "VERIFIED homes (no pass condition)" registry line as provenance. OWED: the in-document display of the V11/V15 arc's models and pass-cleanliness (or one fresh named hostile pass) — until then the leaf is graded **PROVED-UNVERIFIED with this named pass row**, NOT VERIFIED: the §0 bar (≥ 2 consecutive clean passes, ≥ 2 models) is not checkable from this document's four corners. This resolves the VC10-noted asymmetry precedent (O-2a's core is thrice confirmed yet carries P(O-2a r5)): every consumed PROVED-UNVERIFIED leaf now carries a named §3.2 pass row, O-14a′ included — the §§13/14 invariant is checkable in-document | O-14a′ (Step 9) |
| **D-11 verification cycle (at r4 REVISION 3)** [synced at REVISIONS 1/3; clause 4 carved out at REVISION 4, finding VC4-3; the pass-3-owed disposition RETIRED at REVISION 10, r11 — replaced by the two-pass r4 record] | pass 1 NOT ACCEPTED as billed (2587c70) → r2 (566b967) → pass 2: 2 criticals, both CONTAINED at r3 (8a0786c) → **pass 3 RAN: CRITICAL** (`D11_owedpass_2026-08-03.md`, 6 crit/3 gap — the STOP-THE-LINE event, ledger 44ce0a7) → the five-round repair design (`R11_REPAIR_DESIGN_V5_2026-08-04.md`) → **r4** (`D11_rev4_2026-08-04.md`: true-scope repairs + the obligations (b-EX)/(O-1)–(O-5)/(O-GLOB)/(ROW-Σ) written out + the (CH-COV)/§2A.4 audits + the §4B (NSF-INT) leaf proof) → **r4 pass 1: CRITICAL** (`D11r4_pass1_2026-08-04.md`, 3 crit/3 gap; folded at the leaf's REVISION 2) → **r4 pass 2: GAPS-ONLY** (`D11r4_pass2_2026-08-04.md`: NO critical remains; all six pass-1 repairs adjudicated VALID; the design-§7 obligations adjudicated AS PROOFS; 3 bookkeeping gaps folded at the leaf's REVISION 3). OWED NOW: one confirming clean pass on the REVISION-3 text toward the leaf's own two-consecutive-clean bar (pass 2's gaps were folded AFTER it, so the leaf bar is not yet met) — since REVISION 19, r20, this owed pass IS the (H9) retirement's displayed **RE-OPEN TRIGGER**: if it finds the §4B leaf proof defective, I-3 re-opens and the (NSF-INT) retirement SUSPENDS (the H9 retirement row above). **Scope of this [V] row, exact:** it prices the passes on D-11's ON-FILE proof layers (M1 families/M1′/M3/M2-at-(RE)/M2.0/(ROW-Σ)/(b-EX)/(O-GLOB) at their displayed conditionality) ONLY — clause 4 (the actual-pack correspondence) and the (RE) profile of the consumed member ((PACK-iv)) have NO proof on file and are NOT covered here: they are the §3.1 [M] row (PACK) = (ROOT-C)(H5); the (H9) members' record sits at this table's H9 = (NSF) retirement row (both members retired at REVISION 19, r20 — never covered here) | D-11 (Steps 17/18b) |
| **O-11 verification cycle (at r3)** [synced at REVISIONS 1/3; the 2026-08-03 un-banked run RECORDED at REVISION 12, finding VC13-4] | pass 1 NOT ACCEPTED AS STATED (a7ede72) → r2 (1bd8bd1, D-a/D-b/D-c declared + oracle-pin supplement) → pass 2 (V32): billing precision → **r3** (7aef1b9: tier 2 split into hypothesis-grade rows H-1..H-4; walk/cores byte-stable) → **an O-11 hostile pass RAN (2026-08-03, the owed-pass campaign; `lean/notes/openmath/O11_owedpass_2026-08-03.md`): verdict CRITICAL — adjudicated NOT BANKED at the report itself ("O-11's r3 counter does NOT advance toward its bar on this pass")**; its three findings: **G-1** (CRITICAL — Theorem O11 as displayed proves only the n ≥ 2 restriction of the advertised for-every-n target row; n = 0, 1 untreated), **G-2** (GAP — the CUT-WD serving map ← D-14 + CU-3 is a correspondence claim, not a demonstrated implication: overlapping ownership, an unowned read, or two different shallow decompositions are unexcluded — THE finding priced as the §3.1 **(H8) = (K-CUT)** row at REVISION 10, r11 — that row RETIRED at REVISION 16, r17, into the §3.2 K-CUT retirement row; its residual interfaces re-priced as §3.1 (H8′) at REVISION 17, r18), **G-3** (GAP — the "RESUM-n3 65/65 end-to-end" rederivation claim unsupported beyond the five displayed identities). This row's former "pass 3 owed" tail did not record that run, leaving the (H8) row's "owed-pass G-2" provenance irreconcilable in-document (finding VC13-4); the BANKED pass 3 — one advancing the r3 counter, on text repaired for G-1/G-2/G-3 — stays owed | O-11 (Step 18) |
| **K-CUT RETIREMENT ROW — the retired (H8) package + the moved residues** [NEW row at REVISION 16, r17 — the SECOND post-acceptance row retirement (D-12r at REVISION 14 the first); the former §3.1 (H8) row retires HERE on the round-3 discharge gate] | **THE PACKAGE** (all sorry-free, Lean-core axiom footprints; `lean/LeanUrat/Scaffold/HDischarge/H8/`, blueprint `lean/blueprints/HDISCHARGE_H8.md`; ELEVEN Lean units + 2 documentary units): the ownership partition (`firstEntAbove_unique`, `read_cut_exhaustive`, `shallow_block_disjoint`, `block_block_disjoint`, `bdy_display` — exhaustive-exclusive ownership PROVED outright at the ownership-partition level), the level-(1b) forward extraction (`cutOf`, `cut_injective` + conformance + countability transport), the count/series regroups incl. M04 Lemma 3.3 (`seriesSum_cut`/`seriesSum_cut_conv`, `siteExp_cut`, `fiber_count_cut`), the conditional consumer bridge `kcut_reduce`, the factorwise mass law **`KCutMassFactor`** (`massFactor_mass_eq` PROVES factorwise ⇒ the aggregate `mass_eq`), the exact-form K1 tie **`KCutK1Tie`** (`kcut_k1_form` PROVES Step 18 K1's displayed transported-pool convolution from `kcut_reduce` + tie/support/injectivity; `kcut_k1_form_of_massFactor` wires C2 into C3), and **THE REPAIRED ADJUDICATION DISPLAY** (`Scaffold/HDischarge/H8/R2RootSync.lean` §§C–G, the display of record; `R1Adjudication.lean` the charge-(vi) record). **THE 3-ROUND PASS ARC**: round 1 CRITICAL — 7 crit + 5 gap, ALL display-layer, the Lean declarations audited FAITHFUL (`lean/notes/openmath/KCUT_pass_2026-08-05.md`) → displays repaired + the two missing content units C2/C3 specced, landed, proved (commit bfe0a85) → round 2 CRITICAL on display STALENESS only, content confirmed proved + drift-free (`KCUT_pass2_2026-08-05.md`) → displays re-synced → **round 3 DISCHARGES** (`KCUT_pass3_2026-08-06.md`; its scope line VERBATIM: "This discharges the Round-3 review gate—not the remaining semantic owner rows and not the unconditional ROOT theorem"). **WHAT RETIRED**: the separate §3.1 [M] pricing of the WHOLE cut decomposition — the row's consumed statement is proved at the consumption site CONDITIONAL on the three typed residual interfaces (GRAFT / MASS-ID / POOL-DICT), which are PRICED AT the §3.1 row **(H8′)**; the owner arcs are retirement ROUTES only, NOT homes, and a standing full-strength (H8) row would double-price content that is either machine-proved or priced at (H8′) [REWRITTEN at REVISION 18, r19, executing VC20 blocker 2 — the r17 clauses "owner-displayed interfaces" / "priced at its owners" are STRUCK as incompatible with the VC19 adjudication]. **THE RESIDUES — NOTHING DISCHARGED; PRICED at the §3.1 row (H8′) since REVISION 17, r18** [executing VC19: the owner labels below are retirement ROUTES, not housing statements — VC19 found no cited owner statement states these interfaces, so the r17 "MOVED to their owners" framing OVERSHOT; the residues' priced home is now (H8′)] (C2/C3 are "UNPINNED assumption packages, not semantic discharges" — the round-3 adjudication's own classification; no display may cite this row as discharging any of them): **(i) graft completeness = (H8′)(i) GRAFT** (`graft`/`graft_cut`/`cut_graft` — full decomposition bijectivity/realizability; continuation memorylessness is its semantic core) → retirement ROUTE: a future completeness theorem on the **D-14 + CU-3 + REL.2 arc** (Steps 6/11 homes and their debts; the H8-R1 charge-(vi) record STANDS — (vi-a) FAIL, (vi-b-1)/(vi-b-2) NO: the O-11 r3 serving map "CUT-WD ← D-14 + CU-3" is NOT re-pointed as demonstrated; the package retirement rode the proved package, not that route — and (vi-a) FAIL stands as the (H8′)(i) member's status); **(ii) measured-mass identification = (H8′)(ii) MASS-ID** (the MEASURED semantics of C2's `shMass`/`blkMass` — factorization forbids only slot-dependent compensation, not mis-identified block weights) → retirement ROUTE: the **(H7)/sibjc TREE-EXP + [2r]/CL-8 development + the O-1/O-3 count-tie** (§3.1 (H7) + the §3.4 item-9/10 homes); **(iii) the τ/δ pool dictionary = (H8′)(iii) POOL-DICT** (C3's pool rows have NO typed node-level read-off beyond the pinned `e_key`; `vBridge` constrained only by keying/injectivity/support; `blockTotal_tie`'s instantiation) → retirement ROUTE: the **REL.2 pool dictionary ([2r]/CL-8, open)** + the graft route's continuation memorylessness; the deliberately-absent W-resummation leg → **CL-17(ii)/W17ii** (§3.4 item 12, `w17ii_pin` proved). Standing at their prior homes (not moved by this row): the entrance-predicate cell-data supply, wholly with **CTS-M(i)/(iii)/CL-13**, the (H1)-adjacent census rows (`supplyDec` classically vacuous, consumed by no proof); the `real`/`vOf`/`verdict_key` realization/keying rows, **TREE-N/[3t] seam territory** (§3.4 item 10 / Table B `series_tie`). Step 18's K1 (clause (R)) and the transported chain (clause (UB), q ↦ q^δ) now consume the package + the §3.1 (H8′) row [synced at REVISION 17, r18] | the retired H8 arc (record only); the residues: the §3.1 (H8′) row (their retirement routes displayed here) |
| **H9 = (NSF) RETIREMENT ROW — the ns-routing interface row, BOTH members retired** [NEW §3.2 row at REVISION 19, r20 — the THIRD post-acceptance row retirement (D-12r at REVISION 14, (H8) at REVISION 16 the first two); the former §3.1 (H9) row (NEW there at REVISION 10, r11) retires HERE on ledger entry **H9-C3** (commit 9abc3ab); the TWO members retired SEPARATELY — neither retirement discharges the other: (NSF-KEY) at REVISION 13, r14, per the H9 lead's ledger adjudication; (NSF-INT) at H9-C3, executed at this revision. **RE-OPEN TRIGGER, displayed:** the D-11 leaf bar still owes one confirming clean pass (this table's D-11 row); if that pass finds §4B defective, I-3 re-opens and the (NSF-INT) retirement SUSPENDS] | THE TARGET (the row's conditional statement): at any instantiation satisfying (q-4)'s interface pin (the ClassifierSpec docstring: `some σ` = complete finite realizable tree, all leaves (τ-irr)/(τ-hen), certified at level N), every K1/K4-labeled box lies in the decided-ns-free counted corpus and D-15's exact-classification package applies with no a.e. weakening — the round-3 attempt to PROVE this by chaining displayed quotes was refuted and stands WITHDRAWN; the two members, retirement records kept in full: **(NSF-INT) — the interface implication chain (pin → predicate → corpus) — RETIRED [REVISION 19, r20; ledger H9-C3; executed status].** At every consumed site of Step 18's K1/K4 chain: (i) irr/hen-terminal completeness excludes ns-marks — a complete finite realizable tree with all leaves (τ-irr)/(τ-hen) carries no ns-marks (in particular the Hensel payload carries none) — so every box labeled under (q-4)'s pin fibers a tree that is decided ns-free in (q-1)'s sense; and (ii) M04's "complete-finite-realizable" scope coincides with the guarded TREE-N corpus ((q-3)'s all-leaves-(τ-irr)/(τ-hen), `hdet`-guarded trees) at every consumed site. The interface quotes (q-1)–(q-6) are CONTEXT, not proof. **Displayed premises housed IN this member (one-home; the D-11 r4 passes surfaced them):** the §1.4 marks-vocabulary premise (a mark is the RECORD of an NsHalts event — §1.4's sole generation rule; claim (i)'s premise) and **(K1-CANON)** — at every consumed Step-18 site, K1's "canonical tree" denotes (q-4)'s pinned notion; equivalently (the interface-lemma form), every tree at which the mandatory path invokes K1 satisfies the all-leaves-(τ-irr)/(τ-hen) guard [the r4 source-inheritance inference — that K1's bare quantifier carries the guard FROM its cited sources — is WITHDRAWN (r4 pass-1 gap 5): a theorem's quantifier does not inherit its source lemmas' restriction, and the `hdet` guard scopes one machine-checked candidate, not K1]. The D-11 r4 §4B leaf proof (artifact I-1) proves (i) from the §1.4 displays + the marks-vocabulary premise and (ii) site-by-site over the closed I-2 list {K1, K4} — the K4 leg at the pin directly, the K1 leg UNDER (K1-CANON). **(NSF-KEY) — as-built keying conformance [ONE proposition] — RETIRED [REVISION 13, r14; executed status].** At the wired instance: for every n, p, level N, box f, and σ — if the wired classifier labels f (`bridgeCanonical … N f = some σ`) then f fibers a carried tree of the σ-carrier satisfying `MovesT.NsFree` (no ns-marked chain) at threshold ≤ N. RETIREMENT PROVENANCE (historical — the pre-Unit-C record, re-labeled at r14, kept so the repair's necessity stays visible): the member was **KNOWN FALSE AT HEAD** from the M02 record — the exact-square poison V₀ (ns-marked, self-realized, carried, because the PRE-REPAIR `bridgeTree` carried no ns-free conjunct) was labeled "totally split" by `bridgeCanonical`; c_{V₀}(N) = p^{N−2} against (SCALE)'s forced p²-per-level growth — `count_tie` as then typed REFUTED, `canonical_stable` refuted at the intended instance, `VPSound` unsatisfiable; an obligation certain to fire, which fired. EXECUTED (Unit C, the sanctioned mechanism — M02's "Minimal repair" — landed at HEAD under the on-record sign-off): the carrier is now the GUARDED form, verbatim `{V // V.typemult = σ.1 ∧ RealizedSelf n p V ∧ MovesT.NsFree V}` (`BridgeD3_treeCarrier.lean:94–96`; commits 322ec54 + dfdb558); V₀ DEAD (`bridgeVt_nsFree` compiled, consuming wiring recompiled); `count_tie` sorry-free at the guarded corpus (the hdet scope); `bridgeCanonical_stable` re-proved green; the three statement texts byte-identical (re-scoped THROUGH the carrier only). THE SIX (NSF-KEY-RET) ARTIFACTS, all on file: R-0 Asvin sign-off (ledger verbatim) · R-1 the conjunct diff + the closed ripple enumeration (`UNITC_ripple.md`) · R-2 byte-identical statement texts · R-3 the three per-statement validations · R-4 `lake build` green (8579 jobs) + AxChk census identical (740 footprints, 0 sorryAx) · R-5 fresh-context Codex diff confirmation (`UNITC_diff_confirm.md`, VERDICT: CONFIRMED). Adjudication of record: ledger entries "UNIT C EXECUTED" + "H9 LEAD ADJUDICATION"; compiled-proposition witness **H9-U1** = `Scaffold/HDischarge/H9/NsfKeyProp.lean` (landed green, Lean-core — a WITNESS of the retired state, carrying no retirement weight of its own). Bounds inherited from the adjudication: no carrier non-vacuity claim (compiled existence of a carried tree stays OPEN at the realization seam); the BridgeD16/TreePin.vt_surj completeness residue stands (Phase-B queue). One-home argument: both members are the ns-routing face of the SAME classifier interface — (NSF-INT) the interface/paper side, (NSF-KEY) the as-built/Lean side; same seam (K1/N2 label routing at Step 18), same owners, same consumption chain (the VC7-2 one-home precedent) | the O-1thr/M04/M02 classifier-interface arc. RETIREMENT, exact: **(NSF-INT)** by (NSF-INT-RET) — I-1 (the §4B leaf proof, ON FILE) + I-2 (the closed site list {K1 (M04 §5.5 (K1) + Lemma 3.3), K4 (M04 §5.5 (K4) + D-15)}, closed by the §4A walk relative to (CH-RES)) + I-3 (the r4 hostile pass's ACCEPT of §4B as a proof — GRANTED at the r4 passes: pass 2 adjudicated the conditional argument VALID) **+ the (K1-CANON) discharge, EXECUTED** (leaf-external, exactly as owed — r4 pass-2 gap 3 had ruled that I-3's accept of a CONDITIONAL proof verifies only (K1-CANON) ⇒ the K1 scope conclusion and cannot itself establish (K1-CANON): the discharge landed as the (ed-1) M04 §5.5 scope pin — K1's quantifier gains the (q-4) scope parenthetical, Theorem 4's post-K4 sum display is pinned to the SAME corpus, and a §5.5 preamble line records the pin; Lemma 3.3 untouched — executed under Asvin's STANDING statement-change authority and SURFACED for review, warrant note `lean/notes/openmath/K1CANON_discharge_2026-08-02.md` §D with the 2-round hostile pass — substance PASS on all six charged checks BOTH rounds, bookkeeping findings folded, gates g-A..g-D closed, companion compile gate `NsfKeyProp.lean` EXIT=0) — ALL FOUR GROUNDS ON FILE: **(NSF-INT) is RETIRED** [REVISION 19, r20; ledger H9-C3]; **(NSF-KEY)** RETIRED by Unit C per (NSF-KEY-RET) — EXECUTED, the six artifacts R-0..R-5 on file (never by any leaf text; H9-U1's compiled proposition is corroboration, not a second retirement route) [synced at REVISION 13, r14] | Enters Step 18's K1/K4 chain and Step 17's M3 leg (via Steps 11/12) — since REVISION 19, r20, consumed there as the RETIRED target at the pinned scope: clauses (R)/(UB) no longer carry an (H9) hypothesis. NOT (0)/(SQ). Two-channel consumption (D-11 r4 §4A, outcome CLEAN at the walked closure, residue (CH-RES)): (ch-1) label-blind exact combinatorics; (ch-2) mass-mediated semantic tie at Step 19's squeeze. Display rule [updated at REVISION 19, r20 — the former closing rider "the target must NOT be displayed as discharged while (NSF-INT) stands priced" is STRUCK: both members are retired, so the target IS dischargeable and DISCHARGED at the pinned scope]: the target holds at the INTENDED/PINNED instantiation SUBJECT TO the displayed bounds, homes unchanged — the I-2 site closure is (CH-RES)-relative; no carrier non-vacuity claim (realization seam); the BridgeD16/TreePin.vt_surj completeness residue stands (Phase-B); the marks-vocabulary premise stays CARRIED (grounded in (q-1)'s display; compiled conformance face H9-U4; not separately owed per (NSF-INT-RET)) — and SUBJECT TO the RE-OPEN TRIGGER (the D-11 leaf bar, the header bracket) |
| **(H4a) = (R1)–(R4) RETIREMENT ROW — the O-8b base package, WRITTEN AND PASSED** [NEW §3.2 row at REVISION 22, r23 — the FOURTH post-acceptance row retirement (D-12r at REVISION 14, (H8) at REVISION 16, (H9) at REVISION 19 the first three); the former §3.1 (R1)–(R4) row (in the roster from the original composition; = (ROOT-C)(H4a) since the REVISION-2 split, finding V2-2) retires HERE on blueprint HDISCHARGE_H4 §1 clause **F-(iv)**, fired per H4M7 §§2–3 at the P(O-8b/M14) composite ACCEPT] | **THE TARGET (the retired row's statement):** the T-1 stage-tower laws re-proved verbatim over O_δ = W(F_{p^δ}) — O-8b's base, consumed at Step 16 (the alphabet law: F_{p^δ}-linearity of the re-based slot map + a_δ = δ·a₁ at δ-stable position classes) AND at Step 18's K3-c (the base-changed β-legs at δ > 1 pools; O-11 r2's declared row D-c — NOT clause-(UB)-only, finding V2-2). **THE F-CHAIN, complete (provenance):** **F-(i)** — `H4M1_rpack_attempt.md` ACCEPTED (passes 2+3 CLEAN, model-diverse: (R1)–(R3) over W(F_q), ONE theory, δ absolute; the D.10 recentering leg walked at its rev-2 §3.5) + `H4M2_dict_attempt.md` DONE (5-pass arc, pass 5 CLEAN on its REVISION 4: (R4) via the FACTOR-CORRESPONDENCE dictionary, `stable_iff_R4` displayed) + H4-M4 rev 4 (the Route-B adjudication, the (K3-δ) side) + H4-M5 textual bar MET (passes 6+7 CLEAN, model-diverse) + numerics N-T4 ALL PASS and N-K3 ALL GREEN (376/376, 0 violations, commit 5fdf350; PARI oracle clean) — **SIX records** at the "records" unit: H4-M1 · H4-M2 · H4-M4 · H4-M5 · N-T4 · N-K3 [count display added at REVISION 23, r24 — VC25 gap 2: prior displays said "five", the ledger-summary under-count that folded the two numerics records into one entry]. **F-(ii)** automatic (M14's conditional proof on file). **F-(iii)** — the composite P(O-8b/M14) = M14 + M1 + M2: pass 1 CLEAN (4aae1b5, pre-F-(i)); pass 2 GAPS-ONLY 0C/1G (`PO8bM14_pass2_2026-08-02.md` — the M14 Lemma-1(b) top-space scale display stale vs M1's accepted two-case γ* form) → the gap folded at **M14 REVISION 1** (c152602/fb59459: the two-case display carried verbatim-in-content, the top-space instance routed through Lemma 1(a); echo audit clean, the H4M5 abridged quote flagged not edited); pass 3 CLEAN 0C/0G (Codex, fresh context, on M14 REVISION 1 — full walk-record incl. the cured itemization); pass 4 CLEAN (Fable decider, `PO8bM14_pass4_2026-08-02.md` — nothing classifiable; Lemma 2 re-derived end-to-end; four sub-bar observations recorded) = two consecutive CLEAN, model-diverse — **ACCEPT per H4M7 §2** (ledger 5a10256). **F-(iv)** — executed at THIS revision: the §3.1 row retires here; Steps 16/18's K3-c (R1)–(R4) leg upgrades to PROVED-VERIFIED at this record; clauses (R)/(UB) each LOSE (H4a). **WHAT DID NOT RETIRE:** the **(K3-δ)** audit note SURVIVES (moved to the §3.1 (UB-X) row per the pass-1 ledger pin "retires (H4a) mod (K3-δ)") — clause (R) STILL consumes (K3-δ), and the H4M7 §4 Route-B re-scope (five conditions incl. Asvin statement-change sign-off + the consumer-need walk; M4/M5/N-K3 legs green, the rest outstanding) is its separate, ungated-here disposition. **DISPLAYED BOUNDS carried (the composite's own conditionality, never silently consumed):** M1's §0.4 perimeter fence (recipe-presented towers with (I-aug) reads; the run-production layer D.8/D.10 stays REL.1's wider walk — M14 consumes a presented matched prefix, shape-consistent); M2's DictModel grammar rows + `GcdSlice` [M]-rows awaiting the H4-F6 carriers, with DICT-0's four [IN-C] clauses displayed open at consumer strength; the [IN-U] MacLane pin-precision = LITUNIT duty; the counting layer NOT consumed (M1 §7 check). **Scaffold face:** `RootHyps.h4a_r1r4` stands; its eventual field refinement targets the H4-F6 four-law carriers, NEVER `H4aConsumptionFace` (H4M7 §3 item 4 / blueprint L.3 warning) — a BP_VI/fenced-H4-F3 move, not this row's | the retired H4a arc (record only); the surviving (K3-δ) fragment: the §3.1 (UB-X) row; the Route-B re-scope queue: H4M7 §4 (Asvin sign-off gate) |
| **(AGR) inherited [V]** [relocated from §3.1 at REVISION 1, finding 10; residue sentence widened at REVISION 12, finding VC13-5] | the drainage agreement seam, ASSEMBLED from CU-2 + CU-4 + GMN Cor 4.19 (all on file); its residue = the pass debts of CU-2/CU-4 (this table) + the Cor 4.19 literature row (§4) + the [M] conditionalities CU-4's own consumption displays — the **D-12r** TB-CAP (τ-irr) perimeter (Step 11 grades CU-4 "TB-CAP (τ-irr) perimeter-conditional"; since REVISION 14, r15, priced at the §4 D-12r literature pin — no longer [M]) and **(E-N)**'s CU-4 touch through Thm 3(c) (the (E-N) row's displayed entry, a §3.1 (H3) member riding its own row) — both already priced at their own named homes, so nothing is hidden — and nothing else [the former "exactly the pass debts + the literature row — nothing else" elided CU-4's displayed §3.1 conditionality; membership home synced at REVISION 14] | O-4/O4T TR-OM (Step 13) |
| **Dfloor 3 minor gaps** | ID reduced-rep, ADD induction coprimality, TRANSFER sum-regularity — ride the next doc touch | Dfloor (Step 1) |
| **O-10 2 minor gaps** [added at REVISION 3 status sync] | the D-8 value-vs-law framing (collapses definitionally) + the box-count transport cite — found at the VERIFYING pass 2; **repaired at O-10 r2, landed at revision close** (3956500: the D-8 separation dropped as definitional with the existence range displayed; the (BOX) transport display + M07 §2.1 cite row; cores byte-stable). Per the wave-14 ledger: VERIFIED (leaf #6) STANDS, framing fixed; the r2 diff-confirmation rides the next harvest | O-10 (Step 15) |
| **B-layer verification rounds** [row added at REVISION 1, finding 3; B-4 added at REVISION 2, finding V2-4; **B-3 (doc level) added at REVISION 3, finding B4**] | B-1, B-2 (doc level), **B-3 (doc level)**, **B-4 (order-r layer)**, B-5, B-6, B-7 are PROVED-UNVERIFIED June documents (grades displayed at Step 2); the tree §4 protocol already schedules their verification-only rounds; B-3's core and B-2's counting core are VERIFIED-LEAN (B-3's doc level has the same status shape as B-2's — Lean-verified core + PROVED-UNVERIFIED June document — and is now listed like it); B-4's order-1 instance = B-3's VERIFIED-LEAN core, and its order-r content (`M6_lemma.md`) has as modern face O-9's OL-B, discharged to GD-3 — on the modern path the orders-≥ 2 content rides §3.1's (GR-B), so this row prices only the June-document verification rounds; B-8 SUPERSEDED, cited nowhere | B-layer (Steps 2, 3) |
| **K-12 faithfulness-audit record** | the Lean `undec`/`hExhaustP` vs mathematical-classifier audit (DELTA-1 §D1.5(g)); grade held pending | K-12/n = 2 anchor |
| **M17/RegPFinite faithfulness-audit record** [NEW row at REVISION 13, VC15 advisory 2 — grade-display conformance, the O-14a′/K-12 precedent] | the Lean-statement-vs-mathematical-statement audit for Step 4's (REG-p) finiteness leaf: the `MovesU/RegPFinite.lean` declarations (`regP_iff_pool_avoids`/`regP_failing_eq`/`regP_failing_finite`/`pow_hits_finite`/`UCarriers.detHyp` + companions, 14 theorems Lean-core) vs M17's mathematical Theorems A/B/C + Corollary U. ON RECORD: dual provenance (Fable proof + independent Codex twin from a neutral brief, load-bearing steps agreeing; `lean/notes/M17_FORMALIZED_2026-07-31.md` incl. its honesty ledger — Theorem C and the cardinality bound NOT formalized; the vacuity note on supplied pinned families). OWED: the faithfulness-audit record itself (the K-12 genre). Until it lands, Step 4's grade is **PROVED-UNVERIFIED** (the exact §0 word, rendered at Step 4 at REVISION 15, r16, VC17 advisory 2), NOT VERIFIED-LEAN — §0's definition requires the audit; Step 5's independent emptiness-at-𝔅_n result contains the impact | K-13/M17 (Step 4) |
| **End-to-end passes on THIS document** [synced at REVISIONS 1/3/4/5/6/7/8/9] | passes 1–8 RAN, each COMPOSITION-GAPS → each repaired by its revision (§§7–14): VC1 (Codex) → REVISION 1; VC2 (Codex) → REVISION 2; VC3 (Fable, model-diverse) → REVISION 3 — VC3 confirmed the composition's MATHEMATICS; VC4 (Codex) → REVISION 4 — again all attribution/ledger integrity (a new hypothesis-grade row surfaced ((PACK)), one attribution weakened favorably ((SQ) unconditional)); VC5 (Codex) → REVISION 5 — two blockers of ONE genre (consumed [T] rows are open propositions: the (H6) promotion) + the standing T-U advisory, with the eight-row mapping, both unconditional clauses, the (ADM) operative form, the (PACK)/(K3-δ) propagation, acyclicity, and the Σ_τ R_τ = 1 derivation all PASSING; VC6 (Fable, model-diverse) → REVISION 6 — 3 blockers, the VC5 genre WALKED TO COMPLETION (the last consumed-open-in-[T] rows: (H1) enlarged by GD-1-beyond-1 + the GD-6 instances; (H7) = (I-τ)/K-LOC) + 6 advisories (incl. A-9: the T-U §U.SQ.6 table now EXECUTED at §3.4), with the nine-row bijection, per-clause attribution, acyclicity, Σ_τ R_τ = 1, (SL≥2), and the leaf-#8 interlock all PASSING; VC7 (Codex) → REVISION 7 — 2 blockers, BOTH attribution/warrant findings at REVISION-6 A-6 adjudications (Step 6's all-order KEY1 consumption had no displayed (H1) member → member (d) added; (PACK)'s correspondence could not warrant the three pack row-laws → the (H5) enlargement PACK-i/ii/iii) + 3 advisories, with VC7's findings 6–11 CONFIRMING the composition (the §3.4 T-U tables genuine incl. the three flagged adjudications; the (H1) enlargement + (H7) sound; the TEN-row bijection correct with per-clause attribution consistent; the walk proving (ROOT-C) quantifier-for-quantifier; acyclicity PASSING); VC8 (Codex) → REVISION 8 — 1 blocker + 1 advisory, the NARROWEST residue of the eight passes, BOTH at the REVISION-7 repairs themselves ((H1)(d)'s "key has degree > 1" quantifier rested on an UNPROVEN identification with the order-≥ 2 reads → the member REQUANTIFIED to read order; the PACK K2/K3 sub-attribution inconsistent across summaries → synced, PACK-i/ii at K2's glue, PACK-iii at K3's ring-hom with D-6), with all three r8 advisory repairs, the TEN-row count, the quantifier walk (Σ_τ R_τ = 1 properly delayed to Step 19), acyclicity incl. 17 ≺ 18 ≺ 18b, and no regression on the §3.4 tables/(H7)/bijection/pack-law non-inference all PASSING. The criterion: ≥ 2 consecutive CLEAN end-to-end passes, ≥ 2 models, verifiers who saw none of the per-leaf rounds (tree §4 Root criterion); after pass 8 the consecutive-clean counter stood at ZERO, with VC9 next (charge at §14). **ACCEPTANCE [synced at REVISION 9]:** VC9 (Codex) and VC10 (Fable — the model-diverse pass) RAN on the IDENTICAL REVISION-8 text and BOTH returned **COMPOSITION-SOUND, ZERO blockers** (`lean/notes/openmath/VC9_root_report.md`, `lean/notes/openmath/VC10_fable_report.md`) — the criterion is MET (two consecutive clean passes, two models, fresh verifiers): the composition is **ACCEPTED at (ROOT-C)'s displayed conditional scope**, and the consecutive-clean counter is **TWO**. VC10's three ADVISORIES (display hygiene only) are dispositioned at §15 (REVISION 9, post-acceptance) — advisories do NOT reset the counter; it STAYS TWO. **SUSPENSION + REVISION 10 [r11, 2026-08-04 — the CURRENT standing]:** the acceptance was **SUSPENDED** at the stop-the-line (ledger 44ce0a7): the owed D-11 pass 3 returned CRITICAL and Steps 17/18b consumed the refuted layers AS PROVED. REVISION 10 (§16) CURES the composition-level defect (the repaired r4 leaf consumed at true scope; every missing premise a displayed [M] member; TWELVE rows ↔ (H1)–(H9)) — but a suspension cure is a TEXT CHANGE: **the consecutive-clean counter is ZERO of 2**, the acceptance is NOT in force, and it is re-earned ONLY by two fresh consecutive clean model-diverse end-to-end passes on the IDENTICAL REVISION-10 text (fresh verifiers, tree §4 Root criterion). **VC11 [synced at REVISION 11, r12]:** the pass RAN on the REVISION-10 text (`lean/notes/openmath/VC11_report.md`) — **COMPOSITION-GAPS** (4 blockers + 1 advisory, ALL r11 RIPPLE-SYNC defects: Steps 16/19's scope displays + the final ∎ stale at (H7)/PACK-i-iii, §3.4 item 12's routing contradicting the (H8) row, the 6(iii)–(ix)+7 residue list omitting (H9), Step 18b's Consumes summary omitting (ADM)) — with the TARGETED r11 repairs CONFIRMED (Step 17's per-clause measures, Step 18b at (RE)/(PACK-iv), the twelve-row bijection, (NSF-KEY) known-false-at-HEAD with gated retirement, (0)/(SQ) unconditional, 17 ≺ 18 ≺ 18b) → REVISION 11 (§17), pure display syncs; a text change: **the counter STAYS ZERO of 2**. **VC12 + VC13 [synced at REVISION 12, r13]:** VC12 RAN on the REVISION-11 text (`lean/notes/openmath/VC12_report.md`) — **COMPOSITION-SOUND, ZERO findings**: the counter reached ONE of 2; VC13 (Fable — the model-diverse pass) RAN on the same REVISION-11 text (`lean/notes/openmath/VC13_fable_report.md`) — **COMPOSITION-GAPS** (2 blockers + 3 advisories, ALL the stale-hypothesis-enumeration genre: §1's (H5) display at PACK-i-iii only, Step 18's stale "— §1" clause-(R) quote + five-hypothesis count, Step 17's Consumes omitting (H9)/(PACK-iv), the (H8) "owed-pass G-2" provenance irreconcilable with the §3.2 O-11 row, the (AGR) residue eliding CU-4's [M] conditionality — the walk/bijection/seams/acyclicity/(NSF-KEY) discipline all CONFIRMED) → REVISION 12 (§18): pure display syncs + the GLOBAL ENUMERATION AUDIT (every operative hypothesis-enumeration display checked against §1 and synced); **the counter RESETS to ZERO of 2**; VC14 next (charge at §18). **VC14 + VC15 — RE-ACCEPTANCE [synced at REVISION 13, r14]:** VC14 (Codex, fresh context; `lean/notes/openmath/VC14_report.md`) RAN on the REVISION-12 text — **COMPOSITION-SOUND, clean** (the enumeration-audit claim TESTED and held; zero regressions): counter ONE of 2; VC15 (Fable, fresh context — the model-diverse, deciding pass; `lean/notes/openmath/VC15_fable_report.md`) RAN on the same REVISION-12 text — **COMPOSITION-SOUND, ZERO blockers** (explicit null result on the blocker hunt; 4 advisories, display hygiene) — the bar is MET (two consecutive clean model-diverse passes, fresh verifiers): **the composition is RE-ACCEPTED at (ROOT-C)'s twelve-row displayed conditional scope** (ledger "RE-ACCEPTANCE: THE BAR IS MET AT TWELVE ROWS"). REVISION 13 (r14, §19) is the post-acceptance display/status-sync batch (the r10/REVISION-9 precedent); it is a TEXT CHANGE, so a confirming end-to-end pass **VC16 is OWED to re-anchor the accepted text on the REVISION-13 wording** — until it lands, the acceptance citation points at the REVISION-12 text. **VC16 [synced at REVISION 14, r15]:** the confirming pass RAN on the REVISION-13 text (`lean/notes/openmath/VC16_report.md`) — **COMPOSITION-SOUND, zero blockers** (advisories only: the Step-4 grade reads as PROVED-UNVERIFIED under §0's vocabulary until its audit lands; the D-12r §4 roster sync owed at the move; plus five confirmations incl. the (NSF-KEY) retirement, the K-CUT re-scope, the structural invariants, and no REVISION-12 regression) — **the acceptance RE-ANCHORS on the REVISION-13 text**, and finding 2 adjudicates the D-12r reclassification WARRANTED. REVISION 14 (r15, §20) EXECUTES the move (D-12r [M] → the §4 literature pin; ELEVEN rows ↔ (H1)–(H9)); a text change: the confirming pass **VC17 is OWED on the REVISION-14 wording** — until it lands, the acceptance citation points at the REVISION-13 text. **VC17 + VC18 [synced at REVISION 16, r17 — repairing VC18's advisory 1, which found THIS row stale at VC16]:** VC17 RAN on the REVISION-14 text (`lean/notes/openmath/VC17_report.md`) — **COMPOSITION-GAPS** (1 blocker + 1 advisory, both display-conformance at the r15 edits; all other charged checks PASSING) → REVISION 15 (§21), two in-place display syncs — a text change, the confirming pass rolls forward; VC18 (the confirming pass, fresh context) RAN on the REVISION-15 text (`lean/notes/openmath/VC18_report.md`) — **COMPOSITION-SOUND, zero blockers** (ONE advisory: this row's staleness, repaired here) — **the acceptance RE-ANCHORS on the REVISION-15 text**. REVISION 16 (§22, unit ROOT r17) then EXECUTES the (H8) = (K-CUT) retirement (the §3.1 roster to TEN rows; record at the §3.2 K-CUT retirement row above); a text change: the confirming pass **VC19 is OWED on the REVISION-16 wording** — until it lands, the acceptance citation points at the REVISION-15 text. **VC19 [synced at REVISION 17, r18]:** VC19 RAN on the REVISION-16 text (`lean/notes/openmath/VC19_report.md`) — **COMPOSITION-GAPS** (2 blockers, ONE defect: the r17 retirement OVERSHOT — the three K-CUT residues were assigned to owners whose statements do NOT house them, so clause (R), and (UB) by inheritance, silently consumed unpriced content at Step 18's K1; the (K-CUT) package and its retirement record CONFIRMED standing; acyclicity incl. 17 ≺ 18 ≺ 18b confirmed; no independent regression on the D-12r/NSF-KEY/PACK/(SL≥2)/(0)-(SQ)/Σ_τR_τ=1 repairs) → REVISION 17 (§23): the NEW slimmed §3.1 row **(H8′)** prices the three interfaces (GRAFT / MASS-ID / POOL-DICT; the roster to ELEVEN rows); a text change: the confirming pass **VC20 is OWED on the REVISION-17 wording** — until it lands, the acceptance citation points at the REVISION-15 text. **VC20 [synced at REVISION 19, r20]:** VC20 RAN on the REVISION-17 text (`lean/notes/openmath/VC20_report.md`) — **COMPOSITION-GAPS** (2 blockers, BOTH leftover sentences from the r17→r18 arc, the (H8′) substance CONFIRMED: §1's retired-(H8) bracket still counted TEN; the §3.2 K-CUT retirement row carried the pre-r18 owner-housing clauses) → REVISION 18 (§24, unit ROOT r19), both repaired in place — pure display syncs; the confirming pass rolls forward to **VC21**. **VC21 [synced at REVISION 19, r20 — repairing VC21's advisory 1, which found THIS row's tail stale at VC19]:** VC21 (the confirming pass, fresh context) RAN on the REVISION-18 text (`lean/notes/openmath/VC21_report.md`) — **COMPOSITION-SOUND, zero blockers** (ONE advisory: this row's staleness, repaired here) — **the acceptance RE-ANCHORS on the REVISION-18 text**. REVISION 19 (§25, unit ROOT r20) then EXECUTES the (H9) = (NSF) retirement (the §3.1 roster to TEN rows; record at this table's H9 = (NSF) retirement row; RE-OPEN TRIGGER = the D-11 confirming pass); a text change: the confirming pass **VC22 is OWED on the REVISION-19 wording**. **REVISION 20 [§26, unit ROOT r21]** is the leaf-status/display sync batch (the (GR-B) re-acceptance, the B-M2 acceptance, H1-U14/H2-U9 stated-behind-gates, the III-S4 repair + H6-A4b — all ledger-recorded; NO roster change, TEN rows recounted; no clause attribution moved); VC22 had NOT yet run, so the owed confirming pass ROLLS FORWARD: **VC22 is OWED on the REVISION-20 wording** — until it lands, the acceptance citation points at the REVISION-18 text. **VC22 [synced at REVISION 21, r22]:** the confirming pass RAN on the REVISION-20 text (ledger 2026-08-08, "VC22 CONFIRMED") — **COMPOSITION-SOUND, zero blockers** (full-document fresh-context pass: the Step 1–19 spine composes end-to-end post-r19/r20/r21; TEN rows recounted; 8 honesty spot-checks — 5 touched + 3 controls — all FAITHFUL against artifacts and commits; the §26 audit table reproduced; ONE ADVISORY, metadata-only: the (GR-B) row's 55/2-NEW grep counts read 54/3 under the line-site unit, zero stale operative displays) — **the acceptance RE-ANCHORS on the REVISION-20 text**. REVISION 21 (§27, unit ROOT r22) is the next text change (the H4 F-chain/P(O-8b/M14) records, the TR3-S1 stratum annotation, the (UB-X) Route-B status, §6 campaign pointers; NO roster change, TEN rows recounted): the confirming pass **VC23 is OWED on the REVISION-21 wording** (charge at §27) — until it lands, the acceptance citation points at the REVISION-20 text; VC24 names the pass after it in the standing cadence (model diversity per the tree §4 bar). **REVISION 22 [§28, unit ROOT r23]** is the next text change (the (H4a) retirement EXECUTED at the P(O-8b/M14) ACCEPT — passes 3+4 consecutive CLEAN, model-diverse, ledger 5a10256 — the roster to NINE rows, record at the §3.2 (H4a) retirement row, the (K3-δ) note moved to (UB-X); plus the 2026-08-08 campaign folds: the SITE-EXH arc, GRADED-READ/B2D, the V9 e′ ≥ 2 ruling, the strata probe, B-M2 REVISION 15): **VC23/VC24 NEVER RAN on the REVISION-21 text** (superseded before dispatch; they stand as named-but-unrun slots) — the owed confirming pass rolls forward onto the REVISION-22 wording at the cadence's next fired number: **VC25 is OWED on the REVISION-22 wording** (charge at §28), **VC26** the pass after it (model diversity per the tree §4 bar); until VC25 lands, the acceptance citation points at the REVISION-20 text. **VC25 [synced at REVISION 23, r24]:** RAN on the REVISION-22 text (report `lean/notes/openmath/VC25_report.md`, archived ea9f615) — **NOT-CONFIRMED, 1 critical + 3 gaps** (the census, NINE-row recount, and clause recounts all REPRODUCED; the critical = the (H4a) retirement not propagated into the operative §3.4 items 6(iii)–(ix)+7 / 9 and Step 16 clause (ii); gaps = the F-(i) five-vs-SIX record count, the granular F-chain pointers unresolvable from the pasted ledger tail (re-verified in-repo at r24 — audit-input artifact), the unmarked stale 2026-08-02 close). ALL FOUR DISPOSED at REVISION 23 (§29). **The acceptance citation STAYS at the REVISION-20 text (VC22)**; the owed confirming pass rolls forward: **VC26 is OWED on the REVISION-23 wording** (charge at §29). **VC26 + VC26b [synced at REVISION 24, r25]:** VC26 RAN on the REVISION-23 text (report `lean/notes/openmath/VC26_report.md`, archived 565aa28) — **0 critical / 1 gap**: all four VC25 dispositions PASS at their sites; the whole-operative sweep found ZERO surviving unbracketed (H4a)/(R1)–(R4) live uses; the full 12-pattern occurrence census reproduced EXACTLY with every delta's recorded reason; NINE rows recounted; clause (R)/(UB) recounts 8/8; the line-anchored falsifier 0 hits; all fold commits + the §29 ASVIN FLAG verified displayed — the single gap an AUDIT-INPUT artifact (the orchestrator's provenance appendix omitted N-T4, which is not a separate file but the §N NUMERIC-GATE RECORD embedded at `H4M1_rpack_attempt.md:1091–1186`; the r24-3 lesson recurring at one pointer). VC26b (the narrow confirming pass, fresh context, on the COMPLETED appendix with the embedded §N slice pasted verbatim; report `VC26b_report.md`, b6121a5) — **CONFIRMED, 0C/0G**, the auditor additionally re-executing the embedded N-T4 script independently in-process (13/13 assertions PASS) — **the acceptance RE-ANCHORS on the REVISION-23 text** (ledger 2026-08-08 "VC26 + VC26b CONFIRMED"; the anchor moves REVISION 20/VC22 → REVISION 23/VC26+VC26b, the citation-text update riding r25 per the VC22 precedent). REVISION 24 (§30, unit ROOT r25) is the next text change (the anchor-citation update + the wave-8 folds: TR3-S2 ACCEPTED/TR-3′-GEN order-2 theorem, REC-SL narrowed, the readsOf restatement, HUNI-V2, the forge re-pricing; NO roster change, NINE rows recounted): the confirming pass **VC27 is OWED on the REVISION-24 wording** (charge at §30; model diversity per the tree §4 bar) — until it lands, the acceptance citation points at the REVISION-23 text. **VC27 + VC27b [synced at REVISION 25, r26]:** VC27 RAN on the REVISION-24 text (report `lean/notes/openmath/VC27_report.md`, archived 5e8dc97) — **0 critical / 2 gaps**: ALL substantive checks PASSED (3/3 re-anchor sites; no self-confirmation; TR3-S2 scope discipline; REC-SL's exact two-row display; the readsOf fence + no-consumer disposition; huni v3 + v4 both recorded; forge = junc_bundle alone with reasons; the §29 ASVIN FLAG standing; the full 17-pattern census arithmetically exact incl. VC26's +58; NINE rows; clauses 8/8; falsifier 0) — both gaps AUDIT-INPUT appendix defects (the N-T4 §N slice named but not pasted; the GRB bracket slice empty from a wrong grep pattern), the recurring genre now a saved orchestrator checklist. VC27b (the narrow confirming pass, fresh context, on the verbatim slices; report `VC27b_report.md`, 6aa3b20) — **CONFIRMED, 0C/0G** — **the acceptance RE-ANCHORS on the REVISION-24 text** (ledger 2026-08-08 "VC27 + VC27b CONFIRMED"; the anchor moves REVISION 23/VC26+VC26b → REVISION 24/VC27+VC27b, the citation-text update riding r26 per the VC22 precedent). REVISION 25 (§31, unit ROOT r26) is the next text change (the anchor-citation update + the wave-9 folds: POOL-DICT decided countermodel-first, the TR-4 empirical debt discharged, the CU2t k′=0 split verdict + `BasePolygonLengthLaw`, leakFree typed, D-ITER restated at the ITER-LAW, the forge consolidation display; NO roster change, NINE rows recounted): the confirming pass **VC28 is OWED on the REVISION-25 wording** (charge at §31; model diversity per the tree §4 bar) — until it lands, the acceptance citation points at the REVISION-24 text. **VC28 [synced at REVISION 26, r27]:** VC28 RAN on the REVISION-25 text (report `lean/notes/openmath/VC28_report.md`, archived d66bef2) — **CONFIRMED FIRST-PASS, 0 critical / 0 gaps**, the FIRST first-pass confirmation of the cadence: all folds verified at their sites (anchor citations; POOL-DICT displayed derivation-route-dead with the row intact; TR-4 empirical-only; BasePolygonLengthLaw displayed NOT rostered, roster NINE; the leakFree honesty display; the D-ITER open-lemma discipline; the three-target forge display); the 26-pattern census reproduced exactly incl. the VC27/VC27b substring relationship; provenance fully resolved (the appendix built per the saved orchestrator checklist — its first validation, after three first-passes lost to appendix generation) — **the acceptance RE-ANCHORS on the REVISION-25 text** (ledger 2026-08-08 "VC28 CONFIRMED FIRST-PASS"; the anchor moves REVISION 24/VC27+VC27b → REVISION 25/VC28, the citation-text update riding r27 per the VC22 precedent). REVISION 26 (§32, unit ROOT r27) is the next text change (the anchor-citation update + the synthesis-pass-6 ADOPTION: the forge decision — `junc_bundle` chartered alone, the nonempty-parent and two-equal-state targets declined with displayed dispositions — `BasePolygonLengthLaw` chartered HIGH, the F3 collapse map, the (ADM) G-ADM-2 status correction, the sorry-census refresh; NO roster change, NINE rows recounted): the confirming pass **VC29 is OWED on the REVISION-26 wording** (charge at §32; model diversity per the tree §4 bar) — until it lands, the acceptance citation points at the REVISION-25 text. **REVISION 27 [§33, unit ROOT r28]** is the wave-10 fold batch (`BasePolygonLengthLaw p` PROVED for every prime with the consumer `terminalPolySem_kp0_unconditional` FIRED — the (H6) k′ = 0 numerical input theorem-backed, the open-row display retired to proved status; forge `junc_bundle` stage 1 ALL GREEN 4,116 samples with the clause-(iii) two-scale law displayed as a MEASURED reduction and the D0 `gate_glue_junction` INSTANCE-FALSE FLAG pending the stage-2 adjudication; G-ADM-2 EXECUTED — the (ADM) row routes to track A/(ADM-REAL′), the scan adjudicating only; the Step-10/K1 classifier consumer face FIRED at the v4 chain + fence; the (H7) first probe — VACUOUS-BY-TYPE at the instance bin, N-H7-1 numerics first; NO roster change, NINE rows recounted; no clause attribution moved): **VC29 had NOT yet run** at this fold, so the owed confirming pass ROLLS FORWARD: **VC29 is OWED on the REVISION-27 wording** (charge at §33; model diversity per the tree §4 bar) — until it lands, the acceptance citation points at the REVISION-25 text. **VC29 [synced at REVISION 28, r29]:** VC29 RAN on the REVISION-27 text (report `lean/notes/openmath/VC29_report.md`, archived 3d457cc) — **CONFIRMED FIRST-PASS, 0 critical / 0 gaps**, the SECOND consecutive first-pass confirmation of the cadence (the saved appendix checklist validated twice): all r28 folds verified at their sites; the 34-pattern census reproduced; NINE rows recounted; no self-confirmation found — **the acceptance RE-ANCHORS on the REVISION-27 text** (ledger 2026-08-08 "VC29 CONFIRMED FIRST-PASS"; **the anchor moves REVISION 25/VC28 → REVISION 27/VC29**, the citation-text update riding r29 per the VC22 precedent; LINEAGE: REVISION 26 was properly superseded mid-cadence by the r28 roll-forward — NO pass ever ran on the REVISION-26 text). REVISION 28 (§34, unit ROOT r29) is the next text change (the anchor-citation update + the wave-11 folds: the `junc_bundle` VERDICT OF RECORD — instance-false at row A, kernel-proved, `gate_glue_conditional` vacuity a theorem, the source question OPEN, the FGMN re-derivation charter ASVIN-FLAGGED; ADM-REAL′ ACCEPTED — CLASS-LAT a lattice theorem at every order, the (GR-B) conditionality removed from track A, opens = (B-PIN)/(C-JCT)/F-ADM-3; the depth-3 probe (IDENT-3) exact + TR3-S3 in verification, NO status upgrade; N-H7-1 first-contact survival with two restatement pins; the JFS2 faithfulness arc closed; synthesis pass 7 OWED; NO roster change, NINE rows recounted; no clause attribution moved): the confirming pass **VC30 is OWED on the REVISION-28 wording** (charge at §34; model diversity per the tree §4 bar) — until it lands, the acceptance citation points at the REVISION-27 text. **VC30 + VC30b [synced at REVISION 29, r30]:** VC30 RAN on the REVISION-28 text (report `lean/notes/openmath/VC30_report.md`, archived 573bd1b) — **1 critical / 0 gaps**: the §34 closing line's UNQUALIFIED `junc_bundle` restatement, the one verdict-tier site that omitted the three restrictions (the qualifier-propagation defect class's last appearance); every fold, the 38-pattern census, roster NINE, and all discipline checks otherwise passed. The r29-ERRATUM LINEAGE: the dated pre-confirmation erratum bracket applied the three restrictions at that line (573bd1b), and the narrow confirming pass **VC30b RAN on the errata-corrected REVISION-28 text and CONFIRMED, 0 critical / 0 gaps** (report `VC30b_report.md`, 6984d5d — the VC26b/VC27b narrow-companion genre) — **the acceptance RE-ANCHORS on the REVISION-28 text** (ledger 2026-08-08 "VC30 + VC30b CONFIRMED"; **the anchor moves REVISION 27/VC29 → REVISION 28/VC30+VC30b**, the citation-text update riding r30 per the VC22 precedent). REVISION 29 (§35, unit ROOT r30) is the next text change (the anchor-citation update + the TR3-S3 acceptance fold: TR-3′-GEN proved on {all order-2} ∪ {order-3 all-e_j = 1} CONDITIONAL ON (H-v3), attribution only — the finite identity unconditional; PIN-GEN-i displayed with the pdftotext leg owed at Defs 3.15/3.16/3.18; deeper strata open probe-first; the GRB second post-acceptance bracket additive; synthesis pass 7 displayed RUNNING, nothing claimed from it; NO roster change, NINE rows recounted; no clause attribution moved): the confirming pass **VC31 is OWED on the REVISION-29 wording** (charge at §35; model diversity per the tree §4 bar) — until it lands, the acceptance citation points at the REVISION-28 text. **VC31 + VC31b + VC31c [synced at REVISION 30, r31]:** VC31 RAN on the REVISION-29 text (report `lean/notes/openmath/VC31_report.md`, archived f9aa7ef) — **0 critical / 2 gaps** (the 43-pattern census EXACT, NINE rows recounted, every fold verified at its sites; gap 1 = a truncated appendix slice, AUDIT-INPUT — the saved orchestrator checklist extended to completeness; gap 2 = the (H1) fence display named only the e_j ≥ 2 complement of the proved union) → the dated **r30-ERRATUM** (f9aa7ef) made the fence name the ENTIRE complement (orders ≥ 4 incl. all-e_j = 1 explicit; DISPLAY ONLY — the proved union was always exact); VC31b (the narrow companion on the completed appendix; report `VC31b_report.md`, a63b9ef) — BOTH substance checks PASSED (the complete GRB slice + the erratum fence) and raised the dating observation → the standing **DATING CONVENTION disclosure** (a63b9ef: window-label dating on artifacts/entries; git commit order = the AUTHORITATIVE chronology); **VC31c CONFIRMED, 0 critical / 0 gaps** (report `VC31c_report.md`, 620417b — the sequencing claims verified from the machine-recorded commit chain regardless of label dates; no claim found that depends on a wall-clock date) — **the acceptance RE-ANCHORS on the (errata-corrected) REVISION-29 text** (ledger 620417b "VC31 + VC31b + VC31c CONFIRMED"; **the anchor moves REVISION 28/VC30+VC30b → REVISION 29/VC31+VC31b+VC31c**, the citation-text update riding r31 per the VC22 precedent). REVISION 30 (§36, unit ROOT r31) is the next text change (the anchor-citation update + the wave-14 folds: the FGMN re-derivation PROPOSAL on record NOT applied, with PIN-GEN-i DISCHARGED as its rider; the (H-v3) discharge on record PENDING VERIFICATION (HV3-THM); F-ADM-3 RE-KEY APPLIED with CLASS-LAT machine-checked at every order and `hclass` retired; the TWIST-3 law MEASURED EXACT with TR3-S4 chartered; the (UB-X) walk on file with RB-DELTA adjudicated CONSUMER-VISIBLE; NO roster change, NINE rows recounted; no clause attribution moved): the confirming pass **VC32 is OWED on the REVISION-30 wording** (charge at §36; model diversity per the tree §4 bar) — until it lands, the acceptance citation points at the REVISION-29 text. **VC32 + VC32b [synced at REVISION 31, r32]:** VC32 RAN on the REVISION-30 text (report `lean/notes/openmath/VC32_report.md`, archived e3425b1) — **0 critical / 3 gaps**: ALL folds PASSED (the FGMN proposal-only discipline; no HV3 scope leak; the F-ADM-3 flag-lineage record; the TWIST-3 honest RED; the UBX gate; roster NINE recounted; the 51-pattern census EXACT); the gaps were ALL fold-lag/wording syncs → the dated **r31-ERRATUM** (e3425b1: the FGMN/HV3 pass-1 status displays synced to LANDED + REVISION 1 applied; the 336-SAT claim rescoped to the window/support check; the (UB-X) gate wording harmonized to the §36 disposition); **VC32b CONFIRMED, 0 critical / 0 gaps** (report `VC32b_report.md`, e32ebef — the narrow companion on the corrected passages verbatim, the VC30b/VC31b genre) — **the acceptance RE-ANCHORS on the (errata-corrected) REVISION-30 text** (ledger e32ebef "VC32b CONFIRMED"; **the anchor moves REVISION 29/VC31+VC31b+VC31c → REVISION 30/VC32+VC32b**, the citation-text update riding r32 per the VC22 precedent). REVISION 31 (§37, unit ROOT r32) is the next text change (the anchor-citation update + the HV3 ACCEPTANCE FOLD: (H-v3) DISCHARGED at fa2180e — passes 4+5 consecutive CLEAN 0C/0G, model-diverse, adversarial control on record; TR-3′-GEN PRINT-UNCONDITIONAL at {all order-2} ∪ {order-3 all-e_j = 1} per HV3-THM's four standing conditions; the 47-site bracket discharge sweep EXECUTED in TR3S3/ROOT/GRB with the reconciliation displayed at §37; PIN-GEN-i stays displayed, its leg discharged separately at 303b11e; plus the FGMN status sync — pass 5 CRITICAL → REVISION 4, the complete grep-disciplined DictIII perimeter, the change-set priced UP, passes 6+7 decide, proposal Asvin-gated; NO roster change, NINE rows recounted; no clause attribution moved): the confirming pass **VC33 is OWED on the REVISION-31 wording** (charge at §37; model diversity per the tree §4 bar) — until it lands, the acceptance citation points at the REVISION-30 text. **VC33 + VC33b + VC33c [synced at REVISION 32, r33]:** VC33 RAN on the REVISION-31 text (report `lean/notes/openmath/VC33_report.md`) — **0 critical / 2 gaps** (gap 1 = the FGMN pass-6 concurrency displays, fold-lag; gap 2 = AUDIT-INPUT — the verifier's read-only sandbox failed to launch, so the charge's grep-based checks were not independently executed; the execution duty transferred) → the dated **r32-ERRATUM** (e7ff234: the six p6-concurrency disclosure brackets applied at the operative §0/§37 status displays; the grep-execution duty transferred to the narrow pass); VC33b (the narrow companion WITH execution; report `VC33b_report.md`) — **0 critical / 1 gap**: the 12/3/6 discharge-core transcripts CONFIRMED under execution; the gap = three multi-line brackets missing from the orchestrator's `grep -o` extraction (audit-input); **VC33c CONFIRMED, 0 critical / 0 gaps** (report `VC33c_report.md` — all six brackets verified in full context) — **the acceptance RE-ANCHORS on the (errata-corrected) REVISION-31 text** (ledger 1de8ec2 "VC33c CONFIRMED"; **the anchor moves REVISION 30/VC32+VC32b → REVISION 31/VC33+VC33b+VC33c**, the citation-text update riding r33 per the VC22 precedent). REVISION 32 (§38, unit ROOT r33) is the next text change (the anchor-citation update + the FGMN ACCEPTANCE fold: the 14-pass/10-revision arc closed at ec64503 — pass 12 Fable executed CLEAN + pass 14 Codex content-CLEAN, model-diverse; the glue route's source-level question ANSWERED, the print's glue step fires at row A, the compiled walls transcription artifacts of the double-scaled clause; the acceptance binds the MATHEMATICS ONLY per the REVISION-9 scope cut, the application change-set the Asvin-gated application unit's deliverable; plus the division-of-labor adjudication of record (3875c49), the TR3-S4 composed + sealed-falsifier status with Lemma HV3-E2 derived from print and pass 1 0C/2G archived — NO status upgrade, and synthesis pass 8 OWED at the wave-15 close; NO roster change, NINE rows recounted; no clause attribution moved): the confirming pass **VC34 is OWED on the REVISION-32 wording** (charge at §38; model diversity per the tree §4 bar) — until it lands, the acceptance citation points at the REVISION-31 text. **VC34 [synced at REVISION 33, r34]:** VC34 RAN on the REVISION-32 text (report `lean/notes/openmath/VC34_report.md`) — **CONFIRMED FIRST-PASS, 0 critical / 0 gaps**, the third first-pass confirmation of the window (ALL folds verified: the FGMN acceptance display exactly the REVISION-10 list with no application leak and the wall theorems byte-frozen; the TR3-S4 un-upgraded status with the F2c obligation displayed; the division-of-labor discipline; the Asvin batch with its verified top item; the 59-pattern census exact; roster NINE) — **the acceptance RE-ANCHORS on the REVISION-32 text** (ledger f3b1ff8 "VC34 CONFIRMED FIRST-PASS"; **the anchor moves REVISION 31/VC33+VC33b+VC33c → REVISION 32/VC34**, the citation-text update riding r34 per the VC22 precedent). REVISION 33 (§39, unit ROOT r34) is the next text change (the anchor-citation update + the TR3-S4 ACCEPTANCE fold: the 4-pass/2-revision arc closed at cd611af — pass 3 Fable executed CLEAN with BOTH execution legs incl. the F2c verifier-owned from-note implementation discharged IN-PIPELINE for the first time, 1,108 checks / 0 failures with mutation-control teeth, + pass 4 Codex content-CLEAN, model-diverse; TR-3′-GEN now PROVED on the THREE-component union through the order-3 e₀ = 1/e₁ ≥ 2/e₂ = 1 stratum — conditional there on (H-ℓ) + the (KP_i)+[T] consumptions at S6.2 + the PIN-GEN-i attribution display, with Lemma HV3-E2 derived from print — deeper strata OPEN probe-first, NO candidate sealed; plus the ONE TWIST3-probe hygiene bracket at the pre-correction KEY0-LIFT orientation label; NO roster change, NINE rows recounted; no clause attribution moved): the confirming pass **VC35 is OWED on the REVISION-33 wording** (charge at §39; model diversity per the tree §4 bar) — until it lands, the acceptance citation points at the REVISION-32 text. **VC35 + VC35b [synced at REVISION 34, r35]:** VC35 RAN on the REVISION-33 text (report `lean/notes/openmath/VC35_report.md`) — **0 critical / 2 gaps** (gap 1 = the §39 closing summary abbreviated the third strata component's conditions → the dated **r34-ERRATUM** d79f295 spells out (H-ℓ) + the (KP_i)+[T] consumptions at S6.2 + the PIN-GEN-i attribution display at the closing site too; gap 2 = AUDIT-INPUT — the appendix's TWIST3-bracket slice was empty, a grep-pattern artifact, checklist rule 5); **VC35b CONFIRMED, 0 critical / 0 gaps** (report `VC35b_report.md` — the corrected closing passage + the ACTUAL TWIST3 bracket verified in full context, the VC30b/VC31b narrow-companion genre) — **the acceptance RE-ANCHORS on the (errata-corrected) REVISION-33 text** (ledger 345b432 "VC35 + VC35b CONFIRMED"; **the anchor moves REVISION 32/VC34 → REVISION 33/VC35+VC35b**, the citation-text update riding r35 per the VC22 precedent — the SIXTEENTH audited re-anchoring). REVISION 34 (§40, unit ROOT r35) is the next text change (the anchor-citation update + the WAVE-16 fold: **ITER-LAW PROVED AT ATTEMPT GRADE on the harness tower class** — standard-lift towers, the class DITER_RESTATE §S2 pins — seal ab7c31c, run 242ba1e, ALL GREEN FIRST PASS 35,232/0, acceptance counter 0 with hostile passes in flight, (ITER-LAW-LIFT) open exactly at g₀ = 1 ∧ δ₁ = 1; **the banked sorry RETIRED** at 15372d1 — `npVertices_stable_of_hull_preserved` PROVED Lean-core, statement byte-unchanged, AxChk clean, the corpus's banked-sorry count 1 → 0; **the H7 forge** at 037ac6f — the first compiled non-vacuous (H7) decision point, nothing discharged; **the DITERSUP statement** at 2a25ed9 — display-only, every gate BLOCKED-ON-APPLICATION; NO roster change, NINE rows recounted; no clause attribution moved): the confirming pass **VC36 is OWED on the REVISION-34 wording** (charge at §40; model diversity per the tree §4 bar) — until it lands, the acceptance citation points at the REVISION-33 text | Phase C |

**GD23 row RETIRED [REVISION 1 status sync]:** the former "GD23 gap-9 light repairs"
row is DISCHARGED — rev 4 repaired all 9 gaps (f9878d1, cores byte-stable), the
gap-check ran clean, and GD23 was ADJUDICATED **VERIFIED (leaf #4)** at the 2026-08-02
harvest (c43b13b). No GD23 pass debt remains; its (GR-B) conditionality stays in §3.1.

**CU-1 and O-10 rows RETIRED [REVISION 3 status sync]:** the former "CU-1 r5" and
"O-10 pass 2" rows are DISCHARGED at the 2026-08-02-night harvest (ledger 7b28562) —
CU-1's r5 gap-check ran GAPS-CLOSED (0/0, commit 5329a4e) → **VERIFIED (leaf #5)**;
O-10's model-diverse pass 2 ran SOUND-WITH-GAPS with the mathematics airtight
(5296528) → **VERIFIED (leaf #6) at scope** (its two minor gaps ride as the doc-touch
row above). No CU-1/O-10 pass debt remains; CU-1's (GR-B)/pin conditionality and
O-10's K-LOC/(I-τ) interfaces stay where they are (§3.1/§3.3).

**O4T row RETIRED [REVISION 4 status sync]:** the former "O4T r4 + confirming pass"
row is DISCHARGED — the confirming pass (pass 4, acceptance) ran **SOUND, CLEAN: 0
criticals / 0 gaps** (8602e97: core independently re-derived incl. both CEIL corners;
harness re-run digit-for-digit vs the sealed JSON) and O4T was ADJUDICATED
**VERIFIED (leaf #7)** at the confirming-passes harvest (ledger 85670f7) —
**VERIFIED = 7: O-13, O-12, Dfloor, GD23, CU-1, O-10, O4T**. No O4T pass debt
remains; the (AGR) inherited-[V] row and Step 13's displayed conditionality
attribution (VC4-4) stay where they are.

**O-9 and CU-2t rows RETIRED [REVISION 5 status sync]:** the former "O-9 r4 +
pass 4" and "CUC + CU-2t R3 pass" rows are DISCHARGED at the 6a7593b harvest.
**O-9**: pass 4 on r4 SOUND (0 criticals; two gloss-survival display sites) → r5
(9335057, gloss purge only, diff-audited) → pass 5 CLEAN 0/0 (825378d, K7 re-executed
byte-identical) → **VERIFIED (leaf #8)** at its honest scope (CEN-W/CEN-J conditional
on (GR-B) + (FRESH) + (ADM)-FULL, all §3.1 rows — unchanged by the adjudication).
**CU-2t**: R3 pass SOUND-WITH-GAPS 0-critical (both logic repairs verified) → R4
(b9f05db: the (T-READ) re-grade + the fold-state and a₀ ≥ 2 prose repairs) → R4
spot-check CLEAN (d14cd8c, no further pass owed per the verifier's own disposition)
→ **VERIFIED at scope (leaf #9)**, conditional per §9.5 on exactly the three terminal
rows (now the §3.1 (H6) group) + the pinned literature —
**VERIFIED = 9: O-13, O-12, Dfloor, GD23, CU-1, O-10, O4T, O-9, CU-2t**. The CUC
consolidation record's remaining debt is its constituent re-label passes = the named
registry rows above (P(O-2a r5)/P(O-1thr r4)/P(O5t r3)); no separate CUC row remains.

### 3.3 Transcription/audit debt [T] and named pins/flags

| item | content | owner |
|---|---|---|
| **GD-5 residue: the S-1 audit (incl. C.1.5)** | the one-page composition note + machine transcription audit (O-1thr machine ↔ O-2a stage interface F.0–F.2 at order 1) + the order-≥ 2 KEY1 block (same method). **Explicitly includes C.1.5** [REVISION 1, finding 5]: T-2's deferred graded-presentation obligation ("downsets are literal digit systems") — definitional at the mathematical machine (O-1thr §4 (S-1)); its transcription half is THIS audit. **KEY1-generalization adjudication [REVISION 6, finding A-6(i); SUPERSEDED at REVISION 7, finding VC7-1]:** proof on file — NO (unchanged); but the REVISION-6 consumption ruling ("consumed by NO order-≤ 1 mandatory step ... the item's mathematical face lives in the GD order-≥ 2 campaign perimeter priced by (H1)") is WITHDRAWN as insufficient — VC7 ruled that clearing the order-≤ 1 steps does not address Step 6's ALL-SITE theorems at higher OM orders, and "the (H1) campaign perimeter" named NO member assuming the statement: it was a consumed open proposition without an [M] home. The mathematical face is NOW the displayed §3.1 member **(H1)(d)** (stated there to match Step 6's use — the WGEO-consumed key-compliance block at EVERY order-≥ 2 site, key degree irrelevant; REQUANTIFIED to read order at REVISION 8, finding VC8-1), consumed at Step 6's order-≥ 2 conditionality line; this row's [T] content is the transcription/composition audit alone (unchanged) | GD-5 (D2.6 step 2) / T-2 → Step 6 |
| **D-5 disposition** [row added at REVISION 1, finding 5] | the tree's D-5 (WELLPOSED) carrier/equality design = O-2a rev 5 §§1–2 (two carriers, Θ, consistency predicate, Lemma R) — PROVED CONTENT of that leaf, verification debt = P(O-2a r5); the wider semantic-invariance clause is carried by each consuming leaf's own displayed carrier section (Steps 3, 6, 10), consumed nowhere as a separate node | O-2a (Step 7) |
| **(O6) canonicalizations** [row added at REVISION 1, finding 5] | O3design §1: branch-letter keying + deep-marker pooling — DEFINITIONAL census-side bookkeeping, no proof content (CUC §3(c)); V16/V21 arc on the O3design record | O3design → CU-3 (Step 11) |
| **GD-4 / GD-6 engine-bookkeeping units** | emission/record discipline (W1–W5, (c0), AUX band; K-OB-E's GMN Def-3.9 arithmetic is the one semantic clause) and run-decomposition conformance; proof-by-construction routes on record; CU-2t's terminal rows — RE-BILLED at the CU-2t R2 (567814d, CUC F4) and AMENDED at the CU-2t R3 (81589e9, CU2tpass G1; folded here at REVISION 4 per the R3's §9.5 touch (3)) as **THREE genuinely NEW unproved mandatory-path GD-4 obligations: (T-DEC) (split at R3 into (T-DEC-dec) emission discipline at decided sites + (T-DEC-cor), the corner fence), (T-READ) (emitted shape/position fields = the (c1)-shape polygon-side datum — the (RC-read) supply; NO displayed machine-side home on file, purely owed), and (T-VERD)** (in no printed clause list before CUC §9.4 displayed them; no new open family, but the residue GREW by these three rows — the count history 0 → 2 → 3 across rev1/R2/R3 is stated plainly: rev 1's "zero new inputs" was retracted at R2, and R2's TWO undercounted by one); GD-4's owner brief absorbs the §9.4 scope note at its next revision (the three rows + the §9.2a inventory-closure question + (T-DEC-cor)'s exhaustiveness clause, the last jointly with GD-8); **enters Step 18 through D-15's (M6b)(iii)/(ii−) clauses** (O-11 K4-CYL — declared row D-a at O-11 r2). **[REVISION 5, finding VC5-2: the three terminal rows are PROMOTED OUT of this [T] row** to the §3.1 [M] group (H6) — under the ledger's own classification rule they are open propositions consumed on clause (R)'s mandatory path, not transcription debts; (T-READ) carries CU-2t R4's GD-2/GD-5-family re-grade. This row KEEPS the engine-bookkeeping content (W1–W5, (c0), AUX band, K-OB-E's semantic clause) and the owner-brief absorption duty; its Step-18 D-a entry line now points at the (H6) row for the promoted content. **[REVISION 6, finding F-3 — two further repairs: (i)** the GD-6 run-decomposition-conformance INSTANCES consumed at orders ≥ 2 (Step 10's conditionality line) are PROMOTED OUT of this row to §3.1 (H1)(c) — "proof-by-construction routes on record" are routes, not proofs, the same evidentiary status under which (T-DEC)/(T-VERD) were promoted at VC5-2; this row keeps only GD-6's UNCONSUMED bookkeeping/route remainder. **(ii)** the D-a consumption ambiguity resolved: what Step 18's D-a/K4-CYL consumes of this area's OPEN content is exactly the (H6) row — the KEPT bookkeeping content (W1–W5, (c0), AUX band, K-OB-E's clause) is consumed at the CU/GD construction level where its proof-by-construction records live, by NO mandatory §2 step as an open general lemma; if a future pass finds a mandatory consumption of the kept content, the VC5/VC6 genre rule applies and that item promotes] | GD-4/GD-6 owners |
| **GD-7 flags: (GMN-COH)/(R-0)/(R-1)/(R-2)/(R-3)** | O5triple's five citation-grade flags (sprouted-type dissection, flow coherence, order-0 anchor, ADEQ read-back, representative lookup) — one GMN §2.1/§3/§4.1 reading session covers them; NOT covered by the executed LITUNIT. **Pins note [REVISION 6, finding A-7]:** the section-granularity pin is a CLAIMED range, unexecuted — [T] here is honest only with the discharge made a NAMED acceptance precondition, so it is: this row (with the LIT-1 pagination row it shares a session with) must be EXECUTED — per-flag verbatim extraction lines at the printed source, LITUNIT-style MATCH verdicts — before the §6 clean-pass acceptance bar; partial per-flag loci already on record (O5triple attempt): (GMN-COH) ← the sprouted-type cluster identity under eq. (37) (the eq.-(37) locus itself is §4-pinned), (R-1) ← the representative's "of type t" shape (GMN §3 region), (R-2) ← the exact-key polygon convention at order r (order-1 falsifier-covered, O2a F5 871 samples); (R-0)/(R-3) loci owed at the session. Given this project's two-false-axioms history, no flag is consumable from the range pin alone | GD-7/literature unit (Step 12) |
| **GD-8 order ≥ 2** | the exact-key polygon seam beyond order 1 (order-1 proved, falsifier-covered); expected verbatim over GD-2's frame tie. **Consumption scope [REVISION 6, finding A-6(iii)]:** consumed by NO mandatory §2 step — its only named consumers are the QUARANTINED Corollary CU-2t-deep (displayed non-mandatory at Step 11) and GD-8's share of Step 12's five-flag duty, which is the flags' citation-grade reading (the GD-7 row's session), not the ≥ 2 seam itself; O-9's VERIFIED reduction names exactly TWO ≥ 2 opens ((GR-B)/(FRESH)) — the census does not consume this seam — and Step 10 consumes GD-2/GD-3(min)/GD-6 (the (H1) group), not GD-8. The ≥ 2 write-up belongs to the GD order-≥ 2 campaign; recorded here LC-COH-style (its "(R1)–(R4) shape" stays honestly open, unconsumed) | GD-8 |
| **(KP)/(KPirr): the properness-proviso leg** [row RESCOPED at REVISION 6, finding F-2] | properness — a genuine side condition only at e·g = 1 refinement reads — with the FGMN Lemma-5.3 discharge DISPLAYED: this leg is pinned to a printed source, so it keeps [T]. **The beyond-order-1 (KP)/(KPirr) representative-admissibility clauses are PROMOTED OUT of this row** to §3.1 (H1)(b): they carried no pin and no proof on file while consumed at Steps 10–12 through O-2a's admissible-representative-family clause (= OL-2a-1, an exactly-stated open lemma) — the same-shape GD-2/GD-3 residue was already (H1), and the asymmetric classification was the F-2 blocker | GD-1 |
| **Declared pins (P-ROOT)/(P-KEY)/(FR≡)** | CU-1's three declared pins replacing silent cross-brief glue; (FR≡) has refutable order-2 content = the gate's coherent-convention finding. **(FR≡) adjudication [REVISION 6, finding A-6(iv)] — why refutable content is [T] here:** a declared CONVENTION pin is the Step-6 sloped/sheared genre — CU-1's proofs are STATED OVER the pinned convention and the leaf is VERIFIED with the pin declared, so no open proposition is consumed: what is owed is the transcription audit that every consumer reads the SAME convention (this row's [T] content). The order-2 content being refutable makes the pin FALSIFIABLE, and the gates ran COHERENT (the coherent-convention finding — a flag supporting the pin, never carrying it); a future refutation would be a fence event on CU-1's declared perimeter, not a silent edge | CU-1 (Step 10) |
| **Sloped/sheared convention pin** | O-1thr's window-height convention (the depth-2 falsifier forced it; 41,720/0 vs 7,472 violations unsheared) | O-1thr (Step 6) |
| **(I-τ) + K-LOC interface** [PROMOTED at REVISION 6, finding F-1 — tombstone] | the adapted-cell → engine-stratum presentation for O-10's Theorem 2 (K-LOC = M07 OL-3: continuation reads of branch j = OM reads of f_j up to cell-constant normalization) — **now the §3.1 [M] row (H7)**: an exactly-stated open lemma + an unproved presentation, consumed on clause (R)'s mandatory path (Steps 15/18 K1) with no pin and no supplying row, could not sit in [T] under the ledger's own rule; the LAST of O-11 r2's D-a/D-b/D-c triple adjudicated. This tombstone keeps the invariant's one-home-per-pin trail; content and owners at the (H7) row | → §3.1 (H7) |
| **(S1)/(S2) seams of D-11 M2** | (S1) = GD-3-FULL/O-9 hand-off ← Step 14 (+ (GR-B)); (S2) = the O-11 composition datum ← Step 18. Since REVISION 1 (finding 2) the step order 17 ≺ 18 ≺ 18b makes both seams STRUCTURALLY earlier — the acyclicity is in the walk itself, and the seams discharge when Steps 14/18's passes land. D-11 r2 (566b967) declares a THIRD seam **(S3)** = the projective assembly weight's MASS reading, owner annex A-1 — POSITIONED at REVISION 2 (finding V2-1): (S3) conditions ONLY the projective reading of M2's conclusion (the r2's own scoping), which NO §2 step consumes — Step 18b consumes M2 at monic scope, and the projective normalization is annex by standing directive; (S3) is therefore an ANNEX A-1 obligation, not mandatory-path residue, and appears in no (ROOT-C) hypothesis | D-11 (Step 18b, non-consuming) / annex A-1 |
| **OL-O12-1 contingent-escape flag** [row added at REVISION 3, finding A7] | Step 5's (B-INST) escape: fires ONLY if a future pack departs from 𝔅_n's three on-record kernel organizations — currently UNFIRED and consumed by NOTHING; homed here per the §7 closing standard (every named pin/flag has exactly one ledger home). If it ever fires, the O-12 pole-freeness theorems need re-derivation at the new organization | O-12 (Step 5) |
| **OL-O12-2 data pass** | the finite transcription/correspondence pass identifying the complete degree-3 table as an (O3) member of 𝔅₃ (math half closed at D-11). Since REVISION 4 (finding VC4-3) its GENERAL-n face is hypothesis-grade: the §3.1 (PACK) row (= (ROOT-C)(H5), D-11's open clause 4) — this row keeps only the executed-instance duty at n = 3 | O-12/D-11 |
| **T-U §U.SQ.6 correspondence audit** [row added at REVISION 3, finding A8] | Step 19 consumes THEOREM U conditional on its displayed §U.SQ.6 kernel ledger, discharged by the claim that the ledger's items coincide with the Steps 1–18b roster + the recorded evictions/re-routings (e.g. the R-QNT kernels struck at DELTA-1 §D1.4) + the annex items; the ROW-BY-ROW correspondence was ASSERTED, not displayed, through REVISION 5. [RE-CONFIRMED as the recorded [T] audit row at REVISION 5, advisory VC5-3.] **[EXECUTED at REVISION 6, finding A-9 — the third consecutive recording forced the display: the per-item pointer table (§U.SQ.6 items 1–21 → step/eviction/annex, with the Lean-bridge kernel rows of `REMAINING_CENSUS_2026-08-01.md` §2.3 / `MovesU/BridgeKernels.lean` cross-walked) is now §3.4.** Residual duty of this row: VC7 confirms the table (three of its dispositions are NEW adjudications made at the table and are flagged there: items 4/13c's consumer-audit evictions and the items-15–21 empirical-roster disposition); the row RETIRES on that confirmation. **[CONFIRMED at VC7 (findings 5/6): the table's warrants ruled compositionally genuine and the three NEW adjudications sound — two corrections applied at REVISION 7 (the 6(iii)–(ix)+7 residue list, advisory VC7-3; the items-15–21 gate-preserving wording, advisory VC7-5). This row is RETIRED as of REVISION 7, kept as tombstone per the one-home trail]**] | T-U (Step 19) / Phase C → §3.4 |
| **BASE-1(f) fence** | Dfloor's classifier-side n = 1 clause, fenced to the classifier steps (served by Steps 6–13's package) | Dfloor/CU |
| **LC-COH** | the displayed open note from the O-3 clause-3 re-key — CONSUMED BY NOTHING (kept for honesty) | tree D2.1 line 1 |
| **(REP-IRR)** | the unpinned literature pull behind the QUARANTINED Corollary CU-2t-deep (GD-8 corner) — consumed by nothing mandatory; flagged to the literature unit | CU-2t/literature unit |
| **W3 carrier note** | the τ-irr seam object is a PAIR, not an 𝔈-concatenation (CU-2t display; convergent with CU-1's split) | CU-2t |
| **LIT-1 printed-TAMS pagination** | the standing GMN confirmation duty (LITERATURE-CONDITIONAL until confirmed); one session with the printed TAMS covers it + the GD-7 flags. **PARTIALLY DISCHARGED [REVISION 14, r15 — the VC16 finding-2 roster sync]:** the (OM-SAT) slice is CONFIRMED on file (unit D12R-U3, `lean/notes/openmath/H3_omsat_brief.md` §1 — six loci VERBATIM against the printed TAMS: Cor 1.20 p. 374, Cor 3.8 pp. 398–399, Def 3.9 p. 399, Def 3.10 + eq. (37) p. 399, Lemma 3.11 p. 400, the base-field convention p. 366; citation line EXACT; printed = arXiv v2 numbering, no drift), and the brief contributes a located-page map for the REST of the §4 item-1 pin list (NOT verbatim-checked). The remaining loci + the GD-7 flags still owe the session — this row stays until it runs | literature unit |
| **LIT-3 numbers + Mathlib probe** | Neukirch II §4 Prop 4.6 / Serre LF I §6 numbers to confirm at declaration; probe `HenselianLocalRing` first. **DISCHARGED AS STATED [REVISION 14, r15 — the VC16 finding-2 roster sync]:** numbers CONFIRMED on file (unit D12R-U1, `lean/notes/openmath/H3_henlift_brief.md` — Neukirch (4.6) "Hensel's Lemma" printed p. 129, index cross-checked; the printed (4.6) carries NO uniqueness clause, disposed via the brief's §2 displayed companion lemma; Serre LF I §6 Prop 15 printed pp. 17–18); the Mathlib probe RAN, answer **ABSENT** (`HenselianLocalRing` supplies root-lifting only — NO coprime-factor lifting) — the pin STANDS AS A PAPER CITE (§4 item 5), and the optional Lean materialization (D12R-U2a/b/c) owes its OWN axiom-policy loop (citation + faithfulness entry + guardian audit) if/when it lands | literature unit (Step 12) |
| **LIT-8 per-use-site transcriptions** | LIT-8a..8e split (DELTA-1 §D1.5(d)); exact numbering owed at each declaration site (O4T's inventory did this for its own sites; Dfloor displays statements) | per consumer |
| **O-11 → D-11 widened-charge items (v)–(vii)** [row added at unit TREE-GAPS, audit gap G-4 — REVISION 1's §7 note "(5) ... noted pending acceptance" promoted to an operative row] | O-11 r2/r3's restated D-11 entry WIDENED the WAVE-8 charge: (v) the pack's carried laws rsh_interp/r_bdd/wsh_ok; (vi) the DVR scope-hygiene audit of K3-c; **(vii) general-n H.6 in-fence conformance of the built tables** — the surviving live condition of the tree T-5 row's "six H.6 fences gate [1v]-FULL" (Step 18's fence display; full adjudication + fence-by-fence mapping: tree DELTA 4 §D4.2): the six fences stay the H-DOMAINS perimeter's open kernels (owners/refuters unchanged, off the mandatory path); what Step 18's K3-a consumes is ONLY this conformance, proved nowhere at general n (V-n3/H-n3 are sealed n = 3 evidence). **[REVISION 5, finding VC5-1: item (vii) PROMOTED to §3.1** — the (H6) group, ending the "kept [T]" disposition: VC5 ruled the condition an undischarged mathematical premise on clause (R) (Step 18's K3-a consumes it; "proved nowhere at general n" is this row's own text), analogous to (PACK) — the escalation happened by classification-rule ruling, not by owner rejection or perimeter exit. This row KEEPS items (v) (the pack's carried laws rsh_interp/r_bdd/wsh_ok) and (vi) (the DVR scope-hygiene audit of K3-c) at their [T] grade; acceptance of the widened charge still owed by the D-11 owner. **Item-(v) adjudication [REVISION 6, finding A-6(ii); RESOLVED at REVISION 7, finding VC7-2 — the flag FIRED]:** proof on file — NONE DISPLAYED in the D-11/O-11 records this document consults (unchanged); consumed where — Step 18's restated D-11 entry item (v), sub-attributed [REVISION 8, finding VC8-2]: PACK-i/ii at K2's evaluation glue, PACK-iii at K3's evaluation ring-hom (with D-6). The REVISION-6 pricing ("a pack equal to the intended 𝔅_n instantiation entry-for-entry carries its 𝔅_n-side laws ... item (v) therefore rides (H5)") is WITHDRAWN as an invalid inference: entry-for-entry equality transfers the laws only if an earlier displayed theorem establishes them at the corresponding 𝔅_n rows, and NO displayed theorem does — the parenthetical "the T-layer/O-12 displayed statements at 𝔅_n" named no statement stating rsh_interp/r_bdd/wsh_ok or implying them (Steps 2/5 state neither — VC7's warrant charge). The honest disposition is the (H5) ENLARGEMENT: the three laws are now EXPLICIT clauses **(PACK-i)/(PACK-ii)/(PACK-iii)** of the §3.1 (PACK) row — still no separate open row, but carried as displayed hypothesis content, never as a correspondence corollary; this row keeps only the owner's acceptance-of-charge duty] | D-11 (Steps 17/18) / O-11 r3 §0 |
| **O-14a/b/c engine-residue dispositions** [row added at unit TREE-GAPS, audit gaps G-1/G-2/G-3] | tree D-10's O-14 split, homed: **O-14a** guarded vertex-transport law — CLOSED at the e′ = 1 sub-perimeter, Lean twin `lean/LeanUrat/HC2/HK11a_vertexTransport.lean` (σV-vertex law, Lean-core); unguarded form stays REFUTED (HK52_stretchGate); ≥ 2 disposition = sign-off item 28 (the PARKED row above) **[REVISION 22, r23 — the parked e′ ≥ 2 question ADJUDICATED (hardest-first Tier-1 item 2, commit d632305): VERDICT **VACUITY-RESCOPE** — the FALSE-SUSPECT mapping is RETIRED, none of the three ruling-menu options adopted: the e′ ≥ 2 sub-perimeter SELF-FENCES inside `ReadsOf` before the vertex question is reached. Compiled leaf `HC2/HK53_e2Ruling.lean` (8 theorems, 0 sorry, Lean-core; statements elsewhere BYTE-UNTOUCHED) + numerics-first trace `verification/hk53_e2_ruling_trace.py` F1–F5 asserted green: the stride fence (nonzero digits k ≡ 0 mod e′, e′² ∣ wSide), the below-window conspiracy fence (`sideReads_belowWindow_tie`), and the s0 = 0 kill **`readsOf_e2_s0_zero_empty`** — every coherent ReadsOf record with a non-recentering-parented child read at e ≥ 2 ∧ s0 = 0 is CONTRADICTORY, so the whole HK23 carrier family carries no f and the HK-12 F4 countermodel plan is IMPOSSIBLE, not merely expensive. The V9 sorries STAND byte-unchanged; fill routes: e′ = 1 legs — the HK11a suppliers (unchanged); e′ ≥ 2 legs — VACUITY behind ONE pinned open lemma (the below-window CONSPIRACY EXCLUSION, exact shape pinned: conspirators at j < s0, j ≡ s0 mod e′, wPrev-pinned). The corrected all-e′ display is COMPILED at the strFrame(i+2) scale (`readVertex_stretch`/`readBox_stretch`, = the current display at e′ = 1); if the corpus ever repopulates the e′ ≥ 2 perimeter, the statements must move to that scale. K1 vertex-pin consumers unchanged, still waiting on the V9 fills; HK-11b/c unblocked at e′ = 1 exactly as HK-12 planned]**; consumed by NO §2 step (engine-internal — the Lean bridge's V9/HK cone; GD-2's clause-(c) HK52 trap is the ≥ 2 countermodel, not a consumption). **O-14b** cl19 repaired carrier ladder (post-K-7: the O1–O3 semantic ladder at a real carrier pack) — OPEN; K-7's "repaired carrier form" pointer lands HERE; sole consumer = the Lean-bridge slot wiring (queue item 22, `REMAINING_CENSUS_2026-08-01.md`; theoremU's slot bare-Prop, SAFE), PARKED by the 2026-07-31 math-first directive (fe3cda5). **O-14c** hStateNe — OPEN, expected derivable; = `BridgePre.hStateNe`, a named ratified Lean-bridge premise (BRIDGE_ADJUDICATIONS Q3); standing disposition = the IB-B2 derivation attempt (row drops if it closes). None of the three is consumed by any §2 step; none appears in any (ROOT-C) hypothesis (display sweep at unit TREE-GAPS, incl. the REVISION-4 (H5)) | Lean side, post-math (tree DELTA 4 §§D4.3/D4.4) |
| **Item-27/28 Lean statement repairs + O-14d instance population** | PARKED engineering (Lean statement fences; the wave-D carrier population) — outside the math criterion by DELTA-1 §D-10 | Lean side, post-math |

**What is NOT on the ledger (evicted from the mandatory path, recorded):** O-2b/OB1
(trace correspondence), OB2/CU-6 (σ-invariance under ∼_OM — the DELTA-3 audit: D-11
needs per-box lift uniformity instead), LIT-5/BNS entirely (zero root-side touch-points),
K-OB-C (Okutsu proximity), **K-OB-D (certificate-firing at O-5's threshold — evicted by
containment: sole consumer O-7; named here at unit TREE-GAPS, audit A-3 — the last IFK
REV-1 obligation without an explicit disposition line)**, D-16 (δ-tier execution
transport), O-6/O-7/O-8a (track count/weight charge/linear rates), **and `cl3_tails`
(X2-TAILS measure/cone tail estimate — the tree O-8 row's SEPARATE open estimate, owner
§H-DOMAINS family; its OWN eviction row at unit TREE-GAPS, audit G-5: quantitative-rate
content of the X.2 pricing family, consumed by nothing count-native — the root's
undecided-mass control is drainage (Step 13), never tail rates; tree DELTA 4 §D4.5)** —
ALL route R-QNT, needed only if explicit rates are
elected. htameFE/palindromy + the projective bridge are ANNEX (A-1/A-2).

**Closed/refuted tree ids — content homes (tree-§4 criterion-4 display; added at unit
TREE-GAPS, audit A-1/A-4/A-5).** T-11/JOINT-D1 → Step 2 inside T-1's perimeter
(D1-marginal machine-checked, `OM/DevBijection.lean`) · K-1 ksub / K-2 w17ii / K-15 →
CLOSED Lean-interface rows, nothing left for the composition to consume · K-3 →
refuted (caterpillar countermodel cited at Step 3); repaired math = O-6, route R-QNT ·
K-5 → refuted; the per-site true statement = O-1 (Step 6) · K-7 cl19_rep → refuted;
repaired carrier form = O-14b (the §3.3 O-14 row above) · K-8 → adapted-cell math =
O-10 (Step 15) · K-9 cl6 → level 1 at Step 14 (M08 Thm 2); order ≥ 2 = O-9 · K-10
cl15_align → Steps 7/8 + the §4 GMN Thm 4.18 pin · K-14 duties: (I0)/(I1)/(I3-rc) →
Step 10 + the D2.3 consumer walk; **(I2) level coherence → definitional at the
statement layer against T-3 §D4-R's D4R.4/L12 stabilization level (N(P̂) = max
per-prefix resolution level, finite by L8; M18 fable §6.2's displayed warrant) — its
instance-build face rides the O-14d/D-11 population; not a missing consumer-walk
edge (audit A-4)**.

### 3.4 The T-U §U.SQ.6 correspondence table [EXECUTED at REVISION 6, finding A-9]

Step 19 consumes THEOREM U (T-U, §U-SQUEEZE SQ.4) at its VERIFIED-DUAL grade,
conditional on the note's own displayed distance-to-done ledger **§U.SQ.6**
(`lean/notes/MOVES_2026-07-24.md` §SQ.6, items 1–21 over the capstone ledger
CL-1..CL-19; frozen 2026-07-24). Through REVISION 5 the claim "§U.SQ.6 ⊆ the Steps
1–18b roster + recorded evictions/annex" was ASSERTED (the A8/VC5-3/A-9 audit row).
This section DISPLAYS it. Sources read for the execution: the §SQ.6 list itself; the
kernel-ledger materialization `lean/notes/REMAINING_CENSUS_2026-08-01.md` §2.3 (the
BridgePre/Ctor/BK/seam rows) and `lean/LeanUrat/MovesU/BridgeKernels.lean` (whose
docstring pins every field to the frozen ledger display). Reading rule: "→ Step k"
means the item's mathematical content is supplied by that step's declared statement
at its displayed conditionality (open residue priced by the named §3.1 rows);
"EVICTED" means the recorded R-QNT/annex disposition applies (§3.3's eviction
paragraph); NEW adjudications made at this table (no prior recorded disposition) are
marked **[NEW — VC7]** (VC7 RAN and CONFIRMED all three — items 4/13c at finding 6,
items 15–21 at finding 5 as a consumption ruling; the markers stay as provenance,
and the two VC7 advisory corrections are applied in place below [REVISION 7,
findings VC7-3/VC7-5]).

**Table A — §U.SQ.6 items 1–21 → step / eviction / annex.**

| §U.SQ.6 item | content (compressed) | ROOT home + warrant |
|---|---|---|
| 1. CL-1 ESCAPE(E0), per-pool q₀ = p^δ | active-block solvability / spectral escape | → Steps 4/5 (O-12 Thms 1/3/4 + Lemma RR: margins ⇒ spectral radius < 1, every denominator a unit of ℛ at every prime power; K-13 the cofinite cap) — Step 18's walk consumes it as "Neumann under spectral escape" through O-12 at 𝔅_n; margins re-read at Step 17 (r1) |
| 2. CL-2 WEIGHT-CHARGE = X.1b (+ s(n)) | envelope weight-charge pricing | EVICTED — tree O-7, route R-QNT (DELTA-1 §D1.4; §3.3 eviction list); consumed by nothing count-native (census §2.4's cl2_route stays OPEN-MATH, owner [5], off-path) |
| 3. CL-3 X.2 PROGRESS ((X2-AFF)/(X2-BRIDGE)/(X2-CAP)) + (X2-TAILS) | coercivity/rate dichotomy | EVICTED — tree O-8a (linear rates), route R-QNT; (X2-TAILS) = `cl3_tails`, its OWN recorded eviction (TREE-GAPS G-5, §3.3): undecided-mass control is drainage (Step 13), never tail rates |
| 4. (X1a-ALIGN) (ALIGN-inc)/(ALIGN-rec) | the X.1a dictionary's proof duty | EVICTED by consumer audit **[NEW — VC7]**: its displayed consumers are X.2's constants + the depth ⟹ disc envelope — both the struck rate family (items 3/13c); the mandatory path's discriminant control is Step 13's Lemma C (M05-verified, self-contained). The X-n3 probe's recorded refutation of an X.1b reading (155,648 violations, adjudicated 2026-07-30) touches only this off-path family |
| 5. CL-4 X.3 termination a.e. | a.e. decidedness | → Step 13 (Lemma A: drainage ⇔ a.e.-decidedness + Theorem E at every prime, SEMANTIC layer unconditional; canonical-classifier transfer at general n = (AGR), the §3.2 inherited-[V] row; n ≤ 3 unconditional by Thm N3) |
| 6(i). CL-5 XHD = M4a-X (+ (ENT-U)) | the height-domains package | → Step 2 (T-5 H-DOMAINS at its VERIFIED-DUAL perimeter), consumed by Step 18's K3-a at exactly that perimeter; the package's open kernels are item 14's six fences — see item 14 |
| 6(ii). CL-5 D4R.0 | marked CLOSED in-list (ledger echo, not distance) | → Step 2 (T-3 §D4-R, dual-verified derivation; (a)/(b) discharged); the K-residual (c) = VP routes to item 10 |
| 6(iii)–(ix) + 7. the CTS-M family (PART / REP / MEAS / HMC / ACT / INIT+ENT-COUNT / COMP-typing) + CTS-M(iv)-POLY (CL-6) | the per-species value/mass semantic laws | governed by the recorded re-scopes (DELTA-1 §D1.5(e): T-1/T-7 semantic clauses route through the dictionary; D-9: [1v]-FULL re-owned by O-11): symbolic halves → Step 2 (T-7s/T-3); mass laws → Step 17 (M1/M1′/M3 over O-12's L1–L6′ base, Step 5 — at M1's r4 per-clause scope [REVISION 10, r11, edit E6]: families (i)–(iii) conditional on C_e, family-(v) per-occurrence readings, families (iv)/(vi)/(vii) excluded at (a″); M3 at separable-lift scope; its former (H9) routing price RETIRED at REVISION 19, r20 — the M04 §5.5 pin, the §3.2 H9 retirement row) with measurability/cylinder structure → Steps 1/6 (D-1, D-15) and leaf labels → Step 12; the COUNT laws (CL-6's polynomiality = the census) → Step 14 (M08/O-9) and Step 18's K3 assembly; COMP-typing → Steps 7/10 (Θ's typing; OL-6 binding); ACT's (REG-p)-separateness note → Steps 4/5 + the D1.5(f) T-8 re-scope. Open residue priced at §3.1 [residue list CORRECTED at REVISION 7, finding VC7-3 — the routes through Steps 6/11/12/17 also inherit D-12r and (E-N), i.e. the rest of (H3); the table's reading rule (step pointers inherit each step's declared conditionality) already prevented a hidden hypothesis, but the former list ((H1), (FRESH), (ADM), (PACK), (H6)) was inaccurate as a purported exhaustive residue]: (H1) group, (H2) = (FRESH), the FULL (H3) ((ADM) + (E-N); the routes ALSO inherit the §4 D-12r pin's TB-CAP perimeter, literature-conditional since its REVISION-14 reclassification out of (H3)), **(K3-δ)** — the LIVE input, housed at the §3.1 (UB-X) row and consumed by clause (R): this row's own route through Step 18's K3 inherits it at K3-c's base-changed β-legs (the δ > 1 pools), exactly as row 9 names **[(H4a) REMOVED from this list at REVISION 23, r24 — VC25 finding 1: the former entry "(H4a) + (K3-δ)" (RUN TO CLOSURE at REVISION 9, VC10 advisory 3; the VC7-3 correction completed) predated F-(iv) — (H4a) = (R1)–(R4) is RETIRED, the base under those β-legs is the PROVED package at the §3.2 (H4a) = (R1)–(R4) retirement row and is no longer open residue]**, (PACK), (H6) [the formerly-listed **(H9)** — added at REVISION 11, r12, finding VC11-4: the route through Step 17's M3 leg (via Steps 11/12) inherited (H9) = (NSF) — was RETIRED at REVISION 19, r20: the M3 routing price is discharged at the retired target's pinned scope (the §3.2 H9 retirement row), so it leaves the open-residue list] — the list is now CLOSED under the table's own reading rule (step pointers inherit each step's declared conditionality; every step route this row names has been traced): no CTS-M face is consumed outside these homes |
| 8. CL-7 TRACK-COUNT + L(n)/D(n) + positive-genuine-increment | p-uniform decided-family bounds | EVICTED — tree O-6, route R-QNT (§3.3 eviction list; K-3's repaired math = O-6, likewise struck); the count-native walk needs only per-level finiteness (inside Step 1's cylinder count) + the p-free menu bound (Step 3). Lean face `BK.slice_bound` carried, permanently fenced (census §2.3) |
| 9. CL-8 REL.1 / REL.2(a)(b)(d)(e) / REL.3 | the relative-measure ledger | → Steps 15 (O-10's adapted-cell product law — the (SIB) mass statements), 17 (M1 mass laws), and the β(q^δ)-typing leg (e) → Step 16 + Step 18's K3-c = (K3-δ) (the §3.1 (UB-X) row) **[(H4a) DROPPED from this route display at REVISION 23, r24 — VC25 finding 1: the former "= (H4a) + (K3-δ) (§3.1)" pointer DANGLED after F-(iv) ((H4a) has had no §3.1 row since REVISION 22); the (R1)–(R4) leg of this route now rides the PROVED package at the §3.2 (H4a) = (R1)–(R4) retirement row, while (K3-δ) stays the live consumed input]**; the PCI/height-elimination package (REL.2(a)/(b)/(d) + XRB) is CL-9's RS.0(β), proved in [3] = the VERIFIED-DUAL T-5/T-8 layer (Step 2) |
| 10. CL-10 (SIB)/(SIB-STEP) + (JC-multi); VP; VP-SOUND; TREE-EXP/TREE-N | the tree-assembly package | (SIB)/(SIB-STEP)/(JC-multi) → Step 15 (Thm 2 unconditional in factor coordinates; T-6's (SIB-STEP) at the statement layer per Step 15's Delivers; engine-stratum application = (H7), §3.1). VP = the D4R.0-K(c) verdict pin → Step 6 (D-15, the pin's home) with TB-CAP per clause: (τ-hen) proved, (τ-irr) = the §4 D-12r literature pin [re-pointed at REVISION 14, r15 — formerly the (H3) row], (ns-null) → Step 1's Appendix A + T-6 (Step 2). VP-SOUND's two cites → Step 12 (LIT-3 coprime Hensel + GMN Cor 3.8/Def 3.9 — both §4-pinned). TREE-EXP/TREE-N → Step 2 (T-3/T-6) + Step 6 (D-14's per-site read locality = the transfer kernel's repaired form) |
| 11. CL-11 no-equal-e-feedback + DEG-CONS | degree conservation / well-foundedness | DISCHARGED: PROVED at all n Lean-core (M09 `KsubGeneral.lean`, census §2.3) + Step 5's (SL≥2)/L4 strict descent + Step 17's M1 degree checksums (Step 12 Thm B consumes checksums only) |
| 12. CL-17 shallow assembly (routing + unique shallow cut + W_Ŝ regrouping; (ii) production) | the RS.1 interface | RE-ADJUDICATED at REVISION 11, r12 — finding VC11-3 (the former routing sent the cut content to the verified Step-2 layer while the (H8) row displays it as OPEN; both could not be true): the shallow-CUT content — routing + EXACTLY-ONE shallow decomposition with exhaustive-exclusive read ownership + the weight-preserving W_Ŝ regrouping — is HYPOTHESIS-GRADE → **§3.1 (H8) = (K-CUT)** (an OPEN kernel of T-6, M04 Lemma 3.3's hypothesis, consumed inside Step 18's K1 regrouping — clause (R); clause (UB) through the transported chain); Step 2 supplies only the SOLVE face (T-8's block-triangular solve + T-5 domains, VERIFIED-DUAL — no cut-decomposition claim); the production burden is PROVED Lean-core at every chain (`w17ii_pin`, census §2.3 — the banked burden retired). SYNCED at REVISION 16, r17 — **the (H8) row RETIRED on the round-3 discharge gate**: the cut content now routes to the RETIRED (K-CUT) package (machine-proved Lean-core, exact-form K1 tie) + the residual interfaces priced at **§3.1 (H8′)** [re-priced at REVISION 17, r18, executing VC19 — the r17 owner labels are retirement routes, not homes] — the §3.2 K-CUT retirement row; this item's own CL-17(ii)/W17ii home additionally carries the package's deliberately-absent W-resummation leg (the (H8′)(iii) POOL-DICT route's W-leg) |
| 13. CL-18 (J-RAT); CL-19 (JC-INV) | per-cell rational tables; joint-cell invariance | (J-RAT) → Step 2 (T-7s tables) + Step 14 (census polynomials) + Step 17 (M1 r4: family-(i)/(ii)/(iii) entries ARE masses CONDITIONAL on C_e, families (iv)/(vi)/(vii) excluded at (a″) [re-scoped at REVISION 10, r11, edit E6]); its general-n table BUILD = Step 18's K3 + (H5)/(PACK). (JC-INV) = `cl19_rep`: REFUTED AS STATED (K-7 → refuted, §3.3 closed/refuted roster; M11's 4 ≠ 0 countermodel at n = 2) — the mandatory path never consumes it: D-11 needs per-box lift uniformity instead (D-15, Step 6; DELTA-3 §D3.1, zero ∼_OM/OB2 touch-points); the repaired carrier form = O-14b, §3.3, consumed by NO §2 step |
| 13a. CL-13 CTS-M(i) SYNTAX | the ONE fixed finite p-independent state set | → Steps 2/3 (T-4's letter catalogue + O-13/D-12's p-free Sk(n); `cl13_wf` DISCHARGED — `menuWFT_holds`, census §2.3) |
| 13b. CL-14 M1 + M5 | marked CLOSED in-list (ledger echo) | → Steps 2/3 (the finite branch-shape catalogue); residuals route per the list itself (VP → item 10; SP-FIN/SP-OUT → item 13a) |
| 13c. CL-15 X.1a per-species GMN index dictionary | envelope-constants dictionary | EVICTED by consumer audit **[NEW — VC7]**: CL-15's own text names its consumers as X.2's constants + the depth ⟹ disc envelope — the R-QNT family (items 2/3/4); distinct from item 4's proof duty but same disposition; the count-native discriminant bound is Step 13's Lemma C |
| 13d. CL-16 M4b height-translation constancy + M4b-T equivariance (π_v) | count-closure at fixed template | → Step 2 (T-5's interface at its VERIFIED-DUAL perimeter); the beyond-perimeter audit face = M4b-T-AUD, one of item 14's six fences (off-path) |
| 14. the six H.6 fences (XHD-d-EX(∂), M4b-T-AUD, U-R-COUNT, XHD-u-X, H-LIST-GEN, E-POS) | the H-DOMAINS perimeter's open kernels | → Step 18's K3-a fence display + §3.1 (H6) item (vii): NO fence is consumed as an open general lemma; the consumed residue is exactly in-fence CONFORMANCE ((H6)(vii)) with the displayed escalation clause; roster/owners at tree DELTA 4 §D4.2 — the fences stay off the mandatory path |
| 15–21. event blockers / unrun censuses (STATE-probe-2; X-n3 + STALL-probe; ESCAPE-probe (a)/(b); TRACK-probe; RESUM-n3 + (ORC-1); SIB deg-≥5, H-n3 re-seal, HMC-probe, n = 5 VALUE, REL-n4; gate SQUEEZE-n3) | the plan-§7 empirical roster | NOT mathematical kernels — tree §0's standing rule (numerics are FLAGS, never substitutes) means Step 19 consumes NO census; their successors are the §5 matrix + the per-leaf countermodel-first falsifiers **[disposition NEW at this table — VC7]**. Executed members: RESUM-n3 65/65 (item 19); STATE-probe-2, H-n3 151/151, V-n3 178/178 (items 15/20, §5's sealed T-layer gates); X-n3 layers ran (item 16 — output: the recorded X.1b-reading refutation, R-QNT content); N6-regp/ext2000 = the escape/margin gates (item 17's modern face, 25/25 + 303/303); N1/N2 = the squeeze/density ties to n ≤ 5 (item 21's class, executed more broadly than SQUEEZE-n3's spec). Never-run members (TRACK-probe, STALL-probe, HMC-probe, REL-n4, SIB deg-≥5) gate only R-QNT/fence content — off-path; root criterion 3 (§6: the §5 matrix re-runs green at acceptance) is the standing empirical bar. **Scope of this ruling, sharpened [REVISION 7, finding VC7-5]:** the disposition is a CONSUMPTION ruling ONLY — none of items 15–21 is a (ROOT-C) hypothesis — and NOT a retirement of any acceptance gate: the §5 rerun acceptance-gate obligation (root criterion 3) stands in full |

**Table B — the Lean-bridge kernel rows (census §2.3 / `BridgeKernels.lean`),
cross-walked.** These are the frozen ledger's Lean materialization ("the BK rows
below are the ledger CONTENT" — census §2.2); every row lands on a home above.

| Lean row | §U.SQ.6 face | ROOT home | tree-verified status (census §2.3) |
|---|---|---|---|
| `BridgePre.hdet` | (REG-p)/(r1)'s symbolic determinant face (SQ.4's own hypothesis schema, not a numbered item) | Steps 4/5 (+ Step 2's D1.5(f) (REG-p)-conditional T-8 re-scope) | ratified premise; `RegPin.detHyp` derives it from any (REG-p) prime |
| `BridgePre.hStateNe` | per-block roster nonemptiness (§S-RESUM vocabulary) | §3.3 O-14c row — consumed by NO §2 step; IB-B2 derivation attempt standing | ratified premise (drops if the derivation closes) |
| Ctor.`transfer` | item 10's TREE-N stability face | Step 6 — D-14's per-site noncircular form IS the repaired mathematics; the refuted form is the BOX-level reading O-1thr's requantification removed | REFUTED at the canonical instance as typed (M03 Eisenstein family) — fence event 25, Lean-side per-site guard re-type owed (§3.3 parked-engineering class) |
| BK.`slice_bound` | item 8 (CL-7) | EVICTED (O-6, R-QNT) | carried, permanently fenced |
| BK.`series_tie` | the [3t] solve-side seam | Step 18 (O-11 SERIES-TIE — the same statement; proved by the M04 walk re-priced at D-9) | OPEN-MATH REDUCED (M04) — the Lean row awaits the O-11 chain's formalization; the MATH home is Step 18 |
| BK.`env_tendsto` | item 5 (CL-4) | Step 13 | M05 PARTIAL-VERIFIED (semantic core proved, every prime incl. wild); residual = the classifier tie = (AGR), §3.2 |
| BK.`vp_sound` | item 10's VP-SOUND | Step 12 + Step 6 (D-15's ns-free verdict classes) | REFUTED AS BUILT (M06: finite-level ns-verdict without the ns-free clause) — the refutation is exactly why D-15/T-6 and the §2 standing preconditions are ns-free-scoped; Lean repair = fence event 24's NsFree clause |
| BK.`sibjc` | item 10's (SIB)/(JC-multi) | Step 15 + (H7) (the adaptedness presentation is precisely what the Lean type omits) | PLAUSIBLY FALSE AS TYPED (M07: CellData lacks an adaptedness law) — fence event 27; the countermodel targets the UNGUARDED type, not O-10's adapted-cell theorem |
| BK.`cl6` | item 7 (CTS-M(iv)-POLY) | Step 14 (M08/O-9 census) + §3.1 (GR-B)/(FRESH)/(ADM) | n = 2 POSITIVE (compiled witness); general n OPEN-MATH = exactly the census layer's displayed conditionality |
| BK.`cl11_ksub` | item 11 | PROVED at all n (M09, Lean-core) | discharged |
| BK.`cl17` | item 12's production burden | PROVED at every chain (`w17ii_pin`, Lean-core) | discharged |
| BK.`cl19_rep` | item 13's (JC-INV) | REFUTED as stated; O-14b (§3.3, parked); mandatory path = per-box uniformity (Step 6) | refuted (M11); queue item 22's carrier re-type parked (math-first directive) |
| seam.`count_tie` | CL-10's TREE-N/[3t] count row | Step 6 (D-15's CYL law — the ns-free-scoped form) + Step 2 (T-3/T-6 mass laws) + Step 17 (M1) | REFUTED AS TYPED (M02: bridgeTree lacks the ns-free clause; exact-square ×p family) — fence event 24; post-repair REDUCES to the CYL law = D-15's displayed content |
| `cl13_wf` | item 13a's well-foundedness face | Step 3 | DISCHARGED (`menuWFT_holds`) |
| (`bridge_slice_finite`) | former slice_finite row | Step 3's finiteness at the instance | PROVED 2026-07-30 (left the pack; Lean-core probe theorems) |

**Verdict of the executed audit.** Every §U.SQ.6 item and every Lean-bridge kernel
row lands on a §2 step at its declared statement, a recorded eviction/annex
disposition, or a §3.1 row already carried by (ROOT-C) — NO item names mathematics
outside the walk. [The concluding claim "no new hypothesis surfaced" is CORRECTED
at REVISION 11, r12 — finding VC11-3: it was true of the REVISION-6 execution but
is STALE since REVISION 10, r11 — item 12's shallow-cut content is now priced as
**(H8) = (K-CUT)** and item 6(iii)–(ix)+7's M3 route carries **(H9) = (NSF)**;
both are §3.1 rows carried by (ROOT-C), so the landing invariant above still
holds, but the table DID surface hypothesis-grade content once re-adjudicated at
the r11 scope.] [(H8) subsequently RETIRED at REVISION 16, r17 — item 12's
landing is now the §3.2 K-CUT retirement row (the machine-proved package) + the
§3.1 (H8′) residual-interface row [re-priced at REVISION 17, r18, executing
VC19: the r17 owner homes did not house the residues]; (H9) subsequently
RETIRED at REVISION 19, r20 — item 6(iii)–(ix)+7's M3 route now lands on the
§3.2 H9 = (NSF) retirement row: the landing invariant still holds — every
item lands on a displayed home.] The compiled refutations in the
row set (M02/M03/M06/M07/M11) all target UNSCOPED Lean typings whose mathematical
repairs are the ROOT's own displayed scopings (ns-free preconditions, the per-site
quantifier form, adapted cells, per-box lift uniformity) — none touches a consumed
statement. Three dispositions were NEW at this table (items 4/13c by consumer audit;
items 15–21's roster disposition) and were VC7 charge items — **VC7 CONFIRMED them**
(items 4/13c at finding 6: only the evicted R-QNT family consumes X.1a/X.1b/CL-15,
with Step 13 supplying independent count-native discriminant control; items 15–21
at finding 5, as a consumption ruling — wording sharpened per advisory VC7-5, and
the 6(iii)–(ix)+7 residue list corrected per advisory VC7-3); everything else
restates a recorded adjudication (DELTA-1 §§D1.4/D1.5, DELTA-2 §D2.3, DELTA-3
§D3.1, D-9, TREE-GAPS G-4/G-5) with its pointer displayed.

---

## 4. LITERATURE ROSTER (the goal's conditionality class)

Every external citation the assembled proof consumes, with its pin status. Discipline:
tree §4 rule 5 (verbatim published-text transcription + faithfulness entry + guardian
audit + sign-off before any axiom-grade use); the LITUNIT
(`lean/notes/openmath/LITUNIT_2026-08-01.md`) resolved every GD23 flag MATCH with
verbatim quotes at named extraction lines.

1. **[GMN] Guàrdia–Montes–Nart**, *Newton polygons of higher order in algebraic number
   theory*, Trans. AMS 364 (2012) 361–416 (= arXiv:0807.2620v2). THE machinery import.
   Pinned loci (via `docs/GMN_citations.md` + LITUNIT): Lemma 2.2(2); Prop 2.7(1)(3)(4);
   Lemma 2.17(2)(3); Def 1.8; Thm 1.15/1.19/Cor 1.20; Thm 2.11 + Defs 2.19–2.21;
   Thm 3.1/3.7, Cor 3.3/3.8, Defs 3.9/3.10, eq. (37); Lemma 3.11(3); Thm 4.18 (theorem
   of the index — declared, guardian-audited Lean axiom, read-set-restricted) +
   Cor 4.19 (termination); Defs 4.11/4.12/4.15. **LIT-4/(OM-SAT) rides this entry by
   id [added at unit TREE-GAPS, audit A-2]:** a realized OM branch with residual
   multiplicity 1 at its last read certifies an irreducible factor with e = e₁⋯e_r,
   f = f₀⋯f_r (Cor 3.8 + Def 3.9; order-1 instance Cor 1.20 — the pins Steps 11/12
   already consume); audited scope **"K = ℚ_p exactly"**, consumed only through the
   dictionary node O-2; its K_δ (δ > 1) re-scope duty is exactly (UB-X)(a) content
   (the per-leaf O_δ re-base audit, §3.1) — named here rather than ridden
   generically. Status: LITERATURE-CONDITIONAL
   (printed-TAMS pagination CONFIRMED for the (OM-SAT) slice [REVISION 14, r15]:
   Cor 1.20 / Cor 3.8 / Def 3.9 / Def 3.10 + eq. (37) / Lemma 3.11 / the base-field
   convention, verbatim at printed pp. 374/398–399/399/399/400/366 — unit D12R-U3,
   `lean/notes/openmath/H3_omsat_brief.md` §1, no arXiv-v2 numbering drift; the
   REMAINING pinned loci still owe the LIT-1/GD-7 printed-source session — §3.3,
   which now holds the brief's located-page map as input). Consumed by Steps 7, 8, 11
   (CU-2t consumes Cor 1.20/3.8 directly — added at REVISION 3, finding A6(i)), 12,
   13, 14, and 15 (K-FACT = Thms 1.15/1.19 + 3.1/3.7 — Step 15's pinned perimeter,
   all four loci above) [Step 15 added at REVISION 2, finding V2-7].
2. **[ML] MacLane**, *A construction for absolute values in polynomial rings*, Trans.
   AMS 40 (1936) 363–395. Pinned (LITUNIT, re-typeset caveat displayed): Thm 4.2 (the
   (V1) multiplicativity), Def 4.1, Thm 10.2, Lemma 10.1, Lemmas 11.1–11.2, Thm 12.1,
   Cor 12.2 (commensurable hypothesis automatic here). Consumed by Steps 6, 8, and 9
   (Step 9's loci: Def 4.1 + Thm 4.2 — the (V1) frame-semantics pin, LITUNIT §4
   verdict MATCH; consumed as the intended semantics of T-1/T-2's frame vocabulary,
   no further MacLane theorem — Step 9 added at REVISION 2, finding V2-7) (and
   (GR-B)'s pinned targets).
3. **[FGMN] Fernández–Guàrdia–Montes–Nart**, *Residual ideals of MacLane valuations*,
   J. Algebra 427 (2015) 30–75 (= arXiv:1305.0775). Pinned: Thm 4.2 (BOTH printed
   pairings; exact match verified clause-for-clause at GD23 r3), Cor 4.4, Thm 4.8,
   Cor 4.9, Cor 5.6, Thm 5.8, Cor 5.12, Prop 5.14/Cor 5.15, Lemma 5.3 (properness).
   Consumed by Step 8 and (GR-B)'s target statements.
4. **[Serre LF] Serre**, *Corps Locaux / Local Fields* (GTM 67). Ch. II §2 Prop 3
   (integral closure is a DVR — the declared axiom `AX_integralClosure_dvr`, guardian
   audit PASSED, V3: FAITHFUL/EXACT); Ch. II §2–§3 (unique valuation extension;
   Σe_if_i = [L:K] defectlessness); Ch. III §6 Prop 13 (wild different bound, June
   L5fix); **Ch. I §6 Prop 15, printed pp. 17–18 (B_f = A[X]/(f) a DVR with maximal
   ideal m·B_f and residue field k[X]/(f̄) when f̄ is irreducible — the (HL-c)
   read-off of the D-12r pin, CONFIRMED at unit D12R-U1,
   `lean/notes/openmath/H3_henlift_brief.md` §3; added to this consumed-loci
   enumeration at REVISION 14, r15, per VC16 finding 2)**. Consumed by Steps 1, 12,
   13 (Facts EXT/EF), B-6, and the item-10 D-12r pin ((HL-c)).
5. **[Neukirch] Neukirch**, *Algebraic Number Theory* (Grundlehren 322). Thm II.4.8
   (valuation extension; the M16-precedent high-confidence pin); II §4 (4.6)
   "Hensel's Lemma" = LIT-3 coprime-Hensel — **numbers CONFIRMED at printed p. 129
   [REVISION 14, r15 — the honest state per VC16 finding 2]:** unit D12R-U1
   (`lean/notes/openmath/H3_henlift_brief.md`) confirmed the statement + page (index
   cross-check "Hensel's lemma 129") and RAN the Mathlib probe — answer **ABSENT**
   (`HenselianLocalRing` is root-lifting only; no coprime-factor lifting), so the pin
   STANDS AS A PAPER CITE; the printed (4.6) states EXISTENCE only — the uniqueness
   clause is ABSENT and is supplied by the brief's §2 displayed companion lemma (the
   (HL-b) disposition, never a silent strengthening); the optional Lean
   materialization (D12R-U2a/b/c) owes its own axiom-policy loop (citation +
   faithfulness entry + guardian audit) when it lands; II §6 region (fundamental
   identity). Consumed by Steps 12, 13, and the item-10 D-12r pin ((HL-a)/(HL-b)).
6. **[Lang] Lang**, *Algebra* (rev. 3rd ed., GTM 211), Ch. IV §8
   (resultant/discriminant; number flagged for page verification) — O4T Fact D.
   **[Cohen] Cohen**, *A Course in Computational Algebraic Number Theory* (GTM 138)
   §3.3 (coefficient-side resultant — also what the harnesses compute). Step 13.
   **O4T import inventory — COMPLETE at rev 3 [REVISION 1, finding 8]:** the eight
   named Facts beyond NP/SF/HEN are EXT, EF, D, GRD, UCT, HRG (rev 2, sources as
   displayed in the O4T record) **+ SEP (char-0 separability of irreducibles) + DES
   (Galois descent/fixed field)** — the pass-2 "Galois descent + one more" pair, both
   added at rev 3 (commit bb4326c) as IN-LINE-PROVED imports with consumption-site
   cite tags (standard field theory; no new external citation is consumed — the
   proofs are in the leaf). Six-vs-eight reconciled: EF and GRD each bundle two of
   V30's items.
7. **[LIT-8 split, per DELTA-1 §D1.5(d)]** LIT-8a Krasner + the 2v+1 polish constant
   (inside VERIFIED M05 Lemma D); LIT-8b Newton–Hensel; LIT-8c Vandermonde/resultant
   identities (Bourbaki *Algèbre* IV §6 / Lang); LIT-8d Igusa §7.4 change of variables;
   LIT-8e Gauss/Möbius counts. Each LITERATURE-CANDIDATE, transcribed at its use site.
   Consumed by Steps 1, 13, 15 and the B-layer.
8. **Standard graduate measure theory with displayed statements** (Dfloor's audit:
   Folland *Real Analysis* Thm 1.14 (premeasure extension), Thm 2.37 (product/Tonelli),
   Tychonoff/Carathéodory/π–λ). Step 1. **[Serre CA] Serre**, *A Course in Arithmetic*
   (the n = 2 split criterion used in Dfloor's unconditional instance).
9. **In-project Lean-audited axiom set** (not literature but axiom-grade trust,
   `docs/AXIOM_FAITHFULNESS.md`): the GMN index theorem (item 1's Thm 4.18 declaration)
   and — measure route ONLY, NOT consumed by this count-native assembly — the
   Denef–Igusa cell recursion (`PadicMeasure.AX_cellRecursion`).
10. **[D-12r] — TB-CAP's (τ-irr) completeness perimeter: the D4R.0-K(c)
    verdict-pin residual** [MOVED HERE from §3.1 [M] at REVISION 14, r15,
    executing VC16 finding 2 — the reclassification adjudicated WARRANTED: no
    D-12r-specific open mathematics remains; the residue is exactly the two
    literature pins below. A [T]/LITERATURE-class entry, not open mathematics].
    THE REDUCTION, dual-accepted and displayed (MOVES §T.5(2), T-ASSEMBLY rev 5,
    passes 5+6 clean + fresh-Fable pass — quoted verbatim at HDISCHARGE_H3 §2.1):
    D4R.0-K parts (a)/(b) DISCHARGED (D4R0K_DERIVATION rev 3, dual-verified);
    part (c) is owned by [3t] VP with TB-CAP per clause ((hen) proved; (irr)
    proved from the accepted corpus — see the TB-CAP(irr) sentence below; (ns)
    refuted-as-worded, replaced by the proved (ns-lump)+(ns-null) pair), and
    "**D4R.0-K(c) is therefore NOT completely discharged: what remains is exactly
    {VP-SOUND's cites}**" — the §T.2 status block names the set's TWO members:
    **(HEN-LIFT)** (coprime-factor Hensel lifting package, three-part) and
    **(OM-SAT)** (realized-branch irreducibility read-off). THE PINS, confirmed
    on file: (HL-a) existence = **[Neukirch]** II §4 (4.6), printed p. 129,
    CONFIRMED (item 5 above); (HL-b) uniqueness = printed clause ABSENT in (4.6)
    — DISPOSED via the displayed companion lemma carried by the brief
    (`lean/notes/openmath/H3_henlift_brief.md` §2, never a silent strengthening);
    (HL-c) read-off = **[Serre LF]** I §6 Prop 15, printed pp. 17–18, CONFIRMED
    (item 4 above; Neukirch frame pinned as alternate); (OM-SAT) = GMN Cor 3.8 +
    Def 3.9 — **rides the §4 GMN entry (item 1) by id**, TAMS pagination PASSED
    at printed pp. 374/398–400 (`lean/notes/openmath/H3_omsat_brief.md` §1;
    consumed only through the dictionary node O-2, whose conditionality it
    inherits — the item-1 rider). Mathlib probe RAN, answer ABSENT (root-lifting
    only; no coprime-factor lifting) — the optional Lean upgrade D12R-U2a/b/c is
    a genuine unit, NOT load-bearing for the door, owing its own axiom-policy
    loop when it lands. **TB-CAP(irr), the precise sentence [Codex pass-1
    finding 3]:** TB-CAP(irr) is PROVED at [3t] from the accepted §C rev-14
    Theorem C — a citation into the dual-accepted corpus, not an open
    hypothesis; this entry never carried §C's perimeter as its content.
    CONSUMED (perimeter-conditional wherever TB-CAP is consumed, the language
    now "perimeter-conditional on the §4 D-12r pin"): Steps 2 (T-6's declared
    TB-CAP conditionality), 3, 11, 12, and Step 19 — where T-6's leg serves the
    classifier-side counting tie inside the evaluation squeeze, at clause (R)'s
    conditionality — AND Step 16/clause (UB) through the (UB-X)(a)-transported
    dictionary/CU/drainage layers, the §1 B1 attribution's displayed entry
    [REVISION 9, VC10 advisory 2]; the REVISION-3 A6(ii) phrasing "the (SQ)
    bracket consumes T-6" is SUPERSEDED at REVISION 4, finding VC4-4.
    DEPENDENCY DECOMPOSITION, preserved verbatim (the H3 lead's routing table,
    HDISCHARGE_H3 §2.1 — why the OTHER conditions riding the [3t] acceptance
    record are NOT this entry's residual, each at its own priced home:
    (SIB)/(SIB-STEP)/(JC-multi) → Step 15 + (H7); X.3/CL-4 → the drainage chain
    (Steps 13/19, TREE-EXP/TREE-N X.3-free per MOVES §T.0); [2r] REL.2 →
    (H4b)/(UB-X)(a); the (τ-ns) (z-3) identification → D4R.0-K(b), discharged,
    with the residual application inside the (OM-SAT) cite; the O-2 dictionary
    identification → the §3.1 (H1) rows + the item-1 rider (priced there, never
    re-priced here); P(O5t r3) (Step 12's owed pass, which consumes the
    read-offs) → the §3.2 row. This entry's move emptied the former §3.1 row
    without absorbing any sibling row's debt. Status: LITERATURE-CONDITIONAL at
    its two pins (both pinned-confirmed); fully retires when the pins clear the
    standing §4/§3.3 literature discipline (LIT-1's remaining session does NOT
    gate this entry — its (OM-SAT) slice is already verbatim-confirmed).

**Explicitly NOT consumed:** [BNS] Bauch–Nart–Stainsby LMS JCM 16 (2013) — route
R-QNT only (DELTA-3: zero root-side touch-points); [DCD] Del Corso–Dvornicich — annex
A-2 only (the 2026-07-21 retirement stands); the companion 2212.00294 (excluded by
standing goal directive).

---

## 5. NUMERICS CORROBORATION INDEX (flags, never substitutes — tree §0)

Standing matrix (tree §5) + the Phase-B falsifiers, keyed to §2's steps. Every leaf
with a runnable falsifier RAN it before acceptance (countermodel-first discipline).

| suite / falsifier | what it corroborates (step) | result |
|---|---|---|
| n = 2 Lean capstone + `verification/census_n2_uniform.py` | THE ROOT at n = 2, machine-checked + external census (anchor) | exact at p = 2, 3 |
| RESUM-n3 | n = 3 densities at the gate standard (Steps 2, 18) — G-3 caveat [REVISION 13, VC15 advisory 3]: the "what it corroborates" reading is scoped by the §3.2 O-11 row's recorded finding G-3 (the "65/65 END-TO-END rederivation of the assembled pipeline" claim is unsupported beyond the FIVE displayed identities); the Step-18 corroboration is at those identities, not a demonstrated end-to-end walk | 65/65 exact, PARI 0 mismatches (flag; G-3-scoped) |
| N1 exhaust / N2 density-tie | bracket + drainage; the root numerically to n ≤ 5 incl. (5,5) (Steps 13, 19) | CONFIRMED (max ratio 0.858 < 1; sandwich ~1e-5) |
| `dfloor_checks.py` | Step 1 (incl. the T-F n = 3 interpolation certificate) | ALL PASS |
| O-13 enumerator reproduction | Step 3 (verifier re-implemented from text alone) | \|S_n\| = 1, 15, 53, 197, 509, 1460 exact |
| N6-regp + ext2000 | Steps 4, 5, 17 ((REG-p) emptiness, margins) | 25/25 gates; 303/303 primes < 2000, margin sharp 6/7 |
| O-1thr depth-2 falsifier (T-E/T-F harnesses) | Step 6's sheared convention | 41,720 instances 0 violations (7,472 violations of the UNsheared reading — the pin is load-bearing) |
| GD23 (L3+) harness | Step 8's order-1 exact-key seam | 871 forced samples, 0 findings |
| U6 / IFK-U6-FIRST + the depth-3 falsifier (CU-1's gate) | Steps 10–11 (no-phantom, counting tie, equal fibers) | 174/174 length-2 strata T = T̂, 369 chains, 0 phantoms, PARI 1600/1600; depth-3 gate SURVIVED pre-rev-1 |
| `O4T_transfer_check.py` | Step 13 (n ≤ 3 ceiling sharpness, envelope) | 2,534,851 exhaustive classes, 5 configs incl. wild, ALL PASS, max ceiling slack 0 |
| `o9_order2_census_check.py` (M08-T6) | Step 14 (order-2 census polynomiality) | 8,220,994 boxes, 1,128,288 order-2 instances, K1–K4+T2 0 violations, 18 cross-p cells, ε = 0 |
| `o10_kcount_harness.py` | Step 15 (K-COUNT fiber law; sealed predictions) | 68 PASS / 0 FAIL |
| N8 eq2 | Step 16 (the alphabet law) | 199,723 alphabets / 17 fields, 0 violations |
| `o11_seriestie_check.py` | Step 18 (incl. Finding F1's test at the assembled-pack proxy) | 38/38 billed; the O-11 pass-1 record corrects the live count to 37/37 (one test = discriminant proxy) — flag stands either way; n = 2 true triple identical in ℚ(q) |
| N3/N4/N5/N7 + the 22 sealed T-layer gates (D4-n3 404M boxes, SIB 32/32, T-n3, M-n3-V2, H-n3 151/151, V-n3 178/178, STATE-probe-2, …) | the §2.2 layer (Step 2) | ALL PASSING |

Matrix synthesis (adjudications ledger): no Fable-vs-Codex-vs-numerics conflict
anywhere; every refutation triangulated; every surviving conjecture numerically
supported. Root criterion 3 (tree §4): this matrix re-runs green at acceptance time.

---

## 6. Change discipline and next actions

This document is Phase C's spine. Update rules: it re-states the tree + DELTAs and the
wave-10 ledger; where they move (DELTA 4 at the next fold), a dated appendix here
records the delta — the body above stays byte-stable per the tree's own discipline.
Acceptance path (tree §4 Root): (1) empty §3.1 and §3.2 into VERIFIED (the [M] rows
via the GD campaign's order-≥ 2 arc; the [V] rows via the named passes); (2) ≥ 2
consecutive clean END-TO-END passes on this document by fresh verifiers, ≥ 2 models;
(3) the §5 matrix re-runs green; (4) the completeness audit: every tree node id appears
here or in the recorded eviction list (§3.3's final paragraph + the tree's DELTAs).

Immediate dispatch order (updated at REVISION 1; the O-9 E′ and O4T N3-BRACKET
repairs have LANDED): the D-11 and O-11 r2s (+ their re-passes); CU-1 r5 (in flight);
the O4T full clean pass; the CUC/CU-2t pass; the three registry passes
P(O-2a r5)/P(O-1thr r4)/P(O5t r3); O-10's pass 2; the (UB-X)/(R1)–(R4) write-up; then
the GD order-≥ 2 campaign for (GR-B)/(FRESH) — the last genuine mathematics between
this assembly and the theorem. [Superseded event: the pass-2 slot RAN as VC2 and
returned COMPOSITION-GAPS — dispositioned at §8 (REVISION 2); the next end-to-end
pass (VC3, fresh verifier) runs on THIS twice-revised document, and the ≥ 2
consecutive-clean counter is still at zero.] [Updated at REVISION 3: VC3 RAN —
COMPOSITION-GAPS, all attribution/ledger integrity, the mathematics CONFIRMED —
dispositioned at §9. Dispatch now: **VC4 (Codex) on this thrice-revised document**
toward the two-consecutive-clean bar; the wave-14 repairs LANDED at revision close
(460ea59/567814d/3956500: O-9 r3, CU-2t R2, O4T r4 + O-10 r2) — their confirming
passes (O-9 pass 3, the CU-2t R2 re-pass, O4T's acceptance pass) are the in-flight
debts, with the D-11/O-11 pass 3s; owed: P(O-2a r5)/P(O-1thr r4)/P(O5t r3),
P(O-8b/M14) (§3.2, new), the (UB-X)/(R1)–(R4) write-up; then the GD order-≥ 2
campaign for (GR-B)/(FRESH).] [Updated at REVISION 4: VC4 RAN — COMPOSITION-GAPS,
all attribution/ledger integrity again — dispositioned at §10; the confirming-pass
round resolved O4T → VERIFIED (leaf #7) but sent O-9 and CU-2t one more rung
(pass 3 UNSOUND → r4 FULL-attainment; R2 pass 0-critical → R3 three-row pricing);
the REG leg ran ZERO-REGRESSIONS (044a6d9, all 18 suites — root criterion 3 evidence
at HEAD, accepted) and tree DELTA 4 landed (unit TREE-GAPS: audit gaps G-1..G-5
homed/adjudicated, its ROOT touches executed in this document concurrently with this
revision). Dispatch now: **VC5 on this four-times-revised document**; in flight:
O-9 pass 4, the CU-2t R3 pass, the D-11/O-11 pass 3s; owed:
P(O-2a r5) (now carrying the CU-2t W3/2(D) delivery)/P(O-1thr r4)/P(O5t r3)/
P(O-8b/M14), the (UB-X)/(R1)–(R4) write-up + the (K3-δ) match display, the (PACK)
data-pass execution (the new (H5)); then the GD order-≥ 2 campaign for
(GR-B)/(FRESH).] [Updated at REVISION 5: VC5 RAN — COMPOSITION-GAPS, two blockers of
ONE genre (consumed [T] rows are open propositions) + the standing T-U advisory —
dispositioned at §11; the harvest resolved O-9 → VERIFIED (leaf #8) and CU-2t →
VERIFIED at scope (leaf #9): **VERIFIED = 9**, and the promoted content = the NEW
§3.1 (H6) group. Dispatch now: **VC6 on this five-times-revised document** (fresh
verifier; model diversity favors Fable for the eventual two-model clean bar); in
flight: the D-11/O-11 pass 3s; owed: P(O-2a r5)/P(O-1thr r4)/P(O5t r3)/P(O-8b/M14),
the (UB-X)/(R1)–(R4) write-up + the (K3-δ) match display, the (PACK) data pass
((H5)), the (H6) conformance-unit write-ups (the O-3 conformance routes for
(T-DEC)/(T-READ)/(T-VERD); the D-11 owner's item-(vii) general-n display); then the
GD order-≥ 2 campaign for (GR-B)/(FRESH) — the heavy remainder.] [Updated at
REVISION 6: VC6 RAN — COMPOSITION-GAPS, 3 blockers + 6 advisories, the blockers the
VC5 genre WALKED TO COMPLETION (the last consumed-open-in-[T] rows) — dispositioned
at §12; NO status syncs (a composition-only pass; VERIFIED = 9 stands). Dispatch
now: **VC7 on this six-times-revised document** (fresh verifier; the §12 charge
includes confirming the NEW §3.4 table); in flight: the D-11/O-11 pass 3s; owed:
P(O-2a r5)/P(O-1thr r4)/P(O5t r3)/P(O-8b/M14), the (UB-X)/(R1)–(R4) write-up + the
(K3-δ) match display, the (PACK) data pass ((H5)), the (H6) conformance-unit
write-ups, the NEW (H7) discharge unit (the K-LOC = M07 OL-3 lemma + the (I-τ)
presentation write-up), the GD-7 flag reading session (now a named acceptance
precondition — A-7); then the GD order-≥ 2 campaign for the ENLARGED (H1) group
((GR-B) + GD-1-beyond-1 + the GD-6 instances) and (FRESH) — the heavy remainder.]
[Updated at REVISION 7: VC7 RAN — COMPOSITION-GAPS, 2 blockers + 3 advisories,
the blockers BOTH attribution/warrant findings (Step 6's all-order KEY1
consumption without a displayed [M] member → the NEW (H1)(d); the
(PACK)-correspondence law-transfer inference → the (H5) PACK-i/ii/iii
enlargement), and findings 6–11 CONFIRMING the composition incl. the §3.4 tables
— dispositioned at §13; NO status syncs (composition-only; VERIFIED = 9 stands);
the §3.3 T-U audit row RETIRED on VC7's table confirmation. Dispatch now: **VC8
on this seven-times-revised document** (fresh verifier; charge at §13: verify the
two enlargements + the three advisory fixes — VC7's confirmations need no
re-litigation but must not regress); in flight: the D-11/O-11 pass 3s; owed:
P(O-2a r5)/P(O-1thr r4)/P(O5t r3)/P(O-8b/M14), the (UB-X)/(R1)–(R4) write-up +
the (K3-δ) match display, the (PACK) data pass + the three row-law displays at
𝔅_n ((H5), now incl. PACK-i/ii/iii), the (H6) conformance-unit write-ups, the
(H7) discharge unit, the GD-7 flag reading session; then the GD order-≥ 2
campaign for the ENLARGED (H1) group (now also (d), the degree-g > 1 KEY1
generalization) and (FRESH) — the heavy remainder.] [Updated at REVISION 8: VC8
RAN — COMPOSITION-GAPS, 1 blocker + 1 advisory, the narrowest residue of the
eight passes, both at the REVISION-7 repairs themselves ((H1)(d)'s key-degree
quantifier → REQUANTIFIED to read order, finding VC8-1; the PACK K2/K3
sub-attribution → synced, finding VC8-2), with the three r8 advisory repairs,
the TEN-row count, the walk, and acyclicity all CONFIRMED — dispositioned at
§14; NO status syncs (composition-only; VERIFIED = 9 stands). Dispatch now:
**VC9 on this eight-times-revised document** (fresh verifier; charge at §14:
the (H1)(d) requantification + the PACK sub-attribution sync + no regression);
in flight: the D-11/O-11 pass 3s; owed: P(O-2a r5)/P(O-1thr r4)/P(O5t r3)/
P(O-8b/M14), the (UB-X)/(R1)–(R4) write-up + the (K3-δ) match display, the
(PACK) data pass + the three row-law displays at 𝔅_n, the (H6) conformance-unit
write-ups, the (H7) discharge unit, the GD-7 flag reading session; then the GD
order-≥ 2 campaign for the ENLARGED (H1) group (member (d) now the order-≥ 2
KEY1 block — read-order membership) and (FRESH) — the heavy remainder.]
[Updated at REVISION 9 — ACCEPTANCE: VC9 (Codex) and VC10 (Fable, the
model-diverse pass) RAN on the identical REVISION-8 text and BOTH returned
COMPOSITION-SOUND with ZERO blockers — acceptance-path criterion (2) above is
MET (two consecutive clean end-to-end passes, two models, fresh verifiers;
counter = TWO) and the composition is ACCEPTED at (ROOT-C)'s displayed
conditional scope. VC10's three advisories (display hygiene only) are
dispositioned at §15 and repaired in place — post-acceptance hygiene: no [M]
member, step content, or clause attribution changed; the counter STAYS TWO.
The remaining acceptance-path criteria toward THE COMPLETE PROOF are (1), (3),
(4): (1) empty §3.1/§3.2 into VERIFIED — owed: the D-11/O-11 pass 3s (in
flight), P(O-2a r5)/P(O-1thr r4)/P(O5t r3)/P(O-8b/M14) + the NEW P(O-14a′ r3)
(the REVISION-9 grade-display row), the (UB-X)/(R1)–(R4) write-up + the (K3-δ)
match display, the (PACK) data pass + the three row-law displays at 𝔅_n, the
(H6) conformance-unit write-ups, the (H7) discharge unit, the GD-7 flag
reading session; then the GD order-≥ 2 campaign for the ENLARGED (H1) group
and (FRESH) — the heavy remainder; (3) the §5 matrix re-runs green at final
acceptance; (4) the completeness audit.] [Updated at REVISION 10 — SUSPENSION
CURED, ACCEPTANCE NOT RE-EARNED: the D-11 pass 3 ran CRITICAL (the stop-the-line,
ledger 44ce0a7 — acceptance SUSPENDED per `docs/PROJECT_STATE.md`); the five-round
repair design (`R11_REPAIR_DESIGN_V5_2026-08-04.md`) executed as Unit A (the D-11
r4 leaf, twice hostile-passed: pass 1 CRITICAL folded, pass 2 GAPS-ONLY with all
design-§7 obligations VALID as proofs) and Unit B (THIS document's r11 = §16:
Steps 17/18b re-pointed to the repaired true-scope leaf; (PACK-iv) into (H5);
NEW rows (H8) = (K-CUT) and (H9) = (NSF); E-M3 at Step 12; TWELVE rows ↔
(H1)–(H9)). Dispatch now: **VC-next — the fresh end-to-end bar on this
REVISION-10 text, counter ZERO of 2** (charge at §16: verify the r11 edits, the
twelve-row invariant, no regression); in flight/owed BEYOND the bar: the D-11
confirming clean pass on the leaf's REVISION-3 text + the O-11 pass 3, Unit C =
the sign-off-gated (NSF-KEY) execution ((NSF-KEY-RET) R-0..R-5), the (K1-CANON)
discharge (M04-side pin display or interface lemma), the (H8) retirement
adjudication (O-11 §8 charge (vi)), the (PACK) data pass now INCLUDING
(route-a)'s booking-profile duty, and the standing queue above ((H6)/(H7)
units, registry passes, (UB-X)/(R1)–(R4), the GD order-≥ 2 campaign).]
[Updated at REVISION 11 (r12): VC11 RAN on the REVISION-10 text —
COMPOSITION-GAPS, 4 blockers + 1 advisory, ALL r11 RIPPLE-SYNC defects (the
targeted r11 repairs themselves CONFIRMED) — dispositioned at §17 and repaired
in place as pure display syncs: Steps 16/19's evaluation/(UB) scope summaries +
the final ∎ run to the twelve-row (H1)–(H9) scope with (H5) at PACK-i/ii/iii/iv,
§3.4 item 12 re-adjudicated to (H8) = (K-CUT) with the stale "no new hypothesis"
concluding claim corrected, the 6(iii)–(ix)+7 residue list closed with (H9),
Step 18b's Consumes summary restored to (GR-B)/(FRESH)/(ADM). No [M] member,
step proof content, or leaf pointer changed; NO status syncs (VERIFIED = 9
stands). Dispatch now: **VC12 — the fresh end-to-end bar on this REVISION-11
text, counter ZERO of 2** (charge at §17: verify the five syncs + no regression;
the r11 repairs need not be re-litigated but must not regress); in flight/owed
BEYOND the bar: unchanged from the REVISION-10 list above.]
[Updated at REVISION 12 (r13): VC12 RAN on the REVISION-11 text — COMPOSITION-SOUND,
ZERO findings (counter ONE of 2); VC13 (Fable, the model-diverse pass) RAN on the
same text — COMPOSITION-GAPS, 2 blockers + 3 advisories, ALL the
stale-hypothesis-enumeration genre (the walk, twelve-row bijection, seams,
acyclicity, and (NSF-KEY) discipline CONFIRMED) — dispositioned at §18 and repaired
in place as pure display syncs, PLUS the class kill: the GLOBAL ENUMERATION AUDIT
(§18's audit record — every operative §§0–6 + §3.4 display enumerating hypothesis
groups/members/counts checked against §1's current (ROOT-C) display and synced).
No [M] member, step proof content, or leaf pointer changed; NO status syncs
(VERIFIED = 9 stands); the §3.2 O-11 row now RECORDS the un-banked 2026-08-03
hostile pass (VC13-4). A text change: **the counter RESETS to ZERO of 2**.
Dispatch now: **VC14 — the fresh end-to-end bar on this REVISION-12 text** (charge
at §18: the five VC13 syncs + the audit's claim "every operative
hypothesis-enumeration display matches §1" + no regression); in flight/owed BEYOND
the bar: unchanged from the REVISION-10 list above.]
[Updated at REVISION 13 — RE-ACCEPTANCE + the post-acceptance batch: VC14 (Codex)
and VC15 (Fable, model-diverse) both returned COMPOSITION-SOUND on the identical
REVISION-12 text — the bar is MET and the composition is RE-ACCEPTED at
(ROOT-C)'s twelve-row scope. REVISION 13 (§19) folds VC15's four advisories +
the discharge syncs ((NSF-KEY) RETIRED at the executed Unit C; D-12r rewritten
at its confirmed cites, reclassification VC-gated; (K-CUT) re-scoped to the
landed H8 development, NOT discharged; (E-N)'s mid-campaign rewrite adopted).
Dispatch now: **VC16 — the confirming end-to-end pass on this REVISION-13 text**
(charge at §19: the four advisory repairs + the four row syncs + no regression;
re-anchors the acceptance on the current wording). In flight/owed BEYOND VC16:
the D-11 confirming clean pass + the banked O-11 pass 3; the (K1-CANON)
discharge (after it, (NSF-INT) retires on I-1+I-2+I-3 and the (H9) row closes);
the H8 fan-out's unit-level hostile pass (the (H8) ratification gate); the H4
ROOT-sync briefs' firing conditions (M1 ×2 / M2 ×1 passes, M4 pass-2 record, M5
second clean, the N-K3 run, Asvin sign-off on the Route-B re-scope); the
M17/RegPFinite faithfulness-audit record (§3.2, new); the standing queue (the
(PACK) data pass incl. route-a's booking-profile duty, (H6)/(H7) units, registry
passes, the GD order-≥ 2 campaign).]
[Updated at REVISION 14 (r15): VC16 RAN on the REVISION-13 text —
COMPOSITION-SOUND, zero blockers (advisories only) — the acceptance RE-ANCHORS
on REVISION 13, and finding 2 adjudicates the D-12r reclassification WARRANTED.
REVISION 14 (§20) EXECUTES the move: D-12r [M] → the §4 literature pin (entry
10), §3.1 = ELEVEN rows ↔ (H1)–(H9) with (H3) = {(ADM), (E-N)}; the §4 roster
synced honestly (item 1's (OM-SAT)-slice pagination CONFIRMED, the LIT-1-wide
session still owed; item 5's Neukirch numbers CONFIRMED with the Mathlib probe
ABSENT — paper cite stands; Serre LF I §6 Prop 15 added to item 4). Dispatch
now: **VC17 — the confirming end-to-end pass on this REVISION-14 text** (charge
at §20). In flight/owed BEYOND VC17: unchanged from the REVISION-13 list above,
MINUS nothing (the D-12r move retires no pass debt) — the (K1-CANON) discharge,
the H8 fan-out hostile pass, the H4 firing conditions, the M17/RegPFinite
audit, the standing queue; PLUS the LIT-1 remaining-loci session (input: the
D12R-U3 located-page map).]
[Updated at REVISION 15 (r16): VC17 RAN on the REVISION-14 text —
COMPOSITION-GAPS, 1 blocker + 1 advisory, BOTH display-conformance at the r15
edits (Step 12 not locally re-pointed at the D-12r perimeter; Step 4's grade
outside the §0 vocabulary), with all other charged checks PASSING (the D-12r
move's faithfulness, the ELEVEN-row bijection, the walk, acyclicity, no
regression) — dispositioned at §21 and repaired in place as pure display syncs.
Dispatch now: **VC18 — the confirming end-to-end pass on this REVISION-15 text**
(charge at §21). In flight/owed BEYOND VC18: unchanged from the REVISION-14
list above.]
[Updated at REVISION 16 (r17): VC18 RAN on the REVISION-15 text —
COMPOSITION-SOUND, zero blockers, ONE advisory (the §3.2 end-to-end row stale,
repaired at this revision) — the acceptance RE-ANCHORS on REVISION 15. The
(K-CUT) round-3 discharge gate PASSED (`KCUT_pass3_2026-08-06.md`, DISCHARGES —
the review gate, not the semantic owner rows): REVISION 16 (§22) EXECUTES the
second post-acceptance row retirement — the (H8) = (K-CUT) row leaves §3.1 for
the §3.2 K-CUT retirement record (the repaired adjudication display +
`KCutMassFactor` + `KCutK1Tie`, machine-proved Lean-core; the 3-round pass
arc), its three semantic residues MOVED to their owning leaves (graft → the
D-14 + CU-3 + REL.2 arc; measured mass → the (H7)/TREE-EXP + [2r]/CL-8 +
O-1/O-3 owners; the τ/δ pool dictionary → [2r]/CL-8, W-leg at CL-17(ii)/W17ii);
the §3.1 roster is TEN rows — (H1)–(H9) minus the retired (H8). Dispatch now:
**VC19 — the confirming end-to-end pass on this REVISION-16 text** (charge at
§22). In flight/owed BEYOND VC19: the REVISION-14 list above MINUS the H8
fan-out hostile pass (run to discharge, rounds 1–3); the moved residues now
ride their owners' standing debts (the D-14/CU-3/REL.2 arc, the (H7) unit,
[2r]/CL-8, the O-1/O-3 count-tie); unchanged otherwise.]
[Updated at REVISION 17 (r18): VC19 RAN on the REVISION-16 text —
COMPOSITION-GAPS, 2 blockers, ONE defect: the r17 retirement OVERSHOT — the
three K-CUT residues were assigned to owners whose statements do NOT house
them (the previous bracket's "ride their owners' standing debts" is thereby
SUPERSEDED), so clause (R), and (UB) by inheritance, silently consumed
unpriced content at Step 18's K1; the (K-CUT) package + its §3.2 retirement
record CONFIRMED standing, acyclicity confirmed, no independent regression.
REVISION 17 (§23) EXECUTES the honest re-pricing: the NEW slimmed §3.1 row
**(H8′) — the K-CUT residual-interface row** (members GRAFT / MASS-ID /
POOL-DICT, stated from the package's own displays) houses the interfaces;
the roster is ELEVEN rows — (H1)–(H9) minus the retired (H8), plus (H8′);
(R)/(UB) gain exactly (H8′). Dispatch now: **VC20 — the confirming
end-to-end pass on this REVISION-17 text** (charge at §23). In flight/owed
BEYOND VC20: unchanged from the REVISION-16 list, with the former
moved-residue riders now reading as (H8′) discharge ROUTES (the
D-14/CU-3/REL.2 arc, the (H7) unit, [2r]/CL-8, the O-1/O-3 count-tie,
CL-17(ii)/W17ii).]
[Updated at REVISION 18 (r19): VC20 RAN on the REVISION-17 text —
COMPOSITION-GAPS, 2 blockers, BOTH leftover sentences (the (H8′) substance,
the ELEVEN-row attributions, the walk, acyclicity, and no-regression all
CONFIRMED): §1's retired-(H8) bracket still counted TEN live rows, and the
§3.2 K-CUT retirement row still carried the pre-r18 "owner-displayed
interfaces" / "priced at its owners" clauses alongside the r18 correction.
REVISION 18 (§24) repairs both in place — §1's bracket now reads ELEVEN with
the (H8′) pointer; the retirement row's WHAT-RETIRED sentence REWRITTEN to
one voice (proved conditional on the three interfaces, PRICED AT (H8′),
owner arcs retirement ROUTES only). Pure display syncs — no [M] member, step
proof content, leaf pointer, or clause attribution changed. Dispatch now:
**VC21 — the confirming end-to-end pass on this REVISION-18 text** (charge
at §24). In flight/owed BEYOND VC21: unchanged from the REVISION-17 list
above.]
[Updated at REVISION 19 (r20): VC21 RAN on the REVISION-18 text —
COMPOSITION-SOUND, zero blockers, ONE advisory (the §3.2 end-to-end row's
tail stale at VC19, repaired at this revision) — the acceptance RE-ANCHORS
on REVISION 18. The (K1-CANON) discharge LANDED (ledger entry H9-C3, commit
9abc3ab: the (ed-1) M04 §5.5 scope pin under the standing statement-change
authority, SURFACED for review; warrant note
`K1CANON_discharge_2026-08-02.md`; 2-round hostile pass, substance PASS both
rounds), retiring (NSF-INT) on (NSF-INT-RET)'s four grounds: REVISION 19
(§25) EXECUTES the THIRD post-acceptance row retirement — the (H9) = (NSF)
row leaves §3.1 for the §3.2 H9 = (NSF) retirement record; the roster is TEN
rows — (H1)–(H9) minus the retired (H8) and (H9), plus (H8′); (R)/(UB) each
LOSE (H9). RE-OPEN TRIGGER, displayed: the D-11 confirming clean pass (the
§3.2 D-11 row) — a §4B-defective finding re-opens I-3 and suspends the
(NSF-INT) retirement. Dispatch now: **VC22 — the confirming end-to-end pass
on this REVISION-19 text** (charge at §25). In flight/owed BEYOND VC22: the
REVISION-17 list MINUS the (K1-CANON) discharge (landed at H9-C3), with the
D-11 confirming pass now doubling as the (H9) re-open trigger; unchanged
otherwise.]
[Updated at REVISION 20 (r21): the leaf-status sync batch — VC22 had NOT
yet run, so no pass is recorded here; the five ledger-recorded events
folded into the standing rows: the (GR-B) leaf RE-ACCEPTED at its
REVISION 14 (58534d4; the extraction-artifact incident closed, N-1 bar
MET) and B-M2 (LIFT-CONF) ACCEPTED at its REVISION 14 (416c213) — the
(H1) row now displays both leaf records, member statuses unchanged [M];
H1-U14 STATED gate-positive (6df3eae) and H2-U9 LANDED (23d2c03) — the
(H1)/(FRESH) rows display the stated [M]-rows + their proved consumers;
III-S4 REPAIRED + H6-A4b LANDED (f56a536) — the (H6) row displays the
repair and the cleared A4b blocker. The roster RECOUNTED: TEN rows,
unchanged. Dispatch now: **VC22 — the confirming end-to-end pass, rolled
forward onto this REVISION-20 text** (charge at §26). In flight/owed
BEYOND VC22: the REVISION-19 list, MINUS nothing (no pass debt retires at
a sync batch); PLUS the Asvin review-surface queue (the `hf` Monic
pricing, the (ed-1) M04 §5.5 pin, the `TerminalPolySem` supplier — ledger
+ standing queue); the H1 campaign's remaining distance (GRADED-READ /
REC-SL / SITE-EXH / the readsOf witness debt; members (b)/(c)/(d) proper;
the (GR-B) leaf's open residue TR-3′-GEN / TR-3-ORD-X / D-ITER / TR-4);
the H2 U10 chain-keyed twins (`leakFree` typed carrier); the H6 remaining
members ((T-DEC)/(T-READ)/(T-VERD) semantic suppliers incl.
`TerminalPolySem`'s, item (vii)).]
[Updated at REVISION 21 (r22): **VC22 CONFIRMED** — COMPOSITION-SOUND, zero
blockers on the REVISION-20 text (TEN rows recounted; 8 honesty spot-checks
faithful; one metadata-only advisory) — the acceptance RE-ANCHORS on
REVISION 20. This revision records: the (H4a) F-chain COMPLETE at F-(i)
(M1/M2/M4/M5/N-K3 all landed per the ledger's 2026-08-08 entries) with
P(O-8b/M14) pass 2 FIRED on the current texts — GAPS-ONLY (0C/1G, the M14
Lemma-1(b) top-space display seam), so the (H4a) retirement is
READY-PENDING-CLEAN-PASS, NOT executed (roster stays TEN); the TR3-S1
stratum-served annotation ((H1) row); the (UB-X) Route-B adjudication
status (nothing discharged; Asvin sign-off among the five re-scope gates).
Dispatch now: **VC23 — the confirming end-to-end pass on this REVISION-21
text** (charge at §27; VC24 = the pass after it in the cadence). In
flight/owed BEYOND VC23: the REVISION-20 list, PLUS the (H4a) retirement
residue (the M14 Lemma-1(b) sync fold — the O-8b/M14 note owner's move —
then one clean composite pass, then F-(iv) per H4M7 §3); the H4M7 §4
Route-B re-scope queue (five conditions; M4/M5/N-K3 legs now green,
Asvin sign-off + the consumer-need walk outstanding); the **B2D Lean
campaign** (blueprint `lean/blueprints/B2DEF_LEAN_2026-08-08.md`, design
FROZEN at commit 90b4072 after two Codex rounds; **wave E LANDED** at
commit 1c10fe6 — `lean/LeanUrat/B2D/` 12 files + MANIFEST, GR-0 E-gate
PASS-with-displayed-residue (`hscale`/`hψ0`), 15 E-phase sorries; next
wave 1 = TD-0 + TD-3 + CL5 with N-TD1/N-GR1 built first **[STALE POINTER,
marked at REVISION 23, r24 per synthesis-pass-5 F6: those waves ALL
EXECUTED and landed — TD front sorry-free, CL5 proved, GR wave 2a + GR
ROUTE (a); B2D is at 0 sorries; the current queue is
`docs/SYNTHESIS_PASS5_2026-08-08.md`'s re-ranked list]**); and the **H4
pass-report archive** (`lean/notes/openmath/H4M{1,2,4,5}_pass*_2026-08-08.md`,
`PO8bM14_pass2_2026-08-02.md`, the N-K3 record at M5 §8 +
`verification/openmath/nk3_check.py`).]
[Updated at REVISION 22 (r23): **THE (H4a) RETIREMENT EXECUTED** — the M14
Lemma-1(b) fold landed (M14 REVISION 1), then composite passes 3 (Codex,
0C/0G) + 4 (Fable decider, nothing classifiable) ran consecutive CLEAN,
model-diverse: P(O-8b/M14) **ACCEPTED** (ledger 5a10256), F-(iv) fired per
H4M7 §3 — the (R1)–(R4) row leaves §3.1 for the §3.2 (H4a) retirement row,
the (K3-δ) audit note moves to the (UB-X) row (still consumed by clause
(R)), Steps 16/18 K3-c displays upgraded on the (R1)–(R4) leg, clauses
(R)/(UB) each lose (H4a); **the roster is NINE rows** (recounted). Also
folded from the same ledger batch: the **SITE-EXH arc** (probe 355c1eb →
sign-off 49b778e → E-1/E-2/E-4 landed f36ab72 (`siteExh_elimination'`
unconditional at the engine face, two-layer; `TowerMoveClosureL`
re-ratification PENDING ASVIN) → E-3 glue db47e35 (`gate_ftie_chain`
end-to-end; residuals `junc_bundle`/`DIterJunctionSupplier`/box face)); the
**B2D campaign state** (TD front SORRY-FREE end-to-end — TD-3 proved on v3
after the v1/v2 countermodel arc, TD-6's `transDeep_of_ledger` compiled
filling `fresh_of_transDeep` at the ledger-instance scope — the (FRESH) row
bracket; CL5 PROVED (942becd, the CI-1 unit-walk kernel, C6:129 closed);
GR wave 2a: 5/8 sorries proved + `graded_read_of_history` = the
consumer-ready (i-b) discharger, 3 frozen sorries OPEN-OBSTRUCTED at
abstract σ.e ≥ 2 frames — remaining B2D sorries = 3); the **V9 e′ ≥ 2
VACUITY-RESCOPE ruling** (d632305, §3.3 O-14a bracket); the **strata probe**
(8db1b00 — TR-3′-GEN(ii)'s closed-form twist law on record; D-ITER's cyclic
presentation FALSE, ITER-LAW measured, restatement owed **[EXECUTED at
REVISION 25, r26: 3bcae68]**); **B-M2
REVISION 15** (38d98f6 — the accepted opens re-priced, acceptance NOT reset;
census 226 rows; `readsOf_landing_K0` debt narrowed-not-discharged).
Dispatch now: **VC25 — the confirming end-to-end pass on this REVISION-22
text** (charge at §28; VC23/VC24 never ran, superseded — VC26 next). In
flight/owed BEYOND VC25: the REVISION-21 list MINUS the (H4a) retirement
residue (executed) and MINUS the B2D waves TD/CL5/GR-2a (landed), PLUS: the
`TowerMoveClosureL` layering re-ratification (Asvin); the H4M7 §4 Route-B
re-scope queue (unchanged — sign-off + consumer-need walk outstanding); the
B2D residue (GR-3 ×2 + GR-4 ×1 open-obstructed; the p ≠ 2 stage-forge
countermodel pricing; the §4 interface re-adjudication decision); the
SITE-EXH residuals (`junc_bundle` deep-instance unit, `DIterJunctionSupplier`,
the box face); the D-ITER restatement at the measured ITER-LAW; the V9
conspiracy-exclusion lemma; the U10 `leakFree` typed carrier + Step-10/K1
classifier face (the (FRESH) general-instance supplier).]
[Updated at REVISION 23 (r24): **VC25 RAN — NOT-CONFIRMED (1 critical +
3 gaps)** on the REVISION-22 text (report `lean/notes/openmath/
VC25_report.md`, archived ea9f615; the census + all recounts REPRODUCED);
ALL FOUR FINDINGS DISPOSED at this revision — the (H4a) retirement
propagated into the last three OPERATIVE displays (§3.4 Table A items
6(iii)–(ix)+7 and 9; Step 16 honest-scope clause (ii)) with (K3-δ) kept
live at the (UB-X) row; the F-(i) count corrected five → SIX records; the
granular F-chain pointers re-verified in-repo (audit-input artifact); the
stale 2026-08-02 close below marked HISTORICAL. Folded from the window's
ledger: **GR ROUTE (a) EXECUTED** (5b7f1ee — `ReadHyps` frame-pin fields
`hσs`/`hσt`, a fence-(vii) statement change under the standing authority,
zero consumers + believed-false at the bound scope; **B2D 0 sorries**;
`graded_read_of_history` signature-unchanged; the F5 stage-forge GR leg
DECLINED — **flagged for Asvin at §29**); **TR3-S2 attempt ON RECORD**
(330cc6b; hostile pass 1 CRITICAL 1C/2G at 1bd75de, REVISION 1 dispatched
— no status upgrade); the F6 hygiene batch (112b9ac). Dispatch now:
**VC26 — the confirming end-to-end pass on this REVISION-23 text** (charge
at §29). The acceptance citation stays at the REVISION-20 text (VC22)
**[superseded at REVISION 24, r25: VC26 + VC26b CONFIRMED — the acceptance
RE-ANCHORS on the REVISION-23 text; the r25 bracket below is current]**. In
flight/owed BEYOND VC26: the REVISION-22 list MINUS the B2D GR-3/GR-4
disposition (executed at GR ROUTE (a)) and MINUS the p ≠ 2 stage-forge GR
pricing (DECLINED — the forge re-prices against junc_bundle/readsOf need
only, synthesis-pass-5 F5), PLUS: the TR3-S2 REVISION-1 fold + its
hostile-pass arc; the synthesis-pass-5 queue tail (REC-SL suppliers, the
readsOf consumer walk, the countermodel-first batch, the small-units
dispatch, the D-ITER restatement).]
[Updated at REVISION 24 (r25): **VC26 + VC26b CONFIRMED — the acceptance
RE-ANCHORS on the REVISION-23 text** (VC26: 0C/1G, everything countable
reproduced, the gap an audit-input artifact — the N-T4 pointer resolves to
the §N gate record embedded at `H4M1_rpack_attempt.md:1091–1186`; VC26b on
the completed appendix: CONFIRMED 0C/0G with the embedded N-T4 script
independently re-executed, 13/13; reports 565aa28 + b6121a5; the anchor
moves REVISION 20/VC22 → REVISION 23/VC26+VC26b) **[superseded at
REVISION 25, r26: VC27 + VC27b CONFIRMED — the acceptance RE-ANCHORS on the
REVISION-24 text; the r26 bracket below is current]**. This revision folds the
wave-8 batch: **TR3-S2 ACCEPTED** (6663aa4) — TR-3′-GEN CLOSES AT ORDER 2 as
a two-stratum theorem (S3.4 REV-11 form; units-only phrasing permanently
refuted via Lemma WIT; ORD-INV on record; deeper orders open; the GRB note's
post-acceptance bracket applied at this unit); **REC-SL NARROWED** (1adac62
+ 85e11a4: closed at every compiled site, gates positive on H₃rc; surviving
rows = the `hsc` StageCore-provenance row on abstract carriers + the
`hfence` GR0b recentering-chain fence; B-M2 REVISION 17 applied at this
unit); **the readsOf debt RESTATED at the `ReadsOfFrom` face** (F3 walk
(a)/NO-CONSUMER 878ac17; B-M2 REVISION 16 applied e7a673a; species form = a
fence display of the dead From→ReadsOf converse); **HUNI-V2 LANDED**
(375487c refutation → 058aaf3 v4 carrier repair: huni
displayed-and-satisfiable at `ledger_freshRowOnV4`; H2 statements
byte-unchanged; B2D 0 sorries incl. v4). Dispatch now: **VC27 — the
confirming end-to-end pass on this REVISION-24 text** (charge at §30). In
flight/owed BEYOND VC27: the REVISION-23 list MINUS the TR3-S2 fold
(accepted), MINUS the REC-SL supplier question (narrowed to the two
displayed rows), MINUS the readsOf consumer walk (executed, no-consumer)
and MINUS the huni countermodel item (repaired at v4), PLUS: **the F5
deep-instance forge now priced against `junc_bundle` ALONE** (GR leg
DECLINED at ROUTE (a), readsOf leg DEAD at no-consumer — one gate, the
deg f > deg Φ₁ merged-junction run); the **StageCore docstring doc-debt**
(`Moves/DefsCore.lean` area: the docstring cites the RETIRED FAITHLESS
MovesGr held units L1.baseStage_exists/L4.TRANSstage/L5.recTRANSRS — the
live chain is HC1 `T1_baseStage`/`S9_transStage`/`S10_recStage` per the
RECSL_WIRE trace; displayed here, NOT edited at r25); the D-ITER
restatement + `DIterJunctionSupplier`; the small-units dispatch
(TR-4 harness family, TerminalPolySem k′ = 0, leakFree general-instance
face); the ADM-U0/POOL-DICT countermodel-batch tail (the ADM-U0 queue line
itself STALE — executed at H3 Wave 0 e1cf117; §30 disposition); the Asvin
queue (TowerMoveClosureL re-ratification; Route-B re-scope; the §29
fence-(vii) ROUTE (a) flag, now also carrying the RECSL `hσt`-pin
consumption).]
[Updated at REVISION 25 (r26): **VC27 + VC27b CONFIRMED — the acceptance
RE-ANCHORS on the REVISION-24 text** (VC27: 0C/2G, ALL substantive checks
passed — 3/3 re-anchor sites, the 17-pattern census arithmetically exact,
NINE rows, clauses 8/8, falsifier 0; both gaps AUDIT-INPUT appendix defects,
the recurring genre now a saved orchestrator checklist; VC27b on the
verbatim slices: CONFIRMED 0C/0G; reports 5e8dc97 + 6aa3b20; the anchor
moves REVISION 23/VC26+VC26b → REVISION 24/VC27+VC27b) **[superseded at
REVISION 26, r27: VC28 CONFIRMED FIRST-PASS — the acceptance RE-ANCHORS on
the REVISION-25 text; the r27 bracket below is current]**. This revision folds
the wave-9 batch: **POOL-DICT DECIDED countermodel-first** (d7f7f58 —
derive-from-wiring REFUTED by the compiled `PoolDictProbe.lean`
underdetermination witness; (H8′)(iii) confirmed the honest form, now
blocked-on-owners WITH THE BLOCKER COMPILED; τ-leg = the two-equal-state
table, forge-class); **the TR-4 EMPIRICAL debt DISCHARGED** (4761cdd —
TR4a–f, 19,226 samples 0 violations, preregistered; TR-4 stays
paper-secured, no proof-status change; GRB brackets additive); **the CU2t
k′=0 charter SPLIT-decided** (9cb30d1 — 3 compiled negative gates refute
literal derivability; `terminalPolySem_kp0` PROVED, the first
ReadThroughIota-certified `TerminalPolySem` witness at HEAD; NEW named open
row `BasePolygonLengthLaw p`, displayed at the (H6) residuals); **leakFree
TYPED** (d237149 — `LeakFreeCarrierV4` + the general supplier
`ledger_leakFreeV4` + the de-vacuity fence; TransDeep rows carry a PROVED
(L) field; the H2 field stays bare per the U9 fold); **D-ITER RESTATED**
(3bcae68 — the ITER-LAW is the statement of record, open lemma 0/804; the
cyclic form refuted 22/28 and bracketed at all display sites; the
`DIterJunctionSupplier` consumable spec on record; queue #11 unblocked).
Dispatch now: **VC28 — the confirming end-to-end pass on this REVISION-25
text** (charge at §31). In flight/owed BEYOND VC28: the REVISION-24 list
MINUS the small-units dispatch (all three executed: TR-4 harness,
TerminalPolySem k′=0, leakFree general-instance face), MINUS the D-ITER
restatement (executed) and MINUS the ADM-U0/POOL-DICT countermodel-batch
tail (ADM-U0 was stale at r25; POOL-DICT decided here), PLUS: **the
CONSOLIDATED FORGE QUESTION — THREE named forge-class targets**
(`junc_bundle`, the surviving F5 justification — the deg f > deg Φ₁
merged-junction gate at choice-built stages, the D-1 wall; the
nonempty-parent lawful leakFree instance; the two-equal-state table for the
POOL-DICT τ-leg) with the chartering decision = **synthesis pass 6 (OWED,
the /goal cadence — this window's collapses need re-mapping)**;
`BasePolygonLengthLaw p` (the OL5-adjacent convexity unit, proof route in
its docstring — the CU2t note's named next target); the Step-10/K1
classifier consumer face (the leakFree CONSUMER, still owed); the
`DIterJunctionSupplier` formalization (queue #11, unblocked on #9, priced
with the forge); the StageCore docstring doc-debt (displayed at r25, still
NOT edited — rides a future Lean-touching unit); the Asvin queue
(TowerMoveClosureL two-layer re-ratification; the H4M7 §4 Route-B re-scope;
the §29/§30 fence-(vii) ROUTE (a) flag lineage incl. the RECSL `hσt`-pin
consumption).]
[Updated at REVISION 26 (r27): **VC28 CONFIRMED FIRST-PASS — the acceptance
RE-ANCHORS on the REVISION-25 text** (VC28: CONFIRMED 0C/0G, the FIRST
first-pass confirmation of the cadence — all folds verified at their sites,
the 26-pattern census reproduced exactly, provenance fully resolved with
the appendix built per the saved checklist; report
`lean/notes/openmath/VC28_report.md`, d66bef2; the anchor moves
REVISION 24/VC27+VC27b → REVISION 25/VC28) **[superseded at REVISION 28,
r29: VC29 CONFIRMED FIRST-PASS — the acceptance RE-ANCHORS on the
REVISION-27 text; the r29 bracket below is current]**. This revision ADOPTS SYNTHESIS
PASS 6 (`docs/SYNTHESIS_PASS6_2026-08-08.md`, 976484d) as the DISPATCH OF
RECORD: **the FORGE DECISION** — `junc_bundle` CHARTERED ALONE (staged
numerics-first: the deep compiled deg f > deg Φ₁ run preregistered from
numerics with the clause-(iii) residual traced BEFORE the Lean build →
`gate_glue_junction` un-hypothesized → the `DIterJunctionSupplier` gates),
the nonempty-parent leakFree instance DECLINED (revisit only if the
classifier consumer face demands it for non-vacuity) and the
two-equal-state table DECLINED (a second leg of a compiled blocker is not
progress) — dispositions displayed at the (FRESH)/(H8′) rows; the GR
stage-forge stays DECLINED, the readsOf run-forge stays DEAD;
**BasePolygonLengthLaw CHARTERED HIGH** (it IS the convexity gap; route
quarried from proved kernels; still an OPEN lemma until its unit lands);
**the F3 collapse map** (the Step-10/K1 classifier consumer face
ATTEMPT-GRADE, queued; `hsc` PERMANENTLY hypothesis-shaped — optional S
witness only, no standalone dispatch; depth-3 NOT finite-algebra — a
preregistered ORDER-3 all-e_j = 1 probe first; junc_bundle = B-M2's only
non-hypothesis-shaped wall); **the (ADM) row corrected NOT BLOCKED** —
G-ADM-2 never ran and is the row's countermodel-first entry (preregistration
sealed pre-run at 4d7dba1; results ride r28); **the sorry census refreshed**
(36 sites repo-wide, B2D 0 — re-verified at r27: 35 in `LeanUrat/` + 1
tracked scratch probe; `REMAINING_CENSUS_2026-08-01.md` superseded)
[r36 census re-sweep 2026-08-03: NOW **35 repo-wide** — 34 in
`LeanUrat/` (strict non-comment unit) + 1 tracked scratch probe; the
−1 is the r35-retired banked classifier sorry; census of record at
§41].
Dispatch now: **VC29 — the confirming end-to-end pass on this REVISION-26
text** (charge at §32); **wave 10 IS DISPATCHED** (pass-6 queue #2–#6
running concurrently with this fold — junc_bundle stage 1 carrying the
StageCore docstring sync; BasePolygonLengthLaw; G-ADM-2; the classifier
consumer face; the depth-3 probe — results ride r28, nothing from them is
claimed here). In flight/owed BEYOND VC29, per the pass-6 queue of record:
the (H7) K-LOC/(I-τ) first probe (#7 — the last never-probed conformance
row); the (UB-X) consumer-need walk note (#8, doc-only — tees up the
Asvin Route-B decision); the `DIterJunctionSupplier` formalization (#9,
statement can land now, its three sanity gates BLOCKED on #2 stage 1); the
ITER-LAW proof attempt (#10, attempt-grade, slotted after the depth-3
probe); the Asvin batch (#11: the `TowerMoveClosureL` two-layer
re-ratification; the fence-(vii) ROUTE (a) ratify-or-reopen incl. the RECSL
`hσt`-pin rider; the Route-B re-scope after #8); the optional S batch
(#12, folded into passing units: the hsc two-instance sign witness, the
GRAFT n2 bijectivity check); background/BLOCKED with no agents absent a new
mechanism (#13: the (H6) C2 design note, (H8′)(i)/(ii) proper, (PACK)
route-a, (E-N), the two declined forges under their displayed
dispositions); the StageCore docstring doc-debt (ASSIGNED to the first
landing Lean unit — #2 stage 1, else #5).]
[Updated at REVISION 27 (r28): the WAVE-10 FOLD BATCH — VC29 had NOT yet
run, so no pass is recorded here (the acceptance stays anchored on the
REVISION-25 text, VC28) **[superseded at REVISION 28, r29: VC29 has since
RUN and CONFIRMED FIRST-PASS — the anchor is now REVISION 27/VC29; the r29
bracket below is current]**; the five dispatched units' ledger-recorded
results folded into the standing rows: **`BasePolygonLengthLaw p` PROVED
for EVERY prime** (a6fcc4d — sorry-free, Lean-core, statement verbatim;
the OL5-adjacent convexity gap NEVER HIT; **consumer FIRED:
`terminalPolySem_kp0_unconditional`**, the first unconditional
`TerminalPolySem` supplier at an 𝐇°-keyed k′ = 0 seam — the (H6) (T-READ)
k′ = 0 numerical input now THEOREM-BACKED; the open-row display at the
(H6) residuals retired to PROVED status; the −∞ corner stays open); **FORGE
`junc_bundle` STAGE 1 ALL GREEN** (023a9d7 + 53f5d03 — 10/10 preregistered
rows, 4,116 samples, 28 tower instances; the clause-(iii) two-scale law a
MEASURED reduction, NOT proved; ITER-LAW 0/840 extended, still open; the
D0 `gate_glue_junction` hbundle INSTANCE-FALSE FLAG pending the stage-2
orchestrator adjudication — no gate display changed; stage-2 spec fixed:
row A/ℤ₂, the junction at read pair (2,3); the StageCore docstring debt
CLEARED, comment-only); **G-ADM-2 EXECUTED** (38ac01e — RIGHT-END-ONLY
PARTIAL, no track-B fire, track A live as **(ADM-REAL′)** per
`ADM_U7_TRACKA_ATTEMPT.md`; the scan adjudicates routing only, proves
nothing); **the Step-10/K1 classifier consumer face FIRED** (380cc37 —
`K1ConsumerFace` + the exact-strength parent-blind pin, supplied through
the v4 chain, fired at td2V4, de-vacuity fence compiled; the (FRESH)
residual DISCHARGED; parentBlock = ∅ honesty displayed; the
nonempty-parent instance stays DECLINED); **the (H7) first probe DECIDED**
(2c72c5f — (I-τ)/K-LOC VACUOUS-BY-TYPE at every compiled instance,
statements STAND unrepaired; KL-1a holds at ℤ/3, F4, ℤ/2; OL-2 sharpened
to the exact inhabitant spec; the route of record = N-H7-1 numerics
first). Dispatch now: **VC29 — the confirming end-to-end pass, rolled
forward onto this REVISION-27 text** (charge at §33); **wave 11 IS
DISPATCHED** (forge stage 2 incl. the D0 adjudication; the (ADM-REAL′)
attempt; the depth-3 probe, in flight from wave 10 with no result yet; the
N-H7-1 numerics — results ride r29, nothing from them is claimed here).
In flight/owed BEYOND VC29: the REVISION-26 list MINUS the five landed
wave-10 units, PLUS the wave-11 units above; the (UB-X) consumer-need
walk note (#8, doc-only); the `DIterJunctionSupplier` formalization (#9 —
its three sanity gates now waiting on forge stage 2); the ITER-LAW proof
attempt (#10, after the depth-3 probe); the Asvin batch (#11); the
optional S batch (#12); the background/BLOCKED list (#13, incl. the (H6)
C2 design note — C2 stays blocked, the BPLL proof does not touch
k′ ≥ 1).]
[Updated at REVISION 28 (r29): **VC29 CONFIRMED FIRST-PASS — the
acceptance RE-ANCHORS on the REVISION-27 text** (VC29: CONFIRMED 0C/0G on
the REVISION-27 wording, the SECOND consecutive first-pass confirmation —
the saved appendix checklist validated twice; report
`lean/notes/openmath/VC29_report.md`, 3d457cc; **the anchor moves
REVISION 25/VC28 → REVISION 27/VC29** **[superseded at REVISION 29, r30:
VC30 + VC30b have since CONFIRMED — the anchor is now REVISION
28/VC30+VC30b; the r30 bracket below is current]**; lineage: REVISION 26 was properly
superseded mid-cadence by the r28 roll-forward — no pass ever ran on it).
This revision folds the wave-11 acceptance/verdict batch: **the
`junc_bundle` VERDICT OF RECORD** (036ee8c six Lean-core theorems + the
REVISION 1–4b faithfulness hardening + 09f93c1 arc closure, round 5 CLEAN
— the compiled CHAR-2, Φ₂-LANDING, INTERIOR-Φ₂A-PINNED bundle
INSTANCE-FALSE at row A, kernel-proved; the parity wall
`juncforge_no_e2_junction` alone char-free/Φnext-free, explaining the C-e
escapee at the row-A read; `gate_glue_conditional` VACUOUS at its compiled
instance now a THEOREM; `DIterJunctionSupplier` NOT refuted; the
source-level question OPEN; **the FGMN transcription-scale re-derivation
design unit CHARTERED** — proposal-only, statement-change genre, the
ASVIN-FLAG decision point; `junc_bundle` LEAVES the open-wall list);
**ADM-REAL′ ACCEPTED** (24e51df, passes 3+4 consecutive CLEAN
model-diverse — **CLASS-LAT: (CLASS-d) a lattice theorem at every order,
unconditional**, the (GR-B) conditionality REMOVED from track A; displayed
opens = (B-PIN) + the (C-JCT) fan-out audit + F-ADM-3 EDIT-GATED at
`CensusCore.lean:155`); **the depth-3 probe MEASURED** (54c518d —
(IDENT-3) EXACT 0/1,656, no ǫ-revival on all-e_j = 1) + **TR3-S3 IN
VERIFICATION** (482150b composed; REVISION 3 f05df77; p2 Codex CLEAN
pre-revision, p3 Fable GAPS-ONLY, p4 Codex GAPS-ONLY, p5 running; counter
0/2 — NO status upgrade); **N-H7-1 EXECUTED** (f8aec25 — the (H7) pair
SURVIVES first contact at six real m ≥ 2 sites, 139 PASS; two measured
restatement pins: slope-keyed ε, the D-8 guard boundary; the slope-pooled
strengthening REFUTED); **the JFS2 faithfulness arc CLOSED** (5 rounds;
plus the two ops one-liners: the vc-audit-appendix checklist validated at
VC28+VC29, the pkill self-match trap saved). Dispatch now: **VC30 — the
confirming end-to-end pass on this REVISION-28 text** (charge at §34);
**SYNTHESIS PASS 7 is OWED** (the wave 10–13 window's new collapses —
CLASS-LAT, the junc_bundle verdict, the BPLL theorem, the v4 chain — need
the /goal re-map; it should also adjudicate the next wave's shape). In
flight/owed BEYOND VC30: TR3-S3 passes 5+6 (the bar decides; folds ride
the next ROOT unit); the FGMN transcription-scale re-derivation design
unit (chartered, proposal-only — its product is an ASVIN decision input,
NOT an edit); the (ADM) displayed opens ((B-PIN)/(C-JCT) audit; the
F-ADM-3 carrier re-key = EDIT-GATED design input for a chartered unit);
the (H7) Lean forge consuming the two N-H7-1 pins (restatement genre);
the (UB-X) consumer-need walk note (#8, doc-only); the
`DIterJunctionSupplier` formalization (#9 — its sanity gates now
re-scoped by the junc_bundle verdict: supplier NOT refuted, antecedent at
deg σ₁.Φ < deg f); the ITER-LAW proof attempt (#10); the Asvin batch
(#11: `TowerMoveClosureL`; the fence-(vii) ROUTE (a) lineage now incl.
the FGMN charter alongside the RECSL `hσt`-pin rider; the Route-B
re-scope after #8); the optional S batch (#12); the background/BLOCKED
list (#13).]
[Updated at REVISION 29 (r30): **VC30 + VC30b CONFIRMED — the acceptance
RE-ANCHORS on the REVISION-28 text**. The r29-ERRATUM lineage, displayed:
VC30 on the REVISION-28 wording returned **1 critical / 0 gaps** — the
§34 closing line's UNQUALIFIED `junc_bundle` restatement, the one
verdict-tier site that omitted the three restrictions (the
qualifier-propagation defect class's last appearance); every fold, the
38-pattern census, roster NINE, and all discipline checks otherwise
passed; CURED by the dated pre-confirmation r29-ERRATUM bracket
(573bd1b); the narrow pass **VC30b CONFIRMED 0C/0G** on the
errata-corrected text (reports `lean/notes/openmath/VC30_report.md`,
573bd1b, + `VC30b_report.md`, 6984d5d; the VC26b/VC27b narrow-companion
genre); **the anchor moves REVISION 27/VC29 → REVISION 28/VC30+VC30b**
(ledger 2026-08-08 "VC30 + VC30b CONFIRMED") **[superseded at REVISION
30, r31: VC31 + VC31b + VC31c have since CONFIRMED — the anchor is now
REVISION 29/VC31+VC31b+VC31c; the r31 bracket below is current]** — now
INSIDE the certified
anchor: the `junc_bundle` verdict of record + the FGMN charter, the
ADM-REAL′ acceptance (CLASS-LAT), the N-H7-1 pins, the JFS2 arc, the
strata3 measurement. This revision folds **TR3-S3 ACCEPTED** (96b7f54;
`TR3S3_2026-08-08.md` REVISION 4 — passes 7 Codex + 8 Fable consecutive
CLEAN 0C/0G, model-diverse, counter 2/2; the eight-pass arc with four
orchestrator-applied revisions; THREE independent from-note
implementations, 93 + 1,564 + 821 checks, 0 failures — the strongest
external-leg record of any accepted note): with accepted TR3-S1 +
TR3-S2, **TR-3′-GEN is PROVED on {all order-2} ∪ {order-3 all-e_j = 1},
CONDITIONAL ON (H-v3)** (attribution only — the finite identity
σ₂(R^F₃(f)) = R_λ₃(f), discrepancy ≡ 1, is unconditional; **PIN-GEN-i**
the displayed attribution pin, the fresh-pdftotext leg owed at Defs
3.15/3.16/3.18) [the (H-v3) conditionality
DISCHARGED at HV3 acceptance fa2180e (REVISION 31, r32) — the scope is UNCONDITIONAL ON PRINT per
HV3-THM's four standing conditions; the PIN-GEN-i leg discharged
separately at 303b11e, the pin stays displayed]; the GRB S3.4 second
post-acceptance bracket applied
ADDITIVE (that leaf's acceptance counters NOT reset); deeper strata stay
OPEN, probe-first (e_j ≥ 2 at orders ≥ 3 fenced; the sealed UNTESTED
(TWIST-3) candidate displayed). Dispatch now: **VC31 — the confirming
end-to-end pass on this REVISION-29 text** (charge at §35); **SYNTHESIS
PASS 7 is RUNNING** (in flight — its report becomes the next dispatch of
record when it lands; nothing is claimed from it here). In flight/owed
BEYOND VC31: the FGMN transcription-scale re-derivation design unit
(chartered, proposal-only — its product is an ASVIN decision input, NOT
an edit); the (ADM) displayed opens ((B-PIN)/(C-JCT) audit; the F-ADM-3
carrier re-key = EDIT-GATED design input); the (H7) Lean forge consuming
the two N-H7-1 pins (restatement genre); the (UB-X) consumer-need walk
note (#8, doc-only); the `DIterJunctionSupplier` formalization (#9); the
ITER-LAW proof attempt (#10); the Asvin batch (#11: `TowerMoveClosureL`;
the fence-(vii) ROUTE (a) lineage incl. the FGMN charter alongside the
RECSL `hσt`-pin rider; the Route-B re-scope after #8); the optional S
batch (#12); the background/BLOCKED list (#13).]
[Updated at REVISION 30 (r31): **VC31 + VC31b + VC31c CONFIRMED — the
acceptance RE-ANCHORS on the (errata-corrected) REVISION-29 text**. The
VC31 arc: VC31 **0C/2G** on the REVISION-29 wording (the 43-pattern
census EXACT; gap 1 = a truncated appendix slice, audit-input — the
checklist extended; gap 2 = the (H1) fence display) → the dated
**r30-ERRATUM** (f9aa7ef: the fence names the ENTIRE complement of the
proved union — orders ≥ 4 incl. all-e_j = 1 explicit; display only) →
VC31b BOTH substance checks PASSED + the dating observation → the
standing **DATING CONVENTION disclosure** (a63b9ef: window-label dating
on artifacts/entries; git commit order = the authoritative chronology) →
**VC31c CONFIRMED 0C/0G** (620417b) — **the anchor moves REVISION
28/VC30+VC30b → REVISION 29/VC31+VC31b+VC31c** (reports
`lean/notes/openmath/VC31_report.md` / `VC31b_report.md` /
`VC31c_report.md`) **[superseded at REVISION 31, r32: VC32 + VC32b have
since CONFIRMED — the anchor is now REVISION 30/VC32+VC32b; the r32
bracket below is current]**; now INSIDE the certified anchor: the TR3-S3
acceptance (the strata trilogy), the erratum-corrected fence, the dating
disclosure itself. This revision folds the WAVE-14 batch: **the FGMN
re-derivation PROPOSAL on record, NOT applied** (303b11e — the compiled
`SideReads` clauses (i)/(ii) double-scale the stride weight vs the
print's parent-valuation keying; the JuncForge parity wall = the
artifact's arithmetic shadow; THE ROW-A SOURCE VERDICT: the source glue
step FIRES at row A; the cure = the FOUR-TOKEN σ.w → σ.wPrev re-key,
verbatim diff at note §S3; hostile pass 1 RUNNING; application
ASVIN-GATED, the flag lineage's TOP item; the JuncForge refutation
theorems REMAIN TRUE of the old clause; **PIN-GEN-i DISCHARGED** as the
rider — the fresh column-verified `-layout` extraction at Defs
3.15/3.16/3.18, the PDF sha256-pinned into `docs/references/`); **the
(H-v3) discharge ON RECORD, pending verification** (f90d68a — HV3-THM:
v₃ = w₂ at e₀ = e₁ = 1, e₂ free, on THREE displayed conditions — the
pinned-print baseline, accepted TR3-S1, the standing (H-CHAIN)/RESCALE
consumptions; hostile pass 1 RUNNING; IF it survives its bar, TR-3′-GEN's
order-3 scope becomes UNCONDITIONAL ON PRINT; the 47 bracket sites ride
the acceptance — NONE edited) [it SURVIVED: HV3 ACCEPTED at fa2180e —
(H-v3) DISCHARGED, the sweep executed at REVISION 31, r32; the r32
bracket below is current]; **F-ADM-3 RE-KEY APPLIED** (ba02d60 +
8648d99 — `wphi` to the O-9 §4 top-normalized ledger under the standing
definition-change authority, Codex diff confirmation CONFIRM; CLASS-LAT
machine-checked at EVERY order, `classCard_eq_d` Lean-core; `hclass`
retired, `admFull_iff_maxClassWeight` hypothesis-free; 33-module cone
green, capstone fence EMPTY intersection; the R7 acceptance test 88 → 0
with the historical twin == 88 exactly); **the TWIST-3 law MEASURED
EXACT** (76513f8 + 48c07b4 — the ǫ-recurrence REVIVES at order-3
e₁ ≥ 2 with θ₂ = ℓ₁g₁γ₂ exactly, 0/1,656; IDENT-3-persists refuted
710/710; the genre's first PARI-certified RAMIFIED order-3 keys; the
honest P4′ SECONDARY-display RED disclosed — post-hoc explained, harness
unamended, exit 1 stands; **TR3-S4 CHARTERED**, verifier-owned
implementation required at acceptance); **the (UB-X) walk ON FILE**
(4924dcb — RB-DELTA adjudicated **CONSUMER-VISIBLE, not display-only**
at all three site families; F-B (1)–(4) satisfied; condition (5) = Asvin
sign-off + the ROOT owner's acceptance pass = the ONLY open gate — the
Route-B decision paragraph DECISION-READY at note §6, joins the Asvin
batch). Dispatch now: **VC32 — the confirming end-to-end pass on this
REVISION-30 text** (charge at §36); **SYNTHESIS PASS 8 NOT YET OWED**
(pass 7's queue still executing). In flight/owed BEYOND VC32: the FGMN
+ (H-v3) hostile passes (RUNNING — their verdicts ride the next fold)
[landed at REVISION 31, r32: HV3 ACCEPTED fa2180e, the discharge sweep
executed; FGMN p5 CRITICAL → REVISION 4, passes 6+7 decide — the r32
bracket below is current];
the ITER-LAW proof attempt (pass-7 #6); the (H7) restatement forge on
the two N-H7-1 pins (#8); the `DIterJunctionSupplier` statement (#9 —
its junction gates blocked on the FGMN adjudication, displayed); the
banked-sorry attempt (#10); WAVE-15 CANDIDATES displayed: the TR3-S4
compose + the held pass-7 items above; the Asvin batch (#11), RESTATED
IN FULL: the `TowerMoveClosureL` two-layer re-ratification; the
fence-(vii) ROUTE (a) ratify-or-reopen lineage — the GR route (a) + the
RECSL `hσt`-pin rider + the EXECUTED F-ADM-3 re-key recorded for
ratify-or-reopen; the FGMN four-token proposal AT TOP; the Route-B
RB-DELTA decision (decision-ready per the walk); the optional S batch
(#12); the background/BLOCKED list (#13).]
[Updated at REVISION 31 (r32): **VC32 + VC32b CONFIRMED — the acceptance
RE-ANCHORS on the (errata-corrected) REVISION-30 text**. The VC32 arc:
VC32 **0C/3G** on the REVISION-30 wording (ALL folds passed — the FGMN
proposal-only discipline, no HV3 scope leak, the F-ADM-3 flag-lineage
record, the TWIST-3 honest RED, the UBX gate, roster NINE, the
51-pattern census EXACT; the gaps ALL fold-lag/wording syncs) → the
dated **r31-ERRATUM** (e3425b1: FGMN/HV3 pass-1 status synced to LANDED
+ REVISION 1 applied; the 336-SAT rescope; the (UB-X) gate wording
harmonized) → **VC32b CONFIRMED 0C/0G** (e32ebef, the narrow companion
on the corrected passages verbatim) — **the anchor moves REVISION
29/VC31+VC31b+VC31c → REVISION 30/VC32+VC32b** (reports
`lean/notes/openmath/VC32_report.md` / `VC32b_report.md`) **[superseded
at REVISION 32, r33: VC33 + VC33b + VC33c have since CONFIRMED — the
anchor is now REVISION 31/VC33+VC33b+VC33c; the r33 bracket below is
current]**; now INSIDE
the certified anchor: the FGMN proposal-on-record with the source-fires
verdict, the HV3-THM discharge-pending display, the applied F-ADM-3
re-key, the TWIST-3 law + TR3-S4 charter, the UBX walk verdict, the
four-item Asvin batch. This revision folds: **HV3 ACCEPTED — (H-v3)
DISCHARGED** (fa2180e; passes 4 Codex + 5 Fable consecutive CLEAN
0C/0G, model-diverse — the pass-5 independent arXiv re-fetch, the
1,655-check two-way falsifier at off-roster p ∈ {7,17}, the ADVERSARIAL
CONTROL: type-hood deleted → exactly the step-5 leg breaks; **HV3-THM:
v₃ = w₂ at e₀ = e₁ = 1, e₂ free, on its FOUR standing conditions** —
the pinned-print baseline, accepted TR3-S1, the standing
(H-CHAIN)+RESCALE consumptions, the standing projective-dictionary
consumptions): **TR-3′-GEN is PRINT-UNCONDITIONAL at {all order-2} ∪
{order-3 all-e_j = 1}** and the **47-site (H-v3) bracket discharge
sweep is EXECUTED** (TR3S3 + ROOT + GRB, additive dated brackets, old
text standing, accepted-note counters NOT reset; reconciliation at §37;
PIN-GEN-i stays displayed — its leg discharged separately at 303b11e;
the e₁ ≥ 2 (TWIST-3)/TR3-S4 regime NOT reached by HV3-THM, its
hypothesis displays stand); and **the FGMN status sync** (p5 CRITICAL
af88f12 → REVISION 4 fa2180e: the complete grep-disciplined DictIII
perimeter — the SideClauseR2/sideReads_r2 pair, the (†)-transfer twins
needing RE-DERIVED readCeil arithmetic at e ≥ 2, Window :554/:557, the
R5 tie family; the THREE-token diff of record; the change-set PRICED UP
in the Asvin batch; counter 0/2, passes 6+7 decide; ASVIN-GATED,
nothing applied). Dispatch now: **VC33 — the confirming end-to-end pass
on this REVISION-31 text** (charge at §37); **SYNTHESIS PASS 8 NOT YET
OWED** (the FGMN arc still executing). In flight/owed BEYOND VC33: the
FGMN passes 6+7 (the deciders); the ITER-LAW proof attempt (pass-7 #6);
the (H7) restatement forge on the two N-H7-1 pins (#8); the
`DIterJunctionSupplier` statement (#9 — its junction gates blocked on
the FGMN adjudication, displayed); the banked-sorry attempt (#10);
WAVE-15 CANDIDATES displayed, **TR3-S4 the lead** (the compose unit on
the measured TWIST-3 law; verifier-owned implementation required at
acceptance) + the held pass-7 items above; the Asvin batch (#11),
RESTATED IN FULL: the `TowerMoveClosureL` two-layer re-ratification;
the fence-(vii) ROUTE (a) ratify-or-reopen lineage — the GR route (a) +
the RECSL `hσt`-pin rider + the EXECUTED F-ADM-3 re-key recorded for
ratify-or-reopen; the FGMN proposal AT TOP, now priced at the COMPLETE
DictIII change-set (the three-token re-key + the primed-mirror set +
the re-derived readCeil arithmetic for the (†)-transfer twins); the
Route-B RB-DELTA decision (decision-ready per the walk); the optional S
batch (#12); the background/BLOCKED list (#13).]
[Updated at REVISION 32 (r33): **VC33 + VC33b + VC33c CONFIRMED — the
acceptance RE-ANCHORS on the (errata-corrected) REVISION-31 text**. The
VC33 arc: VC33 **0C/2G** on the REVISION-31 wording (gap 1 = the FGMN
pass-6 concurrency displays, fold-lag; gap 2 = AUDIT-INPUT — the
read-only sandbox failed to launch, the grep-execution duty
transferred) → the dated **r32-ERRATUM** (e7ff234: the six
p6-concurrency disclosure brackets; the execution duty to the narrow
pass) → **VC33b 0C/1G WITH execution** (the 12/3/6 discharge-core
transcripts CONFIRMED; the gap = three multi-line brackets missing from
the orchestrator's `grep -o` extraction, audit-input) → **VC33c
CONFIRMED 0C/0G** (all six brackets verified in full context) — **the
anchor moves REVISION 30/VC32+VC32b → REVISION 31/VC33+VC33b+VC33c**
(reports `lean/notes/openmath/VC33_report.md` / `VC33b_report.md` /
`VC33c_report.md`) **[superseded at REVISION 33, r34: VC34 has since
CONFIRMED FIRST-PASS — the anchor is now REVISION 32/VC34; the r34
bracket below is current]**; now INSIDE the certified anchor: the HV3 acceptance
+ the complete (H-v3) discharge sweep (TR-3′-GEN PRINT-UNCONDITIONAL at
{all order-2} ∪ {order-3 all-e_j = 1}), the FGMN REVISION-4-through-7
perimeter-hardening displays, the re-stated Asvin batch. This revision
folds: **FGMN RE-DERIVATION ACCEPTED** (ec64503 — the 14-pass,
10-revision arc closes: p12 Fable CLEAN 0C/0G with the full execution
leg + p14 Codex content-CLEAN 0C/0G under the division-of-labor
disposition, model-diverse; three independent executed legs, passes
3/9/12; **the glue route's source-level question ANSWERED**: the
print's glue step FIRES at row A, the compiled parity/landing walls
TRANSCRIPTION ARTIFACTS of the double-scaled clause; ACCEPTED = the
two-scale diagnosis + the ROW-A SOURCE-FIRES verdict + the THREE-token
proposal diff with its fixed points incl. the JuncForge/D0 retained
semantics + the PIN-GEN-i discharge + the pinned sources/battery
record; THE APPLICATION = the Asvin-gated application unit's
deliverable per the REVISION-9 scope cut — the scratch-worktree diff
adjudicated at the Asvin gate on the diff itself with a
post-application hostile pass, the REVISION 4-8 censuses the reviewer's
informative map; the batch's TOP item now FULLY VERIFIED); **the
DIVISION-OF-LABOR adjudication of record** (3875c49 — Fable executes /
Codex adjudicates content; an infra-only inability-to-execute finding
on a Codex pass = a NOTE, not a gap, given a Fable-executed clean on
the same revision; no content finding ever downgraded); **TR3-S4
composed + sealed-falsifier ALL GREEN, NOTHING accepted** (seal 7fa8825
→ first run 13,679/0 at 79eaee6, rerun JSON-identical; **Lemma HV3-E2
DERIVED from the pinned print** — v₃ = w₂ at e₀ = 1/e₁ ≥ 2, no bespoke
hypothesis, HV3-THM's acceptance NOT transferring; p1 Codex 0C/2G
archived; REVISION 1 owed; counter 0/2; NO status upgrade; the F2c
verifier-owned from-note implementation = the acceptance obligation).
Dispatch now: **VC34 — the confirming end-to-end pass on this
REVISION-32 text** (charge at §38); **SYNTHESIS PASS 8 OWED at the
wave-15 close** (the FGMN arc that held it has closed). In flight/owed
BEYOND VC34, the WAVE-15 REMAINDER: the TR3-S4 pipeline (REVISION 1 →
hostile passes → the F2c obligation); the synthesis-7 tail — the
ITER-LAW proof attempt (#6), the (H7) restatement forge on the two
N-H7-1 pins (#8), the `DIterJunctionSupplier` statement (#9 — its
junction gates now RE-PRICE against the ACCEPTED FGMN diagnosis: the
note's §S6 gate display is accepted content, no longer
blocked-on-adjudication), the banked-sorry attempt (#10); the Asvin
batch (#11), RESTATED IN FULL: the `TowerMoveClosureL` two-layer
re-ratification; the fence-(vii) ROUTE (a) ratify-or-reopen lineage —
the GR route (a) + the RECSL `hσt`-pin rider + the EXECUTED F-ADM-3
re-key recorded for ratify-or-reopen; **the FGMN proposal AT TOP, now
FULLY VERIFIED (ACCEPTED ec64503)** — the application the gated unit's
deliverable (the scope-cut form); the Route-B RB-DELTA decision
(decision-ready per the walk); the optional S batch (#12); the
background/BLOCKED list (#13).]

[Updated at REVISION 33 (r34): **VC34 CONFIRMED FIRST-PASS 0C/0G — the
acceptance RE-ANCHORS on the REVISION-32 text** (report
`lean/notes/openmath/VC34_report.md`; ledger f3b1ff8 — the third
first-pass confirmation of the window: ALL folds verified — the FGMN
acceptance display exactly the REVISION-10 list, no application leak,
the wall theorems byte-frozen; the TR3-S4 un-upgraded status with the
F2c obligation displayed; the division-of-labor discipline; the Asvin
batch with its verified top item; the 59-pattern census exact; roster
NINE) — **the anchor moves REVISION 31/VC33+VC33b+VC33c → REVISION
32/VC34** **[superseded at REVISION 34, r35: VC35 + VC35b have since
CONFIRMED — the anchor is now REVISION 33/VC35+VC35b; the r35 bracket
below is current]**; now INSIDE the certified anchor: THE WAVE-14/FGMN MEGA-ARC,
CLOSED AND CERTIFIED END-TO-END (the glue route's source-level question
answered inside the anchor; the three-token proposal fully verified at
the Asvin gate), the division-of-labor discipline, the TR3-S4
composed-not-accepted status. This revision folds: **TR3-S4 ACCEPTED**
(cd611af — the 4-pass/2-revision arc closes: p3 Fable CLEAN 0C/0G with
BOTH execution legs — the sealed battery byte-identical, 13,679/0, AND
**the F2c verifier-owned from-note implementation DISCHARGED
IN-PIPELINE for the first time**: 1,108 checks / 0 failures at
off-roster p ∈ {5,7}, the decorrelated geometric ordinate leg, 3
mutation controls each RED — plus p4 Codex content-CLEAN 0C/0G,
model-diverse under the division-of-labor discipline; with accepted
TR3-S1/S2/S3, **TR-3′-GEN is PROVED on the THREE-component union {all
order-2} ∪ {order-3 all-e_j = 1, PRINT-UNCONDITIONAL} ∪ {order-3
e₀ = 1, e₁ ≥ 2, e₂ = 1, conditional on (H-ℓ) + the (KP_i)+[T]
consumptions at S6.2 + the PIN-GEN-i attribution display, Lemma HV3-E2
DERIVED from the pinned print}** — the strata family at FOUR accepted
members; deeper strata — e₀ ≥ 2 stacked, e₂ ≥ 2, orders ≥ 4 — stay
OPEN, PROBE-FIRST, NO candidate sealed); and the **TWIST3-probe
HYGIENE bracket** (the pass-3 non-classifiable observation: the probe
note's pre-correction KEY0-LIFT orientation label — "GMN-lawful =
θ₂-twisted ψ₂" — superseded by the TR3-S4 REVISION-1 corrected
orientation, the GMN-lawful class the UNTWISTED ψ₂, the twisted class
the FGMN-normalized lift; ONE dated additive bracket, the probe's
measured data untouched). Dispatch now: **VC35 — the confirming
end-to-end pass on this REVISION-33 text** (charge at §39);
**SYNTHESIS PASS 8 OWED at the wave-15 close**. In flight/owed BEYOND
VC35, the WAVE-15 REMAINDER = THE queue: the synthesis-7 tail — the
ITER-LAW proof attempt (#6), the (H7) restatement forge on the two
N-H7-1 pins (#8), the `DIterJunctionSupplier` statement (#9 —
re-priced against the ACCEPTED FGMN diagnosis, its §S6 gate display
accepted content, no longer blocked-on-adjudication), the banked-sorry
attempt (#10); the Asvin batch (#11), RESTATED IN FULL: the
`TowerMoveClosureL` two-layer re-ratification; the fence-(vii) ROUTE
(a) ratify-or-reopen lineage — the GR route (a) + the RECSL `hσt`-pin
rider + the EXECUTED F-ADM-3 re-key recorded for ratify-or-reopen;
**the FGMN proposal AT TOP, FULLY VERIFIED (ACCEPTED ec64503)** — the
application the gated unit's deliverable (the scope-cut form); the
Route-B RB-DELTA decision (decision-ready per the walk); the optional
S batch (#12); the background/BLOCKED list (#13).]

[Updated at REVISION 34 (r35): **VC35 + VC35b CONFIRMED — the
acceptance RE-ANCHORS on the (errata-corrected) REVISION-33 text**. The
VC35 arc: VC35 **0C/2G** on the REVISION-33 wording (gap 1 = the §39
closing summary abbreviated the third strata component's conditions →
the dated **r34-ERRATUM** d79f295 spells them out at the closing site
too — (H-ℓ) + the (KP_i)+[T] consumptions at S6.2 + the PIN-GEN-i
attribution display; gap 2 = AUDIT-INPUT, the empty TWIST3-bracket
appendix slice, a grep-pattern artifact) → **VC35b CONFIRMED 0C/0G**
(the corrected closing + the ACTUAL TWIST3 bracket in full context) —
**the anchor moves REVISION 32/VC34 → REVISION 33/VC35+VC35b** (reports
`lean/notes/openmath/VC35_report.md` / `VC35b_report.md`; the SIXTEENTH
audited re-anchoring); now INSIDE the certified anchor: the TR3-S4
acceptance — the strata family's THREE-component union {all order-2} ∪
{order-3 all-e_j = 1, PRINT-UNCONDITIONAL} ∪ {order-3 e₀ = 1, e₁ ≥ 2,
e₂ = 1, conditional on (H-ℓ) + the (KP_i)+[T] consumptions at S6.2 +
the PIN-GEN-i attribution display, with Lemma HV3-E2 DERIVED from the
pinned print} — the F2c in-pipeline discharge, the TWIST3 hygiene.
This revision folds THE WAVE-16 BATCH (the synthesis-7 tail, EXECUTED):
**ITER-LAW PROVED AT ATTEMPT GRADE on the harness tower class**
(standard-lift towers — the class DITER_RESTATE §S2 pins; the entire
instance base sits inside it; seal ab7c31c → run 242ba1e ALL GREEN
FIRST PASS exit 0, 35,232 samples / 0 violations, F1–F10 + CERT,
Case-II coverage MET, row-X NOT-CYCLIC 4/4 exact value-set match;
(W-MULT) recovered, not consumed; HONEST RESIDUALS: (ITER-LAW-LIFT)
general lawful lifts OPEN exactly at g₀ = 1 ∧ δ₁ = 1, the gr(w₂)
wrapper rides the accepted GRB retarget consumer-side, orders ≥ 3 NOT
TOUCHED, ACCEPTANCE COUNTER 0 — hostile passes IN FLIGHT); **the
banked sorry RETIRED** (15372d1: `npVertices_stable_of_hull_preserved`
PROVED Lean-core via the new `OM/HullStability.lean`, statement
byte-unchanged, BPLL §0 kernels copied verbatim with attribution;
AxChk exit 0, ZERO sorryAx in the 1099-line census, capstone
footprints unchanged; the corpus's banked-sorry count 1 → 0); **the H7
forge** (037ac6f: the FIRST compiled non-vacuous (H7) decision point —
5-site depth-1, both OL-2 rfl-degeneracies broken, the two N-H7-1 pins
decided at the instance incl. the pooled-form COMPILED COUNTERMODEL
and the first contentful `KLocCounts` instantiation; depth-1 honesty
displayed; NOTHING discharges (H7)); **the DITERSUP statement**
(2a25ed9: display-only — `SideReads′`/`ReadsOf′`/
`DIterJunctionSupplier′` + the D2 carrier interface with the gr(w₂)
tie displayed as MISSING; gates J1/J2/J3a/J3b/JS1–JS3/P1/P0 priced,
EVERY gate BLOCKED-ON-APPLICATION, the old-clause records byte-frozen;
`TruncRunFrom′` joins the primed-mirror set; J3b may cite ITER-LAW
proved-on-class, attempt grade, standard-lift instances only).
Dispatch now: **VC36 — the confirming end-to-end pass on this
REVISION-34 text** (charge at §40); **SYNTHESIS PASS 8 OWED at the
wave-16 close**; the ITER-LAW hostile-pass arc IN FLIGHT. The Asvin
batch (#11), RESTATED IN FULL: the `TowerMoveClosureL` two-layer
re-ratification; the fence-(vii) ROUTE (a) ratify-or-reopen lineage —
the GR route (a) + the RECSL `hσt`-pin rider + the EXECUTED F-ADM-3
re-key recorded for ratify-or-reopen; **the FGMN proposal AT TOP,
FULLY VERIFIED (ACCEPTED ec64503)** — the application the gated
unit's deliverable (the scope-cut form); the Route-B RB-DELTA decision
(decision-ready per the walk); the optional S batch (#12); the
background/BLOCKED list (#13).]

[Updated at REVISION 35 (r36): **VC36 RAN and returned NOT-CONFIRMED
1C/3G** (report `lean/notes/openmath/VC36_report.md`, 3cf25ca; the v1
charge died silently at launch on the 1,048,576-char Codex input cap —
1,210,905 bytes; the v2 charge = the FROZEN-HISTORY TRIM per checklist
rule 7, 839,009 bytes, full §§0–6 + all 8 r35 diff hunks + full
§§38–40 + the diff-verified byte-identity disclosure for §§7–37 +
census transcripts 69/69). Adjudication: F1 (critical) + F2 (gap) =
FOLD-LAG — the audited text honestly recorded the then-live PC1
critical and F8 gap; both cured by the ITER-LAW errata arc +
acceptance. F3/F4 = GENUINE closing-site qualifier omissions (the
recurring qualifier-propagation-at-closing-restatements defect class)
— cured by the dated r36 errata at the §40 close. **ITER-LAW is
ACCEPTED** (2-clean model-diverse: PC1 NOT-CLEAN 1C/1G acc955a → PE1
CLEAN 64ac340 → errata round 1 7566ccd — S0 scope erratum +
`iterlaw_cor4_mixed.py` 18,396/0, sealed falsifier md5-untouched →
PC2 CLEAN 804b88a + PE2 CLEAN c40a1da; adjudicated 33373cb; counter
2/2); SCOPE AS ACCEPTED: proved and ACCEPTED on the harness tower
class (standard-lift; a STRICT SUPERSET of DITER_RESTATE §S2's pinned
E₂ = e₀e₁ > 1 setting; coincides with the §S2 display on the nose
restricted to E₂ > 1; the E₂ = 1 leg a harmless COR-3-covered
extension outside the statement of record); RESIDUALS all three:
(ITER-LAW-LIFT) open exactly at g₀ = 1 ∧ δ₁ = 1; the gr(w₂) wrapper
rides the accepted GRB retarget consumer-side; orders ≥ 3 untouched.
THE SORRY CENSUS OF RECORD RE-SWEPT (r35-6's owed re-sweep): **35
repo-wide — 34 code-level `sorry` tokens in `LeanUrat/` (strict
non-comment unit) + 1 in the tracked scratch probe
`scratch_S7_escalation.lean`; B2D 0; OM tree 0** (the −1 vs the r27
display is the r35-retired banked classifier sorry; leaders unchanged:
V9_K1nonrec ×4 / SlotsG2_relFamily ×4 / TV_G3 ×2 / G1_toyGate ×2 —
the Moves*/Scaffold E-phase opens are other units' tracked work; full
breakdown at §41). Dispatch now: **VC36b — the NARROW confirming pass
on the r36 remedies only** (charge at §41; full end-to-end NOT
re-owed, the VC36-confirming scope); SYNTHESIS PASS 8 still OWED at
the wave-16 close; the acceptance citation STAYS at REVISION
33/VC35+VC35b until VC36b lands.]

**[HISTORICAL CLOSE — marked at REVISION 23, r24, disposing VC25 gap 4.
The paragraph below is the ORIGINAL 2026-08-02 composition close, kept
verbatim as the head of the running provenance narrative that follows; it
is NOT a current-state display (matching the treatment of the other
superseded dated passages): O4T pass 2 has long since run and been folded,
and the document's provenance extends far beyond HEAD d730669 — the
CURRENT standing is the §0 REVISION-23 paragraph + the latest revision
record (§29).]**

— ROOT ASSEMBLY complete, 2026-08-02. Composed at HEAD d730669 + the recorded
in-flight events (O4T pass 2, untracked). Every §2 grade traces to a named commit or
pass record; nothing above upgrades any leaf's status. REVISION 1 (§7) applied after
VC1 at the 2026-08-02 harvest state (c43b13b). REVISION 2 (§8) applied after VC2
(bfa462f dispatch record; no status syncs — VC2 was a composition pass on this
document alone). REVISION 3 (§9) applied after VC3 at the 2026-08-02-night harvest
state (7b28562: VERIFIED = 6). REVISION 4 (§10) applied after VC4 at the
confirming-passes/O-9-r4/CU-2t-R3 harvest state (85670f7/35faac7: VERIFIED = 7),
concurrent with tree DELTA 4's ROOT touches (unit TREE-GAPS). REVISION 5 (§11)
applied after VC5 at the O-9/CU-2t-verified harvest state (6a7593b: VERIFIED = 9).
REVISION 6 (§12) applied after VC6 at the same harvest state (no status syncs —
VC6 was a composition-only pass; VERIFIED = 9 unchanged); it also EXECUTES the
standing T-U §U.SQ.6 audit as the new §3.4. REVISION 7 (§13, unit ROOT r8) applied
after VC7 at the same harvest state (composition-only pass; VERIFIED = 9
unchanged); it ENLARGES (H1) (the NEW member (d), the degree-g > 1 KEY1
generalization) and (H5) (the NEW explicit clauses PACK-i/ii/iii), applies the
three VC7 advisories, and retires the §3.3 T-U audit row on VC7's table
confirmation. REVISION 8 (§14, unit ROOT r9) applied after VC8 at the same
harvest state (composition-only pass; VERIFIED = 9 unchanged); it REQUANTIFIES
(H1)(d) to READ ORDER (the order-≥ 2 KEY1 block — membership by OM order ≥ 2
alone, the current key's degree irrelevant) and syncs the PACK-i/ii/iii K2/K3
sub-attribution (PACK-i/ii at Step 18's K2 evaluation glue; PACK-iii at Step
18's K3 evaluation ring-hom, with D-6). ACCEPTANCE (2026-08-03): VC9 (Codex) +
VC10 (Fable) both returned COMPOSITION-SOUND, ZERO blockers, on the identical
REVISION-8 text — the ≥ 2-consecutive-clean, ≥ 2-model bar (tree §4 Root
criterion) is MET and the composition is ACCEPTED at (ROOT-C)'s displayed
conditional scope. REVISION 9 (§15, unit ROOT r10) applied POST-ACCEPTANCE —
display hygiene only: it repairs VC10's three advisories (the O-14a′
§0-vocabulary grade display + the NEW §3.2 row P(O-14a′ r3); the D-12r/(E-N)
rows' Step-16/clause-(UB) transport entries; Table A's 6(iii)–(ix)+7 residue
list run to closure with (H4a)+(K3-δ)), with no [M] member, no step content,
and no clause attribution changed. [The "ACCEPTANCE STANDS ... counter TWO"
close of that revision is SUPERSEDED by the record below.] SUSPENSION
(2026-08-03, ledger 44ce0a7): the owed D-11 pass 3 returned CRITICAL and Steps
17/18b consumed the refuted layers as proved — the Phase-C acceptance
SUSPENDED. REVISION 10 (§16, unit ROOT r11, 2026-08-04) applied: Steps 17/18b
re-pointed to the twice-passed true-scope D-11 r4 leaf (REVISION 3); (PACK-iv)
added as (H5)'s fourth row-law (consumed at Step 18b direct + Step 17's E1
(ROW-Σ) display only — the leaf's closed edge set); NEW [M] rows (H8) = (K-CUT)
and (H9) = (NSF) ((NSF-KEY) KNOWN FALSE AT HEAD, Unit-C/sign-off-gated);
Step 12's delivery rescoped to separable lifts (E-M3); the §3.1 roster is
TWELVE rows ↔ (H1)–(H9). **The suspension's composition-level defect is CURED;
the ACCEPTANCE is NOT re-earned: the consecutive-clean counter is ZERO of 2**,
and the end-to-end bar re-runs on this REVISION-10 text (charge at §16).
REVISION 11 (§17, unit ROOT r12, VC11 ripple-sync) applied after VC11 returned
COMPOSITION-GAPS on the REVISION-10 text (4 blockers + 1 advisory, ALL r11
ripple-sync defects; the targeted r11 repairs CONFIRMED): Steps 16/19's
evaluation/(UB) scope displays and Step 19's final ∎ synced to (H1)–(H9) with
(H5) at PACK-i/ii/iii/iv; §3.4 item 12 re-adjudicated to (H8) = (K-CUT) and the
table's "no new hypothesis surfaced" claim corrected; the 6(iii)–(ix)+7 residue
list closed with (H9); Step 18b's Consumes summary restored to
(GR-B)/(FRESH)/(ADM). Pure display syncs — no [M] member, step proof content, or
leaf pointer changed; NO status syncs (VERIFIED = 9 stands). **The counter STAYS
ZERO of 2**; VC12 re-runs the bar on this REVISION-11 text (charge at §17).
VC12 RAN on the REVISION-11 text — COMPOSITION-SOUND, ZERO findings: the counter
reached ONE of 2. VC13 (Fable, the model-diverse pass) RAN on the same text —
COMPOSITION-GAPS (2 blockers + 3 advisories, all the stale-hypothesis-enumeration
genre; the composition itself CONFIRMED). REVISION 12 (§18, unit ROOT r13) applied:
the five VC13 findings repaired in place as pure display syncs (§1's (H5) display
run to PACK-i/ii/iii/iv; Step 18's "— §1" clause-(R) quote run to the full
(H1)–(H9) set with the five-hypothesis count retired; Step 17's Consumes carrying
(H9)/(PACK-iv); the §3.2 O-11 row recording the un-banked 2026-08-03 hostile pass
with the (H8) provenance disambiguated; the (AGR) residue widened to CU-4's
displayed [M] conditionalities) PLUS the GLOBAL ENUMERATION AUDIT killing the
class (§18's audit record). No [M] member, step proof content, or leaf pointer
changed; NO status syncs (VERIFIED = 9 stands). **The counter RESETS to ZERO
of 2**; VC14 re-runs the bar on this REVISION-12 text (charge at §18).
RE-ACCEPTANCE (2026-08-05; ledger "RE-ACCEPTANCE: THE BAR IS MET AT TWELVE
ROWS"): VC14 (Codex) clean + VC15 (Fable, the model-diverse pass)
COMPOSITION-SOUND with ZERO blockers, both on the identical REVISION-12 text —
the composition is RE-ACCEPTED at (ROOT-C)'s twelve-row displayed conditional
scope; the suspension arc closes. REVISION 13 (§19, unit ROOT r14) applied
POST-ACCEPTANCE — the hygiene + discharge-sync batch: VC15's four advisories
repaired (Step 18's Consumes gains Step 2; Step 4's grade conformed to §0 with
the NEW §3.2 M17/RegPFinite faithfulness-audit row; the RESUM-n3 billings carry
the G-3 scope caveat; the (PACK-iv) edge-set/proof-line wording reconciled and
the q/t variable convention declared); **(NSF-KEY) RETIRED** (Unit C executed,
commits 322ec54/dfdb558; the six (NSF-KEY-RET) artifacts on file; compiled
witness H9-U1) — the (H9) row's open member is now (NSF-INT) (its (K1-CANON)
premise owed); the D-12r row rewritten at its confirmed literature pins
(reclassification [M] → §4 VC-gated); the (H8) = (K-CUT) row re-scoped to the
landed 11-unit Lean development (REDUCED, not discharged; charge (vi-a) FAIL on
record — no retirement); the mid-campaign (E-N) rewrite (commit 89176a4)
formally ADOPTED with the process gap recorded; the H4 ROOT-sync briefs
adjudicated STILL-OWED (firing conditions unmet). The acceptance was EARNED on
the REVISION-12 text; REVISION 13 is a text change: **a confirming end-to-end
pass (VC16) is OWED to re-anchor the accepted text on the REVISION-13 wording**
(charge at §19). VC16 RAN on the REVISION-13 text — **COMPOSITION-SOUND, zero
blockers** (`lean/notes/openmath/VC16_report.md`): the acceptance RE-ANCHORS on
REVISION 13, and finding 2 adjudicates the D-12r reclassification WARRANTED.
REVISION 14 (§20, unit ROOT r15) applied: the D-12r row MOVED from §3.1 [M] to
the §4 literature-pin class (entry 10 — (HEN-LIFT) at Neukirch II §4 (4.6)
p. 129 / the displayed companion uniqueness lemma / Serre LF I §6 Prop 15
pp. 17–18; (OM-SAT) riding the §4 GMN entry at the TAMS-confirmed Cor 3.8 +
Def 3.9; dependency-decomposition routing preserved); consuming steps re-pointed
to "perimeter-conditional on the §4 D-12r pin"; the §3.1 roster is **ELEVEN rows
↔ (H1)–(H9)** with (H3) = {(ADM), (E-N)}; the §4 roster synced honestly per VC16
finding 2 (items 1/4/5 + the §3.3 LIT-1/LIT-3 rows). No step proof content, no
leaf pointer, no clause attribution changed. REVISION 14 is a text change: **a
confirming end-to-end pass (VC17) is OWED on the REVISION-14 wording** (charge
at §20). VC17 RAN on the REVISION-14 text — **COMPOSITION-GAPS, 1 blocker + 1
advisory, both display-conformance at the r15 edits** (all other charged checks
PASS; `lean/notes/openmath/VC17_report.md`). REVISION 15 (§21, unit ROOT r16)
applied: Step 12's (τ-irr) description locally re-pointed to
"perimeter-conditional on the §4 D-12r pin (entry 10)" — matching Steps
2/3/11/16/19 — with the §20 r15-5 already-conformant confirmation marked
RESOLVED; Step 4's grade rendered in the exact §0 word, PROVED-UNVERIFIED
(pending: the §3.2 M17/RegPFinite faithfulness-audit row; the Lean-core and
Step-5 facts kept as notes). Pure display syncs — no [M] member, step proof
content, leaf pointer, or clause attribution changed; ELEVEN rows stand.
REVISION 15 is a text change: **the confirming end-to-end pass (VC18) is OWED
on the REVISION-15 wording** (charge at §21). VC18 RAN on the REVISION-15 text
— **COMPOSITION-SOUND, zero blockers** (one advisory;
`lean/notes/openmath/VC18_report.md`): the acceptance RE-ANCHORS on REVISION
15. REVISION 16 (§22, unit ROOT r17) applied: the (K-CUT) discharge gate
PASSED at round 3 (`KCUT_pass3_2026-08-06.md`, DISCHARGES — the review gate,
not the semantic owner rows) and the **(H8) = (K-CUT) row RETIRED from §3.1**
— the package (the repaired adjudication display + `KCutMassFactor` +
`KCutK1Tie`, Lean-core; the 3-round pass arc) recorded at the NEW §3.2 K-CUT
retirement row, the graft / measured-mass / τ/δ-dictionary residues MOVED to
their named owners; the §3.1 roster is **TEN rows — (H1)–(H9) minus the
retired (H8)**; the per-clause attributions synced ((R)/(UB) lose (H8));
VC18's advisory repaired (the §3.2 end-to-end row synced through VC18). No
step proof content and no leaf pointer changed. REVISION 16 is a text change:
**the confirming end-to-end pass (VC19) is OWED on the REVISION-16 wording**
(charge at §22). VC19 RAN on the REVISION-16 text — **COMPOSITION-GAPS, 2
blockers** (`lean/notes/openmath/VC19_report.md`): the r17 retirement
OVERSHOT — the three K-CUT residues had no housing owner statements, so
clause (R) silently consumed unpriced content at Step 18's K1 (the package
and its retirement record themselves CONFIRMED standing). REVISION 17 (§23,
unit ROOT r18) applied: the NEW slimmed §3.1 row **(H8′) — the K-CUT
residual-interface row** (members (i) GRAFT — graft
completeness/bijectivity, charge-(vi-a) FAIL standing as status; (ii)
MASS-ID — the shMass/blkMass measured-mass identification; (iii) POOL-DICT —
the node-level τ/δ pool dictionary + blockTotal_tie instantiation; former
owner labels kept as retirement ROUTES); Step 18's K1 line and the
per-clause attributions consume the retired package + (H8′) ((R)/(UB) gain
exactly (H8′)); the §3.1 roster is **ELEVEN rows — (H1)–(H9) minus the
retired (H8), plus (H8′)**. Net progress: the former (H8) row assumed the
WHOLE cut decomposition; (H8′) assumes only the three interfaces, the core
machine-proved. No step proof content and no leaf pointer changed. REVISION
17 is a text change: **the confirming end-to-end pass (VC20) is OWED on the
REVISION-17 wording** (charge at §23). VC20 RAN on the REVISION-17 text —
**COMPOSITION-GAPS, 2 blockers, both leftover sentences**
(`lean/notes/openmath/VC20_report.md`): the (H8′) substance CONFIRMED (three
members exactly VC19's missing K1 premises; (R)/(UB) gain exactly (H8′); the
walk closes; acyclicity stands; no independent regression), but §1's
retired-(H8) bracket still counted TEN live rows and the §3.2 K-CUT
retirement row still carried the pre-r18 owner-housing clauses. REVISION 18
(§24, unit ROOT r19) applied: both leftover sentences repaired in place —
§1's bracket at ELEVEN with the (H8′) pointer; the retirement row's
WHAT-RETIRED sentence rewritten to one voice (the package proved conditional
on the three interfaces, PRICED AT (H8′); owner arcs retirement ROUTES
only). Pure display syncs; the acceptance was EARNED on the REVISION-12
text; **the confirming end-to-end pass (VC21) is OWED on the REVISION-18
wording** (charge at §24).

---

## 7. REVISION 1 (2026-08-02, unit ROOT-R2) — disposition of VC1, the Phase C end-to-end pass 1

**The pass.** VC1 = the first end-to-end verification of this document (Codex, fresh
context, dependency-graph charge: "a named input counts only if an earlier step
supplies it, a ledger row classifies it, or the literature roster carries it";
`lean/notes/openmath/VC1_root.jsonl`). Verdict: **COMPOSITION-GAPS (findings 1–8
blockers, 9–11 advisories)** — "the assembly is not composition-sound as written...
forward edges, a genuine D-11/O-11 cycle, several unbound inputs, and an incomplete
literature inventory." Every finding was accepted and repaired IN PLACE (the body
above is edited, against the §6 byte-stability default, per the standing repair
directive); this section is the audit trail. The step-by-step walk's FAIL/GAP verdicts
(Steps 1–3, 6, 7, 9, 11, 14, 16–19) are subsumed by the findings below.

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| 1 | BLOCKER | Step 1 silently consumes T-6 (ns-null), supplied only at Step 2 | ACCEPTED; the edge is REMOVED, not waived: the dependency is re-pointed to Dfloor's own Appendix A (self-contained ns-null via LIT-8c, inside the VERIFIED leaf-#3 perimeter — the fact the leaf actually proves); T-6's (ns-null) is declared NOT consumed | Step 1 |
| 2 | BLOCKER | D-11 M2 consumes (S2) = O-11's datum while O-11 consumes D-11's pack side — a genuine cycle | ACCEPTED; STRATIFIED exactly as the verifier proposed: D-11 {M1, M1′, M3} = Step 17 ≺ O-11 = Step 18 ≺ D-11 M2 = NEW Step 18b; both results displayed; no mutual dependency remains | Steps 17/18/18b, audit trail |
| 3 | BLOCKER | B-1..B-7 outside the document's dependency universe | ACCEPTED; grades inlined at Step 2 from tree §2.1 (B-2/B-3 cores VERIFIED-LEAN; B-1/B-2doc/B-4/B-5/B-6/B-7 PROVED-UNVERIFIED; B-8 SUPERSEDED, uncited); §3.2 B-layer row added | Step 2, Step 3, §3.2 |
| 4 | BLOCKER | O-9's refuted census exponent misfiled as [V] ("a proof is on file" was false) | ACCEPTED for the composed state; since then the rev-2 repair LANDED (01c21e6: E′ strict left tail, countermodel now predicted exactly; JUNCTION-PIN withdrawn → (FRESH)(c)) — the row is [V] again, re-pass owed | Step 14, §3.1 (FRESH), §3.2 |
| 5 | BLOCKER | unbound inputs: C.1.5 (Step 6), D-5 (Step 7), (O6) (Step 11); Steps 9/16 lack Consumes lists | ACCEPTED; all suppliers displayed: C.1.5 = T-2's deferred obligation → the S-1 audit row; D-5's carrier design = O-2a rev 5 §§1–2 (proved content, debt = P(O-2a r5)); (O6) = O3design §1 definitional canonicalizations; Consumes lists ADDED to Steps 9 and 16; the registry re-worded as metadata | Steps 6/7/9/11/16, §3.3 |
| 6 | BLOCKER | displayed theorem unconditional; steps prove a conditional implication | ACCEPTED; (ROOT) re-badged as THE TARGET; **(ROOT-C)** — hypotheses (H1)–(H4) = exactly the §3.1 [M] rows — displayed as THE theorem the composition proves, with the [V]/[T] rows displayed as audit debts on the implication's proof, not statement hypotheses; Step 19 closes at (ROOT-C) | §1, Step 19 |
| 7 | BLOCKER | universal (UB) stronger than the Step-16 support (δ-stable classes, conditional (R1)–(R4)) | ACCEPTED; the exhaustion debt DISPLAYED as new §3.1 row **(UB-X)** (per-leaf O_δ audit; class exhaustion; aggregation/base-change commutation); Step 16 restated at honest scope; (ROOT-C)(H4) carries it | §1, Step 16, §3.1 |
| 8 | BLOCKER | literature roster incomplete: O4T's "Galois descent + one more"; K-FACT's GMN source unpinned | ACCEPTED; the missing pair NOW EXISTS — O4T rev 3 (bb4326c) added Facts **SEP** + **DES** as in-line-proved imports (inventory complete at eight named Facts, six-vs-eight reconciled); **K-FACT = GMN Thms 1.15/1.19 + 3.1/3.7** (`docs/GMN_citations.md`), all four loci already §4-pinned | Step 13, Step 15, §4 item 6 |
| 9 | ADVISORY | general-n 𝔅_n instance correspondence insufficiently explicit | ACCEPTED; **(B-INST)** displayed: the consumed pack is BY CONSTRUCTION the 𝔅_n member (three on-record organizations; OL-O12-1 the contingent escape); no theorem claims more; degree-3 realized-table identification = the OL-O12-2 data pass | Step 5 |
| 10 | ADVISORY | (AGR) mislocated in [M] | ACCEPTED; relocated to §3.2 as an inherited-[V] row (assembled from CU-2 + CU-4 + Cor 4.19, all on file) with a §3.1 tombstone note | §3.1, §3.2 |
| 11 | ADVISORY | Step 19 conflates O-11 (series = R_τ) with D-11 M2 (density = series) | ACCEPTED; both results displayed as labeled statements; Step 19's attribution now names both tranches and states neither alone delivers α = R_τ | Steps 18/18b/19 |

**Status syncs applied with the repairs** (all at the 2026-08-02 harvest, ledger
c43b13b): **GD23 = VERIFIED, leaf #4** (rev 4 gap repairs f9878d1 + gaps-closed check;
the premature d730669 adjudication superseded); **O4T = repaired at rev 3, awaiting
one full clean pass**; **CU-1 = rev 5 in flight** (rev 4 landed 8ab8ca6; gap-check
GAPS-REMAIN; NOT VERIFIED — the headline's leaf count corrected 5 → 4); **O-9 =
repaired at rev 2** (01c21e6), re-pass owed; **D-11/O-11 pass 1s = NOT ACCEPTED**
(2587c70 / a7ede72), r2s owed. Nothing in this revision upgrades any leaf; it corrects
this document's own composition and bookkeeping.

**What VC1 pass 2 should check first:** the three structural repairs (Step 1's
Appendix-A re-point; the 17 ≺ 18 ≺ 18b stratification; (ROOT-C) vs the Step-19 close),
then the §3 completeness standard ("every named pin/flag has exactly one ledger
home"), which findings 3/5 showed is the failure-prone invariant.

**Concurrent in-flight events recorded at REVISION 1 close** (landed by parallel
units while this revision was being written; statuses in the body are AS OF the
c43b13b harvest, with bracketed in-flight notes at the affected rows): **D-11 r2**
(566b967 — the pass-1 critical contained as specified; (S3) declared) and **O-11 r2**
(1bd8bd1 — D-a/D-b/D-c declared; oracle-pin supplement 10/10) landed, pass 2s
launched; **CU-1 r5** landed (6299a4a — the GAPS-REMAIN item repaired); the
PROJECT_STATE refresh landed (3896eb0). O-11 r2's §9 owed ROOT touches are EXECUTED
here: (1) Step 18 names D-a/D-b/D-c with engine-proxy language; (2) the §3.3
GD-4/GD-6 row gains its Step-18 entry line; (3) the §3.1 (R1)–(R4) row gains "hence
Step 18"; (4) the (I-τ)+K-LOC row already listed Step 18; (5) D-11's widened charge
items (v)–(vii) noted pending acceptance; (6) the 37/37 falsifier-count correction
displayed (§5 + Step 18). Bookkeeping note: commit 4e03512's staging sweep captured
an intermediate state of THIS revision alongside the O-11/D-11 fold; this commit is
the revision's unit of record.

— REVISION 1 complete. The body above is the operative composition; VC1 pass 2 runs
on this document as a whole.

---

## 8. REVISION 2 (2026-08-02+, unit ROOT) — disposition of VC2, the Phase C end-to-end pass 2

**The pass.** VC2 = the second end-to-end verification (Codex, fresh context, same
dependency-graph charge as VC1, run on the REVISION-1 document;
`lean/notes/openmath/VC2_root.jsonl`). Verdict: **COMPOSITION-GAPS** — 5 blockers +
2 advisories, numbered V2-1..V2-7 in report order. VC2 also CONFIRMED the three
REVISION-1 structural repairs: "the two theorem statements are honestly
distinguished", "the original D-11/O-11 cycle and Step-1 forward edge are repaired"
(V2-3 is the explicit ADVISORY confirming the Appendix-A re-point compositionally
valid). Every finding is dispositioned below and repaired in place; this section is
the audit trail. Repairs are at named sites only; the §2 walk, both theorem
statements' mathematical content, and all leaf grades are otherwise byte-stable —
nothing here upgrades any leaf.

**Headline finding (V2-6, the one REAL mathematical bug in this document):** the §1
clause-(R) display quantified the self-loop resummation 1/(1 − q^{−(e(e+1)/2−1)})
over unrestricted e — at e = 1 the denominator is 1 − q⁰ = 0. Adjudication against
the solve structure (O-12 rev 4 §§2.2–2.4 + Thm 1 + Cor D): **e ≥ 2 is FORCED at
every self-loop** — inter-block bookings descend strictly (Lemma L4: m, μ < e), so
self-loops are within-block only, and block 1 has NO loop (K_1 = (0), Φ_1 = 1,
E(1) = {1}; Theorem 1 is stated "for e ∈ {2, …, n}"; multiplicity-1 atoms terminate
as simple Hensel factors). The e = 1 corner is VACUOUS; the leaf was never wrong —
the ROOT display was. Proof now DISPLAYED as **(SL≥2)** at Step 5; §1 quantified.

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| V2-1 | BLOCKER | Step 18b names only (S1)/(S2)/(r1) while D-11 r2 declares seam (S3), sent to annex A-1 — an unpositioned annex edge; the acyclic composition not closed | ACCEPTED as a display gap; RESOLVED by the r2's own scoping, now displayed: (S3) = the projective assembly weight's MASS reading conditions ONLY the projective reading of M2's conclusion ("M2 as stated (monic ensemble) does not consume it" — D-11 r2 §3 + Theorem M2's bracket); Step 18b consumes M2 at MONIC scope = clause (0)'s ensemble, so NO §2 step consumes (S3); it is an annex A-1 obligation, in no (ROOT-C) hypothesis. Step 18b's scope display added; §3.3 row repositioned. The dependency graph closes without the edge | Step 18b, §3.3 |
| V2-2 | BLOCKER | (H4) declared "for clause (UB) only", but Step 18's K3-c consumes O-8b/(R1)–(R4) (declared row D-c), so clause (R) depends on part of H4; and the "Without (H4)" sentence asserted the (R1)–(R4)-conditional alphabet law hypothesis-free | ACCEPTED — a genuine leakage; (H4) SPLIT: **(H4a)** = (R1)–(R4), consumed by clause (R) (Step 18 K3-c, base-changed β-legs) AND clause (UB) (Step 16); **(H4b)** = (UB-X), clause-(UB)-only. Per-clause hypothesis attribution displayed in (ROOT-C); the "Without (H4)" sentence corrected (alphabet law now stated under (H4a)); Step 16/18/19 + §3.1 rows synced | §1, Steps 16/18/19, §3.1 |
| V2-3 | ADVISORY | Step-1 Appendix-A re-point compositionally valid (confirmation) | RECORDED; no action — the REVISION-1 finding-1 repair stands verified | Step 1 |
| V2-4 | BLOCKER | B-4 consumed at Step 2 and graded PROVED-UNVERIFIED, but the §3.2 B-layer row omits it — a consumed input with no ledger class | ACCEPTED; B-4 (order-r layer) ADDED to the §3.2 B-layer row with its full grade (order 1 = B-3's VERIFIED-LEAN core; order r = `M6_lemma.md`, modern face O-9's OL-B → GD-3, orders ≥ 2 riding §3.1's (GR-B) on the modern path). Three-class completeness restored | §3.2 |
| V2-5 | BLOCKER | Step 14's (ADM) hypothesis (O-9 r2) has no ledger home — a consumed condition in no class | ACCEPTED; new §3.1 [M] row **(ADM)** (ledger-admissibility of the census datum; PROVED automatic at orders ≤ 1; at orders ≥ 2 open but p-free and per-datum decidable — borderline [M]/[T], kept [M] for honesty); carried by **(ROOT-C)(H3)**; enters Step 14 (CEN-W/CEN-J) hence Step 18's K3 census half. Step 14 + §1 headline synced | §3.1, Step 14, §1 |
| V2-6 | BLOCKER | clause (R)'s self-loop formula unquantified — at e = 1 the denominator vanishes, contradicting ℛ-membership and pole-freeness; no step excluded e = 1 | ACCEPTED — the real bug; adjudicated as displayed in the headline above: **e ≥ 2 forced** (L4 strict descent + K_1 = (0)); proof DISPLAYED as **(SL≥2)** at Step 5 with the consumer audit (Steps 5/17/18 consume only through O-12's e ≥ 2-scoped theorems; no consumer instantiates e = 1); §1 formula quantified with the exponent bound e(e+1)/2 − 1 ≥ 2 | §1, Step 5, Step 19 |
| V2-7 | ADVISORY | roster consumer annotations incomplete: MacLane entry omits Step 9 (and Step 9 unpinned); GMN consumer list omits Step 15 | ACCEPTED; MacLane entry + Step 9 now pin the exact loci (Def 4.1 + Thm 4.2 = the (V1) frame-semantics pin, LITUNIT §4 MATCH; no further MacLane theorem enters O-14a′); GMN entry gains Step 15 (K-FACT = Thms 1.15/1.19 + 3.1/3.7, loci already pinned) | Step 9, §4 items 1/2 |

**The completeness invariant, re-run after these repairs** (the §7 closing charge —
"every named pin/flag has exactly one ledger home" — which V2-4/V2-5 again showed is
the failure-prone one): the (ROOT-C) hypothesis list is now (H1) (GR-B) ≥ 2, (H2)
(FRESH), (H3) D-12r + (E-N) + (ADM), (H4a) (R1)–(R4), (H4b) (UB-X) — exactly the
seven §3.1 [M] rows, none clause-misattributed; (S3) is the one declared seam with a
NON-mandatory home (annex A-1), displayed as such at both its touch points.

**What VC3 (the next end-to-end pass) should check first:** (1) the (SL≥2) display
against O-12 rev 4 (is the three-step forcing airtight, and is the consumer audit
complete?); (2) the (H4a)/(H4b) per-clause attribution in (ROOT-C) against Steps
16/18/19 (does any OTHER §3.1 row leak across clauses the same way?); (3) the (S3)
scoping against D-11 r2 §3 (is the monic/projective split verbatim-faithful?).

**Standing note on status syncs:** REVISION 2 makes NO status changes — VC2 was a
composition-only pass; leaf statuses remain as synced at REVISION 1 (the c43b13b
harvest + the recorded in-flight r2/r5 landings). The in-flight passes (D-11/O-11
pass 2s, CU-1 gap-check, O4T clean pass, O-9 re-pass) resolve at the next harvest
and their rows carry unchanged.

— REVISION 2 complete. The body above is the operative composition; VC3 runs on this
document as a whole.

---

## 9. REVISION 3 (2026-08-02 night, unit ROOT r4) — disposition of VC3, the Phase C end-to-end pass 3

**The pass.** VC3 = the third end-to-end verification and the first MODEL-DIVERSE one
(Claude/Fable, fresh context — VC1/VC2 were Codex; read the ROOT document ONLY, no
leaf records; `lean/notes/openmath/VC3_root_fable_verify.md`). Verdict:
**COMPOSITION-GAPS** — 4 blockers (B1–B4) + 4 advisories (A5–A8). The headline is
what VC3 CONFIRMED: "under all five hypotheses jointly, the composition proves
(ROOT-C) quantifier-for-quantifier from the leaves' declared statements + ledger
rows; every defect found is attribution/ledger integrity, not mathematics missing
beyond the displayed hypotheses." Explicit passes: acyclicity edge-by-edge over all
20 steps (incl. the 17 ≺ 18 ≺ 18b stratification); the completeness invariant
((ROOT-C) hypotheses ↔ the seven §3.1 [M] rows, EXACTLY EQUAL); and all three §8
pre-charged spots — the (SL≥2) display internally sound with its consumer audit, the
(H4a)/(H4b) split consistent as displayed, the (S3) monic/projective split coherent.
Every finding is dispositioned below and repaired in place; per the standing charge,
NO mathematical content changed — every repair is attribution, a ledger row, or a
status sync.

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| B1 | BLOCKER | the "exact" per-clause attribution understates clause (UB): it reads "(H4a) + (H4b)" as (UB)'s full hypothesis set, but (UB) also consumes (H1)–(H3) — through Step 8 ((GR-B) at orders ≥ 2), Step 14's census ((FRESH)/(ADM)), and the (UB-X)(a)-transported layers (D-12r, (E-N)); the (ADM) row's Enters-list omitted Step 16 | ACCEPTED — the V2-2 genre in reverse (consumed but unattributed); (ROOT-C)'s attribution completed: clause (UB) consumes **(H1)–(H3) + (H4a) + (H4b)**, only (H4b) is (UB)-exclusive; Step 16's conditionality restated in full; Step 19's (UB) close synced; the (ADM) and (FRESH) rows' Enters-lists gain Step 16/clause (UB). The JOINT theorem (all five hypotheses) was never affected | §1, Steps 16/19, §3.1 (ADM)/(FRESH) |
| B2 | BLOCKER | O-8b/M14's PROVED-UNVERIFIED conditional proof is consumed (Step 16; Step 18's K3 — hence clause (R)'s mandatory path) with NO ledger row naming its pending pass — the exact V2-4/V2-5 failure class | ACCEPTED; new §3.2 row **P(O-8b/M14)** (fresh hostile pass on the M14 proof, owners Steps 16/18); Step 16's status names the pass | §3.2, Step 16 |
| B3 | BLOCKER | Σ_τ R_τ = 1 credited to Step 1 beyond Dfloor's declared statement (R_τ undefinable in the leaf's perimeter; α = R_τ needs Steps 18/18b) — a textual forward edge inside a VERIFIED perimeter, consumed by Step 19 "from Step 1" | ACCEPTED; repaired exactly as the verifier prescribed, no new hypothesis: Step 1 restated to its declared perimeter (Σ_τ α = 1 at every p + the interpolation principle; Delivers-line synced), and the identity CONCLUDED at Step 19 with the derivation displayed (Σα = 1 [Step 1] + α = R_τ ∀p [Steps 18/18b, (H1)–(H3)+(H4a)] + the principle ⇒ Σ_τ R_τ = 1 in ℚ(t)); the conditionality was already priced inside clause (R) | Step 1, Step 19 |
| B4 | BLOCKER | B-3's document-level PROVED-UNVERIFIED grade (same status shape as B-2's: Lean-verified core + June document) missing from the §3.2 B-layer row — a consumed input with no ledger class (the V2-4 class) | ACCEPTED; **B-3 (doc level)** added to the B-layer row | §3.2 |
| A5 | ADVISORY | undisplayed interface match: O-8b's law holds at δ-STABLE position classes only, but nothing displays that K3-c's δ > 1 pools are δ-stable — if one were not, (UB-X)(b) would leak into clause (R) | ACCEPTED as an honesty display: the presumption is now DISPLAYED at Step 18 and recorded as an audit note on the §3.1 (R1)–(R4) row (the match is asserted at the D-c interface, not proved here); the match display (or a re-scope) rides the next O-11/O-8b touch | Step 18, §3.1 |
| A6 | ADVISORY | three consumer-list gaps: (i) GMN roster omits Step 11 (CU-2t consumes Cor 1.20/3.8 directly); (ii) the D-12r row omits Steps 2 and 19 (T-6's TB-CAP conditionality; the bracket consumes T-6); (iii) the (FRESH) row omits Step 10 while Step 10's Delivers-line names "the (FRESH) consumption site" | ACCEPTED; all three lists completed ((iii) resolved as CONSUMPTION at CU-1's step — the row itself prices (FRESH) "at the GD-2/CL-13 vertex-law layer = CU-1's step content"; clause (SQ)'s (H1)–(H3) attribution already covered it) | §4 item 1, §3.1 D-12r/(FRESH) |
| A7 | ADVISORY | OL-O12-1 (Step 5's contingent escape flag) has no ledger home, against the §7 closing standard | ACCEPTED; §3.3 row added (unfired, consumed by nothing — homed for the invariant) | §3.3 |
| A8 | ADVISORY | Step 19 discharges T-U's §U.SQ.6 kernel ledger by the ASSERTED-not-displayed claim that it equals the Steps 1–18b roster; a fresh reader cannot check it | ACCEPTED at the honest-marking option: Step 19's parenthetical now states the claim is asserted, and a §3.3 audit row owes the row-by-row display table (per-item pointer §U.SQ.6 → step/eviction/annex) | Step 19, §3.3 |

**Status syncs applied with the repairs** (the 2026-08-02-night harvest, ledger
7b28562 — "THE FIVE OWED PASSES" section): **CU-1 = VERIFIED, leaf #5** (r5 gap-check
GAPS-CLOSED 0/0, commit 5329a4e); **O-10 = VERIFIED at scope, leaf #6** (pass 2
model-diverse SOUND-WITH-GAPS, math airtight, 5296528; two minor gaps ride a §3.2
doc-touch row) — **VERIFIED = 6: O-13, O-12, Dfloor, GD23, CU-1, O-10** (headline +
(ROOT-C) proof-status synced 4 → 6; the discharged CU-1/O-10 §3.2 rows retired).
Wave-14 rows synced: **O-9** pass 2 UNSOUND (af2293e: the rev-2 (ADM) class-vs-value
attainment equivalence refuted by an r = 1 countermodel) → **r3 with the
VALUE-attained (ADM) reading** (§3.1 (ADM) row annotated); **CUC/CU-2t** pass 1
UNSOUND (b9d1d70: the Θ^τ ν-blindness critical + 9 gaps) → **CU-2t R2** (Definition
RC displayed; the (T-DEC)/(T-VERD) pricing honesty-corrected — the mandatory-path
residue GREW by two GD-4 rows, folded at Step 11 + the §3.3 GD-4/GD-6 row per the
R2's F4 charge), re-pass owed; **O4T** pass 3 SOUND-WITH-GAPS,
ZERO criticals (ed413ae) → **r4 (two one-liners)** + one confirming pass to
the bar. Also recorded (round-3 events pre-dating VC3, rows previously stale): D-11
pass 2 → **r3** (8a0786c, honestly LAYERED) and O-11 pass 2 (V32) → **r3** (7aef1b9,
tier 2 split into H-1..H-4), pass 3s owed. Nothing in this revision upgrades any
leaf beyond the ledger's own adjudications; it corrects this document's composition
bookkeeping and syncs recorded statuses.

**The completeness invariant, re-run after these repairs** (the failure-prone one
again — B2/B4 are its V2-4/V2-5 class, and B1 is its per-clause face): the (ROOT-C)
hypothesis list is unchanged — (H1) (GR-B) ≥ 2, (H2) (FRESH), (H3) D-12r + (E-N) +
(ADM), (H4a) (R1)–(R4), (H4b) (UB-X) — exactly the seven §3.1 [M] rows; the
per-clause attribution is now: (0) unconditional; (SQ)/(R) ← (H1)–(H3), (R)
additionally ← (H4a); (UB) ← (H1)–(H3) + (H4a) + (H4b); (H4b) (UB)-only. Every
consumed PROVED-UNVERIFIED input now has a named §3.2 pass (O-8b/M14 and B-3-doc were
the last two missing); every named pin/flag has exactly one ledger home (OL-O12-1 was
the last).

**What VC4 (the next end-to-end pass, Codex, toward the ≥ 2-consecutive-clean bar)
should check first:** (1) the B3 repair — does Step 19's displayed Σ_τ R_τ = 1
derivation consume only Steps 1/18/18b at their declared statements, and is Step 1's
restated perimeter now exactly the Dfloor leaf's?; (2) the completed (UB) attribution
against Steps 8/14/16 (is (H1)–(H3) + (H4a) + (H4b) now exact, neither under- nor
over-attributed?); (3) the A5 presumption at Step 18 (is the δ-stable audit note
scoped so that clause (R)'s (H4a)-only attribution is honest as displayed?); (4) the
completeness invariant, per standing §7/§8 experience.

**Concurrent in-flight events recorded at REVISION 3 close** (the wave-14 repair
commits landed by parallel units while this revision was being written; body statuses
are AS OF the 7b28562 harvest, with bracketed landed-at-close notes at the affected
rows): **O-9 r3** (460ea59 — (ADM) redefined VALUE-attained; automaticity rescoped
r = 0 automatic / r = 1 displayed decidable inequality / general r ≥ 1 withdrawn; K6
falsifier 0 = 0 exact at the pass-2 countermodel); **CU-2t R2** (unit of record
567814d — Definition RC; Θ^τ carries ν's announced pair; the +2 GD-4 rows pricing);
**O4T r4 + O-10 r2** (3956500 — display fixes, cores byte-stable; O-10's VERIFIED
stands per the wave-14 ledger). Confirming passes owed: O4T (acceptance), O-9 pass 3,
the CU-2t R2 re-pass. Bookkeeping note (the REVISION-1 4e03512 precedent): commit
9136564's staging sweep captured this revision's in-progress body repairs alongside
the wave-14 ledger fold; THIS commit is the revision's unit of record.

— REVISION 3 complete. The body above is the operative composition; VC4 runs on this
document as a whole, and the ≥ 2 consecutive-clean counter remains at ZERO.

---

## 10. REVISION 4 (2026-08-02+, unit ROOT r5) — disposition of VC4, the Phase C end-to-end pass 4

**The pass.** VC4 = the fourth end-to-end verification (Codex, fresh context, run on
the REVISION-3 document with the §9 priority charges;
`lean/notes/openmath/VC4_root.jsonl`). Verdict: **COMPOSITION-GAPS** — 4 blockers +
1 advisory, numbered VC4-1..VC4-5 in report order. What VC4 PASSED on the document's
own declarations: acyclicity (including Step 1's Appendix-A dependency and
17 ≺ 18 ≺ 18b); the (SL≥2) e = 1 exclusion and the displayed denominators; Step 19's
repaired Σ_τ R_τ = 1 derivation (Step 1 + Steps 18/18b + interpolation — the §9
pre-charged B3 repair); clause (UB)'s displayed joint attribution; and the syntactic
correspondence of the seven [M] rows to the grouped hypotheses (with findings 2/3
blocking the SEMANTIC completeness invariant — both now repaired). As at VC3, every
finding is attribution/ledger integrity; the joint conditional theorem was not
touched. Every finding is dispositioned below and repaired in place; NO mathematical
content changed — every repair is an attribution, a ledger row, or a status sync.

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| VC4-1 | BLOCKER | the δ-stable K3-c interface is assumed, not supplied: Step 18 says O-8b applies only to δ-stable position classes and the stability of K3-c's pools is merely ASSERTED — no (H4a) content or [V]/[T] row supplies it, so the claimed exact attribution "clause (R) ← (H1)–(H3)+(H4a) only" is unproved (under all five hypotheses jointly, (UB-X)(b) covers it) | ACCEPTED; the fragment is PRICED, not presumed: named **(K3-δ)** = (UB-X)(b) restricted to K3-c's realized δ > 1 pools, displayed on the (UB-X) row, and clause (R)'s attribution now consumes it explicitly ((H1)–(H3) + (H4a) + (H5) + (K3-δ)); "only (H4b)'s remainder beyond (K3-δ) is (UB)-exclusive" replaces the former sentence; the joint theorem is unchanged ((K3-δ) ⊆ (H4b)); the direct match display rides the next O-11/O-8b touch and would retire (K3-δ) from clause (R) | §1, Step 18, §3.1 (R1)–(R4)/(UB-X), Step 19 |
| VC4-2 | BLOCKER | (ADM) has two incompatible operative definitions — the §3.1 statement column still displayed the REFUTED class-attainment formula (β_k mod e ∈ {wt(𝐣) mod e}) while the r3 annotation said value-attainment governs; §1 and Step 14 retained the false "automatic at orders ≤ 1" | ACCEPTED; the (ADM) row REWRITTEN to a single operative definition — and synced one rung further to O-9 r4's FULL-attainment form (dim G_{β_k} = d; the r3 VALUE form itself was refuted at d ≥ 2 by O-9 pass 3), with both refuted displays quarantined inside the row as history; §1's (H3) parenthetical and Step 14's rev-2 text corrected to "automatic at r = 0 only"; every stale phrasing marked superseded | §3.1 (ADM), §1, Step 14 |
| VC4-3 | BLOCKER | an OPEN clause is consumed with no [M] hypothesis: Step 17's r3 status says the actual-pack correspondence (clause 4) is OPEN, yet Steps 18/18b consume Step 17 AS that correspondence, and the §3.2 D-11 [V] row cannot classify it (no proof on file) — composition closure and the completeness invariant break | ACCEPTED; new §3.1 [M] row **(PACK)** (D-11's clause 4 = OL-O12-2's remaining data half; borderline [M]/[T], kept [M] for honesty), carried as NEW hypothesis **(ROOT-C)(H5)**, consumed by clauses (R)/(UB) at Steps 17 → 18 (K2)/18b; the §3.2 D-11 row's scope restated (proof-layer passes only, clause 4 carved OUT); Step 17/18/18b/19 + the headline + the OL-O12-2 row synced. DISTINCT from D-11 item (vii) (H.6-fence conformance — the TREE-GAPS §3.3 row; promotion question flagged to VC5) | §3.1 (PACK), §1, Steps 16/17/18/18b/19, §3.2, §3.3 |
| VC4-4 | BLOCKER | clause (SQ)'s attribution is not exact: §1 attributed all of (H1)–(H3) incl. (ADM) to (SQ), but (ADM) enters only the Step-14 census/value chain, and for the §1 SEMANTIC definitions of decided_τ(k)/undec(k), Step 1 supplies the bracket and semantic drainage unconditionally — an over-attribution of (ADM) and the entire conditional classifier-transfer route | ACCEPTED — corrected DOWNWARD (a favorable honesty fix: the hypotheses shrink): clause (SQ) = Step 1's bracket + Step 13's semantic drainage layer, **unconditional at every prime, consuming NO §3.1 row**; the classifier-side counting machinery (D-15/T-6 bracket legs, the O4T transfer layer/(AGR)) re-attributed to the evaluation squeeze inside clause (R)'s scope; the REVISION-3 A6(ii)/(iii) phrasings ("the (SQ) bracket consumes T-6", "clause (SQ) inherits (FRESH)") superseded at the D-12r/(FRESH) rows; Step 13's Delivers-line restated | §1, Steps 13/19, §3.1 D-12r/(FRESH)/(ADM) |
| VC4-5 | ADVISORY | Step 18b's local hypothesis account omits (ADM): it consumes Step 14 through seam (S1) but summarized the inherited open rows as (GR-B)/(FRESH) only — a local consumer-accounting defect (the root-level clause-(R) attribution already included (ADM)) | ACCEPTED; Step 18b's (S1) line restated to the census's FULL displayed hypothesis set (GR-B)/(FRESH)/(ADM); the (ADM) row's Enters-list gains Step 18b via (S1) | Step 18b, §3.1 (ADM) |

**Folds executed with this revision (beyond VC4).**
(1) **The CU-2t R3 three-row GD-4 amendment** (81589e9; ledger amended at 35faac7):
the GD-4 terminal residue is THREE rows (T-DEC, split dec/cor at R3 · T-READ ·
T-VERD), count history 0 → 2 → 3 — folded at Step 11 + the §3.3 GD-4/GD-6 row (the
R3's §9.5 touch (3)); touch (5) (the W3-carrier/2(D)-scope delivery) folded into the
§3.2 P(O-2a r5) row's charge.
(2) **Tree DELTA 4 (unit TREE-GAPS) — landed CONCURRENTLY with this revision, its
ROOT touches executed by that unit in this document** (the [TREE-GAPS] tags): the
Step-18 H.6-fence display (audit gap G-4: SUPERSEDED AS ROUTED into D-11 widened-
charge item (vii), no (ROOT-C) hypothesis affected), the §3.3 widened-charge and
O-14a/b/c rows, and the closed-row/roster items (audit advisories A-1..A-5). This
revision's own Step-18 pending-fold placeholder was removed when that display
landed; the two units cross-reference and do not conflict — (H5)/(PACK) is D-11's
clause 4, item (vii) is a different D-11 clause, and both texts say so.
(3) **The REG leg** (044a6d9, accepted at e8e1839): all 18 standing falsifier/
verification suites re-run at HEAD — ZERO-REGRESSIONS, all diffable outputs
line-for-line identical; root criterion 3 evidence at HEAD (not an acceptance event).

**Status syncs applied with the repairs** (the confirming-passes/O-9-r4/CU-2t-R3
harvest, ledger 85670f7 → 35faac7): **O4T = VERIFIED, leaf #7** (pass 4 acceptance
CLEAN 0/0, 8602e97) — **VERIFIED = 7: O-13, O-12, Dfloor, GD23, CU-1, O-10, O4T**
(headline + (ROOT-C) proof-status synced 6 → 7; the discharged O4T §3.2 row
retired). **O-9**: pass 3 on r3 UNSOUND (b7df5c2: value ≠ FULL attainment at d ≥ 2)
→ **r4 with (ADM) at FULL attainment** (35faac7; K7 falsifier ALL PASS incl. the
first d ≥ 2 value gate) — **pass 4 in flight**; the §3.1 (ADM) row and (ROOT-C)(H3)
carry the r4 form. **CU-2t**: R2 pass SOUND-WITH-GAPS 0-critical (4190ca4) → **R3**
(81589e9, three-row pricing) — fresh pass on R3 owed. D-11/O-11 pass 3s unchanged
(owed). Nothing else moved.

**The completeness invariant, re-run after these repairs** (the standing failure-
prone check — VC4-3 is its missing-row face, VC4-1/VC4-4 its per-clause face): the
(ROOT-C) hypothesis list is now **(H1) (GR-B) ≥ 2, (H2) (FRESH), (H3) D-12r + (E-N)
+ (ADM), (H4a) (R1)–(R4), (H4b) (UB-X) (incl. its named (K3-δ) fragment), (H5)
(PACK)** — exactly the EIGHT §3.1 [M] rows (seven at REVISION 3 + (PACK)), none
clause-misattributed. Per-clause attribution: **(0) unconditional** (Step 1); **(SQ)
unconditional** (Steps 1 + 13-semantic — corrected downward at VC4-4); **(R) ←
(H1)–(H3) + (H4a) + (H5) + (K3-δ)**; **(UB) ← (H1)–(H3) + (H4a) + (H4b) + (H5)**;
only (H4b)-minus-(K3-δ) is (UB)-exclusive. Every consumed PROVED-UNVERIFIED input
has a named §3.2 pass; every consumed OPEN clause is now a §3.1 row ((PACK) was the
last); every named pin/flag has exactly one ledger home (the TREE-GAPS rows homed
the audit's G-1..G-5/A-1..A-5 residue). The one flagged promotion question — D-11
item (vii), [T] vs hypothesis grade — is displayed at its §3.3 row for VC5.

**What VC5 (the next end-to-end pass, toward the ≥ 2-consecutive-clean bar) should
check first:** (1) the VC4-4 downward correction — is clause (SQ) as displayed in §1
(semantic definitions) genuinely carried by Step 1 + Step 13's semantic layer alone,
and is no classifier-transfer content silently needed by the (SQ) statement?; (2)
the (PACK)/(K3-δ)/(H5) additions against Steps 17/18/18b — is the eight-row
bijection exact, and is (K3-δ)'s ⊆-(H4b) containment displayed correctly at both
touch points?; (3) the single-definition (ADM) row against O-9 r4 (is the FULL-
attainment form quoted faithfully, with no stale automaticity phrasing surviving
anywhere — grep "orders ≤ 1"); (4) the TREE-GAPS folds (Step 18's fence display +
the widened-charge row: does item (vii) belong at [T], or is it hypothesis-grade
like clause 4?); (5) the completeness invariant, per standing experience.

**Concurrency and bookkeeping.** This revision was written concurrently with the
TREE-GAPS unit's DELTA 4 ROOT touches in this same document (both landed cleanly at
disjoint sites; each references the other where they meet — Step 18 and the §3.3
rows). Commit 698a629's checkpoint sweep captured in-progress states of both; the
commit closing this revision is its unit of record. Body statuses are AS OF the
85670f7/35faac7 harvest; the in-flight passes (O-9 pass 4, the CU-2t R3 pass, the
D-11/O-11 pass 3s) resolve at the next harvest and their rows carry unchanged.

— REVISION 4 complete. The body above is the operative composition; VC5 runs on
this document as a whole, and the ≥ 2 consecutive-clean counter remains at ZERO.

---

## 11. REVISION 5 (2026-08-03, unit ROOT R6) — disposition of VC5, the Phase C end-to-end pass 5

**The pass.** VC5 = the fifth end-to-end verification (Codex, fresh context, run on
the REVISION-4 document with the §10 priority charges;
`lean/notes/openmath/VC5_root.jsonl`). Verdict: **COMPOSITION-GAPS** — 2 blockers +
1 advisory, numbered VC5-1..VC5-3 in report order. The two blockers are ONE genre,
and it is this document's OWN rule turned on itself: §3's character classes define
[T] as "content pinned to printed sources or existing proofs; writing/auditing owed,
no new mechanism expected" — but four §3.3 rows (CU-2t's three terminal rows and
D-11's widened-charge item (vii)) are OPEN PROPOSITIONS consumed on clause (R)'s
mandatory path, expressly described by their own row text as "genuinely NEW unproved
mandatory-path obligations" resp. "proved nowhere at general n". Placing them in [T]
does not supply them; they are undischarged mathematical premises, analogous to
(PACK) at VC4-3. The repair is the honest one: PROMOTION to a §3.1 [M] row group,
carried as the NEW hypothesis **(ROOT-C)(H6)**. As at VC3/VC4, no mathematical
content changed — the leaves were never wrong; this document's classification of
what it consumes was.

**What VC5 PASSED on the displayed material** (the verifier's own pass-list): the
operative eight-row hypothesis mapping syntactically EXACT (H1 = GR-B; H2 = FRESH;
H3 = D-12r, E-N, ADM; H4a = R1–R4; H4b = UB-X; H5 = PACK); clause (0) unconditional
from Step 1; clause (SQ) unconditional from Step 1's bracket + Step 13's semantic
drainage under the §1 semantic definitions (no classifier-transfer hypothesis
needed — the VC4-4 downward correction VERIFIED); the FULL-attainment (ADM) as the
single operative form (older formulations only in explicitly superseded history);
(PACK)/(K3-δ) correctly propagated through Steps 17–19, (K3-δ) visibly a fragment
of (H4b); acyclicity of the displayed dependency graph including the repaired
Step-1 edge and 17 ≺ 18 ≺ 18b; the Step-19 Σ_τ R_τ = 1 derivation; and no
additional undeclared forward edge in the visible walk.

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| VC5-1 | BLOCKER | D-11 item (vii) consumed but misclassified: Step 18's K3-a requires general-n H.6 in-fence conformance of the built tables, which the document itself says is "proved nowhere at general n", yet it sat in §3.3 as [T] — an undischarged mathematical premise on clause (R), analogous to (PACK); the eight [M] rows were not the complete hypothesis list | ACCEPTED; item (vii) PROMOTED to §3.1 — a member of the NEW row group **(H6)**; Step 18's fence-display routing ("no (ROOT-C) hypothesis is affected") RETRACTED and superseded in place; the §3.3 widened-charge row keeps items (v)/(vi) only; the REVISION-4 promotion flag is resolved (promoted) | §3.1 (H6), §1, Steps 18/19, §3.3 widened-charge row, (PACK) row |
| VC5-2 | BLOCKER | the three GD-4 terminal obligations lack suppliers: Step 18 consumes (T-DEC)/(T-READ)/(T-VERD) through D-a/K4-CYL; §3.3 calls them "genuinely NEW unproved mandatory-path obligations" and (T-READ) has "NO displayed machine-side home on file"; placing them in [T] does not supply them — clause (R) silently relied on unresolved content outside its eight [M] hypotheses | ACCEPTED; the three rows PROMOTED to §3.1 as the terminal-seam members of **(H6)**, with CU-2t R4's grading carried (per-row: (T-READ) GD-2/GD-5-family read-semantics conformance — a proof-by-construction route cannot discharge it; (T-DEC)/(T-VERD) GD-4 with construction routes on record); consumed by clause (R) at Step 11's route (CU-2t's §9.5 conditionality, which Step 18 consumes through D-a/K4-CYL) and by clause (UB) through the transported chain; the §3.3 GD-4/GD-6 row keeps only genuine engine-bookkeeping [T] content | §3.1 (H6), §1, Steps 11/18/19, §3.3 GD-4/GD-6 row |
| VC5-3 | ADVISORY | the T-U closing correspondence remains externally opaque: the §U.SQ.6 → Steps 1–18b row-by-row mapping is asserted, not displayed — openly recorded as a [T] debt, so not itself a hidden edge, but the document alone cannot establish that §U.SQ.6 contains no additional consumption | RECORDED, no reclassification: this is exactly the REVISION-3 A8 audit row doing its job — the row stays the recorded [T] audit debt (annotated as re-confirmed); the display table (per-item pointer §U.SQ.6 → step/eviction/annex) remains owed there and gates acceptance through §3.3's emptying, not through a hypothesis | §3.3 T-U row |

**The (H6) group, stated once** (full display at the §3.1 row): (T-DEC) = emission
discipline at decided sites + the corner fence (split dec/cor at CU-2t R3); (T-READ)
= the emitted shape/position fields equal the (c1)-shape polygon-side datum (the
(RC-read) supply); (T-VERD) = terminal verdict-field conformance; item (vii) =
general-n H.6 in-fence conformance of the built tables. Owners: GD-4/GD-2-family per
CU-2t R4's grading; the D-11 owner for item (vii). Enters clauses (R)/(UB) only.

**Status syncs applied with the repairs** (the 6a7593b harvest — "O-9 + CU-2T
VERIFIED"): **O-9 = VERIFIED, leaf #8** (pass 4 SOUND 0-critical → r5 gloss purge,
9335057 → pass 5 confirming CLEAN 0/0, 825378d, K7 re-executed byte-identical) — the
order-r census layer at its honest scope (CEN-W/CEN-J conditional on (GR-B) +
(FRESH) + (ADM)-FULL, all displayed §3.1 rows, unchanged by the adjudication).
**CU-2t = VERIFIED at scope, leaf #9** (R3 pass 0-critical, both logic repairs
verified → R4, b9f05db: the (T-READ) re-grade + prose repairs → R4 spot-check CLEAN,
d14cd8c, no further pass owed per the verifier's own disposition) — conditional per
§9.5 on exactly the three terminal rows (= the (H6) terminal members) + the pinned
literature. **VERIFIED = 9: O-13, O-12, Dfloor, GD23, CU-1, O-10, O4T, O-9, CU-2t**
(headline synced 7 → 9; the discharged §3.2 rows retired). Note the interlock, said
plainly: CU-2t's VERIFIED grade is exactly what makes the (H6) promotion the RIGHT
repair rather than a leaf reopening — the leaf is verified MODULO the three rows it
displays, and the composition must therefore carry them as hypotheses until the
conformance units land. D-11/O-11 pass 3s unchanged (owed). Nothing else moved.

**The completeness invariant, re-run after these repairs** (the standing
failure-prone check — VC5-1/VC5-2 are its missing-row face, at the [T]/[M] boundary
this time): the (ROOT-C) hypothesis list is now **(H1) (GR-B) ≥ 2, (H2) (FRESH),
(H3) D-12r + (E-N) + (ADM), (H4a) (R1)–(R4), (H4b) (UB-X) (incl. its named (K3-δ)
fragment), (H5) (PACK), (H6) the terminal-seam engine-conformance group
((T-DEC)/(T-READ)/(T-VERD) + item (vii))** — exactly the NINE §3.1 [M] rows/groups
(eight at REVISION 4 + the (H6) group), none clause-misattributed. Per-clause
attribution: **(0) unconditional** (Step 1); **(SQ) unconditional** (Steps 1 +
13-semantic); **(R) ← (H1)–(H3) + (H4a) + (H5) + (K3-δ) + (H6)**; **(UB) ←
(H1)–(H3) + (H4a) + (H4b) + (H5) + (H6)**; only (H4b)-minus-(K3-δ) is
(UB)-exclusive. Every consumed PROVED-UNVERIFIED input has a named §3.2 pass; every
consumed OPEN proposition is now a §3.1 row (the four (H6) members were the last —
and the VC5 genre check is now standing: a [T] row must never carry a consumed open
proposition); every named pin/flag has exactly one ledger home.

**What VC6 (the next end-to-end pass, toward the ≥ 2-consecutive-clean bar; model
diversity favors Fable) should check first:** (1) the (H6) promotion — is the
NINE-row bijection exact, are the four members' consumption routes as displayed
(Step 11 §9.5 / Step 18 D-a/K4-CYL / Step 18 K3-a), and is the (R)/(UB)-only
attribution neither under- nor over-stated (in particular: does the (UB) inheritance
through the transported chain hold row-for-row)?; (2) the VC5 genre, re-audited
document-wide: walk EVERY remaining §3.3 [T] row against the §3 classification rule
— does any other [T] row carry an open proposition consumed on a mandatory path
(candidates the VC5 pass-list did not explicitly clear: the S-1 audit row, the
(I-τ)+K-LOC interface row, the GD-7 flags)?; (3) the T-U §U.SQ.6 row (VC5-3, thrice
recorded): does the opacity gate the acceptance criterion as displayed, and should
the display table be executed before VC7?; (4) the CU-2t/O-9 VERIFIED-at-scope
interlocks — do Step 11's and Step 14's synced statuses consume their leaves at
exactly the adjudicated scopes (nothing upgraded in transit)?; (5) the completeness
invariant, per standing experience.

**Concurrency and bookkeeping.** Body statuses are AS OF the 6a7593b harvest; the
in-flight passes (the D-11/O-11 pass 3s) resolve at the next harvest and their rows
carry unchanged. This revision executes the ROOT-side sync of the 6a7593b
adjudication in the same unit as the VC5 disposition (one fold, no intermediate
state); the commit closing this revision is its unit of record (unit ROOT R6).

— REVISION 5 complete. The body above is the operative composition; VC6 runs on
this document as a whole, and the ≥ 2 consecutive-clean counter remains at ZERO.

---

## 12. REVISION 6 (2026-08-03+, unit ROOT R7) — disposition of VC6, the Phase C end-to-end pass 6

**The pass.** VC6 = the sixth end-to-end verification and the SECOND model-diverse
one (Claude/Fable, fresh context, run on the REVISION-5 document with the §11
priority charges; `lean/notes/openmath/VC6_root_fable_verify.md`). Verdict:
**COMPOSITION-GAPS** — 3 blockers (F-1/F-2/F-3) + 6 advisories (A-4..A-9). The
three blockers are ONE genre, and it is the VC5 genre WALKED TO COMPLETION: the
[T]→[M] classification rule ("a [T] row must never carry a consumed open
proposition") applied at REVISION 5 to D-a's terminal content and item (vii), but
not yet to the sibling declared row D-b ((I-τ)/K-LOC), not to O-2a's GD-1 residue
face, and not to the GD-6 instances Step 10's own conditionality line displays. As
at VC3/VC4/VC5, the composition's MATHEMATICS was re-confirmed — VC6's pass-list:
acyclicity edge-by-edge over all 20 steps (no new forward edge from the REVISION-5
edits), the NINE-row bijection EXACT, per-clause attribution consistent at every
displayed site, Σ_τ R_τ = 1, (SL≥2), both unconditional clauses, the leaf-#8
interlock exact, and the (H6) promotion mechanics correctly executed. Every finding
is dispositioned below and repaired in place; NO mathematical content changed and
NO status moved (a composition-only pass; VERIFIED = 9 stands at the 6a7593b
harvest).

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| F-1 | BLOCKER | declared row D-b ((I-τ)/K-LOC) — the LAST unpromoted member of O-11 r2's D-a/D-b/D-c triple — is a consumed open interface in [T]: K-LOC is the document's own named OPEN lemma (M07 OL-3), consumption triple-displayed (Step 15's application line; Step 18's K1; the row's owner column), and "both priced into the GD families" had NO displayed [M] landing | ACCEPTED; PROMOTED as its OWN row **(H7)** (adjudication displayed at the §3.1 row: engine-conformance GENRE kin to (H6), but a disjoint SEAM (K1/product law vs the terminal seam), disjoint owners (O-10/GD-family vs CU-2t/D-11), and (H6)'s membership is pinned by the leaf-#9 interlock — so a sibling row, not an (H6) member); consumed by clauses (R)/(UB); §1/Steps 15/16/18/19/§3.1/§3.3-tombstone synced | §3.1 (H7), §1, Steps 15/16/18/19, §3.3 |
| F-2 | BLOCKER | GD-1's (KP)/(KPirr) clauses beyond order 1 (= OL-2a-1, an exactly-stated open lemma of consumed O-2a) sat in [T] with a pin covering ONLY the properness leg, while the same-shape GD-2/GD-3 residue was (H1) — asymmetric classification of the proved-≤1/open-≥2 dictionary family | ACCEPTED; PROMOTED into the ENLARGED **(H1) group** as member (b) (most natural home: the graded-dictionary family at orders ≥ 2, per the charge); the §3.3 (KP)/(KPirr) row RESCOPED to the pinned properness-proviso leg only (FGMN Lemma 5.3, keeps [T]); Step 7's OL-2a-1 mapping and Step 8's ownership line re-pointed | §3.1 (H1)(b), §1, Steps 7/8, §3.3 (KP)/(KPirr) row |
| F-3 | BLOCKER | Step 10's consumed "GD-6 instances at orders ≥ 2" had no supplying row: (GR-B)'s row was scoped GD-2/GD-3-only, and GD-6's §3.3 home holds "proof-by-construction routes on record" — routes, not proofs (the exact evidentiary status under which (T-DEC)/(T-VERD) were promoted at VC5-2); the GD-4/GD-6 row's post-promotion D-a consumption display was also ambiguous | ACCEPTED; the consumed GD-6 conformance instances PROMOTED into **(H1)** as member (c) — the option under which (H1)'s row was understating its own content, matching Step 10's "through (GR-B)" routing (now "through (H1)"); the GD-4/GD-6 row keeps only the UNCONSUMED bookkeeping remainder with its consumption scope now displayed (kept content consumed at construction level by no mandatory step; D-a's open content = exactly (H6)) | §3.1 (H1)(c), Step 10, §3.3 GD-4/GD-6 row |
| A-4 | ADVISORY | Step 11 renders CU-2t's conditionality twice, unreconciled: the R3-era "P(O-2a r5) + TB-CAP's D-12r perimeter + the GD residue including the three terminal rows" vs the adjudication's "conditional per §9.5: the three terminal rows + the pinned literature"; "including" invited a ⊇ reading | ACCEPTED; the reconciling display added at Step 11: NEW-vs-INHERITED split (the §9.5 form prices the leaf's NEW conditionality exactly; the R3 sentence's other items are inherited debts at named homes — P(O-2a r5) §3.2, D-12r (H3), the GD residue at its §3.1/(H1)/(H6)/§3.3 homes), the earlier sentence marked superseded-as-display, and "including" declared ⊇-free | Step 11 |
| A-5 | ADVISORY | the (H6) route display omitted the Step-12 → Step-18-K1 "CU leaf labels" leg (terminal labels ride O5triple's counting into K1 — the same route the (FRESH) row displays) | ACCEPTED; the K1/CU-leaf-labels leg added to the (H6) route display at §1, the §3.1 (H6) row, Step 18's K1 line, and Step 19; no clause attribution changes ((R)/(UB) already carry (H6)) | §1, §3.1 (H6), Steps 18/19 |
| A-6 | ADVISORY | four [T] sites displayed neither proof-on-file status nor consumption scope: (i) the S-1 row's degree-g > 1 KEY1 generalization; (ii) widened-charge item (v) (rsh_interp/r_bdd/wsh_ok); (iii) the GD-8 order-≥ 2 row; (iv) (FR≡)'s refutable order-2 content | ACCEPTED; item-(vii)-style adjudication lines added at all four rows: (i) no proof on file, consumed by no order-≤ 1 mandatory step, ≥ 2 face re-scoped to (H1)'s campaign perimeter (LC-COH-style record); (ii) no proof-on-file display exists — the three pack-row laws are PRICED INSIDE (H5)=(PACK)'s correspondence perimeter, flagged to VC7; (iii) consumed by NO mandatory step (quarantined corollary non-mandatory; O-9's verified reduction names exactly TWO ≥ 2 opens; Step 10 consumes the (H1) group, not GD-8) — recorded open, unconsumed; (iv) adjudicated a declared CONVENTION pin of the Step-6 sloped/sheared genre — the leaf is verified OVER the pin, the refutable content makes it falsifiable and the gates ran coherent, [T] = the same-convention transcription audit | §3.3 (four rows) |
| A-7 | ADVISORY | GD-7's five consumed flags are pinned only at section granularity, expressly NOT covered by the executed LITUNIT — [T] only under a generous reading of "pinned", against the two-false-axioms history | ACCEPTED at the named-precondition option: the row now states the discharge (per-flag verbatim extraction lines, LITUNIT-style MATCH verdicts, one session with the LIT-1 pagination duty) is a NAMED precondition of the §6 acceptance bar, displays the partial per-flag loci already on record ((GMN-COH) ← the eq.-(37) sprouted-type identity; (R-1) ← the "of type t" shape; (R-2) ← the order-r exact-key convention), and bars consumption from the range pin alone; the session itself is dispatch (§6) | §3.3 GD-7 row, §6 |
| A-8 | ADVISORY | stale "seven leaves VERIFIED" inside the (ROOT-C) proof-status display (a REVISION-5 sync miss; headline/§3.2/§11 all said NINE) | ACCEPTED; corrected to NINE with the sync-miss noted in place | §1 |
| A-9 | ADVISORY (standing, third consecutive recording) | the T-U §U.SQ.6 row-by-row correspondence remained asserted, not displayed — a fresh verifier structurally cannot certify "no §U.SQ.6 item falls outside the steps" from this document | ACCEPTED; **EXECUTED** — the NEW §3.4 displays the per-item pointer table (§U.SQ.6 items 1–21 → step/eviction/annex) PLUS the Lean-bridge kernel-row cross-walk (census §2.3 / `BridgeKernels.lean`), with warrants cited per row and the three NEW-at-the-table adjudications flagged (items 4/13c consumer-audit evictions; the items-15–21 empirical-roster disposition); Step 19's parenthetical re-pointed; the §3.3 audit row records the execution and retires on VC7's confirmation | §3.4 (NEW), Step 19, §3.3 T-U row |

**The completeness invariant, re-run after these repairs** (the standing
failure-prone check — F-1/F-2/F-3 are its missing-row face at the [T]/[M] boundary,
the VC5 class): the (ROOT-C) hypothesis list is now **(H1) the graded-dictionary
group ((GR-B) + GD-1-beyond-1 + the consumed GD-6 conformance instances — ENLARGED
here), (H2) (FRESH), (H3) D-12r + (E-N) + (ADM), (H4a) (R1)–(R4), (H4b) (UB-X)
(incl. its named (K3-δ) fragment), (H5) (PACK), (H6) the terminal-seam
engine-conformance group ((T-DEC)/(T-READ)/(T-VERD) + item (vii)), (H7) the
(I-τ)/K-LOC sibling-interface pair (NEW here)** — exactly the TEN §3.1 [M]
rows/groups (nine at REVISION 5, with the (H1) row enlarged in place + the (H7) row
added), none clause-misattributed. Per-clause attribution: **(0) unconditional**
(Step 1); **(SQ) unconditional** (Steps 1 + 13-semantic); **(R) ← (H1)–(H3) +
(H4a) + (H5) + (K3-δ) + (H6) + (H7)**; **(UB) ← (H1)–(H3) + (H4a) + (H4b) + (H5) +
(H6) + (H7)**; only (H4b)-minus-(K3-δ) is (UB)-exclusive. Every consumed
PROVED-UNVERIFIED input has a named §3.2 pass; every consumed OPEN proposition is a
§3.1 row — the O-11 r2 declared triple D-a/D-b/D-c is now FULLY adjudicated (D-a →
(H6) at VC5-2, D-b → (H7) here, D-c → (H4a)/(K3-δ) at V2-2/VC4-1), and the O-2a OL
residue's six families each have an explicit consumed-face home ((H1) for
GD-1/GD-2/GD-3/GD-6's open faces; §3.3 scoped rows for GD-4's kept bookkeeping,
GD-5's audit, GD-7's flags, GD-8's unconsumed seam); every named pin/flag has
exactly one ledger home (the (I-τ)/K-LOC tombstone preserves the trail). The T-U
closing edge is no longer asserted-only: §3.4 displays it (three NEW dispositions
flagged to VC7 there).

**What VC7 (the next end-to-end pass, toward the ≥ 2-consecutive-clean bar) should
check first:** (1) the §3.4 table — row-by-row against the frozen §SQ.6 list
(MOVES 13542–13609) and census §2.3: is every mapping faithful to the cited
warrant, and are the three NEW adjudications sound (items 4/13c: do X.1a/(X1a-ALIGN)
really have no consumer outside the struck rate family?; items 15–21: is the
flags-never-substitutes disposition airtight at Step 19)?; (2) the (H1)
enlargement — do Steps 10–12 consume GD-1/GD-6's ≥ 2 faces at exactly the displayed
member scopes, and does any OTHER OL-2a face remain consumed-but-unpriced?; (3) the
(H7) row — is the own-row adjudication argument sound, are Step 15/18-K1's
consumption displays exact, and does (UB)'s inheritance hold for the pair?; (4) the
A-6 adjudications — in particular item (v)'s priced-inside-(H5) argument against
the O-11 r3 restated D-11 entry; (5) the completeness invariant (TEN rows/groups ↔
(H1)–(H7)), per standing experience.

**Concurrency and bookkeeping.** No status syncs ride this revision (VC6 was a
composition-only pass; the D-11/O-11 pass 3s remain the in-flight debts and their
rows carry unchanged). The §3.4 execution consumed two sources OUTSIDE the prior
four-corners set (`lean/notes/MOVES_2026-07-24.md` §SQ.6;
`lean/notes/REMAINING_CENSUS_2026-08-01.md` §2.3 + `MovesU/BridgeKernels.lean`) —
they enter as DISPLAY warrants only, never as suppliers (nothing in §2 consumes
them); the commit closing this revision is its unit of record (unit ROOT R7).

— REVISION 6 complete. The body above is the operative composition; VC7 runs on
this document as a whole, and the ≥ 2 consecutive-clean counter remains at ZERO.

---

## 13. REVISION 7 (r8, VC7 fold, 2026-08-03) — disposition of VC7, the Phase C end-to-end pass 7

**The pass.** VC7 = the seventh end-to-end verification (Codex, fresh context, run
on the REVISION-6 document with the §12 priority charges;
`lean/notes/openmath/VC7_root_report.md`). Verdict: **COMPOSITION-GAPS** — 2
blockers + 3 advisories. Both blockers are attribution/warrant findings at the two
REVISION-6 A-6 adjudications VC6 itself had flagged forward: (i) a consumed
ALL-ORDER proposition whose [M] pricing was a perimeter GESTURE — no (H1) member
displayed the degree-g > 1 KEY1 statement Step 6's all-site theorems consume at
order-≥ 2 reads; and (ii) a law-transfer INFERENCE the displayed (PACK)
correspondence could not warrant — entry-for-entry equality with 𝔅_n transfers
rsh_interp/r_bdd/wsh_ok to the concrete pack only if a displayed theorem supplies
them at the 𝔅_n rows, and none does (Steps 2/5 state none of the three by name nor
an implication to them). The composition itself was CONFIRMED — VC7's findings
6–11: the §3.4 T-U tables compositionally genuine (all 15 Table B rows homed; the
refuted Lean rows not consumed as suppliers; the six H.6 kernels not silently
assumed; the items 4/13c evictions sound), the (H1) enlargement otherwise sound and
the (H7) row sound incl. (UB)'s inheritance, the TEN-row syntactic bijection
correct with per-clause attribution internally consistent, the remaining linear
walk proving (ROOT-C) quantifier-for-quantifier at its displayed conditional scope,
and acyclicity passing (17 ≺ 18 ≺ 18b genuinely stratified; the §3.4 cross-walk
adds no proof dependency). Every finding is dispositioned below and repaired in
place; NO mathematical content changed and NO status moved (a composition-only
pass; VERIFIED = 9 stands at the 6a7593b harvest).

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| VC7-1 | BLOCKER | the A-6(i) KEY1 adjudication does not price the open higher-order content: Step 6 states KEY1 outright only at order ≤ 1 while its theorems are ALL-SITE; establishing non-consumption by order-≤ 1 steps does not address the higher OM orders, and "lives in the (H1) campaign perimeter" names NO member — none of (H1)(a)–(c) assumes the degree-g > 1 statement, so it remained a consumed open proposition without an [M] home | ACCEPTED; the degree-g > 1 KEY1 generalization ADDED as the explicitly displayed FOURTH member **(H1)(d)** (§3.1 — stated precisely, matching Step 6's use: the (d1)–(d3) key-compliance block at degree-g > 1 keys under the standard lift, = O-1thr's (V1)/(V2) block beyond its proved order-≤ 1 slice, the block Lemma WGEO consumes); Step 6 gains the order-≥ 2 conditionality display and the (H1)(d) Consumes entry; the §3.3 S-1 adjudication SUPERSEDED in place (mathematical face → (H1)(d); the row keeps only the transcription/composition audit); §1's (H1) display, Step 7's OL-2a-5 mapping, and the headline synced | §3.1 (H1)(d), §1, Steps 6/7, §3.3 S-1 row |
| VC7-2 | BLOCKER | the A-6(ii) pricing of D-11 item (v) inside (H5) lacks the required warrant: Step 18-K2 consumes rsh_interp/r_bdd/wsh_ok, but (PACK) as stated gives only entry-for-entry correspondence, which transfers the laws only if an earlier displayed theorem establishes them at the corresponding 𝔅_n rows — no such theorem is displayed, so (PACK) had not been shown to supply all three predicates (exactly the flagged contingency) | ACCEPTED — the flag FIRED, resolved by HONEST ENLARGEMENT (not by claiming the inference): **(H5) = (PACK)'s displayed statement now carries the three laws at the 𝔅_n rows as explicit clauses (PACK-i) rsh_interp / (PACK-ii) r_bdd / (PACK-iii) wsh_ok** (§3.1 — each stated: interpolation Rsh → Rval at every prime power; the [0,1] bound; the evalAt regular-subring membership); Step 18's K2 line consumes the enlarged (H5) with the row-law legs displayed; the §3.3 item-(v) adjudication re-pointed (the REVISION-6 correspondence-only inference WITHDRAWN as invalid); §1's (H5) display and Steps 16/19's inheritance lines synced | §3.1 (PACK), §1, Steps 16/18/19, §3.3 widened-charge row |
| VC7-3 | ADVISORY | Table A's combined 6(iii)–(ix)+7 row understates inherited conditionality: its purported exhaustive residue list named only (H1), (FRESH), (ADM), (PACK), (H6), though its own routes through Steps 6/11/12/17 inherit D-12r and (E-N), hence the full (H3); the reading rule prevented a hidden hypothesis, but the list was inaccurate | ACCEPTED; the residue list corrected to name the FULL (H3) ((ADM) + D-12r + (E-N)) with the correction's warrant displayed in the row | §3.4 Table A |
| VC7-4 | ADVISORY | (H1)'s properness attribution not one-home clean: (H1)(a) read "with the properness proviso" while (H1)(b) and the §3.3 (KP)/(KPirr) row assign the properness-proviso leg to [T] pinned at FGMN Lemma 5.3 — the same e·g = 1 condition carried two classifications | ACCEPTED; (H1)(a)'s wording now explicitly EXCLUDES the properness-proviso leg, whose SOLE home is the pinned §3.3 [T] row — the clause has one classification | §3.1 (H1)(a) |
| VC7-5 | ADVISORY | the items-15–21 empirical-roster adjudication is sound only as a proof-consumption ruling, not as retirement of the acceptance gates — the §3.4 wording risked a "no remaining campaign obligation" reading | ACCEPTED; the row's wording sharpened in place: NOT a (ROOT-C) hypothesis, AND not a gate retirement — the §5 rerun acceptance-gate obligation (root criterion 3) stands in full | §3.4 Table A |
| VC7-6..11 | CONFIRMATIONS (findings 6–11) | the §3.4 warrants compositionally genuine (items 4/13c evictions sound; H.6 kernels not silently assumed; refuted Lean rows not consumed; all 15 Table B rows homed); the (H1) enlargement otherwise sound (GD-1 via O-2a at Steps 10–12, GD-6 at Step 10); the (H7) row sound (distinct seam/owners; (UB) inheritance with the (UB-X)(a) re-base audit); the TEN-row bijection + per-clause attribution correct ((0)/(SQ) unconditional; (R)/(UB) as displayed); the walk proves (ROOT-C) quantifier-for-quantifier; acyclicity passes | RECORDED; the §3.3 T-U audit row RETIRED on the table confirmation (tombstone kept; Step 19's parenthetical synced); per the VC8 charge below, these confirmations need no re-litigation but must not regress | §3.3 T-U row, §3.4, Step 19 |

**The completeness invariant, re-run after these repairs** (the standing
failure-prone check; this revision's blockers are its member-level face — a
consumed open proposition must be a DISPLAYED [M] member, not a perimeter gesture
or an unwarranted corollary): the (ROOT-C) hypothesis list is **(H1) the
graded-dictionary group — now FOUR displayed members: (a) (GR-B) (properness-proviso
leg EXCLUDED, single-homed at the §3.3 (KP)/(KPirr) [T] pin — VC7-4), (b)
GD-1-beyond-1, (c) the consumed GD-6 conformance instances, (d) the degree-g > 1
KEY1 generalization (NEW here); (H2) (FRESH); (H3) D-12r + (E-N) + (ADM); (H4a)
(R1)–(R4); (H4b) (UB-X) (incl. its named (K3-δ) fragment); (H5) (PACK) — now the
correspondence PLUS the three explicit row-law clauses (PACK-i/ii/iii) (ENLARGED
here); (H6) the terminal-seam engine-conformance group; (H7) the (I-τ)/K-LOC
sibling-interface pair** — exactly the TEN §3.1 [M] rows/groups, the row COUNT
UNCHANGED from REVISION 6: (H1) and (H5) grew members/clauses IN PLACE, no new row
and no new hypothesis letter. Per-clause attribution unchanged: **(0)
unconditional** (Step 1); **(SQ) unconditional** (Steps 1 + 13-semantic); **(R) ←
(H1)–(H3) + (H4a) + (H5) + (K3-δ) + (H6) + (H7)**; **(UB) ← (H1)–(H3) + (H4a) +
(H4b) + (H5) + (H6) + (H7)**; only (H4b)-minus-(K3-δ) is (UB)-exclusive. The new
members' consumption sites are displayed: (H1)(d) at Step 6 (the all-site theorems
at order-≥ 2 reads, propagating with D-14/D-15's deliveries to Steps 10–12/17);
PACK-i/ii/iii at Step 18's K2/evaluation glue (the restated D-11 entry item (v)) —
both inside clauses (R)/(UB) only. Every consumed PROVED-UNVERIFIED input has a
named §3.2 pass; every consumed OPEN proposition is a displayed §3.1 member (the
two VC7 blockers were the last known consumed-open-without-a-member sites); every
named pin/flag has exactly one ledger home (the properness leg now single-homed —
VC7-4; the retired T-U audit row and the (I-τ)/K-LOC row keep tombstones preserving
the trails).

**The VC8 charge (the next end-to-end pass, toward the ≥ 2-consecutive-clean
bar).** Verify the two enlargements and the advisory fixes: (1) **(H1)(d)** — is
the displayed statement exactly what Step 6's all-site theorems consume at
order-≥ 2 sites (the WGEO/(V1)/(V2) key-compliance block at degree-g > 1 keys under
the standard lift, no more and no less), and is its consumption display complete
(Step 6's conditionality line + the propagation through D-14/D-15's consumers)?;
(2) **the (H5) enlargement** — are PACK-i/ii/iii stated correctly against O-11 r3's
§3.5 glue and §5 row 20 (interpolation, [0,1] bound, evalAt membership, at the 𝔅_n
rows), does Step 18-K2's consumption line now carry them honestly, and does NO text
still lean on the withdrawn correspondence-only inference?; (3) the three advisory
fixes (the 6(iii)–(ix)+7 residue list naming the full (H3); the (H1)(a) one-home
exclusion; the items-15–21 gate-preserving wording); (4) the completeness invariant
(TEN rows/groups ↔ (H1)–(H7); (H1) at FOUR members; (H5) at correspondence + three
clauses), per standing experience. **The confirmations of findings 6–11 need NOT be
re-litigated — but they must not REGRESS:** any edit-induced break in the confirmed
items (the §3.4 tables, the (H7) row, the TEN-row bijection, per-clause
attribution, acyclicity, the quantifier-for-quantifier walk) is a VC8 finding.

**Concurrency and bookkeeping.** No status syncs ride this revision (VC7 was a
composition-only pass; the D-11/O-11 pass 3s remain the in-flight debts and their
rows carry unchanged). No new sources enter the four-corners set — both
enlargements display content already named in consulted records (O-1thr's KEY1 and
the §3.3 S-1 row for (H1)(d); O-11 r3's §5 row 20 / restated D-11 item (v) for
PACK-i/ii/iii); the commit closing this revision is its unit of record (unit ROOT
r8).

— REVISION 7 complete. The body above is the operative composition; VC8 runs on
this document as a whole, and the ≥ 2 consecutive-clean counter remains at ZERO.

---

## 14. REVISION 8 (r9, VC8 fold, 2026-08-03) — disposition of VC8, the Phase C end-to-end pass 8

**The pass.** VC8 = the eighth end-to-end verification (Codex, fresh context, run
on the REVISION-7 document with the §13 charges;
`lean/notes/openmath/VC8_root_report.md`). Verdict: **COMPOSITION-GAPS** — 1
blocker + 1 advisory, the NARROWEST residue of the eight passes, both findings at
the REVISION-7 repairs themselves: (i) the new (H1)(d) member quantified over
realized sites "whose current key has degree > 1", parenthetically identified
with the order-≥ 2 reads — an identification NO earlier statement proves (the
document itself recognizes e·g = 1 refinement reads, where key degree need not
increase; nor does the proved order-≤ 1 slice cover a higher-order site merely
because its key stays degree 1), with a further ambiguity between "degree-g > 1
key" and the residual factor ψ of degree g — so the claimed all-site coverage,
and with it the completeness invariant's semantic face, was unsupported; (ii) the
PACK K2/K3 sub-attribution was not exact — several summaries assigned all three
row-laws to "K2/evaluation glue" while the §3.1 row itself routes wsh_ok to Step
18's K3 evaluation (no clause-level loss: (H5) covers both). Everything else was
CONFIRMED: all three r8 advisory repairs pass (Table A's 6(iii)–(ix)+7 row names
the full (H3); the properness-proviso leg one-home; items 15–21 non-hypothesis
with the §5 acceptance-gate rerun preserved); the TEN-row count syntactically
correct (H1, FRESH, R1–R4, ADM, UB-X, PACK, H6, H7, D-12r, E-N ↔ (H1)–(H7)); the
walk supports the displayed quantifiers and clause scopes ((0)/(SQ)
unconditional; (R)/(UB) carrying their listed hypotheses; Σ_τ R_τ = 1 properly
delayed to Step 19); acyclicity passes incl. 17 ≺ 18 ≺ 18b; NO regression in the
§3.4 T-U tables, (H7), the syntactic bijection, or the pack-law non-inference.
Both findings are dispositioned below and repaired in place; NO mathematical
content changed and NO status moved (a composition-only pass; VERIFIED = 9
stands at the 6a7593b harvest).

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| VC8-1 | BLOCKER | (H1)(d) does not demonstrably cover every Step-6 order-≥ 2 site: it quantified over realized sites "whose current key has degree > 1", parenthetically identified with the order-≥ 2 reads — an UNPROVEN identification (e·g = 1 refinement reads keep degree-1 keys at higher order; the proved order-≤ 1 slice does not cover a higher-order site merely because its key stays degree 1), plus a key-degree vs residual-factor-degree-g conflation — so the claimed all-site coverage and the per-clause completeness invariant remained unsupported | ACCEPTED; the member REQUANTIFIED IN PLACE to READ ORDER: (H1)(d) is now **the order-≥ 2 KEY1 block** — KEY1's (d1)/(d2)/(d3) conclusion block asserted at EVERY realized site of Step 6's machine at OM ORDER ≥ 2, membership by read order alone, IRRESPECTIVE of the current key's degree, explicitly INCLUDING order-≥ 2 sites whose key remains degree 1 (e.g. e·g = 1 refinement chains), for the standard key lift Φ_{i+1} with read side (e, h) and residual factor ψ of degree g ≥ 1 (g is ψ's degree, unconstrained — the key's degree is irrelevant to membership); the COVERAGE ARITHMETIC displayed at the row: the proved slice is exactly order ≤ 1 (GD23 dictionary + O-1thr (V1)/(V2) at order ≤ 1), so (d) covers precisely the COMPLEMENT of the proved slice in Step 6's site range and total coverage consumes NO key-degree/order equivalence; the member renamed at every current-status site ((H1)(d) title, §1's display, Step 6's conditionality + Consumes lines, Step 7's OL-2a-5 mapping, the §3.3 S-1 adjudication, headline/§3.2/§6/footer) — historical revision records (§§7–13) untouched | §3.1 (H1)(d), §1, Steps 6/7, §3.3 S-1 row |
| VC8-2 | ADVISORY | the PACK internal K2/K3 routing not consistently described: several summaries assigned all three row-laws to "K2/evaluation glue" while the §3.1 PACK row itself says PACK-iii is an input to Step 18's K3 evaluation — no clause-level loss ((H5) covers both), but the sub-attribution was not exact | ACCEPTED; made UNIFORM everywhere: **PACK-i (rsh_interp) + PACK-ii (r_bdd) enter at Step 18's K2 evaluation glue** (the Rsh → Rval conversion; the [0,1] bound behind ENNReal.ofReal); **PACK-iii (wsh_ok) is the membership input to Step 18's K3 evaluation ring-hom evalAt (with D-6)** — synced at §1's (H5) display, the §3.1 (PACK) row, Step 18's K2 and K3 lines, Step 19's evaluation-chain attribution, and the §3.3 widened-charge item-(v) adjudication; clause-level home unchanged ((H5), consumed by clauses (R)/(UB)) | §3.1 (PACK), §1, Steps 18/19, §3.3 widened-charge row |
| VC8-C | CONFIRMATIONS | the three r8 advisory repairs pass; the TEN-row count syntactically correct; the walk supports the displayed quantifiers and clause scopes (Σ_τ R_τ = 1 delayed to Step 19); acyclicity incl. 17 ≺ 18 ≺ 18b; no regression on the §3.4 T-U tables, (H7), the syntactic bijection, or the pack-law non-inference | RECORDED; per the VC9 charge below, these need no re-litigation but must not regress | — |

**The completeness invariant, re-run after these repairs** (the standing
failure-prone check; this revision's blocker is its QUANTIFIER face — a displayed
[M] member must cover its consumption site by its OWN quantifier, not through an
unproven identification): the (ROOT-C) hypothesis list is **(H1) the
graded-dictionary group — FOUR displayed members, unchanged in count: (a) (GR-B)
(properness-proviso leg excluded, single-homed at the §3.3 (KP)/(KPirr) [T]
pin), (b) GD-1-beyond-1, (c) the consumed GD-6 conformance instances, (d) the
order-≥ 2 KEY1 block (REQUANTIFIED IN PLACE here — membership by read order,
covering exactly the complement of the proved order-≤ 1 slice in Step 6's site
range, no key-degree/order equivalence consumed); (H2) (FRESH); (H3) D-12r +
(E-N) + (ADM); (H4a) (R1)–(R4); (H4b) (UB-X) (incl. its named (K3-δ) fragment);
(H5) (PACK) — the correspondence plus the three explicit row-law clauses
PACK-i/ii/iii; (H6) the terminal-seam engine-conformance group; (H7) the
(I-τ)/K-LOC sibling-interface pair** — exactly the TEN §3.1 [M] rows/groups,
UNCHANGED from REVISION 7: no new row, no new member, no new hypothesis letter —
(H1)(d) was requantified in place. Per-clause attribution unchanged: **(0)
unconditional** (Step 1); **(SQ) unconditional** (Steps 1 + 13-semantic); **(R)
← (H1)–(H3) + (H4a) + (H5) + (K3-δ) + (H6) + (H7)**; **(UB) ← (H1)–(H3) + (H4a)
+ (H4b) + (H5) + (H6) + (H7)**; only (H4b)-minus-(K3-δ) is (UB)-exclusive. The
corrected members' consumption sites are displayed: (H1)(d) at Step 6's all-site
theorems at EVERY order-≥ 2 site (read-order membership — proved slice order
≤ 1 + assumed complement order ≥ 2 = the full site range, with no unproven
identification in between), propagating with D-14/D-15's deliveries to Steps
10–12/17; PACK-i/ii at Step 18's K2 evaluation glue and PACK-iii at Step 18's
K3 evaluation ring-hom (with D-6) — all inside clauses (R)/(UB) only. Every
consumed PROVED-UNVERIFIED input has a named §3.2 pass; every consumed OPEN
proposition is a displayed §3.1 member whose quantifier covers its consumption
site; every named pin/flag has exactly one ledger home (tombstones preserved).

**The VC9 charge (the next end-to-end pass, toward the ≥ 2-consecutive-clean
bar).** Verify ONLY: (1) **the (H1)(d) requantification** — does the displayed
member now cover Step 6's order-≥ 2 use WITHOUT any unproven identification
(membership by read order alone, key degree irrelevant, degree-1-key e·g = 1
refinement chains included), and is the proved-slice complement arithmetic sound
(the proved slice exactly order ≤ 1; (d) exactly the complement in Step 6's site
range; total coverage consuming no key-degree/order equivalence)?; (2) **the
PACK sub-attribution sync** — PACK-i/ii at Step 18's K2 evaluation glue and
PACK-iii at Step 18's K3 evaluation ring-hom (with D-6), uniform across §1, the
§3.1 (PACK) row, Step 18's K2/K3 lines, Step 19, and the §3.3 widened-charge
row, with NO surviving OPERATIVE summary lumping all three into "K2/evaluation
glue" (the historical revision records §§7–13 quote the former displays as
provenance and are not consumption displays — this section's invariant re-run
is the operative one);
(3) **no regression** on the VC7/VC8-confirmed items (the three r8 advisory
repairs, the §3.4 tables, the (H7) row, the TEN-row bijection, per-clause
attribution, acyclicity, the quantifier-for-quantifier walk, the Step-19
Σ_τ R_τ = 1 derivation, the pack-law non-inference). Any edit-induced break in a
confirmed item is a VC9 finding.

**Concurrency and bookkeeping.** No status syncs ride this revision (VC8 was a
composition-only pass; the D-11/O-11 pass 3s remain the in-flight debts and
their rows carry unchanged). No new sources enter the four-corners set — the
requantified member displays content already named in consulted records
(O-1thr's KEY1/(V1)/(V2) block and Step 6's site range), and the sub-attribution
restates the §3.1 (PACK) row's own K2/K3 routing; the commit closing this
revision is its unit of record (unit ROOT r9).

— REVISION 8 complete. The body above is the operative composition; VC9 runs on
this document as a whole, and the ≥ 2 consecutive-clean counter remains at ZERO.

---

## 15. REVISION 9 (r10, post-acceptance hygiene, 2026-08-03) — the ACCEPTANCE record + disposition of VC10's three advisories

**The passes and the acceptance.** VC9 = the ninth end-to-end verification
(Codex, fresh context; `lean/notes/openmath/VC9_root_report.md`) and VC10 = the
tenth (Fable, fresh context — the model-diverse pass;
`lean/notes/openmath/VC10_fable_report.md`) both RAN on the IDENTICAL REVISION-8
text and both returned **COMPOSITION-SOUND with ZERO blockers**. The tree §4
Root criterion — ≥ 2 consecutive CLEAN end-to-end passes, ≥ 2 models, verifiers
who saw none of the per-leaf rounds — is MET. **THE ACCEPTANCE STANDS: the
composition is ACCEPTED at (ROOT-C)'s displayed conditional scope** (milestone
addendum, `docs/PROJECT_STATE.md` 2026-08-03; nothing in this revision weakens,
re-opens, or re-litigates it). Acceptance certifies the composition — the
linear walk, the ten-row bijection, the per-clause attribution, acyclicity —
NOT the discharge of §3's ledger: the theorem as proved remains the conditional
(ROOT-C), and the acceptance path's remaining criteria ((1) empty §3.1/§3.2,
(3) the §5 matrix re-run, (4) the completeness audit) stand as displayed at §6.

**This revision is DISPLAY HYGIENE ONLY, and the counter is unmoved.** VC10's
three findings were all ADVISORY (its own closing ruling: "display/ledger
imprecision strictly narrower than VC8's residue; none affects the joint
conditional theorem, the ten-row bijection, the per-clause attribution, or the
acyclic walk"). Advisories do NOT reset the consecutive-clean counter: **the
counter is TWO and STAYS TWO.** Verified of this revision's own edits: NO §3.1
[M] member changed (the D-12r/(E-N) edits touch those rows' notes/consumer-list
columns only — statement columns byte-identical; no row added, removed, or
re-membered; the TEN-row roster and the (H1)–(H7) grouping are unchanged); NO
step content changed (the one step touched is Step 9, whose Statement/Consumes/
Delivers lines are byte-identical — only its Status grade display is rewritten);
NO clause attribution changed (§1's (ROOT-C) display, per-clause lists included,
is byte-untouched — the D-12r/(E-N) clause-(UB) entries were ALREADY displayed
there since the REVISION-3 B1 fix; the rows' lists are brought up to §1, not §1
to the rows). The touched surfaces, exhaustively: Step 9's Status line; the NEW
§3.2 row P(O-14a′ r3); the §3.1 D-12r and (E-N) rows' notes columns; the §3.4
Table A "6(iii)–(ix) + 7" residue list; and the sync surfaces (headline, the
§3.2 end-to-end row, §6 dispatch, footer). Historical sections §§7–14 untouched.

**Disposition table.**

| # | class | finding (compressed) | disposition | where |
|---|---|---|---|---|
| VC10-A1 | ADVISORY | Step 9 certified O-14a′ as "TWICE-STABLE across V11/V15" — not one of the four declared §0 grades, absent from the nine-leaf VERIFIED roster, no named §3.2 pass row — so the §§13/14 invariant ("every consumed PROVED-UNVERIFIED input has a named §3.2 pass") was checkable there only by trusting the cited CUC §0 registry line; asymmetric with O-2a (core thrice confirmed, still carrying P(O-2a r5)) | ACCEPTED; NEW §3.2 row **P(O-14a′ r3)** records exactly what is ON RECORD (core Theorems 1–3 stable across the V11/V15 passes, rev 3 on file; the consumed instance RRE(p, ZMod p) FORMALIZED Lean-core; the CUC §0 registry line as provenance) and what is OWED (the V11/V15 arc's models/cleanliness display, or one fresh named pass); Step 9's Status line REWRITTEN in the declared vocabulary — **PROVED-UNVERIFIED with the named pass row P(O-14a′ r3)** — NOT promoted to VERIFIED (the V11/V15 models/cleanliness are not displayed in-document, so the §0 VERIFIED bar is not checkable from the four corners). The asymmetry precedent is thereby RESOLVED: every consumed PROVED-UNVERIFIED leaf now carries a named §3.2 pass row, O-14a′ included | Step 9, §3.2 (new row) |
| VC10-A2 | ADVISORY | §1 displays D-12r and (E-N) entering clause (UB) through the (UB-X)(a)-transported dictionary/CU/drainage layers (the B1 attribution), but the D-12r row's Enters-list had no Step-16/(UB) entry and the (E-N) row's "NOT consumed by the mandatory path elsewhere" read as denying it — a residual consumer-list gap of exactly the VC3-A6/B1 genre (the B1 fix reached the (ADM)/(FRESH) rows but not these two) | ACCEPTED; the Step 16/clause-(UB) transport entry ADDED to both rows' consumer lists (mirroring the B1 fix at (ADM)/(FRESH)), and the (E-N) row's "elsewhere" sentence REWORDED to scope explicitly over sites OTHER than the CU-4 touch and its (UB) transport, so it can no longer be read as contradicting §1. NO attribution change: §1's per-clause display already carried both entries — the rows' lists are completed up to it | §3.1 D-12r/(E-N) rows |
| VC10-A3 | ADVISORY | §3.4 Table A row "6(iii)–(ix) + 7": the VC7-3-corrected residue list ((H1) group, (FRESH), the FULL (H3), (PACK), (H6)) was still not exhaustive by the table's own reading rule — the row's own route through Step 18's K3 inherits (H4a) + (K3-δ) at K3-c's base-changed β-legs, which row 9 names explicitly but this list did not | ACCEPTED; the list RUN TO CLOSURE: **(H4a) + (K3-δ)** ADDED with the K3-c base-changed β-legs warrant (the δ > 1 pools — exactly row 9's display), and the row now STATES the list is closed under the table's own reading rule (step pointers inherit each step's declared conditionality; every step route the row names traced). As at VC7-3, the reading rule already prevented a hidden hypothesis — the correction is to the list's exhaustiveness claim only | §3.4 Table A |

**Bookkeeping.** No status syncs ride this revision beyond the O-14a′
grade-display conformance (the same record, no adjudication changed — now
displayed in the declared vocabulary); the D-11/O-11 pass 3s remain the
in-flight [V] debts and their rows carry unchanged; VERIFIED = 9 stands at the
6a7593b harvest. No new sources enter the four-corners set: every fact the new
displays state is quoted from records this document already cites (the O-14a′
rev-3/V11/V15 record and the CUC §0 registry line at Step 9; §1's B1
attribution; row 9's (H4a)+(K3-δ) warrant). The commit closing this revision is
its unit of record (unit ROOT r10).

— REVISION 9 complete. The ACCEPTANCE (VC9 + VC10 on REVISION 8) STANDS; this
revision changes displays only; the ≥ 2 consecutive-clean counter is TWO and
stays TWO. [Historical close — superseded by the §16 suspension record.]

## 16. REVISION 10 (r11 executed, 2026-08-04, unit ROOT r11) — the suspension record: stop-the-line CURED, acceptance NOT re-earned, counter ZERO

**The suspension (what happened).** The Phase-C acceptance (VC9 + VC10 on the
REVISION-8 text, §15) was **SUSPENDED at the 2026-08-03 stop-the-line** (ledger
`BRIDGE_ADJUDICATIONS_2026-07-30.md` entry at 44ce0a7; `docs/PROJECT_STATE.md`
correction addendum): the owed D-11 pass 3 (`D11_owedpass_2026-08-03.md`, Codex,
fresh context) returned **CRITICAL** (6 criticals/3 gaps), and the hostile impact
audit showed two MANDATORY steps of REVISION 9 consuming the refuted layers AS
PROVED — Step 17 ← Theorem M1 (refuted at its announced conditioning scope, at
the family-(v) per-entry mass claim, at its billing, and at M1(b)'s multi-step
disjointness) and Step 18b ← Theorem M2 (FALSE at its displayed universal scope;
the missing premise billed in NO hypothesis row). The VC9/VC10 passes remain
valid AS composition-level audits — what failed was the leaf under its owed
pass, exactly what the owed-pass discipline exists to catch.

**The cure (what this revision consumes and why nothing refuted remains).**
Governing design: `lean/notes/openmath/R11_REPAIR_DESIGN_V5_2026-08-04.md`
(five Codex plan-review rounds; execution-ready per its §9). Unit A produced
the repaired leaf `D11_rev4_2026-08-04.md` — true-scope repairs at every pass-3
finding site plus the written-out obligations ((b-PF)/(b-EX)/(LOOP-∞),
(O-1)–(O-5), (O-GLOB), (ROW-Σ)) and audits ((CH-COV) §4A, the (ROW-Σ) edge walk
§2A.4, the (NSF-INT) leaf proof §4B) — which then passed its own hostile bar:
**r4 pass 1 CRITICAL** (`D11r4_pass1_2026-08-04.md`; folded at the leaf's
REVISION 2), **r4 pass 2 GAPS-ONLY** (`D11r4_pass2_2026-08-04.md`: no critical
remains, all six pass-1 repairs VALID, the design-§7 obligations adjudicated AS
PROOFS; its three bookkeeping gaps folded at the leaf's REVISION 3). THIS
revision (Unit B) re-points ROOT's consumption at that final state:

| edit | disposition |
|---|---|
| E1 (Step 17 Statement) | REWRITTEN to the r4 true-scope form: per-clause reference measures; families (iv)/(vi)/(vii) excluded at (a″); the "Σ = 1 across each row" fragment RESCOPED to cite **(ROW-Σ)** (the leaf's §2A.2 PROVED theorem: solved totals = 1; per-booking verdict-row totals = Φ^{(O1)}/Φ^{(O2′)}/Φ^{(O3)}); (b) via (b-PF) + (b-EX) at its route-(a) scope; (LOOP-∞) exact; M3 at separable-lift scope with the (H9) routing price |
| E2 (Step 17 Status) | the pass-3 CRITICAL record + the r4 two-pass record displayed; the leaf's confirming pass rides §3.2 |
| E3 (Step 18b Proof) | re-pointed to Theorem M2 (r4) AT its (RE) scope; arbitrary-α members excluded in-statement; the definitional discharge WITHDRAWN; (RE) at the consumed member = **(PACK-iv)** |
| E4 ((H5) enlargement) | **(PACK-iv) sem_alloc** added as the FOURTH row-law (an addition, never a clarification); consumption set = the leaf's CLOSED two-element edge set {Step 18b direct; Step 17's E1 (ROW-Σ) delivery display} — the design's "untraced cover" clause replaced by the executed §2A.4 walk (residue (EDGE-RES)) |
| E5 (new rows) | **(H8) = (K-CUT)** (the Lemma-3.3 cut kernel, owed-pass G-2; retirement = O-11 §8 charge (vi)) and **(H9) = (NSF)** ((NSF-INT) with its displayed marks-vocabulary + **(K1-CANON)** premises housed in-member; (NSF-KEY) KNOWN FALSE AT HEAD, retiring ONLY by the sign-off-gated Unit C per (NSF-KEY-RET)); §1's (ROOT-C) display, per-clause attribution, Step 18's K1/K4 lines synced |
| E6 (§3.4) | rows 6(iii)–(ix)+7 and 13: "(M1: entries ARE masses)" pointers re-scoped to the r4 per-clause form |
| E7 (§3.2 D-11 row) | pass-3-owed disposition RETIRED → the two-pass r4 record; O-11's row stays owed, unchanged |
| E8 (standing) | header/§1/§3.2/§6/footer read SUSPENDED-then-CURED with the counter at ZERO; no display reads "accepted" as current status |
| E9 ((H9) displays) | Step 18's K1/K4 lines + Step 17's M3 leg carry the (H9) target AT ITS INTERFACE-PIN SCOPE with both members + two-channel attribution + (CH-COV) status; NO display covers the HEAD wiring ((NSF-KEY) unretired — Unit C has NOT run; no executed status is written) |
| E-M3 (Step 12) | the "every lift g" delivery RESCOPED to every MONIC SEPARABLE lift + the ns-null mass note (the leaf's REVISION-2 gap-6/G6 record) |

Nothing refuted is consumed-as-proved anywhere: Step 17/18b consume the
twice-passed repaired layers at their displayed scopes, and every premise the
audit found unpriced is a displayed [M] member ((PACK-iv), (H8), (H9)).

**The completeness invariant, re-run.** §3.1 has **TWELVE rows ↔ (ROOT-C)'s
(H1)–(H9)**: (H1) the graded-dictionary family · (H2) = (FRESH) · (H3) =
{(ADM), D-12r, (E-N)} (three rows, one group) · (H4a) = (R1)–(R4) · (H4b) =
(UB-X) · (H5) = (PACK) (row-law roster now FOUR: PACK-i/ii/iii/iv, plus the
correspondence clause) · (H6) the terminal-seam group · (H7) = (I-τ)/K-LOC ·
**(H8) = (K-CUT)** · **(H9) = (NSF)** (two members). Ten → twelve; (PACK-iv)
adds a MEMBER, not a row. Per-clause attribution: (0)/(SQ) UNTOUCHED
(unconditional, consuming no §3.1 row); (R) and (UB) each gain (H8) + (H9).

**Acyclicity note.** The stratification Step 17 ≺ Step 18 ≺ Step 18b is
INTACT: (ROW-Σ) is proved at Step 17's layer with NO M2 content (hypotheses:
(PACK-iv), M1(a)(ii)/(iii), M1(d), the O-12 solve displays); (PACK-iv) is
hypothesis-class with no step-dependencies, entering at Step 18b (direct) and
at Step 17's E1 display only; no 18b content flows into 17/18.

**The counter and the bar.** REVISION 10 is a text change after the VC9/VC10
passes, and the acceptance those passes earned is suspended: **the
CONSECUTIVE-CLEAN COUNTER = ZERO.** The acceptance condition is restated in
full: **two consecutive CLEAN end-to-end passes on the IDENTICAL REVISION-10
text, fresh-context verifiers, ≥ 2 models (the model-diverse bar), verifiers
who saw none of the per-leaf rounds** — the tree §4 Root criterion, exactly as
at REVISION 8. Any text change after a pass restarts the counter.

**VC-next charge (the next end-to-end pass).** Verify (i) the r11 edits
themselves — E1/E2/E3/E4/E5/E6/E7/E8/E9/E-M3 above, each against the D-11 r4
leaf's final state (REVISION 3) and design V5; (ii) the TWELVE-row invariant —
the §3.1 roster ↔ (H1)–(H9) bijection, the per-clause attribution ((0)/(SQ)
untouched; (R)/(UB) gaining exactly (H8)/(H9)), and the (PACK-iv)
member-not-row accounting; (iii) NO REGRESSION on the VC9/VC10-confirmed
composition items (the walk, acyclicity incl. 17 ≺ 18 ≺ 18b, the §3.4 tables,
the quantifier-for-quantifier (ROOT-C) reading). Standing charge riders: the
(H9) display must nowhere read as covering the HEAD Lean wiring; no display
may read "accepted" as current status.

**Bookkeeping.** Historical §§7–15 untouched (their acceptance language is
historical record, marked superseded at §15's close and the footer). No leaf
status upgraded: D-11 stays PROVED-UNVERIFIED at its two-pass record (the
confirming pass owed); VERIFIED = 9 stands. Unit C ((NSF-KEY) execution) has
NOT run — it awaits Asvin's sign-off per (NSF-KEY-RET) R-0 (statement fence);
the O-11 pass 3 and the (K1-CANON) discharge remain owed. The commit closing
this revision is its unit of record (unit ROOT r11).

— REVISION 10 complete. The stop-the-line is CURED at composition level; the
acceptance is NOT re-earned; the counter is ZERO of 2 toward the restated bar.

---

## 17. REVISION 11 (r12, VC11 ripple-sync) — disposition of VC11, the Phase C end-to-end pass 11

**The pass.** VC11 (fresh context; `lean/notes/openmath/VC11_report.md`) ran on
the REVISION-10 text and returned **COMPOSITION-GAPS**: FOUR blockers + ONE
advisory, ALL of one genre — **r11 RIPPLE-SYNC defects** (displays the REVISION-10
edits should have carried forward but did not). The TARGETED r11 repairs
themselves were CONFIRMED: Step 17 matches the repaired M1's per-clause measures
and distinguishes solved (ROW-Σ) totals from booking margins; Step 18b is scoped
at (RE) = (PACK-iv); (PACK-iv) is a member (not a thirteenth row) with the closed
two-edge consumption set; §3.1 has twelve rows ↔ (H1)–(H9); (NSF-KEY) is
prominently KNOWN FALSE AT HEAD with gated retirement; (K1-CANON) has one home
inside (NSF-INT); (0)/(SQ) remain unconditional; 17 ≺ 18 ≺ 18b, (H7), and the
twelve-row bijection do not regress.

**Disposition table.**

| finding | grade | content | disposition | edit sites |
|---|---|---|---|---|
| VC11-1 | BLOCKER | Step 19 not updated to the REVISION-10 scope: its evaluation and (UB) summaries stopped at (H7), the final ∎ read "(H1)–(H7)" with (H5) at PACK-i/ii/iii only — the displayed closer silently upgraded (ROOT-C) past Step 18's new premises | ACCEPTED; Step 19's evaluation-chain attribution now carries (H8) = (K-CUT) (Step 18's K1 regrouping, Lemma 3.3's cut hypothesis) and (H9) = (NSF) (Step 18's K1/K4 label routing + Step 17's M3 leg) with PACK-iv at its closed edge set; the Σ_τ R_τ = 1 conditional set, the clause-(UB) scope display, and the final ∎ all read (H1)–(H9) with (H5) at PACK-i/ii/iii/iv — the closer proves the CURRENT (ROOT-C) quantifier-for-quantifier with no silent upgrade | Step 19 (four displays) |
| VC11-2 | BLOCKER | Step 16's claimed exact (UB) scope was stale: (H1)–(H7) + pre-r11 PACK content while asserting "exactly as §1 now displays" — a false claim after r11 | ACCEPTED; Step 16's (UB) scope display now carries (H8) + (H9) via the transported K1/K4 chain at q ↦ q^δ and PACK-iv in the transported evaluation chain (Step 18b direct + Step 17's (ROW-Σ) delivery, riding to q^δ) — the "exactly as §1 now displays" claim is made TRUE, not deleted | Step 16 |
| VC11-3 | BLOCKER | §3.4 Table A item 12 routed the shallow-cut content (routing + unique shallow cut + W_Ŝ regrouping) to the VERIFIED Step-2 layer while the (H8) row displays the same content as an OPEN T-6 kernel — two operative displays that cannot both be true; the table's "no new hypothesis surfaced" concluding claim likewise stale | ACCEPTED; item 12 RE-ADJUDICATED to hypothesis-grade: the cut content routes to **(H8) = (K-CUT)** (open T-6 kernel, M04 Lemma 3.3's hypothesis, consumed inside Step 18's K1 regrouping), Step 2 keeping only the SOLVE face (T-8/T-5, no cut-decomposition claim); the concluding claim corrected for (H8)/(H9) with the landing invariant restated | §3.4 Table A item 12 + verdict paragraph |
| VC11-4 | BLOCKER | the 6(iii)–(ix)+7 residue list was not closed: the row itself routes M3 "with the (H9) routing price" yet its purported CLOSED residue list omitted (H9) — a regression of the previously confirmed T-U correspondence audit | ACCEPTED; **(H9)** added to the row's residue list (the M3 route through Step 17's M3 leg, via Steps 11/12, inherits (NSF)); the list is again CLOSED under the table's own reading rule | §3.4 Table A row 6(iii)–(ix)+7 |
| VC11-5 | ADVISORY | Step 18b's Consumes: summary listed (GR-B)/(FRESH) while its own proof paragraph consumes seam (S1) at (GR-B)/(FRESH)/(ADM) — no hidden premise (the fuller local display governs) but an inconsistent consumer account | ACCEPTED; (ADM) restored to the summary line | Step 18b Consumes line |

All five repairs are **pure display syncs**: no [M] member added or changed, no
step proof content or member statement touched, no leaf pointer moved, historical
§§7–16 untouched.

**The completeness invariant, re-run.** §3.1 has **TWELVE rows ↔ (ROOT-C)'s
(H1)–(H9)**, UNCHANGED from REVISION 10: (H1) the graded-dictionary family (four
members) · (H2) = (FRESH) · (H3) = {(ADM), D-12r, (E-N)} · (H4a) = (R1)–(R4) ·
(H4b) = (UB-X) · (H5) = (PACK) (correspondence clause + row-laws
PACK-i/ii/iii/iv) · (H6) the terminal-seam group · (H7) = (I-τ)/K-LOC · (H8) =
(K-CUT) · (H9) = (NSF) (two members). Per-clause attribution, now displayed at
§1, Step 16, and Step 19 IDENTICALLY: clause (0) unconditional (Step 1 alone);
clause (SQ) unconditional (consumes NO §3.1 row); clause (R) consumes (H1)–(H3)
+ (H4a) + the (K3-δ) fragment of (H4b) + (H5) + (H6) + (H7) + (H8) + (H9);
clause (UB) consumes (H1)–(H3) + (H4a) + (H4b) + (H5) + (H6) + (H7) + (H8) +
(H9). No display anywhere reads a narrower or wider set.

**The counter and the bar.** VC11 was not clean, and REVISION 11 is a text
change: **the CONSECUTIVE-CLEAN COUNTER stays ZERO of 2.** The bar is unchanged:
two consecutive CLEAN end-to-end passes on the IDENTICAL REVISION-11 text,
fresh-context verifiers, ≥ 2 models, verifiers who saw none of the per-leaf
rounds (tree §4 Root criterion).

**VC12 charge (the next end-to-end pass).** Verify (i) the FIVE syncs above —
Step 19's closer against §1's current (ROOT-C) quantifier-for-quantifier; Step
16's (UB) scope against §1 (the "exactly as §1 now displays" claim must be
TRUE); §3.4 item 12's (H8) routing (no verified-layer contradiction remains);
the 6(iii)–(ix)+7 residue list's closure incl. (H9); Step 18b's Consumes/proof
consistency — and (ii) NO REGRESSION: the r11 targeted repairs (Step 17's
per-clause measures, Step 18b at (RE)/(PACK-iv), the twelve-row bijection,
(NSF-KEY)'s known-false-at-HEAD display, (0)/(SQ) unconditional, 17 ≺ 18 ≺ 18b)
need NOT be re-litigated but must not regress. Standing charge riders: the (H9)
display must nowhere read as covering the HEAD Lean wiring; no display may read
"accepted" as current status.

**Bookkeeping.** Historical §§7–16 untouched. NO status syncs: VERIFIED = 9
stands; D-11 stays PROVED-UNVERIFIED at its two-pass record (confirming pass
owed); the O-11 pass 3, Unit C ((NSF-KEY)), and the (K1-CANON) discharge remain
owed. The commit closing this revision is its unit of record (unit ROOT r12).

— REVISION 11 complete. Five ripple-syncs applied, pure display; the acceptance
is NOT re-earned; the counter is ZERO of 2 toward the VC12 bar.

## 18. REVISION 12 (r13, VC13 fold + global enumeration audit)

**The passes.** VC12 (fresh context; `lean/notes/openmath/VC12_report.md`) RAN on
the REVISION-11 text and returned **COMPOSITION-SOUND, ZERO findings** — all five
REVISION-11 syncs verified, no regression: the consecutive-clean counter reached
**ONE of 2**. VC13 (Fable, fresh context — the model-diverse pass;
`lean/notes/openmath/VC13_fable_report.md`) then RAN on the same REVISION-11 text
and returned **COMPOSITION-GAPS**: TWO blockers + THREE advisories, ALL the
STALE-HYPOTHESIS-ENUMERATION genre — displays enumerating or counting the
(ROOT-C) hypothesis roster that the r11/r12 sync rounds should have carried but
did not, now surviving a SECOND sync round. VC13 CONFIRMED everything else it
walked: the Steps 1–19 composition (every consumption a proved-on-record pointer;
no silent unconditional upgrade), the twelve-row §3.1 ↔ (H1)–(H9) bijection with
member statements at consumption strength, (NSF-KEY) nowhere consumed as true,
Step 17/18b's seams ((ROW-Σ) totals, (b-PF)/(b-EX) scopes, (RE) priced as
(PACK-iv) at the closed two-edge set), acyclicity incl. 17 ≺ 18 ≺ 18b, (0)/(SQ)
unconditional, and the suspension/counter status displays.

**Disposition table.**

| finding | grade | content | disposition | edit sites |
|---|---|---|---|---|
| VC13-1 | BLOCKER | §1's (ROOT-C) display of (H5) enumerated only PACK-i/ii/iii — the displayed theorem statement assumed a WEAKER member than Steps 17/18b consume, and the Step-19 closer's hypothesis content was not identical to §1's display at member granularity | ACCEPTED; §1's (H5) member display now carries **(PACK-iv)** sem_alloc with its REVISION-10 (r11) provenance tag and its closed two-edge consumption set (Step 18b DIRECT = Theorem M2's (RE) instantiation + Step 17's E1 delivery display of (ROW-Σ)) — §1's member enumeration is now IDENTICAL (groups and members) to the §3.1 (PACK) row and the Step-19 ∎ | §1 (ROOT-C) (H5) block |
| VC13-2 | BLOCKER | Step 18's δ-stable note quoted §1's clause-(R) attribution as "(H1)–(H3) + (H4a) + (H5) + (K3-δ) — §1", four groups short, and called (ROOT-C) "the joint five-hypothesis theorem" — a REVISION-4-era count | ACCEPTED; the "— §1" quote now reads the FULL current clause-(R) set ((H1)–(H3) + (H4a) + the (K3-δ) fragment of (H4b) + (H5) + (H6) + (H7) + (H8) + (H9)), the five-hypothesis count is RETIRED (the sentence no longer counts: "the theorem's hypothesis roster is unchanged by the NAMING since (K3-δ) ⊆ (H4b)") | Step 18 δ-stable interface note |
| VC13-3 | ADVISORY | Step 17's Consumes summary omitted the [M] rows its own Statement/Delivers display it consuming ((H9), (PACK-iv)) — against the post-VC11-5 convention (Consumes summaries carry consumed [M] rows) | ACCEPTED; (H9) = (NSF) (the M3 label-routing price) and (PACK-iv) ((H5), through (ROW-Σ)'s (RE) hypothesis) added to the Consumes line | Step 17 Consumes line |
| VC13-4 | ADVISORY | (H8)'s provenance label "the O-11 owed-pass G-2" was irreconcilable in-document with the §3.2 O-11 row's bare "pass 3 owed" — the row recorded no such pass | ACCEPTED, resolved HONESTLY: the 2026-08-03 owed-pass campaign DID run an O-11 hostile pass (`lean/notes/openmath/O11_owedpass_2026-08-03.md`, verdict CRITICAL, findings G-1 (n ≥ 2 scope restriction) / G-2 (CUT-WD undemonstrated — the (H8) source) / G-3 (the 65/65 rederivation claim unsupported)) which was adjudicated NOT BANKED as pass 3 (the r3 counter did not advance). The §3.2 O-11 row now RECORDS that run (un-banked, findings listed; the banked pass 3 stays owed), and all three (H8) provenance labels (§1, the §3.1 row, Step 18) point to it unambiguously | §3.2 O-11 row; (H8) labels at §1/§3.1/Step 18 |
| VC13-5 | ADVISORY | the (AGR) inherited-[V] row's residue claim "nothing else" elided CU-4's displayed §3.1 [M] conditionalities (the D-12r TB-CAP (τ-irr) perimeter; (E-N)'s CU-4 touch) | ACCEPTED; the residue sentence now names both (as (H3) members riding their own §3.1 rows — nothing hidden before, but the enumeration was imprecise as self-contained) | §3.2 (AGR) row |

**THE GLOBAL ENUMERATION AUDIT (the class kill).** The VC13 genre — a
hypothesis-group/member enumeration or count somewhere in the operative text,
stale against §1 — has now produced findings at THREE consecutive gapped passes
(VC11, VC13, plus the r11 originals). This revision therefore audited EVERY
display in the OPERATIVE sections (§§0–6 + §3.4; historical §§7–17 excluded by
design) that enumerates hypothesis groups or members or counts them (grep
patterns: "(H1)–(H", "PACK-i", "five-hypothesis", "hypothesis group", "— §1",
"per-clause", "incl. PACK", "twelve", plus a manual walk of §§0/1, Steps 16–19,
and §§3.1/3.2/3.4). **Result: 36 displays checked; 10 fixed** — 8 finding-driven
(the table's edit sites: §1's (H5) display; §1's, the §3.1 row's, and Step 18's
(H8) provenance labels; Step 17's Consumes; Step 18's δ-stable note; the §3.2
O-11 row; the §3.2 (AGR) row) plus 2 audit-driven syncs of the
same class caught by the sweep: (i) the §0 headline's "What exists is" §3.1-row
enumeration (stopped at (H7)/PACK-i-iii since the acceptance era; run to the
twelve-row scope with (H8)/(H9) and (PACK-iv)); (ii) Step 18's K2 (H5) row-law
display (now states the FOUR-member roster and that this step consumes exactly
PACK-i/ii/iii, with PACK-iv's closed two-edge set displayed — previously a
three-law enumeration a reader could take as (H5)'s roster). The sweep also
disambiguated the two "owed-pass C6" pointers (Step 18b + the §3.1 (PACK) row)
to the D-11 owed pass by name — the VC13-4 ambiguity genre, fixed before it
could be found (pointer fixes, outside the 36-display enumeration count). The 26
checked-clean displays: §0's suspension, REVISION-11, and (new) REVISION-12
paragraphs (3); §1's clause-(R)/(UB)/(SQ) attributions, the (H1) four-member
display, and the (ROOT) = (ROOT-C) + discharge closer (5); Step 11's A-4 homes
note (1);
Step 16's (UB) scope ("exactly as §1 now displays" — TRUE) (1); Step 17's
Statement (1);
Step 18's (H9) and K3-a/fence displays (2); Step 18b's proof + Consumes (1);
Step 19's
four attribution displays incl. the ∎ (4); §3.1's (PACK) row (four members) (1);
§3.2's D-11 scope note (1); §3.3's widened-charge row (its
PACK-i/ii/iii enumeration is item-(v)-scoped, correctly THREE laws — (PACK-iv)
is not an O-11 widened-charge item but the D-11 r4 (RE) profile) (1); §3.4's item
6(iii)–(ix)+7 residue list (closed: its named routes inherit neither (H7) nor
(H8)), item 12, item 14, Table B's (H7)/cl6 rows, and the corrected verdict
paragraph (5). **The claim
VC14 will test: every operative hypothesis-enumeration display now matches §1's
current display** (groups (H1)–(H9); (H5) members i/ii/iii/iv; (H3) =
{(ADM), D-12r, (E-N)}; (0)/(SQ) unconditional; clause-(R)/(UB) attributions as
displayed at §1/Step 16/Step 19).

All repairs are **pure display syncs**: no [M] member statement changed, no step
proof content touched, no leaf pointer moved, historical §§7–17 untouched. NO
status syncs: VERIFIED = 9 stands; the D-11 confirming pass, the banked O-11
pass 3, Unit C ((NSF-KEY)), and the (K1-CANON) discharge remain owed.

**The completeness invariant, re-run.** §3.1 has **TWELVE rows ↔ (ROOT-C)'s
(H1)–(H9)**, UNCHANGED from REVISION 10: (H1) the graded-dictionary family (four
members) · (H2) = (FRESH) · (H3) = {(ADM), D-12r, (E-N)} · (H4a) = (R1)–(R4) ·
(H4b) = (UB-X) · (H5) = (PACK) (correspondence clause + row-laws
PACK-i/ii/iii/iv) · (H6) the terminal-seam group · (H7) = (I-τ)/K-LOC · (H8) =
(K-CUT) · (H9) = (NSF) (two members). Per-clause attribution, displayed
IDENTICALLY at §1, Step 16, Step 18's δ-stable note (since this revision), and
Step 19: clause (0) unconditional (Step 1 alone); clause (SQ) unconditional
(consumes NO §3.1 row); clause (R) consumes (H1)–(H3) + (H4a) + the (K3-δ)
fragment of (H4b) + (H5) + (H6) + (H7) + (H8) + (H9); clause (UB) consumes
(H1)–(H3) + (H4a) + (H4b) + (H5) + (H6) + (H7) + (H8) + (H9). No operative
display reads a narrower or wider set, at group OR member granularity.

**The counter and the bar.** VC12 was clean (ONE of 2); VC13 was not, and
REVISION 12 is a text change: **the CONSECUTIVE-CLEAN COUNTER RESETS to ZERO
of 2.** The bar is unchanged: two consecutive CLEAN end-to-end passes on the
IDENTICAL REVISION-12 text, fresh-context verifiers, ≥ 2 models, verifiers who
saw none of the per-leaf rounds (tree §4 Root criterion).

**VC14 charge (the next end-to-end pass).** Verify (i) the FIVE VC13 repairs —
§1's (H5) display against the §3.1 row and the Step-19 ∎ (member-for-member
identity); Step 18's "— §1" quote against §1's clause-(R) attribution (the quote
must be TRUE, with no hypothesis count); Step 17's Consumes against its own
Statement/Delivers; the §3.2 O-11 row's record of the un-banked 2026-08-03 pass
against the (H8) labels (provenance reconstructible from the four corners); the
(AGR) residue sentence against Step 11's CU-4 display and the (E-N) row — and
(ii) THE AUDIT'S CLAIM: hunt for ANY operative (§§0–6 + §3.4) display
enumerating or counting hypothesis groups/members that mismatches §1's current
display — the class is claimed DEAD; one counterexample is a blocker. (iii) NO
REGRESSION on the VC12/VC13-confirmed items (the walk, the twelve-row bijection,
the seams, acyclicity, (0)/(SQ) unconditional, (NSF-KEY) discipline). Standing
charge riders: the (H9) display must nowhere read as covering the HEAD Lean
wiring; no display may read "accepted" as current status.

**Bookkeeping.** Historical §§7–17 untouched. The commit closing this revision
is its unit of record (unit ROOT r13).

— REVISION 12 complete. Five findings folded + the enumeration class killed by
global audit (36 displays checked, 10 fixed); the acceptance is NOT re-earned;
the counter is ZERO of 2 toward the VC14 bar.

## 19. REVISION 13 (r14, post-acceptance batch) — RE-ACCEPTANCE record + VC15's advisories + the discharge syncs

**The passes and the re-acceptance.** VC14 (Codex, fresh context;
`lean/notes/openmath/VC14_report.md`) RAN on the REVISION-12 text and returned
**COMPOSITION-SOUND, clean** — the §18 enumeration-audit claim TESTED and held,
zero regressions: counter ONE of 2. VC15 (Fable, fresh context — the
model-diverse, deciding pass; `lean/notes/openmath/VC15_fable_report.md`) RAN on
the same REVISION-12 text and returned **COMPOSITION-SOUND, ZERO blockers** with
an explicit null result on the blocker hunt (no unhoused consumption, no weak
member, no silent upgrade, no known-false-consumed-as-true, no attribution
mismatch at member granularity, acyclicity intact incl. 17 ≺ 18 ≺ 18b; the n = 2
anchor triple re-verified as the true quadratic densities) and FOUR advisories
(display hygiene only). The tree §4 Root criterion is MET: **the composition is
RE-ACCEPTED at (ROOT-C)'s twelve-row displayed conditional scope** (ledger entry
"RE-ACCEPTANCE: THE BAR IS MET AT TWELVE ROWS", 2026-08-05). Acceptance certifies
the composition, never the discharge of §3's ledger: the theorem as proved
remains the conditional (ROOT-C).

**This revision (unit ROOT r14).** The post-acceptance hygiene + discharge-sync
batch, per the r10/REVISION-9 precedent (acceptance earned on the pre-batch text;
every edit recorded below; the counter statement updated honestly at the close).
Also executed here: D12R-U4 (routed to this batch after the division workflow's
safety classifier correctly BLOCKED its dispatch through a generic prover
template — doc-edit units now auto-route to the revision workflows).

**Disposition table.**

| item | source (finding traced) | grade | disposition | edit sites |
|---|---|---|---|---|
| r14-1 | VC15 advisory 1 (consumption-summary sync — the VC11-5/VC13-3 convention genre) | ADVISORY | ACCEPTED; Step 18's Consumes line gains **Step 2** with the two direct edges displayed (K3 ← T-7s on the step's own K3 assembly line; K3-a ← T-5's H-DOMAINS package), the not-through-5/17 clarification, the open-residue pricing pointer ((H1)/(H6)(vii) on the displayed K3/K3-a lines), and the acyclicity note (2 ≺ 18) | Step 18 Consumes line |
| r14-2 | VC15 advisory 2 (grade-display conformance — the VC10-advisory-1/O-14a′ precedent) | ADVISORY | ACCEPTED at the honest form: NO faithfulness-audit record is on file for `MovesU/RegPFinite.lean` (checked: the M17 note's dual provenance + honesty ledger exist; a Lean-statement-vs-mathematical-statement audit does not) — Step 4's grade DROPS from "PROVED + VERIFIED-LEAN" to "PROVED (dual provenance) + machine-checked Lean-core; VERIFIED-LEAN PENDING", with the NEW named §3.2 row **M17/RegPFinite faithfulness-audit record** (the K-12 genre); impact contained by Step 5's independent emptiness result | Step 4 status; NEW §3.2 row |
| r14-3 | VC15 advisory 3 (flag billing unsynced with the recorded G-3 finding) | ADVISORY | ACCEPTED; the G-3 scope caveat (the "65/65 end-to-end rederivation" reading unsupported beyond the FIVE displayed identities — the §3.2 O-11 row's un-banked 2026-08-03 pass) now RIDES all three RESUM-n3 billings: §1's verified-instances display, the §5 RESUM-n3 row (incl. its "what it corroborates" column), and the (PACK) row's coverage sentence. Numerics stay flags-never-substitutes; nothing compositional rested on the reading | §1; §5 row; §3.1 (PACK) row |
| r14-4 | VC15 advisory 4 ((PACK-iv) terminology tension + the ℛ ⊂ ℚ(q)-vs-ℚ(t) display nit) | ADVISORY | ACCEPTED; (a) the (PACK) row gains the one-sentence reconciliation: "consumed at" prices the closed EDGE SET while Step 17's Delivers speaks of PROOF-LINE consumption relative to (EDGE-RES) — the E1 edge is a delivery display priced conservatively; both true together, over-pricing never under-pricing; (b) the q/t variable convention declared at ℛ's definition (t = q, ONE formal variable; ℚ(t) = ℚ(q)) | §3.1 (PACK) row; §1 ℛ definition |
| r14-5 | (NSF-KEY) discharge — ledger "UNIT C EXECUTED" (commit 322ec54, swept with dfdb558) + "H9 LEAD ADJUDICATION"; edit spec HDISCHARGE_H9.md §1.4 (E-r14-1..6) | STATUS SYNC (the one sanctioned retirement route, executed) | **(NSF-KEY) RETIRED** on ground (a), the six-artifact (NSF-KEY-RET) iff — R-0 Asvin sign-off (ledger verbatim) · R-1 the verbatim `MovesT.NsFree` conjunct diff at HEAD (`BridgeD3_treeCarrier.lean:94–96`) + closed ripple (`UNITC_ripple.md`) · R-2 statement texts byte-identical · R-3 the three validations (count_tie sorry-free at the guarded corpus/hdet scope; `bridgeCanonical_stable` green; V₀ DEAD = `bridgeVt_nsFree`) · R-4 build green (8579 jobs) + AxChk 740 footprints/0 sorryAx, no regression · R-5 Codex diff CONFIRMED (`UNITC_diff_confirm.md`) — with the compiled-proposition witness **H9-U1** (`Scaffold/HDischarge/H9/NsfKeyProp.lean`, landed green, Lean-core; corroboration, not a second route). The member's KNOWN-FALSE record re-labeled RETIREMENT PROVENANCE (historical); the guarded carrier displayed verbatim; the "must NEVER be displayed as covering HEAD wiring" rider DISCHARGED and replaced by "not dischargeable while (NSF-INT) stands priced". **(H9) is NOT closed**: the open member is (NSF-INT), incl. its (K1-CANON) premise (discharge owed; after it, (NSF-INT) retires on I-1+I-2+I-3, all on file, at the spec'd r15). Bounds carried: no carrier non-vacuity claim; the vt_surj/BridgeD16 completeness residue stands | §1 (H9) block + closer; §3.1 (H9) row (four sub-edits); Step 17 M3 leg; Step 18 K1 + K4 displays; §3.2 D-11 row checked (no leak) |
| r14-6 | D-12r row rewrite — D12R-U4 (HDISCHARGE_H3 §2.3), preconditions verified ON FILE: D12R-U1's (HL-a) CONFIRMED (Neukirch II §4 (4.6), p. 129) / (HL-b) DISPOSED (printed clause absent; companion lemma displayed) / (HL-c) CONFIRMED (Serre LF I §6 Prop 15, pp. 17–18) + D12R-U3's TAMS pagination PASSED (GMN Cor 3.8 + Def 3.9, pp. 374/398–400) | STATUS SYNC (VC-gated) | EXECUTED at the VC-gated wording: the row now displays the dual-accepted reduction quote ("D4R.0-K(c) … what remains is exactly {VP-SOUND's cites}" = (HEN-LIFT) + (OM-SAT)), the per-part confirmed pins, the TB-CAP(irr) precise sentence ("PROVED at [3t] from the accepted §C rev-14 Theorem C — citation into the dual-accepted corpus, not an open hypothesis"), and the §2.1 dependency-decomposition routing table (each sibling condition at its own priced home — (H7), drainage, (UB-X)(a), (H1)/O-2, P(O5t r3)). The reclassification [M] → §4 literature class RIDES VC16, which adjudicates the rewritten row's faithfulness; until then the row STANDS in §3.1 — the twelve-row roster is UNCHANGED by this revision | §3.1 D-12r row |
| r14-7 | (H8) = (K-CUT) row re-scope — H8-R2's drafted spec (`Scaffold/HDischarge/H8/R2RootSync.lean` §§A–D), firing conditions VERIFIED there (waves 0–2 green + H8-R1 on file; B3/C1 landed beyond) | STATUS SYNC (no discharge claimed) | EXECUTED: the row's status is **REDUCED, NOT discharged** — the landed-development pointer (8 Lean units + 2 documentary, green, Lean-core: ownership partition killing the G-2 falsifier channels, level-(1b) forward extraction + injectivity, count/series regroups incl. M04 Lemma 3.3 as `seriesSum_cut_conv`, the conditional consumer bridge `kcut_reduce`) with the remaining content TYPED as `kcut_reduce`'s binder rows ((K-CUT-s)/(K-CUT-g)/(K-CUT-m) + realization/verdict-keying, owners displayed); the retirement route ADJUDICATED at H8-R1: charge **(vi-a) FAIL ⇒ NO retirement** (pre-registered rule; serving map not re-pointable); the HONESTY GATE displayed — no unit-level hostile pass has run on the fan-out; **discharge awaits that hostile pass** (the ledger's "ratification-away" gate) | §3.1 (H8) row (status + owner columns) |
| r14-8 | H4's ROOT-sync briefs (`lean/notes/openmath/H4M7_rootsync_brief.md` §§2–4) | STILL-OWED (preconditions unmet — recorded, NOT executed) | The unit's own §0 verdict: NONE of the three briefs is fire-ready — F-(i) unmet (H4-M1 passes ×2 UNRUN; H4-M2 pass ×1 no record), M4's pass-2 verdict ABSENT from its file, M5's second consecutive clean owed, N-K3 NOT RUN against the sealed predictions, Asvin sign-off on the Route-B statement change not requested. NO (H4a)/(K3-δ)/(UB-X) display changes in this batch; the briefs' drafted deltas remain queued behind their displayed firing conditions F-(i)/F-(iii)/F-(iv)/F-B | none (recorded here) |
| r14-9 | EN-U5 regularization — the (E-N) row was rewritten mid-campaign (commit 89176a4, "WIP sweep") WITHOUT a ledgered revision record | PROCESS ADOPTION | The rewrite is hereby formally ADOPTED. What changed (the 89176a4 diff, on record): the row gained the EN-U1 consumption walk (RESCOPE-CLEAN, `H3_en_walk.md` — the 12-site table; every mandatory-path finite-N object thr-scoped by displayed definition/hypothesis) and the EN-U2 forced absorption verification (gate G-EN-1 = HIT: 11,177 thr > N decided boxes at 19 configs, all draining at Step 13's displayed price; route (a) permanently fenced; the caveat NON-VACUOUS at 77/420 diverging boxes) — the consumption display itself UNCHANGED; the caveat stays TRUE and OPEN, now witness-backed. Evidence: `lean/notes/openmath/H3_en_walk.md` + `verification/h3_en_falsifier.py` logs/results. THE PROCESS GAP, noted: a §3.1 row edit landed in a WIP sweep without a revision record — against this document's own change discipline; the division workflow's doc-guard (auto-BLOCK of doc-edit units, the D12R-U4 catch) now prevents recurrence. Content verdict: evidence-backed, honesty-increasing (the row prices LESS ambiguously than before), consistent with the REVISION-12 twelve-row invariant — ADOPTED as displayed | none (adoption of the standing text; record here) |

**The completeness invariant, re-run.** §3.1 has **TWELVE rows ↔ (ROOT-C)'s
(H1)–(H9)**, UNCHANGED by this revision: (H1) the graded-dictionary family (four
members) · (H2) = (FRESH) · (H3) = {(ADM), D-12r, (E-N)} (D-12r rewritten,
reclassification VC-gated, STANDING here) · (H4a) = (R1)–(R4) · (H4b) = (UB-X) ·
(H5) = (PACK) (correspondence + row-laws PACK-i/ii/iii/iv) · (H6) the
terminal-seam group · (H7) = (I-τ)/K-LOC · (H8) = (K-CUT) (REDUCED, open) ·
(H9) = (NSF) — **membership now (NSF-INT)-only-open** ((NSF-KEY) RETIRED at the
executed Unit C; (NSF-INT) stands priced with its (K1-CANON) premise). Per-clause
attribution unchanged and displayed identically at §1, Step 16, Step 18's
δ-stable note, and Step 19: (0) unconditional; (SQ) unconditional; clause (R)
consumes (H1)–(H3) + (H4a) + the (K3-δ) fragment of (H4b) + (H5) + (H6) + (H7) +
(H8) + (H9); clause (UB) consumes (H1)–(H3) + (H4a) + (H4b) + (H5)–(H9). No
step proof content, no leaf pointer, and no clause attribution changed in this
batch; the falsifier ran: `grep -n "KNOWN FALSE AT HEAD"` now hits ONLY
historical/revision-record sections and the (H9) row's re-labeled RETIREMENT
PROVENANCE clause — no operative display.

**The honest acceptance statement (the counter).** The acceptance was EARNED on
the REVISION-12 text: VC14 + VC15, two consecutive clean model-diverse passes,
fresh verifiers — the bar met at the twelve-row scope; nothing in this batch
withdraws it. But acceptance attaches to TEXT, and REVISION 13 is a text change
(advisory repairs + status syncs, incl. two §3.1 row rewrites): **a confirming
end-to-end pass (VC16) is OWED to re-anchor the accepted text on the REVISION-13
wording.** Until VC16 lands clean, the acceptance citation points at the
REVISION-12 text; a VC16 blocker would re-open the composition question in full
(the standing suspension discipline). [Superseded event, recorded at REVISION 14:
VC16 RAN CLEAN — the acceptance re-anchored on this REVISION-13 text; see §20.]

**VC16 charge (the confirming pass).** Verify (i) the four VC15 advisory repairs
(Step 18's Consumes against its own K3/K3-a lines; Step 4's grade against §0's
definition + the new §3.2 row; the three G-3 caveats against the §3.2 O-11 row;
the (PACK-iv) reconciliation sentence against Step 17's Delivers + the q/t
convention); (ii) the (H9) row sync against the six named Unit C artifacts and
the H9-U1 module (the retirement must trace to Unit C per (NSF-KEY-RET), never to
leaf text; the (NSF-INT)/(K1-CANON) residue must stay priced); (iii) the
REWRITTEN D-12r row's faithfulness (this is D12R-U4's own gate: the reduction
quote against MOVES §T.5(2)/§T.2, the pins against the briefs, the
dependency-decomposition routing against the named ledger homes) — a clean VC16
RATIFIES the row's reclassification to the §4 literature class at the NEXT
revision; (iv) the (H8) re-scope against `R2RootSync.lean` §§C–D (the
"REDUCED, not discharged" line, the (vi-a) FAIL disposition, the honesty gate);
(v) NO REGRESSION on the VC14/VC15-confirmed composition (walk, twelve-row
bijection, seams, acyclicity, (0)/(SQ) unconditional). Standing riders: no
display may claim (H9) discharged while (NSF-INT) stands priced; the acceptance
statement must read exactly as this revision displays it.

**Bookkeeping.** Historical §§7–18 untouched. `docs/PROJECT_STATE.md` synced
(E-r14-6). The commit closing this revision is its unit of record (unit ROOT
r14).

— REVISION 13 complete. Four advisories folded + four discharge/status syncs
(one retirement: (NSF-KEY); two VC-gated row rewrites: D-12r, (H8); one
adoption: (E-N)) + one still-owed record (H4 ROOT-sync); the acceptance stands
as earned on REVISION 12; VC16 is owed to re-anchor it on this text.

## 20. REVISION 14 (r15, the D-12r move) — VC16 record + the [M] → §4 reclassification executed

**The pass.** VC16 (the confirming end-to-end pass, fresh context;
`lean/notes/openmath/VC16_report.md`) RAN on the REVISION-13 text and returned
**COMPOSITION-SOUND** — zero blockers, advisories only: (1) Step 4's grade is
directionally correct but not §0-vocabulary-exact (its exact grade reads
PROVED-UNVERIFIED until the M17/RegPFinite audit lands; composition unaffected —
Step 5 independently supplies the all-prime specialization); (2) the D-12r
reclassification is **WARRANTED** on the displayed evidence (the dual-accepted
reduction confines the residue to (HEN-LIFT)/(OM-SAT); the Hensel uniqueness gap
handled by the companion lemma; TB-CAP(irr) supplied by the accepted [3t]
corpus; all sibling dependencies decomposed to existing homes — "no additional
D-12r-specific open mathematics remains that must stay [M]"), with the §4
roster synchronization named as the move's precondition; (3)–(7) the (NSF-KEY)
retirement, the K-CUT re-scope, the other REVISION-13 edits, the structural
invariants (twelve rows, acyclicity, (0)/(SQ) unconditional), and
no-REVISION-12-regression all PASS. **The acceptance RE-ANCHORS on the
REVISION-13 text** (the §19 charge met).

**This revision (unit ROOT r15).** Executes exactly the VC16-warranted move +
finding 2's three roster-sync items, honestly graded. No step proof content, no
leaf pointer, no clause attribution changed.

**Disposition table.**

| item | source | grade | disposition | edit sites |
|---|---|---|---|---|
| r15-1 | VC16 finding 2 (the adjudication) + the §19 r14-6 VC-gate ("a clean VC16 RATIFIES the row's reclassification to the §4 literature class at the NEXT revision") | STATUS SYNC (the gated move, executed) | **D-12r MOVED §3.1 [M] → §4 (entry 10)**: the row deleted from §3.1 (tombstone paragraph kept for the one-home trail); the §4 entry carries the dual-accepted reduction quote, the per-part confirmed pins ((HL-a) Neukirch II §4 (4.6) p. 129; (HL-b) companion-lemma disposition; (HL-c) Serre LF I §6 Prop 15 pp. 17–18; (OM-SAT) riding §4 item 1 by id at the TAMS-confirmed Cor 3.8 + Def 3.9 with the O-2 rider), the TB-CAP(irr) precise sentence, the consumption enumeration (Steps 2/3/11/12/19 + Step 16/clause (UB)) and the FULL dependency-decomposition routing table — each sibling dependency at its existing home, verbatim from the r14 row | §3.1 (row out + tombstone); §4 entry 10 |
| r15-2 | VC16 finding 2 sync item (a) — "§4 item 1 still says the GMN pagination confirmation is owed" | ROSTER SYNC (honest partial) | §4 item 1's status updated to the on-file state: pagination **CONFIRMED for the (OM-SAT) slice ONLY** (unit D12R-U3, `lean/notes/openmath/H3_omsat_brief.md` §1 — six loci verbatim at printed pp. 374/398–399/399/399/400/366, no arXiv-v2 drift); NOT run to full CONFIRMED — the REMAINING pinned loci still owe the LIT-1/GD-7 session (the §3.3 LIT-1 row now records the partial discharge and holds the brief's located-page map as session input) | §4 item 1; §3.3 LIT-1 row |
| r15-3 | VC16 finding 2 sync item (b) — "§4 item 5 still calls the Neukirch pin a candidate with its probe owed" | ROSTER SYNC (honest state) | §4 item 5 updated: numbers **CONFIRMED** ((4.6) "Hensel's Lemma", printed p. 129 — unit D12R-U1, `lean/notes/openmath/H3_henlift_brief.md`); the Mathlib probe **RAN, answer ABSENT** (`HenselianLocalRing` root-lifting only; no coprime-factor lifting) — the pin STANDS AS A PAPER CITE; the printed (4.6) is existence-only, uniqueness disposed via the brief's §2 companion lemma; the optional Lean materialization (D12R-U2a/b/c) owes its own axiom-policy loop when it lands. §3.3 LIT-3 row synced to the same state | §4 item 5; §3.3 LIT-3 row |
| r15-4 | VC16 finding 2 sync item (c) — "Serre LF I §6 Prop. 15 is not presently enumerated as a consumed locus" | ROSTER SYNC | **Serre LF I §6 Prop 15 (printed pp. 17–18) ADDED** to §4 item 4's consumed-loci enumeration, tagged as the (HL-c) read-off of the D-12r pin, with the entry-10 cross-reference | §4 item 4 |
| r15-5 | the VC13-lesson enumeration audit (this revision's ripple) | RIPPLE SYNC | Every operative (H1)–(H9)/(H3)-membership/row-count display checked and synced: §1's (ROOT-C) (H3) display shrunk to {(ADM), (E-N)} with the reclassification bracket (grouping preserved); §1's clause-(UB) B1 attribution parenthetical re-pointed; headline enumeration run to ELEVEN; Steps 2, 3, 11 (the A-4 reconciling display), 16, and 19 re-pointed to "perimeter-conditional on the §4 D-12r pin"; the §3.1 (E-N) row's leg-C3 mention, the §3.2 (AGR) row, §3.4 Table A (the 6(iii)–(ix)+7 residue list and row 10's (τ-irr) pointer), Step 18's O-11 r3 tier record (its H-2 = D-12r naming bracketed to the §4 home), the §3.2 pass-ledger row, §6, and the footer synced. AUDIT COUNT: 19 displays checked — 16 EDITED (header roster enumeration + headline count note; §1's (H3) display + the clause-(UB) B1 parenthetical; Steps 2, 3, 11 (A-4 display), 16, 19; Step 18's tier record; §3.1 row-out/tombstone + the (E-N) leg-C3 mention; §3.2 (AGR) + pass-ledger rows; §3.4 residue list + row 10) and 3 CONFIRMED already-conformant (Step 12's text names no D-12r directly — its consumption is priced at the §4 entry's enumeration [this confirmation was ADJUDICATED INSUFFICIENT by VC17 blocker 1 against the stronger every-site-re-pointed claim — RESOLVED at REVISION 15, r16: Step 12's (τ-irr) description now carries the local "perimeter-conditional on the §4 D-12r pin (entry 10)" sentence]; §1's (ROOT)-discharge sentence and Step 19's final ∎ name hypothesis GROUPS only, membership displayed at §1's (H3) bracket); the §4/§3.3 roster-sync edits counted at r15-1..r15-4; the historical §§7–19 records untouched by design | §§0/1, Steps 2/3/11/16/18/19, §3.1/§3.2/§3.4/§4/§6, footer |

**The completeness invariant, re-run.** §3.1 has **ELEVEN rows ↔ (ROOT-C)'s
(H1)–(H9)**: (H1) the graded-dictionary family (four members) · (H2) = (FRESH) ·
**(H3) = {(ADM), (E-N)} (TWO members — D-12r out to §4 at this revision)** ·
(H4a) = (R1)–(R4) · (H4b) = (UB-X) · (H5) = (PACK) (correspondence + row-laws
PACK-i/ii/iii/iv) · (H6) the terminal-seam group · (H7) = (I-τ)/K-LOC ·
(H8) = (K-CUT) (REDUCED, open) · (H9) = (NSF) ((NSF-INT)-only-open). Per-clause
attribution UNCHANGED IN FORM and displayed identically at §1, Step 16, Step
18's δ-stable note, and Step 19: (0) unconditional; (SQ) unconditional; clause
(R) consumes (H1)–(H3) + (H4a) + the (K3-δ) fragment of (H4b) + (H5)–(H9);
clause (UB) consumes (H1)–(H3) + (H4a) + (H4b) + (H5)–(H9) — with (H3) now the
two-member group, and the former D-12r conditionality carried as
LITERATURE-grade at the §4 pin (consuming steps say "perimeter-conditional on
the §4 D-12r pin"; nothing evaporated — the perimeter is priced, in the goal's
conditionality class instead of the hypothesis ledger). The falsifier ran:
`grep -n "D-12r"` hits in operative sections are now the §4 entry, the §3.1
tombstone, and re-pointed conditionality language only — no operative display
names a §3.1 D-12r row.

**The honest acceptance statement (the counter).** The acceptance was EARNED on
the REVISION-12 text (VC14 + VC15, two consecutive clean model-diverse passes,
fresh verifiers) and RE-ANCHORED on the REVISION-13 text by VC16's clean
confirming pass. REVISION 14 executes a VC16-WARRANTED reclassification — the
verifier adjudicated the move's substance before it was made — but it IS a text
change: **a confirming end-to-end pass (VC17) is OWED to re-anchor the accepted
text on the REVISION-14 wording.** Until VC17 lands clean, the acceptance
citation points at the REVISION-13 text; a VC17 blocker would re-open the
composition question in full (the standing suspension discipline).

**VC17 charge (the confirming pass).** Verify (i) the D-12r move's faithfulness:
the §4 entry 10 against the §19 r14-6 record and the HDISCHARGE_H3 §2.3 spec
(reduction quote, pins, routing table — nothing dropped, nothing strengthened);
(ii) the ELEVEN-row ↔ (H1)–(H9) bijection with (H3) at two members, and that
every operative enumeration display matches §1 (the r15-5 audit's claim);
(iii) the three roster syncs against the briefs (item 1 partial-only — the
LIT-1 session must still show owed; item 5's probe-ABSENT wording; item 4's
Prop 15 addition); (iv) that no consuming step lost its TB-CAP perimeter
pricing (Steps 2/3/11/12/16/19 all display the §4-pin conditionality);
(v) NO REGRESSION on the VC16-confirmed composition (walk, seams, acyclicity,
(0)/(SQ) unconditional, the (H9)/(H8)/(E-N) states). Standing riders: no
display may claim the D-12r perimeter discharged — it is literature-conditional
at the §4 pins, which is a conditionality class, not a discharge; the
acceptance statement must read exactly as this revision displays it.

**Bookkeeping.** Historical §§7–19 untouched. `docs/PROJECT_STATE.md` sync owed
at the next state-file touch. The commit closing this revision is its unit of
record (unit ROOT r15).

— REVISION 14 complete. One warranted reclassification executed (D-12r [M] →
§4, ELEVEN rows) + three honest roster syncs + the enumeration-audit ripple;
the acceptance stands as earned on REVISION 12 and re-anchored on REVISION 13;
VC17 is owed to re-anchor it on this text.

## 21. REVISION 15 (r16, VC17 fold) — the two display-conformance repairs at the r15 edits

**The pass.** VC17 (the confirming end-to-end pass, fresh context;
`lean/notes/openmath/VC17_report.md`) RAN on the REVISION-14 text and returned
**COMPOSITION-GAPS** — ONE blocker + ONE advisory, both display-conformance
findings at the r15 edits themselves. All other charged checks PASS: the D-12r
move's faithfulness (residue confined to (HEN-LIFT)/(OM-SAT), the uniqueness
companion lemma explicit, sibling debts decomposed at their prior homes); §3.1
at exactly ELEVEN rows ↔ (H1)–(H9) with (H3) = {(ADM), (E-N)}; the operative
clause attributions ((0)/(SQ) unconditional; (R)/(UB) carrying the same groups
at §1 and Steps 16/18/19); the remaining twelve-row/former-(H3) enumerations
confined to dated historical records; the walk conditional at its stated scope
with no unconditional (ROOT) upgrade; acyclicity intact incl. 17 ≺ 18 ≺ 18b;
no REVISION-12/13 regression.

**This revision (unit ROOT r16).** Repairs both findings in place — pure
display syncs. No [M] member, step proof content, leaf pointer, or clause
attribution changed; NO status syncs.

**Disposition table.**

| item | source | grade | disposition | edit sites |
|---|---|---|---|---|
| r16-1 | VC17 blocker 1 (Step 12 not locally re-pointed at the D-12r move) | BLOCKER — repaired in place | Step 12's `(τ-irr)` saturation description now carries the honest conditionality sentence locally: "the (τ-irr) leg's TB-CAP dependence is perimeter-conditional on the §4 D-12r pin (entry 10)" — the language Steps 2/3/11/16/19 received at r15. The r15-5 audit had passed Step 12 as already-conformant on the central-§4-enumeration ground alone; VC17 adjudicated that INSUFFICIENT against the stronger every-former-consumption-site-re-pointed claim — the §20 r15-5 acknowledgment ("Step 12's text names no D-12r directly") is KEPT and marked RESOLVED in place | Step 12 statement; §20 r15-5 row (resolution bracket) |
| r16-2 | VC17 advisory 2 (Step 4's grade outside the §0 vocabulary — completing VC16 finding 1 / the REVISION-13 VC15-advisory-2 conformance) | ADVISORY — repaired in place | Step 4's grade rendered in the EXACT §0 word: **PROVED-UNVERIFIED**, with the named §3.2 row **M17/RegPFinite faithfulness-audit record** displayed as the pending upgrade condition (VERIFIED-LEAN on its landing); the machine-checked-Lean-core fact (dual provenance, `MovesU/RegPFinite.lean`, 14 theorems) and the Step-5 independent-route containment stay as displayed NOTES, not grade words — no silent mathematical upgrade or downgrade; the §3.2 row's own grade sentence synced | Step 4 status; §3.2 M17/RegPFinite row |

**The completeness invariant, re-run.** §3.1 has **ELEVEN rows ↔ (ROOT-C)'s
(H1)–(H9)**, UNCHANGED from REVISION 14: (H1) the graded-dictionary family
(four members) · (H2) = (FRESH) · (H3) = {(ADM), (E-N)} (TWO members) ·
(H4a) = (R1)–(R4) · (H4b) = (UB-X) · (H5) = (PACK) (PACK-i/ii/iii/iv) ·
(H6) the terminal-seam group · (H7) = (I-τ)/K-LOC · (H8) = (K-CUT) (REDUCED,
open) · (H9) = (NSF) ((NSF-INT)-only-open). Per-clause attribution UNCHANGED
and displayed identically at §1, Step 16, Step 18's δ-stable note, and Step 19:
(0) unconditional; (SQ) unconditional; clause (R) consumes (H1)–(H3) + (H4a) +
the (K3-δ) fragment of (H4b) + (H5)–(H9); clause (UB) consumes (H1)–(H3) +
(H4a) + (H4b) + (H5)–(H9) — the former D-12r conditionality carried as
LITERATURE-grade at the §4 pin, now displayed locally at EVERY former
consumption site (Steps 2/3/11/12/16/19 — Step 12 closed at r16-1). The
falsifier re-ran: `grep -n "D-12r"` hits in operative sections are the §4
entry, the §3.1 tombstone, and re-pointed conditionality language only.

**The honest acceptance statement (the counter).** The acceptance was EARNED
on the REVISION-12 text (VC14 + VC15, two consecutive clean model-diverse
passes, fresh verifiers) and RE-ANCHORED on the REVISION-13 text by VC16's
clean confirming pass. VC17 was the confirming pass owed on REVISION 14; it
returned COMPOSITION-GAPS, so it does NOT re-anchor — and REVISION 15, which
repairs its two findings, IS a text change: **the confirming end-to-end pass
(VC18) is OWED to re-anchor the accepted text on the REVISION-15 wording.**
Until VC18 lands clean, the acceptance citation points at the REVISION-13
text; a VC18 blocker would re-open the composition question in full (the
standing suspension discipline).

**VC18 charge (the confirming pass).** Verify (i) the two r16 repairs: Step 12
displays the local "perimeter-conditional on the §4 D-12r pin (entry 10)"
sentence at its (τ-irr) description, and Step 4's grade word is exactly
PROVED-UNVERIFIED with the audit row named and the Lean-core/Step-5 facts as
notes only; (ii) EVERY former D-12r consumption site (Steps 2/3/11/12/16/19)
now displays the §4-pin conditionality locally — the claim VC17 refuted at
Step 12; (iii) the §20 r15-5 resolution bracket is honest (acknowledgment
kept, marked resolved, nothing rewritten); (iv) NO REGRESSION on the
VC17-confirmed items (the D-12r faithfulness, the ELEVEN-row bijection, the
walk, seams, acyclicity, (0)/(SQ) unconditional, the (H9)/(H8)/(E-N) states).
Standing riders unchanged: no display may claim the D-12r perimeter
discharged; the acceptance statement must read exactly as this revision
displays it.

**Bookkeeping.** Historical §§7–20 untouched except the sanctioned r15-5
resolution bracket. `docs/PROJECT_STATE.md` sync owed at the next state-file
touch. The commit closing this revision is its unit of record (unit ROOT r16).

— REVISION 15 complete. VC17's blocker + advisory folded as two in-place
display syncs (Step 12 locally re-pointed; Step 4 at the exact §0 grade);
ELEVEN rows unchanged; the acceptance stands as earned on REVISION 12 and
re-anchored on REVISION 13; VC18 is owed to re-anchor it on this text.

## 22. REVISION 16 (r17, the K-CUT retirement) — VC18 record + the second post-acceptance row retirement

**The pass.** VC18 (the confirming end-to-end pass, fresh context;
`lean/notes/openmath/VC18_report.md`) RAN on the REVISION-15 text and returned
**COMPOSITION-SOUND** — zero blockers, ONE advisory (finding 1: the §3.2
"End-to-end passes on THIS document" row ended at VC16 and still said VC17 was
owed — bookkeeping drift, not a conditionality defect; the headline, §6,
footer, and §21 carried the correct status). Findings 2–7 PASS: both r16
repairs present and sufficient; all six former D-12r consumption sites locally
conditional; the ELEVEN-row ↔ (H1)–(H9) bijection with (H3) = {(ADM), (E-N)};
acyclicity incl. 17 ≺ 18 ≺ 18b; the displayed-scope walk closing only at
(ROOT-C); zero regression. **The acceptance RE-ANCHORS on the REVISION-15
text** (the §21 charge met).

**The gate.** The (K-CUT) row's displayed discharge condition — "discharge
awaits ONLY the clean re-pass" (`Scaffold/HDischarge/H8/R2RootSync.lean`
§§A/G, the display of record since the r14 re-scope) — **FIRED**: the round-3
unit-level hostile pass (`lean/notes/openmath/KCUT_pass3_2026-08-06.md`,
fresh-context Codex, falsifier-first) returned **VERDICT: DISCHARGES**, with
its scope line verbatim: "This discharges the Round-3 review gate—not the
remaining semantic owner rows and not the unconditional ROOT theorem." The
3-round arc: round 1 CRITICAL (7 crit + 5 gap, ALL display-layer; the Lean
declarations audited FAITHFUL) → repairs + the C2/C3 content units landed and
proved (bfe0a85) → round 2 CRITICAL (display staleness ONLY; content confirmed
proved, drift-free) → displays re-synced → round 3 DISCHARGES (staleness
cured; conditional decomposition/ownership/product/K1 composition PROVED at
the consumption site; every material unproved semantic interface displayed as
residue, not consumed). Per the ledger's "KCUT ROUND 3: DISCHARGES — SECOND
ROW RETIREMENT" entry, the row retires into the consuming leaves' displayed
debts.

**This revision (unit ROOT r17).** Executes the retirement + the VC18 record +
the VC18 advisory repair. No step proof content, no leaf pointer changed;
clause attributions synced ((R)/(UB) lose (H8)).

**Disposition table.**

| item | source | grade | disposition | edit sites |
|---|---|---|---|---|
| r17-1 | VC18 (COMPOSITION-SOUND on REVISION 15) | ANCHOR RECORD | the acceptance RE-ANCHORS on the REVISION-15 text; recorded at the header (new CURRENT-standing paragraph), the §3.2 end-to-end row, §6, and the footer | §0, §3.2, §6, footer |
| r17-2 | VC18 advisory 1 (the §3.2 end-to-end row stale at VC16) | ADVISORY — repaired in place | the row's tail now records VC17 (COMPOSITION-GAPS → REVISION 15) and VC18 (COMPOSITION-SOUND, re-anchor), and closes at "VC19 OWED on the REVISION-16 wording" | §3.2 end-to-end row |
| r17-3 | the K-CUT retirement (the round-3 discharge gate + the ledger's named route) | ROW RETIREMENT — executed | the (H8) = (K-CUT) row DELETED from §3.1 with a tombstone keeping the one-home trail; the NEW §3.2 K-CUT retirement row records THE PACKAGE (the repaired adjudication display R2RootSync §§C–G + `KCutMassFactor` (`massFactor_mass_eq`) + `KCutK1Tie` (`kcut_k1_form`, `kcut_k1_form_of_massFactor`) + the ownership/extraction/regroup theorems, ALL Lean-core; the 3-round pass arc) and THE THREE MOVED RESIDUES with owners named: graft completeness → the D-14 + CU-3 + REL.2 arc; measured-mass identification → the (H7)/sibjc TREE-EXP + [2r]/CL-8 + O-1/O-3 count-tie owners; the τ/δ pool dictionary → [2r]/CL-8 + the W-leg at CL-17(ii)/W17ii. HONESTY RIDERS displayed at both records: nothing semantic is discharged (C2/C3 are UNPINNED assumption packages — the round-3 classification); the H8-R1 charge-(vi) adjudication STANDS ((vi-a) FAIL — the serving map "CUT-WD ← D-14 + CU-3" NOT re-pointed as demonstrated): the retirement rides the LATER-EARNED ground (the proved package + the pass arc), not that route | §3.1 (row out + tombstone); §3.2 (new row) |
| r17-4 | the enumeration audit (the standing VC13 lesson, re-run at this retirement) | RIPPLE SYNC | every operative hypothesis-enumeration/row-count display checked and synced to **TEN rows ↔ (H1)–(H9) minus the retired (H8)**. AUDIT COUNT: 21 sites touched — §0's headline enumeration bracket + the NEW current-standing paragraph; §1's (ROOT-C) (H8) member display (→ the RETIRED bracket), clause-(R) attribution, clause-(UB) attribution, and the (ROOT)-discharge-distance sentence; Step 16's (UB) scope display; Step 18's K1 line (now citing the RETIRED package + the residues at their owners) and its δ-stable "— §1" quote; Step 19's evaluation-chain closer, Σ_τ conditional set, clause-(UB) scope, and final ∎; §3.1 (the row deletion + tombstone); §3.2 (the retirement row, the O-11 row's provenance bracket, the end-to-end row); §3.4 item 12 + the verdict paragraph; §6's new dispatch bracket; the footer record. Dated historical displays (the §0 REVISION-10..15 standing paragraphs, §6's dated brackets, the §3.2 pass-arc narrative's dated segments, §§7–21) untouched by design — they record what was true when written | §§0/1, Steps 16/18/19, §3.1/§3.2/§3.4, §6, footer |

**The completeness invariant, re-run.** §3.1 has **TEN rows ↔ (ROOT-C)'s
(H1)–(H9) minus the retired (H8)** (the historical letters kept): (H1) the
graded-dictionary family (four members) · (H2) = (FRESH) · (H3) = {(ADM),
(E-N)} (TWO members) · (H4a) = (R1)–(R4) · (H4b) = (UB-X) · (H5) = (PACK)
(PACK-i/ii/iii/iv) · (H6) the terminal-seam group · (H7) = (I-τ)/K-LOC ·
(H9) = (NSF) ((NSF-INT)-only-open). Per-clause attribution, displayed
identically at §1, Step 16, Step 18's δ-stable note, and Step 19: (0)
unconditional; (SQ) unconditional; clause (R) consumes (H1)–(H3) + (H4a) + the
(K3-δ) fragment of (H4b) + (H5) + (H6) + (H7) + (H9); clause (UB) consumes
(H1)–(H3) + (H4a) + (H4b) + (H5) + (H6) + (H7) + (H9). The former (H8) content
enters both clauses through the RETIRED machine-proved package + the owner
homes (the §3.2 K-CUT retirement row) — no operative display consumes a
separate (H8) hypothesis, and no residue lost its displayed home: graft,
measured mass, and the τ/δ dictionary each carry a named owner already priced
in this ledger.

**The honest acceptance statement (the counter).** The acceptance was EARNED
on the REVISION-12 text (VC14 + VC15, two consecutive clean model-diverse
passes, fresh verifiers), RE-ANCHORED on REVISION 13 by VC16 and on REVISION
15 by VC18. This is the SECOND post-acceptance row retirement (D-12r at
REVISION 14 the first), and REVISION 16 IS a text change: **the confirming
end-to-end pass (VC19) is OWED as the anchor on the REVISION-16 wording.**
Until VC19 lands clean, the acceptance citation points at the REVISION-15
text; a VC19 blocker would re-open the composition question in full (the
standing suspension discipline).

**VC19 charge (the confirming pass).** Verify (i) the retirement's
FAITHFULNESS: the §3.2 K-CUT retirement row's package pointers are real and
Lean-core, `kcut_k1_form`'s statement is Step 18 K1's displayed exact form at
its stated conditionality, and NO residue (graft / measured mass / τ/δ
dictionary) is silently consumed anywhere as proved; (ii) the TEN-row ↔
(H1)–(H9)-minus-(H8) bijection and the per-clause attributions ((R)/(UB) lose
exactly (H8), nothing else moved); (iii) the r17-4 enumeration-audit claim —
every operative hypothesis-enumeration/row-count display reads ten rows, with
(H8) mentions carrying retirement brackets; (iv) Step 18's K1 line cites the
RETIRED package + the owners, with no separate (H8) hypothesis; (v) the
honesty riders: the charge-(vi) FAIL stands displayed, the round-3 scope line
is quoted, no display claims a semantic residue discharged; (vi) the VC18
advisory repair (the §3.2 end-to-end row current through VC18); (vii) NO
REGRESSION on the VC18-confirmed items (the D-12r pins, the walk, seams,
acyclicity, (0)/(SQ) unconditional, the (H9)/(E-N) states).

**Bookkeeping.** Historical §§7–21 untouched. `docs/PROJECT_STATE.md` sync
owed at the next state-file touch. The commit closing this revision is its
unit of record (unit ROOT r17).

— REVISION 16 complete. VC18 recorded (re-anchor on REVISION 15); the (H8) =
(K-CUT) row retired on the round-3 discharge gate — the package Lean-core, the
residues at their named owners, TEN rows ↔ (H1)–(H9) minus the retired (H8);
VC19 is owed as the confirming anchor on this text.

## 23. REVISION 17 (r18, the K-CUT residue re-pricing) — disposition of VC19

**The pass.** VC19 (the confirming end-to-end pass, fresh context;
`lean/notes/openmath/VC19_report.md`) RAN on the REVISION-16 text and returned
**COMPOSITION-GAPS** — TWO blockers, ONE underlying defect: **the r17
retirement OVERSHOT.** The (K-CUT) package is genuinely proved and its §3.2
retirement record STANDS (VC19 confirms it), but the three residual semantic
interfaces were assigned to owners whose statements do NOT house them, so the
ten-row theorem silently consumed unpriced content. VC19 also confirmed: the
displayed order acyclic (incl. 17 ≺ 18 ≺ 18b); no operative stale enumeration
outside dated records; no independent regression on the D-12r, NSF-KEY, PACK,
(SL≥2), unconditional (0)/(SQ), and Σ_τ R_τ = 1 repairs.

**Disposition table.**

| item | source | grade | disposition | edit sites |
|---|---|---|---|---|
| r18-1 | VC19 blocker 1 (residues named but not housed: D-14/CU-3 prove read locality + nonempty realization, NOT graft completeness/bijectivity; (H7)/TREE-EXP/CL-8/O-1/O-3 do not state the shMass/blkMass measured-mass identification; CL-8/W17ii do not supply the node-level τ/δ pool dictionary or blockTotal_tie's instantiation) | BLOCKER — ACCEPTED, repaired by re-pricing | the NEW SLIMMED §3.1 row **(H8′) — the K-CUT residual-interface row**, THREE members stated precisely from the package's own displays (the §3.2 retirement record; R2RootSync §D; the round-3 report): **(i) GRAFT** — the `graft`/`graft_cut`/`cut_graft` completeness/bijectivity interface (the H8-R1 charge-(vi-a) FAIL stands as its status); **(ii) MASS-ID** — C2's `shMass`/`blkMass` ARE the measured masses at the consumed member; **(iii) POOL-DICT** — the node-level τ/δ pool dictionary beyond the pinned `e_key` + `blockTotal_tie`'s instantiation. The former owner labels stand as retirement ROUTES (graft via the D-14/CU-3/REL.2 arc's future completeness theorem; MASS-ID via the (H7)/TREE-EXP + CL-8 development; POOL-DICT via CL-8 + the W-leg at CL-17(ii)/W17ii), NOT as current homes. Enters: Step 18's K1, hence clause (R); clause (UB) inherits through the transported chain | §3.1 (new row); §1 (new member display); §3.2 K-CUT retirement row (residue sentences re-pointed at (H8′), routes kept) |
| r18-2 | VC19 blocker 2 (the ten-row theorem omitted the K1 premises: clause (R), hence transported (UB), relied on unpriced content at Step 18's K1 — the claimed semantic bijection false) | BLOCKER — ACCEPTED, repaired by attribution sync | Step 18's K1 line now DISPLAYS its cut consumption as the RETIRED package + **(H8′)**; §1's clause-(R) and clause-(UB) attributions, Step 16's (UB) scope display, Step 18's δ-stable "— §1" quote, and Step 19's four displays (evaluation chain, Σ_τ conditional set, clause-(UB) scope, final ∎) all gain (H8′) IDENTICALLY — (R)/(UB) gain exactly (H8′), nothing else moves | §1, Steps 16/18/19 |
| r18-3 | the §3.2 K-CUT retirement row + §3.1 tombstone (the r17 "already-priced home" / "moved to their owners" sentences) | RESIDUE RE-POINT | both records keep the proved PACKAGE and the pass arc unchanged; the residue sentences now point at the (H8′) members as the priced home, with the owner labels re-labeled retirement ROUTES; the §3.2 O-11 row's provenance bracket and §3.4 item 12 + the verdict paragraph synced the same way | §3.1 tombstone; §3.2 (retirement + O-11 rows); §3.4 (item 12 + verdict) |
| r18-4 | THE ENUMERATION AUDIT (the standing VC13 lesson, now TWICE-learned: retirement edits must run the audit too — r17 ran it and still under-priced by omitting the residues from every roster) | RIPPLE SYNC | every operative hypothesis-enumeration/row-count display checked and synced to **ELEVEN rows — (H1)–(H9) minus the retired (H8), plus (H8′)**. AUDIT COUNT: 22 sites touched — §0's headline enumeration bracket (the (H8′) entry + the scope-history tail) + the NEW current-standing paragraph (+ the REVISION-16 paragraph retitled superseded); §1's (H8)-retired bracket, the NEW (H8′) member display, the clause-(R) attribution, the clause-(UB) attribution, and the (ROOT)-discharge-distance sentence; Step 16's (UB) scope display + its scope-history tail; Step 18's K1 line and its δ-stable "— §1" quote; Step 19's evaluation-chain closer, Σ_τ conditional set, clause-(UB) scope, and final ∎; §3.1 (the new row + the tombstone correction); §3.2 (the retirement row's residue sentences + owner column, the O-11 row's provenance bracket, the end-to-end row); §3.4 item 12 + the verdict paragraph; §6's new dispatch bracket; the footer record. Dated historical displays (§0's REVISION-10..16 standing paragraphs' interiors, §6's dated brackets, the §3.2 pass-arc narrative's dated segments, §§7–22) untouched by design | §§0/1, Steps 16/18/19, §3.1/§3.2/§3.4, §6, footer |

**The completeness invariant, re-run.** §3.1 has **ELEVEN rows — (ROOT-C)'s
(H1)–(H9) minus the retired (H8), plus (H8′)** (the historical letters kept):
(H1) the graded-dictionary family (four members) · (H2) = (FRESH) · (H3) =
{(ADM), (E-N)} (TWO members) · (H4a) = (R1)–(R4) · (H4b) = (UB-X) · (H5) =
(PACK) (PACK-i/ii/iii/iv) · (H6) the terminal-seam group · (H7) = (I-τ)/K-LOC
· **(H8′) the K-CUT residual-interface row (GRAFT / MASS-ID / POOL-DICT)** ·
(H9) = (NSF) ((NSF-INT)-only-open). Per-clause attribution, displayed
IDENTICALLY at §1, Step 16, Step 18's δ-stable note, and Step 19: (0)
unconditional; (SQ) unconditional; clause (R) consumes (H1)–(H3) + (H4a) + the
(K3-δ) fragment of (H4b) + (H5) + (H6) + (H7) + (H8′) + (H9); clause (UB)
consumes (H1)–(H3) + (H4a) + (H4b) + (H5) + (H6) + (H7) + (H8′) + (H9). The
retired (H8) package enters both clauses at Step 18's K1 as machine-proved
content; its residues enter as (H8′) — nothing rides an owner label. **NET
PROGRESS, stated honestly:** the former (H8) row assumed the WHOLE
shallow-cut decomposition (exactly-one decomposition + read ownership + the
weight-preserving product law); (H8′) assumes ONLY the three interfaces —
the decomposition/ownership/product/K1-composition core is machine-proved
Lean-core (`kcut_reduce` + `kcut_k1_form` + `massFactor_mass_eq`). The r17
arc was net progress mis-stated, not progress reversed.

**The honest acceptance statement (the counter).** The acceptance was EARNED
on the REVISION-12 text (VC14 + VC15), RE-ANCHORED on REVISION 13 by VC16 and
on REVISION 15 by VC18. VC19's blockers re-opened the composition question on
the REVISION-16 wording; REVISION 17 repairs it. **The confirming end-to-end
pass (VC20) is OWED as the anchor on the REVISION-17 wording.** Until VC20
lands clean, the acceptance citation points at the REVISION-15 text; a VC20
blocker would re-open the composition question in full (the standing
suspension discipline).

**VC20 charge (the confirming pass).** Verify (i) the (H8′) row's
FAITHFULNESS: its three members are exactly the round-3 report's residual
interfaces, stated at the package's own display strength, with no fourth
residue omitted and none weakened; (ii) the ELEVEN-row roster and the
per-clause attributions ((R)/(UB) gain exactly (H8′), nothing else moved);
(iii) the r18-4 enumeration-audit claim — every operative display reads
eleven rows with the (H8) retirement + (H8′) brackets consistent; (iv) Step
18's K1 line consumes the retired package + (H8′), with the owner labels
nowhere consumed as housing statements; (v) the honesty riders stand (the
charge-(vi-a) FAIL displayed at (H8′)(i) and the §3.2 row; the round-3 scope
line quoted; nothing displayed as discharged); (vi) NO REGRESSION on the
VC19-confirmed items (the package + retirement record, acyclicity, the
walk, the D-12r/NSF-KEY/PACK/(SL≥2)/(0)-(SQ)/Σ_τ repairs).

**Bookkeeping.** Historical §§7–22 untouched. `docs/PROJECT_STATE.md` sync
owed at the next state-file touch. The commit closing this revision is its
unit of record (unit ROOT r18).

— REVISION 17 complete. VC19 recorded (COMPOSITION-GAPS, the overshoot
caught); the three K-CUT residual interfaces re-priced as the NEW slimmed
§3.1 row (H8′) — GRAFT / MASS-ID / POOL-DICT, owner labels as routes; ELEVEN
honest rows; VC20 is owed as the confirming anchor on this text.

---

## 24. REVISION 18 (r19, VC20 leftover fixes) — disposition of VC20

**The pass.** VC20 (the confirming end-to-end pass, fresh context;
`lean/notes/openmath/VC20_report.md`) RAN on the REVISION-17 text and
returned **COMPOSITION-GAPS** — TWO blockers, BOTH leftover SENTENCES from
the r17→r18 arc; **the (H8′) substance is CONFIRMED**. VC20's own pass-list:
(H8′)'s three members exactly cover VC19's missing K1 premises (GRAFT,
MASS-ID, POOL-DICT including `blockTotal_tie`; no fourth VC19 residue
apparent; stated at the consumed interface strength without silently
reinstating the full retired cut kernel); the live clause attributions at
§1, Step 16, Step 18, and Step 19 agree ((R)/(UB) gain exactly (H8′);
(0)/(SQ) unconditional); the displayed-scope walk closes at (ROOT-C);
acyclicity intact (incl. 17 ≺ 18 ≺ 18b; (H8′) a hypothesis row, no backward
edge); no independent regression in D-12r, NSF-KEY, PACK, (SL≥2), the
Σ_τ R_τ identity, or the K-CUT machine-proved core.

**Disposition table.**

| item | source | grade | disposition | edit sites |
|---|---|---|---|---|
| r19-1 | VC20 blocker 1 (§1's retired-(H8) bracket still said "the live roster is (H1)–(H9) MINUS the retired (H8): TEN §3.1 rows" — contradicting §1's own (H8′) member, Step 16, Step 19, §3.1, and §23) | BLOCKER — ACCEPTED, repaired in place | the bracket now reads: the live roster is (H1)–(H9) MINUS the retired (H8) PLUS the (H8′) row below: **ELEVEN §3.1 rows** — with the correction dated (REVISION 18, r19, executing VC20 blocker 1; the bracket had lagged at the r17 TEN) | §1, the (H8) retired-member bracket |
| r19-2 | VC20 blocker 2 (the §3.2 K-CUT retirement row's WHAT-RETIRED sentence still carried the pre-r18 clauses "proved ... CONDITIONAL on typed, owner-displayed interfaces" and "priced at its owners" alongside the r18 correction later in the same row — the precise owner-housing claim VC19 rejected) | BLOCKER — ACCEPTED, repaired by REWRITE to one voice | the WHAT-RETIRED sentence now reads: the row's consumed statement is proved at the consumption site CONDITIONAL on the three typed residual interfaces (GRAFT / MASS-ID / POOL-DICT), **which are PRICED AT the §3.1 row (H8′)**; the owner arcs are retirement ROUTES only, NOT homes; a standing full-strength (H8) row would double-price content that is either machine-proved or priced at (H8′) — the incompatible r17 clauses STRUCK with a dated strike bracket | §3.2, the K-CUT retirement row |
| r19-3 | THE ENUMERATION AUDIT (the standing VC13 lesson, re-run at this fix round) | RIPPLE SYNC | every operative roster/row-count display re-checked against §1's (ROOT-C): the r19-1 site was the ONLY operative stale count. AUDIT COUNT: 24 sites checked, 2 EDITED (the two blocker sites), 22 CONFIRMED conformant — §0's headline enumeration bracket + scope-history tail; §1's (H8′) member display, clause-(R) attribution, clause-(UB) attribution, (ROOT)-discharge-distance sentence; Step 16's (UB) scope display + scope-history tail; Step 18's K1 line + its δ-stable "— §1" quote; Step 19's evaluation-chain closer, Σ_τ conditional set, clause-(UB) scope, final ∎; §3.1 (the (H8′) row + the tombstone at ELEVEN); §3.2 (the retirement row's residue/owner sentences beyond the rewritten one, the O-11 provenance bracket, the end-to-end row); §3.4 item 12 + the verdict paragraph; §6's dispatch bracket (NEW at this revision); the footer (NEW closing record). Dated historical displays (§0's REVISION-10..17 standing-paragraph interiors, §6's dated brackets, §§7–23) untouched by design | §§0/1, §3.2, §6, footer |

**The completeness invariant, re-stated (unchanged in substance from §23).**
§3.1 has **ELEVEN rows — (ROOT-C)'s (H1)–(H9) minus the retired (H8), plus
(H8′)** (historical letters kept). Per-clause attribution, displayed
IDENTICALLY at §1, Step 16, Step 18's δ-stable note, and Step 19: (0)
unconditional; (SQ) unconditional; clause (R) consumes (H1)–(H3) + (H4a) +
the (K3-δ) fragment of (H4b) + (H5) + (H6) + (H7) + (H8′) + (H9); clause
(UB) consumes (H1)–(H3) + (H4a) + (H4b) + (H5) + (H6) + (H7) + (H8′) + (H9).
The retired (K-CUT) package enters both clauses at Step 18's K1 as
machine-proved content; its residues enter as (H8′) — nothing rides an owner
label, and no display any longer SAYS otherwise.

**The honest acceptance statement (the counter).** The acceptance was EARNED
on the REVISION-12 text (VC14 + VC15), RE-ANCHORED on REVISION 13 by VC16
and on REVISION 15 by VC18. VC19 re-opened the composition question on the
REVISION-16 wording; REVISION 17 re-priced the residues; VC20 CONFIRMED the
re-pricing's substance and blocked only on the two leftover sentences;
REVISION 18 fixes them. **The confirming end-to-end pass (VC21) is OWED as
the anchor on the REVISION-18 wording.** Until VC21 lands clean, the
acceptance citation points at the REVISION-15 text; a VC21 blocker would
re-open the composition question in full (the standing suspension
discipline).

**VC21 charge (the confirming pass).** Verify (i) the two r19 repairs: §1's
retired-(H8) bracket reads ELEVEN with the (H8′) pointer, and the §3.2
retirement row speaks with one voice — proved conditional on the three
interfaces, priced at (H8′), owner arcs routes only, no surviving
owner-housing clause; (ii) the r19-3 audit claim — no operative display
anywhere counts TEN or prices the residues at owners; (iii) NO REGRESSION on
the VC20-confirmed items ((H8′) faithfulness, the per-clause attributions,
Step 18's K1 consumption, the walk, acyclicity, the honesty riders, the
K-CUT package + retirement record).

**Bookkeeping.** Historical §§7–23 untouched. `docs/PROJECT_STATE.md` sync
owed at the next state-file touch. The commit closing this revision is its
unit of record (unit ROOT r19).

— REVISION 18 complete. VC20 recorded (COMPOSITION-GAPS, two leftover
sentences; the (H8′) substance CONFIRMED); both sentences repaired in place;
ELEVEN honest rows; VC21 is owed as the confirming anchor on this text.

---

## 25. REVISION 19 (r20, the (H9) = (NSF) retirement) — disposition of VC21 + the H9-C3 execution

**The pass.** VC21 (the confirming end-to-end pass, fresh context;
`lean/notes/openmath/VC21_report.md`) RAN on the REVISION-18 text and
returned **COMPOSITION-SOUND, zero blockers** — the two r19 repairs present,
the ELEVEN-row roster consistent, (H8′) the sole hypothesis home for the
three K-CUT interfaces, the walk closing at (ROOT-C), acyclicity intact
(incl. 17 ≺ 18 ≺ 18b), no regression — with ONE advisory: the §3.2
end-to-end row's tail stopped at VC19/REVISION 17 (repaired at this
revision). **The acceptance RE-ANCHORS on the REVISION-18 text.**

**The retirement (ledger entry H9-C3, commit 9abc3ab).** The (K1-CANON)
discharge landed as the (ed-1) M04 §5.5 scope pin — (a) K1's quantifier
gains the (q-4) scope parenthetical, (b) Theorem 4's post-K4 sum display is
pinned to the SAME corpus, (c) a §5.5 preamble line records the pin; Lemma
3.3 untouched — executed under Asvin's STANDING statement-change authority
and **SURFACED FOR REVIEW** (exact diffs at the warrant note
`lean/notes/openmath/K1CANON_discharge_2026-08-02.md` §D); gates g-A..g-D
closed (2-round hostile pass: substance PASS on all six charged checks BOTH
rounds, bookkeeping findings folded; companion compile gate
`NsfKeyProp.lean` EXIT=0). With it, (NSF-INT-RET)'s four grounds are ALL on
file (I-1 + I-2 + I-3 + the discharge): **(NSF-INT) is RETIRED**; (NSF-KEY)
was retired at REVISION 13, r14 — **(H9)'s open-member count goes ONE →
ZERO**, and this revision executes the handed-back spec (the discharge unit
edited ROOT not at all; ledger: "the row moves to a retirement record at the
next ROOT unit").

**Disposition table.**

| item | source | grade | disposition | edit sites |
|---|---|---|---|---|
| r20-1 | E-r20-1 (the §1 (ROOT-C) (H9) block) | STATUS SYNC (the retirement) | the (H9) member display REWRITTEN to the RETIRED bracket — both members' retirement grounds named ((NSF-KEY) at Unit C/r14; (NSF-INT) at H9-C3 with I-1/I-2/I-3 + the (K1-CANON) discharge), the RE-OPEN TRIGGER displayed (the D-11 leaf bar), the honesty bounds carried ((CH-RES)-relative closure; no carrier non-vacuity; vt_surj residue); the row LEAVES the open-hypothesis enumeration — clause (R) and clause (UB) each LOSE (H9); the (ROOT)-discharge-distance sentence moves (H9) OFF the distance (trigger-subject); §1's (H8) retired bracket re-synced to TEN (it had said ELEVEN — the VC20-blocker-1 genre, caught by this revision's audit) | §1 (five sub-edits) |
| r20-2 | E-r20-2 (the §3.1 (H9) row) | ROW RETIREMENT (the THIRD post-acceptance: D-12r, (H8), now (H9)) | the row MOVED to the NEW **§3.2 H9 = (NSF) retirement row**, provenance kept in full (the TARGET statement; both members' complete records incl. (NSF-KEY)'s KNOWN-FALSE provenance and the six R-0..R-5 artifacts; the (q-1)–(q-6)-context discipline; the withdrawn inferences); the "until that discharge, (NSF-INT) stands PRICED" clause REPLACED by the executed record (the four grounds + the H9-C3 adjudication); the closing display rule STRUCK and replaced — the target IS dischargeable and discharged at the pinned scope, SUBJECT TO the displayed bounds ((CH-RES); non-vacuity; vt_surj; the carried marks-vocabulary premise) and the RE-OPEN TRIGGER, homes unchanged; a §3.1 tombstone keeps the one-home trail | §3.1 (row out + tombstone); §3.2 (new row) |
| r20-3 | E-r20-3 (Step 18 K1/K4 + Step 17 M3) | RIPPLE SYNC | the (H9) price DROPPED from the Consumes/Delivers lines; each site keeps the historical pointer + the M04 §5.5 pin cite (K1's line carries the full discharge record incl. bounds + trigger; K4's line the executed (NSF-INT)(ii)/pin record; Step 17's M3 leg, Consumes summary, and Delivers line the retired-at-the-pin form) | Step 18 (three displays); Step 17 (three displays) |
| r20-4 | E-r20-4 (clause-(R)/(UB) rosters + the 6(iii)–(ix)+7 residue list) | RIPPLE SYNC | (H9) REMOVED from: §1's clause-(R) and clause-(UB) attributions, Step 18's δ-stable "— §1" quote, Step 19's evaluation-chain closer / Σ_τ conditional set / clause-(UB) scope / final ∎, Step 16's (UB) scope display, and the §3.4 6(iii)–(ix)+7 residue list + verdict paragraph — each with a dated retirement bracket, none silently | §1, Steps 16/18/19, §3.4 |
| r20-5 | E-r20-5 (the §3.2 D-11 [V] row) | VERIFIED + SYNC | NO "(NSF-INT) priced" leak survives in the row; the leaf-bar hygiene item STAYS — and is now DISPLAYED as the (H9) re-open trigger (a §4B-defective finding re-opens I-3 and suspends the retirement); the row's "(H9) members sit in §3.1" tail re-pointed to the retirement row | §3.2 D-11 row |
| r20-6 | VC21 advisory 1 (the stale end-to-end tail) | ADVISORY — ACCEPTED, repaired | the §3.2 end-to-end row's tail extended: VC20 → REVISION 18 recorded, VC21 (COMPOSITION-SOUND, re-anchor on REVISION 18) recorded, VC22 dispatched on this REVISION-19 text | §3.2 end-to-end row |
| r20-7 | THE ENUMERATION AUDIT (the standing VC13 lesson, re-run at this retirement) | RIPPLE SYNC | full-document sweeps of "(H9)", "(NSF-INT)", "(K1-CANON)", "ELEVEN", and "TEN rows": every hit classified. OPERATIVE sites, all in the retired/pointer form after this revision's 23 touched displays — §0's headline bracket + scope tail + the NEW current-standing paragraph; §1's (H9) block, clause-(R), clause-(UB), discharge-distance sentence, and (H8)-bracket count (the ONE stale operative count found, fixed at r20-1); Step 16; Step 17 (×3); Step 18 (×3); Step 19 (×4); §3.1 (row out, tombstone, (H8)-tombstone count); §3.2 (D-11 row, NEW retirement row, end-to-end tail); §3.4 (M3 pointer, residue list, verdict paragraph); §6's NEW dispatch bracket; the footer close. HISTORICAL/RETIREMENT-RECORD sites, untouched by design: §0's REVISION-10..18 dated standing-paragraph interiors, §6's dated brackets, §§7–24, and the §3.2 retirement rows' provenance interiors. FALSIFIER (the spec's): post-edit grep of "(K1-CANON)" hits ONLY historical sections and executed-discharge/retirement-record displays — PASS | whole document |

**The re-derived row count (the r13 audit rule — counted, not copied).**
Counting method: the §3.1 table's data rows (lines starting `| **`, header
and separator excluded) enumerated directly after the edits. THE COUNT IS
**TEN**: (H1) · (FRESH) = (H2) · (R1)–(R4) = (H4a) · (ADM) [(H3) member] ·
(UB-X) = (H4b) · (PACK) = (H5) · the (H6) terminal-seam group · (I-τ)/K-LOC
= (H7) · (H8′) · (E-N) [(H3) member]. Bijection: **TEN rows ↔ (ROOT-C)'s
(H1)–(H9) minus the retired (H8) and (H9), plus (H8′)**, with (H3) =
{(ADM), (E-N)} at two rows. This MATCHES the handed-back spec's expected
ELEVEN → TEN; no discrepancy.

**The completeness invariant, re-run.** Per-clause attribution, displayed
IDENTICALLY at §1, Step 16, Step 18's δ-stable note, and Step 19: (0)
unconditional; (SQ) unconditional; clause (R) consumes (H1)–(H3) + (H4a) +
the (K3-δ) fragment of (H4b) + (H5) + (H6) + (H7) + (H8′); clause (UB)
consumes (H1)–(H3) + (H4a) + (H4b) + (H5) + (H6) + (H7) + (H8′). The
retired (NSF) target enters Step 18's K1/K4 and Step 17's M3 leg as
DISCHARGED content at the pinned scope; the retired (K-CUT) package enters
Step 18's K1 as machine-proved content with its residues at (H8′). No
operative display reads a narrower or wider set.

**The re-open trigger (displayed at §1, §3.1's tombstone, the §3.2
retirement row, the §3.2 D-11 row, and §6).** The D-11 leaf's own acceptance
bar still owes one confirming clean pass on its REVISION-3 text. If that
pass finds the §4B leaf proof defective, I-3 re-opens and the (NSF-INT)
retirement SUSPENDS — (H9) returns to §3.1 and this revision's roster edits
revert.

**The honest acceptance statement (the counter).** The acceptance was
EARNED on the REVISION-12 text (VC14 + VC15), RE-ANCHORED on REVISION 13 by
VC16, on REVISION 15 by VC18, and on REVISION 18 by VC21. REVISION 19 IS a
text change: **the counter RESETS — the confirming end-to-end pass (VC22)
is OWED as the anchor on the REVISION-19 wording** and runs as phase 2 of
this workflow. Until VC22 lands clean, the acceptance citation points at
the REVISION-18 text; a VC22 blocker would re-open the composition question
in full (the standing suspension discipline).

**VC22 charge (the confirming pass).** Verify (i) the (H9) retirement's
faithfulness: the §3.2 retirement row states BOTH members' retirement
grounds exactly as the H9-C3 ledger entry + the warrant note display them,
with the re-open trigger and every honesty bound ((CH-RES); non-vacuity;
vt_surj; the carried marks-vocabulary premise) displayed, nothing weakened;
(ii) the TEN-row roster and the per-clause attributions ((R)/(UB) each lose
exactly (H9), nothing else moved); (iii) the r20-7 audit claim — no
operative display anywhere counts ELEVEN or consumes (H9) as an open
hypothesis; (iv) Step 18's K1/K4 and Step 17's M3 leg consume the retired
target at the pinned scope with the M04 §5.5 pin cited; (v) the VC21
advisory repair (the end-to-end tail current through VC21); (vi) NO
REGRESSION on the VC21-confirmed items ((H8′), the K-CUT record, the walk,
acyclicity, D-12r/NSF-KEY/PACK/(SL≥2)/(0)-(SQ)/Σ_τ).

**Bookkeeping.** Historical §§7–24 untouched. `docs/PROJECT_STATE.md`
synced at this unit (E-r20-6). The commit closing this revision is its unit
of record (unit ROOT r20).

— REVISION 19 complete. VC21 recorded (COMPOSITION-SOUND — the acceptance
re-anchored on REVISION 18); the (H9) = (NSF) row RETIRED to §3.2 on ledger
H9-C3 (both members; the (K1-CANON) discharge executed and surfaced); TEN
honest rows, recounted; the re-open trigger displayed; VC22 is owed as the
confirming anchor on this text.

## 26. REVISION 20 (r21, the leaf-status sync batch) — the window's five ledger discharges folded

**What this revision is.** NO end-to-end pass ran this window (VC22 stands
owed and ROLLS FORWARD onto this text); NO roster change; NO clause
attribution, step proof content, or leaf pointer moved. REVISION 20 folds
the window's five ledger-recorded events (all in
`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`) into the standing
(H1)/(H2)/(H6) rows as dated status brackets — every touched row STAYS [M];
nothing is discharged; every upgrade cites its commit and declarations.

**Disposition table.**

| item | ledger ground | grade | disposition | edit sites |
|---|---|---|---|---|
| r21-1 | "(GR-B) RE-ACCEPTED at REVISION 14 — the full incident arc closed" (commit 58534d4) | LEAF STATUS SYNC — member (a)'s execution of record | the (H1) row's notes cell gains the (a)-member bracket: the GR-B leaf RE-ACCEPTED at its REVISION 14 (21-pass arc; the Def-3.15 extraction-artifact incident — caught by the N-1 harness BECAUSE both prior clean passes had pin-checked the same corrupted extraction — corrected at leaf REVISIONS 11–14, passes 20+21 CLEAN; N-1 bar MET 15/15 GREEN; corrected dictionary λ^GMN = −E_i·λ^FGMN of record); residue taxa displayed (TR-3′-GEN two strata / TR-3-ORD-X re-based / D-ITER / TR-4 empirical debt; TR-3-ORD + D-REAL proved-but-scoped; (KP_i)/(H-CHAIN)/[T] external); member (a) = pinned-and-WRITTEN riding the leaf's [V] record, NOT discharged. STALE-DISPLAY SWEEP (the artifact correction's ROOT ripple): grep gamma_2 / discrepancy / ǫ(epsilon) over the whole document — ZERO ROOT displays ever quoted the leaf's old S3.2 discrepancy law (the two grep hits are unrelated: the §5 numerics table's "ε = 0" cell; §25's "no discrepancy" count sentence); nothing to repair | §3.1 (H1) row |
| r21-2 | "B-M2 (LIFT-CONF) ACCEPTED — the 14-pass arc closed" (commit 416c213) | LEAF STATUS SYNC — member (b)'s engine-lift conformance face | the same (H1) bracket displays B-M2 ACCEPTED at its REVISION 14: the compiled Lean-core perimeter (`Scaffold/HDischarge/H1/{Conformance,RecParamE,RCWitness}.lean`) proves actual-pair (K0)-conformance on BOTH carrier classes — the `k0_actual_of_recentering` hspecRec-transport + the REC-WIRE-G width-law derivation (`recentering_natDegree_eq` + `recParam_eg_of_recentering`, the fiat unusable by type) + the H₃rc interior-recentering witness; the note's opens STAND (GRADED-READ / REC-SL proper / SITE-EXH / the `readsOf_landing_K0` recentering-branch witness debt); KP-STEP's application to fired lifts stays CONDITIONAL | §3.1 (H1) row |
| r21-3 | "H1-U14 LANDED … gate-positive" (commit 6df3eae) | GATED [M]-ROW STATED | the (H1) bracket records `FaithfulTotal` (the TOTAL FGMN Cor 5.15 faithfulness row) STATED in `H1/SiteBlock.lean` §U14 behind its T8-decided N-1 gate (0/2,400; the gate licenses STATING, not asserting; ǫ-caveat discharged — Cor 5.15 checked ǫ-free against the corrected Def-3.15 reading), with proved Lean-core consumers `faithful_at_of_total` / `faithful_total_consume`; displayed + consumed, NEVER proved; supplier = the GRB leaf's future Lean face | §3.1 (H1) row |
| r21-4 | "H2-U9 LANDED: the (TRANS-DEEP) row + the (FRESH-RED) assembly" (commit 23d2c03) | GATED [M]-ROW STATED + PROVED CONSUMERS | the (FRESH) row's notes cell gains the H2-U9 bracket: `TransDeep` STATED (displayed + consumed, not proved) + `fresh_of_transDeep` / `freshRowOn_of_transDeep` PROVED (Lean-core); the B2-FINAL gate SATISFIED by the accepted §B2-DEF supersession (blueprint §1: organizational, not logical); dual statement gate PASSED (Codex R3 ACCEPT 0/0; Fable PASS); `leakFree` bare-Prop placeholder + typed carrier OWED at U10; `huni` rides the consuming theorem, never the row | §3.1 (FRESH) row |
| r21-5 | "III-S4 REPAIRED … H6-A4b LANDED" (commit f56a536) | EXTERNAL-BLOCKER CLEARED + [M]-hypothesis row added at the leaf | the (H6) row's notes cell gains the III-S4/A4b bracket: the refuted `cu2t_readForcing` display (WAS FALSE AS DISPLAYED at the S4Refute countermodel — historical record untouched) REPAIRED at option (b): `TerminalPolySem` (NEW named semantic hypothesis row, NO supplier at HEAD) + `cu2t_readForcing_sem` (+ `hf : f.Monic` priced) with POSITIVE/NEGATIVE compiled gates (the old countermodel fails the row); H6-A4b `terminalDatumD_slope_spec` LANDED verbatim (`H6/Emission.lean`, per-file gates green, Lean-core) — the A4b external blocker CLEARED; the (H6) row's members stay [M] (the repair states a hypothesis, it does not supply one) | §3.1 (H6) row |
| r21-6 | housekeeping (the review-surface items) | TRACKING DISPLAY | ROOT has NO pending-review section (by design — the H9 precedent tracks "SURFACED for review" in-row); per the spec, the ledger note + the standing queue suffice; the three items (the `hf` Monic pricing, the (ed-1) M04 §5.5 pin, the `TerminalPolySem` supplier) are NAMED at the §0 REVISION-20 standing paragraph, the (H6) row bracket, and the §6 dispatch bracket — no new section invented | §0, §3.1 (H6), §6 |

**THE ENUMERATION AUDIT (the r13 rule, run over the post-edit text).**
Full-document greps, every hit classified (OPERATIVE = a live display this
revision must keep consistent; NEW = added by this revision; HISTORICAL =
dated record, untouched by design):

| grep | hits | classification |
|---|---|---|
| `B-M2` / `LIFT-CONF` | 4/3 | ALL NEW (the §0 standing paragraph, the (H1) row bracket, the §3.2 end-to-end tail, the §6 bracket) — no pre-existing site displayed B-M2, so none is stale |
| `U14` / `H2-U9` / `TRANS-DEEP`/`TransDeep` | 4/4/3 | ALL NEW (same four site classes) — no pre-existing site |
| `III-S4` / `A4b` / `TerminalPolySem` | 5/6/5 | ALL NEW — ROOT had NO A4b blocker display before this revision (the blocker lived at the H6 blueprint + the ledger); the (H6) row bracket now records blocker + clearance together |
| `REC-WIRE-G` | 2 | ALL NEW (§0 paragraph; (H1) row bracket) — displayed as DISCHARGED inside B-M2's record, exactly per the leaf's REVISION 12 |
| `(GR-B)` | 55 | 2 NEW ((H1) bracket, §6 bracket); the REST pre-existing and STILL CORRECT — every one displays the (GR-B) HYPOTHESIS (open, §3.1) or a dated historical record; the leaf acceptance verifies the (a)-face's write-up, it does NOT discharge the hypothesis, so no pre-existing (GR-B) display moves |
| `gamma_2` / `discrepancy` / `ǫ`/`epsilon` (the old S3.2 law sweep) | 0 operative | the two hits are unrelated (§5 numerics "ε = 0"; §25 "no discrepancy") — ROOT never quoted the leaf's retired discrepancy law; NOTHING to repair (r21-1) |
| `TWELVE rows` / `ELEVEN rows` / `TEN rows` (the old-count words) | — | every operative count display reads TEN ((§0 standing paragraphs current + superseded-marked; §1's bracket; the §3.1 header count at its REVISION-19 sync; §25's recount; the NEW §0/§26 recounts); ELEVEN/TWELVE survive ONLY inside dated historical paragraphs and revision records — correct by the r20-7 precedent |

**FALSIFIER (the spec's).** Post-edit grep `FALSE AS DISPLAYED` — exactly
ONE hit in the operative text (the (H6) row's NEW bracket, in the
explicitly historical clause "HAD been refuted (FALSE AS DISPLAYED at the
S4Refute countermodel; historical record kept untouched)", immediately
followed by the REPAIRED record); the only other hits are THIS §26
record's own quotations of that clause. No operative display asserts the
refuted form as current — **PASS**.

**The re-derived row count (the r13 audit rule — counted, not copied).**
The §3.1 table's data rows (lines starting `| **`, header and separator
excluded) enumerated directly after the edits. THE COUNT IS **TEN**: (H1) ·
(FRESH) = (H2) · (R1)–(R4) = (H4a) · (ADM) [(H3) member] · (UB-X) = (H4b) ·
(PACK) = (H5) · the (H6) terminal-seam group · (I-τ)/K-LOC = (H7) · (H8′) ·
(E-N) [(H3) member]. Bijection unchanged: **TEN rows ↔ (ROOT-C)'s (H1)–(H9)
minus the retired (H8) and (H9), plus (H8′)**, with (H3) = {(ADM), (E-N)}
at two rows. This MATCHES the expected UNCHANGED TEN (no group closed this
window); no discrepancy.

**The honest acceptance statement (the counter).** The acceptance was
EARNED on the REVISION-12 text (VC14 + VC15), RE-ANCHORED on REVISION 13 by
VC16, on REVISION 15 by VC18, and on REVISION 18 by VC21. VC22 has NOT run;
REVISION 20 IS a text change: **the confirming end-to-end pass (VC22) ROLLS
FORWARD and is OWED as the anchor on the REVISION-20 wording**. Until it
lands clean, the acceptance citation points at the REVISION-18 text.

**VC22 charge (rolled forward from §25, extended).** All six §25 charge
items UNCHANGED, PLUS: (vii) the r21 brackets' faithfulness — the
(H1)/(FRESH)/(H6) row brackets state the five ledger events exactly as the
ledger's entries display them (acceptance revisions, commits, opens/residue
lists, the never-proved status of `FaithfulTotal`/`TransDeep`/
`TerminalPolySem`), with NO row status silently upgraded beyond the
ledger's records; (viii) the r21 enumeration-audit claims (the grep table
above) hold on the text as anchored.

**Bookkeeping.** Historical §§7–25 untouched. `docs/PROJECT_STATE.md`
synced at this unit. The commit closing this revision is its unit of
record (unit ROOT r21).

— REVISION 20 complete. Five ledger discharges folded ((GR-B) re-accepted;
B-M2 accepted; H1-U14 stated; H2-U9 landed; III-S4 repaired + H6-A4b
landed); TEN honest rows, recounted; the enumeration audit run and
recorded; the III-S4 falsifier PASS; VC22 rolls forward as the confirming
anchor on this text.

---

## 27. REVISION 21 (r22, the VC22 re-anchor + the (H4a) F-chain record) — retirement fired and honestly NOT executed

**What this revision is.** VC22 CONFIRMED (ledger 2026-08-08) — the
acceptance RE-ANCHORS on the REVISION-20 text; this revision syncs the
§0/§6/§3.2 counter and anchor lines per the doc-unit discipline. It then
records the H4 window per the ledger's 2026-08-08 entries and EXECUTES the
blueprint `lean/blueprints/HDISCHARGE_H4.md` §1 clause F-(iii) (the ROOT-owner
firing of P(O-8b/M14) on the now-unconditional composite, per the H4M7 §2(iii)
drafted charge). The pass returned GAPS-ONLY, so clause F-(iv) — the (H4a)
row-leaves-enumeration step — is **NOT executed** (the H4M7 §2 firing
condition "P(O-8b/M14) returns ACCEPT (per the standing discipline, after
consecutive clean passes)" is unmet; the pass-1 ledger entry itself pinned
"a second CLEAN composite pass" before retirement). NO roster change, NO
clause-attribution change; the §3.1 roster RECOUNTED at **TEN rows**.

**Disposition table.**

| item | ledger ground | grade | disposition | edit sites |
|---|---|---|---|---|
| r22-1 | "VC22 CONFIRMED — the ROOT acceptance RE-ANCHORS at REVISION 20" (2026-08-08) | ACCEPTANCE RE-ANCHOR | the §0 standing paragraph, the §3.2 end-to-end row tail, and the §6 bracket record VC22 COMPOSITION-SOUND (TEN rows recounted; 8 honesty spot-checks faithful; one metadata-only advisory — the (GR-B) 55/2-NEW vs 54/3 line-site counts); acceptance citation moves REVISION 18 → REVISION 20; VC23 dispatched on this REVISION-21 text, VC24 named as the pass after it in the cadence | §0, §3.2 end-to-end row, §6 |
| r22-2 | the five F-(i) entries (M1 ACCEPTED passes 2+3; M2 DONE pass 5 CLEAN on REVISION 4; M4 rev 4; M5 textual bar passes 6+7; N-K3 ALL GREEN 5fdf350) + the pass-1 entry (4aae1b5) | F-(iii) EXECUTED THIS UNIT; F-(iv) BLOCKED | P(O-8b/M14) composite pass 2 FIRED (Codex, fresh context, high effort, self-contained 177,703-byte prompt; the CURRENT M1-accepted/M2-REVISION-4 texts; report `lean/notes/openmath/PO8bM14_pass2_2026-08-02.md`): **GAPS-ONLY, 0 CRITICAL / 1 GAP** — M14 Lemma 1(b)'s top-space scale display ("γ* = γ/e_{j+1} at the top") STALE against M1's ACCEPTED two-case display (γ* := γ at the top space, no e_{K+1}; M1's own pass-1 F2 CRITICAL, cured in M1, never synced into M14); walk targets (b)–(g) otherwise CLEAN incl. the M2-REVISION-4 composition seam and the counting-layer non-consumption. RETIREMENT-READY-PENDING-CLEAN-PASS displayed at the (H4a) row; owed: the M14 Lemma-1(b) sync fold (O-8b/M14 note owner) + one clean composite pass; on ACCEPT the row retires per H4M7 §3 with the (K3-δ) audit note SURVIVING onto the (UB-X) row | §3.1 (R1)–(R4) row, §3.2 P(O-8b/M14) row |
| r22-3 | "TR3-S1 ACCEPTED (passes 2+3 CLEAN, model-diverse)" + the composition entry (549e8a6) | LEAF STATUS SYNC — (H1)(a) residue re-pricing | the (H1) row gains the TR3-S1 stratum-served bracket: on the order-2/e₀ = 1 stratum σ₁(R^F₂(f)) = R_λ(f) EXACTLY — TR-3′-GEN(i) there a THEOREM, TR-3-ORD-X there PROVED mod (KP_i)+[T], three consumer families served incl. B-M2's OL-1(b) TR-3-ORD-X seam; orders ≥ 3 / e₀ ≥ 2 / general σ_j stay OPEN; the row stays [M]. The B-M2 OL-1(b) seam note itself is NOT displayed at ROOT (the (H1) B-M2 bracket lists GRADED-READ/REC-SL/SITE-EXH/readsOf opens only) — no second edit site; the annotation names the seam inside the (H1) bracket | §3.1 (H1) row |
| r22-4 | the M4 rev-4 / M5 / N-K3 entries | ADJUDICATION STATUS DISPLAY — nothing discharged | the (UB-X) row gains the Route-B status bracket (M4 recommendation ROUTE B; M5's (RB) textual bar MET; N-K3 376/376 with no M4 branch fired); the re-scope stays gated on H4M7 §4's five conditions incl. Asvin statement-change sign-off + the consumer-need walk — member (b) and (K3-δ) stand AS WORDED | §3.1 (UB-X) row |
| r22-5 | the B2DEF charter + wave-E entries (blueprint frozen 90b4072; wave E landed 1c10fe6) + the H4 pass-report archive | TRACKING DISPLAY (§6 dispatch bracket) | the B2D Lean campaign and the H4 pass-report archive pointers added to the §6 in-flight list; no status claim beyond the ledger's | §6 |

**THE ENUMERATION AUDIT (the r13 rule, run over the post-edit text).**
Full-document greps, every hit classified (line-site unit, per the VC22
advisory's convention; counts taken on the FINAL text of this revision, so
they include this record's own quotations):

| grep | hits | classification |
|---|---|---|
| `(R1)–(R4)` | 59 | 1 OPERATIVE ROW (the §3.1 row, live [M] — consistent with F-(iv) NOT executed); ~6 NEW (this record + the F-chain/P-row brackets + §0/§6); the REST pre-existing operative-or-historical displays of the LIVE hypothesis (Step 2/16/18/19 consumption lines, completeness invariants, revision records) — all still correct BECAUSE the row did not retire; zero display claims retirement (grep "(R1)–(R4) RETIRED" = 0) |
| `(H4a)` | 106 | same shape: live-hypothesis displays + dated records + the NEW brackets; no site upgrades (H4a) beyond PROVED-UNVERIFIED-at-the-pass-record; the retirement is displayed only as READY-PENDING-CLEAN-PASS (conditional future) |
| `K3-δ` | 76 | the audit note stays ON the (H4a) row (it moves to (UB-X) only at F-(iv)); clause (R)'s attribution lines still consume (K3-δ) everywhere — unchanged by design; the NEW (UB-X) bracket adds the Route-B status without re-scoping |
| `TR3-S1` | 10 | ALL NEW (the (H1) bracket, §0, §6, this record) — no pre-existing site |
| `P(O-8b/M14)` | 20 | the §3.2 row (record appended), the (H4a) row brackets, §0/§6, + pre-existing dated mentions (VC13 walk, §3.3/§3.4, revision records) — all consistent: fired, pass 1 clean (pre-F-(i)), pass 2 gaps-only, ACCEPT owed |
| `TEN rows` / `ELEVEN rows` / `TWELVE rows` | 28/20/11 | every OPERATIVE count display reads TEN (§0 current paragraph, §1 bracket, the §3.2 end-to-end tail, §25/§26/§27 recounts); ELEVEN/TWELVE and the superseded TEN paragraphs survive only inside dated historical records — the r20-7 precedent. `NINE rows` = 1, this table's own cell — no display counts NINE (correct: no retirement) |
| `VC22` / `VC23` / `VC24` | 36/9/7 | VC22 now recorded CONFIRMED at the four operative sites (§0, §3.2 tail, §6, this record); VC23/VC24 appear only as the NEW dispatch lineage |

**FALSIFIER (the spec's): post-edit grep `(R1)` operative displays vs the
executed disposition.** The executed disposition is NON-retirement. Checked:
(i) the §3.1 (R1)–(R4) row is present and [M] (roster count 10, row 3 of the
enumerated table); (ii) `(H4a) RETIRED` / `(R1)–(R4) RETIRED` — ZERO hits;
(iii) every new bracket states the retirement as PENDING, never as executed;
(iv) no Step 16/18/19 display was upgraded to PROVED-VERIFIED (the H4M7 §3
item-3 delta applies only at F-(iv), which did not fire) — **PASS**.

**The re-derived row count (the r13 audit rule — counted, not copied).**
The §3.1 table's data rows enumerated directly after the edits. THE COUNT IS
**TEN**: (H1) · (FRESH) = (H2) · (R1)–(R4) = (H4a) · (ADM) [(H3) member] ·
(UB-X) = (H4b) · (PACK) = (H5) · the (H6) terminal-seam group · (I-τ)/K-LOC
= (H7) · (H8′) · (E-N) [(H3) member]. Bijection unchanged: TEN rows ↔
(ROOT-C)'s (H1)–(H9) minus the retired (H8) and (H9), plus (H8′), with (H3)
= {(ADM), (E-N)} at two rows. The batch's charge anticipated NINE on full
retirement; the recount rule wins: retirement did not execute, the count is
TEN — no discrepancy with the text as edited.

**VC23 charge (rolled forward from §26, extended).** All §26 charge items
(i)–(viii) as inherited, PLUS: (ix) the r22 brackets' faithfulness — the
(H4a) F-chain record, the P(O-8b/M14) two-pass record (pass 2 = GAPS-ONLY,
0C/1G, the M14 top-space display seam), the TR3-S1 stratum annotation, and
the (UB-X) Route-B status state the ledger's entries exactly, with the
retirement displayed ONLY as pending and (K3-δ) still consumed by clause
(R); (x) the enumeration-audit claims above hold on the text as anchored.

**Bookkeeping.** Historical §§7–26 untouched except the three row-cell
brackets and the two counter-line tails named above. The pass-2 report
`lean/notes/openmath/PO8bM14_pass2_2026-08-02.md` is written but rides the
next notes commit (this unit's commit is scoped to the two docs).
`docs/PROJECT_STATE.md` synced at this unit. The commit closing this
revision is its unit of record (unit ROOT r22).

— REVISION 21 complete. VC22 CONFIRMED folded (acceptance re-anchored on
REVISION 20; VC23 owed on this text, VC24 next in the cadence); the (H4a)
F-chain recorded COMPLETE at F-(i) with P(O-8b/M14) pass 2 fired GAPS-ONLY
and the retirement honestly NOT executed (READY-PENDING-CLEAN-PASS); TR3-S1
stratum service annotated; the (UB-X) Route-B status displayed; B2D/H4
campaign pointers tracked; TEN honest rows, recounted; the enumeration
audit run and recorded; the (R1) falsifier PASS.

## 28. REVISION 22 (r23, the (H4a) retirement + the 2026-08-08 campaign batch) — F-(iv) EXECUTED

**What this revision is.** The ledger's 2026-08-08 batch (commits
355c1eb → 5a10256) synced in one revision. The headline: **the (H4a) =
(R1)–(R4) retirement chain COMPLETED and F-(iv) FIRED** — the fourth
post-acceptance row retirement, executed per blueprint HDISCHARGE_H4 §1 +
H4M7 §§2–3 + the r22 RETIREMENT-READY display. The §3.1 roster moves TEN →
**NINE rows** (recounted below). Clause attribution changes: (R)/(UB) each
LOSE (H4a); (K3-δ) stays consumed by clause (R), its audit note MOVED to the
(UB-X) row. Five further ledger folds ride the same revision (SITE-EXH,
GRADED-READ/B2D, V9 e′ ≥ 2, the strata probe, B-M2 REVISION 15) — all
status-display syncs inside standing rows; no other roster change.

**Disposition table.**

| item | ledger ground | grade | disposition | edit sites |
|---|---|---|---|---|
| r23-1 | "P(O-8b/M14) COMPOSITE ACCEPTED — the (H4a) F-chain COMPLETE" (5a10256; passes 3 = 80635f0 Codex 0C/0G on M14 REVISION 1, 4 = Fable decider `PO8bM14_pass4_2026-08-02.md`, nothing classifiable; the M14 fold at c152602/fb59459) | **F-(iv) EXECUTED** | the §3.1 (R1)–(R4) row DELETED from the table → NEW §3.2 (H4a) = (R1)–(R4) retirement row (full F-chain provenance + displayed bounds) + §3.1 tombstone; the (K3-δ) audit note MOVED to the (UB-X) row (survives, still clause-(R)-consumed); §0 headline + (ROOT-C) (H4a) entry rewritten to RETIRED form; clause (R)/(UB) attribution lines, Step 16's status/consumes, Step 18's K3-c note, Step 19's closer + final ∎ all drop (H4a) with dated brackets; the §3.2 P(O-8b/M14) pass row DISCHARGED into the retirement record; roster counts TEN → NINE at every operative site | §0, §1, Steps 16/18/19, §3.1 table + tombstone, §3.2 ×2, §6 |
| r23-2 | SITE-EXH arc: 355c1eb (probe NEEDS-EXTENSION) → 49b778e (Asvin sign-off) → f36ab72 (E-1/E-2/E-4; two-layer discharge, `siteExh_elimination'` unconditional; Codex pre-commit pass folded; STANDING RE-RATIFICATION item) → db47e35 (E-3 glue; `gate_ftie_chain` end-to-end; `junc_bundle`/`DIterJunctionSupplier` residuals) | LEAF-STATUS SYNC — (H1)(b) open re-priced | the (H1) row's r23 bracket displays the full arc: SITE-EXH DISCHARGED AT THE ENGINE FACE (two-layer as-built) + f-tied coverage; `TowerMoveClosureL` re-ratification PENDING ASVIN displayed; named residuals displayed; row stays [M] | §3.1 (H1) row, §6 |
| r23-3 | a5911fd (GR wave 2a: `graded_read_of_history`, `readHyps_e_eq_one`, GR-3/GR-4 open-obstructed) + 9a10109/a1e14ef/d770cf3/eaa1a5f/5371139 (the TD arc: front CLOSED sorry-free; `transDeep_of_ledger` + `td_transDeep`) + 942becd (CL5 PROVED) | LEAF-STATUS SYNC — (H1)(b)/(FRESH) opens re-priced | GRADED-READ displayed DISCHARGED at interior coherent [NR] reads (M-parametric; vacuous-at-steep scoping; abstract-frame residue OPEN-OBSTRUCTED) on the (H1) row; the (FRESH) row displays the COMPILED ledger-instance TransDeep supplier (machine-checked interface fill; `huni` still displayed; general-instance supplier + Step-10/K1 face still owed); both rows stay [M] | §3.1 (H1)/(FRESH) rows, §6 |
| r23-4 | d632305 (V9 e′ ≥ 2 RULING: VACUITY-RESCOPE; HK53_e2Ruling.lean 8 thms Lean-core; trace F1–F5 green) | ADJUDICATION DISPLAY — engine-internal, no §2 step consumes it | the §3.3 O-14a row's parked "≥ 2 disposition" bracket ADJUDICATED: the FALSE-SUSPECT mapping RETIRED, `readsOf_e2_s0_zero_empty` kills the HK23 family, the V9 sorries stand byte-unchanged behind ONE pinned open lemma (conspiracy exclusion), the corrected strFrame(i+2) display compiled | §3.3 O-14a row |
| r23-5 | 8db1b00 (strata probe: TWIST-LAW confirmed 0/3,640; D-ITER cyclic FALSE 22/28; ITER-LAW measured 0/804) | FALSIFIER RESULT — residue re-description, nothing proved/discharged | the (H1) row's r23 bracket re-describes member (a)'s residue rows: TR-3′-GEN(ii) exact closed-form shape ON RECORD (the lemma stays open); D-ITER to be RESTATED to the two-constant iterated form, the cyclic form fenced out of any Lean kernel (`DIterJunctionSupplier` docstring carries the fence) | §3.1 (H1) row, §6 |
| r23-6 | 38d98f6 (B-M2 REVISION 15: post-acceptance annotation fold, acceptance NOT reset, counter 2/2; census 214 → 226; `readsOf_landing_K0` debt NARROWED) | ACCEPTED-NOTE ANNOTATION — no acceptance history rewritten | the (H1) row's r23 bracket carries the four re-priced opens exactly as the note displays them (GRADED-READ discharged-at-scope / REC-SL unchanged open / SITE-EXH discharged-at-engine-face / the readsOf debt narrowed with the two remaining routes named) | §3.1 (H1) row |
| r23-7 | cadence: VC22 CONFIRMED stands as the anchor; VC23/VC24 named at r22, never fired | CADENCE ROLL-FORWARD | VC23/VC24 recorded NAMED-BUT-UNRUN (superseded by this revision before dispatch); **VC25 owed on the REVISION-22 wording** (this batch's phase-2 confirming pass), VC26 next; the acceptance citation stays at REVISION 20 until VC25 lands | §0, §3.2 end-to-end row, §6 |

**THE ENUMERATION AUDIT (the r13 rule, run over the post-edit text).**
Full-document greps, every hit classified. **Unit: OCCURRENCE COUNTS**
(`grep -o | wc -l`, NOT the line-site unit — the r22/VC24 lesson: the VC22
advisory showed line-site and occurrence counts diverge, so the unit is now
DECLARED), taken on the FINAL text of this revision (they include this
record's own quotations):

| grep (occurrences) | hits | classification |
|---|---|---|
| `(R1)–(R4)` | 98 | 1 OPERATIVE ROW = the §3.2 retirement row (+ its §3.1 tombstone + the P-row closure + the §0/§1/§6/§28 retirement brackets, all NEW and all stating RETIRED/ACCEPT); the REST are dated historical displays (revision records §§7–27, step-history brackets) — every OPERATIVE (non-historical) site now reads the row as RETIRED-at-the-ACCEPT-record; grep for the §3.1 table-row form (line-anchored `| **(R1)–(R4)**`) = 0 — the row is GONE from the table |
| `(H4a)` | 161 | the live-hypothesis displays are ALL retired-form (the §1 entry, the attribution lines, Steps 16/18/19, the ∎); the rest dated records; no site consumes (H4a) as a live hypothesis |
| `K3-δ` | 107 | the audit note's ONE home is now the (UB-X) row (moved); clause (R)'s attribution lines all still consume (K3-δ) — unchanged by design (the Route-B re-scope did NOT fire); zero sites claim (K3-δ) retired |
| `SITE-EXH` | 19 | the (H1)-row bracket + §0/§6/§28 (NEW, the discharged-at-engine-face form with residuals) + pre-existing dated records; zero sites claim SITE-EXH closed beyond the engine face |
| `GRADED-READ` | 15 | same shape: the discharged-at-interior-[NR] display sites (NEW) + dated records; the abstract-frame residue displayed at every new site |
| `TransDeep` + `TRANS-DEEP` | 15 | the (FRESH)-row bracket displays the compiled supplier at ledger-instance scope; the row stays [M]; zero sites claim (FRESH) discharged |
| `TEN rows` / `NINE rows` | 28 / 9 | every OPERATIVE count display reads NINE (§0 current paragraph, the §1 brackets ×3, the tombstones, the §3.2 end-to-end tail, §6, this record); TEN survives only inside dated historical records (the §§22–27 recounts, the r20/r21/r22 paragraphs) — the r20-7 precedent |
| `VC25` / `VC23` | 12 / 17 | VC25 appears only as the NEW dispatch lineage (owed on REVISION 22); every VC23 site is either a dated r22-era record or the NEW named-but-unrun disposition |

**FALSIFIER (the spec's): post-edit grep `(R1)–(R4)` operative =
retirement-record only.** Checked on the final text: (i) the §3.1 table
carries NO (R1)–(R4) row (grep for the table-row prefix = 0 hits); (ii) the
ONLY operative (R1)–(R4) homes are the §3.2 retirement row, its §3.1
tombstone, and the dated retirement brackets — each states RETIRED at the
ACCEPT record, none consumes the row as a live hypothesis; (iii) the r22
RETIREMENT-READY-PENDING-CLEAN-PASS display survives only inside dated
records (§27, the r22 row-history quotes), each now bracketed by the
executed r23 disposition; (iv) Steps 16/18/19 read the (R1)–(R4) leg as
PROVED-VERIFIED at the record, with (K3-δ) + the (H1)-group conditionality
untouched — **PASS**.

**The re-derived row count (the r13 audit rule — counted, not copied).**
The §3.1 table's data rows enumerated directly after the edits. THE COUNT IS
**NINE**: (H1) · (FRESH) = (H2) · (ADM) [(H3) member] · (UB-X) = (H4b) ·
(PACK) = (H5) · the (H6) terminal-seam group · (I-τ)/K-LOC = (H7) · (H8′) ·
(E-N) [(H3) member]. Bijection: NINE rows ↔ (ROOT-C)'s (H1)–(H9) minus the
retired (H4a), (H8), and (H9), plus (H8′), with (H3) = {(ADM), (E-N)} at two
rows and the (H4) letter surviving only as (H4b). The batch's charge
anticipated TEN → NINE; the recount rule wins and agrees: **NINE**.

**VC25 charge.** All §27 charge items as inherited, PLUS: (xi) the r23
brackets' faithfulness — the §3.2 (H4a) retirement row states the ledger's
F-chain and ACCEPT record exactly; the (K3-δ) note's move preserves its
content verbatim-in-content and clause (R) still consumes it; the NINE-row
recount and bijection hold; Steps 16/18/19's upgrades touch ONLY the
(R1)–(R4) leg; (xii) the SITE-EXH/GRADED-READ/(FRESH)/V9/strata/B-M2
brackets state their ledger entries exactly, with every "discharged" claim
scoped (engine face / interior-[NR] / ledger-instance) and every residual
named; (xiii) the enumeration-audit claims above hold on the text as
anchored, at the OCCURRENCE unit as declared.

**Bookkeeping.** Historical §§7–27 untouched except the row-cell brackets,
counter-line tails, and count syncs named in the disposition table.
`docs/PROJECT_STATE.md` synced at this unit (new r23 checkpoint). The commit
closing this revision is its unit of record (unit ROOT r23; scoped to the
two docs).

— REVISION 22 complete. F-(iv) EXECUTED at the P(O-8b/M14) ACCEPT (passes
3+4 clean, model-diverse): the (H4a) = (R1)–(R4) row retired to §3.2 with
provenance, the (K3-δ) audit note moved to (UB-X) and still
clause-(R)-consumed, NINE honest rows recounted; the SITE-EXH arc,
GRADED-READ/B2D, V9 e′ ≥ 2, strata-probe, and B-M2 REVISION-15 folds
displayed at their rows; the enumeration audit run at the declared
occurrence unit; the (R1)–(R4) falsifier PASS; VC25 owed on this text
(VC23/VC24 named-but-unrun; VC26 next).

## 29. REVISION 23 (r24, the VC25 disposition + the GR ROUTE (a) / TR3-S2 folds) — VC25 NOT-CONFIRMED, all four findings disposed

**What this revision is.** The r24 cadence event. **VC25 RAN on the
REVISION-22 wording and returned NOT-CONFIRMED — 1 critical + 3 gaps**
(report `lean/notes/openmath/VC25_report.md`, archived at ea9f615). The
audit's positive half REPRODUCED everything countable: the §28 occurrence
census exact at all ten patterns, the NINE-row roster recount, the clause
(R)/(UB) 8-entry recounts, the 20-step/9-leaf/13-charge-item enumerations.
Its negative half caught that the (H4a) retirement had NOT been propagated
into three OPERATIVE displays — VC25 stopped certifying the Step-19 close
there, so **the acceptance anchor STAYS at REVISION 20/VC22** and this
revision's whole first half is the finding-by-finding disposition. The
second half folds the window's ledger events (GR ROUTE (a), TR3-S2, F6
hygiene). NO roster change: **NINE rows** stand.

**FLAG FOR ASVIN — a consequential statement change surfaced after the
fact per the standing authority.** GR ROUTE (a) (commit 5b7f1ee, executed
per the pricing note `GR34_DISPOSITION_2026-08-08.md` @ 205e3f8) is a
fence-(vii) LEAN STATEMENT CHANGE adjudicated under your standing
statement-change authority (2026-08-05): `ReadHyps` gained the frame-pin
FIELDS `hσs : ν.σ.s = 1` / `hσt : ν.σ.t = 0` (+ pass-through binders on
three suppliers; 4 signature diffs total), and the three frozen GR-3/GR-4
sorry statements were thereby discharged as one-liners — i.e. the
abstract-σ.e ≥ 2-frame scope of those statements is RETIRED from the
interface rather than proved. The decisive facts, re-verified at
execution: ZERO Lean consumers (docstring mentions only; no module
imports the two home files; nothing under OM/ or the capstone path
imports ANY B2D module) and the statements believed FALSE at the bound
scope (the ψ = φ(z^σ.e) gap countermodel, wave-2a record). B2D census
3 → **0 sorries**, all footprints Lean-core; the consumed face
`graded_read_of_history` is signature-UNCHANGED. Review point: whether
you ratify the pin-into-`ReadHyps` reading or want the history-keyed
route (b) reopened.

**Disposition table.**

| item | ground | grade | disposition | edit sites |
|---|---|---|---|---|
| r24-1 | VC25 finding 1 (CRITICAL): "(H4a) remains live in operative dependency displays" — §3.4 Table A item 6(iii)–(ix)+7 still listed "(H4a) + (K3-δ)"; item 9 still routed "= (H4a) + (K3-δ) (§3.1)" (a DANGLING pointer — no (H4a) §3.1 row exists since r23); Step 16 clause (ii) still read "conditional on (R1)–(R4)" | REPAIRED — the retirement propagated | all three sites rewritten so **(K3-δ) stays the LIVE input** (housed at the §3.1 (UB-X) row, clause-(R)-consumed) and **(H4a) appears only under explicit retirement brackets** pointing at the §3.2 (H4a) = (R1)–(R4) retirement row; Step 16 clause (ii) now reads "formerly conditional on (R1)–(R4)" with the discharge routed through the retired row's F-chain and the δ-stable scope + (K3-δ) conditionality explicitly untouched (the VC25-confirmed narrow scope: the upgrade is the (R1)–(R4) leg ONLY) | §3.4 Table A ×2, Step 16 |
| r24-2 | VC25 gap 2: "F-(i) five records" does not reproduce — six named records at the declared unit | REPAIRED — count corrected five → **SIX** | the §3.2 retirement row now displays "**SIX records**: H4-M1 · H4-M2 · H4-M4 · H4-M5 · N-T4 · N-K3"; the §0 r23 paragraph's "five records" carries a dated correction bracket; provenance of the under-count displayed (the ledger's "F-(i) five records ✔" summary folded the two numerics records N-T4 + N-K3 into one entry; the r22-era "five entries" enumerations stand as dated history) | §0, §3.2 retirement row |
| r24-3 | VC25 gap 3: the granular F-chain pointers (`H4M1_rpack_attempt.md`, `H4M2_dict_attempt.md`, `H4M4_k3delta_adjudication.md`, `H4M5_k3delta_attempt.md`, the N-T4 report, commits 80635f0 / c152602 / fb59459 / 5a10256) were not independently resolvable from the ledger tail pasted into VC25's prompt | RECORDED — an AUDIT-INPUT artifact, not a document defect | the orchestrator RE-VERIFIED every pointer in-repo before this revision: all four attempt files exist at `lean/notes/openmath/`, and all four commits resolve (`git log -1` each: 80635f0 = composite pass 3 CLEAN; c152602 = pass-2 report archive; fb59459 = M14 REVISION 1; 5a10256 = COMPOSITE ACCEPTED). No document edit needed beyond this record; future VC prompts should carry the file listing + commit resolutions when the ledger tail alone is supplied | §29 (this record) |
| r24-4 | VC25 gap 4: the "— ROOT ASSEMBLY complete, 2026-08-02. Composed at HEAD d730669 + … (O4T pass 2, untracked)" close was an UNMARKED stale current-state display | REPAIRED — marked HISTORICAL | a **[HISTORICAL CLOSE]** bracket now precedes the paragraph (matching the style of the other superseded dated passages): kept verbatim as the head of the §6 provenance narrative, explicitly NOT current standing; the current-state pointers are the §0 REVISION-23 paragraph + this record | §6 close |
| r24-5 | synthesis-pass-5 F6 dead-weight item: the r22-era "next wave 1 = TD-0 + TD-3 + CL5…" pointer in §6's r22 bracket read as a live queue | REPAIRED — marked superseded | inline **[STALE POINTER]** bracket: those waves all executed and landed (TD front sorry-free, CL5 proved, GR wave 2a + ROUTE (a); B2D 0 sorries); the current queue is `docs/SYNTHESIS_PASS5_2026-08-08.md`'s re-ranked list | §6 r22 bracket |
| r24-6 | ledger 2026-08-08 "GR ROUTE (a) EXECUTED" (commit 5b7f1ee; pricing note `GR34_DISPOSITION_2026-08-08.md` @ 205e3f8) | LEAF-STATUS SYNC + **STATEMENT-CHANGE FLAG** (see the Asvin flag above) | the (H1) row's GRADED-READ display re-priced: the abstract σ.e ≥ 2 frame residue **RETIRED as a fence-(vii) statement change** (`ReadHyps` frame pins; zero consumers + believed-false at the bound scope; standing authority 2026-08-05); **B2D census 3 → 0 sorries** (grep-verified, all Lean-core); `graded_read_of_history` signature-UNCHANGED; the F5 stage-forge GR leg DECLINED (the forge re-prices against the junc_bundle/readsOf need only); row stays [M] (members (a)/(c)/(d) + the non-GR (b) residue unchanged) | §0, §3.1 (H1) row, §6 |
| r24-7 | TR3-S2 attempt (commit 330cc6b, note `lean/notes/openmath/TR3S2_2026-08-08.md`) + hostile pass 1 (commit 1bd75de) | ATTEMPT ON RECORD — acceptance bar NOT met, NO status upgrade | the (H1) row's member-(a) residue bracket displays: TR-3′-GEN(ii) at order 2 proved-informal in the §3.4-REV-11 two-stratum form (Lemma A-GEN + the Bézout EXP kernel; v2 falsifiers 11,748 samples, 0 violations); the units-only record phrasing REFUTED on (ii); the ORD-INV state-key bifurcation finding displayed; hostile pass 1 returned **CRITICAL (1C/2G** — the core compose chain S2–S5 verifies; the critical is the universal key-divergence scope claim**)**, REVISION 1 dispatched, bar reset — the lemma's row status UNCHANGED | §0, §3.1 (H1) row |
| r24-8 | cadence + hygiene: F6 batch commit 112b9ac; VC25 report archived ea9f615 | CADENCE ROLL-FORWARD | VC25 was NOT-CONFIRMED, so the acceptance citation **STAYS at the REVISION-20 text (VC22 CONFIRMED)** — no re-anchor fires at r24; the owed confirming pass rolls forward onto THIS text: **VC26 is OWED on the REVISION-23 wording** (charge below; model diversity per the tree §4 bar) | §0, §3.2 end-to-end row, §6 |

**THE ENUMERATION AUDIT (the r13 rule, run over the post-edit text).**
Unit: OCCURRENCE COUNTS (`grep -o | wc -l`, the r23-declared unit), taken
on the FINAL text of this revision (they include this record's own
quotations). Every delta vs the r23 census is EXPECTED and its cause
named — this revision's own edits add occurrences by design (retirement
brackets quote the patterns they retire):

| grep (occurrences) | r23 | r24 | delta | why |
|---|---:|---:|---:|---|
| `(R1)–(R4)` | 98 | 110 | +12 | this revision's own edits, by design: the r24-1 repair brackets quote the pattern they retire (Step 16, §3.4 ×2), and the §0 REVISION-23 paragraph, §3.2 VC25-sync tail, §6 r24 bracket, and this record's disposition/census rows carry the rest — every NEW site states RETIRED/DISCHARGED-at-the-record; the operative reading is unchanged: retirement-record-and-brackets only |
| `(H4a)` | 161 | 186 | +25 | same cause: the r24-1 brackets name (H4a) to state its retirement (§3.4 ×2 sites, multiple mentions each); the §0/§3.2/§6/§29 disposition text quotes VC25's finding and the fix — zero sites consume (H4a) as a live hypothesis (the VC25-critical sites now carry retirement brackets) |
| `K3-δ` | 107 | 122 | +15 | the repaired §3.4 sites now display (K3-δ) as the LIVE input housed at (UB-X) (that is the fix), plus the Step-16 bracket and the §0/§3.2/§29 records of that fix; zero sites claim (K3-δ) retired |
| `SITE-EXH` | 19 | 20 | +1 | no operative change — the +1 is this census table's own pattern cell (the r23 precedent: counts include the record's own quotations) |
| `GRADED-READ` | 15 | 18 | +3 | the (H1)-row r24 bracket + this record's r24-6 row + this table's own cell; every NEW site displays the residue disposition (retired-by-statement-change, not proved) |
| `TransDeep` + `TRANS-DEEP` | 15 | 17 | +2 | no operative change — both hits are this census table's own pattern cells |
| `TEN rows` | 28 | 29 | +1 | historical-only; the +1 is this table's own cell |
| `NINE rows` | 9 | 14 | +5 | the §0 REVISION-23 paragraph, this record's headline/close, and this table's own cell (roster restated NINE, no change) |
| `VC25` | 12 | 45 | +33 | the r24 disposition IS the VC25 record: §0 paragraph, §3.2 sync tail, §6 bracket, and this record's headline/flag/disposition-table/charge rows |
| `VC23` | 17 | 18 | +1 | named-but-unrun history stands; the +1 is this table's own cell |
| `VC26` | 5 | 16 | +11 | NEW dispatch lineage: VC26 owed on THIS text (§0, §3.2, §6, this record); the 5 r23-era sites were the "VC26 next" cadence namings |
| `ROUTE (a)` | 0 | 13 | +13 | NEW marker introduced at r24 for the GR disposition fold (§0, (H1) row, §6, this record); the ledger's lowercase "route (a)" forms are not counted at this exact-occurrence unit |

**The re-derived row count (the r13 audit rule — counted, not copied).**
The §3.1 table's data rows enumerated directly after the edits: (H1) ·
(FRESH) = (H2) · (ADM) [(H3) member] · (UB-X) = (H4b) · (PACK) = (H5) ·
the (H6) terminal-seam group · (I-τ)/K-LOC = (H7) · (H8′) · (E-N) [(H3)
member]. THE COUNT IS **NINE** — unchanged; this revision touches row
CONTENT (the (H1) bracket, the (UB-X)-adjacent §3.4 pointers) but adds and
retires no row.

**VC26 charge.** All §28 charge items as inherited, PLUS: (xiv) the r24-1
repairs — the three formerly-critical sites now read (K3-δ) as the live
input at the (UB-X) home and (H4a) only under retirement brackets whose
§3.2 pointer resolves; NO other operative site consumes (H4a) or
(R1)–(R4) live (the §28 falsifier re-run passes on this text); (xv) the
SIX-record F-(i) display matches the §3.2 retirement row's own
enumeration; (xvi) the GR ROUTE (a) fold states the ledger entry exactly
— a fence-(vii) STATEMENT CHANGE (never "proved"), zero-consumer +
believed-false warrant displayed, B2D 0 sorries, `graded_read_of_history`
signature-unchanged, the Asvin flag visible; (xvii) the TR3-S2 fold claims
NO status upgrade (attempt-on-record; pass 1 CRITICAL displayed); (xviii)
the census above holds at the declared occurrence unit with the stated
delta-reasons. VC26 should receive the in-repo file listing + commit
resolutions alongside the ledger tail (the r24-3 lesson).

**Bookkeeping.** Historical §§7–28 untouched except the dated brackets
and count-correction sites named in the disposition table (§0 r22/r23
paragraphs, §3.2 rows, §6 brackets — all changes are dated additions;
no historical text deleted). `docs/PROJECT_STATE.md` synced at this unit
(new r24 checkpoint paragraph); the ledger entry appended
(`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`, 2026-08-08). The commit
closing this revision is its unit of record (unit ROOT r24; scoped to the
two docs + the ledger).

— REVISION 23 complete. VC25's four findings disposed one by one (the
operative (H4a) propagation completed with (K3-δ) kept live at (UB-X);
SIX F-(i) records; the provenance pointers re-verified in-repo; the stale
close marked historical); GR ROUTE (a) and TR3-S2 folded at their honest
grades (a flagged statement change and an attempt-on-record); NINE rows
recounted; the census re-run at the declared unit with every delta
explained; **VC26 owed on this REVISION-23 text** — until it lands, the
acceptance citation stays at REVISION 20 (VC22 CONFIRMED).

## 30. REVISION 24 (r25, the VC26+VC26b re-anchor + the wave-8 acceptance folds) — the anchor moves to REVISION 23; TR-3′-GEN closes at order 2

**What this revision is.** The r25 cadence event. **VC26 RAN on the
REVISION-23 wording and returned 0 critical / 1 gap** (report
`lean/notes/openmath/VC26_report.md`, archived 565aa28): every countable
claim REPRODUCED — the four VC25 dispositions PASS at their sites, the
whole-operative sweep found ZERO surviving unbracketed (H4a)/(R1)–(R4)
live uses, the full 12-pattern §29 occurrence census reproduced EXACTLY
with every delta's recorded reason, NINE rows, both clause recounts 8/8,
the line-anchored falsifier 0 hits, all fold commits + the §29 ASVIN FLAG
verified displayed. The single gap was an AUDIT-INPUT artifact (the r24-3
genre recurring at ONE pointer): the orchestrator's provenance appendix
omitted N-T4 — not a separate file, but the §N NUMERIC-GATE RECORD
embedded at `H4M1_rpack_attempt.md:1091–1186`. **VC26b — the narrow
confirming pass on the completed appendix (the embedded §N slice pasted
verbatim) — returned CONFIRMED, 0C/0G** (report `VC26b_report.md`,
b6121a5), the auditor additionally re-executing the embedded N-T4 script
independently in-process: 13/13 assertions PASS. Per the ledger
(2026-08-08, "VC26 + VC26b CONFIRMED"): **the acceptance anchor moves
REVISION 20/VC22 → REVISION 23/VC26+VC26b**, and this revision executes
the citation-text update at every operative anchor site (§0, the §3.2
end-to-end row, §6 — the old REVISION-20 citations bracketed with dated
supersessions, per the VC22 precedent). The revision's second half folds
the wave-8 ledger batch (TR3-S2 acceptance, the REC-SL narrowing, the
readsOf restatement, HUNI-V2, the forge re-pricing, two hygiene
dispositions). NO roster change: **NINE rows** stand.

**The §29 ASVIN FLAG — lineage continued (fold 9).** The fence-(vii)
GR ROUTE (a) statement-change flag (§29: `ReadHyps` gained the frame-pin
fields `hσs`/`hσt`; review point — ratify the pin-into-`ReadHyps` reading
or reopen the history-keyed route (b)) STAYS DISPLAYED and is NOT
resolved here. NEW under the same lineage, from this window: the wave-8
REC-SL suppliers CONSUME the `hσt` pin — `RECSL_F2_2026-08-08.md`'s
(SL-dev) leg reads σ.R(−t) = C(−c̃)·T⁰ with the position killed exactly
by `ReadHyps.hσt` (the GR34_DISPOSITION field), and the `RecSLWire`
consumers ride those suppliers — so a route-(a) reversal now ALSO
re-opens the REC-SL supplier chain (the narrowing of fold 3 is
downstream of the flagged pin). Priced for the review, not a new flag.

**Disposition table.**

| item | ground | grade | disposition | edit sites |
|---|---|---|---|---|
| r25-1 | ledger 2026-08-08 "VC26 + VC26b CONFIRMED — the ROOT acceptance RE-ANCHORS at REVISION 23" (commit b6121a5; reports 565aa28 + b6121a5) | ACCEPTANCE RE-ANCHOR — the citation-text update rides this revision per the VC22 precedent | every operative anchor site updated: the §0 REVISION-24 standing paragraph states the new anchor (REVISION 23/VC26+VC26b) with the REVISION-23 paragraph header superseded; the §3.2 end-to-end row gains the VC26+VC26b sync tail (RE-ANCHORS on REVISION 23; VC27 owed on THIS text); the §6 r24 bracket's "stays at the REVISION-20 text (VC22)" sentence carries a dated supersession bracket and the new r25 bracket is current. Historical records (§§7–29) untouched | §0, §3.2 end-to-end row, §6 |
| r25-2 | ledger 2026-08-08 "TR3-S2 ACCEPTED — TR-3′-GEN CLOSES AT ORDER 2" (6663aa4; passes 2 Codex 96d3d9b + 3 Fable 400f000 consecutive CLEAN on `TR3S2_2026-08-08.md` REVISION 1 5844572) | LEAF ACCEPTANCE — member (a) residue re-priced; the row STAYS [M] | the (H1) row's r25 bracket displays: **TR-3′-GEN at ORDER 2 = a two-stratum THEOREM** (TR3-S1 stratum (i) + TR3-S2 stratum (ii), S3.4 REV-11 form); the units-only record phrasing PERMANENTLY REFUTED on (ii) (Lemma WIT, unconditional); ORD-INV (the state-key bifurcation, divergence exactly on the twist-visible set) on record as the convention-independence law; deeper orders ≥ 3 OPEN — no scope creep, member (a) NOT discharged (D-ITER/TR-4 stand); TR-3-ORD-X-on-(ii) displayed PROVED CONDITIONAL on (KP_i)+[T], not promoted. GRB-adjacent display: the GRB note's open-lemma #1 display gains its post-acceptance bracket AT THIS UNIT (`GRB_ORDER2_2026-08-05.md` §S3.4; acceptance counters NOT reset) | §0, §3.1 (H1) row, §6; note bracket A |
| r25-3 | ledger 2026-08-08 "KP-STEP [RC] WIRING (RecSLWire)" (85e11a4) + the F2 suppliers (1adac62; `RECSL_F2_2026-08-08.md`) | COMPILED NARROWING — B-M2's REC-SL open NARROWED-TO two displayed rows, CLOSED at every compiled site | the (H1) row's r25 bracket displays the two surviving rows EXACTLY (RECSL_WIRE §3): **the `hsc` StageCore-provenance row** at successor-having recentering frames on abstract carriers (the RECSL-SIGN carrier — σ.R(−1) = −1 via `signLaw_of_stageCore`; discharged on all engine-built towers T1/S9/S10 and at every compiled site, `score_σ₁rec.core`) + **the `hfence` GR0b recentering-chain fence** (consecutive-[RC] legs, inherited, not sign-related); the KP-STEP `hΦ : IsNodeLift` obligations are compiled-fed at [RC] via `recsl_isNodeLift_of_history_stageCore` and at [NR] from coherence; gates positive on H₃rc (`rc_recSL_hPhi_gate` matching GlueRun's hand-rolled instance, `rc_hlink_gate` both rows discharged); parity ties + (K1)+(K2)/GRADED-READ untouched, as priced. B-M2 note re-priced at its REVISION 17 (note bracket B, this unit; counter NOT reset, stays 2/2) | §0, §3.1 (H1) row, §6; note bracket B |
| r25-4 | ledger 2026-08-08 "B-M2 REVISION 16: the F3 restatement APPLIED" (e7a673a; walk `READSOF_F3_WALK_2026-08-08.md` 878ac17, verdict (a)/NO-CONSUMER) | DEBT RESTATED — the (H1) row's readsOf line updated | the debt of record is RESTATED at the `ReadsOfFrom` face (witnessed by `gate_ftie_chain`/`landingKey_ν₁rcRoot`); the ReadsOf-SPECIES form leaves the opens list as an obligation and re-enters as a FENCE DISPLAY of the dead From→ReadsOf converse (the C-e exhibit); zero term-level consumers; `kp_step`'s slot demands `IsNodeLift`, which the species branch could never feed; the F5 run-forge NOT chartered by this consumer; census row C237 re-verified true as written | §0, §3.1 (H1) row, §6 |
| r25-5 | the HUNI arc: probe 375487c (`HuniProbe.lean`; note `HUNI_PROBE_2026-08-08.md`) + repair 058aaf3 (§REPAIR RECORD; standing statement-change authority, countermodel-pinned) | REFUTED-THEN-REPAIRED — the (FRESH) row's huni face re-keyed v3 → v4 | the (FRESH) row's r25 bracket displays: the v3 face REFUTED at the compiled td2 countermodel (the alphabet seam — single-leaf read caps digits at q₀ < q₀^d; `FreshReadsLaw` failed too, so the CHAIN was re-keyed, not the law re-guarded) → **HUNI-V2 round-4 carrier repair LANDED** (`TDDefsV4`/`LedgerLawfulV4` — 15 v3 clauses, exactly 5 re-keyed to `slotRead`/`TDV4_chain`; `SlotUniformLaw` HOLDS at td2V4, the old z = 0/z = 2 pair now 1 = 1; `FreshReadsLaw` DELIVERED at `td2V4_freshReads`); ALL H2 statements byte-unchanged; v1–v3 records byte-unchanged; **B2D 0 sorries incl. v4**; the huni face is now **displayed-and-satisfiable at the v4 carrier** (`ledger_freshRowOnV4` the live face) and the v3 binder is recorded DEAD (`ledger_freshRowOn`, the round-3 record); huni still rides the consuming theorem, never the row | §0, §3.1 (FRESH) row, §6 |
| r25-6 | synthesis-pass-5 F5 + the window's two DECLINE/DEAD verdicts (GR leg: ROUTE (a) 5b7f1ee; readsOf leg: the F3 walk 878ac17) | FORGE RE-PRICING — residuals display | the F5 deep-instance forge is now priced against **`junc_bundle` ALONE** (the deg f > deg Φ₁ merged-junction gate at choice-built stages, the D-1 wall): the GR stage-forge leg was DECLINED at ROUTE (a) (its only payoff was refuting statements that change deleted from the interface) and the readsOf run-forge leg is DEAD at no-consumer (r25-4). Displayed in the §6 r25 in-flight list and here | §0, §6 |
| r25-7 | synthesis-pass-5 F1(ii): "ADM-U0 triangular repair (known defect, authority on file)" as a live queue target | STALE QUEUE LINE — disposition recorded; NO ROOT edit site exists | the queue line was STALE AT WRITING: the ADM-U0 repair was already executed at H3 Wave 0 (commit e1cf117, 2026-08-01 — "7/7 units; ADM-U0 the sole sign-off gate"). The line is NOT quoted in any ROOT operative text (grep ADM-U0: zero hits outside this record), so no in-document bracket site exists; the disposition lives here and in the §6 r25 bracket's countermodel-batch tail note; the F1 countermodel-first batch's live remainder is POOL-DICT | §30 (this record), §6 |
| r25-8 | the RECSL_WIRE §1 provenance-trace CAUTION (85e11a4) | HYGIENE DISPLAY — doc-debt recorded, Lean NOT edited | the `StageCore` docstring (`Moves/DefsCore.lean` area) cites the RETIRED FAITHLESS MovesGr held units (`L1.baseStage_exists`/`L4.TRANSstage`/`L5.recTRANSRS` — `MovesGr/HeldUnits_certs.lean`/`HeldUnits_L5_cert.lean`); the LIVE chain is HC1 `T1_baseStage`/`S9_transStage`/`S10_recStage`, all carrying `StageCoreL ⊇ StageCore` (the RECSL_WIRE trace). Recorded as a DISPLAYED DOC-DEBT item (§6 r25 in-flight list); the docstring is deliberately NOT edited at this unit (doc-only revision; the fix is a one-line docstring sync riding a future Lean-touching unit) | §30 (this record), §6 |
| r25-9 | cadence | CADENCE ROLL-FORWARD | REVISION 24 is a text change on the newly re-anchored text: the confirming pass **VC27 is OWED on the REVISION-24 wording** (charge below; model diversity per the tree §4 bar); until it lands, the acceptance citation points at the REVISION-23 text | §0, §3.2 end-to-end row, §6 |

**THE ENUMERATION AUDIT (the r13 rule, run over the post-edit text).**
Unit: OCCURRENCE COUNTS (`grep -o | wc -l`, the r23-declared unit), taken
on the FINAL text of this revision (counts include this record's own
quotations — the r23/r24 precedent). The §29 twelve patterns plus FIVE
new markers introduced or promoted at r25 (TR3-S2, REC-SL, RECSL,
HUNI-V2, VC27); every delta vs the r24 census named:

| grep (occurrences) | r24 | r25 | delta | why |
|---|---:|---:|---:|---|
| `(R1)–(R4)` | 110 | 114 | +4 | no operative change — all four new sites QUOTE VC26's zero-surviving-live-uses finding (the §0 REVISION-24 paragraph, the §3.2 VC26 sync tail, this record's opening paragraph, this table's own pattern cell); the operative reading is unchanged: retirement-record-and-brackets only |
| `(H4a)` | 186 | 192 | +6 | same cause and same sites: every new occurrence sits inside "zero surviving unbracketed (H4a)… live uses" quotations of the VC26 report; zero sites consume (H4a) as a live hypothesis |
| `K3-δ` | 122 | 124 | +2 | no operative change — this census table's own pattern cell + this why-cell's restatement that (K3-δ) stays the live (UB-X)-housed clause-(R) input, untouched at r25 |
| `SITE-EXH` | 20 | 22 | +2 | no operative change — this table's own pattern cell + this why-cell's note that the SITE-EXH residuals (junc_bundle etc.) are re-displayed at the forge re-pricing without status change |
| `GRADED-READ` | 18 | 22 | +4 | no operative change — the (H1) r25 bracket's "parity ties + (K1)+(K2)/GRADED-READ untouched" display, the r25-3 row's same clause, this table's pattern cell, and this why-cell quote |
| `TransDeep` + `TRANS-DEEP` | 17 | 23 | +6 | no operative change — the (FRESH) r25 bracket's H2-statements-byte-unchanged display names `TransDeep` once; this table's own pattern cells and this why-cell mention account for the rest, with the fixed-point note below carrying the final two — the v4 twins' decl names (lowercase `transDeep_…` forms) do not hit the exact-occurrence unit |
| `TEN rows` | 29 | 30 | +1 | historical-only; the +1 is this table's own pattern cell |
| `NINE rows` | 14 | 22 | +8 | roster restated NINE, no change — the §0 REVISION-24 paragraph (×2: the VC26 recount quote + the roster restatement), the §3.2 sync tail (×2, same pair), this record's opening/what-this-is/close restatements, and this table's own cell |
| `VC25` | 45 | 52 | +7 | history quoted at the new VC26 sync sites ("all four VC25 dispositions PASS": §0, §3.2, this record's opening) and this table's cells; the VC25 disposition itself is closed history (§29) |
| `VC23` | 18 | 19 | +1 | named-but-unrun history stands; the +1 is this table's own cell |
| `VC26` | 16 | 74 | +58 | the r25 fold IS the VC26/VC26b record (the largest delta by design, the VC25→r24 precedent): the §0 REVISION-24 paragraph, the §3.2 sync tail, the §6 supersession + r25 brackets, and this record's headline/flag/disposition/census/charge rows — the 16 r24-era sites were the "VC26 owed" cadence namings |
| `ROUTE (a)` | 13 | 21 | +8 | the flag-lineage paragraph and the forge re-pricing displays name the executed route (§0, §6, the flag paragraph, the r25-2/r25-6 rows, this table's cells); still zero sites claim route (a) proved anything — it remains the flagged fence-(vii) statement change |
| `TR3-S2` | 9 | 20 | +11 | NEW MARKER PROMOTED at r25 (baseline 9 = the r24 attempt-on-record sites): the acceptance fold adds the §0 paragraph, the (H1) r25 bracket, the §3.2 sync, the §6 r25 bracket, and this record's rows |
| `REC-SL` | 9 | 24 | +15 | NEW MARKER PROMOTED at r25 (baseline 9 = the pre-narrowing open-lists): the narrowing fold adds the §0 paragraph, the (H1) r25 bracket, the §3.2 sync, the §6 r25 bracket, and this record's flag/disposition/charge rows |
| `RECSL` | 0 | 21 | +21 | NEW at r25 — the compiled-unit surname (RECSL-SIGN, `RECSL_suppliers`, the RECSL_F2/RECSL_WIRE note names; the case-sensitive unit does NOT count `RecSLWire`); introduced by the fold displays (§0, (H1) bracket, §6, this record) |
| `HUNI-V2` | 0 | 8 | +8 | NEW at r25 — the v4 repair's name: §0, the §3.2 sync, the (FRESH) r25 bracket, the §6 r25 bracket, this record's r25-5 row + charge + this table's cells |
| `VC27` | 0 | 12 | +12 | NEW dispatch lineage: VC27 owed on THIS text (§0, §3.2, §6, this record's rows + charge) |

Fixed point: the counts above were re-grepped AFTER the last edit of this
revision; the one count-sensitive subtlety is recorded at the
`TransDeep`/`TRANS-DEEP` row (decl-suffixed forms like
`transDeep_of_lawful_v4` do not hit the exact-occurrence unit — lowercase
`transDeep` is a different string; the row counts the exact patterns only).

**The re-derived row count (the r13 audit rule — counted, not copied).**
The §3.1 table's data rows enumerated directly after the edits: (H1) ·
(FRESH) = (H2) · (ADM) [(H3) member] · (UB-X) = (H4b) · (PACK) = (H5) ·
the (H6) terminal-seam group · (I-τ)/K-LOC = (H7) · (H8′) · (E-N) [(H3)
member]. THE COUNT IS **NINE** — unchanged; this revision touches row
CONTENT only (the (H1) and (FRESH) brackets) and adds and retires no row.

**VC27 charge.** All §29 charge items as inherited (with (xvi)–(xviii)
now checked against the r25 wording), PLUS: (xix) the anchor-citation
update is COMPLETE and CONSISTENT — every operative site cites
REVISION 23/VC26+VC26b, the old REVISION-20/VC22 citations survive only
in dated historical records and under dated supersession brackets, and
no operative site still points the acceptance at REVISION 20; (xx) the
TR3-S2 fold claims exactly the ledger grade — TR-3′-GEN closed AT ORDER 2
(two strata), deeper orders open, member (a) NOT discharged, the (H1) row
still [M], TR-3-ORD-X-on-(ii) conditional-displayed; (xxi) the REC-SL
display matches RECSL_WIRE §3 verbatim-in-content — exactly TWO surviving
rows (`hsc` + `hfence`), closed-at-every-compiled-site never inflated to
closed; (xxii) the readsOf restatement claims NO discharge — the debt is
RESTATED at `ReadsOfFrom`, the species form a fence display; (xxiii) the
huni fold displays both halves honestly — v3 REFUTED (the countermodel
kept as a record) and v4 satisfiable-not-supplied, H2 statements
byte-unchanged, huni never moved onto the row; (xxiv) the forge
re-pricing shows `junc_bundle` as the ONLY live forge gate with both
declined/dead legs cited; (xxv) the census above holds at the declared
occurrence unit with the stated delta-reasons, including the five new
markers' baselines. VC27 should receive the in-repo file listing + commit
resolutions + the COMPLETE provenance appendix including embedded-record
pointers (the VC26 lesson: N-T4-style embedded records need their
host-file line ranges supplied).

**Bookkeeping.** Historical §§7–29 untouched except the dated
supersession brackets named in the disposition table (§0 REVISION-23
paragraph header; the §6 r24 bracket's anchor sentence — dated additions,
no historical text deleted). The two note brackets applied at this unit:
(A) `lean/notes/openmath/GRB_ORDER2_2026-08-05.md` — the §S3.4
post-acceptance bracket (open lemma #1 closed at order 2; counters NOT
reset); (B) `lean/notes/openmath/H1_BM2_2026-08-06.md` — REVISION 17
(the REC-SL re-pricing per RECSL_WIRE; REV-15/16 mechanics, additive,
counter stays 2/2). `docs/PROJECT_STATE.md` synced at this unit (new r25
checkpoint paragraph); the ledger entry appended
(`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`, 2026-08-08). The commit
closing this revision is its unit of record (unit ROOT r25; scoped to the
two docs + the two notes + the ledger).

— REVISION 24 complete. The acceptance RE-ANCHORED at REVISION 23
(VC26+VC26b, the first anchor move since VC22); TR-3′-GEN closed at
ORDER 2 as a two-stratum theorem with deeper orders honestly open; REC-SL
narrowed to two displayed rows and closed at every compiled site; the
readsOf debt restated at its consumer-true face; huni refuted-then-
repaired at the v4 carrier with zero signature changes; the forge priced
to one gate; NINE rows recounted; the census re-run at the declared unit
with every delta explained; **VC27 owed on this REVISION-24 text** —
until it lands, the acceptance citation points at the REVISION-23 text.

## 31. REVISION 25 (r26, the VC27+VC27b re-anchor + the wave-9 folds) — the anchor moves to REVISION 24; the countermodel-first window lands

**What this revision is.** The r26 cadence event. **VC27 RAN on the
REVISION-24 wording and returned 0 critical / 2 gaps** (report
`lean/notes/openmath/VC27_report.md`, archived 5e8dc97): ALL substantive
checks PASSED — 3/3 re-anchor sites verified; no self-confirmation; the
TR3-S2 scope discipline; REC-SL's exact two-row display; the readsOf fence
+ no-consumer disposition; the huni v3 refutation + v4 repair both
recorded; the forge displayed as `junc_bundle` alone with both
declined/dead legs' reasons; the §29 ASVIN FLAG standing; the full
17-pattern §30 occurrence census arithmetically exact including VC26's
+58; NINE rows; both clause recounts 8/8; the line-anchored falsifier 0
hits. Both gaps were AUDIT-INPUT appendix defects (the N-T4 §N slice named
but not pasted — the r24-3/VC26 genre recurring a third time; the GRB
bracket slice empty from a wrong grep pattern), now a saved orchestrator
checklist (memory: vc-audit-appendix-checklist). **VC27b — the narrow
confirming pass on the verbatim slices — returned CONFIRMED, 0C/0G**
(report `VC27b_report.md`, 6aa3b20). Per the ledger (2026-08-08, "VC27 +
VC27b CONFIRMED"): **the acceptance anchor moves REVISION 23/VC26+VC26b →
REVISION 24/VC27+VC27b**, and this revision executes the citation-text
update at every operative anchor site (§0, the §3.2 end-to-end row, §6 —
the old REVISION-23 citations bracketed with dated supersessions, per the
VC22 precedent). The revision's second half folds the wave-9 ledger batch
— the five 2026-08-08 entries after the VC27+VC27b entry: POOL-DICT
(d7f7f58), TR-4 (4761cdd), CU2t k′=0 (9cb30d1), leakFree (d237149), D-ITER
(3bcae68) — plus the consolidated forge display. NO roster change: **NINE
rows** stand; `BasePolygonLengthLaw p` enters as a DISPLAYED DictIII-side
open lemma at the (H6) row's residuals, NOT as a §3.1 roster row.

**The §29/§30 ASVIN FLAG — lineage continued, nothing added.** The
fence-(vii) GR ROUTE (a) statement-change flag (§29: `ReadHyps` gained the
frame-pin fields `hσs`/`hσt`; review point — ratify the pin-into-`ReadHyps`
reading or reopen the history-keyed route (b)) and its §30 extension (the
REC-SL suppliers consume the `hσt` pin, so a route-(a) reversal also
re-opens the REC-SL supplier chain) STAY DISPLAYED and are NOT resolved
here; none of the five wave-9 units consumes the pinned fields (POOL-DICT/
CU2t/leakFree are probe- or supplier-class outside the ReadHyps perimeter;
TR-4/D-ITER are numerics/restatement units). The standing Asvin queue is
re-displayed at the §6 r26 bracket: the `TowerMoveClosureL` two-layer
re-ratification, the H4M7 §4 Route-B re-scope, and this flag lineage.

**Disposition table.**

| item | ground | grade | disposition | edit sites |
|---|---|---|---|---|
| r26-1 | ledger 2026-08-08 "VC27 + VC27b CONFIRMED — the ROOT acceptance RE-ANCHORS at REVISION 24" (commit 6aa3b20; reports 5e8dc97 + 6aa3b20) | ACCEPTANCE RE-ANCHOR — the citation-text update rides this revision per the VC22 precedent | every operative anchor site updated: the §0 REVISION-25 standing paragraph states the new anchor (REVISION 24/VC27+VC27b) with the REVISION-24 paragraph header superseded; the §3.2 end-to-end row gains the VC27+VC27b sync tail (RE-ANCHORS on REVISION 24; VC28 owed on THIS text); the §6 r25 bracket's anchor sentence carries a dated supersession bracket and the new r26 bracket is current. Historical records (§§7–30) untouched beyond the named dated brackets | §0, §3.2 end-to-end row, §6 |
| r26-2 | ledger 2026-08-08 "POOL-DICT ATTEMPT" (d7f7f58; `POOLDICT_2026-08-08.md`; `Scaffold/HDischarge/H8/PoolDictProbe.lean` — NEW file/decls, green, Lean-core, zero sorry) | COUNTERMODEL-FIRST DECISION — the derivation route REFUTED compiled; NOTHING discharged, no statement touched | the (H8′) row's r26 bracket displays: two full `KCutK1Tie` instances over ONE shared pack (the REAL n2 ledger carriers, `n2hdet`, a real `BlockDeriv` with `e_key` non-vacuous) agreeing on `eOf`/`τOf`/`vBridge` and differing in `δOf` — the tie holds NON-vacuously at all three chain-Sigmas members via the NEW `pd_blockSolve1_zero` + `powSubst δ 0 = 0`; display `pooldict_delta_underdetermined`; hence NO law over (C, R, hdet) can pin the pool dictionary — genuinely NEW interface content. The (iii) wording STANDS as the honest form, now blocked-on-owners ([2r]/CL-8 REL.2 + (K-CUT-g)) WITH THE BLOCKER COMPILED; δ-leg compiled, τ-leg same-genre NOT compiled (the two-equal-state table = forge-class, fold r26-7); the pass-4 F2a "attempt before treating as blocked" disposition EXECUTED and closed | §0, §3.1 (H8′) row, §6 |
| r26-3 | ledger 2026-08-08 "TR-4 HARNESS EXECUTED — ALL GREEN" (4761cdd; `TR4_HARNESS_2026-08-08.md`; `verification/openmath/tr4_check.py` + output log) | EMPIRICAL DEBT DISCHARGED — NO proof-status change | the (H1) row's r26 bracket displays: the GRB leaf's displayed evidence gap (Cor-4.5/4.11 factorization/bijectivity ↔ NO listed family) closed by the preregistered family TR4a–f (seed + all violation predictions committed BEFORE running; full N-1 roster A–J × 4 base rings; the fixed-γ transcription, coset vanishing both halves, factorization both directions, surjectivity via arbitrary-target realizers, exhaustive-window bijectivity with non-circular w₂-only injectivity): **19,226 samples, 0 violations, ALL GREEN**; TR-4 stays PAPER-SECURED on printed Cor 4.4(1)/4.5/4.11 — a GREEN run is instance evidence, not proof; ORD-INV not engaged (convention-internal, displayed); GRB brackets applied additively at the TR-4 unit (R11.5 + R13.4 + residue-of-record; counters NOT reset) | §0, §3.1 (H1) row, §6 |
| r26-4 | ledger 2026-08-08 "TERMINALPOLYSEM k′=0" (9cb30d1; `CU2T_KP0_2026-08-08.md`; `Scaffold/DictIII/CU2tKp0.lean` — NEW file/decls, green, six audited footprints Lean-core) | CHARTER SPLIT-DECIDED — literal derivability REFUTED compiled + the honest reduction PROVED; NEW NAMED OPEN ROW displayed, roster UNCHANGED | the (H6) row's r26 bracket displays: THREE compiled negative gates (saturation necessary at f = X+1; the −∞ leg unsupplied at f = X, junk e = 0 side passes A6b; the CU2t terminal-appended index TERMINAL-BLIND on `S4Refute.H0`) refute "derivable from the landed emission specs"; the 𝐇°-keyed salvage `terminalPolySem_kp0` PROVED with the positive gate firing at the REAL `gmnDataOrder1` read of X+2 — **the first ReadThroughIota-certified `TerminalPolySem` witness at HEAD**; the face reduces to **`BasePolygonLengthLaw p`** ([M]-discipline; the OM base length law ℓ(N⁻(f)) = ord_X(f̄) + side nondegeneracy — H-free, D-free pure polygon geometry, NOT provable at HEAD, the OL5-adjacent convexity gap, proof route in its docstring) — a DictIII-side open lemma DISPLAYED at the (H6) residuals; it does NOT change the §3.1 hypothesis-group count (NINE rows recounted below) | §0, §3.1 (H6) row, §6 |
| r26-5 | ledger 2026-08-08 "LEAKFREE TYPED" (d237149; `LEAKFREE_2026-08-08.md`; `B2D/LeakFaceV4.lean` — 14 NEW decls, zero sorry, Lean-core, every existing statement byte-unchanged) | TYPED-FACE COMPONENT LANDED — the (FRESH) residuals updated; nothing discharged at the row | the (FRESH) row's r26 bracket displays: the typed carrier `LeakFreeCarrierV4` (TD-5 pair + interior read leaves avoid the parent block; NO counting law — the H2-U9 R2-C1 bar respected) + the display face `read_parentBlind` + **the general supplier `ledger_leakFreeV4`** (every `LedgerLawfulV4` datum) + the compiled de-vacuity fence `td2V4bad_not_leakFreeV4`; the chain's displayed `leakFree` upgraded rfl-INSPECTED → DISCHARGED at builders AND family suppliers (Exists.choose transport); the fired TransDeep rows carry a PROVED (L) field; the H2 `TransDeep.leakFree` FIELD stays bare per the U9 fold (displayed); STILL OWED: the Step-10/K1 classifier consumer face + a nonempty-parent lawful instance (forge-class, fold r26-7) — the (FRESH) general-instance residue retired ONLY in its leakFree typed-face component | §0, §3.1 (FRESH) row, §6 |
| r26-6 | ledger 2026-08-08 "D-ITER RESTATED" (3bcae68; `DITER_RESTATE_2026-08-08.md` §§S1–S4; pin = the strata probe's B3-adj adjudication, kill-shot row N) | COUNTERMODEL-PINNED RESTATEMENT under the standing authority — the (H1) member-(a) residue re-described; open lemma, NO status upgrade | the (H1) row's r26 bracket displays: the single-ζ′ cyclic presentation REFUTED at the canonical dig-consumed alphabet (22/28 mixed towers, two failure genres) and BRACKETED at all display sites (MOVES §F.2 + GRB S5.3/S8, additive; survives only as the e_b = 1 D-REAL stratum + the information-free abstract iso); **the ITER-LAW is the STATEMENT OF RECORD** — c(γ,γ′) = z̄^{δ₂}·z₁^{(s₁-defect + ℓ₀γ₂δ₂)/e₀}, an OPEN LEMMA instance-true at 0/804 over 52 towers, e₀ = 1 leg theorem-backed, CARRY-1/W-MULT fibred proof route displayed, orders ≥ 3 template-only; the `DIterJunctionSupplier` CONSUMABLE SPEC on record (three mandatory features — iterated 𝒟₂ carrier, both-constant cocycle + fibration term, three sanity gates; cyclic kernels fenced; consumption only as a displayed hypothesis until proved); queue #11 UNBLOCKED on #9; the two ROOT "restatement owed"/"to be RESTATED" flags (§0 r23 paragraph, §6 r23 bracket) carry dated EXECUTED brackets | §0 (incl. the r23 flag), §3.1 (H1) row, §6 (incl. the r23 flag) |
| r26-7 | synthesis-pass-5 F5 as re-priced at r25 + the wave-9 τ-leg (r26-2) and nonempty-parent (r26-5) verdicts | FORGE CONSOLIDATION DISPLAY — the target list grows 1 → 3; chartering NOT decided here | the forge-class target list now has **THREE named targets, displayed together as the consolidated forge question**: (1) `junc_bundle` — the surviving F5 justification (the deg f > deg Φ₁ merged-junction gate at choice-built stages, the D-1 wall); (2) the nonempty-parent lawful leakFree instance (all compiled v4 instances have `parentBlock = ∅`); (3) the two-equal-state table (the POOL-DICT τ-leg; every compiled table carries `State ≡ Unit`). The chartering decision (which, if any, to build) = **synthesis pass 6, OWED** — this revision only displays the consolidated question | §0, §6, this record |
| r26-8 | housekeeping (the standing obligations at this window's close) | TRACKING DISPLAY — no ROOT operative change beyond the §6 list | **synthesis pass 6 is OWED** (the /goal cadence; this window's collapses — the small-units dispatch fully executed, the countermodel batch decided, the forge list consolidated — need re-mapping); the standing Asvin items remain displayed: the `TowerMoveClosureL` two-layer re-ratification, the fence-(vii) ROUTE (a) review (§29/§30 flag lineage, restated above), the H4M7 §4 Route-B re-scope; the StageCore docstring doc-debt stands displayed and deliberately un-edited (doc-only revision — the fix rides a future Lean-touching unit) | §6, this record |
| r26-9 | cadence | CADENCE ROLL-FORWARD | REVISION 25 is a text change on the newly re-anchored text: the confirming pass **VC28 is OWED on the REVISION-25 wording** (charge below; model diversity per the tree §4 bar); until it lands, the acceptance citation points at the REVISION-24 text; **never self-confirm** — VC28 is a fresh-context pass, not this unit | §0, §3.2 end-to-end row, §6 |

**THE ENUMERATION AUDIT (the r13 rule, run over the post-edit text).**
Unit: OCCURRENCE COUNTS (`grep -o | wc -l`, exact case-sensitive patterns,
the r23-declared unit), taken on the FINAL text of this revision (counts
include this record's own quotations — the r23/r24/r25 precedent). The §30
seventeen patterns plus SEVEN new markers introduced or promoted at r26
(POOL-DICT, TR-4, BasePolygonLengthLaw, ITER-LAW, the leakFree pair,
VC27b, VC28); every delta vs the r25 census named:

| grep (occurrences) | r25 | r26 | delta | why |
|---|---:|---:|---:|---|
| `(R1)–(R4)` | 114 | 115 | +1 | no operative change — the +1 is this table's own pattern cell; the retirement-record-and-brackets reading stands |
| `(H4a)` | 192 | 194 | +2 | no operative change — this table's own pattern cell + this why-cell's restatement that zero sites consume (H4a) as a live hypothesis |
| `K3-δ` | 124 | 126 | +2 | no operative change — this table's own pattern cell + this why-cell's note that (K3-δ) stays the live (UB-X)-housed clause-(R) input, untouched at r26 |
| `SITE-EXH` | 22 | 24 | +2 | no operative change — this table's own pattern cell + this why-cell's note that the SITE-EXH residuals are re-displayed inside the forge consolidation without status change |
| `GRADED-READ` | 22 | 23 | +1 | no operative change — this table's own pattern cell only; the r25 discharge scope stands untouched |
| `TransDeep` + `TRANS-DEEP` | 23 | 35 | +12 | the leakFree fold names the carrier at every display — §0, the (FRESH) r26 bracket (incl. `TransDeep.leakFree_of_heq` and the bare-field sentence), the §6 bracket, the r26-5 row, this table's cells; suffixed lowercase decl names (`td_transDeep` etc.) do not hit the case-sensitive exact-occurrence unit |
| `TEN rows` | 30 | 31 | +1 | historical-only; the +1 is this table's own pattern cell |
| `NINE rows` | 22 | 31 | +9 | roster restated NINE, no change — the §0 REVISION-25 paragraph, the §3.2 sync tail (the audit-checks quote + the recount), the §6 r26 bracket, this record's headline (the audit-checks quote + the roster restatement), the r26-4 row, the close, and this table's own cell |
| `VC25` | 52 | 53 | +1 | closed history — this table's own pattern cell |
| `VC23` | 19 | 20 | +1 | named-but-unrun history stands; the +1 is this table's own cell |
| `VC26` | 74 | 93 | +19 | history at the moved-anchor displays — every operative re-anchor site spells REVISION 23/VC26+VC26b as the SOURCE of the move (§0, §3.2, §6, this record's headline/charge), plus the appendix-defect genre cite and this table's cells; VC26's disposition is closed history (§30) |
| `ROUTE (a)` | 21 | 25 | +4 | the flag-lineage paragraph and the r26-8 row restate the standing review item (§6 ×1, this record ×3 incl. this cell); still zero sites claim route (a) proved anything |
| `TR3-S2` | 20 | 24 | +4 | closed history quoted at the VC27 substantive-checks lists (§0, the §3.2 sync tail, this record's headline) + this table's own cell — the acceptance itself folded at r25 |
| `REC-SL` | 24 | 30 | +6 | closed-at-r25 status quoted at the VC27 substantive-checks lists (§0, §3.2, this record's headline) + the flag-lineage paragraph's supplier-chain restatement (×2) + this table's own cell; no status change |
| `RECSL` | 21 | 23 | +2 | the §6 r26 bracket's flag-lineage line (`hσt`-pin consumption) + this table's own cell; the case-sensitive unit still does not count `RecSLWire` |
| `HUNI-V2` | 8 | 9 | +1 | the r25 fold is closed history — the +1 is this table's own pattern cell; the VC27 checks quote "huni v3 + v4" without the marker |
| `VC27` | 12 | 81 | +69 | the r26 fold IS the VC27/VC27b record (the largest delta by design, the VC26-fold precedent at r25): the §0 REVISION-25 paragraph, the §3.2 sync tail, the §6 supersession + r26 bracket, and this record's headline/disposition/census/charge rows; the 12 r25-era sites were the "VC27 owed" cadence namings; the exact-substring unit counts every VC27b hit here too — the sub-pattern arithmetic is displayed at the VC27b row and the fixed-point note |
| `VC27b` | 0 | 32 | +32 | NEW at r26 — the narrow confirming pass's name (each hit also counts one VC27 occurrence under the exact-substring unit; the VC27 row's total includes these 32) |
| `VC28` | 0 | 13 | +13 | NEW dispatch lineage: VC28 owed on THIS text (§0, §3.2, §6, this record's rows + charge + these cells) |
| `POOL-DICT` | 25 | 41 | +16 | MARKER PROMOTED at r26 (baseline 25 = the (H8′) row-member displays since r18): the decision fold adds the §0 paragraph, the (H8′) r26 bracket, the §6 r26 bracket, and this record's headline/flag/disposition/charge rows + these cells |
| `TR-4` | 6 | 26 | +20 | MARKER PROMOTED at r26 (baseline 6 = the (H1) residue-taxa displays): the discharge fold adds the §0 paragraph, the (H1) r26 bracket (the full harness display), the §3.2 sync, the §6 r26 bracket, and this record's headline/disposition/charge rows + these cells |
| `BasePolygonLengthLaw` | 0 | 12 | +12 | NEW at r26 — the CU2t unit's named open row: §0, the (H6) r26 bracket, the §3.2 sync, the §6 r26 bracket, this record's headline/disposition/recount/charge rows + this cell |
| `ITER-LAW` | 7 | 20 | +13 | MARKER PROMOTED at r26 (baseline 7 = the r23-era measured-law displays): the restatement fold adds the §0 paragraph + the §0 dated flag bracket, the (H1) r26 bracket, the §6 r26 bracket + the §6 dated flag bracket, this record's disposition/charge rows + these cells |
| `leakFree` | 8 | 43 | +35 | MARKER PROMOTED at r26 (baseline 8 = the U10-twin debt displays): the typed-face fold adds the §0 paragraph, the (FRESH) r26 bracket, the §3.2 sync, the §6 r26 bracket, this record's rows + these cells; the v4 declaration names (`ledger_leakFreeV4`/`td2V4bad_not_leakFreeV4`) hit the unit as exact substrings, which carries most of the delta |
| `LeakFreeCarrierV4` | 0 | 5 | +5 | NEW at r26 — the typed carrier's name: §0, the (FRESH) r26 bracket, the §6 r26 bracket, the r26-5 row, and this table's own cell |

Fixed point: the counts above were re-grepped AFTER the last edit of this
revision; the two count-sensitive subtleties are recorded at their rows —
`VC27b` hits are also `VC27` hits (exact-substring unit; VC27's 81 includes
the 32), and `leakFree` counts substring hits inside the v4 declaration
names (`ledger_leakFreeV4` etc.) while suffixed lowercase `transDeep_…`
decl names do NOT hit the `TransDeep` pattern (case-sensitive).

**The re-derived row count (the r13 audit rule — counted, not copied).**
The §3.1 table's data rows enumerated directly after the edits: (H1) ·
(FRESH) = (H2) · (ADM) [(H3) member] · (UB-X) = (H4b) · (PACK) = (H5) ·
the (H6) terminal-seam group · (I-τ)/K-LOC = (H7) · (H8′) · (E-N) [(H3)
member]. THE COUNT IS **NINE** — unchanged; this revision touches row
CONTENT only (the (H1)/(FRESH)/(H6)/(H8′) brackets) and adds and retires
no row; `BasePolygonLengthLaw p` is a displayed residual at the (H6) row,
NOT a roster row.

**VC28 charge.** All §30 charge items as inherited (with (xix)–(xxv) now
checked against the r26 wording), PLUS: (xxvi) the anchor-citation update
is COMPLETE and CONSISTENT — every operative site cites
REVISION 24/VC27+VC27b, the old REVISION-23/VC26+VC26b citations survive
only in dated historical records and under dated supersession brackets,
and no operative site still points the acceptance at REVISION 23;
(xxvii) the POOL-DICT fold claims exactly the ledger grade — the
derivation route refuted by the compiled underdetermination witness,
NOTHING discharged, the (iii) wording unchanged, blocked-on-owners with
the blocker compiled, the τ-leg displayed as not-compiled/forge-class;
(xxviii) the TR-4 fold claims ONLY the empirical-debt discharge — TR-4
stays paper-secured, no proof-status change, the GRB brackets additive;
(xxix) the CU2t fold displays both halves honestly — the literal charter
REFUTED at three compiled gates AND the reduction PROVED, with
`BasePolygonLengthLaw p` displayed as a DictIII-side open lemma at the
(H6) residuals and the §3.1 roster count UNCHANGED at NINE; (xxx) the
leakFree fold claims the typed-face component only — the H2 field bare,
the classifier consumer face and the nonempty-parent instance displayed
owed; (xxxi) the D-ITER fold never claims the ITER-LAW proved — open
lemma, instance-true, the cyclic form bracketed at every display site
incl. the two dated EXECUTED flags; (xxxii) the forge display names
exactly THREE targets with the chartering decision owed at synthesis
pass 6 — no target silently chartered; (xxxiii) the census above holds at
the declared occurrence unit with the stated delta-reasons, including the
two displayed substring subtleties. VC28 should receive the in-repo file
listing + commit resolutions + the COMPLETE provenance appendix with
VERBATIM slices per the saved checklist (memory:
vc-audit-appendix-checklist — the VC26/VC27 lesson, three first-passes
lost to appendix generation).

**Bookkeeping.** Historical §§7–30 untouched except the dated brackets
named in the disposition table (the §0 REVISION-24 paragraph header; the
§6 r25 bracket's anchor sentence; the two D-ITER EXECUTED flags — dated
additions, no historical text deleted). NO note brackets ride this unit:
the five wave-9 units applied their own note/leaf brackets at land time
(GRB R11.5/R13.4/residue at the TR-4 unit; MOVES §F.2 + GRB S5.3/S8 at
the D-ITER unit) — this unit touches ROOT + `docs/PROJECT_STATE.md` (new
r26 checkpoint paragraph) + the ledger
(`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`, r26-executed entry)
ONLY. The commit closing this revision is its unit of record (unit ROOT
r26; scoped add).

— REVISION 25 complete. The acceptance RE-ANCHORED at REVISION 24
(VC27+VC27b — the whole wave-8 window now inside the certified anchor);
POOL-DICT decided countermodel-first with the blocker compiled; the TR-4
empirical debt discharged at 19,226/0 with no proof-status change; the
CU2t k′=0 charter split-decided — three compiled refutation gates, the
reduction proved, `BasePolygonLengthLaw p` the new named open row
(displayed, not rostered); leakFree typed with a general supplier and a
compiled de-vacuity fence; D-ITER restated at the ITER-LAW statement of
record; the forge consolidated to THREE named targets awaiting the
synthesis-pass-6 chartering decision; NINE rows recounted; the census
re-run at the declared unit with every delta explained; **VC28 owed on
this REVISION-25 text** — until it lands, the acceptance citation points
at the REVISION-24 text.

## 32. REVISION 26 (r27, the VC28 re-anchor + the pass-6 adoption) — the anchor moves to REVISION 25; the forge chartered to junc_bundle alone

**What this revision is.** The r27 cadence event. **VC28 RAN on the
REVISION-25 wording and returned CONFIRMED FIRST-PASS, 0 critical / 0 gaps**
(report `lean/notes/openmath/VC28_report.md`, archived d66bef2) — the FIRST
first-pass confirmation of the cadence: all folds verified at their sites
(anchor citations; POOL-DICT displayed derivation-route-dead with the row
intact; TR-4 empirical-only; BasePolygonLengthLaw displayed NOT rostered,
roster NINE; the leakFree honesty display; the D-ITER open-lemma discipline;
the three-target forge display); the 26-pattern census reproduced exactly
(incl. the VC27/VC27b substring relationship); provenance fully resolved —
the appendix was built per the saved vc-audit-appendix-checklist (every
bracket sliced verbatim, wording verified pre-slice), its FIRST validation
after three first-passes lost to appendix generation. Per the ledger
(2026-08-08, "VC28 CONFIRMED FIRST-PASS"): **the acceptance anchor moves
REVISION 24/VC27+VC27b → REVISION 25/VC28**, and this revision executes the
citation-text update at every operative anchor site (§0, the §3.2
end-to-end row, §6 — the old REVISION-24-anchor displays bracketed with
dated supersessions, per the VC22 precedent). The revision's second half
ADOPTS SYNTHESIS PASS 6 (`docs/SYNTHESIS_PASS6_2026-08-08.md`, commit
976484d; ledger entry "SYNTHESIS PASS 6") as the dispatch of record — the
forge decision (F1), the `BasePolygonLengthLaw` charter (F2), the collapse
map (F3), the row-route review incl. the (ADM) status correction (F4), the
CLEAN pass-5 execution audit (F5, dropped-again count ZERO — the first
clean audit of the cadence), and the census refresh + housekeeping (F6).
NO roster change: **NINE rows** stand; charters change DISPATCH status
only, never proof status — every chartered object remains open until its
unit lands and is audited.

**The §29/§30 ASVIN FLAG — lineage continued, nothing added.** The
fence-(vii) GR ROUTE (a) statement-change flag (§29) and its §30 extension
(the REC-SL suppliers consume the `hσt` pin, so a route-(a) reversal also
re-opens the REC-SL supplier chain) STAY DISPLAYED and are NOT resolved
here; r27 is a doc-only revision — no Lean statement is touched and nothing
consumes the pinned fields. The standing Asvin queue is re-displayed at the
§6 r27 bracket: the `TowerMoveClosureL` two-layer re-ratification, the
fence-(vii) ROUTE (a) ratify-or-reopen (incl. the RECSL `hσt`-pin rider),
and the H4M7 §4 Route-B re-scope — now teed up by the queued (UB-X)
consumer-need walk (pass-6 #8), which is OUR half of the Route-B adoption
gate and is not yet on file.

**Disposition table.**

| item | ground | grade | disposition | edit sites |
|---|---|---|---|---|
| r27-1 | ledger 2026-08-08 "VC28 CONFIRMED FIRST-PASS (0C/0G) — the ROOT acceptance RE-ANCHORS at REVISION 25" (commit d66bef2; report `VC28_report.md` same commit) | ACCEPTANCE RE-ANCHOR — the citation-text update rides this revision per the VC22 precedent | every operative anchor site updated: the §0 REVISION-26 standing paragraph states the new anchor (REVISION 25/VC28) with the REVISION-25 paragraph header superseded; the §3.2 end-to-end row gains the VC28 sync tail (RE-ANCHORS on REVISION 25; VC29 owed on THIS text); the §6 r26 bracket's anchor sentence carries a dated supersession bracket and the new r27 bracket is current. Historical records (§§7–31) untouched beyond the named dated brackets | §0, §3.2 end-to-end row, §6 |
| r27-2 | synthesis pass 6 F1 (976484d) — the forge chartering decision the r26-7 display OWED | FORGE DECISION ADOPTED — one charter, two declines; NOTHING proved, dispatch status only | the three r26-7 targets are three SPECIES, priced separately: **`junc_bundle` CHARTERED ALONE** (price L, staged: the deep compiled deg f > deg Φ₁ run preregistered from strata-probe/BM2_TRACE numerics with the clause-(iii) residual traced numerically BEFORE the Lean build → `gate_glue_junction` un-hypothesized → the `DIterJunctionSupplier` sanity gates with ITER-LAW consumed only as displayed hypothesis) — the only target with a live consumer chain (last SITE-EXH residual; queue-#11 substrate; B-M2's last non-hypothesis-shaped wall); the **nonempty-parent lawful leakFree instance DECLINED** with its disposition displayed at the (FRESH) row (revisit ONLY if the classifier consumer face, once stated, demands it for non-vacuity); the **two-equal-state table DECLINED** with its disposition displayed at the (H8′) row (a second leg of a compiled blocker is not progress; the r26-2 τ-leg display stands as the permanent record); the GR stage-forge stays DECLINED, the readsOf run-forge stays DEAD — neither resurrected | §0, §3.1 (H1)/(FRESH)/(H8′) rows, §6 |
| r27-3 | synthesis pass 6 F2 (976484d) + the CU2t unit's docstring route (9cb30d1) | CHARTER HIGH — the named open row gets a dedicated unit; the law STAYS OPEN | **`BasePolygonLengthLaw p` CHARTERED HIGH** (price M): it IS the recorded convexity gap, honestly priced — H-free, D-free, seam-free pure polygon geometry; route quarried from PROVED kernels (`npHeight_convexOn`, `npVertices_sorted`, `valSupport_facts`, zip-consecutive + `Rat.den` divisibility) with the genuinely new machinery = the negative-prefix telescoping; both perimeter legs countermodel-forced (`Kp0GateA`/`Kp0GateB`); acceptance = `terminalPolySem_kp0_of_law` firing stratum-wide, Kp0 gates green; the −∞ corner stays separately priced; displayed at the (H6) row's r27 bracket, still NOT a §3.1 roster row | §0, §3.1 (H6) row, §6 |
| r27-4 | synthesis pass 6 F3 (976484d; grounds RECSL_WIRE §1, `ResVal.R_neg_one_sq`, TR3S2 §S7, the KP-STEP [RC] wiring) | COLLAPSE MAP ADOPTED — reach re-graded at four faces; no status upgraded | (a) the Step-10/K1 classifier consumer face is **ATTEMPT-GRADE** (the v4 layer supplies the typed object + method; missing = only the Step-10-side statement; one statement-design unit at `td2V4`, fence mandatory — was unpriced-shape); (b) the `hsc` row is **PERMANENTLY hypothesis-shaped — the terminal honest state** (records bundle NO `StageCore`; round-2 fields give only σ.R(−1)² = 1; hypothesis at abstract carriers, closed at every compiled site; optional S two-instance witness folded into passing units, no standalone dispatch); (c) deeper strata **NOT yet finite-algebra** (the ǫ-recurrence can revive at order 3 even on e₀ = 1; entry = a preregistered ORDER-3 all-e_j = 1 probe, compose unit only on an exact law); (d) `junc_bundle` is **B-M2's ONLY non-hypothesis-shaped wall** (B-M2 opens = {hsc, hfence, junc_bundle, K1/K2 parity ties}), concentrating the F1 charter | §0, §3.1 (H1)/(FRESH) rows, §6 |
| r27-5 | synthesis pass 6 F4-(ADM) (976484d; HDISCHARGE_H3 §1.4; the ADM-U0 stale-line lesson at r25-7) | ROW STATUS CORRECTED — blocked → NOT BLOCKED, the adjudicating gate never ran | the (ADM) row's r27 bracket displays: **G-ADM-2** (log dim G_{β_k} vs d at n ≤ 5, p ∈ {2,3,5}) routes track A (prove ADM-REAL over realized shape families) vs track B (the U6a/U6b CEN-W generalization, statement-change-gated) and had NEVER RUN — the row's exact countermodel-first entry; single unblocking artifact = the committed log + a one-page disposition; dispatched at wave 10 with the preregistration SEALED pre-run (4d7dba1) — NO attainment outcome claimed at this revision; U6a/U6b gated on G-ADM-2, not on U0 | §0, §3.1 (ADM) row, §6 |
| r27-6 | synthesis pass 6 F6 (976484d) + the r27 in-repo re-measure (this unit) | CENSUS REFRESH — display sync only | the sorry census of record: **36 sites repo-wide, B2D 0** (pass-6 F6, grep-verified at its HEAD; `lean/` unchanged 976484d → this revision). Re-verified independently at r27 under the strict non-comment unit: **35 sites in `LeanUrat/`** + 1 in the tracked scratch probe `scratch_S7_escalation.lean` (= 36; the dead numeric scratch `scratch_n2rsh.lean` is outside the corpus unit), leaders V9_K1nonrec ×4 / SlotsG2_relFamily ×4 / TV_G3 ×2 / G1_toyGate ×2 and the banked `npVertices_stable_of_hull_preserved` classifier sorry all reproduced exactly; `REMAINING_CENSUS_2026-08-01.md` predates the window and is superseded by this display | §0, §6, this record |
| r27-7 | housekeeping (pass-6 F5/F6 + the wave-10 dispatch event) | TRACKING DISPLAY — no operative change beyond the §6 list | the pass-6 queue is the DISPATCH OF RECORD; **wave 10 IS DISPATCHED** (queue #2–#6 running concurrently with this fold: junc_bundle stage 1 carrying the StageCore docstring sync, BasePolygonLengthLaw, G-ADM-2, the classifier consumer face, the depth-3 probe — results ride r28, nothing from them claimed here); the pass-5 execution audit CLEAN (dropped-again ZERO; the successor rule kept — audit queue lines against the ledger at write time); the StageCore docstring doc-debt ASSIGNED (#2 stage 1, else #5), still deliberately un-edited at this doc-only revision; the Asvin items re-displayed (flag paragraph above) | §6, this record |
| r27-8 | cadence | CADENCE ROLL-FORWARD | REVISION 26 is a text change on the newly re-anchored text: the confirming pass **VC29 is OWED on the REVISION-26 wording** (charge below; model diversity per the tree §4 bar); until it lands, the acceptance citation points at the REVISION-25 text; **never self-confirm** — VC29 is a fresh-context pass, not this unit | §0, §3.2 end-to-end row, §6 |

**THE ENUMERATION AUDIT (the r13 rule, run over the post-edit text).**
Unit: OCCURRENCE COUNTS (`grep -o | wc -l`, exact case-sensitive patterns,
the r23-declared unit), taken on the FINAL text of this revision (counts
include this record's own quotations — the r23/r24/r25/r26 precedent). The
§31 twenty-six patterns plus THREE markers introduced or promoted at r27
(`junc_bundle` PROMOTED at the charter; G-ADM-2 and VC29 NEW); every delta
vs the r26 census named:

| grep (occurrences) | r26 | r27 | delta | why |
|---|---:|---:|---:|---|
| `(R1)–(R4)` | 115 | 116 | +1 | no operative change — the +1 is this table's own pattern cell |
| `(H4a)` | 194 | 196 | +2 | no operative change — this table's own pattern cell + this why-cell's restatement that zero sites consume (H4a) as a live hypothesis |
| `K3-δ` | 126 | 128 | +2 | no operative change to the clause input — this table's own pattern cell + this why-cell; (K3-δ) stays the live (UB-X)-housed clause-(R) input, untouched at r27 |
| `SITE-EXH` | 24 | 30 | +6 | the junc_bundle charter re-displays "the last SITE-EXH residual" as consumer-chain leg (i) — §0, the (H1) r27 bracket, the r27-2 row + this table's row (which names SITE-EXH twice beyond its pattern cell); no arc status change |
| `GRADED-READ` | 23 | 24 | +1 | no operative change — this table's own pattern cell only |
| `TransDeep` + `TRANS-DEEP` | 35 | 39 | +4 | no operative change — the whole +4 is this record's own cells (this row's joint pattern cell + why + the fixed-point note on the case-sensitive TransDeep unit); the (FRESH) r27 bracket names only the v4 carriers; the r26 display stands |
| `TEN rows` | 31 | 32 | +1 | historical-only; the +1 is this table's own pattern cell |
| `NINE rows` | 31 | 36 | +5 | roster restated NINE, no change — the §0 REVISION-26 paragraph, the §3.2 sync tail, this record's roster line and close, and this table's own cell |
| `VC25` | 53 | 54 | +1 | closed history — this table's own pattern cell |
| `VC23` | 20 | 21 | +1 | named-but-unrun history stands; the +1 is this table's own cell |
| `VC26` | 93 | 96 | +3 | closed history — this table's own pattern cell + this why-cell + the fold-precedent naming at the largest-delta row's why; no operative VC26 site changes |
| `ROUTE (a)` | 25 | 30 | +5 | the §0 Asvin-items redisplay, the §6 r27 bracket, and the flag-lineage paragraph (×2) restate the standing review item + this table's own cell; still zero sites claim route (a) proved anything |
| `TR3-S2` | 24 | 26 | +2 | closed history — this table's own pattern cell + the fixed-point note below (the depth-3 entry cites the note as TR3S2, a different exact pattern) |
| `REC-SL` | 30 | 33 | +3 | the flag paragraph's supplier-chain restatement (×2) + this table's own cell; no status change — the two-row narrowing stands with hsc now displayed terminal |
| `RECSL` | 23 | 31 | +8 | the §0/§6 Asvin-item redisplays + the flag paragraph name the `hσt`-pin rider, and the (H1) r27 bracket + the r27-4 ground cite RECSL_WIRE §1 (the RECSL_WIRE filename hits the exact-substring unit) + this cell; the case-sensitive unit still does not count `RecSLWire` |
| `HUNI-V2` | 9 | 10 | +1 | closed history — this table's own pattern cell |
| `VC27` | 81 | 107 | +26 | history at the moved-anchor displays: every operative re-anchor site and charge row spells REVISION 24/VC27+VC27b as the SOURCE of the move (§0, §3.2, §6, this record), and the confirming-pass verified-checks quotes carry the substring-relationship display + these cells; the exact-substring unit counts every VC27b hit here too — the 44 below are all included in the 107 |
| `VC27b` | 32 | 44 | +12 | the moved-anchor spelling at the re-anchor sites + this cell (each hit also counts one VC27 occurrence under the exact-substring unit; the VC27 row's total includes these) |
| `VC28` | 13 | 47 | +34 | the r27 fold IS the VC28 record (the largest delta by design — the VC26-fold and VC27-fold precedents): the §0 REVISION-26 paragraph, the §3.2 sync tail, the §6 supersession + r27 bracket, this record's headline/disposition/census/charge rows; the 13 r26-era sites were the "VC28 owed" cadence namings |
| `VC29` | 0 | 14 | +14 | NEW dispatch lineage: VC29 owed on THIS text (§0, §3.2, §6, this record's rows + charge + these cells) |
| `POOL-DICT` | 41 | 45 | +4 | closed-at-r26 history quoted at the VC28 verified-folds lists (§0, §3.2, this record's headline) + this cell; the decision itself unchanged |
| `TR-4` | 26 | 30 | +4 | closed-at-r26 history quoted at the VC28 verified-folds lists (§0, §3.2, this record's headline) + this cell; paper-secured status unchanged |
| `BasePolygonLengthLaw` | 12 | 27 | +15 | the F2 charter fold: §0 (×2), the §3.2 sync tail (×2), the (H6) r27 bracket, the §6 r27 bracket incl. the wave-10 list (×2), this record's headline/checks/disposition/wave-10/charge/recount/close rows + this cell; the law STAYS OPEN — every new site is charter/dispatch language |
| `ITER-LAW` | 20 | 26 | +6 | the junc_bundle staged-charter displays (the law consumed only as displayed hypothesis — §0, the (H1) r27 bracket, the r27-2 row) + the (H1) attempt-grade line + the §6 queue-#10 line + this cell; open-lemma status unchanged |
| `leakFree` | 43 | 52 | +9 | the F1 decline + F3a displays name the instance and the v4 decl names as exact substrings (§0, the (FRESH) r27 bracket, the §6 r27 bracket, this record's rows + this cell); typed-face status unchanged |
| `LeakFreeCarrierV4` | 5 | 7 | +2 | the (FRESH) r27 bracket's F3a method line + this table's own cell |
| `junc_bundle` | 20 | 40 | +20 | MARKER PROMOTED at r27 (baseline 20 = the F5/forge displays since r24): the charter fold adds the §0 paragraph, the (H1) r27 bracket, the §6 r27 bracket incl. its wave-10 and queue lines, and this record's headline/flag/disposition/census/charge rows and close + these cells |
| `G-ADM-2` | 0 | 14 | +14 | NEW at r27 — the (ADM) row's adjudicating gate: §0, the (ADM) r27 bracket, the §6 r27 bracket (incl. the wave-10 dispatch lines), this record's headline/disposition/charge rows + these cells |

Fixed point: the counts above were re-grepped AFTER the last edit of this
revision; the count-sensitive subtleties recorded at their rows carry over
from §31 — `VC27b` hits are also `VC27` hits and `VC28`/`VC29` are NOT
substrings of each other; `leakFree` counts substring hits inside the v4
declaration names; `TR3S2` (the note's filename form) does NOT hit the
`TR3-S2` pattern; suffixed lowercase decl names do not hit the
case-sensitive `TransDeep` pattern.

**The re-derived row count (the r13 audit rule — counted, not copied).**
The §3.1 table's data rows enumerated directly after the edits: (H1) ·
(FRESH) = (H2) · (ADM) [(H3) member] · (UB-X) = (H4b) · (PACK) = (H5) ·
the (H6) terminal-seam group · (I-τ)/K-LOC = (H7) · (H8′) · (E-N) [(H3)
member]. THE COUNT IS **NINE** — unchanged; this revision touches row
CONTENT only (the (H1)/(FRESH)/(ADM)/(H6)/(H8′) r27 brackets) and adds and
retires no row; `BasePolygonLengthLaw p` remains a displayed residual at
the (H6) row, NOT a roster row, chartered or not.

**VC29 charge.** All §31 charge items as inherited (with (xxvi)–(xxxiii)
now checked against the r27 wording), PLUS: (xxxiv) the anchor-citation
update is COMPLETE and CONSISTENT — every operative site cites
REVISION 25/VC28, the old REVISION-24/VC27+VC27b citations survive only in
dated historical records and under dated supersession brackets, and no
operative site still points the acceptance at REVISION 24; (xxxv) the
forge fold claims exactly the pass-6 grades — `junc_bundle` CHARTERED with
NOTHING proved (dispatch status only), both declines displayed with their
dispositions at the named rows, no target silently chartered or silently
dropped, the GR/readsOf legs still declined/dead; (xxxvi) the
BasePolygonLengthLaw fold is a CHARTER only — the law stays an OPEN lemma
displayed at the (H6) residuals, not rostered, with no proof-status
language; (xxxvii) the (ADM) correction claims only that G-ADM-2 never ran
and is dispatched — NO attainment outcome appears anywhere in the r27
text; (xxxviii) the hsc display claims hypothesis-shape PERMANENCE on the
F3b ground (no StageCore in records; σ.R(−1)² = 1 only) and does NOT claim
any discharge; (xxxix) the census-refresh display matches an at-HEAD grep
(36 repo-wide of record; 35 in `LeanUrat/` under the strict non-comment
unit; B2D 0; the banked classifier sorry standing); (xl) the wave-10
display claims dispatch only — no result from any running unit is consumed
by any r27 display; (xli) the census above holds at the declared
occurrence unit with the stated delta-reasons. VC29 should receive the
in-repo file listing + commit resolutions + the COMPLETE provenance
appendix with VERBATIM slices per the saved checklist (memory:
vc-audit-appendix-checklist — validated at VC28).

**Bookkeeping.** Historical §§7–31 untouched except the dated brackets
named in the disposition table (the §0 REVISION-25 paragraph header; the
§6 r26 bracket's anchor sentence — dated additions, no historical text
deleted). This is a DOC-ONLY revision: no Lean file is touched, no
statement changes, no note brackets ride this unit (the pass-6 file is the
pass's own record; the declined targets' dispositions live at the named
§3.1 rows and in this record). This unit touches ROOT +
`docs/PROJECT_STATE.md` (new r27 checkpoint paragraph) + the ledger
(`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`, r27-executed entry)
ONLY. The commit closing this revision is its unit of record (unit ROOT
r27; scoped add).

— REVISION 26 complete. The acceptance RE-ANCHORED at REVISION 25 (VC28
CONFIRMED FIRST-PASS — the whole wave-9 window now inside the certified
anchor, and the cadence's first zero-finding full audit); synthesis pass 6
ADOPTED as the dispatch of record — the forge chartered to `junc_bundle`
ALONE (staged, numerics-first) with both other targets DECLINED under
displayed dispositions; `BasePolygonLengthLaw p` chartered HIGH and still
open; the collapse map folded (classifier consumer face attempt-grade; hsc
terminally hypothesis-shaped; depth-3 probe-first; junc_bundle B-M2's only
non-hypothesis-shaped wall); the (ADM) row corrected to NOT BLOCKED with
G-ADM-2 dispatched preregistration-sealed; the sorry census refreshed
(36/B2D 0, independently re-verified); wave 10 running with results riding
r28; NINE rows recounted; the census re-run at the declared unit with
every delta explained; **VC29 owed on this REVISION-26 text** — until it
lands, the acceptance citation points at the REVISION-25 text.

## 33. REVISION 27 (r28, the wave-10 fold batch) — BasePolygonLengthLaw PROVED; the forge's first deep runs all green; NO anchor move (VC29 rolls forward)

**What this revision is.** The r28 cadence event: the WAVE-10 FOLD BATCH.
**No verification pass ran at this unit** — VC29 (owed on the REVISION-26
wording since r27) had NOT yet run when this fold landed, so per the
REVISION-20 precedent the owed confirming pass ROLLS FORWARD onto this
REVISION-27 wording, and **the acceptance citation stays at the
REVISION-25 text (VC28)** at every operative site — no anchor-citation
text changes at this revision; this unit ran no audit and NEVER
self-confirms. What it folds: the five wave-10 units dispatched at r27
returned and their results are ledger-recorded — `BasePolygonLengthLaw p`
PROVED (a6fcc4d), FORGE `junc_bundle` stage 1 ALL GREEN (023a9d7 +
53f5d03), G-ADM-2 EXECUTED (38ac01e), the Step-10/K1 classifier consumer
face FIRED (380cc37), the (H7) first probe DECIDED (2c72c5f). NO roster
change: **NINE rows** stand — one fold RETIRES a displayed open-lemma row
(BasePolygonLengthLaw, which was never a §3.1 roster row) to PROVED
status; every other fold changes display/dispatch status only.

**The §29/§30 ASVIN FLAG — lineage continued, nothing added.** The
fence-(vii) GR ROUTE (a) statement-change flag (§29) and its §30 extension
(the REC-SL suppliers consume the `hσt` pin) STAY DISPLAYED and are NOT
resolved here. The wave-10 units touched none of the pinned fields (all
five additive or numeric; zero existing statements changed, per their
ledger entries). The standing Asvin queue is unchanged from the §6 r27
bracket (TowerMoveClosureL; fence-(vii) ROUTE (a) incl. the RECSL
`hσt`-pin rider; the Route-B re-scope awaiting the (UB-X) walk note).

**Disposition table.**

| item | ground | grade | disposition | edit sites |
|---|---|---|---|---|
| r28-1 | ledger 2026-08-08 "BASEPOLYGONLENGTHLAW PROVED" (a6fcc4d; `Scaffold/DictIII/BasePolyLength.lean`; note `BPLL_2026-08-08.md`) | PROVED FOLD — the displayed open row retires to proved status; the (H6) row STAYS [M] | **`BasePolygonLengthLaw p` PROVED for EVERY prime, sorry-free, Lean-core, statement VERBATIM** as displayed at the (H6) residuals since r26: the abstract core `BPLL.npSides_neg_length_sum` (two NEW valid-line constructions; `dropCollinear_between`; the negative-prefix `telescope_sum`) + e ∣ length via `Rat.den_dvd`; the OL5-adjacent convexity gap NEVER HIT; gates `gate_hensel`/`gate_eisenstein_sum` green; test-before-proving honored (20,000-hull + 100,000-point falsifiers pre-proof). **CONSUMER FIRED: `terminalPolySem_kp0_unconditional`** — the first unconditional `TerminalPolySem` at an 𝐇°-keyed k′ = 0 seam; **the (H6) (T-READ) k′ = 0 numerical input is THEOREM-BACKED** (the r27 charter's acceptance criterion met). Honesty: the −∞ corner stays the separately-priced open leg; `Kp0GateA/B/C` all stand; C2 (k′ ≥ 1) untouched; the (H6) row and its other members stay [M] | §0, §3.1 (H6) row, §3.2 end-to-end row, §6 |
| r28-2 | ledger "FORGE junc_bundle STAGE 1" (023a9d7 preregistration SEALED pre-run + 53f5d03; note `JUNCFORGE_S1_2026-08-08.md`) | MEASURED HARVEST + PENDING FLAG — numerics fold; nothing proved, ITER-LAW stays open | stage 1 ALL GREEN first-pass (10/10 preregistered rows, 4,116 samples, 28 tower instances, P1–P7 exact): the clause-(iii) residual PINNED — the TWO-SCALE LAW (read-pair scale carries the pattern 336/336; coarser stage scales read the key-power monomial 336/336) displayed as a **MEASURED reduction** (clause (iii) interface-derivable from (i)+(ii) at read-pair-graded stages), **NOT proved** — the Lean discharge is stage 2's; the terminal residual = the rank-2 binomial with the 𝒟₂-anchor digit coordinate (56/56); ITER-LAW extended 0/840 on run-realized classes, still the open lemma. **The D0 stratum flag displayed WITHOUT assertion**: `gate_glue_junction`'s hbundle instantiates the degenerate f = key stratum (bundle appears instance-unsatisfiable) — PENDING the orchestrator adjudication riding forge stage 2; no gate display changed. Stage-2 spec on record (row A/ℤ₂, read pair (2,3)); StageCore docstring debt CLEARED (comment-only diff) | §0, §3.1 (H1) row, §3.2 end-to-end row, §6 |
| r28-3 | ledger "G-ADM-2 EXECUTED (orchestrator harvest)" (38ac01e; sealed 4d7dba1; note `GADM2_2026-08-08.md`) | ROUTING ADJUDICATED — the scan proves NOTHING; the (ADM) row stays [M] | the sealed disposition verbatim: **RIGHT-END-ONLY PARTIAL — no track-B fire; track A live as (ADM-REAL′); ADM-U7 dichotomy CONFIRMED at scan scope** (198 rows, 13 configs, P1–P7 ALL PASS; all 85 partials at the ρ-pinned right-end slot CEN-J never charges; off-end partials ZERO). The no-hit discipline displayed at the row: the scan ADJUDICATES ROUTING ONLY — no attainment instance is upgraded; track B (statement-change-gated) NOT fired. Next (ADM) unit = the (ADM-REAL′) attempt per `ADM_U7_TRACKA_ATTEMPT.md`, dispatched at wave 11. Harvest honesty displayed: the unit ended mid-battery; the orchestrator wrote up from the unit's own sealed artifacts only | §0, §3.1 (ADM) row, §3.2 end-to-end row, §6 |
| r28-4 | ledger "K1 CONSUMER FACE STATED + FIRED" (380cc37; `B2D/K1ConsumerFace.lean`; note `K1FACE_2026-08-08.md`) | RESIDUAL DISCHARGED — the (FRESH) row's consumer-face component; the row STAYS [M] | **`K1ConsumerFace`** stated (interior slots' reads factor through off-parent labels; k_s excluded as clause (c)'s parent charge), pinned at exact strength (`k1ConsumerFace_iff_parentBlind` — exactly the (L) content, R2-C1 bar respected consumer-side), supplied through the v4 chain (`k1ConsumerFace_of_leakFreeV4` → `ledger_leakFreeV4` → family), **FIRED at `td2V4`**, de-vacuity fence `td2V4bad_not_k1ConsumerFace` compiled. The (FRESH) residual "Step-10/K1 classifier consumer face (the leakFree CONSUMER, still owed)" is DISCHARGED at this chain + fence. Honesty: parentBlock = ∅ at all compiled instances; the nonempty-parent DECLINED disposition stands (revisit trigger did NOT fire); NO tie to the Step-18 K1 exact form claimed; the full §B2-DEF non-ledger supplier residue stands | §0, §3.1 (FRESH) row, §3.2 end-to-end row, §6 |
| r28-5 | ledger "H7 FIRST PROBE" (2c72c5f; `Scaffold/HDischarge/H7/H7Probe.lean`, 12 thms Lean-core; note `H7PROBE_2026-08-08.md`) | DECIDED AT THE INSTANCE BIN — vacuity certified, statements STAND; the row stays [M] | (I-τ) and full K-LOC **VACUOUS-BY-TYPE at every compiled instance** (four bin instances; no countermodel obtainable — pass-6 F4's "no decision point of its own" confirmed at compile level); the only m ≥ 2 site (toyTreeA/tA1) certified card-2 but TRIPLY DEGENERATE (diagonal branchCellOf; sides ≡ 1; sorryAx carrier kept out of the compiled perimeter); KL-1a's order-1 count face HOLDS at ℤ/3, F4, ℤ/2; **OL-2 SHARPENED to the exact inhabitant spec** (sides ≥ 2 at a branching site AND non-diagonal branchCellOf on a sorry-free TreeModel); the route of record = **N-H7-1 numerics first** (h7_kloc_probe.py, dispatched at wave 11). Nothing repaired, nothing discharged — the row's statements stand unrepaired | §0, §3.1 (I-τ)/K-LOC row, §3.2 end-to-end row, §6 |
| r28-6 | cadence (the REVISION-20/r21 roll-forward precedent) | CADENCE ROLL-FORWARD — NO anchor move; never self-confirm | the acceptance stays anchored on the REVISION-25 text (VC28) — no operative anchor-citation text changes at r28; REVISION 27 IS a text change and VC29 had NOT yet run, so the owed confirming pass ROLLS FORWARD: **VC29 is OWED on the REVISION-27 wording** (charge below; model diversity per the tree §4 bar); until it lands, the acceptance citation points at the REVISION-25 text | §0, §3.2 end-to-end row, §6, this record |
| r28-7 | housekeeping (the wave-11 dispatch event) | TRACKING DISPLAY — dispatch only, nothing claimed | **wave 11 IS DISPATCHED**: forge stage 2 (the first compiled e ≥ 2 junction at the §S5 spec + the D0 hbundle adjudication); the (ADM-REAL′) attempt; the depth-3 probe (in flight from wave 10, NO result yet — nothing folded for it here); the N-H7-1 numerics harness — results ride r29, nothing from any running unit is consumed by any r28 display. The sorry census of record stays the r27 display (36 repo-wide / B2D 0; the wave-10 units added zero sorries per their ledger entries — BPLL sorry-free, K1ConsumerFace zero-sorry, H7Probe Lean-core; not re-measured at this doc-only unit) | §6, this record |

**THE ENUMERATION AUDIT (the r13 rule, run over the post-edit text).**
Unit: OCCURRENCE COUNTS (`grep -o | wc -l`, exact case-sensitive patterns,
the r23-declared unit), taken on the FINAL text of this revision (counts
include this record's own quotations — the r23–r27 precedent). The §32
twenty-nine patterns plus FIVE markers introduced or promoted at r28
(`BPLL`, `K1ConsumerFace`, `N-H7-1`, `VACUOUS-BY-TYPE` NEW at the folds;
`ADM-REAL` PROMOTED at the track-A routing); every delta vs the r27 census
named:

| grep (occurrences) | r27 | r28 | delta | why |
|---|---:|---:|---:|---|
| `(R1)–(R4)` | 116 | 117 | +1 | no operative change — this table's own pattern cell |
| `(H4a)` | 196 | 197 | +1 | no operative change — this table's own pattern cell |
| `K3-δ` | 128 | 129 | +1 | no operative change — this table's own pattern cell |
| `SITE-EXH` | 30 | 31 | +1 | no arc status change — this table's own pattern cell (the r28 (H1) bracket does not restate the consumer chain) |
| `GRADED-READ` | 24 | 25 | +1 | no operative change — this table's own pattern cell |
| `TransDeep` + `TRANS-DEEP` | 39 | 41 | +2 | no operative change — both increments are this row's own joint pattern cell (one hit each); the K1 fold names only `K1ConsumerFace`/v4 decls |
| `TEN rows` | 32 | 33 | +1 | historical-only — this table's own pattern cell |
| `NINE rows` | 36 | 41 | +5 | roster restated NINE, no change — the §0 REVISION-27 paragraph, the §3.2 roll-forward tail, this record's headline and close, and this table's own cell |
| `VC25` | 54 | 55 | +1 | closed history — this table's own pattern cell |
| `VC23` | 21 | 22 | +1 | named-but-unrun history stands — this table's own cell |
| `VC26` | 96 | 97 | +1 | closed history — this table's own pattern cell |
| `ROUTE (a)` | 30 | 33 | +3 | the r28 flag-lineage paragraph (×2) + this table's own cell; still zero sites claim route (a) proved anything |
| `TR3-S2` | 26 | 28 | +2 | closed history — this table's own pattern cell + the fixed-point note below |
| `REC-SL` | 33 | 36 | +3 | the r28 flag-lineage paragraph's supplier restatement + this table's own cell + the hyphen note at the row below; status unchanged |
| `RECSL` | 31 | 34 | +3 | the flag paragraph's `hσt`-pin rider naming + this table's own cell + the fixed-point note below; the hyphenated `REC-SL` pattern does NOT hit this one (the hyphen breaks the substring — the two counts are independent, as at every prior census), and `RecSLWire` stays invisible to the case-sensitive unit |
| `HUNI-V2` | 10 | 11 | +1 | closed history — this table's own pattern cell |
| `VC27` | 107 | 113 | +6 | history + the roll-forward displays quote the r27 lineage — §0/§3.2/this record + these cells; the exact-substring unit counts every VC27b hit here too |
| `VC27b` | 44 | 47 | +3 | closed history — this table's own pattern cell, the row above's why-cell naming, and the fixed-point note below (each hit also counts one VC27 occurrence — the three are included in the row above's +6) |
| `VC28` | 47 | 58 | +11 | the NO-anchor-move display: §0's REVISION-27 paragraph, the §3.2 roll-forward tail, the §6 r28 bracket, and this record's headline/disposition/charge rows each restate "anchored on REVISION 25 (VC28)" + these cells |
| `VC29` | 14 | 32 | +18 | the roll-forward IS this revision's cadence content: §0, §3.2, the §6 r28 bracket, this record's headline/flag/disposition/charge rows + these cells; VC29 still NAMED-NOT-RUN — zero sites claim it ran |
| `POOL-DICT` | 45 | 46 | +1 | closed history — this table's own pattern cell |
| `TR-4` | 30 | 31 | +1 | closed history — this table's own pattern cell |
| `BasePolygonLengthLaw` | 27 | 39 | +12 | the r28-1 PROVED fold: §0 (×2), the §3.1 (H6) r28 bracket, the §3.2 tail, the §6 r28 bracket, this record's title/headline/disposition/charge rows + these cells — every new site is PROVED-status or fold language, none re-opens the law |
| `ITER-LAW` | 26 | 33 | +7 | the forge fold restates the 0/840 extension and open-lemma status — §0, the (H1) r28 bracket, the §6 r28 bracket, the r28-2 row + this cell; open-lemma status UNCHANGED |
| `leakFree` | 52 | 61 | +9 | the K1 fold names the lowercase v4 supplier decls as exact substrings — the (FRESH) r28 bracket (×2), the r28-4 row incl. the quoted retired residual (×3), the row below's why-cell (×2) — + the fixed-point note + this cell; typed-face status unchanged |
| `LeakFreeCarrierV4` | 7 | 8 | +1 | no new consumption — this table's own pattern cell (the r28 K1 displays cite the supplier chain by its `k1ConsumerFace_of_leakFreeV4`/`ledger_leakFreeV4` names) |
| `junc_bundle` | 40 | 47 | +7 | the r28-2 stage-1 fold: §0, the (H1) r28 bracket, the §3.2 tail, the §6 r28 bracket, this record's title/headline/disposition rows + these cells; charter status unchanged (stage 2 dispatched, not landed) |
| `G-ADM-2` | 14 | 23 | +9 | the r28-3 executed fold: §0, the (ADM) r28 bracket, the §3.2 tail, the §6 r28 bracket, this record's headline/disposition/charge rows + these cells |
| `BPLL` | 0 | 13 | +13 | NEW at r28 — the proved unit's namespace + note name: the §0 fold display, the (H6) r28 bracket (core decl + note), the §6 r28 bracket, this record's rows + these cells |
| `K1ConsumerFace` | 0 | 12 | +12 | NEW at r28 — the fired face's capitalized type/file name at §0, the (FRESH) r28 bracket, the §6 r28 bracket, this record's rows + these cells; the lowercase `k1ConsumerFace_*`-style decl names do NOT hit the case-sensitive pattern |
| `N-H7-1` | 0 | 12 | +12 | NEW at r28 — the (H7) route of record: §0, the (I-τ)/K-LOC r28 bracket, the §3.2 tail, the §6 r28 bracket (×2 incl. the wave-11 list), this record's rows + these cells |
| `VACUOUS-BY-TYPE` | 0 | 8 | +8 | NEW at r28 — the H7 probe verdict marker: §0, the (I-τ)/K-LOC r28 bracket (×2), the §3.2 tail, the §6 r28 bracket, this record's rows + these cells; every site pairs it with "statements STAND" |
| `ADM-REAL` | 3 | 21 | +18 | PROMOTED at r28 (baseline 3 = the r27 track-A definition sites): the track-A routing names (ADM-REAL′) as the next unit — §0, the (ADM) r28 bracket (×2), the §3.2 tail, the §6 r28 bracket (×2 incl. the wave-11 list), this record's rows + these cells; the exact-substring unit counts (ADM-REAL′) hits as ADM-REAL hits |

Fixed point: the counts above were re-grepped AFTER the last edit of this
revision; the count-sensitive subtleties carry over from §§31–32 (`VC27b`
⊂ `VC27`; `VC28`/`VC29` disjoint; `leakFree` counts the v4 decl-name
substrings; `TR3S2` ≠ `TR3-S2`; `RecSLWire` invisible to the
case-sensitive `RECSL`), plus the r28-new ones recorded at their rows
(`ADM-REAL′` hits count under `ADM-REAL`; `BPLL` counts the namespace
prefix inside `BPLL.npSides_neg_length_sum`; the lowercase
`k1ConsumerFace_*` decl names do NOT hit the case-sensitive
`K1ConsumerFace` pattern — only the capitalized type/file name does).

**The re-derived row count (the r13 audit rule — counted, not copied).**
The §3.1 table's data rows enumerated directly after the edits: (H1) ·
(FRESH) = (H2) · (ADM) [(H3) member] · (UB-X) = (H4b) · (PACK) = (H5) ·
the (H6) terminal-seam group · (I-τ)/K-LOC = (H7) · (H8′) · (E-N) [(H3)
member]. THE COUNT IS **NINE** — unchanged; this revision touches row
CONTENT only (the (H1)/(FRESH)/(ADM)/(H6)/(H7) r28 brackets) and adds and
retires no row; `BasePolygonLengthLaw p` — now PROVED — was never a roster
row, so its retirement to proved status changes no count.

**VC29 charge.** All §32 charge items as inherited ((xxxiv)–(xli) now
checked against the r28 wording — note (xxxiv) becomes: every operative
site STILL cites REVISION 25/VC28, r28 having moved nothing), PLUS:
(xlii) the BPLL fold claims PROVED exactly at the committed artifact —
statement verbatim vs the r26 display, consumer
`terminalPolySem_kp0_unconditional` fired, the −∞ corner still open,
`Kp0GateA/B/C` standing, C2 untouched, the (H6) row still [M];
(xliii) the forge fold claims MEASURED status only — the two-scale law is
displayed as a numerics-backed reduction with the Lean discharge assigned
to stage 2, and the D0 `gate_glue_junction` flag is displayed PENDING with
NO gate display changed and NO hypothesis asserted false; (xliv) the
G-ADM-2 fold claims routing only — no attainment outcome is consumed
anywhere, track B is displayed not-fired, and the (ADM) operative
definition is unchanged; (xlv) the K1 fold's discharge is scoped to the
consumer-face residual alone — the ∅-parent honesty displayed, the
nonempty-parent decline standing, no Step-18 exact-form tie claimed;
(xlvi) the H7 fold claims vacuity-by-type with statements STANDING — no
repair, no discharge, the row still [M], N-H7-1 displayed as dispatch not
result; (xlvii) the wave-11 display claims dispatch only — in particular
the depth-3 probe (in flight since wave 10) contributes NO folded content
anywhere in the r28 text; (xlviii) the census above holds at the declared
occurrence unit with the stated delta-reasons. VC29 should receive the
in-repo file listing + commit resolutions + the COMPLETE provenance
appendix with VERBATIM slices per the saved checklist (memory:
vc-audit-appendix-checklist — validated at VC28).

**Bookkeeping.** Historical §§7–32 untouched except the dated additions
named in the disposition table (the §0 REVISION-26 paragraph header
superseded; the r28 brackets appended at the five named §3.1 rows; the
§3.2 roll-forward tail; the §6 r28 bracket). This is a DOC-ONLY revision:
no Lean file is touched, no statement changes, no note brackets ride this
unit (the five units' own notes are their records). This unit touches
ROOT + `docs/PROJECT_STATE.md` (new r28 checkpoint paragraph) + the ledger
(`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`, r28-executed entry)
ONLY. The commit closing this revision is its unit of record (unit ROOT
r28; scoped add).

— REVISION 27 complete. The wave-10 harvest folded: `BasePolygonLengthLaw
p` PROVED for every prime with the k′ = 0 consumer fired (the first
wave-10 result to upgrade a displayed open lemma to a theorem); the
forge's first deep runs ALL GREEN with the clause-(iii) residual pinned as
a MEASURED reduction and the D0 flag held for stage-2 adjudication;
G-ADM-2 routed the (ADM) row to track A without proving anything; the
Step-10/K1 classifier consumer face fired at the v4 chain; the (H7) pair
certified vacuous-by-type at the whole instance bin with N-H7-1 numerics
next; wave 11 running with results riding r29; NINE rows recounted; the
census re-run at the declared unit with every delta explained; **the
acceptance anchor UNMOVED at REVISION 25/VC28** and **VC29 owed on this
REVISION-27 text** — until it lands, the acceptance citation points at
the REVISION-25 text.

## 34. REVISION 28 (r29, the VC29 re-anchor + the wave-11 acceptance/verdict folds) — the anchor moves to REVISION 27; the junc_bundle question goes to the source

**What this revision is.** The r29 cadence event: **VC29 CONFIRMED
FIRST-PASS (0C/0G)** on the REVISION-27 text (ledger 2026-08-08, commit
3d457cc; report `lean/notes/openmath/VC29_report.md`) — the SECOND
consecutive first-pass confirmation of the cadence — so **the acceptance
anchor moves REVISION 25/VC28 → REVISION 27/VC29** and this revision
executes the citation-text update at every operative site (the VC22
precedent). The LINEAGE, displayed once for the record: VC29 was
originally owed on the REVISION-26 wording; REVISION 26 was properly
SUPERSEDED MID-CADENCE by the r28 roll-forward (the REVISION-20
precedent), so the pass ran on REVISION 27 and **no pass ever ran on the
REVISION-26 text** — REVISION 26 stands as a named-but-never-audited
intermediate, exactly like the VC23/VC24 named-but-unrun slots. On the
newly re-anchored text this revision then folds the WAVE-11
acceptance/verdict batch (all ledger-recorded): the `junc_bundle` VERDICT
OF RECORD + the FGMN re-derivation charter, ADM-REAL′ ACCEPTED, the
depth-3 probe + TR3-S3 tracking, N-H7-1, the JFS2 faithfulness-arc
closure, and the synthesis-pass-7 housekeeping. NO roster change: **NINE
rows** recounted; no clause attribution moved; this unit ran no audit and
NEVER self-confirms — **VC30 is OWED on this REVISION-28 wording**.

**The §29/§30/§33 ASVIN FLAG — lineage continued, ONE item ADDED.** The
fence-(vii) GR ROUTE (a) statement-change flag (§29), its §30 extension
(the REC-SL suppliers consume the `hσt` pin), and the §33 continuation
STAY DISPLAYED and are NOT resolved here. NEW at r29, SAME GENRE: **the
FGMN transcription-scale re-derivation charter (JUNCFORGE_S2 §R3) joins
the flag lineage** — its product is a corrected `SideReads`/`junc_bundle`
transcription as a PROPOSAL (a statement change: NO application to the
corpus without ROOT adjudication and the Asvin decision; until then every
JuncForge theorem keeps its exact current meaning and the compiled
`SideReads` stands as-is). The standing Asvin queue is now: the
`TowerMoveClosureL` two-layer re-ratification; the fence-(vii) ROUTE (a)
ratify-or-reopen incl. the RECSL `hσt`-pin rider AND the FGMN
transcription-scale charter (this row); the Route-B re-scope awaiting the
(UB-X) walk note.

**Disposition table.**

| item | ground | grade | disposition | edit sites |
|---|---|---|---|---|
| r29-1 | ledger 2026-08-08 "VC29 CONFIRMED FIRST-PASS (0C/0G)" (3d457cc; report `VC29_report.md` same commit) | ACCEPTANCE RE-ANCHOR — the citation-text update rides this revision per the VC22 precedent | every operative anchor site updated: the §0 REVISION-27 paragraph header superseded and the NEW REVISION-28 standing paragraph states the anchor (REVISION 27/VC29) with the mid-cadence supersession of REVISION 26 displayed; the §3.2 end-to-end row gains the VC29 sync tail (RE-ANCHORS on REVISION 27; VC30 owed on THIS text); the §6 r27 bracket's anchor sentence and the r28 bracket's stays-anchored clause carry dated supersession brackets and the new r29 bracket is current. Historical records (§§7–33) untouched beyond the named dated brackets | §0, §3.2 end-to-end row, §6 |
| r29-2 | ledger "FORGE junc_bundle S2" arc (036ee8c six Lean-core theorems; REVISION 1 + disposition 3e8ca8b; REVISIONS 2–4b d28abe6/0aad9c8/c6924c1/281553a; arc closure 09f93c1 round-5 CLEAN; note `JUNCFORGE_S2_2026-08-08.md`) | VERDICT OF RECORD + CHARTER — kernel-proved at the compiled instance; the source-level question OPEN; the row STAYS [M] | the compiled CHAR-2, Φ₂-LANDING, INTERIOR-Φ₂A-PINNED `junc_bundle` is **INSTANCE-FALSE at row A** (kernel-proved; the §S5 stage-2 discharge IMPOSSIBLE as transcribed); the PARITY WALL `juncforge_no_e2_junction` ALONE is char-free/Φnext-free and EXPLAINS the C-e escapee at the row-A read; `gate_glue_conditional` VACUOUS at its compiled instance is now a THEOREM (`juncforge_gate_hbundle_false` at definitional equality + `juncforge_gluePacket_false` the independent leg); the POSITIVE fragment `juncforge_clauseIII_derivable` stands; `DIterJunctionSupplier` NOT refuted. HONESTY: a TRANSCRIPTION verdict, NOT established falsity of the FGMN source (the incompatible-transcription-scales reading — a `SideReads` repair could RESTORE the paper-source glue step). **CHARTERED: the FGMN transcription-scale re-derivation design unit** (§R3; proposal-only, statement-change genre — the ASVIN FLAG addition above). `junc_bundle` LEAVES the open-wall list at the (H1) row and becomes this displayed verdict + charter; B-M2's wall list re-reads accordingly | §0, §3.1 (H1) row, §3.2 end-to-end row, §6, the flag paragraph above |
| r29-3 | ledger "ADM-REAL′ ACCEPTED" (24e51df; passes 3 (Codex) + 4 (Fable, 24a595c) consecutive CLEAN 0C/0G, model-diverse; note `ADMREAL_2026-08-08.md` at REVISION 2; seal chain 045b435 → e05e660/90e1ecb → b2e173a) | ACCEPTANCE FOLD — the track-A attempt DISCHARGED to displayed opens; the row STAYS [M] | **(CLASS-d) is a lattice THEOREM at every order — Lemma CLASS-LAT, UNCONDITIONAL** (the (GR-B) conditionality REMOVED from the track-A chain); NON-END + LOC + W-CAP at FULL any-type scope (the GMN Lemma 2.14 route, pins (P-OMJI)/(P-VRPHII), sha256-pinned source leg); the displayed opens now carried at the (ADM) row: **(B-PIN)** (boundary hypothesis; consumption corollary FULL-at-the-L′-slots conjunct only; value-attainment OPEN) + the **(C-JCT)** fan-out audit + **F-ADM-3** (the CensusCore `wphi` missing-rescale finding, verified at `CensusCore.lean:155` — EDIT-GATED carrier re-key design input, NO edit made). Track B stays statement-change-gated, NOT fired | §0, §3.1 (ADM) row, §3.2 end-to-end row, §6 |
| r29-4 | ledger "DEPTH-3 STRATA PROBE" (54c518d, sealed d8ef8d8; note `STRATA3_2026-08-08.md`) + "TR3-S3 ATTEMPT" (482150b, sealed b4a142b) + the verification arc (REVISION 1 8b003c4, REVISION 2 at 24e51df, REVISION 3 f05df77; passes archived) | MEASURED + IN-VERIFICATION TRACKING — NO status upgrade anywhere | the depth-3 probe MEASURED: **(IDENT-3) EXACT** (C^F3 = c^G3 identically, 0/1,656; 21,950 samples, 24 order-3 towers, ALL GREEN first-pass on the sealed header; no ǫ-revival on the all-e_j = 1 stratum at probe scope; the level-2 Def-3.15 reading discriminated externally for the first time). The chartered compose unit **TR3-S3 COMPOSED and IN VERIFICATION**: TR-3′-GEN at order 3, all-e_j = 1, conditional on (H-v3); REVISION 3 (Def 3.18 propagated per the pass-4 gap); the pass arc: p1 GAPS-ONLY → REV1; p2 Codex CLEAN (pre-revision); p3 Fable GAPS-ONLY → REV2; p4 Codex GAPS-ONLY → REV3; p5 running — **counter 0/2, passes 5+6 decide**; the pass-3 verifier's independent 93-sample from-note external leg on record (0 failures, off-roster tower). NOTHING upgraded: order 3 stays attempt-grade; e_j ≥ 2 strata stay fenced | §0, §3.1 (H1) row, §6 |
| r29-5 | ledger "N-H7-1 EXECUTED" (f8aec25; `verification/openmath/h7_kloc_probe.py` sealed pre-run; note `NH71_2026-08-08.md`) | FIRST-CONTACT EVIDENCE + TWO RESTATEMENT PINS — numerics only; statements still OPEN; the row STAYS [M] | the (H7) (I-τ)/K-LOC pair **SURVIVES first contact at six REAL m ≥ 2 sites** (139 PASS / 8 FAIL classified / 26 INFO; incl. the blueprint-named wild pair, m = 3, genuine-f₀/C₀, the KL-1b side-separation face): K-JAC, the λ-side translation, fixed-λ vertex units, the (iii′)-core, and the (I-τ) count/fiber/product laws EXACT everywhere. TWO MEASURED PINS displayed for the future Lean forge: **PIN 1** ε must be SLOPE-KEYED (the slope-pooled strengthening REFUTED at S5 — the blueprint's per-slope clause (ii) itself survives); **PIN 2** the D-8 guard is the measured extraction round-trip boundary (0 false-positives in-guard). Pins are restatement INPUTS — no statement changed at this fold | §0, §3.1 (I-τ)/K-LOC row, §6 |
| r29-6 | ledger "JUNCFORGE S2 FAITHFULNESS ARC CLOSED" (09f93c1; rounds 1–4 CRITICAL → round 5 CLEAN 0C/0G) | PROCESS RECORD — display discipline, no content change | the statement-faithfulness arc on the S2 note is COMPLETE (the note of record states exactly what the Lean theorems prove — the scoped verdict at r29-2); two campaign-discipline one-liners enter the record: the vc-audit-appendix checklist is VALIDATED TWICE (the VC28 and VC29 first-passes both rode appendices built per it); the pkill self-match trap (three exit-144 self-kills in rounds 4–5) is a saved memory | §6, this record |
| r29-7 | cadence (the VC22 precedent on the re-anchored text) | CADENCE — VC30 OWED; never self-confirm | REVISION 28 is a text change on the newly re-anchored text: the confirming pass **VC30 is OWED on the REVISION-28 wording** (charge below; model diversity per the tree §4 bar); until it lands, the acceptance citation points at the REVISION-27 text; VC30 is a fresh-context pass, not this unit | §0, §3.2 end-to-end row, §6, this record |
| r29-8 | housekeeping (the /goal cadence) | TRACKING DISPLAY — dispatch bookkeeping only | **SYNTHESIS PASS 7 becomes OWED**: the wave 10–13 window created new collapses the standing map does not yet price (CLASS-LAT unconditional at (ADM); the junc_bundle verdict + charter at (H1); the BPLL theorem + fired k′ = 0 consumer at (H6); the v4 chain at (FRESH)) — the re-map is due before the next big dispatch. The Asvin batch re-displayed (the flag paragraph above). The sorry census of record stays the r27 display (36 repo-wide / B2D 0; the wave-11 units added zero sorries per their ledger entries — JuncForge/H7Probe Lean-core, ADM-REAL′ and the probes Lean-untouched; not re-measured at this doc-only unit) | §6, this record |

**THE ENUMERATION AUDIT (the r13 rule, run over the post-edit text).**
Unit: OCCURRENCE COUNTS (`grep -o | wc -l`, exact case-sensitive patterns,
the r23-declared unit), taken on the FINAL text of this revision (counts
include this record's own quotations — the r23–r28 precedent). The §33
thirty-four patterns plus FOUR markers introduced at r29 (`CLASS-LAT` and
`TR3-S3` NEW at the folds; `JUNCFORGE` PROMOTED — baseline 4, the r28
stage-1 note-name cites; `VC30` NEW at the cadence); every delta vs the
r28 census named:

| grep (occurrences) | r28 | r29 | delta | why |
|---|---:|---:|---:|---|
| `(R1)–(R4)` | 117 | 118 | +1 | no operative change — this table's own pattern cell |
| `(H4a)` | 197 | 198 | +1 | no operative change — this table's own pattern cell |
| `K3-δ` | 129 | 130 | +1 | no operative change — this table's own pattern cell |
| `SITE-EXH` | 31 | 32 | +1 | no arc status change — this table's own pattern cell |
| `GRADED-READ` | 25 | 26 | +1 | no operative change — this table's own pattern cell |
| `TransDeep` + `TRANS-DEEP` | 41 | 43 | +2 | no operative change — both increments are this row's own joint pattern cell (one hit each) |
| `TEN rows` | 33 | 34 | +1 | historical-only — this table's own pattern cell |
| `NINE rows` | 41 | 46 | +5 | roster restated NINE, no change — the §0 REVISION-28 paragraph, the §3.2 sync tail, this record's headline and close, and this table's own cell |
| `VC25` | 55 | 56 | +1 | closed history — this table's own pattern cell |
| `VC23` | 22 | 25 | +3 | named-but-unrun history stands — this record's lineage display cites the VC23/VC24 precedent for the never-audited REVISION 26 + this table's own cells (the pattern cell and this why-cell's own naming) |
| `VC26` | 97 | 98 | +1 | closed history — this table's own pattern cell |
| `ROUTE (a)` | 33 | 38 | +5 | the §0 housekeeping line, the §6 r29 bracket's Asvin-batch line, the flag-lineage paragraph's two namings + this table's own cell; still zero sites claim route (a) proved anything |
| `TR3-S2` | 28 | 31 | +3 | closed history — this table's own pattern cell + the fixed-point note's two disjointness namings below; `TR3-S3` does NOT hit this pattern (disjoint substrings) |
| `REC-SL` | 36 | 39 | +3 | the r29 flag-lineage paragraph's supplier restatement + this table's own cell + the hyphen note at the row below; status unchanged |
| `RECSL` | 34 | 39 | +5 | the `hσt`-pin rider namings at §0, the §6 r29 bracket, and the flag paragraph + this table's own cell + the fixed-point note below; the hyphenated `REC-SL` pattern stays disjoint (the hyphen breaks the substring) and `RecSLWire` stays invisible to the case-sensitive unit |
| `HUNI-V2` | 11 | 12 | +1 | closed history — this table's own pattern cell |
| `VC27` | 113 | 119 | +6 | history — this table's own cells at this row and the row below + the fixed-point note below; the anchor-move displays cite "REVISION 27/VC29", which does NOT hit this pattern (VC27 ≠ VC29) — the increments are all census-apparatus cells |
| `VC27b` | 47 | 49 | +2 | closed history — this table's own pattern cell + the fixed-point note below (each hit also counts one VC27 occurrence, included in the row above) |
| `VC28` | 58 | 72 | +14 | the anchor-move display: every "REVISION 25/VC28 → REVISION 27/VC29" lineage statement at §0/§3.2/§6/this record + these cells; all new sites are the OLD side of the move — zero sites still cite VC28 as the current anchor |
| `VC29` | 32 | 73 | +41 | the re-anchor IS this revision's cadence content: §0's REVISION-28 paragraph, the §3.2 sync tail, the §6 supersession + r29 brackets, this record's headline/lineage/disposition/charge rows + these cells; every site now states VC29 RAN and CONFIRMED |
| `POOL-DICT` | 46 | 47 | +1 | closed history — this table's own pattern cell |
| `TR-4` | 31 | 32 | +1 | closed history — this table's own pattern cell |
| `BasePolygonLengthLaw` | 39 | 40 | +1 | proved status stands — this table's own pattern cell |
| `ITER-LAW` | 33 | 35 | +2 | open-lemma status UNCHANGED — the §6 r29 bracket's queue restatement + this table's own cell |
| `leakFree` | 61 | 63 | +2 | typed-face status unchanged — this table's own pattern cell + the fixed-point note below |
| `LeakFreeCarrierV4` | 8 | 9 | +1 | no new consumption — this table's own pattern cell |
| `junc_bundle` | 47 | 71 | +24 | the r29-2 verdict fold: §0, the (H1) r29 bracket, the §3.2 sync tail, the §6 r29 bracket, this record's title/flag/disposition rows + these cells; every new site is the SCOPED verdict (instance-false at row A) or the charter — none asserts source-level falsity |
| `G-ADM-2` | 23 | 24 | +1 | closed routing history — this table's own pattern cell |
| `BPLL` | 13 | 17 | +4 | proved status stands — the synthesis-7 collapse listings (§0 housekeeping, the §6 r29 bracket, the r29-8 row) + this table's own cell |
| `K1ConsumerFace` | 12 | 14 | +2 | discharged-face status stands — this table's own pattern cell + the fixed-point note below |
| `N-H7-1` | 12 | 21 | +9 | the r29-5 fold: §0, the (I-τ)/K-LOC r29 bracket, the §6 r29 bracket, this record's rows + these cells; every site pairs survival with "statements still OPEN" |
| `VACUOUS-BY-TYPE` | 8 | 9 | +1 | the r28 verdict stands — this table's own pattern cell |
| `ADM-REAL` | 21 | 35 | +14 | the r29-3 acceptance fold: §0, the (ADM) r29 bracket, the §3.2 sync tail, the §6 r29 bracket, this record's rows + these cells; the exact-substring unit counts (ADM-REAL′) hits |
| `CLASS-LAT` | 0 | 13 | +13 | NEW at r29 — the accepted lemma's name: the §0 paragraph (fold + synthesis-7 listing), the (ADM) r29 bracket, the §3.2 sync tail, the §6 r29 bracket (fold + synthesis-7 listing), this record's rows, the census header + this cell; every site says THEOREM, unconditional, at every order |
| `JUNCFORGE` | 4 | 14 | +10 | PROMOTED at r29 (baseline 4 = the r28 stage-1 note-name cites): the S2 note-name + §R3-charter citations at §0 and the (H1) r29 bracket, this record's ground/flag rows, the census header, the fixed-point note below + this cell |
| `TR3-S3` | 0 | 16 | +16 | NEW at r29 — the in-verification compose unit: §0, the (H1) r29 bracket, the §3.2 sync tail, the §6 r29 bracket, this record's rows + these cells; ZERO sites claim it accepted (counter 0/2 displayed at every status site) |
| `VC30` | 0 | 16 | +16 | NEW at r29 — the owed confirming pass: §0, the §3.2 sync tail, the §6 r29 bracket, this record's cadence/charge rows + these cells; VC30 NAMED-NOT-RUN — zero sites claim it ran |

Fixed point: the counts above were re-grepped AFTER the last edit of this
revision; the count-sensitive subtleties carry over from §§31–33 (`VC27b`
⊂ `VC27`; `VC28`/`VC29`/`VC30` pairwise disjoint; `leakFree` counts the v4
decl-name substrings; `TR3S2` ≠ `TR3-S2`; `RecSLWire` invisible to the
case-sensitive `RECSL`; `ADM-REAL′` hits count under `ADM-REAL`; the
lowercase `k1ConsumerFace_*` decl names do NOT hit `K1ConsumerFace`), plus
the r29-new ones recorded at their rows (`TR3-S3` disjoint from `TR3-S2`;
`JUNCFORGE` counts the uppercase note/arc names only — the lowercase
`juncforge_*` theorem names do NOT hit the case-sensitive pattern;
`CLASS-LAT` disjoint from `CLASS-d`, which is not a tracked pattern).

**The re-derived row count (the r13 audit rule — counted, not copied).**
The §3.1 table's data rows enumerated directly after the edits: (H1) ·
(FRESH) = (H2) · (ADM) [(H3) member] · (UB-X) = (H4b) · (PACK) = (H5) ·
the (H6) terminal-seam group · (I-τ)/K-LOC = (H7) · (H8′) · (E-N) [(H3)
member]. THE COUNT IS **NINE** — unchanged; this revision touches row
CONTENT only (the (H1)/(ADM)/(I-τ)/K-LOC r29 brackets) and adds and
retires no row; ADM-REAL′'s acceptance discharges an ATTEMPT to displayed
opens inside the standing (ADM) row, and the junc_bundle verdict converts
a forge target into a displayed verdict + charter inside the standing
(H1) row — neither changes any count.

**VC30 charge.** All §33 charge items as inherited ((xxxiv)–(xlviii) now
checked against the r29 wording — note (xxxiv) becomes: every operative
site cites REVISION 27/VC29, with the REVISION-26 mid-cadence supersession
displayed and NO site citing VC28 as current), PLUS: (xlix) the
junc_bundle fold claims EXACTLY the scoped kernel verdict — instance-false
at row A under the CHAR-2/Φ₂-LANDING/INTERIOR-Φ₂A-PINNED restrictions at
every verdict-tier site, the parity wall alone displayed char-free, the
gate vacuity displayed as a theorem at its compiled instance, the
source-level question displayed OPEN, and the charter displayed
PROPOSAL-ONLY with the Asvin flag; (l) the ADM-REAL′ fold claims the
acceptance exactly — CLASS-LAT unconditional at every order, the (GR-B)
conditionality removed from track A ONLY (the (H1)(a) row untouched), the
three displayed opens ((B-PIN)/(C-JCT)/F-ADM-3) present at the (ADM) row,
F-ADM-3 displayed EDIT-GATED with no edit made; (li) the depth-3/TR3-S3
display claims NO status upgrade — (IDENT-3) displayed as measured, TR3-S3
displayed in-verification at counter 0/2 with passes 5+6 deciding; (lii)
the N-H7-1 fold claims numerics-only survival — both pins displayed as
restatement inputs, the slope-pooled refutation displayed, no statement
changed; (liii) the JFS2 closure is displayed as a process record only;
(liv) the synthesis-pass-7 OWED display and the Asvin-batch re-display
match the §6 r29 bracket; (lv) the census above holds at the declared
occurrence unit with the stated delta-reasons. VC30 should receive the
in-repo file listing + commit resolutions + the COMPLETE provenance
appendix with VERBATIM slices per the saved checklist (memory:
vc-audit-appendix-checklist — validated at VC28 AND VC29).

**Bookkeeping.** Historical §§7–33 untouched except the dated additions
named in the disposition table (the §0 REVISION-27 paragraph header
superseded; the r29 brackets appended at the three named §3.1 rows; the
§3.2 sync tail; the §6 supersession brackets + the r29 bracket). This is a
DOC-ONLY revision: no Lean file is touched, no statement changes, no note
brackets ride this unit (the wave-11 units' own notes are their records).
This unit touches ROOT + `docs/PROJECT_STATE.md` (new r29 checkpoint
paragraph) + the ledger (`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`,
r29-executed entry) ONLY. The commit closing this revision is its unit of
record (unit ROOT r29; scoped add).

— REVISION 28 complete. VC29 CONFIRMED FIRST-PASS and the acceptance
RE-ANCHORED on the REVISION-27 text (the anchor moves REVISION 25/VC28 →
REVISION 27/VC29; REVISION 26 superseded mid-cadence, never audited); the
wave-11 batch folded: the compiled CHAR-2, Φ₂-LANDING, INTERIOR-Φ₂A-PINNED
junc_bundle adjudicated INSTANCE-FALSE at row A **[r29-ERRATUM, VC30
finding 1: the three restrictions were omitted from this one closing line
while every other verdict-tier site carried them — added pre-confirmation;
the census's "every new site is the SCOPED verdict" claim holds as of this
erratum]** with the source question OPEN and the FGMN re-derivation
chartered proposal-only (the new Asvin-flag item); ADM-REAL′ ACCEPTED with
CLASS-LAT a lattice theorem at every order and the (ADM) opens displayed;
the depth-3 law measured exact with TR3-S3 in verification at counter 0/2;
the (H7) pair surviving first contact with two measured restatement pins;
the JFS2 faithfulness arc closed; synthesis pass 7 OWED; NINE rows
recounted; the census re-run at the declared unit with every delta
explained; **VC30 owed on this REVISION-28 text** — until it lands, the
acceptance citation points at the REVISION-27 text.

## 35. REVISION 29 (r30, the VC30+VC30b re-anchor + the TR3-S3 acceptance fold) — the anchor moves to REVISION 28; the strata trilogy completes

**What this revision is.** The r30 cadence event: **VC30 + VC30b
CONFIRMED** (ledger 2026-08-08 "VC30 + VC30b CONFIRMED", commit 6984d5d)
— so **the acceptance anchor moves REVISION 27/VC29 → REVISION
28/VC30+VC30b** and this revision executes the citation-text update at
every operative site (the VC22 precedent). The r29-ERRATUM LINEAGE,
displayed once in full for the record: VC30 (full audit of the
REVISION-28 text) returned **1 critical / 0 gaps** — the critical: the
§34 closing line's UNQUALIFIED `junc_bundle` restatement, the ONE
verdict-tier site that omitted the three restrictions while every other
site carried them (the qualifier-propagation defect class's last
appearance); every fold, the 38-pattern census, roster NINE, and all
discipline checks otherwise passed (report
`lean/notes/openmath/VC30_report.md`, archived 573bd1b). The cure: the
dated pre-confirmation **r29-ERRATUM** bracket applied the three
restrictions at that line (573bd1b — visible in §34's closing paragraph),
and the narrow confirming pass **VC30b RAN on the errata-corrected text
and CONFIRMED, 0 critical / 0 gaps** (report `VC30b_report.md`, 6984d5d —
the VC26b/VC27b narrow-companion genre). Now INSIDE the certified anchor:
the `junc_bundle` verdict of record + the FGMN charter (the Asvin flag),
the ADM-REAL′ acceptance (CLASS-LAT), the N-H7-1 pins, the JFS2 arc, the
strata3 measurement. On the newly re-anchored text this revision folds
the **TR3-S3 acceptance** (the ledger-recorded 96b7f54 event) plus
housekeeping. NO roster change: **NINE rows** recounted; no clause
attribution moved; this unit ran no audit and NEVER self-confirms —
**VC31 is OWED on this REVISION-29 wording**.

**The §29/§30/§33/§34 ASVIN FLAG — lineage continued, NO new item.** The
fence-(vii) GR ROUTE (a) statement-change flag (§29), its §30 extension
(the REC-SL suppliers consume the `hσt` pin), the §33 continuation, and
the §34 addition (the FGMN transcription-scale re-derivation charter,
JUNCFORGE_S2 §R3 — proposal-only) STAY DISPLAYED and are NOT resolved
here. The standing Asvin queue, re-displayed unchanged: the
`TowerMoveClosureL` two-layer re-ratification; the fence-(vii) ROUTE (a)
ratify-or-reopen incl. the RECSL `hσt`-pin rider AND the FGMN
transcription-scale charter; the Route-B re-scope awaiting the (UB-X)
walk note.

**Disposition table.**

| item | ground | grade | disposition | edit sites |
|---|---|---|---|---|
| r30-1 | ledger 2026-08-08 "VC30 + VC30b CONFIRMED" (6984d5d; VC30 report + the r29-ERRATUM at 573bd1b; VC30b report at 6984d5d) | ACCEPTANCE RE-ANCHOR — the citation-text update rides this revision per the VC22 precedent; the erratum lineage displayed at every operative site | every operative anchor site updated: the §0 REVISION-28 paragraph header superseded and the NEW REVISION-29 standing paragraph states the anchor (REVISION 28/VC30+VC30b) with the full erratum lineage (VC30 1C/0G → the dated pre-confirmation r29-ERRATUM → VC30b 0C/0G narrow); the §3.2 end-to-end row gains the VC30+VC30b sync tail (RE-ANCHORS on REVISION 28; VC31 owed on THIS text); the §6 r29 bracket's anchor sentence carries a dated supersession bracket and the new r30 bracket is current. Historical records (§§7–34) untouched beyond the named dated brackets (§34's erratum bracket predates this unit, 573bd1b) | §0, §3.2 end-to-end row, §6 |
| r30-2 | ledger 2026-08-08 "TR3-S3 ACCEPTED" (96b7f54; note `TR3S3_2026-08-08.md` REVISION 4; passes 7 Codex dc40668 + 8 Fable 83870a8 consecutive CLEAN 0C/0G, model-diverse, counter 2/2; the eight-pass arc with four orchestrator-applied revisions; THREE independent from-note implementations — 93 + 1,564 + 821 checks, 0 failures) | ACCEPTANCE FOLD — the strata trilogy completes; the (H1) row STAYS [M]; scope enlarged, nothing else moves | with accepted TR3-S1 + TR3-S2: **TR-3′-GEN is PROVED on {all order-2} ∪ {order-3 all-e_j = 1}, CONDITIONAL ON (H-v3)** — ATTRIBUTION ONLY (the finite identity σ₂(R^F₃(f)) = R_λ₃(f), discrepancy ≡ 1, support equal, total in nonzero f, is UNCONDITIONAL algebra; (H-v3) = the order-3 ordinate identity v₃ = w₂ carries the GMN-operator attribution), with **PIN-GEN-i** the displayed attribution pin (the owed non-textual leg: a fresh `pdftotext -layout` re-extraction of FGMN at Defs 3.15/3.16/3.18, hand-verified at the instantiated indices — the extraction-corruption discipline); the (H1) member-(a) display gains the r30 bracket with the enlarged union; the GRB note `GRB_ORDER2_2026-08-05.md` S3.4 gains the SECOND post-acceptance bracket, ADDITIVE (that leaf's acceptance counters NOT reset; its first bracket and pass arc stand byte-unchanged) and its statement-of-record scope line is extended in place; **deeper strata stay OPEN, probe-first** — e_j ≥ 2 strata at orders ≥ 3 fenced, the sealed UNTESTED (TWIST-3) candidate displayed (a compose unit fires only on an exact measured law, the STRATA3 charter rule); D-ITER/TR-4 residues stand; member (a) NOT discharged | §0, §3.1 (H1) row, §3.2 end-to-end row, §6, the GRB note |
| r30-3 | housekeeping (the /goal cadence) | TRACKING DISPLAY — dispatch bookkeeping only | **SYNTHESIS PASS 7 is RUNNING** (dispatched after the r29 fold; in flight at this unit — its report becomes the NEXT DISPATCH OF RECORD when it lands; NOTHING is folded or claimed from it here). The Asvin batch re-displayed (the flag paragraph above). The sorry census of record stays the r27 display (36 repo-wide / B2D 0; this is a doc-only unit — not re-measured; the TR3-S3 arc is Lean-untouched per its ledger entries) | §0, §6, this record |
| r30-4 | cadence (the VC22 precedent on the re-anchored text) | CADENCE — VC31 OWED; never self-confirm | REVISION 29 is a text change on the newly re-anchored text: the confirming pass **VC31 is OWED on the REVISION-29 wording** (charge below; model diversity per the tree §4 bar); until it lands, the acceptance citation points at the REVISION-28 text; VC31 is a fresh-context pass, not this unit | §0, §3.2 end-to-end row, §6, this record |

**THE ENUMERATION AUDIT (the r13 rule, run over the post-edit text).**
Unit: OCCURRENCE COUNTS (`grep -o | wc -l`, exact case-sensitive patterns,
the r23-declared unit), taken on the FINAL text of this revision (counts
include this record's own quotations — the r23–r29 precedent). The §34
thirty-eight patterns plus FIVE markers introduced at r30 (`VC30b` and
`VC31` NEW at the cadence; `H-v3`, `PIN-GEN-i`, `TWIST-3` NEW at the
acceptance fold — `H-v3` baseline 3 and `TWIST-3` baseline 1, both from
the r29 in-verification displays; `PIN-GEN-i` baseline 0); every delta vs
the r29 census named, with the r29-ERRATUM's own post-census
contributions (573bd1b, pre-dating this unit) folded into the reasons:

| grep (occurrences) | r29 | r30 | delta | why |
|---|---:|---:|---:|---|
| `(R1)–(R4)` | 118 | 119 | +1 | no operative change — this table's own pattern cell |
| `(H4a)` | 198 | 199 | +1 | no operative change — this table's own pattern cell |
| `K3-δ` | 130 | 131 | +1 | no operative change — this table's own pattern cell |
| `SITE-EXH` | 32 | 33 | +1 | no arc status change — this table's own pattern cell |
| `GRADED-READ` | 26 | 27 | +1 | no operative change — this table's own pattern cell |
| `TransDeep` + `TRANS-DEEP` | 43 | 45 | +2 | no operative change — both increments are this row's own joint pattern cell (one hit each) |
| `TEN rows` | 34 | 35 | +1 | historical-only — this table's own pattern cell |
| `NINE rows` | 46 | 51 | +5 | roster restated NINE, no change — the §0 REVISION-29 paragraph, the §3.2 sync tail, this record's headline and close, and this table's own cell |
| `VC25` | 56 | 57 | +1 | closed history — this table's own pattern cell |
| `VC23` | 25 | 26 | +1 | named-but-unrun history stands — this table's own pattern cell |
| `VC26` | 98 | 104 | +6 | closed history as a pass; the increments are the narrow-companion genre namings at §0, the §3.2 sync tail, the §6 r30 bracket, and this record (each names the two historical b-suffixed narrow companion passes, whose first name contains this pattern) + this table's own cell + the fixed-point note below |
| `ROUTE (a)` | 38 | 43 | +5 | the §0 Asvin-batch line, the §6 r30 bracket's Asvin-batch line, the flag-lineage paragraph's two namings + this table's own cell; still zero sites claim route (a) proved anything |
| `TR3-S2` | 31 | 36 | +5 | the acceptance fold consumes the accepted parents — the parent-pair namings at §0, the (H1) r30 bracket, the §6 r30 bracket, this record's r30-2 row + this table's own cell; `TR3-S3` does NOT hit this pattern (disjoint substrings) |
| `REC-SL` | 39 | 41 | +2 | the r30 flag-lineage paragraph's supplier restatement + this table's own cell; status unchanged |
| `RECSL` | 39 | 43 | +4 | the `hσt`-pin rider namings at §0, the §6 r30 bracket, and the flag paragraph + this table's own cell; the hyphenated variant stays disjoint (the hyphen breaks the substring) |
| `HUNI-V2` | 12 | 13 | +1 | closed history — this table's own pattern cell |
| `VC27` | 119 | 127 | +8 | history — the four narrow-companion genre namings (via the b-suffix containment, as at the row above) + this table's own cells at this row and the row below + the fixed-point note below; no site cites it as anything but closed history |
| `VC27b` | 49 | 55 | +6 | closed history — the four narrow-companion genre namings + this table's own cell + the fixed-point note below (each hit also counts one hit at the row above) |
| `VC28` | 72 | 74 | +2 | closed history — the charge row's checklist-validation naming + this table's own pattern cell; zero sites cite it as the current anchor |
| `VC29` | 73 | 81 | +8 | the re-anchor displays: every anchor-move lineage statement at §0/§3.2/§6/this record states the OLD side of the move, plus the charge rows + this table's own cell; zero sites cite it as the CURRENT anchor |
| `POOL-DICT` | 47 | 48 | +1 | closed history — this table's own pattern cell |
| `TR-4` | 32 | 35 | +3 | the deeper-residue-taxa NOT-touched displays at the (H1) r30 bracket and this record's r30-2 row + this table's own cell; the taxon stays untouched |
| `BasePolygonLengthLaw` | 40 | 41 | +1 | proved status stands — this table's own pattern cell |
| `ITER-LAW` | 35 | 37 | +2 | open-lemma status UNCHANGED — the §6 r30 bracket's queue restatement + this table's own cell |
| `leakFree` | 63 | 64 | +1 | typed-face status unchanged — this table's own pattern cell |
| `LeakFreeCarrierV4` | 9 | 10 | +1 | no new consumption — this table's own pattern cell |
| `junc_bundle` | 71 | 79 | +8 | the r29-ERRATUM lineage displays: the r30-1 critical NAMED (the unqualified-restatement defect) and the inside-the-anchor pointer, at §0, the §3.2 sync tail, the §6 r30 bracket, and this record + this table's own cell; every new site is a DEFECT-record or POINTER display — none restates the verdict tier, so none carries (or needs) the three restrictions |
| `G-ADM-2` | 24 | 25 | +1 | closed routing history — this table's own pattern cell |
| `BPLL` | 17 | 18 | +1 | proved status stands — this table's own pattern cell |
| `K1ConsumerFace` | 14 | 15 | +1 | discharged-face status stands — this table's own pattern cell |
| `N-H7-1` | 21 | 26 | +5 | inside-the-certified-anchor pointer displays (§0; the §6 r30 bracket twice — the anchor list and the beyond-cadence queue's pin consumer; this record's lineage paragraph) + this table's own cell; every site still pairs the pins with statements-OPEN status |
| `VACUOUS-BY-TYPE` | 9 | 10 | +1 | the r28 verdict stands — this table's own pattern cell |
| `ADM-REAL` | 35 | 39 | +4 | inside-the-certified-anchor pointer displays at §0, the §6 r30 bracket, this record's lineage paragraph + this table's own cell (the exact-substring unit counts the prime-suffixed acceptance name) |
| `CLASS-LAT` | 13 | 17 | +4 | inside-the-certified-anchor pointer displays at §0, the §6 r30 bracket, this record's lineage paragraph + this table's own cell; theorem status unchanged |
| `JUNCFORGE` | 14 | 16 | +2 | the flag paragraph's §R3 charter citation + this table's own cell; the charter stays proposal-only |
| `TR3-S3` | 16 | 29 | +13 | the acceptance IS this revision's fold content: §0, the (H1) r30 bracket, the §3.2 sync tail, the §6 r30 bracket, this record's headline/lineage/disposition/housekeeping/charge/close + these cells (incl. the naming two rows above); every site states ACCEPTED at the 2-clean model-diverse bar, counter 2/2 — zero sites claim any e_j ≥ 2 scope |
| `VC30` | 16 | 91 | +75 | the re-anchor IS this revision's cadence content: every erratum-lineage display names the pass AND its narrow companion, and the companion's name contains this pattern, so each pairing counts twice at this row (§0, the §3.2 sync tail, the §6 supersession + r30 brackets, this record's headline/lineage/disposition/charge rows + these cells and the note below); +1 of this delta is the §34 erratum bracket's own naming (573bd1b, applied pre-confirmation BEFORE this unit — the r29 column predates it); every site states CONFIRMED-with-erratum-lineage — zero sites claim a first-pass clean |
| `VC30b` | 0 | 37 | +37 | NEW at r30 — the narrow companion pass: every erratum-lineage display at §0/§3.2/§6/this record + the census header + these cells and the note below; every hit also counts one hit at the row above (containment) |
| `VC31` | 0 | 16 | +16 | NEW at r30 — the owed confirming pass: §0, the §3.2 sync tail, the §6 r30 bracket, this record's cadence/charge rows, the census header + these cells; VC31 NAMED-NOT-RUN — zero sites claim it ran |
| `H-v3` | 3 | 17 | +14 | the conditionality display rides EVERY enlarged-scope site (attribution only, at §0, the (H1) r30 bracket, the §3.2 sync tail, the §6 r30 bracket, this record's rows, the census header) + these cells and the note below; the r29 baseline 3 = the in-verification displays |
| `PIN-GEN-i` | 0 | 11 | +11 | NEW at r30 — the displayed attribution pin with its owed pdftotext leg, at every fold site + the census header + these cells and the note below; zero sites claim the pin discharged |
| `TWIST-3` | 1 | 11 | +10 | the sealed-candidate probe-first display at §0, the (H1) r30 bracket, the §6 r30 bracket, this record's r30-2/charge/close, the census header + these cells and the note below; the r29 baseline 1 = the (H1) r29 bracket's ride-the-note line; the candidate stays SEALED and UNTESTED |

Fixed point: the counts above were re-grepped AFTER the last edit of this
revision. The count-sensitive subtleties of §§31–34 carry over UNCHANGED
and are not re-listed (the containment, hyphen-disjointness,
case-sensitivity, and decl-name-visibility notes at the §34 fixed-point
paragraph — each re-checked, none changed sign); the r30-new ones:
**`VC30b` ⊂ `VC30`** — every b-suffixed hit counts once under its parent
too (the `VC27b` genre), and the same containment carries the
narrow-companion genre namings into the `VC26` and `VC27` rows; `VC31`
is pairwise disjoint from the other cadence names; `H-v3` and `TWIST-3`
count their parenthesized displays; `PIN-GEN-i` is exact-case.

**The re-derived row count (the r13 audit rule — counted, not copied).**
The §3.1 table's data rows enumerated directly after the edits: (H1) ·
(FRESH) = (H2) · (ADM) [(H3) member] · (UB-X) = (H4b) · (PACK) = (H5) ·
the (H6) terminal-seam group · (I-τ)/K-LOC = (H7) · (H8′) · (E-N) [(H3)
member]. THE COUNT IS **NINE** — unchanged; this revision touches row
CONTENT only (the (H1) r30 bracket) and adds and retires no row; the
TR3-S3 acceptance enlarges a residue display inside the standing (H1)
row — it changes no count.

**VC31 charge.** All §34 charge items as inherited ((xxxiv)–(lv) now
checked against the r30 wording — note (xxxiv) becomes: every operative
site cites REVISION 28/VC30+VC30b, with the r29-ERRATUM lineage displayed
(VC30 1C/0G → erratum 573bd1b → VC30b 0C/0G) and NO site citing VC29 as
the current anchor), PLUS: (lvi) the TR3-S3 fold claims EXACTLY the
acceptance — the union scope {all order-2} ∪ {order-3 all-e_j = 1}
displayed with the (H-v3) conditionality (attribution-only, the finite
identity unconditional) at EVERY scope site, PIN-GEN-i displayed with the
owed pdftotext leg at Defs 3.15/3.16/3.18, deeper strata displayed OPEN
probe-first with the sealed (TWIST-3) candidate named, and NO site
claiming TR-3′-GEN at any e_j ≥ 2 stratum of order ≥ 3; (lvii) the GRB
second post-acceptance bracket is ADDITIVE — the first bracket and the
leaf's acceptance counters byte-unchanged, the scope-line extension
consistent with the bracket; (lviii) the synthesis-pass-7 display claims
RUNNING only — nothing folded from it anywhere; (lix) the census above
holds at the declared occurrence unit with the stated delta-reasons.
VC31 should receive the in-repo file listing + commit resolutions + the
COMPLETE provenance appendix with VERBATIM slices per the saved checklist
(memory: vc-audit-appendix-checklist — validated at VC28 AND VC29; VC30's
single critical was a text defect, not an appendix failure).

**Bookkeeping.** Historical §§7–34 untouched except the dated additions
named in the disposition table (the §0 REVISION-28 paragraph header
superseded; the r30 bracket appended at the (H1) row; the §3.2 sync tail;
the §6 supersession bracket + the r30 bracket; §34's r29-ERRATUM bracket
was applied at 573bd1b BEFORE this unit and is cited, not edited). This
is a DOC-ONLY revision: no Lean file is touched, no statement changes.
ONE note bracket rides this unit (the r25/GRB precedent): the GRB
`GRB_ORDER2_2026-08-05.md` S3.4 second post-acceptance bracket +
scope-line extension. This unit touches ROOT + `docs/PROJECT_STATE.md`
(new r30 checkpoint paragraph) + the GRB note + the ledger
(`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`, r30-executed entry)
ONLY. The commit closing this revision is its unit of record (unit ROOT
r30; scoped add).

— REVISION 29 complete. VC30 + VC30b CONFIRMED and the acceptance
RE-ANCHORED on the REVISION-28 text (the anchor moves REVISION 27/VC29 →
REVISION 28/VC30+VC30b; the r29-ERRATUM lineage displayed — VC30 1C/0G,
the erratum bracket, VC30b 0C/0G narrow); TR3-S3 ACCEPTED and folded:
TR-3′-GEN PROVED on {all order-2} ∪ {order-3 all-e_j = 1} CONDITIONAL ON
(H-v3) (attribution only — the finite identity unconditional) with
PIN-GEN-i displayed and the GRB second post-acceptance bracket applied
ADDITIVE; deeper strata OPEN probe-first (the sealed (TWIST-3) candidate
displayed); synthesis pass 7 RUNNING with nothing claimed from it;
NINE rows recounted; the census re-run at the declared unit with every delta
explained; **VC31 owed on this REVISION-29 text** — until it lands, the
acceptance citation points at the REVISION-28 text.

## 36. REVISION 30 (r31, the VC31+VC31b+VC31c re-anchor + the wave-14 fold batch) — the anchor moves to REVISION 29; the source-question window lands

**What this revision is.** The r31 cadence event: **VC31 + VC31b + VC31c
CONFIRMED** (ledger 620417b "VC31 + VC31b + VC31c CONFIRMED") — so **the
acceptance anchor moves REVISION 28/VC30+VC30b → REVISION
29/VC31+VC31b+VC31c** and this revision executes the citation-text
update at every operative site (the VC22 precedent). The VC31 ARC
LINEAGE, displayed once in full for the record: VC31 (full audit of the
REVISION-29 text) returned **0 critical / 2 gaps** — the 43-pattern
census EXACT, NINE rows recounted, every r30 fold verified at its sites;
gap 1 = a truncated appendix slice (AUDIT-INPUT — the saved orchestrator
checklist extended to completeness), gap 2 = the (H1) fence display
named only the e_j ≥ 2 complement of the proved union (report
`lean/notes/openmath/VC31_report.md`, archived f9aa7ef). The cure for
gap 2: the dated **r30-ERRATUM** bracket (f9aa7ef — visible in the (H1)
row) made the fence name the ENTIRE complement (orders ≥ 4 incl.
all-e_j = 1 explicit; DISPLAY ONLY — the proved union was always exact).
VC31b (the narrow companion on the completed appendix; report
`VC31b_report.md`, a63b9ef) PASSED both substance checks (the complete
GRB slice + the erratum fence) and raised the dating observation, cured
by the standing **DATING CONVENTION disclosure** (a63b9ef: window-label
dating on artifacts/entries; git commit order = the AUTHORITATIVE
chronology). **VC31c CONFIRMED, 0 critical / 0 gaps** (report
`VC31c_report.md`, 620417b) — the sequencing claims verifiable from the
machine-recorded commit chain regardless of label dates; no claim found
that depends on a wall-clock date. Now INSIDE the certified anchor: the
TR3-S3 acceptance (the strata trilogy), the erratum-corrected fence, and
the dating-convention disclosure itself. On the newly re-anchored text
this revision folds the **WAVE-14 batch** (five ledger-recorded units)
plus housekeeping. NO roster change: **NINE rows** recounted; no clause
attribution moved; this unit ran no audit and NEVER self-confirms —
**VC32 is OWED on this REVISION-30 wording**.

**The §29/§30/§33/§34/§35 ASVIN FLAG — lineage continued; ONE item
EXECUTED-under-authority added, ONE item promoted to TOP.** The standing
Asvin batch, RESTATED IN FULL: (1) the `TowerMoveClosureL` two-layer
re-ratification; (2) the fence-(vii) ROUTE (a) ratify-or-reopen lineage —
the GR route (a) + the RECSL `hσt`-pin rider + NEW: the **EXECUTED
F-ADM-3 re-key** (ba02d60+8648d99, applied under the STANDING
DEFINITION-CHANGE AUTHORITY with Codex diff confirmation — recorded here
for ratify-or-reopen, the lineage's first executed-then-ratify entry);
(3) **the FGMN four-token proposal — the flag lineage's TOP item** (the
σ.w → σ.wPrev re-key of `SideReads` clauses (i)/(ii), verbatim diff at
FGMN_REDERIVATION §S3; PROPOSAL ONLY, hostile pass 1 RUNNING; the
adjudication is Asvin's statement-change decision point); (4) the
Route-B **RB-DELTA decision, now DECISION-READY** (the (UB-X) walk on
file = condition (5)'s substance; the note's §6 is the one-paragraph
decision: full-scope RB-DELTA vs the clause-(R)-only narrow fallback).

**Disposition table.**

| item | ground | grade | disposition | edit sites |
|---|---|---|---|---|
| r31-1 | ledger 620417b "VC31 + VC31b + VC31c CONFIRMED" (VC31 0C/2G f9aa7ef → r30-ERRATUM f9aa7ef → VC31b a63b9ef → DATING CONVENTION disclosure a63b9ef → VC31c 0C/0G 620417b) | ACCEPTANCE RE-ANCHOR — the citation-text update rides this revision per the VC22 precedent; the VC31 arc lineage displayed at every operative site | every operative anchor site updated: the §0 REVISION-29 paragraph header superseded and the NEW REVISION-30 standing paragraph states the anchor (REVISION 29/VC31+VC31b+VC31c) with the full arc lineage; the §3.2 end-to-end row gains the VC31+VC31b+VC31c sync tail (RE-ANCHORS on the errata-corrected REVISION 29; VC32 owed on THIS text); the §6 r30 bracket's anchor sentence carries a dated supersession bracket and the new r31 bracket is current. Historical records (§§7–35) untouched beyond the named dated brackets (the (H1) r30-ERRATUM bracket predates this unit, f9aa7ef) | §0, §3.2 end-to-end row, §6 |
| r31-2 | ledger 303b11e (note `FGMN_REDERIVATION_2026-08-08.md`; harness 1,093 checks ALL GREEN first pass; PDF + `-layout` extraction sha256-pinned into `docs/references/`) | PROPOSAL DISPLAY — ON RECORD, NOT applied; nothing proved, nothing edited in Lean; hostile pass 1 RUNNING; Asvin-gated | the (H1) row gains the r31 bracket item (1): THE DIAGNOSIS (the compiled `SideReads` clauses (i)/(ii) DOUBLE-SCALE the stride weight vs the print's parent-valuation keying; the JuncForge parity wall = the artifact's arithmetic shadow); **THE ROW-A SOURCE VERDICT** (defined term, note §S4) — the source glue step FIRES at row A, the parity wall + C-e escapee TRANSCRIPTION ARTIFACTS; the FOUR-TOKEN σ.w → σ.wPrev re-key displayed as PROPOSAL (verbatim diff at note §S3); **the JuncForge refutation theorems REMAIN TRUE of the old clause** (statements unedited, meanings unchanged); the flag lineage's TOP item (paragraph above). **PIN-GEN-i DISCHARGED as the rider** (note §S7: fresh decorrelated `-layout` extraction, Def 3.15's prime placement COLUMN-VERIFIED = the mode-C reading, genericity hand-verified, from-print operator tie 336/336; residual displayed: single extractor pass) — the (H1) PIN-GEN-i displays updated (the r30 bracket gains the dated discharged-pointer; the TR3S3/GRB note brackets ride their own next revisions) | §0, §3.1 (H1) row, §6 |
| r31-3 | ledger f90d68a (note `HV3_DISCHARGE_2026-08-08.md`; `HigherNewton.tex` re-fetched byte-identical to the recorded sha256 and PINNED at `docs/references/`) | DISCHARGE-PENDING-VERIFICATION — ON RECORD; hostile pass 1 RUNNING; NOTHING upgraded, NO bracket edited | the (H1) row gains the r31 bracket item (2): **HV3-THM** (defined term, note §D0) = v₃ = w₂ at e₀ = e₁ = 1 (e₂ FREE), a theorem given ONLY the FOUR displayed conditions [r31-ERRATUM, VC32 gap 2: (iv) the dictionary consumptions, added at HV3 REVISION 1] ((i) the standing print-attribution baseline for the pinned generic displays — Def 2.5, the H_{r−1} mapping, Prop 2.6, Prop 2.7(3)(4), with the r = 2 instantiation reproducing the accepted TR3-S1 pin VERBATIM; (ii) accepted TR3-S1; (iii) the standing (H-CHAIN)-at-depth-3 + Lemma RESCALE consumptions); IF it survives its bar, **TR-3′-GEN's order-3 scope becomes UNCONDITIONAL ON PRINT**; the 47 listed (H-v3) sites (TR3S3 30 / ROOT 14 / GRB 3) RIDE THE ACCEPTANCE — none edited here | §0, §3.1 (H1) row, §6 |
| r31-4 | ledger 8648d99 (seal pair ba02d60 + 8648d99; note `FADM3_REKEY_2026-08-08.md`; blueprint execution record in `HDISCHARGE_H3.md`) | DEFINITION CHANGE EXECUTED under the standing authority (Codex diff confirmation CONFIRM) + Lean THEOREMS — the (ADM) row STAYS [M] on its surviving opens | the (ADM) row gains the r31 bracket: `wphi` re-keyed to the O-9 §4 top-normalized ledger (old body displayed); **CLASS-LAT MACHINE-CHECKED at every order** (`classCard_eq_d` Lean-core) + **`hclass` RETIRED** (`classCount_eq_d` proved; `admFull_iff_maxClassWeight` hypothesis-free, old form displayed); full 33-module cone green; capstone fence verified EMPTY intersection; the R7 acceptance test PASSED (88 → 0; historical twin == 88 exactly; REKEY-CLEAN the note's defined verdict); F-ADM-3 LEAVES the open list; the executed edit recorded in the flag lineage for ratify-or-reopen | §0, §3.1 (ADM) row, §6, the flag paragraph |
| r31-5 | ledger 48c07b4 (seal 76513f8 pre-run; note `TWIST3_PROBE_2026-08-08.md`; 23,733 samples, rerun JSON-identical) | MEASUREMENT — instances not proofs; the honest RED disclosed; TR3-S4 CHARTERED (the F2b rule: a compose unit fires only on an exact measured law) | the (H1) row gains the r31 bracket item (3): the ǫ-recurrence REVIVES at order-3 e₁ ≥ 2 with EXACTLY the sealed character — **(TWIST-3) θ₂ = ℓ₁g₁γ₂, EXACT 0/1,656**; IDENT-3-persists refuted 710/710; KEY0/ORD-INV lift confirmed; **CERT3 = the first PARI-certified RAMIFIED order-3 keys in the genre**; HONESTY: exit 1 — the P4′ SECONDARY display clause RED 10/24, root-caused post-hoc, ZERO harness amendments, the red stands as committed; **TR3-S4 CHARTERED** with the route on record + the verifier-owned from-note implementation REQUIRED at acceptance (pass-7 F2c); the next stratum (e₀ ≥ 2 / e₂ ≥ 2) probe-first, NO candidate sealed | §0, §3.1 (H1) row, §6 |
| r31-6 | ledger 4924dcb (note `UBX_WALK_2026-08-08.md`; doc walk, no Lean edits) | ADJUDICATION INPUT ON FILE — nothing adopted, nothing discharged; the Asvin gate is SUBSTANTIVE (the display-only branch REFUTED) | the (UB-X) row gains the r31 bracket: **RB-DELTA is CONSUMER-VISIBLE, not display-only** at all THREE site families (clause (R)/K3-c: NEED-EXACT, strictly improving; clause (UB)/Step 16: supply NARROWING with NO on-file consumer of the dropped linearity conjunct, honest limit displayed — the clause-(UB) composition is unwritten; the Scaffold typed faces: Prop-content change, derivation shape preserved); F-B (1)–(4) SATISFIED; **condition (5) = Asvin sign-off + the ROOT owner's acceptance pass = the ONLY open gate**; the note's §6 = the decision-ready paragraph — the Route-B gate joins the ASVIN BATCH decision-ready | §0, §3.1 (UB-X) row, §6, the flag paragraph |
| r31-7 | housekeeping (the /goal cadence) + cadence (the VC22 precedent on the re-anchored text) | TRACKING DISPLAY + CADENCE — VC32 OWED; never self-confirm | **SYNTHESIS PASS 8 is NOT YET OWED** (pass 7's queue is still executing: the FGMN + (H-v3) hostile passes RUNNING; the ITER-LAW attempt, the (H7) forge, the `DIterJunctionSupplier` statement, the banked-sorry attempt held). **WAVE-15 CANDIDATES displayed** (nothing dispatched here): the TR3-S4 compose + the held pass-7 items. The Asvin batch RESTATED IN FULL (the flag paragraph above). The sorry census of record stays the r27 display (36 repo-wide / B2D 0 — this fold is doc-only; the F-ADM-3 unit re-verified zero NEW sorry at its own commits). REVISION 30 is a text change on the newly re-anchored text: the confirming pass **VC32 is OWED on the REVISION-30 wording** (charge below; model diversity per the tree §4 bar); until it lands, the acceptance citation points at the REVISION-29 text; VC32 is a fresh-context pass, not this unit | §0, §3.2 end-to-end row, §6, this record |

**THE ENUMERATION AUDIT (the r13 rule, run over the post-edit text).**
Unit: OCCURRENCE COUNTS (`grep -o | wc -l`, exact case-sensitive patterns,
the r23-declared unit), taken on the FINAL text of this revision (counts
include this record's own quotations — the r23–r30 precedent). The §35
forty-three patterns plus EIGHT markers introduced at r31 (`VC31b`,
`VC31c`, `VC32` NEW at the cadence — `VC31b`/`VC31c` baseline 0, they
were named nowhere before this fold; `HV3-THM`, `TR3-S4`, `RB-DELTA`,
`classCount_eq_d` NEW at the wave-14 folds, baseline 0; `wPrev` NEW at
the FGMN proposal display, baseline 1 — the historical O-14a
conspiracy-exclusion display). The r30 column = the §35 recorded census;
the r30-ERRATUM's own post-census contribution (f9aa7ef, pre-dating this
unit: +1 at `VC31`) is folded into that row's reason. Every delta named:

| grep (occurrences) | r30 | r31 | delta | why |
|---|---:|---:|---:|---|
| `(R1)–(R4)` | 119 | 120 | +1 | no operative change — this table's own pattern cell |
| `(H4a)` | 199 | 200 | +1 | no operative change — this table's own pattern cell |
| `K3-δ` | 131 | 137 | +6 | the (UB-X) r31 bracket's FOUR fragment displays (the site-1 need-exact read, the scope-mismatch note, the as-worded close pair) + this table's own cells; status UNCHANGED — clause (R) keeps consuming (K3-δ) until the Asvin gate fires |
| `SITE-EXH` | 33 | 34 | +1 | no arc status change — this table's own pattern cell |
| `GRADED-READ` | 27 | 28 | +1 | no operative change — this table's own pattern cell |
| `TransDeep` + `TRANS-DEEP` | 45 | 47 | +2 | no operative change — both increments are this row's own joint pattern cell (one hit each) |
| `TEN rows` | 35 | 36 | +1 | historical-only — this table's own pattern cell |
| `NINE rows` | 51 | 59 | +8 | roster restated NINE, no change — the §0 REVISION-30 paragraph (the arc display + the recount line), the §3.2 sync tail (twice), this record's opening/arc-lineage/close, and this table's own cell |
| `VC25` | 57 | 58 | +1 | closed history — this table's own pattern cell |
| `VC23` | 26 | 27 | +1 | named-but-unrun history stands — this table's own pattern cell |
| `VC26` | 104 | 105 | +1 | closed history — this table's own pattern cell |
| `ROUTE (a)` | 43 | 47 | +4 | the THREE full Asvin-batch restatements (the §0 batch line, the §6 r31 bracket's batch line, the flag paragraph) + this table's own cell; still zero sites claim route (a) proved anything |
| `TR3-S2` | 36 | 41 | +5 | the TWIST-3 fold displays the law as the level shift of TR3-S2's accepted order-2 θ (the (H1) r31 bracket) + this table's own cells (this row's namings + the fixed-point note); TR3-S2's own status unchanged |
| `REC-SL` | 41 | 42 | +1 | status unchanged — this table's own pattern cell |
| `RECSL` | 43 | 47 | +4 | the `hσt`-pin rider namings in the full Asvin-batch restatements (§0, §6 r31 bracket, the flag paragraph) + this table's own cell; status unchanged |
| `HUNI-V2` | 13 | 14 | +1 | closed history — this table's own pattern cell |
| `VC27` | 127 | 129 | +2 | closed history — this table's own cells (this row and the row below) |
| `VC27b` | 55 | 56 | +1 | closed history — this table's own pattern cell (each hit also counts one at the row above) |
| `VC28` | 74 | 76 | +2 | closed history — the charge paragraph's checklist-validation naming + this table's own pattern cell |
| `VC29` | 81 | 83 | +2 | closed history — the charge paragraph's checklist-validation naming + this table's own pattern cell; zero sites cite it as the current anchor |
| `POOL-DICT` | 48 | 49 | +1 | closed history — this table's own pattern cell |
| `TR-4` | 35 | 36 | +1 | residue-taxon status unchanged — this table's own pattern cell |
| `BasePolygonLengthLaw` | 41 | 42 | +1 | proved status stands — this table's own pattern cell |
| `ITER-LAW` | 37 | 41 | +4 | open-lemma status UNCHANGED — the held-queue displays (§0, the §6 r31 bracket, the r31-7 row) name the pending attempt + this table's own cell |
| `leakFree` | 64 | 65 | +1 | typed-face status unchanged — this table's own pattern cell |
| `LeakFreeCarrierV4` | 10 | 11 | +1 | no new consumption — this table's own pattern cell |
| `junc_bundle` | 79 | 80 | +1 | NO new verdict-tier restatement anywhere in this revision (the FGMN fold speaks of the `SideReads` clauses and the JuncForge THEOREMS, deliberately not restating the bundle verdict) — this table's own pattern cell only |
| `G-ADM-2` | 25 | 26 | +1 | closed routing history; the F-ADM-3 re-key discharges its re-run duty at the lattice level (stated in the note, not re-displayed here) — this table's own pattern cell |
| `BPLL` | 18 | 19 | +1 | proved status stands — this table's own pattern cell |
| `K1ConsumerFace` | 15 | 16 | +1 | discharged-face status stands — this table's own pattern cell |
| `N-H7-1` | 26 | 28 | +2 | the §6 r31 bracket's held-queue display names the forge on the two pins + this table's own cell; statements still OPEN |
| `VACUOUS-BY-TYPE` | 10 | 11 | +1 | the r28 verdict stands — this table's own pattern cell |
| `ADM-REAL` | 39 | 40 | +1 | acceptance history stands; the F-ADM-3 fold cites its OWN note (`FADM3_REKEY`), which does not hit this pattern — this table's own pattern cell only |
| `CLASS-LAT` | 17 | 24 | +7 | the machine-checked upgrade IS fold content: the (ADM) r31 bracket, §0, the §3.2 sync tail, the §6 r31 bracket, the r31-4 row, this record's close + this table's own cell; every site states Lean-core at every order |
| `JUNCFORGE` | 16 | 17 | +1 | the exact-case unit does NOT count the mixed-case `JuncForge` theorem namings that carry this revision's FGMN displays — this table's own pattern cell only |
| `TR3-S3` | 29 | 35 | +6 | accepted status unchanged — the inside-the-certified-anchor namings (§0, the §6 r31 bracket, this record's arc-lineage paragraph) + the (H-v3) stratum-containment display at the (H1) r31 bracket + this table's own cells (incl. the fixed-point note) |
| `VC30` | 91 | 109 | +18 | the anchor-move OLD-side statements at every re-anchor display (§0, the §3.2 sync tail, the §6 r31 bracket, this record) name REVISION 28/VC30+VC30b, and each `VC30b` hit counts here too (containment) + this table's own cells |
| `VC30b` | 37 | 47 | +10 | the OLD side of the anchor move, named at every re-anchor display + this table's own cell; every hit also counts one at the row above |
| `VC31` | 16 | 131 | +115 | the re-anchor IS this revision's cadence content: every arc-lineage display names VC31 AND its two suffixed companions (each `VC31b`/`VC31c` hit counts here too — containment), at §0, the §3.2 sync tail, the §6 supersession + r31 brackets, this record's headline/lineage/disposition/charge + these cells and the census header (the two suffixed companions contribute 71 of the increment by containment: 35 + 36); +1 of the increment is the (H1) r30-ERRATUM bracket's own naming (f9aa7ef, pre-dating this unit — the r30 column predates it) |
| `VC31b` | 0 | 35 | +35 | NEW at r31 — the narrow companion pass: every arc-lineage display + the census header + these cells; every hit also counts one at the row above (containment) |
| `VC31c` | 0 | 36 | +36 | NEW at r31 — the deciding companion pass: every arc-lineage display + the census header + these cells; every hit also counts one at the `VC31` row (containment) |
| `VC32` | 0 | 16 | +16 | NEW at r31 — the owed confirming pass: §0, the §3.2 sync tail, the §6 r31 bracket, this record's cadence/charge rows, the census header + these cells; VC32 NAMED-NOT-RUN — zero sites claim it ran |
| `H-v3` | 17 | 35 | +18 | the discharge-pending-verification displays ride every (H-v3) fold site WITH the standing conditionality display intact (§0, the (H1) r31 bracket, the §3.2 sync tail, the §6 r31 bracket, this record) + these cells; ZERO sites claim the discharge accepted — every new site says PENDING VERIFICATION or RIDES THE ACCEPTANCE |
| `HV3-THM` | 0 | 13 | +13 | NEW at r31 — the defined-term verdict of the discharge note, displayed at every (H-v3) fold site + the census header + these cells; zero sites consume it as accepted |
| `PIN-GEN-i` | 11 | 21 | +10 | the DISCHARGE is fold content: the rider display at §0, the (H1) r31 bracket, the r30 bracket's dated discharged-pointer, the §3.2 sync tail, the §6 r31 bracket, this record + these cells; every site pairs the discharge with the displayed residual (single extractor pass) |
| `TWIST-3` | 11 | 23 | +12 | the measured-law fold IS this revision's content: §0, the (H1) r31 bracket, the §3.2 sync tail, the §6 r31 bracket, this record's rows + these cells; every site states MEASURED/instances-not-proofs — zero sites claim a theorem at e₁ ≥ 2 |
| `TR3-S4` | 0 | 14 | +14 | NEW at r31 — the chartered compose unit: displayed at every TWIST-3 fold site + the wave-15 candidate lines + the census header + these cells; CHARTERED-NOT-RUN — zero sites claim it proved anything |
| `RB-DELTA` | 0 | 14 | +14 | NEW at r31 — the (UB-X) walk's defined term: the (UB-X) r31 bracket, §0, the §3.2 sync tail, the §6 r31 bracket, this record + the census header + these cells; every site states CONSUMER-VISIBLE + Asvin-gated, zero sites claim adoption |
| `wPrev` | 1 | 15 | +14 | the FGMN proposal display (the four-token σ.w → σ.wPrev re-key + the parent-scale diagnosis) at §0, the (H1) r31 bracket, the §6 r31 bracket, the flag paragraph, this record + these cells; the baseline 1 = the historical O-14a display; zero sites apply the re-key |
| `classCount_eq_d` | 0 | 6 | +6 | NEW at r31 — the proved replacement of `hclass`: the (ADM) r31 bracket, §0, the r31-4 row + the census header + this cell and the fixed-point note; every site states Lean-core proved |

Fixed point: the counts above were re-grepped AFTER the last edit of this
revision. The count-sensitive subtleties of §§31–35 carry over UNCHANGED
(containment, hyphen-disjointness, case-sensitivity, decl-name-visibility
— each re-checked, none changed sign); the r31-new ones: **`VC31b` ⊂
`VC31` and `VC31c` ⊂ `VC31`** — every suffixed hit counts once under the
parent (the `VC30b` genre, doubled); `VC32` is pairwise disjoint from the
other cadence names; `HV3-THM` does NOT hit `H-v3` (different characters)
and vice versa; `TR3-S4` is disjoint from `TR3-S2`/`TR3-S3`;
`classCount_eq_d` does NOT count the distinct Lean name `classCard_eq_d`;
`wPrev` counts its `σ.wPrev`/`ν.σ.wPrev` token displays.

**The re-derived row count (the r13 audit rule — counted, not copied).**
The §3.1 table's data rows enumerated directly after the edits: (H1) ·
(FRESH) = (H2) · (ADM) [(H3) member] · (UB-X) = (H4b) · (PACK) = (H5) ·
the (H6) terminal-seam group · (I-τ)/K-LOC = (H7) · (H8′) · (E-N) [(H3)
member]. THE COUNT IS **NINE** — unchanged; this revision touches row
CONTENT only (the (H1)/(ADM)/(UB-X) r31 brackets) and adds and retires
no row; nothing in the wave-14 batch changes any count.

**VC32 charge.** All §35 charge items as inherited ((xxxiv)–(lix) now
checked against the r31 wording — note (xxxiv) becomes: every operative
site cites REVISION 29/VC31+VC31b+VC31c, with the VC31 arc lineage
displayed (VC31 0C/2G → r30-ERRATUM f9aa7ef → VC31b + the dating
disclosure a63b9ef → VC31c 0C/0G) and NO site citing VC30+VC30b as the
current anchor), PLUS: (lx) the FGMN fold claims EXACTLY a proposal —
the four-token diff displayed NOT applied, hostile pass 1 RUNNING claimed
RUNNING only, the JuncForge refutation theorems displayed as REMAINING
TRUE of the old clause at every mention, and NO site stating `SideReads`
repaired; (lxi) the (H-v3) fold claims EXACTLY
discharge-pending-verification — HV3-THM's three conditions displayed at
every consumption site, NO bracket among the 47 edited, and NO site
claiming TR-3′-GEN unconditional-on-print as CURRENT; (lxii) the F-ADM-3
fold displays the executed definition change WITH the authority + Codex
confirmation + old-form displays, the (ADM) row STAYS [M] on
(B-PIN)/(C-JCT), and the flag lineage carries the executed edit for
ratify-or-reopen; (lxiii) the TWIST-3 fold displays the measured law as
instances-not-proofs WITH the honest P4′ RED (exit 1 stands, zero
amendments) at every verdict-tier site, and TR3-S4 as CHARTERED only;
(lxiv) the (UB-X) fold claims EXACTLY the walk verdict — RB-DELTA
CONSUMER-VISIBLE, condition (5) the only open gate, nothing adopted;
(lxv) the census above holds at the declared occurrence unit with the
stated delta-reasons. VC32 should receive the in-repo file listing +
commit resolutions + the COMPLETE provenance appendix with VERBATIM
slices per the saved checklist (memory: vc-audit-appendix-checklist —
validated at VC28 AND VC29; extended to completeness after VC31 gap 1).

**Bookkeeping.** Historical §§7–35 untouched except the dated additions
named in the disposition table (the §0 REVISION-29 paragraph header
superseded; the r31 brackets appended at the (H1)/(ADM)/(UB-X) rows; the
dated discharged-pointer inside the (H1) r30 bracket's PIN-GEN-i display;
the §3.2 sync tail; the §6 supersession bracket + the r31 bracket; the
(H1) r30-ERRATUM bracket was applied at f9aa7ef BEFORE this unit and is
cited, not edited). This is a DOC-ONLY revision: no Lean file is
touched, no statement changes (the F-ADM-3 statement changes happened at
their OWN sealed commits ba02d60+8648d99, folded here as record). NO
note bracket rides this unit (the TR3S3/GRB (H-v3) brackets ride the
HV3-THM acceptance; the FGMN application rides the Asvin gate). This
unit touches ROOT + `docs/PROJECT_STATE.md` (new r31 checkpoint
paragraph) + the ledger (`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`,
r31-executed entry) ONLY. The commit closing this revision is its unit
of record (unit ROOT r31; scoped add).

— REVISION 30 complete. VC31 + VC31b + VC31c CONFIRMED and the
acceptance RE-ANCHORED on the (errata-corrected) REVISION-29 text (the
anchor moves REVISION 28/VC30+VC30b → REVISION 29/VC31+VC31b+VC31c; the
VC31 arc lineage displayed — 0C/2G, the r30-ERRATUM, the dating
disclosure, VC31c 0C/0G); the WAVE-14 batch folded: the FGMN four-token
proposal ON RECORD not applied (THE ROW-A SOURCE VERDICT; PIN-GEN-i
DISCHARGED as rider), the (H-v3) discharge ON RECORD pending
verification (HV3-THM — if it survives, TR-3′-GEN order-3 goes
unconditional-on-print), F-ADM-3 RE-KEY APPLIED (CLASS-LAT
machine-checked at every order; `hclass` retired), the TWIST-3 law
MEASURED EXACT with the honest RED disclosed and TR3-S4 CHARTERED, and
the (UB-X) walk ON FILE (RB-DELTA CONSUMER-VISIBLE; the Route-B gate
decision-ready); NINE rows recounted; the census re-run at the declared
unit with every delta explained; **VC32 owed on this REVISION-30 text**
— until it lands, the acceptance citation points at the REVISION-29
text.

## 37. REVISION 31 (r32, the VC32+VC32b re-anchor + the HV3 acceptance fold) — the anchor moves REVISION 30; the strata trilogy goes PRINT-UNCONDITIONAL

**What this revision is.** The r32 cadence event: **VC32 + VC32b
CONFIRMED** (ledger e32ebef "VC32b CONFIRMED") — so **the acceptance
anchor moves REVISION 29/VC31+VC31b+VC31c → REVISION 30/VC32+VC32b** and
this revision executes the citation-text update at every operative site
(the VC22 precedent). The VC32 ARC LINEAGE, displayed once in full for
the record: VC32 (full audit of the REVISION-30 text) returned **0
critical / 3 gaps** — ALL folds PASSED (the FGMN proposal-only
discipline; no HV3 scope leak; the F-ADM-3 flag-lineage record; the
TWIST-3 honest RED; the UBX gate; roster NINE recounted; the 51-pattern
census EXACT), the gaps ALL fold-lag/wording syncs (report
`lean/notes/openmath/VC32_report.md`, archived e3425b1). The cure: the
dated **r31-ERRATUM** (e3425b1) — the FGMN/HV3 pass-1 status displays
synced to LANDED + REVISION 1 applied; the 336-SAT claim rescoped to the
window/support check; the (UB-X) gate wording harmonized to the §36
disposition. The narrow confirming pass **VC32b CONFIRMED, 0 critical /
0 gaps** on the corrected passages verbatim (report `VC32b_report.md`,
e32ebef — the VC30b/VC31b narrow-companion genre). Now INSIDE the
certified anchor: the FGMN proposal-on-record with THE ROW-A SOURCE
VERDICT, the HV3-THM discharge-pending display, the applied F-ADM-3
re-key, the TWIST-3 measured law + the TR3-S4 charter, the UBX walk
verdict, the four-item Asvin batch. On the newly re-anchored text this
revision folds the **HV3 ACCEPTANCE** (the wave's one acceptance) and
the **FGMN status sync** (a status display, nothing accepted). NO roster
change: **NINE rows** recounted; no clause attribution moved; this unit
ran no audit and NEVER self-confirms — **VC33 is OWED on this
REVISION-31 wording**.

**The §29/§30/§33/§34/§35/§36 ASVIN FLAG — lineage continued; the TOP
item RE-PRICED.** The standing Asvin batch, RESTATED IN FULL: (1) the
`TowerMoveClosureL` two-layer re-ratification; (2) the fence-(vii)
ROUTE (a) ratify-or-reopen lineage — the GR route (a) + the RECSL
`hσt`-pin rider + the EXECUTED F-ADM-3 re-key (ba02d60+8648d99, recorded
for ratify-or-reopen); (3) **the FGMN proposal — the flag lineage's TOP
item, RE-PRICED at this revision**: the change-set is now the COMPLETE
grep-disciplined DictIII perimeter of FGMN REVISION 4 (fa2180e) — the
THREE-token `SideReads` re-key (Defs.lean 285/287/292) + the primed
mirror set incl. the `SideClauseR2`/`sideReads_r2` pair + the
(†)-transfer twins with RE-DERIVED readCeil arithmetic at e ≥ 2 +
`Window.lean` :554/:557 + the R5 tie family; RECOMMENDED mode = the NEW
definition `SideReads'` (JuncForge theorems byte-frozen); PROPOSAL ONLY,
counter 0/2, passes 6+7 decide [r32-ERRATUM, VC33 gap 1: pass 6
(c7df512) landed concurrently, rides the next fold] — the adjudication is Asvin's
statement-change decision point; (4) the Route-B **RB-DELTA decision,
DECISION-READY** (the (UB-X) walk = condition (5)'s substance; the
note's §6 is the one-paragraph decision).

**Disposition table.**

| item | ground | grade | disposition | edit sites |
|---|---|---|---|---|
| r32-1 | ledger e32ebef "VC32b CONFIRMED" (VC32 0C/3G e3425b1 → r31-ERRATUM e3425b1 → VC32b 0C/0G e32ebef) | ACCEPTANCE RE-ANCHOR — the citation-text update rides this revision per the VC22 precedent; the VC32 arc lineage displayed at every operative site | every operative anchor site updated: the §0 REVISION-30 paragraph header superseded and the NEW REVISION-31 standing paragraph states the anchor (REVISION 30/VC32+VC32b) with the full arc lineage; the §3.2 end-to-end row gains the VC32+VC32b sync tail (RE-ANCHORS on the errata-corrected REVISION 30; VC33 owed on THIS text); the §6 r31 bracket's anchor sentence carries a dated supersession bracket and the new r32 bracket is current. Historical records (§§7–36) untouched beyond the named dated brackets | §0, §3.2 end-to-end row, §6 |
| r32-2 | ledger fa2180e "HV3 ACCEPTED" (note `HV3_DISCHARGE_2026-08-08.md` ACCEPTANCE RECORD: passes 4 Codex + 5 Fable b2e5b20 consecutive CLEAN 0C/0G, model-diverse; the pass-5 independent arXiv re-fetch byte-identical to the pin; the fresh 1,655-check two-way falsifier at off-roster p ∈ {7,17}; the ADVERSARIAL CONTROL — type-hood deleted → exactly the step-5 leg breaks) | ACCEPTANCE + DISCHARGE SWEEP EXECUTED — **(H-v3) is DISCHARGED**; **HV3-THM** stands (v₃ = w₂ at e₀ = e₁ = 1, e₂ FREE) on its FOUR standing conditions (the pinned-print attribution baseline; accepted TR3-S1; the standing (H-CHAIN)+RESCALE consumptions; the standing projective-dictionary consumptions — all standing rows, no bespoke hypothesis); **TR-3′-GEN is PRINT-UNCONDITIONAL at {all order-2} ∪ {order-3 all-e_j = 1}** — nothing becomes machine-checked or attribution-free that wasn't | the additive dated bracket "DISCHARGED at HV3 acceptance fa2180e — the scope is UNCONDITIONAL ON PRINT per HV3-THM's four standing conditions" applied at every OPERATIVE conditionality bracket: `TR3S3_2026-08-08.md` 11 discharge sites (the S0 hypothesis display; the three summary-table rows; the S1 sketch bullet; the S6.1 blockquote bracket + the S6.1 consumer clause; S6.2; S6.3; the S7 (H-v3) scope item; S9 item 1) + 2 PIN-GEN-i pointer sites (S7 transcription bullet, S9 item 6) + the new POST-ACCEPTANCE BRACKET (counters NOT reset); `GRB_ORDER2_2026-08-05.md` 2 discharge sites (the second bracket's CONDITIONALITY display; the Open-Lemma status line) + the new THIRD POST-ACCEPTANCE BRACKET (counters NOT reset); THIS DOCUMENT: the (H1) r30 bracket consequence display, the (H1) r31 bracket item (2) superseded-in-place, the §6 r30/r31 bracket tags, the §0/§3.2/§6-r32/§37 standing displays. **PIN-GEN-i stays DISPLAYED** (its owed leg discharged SEPARATELY at 303b11e — the FGMN §S7 rider; cited at every touched PIN-GEN-i site). The e₁ ≥ 2 (TWIST-3)/TR3-S4 regime is NOT reached (HV3-THM requires e₁ = 1) — the r32 NOTE at the (H1) TR3-S4 display says exactly that; the hypothesis displays there STAND. Reconciliation vs the D7 census below | §0, §3.1 (H1) row, §6, TR3S3 note, GRB note |
| r32-3 | ledger fa2180e (FGMN REVISION 4) + af88f12 (pass 5) | STATUS SYNC — nothing accepted, nothing applied; the proposal stays ASVIN-GATED | the (H1) r31 bracket item (1) gains the r32 STATUS SYNC bracket: the arc p1 0C/4G → REV1 (236d85c) → p3 Fable 0C/4G (EXACT execution leg) → REV2 (0170d68: THREE tokens of record; the NEW-definition mode RECOMMENDED) → p4 Codex 0C/3G → REV3 (f64307e) → **p5 Fable CRITICAL** (af88f12: the REVISION-3 mirror enumeration INCOMPLETE) → **REVISION 4** (fa2180e: the verifier's complete DictIII site list ADOPTED — the perimeter defined by the GREP DISCIPLINE); the application change-set PRICED UP in the Asvin-batch displays (§0, the §6 r32 bracket, the flag paragraph above); counter 0/2, passes 6+7 decide [r32-ERRATUM, VC33 gap 1: FGMN pass 6 (c7df512, 0C/2G GAPS-ONLY) landed CONCURRENTLY with this fold's assembly and is NOT folded — it rides the next fold; the counter display stands] | §0, §3.1 (H1) row, §6, the flag paragraph |
| r32-4 | housekeeping (the /goal cadence) + cadence (the VC22 precedent on the re-anchored text) | TRACKING DISPLAY + CADENCE — VC33 OWED; never self-confirm | **SYNTHESIS PASS 8 is NOT YET OWED** (the FGMN arc still executing [r32-ERRATUM, VC33 gap 1: pass 6 landed concurrently, rides the next fold]). **WAVE-15 CANDIDATES displayed** (nothing dispatched here): **TR3-S4 the lead** (the compose unit on the measured TWIST-3 law; verifier-owned implementation required at acceptance) + the held pass-7 items (the ITER-LAW attempt, the (H7) restatement forge, the `DIterJunctionSupplier` statement, the banked-sorry attempt). The Asvin batch RESTATED IN FULL (the flag paragraph above). The sorry census of record stays the r27 display (36 repo-wide / B2D 0 — this fold is doc-only). REVISION 31 is a text change on the newly re-anchored text: the confirming pass **VC33 is OWED on the REVISION-31 wording** (charge below; model diversity per the tree §4 bar); until it lands, the acceptance citation points at the REVISION-30 text; VC33 is a fresh-context pass, not this unit | §0, §3.2 end-to-end row, §6, this record |

**THE DISCHARGE-SITE RECONCILIATION (counted, not copied — the D7
census vs this unit's own sweep).** The HV3 note's D7 census, taken at
its authoring commit (f90d68a, pre-r31), counted **47 live (H-v3)
mention-lines: TR3S3 30 / ROOT 14 / GRB 3** (mention-line unit, exact
line lists in the note). This unit's own pre-sweep re-count at HEAD
(same `grep -n 'H-v3'` line unit, the same three files): **TR3S3 30
(UNCHANGED — matches D7 line-for-line), GRB 3 (UNCHANGED), ROOT 27** —
the ROOT +13 vs D7 is the r31 fold's OWN (H-v3) content, all
post-authoring and all expected: the §0/§3.2/§6 pending-verification
displays, the (H1) r31 bracket item (2), the §36 record's rows and
census cells, and the r31-ERRATUM tag. Total at this unit's start:
**60 mention-lines**, of which D7's 47 remain identifiable one-for-one
(the 30+3 exactly; D7's ROOT 14 at shifted line numbers). SWEEP
DISPOSITION, every line accounted: TR3S3 — its 30 pre-sweep lines split
17 + 1 + 5 + 6 + 1: 17 lines inside the 11 BRACKETED operative
conditionality sites (listed at r32-2), 1 S6.1 heading line (carried by
the blockquote bracket directly beneath it), 5 proof-internal
consumption lines NOT bracketed (S0 ordinate line, S1 extension + pins
lines, S5(i) ×2 — they consume the S0-displayed hypothesis, whose
display site carries the discharge), 6 REVISION-1 historical record
lines NOT bracketed (the post-acceptance discipline), and 1
acceptance-record line with the new POST-ACCEPTANCE BRACKET sitting
directly beneath it; GRB — 2 operative sites BRACKETED + the
THIRD POST-ACCEPTANCE BRACKET added, 0 left unbracketed; ROOT — the
operative displays bracketed/superseded-in-place (the (H1) r30/r31
brackets, the §6 r30/r31 brackets), the historical §§33–36 records, the
superseded §0 paragraphs, and census cells NOT edited (bookkeeping
rule). The sweep's uniform greppable core is the string
"DISCHARGED at HV3 acceptance fa2180e": TR3S3 12 (10 discharge sites + S9-item-1 +
the post-acceptance bracket), GRB 3, ROOT 6 — every occurrence additive,
zero old bracket text removed. Ancillary files (PROJECT_STATE,
SYNTHESIS_PASS7, STRATA3, pass/VC reports) are NOT proof-note brackets
(D7's own ruling): PROJECT_STATE gets its r32 checkpoint + dated tags
in this unit's commit; the others stand as historical records.

**THE ENUMERATION AUDIT (the r13 rule, run over the post-edit text).**
Unit: OCCURRENCE COUNTS (`grep -o | wc -l`, exact case-sensitive patterns,
the r23-declared unit), taken on the FINAL text of this revision (counts
include this record's own quotations — the r23–r31 precedent). The §36
fifty-one patterns plus FOUR markers introduced at r32 (`VC32b`, `VC33`
NEW at the cadence, baseline 0 — named nowhere before this fold;
`SideClauseR2`, `readCeil` NEW at the FGMN REVISION-4 status sync,
baseline 0). The r31 column = the §36 recorded census; the r31-ERRATUM's
own post-census contribution (e3425b1, pre-dating this unit: +5 at
`VC32`, the three §0 gap tags + the (H1) row tag + the §36 r31-3 row
tag) is folded into that row's reason. Every delta named:

| grep (occurrences) | r31 | r32 | delta | why |
|---|---:|---:|---:|---|
| `(R1)–(R4)` | 120 | 121 | +1 | no operative change — this table's own pattern cell |
| `(H4a)` | 200 | 201 | +1 | no operative change — this table's own pattern cell |
| `K3-δ` | 137 | 138 | +1 | status unchanged (clause (R) keeps consuming it until the Asvin gate fires) — this table's own pattern cell |
| `SITE-EXH` | 34 | 35 | +1 | no arc status change — this table's own pattern cell |
| `GRADED-READ` | 28 | 29 | +1 | no operative change — this table's own pattern cell |
| `TransDeep` + `TRANS-DEEP` | 47 | 49 | +2 | no operative change — both increments are this row's own joint pattern cell (one hit each) |
| `TEN rows` | 36 | 37 | +1 | historical-only — this table's own pattern cell |
| `NINE rows` | 59 | 64 | +5 | roster restated NINE, no change — the §0 REVISION-31 paragraph, the §3.2 sync tail, this record's opening/close/recount, and this table's own cell |
| `VC25` | 58 | 59 | +1 | closed history — this table's own pattern cell |
| `VC23` | 27 | 28 | +1 | named-but-unrun history stands — this table's own pattern cell |
| `VC26` | 105 | 106 | +1 | closed history — this table's own pattern cell |
| `ROUTE (a)` | 47 | 51 | +4 | the THREE full Asvin-batch restatements (§0, the §6 r32 bracket, the flag paragraph) + the §6 r31-bracket's batch line unedited + this table's own cell; still zero sites claim route (a) proved anything |
| `TR3-S2` | 41 | 42 | +1 | accepted status unchanged — this table's own pattern cell |
| `REC-SL` | 42 | 43 | +1 | status unchanged — this table's own pattern cell |
| `RECSL` | 47 | 51 | +4 | the `hσt`-pin rider namings in the full Asvin-batch restatements (§0, §6 r32 bracket, the flag paragraph) + this table's own cell; status unchanged |
| `HUNI-V2` | 14 | 15 | +1 | closed history — this table's own pattern cell |
| `VC27` | 129 | 131 | +2 | closed history — this table's own cells (this row and the row below) |
| `VC27b` | 56 | 57 | +1 | closed history — this table's own pattern cell (each hit also counts one at the row above) |
| `VC28` | 76 | 78 | +2 | closed history — the VC33-charge's checklist-validation naming + this table's own pattern cell |
| `VC29` | 83 | 85 | +2 | closed history — the VC33-charge's checklist-validation naming + this table's own pattern cell |
| `POOL-DICT` | 49 | 50 | +1 | closed history — this table's own pattern cell |
| `TR-4` | 36 | 37 | +1 | residue-taxon status unchanged — this table's own pattern cell |
| `BasePolygonLengthLaw` | 42 | 43 | +1 | proved status stands — this table's own pattern cell |
| `ITER-LAW` | 41 | 45 | +4 | open-lemma status UNCHANGED — the held-queue displays (§0, the §6 r32 bracket, the r32-4 row) + this table's own cell |
| `leakFree` | 65 | 66 | +1 | typed-face status unchanged — this table's own pattern cell |
| `LeakFreeCarrierV4` | 11 | 12 | +1 | no new consumption — this table's own pattern cell |
| `junc_bundle` | 80 | 81 | +1 | NO new verdict-tier restatement anywhere in this revision — this table's own pattern cell only |
| `G-ADM-2` | 26 | 27 | +1 | closed routing history — this table's own pattern cell |
| `BPLL` | 19 | 20 | +1 | proved status stands — this table's own pattern cell |
| `K1ConsumerFace` | 16 | 17 | +1 | discharged-face status stands — this table's own pattern cell |
| `N-H7-1` | 28 | 30 | +2 | the held-queue displays (§6 r32 bracket, the r32-4 row) + this table's own cell; statements still OPEN |
| `VACUOUS-BY-TYPE` | 11 | 12 | +1 | the r28 verdict stands — this table's own pattern cell |
| `ADM-REAL` | 40 | 41 | +1 | acceptance history stands — this table's own pattern cell |
| `CLASS-LAT` | 24 | 25 | +1 | machine-checked status stands — this table's own pattern cell |
| `JUNCFORGE` | 17 | 18 | +1 | the exact-case unit does NOT count the mixed-case `JuncForge` namings in this revision's FGMN displays — this table's own pattern cell only |
| `TR3-S3` | 35 | 36 | +1 | accepted status unchanged (this revision's TR3S3-note citations are hyphenless file names, not this pattern) — this table's own pattern cell |
| `VC30` | 109 | 116 | +7 | closed history; the narrow-companion genre citations at the new re-anchor displays name VC30b (each hit counts here too — containment) + this table's own cells |
| `VC30b` | 47 | 53 | +6 | the narrow-companion genre precedent named at the §0/§3.2/§6-r32 re-anchor displays + this table's own cell; every hit also counts one at the row above |
| `VC31` | 131 | 159 | +28 | the OLD side of the anchor move, named at every re-anchor display (§0, the §3.2 sync tail, the §6 supersession + r32 brackets, this record) with its two suffixed companions (each `VC31b`/`VC31c` hit counts here too — containment) + these cells |
| `VC31b` | 35 | 47 | +12 | the OLD-side arc + the narrow-companion genre namings + the census header + this cell; every hit also counts one at the row above (containment) |
| `VC31c` | 36 | 44 | +8 | the OLD-side arc namings + this cell; every hit also counts one at the `VC31` row (containment) |
| `VC32` | 16 | 93 | +77 | the re-anchor IS this revision's cadence content: every arc-lineage display names VC32 AND its suffixed companion (each `VC32b` hit counts here too — containment; the companion contributes 35 of the increment), at §0, the §3.2 sync tail, the §6 supersession + r32 brackets, this record's headline/lineage/disposition/charge + these cells and the census header; +5 of the increment is the r31-ERRATUM's own pre-dating contribution (e3425b1 — the r31 column predates it) |
| `VC32b` | 0 | 35 | +35 | NEW at r32 — the narrow companion pass: every arc-lineage display + the census header + these cells; every hit also counts one at the row above (containment) |
| `VC33` | 0 | 18 | +18 | NEW at r32 — the owed confirming pass: §0, the §3.2 sync tail, the §6 r32 bracket, this record's cadence/charge rows + the two charge-citing reason cells above, the census header + these cells; VC33 NAMED-NOT-RUN — zero sites claim it ran |
| `H-v3` | 35 | 56 | +21 | the DISCHARGE is this revision's content: the §0 REVISION-31 paragraph, the (H1) r30/r31 bracket tags, the §3.2 sync tail, the §6 tags + r32 bracket, this record's rows + the reconciliation + these cells; every NEW site says DISCHARGED (or, at the TR3-S4/e₁ ≥ 2 sites, explicitly OUT OF SCOPE); the pre-existing pending-verification displays stand as superseded history with dated tags |
| `HV3-THM` | 13 | 33 | +20 | the accepted theorem is displayed at every discharge site + the four-condition closure restatements (§0, the (H1) tags, §6, this record) + these cells; every site states the four standing conditions, zero sites claim more than v₃ = w₂ at e₀ = e₁ = 1 |
| `PIN-GEN-i` | 21 | 31 | +10 | the stays-displayed rider: every discharge display pairs the (H-v3) discharge with the pin's SEPARATE 303b11e discharge cite (§0, the (H1) r30 bracket tag, the §6 r30 bracket tag, the r32-2 row) + these cells |
| `TWIST-3` | 23 | 37 | +14 | measured-law status unchanged — the NOT-REACHED displays (HV3-THM requires e₁ = 1) at §0, the (H1) r32 NOTE, the §6 r32 bracket, this record + these cells; zero sites claim a theorem at e₁ ≥ 2 |
| `TR3-S4` | 14 | 29 | +15 | CHARTERED-NOT-RUN status unchanged — the wave-15 LEAD displays (§0, §6 r32 bracket, the r32-4 row) + the NOT-REACHED displays + these cells; zero sites claim it proved anything |
| `RB-DELTA` | 14 | 18 | +4 | the Asvin-batch restatements (§0, §6 r32 bracket, the flag paragraph) + this cell; every site states CONSUMER-VISIBLE + Asvin-gated, zero sites claim adoption |
| `wPrev` | 15 | 16 | +1 | proposal status unchanged (this revision's FGMN sync displays the THREE-token diff by line numbers, not by token) — this table's own pattern cell |
| `classCount_eq_d` | 6 | 7 | +1 | proved status stands — this table's own pattern cell |
| `SideClauseR2` | 0 | 8 | +8 | NEW at r32 — the FGMN REVISION-4 perimeter member, displayed at the status syncs (§0, the (H1) r32 STATUS SYNC, the §6 r32 bracket, the flag paragraph, the r32-3 row) + the census header + this cell; zero sites claim any Lean edit |
| `readCeil` | 0 | 10 | +10 | NEW at r32 — the (†)-transfer twins' re-derivation requirement, displayed at the same status syncs + the census header + this cell; zero sites claim the arithmetic done |

Fixed point: the counts above were re-grepped AFTER the last edit of this
revision. The count-sensitive subtleties of §§31–36 carry over UNCHANGED
(containment, hyphen-disjointness, case-sensitivity, decl-name-visibility
— each re-checked, none changed sign); the r32-new ones: **`VC32b` ⊂
`VC32`** — every suffixed hit counts once under the parent (the
`VC30b`/`VC31b` genre); `VC33` is pairwise disjoint from the other
cadence names; `SideClauseR2` and `readCeil` are disjoint from every
prior pattern (in particular `SideClauseR2` does not hit the historical
`SideClauseR1` namings and `readCeil` is a distinct camel-case token);
the sweep string "DISCHARGED at HV3 acceptance fa2180e" contains neither
`H-v3` nor any census pattern — the discharge brackets inflate no
pre-existing row.

**The re-derived row count (the r13 audit rule — counted, not copied).**
The §3.1 table's data rows enumerated directly after the edits: (H1) ·
(FRESH) = (H2) · (ADM) [(H3) member] · (UB-X) = (H4b) · (PACK) = (H5) ·
the (H6) terminal-seam group · (I-τ)/K-LOC = (H7) · (H8′) · (E-N) [(H3)
member]. THE COUNT IS **NINE** — unchanged; this revision touches row
CONTENT only (the (H1) r32 tags/brackets) and adds and retires no row;
the HV3 acceptance discharges a CONDITIONALITY BRACKET on member (a)'s
residue displays, not a row or member.

**VC33 charge.** All §36 charge items as inherited ((xxxiv)–(lxv) now
checked against the r32 wording — note (xxxiv) becomes: every operative
site cites REVISION 30/VC32+VC32b, with the VC32 arc lineage displayed
(VC32 0C/3G → r31-ERRATUM e3425b1 → VC32b 0C/0G e32ebef) and NO site
citing VC31+VC31b+VC31c as the current anchor), PLUS: (lxvi) the HV3
fold claims EXACTLY an acceptance-plus-sweep — (H-v3) DISCHARGED
at fa2180e with the four standing conditions displayed at every discharge
site, the sweep ADDITIVE everywhere (old bracket text intact, accepted
notes' counters not reset), the reconciliation table above reproducible
by grep (mention-line counts 30/27/3 pre-sweep; the core string at
TR3S3 12 / GRB 3 / ROOT 6), and NO site claiming anything became machine-checked or
attribution-free; (lxvii) the TR3-S4/e₁ ≥ 2 displays are NOT discharged
— every such site either keeps the hypothesis displayed or states
out-of-scope explicitly; (lxviii) the FGMN fold claims EXACTLY a status
sync — pass 5 CRITICAL + REVISION 4 displayed, the perimeter
grep-disciplined, the change-set priced UP in all three Asvin-batch
restatements, counter 0/2 with passes 6+7 deciding, NO site stating the
proposal applied or `SideReads` repaired; (lxix) PIN-GEN-i displays pair
the pin (stays displayed) with the separate 303b11e discharge at every
touched site; (lxx) the census above holds at the declared occurrence
unit with the stated delta-reasons. VC33 should receive the in-repo
file listing + commit resolutions + the COMPLETE provenance appendix
with VERBATIM slices per the saved checklist (memory:
vc-audit-appendix-checklist — validated at VC28, VC29, and VC32).
[r32-ERRATUM, VC33 gap 2: AUDIT-INPUT ARTIFACT, not a text defect —
VC33's read-only repository sandbox failed to launch (missing
bubblewrap), so the charge's grep-based checks (the 12/3/6 core-string
count, the 55-pattern census, the NINE-row recount, the line-anchored
falsifier, the verbatim file-to-appendix comparison) were NOT
independently executed; VC33 verified the displayed arithmetic as
internally consistent only. NO content is edited under this bracket —
the displayed counts stand as authored. DISPOSITION: the execution
duty transfers to the narrow confirming pass (VC33b), which must
either run in an environment that can execute the greps or receive
pinned raw grep transcripts as audit inputs — hostile verification
requires execution, not acceptance.]

**Bookkeeping.** Historical §§7–36 untouched except the dated additions
named in the disposition table (the §0 REVISION-30 paragraph header
superseded; the r32 tags at the (H1) r30/r31 brackets and the TR3-S4
display; the §3.2 sync tail; the §6 supersession bracket + the two dated
tags + the r32 bracket). This is a DOC-ONLY revision: no Lean file is
touched, no statement changes. The note brackets riding this unit: the
TR3S3 POST-ACCEPTANCE BRACKET + its 11 site-level brackets; the GRB
THIRD POST-ACCEPTANCE BRACKET + its 2 site-level brackets (both notes'
acceptance counters NOT reset). This unit touches ROOT + the TR3S3 note
+ the GRB note + `docs/PROJECT_STATE.md` (new r32 checkpoint paragraph +
dated tags at its three stale (H-v3) status displays — the r30
conditional display, the r31 pending display, the r31 frontier line;
its census-pattern list line is not a status display) + the ledger
(`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`, r32-executed entry)
ONLY. The commit closing this revision is its unit of record (unit ROOT
r32; scoped add).

— REVISION 31 complete. VC32 + VC32b CONFIRMED and the acceptance
RE-ANCHORED on the (errata-corrected) REVISION-30 text (the anchor moves
REVISION 29/VC31+VC31b+VC31c → REVISION 30/VC32+VC32b; the VC32 arc
lineage displayed — 0C/3G, the r31-ERRATUM, VC32b 0C/0G); the HV3
ACCEPTANCE folded: (H-v3) DISCHARGED at fa2180e (passes 4+5 consecutive
CLEAN, model-diverse; the adversarial control on record) — **TR-3′-GEN
is PRINT-UNCONDITIONAL at {all order-2} ∪ {order-3 all-e_j = 1}** per
HV3-THM's four standing conditions, the 47-site bracket discharge sweep
EXECUTED additively across TR3S3/ROOT/GRB with the reconciliation on
record, PIN-GEN-i displayed with its separate 303b11e discharge, the
e₁ ≥ 2 regime untouched; the FGMN status synced (pass 5 CRITICAL →
REVISION 4; the complete DictIII perimeter; the change-set priced UP;
passes 6+7 decide; Asvin-gated; [r32-ERRATUM, VC33 gap 1] pass 6
landed concurrently, rides the next fold); NINE rows recounted; the census re-run
at the declared unit with every delta explained; **VC33 owed on this
REVISION-31 text** — until it lands, the acceptance citation points at
the REVISION-30 text.

## 38. REVISION 32 (r33, the VC33+VC33b+VC33c re-anchor + the FGMN acceptance fold) — the anchor moves REVISION 31; the glue route's source question is ANSWERED

**What this revision is.** The r33 cadence event: **VC33 + VC33b +
VC33c CONFIRMED** (ledger 1de8ec2 "VC33c CONFIRMED") — so **the
acceptance anchor moves REVISION 30/VC32+VC32b → REVISION
31/VC33+VC33b+VC33c** and this revision executes the citation-text
update at every operative site (the VC22 precedent). The VC33 ARC
LINEAGE, displayed once in full for the record: VC33 (full audit of the
REVISION-31 text) returned **0 critical / 2 gaps** — gap 1 the FGMN
pass-6 concurrency displays (fold-lag), gap 2 an AUDIT-INPUT ARTIFACT
(the verifier's read-only sandbox failed to launch, so the charge's
grep-based checks were not independently executed; report
`lean/notes/openmath/VC33_report.md`). The cure: the dated
**r32-ERRATUM** (e7ff234) — the six p6-concurrency disclosure brackets
applied at the operative §0/§37 status displays + the grep-execution
duty TRANSFERRED to the narrow pass. **VC33b 0C/1G WITH execution**
(report `VC33b_report.md`): the 12/3/6 discharge-core transcripts
CONFIRMED under execution; the gap = three multi-line brackets missing
from the orchestrator's `grep -o` extraction (audit-input). **VC33c
CONFIRMED, 0 critical / 0 gaps** (report `VC33c_report.md` — all six
brackets verified in full context). Now INSIDE the certified anchor:
the HV3 acceptance + the complete (H-v3) discharge sweep (TR-3′-GEN
PRINT-UNCONDITIONAL at {all order-2} ∪ {order-3 all-e_j = 1}), the FGMN
REVISION-4-through-7 perimeter-hardening displays, the re-stated Asvin
batch. On the newly re-anchored text this revision folds the **FGMN
ACCEPTANCE** (the wave's one acceptance), the **DIVISION-OF-LABOR
adjudication** (a standing-discipline record), and the **TR3-S4 status
sync** (a status display, nothing accepted). NO roster change: **NINE
rows** recounted; no clause attribution moved; this unit ran no audit
and NEVER self-confirms — **VC34 is OWED on this REVISION-32 wording**.

**The §29/§30/§33/§34/§35/§36/§37 ASVIN FLAG — lineage continued; the
TOP item is now FULLY VERIFIED.** The standing Asvin batch, RESTATED IN
FULL: (1) the `TowerMoveClosureL` two-layer re-ratification; (2) the
fence-(vii) ROUTE (a) ratify-or-reopen lineage — the GR route (a) + the
RECSL `hσt`-pin rider + the EXECUTED F-ADM-3 re-key (ba02d60+8648d99,
recorded for ratify-or-reopen); (3) **the FGMN proposal — the flag
lineage's TOP item, now a FULLY VERIFIED proposal (ACCEPTED at ec64503,
the 14-pass/10-revision bar met: pass 12 Fable executed CLEAN + pass 14
Codex content-CLEAN, model-diverse)**: the accepted mathematics (the
two-scale diagnosis; the ROW-A SOURCE-FIRES verdict; the THREE-token
diff at HC2/Defs.lean:285/287/292 with its fixed points incl. the
JuncForge/D0 retained semantics; the PIN-GEN-i discharge) needs no
further verification — the Asvin decision is now EXACTLY the
statement-change application itself, whose adjudication object is the
APPLICATION UNIT's scratch-worktree diff per the REVISION-9 scope cut
(adjudicated ON THE DIFF at the gate, with a POST-APPLICATION hostile
pass as the completeness test; the REVISION 4-8 censuses the reviewer's
informative map); (4) the Route-B **RB-DELTA decision, DECISION-READY**
(the (UB-X) walk = condition (5)'s substance; the note's §6 is the
one-paragraph decision).

**Disposition table.**

| item | ground | grade | disposition | edit sites |
|---|---|---|---|---|
| r33-1 | ledger 1de8ec2 "VC33c CONFIRMED" (VC33 0C/2G → r32-ERRATUM e7ff234 → VC33b 0C/1G WITH execution → VC33c 0C/0G) | ACCEPTANCE RE-ANCHOR — the citation-text update rides this revision per the VC22 precedent; the VC33 arc lineage displayed at every operative site | every operative anchor site updated: the §0 REVISION-31 paragraph header superseded and the NEW REVISION-32 standing paragraph states the anchor (REVISION 31/VC33+VC33b+VC33c) with the full arc lineage; the §3.2 end-to-end row gains the VC33+VC33b+VC33c sync tail (RE-ANCHORS on the errata-corrected REVISION 31; VC34 owed on THIS text); the §6 r32 bracket's anchor sentence carries a dated supersession bracket and the new r33 bracket is current. Historical records (§§7–37) untouched beyond the named dated brackets | §0, §3.2 end-to-end row, §6 |
| r33-2 | ledger ec64503 "FGMN RE-DERIVATION ACCEPTED" (note `FGMN_REDERIVATION_2026-08-08.md` ACCEPTANCE RECORD: the 14-pass/10-revision arc — pass 12 Fable CLEAN 0C/0G WITH the full execution leg (pins + layout regenerated byte-identical; 1,093-check battery byte-identical; the four JuncForge/D0 fixed-point theorems compiled directly; a from-note 336-site reimplementation exact) + pass 14 Codex content-CLEAN 0C/0G under the division-of-labor disposition; THREE independent executed legs, passes 3/9/12) | ACCEPTANCE — the source-level question is ANSWERED: the print's glue step FIRES at row A; the compiled parity/landing walls were TRANSCRIPTION ARTIFACTS of the double-scaled clause. ACCEPTED per the REVISION-10 list: (a) the two-scale diagnosis; (b) the ROW-A SOURCE-FIRES verdict; (c) the THREE-token diff (σ.w → σ.wPrev) + fixed points incl. the JuncForge/D0 RETAINED SEMANTICS (byte-frozen old-clause meanings — INSIDE acceptance); (d) the PIN-GEN-i discharge; (e) the pinned sources + battery record. THE APPLICATION change-set is OUT OF ACCEPTANCE SCOPE (the REVISION-9 cut): the application unit's scratch-worktree diff, adjudicated at the ASVIN GATE on the diff itself with a post-application hostile pass (the strongest available adjudication, not a totality oracle; the five executed perimeter refutations = the record of why); NO `.lean` file touched, `SideReads` stands as compiled until the gate fires | the (H1) row's glue-route display gains the r33 ACCEPTANCE bracket (the question ANSWERED); the §0 REVISION-32 paragraph fold (1); the §6 r33 bracket; the flag paragraph above (TOP item FULLY VERIFIED) | §0, §3.1 (H1) row, §6, the flag paragraph |
| r33-3 | ledger 3875c49 "ORCHESTRATOR ADJUDICATION" (FGMN pass 13 0C, items 1-6 pass, sole gap the infra item) | STANDING CAMPAIGN DISCIPLINE record — the execution-leg division of labor for model-diverse bars on this host: the FABLE leg carries EXECUTION (hash verification, battery re-runs, compilation, from-note reimplementation), the CODEX leg adjudicates CONTENT (statements, derivations, arithmetic reconciliation, honesty sweeps); an infrastructure-only inability-to-execute finding on a Codex pass = a NOTE, not a gap, PROVIDED a Fable-executed clean on the same revision is on record — a specification of existing practice (the TR3-S3 p5/p7 precedent), NOT a weakening: no content finding is ever downgraded, every artifact retains ≥ 1 fully-executed independent leg | displayed at the §0 REVISION-32 paragraph fold (2), the §6 r33 bracket, and this record; applies to all current and future bars on this host | §0, §6, this record |
| r33-4 | seals 7fa8825 (preregistration) + 79eaee6 (first run) + ledger "TR3-S4 ATTEMPT" entries; pass report `TR3S4_pass1_report.md` | STATUS SYNC — nothing accepted, NO status upgrade: the compose note `TR3S4_2026-08-08.md` LANDED (the measured (TWIST-3) law composed as an identity on the order-3 e₀ = 1/e₁ ≥ 2/e₂ = 1 stratum, attempt grade; θ₂ = ℓ₁g₁γ₂); **Lemma HV3-E2 DERIVED from the pinned print, not hypothesized** (v₃ = w₂ at e₀ = 1, e₁ ≥ 2, from the sha256-pinned HigherNewton.tex generic displays at \|λ₂\| = h₁/e₁; no bespoke ordinate hypothesis remains; the accepted e₁ = 1 theorem's acceptance does NOT transfer — the new instantiation is the note's own hostile-pass obligation); falsifier two-commit protocol honored: seal → first run exit 0, 13,679 samples / 0 violations, rerun JSON-identical, zero amendments; pass 1 (Codex) 0C/2G archived (the S6.2 key-orientation conflation; the missing second seal hash) — REVISION 1 owed next cycle; counter 0/2; **the F2c VERIFIER-OWNED from-note implementation stays the acceptance obligation** (note S8.4 — the prover-side harness does not satisfy it) | the (H1) row's deeper-strata display gains the TR3-S4 status inside the r33 bracket; the §0 REVISION-32 paragraph fold (3); the §3.2 sync tail; the §6 r33 bracket | §0, §3.1 (H1) row, §3.2, §6 |
| r33-5 | housekeeping (the /goal cadence) + cadence (the VC22 precedent on the re-anchored text) | TRACKING DISPLAY + CADENCE — VC34 OWED; never self-confirm | **SYNTHESIS PASS 8 is OWED at the WAVE-15 CLOSE** (the FGMN arc that held it has closed). The WAVE-15 REMAINDER displayed (nothing dispatched here): the TR3-S4 pipeline (REVISION 1 → hostile passes → the F2c obligation); the synthesis-7 tail — the ITER-LAW proof attempt, the (H7) restatement forge on the two N-H7-1 pins, the `DIterJunctionSupplier` statement (its junction gates now RE-PRICE against the ACCEPTED FGMN diagnosis — the note's §S6 gate display is accepted content, no longer blocked-on-adjudication), the banked-sorry attempt; the optional S batch. The Asvin batch RESTATED IN FULL (the flag paragraph above). The sorry census of record stays the r27 display (36 repo-wide / B2D 0 — this fold is doc-only). REVISION 32 is a text change on the newly re-anchored text: the confirming pass **VC34 is OWED on the REVISION-32 wording** (charge below; model diversity per the tree §4 bar); until it lands, the acceptance citation points at the REVISION-31 text; VC34 is a fresh-context pass, not this unit | §0, §3.2 end-to-end row, §6, this record |

**THE ENUMERATION AUDIT (the r13 rule, run over the post-edit text).**
Unit: OCCURRENCE COUNTS (`grep -o | wc -l`, exact case-sensitive patterns,
the r23-declared unit), taken on the FINAL text of this revision (counts
include this record's own quotations — the r23–r32 precedent). The §37
fifty-five patterns plus FOUR markers introduced at r33 (`VC33b` and
`VC33c` NEW at the cadence — the narrow companions; `VC34` NEW at the
cadence, baseline 0 — named nowhere before this fold; `HV3-E2` NEW at
the TR3-S4 status fold, baseline 0). The r32 column = the §37 recorded
census; the r32-ERRATUM's own post-census contribution (e7ff234,
pre-dating this unit: +10 at the plain cadence name and +1 at the first
narrow companion, the six disclosure brackets + the transfer clause) is
folded into those rows' reasons. Every delta named:

| grep (occurrences) | r32 | r33 | delta | why |
|---|---:|---:|---:|---|
| `(R1)–(R4)` | 121 | 122 | +1 | no operative change — this table's own pattern cell |
| `(H4a)` | 201 | 202 | +1 | no operative change — this table's own pattern cell |
| `K3-δ` | 138 | 139 | +1 | status unchanged (clause (R) keeps consuming it until the Asvin gate fires) — this table's own pattern cell |
| `SITE-EXH` | 35 | 36 | +1 | no arc status change — this table's own pattern cell |
| `GRADED-READ` | 29 | 30 | +1 | no operative change — this table's own pattern cell |
| `TransDeep` + `TRANS-DEEP` | 49 | 51 | +2 | no operative change — both increments are this row's own joint pattern cell (one hit each) |
| `TEN rows` | 37 | 38 | +1 | historical-only — this table's own pattern cell |
| `NINE rows` | 64 | 68 | +4 | roster restated NINE, no change — the §0 REVISION-32 paragraph, the §3.2 sync tail, this record's close, and this table's own cell (the record's opening and recount namings wrap or drop the word at the line unit and do not count) |
| `VC25` | 59 | 60 | +1 | closed history — this table's own pattern cell |
| `VC23` | 28 | 29 | +1 | named-but-unrun history stands — this table's own pattern cell |
| `VC26` | 106 | 107 | +1 | closed history — this table's own pattern cell |
| `ROUTE (a)` | 51 | 54 | +3 | the batch restatements that fall unwrapped at the census's line unit (the §6 r33 bracket, the flag paragraph above) + this table's own cell — the §0 restatement wraps mid-pattern and does not count at this unit; still zero sites claim route (a) proved anything |
| `TR3-S2` | 42 | 43 | +1 | accepted status unchanged — this table's own pattern cell |
| `REC-SL` | 43 | 44 | +1 | status unchanged — this table's own pattern cell |
| `RECSL` | 51 | 55 | +4 | the `hσt`-pin rider namings in the full Asvin-batch restatements (§0, the §6 r33 bracket, the flag paragraph) + this table's own cell; status unchanged |
| `HUNI-V2` | 15 | 16 | +1 | closed history — this table's own pattern cell |
| `VC27` | 131 | 133 | +2 | closed history — this table's own cells (this row and the row below) |
| `VC27b` | 57 | 58 | +1 | closed history — this table's own pattern cell (each hit also counts one at the row above) |
| `VC28` | 78 | 80 | +2 | closed history — the VC34-charge's checklist-validation naming + this table's own pattern cell |
| `VC29` | 85 | 87 | +2 | closed history — the VC34-charge's checklist-validation naming + this table's own pattern cell |
| `POOL-DICT` | 50 | 51 | +1 | closed history — this table's own pattern cell |
| `TR-4` | 37 | 38 | +1 | residue-taxon status unchanged — this table's own pattern cell |
| `BasePolygonLengthLaw` | 43 | 44 | +1 | proved status stands — this table's own pattern cell |
| `ITER-LAW` | 45 | 49 | +4 | open-lemma status UNCHANGED — the wave-15-remainder displays (§0, the §6 r33 bracket, the r33-5 row) + this table's own cell |
| `leakFree` | 66 | 67 | +1 | typed-face status unchanged — this table's own pattern cell |
| `LeakFreeCarrierV4` | 12 | 13 | +1 | no new consumption — this table's own pattern cell |
| `junc_bundle` | 81 | 83 | +2 | the retained-semantics theorem naming at the (H1) r33 bracket (one containment hit inside the compiled theorem name) + this table's own cell; the accepted fixed point RETAINS the old-clause meaning — zero sites re-tier the verdict |
| `G-ADM-2` | 27 | 28 | +1 | closed routing history — this table's own pattern cell |
| `BPLL` | 20 | 21 | +1 | proved status stands — this table's own pattern cell |
| `K1ConsumerFace` | 17 | 18 | +1 | discharged-face status stands — this table's own pattern cell |
| `N-H7-1` | 30 | 34 | +4 | the wave-15-remainder displays (§0, the §6 r33 bracket, the r33-5 row) + this table's own cell; statements still OPEN |
| `VACUOUS-BY-TYPE` | 12 | 13 | +1 | the r28 verdict stands — this table's own pattern cell |
| `ADM-REAL` | 41 | 42 | +1 | acceptance history stands — this table's own pattern cell |
| `CLASS-LAT` | 25 | 26 | +1 | machine-checked status stands — this table's own pattern cell |
| `JUNCFORGE` | 18 | 19 | +1 | the exact-case unit does NOT count the mixed-case namings in this revision's FGMN acceptance displays — this table's own pattern cell only |
| `TR3-S3` | 36 | 39 | +3 | accepted status unchanged — the division-of-labor precedent namings (§0, the r33-3 row) + this table's own cell |
| `VC30` | 116 | 118 | +2 | closed history — this table's own cells (this row and the containment hit at the row below) |
| `VC30b` | 53 | 54 | +1 | closed history — this table's own pattern cell; every hit also counts one at the row above |
| `VC31` | 159 | 162 | +3 | closed history — this table's own cells (this row + the containment hits at the two rows below) |
| `VC31b` | 47 | 48 | +1 | closed history — this table's own pattern cell; every hit also counts one at the row above |
| `VC31c` | 44 | 45 | +1 | closed history — this table's own pattern cell; every hit also counts one at the row above |
| `VC32` | 93 | 108 | +15 | the OLD side of the anchor move, named at every re-anchor display (§0, the §3.2 sync tail, the §6 supersession + r33 brackets, this record's lineage/disposition/charge) with its suffixed companion (each companion hit counts here too — containment) + the VC34-charge's checklist-validation naming + these cells |
| `VC32b` | 35 | 42 | +7 | the OLD-side arc namings at the re-anchor displays + this table's own cell; every hit also counts one at the row above (containment) |
| `VC33` | 18 | 127 | +109 | the re-anchor IS this revision's cadence content: every arc-lineage display names the pass AND its two suffixed companions (each companion hit counts here too — containment), at §0, the §3.2 sync tail, the §6 supersession + r33 brackets, this record's headline/lineage/disposition/charge + these cells and the census header + the fixed-point paragraph; +10 of the increment is the r32-ERRATUM's own pre-dating contribution (e7ff234 — the r32 column predates it) |
| `VC33b` | 0 | 30 | +30 | NEW at r33 — the narrow companion WITH execution: every arc-lineage display + the census header + the fixed-point paragraph + these cells; +1 of the increment is the r32-ERRATUM's pre-dating transfer clause; every hit also counts one at the row above (containment) |
| `VC33c` | 0 | 34 | +34 | NEW at r33 — the second narrow companion: every arc-lineage display + the census header + the fixed-point paragraph + these cells; every hit also counts one at the plain-name row above (containment) |
| `VC34` | 0 | 19 | +19 | NEW at r33 — the owed confirming pass: §0, the §3.2 sync tail, the §6 r33 bracket, this record's cadence/charge rows + the two charge-citing reason cells above, the census header + these cells; VC34 NAMED-NOT-RUN — zero sites claim it ran |
| `H-v3` | 56 | 61 | +5 | discharged status STANDS — the certified-anchor restatements at the re-anchor displays (§0, §6, this record) + the fixed-point paragraph + this cell; zero sites re-open the hypothesis |
| `HV3-THM` | 33 | 38 | +5 | accepted status stands — the non-transfer displays at the TR3-S4 status sites (its acceptance does NOT reach the e₁ ≥ 2 stratum) + the fixed-point paragraph + this table's own cell |
| `PIN-GEN-i` | 31 | 37 | +6 | the accepted FGMN list's item (d) namings (§0, the (H1) r33 bracket, the §6 r33 bracket, the flag paragraph, the r33-2 row) + this table's own cell; the pin's discharge history unchanged |
| `TWIST-3` | 37 | 41 | +4 | measured-law status unchanged — the composed-as-identity displays at the TR3-S4 status sites (§0, the (H1) r33 bracket, the r33-4 row) + this table's own cell; zero sites claim more than attempt grade |
| `TR3-S4` | 29 | 47 | +18 | the wave's status fold: the status displays (§0, the §3.2 sync tail, the (H1) r33 bracket, the §6 r33 bracket, this record's rows, the charge, the close, and the census header) + this table's own cell and the naming reason-cells above; zero sites claim acceptance — counter 0/2 and NO status upgrade everywhere |
| `RB-DELTA` | 18 | 22 | +4 | the Asvin-batch restatements (§0, the §6 r33 bracket, the flag paragraph) + this cell; every site states decision-ready + Asvin-gated, zero sites claim adoption |
| `wPrev` | 16 | 20 | +4 | the accepted three-token diff displays (§0, the (H1) r33 bracket, the r33-2 row) + this table's own cell; zero sites claim the tokens applied |
| `classCount_eq_d` | 7 | 8 | +1 | proved status stands — this table's own pattern cell |
| `SideClauseR2` | 8 | 9 | +1 | the REVISION-4 perimeter member is now the informative map's content under the scope cut — no new naming; this table's own pattern cell |
| `readCeil` | 10 | 11 | +1 | same disposition as the row above — no new naming; this table's own pattern cell |
| `HV3-E2` | 0 | 9 | +9 | NEW at r33 — the TR3-S4 fold's derived lemma, displayed at every status site (§0, the §3.2 sync tail, the (H1) r33 bracket, the §6 r33 bracket, the r33-4 row, this record's close) + the census header + the fixed-point paragraph + this cell; every site says DERIVED from the pinned print with the note's own hostile-pass obligation, zero sites transfer the accepted e₁ = 1 theorem's acceptance |

Fixed point: the counts above were re-grepped AFTER the last edit of this
revision. The count-sensitive subtleties of §§31–37 carry over UNCHANGED
(containment, hyphen-disjointness, case-sensitivity, decl-name-visibility
— each re-checked, none changed sign); the r33-new ones: **`VC33b` ⊂
`VC33` and `VC33c` ⊂ `VC33`** — every suffixed hit counts once under the
parent (the established suffix-companion genre); `VC34` is pairwise
disjoint from the other cadence names; **`HV3-E2` is disjoint from
`HV3-THM`** (distinct suffixed tokens) **and from `H-v3`**
(case-sensitivity: the lowercase-v pattern never matches the uppercase
token) and from every prior pattern; the wrapped batch restatement at §0
(noted at its row) is the one place a load-bearing display falls outside
the line-based unit — the display is present, the census simply does not
count it.

**The re-derived row count (the r13 audit rule — counted, not copied).**
The §3.1 table's data rows enumerated directly after the edits: (H1) ·
(FRESH) = (H2) · (ADM) [(H3) member] · (UB-X) = (H4b) · (PACK) = (H5) ·
the (H6) terminal-seam group · (I-τ)/K-LOC = (H7) · (H8′) · (E-N) [(H3)
member]. THE COUNT IS **NINE** — unchanged; this revision touches row
CONTENT only (the (H1) r33 bracket) and adds and retires no row; the
FGMN acceptance verifies a PROPOSAL about member (a)'s glue route — it
discharges no row and no member.

**VC34 charge.** All §37 charge items as inherited ((xxxiv)–(lxx) now
checked against the r33 wording — note (xxxiv) becomes: every operative
site cites REVISION 31/VC33+VC33b+VC33c, with the VC33 arc lineage
displayed (VC33 0C/2G → r32-ERRATUM e7ff234 → VC33b 0C/1G WITH
execution → VC33c 0C/0G, 1de8ec2) and NO site citing REVISION
30/VC32+VC32b as the current anchor), PLUS: (lxxi) the FGMN fold claims
EXACTLY the REVISION-10 acceptance — the accepted list (a)–(e) displayed
with the JuncForge/D0 retained semantics INSIDE scope, the application
change-set OUT of scope at every site (the REVISION-9 scope-cut form),
and NO site claiming `SideReads` repaired, any `.lean` edit, or a total
pre-application perimeter; (lxxii) the division-of-labor record claims a
SPECIFICATION of existing practice — no content finding downgraded
anywhere, every acceptance retaining at least one fully-executed
independent leg; (lxxiii) the TR3-S4 displays claim composed +
sealed-falsifier ONLY — attempt grade everywhere, counter 0/2, REVISION
1 owed, the F2c verifier-owned implementation displayed as the
acceptance obligation at every status site, and the derived-lemma
displays claiming derivation from the pinned print WITHOUT transferring
the accepted e₁ = 1 theorem's acceptance; (lxxiv) synthesis pass 8 is
displayed OWED at the wave-15 close and NOT claimed run; (lxxv) the
census above holds at the declared occurrence unit with the stated
delta-reasons. VC34 should receive the in-repo file listing + commit
resolutions + the COMPLETE provenance appendix with VERBATIM slices per
the saved checklist (memory: vc-audit-appendix-checklist — validated at
VC28, VC29, and VC32), AND — the VC33-lineage lesson — either an
execution-capable environment or pinned raw grep transcripts as audit
inputs (the narrow-pass execution-transfer precedent; hostile
verification requires execution, not acceptance).

**Bookkeeping.** Historical §§7–37 untouched except the dated additions
named in the disposition table (the §0 REVISION-31 paragraph header
superseded; the (H1) r33 bracket appended; the §3.2 sync tail; the §6
supersession bracket + the r33 bracket). This is a DOC-ONLY revision: no
Lean file is touched, no statement changes, no note brackets ride this
unit (the FGMN note's ACCEPTANCE RECORD and the TR3-S4 note's S8.3/S8.4
records were written by their own units). This unit touches ROOT +
`docs/PROJECT_STATE.md` (new r33 checkpoint paragraph + dated tags at
the r32 checkpoint's three stale status displays — the FGMN
passes-decide display, the synthesis-8 display, the frontier line) + the
ledger (`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`, r33-executed
entry) ONLY. The commit closing this revision is its unit of record
(unit ROOT r33; scoped add).

— REVISION 32 complete. VC33 + VC33b + VC33c CONFIRMED and the
acceptance RE-ANCHORED on the (errata-corrected) REVISION-31 text (the
anchor moves REVISION 30/VC32+VC32b → REVISION 31/VC33+VC33b+VC33c; the
VC33 arc lineage displayed — 0C/2G, the r32-ERRATUM, the executed narrow
companion, VC33c 0C/0G); the FGMN ACCEPTANCE folded: the
14-pass/10-revision arc closed at ec64503 — **the glue route's
source-level question is ANSWERED** (the print's glue step fires at row
A; the compiled walls were transcription artifacts), the accepted
mathematics enumerated per the REVISION-10 list, the application
change-set the Asvin-gated application unit's deliverable per the
REVISION-9 scope cut, the batch's TOP item now FULLY VERIFIED; the
division-of-labor adjudication recorded as standing discipline
(3875c49); the TR3-S4 status synced (composed + sealed 13,679/0 at
7fa8825/79eaee6; Lemma HV3-E2 DERIVED from print; pass 1 0C/2G archived;
REVISION 1 owed; counter 0/2; NO status upgrade; the F2c verifier-owned
implementation the acceptance obligation); SYNTHESIS PASS 8 OWED at the
wave-15 close; NINE rows recounted; the census re-run at the declared
unit with every delta explained; **VC34 owed on this REVISION-32 text**
— until it lands, the acceptance citation points at the REVISION-31
text.

## 39. REVISION 33 (r34, the VC34 re-anchor + the TR3-S4 acceptance fold) — the anchor moves REVISION 32; the strata family closes at FOUR accepted members

**What this revision is.** The r34 cadence event: **VC34 CONFIRMED
FIRST-PASS, 0 critical / 0 gaps** (ledger f3b1ff8 "VC34 CONFIRMED
FIRST-PASS"; report `lean/notes/openmath/VC34_report.md`) — the third
first-pass confirmation of the window, on the REVISION-32 wording
directly: ALL folds verified (the FGMN acceptance display exactly the
REVISION-10 list, no application leak, the wall theorems byte-frozen;
the TR3-S4 un-upgraded status with the F2c obligation displayed; the
division-of-labor discipline; the Asvin batch with its verified top
item; the 59-pattern census exact incl. the honestly displayed §37
baseline corrections; roster NINE) — so **the acceptance anchor moves
REVISION 31/VC33+VC33b+VC33c → REVISION 32/VC34** and this revision
executes the citation-text update at every operative site (the VC22
precedent). CONSEQUENCE of the re-anchor: **THE WAVE-14/FGMN MEGA-ARC
IS CLOSED AND CERTIFIED END-TO-END** — the glue route's source-level
question is answered INSIDE the certified anchor; the three-token
proposal is fully verified at the Asvin gate. On the newly re-anchored
text this revision folds the **TR3-S4 ACCEPTANCE** (the wave's one
acceptance — the r33 status sync UPGRADES to the accepted family) and
the **TWIST3-probe HYGIENE bracket** (the pass-3 observation). NO
roster change: **NINE rows** recounted; no clause attribution moved;
this unit ran no audit and NEVER self-confirms — **VC35 is OWED on
this REVISION-33 wording**.

**The §29/§30/§33/§34/§35/§36/§37/§38 ASVIN FLAG — lineage continued;
the batch stands at FOUR items, unchanged by this fold.** The standing
Asvin batch, RESTATED IN FULL: (1) the `TowerMoveClosureL` two-layer
re-ratification; (2) the fence-(vii) ROUTE (a) ratify-or-reopen lineage
— the GR route (a) + the RECSL `hσt`-pin rider + the EXECUTED F-ADM-3
re-key (ba02d60+8648d99, recorded for ratify-or-reopen); (3) **the FGMN
proposal — the flag lineage's TOP item, FULLY VERIFIED (ACCEPTED at
ec64503, the 14-pass/10-revision bar met)**: the accepted mathematics
needs no further verification — the Asvin decision is EXACTLY the
statement-change application itself, whose adjudication object is the
APPLICATION UNIT's scratch-worktree diff per the REVISION-9 scope cut
(adjudicated ON THE DIFF at the gate, with a POST-APPLICATION hostile
pass as the completeness test; the REVISION 4-8 censuses the reviewer's
informative map); (4) the Route-B **RB-DELTA decision, DECISION-READY**
(the (UB-X) walk = condition (5)'s substance; the note's §6 is the
one-paragraph decision).

**Disposition table.**

| item | ground | grade | disposition | edit sites |
|---|---|---|---|---|
| r34-1 | ledger f3b1ff8 "VC34 CONFIRMED FIRST-PASS" (0 critical / 0 gaps, first-pass on the REVISION-32 wording; report `lean/notes/openmath/VC34_report.md`) | ACCEPTANCE RE-ANCHOR — the citation-text update rides this revision per the VC22 precedent; the VC34 first-pass record displayed at every operative site | every operative anchor site updated: the §0 REVISION-32 paragraph header superseded and the NEW REVISION-33 standing paragraph states the anchor (REVISION 32/VC34) with the first-pass record; the §3.2 end-to-end row gains the VC34 sync tail (RE-ANCHORS on REVISION 32; VC35 owed on THIS text); the §6 r33 bracket's anchor sentence carries a dated supersession bracket and the new r34 bracket is current. Historical records (§§7–38) untouched beyond the named dated brackets | §0, §3.2 end-to-end row, §6 |
| r34-2 | ledger cd611af "TR3-S4 ACCEPTED" (note `TR3S4_2026-08-08.md` ACCEPTANCE RECORD: the 4-pass/2-revision arc — pass 1 Codex 0C/2G GAPS-ONLY → REVISION 1 (the S6.2 key-orientation conflation cured at five sites; the S8.3 second seal hash displayed) → pass 2 Codex 0C/1G GAPS-ONLY → REVISION 2 (the sealed script header's stale orientation sentence superseded by note bracket, the seal byte-untouched) → pass 3 Fable CLEAN 0C/0G WITH BOTH EXECUTION LEGS (the sealed battery re-run byte-identical, 13,679 samples / 0 violations; **the F2c VERIFIER-OWNED from-note implementation DISCHARGED IN-PIPELINE for the first time** — 1,108 checks / 0 failures at off-roster p ∈ {5,7} on 6 towers, GMN ordinates via the decorrelated geometric (E4)-Fraction leg rather than the hard-coded ordinate reading, dedicated s₂ > 0 face + holes + negative-weight off-line slots, teeth validated by 3 mutation controls, each RED) → pass 4 Codex content-CLEAN 0C/0G — model-diverse under the standing division-of-labor discipline) | ACCEPTANCE — with accepted TR3-S1/S2/S3, **TR-3′-GEN is PROVED on the THREE-component union {all order-2} ∪ {order-3 all-e_j = 1, PRINT-UNCONDITIONAL} ∪ {order-3 e₀ = 1, e₁ ≥ 2, e₂ = 1, conditional on (H-ℓ) + the (KP_i)+[T] consumptions at S6.2 + the PIN-GEN-i attribution display, with Lemma HV3-E2 DERIVED from the pinned print, not hypothesized}** — the strata family at FOUR accepted members; the F2c standing rule satisfied IN-PIPELINE for the first time. Deeper strata (e₀ ≥ 2 stacked; e₂ ≥ 2; orders ≥ 4) stay OPEN, PROBE-FIRST, NO candidate sealed (the mechanism note predicts a non-scalar inner-twisted evaluation — the next probe must MEASURE the shape). Member (a) of (H1) is NOT discharged; the row STAYS [M] | the (H1) row's deeper-strata display gains the r34 ACCEPTANCE bracket; the §0 REVISION-33 paragraph fold (1); the §3.2 sync tail; the §6 r34 bracket | §0, §3.1 (H1) row, §3.2, §6 |
| r34-3 | pass report `TR3S4_pass3_report.md` §6 item 1 (a non-classifiable observation, outside the pass's perimeter): `TWIST3_PROBE_2026-08-08.md` line 80 carries the pre-REVISION-1 orientation mislabel ("the GMN-lawful level-3 key class is the θ₂-TWISTED ψ₂ class") — the same label the sealed script header carried; a future consumer reading the probe note alone would inherit it | HYGIENE — ONE dated supersession bracket applied in the probe note at its S2 KEY0-LIFT orientation prose, pointing at the TR3-S4 REVISION-1 corrected orientation: the GMN-lawful class is the UNTWISTED ψ₂ (R_λ₃(Φ₃) = z̄₂^{−θ₂g₂}·ψ₂, from lawfulness + the verified pin t₂(g₂) = −θ₂g₂); the θ₂-twisted T_{c⁻¹}ψ₂ class belongs to the FGMN-NORMALIZED lift. ADDITIVE: the probe's measured divergence data (the P4′/P5′/M8 counts) stand untouched — they compare the same two objects throughout; only the attribution LABEL was wrong; the S3 charter's lowercase display-lemma echo is named inside the same bracket; the sealed harness stays byte-untouched (the seal discipline) | the ONE bracket in `TWIST3_PROBE_2026-08-08.md` (S2, directly after the mislabeled sentence); displayed at the §0 REVISION-33 paragraph fold (2), the (H1) r34 bracket, the §6 r34 bracket, this record | the TWIST3 probe note, §0, §3.1 (H1) row, §6 |
| r34-4 | housekeeping (the /goal cadence) + cadence (the VC22 precedent on the re-anchored text) | TRACKING DISPLAY + CADENCE — VC35 OWED; never self-confirm | the WAVE-15 REMAINDER displayed as THE queue (nothing dispatched here): the synthesis-7 tail — the ITER-LAW proof attempt, the (H7) restatement forge on the two N-H7-1 pins, the `DIterJunctionSupplier` statement (re-priced against the ACCEPTED FGMN diagnosis — its §S6 gate display is accepted content, no longer blocked-on-adjudication), the banked-sorry attempt; the optional S batch. **SYNTHESIS PASS 8 is OWED at the WAVE-15 CLOSE.** The Asvin batch RESTATED IN FULL (the flag paragraph above; FOUR items, the TOP = the verified FGMN proposal). The sorry census of record stays the r27 display (36 repo-wide / B2D 0 — this fold touches no Lean file). REVISION 33 is a text change on the newly re-anchored text: the confirming pass **VC35 is OWED on the REVISION-33 wording** (charge below; model diversity per the tree §4 bar); until it lands, the acceptance citation points at the REVISION-32 text; VC35 is a fresh-context pass, not this unit | §0, §3.2 end-to-end row, §6, this record |

**THE ENUMERATION AUDIT (the r13 rule, run over the post-edit text).**
Unit: OCCURRENCE COUNTS (`grep -o | wc -l`, exact case-sensitive patterns,
the r23-declared unit), taken on the FINAL text of this revision (counts
include this record's own quotations — the r23–r33 precedent). The §38
fifty-nine patterns plus TWO markers introduced at r34 (`VC35` NEW at the
cadence, baseline 0 — named nowhere before this fold; `KEY0-LIFT` NEW at
the hygiene fold, baseline 0 — the probe note's orientation-prose label
enters ROOT with this revision's displays). The r33 column = the §38
recorded census. Every delta named:

| grep (occurrences) | r33 | r34 | delta | why |
|---|---:|---:|---:|---|
| `(R1)–(R4)` | 122 | 123 | +1 | no operative change — this table's own pattern cell |
| `(H4a)` | 202 | 203 | +1 | no operative change — this table's own pattern cell |
| `K3-δ` | 139 | 140 | +1 | status unchanged (clause (R) keeps consuming it until the Asvin gate fires) — this table's own pattern cell |
| `SITE-EXH` | 36 | 37 | +1 | no arc status change — this table's own pattern cell |
| `GRADED-READ` | 30 | 31 | +1 | no operative change — this table's own pattern cell |
| `TransDeep` + `TRANS-DEEP` | 51 | 53 | +2 | no operative change — both increments are this row's own joint pattern cell (one hit each) |
| `TEN rows` | 38 | 39 | +1 | historical-only — this table's own pattern cell |
| `NINE rows` | 68 | 73 | +5 | roster restated NINE, no change — the §0 REVISION-33 paragraph, the §3.2 sync tail, this record's opening and close, and this table's own cell (the other restatements wrap or drop the word at the line unit and do not count) |
| `VC25` | 60 | 61 | +1 | closed history — this table's own pattern cell |
| `VC23` | 29 | 30 | +1 | named-but-unrun history stands — this table's own pattern cell |
| `VC26` | 107 | 108 | +1 | closed history — this table's own pattern cell |
| `ROUTE (a)` | 54 | 57 | +3 | the batch restatements that fall unwrapped at the census's line unit (the §0 REVISION-33 paragraph, the flag paragraph above) + this table's own cell — the §6 r34 bracket's restatement wraps mid-pattern and does not count at this unit; still zero sites claim route (a) proved anything |
| `TR3-S2` | 43 | 44 | +1 | accepted status unchanged — this table's own pattern cell; the family namings in this revision's displays are slash-compounds that do not match at this exact unit |
| `REC-SL` | 44 | 45 | +1 | status unchanged — this table's own pattern cell |
| `RECSL` | 55 | 59 | +4 | the `hσt`-pin rider namings in the full Asvin-batch restatements (§0, the §6 r34 bracket, the flag paragraph) + this table's own cell; status unchanged |
| `HUNI-V2` | 16 | 17 | +1 | closed history — this table's own pattern cell |
| `VC27` | 133 | 135 | +2 | closed history — this table's own cells (this row and the row below) |
| `VC27b` | 58 | 59 | +1 | closed history — this table's own pattern cell (each hit also counts one at the row above) |
| `VC28` | 80 | 82 | +2 | closed history — the VC35-charge's checklist-validation naming + this table's own pattern cell |
| `VC29` | 87 | 89 | +2 | closed history — the VC35-charge's checklist-validation naming + this table's own pattern cell |
| `POOL-DICT` | 51 | 52 | +1 | closed history — this table's own pattern cell |
| `TR-4` | 38 | 39 | +1 | residue-taxon status unchanged — this table's own pattern cell |
| `BasePolygonLengthLaw` | 44 | 45 | +1 | proved status stands — this table's own pattern cell |
| `ITER-LAW` | 49 | 53 | +4 | open-lemma status UNCHANGED — the wave-15-remainder displays (§0, the §6 r34 bracket, the r34-4 row) + this table's own cell |
| `leakFree` | 67 | 68 | +1 | typed-face status unchanged — this table's own pattern cell |
| `LeakFreeCarrierV4` | 13 | 14 | +1 | no new consumption — this table's own pattern cell |
| `junc_bundle` | 83 | 84 | +1 | the accepted fixed point RETAINS the old-clause meaning — no new naming this revision; this table's own pattern cell |
| `G-ADM-2` | 28 | 29 | +1 | closed routing history — this table's own pattern cell |
| `BPLL` | 21 | 22 | +1 | proved status stands — this table's own pattern cell |
| `K1ConsumerFace` | 18 | 19 | +1 | discharged-face status stands — this table's own pattern cell |
| `N-H7-1` | 34 | 38 | +4 | the wave-15-remainder displays (§0, the §6 r34 bracket, the r34-4 row) + this table's own cell; statements still OPEN |
| `VACUOUS-BY-TYPE` | 13 | 14 | +1 | the r28 verdict stands — this table's own pattern cell |
| `ADM-REAL` | 42 | 43 | +1 | acceptance history stands — this table's own pattern cell |
| `CLASS-LAT` | 26 | 27 | +1 | machine-checked status stands — this table's own pattern cell |
| `JUNCFORGE` | 19 | 20 | +1 | the exact-case unit does NOT count mixed-case namings — this table's own pattern cell only |
| `TR3-S3` | 39 | 40 | +1 | accepted status unchanged — this table's own pattern cell; the accepted-family namings in this revision's displays are slash-compounds that do not match at this exact unit |
| `VC30` | 118 | 120 | +2 | closed history — this table's own cells (this row and the containment hit at the row below) |
| `VC30b` | 54 | 55 | +1 | closed history — this table's own pattern cell; every hit also counts one at the row above |
| `VC31` | 162 | 165 | +3 | closed history — this table's own cells (this row + the containment hits at the two rows below) |
| `VC31b` | 48 | 49 | +1 | closed history — this table's own pattern cell; every hit also counts one at the row above |
| `VC31c` | 45 | 46 | +1 | closed history — this table's own pattern cell; every hit also counts one at the row above |
| `VC32` | 108 | 111 | +3 | closed history — the VC35-charge's checklist-validation naming + this table's own cells (this row and the containment hit at the row below) |
| `VC32b` | 42 | 43 | +1 | closed history — this table's own pattern cell; every hit also counts one at the row above |
| `VC33` | 127 | 149 | +22 | the OLD side of the anchor move: the re-anchor displays name the superseded arc with both suffixed companions (each companion hit counts here too — containment) at §0, the §3.2 sync tail, the §6 r34 bracket, and this record's opening/disposition/charge/close, plus the charge's lineage-lesson naming + these cells (this row and the two rows below) |
| `VC33b` | 30 | 37 | +7 | the OLD-side arc namings at the re-anchor displays + this table's own cell; every hit also counts one at the row above (containment) |
| `VC33c` | 34 | 41 | +7 | the OLD-side arc namings at the re-anchor displays + this table's own cell; every hit also counts one at the plain-name row above (containment) |
| `VC34` | 19 | 50 | +31 | the re-anchor IS this revision's cadence content — the NEW side of the anchor move with the first-pass record, displayed at §0, the §3.2 sync tail, the §6 supersession + r34 brackets, and throughout this record (headline, opening, disposition, charge, bookkeeping, close) + this table's own cell; every site cites the pass as CONFIRMED FIRST-PASS on the REVISION-32 wording, none as run on THIS text |
| `H-v3` | 61 | 62 | +1 | discharged status STANDS — this table's own pattern cell; zero sites re-open the hypothesis |
| `HV3-THM` | 38 | 39 | +1 | accepted status stands — this table's own pattern cell; this revision's non-transfer displays say 'the accepted e₁ = 1 theorem' rather than naming this pattern |
| `PIN-GEN-i` | 37 | 44 | +7 | the third strata-component's conditionality display at every acceptance site (§0, the (H1) r34 bracket, the §3.2 sync tail, the §6 r34 bracket, the r34-2 row, the charge) + this table's own cell; the pin's discharge history unchanged |
| `TWIST-3` | 41 | 42 | +1 | the measured law is now composed INSIDE the accepted note (this fold); this revision's new displays name the probe unhyphenated, so only this table's own pattern cell counts at this exact unit — zero sites downgrade or re-measure the law |
| `TR3-S4` | 47 | 69 | +22 | the wave's acceptance fold: the acceptance displays (§0, the §3.2 sync tail, the (H1) r34 bracket, the §6 supersession + r34 brackets, this record's headline/opening/flag-adjacent/disposition/charge/close) + this table's own cell; every site claims EXACTLY the note's ACCEPTANCE RECORD — the three-component union with the third component's conditionality displayed, deeper strata OPEN |
| `RB-DELTA` | 22 | 26 | +4 | the Asvin-batch restatements (§0, the §6 r34 bracket, the flag paragraph) + this cell; every site states decision-ready + Asvin-gated, zero sites claim adoption |
| `wPrev` | 20 | 21 | +1 | no new naming this revision (the accepted diff is referenced without its token text) — this table's own pattern cell; zero sites claim the tokens applied |
| `classCount_eq_d` | 8 | 9 | +1 | proved status stands — this table's own pattern cell |
| `SideClauseR2` | 9 | 10 | +1 | the informative-map member under the scope cut — no new naming; this table's own pattern cell |
| `readCeil` | 11 | 12 | +1 | same disposition as the row above — no new naming; this table's own pattern cell |
| `HV3-E2` | 9 | 17 | +8 | the accepted note's derived lemma, displayed at every acceptance site (§0, the §3.2 sync tail, the (H1) r34 bracket, the §6 r34 bracket, the r34-2 row, the charge, this record's close) + this cell; every site says DERIVED from the pinned print, zero sites transfer the accepted e₁ = 1 theorem's acceptance |
| `VC35` | 0 | 19 | +19 | NEW at r34 — the owed confirming pass: §0, the §3.2 sync tail, the §6 r34 bracket, this record's opening/disposition/charge/close, the census header + the fixed-point paragraph, the three charge-citing reason cells above + these cells (the pattern cell and this reason's own naming); VC35 NAMED-NOT-RUN — zero sites claim it ran |
| `KEY0-LIFT` | 0 | 9 | +9 | NEW at r34 — the hygiene fold's orientation label, displayed at the bracket sites (§0, the (H1) r34 bracket, the §3.2 sync tail, the §6 r34 bracket, the r34-3 row, this record's close) + the census header + the fixed-point paragraph + this cell; case-disjoint from the lowercase display-lemma spelling, which never counts here |

Fixed point: the counts above were re-grepped AFTER the last edit of this
revision. The count-sensitive subtleties of §§31–38 carry over UNCHANGED
(containment among the suffixed cadence companions, hyphen-disjointness,
case-sensitivity, decl-name-visibility — each re-checked, none changed
sign); the r34-new ones: `VC35` is pairwise disjoint from every other
cadence name (no suffix relation in either direction); `KEY0-LIFT` is
case-sensitively disjoint from the probe note's lowercase display-lemma
spelling ("KEY0-lift" — which therefore does NOT count at this unit, even
where this record's r34-3 row describes it) and disjoint from every prior
pattern; the wrapped batch restatement in the §6 r34 bracket (the
fence-(vii) item wraps mid-pattern at the line unit) is the one place a
load-bearing display falls outside the line-based unit — the display is
present, the census simply does not count it.

**The re-derived row count (the r13 audit rule — counted, not copied).**
The §3.1 table's data rows enumerated directly after the edits: (H1) ·
(FRESH) = (H2) · (ADM) [(H3) member] · (UB-X) = (H4b) · (PACK) = (H5) ·
the (H6) terminal-seam group · (I-τ)/K-LOC = (H7) · (H8′) · (E-N) [(H3)
member]. THE COUNT IS **NINE** — unchanged; this revision touches row
CONTENT only (the (H1) r34 bracket) and adds and retires no row; the
TR3-S4 acceptance upgrades member (a)'s deeper-strata DISPLAY — it
discharges no row and no member.

**VC35 charge.** All §38 charge items as inherited ((xxxiv)–(lxxv) now
checked against the r34 wording — note (xxxiv) becomes: every operative
site cites REVISION 32/VC34, with the first-pass record displayed (VC34
CONFIRMED FIRST-PASS 0C/0G, f3b1ff8) and NO site citing REVISION
31/VC33+VC33b+VC33c as the current anchor), PLUS: (lxxvi) the TR3-S4
fold claims EXACTLY the note's ACCEPTANCE RECORD — the THREE-component
union displayed with the third component's conditionality at every site
((H-ℓ) + the (KP_i)+[T] consumptions at S6.2 + the PIN-GEN-i
attribution display; Lemma HV3-E2 derived from the pinned print, NEVER
transferring the accepted e₁ = 1 theorem's acceptance), the F2c
discharge claimed IN-PIPELINE with the 1,108-check record and its
mutation controls, and NO site claiming any deeper stratum (e₀ ≥ 2
stacked; e₂ ≥ 2; orders ≥ 4) proved or a candidate sealed; (lxxvii) the
hygiene fold is ONE dated ADDITIVE bracket at the probe note's
orientation prose — the label superseded, no measured datum altered,
the sealed harness untouched, and NO ROOT site inheriting the
pre-correction orientation; (lxxviii) synthesis pass 8 is displayed
OWED at the wave-15 close and NOT claimed run; (lxxix) the census above
holds at the declared occurrence unit with the stated delta-reasons.
VC35 should receive the in-repo file listing + commit resolutions + the
COMPLETE provenance appendix with VERBATIM slices per the saved
checklist (memory: vc-audit-appendix-checklist — validated at VC28,
VC29, and VC32), AND either an execution-capable environment or pinned
raw grep transcripts as audit inputs (the VC33-lineage lesson; hostile
verification requires execution, not acceptance).

**Bookkeeping.** Historical §§7–38 untouched except the dated additions
named in the disposition table (the §0 REVISION-32 paragraph header
superseded; the (H1) r34 bracket appended; the §3.2 sync tail; the §6
supersession bracket + the r34 bracket). This revision touches NO Lean
file and NO statement; ONE note bracket rides this unit (the dated
supersession bracket in `TWIST3_PROBE_2026-08-08.md` — the r34-3
disposition; the TR3-S4 note's own ACCEPTANCE RECORD was written by its
unit). This unit touches ROOT + `docs/PROJECT_STATE.md` (new r34
checkpoint paragraph + dated tags at the r33 checkpoint's stale status
displays — the TR3-S4 passes-decide display, the VC34-pending frontier
line) + the probe note (the ONE bracket) + the ledger
(`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`, r34-executed entry)
ONLY. The commit closing this revision is its unit of record (unit ROOT
r34; scoped add).

— REVISION 33 complete. VC34 CONFIRMED FIRST-PASS and the acceptance
RE-ANCHORED on the REVISION-32 text (the anchor moves REVISION
31/VC33+VC33b+VC33c → REVISION 32/VC34; the wave-14/FGMN mega-arc now
CLOSED AND CERTIFIED END-TO-END inside the anchor); the TR3-S4
ACCEPTANCE folded: the 4-pass/2-revision arc closed at cd611af — with
accepted TR3-S1/S2/S3, **TR-3′-GEN is PROVED on the THREE-component
union {all order-2} ∪ {order-3 all-e_j = 1, PRINT-UNCONDITIONAL} ∪
{order-3 e₀ = 1, e₁ ≥ 2, e₂ = 1, conditional on (H-ℓ) + (KP_i)+[T] at
S6.2 + the PIN-GEN-i attribution display [r34-ERRATUM, VC35 gap 1: the
three conditions spelled out at this closing site too — the prior
"displayed standing rows" abbreviation met the charge nowhere else],
with Lemma HV3-E2 derived from the pinned print}** — the strata
family at FOUR accepted members, the F2c verifier-owned implementation
discharged IN-PIPELINE for the first time (1,108 checks with
mutation-control teeth), deeper strata OPEN probe-first with NO
candidate sealed; the TWIST3-probe HYGIENE bracket applied (ONE dated
additive bracket at the pre-correction KEY0-LIFT orientation label, the
measured data untouched); SYNTHESIS PASS 8 OWED at the wave-15 close;
NINE rows recounted; the census re-run at the declared unit with every
delta explained; **VC35 owed on this REVISION-33 text** — until it
lands, the acceptance citation points at the REVISION-32 text.

## 40. REVISION 34 (r35, the VC35+VC35b re-anchor + the WAVE-16 fold) — the anchor moves REVISION 33; ITER-LAW at attempt grade on the harness tower class; the banked sorry retired; the first (H7) decision point; DITERSUP priced

**What this revision is.** The r35 cadence event plus the wave-16 fold.
**VC35 + VC35b CONFIRMED** (ledger 345b432 "VC35 + VC35b CONFIRMED";
reports `lean/notes/openmath/VC35_report.md` (archived at d79f295) and
`VC35b_report.md`): VC35 (full audit of REVISION 33) returned 0
critical / 2 gaps — gap 1 the §39 closing abbreviation, cured by the
dated **r34-ERRATUM** (d79f295: the third strata component's conditions
spelled out at the closing site too — (H-ℓ) + the (KP_i)+[T]
consumptions at S6.2 + the PIN-GEN-i attribution display); gap 2
AUDIT-INPUT (the empty TWIST3-bracket appendix slice — the checklist's
rule-5 wording defect, recorded); VC35b (the narrow companion on the
corrected closing + the ACTUAL bracket slice, the VC30b/VC31b genre)
CONFIRMED 0C/0G — so **the acceptance anchor moves REVISION 32/VC34 →
REVISION 33/VC35+VC35b** (the SIXTEENTH audited re-anchoring; the
citation-text update rides this revision per the VC22 precedent).
CONSEQUENCE of the re-anchor: the TR3-S4 arc is now CERTIFIED
END-TO-END INSIDE the anchor — the strata family's THREE-component
union {all order-2} ∪ {order-3 all-e_j = 1, PRINT-UNCONDITIONAL} ∪
{order-3 e₀ = 1, e₁ ≥ 2, e₂ = 1, conditional on (H-ℓ) + the (KP_i)+[T]
consumptions at S6.2 + the PIN-GEN-i attribution display, with Lemma
HV3-E2 DERIVED from the pinned print}, the F2c in-pipeline discharge,
and the TWIST3 hygiene. On the newly re-anchored text this revision
folds **THE WAVE-16 BATCH** — the four synthesis-7-tail units, all
EXECUTED and ledger-recorded (2a25ed9 / ab7c31c / 242ba1e / 15372d1 /
037ac6f), records in the four subsections below. NO roster change:
**NINE rows** recounted; no clause attribution moved; this unit ran no
audit and NEVER self-confirms — **VC36 is OWED on this REVISION-34
wording**.

### 40.(i) ITER-LAW — PROVED AT ATTEMPT GRADE on the harness tower class

The wave's headline result (note `ITERLAW_2026-08-08.md`; ledger
"ITER-LAW ATTEMPT ... PROVED at attempt grade"): D-ITER's statement of
record — DITER_RESTATE §S2's (ITER-LAW), previously an OPEN LEMMA at
0/804 + 0/840 — is **PROVED as a finite-algebra identity on the
harness tower class** (standard-lift towers — exactly the class the
statement of record pins, and the class the ENTIRE instance base
inhabits: all 52 strata-probe towers + all JUNCFORGE run-realized
classes): for all window pairs, c(γ,γ′) =
z̄^{δ₂}·z₁^{(s₁(γ)+s₁(γ′)−s₁(γ+γ′)+ℓ₀γ₂δ₂)/e₀}. ROUTE = the chartered
CARRY-1/W-MULT bookkeeping run fibred: L1 EQ12-ADD at BOTH levels
(outer s₂/e₁ carry against γ₂ stacked over inner s₁/e₀ carry against
h₀) + L2 INNER-SHIFT (γ₁(γ+γ′) = γ₁+γ₁′+δ₂γ₂) + L3 WINDOW closure +
L4 EPS-CLOSED (ε₁(β) = z₁^{(s(β)−ℓ₀β)/e₀}) + L5 EPS-MULT + Lemma EXP
(the two-line Bézout compose kernel, the TR3-S2 Lemma-EXP genre) + L6
ANCHOR-VAL (val(φ_γ) = ε₁(γ₁(γ))) + L7 PAIR-VAL (Case I the
single-digit inner-slot shift z₁^{δ₁}; Case II the g₀ = 1 standard-lift
reduction with residue −ψ₀(0) = z₁ — the two mechanisms give the SAME
constant). NO printed formula, extraction, or GMN semantics consumed —
every object a harness-calculus definition; (W-MULT) is RECOVERED on
anchor pairs as a corollary, NOT consumed. Displayed corollaries = the
DITER §S3 sanity gates: B0a associativity (two proofs), B1
integrality, the D-REAL e₀ = 1 degeneration (c = z̄^{δ₂} verbatim) and
the e₁ = 1 degeneration, B0c ℤ/E₂ descent, plus the S6.6 NOT-CYCLIC
compatibility display. **Two-commit seal honored**: falsifier
`iterlaw_check.py` sealed PRE-RUN at ab7c31c (fresh rows P/Q/R/S/U/V/W/X,
fresh seed 20260825, F1–F10 + CERT, verdict block PENDING); run landed
at 242ba1e — **ALL GREEN FIRST PASS, exit 0**: 35,232 samples, 0
violations in every family; Case-II coverage MET (the proof's one
lift-form-consuming step exercised); row-X gate NOT-CYCLIC 4/4 with
EXACT value-set match to the strata-probe row-N tables {z₁, z̄, z̄z₁^b}.
**HONEST RESIDUALS (verbatim from the note's scope box):**
**(ITER-LAW-LIFT)** — general lawful (non-standard) lifts OPEN exactly
at g₀ = 1 ∧ δ₁ = 1 (the one lift-form-consuming step; everything else
is lift-form-free; NO instance evidence exists outside the proved
class); the gr(w₂) semantics wrapper rides the accepted GRB retarget,
consumed by IFACE(c) CONSUMERS, not by the theorem; orders ≥ 3 NOT
TOUCHED; **ACCEPTANCE COUNTER 0** — the hostile-pass arc is the
acceptance bar and is IN FLIGHT (Codex content pass + Fable
execution/F2c pass, running concurrently with this fold; the F2c
verifier-owned from-note implementation expected there). Status-upgrade
brackets applied THIS fold (the ITER-LAW unit's owed list): DITER_RESTATE
§S2 (OPEN LEMMA → theorem-on-class bracket), GRB S5.3 + S8 roster item
2 (same bracket), PROJECT_STATE (the (H1)(iii)-genre ITER-LAW status
displays; compressed). The §3.1 (H1) row carries the r35 bracket; the
row STAYS [M] — an attempt-grade result with counter 0 upgrades NO
acceptance status anywhere.

**[CONCURRENCY DISCLOSURE — added at this fold's close, before its
commit: BOTH hostile passes LANDED while this fold was executing (each
read the proof note as committed; nothing in this fold touched it).
The Fable execution/F2c leg returned CLEAN 0 critical / 0 gaps
(64ac340 — the verifier-owned from-note reimplementation ALL GREEN,
89,056 samples / 0 violations on 6 fresh rows at a fresh seed, 4/4
mutation controls detected, PLUS the sealed runner re-run byte-exact).
The Codex content leg returned NOT-CLEAN, 1 critical + 1 gap (acc955a
— the critical: an E₂ > 1 SCOPE MISMATCH between the proof note's
setting (e₀, e₁ ≥ 1) and the statement of record's pinned E₂ > 1
composite-stage setting, orchestrator-confirmed; the gap: an F8
mixed-block under-test). THE ACCEPTANCE COUNTER STAYS 0; the errata
round is OWED TO THE NEXT UNIT — this fold applies no fix and
adjudicates nothing; the attempt-grade displays of this revision
stand, since they claim attempt grade only, which the pending errata
round does not retract.]**

**[ACCEPTANCE UPGRADE — applied at the r36 fold (REVISION 35, §41),
curing VC36 F1/F2 as FOLD-LAG: the hostile-pass arc CLOSED at the
2-clean model-diverse bar and **ITER-LAW is ACCEPTED** (orchestrator
adjudication, ledger 33373cb). THE ARC OF RECORD: PC1 (Codex content)
NOT-CLEAN 1C/1G (acc955a — the E₂ > 1 scope-mismatch critical + the
F8 mixed-block gap, the two facts the concurrency disclosure above
recorded) → PE1 (Fable execution/F2c) CLEAN 0C/0G (64ac340 — the
verifier-owned from-note reimplementation ALL GREEN, 89,056 samples /
0 violations on 6 fresh rows at a fresh seed, 4/4 mutation controls
detected, + the sealed runner re-run byte-exact) → ERRATA ROUND 1
(7566ccd — remedy 1: the S0 scope ERRATUM, striking the note's "on
the class its setting pins" clause: the harness class takes
e₀, e₁ ≥ 1, a STRICT SUPERSET of DITER_RESTATE §S2's pinned
E₂ = e₀e₁ > 1 setting, corrected to restricted-coincidence; remedy 2:
the COR-4 mixed-block supplement `iterlaw_cor4_mixed.py` — all four
mixed blocks W2×W1/W1×W2/W3×W1/W1×W3 + the fresh random-offset window
W4 both ways + closed-form-at-mixed-args + two fresh rows Y/Z with
PARI T11 fired — **ALL GREEN, exit 0: 18,396 samples / 0 violations,
seed 20260808** (M1–M4 1,524/0 each · M5 3,048/0 · CF 9,144/0 · CERT
108/0); the sealed falsifier BYTE-UNTOUCHED, md5 == seal ab7c31c) →
PC2 (Codex content, fresh) CLEAN 0C/0G (804b88a) + PE2 (Fable
execution + content, fresh) CLEAN 0C/0G (c40a1da — sealed re-run
byte-exact 35,232/0 with all family counts equal to the sealed JSON;
supplement re-run 18,396/0; F2c re-run 89,056/0; errata brackets
verified insertions-only). **ACCEPTANCE COUNTER 2/2 — the bar is
MET** (errata rounds do not count; the two CLEAN legs are the fresh
post-errata pair, model-diverse). SCOPE AS ACCEPTED — superseding
EVERY "the class DITER_RESTATE §S2 pins / exactly the class the
statement of record pins" phrasing in this record: **proved and
ACCEPTED on the harness tower class** (standard-lift; a STRICT
SUPERSET of DITER_RESTATE §S2's pinned E₂ = e₀e₁ > 1 setting;
coincides with the §S2 display on the nose restricted to E₂ > 1; the
E₂ = 1 leg a harmless COR-3-covered extension outside the statement
of record). RESIDUALS unchanged by acceptance, ALL THREE:
**(ITER-LAW-LIFT)** open exactly at g₀ = 1 ∧ δ₁ = 1; the gr(w₂)
semantics wrapper rides the accepted GRB retarget consumer-side;
orders ≥ 3 untouched. The FOURTH accepted member of the wave-15/16
lineage families; closes synthesis-7 queue #6 end-to-end. Display
sites swept at r36: this record, the §0 standing paragraph, the §3.1
(H1) row, the §40 close (the VC36-F3 erratum), DITER_RESTATE §S2, GRB
S5.3 + S8 item 2, DITERSUP §S4/J3b, PROJECT_STATE.]**

### 40.(ii) THE BANKED SORRY RETIRED — `npVertices_stable_of_hull_preserved` PROVED, Lean-core, statement byte-unchanged

(15372d1; note `BANKED_SORRY_2026-08-08.md`; synthesis-7 queue #10,
pass-6 finding F2d.) The corpus's ONE banked `sorry` —
`OM.Classifier.npVertices_stable_of_hull_preserved`, the repaired
three-hypothesis hull-invariance contract, off the capstone path,
banked since the 2026-07 REDSTAB2 round — is **PROVED**, and the
STATEMENT IS BYTE-UNCHANGED (only `:= by sorry` became a term-mode
`exact` of the new core theorem). Vehicle: the NEW module
`lean/LeanUrat/OM/HullStability.lean` (~1740 lines, imports Mathlib +
`OM/NewtonPolygon` ONLY; the BPLL §0 list kernels copied VERBATIM with
attribution — a DELIBERATE hygiene copy: importing BPLL would drag the
183-module Scaffold closure under the capstone import path; no import
cycle either way, checked). Key lemmas: (§D) the SURVIVOR-CORNER law
`dropCollinear_corner` (dropCollinear on a strictly-sorted list leaves
NO collinear consecutive triple — the converse companion of BPLL's
`dropCollinear_between`; junction closed by the cross-product identity
`collinear₃_trans'`); (§E) `full_zip_valid` (consecutive FULL-vertex
chords are valid — the flattest-chord transfer), `glue_aux`/
`vert_zip_attain` (the fuel induction lifting chord attainment to
consecutive GENUINE vertices); (§F) the two-hull agreement on the
common abscissa range + slope transfer + `sorted_eq_of_mem_iff`.
Falsifier (test-before-prove, falsifiers only):
`verification/openmath/banked_sorry_falsifier.py` — 0/4000 (repaired
statement) + 0/4000 (survivor-corner law) PRE-PROOF, Lean-defs-exact
mirror incl. junk-0 division. COMPILED EVIDENCE: full `lake build`
green (8698 jobs); `lake env lean LeanUrat/AxChk_baseline.lean` exit 0
with **ZERO `sorryAx` anywhere in the 1099-line census**, ALL capstone
footprints UNCHANGED Lean-core; direct probes:
`npVertices_stable_of_hull_preserved` and
`boxValSupport_reduce_stable_R` now `[propext, Classical.choice,
Quot.sound]`; `certLevel_stabilizes` drops its `sorryAx` to `[propext,
Classical.choice, Quot.sound, omReadValuation_lt_of_certLevel_fkeyed]`
— the declared NAMED CITE axiom ONLY, exactly as predicted. The OM
tree greps ZERO `sorry` statements. `Classifier.lean` touched ONLY at
one import line + the term-mode fill + four honesty-comment sites;
BPLL UNTOUCHED. **The corpus's banked-sorry count: 1 → 0** — the
CLAUDE.md / PROJECT_STATE "exactly one banked sorry" lines are updated
at THIS fold (dated, honest); `docs/AXIOM_FAITHFULNESS.md` swept — it
carries no banked-sorry mention, no edit needed. RESIDUALS: none
mathematical; the kernel IS this unit's verification leg (pure Lean
unit); a fresh-context verifier pass beyond the kernel check is at the
ROOT's discretion at the next acceptance arc.

### 40.(iii) H7 FORGE — the FIRST compiled NON-VACUOUS (H7) decision point

(037ac6f; note `H7FORGE_2026-08-08.md`; synthesis-7 queue #8, pass-7
F4, on H7PROBE P-4's sharpened OL-2 inhabitant spec + N-H7-1's two
measured pins.) Artifact:
`lean/LeanUrat/Scaffold/HDischarge/H7/H7Forge.lean` — green exit 0,
in-file census **15 declarations ALL Lean-core, zero sorryAx**. The
instance: a sorry-free FIVE-SITE DEPTH-1
`TreeModel`+`CellData`+`VTree`+`SiteLedger` at (p,n,N,m) = (2,2,2,4),
the S6 shape (m = 2 roster, side-separated recorded slopes 1 vs 2).
BOTH P-2 rfl-degeneracies BROKEN: (a) **non-diagonal `branchCellOf`**,
certified THREE ways — `forge_read_nondiagonal` (sibling read maps
differ at a point), `forge_states_crosscut` (sibling state events
overlap non-trivially AND differ — diagonal-unrepresentable), and the
STRUCTURAL breaker `forge_no_diagonal_read` (NO single-map read over
ANY cell type presents both sibling states, quantified over (γ, Ψ));
(b) **sides = 2** at the joint site fHj with the REAL `SideSplit`
(2-clause fresh system over disjoint side spans, hull slopes 1 < 2
tied to the roster nodes' recorded slopes; `splitAt`/`hsplit_k`/
`hsplit_exp` all proved) — **(JC-multi) DECIDED TRUE**
(`forge_jcmulti`, 1·2² = 4). THE PIN-CORRECTED FACES DECIDED AT THE
INSTANCE: **PIN 1** — `forge_kloc_slope_keyed` PROVED (every branch
read IS the slope-keyed normalization of the joint read) +
`forge_kloc_pooled_refuted` = the COMPILED COUNTERMODEL to the
slope-pooled strengthening at this very instance (the S5 pooled-u
refutation's compiled twin: any `KLocSemantic` row must key ε per
realized slope, never pool); **PIN 2** — `forge_guard : InGuard
forgeDatum` + the decide-small gates stated ONLY in-guard ((N, τ,
e_max) = (2, 1, 0)): **`forge_klocCounts` = the FIRST contentful
`KLocCounts` instantiation in the corpus** (H7PROBE P-3 recorded
zero) — the (SIB)-shaped identity holds EXACTLY at every realized
joint read word (1·4 = 2·2), decided by kernel evaluation over the
16-point box; `forge_stratum_count`/`forge_branch_counts` tie the
datum integers to the ACTUAL box cardinalities (not free pins). The G1
`mem_realizable` wall avoided STRUCTURALLY (all chains one-node).
**HONESTY (verbatim from the note):** DEPTH-1 — the sides-2 site's own
continuations are NOT realized (that needs coherent 2-node histories —
the HK23/HK25 transplant, the NAMED residual, out of scope); node
literals SYNTHETIC-BUT-LAWFUL (the S2 center-distinction genre — the
read tables are the forge's own keying, NOT derived from an engine
run); engine-tied `ITauSemantic`/`KLocSemantic` (H7-W3a) remain the
unchanged Tier-M obligation; `ITauShadow` at the forge integers NOT
attempted (Tier-S, H7-D1b genre — the shadow row is never evidence).
**NOTHING here discharges (H7)** — the forge replaces H7PROBE's "no
compiled decision point of its own" state with a first compiled
non-vacuous DECISION POINT; the row STAYS [M] (r35 bracket at the §3.1
(I-τ)/K-LOC row).

### 40.(iv) DITERSUP STATEMENT — the supplier's corrected interface displayed; EVERY gate BLOCKED-ON-APPLICATION; priced

(2a25ed9; note `DITERSUP_STMT_2026-08-08.md`; synthesis-7 queue #9,
re-priced per the r34/§39 standing against the ACCEPTED FGMN
diagnosis.) **STATEMENT-ONLY: no `.lean` touched, no proofs, nothing
asserted** — every Lean block in the note is a DISPLAY. Displays,
verbatim-typed against the accepted diagnosis in its RECOMMENDED
new-definition mode: `SideReads′` (= the compiled `SideReads` verbatim
EXCEPT the three re-keyed σ.wPrev tokens, marked; clauses (iii)–(vi)
byte-identical), `ReadsOf′`, `DIterJunctionSupplier′` (three name
substitutions, shape verbatim, the ITER-LAW docstring fence carried
over), the §S3 D2 carrier interface (`DIterCarrier`/`δ₂`/
`InnerIntegral`/`IterLaw` — with the honest display that the clause
tying c to the ACTUAL degree-0 digit algebra of gr(w₂) is MISSING from
the corpus, the deep-wild order ≥ 2 classifier obligation, OUT OF
SCOPE, displayed so no gate silently pretends to it), and the junction
gates with prices: **J1** row-A e ≥ 2 recording (M) · **J2** first
non-vacuous glue at the row-A seam (L) · **J3a** the supplier's first
inhabited instance — ITER-LAW-FREE by the D-REAL degeneration at
e₀ = 1, the degeneration DISPLAYED (M) · **J3b** the general discharge
(L; blocked on the tie layer + ITER-LAW) · **JS1/JS2/JS3** sanity
gates (S/S/M) · carrier layer **P1** (M) · the application itself
**P0** (L, Asvin-gated). **EVERY gate is BLOCKED-ON-APPLICATION**: the
three-token repair (`ν.σ.w` → `ν.σ.wPrev` at HC2/Defs.lean:285/287/292)
is Asvin-gated and NOT applied; until the gate fires the ONLY compiled
truths in the area are the old-clause records, byte-frozen exactly
where JUNCFORGE_S2 left them (the e ≥ 2 junction walls REFUTED at the
row-A fiber; the compiled glue gate pair VACUOUS at its D0 seam — NOT
resurrected under either scale). PERIMETER FINDING OF RECORD:
`TruncRunFrom` binds `SideReads` by name at its [T-5] interior clause
(TruncatedRun.lean:230), so `TruncRunFrom′` joins the primed-mirror
change-set (`FTiedTruncLanding′`/`ftie_extends′` likewise) —
consistent with the REVISION-9 rule that completeness is adjudicated
on the actual diff. **CROSS-REFERENCE APPLIED THIS FOLD (both here and
in the note's §S4/J3b gate display):** J3b's blocked-on-ITER-LAW leg
MAY now cite **ITER-LAW proved-on-class** — the harness tower class
(standard-lift towers), 40.(i) — at ATTEMPT GRADE, acceptance pending
(counter 0, hostile passes in flight); the citation is valid only at
standard-lift instances (all compiled ones are), and the gr(w₂) tie
layer remains a separate prerequisite.

**The §29/§30/§33/§34/§35/§36/§37/§38/§39 ASVIN FLAG — lineage
continued; the batch stands at FOUR items, unchanged by this fold.**
The standing Asvin batch, RESTATED IN FULL: (1) the `TowerMoveClosureL`
two-layer re-ratification; (2) the fence-(vii) ROUTE (a)
ratify-or-reopen lineage — the GR route (a) + the RECSL `hσt`-pin
rider + the EXECUTED F-ADM-3 re-key (ba02d60+8648d99, recorded for
ratify-or-reopen); (3) **the FGMN proposal — the flag lineage's TOP
item, FULLY VERIFIED (ACCEPTED at ec64503)**: the Asvin decision is
EXACTLY the statement-change application itself, adjudicated on the
APPLICATION UNIT's scratch-worktree diff per the REVISION-9 scope cut
(post-application hostile pass as the completeness test; the REVISION
4-8 censuses the reviewer's informative map) — NOTE: the wave-16
DITERSUP statement (40.(iv)) PRICES the post-application gates but
applies NOTHING, and its P0 row is this very gated item; (4) the
Route-B **RB-DELTA decision, DECISION-READY** (the (UB-X) walk =
condition (5)'s substance; the note's §6 is the one-paragraph
decision).

**Disposition table.**

| item | ground | grade | disposition | edit sites |
|---|---|---|---|---|
| r35-1 | ledger 345b432 "VC35 + VC35b CONFIRMED" (VC35 0C/2G → r34-ERRATUM d79f295 → VC35b 0C/0G; reports `VC35_report.md`/`VC35b_report.md`) | ACCEPTANCE RE-ANCHOR — the SIXTEENTH; citation-text update rides this revision per the VC22 precedent | every operative anchor site updated: the §0 REVISION-33 paragraph header superseded and the NEW REVISION-34 standing paragraph states the anchor (REVISION 33/VC35+VC35b) with the arc lineage; the §3.2 end-to-end row gains the VC35+VC35b sync tail (RE-ANCHORS on REVISION 33; VC36 owed on THIS text); the §6 r34 bracket's anchor sentence carries a dated supersession bracket and the new r35 bracket is current. Historical records (§§7–39) untouched beyond the named dated brackets | §0, §3.2 end-to-end row, §6 |
| r35-2 | ledger "ITER-LAW ATTEMPT ... PROVED at attempt grade on the harness tower class" (note `ITERLAW_2026-08-08.md`; seal ab7c31c, run 242ba1e, ALL GREEN FIRST PASS exit 0: 35,232/0, F1–F10+CERT, Case-II coverage MET, row-X NOT-CYCLIC 4/4 exact value-set match) | ATTEMPT-GRADE FOLD — status displays upgrade to proved-on-class; NO acceptance claim (counter 0, hostile passes IN FLIGHT); the class qualifier (standard-lift towers, the class DITER_RESTATE §S2 pins) carried at EVERY mention | the §0 REVISION-34 paragraph fold (1); the §3.1 (H1) row's r35 bracket at its (iii) member; §40.(i); the owed status brackets applied note-side: DITER_RESTATE §S2, GRB S5.3 + S8 item 2, PROJECT_STATE ITER-LAW displays | §0, §3.1 (H1) row, this record; note-side: DITER_RESTATE, GRB, PROJECT_STATE |
| r35-3 | ledger "BANKED SORRY DISCHARGED" (15372d1; note `BANKED_SORRY_2026-08-08.md`; falsifier 0/4000+0/4000 pre-proof; lake build 8698 jobs green; AxChk exit 0, zero sorryAx in the 1099-line census, capstone footprints unchanged) | LEAN DISCHARGE — statement byte-unchanged; the corpus's banked-sorry count 1 → 0; the stale "exactly one banked sorry" operative lines updated honestly at this fold | §0 fold (2); §40.(ii); repo-side: CLAUDE.md axiom-policy line + PROJECT_STATE banked-sorry section (dated brackets); AXIOM_FAITHFULNESS swept, no mention found, no edit | §0, this record; repo-side: CLAUDE.md, PROJECT_STATE |
| r35-4 | ledger "H7 FORGE" (037ac6f; note `H7FORGE_2026-08-08.md`; `H7Forge.lean` 15 decls Lean-core, zero sorryAx) | FIRST COMPILED NON-VACUOUS (H7) DECISION POINT — both OL-2 rfl-degeneracies broken; the two N-H7-1 pins decided at the instance (slope-keyed PROVED; pooled COMPILED-REFUTED; first contentful KLocCounts); NOTHING discharges (H7); the row STAYS [M] | §0 fold (3); the §3.1 (I-τ)/K-LOC row's r35 bracket; §40.(iii) | §0, §3.1 (I-τ)/K-LOC row, this record |
| r35-5 | ledger "DITERSUP STATEMENT" (2a25ed9; note `DITERSUP_STMT_2026-08-08.md`; statement-only, no .lean touched) | PRICED + GATED — every gate BLOCKED-ON-APPLICATION; old-clause records byte-frozen; `TruncRunFrom′` perimeter finding of record; the J3b cross-reference to ITER-LAW proved-on-class (attempt grade) added note-side this fold | §0 fold (4); §40.(iv); note-side: the ONE J3b cross-reference bracket in `DITERSUP_STMT_2026-08-08.md` §S4 | §0, this record; note-side: the DITERSUP note |
| r35-6 | housekeeping (the /goal cadence) + cadence (the VC22 precedent on the re-anchored text) | TRACKING DISPLAY + CADENCE — VC36 OWED; never self-confirm | **SYNTHESIS PASS 8 is OWED at the WAVE-16 CLOSE** (the synthesis-7 tail is now fully executed; the ITER-LAW hostile-pass arc + the VC36 pass are THE in-flight verification). The Asvin batch RESTATED IN FULL (the flag paragraph above; FOUR items, TOP = the verified FGMN proposal). The sorry census of record: the r27 display (36 repo-wide / B2D 0) is STALE BY AT LEAST THE RETIRED BANKED TOKEN (this wave touched Lean: HullStability + the Classifier fill + H7Forge, all sorry-free; OM tree ZERO sorry) — no full repo-wide re-sweep this fold, the re-sweep rides the next census unit. REVISION 34 is a text change on the newly re-anchored text: the confirming pass **VC36 is OWED on the REVISION-34 wording** (charge below; model diversity per the tree §4 bar); until it lands, the acceptance citation points at the REVISION-33 text; VC36 is a fresh-context pass, not this unit | §0, §3.2 end-to-end row, §6, this record |

**THE ENUMERATION AUDIT (the r13 rule, run over the post-edit text).**
Unit: OCCURRENCE COUNTS (`grep -o | wc -l`, exact case-sensitive patterns,
the r23-declared unit), taken on the FINAL text of this revision (counts
include this record's own quotations — the r23–r34 precedent). The §39
sixty-one patterns plus NINE markers introduced at r35 (`VC35b` — the
narrow companion now inside the anchor citation; `VC36` — the owed
confirming pass, named nowhere before this fold; `ITERLAW` / `H7FORGE` /
`DITERSUP` / `BANKED_SORRY` — the four wave-16 note names entering ROOT
with this revision's displays; `HullStability` / `H7Forge` — the two new
Lean modules; `ITER-LAW-LIFT` — the wave's named open residual). The r34
column = the §39 recorded census; where the CURRENT pre-edit text differs
from that recording, the difference is the post-census r34-ERRATUM
(d79f295 — the only ROOT edit between the §39 census and this fold) and
is called out in the reason cell (the §37/§39 baseline-correction
precedent). Every delta named:

| grep (occurrences) | r34 | r35 | delta | why |
|---|---:|---:|---:|---|
| `(R1)–(R4)` | 123 | 124 | +1 | no operative change — this table's own pattern cell |
| `(H4a)` | 203 | 204 | +1 | no operative change — this table's own pattern cell |
| `K3-δ` | 140 | 141 | +1 | status unchanged (clause (R) keeps consuming it until the Asvin gate fires) — this table's own pattern cell |
| `SITE-EXH` | 37 | 38 | +1 | no arc status change — this table's own pattern cell |
| `GRADED-READ` | 31 | 32 | +1 | no operative change — this table's own pattern cell |
| `TransDeep` + `TRANS-DEEP` | 53 | 55 | +2 | no operative change — both increments are this row's own joint pattern cell (one hit each) |
| `TEN rows` | 39 | 40 | +1 | historical-only — this table's own pattern cell |
| `NINE rows` | 73 | 78 | +5 | roster restated NINE, no change — the §0 REVISION-34 paragraph, the §3.2 sync tail, this record's opening and close, and this table's own cell |
| `VC25` | 61 | 62 | +1 | closed history — this table's own pattern cell |
| `VC23` | 30 | 31 | +1 | named-but-unrun history stands — this table's own pattern cell |
| `VC26` | 108 | 109 | +1 | closed history — this table's own pattern cell |
| `ROUTE (a)` | 57 | 60 | +3 | the Asvin-batch restatements that fall unwrapped at the census's line unit (the §0 REVISION-34 paragraph, the flag paragraph above) + this table's own cell; still zero sites claim route (a) proved anything |
| `TR3-S2` | 44 | 46 | +2 | accepted status unchanged — §40.(i) names its kernel genre once (the compose kernel's lineage) + this table's own pattern cell |
| `REC-SL` | 45 | 46 | +1 | status unchanged — this table's own pattern cell |
| `RECSL` | 59 | 63 | +4 | the hσt-pin rider namings in the full Asvin-batch restatements (§0, the §6 r35 bracket, the flag paragraph) + this table's own cell; status unchanged |
| `HUNI-V2` | 17 | 18 | +1 | closed history — this table's own pattern cell |
| `VC27` | 135 | 137 | +2 | closed history — this table's own cells (this row and the row below) |
| `VC27b` | 59 | 60 | +1 | closed history — this table's own pattern cell (each hit also counts one at the row above) |
| `VC28` | 82 | 84 | +2 | closed history — the charge's checklist-validation naming + this table's own pattern cell |
| `VC29` | 89 | 91 | +2 | closed history — the charge's checklist-validation naming + this table's own pattern cell |
| `POOL-DICT` | 52 | 53 | +1 | closed history — this table's own pattern cell |
| `TR-4` | 39 | 40 | +1 | residue-taxon status unchanged — this table's own pattern cell |
| `BasePolygonLengthLaw` | 45 | 46 | +1 | proved status stands — this table's own pattern cell |
| `ITER-LAW` | 53 | 98 | +45 | THE WAVE-16 HEADLINE FOLD — the attempt-grade displays with the class qualifier at §0 fold (1), the §3.1 (H1) row's r35 bracket, the §3.2 sync tail, the §6 r35 bracket, §40.(i)/(iv), the disposition table, the charge, and the close, PLUS containment (every hit of the residual's name three rows from the bottom counts here too) + this table's own cells (this row and that residual row); status = PROVED AT ATTEMPT GRADE ON THE HARNESS TOWER CLASS, counter 0 — zero sites claim acceptance or a general-lift scope |
| `leakFree` | 68 | 69 | +1 | typed-face status unchanged — this table's own pattern cell |
| `LeakFreeCarrierV4` | 14 | 15 | +1 | no new consumption — this table's own pattern cell |
| `junc_bundle` | 84 | 85 | +1 | the accepted fixed point retains the old-clause meaning — no new naming this revision; this table's own pattern cell |
| `G-ADM-2` | 29 | 30 | +1 | closed routing history — this table's own pattern cell |
| `BPLL` | 22 | 29 | +7 | the banked-sorry fold's provenance displays (the verbatim-copy hygiene decision, its kernels and its untouched status) at §0 fold (2), the §6 r35 bracket, and §40.(ii) + this table's own cell; proved status stands, the module itself untouched |
| `K1ConsumerFace` | 19 | 20 | +1 | discharged-face status stands — this table's own pattern cell |
| `N-H7-1` | 38 | 44 | +6 | the forge fold's pin displays (the §3.1 (I-τ)/K-LOC row's r35 bracket, the §6 r35 bracket, §40.(iii), the disposition table, the close) + this table's own cell; the row's statements still OPEN |
| `VACUOUS-BY-TYPE` | 14 | 15 | +1 | the r28 verdict stands (now superseded as the forge's predecessor state) — this table's own pattern cell |
| `ADM-REAL` | 43 | 44 | +1 | acceptance history stands — this table's own pattern cell |
| `CLASS-LAT` | 27 | 28 | +1 | machine-checked status stands — this table's own pattern cell |
| `JUNCFORGE` | 20 | 23 | +3 | the exact-case unit counts the instance-base class naming at §40.(i) and the byte-frozen-record naming at §40.(iv) + this table's own cell; mixed-case namings never count here |
| `TR3-S3` | 40 | 41 | +1 | accepted status unchanged — this table's own pattern cell |
| `VC30` | 120 | 126 | +6 | closed history — the narrow-companion genre namings (each hit of the suffixed name counts here too, containment) at the §3.2 sync tail, this record's opening, and the fixed-point paragraph + this table's own cells (this row and the row below) |
| `VC30b` | 55 | 59 | +4 | the narrow-companion genre namings (the §3.2 sync tail, this record's opening, the fixed-point paragraph) + this table's own pattern cell; every hit also counts one at the row above |
| `VC31` | 165 | 170 | +5 | closed history — the genre namings' suffixed hits (containment) + this table's own cells (this row + the two rows below) |
| `VC31b` | 49 | 52 | +3 | the narrow-companion genre namings (the §3.2 sync tail, this record's opening) + this table's own pattern cell; every hit also counts one at the row above |
| `VC31c` | 46 | 47 | +1 | closed history — this table's own pattern cell; every hit also counts one at the plain-name row above |
| `VC32` | 111 | 114 | +3 | closed history — the charge's checklist-validation naming + this table's own cells (this row and the containment hit at the row below) |
| `VC32b` | 43 | 44 | +1 | closed history — this table's own pattern cell; every hit also counts one at the row above |
| `VC33` | 149 | 153 | +4 | closed history — the charge's lineage-lesson naming + this table's own cells (this row + the containment hits at the two rows below) |
| `VC33b` | 37 | 38 | +1 | closed history — this table's own pattern cell; every hit also counts one at the row above |
| `VC33c` | 41 | 42 | +1 | closed history — this table's own pattern cell; every hit also counts one at the plain-name row above |
| `VC34` | 50 | 57 | +7 | the OLD side of the anchor move — the re-anchor displays name the superseded anchor at §0, the §3.2 sync tail, the §6 r35 bracket, this record's opening and close, and the charge's no-stale-anchor check + this table's own cell; every site cites it as SUPERSEDED, none as current |
| `H-v3` | 62 | 63 | +1 | discharged status stands — this table's own pattern cell |
| `HV3-THM` | 39 | 40 | +1 | accepted status stands — this table's own pattern cell |
| `PIN-GEN-i` | 44 | 54 | +10 | +1 is a BASELINE CORRECTION (the post-census r34-ERRATUM d79f295 added the closing-site conditionality display); the rest = the third strata-component's conditions at every union restatement and every arc-lineage display of this revision (§0, the §3.2 sync tail, the §6 r35 bracket, this record's opening and close) + this table's own cell; the pin's discharge history unchanged |
| `TWIST-3` | 42 | 43 | +1 | status unchanged — this revision's hygiene namings are unhyphenated and do not match at this exact unit; this table's own pattern cell only |
| `TR3-S4` | 69 | 73 | +4 | the arc is now INSIDE the certified anchor — the re-anchor displays (§0, this record's opening, the §6 r35 bracket) + this table's own cell; no status change beyond the certified re-anchor |
| `RB-DELTA` | 26 | 30 | +4 | the Asvin-batch restatements (§0, the §6 r35 bracket, the flag paragraph) + this cell; every site states decision-ready + Asvin-gated, zero sites claim adoption |
| `wPrev` | 21 | 25 | +4 | the supplier-statement displays name the three re-keyed tokens (§0 fold (4), §40.(iv) twice) + this table's own pattern cell; zero sites claim the tokens applied |
| `classCount_eq_d` | 9 | 10 | +1 | proved status stands — this table's own pattern cell |
| `SideClauseR2` | 10 | 11 | +1 | the informative-map member under the scope cut — no new naming; this table's own pattern cell |
| `readCeil` | 12 | 13 | +1 | same disposition as the row above — no new naming; this table's own pattern cell |
| `HV3-E2` | 17 | 22 | +5 | the derived-lemma clause of the strata-union restatements (§0, the §6 r35 bracket, this record's opening and close) + this cell; every site says DERIVED from the pinned print |
| `VC35` | 19 | 90 | +71 | +1 is a BASELINE CORRECTION (the post-census r34-ERRATUM bracket names its gap source); the rest = the NEW side of the anchor move — the arc record (0C/2G → erratum → narrow companion) displayed at §0, the §3.2 sync tail, the §6 supersession + r35 brackets, and throughout this record, PLUS containment (every hit of the suffixed companion at the row below counts here too) + this table's own cells; every site cites the arc as CONFIRMED on the REVISION-33 wording, none as run on THIS text |
| `KEY0-LIFT` | 9 | 10 | +1 | hygiene history stands (now inside the certified anchor) — this table's own pattern cell |
| `VC35b` | 0 | 34 | +34 | NEW at r35 — the narrow companion enters the anchor citation: displayed at every re-anchor site (§0, §3.2, §6, this record) + the fixed-point paragraph + this table's own cell; every hit also counts one at the plain-name row above |
| `VC36` | 0 | 15 | +15 | NEW at r35 — the owed confirming pass: §0, the §3.2 sync tail, the §6 r35 bracket, this record's opening/disposition/charge/close + this table's own cell; NAMED-NOT-RUN — zero sites claim it ran |
| `ITERLAW` | 0 | 8 | +8 | NEW at r35 — the wave-16 proof note's file name (no-hyphen spelling, exact-case disjoint from the law's name), at the note-citation displays (§0, the §3.1 (H1) bracket, §40.(i), the disposition table) + this table's own cell |
| `H7FORGE` | 0 | 7 | +7 | NEW at r35 — the forge note's file name (case-disjoint from the module name two rows below), at the note-citation displays + this table's own cell |
| `DITERSUP` | 0 | 21 | +21 | NEW at r35 — the supplier-statement program name + its note-name compounds (containment by design), at §0 fold (4), the §3.1 brackets, §40.(iv), the disposition table, the bookkeeping, and the close + this table's own cell; every site displays BLOCKED-ON-APPLICATION |
| `BANKED_SORRY` | 0 | 6 | +6 | NEW at r35 — the retirement note's file name, at the note-citation displays (§0, §40.(ii), the disposition table, the bookkeeping) + this table's own cell; the prose spellings of the phrase do not count at this unit |
| `HullStability` | 0 | 8 | +8 | NEW at r35 — the new Lean module carrying the banked-sorry proof, named at §0 fold (2), the §3.2 sync tail, §40.(ii), the disposition row, and the close + this table's own cell |
| `H7Forge` | 0 | 7 | +7 | NEW at r35 — the new Lean module (case-disjoint from the note name three rows above), named at the forge displays + this table's own cell |
| `ITER-LAW-LIFT` | 0 | 10 | +10 | NEW at r35 — the wave's named open residual (general lawful lifts at g₀ = 1 ∧ δ₁ = 1), displayed at §0, the §3.1 (H1) bracket, the §3.2 sync tail, the §6 r35 bracket, §40.(i), the charge, and the close + this table's own cell; every hit also counts one at the law's row (containment) |

Fixed point: the counts above were re-grepped AFTER the last edit of this
revision. The count-sensitive subtleties of §§31–39 carry over UNCHANGED
(containment among the suffixed cadence companions, hyphen-disjointness,
case-sensitivity, decl-name-visibility — each re-checked, none changed
sign); the r35-new ones: every `VC35b` hit ALSO counts at `VC35`
(containment, the VC30/VC30b genre) and `VC36` is pairwise disjoint from
every other cadence name; every `ITER-LAW-LIFT` hit ALSO counts at
`ITER-LAW` (containment — the residual's name embeds the law's); `ITERLAW`
(no hyphen, the note-file spelling) is exact-case disjoint from `ITER-LAW`
and matches only the note-name displays; `H7FORGE` (the note name) and
`H7Forge` (the module name) are case-sensitively DISJOINT and neither
matches the older `H7PROBE`/`H7Probe` pair; `DITERSUP` counts both the
bare program name and the `DITERSUP_STMT` note-name compounds
(containment by design); `BANKED_SORRY` (the note name) is disjoint from
the prose "banked sorry"/"banked-sorry" spellings, which do NOT count at
this unit; `HullStability` appears only at this revision's displays.

**The re-derived row count (the r13 audit rule — counted, not copied).**
The §3.1 table's data rows enumerated directly after the edits: (H1) ·
(FRESH) = (H2) · (ADM) [(H3) member] · (UB-X) = (H4b) · (PACK) = (H5) ·
the (H6) terminal-seam group · (I-τ)/K-LOC = (H7) · (H8′) · (E-N) [(H3)
member]. THE COUNT IS **NINE** — unchanged; this revision touches row
CONTENT only (the (H1) and (I-τ)/K-LOC rows' r35 brackets) and adds and
retires no row; the wave-16 folds discharge no row and no member (the
ITER-LAW result is attempt-grade with counter 0; the banked-sorry
retirement is off-roster Lean hygiene; the forge decides instance faces
only; DITERSUP asserts nothing).

**VC36 charge.** All §39 charge items as inherited ((xxxiv)–(lxxix) now
checked against the r35 wording — note (xxxiv) becomes: every operative
site cites REVISION 33/VC35+VC35b, with the arc record displayed (VC35
0C/2G → r34-ERRATUM d79f295 → VC35b CONFIRMED 0C/0G, ledger 345b432)
and NO site citing REVISION 32/VC34 as the current anchor), PLUS:
(lxxx) the ITER-LAW fold claims EXACTLY attempt grade — every mention
carries the class qualifier (the harness tower class, standard-lift
towers, the class DITER_RESTATE §S2 pins), the acceptance counter is
displayed 0 with the hostile-pass arc in flight, the residuals
((ITER-LAW-LIFT) at g₀ = 1 ∧ δ₁ = 1; the gr(w₂) wrapper riding the GRB
retarget consumer-side; orders ≥ 3) are displayed wherever the result
is used, and NO site claims acceptance, verification, or a general-lift
scope; the note-side brackets (DITER_RESTATE §S2, GRB S5.3 + S8 item 2,
PROJECT_STATE) are ADDITIVE with records byte-unchanged and the sealed
falsifier untouched; (lxxxi) the banked-sorry fold claims the statement
BYTE-UNCHANGED and the AxChk record exactly (8698 jobs; exit 0; zero
sorryAx in the 1099-line census; capstone footprints unchanged; the
NAMED CITE axiom the sole survivor on `certLevel_stabilizes`), and the
repo-side "one banked sorry" lines carry dated retirement brackets with
NO line still asserting a live banked sorry; (lxxxii) the H7-forge fold
claims a DECISION POINT only — depth-1 honesty, synthetic-but-lawful
nodes, and "NOTHING discharges (H7)" displayed at every site, the
(I-τ)/K-LOC row still [M]; (lxxxiii) the DITERSUP fold displays EVERY
gate BLOCKED-ON-APPLICATION, NO site claims the three-token repair
applied or any old-clause record weakened, and the J3b cross-reference
carries the attempt-grade + standard-lift qualifiers; (lxxxiv) the
census above holds at the declared occurrence unit with the stated
delta-reasons. VC36 should receive the in-repo file listing + commit
resolutions + the COMPLETE provenance appendix with VERBATIM slices per
the saved checklist (memory: vc-audit-appendix-checklist — validated at
VC28, VC29, and VC32; mind rule 5: grep the ACTUAL bracket wording, the
VC35 gap-2 lesson), AND either an execution-capable environment or
pinned raw grep transcripts as audit inputs (the VC33-lineage lesson).

**Bookkeeping.** Historical §§7–39 untouched except the dated additions
named in the disposition table (the §0 REVISION-33 paragraph header
superseded; the (H1) and (I-τ)/K-LOC rows' r35 brackets appended; the
§3.2 sync tail; the §6 supersession bracket + the r35 bracket). This
revision touches NO Lean file and NO statement. Note-side edits riding
this unit (ALL additive brackets): `DITER_RESTATE_2026-08-08.md` §S2
(the ITER-LAW status bracket), `GRB_ORDER2_2026-08-05.md` (the second
S5.3 bracket + the S8 item-2 pointer bracket),
`DITERSUP_STMT_2026-08-08.md` §S4 (the J3b cross-reference bracket) —
`ITERLAW_2026-08-08.md` is NOT touched (the hostile passes in flight
read it as committed; its acceptance counter updates only on accepted
passes); no sealed falsifier file or sealed prediction block touched
anywhere. This unit touches ROOT + `docs/PROJECT_STATE.md` (new r35
checkpoint + dated tags at the r34 checkpoint's stale displays + the
banked-sorry retirement bracket + the wave-16 standing-obligations tag
+ the ITER-LAW display bracket) + `CLAUDE.md` (the zero-banked-sorries
update, dated) + the three notes above + the ledger
(`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`, r35-executed entry)
ONLY (`docs/AXIOM_FAITHFULNESS.md` swept: no banked-sorry mention, no
edit). The commit closing this revision is its unit of record (unit
ROOT r35; scoped add).

— REVISION 34 complete. VC35 + VC35b CONFIRMED and the acceptance
RE-ANCHORED on the (errata-corrected) REVISION-33 text (the anchor
moves REVISION 32/VC34 → REVISION 33/VC35+VC35b — the SIXTEENTH
audited re-anchoring; now inside the anchor: the strata family's
THREE-component union {all order-2} ∪ {order-3 all-e_j = 1,
PRINT-UNCONDITIONAL} ∪ {order-3 e₀ = 1, e₁ ≥ 2, e₂ = 1, conditional on
(H-ℓ) + the (KP_i)+[T] consumptions at S6.2 + the PIN-GEN-i
attribution display, with Lemma HV3-E2 derived from the pinned print},
the F2c in-pipeline discharge, the TWIST3 hygiene); THE WAVE-16 BATCH
folded: **ITER-LAW PROVED AT ATTEMPT GRADE on the harness tower class**
(standard-lift towers — the class DITER_RESTATE §S2 pins; seal ab7c31c,
run 242ba1e, ALL GREEN FIRST PASS 35,232/0; acceptance counter 0,
hostile passes in flight — both landed at the fold's close, one CLEAN
and one NOT-CLEAN 1C/1G with the errata round owed, per the §40.(i)
concurrency disclosure; (ITER-LAW-LIFT) open exactly at
g₀ = 1 ∧ δ₁ = 1; orders ≥ 3 untouched) **[r36 ERRATUM + STATUS
UPGRADE (VC36 finding 3; REVISION 35, §41): this closing restatement
omitted the THIRD residual — the FULL residual list, all three, is:
(ITER-LAW-LIFT) open exactly at g₀ = 1 ∧ δ₁ = 1; the gr(w₂) semantics
wrapper rides the accepted GRB retarget consumer-side; orders ≥ 3
untouched. And the result has since been ACCEPTED (2-clean
model-diverse: PC1 NOT-CLEAN 1C/1G acc955a → PE1 CLEAN 64ac340 →
errata round 1 7566ccd → PC2 CLEAN 804b88a + PE2 CLEAN c40a1da;
adjudicated 33373cb; counter 2/2), with the class scope CORRECTED —
SCOPE AS ACCEPTED: proved and ACCEPTED on the harness tower class
(standard-lift; a STRICT SUPERSET of DITER_RESTATE §S2's pinned
E₂ = e₀e₁ > 1 setting; coincides with the §S2 display on the nose
restricted to E₂ > 1; the E₂ = 1 leg a harmless COR-3-covered
extension outside the statement of record) — superseding this close's
"the class DITER_RESTATE §S2 pins" phrasing; full record at §40.(i)'s
acceptance-upgrade bracket + §41]**; **the banked sorry RETIRED**
(15372d1 — statement byte-unchanged, `OM/HullStability.lean`, AxChk
clean, the corpus's banked-sorry count 1 → 0); **the first compiled
non-vacuous (H7) decision point** (037ac6f — both OL-2 rfl-degeneracies
broken, the N-H7-1 pins decided at the instance, NOTHING discharged)
**[r36 ERRATUM (VC36 finding 4; REVISION 35, §41): this closing
restatement dropped two of the forge's standing honesty qualifiers —
the FULL list, at every (H7)-forge site: DEPTH-1 (the sides-2 site's
OWN continuations are NOT realized; that needs coherent 2-node
histories — the HK23/HK25 transplant, the NAMED residual); node
literals SYNTHETIC-BUT-LAWFUL (the S2 center-distinction genre — the
read tables are the forge's own keying, NOT derived from an engine
run); and NOTHING here discharges (H7) — the (I-τ)/K-LOC row stays
[M]]**;
**DITERSUP priced** (2a25ed9 — every gate BLOCKED-ON-APPLICATION, the
old-clause records byte-frozen); SYNTHESIS PASS 8 OWED at the wave-16
close; NINE rows recounted; the census re-run at the declared unit
with every delta explained; **VC36 owed on this REVISION-34 text** —
until it lands, the acceptance citation points at the REVISION-33
text.


## 41. REVISION 35 (r36, the VC36 disposition + the ITER-LAW acceptance sweep + the census re-sweep) — NO anchor move; all four VC36 findings remedied; ITER-LAW ACCEPTED on the harness tower class; the sorry census of record 36 → 35; VC36b owed

**What this revision is.** The r36 fold: the disposition of VC36 (the
end-to-end audit owed on the REVISION-34 wording), the ITER-LAW
acceptance sweep (the display upgrades owed at r36 by the ledger's
acceptance adjudication), and the sorry-census re-sweep owed at r35-6.
THE ACCEPTANCE ANCHOR DOES NOT MOVE: it STAYS at REVISION
33/VC35+VC35b (the §40 arc record, unchanged by this revision), and
this unit ran no audit and NEVER self-confirms — **VC36b is OWED on
the r36 remedies** (charge below). NO roster change: **NINE rows**
recounted; no clause attribution moved; no Lean file touched.

### 41.(i) THE VC36 ARC AND DISPOSITION — NOT-CONFIRMED 1C/3G, all four findings remedied

VC36 ran on the pinned REVISION-34 text (23282f5); report
`lean/notes/openmath/VC36_report.md` (3cf25ca), which carries the
orchestrator adjudication at its head. LAUNCH RECORD (process fact,
disclosed): the v1 charge (1,210,905 bytes) exceeded the
1,048,576-char Codex input cap and DIED SILENTLY at launch; the v2
charge (839,009 bytes) is the FROZEN-HISTORY TRIM per checklist rule
7 — full §§0–6 + all 8 r35 diff hunks in context + full §§38–40 + the
diff-verified byte-identity disclosure for §§7–37 + the census
transcripts over the FULL pinned text (69/69 rows matching at the
declared occurrence unit). VERDICT: **NOT-CONFIRMED, 1 critical + 3
gaps.** The findings and their remedies, all applied at this fold:

* **F1 [CRITICAL] — the ITER-LAW proof scope vs the statement of
  record** (the E₂ > 1 scope mismatch): adjudicated **FOLD-LAG, cured
  post-pin** — the audited text honestly recorded the then-live PC1
  critical (the §40.(i) concurrency disclosure); the errata arc + the
  acceptance resolve it. REMEDY: the §40.(i) ACCEPTANCE-UPGRADE
  bracket (the arc, the counter 2/2, the supplement actuals, the
  scope-as-accepted wording superseding every pinned-class phrasing).
* **F2 [GAP] — the F8 mixed-block under-test**: FOLD-LAG, same cure —
  the COR-4 mixed-block supplement `iterlaw_cor4_mixed.py` (18,396
  samples / 0 violations, seed 20260808; all four mixed blocks + the
  fresh window W4 both ways + closed-form-at-mixed-args + fresh rows
  Y/Z with PARI T11 fired), folded at the same §40.(i) bracket.
* **F3 [GAP] — the gr(w₂) residual omitted from the REVISION-34
  closing ITER-LAW summary**: GENUINE qualifier omission. REMEDY: the
  dated r36 ERRATUM bracket at the §40 close — ALL THREE residuals
  displayed + the ACCEPTED status + the corrected class scope, so the
  site is edited once with the full qualifier list.
* **F4 [GAP] — the depth-1 and synthetic-node qualifiers omitted from
  the same close's H7 summary**: GENUINE qualifier omission (defect
  class: qualifier-propagation at closing restatements, RECURRING —
  the VC35-gap-1 kin). REMEDY: the dated r36 ERRATUM bracket at the
  §40 close — depth-1 honesty (the sides-2 site's own continuations
  unrealized; the HK23/HK25 transplant the named residual) +
  synthetic-but-lawful nodes (the S2 center-distinction genre) +
  "NOTHING discharges (H7)" all displayed.

CADENCE CONSEQUENCE: the acceptance citation STAYS at the REVISION-33
text; the confirming obligation narrows to **VC36b** on the r36
remedies (VC36's own confirming scope was the r35 wording, whose
F1/F2 are cured by facts of record, not by re-auditable text).

### 41.(ii) THE ITER-LAW ACCEPTANCE FOLD — ACCEPTED on the harness tower class (scope-corrected); counter 2/2

The r36-owed display upgrades, per the ledger acceptance adjudication
(33373cb). THE ARC OF RECORD on note `ITERLAW_2026-08-08.md`: PC1
(Codex content) NOT-CLEAN 1C/1G (acc955a) → PE1 (Fable
execution/F2c) CLEAN 0C/0G (64ac340 — F2c verifier-owned from-note
reimplementation ALL GREEN, 89,056 samples / 0 violations, 4/4
mutation controls detected) → errata round 1 (7566ccd: the S0 scope
ERRATUM + the COR-4 mixed-block supplement, 18,396/0; the sealed
falsifier BYTE-UNTOUCHED, md5 == seal ab7c31c) → PC2 CLEAN 0C/0G
(804b88a, `ITERLAW_passPC2_report.md`) + PE2 CLEAN 0C/0G (c40a1da,
`ITERLAW_passPE2_report.md` — sealed re-run byte-exact 35,232/0).
**ACCEPTANCE COUNTER 2/2 — the 2-clean model-diverse bar is MET**
(errata rounds do not count; the two CLEAN legs are the fresh
post-errata pair). **SCOPE AS ACCEPTED** (the wording of record,
replacing every "the class DITER_RESTATE §S2 pins" formula): proved
and ACCEPTED on the harness tower class (standard-lift; a STRICT
SUPERSET of DITER_RESTATE §S2's pinned E₂ = e₀e₁ > 1 setting;
coincides with the §S2 display on the nose restricted to E₂ > 1; the
E₂ = 1 leg a harmless COR-3-covered extension outside the statement
of record). **RESIDUALS, ALL THREE, wherever the result is stated:**
(ITER-LAW-LIFT) open exactly at g₀ = 1 ∧ δ₁ = 1; the gr(w₂) semantics
wrapper rides the accepted GRB retarget consumer-side; orders ≥ 3
untouched. SITES SWEPT (all additive brackets): ROOT §40.(i) + the §0
standing paragraph + the §3.1 (H1) row + the §40 close (via the F3
erratum) + the §6 r36 bracket; note-side: DITER_RESTATE §S2 (the
SECOND status bracket — ACCEPTED + the ledger-owed one-line
class-phrasing correction, striking the r35 bracket's "exactly the
class this section's setting pins" repeat of the same defect), GRB
S5.3 (THIRD bracket) + S8 item 2 (r36 pointer bracket), DITERSUP
§S4/J3b (SECOND cross-reference bracket: J3b MAY now cite ITER-LAW
ACCEPTED-on-class — standard-lift instances only, the gr(w₂) tie
layer still a separate prerequisite, the Asvin application gate
stands), PROJECT_STATE (display brackets + the r36 checkpoint). The
proof note itself NOT touched by this unit (its acceptance brackets
landed at 33373cb).

### 41.(iii) THE SORRY-CENSUS RE-SWEEP — the census of record: 36 → 35 repo-wide

The r35-6 owed re-sweep, EXECUTED this unit (`grep -rn "sorry"
lean/LeanUrat --include=*.lean`, every hit classified; strict
non-comment unit — code-level `sorry` tokens only, comment/docstring
hits excluded by a block-comment-aware scan). **THE CENSUS OF RECORD:
35 repo-wide — 34 code-level tokens in `LeanUrat/` + 1 in the tracked
scratch probe `lean/scratch_S7_escalation.lean`; B2D 0; OM tree 0.**
DELTA vs the r27 display (36 repo-wide): exactly **−1 = the
r35-retired banked classifier sorry**
(`npVertices_stable_of_hull_preserved`, proved at 15372d1) — every other r27 leader reproduces
exactly: V9_K1nonrec ×4, SlotsG2_relFamily ×4, TV_G3 ×2, G1_toyGate
×2. BREAKDOWN of the 34 (all are the Moves*/Scaffold/HC* E-phase
opens — other units' tracked work; none banked; none on the capstone
path): MovesV 10 (TV_G3 ×2, TV_F2–F6 ×5, V7_livC/V7_scsData/V7_rbC) ·
MovesU 7 (SlotsG2_relFamily ×4, SlotsG3_rel3, SlotsG11c_seamAudit,
BridgeD19_omAgree) · Scaffold 6 (DictIII CU1/Devid/Locality,
MeasureFloor/SumOne, ValueSide/Transfer, O12/Semantic) · HC1 5
(V9_K1nonrec ×4, CL04_addrDichotomy) · MovesT 4 (TV_B7, G1_toyGate
×2, E11_treeN) · HC2 2 (U17a_liftSwap, U26_fiberWelldef). Display
sync: the §6 r36 bracket carries the new census of record; dated
brackets at the §6 r27 display and at PROJECT_STATE's r27-checkpoint
census line; the r27-era "36 repo-wide" texts inside superseded
standing paragraphs and historical disposition rows stand as dated
history, superseded by THIS display.

**Disposition table.**

| item | ground | grade | disposition | edit sites |
|---|---|---|---|---|
| r36-1 | report `VC36_report.md` (3cf25ca) + the orchestrator adjudication at its head (2026-08-03) | AUDIT DISPOSITION — VC36 NOT-CONFIRMED 1C/3G; F1/F2 FOLD-LAG (cured post-pin by the accepted arc); F3/F4 GENUINE closing-site qualifier omissions | F1/F2: the §40.(i) acceptance-upgrade bracket folds the errata arc + acceptance + supplement actuals; F3: the §40-close ITER-LAW erratum (all three residuals + ACCEPTED + corrected class scope, edited once); F4: the §40-close H7 erratum (depth-1 + synthetic-but-lawful + NOTHING-discharges-(H7)); the launch record (v1 input-cap failure → frozen-history-trim v2, checklist rule 7) disclosed at 41.(i) | §40.(i), §40 close ×2, §0, §6, this record |
| r36-2 | ledger 33373cb "ITER-LAW ACCEPTED" (arc acc955a → 64ac340 → 7566ccd → 804b88a + c40a1da; 2-clean model-diverse) | ACCEPTANCE FOLD — every attempt-grade display upgrades to ACCEPTED-on-class with the corrected scope wording; counter 2/2; residuals ALL THREE at every site | sites: the §0 standing paragraph's r36 bracket; the §3.1 (H1) row's r36 bracket; §40.(i)'s acceptance-upgrade bracket; the §40 close (F3 erratum); the §6 r36 bracket; note-side: DITER_RESTATE §S2 second status bracket (incl. the ledger-owed class-phrasing correction), GRB S5.3 third bracket + S8 item-2 bracket, DITERSUP §S4/J3b second cross-reference bracket, PROJECT_STATE displays + r36 checkpoint | §0, §3.1 (H1) row, §40.(i), §40 close, §6, this record; note-side: DITER_RESTATE, GRB, DITERSUP, PROJECT_STATE |
| r36-3 | the r36 in-repo census re-run (this unit; block-comment-aware scan, strict non-comment unit) | CENSUS REFRESH — the census of record 36 → **35 repo-wide** (34 `LeanUrat/` + 1 tracked scratch probe; B2D 0; OM tree 0); the −1 = the r35-retired banked classifier sorry | the full breakdown at 41.(iii); the §6 r36 bracket carries the census of record; dated brackets at the §6 r27 display + PROJECT_STATE's r27-checkpoint line; historical "36 repo-wide" records stand as dated history | §6 ×2, this record; repo-side: PROJECT_STATE |
| r36-4 | housekeeping (the /goal cadence) + cadence | TRACKING DISPLAY + CADENCE — VC36b OWED; never self-confirm | **VC36b** = the NARROW confirming pass on THIS revision's remedies ONLY (charge below; full end-to-end NOT re-owed — VC36's confirming scope); **SYNTHESIS PASS 8 still OWED at the wave-16 close**; the Asvin batch UNCHANGED at FOUR items (restated in full at the §40 flag paragraph; nothing consumed here); the acceptance citation stays REVISION 33/VC35+VC35b | §0, §6, this record |

**THE ENUMERATION AUDIT (the r13 rule, run over the post-edit text).**
Unit: OCCURRENCE COUNTS (`grep -o | wc -l`, exact case-sensitive patterns,
the r23-declared unit), taken on the FINAL text of this revision (counts
include this record's own quotations — the r23–r35 precedent). The §40
sixty-nine rows (seventy patterns) plus ONE marker introduced at r36
(`VC36b` — the owed narrow pass, named nowhere before this fold). The
r35 column = the §40 recorded census, VERIFIED as the pre-edit baseline
this unit (69/69 matching — the same verification VC36's Appendix-D
transcripts report). Every delta named:

| grep (occurrences) | r35 | r36 | delta | why |
|---|---:|---:|---:|---|
| `(R1)–(R4)` | 124 | 125 | +1 | no operative change — this table's own pattern cell |
| `(H4a)` | 204 | 205 | +1 | no operative change — this table's own pattern cell |
| `K3-δ` | 141 | 142 | +1 | status unchanged (clause (R) keeps consuming it until the Asvin gate fires) — this table's own pattern cell |
| `SITE-EXH` | 38 | 39 | +1 | no arc status change — this table's own pattern cell |
| `GRADED-READ` | 32 | 33 | +1 | no operative change — this table's own pattern cell |
| `TransDeep` + `TRANS-DEEP` | 55 | 57 | +2 | no operative change — both increments are this row's own joint pattern cell (one hit each) |
| `TEN rows` | 40 | 41 | +1 | historical-only — this table's own pattern cell |
| `NINE rows` | 78 | 81 | +3 | roster restated NINE, no change — this record's opening and close + this table's own cell |
| `VC25` | 62 | 63 | +1 | closed history — this table's own pattern cell |
| `VC23` | 31 | 32 | +1 | named-but-unrun history stands — this table's own pattern cell |
| `VC26` | 109 | 110 | +1 | closed history — this table's own pattern cell |
| `ROUTE (a)` | 60 | 61 | +1 | the Asvin-batch status is UNCHANGED and cited by pointer only this revision — this table's own pattern cell |
| `TR3-S2` | 46 | 47 | +1 | accepted status unchanged — this table's own pattern cell |
| `REC-SL` | 46 | 47 | +1 | status unchanged — this table's own pattern cell |
| `RECSL` | 63 | 64 | +1 | status unchanged — this table's own pattern cell |
| `HUNI-V2` | 18 | 19 | +1 | closed history — this table's own pattern cell |
| `VC27` | 137 | 139 | +2 | closed history — this table's own cells (this row and the containment hit at the row below) |
| `VC27b` | 60 | 61 | +1 | closed history — this table's own pattern cell (each hit also counts one at the row above) |
| `VC28` | 84 | 86 | +2 | closed history — the charge's checklist-validation naming + this table's own pattern cell |
| `VC29` | 91 | 93 | +2 | closed history — the charge's checklist-validation naming + this table's own pattern cell |
| `POOL-DICT` | 53 | 54 | +1 | closed history — this table's own pattern cell |
| `TR-4` | 40 | 41 | +1 | residue-taxon status unchanged — this table's own pattern cell |
| `BasePolygonLengthLaw` | 46 | 47 | +1 | proved status stands — this table's own pattern cell |
| `ITER-LAW` | 98 | 129 | +31 | THE ACCEPTANCE FOLD — the ACCEPTED-on-class displays with the corrected scope at the §0 r36 brackets, the §3.1 (H1) r36 bracket, §40.(i)'s acceptance-upgrade bracket, the two §40-close errata, the §6 r36 bracket, and this record, PLUS containment (every hit of the residual's name three rows from the bottom counts here too) + this table's own cells (this row and that residual row); status = ACCEPTED on the harness tower class (scope-corrected), counter 2/2 — zero sites claim a general-lift scope, and no unqualified verbatim-pinned-class claim survives |
| `leakFree` | 69 | 70 | +1 | typed-face status unchanged — this table's own pattern cell |
| `LeakFreeCarrierV4` | 15 | 16 | +1 | no new consumption — this table's own pattern cell |
| `junc_bundle` | 85 | 86 | +1 | the accepted fixed point retains the old-clause meaning — no new naming; this table's own pattern cell |
| `G-ADM-2` | 30 | 31 | +1 | closed routing history — this table's own pattern cell |
| `BPLL` | 29 | 30 | +1 | proved status stands, module untouched — this table's own pattern cell |
| `K1ConsumerFace` | 20 | 21 | +1 | discharged-face status stands — this table's own pattern cell |
| `N-H7-1` | 44 | 45 | +1 | the pins' status unchanged (the F4 erratum spells out the forge qualifiers without renaming the pins) — this table's own pattern cell |
| `VACUOUS-BY-TYPE` | 15 | 16 | +1 | the r28 verdict stands — this table's own pattern cell |
| `ADM-REAL` | 44 | 45 | +1 | acceptance history stands — this table's own pattern cell |
| `CLASS-LAT` | 28 | 29 | +1 | machine-checked status stands — this table's own pattern cell |
| `JUNCFORGE` | 23 | 24 | +1 | status unchanged — this table's own pattern cell |
| `TR3-S3` | 41 | 42 | +1 | accepted status unchanged — this table's own pattern cell |
| `VC30` | 126 | 131 | +5 | closed history — the narrow-pass genre namings (the charge's suffixed hit at the row below + the fixed-point paragraph's genre naming, which contains this exact pattern twice) + this table's own cells (this row and the row below) |
| `VC30b` | 59 | 62 | +3 | the genre namings (the charge + the fixed-point paragraph) + this table's own pattern cell; every hit also counts one at the row above |
| `VC31` | 170 | 174 | +4 | closed history — the genre naming's suffixed hit (containment) + this table's own cells (this row + the two rows below) |
| `VC31b` | 52 | 54 | +2 | the narrow-pass genre naming in the charge + this table's own pattern cell; every hit also counts one at the row above |
| `VC31c` | 47 | 48 | +1 | closed history — this table's own pattern cell; every hit also counts one at the plain-name row above |
| `VC32` | 114 | 117 | +3 | closed history — the charge's checklist-validation naming + this table's own cells (this row and the containment hit at the row below) |
| `VC32b` | 44 | 45 | +1 | closed history — this table's own pattern cell; every hit also counts one at the row above |
| `VC33` | 153 | 157 | +4 | closed history — the charge's lineage-lesson naming + this table's own cells (this row + the containment hits at the two rows below) |
| `VC33b` | 38 | 39 | +1 | closed history — this table's own pattern cell; every hit also counts one at the row above |
| `VC33c` | 42 | 43 | +1 | closed history — this table's own pattern cell; every hit also counts one at the plain-name row above |
| `VC34` | 57 | 58 | +1 | the superseded anchor — every site cites it as SUPERSEDED, none as current; this table's own pattern cell |
| `H-v3` | 63 | 64 | +1 | discharged status stands — this table's own pattern cell |
| `HV3-THM` | 40 | 41 | +1 | accepted status stands — this table's own pattern cell |
| `PIN-GEN-i` | 54 | 55 | +1 | the pin's discharge history unchanged; no union restatement this revision (the anchor does not move) — this table's own pattern cell |
| `TWIST-3` | 43 | 44 | +1 | status unchanged — this table's own pattern cell |
| `TR3-S4` | 73 | 74 | +1 | certified-inside-the-anchor status unchanged — this table's own pattern cell |
| `RB-DELTA` | 30 | 31 | +1 | decision-ready + Asvin-gated, unchanged; cited by pointer only — this table's own pattern cell |
| `wPrev` | 25 | 26 | +1 | zero sites claim the tokens applied — this table's own pattern cell |
| `classCount_eq_d` | 10 | 11 | +1 | proved status stands — this table's own pattern cell |
| `SideClauseR2` | 11 | 12 | +1 | no new naming — this table's own pattern cell |
| `readCeil` | 13 | 14 | +1 | no new naming — this table's own pattern cell |
| `HV3-E2` | 22 | 23 | +1 | derived-lemma status unchanged; no union restatement this revision — this table's own pattern cell |
| `VC35` | 90 | 103 | +13 | the standing-anchor citations of this revision (the acceptance citation STAYS at REVISION 33 — §0's r36 bracket, the §6 r36 bracket, this record's opening, disposition, and charge) + the two lesson namings (the gap-1 kin note at 41.(i); the gap-2 slice rule in the charge), PLUS containment (every hit of the suffixed companion at the row below counts here too) + this table's own cells; every site cites the arc as the STANDING ANCHOR, none as run on this text |
| `KEY0-LIFT` | 10 | 11 | +1 | hygiene history stands — this table's own pattern cell |
| `VC35b` | 34 | 40 | +6 | the standing-anchor citations + the narrow-pass genre naming in the charge + this table's own pattern cell; every hit also counts one at the row above |
| `VC36` | 15 | 56 | +41 | THE DISPOSED AUDIT OF THIS REVISION — the NOT-CONFIRMED 1C/3G arc displayed at §0's r36 bracket, §40.(i)'s acceptance-upgrade bracket (the F1/F2 cure note), the two §40-close errata (the F3/F4 cures), the §6 r36 bracket, and throughout this record, PLUS containment (every hit of the narrow companion at the row below counts here too) + this table's own cells; every site cites it as RUN-AND-DISPOSED, none as confirming |
| `ITERLAW` | 8 | 13 | +5 | the proof-note and pass-report file namings at this record's acceptance fold + this table's own pattern cell (no-hyphen spelling, exact-case disjoint from the law's name) |
| `H7FORGE` | 7 | 8 | +1 | the forge note's status unchanged — this table's own pattern cell |
| `DITERSUP` | 21 | 29 | +8 | the J3b ACCEPTED-on-class citation upgrade displays (the §0 r36 bracket + this record's acceptance fold, disposition row, charge item, and bookkeeping incl. the note-name compound — containment by design) + this table's own cell; every site still displays BLOCKED-ON-APPLICATION and the standing Asvin application gate |
| `BANKED_SORRY` | 6 | 7 | +1 | retirement history stands — this table's own pattern cell |
| `HullStability` | 8 | 9 | +1 | proved-module status stands — this table's own pattern cell |
| `H7Forge` | 7 | 8 | +1 | module status unchanged — this table's own pattern cell |
| `ITER-LAW-LIFT` | 10 | 18 | +8 | the first residual displayed at every acceptance site of this revision (§0, §3.1, §40.(i), the close erratum, §6, this record) + this table's own cell; every hit also counts one at the law's row (containment) |
| `VC36b` | 0 | 15 | +15 | NEW at r36 — the owed NARROW confirming pass on this revision's remedies: §0's r36 bracket, the §6 r36 bracket, this record's disposition/charge/close + this table's own cell; every hit also counts one at the plain-name row above; NAMED-NOT-RUN — zero sites claim it ran |

Fixed point: the counts above were re-grepped AFTER the last edit of this
revision. The count-sensitive subtleties of §§31–40 carry over UNCHANGED
(containment among the suffixed cadence companions, hyphen-disjointness,
case-sensitivity, decl-name-visibility — each re-checked, none changed
sign); the one r36-new subtlety: every `VC36b` hit ALSO counts at `VC36`
(containment, the VC30/VC30b genre), and `VC36b` is pairwise disjoint
from every other cadence name.

**The re-derived row count (the r13 audit rule — counted, not copied).**
The §3.1 table's data rows enumerated directly after the edits: (H1) ·
(FRESH) = (H2) · (ADM) [(H3) member] · (UB-X) = (H4b) · (PACK) = (H5) ·
the (H6) terminal-seam group · (I-τ)/K-LOC = (H7) · (H8′) · (E-N) [(H3)
member]. THE COUNT IS **NINE** — unchanged; this revision touches row
CONTENT only (the (H1) row's r36 bracket) and adds and retires no row;
the ITER-LAW acceptance discharges NO row and NO member (member (a)
stands; the accepted result is the (iii)-member law on the harness
class).

**VC36b charge (the NARROW confirming pass — the VC30b/VC31b/VC35b
genre, on THIS revision's remedies ONLY; the full end-to-end audit is
NOT re-owed: VC36's confirming scope was the r35 wording, and its
F1/F2 findings are cured by facts of record, not by re-auditable
text).** Items: (i) the four VC36 findings' sites re-checked against
the r36 wording — F1/F2: the §40.(i) acceptance-upgrade bracket states
the arc (acc955a → 64ac340 → 7566ccd → 804b88a + c40a1da → adjudicated
33373cb), the counter 2/2, the supplement actuals (18,396/0: M1–M4
1,524/0 each · M5 3,048/0 · CF 9,144/0 · CERT 108/0, seed 20260808;
F2c 89,056/0 with 4/4 mutation controls), and the SCOPE-AS-ACCEPTED
wording, with NO surviving unqualified "the class DITER_RESTATE §S2
pins" verbatim-class claim anywhere in the operative displays; F3: the
§40-close ITER-LAW erratum displays ALL THREE residuals + the ACCEPTED
status + the corrected class scope; F4: the §40-close H7 erratum
displays depth-1 honesty + synthetic-but-lawful nodes + NOTHING
discharges (H7). (ii) The acceptance-sweep sites verbatim-checked
(ROOT §0 ×2 / §3.1 (H1) / §40.(i) / §40 close ×2 / §6 ×2;
DITER_RESTATE §S2 second bracket incl. the class-phrasing correction;
GRB S5.3 third bracket + S8 item-2 bracket; DITERSUP §S4/J3b second
bracket; PROJECT_STATE brackets + r36 checkpoint; the ledger r36
entry): every bracket ADDITIVE, record texts byte-unchanged, the
sealed falsifier + prereg untouched, and NO site cites the acceptance
without the class scope and all three residuals. (iii) Census spot
rows: ≥ 5 patterns of the audit table re-grepped at the declared
occurrence unit, plus the sorry-census actuals re-derived from the
41.(iii) display (the breakdown must reproduce). STANDING DISCLOSURES
to carry in the charge: (a) the execution-environment disclosure (if
sandboxed execution is unavailable, the pinned raw transcripts are the
audit input — not a finding; the VC33-lineage lesson); (b) the
division-of-labor disclosure (the orchestrator adjudicated F1/F2 as
fold-lag; VC36b confirms the REMEDIES and does not re-adjudicate); (c)
the DATING CONVENTION disclosure (campaign dates 2026-08-08 vs
wallclock 2026-08-03); (d) the FROZEN-HISTORY-TRIM note (checklist
rule 7: the charge may trim byte-frozen history to a diff-verified
byte-identity disclosure — the VC36-v2 precedent). The charge MUST
include the in-repo file listing + commit resolutions + the COMPLETE
provenance appendix with verified-complete VERBATIM slices of EVERY
r36-edited site (memory: vc-audit-appendix-checklist — validated at
VC28, VC29, and VC32; grep the ACTUAL bracket wording first, note
brackets — the VC35-gap-2 lesson).

**Bookkeeping.** Historical §§7–40 untouched except the dated
additions named in the disposition table (the §0 standing paragraph's
two r36 brackets; the §3.1 (H1) row's r36 bracket; the §40.(i)
acceptance-upgrade bracket; the two §40-close errata brackets; the §6
r36 bracket + the §6 r27-census inline bracket). This revision touches
NO Lean file and NO statement; no sealed falsifier, sealed prediction
block, or byte-frozen record touched anywhere. Note-side edits riding
this unit (ALL additive brackets): `DITER_RESTATE_2026-08-08.md` §S2
(the SECOND status bracket), `GRB_ORDER2_2026-08-05.md` (the THIRD
S5.3 bracket + the S8 item-2 r36 bracket),
`DITERSUP_STMT_2026-08-08.md` §S4 (the SECOND J3b cross-reference
bracket) — `ITERLAW_2026-08-08.md` is NOT touched (its acceptance
brackets landed at the adjudication commit, not this unit). This unit
touches ROOT + `docs/PROJECT_STATE.md` (r36 checkpoint + the ITER-LAW
display brackets + the r27-checkpoint census-line bracket) + the three
notes above + the ledger (`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`,
r36-executed entry) ONLY. The commit closing this revision is its unit
of record (unit ROOT r36; scoped add). PROCESS RECORD (the recovery,
wallclock 2026-08-03; disclosed per the honesty discipline): the unit
first executing this revision DIED mid-run on repeated API 529 errors
(workflow wf_afbc2195-403, 68 tool calls in) with its edits applied
but UNVERIFIED-complete; the mid-work was preserved AS-IS at WIP
checkpoint 7598491 (687 inserted lines across the six files of this
unit), and a fresh RECOVERY unit completed the revision: a full
idempotency audit of every edit site against the task list (every
owed bracket found applied exactly once, none half-written, nothing
double-applied, no repair needed), an independent re-run of the sorry
census with a block-comment-aware scan (the 34 + 1 display at
41.(iii) reproduced exactly, per-file breakdown identical), and the
enumeration-audit re-grep to fixed point on the final text (all
seventy table rows matching as displayed — the dead unit's recorded
counts verified, zero corrections needed). The recovery unit's only
textual additions are this process record, the matching checkpoint
sentence in `docs/PROJECT_STATE.md`, and the ledger's recovery note;
the commit closing this revision is the recovery unit's.

— REVISION 35 complete. VC36 NOT-CONFIRMED (1C/3G) DISPOSED: F1/F2
fold-lag, cured by the ITER-LAW acceptance — **ITER-LAW ACCEPTED on
the harness tower class** (standard-lift; a STRICT SUPERSET of
DITER_RESTATE §S2's pinned E₂ = e₀e₁ > 1 setting; coincides with the
§S2 display on the nose restricted to E₂ > 1; the E₂ = 1 leg a
harmless COR-3-covered extension outside the statement of record;
counter 2/2, adjudicated 33373cb; RESIDUALS ALL THREE: (ITER-LAW-LIFT)
open exactly at g₀ = 1 ∧ δ₁ = 1, the gr(w₂) semantics wrapper rides
the accepted GRB retarget consumer-side, orders ≥ 3 untouched); F3/F4
genuine, cured by the dated errata at the §40 close (the full residual
list; the full H7 honesty list — depth-1 with the HK23/HK25 transplant
the named residual, synthetic-but-lawful nodes, NOTHING discharges
(H7)); the sorry census of record re-swept **36 → 35 repo-wide** (34
in `LeanUrat/` + 1 tracked scratch probe; B2D 0; OM tree 0; the −1 =
the retired banked classifier sorry); NINE rows recounted; the census
re-run at the declared unit with every delta explained; **VC36b owed
on the r36 remedies** — until it lands, the acceptance citation points
at the REVISION-33 text.
