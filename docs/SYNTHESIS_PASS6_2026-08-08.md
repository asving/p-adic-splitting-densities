# SYNTHESIS PASS 6 (2026-08-08) — post-wave-9 synthesis + the forge chartering

Charge: the /goal's standing synthesis clause; successor to `SYNTHESIS_PASS5_2026-08-08.md`.
Sources: PROJECT_STATE r26 checkpoint; ROOT §§0/3.1/3.2/6 + records 28–31 (the r23–r26
arc); the full 2026-08-08 ledger tail (SYNTHESIS PASS 5 → VC28 CONFIRMED); unit notes
TR3S2 / RECSL_F2 / RECSL_WIRE / READSOF_F3_WALK / HUNI_PROBE / POOLDICT / TR4_HARNESS /
CU2T_KP0 / LEAKFREE / DITER_RESTATE / GR34_DISPOSITION / ADMU0_REPAIR; GlueRun.lean +
CU2tKp0.lean docstrings; blueprints HDISCHARGE_H3 §1.4 / HDISCHARGE_H6; live sorry census
re-measured at HEAD.

HEADLINE. The pass-5 queue is the first with a CLEAN execution audit: every
agent-dispatchable item (1, 3–9) EXECUTED; item 2 blocked-on-Asvin (not dropped); items
10–11 deliberately deferred to THIS pass — zero dropped-again items. The anchor advanced
r20/VC22 → **REVISION 25/VC28** through five audited revisions, VC28 landing **CONFIRMED
FIRST-PASS 0C/0G** (d66bef2, after the r26 fold): the entire wave-6-9 harvest is inside
the certified anchor; the r27 citation fold is now due. Countermodel-first is no longer a
protocol but the operating engine (all five wave-9 decisions were compiled-instance-first).
This pass DECIDES the forge question: **charter `junc_bundle` alone; decline the other two
targets** with displayed dispositions.

## F1 — THE FORGE DECISION: three species, one charter (junc_bundle), two declines

The three named targets share NO infrastructure — three carrier landscapes: `junc_bundle`
lives on OM tower runs (`H1/TruncatedRun`+`GlueRun`, `Polynomial ℤ_[p]` + `SideReads`
bundles); the nonempty-parent instance on B2D ledger data (`LedgerLawfulV4`, 15 counting
clauses); the two-equal-state table on H8 K-CUT carriers (`TableShape` + chain-Sigmas).
Only the DISCIPLINE (hand-build a deep instance, fire the laws at it) is shared — one
"campaign" would be a name, not a saving. Priced separately:

- **`junc_bundle` — CHARTERED (price L).** Hardest obstacle (GlueRun gate record): at the
  choice-built σ₁rec the bundle's clause (iii) reads the residual of an OUT-OF-CARRIER
  element (σ.R f at deg f = deg Φ) — unprovable and irrefutable from the stage interface
  (deviation D-1's wall); a discharged merged gate needs a genuinely deep compiled run
  (deg f > deg Φ₁ with REAL digit data) — a U31-scale unit, the corpus's first compiled
  order-2 digit computation. It is the ONLY target with a live consumer chain: (i) the
  last SITE-EXH displayed residual; (ii) the instantiation substrate for
  `DIterJunctionSupplier` (queue #11's three sanity gates need exactly this junction);
  (iii) B-M2's last non-hypothesis-shaped wall (see F3d). Staged charter: **stage 1** the
  deep compiled run, target tower preregistered from strata-probe/BM2_TRACE numerics with
  the clause-(iii) residual traced numerically BEFORE the Lean build (concrete-trace
  rule); **stage 2** fire `gate_glue_junction` with the bundle discharged, un-hypothesized;
  **stage 3** instantiate the `DIterJunctionSupplier` gates (ITER-LAW consumed only as the
  displayed hypothesis; cyclic kernels fenced per the §S3 spec).
- **Nonempty-parent lawful leakFree instance — DECLINED (price M).** Obstacle: a two-level
  `LedgerLawfulV4` datum satisfying the TD-3 finite-box counting clauses at an occupied
  parent block. Value low standalone: the law is already falsifiable (`td2V4bad` fence)
  and generally supplied (`ledger_leakFreeV4`); the real consumer is the Step-10/K1
  classifier face, statable and fireable at the ∅-parent instances first (F3a).
  Disposition to display at the (FRESH) residual: *revisit ONLY if the classifier consumer
  face, once stated, demands a nonempty-parent witness for its own non-vacuity.*
- **Two-equal-state solve table — DECLINED (price S/M).** Obstacle: a fresh
  TableShape+chain with two equal-e states + its hdet/solve laws. Value ~zero for status:
  the δ-leg (`pooldict_delta_underdetermined`) already refutes the derivation route and
  (H8′)(iii) is already blocked-on-owners WITH THE BLOCKER COMPILED — a compiled τ-leg
  twin changes no row text. Disposition: the r26-2 display ("τ-leg same-genre, NOT
  compiled, forge-class") STANDS as the permanent record, annotated at the next ROOT touch
  with *declined at pass 6 — a second leg of a compiled blocker is not progress.*

Standing (unchanged): the GR stage-forge is DECLINED (route (a)); the readsOf run-forge is
DEAD (no-consumer). Neither is resurrected here.

## F2 — BasePolygonLengthLaw: a genuine self-contained unit; CHARTER HIGH (price M)

It does not HIDE the recorded convexity gap — it IS the gap, honestly priced: the corpus
has no negative-prefix hull geometry beyond `Order0Hull`'s flat case, and this law
(ℓ(N⁻(f)) = ord_X(f̄) + side nondegeneracy, at monic f with 0 ≠ f(0) ≡ 0 mod p) is
H-free, D-free, seam-free pure polygon geometry. The docstring route is concrete and
mostly quarried from PROVED kernels: per-side e ∣ length + 1 ≤ length (zip-consecutive +
`npVertices_sorted` + `Rat.den` divisibility), slope monotonicity from the PROVED
`npHeight_convexOn`, endpoint pins from `valSupport_facts`; the genuinely new machinery is
the negative-prefix telescoping to the first zero-height vertex. Both perimeter legs are
countermodel-forced (`Kp0GateA`/`Kp0GateB`), so no statement slack. Value: unlocks
`terminalPolySem_kp0_of_law` wholesale — stratum-wide `TerminalPolySem` at k′ = 0, the
(H6) (T-READ) k′=0 face's one numerical input — and builds exactly the hull machinery the
OL5-adjacent work (and the banked `npVertices_stable_of_hull_preserved` sorry) sits next
to. Fence: the −∞ corner stays separately priced (the f(0) ≠ 0 perimeter excludes it).

## F3 — NEW COLLAPSES (what the window's machinery newly reaches)

- **(a) v4 carrier → Step-10/K1 classifier consumer face: PARTIAL REACH — now
  attempt-grade.** `LeakFreeCarrierV4` + supplier + de-vacuity fence provide the exact
  typed object AND the method (consequence-of-clauses carrier, display face, compiled
  fence). Missing: only the Step-10-side statement (how the classifier census consumes
  parent-blindness). One statement-design unit, fired at `td2V4`, fence mandatory
  (price M; was unpriced-shape). Queued.
- **(b) RecSLWire pattern → the `hsc` row: NO — permanently hypothesis-shaped, and that
  is now proved-adjacent.** `HistoryCoherent`'s records bundle NO `StageCore` (RECSL_WIRE
  §1), and the round-2 fields determine only σ.R(−1)² = 1 (`ResVal.R_neg_one_sq` — both
  signs consistent), so no transport can manufacture the sign at an abstract carrier. The
  row's terminal honest state: hypothesis at abstract carriers, closed at every compiled
  site. Optional S hygiene (PoolDictProbe genre): a compiled two-instance witness
  differing only at σ.R(−1), moving the row to blocked-with-blocker-compiled; fold into
  any H1-touching unit, no standalone dispatch.
- **(c) ORD-INV + the order-2 theorem → deeper strata: NOT yet finite-algebra.** TR3S2
  §S7 is explicit: at order ≥ 3 the inner reads are marched operators with their own
  ǫ-systems, and the ǫ-recurrence can REVIVE even on e₀ = 1 strata — the recursion is
  unwritten. What transfers: ORD-INV fences the key-convention ambiguity in advance, and
  the strata-probe harness extends. **Next stratum candidate: ORDER 3, all-e_j = 1** (the
  revival question is sharpest there; D-REAL + the ITER-LAW e₀ = 1 leg are
  theorem-backed). Entry per the [M]-row protocol: a preregistered depth-3 probe
  MEASURING the composed ǫ-recurrence; a compose unit only if a law returns exact.
- **(d) KP-STEP [RC] collapse → B-M2 pricing: value side changed, cost side not.** With
  hlink compiled-fed at every leg and REC-SL two-row, B-M2's remaining opens are {hsc
  (hypothesis-shaped, (b)), hfence (GR0b-inherited fence), junc_bundle, the K1/K2 parity
  ties (untouched, as priced)} — `junc_bundle` is now B-M2's ONLY non-hypothesis-shaped
  wall, which concentrates (and partly justifies) the F1 charter.

## F4 — REMAINING-ROW ROUTE REVIEW (the NINE rows)

- **(H1)** LIVE (the window's main harvest). Remaining: (a)-residues = deeper orders ≥ 3
  (entry F3c), TR-3-ORD-X beyond order 2, ITER-LAW (open lemma, route displayed);
  (b)/(c)/(d) unchanged. Unblocking artifacts: the depth-3 probe log; an ITER-LAW proof
  attempt at e₀ ≥ 2 via the CARRY-1/W-MULT fibred route (attempt-grade now the statement
  is of record).
- **(FRESH)** LIVE. Residuals: the classifier consumer face (F3a, queued) + the full
  §B2-DEF non-ledger general supplier (theorem-strength, stands).
- **(ADM) A/B: NOT BLOCKED — the adjudicating gate was never run.** Track A = prove
  (ADM-REAL) over realized shape families; track B = the U6a/U6b CEN-W generalization
  (statement-change-gated, contingency). **G-ADM-2** (HDISCHARGE_H3 §1.4: instrument the
  order-2 census harness to log dim G_{β_k} vs d at n ≤ 5, p ∈ {2,3,5}) routes between
  them and has NEVER RUN — it is this row's exact countermodel-first entry. Single
  unblocking artifact: the G-ADM-2 committed log + one-page disposition. Queued. (ADM-U0
  repair confirmed executed pre-pass-5; U6a/U6b gated on G-ADM-2, not on U0.)
- **(H6)** BasePolygonLengthLaw chartered (F2). **C2 STAYS BLOCKED** — no new mechanism:
  the window's re-key mechanism (v4 twins; the CU2tKp0 𝐇°-keying) worked at k′ = 0
  empty-nodes keying, but C2's face is k′ ≥ 1 where the terminal-binding + OL2min routing
  hits the III-G3a/G4/G6 typeclass wall unchanged. Honest route: ONE dedicated GDOrder1
  typeclass-repair DESIGN NOTE (no agents). (T-DEC)/(T-VERD): construction routes on
  record, background.
- **(H7)** LIVE-BUT-NEVER-PROBED — the pair has no compiled or numeric decision point of
  its own (O-10's harness exercises the adapted-cell law, not the presentation/locality
  tie). Unblocking artifacts: an instrumented O-10 comparison (continuation reads vs OM
  reads at realized strata — numerics, S) + the (I-τ) presentation note skeleton. Queued
  (the F1-protocol extension to the last unprobed conformance row).
- **(H8′)** (i) GRAFT: BLOCKED (theorem-strength graft completeness on the
  D-14+CU-3+REL.2 arc; no new mechanism). Cheap entry available: a PoolDictProbe-genre
  bijectivity check of `graft`/`cut_graft` at the compiled n2 pack — decides whether the
  interface is even instance-true (S/M, optional batch). (ii) MASS-ID: BLOCKED on owners
  ((H7)/TREE-EXP + [2r]/CL-8 + the count-tie); artifact = the O-1/O-3 count-tie note.
  (iii) POOL-DICT: DECIDED this window; blocked-on-owners with the blocker compiled;
  nothing further.
- **(UB-X)/(K3-δ)** — waits on the Route-B adoption (five conditions incl. Asvin
  sign-off AND the ROOT owner's consumer-need walk). The walk is OUR half and is not on
  file: queue it (S, doc-only) so the Asvin decision is fully teed up.
- **(PACK)** unstaffed; displayed routes stand (route-a n=3 transcription+booking pass,
  route-b O-14d construction) — a large data-verification unit, background this window.
- **(E-N)** DORMANT by design (witness-backed display; prices nothing beyond the CU-4
  touch). Only mover: a future CU-4 full walk — not this row's own work.

## F5 — EXECUTION AUDIT of pass 5 (queue 1–12)

1 EXECUTED (VC25 disposed at r24; F6 hygiene 112b9ac). 2 BLOCKED-ON-ASVIN, not dropped
(re-displayed at r26-8; carried to this queue's Asvin batch). 3 EXECUTED (TR3-S2
ACCEPTED). 4 EXECUTED (GR ROUTE (a); B2D 0 sorries). 5 EXECUTED (RECSL_F2 + RecSLWire).
6 EXECUTED (F3 walk → B-M2 REV 16). 7 EXECUTED all parts (huni probe → v4 repair; ADM-U0
adjudicated stale-at-writing, executed pre-pass; POOL-DICT decided). 8 EXECUTED all parts
(TR-4 19,226/0; CU2t k′=0; leakFree typed). 9 EXECUTED (D-ITER restated). 10–11
DELIBERATELY DEFERRED to this pass — the chartering IS F1 (not a drop). 12 background
held correctly (C2 stayed blocked). **Dropped-again count: ZERO** — the named-dispatch
treatment (pass-5 F6) worked; no item needs it this round. Successor rule kept: audit
queue lines against the ledger at write time (pass 5's one defect was the stale ADM-U0
line, r25-7; this pass verified G-ADM-2 never-ran before queueing it).

## F6 — DEAD WEIGHT + assignments

- **StageCore docstring debt** (displayed r25-8/r26-8, deliberately un-edited): ASSIGNED
  to the first chartered Lean-touching unit to land — queue #2 stage 1 (same
  neighborhood) or, if the forge stalls, queue #5. One-line sync: cite HC1
  `T1_baseStage`/`S9_transStage`/`S10_recStage` for the live chain, not the retired
  MovesGr trio.
- **The r27 fold is due**: VC28 CONFIRMED FIRST-PASS (d66bef2) after the r26 fold — the
  anchor is REVISION 25/VC28 but ROOT's citation text still says REVISION 24; r27 also
  folds THIS pass's dispositions (charter + two declines + the BasePolygonLengthLaw and
  G-ADM-2 charters). VC29 owed on the r27 text.
- **Census drift**: `REMAINING_CENSUS_2026-08-01.md` predates the entire window.
  Re-measured at HEAD: 36 actual sorry sites repo-wide (B2D **0**, grep-verified; the
  parked V9_K1nonrec ×4, SlotsG2_relFamily ×4, TV_G3 ×2, G1_toyGate ×2 lead the list; the
  banked classifier sorry stands). Census refresh rides r27 (doc-only).
- Cleared previously, verified still clean: the r22 "wave 1 next" pointer (cured r24);
  B2D MANIFEST (F6-hygiene batch); TDDefs/GRDefs headers. SYNTHESIS_PASS3/4/5 +
  HARDEST_FIRST_ORDER are correctly historical. No other drift found.

## REVISED PRIORITY QUEUE (value/cost; hardest-first slotting; blockers named)

1. **ROOT r27 fold** (S): citation → REVISION 25/VC28; fold F1's charter+declines, F2/F4
   charters, F6 census refresh; VC29 owed on the r27 text. Blocker: none.
2. **FORGE: junc_bundle stage 1** (L — the chartered campaign, hardest-first): the
   deg f > deg Φ₁ compiled run with real digit data; target tower preregistered from
   numerics; clause-(iii) residual traced numerically before building; carries the
   StageCore docstring sync. Then stages 2–3 (gate un-hypothesized;
   DIterJunctionSupplier gates). Blocker: none.
3. **BasePolygonLengthLaw** (M, F2): the negative-prefix hull unit per the docstring
   route; acceptance = `terminalPolySem_kp0_of_law` firing stratum-wide, Kp0 gates green.
   Parallel-safe with 2 (disjoint files). Blocker: none.
4. **G-ADM-2 attainment scan** (S/M numerics, F4-(ADM)): committed log + one-page
   disposition routing track A vs track B. Blocker: none — never run.
5. **Step-10/K1 classifier consumer face** (M, F3a): statement-design unit consuming
   `LeakFreeCarrierV4`; fired at `td2V4`; de-vacuity fence mandatory. Blocker: none.
6. **Depth-3 strata probe** (M numerics, F3c): preregistered ǫ-recurrence measurement on
   order-3 all-e_j = 1 towers; compose unit ONLY on an exact law. Blocker: none.
7. **(H7) K-LOC/(I-τ) first probe** (S numerics + note skeleton, F4). Blocker: none.
8. **(UB-X) consumer-need walk note** (S doc, F4): tees up the Route-B adoption; the
   adoption itself Asvin-gated. Blocker: none for the walk.
9. **DIterJunctionSupplier formalization** (M/L): statement against the §S3 spec can land
   now; the three sanity gates BLOCKED ON #2 stage 1. ITER-LAW consumed as displayed
   hypothesis only.
10. **ITER-LAW proof attempt** (L math, e₀ ≥ 2 order 2, CARRY-1/W-MULT route;
    falsifiers on file 0/804): attempt-grade, hostile-pass bar; slot after #6 (the
    depth-3 probe may reshape the target). Blocker: none mathematically.
11. **Asvin batch** (zero-cost, standing): TowerMoveClosureL re-ratification; fence-(vii)
    ROUTE (a) ratify-or-reopen (now also gates the REC-SL suppliers); Route-B re-scope
    (after #8). Blocker: Asvin.
12. **Optional S batch (PoolDictProbe genre, fold into passing units)**: the hsc
    two-instance sign witness (F3b); the GRAFT n2 bijectivity check (F4-(H8′)(i)).
13. Background/BLOCKED (no agents without a new mechanism): (H6) C2 (design note only);
    (H8′)(i)/(ii) proper; (PACK) route-a; (E-N); the two declined forges (dispositions
    displayed per F1).

— Synthesis pass 6, 2026-08-08.
