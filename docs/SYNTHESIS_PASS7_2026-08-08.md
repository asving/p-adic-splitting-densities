# SYNTHESIS PASS 7 (2026-08-08) — post-wave-13 synthesis: the source-question window

Charge: the /goal's standing synthesis clause; successor to `SYNTHESIS_PASS6_2026-08-08.md`;
owed since ROOT r29 (§34 r29-8). Sources: PROJECT_STATE r29 checkpoint; ROOT §§33–34
(§35 NOT landed — the last record is REVISION 28); the full 2026-08-08 ledger arc from
"SYNTHESIS PASS 6" through "TR3-S3 ACCEPTED"; unit notes JUNCFORGE_S1/S2 (+§R3 charter),
ADMREAL (+§5/§8, pass reports), TR3S3, STRATA3, NH71, GADM2, BPLL, K1FACE, H7PROBE,
LEAKFREE, DITER_RESTATE; DITER §S3 consumable spec; sorry census of record (r27 unit).

HEADLINE. The window this pass maps (waves 10–13 + the cadence arc) is the corpus's
first SOURCE-QUESTION window: the forge's chartered target `junc_bundle` came back
INSTANCE-FALSE AS TRANSCRIBED (the compiled CHAR-2, Φ₂-LANDING, INTERIOR-Φ₂A-PINNED
bundle, kernel-proved at row A) — converting a discharge campaign into a transcription
question against the FGMN print, with the re-derivation design unit chartered
(proposal-only, Asvin-flag). Around it, four unqualified upgrades: BasePolygonLengthLaw
PROVED every prime (consumer fired), ADM-REAL′ ACCEPTED with **CLASS-LAT an
unconditional lattice theorem at every order**, TR3-S3 ACCEPTED (the strata trilogy
complete: TR-3′-GEN on {all order-2} ∪ {order-3 all-e_j = 1}, conditional (H-v3)), and
the (H7) pair surviving first contact with two restatement pins. Everything through the
junc_bundle verdict is inside the certified anchor (REVISION 28/VC30+VC30b); TR3-S3's
acceptance is ledger-recorded post-r29 and folds at r30. This pass finds the window's
cheapest big lever — **the (H-v3) discharge is an S/M source unit, not an open-math
problem** (F3) — and one template fix for the window's dominant defect class (F6).

## F1 — THE FGMN RE-DERIVATION: dispatch-ready WITH four riders; queue #2 (hardest-first)

Spec-check of the §R3 charter (JUNCFORGE_S2) against what stage 1 measured. The charter's
two fixed points are exactly the stage-1 artifacts: the TWO-SCALE LAW (read-pair scale
carries the pattern, coarser stage scales read the key-power monomial — 336/336 both
legs, wPrev(24) = 3 vs w(24) = 6 at the (2,3) pins) and the DERIVABLE clause (iii)
(`juncforge_clauseIII_derivable`, Lean-proved at the (2,3)-graded pins). The task —
determine from the source the correct scale for clause (ii)'s window functional at
junction frames (parent vs graded) — is exactly the incompatible-transcription-scales
reading the S2 disposition isolated. VERDICT: **DISPATCH-READY**, with four riders the
charter text does not pin:

1. **Row-A satisfiability leg.** The corrected transcription must be SATISFIABLE at the
   row-A read (harness-checkable: the S1 numerics carry real (2,3) junction data there);
   a proposal that still trips `juncforge_no_e2_junction`'s parity arithmetic is dead on
   arrival. Acceptance leg: the corrected clauses instantiated numerically at row A +
   the 336-site battery re-run under the corrected clause-(ii) scale.
2. **PIN-GEN-i rider.** TR3-S3's owed non-textual leg (fresh `pdftotext -layout` at FGMN
   Defs 3.15/3.16/3.18) is the SAME source, same discipline, same corrupted-extraction
   warning (fgmn.txt BAD at Def 3.15). One extraction, two consumers: fold the
   PIN-GEN-i discharge into this unit as a rider.
3. **Provenance-pin the PDFs.** `/tmp/litunit/fgmn.pdf` is volatile and the repo's
   reference set holds only `montes-blueprint.pdf`; copy fgmn.pdf into
   `docs/references/` with sha256 recorded (and do the same for the GMN TeX at F3).
4. **DITER §S3 obligations as fixed points.** The corrected `junc_bundle` must be
   statable in a form `DIterJunctionSupplier` can host: 𝒟₂ carrier on the canonical
   anchor indexing, (ITER-LAW) cocycle (both constants + fibration term), the three
   sanity gates (B0a associativity / D-REAL degeneration / NOT-CYCLIC on row N). The
   charter names the supplier's antecedent but not these; the dispatch must.

Priority: the chartered hardest-first item — the ONLY route to junc_bundle's
source-level question; gates DIterJunctionSupplier's junction legs (queue #9); resolves
B-M2's last non-hypothesis-shaped wall one way or the other; discharges PIN-GEN-i as a
rider. Design/proposal genre: the unit itself needs NO Asvin decision — only its
PRODUCT does (statement change; ROOT adjudication + the standing flag).

## F2 — NEW COLLAPSES

- **(a) CLASS-LAT's reach: (ADM)-internal only — but it names a concrete Lean target.**
  CLASS-LAT (#{𝐣 : wt(𝐣) ≡ c mod e} = d, every order) is wt-lattice equidistribution on
  a type's ledger; (PACK)'s obligations are data transcription/booking (route-a) and the
  O-14d construction — not lattice-shaped; (E-N) is a witness-backed display; the (UB-X)
  walk is consumer-need documentation. NO reach. What DOES reach: ADMREAL §8's named
  design — rescale `wphi` by E_i (F-ADM-3), re-run the F-ADM-2 scan (prediction: clean
  — that IS CLASS-LAT), and **replace ADM-U2's `hclass` hypothesis by a proved
  `classCount_eq_d`** ("Finset-arithmetic-grade Lean"). The re-key is the EDIT-GATED
  item; the standing definition-change authority (Codex confirms the applied diff) is
  the on-record sign-off mechanism — queue #4, displayed as executing the gated item
  under that authority. Corruption note: the call-path audit found 6 consumer files,
  zero rescale sites, all compiled instances r ≤ 1/degenerate — the re-key must land
  BEFORE anyone instantiates `ADMFull`/`maxClassWeight`/ADM-U1/U2 at r ≥ 2 with some
  E_i > 1.
- **(b) TWIST-3: probe-ready YES; the next leaf is the PROBE, not TR3-S4.** The sealed
  untested candidate (C^F3_j = z₂^{θ₂s_j}·c^G3, θ₂ = ℓ₁g₁γ₂) + the strata3 harness
  (extends directly to e₁ ≥ 2 rows) + the ORD-INV key-convention fence are all on file;
  the [M]-row protocol binds: preregistered probe MEASURING the composed ǫ-recurrence on
  order-3 e₁ ≥ 2 strata; TR3-S4 charters ONLY on an exact law. (The depth-3 result —
  no ǫ-revival on all-e_j = 1 — makes e₁ ≥ 2 exactly where the revival question lives.)
- **(c) Verifier-owned implementations: PROMOTE to a standing acceptance expectation.**
  Three accepted arcs in one window carried verifier-owned from-note implementations on
  off-roster instances (TR3-S3: 93 + 1,564 + 821 checks, 0 failures — "the strongest
  external-leg record of any accepted note"). This is the extraction-corruption
  decorrelation logic at the implementation level: the note's own harness shares the
  note's blind spots. STANDING RULE (this pass, ops-level — no statement content): for
  proof notes whose theorems consume a measured law or falsifier harness, at least one
  verification pass of the acceptance arc must include a verifier-owned from-note
  implementation on off-roster instances. Scope: numerics-backed proof notes ONLY (pure
  Lean units carry the kernel as their leg). Cost: near zero — verifiers already did it
  unprompted 3/3 times; the rule makes the floor official.
- **(d) BPLL's hull core reaches the repo's ONE banked sorry.** The abstract kernels
  (`npSides_neg_length_sum`, `dropCollinear_between` the straddle killer,
  `collinear₃_trans`, `telescope_sum`, the two valid-line constructions) are exactly
  "the geometry the banked `npVertices_stable_of_hull_preserved` sorry sits next to"
  (BPLL note §core). An attempt on the banked Classifier sorry is now ATTEMPT-GRADE
  (off-capstone hygiene, but retiring the corpus's single banked sorry is a standing
  honesty asset). The −∞ corner stays separately priced; no other open polygon
  obligation found (Kp0GateA/B/C are countermodel fences, not opens).

## F3 — THE (H-v3) QUESTION: an S/M source unit, already half-built by ADM-REAL′

(H-v3) — the GMN order-3 ordinate reads v₃ = w₂ on the stratum — is TR-3′-GEN's whole
order-3 conditionality (attribution only: a false (H-v3) would re-key R_λ₃ to the
project operator, not break the finite identity). The TR3S3 §S1 sketch already derives
it from GMN Def 2.5 (v_r(P) = e_{r−1}·H_{r−1}(S_{r−1}(P))) GIVEN the H_{r−1} generic
display in the pinned normalization — kept hypothesis-shaped ONLY because the r = 2
instance was consumed evaluated, never as a generic formula (extraction-corruption
discipline). THE COLLAPSE: ADM-REAL′ REVISION 1 already produced the decorrelated
source leg — `HigherNewton.tex`, sha256-pinned (8253d4d6…), environment-count map
reproducing all seven pinned labels INCLUDING Def 2.5. Cheapest discharge = ONE source
unit: re-fetch the arXiv:0807.2620v2 e-print by the recorded hash (pin the TeX into
`docs/references/` this time), extract the H_{r−1} display at generic r, check it
against the pinned evaluated r = 2 instance, and run the §S1 sketch to v₃ = w₂ at
e₁ = 1. Numeric cover ALREADY on file (STRATA3 P0/CERT3 ran under exactly this reading,
0 violations, PARI-certified towers) — a further numerics pin adds nothing; a full
GMN-print derivation beyond the display extraction is not needed (the sketch collapses
once the display is pinned). Price: **S/M**. Payoff: TR-3′-GEN's order-3 leg drops to
the standing print-attribution baseline — the trilogy's only non-print hypothesis
gone. Queue #3.

## F4 — ROW REVIEW (the NINE rows; single unblocking artifact each)

- **(H1)** LIVE, three currents: (i) junc_bundle → verdict of record + FGMN charter
  (artifact: the queue-#2 proposal); (ii) the trilogy at order 3 conditional (H-v3)
  (artifact: the queue-#3 discharge note) + TWIST-3 (artifact: the queue-#5 probe log);
  (iii) ITER-LAW the open lemma, falsifiers 0/804 + 0/840, route displayed, slot
  condition met (artifact: the queue-#6 attempt note). (a)-(GR-B)/(b)/(c)/(d) unchanged.
- **(FRESH)** LIVE. K1 consumer face discharged (v4 chain + fence); residual = the full
  §B2-DEF non-ledger general supplier (theorem-strength). No cheap artifact; background.
- **(ADM)** LIVE post-acceptance. Track-A DISCHARGED to (B-PIN) + (C-JCT) + F-ADM-3.
  NEXT UNIT = the F-ADM-3 carrier re-key + `classCount_eq_d` (queue #4, F2a — the
  edit-gated item executed under the standing definition-change authority, Codex diff
  confirmation, ROOT display duty). The (C-JCT) fan-out audit (which owns (B-PIN)'s
  discharge) follows as a design/audit unit on the honest carrier — artifact: the
  C-JCT audit note with the ε = 1-exclusion equivalence settled.
- **(UB-X)/(K3-δ)** BLOCKED-ON-ASVIN for adoption; OUR half (the consumer-need walk)
  still not on file after two queue appearances — NAMED DISPATCH, queue #7.
- **(PACK)** unstaffed background; routes stand (route-a n=3 transcription+booking,
  route-b O-14d); no CLASS-LAT shortcut (F2a). Artifact: the route-a booking pass
  (large; background this window).
- **(H6)** BPLL PROVED + `terminalPolySem_kp0_unconditional` fired; residuals: **C2
  STAYS BLOCKED — still no new mechanism** at k′ ≥ 1 (the III-G3a/G4/G6 typeclass wall);
  the sanctioned unit remains the GDOrder1 typeclass-repair DESIGN NOTE (no agents) —
  carried twice as background, now named in the optional batch (queue #12c). The −∞
  corner and (T-DEC)/(T-VERD) construction routes: background.
- **(I-τ)/K-LOC = (H7)** OPEN with the strongest evidence state of any [M] row:
  vacuous-by-type at every compiled instance + first-contact survival at six real
  m ≥ 2 sites + TWO measured restatement pins (slope-keyed ε — slope-pooled REFUTED;
  the D-8 guard = the extraction boundary). Artifact: the restatement forge (queue #8)
  — `KLocSemantic`/(I-τ) Lean statements honoring both pins + an OL-2 inhabitant per
  the sharpened spec (sides ≥ 2 at a branching site, non-diagonal branchCellOf,
  sorry-free TreeModel).
- **(H8′)** (i) GRAFT blocked (optional n2 bijectivity check STILL unexecuted — rides
  the optional batch); (ii) MASS-ID blocked on owners; (iii) POOL-DICT decided,
  blocker compiled. No change.
- **(E-N)** DORMANT by design; only mover = a future CU-4 full walk. No change.

## F5 — EXECUTION AUDIT of pass 6's queue (1–13)

1 EXECUTED (r27 fold, REVISION 26). 2 EXECUTED stages 1–2 (stage 1 ALL GREEN 023a9d7;
stage 2 → the INSTANCE-FALSE verdict + 5-round faithfulness arc + charter; StageCore
docstring debt cleared); stage 3 BLOCKER-SUPERSEDED, not dropped (supplier gates now
gate on the FGMN proposal — re-queued as #9's displayed blocker). 3 EXECUTED (BPLL
PROVED, consumer fired). 4 EXECUTED (G-ADM-2 → track A → ADM-REAL′ ACCEPTED). 5
EXECUTED (K1 face fired + fence). 6 EXECUTED ((IDENT-3) exact → TR3-S3 ACCEPTED). 7
EXECUTED twice over (H7Probe Lean + N-H7-1). **8 DROPPED-AGAIN — the (UB-X)
consumer-need walk: second consecutive queue appearance with zero artifact; named-
dispatch treatment this pass (queue #7).** 9 NOT EXECUTED, blocker changed (was "#2
stage 1", is now the FGMN proposal + ROOT adjudication — honest displayed state). 10
NOT EXECUTED; its slot condition ("after #6 — the probe may reshape the target") RESOLVED
at window end (no reshape: revival confined to e_j > 1) — re-queued top-tier (#6), no
longer conditional. 11 BLOCKED-ON-ASVIN, re-displayed at r29 (+1 item: the FGMN
charter). 12 NOT EXECUTED (optional, no passing unit adopted either check — carried).
13 held correctly (C2 no-mechanism honored; declined forges stayed declined).
Dropped-again count: **ONE** (item 8) — the pass-5/6 rule (audit queue lines against
the ledger at write time) applied; the walk gets a named dispatch line, not a re-queue.

## F6 — DEAD WEIGHT + the ops-discipline distillate

- **The r30 fold is DUE** (doc-only, queue #1): the ledger tail past r29 holds VC30
  (1 critical → r29-ERRATUM 573bd1b) + VC30b CONFIRMED (0C/0G) — **the acceptance
  anchor is REVISION 28/VC30+VC30b, but ROOT's citation text still says REVISION
  27/VC29** — plus TR3-S3 ACCEPTED (96b7f54; the GRB TR-3′-GEN scope-union update
  rides r30 per its ledger entry). PROJECT_STATE's r29 checkpoint predates all of
  this. VC31 owed on the r30 text.
- **Census**: the sorry census of record stays the r27 measurement (36 sites repo-wide,
  B2D 0; every wave-10–13 unit added zero per its ledger entry); re-measure under the
  declared strict unit rides r30.
- **Reference-set gap** (new): the corpus's two load-bearing external sources exist
  only as volatile /tmp artifacts (fgmn.pdf) or recorded-hash-without-file
  (HigherNewton.tex) — `docs/references/` holds only the Montes blueprint. Queue #2/#3
  carry the pinning rider.
- **THE QUALIFIER-PROPAGATION DEFECT CLASS — 7 appearances, one template fix.** The
  window's dominant verification finding: a correctly-scoped verdict restated WITHOUT
  its qualifiers (JFS2 faithfulness rounds 1/2/3/4 — global scope, per-theorem scopes,
  verdict-paragraph restrictions, headline-tier interior pin; VC30 finding 1 — the §34
  closing line; TR3-S3 pass-1 gap 1 — consumer displays missing "conditional on
  (H-v3)"; ADM-REAL′ pass-1 critical — the any-type/optimal-type scope overrun at
  source). Every instance is the same mechanism: prose restatement re-derives the
  verdict from memory and drops restrictions. TEMPLATE FIX — **DEFINED-TERM VERDICTS**:
  any scoped verdict/acceptance that will be restated ≥ 2 times gets a NAMED term at
  its first record with the qualifiers INSIDE the definition (e.g. "the ROW-A VERDICT
  := the compiled CHAR-2, Φ₂-LANDING, INTERIOR-Φ₂A-PINNED junc_bundle is INSTANCE-FALSE
  at row A"); every other site uses the term VERBATIM, unexpanded; the occurrence
  census tracks the term; verifiers check ONE definition site + verbatim usage
  (mechanical grep) instead of N prose restatements. Adopt as standing ops discipline
  at the next ROOT touch; candidate for a saved memory.
- Verified still clean: StageCore docstring debt (cleared at stage 1, comment-only
  diff); the two declined forges (dispositions displayed, neither resurrected);
  SYNTHESIS_PASS3–6 correctly historical; no other drift found.

## REVISED PRIORITY QUEUE (hardest-first slotting; blockers named; artifacts concrete)

1. **ROOT r30 fold** (S, doc): citation → REVISION 28/VC30+VC30b; fold TR3-S3
   acceptance + GRB scope union; census re-measure; this pass's dispositions (incl.
   the F2c standing rule + F6 defined-term discipline); VC31 owed on the r30 text.
   Blocker: none.
2. **FGMN transcription-scale re-derivation** (M/L, the chartered hardest-first;
   design/proposal genre): per §R3 + the FOUR riders (row-A satisfiability leg;
   PIN-GEN-i discharge rider at Defs 3.15/3.16/3.18; fgmn.pdf pinned into
   docs/references + sha256; DITER §S3 obligations as fixed points). Product =
   PROPOSAL (Asvin-flag; no corpus application without ROOT adjudication). Blocker:
   none for the unit.
3. **(H-v3) discharge** (S/M source unit, F3): re-fetch HigherNewton.tex by recorded
   sha256 (pin into docs/references); extract the H_{r−1} generic display; derive
   v₃ = w₂ at e₁ = 1 per the TR3S3 §S1 sketch. Acceptance: TR-3′-GEN order-3
   conditionality reduces to standing print attributions. Blocker: none.
4. **F-ADM-3 carrier re-key + `classCount_eq_d`** (M Lean, F2a): rescale `wphi` by E_i
   per ADMREAL §8; re-run the F-ADM-2 scan (prediction: clean = CLASS-LAT); replace
   ADM-U2's `hclass` by the proved lemma; executed under the standing definition-change
   authority with Codex diff confirmation; (ADM) row display updated. Blocker: none
   (authority on record).
5. **TWIST-3 probe** (M numerics, F2b): preregistered order-3 e₁ ≥ 2 measurement of the
   sealed candidate (θ₂ = ℓ₁g₁γ₂); extends strata3_probe.py; TR3-S4 charters ONLY on an
   exact law. Blocker: none.
6. **ITER-LAW proof attempt** (L math): e₀ ≥ 2 order 2, CARRY-1/W-MULT fibred route;
   falsifiers 0/804 + 0/840 on file; hostile-pass bar; slot condition resolved (F5-10).
   Blocker: none mathematically.
7. **(UB-X) consumer-need walk note** (S doc — NAMED DISPATCH, dropped twice): the walk
   that tees the Route-B Asvin decision. Blocker: none for the walk.
8. **(H7) restatement forge** (M Lean, F4): `KLocSemantic`/(I-τ) statements re-keyed to
   the two N-H7-1 pins (slope-keyed ε; in-guard decide-small gates) + an OL-2
   inhabitant attempt per the sharpened spec. Blocker: none (pins on file).
9. **DIterJunctionSupplier statement** (M): landable NOW against the DITER §S3 spec
   with ITER-LAW as displayed hypothesis; its JUNCTION gates BLOCKED on #2's proposal +
   ROOT adjudication (displayed). Blocker: partial, named.
10. **Banked-sorry attempt** (S/M optional, F2d): `npVertices_stable_of_hull_preserved`
    via the BPLL kernels — off-capstone; retires the corpus's one banked sorry.
    Blocker: none.
11. **Asvin batch** (zero-cost, standing): TowerMoveClosureL re-ratification;
    fence-(vii) ROUTE (a) + RECSL hσt rider; the FGMN proposal adjudication (after #2);
    Route-B re-scope (after #7). Blocker: Asvin.
12. **Optional S batch** (fold into passing units): (a) hsc two-instance sign witness;
    (b) GRAFT n2 bijectivity check; (c) the C2 GDOrder1 typeclass-repair design note.
13. Background/BLOCKED (no agents without a new mechanism): (H8′)(i)/(ii) proper;
    (PACK) route-a; (E-N); C2 beyond the design note; the declined forges.

— Synthesis pass 7, 2026-08-08.
