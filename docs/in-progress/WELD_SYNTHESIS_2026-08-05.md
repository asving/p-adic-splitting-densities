# WELD SYNTHESIS — three unification hypotheses over the composed weld (H1 ONE-TRANSPORT / H2 ONE-COCYCLE / H3 J-D SHORTCUT), adjudicated at statement level with exact-arithmetic spot checks — the headline: the four normalization laws are faces of ONE 1-COCHAIN (the eq-12 twist kit t_j / ε_j), and the [ILN]† inner cocycle factor is its coboundary (7,999 spot samples, 0 violations)

**Unit:** SYNTHESIS (read-only on the corpus; this ONE new note). **Genre:
ADJUDICATION / DESIGN-INPUT note — nothing is proved at grade here, no note
is edited, no gate fires, no acceptance arc opens.** Every "LAW" display
below is a CANDIDATE under the trust boundary (new-statement sites,
review-owed) whose sole evidentiary legs are (a) statement-level
specialization checks against the four composed junction notes and (b) the
Appendix-A scratch instrument (NOT a sealed runner; disclosed in full).
Author: Fable synthesis unit, 2026-08-05 (campaign date 2026-08-08).

**Snapshot caveat.** The four notes are under active repair by other units.
This synthesis reads: `GRTW2_PROOF_2026-08-08.md` (r0 as committed),
`GRTJA_PROOF_2026-08-08.md` (post-r1, arc 0/1 clean),
`GRTJB_PROOF_2026-08-08.md` (post-r3, §0M conditionality matrix in force),
`GRTJC_PROOF_2026-08-08.md` (post-r2, (DMULT) two-tier split in force),
`GRT_WELD_BLUEPRINT_2026-08-08.md`, `LEDGERLAMBDA_2026-08-08.md`,
`ITERLAWN_PROOF_2026-08-08.md` (ACCEPTED), and the weld tail of
`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`. Anything those arcs change
later must be re-checked against this note's quotes.

**Grades consumed (never upgraded):** [ILN]† ACCEPTED (its integer layer —
SHIFT-(n), D_j-INT, (★)-POS — "PROVED, generic, unconditional", its value
law on the scored stratum); W-2 / J-A / J-B / J-C at ATTEMPT grade 0/2 with
their full conditionality ledgers ((C-coll), JA-BOX-1..5, JB §0M, (DMULT-w/s),
JC-BOX-1..7). Nothing below may be consumed at proof grade.

---

## §0. The four statements of record (the objects under synthesis, one line each)

* **W-2 (lift junction).** Clause 2 + §3.4: at a shared-key junction the
  print key and the harness lift differ by a per-slot z-character, with the
  closed form `ξ_{m+1} := χ_m^{γ_{m+1}}·z_m^{−e_m·A_m}`,
  `w_{m+1} := χ_m^{−g_m·γ_{m+1}}`,
  `χ_m := χ_{m−1}^{ℓ′_{m−1} − ℓ_{m−1}·w_{m−1}Φ_{m−1}}·z_{m−1}^{A_{m−1}·ℓ_{m−1}}`,
  and the print tie "z_m^{−A_m} = c₁ (the Theorem-`phir` constant)";
  scalar form valid on (C-coll), exact law = (HR-REC); clause 3 =
  ψ-transport; value layer = W2-OPEN-1 (OPEN).
* **J-A (dictionary junction).** JA-VAL `w_m = v_{m+1}`; JA-EPS
  `ε_m(β) = z_m^{−t_m(β)}` ("the harness ε-kit IS the print's t(i)-twist
  rewritten in eq12-canonical coordinates"); JA-GRID (eq-12 = T-side initial
  point; carry = lattice crossing); JA-RES per-slot law
  `c_i = z_{m−1}^{−i·A_{m−1}} · C_i` (unconditional at m = 2; per-digit
  proviso above; JA-BOX-1 refutes the blueprint's slot-independent c).
* **J-B (tree junction).** JB-DEV/JB-VTX (same division; polygon
  vertex-for-vertex through the displayed shear Σ_q); JB-TREE (the [RMG]
  tree = the classifier's own division arithmetic; LED-Λ's line face = the
  polygon bookkeeping); LEMMA JB-EPS (unconditional):
  `t_m(V+V′) − t_m(V) − t_m(V′) = ε′ = ⌊(s_m^wt(V)+s_m^wt(V′))/e_{m−1}⌋`;
  JB-CREAD `ρ_m(R) = c_m·ρ_m(f)` with `c_m = ψ_{m,0} = −z_{m+1}` (the carry
  cancels in print coordinates — JB-BOX-1); conditionality = the §0M matrix
  ((VD-m) free at m ≤ 1 only).
* **J-C (carry-algebra junction).** W-6/W-7/W-8/W-9 on B_n (JC-LOC: "B *is*
  the object FGMN's theorems speak about"); Ψ : (⊕K·u_ν, c_T) ≅ 𝒜(T);
  `c_{m+1}(γ,γ′) = z̄^δ·∏_{j=1}^{m} z_j^{D_j}` on [ILN]†'s scored stratum;
  JC-COB (on (DMULT-s)): the inner factor `= a_γa_{γ′}/a_{γ+γ′}` (anchor
  coboundary); W-9: 𝒜(T) = the twisted group algebra K^c[ℤ/E]; JC-F1:
  `𝒜(T) ≅ K[v]/(v^E − ζ_T)` abstractly at EVERY composite stage (the cyclic
  FENCE is about the anchor-indexed presentation (CYC), not the class).
* **[ILN]† (the accepted engine, consumed by all four).** S0.3:
  `δ := ⌊(s_{n+1}+s′_{n+1})/e_n⌋`, `S_n := δ·γ_{n+1}`, and for j = n..1
  `D_j := (s_j-defect + ℓ_{j−1}·S_j)/e_{j−1}`,
  `S_{j−1} := D_j·γ_j + S_j·(ℓ′_{j−1} − ℓ_{j−1}·w_{j−1}Φ_{j−1})`;
  SHIFT-(n) `β_j″ = B_j + S_j` PROVED unconditional (integer layer).

---

## §1. H1 — ONE TRANSPORT: verdict SUBSUMED-PLUS-SHARPENING at all four junctions, at the price that the master statement is an ORGANIZING umbrella, not new mathematics

### 1.1 The candidate master statement (written precisely, as charged)

**CANDIDATE (M), the anchored graded transport [review-owed; NOT asserted].**
For every tower T of the [ILN]† S0.1 class pin realized as a principal-read
OM type (JA-PIN's map T(t)), there is one transport 𝕋 between the harness
calculus (Φ-developments, weights w_m, reads R_m v, ε-kit, canonical lifts)
and the classifier/print calculus (φ-adic developments, v_{m+1}, residual
operators, t(i)-twists, GMN keys), with four clauses:

    (M0) [substrate] 𝕋 is the IDENTITY on the underlying division
         arithmetic: same canonical division chain by the same monic key at
         the admissible representative (domain statement = JA-PIN; W2-C1
         supplies representative legitimacy).
    (M1) [gradings] 𝕋 preserves weights in the sheared frame (w = v), and
         its induced map on polygon/grading data is the explicit affine
         shear Σ_q — lower hulls to lower hulls, vertex-for-vertex.
    (M2) [reads/operators] 𝕋 acts on packed read data by explicit per-slot
         z-characters generated by ONE 1-cochain (the eq-12 twist kit t_j /
         ε_j of §2): scalar per slot exactly on the collapse perimeter,
         exact recursion (HR-REC genre) elsewhere.
    (M3) [multiplication] 𝕋 transports multiplication up to the carry
         cocycle: on B_n coordinates the target is the twisted group
         algebra K^c[ℤ/E] with c = z̄^δ·∏_j z_j^{D_j} on the scored stratum,
         and harness-val = TC-read.
    (M4) [covariance] 𝕋 is covariant in the lift/representative choice:
         changing the representative (harness canonical lift ↔ print key ↔
         ℓ-orbit) acts on all of (M2)-(M3) by the (ξ, w) character system,
         with ψ-transport making the two chains carry the same type.

### 1.2 Junction-by-junction specialization check (statement level)

| junction | is the composed theorem a specialization of (M)? | what it carries BEYOND the specialization | verdict |
|---|---|---|---|
| **J-A** | YES: JA-VAL = (M1)'s weight clause ("w_m(A) = v_{m+1}(A)"); JA-EPS = (M2)'s cochain identity ("ε_m(β) = z_m^{−t_m(β)}"); JA-GRID = (M0)/(M1)'s grid face; JA-RES = (M2) on digits ("c_i = z_{m−1}^{−i·A_{m−1}}·C_i") | (a) JA-PIN — this is (M)'s DOMAIN clause, which (M) consumes rather than implies (the class-pin conformance is prior to any transport); (b) the exact collapse geography (JA-VDIND's absolute-triviality condition, the unsealed converse, the CK-RES boundary census T3B/I4B); (c) JA-BOX-1's refutation of the slot-independent form — a sharpening (M) must inherit verbatim (M2 must be stated per-slot, never scalar) | **SUBSUMED-PLUS-SHARPENING** |
| **J-B** | YES: JB-DEV = (M0) ("both are THE canonical division chain"); JB-VTX = (M1) ("H(f) = Σ_q(U(f))" + JB-AFF — the shear IS the induced affine map on gradings, exactly the H1 conjecture's phrasing); JB-TREE = (M0) iterated (the tree is division arithmetic, on which 𝕋 is the identity — "(i)/(ii) are PERIMETER-FREE" is exactly what (M0)-invariance predicts); JB-CREAD = (M2)+(M3) evaluated at the correction digit | (a) LEMMA JB-EPS — NOT a specialization: it is the HINGE between (M2) and (M3) (the cochain's coboundary law, §2 LAW-2), i.e. part of (M)'s proof kit; (b) JB-BOX-1's frame correction (the carry cancels in print coordinates — c_m not τ_m) — a sharpening; (c) the §0M conditionality matrix — the fence surface (M) must reproduce cell-for-cell; (d) the LED-Λ citation (polygon bookkeeping consumed once) | **SUBSUMED-PLUS-SHARPENING** |
| **J-C** | YES: W-6/W-7/W-8/W-9 = (M3) verbatim (the target exists; Ψ iso; val = TC-read; K^c[ℤ/E]) | (a) JC-LOC — (M)'s CODOMAIN identification (B_n, not gr_n, is where the transport lands: "B *is* the object FGMN's theorems speak about") — prior to (M3), consumed by it; (b) JC-LOAD/JC-PSIKER sharps (TC-3 load-bearing EXACTLY at δ ≥ g_m; kernel exactly ψ_m·Δ) — internal structure (M) does not need but J-D does (§3); (c) the (DMULT-w)/(DMULT-s) tier split — (M)'s single literature input, priced there; (d) JC-F1/(CYC) — the presentation-vs-class distinction §2.5 explains | **SUBSUMED-PLUS-SHARPENING** |
| **W-2** | YES: clauses 1–3 = (M4) (admissibility of the representative, the (ξ,w) covariance, ψ-transport) | (a) the CLOSED FORM §3.4 — much sharper than "(M4): a character system exists"; (b) W2-C3's transport CONSTRUCTION — (M4) is not even well-posed off byte-equal chains without it; (c) W2-BOX-1's boundary (the I4B mod-wrap) — the exact place (M2)'s scalar form dies; (d) W2-OPEN-1 — (M)'s value layer on transported lifts is OPEN, and (M) inherits it as an open leg, displayed | **SUBSUMED-PLUS-SHARPENING** |

### 1.3 The honest content assessment

The specialization check PASSES at all four junctions — but partly because
(M) is engineered post hoc to have the four as faces. What is genuinely
gained by composing (M), beyond repackaging:

1. **The fence surface collapses.** The four notes carry four separately
   worded conditionality systems. Under (M) they are THREE master fences:
   (F-a) the shared-key/byte perimeter (where the two chains use the same
   keys — (M0)'s hypothesis); (F-b) the COLLAPSE PERIMETER — and here the
   synthesis finds that W-2's (C-coll), J-A's (R-coll)/JA-VDIND condition,
   and J-B's (VD-m) fence are instances of ONE condition, "the transport
   characters generated by the §2 cochain act trivially on the attained
   support" (W-2 §3.2's letter twist ϑ_Q, J-A's ϑ_m := z_{m−1}^{−e_{m−1}A_{m−1}},
   and JB's (VD-m) hypothesis are the same character family: at trivial
   lower χ-chain, W-2's interior junction character ξ_Q = z^{−e·A} IS
   JA-VDIND's ϑ; J-B consumes the J-A condition verbatim). Caveat displayed:
   the alignment is exact only modulo the χ-chain corrections at deeper
   levels and the mod-wrap boundary (W2-BOX-1's I4B witness) — the master
   note must carry that boundary, not erase it. (F-c) the (DMULT-w)
   citation + the ℓ-orbit convention fence (JA-BOX-3/W-2 §4.3).
   JB's r3 round exists BECAUSE per-site fence patching did not converge
   ("A fifth sweep would find a sixth site"); one fence surface for the
   whole weld is the same medicine at the campaign level.
2. **The hinge lemmas get one home.** JB-EPS, W2-L2, JA-EPS, JC-COB are
   currently distributed; §2 shows they are four faces of one display.
3. **Nothing else.** (M) proves no new instance, discharges no open leg
   (W2-OPEN-1, the (VD-m) strata, (DMULT-s), JB row 5/12/22 evidence
   frontier all ride), and must NOT be advertised as more than the
   LEDGERLAMBDA genre: one telescoped statement + faces as corollaries,
   consumption-only.

---

## §2. H2 — ONE COCYCLE: verdict **PARTIAL as literally stated / HOLDS in the corrected ONE-COCHAIN form**, with the dictionary displayed and machine-spot-checked (7,999 samples, 0 violations)

### 2.1 The correction that makes it true

The hypothesis as posed — "the four normalization laws are all
values/faces of the SINGLE carry cocycle c of K^c[ℤ/E]" — is FALSE in one
direction and TRUE after one demotion: the four laws are not all values of
the 2-COCYCLE c; they are all generated by the single 1-COCHAIN underneath
it. Define, per level j (read (e_{j−1}, h_{j−1}), normalized Bézout
ℓ_{j−1}):

    THE WELD COCHAIN      t_j(β) := (ℓ_{j−1}·β − s_j(β))/e_{j−1} ∈ ℤ,
                          s_j(β) := ℓ_{j−1}β mod e_{j−1}
    (field form, JA-EPS)  ε_j(β) = z_j^{−t_j(β)}
    (composite form)      E(γ)  := ∏_{j=1}^{n} ε_j(β_j(γ)),  γ ∈ W,
                          β_j(γ) the split's own level-j weight.

t_j is exactly J-A's print-twist exponent (JA-EPS, PROVED there), so this
cochain is not new — what is new is the observation that ALL FOUR laws are
its faces:

### 2.2 The dictionary (each law as a cochain statement), with spot-check numbers

All checks: Appendix A instrument, towers T2A / T2C / T3A / T3E / I4B
(wild p ∈ {2,3}, h = 2 reads, d₀ = 2, flat middle, 4-read boundary tower),
window-pair grids; exact arithmetic; teeth = none (scratch instrument,
disclosed as such).

* **LAW-1 (the J-C face; the headline).** The [ILN]† inner exponents are
  the coboundary of the weld cochain at the split's own arguments:

      D_j = t_j(β_j(γ+γ′)) − t_j(β_j(γ)) − t_j(β_j(γ′))     for j = 1..n,

  equivalently `z_j^{D_j} = ε_j(β_j)·ε_j(β_j′)·ε_j(β_j″)^{−1}`, hence

      c_{n+1}(γ,γ′) = z̄^δ · (∂E)(γ,γ′)      [∂E = E(γ)E(γ′)/E(γ+γ′)]

  — THE ENTIRE INNER FACTOR OF THE ACCEPTED COCYCLE IS A COBOUNDARY (of
  the ε-kit composite cochain E). One-display derivation (review-owed;
  elementary GIVEN [ILN]†'s unconditional integer layer): t_j(β) + t_j(β′)
  − t_j(β″) = [ℓ_{j−1}(β+β′−β″) − s_j-defect]/e_{j−1} = [−ℓ_{j−1}S_j −
  s_j-defect]/e_{j−1} = −D_j, using SHIFT-(n) (β_j″ = B_j + S_j, PROVED
  unconditional) and the S0.3 definition of D_j; the field form is JA-EPS.
  So the identity's conditionality footprint is: [ILN]† integer layer
  (unconditional, ALL pairs incl. DCX) + JA-EPS (PROVED) — note this is
  SMALLER than the cocycle VALUE law's own footprint (scored stratum).
  **[Machine: CHK-1, 644 (pair, level) samples, 0 violations; field form
  CHK-4, 644, 0 violations.]**
* **LAW-2 (the J-B face).** JB-EPS is the PLAIN (unshifted) coboundary of
  the same cochain at one level — `∂t_m(V,V′) = ε′` the plain carry —
  i.e. exactly LAW-1's shape with S ≡ 0; and [ILN]†'s own restart case
  (S_j = 0 ⟹ D_j = δ_j^{pl}, ILN S2) is the statement that on the restart
  stratum the inner exponent IS J-B's ε′. **[Machine: CHK-3a, 5,625
  samples, 0 violations; restart tie CHK-3b, 441, 0 violations.]**
  J-B's OTHER law, c_m = ψ_{m,0} = −z_{m+1}, is NOT a cochain value: it is
  the OUTER letter — TC-3's defining relation at g_m = 1 (ψ_m(z̄) = 0 ⟺
  z̄ = −ψ_{m,0}) surfacing as a transport constant. Assignment: c_m belongs
  to the cocycle's outer factor z̄^δ (the part LAW-1 shows is NOT a
  coboundary), matching J-C §3.3's split of the germ (confirmed as value
  law; refuted as inner factor).
* **LAW-3 (the J-A face).** The per-slot character exponent i·A_{m−1} is
  the cochain evaluated on the KEY SUBLATTICE, where it is additive:
  s_m(W_m) = 0 (W_m = e_{m−1}g_{m−1}γ_m ≡ 0 mod e_{m−1}), so

      t_m(k·W_m) = k·A_m   and   t_m(V + k·W_m) = t_m(V) + k·A_m  (carry-free),

  which is W2-L2 restated: the JA-RES character `z^{−i·A_{m−1}}` is the
  first difference of the cochain along the key direction (slot i sits at
  ordinate β_i + i·W_{m−1}). **[Machine: CHK-2, 609 samples, 0 violations.]**
* **LAW-4 (the W-2 face).** The covariance SEED is the cochain at the key
  weight: the Theorem-`phir` constant c₁ = z_m^{−A_m} = ε_m(W_m) (so the
  m = 1 closed form ξ = c₁^{e₁} is the cochain value raised to the read's
  ramification), and ε_m(k·W_m) = z_m^{−k·A_m} exactly. **[Machine: CHK-5,
  36 samples, 0 violations.]** The COMPOSITION of the covariance up the
  tower is the same descent operator as the cocycle's shift chain —
  byte-compare the two displays:

      W-2 §3.4:  χ_m = χ_{m−1}^{ ℓ′_{m−1} − ℓ_{m−1}·w_{m−1}Φ_{m−1} } · z_{m−1}^{A_{m−1}·ℓ_{m−1}}
      [ILN]† S0.3: S_{j−1} = D_j·γ_j + S_j·( ℓ′_{j−1} − ℓ_{j−1}·w_{j−1}Φ_{j−1} )

  — identical multiplier (W-2 §3.4 remark 1 already says this; the
  synthesis promotes it from remark to load-bearing evidence for H2).
  **What resists (the honest residue):** the χ-chain at m ≥ 2 and the
  mod-wrap boundary (W2-BOX-1, the I4B r = 5 witness) are NOT reduced to
  cochain values by this dictionary — the wrap is precisely where the
  scalar one-cochain picture fails, and it fails at the SAME boundary in
  W-2 ((C-coll)), J-A ((R-coll)) and J-B ((VD-m)) — §1.3's one-perimeter
  observation seen from the other side.

### 2.3 Verdict line for H2

**ONE-COCYCLE as stated: PARTIAL** (J-B's c_m and the outer z̄^δ are not
coboundary/cochain values; the χ mod-wrap boundary resists).
**ONE-COCHAIN (the corrected form): HOLDS on the checked perimeter** — all
four normalization laws are displayed faces of t_j/ε_j, exponent-exactly,
with LAW-1 as the new load-bearing display: `c_{n+1} = z̄^δ·∂E`.

### 2.4 The cohomological corollary (candidate, review-owed)

LAW-1 says the composite carry cocycle is cohomologous to its OUTER part
alone: `[c_{n+1}] = [z̄^δ] in H²(ℤ/E, K^*)` (on the scored stratum, where
the value law holds). This EXPLAINS J-C's FINDING JC-F1 — `𝒜(T) ≅
K[v]/(v^E − ζ_T)` abstractly at every composite stage — cohomologically:
twisting by a coboundary does not change the algebra class, so only the
carry-in-z̄ part can obstruct cyclicity, and it never does abstractly. It
is also CONSISTENT with the cyclic FENCE: (CYC) is a property of the
anchor-indexed PRESENTATION (the specific cochain), and coboundaries are
exactly what presentations see and classes do not. The fence and the
abstract cyclicity stop looking like a tension and become the two sides of
"cocycle vs cochain".

### 2.5 A second candidate corollary (flagged for the master note, NOT asserted)

JC-COB (on the MEASURED tier (DMULT-s)) gives inner = `a_γa_{γ′}/a_{γ+γ′}`
= ∂a; LAW-1 gives inner = ∂E. Hence ∂(a/E) = 1 on the scored stratum: the
ratio a_λ/E(λ) of the anchor digit constants to the ε-composite is
MULTIPLICATIVE in λ where measured. If a closed form for a_λ/E(λ) were
proved, (DMULT-s)'s ω ≡ 1 — currently MEASURED-not-cited, J-C's single
remaining exact-tier gap — would reduce to the multiplicativity of an
explicit unit system. This is the highest-value single target the
synthesis surfaces: it would move JC-COB's tier from MEASURED toward
PROVED. (Status: candidate; nothing here proves it; the ε-kit
multiplicativity is exactly what GRTJC §2.10 says "this note does not do".)

---

## §3. H3 — J-D SHORTCUT: verdict **NOT VIABLE as stated** (witnessed degrees-of-freedom mismatch), with the actual reduction J-C does buy displayed

### 3.1 The literal test, run

**Order-0 machine-checked instance.** The Lean capstone leg
(`OM/OMCountV2.lean`, `gate3_inert_q2`): `omCount (sepShape 2 inert) 2 =
1/4` — at general q the inert order-0 count is (#monic irreducible
quadratics over F_q)/q² = (q²−q)/(2q²), a q-DEPENDENT rational (1/4 at
q = 2). The twisted-algebra side at order 0: no reads, E = 1, K^c[ℤ/1] = K
— exactly ONE character, no q-dependence available. Already at the
machine-checked base case the character/orbit count of A(T) cannot express
the stratum count.

**One order-1 decided shape.** Take the ramified-quadratic shape (d₀ = 1,
one read (e,h,g) = (2,1,1); the Eisenstein stratum of n = 2). The L3 × L4
count of monic quadratics x² + a₁x + a₀ realizing it: v(a₀) = 1 (residual
unit choice: measure (q−1)/q²) × v(a₁) ≥ 1 (lattice leg: 1/q) =
**(q−1)/q³** — again a q-polynomial family. The twisted algebra: E = 2,
𝒜 ≅ K[v]/(v² − z̄) (order-1: no inner factor; ζ = z̄); its
character/factor count is 2 or 1 according as z̄ is a square in K^* —
bounded by E = 2, q-independent, and not even monotone in q. **The two
sides do not agree and cannot: dim_K K^c[ℤ/E] = E is an invariant of the
SHAPE, while the stratum count is a measure (a rational function of q of
unbounded data). J-D2 is NOT a character/orbit count of A(T).**

The category error, named: A(T) is built from ONE tower with FIXED residual
data (its letters z_j are the given ψ-roots); J-D2 counts the CHOICES of
residual data and digits across all f realizing the shape. The carrier
never sees the choice space.

### 3.2 What J-C + the synthesis DO buy J-D2 (the actual reduction)

* **(J-D0, candidate lemma — the real shortcut) COUNT GAUGE-BLINDNESS.**
  Every conditional layer of the weld's value dictionary is a UNIT
  CHARACTER acting slot-wise (JA-RES's z^{−iA}·C_i, W-2's (ξ,w), the
  ℓ-orbit rescalings ORB-RES) — i.e. a bijection of each slot's value
  space K_m. Stratum COUNTS computed through the welded read are therefore
  invariant under the ENTIRE gauge layer: W-10's count equality needs only
  (a) the PROVED integer/grid faces (JA-GRID's grid = T-side abscissas,
  JB-VTX's vertex-for-vertex polygon with slope/length/degree preserved,
  JB-DEV's same-division), which fix the POSITIONS (the L4 lattice
  exponent), and (b) per-slot VALUE counts (the L3 leg), which unit
  bijections cannot change — and does NOT need (VD-m), (C-coll), (R-coll),
  or (DMULT-s). **The count equality escapes the collapse-perimeter
  conditionality entirely.** This is the single most consequential
  simplification this synthesis finds for J-D: the hard open fences of the
  weld are value-layer fences, and J-D2 is a count-layer statement.
* **The wild-stage WARRANT (what J-C supplies).** Distinct strata must not
  collapse under the read at composite stages — the no-cancellation /
  level-detection clause the blueprint's O-1 cites. That is exactly
  JC-PSIKER(iii) (the read's kernel on every B_n is EXACTLY ψ_m·Δ — the
  next key and nothing else) + JC-LOAD (the load geography δ ≥ g_m). J-C
  enters J-D as warrant, not as counting formula.
* **What W-10/W-11 still owe (not obtainable from the four junctions).**
  (i) The menu enumeration (which decided order-≥1 shapes exist per σ —
  Phase-B omMenu growth, a genuinely fresh computation); (ii) the L3
  residual-count formula per shape (counting monic irreducible ψ /
  residual values over F_q — classical, but not a weld consequence);
  (iii) the L4 exponent B(c) tie to the welded grid (mostly ready: JA-GRID
  + JB-VTX(d) — noting JB row 12's u ≥ 0 proviso and missing machine leg
  ride exactly here and should be discharged before W-10 consumes (d4));
  (iv) hExhaust.

**H3 verdict: NOT VIABLE as a character/orbit-count shortcut (witness
displayed); the J-D block should go the BLUEPRINT route (W-10/W-11 as
designed), preceded by the small candidate lemma (J-D0) above, with J-C
consumed as warrant.**

---

## §4. RECOMMENDATION

**(i) Is a MASTER-TRANSPORT capstone note worth composing? YES — as a
LEDGERLAMBDA-genre consolidation, not as a new proof unit.** Estimated
content: THEOREM (M) as §1.1 (statement-level umbrella, consuming the four
notes at their grades) + the ONE-COCHAIN dictionary of §2 as its normal-form
display (LAW-1..LAW-4; the c_{n+1} = z̄^δ·∂E display is the one genuinely
new theorem-shaped item, and its proof is three lines GIVEN [ILN]†'s
unconditional integer layer) + the ONE-PERIMETER fence surface (F-a/F-b/F-c
of §1.3, replacing four per-note fence systems for downstream consumers,
with the I4B mod-wrap boundary carried explicitly) + the §2.4 cohomological
corollary. What it would consume: the four junction notes + [ILN]† verbatim,
each at its own grade. What revision it would force on the four notes:
**NONE** (consumption-only; any supersession brackets happen at the next
fold, per the LEDGERLAMBDA precedent — "SUBSUMES … does not edit, bracket,
or retire"). What it must NOT claim: any grade upgrade; any discharge of
W2-OPEN-1, the (VD-m) strata, (DMULT-s), or JB's rows 5/12/22 evidence
frontier. Machine leg: promote Appendix A's five families to a sealed
two-commit runner (cheap; the scratch run is 7,999/0). Priority note: the
four hostile ARCS to 2-clean remain the campaign's gating work — the master
note is high value-per-cost but must not displace the arcs.

**(ii) J-D composition plan: BLUEPRINT ROUTE.** W-10/W-11 as designed in
GRT_WELD_BLUEPRINT §S2.4/S3.1, with this order: (1) compose (J-D0)
count-gauge-blindness (small, S-priced; it deletes the value-layer
conditionality from W-10's hypotheses); (2) W-10 at ONE order-1 decided
shape end-to-end (the (2,1,1) Eisenstein shape of §3.1 is the natural
first row — its count (q−1)/q³ is already displayed here) against PARI;
(3) W-11 menu tie (Phase-B). The H3 shortcut is rejected with the §3.1
witness; do not re-attempt character counting.

**(iii) Simplifications NOT on the orchestrator's list, surfaced by this
pass:**
1. **The one-perimeter unification (F-b).** (C-coll) [W-2], (R-coll)/
   JA-VDIND [J-A], and the (VD-m) fence [J-B] are one condition on one
   character family (§1.3). A master note that states it ONCE — with the
   I4B boundary — collapses the fence bookkeeping that consumed JB's
   entire r1–r3 arc ("per-site fence patching has not converged").
2. **The a/E multiplicativity target (§2.5).** ∂(a/E) = 1 where measured;
   a proved closed form for a_λ/E(λ) would upgrade (DMULT-s) from
   MEASURED toward PROVED — J-C's single remaining exact-tier gap, and
   with it JC-COB's sharp inner-factor form.
3. **The χ-from-S candidate.** LAW-4's shared descent operator suggests
   W-2's χ_m may have a closed form in [ILN]† shift-chain data (same
   affine recursion, different seed). If true, W-2 §3.4 and [ILN]† S0.3
   become one recursion table with two seed columns — a further
   LEDGERLAMBDA-style compression. (Candidate only; not checked beyond the
   multiplier byte-identity.)

---

## Appendix A — the spot-check instrument (scratch; disclosed verbatim)

NOT a sealed runner: no preregistration, no teeth, no two-commit seal;
evidence grade = exploratory spot check only. Script:
`/tmp/weld_synthesis_spotcheck.py` (reproduced below); engine: the
committed `verification/openmath/iterlawn_pe_reimpl.py` (Tower, split,
constants, eps — verbatim import, no modification); towers: T2A, T2C, T3A,
T3E (weld-probe roster specs, copied byte-for-byte) + I4B
(`ITL.INSTANCES[1]`); pair grids: `window_reps` per roster pairn.

Output (2026-08-05 run, verbatim):

    T2A  done  (pairs=16, levels=1)
    T2C  done  (pairs=36, levels=1)
    T3A  done  (pairs=64, levels=2)
    T3E  done  (pairs=16, levels=2)
    I4B  done  (pairs=144, levels=3)

    family   violations    samples
    CHK-1             0        644   [LAW-1: D_j = coboundary of t_j]
    CHK-2             0        609   [LAW-3: A_m = t_m(W_m); key-lattice additivity]
    CHK-3a            0       5625   [LAW-2: JB-EPS plain-carry face]
    CHK-3b            0        441   [LAW-2: restart face D_j = plain carry at S_j = 0]
    CHK-4             0        644   [LAW-1 field form: z^{D_j} = ∂ε_j]
    CHK-5             0         36   [LAW-4: ε_m(kW_m) = z_m^{−kA_m} = c₁-tie]
    VERDICT: ALL GREEN

```python
import sys
sys.path.insert(0, '<repo>/verification/openmath')
import iterlawn_pe_reimpl as ITL

SPECS = [
 dict(id='T2A', ring='Zp', p=2, d0=1, Phi0=[0, 1], reads=[(2,1,2),(2,1,1)],
      psi=[[1,1,1],[('z',1,1),1]], pairn=6),
 dict(id='T2C', ring='Zp', p=3, d0=1, Phi0=[0, 1], reads=[(2,1,1),(3,2,1)],
      psi=[[1,1],[('z',1,1),1]], pairn=6),
 dict(id='T3A', ring='Zp', p=2, d0=2, Phi0=[1, 1, 1],
      reads=[(2,1,1),(2,1,1),(2,1,1)],
      psi=[[('X',1),1],[('z',1,1),1],[('z',2,1),1]], pairn=8),
 dict(id='T3E', ring='Zp', p=2, d0=1, Phi0=[0, 1],
      reads=[(2,1,2),(1,1,1),(2,3,1)],
      psi=[[1,1,1],[('z',1,1),1],[('z',1,1),1]], pairn=8),
 ITL.INSTANCES[1],   # I4B
]

def t_cochain(T, j, beta):
    e, l = T.e[j-1], T.l[j-1]
    s = (l*beta) % e
    return (l*beta - s)//e

def s_wt(T, j, beta):
    e, l = T.e[j-1], T.l[j-1]
    return (l*beta) % e

def kpow(K, x, n):
    if n < 0:
        return kpow(K, K.inv(x), -n)
    acc = K.one()
    for _ in range(n):
        acc = K.mul(acc, x)
    return acc

tot = {k: [0, 0] for k in ('CHK-1', 'CHK-2', 'CHK-3a', 'CHK-3b',
                           'CHK-4', 'CHK-5')}
for spec in SPECS:
    T = ITL.build_tower(spec)
    n, NR = T.n, T.NR
    W1 = ITL.window_reps(T, spec.get('pairn', 8))
    pairs = [(ga, gb) for ga in W1 for gb in W1 if T.inW(ga+gb)]
    for (ga, gb) in pairs:
        c = T.constants(ga, gb)
        sa, sb, sc = T.split(ga), T.split(gb), T.split(ga+gb)
        for j in range(1, NR):
            ba, bb, bc = sa['beta'][j], sb['beta'][j], sc['beta'][j]
            pred = t_cochain(T, j, bc) - t_cochain(T, j, ba) - t_cochain(T, j, bb)
            tot['CHK-1'][1] += 1
            if c['D'][j] != pred: tot['CHK-1'][0] += 1
            Kj = T.K[j]
            lhs = kpow(Kj, T.z[j], c['D'][j])
            rhs = Kj.mul(Kj.mul(T.eps(j, ba), T.eps(j, bb)),
                         Kj.inv(T.eps(j, bc)))
            tot['CHK-4'][1] += 1
            if lhs != rhs: tot['CHK-4'][0] += 1
            if c['S'][j] == 0:
                dpl = (sa['s'][j] + sb['s'][j]) // T.e[j-1]
                tot['CHK-3b'][1] += 1
                if c['D'][j] != dpl: tot['CHK-3b'][0] += 1
    for m in range(1, NR):
        A_m = T.l[m-1]*T.g[m-1]*T.gam[m]
        W_m = T.wPhi[m]
        tot['CHK-2'][1] += 1
        if t_cochain(T, m, W_m) != A_m: tot['CHK-2'][0] += 1
        for V in range(0, 4*W_m + 5, max(1, W_m//3)):
            for k in range(0, 4):
                tot['CHK-2'][1] += 1
                if t_cochain(T, m, V + k*W_m) != t_cochain(T, m, V) + k*A_m:
                    tot['CHK-2'][0] += 1
        for V in range(0, 25):
            for Vp in range(0, 25):
                eprime = (s_wt(T, m, V) + s_wt(T, m, Vp)) // T.e[m-1]
                tot['CHK-3a'][1] += 1
                if (t_cochain(T, m, V+Vp) - t_cochain(T, m, V)
                        - t_cochain(T, m, Vp)) != eprime:
                    tot['CHK-3a'][0] += 1
        Km = T.K[m]
        for k in range(0, 4):
            tot['CHK-5'][1] += 1
            if T.eps(m, k*W_m) != kpow(Km, T.z[m], -k*A_m):
                tot['CHK-5'][0] += 1
```

Sign/indexing pins used (so a hostile reader can re-derive): t_j uses read
(e_{j−1}, h_{j−1}) — harness level-j eq-12 kit; β_j(γ) is the split's own
nested value (`Tower.split`); D_j/S_j from `Tower.constants` (the S0.3
recursion verbatim); ε via `Tower.eps(j, β)`; A_m := ℓ_{m−1}g_{m−1}γ_m,
W_m := w_mΦ_m = T.wPhi[m].

— WELD SYNTHESIS unit, 2026-08-05.
