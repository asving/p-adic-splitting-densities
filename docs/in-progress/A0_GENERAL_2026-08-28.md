# A0G — the uniform general-`n` `a0` mechanism

Date: 2026-08-28  
Status: **design complete; uniform theorem not proved; one research stop-line remains**.  
Target: the `a0` field of `I10_I15_I18.lean`, namely `DecidedSliceAt n`.

No owner's paper is an authority for this blueprint.  The quartic formulas below are checked
by `verification/a0g_quartic_cert.py`; they are evidence at degree four and are not promoted to
a theorem at degree five or above.

## 1. Route adjudication

**Do not start a degree-by-degree campaign.  Select the pure-engine architecture, but mark the
uniform theorem BLOCKED at `A0G-GC`, the finite rational genre-cover theorem.**  The completed
dv-graded Hensel engine makes the local factorization step degree-generic.  It does not prove
that all inputs fall into finitely many *symbolic, countable* coefficient genres, nor that the
infinite refinement parameters sum to rational functions of `q`.

The currently available cite-backed route does not repair that gap:

* FGMN, *Residual ideals of MacLane valuations*, Theorem 6.6, is an excellent source for the
  slope/residual-factor dissection already isolated by `C33Cite.exists_dvDissection`.  It is a
  local factorization theorem at a supplied inductive valuation and proper key polynomial; it
  is not a finite coefficient-cylinder classification or a rational-measure theorem.
* Alberich-Carramiñana–Guàrdia–Nart–Poteaux–Roé–Weimann, *Polynomial factorization over
  henselian fields*, Theorem 6.12, proves correctness/termination of its rank-one
  factorization algorithm under the stated factorization-section hypotheses.  That section
  assumes a monic square-free input and `char(k)=0` or `char(k)>deg(g)`.  The present theorem
  ranges over complete DVRs with finite residue field, including wild primes dividing `n`, so
  that result cannot be imported as an all-characteristic backstop.

Even an all-characteristic, pointwise OM-termination theorem would still be weaker than the
needed statement.  `DecidedSliceAt` asks for one rational function valid for every complete DVR
with residue cardinality `q`.  The proof needs a uniform finite schema of cells, exact cell
counts, and a proof that all unbounded depth parameters are geometric/Presburger summations.
Pointwise termination alone supplies none of those three assertions.

Accordingly:

| route | decision | what it supplies | faithfulness cost / missing clause |
|---|---|---|---|
| pure engine | **selected design; presently blocked** | factor splitting, exact degrees, residual transport, type algebra | must prove `A0G-GC` and the rational cell-count grammar internally |
| cite-backed OM completeness | **not sufficient as sole route** | may justify local dissection or future termination | current broad termination source excludes wild residue characteristic; any pointwise theorem still lacks uniform rational counting |
| direct density axiom/cite | **rejected** | would close the field syntactically | no source-faithful theorem with the exact quantifiers; it would hide the main mathematical content |

The permitted future hybrid is narrow: cite an exact all-characteristic factorization or
termination clause, bind every hypothesis to the repository's complete-DVR setting, and prove
the finite genre enumeration, measures, disjointness, and rational summation in Lean.  No cite
may directly assert the desired density formula.

## 2. The theorem that must ultimately land

The capstone-strength statement is deliberately displayed without a positivity guard:

```lean
theorem decidedSliceAt_all : ∀ n : ℕ, DecidedSliceAt n := by
  intro n
  exact decidedSliceAt_of_clusterRecursion n
```

Equivalently, after naming the package proposed below:

```lean
def UniformClusterPackage (n : ℕ) : Prop :=
  -- finite residue patterns; finite parametric NP/OM genres; soundness,
  -- disjoint exhaustion; exact rational weights; decreasing recursive calls
  ...

theorem decidedSliceAt_of_uniformClusterPackage
    (n : ℕ) (h : UniformClusterPackage n) : DecidedSliceAt n := ...

theorem uniformClusterPackage_all : ∀ n, UniformClusterPackage n := ...

theorem decidedSliceAt_all : ∀ n, DecidedSliceAt n :=
  fun n => decidedSliceAt_of_uniformClusterPackage n (uniformClusterPackage_all n)
```

This is stronger at `n=0` than the positive-degree body of
`UniformityStatementDecided`; `I02.lean:98` explicitly records that mismatch.  Node `A0G-Z`
must prove the zero slice directly: the unique monic degree-zero input is `1`, its actual type
is empty, and every other degree-zero `FactorizationType` gets density zero.  This must be
proved from the definitions—malformed degree-zero multisets containing zero coordinates make
it unsafe merely to say “there is one type.”

## 3. What is already uniform, and what is not

There are three different meanings of “generic” here; they must not be conflated.

### 3.1 Landed degree-generic mathematics

| function in the uniform proof | exact landed declaration | reusable content |
|---|---|---|
| convergence of decided counts | `GenuineDensity.decidedSeq_tendsto` (`Density/GenuineDensity.lean:286`) | every `n`, every type |
| drainage conversion, when supplied | `genuineDensity_eq_of_drainage` (`Density/GenuineDensity.lean:348`) | every `n`, conditional on `DrainageAt n` |
| multiplication of coprime residual blocks | `typeOf_of_residual_coprime` (`Density/TypeOfAlgebra.lean:131`) | arbitrary monic degree |
| translations and root scaling | `typeOf_shift` / `typeOf_scaleRoots` (`Density/TypeOfInvariance.lean:156,486`) | arbitrary polynomial degree |
| dv residual product law | `C133mh3.dvResPoly_mul_gen` (`ChapC/C133mh3.lean:1843`) | arbitrary polynomial and key frame |
| residual Bézout/splitting uniqueness | `C133mh10.dvResidualBezout`; `C133mh11.dv_oneSlope_split_unique` (`C133mh11.lean:182`) | arbitrary stage field and residual factors |
| complete Hensel split | `C133mh9.theoremA` (`C133mh9.lean:1470`), with statement at `C133mh14.lean:264` | exact factors, degrees, purity, heights and residuals at a supplied frame/direction |
| placement/frontier | `C133mh13.theoremC_placement`; `C133mh15.blockFrontier_of_context` | turns a supplied block context into a certified factor block |
| context split and later level operators | `C134csx.context_split`, `C134cfs.*`, `C134dv2g.*`, the `C136l2e*` engine | generic construction once the level/frame data are supplied |
| finite output-type menu | `IFC6.exists_uniform_coveringMenu` and `IFC6.menuLawAt_all` (`IFC6.lean:99,200`) | finitely many possible `FactorizationType` labels at a fixed degree |

The C.33 dissection boundary remains explicit: `C33Cite.exists_dvDissection` and
`fgmn_dvDissection_factor_eq` (`C33Cite.lean:76,85`) are the allowlisted source axioms inherited
by the frontier chain.  Theorem A itself is now proved by the complete dv-graded engine and is
not the missing uniform-density theorem.

`exists_uniform_coveringMenu` is useful only for the *output labels*.  It does not partition
coefficient space, attach a weight, or relate an `A1Cell` to a Newton/OM genre.  It therefore
cannot serve as `A0G-GC`.

### 3.2 Degree-generic proof patterns not yet exported generically

The cubic fixed-point argument has a uniform mathematical form:

1. partition the first residue layer;
2. decide simple residual factors immediately;
3. recurse on each repeated residual cluster;
4. split coprime residual labels by the Hensel engine;
5. isolate the all-roots-together rescaling loop;
6. solve a scalar fixed point and assemble the type convolution.

For a size-`m` rational cluster, scaling
`x = πy` divides the coefficients by `π^m, π^(m-1), ..., π`; its conditional Jacobian is

```text
q^(-κ_m),              κ_m = m(m-1)/2.
```

In the full monic degree-`m` box, choosing the common residue centre contributes `q^(1-m)`,
so the corresponding full-box loop has weight

```text
q^(1-T_m),             T_m = m(m+1)/2.
```

At `m=3` this is `q^-5`, exactly IFC7's step from `M` to `M+3`.  The future solver must
parameterize `m`, `κ_m`, the shift, and the base sequence; IFC7's
`density_ge_of_step` (`IFC7.lean:2031`) hard-codes `3` and `5` and is only the model.
Since `q≥2` and `κ_m>0` for `m≥2`, `1-q^-κ_m` is positive and every fixed-point denominator is
nonzero.  `m=0,1` are base cases, not uses of that division.

The mass/drainage move is also a reusable *pattern*, not a landed all-degree theorem.  Cubic
`totalMass_three` and `drainageAt_three` are at `H97b.lean:226,236`; the conditional general
reduction `H98.drainage_of_package` (`H98.lean:148`) still assumes
`∀ n, InductionPackage n`.  Therefore the uniform A0 proof should establish exact disjoint
recurrences for `decidedDensity` directly.  Reusing IFC7's five lower bounds plus a total-mass
squeeze at every degree would create an unnecessary dependency on the still-open uniform
drainage campaign.

### 3.3 Genuinely degree-specific landed material

The four cubic family genres and their concrete certificates are not generic declarations:

* IFC7's cube frames, residue tags, `Fin 3` translations, level-one bases, DBL peel, and the
  five step inequalities are degree three.
* `H97r1.shiftVec`, `H97r3.mtrunc`, and `H97r4.decidedAt_of_peel_decided` are hard-coded to
  `Fin 3`; IFC7's `shiftVec2` is separately hard-coded to `Fin 2`.
* `IFC6.CubicValueLaw`, `decidedSliceAt_three_of_values`, IFC7's sharp bounds, and
  `decidedSliceAt_three` (`IFC6.lean:232,245`; `IFC7.lean:2401`) enumerate five cubic types.
* `totalMass_three` is an `n=3` identity.  It is not a generic mass theorem with a hidden
  degree parameter.

Thus “level-1 certificate / recentred NP leaf / Hensel peel / π-rescale loop” is a generic
mechanism list, while the particular cubic families implementing those mechanisms are a
degree-three genre bank.

The precise IFC7 reuse map is:

| IFC7 section | cubic role | general-`n` disposition |
|---|---|---|
| §0 residue representatives | choose canonical lifts and prove congruence facts | generalize once for every finite residue field |
| §1 cube frames | `Fin 3` coefficient cylinders and translations | replace by A0G-CV; none of the `Fin 3` carrier is reusable as-is |
| §2 parameters | E/R/L/loop box sizes | replace numerals by schema fields and `m`, `κ_m`, `T_m` |
| §3 tags | five cubic residue tags and disjointness | replace by `ResiduePattern n` plus the parametric NP subgenres |
| §4 DBL | linear×quadratic Hensel peel | subsumed by A0G-PD/A0G-HE for arbitrary coprime residual products |
| §5 direct/scale | Eisenstein leaves, quadratic rescale, cubic loop | subsumed by face leaves, smaller cluster calls, and the unique size-`m` loop |
| §6 bases | finite level-one lower-bound families | replace by exact genre weights and child laws |
| §7 five steps | one inequality for each cubic output type | replace by a finite map-valued recurrence over all degree-`n` types |
| §8 fixed point | hard-coded shift `+3` and factor `q^-5` | export a solver parameterized by the shift and positive decay exponent |
| §9 mass squeeze | turn five lower bounds into equalities | do not generalize; exact disjoint recurrence avoids uniform drainage |
| §10 laws/socket | five `CubicValueLaw`s feed IFC6 | replace by `DecidedValueLaw` and the uniform slice socket |

Degree four already exhibits genres absent from the cubic bank: two simultaneous repeated
rational clusters `(1,2)^2`, a repeated non-rational closed point `(2,2)`, a size-four total
cluster `(1,4)`, and mixed products such as `(1,2)(2,1)`.  Inside a cluster there can also be
several fractional faces and repeated residual factors over extension residue fields.  These
are not additional ad hoc family names: the uniform classifier must generate them from face
lengths, residual degree/multiplicity patterns, and convolution.

## 4. The uniform genre classification to prove

### 4.1 First layer: residue-divisor patterns

For fixed `n`, define a residue pattern to be a finite multiset of pairs `(δ,m)` satisfying

```text
δ ≥ 1,  m ≥ 1,  and  Σ δ*m = n.
```

It describes closed residue points of degree `δ` occurring with multiplicity `m`.  There are
finitely many such patterns.  The number of choices is a polynomial in `q`, obtained from the
standard irreducible-polynomial census

```text
I_δ(q) = (1/δ) * Σ_{d|δ} μ(d) q^(δ/d),
```

with falling factorials and symmetry factors for repeated multiplicity labels.  A block with
`m=1` is already the unramified factor `(e,f)=(1,δ)`.  A block with `m>1` invokes the size-`m`
cluster law over residue cardinality `q^δ`; its output residue degrees are multiplied by `δ`.
Distinct closed points combine by multiset convolution, justified algebraically by
`typeOf_of_residual_coprime`.

All patterns except `(δ,m)=(1,n)` call strictly smaller cluster sizes.  That remaining pattern
is the single rational-cluster fixed point.

### 4.2 Inside one cluster: finitely many parametric NP/OM schemas

A genre is not one literal Newton polygon.  Slopes can have arbitrarily large numerators as
roots become arbitrarily close.  The needed finiteness statement is instead:

```lean
structure ClusterGenre (m : ℕ) where
  -- finite combinatorial data: face lengths, reduced slope denominators,
  -- residual degree/multiplicity patterns and recursive child positions
  schema : ...
  params : Fin r → ℕ
  constraints : PresburgerRegion r
  weight : RatFunc                         -- after summing params
  children : List (Σ k : Fin m, ClusterCall k)
  children_lt : ∀ c ∈ children, c.1 < m
  sound : ...

def FiniteGenreCoverAt (m : ℕ) : Prop :=
  ∃ G : Finset (ClusterGenre m),
    PairwiseDisjoint G ∧ ExhaustsClusterBox G ∧
    (∀ g ∈ G, ExactRationalWeight g) ∧
    (∀ g ∈ G, SoundTypeConvolution g)
```

For each lower fractional Newton face, write its reduced slope as `a/b`, `b≥2`.  Its horizontal
length is `b*d`; residual factors of degree `D` and multiplicity `μ` generate leaves `(b,D)`
when `μ=1`, or recursive clusters of size `μ` over `F_(q^D)` when `μ>1`.  Since
`μ≤d≤m/b<m`, recursion decreases.  Integral translations/recentrings either expose such a
face or enter the one π-rescale loop.  The unbounded numerator/gap variables must be summed as
geometric series.  This is the general classification argument that the degree-four engine is
experimentally implementing.

The stop-line theorem is:

```lean
theorem finiteGenreCoverAt_all : ∀ m : ℕ, FiniteGenreCoverAt m
```

Its proof must include all of the following, not merely OM factorization correctness:

1. every coefficient vector enters exactly one schema;
2. schemas are disjoint at every truncation level;
3. all non-loop recursive multiplicities are strictly smaller;
4. every unbounded depth variable has a positive decay exponent;
5. the symbolic sums are rational functions of `q` with denominators nonzero for `q≥2`;
6. every engine-produced factor and residual label transports to the claimed `typeOf`.

Without this theorem, a verified genre table at `n=4` remains a degree-four result.

## 5. Uniform recursion and reduction to `DecidedSliceAt`

Strong induction on cluster size constructs a finite law

```lean
def ClusterLaw (m δ : ℕ) :
    Finset (FactorizationType × RatFunc)
```

with `δ` denoting the residue closed-point degree.  The induction uses three classes of terms:

* shallow NP/OM leaves, decided by Theorem A and the type algebra;
* products of `ClusterLaw μ D` with `μ<m`;
* the diagonal rescale term `q^-κ_m * ClusterLaw m 1`.

For every output type `σ`, the last term is diagonal: it returns the same `σ`.  Hence

```text
L_m(σ) = shallow_m(σ) + q^-κ_m * FullMonic_m(σ; L_m),
```

and the only occurrence of the unknown at size `m` is its own all-at-one-rational-point cell.
Solving this scalar equation gives a rational function.  The residue-pattern convolution then
produces the full monic law at degree `n`.

The Lean-facing endpoint should target `decidedDensity`, not detour through
`genuineDensity`:

```lean
def DecidedValueLaw (n : ℕ) (σ : FactorizationType) : Prop :=
  ∃ num den : Polynomial ℚ, den ≠ 0 ∧
    ∀ (O : Type) [CommRing O] [IsDomain O] [IsDiscreteValuationRing O]
      [IsAdicComplete (IsLocalRing.maximalIdeal O) O]
      [Finite (IsLocalRing.ResidueField O)],
      den.eval ((residueCard O : ℕ) : ℚ) ≠ 0 ∧
      decidedDensity O n σ =
        ((num.eval ((residueCard O : ℕ) : ℚ) /
          den.eval ((residueCard O : ℕ) : ℚ) : ℚ) : ℝ)

theorem decidedSliceAt_of_valueLaws
    (n : ℕ) (h : ∀ σ, σ.degree = n → DecidedValueLaw n σ) :
    DecidedSliceAt n := h
```

The substantive equality comes from the disjoint finite-level genre recurrence, passage to
the limit by `decidedSeq_tendsto`, and uniqueness of the scalar fixed point.  An off-menu type
gets the rational function `0/1`, using the output menu and `typeOf_degree`.

## 6. Formalization nodes

Proposed files are new `ChapI/IFCG*` modules; the exact names below are candidate public names,
not claims that declarations already exist.  Sizings are source lines.  Every row has an
independent stop gate in the MU3/DWC style.

| node | min | candidate statement / exit condition | exact landed inputs | size | gate |
|---|---:|---|---|---:|---|
| A0G-Z zero slice | 45 | `decidedSliceAt_zero : DecidedSliceAt 0`; separate empty actual type from malformed degree-zero labels | `I02.DecidedSliceAt`; `monicPoly`; `typeOf_degree`; `DecidedAt` | 120–220 | values `1/1` or `0/1`; no positivity assumption |
| A0G-RP0 pattern carrier | 45 | define `ResiduePattern n`, positivity and `Σ δ*m=n` | `FactorizationType`; `Multiset`/`Finset` | 180–320 | zero coordinates unrepresentable |
| A0G-RP1 pattern enumeration | 60 | finite enumeration and completeness for every `n` | RP0; `IFC6.exists_uniform_coveringMenu` as precedent only | 250–450 | enumerate through `n=6`; prove generally |
| A0G-FF0 irreducible census | 60 | `I_δ(q)` counts monic irreducibles of degree `δ` | Mathlib finite-field factorization/counting | 300–500 | pin `δ=1,2,3,4` |
| A0G-FF1 configuration census | 60 | falling-factorial/symmetry count per pattern | FF0/RP0 | 300–500 | total `q^n`; quartic rows match §8 |
| A0G-CV0 generic boxes | 45 | define `shiftVecN`, `MBoxN`, `mtruncN` | models `H97r1.shiftVec`, `H97r3.mtrunc`, IFC7 §1 | 250–450 | no public `Fin 3` |
| A0G-CV1 box laws | 60 | translation/scale bijections, truncation surjectivity and cardinalities | CV0; `typeOf_shift`; `typeOf_scaleRoots` | 300–500 | recover n=2,3 identities |
| A0G-PD0 binary product | 60 | generic two-block decided/type injection | `typeOf_of_residual_coprime`; `C133mh3.dvResPoly_mul_gen`; model `H97r4.decidedAt_of_peel_decided` | 300–500 | recovers cubic peel; exact degrees |
| A0G-PD1 finite convolution | 45 | n-ary product and type-multiset convolution | PD0 | 220–380 | order-independent |
| A0G-NP0 face skeleton | 60 | finite face-length/denominator/residual skeletons at mass `m` | `dvSide*`, `IsDvPure`, `GenreDatum` (`H01.lean:38`) | 300–500 | `b*d≤m`; no height bound |
| A0G-NP1 gap parameters | 60 | canonical gap/slope parameters and Presburger constraints | NP0/CV1 | 300–500 | reconstruction and uniqueness |
| A0G-HE0 Hensel split | 45 | bind coprime residual splits to exact pure blocks | `C133mh9.theoremA`; `C133mh11.dv_oneSlope_split_unique`; `C134csx.context_split` | 220–400 | core plus inherited C.33 cite |
| A0G-HE1 placement/type | 60 | place blocks and identify type contribution | `C133mh13.theoremC_placement`; `C133mh15.blockFrontier_of_context`; PD0 | 280–450 | certify `(e,f)`, not only degrees |
| A0G-CH child decrease | 45 | repeated residual child has `μ<m` off loop | NP0 face-length identity | 180–320 | field-free arithmetic |
| **A0G-GC0 classifier** | **60** | assign canonical schema/parameters to every non-rescale input | CV1/NP0/NP1 | 300–500 | no bounded-precision premise |
| **A0G-GC1 exhaustion** | **60** | every input is classified or in exact π-rescale cell | GC0; dv dissection | 300–500 | **STOP LINE:** all characteristics |
| A0G-GC2 disjointness | 60 | unique schema/parameters modulo finite symmetries | GC0/NP1 | 300–500 | unique at every truncation |
| A0G-GC3 recursive soundness | 60 | children sound/smaller; rescale sole same-mass edge | HE1/CH/PD1 | 300–500 | non-loop graph has `<m` edges |
| A0G-GC4 cover package | 45 | `finiteGenreCoverAt_all` | GC0–GC3 | 180–300 | finite/exhaustive/disjoint/decreasing/sound |
| A0G-RW0 finite weights | 60 | exact q-power measure at fixed parameters | CV1/FF1/GC4 | 300–500 | finite-window census |
| A0G-RW1 geometric sums | 60 | sum unbounded gaps; rationality/convergence | RW0/NP1 | 300–500 | positive decay per variable |
| A0G-RW2 rational package | 45 | `Polynomial ℚ` numerator/denominator pairs | RW1 | 220–400 | polynomial and `q≥2` evaluation nonzero |
| A0G-RS rescale | 60 | generic loop, `κ_m`, full weight `q^(1-T_m)` | `typeOf_scaleRoots`; CV1 | 300–500 | pins m=2,3,4 |
| A0G-FP0 sequence recurrence | 60 | exact finite-level recurrence with boundary | `DecidedAt`; `decidedSeq`; GC4/RW2/RS | 300–500 | equality; no drainage |
| A0G-FP1 limit solver | 45 | pass to limit and solve diagonal fixed point | `decidedSeq_tendsto`; IFC7 precedent | 250–450 | uniqueness/positivity |
| A0G-CL0 cluster induction | 60 | `clusterLaw_one` by strong induction | PD1/GC4/RW2/FP1 | 300–500 | all calls `<m`; self-call only FP1 |
| A0G-CL1 residue extension | 45 | `clusterLaw_all` via `q↦q^δ`, `f↦δf` | CL0; stage-field/base-change laws | 250–450 | denominators/types scale |
| A0G-AS0 affine assembly | 60 | convolve over residue patterns | RP1/FF1/PD1/CL1 | 300–500 | masses sum to one |
| A0G-AS1 value laws | 45 | `DecidedValueLaw n σ`; off-menu `0/1` | AS0; `IFC6.exists_uniform_coveringMenu`; `typeOf_degree` | 250–450 | law for every label |
| A0G-DS slice socket | 30 | `decidedSliceAt_of_clusterRecursion` | `I02.DecidedSliceAt`; AS1 | 120–220 | both denominator obligations |
| A0G-ALL capstone | 15 | `decidedSliceAt_all : ∀ n, DecidedSliceAt n` | Z/DS | 20–50 | no `0<n` guard |

Critical path:

```text
RP → FF ─┐
CV → NP → HE → CH → GC → RW ─┐
CV ───────────────→ RS ──────┼→ FP → CL → AS → DS → ALL
PD ───────────────────────────┘
Z ────────────────────────────────────────────────→ ALL
```

Estimated new Lean source: 8,000–14,000 lines in **31 separately gated nodes**.  GC0–GC4 is
the research stop-line; **GC1 exhaustion** is the single highest-risk node.  A campaign that
begins downstream of GC4 is packaging a conjectural classifier.

## 7. Gates and falsifiers

1. **Wild-prime gate:** every theorem is instantiated at `(n,q)=(4,2)` and `(5,5)` or another
   case with residue characteristic dividing the degree.  A tame-only imported premise fails.
2. **No pointwise-termination substitution:** the genre-cover theorem must expose one finite
   schema list before an input coefficient vector is supplied.
3. **No bounded-depth substitution:** arbitrarily large slope numerators are represented by
   parameters and summed; no fixed precision cap occurs in the theorem.
4. **Disjointness tooth:** every finite truncation class has a unique genre and unique parameter
   tuple, up to explicitly divided finite symmetries.
5. **Decrease tooth:** remove the rescale genre and print a proof that every recursive child has
   cluster mass `<m`.
6. **Loop tooth:** the only same-mass edge is the all-at-one rational cluster, with coefficient
   exactly `q^-κ_m` conditionally and `q^(1-T_m)` in the full box.
7. **Faithfulness footprint:** Theorem A/frontier consumers print only Lean core plus the existing
   C.33 dissection cite.  Any new literature axiom gets its own hypothesis-by-hypothesis ledger.
8. **Density target tooth:** final value laws mention `decidedDensity`; no hidden general
   `DrainageAt` premise enters through a genuine-density conversion.
9. **Denominator gate:** prove both the polynomial denominator is nonzero and its evaluation at
   every residue cardinality is nonzero, exactly as `DecidedSliceAt` demands.
10. **Examples-first gate:** n=2 and n=3 reproduce landed values; n=4 reproduces all eleven rows
    in §8.  These are regression tests only.

Per-file verification only, from `leanfinal/`, by
`lake env lean Uniformity/ChapI/<new-file>.lean`.  Never use a bare `lake build` for this
campaign.

## 8. Quartic genre table: executable reality check

The quartic residue layer has exactly eleven divisor patterns.  Here `(δ,m)` means one closed
point of degree `δ` with multiplicity `m`, and
`I₂=(q²-q)/2`, `I₃=(q³-q)/3`, `I₄=(q⁴-q²)/4`.

| residue genre | number of monic residue polynomials | recursive content |
|---|---:|---|
| `(1,1)^4` | `C(q,4)` | four fixed unramified linear leaves |
| `(1,1)^2(1,2)` | `q C(q-1,2)` | one rational size-2 cluster + two simple rational leaves |
| `(1,1)^2(2,1)` | `C(q,2) I₂` | two simple rational leaves + one quadratic leaf |
| `(1,1)(1,3)` | `q(q-1)` | one rational size-3 cluster + one simple rational leaf |
| `(1,1)(3,1)` | `q I₃` | one rational leaf + one cubic leaf |
| `(1,2)^2` | `C(q,2)` | two distinct rational size-2 clusters |
| `(1,2)(2,1)` | `q I₂` | rational size-2 cluster + quadratic leaf |
| `(1,4)` | `q` | rational size-4 cluster; contains the quartic self-loop |
| `(2,1)^2` | `C(I₂,2)` | two distinct quadratic leaves |
| `(2,2)` | `I₂` | size-2 cluster over `F_(q²)` |
| `(4,1)` | `I₄` | one quartic leaf |

The counts sum to `q⁴`.  Convolving `m=1` leaves with the cluster laws `L_m^(δ)` gives the
eleven output types and the following exact monic densities.  Set

```text
Φ₂=q²+q+1,   Φ₃=q⁶+q³+1,   Φ₅=q⁴+q³+q²+q+1.
```

| output type | density |
|---|---|
| `(1,1)^4` | `q⁶(q⁸-2q⁷+q⁶+2q⁵-q⁴+2q³+q²-2q+1)/(24(q+1)²Φ₂Φ₃Φ₅)` |
| `(1,1)^2(1,2)` | `q⁶(q⁸+2q⁷+3q⁶+2q⁵+q⁴+2q³+3q²+2q+1)/(4(q+1)²Φ₂Φ₃Φ₅)` |
| `(1,1)^2(2,1)` | `q³(q¹⁰+q⁸+3q⁷+q⁶+q⁵+3q⁴+2q³+2q²+2q+1)/(2(q+1)²Φ₂Φ₃Φ₅)` |
| `(1,1)(1,3)` | `q⁶(q+1)²(q²-q+1)/(3Φ₃Φ₅)` |
| `(1,1)(3,1)` | `q(q⁹+2q⁷+q⁶+q⁵+2q⁴+2q³+q²+q+1)/(Φ₂Φ₃Φ₅)` |
| `(1,2)^2` | `q⁶(q⁶+q⁵-2q³+q+1)/(8(q+1)²(q²+1)Φ₂Φ₃)` |
| `(1,2)(2,1)` | `q³(q⁶+q⁵+q⁴+q+1)/(2(q+1)²Φ₂Φ₃)` |
| `(1,4)` | `q⁶Φ₅/(4(q²+1)Φ₂Φ₃)` |
| `(2,1)^2` | `q²/(2(q+1)²Φ₂)` |
| `(2,2)` | `q²(q⁷+q⁶+q⁴+q³+2q²+q+1)/(2(q+1)(q²+1)Φ₂Φ₃)` |
| `(4,1)` | `(q⁶+q⁵+q³+q+1)/((q+1)Φ₂Φ₃)` |

`verification/a0g_quartic_cert.py --fast` performs 32 checks and exits with zero failures:

* symbolic sum `=1`, expected root count `q/(q+1)`, positivity, and the q=2,3 anchors;
* exhaustive factorization of every monic quartic over `F_p` for `p=2,3,5,7`, matching all
  eleven residue counts;
* an independently coded eleven-row convolution, termwise equal both to the displayed forms
  and to `om_density_engine.alpha_full(4)`;
* a decorrelated PARI/GP factorization sample for every output type at `p=2,3,5,7`, with every
  row inside its four-sigma gate and no stray shapes.

This validates the residue-pattern/convolution architecture at degree four.  The cluster laws
used in the repeated rows still rely on the computational Newton recursion; only `A0G-GC` can
promote that recursion to all degrees.

## 9. Literature boundary

Primary sources used only at their stated scope:

1. J. Fernández, J. Guàrdia, J. Montes, E. Nart, *Residual ideals of MacLane valuations*,
   Theorem 6.6, arXiv:1305.0775v3: https://arxiv.org/abs/1305.0775.
2. M. Alberich-Carramiñana, J. Guàrdia, E. Nart, A. Poteaux, J. Roé, M. Weimann,
   *Polynomial factorization over henselian fields*, especially the hypotheses preceding its
   factorization algorithm and Theorem 6.12, arXiv:2207.02139:
   https://arxiv.org/abs/2207.02139.

The first already underwrites the narrow C.33 dissection genre.  The second is a warning
against overclaiming termination at wild primes, not a license for `decidedSliceAt_all`.

## [A0G2 2026-08-28] Node A0G-CV LANDED + a §3.3 correction

`IFCG1.lean` (527 lines, 28/28 Lean-core) — the full CV bank with the n = 2, 3
definitional recoveries. §3.3's line "none of the `Fin 3` carrier is reusable as-is"
is CORRECTED: a generic recentring was already landed at H123a
(`Induction.recentreVec`/`recentreClass`), unlisted in the A0G-CV row's inputs;
`shiftVecN` consumes it as a reducible wrapper. Downstream nodes should check H123a's
bank before re-deriving.

## [A0G3 2026-08-28] Node A0G-PD LANDED + an input correction

`IFCG2.lean` (553 lines, 31/31 Lean-core). The row's LISTED inputs
(`typeOf_of_residual_coprime`, `dvResPoly_mul_gen`) were the wrong tools at the
`Coeff`/`DecidedAt` class level — the right binary engine was already landed:
chapter H's `Induction.decidedAt_mulClass` (H.103, the 2026-08-16 general-induction
unit), fully degree-generic. The n-ary fold, the H97r4 bridge, the byte-identical cubic
recovery, and order-independence are the file's genuine content. Downstream nodes
(A0G-CL especially) should treat H.103/H.104 as the product engine.
