# SYNTHESIS PASS 9 (2026-08-08 campaign, wallclock 2026-08-04) — the closure-program pattern hunt: ONE graded ledger, ONE window calculus, the class-pin kill genre, and the twisted-character layer

Charge: the /goal's standing synthesis clause ("find big unifying ideas and cut down the
proof"); successor to `docs/SYNTHESIS_PASS8_2026-08-08.md` (which mapped the wave-15/17
ITER-LAW/FGMN window and ran BEFORE the closure program). Sources read for this pass:
PROJECT_STATE's three 2026-08-04 brackets; the ledger tail of
`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md` from [RMG] down through the evening
checkpoint (f7a682c/b6e66d9); and the closure-program corpus in full at
header/statement/claims-table depth, proofs selectively: RMENGINE ([RMG], ACCEPTED),
WMULTDCX (PID†, attempt grade 0/2), EMPTY_PROOF (E arc 1/2), SURV_PROOF (S arc 0/2),
SURVDISJ_PROOF (SD arc 1/2), SURVK2_PROOF (K2 arc 0/1), SDYJ_PROOF (SDYJ arc 0/1),
SURVDISJ_CONSTRUCT, GRT_WELD_BLUEPRINT + GRTWELD_PROBE + GRTW2_PROOF (W2 arc 0/2).

**GENRE: PATTERN PASS.** Nothing below is composed, proved, or retired in place. Every
"subsumes" claim is a design claim about a FUTURE consolidation unit; every accepted or
committed note's text is untouched; all grade caps ride (the W arc at 0/2 CLEAN binds
every Q2 grade, and the five-plus Codex legs of the Thursday queue are unaffected).

HEADLINE. The closure program's seven proof notes re-derive, in per-note dialects, what
turns out to be TWO shared substrates plus ONE shared kill pattern: (1) every additive
bookkeeping identity in the E/SD/SDY/W chain — E-L1, SD-L2, (★), W-D4, W-D5's floor,
L-MONO, and SDY-L2's additive spine — is a weight-functional specialization of ONE
graded path ledger over the [RMG] S2.2 tree (S2 below: LEDGER-Λ, the pass's
highest-value unification, subsumption count 7 + 3 pool-formula sites, risk LOW); (2)
the drain-window normal form K2-L1 is the level-0 slice of a NESTED per-level window
calculus whose g = 1 degeneration is exactly SDY-L1's forced chain — two notes, not one,
replace the four notes' path bookkeeping (S3); (3) the A = {1} class-pin kill (K2-L8)
generalizes to a PIN-CENSUS genre that has one more corner half-dead already and feeds
the W2 support-triviality analysis, but the remaining (K2-RES) strata are pin-IMMUNE and
die (if at all) by budget/case-table arithmetic (S4); (4) the weld-corpus grading unity
is REAL but partial: the carry algebra D and J-C's fibred carrier are both twisted group
algebras over the same carry cocycle, L-ZMULT is that algebra's structure morphism, and
the (ξ,w) covariance composes by the [ILN] shift-descent — yielding one concrete
probe-first unit (W2-COB) at W2-OPEN-1's ratio-shaped half — while "J-C nearly
automatic" is REFUTED (S5). The consolidation harvest (S6) is a 9-item
retirement/extraction list, headed by: the (SURV-RES) box's two sub-forms are now
theorems on their loci and the Q2 residual boxes live in four places — one FRONTIER
display of record is owed.

---

## S1 — WHAT THE WINDOW ADDED (compressed, for re-grounding)

Since pass 8: Q1 CLOSED ([RMG] accepted — the four (RM-m) clauses level-generically on
T-scope(m) = d₀ = 1 ∨ (e₀ ≥ 2 ∧ eᵢ ≥ 2); COR-A applied). Q2 reduced and then largely
closed: row 11c refuted → PID† (attempt grade); LAW-DCX-(m+1) reduced to
(EMPTY) + (SURV); (EMPTY-(m+1)) proved g₀/d₀-free; (SURV)(i) reduced to slot survival
(S-THM) with the obstruction isolated as same-slot residue cancellation; (SURV-DISJ)
proved at g₀ = 1 ∧ d₀ = 1 via the E-weighted mass ledger ((NEP) discharged to theorem);
(SURV-K2) proved at g₀ = 2 ∧ REG via the window calculus + the four-form dichotomy, with
the A = {1} threat retired CLASS-EMPTY (K2-L8); (SD-YJ′) proved via the digit-valuation
floor + multiplicative ledger, extending SD-THM-A to EVERY d₀. Open Q2 mathematics =
exactly the (K2-RES) strata (glued (a), out-of-list (b), placement (e), the A = {0}
corner) + g₀ ≥ 3. Q4 opened: weld blueprint (J-A/J-B/J-C/J-D, W-0..W-12), the W-2 probe
(byte-equality falsified, failure localized), W-2 composed in three-clause form with the
(ξ,w) closed form and ψ-transport (96 → 0), boxes W2-BOX-1 + W2-OPEN-1. Arc counters at
the checkpoint: W 0/2 · E 1/2 · S 0/2 · SD 1/2 · K2 0/1 · W2 0/2 · SDYJ 0/1.

---

## S2 — SEED 1 VERDICT: **ACCEPT, strengthened.** The two ledgers ARE two faces of one graded ledger — and there are FIVE faces, not two

**The observation.** Each chain note multiplies the same raw per-level identity — W-D3's
LED_l, itself edge-by-edge bookkeeping of the [RMG] S2.2 child inventory — by its own
weight vector and telescopes:

* E-L1 uses W_l := 1/(P_l⋯P_{m−2}) (promotion-NULL: W_{l+1} − P_l·W_l = 0) → the
  π_{m−1} identity;
* SD-L2 uses E_l := e₀⋯e_{l−1} (promotion loss E_l·(g_{l−1} − 1) ≥ 0, vanishing at
  g = 1) → the mass ledger and (★);
* W-D4 uses the top indicator δ_{l,m−1} → the depth threshold;
* W-D5/L-MONO use the line scales Γ_l (promotion gain the EXACT quantum
  h_{i+1}·∏_{i+1<t<m}e_t, correction-main edges line-NULL by L-LINE(ii)) → the
  line floor;
* SDY-L2 is the SAME telescope on the coordinate the others ignore: the coefficient
  valuation w₀(b) is the π-EXPONENT — the "level −1" coordinate of the extended
  exponent vector — untouched by promotions/exits ([RMG] S2.2: b rides unchanged) and
  moved only by L-NORM split edges, exactly (+w₀(c_d), L-child), ≥ +1 (π-junk child),
  ≥ +0 (Φ₀Y-child). The line functional is the mixed face that ties them:
  line(C) = E_m·w₀(b_C) + Σ_l j_l·Γ_l.

**The unification (candidate statement, for a compose unit — review-owed).**

    LEDGER-Λ (the graded path ledger over the [RMG] tree).  On any row of
    the class, extend the exponent map to J(C) := (w₀(b_C); j₀, …, j_{m−1})
    ∈ ℤ^{1+m}.  (A) [exact lattice ledger] Every S2.2 edge type carries a
    displayed quantum vector Δ ∈ ℤ^{1+m} — ONE table, assembled fresh from
    the S2.2 inventory: promotion/exit rows exact; correction rows exact in
    the level coordinates with the L-child exact and the junk children
    bounded below in the (−1)-coordinate (π-child +1; Y-child +1 at level 0,
    ≥ 0 at coordinate −1).  For every weight functional λ ∈ ℚ^{1+m}, λ·J
    telescopes along any root-to-terminal path: λ·J(C) = λ·J(seed) +
    arrival terms + Σ_edges λ·Δ(edge), with equality on junk-free paths and
    the displayed inequalities otherwise.  (B) [the five faces] λ = (0; W_l)
    is E-L1; λ = (0; E_l) is SD-L2 (whence (★)); λ = (0; δ_{l,m−1}) is
    W-D4; λ = (1; 0⃗) is SDY-L2's additive spine; λ = (E_m; Γ_l) is the
    line calculus (L-MONO, W-D5's floor), with correction-main edges
    line-null.  (C) [residue face] On junk-free paths the terminal residue
    is the product of the seed residue and the consumed digit residues
    (L-NORM(a) chained) — the substrate S-L1 and K2-L4 instantiate.

**What it subsumes (count: 7 displays + 3 pool sites).** E-L1, SD-L2, (★)'s telescope,
W-D4 (re-derived), W-D5's floor leg, L-MONO, SDY-L2's additive spine — across
EMPTY/SURVDISJ/WMULTDCX/RMENGINE/SDYJ. Foldable into the same note: the POOL-UNROLL
extraction (S6 item 7) — the carry chain c_l = (t_l + c_{l−1} − ρ_l)/e_{l−1} with the
line value β̂ = λ̂ + Σ c_l·q_l currently proved/recomputed in three places (E-L4/E-L5,
S-L1, the SDYJ Y-r1 de-tautologized runner formula).

**What it does NOT subsume (honest fences).** K2-L1's window normal form (event-ORDER
content, not a ledger face — see S3); the residue ALGEBRA (S-L1's nonvanishing, the
K2-THM-A dichotomy — the ledger only carries the product structure); the integrality
arguments (SDY-L1's (INV) chain, E-L5's integer floor — the (INT-FLOOR) move, S6 item
8); and every hypothesis-dependent step (the ledgers' consumers, not the ledgers).

**Proof debt: LOW.** The theorem is proved three times over in the corpus already (E-L1
and SD-L2 are both literally "multiply LED_l by weights and sum"; the quanta are W-D3's
proof + L-LINE + L-NORM). The unit's real work is (i) assembling the Δ-table FRESH from
[RMG] S2.2 (extraction-decorrelation law: no verbatim import from the consuming notes;
the non-textual leg = a walk-replay runner computing all five faces simultaneously on
both committed rosters), and (ii) the byte-fidelity audit that each face's corollary
statement matches its committed counterpart EXACTLY (any mismatch is a finding, not a
silent fix).

**Cut-down estimate.** The definitive Q2 write-up loses ~5 proof sections (the five
telescopes) plus the triplicated pool formula; future units (K2-CLOSE, g₀ ≥ 3, the
glued case table) consume one lemma instead of five; the eventual Lean transcription
gets ONE ledger theorem + a quantum table instead of five bespoke inductions.

**Risk.** LOW. The one subtlety is keeping the equality lattice (exponent coordinates)
typed separately from the inequality coordinate (valuation slack at junk edges) — a
statement-hygiene matter, exactly the kind hostile round 1 catches. Priced unit: S7.U1.

---

## S3 — SEED 2 VERDICT: **REFINE.** K2-L1 is the level-0 slice of a NESTED window calculus; two notes (not one) replace the four notes' path bookkeeping

**Verify.** K2-L1's normal form is genuinely canonical AT LEVEL 0: arrival-driven
maximal runs, complete drains (close below P₀), interior blindness to drain content
through everything except the promotion count, full realization of legal junk-free
substitutions. K2-L2/L3 (budget ≤ 2g₀ − 1; the complete g₀ = 2 inventory) live on it,
and the whole K2-THM-A dichotomy is a statement about its drain inventory.

**Refine.** The canonical object for EVERY path analysis is the NESTED decomposition:
the S2.2 walk fires at the FIRST overflowing level (E1-priority), so level-l events
enclose complete lower-level window trees — the walk is a nested-radix odometer. Two
consequences the corpus already realizes without naming:

* at g_{l−1} = 1 the level-(l−1) window has a UNIQUE legal drain (the t-freedom
  t ∈ [0, g_{l−1}) collapses) — SDY-L1's forced v-chain IS the all-g = 1 degeneration
  of the window calculus, and W-D2(iii)'s unique principal d-cell is its seed-side
  shadow;
* the g₀ ≥ 3 program's entire substrate is the level-0 inventory at budget
  n ≤ 2g₀ − 1 — the ONLY g₀-specific ingredient; the pillar failures (count −2
  drains, order multiplicity at n = 5) are inventory facts, and the T1-RES door
  question is a window-reachability question (S4).

**Refute (the seed's strong form).** "A single WINDOW CALCULUS note replaces four notes'
path bookkeeping" — NO: most of what E/S/SD re-derive is LEDGER content (S2), not window
content. The honest split: LEDGER-Λ (the additive/valuation/residue faces) + WINDOW-CALC
(the event-order normal form) together are the complete path-analysis substrate; each
alone is roughly half.

**Proof debt: M/L.** The nesting definition is the hard step: a level-l correction dumps
into levels < l, spawning lower windows INSIDE the level-l run, and promotions out of
level l−1 arrive at level l mid-run — "maximal run" needs the nested (odometer)
formulation, not K2-L1(a)'s flat one. Probe-first: generalize the K2-XWALK recording
walk to emit the nested decomposition and check it reproduces both committed walks
byte-exactly before composing. Cut-down: K2 S1–S3 and SDYJ S2's chain half become
instances; the g₀ ≥ 3 instrument gets its substrate for free. Priced unit: S7.U2.

---

## S4 — SEED 3 VERDICT: **ACCEPT the genre, with a per-box table.** The pin kills corners of the CLASS; budgets and case tables kill corners of the COMBINATORICS — and each open box now has a named kill route

K2-L8's anatomy: the A = {1} corner died because a CLASS clause ([ILN] S0.1's
ψ_j(0) ≠ 0) plus a two-line arithmetic fact (irreducibility at deg ≥ 2) made the boxed
tower data unrealizable. The generalizable content is not "the pin kills boxes" but:
**every box over tower data must be intersected with the class pin's arithmetic BEFORE
being graded machine-dark** — now effectively corpus practice, worth making law + one
cheap standing artifact (S7.U3). The per-box audit:

| box | pin clause bearing on it | verdict |
|---|---|---|
| (K2-RES)(a) glued systems | none — stratum REALIZED (29/332 rows) | pin-IMMUNE; kill route = the finite glued case table over the enlarged identity set (the note's own named next unit; folded into S7.U4) |
| (K2-RES)(b) out-of-list forms | none directly | pin-immune but plausibly BUDGET-dead: the candidate books need multi-window placements the n ≤ 3 inventory may forbid class-wide — a WINDOW-CALC corollary candidate (K2-L9), S/M, folded into S7.U4 |
| (K2-RES)(e) placement pair | none | pin-immune; disfavored by L-LINE(i) quanta but "forced-carry rows" keep it open; same K2-L9 genre (window-chain placement arithmetic), folded into S7.U4 |
| A = {0} corner (ψ₀_1 = 0) | HALF-dead by pin-adjacent arithmetic already: char 2 killed by Artin–Schreier (K2-L8's Reading); odd char realizable | promote the displayed singleton-safety arithmetic to a LEMMA (S — it is probe-verified, "displayed, not composed"); then the corner exits the open set with no new mathematics. Folded into S7.U4 |
| (K2-RES)(d) g₀ ≥ 3 doors | pin forces 0 ∈ A at every g₀ (K2-L8 is degree-general); the −2 window needs ψ₀_2 ≠ 0 — a SUPPORT-PATTERN availability question per (q, g); note the construct unit found small fields work FOR the adversary (ψ̄₀ = ψ̄₂³ automatic over F₂/F₄) | the decisive object is the T1-RES fork: EITHER the construction reaches parked+arrival ≥ 10 (door opens, counterexample candidate lives) OR a window-cap lemma proves ≤ 9 (door dies class-wide at n = 5). One unit, two exits, both findings — the cleanest open question in Q2 (S7.U2's first consumer) |
| W2-BOX-1 (mod-wrap boundary) | none — a character/support phenomenon, not tower-data realizability | pin-immune; the honest route is the ϑ-exponent tracking inside HR-REC (already exact); NOTE the cross-feed: support-triviality (ξ^j constant on supp ψ) is the SAME support-pattern arithmetic as the pin census — one table serves both (S7.U3) |
| W2-OPEN-1 | none | not a corner — an open lemma; see S5 |

**The one new artifact worth minting (S7.U3, priced S): PIN-CENSUS** — the exhaustive
support-pattern availability table for ψ over small residue fields (q ≤ 25, g ≤ 4:
which coefficient-support patterns admit monic irreducibles with ψ(0) ≠ 0), plus the
two general lemmas already in hand (Artin–Schreier at char 2 for y² + c; the cube-map
census for y³ + c), stated once. Consumers: any future box's realizability check
(lookup, not probe), the g₀ ≥ 3 door arithmetic, W2's support-triviality analysis.

---

## S5 — SEED 4 VERDICT: **PARTIAL-ACCEPT.** The twisted-character layer is real and worth naming; "J-C nearly automatic" is REFUTED

**What is genuinely ONE object (verified against the texts).**

1. The carry algebra D = F[u]/(u^e − z̄) IS the twisted group algebra F^c[ℤ/e] with
   cocycle c(a,b) = z̄^{⌊(a+b)/e⌋} — the carry cocycle. J-C(ii)'s fibred carrier
   (⊕_γ K_{m+1}·u_γ, u_γu_{γ′} = c_{m+1}(γ,γ′)·u_{γ+γ′}) is BY CONSTRUCTION the
   twisted group algebra K_{m+1}^{c_T}[ℤ/E_{m+1}]. GRB D-REAL says the last-read
   layer of gr IS the first; J-C(ii) asks whether 𝒜(T) ⊂ deg-0 gr(w_{m+1}) realizes
   the second.
2. L-ZMULT ([RMG]'s kernel) is EXACTLY the structure morphism of this algebra: the
   read monomial 𝔷 is multiplicative up to the single top carry z_m^{ε̃},
   ε̃ = ⌊(s_m(β)+s_m(β′))/e_{m−1}⌋ — the same cocycle, appearing as (i) L-ZMULT's ε̃,
   (ii) the (C-EPS) box clause, (iii) [ILN]'s c_{n+1} constants, (iv) W2-T3E's
   overflow channel (s_j(γ) + s_j(γ′) ≥ e_{j−1} is when twisted bytes enter a read).
3. GRTW2 §3.4 remark 1 (verified verbatim): the covariance χ-chain composes by the
   SAME descent operator ℓ′_{m−1} − ℓ_{m−1}w_{m−1}Φ_{m−1} as the [ILN] shift
   recursion — the (ξ,w) gauge and the cocycle share their transition structure. And
   §4.3's ℓ-orbit acts on residuals exactly by covariance characters — so the lift
   freedom, the covariance, and the cocycle all live in ONE character group
   𝒵 := ⟨z-monomials⟩ ⊂ K_{m+1}^×.

**The concrete unit this buys (probe-first, S design + M probe — S7.U5): W2-COB.**
State the lift change (harness ↔ transported classifier keys, and the ℓ-orbit) as a
𝒵-valued 1-cochain on the anchor window; conjecture: c_T transforms by its COBOUNDARY,
so every cocycle-RATIO-shaped battery member (K7 the law; the cocycle half of K6) is
lift-invariant — which would prove the ratio-shaped half of W2-OPEN-1 rather than
instance-confirm it (the mechanism is already visible in GRTW2 §5.4: "a common
character can cancel in the cocycle ratio"). Sealed prereg on the 16-tower roster;
mutation controls = the garbled-character teeth (MT-TW genre).

**REFUTED (the seed's strong form).** J-C does NOT become nearly automatic: OB-4
(rank-one independence, the Thm-4.2 analogue) and OB-6 (harness-val = TC-read) are
gr(w)-content the torsor/coboundary language does not touch, and the NOT-CYCLIC
adjudication (22/28 towers, byte-frozen) already killed any one-object shortcut at
composite stages. The value of the layer is (i) W2-COB, (ii) a definitions note worth
half a page inside the next weld unit — naming F^c[ℤ/e] / K^{c_T}[ℤ/E] once so J-C,
DIG, and L-ZMULT stop paraphrasing each other — and (iii) the right frame for the
value-layer boundary: K5/K12 are VALUE-shaped (not ratio-shaped) and genuinely need
the transported-coordinate bookkeeping; W2-BOX-1 shows the character action is not
globally scalar. Those stay hard; display them as the honest remainder.

**Also filed here (fresh eyes, seed 6 adjacent):** SD-L6 already states survival ⟺ a
signed residue sum ≠ 0, and K2-THM-A's dichotomy is a guaranteed-unmatched-term
argument. The g₀ ≥ 3 charge should therefore be STATED as: "does the window inventory
admit a sign-reversing involution whose fixed points survive every residue
assignment?" — the mechanism-shaped form of the open question, replacing "may need a
different mechanism or be FALSE". This costs one sentence in the next K2-family charge
and gives the g₀ ≥ 3 instrument its preregistration target.

---

## S6 — SEED 5: THE CONSOLIDATION LIST (retire / annotate / extract; nothing executed here)

1. **(SURV-RES) box (SURV_PROOF S7): both sub-forms are now theorems on their loci** —
   (NEP) discharged to theorem at g₀ = 1 ∧ d₀ = 1 (SD-THM-A), extended to every d₀
   (SDY-THM); (SURV-DISJ) proved on (g₀ = 1) ∪ (g₀ = 2 ∧ REG). The box is the
   corpus's stalest live display. ACTION: dated supersession bracket at the next
   S-arc round (statements byte-unchanged; the m ≥ 3 junk-forcing caveat and the
   g₀ ≥ 2 kill genres stay displayed as the box's surviving content).
2. **Four residual-box homes → ONE frontier display.** The Q2 open set currently
   lives in SURVDISJ S6 + SURVK2 S7 + SDYJ S6 + CONSTRUCT T1-RES/T2 (plus the SURV
   box above). The SD-r1/N-4 stale-header incident is the precedent for drift.
   ACTION: a Q2-FRONTIER block of record in PROJECT_STATE at the next fold, each
   note's box annotated "refined by/authority" (cross-file dated brackets only).
3. **WMULTDCX row 11c / (K2-RES)(c)**: already superseded in place — no action;
   listed to mark the supersede-at-refutation convention as the working pattern.
4. **S-L4(iii)'s m ≥ 3 forcing box at g₀ = 1**: candidate subsumption by
   SDY-L2/L3 (junk-free forcing content now flows through the valuation ledger at
   every d₀). VERIFY-THEN-BRACKET at the next S round — NOT claimed here (the
   g₀ ≥ 2 half of the box definitely survives).
5. **(NEP)-as-hypothesis citation sites** (S-THM(c) and consumers): annotate
   discharged-on-locus at the next S round.
6. **The E-L3 = W-D4 pattern is the model**: EMPTY consumed W-D4 verbatim instead of
   re-proving — the convention LEDGER-Λ's corollary table generalizes. Keep.
7. **POOL-UNROLL extraction**: the carry chain + β̂ = λ̂ + Σ c_l·q_l formula proved or
   recomputed in E-L4/E-L5, S-L1, and the SDYJ v2 runner — one shared lemma (folds
   into S7.U1's note).
8. **Micro-move extractions** (two-line lemmas, fold into S7.U1): the mixed-radix
   telescope Σ(P_l − 1)W_l = W_i − W₀ / Σ(e_l − 1)E_l = E_i − 1 (used in E-L2, SD-L1,
   E-L4); (INT-FLOOR) "rational bound + integrality ⇒ +1" (SDY-L1's (INV), E-L5's
   floor); the forced-chain-at-g = 1 move (SDY-L1, W-D2(iii), S-L1) — the last
   becomes WINDOW-CALC's g = 1 degeneration.
9. **ENGINE-COVERAGE census (honesty infrastructure, S).** Two capability walls were
   exposed by construction units (K2-ENG: deg ≤ 2 pick — the false "g₀ ≥ 3 cost
   wall"; the d₀ ≥ 2 quadratic-Φ₀ constructor). ACTION: one committed table of what
   (g₀, d₀, deg ψ, char) the committed engines can construct, cited by every future
   "machine-dark" grade; the two queued cross-note errata (SURVK2 instrument wording;
   the construct-note X-child bracket — already applied at Y-r1) close under it.
   NOT retired: per-note machine legs (decorrelation is deliberate, never redundancy).

---

## S7 — THE PRICED QUEUE (units this pass charters; all compose-units carry the standing two-commit seal + hostile-arc discipline)

* **U1 — LEDGER-Λ, the graded path ledger** (compose, M; S2). THE HIGHEST-VALUE
  UNIFICATION. Deliverables: the fresh-from-[RMG]-S2.2 quantum table Δ ∈ ℤ^{1+m} per
  edge type (+ residue and valuation-slack columns); theorem LED-Λ(A)/(B)/(C); the
  five-face corollary table with a byte-fidelity audit against E-L1 / SD-L2+(★) /
  W-D4 / W-D5+L-MONO / SDY-L2; POOL-UNROLL + the S6.8 micro-moves as displayed
  lemmas. Machine leg: one walk-replay runner computing ALL faces on both committed
  rosters, mutation controls (drop the l = 0 term — the CTRL-TEL0 precedent; corrupt
  one Δ entry). Falsifier: any face diverging from its committed counter. Fences:
  consumption-only (no accepted/committed statement edited); grade = attempt until
  its own arc runs; caps ride.
* **U2 — WINDOW-CALC, the nested window calculus** (probe → compose, M/L; S3).
  Probe-first: extend K2-XWALK to emit the nested per-level decomposition,
  byte-exact against both committed walks; then compose K2-L1/L2 at every level +
  the g = 1 degeneration (= SDY-L1's chain) + the budget table. First consumer: the
  T1-RES fork (S4's g₀ ≥ 3 row) — window-cap lemma vs construction, one unit, two
  exits, both findings.
* **U3 — PIN-CENSUS** (S; S4). The support-pattern availability table (q ≤ 25,
  g ≤ 4, exhaustive) + the two general lemmas; corpus-law rider: every future box
  intersects the class pin before a machine-dark grade. Feeds U4, the g₀ ≥ 3 door,
  and W2 support-triviality.
* **U4 — K2-CLOSE** (compose, M; S4). (SURV-K2) class-wide at g₀ = 2: the glued
  case table for (a) (finite, per glue shape — the note's own named next unit) +
  the A = {0} safety LEMMA (promote the displayed arithmetic) + the K2-L9
  budget/placement kills for (b)/(e) (WINDOW-CALC corollaries). On success the Q2
  open set at g₀ = 2 is EMPTY and (SURV-DISJ) coverage becomes
  (g₀ = 1, every d₀) ∪ (g₀ = 2, unconditional) — the LAW-DCX pinned class widens
  a third time.
* **U5 — W2-COB, the coboundary probe** (S design + M sealed probe; S5). The
  𝒵-cochain statement of lift freedom; prereg: c_T transforms by the coboundary and
  the ratio-shaped battery members are lift-invariant; on an exact 0-violation law,
  compose the ratio-shaped half of W2-OPEN-1. Runner-up to U1 in value; first in
  centrality (it advances the weld = the directive's most-central math).
* **U6 — the consolidation batch** (S, orchestrator-executable; S6 items 1/2/5 +
  the ENGINE-COVERAGE table). Cross-file dated brackets + the Q2-FRONTIER display
  at the next fold; nothing retires silently.
* **Standing, unchanged, unblocked by none of the above:** the Thursday Codex queue
  (VC40 + W/E/S/SD acceptance legs + K2/W2/SDYJ legs + the weld-blueprint
  plan-review) — S8's cap observation makes the W-arc leg the queue's first seat.

## S8 — STRUCTURAL FINDINGS (dispatch facts, not units)

1. **The W arc is the single grade cap on all of Q2.** Every closure-program note
   consumes WMULTDCX at attempt grade (0/2 CLEAN), so E/SD/SDY/K2's grades are all
   capped by ONE acceptance debt. The W-arc Codex leg is the highest-leverage
   verification action on the board — worth stating as the Thursday queue's
   priority order, ahead of VC40.
2. **The closure program's per-note dialects are a WRITE-UP debt, not a proof
   debt.** Nothing found in this pass suggests a gap in the proved perimeter; the
   duplication is real but benign until the definitive Q2 write-up — U1/U2 are
   therefore sequenced as write-up-preparation units, safely behind the acceptance
   legs, EXCEPT where they feed live mathematics (U2 → the T1-RES fork; U3/U4 → the
   last Q2 corners).
3. **Q2's endgame is visible.** With U4 landed, the only Q2 mathematics left is
   g₀ ≥ 3 — and S5's involution framing + U2's substrate turn it from "different
   mechanism or FALSE" into a preregistrable fixed-point question.

— Synthesis pass 9, 2026-08-08 campaign (wallclock 2026-08-04).
