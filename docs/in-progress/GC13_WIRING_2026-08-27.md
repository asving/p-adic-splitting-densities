# GC-13 WIRING — decomposer blueprint for the read-dictionary campaign

**[FGCW, 2026-08-27]** · Protocol L (agent-facing).  Companion Lean:
`leanfinal/Uniformity/ChapI/IFC2.lean` (landed, Lean-core).  Verdict:
`runs/wave-b/verdict_FGCW.md`.  Predecessors: IFC1 ([FDTB] — the translation lemma that
DISPLAYED `hread`/`htheta`), `FGMNCALCULUS_FIELDLIST_2026-08-24.md` §8 (the OPEN-DICT
registry), `PACKAGING_ROUTE_2026-08-25.md` (PK-1: FGMN legs OUT of the node carrier).

## §0 State after IFC2 (what a resuming agent may rely on, by name)

CLOSED (theorems, Lean-core, `Uniformity.Density.IFC2.*`):

* `theta_one` — `A.theta q 1 = 1` for EVERY arena (D.04/D.08 theorem; no dictionary).
* `theta_zero` — `A.theta q 0 = 1`; D.44's `s = 0` clause ≡ `R (N.n 0) = 1`.
* `thetaRatioCleared_one` — any calculus, `t = 1` ratio `= 1` given the nonzero `d = 1` read.
* `s2Mu3_htheta_discharged` — **IFC1's `htheta` byte-shape, PROVED** for every arena and
  every receiver at the μ₃ point `(2,1,21)`.  GC-13's `θ`-half at `s = 1` is CLOSED.
* `s2Mu3_gentowWWindow_of_hread` — the μ₃ tooth now conditional on `hread` ALONE.
* `s2Mu3_read_eq_one` + `s2Mu3_hread_iff` — the μ₃ FGMN read is the UNIT `1` of the
  2-element terminal field; `hread ↔ R(ladder point) = 1`.

REFUTED / PINNED (the adjudication evidence):

* `s2_towerReadFamily_zero` — the landed witness (`s2FrontierRealizedInput`) exports
  `towerRead ≡ 0` (NP-8's explicitly FREE convenience choice).
* `s2_hread_refuted_at_landed_exports` — `hread` is FALSE at the landed witness's OWN
  exports (every level, receiver, lattice point).
* `s2_gentowW_false_at_landed_exports` — the FULL D.44 `GentowW` fails there.
* `s2_gentowW_of_read_one` — ⚠ D-D12-fenced freedom half: the equally admissible read
  `≡ 1` satisfies FULL `GentowW` over the S2 terminal field.  NOT supply.

**The adjudication**: both truth values of the GC-13 wiring are realized by admissible
node data, because `towerRead` is a free field ("nothing in the carrier reads this field",
NP-8).  Therefore `hread` at honest data is NOT a provable identification over the landed
structures — it is a MISSING SOURCE-FAITHFUL LAW.  This is OPEN-DICT-3/4 content
(FIELDLIST §8 items 3–4: the scalar-coordinate specialization of `R_{r,α}` through
`W.fld i ≃ F_i`, and the normalizer/letter compatibility `γ_i → y_i → z_i`), i.e. exactly
D.44's never-formalized GC-13 note `w_i = R_{i+1,κ̄_i}(n̂_i(u_{i+1}))`,
`θ_i(t) = Θ_N(s;u_{i+1})` (the second is now a theorem at `s = 1`).

## §1 The missing statement (exact shape, with the open design forks)

Proposed carrier (names provisional; leanspec signing is GCW-1):

```
/-- GC-13 wiring: at an arising witness, the exported tower read at the level-j ladder
points IS the receiver image of the FGMN normalizer read at the cleared grade. -/
structure GC13Wiring (…node/tower/per-level data…) : Prop where
  read_norm : ∀ j, (hj : GaugeLive r j) → ∀ s, 1 ≤ s → s ≤ f' j →
    towerRead j ((N.n (s * q j)) ⟨j, hj⟩)
      = φ j ((I j).Rgr (e' j * (s * u' j)) ((I j).chainNormBelow r (s * u' j)))
```

with `q j := W.u (j+1)` (= `useHeightFamily`'s live value), grades in A-C.21's cleared
convention (OPEN-DICT-2 decision record), `φ j : W.fld r →+* Kt` the level receiver
(at S2: `Kt = W.fld 2`, `φ = id` canonical).  Honest design forks that GCW-0 must decide:

* **F1 (lattice↔polynomial)**: LHS reads a LATTICE component of `N.n (s·q)`; RHS reads a
  POLYNOMIAL `chainNormBelow`.  The identification presupposes the normalizer dictionary
  (arena `NormSection` ↔ `FGMNCalculus.chainNorm`'s `towerNorm` exponent solve) — itself
  OPEN-DICT-4 territory; the law may need to be stated against `arenaNormSection0`'s
  concrete Laurent normalizer rather than an abstract `N`.
* **F2 (placement)**: three options — (P1) a law field of `SplitNodePointSource`
  (WRONG post-PK-1: FGMN legs were deliberately factored OUT of the node); (P2) a
  standalone Prop-record demanded by the chapter-I suppliers at arising witnesses
  (RECOMMENDED: keeps PK-1 clean, no vacuity risk, CIFACE already tracks the teeth);
  (P3) definitional at node assembly (choose `towerRead` to satisfy it) — legitimate ONLY
  after the law is signed, else it is exactly the D-D12 "cooked data" failure.
* **F3 (source status)**: does the source PROVE the identification (then `read_norm` is a
  THEOREM of the OPEN-DICT-3/4 dictionary once its carriers exist), or does it DEFINE the
  gauge read that way (then P3 is the honest enactment)?  Print-read targets:
  `EFF.T3.21`'s GC-13 note + `GENTOW2_PROOF_2026-08-09.md` S5.2 ll. 711–748 (the peel
  step that produces `w_i` AS the residual read of `n̂`).

## §2 Node DAG

* **GCW-0** [print-read, Codex second-opinion] Decide F1/F2/F3 against the sources above;
  output: decision record + the exact law statement.  BLOCKS everything below.
* **GCW-1** [leanspec, A-C amendment round] Sign the `GC13Wiring` carrier and the amended
  DT0 demand (supplier gains the wiring leg, or the `CanonicalDeepTwistConfig` guard is
  extended).  New-statement review flags per the standing queue.
* **GCW-2** [leanfinal twin] Land the carrier + upgrade IFC1's translation: replace the
  `hread` binder by the `GC13Wiring` leg (IFC2's `s2Mu3_gentowWWindow_of_hread` is the
  plug point; `htheta` is already gone).
* **GCW-3** [S2 discharge] Re-assemble the S2 node with the law-satisfying read (at S2 the
  law forces `towerRead = 1` at ladder points — `s2Mu3_hread_iff`; the constant-1 read
  satisfies it and, by `s2_gentowW_of_read_one`, the FULL D.44 identity).  Prove
  `GC13Wiring` at the new witness; fire the tooth → the first `GentowWWindow` at an
  arising witness's OWN exports.  FENCE: only after GCW-0/GCW-1 (else cooked).
* **GCW-4** [open math] The `θ`-dictionary beyond `s = 1` (needed at any rung `f' ≥ 2`):
  relate `thetaRatioCleared t` to arena `theta q t` at general `t` — the A-C.21 ratio
  engine vs D.08's `vartheta_succ` recursion.  Independent of GCW-1..3 after GCW-0.
* **GCW-5** [range residue] D.44's `s = 0` clause (now `R (N.n 0) = 1`, `theta_zero`) and
  the `s > f'` tail — at S2 both fall to the GCW-3 assembly (constant-1 read satisfies all
  exponents); at general instances they are supply obligations of the same law record.
* **GCW-6** [standing design constraint, deep-witness campaign] THE TENSION RECORD: by
  `s2_gentowW_false_at_landed_exports` + read-freedom, any depth-`≥ 4` witness assembled
  with a zero read REFUTES `DeepGentowSupplier` outright.  The wiring law must therefore
  enter the arising-witness definition (or the supplier's guard) BEFORE any depth-`≥ 4`
  witness lands.  Stop-the-line note for the DT0/CIFACE chapter.

## §3 Falsifiers / anti-cook teeth (carry into every GCW unit)

1. Any claimed `hread` discharge must be at a witness whose read satisfies the SIGNED law
   — `s2_hread_refuted_at_landed_exports` is the standing counterexample to "it's rfl".
2. `gentowW_false_of_read_zero` kills any packaging that quantifies `GentowW` over
   unconstrained reads.
3. The freedom half (`s2_gentowW_of_read_one`) must NEVER be cited as supply — it carries
   the D-D12 fence in its docstring; a verdict citing it as teeth is auto-rejected.

## §4 Not delivered by this campaign

Depth-`≥ 4` witnesses and per-level calculi `j ≥ 3` (the `WleCleared` tower); the entire
`vartheta`/E.61 half; CIFACE's degree-2 non-vacuity teeth; the `S2LevelOneThreshold`
faithfulness datum.  All unchanged.

---
**[ORCHESTRATOR ADJUDICATION, 2026-08-27 21:57]** — F2 placement: **P2 ADOPTED** (the
standalone Prop-record demanded by the chapter-I suppliers at arising witnesses), per this
blueprint's own recommendation and the architecture's standing pattern (PK-1 factored the
FGMN legs OUT of the node deliberately — A-C.21's carrier discipline; the IFC0 suppliers
are exactly where arising-witness laws live; P3's definitional route is fenced to
witness-construction sites only, per the D-D12 fabricability discipline). The GCW-0..6
nodes proceed under P2; the depth-≥4 tension (a zero-read deep witness would refute
`DeepGentowSupplier`) is P2's non-vacuity tooth obligation, inherited by the supplier
campaign. Decision made under the compile-is-the-validator authority; review row inherited
by the GCW nodes' landings.

## [GCW-0] F1/F3 adjudication and signed-law candidate

**[GCW-0, 2026-08-27] Decision.** F2=P2 is inherited from the orchestrator.  The remaining
forks are **F1 = relational read bridge at the exported D.44 surface, with abstract `N`
retained**, and **F3 = DEFINE on the source side; require a law at the split Lean surface**.

### [GCW-0] F1 — do not equate lattice and polynomial carriers

There is no proposed equality between a `GaugeLattice` element and a `Polynomial O`.
The dictionary is the equality of their **scalar reads**.  Keep D.44's abstract
`N : NormSection G` and `R : G → Kt` in the standalone law:

* `EFF.T3.21` first pins `q = u_{i+1}`, `N(q) = n̂_i(u_{i+1})`, and
  `N(sq) = n̂_i(su_{i+1})`, then defines
  `w_i = R_{i+1,k̄_i}(n̂_i(u_{i+1}))`.  Thus the source bridge is at the read-value
  surface, not an equality of the two carrier types.
* GENTOW2 S5.2 lines 729–752 takes
  `w := u(k̄) = R_{3,k̄}(n̂₂(u₃))` and proves the peel identity from it.  The object read
  on the polynomial side is therefore exactly the below-current-key normalizer.
* OPEN-DICT-2 fixed that normalizer at repo depth `r > 0` as
  `chainNormBelow I r k = chainNorm I (r - 1) k`, and fixed the honest read grade as the
  full-current coordinate `e' * (s * u')`.  No `chainNorm r` or uncleared grade is allowed.
* `C130vw.GaugeFamilyViewEq` already pins an arising witness's exported `N.n` to the
  transported `arenaNormSection0.n`; `ArenaFamilyViewEq` pins `R j` on live indices to
  the same witness's `towerRead`.  Replacing the law's `N` by a concrete
  `arenaNormSection0` would duplicate those view pins, couple P2 to chapter-C internals,
  and fail to match IFC1/IFC2's abstract D.44 binder.

Accordingly, `GC13Wiring` is sitewise.  A chapter-I supplier must demand an instance at
each consumed live level using that arising witness's own exported `(N, q j, R j)`; this is
the D-D12 anti-cook guard.  The relation itself carries the missing OPEN-DICT-3 scalar
specialization and OPEN-DICT-4 normalizer/letter compatibility.

### [GCW-0] F3 — source definition, Lean law

The source does not prove equality with an independently chosen gauge read.  It says
“Define” and sets `w_i` to the FGMN residual read of `n̂_i(u_{i+1})`; GENTOW2 B′ likewise
defines `u(β) := R_{3,β}(n̂₂(β̂))`.  T3's boundary telescope then **proves** the B-law from
BR1–BR5; its explicit note that the endpoint equation was not supplied concerns that
subsequent telescope identity, not the definition of `w_i`/`u(β)`.

Lean's split carrier deliberately introduced `towerRead` as a free field, so the source
definition is not a theorem of the landed structures.  Therefore F3 is enacted as the P2
law below.  P3 is honest only at a witness-construction site after signing: defining that
witness's `towerRead` to satisfy this law realizes the source definition.  It is not an
alternative placement and supplies nothing by itself.

### [GCW-0] Exact `GC13Wiring` statement

This is the Lean-ready, elaboration-checked statement.  `A` is intentionally absent: the
remaining GC-13 read dictionary uses only D.44's `N`, `q`, and `R`; the already-discharged
theta dictionary is separate.

```lean
universe uT

/-- GC-13's read/normalizer dictionary at one D.44 site. -/
structure GC13Wiring {G : Type*} [CommGroup G] {Kt : Type*} [Field Kt]
    (N : NormSection G) (q : ℤ) (R : G → Kt)
    {O : Type} [CommRing O] [IsDomain O] [IsDiscreteValuationRing O] {π : O}
    {F : KeyFrame O π} {H₀ : ℕ} {hpin : F.Pin H₀} {r : ℕ}
    {W : DeepTower.{0, uT} F H₀ hpin r} {e' f' u' : ℕ}
    (I : FGMNCalculus W e' f' u') (φ : W.fld r →+* Kt) : Prop where
  read_norm : ∀ s : ℕ, 1 ≤ s → s ≤ f' →
    R (N.n (s * q)) =
      φ (I.Rgr (e' * (s * u')) (I.chainNormBelow r (s * u')))
```

The grade is deliberately `e' * (s * u')` (OPEN-DICT-2 full-current clearing), and the
normalizer is deliberately `chainNormBelow r` (the decided `n̂_i`, not the current key).

### [GCW-0] IFC2 consumer-shape check

At `(W,e',f',u',I) = (s2DepthTwo h2 hq,2,1,21,s2Mu3Calculus h2 hq)`, the projection

```lean
hwire.read_norm 1 le_rfl le_rfl
```

has exactly IFC2's remaining binder:

```lean
R (N.n ((1 : ℕ) * q)) =
  φ ((s2Mu3Calculus h2 hq).Rgr (2 * (1 * 21))
      ((s2Mu3Calculus h2 hq).chainNormBelow 2 (1 * 21)))
```

and is accepted directly by `s2Mu3_gentowWWindow_of_hread`, with no rewrite or
repackaging.  Independently, `s2Mu3_hread_iff` rewrites the same equation to
`R (N.n ((1 : ℕ) * q)) = 1`; it does not weaken or replace the signed dictionary.
The exact term is compiled in `leanfinal/scratch/GCW0_probe.lean`.

### [GCW-0] Continuation closure — print-read and compile validation

**[GCW-0, 2026-08-27] COMPLETE.** The continuation audit found no evidence requiring a
change to the five decisions/sections above.  In particular, GENTOW2 S5.1 defines the
whole per-grade family
`u(β) := R_{3,β}(n̂₂(β̂))`, while S5.2 specializes its top-slot member to
`w := u(κ̄) = R_{3,κ̄}(n̂₂(u₃))` and proves the peel/power factorization.  Thus F3's
source-side `DEFINE` verdict applies to the read itself; the later B-law is the theorem.
EFF.T3.21 repeats the same definition at general `i ≥ 3`.  OPEN-DICT-2 fixes the RHS to
the full-current grade and below-current normalizer, and FIELDLIST §8 items 3–4 confirm
that the cross-carrier equality remains realization-dictionary content.

Validation from `leanfinal/`:

```text
lake build Uniformity.ChapI.IFC2              # exit 0
lake env lean scratch/GCW0_probe.lean         # exit 0
```

The probe passes `hwire.read_norm 1 le_rfl le_rfl` directly to
`s2Mu3_gentowWWindow_of_hread`; `#print axioms` reports only
`[propext, Classical.choice, Quot.sound]`.  F1/F2/F3 and the exact statement above are
therefore the final GCW-0 handoff to GCW-1.

## [GCW-1] ENACTED (2026-08-27, unit GCW1) — the signing round

**Signed**: AMENDMENT A-I.5 (`leanspec/Leanspec/ChapI.lean`) — NODE I.10c `GC13Wiring`
(GCW-0's record VERBATIM) + NODE I.10d `WiredGentowDemand` (the amended DT0 demand:
`GC13Wiring ∧ GentowW` at one site — the SUPPLIER-GAINS-THE-LEG fork, sitewise;
`CanonicalDeepTwistConfig`/`CapstoneHypotheses` byte-unchanged, the guard-extension fork
rejected as chapter-C coupling + gate-(a)); `#axiom_core` census extended (+2, both pass);
gate items (18)–(20).  **Landed**: `leanfinal/Uniformity/ChapI/IFC3.lean` (10 declarations,
Lean-core) — the twins, the GCW-2 upgrade (`s2Mu3_gentowWWindow_of_wiring` replaces the
`hread` binder at IFC2's plug point, byte-shape; general form `gentowWWindow_of_wiring`),
GCW-3's assembly datum (`s2_wiring_forces_read_one`), the anti-cook tooth
(`s2_wiring_refuted_at_landed_exports` — falsifier §3.1 survives the signing), and the
fabricability fences (`gc13Wiring_vacuous_of_f'_zero`, `wiredGentowDemand_iff_of_f'_zero`).
**GCW-2 is thereby CLOSED** (the translation upgrade was within the signing round's reach).
**BLOCKED-BECAUSE, recorded**: no UNIVERSAL wired `DeepGentowSupplier` twin — the per-level
calculus/receiver correspondence has no landed exporter (`WleCleared`, §4); guard-side or
`∃`-bound sites collapse to the refutable unwired form through `f' = 0` junk sites, `∀`-bound
sites are false, def-parameter site families cannot see the witness in the guard's
existential.  IFC0 untouched (`DeepGentowSupplier` retired as target, kept as the GCW-6
tension record); VAUD's parallel vartheta finding noted, not enacted (owner-gated).
Verdict: `runs/wave-b/verdict_GCW1.md`.  NEXT: GCW-3 (law-satisfying S2 re-assembly), GCW-4/5
(θ beyond `s = 1`; the `s = 0`/tail clauses), the exporter for the universal wired supplier.

## [WLE 2026-08-28] The witness-dependent `WleCleared` exporter

**Status: DESIGNED-ANCHORED; deep inhabitants OPEN.**  Statement authority is the compiling,
zero-`sorry` probe `leanfinal/scratch/WLE_probe.lean`.  This section changes no landed Lean,
leanspec declaration, socket, guard, or `CapstoneHypotheses` byte.  In particular it does not
claim that a bare `CanonicalDeepTwistConfig` is wired: IFC3's zero-read counterexample and the
GCW-6 stop-the-line record remain in force.

### (a) Exact per-level exporter

The missing object is not a free tuple `(e',f',u',I,φ)` outside the arising guard.  It is an
owner-attached level package whose first parameter is the explicit `RealizedInput X`.  Its
receiver is definitionally the same witness's `Aℛ.receiver.levelHom j`; its gauge data are the
same witness's `normalizer`, `useHeightFamily`, and `towerReadFamily`; and `f_pos` prevents the
machine-certified `f' = 0` collapse.  Exact elaborated statement (probe lines 21–53):

```lean
structure WleClearedLevelExport
    {O : Type} [CommRing O] [IsDomain O] [IsDiscreteValuationRing O]
    {Kt : Type uKt} [Field Kt] {E : Type uE} [Field E]
    {L : Type uL} [Field L] [Algebra Kt L] {n : ℕ}
    {core : ArisingCore (O := O) Kt L n}
    {Aℛ : ChainRealization (O := O) (π := core.π) (F := core.F) (H₀ := core.H₀)
      (hpin := core.hpin) core.T Kt E L}
    (X : RealizedInput core Aℛ) (j : ℕ) (hjr : j ≤ core.r) :
    Type (max uE uG uKt uL) where
  e' : ℕ
  f' : ℕ
  u' : ℕ
  f_pos : 0 < f'
  calculus : ∀ i, (hi : i ≤ core.r) →
    FGMNCalculus (core.T.trunc i hi) e' f' u'
  cleared : WleCleared core.T e' f' u' calculus j
  wiring : IFC3.GC13Wiring
    (Aℛ.normalizer.arenaNormSection0.transport
      (show GaugeLattice.{uG} core.r ≃* GaugeLattice0 core.r from
        gaugeLatticeEquiv core.r).symm)
    (useHeightFamily X j) (towerReadFamily X j) (calculus j hjr)
    (Aℛ.receiver.levelHom j)

def WleClearedPerLevelExporter
    {O : Type} [CommRing O] [IsDomain O] [IsDiscreteValuationRing O]
    {Kt : Type uKt} [Field Kt] {E : Type uE} [Field E]
    {L : Type uL} [Field L] [Algebra Kt L] {n : ℕ}
    {core : ArisingCore (O := O) Kt L n}
    {Aℛ : ChainRealization (O := O) (π := core.π) (F := core.F) (H₀ := core.H₀)
      (hpin := core.hpin) core.T Kt E L}
    (X : RealizedInput core Aℛ) : Prop :=
  ∀ j, (hj : DeepLive core.r j) →
    Nonempty (WleClearedLevelExport.{uE, uG, uKt, uL} X j hj.2.le)
```

For `hj : DeepLive core.r j`, `S.cleared j hj.1 le_rfl hj.2.le` has exactly type

```lean
GENTOW5WCleared (core.T.trunc j hj.2.le) S.e' S.f' S.u'
  (S.calculus j hj.2.le)
```

and is compiled as `WleClearedLevelExport.top_cleared` (probe lines 78–89).  This is the
level-by-level extraction from the cumulative carrier at C132sg3 lines 104–108.  Notice that
the existing signed `WleCleared` has one fixed `(e',f',u')` for the entire calculus family;
the level package follows that byte-shape rather than silently replacing it by an
index-dependent family.

### (b) Exact universal wired supplier

The honest universal quantifier is over explicit arising witnesses `X`, followed by the level
quantifier, not over raw sites.  A *wired witness* here means one equipped with
`WleClearedPerLevelExporter X`.  The conclusion uses only `X`'s own exports and the level
package's own calculus plus the canonical receiver (probe lines 58–74):

```lean
def UniversalWiredDeepGentowSupplier (n : ℕ) : Prop :=
  ∀ (O : Type) [CommRing O] [IsDomain O] [IsDiscreteValuationRing O]
    (Kt : Type uKt) [Field Kt] (E : Type uE) [Field E]
    (L : Type uL) [Field L] [Algebra Kt L]
    (core : ArisingCore (O := O) Kt L n)
    (Aℛ : ChainRealization (O := O) (π := core.π) (F := core.F) (H₀ := core.H₀)
      (hpin := core.hpin) core.T Kt E L)
    (X : RealizedInput core Aℛ),
    WleClearedPerLevelExporter.{uE, uG, uKt, uL} X →
      ∀ j, (hj : DeepLive core.r j) →
        ∃ S : WleClearedLevelExport.{uE, uG, uKt, uL} X j hj.2.le,
          IFC3.WiredGentowDemand
            (arenaFamily.{uE, uG, uKt, uL} X
              (by rcases hj with ⟨hj3, hjr⟩; omega) j)
            (useHeightFamily X j) (towerReadFamily X j)
            (Aℛ.node.peelUnitFamily j) (S.calculus j hj.2.le)
            (Aℛ.receiver.levelHom j)
```

This is the wired strengthening of IFC0's flat conclusion at lines 189–197.  Projecting
`WiredGentowDemand.gentowW` recovers the old per-level `GentowW` conclusion at the explicit
witness.  It deliberately does **not** alter `CanonicalDeepTwistConfig` or add a premise to
`CapstoneHypotheses.deepTwist`.  A later capstone binding still needs a theorem extracting a
wired explicit witness from every configuration it intends to consume; such a theorem cannot
hold for an arbitrary zero-read deep realization and is not asserted here.

The assembly itself is already Lean-core once the three residual dictionaries below are
available.  Probe theorem `wiredDemand_of_level_export` (lines 148–180) applies
`IFC1.gentowWWindow_of_cleared_law` (IFC1 lines 121–146), uses `S.wiring.read_norm`, and closes
the full identity with `IFC1.gentowW_iff_zero_window_tail` (IFC1 lines 92–106).

### Quantifier audit against every GCW defect

* **Guard-side / existential free site (`f' = 0`)**: avoided.  IFC3
  `gc13Wiring_vacuous_of_f'_zero` (lines 128–140) and
  `wiredGentowDemand_iff_of_f'_zero` (lines 258–270) still refute the old placement.  Here the
  package is a conclusion after explicit `X` and `j`, carries `0 < f'`, and at `j ≥ 3` its
  `WleCleared` top projection is nonvacuous.  No junk `f' = 0` package inhabits this carrier.
* **Universal over all sites/calculi**: avoided.  The statement asks for one owner-exported
  package `S` at each `X,j`; it never asserts `GC13Wiring` for an arbitrary calculus or receiver.
  The receiver is fixed to `Aℛ.receiver.levelHom j`.
* **Definition-parameter site family outside the guard**: avoided.  Binder order is
  `core, Aℛ, X` first, then `j`, then the dependent package.  Thus the calculus, receiver,
  normalizer, height, read, arena, and peel unit can all mention the same witness.  No surface
  family is asked to see through `CanonicalDeepTwistConfig`'s existential.
* **Wrong/off-live export indices**: avoided.  The universal target is exactly
  `DeepLive core.r j`, matching IFC0's `3 ≤ j < r`; it does not quantify over the junk branches
  of `towerReadFamily` or `useHeightFamily`.

**BLOCKED-BECAUSE (remaining carrier wall).**  `ChainRealization` intentionally has no FGMN
calculus field (C130fg lines 279–315; PK-1 factored it out), so `WleClearedLevelExport` is the
new standalone owner package, not a theorem obtainable by projection from today's bare `X`.
At deep levels its inhabitants require a source-faithful FGMN realization/calculus and the
open C.89 single-`w` mathematics.  Treating the existential calculus as freely fabricable
would repeat the old defect; a proof of the exporter must construct it from the future
FGMN realization data (FIELDLIST §8 OPEN-DICT-1–4), not from arbitrary class fields.

### (c) S2 proof plan and the exact discrepancy

The landed S2 chain validates the *local algebraic route* but is not literally an instance of
the deep exporter above.

1. `IFC4.s2SplitNodeWired` (lines 120–128) and `s2FrontierWired` (145–151) give the explicit
   wired witness; `s2FrontierWiredRealizedInput` is its `X` (178–180).
2. The positive cleared top datum is exactly
   `C132sg3.s2Mu3_gentow5w_two : GENTOW5WCleared (s2DepthTwo h2 hq) 2 1 21
   (s2Mu3Calculus h2 hq)` (C132sg3 lines 329–340), so `f_pos` is `by norm_num`.
3. The own-export law is `IFC4.s2Wired_gc13Wiring` (lines 249–269), with receiver
   `RingHom.id _`.  Feeding it to `IFC3.s2Mu3_gentowWWindow_of_wiring` is exactly the proof of
   `IFC4.s2Wired_gentowWWindow` (lines 289–301), whose own-export specialization is
   `s2Wired_gentowWWindow_at_own_exports` (310–318).  Thus the designed assembly route does
   recover that theorem's window calculation, and IFC4 already supplies the stronger
   `s2Wired_wiredGentowDemand_at_own_exports` (370–380).

The promised literal "level-2 instance" does **not** exist: `WleCleared ... 2` is vacuous
because its definition begins at `3` (C132sg3 lines 104–108), and `DeepLive 2 j` is empty.
Moreover the own-export S2 theorem is at the only `GaugeLive 2 j`, namely `j = 1`, while its
FGMN calculus has depth `2`; `j = 2` is off the gauge-live range.  Probe theorems
`no_s2_deep_level` and `wleCleared_two_vacuous` (lines 185–196) machine-pin both facts.
Accordingly S2 is a depth-2/base **shape tooth**, exactly as IFC4 lines 384–398 already say,
not evidence for any `3 ≤ j < r` exporter inhabitant.  Relabeling it as one would be index drift.

### (d) Deep campaign demand list (all OPEN-LEMMA)

For every explicit deep witness and `hj : DeepLive core.r j`, the arising-witness campaign
must prove the following named statements.  These are exact probe types, not placeholders:

```lean
-- OPEN-LEMMA WLE-DEEP-EXPORT (contains the deep C.89/WleCleared mathematics,
-- source-faithful calculus construction, positive f', and own-export GC13 wiring).
theorem deep_wleCleared_level_export (X : RealizedInput core Aℛ)
    (j : ℕ) (hj : DeepLive core.r j) :
    Nonempty (WleClearedLevelExport.{uE, uG, uKt, uL} X j hj.2.le)

-- OPEN-LEMMA WLE-DEEP-THETA (GCW-4 at every cleared exponent).
theorem deep_thetaCompatibleAt (X : RealizedInput core Aℛ)
    {j : ℕ} (hj : DeepLive core.r j)
    (S : WleClearedLevelExport.{uE, uG, uKt, uL} X j hj.2.le) :
    ThetaCompatibleAt X (by rcases hj with ⟨hj3, hjr⟩; omega) S

-- OPEN-LEMMA WLE-DEEP-PEEL: the single-w witness supplied by the top cleared
-- law maps to this same arising witness's exported peelUnit.
theorem deep_peelCompatibleAt (X : RealizedInput core Aℛ)
    {j : ℕ} (hj : DeepLive core.r j)
    (S : WleClearedLevelExport.{uE, uG, uKt, uL} X j hj.2.le) :
    PeelCompatibleAt X S

-- OPEN-LEMMA WLE-DEEP-RANGE (GCW-5 at general depth: s=0 plus s>S.f').
theorem deep_rangeCompleteAt (X : RealizedInput core Aℛ)
    {j : ℕ} (hj : DeepLive core.r j)
    (S : WleClearedLevelExport.{uE, uG, uKt, uL} X j hj.2.le) :
    RangeCompleteAt X (by rcases hj with ⟨hj3, hjr⟩; omega) S
```

`ThetaCompatibleAt` is printed in the probe at lines 92–103;
`PeelCompatibleAt` at 106–120 (including the explicit cleared witness and own-peel equality);
`RangeCompleteAt` at 123–144 (zero clause and tail).  Once these four lemmas land,
`wiredDemand_of_level_export` proves the sitewise signed demand, and ordinary quantifier
assembly proves `UniversalWiredDeepGentowSupplier`.  Today all four are open at `r ≥ 4`;
the first is also blocked on the absent source-faithful deep FGMN realization carrier.

Validation from `leanfinal/` on 2026-08-28:

```text
timeout 580 ~/.elan/bin/lake env lean scratch/WLE_probe.lean   # exit 0; no warnings/sorries
```
