# UNIT NOTE — UNIFORMITY-CHECK-N3: the `n = 3` definition check, 2026-08-13

> 🔁 **RE-KEYED 2026-08-13 by unit UNIFORMITY-P4.** `genuineDensity` now names the
> *consistent-class* limit `⨅_N possibleSeq` (THE density); the old decided limit
> `⨆_N decidedSeq` is `decidedDensity`. Names in this note have been mechanically updated to
> match the code. Rename map and the honest `n = 3` consequences:
> `leanfinal/notes/GENUINE_DENSITY_2026-08-13.md` §8. **At `n = 3` drainage is OPEN**, so the
> certified upper bounds here do NOT transfer to THE density without the `+ 1/q²` slack of
> `genuineDensity_three_le` — see `N3Exact.lean` §3.

**Charge.** Check `Uniformity.Density.decidedDensity` at degree 3, the way
`N2_EXACT_2026-08-13.md` checked it at degree 2: decidedness certificates for each of the five
cubic splitting types over a general complete DVR, proved lower bounds, upper bounds by total
mass, and machine-checked containment of the informal corpus's predicted values
(`lean/notes/openmath/HMENU3_PROOF_2026-08-08.md`, COROLLARY HM3.D).

**Where the work lives.** Package `UniformityCheck` in `leancheck/`, a lake path-dependency on
`../leanfinal` (read-only). Nothing under `leanfinal/` was created, modified, or deleted by this
unit. The old `lean/` tree was used only as a mathematical quarry (HMENU3's expected values, HEX3's
drainage law); no code was copied from it.

---

## 1. RUNGS LANDED

The ladder in the charge was FLOOR (certificates + lower bounds + brackets) → MIDDLE (exact
per-level family cardinalities) → TOP (drainage at `n = 3`).

* **FLOOR: COMPLETE**, at the strongest rung (general `O`, no residue-characteristic hypothesis,
  no `ℤ_[p]` fallback needed for any of the five types).
* **MIDDLE: COMPLETE for all five types.** The exact level-1 / level-2 cardinalities of the
  certificate families are proved and used, so every lower bound is a family bound, not a
  single-class bound — including the split type, whose level-1 family has
  `C(q,3) = q(q−1)(q−2)/6` members (`six_mul_card_split3`). ⚠ That split family is EMPTY at
  `q = 2` (no cubic over `𝔽₂` has three distinct roots), so the split type also carries a
  general-`O` DEEP certificate (a single class at level 3, bound `1/q⁹`) which is what
  `lowers_three` uses; the level-1 family bound `(q−1)(q−2)/(6q²)` is stated separately
  (`gate_split3_res_lower`) and is what the `q = 3` gate uses, beating the deep class by `729×`.
* **TOP: NOT ATTEMPTED — reconnaissance delivered instead** (§7). The `class_pinned` feasibility
  verdict is one line: *feasible in arithmetic, blocked on a missing mechanism* — see §7.

`O` ranges over every complete discrete valuation ring with finite residue field
(`[CommRing O] [IsDomain O] [IsDiscreteValuationRing O] [IsAdicComplete (maximalIdeal O) O]
[Finite (ResidueField O)]`), `q = residueCard O`. Wild primes 2 and 3 are not special-cased
anywhere: the simple-root derivative values that Hensel consumes are `ρ² + β₁ρ + β₀`, `a₂²`,
`(ρ₁-ρ₂)(ρ₁-ρ₃)` and `1`, all units in every characteristic.

### The five certificates

| σ | theorem | certificate | level |
|---|---|---|---|
| `c3split = {(1,1),(1,1),(1,1)}` | `typeOf_split3_of_residue` | three DISTINCT residue roots | 1 |
| `c3split` | `typeOf_split3_of_deep` | `a ≡ (0, π, −(1+π)) mod 𝔪³` (the class of `X(X−1)(X−π)`) | 3 |
| `c3linInert = {(1,1),(1,2)}` | `typeOf_linInert_of_residue` | `f̄ = (X−ρ̄)(X²+β̄₁X+β̄₀)`, quadratic residually rootless | 1 |
| `c3inert = {(1,3)}` | `typeOf_inert3_of_residue_irreducible` | `f̄` irreducible | 1 |
| `c3linRam = {(1,1),(2,1)}` | `typeOf_linRam_of_residue` | `a₂` a unit, `a₁ ∈ 𝔪`, `a₀ ∈ 𝔪 \ 𝔪²` | 2 |
| `c3ram = {(3,1)}` | `typeOf_ram3_of_eisenstein` | Eisenstein: `a₂, a₁ ∈ 𝔪`, `a₀ ∈ 𝔪 \ 𝔪²` | 2 |

### The five lower bounds (`lowers_three`, general `O`)

```lean
theorem lowers_three :
    1 / (residueCard O : ℝ) ^ 9 ≤ decidedDensity O 3 c3split
    ∧ ((residueCard O : ℝ) ^ 3 - (residueCard O : ℝ) ^ 2) / (2 * (residueCard O : ℝ) ^ 3)
        ≤ decidedDensity O 3 c3linInert
    ∧ ((residueCard O : ℝ) ^ 3 - (residueCard O : ℝ)) / (3 * (residueCard O : ℝ) ^ 3)
        ≤ decidedDensity O 3 c3inert
    ∧ ((residueCard O : ℝ) - 1) ^ 2 / (residueCard O : ℝ) ^ 4 ≤ decidedDensity O 3 c3linRam
    ∧ ((residueCard O : ℝ) - 1) / (residueCard O : ℝ) ^ 4 ≤ decidedDensity O 3 c3ram
```

i.e. `1/q⁹`, `(q−1)/(2q)`, `(q³−q)/(3q³)`, `(q−1)²/q⁴`, `(q−1)/q⁴`. The middle two come from the
exact residue-field counts `#{monic irreducible cubics} = (q³−q)/3` and
`#{(linear)·(irreducible quadratic)} = q²(q−1)/2`; the last two from the exact valuation-stratum
counts `q²(q−1)²` and `q²(q−1)` at level 2.

---

## 2. THE MATHEMATICS (written before the Lean)

Write `f = monicPoly a = X³ + a₂X² + a₁X + a₀`.

**Peeling.** `typeOf` is additive on monic products because `monicFactors` is
(`monicFactors_mul` → `typeOf_mul` → `typeOf_linear_mul`, `N3Base`). So the three composite types
reduce to (linear factor) × (quadratic whose type `leanfinal` already certifies). The linear
factor comes from Hensel at a simple residue root, and dividing it out is a *`ring` identity, not
a division*:

    f(r) = 0  ⟹  monicPoly a = (X − C r) · monicPoly ![a₁ + a₂r + r², a₂ + r]     (`cubic_peel`)

with the useful corollary `a₀ = −r · g₀` (`peel_const`), which transports the exact valuation of
`a₀` onto the cofactor's constant term whenever `r` is a unit.

**`{(1,1),(1,2)}`.** `f̄ = (X − ρ̄)(X² + β̄₁X + β̄₀)` with the quadratic rootless. Then
`f̄′(ρ̄) = ρ̄² + β̄₁ρ̄ + β̄₀ = quadForm ![β₀,β₁] (−ρ) 1`, a unit precisely because the quadratic has no
residue root; Hensel gives `r ≡ ρ`, and the cofactor's residues are `ḡ₁ = β̄₁`, `ḡ₀ = β̄₀`, so
`Anisotropic (peel a r)` by `leanfinal`'s `Anisotropic_congr` and
`typeOf_inert_of_anisotropic` finishes.

**`{(1,1),(2,1)}`.** `f̄ = X²(X + ā₂)` with `ā₂ ≠ 0`. The residue root `ρ = −a₂` is simple:
`f̄′(−ā₂) = 3ā₂² − 2ā₂² = ā₂²`, a unit in every characteristic. Hensel gives a UNIT root `r`, the
cofactor has `ḡ₁ = ḡ₀ = 0`, and `a₀ = −r g₀` with `r` a unit transports `v(a₀) = 1` to
`v(g₀) = 1`: the cofactor is Eisenstein, `typeOf_ram_of_eisenstein` finishes.

**`{(1,1)}³`, level 1.** Three distinct residue roots `ρ̄₁,ρ̄₂,ρ̄₃`. Hensel at `ρ₁` (derivative
`(ρ₁−ρ₂)(ρ₁−ρ₃)`), peel, and the cofactor still has a simple residue root at `ρ̄₂` (derivative
`ρ̄₂ − ρ̄₃`); a second Hensel gives an actual root of the quadratic in `O` and
`typeOf_split_of_root` finishes. **Vacuous at `q = 2`** — no cubic over `𝔽₂` has three distinct
roots — which is why the split type also needs:

**`{(1,1)}³`, deep (level 3).** Every `a ≡ (0, π, −(1+π)) mod 𝔪³` splits. Residually
`f̄ = X²(X−1)`, so Hensel at `1` gives `r ≡ 1 (mod 𝔪)`. The bootstrap is the interesting step:
substituting `a₀ = π³u₀`, `a₁ = π + π³u₁`, `a₂ = −(1+π) + π³u₂` into `f(r) = 0` gives

    r (r − π) (r − 1) = π³ · (−(u₂r² + u₁r + u₀)) ,

and `r`, `r − π` are units (both `≡ 1 mod 𝔪`), so `π³ ∣ (r − 1)`: the Hensel root is pinned to
depth 3, not depth 1. Hence `g₁ = a₂ + r = −π + π³(…)` has EXACT valuation 1 while
`g₀ = −a₀/r` has valuation `≥ 3`, which is `leanfinal`'s `CertSplit` at the centre `0` with
`w = 1` (`v(F(0)) = 3 > 2 = 2w`), and `typeOf_of_certSplit` finishes.

**`{(3,1)}` and `{(1,3)}`** both need the cubic norm form, which is built in `N3Norm` by the same
route `QuadCert.lean` uses at degree 2: the power basis `(1, θ, θ²)` of `AdjoinRoot (cub3 b)`, the
explicit `3 × 3` multiplication matrix `cubMat b u`, and `Algebra.norm = det`.

* Eisenstein: `N(θ) = −a₀` has valuation exactly 1, so `1 ∈ normValues`, so `inertiaDegOf = 1`,
  `ramIndexOf = 3`, and irreducibility comes from Mathlib's `IsEisensteinAt.irreducible`.
* Residually irreducible: `3 ∈ normValues` is free (`natDegree_mem_normValues`), and
  `3 ∣ v(N(x))` for every `x ≠ 0` by strong induction on `v(N(x))`. The base case is the key
  lemma `isUnit_norm_of_unit_coeff`: because `cubMat` is a *polynomial expression in the
  coefficients*, `residue O (N(x)) = N_{k}(x̄)` (`residue_norm_cub`, one `RingHom.map_det`), and
  `k[x]/(f̄)` is a FIELD when `f̄` is irreducible, so a nonzero `x̄` has nonzero norm. The step
  case peels `π`: `N(πy) = π³N(y)`.

**From certificates to densities.** `decidedDensity_ge_of_inj` (N3Density) converts an
*injection* of a finite type into the σ-decided level-`N` classes into the bound
`#A / q^{3N} ≤ decidedDensity`; only an injection is needed, never an exact cardinality of the
decided set, which is what makes the family bounds cheap. Upper bounds come from
`leanfinal`'s `sum_decidedDensity_le_one`, **which is general in `n`** (checked: the `n = 2`-only
statement is `sum_three_decidedDensities_eq_one`, and it is NOT used here) — packaged as `bracket_five`.

---

## 3. EVERY NEW DEFINITION, WITH ITS FAITHFULNESS PARAGRAPH

Definitions are the trust boundary. None of these appears in the statement of a headline density
theorem — those mention only `leanfinal`'s `decidedDensity` and `residueCard` plus the five named
types — but the five types DO appear, so they head the list.

### `c3split`, `c3linInert`, `c3inert`, `c3linRam`, `c3ram` (`N3Base.lean`)

```lean
def c3split    : FactorizationType := ⟨{(1, 1), (1, 1), (1, 1)}⟩
def c3linInert : FactorizationType := ⟨{(1, 1), (1, 2)}⟩
def c3inert    : FactorizationType := ⟨{(1, 3)}⟩
def c3linRam   : FactorizationType := ⟨{(1, 1), (2, 1)}⟩
def c3ram      : FactorizationType := ⟨{(3, 1)}⟩
```

**Faithfulness.** `FactorizationType` wraps a multiset of `(e, f)` pairs and
`FactorizationType.degree σ = ∑ eᵢfᵢ`. `c3*_degree : … = 3` is proved by `decide` for all five, so
these are exactly the five degree-3 types with the standard `(ramification, residue degree)`
ordering that `leanfinal`'s `typeOf` produces (`efPair g = (ramIndexOf g, inertiaDegOf g)`).
Pairwise distinctness (`c3split_ne_c3linInert` and the other nine) is `decide` on the underlying
multisets — needed because `sum_decidedDensity_le_one` sums over a `Finset` of types and would be
vacuous if two coincided. ⚠ The ORDER convention is the one place a silent error could hide: if
`typeOf` returned `(f, e)` instead of `(e, f)`, `c3inert = ⟨{(1,3)}⟩` and `c3ram = ⟨{(3,1)}⟩` would
be swapped and every result here would still compile. The convention is fixed and checked by
`leanfinal`'s own `typeOf_ram_of_eisenstein : typeOf (Eisenstein quadratic) = ramType = ⟨{(2,1)}⟩`,
which this unit consumes for the `{(1,1),(2,1)}` type, and by `ramIndexOf_mul_inertiaDegOf`.

### `peel` (`N3Base.lean`)

```lean
def peel (a : Fin 3 → O) (r : O) : Fin 2 → O := ![a 1 + a 2 * r + r ^ 2, a 2 + r]
```

**Faithfulness.** The coefficient vector of the quadratic cofactor `f / (X − r)`. Junk unless
`r` is a root; the only property used is `cubic_peel`, which states
`monicPoly a = (X − C r) * monicPoly (peel a r)` UNDER the hypothesis `f(r) = 0` and is a `ring`
identity after substituting that hypothesis. No division and no `Polynomial.divByMonic` is
involved, so nothing can be lost when the hypothesis is absent.

### `cub3`, `cubBasis`, `cubElt`, `cubMat`, `resVec` (`N3Norm.lean`)

```lean
noncomputable def cub3 (b : Fin 3 → R) : Polynomial R := X ^ 3 + C (b 2) * X ^ 2 + C (b 1) * X + C (b 0)
noncomputable def cubElt (b u : Fin 3 → R) : AdjoinRoot (cub3 b) := algebraMap R _ (u 0) + algebraMap R _ (u 1) * θ + algebraMap R _ (u 2) * θ ^ 2
def cubMat (b u : Fin 3 → R) : Matrix (Fin 3) (Fin 3) R := !![ … ]     -- displayed in the file
noncomputable def resVec (a : Fin 3 → O) : Fin 3 → ResidueField O := fun i => residue O (a i)
```

**Faithfulness.** `cub3` is `monicPoly` at `n = 3` — `monicPoly_eq_cub3 : monicPoly a = cub3 a` is
proved (it is `cubic_expand`), so the two are interchangeable and no separate polynomial is
introduced. `cubBasis` is `(AdjoinRoot.powerBasis' (cub3_monic b)).basis` reindexed along
`cub3_natDegree`, i.e. literally `(1, θ, θ²)` (`cubBasis_zero/_one/_two` proved), the same
construction `QuadCert.quadBasis` uses at degree 2. `cubMat` is NOT assumed to be the
multiplication matrix: `leftMulMatrix_cub` PROVES that
`Algebra.leftMulMatrix (cubBasis b) (cubElt b u) = cubMat b u`, entry by entry, from the defining
relation `root_cube : θ³ = −b₂θ² − b₁θ − b₀`. Everything is stated over an arbitrary nontrivial
commutative ring, which is exactly what makes the reduction lemma
`cubMat_map : (cubMat a u).map (residue O) = cubMat (resVec a) (resVec u)` a one-line `simp`, and
hence `residue_norm_cub` a one-line `RingHom.map_det`.

### `NoRootCubic`, `linAniCoeff`, `LinAni` (`CubicCount.lean`)

```lean
def NoRootCubic {K} [Field K] (c : Fin 3 → K) : Prop := ∀ y : K, y ^ 3 + c 2 * y ^ 2 + c 1 * y + c 0 ≠ 0
def linAniCoeff {K} [Field K] (ρ : K) (p : K × K) : Fin 3 → K := ![-(ρ * p.1), p.1 + ρ * p.2, -(p.2 + ρ)]
def LinAni {K} [Field K] (c : Fin 3 → K) : Prop := ∃ (ρ : K) (p : K × K), AniK p ∧ c = linAniCoeff ρ p
```

**Faithfulness.** `NoRootCubic c` says the monic cubic with coefficient vector `c` has no root in
`K`; over a field a cubic is irreducible **iff** it has no root, and the direction used
(`no root ⟹ irreducible`) is Mathlib's `irreducible_of_degree_le_three_of_not_isRoot`, applied to
`cub3 c` after `cub3_eval` identifies the displayed polynomial expression with `eval`.
`linAniCoeff ρ (b₀,b₁)` is the coefficient vector of `(X − ρ)(X² − b₁X + b₀)` — expand and check.
`AniK (b₀,b₁)` (from the `n = 2` unit) says `y² − b₁y + b₀` has no root, so `LinAni` is exactly
"linear factor times irreducible quadratic". ⚠ **Sign bookkeeping is the live hazard here**:
`AniK` is keyed to `X² − b₁X + b₀` while `leanfinal`'s `Anisotropic ![β₀, β₁]` is keyed to
`monicPoly ![β₀,β₁] = X² + β₁X + β₀`, so the transport in `N3Gates.linInert_decided_res` passes
`β₁ = −b₁` and routes through the explicitly proved bridge `aniK_neg : AniK (x,y) → AniK (x,−y)`
(substitution `t ↦ −t`). The numeric check of §6 is what makes this bookkeeping non-circular:
`#LinAni = 2, 9, 50` at `q = 2, 3, 5` matches `q²(q−1)/2`.

### `unitSet` (`N3Density.lean`)

```lean
def unitSet (π : O) (N : ℕ) : Set (Res O N) := (dvdSet π 1 N)ᶜ
```

**Faithfulness.** The complement of the valuation-`≥ 1` stratum of `Res O N`, i.e. the units of
the truncation. Read downstairs; that this agrees with the upstairs reading is `dvd_res_iff` (the
`n = 2` unit's lemma), and its cardinality `q^N − q^{N−1}` is proved at `N = 2` by
complementation (`card_unitSet_two : #unitSet + q = q²`), not assumed.

### `res1Equiv`, `liftRes1` (`N3Gates.lean`)

```lean
noncomputable def res1Equiv (O) [ … ] : Res O 1 ≃+* ResidueField O := Ideal.quotEquivOfEq (pow_one (maximalIdeal O))
noncomputable def liftRes1 (v : Fin 3 → ResidueField O) : Coeff O 3 1 := fun i => (res1Equiv O).symm (v i)
```

**Faithfulness.** `Res O 1 = O ⧸ 𝔪¹` and `ResidueField O = O ⧸ 𝔪` are the same ring; `res1Equiv`
is the canonical identification and `res1Equiv_mk` (proved) says it commutes with the quotient
maps, which is the only property used. `liftRes1` is therefore a bijection onto the level-1
classes, and `resVec_of_proj` proves that every lift of `liftRes1 v` has residue vector exactly
`v` — the link between the residue-field counts and the level-1 decided classes.

---

## 4. FILE MAP

| file | lines | content |
|---|---|---|
| `UniformityCheck/N3Base.lean` | 220 | the five types + distinctness, **`typeOf_mul`**, `cubic_expand/_eval/_deriv_eval`, `peel` + **`cubic_peel`** + `peel_const`, `exists_cubic_root` (Hensel), `irreducible_of_map_irreducible` |
| `UniformityCheck/N3Certs.lean` | 325 | **`typeOf_linInert_of_residue`**, **`typeOf_linRam_of_residue`**, `exists_quad_root`, **`typeOf_split3_of_residue`**, **`typeOf_split3_of_deep`** |
| `UniformityCheck/N3Norm.lean` | 444 | `cub3` and the cubic power basis, `cubMat` + **`norm_cub`**, `norm_root_cub`, `residue_norm_cub`, `isUnit_norm_of_unit_coeff`, `three_dvd_of_addVal_norm`, **`typeOf_ram3_of_eisenstein`**, **`typeOf_inert3_of_residue_irreducible`** |
| `UniformityCheck/CubicCount.lean` | 346 | **`three_mul_card_noRootCubic`** (`3·#irred + q = q³`), **`two_mul_card_linAni`** (`2·#linAni + q² = q³`) |
| `UniformityCheck/SplitCount.lean` | 203 | **`six_mul_card_split3`** (`6·#split + 3q² = q³ + 2q`, i.e. `C(q,3)`) |
| `UniformityCheck/N3Density.lean` | 276 | `proj3_dvd`, **`decidedDensity_ge_of_inj`**, **`bracket_five`**, `unitSet` + stratum counts, `gate_linRam_lower`, `gate_split3_lower`, `gate_linInert_lower` |
| `UniformityCheck/N3Gates.lean` | 459 | `res1Equiv`/`liftRes1`, `inert3_decided`, `linInert_decided_res`, `ram3_decided`, the five sharpened lower bounds, **`lowers_three`**, **`gate_bracket3_padic_two/_three`**, **`gate_bracket3_hmenu3_two/_three`** |
| `UniformityCheck/N3Struct.lean` | 286 | **(N3B)** `cubeCoeff`, **`cubic_trichotomy`**, `cubeCoeff_injective`, **`cubic_structure`**, `proj_of_dvd`, **`exists_root_congr`**, `peel_congr`, **`decidedAt_of_peel`** |
| `UniformityCheck/N3Drain.lean` | 370 | **(N3B)** `peelSet`/`tripleSet`/`tripleRes`, `proj_one_eq_liftRes1`, **`card_peelSet_le`**, **`card_tripleSet_le`**, **`undecidedSet_subset`**, `tripleUndecidedSeq`, `undecidedSeq_three_le`, **`genuineDensity_three_le`**, **`drainage_three_of_triple`** |
| `UniformityCheck/N3Exact.lean` | 178 | **(N3B)** `linQuadCoeff`, **`inert3_decided_iff`**, `decidedSet_inert3_one`, **`decidedCount_inert3_one`**, **`decidedSeq_inert3_one`**, the four `ℤ_[2]`/`ℤ_[3]` rungs |
| `verification/n3_family_check.py` | — | brute-force family census + bracket check (§6) + **(N3B)** Part 3, the structure theorem (§11) |

Total 2 273 new Lean lines at N3, **plus 834 at N3B**; **zero `sorry`s** in either
(`grep -rn sorry leancheck/UniformityCheck/` returns nothing) and no `axiom` command anywhere in
the package. `lake build` from `leancheck/` green (8 590 jobs after N3B).

---

## 5. THE BRACKET TABLES vs HMENU3

The corpus's predicted cubic densities (HMENU3 COROLLARY HM3.D, `Φ = q⁴+q³+q²+q+1`):

    R_{(1,1)³}      = q³(q²−q+1) / (6(q+1)Φ)
    R_{(1,2),(1,1)} = q³(q²+q+1) / (2(q+1)Φ)
    R_{(1,3)}       = q³(q+1) / (3Φ)
    R_{(2,1),(1,1)} = q(q³+q+1) / ((q+1)Φ)
    R_{(3,1)}       = (q²+1) / Φ

**`q = 2` (`gate_bracket3_padic_two`, `gate_bracket3_hmenu3_two`):**

| σ | lower (proved) | HMENU3 | upper (proved) | contained |
|---|---|---|---|---|
| `{(1,1)}³` | 1/512 ≈ 0.00195 | 4/93 ≈ 0.04301 | 3/8 = 0.375 | ✔ machine-checked |
| `{(1,1),(1,2)}` | 1/4 = 0.25 | 28/93 ≈ 0.30108 | 319/512 ≈ 0.62305 | ✔ |
| `{(1,3)}` | 1/4 = 0.25 | 8/31 ≈ 0.25806 | 319/512 ≈ 0.62305 | ✔ |
| `{(1,1),(2,1)}` | 1/16 = 0.0625 | 22/93 ≈ 0.23656 | 223/512 ≈ 0.43555 | ✔ |
| `{(3,1)}` | 1/16 = 0.0625 | 5/31 ≈ 0.16129 | 223/512 ≈ 0.43555 | ✔ |

**`q = 3` (`gate_bracket3_padic_three`, `gate_bracket3_hmenu3_three`):**

| σ | lower (proved) | HMENU3 | upper (proved) | contained |
|---|---|---|---|---|
| `{(1,1)}³` | 1/27 ≈ 0.03704 | 63/968 ≈ 0.06508 | 8/27 ≈ 0.29630 | ✔ machine-checked |
| `{(1,1),(1,2)}` | 1/3 ≈ 0.33333 | 351/968 ≈ 0.36260 | 16/27 ≈ 0.59259 | ✔ |
| `{(1,3)}` | 8/27 ≈ 0.29630 | 36/121 ≈ 0.29752 | 5/9 ≈ 0.55556 | ✔ |
| `{(1,1),(2,1)}` | 4/81 ≈ 0.04938 | 93/484 ≈ 0.19215 | 25/81 ≈ 0.30864 | ✔ |
| `{(3,1)}` | 2/81 ≈ 0.02469 | 10/121 ≈ 0.08265 | 23/81 ≈ 0.28395 | ✔ |

The `{(1,3)}` bracket at `q = 3` is the tightest: the proved lower bound `8/27 = 0.296296` and the
predicted value `36/121 = 0.297521` differ by `0.0012`. The `{(1,1),(1,2)}` bracket at `q = 3` is
next (`0.33333` vs `0.36260`). Both are level-1 families, so the residual gap is exactly the mass
the corpus attributes to the DEEPER strata of those types — a quantitative, machine-checked
consistency check on HMENU3's deep-family laws, not merely on its level-0 rows.

`hmenu3_sums_to_one` additionally checks (by `norm_num`) that the five predicted values sum to
exactly 1 at `q = 2` and `q = 3` — an arithmetic audit of the corpus table itself, independent of
anything proved here.

---

## 6. NUMERIC CROSS-CHECK

`leancheck/verification/n3_family_check.py` (stdlib only, 6 s, exits `ALL CHECKS PASSED`; run it
with `cd leancheck/verification && python3 n3_family_check.py`).

### 6a. The certificate families, brute force vs closed form (54/54 rows OK)

Each family is enumerated over `Z/pᴺ` **exactly as the Lean defines it** and compared with the
closed form the Lean proves, for `p ∈ {2,3,5}` and `N ∈ {1,2,3}`:

| family (Lean name) | closed form at level `N` | `p=2, N=3` | `p=3, N=3` | `p=5, N=3` |
|---|---|---|---|---|
| `F_I` = residually irreducible (`c3inert`) | `((p³−p)/3)·p^{3(N−1)}` | 128 | 5832 | 625000 |
| `F_LI` = linear × rootless quadratic (`c3linInert`) | `(p²(p−1)/2)·p^{3(N−1)}` | 128 | 6561 | 781250 |
| `F_S3` = three distinct residue roots (`c3split`, level 1) | `C(p,3)·p^{3(N−1)}` | 0 | 729 | 156250 |
| `F_LR` = linear × Eisenstein quadratic (`c3linRam`) | `p²(p−1)²·p^{3(N−2)}` | 32 | 972 | 50000 |
| `F_R` = Eisenstein cubic (`c3ram`) | `p²(p−1)·p^{3(N−2)}` | 32 | 486 | 12500 |
| `F_Sdeep` = the class of `X(X−1)(X−p)` (`c3split`, level 3) | `p^{3(N−3)}` | 1 | 1 | 1 |

Every row matches, at all three primes including the two wild ones, and the six families are
checked pairwise disjoint. `F_S3` is `0` at `p = 2`, confirming that the split type genuinely has
no level-1 certificate over `𝔽₂` and that `F_Sdeep` is not a redundancy.

### 6b. The brackets, recomputed independently

The script recomputes the bracket table of §5 in exact rational arithmetic (and also at `q = 5`,
where the pattern continues) and confirms containment of every predicted `R_σ`, with no failure.
An independent check run by hand at `q = 2, 3, 5, 7` agrees.

### 6c. An honest Ore/Newton decider (Part 2 of the script)

Beyond the certificate families, the script implements a real recentring Newton-polygon decider,
sound by construction (finite-precision valuations tracked per coefficient; anything the data does
not force is declared UNDECIDED). It reaches `p = 2, N ≤ 8` and `p = 3, N ≤ 5`. The certified
fractions rise toward the predicted densities and **never exceed them**; at `p = 3, N = 5`:

    {(1,3)}        certified 0.297514   vs predicted 36/121  = 0.297521
    {(1,1),(1,2)}  certified 0.362      vs predicted 351/968 = 0.362603
    {(1,1)}³       certified 0.0646     vs predicted 63/968  = 0.065083

and the undecided fraction falls monotonically (`p=2`: 50% → 18.75% → 9.4% → 4.7% → 2.1% → 1.07%
→ 0.54% → 0.26%; `p=3`: 33% → 8.6% → 2.9% → 0.96% → 0.31%). This is an independent numerical
confirmation of all five HMENU3 values, *and* independent evidence that drainage at `n = 3` really
does happen — the thing §7 says is not proved.

Validation of the decider itself (all OK): orbit-representative counting agrees with full
enumeration (5 cases); no level-to-level contradiction (a class certified `σ` at level `N` never
has a level-`N+1` subclass certified `≠ σ`); all six Part-1 families are certified with exactly
their claimed types (an independent check on the `(e,f)` ordering convention of §3 and on the
`AniK`-sign bookkeeping); and no squarefree-residue class is left undecided.

### 6d. ⚠ A reconciliation item for the corpus side (reported, not resolved)

The decider's TOTAL undecided count does not agree with HEX3's `U₃^conv(q,N) = q·u(q,N)`; e.g. at
`p = 2, N = 7` the corpus value 11 328 EXCEEDS the decider's 11 264. This is **not** by itself a
contradiction — HEX3.B itself asserts `U₃^σ ≤ U₃^conv`, i.e. the conservative reader over-counts
by design, and this decider is a sharper reader, so both are upper bounds on the true undecided
count and the corpus's being larger is expected. What it does mean is that the two numbers are
**not measuring the same set**, so these runs neither confirm nor refute THEOREM HEX3.A. Two
further data points, recorded as found and NOT tuned: (i) restricted to the triple-root branch
(residue `= (X−ρ)³`) the decider's count equals `u(q,N)` exactly at `N = 2, 3` for both `q = 2, 3`
(4, 16 and 9, 81) but departs at `N = 1` and at `N = 4`, where the corpus's extra term
`(q−1)²q³` is absent because this decider certifies those classes; (ii) the complementary branch
(residue `= (X−ρ)²(X−τ)`) contributes exactly `q(q−1)·q^{2N−2}` undecided classes in every case
computed. Reconciling the two readers' conventions (per-centre vs whole-space, and the
`B₀ = 0 in-window ⟹ UNDECIDED` exit) is a task for whoever next touches HEX3; nothing in this
unit depends on it.

---

## 7. `class_pinned` AT `n = 3` — THE FEASIBILITY VERDICT (reconnaissance)

> ⚠ **SUPERSEDED 2026-08-13 by unit N3B — read §9.1 and §9.4 instead.** This section's central
> diagnosis, *"branch (ii) is blocked on a Hensel coprime-factorisation lemma"*, is **WRONG**,
> as `leanfinal/notes/HENSEL_FACT_2026-08-13.md` §4.5/§7 first suspected and as §9.1 below now
> proves: the residual shape `(X − ρ̄)(X − c̄)²` has a SIMPLE root `ρ̄`, so the ordinary ROOT form
> of Hensel peels it, and §9.2's `decidedAt_of_peel` closes branch (ii) with no loss of level.
> Two further corrections. (i) The branch split is not "triply tangent vs. one genre" but "no
> simple residual root vs. simple residual root"; the no-simple-root side is exactly
> {residually irreducible} ∪ {residually a perfect cube} (`cubic_trichotomy`). (ii) The tool the
> REMAINING branch is short of is **strong Hensel** (`v(F(γ)) > 2v(F′(γ))` with `v(F′(γ)) > 0`),
> NOT the factorisation form — see §9.4. What survives from this section unchanged: the branch-(i)
> Taylor computation, and the naming of `cert_of_not_tang` / `decidedAt_of_not_tang` /
> `class_pinned` / `undecidedCount_le` as the `n = 2` machinery being generalised.

**Verdict in one line: the pinning ARITHMETIC generalises cleanly and gives drainage, but a single
`class_pinned` analogue is FALSE at `n = 3`; two branches are needed, and the second branch is
blocked on a Hensel *coprime-factorisation* lemma that neither `leanfinal` nor (readily) Mathlib
provides.**

**What `n = 2` does** (the precise statements, not a gesture: `Drainage.cert_of_not_tang`,
`Drainage.decidedAt_of_not_tang`, `Drainage.class_pinned`, `Drainage.undecidedCount_le`).
`decidedAt_of_not_tang` is the one that makes "one cause" precise: if NO centre `γ` satisfies
`Tang π a N γ` (`π^N ∣ F(γ)` and `π^{⌈N/2⌉} ∣ F'(γ)`) then the class IS decided; contrapositively,
every undecided level-`N` class has a centre of tangency depth `N`, which is the quantitative
hypothesis `class_pinned` consumes. `class_pinned` says: two data `(a, γ)`, `(a', γ')` with
tangency depth `2M` (`π^{2M} ∣ F(γ)`, `π^M ∣ F'(γ)`), with `γ ≡ γ' mod π^M` and
`a₁ ≡ a₁' mod π^{2M}`, have `a₀ ≡ a₀' mod π^{2M}`. So the undecided classes at level `2M` inject
into `(γ mod π^M) × (a₁ mod π^{2M})`, giving `undecidedCount ≤ q^{3M}` out of `q^{4M}`. The
mechanism is that `F(γ+δ) = F(γ) + F'(γ)δ + δ²` and BOTH error terms are killed by the tangency
hypothesis once `v(δ) ≥ M`. Its force comes from `cert_of_not_tang`: at `n = 2` "undecided" has
exactly ONE cause, a near-double root, and the certificate trichotomy is exhaustive.

**Branch (i) — the triple-tangency stratum: the arithmetic works.** At `n = 3` recentre
`f(X+γ) = X³ + B₂X² + B₁X + B₀` with `B₂ = 3γ + a₂`, `B₁ = 3γ² + 2a₂γ + a₁`, `B₀ = f(γ)`. On the
TRP stratum (`f̄ = (X − c̄)³`, HEX3 §S1's undecided locus) the natural depth-`N` tangency is
`v(B₀) ≥ N`, `v(B₁) ≥ ⌈2N/3⌉`, `v(B₂) ≥ ⌈N/3⌉` — the slope-`1/3` Newton condition. Taking
`δ = π^m s` with `m = ⌈N/3⌉`, the Taylor expansion
`B₀(γ+δ) = B₀ + B₁δ + B₂δ² + δ³` has all three error terms of valuation `≥ N`
(`2N/3 + N/3`, `N/3 + 2N/3`, `3·N/3`), so `(γ mod π^{⌈N/3⌉}, a₂ mod π^N, a₁ mod π^N)` pins
`a₀ mod π^N`. That is an injection into a set of size `q^{N/3} · q^{2N}` out of `q^{3N}`: a
saving of `q^{2N/3}`, enough for drainage. This branch is a direct, characteristic-free Taylor
computation and would formalise at roughly the length of `class_pinned` itself.

**Branch (ii) — why one branch does not suffice.** A cubic can be undecided while NO centre is
triply tangent. Take `f̄ = (X − ρ̄)(X − c̄)²` with `ρ̄ ≠ c̄`: at the centre `c` one has
`B̄₂ = 3c̄ + ā₂ = c̄ − ρ̄ ≠ 0`, so the depth-`N` tangency above fails at every centre, yet the class
is undecided exactly when the quadratic block `(X − c)²`-part is `n = 2`-undecided. This is
precisely HEX3's "β re-entry" family, and it is why HEX3's law
`u(q,N) = q^{2N−2} + (q−1)Σ_k q^{4k−1}(q^{N−3k}−1)R(N−3k)` carries the `n = 2` block laws `R(M)`
as factors. The pinning for this branch is: the Hensel root `r` of the separable linear factor is
determined by the class (`q^N` choices), and the quadratic cofactor is pinned by `n = 2`'s own
`class_pinned` (`q^{3N/2}`), total `q^{5N/2}` out of `q^{3N}` — also draining, at rate `q^{−N/2}`.

**The missing mechanism.** Branch (ii) needs, as a *theorem*, the statement "undecided at level
`N` and not triply tangent ⟹ `f` factors as (decided linear) × (quadratic), with the factorisation
determined by the class". That is Hensel's lemma in its FACTORISATION form — lift a coprime
residual factorisation `f̄ = ḡ · h̄` to `f = g · h` — which
* `leanfinal` does not have (it only has the ROOT form, `HenselianLocalRing.is_henselian`), and
* Mathlib does not obviously have for a general henselian local ring (grep found only the root
  form and `HenselianLocalRing.TFAE`).
This unit's certificates dodge the issue because they always have a *simple residue root* to
Hensel at, and then peel by the factor theorem; that dodge is unavailable for a general undecided
class, where the residual repeated factor may be the quadratic one.

**Recommendation for the eventual general-`n` drainage design.** Either
(a) prove the coprime-factorisation Hensel lemma once (it is the reusable object: at degree `n`
the induction "separable residual factor ⟹ split off and recurse" is the whole drainage
skeleton), or
(b) route drainage through the DISCRIMINANT instead: `{v(disc f) ≥ N}` is a hypersurface
neighbourhood whose measure is easy to bound (`disc` is quadratic in `a₀`, so each fibre
contributes `O(q^{−N/2})`), and the required implication "small `v(disc)` ⟹ decided" is a single
Krasner-type statement, independent of `n`. Route (b) trades one hard lemma for another but the
counting side becomes trivial and uniform in `n`; route (a) matches HMENU3/HEX3's own recursive
architecture and would be reusable by the menu side. **No evidence was gathered in this unit for
which is cheaper; that comparison is itself the next reconnaissance task.**

---

## 8. STATUS

### PROVED (landed, sorry-free, general `O`, Lean-core axioms only)

* the five cubic types with pairwise distinctness and degree 3;
* `typeOf` additivity on monic products, and the division-free root peel;
* the cubic norm form over an arbitrary commutative ring, its reduction compatibility, and the
  two intrinsic-type theorems it unlocks (Eisenstein ⟹ `{(3,1)}`; residually irreducible ⟹
  `{(1,3)}`);
* five decidedness certificates covering all five cubic types, with no residue-characteristic
  hypothesis;
* the residue-field counts `#{irreducible monic cubics} = (q³−q)/3` and
  `#{linear × irreducible quadratic} = q²(q−1)/2`, both characteristic-uniform;
* the five density lower bounds at general `O`, the 5-type bracket engine, the certified brackets
  at `q = 2` and `q = 3`, and machine-checked containment of all ten HMENU3 values.

### NOT DONE / OPEN

* **Drainage at `n = 3` (`UndecidedVanishes O 3 σ`) — NOT attempted.** §7 is the reconnaissance
  deliverable in its place. Consequently `genuineDensity O 3 σ = decidedDensity O 3 σ` is NOT
  available at `n = 3`, and **every bound in this unit is a bound on the DECIDED density**, i.e.
  on `⨆_N decidedSeq`, not (formally) on a measure.
* **No exact `n = 3` values.** Unlike the `n = 2` unit, which closed the brackets to points, this
  unit only brackets. Closing them needs both drainage AND the deeper families of HMENU3's menu
  (the `DEEP-X`, `B1/B2/B3` tiers), none of which is certified here.
* **The split type has no nonvacuous LEVEL-1 bound at `q = 2`**, so its general-`O` bound is the
  deep single class `1/q⁹` — three orders of magnitude below the predicted `4/93`. Closing that
  gap needs the deeper split families of HMENU3's menu, not just a bigger level-1 count.
* **The `(e,f)` ordering convention** in the five type definitions is a trust-boundary item
  flagged in §3; it is pinned by `leanfinal`'s own `ramType`/`inertType` usage but deserves a
  human read.
* **An independent adversarial (Codex) pass WAS run** on the six certificate statements, the
  three count claims, the numerator/denominator pairing of the five density bounds, the bracket
  arithmetic at `q = 2, 3`, the vacuity of the level-1 split family at `q = 2`, and the §7
  reconnaissance. Verdict: all six certificates **clean**, all counts **clean**, all bracket
  arithmetic **clean**, no lower bound exceeds its predicted value, and the `ℤ_2` deep split class
  independently re-derived (strong Hensel at `0` where `v(f(0)) ≥ 3 > 2 = 2·v(f'(0))`, ordinary
  Hensel at `1` where `f'(1)` is odd, third root `≡ 2 mod 4`). ONE finding, a (G) justification gap
  in the PROSE of §7 — "at `n = 2` … so a single pinning argument suffices" was gesturing rather
  than citing; §7 has been amended to name `cert_of_not_tang` / `decidedAt_of_not_tang` /
  `class_pinned` / `undecidedCount_le` explicitly. No finding touched any Lean statement.
* **`leanfinal`'s own notes are NOT updated.** This unit is fenced out of `leanfinal/`.

---

# PART II — UNIT N3B (2026-08-13): the structure theorem, branch (b), drainage

*Appended by the follow-on unit `UNIFORMITY-CHECK-N3B`, unblocked by
`leanfinal/notes/HENSEL_FACT_2026-08-13.md`. §§9–12 are new; §7 above is superseded by §9.1 and
§8's "NOT DONE" list is superseded by §12. Informal arguments were written here BEFORE the Lean.*

## 9. THE MATHEMATICS OF PART II (written before the Lean)

### 9.1 The residue trichotomy — the enumeration §7 asked for, re-derived and CORRECTED

**THEOREM (trichotomy).** Let `K` be a field and `F = X³ + c₂X² + c₁X + c₀ ∈ K[X]`. Exactly one
of the following holds:

* **(T1)** `F` has a SIMPLE root in `K`: some `ρ` with `F(ρ) = 0` and `F′(ρ) ≠ 0`;
* **(T2)** `F` has NO root in `K` (equivalently, `F` is irreducible over `K`);
* **(T3)** `F = (X − γ)³` for a (unique) `γ ∈ K`.

*Proof.* Suppose `F` has a root but no simple root, and let `ρ` be a root; then `F′(ρ) = 0`.
Peel: `F = (X − ρ)(X² + bX + d)` with `b = c₂ + ρ`, `d = c₁ + c₂ρ + ρ²` (this is `peel`).
Differentiating the product, `F′(ρ) = ρ² + bρ + d`, so the cofactor also vanishes at `ρ`, hence
`F = (X − ρ)²(X − s)` with `s = −c₂ − 2ρ`. Now `F(s) = 0` and `F′(s) = (s − ρ)²`. If `s ≠ ρ`
then `s` is a simple root — contradiction. So `s = ρ`, i.e. `c₂ = −3ρ`, and then `c₁ = 3ρ²`,
`c₀ = −ρ³`, i.e. `F = (X − ρ)³`. Uniqueness of `γ`: from `3γ = 3γ′`, `3γ² = 3γ′²`, `γ³ = γ′³`
one gets `(γ − γ′)³ = γ³ − 3γ²γ′ + 3γγ′² − γ′³ = γ³ − 3γ′³ + 3γ′³ − γ′³ = 0`. ∎

Everything is a `ring` identity; **no characteristic hypothesis is used**, so the wild primes
`2` and `3` are covered (in characteristic `3`, (T3) reads `F = X³ − γ³` with `c₂ = c₁ = 0`).

**Consequence — the Hensel note's retraction is CONFIRMED, in a stronger form.** `N3_CHECK` §7
diagnosed branch (ii) as blocked on a coprime-factorisation Hensel lemma. It is not: by (T1)–(T3)
the only residual shapes WITHOUT a simple root are "irreducible" and `(X − γ̄)³`, and the first is
already decided at level 1 by `typeOf_inert3_of_residue_irreducible`. So every undecided class
that is not residually a perfect cube has a SIMPLE residual root, and the ROOT form of Hensel
peels it. **No third genre exists.** Two refinements of the Hensel note's phrasing:

* the note said the only undecided-not-triply-tangent genre is `(X − ρ̄)(X − c̄)²`. That is true
  but it is *not* a consequence of the enumeration alone: the shapes "three distinct roots" and
  "linear × irreducible quadratic" also have simple roots and are excluded only because this
  repo's own level-1 certificates DECIDE them. The enumeration-only statement is the weaker and
  cleaner "has a simple residual root".
* the peel below therefore does not need the `(X − ρ̄)(X − c̄)²` shape at all: one theorem covers
  all three simple-root shapes.

### 9.2 Branch (b): the peel reduces `n = 3` decidedness to `n = 2` decidedness, LOSSLESSLY

Write `F_a = monicPoly a = X³ + a₂X² + a₁X + a₀`, and `g = peel a r = ![a₁ + a₂r + r², a₂ + r]`
for the quadratic cofactor at a root `r` (`cubic_peel`: `F_a = (X − r)·monicPoly g`, a `ring`
identity).

**LEMMA B (branch-(b) decidedness).** Let `N ≥ 1`, let `r ∈ O` satisfy `F_a(r) = 0` with
`F_a′(r)` a UNIT, and suppose the quadratic cofactor `g = peel a r` has NO centre of tangency
depth `N` (`¬ ∃ γ, Tang π g N γ`). Then the level-`N` class of `a` is decided, with type
`{(1,1)} ∪ typeOf (monicPoly g)`.

*Proof.* Let `a′` be any lift of the same class, so `π^N ∣ a′ᵢ − aᵢ` for each `i`.

1. `E := F_{a′}(r) = F_a(r) + Σᵢ (a′ᵢ − aᵢ)rⁱ ∈ π^N O`.
2. `F_{a′}′(r) − F_a′(r) = 2(a′₂ − a₂)r + (a′₁ − a₁) ∈ 𝔪` (here `N ≥ 1` is used), so
   `F_{a′}′(r)` is a unit too.
3. Ordinary Hensel at `r` (mathlib's root form, via `exists_cubic_root`) gives `r′` with
   `F_{a′}(r′) = 0` and `δ := r′ − r ∈ 𝔪`.
4. **The quantitative step, and it is elementary.** Expanding the cubic exactly,
   `0 = F_{a′}(r + δ) = E + δ·(F_{a′}′(r) + (3r + a′₂)δ + δ²)`. The bracket is a unit + `𝔪`,
   hence a unit; so `δ·(unit) = −E ∈ π^N O`, whence `π^N ∣ δ`. **No digits are lost.**
   *(This is the finite-level class-determinacy that `HENSEL_FACT` §7 item 2 asked for, in the
   only case branch (b) needs. It does not require re-running the Newton iteration, because the
   derivative is a unit: the correction is first-order exactly.)*
5. Hence `peel a′ r′ ≡ peel a r (mod π^N)` coefficientwise (`peel` is a polynomial in `a` and `r`).
6. `leanfinal`'s `decidedAt_of_not_tang` says the level-`N` class of `g` is
   `typeOf (monicPoly g)`-decided; by 5, `typeOf (monicPoly (peel a′ r′)) = typeOf (monicPoly g)`.
7. `typeOf` is additive on monic products (`typeOf_mul`), so
   `typeOf F_{a′} = (1,1) ::ₘ typeOf (monicPoly (peel a′ r′)) = (1,1) ::ₘ typeOf (monicPoly g)
   = typeOf F_a`. ∎

### 9.3 The count: undecided ⊆ peel-tangent ∪ triple-root, and the two bounds

**STRUCTURE (drainage dichotomy).** For `N ≥ 1`,

    undecidedSet O 3 N  ⊆  peelSet π N  ∪  tripleSet π N

where `peelSet π N = {c | some lift a of c has a root r with Tang π (peel a r) N γ for some γ}`
and `tripleSet π N = {c | some lift a of c is residually (X − γ)³ for some γ}`. *Proof.* Apply
the trichotomy to the residue of a lift `a`. (T2) ⟹ decided at level 1, hence at level `N`,
contradicting undecidedness. (T1) ⟹ Hensel gives `r` with `F_a(r) = 0` and unit derivative; if
`peel a r` had no tangent centre, Lemma B would decide the class — so it has one, i.e.
`c ∈ peelSet`. (T3) ⟹ `c ∈ tripleSet`. ∎

**BOUND 1 (branch b).** `#(peelSet π (2M)) ≤ q^{5M}`, out of `q^{6M}` classes.
*Proof.* A cubic with a root `r` and cofactor `g` has `a = ![−r g₀, g₀ − g₁ r, g₁ − r]`, so the
class is a function of `(r, g₀, g₁) mod π^{2M}`. Map `c ↦ (r mod π^{2M}, γ mod π^M, c₂)`, where
`γ` is a tangency centre of depth `2M` for `g`. If two classes have the same image then
`g₁ = a₂ + r` agrees mod `π^{2M}`, and `leanfinal`'s `class_pinned` (exported completeness-free
as `class_pinned'` in `DeepSet.lean`) forces `g₀` to agree mod `π^{2M}` — so the classes are
equal. The target has `q^{2M}·q^M·q^{2M} = q^{5M}` elements. ∎

**BOUND 2 (triple-root stratum).** `#(tripleSet π N) ≤ q^{3N−2}` for `N ≥ 1`: the level-1
truncation of such a class is `(X − γ̄)³`, and `γ̄` is unique, so at most `q` of the `q³` level-1
classes are hit; each has `q^{3(N−1)}` refinements.

Hence `undecidedSeq O 3 (2M) ≤ q^{−M} + q^{−2}`, and therefore for every `σ`

    genuineDensity O 3 σ  ≤  decidedDensity O 3 σ  +  1/q²  ,

i.e. **the `n = 3` bracket between the certified (decided) density and the possible density
closes to within `1/q²`** — `1/4` at `q = 2`, `1/9` at `q = 3`. Full drainage is exactly the
statement that the *triple-root* part of the undecided set drains; branch (b) is finished.

### 9.4 What full `n = 3` drainage still needs (the honest gap, diagnosed)

On the triple-root stratum, recentre at a lift `c` of `γ̄`: `F(X + c) = X³ + B₂X² + B₁X + B₀`
with all `Bᵢ ∈ 𝔪`. Reading the Newton polygon of `(B₀, B₁, B₂, 1)`:

| polygon | verdict | tool status |
|---|---|---|
| `v(B₀) = 1` | Eisenstein, `{(3,1)}` | HAVE (`typeOf_ram3_of_eisenstein`) — but needs typeOf SHIFT-invariance to use at `c ≠ 0` |
| `v(B₀) = t ≢ 0 (3)`, single side | `e = 3`, `{(3,1)}` | provable by the same norm route (`CertRam3`, not landed) |
| single side of INTEGRAL slope `m` | rescale `X = π^m Y`, recurse on the residual cubic | needs typeOf SCALE-invariance |
| **two or three sides** | a root of `F` in `O` whose derivative is NOT a unit | **BLOCKED: needs STRONG HENSEL** |

**The blocking tool is strong Hensel, not the factorization form.** Precisely: `F` monic with
`v(F(x₀)) > 2·v(F′(x₀)) = 2w`, `w ≥ 1` ⟹ `∃ r, F(r) = 0 ∧ v(r − x₀) > w`. Two observations
recorded for the next unit:

* mathlib has this only for `ℤ_[p]` (`hensels_lemma` in `NumberTheory/Padics/Hensel.lean`), not
  for a general complete DVR; `HenselianLocalRing.is_henselian` is the `w = 0` case only.
* **`Uniformity.Hensel.exists_monic_factorization` does NOT supply it.** After recentring, the
  residual factorization is `X · X²`, which is not coprime; and the scalings that separate the
  polygon sides are non-integral, so no monic model exists to feed the factorization form. The
  factorization form remains the right tool for `n ≥ 4` (two repeated residual blocks), as its
  own note says — it is simply not what the `n = 3` triple-root branch is short of.
* degree 2 escapes this because `F(γ + π^w y)/π^{2w}` is monic when `deg F = 2`
  (`typeOf_of_certSplit`'s trick); at degree 3 the same division leaves leading coefficient
  `π^w`, i.e. a NON-MONIC polynomial with a simple residual root — the general statement mathlib
  also lacks. Strong Hensel and non-monic Hensel are the same missing lemma.

Once strong Hensel exists, the triple-root branch closes by the recursion
`u(N) ≤ q^{−N/2} + q^{−5}·u(N − 3)` (each round consumes three digits of level and pins the
centre), which gives drainage; that estimate is written out here so the next unit can check it
against its own bookkeeping, but it is NOT proved.

---

## 10. WHAT LANDED (unit N3B)

834 new Lean lines in three files, **zero `sorry`s, zero `axiom` commands**, every declaration's
`#print axioms` footprint exactly `[propext, Classical.choice, Quot.sound]` (printed by the
`AxCheck` blocks at the foot of each file; `lake build` from `leancheck/` green, 8 590 jobs).
Every statement is at the STRONGEST rung — general complete DVR `O` with finite residue field,
no residue-characteristic hypothesis — except the four explicitly `ℤ_[p]` corollaries.

### 10.1 `N3Struct.lean` — the structure theorem and the peel

| name | statement (abbreviated) | rung |
|---|---|---|
| `cubic_trichotomy` | over any field `K`: simple root ∨ `NoRootCubic` ∨ `∃ γ, c = cubeCoeff γ` | any field |
| `cubic_trichotomy_disjoint` | the three branches are pairwise exclusive | any field |
| `cubeCoeff_injective` | `(X−γ)³ = (X−γ′)³ ⟹ γ = γ′` | any field |
| `cubic_structure` | over `O`: `(∃ r, F(r)=0 ∧ IsUnit F′(r)) ∨ NoRootCubic (resVec a) ∨ (∃ γ, resVec a = cubeCoeff (residue γ))` | general `O` |
| `exists_root_congr` | `F_a(r)=0`, `F_a′(r)` a unit, `a′ ≡ a mod π^N`, `N ≥ 1` ⟹ `∃ r′, F_{a′}(r′)=0 ∧ π^N ∣ r′−r` | general `O` |
| `peel_congr` | the cofactors then agree mod `π^N` | general `O` |
| **`decidedAt_of_peel`** | `F_a(r)=0`, `F_a′(r)` unit, `¬∃γ Tang π (peel a r) N γ` ⟹ `DecidedAt O 3 (typeOf (monicPoly a)) N (proj O 3 N a)` | general `O` |

`cubic_structure` is the Lean form of the corrected §7 enumeration. Note that the first branch
is stated over `O` with the root ALREADY Hensel-lifted and its derivative already known to be a
unit — the residue-field bookkeeping is discharged inside the theorem, so the consumer never
touches the residue field.

### 10.2 `N3Drain.lean` — the counting and the two payoffs

| name | statement | rung |
|---|---|---|
| `card_peelSet_le` | `#(peelSet π (2M)) ≤ q^{5M}` | general `O` |
| `card_tripleSet_le` | `#(tripleSet N) · q² ≤ q^{3N}` (`N ≥ 1`) | general `O` |
| **`undecidedSet_subset`** | `undecidedSet O 3 N ⊆ peelSet π N ∪ tripleSet N` (`N ≥ 1`) | general `O` |
| `undecidedSeq_three_le` | `undecidedSeq O 3 (2M) ≤ (1/q)^M + tripleUndecidedSeq (2M)` | general `O` |
| `tripleUndecidedSeq_le` | `tripleUndecidedSeq N ≤ 1/q²` (`N ≥ 1`) | general `O` |
| **`genuineDensity_three_le`** | `genuineDensity O 3 σ ≤ decidedDensity O 3 σ + 1/q²`, every `σ` | general `O` |
| **`drainage_three_of_triple`** | `tripleUndecidedSeq (2M) → 0` ⟹ `UndecidedVanishes O 3 σ`, every `σ` | general `O` |
| `tripleUndecidedSeq_le_undecidedSeq` | the reduction's hypothesis measures a SUBSET of the undecided classes | general `O` |

`genuineDensity_three_le` is the unit's headline unconditional theorem: **at `n = 3` the bracket
between the certified density and the possible density is at most `1/q²` wide** — `1/4` at
`q = 2`, `1/9` at `q = 3` (`genuineDensity_three_padic_two/_three`). `drainage_three_of_triple` is
the headline reduction: branch (b) is discharged outright, so `UndecidedVanishes O 3 σ` now has
a single named obstruction, and it concerns residually-perfect-cube classes only.

⚠ Scope of `drainage_three_of_triple`: only the sufficient direction is proved. The converse
would need the `n = 3` analogue of `typeOf_two_cases` ("a monic cubic has one of exactly five
types"), which is NOT landed.

### 10.3 `N3Exact.lean` — the one exact `n = 3` value that is currently reachable

| name | statement | rung |
|---|---|---|
| **`inert3_decided_iff`** | a level-1 class is `{(1,3)}`-decided **iff** its residue cubic has no root | general `O` |
| `decidedSet_inert3_one` | `decidedSet O 3 c3inert 1 = liftRes1 '' {v | NoRootCubic v}` | general `O` |
| **`decidedCount_inert3_one`** | `3·decidedCount O 3 c3inert 1 + q = q³` | general `O` |
| **`decidedSeq_inert3_one`** | `decidedSeq O 3 c3inert 1 = (q³ − q)/(3q³)` | general `O` |
| `decidedSeq_inert3_one_padic_two/_three` | `= 1/4` at `q = 2`, `= 8/27` at `q = 3` | `ℤ_[2]`, `ℤ_[3]` |

The converse direction of `inert3_decided_iff` is the new content: a residual root `ρ̄` yields a
REDUCIBLE lift of the same class, namely `(X − ρ)(X² + β₁X + β₀)` with `(β₀, β₁)` any lift of the
residue peel, whose type contains the part `(1,1)` and hence is not `{(1,3)}`.

**On the charge's "exact values" ladder, this is the honest floor and the ceiling is far above
it.** An exact `n = 3` DENSITY is out of reach of this unit, for a structural reason worth
recording. `decidedDensity O 3 c3inert` is a supremum over ALL levels; the level-1 census above
is only its level-1 term, and the corpus's predicted density `q³(q+1)/(3Φ)` already exceeds it
(`8/31 ≈ 0.2581` vs `1/4` at `q = 2`; `36/121 ≈ 0.29752` vs `8/27 ≈ 0.29630` at `q = 3`), so
deep strata carry mass for every type. Worse, the natural parametrisation of a decomposable type
— `(r, g) ↦ (X − r)·g` — is **not** measure preserving: its Jacobian is `g(r)`, the resultant of
the two factors, which is exactly why `HMENU3`'s values carry the denominator
`Φ = q⁴+q³+q²+q+1` rather than being products of the `n = 2` values. So the hoped-for
"lin·inert and lin·ram decompose into linear × the exact `n = 2` values" does NOT hold: those
five values need the resultant-weighted integral, i.e. the deep menu, not the peel.

## 11. NUMERIC LEG FOR PART II (`verification/n3_family_check.py`, PART 3)

Added, all passing (`ALL CHECKS PASSED`, ~7 s):

* **trichotomy**, brute-forced over `F_p` for `p ∈ {2,3,5,7,11,13}`: for every one of the `p³`
  monic cubics, EXACTLY ONE of (T1) simple root / (T2) no root / (T3) perfect cube holds; the
  counts are `#T2 = (p³−p)/3`, `#T3 = p`, `#T1` the rest. Both wild primes included.
* **`cubeCoeff` injectivity**: no coefficient vector is a perfect cube in two ways.
* **the reducible lift** behind `inert3_decided_iff`: for every (residue class, root) pair the
  vector `(−ρB₀, B₀−ρB₁, B₁−ρ)` built from the residue peel reduces to the SAME class.
* **the triple stratum count**: exactly `p^{3N−2}` classes at level `N`, for `p ∈ {2,3,5}`,
  `N ≤ 3` — so `card_tripleSet_le`'s bound is sharp, not merely an over-estimate.

Also relevant, and already in §6c: the Part-2 decider reports **no squarefree-residue class is
left undecided** at any level computed. That is the empirical form of §9.1's corrected claim
(undecided ⟹ residue has a repeated root).

## 12. STATUS AFTER UNIT N3B (supersedes §8's "NOT DONE" list)

### Now PROVED (in addition to everything in §8's PROVED list)

* the residue trichotomy, with pairwise exclusivity, characteristic-free;
* branch (b) in full: the lossless peel and `decidedAt_of_peel`;
* `undecidedSet O 3 N ⊆ peelSet π N ∪ tripleSet N` with both strata counted;
* `genuineDensity O 3 σ ≤ decidedDensity O 3 σ + 1/q²` — unconditional, every `σ`, general `O`;
* `drainage_three_of_triple`;
* the exact level-1 `{(1,3)}` census and its exhaustiveness.

### Still OPEN, and now sharply named

1. **Drainage on the triple-root stratum** — the whole remaining obligation for
   `UndecidedVanishes O 3 σ`. §9.4 diagnoses it: the blocking tool is **strong Hensel**
   (`v(F(x₀)) > 2 v(F′(x₀))` with `v(F′(x₀)) > 0`), equivalently Hensel at a simple residual
   root for a NON-MONIC polynomial. Mathlib has it only for `ℤ_[p]`; the coprime-factorisation
   form does not supply it (§9.4 — though that is a statement about the tools now in the repo,
   not a proof that no indirect derivation exists). Two further prerequisites the recursion
   needs, neither landed: **shift-invariance** and **scale-invariance** of `typeOf`.
2. **No exact `n = 3` density**, for the structural reason in §10.3 (the peel parametrisation is
   resultant-weighted). The five `HMENU3` values remain bracketed, not proved.
3. **The `n = 3` analogue of `typeOf_two_cases`** ("exactly five types in degree 3") — needed
   for the converse of `drainage_three_of_triple` and for any `Σ = 1` identity at `n = 3`.
4. **The `(e,f)` ordering convention** (§8) and **`leanfinal`'s notes** (§8) remain as recorded.

### Adversarial pass on Part II (Codex, fresh context, 2026-08-13)

The three new files plus §9 were handed to Codex with a quote-and-classify verifier charge
(hunt vacuity, silent weakening, mis-binding, definitional traps, prose/statement mismatch; fix
nothing). **Verdict: no critical error.** Nine findings CLEAN — including an independent
non-vacuity witness for `decidedAt_of_peel` (`F = X(X²+1)` over `ℤ_[3]` at `r = 0`, `N = 1`), an
independent confirmation that `cubeCoeff_injective` needs no Frobenius surjectivity in
characteristic 3, an independent check of the `q^{5M}` target size and of the peel-set injection,
and agreement that `genuineDensity_three_le` is not implied by the trivial bounds. Four (G)
justification gaps, ALL ACTED ON, all prose-side:

* "exactly one" claimed for the trichotomy while Lean proved only the covering → the exclusivity
  is now LANDED as `cubic_trichotomy_disjoint`;
* `tripleSet 0` is everything (the residue cubic is not a class property at level 0) → docstring
  now carries the `N ≥ 1` caveat; every quantitative statement already assumed it;
* "drainage at `n = 3` is EXACTLY drainage on the triple-root stratum" overclaimed the converse →
  reworded to "follows from", and `tripleUndecidedSeq_le_undecidedSeq` was landed to show the
  hypothesis is not a disguised restatement of the conclusion;
* `N3Exact`'s header asserted a strict density gap that no theorem in the file proves → reworded
  as explicitly unproved orientation.

No finding touched a Lean statement's meaning; the two Lean additions
(`cubic_trichotomy_disjoint`, `tripleUndecidedSeq_le_undecidedSeq`) strengthen the file rather
than repair it.

---

## 13. THE MATHEMATICS OF PART III (unit N3C, written before the Lean)

Part III closes the triple-root stratum. The tools §9.4 named as missing landed as unit
`UNIFORMITY-P5` (`leanfinal/notes/P5_INFRA_2026-08-14.md`): strong Hensel
(`Uniformity.Hensel.strongHensel`), `typeOf` shift invariance (`typeOf_shift`) and the scale
relation (`typeOf_scale`). What was left, per P5 §7, was **the polygon case analysis and the level
bookkeeping (HYP.08)**. Both are done below, and the case analysis is CORRECTED: §9.4's table is
under-refined.

Throughout, `π` is a uniformizer, `q = #(O/𝔪)`, `F_a = monicPoly a = X³ + a₂X² + a₁X + a₀`, and
`v` is the `π`-adic valuation written as divisibility (`π^k ∣ x`).

### 13.1 Recentring, and the CORRECTED four-case split

**The recentred vector.** For `a : Fin 3 → O` and `γ ∈ O` put

    shiftVec a γ := ![F_a(γ), F_a′(γ), a₂ + 3γ]
                  = ![γ³ + a₂γ² + a₁γ + a₀, 3γ² + 2a₂γ + a₁, a₂ + 3γ] .

Three facts, all `ring` identities or one-liners:

* `monicPoly (shiftVec a γ) = (monicPoly a).comp (X + C γ)`, hence
  `typeOf (monicPoly (shiftVec a γ)) = typeOf (monicPoly a)` by P5's `typeOf_shift`;
* `shiftVec (shiftVec a γ) (−γ) = a` (recentring is invertible, coefficientwise);
* `π^N ∣ a′ᵢ − aᵢ` for all `i` ⟹ `π^N ∣ shiftVec a′ γ ᵢ − shiftVec a γ ᵢ` for all `i`
  (each component is a polynomial in `a` and `γ`). So recentring at a FIXED `γ` is a bijection
  of level-`N` classes.
* `(∀ i, shiftVec a γ i ∈ 𝔪) ↔ resVec a = cubeCoeff (residue γ)`: the triple-root stratum is
  exactly the recentred-into-`𝔪³` stratum. (Both directions are the three component identities
  `v₂ = −3g`, `v₁ = 3g²`, `v₀ = −g³`; no characteristic hypothesis, so wild `3` is included.)

**The split.** Let `B = shiftVec a γ` with all `Bᵢ ∈ 𝔪`, and write `α = v(B₀)`, `β = v(B₁)`,
`δ = v(B₂) ≥ 1`. Exactly one of:

| case | condition | verdict | tool |
|---|---|---|---|
| **E** | `π² ∤ B₀` (`α = 1`) | `c3ram = {(3,1)}` | Eisenstein (landed) |
| **L** | `π² ∣ B₀`, `π² ∤ B₁` (`α ≥ 2`, `β = 1`) | `c3linRam = {(1,1),(2,1)}` | **strong Hensel at `x₀ = πt`** |
| **R** | `π² ∣ B₁`, `π² ∣ B₀`, `π³ ∤ B₀` (`α = 2`, `β ≥ 2`) | `c3ram = {(3,1)}` | **no root + coprime norms** |
| **S** | `π³ ∣ B₀`, `π² ∣ B₁` (`α ≥ 3`, `β ≥ 2`) | recurse on the extraction | `typeOf_scale` |

The split is by construction exhaustive and disjoint (test `π² ∣ B₀`, then `π² ∣ B₁`, then
`π³ ∣ B₀`), and every condition is readable from `B mod π³` — so E, L, R are level-3 (hence
level-`N`, `N ≥ 3`) DECIDEDNESS certificates, not merely single-polynomial computations.

**Two corrections to §9.4's table.**

1. §9.4's row "`v(B₀) = t ≢ 0 (3)`, single side ⟹ `e = 3`" is **FALSE as stated** for `t = 2`
   without a hypothesis on `B₁`: at `α = 2, β = 1` the polygon of `(B₀,B₁,B₂,1)` has the two
   sides `(0,2)→(1,1)` (slope `−1`) and `(1,1)→(3,0)` (slope `−1/2`), so the type is
   `{(1,1),(2,1)}`, NOT `{(3,1)}`. Concretely `F = X³ + πX + π²` over `ℤ_[p]`: `F(−π) = −π³`, so
   strong Hensel at `x₀ = −π` (where `v(F) = 3 > 2 = 2·v(F′)`) returns a root of valuation `1`,
   whose cofactor `π + r²` is Eisenstein. The single-side reading needs `β ≥ 2`, which is case R's
   hypothesis. (The `q = 2, 3` numerics of PART 4 below check both readings on real classes.)
2. The row "two or three sides ⟹ a root whose derivative is not a unit" is exactly case L, and
   strong Hensel does NOT fire at `x₀ = 0` there (at `α = 2` one has `v(F(0)) = 2 = 2·v(F′(0))`,
   failing `k > 2w` by one). It fires at a SHIFTED centre `x₀ = πt` with `t` an explicit unit;
   §13.2 gives `t`.

No case ever needs the side structure of the polygon: case S's hypothesis is precisely the
legality of the `X = πY` extraction (all root valuations `≥ 1`), and E/L/R are what is left over.
That is why the analysis is four short arguments rather than a polygon algorithm.

### 13.2 The two new certificates

**CERT L (`typeOf_linRam_of_val_one`).** Let `π ∣ B₂`, `B₁ = πu` with `u` a unit (this is
`π ∣ B₁ ∧ π² ∤ B₁`), and `B₀ = π²C`. Then `typeOf (monicPoly B) = c3linRam`.

*Proof.* Put `t := −C·u⁻¹` and `x₀ := πt`. Writing `B₂ = πB₂′`,

    F(x₀) = π³t³ + B₂π²t² + πu·πt + π²C = π²(ut + C) + π³(t³ + B₂′t²) = π³(t³ + B₂′t²) ,

because `ut + C = 0` exactly. So `π³ ∣ F(x₀)`, i.e. `k = 3`. And

    F′(x₀) = 3π²t² + 2B₂πt + πu = π·(u + π(3t² + 2B₂′t)) ,

a unit times `π` (unit + `𝔪` is a unit in a local ring), so `w = 1` EXACTLY and `2w = 2 < 3 = k`.
Strong Hensel returns `r` with `F(r) = 0` and `π^{k−w} = π² ∣ r − x₀`; in particular `π ∣ r`.
Peel: `g = peel B r = ![B₁ + B₂r + r², B₂ + r]`. Then `π ∣ g₁`; and `g₀ = πu + B₂r + r²` with
`π² ∣ B₂r` and `π² ∣ r²`, so `π ∣ g₀` and `π² ∤ g₀` (else `π² ∣ πu`, i.e. `π ∣ u`). So `g` is an
Eisenstein quadratic: `typeOf (monicPoly g) = ramType = {(2,1)}` (`typeOf_ram_of_eisenstein`).
Finally `monicPoly B = (X − C r)·monicPoly g` (`cubic_peel`) and `typeOf` is additive on monic
products, so `typeOf (monicPoly B) = (1,1) ::ₘ {(2,1)} = c3linRam`. ∎

*Where the strength went.* `w = 1` is forced to be EXACT (not just `≥ 1`) — that is what
`π² ∤ B₁` buys, and it is what makes `k > 2w` reachable with only `k = 3`.

**CERT R (`typeOf_ram3_of_val_two`).** Let `π ∣ B₂`, `π² ∣ B₁`, `π² ∣ B₀`, `π³ ∤ B₀`. Then
`typeOf (monicPoly B) = c3ram`.

*Proof.* (i) *`F` has no root in `O`.* If `F(r) = 0` then `r³ = −(B₂r² + B₁r + B₀) ∈ 𝔪`, so
`π ∣ r`. Then `π³` divides each of `r³`, `B₂r²` (`π ∣ B₂`, `π² ∣ r²`) and `B₁r` (`π² ∣ B₁`,
`π ∣ r`), hence `π³ ∣ B₀` — contradiction. (ii) *`F` is irreducible.* A monic cubic over a domain
that factors non-trivially has a monic degree-1 factor (degrees add; a degree-0 factor of a monic
polynomial is a unit), whose root is a root of `F`. (iii) *`f = 1`.* `N(θ) = −B₀` has valuation
exactly `2` (`B₀ = π²·unit`), and `deg F = 3` is always a norm-valuation (the norm of `π`), so
`inertiaDegOf F` divides `gcd(2,3) = 1` (`inertiaDegOf_eq_one_of_coprime`). Hence `e = 3/1 = 3`
and `typeOf F = {(3,1)}`. ∎

*Why the gcd definition of `inertiaDegOf` earns its keep here:* `1` is NOT a norm-valuation in
case R (the order `O[θ]` has index `π` in the maximal order), so the "least positive norm
valuation" reading would return `2` and misclassify this stratum as `(1,2)`. This is the same
defect the P2 rewrite fixed at `n = 2` for `X² − π³`.

### 13.3 The step lemma: HYP.08, the level bookkeeping

**LEMMA (triple-root step).** Let `M : ℕ`, `a : Fin 3 → O`, `γ ∈ O` with all
`shiftVec a γ ᵢ ∈ 𝔪`, and suppose the level-`(M+3)` class of `a` is UNDECIDED. Then, writing
`B = shiftVec a γ`, there is `d : Fin 3 → O` with

    B₀ = π³d₀ ,  B₁ = π²d₁ ,  B₂ = πd₂ ,

and the level-`M` class of `d` is UNDECIDED.

*Proof.* Cases E, L, R each produce a type `σ` such that EVERY `a′` with `π^{M+3} ∣ a′ᵢ − aᵢ`
has `typeOf (monicPoly a′) = σ`: indeed `B′ = shiftVec a′ γ ≡ B (mod π^{M+3})` and `M + 3 ≥ 3`,
so `B′` satisfies the same case hypotheses (all of them are `mod π³` conditions), the certificate
gives `typeOf (monicPoly B′) = σ`, and `typeOf (monicPoly a′) = typeOf (monicPoly B′)` by shift
invariance. That contradicts undecidedness. So case S holds, which is exactly the displayed
divisibility, and `d` exists.

For the second clause, suppose `σ` were decided on the level-`M` class of `d`. Let `a′` be any
lift of the level-`(M+3)` class of `a`; put `B′ = shiftVec a′ γ`. From `π^{M+3} ∣ B′ᵢ − Bᵢ` and
`M + 3 ≥ 3` we get `π³ ∣ B′₀`, `π² ∣ B′₁`, `π ∣ B′₂`, so `d′` exists with
`B′₀ = π³d′₀, B′₁ = π²d′₁, B′₂ = πd′₂`. Cancelling powers of `π` in the domain `O`:

    π^{M} ∣ d′₀ − d₀ ,   π^{M+1} ∣ d′₁ − d₁ ,   π^{M+2} ∣ d′₂ − d₂ ,

so `d′` and `d` have the same level-`M` class and `typeOf (monicPoly d′) = σ`. Now
`(monicPoly B′).comp (C π · X) = C (π³)·monicPoly d′` — a coefficient identity — so
`typeOf (monicPoly B′) = typeOf (monicPoly d′) = σ` by P5's `typeOf_scale`, and
`typeOf (monicPoly a′) = σ` by shift invariance. So `σ` is decided on the level-`(M+3)` class of
`a`, contradiction. ∎

**This is HYP.08.** The bookkeeping is asymmetric — the extraction gives `d` to precisions
`(M, M+1, M+2)` in its three coordinates, not to a uniform precision — and that asymmetry is the
whole content: taking the WEAKEST of the three (`M`) is what makes the step land in the
level-`M` undecided set.

### 13.4 The recursion, and unconditional drainage

**COUNT.** Fix a section `sect` of `residue : O → O/𝔪` (one lift per residue). For each
undecided triple-root class `c` at level `M+3`, the step lemma applied at `γ = sect(γ̄_c)`
(legal: the residual centre `γ̄_c` is unique by `cubeCoeff_injective`, and only its residue
matters) produces `d`. Map

    Φ : c ⟼ ( γ̄_c , level-(M+2) class of d ) .

*`Φ` is injective.* `γ̄` determines `γ = sect γ̄`. From `π^{M+2} ∣ d′ᵢ − dᵢ` and `Bᵢ = π^{3−i}dᵢ`
one gets `π^{M+3} ∣ B′ᵢ − Bᵢ` for every `i` (the slack is `+2`, `+1`, `0`), and then
`a = shiftVec B (−γ)` gives `π^{M+3} ∣ a′ᵢ − aᵢ`, i.e. `c = c′`.
*Its image* lies in `(O/𝔪) × (coeffFactor)⁻¹(undecidedSet O 3 M)`, of cardinality
`q · q^{3(M+2)−3M} · undecidedCount O 3 M = q⁷ · undecidedCount O 3 M`. Dividing by `q^{3(M+3)}`:

    tripleUndecidedSeq (M+3)  ≤  q^{−2} · undecidedSeq M .                    (★)

**(★) is §9.4's recursion with the constant `q^{−2}` in place of `q^{−5}`.** The sharp constant
needs the mixed-precision fibre count (record `d` to precisions `(M, M+1, M+2)` instead of
uniformly to `M+2`, whose fibre is `q³` rather than `q⁶`); the uniform-precision record throws
away three digits and is three digits cheaper to formalize. Since drainage only needs a constant
`< 1`, and `q^{−2} ≤ 1/4`, (★) is what is landed. §13.5 builds the sharp version where an exact
value genuinely needs it.

**DRAINAGE.** `undecidedSeq O n` is ANTITONE: a level-`M` class that is `σ`-decided stays
`σ`-decided at every refinement (`preimage_decidedSet_subset`), so
`undecidedSet (M) ⊆ coeffFactor⁻¹(undecidedSet N)` for `N ≤ M` and the proportions compare by
the fibre-counting lemma. Hence `s(N) := undecidedSeq O 3 N` converges to `S := ⨅_N s(N) ≥ 0`.
Combining the landed branch-(b) bound `s(2M) ≤ q^{−M} + tripleUndecidedSeq (2M)`
(`undecidedSeq_three_le`) with (★) at `M ↦ 2M − 3`:

    s(2M) ≤ q^{−M} + q^{−2}·s(2M − 3)      (M ≥ 2) .

Let `M → ∞`: both `s(2M)` and `s(2M−3)` tend to `S`, so `S ≤ q^{−2}S`, i.e. `(1 − q^{−2})S ≤ 0`
with `1 − q^{−2} ≥ 3/4 > 0`, so `S = 0`. Therefore

* `undecidedSeq O 3 N → 0` — **UNCONDITIONAL, general `O`**;
* `UndecidedVanishes O 3 σ` for every `σ` (`drainage_of_undecided_comp` at `φ = id`);
* `genuineDensity O 3 σ = decidedDensity O 3 σ` for every `σ`
  (`genuineDensity_eq_of_drainage`) — the `n = 3` bracket CLOSES, and every `+1/q²` slack in
  `N3Exact.lean` §3 may be dropped;
* `tripleUndecidedSeq (2M) → 0`, which discharges the hypothesis of `drainage_three_of_triple`
  (kept as a second, now redundant, route).

### 13.5 The exact inert density `{(1,3)}`, and why the other four are not next

**THEOREM (claimed, exact).** For every complete DVR `O` with finite residue field,

    genuineDensity O 3 c3inert = decidedDensity O 3 c3inert
        = q³(q+1) / (3·(q⁴+q³+q²+q+1)) .

At `q = 2` that is `8/31 ≈ 0.25806`; at `q = 3`, `36/121 ≈ 0.29752` — **exactly the HMENU3
predicted values** (`lean/notes/openmath/HMENU3_PROOF_2026-08-08.md`, COROLLARY HM3.D), which
§10.3 could only bracket.

*The derivation.* Condition a monic cubic on its residue, using the §9.1 trichotomy.

* *Residue irreducible* (proportion `(q³−q)/(3q³)`, `three_mul_card_noRootCubic`): the class is
  `{(1,3)}`-decided (`typeOf_inert3_of_residue_irreducible`), and CONVERSELY these are the only
  level-1 classes that are (`inert3_decided_iff`).
* *Residue with a simple root*: every lift has a root in `O` (Hensel), so every lift's type
  contains `(1,1)` and is `≠ {(1,3)}`. Contributes `0`, and is not even `{(1,3)}`-possible.
* *Residue a perfect cube* (proportion `q^{−2}`): recentre. Cases E, L, R give types
  `{(3,1)}`, `{(1,1),(2,1)}`, `{(3,1)}` — never `{(1,3)}`, and (being class-certificates) they
  exclude `{(1,3)}`-possibility too. Case S has `typeOf F = typeOf (monicPoly d)` with `d` the
  extraction, and — this is the self-similarity — as `B` ranges over
  `π³O × π²O × πO` the vector `d` ranges over ALL of `O³`, uniformly. The measure of case S
  inside one residual centre is `q^{−3}·q^{−2}·q^{−1} = q^{−6}`, and there are `q` centres, so

      I  =  (q³−q)/(3q³)  +  q·q^{−6}·I  =  (q³−q)/(3q³)  +  q^{−5}·I ,

  whence `I = (q³−q)/(3q³) · (1 − q^{−5})^{−1} = q(q²−1)q⁵/(3q³(q⁵−1)) = q³(q+1)/(3Φ)` with
  `Φ = q⁴+q³+q²+q+1`, using `q⁵−1 = (q−1)Φ` and `q²−1 = (q−1)(q+1)`. ∎

**What the Lean form needs beyond §13.4.** The `q^{−5}` here is the SHARP constant, so this
value — unlike drainage — cannot be had from (★). It needs the mixed-precision count: with
`MBox M := Res M × Res (M+1) × Res (M+2)` and `τ : MBox M → Coeff O 3 M` the truncation (a
surjective additive hom, so `#(τ⁻¹S) · q^{3M} = #S · q^{3M+3}`), case-S classes at level `M+3`
correspond bijectively to `(O/𝔪) × MBox M`, and the two inequalities

    decidedSeq (M+3) ≥ (q³−q)/(3q³) + q^{−5}·decidedSeq M ,
    possibleSeq (M+3) ≤ (q³−q)/(3q³) + q^{−5}·possibleSeq M

pass to the limit `I` (equal for both by §13.4's drainage) and pin `I` from both sides.

**Why the other four HMENU3 values are NOT next.** §10.3's obstruction is untouched by drainage:
the peel parametrisation `(r, g) ↦ (X − r)g` of a decomposable type is resultant-weighted (its
Jacobian is `g(r)`), so `c3split`, `c3linInert`, `c3linRam` do not decompose into the exact `n = 2`
values, and `c3ram` collects mass from cases E and R at every depth of the recursion (their
`typeOf` verdicts are known, but their measures enter the recursion with the same `q^{−5}`
self-similar tail plus two inhomogeneous terms, one per case). The inert value is the ONLY one of
the five whose level-1 census is both exhaustive and self-similar with a single inhomogeneous
term, which is exactly why it falls out first.

---

## 14. LANDED (2026-08-15): §13 is now Lean

§13.4's drainage transcription landed first (commit `b00b0505`: `N3Shift` / `N3CertRam` /
`N3CertLinRam` / `N3Recur` / `N3Drain3` / `N3MBox` / `N3Tie` — `drainage_three`,
`genuineDensity_three_eq_decidedDensity`, the q=2/q=3 brackets without the `+1/q²` slack).
§13.5's exact value landed the same day in two files, statements byte-identical to the
skeletons authored that morning:

* `N3InertLower.lean` (`8098a11c`): the sharp self-similar LOWER bound `decidedSeq_inert_step`
  — `decidedSeq (M+3) ≥ (q³−q)/(3q³) + q⁻⁵·decidedSeq M` — via the mixed-box reconstruction
  count `card_boxImage` (`#(boxImage) = q⁴·#S`) and the disjointness of the level-1 and
  reconstruction families.
* `N3InertExact.lean` (`ace8d018`, wired into the roll-up at `aac91374`): the UPPER twin
  `possibleSeq_inert_step` via the residue trichotomy + forced case S, and the limit:

      inert3_density_eq : genuineDensity O 3 c3inert = q³(q+1) / (3(q⁴+q³+q²+q+1))

  with gates `inert3_density_padic_two = 8/31` and `inert3_density_padic_three = 36/121` —
  COROLLARY HM3.D's predicted values, now theorems. Full `lake build` green (8,715 jobs); all
  14 `#print axioms` gates across the pair report Lean core only.

**The §13.5 header "THEOREM (claimed, exact)" is discharged: it is a THEOREM (Lean).** The
closing paragraph of §13.5 (why the other four HMENU3 values are NOT next) remains the honest
status of the remaining rows.
