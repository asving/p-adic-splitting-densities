import Uniformity
/-!
# UniformityCheck — definition checks, kept OUT of the final repo

Asvin 2026-08-13: "Lets finish the check of the definition at n=2 and then
n=3 using agents, but lets keep it out of the leanfinal directory."
This project imports the final repo's `Uniformity` package read-only and
holds the n=2 / n=3 verification theorems (exact values, censuses, gate
batteries) that validate `genuineDensity` without living in the final
artifact. Rules: never edit ../leanfinal from here; zero sorries; Lean-core
axioms only; quarry rules (../lean read-only, provenance comments) apply.
-/
