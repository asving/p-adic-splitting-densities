/-!
# H6 unit D2 — item (vii) fence documentation (DOCUMENTATION-ONLY)

**NO Lean declaration lands in this file** (BP `blueprints/HDISCHARGE_H6.md` §3.5 +
§4 unit D2; restructured at fold round 1, Codex finding 8 CRITICAL accepted: the
constructor-chosen six-Prop-slot `FenceVII` device is WITHDRAWN — caller-selected
Prop slots + a conjunction field are True-instantiable BY THE PROVER, so as an
assumed row such a structure asserts NOTHING about the built pack, unlike the
RootHyps precedent where the bare Prop is a FIELD of an assumed record, fixed by
the assumer and opaque to the prover).

## Item (vii)'s Lean face at HEAD

D-11 widened-charge item (vii) — general-n H.6 in-fence conformance of the BUILT
tables — keeps its ONLY honest Lean face at HEAD: the ALREADY LANDED bare field
`RootHyps.h6_vii : Prop` (`LeanUrat/Scaffold/Hypotheses.lean`, assumed, never
constructed), consumed at Step 18's K3-a. This file adds no typed carrier.

## The six-fence roster (H.x pointers) — intended content for the `h6_vii`
## docstring owner (re-displayed here; NO edit to `Hypotheses.lean`, whose owner
## is the spine lead)

The six H.6 fences — the [2b]-parked perimeter of T-5's dual acceptance — GATE
the [1v]-FULL event at every n; that the built per-n tables sit INSIDE them is
proved nowhere at general n:

1. XHD-d-EX(∂) — H.2.3
2. M4b-T-AUD — H.5.1
3. U-R-COUNT — H.4.2/H.5.3
4. XHD-u-X — H.4.3
5. H-LIST-GEN — beyond class (TRI)
6. E-POS — ⟨w_E, v⟩ ≥ 1

## Sealed-flag note (gate G3)

Sealed n = 3 evidence: V-n3 178/178, H-n3 151/151, off-census q = 16/25 —
`verification/` artifacts of the T-5/T-7 dual acceptance (MATH tree §D4.2).
These are FLAGS (evidence), NOT proofs. No Lean `decide` gate is feasible: the
gates are PARI/sympy sealed runs, not Lean-computable at HEAD. This docstring
cross-reference IS gate G3 (§4, Gates).

## Escalation fence (verbatim)

"Escalation fence: if a built pack ever NEEDS fenced territory (sharpest:
H-LIST-GEN beyond class (TRI)), the kernel so touched becomes ADDITIONAL open
mathematics beyond this row."

## BLOCKED-ON-CARRIER record (units D2/D3, fold round 1)

The eventual typed `FenceVII` must state a FIXED external proposition about the
BUILT pack: each fence a CLOSED predicate of the pack/H-LIST datum, with NO
caller-selected Prop slots. It is constructible only when the value-side
H-LIST/pack-builder carrier lands (D-11/BP_IV territory) — at HEAD there is NO
Lean carrier for the H-LIST height-resummation domains or the six fence kernels
(they live in the tree/assembly notes, [2b]-parked). Its general-n proof remains
[M] regardless. Transcribing the kernels as typed general lemmas is REJECTED:
they are explicitly OFF the mandatory path (consumed by no mandatory step —
VC10's hunt confirmed zero consumption); transcribing them as provable
statements would ADD open mathematics the root does not consume — exactly the
escalation the ROOT row fences. Correspondingly, `FenceVIIRow n` (unit D3) is
POSTPONED with this record; BP_V's `HypRows.fenceVii` field keeps its own bare
named row per ITS charter until the typed carrier exists (its call, our record).
Until the carrier lands, the row rides ONLY as `RootHyps.h6_vii`.
-/

namespace LeanUrat.Scaffold.HDischarge.H6

-- Intentionally empty: unit D2 is documentation-only (see module docstring).
-- The typed `FenceVII` is BLOCKED-ON-CARRIER; item (vii)'s HEAD face is
-- `RootHyps.h6_vii` in `LeanUrat/Scaffold/Hypotheses.lean`.

end LeanUrat.Scaffold.HDischarge.H6
