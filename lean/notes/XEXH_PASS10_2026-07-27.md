1. **CORRECT — T1 gloss.** The added “μ·g = 1 engine-continuing selections on ℓ ≥ 2 sides” are exhaustive: μ, g ≥ 1 forces (g, μ) = (1,1); e ≥ 2 gives an increment, while e = 1 gives a same-degree recentering. The formula  
   `ind(S) = ½ℓ(ehℓ − e − h + 1)` depends only on the side data `(e,h,ℓ)`, not the selection, so it applies unchanged and remains ≥ 1.

2. **CORRECT — deep-leg slope.** With `c_d(3)=1/2`, `s(3)+1/2=5/2`, and `n−1=2`, the per-level exponent is
   \[
   \frac{1/2}{(5/2)\cdot2}=\frac1{10}.
   \]
   Removing the spurious factor 2 while retaining `1/10` is exact.

3. **CORRECT — STATUS stamp.** “ACCEPTED at the Codex bar,” Fable mathematics clean with its display issues incorporated, and “census layers … not yet run” accurately distinguish note acceptance, the pending rev-8 dual confirmation, and the unexecuted sealed gate.

4. **CLEAN — full end-to-end sweep.** The index arithmetic and endpoint conventions, certified/uncertified partition, additive T3 charge, both X.3 termination routes, `(τ-ns)` exclusions, cap-domain conventions, bridge/envelope tags, qualitative density assembly, consumer map, sealed numerical tails, and historical corrections remain coherent. Open kernels and external hypotheses are consistently fenced; no conditional claim is presented as closed.

Counts: **0 critical, 0 gaps.**

**VERDICT: ACCEPTED (0/0).**
