1. **CORRECT — rev-5 family predicate.** The condition “finite branch or prefix of a complete τ-halted tree AND `(τ-ns)`-FREE” is well-typed. By VP-2, `(τ-ns)` is the cell-data predicate `J(f)=∅`; hence membership of a node’s track in the `(τ-ns)` part is decidable from the finite halted tree’s node/cell data. No property external to the canonical data is required.

2. **CORRECT — `cap(H)` on that family.** Every member falls into exactly one applicable convention:

   - a cap-detectable `(τ-irr)` branch has a nonempty finite collection of its own TB-CAP cells, so the maximum exists;
   - a continuing prefix or hen/no-window history has `cap(H):=0`;
   - `(τ-ns)` histories are excluded intrinsically.

   Thus `cap(H)` is total and unambiguous on the explicitly defined FOURTH-PIECE FAMILY. The empty-history companion `max h(∅):=0` is also total.

3. **CRITICAL — the repaired predicate is not actually imposed in the displayed fourth decomposition piece.** `(X2-BRIDGE)` still states
   > `{f : ∃ finite branch H, threshold(H) + cap(H) > N}`

   rather than quantifying `H` over the newly defined FOURTH-PIECE FAMILY or explicitly adding `(τ-ns)`-FREE. Likewise, its preceding bridge statement quantifies
   > “SOME branch H of `T_can^τ(f)`”

   without the family predicate. Since the same block says that `(τ-ns)` branches “take NO convention,” `cap(H)` remains undefined for branches admitted by these unrestricted quantifiers. The accompanying claim that the unordered union “routes” such branches into the `𝒯^ns` piece is exactly the pass-4-invalid argument: membership in the second union piece does not remove an object from the fourth piece. The rev-5 predicate repairs X.2’s own quantifier but was not propagated to the bridge/decomposition display that caused the critical.

4. **CORRECT — remainder of the end-to-end sweep.** No further defect was found in the certified-depth/index chain, T1–T4 population accounting, additive T3 charge, WEIGHT-CHARGE conditionality, linear versus √N envelope tags, both X.3 termination routes, König bridge, TB-CAP/NS-ROUTE use, CL-4 consumer map, or the explicitly toothless gate claims.

Counts: **1 critical, 0 gaps.**

Verdict: **NOT ACCEPTED (1/0)**
