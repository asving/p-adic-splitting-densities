I find the SAE repair mathematically effective, but §C rev 12 is not yet clean: the realizability hypothesis was not propagated into two load-bearing lemma statements, and the repaired ZC downset is contradicted later in C.2.

### SAE audit

- Pure-data status: yes. SAE compares `old`, determined by \(\nu_i\), with `new`, determined by \(\nu_{i+1}\). No \(f\), stratum point, or equation-system solution is needed. Given the strictly negative slope of `new − old`, the endpoint reductions are valid:
  - adjacent: the vertex tie implies strict inequality at every earlier span slot;
  - non-adjacent: `new(j*) > old(j*)` implies it at all earlier span slots.

- Certified instance: correctly excluded. Here
  \[
  \operatorname{old}=(3,2,1,0),\qquad
  \operatorname{new}=(7,4,1,-2),\qquad \mu_0=3,\quad j^*=2.
  \]
  SAE requires \(\operatorname{new}(2)>\operatorname{old}(2)\), but both equal \(1\). Thus F-EMPTY fails SAE even though (NA) holds because \(-2<0\), and (HV) is vacuous.

- Circular source removal: the former span-entry deductions from “D.8’s box” have been replaced correctly in DOM(2), C.1(i)(α), and the non-adjacent hinge by direct citations to SAE. The remaining D.8/stratum references in DOM are commentary, slope/landing arguments, or conditional clause implications; I found no residual derivation of the span-entry inequality from stratum membership.

- Completeness in substance: for a legitimate sequence of D-node data satisfying all three transition conditions, C.1.5’s intended recursion gives positive mass at every finite stage, so it rules out an empty joint stratum. I do not have a new \((\mathrm{NA})+(\mathrm{HV})+(\mathrm{SAE})\)-passing counterexample. That conclusion is subject to the statement defects below.

### Other repairs

- The species-correct ZC tops are the right correction: inclusive at strict-bound/BOX interior positions and exclusive at value-carrying rim positions. That is consistent with DIG carrying the nonzero level-\(F_i\) values.
- The weak inequality repair in C.1.5 is correct at the adjacent \(s'_0=0\) boundary.
- The admissibility rewording correctly says that \(Z\)’s leading coordinates must be free on \(\Sigma_k\).
- Strengthening the \((\beta)\)-slot comparison to strict is justified by SAE and the negative slope difference.
- Retyping `old` as the prior node’s data line is correct and removes the former dependence on a stratum witness.
- Subject to the missing historical text noted below, I see no unsound item among the six described meaning changes.

## Numbered findings

1. **CRITICAL ERROR — realizability was not propagated into C.1 and C.1.5’s statements.**

   C.1 says:

   > “Let \(\nu_{i+1}\) be any read of the stage that node \(\nu_i\)’s landing produces…”

   but its proof invokes:

   > “By C.0’s (SAE) (realizability — a DATA condition…)”

   Nothing in the lemma statement assumes that the extended prefix through \(\nu_{i+1}\) satisfies SAE, NA, and HV. Therefore the certified F-EMPTY read remains a counterexample to C.1(i) as literally quantified.

   The same defect occurs in C.1.5:

   > “Let the prefix through \(\nu_i\) satisfy (ZC) and be realizable … Let \(\nu_{i+1}\) be a read…”

   Realizability of the prefix through \(\nu_i\) does not impose the transition conditions at the newly appended \(\nu_{i+1}\). Taking the realizable root prefix and appending F-EMPTY makes C.1.5(1) assert the false positive-mass recursion again.

   Theorem C itself quantifies over a fully realizable history, so its intended induction only applies these lemmas in the good case. Nevertheless, two displayed lemmas remain false as stated. The propagation demanded by the repair is incomplete.

2. **JUSTIFICATION GAP — the displayed “full” node datum still contains undefined/unlisted inputs.**

   Later formulas use

   > \(p_j(\gamma_i)-a_i\)

   in \(R_{\rm anch}(\nu_i)\), and

   > \(\bar z^{-\mu_i\widehat m_i}\)

   in `vtx(ν_i)`.

   Neither \(\gamma_i\) nor \(\widehat m_i\) appears in the displayed node tuple, nor are they explicitly listed among the derived frame quantities. Thus the claim

   > “DERIVED from the tuple … \(R_{\rm anch}(\nu_i)\) and \(\operatorname{vtx}(\nu_i)\)”

   is not self-contained as written. The tuple otherwise covers the principal fields consumed by C.1/LST/TYP—shape, endpoints, line height, stride positions, digits, residual factor, anchor, and recentering lift—but it has not fully repaired the dangling-data problem.

3. **JUSTIFICATION GAP — C.2 contradicts the corrected species-dependent ZC downset.**

   ZC-a correctly says that at value-carrying rim indices the zero downset is exclusive:

   > “zeros at \(ht<F_i(b)\) only — the \(ht=F_i\) level set there belongs to \(DIG_i\)”

   But C.2 subsequently claims that the determined coordinates are exactly

   > “its floor downset \(\{(b,l):ht(b,l)\le F_k(b)\}\) plus \(DIG_k\)’s lead level sets”

   The uniform `≤` formula incorrectly includes rim value-level coordinates in the zero downset and is incompatible with the species-correct repair. The primary locus-based definition of admissibility remains intelligible, but the advertised node-data check is wrong as displayed.

4. **JUSTIFICATION GAP — completeness of the meaning-change fence cannot be audited from the pasted record.**

   The prompt says that a six-item statement-fence list is “on record,” but the actual six-item list and the complete rev-11b predecessor text are not included. The live annotations support the described changes and I found no additional undeclared semantic change visible from the Fable quotations, but a claim that the fence is complete requires an actual old-versus-new comparison. That verification cannot be made from the supplied text alone.

**Counts: 1 critical / 3 gaps.**

**Verdict: NOT ACCEPTED.**
