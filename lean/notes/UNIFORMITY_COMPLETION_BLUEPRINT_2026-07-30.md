# THE UNIFORMITY COMPLETION BLUEPRINT (2026-07-30)

GOAL (the /goal directive): complete the formalization of the uniformity theorem —
ρ(n,σ;p) = R_σ(p) for every prime (Theorem U fired through the real corpora), with the
smallest possible explicitly-named hypothesis surface. Method: tiny pieces, one agent
per piece, opus→fable cascade for proving, Codex for semantic ratification.

## THE E8 ADJUDICATION (2026-07-30, orchestrator note read — BINDING)
TREE-EXP (MOVES 7440–7460): each read site carries its OWN full joint cell c_s —
"in general FINER than any single branch's (side,ψ) node stratum" — and the product
charges "every site exactly once" where sibling CHILD sites are DISTINCT sites with
distinct digit pins (the F1 ledger check displays the separate charges). The E-phase
keyed `state_cell` by the SHARED PARENT state — that is the machine-checked seam.
RULING: candidate (1) — per-site cell keying. `state_cell` (and the E8 premise row)
re-keys the site cell by the site's own realized read datum; the REV-6 F1 record is
then CORRECT under per-site keying (the leaves' distinct pins are different sites'
cells). Σ_H siteExp is per-site = the note. Codex confirm on the applied diff required.

## PIECE LEDGER

### WAVE A (in flight — one agent per piece)
A1+A2 [fable]: the E8 per-site re-key (the ruling above; Defs/CellAssign + the E8
  premise row + the forcing-lemma retirement + the F1 all-sites recomputation) THEN the
  treeExp assembly per the recorded plan; E10/E11 un-gate; axioms reported.
A3 [opus→fable]: E5 hnostray — head-roster completeness, the (c2) m_i=1 τ-hen leg
  (owner HC-2/D4R0K; the canonical-instance content).
A4 [opus]: D2_eligGate residual sorry. A5 [opus]: G2_toyOneF residual.
A6 [fable]: D11 fibOf_partition — the rep-normalization law added to ClassCover's
  classification clause (the recorded necessity analysis at the sorry site) + prove.
B1 [opus]: V2_valB. B2 [opus]: V3_instbijA/B/C (one family agent).
B3 [fable]: the V3_spword family (spwordA under the width hyps; spwordB ×2;
  spwordD writeHeights_total + spWord_faithful).
B4 [opus]: V4_hmcaug ×4. B5 [opus]: V4_meas + V4_part2.
B6 [fable]: V7_ctsm ×3 + V7_pgLaws ×2 (the CtsmConclusions fields; the tCount supply).
E1 [opus]: RepPrefixClosed/LeafReg discharge attempt (PresentNorm vocabulary).
P1–P3 [python agents]: ESCAPE-probe(a) the symbolic closed-class refuter (n ≤ 4, q = p:
  build A's transition graph from the corpus tables, SCC scan for closed stochastic
  classes — a finding REFUTES E0); ESCAPE-probe(b) the k = 1..8 decay census;
  the RESUM-n3 ACT seal + SQUEEZE-n3 gate arithmetic (verification/ dir, sealed
  numbers as acceptance).

### WAVE B (after A stabilizes)
C1–C7: the seven MovesS seam-gated units (N2PolyGeom, Interfaces instances,
  N2Activity, N2Rexact, N2Sigmas, Rs2Unique, Defs seam residues) re-keyed/discharged
  against BUILT MovesT/MovesV per the §W4-SYNC contract.
C8: MovesD absorbs the W4-1 covering clause (delta entry 10).
C9 [orchestrator+agent]: THE INSTANTIATION LEDGER — CL-1..19 → the built decl or the
  named open kernel, one row each; this is Theorem U's discharge map.
C10+: the instantiation units the ledger enables.
C11+: the Moves/§B2-DEF backlog sorries (~8 pieces, one agent each).

### WAVE C (the gates)
R1/R2 [Codex]: as-built ratification MovesT vs §T-ASSEMBLY, MovesV vs §V-TABLES.
R3/R4 [fable]: the Fable legs on identical charges.
R5 [Codex]: the consolidated confirm on ALL post-E-phase statement changes (the
  adjudicated repairs incl. A1's re-key).

### WAVE D (the carriers)
W5-1..n: HC-1 carrier exports (the W4-5 slot-selection discharge; the lower-stage
  realizer vocabulary); MovesR unparking (CInterface re-key + ~14 units).

### PERMANENTLY FENCED (never fleet targets)
The banked W17ii (V7_w17ii — note-owned hypothesis [3]); G1's four Q1-gated sorries
(the V10/HistoryCoherent sign-off cluster, ASVIN); Wall B's jet-existence premise;
htameFE; hExhaust general-n; E0/HMC/X.3 as theorems (the open mathematics — the
probes TEST E0, nothing proves it here).

## DONE CRITERION
Full build green; every remaining sorry is on the FENCED list; Theorem U's CL ledger
fully mapped (each row = built decl or fenced kernel); both as-built ratifications +
the statement-change confirm CLEAN; AxChk census: Lean-core + the two trusted axioms
only. That state = "the uniformity theorem is machine-checked conditional on the
named kernel list" — the maximal honest completion available without new mathematics.
