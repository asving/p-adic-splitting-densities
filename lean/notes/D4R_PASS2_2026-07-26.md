I find rev 2 materially improved and the gate’s numerical record consistent with the displayed arithmetic. Two issues remain: D4R.2 no longer proves the literal bound required by the original plan, and D4-R.6 still does not reproduce the full sealed partition specification internally.

1. L1 canonical-choice totality — GENUINE scope/status repair

Rev-2 repair:

> “Consumes: the accepted §C … [and] the accepted §B2-DEF — whose D.5/(S6b′) offset-P-lift construction is L1’s cited source…”

and:

> “[OPEN — kernel D4R.0-K part (a)… L1 is stated and CONSUMED AS OPEN…]”

This faithfully corrects the former “ONLY §C” claim and no longer presents the missing boundary enumeration as proved. It does not prove totality, but it does not claim to: all consumers are explicitly conditional on D4R.0-K.

The citation to §C is faithful: §C names the canonical offset-P lift but does not establish the global eligible-node quantifier. Rev 2 now says exactly that.

2. L5 exhaustive outcome classification — GENUINE scope/status repair

Rev-2 repair:

> “The case labels (z)/(ep)/(ns) above are the ENUMERATION OBLIGATION, not its discharge…”

and:

> “Nothing in this note discharges L5; every consumer below is conditional on D4R.0-K.”

This accurately adopts pass 1’s objection. The outcome enumeration remains open mathematics, but it is no longer smuggled into an unconditional theorem. D4R.0, L2, L6, L7, D4R.1 and D4R.4 are consistently conditional on it.

3. Empty-prefix base case in L5/L6 — GENUINE

Rev-2 repair:

> “Fix a realized prefix η — INCLUDING the EMPTY prefix η = ∅ (k = −1), whose state cylinder is the FULL BOX…”

and:

> “BASE i = 0 … the L5 application below is L5 AT THE EMPTY PREFIX…”

This supplies exactly the previously missing scope at \(i=0\). It is faithful to §C’s explicit convention \(T(\varnothing,Z)=Z\).

There is a minor terminological stretch because L3 introduces realized prefixes in the form \((\nu_0,\ldots,\nu_k)\), but L5 expressly extends that convention to \(k=-1\). The empty object is not inserted into \(\operatorname{Pref}(\widehat P)\), so no substantive conflict follows.

4. L11 enlarged encoding and D4R.2 — PARTIAL

The central repair is:

> “enc′: η ⟼ (the side-digit tuples …; the selection indices \((s_r)\))”

with:

> “given the shape and the digit tuples through read \(i-1\) AND the indices \(s_0,\ldots,s_{i-1}\), the node data … and hence the frame … are reconstructed…”

The injection is now adequately argued on \(\operatorname{Pref}(\widehat P)\):

- The digit tuple determines \(R_{\rm anch}\).
- The selection rank determines \(\psi_r\) among eligible same-shape factors.
- Earlier choices reconstruct the residue tower and its fixed bases, so the later lexicographic ranks are interpreted in a determined field/frame.
- The canonical lift is then determined from node data, conditional on L1.
- `(NA)` and `(HV)` are correctly used only after the injection, as predicates that remove encoded tuples.

The factor bound is also more than a naked assertion:

> “\(m_r\le\lfloor\deg R_{\rm anch}/(g_r\mu_r)\rfloor\le n\).”

This follows from degree additivity: distinct eligible irreducible factors of degree \(g_r\), each occurring to order \(\mu_r\), consume at least \(g_r\mu_r\) degrees. The displayed sharper choice

\[
M(\widehat P)=\prod_r\left\lfloor\frac n{g_r\mu_r}\right\rfloor
\]

does require the implicit fact \(\deg R_{\rm anch}\le n\); that is natural from the degree-\(n\) development, but it should have been stated at the point where the sharper bound is claimed. The weaker \(M\le n^{|\widehat P|}\) is secure from the displayed argument.

The substantive limitation is explicitly admitted:

> “NOT the literal (3i): the rev-1 ‘verbatim’ claim … is withdrawn; any consumer needing the bare \(p^{-A_{\rm net}}\) must first bound \(M(\widehat P)\).”

Thus D4R.2 proves a fixed-shape, \(p\)-uniform Big-O inequality,

\[
\mu(\widehat P)\le M(\widehat P)p^{-A_{\rm net}(\widehat P)},
\]

but not the original plan’s coefficient-one inequality

\[
\mu(\widehat P)\le p^{-A_{\rm net}(\widehat P)}.
\]

The withdrawal is propagated through the header, introductory display, L8, L11, D4R.2, D4R.5, D4R.6 and the pass record. Nevertheless, if OPEN-D4 really requires literal `(3i)`, that assembly-plan obligation is still unmet. Calling the result “the original (3i), up to a p-free shape factor” does not make it the original inequality.

5. Empty \(\operatorname{Pref}(\widehat P)\) in D4R.4 — GENUINE

Rev-2 repair:

> “\(N(\widehat P):=\max_{\eta\in\operatorname{Pref}(\widehat P)}N(\eta,\top)\) if \(\operatorname{Pref}(\widehat P)\ne\varnothing\),  
> \(N(\widehat P):=1\) if \(\operatorname{Pref}(\widehat P)=\varnothing\).”

and:

> “the max-over-\(\varnothing\) expression is never formed.”

This makes D4R.4 total. In the empty case both the event and \(C_{\widehat P}(p)\) are empty/zero, so the finite-level equality is valid for every \(N\ge1\). There is no need to assume \(C_{\widehat P}(p)>0\).

6. Conditionality/status labels — GENUINE

Rev 2 now says:

> “D4R.0 is therefore OPEN…”

and:

> “CONDITIONAL, proved here GIVEN the accepted §C + §B2-DEF AND the open kernel D4R.0-K…”

This removes the former contradiction between “unconditional” and “modulo an OPEN kernel.” The header, theorem ledger, honest perimeter and pass record agree on the conditional status.

L4’s concluding bracket—

> “it is to be PROVED, not assumed”

—is slightly stale wording beside the displayed induction and later statement that L4 is discharged conditional on L1. It does not create another mathematical dependency: §C explicitly states that a lift-carrying history determines the move maps, and L4’s induction supplies the required input-independence.

7. Gate D4-n3 prediction derivability — PARTIAL

The mass-law part of the gate is now genuinely reproducible. The note displays the box, both nodes, key, digit alphabets, `(HV)`, absence of `(NA)`, ledger and levels. A referee can derive:

\[
A=9+2=11,\qquad W=3+4=7,\qquad A_{\rm net}=4,\qquad M=1,\qquad N=5,
\]

and

\[
C_{\widehat P^*}(p)=\frac{p^2-p}{2}(p-1)(p^2-1),
\]

hence \(C(2)=3\) and \(C(3)=48\). The four predicted counts also check:

- \(3\cdot2^{18-11}=384\)
- \(3\cdot2^{21-11}=3072\)
- \(48\cdot3^{15-11}=3888\)
- \(48\cdot3^{18-11}=104976\)

The total number of inspected boxes in the supplied gate record is also exact:

\[
2^{18}+2^{21}+3^{15}+3^{18}=404{,}128{,}692.
\]

But the partition part is not fully self-contained. It says:

> “FC8’s stratum list — non-cluster ∪ 20 valuation lumps ∪ the \(p^2(p-1)\) on-line digit cells…”

and:

> “FC9’s list…”

The twenty valuation lumps are neither listed nor derived in the note. “FC8’s stratum list” and “FC9’s list” point to the external seal rather than reproducing it verbatim. A referee can derive the expected verdict “membership sum \(=1\), zero double-counts, zero orphans” conditional on D4R.0, but cannot reconstruct the exact sealed step-0 partition test from this note alone.

Therefore the repair makes all four numerical mass predictions derivable, but not every sealed partition specification. The claim

> “FC1–FC9 … all adopted VERBATIM below”

is stronger than the live text supports.

8. Pass-record audit

Findings 1–6 in the pass record accurately describe the corresponding live edits.

Finding 7 is accurate about the numerical pin, ledger, levels and counts, but overstates completeness because the live note does not reproduce FC8’s twenty valuation lumps. It references them.

The supplied post-pass gate summary matches the live predictions:

- \(C(2)=3\), \(C(3)=48\)
- \(A=11\)
- levels \(\{6,7\}\) and \(\{5,6\}\)
- all four predicted counts
- total \(404{,}128{,}692\) boxes
- expected \(p^3\) level-to-level ratio
- zero double-counts and zero orphans.

The live pass record itself does not yet record that D4-n3 ran and passed; it remains written as a repair/provenance record. That is an omission of later status, not a contradiction. The gate’s PASS also correctly does not close D4R.0-K.

9. Fresh sweep

No new defect arises from adding factor-selection indices to L9 or the \(A(\widehat P)\) ledger. Selection indices count alternative histories; they do not impose fresh coefficient equations. Section C.3 says fresh alphabet cardinalities depend only on shape data, so every selected factor of the same retained degree/type has the same per-history fresh volume. The extra multiplicity belongs in \(C_{\widehat P}(p)\) and its bound \(M(\widehat P)\), not in \(A(\widehat P)\).

Likewise, the enlarged encoding does not alter \(W\): \(W\) counts digit-pool coordinates, while the factor-selection multiplicity is separated into the \(p\)-free \(M\).

The citations to Theorem C at \(Z=\top\), including the root contribution, finite-level threshold and lack of any termination assumption, remain faithful. L6 continues to use maximal finite-or-infinite runs and does not reintroduce a completion-union argument.

Final findings:

1. The literal plan inequality `(3i)` is still not obtained; D4R.2 proves it only up to the shape factor \(M(\widehat P)\). — JUSTIFICATION GAP  
2. D4-R.6 does not internally reproduce FC8’s full partition list despite claiming FC1–FC9 were adopted verbatim, so the complete sealed partition test is not reconstructible from the note alone. — JUSTIFICATION GAP

Counts: **0 critical errors / 2 justification gaps.**

Verdict: **NOT ACCEPTED.**
