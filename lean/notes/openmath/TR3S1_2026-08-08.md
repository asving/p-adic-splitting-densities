# TR3-S1 — the assembled coefficient agreement for the marched order-2 read on the unramified-below stratum (e₀ = 1): FGMN = GMN on the nose, with the σ-identifications constructed

Unit chartered by SYNTHESIS PASS 4 F1 (docs/SYNTHESIS_PASS4_2026-08-08.md): the ONE
statement both GRB open lemmas reduce to on the stratum {state order 2, e₀ = 1} —
prove it. Parent leaf: GRB_ORDER2_2026-08-05.md (S3.1 REV 11 corrected ǫ, S3.2
corrected picture, S3.4 REV 13 fact/conjecture split, R2.3). Genre: leaf proof note;
finite algebra executed in full; every formula pin carries a NON-TEXTUAL leg
(the shared fgmn.txt extraction is CORRUPTED at Def 3.15 — repinned below).

## S0. Statement and verdict

**Theorem TR3-S1 (proved below; falsifiers green).** Standing setting of the GRB
leaf S1.1 (GD23 §1.2), specialized to a state of ORDER 2 on the unramified-below
stratum: reads ν₀ = (e₀, h₀, ψ₀) with **e₀ = 1**, then ν₁ = (e₁, h₁, ψ₁),
(e_j, h_j) coprime, ψ_j monic irreducible with ψ_j(0) ≠ 0; Φ₀ a monic root lift
(w₀(Φ₀) = 0), Φ₁ a lawful monic lift of the read ν₀ (deg Φ₁ = g₀d₀,
w₁(Φ₁) = g₀h₀, order-1 residual condition R_{λ₀}(Φ₁) ∼ ψ₀); w₂ the order-2 state
valuation, γ₂ = e₁w₁(Φ₁) + h₁. Let σ₀ : F₁ → K₀ and σ₁ : F₂ → K₁ be the
tower identifications of S2 (z₀ ↦ X̄, z₁ ↦ z̄, σ₁|_{F₁} = σ₀). Then for EVERY
nonzero f ∈ O[x], with γ := w₂(f), j₀ := ⌊s₀(f)/e₁⌋:

    (TR3-S1)   σ₁-coefficientwise:  R^F_{2,γ}(f) = y^{j₀} · R_λ(f)
               and hence            σ₁( R^F₂(f) ) = R_λ(f)   EXACTLY,

where R^F_{2,γ}/R^F₂ are FGMN's marched weight/side residual operators (Defs
3.16/3.18, corrected Def 3.15, read in integer weights through the leaf's Lemma
RESCALE) and R_λ is the GMN-convention order-2 residual polynomial (Defs
2.19–2.21) **with the normalized Bézout representative 0 ≤ ℓ_{r−1} < e_{r−1}
(hence ℓ₁ = 0 on the stratum) — a DISPLAYED HYPOTHESIS of this theorem: GMN's
print fixes ℓ only up to ℓh − ℓ′e = 1, and the normalized choice is consumed
here as the project's standing engine convention (GD23 rows; the CU-1/N-1
harness), not re-verified from GD23 [REV 2: pass-1 gap 1]** — the project's
engine-convention operator under the GD23 standing identification. The
per-argument units of TR-3′-GEN are ≡ 1 on this stratum; the identification
family {σ₀, σ₁} is march-compatible (S2). No (R1)/(R2) hypothesis is consumed:
the identity is total in nonzero f ∈ O[x].

| clause | status | where |
|---|---|---|
| σ₀, σ₁ constructed, well-defined, march-compatible | PROVED | S2 |
| Lemma A (level-1 weight read = z-shift of the untwisted GMN order-1 read) | PROVED | S3 |
| twist triviality ǫ₁ ≡ 1 on the stratum | FACT restated from the leaf (S3.2 corrected picture); one-line proof displayed | S4 |
| the assembled per-slot agreement + support + two-form | PROVED (the unit's content) | S5 |
| TR-3′-GEN(i) restricted to order 2 | THEOREM (conjecture → proved on the stratum) | S6.1 |
| TR-3-ORD-X restricted to order 2, e₀ = 1 | PROVED conditional on (KP_i) + [T]-properness (inherited, displayed) | S6.2 |
| consumers (GD3-FULL/OL-B; GD3-MIN Route B + clause-(4); B-M2 OL-1(b) GMN face) | served RESTRICTED (exact restrictions displayed) | S6.3 |
| deeper orders, e₀ ≥ 2, general σ_j family | NOT PROVED — scope box | S7 |
| falsifiers: fresh N-1 T9/T2 + new preregistered TS1 family | ALL GREEN (0 violations) | S8 |

## S1. Setting, dictionary specialization, and the two operators verbatim

Consumed from the GRB leaf (not re-derived): TR-5 (index dictionary, r = i+1 = 2,
φ_k ↔ Φ_{k−1}), Lemma RESCALE (S2.1: w₂ = E₂µ₂ exactly, gradeds identified under
γ = E₂α), TR-1-corr (sign mirror), (H-CHAIN) (the tower is a MacLane chain of
µ₂ = w₂/E₂ in FGMN's Def 3.1 sense — the leaf's standing input, consumed here at
depth 2), and the GD23 standing GMN rows (w_i ↔ v_{i+1}, K_i ↔ F^{GMN}_{i+1},
engine residuals = GMN-convention residuals). On the stratum: E₁ = e₀ = 1,
E₂ = e₁; FGMN level data (e₁, h₁)^F = (1, h₀), (e₂, h₂)^F = (e₁, h₁);
e(µ₁) = 1 (so Γ₁ = ℤ: level-1 FGMN weights are ALREADY integers — the only
RESCALE dilation firing is the outer γ = e₁α), e(µ₂) = e₁.

**Formula pins (each with its non-textual leg; §S8.1 lists the mechanical pins).**

* **FGMN eq (12)** (fgmn.txt:1195): u(α)e_i + s(α)h_i = e(µ_i)α, 0 ≤ s(α) < e_i;
  Bézout ℓ_ih_i + ℓ′_ie_i = 1 with **0 ≤ ℓ_i < e_i** (fgmn.txt:1073, §3.3), and
  s(1/e(µ_i)) = ℓ_i, u(1/e(µ_i)) = ℓ′_i (remark under eq (12)). Level-0
  conventions (fgmn.txt:1040–1075): e₀^F = 1, h₀^F = 0, φ₀ = x, π₀ = π₁ = π,
  so ℓ₀ = 0, ℓ′₀ = 1. Numerical data: w_i := µ_{i−1}(φ_i) — hence
  w_i + λ_i = µ_i(φ_i) (augmentation display).
* **FGMN Def 3.15 (CORRECTED — the N-1 headline artifact).** True display:
  **ǫ_i(α) = (z_i)^{ℓ′_i s(α) − ℓ_i u(α)} ∈ F*_{i+1}**. Non-textual legs, redone
  for this unit: (leg 1) fresh `pdftotext -layout` on /tmp/litunit/fgmn.pdf p.18:
  the ′ is an isolated glyph at byte-column 51 of its own line, directly above
  the FIRST ℓ (column 50); the second ℓ sits at column 58 — the prime belongs to
  the s-multiplier. (leg 2) the i = 0 semantic check printed under the
  definition: "For i = 0, we get ǫ₀(α) = (z₀)⁰ for all α ∈ ℤ" — with
  (ℓ₀, ℓ′₀) = (0, 1) and s ≡ 0 (e₀^F = 1) the corrected reading gives z₀⁰
  verbatim; the garbled reading gives z₀^{−α}. (leg 3) harness pins T0
  (Lemma-4.3 Bézout algebra, 0/23,111 symbolic), P3/P12 (Cor 4.7(3) fails under
  the misreading; the misread canonical lift FACTORS over ℚ₂ by PARI while the
  corrected lift is irreducible — 2/2 kill-shot instances, re-run fresh, S8).
* **FGMN Def 3.16** (fgmn.txt:1246): for g = Σ_s a_sφ_i^s ∈ P_α(µ_i),
  R_{i,α}(g) = Σ_{0≤j} ǫ_{i−1}(α_j)·R_{i−1,α_j}(a_{s_j})(z_{i−1})·y^j ∈ F_i[y],
  s_j := s(α) + je_i, α_j := α − s_j(w_i + λ_i) ∈ Γ_{i−1}; and at i = 0:
  R_{0,α}(g) = g(y)/π^α ∈ F₀[y].
* **FGMN Def 3.18** (fgmn.txt:1354): R_i(g) := R_{i,α}(g)/y^{j₀},
  j₀ = ⌊s(g)/e_i⌋ (s(g) = the initial abscissa of the λ_i-component of N_i(g));
  R_i(0) := 0.
* **GMN Defs 2.19–2.21** (gmn.txt:1823–1906; layout re-extraction of gmn.pdf
  p.26 confirms a single UNPRIMED ℓ_{r−1} in both t-displays — no glyph float
  on the GMN side): t_{r−1}(i) = (s_{r−1}(a_i) − ℓ_{r−1}u_i)/e_{r−1} with
  u_i := v_r(a_iφ_r^i) = v_r(a_i) + i·v_r(φ_r) (their (18) note: the ordinate
  includes the key value); c_i := z_{r−1}^{t_{r−1}(i)}·R_{r−1}(a_i)(z_{r−1}) if
  (i, u_i) lies on N := N_r^−(f), c_i := 0 strictly above;
  R_{λ_r}(f)(y) := c_s + c_{s+e_r}y + ⋯ + c_{s+de_r}y^d, (s, u) the initial
  point and d the degree of the λ_r-component S. Inner data (gmn.txt §2.1):
  s_{r−1}(a) = the initial abscissa of S_{r−1}(a) = S_{λ_{r−1}}(N^−_{r−1}(a));
  R_{r−1} = the order-(r−1) residual operator w.r.t. λ_{r−1}; the type fixes
  λ_{r−1}, so the inner read is taken at the tower's FIRST-read slope. Order-1
  layer (gmn.txt:389–483): F_φ := O[x]/(π, φ), red : O[x] → F_φ,
  v(Σb_jx^j) := min v(b_j); c_i = red(a_i/π^{u_i}) on the component, 0 above;
  R_λ(f) = c_s + c_{s+e}y + ⋯ + c_{s+de}y^d. Valuation (Def 2.5):
  v_r(P) = e_{r−1}·H_{r−1}(S_{r−1}(P)) — at r = 2 on the stratum
  (e₁^{GMN} = e₀ = 1, |λ₁| = h₀): v₂(P) = min_m(w₀(b_m) + m·h₀) = w₁(P)
  verbatim, and v₂(φ₂) = w₁(Φ₁); order-1: v₁ = w₀, v₁(φ₁) = 0.
* **GMN Bézout convention note (display duty).** GMN §2.1 prints
  "ℓ_i, ℓ′_i ∈ ℤ are FIXED integers such that ℓ_ih_i − ℓ′_ie_i = 1" — sign
  opposite FGMN's ℓ′-term and NO normalization range. Changing ℓ_{r−1} by
  e_{r−1}k changes t_{r−1}(i) by −k·u_i, i.e. rescales the residual polynomial
  by a z-power character — the same normalization freedom GMN themselves
  display for a change of π at order 1 (gmn.txt:487). The project's standing
  GMN-convention operator (GD23 rows; the CU-1/N-1 harness) takes the
  NORMALIZED representative 0 ≤ ℓ < e, which agrees with FGMN's ℓ mod e; at
  e_{r−1} = 1 (our stratum) it forces **ℓ₁^{GMN} = 0 and t₁(i) = s₁(a_i)**.
  Everything below is w.r.t. that normalized choice — a displayed hypothesis
  of the theorem (S0). **Exact transformation law under an unnormalized choice
  [REV 2: pass-1 gap 2 — the acceptance-draft sentence "absorbed by
  TR-3′-GEN's per-argument-unit allowance" was WRONG and is struck]:**
  replacing ℓ by ℓ + qe changes t_{r−1}(i) to t_{r−1}(i) − q·u_i, and along
  the λ-side the ordinates march as u_{s+je₁} = u − jh₁ (from
  e₁u_i + ih₁ = γ), so the coefficients acquire z^{−q(u−jh₁)} and

      R′_λ(y) = z^{−q·u(f)} · R_λ( z^{q·h₁} · y )

  — a per-argument SCALAR times a fixed VARIABLE RESCALING y ↦ z^{qh₁}y (the
  V26-genre operation), NOT a per-argument unit alone; it degenerates to a
  scalar only when z^{qh₁} = 1 or d = 0. The ord-level consumers survive any
  such choice (a variable rescaling preserves ψ-adic orders under the matching
  label rescaling, exactly R2.3's argument), but the ON-THE-NOSE identity is
  normalization-scoped (S7).
* **Both operators read the SAME development.** FGMN Def 3.16 at i = 2 reads
  the φ₂-expansion; GMN (18) at r = 2 reads the φ₂-adic development; under
  TR-5, φ₂ ↔ Φ₁ on both sides: the common object is the Φ₁-development
  f = Σ_i a_iΦ₁^i, deg a_i < deg Φ₁. (The state key Φ₂ = φ₃ ↔ the NEXT key is
  not in the development; it enters only at the S6.2 label tie.)

**Integer-weight forms on the stratum (through RESCALE, displayed once).**
Outer eq (12) at i = 2: with γ = E₂α = e₁α, (12-int) u·e₁ + s·h₁ = γ,
0 ≤ s < e₁ — the leaf's S5.2 display. Slot data: s_j = s(γ) + je₁;
α_j = α − s_jµ₂(φ₂) and E₂µ₂(φ₂) = e₁(w₁(Φ₁) + h₁/e₁) = γ₂, so the level-1
integer weight of the slot-j digit is

    (SLOT)   γ_j := E₁α_j = α_j = (γ − s_j·γ₂)/e₁ ∈ ℤ

(integrality: γ ≡ s(γ)h₁ and s_jγ₂ ≡ s_jh₁ ≡ s(γ)h₁ (mod e₁)). Level-1 eq (12)
at i = 1, (e₁, h₁)^F = (1, h₀), e(µ₁) = 1: u·1 + s·h₀ = β, 0 ≤ s < 1 forces
**(s, u) = (0, β) for every β ∈ ℤ**; Bézout at level 1: ℓ₁h₀ + ℓ′₁·1 = 1 with
0 ≤ ℓ₁ < 1 forces **(ℓ₁, ℓ′₁) = (0, 1)**. Level-1 slot data (i = 1):
s_m = s(β) + m·e₁^F = m; β_m = β − m·µ₁(φ₁) = β − m(µ₀(φ₁) + λ₁) = β − m·h₀
(µ₀(φ₁) = w₀(Φ₀) = 0 by the monic-lift normalization; λ₁ = h₀/e(µ₁) = h₀).

## S2. The σ-identifications, constructed and march-compatible

The two residue towers. FGMN (fgmn.txt:1027–1032): F₀ = F,
F_{i+1} = F_i[z_i] with ψ_i := the minimal polynomial of z_i over F_i. Project:
K₀ = F[X]/(φ̄₀(X)), K₁ = K₀[z]/(ψ₀(z)), z̄ := the class of z.

**σ₀ : F₁ → K₀, z₀ ↦ X̄.** Well-defined iso: by FGMN Cor 5.6(2)
(fgmn.txt:1752, chain-internal at index 0 < r — only the chain (H-CHAIN)
provides is read), ψ₀^F = R₀(φ₁) = φ₁(y)/π^{µ₀(φ₁)} reduced = φ̄₀(y) (Gauss
content 0). So F₁ = F[y]/(φ̄₀) and K₀ = F[X]/(φ̄₀) are the same F-algebra under
y ↦ X; σ₀ matches the distinguished generators. Under σ₀ the level-0 reads are
LITERALLY equal: for b ∈ O[x], deg b < d₀, and δ ≤ w₀(b),

    (RES0)   σ₀( R_{0,δ}(b)(z₀) ) = red( b/π^δ )(X̄) ∈ K₀

— both sides are "divide by π^δ, reduce coefficientwise, evaluate at the root
class" (FGMN Def 3.16 i = 0 line ↔ GMN's red; note red(b/π^δ) = 0 whenever
w₀(b) > δ, including δ < 0, since every coefficient of bπ^{−δ} then has
positive valuation).

**σ₁ : F₂ → K₁ extending σ₀ with z₁ ↦ z̄.** Well-defined iso iff
σ₀[y](ψ₁^F) = ψ₀ (image of the minimal polynomial = the minimal polynomial of
the target generator). Supply: (i) ψ₁^F = R₁^F(φ₂) = R₁^F(Φ₁) — Cor 5.6(2)
chain-internal at index 1 < r = 2 ((H-CHAIN) consumed, nothing extended);
(ii) Lemma A below at a := Φ₁ (Lemma A's statement and proof quantify over
ALL nonzero a ∈ O[x] — only the finite Φ₀-development is used, no degree
bound; see its scope note), giving
σ₀[y](R^F_{1,β}(Φ₁)) = y^{s₁(Φ₁)}R_{λ₀}(Φ₁) with β = w₁(Φ₁), hence on side
reads σ₀[y](R₁^F(Φ₁)) = R_{λ₀}(Φ₁) (FGMN j₀ = ⌊s(Φ₁)/e₁^F⌋ = s₁(Φ₁) at
e₁^F = 1, and FGMN's s(g) is the same initial-abscissa datum as GMN's s₁ —
identical polygon, S3 step (A4)); (iii) the read's residual condition
R_{λ₀}(Φ₁) ∼ ψ₀ (GD23 §1.2 lawful-read datum, an INPUT), upgraded to equality:
Φ₁ monic makes the top Φ₀-digit b_{g₀} = 1 with w₀ = 0, which is on-line
(0 + g₀h₀ = w₁(Φ₁)), so R_{λ₀}(Φ₁) has monic leading coefficient c_{g₀} = 1;
lawfulness forces residual degree g₀ = deg ψ₀, so s₁(Φ₁) = 0 and R_{λ₀}(Φ₁) is
MONIC of degree g₀; a monic associate of the monic ψ₀ equals ψ₀. Then
σ₀(ψ₁^F) = σ₀(R₁^F(Φ₁)) = R_{λ₀}(Φ₁) = ψ₀. ∎

**March-compatibility.** (i) σ₁|_{F₁} = σ₀ and both squares with the tower
inclusions F₀ ⊂ F₁ ⊂ F₂ / F ⊂ K₀ ⊂ K₁ commute (construction). (ii) The
evaluation square: for every P ∈ F₁[y],

    (SQ)   σ₁( P(z₁) ) = ( σ₀[y]P )( z̄ )

(σ₁ is the quotient of σ₀[y] by (ψ₁^F) ↦ (ψ₀)). This is the exact sense in
which the SAME σ-thread runs through both march levels: the level-1
identification used INSIDE the level-2 coefficients is σ₀, transported by (SQ)
— the "level-by-level compatible family" clause of TR-3′-GEN, at order 2.

## S3. Lemma A — the level-1 finite algebra (the inner reads agree up to the recorded z-shift)

**Lemma A.** Let a ∈ O[x] be nonzero with Φ₀-development a = Σ_{m≥0} b_mΦ₀^m
(deg b_m < d₀; the development is finite), and let β := w₁(a) =
min_m(w₀(b_m) + m·h₀). Call m ON-LINE iff w₀(b_m) = β − m·h₀ (equivalently
(m, w₀(b_m)) lies on the λ₀-component of N₁(a), slope −h₀ — the polygon over
the SAME development with ordinates u_m = w₀(b_m), since v₁ = w₀ and
v₁(φ₁) = 0), and let s₁(a) := the least on-line m. Then, in K₀[y]:

    (A)   σ₀[y]( R^F_{1,β}(a) ) = Σ_{m on-line} red(b_m/π^{β−mh₀})(X̄) · y^m
                                = y^{s₁(a)} · R_{λ₀}(a).

*Proof, coefficient by coefficient.*
(A1) Def 3.16 at i = 1 with the S1 stratum data: s_m = m, β_m = β − mh₀, and
ǫ₀(β_m) = 1 for every m — BOTH by FGMN's printed convention under Def 3.15
("we convene that ǫ₀(α) = 1, even in the case z₀ = 0") AND by the corrected
formula itself (exponent ℓ′₀s − ℓ₀u = 1·0 − 0·u = 0, using s ≡ 0 at e₀^F = 1).
So R^F_{1,β}(a) = Σ_m R_{0,β_m}(b_m)(z₀)·y^m.
(A2) The m-th coefficient: R_{0,β_m}(b_m)(z₀) = the reduction of b_m/π^{β_m}
evaluated at z₀ (Def 3.16's i = 0 line). Domain check: the development digit
satisfies w₀(b_m) ≥ β − mh₀ = β_m (definition of β as the min), i.e.
b_m ∈ P_{β_m}(µ₀) always — Def 3.16 is applied inside its printed domain.
(A3) Dichotomy: if w₀(b_m) > β_m (off-line, including b_m = 0 and β_m < 0),
every coefficient of b_mπ^{−β_m} has positive valuation, so the reduction is 0
and the coefficient vanishes. If w₀(b_m) = β_m (on-line), the coefficient is
σ₀-image red(b_m/π^{β_m})(X̄) ≠ 0 by (RES0) (nonzero because some coefficient
of b_m has valuation exactly β_m and deg b_m < d₀ = deg φ̄₀). This proves the
first equality of (A), and shows supp = the on-line set.
(A4) The GMN side (order-1 pins, S1): N₁(a) is the polygon of the SAME points
(m, w₀(b_m)); its λ₀ = −h₀-component consists exactly of the on-line m (the
touching line of slope −h₀ has intercept min(w₀(b_m) + mh₀) = β); GMN's
c_m = red(b_m/π^{u_m}) with u_m = w₀(b_m) = β − mh₀ on the component, 0
strictly above; R_{λ₀}(a) = c_{s₁} + c_{s₁+1}y + ⋯ + c_{s₁+d}y^d (stride
e₀ = 1). Two facts close (A): (a) every on-line m lies in [s₁, s₁ + d] (the
component's abscissa range, s₁ + d = the last on-line m), and within that range
GMN's c_{s₁+k} is red(b/π^δ)(X̄) on-line and 0 off-line — the SAME values as
(A3) at abscissa m = s₁ + k; (b) below s₁ and above s₁ + d both sides vanish
((A3) off-line; GMN reads nothing there). Hence
Σ_{m on-line} red(·)y^m = y^{s₁}·Σ_k c_{s₁+k}y^k = y^{s₁}R_{λ₀}(a). ∎

Scope note: the computation never uses deg a < deg Φ₁ — only the finite
Φ₀-development. It applies verbatim to a = Φ₁ (used at S2(ii)). For the
Theorem's slots the digits do satisfy deg a < deg Φ₁.

## S4. The twist is gone on the stratum (the leaf's FACT, proof displayed)

**Restated from GRB S3.2 corrected picture / S3.4 REV 13 — FACT, not new.**
For every slot weight β ∈ ℤ = Γ₁: ǫ₁(β) = z₁^{ℓ′₁s(β) − ℓ₁u(β)} = z₁^{1·0 − 0·β}
= 1, since at e₁^F = e₀ = 1 the eq-(12) data are (s, u) = (0, β) and the
normalized Bézout pair is (ℓ₁, ℓ′₁) = (0, 1) (S1). **ǫ₁ ≡ 1.** This is the
entire marched-twist system at depth 2 (the inner ǫ₀ ≡ 1 already entered at
(A1)); the "pre-ǫ" qualifier of the charter is therefore discharged into the
FULL coefficient agreement below. Falsifier: TS1d, 0/2,080 (S8).

## S5. The theorem: the assembled per-slot computation

Fix nonzero f ∈ O[x] with Φ₁-development f = Σ_i a_iΦ₁^i and γ := w₂(f) =
min_i(e₁w₁(a_i) + iγ₂); write (s(γ), u(γ)) for the (12-int) data.

**(i) Support is the same set on both sides.** GMN's order-2 ordinate is
u_i = v₂(a_i) + i·v₂(φ₂) = w₁(a_i) + i·w₁(Φ₁) (S1 pins), so

    e₁·u_i + i·h₁ = e₁w₁(a_i) + i·(e₁w₁(Φ₁) + h₁) = e₁w₁(a_i) + i·γ₂ ≥ γ,

with equality iff (i, u_i) lies on the λ₁ = −h₁/e₁-component S of N₂^−(f)
(the touching-line intercept of the λ-line is γ/e₁ in µ-ordinates; this is the
leaf's A-LAW-2 row, re-derived in two lines). Call these i ON-LINE. Every
on-line i satisfies i ≡ s(γ) (mod e₁): ih₁ ≡ γ ≡ s(γ)h₁ (mod e₁) and
gcd(e₁, h₁) = 1. Since s(γ) is the least non-negative residue, every on-line i
equals s_j = s(γ) + je₁ for a unique slot j ≥ 0. Conversely FGMN's slot j is
on-line iff w₁(a_{s_j}) = γ_j (SLOT): the two on-line notions coincide, and the
initial/final on-line abscissas s := s₀(f) and s′ give GMN's component data
(s, d) with d = (s′ − s)/e₁ and FGMN's j₀ = (s − s(γ))/e₁ = ⌊s/e₁⌋
(0 ≤ s(γ) < e₁ and s ≡ s(γ)).

**(ii) Per-slot identity.** Slot j, abscissa s_j, digit a := a_{s_j}, level-1
weight γ_j (SLOT). Three cases.
* a = 0, or a ≠ 0 with w₁(a) > γ_j (off-line): FGMN's coefficient is
  ǫ₁(γ_j)·R^F_{1,γ_j}(a)(z₁) with EVERY level-1 coefficient of R^F_{1,γ_j}(a)
  zero — for each Φ₀-digit b_m of a, w₀(b_m) ≥ w₁(a) − mh₀ > γ_j − mh₀, so
  (A3)'s dichotomy gives 0 (domain: a ∈ P_{γ_j}(µ₁), i.e. w₁(a) ≥ γ_j — true
  for EVERY nonzero digit from γ = min_i(e₁w₁(a_i) + iγ₂); for a = 0 the read
  is 0 by linearity/the printed R_i(0) = 0 convention). GMN: (s_j, u_{s_j}) strictly above N (or
  a = 0, u = ∞), so c_{s_j} := 0. Both sides 0. If s_j is outside GMN's
  component range [s, s′] entirely, GMN reads no coefficient there and FGMN's
  is 0 by the same dichotomy — consistent (below s: strictly above the line
  since s is the FIRST on-line abscissa; above s′: likewise).
* on-line: w₁(a) = γ_j, a ≠ 0. Then

      σ₁( C^F_j ) = σ₁( ǫ₁(γ_j) · R^F_{1,γ_j}(a)(z₁) )
                  = σ₁( R^F_{1,γ_j}(a)(z₁) )                 [S4: ǫ₁ ≡ 1]
                  = ( σ₀[y] R^F_{1,γ_j}(a) )( z̄ )            [(SQ)]
                  = z̄^{s₁(a)} · R_{λ₀}(a)(z̄)                [Lemma A, β = γ_j]
                  = z̄^{t₁(s_j)} · R₁(a)(z̄)                  [t₁(s_j) = s₁(a):
                                                    ℓ₁^{GMN} = 0 normalized, S1]
                  = c^G_{s_j}.                               [GMN Def 2.20]

  (GMN's inner R₁(a) is the order-1 residual w.r.t. λ₀ = the first-read slope —
  fixed by the type data, S1 — i.e. exactly Lemma A's R_{λ₀}(a).)

**(iii) Assembly.** By (i)+(ii), σ₁-coefficientwise the FGMN weight read
R^F_{2,γ}(f) = Σ_j C^F_j y^j has support {j : s_j on-line} ⊆ [j₀, j₀ + d],
with σ₁(C^F_j) = c^G_{s_j} at every j and c^G nonzero exactly on-line. GMN's
side polynomial is R_λ(f) = Σ_{k=0}^{d} c_{s+ke₁}y^k. Re-indexing j = j₀ + k
(s_j = s + ke₁):

    σ₁( R^F_{2,γ}(f) ) = Σ_k c_{s+ke₁} y^{j₀+k} = y^{j₀} · R_λ(f),

and dividing by y^{j₀} (FGMN Def 3.18, j₀ = ⌊s₀(f)/e₁⌋ = ⌊s/e₁⌋ by (i)):
σ₁(R^F₂(f)) = R_λ(f) EXACTLY — nonzero constant terms aligned at the shared
initial abscissa s, degrees d on both sides. Per-argument units ≡ 1. ∎

**Remark (what made this possible).** The proof is 100% printed-formula
algebra once three stratum facts hold: s ≡ 0 and ℓ = 0 at level 1 kill the
outer twist (S4) AND collapse GMN's t-exponent to the bare initial abscissa,
and stride-1 level-1 marching makes FGMN's weight read a y-shift of GMN's side
read (Lemma A). At e₀ ≥ 2 all three fail simultaneously — that is the open
stratum (S7), and nothing here bears on it.

## S6. Consumers: what is now a theorem, and for whom

### S6.1 TR-3′-GEN(i) restricted to order 2 — conjecture → THEOREM

The S3.4 REV-11/13 statement, clause (i), demanded on the stratum: the
level-by-level compatible σ_j family, per-argument units, FGMN side read =
project/GMN R_λ. Delivered at ORDER 2 with e₀ = 1 by S2 + S5 with units ≡ 1
and the σ-family {σ₀, σ₁}. Inputs consumed: (H-CHAIN) (σ-construction, Cor 5.6
chain-internal legs), the lawful-read residual data for Φ₁ (S2(iii)), the GD23
standing GMN rows, RESCALE — i.e. the (GR-B-STR) ladder inputs the leaf's S6.2
already prices, NOTHING NEW. Through TR-4 (leaf S4, R^{gr}(in f) = R₂^F(f)),
the (2′) final leg "R^{gr}(in f) ∼ R_λ(f)" holds at these states in the exact
form σ₁(R^{gr}(in f)) = R_λ(f). **Consumers GD3-FULL/OL-B: served at every
order-2, e₀ = 1 state; at deeper orders and at e₀ ≥ 2 they continue to sit on
OPEN TR-3′-GEN.**

### S6.2 TR-3-ORD-X restricted to order 2, e₀ = 1 — proved with inherited conditionality

TR-3-ORD-X (leaf R2.3): ord_{ψ₁}(R_λ(f)) = ord_L(R^{FGMN}(f)), labels tied by
clause (3) on each side. On the stratum:
1. **Label tie.** ψ₂^F := R₂^F(Φ₂) is the minimal polynomial of z₂ by Cor
   5.6(2) read in the EXTENDED chain (Φ₀, Φ₁, Φ₂) — this consumes **(KP_i)**
   (Φ₂ ∈ KP(µ₂) + the chain condition), exactly as leaf S6.3 displays; the
   Theorem at f = Φ₂ gives σ₁(ψ₂^F) = R_λ(Φ₂); the state read's datum
   R_λ(Φ₂) ∼ ψ₁ upgrades to equality **[REV 2: pass-1 gap 3 — the Φ₂
   analogue of S2(iii)'s argument, now displayed rather than waved at]**: the
   lawful monic state-key lift has deg Φ₂ = e₁g₁·deg Φ₁ and
   w₂(Φ₂) = e₁g₁·γ₂ (the read's data; leaf T5 rows), so its Φ₁-development
   has top digit a_{e₁g₁} = 1 (monic, degree exact) with w₁(a_{e₁g₁}) = 0,
   whose ordinate u = e₁g₁·w₁(Φ₁) satisfies
   e₁u + (e₁g₁)h₁ = e₁g₁(e₁w₁(Φ₁) + h₁) = e₁g₁γ₂ = w₂(Φ₂) — ON-LINE — so
   c_{e₁g₁} = 1 is the leading coefficient; ∼ψ₁ forces
   deg R_λ(Φ₂) = g₁ = (e₁g₁ − s₀(Φ₂))/e₁, i.e. s₀(Φ₂) = 0; a monic associate
   of the monic ψ₁ equals ψ₁. So σ₁(ψ₂^F) = ψ₁.
2. **Transport.** σ₁ extends to a ring iso F₂[y] → K₁[y] (coefficientwise),
   so ord_{ψ₁}(R_λ(f)) = ord_{σ₁ψ₂^F}(σ₁R₂^F(f)) = ord_{ψ₂^F}(R₂^F(f)) for
   every nonzero f (Theorem + iso-invariance of divisibility order).
3. **FGMN-internal leg.** ord_{ψ₂^F}(R₂^F(f)) = ord_L(R(f)) is Cor 5.15
   (fgmn.txt:1973) with the **[T]-pinned properness** of the state key — the
   TR-6 one-home (leaf S6.4); NOT discharged here, exactly as in proved Lemma
   TR-3-ORD.
**Hence TR-3-ORD-X holds at order-2, e₀ = 1 states, conditional on
(KP_i) + [T]-properness — the same package its consumers already price.**
Equivalently: Route B's ord chain closes PROJECT-FACING (not merely
FGMN-internally) on this stratum. **Consumers GD3-MIN Route B and ALL
clause-(4) ord-consumers: served at order-2, e₀ = 1 states, with (KP_i)+[T]
inherited; elsewhere they continue to sit on OPEN TR-3-ORD-X.**

### S6.3 B-M2 OL-1(b), GMN-verbatim face

B-M2's OL-1(b) is conditional on GRADED-READ, REC-SL where applicable, AND
TR-3-ORD-X (BM2 pass-2 fold, item 6); its GMN-verbatim face rides the
TR-3-ORD-X seam. **That seam is now discharged at order-2, e₀ = 1 states
(S6.2, with (KP_i)+[T] inherited). OL-1(b) remains conditional on its OTHER
displayed seams (GRADED-READ; REC-SL where applicable) everywhere, and on
TR-3-ORD-X outside the stratum.** No other B-M2 clause is touched.

## S7. Scope box — what this unit does NOT prove

* **Deeper orders (≥ 3), including all-e_j = 1 towers.** Lemma A and the S5
  assembly are depth-2-specific (the inner operator is the LEVEL-1 read). At
  order 3 the inner reads are level-2 marched operators carrying their own ǫ
  system; even with every e_j = 1 the inner-vs-inner comparison needs the
  level-shifted analogue plus a recursion that is NOT written here. The
  general σ_j family of TR-3′-GEN stays OPEN.
* **e₀ ≥ 2 at order 2.** The conjectured corrected-ǫ twist law (S3.4(ii))
  stands OPEN and UNTESTED against GMN (N-1 honest deferral (b)); nothing here
  bears on it.
* **TR-3′-GEN and TR-3-ORD-X as stated (all orders, whole strata)** remain
  OPEN; this unit converts exactly their {order 2, e₀ = 1} restrictions.
* **D-ITER / the E_i > 1 composite carry presentation** — untouched.
* **(KP_i) and the [T] properness pin** — consumed, not discharged (S6.2).
* **Normalization scope.** The on-the-nose identity is w.r.t. the project's
  standing NORMALIZED GMN convention (0 ≤ ℓ < e; at e = 1, ℓ = 0) — a
  displayed hypothesis (S0), external to GMN's print. Under an unnormalized
  choice R_λ transforms by the S1 law R′_λ(y) = z^{−qu(f)}R_λ(z^{qh₁}y) — a
  per-argument scalar TIMES a variable rescaling **[REV 2: pass-1 gap 2 —
  this is NOT absorbable as a per-argument unit alone]**; the on-the-nose
  clause does not extend, though the S6.2 ord conclusions survive any fixed
  choice (label and argument rescale together).
* **GMN's printed base scope** is finite extensions of ℚ_p (gmn.txt §1.2);
  the GMN-convention operator is consumed here as the project's engine
  convention over an arbitrary complete DVR (GD23 standing identification,
  leaf S1.1 scope note). The equal-characteristic towers are covered by the
  falsifiers (F_p[[t]] rings) against exactly that reading.

## S8. Falsifier record (all preregistered, all green, 2026-08-08)

### S8.1 Fresh re-run of the N-1 harness (T9/T2 cover + the ǫ pins)

`python3 verification/openmath/grb_order2_check.py` (seed 20260808, exact
arithmetic, 40 towers = 10 rosters × 4 rings; log
/tmp/openmath/TR3S1_grb_fresh_run.log): **ALL GREEN, 33,547 samples, 0
violations on every predicted-0 family.** The rows this unit rides:
T0 0/23,111 (Lemma-4.3 exponent algebra — pins the corrected ǫ);
T9 0/500 (pre-ǫ agreement + ratio ≡ 1 at e₀ = 1) with T9(iii-C) 300/300
(the retired artifact law refuted against the true operator); T2 0/1,200
(two-form/support); T5 0/40 (R₂(Φ₂) = ψ₁ — the S6.2 label tie, FGMN side);
T11 0/20 + P12 2/2 kill-shots [False, True] (external PARI discrimination of
the ǫ reading); P3 145 reading-P violations, 0 on z₁ = 1 towers (character
blindness), 13 on (ℤ₃, C).

### S8.2 The NEW preregistered TR3-S1 family (this unit's sharpening)

T9 tests ONE instance shape (three consecutive on-line slots, s(γ) = 0,
exactly-on-line digits). The Theorem is UNIVERSAL in nonzero f ∈ O[x], so per
the charter a new family was preregistered and run:
`verification/openmath/tr3s1_check.py` (seed 20260812; e₀ = 1 roster towers
A/B/F/G/I × 4 rings). **Independence perimeter, stated exactly [REV 2:
pass-1 gap 4 — the acceptance-draft "INDEPENDENTLY" was too broad]:** the
GMN side re-implements from Defs 1.6/1.9 + (18)/2.19–2.21 its OWN polygons,
on-line sets, initial abscissas s₁, twist exponents t₁ (hard-coding the
normalized ℓ₁ = 0, i.e. t₁ = s₁ — the S0 hypothesis, so the family CANNOT
falsify the normalization choice), and coefficient assembly; it SHARES with
the FGMN side the Tower object (base-ring arithmetic, Φ₀/Φ₁/Φ₂, residue
fields) and the valuations w₀/w₁ consumed as v₁/v₂ (the standing
dictionary). So TS1 falsifies the S5 comparison CONDITIONAL on those shared
inputs; the shared inputs themselves are covered by the EXTERNAL legs of the
fresh N-1 run — T11/P11/P12 (PARI factorpadic decides tower/key/twist
semantics with no shared code) and T0 (symbolic Bézout pin) — not by TS1.
σ₁ = identity of the shared concrete K₁ model (the σ-content is S2's
mathematics, not testable numerics). Sample shapes: arbitrary random f,
structured mixtures, Φ₁^a·g corners (j₀ > 0), constants/π-powers,
π^k-scalings, Φ₂^μ·Q multiples, f = Φ₂. **TS1b amendment (pass-1 gap 4,
minor):** the side-read comparison now ALSO checks R₂(f) carries no trailing
zeros and has length exactly d + 1 with nonzero ends (the acceptance-draft
check compared fp_norm-ed lists, which would have passed a spurious
trailing-zero R₂); amended, re-run, still 0 violations.

    TS1a  per-abscissa coefficient + support agreement   0/2,080   GREEN
    TS1b  side-read alignment (R₂ list = R_λ list + j₀)  0/2,080   GREEN
    TS1c  label tie at f = Φ₂ (both side reads = ψ₁)     0/20      GREEN
    TS1d  ǫ₁ = 1 at every on-line slot                   0/2,080   GREEN
    TOTAL 6,260 samples, 0 violations — ALL GREEN.

A TS1a/b violation falsifies S5; TS1c falsifies the S6.2 label tie; TS1d
falsifies S4.

## S9. Honesty ledger and attack surface for the hostile pass

1. The Def 3.15 pin is the load-bearing correction: attack the three
   non-textual legs (S1). The byte-column check was REDONE for this unit, not
   inherited.
2. FGMN w_i = µ_{i−1}(φ_i) (fgmn.txt:1040 block) → α_j = α − s_jµ₂(φ₂) → the
   (SLOT) integer form: an off-by-one here shifts every γ_j; cross-checked by
   T9/TS1 greens and by γ_j-integrality.
3. The GMN ℓ-sign/normalization note (S1): verify GMN's ℓh − ℓ′e = 1 print;
   note the REPRESENTATIVE (not just ℓ mod e) enters t — the S1 REV-2
   transformation law displays the dependence; the on-the-nose claim is
   normalization-scoped as an S0 hypothesis (S7).
4. S2(iii)'s monicity upgrade (∼ to =) at Φ₁ and at Φ₂ (S6.2): uses lawful
   monic lifts of full residual degree; a non-monic or partial-degree lift
   would leave only ∼, degrading the label tie to associates — which the ord
   conclusions survive, but the displayed EQUALITIES would not.
5. Lemma A's negative-β_m and zero-digit corners (A3): both routed through
   "reduction of b·π^{−β_m} with all-positive valuations is 0".
6. The claim that GMN's inner R₁ is read at the tower's first-read slope:
   gmn.txt §2.1 ("S_i(P) is the λ_i-component … depend only on the reduced
   type") — the type fixes λ₁.
7. GMN's u_i includes i·v₂(φ₂) ((18) note). It cancels from t₁ only because
   ℓ₁ = 0 on the stratum; at e₀ ≥ 2 it does not — one reason S7's second
   bullet is genuinely open.
8. Everything consumed from the GRB leaf is at its REV-14 state (corrected ǫ,
   REV-13 scoping); nothing consumed from the superseded S3.2 law.

## REVISION 2 (2026-08-08) — hostile pass 1 folded

Pass 1 (Codex, fresh context, quote-and-classify; transcript
/tmp/openmath/TR3S1_pass1.jsonl): **VERDICT 0 critical / 4 justification
gaps.** Confirmed by the verifier: every literature transcription including
the corrected Def 3.15 prime placement; Lemma A; twist triviality; (SLOT);
support/reindexing; the S5 coefficient chain; the S6.2 divisibility-order
transport; NO circularity in the σ₁ construction (Lemma A independent of S5,
Cor 5.6 chain-internal); the S2 monicity upgrade; all degenerate corners
(zero digits, negative β_m, constants, π-powers, d = 0, initial zeros).
Folds executed at the flagged sites:
* gap 1 → the ℓ-normalization is now a DISPLAYED HYPOTHESIS in S0 (it is
  project convention, not GMN print);
* gap 2 → the wrong "absorbed by per-argument units" sentence STRUCK; the
  exact unnormalized-choice law R′_λ(y) = z^{−qu(f)}R_λ(z^{qh₁}y) displayed
  in S1 (scalar × variable rescaling), S7 + S9.3 synced;
* gap 3 → the Φ₂ monicity/on-line/degree computation displayed in S6.2
  (was "same two-line argument");
* gap 4 → S8.2 restated with the exact TS1 independence perimeter (what the
  family can and cannot falsify; external cover = T11/P11/P12 PARI + T0)
  and the TS1b trailing-zero/length check added to the harness and re-run
  (0 violations; amendment block in the script header).

— TR3-S1 composed 2026-08-08; falsifiers green; hostile pass 1 = 0 crit /
4 gap, all four folded above (REVISION 2); the 2-clean bar continues under
the orchestrator.

## PASS 2: CLEAN (first of two, on REV 2)

Pass 2 (Codex, hostile): **VERDICT CLEAN — 0 critical / 0 justification
gaps.** All four REV-2 folds verified sound with independent re-derivation
(the transformation law R′_λ(y) = z^{−qu(f)}·R_λ(z^{qh₁}y) re-derived from
the t_i shift; the units-≡-1 claim confirmed properly scoped to the
normalized q = 0 convention; the Φ₂ monicity top-digit computation checked;
the normalization hypothesis display correct). Report:
`TR3S1_pass2_2026-08-08.md`.

**Clean counter: 1/2.** Pass 3 (Fable fresh-context, max effort) decides.
