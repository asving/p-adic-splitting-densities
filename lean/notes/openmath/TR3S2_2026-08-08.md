# TR3-S2 — stratum (ii) of TR-3′-GEN closed by the compose argument: at order 2, ALL e₀, the FGMN/GMN discrepancy is EXACTLY the slot-keyed character z₁^{ℓ₀g₀h₀s_j}, and the compose kernel is a two-line Bézout identity

Unit chartered by SYNTHESIS PASS 5 F4 (docs/SYNTHESIS_PASS5_2026-08-08.md): thread
the slot-keyed character through the measured closed form (strata_probe FAMILY A,
(TWIST-LAW), 0/3,640) and derive TR-3′-GEN(ii) as a finite-algebra identity, in the
genre of the ACCEPTED TR3-S1 (lean/notes/openmath/TR3S1_2026-08-08.md). Parent leaf:
GRB_ORDER2_2026-08-05.md (REV 14; S3.4 REV-11 two-stratum re-pricing; corrected ǫ).
Genre: attempt-grade leaf proof note; finite algebra executed in full; every formula
pin carries a NON-TEXTUAL leg (the shared fgmn.txt extraction is CORRUPTED at
Def 3.15 and is NEVER consulted here — the corrected display is consumed from the
leaf/TR3-S1 with its N-1 pins T0/P3/P12). HARD FENCE honored: no D-ITER object
(digit-algebra iteration, composite anchors, cocycles) is consumed anywhere below;
the whole argument lives at the residual-OPERATOR level (see S7).

## S0. Statement and verdict

**Theorem TR3-S2 (proved below at attempt grade; falsifiers green through run 3;
pass 1 returned CRITICAL on three side displays — fixed at REVISION 1 below, the
theorem's compose chain itself verified in full; acceptance counter 0/2).** Standing setting of the GRB leaf S1.1 (GD23 §1.2), state of ORDER 2,
now on the FULL e₀-range (e₀ ≥ 1; at e₀ = 1 every twist factor below is 1 and the
statement degenerates to accepted TR3-S1): reads ν₀ = (e₀, h₀, ψ₀) then
ν₁ = (e₁, h₁, ψ₁), (e_j, h_j) coprime, ψ_j monic irreducible with ψ_j(0) ≠ 0;
Φ₀ a monic root lift (w₀(Φ₀) = 0, deg Φ₀ = d₀); Φ₁ a lawful monic lift of ν₀
(deg Φ₁ = e₀g₀d₀, w₁(Φ₁) = e₀g₀h₀, order-1 residual condition R_{λ₀}(Φ₁) ∼ ψ₀);
w₂ the order-2 state valuation, γ₂ = e₁w₁(Φ₁) + h₁. Let (ℓ₀, ℓ₀′) be the
**NORMALIZED level-1 Bézout pair**: ℓ₀h₀ + ℓ₀′e₀ = 1 with 0 ≤ ℓ₀ < e₀ — a
DISPLAYED HYPOTHESIS exactly as in TR3-S1 S0 (GMN's print fixes ℓ only up to
representative and sign convention; the normalized choice is the project's standing
engine convention, not re-verified from GD23). Let σ₀ : F₁ → K₀ and σ₁ : F₂ → K₁
be the tower identifications of S2 (z₀ ↦ X̄, z₁ ↦ z̄, σ₁|_{F₁} = σ₀ — constructed
below for ALL e₀). Write θ := ℓ₀g₀h₀ (**the character exponent**). Then for EVERY
nonzero f ∈ O[x], with γ := w₂(f), s := s₀(f) the initial abscissa of the
λ₂-component, j₀ := (s − s(γ))/e₁ = ⌊s/e₁⌋:

    (TR3-S2-coeff)   σ₁-coefficientwise, at every slot j (abscissa s_j = s(γ)+je₁):
                     σ₁( C^F_j ) = z̄^{θ·s_j} · c^G_{s_j},
                     with support EQUAL on both sides (nonzero exactly on-line);

    (TR3-S2-form)    σ₁( R^F₂(f) )(y) = z̄^{θ·s₀(f)} · R_λ(f)( z̄^{θe₁} · y ),

where C^F_j are the coefficients of FGMN's marched weight read R^F_{2,γ}(f)
(Defs 3.16/3.18, corrected Def 3.15, integer weights via RESCALE) and
c^G_i, R_λ(f) are the GMN-convention order-2 residual data (Defs 2.19–2.21 with
the (18) ordinate, normalized ℓ) — the project's engine-convention operator under
the GD23 standing identification. The discrepancy is a per-argument SCALAR
z̄^{θ·s₀(f)} times the FIXED variable rescaling y ↦ z̄^{θe₁}y; it is NOT a
per-argument unit alone (S5 Remark 2 + Lemma WIT — the units-only phrasing of the
acceptance-era TR-3′-GEN record statement is REFUTED at every state of this
stratum with z̄^{θe₁} ≠ 1, witness constructed), and it is
EXACTLY "the corrected weight-driven unit ǫ threaded per slot" conjectured by the
leaf's S3.4 REV-11 clause (ii). No (R1)/(R2) hypothesis is consumed: the identity
is total in nonzero f ∈ O[x].

| clause | status | where |
|---|---|---|
| σ₀, σ₁ constructed, well-defined, march-compatible at ALL e₀ | PROVED | S2 |
| Lemma A-GEN (level-1 read at stride e₀ = y-shift of GMN's order-1 read) | PROVED | S3 |
| Lemma EXP (the compose kernel — a two-line Bézout integer identity) | PROVED | S4 |
| the assembled per-slot agreement + support + two-form (TR3-S2) | PROVED (the unit's content) | S5 |
| units-only phrasing of the record TR-3′-GEN statement on stratum (ii) | REFUTED at every state with c ≠ 1 (witness constructed) | S5 Rem. 2 + Lemma WIT |
| TR-3′-GEN(ii) at order 2 (S3.4 REV-11 form) | THEOREM (conjecture → proved) | S6.1 |
| TR-3′-GEN at ORDER 2 = two-stratum theorem (with TR3-S1) | CLOSED at order 2; deeper orders OPEN | S6.1 |
| TR-3-ORD-X on stratum (ii) at order 2, GMN-lawful key | PROVED conditional on (KP_i) + [T]-properness (inherited, displayed) | S6.2 |
| the state-key conventions CAN differ on (ii) (GMN-lawful vs FGMN-normalized); they diverge EXACTLY on the twist-visible states, coinciding when the character is invisible on ψ₁'s support; invariant form (ORD-INV) | FOUND by the run-1 falsifier; PROVED + measured (20/52 towers twist-visible = exactly the divergent set) | S6.2 step 0 (Lemma KEY0), S8.2 |
| consumers (GD3-FULL/OL-B; GD3-MIN Route B + clause-(4); B-M2 OL-1(b) GMN face) | served at order 2 (exact forms displayed) | S6.3 |
| deeper orders (≥ 3), general σ_j family, D-ITER | NOT PROVED / NOT TOUCHED — scope box | S7 |
| falsifiers: recorded strata_probe FAMILY A + new preregistered TS2 family (runs 2–3, incl. TS2f witness construction) | ALL GREEN (0 violations) | S8 |

## S1. Setting, dictionary specialization at general e₀, and the operator pins

Consumed from the GRB leaf and TR3-S1 (not re-derived): TR-5 (index dictionary,
r = i+1 = 2, φ_k ↔ Φ_{k−1}), Lemma RESCALE (w_i = E_iµ_i exactly; E₁ = e₀,
E₂ = e₀e₁ — note that UNLIKE the e₀ = 1 stratum, RESCALE now fires at BOTH levels:
Γ₁ = (1/e₀)ℤ and level-1 FGMN weights are integers only after the E₁ = e₀
dilation), TR-1-corr, (H-CHAIN) (the tower is a MacLane chain of µ₂ = w₂/E₂,
consumed at depth 2), and the GD23 standing GMN rows (w_i ↔ v_{i+1},
K_i ↔ F^{GMN}_{i+1}, engine residuals = GMN-convention residuals). FGMN level
data on the stratum: (e₁, h₁)^F = (e₀, h₀), (e₂, h₂)^F = (e₁, h₁); e(µ₁) = e₀,
e(µ₂) = e₀e₁.

**Formula pins (all carried over from TR3-S1 S1 with their non-textual legs —
the byte-column/i = 0-semantic/PARI legs are INHERITED, not redone; what is NEW
at e₀ ≥ 2 is only which terms survive).**

* **FGMN eq (12)** at level i: u(α)e_i + s(α)h_i = e(µ_i)α, 0 ≤ s(α) < e_i;
  Bézout ℓ_ih_i + ℓ′_ie_i = 1 with 0 ≤ ℓ_i < e_i (FGMN's OWN print normalizes
  the representative, fgmn.txt:1073). **Level-1 integer form**: with β := E₁α =
  e₀α, (12-int₁): u·e₀ + s·h₀ = β, 0 ≤ s < e₀ — write (s(β), u(β)) for this
  pair (= eq12(β, e₀, h₀) in the harness). Level-1 Bézout pair = the
  hypothesis pair (ℓ₀, ℓ₀′): ℓ₀h₀ + ℓ₀′e₀ = 1, 0 ≤ ℓ₀ < e₀ — now GENUINELY
  NONTRIVIAL (ℓ₀ = h₀⁻¹ mod e₀; ℓ₀ = 0 iff e₀ = 1). **Level-2 integer form**
  (unchanged from TR3-S1): with γ = E₂α, (12-int₂): u·e₁ + s·h₁ = γ,
  0 ≤ s < e₁; write (s(γ), u(γ)).
* **FGMN Def 3.15 (CORRECTED)**: ǫ_i(α) = (z_i)^{ℓ′_i s(α) − ℓ_i u(α)}. At
  level 1 in integer weights: **ǫ₁(β) = z₁^{ℓ₀′s(β) − ℓ₀u(β)}** — at e₀ ≥ 2
  this is a genuinely nontrivial character of β (the object whose threading is
  this unit's charge). ǫ₀ ≡ 1 (printed convention + corrected formula, as in
  TR3-S1 (A1)). Pin legs: N-1's T0 (0/23,111 symbolic Bézout), P3/P12 (PARI
  kill-shots), inherited; the corrupted extraction never consulted.
* **FGMN Def 3.16** (level-2 read): for f = Σ_i a_iΦ₁^i (the Φ₁-development,
  deg a_i < deg Φ₁; both operators read this SAME development, TR3-S1 S1 last
  pin), R_{2,γ}(f) = Σ_j ǫ₁(β_j)·R^F_{1,β_j}(a_{s_j})(z₁)·y^j with slot
  abscissas s_j = s(γ) + je₁ and slot weights (SLOT-gen below). **Def 3.16 at
  level 1** (the inner read, now at stride e₀): for a = Σ_m b_mΦ₀^m,
  R^F_{1,β}(a) = Σ_{m′≥0} R_{0,δ_{m′}}(b_{s(β)+m′e₀})(z₀)·y^{m′} (ǫ₀ ≡ 1),
  with level-1 slot weights δ_{m′} derived in S3 step (A1-gen). Level-0 read:
  R_{0,δ}(b) = the reduction of b/π^δ (zero whenever w₀(b) > δ, including
  δ < 0).
* **FGMN Def 3.18**: R^F_i(g) = R_{i,α}(g)/y^{j₀}, j₀ = ⌊s(g)/e_i^F⌋, s(g) the
  initial abscissa of the λ_i-component of N_i(g).
* **(SLOT-gen).** Level-2 slot weights in integer form: β_j := E₁α_j = e₀α_j
  with α_j = α − s_jµ₂(φ₂) and µ₂(φ₂) = γ₂/E₂ (w₂(Φ₁) = e₁w₁(Φ₁) + h₁ = γ₂,
  RESCALE at level 2), so

      (SLOT-gen)   β_j = e₀·(γ/E₂ − s_jγ₂/E₂) = (γ − s_j·γ₂)/e₁ ∈ ℤ

  (integrality: γ ≡ s(γ)h₁ and s_jγ₂ ≡ s_jh₁ ≡ s(γ)h₁ (mod e₁), since
  γ₂ ≡ h₁ (mod e₁)). Same display as TR3-S1's (SLOT); e₀ enters only through
  what β_j MEANS at level 1 (an e₀-dilated weight), not through its formula.
* **Valuations.** w₁(a) = min_m( e₀·w₀(b_m) + m·h₀ ) over the Φ₀-development
  (the e₀-weighted level-1 valuation; at e₀ = 1 this was TR3-S1's w₁);
  w₁(Φ₁) = e₀g₀h₀ (lift datum, S0); w₂(f) = min_i( e₁·w₁(a_i) + i·γ₂ ).
* **GMN pins (Defs 2.19–2.21 + (18) + Def 2.5), general e₀.** v₂ = w₁
  verbatim: Def 2.5 gives v₂(P) = e₁^{GMN}·H₁(S₁(P)) with e₁^{GMN} = e₀ and
  H₁(S₁(P)) = min_m(w₀(b_m) + m·h₀/e₀) = w₁(P)/e₀. Ordinates
  u_i := v₂(a_iφ₂^i) = w₁(a_i) + i·w₁(Φ₁). Twist exponent
  **t₁(i) = (s₁(a_i) − ℓ₀·u_i)/e₀** (GMN's t_{r−1} with the NORMALIZED
  representative — the S0 hypothesis; at e₀ = 1 it collapsed to s₁(a_i), and
  the ℓ₀u_i term is exactly what no longer cancels at e₀ ≥ 2 — TR3-S1 S9.7's
  trap, now load-bearing). Coefficients c^G_i = z₁^{t₁(i)}·R₁(a_i)(z₁) for
  (i, u_i) on the λ₂-component of N₂⁻(f), c^G_i = 0 strictly above;
  R_λ(f) = c^G_s + c^G_{s+e₁}y + ⋯ + c^G_{s+de₁}y^d. Inner operator (order-1
  layer at the first read, slope λ₀ = −h₀/e₀ FIXED BY THE TYPE): for a
  nonzero digit a with Φ₀-development (b_m), on-line set = the minimizers of
  e₀w₀(b_m) + mh₀; s₁(a) = the least on-line abscissa; **stride e₀**:
  R₁(a)(y) = Σ_k c^{(1)}_{s₁+ke₀}·y^k with c^{(1)}_m = red(b_m/π^{u_m}),
  u_m = w₀(b_m) on the component, 0 strictly above. GMN's ℓ′ (their opposite
  sign convention) never enters t₁ — only the representative ℓ₀ does.
* **GMN Bézout/normalization note (display duty, verbatim TR3-S1 S1).** The
  print fixes ℓ only up to ℓh − ℓ′e = 1; replacing ℓ₀ by ℓ₀ + qe₀ changes
  t₁(i) by −q·u_i, and along the λ₂-component u_{s+ke₁} = u(f) − kh₁, so
  R′_λ(y) = z^{−q·u(f)}·R_λ(z^{qh₁}y) — the SAME scalar-times-rescaling genre
  as (TR3-S2-form) itself. The on-the-nose identity below is scoped to the
  normalized choice (S7); the ord-level consequences survive any fixed choice
  (S6.2's transport argument).

## S2. The σ-identifications at general e₀

The towers: FGMN F₀ = F, F₁ = F₀[z₀], F₂ = F₁[z₁] (ψ_i^F := min poly of z_i);
project K₀ = F[X]/(φ̄₀), K₁ = K₀[z]/(ψ₀), z̄ := the class of z.

**σ₀ : F₁ → K₀, z₀ ↦ X̄ — UNCHANGED from TR3-S1 S2.** Level 0 is untouched by
e₀: ψ₀^F = R₀(φ₁) = φ̄₀ by Cor 5.6(2) chain-internal at index 0 < r, and (RES0)
(σ₀ ∘ level-0 read = red ∘ π-division, both "divide, reduce, evaluate at the
root class") holds verbatim. Nothing to redo.

**σ₁ : F₂ → K₁ extending σ₀ with z₁ ↦ z̄.** Well-defined iso iff
σ₀[y](ψ₁^F) = ψ₀. Supply, at general e₀:
(i) ψ₁^F = R₁^F(Φ₁) — Cor 5.6(2) chain-internal at index 1 < r = 2 ((H-CHAIN)
consumed, nothing extended);
(ii) **Lemma A-GEN below at a := Φ₁** (its statement quantifies over all
nonzero a ∈ O[x]; only the finite Φ₀-development is used):
σ₀[y](R^F_{1,β}(Φ₁)) = y^{m₀(Φ₁)}·R_{λ₀}(Φ₁)(y) at β = w₁(Φ₁) = e₀g₀h₀, with
m₀(Φ₁) = (s₁(Φ₁) − s(β))/e₀. Def 3.18 divides by y^{j₀}, j₀ = ⌊s₁(Φ₁)/e₀⌋; and
since s₁(Φ₁) = s(β) + m₀e₀ with 0 ≤ s(β) < e₀, ⌊s₁(Φ₁)/e₀⌋ = m₀(Φ₁) EXACTLY,
so on side reads σ₀[y](R₁^F(Φ₁)) = R_{λ₀}(Φ₁).
(iii) **Monicity upgrade of the read datum R_{λ₀}(Φ₁) ∼ ψ₀ to equality, with
the e₀-stride.** Φ₁ is monic of exact degree e₀g₀d₀, so its top Φ₀-digit is
b_{e₀g₀} = 1 with w₀ = 0, ordinate check e₀·0 + e₀g₀·h₀ = e₀g₀h₀ = β — ON-LINE;
hence c^{(1)}_{e₀g₀} = red(1) = 1 and R_{λ₀}(Φ₁) has monic leading coefficient
at stride-index (e₀g₀ − s₁(Φ₁))/e₀. Lawfulness forces
deg R_{λ₀}(Φ₁) = g₀ = deg ψ₀, i.e. (e₀g₀ − s₁(Φ₁))/e₀ = g₀, i.e.
**s₁(Φ₁) = 0** (and then also s(β) = 0 since s(β) ≡ ℓ₀β ≡ ℓ₀e₀g₀h₀ ≡ 0
(mod e₀), so m₀(Φ₁) = 0). A monic associate of the monic ψ₀ equals ψ₀. Then
σ₀(ψ₁^F) = σ₀(R₁^F(Φ₁)) = R_{λ₀}(Φ₁) = ψ₀. ∎

**March-compatibility.** As in TR3-S1 S2: σ₁|_{F₁} = σ₀, the tower squares
commute, and the evaluation square

    (SQ)   σ₁( P(z₁) ) = ( σ₀[y]P )( z̄ )   for every P ∈ F₁[y]

(σ₁ = the quotient of σ₀[y] by (ψ₁^F) ↦ (ψ₀)) — the level-by-level compatible
family {σ₀, σ₁} of TR-3′-GEN, at order 2, general e₀.

## S3. Lemma A-GEN — the level-1 finite algebra at stride e₀

**Lemma A-GEN.** Let a ∈ O[x] be nonzero with Φ₀-development a = Σ_{m≥0} b_mΦ₀^m
(deg b_m < d₀, finitely many), and let β := w₁(a) = min_m(e₀·w₀(b_m) + m·h₀).
Call m ON-LINE iff e₀·w₀(b_m) + m·h₀ = β (equivalently (m, w₀(b_m)) lies on the
λ₀ = −h₀/e₀-component of N₁(a)); let s₁(a) := the least on-line m, and
(s(β), u(β)) the (12-int₁) data. Then:
(a) every on-line m satisfies m ≡ s(β) (mod e₀), so m = s(β) + m′e₀ with
    m′ ≥ 0; in particular m₀ := m₀(a) := (s₁(a) − s(β))/e₀ ∈ ℤ_{≥0};
(b) in K₀[y]:

    (A-GEN)   σ₀[y]( R^F_{1,β}(a) )
              = Σ_{m on-line} red( b_m/π^{w₀(b_m)} )(X̄) · y^{(m−s(β))/e₀}
              = y^{m₀} · R₁(a)(y),

    where R₁(a)(y) = Σ_k c^{(1)}_{s₁+ke₀}·y^k is GMN's order-1 residual
    polynomial (S1 pins), read through σ₀ coefficientwise.

*Proof.*
(A1-gen) **Level-1 slot weights.** Def 3.16 at i = 1 with the stratum data:
slot abscissas s_{m′} = s(β) + m′·e₁^F = s(β) + m′e₀; slot weights
α_{m′} = α − s_{m′}·µ₁(φ₁) with µ₁(φ₁) = µ₀(φ₁) + λ₁ = 0 + h₀/e₀ (the
monic-lift normalization w₀(Φ₀) = 0 and λ₁ = h₀/e(µ₁) = h₀/e₀). Level-0
weights are already integers (Γ₀ = ℤ), and with α = β/e₀:

    δ_{m′} := α_{m′} = β/e₀ − (s(β) + m′e₀)·h₀/e₀ = u(β) − m′·h₀

(using e₀u(β) = β − s(β)h₀ from (12-int₁)). At e₀ = 1 this is TR3-S1's
β_m = β − mh₀ verbatim. ǫ₀ ≡ 1 (S1 pin). So
R^F_{1,β}(a) = Σ_{m′} R_{0,δ_{m′}}(b_{s(β)+m′e₀})(z₀)·y^{m′}.
(A2-gen) **Domain.** β = min gives e₀w₀(b_m) ≥ β − mh₀ for every m; at the read
abscissas m = s(β) + m′e₀ the right side is e₀δ_{m′}, so w₀(b_m) ≥ δ_{m′}, i.e.
each read digit lies in P_{δ_{m′}}(µ₀) — Def 3.16 applied inside its domain.
(A3-gen) **Dichotomy at a read abscissa** m = s(β) + m′e₀: if
e₀w₀(b_m) + mh₀ > β (off-line, including b_m = 0 and δ_{m′} < 0) then
w₀(b_m) > δ_{m′} and every coefficient of b_mπ^{−δ_{m′}} has positive
valuation — the reduction is 0. If on-line, then w₀(b_m) = δ_{m′} and the
coefficient is red(b_m/π^{w₀(b_m)})(X̄) ≠ 0 by (RES0) (some coefficient of b_m
has valuation exactly w₀(b_m), and deg b_m < d₀ = deg φ̄₀).
(A3.5-gen) **The forced progression misses no on-line abscissa (claim (a)).**
m on-line forces mh₀ ≡ β (mod e₀); gcd(e₀, h₀) = 1 gives the unique class
m ≡ ℓ₀β ≡ s(β) (mod e₀) (the (12-int₁) congruence). Since 0 ≤ s(β) < e₀ and
m ≥ 0, m = s(β) + m′e₀ with m′ ≥ 0; s₁(a) is the least such, so m₀ ≥ 0. Thus
FGMN's arithmetic progression of read abscissas contains ALL on-line m, and by
(A3-gen) the read is supported exactly on the on-line set — the first equality
of (A-GEN).
(A4-gen) **GMN side.** GMN's order-1 component is the SAME set of points
(m, w₀(b_m)) touched by the slope −h₀/e₀ line (intercept β/e₀ in their
µ-ordinates); their window is [s₁, s₁′] at stride e₀ with
c^{(1)}_m = red(b_m/π^{w₀(b_m)}) on-line, 0 strictly above; below s₁ and above
s₁′ both sides vanish ((A3-gen) off-line; GMN reads nothing there). Re-index
m = s₁ + ke₀ = s(β) + (m₀ + k)e₀: the FGMN coefficient at y^{m₀+k} equals
GMN's c^{(1)}_{s₁+ke₀} for every k ∈ [0, (s₁′−s₁)/e₀], i.e.
Σ_{m on-line} red(·)y^{(m−s(β))/e₀} = y^{m₀}·R₁(a)(y). ∎

Scope note (as in TR3-S1): no use of deg a < deg Φ₁ — the lemma applies
verbatim to a = Φ₁ (S2(ii)). The degree bound enters ONLY at the
evaluation-nonvanishing step of S5(ii), where the theorem's slots do satisfy it.

## S4. Lemma EXP — the compose kernel (a two-line Bézout identity)

The entire twist bookkeeping of the unit reduces to the following exact identity
of INTEGERS (stronger than the needed identity in the finite group ⟨z₁⟩ ⊆ F₂*).

**Lemma EXP.** Let (ℓ₀, ℓ₀′) satisfy ℓ₀h₀ + ℓ₀′e₀ = 1. Let β ∈ ℤ with
(12-int₁) data (s, u) (u·e₀ + s·h₀ = β), and let s₁ = s + m₀e₀ for some
m₀ ∈ ℤ. Then for EVERY abscissa S ∈ ℤ:

    (EXP)   [ ℓ₀′s − ℓ₀u ] + m₀  =  ( s₁ − ℓ₀(β + S·e₀g₀h₀) )/e₀  +  ℓ₀g₀h₀·S,

both sides being the integer (s₁ − ℓ₀β)/e₀. In words: FGMN's composed per-slot
exponent (corrected-ǫ exponent + the Lemma A-GEN shift m₀) and GMN's composed
per-slot exponent (t₁ at ordinate u_S = β + S·e₀g₀h₀, plus the character
θ·S = ℓ₀g₀h₀·S) are BOTH equal to the S-free quantity (s₁ − ℓ₀β)/e₀.

*Proof.* Multiply by e₀ and substitute e₀u = β − sh₀:
LHS·e₀ = e₀ℓ₀′s − ℓ₀(β − sh₀) + (s₁ − s) = s₁ − ℓ₀β + s(ℓ₀′e₀ + ℓ₀h₀ − 1)
       = s₁ − ℓ₀β.
RHS·e₀ = s₁ − ℓ₀β − ℓ₀S·e₀g₀h₀ + e₀·ℓ₀g₀h₀·S = s₁ − ℓ₀β. ∎

Integrality remark: s₁ ≡ s ≡ ℓ₀β (mod e₀) (A3.5-gen congruence) makes
(s₁ − ℓ₀β)/e₀ ∈ ℤ; GMN's printed integrality of t₁ follows for free
(s₁ − ℓ₀u_S ≡ s₁ − ℓ₀β ≡ 0 (mod e₀)) — the harness's A0-sealed row, now
re-derived. At e₀ = 1 every term with an ℓ₀ dies and (EXP) reads m₀ = s₁ —
TR3-S1's t₁ = s₁(a) collapse, exactly.

## S5. The theorem: the assembled per-slot compose

Fix nonzero f ∈ O[x] with Φ₁-development f = Σ_i a_iΦ₁^i and γ := w₂(f) =
min_i(e₁w₁(a_i) + iγ₂); (s(γ), u(γ)) the (12-int₂) data.

**(i) Outer support is the same set on both sides — verbatim TR3-S1 S5(i).**
GMN's order-2 ordinate is u_i = w₁(a_i) + i·w₁(Φ₁), so

    e₁·u_i + i·h₁ = e₁w₁(a_i) + i·(e₁w₁(Φ₁) + h₁) = e₁w₁(a_i) + i·γ₂ ≥ γ,

with equality iff (i, u_i) lies on the λ₁ = −h₁/e₁-component S of N₂⁻(f)
(GMN's λ₂ in their r-indexing; project read-indexing ν₁ here, as in TR3-S1).
Call these i ON-LINE. Every on-line i satisfies i ≡ s(γ) (mod e₁) (ih₁ ≡ γ and
gcd(e₁, h₁) = 1), so i = s_j for a unique slot j ≥ 0; conversely FGMN's slot j
is on-line iff w₁(a_{s_j}) = β_j (by (SLOT-gen), the same equality). The
initial/final on-line abscissas s := s₀(f), s′ give GMN's component data (s, d),
d = (s′ − s)/e₁, and FGMN's j₀ = (s − s(γ))/e₁ = ⌊s/e₁⌋. The ONLY e₀-dependence
in this step is inside w₁'s formula; the two-line computation is otherwise
untouched from TR3-S1.

**(ii) Per-slot identity.** Slot j, abscissa s_j, digit a := a_{s_j}, slot
weight β_j. Three cases.
* a = 0, or a ≠ 0 with w₁(a) > β_j (off-line): FGMN's coefficient is
  ǫ₁(β_j)·R^F_{1,β_j}(a)(z₁) with every level-1 coefficient zero — for each
  read digit b_m of a (m = s(β_j) + m′e₀), e₀w₀(b_m) + mh₀ ≥ w₁(a) > β_j, so
  (A3-gen)'s dichotomy gives 0 (this covers β_j < 0 as well, where no digit
  can be on-line since w₁(a) ≥ 0; domain: every nonzero digit has
  w₁(a) ≥ β_j from γ = min; a = 0 reads 0). GMN: (s_j, u_{s_j}) strictly
  above N (or a = 0), so c^G_{s_j} := 0; outside the component window GMN
  reads nothing and FGMN's coefficient is 0 by the same dichotomy. Both
  sides 0 — support agrees at every abscissa.
* on-line: w₁(a) = β_j, a ≠ 0. Write (s, u) := (s(β_j), u(β_j)),
  s₁ := s₁(a), m₀ := (s₁ − s)/e₀ ∈ ℤ_{≥0} (Lemma A-GEN(a)), and
  u_{s_j} = β_j + s_j·e₀g₀h₀ (the on-line ordinate). Then

      σ₁( C^F_j ) = σ₁( ǫ₁(β_j) · R^F_{1,β_j}(a)(z₁) )
                  = z̄^{ℓ₀′s − ℓ₀u} · ( σ₀[y] R^F_{1,β_j}(a) )( z̄ )   [(SQ); σ₁z₁ = z̄]
                  = z̄^{ℓ₀′s − ℓ₀u} · z̄^{m₀} · R₁(a)( z̄ )            [Lemma A-GEN(b)]
                  = z̄^{t₁(s_j)} · z̄^{θ·s_j} · R₁(a)( z̄ )            [Lemma EXP at
                                                       β = β_j, S = s_j: both
                                                       exponents = (s₁ − ℓ₀β_j)/e₀]
                  = z̄^{θ·s_j} · c^G_{s_j}.                           [GMN Def 2.20]

  **Nonvanishing (both sides).** R^F_{1,β_j}(a) is a NONZERO polynomial (its
  on-line coefficients are ≠ 0, (A3-gen)) of y-degree ≤
  ⌊(e₀g₀ − 1 − s)/e₀⌋ ≤ g₀ − 1 (the development digit has deg a < deg Φ₁ =
  e₀g₀d₀, so its Φ₀-digits sit at abscissas m ≤ e₀g₀ − 1 — THIS is where the
  degree bound is consumed). Its σ₀-image has degree < g₀ = deg ψ₀ = the
  degree of z̄'s minimal polynomial over K₀, so the evaluation at z̄ is
  nonzero; multiplying by the units z̄^{...} keeps it nonzero, so
  σ₁(C^F_j) ≠ 0 and (reading the chain backwards) c^G_{s_j} ≠ 0 — GMN's
  printed on-line nonvanishing, re-derived; combined with the off-line case,
  support is EXACTLY the on-line set on both sides.

**(iii) Assembly and the two-form.** By (i)+(ii): R^F_{2,γ}(f) = Σ_j C^F_j y^j
has support {j : s_j on-line} ⊆ [j₀, j₀ + d], with
σ₁(C^F_j) = z̄^{θs_j}c^G_{s_j} at EVERY j (both sides 0 off-line). Re-index
j = j₀ + k (s_j = s + ke₁):

    σ₁( R^F_{2,γ}(f) ) = Σ_{k=0}^{d} z̄^{θ(s+ke₁)} · c^G_{s+ke₁} · y^{j₀+k},

and dividing by y^{j₀} (Def 3.18; j₀ = ⌊s/e₁⌋ by (i), and the k = 0 term is
nonzero, so the observed initial slot IS j₀):

    σ₁( R^F₂(f) )(y) = z̄^{θs} · Σ_{k=0}^{d} c^G_{s+ke₁} (z̄^{θe₁}y)^k
                     = z̄^{θ·s₀(f)} · R_λ(f)( z̄^{θe₁} y ).            ∎

**Remark 1 (what the compose bought).** Every ingredient was already priced:
Def 3.16's two-level nesting (S1 pins), Lemma A-GEN (the e₀-stride version of
TR3-S1's Lemma A), and the corrected ǫ. The ONLY new mathematics is Lemma EXP —
and it is two lines. The measured (TWIST-LAW) of strata_probe is exactly the
S = s_j instance of (EXP) read through the compose chain; the derivation
recorded in the probe's header (fixed BEFORE its run, 0/3,640) is the same
algebra, now embedded in a full proof with support, σ-construction, assembly,
and normalization displayed.

**Remark 2 (the units-only phrasing is REFUTED on stratum (ii)).** The
acceptance-era TR-3′-GEN record statement asks for identifications σ_j plus
PER-ARGUMENT UNITS alone. That is impossible whenever z̄^{θe₁} ≠ 1: suppose
σ₁′ : F₂ → K₁ is ANY field identification and u_f ∈ K₁* per-argument units
with σ₁′(R^F₂(f)) = u_f·R_λ(f). A field iso applied coefficientwise cannot
introduce slot-dependence: take f with two consecutive on-line slots whose GMN
coefficients c^G_s, c^G_{s+e₁} are both nonzero (Lemma WIT below constructs such
an f — with the two coefficients EQUAL, both = 1 — at EVERY S0 state; the
harness's builds are instances of it). By (TR3-S2-form) — whose right side is
σ₁-independent data — the coefficient RATIOS across the two slots differ
between σ₁(R^F₂(f)) and R_λ(f) by z̄^{θe₁} ≠ 1, and ratios of corresponding
coefficients are invariant under both per-argument scalars and any relabeling
σ₁′ = τ∘σ₁ (τ a field automorphism of K₁ moves both ratio entries together:
τ(x)/τ(y) = τ(x/y), and x/y ∈ K₁ would need τ(x/y) = x/y·z̄^{... } — the
mismatch z̄^{θe₁} is a FIXED nonidentity factor between the two ratio values,
which no single τ can produce for all such f simultaneously, e.g. f ranging
over realizations with x/y = 1, where τ(1) = 1 ≠ z̄^{−θe₁}). Concretely: any
f whose two consecutive on-line GMN coefficients are EQUAL has FGMN
coefficients in ratio z̄^{θe₁} ≠ 1 — no unit and no relabeling equates them.
Empirical face: strata_probe's A6 meter — the naive identity C^F = c^G failed
at 1,023/1,023 twist-visible slots. The correct general statement is
(TR3-S2-form): per-argument scalar TIMES the fixed variable rescaling — i.e.
the leaf's S3.4 REV-11 clause (ii) ("the discrepancy is the corrected-ǫ twist
law threaded per slot"), which this unit PROVES; the record statement's
"per-argument units" clause must be read as AMENDED by the displayed rescaling
(the V26-genre operation, exactly the same genre TR3-S1's REV-2 law displayed
for an unnormalized ℓ).

**Lemma WIT (two-slot witness realization; displayed at REVISION 1 — pass-1
gap 3).** Fix the S0 state (any e₀ ≥ 1). For EVERY pair x⁰, x¹ ∈ K₁* there is
an f ∈ O[x] whose λ₂-component consists of EXACTLY the two consecutive on-line
slots at abscissas 0 and e₁, with GMN coefficients c^G_0 = x⁰ and
c^G_{e₁} = x¹. In particular (x⁰ = x¹ = 1) the equal-nonzero-coefficient
witness consumed above exists at every state: the per-slot GMN coefficient
ranges over ALL of K₁*, freely and independently across the two slots.

*Proof.* Set W := (e₀g₀ − 1)h₀, β⁰ := γ₂ + W (slot 0), β¹ := W (slot e₁), and
slot ordinates u₀ := β⁰, u₁ := β¹ + e₁e₀g₀h₀.
(W1) **u(β) is large enough.** For any β ≥ (e₀g₀ − 1)h₀, (12-int₁) gives
u(β) = (β − s(β)h₀)/e₀ ≥ (β − (e₀−1)h₀)/e₀ ≥ (e₀g₀ − e₀)h₀/e₀ = (g₀−1)h₀ ≥ 0.
Both β¹ = W and β⁰ = γ₂ + W > W qualify.
(W2) **Digit realization (the A-GEN dichotomy read backwards).** Given such a
β and any target x ∈ K₁*: write x = Σ_{k=0}^{g₀−1} x_k z̄^k in the K₀-basis
{1, z̄, …, z̄^{g₀−1}} of K₁ (deg ψ₀ = g₀), and set

    a := Σ_{k : x_k ≠ 0} π^{u(β)−kh₀} · B_k · Φ₀^{s(β)+ke₀},

with B_k a degree < d₀ lift of x_k ∈ K₀* of unit content (w₀(B_k) = 0). The
exponents u(β) − kh₀ ≥ u(β) − (g₀−1)h₀ ≥ 0 by (W1); the abscissas satisfy
s(β) + ke₀ ≤ (e₀−1) + (g₀−1)e₀ = e₀g₀ − 1, so deg a < e₀g₀d₀ = deg Φ₁ and the
displayed sum IS the Φ₀-development of a. Every included digit has level-1
value e₀(u(β) − kh₀) + (s(β) + ke₀)h₀ = e₀u(β) + s(β)h₀ = β, so ALL nonzero
digits of a are on-line and w₁(a) = β; the on-line set is
{s(β) + ke₀ : x_k ≠ 0}, hence s₁(a) = s(β) + k₋e₀ with k₋ := min{k : x_k ≠ 0},
and (S1 pins) R₁(a)(z̄) = Σ_{k ≥ k₋} x_k z̄^{k−k₋} = z̄^{−k₋}·x. So at a slot of
ordinate u carrying this digit on-line,

    c^G = z̄^{(s₁(a) − ℓ₀u)/e₀} · R₁(a)(z̄) = z̄^{t(β,u)} · x,
    t(β,u) := (s(β) − ℓ₀u)/e₀,

the k₋-dependence cancelling exactly — the twist factor depends only on
(β, u), so x ↦ c^G is a BIJECTION of K₁* onto K₁* for fixed slot data (the
freeness claim, note-internal).
(W3) **Integrality of t at the two slots.** s(β⁰) − ℓ₀u₀ ≡ ℓ₀β⁰ − ℓ₀β⁰ = 0
(mod e₀), and s(β¹) − ℓ₀u₁ ≡ ℓ₀β¹ − ℓ₀(β¹ + e₁e₀g₀h₀) ≡ 0 (mod e₀); both
t(β⁰, u₀), t(β¹, u₁) ∈ ℤ.
(W4) **Assembly.** Let a₀ realize (β⁰, z̄^{−t(β⁰,u₀)}x⁰) and a₁ realize
(β¹, z̄^{−t(β¹,u₁)}x¹) via (W2), and set f := a₀ + a₁Φ₁^{e₁} — this is its
Φ₁-development (both digits of degree < deg Φ₁). Ordinates:
u_0 = w₁(a₀) + 0 = γ₂ + W and u_{e₁} = w₁(a₁) + e₁·w₁(Φ₁) = W + e₁e₀g₀h₀ = u₁;
values e₁u_0 + 0·h₁ = e₁(γ₂ + W) and e₁u_{e₁} + e₁h₁ = e₁(W + e₁e₀g₀h₀ + h₁)
= e₁(W + γ₂) — EQUAL, and there are no other digits, so both abscissas are
on-line and the λ₂-component is exactly the segment joining them:
γ = w₂(f) = e₁(γ₂ + W), s(γ) = 0, s₀(f) = 0, d = 1 — two consecutive on-line
slots at 0 and e₁. By (W2), c^G_0 = z̄^{t(β⁰,u₀)}·z̄^{−t(β⁰,u₀)}x⁰ = x⁰ and
c^G_{e₁} = x¹. ∎

(Falsifier face: TS2f, S8.3, constructs exactly this witness at x⁰ = x¹ = 1
on every tested tower with c ≠ 1 and verifies the two-slot property and the
refutation inequality — preregistered 0 construction failures.)

## S6. Consumers: what is now a theorem, and for whom

### S6.1 TR-3′-GEN(ii) at order 2 — conjecture → THEOREM; TR-3′-GEN closes at order 2 as a two-stratum theorem

The GRB leaf's S3.4 REV-11 clause (ii) conjectured, on the e₀ ≥ 2 stratum at
the leaf's standing order-2 perimeter: "the conjectured discrepancy is the
corrected weight-driven unit ǫ_i(α) = z_i^{ℓ′s−ℓu} threaded per slot, UNTESTED
against GMN." Theorem TR3-S2 proves exactly this, with the threading executed
and its closed form displayed: per slot the discrepancy is
z̄^{θs_j} (θ = ℓ₀g₀h₀), assembling to (TR3-S2-form). Inputs consumed:
(H-CHAIN), the lawful-read residual data for Φ₁ (S2(iii)), the GD23 standing
GMN rows, RESCALE, the normalized-ℓ hypothesis (S0) — the same (GR-B-STR)
ladder inputs TR3-S1 consumed, NOTHING NEW. Combined with accepted TR3-S1
(stratum (i), e₀ = 1, discrepancy ≡ 1 — the θ = 0 instance of the same
display):

> **TR-3′-GEN at ORDER 2 is now a TWO-STRATUM THEOREM** (attempt grade for the
> (ii) half pending hostile passes): for every order-2 state, under {σ₀, σ₁}
> and the normalized-ℓ convention, σ₁(R^F₂(f)) = z̄^{θ·s₀(f)}·R_λ(f)(z̄^{θe₁}y)
> with θ = ℓ₀g₀h₀ — an IDENTITY at e₀ = 1, the slot-keyed character at e₀ ≥ 2.
> GRB open lemma #1 closes AT ORDER 2 in this corrected form (the units-only
> phrasing being refuted on (ii), S5 Remark 2). At deeper orders (≥ 3) the
> lemma stays OPEN in full (S7) — the REV-12/REV-13 narrowings (ǫ-revival on
> unramified-below strata at depth ≥ 3) are untouched by this unit.

Through TR-4 (leaf S4), the (2′) final leg at these states holds in the exact
form σ₁(R^{gr}(in f)) = z̄^{θs₀(f)}·R_λ(f)(z̄^{θe₁}y). **Consumers
GD3-FULL/OL-B: served at EVERY order-2 state — in the twisted form.** Any
consumer that reads only ψ-adic orders of residuals is fully served (the
rescaling and scalar preserve them, S6.2); a consumer needing the ∼-form
("R^{gr}(in f) ∼ R_λ(f)" as ASSOCIATES) is served on-the-nose exactly at the
states with c := z̄^{θe₁} = 1 (all of e₀ = 1, plus the e₀ ≥ 2 states where the
character value is trivial) and must thread the character at the states with
c ≠ 1 — the exact form above is what exists.
At deeper orders they continue to sit on OPEN TR-3′-GEN.

### S6.2 TR-3-ORD-X on stratum (ii) at order 2 — proved with inherited conditionality

TR-3-ORD-X (leaf R2.3): ord_{ψ₁}(R_λ(f)) = ord_L(R^{FGMN}(f)) for every
nonzero f, labels tied by clause (3) on each side. On the stratum, at order 2:

0. **WHICH state key — the key-convention sensitivity (FOUND BY THE RUN-1
   FALSIFIER, S8.2; a display this unit owes its consumers).** On stratum (ii)
   "lawful monic state key for the label ψ₁" is CONVENTION-SENSITIVE: the
   GMN-LAWFUL key satisfies R_λ(Φ₂) ∼ ψ₁ (the GD23 read datum under the
   project's standing GMN rows), while the FGMN-NORMALIZED key (the project
   harness's canonical twist-corrected lift — N-1's T5 object) satisfies
   σ₁(R₂^F(Φ₂^h)) = ψ₁ exactly. By (TR3-S2-form) these transform into each
   other through the character:

   **Lemma KEY0 (the FGMN-normalized key under the two-form; displayed at
   REVISION 1 — pass-1 gap 2).** Let Φ₂^h ∈ O[x] be monic with
   deg Φ₂^h = e₁g₁·deg Φ₁ and σ₁(R₂^F(Φ₂^h)) = ψ₁ (the FGMN-normalized key
   data). Then s₀(Φ₂^h) = 0, the λ₂-component window is exactly [0, e₁g₁],
   and R_λ(Φ₂^h) = T_{c⁻¹}ψ₁ (coefficients ψ₁_k·z̄^{−θe₁k}).
   *Proof* (mirroring step 1's monicity argument, with the side read
   supplying the degree). (1) deg Φ₂^h = e₁g₁·deg Φ₁ bounds the
   Φ₁-development abscissas by e₁g₁, so the final on-line abscissa satisfies
   s′ ≤ e₁g₁. (2) By S5(ii)+(iii), the side read R₂^F(Φ₂^h) is a polynomial
   of degree EXACTLY d = (s′ − s₀)/e₁ (both end slots are on-line, hence
   their coefficients are nonzero); σ₁ is a coefficientwise iso, so
   d = deg ψ₁ = g₁. (3) Hence s₀ = s′ − e₁g₁ ≤ 0; development abscissas are
   ≥ 0, so s₀(Φ₂^h) = 0 and s′ = e₁g₁. (4) (TR3-S2-form) at f = Φ₂^h with
   s₀(Φ₂^h) = 0 reads ψ₁(y) = z̄^{θ·0}·R_λ(Φ₂^h)(cy), i.e.
   R_λ(Φ₂^h)(y) = ψ₁(c⁻¹y) = T_{c⁻¹}ψ₁. ∎ (This is the step whose "By the
   two-form" shorthand pass 1 flagged: the silent input was s₀(Φ₂^h) = 0,
   now derived.)

   T_{c⁻¹}ψ₁ is ∼ ψ₁ ONLY when the character is invisible on ψ₁'s support.
   Call the state TWIST-VISIBLE (for the label ψ₁) iff some k < g₁ has
   ψ₁_k ≠ 0 and z̄^{θe₁(g₁−k)} ≠ 1 — equivalently, iff the monicized
   T_{c⁻¹}ψ₁ (coefficients ψ₁_k·z̄^{θe₁(g₁−k)}) differs from ψ₁. The two key
   classes COINCIDE at e₀ = 1 (c = 1, TR3-S1's setting — where the
   distinction was invisible); at e₀ ≥ 2 they CAN differ, and they diverge
   EXACTLY on the twist-visible states — coinciding whenever the character
   is invisible on ψ₁'s support, which does occur at e₀ ≥ 2 (measured: 20 of
   the 52 tower instances are twist-visible = exactly the divergent set;
   the other 32, among them 16 of the 36 e₀ ≥ 2 instances, COINCIDE —
   S8.2). [REVISION 1: the pre-revision text claimed divergence at ALL
   e₀ ≥ 2 states — the pass-1 CRITICAL; rescoped to the criterion the
   falsifier implements.] The INVARIANT form, for ANY shared monic key Φ₂
   with the S6.2 data:

       (ORD-INV)   ord_{R_λ(Φ₂)}( R_λ(f) ) = ord_{ψ₂^F}( R₂^F(f) ),

   proved by step 2's transport verbatim (match the label to the SAME key on
   both sides). TR-3-ORD-X's displayed form (ψ₁ on the GMN side) is (ORD-INV)
   for a GMN-LAWFUL key; steps 1–3 execute that case. Pairing MISMATCHED
   labels (ψ₁ on both sides with an FGMN-normalized key) CAN FAIL at
   e₀ ≥ 2 — it fails exactly where the key classes diverge (the
   twist-visible states; at twist-invisible states the two pairings agree)
   — measured at 214/3,536 samples, all on the 20 twist-visible towers, in
   the run-1 falsifier before the pairing was corrected (S8.2's amendment
   record; the run-1 RED is exactly this divergence, not a transport
   failure).
1. **Label tie — now with an EXPLICIT nontrivial scalar.** ψ₂^F := R₂^F(Φ₂) is
   the minimal polynomial of z₂ by Cor 5.6(2) read in the EXTENDED chain
   (Φ₀, Φ₁, Φ₂) — consuming **(KP_i)**, exactly as TR3-S1 S6.2. The Theorem at
   f = Φ₂, plus the Φ₂-monicity computation (the TR3-S1 S6.2 argument at
   stride e₀): a GMN-LAWFUL monic state key (step 0) has deg Φ₂ = e₁g₁·deg Φ₁ and
   w₂(Φ₂) = e₁g₁γ₂, top Φ₁-digit a_{e₁g₁} = 1 with w₁ = 0, ordinate
   u = e₁g₁·w₁(Φ₁) satisfying e₁u + e₁g₁h₁ = e₁g₁γ₂ — ON-LINE; lawfulness
   (R_λ(Φ₂) ∼ ψ₁, deg ψ₁ = g₁) forces s₀(Φ₂) = 0. But at e₀ ≥ 2 the leading
   coefficient NEED NO LONGER be 1 — it is the explicit character value
   (trivial exactly when z̄^{θe₁g₁} = 1): the top digit's inner data are
   R₁(1) = 1, s₁(1) = 0,
   so c^G_{e₁g₁} = z̄^{t₁(e₁g₁)} = z̄^{−ℓ₀·e₁g₁·g₀h₀} = z̄^{−θe₁g₁} (Lemma EXP
   integrality; the ∼-upgrade of TR3-S1 becomes a SCALAR pin):

       R_λ(Φ₂) = z̄^{−θe₁g₁} · ψ₁,

   and with s₀(Φ₂) = 0, j₀ = 0, (TR3-S2-form) gives the tied label

       σ₁( ψ₂^F )(y) = z̄^{−θe₁g₁} · ψ₁( z̄^{θe₁} y )  =: z̄^{−θe₁g₁}·(T_cψ₁)(y),

   where c := z̄^{θe₁} and T_c : K₁[y] → K₁[y] is the K₁-algebra automorphism
   y ↦ cy.
2. **Transport.** σ₁ extends to a ring iso F₂[y] → K₁[y] (coefficientwise),
   T_c is a K₁[y]-automorphism carrying (ψ₁) to (T_cψ₁) (T_cψ₁ irreducible),
   and nonzero scalars change no ψ-adic order. Chain, for every nonzero f:

       ord_{ψ₁}( R_λ(f) )
         = ord_{T_cψ₁}( T_cR_λ(f) )                      [T_c automorphism]
         = ord_{z̄^{−θe₁g₁}T_cψ₁}( z̄^{θs₀(f)}T_cR_λ(f) ) [scalars]
         = ord_{σ₁ψ₂^F}( σ₁R^F₂(f) )                     [step 1 + (TR3-S2-form)]
         = ord_{ψ₂^F}( R^F₂(f) ).                        [σ₁ iso]

   This is R2.3's "a rescaling is a rescaling" argument, executed: label and
   argument rescale by the SAME T_c, so ord survives — and this is also why
   the S6.1 ord-consumers are fully served despite the twist.
3. **FGMN-internal leg.** ord_{ψ₂^F}(R^F₂(f)) = ord_L(R(f)) is Cor 5.15 with
   the **[T]-pinned properness** of the state key — NOT discharged here,
   exactly as in TR3-S1 S6.2 and proved Lemma TR-3-ORD.

**Hence TR-3-ORD-X holds at order-2, e₀ ≥ 2 states — with the GMN-LAWFUL
state key (step 0's convention display) — conditional on
(KP_i) + [T]-properness; and combined with TR3-S1 S6.2, at ALL order-2
states.** Route B's ord chain closes PROJECT-FACING at order 2 on both
strata. **Consumers GD3-MIN Route B and ALL clause-(4) ord-consumers: served
at every order-2 state, with (KP_i)+[T] inherited AND the step-0 key
convention displayed (any consumer whose state key is the FGMN-normalized
lift must use (ORD-INV)'s matched labels — ψ₁ then sits on the FGMN side;
the naive both-sides-ψ₁ pairing fails exactly at the twist-visible states);
at deeper orders they continue to sit on OPEN TR-3-ORD-X.**

### S6.3 B-M2 OL-1(b), GMN-verbatim face

TR3-S1 S6.3 discharged the TR-3-ORD-X seam of B-M2's OL-1(b) at order-2
e₀ = 1 states. With S6.2 the same seam is discharged at ALL order-2 states
(with (KP_i)+[T] inherited). OL-1(b) remains conditional on its OTHER
displayed seams (GRADED-READ; REC-SL where applicable) everywhere, and on
TR-3-ORD-X beyond order 2. No other B-M2 clause is touched.

## S7. Scope box — what this unit does NOT prove

* **Deeper orders (≥ 3).** Lemma A-GEN and the S5 assembly are depth-2-specific
  (the inner operator is the LEVEL-1 read). At order ≥ 3 the inner reads are
  marched operators carrying their own ǫ-systems and the ǫ-recurrence can
  REVIVE even on e₀ = 1 strata (the leaf's REV-12 narrowing) — the recursion
  is NOT written here. TR-3′-GEN and TR-3-ORD-X at deeper orders stay OPEN;
  this unit converts exactly their order-2 restrictions (all e₀).
* **The general σ_j family** of TR-3′-GEN (all orders) stays OPEN; only
  {σ₀, σ₁} is constructed.
* **Normalization scope.** The on-the-nose identity is w.r.t. the project's
  standing NORMALIZED convention 0 ≤ ℓ₀ < e₀ — a displayed hypothesis (S0),
  external to GMN's print. Under ℓ₀ ↦ ℓ₀ + qe₀ the S1 transformation law
  applies (a further scalar × rescaling); the S6.2 ord conclusions survive
  any fixed choice; the displayed θ = ℓ₀g₀h₀ uses the normalized ℓ₀.
* **The units-only record phrasing** of TR-3′-GEN does NOT extend to (ii)
  (S5 Remark 2 + Lemma WIT — REFUTED at every state with c ≠ 1, not merely
  unproved; at the states with c = 1 the two-form itself is already
  units-only); the closure is in the corrected S3.4 REV-11 form,
  per-argument scalar × fixed rescaling.
* **The key-convention sensitivity (S6.2 step 0)** is a display, not a
  defect: consumers must name their key class at e₀ ≥ 2 (the two classes
  diverge exactly at the twist-visible states and coincide at the
  twist-invisible ones). Which class the
  GD23 carriers record project-wide is a LEAF-FOLD question (the GRB leaf's
  clause-(3) sites), not settled here; both directions of the tie are proved
  and tested.
* **D-ITER / the composite digit algebra — UNTOUCHED (the charter's hard
  fence).** Nothing here consumes or constrains the digit-algebra iteration
  law (ITER-LAW), the composite anchors, or the cocycle tables; the character
  law proved here lives at the residual-operator level. The synthesis-pass-5
  ruling that these are DISTINCT mechanisms is respected: no step above
  needed any D-ITER material (checked at compose time — the only shared
  object is the corrected ǫ itself, which both consume from N-1).
* **(KP_i) and [T]-properness** — consumed, not discharged (S6.2, exactly as
  TR3-S1).
* **GMN's printed base scope** is finite extensions of ℚ_p; the
  GMN-convention operator is consumed as the project's engine convention
  over an arbitrary complete DVR (GD23 standing identification); the
  equal-characteristic towers are covered by the falsifiers against exactly
  that reading.
* **Evaluation-nonvanishing degree scope**: the support clause uses
  deg a < deg Φ₁ (development digits only); Lemma A-GEN itself is
  degree-free.

## S8. Falsifier record (2026-08-08)

### S8.1 The recorded strata_probe FAMILY A (the closed form, prior to this unit)

`verification/openmath/strata_probe.py` (seed 20260815; recorded output
strata_probe_output.txt / strata_probe_results.json; e₀ ≥ 2 rows C/D/E/H/J +
designed rows K/L/M/N with CERT certification 0/344 incl. PARI, e₀ = 1
controls, 4 rings): A0 0/20,828 (GMN-side integrity, sealed); A1 0/7,255
(support); A2 0/4,297 (Lemma-A-general inner shift); **A3 0/3,640 (the
TWIST-LAW closed form — the object this unit proves)**; A5 0/1,252 (e₀ = 1
controls); A6 naive-identity refuted 1,023/1,023 at twist-visible slots
(consumed at S5 Remark 2); A7 retired-S3.2-shape refuted 293/293. The
FAMILY B rows of that run are D-ITER material and are NOT consumed here.

### S8.2 The NEW preregistered TS2 family (this unit's falsifier) — with its amendment record

`verification/openmath/tr3s2_check.py` (seed 20260816, fresh; independent of
N-1's 20260808, tr3s1's 20260812, strata_probe's 20260815; same tower roster
as S8.1; sample shapes: rand, structured mixtures, Φ₁^a·g corners, π^k,
π^k·struct, Φ₂^µ·Q, f = Φ₂, f = Φ₂²). Independence perimeter: stated in the
script header, the TR3-S1 REV-2 discipline (fresh GMN implementation; shares
the Tower object + w₀/w₁-as-v₁/v₂; external cover = N-1's T11/P11/P12 PARI +
T0 + strata_probe CERT; the normalized ℓ₀ hard-coded, so the family cannot
falsify the S0 hypothesis; σ₁ = identity of the shared concrete model).

**RUN 1 (v1 predictions; log tr3s2_check_run1_output.txt).** The THEOREM rows
came back green on first contact: TS2a 0/3,536, TS2b 0/3,536, TS2e 0/1,088.
TS2c 20/52 RED and TS2d 214/3,536 RED — traced (all 20 towers, exact lists):
v1 had preregistered the label tie for the WRONG KEY CLASS: the harness's
build_Phi2 is the FGMN-NORMALIZED key (side read = ψ₁ exactly — N-1's T5
row), whose GMN residual is T_{c⁻¹}ψ₁, not the GMN-lawful key's
z̄^{−θe₁g₁}ψ₁; v1's TS2d then paired ψ₁ with ψ₁ across the seam — the
MISMATCHED pairing that the note's own transport says must fail exactly on
the twist-visible towers (and did: all 214 mismatches sit on the 20
twist-visible instances). Classification: preregistration error rooted in a
REAL finding (the key conventions CAN diverge on stratum (ii) — exactly at
the twist-visible states, S6.2 step 0); the theorem rows were never red.
Both label forms and the divergence itself were then preregistered as v2
BEFORE run 2 (script AMENDMENT block).

**RUN 2 (v2 predictions; log tr3s2_check_output.txt): ALL GREEN.**

    TS2a  per-slot compose chain (support + INTEGER kernel
          (EXP) + A-GEN factorization + closed form)       0/3,536   GREEN
    TS2b  assembled two-form on side reads + j₀ law        0/3,536   GREEN
    TS2c  label ties at BOTH key conventions + meter       0/52      GREEN
    TS2d  MATCHED-label ord transport, both pairings       0/3,536   GREEN
    TS2e  e₀ = 1 controls (TR3-S1 identity, fresh code)    0/1,088   GREEN
    key-divergence meter: obs 20 == pred 20 of 52 towers   MATCH
    ord histogram: 0:3,192  1:182  2:162 (ord ≥ 1 present) MATCH
    TOTAL 11,748 samples, 0 violations — ALL GREEN.

A TS2a violation falsifies S3/S4/S5(ii) (the kernel row is the note's PROVED
integer identity — a red there is a refutation of the unit's core or a
harness bug); TS2b falsifies S5(iii); TS2c falsifies the S6.2 label ties
(either convention) or the step-0 divergence law; TS2d falsifies the S6.2
transport; TS2e falsifies compatibility with accepted TR3-S1.

### S8.3 RUN 3 (REVISION 1: TS2f added, TS2c meter relabeled; preregistered before the run) — ALL GREEN

TS2f (the Lemma WIT falsifier, pass-1 gap 3): on every tested tower with
c := z̄^{θe₁} ≠ 1, CONSTRUCT Lemma WIT's witness f = a₀ + a₁Φ₁^{e₁} at
x⁰ = x¹ = 1 (W = (e₀g₀ − 1)h₀; digits via the (W2) realization with the
z̄^{−t(β,u)} pre-twist) and verify (f-i) the two-slot property — component
exactly {0, e₁} (s₀ = 0, d = 1), c^G_0 = c^G_{e₁} = 1 equal-nonzero at the
TARGET value; (f-ii) side read length 2 with j₀ = 0; (f-iii) the refutation
inequality — the FGMN consecutive-coefficient ratio differs from the GMN
ratio, by exactly the factor c. Preregistered: 0 violations, 0 construction
failures.

    TS2a  0/3,536   TS2b  0/3,536   TS2c  0/52   TS2d  0/3,536
    TS2e  0/1,088   TS2f  0/20 (NEW)                       — all GREEN
    twist-visibility meter (relabeled): obs 20 == pred 20 of 52   MATCH
    TS2f witness meter: constructed on 20 towers with c ≠ 1
      (c = 1 skipped: 32 = 16 e₀ = 1 controls + 16 e₀ ≥ 2 instances);
      construction failures 0 (pred 0); twist-visible covered 20 == 20  MATCH
    ord histogram unchanged: 0:3,192  1:182  2:162             MATCH
    TOTAL 11,768 samples, 0 violations — ALL GREEN
    (log tr3s2_check_run3_output.txt; run-2 family rows byte-reproduced)

On this roster c ≠ 1 coincides with twist-visibility (20 = 20); the meter
keeps them as separate predicates (twist-visible ⟹ c ≠ 1 is checked as an
implication, not assumed). A TS2f red would falsify Lemma WIT (construction
or freeness) or the S5 Remark 2 refutation inequality.

## S9. Honesty ledger and attack surface for the hostile pass

1. **Lemma EXP is the whole twist argument** — attack it first: re-derive
   (EXP) independently (two lines from ℓ₀h₀ + ℓ₀′e₀ = 1 and
   e₀u = β − sh₀); then check its CONSUMPTION sites: ǫ₁'s exponent uses the
   (12-int₁) data of β_j (not of u_{s_j}), t₁ uses the ordinate
   u_{s_j} = β_j + s_j·e₀g₀h₀ (GMN's (18) note: the ordinate includes the
   key value — TR3-S1 S9.7's trap, load-bearing here).
2. **The level-1 slot-weight derivation** δ_{m′} = u(β) − m′h₀ (A1-gen):
   µ₁(φ₁) = h₀/e₀ and the Γ₀ = ℤ read; an off-by-one or a wrong dilation
   shifts every inner read. Cross-checked by TS2a(k2) 0/3,536.
3. **(A3.5-gen)** — the claim that FGMN's forced progression s(β) + m′e₀
   contains ALL on-line abscissas (gcd + normalized-representative
   argument); if a lift's on-line set could escape the progression the
   support clause breaks. Also verify m′ ranges far enough in Def 3.16's
   print (the sum is over all j ≥ 0).
4. **The monicity upgrades**: at Φ₁ (S2(iii) — s₁(Φ₁) = 0 forced by lawful
   degree; equality with ψ₀); at Φ₂ (S6.2 — s₀(Φ₂) = 0 plus the EXPLICIT
   scalar z̄^{−θe₁g₁} via R₁(1) = 1, t₁(e₁g₁) = −θe₁g₁). A non-monic or
   partial-degree lift degrades = to ∼ (ord conclusions survive).
5. **The key-convention sensitivity (S6.2 step 0)** is a finding this unit
   itself introduced — attack Lemma KEY0 (the s₀(Φ₂^h) = 0 derivation
   displayed at REVISION 1: degree bound → s′ ≤ e₁g₁; side read → d = g₁;
   the squeeze s₀ = s′ − e₁g₁), and verify (ORD-INV) is convention-free;
   check no consumer of TR-3-ORD-X was silently assuming the naive
   both-sides-ψ₁ pairing at e₀ ≥ 2 (at e₀ = 1 it is correct; at e₀ ≥ 2 it
   is correct exactly at the twist-INVISIBLE states and fails at the
   twist-visible ones).
6. **The evaluation-nonvanishing degree bound** (S5(ii)): deg ψ₁^F = g₀
   under the dictionary (ψ₁^F ↦ ψ₀); the y-degree bound
   ⌊(e₀g₀ − 1 − s)/e₀⌋ ≤ g₀ − 1; both needed for support exactness.
7. **S5 Remark 2's refutation argument** (no σ-relabeling + units absorb the
   rescaling): at REVISION 1 the witness-existence leg was tightened to the
   displayed Lemma WIT (pass-1 gap 3), so the "whenever c ≠ 1" scope is now
   unconditional — attack the lemma (the (W2) freeness display: the
   k₋-cancellation in c^G = z̄^{t(β,u)}·x; the (W4) two-digit polygon); the
   A6 empirical face (1,023/1,023) and TS2f's constructed witnesses are
   independent of that argument.
8. **Transcription risk**: the GMN t₁-formula and (18) ordinate were freshly
   consumed here at e₀ ≥ 2 (TR3-S1 only consumed their e₀ = 1 degenerations).
   Non-textual cover: gmn.txt's t-display checked clean at N-1 (single
   unprimed ℓ); the A0-sealed integrality re-derived in S4; the PARI legs
   cover the shared tower semantics, not the t-formula itself — this is the
   unit's largest residual extraction exposure, flagged per the
   extraction-corruption protocol.
9. Everything consumed from the GRB leaf is at its REV-14 state; from
   TR3-S1 at its ACCEPTED (REV-2, two-clean) state; nothing consumed from
   the superseded S3.2 law or the retired artifact ǫ.

— TR3-S2 composed 2026-08-08; falsifiers: strata_probe FAMILY A recorded
green (closed form), TS2 v2 ALL GREEN (11,748 samples; run-1 amendment on
record, theorem rows green in both runs); hostile passes NOT yet run — the
2-clean acceptance bar starts under the orchestrator.

## REVISION 1 (2026-08-02) — response to hostile pass 1; acceptance counter RESET to 0/2

Pass 1 (lean/notes/openmath/TR3S2_pass1_report.md): VERDICT CRITICAL — 1
critical + 2 gaps. Verified in full by the same pass (untouched here): the
S2–S5 compose chain including both declared attack points A (EXP/ordinate
bookkeeping) and B (level-1 slot weight), the reindexing to (TR3-S2-form),
ORD-INV's statement and support, and every count reconciliation. The three
findings and their REVISION 1 fixes:

1. **CRITICAL — false universal: "the two key classes … DIVERGE at e₀ ≥ 2"**
   (quote). The unit's own falsifier measures 20/52; 16 of the 36 e₀ ≥ 2
   instances COINCIDE. Rescoped at every site to the criterion the (c-iii)
   falsifier implements: the key classes diverge IFF the state is
   TWIST-VISIBLE (some k < g₁ with ψ₁_k ≠ 0 and z̄^{θe₁(g₁−k)} ≠ 1 — the
   character visible on ψ₁'s support); "bifurcates" claims now read "CAN
   differ on (ii), diverging EXACTLY on the twist-visible set". Sites
   edited: S0 verdict line + two summary-table rows; S6.2 step 0 (the main
   display, with the pre-revision claim quoted in place); the S6.2
   mismatched-pairing sentence (fails exactly on the twist-visible towers —
   all 214 run-1 mismatches sat on the 20); the S6.2 consumer note; S7
   bullets (key-convention, units-only); S8.2 run-1 narrative; S9 items 5
   and 7; the falsifier's TS2c meter relabeled to match. Dependent claims
   checked: the ORD-INV transport and all S6 consumer services never
   consumed universal divergence (matched labels are convention-free); two
   adjacent same-genre universals rescoped — S6.1's ∼-form consumer clause
   (on-the-nose exactly where c = 1, not "only at e₀ = 1") and S6.2 step
   1's "leading coefficient is NOT 1" (→ need no longer be 1; it is the
   explicit character value).
2. **GAP — S6.2 step 0 silently used s₀(Φ₂^h) = 0** in "by (TR3-S2-form)
   … R_λ(Φ₂^h) = T_{c⁻¹}ψ₁". Now displayed and PROVED as **Lemma KEY0**
   (S6.2 step 0): the fixed degree e₁g₁·deg Φ₁ bounds s′ ≤ e₁g₁; the
   degree-g₁ side read (= ψ₁ through the coefficientwise iso σ₁, ends
   nonzero by S5(ii)+(iii)) forces d = g₁; the squeeze
   s₀ = s′ − e₁g₁ ≤ 0 ≤ s₀ gives s₀ = 0; then the two-form yields the
   displayed T_{c⁻¹}ψ₁ — exactly the argument genre of step 1, executed.
3. **GAP — the units-only refutation assumed witness realization** ("the
   harness builds them at will"). Fixed by route (a): **Lemma WIT** (S5)
   displays the general construction — the per-slot GMN coefficient is
   z̄^{t(β,u)}·x with the read value x ranging FREELY over K₁* (A-GEN read
   backwards; the s₁-shift k₋ cancels exactly), so a two-slot f with
   c^G_0 = c^G_{e₁} = 1 exists at EVERY S0 state; the refutation's
   "whenever c ≠ 1" scope is now unconditional, no downgrade needed.

Falsifier: TS2f added (preregistered BEFORE run 3; script REVISION 1
ADDENDUM block): per tower with c ≠ 1, construct Lemma WIT's witness at
x⁰ = x¹ = 1 and verify the two-slot property + the refutation inequality
(mismatch factor exactly c). RUN 3 (tr3s2_check_run3_output.txt): ALL GREEN
— TS2f 0/20, 0 construction failures, all 20 twist-visible towers covered;
every prior row byte-reproduced (TS2a/b/d 0/3,536 each, TS2c 0/52, TS2e
0/1,088); TOTAL 11,768 samples, 0 violations.

The 2-clean acceptance bar restarts at 0/2 under the orchestrator.
