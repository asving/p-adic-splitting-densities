# ITERLAWN-PC5 — Codex round-5 content pass (note @362d118): GAPS-ONLY 0C/1G

The gap: the 23-row closed census is under-enumerated even for the pinned text — two additional in-scope hits (II-3 'WEIGHT clauses only'; S1 'INSTANCE EVIDENCE ONLY, never proof steps'), both content-TRUE. PC5 explicitly confirms: row 23 correct, arithmetic correct, the regress-proofing successfully prevents forward leaks, S0 genuinely unconsumed, and NO additional mathematical defect anywhere (architecture/integer layer/EXP-KIT/T-notation/branches/corollaries/S8/rule-8 all coherent). Adjudication: the hand-census approach is the failure mode — errata round 6 installs PE6's MACHINE census (every grep hit programmatically mapped), complete by construction, subsuming PC5's two sites.

## Codex output (verbatim)

I’ll audit the amended file itself: first pin the commit chain and additive diff, then independently enumerate every grep-pattern hit and re-check row 23, the closed-dated census wording, and the standing mathematical surface. I’ll report only quote-and-classify findings; no edits.
1. **GAP — the closed census is already incomplete for pinned 0672082.** The restatement claims: “the 23 sites enumerated by the stated grep patterns over the note text AS OF commit 0672082.” But additional matching dependency claims are not separately enumerated, including S4.6’s “E₁ᴵᴴĈ = R¹+Q¹Φ (**WEIGHT clauses only**)” and S1’s “Sealed artifacts … **INSTANCE EVIDENCE ONLY, never proof steps**.” Their content is true, but they are sites matching the declared `only / never` sweep and do not appear among rows 1–23. Thus row 23 itself is correct, and 22 + 1 = 23 is arithmetically correct, but the asserted full-text census remains false. Dating the assertion successfully prevents future brackets from expanding its scope; it does not cure under-enumeration already present in the pinned text.

2. **NOTE — hygiene execution unavailable.** Repository commands could not start because the environment lacks `bubblewrap`; therefore I could not independently verify chain `0672082 → b71f170 → e4a8e8a → 362d118` or insertion-only/core-untouched hygiene. This is not counted as a finding. Content re-verification of the supplied complete text found no additional mathematical defect: S₀ is genuinely unconsumed, and the architecture, integer layer, EXP-KIT, T-notation, both S4.6/S7 branches, corollaries, S8 claims, and rule-8 apparatus remain coherent.

ITERLAWN-PC5 FINDINGS: 0 critical, 1 gaps
VERDICT: GAPS-ONLY
