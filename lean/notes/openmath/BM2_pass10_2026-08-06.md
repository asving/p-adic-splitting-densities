## Hostile pass 10 findings

### CRITICAL 1 — a live synthetic-only sentence survived the headline sync

The current §S2 theorem header still says:

> “case (b)/[RC] … its compiled conclusions are at the synthetic pair (1, 1), not at (νᵢ.e, νᵢ.g)”

This is neither historical nor fenced. It directly contradicts §S0, the immediately following case-(b) body, and compiled `k0_actual_of_recentering`. It is exactly the pass-9 inverse genre the REVISION-11 sweep claimed to eliminate.

The opening headline itself is correctly synced, but the requested grep-minded whole-note result is therefore not clean.

Classification: **CRITICAL**.

### CRITICAL 2 — the live “tied NOWHERE” correction is stale again

The §S2 evidence paragraph still says:

> “the corpus never TIES νᵢ.e/νᵢ.g at recentering legs”

and:

> “the g-half is tied NOWHERE … nowhere at the STAGE/WIRING level”

That conflicts with REVISION 11’s new determination that the coherence width law, `Node.childWidth`, and the two `hDwidth` links do carry enough g-content at the history/wiring layer.

The same stale assertion survives in `RecParamE.lean`:

> “the trace’s ‘tied NOWHERE’ verdict stands AT THE STAGE/WIRING LEVEL”

The narrower statement “no `Stage` field directly records g, and the e-half’s `child_e` route has no direct g analogue” is sound. “Tied nowhere at the wiring level” is not.

This breaks the claimed S0/body/three-file consistency.

Classification: **CRITICAL**.

### GAP 1 — REC-WIRE-G’s mathematics is well-typed, but its routing is not encoded by the displayed proposition

The quoted types are internally consistent:

- `Node.hDwidth` has the used orientation `Dwidth = σ.Φ.natDegree`.
- `Node.childWidth = e * g * Dwidth`.
- The `HistoryCoherent` width component used in `coherent_H₃rc` is exactly child width = parent `e*g*Dwidth`.
- `IsRecentering σ σ' cc tt` supplies `σ'.Φ = σ.Φ - tt` and `inC σ.Φ tt`.
- Hence the proposed stage kernel
  `IsRecentering σ σ' cc tt → σ'.Φ.natDegree = σ.Φ.natDegree`
  is well-posed and true by lower-degree subtraction.
- Combining that kernel with the width equation, both `hDwidth` links, and `Stage.hdeg` yields `e*g*d=d` with `d≥1`, hence `e*g=1`.

So the candidate route is not false, and the stage-layer kernel genuinely cannot invoke `Node.hspecRec` because no `Node` occurs in its type.

However, the displayed top-level proposition

> species-recentering node ⟹ `ν.e * ν.g = 1`

does not itself enforce that route. It has an immediate fiat proof from `ν.hspecRec`, followed by arithmetic. “Derived from transition data” and “must factor through” remain prose constraints on the proof, not propositions Lean can check.

Moreover, “transition data” is imprecise: the proposed route uses `IsRecentering` plus `HistoryCoherent`’s width clause, not a `TransitionData` g-field.

Thus REVISION 11 made the intended route falsifiable only by displaying the separate stage kernel; it did not make the advertised REC-WIRE-G theorem itself provenance-sensitive.

Classification: **GAP**.

### Revision-11 fold audit against the embedded pass-9 findings

The five requested fold actions are present:

- Critical 3: opening headline synced.
- Critical 4: both RG-2 e≥2-recentering sentences replaced.
- Gap 5: the live evidence paragraph fenced and reassigned to residual wiring obligations.
- Gap 6: REC-WIRE-G receives a concrete proposition and candidate route, though the routing defect above remains.
- Critical 7: C135–C184 are published.

Pass-9 findings 1–2 are also separately recorded as positive validations.

One record-level misdescription remains:

> “all five defects are STALE TEXT UNDER-CLAIMING”

That is not accurate for pass-9 GAP 6 (under-specification of REC-WIRE-G) or Critical 7 (missing census audit trail). The individual finding descriptions are substantially faithful; the profile summary is not.

Classification: **GAP**.

### Uninhabitability replacement

The narrow assertion is sound:

> `Node.hspecRec` … forces e = 1 ∧ g = 1 at every species-recentering node

Therefore an e≥2 species-recentering `Node` is unconstructible.

The replacement generally preserves the layer boundary: it repeatedly says “at the node level” and does not claim that raw stage-layer `IsRecentering` data are impossible. The sentence “e≥2 lives only at NON-RECENTERING reads” is defensible because reads are represented by `Node`s, but it must not be read as a theorem about all stage transitions or all engine firings; SITE-EXH remains open and is still displayed.

Classification: **CLEAN**, with the stated layer-sensitive reading.

### Census audit

The published arithmetic is exact:

- C001–C134: 134 rows.
- Superseded: 2 at REV 9 + 35 at REV 10 + 2 at REV 11 = 39.
- Active old rows: 134 − 39 = 95.
- New rows C135–C184 inclusive: 50.
- Total: 95 + 50 = **145**.

The new-row class arithmetic also checks: 46 tagged + 4 status-free = 50.

Spot audit found no missing ID or arithmetic discrepancy. C175 and C184 are debatable as `[STATUS-FREE]`, but both are corpus/routing statements rather than unconditioned fired-lift status claims, so I do not classify them as automatic census failures. The census nevertheless failed its larger purpose to expose the active stale assertion in the old C030 sentence; `[TAGGED]` does not imply semantically current.

### S0/body/Lean consistency

The substantive compiled upgrade is otherwise consistent across the four faces:

- `K0Conformant` argument order is `(σ, g, e')`, with degree `e' * g * deg`.
- `k0_actual_of_recentering` rewrites `e` and `g` correctly.
- Both actual-pair witness gates instantiate it correctly.
- `recParam_e_of_transition` retains exactly its displayed interior/previous-non-recentering scope.
- The two dedicated [RC] corollaries are witnessed; the `ReadsOf` recentering branch remains unwitnessed.
- S0’s OL-1(a) and DevBound actual-degree upgrades follow.

But the §S2 synthetic-only theorem header and the note/`RecParamE.lean` “tied NOWHERE” wording prevent four-way consistency overall.

Clean counter remains **0/2**.

VERDICT: CRITICAL