Both REVISION-14 repairs pass.

- **S3.1 — CORRECT.** The acceptance reading  
  `z^{ℓ s − ℓ′ u}` places the prime on the **second** ℓ, multiplying `u`.  
  The true reading  
  `z^{ℓ′ s − ℓ u}` places it on the **first** ℓ, multiplying `s`.  
  The repaired narrative—“the leaf re-attached it to the SECOND ℓ (the u-multiplier)”—therefore agrees with both displays. The geometry sentence, “′ at column 51 directly above the first ℓ at column 50,” agrees with the true reading.

- **N-1 headline — CORRECT.** “pdftotext ORPHANED the glyph … attaching it to NEITHER ℓ, and the leaf then re-attached it to the wrong (second) ℓ” precisely separates extraction behavior from the leaf’s mistaken reconstruction.

Other attachment/provenance occurrences in the supplied leaf:

- “floating-prime artifact … orphaned onto its own line … re-attached by the leaf to the wrong ℓ” — **CORRECT**, direction-neutral but consistent.
- R11.1’s “orphaned pdftotext glyph … re-attached by the leaf to the wrong ℓ” — **CORRECT**.
- S5.3/R11.4’s “recurrence-EXISTENCE cite is attachment-independent” — **CORRECT**; it claims only existence of the recurrence, not either exponent placement.
- REVISION 14’s quotation of the former “first ℓ” narrative — **HISTORICAL/INVALIDATED**, explicitly identified as the pass-19 defect and followed by the correct second-ℓ account.
- All remaining “artifact,” “garbled-prime,” and fresh-extraction provenance statements — **CORRECT/CONSISTENT**; none assigns the orphaned glyph to the wrong side.

Fresh sweep: no operative old exponent, γ₂ artifact law, collapse-refutation, broad order-independent triviality, stale D-ITER scope, or carried/re-earned misclassification escaped its supersession brackets. No new critical or gap-class finding. The unavailable `bwrap` prevented an on-disk `rg`, but the complete supplied REVISION-14 text was censused directly.

Clean counter: **1/2**.

VERDICT: CLEAN