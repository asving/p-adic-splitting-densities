# TOWER-RAT BATCH #2 pre-verdict scaffold (written BEFORE reading any Codex output)

Orchestrator's own pre-verdict key for the four FROZEN accepted tower notes
(GENTOW5/3/4/6) + the batch-#1 discharge-confirmation run, per the RAT
decorrelation discipline (cf. `towerrat_scaffold_2026-08-10.md`, batch #1).
Committed with the briefs before the runs return.

## RUN A — GENTOW5 (GENTOW5_PROOF_2026-08-09.md, accepted 2/2 at PE5)

1. **The level-r grammar (DEF GENTOW5-1).** The normalizer recursion
   n̂_{i+1}(k), the floor chain u_{i+1} > e_{i+1}E_i, the corrected
   per-flavor lift display M_{r,t}(m) (r2 F-2 repair). Expect HOLD — the
   r2 repair re-solved the base per flavor and the machine row (FA1-LIFT)
   pins the non-existence of the naive flavor. Risk: whether ϑ_{i,s}
   (the telescope unit) is genuinely FIXED (point-independent) at i ≥ 2 —
   the note routes it through ι^{(i)} as a letter monomial in
   η, β_1, ..., β_{i-1}; a hostile read may grade the S2.3 step-(3)
   computation as asserted-not-displayed at general i. Expect at most GAP
   on display grounds; the level-2 instance is machine-anchored.
2. **The ψ^{(w)} re-scope (THEOREM GENTOW5-B(a), r2 F-1).** The realized
   residual is the w-twisted ψ_{i+1}^{(w_i)}; w_i = 1 open; consumers
   w-blind (walked after (e)). Expect HOLD at i = 2 (verbatim GENTOW2 r3
   B-law); risk at i > 2: the claim that the B-law's θ-leg "cancels
   against the S2.1 ϑ twist" level-generally is an extrapolation of a
   level-2 proved law — a hostile reader may grade the level-general leg
   GAP. My read: the note prices this honestly (named pin, w_i open), so
   annex not challenge — unless Codex exhibits a concrete exponent
   mismatch in the (b) residual-power display ψ^{l_j...l_{i+1}} under
   per-level twists (my own soft spot: the twist of a POWER vs the power
   of a twist — check (Z/w)^l bookkeeping).
3. **The (c) transport clause (r3 F-1 re-index).** Entry read anchored at
   i+1 (slope κ_{i+1}, residual ψ_{i+1}^μ), refuted-then-verified at the
   committed P-B3 rows. Expect CLEAN — this was machine-adjudicated both
   ways. Residual risk: the index-0/PE5 scope fold's claim that no clause
   consumes the unclaimed T_1 instance (grep-audited; I expect it holds).
4. **Depth-4 witness + battery.** Expect CLEAN (exact ladder, dual routes).

Predicted outcome: RATIFIED-WITH-ANNEX (annex on the level-general
ϑ/w-twist displays); CHALLENGE only if the (b) power-twist bookkeeping
genuinely mismatches.

## RUN B — GENTOW3 (GENTOW3_PROOF_2026-08-09.md, accepted 2/2 at PE5)

1. **The Θ_j threshold (S0/S2).** δ := u₂ − e₂D′h ≥ 1, Θ_j = (μ₂−j)E₂ + δ.
   Faithful below Θ_j at every consulted height; first failure no earlier
   than Θ_j. Expect HOLD — the discrepancy pricing (Φ′(x₀)H_j at
   dv₂ ≥ u₂ + dv₂(H_j-class)) is the consumed T(b)′(iv) mechanism. Risk:
   the lower-bound leg at non-x-free genres with entry-digit-driven
   overflows (scope remark (2)) — does the S2 ledger price THOSE below
   Θ_j too, or only the key power's own shadow? The r3 one-siding
   narrowed the ATTAINMENT claim; the BELOW-Θ_j faithfulness claim still
   quantifies over members — my main predicted probe surface.
2. **The certificate scoping story (r2 → r3).** After r3 the composed
   claim is one-sided (certificate ⟹ failure AT Θ_j; where the digit
   cancels, only the key power's own divergence is priced strictly
   above). Expect CLEAN — the false only-if was dropped and the W member
   is measured. Risk: residual only-if language surviving somewhere
   un-re-scoped (S3/S4 wording).
3. **The gauge annex (S7.1).** chat_t := lift(c_t·η^{W(t)}) pinned; the
   x-free "equivalently" clause re-scoped to f₁ = 1. Expect CLEAN
   (pattern-matched to the GENTOW1/GENTOW4 annexes, FR-GL machine row).
4. **x-free genres faithful at EVERY height.** A per-note universal —
   expect Codex to probe its proof leg (x-free lift coefficients commute
   with the mod-Φ′ projection); my read: sound at f₁ = 1 post-annex.

Predicted outcome: RATIFIED or RATIFIED-WITH-ANNEX; CHALLENGE only if
the below-threshold faithfulness leg fails at an overflow genre BELOW
Θ_j (would need a concrete instance; the battery is 582/0 there).

## RUN C — GENTOW4 (GENTOW4_PROOF_2026-08-09.md, accepted 2/2 at PE3)

1. **The bouquet + block projection (S2/S3).** Label-class split
   (HE7-13′ mechanism at the tower entry) + LEMMA HE6R1-2's projection
   consumed at exactly its proved proper-block configuration. Expect
   HOLD on the split; risk on the TRANSLATION constant c_g = dv₂(g(ξ))
   — constancy across ξ ∈ roots(f_S) is a claim about the complementary
   factor's value; if the note derives it from label-class separation
   (other classes sit at different dv(Φ′)), expect CLEAN; if asserted,
   GAP candidate. Second risk: per-block HE7.A instances inherit the
   batch-#1 HETOW F-1 seam (n̂₂/n₂ routing transport) — if Codex
   re-finds it here, adjudication = discharged corpus-side by LEMMA
   HETOW-4 (2026-08-10 append), annotation not challenge.
2. **The four-site gauge annex (S8).** Corrected chat_t at S0,
   4.1(c)/S3 re-pointed at restated HETOW-1/2 with supply audit,
   BOX-3's η-gauge axis, S4 remark re-scoped. Expect CLEAN (the PE2/PE3
   passes walked it).
3. **COR GENTOW-4.C (the c_g-read).** Consumes the tie 4.2 (pure-power
   only; mixed boxed → closed later by GENTOW6.1). Expect probe on
   whether 4.C's statement leaks past the pure-power scope; the note
   boxes mixed sides honestly (S7 BOX-1) — annex at worst.

Predicted outcome: RATIFIED-WITH-ANNEX; CHALLENGE if c_g constancy is
genuinely underived at mixed-class configurations.

## RUN D — GENTOW6 (GENTOW6_PROOF_2026-08-09.md, accepted 2/2 at PE3)

1. **The mixed-side tie (LEMMA GENTOW-6.1).** Steps I (block pure-power
   residual via HE6-3(b)/HE6-4 at f_C), II (FGMN residual
   multiplicativity — the cite), III (UFD match k_r = m_r). Expect the
   heaviest probe at Step II's cite fidelity (internal-only; the FGMN
   print is quoted at GENTOW2's verified numbering) and at Step III's
   completeness leg (Σ over classes exhausts the side residual — needs
   the bouquet split's totality, GENTOW-4.A(i)). My read: HOLD; a GAP
   grade could land on the unit-normalization ("residual EXACTLY r^{m_r},
   monic; unit-free") — coherent-normalization bookkeeping between f and
   its block factors is where I'd hunt.
2. **CERT-TOP + the f₁ = 1 scope pin (S3/THEOREM 6.3).** Post-r1 the
   theorem head is pinned f₁ = 1 (f₁ ≥ 2 = ANNEX-6.3′, machine-unexercised
   = BOX-4). Expect CLEAN as scoped; risk: a stale "every genre" sentence
   the PE2 rider missed.
3. **THE CAP LEMMA (GENTOW-6.4) + its 4.C conditionality.** The 𝒯-free
   cap replacing GENTOW-5(a)'s 𝒯-scoped consumption (PE1 F-2 repair);
   its (a) codomain was corrected at the PE2 rider (above-top slots
   forced 0, exact condition n mod D₂ ≠ D₂ − 1). Expect probes on the
   corrected codomain's bijection count and on what exactly the 4.C
   conditionality clause conditions on — if the conditionality is
   disclosed (COR 4.C consumed at its stated scope), annex; if a 6.4
   consumer fires 4.C outside pure-power scope, GAP.
4. **The three boxes (BOX-1 μ₂ ≥ 3, BOX-2 ledger half, BOX-4 f₁ ≥ 2).**
   Honest-residue checks; expect CLEAN.

Predicted outcome: RATIFIED-WITH-ANNEX; CHALLENGE only if Step III's
completeness or the 6.4 codomain count carries a concrete mismatch.

## RUN E — discharge confirmation (batch #1 repairs)

Findings under confirmation: HETOW F-1/F-2/F-3 (→ LEMMA HETOW-4 +
two annexes) and GENTOW1 F-1/F-2/F-3/F-4 (→ R2a/R2b/R2c + F-4 reading).

- **HETOW F-1 → HETOW-4:** expect DISCHARGED — (a) computes the ratio
  exactly (grade-determined wrap-letter monomial), (b) proves the
  per-height NON-cancellation the challenge demanded honesty about,
  (c) telescopes into the J-D0-covered species, (d) transports both
  routing predicates; machine leg 203/0 with the T-RAW honesty tooth.
  INSUFFICIENT risk: (d)'s α-refine leg ("ONE w realizes both
  prescriptions") — if the confirmer reads the two prescriptions as
  independent constraints rather than δ-translates, it may grade the
  single-w claim under-derived. My read: (a) at k = λ₂ is exactly the
  translation, so discharged.
- **HETOW F-2 → cite annex:** DISCHARGED (mechanical cite attach).
- **HETOW F-3 → row annotation:** DISCHARGED (frame-data scoping).
- **GENTOW1 F-1 → R2a clip:** DISCHARGED (max(0,·) + counter-instance
  reproduced in-scope + consumer re-scope of (e)).
- **GENTOW1 F-2 → R2b:** expect DISCHARGED — both sets defined, counting
  lemma with the SAME clipped exponent, enumeration row 16/16.
  INSUFFICIENT risk: the inverse-limit leg (window bijections →
  full-string bijection) — compatibility across N is used silently;
  height-preserving triangularity supplies it, but the confirmer may
  want it displayed.
- **GENTOW1 F-3 → R2c three-band:** DISCHARGED (band 2 carries the old
  iff's counter-instance as a machine row).
- **GENTOW1 F-4 → reading of record:** DISCHARGED.

Predicted: 7/7 DISCHARGED, with INSUFFICIENT most likely (if anywhere)
at HETOW-4(d)'s refine leg or R2b's inverse-limit leg.
