# QSCOUT22 — THE QUARTIC (2,2) EMPIRICAL TABLE (composite-stage scout): the two order-2 genres at the quadruple center (E: e-first, F: f-first), the four-slot model as the preregistered count law, the half-lattice (E=2) and the squared alphabet (f₁=2) as the observed mechanism, and the exact line where W-12's tame law stops

**Unit:** QSCOUT22, the first composite-stage examples unit (n = 4), the
empirical ground truth the GENIND composer tests against. INSTRUMENT
grade: measured tables + preregistered slot-model laws + EXPLORATORY
labels where post-hoc. **Charge:** monic quartics over O/π^N realizing
composite refinement histories — a degree-2 key acquiring a degree-2
residual/refinement structure — both characteristics, p = 2 as the
doubly wild crux. **Instrument:** two-commit sealed battery
`verification/openmath/qscout22_checks.py`; verdict §S8 appended at
commit 2 FROM the committed artifacts.

**Reconciliation with W-12 (where the tame law's scope ends).** W-12's
8 quartic stratum rows check the quadruple-center one-side e = 2 shapes
QRT-G2(h; λ): per center, S_λ(q)·q^{4N−5h−3} for λ ∈ {(1,1), (2)} —
including the WILD (2,2) at λ = (2) (σ = {(2,2)}, doubly wild at p = 2)
— by the ORDER-1 digit calculus of W-12 S2.2. That is what "the wild
(2,2) counted by the tame law" means: the COUNT is an order-1 shape law
(census × lattice q-power) because the residual is separable; no
composite stage opens; wildness of σ is invisible to the count. The
scope ends EXACTLY at the repeated-residual rows: λ = (1²) on the e = 2
side (this note's GENRE E) and λ = (2²) on the e = 1 full side (GENRE
F), where a degree-2 key polynomial φ₂ opens (order 2) and W-12.A
explicitly stops (W12-BOX-1, the [W12-H] hypothesis box). This note is
the empirical map of that territory.

## S1. THE COMPOSITE TAXONOMY (derived pre-run; the statements the table keys on)

Conventions as W-12 S1 (O complete DVR, residue F_q, both
characteristics; window N; recentered quadruple-center coordinates
f = x⁴ + B₃x³ + B₂x² + B₁x + B₀, all v(B_j) ≥ 1; counts per center ×
q centers). Level-1 polygon = lower hull of {(j, v(B_j))}_{j<4} ∪
{(4,0)}.

**LEMMA Q22-A (composite entry classification; proof: the S2.2-style
case check, machine-enforced both directions).** An order-2 (composite)
stage opens at the quadruple center iff the polygon is ONE side and
either:
* **GENRE E (e-first, e₁ = 2, f₁ = 1):** slope h/2, h odd (v(B₀) = 2h),
  residual y² + r₂y + r₀ = (y − z)², z ≠ 0 — the QRT-G2(h,(1²)) locus,
  entry volume (q−1)·q^{4N−5h−3} per center (PROVED, W-12 S2.5); key
  φ₂ = x² − ẑπ^h; value group index [Γ₁ : Γ₀] = 2.
* **GENRE F (f-first, e₁ = 1, f₁ = 2):** integer slope k (v(B₀) = 4k),
  residual quartic = ψ(y)², ψ irreducible quadratic — entry volume
  ((q²−q)/2)·q^{4N−10k−4} per center (the W-12.A order-1 law pricing
  the (2²) locus; (q²−q)/2 = #ψ); key φ₂ = x² + ĉ₁π^k x + ĉ₀π^{2k}
  (ψ = y² + c₁y + c₀); residue field extension F_{q²}/F_q.
Every other repeated-residual case recenters into an n ≤ 3 subsystem
(order 1) except λ = (1⁴), which recenters to a DEEPER quadruple center
(prehistory step: depth k, letter z, locus (q−1)·q^{4N−10k−4} per
center) from which E/F entry recurses with parameter floors h ≥ 2k+1
resp. k′ ≥ k+1.

**LEMMA Q22-B (the four-slot coordinates, genre E; proved by the
displayed bijections, machine-checked as the predictor).** On the
E(h, z) entry locus, φ₂-adic division f = φ₂² + A₁φ₂ + A₀
(A₁ = α₁x + α₀, A₀ = β₁x + β₀) has free-coordinate budgets
v(α₁) ≥ (h+1)/2, v(α₀) ≥ h+1, v(β₁) ≥ (3h+1)/2, v(β₀) ≥ 2h+1 (the maps
B₃ ↦ α₁, B₂ ↦ α₀ = B₂+2w, B₁ ↦ β₁ = B₁+B₃w, B₀ ↦ β₀ = B₀+B₂w+w²,
w = ẑπ^h, are digit-budget bijections; total 4N−5h−3 ✓ = the entry
volume exponent). In DOUBLED units dv := 2v₁, the two slot strings of
A₁ interleave to the FULL lattice {2h+1, 2h+2, ...} (odd heights from
α₁, even from α₀ — h odd forbids ties: no cancellation, the JC
no-cancellation warrant holds automatically here), and A₀'s to
{4h+1, 4h+2, ...}. The level-2 polygon is the hull of {(0, v₁(A₀)),
(1, v₁(A₁)), (2, 0)}; all slopes > h automatically.

**Level-2 leaf menu (genre E; derivation in S4):** one side, dv(A₀)
odd → e₂ = 2, σ = {(4,1)} (E-RAM); vertex at 1 → {(2,1),(2,1)}
(E-2SIDED); one side, dv(A₀) = 2dμ → residual T² + S₁T + S₀ over F_q
(S₁ = A₁'s digit at dμ, S₀ = A₀'s digit at 2dμ; for dμ odd the S₀ slot
is z̄^{−1}-twisted — the twisted-algebra letter, type-invariant):
'11' → {(2,1),(2,1)} (E-SPLITEQ), '2' → {(2,2)} (E-INERT: the
COMPOSITE-route wild (2,2), e-then-f), '1sq' → REFINE φ₂ (depth dμ,
letter s, q−1 letters), recurse. Tails: A₀ window-zero with
dv(A₁) < N certifies {(2,1),(2,1)} (E-SPLTAIL); else UNDECIDED
(conservative convention, disclosed).

**Genre F leaf menu:** Γ₁ = ℤ, alphabet F_{q²} (each integer height
carries a q²-slot: digit pair (α₁, α₀) read as ᾱ₁y + ᾱ₀ ∈ F_{q²};
ψ irreducible ⟹ no cancellation); leaves: dv(A₀) odd → e₂ = 2,
σ = {(2,2)} (F-RAM: the f-then-e wild (2,2)); vertex → {(1,2),(1,2)};
integer dμ → residual over F_{q²}: '11' → {(1,2),(1,2)}, '2' → {(1,4)},
'1sq' → REFINE (q²−1 letters); tail/undecided as in E.

**THE PREREGISTERED PREDICTOR (the (2,2)-BRACKET model).** Counts per
(center, entry letter) are the slot-model evaluations: each leaf =
(census factor over the level-1 residue field: F_q for E, F_{q²} for F)
× q^{#free slots above the pins}, with refinement at depth dμ
transferring measure-isomorphically to the fresh node with floors
(dv(A₁) > dμ, dv(A₀) > 2dμ) and one (q−1)-letter factor (resp. q²−1
for F) — the exact analogue of W-11 S2.3 one level up, with prehistory
chains contributing (q−1)^r and parameter floors per LEMMA Q22-A. The
depth-0 laws and entry volumes are DERIVED (above); the refinement
transfer and the tail split are CONJECTURED at seal time and graded by
the battery. Structural predictions:
* **P-A (shape-only / J-D0 at order 2):** every keyed count is
  letter-uniform — independent of z, of ψ, of refinement letters, and
  of the characteristic (mixed vs eq-char rows identical tables).
* **P-B (the α-bracket analogue):** aggregated over histories, decided
  leaf counts obey the bracket with the depth lattice DOUBLED in genre
  E: available refinement depths are ALL integers dμ (density 2 per
  v₁-unit = E), vs W-11's integer k's — the twisted-algebra dimension
  E = e₁ = [Γ₁:Γ₀] appears as the DENSITY of the refinement ladder,
  not as a count multiplier.
* **P-C (the f-side):** genre F's censuses and letters live over
  F_{q^{f₁}} = F_{q²} (alphabet squares, lattice stays ℤ): the
  composite-stage count law is the W-12.A form with the level-1
  residual field replacing the level-0 one.
* **P-D (char-2 forced cascade):** at q even, an integer-dμ side with
  dead S₁ slot (S₁ = 0 forced) has T² + S₀ ≡ (T + √S₀)²: '1sq' with
  probability 1 — refinement is FORCED where odd characteristic splits
  '11'/'2' evenly; the (1²)-cascade is the doubly wild signature. (At
  the entry level this is not reachable — S₁'s slot always exists —
  but it IS reachable inside refined nodes at q even iff the killed
  digit heights align; the battery measures where.)

[Tables §S3–S5, verdict §S8 at commit 2. Boxes §S7.]

## S2. THE INSTRUMENT (design; roster + families + teeth preregistered here)

Runner `verification/openmath/qscout22_checks.py`. Exact arithmetic;
frozen imports w10_checks (rings/fields) + w12_checks (shift4, hull,
s_lam) by md5 pin; PARI/GP subprocess for the oracle legs.

**Rosters.**
* FULL sweeps (every quadruple-center member, q^{4N−3} each; the reader
  tabulates the COMPLETE key partition incl. order-1 rows):
  Zp (2,4),(2,5),(2,6),(3,4),(5,3); Fqt (2,5),(3,4),(4,3).
* CONSTRUCTIVE composite strata (four-slot / pinned-B enumeration,
  cross-checked against the full sweep on every overlapping row):
  genre E: Zp (2,7,h=1),(2,7,h=3),(3,5,h=1),(5,4,h=1); Fqt (2,7,h=1),
  (4,4,h=1). genre F: Zp (2,7,k=1),(2,8,k=1),(3,6,k=1),(5,5,k=1);
  Fqt (2,7,k=1),(3,5,k=1). Prehistory: Zp (2,7): (1⁴)@k=1 chains.
* ORACLE (PARI factorpadic, Zp only): every decided composite member at
  (2,5),(2,6),(3,4),(5,3) full rows + deterministic 1-in-8 sample of
  the (2,7)/(2,8) constructive rows; σ multiset must equal the reader's.

**Families:** Q22-PIN [NONE] md5 pins; Q22-SUM [SAME] full-row
partition Σ = q^{4N−3}; Q22-W12TIE [SAME] G2 decided rows == the SEALED
W-12 law + E/F entry volumes == LEMMA Q22-A; Q22-CONS [SAME]
constructive == full-sweep restriction, key by key; Q22-E / Q22-F
[SAME] every composite key == the slot-model predictor, BOTH directions
(unpredicted or missing key = violation); Q22-PRE [SAME] prehistory
keys == (q−1)^r × floored fresh law; Q22-JD0 [SAME] letter-resolved
counts uniform (entry letters; first refinement letter); Q22-XCHAR
[SAME] mixed vs eq-char key tables identical at matching (q,N);
Q22-ORACLE [IND] PARI σ per decided composite member.

**Teeth (each must fire or the battery is RED):** Q22-T-LAT (genre-E
predictor corrupted to the INTEGER-only depth lattice — W-11's naive
transport — must mismatch on every row realizing odd-dv keys);
Q22-T-CEN (E-INERT census q(q−1)/2 corrupted to q(q−1) must break);
Q22-T-ALPH (genre-F census field q² corrupted to q must break);
Q22-T-SIG (F-RAM σ mis-keyed {(2,2)} → {(1,4)} must be caught by PARI).

**Disclosed conventions:** conservative tails (no certification beyond
the displayed SPLTAIL bound; measured UND is an upper count for the
σ-residue); canonical digit-lift of letters in φ₂ (the W-2/LIFTCORNER
lift-convention seam NOT consumed — key histories are convention-keyed
exactly as prior batteries); genre F at q = 4 boxed (F₁₆ arithmetic not
in the frozen kit); order-1 rows tabulated but only partition-checked
(their laws are W-12's, not re-claimed).

**Smoke disclosure (actual pre-seal record).** Developed and debugged
with `--smoke`: full rows Zp (2,4),(2,5),(3,4) + Fqt (2,5); consE
Zp/Fqt (2,6,h=1); consF Zp (2,7,k=1); pre Zp (2,7,k₁=1); oracle full
(2,5) 4,000 scored + (3,4) 12,636 scored, 0 bad. Observed at smoke:
GREEN — 0 violations in all ten families, all four teeth firing
(final smoke 32.4 s). Two development findings folded in BEFORE the
seal and disclosed: (i) lift-stability caps — a dv-value at/above 2N
(genre E) resp. above N (genre F) can be undercut by an out-of-window
digit of a lift, so reader AND model treat those slots as hidden-class
(the first smoke draft misclassified them); (ii) the T-ALPH tooth is
armed only on rows realizing an even-dμ genre-F key (N ≥ 7 at k = 1),
so it is checked on the constructive rows. No full-roster run before
the seal commit.

## S3. THE EMPIRICAL TABLE (appended at commit 2 from artifacts)

Raw tables: `qscout22_results.json` (23 rows, 7,614,473 members read;
every key with its exact count). Counts below are B-space (= per
center); keys are (genre, pre, h/k, H₂, leaf, params) with dv in
doubled units for genre E, integer v₁ for genre F.

**Genre E at (Zp, q=2, N=7, h=1)** — 114 keys, refinement depth up to
t = 4; mass by depth t: 898,816 / 128,512 / 18,432 / 2,560 / 256.
The realized depth-4 history H₂ = (3,4,5,6) is the CONSECUTIVE integer
dv-ladder — in v₁-units the depths are 3/2, 2, 5/2, 3: the
half-integer (E = 2) ladder realized in full, exactly P-B. Spot laws
(all = the model, 0 violations): E-RAM(5) at (2,6) = 32,768 =
(q−1)²q^{4N+h−u−(u+1)/2−2}; E-INERT(5) = 128 identically at H₂ = (),
(3), (4), (3,4) — the (q−1)^t bracket at q = 2.

**The bracket letter factor at q = 3** (full row (3,4), h=1):
RAM(7)@H₂=(3) = 648 = (q−1) × RAM(7)-at-floors [324 × 2] — the W-11
letter bracket one level up, per prediction.

**Genre F at (Zp, q=2, N=8, k=1)** (per ψ; Q = q² = 4): RAM(5) =
196,608 → σ={(2,2)}; INERT(3) = 24,576 → {(1,4)}; SPLITEQ(3) = 12,288
→ {(1,2),(1,2)}; ratio INERT/SPLITEQ = 2 = [Q(Q−1)/2] / [(Q−1)(Q−2)/2]
at Q = 4 — over F_q it would be (1, 0): SPLITEQ ≠ 0 refutes the F_q
reading outright (the alphabet SQUARES, P-C); refined (3,)-keys carry
the (Q−1) = q²−1 letter factor (RAM(7)@(3) = 9,216 = 3 × 3,072).

**Prehistory transfer** (pre rows (2,7)/(2,8), k₁=1): the (1⁴)@1
recentering chains compose with genre-E entry at h = 3 exactly as the
floored fresh law × (q−1)^r — e.g. (2,8): E(pre=(1), h=3) INERT(7) =
2,048, RAM(13) = 8,192, incl. a depth-1 refined key (7,)-RAM(15) =
1,024; 16 partition keys, 0 violations (Q22-PRE).

**Wild strata:** every σ genre is realized and oracle-scored at p = 2:
{(4,1)} (E-RAM), {(2,2)} via BOTH routes (E-INERT: e-then-f; F-RAM:
f-then-e), {(2,1),(2,1)}, {(1,4)}, {(1,2),(1,2)} — 35,380 PARI checks,
0 disagreements.

## S4. DERIVATIONS BEHIND THE PREDICTOR (fixed at seal)

Genre E residual normalizations: for a one-side level-2 polygon of
slope μ = dμ/2 (dμ ∈ ℤ), T := φ₂/n₂: dμ even: n₂ = π^{dμ/2}, R₂(T) =
T² + S₁T + S₀ with S₁ = digit_{dμ/2}(α₀-part of A₁)… wait — S₁ is A₁'s
residual AT dv-height dμ: dμ even ⟹ the α₀ slot: S₁ = digit_{dμ/2}(α₀);
dμ odd ⟹ the α₁ slot: S₁ = digit_{(dμ−h)/2}(α₁); S₀ = A₀'s residual at
2dμ (even ⟹ β₀ slot: digit_{dμ}(β₀)). For dμ odd, n₂ = x·π^{(dμ−h)/2}
and T² normalizes through x² = φ₂ + w: R₂(T) = T² + S₁T + z̄^{−1}S₀ —
the z̄-twist sits on S₀ alone; the census is twist-invariant
((S₁,S₀) ↦ (S₁, z̄S₀) is a bijection of each type class since type is
invariant under T-scaling), so COUNTS are twist-blind while LETTERS are
twisted: J-D0's mechanism, literal at order 2. Refinement: φ₂ ← φ₂ −
ŝ·n₂; the double root forces S₁ = −2s so the killed digit dies
(A₁′ = A₁ + 2ŝn₂ + division corrections at strictly greater dv), giving
the floor transfer (dv(A₁) > dμ, dv(A₀) > 2dμ) — the conjectured
measure-isomorphism the battery grades. Genre F: no twist (normalizers
π^m exist at every integer m); residuals are the digit PAIRS
ᾱ₁y + ᾱ₀ ∈ F_{q²}; refinement letter S = (a, b): η = âπ^{μ−k}x + b̂π^μ.
Prehistory transfer: the (1⁴)@k shift map is a per-letter bijection
onto the floor set {v(B_j) ≥ (4−j)k + 1} (budget check
Πq^{N−1−(4−j)k} = q^{4N−10k−4} ✓), so chains contribute (q−1)^r with
parameter floors and NO other change — fresh-law reuse.

## S5. THEORY-CANDIDATE TESTS (verdicts at commit 2)

(a) J-D0 count-gauge-blindness at the composite stage: Q22-JD0 +
Q22-XCHAR. (b) The α-bracket analogue (q−1)q^{f(m,n)−1}: the slot-model
bracket, with the E-density doubling (P-B) — Q22-E both directions.
(c) The twisted-algebra dimension E: P-B's reading (density, not
multiplier) vs any count multiplier E — the odd-dv keys separate them.
(d) The tame-law boundary: W-12.A's form survives with (census field,
lattice density) re-based at the level-1 data (P-C) — Q22-F.

## S6. CANDIDATE LEMMA STATEMENTS (for GENIND; grades at commit 2)

* **Q22-L1 (composite count law, quartic (2,2) instance).** At every
  composite key of genre G ∈ {E, F} with entry parameter (h resp. k),
  history H₂, leaf ℓ: count per center = (q−1)^{r+t_E}·(q²−1)^{t_F}·
  C_ℓ(q^{f₁})·q^{X} with X affine in (N, h/k, ΣH₂) — census over
  F_{q^{f₁}}, lattice exponent with density e₁ — i.e. W-12.A's
  cell law with (e₁, f₁)-re-based inputs.
* **Q22-L2 (order-2 refinement transfer).** The level-2 refine locus at
  depth dμ maps measure-isomorphically onto the floored fresh node —
  the W-11 S2.3 transfer one level up (the GENIND induction step).
* **Q22-L3 (drainage of the composite stratum).** The σ-undecided mass
  of each composite entry stratum is EXACTLY the UND-key law (one
  q-power per entry), the n = 4 composite contribution to hExhaust-4.

## S7. HONEST BOXES

* **Q22-BOX-1 (σ-resolution ≠ proof).** The reader's σ at composite
  leaves is oracle-tied (PARI, mixed char, prime q only) — evidence,
  not the [W12-H] discharge; eq-char and prime-power-q rows are
  SAME-class.
* **Q22-BOX-2 (transfer conjecture).** The refinement/prehistory
  measure-isomorphisms are machine-graded conjectures here; the proofs
  are GENIND's (Q22-L2).
* **Q22-BOX-3 (conventions).** Conservative tails; canonical-lift
  convention (W-2 seam open); q = 4 genre F unbuilt; no drainage claim
  beyond the measured UND keys; vertex/order-1 rows partition-checked
  only.
* **Q22-BOX-4 (visibility).** Deep refinement (t ≥ 2) realized only at
  the largest windows; laws at unrealized depths are extrapolations of
  the predictor, not measurements.

## S8. VERDICT (appended at commit 2 FROM the committed artifacts)

**GREEN — full roster, exit 0, 245.2 s** (`qscout22_output.txt` /
`qscout22_results.json` @ this commit). 23 rows = 7,614,473 quartic
members read individually (8 full B-space sweeps, 13 constructive
composite/prehistory rows, cross-checked where overlapping); all ten
families zero violations: Q22-PIN 2/0 · Q22-SUM 23/0 · Q22-W12TIE 37/0
· Q22-E 389/0 · Q22-F 40/0 · Q22-PRE 16/0 · Q22-CONS 1/0 · Q22-JD0
858/0 · Q22-XCHAR 94/0 · Q22-ORACLE 35,380/0. All four teeth fired:
T-LAT 15 · T-CEN 12 · T-ALPH 3 · T-SIG 6.

**Prediction verdicts.**
* **P-A (J-D0 at order 2): HOLDS.** 858 letter-uniformity checks
  (entry letters z / ψ; first refinement letters) all exactly uniform;
  Zp vs Fqt composite tables identical on all 94 XCHAR keys — counts
  are letter- and characteristic-blind at the composite stage.
* **P-B (bracket with E-densified ladder): HOLDS.** The four-slot
  model matched BOTH directions on every genre-E key of every row (389
  checks) including depth-4 histories on the full half-integer ladder;
  the integer-lattice corruption (W-11 transported naively) mismatched
  on every armed row (T-LAT 15/15). E appears as LADDER DENSITY, not
  as a count multiplier — no bare factor E anywhere in the fitted
  laws; dim K^c[ℤ/E] = E surfaces as the [Γ₁:Γ₀]-fold densification of
  refinement depths (and as the z̄-twist on odd-dμ residual letters,
  which counts never see).
* **P-C (alphabet squares at f₁ = 2): HOLDS.** Genre-F censuses and
  refinement letters are exactly the F_{q²} ones (T-ALPH 3/3; the
  q = 2 SPLITEQ ≠ 0 kill); boundary-band slots priced correctly.
* **P-D (char-2 forced (1²) cascade): NOT REALIZED IN-ROSTER.** The
  dead-S₁-slot configuration was not reachable at the windows run
  (S₁'s slot always exists at reachable dμ); P-D stands EXPLORATORY,
  untested — no claim.

**The measured mechanism (one paragraph).** At a composite stage the
order-1 tame law survives in FORM — count = census × lattice q-power —
with its two inputs re-based by the level-1 data (e₁, f₁): the census
field becomes F_{q^{f₁}} (genre F: q² alphabet, q²−1 refinement
letters) and the depth ladder densifies by e₁ (genre E: half-integer
μ-lattice, all integers dμ). Aggregated over histories the bracket
telescopes to q^{(#ladder points below the leaf)}, giving rate
q^{deg φ₂ · Δμ} = q^{e₁f₁·Δμ} in both genres — two different
microscopic mechanisms (density vs alphabet), one macroscopic rate
keyed to the KEY DEGREE. That is the exact sense in which "the wild
(2,2) is counted by the tame law": wildness never enters the count;
what changes at the composite boundary is only the (census field,
ladder density) re-basing, and σ-resolution now needs the level-2
read.

**Candidate lemma grades (S6).** Q22-L1: measured law, EXACT on all
rows (389 + 40 checks), status CONJECTURE with machine-exact instance
support — the GENIND target. Q22-L2 (refinement/prehistory transfer):
the model's floor-transfer reproduced every refined and prehistory
key (16 PRE checks; depth ≤ 4); status CONJECTURE, proof owed by
GENIND (W-11 S2.3's argument one level up; the smoke-disclosed
lift-stability caps are part of the correct statement). Q22-L3 (UND
law): measured EXACT per entry (e.g. genre E per center
(q−1)q^{N+(h−1)/2}); CONJECTURE.

**Deviations disclosed.** (i) S2's roster prose said "1-in-8 sample"
for the constructive oracle legs; the sealed runner's authoritative
steps are 4–1024 by row (largest rows sampled thinner) — prose
approximation, no post-seal change. (ii) The even-dμ genre-F keys
(where T-ALPH bites) are realized only at q = 2 in-roster (odd-q
would need N ≥ 7 at 3^{14}+ members) — the F_{q²}-census evidence at
odd q is via the (q²−q)/2 ψ-census and (q²−1) letter factors only
(Q22-BOX-4 extended). (iii) P-D untested (above).
