## VERDICT: GAPS-ONLY

The mathematical Revision-5 repairs are correct, but the claimed propagation is not complete.

- **(H-CHAIN) range:** Correct. FGMN requires  
  \(\phi_{k+1}\not\sim_{\mu_k}\phi_k\) for every \(1\le k<r\). With \(k=j+1\), \(r=i+1\), \(\phi_k\leftrightarrow\Phi_{k-1}\), and \(\mu_k\leftrightarrow w_k/E_k\), this becomes
  \[
  \Phi_{j+1}\not\sim_{w_{j+1}/E_{j+1}}\Phi_j,\qquad 0\le j\le i-1.
  \]
  This agrees with the primary definition. At order \(2\), \(i=1\), the range contains \(j=0\), giving the required \(\Phi_1\not\sim_{w_1/E_1}\Phi_0\); it is therefore non-vacuous. [FGMN, §3.1](https://upcommons.upc.edu/bitstream/handle/2117/28203/ResidualIdealsArxiv.pdf)

- **In-leaf propagation:** Complete. The corrected range reaches S1.4’s definition and supply discussion, S2.2’s unit-class use, S8.2’s ledger, and S8.5’s attack item. Later S5/S6 invocations consume the global `(H-CHAIN)` hypothesis without restating a conflicting bound.

- **Remaining propagation gap:** The leaf expressly admits that `HDISCHARGE_H1` member (b) still carries the old empty-at-order-2 range. That member is a consumer/supplier on which S1.4 says it relies. Therefore “propagated to every consumer” is false until the blueprint row is synchronized to \(0\le j\le i-1\). This is noncritical because the leaf states the correct hypothesis and records the debt; it is nevertheless a real remaining gap.

- **One-instance wording:** Correctly repaired. The live statements now consistently say that one specified depth-2 setup was computed/tested; none claims the general order-2 law.

- **S3.1 assembled coefficients:** Correctly repaired. It no longer identifies the inner FGMN and GMN operators. It identifies only the assembled pre-\(\epsilon_1\) expressions, with GMN’s external \(z_1^{t_1}\) factor supplying the power carried internally by the FGMN expression. S3.2’s ratio calculation uses that assembled equality, so its logic is unchanged.

- **Fresh sweep:** No new critical mathematical error or project-facing ord-service leak found.

The residue is complete if the blueprint sync is counted explicitly:

1. `TR-3′-GEN`;
2. `TR-3-ORD-X`;
3. `D-ITER` for \(E_i>1\), including order-2 \(e_0>1\);
4. member-(b)/`(H-CHAIN)(b)` refinement-step input and the owed blueprint range sync;
5. N-1;
6. the two-clean-pass acceptance bar.

I could not rerun the local gates or inspect workspace files because the command sandbox launcher is unavailable; this verdict covers the supplied Revision-5 text and the primary-paper check.