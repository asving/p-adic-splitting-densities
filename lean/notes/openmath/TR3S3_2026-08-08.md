# TR3-S3 — TR-3′-GEN at ORDER 3 on the all-e_j = 1 stratum: the composed identity σ₂(R^F₃(f)) = R_λ₃(f) proved as finite algebra (discrepancy ≡ 1)

Unit chartered by the depth-3 strata probe (`STRATA3_2026-08-08.md` S3: "a
compose unit fires only on an exact measured law"; the measured law =
(IDENT-3), 0/1,656). Parents: `TR3S1_2026-08-08.md` (ACCEPTED: order 2,
e₀ = 1 — the σ-construction genre, Lemma A, the assembly), `TR3S2_2026-08-08.md`
(ACCEPTED: order 2, all e₀ — Lemma A-GEN/EXP, the ORD-INV matched-key
discipline), `GRB_ORDER2_2026-08-05.md` REV 14 (corrected ǫ; REV-12/13
narrowings). Genre: attempt-grade leaf proof note; finite algebra executed in
full; the numerics are FALSIFIERS only (`strata3_probe.py` recorded 21,950/0 +
the NEW preregistered `tr3s3_check.py`, S8). Every formula pin carries a
non-textual leg or an explicit transcription-scope display (the shared
fgmn.txt extraction is CORRUPTED at Def 3.15 and is never consulted; S1).
HARD FENCES honored: no stratum with any e_j ≥ 2 is touched (the sealed
(TWIST-3) candidate stays sealed, S7); no D-ITER object is consumed (E₃ = 1
makes the composite digit algebra rank 1 — empty here).

## S0. Statement and verdict

**Theorem TR3-S3 (proved below at attempt grade; falsifiers green; acceptance
counter 0/2).** Standing setting of the GRB leaf S1.1 (GD23 §1.2), now at a
state of ORDER 3 on the totally-unramified-reads stratum: reads
ν₀ = (e₀, h₀, ψ₀), ν₁ = (e₁, h₁, ψ₁), ν₂ = (e₂, h₂, ψ₂) with
**e₀ = e₁ = e₂ = 1**; ψ_j monic irreducible with ψ_j(0) ≠ 0; Φ₀ a monic root
lift (w₀(Φ₀) = 0, deg Φ₀ = d₀); Φ₁ a lawful monic lift of ν₀
(deg Φ₁ = g₀d₀, w₁(Φ₁) = g₀h₀, R_{λ₀}(Φ₁) ∼ ψ₀); Φ₂ a lawful monic lift of
ν₁ (deg Φ₂ = g₁·deg Φ₁, w₂(Φ₂) = g₁γ₂, R_{λ₂}(Φ₂) ∼ ψ₁); w₃ the order-3
state valuation; γ₂ = w₁(Φ₁)·e₁ + h₁ = g₀h₀ + h₁ and
γ₃ = e₂·w₂(Φ₂) + h₂ = g₁γ₂ + h₂. Let σ₀ : F₁ → K₀, σ₁ : F₂ → K₁ be the
ACCEPTED TR3-S1 identifications (consumed, S2) and σ₂ : F₃ → K₂ the extension
constructed in S2 (z₂ ↦ z̄₂, σ₂|_{F₂} = σ₁). DISPLAYED HYPOTHESES:
**(H-ℓ)** the normalized Bézout convention 0 ≤ ℓ_j < e_j at every level (the
project's standing engine convention, exactly TR3-S1 S0's hypothesis; on this
stratum it forces (ℓ_j, ℓ′_j) = (0, 1) — S3); **(H-v3)** the GMN order-3
valuation/ordinate is v₃ = w₂, i.e. u_i = w₂(A_i) + i·w₂(Φ₂) — the order-3
extension of the PINNED order-2 Def-2.5 identity v₂ = w₁ (derivation sketch
displayed in S1; NOT proved from the print here — this is the probe's (H-v3),
kept displayed per the charter). [DISCHARGED at HV3 acceptance fa2180e — the
scope is UNCONDITIONAL ON PRINT per HV3-THM's four standing conditions: the
dedicated unit `HV3_DISCHARGE_2026-08-08.md` proved v₃ = w₂ as a THEOREM at
e₀ = e₁ = 1 (e₂ free — this stratum contained), passes 4 (Codex) + 5 (Fable)
consecutive CLEAN 0C/0G, model-diverse, with the adversarial type-hood
control on record; every (H-v3) consumption below now reads through that
theorem] Then for EVERY nonzero f ∈ O[x], with
γ := w₃(f), s := s₀(f) the initial abscissa of the λ₃-component of N₃⁻(f),
j₀ := ⌊s/e₂⌋ = s:

    (TR3-S3-coeff)   σ₂-coefficientwise, at every slot j (abscissa s_j = j):
                     σ₂( C^F3_j ) = c^G3_j ,
                     with support EQUAL on both sides (nonzero exactly on-line);

    (TR3-S3-form)    σ₂( R^F₃(f) ) = R_λ₃(f)   EXACTLY — discrepancy ≡ 1,
                     per-argument units ≡ 1, no variable rescaling,

where C^F3_j are the coefficients of FGMN's marched level-3 weight read
R^F_{3,γ}(f) (Def 3.16 at i = 3, corrected Def 3.15 at level 2, integer
weights — RESCALE is DEGENERATE here: E₁ = E₂ = E₃ = 1, all Γ_i = ℤ) and
c^G3_i, R_λ₃(f) are the GMN-convention order-3 residual data (Defs 2.19–2.21
at r = 3 with the (18) ordinate under (H-v3), normalized ℓ). The ǫ-recurrence
does NOT revive on this stratum: this is the theorem form of the probe's
measured (IDENT-3), and it is the exact order-3 all-unramified restriction of
TR-3′-GEN in the corrected (TR3-S2-form) genre with θ-analogues ≡ 0. No
(R1)/(R2) hypothesis is consumed: the identity is total in nonzero f ∈ O[x].

| clause | status | where |
|---|---|---|
| σ₂ constructed, well-defined, march-compatible (family {σ₀, σ₁, σ₂}) | PROVED (σ₀, σ₁ consumed from accepted TR3-S1) | S2 |
| Lemma KER (the ǫ-triviality kernel: s ≡ 0, ℓ_j = 0, ǫ₁ ≡ ǫ₂ ≡ 1) | PROVED (one line per level) | S3 |
| Lemma A3 (the level-3 inner identity = accepted TR3-S1 on the digit; m₀ = s₂(A)) | PROVED (restriction argument displayed) | S4 |
| Lemma Z2 (off-line vanishing of the level-2 read below its weight) | PROVED (two-level dichotomy) | S4 |
| the assembled per-slot compose + support + two-form (TR3-S3) | PROVED (the unit's content) | S5 |
| TR-3′-GEN restricted to order 3, all e_j = 1 | THEOREM, proved CONDITIONAL ON (H-v3) [REVISION 1: the identity itself stands; (H-v3) carries its attribution to the GMN order-3 operator] [DISCHARGED at HV3 acceptance fa2180e — the scope is UNCONDITIONAL ON PRINT per HV3-THM's four standing conditions] | S6.1 |
| TR-3-ORD-X at these states (GMN-lawful key = FGMN-normalized key here) | PROVED conditional on (H-v3) + (KP_i) + [T]-properness (inherited, displayed) [the (H-v3) leg DISCHARGED at HV3 acceptance fa2180e — the scope is UNCONDITIONAL ON PRINT per HV3-THM's four standing conditions; (KP_i)/[T] stand, still priced] | S6.2 |
| consumers (GD3-FULL/OL-B (2′) leg; GD3-MIN Route B + clause-(4); B-M2 OL-1(b)) | served at these states CONDITIONAL ON (H-v3) (exact forms displayed) [DISCHARGED at HV3 acceptance fa2180e — the scope is UNCONDITIONAL ON PRINT per HV3-THM's four standing conditions; the OTHER displayed seams per site are untouched] | S6.3 |
| strata with any e_j ≥ 2 (the (TWIST-3) revival regime), orders ≥ 4, D-ITER | NOT PROVED / NOT TOUCHED — scope box | S7 |
| falsifiers: recorded strata3_probe + NEW preregistered tr3s3_check (composed E2E) | see S8 | S8 |

## S1. Setting, dictionary at depth 3, and the operator pins

Consumed from the GRB leaf and TR3-S1/TR3-S2 (not re-derived): TR-5 (index
dictionary, now r = 3: FGMN φ_k ↔ project Φ_{k−1}, FGMN level data
(e_i, h_i)^F = (e_{i−1}, h_{i−1})^{proj}, so here (e₁,h₁)^F = (1,h₀),
(e₂,h₂)^F = (1,h₁), (e₃,h₃)^F = (1,h₂)); Lemma RESCALE (w_i = E_iµ_i; on this
stratum E₁ = E₂ = E₃ = 1, every e(µ_i) = 1, every Γ_i = ℤ — the dilation is
the identity at every level and all FGMN weights below are already integers);
TR-1-corr; **(H-CHAIN) at depth 3** (the tower is a MacLane chain of
µ₃ = w₃ in FGMN's Def 3.1 sense — the leaf's standing input, consumed here
one level deeper than TR3-S1 consumed it); and the GD23 standing GMN rows
(w_i ↔ v_{i+1}, K_i ↔ F^{GMN}_{i+1}, engine residuals = GMN-convention
residuals), extended at the top by (H-v3) (below).

**Formula pins.**

* **FGMN eq (12)** at level i: u(α)e_i + s(α)h_i = e(µ_i)α, 0 ≤ s(α) < e_i;
  Bézout ℓ_ih_i + ℓ′_ie_i = 1 with 0 ≤ ℓ_i < e_i (FGMN's own print
  normalizes; fgmn.txt:1073). All non-textual legs inherited from N-1/TR3-S1
  (T0 symbolic pin 0/23,111). On this stratum every e_i^F = 1 — see S3.
* **FGMN Def 3.15 (CORRECTED, reading C)**: ǫ_i(α) = (z_i)^{ℓ′_i s(α) − ℓ_i u(α)}.
  **Transcription scope, displayed:** the print's display is GENERIC in the
  level index i; the N-1 triple pin (byte-column re-extraction, the printed
  i = 0 semantic check, the PARI kill-shots P3/P12) fixed the corrected
  reading at the display level and discriminated it externally AT LEVEL 1;
  the depth-3 probe's M1/CERT3 rows (1,641/1,641 reading-P refutations on
  PARI-certified order-3 towers) discriminated the SAME reading AT LEVEL 2
  for the first time. The corrupted raw extraction is never consulted. What
  this unit consumes: ǫ₁ and ǫ₂ in reading C — and on this stratum BOTH are
  ≡ 1 (S3), so the reading enters only through the falsifier meters, not
  through the algebra.
* **FGMN Def 3.16** (fgmn.txt:1246, generic in i). At i = 3: for
  f = Σ_i A_iΦ₂^i (the Φ₂-development, deg A_i < deg Φ₂ — both operators
  read this SAME development, φ₃ ↔ Φ₂ under TR-5),
  R_{3,γ}(f) = Σ_j ǫ₂(γ_j)·R_{2,γ_j}(A_{s_j})(z₂)·y^j, slot abscissas
  s_j = s(γ) + je₂ = j (S3: s(γ) = 0, e₂ = 1), slot weights

      (SLOT-3)   γ_j = γ − s_j·γ₃ = γ − j·γ₃ ∈ ℤ

  (µ₃(φ₃) = µ₂(φ₃) + λ₃ = w₂(Φ₂) + h₂ = γ₃ at E = 1 — no integrality
  condition survives, stride 1). At i = 2 (the inner read): for
  A = Σ_m a_mΦ₁^m, R_{2,β}(A) = Σ_m ǫ₁(β_m)·R_{1,β_m}(a_m)(z₁)·y^m with
  β_m = β − mγ₂ (stride 1, forced start 0). At i = 1 and the i = 0 line:
  as in TR3-S1 S1 (level-0 read = reduction of b/π^δ, zero whenever
  w₀(b) > δ including δ < 0).
* **(PIN-GEN-i) — named OPEN ATTRIBUTION PIN (REVISION 1; the pass-1
  gap-2 display).** The identification of the corrected Def 3.15 AT LEVEL 2
  (ǫ₂), of Def 3.16 AT LEVEL 3, AND of Def 3.18 AT i = 3 \[REV2: Def 3.18
  added to the pin per pass-3 gap 1 — it shares the generic-index-only
  provenance\] with the printed FGMN operators rests on
  (a) the print's level-GENERIC displays and (b) computational
  discrimination only (N-1's triple pin at level 1; the probe's M1/CERT3
  rows at level 2) — no hand-verified print leg at the instantiated indices
  i = 2 / i = 3 exists, and the stipulated corrupted fgmn.txt extraction
  cannot supply one. Per the extraction-corruption discipline the owed
  non-textual leg is a fresh `pdftotext -layout` re-extraction of the FGMN
  PDF at Defs 3.15/3.16/3.18 [REV3: 3.18 added to the discharge recipe too, per pass-4 gap 1] (decorrelated from the shared extraction), executed
  and hand-verified at the instantiated indices under a provenance-pinned
  copy of the PDF (the repo's reference set carries none; an ephemeral /tmp
  copy from a prior unit exists but is unpinned). That is a dedicated
  source-verification unit, NOT this revision: the leg is DISPLAYED AS
  OWED here, not attempted. Scope of what the pin gates: the ATTRIBUTION of the
  displayed operators to the FGMN print — NOT the finite identity of this
  unit, whose algebra consumes only ǫ ≡ 1 on-stratum (Lemma KER) and the
  displayed Def-3.16 and Def-3.18 shapes [REV4: Def 3.18 added — the fifth
  site of the pass-4 species; S5(iii)'s two-form division consumes its
  displayed shape].
* **FGMN Def 3.18**: R^F_i(g) = R_{i,α}(g)/y^{j₀}, j₀ = ⌊s(g)/e_i^F⌋; at
  e₃^F = e₂ = 1, j₀ = s(g) = the initial abscissa of the λ₃-component.
* **GMN Defs 2.19–2.21 at r = 3** (the level-generic recursion,
  gmn.txt:1823–1906; the r = 2 instance was pinned at TR3-S1/TR3-S2 —
  the r = 3 instance is the same print with r−1 = 2):
  ordinates u_i := v₃(A_iφ₃^i) = v₃(A_i) + i·v₃(φ₃), consumed under (H-v3)
  as **u_i = w₂(A_i) + i·w₂(Φ₂)**; twist exponent
  **t₂(i) = (s₂(A_i) − ℓ₁u_i)/e₁** where s₂(A_i) = the initial abscissa of
  S₂(A_i) (the λ₂-component of N₂⁻(A_i), the tower's SECOND-read slope,
  fixed by the type — the same "type fixes the inner slope" pin as TR3-S1
  S9.6, one level up) and (ℓ₁, e₁) are the GMN level-2 = project read-ν₁
  data; coefficients c^G3_i = z₂^{t₂(i)}·R₂(A_i)(z₂) on the λ₃-component of
  N₃⁻(f), 0 strictly above; R_λ₃(f) = c^G3_s + c^G3_{s+e₂}y + ⋯ + c^G3_{s+de₂}y^d.
  The inner R₂(A) is GMN's order-2 residual polynomial w.r.t. λ₂ — under the
  GD23 rows and accepted TR3-S1 this is the project operator R_λ₂(A) of
  TR3-S1's S1 pins (its coefficients c^G2 are the objects TR3-S1's theorem
  matches). Evaluation R₂(A)(z₂) is Def 2.21's substitution of the residue
  generator, from the ACTUAL initial abscissa s₂(A) (constant term =
  c^G2_{s₂(A)}).
* **(H-v3) derivation sketch (why the hypothesis is the expected identity —
  and why it stays a hypothesis).** Def 2.5: v_r(P) = e_{r−1}·H_{r−1}(S_{r−1}(P)).
  At r = 3 on the stratum: e₂^{GMN} = e₁ = 1, and IF H₂ is the λ₂-intercept
  datum in the same normalization as the pinned r = 2 instance
  (H₁(S₁(P)) = min_m(u_m + mλ₁), which gave v₂ = w₁ verbatim), then
  H₂(S₂(P)) = min_i(w₁(a_i) + i·w₁(Φ₁) + i·h₁) = min_i(w₁(a_i) + iγ₂) = w₂(P)
  at e₁ = 1 — i.e. v₃ = w₂. The H_{r−1} display at generic r was NOT
  re-extracted for this unit (the r = 2 instance was consumed evaluated, not
  as a generic formula), so per the extraction-corruption discipline the
  identity is kept as the DISPLAYED HYPOTHESIS (H-v3), shared-input scope:
  both sides below consume the SAME w₂. External cover at instance level:
  the probe's P0/CERT3 rows (GMN-3 integrity + PARI order-3 certification)
  ran under exactly this reading, 0 violations.
  [DISCHARGED at HV3 acceptance fa2180e — the scope is UNCONDITIONAL ON PRINT per HV3-THM's
  four standing conditions: this sketch is exactly what the dedicated unit
  `HV3_DISCHARGE_2026-08-08.md` completed — the H_{r−1} generic display
  RE-EXTRACTED under the sha256-pinned `HigherNewton.tex` ((E1)–(E4)), the
  five-step derivation written in full, the r = 2 instantiation reproducing
  the accepted TR3-S1 pin verbatim; accepted at passes 4+5 consecutive
  CLEAN, model-diverse, with fresh 615/615 + 1,655/1,655 two-way falsifiers
  at off-roster primes and the adversarial type-hood control]
* **GMN normalization note (display duty, verbatim TR3-S2 S1).** The print
  fixes ℓ only up to representative/sign; replacing ℓ₁ = 0 by qe₁ = q
  changes t₂(i) by −q·u_i and transforms R_λ₃ by the standing law
  R′_λ(y) = z₂^{−q·u(f)}·R_λ(z₂^{qh₂}y) — scalar × variable rescaling. The
  on-the-nose identity below is scoped to the normalized choice (H-ℓ); the
  S6.2 ord conclusions survive any fixed choice.

## S2. The σ-family at depth 3: σ₂ constructed

Towers: FGMN F₀ = F, F₁ = F₀[z₀], F₂ = F₁[z₁], F₃ = F₂[z₂] (ψ_i^F := the
minimal polynomial of z_i over F_i); project K₀ = F[X]/(φ̄₀),
K₁ = K₀[z]/(ψ₀) with z̄ := the class, K₂ = K₁[z]/(ψ₁) with z̄₂ := the class.

**σ₀ and σ₁ — CONSUMED from accepted TR3-S1 S2** (z₀ ↦ X̄, z₁ ↦ z̄,
σ₁|_{F₁} = σ₀, the evaluation square (SQ)). Both constructions read Cor
5.6(2) chain-internally at indices 0, 1 < r; at r = 3 these indices are
chain-internal a fortiori — nothing to redo. The order-2 SUBSTATE consumed
throughout this note is the tower's own truncation (reads ν₀, ν₁, keys
Φ₀, Φ₁, the same K₀, K₁): a MacLane chain of µ₃ truncates to a MacLane chain
of µ₂ (drop the last augmentation — Def 3.1 is nested by construction), so
(H-CHAIN) at depth 3 supplies (H-CHAIN) at depth 2 for the substate, and the
substate's read data (lawful Φ₁, residual condition R_{λ₀}(Φ₁) ∼ ψ₀) are the
state's own first-read data. The substate lies on TR3-S1's stratum
(e₀ = 1) — accepted TR3-S1 applies to it in full.

**σ₂ : F₃ → K₂ extending σ₁ with z₂ ↦ z̄₂.** Well-defined iso iff
σ₁[y](ψ₂^F) = ψ₁. Supply:
(i) ψ₂^F = R₂^F(φ₃) = R₂^F(Φ₂) — Cor 5.6(2) chain-internal at index
2 < r = 3 ((H-CHAIN) at depth 3 consumed; NO (KP_i) needed here — the
extended-chain reading is needed only at the state key Φ₃, S6.2, exactly
mirroring how TR3-S1 needed it only at Φ₂);
(ii) accepted TR3-S1 at f := Φ₂ (its theorem is total in nonzero f):
σ₁(R₂^F(Φ₂)) = R_λ₂(Φ₂);
(iii) monicity upgrade of the read datum R_λ₂(Φ₂) ∼ ψ₁ to EQUALITY — the
TR3-S1 S6.2 step-1 computation at stride 1, displayed: Φ₂ monic of exact
degree g₁·deg Φ₁ has top Φ₁-digit a_{g₁} = 1 with w₁ = 0, ordinate
u = g₁·w₁(Φ₁), on-line check e₁u + g₁h₁ = g₁(w₁(Φ₁) + h₁) = g₁γ₂ = w₂(Φ₂);
the digit 1 has inner data R₁(1) = 1, s₁(1) = 0, so t₁ = (0 − ℓ₀u)/e₀ = 0
(ℓ₀ = 0) and c^G2_{g₁} = 1 — the leading coefficient is 1 (the θ-character
of TR3-S2 is ≡ 1 here); lawfulness (deg R_λ₂(Φ₂) = g₁ = deg ψ₁) forces
s₂(Φ₂) = 0; a monic associate of the monic ψ₁ equals ψ₁. Hence
σ₁(ψ₂^F) = R_λ₂(Φ₂) = ψ₁. ∎

**March-compatibility.** σ₂|_{F₂} = σ₁, all tower squares commute, and the
evaluation square

    (SQ₂)   σ₂( P(z₂) ) = ( σ₁[y]P )( z̄₂ )   for every P ∈ F₂[y]

(σ₂ = the quotient of σ₁[y] by (ψ₂^F) ↦ (ψ₁)). {σ₀, σ₁, σ₂} is the
level-by-level compatible family of TR-3′-GEN at order 3.

## S3. Lemma KER — the ǫ-triviality kernel (route step (i))

**Lemma KER.** At every FGMN level i ∈ {1, 2, 3} of this stratum
(e_i^F = 1): for every β ∈ ℤ, the eq-(12) data are (s(β), u(β)) = (0, β),
the normalized Bézout pair is (ℓ, ℓ′) = (0, 1), and hence
ǫ₁(β) = z₁^{1·0 − 0·β} = 1 and ǫ₂(β) = z₂^{1·0 − 0·β} = 1 for every β
(ǫ₀ ≡ 1 by the printed convention AND the formula, as in TR3-S1 (A1)).

*Proof.* u·1 + s·h = β with 0 ≤ s < 1 forces s = 0, u = β; ℓh + ℓ′·1 = 1
with 0 ≤ ℓ < 1 forces ℓ = 0, ℓ′ = 1. Substitute into the corrected Def 3.15
exponent ℓ′s − ℓu = 0. ∎

Consequences used silently below: every forced abscissa progression starts
at 0 with stride 1 (all slots exist at all abscissas j ≥ 0); GMN's twist
exponents lose their ℓu-terms (t₁(i) = s₁(a_i), t₂(i) = s₂(A_i) — route
step (iv), consumed at S5(ii)); and GMN's printed t-integrality is trivial.
This is TR3-S2's Lemma EXP with every ℓ₀-term dead at BOTH levels — the
compose kernel of this stratum is literally zero lines of Bézout algebra.

## S4. Lemma A3 and Lemma Z2 — the level-2 input, packaged

**Lemma A3 (route steps (ii)+(iii): the level-3 A-GEN shift and the digit
identity are ONE consumed theorem).** Let A ∈ O[x] be nonzero and
β := w₂(A). Then, in K₁[y]:

    (A3)   σ₁[y]( R^F_{2,β}(A) ) = y^{s₂(A)} · R_λ₂(A),

where s₂(A) is the initial abscissa of the λ₂-component of N₂⁻(A) and
R_λ₂(A) the GMN-convention order-2 residual polynomial of the substate.

*Proof.* This is accepted TR3-S1's display (TR3-S1-coeff) applied to the
order-2 substate (S2) at f := A, γ := w₂(A): TR3-S1 gives
R^F_{2,γ}(A) = y^{j₀}·R_λ(A) σ₁-coefficientwise with j₀ = ⌊s₀(A)/e₁⌋; at
e₁ = 1, j₀ = s₀(A) = s₂(A) (the same initial-abscissa datum — TR3-S1 S5(i)
identifies the two on-line notions and their endpoints). TR3-S1 is total in
nonzero f ∈ O[x] with no degree bound (its scope note), so the digit
qualifies whatever its degree. The forced-vs-actual abscissa bookkeeping
m₀ = (s₂(A) − s(β))/e₁ = s₂(A) (s(β) = 0, Lemma KER) is the probe's row-P2
identity — here it is just the y^{s₂(A)} factor of (A3). ∎

**Lemma Z2 (off-line vanishing; the slot-domain input of S5(ii)).** Let
A ∈ O[x] be nonzero and β ∈ ℤ with β < w₂(A). Then R^F_{2,β}(A) = 0 — every
coefficient vanishes.

*Proof.* By S1's Def-3.16 pin at i = 2, the m-th coefficient is
ǫ₁(β_m)·R_{1,β_m}(a_m)(z₁), β_m = β − mγ₂, over the Φ₁-development
A = Σ a_mΦ₁^m. Each nonzero digit has w₁(a_m) ≥ w₂(A) − mγ₂ > β − mγ₂ = β_m
(the min defining w₂). In turn each Φ₀-digit b of a_m has
w₀(b) ≥ w₁(a_m) − m′h₀ > β_m − m′h₀ = the level-0 read weight δ, so every
coefficient of b·π^{−δ} has positive valuation and its reduction is 0 —
including δ < 0 (TR3-S1 (A3)'s corner, verbatim). Every level-1 coefficient
of every slot vanishes, hence so does every level-2 coefficient. ∎
(Domain check: β < w₂(A) means A ∈ P_β(µ₂) — Def 3.16 is applied inside its
printed domain; A = 0 reads 0 by the printed convention.)

Falsifier faces: (A3) = rows RII (and P3/P2 of the probe); (Z2) = the
constructed row Z2 and the honestly-computed off-line slots of E2A — the
probe HARD-CODED off-line slots to zero, so (Z2) was an implementation
assumption there; the new harness computes them (S8).

## S5. The theorem: the assembled per-slot compose

Fix nonzero f ∈ O[x] with Φ₂-development f = Σ_i A_iΦ₂^i and
γ := w₃(f) = min_i(w₂(A_i) + iγ₃) (e₂ = 1); s(γ) = 0 (Lemma KER), so FGMN's
slot j sits at abscissa s_j = j for every j ≥ 0.

**(i) Outer support is the same set on both sides.** Under (H-v3) GMN's
order-3 ordinate is u_i = w₂(A_i) + i·w₂(Φ₂), so

    e₂·u_i + i·h₂ = w₂(A_i) + i·(w₂(Φ₂) + h₂) = w₂(A_i) + i·γ₃ ≥ γ,

with equality iff (i, u_i) lies on the λ₃-component S of N₃⁻(f). Call these
i ON-LINE. At stride e₂ = 1 no congruence condition survives: EVERY on-line
abscissa i is the slot j = i, and conversely FGMN's slot j is on-line iff
w₂(A_j) = γ_j = γ − jγ₃ ((SLOT-3)) — the same equality. The initial/final
on-line abscissas s := s₀(f), s′ give GMN's component data (s, d) with
d = s′ − s, and FGMN's j₀ = ⌊s/e₂⌋ = s. [This is TR3-S1 S5(i) transcribed
one level up; the only depth-3 novelty is that the ordinate consumes (H-v3).]

**(ii) Per-slot identity.** Slot j, digit A := A_j, slot weight γ_j. Three
cases.
* A = 0, or A ≠ 0 with w₂(A) > γ_j (off-line): FGMN's coefficient is
  ǫ₂(γ_j)·R^F_{2,γ_j}(A)(z₂) = 0 by Lemma Z2 (domain: every nonzero digit
  has w₂(A) ≥ γ_j from γ = min; this covers γ_j < 0, where no digit can be
  on-line since w₂ ≥ 0). GMN: (j, u_j) strictly above N (or A = 0), so
  c^G3_j := 0; outside the component window GMN reads nothing and FGMN's
  coefficient is 0 by the same lemma. Both sides 0.
* on-line: w₂(A) = γ_j, A ≠ 0. Then

      σ₂( C^F3_j ) = σ₂( ǫ₂(γ_j) · R^F_{2,γ_j}(A)(z₂) )
                   = σ₂( R^F_{2,γ_j}(A)(z₂) )              [Lemma KER: ǫ₂ ≡ 1]
                   = ( σ₁[y] R^F_{2,γ_j}(A) )( z̄₂ )        [(SQ₂)]
                   = z̄₂^{s₂(A)} · R_λ₂(A)( z̄₂ )            [Lemma A3, β = γ_j
                                                             = w₂(A)]
                   = z̄₂^{t₂(j)} · R₂(A)( z̄₂ )              [t₂(j) = s₂(A):
                                                    ℓ₁ = 0 (KER), e₁ = 1 —
                                                    Def 2.19 verbatim; and
                                                    GMN's inner R₂ = R_λ₂ at
                                                    the type-fixed λ₂, S1]
                   = c^G3_j .                               [GMN Def 2.20]

  **Nonvanishing (both sides).** R_λ₂(A) is a NONZERO polynomial of degree
  ≤ g₁ − 1: nonzero with nonzero ends by TR3-S1 S5(iii) (both component
  endpoints on-line); the degree bound because deg A < deg Φ₂ = g₁·deg Φ₁
  confines the Φ₁-development to abscissas ≤ g₁ − 1, so the component window
  has length ≤ g₁ − 1 — THIS is where the digit's degree bound is consumed.
  Since deg R_λ₂(A) < g₁ = deg ψ₁ = the degree of z̄₂'s minimal polynomial
  over K₁, the evaluation at z̄₂ is nonzero ({1, z̄₂, …, z̄₂^{g₁−1}} is a
  K₁-basis of K₂); multiplying by the unit z̄₂^{s₂(A)} keeps it nonzero. So
  σ₂(C^F3_j) = c^G3_j ≠ 0 at every on-line slot — GMN's printed on-line
  nonvanishing re-derived, and (reading the chain backwards through the
  field iso) C^F3_j ≠ 0. Combined with the off-line case: support is
  EXACTLY the on-line set on both sides.

**(iii) Assembly.** By (i)+(ii), σ₂-coefficientwise R^F_{3,γ}(f) = Σ_j C^F3_j y^j
has support = the on-line set ⊆ [s, s′] with σ₂(C^F3_j) = c^G3_j at EVERY
j ≥ 0 (both sides 0 off-line). Hence

    σ₂( R^F_{3,γ}(f) ) = Σ_{k=0}^{d} c^G3_{s+k} · y^{s+k} = y^{s} · R_λ₃(f),

and dividing by y^{j₀} (Def 3.18; j₀ = s by (i), and the k = 0 term is
nonzero, so the observed initial slot IS j₀):
σ₂(R^F₃(f)) = R_λ₃(f) EXACTLY — aligned constant terms at the shared initial
abscissa s, equal degrees d, per-argument units ≡ 1, no rescaling. ∎

**Remark (what the compose bought, and what it did not need).** Route steps
(i)–(iv) of the probe's sealed derivation are exactly Lemma KER, Lemma A3's
y-shift, Lemma A3's digit identity, and the Def-2.19 collapse t₂ = s₂(A) —
each either zero-line algebra (KER, t₂) or a citation of an ACCEPTED theorem
(A3 = TR3-S1). The only genuinely new proof obligations at depth 3 were
Lemma Z2 (the off-line domain lemma, which TR3-S1 needed only one level
down) and the σ₂ construction (S2). No marched-ǫ residue survives — the
REV-12 revival mechanism has nothing to act on here because BOTH inner-level
characters are killed by KER before the march starts; nothing in this proof
constrains the e_j ≥ 2 strata where they are not (S7).

## S6. Consumers

### S6.1 TR-3′-GEN restricted to order 3, all e_j = 1 — probe conjecture → THEOREM conditional on (H-v3)

The probe's (IDENT-3) (instance-confirmed 0/1,656, conjecture grade) is now
the identity clause of Theorem TR3-S3: under {σ₀, σ₁, σ₂}, (H-ℓ) and (H-v3),
σ₂(R^F₃(f)) = R_λ₃(f) identically. Combined with the accepted order-2
two-stratum theorem (TR3-S1 + TR3-S2):

> **TR-3′-GEN is now proved at: all order-2 states (two strata, the
> TR3-S2-form with θ = ℓ₀g₀h₀), and — proved CONDITIONAL ON (H-v3) — all
> order-3 states with e₀ = e₁ = e₂ = 1 (this unit, attempt grade pending
> hostile passes; discrepancy ≡ 1).** The order-3 identity itself stands as
> displayed; what (H-v3) carries is its attribution to the actual GMN
> order-3 operator — a false (H-v3) would leave the displayed identity true
> of the project-facing operator and strip only the GMN label (S7). At
> order 3 with any e_j ≥ 2, and at all orders ≥ 4, it stays OPEN — the
> sealed (TWIST-3) candidate is the next stratum's conjecture, untouched
> (S7).

[DISCHARGED at HV3 acceptance fa2180e — the scope is UNCONDITIONAL ON
PRINT per HV3-THM's four standing conditions: the blockquote's
"CONDITIONAL ON (H-v3)" clause is now carried by the accepted theorem
HV3-THM (v₃ = w₂ at e₀ = e₁ = 1, e₂ free), whose four conditions — the
pinned-print attribution baseline, accepted TR3-S1, the standing
(H-CHAIN)+RESCALE consumptions, the projective-dictionary consumptions —
are all standing rows, no bespoke hypothesis; the e_j ≥ 2 / orders ≥ 4
OPEN clauses are unchanged]

Through TR-4 (leaf S4), the (2′) final leg holds at these states in the
exact form σ₂(R^{gr}(in f)) = R_λ₃(f) — ON THE NOSE, no character to
thread. **Consumers GD3-FULL/OL-B: served CONDITIONAL ON (H-v3) at every
order-3 all-e_j = 1 state in the ∼-form AND the =-form** (the strongest
service the (2′) leg admits; the conditionality is the GMN-attribution,
exactly as above); elsewhere they continue to sit on OPEN TR-3′-GEN.
[DISCHARGED at HV3 acceptance fa2180e — the scope is UNCONDITIONAL ON
PRINT per HV3-THM's four standing conditions; the "elsewhere" clause is
unchanged]

### S6.2 TR-3-ORD-X at these states — proved with inherited conditionality

On this stratum the TR3-S2 key-convention bifurcation is INVISIBLE: the
discrepancy is ≡ 1, so the GMN-lawful and FGMN-normalized key classes
coincide (every state is twist-invisible; the probe's P4 row, 24/24). The
S6.2 chain, at order 3:
1. **Label tie.** ψ₃^F := R₃^F(Φ₃) is the minimal polynomial of z₃ by Cor
   5.6(2) read in the EXTENDED chain (Φ₀, Φ₁, Φ₂, Φ₃) — this consumes
   **(KP_i)** (Φ₃ ∈ KP(µ₃) + the chain condition), exactly as TR3-S1 S6.2
   consumed it at Φ₂. The Theorem at f = Φ₃ gives σ₂(ψ₃^F) = R_λ₃(Φ₃); the
   read datum R_λ₃(Φ₃) ∼ ψ₂ upgrades to equality by the S2(iii) argument
   one level up: Φ₃ monic with deg Φ₃ = g₂·deg Φ₂ and w₃(Φ₃) = g₂γ₃ has top
   Φ₂-digit A_{g₂} = 1, ordinate u = g₂·w₂(Φ₂), on-line
   (u + g₂h₂ = g₂γ₃ = w₃(Φ₃)); the digit 1 has s₂(1) = 0, R₂(1) = 1, so
   t₂ = 0 and c^G3_{g₂} = 1 — monic leading coefficient; lawfulness forces
   deg R_λ₃(Φ₃) = g₂, i.e. s₀(Φ₃) = 0; a monic associate of the monic ψ₂
   equals ψ₂. So σ₂(ψ₃^F) = ψ₂.
2. **Transport.** σ₂ extends to a ring iso F₃[y] → K₂[y] (coefficientwise),
   so ord_{ψ₂}(R_λ₃(f)) = ord_{σ₂ψ₃^F}(σ₂R₃^F(f)) = ord_{ψ₃^F}(R₃^F(f)) for
   every nonzero f. (This is (ORD-INV) with matched labels; since the key
   classes coincide here, the naive both-sides-ψ₂ pairing is also correct —
   no convention display owed at these states.)
3. **FGMN-internal leg.** ord_{ψ₃^F}(R₃^F(f)) = ord_L(R(f)) is Cor 5.15 with
   the **[T]-pinned properness** of the state key — NOT discharged here,
   exactly as in both parents.

**Hence TR-3-ORD-X holds at order-3 all-e_j = 1 states, conditional on
(H-v3) + (KP_i) + [T]-properness — (H-v3) carrying the GMN-attribution of
R_λ₃ as everywhere in this unit, (KP_i)/[T] the same package its consumers
already price. Consumers GD3-MIN Route B and ALL clause-(4) ord-consumers:
served at these states under exactly that conditionality; elsewhere they
continue to sit on OPEN TR-3-ORD-X** (order-2 service is the parents').
[The (H-v3) leg DISCHARGED at HV3 acceptance fa2180e — the scope is
UNCONDITIONAL ON PRINT per HV3-THM's four standing conditions; the
(KP_i) + [T]-properness legs STAND exactly as displayed]

### S6.3 B-M2 OL-1(b), GMN-verbatim face

The TR-3-ORD-X seam of B-M2's OL-1(b) is now discharged at order-3
all-e_j = 1 states as well (with (H-v3) + (KP_i) + [T] inherited). OL-1(b) remains
conditional on its OTHER displayed seams (GRADED-READ; REC-SL where
applicable) everywhere, and on TR-3-ORD-X beyond {order ≤ 2} ∪ {order 3,
all e_j = 1}. No other B-M2 clause is touched. [The inherited (H-v3) leg
DISCHARGED at HV3 acceptance fa2180e — the scope is UNCONDITIONAL ON PRINT
per HV3-THM's four standing conditions; (KP_i)/[T] and the OTHER displayed
seams stand]

## S7. Scope box — what this unit does NOT prove

* **Order 3 with ANY e_j ≥ 2 — the genuine ǫ-revival regime.** Fenced HARD.
  The sealed candidate (TWIST-3) C^F3_j = z₂^{θ₂s_j}·c^G3_{s_j},
  θ₂ = ℓ₁g₁γ₂ (the probe's preregistered shape) is UNTESTED and NOT
  constrained by anything here: Lemma KER dies at the first e_j ≥ 2 (the
  ℓ-terms revive at that level), Lemma A3's citation of TR3-S1 requires the
  substate's e₀ = 1, and the t₂-collapse requires ℓ₁ = 0. A future unit
  needs the TR3-S2 mechanism (EXP-genre kernel) threaded through TWO live
  levels — genuinely new mathematics, per the leaf's REV-12 narrowing.
* **Orders ≥ 4** (even all-unramified): the recursion pattern of this note
  plausibly iterates (KER kills every level's ǫ; the inner citation would
  be THIS theorem one level down), but the induction is NOT written and
  TR3-S1's acceptance covers only order 2 — stating it would be exactly the
  general-σ_j-family clause of TR-3′-GEN, which stays OPEN.
* **(H-v3)**: displayed, not proved (S1 sketch; the H_{r−1} generic display
  was not re-extracted). The GMN-verbatimness of the order-3 ordinate rides
  it; a wrong (H-v3) would make R_λ₃ above the PROJECT operator rather than
  GMN's — the theorem would survive as the project-facing identity, its
  GMN-attribution would not. [DISCHARGED at HV3 acceptance fa2180e — the
  scope is UNCONDITIONAL ON PRINT per HV3-THM's four standing conditions:
  the H_{r−1} generic display WAS re-extracted by the dedicated unit
  (`HV3_DISCHARGE_2026-08-08.md`, sha256-pinned `HigherNewton.tex`) and
  v₃ = w₂ proved at e₀ = e₁ = 1 (e₂ free); this scope-box item closes]
* **(H-ℓ) normalization scope** — as in both parents (S1 note); the ord
  conclusions survive any fixed choice.
* **(KP_i) and [T]-properness** — consumed at S6.2 step 1/3, not discharged.
* **Transcription scope = the named open pin (PIN-GEN-i) (S1, REVISION
  1)** [the owed leg DISCHARGED separately at 303b11e — the FGMN
  re-derivation unit's §S7 rider: fresh column-verified `pdftotext
  -layout` extraction at Defs 3.15/3.16/3.18 under the sha256-pinned PDF,
  genericity hand-verified at the instantiated indices; residual
  displayed there: single extractor pass; the pin STAYS DISPLAYED as the
  attribution record]: ǫ₂ (corrected Def 3.15 at level 2), Def 3.16
  at i = 3, and Def 3.18 at i = 3 [REV3] are consumed as the print's
  level-generic displays; external
  discrimination of the level-2 ǫ reading exists at probe scope only
  (M1/CERT3, 24 towers, p ∈ {2,3}) — no hand-verified print pin at i = 2
  exists beyond genericity. The fresh `pdftotext -layout` leg on the FGMN
  PDF at those definitions stays OWED (S1). On THIS stratum the algebra
  consumes only ǫ ≡ 1; a reading error would surface as a bug-detector RED
  in the falsifiers' P-meters, not silently.
* **D-ITER / composite digit algebra**: untouched (E₃ = 1, rank-1 carry —
  the probe's V3 δ ≡ 0 row is the degenerate control).
* **GMN's printed base scope** (finite extensions of ℚ_p) vs the project's
  DVR engine convention: as in the parents; equal-characteristic towers
  covered by the falsifiers against exactly that reading.

## S8. Falsifier record (2026-08-08)

### S8.1 Recorded: the depth-3 probe (prior to this unit)

`verification/openmath/strata3_probe.py` (seed 20260817, sealed pre-run at
d8ef8d8): C1 (IDENT-3) 0/1,656; P0–P5, V1–V5, CERT2/CERT3, M1–M4 all at
predicted counts; 21,950 samples, exit 0, first full run ALL GREEN. The
probe's C1/P2/P3/P4/P5 rows are instance faces of S5/S4/S6.2.

### S8.2 NEW: the preregistered composed-identity family

`verification/openmath/tr3s3_check.py` (seed 20260818, fresh; predictions
sealed in the script header BEFORE the run; independence perimeter stated
there — shares Tower3 + the probe's fresh GMN side, brings a FRESH FGMN
pipeline R2w_at/fresh_R3 that computes off-line slots HONESTLY, tests the
composed identity end-to-end as side-read LISTS, decomposes the S5(ii) chain
into rows RI–RIV, and adds the prescribed-value family W: constructed f with
a-priori slot prescriptions, noise-dressed on half the draws). Rows: E2A
(per-abscissa composed identity), E2B (two-form as lists + j₀ law), RI
(Lemma KER), RII (Lemma A3 + m₀ = s₂(A)), RIII (t₂-absorption), RIV
(recomposition from level-2 GMN data only — non-circular vs E2A), Z2
(Lemma Z2 constructed + own-weight cross-validation), XV (fresh-vs-probe
FGMN), W, K (S6.2 label ties), O (matched-label ord transport), CERT
(re-fired external legs incl. PARI). All predicted 0; meters m0pos/j0pos/
noff/noisy/holes/ord≥1 must fire.

**Provenance (REVISION 1; the pass-1 gap-4 evidence).** The temporal
claims below are now carried by the repository record, not by assertion:
the preregistration seal is commit **b4a142b** (this note + `tr3s3_check.py`
with the sealed prediction header, committed BEFORE the first execution);
the first-run record is commit **482150b** (log + JSON of the run below).
The REVISION 1 rerun (RI legs added; S8.3) is recorded in
`tr3s3_check_run2_output.txt`.

RESULTS (first full run on the sealed predictions, seed 20260818, exit 0,
23.6 s; ZERO amendments; log `tr3s3_check_output.txt`, JSON
`tr3s3_check_results.json`; rerun identical mod timestamps):

    E2A  per-abscissa composed identity + support   0/2,241   GREEN
    E2B  side-read two-form as lists + j0 law       0/816     GREEN
    RI   kernel: s == 0, l == 0, eps1 == eps2 == 1  0/14,797  GREEN
         [run-1 scope, per pass-1 gap 3: level-2 split + l1, level-3
          split + l2, eps values only — the l0 + level-1-split legs
          were ADDED at REVISION 1 and are green at S8.3]
    RII  inner TR3-S1 on the digit + m0 = s2(A)     0/1,141   GREEN
    RIII t2 absorption t2 == s2(A) integral         0/1,141   GREEN
    RIV  recomposition from level-2 GMN data        0/1,141   GREEN
    Z2   off-line vanishing + own-weight cross-val  0/768     GREEN
    XV   fresh vs probe FGMN pipeline               0/816     GREEN
    W    prescribed-value end-to-end (noise/holes)  0/144     GREEN
    K    label ties at Phi3 (both = psi2)           0/24      GREEN
    O    matched-label ord transport                0/816     GREEN
    CERT external/integrity legs (incl. PARI)       0/24      GREEN
    METERS all fire: m0pos 85, j0pos 173, noff 902 (the honestly-computed
      off-line slots — Lemma Z2's E2A face), noisy 72, holes 64, wfail 0,
      ord histogram 0:744 1:38 2:34 (ord >= 1 present)
    TOTAL 23,869 samples, 0 violations — ALL GREEN.

A red on E2A/E2B falsifies S5; RI falsifies Lemma KER; RII falsifies Lemma
A3 (and accepted TR3-S1 at the substate); RIII the Def-2.19 collapse; RIV
the S5(ii) chain's recomposition; Z2 Lemma Z2; W the theorem against an
a-priori prescription; K the S6.2 label tie; O the S6.2 transport \[REV2, pass-3 gap 2: row O's
violation branch is UNREACHABLE given E2B's early-return + list equality
(equal side-read lists force equal ords), so O does NOT independently
falsify the S6.2 transport — only its ord-histogram meters are live; the
transport's falsification lives in E2B itself\].

### S8.3 REVISION 1 rerun — the RI l0 + level-1 eq-(12) legs (pass-1 gap 3)

The run-1 RI implementation was NARROWER than its sealed preregistration
("s == 0; l0 == l1 == l2 == 0; every consumed eps1/eps2 value == 1"): it
checked the level-2 split + l1 (`R2w_at`), the level-3 split + l2
(`fresh_R3`), and the eps values — never l0, never the level-1 eq-(12)
split (Lemma KER's level-1 clause was carried by the proof alone). ADDED
at REVISION 1, preregistered in the script's REVISION 1 ADDENDUM header
block BEFORE the rerun: per nonzero digit in `R2w_at`, the level-1
eq-(12) split of the slot weight β_m at (e₀, h₀) — predicted s = 0 — and
the l0 == 0 check; no randomness consumed by the addition; predicted 0
new violations, with ONLY the RI sample count and the TOTAL allowed to
move (both up by the same amount), any other deviation = STOP.

RERUN (same seed 20260818, exit 0, 24.2 s; output
`tr3s3_check_run2_output.txt`, JSON `tr3s3_check_results.json`
regenerated): ALL GREEN — RI 0/21,741 (run 1: 0/14,797; +6,944 = the
nonzero level-1 digit reads), TOTAL 30,813 (+6,944, the SAME delta, as
preregistered). `diff` against `tr3s3_check_output.txt`: deviations
ONLY at the four timing lines, the RI sample count, and the TOTAL line —
every other row, meter, and histogram value byte-identical. The RI row
now tests the entire stated prediction; Lemma KER's every clause has a
falsifier face.

## S9. Honesty ledger and attack surface for the hostile pass

1. **(H-v3) is the largest exposure**: the only site where order-3 GMN
   semantics enters beyond the level-generic recursion. Attack: re-extract
   Def 2.5's H_{r−1} display and check the S1 sketch; check no other
   consumer of v₃ hides in Defs 2.19–2.21's r = 3 instance (the component
   N₃⁻ and ordinates both ride u_i). [This attack was EXECUTED as the
   dedicated unit `HV3_DISCHARGE_2026-08-08.md` and the exposure CLOSED —
   DISCHARGED at HV3 acceptance fa2180e; the scope is UNCONDITIONAL ON
   PRINT per HV3-THM's four standing conditions]
2. **The Lemma A3 citation discipline**: TR3-S1 is applied to the SUBSTATE —
   verify the truncation argument (S2: chain truncates, read data are the
   state's own, e₀ = 1 holds) and that TR3-S1's statement is genuinely total
   in f (no degree bound) with j₀ = s₀ at e₁ = 1. Also verify the inner
   operator match: FGMN's R_{2,γ_j} inside Def 3.16-at-i=3 is the SAME
   operator TR3-S1's theorem reads (same Def 3.16 at i = 2, same weight
   γ_j = w₂(A) at on-line slots).
3. **Lemma Z2's negative-weight and zero-digit corners**: all routed through
   "reduction of an all-positive-valuation polynomial is 0"; check the
   domain direction (β < w₂(A) is INSIDE P_β(µ₂), not outside).
4. **The monicity upgrades** (S2(iii) at Φ₂; S6.2 at Φ₃): both need lawful
   monic full-degree lifts; a partial-degree key degrades = to ∼ and the
   label ties to associates (ord conclusions survive).
5. **Index dictionary at depth 3**: (e₃,h₃)^F = (e₂,h₂)^{proj}, γ₃ = the
   µ₃(φ₃) datum, GMN's t₂ uses GMN-level-2 = project-read-ν₁ data. An
   off-by-one here shifts every slot weight; cross-checked by XV/RII/RIII
   and the probe's V-rows.
6. **The transcription scope of ǫ₂/Def-3.16-at-i=3/Def-3.18-at-i=3** (S7 bullet; the named
   open pin (PIN-GEN-i), S1): generic-i
   print + probe-scope external discrimination only; on-stratum the algebra
   consumes only ǫ ≡ 1. The owed leg is the fresh `pdftotext -layout`
   re-extraction displayed at (PIN-GEN-i). [The owed leg DISCHARGED
   separately at 303b11e — the FGMN re-derivation unit's §S7 rider; the pin
   stays displayed (S7 bullet)]
7. Everything consumed from the parents is at their ACCEPTED states (TR3-S1
   REV 2 two-clean; TR3-S2 REVISION 1 two-clean; GRB leaf REV 14); nothing
   from the superseded S3.2 law or the retired artifact ǫ.

— TR3-S3 composed 2026-08-08; the falsifier run record and any amendments
follow in S8.2; hostile passes NOT yet run — the 2-clean acceptance bar
starts under the orchestrator.

---

## REVISION 1 (2026-08-08) — response to hostile pass 1 (GAPS-ONLY); acceptance counter stays 0/2

Pass 1 (`lean/notes/openmath/TR3S3_pass1_report.md`): VERDICT GAPS-ONLY —
0 critical / 4 gaps; "no algebraic contradiction found. The finite identity
is sound conditional on the displayed recursion, (H-v3), and the normalized
Bézout convention." Verified in full by the same pass and UNTOUCHED here:
Lemma KER, Lemma A3 (incl. the truncation and citation discipline), Lemma
Z2 (corners + domain direction), the S5 on-line equivalence / evaluation
square / nonvanishing / support equality / y^s division, the σ₂
construction's Cor-5.6(2) index discipline ((KP_i) only at Φ₃), the
monicity upgrades and matched-label ord transport, all scope fences, all
count reconciliations. The four gaps and their REVISION 1 fixes:

1. **GAP — "(H-v3) does not support the unconditional consumer status"**
   ("the unconditional 'proved' and 'consumers … served' language exceeds
   what was established; only the result conditional on (H-v3) is
   available"). FIXED: every consumer display for TR-3′-GEN at order 3 now
   reads proved/served **CONDITIONAL ON (H-v3)**, with the exact content
   stated at each site: the identity itself stands as displayed; what
   (H-v3) carries is its attribution to the actual GMN order-3 operator.
   Sites edited: the three summary-table rows (TR-3′-GEN order-3,
   TR-3-ORD-X, consumers); S6.1 heading + blockquote + the GD3-FULL/OL-B
   clause; the S6.2 conclusion + consumer clause (now (H-v3) + (KP_i) +
   [T]); S6.3.
2. **GAP — "the generic-index source transcription remains unverified"**
   (corrected Def 3.15 at level 2 + Def 3.16 at level 3 rest on generic-i
   prints + computational discrimination; neither source transcription
   proved). FIXED as a display: the named OPEN ATTRIBUTION PIN
   **(PIN-GEN-i)** (S1), stating the owed non-textual leg — a fresh
   `pdftotext -layout` re-extraction of the FGMN PDF at Defs 3.15/3.16/3.18,
   decorrelated from the corrupted shared extraction, under a
   provenance-pinned copy of the PDF (the repo's reference set carries
   none). Discharging it is a dedicated source-verification unit; here the
   leg is displayed as OWED, not attempted. S7's transcription-scope
   bullet and S9 item 6 now name the pin.
3. **GAP — "RI's preregistered claim is broader than its implementation"**
   (the script never checked l0 nor the level-1 eq-(12) split; 0/14,797
   did not test the entire stated RI prediction). FIXED in the falsifier:
   both legs added inside `R2w_at`, preregistered in the script's
   REVISION 1 ADDENDUM header block BEFORE the rerun; rerun ALL GREEN, RI
   0/21,741, and the diff against the run-1 record deviates ONLY at the
   preregistered perimeter (timing lines, RI count, TOTAL) — S8.3.
4. **GAP — "temporal preregistration and rerun provenance are asserted
   but not evidenced by the supplied record"**. FIXED: the seal commits
   are cited in S8.2 — preregistration **b4a142b** (note + sealed script,
   pre-run) + first-run record **482150b**; the REVISION 1 rerun output is
   `tr3s3_check_run2_output.txt`.

The verified algebraic perimeter (S2–S5, the lemma proofs, the scope
fences) is textually untouched by this revision. The 2-clean acceptance
bar stays at counter 0/2; pass 2 next.

---

## REVISION 2 (orchestrator-applied, per pass 3)

Pass 3 (Fable, independence-fenced; TR3S3_pass3_report.md): GAPS-ONLY 0C/2G. Fixes:
(1) PIN-GEN-i now includes Def 3.18-at-i=3 (same generic-index provenance — the pin's
own discipline applied to the one omitted operator); (2) S8.2's row-O description
corrected — its violation branch is unreachable (E2B early-return + list equality), so
only its histogram meters are live; the S6.2-transport falsification is E2B's. The
pass-3 verifier additionally ran an INDEPENDENT from-note implementation (off-roster
tower, own field models): 93 samples / 189 slot checks / 0 failures — recorded here as
an external leg. Counter stays 0/2 (the pass-2 clean predates these edits); passes 4+5
decide.

## REVISION 3 (orchestrator-applied, per pass 4)

Pass 4 (Codex; TR3S3_pass4_report.md): GAPS-ONLY 0C/1G — Def 3.18 had joined PIN-GEN-i
(REV2) but not the pin's owed discharge recipe nor the S7/S9.6 summaries. Fixed at all
four sites in one sweep (grep-verified: every operator-set listing now reads Defs
3.15/3.16/3.18 or names all three). Pass 4 also confirmed the row-O correction against
the script and re-derived the full chain clean. Counter stays 0/2; passes 5+6 decide.

## REVISION 4 (orchestrator-applied, per pass 6)

Pass 6 (Fable, independence-fenced; TR3S3_pass6_report.md, df28124): GAPS-ONLY 0C/1G —
the PIN-GEN-i scope sentence omitted the displayed Def-3.18 shape (fifth site of the
pass-4 species; display-level, operationally inert). One-token-class fix applied at the
S1 sentence. The pass-6 verifier's independent from-note implementation (off-roster
p = 5, 3 towers incl. degeneracy corners, 1,564 checks, 0 failures) recorded as a second
external leg. Counter 0/2; passes 7+8 decide.

---

## ACCEPTANCE RECORD (orchestrator, 2026-08-08)

ACCEPTED at REVISION 4: pass 7 (Codex) CLEAN 0C/0G + pass 8 (fresh Fable,
independence-fenced) CLEAN 0C/0G — the 2-clean model-diverse bar met, counter 2/2.
Eight-pass arc: p2 Codex CLEAN (pre-revision) → p3 Fable 0C/2G → REV2 → p4 Codex 0C/1G
→ REV3 → p5 Codex CLEAN → p6 Fable 0C/1G (display-inert) → REV4 → p7 Codex CLEAN + p8
Fable CLEAN. THREE independent from-note implementations on record (p3: 93 checks; p6:
1,564; p8: 821 at p = 7/11 — all 0 failures). With accepted TR3-S1 + TR3-S2:
TR-3'-GEN proved on {all order-2} ∪ {order-3 all-e_j=1}, CONDITIONAL ON (H-v3)
(displayed; carries the GMN-operator attribution only) with PIN-GEN-i the displayed
attribution pin. Ledger entry of even date.

**[POST-ACCEPTANCE BRACKET (applied at ROOT r32; ADDITIVE — everything
above stands byte-unchanged and this note's acceptance counters are NOT
reset): (H-v3) DISCHARGED at HV3 acceptance fa2180e — the scope is
UNCONDITIONAL ON PRINT per HV3-THM's four standing conditions.** The
dedicated discharge unit `HV3_DISCHARGE_2026-08-08.md` proved **HV3-THM**:
v₃(P) = w₂(P) for every nonzero P ∈ O[x] at e₀ = e₁ = 1 (**e₂ FREE** —
this note's stratum contained), given ONLY (i) the standing
print-attribution baseline for the sha256-pinned `HigherNewton.tex`
generic displays (Def 2.5, the H_{r−1} mapping, Prop 2.6, Prop 2.7(3)(4);
the r = 2 instantiation reproduces the accepted TR3-S1 pin VERBATIM),
(ii) accepted TR3-S1, (iii) the standing (H-CHAIN)-at-depth-3 + Lemma
RESCALE consumptions, (iv) the standing projective-dictionary
consumptions. Acceptance arc: discharge f90d68a → passes 1/2 Codex 0C/2G
each → pass 3 Fable 0C/2G + fresh 615/615 two-way falsifier → REVISION 3
→ **pass 4 (Codex) CLEAN 0C/0G + pass 5 (fresh Fable, independence-fenced,
b2e5b20) CLEAN 0C/0G** — 2-clean model-diverse, with the pass-5 verifier's
independent arXiv re-fetch (byte-identical), a fresh 1,655-check two-way
falsifier at off-roster p ∈ {7,17}, and an ADVERSARIAL CONTROL (type-hood
deleted → exactly the step-5 leg breaks). CONSEQUENCE: with accepted
TR3-S1/S2/S3, **TR-3′-GEN is PRINT-UNCONDITIONAL at {all order-2} ∪
{order-3 all-e_j = 1}** — the trilogy's only non-print hypothesis is gone;
nothing becomes machine-checked or attribution-free that wasn't.
(PIN-GEN-i) stays DISPLAYED — its owed leg was discharged SEPARATELY at
303b11e (the FGMN re-derivation unit's §S7 rider). The e_j ≥ 2 strata at
orders ≥ 3 and all orders ≥ 4 remain OPEN exactly as fenced above —
HV3-THM requires e₁ = 1 and does NOT reach the (TWIST-3)/TR3-S4 regime.
The dated DISCHARGED brackets at the S0 hypothesis display, the three
summary-table rows, the S1 sketch bullet, S6.1/S6.2/S6.3, the S7 (H-v3)
item, S9 item 1, and this record are THIS bracket's site-level
executions.]**
