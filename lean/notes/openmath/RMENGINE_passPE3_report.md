# RMENGINE pass PE3 report (Fable, round 3 — THE ACCEPTANCE ATTEMPT) — 2026-08-08 campaign (wallclock 2026-08-04)

**Pass:** RMENGINE-PE3, hostile content verification, fresh context. Verify only; fix nothing.
**Object:** `lean/notes/openmath/RMENGINE_2026-08-08.md` at commit **423b399** (confirmed: `git log -1 --format=%h -- <file>` → 423b399; 1288 lines). The r2 text.
**Bar:** acceptance = this pass AND the concurrent Codex PC3 both CLEAN (0 critical, 0 gaps; notes do not block).
**Charge:** (1) r2-remedy discharge — rows C23–C28 true + basis-accurate, the corrected L-NORM citation faithful to ITERLAW3_PROOF S4.1 read at source, the hygiene sentence vs `git diff bf18bfe..423b399`, the r2 byte-identity claim vs `git diff 8835d20 423b399`; (2) independent whitespace-normalized only/never/nothing/free/self-contained/automatic census, table coverage; (3) full spine re-derivation (headline/T-scope sites, L-NORM (a)–(c), S2.2 tree + L-EXACT, S3 L-GRID/L-ZMULT/L-LINE, S4.1 both branches + necessity iff + T-TERM, S4.2 inspection census re-grepped, S5 DEV-(m)/L-VNZ/RM-GEN, S6 grades, S7 COR-A, S8–S11 coherence); (4) machine spot-check — re-run `verification/openmath/rmengine_pe2_ext.py` read-only.

---

## 1. R2-remedy discharge (charge 1)

### 1.1 The r2 byte-identity claim — VERIFIED (with one two-word caveat, note-grade)

`git diff 8835d20 423b399 -- lean/notes/openmath/RMENGINE_2026-08-08.md` = 124 lines, five hunks, fully read. Hunks: (i) head bracket R6 line + new REPAIR ROUND 2 bracket (pre-S0); (ii) the S2.1 L-NORM citation sentence; (iii) the S8 claims-table preamble bracket + C21/C22 tag/wording edits + new rows C23–C28; (iv) the round-1 repair-record hygiene sentence rewrite; (v) the new round-2 repair record + tail counter. **No S2–S7 displayed equation, lemma statement, or proof step changed other than the L-NORM citation sentence** — the claim "every S2–S7 display is byte-identical to 8835d20 except the L-NORM citation parenthetical" is TRUE in substance. Caveat (see N-1): the r2 edit also changed two connective words *outside* the parenthesis — "applies to" → "applies **in particular** to" — in the same sentence; the exception clause under-describes the changed region by those two words. No math content changed: confirmed.

### 1.2 The corrected L-NORM citation — FAITHFUL (verified at source)

`ITERLAW3_PROOF_2026-08-08.md` lines 386–391, read directly:

> **Lemma DIGIT-SPLIT.** For α, β ∈ O[x] of degree < d₀: αβ = lift(q(α)q(β)) + πX + Φ₀Y with deg X < d₀, deg Y < d₀, where q: O[x] → K₀ = O[x]/(π, Φ₀) is the reduction ring-homomorphism. *Proof.* Divide: αβ = YΦ₀ + ρ ... Apply q ... ∎

Stated for ALL α, β of degree < d₀ — **no primitivity hypothesis**, exactly as the r2 parenthetical says; the proof is division by the monic Φ₀ plus the reduction homomorphism — **engine-free**, as claimed. The r2 gloss "primitivity is where the L-child is guaranteed nonzero, not a validity condition" is correct: q(α)q(β) ≠ 0 needs α, β primitive (nonzero reduction), validity does not. "Applies in particular to the primitive product b̂ĉ" is the correct specialization logic. DISCHARGED.

### 1.3 Rows C23–C27 — claims TRUE against the body; C26's grep key is DEAD (finding G-1)

Machine check (whitespace-normalized full text; counts = body hits + the table row's own occurrence):

    C23 "NO leg other than L-MU consumed e_i ≥ 2"      → 2 (S4.2 body + table)  OK
    C24 "PROMOTION-FREE and junk-free: carry depth 0"  → 2 (S3.2 body + table)  OK
    C25 "L-EXACT is scope-free"                        → 2 (S4.2 body + table)  OK
        "(L-LINE(i), scope-free)"                      → 2 (S6.2 body + table)  OK
    C26 "proved there, engine-free"                    → 1 (TABLE ROW ONLY — no body site)
    C27 "valuation + division arithmetic only"         → 2 · "needs only h ≥ 1" → 2 · "gcd(e_j, h_j) = 1 only" → 2  OK
    C28 "the only statement changes are"               → 1 case-sensitive (table only); body has "The only ..." (sentence-initial after the r2 rewrite)

C26: the r2 repair of PE2-3 rewrote the S2.1 citation to "stated and proved there for ALL α, β ... engine-free", and the S1 manifest site reads "proved, engine-free" (no "there") — so the key "proved there, engine-free", added at r2, matches NOTHING in the body of the committed text. The row's CLAIM (DIGIT-SPLIT's anti-circularity — [IL3]† S4.1 proved without this note's engine) is TRUE (verified at source, §1.2), but the table's charter is "grep-stable wording ... machine-censusable against this text as committed", and this key is not grep-stable: a machine census against the committed text fails row C26. Same mechanism for C28's case: the key quotes the r1-era lowercase "the only statement changes are", while the r2 hygiene rewrite made the phrase sentence-initial ("The only ..."); the key survives only case-insensitively (the charter promises whitespace normalization, not case normalization). Both keys were broken by the very r2 edits that landed them. → Finding **G-1** (gap, documentation layer).

Claim-truth verification for the surviving rows (each re-derived, not trusted):
* **C23** TRUE — my own e-bound consumer grep + per-leg re-derivation in §3.5 below (independent of PE2's).
* **C24** TRUE — re-derived: by L-MONO every tree edge has Δline ≥ 0 with promotions rising by h_{i+1}∏e_t > 0 (L-LINE(i)) and junk children by ≥ E_m resp. Γ₀ > 0 (L-LINE(iii)); initial product lines ≥ λ + γ_{m+1} = the target; hence any cell ON the target line has only line-preserving (= correction-main) edges in its ancestry: no promotion, no junk. Carry depth 0.
* **C25** TRUE — L-EXACT is an exact-algebra identity conditional only on tree finiteness (its statement says "whenever the tree ... is finite (S4)": finiteness is a HYPOTHESIS there, discharged by T-TERM at consumption; no e-bound in the lemma itself). L-LINE(i) is the weight-recursion computation, no e-bound.
* **C26** claim TRUE (key dead — G-1).
* **C27** TRUE with a hyper-literal caveat recorded in §3.5: "valuation + division arithmetic only" glosses S2.1's mechanisms; L-CELL(ii)'s proof also consumes READ-ADD-(m−1), an [ILN]† class-level fact whose e ≥ 1-safety the census covers under its S5.1 bullet — the census CONCLUSION (no e_i ≥ 2 consumption) is unaffected. Nano; no separate finding.

### 1.4 Row C28 + the round-2 bracket: the "verified by both against the actual git diff" claim is FALSE for PC2 (finding G-2)

C28's basis column: "both round-2 passes verified against `git diff bf18bfe 8835d20`". The round-2 head bracket (same commit) says: "the no-weakening claim was verified by both against the actual git diff." But PC2's own report (`RMENGINE_passPC2_report.md`, item 7, verbatim):

> **NOTE — execution unavailable.** I attempted only a read-only revision inspection, but the local command launcher failed because `bubblewrap` is unavailable. Per the content-only role, I did not attempt an alternative execution route; diagnostics and hashes were treated solely as stated evidence.

PC2 could not run git; its check of the hygiene claim was against the composed-text passages quoted inside the appendixed round-1 reports (its charge appendices: repaired note @8835d20, both round-1 reports, [IL3] S4 slice — no diff, no composed text). PE2 did run the actual diff (`RMENGINE_passPE2_report.md` §4: "git diff bf18bfe 8835d20 on the note (511+/202−), all grade-bearing lines extracted"). So the C28 basis column and the round-2 bracket over-state PC2's verification provenance. The underlying CLAIM of C28 (r1 statement-diff census: T-scope/T-class strengthening + L-LEX regrade, nothing else) is TRUE — I re-verified it myself against the actual diff (§1.5). → Finding **G-2** (gap: basis-column/bracket provenance inaccuracy; the charge for this pass explicitly demands "basis column accurate").

### 1.5 The hygiene sentence vs `git diff bf18bfe..423b399` — ACCURATE

My own extraction of every grade-bearing/statement-bearing line from `git diff bf18bfe 8835d20` (511+/202−; r2 adds no math change per §1.1): T-scope/T-class definitions widened (composed (e₀ ≥ 2 ∨ d₀ = 1) ∧ ∀e_i ≥ 2 → amended d₀ = 1 ∨ (e₀ ≥ 2 ∧ ∀e_i ≥ 2), a strict superset — strengthening) at title, headline, S4.1/S4.2, S5.2, S7.1, S8 rows 4/6/7/8, S10, S11; L-MU statement gains branch (b) (strengthening); new lemmas L-NORM/L-GRID/L-VNZ ADDED (new rows 1a/2a/2b, additions not changes); row 5 L-LEX PROVED→REMARK (disclosed by name in the hygiene sentence: "the L-LEX regrade to remark (its content retained)" — content verbatim in S4.2, consumer covered by branch (b)); row 11 PROVED→11a/11b/11c with 11c HEURISTIC (the named regrade); row 12 gains the n ≥ 3 restriction + at-acceptance rider (the composed n = 1, 2 claim imposed a spurious T-condition — its removal is a strengthening, and the exact-union claim was corrected, not weakened); LAW-DCX grades identical both sides. "No established clause was weakened and no proof content was lost": TRUE — nothing previously PROVED-and-correctly-graded lost strength; the two grade movements (row 11c, row 5) are both named in the record. Sentence ACCURATE. (Nano, no finding: "one PROVED-labeled row was honestly REGRADED" counts row 11 only; row 5's L-LEX move is also a PROVED-labeled→REMARK move, but it is explicitly named as such in the immediately following sentence, so the record hides nothing.)

## 2. Independent keyword census (charge 2 — the recurring defect: NOT dead)

My own census: whitespace-normalized (all runs of whitespace → single space), case-insensitive, all six keywords over the full r2 text: **136 raw hits** (PE2's 112 was evidently case-sensitive/narrower; superset checked). Every hit read in context and classified. Out-of-scope classes (consistent with both prior passes): verdict tokens (GAPS-ONLY, READ-ONLY), quotes inside the byte-frozen [IL]†/[IL3]†/[ILN]† pastes, the C-table's own rows, machine-shadow measurement prose ("counter C-GRID never fired"), "whenever" false positives, and mathematical proof content ("nonzero τ's only", "the only j₀-resupply", "λ_{l−1} ∈ ℤ automatically", "exit contributes nothing to R₀", "d₀ ≥ 2 only" etc. — verified as math in §3, not census targets). In-scope dependency/consumption claims all mapped to rows C1–C28 (site-echoes accepted claim-unit-wise: L-EXACT's "deg Q ... automatic" ↔ C2; S1's "EXP-KIT ... self-contained" ↔ C7; L-LINE(ii)'s "NEVER absent" ↔ C18) — with the following EXCEPTIONS:

* **UNCOVERED (clean witness):** L-LEX's consumer-census claim "consumed by nothing" — three sites: S4.2 "The σ-observation stays on record as a remark (a valid tiebreak mechanism, consumed by nothing)" (body text), S8 row 5 grade cell "valid, consumed by nothing", S11 DAG "L-LEX ─→ (remark at r1; consumed by nothing)". This is EXACTLY the W-WIN genre, which the table enumerates TWICE (C5 "consumed by NOTHING" + C14 "nothing consumes it"). No C-row covers it. It was present in the r1 text (verified: `git show 8835d20` line 639 etc.), i.e. inside the text PE2's census ran over — so the r2 bracket's assertion that PE2's "complete in-scope absentee list is exactly C23–C27" is not true, and the charter's "every ... claim of this note, enumerated" remains unsatisfied. The claim itself is TRUE (verified: T-TERM's proof cites L-MU strict descent only; grep of all L-LEX sites shows remark/table/DAG/record occurrences only, no consumer) — zero chain impact.
* **Arguable residue (recorded, not separately counted):** (i) S7.1 "modulo nothing but the accepted [IL]†/[IL3]†/[ILN]† packages" (+ headline echo "(modulo the accepted ... packages)") — a conditionality census claim; substantively carried by S8 row 12's "PROVED given row 8 + [ILN]†" but not enumerated as a C-row. (ii) S4.2/S5.2 census-internal "nothing new" — C27's claim column ("the inspection census's own per-leg dependency claims") arguably embraces it; its key list does not name it. (iii) Head-declaration "full pastes recur only at operative consumption sites" — procedural formatting convention, treated out-of-scope by all three prior passes; concur.

→ Finding **G-3**. The defect is smaller each round (8 rows → 6 rows → 1 clean item + 2 arguables) but the exhaustiveness claim as re-pinned at r2 is still not literally true of the committed text.

## 3. Spine sweep (charge 3 — all re-derived independently; NO mathematical finding)

**3.1 Headline + T-scope algebra.** Amended T-scope(m) = d₀=1 ∨ (e₀≥2 ∧ ∀1≤i≤m−1: e_i≥2). OFF-complement (headline L69, fence box, S5.2(iv), S8 row 6): ¬T = d₀≥2 ∧ (e₀=1 ∨ ∃i: e_i=1) — exact complement ✓ (e's ≥ 1). Combined-scope algebra of S4.1: [branch (a) ∧ (e₀≥2 ∨ d₀=1)] ∪ branch (b) = [(∀i≥1 e_i≥2) ∧ (e₀≥2 ∨ d₀=1)] ∪ [d₀=1] = d₀=1 ∨ (e₀≥2 ∧ ∀e_i≥2) ✓ exactly. m=1: T-scope(1) = d₀=1 ∨ e₀≥2 = e₀≥2 ∨ (e₀=1∧d₀=1) = the [IL3] RM-1 scope; T-scope₀(1) coincides (empty ∀) ✓. All sites carry the amended form (headline, S4.1/S4.2, S5.1/S5.2, S7.1 T-class i≤n−2, S8 rows 4/6/7/8, S10, S11, D7 roster).

**3.2 L-NORM (a)–(c) re-derived.** b̂ĉ primitive parts, DIGIT-SPLIT (verified at source, §1.2) on b̂ĉ, restore π^w, w = w₀(b)+w₀(c) = w₀(bc) (Gauss valuation). (a) w₀(π^w lift(b̄c̄)) = w exactly AND L-child never zero: K₀ = k₀[x]/(ψ̄₀) a field (ψ̄₀ irreducible, class pin); a primitive nonzero poly of deg < d₀ = deg ψ̄₀ has nonzero image; product of nonzeros in a field ≠ 0 ✓. (b) π-junk w₀ ≥ w+1 ✓. (c) Y-junk w₀ ≥ w; at d₀=1: deg(b̂ĉ) ≤ 2d₀−2 = 0 < 1 forces Ŷ = 0 ✓.

**3.3 S2 tree + L-EXACT.** Reduced-cell degree telescope: Σ(P_l−1)degΦ_l = degΦ_m − d₀ ⟹ deg C ≤ degΦ_m − 1 ✓. (ID-1) = the standard lift rearranged, C_k^{(0)} = lift(ψ₀,k)π^{(g₀−k)h₀} ✓; (ID-(i+1)) from the canonical lift, consumed per manifest. L-EXACT: stepwise exact rewrite, R₀ = ΣR-leaves reduced ⟹ deg < Φ_m ⟹ L-CANON pins the canonical pair; window-free ✓. Exit-cofactor single-digit remark: E1-priority at i = m−1 forces j_l ≤ P_l−1 ∀l ≤ m−2 ⟹ S reduced at level m−1, cofactor a single Φ_{m−1}-digit, w_m = e_{m−1}w_{m−1}(S) + j′γ_m = its line exactly ✓ — carries (C-W_Q) unconditionally.

**3.4 S3.** L-GRID: λ_l = e_{l−1}λ_{l−1} + j_{l−1}γ_l ✓ (recursion inverted); truncation-line identification via Γ_t^{(l)} = e_{l−1}Γ_t^{(l−1)} (t<l−1), Γ_{l−1}^{(l)} = γ_l ✓; congruence s_l(λ_l) ≡ ℓ_{l−1}λ_l ≡ ℓ_{l−1}j_{l−1}h_{l−1} ≡ j_{l−1} mod e_{l−1} (γ_l ≡ h_{l−1}; ℓh ≡ 1) ⟹ t_l ∈ ℤ ✓. L-ZMULT: λ-chains add (linear, no floor) ✓; s_l(λ+λ′) = s_l(λ)+s_l(λ′)−δ_le_{l−1}, δ_l ∈ {0,1} (s ∈ [0,e−1]) ✓ L1; t_l^Σ = t_l+t_l′+δ_l ✓; EXP-KIT(b) ε_l(λ+λ′) = z_l^{−δ_l}ε_l(λ)ε_l(λ′): t-leg z^{+δ} cancels ε-leg z^{−δ} at l = 1..m−1; level m has t-leg, no ε-leg ⟹ sole survivor z_m^{ε̃}, ε̃ = δ_m ✓. L-LINE(i): γ_{i+2} − e_{i+1}P_iγ_{i+1} = h_{i+1} via w_{i+1}Φ_{i+1} = P_iγ_{i+1} ⟹ quantum h_{i+1}∏_{i+1<t<m}e_t > 0 ✓; exit drop P_{m−1}γ_m = w_mΦ_m ✓. (ii): scaling the realize on-line identity by ∏_{i≤t<m}e_t: E_i↦E_m, Γ_l^{(i)}↦Γ_l, (g_i−k)γ_{i+1}↦(P_i−e_ik)Γ_i (e_i(g_i−k) = P_i−e_ik) ⟹ Δline = 0 exactly, L-child w₀ exact by L-NORM(a) ✓. (iii): π-child +E_m, Y-child +Γ₀; i=0: Γ₀ = h₀E_m/e₀, Δline = E_m(g₀−k)h₀ − e₀(g₀−k)·h₀E_m/e₀ = 0 ✓. (iv) single nested digit exact ✓. L-MONO + line additivity under product formation (w₀ valuation) ⟹ initial lines ≥ λ+γ_{m+1}; on-line skeleton promotion- and junk-free (C24) ✓.

**3.5 S4.1 BOTH branches (branch (b) invariant re-derived), necessity iff, T-TERM, inspection census.**
*Branch (a):* interval (S_i/e_i, w_{i−1}P_{i−1}); nonempty ⟸ w_{i−1}P_{i−1}e_i ≥ 2w_{i−1}P_{i−1} > S_{i−1} + w_{i−1}ρ_{i−1} = S_i using (B_{i−1}) (w_{i−1}e_{i−1} > S_{i−1}, P_{i−1} ≥ e_{i−1}) and ρ_{i−1} ≤ P_{i−1} ✓; base (ρ₀/e₁, P₀) nonempty at e₁ ≥ 2 since ρ₀ ≤ P₀ ✓.
*Branch (b), re-derived from scratch:* d₀=1 ⟹ ρ₀ = P₀−1, ρ_l = P_l−1 all l. Invariant w_i > S_i. Any w_i ∈ (S_i, w_{i−1}P_{i−1}) gives (B_i) via w_ie_i ≥ w_i > S_i (e_i ≥ 1 suffices!) and (A_i) directly. Nonemptiness ⟺ w_{i−1}P_{i−1} > S_i = S_{i−1} + w_{i−1}(P_{i−1}−1) ⟺ w_{i−1} > S_{i−1} = the invariant one step down; base (S₁, w₀P₀) = (P₀−1, P₀) ≠ ∅ ✓, w₀ = 1 > S₀ = 0 ✓. Midpoint choice maintains the invariant; no e_i lower bound consumed ✓. Common denominator ⟹ μ well-founded ✓.
*Descent legs:* promotion Δμ < 0 by (A_{i+1}); exit −w_{m−1}P_{m−1}; correction i ≥ 1: consumed ≥ e_i at level i (k ≤ g_i−1), dumped ≤ ρ_l per level l < i (a_{d,l} ≤ P_l−1; +1 at l=0 on Y, absent at d₀=1 — ρ₀ accounts both cases) ⟹ Δμ < 0 by (B_i); level-1: Δj₀ ≤ −e₀ + [d₀≥2] ≤ −1 iff e₀ ≥ 2 ∨ d₀ = 1 ✓ (the DEV-1 corner reproduced, honestly fenced).
*Necessity cascade re-derived as an iff:* flat e_i = 1 (i ≥ 1) makes (B_i) read w_i > S_i; with (A_i): w_{i−1}P_{i−1} > S_{i−1} + w_{i−1}(P_{i−1}−1) ⟹ w_{i−1} > S_{i−1}; the same rearrangement cascades through (A_l) down to w₀P₀ > S₁ = w₀ρ₀ ⟹ ρ₀ < P₀ ⟺ d₀ = 1. Converse: branch (b). IFF ✓. The fence box's conclusion is correctly scoped to the L-MU family ("NO linear measure of the L-MU family") and termination is honestly OPEN, not disproved, on d₀ ≥ 2 flats ✓. Consistency display: (2e₁, P₀(e₁+1)) satisfies (A₁)/(B₁) ⟺ e₁ ≥ 2 ✓ re-checked.
*Inspection census (C23), my own grep + per-leg re-derivation:* every "≥ 2" site in the note grepped (they live in: T-scope displays, branch (a), the fence, d₀ ≥ 2 riders, L-CAP's ≤ 2, W-WIN's 2P bounds — none a hidden consumer). Per-leg: L-CELL/L-NORM (division + valuation, e-free); S2.2 inventory (consumed mass e_i(g_i−k) ≥ e_i ≥ 1); L-GRID/L-ZMULT at e = 1: s ≡ 0, δ = ⌊0/1⌋ = 0, t ∈ ℤ automatic, ε ≡ 1 — all displays hold degenerately ✓; L-LINE needs h ≥ 1 only ✓; DEV-(m) carrier = T-TERM (extended) + class-level facts (gcd pin only) ✓; RM-GEN adds nothing ✓. NO leg other than L-MU consumes e_i ≥ 2 — independently CONFIRMED. (Hyper-literal caveat: "valuation + division arithmetic only" as an S2.1 gloss — L-CELL(ii)'s proof also consumes READ-ADD-(m−1), whose e ≥ 1-safety the census covers under its S5.1 bullet; conclusion unaffected. Nano.)

**3.6 S5.** DEV-(m) (D1)–(D3): induction on μ; leaf = L-LINE(iv); (D2) exit leg via the single-digit remark ✓; (D3) three cases — above-line children read 0 (L-MONO + IH); off-grid-at-own-line EMPTY by L-GRID (converse-integrality appeal verified deleted) ✓; T-tele(1): λ-chain agrees at levels ≥ 1 above the change, t₁ shift k−g₀, sign − from (ID-1), Σ_kψ̄₀,kz₁^{k−g₀} = z₁^{−g₀}(ψ₀(z₁)−z₁^{g₀}) = −1 ⟹ collapse to res(b̄)𝔷 ✓. T-tele(i+1): Δλ_i = (P_i−e_ik)γ_{i+1}/e_i = (g_i−k)γ_{i+1} = β_k exactly ✓; t_{i+1} shift k−g_i ✓; levels > i+1 untouched ✓; child level-≤i data = product shape, L-ZMULT at level i gives z_i^{ε̃_k} with ε̃_k = ⌊(s_i(λ_i)+s_i(β_k))/e_{i−1}⌋ ✓; B-READ inside the k-sum legitimate (finite sums; C_k's cells on-line on-grid by realize) assembling τ_k; EPS-MULT ε_i(λ_i+β_k)z_i^{ε̃_k} = ε_i(λ_i)ε_i(β_k) and ε_i(β_k)τ_k = ψ_{i,k} (R3) ⟹ k-sum collapses via ψ_i(z_{i+1}) = 0 with the − sign ⟹ res(b̄)𝔷(G;β*) ✓. L-VNZ: full induction re-walked — deg f < deg Φ_m caps j ≤ P_{m−1}−1, slots t ∈ [0, g_{m−1}−1], min-achieving digit on-grid automatically (L-GRID congruence), slot weight = w_{m−1}(f_j), IH coefficient ≠ 0, K_{m−1}-polynomial in z_m of deg < [K_m:K_{m−1}] ≠ 0 ✓. RM-GEN all four clauses: (C-W_R) via (D2) + developments-add; (C-W_Q) via exit lines ≥ β* − w_mΦ_m; (C-EPS) = L1; (C-READ): junk initial children + above-line x-cells read 0, surviving (on-line x)×(D) L-children carry residue b̄_xc̄_D (L-NORM(a)) and 𝔷 = z_m^{ε̃}𝔷(x;λ)𝔷(D;γ_{m+1}) with ε̃ = ε′ UNIFORM (shared target pair), double sum factorizes to z_m^{ε′}·v·τ_m by B-READ both sides + (Ĉ3) ✓. SOUND — no finding.

**3.7 S6 grades.** LAW-DCX-(m+1) sealed-not-proved at every site (spot-checked all: head declaration, S1 pt 3, S6.1 STATUS + audit, S6.2, S8 row 10, S9 D5/D6 standing lines, S10, S11 DAG, C12) ✓. Specializations re-derived: m=1 (c₀=0, u₁=γ₂) = T₁ + s₁(γ₂) ≥ 2e₀ on TRACK = [IL3]'s proved sharp form ✓; m=2 = v2 verbatim with u₁ = (γ₃−s₂(γ₃)γ₂)/e₁ vs the head-paste's u₀ ✓. L-CAP re-proved: pool₁ ≤ 3e₀−3 ⟹ c₁ ≤ 2; pool_l ≤ 3e_{l−1}−1 ⟹ c_l ≤ 2; e=1 branch pool = c_{l−1} ✓. 11a/11b/11c split honest: crossing law from the child inventory alone (promotions the only upward mass carrier, corrections dump strictly below, exits leave — re-checked against S2.2) with the μ-citation T-scope-pinned ✓; pool identification + residue liveness honestly HEURISTIC/conjectural, consumed by nothing proved ✓.

**3.8 S7.** COR-A: ∧_{m=2}^{n−1} T-scope(m) = d₀=1 ∨ (e₀≥2 ∧ e_i≥2 ∀i≤n−2) = T-class(n) — distribution re-checked ✓; n=1,2 empty call family, accepted scopes verbatim, no T-condition ✓; at-acceptance rider coherent at all four surfaces; e_{n−1}-absence remark consistent (level-(n−1) engine reads levels ≤ n−2) ✓. COND-B stands conditional, not a corollary ✓.

**3.9 S8–S11 coherence.** S8 grade rows re-checked against body sites (rows 4/5/6/7/8/10/11a-c/12/13 all match). S9: all four md5 pins verified against the committed artifacts (runner c50c08e..., output d9118f5..., results JSON 1ce263df..., PE1 referee 103c1a9c...) ✓. S10 fences match the proved perimeter ✓. S11 DAG consistent (L-VNZ consumed by no theorem clause; L-LEX by nothing; W-WIN by nothing) ✓.

## 4. Machine section (charge 4)

* `verification/openmath/rmengine_pe2_ext.py` (committed referee, md5 574f87d89fae5794bede7e0b38121877 = PE2's pin, UNTOUCHED): re-run read-only, exit 0, 0.5 s. Output BYTE-EXACT vs committed `rmengine_pe2_ext_output.txt` (md5 663fcb11...) modulo the volatile elapsed line. Actuals reproduced: XA 67 fresh flat-tower engine calls, v=0 realized 0; XB(i) 1080 branch-(b) shapes 0 failures; XB(ii) 225 d₀≥2-flat shapes 0 feasible; XB(iii) 252 branch-(a) shapes 0 failures; all 12 XV verdict lines GREEN [0 violations].
* Grep-key machine census of rows C23–C28 (§1.3 table) — the instrument that surfaced G-1.
* No extension referee needed: every doubted claim was resolved by hand-derivation or the above; no `rmengine_pe3_ext.py` created. Nothing committed; no committed file mutated.

## 5. Findings (quote-and-classify)

**G-1 [JUSTIFICATION GAP — claims-table key accuracy; no chain impact].** Row C26's grep key, verbatim: `S1 + S2.1 "proved there, engine-free"`. Whitespace-normalized census of the committed r2 text: the string "proved there, engine-free" occurs EXACTLY ONCE — inside row C26 itself. The S1 site reads "proved, engine-free" (no "there"); the S2.1 site was rewritten by the r2 PE2-3 repair to "stated and proved there for ALL α, β of degree < d₀ with no primitivity hypothesis, engine-free". The table's charter — "grep-stable wording ... machine-censusable against this text as committed" — fails for C26: a machine census returns zero body matches. Same mechanism, lesser: C28's key "the only statement changes are …" survives only case-insensitively (the r2 hygiene rewrite made the body phrase sentence-initial, "The only ..."; the charter promises whitespace normalization, not case normalization). Both keys were landed and broken in the same r2 commit. The claims of both rows are TRUE (verified §1.2/§1.5).

**G-2 [JUSTIFICATION GAP — false verification-provenance statement; no chain impact].** Row C28 basis column, verbatim: "both round-2 passes verified against `git diff bf18bfe 8835d20`"; round-2 head bracket, verbatim: "the no-weakening claim was verified by both against the actual git diff." PC2's own report (item 7): "the local command launcher failed because `bubblewrap` is unavailable ... diagnostics and hashes were treated solely as stated evidence" — PC2 never executed git and its charge appendices contained neither the diff nor the composed text; its hygiene-claim check ran against passages quoted in the round-1 reports. "Verified by both against the actual git diff" is therefore false as to PC2. The underlying hygiene claim is TRUE (verified against the actual diff by PE2 and independently by this pass, §1.5).

**G-3 [JUSTIFICATION GAP — charter completeness, round-3 residue of the recurring defect; no chain impact].** Charter, verbatim: "every 'only/never/nothing/free/self-contained/automatic' claim of this note, enumerated ... [r2 ...]: the r1 census was STILL short — rows C23–C28 added; ... whose complete in-scope absentee list is exactly C23–C27". My independent 136-hit census (§2) finds an in-scope absentee: L-LEX's "consumed by nothing" (S4.2 body remark + S8 row 5 + S11 DAG; present in the r1 text PE2 censused) — genre-identical to W-WIN's consumer-census claim, which the table enumerates twice (C5, C14). The "complete in-scope absentee list" assertion and the charter's exhaustiveness promise are still not true of the committed text. The uncovered claim is itself TRUE (no consumer of L-LEX exists; verified). Arguable further residue recorded in §2 (S7.1 "modulo nothing but ..."; census-internal "nothing new").

**N-1 [NOTE — exception-clause precision].** Round-2 record, verbatim: "every S2–S7 display is byte-identical to 8835d20 except the L-NORM citation parenthetical." The r2 edit also changed two connective words OUTSIDE the parenthesis in the same sentence ("applies to" → "applies in particular to"). No math content changed; the exception clause under-describes the edit by two words.

**N-2 [NOTE — census gloss].** S4.2 census leg "S2.1 (... L-CELL, L-NORM: valuation + division arithmetic only ...)": L-CELL(ii)'s proof also consumes READ-ADD-(m−1) ([ILN]† class-level, e ≥ 1-safe — covered by the census's own S5.1 bullet). The census CONCLUSION (C23) is unaffected; hyper-literal reading only.

## 6. Verdict block

The MATHEMATICS survives this pass completely: every spine item of charge 3 re-derived independently — L-NORM (a)–(c), the tree + L-EXACT + the exit-cofactor remark, L-GRID/L-ZMULT/L-LINE quanta, BOTH L-MU branches (branch (b)'s invariant re-derived from scratch), the necessity cascade as an iff, T-TERM, the S4.2 inspection census (my own grep + per-leg re-derivation), DEV-(m) with both telescope families, L-VNZ, all four RM-GEN clauses, the honest S6 grade split, COR-A's exact-union algebra, S8–S11 coherence — ZERO mathematical findings, and the committed referee reproduces byte-exact with all-GREEN actuals. Every r2 remedy's mathematical content discharges: the L-NORM citation is now faithful to the upstream lemma read at source; the hygiene sentence is accurate against the actual diffs; rows C23–C27's claims are true against the body. What does NOT survive is the r2 documentation layer itself, in three places: one dead grep key landed-and-broken in the same commit (C26; C28 case-marginal), one false verification-provenance statement (C28 basis + round-2 bracket: PC2 could not and did not run the git diff it is credited with), and a charter exhaustiveness assertion ("complete in-scope absentee list") that my independent census refutes with a clean same-genre witness (L-LEX's "consumed by nothing"). All three are documentation-layer with zero chain impact, but each is a false-as-stated claim of the committed text, and by this arc's own consistent grading precedent (PC2-g1, PC2-g2, PE2-1) each classifies as a JUSTIFICATION GAP, not a note. The bar is CLEAN; this is not CLEAN.

RMENGINE-PE3 FINDINGS: 0 critical, 3 gaps
VERDICT: GAPS-ONLY
