# TERMINALPOLYSEM k′=0 — unit record (2026-08-08)

Unit: synthesis pass-4 F2(b) / pass-5 F6 item 8 ("TerminalPolySem k′=0" —
dropped twice from queue tails, owed to the small-units dispatch).  Charter
(pass-4 §F2(b) verbatim): "**TerminalPolySem (H6)** — no supplier at HEAD; its
k′ = 0 face may be derivable from the landed emission specs
(`terminalDatumD_slope_spec`/`ReadThroughIota`). Attempt-grade."  Artifact:
`lean/LeanUrat/Scaffold/DictIII/CU2tKp0.lean` (NEW file, NEW declarations only;
no existing statement touched; green; all six audited decls Lean-core).

## The face, stated precisely

`TerminalPolySem f H D` (the III-S4-repair row, `DictIII/CU2t.lean`) reads the
side list at index `H.nodes.length`; `ReadThroughIota f H D` (H6-A6b) pins
index 0 corpus-real (= `principalData f`) and deeper indices only through
`OL1 (Theta H)` grants.  So "k′ = 0" splits into two inequivalent keyings, and
the unit decided BOTH:

* **𝐇°-keyed** (`H.nodes = []`, the HDISCHARGE_H6 displayed k′=0 seam; terminal
  index = 0 = the corpus-real level).  FACE = `terminalPolySem_kp0`:
  `H.nodes = []` + `ReadThroughIota` + `f.coeff 0 ≠ 0` + the two numerical legs
  on `principalData f` (nondegeneracy `1 ≤ e ∧ 1 ≤ ℓ`; length tie
  `Σ e·ℓ = j₀` with `X^{j₀} ∥ f̄`, `1 ≤ j₀`) ⟹ `TerminalPolySem f H D`
  (witness key φ = X).  **PROVED** — consumes ONLY A6b clauses (i)/(ii), the
  CUC "unconditional at k′ = 0" bracket; clause (iii)/OL-1 untouched.
* **terminal-appended** (the CU2t consumption keying: DecHen terminal-first,
  `H.nodes.length = 1`, terminal index = 1).  **REFUTED as underivable** —
  compiled countermodel (below): `ReadThroughIota` leaves index 1 unconstrained
  on terminal-first histories (`(Theta H).slopes = []` ⇒ no OL1 grants — the
  III-A3 terminal-blindness seam family).

## Instance-test verdicts (TEST BEFORE PROVING, the S4Refute precedent)

1. **POSITIVE — face TRUE at the instance.** p = 2, f = X + 2, `Hkp0`
   (= `S4Refute.H0` with `nodes := []`), D = the REAL corpus read
   `gmnDataOrder1` (ι-certified by the landed A6b positive gate).  Every leg
   concrete (`j₀ = 1`; the one side `(e,ℓ) = (1,1)` from the compiled
   `principalData_X_add_two`); the face FIRES:
   `Kp0Gate.terminalPolySem_kp0_gate_pos` — the FIRST real-read
   (ReadThroughIota-certified) `TerminalPolySem` witness at HEAD
   (`S4SemGate.sem1`'s `D1` was hand-built data).
2. **Saturation necessary.** f = X + 1 (undecided seam, unit constant term):
   `Kp0GateA.readThroughIota_unsat` HOLDS on the empty datum (the real read of
   X + 1 is empty — flat chord, `Order0Hull` γ1 gate) yet
   `Kp0GateA.not_terminalPolySem_unsat`.  The face cannot drop `p ∣ f(0)`.
3. **The −∞ leg is NOT supplied.** f = X (`coeff 0 = 0`): a junk slope-−∞ side
   with `e = 0` passes certification (`Kp0GateB.readThroughIota_neginf` — A6b
   clause (ii) constrains a −∞ entry only via `f(0) = 0`) yet kills
   nondegeneracy (`Kp0GateB.not_terminalPolySem_neginf`).  The face must carry
   `f.coeff 0 ≠ 0`; the −∞ corner stays the separately-priced TerminalPolySem
   HONESTY residue.  (New helper `npSides_of_card_le_one`: ≤ 1 support dot ⇒
   no sides — pure list bookkeeping, no hull-height evaluation.)
4. **The CU2t-keyed obstruction (the naive charter refuted).** On the
   DEC-decided terminal-first `S4Refute.H0` (`hdec_term` compiled), the REAL
   ι-certified read of X + 2 FAILS the row:
   `Kp0GateC.not_terminalPolySem_terminal_indexed`.  A supplier at the CU2t
   consumption index needs new content tying index k′+1-of-full-history to the
   continuing part's read — not owned here.

## Verdict

The charter's literal claim ("derivable from the landed emission specs") is
**REFUTED, compiled** (gates 2–4).  The honest salvage is **PROVED**: on
𝐇°-keyed k′=0 seams with the saturation + finite-slope perimeter, the face
reduces via `terminalPolySem_kp0` to **`BasePolygonLengthLaw p`** — a NAMED
open numerical row ([M]-discipline, like `Lit3Hensel`/`OL1`; per-f corollary
`terminalPolySem_kp0_of_law`): the OM base length law ℓ(N⁻(f)) = ord_X(f̄) +
side nondegeneracy, for monic f with `f(0) ≠ 0`, `p ∣ f(0)`.  This row is
H-free, D-free, seam-free — pure Newton-polygon geometry.  NOT provable at
HEAD: the corpus has only the FLAT hull machinery (`Order0Hull`); the general
negative-prefix geometry (adjacent-slope monotonicity from the proved
`npHeight_convexOn`, prefix telescoping, endpoint pins, per-side `e ∣ length`)
is exactly the recorded OL5-adjacent "convexity work" gap — the next unit's
target, proof route displayed in the row's docstring.
`terminalDatumD_slope_spec` was NOT consumed (it ties slope reads, not
side-list mass).

## Ledger

* Build: `lake env lean LeanUrat/Scaffold/DictIII/CU2tKp0.lean` GREEN, zero
  warnings; footprints all `[propext, Classical.choice, Quot.sound]`
  (`terminalPolySem_kp0`, `terminalPolySem_kp0_of_law`,
  `Kp0Gate.terminalPolySem_kp0_gate_pos`, `Kp0GateA.not_terminalPolySem_unsat`,
  `Kp0GateB.not_terminalPolySem_neginf`,
  `Kp0GateC.not_terminalPolySem_terminal_indexed`).
* Rebuild note: `CU2t.olean` was stale at unit start (predated the 2026-08-02
  S4 repair); `lake build LeanUrat.Scaffold.DictIII.CU2tKp0` refreshed the
  chain.
* New declarations (complete): `BasePolygonLengthLaw`, `terminalPolySem_kp0`,
  `terminalPolySem_kp0_of_law`, `Hkp0`, `theta_Hkp0_slopes`; `Kp0Gate.{fkp0,
  Dkp0, readThroughIota_kp0, principalData_fkp0, map_fkp0, sideDatum_fkp0,
  terminalPolySem_kp0_gate_pos}`; `Kp0GateA.{fA, valSupport_fA,
  principalData_fA, DA, readThroughIota_unsat, not_terminalPolySem_unsat}`;
  `Kp0GateB.{npSides_of_card_le_one, fB, valSupport_fB, principalData_fB,
  junkNegInfty, DB, readThroughIota_neginf, not_terminalPolySem_neginf}`;
  `Kp0GateC.{DC, readThroughIota_term, hdec_term,
  not_terminalPolySem_terminal_indexed}`.
