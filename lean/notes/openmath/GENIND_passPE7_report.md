# GENIND pass PE7 — HOSTILE VERIFICATION REPORT (fresh context, seventh arc pass; the r6 supplier re-pin — S16 — attacked for the first time)

**VERDICT: CLEAN. 0 CRITICAL, 0 GAP, 3 MINOR (+2 remarks). THE
2-CLEAN COUNT STARTS: the counter moves 0/2 → 1/2.** Nothing
GAP-or-worse survived my verification attempts. The S16 supplier
table — the artifact no prior pass had attacked — held at every row
I walked against git (seven of eight rows checked commit-by-commit,
the eighth trivially); both heavy verbatim quotes (W-12.D @ ee17210,
GENH4.B @ 4b0d946) are character-faithful with honestly marked
elisions; the mathematics of S6 Step 4 (the (A2) closure with
(CS-EXACT) and the mixed bucket through GENIND-6(d)) and Step 5 (the
W-12.D firing) survived an end-to-end re-derivation, including an
independent recount of the M6 worked ledger's 11 digit constraints
and 9/7 fiber exponents. My fresh route (§7; disjoint from all eight
prior routes) took GENIND's laws to territory no leg of this unit
had ever touched — the (m,d) = (2,5) cluster system at q = 2: n = 10
AND the first d ≥ 2 machine contact (letter field F₃₂, the first
non-prime Q on this unit's own laws) — and every preregistered
count, transport, bracket, and PARI σ-prediction fired exactly
(scoring run: 2,068,533 checks, 0 violations, exit 0). All 16
frozen md5s + GT-PIN 5/5 + the sealed artifact pair re-verified;
ALL SIX committed legs re-run in isolation, six GREEN, leaf-diffs
elapsed-only (PE4 bit-identical). The three MINORs are
wording-precision defects inside the r6 record itself (quoted in
§4); none touches a count law, a theorem clause, a consumed
supplier conclusion, or a frozen number.

**Target:** `lean/notes/openmath/GENIND_PROOF_2026-08-08.md` at r6
(760afed / dcc20bb / 993935d; file untouched since 993935d,
verified at verdict time against HEAD 7f27065). **Role:** hostile
verifier, fresh context, zero prior stake. I fix nothing; every
finding quotes its passage. Classification: CRITICAL (breaks the
chain) / GAP (justification missing, conclusion assumed downstream)
/ MINOR (display, scope, or scar; no conclusion at stake).
**Record rule compliance:** every record sentence below is
enumerated or pattern-scoped; no universal quantifiers.

Prior verdict lines read first (per charge): PE1 0C+3G+4m → PE2
0C+2G+1m → PE3 0C+2G+4m → PE4 0C+2G+0m → PE5 0C+1G+2m → PE6
0C+1G+1m — zero CRITICALs across six passes; five consecutive
record-integrity findings (PE2-G2 → PE6-G1) diagnosed as the stale
supplier pin set, which r6 answered with the 8-row re-pin table.
This pass's charge: attack the cure.

---

## 1. THE S16 TABLE — walked against git (attack 1)

**Row pins and movement inventories** (each item below checked by
`git log --oneline` on the supplier's note file this round):

* **Row 1, W-12.** Old pin r2 99f1813 ✓ exists; new pin r5
  be4a1dc/ee17210 = the file's current HEAD ✓. Movement: r3
  b17238a (commit message: "undecided(n,q,N) sigma-pinned to
  BOX-7's notion" — matches the row) ✓; r4 d2d91b7/5728258 ("F1
  CRITICAL repaired (S3.2's b2=0 witness pair)" + "G1
  sigma-LABELING clause at (A1)... FIRING CHECK at all suppliers
  (W-11(i)/(iii), HM3.C, GENH4.C, GENHN.C; n>=6 = the open
  GENHN-HE box)" — matches) ✓; r5 be4a1dc ("PE5's 1 GAP + 3 minors")
  ✓. Evidence claims: the W12 r5 supply-table sentence "The n = 3
  firing SURVIVES unchanged" found VERBATIM at ee17210 (W12 L434)
  ✓; the "zero S3.2 cites" claim — see MINOR 1 (§4): substance
  verified (the four string hits are all r6's own disclosure
  sentences; the consumed-clause list W-12.D/W-12.A/S2.3/S2.4/
  S2.5/S3.4/L0/L1 contains no S3.2), wording imprecise.
* **Row 2, HEX3.** Old pin r1 2580f12 ✓; new pin r3 b6a1d69 = file
  HEAD ✓; movement r2 a707430 ("11 -> TWELVE pairs" census + "S8.2's
  k=1 N-linear mass regains its (1 − q^{−(N−3)}) factor") ✓, r3
  ("S8.2 W-12.B consistency clause CONDITIONALIZED... BOX-1") ✓.
  Evidence: both corrected items are S8.2/census records — grep this
  round: the GENIND note's HEX3 consumptions are H-1..H-4, u₃(M),
  and the GT-N3TIE species tie at the md5-frozen
  `hex3_checks_results.json` (ce6f4116, re-verified by md5sum this
  round, §5) ✓.
* **Row 3, HMENU3.** ACCEPTED body 050425d + ANNEX A 19c0285 = the
  file's two most recent commits ✓; movement r1 3235e1b + r2
  e1280be present ✓. The quoted annex wording "every disc ≠ 0 lift
  of that member has σ = the leaf table's entry" found in the
  HMENU3 file (L904–905, line-wrapped — verified by
  whole-file linearized match) ✓.
* **Row 4, W-11.** ACCEPTED 2/2 @ 35e6545 = file HEAD ✓; `git diff
  31506b3 35e6545` on the file = 30 insertions, 0 deletions, all in
  a tail ACCEPTANCE RECORD block — append-only annotation, zero
  content diff to the consumed body ✓ exactly as the row states.
* **Row 6, GENHN.** 850e77e = file HEAD, unmoved ✓. The three
  queued errata exist VERBATIM in PROJECT_STATE (L3374–3376:
  "GENHN's erratum queue now 3 items (UND phrasing; comp-weighting;
  A(i) transport)") ✓; at ledger granularity none of the three
  names a clause at all, so the row's per-clause check passes — but
  see MINOR 2 on the four-clause inventory's completeness.
* **Row 7, GENH4.** Accepted body 4b0d946 exists; ANNEX R2 =
  760d20f ✓; annex-pass #1 NOT-CLEAN @ a8dec16 / fresh leg fed3cff
  cited correctly per the file log ✓. "Pass #2 in flight, not landed
  at this commit": verified by timestamps — dcc20bb landed 08:19:53,
  the annexpass2 commits 510c0cf/9049823/d6a9ac8 landed
  08:27:58–08:33:40 ✓. The quoted "W-12.D consumes (b) only" is
  VERBATIM in 760d20f's commit message (and the annex body carries
  the equivalent "W-12.D's supply row consumes clause (b) only",
  GENH4 L2002) ✓.
* **Row 8, HE7.** `grep -c "HE7\|HE6"` on the r5 text (git show
  9f2da0f) = 0 ✓ exactly as the row states.
* **Row 5, QSCOUT22.** Pins dd67cda/fcaf268 = the file's two most
  recent commits ✓; nothing consumed — trivial survival, agreed.

**The four unmoved pins** (S16's second block), each checked: J-D0
note last touched 9387eb1 ✓; DULEMMA f819f26 ✓; GRTJC file last
touched 63091ce (annex; accepted body pin stands) ✓; WELDMASTER
fdf1d99 (fold-carry annexes) ✓. Four checked, four match.

**The two heaviest consumptions, re-diffed character-by-character:**
(1) The S16 W-12.D quote vs `git show ee17210` W12 S1(iii)
(L267–305): the non-elided text matches character-for-character;
the three elisions ((A0)/(A1)/(A2) bodies) are marked with … and
honestly summarized; the conclusion block (R_τ display + four
conclusion clauses) is verbatim. The Step 5 use-site quote matches
the note's own L1102–1105 verbatim. Hypothesis-triple and
conclusion-clause match: confirmed one-for-one by my own reading.
(2) The S16 GENH4.B quote vs `git show 4b0d946` (L149–163): the
displayed laws (genre E per-center law, genre F two-term bracket,
the CAP(F) even-N clause) are verbatim; the elisions marked. The
byte-identity claim re-verified AT MY VERDICT TIME: the 15-line
GENH4.B display at 4b0d946 vs current worktree HEAD (which now
includes 6568114's post-pass-2 dated corrections AND the pass-3
acceptance) — diff EMPTY, byte-identical ✓. The Step 4 use-site
quote matches the note's L1073–1078 verbatim.

**Timing/photo-finish claims:** TAMEKQX 9b170f0 (08:19:40) indeed
landed between 760afed (08:16:45) and dcc20bb (08:19:53) — the
993935d micro-append's disclosure is accurate to the second ✓.
The r6 scope claim "files edited this round are exactly one" —
verified: all three r6 commits touch only the note file ✓.

## 2. The r6 dated appends + the C14 correction (attack 2)

S9.3 read in full. The seven [r6] bullet appends each carry: the
stale original preserved in place, the movement inventory, the new
pin, and a pointer to its S16 row — checked against §1's git walks;
the six bullets with movement match their rows; the GENHN bullet
matches PROJECT_STATE. S14's C14: the false close is QUOTED
verbatim inside the [r6] bracket ("each git-log-checked at commit
time, none moved from its pin...") and declared FALSE at FIVE named
suppliers with the refutation ("refuted by the git log at both the
r4 and r5 commit times") — this matches what §1's walks found
(W-12 three rounds, HEX3 two, HMENU3 accepted+annex, W-11
accepted, QSCOUT22 verdict+annex). The replacement pointer (the
S16 table) is accurate. Collateral: the C14 bracket's list of the
five prior defect-class findings (PE2-G2 → PE6-G1) is consistent
with the six prior reports' verdict lines. No new defect found in
the appends beyond MINORs 1–3.

## 3. THE MATHEMATICS, re-derived fresh (attack 3)

**(A) Step 4 — the (A2) closure with (CS-EXACT), and the mixed
bucket through GENIND-6(d).** Re-derived end-to-end. The r(N)
inventory (conservative drains / boundary families / ragged bands /
CS-internal residues) is exhaustive against the S2–S5 grammar I
re-checked at GENIND.A(I). The mixed-frame drain composition is the
exact set identity U = Π_b total_b − Π_b (total_b − u_b) — sound
GIVEN (i) the product decomposition with constant q-power fiber
(GENIND-6(c), whose M6 instance I recounted below), (ii) exact
child u's ((IH) member 4), (iii) exact stage drains ((CS-EXACT)).
The note consumes exactly these three at exactly these clauses —
the [r1]/[r2] records' consumption-path argument (a degree-n′ > n
fracture into a degree-n child runs S5.2's inclusion–exclusion,
an identity only on exact u's) is correct, and the discharge
geography (n = 4 de facto via GENH4.A/B + GENH4-1F; general n a
hypothesis on the box) is stated without overclaim. **M6 ledger
recount (independent):** pinned digits at the M6 genre = 4+3+2+1+1
= 11 constraints relative to the ā = 0 baseline → locus
(q−1)²q^{6N−6−11} = (q−1)²q^{6N−17} ✓; D = min_j(v(a_j)+j) = 4 ✓
(also attained at j = 1 at odd q — the note's "attained at j = 0
and the vertex j = 2" is existential, min unaffected); ledger
stage-window free digits 4(N−4) = 4(N−5)+4 ✓; fiber exponents
6N−17−2(N−5)−(4N−16) = 9, sharp 7 ✓ — all four displayed values
reproduced.
**(B) Step 5 — the W-12.D firing.** The hypothesis triple matches
the supplier statement clause-for-clause (§1). The one seam worth
pressure: W12 r4's σ-LABELING clause STRENGTHENS (A1), so P(n) must
now deliver σ-labeled families. My re-derivation: order-1 families
carry σ by Ore (proved); products compose branchwise (Step 3); CS
families carry σ by (CS-3) — a member of [GENIND-H], so the
strengthened member is delivered under exactly the box the theorem
already assumes, and the base packages P(2)/P(3) carry it per W12
r4's own FIRING CHECK (W-11(i)/(iii), HM3.C — verified present in
the W12 r4 commit record). The strengthening therefore moves
NOTHING in GENIND.B's conditionality structure. Sound.
**(C) One structural observation, not charged (Remark R1):** P(k)'s
box line names its fourth member as "exact-complement (A2)", while
the form the β-consumption actually uses is the CLUSTER-SYSTEM,
WINDOW-SHIFTED drain u_{μ,d}(M) — which the proof constructs
(GENIND.C per cluster system + Step 4's assembly) and the menu
member's "history-resolved (HM3.A-form)" qualifier parallels, but
the box line itself compresses. The theorem's GRADE line ("PROVED
at the S6 level of detail (scaffold)") prices this; the content
exists in-note; no false sentence. Recorded for a future repair
round's consideration, zero consequence today.

## 4. FINDINGS (quote-and-classify; I fix nothing)

* **MINOR 1 (S9.3 W-12 bullet + S16 row 1 — a grep claim whose own
  method returns nonzero).** Quoted (L1480–1481): "zero S3.2 cites
  in this note (grep executed this round)"; row 1: "zero S3.2 cites
  in this note (grep, this round)". Executing the stated method —
  `grep "S3.2"` on the note at r6 — returns FOUR hits (L1474,
  L1480, L2354, L2427). All four are r6's own disclosure sentences
  (describing W12's repair), none a citation of W12 S3.2's content,
  so the intended claim is TRUE and the consumption verdict stands;
  but the sentence as worded fails its own stated check, the exact
  defect shape this arc's standing rule exists for. Failure
  scenario: a later pass runs the grep, gets 4, and must charge or
  re-adjudicate; honest wording would be "zero S3.2 cites outside
  this round's own disclosure sentences (grep: 4 string hits, all
  disclosure)".
* **MINOR 2 (S9.3 GENHN bullet + S16 row 6 — the consumption
  inventory undercounts the note's GENHN surface).** Quoted
  (L1547–1548): "none of the three names A(iv), A(v), GENHN-3, or
  S6.2 — the four clauses consumed here". The note also leans on
  GENHN at three sites outside the four: the GENIND-6(a) statement's
  embedded-genre display echo (L623: "the sibling-Hensel-loss
  composition GENHN already displays"), the GENIND-6 proof-of-(a)
  schema cite (L684: "an explicit affine ledger per genre, exactly
  the GENHN S11.F computation"), and Step 4's display cite (L1094:
  "GENHN S6.3 (@ 850e77e) displays the same shape at general
  genres"). The row's verdict survives regardless — at ledger
  granularity the three queued errata name NO clause, so the same
  evidence covers the omitted sites — but the inventory sentence's
  function is forward erratum-risk screening, and a future erratum
  against S11.F or S6.3 would pass the four-clause check silently.
  Failure scenario: GENHN executes an erratum touching S11.F's
  worked ledger; S9.3's per-clause check reports no contact; the
  GENIND-6(a) compression cite silently ages.
* **MINOR 3 (S16 row 1 — "displays" vs "carries").** Quoted (row
  1): "n ≥ 6 = [GENHN-HE(μ ≥ 3)], the box this note already
  displays". Grep this round: the string "GENHN-HE" occurs in the
  note ONLY inside S16 itself (L2354, L2397) — the note displays no
  box by that name; what it displays is [GENIND-H], inside which
  the μ ≥ 3 stage-law content rides (a (ψ, μ = 3) block consumes
  ≥ 6 abscissas, so [GENIND-H(n ≥ 6)] contains exactly that
  surface — I re-derived the containment). S16's own
  re-verification paragraph uses the accurate verb ("the exact open
  box this note's [GENIND-H] display already carries", L2397–2398).
  Failure scenario: a reader hunts the note for the displayed
  [GENHN-HE] box and finds it only in the sentence that claims it
  is displayed elsewhere.
* **Remark R1** — the P(k) box-line compression (§3(C)).
* **Remark R2** — the fresh route surfaced a true statement worth a
  future display: at (2,5,N), the conservative-DRAIN slice with
  v(a₁) = 1 is σ-CONSTANT ({(1,5),(1,5)} on 24/24 sampled disc ≠ 0
  lifts; hull argument: (1,1) sits below the chord (0,w)–(2,0) for
  w ≥ 3, forced two integer-slope length-1 sides) — σ-certifiable
  mass inside the conservative drain, the n = 10, d = 5 echo of
  HEX3-BOX-1's open tail-σ territory. Consistent with the S1
  conservative convention (the READ still exits UNDECIDED); no note
  law touched.

**Clean charges from the GENIND-6 honesty box** (the note's own
attack list, checked): (i) multi-stage-block genres — still not
live at n ≤ 7, consistent with S5.1's ≥ 8-abscissa budget I
re-derived; (ii) the Bezout budget input v(r₁) ≥ 2 — consumed only
certified-direction, as displayed; (iii) odd-q pins — PE3's q = 3
reader-ON leg stands, my route adds the F₃₂ letter algebra (a
different generalization axis); (iv) stability-vs-determinism —
my fresh leg's PARI legs compare independent-lift factorizations,
honoring the lesson.

## 5. MACHINE-LEG INTEGRITY (all pins; six isolated re-runs; tautology audit)

* **Frozen md5s, re-verified by md5sum this round — 16/16:** sealed
  runner e7ca150b ✓; r2 supp triple 7335fc16/63b7413a/6c93f29c ✓;
  PE3 fresh triple fce9cb08/2075dd28/360eb239 ✓; PE4 fresh triple
  aa2b9af8/1a29a9c1/e549331a ✓; PE5 fresh triple
  6ef3132e/a0452521/bdc04c4d ✓; PE6 fresh triple
  87b0f27e/1c2f8eed/fb473a57 ✓. Plus GT-PIN suppliers 5/5 (w11
  500aae15 / w12 7dc040d9 / hex3 83faf7ed / hmenu3 65326f85 /
  hex3_checks_results.json ce6f4116) ✓ and the committed sealed
  artifact pair (c157dc31/366f2143) ✓. 23 hashes checked, 23 match.
* **Isolated re-runs** (`/tmp/genind_pe7_run`; the six committed
  runners + the four GT-PIN suppliers + the hex3 JSON copied;
  committed artifacts never touched) — the third pass ever to
  re-run committed legs, the first to re-run all SIX: sealed
  battery **GREEN 163,536/0** (exit 0; teeth per its committed
  record); r2 supp **GREEN 2,128,163/0**; PE3 fresh **GREEN
  705,133/0**; PE4 fresh **GREEN** (exit 0); PE5 fresh **GREEN**
  (exit 0); PE6 fresh **GREEN** (exit 0). Results-JSON leaf-diffs
  vs the committed artifacts: PE4 = NONE (bit-identical); the
  other five = `elapsed_s` only. Six of six.
* **Tautology audit (my angle).** My fresh leg (§7) imports NOTHING
  from the sealed runner — classifier, transport, and bracket legs
  are written from my own re-derivation of the note's laws, so the
  exact count ties (six preregistered Q = 32 numbers with zero free
  parameters, values computed nowhere in the corpus before) are
  law-vs-independent-implementation ties, not self-reads; the
  transport leg exercises the REAL recentering (integer-exact
  division by the recentered key, GENIND-2(a) mechanics), and its
  ONTO/constant-fiber verdicts are count-level (bucket census over
  31 × 1,024 cells); the PARI legs are mechanism-free (poldisc +
  factor + nfinit([·,[2]]) + idealprimedec on independent lifts;
  PARI never sees my classifier; oracle self-checked on x²−2 and
  x²+x+1 before scoring). Teeth armed at exact fire counts
  (992/30/60), each fired exactly. Non-tautological.

## 6. AS-OF STATE AT VERDICT TIME (attack 4)

Repo HEAD at verdict: 7f27065. Movements DURING this pass, checked
against the git log: **GENH4 annex-stack pass #2 CLEAN (d6a9ac8,
0C+0G+3m, stack 0/2 → 1/2), post-pass-2 dated corrections
(6568114), and pass #3 CLEAN (7f27065, 0C+0G+1m) — the stack is
ACCEPTED 2/2 and GENH4 is now FULLY FROZEN** (second note through
both bars after HE7). Effect on GENIND: STRENGTHENING only — the
(CS-EXACT) n = 4 de facto discharge now cites a fully frozen
supplier; the GENH4.B display remains byte-identical to the 4b0d946
pin at 7f27065 (my §1 diff ran against the current worktree). The
S9.3 GENH4 bullet's "pass #2 in flight" line and the S16 row-7
"stack 0/2" figure are TRUE AT THEIR COMMIT TIME (dated records,
per the note's convention); the next dated append owes the 2/2
disclosure — the same forward obligation the bullet already
declares for the GENHN errata. HE7: FULLY FROZEN @ ca3754d = its
file HEAD ✓. TAMEKQX: new unit at 0/2, NOT in GENIND's stack —
correct absence (grep: 3 "TAMEK" mentions in the note, all inside
S16's photo-finish/as-of disclosure, zero consumption). GENIND's
note file: untouched since 993935d ✓; its 16 pinned artifacts:
untouched by the wave-12/13 commits (git log on
verification/openmath/genind_* since 993935d = my own PE7 additions
only). My own commits this pass are append-only new files (report +
genind_pe7_fresh.* artifacts).

## 7. FRESH ROUTE (mandatory; disjoint from all eight prior routes)

**Design.** All eight prior routes (seal n=4,5 / PE1 symbolic / PE2
n=6 / r2 fibration / PE3 M7T+TWOCHILD+SS6 / PE4 3-child+n=8 / PE5
NINEMIX+SAMESIDE+STEEPSCAN / PE6 f-first 2.1M) sit at d = 1 —
letters in the PRIME field F_q; GENIND-BOX-3 discloses prime-power
q as supplier-carried, and the sealed roster header confirms d = 1.
My route: **the (m, d) = (2, 5) cluster system at q = 2** — a
degree-10 tower (first n = 10 territory) whose letter field is
F₃₂ = F₂[x]/(x⁵+x²+1), the FIRST non-prime Q exercised on GENIND's
own laws (the corpus-consistent non-prime choice: it keeps the base
ℚ₂, so the PARI σ-oracle runs directly on degree-10 lifts — GENH4's
W(F₄) precedent needed a relative construction; d ≥ 2 gets
non-prime letters for free). Runner
`verification/openmath/genind_pe7_fresh.py` (md5 31529d22; output
40f1d42e; results 5dbba4d3; committed @ ac3cbd5 with run-1/run-2
artifacts preserved). ALL predictions hand-derived in the docstring
BEFORE any run: the four GENIND.A(I) locus laws at Q = 32 + the
trace-kernel σ-split (#ker(Tr: F₃₂→F₂) = 16), the α-transport
fibration, the bracket identity, and four PARI σ-families.
**Scoring run: GREEN — 2,068,533 checks, 0 violations, exit 0.**
* **P1 (N = 3 EXHAUSTIVE, 32⁴ = 1,048,576 states):** DRAIN 1,024 =
  Q^{(m−1)(N−1)} ✓; α(1) 992 = (Q−1)Q^{m(N−1)−k·m(m+1)/2} ✓ (the
  note's α locus law at its first d ≥ 2 point); DEC-RAM 1,015,808 =
  31·32³ ✓; the σ-refined split of the slope-1 DEC families
  DEC-S55 14,880 = 31·15·32 / DEC-I10 15,872 = 31·16·32 ✓ (the
  15/16 trace-kernel split); β = CS = 0 ✓ (GENIND.A(IV) at m = 2:
  composite-free, machine-exhaustive at a fresh degree).
* **P2 (N = 4 α-TRANSPORT, real recentering):** the full α(1) locus
  (1,015,808 states; the d₁-fiber factor carried by a per-(z, c₁)
  machine-asserted independence gate, 992 gates, the (z, c₁, d₀)
  space enumerated directly) maps ONTO (31 letters) × (1,024 child
  (2,5,2) states) with CONSTANT fiber 32 = the ghost Q^{k·c(2)} ✓;
  per-cell asserts: entry digit killed (digit₂(a₀′) = 0), v(a₁′) ≥ 2,
  v(s) ≥ 2, all green; child grammar totals 984,064 RAM / 31,744
  DRAIN exact ✓. GENIND-2(a) mechanics at d = 5, first contact.
* **P3 (bracket):** Σ_{compositions of μ} Π(Q−1)Q^{k_i} =
  (Q−1)Q^{2μ−1} exact at Q = 32, μ = 1..4 ✓ (GENIND-2(b)'s c(2)+1
  = 2 exponent at the first d ≥ 2 instantiation).
* **P4 (PARI σ, mechanism-free):** RAM 30/30 = {(2,5)}; S55 30/30 =
  {(1,5),(1,5)}; I10 30/30 = {(1,10)}; TOWER (α-prefix → child-RAM
  at N = 4, members produced by the real recentering) 20/20 =
  {(2,5)} — the first PARI-certified n = 10 tower reads in the
  corpus. DRAIN-b1nz 24/24 = {(1,5),(1,5)} (Remark R2's σ-constant
  tail slice, predicted after run 1). Drain-honesty, deterministic
  form: 4 DRAIN-deep + 4 ALPHA states × 3 CONSTRUCTED lifts each
  (digit-targeted in the recentered frame), each state realizing 3
  distinct shapes {(2,5)}/{(1,5),(1,5)}/{(1,10)} — 24/24 ✓.
* **TEETH (armed, exact):** T1 b₁-corruption reclassifies 992/992
  α-states to DEC ✓; T2 counter-σ "RAM = {(1,10)}" refuted 30/30 ✓;
  T3 flipped-trace predictor mismatches 60/60 ✓.
* **INSTRUMENT ARC (disclosed; my own gates, not the note's laws —
  every note-law gate was GREEN in all three runs):** run 1 RED on
  MY drain-honesty gate — diagnosis: the gate's "≥ 2 shapes over
  random-lift samples" prediction was mis-derived (the b₁ ≠ 0 drain
  slice is σ-constant, Remark R2 — a true fact my gate design
  missed); run 2 RED on the redesigned random-lift form — the
  non-{(2,5)} branch rides a ~1/32-per-lift digit coincidence
  (run 1 had passed the ALPHA half by luck: an honest instrument
  lesson — probabilistic honesty gates are seed-fragile); run 3 =
  the scoring run with CONSTRUCTED lifts (the
  constructed-counterexamples discipline), deterministic, GREEN.
  Run-1/run-2 artifacts committed alongside
  (genind_pe7_fresh_run{1,2}_*).

## 8. GRADE

**CLEAN: 0 CRITICAL, 0 GAP, 3 MINOR (+2 remarks).** Under the
arc's standing bar (nothing GAP-or-worse survives; precedent:
GENH4 annexpass2 took its clean mark at 0C+0G+3m), **the
consecutive-clean count STARTS: 0/2 → 1/2.** PE8 (a fresh hostile
pass) attempts the second clean; the r-round owed for the three
MINORs is at the composer's discretion (none blocks the count —
each is a wording-precision defect inside the r6 record, quoted in
§4 with its disposition; the mathematics, the count laws, the
sealed numbers, and the consumption verdicts stand un-refuted
after seven passes and nine machine routes).
