The two Wave-7 statements landed verbatim and their advertised derivation theorems are proved, but the package still fails this round: the display of record says C2/C3 are open and counts only nine Lean units. That is a current, chain-breaking status defect.

## New findings

1. Quote:

> “the factorwise measured mass law is OPEN unit H8-C2 (K-CUT-m-factor)”

and:

> “that tie is OPEN unit H8-C3 (K-CUT-K1-tie)”

and:

> “discharge awaits H8-C2 + H8-C3 + a clean re-pass.”

These statements in R2RootSync §D are now false. Both units are landed and proved under the supplied machine attestation. The package identifies §D as:

> “THE display of record for the fresh adjudication”

and the requested DISCHARGES bar requires that display to be the honest current residue. It instead records already-completed work as open. This is not merely historical language: the false status appears in the proposed ROOT replacement row and its standing-status sentence.

Classification: **CRITICAL** — the governing replacement display is stale and cannot be folded into ROOT truthfully.

2. Quote:

> “all NINE Lean units + 2 documentary units LANDED”

Nine was the correct repair of Round 1’s original eight-unit undercount, but C2 and C3 add two Lean units. The current package therefore has eleven H8 Lean content units, plus the two documentary units. The blueprint unit summary likewise still labels C2/C3 “OPEN.”

Classification: **CRITICAL** — a new documentary count mismatch introduced by landing C2/C3; it reopens the counting channel behind Round-1 finding 12.

3. Quote:

> “`KCutMassFactor` is the STRICTLY STRONGER factorwise law”

The declaration genuinely adds the required typed factorization interface:

> `W_factor : ∀ Ŝ : R.ShL, R.W Ŝ = shMass (R.shape Ŝ)`

> `weight_factor : ∀ ... D, R.weight Ŝ i D = blkMass (R.dcar Ŝ i D)`

> `mass_factor : ∀ ... T, Fb.mass σ T = shMass (...) * ∏ ε ∈ minEnts ..., blkMass (...)`

and `massFactor_mass_eq` proves that this interface supplies C1’s aggregate `mass_eq`.

However, the stronger documentary phrase:

> “per-index compensation is killed”

is true only in the limited sense that weights cannot depend on the abstract slot separately from `dcar`. Arbitrary compensation between different concrete `BlockDeriv` values remains possible until `blkMass` is identified with the intended measured/grammar weight. The file immediately and correctly concedes:

> “identifying `shMass`/`blkMass` with the MEASURED semantics … is OWNER-ROW work”

Thus C2 is a faithful unpinned interface, not a proof of measured factor semantics.

Classification: **GAP** — honest owner-row instantiation residue; the “killed” gloss is broader than the typed theorem.

4. Quote:

> “`KCutK1Tie` is the TYPED form of finding 7’s missing content”

The structure does type the consumption expression: `eOf`, `τOf`, `δOf`, `powSubst (δOf Ŝ i)`, the real `blockSolve`, and `blockTotal_tie`. Given this structure and the graft/mass binders, `kcut_k1_form` proves the exact displayed transported-block-total convolution. The C2→C3 theorem also correctly wires the factorwise assumption into that consumption theorem.

But only the e-leg has a Lean conformance row:

> `e_key : ... (R.dcar Ŝ i D).entE = (eOf Ŝ i).1`

There is no typed equation connecting `τOf` or `δOf` to `BlockDeriv.iface` or another concrete extraction field. The file concedes this accurately:

> “the τ/δ pool rows (`τOf`/`δOf`) are DATA whose node-level read-off pins … are part of the unit’s own instantiation obligation”

That obligation is documentary, not a field of `KCutK1Tie`. Consequently an instance can select arbitrary τ/δ values whenever the corresponding `bOK` and `blockTotal_tie` equations happen to hold. Likewise `vBridge` is arbitrary except for `vBridge_vOf`, injectivity on `C.chain.Sigmas`, and the support equation. This does not invalidate the theorem: when instantiated at the intended semantics, it genuinely states the missing Step-18 form. It does prevent treating possession of an arbitrary `KCutK1Tie` as proof that those semantics were instantiated faithfully.

Classification: **GAP** — accurately disclosed, unpinned owner-row work; not an outright discharge.

5. Quote:

> “`kcut_k1_form` IS proved … from `kcut_reduce` + tie/supp/inj — the CONTENT is the structure’s instantiation.”

This is accurate. The theorem proves no graft completeness, mass semantics, τ/δ read-off, or grammar dictionary. It performs substitution of `blockTotal_eq_tsum` into the already-conditional convolution. Similarly, `massFactor_mass_eq` is a conformance/regrouping derivation; the substantive content is assumed in `KCutMassFactor`.

Classification: **ADVISORY** — no derivation/semantics conflation remains in these honesty notes.

6. Quote:

> `graft_cut : ∀ σ (T : Fb.Tree σ), graft σ (R.cutFun σ T) = T`

> `cut_graft : ∀ σ (x : R.CompatTuple σ), R.cutFun σ (graft σ x) = x`

The row’s exactly-one decomposition is not proved outright. The ownership partition and B3 forward extraction are proved, but the consumer-level equivalence is conditional on these inverse-law binders. This is honestly displayed in §D:

> “full decomposition bijectivity/realizability stays open through the graft rows”

Classification: **GAP** — honest displayed mathematical residue.

7. New-declaration drift audit:

- `KCutMassFactor` — verbatim Wave-7 specification.
- `massFactor_mass_eq` — verbatim Wave-7 specification.
- `KCutK1Tie` — verbatim Wave-7 specification.
- `kcut_k1_form` — verbatim Wave-7 specification.
- `KCutReKeys.prod_ent` — not separately pinned verbatim, but exactly the blueprint-predicted “multiplicative `sum_ent` analogue”; no strengthening or weakening.
- `KCutK1Tie.blockTotal_eq_tsum` — auxiliary consequence of the specified tie/support/injectivity fields; its statement matches the advertised per-slot proof core.
- `kcut_k1_form_of_massFactor` — not separately pinned in Wave 7, but a conservative composition replacing `mass_eq` by `KCutMassFactor` and invoking `massFactor_mass_eq`.

No statement drift was found. The three auxiliary declarations are derived extensions, not literal transcriptions, and introduce no additional assumptions.

Classification: **ADVISORY**.

## What is proved versus assumed

Proved outright:

- First-entrance uniqueness and existence for fixed arbitrary `E`.
- Exhaustive-exclusive ownership and the BDY rule for fixed `(Tr,E)`.
- Concrete forward extraction and injectivity up to `VTree.ext`.
- Verdict and carrier conformance supplied by B3.
- Count and abstract series regrouping.
- `KCutMassFactor → mass_eq`.
- `KCutK1Tie →` the per-slot grammar-total expansion.
- The exact displayed K1 convolution, conditional on `R`, `TIE`, graft inverse laws, and mass factorization.
- The C2→C3 composition theorem.

Typed but unpinned assumptions include:

- All `KCutReKeys` carrier, map, countability, conformance, realization, and verdict-keying fields.
- The genuine cell-data characterization of `E`; `supplyDec` does not provide it.
- Graft and both inverse laws.
- `KCutMassFactor`, including identification of its functions with intended measured semantics.
- `KCutK1Tie`, including `bOK`, grammar-total equality, support, and the intended—though not type-enforced—τ/δ read-offs.
- The missing W-resummation leg, deliberately assigned to CL-17(ii)/W17ii.

Thus the row itself remains conditional, exactly as permitted by this adjudication’s scope. The failure here is that the proposed ROOT display has not been synchronized to this new conditional surface.

## Round-1 cure table

| # | Classification | Exact curing or failing quote |
|---|---|---|
| 1 | **CURED** | “`supplyDec` is CLASSICALLY VACUOUS … and is CONSUMED BY NO PROOF … a registration row, NOT a consuming face” |
| 2 | **CURED** | “`kcut_reduce` does NOT consume `cutOf`/`cut_injective` … its forward map is RE-ASSUMED … injectivity riding the assumed `graft_cut`” |
| 3 | **CURED** | “countability rows `ctblS`/`ctblD`”; and “`cutFamily_countable` — realized-family countability NOT proved” |
| 4 | **CURED** | “`mass_eq` is the AGGREGATE product only”; plus the landed `KCutMassFactor` fields and proved `massFactor_mass_eq` |
| 5 | **CURED** | “`real`/`vOf`/`verdict_key` [UNCONDITIONED assumptions — no realizability/faithfulness/canonicity/injectivity condition]” |
| 6 | **CURED** | §D’s full census: “carrier/map rows … conformance rows … countability rows … realization/keying rows … `supplyDec` … graft … mass” |
| 7 | **CURED** | C1 downgraded to “ABSTRACT CONVOLUTION form”; landed `KCutK1Tie`, `blockTotal_eq_tsum`, and `kcut_k1_form` now supply the typed exact-form interface |
| 8 | **CURED** | “full decomposition bijectivity/realizability stays open through the graft rows”; all three graft binders are displayed |
| 9 | **CURED** | “killing … at the ownership-partition level (full decomposition bijectivity/realizability stays open through the graft rows)” |
| 10 | **CURED** | “(vi-b-1) NO … residues = the ROUTING law (classified CORRESPONDENCE CLAIM, still undischarged) + the consumer-bijection level; (vi-b-2) NO” |
| 11 | **CURED** | “(K-CUT-2) exhaustive-exclusive read ownership”; `siteExp_cut`/`fiber_count_cut` are placed under “regroupings of (K-CUT-3)” |
| 12 | **PARTIALLY CURED** | “all NINE Lean units” correctly repaired the old eight-versus-nine error, but became false after C2/C3 landed; the current total is eleven Lean units |

VERDICT: CRITICAL