# L3FINGER (M4) — value-set / associativity / crossed-product FINGERPRINTS of the measured level-3 cocycle: the shape constraints hold exactly (swap-symmetric; ℤ/E₃-descending; B0a genre; restricts to level 2 on the e₂-subwindow ON THE NOSE; first deviation exactly at δ₃ = 1, z̄-free; denominators {e₁ middle, e₀ inner}, never e₀e₁)

**POST-SEAL DIAGNOSTIC — NON-PREREG.** The sealed prereg of record
(`ITERLAWR_PROBE_2026-08-08.md` S0–S4 + `iterlawr_probe.py`, seal **911431c**) is
READ-ONLY and byte-untouched by this unit. Everything below is representation-level
structure measurement — checks that constrain the level-3 law's SHAPE independently of
any template fit. All patterns are **CONJECTURES at instance grade** (no sealed
confirmation cycle here); the checks were run once, first-run outputs committed as-is.
Unit: M4 of the 2026-08-03 level-3 pattern-hunt directive ("run small numerics to see
what the pattern is; use a multitude of methods and checks"). Author: Fable,
campaign date 2026-08-08 (wallclock 2026-08-03). Seed 20260813 (fresh).

## S0. This unit's own reading of the run-1 "RED"

Formed from the raw committed artifacts before consuming the recovery S5 text:
`iterlawr_probe_results.json` has `violations == []` — **no preregistered
predicted-0 family was violated anywhere** (F0a/F0b/G1–G8/CERT all 0). The runner's
exit-1 "RED" line traces to exactly one meter: mutation control m4 undetected
(0/992), i.e. a TEETH failure of the reduced mutation battery (all its rows have
g₂ = 1, z̄ = 1, so a wrong z̄-exponent is field-invisible), not a law failure. This
unit's verdict concurs with the recovery-filled S5 and with M1/M2/M5: **the sealed
composed candidate (ITER-LAW-3) survived; "RED" = m4-teeth-only.** The briefing
phrase "the sealed candidate came back RED" is contradicted by the artifacts.

## S1. Method, data, artifacts

**Consumed verbatim (read-only):** the sealed harness classes
(`iterlawr_probe.Composite3` importing `grb_order2_check.Tower`,
`strata3_probe.Tower3`, `strata_probe.Composite`); the committed sealed results
JSON (spot-check target); M5's committed independent-harness records
(`level3_xcheck_results.json`, spot-check target).

**New artifacts (this unit):**
* `verification/openmath/l3finger_m4.py` — the fingerprint battery (FP0–FP7 below);
  output `l3finger_m4_output.txt` (full eyeball tables for 5 display towers),
  results `l3finger_m4_results.json`. Exit 0, first run, 25.1 s.
* `verification/openmath/l3finger_m4_devid.py` — the inflation-defect identity
  supplement; output `l3finger_m4_devid_output.txt`. Exit 0.

**Fresh tiny-tower roster** (E₃ ∈ {4,6,8}, all four rings {ℤ₂,ℤ₃,F₂[[t]],F₃[[t]]},
44 towers; none is a sealed-roster row):

| tag | d₀; (e,h,g) L0 · L1 · L2 | E₃ | design point |
|---|---|---|---|
| Q4A | 1; (2,1,2)·(1,1,2)·(2,1,2) | 4 | all three letters genuine; e₁ = 1 |
| Q4B | 1; (1,1,2)·(2,1,2)·(2,1,1) | 4 | e₀ = 1 control; top cascade g₂ = 1 |
| Q4C | 1; (2,3,1)·(1,1,1)·(2,3,2) | 4 | h-track 3; inner cascade g₀ = 1 |
| Q4D | 1; (2,1,2)·(2,1,2)·(1,1,2) | 4 | e₂ = 1 control (restriction trivial) |
| Q6A | 1; (2,1,2)·(1,1,1)·(3,1,2) | 6 | e₂ = 3 top track (E₃ = 6 NEW: no sealed row) |
| Q6B | 1; (3,2,1)·(1,1,2)·(2,1,2) | 6 | e₀ = 3, ℓ₀ = 2 |
| Q6C | 1; (1,1,2)·(2,1,1)·(3,2,2) | 6 | e₁ = 2 ≠ e₂ = 3; middle cascade g₁ = 1 |
| Q6D | 1; (2,1,2)·(3,1,1)·(1,1,2) | 6 | e₂ = 1 control at E₂ = 6 |
| Q8A | 1; (2,1,2)·(2,1,2)·(2,1,2) | 8 | full stack, all letters genuine |
| Q8B | 1; (2,1,1)·(2,3,1)·(2,1,2) | 8 | double cascade g₀ = g₁ = 1, z̄ visible |
| Q8C | 2; (2,3,1)·(2,3,2)·(2,3,1) | 8 | d₀ = 2 base inertia, h-track 3 |

Per tower: full W1×W1 measured cocycle table (W1 = [Γ₀, Γ₀+E₃), Γ₀ ≡ 0 mod E₃ by
the sealed window scan), fresh-window shift blocks, ≤ 120 sampled triples, plus the
sealed tower-data certification (T1/T2/T5 level-2 + cert3_internal level-3; PARI leg
not run here — the sealed battery already certified this genre; my fresh rows carry
the internal certs only). Monomial reps are found by box search (z̄ exponent within
±4, z₂/z₁ within ±40, all modulo the measured letter orders); ALL box matches are
recorded, so letter relations (e.g. z₂ = z₁⁴ on Q6C at p = 3) are flagged as
ambiguity counts, not silently resolved.

## S2. VERDICTS (the headline table; every predicted-0 family came in 0, first run)

| leg | check | result |
|---|---|---|
| SPOT-A | sealed row-G value sets, recomputed with THIS unit's machinery vs committed JSON | 4/4 MATCH |
| SPOT-B | M5 independent-harness integer data (iterated splits s₃/s₂/s₁ + δ₃, all recorded pairs) | 248/248 agree |
| FP0 | anchor = ε-chain; single-top-slot shape; ITER-LAW-3 closed form (decorrelated confirm) | 0 viol / 512 + 1600 + 1600 |
| FP2 | **SWAP**: c₃(γ,γ′) = c₃(γ′,γ) exactly (value AND top slot) | **0 / 1600 — exactly symmetric** |
| FP3 | **DESCENT**: single-arg, simultaneous, k ∈ {2,5} representative shifts | **0 / 6208 — COR-4 holds at level 3** |
| FP4 | **B0a genre**: c₃ = z̄^{j_meas}·dF, F = ε-chain | **0 / 1600** |
| FP4 | j_meas = ⌊(s₃+s₃′)/e₂⌋; j symmetric; j 2-cocycle identity | 0 / 1600 + 0 / 4384 triples |
| FP4 | measured associativity (cocycle identity) | 0 / 4384 triples |
| FP5 | **SUBWINDOW RESTRICTION**: c₃ on (e₂ℤ)² = embed(measured level-2 c₂) | **0 / 516 — ON THE NOSE** |
| FP5 | shadow deviation at δ₃ = 0 (want none) | 0 / 1600 |
| FP5 | deviation with a z̄ component (want none) | 0 / 224 deviating pairs |
| FP6 | top slot j factors through (γ mod e₂, γ′ mod e₂) | 0 multivalued / 192 residue classes |
| FP7 | middle e₁-warrant e₁ \| (s₂-def + ℓ₁γ₃δ₃) | 0 / 1600 field + 0 / 20000 symbolic |
| FP7 | inner e₀-warrant (composed) e₀ \| (s₁-def + ℓ₀Σ) | 0 / 1600 field + 0 / 20000 symbolic |
| DEVID | inflation-defect identity (S6 display) | **0 / 1600, supplement run** |

Zero findings; verdict line "M4 FINGERPRINT VERDICT: ALL CLEAN"; exit 0.

## S3. Value sets and the product-of-carries multiplicity spectrum

Per-tower value multisets over W1×W1 (full list in the JSON `valuesets` block).
Two structural facts, both exact on all 44 towers:

**(V1) Ring independence.** At fixed (p, row), the ℤ_p tower and the F_p[[t]] tower
have IDENTICAL value-set multiplicities, deviation censuses, and quotient tables —
22/22 matched pairs. The fingerprint sees only the integer data (e,h,g,ℓ) and p
(through letter orders), not the ring's characteristic. (Conjecture grade;
consistent with the law's constants being pure ℤ-data.)

**(V2) Multiplicity spectrum = product of per-level carry counts.** Example Q8A
(e = (2,2,2), 64 pairs, 8 values): multiplicities {27, 9,9,9, 3,3,3, 1} — exactly
(3|1)×(3|1)×(3|1) where at each level the carry-0 fiber has 3 of 4 residue pairs and
the carry-1 fiber 1 of 4. Example Q6A (e = (2,1,3), 36 pairs): {18, 6, 9, 3} =
(6|3 of 9 at e₂ = 3)×(3|1 of 4 at e₀ = 2). The measured table is the pushforward of
the product carry lattice — the iterated-extension (crossed-product) structure is
visible in the multiplicities alone, before any exponent is read.

Sample value sets (nonneg-rep monomials; z̄ = level-3 letter, z₂ middle, z₁ inner):

* `Zp,p=3,Q8A` (ords z̄,z₂,z₁ = 41,5,8): {1:27, z₁:9, z₂z₁⁻³:9, z₂z₁⁴:3, z̄z₂:3,
  z̄z₂z₁:1, z̄z₂²z₁⁻³:9, z̄z₂²z₁⁴:3} — all three letters live, all exponents as the
  sealed law's (δ₃, D₂, D₁) triples reduced mod orders.
* `Zp,p=2,Q4B` (e₀ = 1 ⟹ ℓ₀ = 0, s₁ ≡ 0): {1:10, z₂:6} — inner letter absent
  exactly as the degeneration predicts.
* `Zp,p=2,Q8B` (double cascade, z₂ = z₁ = 1): {1:48, z̄:16} — pure top carry.

Box-decomposition ambiguity is nonzero exactly on towers with letter relations
(e.g. Q6C at p = 3: ord z₂ = 2, ord z₁ = 8, z₂ = z₁⁴ — the namer then prints z₁⁴
for z₂; flagged `ambiguous_reps` in the JSON, never silently collapsed).

## S4. Swap and descent (FP2, FP3)

The measured cocycle is EXACTLY symmetric, c₃(γ,γ′) = c₃(γ′,γ), value and top slot,
1600/1600 (structurally forced — φ_γφ_{γ′} = φ_{γ′}φ_γ — but measured here as a
harness-independence check). The antisymmetric split is trivial: the class is
symmetric, consistent with a COMMUTATIVE iterated crossed product (twisted group
algebra of an abelian group with symmetric 2-cocycle).

ℤ/E₃ descent holds exactly in the COR-4-separately-per-argument sense: shifting
EITHER argument by kE₃ (k ∈ {1,2,5}) or both simultaneously leaves every table
entry unchanged (6,208 checks, 0 violations). Representative independence at
level 3 is CONFIRMED at instance grade; the cocycle is a function on (ℤ/E₃)².

## S5. B0a genre at level 3 (FP4)

The accepted order-2 decomposition (ITERLAW S6.2: c = z̄^{δ₂} × coboundary of
F = ε₁∘γ₁) exists at level 3 in the SAME genre, with the same carrier one level up:

    c₃(γ,γ′) = z̄^{j(γ,γ′)} · F₃(γ)F₃(γ′)/F₃(γ+γ′),   F₃ = ε₂(β₂(·))·ε₁(β₁(·)),

0/1600 exact, with j = the MEASURED top slot, which equals ⌊(s₃(γ)+s₃(γ′))/e₂⌋ on
every pair (0/1600). j is symmetric and satisfies the 2-cocycle identity on triples
(0/4384); the measured cocycle satisfies associativity on the same triples
(0/4384). So the class decomposes as [pure top carry] × [explicit ε-chain
coboundary] — M2's finding reproduced on a disjoint fresh roster by a disjoint
code path (decorrelation: this unit shares no roster row and no analysis code with
M2's l3cohom_m2.py; both consume the sealed harness classes).

## S6. The crossed-product restriction fingerprint (FP5 + DEVID) — the unit's core

**Setup.** The subgroup e₂ℤ/E₃ℤ ≅ ℤ/E₂ sits inside ℤ/E₃ with quotient ℤ/e₂. On the
subwindow {γ ∈ W1 : e₂ | γ} (equivalently s₃(γ) = 0, verified equivalent on every
pair), β₂(γ) = γ/e₂, and the level-3 anchor φ_γ IS the level-2 anchor of the
subtower at β₂.

**(R1) Restriction ON THE NOSE, measured vs measured.** For all subwindow pairs,
c₃(γ,γ′) = K₃-embed(c₂(γ/e₂, γ′/e₂)) with c₂ the MEASURED level-2 cocycle of the
subtower — 0 violations / 516 subwindow pairs (E₂² per e₂ ≥ 2 tower + all pairs on
the e₂ = 1 controls). Level 3 restricted to the canonical E₂-subgroup IS level 2,
with the letter dictionary (level-2 z̄ ↦ z₂, z₁ ↦ z₁), exactly.

**(R2) The inflation-defect identity (where and how level 3 first deviates).** For
EVERY W1×W1 pair (supplement `l3finger_m4_devid.py`, 0 viol / 1600, 0 shadow
nonintegral, 0 window misses):

    c₃(γ,γ′) = z̄^{δ₃} · embed(c₂(β₂(γ), β₂(γ′))) · z₂^{D₂−δ₂ˢ} · z₁^{D₁−Dˢ},

where c₂(β₂,β₂′) is the measured level-2 shadow cocycle, δ₂ˢ = ⌊(s₂+s₂′)/e₁⌋ and
Dˢ = (s₁+s₁′−s₁(split of β₂+β₂′) + ℓ₀γ₂δ₂ˢ)/e₀ its accepted-law constants, and
(D₂, D₁) the sealed composed constants. Consequences, all measured exactly:

* **First deviation = δ₃ = 1, nowhere else.** Every deviating pair (224 of 1600)
  has δ₃ = 1; at δ₃ = 0 the deviation factor is 1 identically (0 exceptions) —
  at δ₃ = 0, level 3 IS the level-2 law of the β₂-shadow, letters relabeled.
* **The deviation is z̄-free**: never a z̄ component (0/224). Top-carry leakage
  lands ONLY in the two lower letter tracks — the deviation monomial census per
  tower (JSON `dev_census`) has values like z₂, z₂², z₁^{±1}, z₂z₁, z₂²z₁^{-1};
  on e₁ = 1 towers it is a pure z₁ power (composed inner shift only), on e₀ = 1
  towers a pure z₂ power (middle correction only).
* 148 δ₃ = 1 pairs deviate by exactly 1: on letter-trivial towers (z₂ = z₁ = 1,
  e.g. Q4C/Q8B at p = 2) the defect is field-invisible, and on genuine-letter
  towers the defect exponents can vanish mod letter orders (e.g. Q6C p = 3:
  z₂^{16} = 1 at ord z₂ = 2). "No deviation" is visibility-limited, not a second
  exact stratum — the R2 identity is the exact statement.

**Reading.** (R1)+(R2) is the iterated-extension compatibility the crossed-product
picture demands: res_{ℤ/E₂}[c₃] = [c₂] exactly (not just cohomologously), and the
failure of naive inflation is a single explicit z̄-free defect keyed to the top
carry. This is the fingerprint version of the seal's "top-carry leakage"
localization, now stated against the MEASURED level-2 object rather than a
template.

## S7. The e₂-quotient fingerprint (FP6)

The measured top slot j(γ,γ′) factors through (γ mod e₂, γ′ mod e₂) EXACTLY (0
multivalued classes / 192 residue classes over all towers and windows), and the
induced table on (ℤ/e₂)² is the standard carry cocycle of

    0 → ℤ/E₂ → ℤ/E₃ → ℤ/e₂ → 0

in the h₂-twisted coordinate (s₃ ≡ h₂^{-1}γ mod e₂). Example `Zp,p=3,Q6A`
(e₂ = 3, h₂ = 1): j-table rows (γ mod 3, γ′ mod 3) ↦ j: (1,2),(2,1),(2,2) ↦ 1,
all else 0 — the ℤ/3 carry table verbatim. So the z̄-part of the class is the
pullback (inflation) of the extension class of the top quotient; the value-group
part [z̄^{(E₃/e₂)ℓ₂}...] invariant story is M2's lane and untouched here.

## S8. Denominators / divisibility warrants (FP7) — what the level-3 law needs

Field battery (1600 pairs) + fresh symbolic census (20,000 draws, e_j ≤ 12,
h_j ≤ 39, g_j ≤ 4, own stream seeded SEED+1 = 20260814):

* **Middle track: denominator e₁, NEW at level 3, correction load-bearing.**
  e₁ | (s₂-defect + ℓ₁γ₃δ₃) always (0 viol field + symbolic). The raw defect
  ALONE fails e₁-divisibility on 208/1600 field pairs and 6,031/20,000 draws —
  the ℓ₁γ₃δ₃ correction is what buys integrality; at level 2 there is no middle
  track at all, so /e₁ is the genuinely new denominator.
* **Inner track: denominator e₀ exactly as accepted**, with the COMPOSED shift:
  e₀ | (s₁-defect + ℓ₀Σ) always (0 viol field + symbolic). The naive per-level
  correction ℓ₀γ₂δ₂ leaves a NONINTEGRAL numerator on 164/1600 field pairs and
  5,653/20,000 draws — ALL at δ₃ = 1 (0 at δ₃ = 0, both censuses), and the
  failing residues mod e₀ take every nonzero value 1..11 (JSON
  `symbolic_naive_mod_e0`), so no sub-denominator rescues the naive form.
* **No e₀e₁ events.** No pair and no draw needs a finer denominator than the
  single-level ones (the `mid_e0e1_phantom` counter — draws where the middle
  defect would become integral only against e₀e₁ — is 0). The level-3
  divisibility warrant is: per-level denominators, each cured by its own
  level's carry correction, composed downward. This pins the shape of the
  W-MULT-(n)/EXP-(n) integrality lemmas for the ITER-LAW-n cycle: one Bézout
  kernel per level, no cross-level denominators.

## S9. Eyeball tables (full W1×W1; per-pair rows)

Reading guide: s₃,s₃′ the top splits; (d₃ D₂ D₁) the sealed composed constants
(law value == measured on ALL rows of ALL 44 towers, FP0); "measured c₃" the
box-canonical monomial (lex-min rep; on Q6C p=3 note z₂ = z₁⁴, so z₂-values print
as z₁⁴); "sub" = the pair lies on the e₂-subwindow; "shadow-dev" = the deviation
from the embedded measured level-2 shadow (S6). Three more display towers
(Zp,p=2,Q4C · Fpt,p=3,Q6A · Zp,p=3,Q8A) are in `l3finger_m4_output.txt`.

**TOWER Zp,p=3,Q4A** — d₀=1, L0=(2,1,2), L1=(1,1,2), L2=(2,1,2); E₃=4, E₂=2,
Γ₀=32, γ₂=5, γ₃=21, (ℓ₀,ℓ₁)=(1,0), ords(z̄,z₂,z₁)=(41,5,8):

| γ | γ′ | s₃,s₃′ | s₁,s₁′ | d₃ D₂ D₁ | measured c₃ | sub | shadow-dev |
|---|---|---|---|---|---|---|---|
| 32 | 32 | 0,0 | 0,0 | 0 0 0 | 1 | Y | 1 |
| 32 | 33 | 0,1 | 0,0 | 0 0 0 | 1 |  | 1 |
| 32 | 34 | 0,0 | 0,1 | 0 0 0 | 1 | Y | 1 |
| 32 | 35 | 0,1 | 0,1 | 0 0 0 | 1 |  | 1 |
| 33 | 32 | 1,0 | 0,0 | 0 0 0 | 1 |  | 1 |
| 33 | 33 | 1,1 | 0,0 | 1 0 10 | z̄·z₁² | | z₁² |
| 33 | 34 | 1,0 | 0,1 | 0 0 0 | 1 |  | 1 |
| 33 | 35 | 1,1 | 0,1 | 1 0 11 | z̄·z₁³ | | z₁³ |
| 34 | 32 | 0,0 | 1,0 | 0 0 0 | 1 | Y | 1 |
| 34 | 33 | 0,1 | 1,0 | 0 0 0 | 1 |  | 1 |
| 34 | 34 | 0,0 | 1,1 | 0 0 1 | z₁ | Y | 1 |
| 34 | 35 | 0,1 | 1,1 | 0 0 1 | z₁ |  | 1 |
| 35 | 32 | 1,0 | 1,0 | 0 0 0 | 1 |  | 1 |
| 35 | 33 | 1,1 | 1,0 | 1 0 11 | z̄·z₁³ | | z₁³ |
| 35 | 34 | 1,0 | 1,1 | 0 0 1 | z₁ |  | 1 |
| 35 | 35 | 1,1 | 1,1 | 1 0 11 | z̄·z₁³ | | z₁² |

(All six deviating rows have d₃ = 1; the sub-marked 2×2 block {32,34}² is exactly
the measured level-2 cocycle of the subtower: 1 on three pairs, z₁ at (34,34) —
the accepted order-2 z₁^{δ₁} at e₁ = 1. D₁ values 10, 11 print reduced mod
ord z₁ = 8 as z₁², z₁³.)

**TOWER Zp,p=3,Q6C** — d₀=1, L0=(1,1,2), L1=(2,1,1), L2=(3,2,2); E₃=6, E₂=2,
Γ₀=90, γ₂=5, γ₃=32, (ℓ₀,ℓ₁)=(0,1), ords(z̄,z₂,z₁)=(10,2,8), relation z₂ = z₁⁴
(so the namer prints z₂ as z₁⁴); e₀ = 1 ⟹ D₁ ≡ 0, the whole non-carry content
sits in the MIDDLE track — shadow-dev ≡ 1 because z₂^{D₂−δ₂ˢ} has even exponent
throughout (ord z₂ = 2, visibility-limited; the R2 identity still checked exact):

| γ | γ′ | s₃,s₃′ | s₂,s₂′ | d₃ D₂ D₁ | measured c₃ | sub |
|---|---|---|---|---|---|---|
| 90 | 90 | 0,0 | 0,0 | 0 0 0 | 1 | Y |
| 90 | 93 | 0,0 | 0,1 | 0 0 0 | 1 | Y |
| 91 | 91 | 2,2 | 1,1 | 1 17 0 | z̄·z₂ (= z̄·z₁⁴) |  |
| 91 | 92 | 2,1 | 1,0 | 1 16 0 | z̄ |  |
| 91 | 93 | 2,0 | 1,1 | 0 1 0 | z₂ (= z₁⁴) |  |
| 92 | 92 | 1,1 | 0,0 | 0 0 0 | 1 |  |
| 92 | 94 | 1,2 | 0,0 | 1 16 0 | z̄ |  |
| 93 | 93 | 0,0 | 1,1 | 0 1 0 | z₂ | Y |
| 94 | 94 | 2,2 | 0,0 | 1 16 0 | z̄ |  |
| 95 | 95 | 1,1 | 1,1 | 0 1 0 | z₂ |  |

(Representative rows; the full 36-row table is in `l3finger_m4_output.txt` and
matches the sealed law on every row. Note D₂ = 16, 17 at d₃ = 1: the ℓ₁γ₃δ₃ = 32
correction over e₁ = 2 contributes 16 — field-invisible at ord z₂ = 2, but the
INTEGER constants are pinned by FP7's warrant, and on the m4-supplement-genre
towers (e₁ ≠ e₂, g₂ = 2) the same correction is field-visible.)

## S10. What the fingerprints pin for the wave-19 compose/ITER-LAW-n cycle

Shape constraints now measured, independent of any fit (all conjecture-grade,
instance-exact on 44 fresh towers + spot-checked against the sealed and M5
artifacts):

1. **The induction carrier is confirmed from a third angle**: level-(n) cocycle =
   z̄^{top carry} × d(ε-chain gauge) with the top carry inflated from ℤ/e_{top}
   (S5, S7). Any candidate ITER-LAW-n MUST restrict on the nose to ITER-LAW-(n−1)
   on the e_top-subwindow (S6 R1) — a strong, cheap gate for future sealed cycles:
   check the subwindow FIRST.
2. **Deviation budget**: off the subwindow the entire level-n novelty is a z̄-free
   defect supported on δ_top = 1 with per-level exponents (S6 R2) — the recursion
   S_{j−1} = D_jγ_j + S_j(ℓ′ − ℓw) of the S6 recovery display is exactly what
   this defect measures. A level-4 probe should preregister the R2-style identity
   against the MEASURED level-3 shadow, not only the closed form.
3. **Integrality architecture**: one Bézout kernel per level, denominators
   {e₀, e₁} (and e₂ only through the carry floor), never products (S8) — the
   EXP-(n) lemma bank can be stated per-level.
4. **Design lesson institutionalized**: fingerprints (multiplicity spectra,
   quotient tables) separate laws BEFORE letter visibility — the Q6C middle track
   is invisible to the field but fully pinned by the warrant census; future
   mutation batteries should include a warrant-level (integer) detection leg in
   addition to field detection (the m4-teeth failure could have been caught at
   design time this way).

## S11. Fences

No proof claims; no acceptance; every pattern above is a measured conjecture. No
edit to any sealed or accepted record (seal 911431c byte-untouched; ITERLAW /
DITER_RESTATE / TWIST3 untouched). Orders ≥ 4 untouched. (ITER-LAW-LIFT)
propagates: standard-lift towers only. The PARI external leg was not run on the
fresh rows (internal certs only — T1/T2/T5 + cert3_internal all green); nothing
here upgrades any statement's status.

— M4 fingerprint unit, campaign date 2026-08-08 (wallclock 2026-08-03).
