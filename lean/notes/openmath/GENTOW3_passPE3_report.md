# GENTOW3 passPE3 — THIRD HOSTILE PASS (2026-08-10)

**Target:** `lean/notes/openmath/GENTOW3_PROOF_2026-08-09.md` at HEAD
(r2 round complete at 0d09d65; [GT3-r2] certificate scoping: S0
geography clause, S2 (iv) statement pin, S4 delta = 1 bullet, S4
count-law, S7.6 record).
**Arc:** seal → PE1 NOT CLEAN 1 GAP + 3 MINOR (3101cf5) → r1
(e1a8dfe) → PE2 NOT CLEAN 1 GAP (c048154) → r2 (0d09d65) → this
pass. Counter 0/2 entering; CLEAN would move it to 1/2.
**Protocol:** quote-and-classify, fix nothing. Fresh context, zero
prior stake. PE1's routes (the Θ_j re-derivation ×2, the 485/0
three-frame instrument, the supplier census) and PE2's routes (the
gauge-annex re-derivation, FR2-C1, the δ = 0 corner) are NOT
re-walked. This pass stressed (1) the r2 certificate scoping by
independent re-derivation from the (iv) proof + BOX-1, at the two
refuting instances AND at attainment rows, with a vacuity check;
(2) the count-law's downstream consumers under the new scoping;
(3) the note as a dated-layer system after r1+r2; (4) a free hunt
— with a fresh machine route disjoint from all five prior frames.

## VERDICT: NOT CLEAN — 1 GAP (F-1) + 1 MINOR (m-A). Counter
## stays 0/2.

The r2 scoping is CORRECT at three of its four sites: the (iv)
statement pin, the S4 δ = 1 bullet, and the S4 count-law rewrite
all carry exactly what the (iv) proof + BOX-1 support (re-derived
independently; verified at the FR-M3 slack instance, at the
Φ₂³+32 counter-member, and at attainment rows — the certificate
still bites at FAM-A5 and at this pass's fresh frame). The GAP is
the fourth site: the corrected S0 geography clause asserts a
BICONDITIONAL — "it fails AT THETA_j exactly at the coordinates
carrying (iv)'s certificate" — whose only-if direction is neither
proved by S2 (which gives attainment only FOR THE KEY POWER) nor
true: this pass CONSTRUCTS a 𝒯-member at the very frame the note
cites (FR-M3, certificate CANCELLED at j = 0) whose shadow read
first fails at EXACTLY Θ₀ = 10 via an entry-digit-driven overflow
— the mechanism the note's own scope remark (2) names as covered
by the ledger but unwitnessed. Machine face: sealed fresh route
GREEN (dual oracles), plus all three prior instruments re-run
bit-stable in isolation.

---

## FINDINGS

### F-1 (GAP — the corrected S0 clause's "exactly" biconditional:
### failure AT Θ_j is NOT confined to certificate coordinates;
### refuted by a constructed member at the note's own frame)

QUOTE (S0, the [GT3-r2] corrected clause, L58–63): "at a
non-x-free genre it can first fail no earlier than THETA_j, and
it fails AT THETA_j exactly at the coordinates carrying (iv)'s
certificate (the key power's assembled height-THETA_j K2-digit
does not cancel — GENTOW3-BOX-1): there the floor is attained by
the key power's own shadow"; announced as "(proved in S2)".

CLASSIFICATION: GAP (one cure: drop or one-side the "exactly" —
the proved content is "at certificate coordinates it DOES fail at
THETA_j, attained by the key power's own shadow"; at
certificate-cancelled coordinates only the KEY POWER's first
divergence is pushed strictly above THETA_j — other members can
and do fail AT THETA_j there, via entry-digit-driven overflows).

WHAT S2 PROVES (re-derived independently from the (iv) proof +
BOX-1, per the r2 charge): Step 2 floors every discrepancy at
Θ_j (any member). For f = Phi2^{mu2} the minimal branch is a
single Phi'-extraction at height Θ_j whose digit is the assembled
K2-digit; certificate ⟺ that digit ≠ 0 ⟺ the KEY POWER's
self-shadow pin = Θ_j (cancellation pushes the self-shadow's
first height strictly above). This is a biconditional FOR THE
KEY POWER only. S2 proves nothing anywhere about the absence of
Θ_j-exact failure by OTHER members at cancelled coordinates —
and the note's own scope remark (2) + GENTOW3-BOX-1 name the
competing mechanism (entry-digit-driven overflows: quotient
digits of the member meeting x-ful K2-coefficients cross D'),
which the Step-2 ledger prices at exactly the same floor Θ_j.

THE REFUTING INSTANCE (this pass's fresh route, sealed,
dual-oracle; at the SAME frame the corrected clause cites for its
certificate-cancellation reading): FR-M3 (p = 2, Phi' = x²−2,
Phi2 = x²−2x−2, e2 = f2 = 1, u2 = 3, mu2 = 3, δ = 1,
Θ = [10, 7, 4]; key-power certificate CANCELLED at j = 0 —
committed self-shadow ShC_0 = 64, pin 12 > 10, height-10 digit
= 0). Member

    f = Phi2³ + 2x·Phi2² + 8·Phi2 + 64
      = x⁶ − 4x⁵ − 2x⁴ + 16x³ + 12x² − 32x + 40

is in 𝒯 (level-1 gate: entries weakly above the inner side, J = 0
exact) and NON-degenerate (honest read decided; PARI-checked).
Honest pins [12, 6, 3]; shadow pins [10, 6, 3]; the discrepancy
ShC_0 − C_0 = 96 − 64 = 32 first disagrees at EXACTLY
2·v₂(32) = 10 = Θ₀: mindiff = [10, 9, 4]. The shadow undercuts
the honest deciding vertex (0,12) → (0,10) and prices a different
first-edge stratum (slope 4 vs 6) — a live δ = 1 mislabel AT Θ₀
at the certificate-cancelled coordinate. Hand-derivation of the
route (exact, sign-checked, machine-identical): the member digits
2x (at j = 2, on-side weight 9) and 8 (at j = 1) meet K2 = Y − 2x
in the shadow division; the product's −16x² overflows D' = 2
(x² = Phi' + 2), the shadow keeps the tail −32 while the honest
frame keeps −16Phi': single extraction, height
9 + δ − 0·E2 = 10 = Θ₀, assembled digit −32 ≠ 0; total
ShC_0 = SelfSh_0 + D_0(g) = 64 − 32 = 32 over C_0 = 0 (W-A), and
96 over 64 at the displayed nondegenerate variant (W-B). Thirty-two
distinct sign/unit variants pass the gate with mindiff_0 = 10
(design disclosure in the runner); the phenomenon is generic, not
a tuned coincidence.

FAILURE SCENARIO: a consumer reads the corrected S0 as the
geography ("fails at Θ_j ⟺ certificate") and, at a genre with a
cancelled coordinate (FR-M3 j = 0 is measured), concludes strata
priced there are safe AT Θ_j — first corruption strictly above —
and under-corrects exactly the members my witness instantiates
(shadow undercuts the deciding vertex at Θ₀): the mirror image of
the over-count PE2's F-1 prevented. The S4 count-law (corrected,
one-sided) does NOT protect against this misreading, since S0
carries the only biconditional.

SCOPE OF DAMAGE (verified): THEOREM GENTOW-3 (i)–(iv) as pinned
at r2 are ALL TRUE — (iv) is scoped to f = Phi2^{mu2} and its
certificate pin is the correct reading (my re-derivation
concurs); the S4 δ = 1 bullet and count-law rewrites are
correctly one-sided ("can be corrupted ONLY…", "NECESSARY, not
sufficient") and my witness is consistent with both; S7.6's
re-derivation paragraph is key-power-scoped and correct. Only the
S0 compression loses the key-power subject and gains a false
only-if. No downstream consumer cites the S0 clause (GENTOW6's
quadrichotomy keeps entry-driven divergence explicitly separate —
regime (4) "only entry-driven divergence remains"; its BOX-3
names the classification open). Incidentally my witness is the
first committed Θ-EXACT entry-driven attainment row in any leg
(GENTOW6's E-ENTRY row diverges at a height ≥ Θ, not pinned at
Θ; GENTOW6-BOX-3 named exact-Θ entry attainment as undisplayed)
— it confirms the ledger's sharpness for member content too.

### m-A (MINOR — S7.6 frame misstatement: Phi' vs Phi2 flipped at
### the FR-M3 record)

QUOTE (S7.6, refuting-instance bullet 1): "FR-M3 (gentow3_pe1_
fresh.py + output, S7.5 md5s; p = 2, Phi' = x² − 2x − 2,
e2 = f2 = 1, u2 = 3, mu2 = 3, delta = 1, E2 = 3, THETA =
[10, 7, 4])".

In the md5-pinned instrument (gentow3_pe1_fresh.py L363–364):
`P12 = [-2, 0, 1]  # x^2 - 2` and `FRM3 = Fr('FR-M3', 2, P12, 1,
2, 1, 1, 3, 3, [-2, -2, 1])` — Phi' = x² − 2 and Phi2 =
x² − 2x − 2 (= Phi' − 2x = Phi' − n̂(3), consistent with u2 = 3).
The PE2 report and the instrument both carry Phi2 = x²−2x−2; the
S7.6 record hands that polynomial to Phi'. Every derived constant
in the sentence (δ, E2, Θ) is unaffected; record-accuracy only.
MINOR.

---

## CLEAN CHARGES (verified, nothing else at GAP-or-worse found)

1. **The r2 certificate reading re-derived from the (iv) proof +
   BOX-1 — CONFIRMED (the pin is right; the S0 compression is
   the sole defect).** The (iv) proof's minimal-branch display
   ("a single Phi'-extraction: height THETA_j …, NONZERO whenever
   the assembled K2-digit does not cancel") + BOX-1's "the key
   power's height-Θ K2-digit ≠ 0" support exactly: touched(j) ⟺
   assembled height-Θ_j digit ≠ 0 ⟺ key-power self-shadow pin
   = Θ_j. The r2 pin in the (iv) statement says precisely this,
   correctly rejects the weaker "ShC_j ≠ 0" reading (FR-M3 j = 0
   separates them: ShC_0 = 64 ≠ 0, pin 12, height-10 digit
   cancels), and is no weakening: it is the reading the composed
   proof already carried. S7.6's re-derivation paragraph is
   sound; its "first height sits strictly ABOVE THETA_j" is
   correct in its key-power context (and exact: FR-M3 measures
   12 > 10).
2. **The corrected sentences verified at the refuting instances +
   attainment rows; the certificate hypothesis still bites.**
   FR-M3 slack: replicated with THIS pass's own primitives
   (self-shadow [64, 40x, 12], pins [12, 7, 4] vs Θ [10, 7, 4] —
   slack at j = 0, attained at j = 1, 2, exactly the corrected
   S0 bracket). Count-law counter-member Phi2³ + 32: replicated
   (honest pin 10 = Θ₀ decided, shadow fake pins (1,7)/(2,4)
   strictly above the deciding edge — line values 20/3 and 10/3
   — hulls identical, vertex residues both 1, faithful; PARI σ
   {(6,1)} both routes): the corrected δ = 1 bullet and count-law
   sentences are TRUE on it. NON-VACUITY: at FAM-A5 the
   certificate HOLDS at j = 0 (sealed SHARP row, pin 21 = Θ₀)
   and (iv) fires; at this pass's fresh frame FR3-B7 the
   certificate holds at BOTH coordinates with two-slot digits
   (below) — the certificate hypothesis is live, not decorative;
   with F-1's witness on record its ROLE is correctly confined
   to the key power's own attainment, which is all (iv) claims.
3. **Count-law consumers under the new scoping.** GENTOW6_PROOF
   S3 (the one (iv)-adjacent consumer): consumes GENTOW-3(i),
   the Step-2 ledger, and BOX-1's FRAMING, certificate-aware
   throughout; its count box (GENTOW6-BOX-2) cites GENTOW-1's
   count-law remark (the product-measure ledger), NOT GENTOW-3's
   S4 paragraph; its quadrichotomy keeps entry-driven divergence
   a separate regime. GENHN_PROOF's "count strata priced through
   the shadow pins are systematically mislabeled" (L1304) is
   enumeratively scoped to the 35/35 compared supp rows —
   pre-GENTOW3 text, not a consumer of the S4 count-law.
   PROJECT_STATE append #12 records floor/band/x-free + BOX-1
   residue (no attainment overclaim; #13 logs the PE2 finding);
   BRIDGE_ADJUDICATIONS carries GENTOW3-BOX-1 as residue and
   GENTOW6's μ₂ = 2 closure. No downstream count assumes the
   whole-leaf-population reading. No consumer edit owed.
4. **The note as a system after r1+r2 (dated layers cohere).**
   The r2 diff (e1a8dfe..0d09d65) touches exactly the six
   manifest sites; the (iv) statement change is a disclosed
   definition PIN (no weakening — verified against the proof
   text); THEOREM (i)–(iii), COR 3.C, and the S1/S3/S5 displays
   sit outside the r2 edits. [GT3-r1] (gauge/f1 seam) and
   [GT3-r2] (certificate seam) are orthogonal and consistently
   dated; the header ARC block matches both reports' verdicts and
   hashes (3101cf5, c048154). All NINE machine-artifact md5s
   (six S7.5 + three PE2-fresh in S7.6) re-verified byte-frozen
   at HEAD.
5. **Supplier stack as of this read.** GENTOW1_PROOF @ 4caf0fb
   (TOWERRAT-R2 landed): GENTOW3's remark-(3) consumption of
   LEMMA GENTOW-5(a)'s cap is the strict-below-cap direction =
   BAND 1 of the three-band restatement, which explicitly
   preserves consumers riding "heights and band-1/2
   residue-determinacy" — consumption survives verbatim (PE2's
   adjudication re-confirmed at today's HEAD). GENTOW5 unit note:
   grep re-check — GENTOW3 cites LEMMA GENTOW-5 (GENTOW1's
   lemma) only; NO GENTOW5-unit clause consumed. GENTOW5 state
   read for this check: cc7c944 (post-PE4 rider, counter 1/2,
   PE5 pending) — commit line only, no clause touched. GENHN
   erratum 59c1966 pins verified in place at the S2/S7.1 sites.
6. **Free hunt.** S0/S1 constants re-checked against my own
   frame arithmetic (δ, E2, Θ_j at all five families + FR-M3 +
   FR3-B7); the corrected count-law's measured
   instances/non-instances all verified (PE1-1/PE1-2 σ-flips,
   A7@31, Φ₂³+32, PE1-0 parity luck); S7.6's consumer-walk and
   manifest claims verified; the r2 header/grade lines accurate
   (grade stays 0/2). Nothing further at any grade.

## MACHINE LEG (seals + isolated re-runs + audit)

All nine md5 pins match HEAD artifacts (runner 7cf3a7b8, output
926cd05f, PE1 fresh 35ad32aa / 94160b22 / RED 485946fc, import
base 6df4b213, PE2 fresh 55208baf / 06b4bddf / RED 3ca64bd4).
ISOLATED RE-RUNS (fresh scratch /tmp/gentow3_pe3_iso_20260810,
runners + import base only): sealed battery exit 0 GREEN 582/0,
3/3 teeth — output content-identical to the committed file modulo
ONE environmental gp stderr line (a parisizemax stack-size
warning printed on this node; all 582 check lines and the tally
byte-identical); PE1 fresh exit 0 GREEN 485/0 BIT-IDENTICAL
(94160b22); PE2 fresh exit 0 GREEN 149/0 BIT-IDENTICAL
(06b4bddf). Tautology spot-audit (PE1's full audit not
re-walked): confirmed the two reads run structurally different
routes in the sealed runner and Θ enters only as preregistered
constants; my fresh route reproduces the sealed SELF/SHARP
values with independently written primitives (dict-based polys,
own division loop), which is itself a non-tautology witness.

## FRESH ROUTE (gentow3_pe3_fresh.py — own primitives, disjoint
## from PE1's FR-M3/FR-D5/FR-GL sweeps and PE2's FR2-C1/δ=0/top)

Frames/probes: **FR3-B7** — δ = 7 (> 5; largest anywhere; battery
max 3, PE1 max 5) at an f2 = 2, η = 1 genre (p = 2, Phi' = x²−2,
e2 = 1, f2 = 2, u2 = 9, mu2 = 2, Phi2 = Phi'² − 16x·Phi' − 512,
E2 = 18, Θ = [43, 25]): the first frame whose key-power
certificate is LIVE at an INTERIOR coordinate with a TWO-SLOT
witness (ShC_1 lands on BOTH Φ'-slots at exactly Θ₁ = 25), and
the first δ ≥ 2 frame with a both-coordinate fake (faithful-band
members must additionally keep the fake j = 1 pin at Θ₁ OFF
their hulls — a hull interaction no battery row exercises, since
the only prior both-coordinate family, FAM-B, has δ = 1 = empty
band). Hand-derived BEFORE the run (two independent routes:
direct two-variable division on paper + the K2²-comparison
identity F ≡ K2² + 256Y³ mod (x²−2)): self-shadow ShC_0 =
2¹⁸·Phi' + 2²¹·x (slots 45/43, pin 43 = Θ₀), ShC_1 = 2⁸·Phi' +
2¹²·x (both slots 25 = Θ₁) — machine EXACT match, and the Θ₁
digit's Phi'-slot 2⁸ = (ĉ_{t*})²π^{2a_{t*}} agrees with GENTOW6's
CERT-TOP display (independent corroboration across notes).
**FR3-ENT** — the F-1 witness family at FR-M3 (above). Plus
faithful-band rows at pins 37/39/41 (width-6 band sampled at
three heights, PARI σ {(4,2)} each), divergence rows at exactly
43 (cancel-flavor: shadow j = 0 coordinate identically ZERO on a
member whose honest read is tRAM(43); pin-displacement flavor:
shadow 45 vs honest 43, σ intact = parity luck at δ = 7),
replication rows (FR-M3 self-shadow + Φ₂³+32), 195 FLOOR + 195
ENTRY coordinate checks on gated random members (seeded), and a
PARI leg (carrier ef {(2,2)} at FR3-B7 + σ on every decided row
displayed — all eight jobs at their preregistered exact values).
TEETH: T3-UP (Θ+1 mutant killed at the attained pins 43/25/10/43,
×8), T3-DOWN (Θ−1 mutant refuted by height-42 digit agreement on
all five Θ-adjacent rows + the measured first violations at
exactly 43 + PARI, ×5), T3-BICOND (the S0 only-if reading killed
by W-A/W-B at the certificate-cancelled coordinate, ×2).

RESULT (sealed pre-run at 16a729b, predictions P3-1..P3-8 + 3
teeth in the docstring): run 1 **GREEN — 451 checks, 0
violations, 3/3 teeth** (runner md5
bde312470601ede13338691573cfb96f, output md5
3ce4128ad44b360ecc8cea762b12af69); run 2 BIT-IDENTICAL. Witness
census: exactly 32 gated members with mindiff_0 = 10 at FR-M3
(the preregistered design count — the F-1 phenomenon is generic,
not tuned). Design-phase exploration (the witness search and the
PARI σ pre-computation) disclosed in the runner docstring; no
prediction changed after seal.

## GRADE LINE

**passPE3: NOT CLEAN — 1 GAP (F-1) + 1 MINOR (m-A); acceptance
counter stays 0/2.** Three of the four r2 sites are correct and
verified (the (iv) pin, the δ = 1 bullet, the count-law rewrite);
the machine stack is bit-stable in isolation; the fresh route
extends the theorem's confirmed geography to δ = 7 and to a live
interior-coordinate certificate. The owed repair is ONE sentence
plus a record fix: one-side the S0 geography clause (proved form:
certificate ⟹ failure AT Θ_j attained by the key power's own
shadow; certificate-cancelled ⟹ only the KEY POWER's first
divergence sits strictly above Θ_j — member failure AT Θ_j via
entry-digit-driven overflow is measured and committed at FR-M3
j = 0), and correct S7.6's Phi'/Phi2 attribution (m-A). Re-derive
from the note's own (iv) proof/scope remark (2)/BOX-1 — not from
this report's sentences.
