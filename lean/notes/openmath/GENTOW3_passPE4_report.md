# GENTOW3 passPE4 — FOURTH HOSTILE PASS (2026-08-10)

**Target:** `lean/notes/openmath/GENTOW3_PROOF_2026-08-09.md` at HEAD
(r3 round complete at 8165208; [GT3-r3] one-siding of the S0
geography clause + m-A S7.6 Phi'/Phi2 label fix + ARC line; diff
= 3 hunks in the one file, THEOREM (i)–(iv)/COR 3.C statement
displays untouched — verified on `git diff a1c181d..8165208`).
**Arc:** seal → PE1 NOT CLEAN 1G+3m (3101cf5) → r1 (e1a8dfe) →
PE2 NOT CLEAN 1G (c048154) → r2 (0d09d65) → PE3 NOT CLEAN 1G+1m
(a1c181d) → r3 (8165208) → this pass. Counter 0/2 entering.
**Protocol:** quote-and-classify, fix nothing. Fresh context, zero
prior stake. PE1's routes (Θ_j re-derivation ×2, the 485/0
instrument, supplier census), PE2's (gauge-annex re-derivation,
FR2-C1, δ = 0 corner), and PE3's (the r2 four-site walk, FR3-B7,
the W construction) are NOT re-walked. Surfaces: (1) the r3
one-sided sentence re-derived from S2 + a grep walk for surviving
only-if forms; (2) three-repair-layer coherence; (3) the count-law
and consumers under the final scoping; (4) free hunt — plus a
fresh machine route disjoint from all five prior frames and both
constructed members.
**As-of states read:** GENTOW1 accepted (4caf0fb TOWERRAT-R2 era,
not re-walked); GENTOW5 ACCEPTED 2/2 @ 43b42e9c (NEW since PE3 —
consumption re-checked below: none gained); GENTOW4 mid-r1 @
eb526a0d (no GENTOW-3 cites); GENTOW6 @ 5fb8fd7f (verdict commit,
byte-unchanged since PE3's consumer walk); GENHN erratum 59c1966
pins present at the S2/S7.1 sites.

## VERDICT: CLEAN — 0 GAP, 1 MINOR (m-1, record-discipline only).
## Nothing at GAP-or-worse survives. Counter 0/2 → 1/2.

The r3 one-siding is CORRECT and COMPLETE: the S0 clause now
asserts exactly the conjunction {(ii)'s band; (iv)'s if-direction;
floor + digit-cancellation ⟹ key-power slack; an existential,
measured member-failure clause} — each part re-derived from the
note's own Step 0–2/(iv)/BOX-1 (Section A) — and a grep walk over
every "exactly"-flavored attainment sentence finds NO surviving
only-if. The three dated layers cohere; the count-law consumers
owe no edit; the eleven artifact md5s match and all four committed
instruments re-ran GREEN in isolation. The fresh route (first
e2 > 1 verifier leg; first e2f2 = 4 frame in ANY leg, battery
included) is GREEN 208/0 with run 1 first-try and every
preregistered value exact — the theorem's geography extends
unbroken to the new slot grid.

---

## FINDINGS

### m-1 (MINOR — the r3-adopted W measurement carries no in-note
### artifact pin; no numbered r3 record section)

QUOTE (S0, the r3 clause): "and measured: the member f = Phi2³ +
2x·Phi2² + 8·Phi2 + 64 at FR-M3 j = 0 has first disagreeing
height exactly 10 = THETA_0 … (GENTOW3_passPE3_report.md @
a1c181d, the W rows)".

The note's own adoption discipline (r1: S7.1 adopted FR-GL with
"runner md5 35ad32aa, output md5 94160b22"; r2: S7.6 adopted the
PE2-fresh artifacts with all three md5s) is not extended to this
r3 adoption: the W measurement's instrument (gentow3_pe3_fresh.py,
md5 bde31247 / output 3ce4128a) is named nowhere in the note — the
citation points one hop away, at the report commit that carries
the pins. Likewise r3 ships no numbered repair-record section (the
record lives in the ARC paragraph + the two inline brackets),
where r1 and r2 each shipped an S7.x with an edit manifest.
NOTHING FALSE IS STATED: the report commit is pinned, the
artifacts byte-match their report pins at HEAD (verified), and
this pass replicated the W values on independent primitives
(REP rows, GREEN). Record-accuracy/traceability only. MINOR.
(Repair, if folded: add the two md5s at the S0 cite or in a short
S7.7 manifest — re-derived from the committed artifacts, not from
this report's sentences.)

---

## SECTION A: THE r3 SENTENCE RE-DERIVED (surface 1) — CLEAN

The corrected S0 clause makes four claims; each checked against
what S2 proves, independently re-derived:

1. "at the coordinates carrying (iv)'s certificate … it DOES fail
   at THETA_j: there the floor is attained by the key power's own
   shadow" — the IF-direction. PROVED BY S2: for f = Phi2^{mu2}
   the composed division is exact (C_j = 0, honest DRAIN); Step 2
   floors every discrepancy at Θ_j; all ≥ 2-extraction and recarry
   branches sit at ≥ (mu2−j)E2 + kδ with k ≥ 2, i.e. ≥ Θ_j + δ >
   Θ_j, so the content at EXACTLY Θ_j is the single-extraction
   assembled K2-digit — the certificate object. Certificate at
   (genre, j) ⟺ that digit ≠ 0 ⟹ ShC_j − C_j = ShC_j carries a
   nonzero digit at exactly Θ_j ⟹ the reads disagree AT Θ_j,
   witnessed by the key power (and kept by (iv)'s deep
   perturbations). Exactly what the clause says; no excess.
2. "Where the digit cancels, what is proved is only that the KEY
   POWER's own first divergence sits strictly above THETA_j" —
   correct and essentially forced: by the same decomposition,
   cancellation removes the only possible content at Θ_j, and (i)
   floors the rest; "only" correctly scopes to attainment (the
   sub-Θ band protection stands, stated in the clause's first
   half). Measured instance accurate (FR-M3 pins [12,7,4],
   replicated on this pass's primitives).
3. "other members can still fail at exactly THETA_j there,
   through scope remark (2)'s entry-digit-driven overflows —
   priced by the same Step-2 ledger at the same floor" —
   EXISTENTIAL + measured, and the pricing claim checks: the W
   member's digits sit ON the side (wt = mu2·E2), one
   Phi'-extraction adds δ = 1, landing at 9 + 1 = 10 = Θ_0 —
   Step 2's first bullet with parent at the membership floor and
   k = 1. Measured values in the clause all verified on this
   pass's own primitives (first disagreeing height 10; vertex
   undercut (0,12) → (0,10); the displayed sextic expansion is
   byte-exact).
4. THE GREP WALK (does any other sentence still carry the refuted
   only-if?): every "exactly/EXACTLY/iff/only-if" site near
   attainment or certificate language walked — S1 READ OFF (i)
   (L116: "pin lands at Θ_j EXACTLY at every non-x-free family")
   is FAMILY-enumerative over the five machine-checked families,
   where the certificate holds at every touched coordinate
   (SHARP rows) — measured, not a genre-general only-if; S2 (iv)
   statement (L193) carries the r2 certificate pin, if-direction
   only; S4 δ = 1 bullet (L344 "EXACTLY the criterion's first
   violation") is scoped to FAM-A5's measured PE1 rows; S4 L364
   quotes the refuted biconditional as history, marked REFUTED;
   S7.6 L685 ("attainment AT THETA_j at exactly the
   certificate-carrying coordinates") is the r2 record's
   KEY-POWER-scoped sentence — true for the key power (whose
   attainment IS biconditional in the certificate, by 1+2 above)
   and already adjudicated by PE3; the r3 layer consistently did
   not touch it. NO surviving only-if about read-failure or
   member population. The ARC r3 paragraph's history sentences
   are accurate against the diff and the PE3 report.

## SECTION B: THREE-LAYER COHERENCE (surface 2) — CLEAN

The r3 diff touches exactly: (1) the header ARC block (new PE3/r3
paragraph); (2) the S0 geography clause; (3) the S7.6 FR-M3
record bullet (m-A). The [GT3-r1] gauge seam (chat_t pin, x-free
scope, BOX-2 rider, FR-GL row), the [GT3-r2] certificate seam
((iv) statement pin, S4 δ = 1 bullet, count-law, S7.6), and the
[GT3-r3] one-siding are pairwise orthogonal and consistently
dated; the S0 clause's rewritten r2-bracket preserves the r2
history accurately (the "overflow genres" defect) while the
FR-M3 slack measurement moved from the bracket into the live
text — content-preserving (checked against the r2-era text in
the diff). All six ARC hashes (3101cf5, e1a8dfe, c048154,
0d09d65, a1c181d, 8165208) match `git log`. m-A verified at the
pinned instrument (gentow3_pe1_fresh.py L361–363: P12 = [-2,0,1]
= x²−2 is Phi'; FRM3's Phi2 = [-2,-2,1] = x²−2x−2 = Phi' − 2x);
every derived constant in the r2 bullet (e2 = f2 = 1, u2 = 3,
mu2 = 3, δ = 1, E2 = 3, Θ = [10,7,4]) re-derived and unaffected.
Statement displays: the (i)–(iv) and COR 3.C texts appear in no
deleted or inserted line of the r3 diff.

## SECTION C: COUNT-LAW + CONSUMERS UNDER THE FINAL SCOPING
## (surface 3) — CLEAN

The S4 count-law paragraph (one-sided: corruption ONLY on
consulted-height ≥ Θ_j members; reaching Θ_j NECESSARY, not
sufficient; corruption further needs the divergent digit present
AND consulted at the pricing granularity) is TRUE on every row of
this pass's fresh frame as well: B57/B58 (consulted heights 57,
58 < 59: faithful, PARI-exact σ {(8,2)} — the protected width-2
band at δ = 3); DIV67 (consulted height 67 ≥ 59: pin-corrupted
(0,67) → (0,59) yet σ-intact {(8,2)} both routes — a NEW
parity-luck instance at e2 = 2: necessary-not-sufficient
measured); CANCEL (consulted height 59 ≥ 59: maximally corrupted
— the shadow's j = 0 coordinate is identically ZERO, pricing a
Phi2-split against a PARI-irreducible {(8,2)} truth). Consumers
at HEAD: GENTOW6_PROOF @ 5fb8fd7f (byte-unchanged since PE3's
walk, pre-r3) consumes GENTOW-3(i), the S0 constants, and BOX-1's
certificate FRAMING, proves its own certificates (THEOREM 6.3),
and keeps entry-driven divergence a separate regime — it cites
neither the S0 geography clause nor the S4 count-law paragraph;
its "GENTOW-3's trichotomy" consequence block is
certificate-conditional throughout. PROJECT_STATE (appends
through the B15 dispatch record) carries the r2-over-correction
history and the owed r3 accurately, no overclaim.
BRIDGE_ADJUDICATIONS (L14981–15140) records the PE2/PE3 verdicts
and GENTOW3-BOX-1 as residue with GENTOW6's μ₂ = 2 closure — all
consistent with the r3 text. GENTOW5: the note's three
"GENTOW-5" sites (L51/302/467) are LEMMA GENTOW-5 in GENTOW1's
note (the Band-1 strict-below-cap consumption PE2 adjudicated);
NO GENTOW5-unit clause consumed, none gained since PE3 despite
GENTOW5's new 2/2 acceptance (43b42e9c). No consumer edit owed.

## SECTION D: FREE HUNT — nothing further at any grade

The S0/S1 constants re-checked through this pass's own frame
arithmetic (the general formulas δ = u2 − e2D'h, E2 = e2f2u2,
Θ_j = (mu2−j)E2 + δ instantiated at FR4-E22 give (3, 28, 59, 31),
confirmed by machine attainment — a sixth independent
instantiation of the S0 display); the W-B sextic in S0 expands
byte-exactly; the ARC "all nine md5s re-verified" claim verified
(and the two PE3-fresh pins besides); the grade-box lines ("0/2,
PE4 next") accurate; the S5/S7 records unchanged by r3 as
claimed. RECORD-RULE spot-check on the r3 insertions: the new
sentences are certificate-scoped, family-scoped, or existential
with named witnesses — no bare universal record sentence.

## MACHINE LEG (seals + isolated re-runs + audit)

All ELEVEN artifact md5s match their pins at HEAD (sealed runner
7cf3a7b8 + output 926cd05f; PE1 fresh 35ad32aa / 94160b22 / RED
485946fc; import base 6df4b213; PE2 fresh 55208baf / 06b4bddf /
RED 3ca64bd4; PE3 fresh bde31247 / 3ce4128a). ISOLATED RE-RUNS
(fresh scratch /tmp/gentow3_pe4_iso_20260810, runners + import
base only, all exit 0): sealed battery GREEN 582/0 3/3 teeth —
all 582 check lines and the tally byte-identical, the only diff
the same environmental gp parisizemax stderr line PE3 recorded on
this node; PE1 fresh GREEN 485/0 BIT-IDENTICAL; PE2 fresh GREEN
149/0 BIT-IDENTICAL; PE3 fresh GREEN 451/0 BIT-IDENTICAL (first
independent re-run of the PE3 instrument since its verdict).
Tautology spot-audit: this pass's instrument computes the honest
read by integer Phi2-division and the shadow by two-variable
K2-division with mod-Phi' reduction — structurally different
routes, Θ entering only as preregistered constants — and its
from-scratch primitives reproduce the sealed FR-M3 values
exactly (REP rows): an independent non-tautology witness on top
of PE1's negative audit (not re-walked).

## FRESH ROUTE (gentow3_pe4_fresh.py — own primitives, disjoint
## from FR-M3/FR-D5/FR-GL/FR2-C1/FR3-B7 and both constructed
## members)

**FR4-E22** — the first e2 > 1 frame in any verifier fresh leg
(PE1/PE2/PE3 all ran e2 = 1; the battery's e2 ∈ {2,3} rows are
imported GENTOW1 families never re-run on independent
primitives), and the first frame ANYWHERE with e2f2 = 4: p = 2,
Phi' = x²−2, e2 = f2 = 2, u2 = 7, mu2 = 2 (mu1 = 8), K2 =
Y⁴ + 8xY² + 128, Phi2 = Phi'⁴ + 8xPhi'² + 128, δ = 3, E2 = 28,
Θ = (59, 31) — new recarry threshold (Phi'-exponent 4), new slot
grid (dv2 = 4v + 2a + 7b, two slots per height class). Self-
shadow hand-derived BEFORE the run on two paper routes (direct
division; the identity F ≡ K2² + 64Y⁵ mod Phi' with shadow(K2²
red) = DRAIN): Ĝ1 = 64Phi' (pin 31 = Θ₁), Ĝ0 = −512xPhi'³ −
8192Phi' (BOTH slots at 59 = Θ₀ — the first two-slot Θ-digit
with distinct Phi'-exponents b = 3, 1; certificate LIVE at both
coordinates) — machine EXACT. Constructed rows: faithful band
sampled at BOTH heights 57 = Θ₀−2 and 58 = Θ₀−1 (δ = 3 width-2
band, PARI σ {(8,2)}); DIV67 (undercut (0,67) → (0,59), mindiff
exactly 59, σ-parity-luck); CANCEL (shadow coordinate identically
zero vs decided irreducible truth — the sharpest witness at
e2 > 1); 40 seeded gated members × 2 coordinates FLOOR + ENTRY
clean (160 checks); FR-M3 REP rows (key-power pins [12,7,4] with
the height-10 digit measured ZERO; W-A/W-B replicated: shadow
[10,6,3], mindiff [10,9,4], the S0 vertex undercut) — the r3
sentence's machine face on this pass's own primitives; 7 PARI
σ/carrier jobs (design-phase probe disclosed in the docstring),
all exact. TEETH (all fed by in-run measured values): T4-UP
(Θ+1 mutant killed 6/6 at the attained pins/mindiffs), T4-DOWN
(Θ−1 mutant refuted: measured ZERO self-shadow content at 58 +
every FR4 first divergence at exactly 59), T4-BICOND (the
retired r2 only-if killed: measured certificate cancellation at
(FR-M3, 0) + two members measured failing at exactly 10).

RESULT (sealed pre-run at cbb534b, predictions P4-1..P4-8 + 3
teeth in the docstring; syntax-check only before seal): run 1
**GREEN — 208 checks, 0 violations, 3/3 teeth — FIRST TRY** (no
RED round; runner md5 5cdd4dc6c9189e6a249c959d02bd7d32, output
md5 4c6b01abbd6b0ad55420747f6e8df1fb, verdict commit 4b0f5f3);
run 2 BIT-IDENTICAL. Tally: SELF 6, BAND 10, DIV 4, CANCEL 5,
FLOOR 80, ENTRY 80, REP 13, GP 7, TEETH 3. Every preregistered
exact value confirmed; no prediction changed after seal.

## GRADE LINE

**passPE4: CLEAN — 0 GAP, 1 MINOR (m-1); acceptance counter
moves 0/2 → 1/2.** The r3 one-siding says exactly what S2 proves
plus what is measured, no other sentence carries the refuted
only-if, the three repair layers cohere, the count-law's
consumers owe nothing, the full machine stack is bit-stable in
isolation, and the fresh route extends the confirmed geography
to e2 > 1 / e2f2 = 4 with a first-try GREEN. The one MINOR is a
traceability fold (pin the PE3-fresh artifact md5s where S0
adopts the W measurement, or add a short r3 manifest) — it does
not touch any mathematical or measured claim. One more CLEAN
pass accepts the note at 2/2.
