All verification complete. Findings report follows.

---

# Hostile pass 4 (deciding) — H1_BM1_2026-08-06.md REVISION 3

**Independence declaration.** Fresh context; no ledger/report files consulted. Evidence: the note itself, the four cited Lean corpus files, /tmp/litunit/fgmn.txt (the extraction the note cites; the repo-path copy `lean/notes/openmath/fgmn.txt` does not exist — the note names /tmp/litunit as its source, so this is not a defect), and an independent rerun of the gate.

## Charge 1 — (KPa) re-derived from the displayed transport: SOUND

From (2′) at f := Φ_{i+1}: in(Φ_{i+1}) = ϕ_{γ̂}·R^gr_{γ̂}(in Φ_{i+1})(ȳ). (K1) makes in(Φ_{i+1}) a legal nonzero weight-γ̂ argument; (K2) substitutes U₀·ψ; γ̂ = e·g·γ_{i+1} ∈ eℤ plus (12-int) uniqueness (s·h ≡ γ̂ ≡ 0 mod e, gcd(e,h)=1 ⟹ s(γ̂)=0, u = g·γ_{i+1}) gives ϕ_{γ̂} = p^{g·γ_{i+1}}; K_i-linearity of evaluation lands U₀·ψ̂_hom. The converse is determined: ϕ_{γ̂} invertible (p a unit) and z ↦ ȳ injective (clause (0)). I also re-checked the supporting arithmetic: hom^m multiplicativity/injectivity (weights e·(deg c)·γ_{i+1} with γ_{i+1} = eV + h ≥ 1 separating degrees), the S3.2 chain ((SL-dev) exponent identity g·γ_{i+1} − hκ − eκV = (g−κ)γ_{i+1}; every stride slot attaining γ̂; sum of classes = U₀ϕ_{γ̂}ψ(ȳ) ≠ 0 in the polynomial ring Δ), and the U₀ = 1 remark (monic division forces top digit C_{eg} = 1; slot eg attains γ̂; x^{eg}-coefficient match p^{egV} = U₀·p^{g(eV+h)−hg} in the free (PRES) basis). All fired inputs are declared (GR-B-STR)/ladder clauses with conditionality displayed (S8 chain). No defect.

## Charge 2 — OP-B1 at the (K0) class: quantifier-complete, no residual hole

- **LOW-CONST**: deg C < deg Φ_i ⟹ single populated slot 0 ⟹ w(C) = e·w_i(C) definitionally; e | w(C) ⟹ s = 0 by (12-int); (0″)'s abscissa clause (s(γ)+κe = κe ≥ e ≥ 1 for κ ≥ 1, unpopulated ⟹ zero coefficient) plus Cor 4.4(1) nonzeroness gives a nonzero constant; (2′) reconstructs in(C) = c·p^{w_i(C)}, x-degree 0. Sound, including at e = 1.
- **IN-FORM**: (a) gcd(e,h) = 1 stride congruence correct; (b) off-S tail strictly heavier (its development IS the off-S digits), quotient-map additivity, and (c) distinct t ∈ S give distinct x-exponents in the free basis — no cancellation. Sound.
- **OP-B1**: deg B < e·g·m ⟹ t_max ≤ ⌊(egm−1)/m⌋ = eg−1 ⟹ x-deg in(B) ≤ eg−1 < eg = x-deg ψ̂_hom (ψ monic); in the (PRES) domain x-degrees are additive and ≥ 0, so every multiple of ψ̂_hom has x-degree ≥ eg. The homogeneous/inhomogeneous-divisor worry closes (graded domain: a divisor of a homogeneous element by a homogeneous element is homogeneous; and the x-degree argument works for arbitrary δ anyway). Statement and proof are genuinely Φ_{i+1}-free.
- **Quantifier closure at (K0)**: the theorem's minimality bound is deg Φ_{i+1}, which (K0) pins to e·g·m — exactly OP-B1's quantifier ∀ nonzero B, deg B < e·g·m. The e·g = 1 corner: (K0) pins deg Φ_{i+1} = deg Φ_i, the window is the LOW-CONST window, x-degree 0 < 1; no case split, no silent re-reading (the rev-0 defect is gone). I hunted for a consumer firing the minimality reading off-class: §S8's consumed classes are all (K0)-conformant per the sources I could check (the D8 shape + `Stage.hmonic`, both verified below; Cor 4.12's supply, verified verbatim). No hole found.
- **(PRES)** itself re-verified: the elimination K_i[y,p^{±1}][x]/(x^e − p^h y) → K_i[p^{±1}][x] is an isomorphism (division basis y^a x^b, 0 ≤ b < e ↦ x^{ea+b}p^{−ha}, distinct exponents by uniqueness of division); UFD as a localization of K_i[p][x] at p; weights a·h + b·e cohere with the TR-1-corr rescale (E_{i+1}·λ_r = h, E_{i+1}/e(µ_{r−1}) = e_i — checked).

## Charge 3 — PAR-MON and its corpus pins: CONFIRMED

Math face: base (seed monic, degree ≥ 1) + step (the S3.2 count run one level down: top stride Φ_j^{e_j g_j} monic of degree e_j g_j m_j at monic parent; lower strides ≤ (m_j −1) + e_j(g_j −1)m_j < e_j g_j m_j) — elementary, no (KP)/valuation input, hence no circularity with the block it serves. The step's premise (every frame key is an (SL-dev)+(SL-top)-shaped standard lift) is a displayed consumed fact of the frame construction (S1.1: "frames … built by lawful reads per GD23 §1.2"; the ladder display already threads "monicity one level down"), not a hidden input. Corpus pins, all read this pass:

- `lean/LeanUrat/Moves/Defs.lean:124`: `hmonic : Φ.Monic` with `hdeg : 1 ≤ Φ.natDegree` at 125 — verbatim; a FIELD of `Stage`, as claimed.
- `lean/LeanUrat/HC1/T1_baseStage.lean:1546–1547`: `Φ := Polynomial.X`, `hmonic := Polynomial.monic_X` — base discharge verbatim ("literally X" confirmed).
- `lean/LeanUrat/MovesD/R7_ramifiedForge.lean:118`: `hmonic := HK47.transHyp_gate.hmonic` — per-transition re-supply confirmed.
- `lean/LeanUrat/HC2/Defs.lean:181–191`: `IsReadLift` ends `Φhat = σ.Φ ^ (e' * g) + ∑ k ∈ Finset.range g, tt k * σ.Φ ^ (e' * k)` — verbatim the note's quote; top coefficient literally 1, digits `inC σ.Φ`; sum over k < g so D_g = 1 is hard-wired. The rev-3 softening ("shape TOGETHER WITH `Stage.hmonic`") is the correct reading of this formula.
- `OM/Classifier.lean:315` `liftKey_monic`, `OM/LiftKeyExt.lean:95` `liftKeyExt_monic` — the S8 engine-lift monicity theorems exist as claimed.

## Charge 4 — DISCHARGED claims' dependencies: no leak onto the open lemmas

Line-by-line inputs of S2/S3.1/S3.2/S5/S6.1/S6.2 are exactly (0)/(0′)/(0″)/(1)/(2′), (V1)_{i+1}, POS-g(iii), the §1.2 development definition, and the three new FGMN pins. Nothing fires TR-3-ORD-X, TR-3′-GEN, (GR-B-KEY)(3)/(4), the 𝒟-clauses, properness/TR-6, or (2′)'s "∼R_λ" leg — the S9(iii) audit matches my read. The GMN-convention (KPb) face is excluded from the theorem and displayed open at S6.3. (s4a)-VOID is correct: Thm 4.13 presents Gr(µ) itself, and fgmn.txt:1140 confirms "H_{µi}(φ_k) is a unit in Gr(µ_i)" and p_i ∈ Gr(µ_i)^* — no localization exists to avoid. FGMN pins verified verbatim at the stated lines: Cor 4.12 (including the source's genuinely inert "for any α ∈ Γ(µ)" — the conclusion is α-free and the printed proof opens "Denote α := e_r f (w_r + λ_r)", exactly as the rev-3 quotation-repair display says; only the α-free claim is consumed), Thm 4.13 (+ its proof's independence/minimality-via-gcd argument), Cor 4.14 (+ the consumed direction's quotient-domain argument — I independently re-verified it: gcd(e_r,h_r) = 1 makes the graded pieces of F′[p^{±1}][x]/(x^{e_r} − p^{h_r}z̄) one-dimensional and z̄ ≠ 0 ⟺ ψ(0) ≠ 0, so it is a domain). The Prop 5.7 REJECTION is accurate: fgmn.txt:1769–1780 shows it is the five-way equivalence R(φ)=R(φ′) ⟺ φ∼_µφ′ ⟺ …, i.e. key-equivalence, not a low-degree block; Lemma 5.3 (fgmn:1714) is KP(µ)-membership, correctly described as adjacent and not consumed. S3.3's retarget constant rides the GRB S2.1/RESCALE identity — a GRB-leaf item, declared three times (S1.1 TR-5 line, S3.3 inline, S9(iv).4) and covered by the displayed wholesale GRB conditionality (0/2 clean, N-1 owed); the S9(i) consumed-list not naming RESCALE separately is below finding threshold given those displays.

## Charge 5 — counterexample and gate honesty: CONFIRMED

The in-statement padding counterexample is valid: T with w_{i+1}(T) > γ̂ and deg T > e·g·m exists (e.g. π^N·x^{egm+1}, N large), in(Φ+T) = in(Φ) so every initial-form datum — weight, weight-read, side-read — survives, while B := Φ (degree egm < deg(Φ+T)) has ψ̂_hom | in(B). Both (SL-dev)-only failure examples ((a) constant D_g = c ≠ 1 with U₀ = c̄; (b) D_g + J at m ≥ 2) check out. Gate: I reran `verification/openmath/kpstep_gate.py` this pass — output reproduces the note's record EXACTLY (336 instances; P1/P2/P3 = 0 violations; P4 = 120/151 nontrivially-twisted mismatches; 50 reducible naked residuals; first tuple (3,2,1,2,1,1), ψ₁=(2,2,1)↦(1,2,1) = (y+1)²). The code's loops match the stated perimeter (p ∈ {2,3,5}; (e₀,h₀) ∈ {(2,1),(2,3),(3,1),(3,2)}; e₁,g₁ ∈ {1,2,3} so e₁g₁ ∈ {1,…,9} including the corner). The docstring itself preregisters P1–P4, declares P3's builder/checker circularity, and instructs honest reporting on P4 — matching the note's scope paragraph. No proof step in S2–S6 cites the gate; "supports, never proves" is enforced in the text.

## Classification

No CRITICAL findings. No justification GAPS. Nothing survives: the rev-2 restriction to (K0)+(K1)+(K2) genuinely closes the pass-1 minimality hole with no residual quantifier gap; the rev-3 folds (PAR-MON with verified corpus carrier; the Cor 4.12 "Denote α" binding) are accurate against the sources; every discharged claim's dependency chain terminates in declared package clauses, verified pins, or displayed conditionality; the open items (lift conformance, GMN face/TR-3-ORD-X, B-M2, GRB's own bar) are fenced out of every proof and correctly priced in the residue lists.

VERDICT: CLEAN