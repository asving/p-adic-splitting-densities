# (K-CUT) unit-level hostile pass — the H8 fan-out adjudicated against the §3.1 row (2026-08-05)

**THE OWED PASS**: the ROOT §3.1 (H8) = (K-CUT) row (REVISION 15) holds the row REDUCED,
NOT discharged, with "discharge AWAITS that pass" = THIS unit-level hostile pass on the
H8 prover fan-out (the blueprint §7 fold note's "next gate"; the R2RootSync §A honesty
gate). Executed per the standing charge: quote-and-classify, falsifier-first, fix
nothing.

**Execution record**: fresh-context Codex, `codex exec --json`, thread
`019fbf6d-f5bb-7253-9fab-ede055846ae7`, detached from `/tmp/openmath` (prompt
`KCUT_pass_prompt.txt`, 177,107 bytes; raw stream `KCUT_pass.jsonl`; budget 12% → 13%
of the 7-day window). Inputs pasted in full: the §3.1 (H8) row verbatim; the ENTIRE
`lean/blueprints/HDISCHARGE_H8.md` (incl. §0 charter with the M04 consumer form (‡),
§6 discharge display, §7 review fold); the FULL charge-(vi) adjudication record
(`Scaffold/HDischarge/H8/R1Adjudication.lean`); and the COMPLETE Lean development —
`Shared/FirstEntrance.lean` + all ten `Scaffold/HDischarge/H8/*.lean` files (compile
status + Lean-core axiom footprints stated to the verifier as machine-checked; its
charge was the STATEMENT layer and the claim structure, sub-charges Q1–Q6:
declaration faithfulness, the three G-2 falsifier channels, the M04 consumption-site
form, the 1:1 residue audit against the row's displayed remainder, the charge-(vi)
use, documentary consistency).

## VERDICT (Codex, verbatim): CRITICAL

**What SURVIVED** (the declaration audit, verbatim): `firstEntAbove_unique`,
`read_cut_exhaustive`, `shallow_block_disjoint`, `block_block_disjoint` faithful;
`bdy_display` "correctly uses ProperPrefix; the entrance read remains shallow-side";
`siteExp_cut`, `fiber_count_cut` (full treeExp row list carried), `seriesSum_cut`,
`seriesSum_cut_conv`, `typemult_cut` faithful at their predicate-parametric scope;
`toy_cut_display` "genuinely nondegenerate"; finding 9: the ownership theorems
"genuinely exclude an unowned read and overlapping ownership … a unique ownership
partition for fixed (Tr,E)"; finding 8: `toCutData`/`cut_compat` faithfully construct
the constrained Equiv given the binder rows. The Lean development itself took no
mathematical hit.

**What FAILED — all findings land on the ROW'S DISPLAY, not the Lean** (7 CRITICAL +
5 GAP; full report below): (1) the `supplyDec` face of (K-CUT-s) is classically
trivial AND unused by `kcut_reduce` — "semantically vacuous and logically unused",
so the row's "enters kcut_reduce at its supplyDec face" gloss is not faithful;
(2) `kcut_reduce` never consumes `cut_injective` — the consumer-side forward map is
RE-ASSUMED (`shIdx`/`cutBlk` + conformance `shIdx_eq`/`cutBlk_car`), injectivity
riding the assumed `graft_cut`; (3) `ctblS`/`ctblD` are direct assumptions the row
does not display; (4) `W`/`weight` are arbitrary functions tied only by the AGGREGATE
product `mass_eq` — compensating factors satisfy it, weaker than (K-CUT-m)'s
factorwise measured semantics; (5) `real`/`vOf`/`verdict_key` carry no
realizability/faithfulness conditions — not the advertised "[3t]↦VTree realization";
(6) the row's "ENTIRE remaining content" claim is FALSE — undisplayed binder rows:
`ctblS`, `ctblD`, `k_card`, `ent_inj`, `ent_mem`, `shIdx_eq`, `cutBlk_car` + the
carrier rows `ShL`/`shape`/`k`/`ent`/`Deriv`/`dcar`/`shIdx`/`cutBlk`; (7) "Step 18
K1's exact form" is an over-claim — the conclusion has no (e_i, τ_i, δ_i), no
transported pool q^δ, no grammar block totals (the file's own honesty note admits
the onward `SolveSeam.series_tie` tie is not landed); (8) GAP: both inverse laws
(graft) assumed — displayed, honest; (9) GAP: "EXACTLY ONE decomposition" proved
only at the ownership-partition level as qualified; (10) GAP: the row omits R1's
(vi-b-1) NO headline and its ROUTING residue ("correspondence claim"); (11) the
count-form theorems are mislabeled "(K-CUT-2)" in the row (blueprint puts them under
(K-CUT-3)'s regroup; (K-CUT-2) = ownership); (12) GAP: "all 8 Lean units" — the
development has NINE Lean units (the blueprint's own table lists nine).

Codex's closing residue statement (verbatim): "The honest remaining mathematical
surface … includes, at minimum, the full `KCutReKeys` carrier/conformance/countability
construction, faithful corpus realization and verdict encoding, genuine cell-data
characterization of `E`, both graft inverse laws, factorwise measured/grammar-weight
identification including the transported-pool interface, and the onward identification
of `R.blockTotal` with the actual Step-18 block totals."

## Adjudication (manager, honest, per the pre-stated rule)

**NO DISCHARGE. NO RETIREMENT.** The pass verdict is CRITICAL, so the (H8) row
PERSISTS. The pass itself is now RUN — the R2RootSync §A honesty gate ("no unit-level
hostile pass has run") is SPENT, but it bought a REPAIR OBLIGATION, not a discharge:
every CRITICAL is a defect in the ROW'S OWN STATUS DISPLAY (over-claims and omissions
in the "REDUCED" text), none in the machine-checked mathematics. What a future ROOT
revision (r17 or later) would have to do — recorded here as FINDINGS, not executed,
not specced as an edit (the disposition rule reserves the edit spec for a DISCHARGES
verdict): re-display the (H8) row with (i) the FULL binder-row census of
`kcut_reduce` (incl. conformance, countability, and carrier rows) in place of
"ENTIRE remaining content"; (ii) "Step 18 K1's exact form" downgraded to "the
abstract convolution form; the typed (e,τ,δ)/pool/grammar-total tie is BP_V/M04-side,
open"; (iii) the `supplyDec` gloss removed or re-typed to a genuinely consuming face;
(iv) (K-CUT-m) displayed as the aggregate product row, factorwise identification open;
(v) the `real`/`verdict_key` rows displayed as unconditioned assumptions; (vi) R1's
(vi-b-1) NO + ROUTING residue restored; (vii) the (K-CUT-2)/(K-CUT-3) member labels
and the 9-unit count corrected. Any such edit is a ledgered ROOT revision with its
own VC pass; the acceptance-counter discipline applies.

---

## The Codex report, verbatim

The full row is not proved. `kcut_reduce` is a conditional abstract convolution theorem whose assumptions contain more—and in several places less semantic content—than the ROOT row displays. The ownership partition is genuine, but the claimed “ENTIRE remaining content” audit and “exact Step 18 K1 form” are false.

### Declaration audit

The following statements are faithful at their limited, predicate-parametric scope:

- `firstEntAbove_unique`, `read_cut_exhaustive`, `shallow_block_disjoint`, and `block_block_disjoint` establish uniqueness, exhaustiveness, and disjointness for an arbitrary predicate `E`.
- `bdy_display` correctly uses `ProperPrefix`; the entrance read remains shallow-side.
- `siteExp_cut` is the advertised finite-sum partition.
- `fiber_count_cut` is the advertised conditional count identity, carrying the full `treeExp` hypothesis list.
- `seriesSum_cut` and `seriesSum_cut_conv` correctly follow from a supplied `CutData`.
- `toy_cut_display` is genuinely nondegenerate.
- `typemult_cut` correctly partitions the `VTree.typemult` leaf multiset for arbitrary `E`.

The qualifications and failures below prevent those results from establishing the row.

1. Quote:

> “(K-CUT-s) entrance-predicate cell-data supply … enters `kcut_reduce` only at its under-typed `supplyDec` face”

Lean says only:

> `supplyDec : ∀ H : History p F, Decidable (E H)`

This does not say that `E` is cell data, is computed from `CellData`, or is the corpus entrance predicate. With classical logic it is available for every arbitrary predicate `E`, and no definition or proof in `toCutData` or `kcut_reduce` uses `supplyDec`.

Thus the formal face is both semantically vacuous and logically unused. Calling it the typed remaining face of the cell-data supply is not faithful.

Classification: **CRITICAL**.

2. Quote:

> “(K-CUT-1) level (1b) FORWARD half — the concrete (Ŝ,(D_ε)) extraction + cut-map injectivity (`cutOf`, `cut_injective`)”

Lean defines:

> `cutOf ... : ShallowData p F × (History p F → BlockDeriv p F)`

and proves:

> `(hB : ∀ ε ∈ minEnts Tr E, ...) : VTree.ext Tr Tr'`

This is not literally an indexed tuple over the entrance family, and `cut_injective` is not `Function.Injective cutOf`; its conclusion is the corpus relation `VTree.ext`, not Lean equality. `cutOf_injective` has the same `VTree.ext` conclusion.

More importantly, the consumer-side forward map is supplied afresh by assumed fields:

> `shIdx : ∀ σ, Fb.Tree σ → ShL`  
> `cutBlk : ∀ σ (T : Fb.Tree σ) ..., Deriv ...`

with assumed conformance:

> `shIdx_eq : ...`  
> `cutBlk_car : ...`

`kcut_reduce` never uses `cut_injective`. Consumer-level injectivity is instead embedded in the assumed inverse law `graft_cut`.

Therefore the concrete extraction result does not by itself deliver the forward/injective half used by `CutData`.

Classification: **CRITICAL**.

3. Quote:

> “countability transport”

The proved statement is only:

> `(h𝒯 : 𝒯.Countable) : ((fun Tr => cutOf Tr E) '' 𝒯).Countable`

It assumes countability of the source tree family. It neither proves that the realized type-σ family is countable nor supplies the consumer fields:

> `ctblS : Countable ShL`  
> `ctblD : ∀ Ŝ i, Countable (Deriv Ŝ i)`

Those fields remain direct assumptions in `KCutReKeys`. The ROOT row does not list them among the “ENTIRE remaining content”; instead it places countability under the proved-forward-half gloss.

Classification: **CRITICAL**.

4. Quote:

> “(K-CUT-m) measured mass tie = `mass_eq` + the `W`/`weight` rows”

The `W` and `weight` rows are merely arbitrary functions:

> `W : ShL → ℝ≥0∞`  
> `weight : ∀ Ŝ i, Deriv Ŝ i → ℝ≥0∞`

The only equation is the aggregate product identity:

> `Fb.mass σ T = R.W (R.shIdx σ T) * ∏ i, R.weight ...`

No statement identifies `W` individually with the measured shallow mass, any `weight` individually with grammar-derivation weight, or a block factor with the transported-pool quantity at `q^{δ_i}`. Compensating arbitrary factors can satisfy the aggregate product equation.

Thus the formal assumption is weaker than the displayed measured-factor semantics of (K-CUT-m).

Classification: **CRITICAL**.

5. Quote:

> “the [3t]↦`VTree` realization + verdict-keying rows (`real`/`verdict_key`)”

Lean assumes:

> `real : ∀ σ, Fb.Tree σ → VTree p F`  
> `vOf : SplittingType n → Multiset (ℕ × ℕ)`  
> `verdict_key : ∀ σ T, (real σ T).typemult = vOf σ`

`real` has no realizability, completeness, canonicity, faithfulness, or injectivity condition. `vOf` is arbitrary and has no equation to the corpus’s intended splitting-type encoding. `verdict_key` only says that the chosen image has the chosen arbitrary multiset.

Consequently these statements do not formalize the displayed “[3t]↦`VTree` realization” and canonical verdict keying at their advertised semantics.

Classification: **CRITICAL**.

6. Quote:

> “its binder rows are the row’s ENTIRE remaining content”

The complete binder/data surface of `kcut_reduce` is:

- Ambient binders: `n`, prime `p`, `F`, `X`, `Fb`, `E`, and `σ`.
- `KCutReKeys` rows:
  `supplyDec`, `real`, `vOf`, `verdict_key`, `ShL`, `ctblS`, `shape`, `k`,
  `k_card`, `ent`, `ent_inj`, `ent_mem`, `Deriv`, `ctblD`, `dcar`, `W`,
  `weight`, `shIdx`, `shIdx_eq`, `cutBlk`, and `cutBlk_car`.
- Additional binders:
  `graft`, `graft_cut`, `cut_graft`, and `mass_eq`.

The ROOT remainder displays `supplyDec`, the three graft rows, `W`/`weight`/`mass_eq`, and `real`/`verdict_key`. It does not display `ctblS`, `ctblD`, `k_card`, `ent_inj`, `ent_mem`, `shIdx_eq`, or `cutBlk_car`, despite those being specifically required conformance/countability assumptions. It also does not display the supplied carrier and map rows `ShL`, `shape`, `k`, `ent`, `Deriv`, `dcar`, `shIdx`, and `cutBlk`.

This directly falsifies “ENTIRE remaining content.”

Classification: **CRITICAL**.

7. Quote:

> “the formal bridge to the M04 consumer EXISTS as the CONDITIONAL theorem `kcut_reduce` (Step 18 K1’s exact form)”

The quoted consumer requires entrance/interface data and grammar totals:

> “entrance lists `((e_i, τ_i, δ_i))`”  
> “`W(Ŝ)·∏_i weight(D_i)`”  
> block totals at the transported pool `q^{δ_i}`

But `kcut_reduce` concludes only:

> `∏ i, R.blockTotal Ŝ i (vt.1 i)`

where `R.blockTotal` is the tsum of arbitrary `R.weight` over arbitrary `R.Deriv`. Its conclusion contains no `e_i`, `τ_i`, `δ_i`, transported pool, grammar block total, or equation to the M04/BP_V block solver.

The file itself admits:

> “The onward tie to `SolveSeam.series_tie`’s `Rval` … is BP_V/M04-side work, NOT landed here.”

Therefore `kcut_reduce` has the abstract convolution pattern, but not the exact instantiated Step-18 K1 consumer form claimed by the row.

Classification: **CRITICAL**.

8. Quote:

> `cut : ∀ σ, Fb.Tree σ ≃ {x : ... // σ0 x.1 + ∑ i, ... = vOf σ}`

and:

> `cut := fun σ => ⟨R.cutFun σ, graft σ, graft_cut σ, cut_graft σ⟩`

Within the abstract `CutData` vocabulary, `toCutData` faithfully constructs an equivalence with the constrained target. `cut_compat` genuinely proves the displayed constraint from `typemult_cut`, `shIdx_eq`, `cutBlk_car`, the entrance enumeration rows, and `verdict_key`.

But both inverse laws are assumptions. In particular, compatible-tuple realizability and uniqueness are not obtained from the ownership results. The full exactly-one statement therefore remains open at least through `graft`, `graft_cut`, and `cut_graft`.

Classification: **GAP** for the genuinely displayed graft obligation; the row’s implication that only surjective graft completeness remains after proved consumer-level injectivity is superseded by Findings 2 and 6.

9. Quote:

> “killing the G-2 falsifier channels (‘overlapping ownership, an unowned read, or two different shallow decompositions’) at the ownership-partition level”

`read_cut_exhaustive`, `shallow_block_disjoint`, and `block_block_disjoint` genuinely exclude an unowned read and overlapping ownership. `firstEntAbove_unique` excludes two first owners. Together with filter-defined extraction, they give a unique ownership partition for fixed `(Tr,E)`.

They do not exclude two abstract compatible decomposition tuples, unrealized compatible tuples, multiple abstract encodings of the same concrete data, or failure of grafting. Those are controlled only by the assumed conformance and inverse-law rows.

The ROOT wording is acceptable only because it expressly limits this claim to the “ownership-partition level.” It does not prove the row’s unqualified “EXACTLY ONE decomposition.”

Classification: **GAP**: full decomposition bijectivity/realizability remains open.

10. Quote:

> `VERDICT (vi-a): FAIL`  
> `VERDICT (vi-b-1): NO`  
> `VERDICT (vi-b-2): NO`

and:

> “Only outcome (vi-a) = PASS retires the row … (vi-a) = FAIL ⇒ NO retirement.”

The retirement disposition is accurately supported: (vi-a) failed, so retirement is forbidden.

However, the ROOT row reports only:

> “(vi-a) FAIL ⇒ NO retirement …; (vi-b-2) NO”

It omits the recorded `(vi-b-1): NO` headline and its separate ROUTING residue:

> “ROUTING classified CORRESPONDENCE CLAIM”

R1 says the ownership combinatorics were individually demonstrated, but consumer-level decomposition uniqueness and ROUTING were not. The row does not identify where the ROUTING residue was later discharged or into which typed binder it was absorbed.

Classification: **GAP** for the omitted adjudication residue; the no-retirement decision itself is faithful.

11. Quote:

> “(K-CUT-2) count-form (`siteExp_cut`, `fiber_count_cut`)”

The blueprint defines `(K-CUT-2)` as:

> “EXHAUSTIVE-EXCLUSIVE READ OWNERSHIP”

while the exponent regroup and count identity belong to the count form of the weight/product leg. The ROOT row relabels the count-form theorems as `(K-CUT-2)`, even though it already uses the ownership theorems for that member.

This is a documentary member-map inconsistency. It does not invalidate the Lean identities, but it prevents a clean 1:1 claim-chain audit.

Classification: **CRITICAL** under the requested documentary-consistency standard.

12. Quote:

> “all 8 Lean units + 2 documentary units LANDED”

The supplied H8 development contains nine H8 Lean units before the two documentary units: `CutDefs`, `CutFirst`, `CutPartition`, `Lemma33`, `SiteExpCut`, `CutSeam`, `CutExtract`, `ToyGate`, and `KCutReduce`. The blueprint’s own unit table likewise lists nine.

This is a documentary count error rather than a proof defect, but the composed ROOT text is not literally consistent with the supplied development.

Classification: **GAP** in the documentary ledger.

The honest remaining mathematical surface is therefore not exactly the ROOT display. It includes, at minimum, the full `KCutReKeys` carrier/conformance/countability construction, faithful corpus realization and verdict encoding, genuine cell-data characterization of `E`, both graft inverse laws, factorwise measured/grammar-weight identification including the transported-pool interface, and the onward identification of `R.blockTotal` with the actual Step-18 block totals.

VERDICT: CRITICAL
