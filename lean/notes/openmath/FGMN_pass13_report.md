Quote-and-classify:

1. **CONFIRMED — acceptance scope.** REVISION 9’s list (a)–(e), extended by REVISION 10 to include the exact compiled meanings of `juncforge_no_e2_junction`, `juncforge_deep_wall`, `juncforge_junc_bundle_false`, and `juncforge_D0_instanceFalse`, is internally complete and faithful to the revision record.

2. **CONFIRMED — perimeter honesty.** The earlier normative procedures are expressly demoted: “Every REVISION 4-8 perimeter formulation … none is normative.” REVISION 10 also supersedes the universal-sounding impossibility sentence with the narrower claim that the five attempted procedure classes failed. No surviving totality or universal-impossibility overreach remains.

3. **CONFIRMED — double scaling.** From the note’s stretch law `w = e·wPrev`, compiled `e·w(Bⱼ)+j·h` becomes `e²·wPrev(Bⱼ)+j·h`, whereas the print-derived functional is `e·wPrev(Bⱼ)+j·h`. The diagnosis is algebraically exact at genuinely graded read-pair stages.

4. **CONFIRMED — row A.**  
   \(f_A=(X+2)^2+24\), with slots \(B_0=24,B_2=1\) and parent weights \(3,0\). Corrected values are \(\{2·3,\,2·3\}=\{6,6\}\); compiled values are \(\{2·6,\,2·3\}=\{12,6\}\). The stated record and source-fires conclusion are consistently fenced as source-plus-numerics, not a Lean theorem or full-bundle construction.

5. **CONFIRMED — repaired any-\(\gamma'\) semantics.** From corrected equality
   \(e\,w_{\rm prev}(B_j)+jh=\gamma\), the compiled value is
   \(e\gamma-jh(e-1)\). Thus distinct supported slots give distinct compiled values when \(e\ge2\) and \(h>0\). Testing whether the supported values have cardinality at most one is the correct any-\(\gamma'\) criterion.

6. **CONFIRMED — disclosures and rescopes.** R1 is limited to window/support legs; R4 is explicitly differential and shares certified tower state; the §S8 block is identified as an edited render with raw-order divergence disclosed; “four-token” is superseded by the correct three identifier replacements.

7. **GAP — fresh execution unavailable.** My first repository read failed before execution with: “bubblewrap is unavailable.” Consequently I could not independently rerun hashes, the 1,093-check battery, direct compilation of the four theorems, or the 336-site from-note implementation. I do not count the prior model’s reported execution as my own. This is an infrastructure verification gap, not a mathematical or textual defect in REVISION 10. No files were changed, and the consecutive-CLEAN counter does not advance on this verdict.

FGMN-P13 FINDINGS: 0 critical, 1 gaps
VERDICT: GAPS-ONLY