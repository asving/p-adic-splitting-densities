The two REVISION-8 repairs are mathematically correct:

- S1.4 correctly restricts \(\phi_{h_i}=x\) to \(e_i\ge2\).
- S5.3 correctly handles \(e_i=1\) via \(U\mapsto\bar z\), yielding
  \(K_{i+1}[U]/(U-\bar z)\cong K_{i+1}\).

One remaining wording gap occurs at two live sites:

> “blanket invertibility would even make \(\bar y=x^{e_i}p^{-h_i}\) invertible” (S1.4)

and equivalently in R7.1:

> “blanket invertibility would even force \(\bar y=x^{e_i}p^{-h_i}\) invertible”

Classification: **justification/wording gap, not critical**. This argument silently assumes \(e_i\ge2\), when \(x=\phi_{h_i}\) is an anchor. At \(e_i=1\), every \(\phi_\gamma\) has \(s(\gamma)=0\) and is genuinely a unit, while \(x\) is not among those anchors and \(\bar y=xp^{-h_i}\) remains noninvertible. The correct per-case statement “\(\phi_\gamma\) is a unit iff \(s(\gamma)=0\)” is unaffected.

The remaining \(e_i=1\) sites are consistent. In particular, the later “abscissa-step generator \(x\)” language remains defensible because \(x=\bar y p^{h_i}\) and gauges to \(\bar z\); it does not identify \(x\) with the nonexistent nontrivial coset \(u_1\).

Fresh whole-leaf sweep found no critical error and no further justification gap.

The residue ledger is complete:

- TR-3′-GEN remains open.
- TR-3-ORD-X remains open.
- D-ITER remains open exactly for \(E_i>1\).
- N-1 remains owed.
- KP/H-CHAIN/[T] are explicitly external inputs.
- TR-3-ORD is consistently limited to its proved FGMN-internal role.

None of these is silently consumed as proved: the project \(R_\lambda\) consumers remain routed through TR-3′-GEN or TR-3-ORD-X, IFACE(c) remains open where D-ITER fires, and clauses (3)/(4) remain conditional on KP/\([T]\). No edits made. Clean counter remains \(0/2\).

VERDICT: GAPS-ONLY