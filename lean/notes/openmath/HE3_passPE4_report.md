# HE3 passPE4 — FOURTH FRESH-CONTEXT HOSTILE PASS (Fable, zero prior stake; THE ACCEPTANCE ATTEMPT)

**VERDICT: CLEAN — 0 CRITICAL, 0 GAP, 2 MINOR. The counter completes:
1/2 → 2/2. HE3 IS ACCEPTED.** The never-owned surfaces of this pass's
charge were re-derived end-to-end and HELD: the §S2/§S6.4 simple-case
(μ = 2) dictionaries (all three stage types re-derived from the label
calculus and machine-tied at four frames new to the arc), the (SEP)
index apparatus (re-keyed at r1, here re-derived sub-case by sub-case,
the [r1-N1] supplement's both branches re-proved, and the inversion
machine-run AS an inversion for the first time), the HE3-4/§S6.2
recentering composite (walked end-to-end through the [r2] coboundary,
including the anchor-consistency computation tying HE3-0's root residue
β_ρ = res(y_ρ/n(λ)(ρ)) to DEFINITION 1 [r2]'s coherent assembly), the
battery's check-family design (audited at code level against what §S8
and HE3-BOX-4b claim — they match), and the post-PE3 dated-correction
rider (both of its corrected lists recomputed exactly; the
conservativity argument verified). The fresh route ran GREEN (0
violations, 4/4 teeth) at geographies and reads disjoint from all prior
legs, including the wrap seam adjudicated in a NEW frame and
characteristic in BOTH flip directions, with PARI siding with the
coherent read both times. The two MINORs are display-locality defects
in the (SEP) block of the same genre as PE3's accepted-through M-1; no
conclusion moves.

**Pass metadata.** Target `HE3_PROOF_2026-08-08.md` at HEAD = 8ba8c87
(the post-PE3 dated-correction rider; body byte-frozen — the rider
commit is a pure EOF append, verified from the commit diff). Arc: PE1
(2C+4G+2m) @ 7950ee4 → r1 → PE2 (2G+3m) @ 6d0ff09 → r2 → PE3 CLEAN
(0C/0G/1m) @ c40cbf0, counter 1/2 → rider 8ba8c87. Protocol:
quote-and-classify, fix nothing. All three prior reports read; their
routes not re-walked.

---

## FINDINGS (ranked)

### M-A — MINOR. The [r1] "(SEP) holds at μ ≤ 3" sub-case enumeration
### has no header for the all-integer ≤ 2-side configuration; the
### closing argument for it is displayed one line up inside (a).

QUOTE (§S6.1 repair block): "Σ_λ ℓ_λ d_λ = μ, so at μ ≤ 3: (a) if some
slope is non-integral (ℓ ≥ 2, forcing ℓd ≥ 2), P(F) has at most TWO
sides, and two slope-classes are separated with no interior sample at
all — the root count ... gives c₁ + c₂ = 0 for free, and samples above
the top slope give c₁λ₁ + c₂λ₂ = 0 ... (b) if there are three sides
... (c) three consecutive integer slopes ...". The configurations with
one or two sides ALL of integer slope (e.g. every 2SIDED member — this
pass's FR-A frames run several) fall under none of the three headers:
(a)'s hypothesis fails, (b)/(c) require three sides. The two-relation
argument displayed inside (a) ("two slope-classes are separated with no
interior sample at all") closes them verbatim (one side: the free count
alone gives c = 0), so the derivation is complete in substance and no
conclusion rests on the omission. FAILURE SCENARIO: a Lean
transcription that cases exactly on the three displayed headers has an
uncovered case whose proof is the (a) tail; nothing false is provable.

### M-B — MINOR. (★★)'s root-side read at AT-SLOPE samples is consumed
### by (SEP)/sub-case (c) but displayed only in §S1 (ii), not in
### HE3-3's own step (2).

QUOTE (§S6.1 step (2)): "Fix κ **not a slope**, and any s"; QUOTE
((SEP)): the measurable index set includes "the slope integers λ at
which K^× contains a non-root of R_λ"; QUOTE (sub-case (c)): "sample AT
the middle slope a+1 with a letter s that is not the ... residual
root". Step (1) covers the STAGE side at such samples (HE3-1′'s
at-slope non-root clause), but the ROOT side of (★★) at a root ρ with
λ_ρ = κ needs the residue-mismatch argument, which is displayed only in
§S1 (ii)'s parenthetical ("at λ_ρ = κ the residues cannot cancel, since
β_ρ is a root of ι_ρ(R_κ) ... while ι_ρ(s) is not"). The justification
exists in the note; the locality is the defect. This pass's FR-B
machine-verified the at-slope equality at 12 samples (below). FAILURE
SCENARIO: a reader reconstructing HE3-3's proof from §S6.1 alone has an
unproven step at the (c)-sample; the cure is a cross-reference.

---

## CLEAN CHARGES (attacked, survived this pass's own verification)

* **§S2 + §S6.4, the μ = 2 dictionaries (surface 1, never the main
  target before).** Re-derived: Σ e_sf_s = 2 admits exactly {(2,1)},
  {(1,2)}, {(1,1),(1,1)}; the label calculus returns {(2e₁,f₁)} [one
  side ℓ = 2, u odd — d = 1 forced], {(e₁,2f₁)} [integer slope,
  irreducible quadratic residual, peel-free by the subtraction bullet],
  {(e₁,f₁)²} [two sides via HE3-3 alone, or one side two rational roots
  via the peel] — GENHN.C's dictionary verbatim, no circularity (the
  2×2-system description in §S2 is exposition of the SUPPLIER's method;
  §S6.4 does not consume it; the SPLITEQ λ₁ = λ₂ degeneration is
  correctly deferred to "after one refine step"). The μ = 3 degeneration
  claim recomputed: single side, three labels → second identity = λ ×
  first; the 10 spurious solutions of HE-T-UNDET = the C(5,2) ordered
  triples — reconfirmed in the isolated battery re-run. Machine: FR-A
  ties all three μ = 2 types at (4,1), (2,3), and two equal-char frames.
* **The (SEP) apparatus (surface 2).** The corrected index set (κ >
  D′h; measurable = non-slopes plus generic-lettered slopes) re-derived
  against HE3-1/1′/1L (every test height reaches all of K^×; the band
  S < κ ≤ D′h correctly excluded — the x²+18/x²+27 counter-instances
  re-checked from the display). Sub-cases (b)/(c) re-solved by hand
  (both interior-sample positions; the at-middle-slope sample) — det-1
  triangular systems. The [r1-N1] supplement re-proved: CASE A₀′ ≠ 0
  (dv(A₁′) = 2a+1 preserved — unique attaining index verified; δ ≥ a+3
  integer; {a, a+1, δ} with a+2 in the gap — invertible; class
  transfer exact); CASE A₀′ = 0 (left-shift development; two-slope
  polygon; n_{a+2} = D′ from Φ″'s own roots); termination in one step
  (neither branch reproduces a consecutive triple). Machine: FR-B runs
  the inversion AS an inversion on measured Ψ.
* **HE3-4 / §S6.2 end-to-end (surface 3, the [r2]-touched composite).**
  Walked: (i) the one-clause equivalence (three dv cases; the F5-
  repaired collapse residue −ι_ρ(s) ≠ 0); (ii) HE3-3 applied AT Φ″
  (invariant maintenance gives (T1)/(T2) at θ″; P(F″)'s slopes all >
  D′h — preserved-below + collapse + absorbed structure; (SEP) at Φ″ by
  the same μ ≤ 3 derivation); (iii) HE3-2(b)'s substitution re-computed:
  M_i·n(λ)^{i−j}/M_j = 1 identically, G″ = G(Z+s), order-m vanishing,
  above-λ length = m, the λ-side residual (Z+s)^T R_λ(Z+s)/Z^m with the
  collapsed root −s of multiplicity T — sign-coherent with [r1, F8];
  (iv) THE ANCHOR CONSISTENCY (this pass's own computation): the side
  part Σ_k A_{j₀+k}(ρ)Y^k under Y = n(λ)(ρ)·Z̃, divided by n(κ₀)(ρ),
  has residue ι_ρ(R_λ)(Z̃) with R_λ EXACTLY DEFINITION 1 [r2]'s
  assembly, and its roots are res(y/n(λ)(ρ)) — i.e. HE3-0's β_ρ read
  and DEF 1's coherent object are convention-matched with no residual
  Z ↦ cZ seam (the anchor swap n(c)/n(λ)^T vs n(κ₀) is one global
  scalar, type-preserving); (v) the μ ≤ 3 base-change-free block: the
  no-root-⟹-irreducible step for degree ≤ 3, the subtraction |S_r| =
  D′·deg r, disjointness by coprimality of distinct irreducible images;
  the starved corner needs no peel complement (all letters are roots —
  FR-C machine-runs exactly this). The stage-α onward read ([r1, F5]
  re-key) consumes only heights > λ of P(F″), where the collapsed
  material never appears — consistent.
* **The battery design (surface 4, code-level audit).** `leg_norm_psi`
  asserts HE-NORM as v_p(Res(f,Φ″)) = f₁·dv(B₀) — the HE3-1a identity
  in v-units, two independent computations (Bareiss resultant vs
  development read); HE-PSI asserts the INEQUALITY dv(B₀) ≥ generic
  plus (★) — exactly what §S8's text claims, no more (the equality
  clause has indirect cover via HE-SIG + the census tie, and now direct
  cover via FR-B); the (★) float tolerance is benign (integer-valued
  sums, error ≪ 1e-9); the κ-range starts at S+1 = D′h+1 legitimately
  (f₁ = 1 geography only, as §S1's scoping note states); HE-T-CAP keeps
  the genuinely buggy reader as a tooth; HE-T-NODE is the disclosed
  diagnostic. Sealed-vs-delivered matches HE3-BOX-4b's disclosure. No
  family asserts a tautology beyond the P4 case already disclosed at
  [r2, F-4].
* **The rider (the post-PE3 dated correction, verified as charged).**
  Both corrected lists recomputed: under d_λ ≤ 3, Q^{f₁} ∈ {2,3,4} ⟺
  (Q,f₁) ∈ {(2,1),(3,1),(2,2),(4,1)} — the (4,1) cure is exact (F₄
  bases are legal; PE3's FR3 ran one); under the in-context d_λ ≤ μ−1
  (the low side consumes ≥ 1 of the μ-budget while the sample is
  consumed — re-derived), Q^{f₁} ∈ {2,3} ⟺ {(2,1),(3,1)}, with (2,2)
  unreachable and (4,1) excluded. Conservativity: the fence consumes
  GENHN [r1] on the LISTED corner ⊇ the reachable corner. The
  1/2-holds-through-minors-only reading matches the program's bar
  (CLEAN = nothing GAP-or-worse) and PE3's precedent. Body byte-frozen:
  the rider commit diff is additions-only at EOF.
* **HE3-5's iff, necessity direction (PE3's suggested charge).**
  Re-verified: a pin failing both (i) and (ii) forces e₁N ≤ line(j) ≤
  m₁ = line(j₁), and convexity puts dv(A′_{j₁}) ≥ m₁ ≥ e₁N, so the
  LEFT endpoint is already uncertifiable — genuinely undecided; the
  boundary is strict (at e₁N = line(j), "≥ e₁N" cannot certify "> line").
* **§S1 (ii) the Ψ-comparison** (re-walked while auditing the (SEP)
  consumption): the hull-mean step, COUNT, R(κ) − κ·COUNT, the no-mid
  positivity, the mid case's forced {low 1, mid 2} with c_mid = −c_low
  = D′ ⟹ λ_low = λ_mid absurd, and κ = D′h+1 not a slope in the mid
  case — sound; the ψ-cancellation entry-side argument for dv(Φ′(ρ)) >
  D′h checks.
* **Cross-round coherence (surface 5).** §S0′'s eight-row and §S0″'s
  five-row tables match the PE1/PE2 reports' findings and dispositions;
  the §S3 Remark [r2] witness block is arithmetic-exact (recomputed:
  W = (0,1,1), ã = (1,η,η), disc 1+η ∉ (F₉^×)², the Z ↦ −Z
  reconciliation with PE2's display); HE3-BOX-1's arc line matches the
  actual arc; the [r2] coverage-note addendum correctly cites
  he3_pe2fresh.py at 6d0ff09.

## MACHINE LEG (pins, isolated re-runs, tautology audit)

* **Pins at HEAD**: `he3r1_supp.py` md5 93681e8d47d447dcbfd46db0ddfe0c55
  = the §S9′ [r2] pin; committed supp output md5
  65ed06e49f4c54abf1b143513633f143 = the pin; `he3_checks.py` and both
  battery artifacts last touched at 872327b (pre-r1), per git log —
  byte-frozen through r1/r2/rider.
* **Isolated re-runs** (`/tmp/he3pe4_iso`, runners copied with import
  closure): battery — VERDICT GREEN, 0 violations, 17.0 s, stdout
  BYTE-IDENTICAL to `he3_checks_output.txt`, results JSON md5
  1b679cd23e337f3e45cfb72ade74fffe = the committed artifact
  bit-identical; supp — exit 0, stdout BYTE-IDENTICAL; `he3_pe2fresh.py`
  — exit 0, GREEN 0 violations, FR-A/B/C/D content matching PE2's
  report (ma → {(2,4)}; HE3-1L sizes {2,2,8,8,8,8}; FR-D both
  branches); `he3_pe3fresh.py` — exit 0, stdout BYTE-IDENTICAL, results
  JSON content-identical. NOTE (observation, not a finding): the
  battery's import closure spans ~20 sibling modules (w12 → w10 →
  iterlawn/grt_jc/... chains) — a "runner copied alone" re-run fails on
  ModuleNotFound; and `he3_pe2fresh.py` has NO committed output
  artifact (PE2 committed the runner only), so its evidence rests on
  PE2's report plus re-runs like this pass's.
* **Tautology audit**: battery families audited at code level (above) —
  decorrelation as PE1 found it, plus the two design softnesses (HE-PSI
  inequality-only; P4 slot-extraction) both DISCLOSED in the note's own
  text and both now covered by decorrelated legs (FR-B here; PE2 FR-C).
  This pass's own instrument: measurements are python-exact Bareiss
  resultants + hull/m-reads with predictions hand-derived and sealed
  before the run; oracles are PARI factorpadic/idealprimedec (pinned
  w12 convention), PARI nfmodpr residue arithmetic (no slot/W formula
  in the loop), explicit constructions, and F₉[[t]]/F₂[[s]] Hensel
  root reads; the one note-derived ingredient in a measurement loop is
  the HE3-1L carrier construction, cross-checked by the nfmodpr leg.

## FRESH ROUTE (`verification/openmath/he3_pe4fresh.py` @ 0c16b6a,
## GREEN, exit 0, 0 violations, 4/4 teeth — disjoint from PE1 A/B/C,
## PE2 FR-A/B/C/D, PE3 FR1/FR2/FR3, and the note's §S8/§S9′ rows)

* **FR-A (μ = 2 dictionary at frames new to the whole arc).**
  (4,1) @ ℤ₃ (first e₁ = 4 anywhere): RAM {(8,1)}, INERT {(4,2)},
  SPLIT {(4,1)²} (a starved slope at (3,1): both letters roots, peel
  m = (1,1)), 2SIDED {(4,1)²} — 4/4 PARI, node floors + HE3-1a exact
  per member. (2,3) @ ℤ₂ (first f₁ = 3 wrap contact, first char-2
  residue-seam contact): RAM {(4,3)}; INERT with coherent R̃ =
  η(Z²+Z+1) irreducible → PARI {(2,6)} while the NAIVE read Z²+Z+η
  SPLITS (tooth T-FLIP-1); SPLIT = (Φ′−4x³)(Φ′−2x⁵) with coherent
  η(Z+η)(Z+η²) → PARI {(2,3)²} = construction while the NAIVE read is
  IRREDUCIBLE (tooth T-FLIP-2 — the flip fires in BOTH directions at
  this frame, PARI siding with DEFINITION 1 [r2] both times); peel
  profiles over all 7 letters of F₈^× exact; W-DIRECT: all 12 coherent
  and naive coefficients computed by nfmodpr (no slot formula, no W
  formula) equal the hand values, η := image of θ²/2; tooth T-REACH:
  the retired ŝ·n(κ) display reaches {1} ⊊ F₈^×, missing both SPLIT
  roots. EQUAL CHAR: q = 3, (1,2) — the arc's FIRST equal-char f₁ ≥ 2
  row: Φ′ = x²+t² over F₃[[t]], f = (x²−t²x+t²)(x²+t²x+t²), σ =
  {(1,2)²} by construction, development A₁ = −t⁴/A₀ = t⁶ symbolic,
  R = Z²+1 split at {η,−η}, and PER-ROOT label reads by Hensel in
  F₉[[t]] at ALL FOUR roots: g₁'s two Frobenius branches BOTH carry
  (3, Z−η), g₂'s BOTH carry (3, Z+η) — LEMMA HE3-6's equivariance
  machine-run at f₁ = 2 for the first time. q = 2, (3,1): Φ′ = x³+t,
  2SIDED {(3,1)²} by construction, slopes {4,5}, per-root dv = 4/5 via
  F₂[[s]] Hensel.
* **FR-B ((SEP) direct).** ℤ₅, (2,1), slopes {3,4,5} CONSECUTIVE
  (sub-case (c) at |K^×| ≥ 2): Ψ(κ,s) measured by resultants at 16
  samples on two members (exact product + junk variant f+5⁹):
  equality Ψ = D′h_F at all 13 non-root samples INCLUDING the
  never-machine-run at-slope generic-letter samples; strict excess at
  the root samples (exact member: Key | f, Ψ = +∞, recorded; junk:
  finite, 36 > 18/22/24); THE INVERSION solved on measured Ψ →
  n = (2,2,2) = construction on both members; PARI {(2,1)³} on both
  (the junk member is a dictionary-only prediction); tooth T-INV:
  corrupted Ψ(4)+2 breaks the solve.
* **FR-C (the starved (2,2) corner — the one listed §S1 (iii)/fence
  geography never machine-run, PE3's suggested charge).** ℤ₂, (1,2),
  Φ′ = x²+2x+4, K = F₄, μ = 3: single side λ = 3 = D′h+1 of length 3,
  R = Z³+1 with root set ALL of F₄^× (genuinely starved — verified by
  F₄ arithmetic), development pins (9,7,4) exact, peel m_s = 1 at each
  of {1, ω, ω²} via HE3-1L carriers, σ = {(1,2)³} by PARI AND by
  construction (three unramified quadratics; 5, −3, 5 mod 8
  nonsquares). The corner the GENHN [r1] fence covers is machine-real
  and the dictionary holds on it.

## GRADE LINE

**Pass verdict: CLEAN (2 MINOR, 0 GAP, 0 CRITICAL — the bar is
GAP-or-worse). Counter: 1/2 → 2/2. HE3 IS ACCEPTED: text frozen, dated
appends only.** With HE3 through the bar, the last of the nine closure
notes below the bar is closed: THE CHAIN'S DEPENDENCY CLOSURE IS
COMPLETE AT THE BAR. (Conditionality unchanged in kind: HE3-BOX-1's
supplier-stack grades and the μ ≥ 4 HE3-BOX-6 box remain exactly as the
note displays them.) Cure suggestions for the two MINORs (dated-append
candidates, not executed here): M-A — one clause under the (SEP)
sub-cases naming the all-integer ≤ 2-side configuration and pointing at
(a)'s two-relation argument; M-B — a cross-reference in §S6.1 step (2)
to §S1 (ii)'s at-slope residue-mismatch parenthetical.
