# TAMEK — roots in a fixed tame extension K: the embedding-count statistic X_K, its Cohen–Lenstra mean, and the limiting law

**Unit:** TAMEK (Asvin's direct question, 2026-08-08: "is there a nice answer if we ask
for the number of roots lying in a fixed tame extension K/Q_p, i.e. the random variable
mapping f to the number of maps Q_p[x]/f(x) → K? In the Cohen–Lenstra spirit, there
might be nice formulas.")
**Predecessors:** MOMENTS_2026-08-08.md (the PGF recursion; X_{Q_p} = the K = F case),
BCFGTIE_2026-08-08.md (the mean machinery), HMENU3/W11 (menus = joint factor-type law),
GRTJC (twist classes / letters), JD0 (letter-blindness of counts).
**Status:** SKELETON — sections landing incrementally; see git log for the arc.
**Grade:** attempt 0/2 (intermediate; no hostile pass yet).

---

## S0. Overview for Asvin

[TO BE WRITTEN LAST — under 60 lines.]

## S1. Set-up: the statistic X_K and the embedding-count lemma TK-0

**Setting.** F a local field with ring O, uniformizer π, residue field F_q, p = char of
the residue field. Fix once and for all a Teichmüller generator ζ of μ_{q^f−1} inside
the unramified closure (so μ_{q^{f'}−1} = ⟨ζ^{(q^f−1)/(q^{f'}−1)}⟩ for f' | f).

**Tame fields and their coordinates (classical, re-derived here).** A tame extension
K/F of ramification index e (p ∤ e) and residue degree f, d := ef, has a UNIQUE
unramified subfield K₀ = F_f (residue field F_Q, Q := q^f), and K = K₀(β) with

      β^e = ζ_K^c · π ,     ζ_K := the Teichmüller generator of μ_{Q−1} ⊆ K₀,

for some c. (Existence: pick any val-1/e element, normalize its e-th power's unit part
to Teichmüller — possible since 1 + m_{K₀} is uniquely e-divisible when p ∤ e, Hensel.)
Two data (e, f, c) and (e, f, c′) give F-isomorphic fields iff c ≡ c′ mod
g_K := gcd(e, Q−1): the **twist class** c mod g_K is the LETTER of K — precisely the
GRTJC letter genre (the residual unit datum that the carry algebra 𝒜(T) = K[v]/(v^E − ζ_T)
carries); J-D0 proved stratum COUNTS blind to it. X_K below is the first statistic in
the corpus that reads it.

**The statistic.** For monic f ∈ O[x] of degree n with Haar-uniform coefficients,

      X_K(f) := #Hom_{F-alg}( F[x]/(f) , K ) = #{ roots of f in K } = #{ roots in O_K }

(roots of a monic integral polynomial are integral). Almost surely f is separable, so
X_K = Σ_{g | f irreducible} #Hom_F(L_g, K), L_g := F[x]/(g). K = F recovers the corpus's
X_n (BCFGTIE/MOMENTS). Projective model as in MOMENTS S1 (uniform degree-n effective
divisor on P¹; X_K^p counts K-points of the divisor, the point at ∞ contributing its
own root count).

### LEMMA TK-0 (the tame embedding count — derived from scratch)

Let K = (e, f, c) as above and L = (e', f', a) tame (so L = F_{f'}(α), α^{e'} = ζ_L^a π,
ζ_L = ζ^{(q^f−1)(q^{f'}−1)⁻¹-power} the Teichmüller generator of μ_{q^{f'}−1}). Set
g := gcd(e', Q−1). Then Hom_F(L, K) = ∅ unless e' | e and f' | f, and in that case

      #Hom_F(L, K) = g · #{ j ∈ {0, …, f'−1} :  ζ_K^{ a r q^j − c }  ∈ (μ_{Q−1})^{e'} } ,

where r := (Q−1)/(q^{f'}−1) (the index of μ_{q^{f'}−1} in μ_{Q−1}). Membership in
(μ_{Q−1})^{e'} — the index-g subgroup — is the congruence a r q^j ≡ c (mod g).
(Note the K-side letter enters only as c mod g — through gcd(e', Q−1), the E-torsion
the target can see at level e'.)

*Proof.* An F-algebra map ι: L → K is a field embedding. (Necessity) ι(F_{f'}) lies in
the maximal unramified subfield K₀, so f' | f; and v(ι(α)) = 1/e' must lie in the value
group (1/e)ℤ of K, so e' | e. (Count) ι is determined by (i) its restriction to F_{f'} —
an F-embedding F_{f'} ↪ F_f, of which there are exactly f' (the unramified extension is
Galois; the restriction is Frob^j∘(fixed inclusion), j = 0..f'−1) — and (ii) a root in K
of the minimal polynomial x^{e'} − σ_j(ζ_L^a)π = x^{e'} − ζ_L^{a q^j} π of α over
σ_j(F_{f'}) ⊆ K₀. Every element of K of valuation 1/e' is x = β^{e̅} w with w ∈ O_K^×,
and O_K^× = μ_{Q−1} × (1 + m_K) with 1 + m_K uniquely e'-divisible (p ∤ e'). So

      x^{e'} = ζ_L^{a q^j} π   with   x^{e'} = (β^{e̅} w)^{e'} = β^e w^{e'} = ζ_K^{c} π w^{e'}
      ⟺   w^{e'} = ζ_K^{ a r q^j − c }        (identifying ζ_L = ζ_K^r).

In the cyclic group μ_{Q−1} (order Q−1) the equation w₀^{e'} = t has solutions iff
t ∈ (μ_{Q−1})^{e'} — the index-g subgroup — and then exactly g of them; the (1+m_K)-part
contributes exactly one solution (unique e'-divisibility). Summing over j gives the
display. ∎

**Remarks.**
1. **K = L:** #Aut(L) = #Hom(L, L) = gcd(e', q^{f'}−1) · #{j : a(q^j − 1) ≡ 0 mod
   gcd(e', q^{f'}−1)} (set K = L in TK-0: r = 1, e̅ = 1, g = gcd(e', q^{f'}−1)); for
   f' = 1 this is just gcd(e', q−1).
2. **Subfield count.** #{subfields of K isomorphic to L} = #Hom(L, K)/#Aut(L)
   (Aut(L) acts freely transitively on embeddings with fixed image). This is the
   Cohen–Lenstra-flavored quantity that the mean law (S4) consumes.
3. **Letters gone at the total.** Σ_{a mod g₀} #Hom(L_a, K) over the g₀ := gcd(e', q^{f'}−1)
   twist classes of (e', f') equals f' e' · [e'|e][f'|f] · (g/g₀)·(g₀/g)-bookkeeping —
   concretely: for each j, the solvability condition a r q^j ≡ c e̅ (mod g) is solvable
   in a for exactly (q^{f'}−1)·gcd-many a's; the machine leg (T-battery, check family
   TK0-SUM) verifies the clean instance Σ_a #Hom(L_a, K) counted with multiplicity
   = #{x ∈ K : v(x) = 1/e', x^{e'} ∈ Teich(F_{f'})·π}-census. The mean-level consequence
   we actually use is displayed (and machine-checked) per-case in S3/S4.

## S2. The structural point: X_K is letter-sensitive; what the letters must do

**Why X_K reads letters.** A root of f in K is an irreducible factor g together with an
embedding L_g ↪ K. Wild factors never embed (K tame: every subfield of K is tame — a
sub-extension of a tame extension has p ∤ e' and its inertia is a subgroup of tame
inertia). So X_K reads exactly the TAME sub-menu of the factorization type, refined by
TK-0's congruence: e' | e AND f' | f AND a r q^j ≡ c (mod g). The last clause is a
condition on the LETTERS (the twist classes a of the factors against the twist class c
of K). J-D0 proved the classifier's stratum counts are gauge/value-blind — one
q-expression per SHAPE; X_K is the first statistic of the program that cannot be
computed from shapes alone. What saves the possibility of clean formulas is not
letter-blindness but letter-EQUIDISTRIBUTION: if, at each type (e', f'), the g₀ twist
classes of factors are equally likely (and suitably independent across factors), then
X_K's law is again a clean function of (q, e, f) — plus one genuinely new invariant of
K, discovered next.

**The mean itself is letter-sensitive: the (2,2) separation.** Take q odd, K of type
(e, f) = (2, 2) — the two twist classes are c = 0, 1 (g_K = gcd(2, q²−1) = 2):
K_c = F_{q²}(√(ζ_K^c π)). Count ramified quadratic subfields L = F(√(ζ^a π)) ⊆ K_c
(type (2,1), a ∈ {0,1}): TK-0 with e' = 2, f' = 1, r = q+1, g = 2, j = 0:
condition a(q+1) ≡ c (mod 2), i.e. (q odd) 0 ≡ c (mod 2). So:

      c = 0:  BOTH ramified quadratics embed (a = 0 and a = 1: #Hom = 2 each,
              #Aut = 2, one subfield each — 2 ramified quadratic subfields);
      c = 1:  NO ramified quadratic embeds.

(Sanity at q = 3: K₀ = Q₃(ζ₈, √3) contains √−1 = ζ₈², hence both √3 and √−3: both
classes ✓; K₁ = Q₃(ζ₈, √(3ζ₈)) contains neither.) Both K_c share the shape (2, 2) and
the same subfields F, F_{q²}, K itself; they differ ONLY in the letter — and S4's mean
law then predicts different E[X_K]: the projective means differ by exactly 2/q. This is
the corpus's first quantity where two towers with equal shape data and different
letters have provably different densities-side statistics — the exact complement of
J-D0(ii), on the value layer where J-D0 never claimed anything.

**What the letters must do for clean laws.** Two separate inputs:
(i) **per-factor letter uniformity** — at each ramified face of a Newton polygon, the
    twist class of the resulting factor is uniform over the g₀ classes (proved at the
    single-face level in S6, LEMMA TK-2, from Haar-uniformity of the leading residual
    unit — this is a measure statement, NOT derivable from the count-layer corpus);
(ii) **joint independence across factors/levels** — the letter of one factor does not
    bias another's (level-0 across-points instance rides (J-D0-PROD); within a cluster
    across faces it is a fresh claim — boxed in S7).
Given (i)+(ii), every X_K quantity reduces to shape-menu data (the corpus's committed
menus) weighted by TK-0 congruence counts — that is the mechanism behind every closed
form in S3–S5.

## S3. Examples: the exact n = 2 law for EVERY tame K, and exact n = 3 anchors

### THEOREM TK-Q (the complete quadratic law). Let q be odd, K any tame extension of
type (e, f, c). For random monic quadratic f: X_K ∈ {0, 2} a.s., and

      P(X_K = 2) = Σ_{m-classes of F*/(F*)² with √m ∈ K} mass(m),
      mass(1) = mass(u) = q/(2(q+1)),   mass(π) = mass(uπ) = 1/(2(q+1))

(u a non-square unit). √m ∈ K per class: √u ∈ K iff 2 | f; √(ζ^aπ) ∈ K iff 2 | e and
a(Q−1)/(q−1) ≡ c (mod 2) (TK-0 with (e',f') = (2,1)). Hence, with the S1 coordinates:

      E[X_K, n=2] = 2·P(X_K = 2)
        K = F:               q/(q+1)
        (1,2) unram quad:    2q/(q+1)     — and any K with f even, e odd
        (2,1) ram quad:      1            — both twists, any q odd
        (2,2), twist c=0:    2            — ALL four quadratic classes embed: X ≡ 2 a.s.
        (2,2), twist c=1:    2q/(q+1)     — classes 1, u only (a(q+1) ≡ 1 mod 2 insoluble)
        (3,1), (1,3):        q/(q+1)      — no quadratic subfields: behaves like F

*Proof.* f = x² + a₁x + a₀ has roots (−a₁ ± √D)/2, D = a₁² − 4a₀; both roots lie in K
or neither, and X = 2 iff D ∈ (K*)² (D ≠ 0 a.s.). For fixed a₁ and Haar a₀, D is Haar
on O; the class masses are the Haar measures of the four square classes intersected
with O: P(v(D) = 2k, unit class s) = (1−1/q)q^{−2k}/2, summing to q/(2(q+1)) per unit
class; odd valuations likewise 1/(2(q+1)) per class. (K*)² ∩ F* = union of the classes
√m ∈ K, computed by TK-0. ∎

    (The (2,2) c=1 row displayed twice above: the honest value is 2q/(q+1) — classes
    1, u in; π-classes out. The c=0 row: all four classes in, P = 1, E = 2 exactly:
    a quadratic polynomial over F a.s. splits in F_{q²}(√π)·… — every quadratic étale
    algebra embeds in K₀ = the (2,2)-field of twist 0. First letter-split row: the two
    (2,2)-fields differ at n = 2 already: E = 2 vs 2q/(q+1).)

### Exact n = 3 anchors (menu rows × TK-0, LEQ-instances proved where used)

From the accepted cubic menu (HMENU3, monic): R_{(2,1)(1,1)} = q(q³+q+1)/((q+1)Φ₅),
R_{(3,1)} = (q²+1)/Φ₅, Φ₅ := q⁴+q³+q²+q+1. The per-twist split of each ramified row is
EXACTLY even at n = 3 (LEMMA TK-3 below). TK-0 then gives, for q odd resp. q ≡ 1 (3):

      E[X_K, n=3](K ram quad, either twist)
          = q/(q+1) + 2·(1/2)·R_{(2,1)(1,1)} = q/(q+1) + q(q³+q+1)/((q+1)Φ₅)
          = 1 − Φ₆/Φ₅                                     (Φ₆ := q²−q+1)
      E[X_K, n=3](K ram cubic, any of the g₀ = gcd(3,q−1) twists)
          = q/(q+1) + #Hom·(R_{(3,1)}/g₀) = q/(q+1) + (q²+1)/Φ₅
          [#Hom(L_match, K) = g₀ by TK-0; per-twist menu share R/g₀ by TK-3 —
           the product is g₀-free: the same value whether q ≡ 1 or 2 mod 3]
      E[X_K, n=3](K unram quad)
          = q/(q+1) + 2·R_{(1,2)(1,1)} = q/(q+1) + q³(q²+q+1)/((q+1)Φ₅)
          [#Hom((1,2)-field, K) = 2 = f'; unramified letters don't exist (g₀ = 1)]
      E[X_K, n=3](K unram cubic) = q/(q+1) + 3·R_{(1,3)} = q/(q+1) + q³(q+1)/Φ₅

**The dip.** For K = ram quad: E(n=2) = 1, E(n=3) = 1 − Φ₆/Φ₅ < 1, and S4's limit
law predicts E(n→∞) = q/(q+1)·(1 + 1/q) = 1 again: the mean is NOT monotone in n and
NOT constant after first attainment — the "each subfield switches on at n = d_L with
its full mass and stays" hypothesis is REFUTED at n = 3 (it held at n = 2 by TK-Q).
Numerically at q = 3: 1, 114/121 ≈ 0.9421, → 1. The machine battery (S8) measures the
dip and the recovery.

### LEMMA TK-3 (letter equidistribution at n ≤ 3, per ramified row — proved)

At n = 2: the (2,1)-factor's field is F(√(−a₀·unit)) with unit-part residue Haar-uniform
⇒ both twists get mass exactly 1/2 of the ram-irreducible row (TK-Q's mass(π) = mass(uπ)
display IS this statement). At n = 3, type (2,1)(1,1): conditional on each polygon
stratum the quadratic factor's leading residual unit is Haar-uniform: (i) level-0
(1)(2)-stratum — the 2-cluster's −b₀ unit part, uniform, refinement strata re-enter the
n = 2 case; (ii) triple-cluster shallow stratum v(a₁) = 1 ≤ v(a₀)−1: factor field
F(√(−a₁-part)), uniform; (iii) deep re-entry: rescale x = πy multiplies the (2,1)-root
pair's squared-generator by π², preserving the twist class — induction on depth. Type
(3,1): the field is F((−a₀-ish·π^h)^{1/3}-normalized) with unit residue uniform on F_q*,
whose image in F_q*/(F_q*)³ is uniform (gcd = 3 case; gcd = 1: single class, nothing to
prove). Same induction. ∎ (The general-n, general-type, JOINT version is S7-BOX-2.)

## S4. The mean E[X_K]: the Cohen–Lenstra law

### The generalized cluster split (LEMMA TK-B′ — BCFGTIE Lemma B with a live shallow band)

For K = F the shallow band (root valuations in (0,1)) was root-free; for ramified K it
is NOT — K contains elements of every valuation in (1/e)ℤ. The corrected split, for a
size-m rational cluster (recentred, v(a_i) ≥ 1):

      ε_m^{(K)}  =  F̃_m^{(K)}  +  Σ_{j=0}^{m} P_m(j) · E_j^{(K)} ,

where P_m(j) is BCFGTIE Lemma C's deep-count law UNCHANGED (j = # roots of valuation
≥ 1, fractional or not — the deep factor rescales by x ↦ πy into a fresh Haar monic
degree-j polynomial, and the rescale multiplies root valuations… shifts them by 1,
PRESERVING each ramified pair's twist class: z ↦ z/π squares/e'-powers the generator by
π^{e'}, an e'-th power), and F̃_m^{(K)} := E[# roots of the cluster in K with valuation
in (0,1)] — the SHALLOW SOURCE TERM, new here. The level-0 telescope (BCFGTIE Lemma A)
holds verbatim with ε^{(K)}:

      E_n^{(K),δ=1-part} = q·[ Σ_{k<n} (1−1/q) q^{−k} ε_k^{(K)} + q^{−n} ε_n^{(K)} ] ,

and for f ≥ 2 the closed points of degree δ | f, δ > 1 contribute their own tower
(S5). Two derivation traps, caught against the accepted menus and recorded so no
successor repeats them: (i) hull convexity — a candidate "shallow face" must be ON the
lower convex hull ((0,2),(2,1),(3,0) is not a hull when (2,1) lies above the segment
(0,2)–(3,0): that stratum is totally ramified, not (2,1)+(1,1)); (ii) refinement
strata — "polygon says integer slope" does NOT preclude ramified factors: residual
double roots refine deeper (v(D) odd via cancellation at v(b₀) = 2v(b₁)); the deep
re-entry term carries them automatically, a face-only census does not.

### The shallow source, computed by TK-0's residual census (LEMMA TK-1)

A shallow face of slope h/e₀ (lowest terms, e₀ ≥ 2) and length ℓ = e₀L carries roots of
valuation h/e₀; such a root lies in K iff e₀ | e, and then z = β^{eh/e₀}·w turns the
face equation into a residual equation R(ζ_K^{ch·…}·w̄^{e₀}) = 0 over F_Q — for each
SIMPLE residual root y in the compatible power class, gcd(e₀, Q−1) Teichmüller
solutions w̄, each Hensel-lifting uniquely: the face's K-root count is the TK-0 count
of the residual datum. Multiple residual roots re-enter as clusters over the ramified
base F(β^{eh/e₀}-twisted) — the corpus's (e₁,f₁)-re-based cell law. Consequently the
mean system CLOSES over the lattice of tame extensions: the F-based X_K theory consumes
the F'-based X_K theory for the finitely many tame F' between F and K. (For MEANS the
letters wash out at every simple face: the leading residual unit is Haar-uniform, so
the compatible-class indicator has mean 1/e₀-of-the-classes × g-count = face-mean
independent of c — LEMMA TK-2, S6. Twist classes of K enter E[X_K] ONLY through which
subfields exist, i.e. through TK-0's global congruence — as in S2's (2,2) example.)

### THE MEAN LAW (main law; grade: theorem at n ≤ 3 + limit-conjecture, machine-tested)

      E^proj_∞[X_K]  =  Σ_{F ⊆ L ⊆ K}  q^{ − v(disc(L/F)) }        (sum over SUBFIELDS)
      E^monic_∞[X_K] =  (q/(q+1)) · E^proj_∞[X_K]
      v(disc(L/F)) = f'(e'−1) for L tame of type (e', f')  ⇒  each subfield contributes
      q^{f'−d'}, d' = e'f': mass 1 for unramified subfields, q^{−f'(e'−1)} ramified.

**The Cohen–Lenstra structure, displayed.** By S1 Remark 2 the subfield sum is

      E^proj_∞[X_K] = Σ_{L iso class}  #Hom_F(L, K) · q^{−v(disc L)} / #Aut(L) ,

i.e. THE EXPECTED NUMBER OF K-POINTS OF THE RANDOM ÉTALE ALGEBRA IS THE
MASS-FORMULA-WEIGHTED EMBEDDING COUNT: each tame L is weighted by 1/#Aut(L) (the
Cohen–Lenstra weight) times the Serre-mass factor q^{−v(disc L)}. Equivalently: in the
n → ∞ limit the expected number of irreducible factors with field ≅ L is

      E_∞[m_L] = (q/(q+1)) · q^{−v(disc L)} / #Aut(L)     (monic; drop q/(q+1) and read
                 per-P¹ for projective)                    [CONJ-MEAN, tested]

— Serre's tame mass formula Σ_{L tot ram deg e'} 1/#Aut(L) = 1 (over iso classes; from
Σ_{L ⊆ F̄} q^{−(d(L)−e'+1)} = e') is exactly what makes the per-type totals letter-free
and menu-compatible: Σ_{twists a} E_∞[m_{(e',f',a)}] = (q/(q+1))·q^{f'−d'}·(1/f'), whose
n = 2, 3 exact-degree counterparts are the committed menu rows' leading behavior.

**Instances proved.** n = 2: TK-Q IS this law restricted to d' ≤ 2 (exact, all K —
there the switch-on is exact). n = 3: the S3 anchors (exact, via the accepted menu +
TK-3). The n = 3 dip shows E_n is NOT given by truncating the subfield sum at d' ≤ n
for n ≥ 3; the limit statement is the honest general claim, CONJECTURE-graded, with
convergence O(n^{q−1} q^{−n}) expected from MOMENTS' Lemma 5.1 coupling (whose
generalization to X_K is S5). Machine: the battery tests the limit law at n = 7
against MC for eleven K-cells including both (2,2) twists (the letter-split pair),
plus exact n ≤ 3 anchors.

**Worked table of the limit law (projective form; monic = ×q/(q+1)):**

      K = F:            1
      (1,f) unram:      τ(f)            = # divisors of f   (all subfields unramified)
      (2,1) ram quad:   1 + 1/q                              (both twists)
      (3,1) ram cubic:  1 + 1/q²                             (all twists)
      (e,1) ram:        Σ_{e'|e} q^{1−e'}                    (exactly one subfield per e';
                        twist-free — the compatible twist always exists, uniquely)
      (2,2) twist 0:    2 + 2/q + 1/q²                       (TWO ram quad subfields)
      (2,2) twist 1:    2 + 1/q²                             (NO ram quad subfield)
      (4,1) ram:        1 + 1/q + 1/q³
      general (e,f,c):  1 per unramified level f'|f, + TK-0-counted ramified subfields.

## S5. The full limiting law: the PGF machinery generalized

**The Euler product over closed points (structure theorem, same suppliers as MOMENTS
Lemma 2).** Roots of f in O_K reduce into the Q = q^f residue discs of O_K; every
residue point of F_Q lies over a closed point x of A¹_{F_q} of degree δ(x) | f. The
level-0 Hensel product ((ADD) + (J-D0-PROD), verbatim from MOMENTS) factors X_K over
the closed points; points of degree δ ∤ f contribute 0 (their discs contain no O_K
points… their residue points don't lie in F_Q), and points of degree δ | f contribute
the K-root count Y^{(K)}_{m,δ} of their cluster. The master identities become, with
W_δ(v,t) := Σ_m E[t^{Y_{m,δ}}] v^m and N_δ := #closed points of degree δ on A¹:

      Σ_n F_n^{(K)}(t) u^n = (1/(1−u)) · Π_{δ | f} [ (1 − (u/q)^δ)^{N_δ} · W_δ((u/q)^δ, t)^{N_δ} ]
                              · Π_{δ | f} … — compactly:  the K = F case multiplies
      1/(1−u) by [(1−u/q)W₁]^q/(1−u/q)^q-type factors only at δ = 1; here every δ | f
      contributes its block, and all δ ∤ f blocks cancel into 1/(1−u) exactly as the
      deg ≥ 2 points did for K = F.  Projective: one extra δ = 1 block (the ∞ point).

      LIMIT LAW:   F_∞^{(K)}(t) = Π_{δ | f} g_δ^{(K)}(t)^{N_δ(q)} ,
                   g_δ^{(K)}(t) := (1 − q^{−δ}) Σ_{m ≥ 0} q^{−δ m} E[t^{Y^{(K)}_{m,δ}}] ,
                   F_∞^{(K),proj} = F_∞^{(K)} · g_1^{(K)}   (one more rational point).

Each degree-δ disc cluster carries geometric multiplicity with ratio q^{−δ} — the exact
iid-profile coupling of MOMENTS Lemma 5.1 generalizes verbatim (the census identity is
about the F_q-level reduction, unchanged), giving d_TV(law(X_n^{(K)}), law(X_∞^{(K)}))
= O(n^{q−1}q^{−n}) again.

**The per-point laws.**
- δ = f-blocks with e = 1 (K unramified): Y_{m,δ} = δ·(root count of ONE re-based
  cluster) — unramified extensions are Galois, so all δ conjugate discs carry EQUAL
  counts: g_δ(t) is a δ-th-power-substituted re-based F′ = F_{q^δ}-theory
  (q ↦ q^δ, K/F′ unramified of degree f/δ, t ↦ t^δ): the whole unramified-K
  distribution theory REDUCES to MOMENTS' recursion applied along the divisor lattice
  of f. This is complete (no new laws needed): the unramified-K PGF is computable at
  every n by the same O(n²) series recursion, per divisor δ | f.
- Ramified K: Y_{m,δ} adds the shallow-band face counts (TK-1) with their residual
  censuses and re-based composite re-entries; the deep re-entry is the same P_m(j)
  self-similarity. The MEAN of this system telescopes (S4); the full PGF is an exact
  finite recursion PER (m, level) but its closed-form limit needs the generalized
  KEY-IDENTITY/(INF=GEO) for the coupled multi-base system — OPEN, boxed (S7-BOX-1).
  What is established structurally: the limit factorizes over the Q residue discs of
  O_K into blocks indexed by closed points, with geometric multiplicities; all
  letter-dependence enters through the per-face TK-0 congruences.

**Conjugate-disc correlation (a genuinely new phenomenon at f ≥ 2, e ≥ 2).** For
ramified K of residue degree f ≥ 2, σ(K) ≠ K is possible (σ Frobenius lift): the δ
conjugate clusters over one closed point have EQUAL means in K (σ restricts to an
F-isomorphism σ^{−i}K ≅ K… the subfield sum is conjugation-invariant) but their JOINT
K-root counts need not be independent or equal realization-by-realization — the
per-point PGF g_δ is a law of a SUM of determined-but-unequal conjugate contributions.
The mean law (S4) is insensitive to this; distribution-level claims at ramified f ≥ 2
are boxed with it (S7-BOX-1).

## S6. Letter equidistribution: what the corpus proves vs what X_K needs

### LEMMA TK-2 (single-face letter uniformity — proved, measure-level)

Fix a polygon stratum with a shallow face of slope h/e₀ and length e₀ (residual degree
1). Conditional on the stratum, the factor's twist class a ∈ ℤ/gcd(e₀, q^{f₀}−1) is
UNIFORM. *Proof.* The class is determined by the residue of the face's trailing/leading
coefficient unit ratio (the −a₀/π^h-genre unit for a first face; a ratio of two
consecutive-vertex units in general): under Haar coefficient measure conditioned on the
valuations that define the stratum, that unit's residue is uniform on F_{q^{f₀}}^×, and
the class map F^× → F^×/(F^×)^{e₀-pattern} pushes uniform to uniform. Deep re-entries
preserve classes (x ↦ πy shifts the generator by an e₀-th power, LEMMA TK-B′). ∎

### What the corpus supplies vs what remains

- **J-D0 (ACCEPTED-grade battery, 0/2 note):** counts are letter-BLIND — the complement
  of what X_K needs; it guarantees the type-level menus (the shape data every S3/S4
  formula consumes) are single q-expressions, letters notwithstanding. Consumed.
- **GRTJC W-9/JC-F1 (ACCEPTED 2/2):** the letter lives in 𝒜(T) ≅ K[v]/(v^E − ζ_T) as
  ζ_T, and coboundary gauges move it within its class — the algebraic home of "twist
  class" used by TK-0; the σ_ξ menu-permutation of J-D0 S2.2(f) is the same residual
  rescaling that TK-2's uniformity rides. Consumed as vocabulary + the class
  well-definedness.
- **SIGMALAW (EC-q):** an internal eigen-coefficient law of the classifier ledger —
  NOT a distributional statement about letters; nothing here consumes it beyond the
  general assurance that residual reads are ξ-covariant (which TK-2 re-derives at the
  measure level for its own face).
- **What X_K needs beyond all of this (the honest gap):** JOINT letter
  equidistribution — at every n, conditional on the full shape/type data, the vector
  of twist classes of ALL ramified factors is uniform-independent over its product of
  class groups. PROVED here: every single-face marginal (TK-2), all instances consumed
  at n ≤ 3 (TK-3), and level-0 across-cluster independence (rides (J-D0-PROD)).
  UNPROVED: within-cluster across-face joint uniformity at n ≥ 4 (two shallow faces of
  one polygon; face + its own composite re-entry) — S7-BOX-2. The battery's
  distribution-level checks at n ≤ 7 test exactly this box's consequences (any joint
  bias would move the (2,2)/(4,1) MC cells off the S4 predictions).

## S7. What resists (boxed honestly)

* **TK-BOX-1 (the ramified limit PGF, and the limit MEAN as theorem).** The mean law's
  n → ∞ value Σ_{L⊆K} q^{−v(disc)} is CONJECTURE-graded (machine-tested, n ≤ 3 proved).
  Proving it needs the generalized KEY IDENTITY / (INF=GEO) for the coupled system over
  the tame-base lattice (S4's closed system): the K = F proof telescoped because the
  shallow band was a pure sink; here it is a source with its own re-based re-entries.
  Concrete open lemma: for the vector system (E^{(K/F')})_{F' ⊆ K tame}, show the
  stabilized solution of the coupled telescopes is E^proj = subfield-mass sum. The
  distribution-level version additionally needs the conjugate-disc joint law (S5).
* **TK-BOX-2 (joint letter equidistribution, n ≥ 4).** TK-2 gives every single-face
  marginal; (J-D0-PROD) gives across-cluster independence at level 0. Within one
  cluster with ≥ 2 ramified faces (first at n = 4: two slope-h/2 faces), the JOINT
  uniformity of the two twist classes is unproved — the residual units at distinct
  vertices of one polygon are measurably correlated through the shared coefficients.
  Testable consequence covered by the battery: the (2,2)-cells' means and the n ≥ 4
  distribution rows would move under any joint bias.
* **TK-BOX-3 (wild K).** Everything here is p ∤ e. For wild K the value group/letter
  classification, the Krasner counts, and TK-0 all change (units are not e-divisible;
  1 + m contributes classes). Out of scope; the natural continuation once the wild
  self-loop legs (Phase B) land.
* **TK-BOX-4 (exact finite-n laws for ramified K, n ≥ 4).** At n ≤ 3 exact via menus;
  at n ≥ 4 the committed quartic/quintic menus give the SHAPE data but the per-twist
  refinement rides TK-BOX-2; no exact finite-n closed form is claimed there — MC only.
* **TK-BOX-5 (literature).** The mean law's Cohen–Lenstra form (embedding counts
  weighted by 1/#Aut × Serre mass) is (c)-labelled: claimed new, UNSEARCHED. Known
  neighbors: Serre's mass formula (consumed, elementary tame case re-derived);
  Bhargava–Cremona–Fisher–Gajović (the K = F distribution, OWED-EXTERNAL as before);
  Del Corso–Dvornicich (tame functional equation genre); a literature pass is owed
  before any novelty claim leaves the repo. In particular E[X_K^proj] = τ(f) for
  unramified K is simple enough that it is likely known.

## S8. Preregistered predictions and machine verdict

**Runner:** `verification/openmath/tamek_checks.py` (two-commit seal; this commit = the
seal, scoring run UNRUN at full samples). Exact O_K arithmetic (tame two-generator
representation mod p^64), theory-light recursive Hensel root counter (only Hensel's
lemma + disc subdivision — independent of the cluster machinery being tested), MC grid:
16 K-cells over p ∈ {3,5,7} (both (2,1) twists at p = 3, 5; all three (3,1) twists at
p = 7; both (2,2) twists and (4,1) at p = 3; unramified quad/cubic), degrees n = 2..7
at p = 3 (150k samples/cell) and n ∈ {2,3,6} at p = 5 (60k), p = 7 (40k).

**Never-measured preregistered targets** (no MC at these cells/degrees existed anywhere
before the scoring run; exact values frozen in the runner's `--prereg` dump, committed
as `tamek_prereg.json`): the (4,1,0) p=3 cell at every n; the (2,2) pair at n ≥ 4; all
p=7 (3,1) twists at n=6; every LIM comparison at n_max.

* **P1 (TK0).** Brute #Hom(L,K) == LEMMA TK-0 on the full roster (~90 pairs incl.
  mixed-type L = (2,2,a) and all zero cases). EXACT, no tolerance.
* **P2 (TKQ).** MC(n=2) within 5σ of THEOREM TK-Q at every cell; support ⊆ {0,2};
  (2,2,c=0) has X ≡ 2.
* **P3 (N3).** MC(n=3) within 5σ of the S3 exact anchors at every cell — including the
  ram-cubic anchors at p=5 (single twist class) and p=7 (three classes).
* **P4 (DIP).** p=3 ram-quad cells: E(2) − E(3) > 0 at 3.5σ (predicted value
  Φ₆/Φ₅ = 7/121 ≈ 0.0579) and E(7) − E(3) > 0 at 2σ (recovery toward the limit 1).
* **P5 (LIM).** |MC(n_max) − limit-law mean| < max(5σ, 0.04(1+E)) at ALL 16 cells —
  consistency band (finite-n vs limit disclosed), incl. the twist-split (2,2) pair and
  (4,1,0) → (3/4)(37/27) = 37/36.
* **P6 (SPLIT).** (2,2,c=0) − (2,2,c=1): exactly 1/2 at n=2 (5σ); > 0.25 at n=7 (5σ);
  predicted limit split 1/2.
* **P7 (LEQ).** Twist-partner cells have equal means at every measured n (5σ):
  (2,1,0)=(2,1,1) at p=3,5; (3,1,0)=(3,1,1)=(3,1,2) at p=7.
* **P8 (TEETH, 4).** T1 wrong-twist TK-0 (formula at c+1 must contradict brute); T2
  mass-blind mean (subfield COUNT in place of mass: predicts 1.5 at the p=3 ram-quad
  cell, true 1); T3 letter-blind (2,2) mean (twist-average 1.75 at n=2 vs exact 2);
  T4 tame-blind mean (K=F value q/(q+1) at the ram-quad cell, i.e. "shallow band
  root-free for all K"). All four must bite (5σ rejections / ≥1 mismatch).

**Falsifiers.** P1 failing kills TK-0 (and S2/S4's congruence layer with it). P2/P3
failing kills TK-Q / the menu×TK-3 anchors — the first suspects would be TK-3's
equidistribution instances. P4–P6 failing kills CONJ-MEAN (the limit law) while leaving
TK-0/TK-Q intact. P7 failing would be a letter-equidistribution violation at the mean —
it would falsify the S6 joint-uniformity picture outright. A tooth not biting voids the
battery's evidential value, not the theorems.

**VERDICT: PENDING — appended at commit 2 from the artifacts.**

## S9. Grade box and conditionality

[STUB]
