# BPLL — BasePolygonLengthLaw PROVED (synthesis pass-6 F2)

**Unit**: synthesis-6 queue #3, charter `docs/SYNTHESIS_PASS6_2026-08-08.md` §F2.
**File**: `lean/LeanUrat/Scaffold/DictIII/BasePolyLength.lean` (new decls only).
**Verdict**: **PROVED, sorry-free, Lean-core** — `BasePolygonLengthLaw p` holds
for every prime `p` (`basePolygonLengthLaw_proved`), and the consumer
`terminalPolySem_kp0_of_law` is FIRED unconditional at the positive-gate
instance (`terminalPolySem_kp0_unconditional`).

## What was proved

The named open row of `CU2tKp0.lean` (the OM base length law at the corpus
carrier), exactly as displayed there — no statement change:

> For monic `f : ℤ_[p][X]` with `f(0) ≠ 0` and `p ∣ f(0)`:
> (i) every side of `principalData f` has `1 ≤ e ∧ 1 ≤ ℓ`;
> (ii) `Σ e·ℓ = j₀` where `X^j₀ ∥ f̄` exactly, `1 ≤ j₀`.

## Test-before-proving record

* **Numeric falsifier** (pre-proof): brute-force lower-hull oracle over
  20 000 random valuation profiles (deg ≤ 8, vals ≤ 4, saturated perimeter)
  — 0 counterexamples for `Σ lengths(neg sides) = j₀` AND per-side
  `e ∣ length`, `ℓ ≥ 1`.
* **Algebra falsifier** (pre-proof): the collinearity-transitivity integer
  identity `G·(c₁−b₁) = (c₁−z₁)·e₁ + (c₁−a₁)·e₂` verified at 100 000 random
  integer points before it became `collinear₃_trans`'s `linear_combination`.
* **Compiled instance gates** (in-file):
  - `BPLLGate.gate_hensel` — the Hensel family (any `p`; `v(f₀) ≥ 1`, `f₁`
    unit): `Σ e·ℓ = 1`, checked against the INDEPENDENTLY landed
    `principalData_hensel_a0_one` (TerminalRead), not against the §4 proof.
  - `BPLLGate.gate_eisenstein_sum` — `f = X² + 2` over `ℤ_[2]` (ramified
    stratum `e = 2`): `Σ e·ℓ = 2`, with `j₀ = 2` pinned independently by the
    exact-multiplicity clauses against `f̄ = X²`.
* The perimeter legs stay countermodel-forced by the pre-existing
  `Kp0GateA` (saturation) and `Kp0GateB` (−∞ corner) — unchanged.

## Proof anatomy (what is NEW vs quarried)

QUARRIED (proved kernels consumed as-is): `npHeight_le`,
`npHeight_convexOn` (+ `slope_mono_adjacent`), `npVertices_sorted`,
`npVertices_on_hull`, `npVerticesFull_sorted/_fst`, `mem_hullAbscissae_iff`,
`hullHeightAt_of_onHull`, `dropCollinear_cons_head`, `dropCollinear_sublist`,
`M2.valSupport_facts`, `mem_principalData`, `pairLine_left/right/diag_const`,
`validLines_nonempty` machinery.

NEW (the negative-prefix hull machinery, all in namespace `BPLL`, all
abstract over a bare support `Finset` where sensible):

1. **Height sign law** — `npHeight_nonneg`; `npHeight_eq_zero_on` (0 between
   two height-0 dots); `npHeight_pos_left` (STRICTLY positive left of a
   saturated zero-anchor; witness = the flattest descending chord into
   `(j,0)` from the argmax of chord slopes over the left-dot finset — a new
   valid supporting line); `npHeight_at_zero` (the leftmost dot is ON the
   hull; witness = the steepest chord out of `(0,v₀)`, argmin construction).
2. **`dropCollinear` endpoint/merge laws** — `dropCollinear_getLast?`
   (keeps-last); **`dropCollinear_between`** (the straddle killer: every
   original point strictly between two consecutive survivors is collinear
   with them — i.e. `dropCollinear` only merges collinear runs), via
   `collinear₃_trans` (integer cross-product transitivity).
3. **`telescope_sum`** — the negative-prefix telescoping: on a sorted vertex
   list with the no-jump property, `Σ (right−left)` over pairs with left
   `< j` equals `j − head`.
4. **`npSides_neg_length_sum`** — THE ABSTRACT CORE: on any support with
   left anchor `(0,v₀)` (unique at abscissa 0), saturated left block,
   anchors `(j,0)`, `(n,0)`, and abscissae ≤ n: all sides increase, and the
   total length of negative-slope sides is exactly `j`.  This is the
   reusable OL5-adjacent hull lemma (the geometry the banked
   `npVertices_stable_of_hull_preserved` sorry sits next to).
5. **Side arithmetic** — `side_e_dvd_length` (`Rat.den_dvd` through
   `Rat.divInt_eq_div`), `side_e_mul_residualDeg` (`e·ℓ = length`),
   `side_nondeg`, `side_h_neg_iff`.
6. **Residue kernels** — `toZMod_eq_zero_iff` (local copy of the repo-
   standard ker route), `p_dvd_iff_one_le_valuation`,
   `coeff_map_toZMod_ne_zero_iff`; `j₀ := (f̄).natTrailingDegree`.

Key mechanism (why no side straddles `j₀`): `(j₀, 0)` is a FULL vertex
(on-hull dot), and a straddling consecutive survivor pair `(P, R)` with
`P.2 ≥ 1`, `R.2 = 0` would force `collinear₃ P (j₀,0) R` by the merge
invariant — whose cross product cancels to `j₀ = R.1`, contradiction.
The recorded OL5-adjacent convexity gap did NOT block the route: chord
attainment on sides was never needed; convexity enters only through
`slope_mono_adjacent` at on-hull points plus the two new explicit valid-line
constructions.

## Consumer fired

`terminalPolySem_kp0_unconditional : TerminalPolySem fkp0 Hkp0 Dkp0` — via
`terminalPolySem_kp0_of_law (basePolygonLengthLaw_proved 2)` + seam
perimeter only (`monic_X_add_C`, `f(0) = 2 ≠ 0`, `2 ∣ 2`).  First
unconditional `TerminalPolySem` supplier at a 𝐇°-keyed k′ = 0 seam: the
(H6) (T-READ) k′ = 0 face's numerical input is now theorem-backed, not
per-instance.

## Build & footprints

* `lake env lean LeanUrat/Scaffold/DictIII/BasePolyLength.lean` — green,
  0 errors, 0 sorries (only repo-standard `push_neg` deprecation warnings).
* `lake build LeanUrat.Scaffold.DictIII.BasePolyLength` — success (8740 jobs).
* `#print axioms`: `basePolygonLengthLaw_proved`,
  `npSides_neg_length_sum`, `dropCollinear_between`, `telescope_sum`,
  `npHeight_pos_left`, `npHeight_at_zero`, `side_e_dvd_length`,
  `gate_hensel`, `gate_eisenstein_sum`,
  `terminalPolySem_kp0_unconditional` — ALL
  `[propext, Classical.choice, Quot.sound]` (Lean-core only).

## Fences / residue (unchanged)

* The −∞ corner (`f(0) = 0`) stays a separately-priced open leg (Kp0GateB).
* The CU2t terminal-appended keying stays refuted (Kp0GateC) — this unit
  supplies the 𝐇°-keyed face only, exactly per charter.
