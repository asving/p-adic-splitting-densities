# K1FACE — the Step-10/K1 classifier CONSUMER face, stated and FIRED (pass-6 F3a unit)

Date: 2026-08-08 (wallclock 2026-08-02). Unit: synthesis pass 6 queue item 5
(F3(a)), "Step-10/K1 classifier consumer face" — the OTHER H2-U10 twin, the
leakFree CONSUMER owed since the LEAKFREE-TYPED unit
(`LEAKFREE_2026-08-08.md` honesty display) and carried at the ROOT (FRESH)
residuals + PROJECT_STATE frontier.

## Spec gate (executed first)

* Spec LOCATED and LIVE: `docs/SYNTHESIS_PASS6_2026-08-08.md` queue item 5
  ("statement-design unit consuming `LeakFreeCarrierV4`; fired at `td2V4`;
  de-vacuity fence mandatory") + F3(a) ("Missing: only the Step-10-side
  statement — how the classifier census consumes parent-blindness").
* NOT already executed: no `K1ConsumerFace` decl existed anywhere in
  `lean/LeanUrat/`; the H2-U10 closure record (HDISCHARGE_H2 §H2-U10) covers
  only the (c)-clause via the guarded `childDetermined`, and LEAKFREE-TYPED
  explicitly left this face owed.

## THE FACE (what Step-10/K1 demands of leakFree-type data, at what signature)

Consumption map of record (HDISCHARGE_H2 §1/§2.2): Step 10 (CU-1's step)
produces the CU LEAF LABELS; clause (R) consumes (FRESH) through Step 18's K1
= the label-keyed regrouping. Both demand ONE thing of the (L) clause:

> **the CU leaf label read at every interior on-line slot k ≠ k_s is a
> well-defined STRATUM functional — a function of the off-parent window
> coordinates only, never of the parent realization.**

(O-9's harness K2 "parent-realization independence", 8,220,994 boxes /
0 violations, is this face's numeric shadow — support, not proof.)

At the as-built v4 carrier (label = `slotRead`, the [R1-G2] d-leaf code;
parent realization enters only through the parent block), the typed demand is
the ∃-label FACTORIZATION (`lean/LeanUrat/B2D/K1ConsumerFace.lean`):

```
def K1ConsumerFace (L : LedgerStratumDataV4 D W P) : Prop :=
  ∀ k : ↥W.onLine, (k : Fin (W.ℓ + 1)) ≠ W.ks →
    ∃ lab : ({c : Coord // c ∉ L.parentBlock} → ℕ) → ℕ,
      ∀ x : Coord → ℕ, L.slotRead x k = lab (fun c => x c.1)
```

The junction slot k_s is EXCLUDED by design: its read is the ρ-determined pin
((J)/`junction`) — clause (c)'s parent charge, not (L)'s scope.

Exact-strength pin `k1ConsumerFace_iff_parentBlind`: the face is EQUIVALENT
to parent-blind descent of the interior reads — the demand consumes exactly
the (L) content, nothing more (no counting law stated or implied; the H2-U9
R2-C1 counting-shadow bar respected on the CONSUMER side too). Regrouping
display `K1ConsumerFace.labelFiber_parentBlind`: label-fiber membership (the
cells K1 regroups by) is invariant under parent-block rewrites.

## OUTCOME: the consumer FIRED at the instance (no obstruction)

RESULTS (all `lake env lean` green; footprints = Lean-core
[propext, Classical.choice, Quot.sound]; zero sorry):

1. `k1ConsumerFace_of_leakFreeV4` — `LeakFreeCarrierV4` SUPPLIES the face
   (its display `read_parentBlind` is exactly the equivalent descent form).
2. `k1ConsumerFace_of_lawful_v4` — every v4-LAWFUL datum supplies it
   (through `ledger_leakFreeV4`); family form `ledgerStrataV4_k1ConsumerFace`.
3. **`td2V4_k1ConsumerFace` — FIRED at the compiled `td2V4` instance** (the
   transported huni countermodel, the LIVE chain's instance — same site as
   `slotUniform_td2V4` / `td2V4_freshClauses` / `td2V4_leakFreeV4`).
4. De-vacuity fence (mandatory) `td2V4bad_not_k1ConsumerFace`: at the raw
   datum with the parent block on slot 1's read leaf, NO label function can
   exist — the all-zero box and the (1,0)-indicator box restrict identically
   off the parent block yet read 0 ≠ 1. The fired gate is contentful;
   lawfulness is load-bearing.

## Honesty display (what this does NOT close)

* `td2V4.parentBlock = ∅`, so the face holds THERE for the trivial reason;
  the content is the general suppliers + the fence — exactly the charter's
  pricing ("statable and fireable at the ∅-parent instances first"). The
  pass-6 DECLINED row's revisit trigger ("revisit ONLY if the classifier
  consumer face, once stated, demands a nonempty-parent witness for its own
  non-vacuity") did NOT fire: the face's non-vacuity is carried by the
  compiled `td2V4bad` refutation, not by a nonempty-parent lawful instance.
* This is the STEP-10-SIDE statement at the LEDGER carrier. NO tie is claimed
  to the Step-18 K1 exact form at the K-CUT/pool carrier
  (`H8/KCutK1Tie.lean`: different vocabulary, instantiation owned by
  [2r]/CL-8 REL.2 + (K-CUT-g)).
* `DictIII.FRESH`, `TransDeep`, and every LeakFaceV4 statement are
  byte-untouched (NEW declarations only). The H2 `TransDeep.leakFree` FIELD
  stays bare per the U9 fold of record.
* The ROOT (FRESH) residual retired by this unit is exactly the "Step-10/K1
  classifier face (the leakFree CONSUMER, still owed)" component; the full
  §B2-DEF non-ledger general supplier residue stands.
