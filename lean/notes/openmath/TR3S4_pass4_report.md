No adverse content findings.

- **CLEAN — HV3-E2.** “\(e_2^{GMN}=e_1\), \(|\lambda_2|=h_1/e_1\)” is the correct \(r=3\) instantiation. Distributing \(e_1\) through the minimum gives exactly
  \[
  \min_i\{e_1w_1(a_i)+i(e_1w_1(\Phi_1)+h_1)\}=w_2(P).
  \]
  All three adjusted \(e_1\)-sites are accounted for. The print’s \(H_{r-1}:S(\lambda)\to\mathbb Z_{\ge0}\) typing is literally too narrow when \(e_1>1\); the intercept may be rational. The note handles this correctly by consuming only \(e_1H\) and Proposition 2.7(3), whose result is integral. [Primary GMN source, Definition 2.5 and Proposition 2.7](https://arxiv.org/pdf/0807.2620).

- **CLEAN — KER-13 / A-GEN-3 / Z2-e2.** Dead-level splits force \(s=0,\ell=0,\ell'=1\). TR3-S1 is invoked precisely on the \(e_0=1\), \(e_1\)-free substate, is used totally in \(A\), and matches the Def-3.16-at-\(i=3\) inner operator. The calculation
  \[
  j_0=\left\lfloor\frac{s(\beta)+m_0e_1}{e_1}\right\rfloor=m_0
  \]
  is sound. Z2-e2 has the correct filtration direction and covers zero digits, negative read weights, and both recursive vanishing levels.

- **CLEAN — EXP-3 and assembly.** Both exponents reduce to
  \[
  \frac{s_2-\ell_1\beta}{e_1}.
  \]
  Thus the live \(\epsilon_2\) factor plus \(m_0\) becomes \(t_2(j)+\theta_2j\), with both sides using \(u_j=\beta+j\,e_1g_1\gamma_2\). Support equality, \(y^{s_0}\)-absorption, and the final scalar/rescaling orientation follow correctly.

- **CLEAN — nonvanishing and \(\sigma_2\).** The bound
  \[
  \deg R_{\lambda_2}(A)<g_1=\deg\psi_1
  \]
  makes evaluation at \(\bar z_2\) nonzero. The top-digit calculation makes \(R_{\lambda_2}(\Phi_2)\) monic; lawfulness then upgrades association to equality, so the construction of \(\sigma_2\) is valid.

- **CLEAN — KEY0/ORD-INV.** The Revision-1 orientations are now consistent:
  \[
  R_{\lambda_3}(\Phi_3)=\bar z_2^{-\theta_2g_2}\psi_2
  \]
  for the GMN-lawful key, while \(T_{c^{-1}}\psi_2\) is the FGMN-normalized lift’s GMN class. The calculation \(t_2(g_2)=-\theta_2g_2\), the monicized versus literal divergence tests, and the matched-label order chain all agree.

- **CLEAN — Revision 2.** The supersession bracket explicitly and correctly overrides only the sealed header’s stale descriptive sentence. Row K’s actual construction/tests have the corrected FGMN-normalized orientation, so the seal remains usable without mutation.

- **CLEAN — scope and conditionality.** \((H-\ell)\), PIN-GEN-i, and the S6.2-only \((KP_i)+[T]\) dependencies are displayed in the proper places. The \(e_0\ge2\), \(e_2\ge2\), order \(\ge4\), and D-ITER fences are not crossed.

- **CLEAN — record reconciliation.** The displayed row counts sum to \(13{,}679\); all listed meters agree with the record. Seals `7fa8825` and `79eaee6` are both named, and Revision 2 explains the sealed-header discrepancy. The separately reported verifier-owned F2c leg discharged 1,108 checks with zero failures.

- **NOTE — local execution unavailable.** This sandbox could not launch commands because `bwrap` is absent. Per the standing disposition, this is not a content gap; no independent rerun or local commit inspection was possible in this pass.

TR3S4-P4 FINDINGS: 0 critical, 0 gaps
VERDICT: CLEAN