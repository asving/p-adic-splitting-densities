1. **The two new theorems — CLEAN mathematically.**

   - `recentering_natDegree_eq` uses the correct route:
     > `obtain ⟨-, -, -, hin, -, -, -, hΦ', -⟩ := h`
     >
     > `degree_sub_eq_left_of_degree_lt hin`

     `hin` is accepted directly as `tt.degree < σ.Φ.degree`; this agrees with the existing `sub_inC_K0` use. Constant edge cases are safe: for a nonzero constant parent only `tt = 0` can satisfy the strict window, while a zero parent admits no such `hin`. Thus the degree-to-`natDegree` conversion does not introduce a constant-polynomial hole.

   - `recParam_eg_of_recentering` has the correct indices:
     > `hstep i hi1`
     >
     > `current.childWidth = next.Dwidth := hwidth.symm`

     This matches `coherent_H₃rc`, where the sixth transition clause at index `i` proves the next node’s `Dwidth` equals the current node’s `e*g*Dwidth`. The two `hDwidth` links and `hker` therefore genuinely produce
     `νᵢ.e * νᵢ.g * Dwidthᵢ = Dwidthᵢ`; `Stage.hdeg` makes the cancelled factor positive. No off-by-one or vacuity defect is present.

   - The subsumption claim is correct, with the stated limited meaning. Apply `recParam_eg_of_recentering` at `j = i + 1`; `hi2 : i + 2 < len` supplies its interior hypothesis and `hrec` supplies recentering. This yields clause (c), `νᵢ₊₁.e = 1`, without `hprev`. It does not subsume clause (b), and the note correctly preserves that distinction.

2. **CRITICAL — REC-WIRE-G was not removed from every active consumer.**

   Three live Lean documentation statements still call the discharged residual open:

   - `Conformance.lean` module header:
     > “the WIRING provenance REC-WIRE-G + the e-half transition-hypothesis removal stay open”

   - `K0Conformant` docstring:
     > “the wiring-provenance residuals stay open”

   - `k0_actual_of_recentering` docstring in `RecParamE.lean`:
     > “(REC-WIRE-G; the e-half's transition-hypothesis removal) remain open”

   These are present-tense source claims, not fenced historical text. They directly contradict the compiled REV-12 declarations and the note’s S0/S4 status.

3. **GAP — provenance wording remains pedantically inaccurate.**

   The note says:
   > “`recParam_eg_of_recentering`, no species field”

   But the theorem explicitly assumes and uses:
   > `hrec : node.species = ReadSpecies.recentering`
   >
   > `(hrecleg hrec).base`

   The valid claim is “no use of `Node.hspecRec`” or “no species-pair fiat,” not “no species field.” Similarly, “Every hypothesis comes from `HistoryCoherent`” is too strong: `hi1` and `hrec` are separate explicit hypotheses, although all substantive width/recentering laws are obtained from coherence.

4. **GAP — the remaining-open display is not exact everywhere requested.**

   - S0 does display GRADED-READ, REC-SL proper, SITE-EXH, and the unwitnessed `readsOf_landing_K0` recentering branch.
   - S4 correctly reduces REC-SL’s owed mathematical legs to REC-SL proper, but it does not include the remaining `readsOf` recentering-branch witness debt in a consolidated current list.
   - The headline does not give the requested exact consolidated list either.
   - The stale Lean-header statements above additionally contradict that list.

5. **The two intended stale-sentence replacements — CLEAN locally.**

   The §S2 wiring note and the `RecParamE.lean` header now accurately say that no `Stage` field directly records `g`, while the actual tie travels through the coherence width law. The defect is in additional stale siblings, not those two replacements.

6. **Census — arithmetic clean, semantic census not clean.**

   - The declared supersession list contains exactly 17 rows.
   - C185–C204 contains exactly 20 successors/new rows, and their advertised heads are present and broadly correctly classified.
   - Therefore `145 − 17 + 20 = 148` is arithmetically correct.
   - However C172 remains active even though its `Conformance.lean` header sentence still says REC-WIRE-G and the e-half removal “stay open.” Under REV-12’s explicit C030 semantic-currency lesson, C172 should have been caught and superseded. Thus the 17-row delta is semantically incomplete and “ZERO untagged known” does not establish a clean current census.
   - The stale `K0Conformant` and `k0_actual_of_recentering` docstrings also escape the published module-doc-block census scope, but fail the required fresh grep-minded sweep.

Clean counter remains 0/2.

VERDICT: CRITICAL