# GENIND pass PE2 — HOSTILE VERIFICATION REPORT (fresh context, second arc pass; the r1 text attacked for the first time)

**VERDICT: UNREFUTED, NOT CLEAN. 0 CRITICAL, 2 GAP, 1 MINOR.** The clean
count does NOT start (stays 0; grade stays 0/2; an r2 is owed, then the
2-clean count begins at the next pass). No sealed number, no count law, and
no clause of THEOREM GENIND.A / GENIND.C / the lemmas was refuted — my fresh
route (a route disjoint from all prior ones, §7) confirmed the scaffold at
**n = 6**, territory no prior leg touched, including a hand-preregistered CS
genre with e₁ = 3 that no roster had ever realized. Both GAPs are at the
GENIND.B ↔ [GENIND-H] seam again, but at two faces PE1 did not mine: the
GENRE GRAMMAR of the box (mixed composite openings, first live at n = 6 —
constructed instance displayed) and the CITATION INTEGRITY of the r1
(CS-EXACT) discharge geography (the cited pin carries a then-known,
undisclosed CRITICAL on the menu half).

**Target:** `lean/notes/openmath/GENIND_PROOF_2026-08-08.md` at HEAD `83ced80`
(file last touched at r1 `66200f2`; seal `c3212cc`; verdict `4984004`; PE1
report @ `4259a93`). **Role:** hostile verifier, fresh context, zero prior
stake; I fix nothing; quote-and-classify only.

---

## 1. FINDINGS

### PE2-G1 [GAP] — Mixed composite openings (CS + coexisting β-child in one polygon) fall between GENIND-3's stated scope and (CS-1)'s displayed fibration; first live at n = 6, and I constructed one.

The event partition (GENIND-1(i)) is fine: a state whose polygon carries
BOTH a composite factor and a repeated-linear e = 1 factor is classified CS,
disjointly (PE1's charge 1, re-verified). The hole is in the TRANSPORT.
The three relevant displays:

* GENIND-3 (S4.1) scopes the β-fibration: *"Fix a β-genre at the (m, d, N)
  system: a polygon P with sides S₁, …, S_r, per-side residual factorization
  types with **repeated factors all linear on e = 1 sides**"* — mixed states
  are excluded by definition.
* (CS-1) (S7.1) displays the stage fibration as *"the opening locus fibers
  as (letter census) × q^{affine} × [the stage state space]"* — no slot for
  a co-evolving child system; the S5.3 genre tables handle only DECIDED
  co-pieces (V1E2's split-off root, FULL-(2²1)'s (y−w)), which are static
  and fold into the census/q-power factors. An evolving child does not.
* Step 2 (S6) assembles the menu in two buckets: *"the β-genres by GENIND-0
  products of the children's finite (A1) menus; the CS stages by
  [GENIND-H]"* — a mixed genre needs BOTH simultaneously (stage menu ×
  child menu × pieces), and no displayed lemma licenses that product:
  GENIND-1(iii)'s fracture permanence is stated *"**after a β-event** the
  separation valuations of the child clusters are CONSTANT…"* — scoped to
  β-events, so even the child's independent evolution at a CS event is not
  covered by any stated lemma (its valuation argument would carry over, but
  that is a repair, not a reading).

**The case is not hypothetical.** n = 4, 5 admit no mixed genre (the CS side
consumes ≥ 4 abscissas; a repeated-linear e = 1 child needs 2 more; the S5.3
tables are complete — PE1 re-enumerated them). At n = 6 I constructed the
first live instance and verified it with my own fresh classifier (§7, Leg 5):

> genre M6 at (q, m, d, N) = (2, 6, 1, 5): hull (0,4)–(2,2)–(6,0); left side
> [0,2], slope 1, e = 1, residual c(y−w)² — a (2,1) β-child entering at
> depth 1; right side [2,6], slope 1/2 (h = 1, e = 2), residual (y−z)² with
> z² = c (the shared vertex unit) — a composite stage factor
> (μ = 2, e·deg ψ = 2). Hand-derived locus law (q−1)²·q^{6(N−1)−11}; at
> (2,6,5): **8,192 states enumerated, 8,192 classified CS with BOTH the
> (e, deg ψ, μ) = (2,1,2) stage factor on [2,6] AND the (1,1,2)
> repeated-linear child on [0,2]; 0 deviants.**

**Failure scenario:** at n = 6, Step 2's menu assembly for genre M6 has no
displayed supplier for the child factor's menu, and Step 4's drain
composition for it ("the frame drains iff some child drains" lives inside
GENIND-3, whose scope excludes M6) is likewise unlicensed. A discharger
could satisfy all four (CS-1)…(CS-EXACT) members stage-side and P(6)'s menu
and complement members would still lack the M6 family's law — the strong
induction's Step 2/4 case analysis has a case its lemma stack, as scoped,
does not reach. No statement is FALSE (GENIND-3's steps (1)–(2) are visibly
factor-generic — the Hensel split separates any coprime blocks — and the
intended reading is presumably the product of GENIND-3's split with (CS-1)
at the stage block), so this breaks no chain: class **GAP**, load-bearing
for GENIND.B at n ≥ 6, invisible at n ≤ 5 (which is why the sealed battery
and both prior passes could not see it).

### PE2-G2 [GAP] — The r1 (CS-EXACT) discharge geography cites a pin (GENH4 @ `dff23f8`) at which the MENU half of the clause carried a then-known, undisclosed CRITICAL; only the DRAIN half is supported in-text.

(CS-EXACT) (S1 box, verbatim): *"the stage menu **and** stage drain that
(CS-1)+(CS-2) deliver are EXACT, one closed-form q-power family per stratum,
not asymptotic bounds."* Step 4's [r1] record and S7.1's fourth member both
support its n = 4 discharge with one citation:

> "at n = 4 the clause is supplied DE FACTO by GENH4 (@ dff23f8) — THEOREM
> GENH4.B (= Q22-L3) delivers the stage drain as ONE exact q-power per
> (center, entry, history) stratum"

GENH4.B is DRAINAGE ONLY. The MENU half of the clause at n = 4 is THEOREM
GENH4.A (= Q22-L1) + GENH4-1F (per GENH4.D's own package: "CS-1 = GENIND-4 +
GENH4-1F + GENH4-2/-3/-4"), and at the cited pin `dff23f8` GENH4.A's
displayed census factor ("Q−1 for RAM and per 2SIDED pin") had been
adjudicated **CRITICAL** — false at genre-F boundary-band pins, with leaf
strata overcounting the entry locus by up to a factor q in 40/50 scanned
cells (GENH4 passPE2 report `2618e9b`, 23:01:28; folded into the ledger at
`0089f9b`, 23:07:17) — BEFORE GENIND r1 landed (`66200f2`, 23:18:53). The
repair (GENH4 r2, `fd35416`) landed at 23:19:42, 49 seconds AFTER GENIND r1,
so it was uncitable — but the adjudicated defect was on the ledger and is
NOWHERE disclosed in the r1 text, which instead labels this paragraph
"Discharge geography, honestly". A reader executing the r1 repair's own
program — checking the box member against the cited pin — finds the menu
half of (CS-EXACT) FALSE AS DISPLAYED at that pin (a wrong closed form is
not an exact law family).

**What survives:** the true n = 4 menu laws ARE exact (the comp-weighted
census closes 216/216 F + 288/288 E per the same PE2 report; GENH4 r2 cured
the display), [GENIND-H] remains a hypothesis on the box either way, and no
conclusion of GENIND.B moves. So: a citation-integrity gap in an [r1]
repair whose stated purpose was to make the box's discharge checkable —
class **GAP** (the discharge-geography claim is not established by the
cited material as pinned; the conclusion survives via material the note
does not cite). Repair is one sentence (re-pin to GENH4 r2 / disclose the
census correction, and name the menu half's supplier).

### PE2-m1 [MINOR] — "ONE exact q-power per (center, entry, history) stratum" mis-describes GENH4.B's genre-F law, which is a TWO-term sum at even N.

GENH4.B @ `dff23f8` (verbatim): *"genre F: UND(k; H₂) = (q²−1)^t (q−1)^r ·
[ q^{2⌊N/2⌋+2k−1} + 1_{2|N}·(q−1)·q^{N+2k−1} ] per (ψ, H₂)"* — at even N
this is a sum of two q-powers per (center, entry, history) stratum (the
GENH4-CAP(F) boundary band), not one. GENH4.B's own header makes the same
overclaim; GENIND's [r1] sentences (Step 4 + S7.1, quoted in PE2-G2)
inherit it, and the (CS-EXACT) gloss "one closed-form q-power family per
stratum" is satisfiable only at a finer stratum granularity (band split off)
than the granularity the sentence itself names. Consequence-free for the
induction — Step 4's inclusion–exclusion needs exact finite sums of closed
forms, which two terms are — but the box member's gloss and its flagship
discharge do not match at their own granularity. Class: MINOR.

## 2. CHARGE 1 — THE r1 TEXT (the parts that held)

* **(CS-EXACT) strong enough for Step 4's consumption: YES.** I inventoried
  the consumption independently: the n′ > n β-fracture into a degree-n child
  runs S5.2's inclusion–exclusion over the child systems' u's and totals;
  totals are trivially exact; u_n = simple drains (GENIND.C recursion over
  IH's exact member 4) + tails (W-12 S3.4 transport) + ragged bands + stage
  drains. Stage menus and drains: covered by (CS-EXACT) verbatim. Ragged
  bands: their COUNTS are structurally exact (N-boundary slot strings are
  S2.2 slot counts) and their (A2)-routability is hypothesized inside the
  corrected (CS-1) ("with the ragged band a boundary family routed through
  (A2)"); their confinement cite (LEMMA GENHN-3) is accurate — GENHN-3(c)(d)
  @ `850e77e` reads "deep histories never consult the ragged band". So with
  the four-member box the induction closes on member 4 — the PE1-G1 seam is
  genuinely shut at statement level. (The residual defects are the two
  findings above, not a strength shortfall.)
* **(CS-EXACT) weak enough for the n = 4 discharge: YES mathematically, NOT
  as cited** — that is exactly PE2-G2/PE2-m1.
* **(CS-1) ragged-window-corrected vs GENHN.A(v) @ `850e77e`: VERBATIM
  MATCH.** GENIND quotes *"the stage read IS the (μ, K)-cluster read in
  dv-units at the S-node, ragged-window-corrected"*; GENHN.A(v) at the pin
  reads character-for-character the same (clause (v), "[THE LAW]"). The
  GENHN [r1] node erratum (f₁ ≥ 2: (μ−j)·D′h + 1) has no GENIND-side
  counterpart (GENIND's only node display is f₁ = 1, where the forms agree
  exactly — re-checked, agreeing with PE1 §6).
* **(CS-2) normalization reconciliation: RE-DERIVED, EXACT, at three fresh
  points.** The note's r1 display checked only (q, μ″) = (2, 2), |K| = 4.
  I re-derived mass = ghost × letters at (|K|, μ″) = (9, 2), (4, 3), (9, 3):
  e.g. |K| = 9, μ″ = 2: compositions (2), (1,1) give 648 + 5184 = 5832 =
  8·9³ (mass form); letters-only 8 + 64 = 72 = 8·9; ghost 9² = 81;
  81·72 = 5832 ✓ (§7 Leg 4, exact integers). The ghost-total identity
  |K|^{c(m′)μ″} and the letter-sum binomial identity
  Σ_r C(μ−1, r−1)(|K|−1)^r = (|K|−1)|K|^{μ−1} both re-derived. The GENHN
  S6.2 citation is verbatim-accurate at the pin.

## 3. CHARGE 2 — THE F₂ PADDING REPLACEMENT [r1, PE1-M2]: CORRECT, edges included

The repaired sufficiency pads with ONE monic irreducible of degree n−4.
Checked against everything S5.1 consumes: (i) existence — an irreducible of
every degree ≥ 1 exists over every F_q (necklace count > 0); at the edge
n−4 = 1 the text's "any of the q−1 other monic linears" is right for all q
(q ≥ 2 ⟹ q−1 ≥ 1, including F₂ where PE1 broke the old display); the edge
n−4 = 0 is scoped out by "at n > 4" (n = 4 is the base realization, no
padding). (ii) coprimality — distinct monic irreducibles are coprime, so
the level-0 Hensel product (W12-S2.1) applies at the same window N.
(iii) separability/decidedness — irreducible over a finite field ⟹
separable; multiplicity 1 at level 0 ⟹ a decided piece. (iv) the count —
positivity of the padded locus: (q−1)q^{4N−5h−3} × q^{(n−4)(N−1)} > 0 at
every window with 2h ≤ N−1 (the [r1, PE1-M3] floor, now displayed). The
alternative route ("or realize the embedded genres of S5.3") is intact.
CLEAN.

## 4. CHARGE 3 — THE INDUCTION'S CLOSURE: my own inventory

I inventoried every consumption of P(k), k < n, in Steps 1–5 without
trusting Step 4's list: Step 1 consumes all four members of each factor
(products via GENIND-0; product complements via inclusion–exclusion over
factors' u's — needs IH member 4 exact ✓ hypothesized); Step 2 consumes
member 1 (history-resolved, at β-children entering at their own refine loci
— the β3 mechanism) and the box's (CS-1)+(CS-2)(+(CS-EXACT) for exact
forms); Step 3 consumes Ore + window coherence + (CS-3); Step 4 consumes IH
member 4 (children's u's in the recursion), the box's stage drains +
(CS-EXACT), the tails, and the ragged bands (now in the inventory, [r1]).
Every consumed member is either re-established at n or hypothesized in
(IH)/[GENIND-H] — the strong induction CLOSES at the statement level,
**except** that Step 2/4's per-genre case analysis has the mixed-genre case
of PE2-G1 outside its lemmas' stated scopes at n ≥ 6. Window condition
[r1, PE1-M1] re-verified: m·k_i ≤ (current window)−1 at step i ⟺
m·(k₁+…+k_i) ≤ N−1, and partial sums increase, so the conjunction is
exactly mμ ≤ N−1 — the display is now correctly hypothesized. The base
grounding (P(2), P(3) proved; [GENIND-H] empty at n ≤ 3 by S5.1) checked.

## 5. CHARGE 4 — THE FIVE-EVENT GRAMMAR at a fresh boundary

PE1 swept the trivial-partition boundary; I attacked the
**high-denominator boundary** (e ≥ 3, where no roster row had ever realized
a CS event — all n ≤ 5 genres have e·deg ψ = 2) and the
**simultaneous-factor boundary** (PE2-G1). Results: (a) no sixth event — at
(q, m) = (2, 6) my fresh classifier partitioned all 266,240 states of two
full sweeps into exactly {DRAIN, DEC, ALPHA, BETA, CS} with zero residue
and zero double-classification (the partition is enforced by construction
+ asserts; none fired); (b) the e = 3 CS genre E6-B (full side (0,2)–(6,0),
slope 1/3, residual (y−z)² with the char-2 pin −2z ≡ 0 — a J-D0
degeneration at a NEW e) obeys the classification and the criterion
(e·μ·deg ψ = 6 ≤ m = 6, ≥ 4 ✓); (c) GENIND.A(IV) holds at n = 6 in the
never-tested direction: α is correctly INVISIBLE at m = 6, N ≤ 6 (mk ≤ N−1
impossible — zero ALPHA states measured, the window-condition tooth);
(d) the classification's disjointness at the mixed boundary holds (CS wins,
per the definition) — the transport gap there is PE2-G1, not a grammar
failure. NO SIXTH EVENT; the partition claim stands at m = 6.

## 6. MACHINE LEG — REPRODUCED; INSTRUMENT NON-TAUTOLOGICAL

* **Runner md5 `e7ca150b1b5cb13ad4f3350ac776aca2`** — IDENTICAL at the worktree, at HEAD
  `83ced80`, at r1 `66200f2`, and at the seal `c3212cc` (four-way check;
  r1's "runner + artifacts BYTE-FROZEN" confirmed). All five GT-PIN
  supplier files match their pinned md5s in the worktree.
* **Fresh isolated re-run** in `/tmp/genind_pe2_run` (runner + the five
  pinned suppliers copied; committed artifacts untouched):
  **GREEN — 163,536 checks, 0 violations, 163.9 s**, teeth 12/12/4/24
  exactly as preregistered. Results JSON vs committed: **1 leaf diff =
  `elapsed_s`** (163.9 vs 164.5); output text differs only in embedded
  per-row timings. Bit-identical content, matching S11 and PE1's run.
* **Tautology audit (my angle, distinct from PE1's):** (i) `depth0_pred` is
  pure closed-form (the note's laws + visibility gates) sharing no code
  with the reader — the reader derives tallies mechanically (hull,
  residual factorization, recentering/Hensel); (ii) `bdrain_pred` builds
  its expectations from the SUPPLIER closed forms `R_law`/`u3pc` (n ≤ 3
  laws), not from the reader's own tallies — the GT-BDRAIN comparison is a
  genuine two-source tie; (iii) GT-CRIT tests `4 <= e*mu*gam <= fm` on
  every recorded CS event at every depth, while the reader's CS-exit
  predicate is only `mu >= 2 and e*gam >= 2` — the criterion's lower bound
  is NOT built into the detection, so an e·μ·γ < 4 event would be recorded
  and would fire the violation; (iv) unlisted genres route to `CSOTHER` →
  GT-CRIT violation, unpredicted keys → GT-DEPTH0 violation (both
  directions real); (v) teeth are hard-equality gated (`TEETH_EXPECT`).
  No tautology found.

## 7. FRESH ROUTE — n = 6, a degree no prior leg touched (disclosed)

Prior routes: (a) the seal battery (n = 3, 4, 5 rosters, GT-* families);
(b) PE1's legs (n = 4, 5 law evaluations: CS5-V1E2/V4E2, B2Q closed form,
bracket re-derivation at m ≤ 5, S5.3 genre re-enumeration at n = 4, 5).
**My route: hand-preregistered exact laws at n = 6 + an independent
classifier written from GENIND-1(i)'s STATEMENT (no code shared with the
sealed runner) + a mechanism-free PARI σ oracle + symbolic brackets at
m = 6, 7.** All laws below were derived BY HAND BEFORE running (derivations
in this section); every check EXACT.

* **Leg 1 (n = 6 depth-0 census, both directions).** Full sweeps at
  (q, m, d) = (2, 6, 1), windows N = 3 (4,096 states) and N = 4 (262,144
  states). Preregistered and confirmed EXACTLY:
  DRAIN₀ = q^{(m−1)(N−1)} = 1,024 / 32,768; N = 3 CS census = exactly ONE
  genre **E6-B** (full side (0,2)–(6,0), slope 1/3, e = 3, residual
  (y−z)², char-2 pin −2z ≡ 0; hand law (q−1)q^{6(N−1)−5}) at **128**;
  N = 4 CS census = exactly TWO genres: **E6-A** (full side (0,3)–(6,0),
  slope 1/2, residual (y−z)³; hand law (q−1)q^{6(N−1)−9}) at **512** and
  E6-B at **8,192**; zero ALPHA (window condition, §5); no genre outside
  my hand enumeration; every CS event obeys the criterion bounds. E6-B is
  the corpus's first e₁ = 3 composite opening — S7.1's stage grammar reads
  it as (m′, D′, E, K) = (2, 3, 3, F_q), consistent with GENHN's
  (e₁, f₁) = (3, 1) genre family. Bonus tie (derived post-hoc, disclosed):
  the single β genre at (2,6,4) — hull (0,3)–(2,1)–(6,0), left (y−w)²
  child — has hand law (q−1)q^{12} = 4,096 = the measured BETA tally on
  the nose.
* **Leg 2 (mechanism-free σ oracle).** Every one of the 2,944 DEC states
  of the (2,6,3) sweep + 2,000 sampled DEC states of (2,6,4), my order-1
  Ore σ (one (e, deg ψ) block per separable factor) vs PARI
  `nfinit`/`idealprimedec` at p = 2 on a disc ≠ 0 lift: **4,944 checks,
  0 bad** — the first n = 6 σ verification in the corpus, wild p = 2,
  including e = 3 and f = 3 blocks.
* **Leg 3 (symbolic bracket at NEW m).** GENIND-2(b) as a polynomial
  identity in Q at m = 6, 7 (exponent coefficients c(m)+1 = **16, 22** —
  the battery capped at m = 5 / coefficient 11), μ = 1..4, by exact
  composition sums in sympy: **8/8 identities**.
* **Leg 4 ((CS-2) reconciliation at fresh points).** §2 above: three fresh
  (|K|, μ″), exact.
* **Leg 5 (constructed mixed genre).** §1 PE2-G1: the M6 locus at
  (2,6,5), 8,192/8,192 classified CS-with-child, 0 deviants.

## 8. CLEAN CHARGES (executed, nothing found)

(1d) (CS-2) re-derivation — clean; (2) F₂ padding incl. n−4 ∈ {0,1} edges —
clean; (3) closure inventory — closes modulo PE2-G1; (4) no sixth event at
the fresh boundary, m = 6 partition exact; [r1] collateral sweep — all
seven r1 repairs verified in place and mutually consistent (S1 box = S7.1
member = S7.4 work order = S9.1 annex, four sites, same four-member box;
GENIND-4's repaired minima min(4h+1, 4h+2) = 4h+1 re-checked; the (h,z)
slice re-scope correct; CS4-E floor correct at the (N,h) = (2,1)
counter-instance; S6's "where the conditionality lives" list still
accurate); supplier pins — 18 §S9.3 hashes + ledger pin resolve, GT-PIN
5/5; S11 frozen-verdict section internally consistent with BOX-5's [r1]
arc state.

## 9. FINDINGS TABLE + GRADE LINE

| id | class | site | one line |
|---|---|---|---|
| PE2-G1 | GAP | S4.1 scope / S7.1 (CS-1) / S6 Step 2+4 | mixed CS+β-child openings (first live n = 6, constructed at (2,6,5), 8,192 states) are outside GENIND-3's stated scope and (CS-1)'s displayed fibration; no displayed lemma licenses the stage × child product transport |
| PE2-G2 | GAP | S6 Step 4 [r1] / S7.1 (CS-EXACT) | the n = 4 de facto discharge cites GENH4 @ dff23f8 for a menu+drain clause; only the drain half is supported, and the menu half at that pin carried a ledger-adjudicated CRITICAL (23:07) predating r1 (23:18), undisclosed |
| PE2-m1 | MINOR | S6 Step 4 [r1] / S7.1 | "ONE exact q-power per (center, entry, history) stratum" is false at genre F even N (GENH4.B's own two-term display); granularity of the gloss vs its flagship discharge |

**Machine leg:** runner md5 four-way identical (worktree = HEAD = r1 =
seal); isolated re-run GREEN 163,536/0 in 163.9 s, teeth 12/12/4/24,
content bit-identical to committed artifacts (elapsed-time leaves only);
instrument audited non-tautological on a fresh angle.

**Fresh route:** n = 6 (never machine-checked): 266,240-state census with
hand-preregistered laws EXACT (E6-A 512, E6-B 128/8,192 — first e₁ = 3
genre; DRAIN exact; zero α = the window condition; β tie 4,096); 4,944 PARI
σ checks 0 bad (first n = 6 oracle, wild p = 2); brackets symbolic at
m = 6, 7 (coefficients 16, 22) 8/8; (CS-2) reconciliation exact at three
fresh (|K|, μ″); the mixed-genre construction 8,192/8,192.

**GRADE LINE: NOT CLEAN — 0C + 2G + 1m. The 2-clean count does NOT start
(0/2 stands; consecutive-clean counter stays at 0). r2 owed on the two
GAPs (both statement-level: one scope sentence + product display for mixed
genres; one citation repair/disclosure for the (CS-EXACT) geography), plus
the m1 granularity sentence; the mathematical content of GENIND.A/C, the
lemmas, the criterion, and the r1 seam-closure survived every attack,
now through n = 6.**

---
*Verifier: fresh-context hostile pass PE2, 2026-08-09. Fixed nothing.
Machine leg re-run in an isolated /tmp sandbox; committed artifacts
untouched. Fresh-route scripts (independent classifier + legs) ran in the
same sandbox; all laws they test are derived in this report.*
