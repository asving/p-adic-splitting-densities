# HE6 — HE3-BOX-6 CLOSED: the ENLARGED TEST FAMILY at FRACTIONAL HEIGHTS pins the label class sizes at EVERY side (ℓ ≥ 2 ∧ deg-res ≥ 2 included), BASE-CHANGE-FREE

**Unit:** HE6 (the last lemma of the uniformity program).
**Status at commit 1:** SKELETON — statements §S1, the first-bite analysis
§S2, the proof plan §S3–S6, sealed runner
`verification/openmath/he6_checks.py` with preregistered predictions P1–P5
and five teeth in its docstring. Proofs + battery land at commit 2.

---

## S0. WHAT THIS UNIT CONSUMES

* **GENHN @ HEAD**: LEMMA GENHN-1 (stage ring / Φ′-development / node),
  **LEMMA GENHN-2 (THE SLOT LEMMA)** — the one load-bearing import:
  for A = Σ_{i<D′}a_ix^i and ANY ξ with (T1) v(ξ) = h/e₁, gcd(h,e₁) = 1 and
  (T2) η_ξ := res(ξ^{e₁}π^{−h}) a root of ψ,
  dv(A(ξ)) = min_i(e₁v(a_i) + ih) EXACTLY, with the residue of the attaining
  slot the universal polynomial Σ_t res(a_{i+e₁t})η_ξ^t — GENHN-3 (window).
* **HE3 @ 7a95449**: DEFINITION 1/2 (stage resolvent, labels, decided
  leaves), LEMMA HE3-0 (every root of f on the opening locus satisfies
  (T1)/(T2)), COROLLARY HE3-0′ (the local forcing) **with the ℓ ≥ 2 repair
  of §S4 below**, LEMMA HE3-1 (the resultant identity), THEOREM HE3.A's
  assembly (§S6.3), COROLLARY HE3.B's strict-descent recursion.
* **Classical only:** resultant symmetry Res(f,g) = ±Π_ρ g(ρ) = ±Π_ξ f(ξ)
  for monic f, g; the ultrametric inequality; Galois-invariance of v.
* **NOT consumed:** any base change (unramified or ramified); Ore's theorem;
  the Montes/GMN order-≥2 index theorem; GENHN-4 (so the W-9 weld face stays
  absent from the σ chain, as in HE3).

---

## S1. STATEMENTS

Frame as in HE3 §S1: O complete DVR, residue field F_Q, both
characteristics; Φ′ the stage key of degree D′ = e₁f₁, roots of v = h/e₁;
K = F_{Q^{f₁}}; dv := e₁v; f = Φ′^μ + Σ_{j<μ}A_jΦ′^j on the genre-G opening
locus; F(Y) = Y^μ + ΣA_j(θ)Y^j the stage resolvent with dv-polygon P(F).

**Normalizer convention (twist-free).** Fix ϖ := x^iπ^a with ih + e₁a = 1
(exists: gcd(h,e₁) = 1); then dv(ϖ(ξ)) = 1 exactly at every (T1)/(T2) point
ξ, and ALL normalizers are taken to be n(k) := ϖ^k. Because they are powers
of ONE element, the normalizer cocycle is trivial and HE3's twist constants
ẑ^{fl} are absent throughout this note. Residual polynomials are defined
relative to this convention: for the side with left endpoint (j₁, m₁), slope
λ = u/ℓ, length L = ℓd,

    R_λ(Z) := Σ_{t=0}^{d} res( A_{j₁+tℓ} · ϖ^{tu} / ϖ^{m₁} ) · Z^t  ∈ K[Z],

so that the residual variable Z corresponds to the class of y^ℓ/ϖ^u.

> **DEFINITION HE6-1 (the enlarged test family).** For a rational κ = u/ℓ in
> lowest terms with κ > D′h, and a monic irreducible r = Z^{d} +
> Σ_{t<d}c_tZ^t ∈ K[Z] with r(0) ≠ 0, put
>
>     Ψ_{κ,r} := Φ′^{ℓd} + Σ_{t<d} B_t·Φ′^{ℓt} ,
>     B_t := the degree-<D′ representative of ĉ_t·ϖ^{(d−t)u} ,
>
> monic of degree D″ := D′ℓd. (ĉ_t ∈ O[x] any lift of c_t.) For ℓ = 1 this
> is HE3's recentered key family enlarged in DEGREE; for ℓ ≥ 2 it is a test
> polynomial at a FRACTIONAL height, which HE3-BOX-6 showed no member of the
> stage ring can reach. These are the order-2 Montes keys in the program's
> own language.

> **LEMMA HE6-1 (the ξ-side: every root of a test polynomial carries the
> intended label).** Every root ξ of Ψ_{κ,r} satisfies (T1)/(T2),
> dv(Φ′(ξ)) = κ, and res(Φ′(ξ)^ℓ/ϖ(ξ)^u) is a root of r.

> **LEMMA HE6-2 (the ρ-side evaluation: the family SEPARATES labels).** For
> every root ρ of f, with label (λ_ρ, r_ρ),
>
>     dv(Ψ_{κ,r}(ρ)) = ℓd·min(λ_ρ, κ) + ε_ρ ,   ε_ρ ≥ 0,
>     ε_ρ > 0  ⟺  λ_ρ = κ and r(β_ρ) = 0  ⟺  (λ_ρ, r_ρ) = (κ, r).

> **LEMMA HE6-3 (slope counts, (SEP)-FREE).** n_λ := #{ρ : dv(Φ′(ρ)) = λ}
> equals D′L_λ for every slope λ of P(F) — with NO hypothesis of the (SEP)
> kind that HE3-3 needed, because the enlarged family samples the support
> function h_F at every RATIONAL height, not only at integers.

> **LEMMA HE6-4 (NONEMPTINESS — the engine).** For every slope λ of P(F) and
> every monic irreducible r | R_λ, the label class
> S_{λ,r} := {ρ : λ_ρ = λ, r(β_ρ) = 0} is NONEMPTY.

> **THEOREM HE6.A (HE3-BOX-6 CLOSED).** At a node whose λ-side residual R_λ
> is separable, |S_{λ,r}| = D′·ℓ·deg r for EVERY monic irreducible r | R_λ,
> at every (ℓ, deg r) — in particular at ℓ ≥ 2 with deg R_λ ≥ 2, the
> configuration HE3-BOX-6 boxed. The proof uses no base change, hence
> HE3-BOX-6's two under-argued branches are SUPERSEDED, not repaired:
> branch (i) (unramified, non-K-rational residual factors) and branch (ii)
> (ramified, ℓ ≥ 2) are both replaced by LEMMA HE6-4 + the orbit sandwich.

> **COROLLARY HE6.B (the program consequence).** [GENHN-HE(μ)] for all μ,
> hence [GENIND-H(n)] for all n, with the conditional stack reduced to
> {arc grades} ∪ {GENHN's S9 compression/coverage boxes} ∪ {HE3's finding-6
> semilinearity gap at f₁ ≥ 2} ∪ {HE6-BOX-1}, where HE6-BOX-1 is the DEEPER
> READ at a repeated residual on an ℓ ≥ 2 side (§S7) — a place HE3's own
> §S6.3 stage-α step passes over silently, first live at the same n = 8.

---

## S2. WHERE HE3-BOX-6 FIRST BITES (re-derived from the box statement)

(To be completed at commit 2: n = 8, μ = 4, D′ = 2; the peel is needed only
when a side carries ≥ 2 distinct labels, which at ℓ ≥ 2 needs ℓd ≥ 4 ≤ μ.)

## S3–S6. PROOFS

(commit 2)

## S7. BOXES

(commit 2)

## S8. THE BATTERY

(commit 2; runner sealed at commit 1 with predictions P1–P5 and teeth
T1–T5 in its docstring)
