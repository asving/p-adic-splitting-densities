All checks complete. Consolidating the pass-7 decision.

**Charge (1) — Route-B core re-derived.** I walked §§2–4 independently. Def SYM is total (k=0 base clause; top-node γ*=γ vs coefficient-node stretch, matching M1 §5.3's two-case display and M14 Lemma 1(b)'s "(γ at the top)"). Lemma S is a same-recurrence-same-seed induction — sound, genuinely one-realization (symbolic side is the recursion by definition). Lemma B: M1 §5.5 base clause + the tower's OWN d. Theorem induction: empty-I and product cases both close; ψ_j-irreducible-over-rebuilt-F_j is the tower's own §B2-DEF datum (the (R4) deletion is real — all four §5.1 sites checked verbatim against M14's text); STAB turns cardinalities into q-powers; Top step composes [LEDGER-READ] + card_eq normalization into a_δ = δ·dim = δ·a_sym. P4 composition walked as charged: P1 at the δ>1 pool + the Theorem's own δ=1 instance (correctly NOT a P1 instance — P1 quantifies over δ>1 pools only) + same-carrier persistence certificate ⇒ a_δ = δ·a₁, with the posLetter bridge properly deferred and the P4-only disposition localizing to the non-P1 premises. F₉ arithmetic re-derived from scratch: branch letter (d=1;(2,1,1)) → 1 leaf → a_sym=1 → a_δ=2·1=2 ✓; ℤ₃ letter → 2 leaves → δ·a₁=4≠2 ✓ = `f9_shape_contrast`. No covert (R4)/stability/dictionary/counting-layer use found in §§2–4.

**Charge (2) — N-K3 seal.** At my read (wallclock, this pass): `verification/openmath/` has NO K3/NK artifact (grep zero), no results section appended (note ends at ledger). All five folds predate any run, so the P1–P4 text as it stands is preregistered intact; triggers key on M4 rev-4's verbatim branch conditions, which I diffed against M4 §5.3 — exact.

**Charge (3) — sibling currency.** M1 ACCEPTED (88eef30) ✓; M5's residue sentence is verbatim M1 §0.4's current fence. M4 at rev 4 (4b919bd, latest commit) ✓. M2: REVISION 3 committed (f1b554b), a REVISION 4 pass-4 fold IN FLIGHT uncommitted at my read — both folds display-only, "identical in strength to REVISION 2" per M2's own header; its pass-4 finding 6 confirms DICT-0's four clauses stay OPEN and fences intact. So the seam substance M5 pins (factor-correspondence match, four open clauses, zero consumption) is unchanged; only the counter datum drifted, exactly what the seam note's dated read + RE-READ-AT-N-K3-FOLD rule covers. Not a substantive mismatch. Blueprint unmoved since composition; M7 exists and carries all five rows incl. [SYM-ID].

**Charge (4) — sediment.** Header count = ledger; batch-vs-ledger artifact naming reconciles under the DATING NOTE; "zero consecutive cleans stand" is correctly timestamped at the pass-5 fold (pass 6 lives in `H4M5_pass6_2026-08-08.md`, CLEAN, first of two, on this same commit ffc6d16). §0 item 1's "(D.8/D.10 bookkeeping)" gloss is coarser than §1.3's exact residue but current-correct (D.4 out) and conservative — below threshold. No contradictions.

**Charge (5) — fresh sweep.** I re-ran the compile gate myself: `lake env lean notes/openmath/H4M5_k3delta_attempt.md` GREEN, exit 0 (pass 6 could not execute it; this pass did). Fenced-face absence verified repo-wide (no `SymConf`/`intrinsic_law`/`tree_sym`/`dbase_sym`; Kernel.lean's `eq2_dim_of_skel` is the Stable-quantified CURRENT face, explicitly fenced off). Typed-skeleton names verified in H4/Defs.lean; declaration list exact. Note text unmodified since ffc6d16 — I adjudicated the same text pass 6 certified.

Nothing classifiable as critical error or justification gap. This is the second consecutive clean, model-diverse (pass 6 Codex, pass 7 Fable). The note now waits only on the N-K3 sealed run.

VERDICT: CLEAN