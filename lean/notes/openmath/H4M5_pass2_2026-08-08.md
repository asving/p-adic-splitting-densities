Hostile pass 2 result: **0 CRITICAL, 6 GAP**. This does not supply the second consecutive clean pass.

Repository grep could not run because the managed shell failed before execution (`bubblewrap is unavailable`). Sibling-currentness below is therefore checked against the current states supplied in the charge, not independently verified section-by-section from disk. No files were modified.

1. **GAP — hostile-pass header contradicts its authoritative ledger.**

   Quote, header: “**THREE passes ran**: pass 1 REJECT … pass 3 REVISE.”

   Quote, ledger: “**Pass 4 … ACCEPT**” and “executed **FOUR times**.”

   The closing ledger is unambiguous, but the opening gate summary remains stale. The present audit is the attempted second clean pass after ledger pass 4.

2. **GAP — M1 seam is stale after M1’s ACCEPTED state and expanded walk.**

   Quotes:

   - “H4-M1 consumed at its **ON-FILE status**.”
   - “its hostile passes … are **NOT asserted here**.”
   - `[RUN-PROD]`: “the residue of this row is **D.8/D.10 bookkeeping only**.”
   - `[LEDGER-READ]`: “It is **D.8/D.10-side ledger bookkeeping**.”

   Current M1 is ACCEPTED and its `(S1)–(S6)` R-pack includes the walked D.10 recentering leg. M5 therefore understates M1’s status and gives an obsolete perimeter decomposition: D.10 cannot remain indiscriminately assigned to the unproved residue. This does not invalidate the conditional theorem, because M5 conservatively retains the rows as hypotheses, but its dependency and residue accounting is not current.

3. **GAP — the operational N-K3 discussion does consume moved M2 content despite the blanket “not a dependency” language.**

   Quotes:

   - “H4-M2’s dictionary is **NOT a dependency of anything below**.”
   - P3: “the gcd criterion is **dictionary-side, H4-M2/Route-A material**.”
   - P4: the carrier bridge is “**H4-F5/H4-M2-side design**.”
   - §7: “**NOT CONSUMED: H4-M2**.”

   M2 is now revision 2, with KEY-FREE factor-correspondence matching and `[IN-C] = DICT-0` displayed as four open clauses. M2 is indeed absent from the §§2–4 proof, but it is used by §6 to classify gcd outcomes, carrier matching, and Route-A consequences. The note neither cites the current matching definition nor states whether its P3/P4 branch readings require any of the four open DICT-0 clauses. The theorem-level non-consumption claim survives; the note-wide and N-K3 consumption claims do not.

4. **GAP — M4 branch currency is asserted, not demonstrated against revision 4.**

   Quotes:

   - “check **(iii) first, then (i), then (ii)**.”
   - “A gcd-only violation … **fires branch (i): Route A dead, this note untouched**.”
   - P4 “**defers to M4’s recorded scope**.”

   These are operational commitments for the imminent run. M5 cites the older section labels and conclusions without displaying revision 4’s branch conditions or explaining how its “branch-robust-for-non-escalating-branches” qualification applies. In particular, the unconditional-sounding “Route A dead, this note untouched” must be traceable through the current KEY-FREE/DICT-0 state, not merely inherited from an earlier adjudication snapshot.

5. **GAP — P3’s sealed outcome language is internally ill-typed.**

   Quotes:

   - “This note makes **NO prediction** on Lemma 4’s gcd criterion.”
   - “A **gcd-only violation (P3’s non-prediction)** fires branch (i).”

   A declared non-prediction cannot itself be violated. The intended trigger appears to be an observed non-coprime pool or failure of the gcd criterion, but the sealed text does not name an exact Boolean event. Consequently P3 is appropriately agnostic mathematically, yet not exact enough as a preregistered branch trigger.

6. **GAP — P4’s derivation and violation handling are incomplete.**

   Quote: “the two instances of **P1** composed at the matched letter.”

   P1 is explicitly quantified only over realized `δ > 1` pools, so the `δ = 1` equality used by P4 is not an instance of P1. It must instead come from the theorem’s root instance, with the relevant rows/conformance assumptions. Moreover, violation handling enumerates P1/P2 and gcd outcomes but gives no explicit disposition for a P4-only failure after its carrier-persistence premises have allegedly been certified.

The core mathematical conditional remains sound on its displayed perimeter: the argument assumes `[RUN-PROD]`, `[LEDGER-READ]`, `[LET-DATA]`, `[SYM-ID]`, and `[M1-STATUS]`; it does not covertly use stability, `(R4)`, a cross-base dictionary, or M2 matching in the dimension induction.

The `(K3-delta)` non-discharge fence also remains intact. The potentially strong sentence “the Route-B promise … is DISCHARGED” is expressly limited to deletion of `(R4)` and dictionary dependencies. Elsewhere the note repeatedly states “**NOTHING IS DISCHARGED AS THE ROWS STAND**,” keeps H4-F5 fenced, and says no consumer is re-pointed.

VERDICT: GAPS-ONLY