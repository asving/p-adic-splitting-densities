1. **BLOCKER — Step 12 was not locally re-pointed.** Section 4 entry 10 and §20 identify Step 12 as a D-12r consumer, and the VC17 charge claims Steps 2/3/11/12/16/19 all display the new §4-pin conditionality. Steps 2, 3, 11, 16, and 19 do; Step 12 does not. It still directly describes the `(τ-irr)` saturation argument and lists `LIT-3`/GMN inputs without saying it is “perimeter-conditional on the §4 D-12r pin.” Section 20 itself acknowledges this discrepancy: “Step 12's text names no D-12r directly.” Central enumeration in §4 preserves the dependency, but it does not satisfy the stronger claim that every former consumption site was re-pointed locally with honest conditionality language.

2. **ADVISORY — Step 4 still lacks an exact §0 status grade.** VC16 finding 1 says the operative grade is `PROVED-UNVERIFIED` until the M17/RegPFinite faithfulness audit lands. Step 4 instead uses the descriptive hybrid “PROVED (dual provenance) + machine-checked Lean-core; VERIFIED-LEAN PENDING.” This is not a silent mathematical upgrade—the pending audit and containing Step-5 route are explicit—but it remains outside the declared vocabulary.

All other charged checks pass:

- The D-12r-specific residue is confined to `(HEN-LIFT)` and `(OM-SAT)`; the uniqueness companion lemma is explicit, and sibling debts remain decomposed at their prior homes.
- Section 3.1 has exactly eleven rows, mapping to `(H1)–(H9)` with `(H3) = {(ADM), (E-N)}`.
- The operative clause attributions agree: `(0)` and `(SQ)` unconditional; `(R)` and `(UB)` carry the same groups at §1, Steps 16/18/19 as applicable.
- Remaining twelve-row and former-`(H3)` enumerations occur only in clearly dated historical revision records or explicitly superseded annotations.
- The displayed walk remains conditional at its stated scope, with no unconditional `(ROOT)` upgrade.
- Acyclicity remains intact, including `17 ≺ 18 ≺ 18b`.
- No other regression was found in the REVISION-12/13-confirmed composition.

VERDICT: COMPOSITION-GAPS