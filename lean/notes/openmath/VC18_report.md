1. **ADVISORY — stale end-to-end debt row.** The operative §3.2 row “End-to-end passes on THIS document” ends with VC16 and says VC17 is owed, although VC17 ran and REVISION 15 now makes VC18 the outstanding pass. The headline, §6, footer, and §21 carry the correct current status. This is bookkeeping drift, not a proof or conditionality defect.

2. **PASS — both REVISION-15 fixes are present and sufficient.** Step 12 now locally states that its `(τ-irr)` TB-CAP leg is “perimeter-conditional on the §4 D-12r pin (entry 10),” and §20’s r15-5 acknowledgment is retained and marked resolved. Step 4’s status is exactly **PROVED-UNVERIFIED**, names the M17/RegPFinite faithfulness audit, and treats Lean-core provenance and Step 5’s independent route only as notes.

3. **PASS — all six former D-12r consumption sites carry local conditionality.** Steps 2, 3, 11, 12, 16, and 19 each locally identify the TB-CAP dependence as perimeter-conditional on the §4 D-12r pin. None treats the perimeter as discharged or as an (H3) member.

4. **PASS — eleven-row invariant and attribution.** The eleven §3.1 rows are:
   H1, FRESH, R1–R4, ADM, UB-X, PACK, H6, H7, H8, H9, and E-N. They map exactly to (H1)–(H9), with **(H3) = {(ADM), (E-N)}**. Clauses (0) and (SQ) remain unconditional; (R) and (UB) carry the displayed groups, with only the `(K3-δ)` fragment of (H4b) entering (R). D-12r remains separately literature-conditional in §4.

5. **PASS — acyclicity.** Every proof edge points backward in the displayed order. In particular, Step 1 has no T-6 forward edge, and the repaired value-side chain remains **17 ≺ 18 ≺ 18b**: Step 17’s `(ROW-Σ)` is M2-free, Step 18 does not consume Step 18b, and Step 18b consumes Steps 14/17/18.

6. **PASS — displayed-scope walk.** The composition closes only at **(ROOT-C)**. It does not silently prove unconditional `(ROOT)`, discharge the eleven [M] rows, discharge the §4 D-12r pins, or upgrade audit-debt leaves. The `Σ_τ R_τ = 1` identity remains correctly delayed to Step 19.

7. **PASS — regression audit.** No regression was found in the D-12r reduction, PACK-i–iv routing, `(H8)` reduced/open status, `(H9)` with only `(NSF-INT)` open, `(E-N)` rescope, unconditional `(0)/(SQ)`, per-clause attribution, seams, or quantifier scope. The sole issue is advisory finding 1.

**VERDICT: COMPOSITION-SOUND**