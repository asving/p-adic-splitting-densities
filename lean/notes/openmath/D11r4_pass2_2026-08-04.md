No CRITICAL defect remains. All six pass-1 repairs are substantively genuine, but three audit/bookkeeping gaps remain.

Repair verification

- M1(a′) ambient-measure repair: VALID. The revised text correctly distinguishes:

  > “vol(C_e) = q₀^{−e} (AMBIENT) … vol(R_e) = q₀^{−E} (AMBIENT)”

  from

  > “q₀^{−(E−e)} = P(R_e | C_e) (CONDITIONAL — the displayed ratio vol(R_e)/vol(C_e))”

  and from the ambient relaxed-box and conditional singleton-residual readings. The former blanket ambient claim is explicitly withdrawn at every substantive echo.

- (b-EX) route-(a) repair: VALID at its narrowed scope. The booked/solved-value distinction is principled for the actual conclusion: `x_σ` and the β-legs are recursively solved continuation values, whereas the only explicitly resummed block-time events are the `(O3)` kernel and `(O3)` scaled exits. The relevant consumers use those two resummations or use the balance algebra without expanding solved values. General-loop histories in `(O-1)/(O-5)` correctly rely directly on `(b-PF)`.

- (LOOP-∞) repair: VALID. The added events

  > “Z^{(L)} := {first L steps = (loop-0), the post-L state ∈ Z_e}”

  restore the missing finite-time alternatives. Their nullity and mutual disjointness follow from the repeated canonical-law property and the one-step partition. The displayed decomposition is now exact when “stopping words” denotes the first non-`loop-0` words used there.

- (O-4) repair: VALID. For a fixed loop word, the corrected product

  > “∏ … P(s_j | C_μ) = q^{−E_μ·|w|}”

  is right: every particular `loop-0` or `loop-c` letter has conditional mass `q^{-E_μ}`. The withdrawn `(κ₀+κ₁)^{|w|}` was indeed the sum over all loop-letter choices.

- K1 invocation-side repair: HONESTLY PRICED. The false source-inheritance inference is withdrawn, and the remaining implication is explicitly conditional on:

  > “(K1-CANON) … NOT proved by this leaf’s quotations”

  Thus it is not smuggled into M3 or K4. `(NSF-INT)` correctly remains hypothesis-grade at `(H9)`.

- Closure-completeness repair: VALID as a rescope. `(EDGE-RES)` and `(CH-RES)` now expose precisely what the quotations do not establish. The edge and channel conclusions are corpus-relative rather than unconditional. `E-M3` also correctly records the still-owed ROOT delivery rescope.

No-churn check

- `(ROW-Σ)`: the per-booking totals and `Σ_σ β_e=1` derivation are unchanged and remain correct.
- `(G-a)`–`(G-d)`: the global termination induction is unchanged modulo the corrected `(O-4)` parenthetical and enlarged null set; both changes strengthen rather than damage it.
- M2: the `(RE)` statement, `(PACK-iv)` pricing, arbitrary-α exclusion, and `(S1)/(S2)` conditionality remain intact.

Remaining findings

1. GAP — the claimed `(b-EX)` consumer inventory is not literally complete.

The inventory says:

> “every consumer of (b-EX) partitions over BOOKED events only — (a)’s (O3) bullet … (ROW-Σ) step 3 … M2’s exhaustiveness note … and (b)/§5’s scope sentences”

But M1(c)’s `(O3)` termination reread is also a consumer:

> “the complement of E_rec in the loop region is exactly the scaled exits”

and `(LOOP-∞)` supplies the exact decomposition used by that reread. Re-running this omitted consumer does not defeat route (a): it too partitions only the two booked `(O3)` resummations plus null residues. The route is sound, but the “every consumer” audit list is incomplete.

2. GAP — the arbitrary-α discussion still uses event-booking language stronger than `(a″)` permits.

The document correctly states:

> “family-(iv) verdict rows under ARBITRARY allocation data α_e … need not be masses at all”

but the `(b-EX)` family-(iv) walk says:

> “the (O2) α-fold books the one-step recentering mass κ₁ into the verdict rows … the booked strata are the one-step recentering strata”

Only the total over σ is fixed to κ₁. For arbitrary rational allocation data, an individual `α_e(σ)q^{-E}` may be negative or exceed any event mass, so it cannot generally “book” a stratum for that row. This does not invalidate `(b-EX)`’s multi-step conclusion—the `(O2)` adjustment has no multi-step content—but the stated row-level event interpretation conflicts with `(a″)`.

3. GAP — `(NSF-INT)`’s advertised retirement gate does not discharge its new open premise.

The document says:

> “RETIRED only at gate I-3 = this file’s owed hostile pass accepting §4B AS A PROOF”

while §4B now expressly says:

> “(K1-CANON) … NOT proved by this leaf’s quotations”

and:

> “claim (ii)’s K1 leg is proved UNDER it.”

Accepting the conditional argument verifies that `(K1-CANON) ⇒` the K1 scope conclusion; it cannot itself establish `(K1-CANON)`. Current use is honestly priced, so no theorem is invalidated, but the stated retirement route is incomplete unless I-3 separately adjudicates or receives a discharge of `(K1-CANON)`.

Fresh sweep found no additional critical arithmetic, measure, termination, or M2-scope error.

VERDICT: GAPS-ONLY