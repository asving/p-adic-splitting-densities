# H2-P1 — (c-GR) — verify brief, rev 0 (Fable prover H2-P1, 2026-08-01)

**Document under verification:** `H2_cGR_attempt.md` (same directory, rev 0).
**Unit spec:** `lean/blueprints/HDISCHARGE_H2.md` §3.3 (c-GR) + §6 H2-P1
(PAPER unit, wave 1). **Gate:** the standard fresh-context hostile pass;
repo bar = several consecutive clean passes (parent CLAUDE.md verification
rule). This brief is the verifier's charge sheet; it adds no mathematics.

## 1. What acceptance would mean (and not mean)

Accepted, the attempt upgrades O-9 rev-5 §5.4's mechanism note for clause
(c) ("NOT A PROOF") to a conditional lemma: **(GR-B) as displayed ⟹ the
graded-level junction pin**, per parent point, with the what-ρ-fixes
content displayed (Lemma ρ-FIX). Combined with H2-P2 (separate unit), it
shrinks (H2) = (FRESH) to exactly the (TRANS-DEEP) kernel. It does NOT:
discharge (GR-B) (open at orders ≥ 2); touch (c-TR) = (TRANS-DEEP)(J)
(the finite-level descent, THE open kernel); assert anything at working
level N; or change any fenced statement.

## 2. Verifier charge (verbatim repo discipline)

Fresh context, repo-blind except the handed documents. Quote each
offending passage and classify: **critical error** (breaks the logical
chain — stop checking dependents) vs **justification gap** (assume the
conclusion, keep verifying downstream). Fix nothing. Hand the verifier:
`H2_cGR_attempt.md` + (for pin-checking) `O9_phaseB_verifybrief_rev5.md`
§§1–2, 5.1–5.4, 6.4 and `GD23_phaseB_verifybrief_rev4.md` §7.2 + the H2
blueprint §§1, 3.3, 5.

## 3. Attack list (where to press hardest)

1. **(†1)'s dictionary shape** — the claim that (2′)'s ∼-dictionary is
   geometric (one scalar + one stride ratio, dependence only on
   (frame, α, s₀)) with the CITATION reading of the GD23 (2′) display +
   O-9 §6.1 TYPE-MARCH consequence. Is this a faithful citation or a
   strengthening? (The attempt flags it as (2′)'s march content; if the
   verifier reads the display as weaker, that is a critical finding
   against Step 1.)
2. **ρ-FIX(i)'s scope** — "each parent-keyed read is level-N₀-determined"
   as the meaning of CEN-J's standing (DET)-over-parent-reads hypothesis;
   the r-fold ledger composition is explicitly deferred to H2-P2. Is
   anything beyond the standing hypothesis silently consumed (e.g. that
   R_r(f)'s FULL coefficient list, zero slots included, is parent-keyed)?
3. **Step 3's vertex-attainment argument** — u_{k_s} = U_s forced by the
   keying (lower-hull right-end vertex must be attained). Check the
   degenerate shapes: ties u_k = U_s at k > k_s (handled via (†5)); s = 1
   single-face data; k₀ = 0; the left-tail integral-value slots.
4. **Step 5's degree-0 identification** — 𝔊₀ ≅ F_{r+1} via F̃[ȳ]/(Ψ),
   and the cancellation (†6) (marched root ρ_m^{-1}z_r against twist
   U(ρ_m·)). Check the Δ-index insensitivity note (N2) really covers both
   readings, including deg Ψ = 1 at F̃ = F_{r+1}.
5. **Step 6's [ANCH] step** — anchor-ratio images lie in ♮(F_{r+1}^×);
   the weight-0 computation β_{k_s} = u_{k_s} − k_s·V̂.
6. **Per-point vs per-realization scope (§1.4)** — verify the M08
   free-end witness (n > e) really refutes the coarse-scope reading, and
   that §5's fit to CEN-J §6.4 step 3 needs only the per-point form
   (Lemma C value-irrelevance).
7. **Clause (4)'s disposition (Step F)** — the attempt says (4) is
   warrant + cross-check, not a value-chain link, and flags this against
   the blueprint's input list. Confirm no hidden firing of (4) inside
   Steps 1–6 (in particular: is exactness of (c-GR-1) really carried by
   U(z_r) ≠ 0 + ψ̂ prime alone?).
8. **(†4)'s fixedness inventory** — every factor of 𝔪 (ϕ(f), ϕ_φ, τ_f,
   τ_φ) claimed (frame, D, discrete-shape)-fixed; in particular
   s₀(f) discrete-shape-determined via ρ-FIX(i). A single f-dependent
   factor anywhere is a critical error against (c-GR-3).
9. **Quantifier hygiene** — the lemma is ∀ f in the fiber; check no step
   silently picks a convenient f (esp. Steps 1 and 4, where P_f and γ are
   f-keyed objects equated to ρ̂-keyed ones).
10. **Consistency with the falsifier record** — nothing claimed that the
    monic-top-only K2 cells could have probed; (N6)'s honesty about the
    queued free-end gate (H2-N1).

## 4. Acceptance bookkeeping

On a clean pass: record the verdict here as rev-1 header material; the H2
lead folds the unit into the blueprint's §3.3 row ((c-GR): written proof
ON FILE, conditional on (GR-B)) and the ROOT (FRESH) row notes. On
findings: standard disposition table (finding → class → repair site),
rev the attempt, re-run fresh-context. No Lean artifact is owed by this
unit; the (TRANS-DEEP) Lean row stays gated at H2-U9 (B2-FINAL
acceptance), per blueprint §6.
