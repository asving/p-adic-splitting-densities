All verification legs are complete. Assembling the decision.

**Legs used (per the mandatory methodology):** (1) semantic — my own derivation of the i=0 vanishing from eq (12)'s level-0 normalization + the printed prime-free Bézout block (fgmn.txt:1077–1080); (2) semantic — Lemma 4.3's Bézout algebra threaded through Thm 4.2, re-derived from scratch (2×2 exponent solve) and hand-checked symbolically (0/20,060); (3) the harness `grb_order2_check.py` re-run this session — deterministic, ALL GREEN, 33,547 samples, output matches the leaf's table to the digit (incl. P12 [False,True]×2, P3 145/13/0, T9(iii-C) 300/300); (4) fresh extraction — `pdftotext -layout` on `/tmp/litunit/fgmn.pdf` (independent of the corrupted raw extraction), which preserves horizontal geometry.

**CHARGE 1 (correction-layer mathematics) — SOUND.** (a) The corrected exponent ǫ_i(α) = z_i^{ℓ′_is(α) − ℓ_iu(α)} is RIGHT: rewriting x_{i−1}^{s̄}p_{i−1}^{ū} in level-i terms via p_i = x_{i−1}^{ℓ}p_{i−1}^{ℓ′}, y_{i−1} = x_{i−1}^{e}p_{i−1}^{−h} forces y-exponent b = ℓ′s̄ − ℓū (h·+e·, ℓ′·−ℓ· eliminations, Bézout ℓh + ℓ′e = 1); the printed Lemma 4.3 proof chain telescopes only with this attachment (verified 0/20,060); pin 1 discriminates (ℓ₀ = 0 forced by the printed `0 ≤ ℓi < ei`; misreading gives z₀^{−u(α)} ≠ 1); and the layout re-extraction shows the floating ′ at column 51 directly above the FIRST ℓ (col 50, the s-multiplier) — the PDF's true display is ℓ′_is(α) − ℓ_iu(α). (b) ǫ₁ ≡ 1 at order 2, e₀ = 1: derived — 0 ≤ s < e₁ = 1 gives s ≡ 0; 0 ≤ ℓ₁ < e₁ = 1 gives ℓ₁ = 0; both exponent terms vanish. The FGMN/GMN coefficient agreement at the S3.2 instance re-derived by hand (c̄z₁^k both sides). (c) TR-3′-GEN two-stratum form: coherent, both strata honestly OPEN; the REV-12 revival claim verified (e_j ≥ 2 ⟹ ℓ_j ≠ 0, so ǫ_j nontrivial even with e₀ = 1); the "equivalently whenever every relevant preceding e_j = 1" extension is arithmetically sound (all-unramified-below kills every level's twist).

**CHARGE 2 (carried/re-earned split) — EXACTLY RIGHT.** Member-checked: TR-1/TR-2/TR-5/TR-6-narrow/CARRY-1+D-REAL/OP-A3 consume no ǫ (slope/filtration/bookkeeping/properness-home/eq-(12-int) arithmetic/hypothesis display); D-REAL's ǫ-recurrence cite is existence-only, attachment-independent. TR-4/TR-7 correctly re-earned, not carried (both concern the ǫ-dependent operator; P3's 145 violations prove TR-7 was never ǫ-free); TR-4's no-harness-family debt is displayed in the S0 key, R11.5, and R13.2 — evidence neither over- nor under-stated.

**CHARGE 3–5 (inter-layer, residue, fresh sweep).** My own greps: every γ₂-law occurrence (9 sites) is inside superseded/record/bracket context; the misread exponent appears exactly 3× — all inside correction brackets (S3.1, N-1, R11.1); no live sentence consumes the artifact law or unscoped stratum-wide triviality; gmn.txt:1824–1833's t-formula verified single-unprimed-ℓ as claimed. Three-taxon residue display complete, consumers routed (TR-3′-GEN → GD3-FULL/OL-B; TR-3-ORD-X → Route B + clause-(4); D-ITER → IFACE(c)); S6.4's FGMN-internal sentence stands honestly. The orphan-glyph position (own line, two above the exponent) confirmed on disk. Checked and cleared as sub-threshold: the N-1 record's "ℓ₀ = 0 … so ǫ₁ ≡ 1" (harness-local naming, documented in the cited docstring); census "6 patterns" vs pass-18 "8-pattern grep" (two distinct grep events, no contradiction); "T9 = this exact S3.2 instance" (under-claims the tested scope — the honest direction).

**ONE FINDING.**

> S3.1 [REV 11] bracket: "At acceptance this display read ǫ_i(α) = (z_i)^{ℓ_i s(α) − ℓ′_i u(α)} — the prime attached to the WRONG ℓ. Mechanism (verified on the extraction): … the ′ of Def 3.15 is an ORPHANED GLYPH that pdftotext floated onto its OWN line …, **and the leaf re-attached it to the first ℓ**"

**JUSTIFICATION GAP (mechanism narrative inverted).** In the quoted acceptance display the erroneous prime sits on the SECOND ℓ-occurrence (the u-multiplier); the FIRST ℓ (the s-multiplier) is where the prime truly belongs (confirmed by the layout geometry: ′ above col 50). So "re-attached it to the first ℓ" states the opposite of what happened — under it, the leaf would have transcribed the CORRECT exponent. The sentence contradicts both flanking displays in its own bracket. Same loose-provenance genre at the N-1 headline ("a floating glyph that pdftotext attaches to the wrong ℓ" — pdftotext attached it to neither; it orphaned it, and the leaf re-attached). No mathematics consumes either sentence — every formula-bearing display of both readings is correct and consistent leaf-wide, so this is not critical. But the provenance-of-the-error display is load-bearing for THIS correction arc (the whole incident is about which ℓ got the prime), and by the leaf's own operating standard (pass-9 false adjective, pass-12 wrong pointer, pass-16 scope qualifiers — all folded as gaps) an inverted mechanism clause in the headline correction bracket is a classifiable defect.

Nothing else classifiable survived verification: the corrected mathematics, the acceptance annotation, the census, and the residue display are sound.

VERDICT: GAPS-ONLY