# O11 — SERIES-TIE ASSEMBLY: the per-σ tree-series = R_σ value tie — Phase-B verify brief REV 2 (Fable, 2026-07-31)

You are verifying a mathematics note. Quote each offending passage and classify
it as *critical error* (breaks the logical chain) or *justification gap* (assume
and continue); fix nothing. This is REV 2, repairing the pass-1 hostile
verification (`O11_pass1_verify.md`, fresh context, 2026-07-31: **NOT ACCEPTED
AS STATED — 1 critical + 8 justification gaps**) of rev 0
(`O11_phaseB_attempt.md`, commit 3fc8f52; no earlier verifybrief exists — the
attempt was the pass-1 subject). Pass 1 explicitly CONFIRMED (its "Checked and
CLEAN" list): the interface equivalence directions (§2.1/§2.2 consumed
orientation-ward only; the §3.5 glue never routes through (D_σ) ⇒ row; drainage
verifiably unconsumed by the §3 walk); that each of the four tier-1 items is
consumed at a displayed step (the "nothing smaller" half of §0); the D-8 guard
engaged only at K1-b's fiber-value clause; the (E-N) non-consumption's internal
coherence; falsifier T1's decision-rule soundness (rule verified by direct
inspection); and the consumption sweep of §5 rows 1/2/3/8/10/12/13/14/16/19 (no
further beyond-set consumption). Everything confirmed is carried **stable**:
§§1, 2.1–2.3, 3.3 (K2), 3.5 (the glue), 4, and the unflagged §5 rows are
word-for-word rev 0 except for explicitly marked *rev-2 notes* in square
brackets (statuses refreshed where an owner unit moved AFTER rev 0 was
written; no consumed statement or walk text touched; line wrapping may shift
where a bracket tag was inserted — §1, §2.1–2.3, §3.3's 𝔅_n bullet, §3.5's
walk, and §4's bullets are byte-verbatim substrings). The repairs live in §0,
§2.4, §3.1 (K4-CYL), §3.2 (K1-a, K1-c), §3.4 (K3-a, K3-b status, K3-c), §5
(rows 4/7/9/11/15/17/18/20 + footer), §6, §7 (rewritten), §8, §9.

## 0-pre. Rev-2 changelog (pass-1 findings → dispositions → repair sites)

| pass-1 finding | class | disposition | repair site |
|---|---|---|---|
| **C-1**: (M6b)(iii) consumed at K4-CYL/row 4 (an open modulus of D-15 = O-1thr Thm 3) but priced into NO tier — the §0 "exactly FOUR ... no fifth mathematical open is consumed" headline unsupported by the document's own display | CRITICAL | **REPAIRED (priced, not closed)** — (M6b)(iii) is NOT closed at O-1thr rev 4 and is NOT (GR-B) content; it is a **GD-4 carrier** verbatim (tree `MATH_COMPLETION_TREE_2026-08-01.md` §GD row GD-4: "O-1thr **(M6b)(iii)** + **(M6b)(ii−)**"; engine bookkeeping, not GMN mathematics). Both it AND (M6b)(ii−) (the lower (N4) band, which rev 0's row-4 status OMITTED — the full O-1thr rev-4 Thm-3 residue is (M6b)(iii) + (M6b)(ii−) + (V1)/(V2)-beyond-slice) are now **DECLARED row D-a of §0**, owner GD-4 = the ROOT ledger's [T] row "GD-4/GD-6 engine-bookkeeping units" (`docs/ROOT_ASSEMBLY_2026-08-02.md` §3.3; proof-by-construction routes on record). The "exactly FOUR" headline is WITHDRAWN and restated: four tier-1 [M] items + three DECLARED interface rows. ROOT ledger line owed (§9) | §0 headline + D-a; §3.1 (K4-CYL); §5 row 4 + footer |
| **G-1**: K1-c's "its ingredients are the GD-2/GD-3 dictionary ... so the residue is (GR-B)" — a reduction asserted by citation to a *pricing*, not a proof; presentation existence/τ-inequality could hide a fifth open under (GR-B)'s label | GAP | **REPAIRED (honest split + declared row)** — the (GR-B) containment claim for the whole of (I-τ)/K-LOC is WITHDRAWN. Content split displayed: (a) dictionary ingredients = GD-2/GD-3 instances (order ≤ 1 PROVED, GD23 arbitrary DVR; order ≥ 2 = (GR-B)); (b) presentation existence ((A1)–(A3) at realized site strata) + τ(c) ≤ thr(c) + ρ(c) + reads-within-ceiling — NO proof and NO proved reduction to (a) on file. (I-τ)/K-LOC = M07 OL-3 is **DECLARED row D-b of §0**, owner M07 OL-3 / GD families = the ROOT [T] row "(I-τ) + K-LOC interface" (which already lists Steps 15 AND 18) | §0 D-b; §3.2 (K1-c); §5 row 11 |
| **G-2**: K3-c's (R1)–(R4) → (GR-B) discharge asserted via "the adopted discipline"; the display that O-8b's O_δ re-based laws at order ≤ 1 are GD-family INSTANCES (vs needing a further re-base lemma) is missing — rev 0's own unadjudicated charge (iii) | GAP | **REPAIRED (route honestly demoted)** — the "discharged ... so (GR-B) again; no fifth open item" claim is WITHDRAWN. (R1)–(R4) is **DECLARED row D-c of §0**, owner O-8b/D-3 = the ROOT ledger's own **[M]** row "(R1)–(R4)" ("borderline [M]/[T]; kept for honesty"). The arbitrary-DVR route is displayed as a ROUTE with its missing step NAMED: the order ≤ 1 instance display is UNWRITTEN (charge (iii) stands, §8). Scope-hygiene audit rides the restated D-11 entry, item (vi) | §0 D-c; §3.4 (K3-c); §5 row 18 |
| **G-3**: D-11's content elastic — two contents (K3-c scope hygiene; row 20's pack laws at §3.5) made to "ride" D-11 beyond its §0 display | GAP | **REPAIRED (stable-content display)** — the §0 D-11 entry is restated ONCE with ALL seven contents/entry points (i)–(vii) enumerated, including the two wideners plus a third found at rev 2 (K3-a's general-n fence conformance, per G-5). Items (v)–(vii) WIDEN the dispatch's WAVE-8 charge: acceptance owed by the D-11 rev-2 owner (D-11 pass 1 ran: commit 2587c70, NOT ACCEPTED as billed; rev 2 owed per ledger 078b042) — until accepted they are flagged as D-11-charge-pending, not silent. ROOT line owed (§9) | §0 D-11 entry; §3.4 (K3-a/K3-d); §5 rows 15/20 |
| **G-4**: K1-a's "the CU cluster" — an unnamed, unstatused load-bearing consumption (which CU units serve CUT-WD/(SIB-STEP), at what status?) | GAP | **REPAIRED (de-anonymized)** — (SIB-STEP) ← O-10 Thm 2 + the declared (I-τ) row (O-10 §5.3's T-6 bullet, verbatim); CUT-WD ← O-1thr Thm 1 (= D-14, PROVED outright, every policy) + **CU-3** (= F3′, the O-3 leaf's re-label: O-14a′ Thms 1–3 + D-14 + (O6); RE-LABEL complete, CUC §3/§8; rider P(O-1thr r4); realizer leg unconditional) — read off the tree's T-6 row ("consumes leaves O-1/O-3/O-10 for its open kernels") + O-10 §5.3. Per-kernel mapping is displayed reasoning, not T-6's own text: NEW verification charge (vi), §8. No unnamed consumption remains; CU-3 adds no new open | §3.2 (K1-a); §5 row 9 |
| **G-5**: K3-a's H.6 fence-respect at general n rests on n = 3 numerics ("sealed evidence"), displayed in no tier | GAP | **REPAIRED (priced into D-11's restated charge)** — split: the fences GATE the [1v]-FULL event at every n (T-5 dual-accepted; tree T-5 row); that the BUILT per-n tables sit INSIDE them is proved nowhere at general n — V-n3/H-n3 are n = 3 evidence. General-n in-fence conformance of the constructed pack = construction obligation of the instance pin, item (vii) of the restated D-11 entry (same territory as row 20's pack laws: O-14d/D-11) | §0 D-11 item (vii); §3.4 (K3-a) |
| **G-6**: "Finding F1's test EXECUTED AT THE REAL PACK" overstates — what ran is `om_density_engine.alpha_full(2)`, a Python symbolic engine; the pack object is pinned only by D-11 (PENDING), so "the REAL pack" does not yet denote a pinned object | GAP | **REPAIRED (language)** — all three sites (§0 header para, §2.4(ii), §7 V1) restated: V1 executes F1's test at the **ENGINE PROXY** of the real pack (the S-area engine, the same block-recursion pipeline RESUM-n3 gates at n = 3), identified with the intended pack by PROVENANCE, not by a pinned object; the pin IS D-11. The test's content is unchanged and remains the dispatch's named falsifier | §2.4(ii); §7 V1; header |
| **G-7**: T1 presented as if the real classifier's decided classes were enumerated (they were not — `decide()` is a hand-written discriminant rule); count claim "38/38" false (37 report lines; the 38th match is the "ALL PASS" summary); "24/24" is 23/23; commit message repeats 38/38 | GAP | **REPAIRED (record + display)** — §7 rewritten: counts corrected to **37/37** (V1 8, V2 6, T1 23) and re-confirmed by a fresh run 4 (2026-07-31, byte-identical results, exit 0); T1's rule displayed as a discriminant PROXY (sound by pass-1 direct inspection), refuting the VALUE R_σ(p) on violation, transferring to "(D_σ) at the real classifier" only through this unit's conditional inputs; symbol fixed R_σ(p) (rev 0's ρ_σ collided with ρ(c), the Jacobian exponent). Commit 3fc8f52's message stands inaccurate in history — noted on the record here and in §9 | §7; §9 |
| **G-8**: "two dead-code defects ... fixed between runs 1–3" false — both were LIVE and CRASHED runs 1–2 before any V2/T1 check; sealed V2/T1 first exercised at run 3; run-3 V2 weaker than the sealed prediction (oracle checked only at 4 numeric points; transcription trusted); "no sealed prediction changed" unverifiable (runs 1–2 predate version control) | GAP | **REPAIRED (honest history + the gap CLOSED by a supplement)** — §7 run history rewritten: run 1 crashed with `ZeroDivisionError` at `pt.monic_cubic_pred(1)`, run 2 with `TypeError` at `pt.monic_cubic_pred(q)`, both BEFORE any V2/T1 check line executed ("dead-code" withdrawn); V1's 8 checks passed identically on all four runs; "no sealed prediction changed" is restated as the author's claim, unverifiable from the repo (single harness commit). The executed-vs-sealed V2 gap is now CLOSED without touching the sealed harness: NEW supplement `o11_v2_oracle_pin.py` pins alpha_full(3) == the ACTUAL `monic_cubic_pred` as RATIONAL FUNCTIONS (exact agreement at 44 integer points ≥ 2 with displayed degree budgets B ≤ 12 per type; 10/10 PASS, exit 0) — the in-harness transcription `true3` is thereby verified a posteriori | §7; new `verification/openmath/o11_v2_oracle_pin.py` + log |
| Minor 1: §0 "(GR-B) enters through THREE faces" vs six §5 rows (K4-CYL face missing) | note | **FIXED** — FOUR faces displayed (K4-CYL added); six rows reconciled in the §5 footer | §0; §5 footer |
| Minor 2: standing ns-free perimeter absent from §0 tier-2 | note | **FIXED** — added to the tier-2 list | §0 tier-2 |
| Minor 3: ρ_σ symbol collision + undefined | note | **FIXED** — R_σ(p) throughout §7 | §7 |
| Minor 4: unit + run of record dated 2026-08-02 (had not occurred at file mtime Jul 31) | note | **FIXED** — rev 2 dated 2026-07-31; run of record = run 4, 2026-07-31; rev 0's mis-dating recorded | header; §7 |

**Statement-change disclosure (per the repo statement-fence):** NO consumed
statement is touched; the Lean row `series_tie` is untouched (PARKED). What
changes at rev 2 is the unit's OWN residue display (the pricing headline),
serving names, and the falsifier record. The §3.5 walk and every K-face
statement consumed from an owner unit are byte-stable. The falsifier harness is
byte-stable (single commit 3fc8f52); run 4 re-executed it unmodified; the new
oracle-pin supplement is a separate file.

**Rev-2 status refreshes (owner units that moved after rev 0 was written, all
marked where used):** CU-2t — the terminal-seam lemma is now PROVED at CUC §9
(unit D3CU2t; landed in the 254d4de sweep, cross-referenced at aa00747),
conditional on P(O-2a r5) + D-12r + GD residue; hostile pass owed (ROOT [V] row
"CUC + CU-2t hostile pass"); retained tier-1 here pending that pass, matching
ROOT Step 18. D-11 — pass 1 ran (2587c70): NOT ACCEPTED as billed (1 critical:
M1(c)'s (S1) consumption mis-ledgered; M1(a,b,d), the (O3) resummation, and the
uniqueness pivot survive); rev 2 owed. O-9 — pass 1 NOT accepted (c17777f),
REPAIRED at rev 2 (01c21e6): E′ strict left tail; JUNCTION-PIN rescoped OPEN as
(FRESH)(c); NEW displayed hypothesis **(ADM)** on CEN-W/CEN-J (automatic at
orders ≤ 1, open at general order) — consumed here at K3-b, displayed at row 17.
GD23/CU-1/O4T gap repairs landed (wave 12, 9102337).

## 0. Verdict and the residue display (the headline, restated at rev 2)

**VERDICT: ASSEMBLED — PROVED-UNVERIFIED, conditional on the FOUR tier-1 named
open items {(GR-B), (FRESH), CU-2t, D-11} PLUS THREE DECLARED interface rows
(D-a/D-b/D-c below), each a named row of the ROOT conditionality ledger
(`docs/ROOT_ASSEMBLY_2026-08-02.md` §3) with an owner.** Rev 0's headline
("conditional on exactly FOUR named open items ... no fifth mathematical open
is consumed") is WITHDRAWN as written — pass-1 C-1: the document's own
consumption display prices SEVEN named items. Nothing smaller was achievable
(pass-1 confirmed each of the four is consumed at a displayed step); nothing is
hidden: the three declared rows are consumed at displayed steps below, carried
on the ROOT ledger at classes [T]/[T]/[M] respectively, and each has a named
owner and discharge route. The dispatch's expected set {D-11 pending, (GR-B),
(FRESH), CU-2t} is matched at the [M] tier; the declared rows are the honest
excess the pass-1 verifier demanded priced.

**Tier-1 [M] (the four, statuses refreshed):**

> **(GR-B)** — the order ≥ 2 side-residual/carry-algebra package (GD-2/GD-3's
>   one open input; GD23 §7.2; order ≤ 1 PROVED over an arbitrary complete
>   DVR; GD23 rev 4 gap repairs landed, gap-check pass owed). Enters through
>   FOUR faces here [rev-2 fix of pass-1 minor 1]: (i) the (I-τ)/K-LOC engine
>   presentation's dictionary INGREDIENTS at K1 (§3.2, via D-b), (ii) the
>   census weights of K3 (O-9 CEN-W, §3.4), (iii) the O-8b re-base ROUTE's
>   order ≥ 2 leg (§3.4, via D-c — conditional on D-c's unwritten instance
>   display), (iv) the K4-CYL face: D-15's (V1)/(V2) beyond the proved slice +
>   CU-1's and CU-2's GD residues (§3.1). Six §5 rows (4/5/6/11/17/18), one
>   package (the D2.2 merge).
> **(FRESH)** — the joint-with-parent level-determinacy separation (O-9's
>   named clause, priced at the GD-2/CL-13 vertex-law layer = CU-1's step
>   content; at O-9 rev 2 it carries clause (c) = the rescoped JUNCTION-PIN
>   and the quantitative floor clause (a′)). Enters through K3's joint census
>   CEN-J (§3.4).
> **CU-2t** — the terminal seam: verdict-carrying terminal records are FORCED
>   by (f, prefix). Rev-2 status refresh: PROVED at CUC §9 (unit D3CU2t),
>   conditional on P(O-2a r5) + D-12r + GD residue; hostile pass owed (ROOT
>   [V] row); RETAINED tier-1 pending that pass (matching ROOT Step 18's
>   display). Enters through K1's leaf verdict labels and K4's decided
>   classes.
> **D-11 (PENDING — the instance pin; pass 1 NOT ACCEPTED as billed, rev 2
>   owed)** — restated ONCE, in full (the stable-content display; pass-1 G-3
>   cure). Its charge as consumed by THIS unit:
>   (i) kernel entries ARE the Haar volumes of the T-3/T-6 continuation
>       events (K3-d);
>   (ii) block system = 𝔅_n, or covered by O-12 Theorem 3's robustness (K2
>       pack side, §3.3);
>   (iii) per-box lift uniformity via O5triple Thm E + D-15 (the WAVE-8
>       pricing; no OB2/∼_OM consumption);
>   (iv) the wiring fence of §2.4(i): the statement's C is the real per-n
>       pack;
>   (v) the pack's carried laws rsh_interp / r_bdd / wsh_ok (§3.5, row 20 —
>       pack construction = O-14d/D-11 territory);
>   (vi) the DVR scope-hygiene audit of K3-c (each consumed table law stated
>       at DVR generality at the pack);
>   (vii) general-n H.6 fence conformance of the built tables (K3-a; n = 3
>       evidence V-n3/H-n3).
>   Items (v)–(vii) WIDEN the dispatch's WAVE-8 charge: acceptance owed by
>   the D-11 rev-2 owner (noted for the ROOT ledger, §9); until accepted they
>   are displayed here as D-11-charge-pending, not silently ridden.

**DECLARED interface rows (consumed beyond the four; the C-1/G-1/G-2 cures —
each derives from NO member of the four on any proof on file):**

> **D-a. (M6b)(iii) + (M6b)(ii−)** — the O-1thr rev-4 interface residue of
>   Theorem 3 (= D-15) at K4-CYL: AUX-at-harvest + the lower (N4) band
>   ((M6b)(ii−) was OMITTED by rev 0's row-4 display; both are consumed).
>   Owner: **GD-4** (tree §GD row GD-4 carriers, verbatim: "O-1thr (M6b)(iii)
>   + (M6b)(ii−)"; engine bookkeeping, NOT GMN mathematics — so NOT (GR-B)
>   content). ROOT class **[T]** (row "GD-4/GD-6 engine-bookkeeping units";
>   proof-by-construction routes on record in the O-2a and O-1thr briefs).
>   Priced, not closed: no claim that either clause is discharged.
> **D-b. (I-τ)/K-LOC (= M07 OL-3)** — realized site strata ARE adapted
>   (A1)–(A3) presentations in branch-factor coordinates with
>   τ(c) ≤ thr(c) + ρ(c) and reads within ceiling. OPEN; owner M07 OL-3 / GD
>   families; ROOT class **[T]** (row "(I-τ) + K-LOC interface", which
>   already lists Steps 15 and 18 as consumers). Its GMN-dictionary
>   ingredients are (GR-B)-priced (§3.2); its presentation-existence content
>   is NOT — no proved reduction on file (pass-1 G-1).
> **D-c. (R1)–(R4)** — O-8b's base: the T-1 stage-tower laws re-proved
>   verbatim over O_δ = W(F_{p^δ}). Owner O-8b/D-3; ROOT class **[M]**
>   ("borderline [M]/[T]; kept for honesty"; N8 exhaustive, 199,723
>   alphabets, 0 violations). Displayed route: the arbitrary-DVR discipline
>   (DELTA-2 §D2.6 step 3) + GD23's order ≤ 1 DVR layer + (GR-B) at ≥ 2 —
>   conditional on the UNWRITTEN order ≤ 1 instance display (§3.4; charge
>   (iii) stands).

**Tier-2 displays (NOT new mathematics; carried per the honesty discipline):**
inherited perimeters — D-12r (rides every TB-CAP window consumed here, via
CU-2t/D-15); **the standing ns-free perimeter** (T-6, tree §2.4) [rev-2 add,
pass-1 minor 2]; the O5triple five flags (GMN-COH)/(R-0)/(R-1)/(R-2)/(R-3) =
GD-1/GD-7/GD-8 literature-transcription duties (owner: the GD campaign's
literature surface; NOT covered by the executed LITUNIT); O-9 rev 2's (ADM)
hypothesis on CEN-W/CEN-J (automatic at orders ≤ 1; per-datum decidable;
displayed at its owner, rides O-9's re-pass — §3.4 K3-b); LIT-1's
LITERATURE-CONDITIONAL grade (§D1.5(b)) — and the pending-pass registry (§6).
Nothing in tier 2 adds an open lemma; each is either a verification duty or an
inherited perimeter already displayed at its owner.

## 1. The target statement [byte-stable, rev 0 §1 verbatim]

Lean row (normative form, `lean/LeanUrat/MovesU/BridgeKernels.lean:223–224`):

```lean
series_tie : ∀ σ : SplittingType n,
    F.seriesSum σ = ENNReal.ofReal (C.chain.Rval (vmap C.T σ) (p : ℚ))
```

Mathematics (the [3t] ↔ [3] solve seam, per-σ): for every splitting type σ
of degree n and every prime p,

> **(TIE_σ)**  Σ_{T ∈ Trees_σ(T_can)} mass(T)  =  R_σ(p)  in [0, ∞],

where the left side sums the fiber masses of the complete realizable
canonical trees of type σ (the σ-graded tree series) and R_σ = Rval(vmap σ)
is the σ-entry of the finite block solve over ℚ(q) evaluated at q = p.

**The D-9 re-owning, executed.** Per DELTA-1 D-9 (V6 findings 18/24): T-7 is
SPLIT — T-7s (the symbolic tables + obligations ledger, VERIFIED-DUAL) is an
input; the **[1v]-FULL value-correctness event is THIS unit's goal content**,
not an input (K3 was struck from O-11's input list because T-7 had routed its
open value content here). §3.4 is where [1v]-FULL is assembled. D-9k (K2-SEM,
the semantic escape input) is consumed at §3.3 with its two-scope pricing.

## 2. The skeleton consumed from M04 (statuses displayed)

All four M04 pieces are consumed at statement level; their grade is
PROVED-UNVERIFIED with a verification-only round owed (tree §4 roster: "M04
Theorems 1/2/4 core"). None is re-proved here; none is silently strengthened.
[§§2.1–2.3 byte-stable, rev 0 verbatim; §2.4 carries the G-6 language repair.]

* **2.1 (M04 Theorem 1 + Corollary 1.1).** Interface-level: seriesSum σ is the
  nondecreasing limit of the level-N slice sums (only `mem_slice_iff` used);
  GIVEN the count/cylinder law (count_tie — here supplied through D-15, §3.1),
  the row is EQUIVALENT to the limiting-density identity
  (D_σ): lim_N decided_σ(N)/p^{nN} = R_σ(p), and the finiteness clause is
  free. [Falsifier T1 probes (D_σ) directly at n = 2, §7.]
* **2.2 (M04 Theorem 2).** The σ-SUMMED tie is derivable from the sibling
  rows; the row's content is exactly the per-σ refinement. Consumed only as
  orientation (the per-σ assembly below never routes through the σ-sum, and
  drainage/env_tendsto is NOT consumed by this unit — it stays root-side,
  §D1.4).
* **2.3 (M04 Lemmas 3.1–3.3 + Theorem 4).** The abstract resummation core —
  tree series = least fixed point (Kleene/Tonelli in [0,∞]); Bekić
  triangularization to per-block affine systems x = Ax + c with the
  verdict-blind common matrix A; Neumann/Cramer under A^k𝟙 → 0 with
  det(I−A) ≠ 0; the shallow-cut regrouping — PROVED self-contained in M04
  (known mathematics, label (b); Harris / Etessami–Yannakakis / Kuich / Bekić
  / Seneta corroborations listed there). Theorem 4's glue walk (K4 → K1 +
  Lemma 3.3 → induction on block size with 3.1/3.2 + K2 → K3 identification →
  `rsh_interp`) is consumed VERBATIM; §3 re-prices its four K-faces.
* **2.4 (M04 Finding F1 + the wiring fence) [G-6 repair].** The as-built
  DEVICE chain `n2Chain` is REFUTED as a discharge site (device triple
  (q², q+1, 0)/(q²+q+1) vs true (p/(2(p+1)), p/(2(p+1)), 1/(p+1))). The
  fence — the final wiring MUST bind C to the REAL pack when F carries true
  masses — is engaged here in two parts: (i) this unit's statement binds C to
  the real per-n pack — the pack OBJECT is pinned only by **D-11 (PENDING)**,
  cited as pending; (ii) the falsifier V1 (§7) executes Finding F1's test at
  the **ENGINE PROXY** of the real pack — `om_density_engine.alpha_full(2)`,
  the Python S-area engine (block recursion + cluster laws), the same
  pipeline RESUM-n3 gates at n = 3 — identified with the intended pack by
  PROVENANCE, not by a pinned object. The proxy's n = 2 solve is the true
  triple identically in ℚ(q): the proxy passes exactly the test the device
  chain fails, so the K3/value leg is alive at the (proxy of the) discharge
  site. The fence itself DISCHARGES only at D-11.

## 3. Theorem O11 — the assembly

**Theorem O11 (rev 2).** Fix n ≥ 2 and a prime p. Bind (X, F, seam) to the
canonical classifier with true-mass fibers over the pinned models, and C to
the real per-n pack (symbolic tables = T-7s; block system pinned by D-11,
PENDING). Then (TIE_σ) holds for every σ, conditional on {(GR-B), (FRESH),
CU-2t, D-11} (tier-1 [M]) **and the declared rows D-a/D-b/D-c** (§0), with
the tier-2 displays of §0.

*Proof.* The M04 Theorem 4 walk, with each K-face supplied as follows.

### 3.1 K4 (mass semantics): LHS = Σ_{T type σ} μ(fiber T)

Three layers, each displayed:

* **(K4-μ) The measure side** [byte-stable] — Haar cylinder mass p^{−nN} at
  every level, Borel loci, continuity from below, the two-sided bracket:
  **Dfloor Theorem MEAS-BRIDGE = D-1(i)–(v), VERIFIED** (V22 Codex clean +
  fresh-Fable pass 2, adjudicated at ledger commit dbccbc1; three minor gaps
  ride the next doc touch). Both decided conventions covered; clause (iv)
  exact in the strict convention.
* **(K4-CYL) Level-exact cylinder reading** [C-1 repair] of decided verdict
  classes with the per-tree fiber scaling (the `bridgeMass T = μ(fiber T)`
  identity = M04's K4 clause; the count_tie face consumed by Corollary 1.1):
  **D-15 = O-1thr Theorem 3** ("the cylinder law": the fiber of a decided
  ns-free tree T is, at every N ≥ thr(T), an EXACT finite union of
  level-thr(T) coefficient cylinders; counts scale by p^n per level) **+
  D-14 (O-1thr Theorem 1, proved outright) + CU-1/CU-2 (per-f faithfulness)
  + TB-CAP windows** — the DELTA-2 D2.1-line-3 routing, D-15 owning ALL
  box-level lift quantification. Statuses, with D-15's FULL rev-4 interface
  residue displayed and priced clause by clause:
  * O-1thr Thm 3 (rev 4) is proved modulo (O-1thr rev-4 §0/§2.4/§8):
    (a) **(V1)/(V2) key compliance beyond the proved slice** — (V1) and
    (V2)(b) are GD-2 carriers and (V2)(c) is GD-3's min core VERBATIM (tree
    §GD rows GD-2/GD-3, carrier columns); GD-2/GD-3's one open input is
    (GR-B) (order ≤ 1 PROVED: GD23 over an arbitrary DVR), so this clause's
    order ≥ 2 residue is **(GR-B)** — a CITED containment (the tree's merge
    table), no longer rev 0's bare "so";
    (b) **(M6b)(iii)** (AUX-at-harvest) **+ (M6b)(ii−)** (the lower (N4)
    band; omitted by rev 0) — **declared row D-a**, owner GD-4, ROOT [T];
    NOT (GR-B) content, NOT closed, priced (pass-1 C-1 cure).
  * CU-1 rev 4 (V29 + Fable pass 2 both 0-critical; the 7 display gaps
    REPAIRED at 8ab8ca6; gap-check rides the next harvest); CU-2 = re-label
    over O-2a Thm 2(A)–(D) (P(O-2a r5); GD residue → **(GR-B)**); TB-CAP
    dual-accepted, perimeter-conditional on **D-12r** (tier-2, displayed).
    Terminal/verdict records inside the decided classes are forced by
    **CU-2t** (tier-1; rev-2 status: PROVED at CUC §9, pass owed).
  * NOTE ((E-N), displayed non-consumption) [byte-stable]: O-1thr Thm 3(c)'s
    σ-count clause carries the (E-N) caveat (open, scoped, owner O-1thr);
    the clauses consumed here are the cylinder-exactness/scaling clauses,
    and per the DELTA-2 remainder table (E-N) is not consumed by the
    mandatory path — if a verifier finds the D-15 consumption engaging 3(c),
    (E-N) must be ADDED to tier 1; flagged as verification charge (§8,
    item v).
* **(K4-ns) Infinite/non-separable trees excluded** [byte-stable]:
  (ns-null), T-6, VERIFIED-DUAL (proved four times per the tree's T-6 node);
  standing ns-free perimeter carried (tree §2.4; now also in §0 tier-2).

With (K4-μ) + (K4-CYL) + (K4-ns): seriesSum σ = Σ_{T type σ} μ(fiber T), and
by Corollary 1.1 the row is equivalent to (D_σ). [Falsifier T1's monotone
bracket is exactly this equivalence's testable face.]

### 3.2 K1 (per-tree product + shallow cut): the grammar weight factorization

The Lemma 3.3 hypothesis — a weight-preserving bijection between complete
type-σ trees and shallow-shape + block-derivation tuples, with
μ(fiber T) = W(Ŝ)·∏ weight(D_i) — is supplied by:

* **(K1-a) Tree architecture / first-split recursion / CUT-WD** [G-4
  repair]: T-6 (§T-ASSEMBLY), VERIFIED-DUAL. Its two open kernels are served
  at statement level by NAMED units (rev 0's "the CU cluster" de-anonymized):
  * **(SIB-STEP)** ← O-10 Theorem 2 + the declared (I-τ) row D-b — O-10
    §5.3's T-6 bullet, verbatim: the stacked one-step joint law consumes
    (SIB) at a site whose events are the engine's own next reads, depth
    ≤ thr(c) + ρ(c) under (I-τ), within the guarded form's read ceiling; no
    sub-guard N consumed.
  * **CUT-WD** (the shallow-cut decomposition / exhaustive-exclusive read
    ownership kernel) ← O-1thr Theorem 1 (= **D-14**, read locality, PROVED
    outright for every policy) + **CU-3** (= F3′ coherent nonempty locus,
    the O-3 leaf's re-label: O-14a′ Thms 1–3 (twice-stable, Lean-core
    instance) + D-14 + (O6); RE-LABEL complete, CUC §3/§8 row; rider
    P(O-1thr r4); realizer leg unconditional).
  The per-kernel mapping is read off the tree's T-6 row ("Consumes leaves
  O-1/O-3/O-10 for its open kernels ((SIB-STEP), CUT-WD)") + O-10 §5.3; it
  is displayed reasoning, not T-6's own text — verification charge (vi), §8.
  Statuses: D-14 proved outright; CU-3 re-label complete (no new open); O-10
  Thm 2 proved (pass 1 CLEAN ec5e9a1; pass 2 owed). No unnamed consumption
  remains at this face.
* **(K1-b) Sibling independence + the per-site Jacobian constant**
  [byte-stable]: **O-10, PROVED SHARPER THAN CHARGED** (wave 8): Theorem 1
  (K-COUNT — constant nonempty fibers p^{Σ min(e_i, N−τ)} at EVERY N ≥ τ,
  = p^{ρ(c)} in the D-8 guard range) + Theorem 2 (the adapted-cell (SIB)
  product law, unconditional in factor coordinates) + Corollary 1 (the
  constant-fiber surjection). THIS unit consumes the FULL D-8 guard
  N ≥ thr(c) + 2ρ(c): the fiber value p^{−ρ(c)} is the per-site Jacobian
  factor in the grammar weights (O-10 §5.3: "this is where the FULL guard is
  genuinely used") — matching D-9's "O-10 guarded" entry. [Pass 1 confirmed:
  the guard is engaged ONLY here, nowhere sub-guard.] Falsifier record:
  68/68 with the Route-1 breakage at exactly ratio p.
* **(K1-c) The engine presentation (I-τ)/K-LOC** [G-1 repair] — that
  realized site strata ARE adapted presentations (A1)–(A3) in branch-factor
  coordinates with τ(c) ≤ thr(c) + ρ(c) and reads within ceiling: **OPEN,
  owned by M07 OL-3 — declared row D-b** (ROOT [T] row "(I-τ) + K-LOC
  interface", consumers Steps 15/18). Content split, displayed:
  (a) its GMN-dictionary ingredients are GD-2/GD-3 instances — order ≤ 1
  PROVED (GD23, arbitrary DVR; pass-2 0-critical), order ≥ 2 residue
  **(GR-B)**;
  (b) the presentation-EXISTENCE and normalization content — that the strata
  admit the (A1)–(A3) form at all, with the τ(c) inequality and in-ceiling
  reads — has NO proof and NO proved reduction to (a) on file: O-10 §4.3's
  "pricing" is a reduction PLAN, exactly as pass-1 G-1 read it. Rev 0's "so
  the residue is (GR-B)" is WITHDRAWN; the residue is D-b itself, expected
  discharge route (GR-B) + the M07 OL-3 write-up.
  K-JAC's engine half is absorbed here as before (O-10 Lemma 3 makes ρ(c)
  automatically cell-constant once the presentation exists — GIVEN D-b).
* **(K1-d) Multi-site composition (JC-multi)** [byte-stable]: M07 Lemma B
  (PROVED-UNVERIFIED; verification-only roster).
* **(K1-e) Leaf verdict labels are the true (e, f) data** [byte-stable; one
  status refresh] — so verdict(D) is the honest σ-grading: O-5 + D-7a/b/c
  via **O5triple Theorems A/B/E** (rev 3, V23 0-critical; P(O5t r3) owed) —
  Theorem A (semantic leaf/factor bijection over the pinned GMN floor,
  conditional on the five §2 flags = GD-1/GD-7/GD-8 duties, tier-2), Theorem
  B (D-7b coverage by counting — NO no-missing consumed), Theorem E (D-7c +
  ASSEMBLY: leaf multiset = factor invariants uniformly over all lifts). The
  terminal-record forcing that ties the engine's emitted verdict fields to
  these semantic labels is **CU-2t** (tier-1; rev-2 status refresh: PROVED
  at CUC §9 conditional on P(O-2a r5) + D-12r + GD residue, pass owed).
  No-phantom for the realized histories underneath: CU-2 (= O-2a Thm 2(D)
  ∘ ι; GD residue → **(GR-B)**).

Given (K1-a)–(K1-e), each complete tree's mass factors as the grammar weight
of its derivation with measured one-step conditional masses at composed
pools, and the type-σ corpus decomposes along shallow shapes — Lemma 3.3
applies.

### 3.3 K2 (spectral escape, = D-9k): A(q₀)^k·𝟙 → 0 at every consumed pool
[byte-stable; D-11 status refreshed]

Two scopes, priced per D-9k:

* **At 𝔅_n (the abstract solve family):** **O-12 Theorem 1 + Corollary D,
  VERIFIED at honest scope** (leaf #2; ledger commit 95a46d2): every block
  kernel substochastic with positive termination margin at every prime power
  q₀ ≥ 2 (margins ≥ 3/4, 7/8, 6/7 at the on-record organizations), hence
  ρ(A) < 1 and det(I−A) ≠ 0; plus D-6 (DENOM-ALL, O-12 Theorems 1+4 + Lemma
  RR): EVERY assembled denominator is cyclotomic-type, pole-free at every
  prime power — so Lemma 3.2(ii)'s Neumann/Cramer step and the `evalAt`
  ring-hom passage are available at every pool q₀ = p^δ in the
  multiplicative closure. Lean partial: `E0Matrix` (BP4-P1).
* **At the intended pack:** that the pack's one-step matrices ARE
  𝔅_n-organized volume matrices (so the 𝔅_n theorems apply) is **D-11
  (PENDING; pass 1 NOT ACCEPTED as billed, rev 2 owed — §0 entry, item
  (ii))**. Numerics: N6 25/25 (extended 303/303 primes < 2000 at the D-11
  attempt); RESUM-n3 det(I−K_e) exact at pools 2, 3, 4; falsifier T5 (M04)
  remains available.

### 3.4 K3 = [1v]-FULL (value correctness — THIS unit's goal content)

The numeric grammar weights equal the pool-evaluations of the symbolic ℚ(q)
entries (K_e, b^term, J, W_Ŝ) consumed by blockSolve/Rsh, with
agreement-after-cancellation at non-all-active pools. Assembled as:

* **(K3-a) The symbolic tables themselves** [G-5 repair]: T-7s,
  VERIFIED-DUAL (V-n3 178/178 exact; tables exact at off-census q = 16/25) —
  the D-9 split's surviving input. T-5's H-DOMAINS package (VERIFIED-DUAL)
  supplies the height resummations. Fence display, split honestly: the six
  H.6 fences GATE this event at every n (tree T-5 row: "Its six H.6 fences
  gate the [1v]-FULL event"); that the BUILT per-n tables sit INSIDE the
  fences is proved nowhere at general n — V-n3/H-n3 (151/151) are the SEALED
  n = 3 EVIDENCE, not a proof. At general n, in-fence conformance of the
  constructed pack is a construction obligation of the instance pin —
  **D-11's restated charge, item (vii)** (§0; same territory as row 20's
  pack laws, O-14d/D-11). No separate tier item; the D-11 owner's acceptance
  is owed (§9).
* **(K3-b) Per-stratum censuses are the claimed q-polynomials** (the
  transition-weight counts) [status refreshed to O-9 rev 2]: **O-9
  CEN-W/CEN-J** — level 1 PROVED at every n, every prime power, every
  complete DVR (M08 Theorem 2, 46-strata machine check); order ≥ 2 REDUCED
  to **(GR-B)** (window form) **+ (FRESH)** (joint-with-parent form; at O-9
  rev 2, (FRESH) carries clauses (a′) quantitative floors and (c) the
  rescoped JUNCTION-PIN). Rev-2 status refresh: O-9 pass 1 was NOT accepted
  (c17777f: E′ left-tail ledger machine-countermodeled); REPAIRED at O-9
  rev 2 (01c21e6: strict ⌊line⌋+1 threshold, K5 predict-from-ledger
  falsifier 11/11 exact incl. the countermodel; re-pass owed). CEN-W/CEN-J
  now carry the displayed hypothesis **(ADM)** (ledger-admissibility) —
  proved AUTOMATIC at orders ≤ 1, open at general order (a p-free per-datum
  decidable); consumed here and displayed at tier-2 (§0) riding O-9's
  re-pass. O-9's census falsifier: 1,128,288 order-2 instances, 0
  violations, ε = 0 throughout. Index correction carried: the census field
  degree is d = f₀⋯f_r (O-9's DELTA-3 flag), not the tree's q^{f̄_r}
  display.
* **(K3-c) Unramified pools (q₀ = p^δ, δ > 1)** [G-2 repair]: the re-based
  classifier obeys the same digit/alphabet laws with a_δ = δ·a₁ — **O-8b
  (M14), PROVED-UNVERIFIED conditional on (R1)–(R4)** (the O_δ re-base of
  the T-1 stage laws; N8 exhaustive, 199,723 alphabets, 0 violations).
  (R1)–(R4) is **declared row D-c** (ROOT [M] row, owner O-8b/D-3). The
  discharge ROUTE is displayed as a route, with its missing step NAMED: the
  adopted arbitrary-DVR discipline (DELTA-2 §D2.6 step 3) states the GD
  families over an arbitrary absolutely-unramified complete DVR from the
  start, and GD23's order ≤ 1 layer is proved at that generality
  (O_δ = W(F_{p^δ}) qualifies); what is UNWRITTEN is the display that
  O-8b's O_δ re-based stage laws, restricted to order ≤ 1, are INSTANCES of
  those GD families rather than needing a further re-base lemma on top —
  rev 0's own suggested charge (iii), still standing (§8). GIVEN that
  instance display, order ≤ 1 closes and the order ≥ 2 residue is (GR-B);
  UNTIL then, (R1)–(R4) is consumed as its own named open (D-c). Rev 0's
  "no fifth open item is introduced" is WITHDRAWN at this face. The
  remaining scope-hygiene audit (each consumed table law stated at DVR
  generality at the pack) is **D-11's restated charge, item (vi)** (§0).
* **(K3-d) Measured weights = table evaluations AT the intended pack** (the
  semantic mass identification) [byte-stable; D-11 pointer]: **D-11
  (PENDING; §0 entry items (i)/(iii))** — kernel entries are the Haar
  volumes of the T-3/T-6 continuation events (T-3 §D4-R mass laws,
  VERIFIED-DUAL, supply the events; O5triple Thm E + D-15 supply the
  per-box lift uniformity per the WAVE-8 pricing), rows pairwise disjoint
  with positive termination margin, block system = 𝔅_n or
  O-12-Thm-3-covered.

### 3.5 The glue [byte-stable, rev 0 verbatim; pass 1: "structurally sound"]

Exactly M04 Theorem 4's proof: K4 (§3.1) rewrites the LHS as
Σ_{T type σ} μ(fiber T); K1 (§3.2) + Lemma 3.3 regroup it as the shallow
convolution of the numeric block series B; Lemma 3.1 + Bekić + Lemma 3.2
with K2 (§3.3), by induction on block size (smaller blocks' pools are pool
powers, covered by §3.3's every-prime-power scope), give
B_{e,·}(σ′)(q₀) = (I − A(q₀))^{−1} c(σ′); K3 (§3.4) identifies this
inductively with the evaluation at q₀ of blockSolve (ring-hom `evalAt` on
the regular subring, memberships from D-6/`wsh_ok`/`rsh_interp`); assembling
over shapes gives the evaluation at p of Rsh(vmap σ); `rsh_interp` (a
carried law of the real pack — RatBurdens' interpolation face, checked at
RESUM-n3) converts it to Rval(vmap σ)(p) ∈ [0,1] (`r_bdd`), and
ENNReal.ofReal is faithful there. ∎ [The rsh_interp/r_bdd/wsh_ok laws are
row 20 = D-11 §0 entry item (v) — rev-2 note, outside the walk text.]

## 4. Displayed non-consumptions (quantifier hygiene) [byte-stable; pass 1
confirmed the first bullet accurate against the §3 text]

* **Drainage (env_tendsto / O-4) is NOT consumed.** The per-σ assembly goes
  through Theorem 4's K1–K4 route; drainage enters only M04 Theorem 2's
  σ-sum remark and the root's squeeze (T-U), both outside this unit. (O-4's
  own residue OL1-ii ← CU-2 + CU-4 + GMN Cor 4.19 stays where DELTA 2 put
  it; O4T's n ≤ 3 unconditional transfer is unconsumed here.)
* **No-missing (CU-1) is consumed ONLY through D-15's routing** (the K4-CYL
  face) — never as a box-keyed universal statement (the V21-1/2 discipline:
  box-level lift quantification lives in D-15 alone).
* **OB1/D-16/LIT-5, K-OB-C: not consumed** (route R-QNT, per DELTA 2). CU-6/
  OB2: not consumed (the WAVE-8 audit: D-11 needs per-box lift uniformity,
  not ∼OM invariance; CU-6 evicted).
* **The row is NOT claimed as the density tie.** (TIE_σ) equates the tree
  series with the solve value; the further identity with α(n, σ; p) is the
  root's business (T-U squeeze over D-1's bracket + O-4), not this unit's.

## 5. The consumption table (every consumption displayed)

Rows 1/2/3/8/10/12/13/14/16/19 byte-stable (pass-1 sweep: clean); rows
4/7/9/11/15/17/18/20 repaired/refreshed as marked.

| # | consumed piece | supplies (site) | status + evidence | residue contributed |
|---|---|---|---|---|
| 1 | M04 Thm 1 + Cor 1.1 | slice-limit form; row ⇔ (D_σ) given the cylinder law (§2.1, §3.1) | PROVED-UNVERIFIED (M04; verification-only roster) | — |
| 2 | M04 Lemmas 3.1–3.3 + Thm 4 glue | lfp/Bekić/Neumann core + the walk (§2.3, §3.5) | PROVED self-contained (M04); same roster | — |
| 3 | Dfloor Thm MEAS-BRIDGE (D-1) | Haar/cylinder measure side (K4-μ) | **VERIFIED** (V22 + Fable pass 2; ledger dbccbc1) | — |
| 4 | O-1thr Thm 3 (= D-15) + Thm 1 (= D-14) [C-1 repair] | exact cylinder law + per-tree scaling (K4-CYL) | proved mod the FULL rev-4 residue: (M6b)(iii) + (M6b)(ii−) + (V1)/(V2) beyond the proved slice; rev 4 landed; P(O-1thr r4) owed | **(GR-B)** (order ≥ 2, the (V1)/(V2) clause) + **D-a** (the (M6b) clauses → GD-4) |
| 5 | CU-1 (no-missing, per-f) | decided-class faithfulness under D-15 (K4-CYL) | rev 4 (8ab8ca6): pass-2 display gaps repaired; gap-check rides next harvest | (GD residue → **(GR-B)**) |
| 6 | CU-2 (no-phantom = O-2a Thm 2(D)∘ι) | realized histories are GMN data (K4-CYL, K1-e) | re-label complete; P(O-2a r5) | **(GR-B)** |
| 7 | **CU-2t** (terminal seam) [status refresh] | verdict-carrying terminal records forced (K4-CYL, K1-e) | **PROVED at CUC §9** (unit D3CU2t; landed 254d4de, cross-ref aa00747), conditional on P(O-2a r5) + D-12r + GD residue; hostile pass owed (ROOT [V]) | **CU-2t** (retained tier-1 pending its pass) |
| 8 | TB-CAP (T-6) windows | (τ-hen)/(τ-irr) verdict windows (K4-CYL, K1-e via CU-2t) | VERIFIED-DUAL, perimeter-conditional | (tier-2: D-12r perimeter) |
| 9 | T-6 (ns-null) + first-split/CUT-WD architecture [G-4 repair] | infinite-tree exclusion (K4-ns); shallow cut (K1-a) | VERIFIED-DUAL; kernels served NAMED (§3.2): (SIB-STEP) ← O-10 Thm 2 + D-b; CUT-WD ← D-14 + CU-3 | — (D-b charged at row 11) |
| 10 | O-10 Thms 1/2 + Cor 1 (K-COUNT + SIB, **D-8-guarded**) | per-site product law + Jacobian constant p^{−ρ(c)} (K1-b) | **PROVED IN FULL** in factor coordinates (wave 8); pass 1 CLEAN (ec5e9a1); pass 2 owed; harness 68/68 | — |
| 11 | (I-τ)/K-LOC (= M07 OL-3) [G-1 repair] | engine cell → factor-coordinate presentation (K1-c); (SIB-STEP)'s read-depth warrant (K1-a) | OPEN — **declared row D-b** (ROOT [T] "(I-τ) + K-LOC interface"); dictionary ingredients: order ≤ 1 PROVED (GD23), order ≥ 2 (GR-B); presentation existence: NO proof on file | **D-b** + **(GR-B)** (ingredient leg) |
| 12 | (JC-multi) = M07 Lemma B | multi-site composition (K1-d) | PROVED-UNVERIFIED (verification-only roster) | — |
| 13 | O5triple Thms A/B/E (O-5 + D-7a/b/c) | semantic leaf labels + leaf/factor bijection (K1-e); per-box lift uniformity input to D-11 | rev 3, V23 0-critical; P(O5t r3) owed; five flags = GD-1/GD-7/GD-8 duties (tier-2) | — |
| 14 | O-12 Thm 1 + Cor D + Thm 4/Lemma RR (D-6, D-9k at 𝔅_n) | substochastic escape + pole-free denominators at every prime power (K2, §3.5 evalAt) | **VERIFIED at honest scope** (leaf #2); `O12PoleFree.lean`/`E0Matrix` partials | — |
| 15 | **D-11** (instance pin / MASS-ID) [status refresh + G-3] | §0 restated entry items (i)–(vii): K2 pack side, K3-d, K3-a(vii), K3-c(vi), fence §2.4, pack laws §3.5 | **PENDING** — attempt 0cae7d8; pass 1 NOT ACCEPTED as billed (2587c70); rev 2 owed; items (v)–(vii) = widened charge, acceptance owed | **D-11** |
| 16 | T-7s (symbolic tables + ledger) + T-5 H-DOMAINS/H.6 | the symbolic ℚ(q) entries + height resummations (K3-a) | VERIFIED-DUAL (V-n3 178/178; H-n3 151/151; off-census q = 16/25); general-n in-fence conformance → D-11 item (vii) | — (via row 15) |
| 17 | O-9 CEN-W/CEN-J over M08 Thm 2 [status refresh] | per-stratum census q-polynomials (K3-b); index fix d = f₀⋯f_r carried | level 1 PROVED; order ≥ 2 REDUCED (wave 8); O-9 rev 2 repairs landed (01c21e6: E′ strict, (ADM) displayed, JUNCTION-PIN → (FRESH)(c)); re-pass owed | **(GR-B) + (FRESH)** (+ (ADM), tier-2 at owner) |
| 18 | O-8b (M14, EQ-2) [G-2 repair] | unramified-pool weight laws (K3-c) | PROVED-UNVERIFIED conditional on (R1)–(R4); N8 exhaustive; DVR route displayed with UNWRITTEN order ≤ 1 instance display | **D-c** ((R1)–(R4), ROOT [M]); route → (GR-B) at ≥ 2 GIVEN the instance display; scope hygiene → D-11 item (vi) |
| 19 | K-FACT (GMN Thms 1.15/1.19/3.1/3.7) | roster members are coprime factors (inside O-10's Thm-2 application) | LITERATURE (LIT-1, conditional grade §D1.5(b), tier-2) | — |
| 20 | rsh_interp / r_bdd / wsh_ok (pack laws) [G-3 repair] | Rsh → Rval conversion + [0,1] bounds (§3.5) | carried laws of the real pack (RatBurdens face); checked at RESUM-n3; = D-11 §0 entry item (v) (pack construction O-14d/D-11) | **D-11** (item (v) of its restated charge) |

**Residue union over the table [footer restated, C-1 cure]:** tier-1 [M] =
{(GR-B), (FRESH), CU-2t, D-11} + declared rows {D-a, D-b, D-c} — exactly the
§0 display. (GR-B) is charged at rows 4/5/6/11/17 as the SAME one package
(the D2.2 merge working as designed) and at row 18 only THROUGH D-c's
unwritten instance display; the four §0 faces cover the six rows [pass-1
minor 1 reconciled].

## 6. Pending-pass registry (verification duties, tier-2; CUC §0 pattern)
[updated to the wave-12 state]

P(O-2a r5) · P(O-1thr r4) · P(O5t r3) · GD23 pass 2 = 0-critical, 9 gaps —
REPAIRED at rev 4 (f9878d1); gap-check pass owed · CU-1 pass 2 = 0-critical,
7 gaps — REPAIRED at rev 4 (8ab8ca6); gap-check rides next harvest · O-10
pass 1 CLEAN (ec5e9a1); pass 2 (model-diverse) owed · O-9 pass 1 NOT accepted
(c17777f) → rev 2 repairs landed (01c21e6); re-pass owed · D-11 pass 1 NOT
ACCEPTED as billed (2587c70); rev 2 owed · THIS unit: pass 1 NOT ACCEPTED AS
STATED (a7ede72) → THIS rev 2 is the repair; pass 2 owed on THIS brief · CUC
+ CU-2t hostile pass owed · the M04 core's verification-only round (tree §4
roster). Per §D1.7 none of these is consumed by an acceptance claim: this
unit's grade is PROVED-UNVERIFIED (conditional), not VERIFIED.

## 7. Falsifier record [rewritten at rev 2 — the G-6/G-7/G-8 + minor 3/4 repairs]

Harness: `verification/openmath/o11_seriestie_check.py` (sealed docstring;
byte-stable since its single commit 3fc8f52; V1/V2 need sympy — run under
`uv run --with sympy`; T1 pure stdlib). **Run of record: run 4**
(`verification/openmath/logs/o11_seriestie_run4.log`, 2026-07-31, this
machine, the unmodified harness re-executed at rev 2): **37/37 PASS + "ALL
PASS" summary, exit 0** — V1 8, V2 6, T1 23. Run 3
(`o11_seriestie_run3.log`, 2026-07-31) is byte-identical in results. COUNT
CORRECTION [G-7]: rev 0 claimed "38/38" (and "T1 24/24") by counting the
terminal "ALL PASS" summary line as a check; the true counts are 37/37 and
23/23 (T1 = (10+1) + (6+1) + (4+1) per-p rows + decay checks). Commit
3fc8f52's message repeats the 38/38 figure and stands inaccurate in history;
corrected here and flagged in §9. No check ever failed; the defect was the
record, not the runs. DATE CORRECTION [minor 4]: rev 0 dated itself and the
run of record 2026-08-02; the actual run dates are 2026-07-31 (file mtimes
17:22–17:24 for runs 1–3; run 4 today).

* **V1 (Finding F1's test at the ENGINE PROXY of the real pack — the
  dispatch's named falsifier) [G-6 scope repair].** What executes is
  `om_density_engine.alpha_full(2)` — the Python symbolic S-area engine
  (block recursion + cluster laws), the same pipeline RESUM-n3 gates at
  n = 3 — NOT a pinned pack object: the pack's pinning IS D-11 (PENDING),
  and the Lean row stays PARKED (§8). The proxy is identified with the
  intended pack by PROVENANCE only. The test: the proxy's n = 2 solve equals
  the true triple (q/(2(q+1)), q/(2(q+1)), 1/(q+1)) IDENTICALLY in ℚ(q)
  (sympy difference ≡ 0, all three types + Σ = 1) and matches the
  oracle-validated `padic_types.monic_quad_pred` exactly at p ∈ {2, 3, 5, 7}.
  The proxy passes exactly where M04 proved the device chain fails — the
  K3/value leg is alive at the proxy of the discharge site. 8/8.
* **V2 (the n = 3 anchor, symbolic) [G-8 repair + supplement].**
  `alpha_full(3)` equals `true3` — an IN-HARNESS line-for-line transcription
  of `monic_cubic_pred` — identically in ℚ(q) (all five types; Σ_σ = 1 in
  ℚ(q)), and equals the ACTUAL oracle numerically at p ∈ {2, 3, 5, 7}. As
  pass 1 noted, four points do not pin a rational function, so the run-3/4
  test is strictly weaker than the sealed docstring UNLESS the transcription
  is trusted. THAT GAP IS NOW CLOSED by the rev-2 supplement
  `verification/openmath/o11_v2_oracle_pin.py` (log
  `logs/o11_v2_oracle_pin_run1.log`, 2026-07-31, **10/10 PASS, exit 0**):
  exact agreement engine-vs-oracle at 44 integer points q = 2..45 per type,
  with the displayed degree-budget argument (engine numerator/denominator
  degrees computed per type, ≤ 5; oracle degrees ≤ 7/6 by inspection of its
  five literal entries; the cross-difference polynomial has degree ≤ B ≤ 12
  < 44, and all sample points avoid the poles) — so alpha_full(3) ==
  monic_cubic_pred AS RATIONAL FUNCTIONS, the sealed prediction itself, and
  the transcription `true3` is verified a posteriori. This re-derives the
  RESUM-n3 65/65 anchor symbolically end-to-end. 6/6 (+ 10/10 supplement).
* **T1 (M04 test T1 — the (D_σ) limit form, which Cor 1.1 makes equivalent
  to the row given K4-CYL) [G-7 display repair].** Exhaustive enumeration of
  (b, c) ∈ (ℤ/p^N)², p ∈ {2, 3, 5}, N ≤ (10, 6, 4). The classifier here is
  `decide()` — a HAND-WRITTEN discriminant rule (p odd: decided iff
  v_p(d) < N, type by parity of v and QR of the unit part; p = 2: decided
  iff v_2(d) ≤ N − 3, type by unit mod 8), NOT the project's OM classifier;
  pass 1 verified the rule sound by direct inspection (incl. ℚ₂(√5)
  unramified-quadratic reading). Checks: per-σ decided mass NONDECREASING
  and the two-sided bracket dmass_σ(N) ≤ R_σ(p) ≤ dmass_σ(N) + env(N) at
  every level, env decaying geometrically (7.81e-3 / 1.37e-3 / 1.60e-3 at
  the caps), where R_σ(p) is the true σ-density triple [symbol fixed —
  rev 0 wrote ρ_σ, colliding with the Jacobian exponent ρ(c)]. 23/23. What a
  violation would refute: the VALUE identity (D_σ) for R_σ(p) at the
  discriminant rule — which transfers to "(D_σ) at the real classifier"
  only through this unit's conditional soundness/exhaustion inputs (the
  K4-CYL and K1-e legs); T1 is EVIDENCE for the row's testable face, not an
  enumeration of the real classifier's decided classes.
* **Run history honesty [G-8 repair, replacing rev 0's "dead-code" note].**
  Runs 1 and 2 CRASHED LIVE, both inside V2's oracle call and both BEFORE
  any V2 or T1 check line executed: run 1 `ZeroDivisionError` at
  `pt.monic_cubic_pred(1)` (d = q⁵−1 = 0 at q = 1), run 2 `TypeError` at
  `pt.monic_cubic_pred(q)` (Fraction rejects a sympy Symbol) — tracebacks
  preserved in `o11_seriestie_run{1,2}.log`. Rev 0's description "two
  dead-code defects" is FALSE and withdrawn. Consequences displayed: V1's 8
  checks passed identically on all four runs; the sealed V2/T1 predictions
  were first EXERCISED at run 3, after two rounds of harness edits to the
  failing call site; runs 1–2 predate version control (single harness
  commit), so rev 0's "no sealed prediction changed" is the author's claim,
  unverifiable from the repo — what IS verifiable is that the run-3/run-4
  harness is byte-identical to the committed one, its docstring predictions
  are those reported here, and the supplement now closes the V2
  executed-vs-sealed gap.

Not run (displayed): M04 T5 (the E0 spectral probe at extracted engine
blocks — subsumable into the D-11 unit's own falsifier, where the pack
matrices live); M04 T2a (n = 3 Monte-Carlo via cypari2 — not installed;
RESUM-n3's sealed PARI gate already covers the values).

## 8. Honesty ledger + suggested verification charges

PROVED here: nothing new — this unit is an assembly; its contribution is
Theorem O11's re-priced walk (§3) + the consumption table (§5) + the F1
fence engagement at the engine proxy (§2.4/§7-V1) + (rev 2) the honest
residue pricing and the V2 oracle pin. CONSUMED: rows 1–20 of §5, statuses
as displayed. OPEN (tier-1 [M]): (GR-B) · (FRESH) · CU-2t (proof on file,
pass owed) · D-11 (pending unit, rev 2 owed). DECLARED (beyond the four):
D-a ((M6b)(iii)+(ii−) → GD-4) · D-b ((I-τ)/K-LOC → M07 OL-3) · D-c
((R1)–(R4) → O-8b/D-3). TIER-2: §0 + §6 lists. NOT CLAIMED: the density tie
(root's business); any unconditional per-σ value at general n; any Lean
discharge (the `series_tie` row and item-27-adjacent wiring stay untouched,
PARKED); any closure of D-a/D-b/D-c.

Charges to pass 2, beyond the standard (rev-0 charges adjudicated: (i) the
tier-union hunt SUCCEEDED at pass 1 = C-1, repaired above; (ii) D-8 guard
CLEAN in-text; (iv) walk re-derivation CLEAN; (iii) and (v) STAND):

* (iii) [standing] confirm §3.4(K3-c)'s route: is the order ≤ 1 instance
  display genuinely the only missing step between (R1)–(R4) and GD23's DVR
  layer (no hidden O_δ order-1 gap)?
* (v) [standing] adjudicate the (E-N)/Thm-3(c) seam of §3.1 against
  O-1thr's Theorem 3 text.
* (vi) [new] check the K1-a per-kernel serving map ((SIB-STEP) ← O-10 Thm 2
  + D-b; CUT-WD ← D-14 + CU-3) against T-6's own §T-ASSEMBLY text — the map
  is displayed reasoning from the tree's T-6 row + O-10 §5.3, not T-6's own
  display.
* (vii) [new] check the three DECLARED rows' pricings against their owners'
  texts: D-a vs tree §GD row GD-4 + O-1thr rev-4 §2.4/§8; D-b vs the ROOT
  [T] "(I-τ) + K-LOC" row + M07; D-c vs the ROOT [M] "(R1)–(R4)" row + M14.
* (viii) [new] audit the restated D-11 entry (§0 items (i)–(vii)) against
  the D-11 rev 2 when it lands: every widened item accepted there, or
  re-declared here as a separate open.
* (ix) [new] recount the falsifier logs (runs 3–4: 37 PASS lines each; the
  supplement: 10) and re-check the supplement's degree-budget argument.

## 9. Files + owed tree/ROOT ledger lines

* This unit: `lean/notes/openmath/O11_phaseB_verifybrief_rev2.md` (rev 0 =
  `O11_phaseB_attempt.md`, superseded as the unit of record; pass 1 =
  `O11_pass1_verify.md`).
* Falsifier: `verification/openmath/o11_seriestie_check.py` (byte-stable) +
  logs `o11_seriestie_run{1,2,3,4}.log` (run 4 = run of record, 37/37) +
  NEW supplement `o11_v2_oracle_pin.py` + log `o11_v2_oracle_pin_run1.log`
  (10/10).
* Owed DELTA-3 lines (orchestrator): O-11 status → PROVED-UNVERIFIED
  conditional on {(GR-B), (FRESH), CU-2t, D-11} + declared rows D-a/D-b/D-c;
  [1v]-FULL assembled at §3.4; the M04 F1 fence recorded ENGAGED AT THE
  ENGINE PROXY (V1) — NOT "executed at the real pack" (G-6); CU-2t owner
  resolved (CUC §9 / unit D3CU2t).
* Owed ROOT (`docs/ROOT_ASSEMBLY_2026-08-02.md`) ledger touches, per the
  dispatch's instruction to note beyond-set consumption:
  1. §2 Step 18's conditionality sentence: widen to name D-a/D-b/D-c (the
     [T]/[T]/[M] rows already exist in §3; only Step 18's pointer is
     missing) and correct its V1 language to the engine-proxy form.
  2. §3.3 row "GD-4/GD-6 engine-bookkeeping units": add "enters Step 18
     through D-15's (M6b)(iii)/(ii−) clauses (O-11 K4-CYL)".
  3. §3.1 row "(R1)–(R4)": add "hence Step 18 (O-11 K3-c)".
  4. §3.3 row "(I-τ) + K-LOC": no change needed (Step 18 already listed).
  5. D-11 rows (§2 Step 17, §3.2, §3.3 (S1)/(S2)): note the widened charge
     items (v)–(vii) pending acceptance at D-11 rev 2.
  6. Record correction: the O-11 falsifier count is 37/37 (3fc8f52's
     message says 38/38 and stands inaccurate in history).
