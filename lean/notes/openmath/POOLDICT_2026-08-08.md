# POOL-DICT ATTEMPT (2026-08-08) — (H8′)(iii) DECIDED: derive-from-wiring REFUTED (compiled)

Unit: synthesis pass 5 F1 target (iii) = pass-4 F2a (twice dropped, now executed).
Charge: countermodel-first — pin the (H8′)(iii) POOL-DICT interface statement, DECIDE
it at the cheapest compiled instance BEFORE supplier work, then attempt or refute.
Spec sources: SYNTHESIS_PASS5 §F1/(iii) + §F6; SYNTHESIS_PASS4 §F2(a); ROOT_ASSEMBLY
§3.1 (H8′)(iii) row (r18 wording, §3.2 retirement record); HDISCHARGE_H8.md §4 Wave 7
(H8-C3) — note: the blueprint lives at `lean/blueprints/HDISCHARGE_H8.md`, not
`lean/notes/` as the dispatch said.

## 1. The pinned interface statement

**(H8′)(iii) POOL-DICT** (from the row's own displays + H8-C3's typed form): the
`KCutK1Tie` pool rows beyond the pinned e-leg —

* `τOf : ∀ Ŝ i, C.T.State (eOf Ŝ i)` and `δOf : ∀ Ŝ i, ℕ+` are bare DATA; the
  interface owes a NODE-LEVEL READ-OFF pinning them against the interface node
  `(R.dcar Ŝ i D).iface` (the W7 boundary-data clause), the way `e_key` pins `eOf`
  via `BlockDeriv.entE = iface.e`;
* `vBridge` is constrained only by keying (`vBridge_vOf`), injectivity
  (`vBridge_inj`), support (`blockTotal_supp`);
* plus `blockTotal_tie`'s instantiation at the intended member (needs the [2r]/CL-8
  REL.2 pool dictionary + (K-CUT-g) continuation memorylessness — open).

Pass-4 F2a's derivation hope (the REC-WIRE-G genre): derive the τ/δ read-off from
the compiled K-CUT laws (`kcut_reduce` ownership + `massFactor` support), so that
POOL-DICT would be wiring, not new content.

## 2. Instance decision (countermodel-first): REFUTED at the compiled n2 instance

Cheapest compiled instance ranking: the corpus has exactly TWO compiled
carrier-pack families of `UCarriers 2` grade — the REAL n2 ledger instance
(`MovesS/N2*`: `n2T/n2M/n2RB/n2hdc/n2hK/n2F/n2Chain`, `n2hdet` PROVED) and the
B15a adversarial pack (`MovesU/BridgeB15a_r2Neg`, coexistence-fenced). No compiled
`KCutReKeys`/`KCutK1Tie` instance existed anywhere (grep-verified) — the C3
structure was instance-free until this unit.

**Decision artifact** — `lean/LeanUrat/Scaffold/HDischarge/H8/PoolDictProbe.lean`
(new file; Lean-core, zero sorry):

* `pdC : UCarriers 2` — the real n2 pack, packed verbatim; `pdHdet := n2hdet`.
* `pd_blockSolve1_zero` — NEW content lemma: the n2 e = 1 block's σ′-keyed solve
  VANISHES at every σ′ ≠ {(1,1)} (the block-1 layer is all-termFin with the single
  degree-1 verdict {(1,1)}, no split outcomes). Since every member of the chain's
  `Sigmas` has degree 2 (`sig_exact`, no private enumeration consumed:
  `pd_sigmas_ne`), the solve is 0 at every tie-bound σ′.
* `pdR : KCutReKeys pdFb pdE` — ONE shared extraction pack: one shape, one slot,
  a REAL `BlockDeriv` (`pdD0`, headed by the G1 toy node, `entE = 1` — `e_key`
  NON-vacuous), weights 0; tree legs empty (honesty: the decision content is the
  pool rows vs the S-side solve, which is non-vacuous — three Sigmas members).
* `pdTIE (δ : ℕ+) : KCutK1Tie pdC pdR pdHdet` — a FULL C3 instance at EVERY pool
  transport δ: `blockTotal_tie` holds non-vacuously at each Sigmas member because
  the solve is 0 there and `powSubst δ 0 = 0` for every δ — the tie CANNOT see δ.
* **`pooldict_delta_underdetermined`** (the refutation pin): ∃ T1 T2 over the SAME
  (pdC, pdR, pdHdet) with T1.eOf = T2.eOf, T1.τOf = T2.τOf, T1.vBridge = T2.vBridge,
  T1.δOf ≠ T2.δOf (δ ≡ 1 vs δ ≡ 2).

**Why this decides the derivation question**: the two instances share EVERY ambient
object. Any law stated over the carrier pack — `kcut_reduce`'s binder rows,
`KCutMassFactor`'s factorwise support, any future wiring lemma about (C, R, hdet) —
holds for T1 iff it holds for T2, so no compiled K-CUT law can pin `δOf`. The pool
dictionary is genuinely NEW interface content, not derivable wiring. Pass-4 F2a's
route is CLOSED with a compiled obstruction (the outcome class its charter named).

## 3. Honest scope + the restatement the countermodel pins

* **δ-leg: compiled.** The refutation is at the δ row.
* **τ-leg: same-genre, NOT compiled.** Every compiled table on file (`n2T`, the
  B15a `cmT`) carries `State ≡ Unit`, so `τOf` is unique THERE. At any table with
  two states whose measured rows agree, the identical construction floats `τOf`;
  building such a table = a fresh TableShape+chain (priced as a forge-class unit,
  the pass-5 F5 genre). The (H8′)(iii) τ-clause stands on the same footing as the
  δ-clause by this argument, but only the δ-clause has a compiled witness.
* **Value-pinning is instance-luck, not a dictionary.** At instances whose solves
  are nonzero and δ-value-injective (e.g. the B15a pack's t̂-entries), the tie DOES
  pin δ numerically — but that is arithmetic of the particular solve values, not a
  node-level read-off; at the n2 instance it fails. A dictionary must be pinned
  from the interface node, exactly as the row displays.
* **Restatement pinned**: (H8′)(iii) as displayed at ROOT §3.1 is CONFIRMED as the
  honest form — node-level `τ_key`/`δ_key` conformance rows against
  `BlockDeriv.iface` fields + `blockTotal_tie` instantiation, blocked on [2r]/CL-8
  REL.2 + (K-CUT-g). No weakening, no discharge claimed. The only change this unit
  makes to the board: the "attempt deriving … before treating it as blocked"
  disposition (pass-4 F2a) is EXECUTED and closed — (H8′)(iii) is now
  legitimately blocked-on-owners, with the blocker compiled.

## 4. Artifacts + verification

* `lean/LeanUrat/Scaffold/HDischarge/H8/PoolDictProbe.lean` — NEW, compiles green
  standalone (`lake env lean`, 2026-08-08):
  `#print axioms pd_blockSolve1_zero` → {propext, Classical.choice, Quot.sound};
  `#print axioms pooldict_delta_underdetermined` → same. Zero sorry.
* Not imported by any existing module (probe-class, like the H8 gates); no
  coexistence issue (BridgeB15a_r2Neg NOT imported; BridgeRosterPins NOT imported).
* No statement of any existing declaration touched; no seam field discharged.

— POOL-DICT attempt unit, 2026-08-08.
