# ITERLAW3-PC — Codex content pass 1 on ITERLAW3_PROOF_2026-08-08.md: NOT-CLEAN, 2C/1G

Charge: /tmp/openmath/ITERLAW3_PC_charge.txt (115,862 bytes; appendices = the proof note + the accepted order-2 note + L3CONVERGENCE). Orchestrator adjudication: all three findings credited pending PE decorrelation — (F1 GAP) DEV-1 induction decrease fails at e0=1 (scope to e0>=2 + explicit base-range leg); (F2 CRITICAL) COR-3 e2=1/e1=1 degenerations overreach: DCX admits e2=1 and is automatic in s2 at e1=1 — corollaries hold OFF-DCX (unconditional only where DCX empty, e0<=2); (F3 CRITICAL, nominal) rung indexing: 3-read theorem = (ITER-LAW-n) rung n=2 per the L3CONVERGENCE convention; the running 4-read battery = rung n=3 — relabel throughout, no math content changes. Errata unit dispatches after PE lands (round-1 cadence); counter stays 0/2.

## Codex output (verbatim)

1. [GAP] “All children have Φ₀-exponent ≤ … = j − e₀ + 1 ≤ j − 1 < j (e₀ ≥ 1) and … the induction applies to each.” The asserted decrease is false when \(e₀=1\): then \(j-e₀+1=j\), so the strong induction proving DEV-1 does not close for the stated class. The later uses at \(e₀=1\) appear confined to the base range, but DEV-1 itself is not proved with its advertised quantifiers.

2. [CRITICAL] “COR-3: degenerations … all unconditional — DCX needs δ₁ = 1 ∧ g₀ = 1 ∧ e₀ ≥ 3, none of which survives the respective degeneration” and “e₂ = 1 … the ACCEPTED order-2 law verbatim.” Neither \(e₂=1\) nor \(e₁=1\) eliminates DCX: its defining conditions allow \(e₂=1\), and when \(e₁=1\), the requirement \(s₂=s₂'=e₁-1\) is automatic. In precisely those cases S4.7 still leaves the required re-overflow/read calculation open. Algebraic simplification of the displayed exponents does not prove that the level-3 value equals the accepted level-2 value. Thus the unconditional \(e₂=1\) and \(e₁=1\) corollaries—and the claim that all COR-3 degenerations avoid DCX—overreach the proved perimeter.

3. [CRITICAL] “COR-6: the exponent recursion — (ITER-LAW-n) at n = 3” and “the proved display is EXACTLY the uniform recursion … at n = 3,” followed by “Confirmed rungs … n = 1 accepted [IL], n = 2 this note.” Appendix C defines \(n\) for an \((n+1)\)-read tower. Therefore this three-read theorem is the \(n=2\) rung; \(n=3\) is the untouched four-read case and would include a further \(D_3,S_3\) layer. The displayed dictionary proves the \(n=2\) specialization, not the claimed \(n=3\) recursion.

ITERLAW3-PC FINDINGS: 2 critical, 1 gaps
VERDICT: NOT-CLEAN
