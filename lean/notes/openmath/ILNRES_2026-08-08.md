# [ILN] NON-(RM) RESIDUALS — the definitive post-COR-A conditionality of (ITER-LAW-n) on T-class(n), with the (DS-SUB) engine-free-support discharge attempt

**Unit:** [ILN] NON-(RM) RESIDUALS (2026-08-08 campaign, wallclock 2026-08-04).
Genre: residual-ledger + discharge-attempt note. Grade: ATTEMPT — acceptance
counter 0, no hostile pass has run on this text; the S3 lemma is a NEW claim
and does not edit any accepted text (incorporation convention: consumers cite
this note only after its own arc).
**Machine leg:** `verification/openmath/ilnres_checks.py` (two-commit seal:
this prediction sheet + the runner committed BEFORE the battery; verdict
appended in commit 2 from the artifacts).

**Sources of record (read in full for S1):** ITERLAWN_PROOF_2026-08-08.md
(head declaration + ACCEPTED bracket, S0.3 status, S4.3 package, S4.4–S4.7
incl. the PE-G1 erratum, S7 + the r38 bracket + the dependency-sweep table);
BRIDGE_ADJUDICATIONS: the [ILN] a46578e block, the [RMG] block + COR-A;
PROJECT_STATE r36/r39 brackets.

## S1. THE DEFINITIVE RESIDUAL ENUMERATION (what still conditions (ITER-LAW-n) on T-class(n) after COR-A)

Frame: [RMG] COR-A discharged the (RM-m) calls of [ILN]† S7 rows 5/6/8 on
T-class(n) := d₀ = 1 ∨ (e₀ ≥ 2 ∧ e_i ≥ 2 for all 1 ≤ i ≤ n−2); the ledger
states the remaining conditionality there is "exactly [ILN]†'s own non-(RM)
residuals". This section is the definitive list of those. Four members.

**[NR-1] (ITER-LAW-LIFT), open exactly at g₀ = 1 ∧ δ₁ = 1.** Statement (the
[IL] package residual, propagated by [ILN] S0.1 verbatim to every level):
the theorem is proved for the STANDARD/canonical ε-corrected lifts only;
"nothing is claimed for any non-canonical lawful lift at any level", and the
lift-independence of the 2-read base law is open exactly at g₀ = 1 ∧ δ₁ = 1.
Intersects T-class(n) (neither g₀ nor δ₁ is constrained by T-class).
Consumers: the S0.1 class pin at every rung; the Q3 lift-corner/(TAIL-STAB)
compose. Grade: OPEN, fenced; priced S/M (ROOT §44.(ii) Q3). Spec: S5.1.

**[NR-2] The gr(w₂) semantics wrapper → the GRT weld.** Statement: the
accepted law is a harness-calculus identity (val/ε-chain semantics; print
faithfulness lives upstream in GRB/TR3); its gr(w)-semantics reading rides
the accepted GRB retarget consumer-side at order 2, and at general n the
weld to the OM objects is GRT-2 (grTie proper) — OPEN, priced L; GRT-1 (the
harness leg) rides P1. Conditions the INTERPRETATION of every rung on all of
T-class(n), not the harness-internal statement. Consumers: every OM-semantic
consumption (Q4; the public map's semantic claims). Grade: OPEN (L). Spec: S5.2.

**[NR-3] (W-MULT-DCX-m).** Statement: on DCX-(n) the W-MULT display is the
open family; the theorem is proved OFF DCX-(n) only. This residual BITES ON
T-class(n): DCX-(n) ∩ T-class(n) ≠ ∅ — the RM2-probe headline witness (the
all-(e = 2, g = 1) stack, DCX₃-live at e₁ = 2) satisfies e_i ≥ 2, hence IS
T-class. Grade: OPEN family; instance-true everywhere probed (132/132 at
m = 2; T5B 5-read exact; RMG D5 51/51 at m = 3); LAW-DCX-(m+1) a
sealed-confirmed conjecture with SHAPE derived generically minus the residue
non-vanishing ((SURV)/(EMPTY) = the Q2 hard core; SURV-DISJ: LAW-DCX holds
on the pinned class; (SURV-K2)+(SD-YJ) boxes standing; PID† attempt grade).
Spec: S5.3.

**[NR-4] The S4.7/(P-DS) chain (the PE-G1 erratum residual) — ATTACKED THIS
UNIT.** Statement: the DCX-(n) outer bound (and the emptiness leg "DCX_m = ∅
whenever g_{m−2} ≥ 2 or g_{m−1} ≥ 2" riding it) consumes the IH (P-DS)
clause (E₁ᴵᴴ ≠ 0 ⟹ CII_{m−1} ⟹ g_{m−2} = 1); at rungs ≥ 3 (P-DS) sits
inside the (RM)-conditional package chain, so the outer bound is conditional
"exactly as S7 rows 5/6, unless the short engine-free-support lemma (that
the (P-DS) support/E₁/CII sub-clauses never call the engine) is proved —
plausible, NOT claimed here" ([ILN] S4.7 erratum). The theorem's own
off-DCX proof consumes the outer bound (the refutation bracket: "consuming
only the outer bound"). Post-COR-A: ON T-class(n) it discharges only VIA the
COR-A rows-5/6/8 adjudication; OFF T-class and at the downstream (H2)-class
discharge sites it stays conditional — the [RMG] honest-residuals paragraph
carries it verbatim. Cure: the engine-free-support lemma = (DS-SUB), S3/S4
below. Grade before this unit: OPEN (a one-lemma gap).

**Perimeter facts, NOT residuals on T-class (listed so the enumeration is
closed):** (i) the fenced corners — [IL3]'s DEV-1 corner {e₀ = 1 ∧ d₀ ≥ 2 ∧
j ≥ g₀} and [RMG]'s d₀ ≥ 2 ∧ (e₀ = 1 ∨ ∃ interior flat) — are exactly the
complement of T-class(n): they price the T-class boundary (off-T-class the
S7 rows stay (RM)-conditional), and are not conditionality ON it; (ii) the
accepted packages [IL]†/[IL3]†/[RMG]† ride through at accepted grade (their
open members are already listed as NR-1/NR-2/NR-3/NR-4 — [IL]'s gr(w₂)
wrapper clause maps to NR-2, [RMG]'s "(H2)-class discharge via (P-DS)" line
maps to NR-4 [ILNRES-r1 2026-08-08 (wallclock 2026-08-04), per ILNRES-PE1
F2: the parenthetical read "NR-1/NR-3", under-inclusive on the mapping;
the enumeration itself was and is unaffected]); (iii) "orders ≥ 3 untouched"
([IL]) is subsumed by [ILN] itself; (iv) measured-evidence rows (S7 row 11)
are evidence, never conditionality.

## S2. TRIAGE (revision-scope × attackability) and the attack pick

| residual | revision scope | attackable now? |
|---|---|---|
| NR-4 (P-DS)/S4.7 | SMALLEST — one short lemma about this note-family's own proof structure; retires an erratum bracket + the [RMG] honest-residual line | YES — the S4.4–S4.6 constructions visibly consume only division-algorithm existence/degree from the engine calls; machine-testable off T-scope |
| NR-1 LIFT | one dedicated unit (lift-corner compose; W-2 shift-descent covariance is the tool) | partially (priced, not attacked here) |
| NR-3 W-MULT-DCX | the standing Q2 program (own arcs in flight) | owned elsewhere |
| NR-2 GRT weld | LARGEST (semantic weld arc, priced L) | no (needs M1-bridge inputs) |

**Pick: NR-4.** Probe-first discipline: the sealed battery below runs BEFORE
the proof section (S6) is composed.

## S3. LEMMA (DS-SUB) — statement + the sealed machine design

**Sub-package (P-DS⁻) at rung r** (the exact sub-clauses the S4.7 outer-bound
proof consumes, extracted from (P-DS)): for a pair off DCX-(r), with T_j :=
s_j(γ)+s_j(γ′) and P_r the rung-r pair product,
(i) [DS-SUPP] the Φ_r-slot support of P_r is ⊆ {T_{r+1}, T_{r+1}+1}, both
    cofactors digits (deg < deg Φ_r);
(ii) [DS-E1] E₁ ≠ 0 ⟹ CII_r, where CII₁ := (g₀ = 1 ∧ δ₁^{pl} = 1) and
    CII_r := (g_{r−1} = 1) ∧ (δ_r^{pl} = 1 ∨ (CII_{r−1} ∧ T_r = e_{r−1}−1)).
EXCLUDED from the sub-package (stay with the full, COR-A-scoped (P-DS)):
E₀ ≠ 0, the weight clauses, the certified read.

**LEMMA (DS-SUB).** (P-DS⁻) holds at every rung r ≥ 1 off DCX-(r),
consuming ONLY: the S0.2 division algorithm (Φ monic), S4.1 WELL-DEF
existence + degree of Ĉ (unconditional-generic), the S2 integer layer, and
the accepted [IL] base at r = 1 — NO (RM-m) call at any m, no weight clause,
no read clause. CONSEQUENCE if proved: the S4.7 outer bound + its emptiness
leg upgrade to unconditional-generic at every rung, ON and OFF T-class; the
PE-G1 erratum's conditionality and the [RMG] "(H2)-class discharge via
(P-DS)" residual line both retire (ledger action, not a text edit).

**Sealed prediction sheet** (`ilnres_checks.py`; rungs r = 1..3 = up to
4-read; towers chosen OFF T-class — interior flats e_i = 1 with d₀ ≥ 2, and
e₀ = 1 ∧ d₀ = 2 (the DEV-1-corner shape) — where (RM) is UNPROVEN, so the
sub-package holding there is decorrelated evidence that it does not ride the
engine; plus a d₀ = 3 flat via `engine_ext` per the standing rule, one
d₀ = 1 T-control, and one DCX-live e₀ = 3 shape):

| family | content | prediction |
|---|---|---|
| DS-SUPP | (i) at r = 1,2,3, pairs off DCX-(r) | 0 violations |
| DS-E1 | (ii) at r = 1,2,3, pairs off DCX-(r) | 0 violations |
| DS-CASE | the S4.4–S4.6 one-substitution construction (E1c = overflow slots shifted down; E0c = low − E1c·Ĉ, division/degree only) reproduces the direct dev digits at r = 2,3 off DCX-(r) | 0 violations |
| DS-CHAT | deg Ĉ_m < deg Φ_m at every consumed level (WELL-DEF instance) | 0 violations |
| OB-OUT | every constructively DCX_m-flagged pair (E₁ᴵᴴ ≠ 0 ∧ Q¹ ≠ 0 ∧ track-extreme) lies in the outer locus {g_{m−1} = 1, s_m = s_m′ = e_{m−1}−1, CII_{m−1}} | 0 violations |
| DS-E0 | full-(P-DS) instance evidence on the OFF-T shapes (E₀ ≠ 0, w_r(E₀) = B_r) — evidence for the excluded clauses, NOT part of (DS-SUB) | 0 violations (evidence-only) |
| DCX-CENSUS | live DCX_m pairs per tower; sub-package behavior ON DCX recorded | measured |
| ENG-GATE | engine_ext.agreement_gate() re-run in-unit (standing rule) | 0 new violations |

**Disclosures (pre-seal):** (a) a `--shakedown` crash-freedom run (reduced
pair windows, no json) runs before commit 1, per the engine_ext precedent;
(b) exact arithmetic, deterministic full windows, no sampling, no seed;
(c) committed engines consumed READ-ONLY (engine_ext is the single disclosed
seam, in-memory only).

## S4. VERDICT (appended in commit 2 from the artifacts)

**ALL GREEN — every sealed prediction hit exactly** (`ilnres_checks_output.txt`,
`ilnres_checks_results.json`; seal a79f0fe; elapsed 0.5 s; exit 0).

- **LAW families, 0 violations:** DS-SUPP 3,650 · DS-E1 3,650 · DS-CASE 2,386
  · DS-CHAT 17 · OB-OUT 110 · ENG-GATE 0 new violations (agreement_gate
  re-run in-unit per the standing rule, full=False disclosed).
- **DS-E0 (evidence-only): 3,650 checks, 0 deviations** — the full-(P-DS)
  E₀ ≠ 0 / w_r(E₀) = B_r clauses hold on every probed OFF-T-class pair too
  (instance evidence beyond the lemma; changes no grade).
- **Coverage that carries the decorrelation weight [FIGURE CORRECTED,
  ILNRES-r1 2026-08-08 (wallclock 2026-08-04), per ILNRES-PE1 F1]:**
  **608 of 1,264 pairs (1,760 of 3,650 sub-package checks, ≈48%) on
  OFF-T-class towers** — interior flats e₁ = 1 at d₀ ∈ {2,3} (X1/X3/X4),
  the DEV-1-corner shape e₀ = 1 ∧ d₀ = 2 ∧ g₀ = 2 (X2) — exactly where
  (RM-m) is UNPROVEN/fenced, so (P-DS⁻) holding 0-violation there is
  evidence it does not secretly ride the engine. Rings ℤ₂/ℤ₃/F₂[[t]]/F₃[[t]].
  The originally appended figure "1,344 pairs (of 1,656)" was NOT derivable
  from the committed artifacts under any reading (caught by PE1's re-run +
  independent recount) — a violation of this section's own as-of-artifacts
  discipline. The commit-2 message (7c6f060) carries the same wrong figure;
  commits are immutable, so this dated in-note correction is the record.
  The only other quoting site is the same-day BRIDGE_ADJUDICATIONS tail
  block (corrected by the r1 ledger entry). The substantive claim SURVIVES
  at the corrected figure: all-green everywhere, roughly HALF the coverage
  genuinely off-T, all four rings — and **the (DS-SUB) discharge derivation
  is UNAFFECTED (PE1 verified: the S6 proof consumes no coverage count).**
- **DCX live coverage (the OB-OUT teeth):** 101 flagged pairs — X3 (e₀ = 3,
  e₁ = 1) 32 DCX₂; X6 (e₀ = 3, T5B-style) 9 DCX₂ + 12 DCX₃; X5 (the d₀ = 1
  T-CONTROL) 48 DCX₃ — every one inside the outer locus. NOTE OF RECORD: the
  d₀ = 1 T-control being DCX₃-live re-confirms S1's NR-3 point (DCX ∩
  T-class ≠ ∅) on a second independent shape family.
- **Measured (no prediction):** on all 101 DCX pairs the SUPPORT sub-clause
  itself still held (on_dcx_supp_dev = 0) — consistent with the S4.7
  mechanism (the second round spills within {T, T+1}); recorded as a design
  input for the (SURV)/(EMPTY) units, claimed nowhere.

## S5. Priced specs for the non-attacked residuals

**S5.1 (NR-1 LIFT-CORNER unit), price M.** Extend the [IL] L7 Case-II
analysis to an arbitrary lawful lift Φ₁′ = Φ₁ + (π-excess) at g₀ = 1 ∧
δ₁ = 1, then ride S0.1's verbatim propagation. Tools already in hand: the
(TAIL-STAB) compose target (Q3, priced S/M at r39) + W-2's covariance = the
shift-descent operator (the lift perturbation enters as a shifted tail; the
window calculus seeds give the tail-weight bookkeeping). Deliverable: one
note + sealed battery in this genre; no accepted text edited.

**S5.2 (NR-2 GRT-2 grTie), price L.** The graded-dictionary weld: gr(w_{j+1})
reading of the harness read at every level, tied to the OM objects; needs
the M1 projective bridge inputs; GRT-1 (harness leg) rides P1 first. Own arc
(Q4); not further priced here.

**S5.3 (NR-3), price = the standing Q2 program.** The residue non-vanishing
((SURV)/(EMPTY)) on top of the [RMG]-derived LAW-DCX shape; (SURV-K2) +
(SD-YJ) boxes + the W-arc Codex legs post-reset. Owned by wave 21; this
unit adds only the S1 record that NR-3 bites ON T-class (the DCX₃ ∩ T-class
witness), so Q2 is NOT off-T-class-only work.

## S6. The proof of (DS-SUB) (composed AFTER the battery, per the probe-first discipline)

Notation as in [ILN]† (raw sums T_j = s_j(γ)+s_j(γ′) per its S4.3 head
erratum; Φ := Φ_{r−1}, d := deg Φ_{r−1}, e := e_{r−1}, g := g_{r−1};
BP_r := the rung-r pair product with the top factor Φ_r^{T_{r+1}} dropped,
so the rung-r digits are dev(BP_r, Φ_r) at relative slots {0,1} ↔
{T_{r+1}, T_{r+1}+1} — the runner's exact objects).

*Proof, by induction on the rung r.*

**Base r = 1.** [IL L7]'s displayed developments, as pasted in [ILN] S4.3:
Case I — single digit at slot T₂, E₁ = 0, (ii) vacuous; Case II (g₀ = 1 ∧
δ₁^{pl} = 1 = CII₁) — digits at {T₂, T₂+1}, E₁ = π^UΦ₀^{T₁−e₀}, degrees
< deg Φ₁. All from the ACCEPTED [IL]; no engine exists at this level. ∎(base)

**Step r−1 → r (r ≥ 2), pair off DCX-(r) (inner pair off DCX-(r−1)).**
IH = (P-DS⁻) at r−1: A := BP_{r−1}-product digits E₀ᴵᴴ, E₁ᴵᴴ at Φ-slots
{T_r, T_r+1}, deg < d, E₁ᴵᴴ ≠ 0 ⟹ CII_{r−1}.

(1) *The case split is available under (P-DS⁻) alone.* [ILN] S4.4's proof is
"direct from the slot support": overflow of slot j (j ≤ T_r+1 ≤ 2e−1)
past eg−1 is impossible at g ≥ 2 (2e−1 < eg), and at g = 1 is governed by
T_r vs e via δ_r^{pl} — integer layer + IH support/E₁ only. The S4.4
"weights consumed below" paragraph is consumed by the W-MULT legs, not here.

(2) *Case I (deg BP_r < deg Φ_r).* BP_r = BP_{r−1}·Φ^{T_r} occupies the
absolute Φ-slots {T_r, T_r+1} (IH support shifted by T_r; digits unchanged);
in Case I both are ≤ eg−1, so BP_r is itself a Φ_r-digit: the development
is the single digit at relative slot 0, E₁ⁿᵉʷ = 0 makes (ii) vacuous, (i)
is immediate. Engine-free.

(3) *Cases II-\* (g = 1, some absolute slot ≥ e occupied).* One substitution
Φ^e = Φ_r − Ĉ (Ĉ := Ĉ_{r−1}, WELL-DEF(ii): exists, deg Ĉ < d, S4.1 =
unconditional-generic; its m = 1 base rides the ACCEPTED [IL3]†). Writing
the occupied absolute slots a ∈ {T_r, T_r+1} as a = e + k′ when a ≥ e:

    E₁ᶜ := Σ_{a ≥ e} E_a·Φ^{a−e},      E₀ᶜ := Σ_{a < e} E_a·Φ^a − E₁ᶜ·Ĉ,
    BP_r ≡ E₀ᶜ + E₁ᶜ·Φ_r  (mod nothing — an identity),

which specializes to the S4.5/S4.6 II-1/II-2/II-3 displays on the nose
(II-2's R⁰/Q⁰/R¹/Q¹ are the PLAIN-DIVISION refinements of E₀ᶜ; the engine
call sites in S4.6 exist to certify the WEIGHT and READ clauses of those
pieces — clauses excluded from (P-DS⁻)). Degrees: each E_a has deg < d (IH)
and deg Ĉ < d, so deg E₁ᶜ ≤ (d−1) + (T_r+1−e)d < ed = deg Φ_r (T_r ≤ 2e−2),
and deg E₀ᶜ < deg Φ_r ⟺ no piece occupies a Φ-slot ≥ e — the only threat
is the Q¹-piece of E₁ᴵᴴĈ at slot k+2 (II-2) resp. 1 (II-3), and its firing
WITH Q¹ ≠ 0 is the DEFINITION of DCX_r membership ([ILN] S4.7); off DCX_r
both are digits. By uniqueness of the division-algorithm development
(Φ_r monic), (E₀ᶜ, E₁ᶜ) ARE the rung-r digits: support ⊆ {T_{r+1},
T_{r+1}+1} — clause (i). [Machine mirror: DS-CASE, 2,386/0.]

(4) *Clause (ii) at r.* E₁ⁿᵉʷ = E₁ᶜ ≠ 0 forces Case II-\*, whose predicates
each imply CII_r: II-1/II-2 have g = 1 ∧ δ_r^{pl} = 1; II-3 has g = 1 ∧
E₁ᴵᴴ ≠ 0 ∧ T_r = e−1, and E₁ᴵᴴ ≠ 0 gives CII_{r−1} by IH — the second
CII_r disjunct. No direction of (ii) needs E₀ ≠ 0, any weight, or any read.

**Consumption census of the whole induction:** division algorithm (S0.2 base
fact, Φ monic) · S4.1 WELL-DEF existence/degree (unconditional-generic) ·
the S2 integer layer (unconditional) · [IL]/[IL3]† accepted bases. NO
(RM-m) call, no weight clause, no read clause — engine-free as claimed. ∎

**THE DISCHARGE (what retires, as a ledger action — no accepted text is
edited).** In the S4.7 outer-bound proof the (P-DS) consumptions are exactly
(a) T_m ≤ 2e_{m−1}−2 [clause (i)], (b) E₁ᴵᴴ ≠ 0 ⟹ CII_{m−1} [clause (ii)],
(c) CII_{m−1} ⟹ g_{m−2} = 1 [definitional]. All three now ride (DS-SUB),
so: **the DCX-(n) outer bound + the emptiness leg "DCX_m = ∅ whenever
g_{m−2} ≥ 2 or g_{m−1} ≥ 2" upgrade to unconditional-generic at every rung,
ON and OFF T-class(n)**; the PE-G1 erratum's "conditional exactly as S7
rows 5/6" clause and the [RMG] honest-residual line "(H2)-class discharge
via (P-DS) conditional at rungs ≥ 3" retire AT THE GRADE OF THIS NOTE. NR-4
is thereby DISCHARGED-AT-ATTEMPT-GRADE: the residual list of record on
T-class(n) shrinks to NR-1/NR-2/NR-3 once this note's own arc closes.

**Honest grade box.** This note: ATTEMPT, acceptance counter 0/2, hostile
arc OWED (charge: quote-and-classify on S1's enumeration completeness and
S6's consumption census; the S6 step (3) degree chain and the DCX_r-
definition tie are the load-bearing lines). Until the arc closes, the
[RMG]/[ILN] erratum lines stand unedited and NR-4 stands as enumerated in
S1; nothing upstream cites this note yet.

---

## S7. r1 record (round 1 of the acceptance arc)

**ILNRES-PE1 (fresh hostile Fable context; report `ILNRES_passPE1_report.md`,
instrument `verification/openmath/ilnres_pe1_ext.py`): VERDICT GAPS-ONLY —
0 critical, 2 gaps.**

**THE COMPLETENESS CONFIRMATION (the load-bearing outcome, displayed
first): S1's FOUR-MEMBER residual list is COMPLETE relative to the sources
of record — NO FIFTH RESIDUAL EXISTS.** PE1 hunted candidates adversarially
and adjudicated every one: (TAIL-STAB) = the missing tool INSIDE NR-1, not
an independent conditionality; the (INV-n) class/GMN transport/gr(w)
wrapper = NR-2's genre or fences on statements never made; the fenced
corners = exactly the T-class(n) complement (set algebra verified,
including the [IL3] DEV-1 corner containment); RM-1's own corner covered
on T-class; the DITER E₂ > 1 scope sentence = NR-2 genre (nearest miss);
no hidden top-level constraint escapes the list.

Also verified: the (DS-SUB) rung induction re-derived in full (case split,
construction identity, degree chain, clause (ii)) with the consumption
census EXACT and the discharge scope exact ON and OFF T-class; the machine
leg re-run byte-identical (results json md5 unchanged) plus 33
fresh-arithmetic hand checks green, including an INDEPENDENT dcx3 = 48
recount; the NR-3 bites-on-T-class finding confirmed on both witness
families; priced specs honest. The two gaps — F1 (the S4 coverage figure)
and F2 (the perimeter-fact-(ii) parenthetical) — are repaired in the dated
[ILNRES-r1] brackets above; no grade changes; NR-4's
DISCHARGED-AT-ATTEMPT-GRADE status stands. PE1's no-finding remark on
OB-OUT's evidentiary weight (a consistency mirror of the outer-bound
derivation, not an independent test) is adopted into the record here so it
is not over-read later. **Arc: 0/1 CLEAN (gaps-only, repaired); round 2 =
the Codex leg (Thursday window).**
