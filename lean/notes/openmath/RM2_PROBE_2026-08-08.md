# RM2_PROBE — sealed preregistration: the (RM-2) ENGINE MEASUREMENT — the correction-product engine's displayed invariants probed at the FIRST UNPROVED LEVEL (m = 2) on 4-read towers where level-2 corrections genuinely fire, BEFORE any compose (wave-21 Q1 opening move; probe-first)

Runner of record: `verification/openmath/rm2_probe.py` (sealed with this
note at commit 1, run AFTER the seal; verdict = commit 2 from artifacts).
The product of this unit is a MEASURED-INVARIANT VERDICT on the (RM-2)
engine statement, NOT a proof and NOT part of any acceptance arc. A
violation of any sealed leg is a probe SUCCESS (the violating tuple is
the unit's product, reported prominently). One resealed candidate cycle
is allowed within the unit if a sealed leg RED demands a corrected
candidate.

## S0. Authority, the statement's home, and the incorporation declarations

THE CLOSURE PROGRAM charter: ROOT §44.(ii) Q1 — "the **(RM-m, m ≥ 2)**
generic engine (the correction-product engine, THE hard core; m = 1
proved inside [IL3]†; price per the ITERLAWN S4.6 box: one dedicated
unit per level-family, the generic-engine unit subsumes the family)".
This unit is the PROBE-FIRST opening move of Q1: a sealed measurement of
the engine's displayed obligations at m = 2 before the generic-engine
compose is attempted.

Incorporation declarations (convention 9f9910c: full paste at the
declaration + operative sites; daggers elsewhere):

**[ILN]† declared.** Ledger block (BRIDGE_ADJUDICATIONS, r39 append /
ROOT §44.(i)), pasted at this declaration:

> [ILN] = the (ITER-LAW-n) theorem note, ACCEPTED 2026-08-03: the
> uniform recursion c_{n+1}(γ,γ′) = z̄^δ·∏ⱼ z_j^{D_j} (S_n = δγ_{n+1};
> D_j = (s_j-defect + ℓ_{j−1}S_j)/e_{j−1}; S_{j−1} = D_jγ_j +
> S_j(ℓ_{j−1}′ − ℓ_{j−1}w_{j−1}Φ_{j−1})) for ALL n ≥ 1 on the
> (n+1)-read standard-lift harness class, PROVED with the structure:
> integer/positivity/window/cocycle/descent + REALIZE/WELL-DEF
> unconditional-generic; L6-(n) every rung from [IL]† (+ S4.1 at rungs
> ≥ 2); W-MULT-(n) Case I generic given the IH package; Cases II generic
> MODULO (RM-m, m ≥ 2) (m = 1 proved inside [IL3]†), off DCX-(n) (outer
> bound proved given the IH package). RESIDUALS: (RM-m, m ≥ 2);
> (W-MULT-DCX-m); the fenced corners; the consumed [IL]†/[IL3]† packages
> ride through. Rung ledger: n = 1 [IL] ACCEPTED · n = 2 [IL3]
> ACCEPTED-OFF-DCX · n = 3 MEASURED GREEN (13b026b) · n = 4/5 measured
> (5fe901e diag) — instance evidence only.

**[IL3]† declared.** Ledger block (BRIDGE_ADJUDICATIONS "[IL3] ACCEPTED"
entry), pasted at this declaration:

> [IL3] = the (ITER-LAW-3) theorem, ACCEPTED 2026-08-03: the display
> c₃ = z̄^{δ₃}·z₂^{D₂}·z₁^{D₁} (δ₃ = ⌊(s₃+s₃′)/e₂⌋; D₂ = (s₂-defect +
> ℓ₁γ₃δ₃)/e₁; Σ = D₂γ₂ + δ₃γ₃(ℓ₁′−ℓ₁w₁Φ₁); D₁ = (s₁-defect + ℓ₀Σ)/e₀)
> PROVED on the 3-read standard-lift harness class OFF the DCX corner
> {g₀ = g₁ = 1, δ₁ = 1, s₂(γ) = s₂(γ′) = e₁−1, S₁+s(γ₂) ≥ 2e₀} (EMPTY at
> e₀ ≤ 2) — RUNG n = 2 of (ITER-LAW-n). RESIDUALS: (W-MULT-3-DCX) OPEN
> (instance-true 132/132); the fenced non-consumed DEV-1 corner
> {e₀=1 ∧ d₀≥2 ∧ j≥g₀}; the consumed [IL] base's package rides through.

Harness of record, consumed VERBATIM read-only (all sealed artifacts
byte-untouched): `grb_order2_check.Tower` (levels 0–2: w2/R2w/eps1/
realize1/build_Phi2), `strata3_probe.Tower3` (level 3: eps2/R2v/
realize2/build_Phi3), `iterlawr_probe` (the disclosed pick_irr3 shim),
`iterlaw4_probe.Tower4/Composite4` (the 4-read tower + window/split).
Class pin: the (n+1)-read standard-lift harness class of [ILN]† at
n = 3 (4 reads), restricted to g₂ = 1 (the (RM-2) setting).

## S1. THE STATEMENT UNDER TEST (sealed) — the (RM-m) box at m = 2

The operative statement is the [ILN]† S4.6 box, quoted VERBATIM
(operative-site paste; the m = 1 STATUS/SCOPE lines and the m ≥ 2
resistance pricing are part of the box):

> **(RM-m) [READ-MULT with the canonical correction, level m], 1 ≤ m.**
> Setting: level m of a class tower with g_m = 1, Ĉ_m the canonical
> correction digit of WELL-DEF-(n)(ii) (deg < deg Φ_m, w_m = γ_{m+1},
> R_{m,γ_{m+1}}(Ĉ_m) = τ_m = ε_m(γ_{m+1})^{−1}c_m). Input: f ∈ O[x],
> f ≠ 0, deg f < deg Φ_m, λ := w_m(f), v := R_{m,λ}(f) its read. Claim:
> there exist R, Q ∈ O[x] with deg R < deg Φ_m, deg Q < deg Φ_m, and
>
>   f·Ĉ_m = R + Q·Φ_m,   w_m(R) ≥ λ + γ_{m+1},
>   w_m(Q) ≥ λ + γ_{m+1} − w_mΦ_m   (Q = 0 allowed),
>   R_{m,λ+γ_{m+1}}(R) = z_m^{ε′}·v·τ_m,
>   ε′ := ⌊(s_m(λ) + s_m(γ_{m+1}))/e_{m−1}⌋  [the level-m EPS-carry].
>
> STATUS. m = 1: PROVED — [IL3] DIGIT-SPLIT + DEV-1 (…) + the
> double-sum factoring (…). SCOPE inherited from the DEV-1 erratum:
> proved for e₀ ≥ 2, and at e₀ = 1 ∧ d₀ = 1 (junkΦ ≡ 0); at
> {e₀ = 1 ∧ d₀ ≥ 2} general inputs enter [IL3]'s fenced DEV-1 corner.
> m ≥ 2: **OPEN.** Expected resistances: (i) TERMINATION — (…) the
> e-descent failure mode of the DEV-1 erratum (non-descending junk
> children at e = 1) recurs at every level — a well-founded measure
> across levels is the real content; (ii) READ-TRANSPARENCY across
> Ĉ-insertions; (iii) input-class closure (the IH (P-DS) exports and
> their junk legs under the weight filtration).

(Ellipses above elide only the m = 1 mechanism prose and pricing lines
of the box, none of the displayed m = 2 obligations; the full box is
[ILN]† S4.6, byte-frozen there.)

**m = 2 instantiation (every symbol in harness terms).** g₂ = 1 tower;
ψ₂ = y + c₂ over K₂ (c₂ = the constant coefficient; z₃ = −c₂);
Ĉ₂ := Φ₃ − Φ₂^{e₂} (WELL-DEF-(n)(ii): the harness `build_Phi3` at
g₂ = 1 produces exactly Φ₂^{e₂} + realize2(τ₂, γ₃)); τ₂ :=
ε₂(γ₃)^{−1}·c₂ ∈ K₂; w₂/R₂-reads = the harness `Tower.w2` /
`Tower3.R2v` (level-2 read VALUE at z₂, target-weight wrapper: read 0
strictly above the line); s₂(β) := eq12(β, e₁, h₁).s; ε′ :=
⌊(s₂(λ)+s₂(γ₃))/e₁⌋ ∈ {0,1}.

**Canonicity reduction (sealed reasoning).** Since deg f < deg Φ₂ and
deg Ĉ₂ < deg Φ₂, ANY decomposition f·Ĉ₂ = R + QΦ₂ with deg R < deg Φ₂
forces (Q, R) = divmod(f·Ĉ₂, Φ₂) (uniqueness of division by the monic
Φ₂; deg Q < deg Φ₂ then automatic). So the box's existence claim is
EQUIVALENT to the canonical division satisfying the displayed clauses —
the probe tests the division.

**THE FIRING PREDICATE (sealed; derived from [ILN]† S4.3/S4.4/S4.6).**
The rung-3 induction calls (RM-2) exactly on the Case-II pairs of the
level-3 top analysis. With the iterated splits of γ, γ′ (Composite4
.split: s₄,s₃,s₂,s₁,b₃,b₂,b₁,u₁), â := π^{u₁}Φ₀^{s₁}Φ₁^{s₂} the
sub-Φ₂ anchor part, and the rung-2 IH exports read off the S4.6 II-2
display "A = E₁ᴵᴴΦ^{T₃+1} + E₀ᴵᴴΦ^{T₃}" backwards:

    E₁ᴵᴴ := quo(â·â′, Φ₂),   E₀ᴵᴴ := rem(â·â′, Φ₂),
    T₃ := s₃ + s₃′,          δ₃^{pl} := ⌊T₃/e₂⌋,

    FIRE(γ,γ′) := g₂ = 1  ∧  [ T₃ ≥ e₂            (II-1: E₁ᴵᴴ = 0;
                                                    II-2: E₁ᴵᴴ ≠ 0)
                               ∨ (E₁ᴵᴴ ≠ 0 ∧ T₃ = e₂−1)   (II-3) ].

On fired rows the engine inputs are f = E₀ᴵᴴ (the note-consumed call:
read clause consumed in II-1/II-2) and f = E₁ᴵᴴ when ≠ 0 (the note
consumes only its WEIGHT clauses; the box claims all clauses
universally — the probe tests all clauses on both, with the provenance
split reported). BREADTH class (the box's full quantifier): monomials
π^u·b·Φ₀^i·Φ₁^j (u ∈ {0,1}; i < e₀g₀; j < e₁g₁; b ∈ {1} ∪ {x at
d₀ = 2}) + 3 fixed two-term sums per tower. Dedup by polynomial key;
deterministic; full W₁×W₁ window blocks; no sampling, no seed.

## S2. The candidate reduction tree (the L3 termination instrument), roster, and design disclosures

**The tree is THE PROBE'S CANDIDATE algorithm** — the note displays NO
m = 2 algorithm (that is the open content); the probe instantiates the
natural m = 2 analogue of the [IL3]† S4.1 mechanism and MEASURES it.
Design (sealed): nodes b·Φ₀^{j₀}Φ₁^{j₁} (deg b < d₀), aggregated per
(j₀,j₁) cell; initial cells = DIGIT-SPLIT of the coefficient products
of the monomial expansions of f and Ĉ₂ (the m = 1 double-sum
template, one level up). E1-PRIORITY reduction: if j₀ ≥ e₀g₀ apply the
level-1 standard-lift identity Φ₀^{e₀g₀} = Φ₁ − Σ_{i<g₀}
lift(ψ₀ᵢ)π^{(g₀−i)h₀}Φ₀^{e₀i} (promotion child (j₀−e₀g₀, j₁+1) +
DIGIT-SPLIT children); elif j₁ ≥ e₁g₁ apply the level-2 canonical
identity Φ₂ = Φ₁^{e₁g₁} + Σ_{k<g₁} C_kΦ₁^{e₁k} (C_k = the harness
build_Phi2 correction digits, replayed and identity-checked): Φ₂-exit
joins Q, DIGIT-SPLIT children of b·(C_k Φ₀-digits); else R-leaf.
E1-priority is what keeps every node inside the window
{j₀ < 2e₀g₀, j₁ < 2e₁g₁} (the m = 2 input-class-closure analogue —
a sealed leg).

**THE TERMINATION MEASURE (sealed prediction).** μ(j₀,j₁) :=
e₀g₀(e₁+1)·j₁ + 2e₁·j₀. Derivation on record: the three in-tree edge
types shift μ by

    E1 non-promotion:  2e₁·Δj₀,  Δj₀ ≤ −(e₀−1)      < 0 iff e₀ ≥ 2
    E1 promotion:      e₀g₀(1−e₁)                    < 0 iff e₁ ≥ 2
    E2 correction:     ≤ e₁e₀g₀(1−e₁)                < 0 iff e₁ ≥ 2

so STRICT μ-descent on every in-tree edge is predicted EXACTLY on the
main scope e₀ ≥ 2 ∧ e₁ ≥ 2 — the m = 2 analogue of the DEV-1 erratum
scope (e₀ ≥ 2), with the box's "(i) TERMINATION … recurs at every
level" bend predicted VISIBLE at e₀ = 1 (junkΦ self-edge, the fenced
corner one level up) and at e₁ = 1 (flat promotion). Out-of-scope
shapes are BEND-CENSUS rows (separate lines, not law violations). Cell
cap 3000 pops per input (legit main-scope trees need ≪ 10³); cap-hit on
a main-scope shape is an L3 violation, on a bend shape a census event.

**Roster** (all g₂ = 1; r_j = (e_j,h_j,g_j); full W₁×W₁ blocks):

| tag | d₀ | r0 | r1 | r2 | r3 | rings | why |
|---|---|---|---|---|---|---|---|
| RM2A | 1 | (2,1,1) | (2,1,1) | (2,1,1) | (1,1,2) | Zp3, Fpt2 | main-scope stack, deep Case II at all levels |
| RM2B | 1 | (3,2,1) | (3,1,1) | (2,5,1) | (1,1,1) | Zp3, Fpt2 | DCX₃-live candidate: e₁ = 3, s₂(γ₃) = 2 via h₂ = 5 |
| RM2C | 2 | (2,1,2) | (2,1,1) | (2,1,1) | (1,1,1) | Zp3 | junk-alive d₀ = 2 + multi-term level-1 identity (g₀ = 2) |
| RM2D | 1 | (2,1,1) | (2,1,2) | (2,1,1) | (1,1,1) | Zp3 | g₁ = 2 multi-C_k level-2 identity; locus control |
| RM2E | 1 | (2,1,1) | (1,1,2) | (2,1,1) | (1,1,1) | Zp3 | BEND: e₁ = 1 (no valid linear μ) |
| RM2F | 2 | (1,1,2) | (2,1,1) | (2,1,1) | (1,1,1) | Zp3 | BEND: e₀ = 1 ∧ d₀ = 2 (the DEV-1 fenced corner, one level up) |
| RM2G | 1 | (2,1,1) | (3,1,1) | (1,2,1) | (2,1,1) | Zp3 | e₂ = 1 II-3-heavy; DCX₃ candidate via the e₂ = 1 branch |

**Design disclosures (pre-seal `--design` run; NO law leg evaluated —
censuses, tower certs, and prediction-side locus counts only; output
`rm2_probe_design.json`).** (a) All 9 tower-runs pass every design
cert: g₂ = 1, ψ₂ monic degree 1, Ĉ₂ properties (Ĉ1) deg < deg Φ₂ /
(Ĉ2) w₂(Ĉ₂) = γ₃ / (Ĉ3) R₂-read = τ₂, the level-1 and level-2
identity replays byte-exact. (b) Firing censuses (fired/pairs):
RM2A 28/64 (II1/II2/II3 = 10/6/12, both rings), RM2B 153/324
(45/36/72, both rings), RM2C 24/64, RM2D 16/64 (all II-1: g₁ = 2 makes
E₁ᴵᴴ ≡ 0 structurally, T₂ ≤ 2(e₁−1) < e₁g₁ — the E1-extra class is
empty there BY DESIGN, disclosed), RM2E 4/16 (all II-1), RM2F 6/16,
RM2G 60/144 (all II-3). (c) Predicted-locus counts (prediction side
only): dcx_pred = 9 on RM2B (both rings; = 3², the s₂ = s₂′ = 2 ∧
s₃ = s₃′ = 1 block) and 16 on RM2G (= 4²); 0 elsewhere (RM2A/C/D at
e₁ ≤ 2 or g₁ = 2: transported emptiness). ROSTER FIX ON RECORD (design
phase, pre-seal): the first-draft roster had h₂ = 1 everywhere, making
s₂(γ₃) = ℓ₁h₂ mod e₁ = 1 and the predicted locus ARITHMETICALLY EMPTY
on all shapes (T₂ ≤ 2e₁−2 < 2e₁−1); RM2B/RM2G h₂ retuned to 5/2 so
s₂(γ₃) = 2. This uses the closed form γ₃ ≡ h₂ (mod e₁) ⟹ s₂(γ₃) =
ℓ₁h₂ mod e₁ — itself a design-level check of the transport's
sensitivity to h₂. (d) MC-D separator rows (3-of-4 clauses, prediction
side): 4/4/18/18/4/1/32 across the runs — MC-D conditionally visible.
(e) MC visibility from tower data: z₂ ≠ 1 on 6/9 runs (z₂ = 1 on
RM2A/Fpt2 + RM2B both rings — MC-A rides the other 6); ε₂(γ₃)² = 1 on
ALL runs (tiny residue fields) — which is WHY MC-C is the z₃-SIGN
mutation τ₂ → ε₂(γ₃)^{−1}(−c₂), visible at every p = 3 run (7/9).
(f) Cost: design pass 0.1 s; the sealed battery is estimated < 60 s
(trees ≪ 10³ pops each). E₀ᴵᴴ = 0 never occurs (E0zero = 0, all runs).

## S3. Preregistered falsifier legs, predictions, and controls

All legs LAW-KEYED; predicted violation count 0 on every leg below
unless marked otherwise; RED on any leg = probe SUCCESS (the violating
tuple is the product). Verdict lines pre-agreed (S5 wording).

**L1-WEIGHT** (every engine input: E₀ᴵᴴ, E₁ᴵᴴ, breadth): the canonical
division satisfies w₂(R) ≥ λ+γ₃ AND (Q = 0 ∨ w₂(Q) ≥ λ+γ₃−w₂Φ₂).
Predicted 0.

**L2-READ** (same inputs; provenance-split E0-consumed / E1-extra /
BR-mono / BR-sum): R₂-read of R at target λ+γ₃ equals z₂^{ε′}·v·τ₂
with every factor pinned (v := R₂-read of f at λ; τ₂ := ε₂(γ₃)^{−1}c₂,
design-certified = the Ĉ₂-read; ε′ := ⌊(s₂(λ)+s₂(γ₃))/e₁⌋; read-above-
the-line := 0). Predicted 0 — INCLUDING on the E1-extra and breadth
classes the note never consumes: a violation there means the box as
displayed BENDS at m = 2 (the highest-value possible finding).

**L2-EPS** (per input): the carry identity s₂(λ) + s₂(γ₃) − s₂(λ+γ₃)
= e₁·ε′ (the m = 2 instance of the m = 1 on-line carry additivity).
Predicted 0.

**L3-TERM / L3-CAP / L3-WINDOW** (main scope RM2A/B/C/D/G only):
strict μ-descent on every in-tree edge; no cap-hit; every node inside
{j₀ < 2e₀g₀, j₁ < 2e₁g₁}. Predicted 0/0/0. BEND CENSUS (RM2E/RM2F;
separate lines, NOT law violations): predicted NONZERO bend events —
RM2E flat-promotion non-descent edges; RM2F junkΦ self-edge events
and/or cap-hits (the fenced corner reproduced one level up). A ZERO
bend census would itself be informative (the corner not reached by
these inputs) and is reported either way.

**CONS** (every input whose tree completes): tree-assembled (R, Q) ==
division (R, Q) as polynomials. Predicted 0. (Validates that the
candidate tree computes THE decomposition, so the L3 measurement is
about the right object.)

**L4a-DCX-OUTER**: measured second-round-overflow rows — DCX₃ᵐᵉᵃˢ :=
{E₁ᴵᴴ ≠ 0 ∧ Q¹ := quo(E₁ᴵᴴĈ₂, Φ₂) ≠ 0 ∧ [(δ₃^{pl} = 1 ∧ s₃ = s₃′ =
e₂−1) ∨ (T₃ = e₂−1 ∧ e₂ = 1)]} (the [ILN]† S4.7 DCX_m definition at
m = 3, operationalized) — satisfy the outer bound {g₁ = 1 ∧ s₃ = s₃′ =
e₂−1}. Predicted 0 (the outer bound is PROVED given the IH package).

**L4b-DCX-SHARP** (conjecture-grade): DCX₃ᵐᵉᵃˢ == DCX₃ᵖʳᵉᵈ as sets per
tower, where the PREDICTED locus is the [IL3]† sharp DCX_2 predicate
transported one level up (the [ILN]† S4.7 conjectured family at m = 3;
naming note: the task-charter's "W-MULT-DCX-2 predicate" = the
(RM-2)-engine member of the (W-MULT-DCX-m) family, whose locus in
[ILN]'s level indexing is DCX₃):

    DCX₃ᵖʳᵉᵈ := { g₁ = g₂ = 1,  δ₂^{pl} := ⌊(s₂+s₂′)/e₁⌋ = 1,
                  s₃ = s₃′ = e₂−1,  (s₂+s₂′) + s₂(γ₃) ≥ 2e₁ }.

Predicted 0 mismatches. [IL3] sharp form quoted for the transport
audit: "DCX_2 = {g₀ = g₁ = 1, δ₁^{pl} = 1, s₂ = s₂′ = e₁−1, S₁ +
s(γ₂) ≥ 2e₀} — EMPTY unless e₀ ≥ 3" ([ILN]† S4.7 sharp-form bullet).

**L4c-DCX-EMPTY** (conjecture-grade): DCX₃ᵐᵉᵃˢ = ∅ on every e₁ ≤ 2
shape (the transported "EMPTY unless e_{m−2} ≥ 3"). Predicted 0 live
rows on RM2A/C/D/E/F.

**Mutation controls (teeth; verdict SEPARATE from the law — the
standing lesson).** MC-A: predicted read × z₂ (ε′+1) — must flag
somewhere (visible on the 6 z₂ ≠ 1 runs). MC-B: weight clause
tightened to λ+γ₃+1 — must flag (visible wherever the R-read ≠ 0, i.e.
w₂(R) = λ+γ₃ exactly; conditional on L1/L2 GREEN). MC-C: the z₃-sign
mutation τ₂ → ε₂(γ₃)^{−1}(−c₂) — must flag (visible at p = 3 runs,
char ≠ 2). MC-D: DCX₃ᵖʳᵉᵈ minus its last clause — must flag on ≥ 1
separator row (design censuses show 77 separator rows roster-wide;
conditional on those rows measuring non-live). MC-E: mutated
termination measure μ′ := j₁ only — must record a flat edge (every E1
non-promotion edge is μ′-flat; visible whenever E1 fires on main
scope). A control NOT tripping = CONTROL FAILURE, reported separately;
it never colors the law verdict.

## S4. What this probe does NOT claim (fences)

1. GREEN everywhere is INSTANCE EVIDENCE for (RM-2) on this roster —
   never a proof, never an acceptance; the S4.6 box stays OPEN at
   m ≥ 2 until the generic-engine compose passes a hostile arc.
2. The reduction tree is the PROBE'S candidate algorithm; L3 measures
   THAT candidate. L3 RED on main scope refutes the candidate
   measure/algorithm pair, not (RM-2) itself (L1/L2 test (RM-2)).
3. The DCX₃ᵖʳᵉᵈ predicate is the mechanical transport of the [IL3]
   sharp form; L4b RED refutes the TRANSPORT (a S4.7-conjecture
   instance), pinning where the m = 1 mechanism bends — a probe
   success, not a crisis.
4. No claim about g₂ ≥ 2 (no single Ĉ₂ exists there), about m ≥ 3, or
   about the full-class DCX-(n) emptiness beyond the two censused
   shapes.
5. The harness is consumed as sealed ground truth; any harness-level
   assertion failure is a HARNESS EVENT (reported, run aborted), not a
   law verdict.

## S5. VERDICT (commit 2, from the artifacts; seal = 0b42885; run
`rm2_probe_output.txt` + `rm2_probe_results.json`, 0.4 s, deterministic)

**THE FOUR VERDICT LINES (transcript tail, verbatim):**

    LAW VERDICT (RM-2 engine L1/L2/CONS/L4a, predicted-0): GREEN [0 violations]
    TERMINATION VERDICT (L3 main scope, predicted-0): GREEN [0 violations]
    LOCUS VERDICT (L4b sharp-transport + L4c emptiness, conjecture-grade predicted-0): RED (probe SUCCESS) [42 violations]
    CONTROL VERDICT (mutation teeth, SEPARATE): MET

**GREEN detail.** L1-WEIGHT 0/405 calls; L2-READ 0/405 with the
provenance split E0-consumed 147 / E1-extra 95 / BR-mono 136 / BR-sum
27 — the read clause holds INCLUDING on the two classes the note never
consumes; L2-EPS 0; CONS 0/405 (every candidate tree completed and
reproduced the canonical division exactly — zero caps anywhere in the
battery); L3 main scope 0 non-descent edges / 0 caps / 0 window
violations; L4a outer bound 0/73 measured DCX₃-live rows. Controls all
tripped (MC-A 208, MC-B 405, MC-C 295, MC-D 42, MC-E 182). No harness
events. No reseal cycle used.

**BEND CENSUS (separate lines).** RM2E (e₁ = 1): 8 non-descent edges,
all flat promotions — exactly the predicted e₁ = 1 bend; trees still
terminated. RM2F (e₀ = 1 ∧ d₀ = 2): EMPTY census (0 bad edges, 0
caps) — the junkΦ self-edge never fired on these inputs (the picked ψ₀
is sparse and the operative/breadth inputs dodge the corner): the
fenced corner is UNTESTED here, not passed — reported honestly.

**THE RED — THE PROBE'S FINDING (42 violations = 39 L4b-SHARP + 3
L4c-EMPTY; every mismatch is meas = True, pred = False — the
transported predicate is too SMALL, never too big).** Headline
violating tuple, reported prominently:

    (RM2A/Zp,p=3, γ = γ′ = 51): the all-(e = 2, g = 1) 4-read stack,
    s₃ = s₃′ = 1, s₂ = s₂′ = 1, s₁ = s₁′ = 1, u₁ = 2:
    E₁ᴵᴴ ≠ 0, Q¹ = quo(E₁ᴵᴴĈ₂, Φ₂) ≠ 0 — DCX₃-LIVE AT e₁ = 2,
    cross-ring confirmed (same row at Fpt,p=2); also live at RM2C.

Consequences at instance grade: (a) the [ILN]† S4.7 CONJECTURED family
instance at m = 3 — "DCX_m = ∅ unless e_{m−2} ≥ 3" — is REFUTED (its
m = 2 base, [IL3]'s proved "EMPTY unless e₀ ≥ 3", does NOT transport);
(b) the S4.7 Consequences bullet's conjectural clause ("full-class
emptiness for n ≥ 3 at e_j ≤ 2") is settled NEGATIVELY — ITERLAWN's
off-DCX-(n) perimeter is genuinely load-bearing at rung 3 even on
all-e ≤ 2 towers ([ILN]'s theorem itself is UNAFFECTED: it excludes
DCX-(n) by construction; only the conjectured shrinkage of the
excluded locus dies); (c) the [IL3] rung-2 sharp form is untouched.

**POST-RUN SEPARATION (additive, display-only, NOT sealed;
`rm2_postrun_separation.py` + output).** On every track-eligible row
(s₃ = s₃′ = e₂−1, E₁ᴵᴴ ≠ 0) of the three DCX-live towers, the measured
locus is EXACTLY (102/102 rows, 0 misfits) the TWO-LEVEL CARRY CHAIN

    DCX₃ᵐᵉᵃˢ = TRACK ∧ [ T₂ + s₂(γ₃) ≥ 2e₁            (direct level-2)
                         ∨ ( T₂ + s₂(γ₃) = 2e₁ − 1
                             ∧ s₁ + s₁′ + s₁(u₀) ≥ e₀ ) (chained lvl-1)]

with u₀ := (γ₃ − s₂(γ₃)γ₂)/e₁ the level-1 weight of Ĉ₂'s Φ₁-digit and
s₁(u₀) its level-1 abscissa (0/0/1 on RM2A/B/G). Reading: at the
level-2 boundary the missing +1 Φ₁-exponent is supplied by a LEVEL-1
overflow promoting into the Φ₁ slots — the tree's E1-promotion edge.
Conjecture-grade candidate for the (RM-2) second-round clause; roster
evidence only.

**ENGINE-SHAPE READING (the unit's charge).** The m = 1 mechanism
GENERALIZES AS DISPLAYED at m = 2 on every displayed obligation: the
box's four clauses are instance-true on all 405 calls (including the
E1-extra and breadth classes beyond the note's consumption), and the
natural two-identity reduction tree with the linear measure
μ = e₀g₀(e₁+1)j₁ + 2e₁j₀ terminates with strict descent exactly on the
predicted m = 1-analogue scope e₀ ≥ 2 ∧ e₁ ≥ 2 (flat exactly at
e₁ = 1, as the box's resistance (i) prices). Where the m = 1 mechanism
BENDS: ONLY the sharp second-round-overflow characterization — at the
m = 2 engine level it is NOT a single-level track predicate; the
boundary is decided by a chained level-1 carry riding the SAME
promotion edge that carries the termination measure. Design input for
the Q1 generic-engine compose: the second-round clause and the
termination measure must thread the promotion edge together.
