# GRB hostile pass 8 — deciding pass (Fable), archived from workflow journal

Leaf: `lean/notes/openmath/GRB_ORDER2_2026-08-05.md` (rev 6). Deciding pass of the
2-clean model-diverse bar (pass 7 = CLEAN, 1/2). Verdict: **GAPS-ONLY** (2 justification
gaps, 0 criticals) — clean counter resets to 0/2. Archived 2026-08-06 from
`wf_add97566-eb3/journal.jsonl` (the report .md was not written by the pass agent).

---

All charge items are now executed. Compiling the verdict.

---

**HOSTILE PASS 8 — GRB_ORDER2_2026-08-05.md (rev 6), fresh context, deciding pass of the 2-clean bar**

**(1) TR-1 re-derivation — CONFIRMED.** From the verified pins alone: FGMN augments by *adding* λ_r ∈ ℚ₊ (Def 1.8, fgmn.txt:293 verbatim) and cuts the polygon of points (s, µ_{r−1}(a_sφ_r^s)) (fgmn.txt:~469) by the line of slope −λ_r touching from below (Lemma 2.3 :504, Def 2.5 :576–577). GMN's polygon shares abscissas but takes ordinates u_i = v_r(a_iφ_r^i) (gmn.txt Def 2.19, verified), v_r ↔ w_i = E_i·µ_{r−1} — an ordinate dilation by E_i, which multiplies every slope by E_i and preserves touching-from-below; GMN records the side slope as the negative rational λ_r = −h_r/e_r (gmn.txt Def 2.21, verified). Hence λ^GMN = −E_i·λ_r^FGMN, and with the printed λ_i = h_i/(e₀⋯e_i) (fgmn.txt eq (9), verified) this is −h_i/e_i — exactly (TR-1-corr). Independent re-derivation agrees; G3 re-run reproduces the recorded split (0/20000 corrected; 6784/17801 mismatches at E_i>1, 0/2199 at E_i=1).

**(2) (H-CHAIN) audit — CONFIRMED.** Def 3.1 verbatim (fgmn.txt:757–759): MacLane chain iff "φ_{i+1} ≁_{µ_i} φ_i for all 1 ≤ i < r". Under r = i+1, φ_k ↔ Φ_{k−1}, µ_k ↔ w_k/E_k, substituting j = k−1 with 1 ≤ k ≤ i gives Φ_{j+1} ≁_{w_{j+1}/E_{j+1}} Φ_j for **0 ≤ j ≤ i−1** — the REV-5 range, nonempty at order 2 (k=1 is φ₂ ≁_{µ₁} φ₁). The automatic-at-degree-increasing argument is printed at :782–784 as claimed. The REV-6 sync claim verified on disk: `lean/blueprints/HDISCHARGE_H1.md` lines 200–203 and 678–681 display the full range with the Def 3.1 cite.

**(3) DISCHARGED dependency chains — CLEAN.** Every pin verified verbatim at its stated line: Cor 4.7(3) is genuinely total (no properness) — the TR-7 pin holds and the Lemma 5.13 re-pricing is correct (5.13 verified: ideal-level R, µ-proper hypothesis, :1933); Thm 4.2 + the eq-(16)/j₀ tie, Thm 4.8, Cor 4.4(1)–(4), 4.5, 4.9, 4.11, Lemma 4.1 + Cor 2.7 endpoint additivity (:636), Lemma 3.14, the s(1/e(µ_i)) = ℓ_i remark (:1202), Bézout pair (:1075ff), Cor 5.6(2), Def 5.10 + both properness remarks, Cor 5.12, Lemma 5.3, Prop 5.14 (with improper ⌈·/e_r⌉ branch), Cor 5.15 — all match. No discharged verdict consumes TR-3′-GEN, TR-3-ORD-X, D-ITER, or N-1; (3)/(4) are conditional only on the declared inputs (KP_i)/[T], displayed. The grep sweep for "served" shows every service claim inside struck/flagged text; the surviving S6.4 sentence claims only the FGMN-internal integer — honest. S3.2's instance computation re-derived by hand (ǫ₁(α_j) = z₁^{−α_j}, t₁(s_j) = k, ratio step z₁^{γ₂}, γ₂ = 2·2+1 = 5) — correct; G2 re-run reproduces 500/500 both ways with z₁⁵ ≠ 1.

**(4) D-REAL scope / D-ITER firing — CONFIRMED.** Rank arithmetic checks: D-REAL's p_r-gauged layer has rank e_i; the [F.2] stage object has rank e_i·E_i; equal iff E_i = 1; at order 2, E₁ = e₀, so the proved stratum is exactly e₀ = 1 and D-ITER fires at E_i > 1 from order 2. Every occurrence (S0 flag, S5.3 flag, R2.2, R2.4, R2.5, R4.4, R5.4, R6.2) is consistent. CARRY-1's proof is correct (uniqueness split at s+s′ ∈ [0,e) vs [e,2e)).

**(5) Gate scoping — HONEST.** Both scripts carry preregistered predictions in docstrings; arithmetic is exact (modular 𝔽₂₅ / `Fraction`); both re-run green reproducing the leaf's recorded numbers to the digit. Post-REV-4/5 text presents G2's positive leg as "support, not proof" and the refutation as counterexample (legitimate). N-1 debt displayed at S0 and in the residue.

**(6) Fresh sweep — two findings, both justification gaps:**

**GAP-1 (S1.4, line 120; echoed at S6a line 635).** Quote: "(0′) the **invertible** anchor-monomial PAIR: weight-indexed ϕ_γ …". Under the leaf's own executed realization ϕ_γ = x_r^{s(γ)}p_r^{u(γ)} in the S2.2 carrier (localization at H(π), H(φ_k), k<r only — the REV-3/4 corrected claim), x_r is **not** a unit: fgmn.txt:1147 "All factors of x_i except for H_µ(φ_i) are units in Gr(µ_i)" and :1701 "x_r is associate in Gr(µ) to the **prime** element H_µ(φ_r)". So ϕ_γ is invertible only when s(γ) = 0 (e.g. ϕ_{h_i} = x_r is a non-unit); blanket invertibility would even force ȳ = x^{e}p^{−h} invertible, contradicting clause (0)'s Δ = K_i[ȳ] polynomial. Same slip inside (W-MULT)'s proof: "cancel the invertible ϕ_{γ+γ′}" — the cancellation is valid (gr is a domain, ϕ_{γ+γ′} ≠ 0; alternatively Thm 4.2's rank-one freeness), so the conclusion stands. No line of the leaf consumes ϕ_γ^{−1} (the S6.3 "invertible factor" is the genuine (KPa) unit u, a different element). Classification: **justification gap** — a false adjective in the leaf's precise clause display, unconsumed downstream, but exportable to a consumer that takes (0′) at its word.

**GAP-2 (S5.2, lines 423–425; S5.3 proof, lines 461–463).** Quote: "ϕ_{h_i} = x^{s(h_i)}p^{u(h_i)} = x (s(h_i) = ℓ_ih_i mod e_i = 1, u(h_i) = 0)" and D-REAL's "generator u := u₁ = [x]_{gauged}… u₁ is the class of the s = 1 anchor ϕ_{h_i} = x". At e_i = 1 (in scope: S1.1 allows e_j ≥ 1, and D-REAL states no e_i ≥ 2 hypothesis) these fail: s(h_i) = 0, u(h_i) = h_i, ϕ_{h_i} = p^{h_i} ≠ x, there is no s = 1 anchor, and u₁ = u₀ = 1 while [x]_gauged = z̄. The lemma's conclusion is trivially true there (both algebras = K_{i+1}, rank 1), so nothing downstream breaks; the identification sentences silently presuppose e_i ≥ 2. Classification: **justification gap** — unqualified degenerate case in statement-adjacent display and proof; conclusion unaffected.

Nothing critical: no discharged verdict rests on an open lemma, no quotation mis-transcribed, no scope claim beyond what the gates or proofs support, (H-CHAIN) and (TR-1-corr) both independently re-derived, D-REAL/D-ITER boundary exact. The two gaps above are the full residue of this pass; per the 2-clean bar they keep the clean counter at 0 until repaired or adjudicated non-findings.

VERDICT: GAPS-ONLY
