Audit basis: the supplied complete note and stated current sibling state; shell re-read was unavailable because the workspace launcher lacks bubblewrap. No edits made.

Six pass-5 folds:

1. “header gate summary stale” — CLOSED. Header says five passes, defers to the authoritative ledger, and explains batch-versus-ledger numbering.
2. “M1 seam stale” — CLOSED. M1 is consistently ACCEPTED; the D.10 stage package is walked, while only D.8 landing and D.10 RS-landing/lift-independence remain in the named residue.
3. “blanket M2 non-dependency” — CLOSED. Non-dependency is restricted to §§2–4; P3/P4 are explicitly unit attribution, not premises.
4. “M4 branch currency asserted” — CLOSED. Revision-4 precedence, branch-(i)/(iii) conditions, outcomes, and robustness scope are quoted consistently.
5. “P3 non-prediction ill-typed as a violation” — CLOSED. The trigger is exactly the GCD-ONLY ANOMALY Boolean, with run-wide P1/P2 success.
6. “P4 mis-derived and lacked disposition” — CLOSED. The derivation and P4-only failure handling are both explicit.

P4 walk: sound. At the δ>1 pool, P1 gives
`a_δ = δ·a_sym(ℓ_δ)`. The Theorem’s own δ=1 instance gives
`a₁ = a_sym(ℓ₁)`; this is correctly not called an instance of P1. The same-carrier persistence certificate supplies `ℓ_δ = ℓ₁`, so substitution yields `a_δ = δ·a₁`. Both equality inputs are on file in this note under the same §1.3 hypotheses. Persistence is properly an additional certificate, not claimed as an on-file universal theorem. The separate `AlphabetData.posLetter` bridge remains explicitly deferred. The P4-only disposition is also correct: it localizes failure to the δ=1 read, certificate, or carrier bridge and does not automatically declare an intrinsic-law violation.

M2 currency: Revision 3’s audit-tally fix does not invalidate the seam. The Revision-2 description is explicitly dated “read at fold,” predicts concurrent advancement, consumes no M2 clause, and requires re-reading only at the N-K3 fold. No N-K3 results section has yet been appended. The mathematical seam—factor correspondence, open DICT-0 status, and attribution-only use—therefore remains accurate; the old audit tally is historical, not asserted as Revision 3’s live tally.

Fresh sweep: no new unconditional claim, covert M2/R4/stability dependency, prematurely landed H4-F5 face, P3 trigger ambiguity, P4 composition error, seal disturbance, or ledger inconsistency found. This is the first consecutive clean on the pass-5-folded text.

VERDICT: CLEAN