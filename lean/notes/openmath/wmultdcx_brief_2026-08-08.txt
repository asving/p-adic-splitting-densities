=== CODEX PROVER BRIEF — THE (W-MULT-DCX-m) SECOND-ROUND CLAUSE ===
Provenance: p-adic-splitting-densities corpus @ commit 3c2ac60 (2026-08-08 campaign).
You are a fresh-context, independent mathematician. You have NO repository access:
every artifact you may use is PASTED VERBATIM below with a provenance pin. Do not
assume any unpasted corpus content; standard mathematics is of course available.

--- MATHEMATICAL GROUNDING (one paragraph) ---
The corpus develops an Okutsu–Montes/MacLane-style higher-order Newton-polygon
calculus over a complete DVR (O, π), residue field F: a tower of key polynomials
Φ_0, Φ_1, ..., Φ_n with per-level data (e_l, g_l, h_l, γ_l), level weights w_l,
graded reads R_{l,λ}, per-level residue units ε_l and letters z_l. The accepted
theorem (ITER-LAW-n) computes the top splitting invariant of a PRODUCT pair
val(P) = z̄^δ · val(φ_γ) · val(φ_{γ'}) by induction on the rung — proved OFF an
excluded locus DCX-(n) (per-level "second-round overflow" loci DCX_m). The single
displayed open family (W-MULT-DCX-m) is exactly: the multiplicative law ON the
DCX locus. All definitions are in the pastes (PASTE A sections S0–S4).

--- THE TASK: PROVER-FIRST ---
Attempt to PROVE the "engine second-round clause" — the statement (SRC-m) below.
This is the [ILN] S4.7 carry-over: "a proof of (RM-m) with a second-round clause
would close both II-legs and DCX at once." The first half — (RM-m), the engine —
is now ACCEPTED ([RMG], PASTE G): its four clauses hold for every m >= 1 on
T-scope(m) := d_0 = 1 OR (e_0 >= 2 AND e_i >= 2 for 1 <= i <= m-1). The second
half is THIS leg. Nobody has attacked it yet; it is the grade cap on the whole
Q2 program.

(SRC-m) — THE STATEMENT TO PROVE (assembled from ITERLAWN S4.6/S4.7, PASTE A;
notation: top rung n, engine level n-1; the family index m of "(W-MULT-DCX-m)"
corresponds to m = n-1, i.e. DCX_n = DCX_{m+1}):

  Setting = ITERLAWN S4.6 Cases II-2/II-3 ON DCX_n: g_{n-1} = 1 tower,
  E_1^IH != 0, the two engine calls E_0^IH·C-hat = R^0 + Q^0·Phi and
  E_1^IH·C-hat = R^1 + Q^1·Phi (Phi := Phi_{n-1}, C-hat := C-hat_{n-1}),
  with Q^1 != 0 landing at Phi_{n-1}-slot k+2 = e_{n-1} (Case II-2, where
  k := T_n - e_{n-1}) or at slot 1 >= e_{n-1} = 1 (Case II-3). The one-round
  development displayed in S4.6 therefore does NOT close: the Q^1 term
  overflows the digit range. Fire the second substitution round
  Phi_{n-1}^{e_{n-1}} = Phi_n - C-hat_{n-1} on the overflowing term (and any
  induced further rounds). TO PROVE:

  (a) [E_0 read survives] After the cascade closes, w_n(E_0^new) = B_n and the
      level-n read at B_n is unchanged: R_nv(E_0^new) = z_n^{delta_n^pl} · v^0 ·
      eps_{n-1}(B_{n-1}) — i.e. every second-round contribution entering the
      slot-T_{n+1} digit either sits strictly above the line B_n at its slot,
      or is off-grid, or its on-line residue contributions cancel exactly.
  (b) [top slot] The -Q^1 joining the slot-(T_{n+1}+1) digit (E_1^new) keeps
      that slot's line >= Gamma'' + h_n, or contributes read 0 at Gamma''.
  (c) [II-3 cascade termination] At e_{n-1} = 1 the induced cascade
      (Q^1·C-hat = R^2 + Q^2·Phi, Q^2 re-overflowing, ...) terminates, and its
      total contribution obeys (a).
  (d) Conclude val(P) = z̄^delta · val(phi_gamma) · val(phi_gamma') AND the
      (P-DS) exports of S4.6 hold ON DCX_n — closing the Case-II legs on DCX,
      i.e. (W-MULT-DCX-m).

  NOTE (orientation sketch, MINE, not corpus text — re-derive or correct it):
  in II-2, -Q^1 Phi^{e_{n-1}} · Phi_n^{T_{n+1}} = -Q^1 Phi_n^{T_{n+1}+1}
  + Q^1 C-hat · Phi_n^{T_{n+1}}; the re-entrant part Q^1·C-hat is then engine-
  divided (R^2 at slot 0, Q^2 at slot 1), closing in one extra round when
  e_{n-1} >= 2. The expected hard core is the READ layer: on DCX the weight
  bounds can be TIGHT (that is what makes DCX sharp — the corpus calls the
  missing layer "DEV-at-depth", the depth->=1 analogue of the engine's (C-READ);
  see PASTE C, S9). Where a second-round contribution can land ON the line,
  you must compute its residue read and show it vanishes, cancels, or combines
  consistently with the displayed C_0. The corpus suggests the class-level
  carry algebra F[u]/(u^e - z̄) as the natural language. Also available: the
  weight floor W-D5 (PASTE C): w_m(Q^1) >= lambda-hat + max(k,0)·h_{m-1}, and
  the measured fact that on live rows the minimal nonzero part depth is
  EXACTLY k (286/286) — Q^1's weight floor is attained on the cascade stratum.

ACCEPTABLE OUTCOMES, in decreasing value (state which you achieved, FIRST LINE):
  TIER-0: (SRC-m) proved generically (T-scope pins allowed where the engine is
     consumed), with LAW-DCX-(m+1) (PASTE B, S6.1) allowed as an EXPLICIT
     HYPOTHESIS wherever residue-survival content is needed. This converts the
     conditional display COND-B into the proved implication
     "LAW-DCX-(m+1) => (W-MULT-DCX-m)" — the chartered prize.
  TIER-1: (SRC-m) proved on the pinned class (H1)-(H3) AND g_0 = 1 AND d_0 = 1
     AND (STK) AND (RW) — where LAW-DCX-(m+1) itself already holds at the
     corpus's capped attempt grade (PASTE F). Name every pin you consume.
  TIER-2: (SRC-m) proved at m = 2 (rung n = 3) only — still new (only the
     m = 1 instance, [IL3] S4.7 "DEV-2", is proved anywhere).
  TIER-3 (honest fallback): NAME THE EXACT OBSTRUCTION — the minimal missing
     sub-lemma as a displayed formal statement, why each pasted tool fails to
     reach it, and a falsifier design (what instance computation would refute
     or support it).

--- RULES (all binding) ---
1. RIGOR IS PARAMOUNT. Every step justified; no "routine"/"clearly"; a
   quantified claim needs a general argument, not instances. Display every
   weight/line/slot computation WITHOUT ellipses.
2. LABEL every step: PROVED-HERE / CONSUMED (name + grade + scope, from the
   pastes) / ASSUMED (explicit hypothesis). Grades you may consume: accepted
   ([IL]/[IL3]/[ILN]/[RMG] = daggered), attempt-grade (WMULTDCX W-D0..W-D7,
   PID+, E-THM, S-THM, SD-THM-A — cite as "attempt grade, capped"), sealed
   conjecture (LAW-DCX-(m+1) at m >= 2 — hypothesis only, never a proof step
   unless TIER-0's explicit-hypothesis slot).
3. ADVERSARIAL DUTY: if a pasted claimed-proved step that you must consume is
   WRONG, do not use it — quote the offending passage verbatim and classify it
   (CRITICAL breaks the chain / GAP assume-and-continue). Fix nothing in the
   pasted texts.
4. HONESTY ABOUT COMPLETENESS: if you cannot fully prove a tier, say so and
   present only the rigorously proven partial results, with unproven steps
   boxed as explicit open lemmas. A partial derivation with an honest box
   beats a papered-over "proof". Any claim of the form "X cannot be proved
   from the pasted tools" must itself be argued.
5. INSTANCE FACTS quoted below (132/132 at m = 2, 10,484 law-keyed stress rows,
   286/286 minimal-depth, 126/126 zero-cell dead rows) are EVIDENCE, never
   proof steps.
6. OUTPUT: markdown. FIRST LINE = "VERDICT: TIER-{0|1|2|3} — <one sentence>".
   Then the derivation/obstruction in full. Keep it under ~1500 lines. Number
   every lemma you introduce (SRC-L1, SRC-L2, ...). End with a dependency list
   (what you consumed, at what grade) — no recap prose.

--- CURRENT STATE SUMMARY (orchestrator-written; consumables are in the pastes) ---
* [RMG] ACCEPTED: the four (RM-m) engine clauses — weight-R, weight-Q,
  R-read = z_m^{eps'}·v·tau_m with v != 0 forced, eps'-carry — every m >= 1 on
  T-scope(m). PASTE G has the exact block; PASTE B has the note's S0/S5-S8.
* LAW-DCX-(m+1) (the second-round LOCUS law, PASTE B S6.1): m = 1 PROVED
  ([IL3]); m = 2 sealed-confirmed conjecture; m >= 3 conjecture with first
  instance data. It holds AT ATTEMPT GRADE (capped) on the pinned class
  (H1)-(H3) AND g_0 = 1 AND d_0 = 1 AND (STK) AND (RW) via E-THM (deadness:
  zero cells when c_{m-1} < k, g_0-free — PASTE D) + SD-THM-A/S-THM (survival:
  cascade slot survives when c_{m-1} >= k — PASTES E/F).
* PID+ (PASTE C, S8): the corrected pool identification — every Q^1 cell
  carries depth pi_{m-1} >= k; the cascade cell attains the floor on the
  dump-free principal subtree under (STK) + the law clause.
* The residue program at g_0 >= 2 (K2 genre) is summary-grade here (K2-THM-B
  on (R0)^(R2)^(B4-scope)^(R4+); residual K2-RES-B) — do NOT consume it; if
  your proof needs g_0 >= 2 survival input, that is a named pin or a TIER drop.
* (W-MULT-DCX-m) instance evidence (never proof): instance-true 132/132 at
  m = 2 ([IL3] roster-B + its diag's 16 DCX pairs / 0 violations); rung-3
  sealed 4-read battery strata; rung-4 T5B: 16 sharp-DCX_2 pairs per instance,
  law exact on ALL; the W-arc stress battery 0/10,484 law-keyed.
* Hypothesis dictionary used above (defined in PASTE C S1, PASTE F): (H1) =
  T-scope pin at the called level; (H2) = g_{m-1} = 1; (H3) = interior g == 1;
  (STK) = stage-1 stock; (RW) = every terminal cell top-reduced
  (j_{m-1} <= P_{m-1} - 1); k := 2e_{m-1} - D where D = (s_m + s_m') + s_m(u_m)
  is the top pool without carry; TRACK := (s_{m+1} = s_{m+1}' = e_m - 1) AND
  E_1^IH != 0.

The pastes follow. PASTE A = the accepted all-orders note (setting, statement,
integer layer, EXP-KIT, REALIZE/WELL-DEF/READ-ADD, L6, the induction package,
CASE-SPLIT, Case I, THE ENGINE BOX + Cases II-1/2/3 (the proofs you must extend
onto DCX), DCX-(n) itself, and the S5 assembly). PASTE B = the engine note's
setting + statement + line/z-kernel calculus + DEV-(m) + THEOREM RM-GEN + the
second-round-clause section (S6) + consequences + claims ledger. PASTE C = the
W-MULT-DCX composer note IN FULL (the two-stage tree computing Q^1, the path
ledger, thresholds, PID+, the (SURV-(m+1)) box with the DEV-at-depth spec).
PASTES D/E/F = the survival-side theorem statements you may consume at attempt
grade. PASTE G = the [RMG] acceptance block.


================================================================
=== PASTE A: ITERLAWN_PROOF_2026-08-08.md ([ILN], ACCEPTED), lines 142-1076 @ 3c2ac60 ===
================================================================
## S0. Setting, statement, status

### S0.1 The (n+1)-read standard-lift harness class (the class pin)

O a DVR with uniformizer π, residue field k₀, valuation w₀ (coefficientwise on
O[x]; harness instances ℤ₂, ℤ₃, F₂[[t]], F₃[[t]], the proof ring-blind). Fix
n ≥ 1 and tower data with reads j = 0, …, n:

* Φ₀ ∈ O[x] monic, deg Φ₀ = d₀ ≥ 1, w₀(Φ₀) = 0, reduction irreducible;
  K₀ := k₀[X]/(φ̄); res and lift as in [IL S0] (ACCEPTED; package pasted in
  the header: harness tower class, standard-lift, strict superset of §S2's
  E₂ > 1 pin, all three residuals).
* Read j (0 ≤ j ≤ n): e_j ≥ 1, h_j ≥ 1, gcd(e_j, h_j) = 1; ψ_j ∈ K_j[y]
  monic irreducible, deg ψ_j = g_j ≥ 1, ψ_j(0) ≠ 0;
  K_{j+1} := K_j[z_{j+1}]/(ψ_j). Letters: z₁, …, z_n and the top letter
  z̄ := z_{n+1}. Normalized Bézout pairs (ℓ_j, ℓ_j′): ℓ_jh_j + ℓ_j′e_j = 1,
  0 ≤ ℓ_j < e_j (ℓ_j = 0, ℓ_j′ = 1 at e_j = 1).
* Weights, with the UNIFORMIZING CONVENTION γ₁ := h₀ (so every level speaks
  one language): γ_{j+1} := e_j·w_jΦ_j + h_j and w_{j+1}Φ_{j+1} :=
  e_jg_jγ_{j+1} for j ≥ 1; w₁Φ₁ := e₀g₀h₀ = e₀g₀γ₁ (so w_jΦ_j =
  e_{j−1}g_{j−1}γ_j for ALL j ≥ 1).
* Lifts: Φ₁ := the STANDARD LIFT of ψ₀ (grb_order2_check :499–507); for
  1 ≤ m ≤ n−1, Φ_{m+1} := the CANONICAL ε_m-CORRECTED LIFT of ψ_m:
  Φ_{m+1} = Σ_{k ≤ g_m, ψ_{m,k} ≠ 0} C_k·Φ_m^{e_mk}, C_{g_m} = 1,
  C_k := realize_m(ε_m((g_m−k)γ_{m+1})^{−1}·ψ_{m,k}, (g_m−k)γ_{m+1}) for
  k < g_m — the build_Phi2/build_Phi3/build_Phi4 pattern verbatim, one level
  up each time (well-definedness on the class: Lemma WELL-DEF-(n), S4.1).
  The top ψ_n needs no lift (the cocycle never consumes Φ_{n+1}).
* E_{n+1} := e₀e₁⋯e_n. E-positivity: every e_j ≥ 1 individually, NO joint
  floor (the erratum lesson of [IL] — ACCEPTED 2026-08-03 on the harness
  tower class, standard-lift, strict superset of DITER_RESTATE §S2's
  E₂ = e₀e₁ > 1 pin, on-the-nose at E₂ > 1, residuals (ITER-LAW-LIFT) /
  gr(w₂)-wrapper / orders ≥ 3 in force — applied proactively as in [IL3]
  S0.1).

The (ITER-LAW-LIFT) residual propagates verbatim to every level: nothing is
claimed for any non-canonical lawful lift at any level.

### S0.2 Calculus, splits, anchors, cocycle

The calculus is the iterated harness read, defined once per level by the
mirror recursion (verbatim semantics of the pinned code, as in [IL3] S0.2):
ε_m(β) := z_m^{ℓ_{m−1}′s − ℓ_{m−1}u} with (s,u) := eq12(β; e_{m−1}, h_{m−1})
(reading C at every level; ε₁ = harness eps1, ε₂ = eps2, ε₃ = eps3, ε₄ = the
diagnostic's eps4); w_{m+1}(f) := min_j{e_m·w_m(A_j) + j·γ_{m+1}} over the
Φ_m-development f = Σ A_jΦ_m^j; the level-(m+1) read at γ := w_{m+1}(f) has
grid s_{m+1}(γ) + je_m, slot weights γ_j := (γ − (s_{m+1}+je_m)γ_{m+1})/e_m,
coefficients C_j := ε_m(γ_j)·R_mv(A_{s_{m+1}+je_m}) when the digit is on-line
(w_m = γ_j exactly), else 0; R_{m+1}v(f) := Σ_j C_j·z_{m+1}^j ∈ K_{m+1};
**val(f) := R_{n+1}v(f) ∈ K_{n+1}** (harness val₃/val₄/val₅ at n = 2, 3, 4).
Base facts consumed (as in [IL S1]; ACCEPTED, package pasted in the header —
harness tower class, standard-lift, superset of §S2's E₂ > 1 pin, three
residuals): division algorithm in O[x] (Φ monic ⟹ unique developments), w₀ a
valuation, res additive and vanishing on strict π-excess, q: O[x] → K₀ a ring
homomorphism.

**The iterated eq-(12-int) split.** For γ ∈ ℤ:

    (s_{n+1}, u_{n+1}) := eq12(γ; e_n, h_n);   β_n(γ) := u_{n+1} − s_{n+1}·w_nΦ_n
    for j = n, …, 2:  (s_j, u_j) := eq12(β_j; e_{j−1}, h_{j−1});
                       β_{j−1}(γ) := u_j − s_j·w_{j−1}Φ_{j−1}
    (s₁, u₁) := eq12(β₁; e₀, h₀)
    [so γ = e_nβ_n + s_{n+1}γ_{n+1} and β_j = e_{j−1}β_{j−1} + s_jγ_j for all j]

Anchor φ_γ := π^{u₁}·Φ₀^{s₁}·Φ₁^{s₂}·⋯·Φ_n^{s_{n+1}}; window
W_{n+1} := {γ : u₁(γ) ≥ 0}; cocycle on anchors, for γ, γ′ ∈ W_{n+1}
(γ+γ′ ∈ W_{n+1} by WINDOW-(n), S2):

    c_{n+1}(γ,γ′) := val(φ_γ·φ_{γ′}) / val(φ_{γ+γ′}).

Pair abbreviations (fixed throughout): s_j := s_j(γ), s_j′ := s_j(γ′),
s_j″ := s_j(γ+γ′); B_j := β_j(γ)+β_j(γ′), β_j″ := β_j(γ+γ′); the s_j-defect
:= s_j + s_j′ − s_j″; plain carries δ_j^{pl} := ⌊(s_j+s_j′)/e_{j−1}⌋ ∈ {0,1}
(j = 1..n+1; the TOP one is δ := δ_{n+1}^{pl}); Γ″ := γ+γ′.

### S0.3 THEOREM (ITER-LAW-n) — the statement, verbatim the recursion of record

**THEOREM (ITER-LAW-n; the L3CONVERGENCE S2 Candidate-2 recursion, verbatim;
= ITERLAWR_PROBE S6 item 1; unrolled instances: the [IL] display at n = 1
(ACCEPTED; package pasted in the header — harness tower class, standard-lift,
strict superset of §S2's E₂ = e₀e₁ > 1 pin, on-the-nose restricted to E₂ > 1,
three residuals in force), the [IL3] display at n = 2 (attempt grade 0/2), the
sealed ITERLAW4 display at n = 3).** For all γ, γ′ ∈ W_{n+1}, with

    δ := ⌊(s_{n+1}(γ) + s_{n+1}(γ′))/e_n⌋ ∈ {0,1}          [top carry]
    S_n := δ·γ_{n+1};                                       [top shift]
    for j = n, …, 1:
      D_j     := (s_j-defect + ℓ_{j−1}·S_j)/e_{j−1} ∈ ℤ     [level-j exponent]
      S_{j−1} := D_j·γ_j + S_j·(ℓ_{j−1}′ − ℓ_{j−1}·w_{j−1}Φ_{j−1})
                                                  [composed shift; S₀ unused]

it holds that

    c_{n+1}(γ,γ′) = z̄^{δ} · z_n^{D_n} · z_{n−1}^{D_{n−1}} · ⋯ · z₁^{D_1}.

(Convention at j = 1: w₀Φ₀ := 0 and γ₁ = h₀, so S₀ = D₁h₀ + S₁ℓ₀′ — computed
by the recursion but consumed by nothing.)

**STATUS (the exact grade, per clause and per rung — the point of this
note).** The theorem is proved BY INDUCTION ON THE RUNG with the level-generic
step of S4, on the following honest perimeter:

* **Excluded locus:** DCX-(n) := ∪_{m=2}^{n} DCX_m, the per-level
  double-cascade corners (S4.7: outer bound proved generically; sharp form
  proved at m = 2 = the [IL3] corner; DCX-(1) = ∅). On DCX-(n) the theorem is
  conjectural (instance-true at every probed instance, all rungs).
  **[Errata r1, 2026-08-03, PE-G1: "outer bound proved generically" reads
  "proved generically (given IH package)" — the S4.7 proof consumes the IH
  (P-DS) clause; see the S4.7 erratum bracket.]**
* **Conditionality off DCX-(n):** rung 1 UNCONDITIONAL (= [IL], ACCEPTED
  2026-08-03 on the harness tower class — standard-lift, strict superset of
  DITER_RESTATE §S2's E₂ = e₀e₁ > 1 pin, on-the-nose at E₂ > 1; residuals
  (ITER-LAW-LIFT) at g₀ = 1 ∧ δ₁ = 1 / gr(w₂) wrapper rides the accepted GRB
  retarget / orders ≥ 3 untouched). Rung 2 = [IL3] at attempt grade (counter 0/2;
  round 2 completed not-clean on narrow display/naming findings, errata
  round 2 → round 3 owed). Rung n ≥ 3: conditional on (i) the [IL3] rung-2 package and
  (ii) the OPEN ENGINE FAMILY (RM-m) for 2 ≤ m ≤ n−1 (S4.6) — the one leg of
  the generic step that resists; every other leg of the inductive step is
  proved level-generically in this note.
* **Unconditional at every rung and on ALL pairs (DCX included):** the entire
  integer layer — the shift cascade β_j″ = B_j + S_j (all j), D_j-integrality
  (all j), the (★)-positivity of the shift chain, WINDOW-(n), the slotwise
  cocycle identity of the displayed constants, ℤ/E_{n+1}-descent of all
  constants, and the degeneration collapses at integer level (S2, S6).

| clause | grade | where |
|---|---|---|
| L1 EQ12-ADD at a general read | ACCEPTED ([IL S2 L1] — harness tower class, standard-lift, strict superset of §S2's E₂ > 1 pin, three residuals in force) | S2 |
| SHIFT-(n): β_j″ = B_j + S_j for j = n..1 | PROVED, generic, unconditional | S2 |
| D_j-INT for all j | PROVED, generic, unconditional | S2 |
| (★)-POS: S_j = 0 or e_jS_j ≥ γ_{j+1} | PROVED, generic, unconditional (NEW display) | S2 |
| WINDOW-(n) | PROVED, generic, unconditional | S2 |
| EXP-KIT (a)/(b)/(c), generic in the level | PROVED (re-displayed self-contained) | S3 |
| TRUNC (the truncation dictionary) | PROVED, generic | S4.0 |
| REALIZE-(m) + WELL-DEF-(n) (canonical lifts exist; Ĉ-clauses) | PROVED, generic (incl. the (I_m) descent invariant — NEW) | S4.1 |
| READ-ADD-(m) (additivity + strict-above kill) | PROVED, generic | S4.1 |
| L6-(n) ANCHOR-VAL = ε-chain, all n | PROVED given only the [IL] base (ACCEPTED on the harness tower class, standard-lift, superset of §S2's E₂ > 1 pin, three residuals; no RM, no DCX) **[attribution (errata r4, 2026-08-03; PE4-F1): + S4.1 at rungs ≥ 2 — the canonical-lift certification (WELL-DEF-(n)/(R1) + C_k existence) consumed via the S0.1 class pin and the S4.2 degree-telescope input; S4.1's m = 1 base rides [IL3]†; rung 1 given [IL] alone]** | S4.2 |
| the induction package (ITER-PKG-r) | STATED; base r = 1 ACCEPTED, r = 2 = [IL3] 0/2 | S4.3 |
| CASE-SPLIT-(n) | PROVED, generic (given IH package) | S4.4 |
| W-MULT-(n) Case I | PROVED, generic (given IH package) | S4.5 |
| (RM-m) engine: m = 1 | PROVED via [IL3] (e₀ ≥ 2; + e₀ = 1 ∧ d₀ = 1; corner inherited) | S4.6 |
| (RM-m) engine: 2 ≤ m ≤ n−1 | **OPEN, priced** (THE resisting leg) | S4.6 |
| W-MULT-(n) Cases II-1/II-2/II-3, off DCX | PROVED generic MODULO (RM-(n−1)) | S4.6 |
| DCX-(n): outer bound per level | PROVED, generic **(given IH package — errata r1, PE-G1)** | S4.7 |
| DCX-(n): sharp form + emptiness | m = 2 proved ([IL3]); m ≥ 3 engine-dependent, conjectured e_{m−2} ≥ 3 | S4.7 |
| W-MULT on DCX-(n) | **OPEN** ((W-MULT-DCX-m) family), instance-true | S4.7 |
| THE THEOREM, off DCX | proved modulo the S7 ledger (per-rung conditionality above) | S5 |
| COR-1..COR-5 (with OFF-DCX qualifiers baked in) | S6 | S6 |
| (INV-n) class, gr(w) wrapper, GMN, general lifts, Lean | NOT TOUCHED (fences) | S9 |

## S1. The induction, architecturally (what is generic, what resists, and why)

The L3CONVERGENCE S3 strategy at general rung, executed: the induction carrier
is the ε-chain gauge. The induction hypothesis is a PACKAGE (ITER-PKG-r,
S4.3): not the cocycle law alone but the three exports the next level
consumes — (P-L6) anchor value = ε-chain with its weight clause, (P-WM) the
pair value with the single-top-slot shape, and (P-DS) the pair product's
digit shapes (weights + reads + degree bounds), certified well enough for the
next level's correction products. The step rung r → rung r+1:

1. **L6-(r+1)**: the anchor's top digit IS the truncated tower's anchor
   (TRUNC); one degree telescope + the IH (P-L6). Fully generic; the L6 chain
   never consumes W-MULT, RM, or any DCX exclusion. **[Attribution (errata
   r4, 2026-08-03; PE4-F1): the telescope's input deg Φ_{j+1} =
   e_jg_j·deg Φ_j is an S4.1 export (REALIZE-(m)'s (R1) degree bound + the
   WELL-DEF(i) existence of the C_k), so the honest ingredient list is
   TRUNC + S4.1 + telescope + IH (P-L6). The negative clause stands
   literally true — S4.1 is none of W-MULT/RM/DCX.]**
2. **W-MULT-(r+1)**: the pair product's top digit A IS the truncated tower's
   anchor-pair product, so the IH (P-WM)/(P-DS) computes it — "L7 stacked on
   L7", now as the literal induction mechanism. Case I (no Φ_{r+1}-overflow)
   is pure slot-grid absorption: generic, proved. Cases II-* (top overflow at
   g_r = 1) substitute Φ_r^{e_r} = Φ_{r+1} − Ĉ_r once and must re-develop the
   products (IH digit)·Ĉ_r at level r — THE one genuinely new object per
   level. At r = 1 the inputs are monomials and [IL3] hand-built the engine
   (DIGIT-SPLIT + DEV-1). At r ≥ 2 the inputs are the IH's E₀/E₁ exports
   (reduction normal forms, not monomials), and the required engine is the
   displayed OPEN family (RM-m): product-with-correction re-development with
   read-transparency and the ε-carry factoring (S4.6). Everything AROUND the
   engine call — the slot bookkeeping, the line tables, the ε′-cancellation
   against the canonical lift, the DS-export propagation, the DCX exclusion —
   is proved level-generically here.
3. **The theorem** is then division plus ONE EXP-KIT(c) kernel per level with
   shift T = S_j (the proved composed shifts), exactly as at rungs 1–2: no
   cross-level denominators ever form.

**Why the engine is the honest boundary:** rung 2's DEV-1 induction already
carried a sharp corner (the e₀ = 1 measure erratum) and its DCX corner is
priced as a DEV-2 unit ([IL3] S4.7). The generic engine is that unit stated
at every level — its termination problem (two substitution identities per
level, the e-descent failure mode recurring at each) is a genuine dedicated
unit, not a paragraph. Attempting it inline would produce exactly the kind of
unproved "routine" step this campaign's verifiers exist to catch. So (RM-m)
is stated exactly, priced, and consumed visibly; the theorem's grade at each
rung is read off the ledger (S7).

**Consumption manifest:** [IL] (ACCEPTED 2026-08-03; rule-8 package, pasted:
"Scope as accepted: the harness tower class (superset of DITER_RESTATE §S2's
E₂ > 1 pin; on-the-nose coincidence restricted to E₂ > 1); residuals:
(ITER-LAW-LIFT) open exactly at g₀ = 1 ∧ δ₁ = 1; gr(w₂) wrapper rides the
accepted GRB retarget; orders ≥ 3 untouched" — standard-lift, strict superset
of the pinned E₂ = e₀e₁ > 1 setting): consumed as the r = 1 package (L1, L6,
L7 + displayed developments, EXP instances). [IL3] (attempt grade 0/2; round
2 completed not-clean on narrow findings, errata round 2 → round 3 owed):
consumed as the r = 2 package + DEV-1/DIGIT-SPLIT (= RM-1) +
EXP-KIT (re-displayed below) + WELL-DEF(i)-(ii) (generalized below). Sealed
artifacts (ITERLAWR, ITERLAW4_PROBE verdicts, the method notes): INSTANCE
EVIDENCE ONLY, never proof steps.

## S2. The integer layer (proved, generic, unconditional — all n, ALL pairs)

**Lemma L1 (EQ12-ADD; [IL S2 L1] — ACCEPTED; rule-8 package as pasted in the
header: harness tower class, standard-lift, strict superset of §S2's
E₂ > 1 pin, on-the-nose at E₂ > 1, all three residuals in force).** For a
read (e,h), gcd(e,h) = 1, and β, β′ ∈ ℤ with eq-(12-int) data (s,u), (s′,u′),
(s″,u″) at β, β′, β+β′, δ := ⌊(s+s′)/e⌋: δ ∈ {0,1}, s″ = s+s′−δe,
u″ = u+u′+δh. ∎ (Consumed; the two-line proof is in [IL S2].)

Throughout S2, γ, γ′ ∈ ℤ are arbitrary (no window hypothesis needed except in
WINDOW-(n) itself).

**Lemma SHIFT-(n) (the composed-shift cascade).** For j = n, …, 1:
β_j″ = B_j + S_j, with S_j as in the S0.3 recursion (S_n = δγ_{n+1}).

*Proof.* Downward induction on j. **Top (j = n):** L1 at (e_n, h_n):
s_{n+1}″ = s_{n+1}+s_{n+1}′−δe_n, u_{n+1}″ = u_{n+1}+u_{n+1}′+δh_n; so
β_n″ = u_{n+1}″ − s_{n+1}″w_nΦ_n = B_n + δ(e_nw_nΦ_n + h_n) = B_n + δγ_{n+1}
= B_n + S_n. **Step (j → j−1, j ≥ 2):** assume β_j″ = B_j + S_j. From
β_j = e_{j−1}β_{j−1} + s_jγ_j at all three arguments:

    e_{j−1}(β_{j−1}″ − B_{j−1}) = (β_j″ − B_j) + (s_j-defect)·γ_j
                                = S_j + (s_j-defect)·γ_j.            (S-rec)

By D_j-INT below (whose proof needs only β_j″ = B_j + S_j, already in hand at
this j), s_j-defect = e_{j−1}D_j − ℓ_{j−1}S_j, so the right side is
e_{j−1}D_jγ_j + S_j(1 − ℓ_{j−1}γ_j), and 1 − ℓ_{j−1}γ_j =
1 − ℓ_{j−1}h_{j−1} − ℓ_{j−1}e_{j−1}w_{j−1}Φ_{j−1} =
e_{j−1}(ℓ_{j−1}′ − ℓ_{j−1}w_{j−1}Φ_{j−1}) by the Bézout identity. Divide by
e_{j−1}: β_{j−1}″ = B_{j−1} + D_jγ_j + S_j(ℓ_{j−1}′ − ℓ_{j−1}w_{j−1}Φ_{j−1})
= B_{j−1} + S_{j−1}. ∎

**Lemma D_j-INT (integrality, every level).** For j = n, …, 1:
e_{j−1} | (s_j-defect + ℓ_{j−1}S_j); hence D_j ∈ ℤ.
*Proof.* s(β) ≡ ℓβ (mod e) for eq-(12-int) data at any read (from ue + sh = β
and ℓh ≡ 1 (mod e)). So, using β_j″ = B_j + S_j (SHIFT at level j):
s_j-defect + ℓ_{j−1}S_j ≡ ℓ_{j−1}(β_j + β_j′ − β_j″ + S_j) = 0
(mod e_{j−1}). (Trivial at e_{j−1} = 1.) ∎

(The two lemmas interleave one level at a time, top-down: SHIFT at j gives
D_j-INT at j gives SHIFT at j−1 — no circularity. This IS the sealed
recursion's integer content: the "realized shift" β_j″ − B_j and the closed
form S_j are ONE object at every level, the G8 identity family of the rung-3
battery, now a theorem for all n.)

**Lemma (★)-POS (the shift chain is positive-or-zero, with a uniform gap —
NEW generic display, the n-level form of [IL3] L3-(3)'s case analysis).**
For j = n, …, 1: S_j ≥ 0, and

    (★_j)   S_j = 0   or   e_j·S_j ≥ γ_{j+1}.

*Proof.* Downward induction. j = n: S_n ∈ {0, γ_{n+1}} and e_nγ_{n+1} ≥
γ_{n+1}. Step (★_j) ⟹ (★_{j−1}), j ≥ 2, using (S-rec):
**Restart case S_j = 0:** then β_j″ = B_j, L1 gives s_j-defect =
δ_j^{pl}e_{j−1}, so D_j = δ_j^{pl} ∈ {0,1} and S_{j−1} = δ_j^{pl}γ_j ∈
{0, γ_j}; e_{j−1}γ_j ≥ γ_j. ✓ **Active case S_j > 0:** s_j-defect ≥
−(e_{j−1}−1), so by (S-rec) and (★_j), using γ_{j+1} = e_je_{j−1}g_{j−1}γ_j
+ h_j:

    e_{j−1}S_{j−1} = S_j + (s_j-defect)γ_j
                   ≥ γ_{j+1}/e_j − (e_{j−1}−1)γ_j
                   = e_{j−1}g_{j−1}γ_j + h_j/e_j − (e_{j−1}−1)γ_j
                   = (g_{j−1}−1)e_{j−1}γ_j + γ_j + h_j/e_j  >  γ_j.

So S_{j−1} > 0 and e_{j−1}S_{j−1} > γ_j ≥ γ_j: (★_{j−1}) holds (and once the
chain is active it stays active). ∎
(Diagnostic: (★_j) verified at every pair, 4 reads and 5 reads — S8.)
**[Note (errata r1, 2026-08-03; PE-N2, no grade impact): the nonzero-branch
bound e_jS_j ≥ γ_{j+1} is never TIGHT on any probed data (equality attained
0 times in 3,444 diag + 3,444 PE-reimpl checks; equality would force
e_j = 1 with carry 1, which the e = 1 grid kills) — correct as displayed
(≥ holds); a sharper display is available if ever needed.]** **[Count
correction (errata r2, 2026-08-03; PE2-N1): the diag-side figure above is
mislabeled — the diag's (★) coverage is 1,008 counter rows / 3,568
pair-levels (3,444 is the PE-reimpl's own check count, correct as stated
for that leg); the never-tight observation and the mechanism derivation
stand unchanged on both legs.]**

**Lemma WINDOW-(n).** γ, γ′ ∈ W_{n+1} ⟹ γ+γ′ ∈ W_{n+1}.
*Proof.* e₀u(β₁″) = β₁″ − s₁″h₀ = e₀(u₁+u₁′) + (s₁-defect)h₀ + S₁ (by SHIFT
at j = 1 and the defining equations). If S₁ = 0: u(β₁″) = u₁+u₁′+δ₁^{pl}h₀ ≥
0 by L1. If S₁ > 0: by (★₁), e₁S₁ ≥ γ₂ = e₁e₀g₀h₀ + h₁ > e₁(e₀−1)h₀, so
S₁ > (e₀−1)h₀ ≥ −(s₁-defect)·h₀'s worst case, giving e₀u(β₁″) > 0. ∎

**Slotwise cocycle identity (unconditional; consumed by COR-2).** For
a, b, t ∈ ℤ the displayed constants satisfy the additive 2-cocycle identity
at every slot: δ-slot by L1 at (e_n,h_n) (associate both ways:
e_n[δ(a,b)+δ(a+b,t)] = s_{n+1}(a)+s_{n+1}(b)+s_{n+1}(t)−s_{n+1}(a+b+t) =
e_n[δ(b,t)+δ(a,b+t)]); then downward induction on j: S_n-totals are
γ_{n+1}·(δ-totals), equal; if the S_j-totals of the two associations agree
then the D_j-totals agree ((s_j-defect)-sums telescope to the symmetric
s_j(a)+s_j(b)+s_j(t)−s_j(a+b+t) in both associations, and ℓ_{j−1}(S_j-total)
agrees), hence the S_{j−1}-totals agree. ∎

**ℤ/E_{n+1}-descent of the constants (unconditional; consumed by COR-4).**
γ ↦ γ+E_{n+1} fixes s_{n+1} (E_{n+1} ≡ 0 mod e_n) and shifts β_n by
Π_n := e₀⋯e_{n−1}; downward, it fixes every s_j (Π_j := e₀⋯e_{j−1} ≡ 0 mod
e_{j−1}) and shifts β_{j−1} by Π_{j−1}; at the bottom it shifts u₁ by 1. So
φ_{γ+E_{n+1}} = π·φ_γ exactly, and every constant of the display (δ, all D_j,
all S_j) depends only on (γ mod E_{n+1}, γ′ mod E_{n+1}). ∎

## S3. EXP-KIT (the per-level Bézout kernel; re-displayed self-contained)

**Lemma EXP-KIT.** Fix a read (e,h), gcd(e,h) = 1, its normalized Bézout pair
(ℓ,ℓ′), a field K with a unit z ∈ K^×, and set ε(β) := z^{ℓ′s(β) − ℓu(β)} on
the eq-(12-int) data of (e,h). Then:

(a) **EPS-CLOSED**: (s(β) − ℓβ)/e ∈ ℤ and ε(β) = z^{(s(β)−ℓβ)/e}.
*Proof.* ℓβ = ℓeu(β) + (1−ℓ′e)s(β), so s(β) − ℓβ = e(ℓ′s(β) − ℓu(β)). ∎
(At e = 1: ℓ = 0, s ≡ 0, ε ≡ 1.)

(b) **EPS-MULT**: ε(β)ε(β′) = z^{δ}·ε(β+β′), δ := ⌊(s(β)+s(β′))/e⌋.
*Proof.* Exponents via (a); substitute s(β+β′) = s(β)+s(β′)−δe (L1). ∎

(c) **EXP-SHIFT**: for any T ∈ ℤ and β″ := β+β′+T, with δ as in (b):

    z^{δ} · ε(β+β′)/ε(β″) = z^{D},   D = (s(β)+s(β′) − s(β″) + ℓT)/e ∈ ℤ.

*Proof.* By (a) the left exponent is δ + [(s(β+β′)−ℓ(β+β′)) − (s(β″)−ℓβ″)]/e;
substitute L1 and ℓ(β″−β−β′) = ℓT; the δ-terms cancel. Integrality: δ plus a
difference of integers by (a) twice. ∎

(This is [IL3] S3 verbatim — already generic in the level there; re-displayed
with proof so that this note's integer/character layers are self-contained
modulo the ACCEPTED [IL] alone ([IL S3 L4/L5/EXP] are (a)/(b)/(c) at level 1;
[IL] package per the header paste: harness tower class, standard-lift, strict
superset of §S2's E₂ > 1 pin, three residuals). Instantiations used in S5:
level j with (e_{j−1}, h_{j−1}, ℓ_{j−1}, z_j) and shift T = S_j, one kernel
per level, j = n, …, 1 — each denominator cured inside its own kernel, no
cross-level denominator ever formed.)

## S4. The value layer: the package induction

### S4.0 Lemma TRUNC (the truncation dictionary)

Reads 0..n−1 of an (n+1)-read tower of the class form an n-read tower OF THE
SAME CLASS with identical Φ_j, K_j, ψ_j, ε_j, reads, and lifts (the canonical
lift construction at level m consumes only data of levels ≤ m). For every
γ ∈ ℤ, the (n+1)-read split of γ below the top equals the n-read split of
β_n(γ) (immediate: the split is defined by the same downward recursion from
β_n), so with φ⁽ⁿ⁾ the truncated tower's anchors:

    φ_γ = φ⁽ⁿ⁾_{β_n(γ)} · Φ_n^{s_{n+1}(γ)},   γ ∈ W_{n+1} ⟺ β_n(γ) ∈ W_n
    (the windows read the same u₁), and B-, defect-, δ^{pl}-data agree. ∎

(At n = 2 this is [IL3] S0.3's consumption dictionary; at general n it is the
same two lines.)

### S4.1 Lemmas REALIZE-(m), WELL-DEF-(n), READ-ADD-(m)

**Lemma REALIZE-(m) (the canonical realizer, all levels; generic).** Define
realize₀(τ₀, w) := lift(τ₀)·π^w (τ₀ ∈ K₀, w ≥ 0), and for 1 ≤ m ≤ n−1,
τ ∈ K_m with z_m-power-basis coordinates τ_i ∈ K_{m−1} (i < g_{m−1}), β ∈ ℤ
with grid (s,u) := eq12(β; e_{m−1}, h_{m−1}), slots k_i := s + ie_{m−1} and
slot weights γ⁽ⁱ⁾ := (β − k_iγ_m)/e_{m−1}:

    realize_m(τ, β) := Σ_{i < g_{m−1}, τ_i ≠ 0}
                       realize_{m−1}(ε_{m−1}(γ⁽ⁱ⁾)^{−1}·τ_i, γ⁽ⁱ⁾) · Φ_{m−1}^{k_i}

(the harness realize1/realize2/realize3 recursion verbatim, with ε₀ := 1).
Suppose the DESCENT INVARIANT (I_m): β ≥ e_{m−1}g_{m−1}γ_m (with (I₀): w ≥ 0
— here γ_m-weights per the γ₁ = h₀ convention and, for m = 1, the [IL3]
realize1 admissibility u(β) ≥ (g₀−1)h₀). Then: (R1) deg realize_m(τ,β) <
deg Φ_m; (R2) every recursive call satisfies its own invariant, and for
τ ≠ 0, w_m(realize_m(τ,β)) = β; (R3) R_{m,β}(realize_m(τ,β)) = τ.

*Proof.* Induction on m. m = 0: trivial; m = 1 is [IL3] WELL-DEF(i)'s display
(single-line monomial sum; note (I₁): β ≥ e₀g₀h₀ gives e₀u(β) = β − s(β)h₀ ≥
e₀g₀h₀ − (e₀−1)h₀, so u(β) ≥ (g₀−1)h₀ + h₀/e₀ > (g₀−1)h₀ — admissible).
Step: **invariant descent.** k_i ≤ (e_{m−1}−1) + (g_{m−1}−1)e_{m−1} =
e_{m−1}g_{m−1} − 1, so by (I_m):

    γ⁽ⁱ⁾ ≥ (β − (e_{m−1}g_{m−1}−1)γ_m)/e_{m−1} ≥ γ_m/e_{m−1}
         = (e_{m−1}e_{m−2}g_{m−2}γ_{m−1} + h_{m−1})/e_{m−1} > e_{m−2}g_{m−2}γ_{m−1},

which is (I_{m−1}) for every child. (R1): each summand has degree
< deg Φ_{m−1}·(1 + k_i) ≤ deg Φ_{m−1}·e_{m−1}g_{m−1} = deg Φ_m. (R2)/(R3):
each summand is the single Φ_{m−1}-digit realize_{m−1}(…) at slot k_i (IH
(R1)), on the line e_{m−1}γ⁽ⁱ⁾ + k_iγ_m = β (IH (R2), τ_i ≠ 0); distinct i
give distinct slots, so developments add slotwise and w_m ≥ β. The level-m
read at β: grid abscissas s + je_{m−1} = exactly the k_i; coefficient at slot
i: ε_{m−1}(γ⁽ⁱ⁾)·R_{m−1,γ⁽ⁱ⁾}(child) = ε_{m−1}(γ⁽ⁱ⁾)·ε_{m−1}(γ⁽ⁱ⁾)^{−1}τ_i =
τ_i (IH (R3)); so the read is Σ τ_i y^i ↦ τ ≠ 0, forcing w_m = β exactly. ∎

**Lemma WELL-DEF-(n) (the canonical lifts exist on the class; the exact
g_m = 1 corner form).** (i) Every C_k of the S0.1 lift construction is
well-defined: its realize_m entry weight gp = (g_m−k)γ_{m+1} ≥ γ_{m+1} =
e_me_{m−1}g_{m−1}γ_m + h_m > e_{m−1}g_{m−1}γ_m satisfies (I_m). (ii) At
g_m = 1 (ψ_m = y + c_m, c_m := ψ_m(0) ≠ 0, z_{m+1} = −c_m, K_{m+1} = K_m):

    Φ_{m+1} = Φ_m^{e_m} + Ĉ_m,   Ĉ_m := realize_m(τ_m, γ_{m+1}),
    τ_m := ε_m(γ_{m+1})^{−1}·c_m ∈ K_m^×,

with (Ĉ1) deg Ĉ_m < deg Φ_m — a single Φ_m-slot-0 digit; (Ĉ2)
w_m(Ĉ_m) = γ_{m+1}; (Ĉ3) R_{m,γ_{m+1}}(Ĉ_m) = τ_m. All three from
REALIZE-(m). ∎ (At m = 1 this is [IL3] WELL-DEF(ii) verbatim.)

**[OPERATIVE CONSUMPTION SITE (errata round 1, 2026-08-03; PE-G2's one
substantive flagged site) — full paste per the incorporation convention:
the m = 1 base legs of REALIZE-(m)/WELL-DEF-(n) above consume [IL3]
WELL-DEF(i)/(ii), where [IL3]† = "[IL3] = the (ITER-LAW-3) theorem,
ACCEPTED 2026-08-03: the display c₃ = z̄^{δ₃}·z₂^{D₂}·z₁^{D₁} (δ₃ =
⌊(s₃+s₃′)/e₂⌋; D₂ = (s₂-defect + ℓ₁γ₃δ₃)/e₁; Σ = D₂γ₂ +
δ₃γ₃(ℓ₁′−ℓ₁w₁Φ₁); D₁ = (s₁-defect + ℓ₀Σ)/e₀) PROVED on the 3-read
standard-lift harness class OFF the DCX corner {g₀ = g₁ = 1, δ₁ = 1,
s₂(γ) = s₂(γ′) = e₁−1, S₁+s(γ₂) ≥ 2e₀} (EMPTY at e₀ ≤ 2) — RUNG n = 2 of
(ITER-LAW-n). RESIDUALS: (W-MULT-3-DCX) OPEN (instance-true 132/132); the
fenced non-consumed DEV-1 corner {e₀=1 ∧ d₀≥2 ∧ j≥g₀}; the consumed [IL]
base's package rides through." The WELL-DEF(i)/(ii) displays consumed here
sit in that accepted note's proved body (realize-constructions,
DCX-independent and engine-free — the fenced DEV-1 corner residual does
not touch them).]**

**Lemma READ-ADD-(m) (read additivity with strict-above kill; generic).** For
f, g ∈ O[x] with w_m(f) ≥ λ, w_m(g) > λ: R_{m,λ}(f+g) = R_{m,λ}(f) (reads at
a prescribed line λ ≤ w: digits develop additively; a digit strictly above
its slot line contributes 0).
*Proof.* Induction on m. m = 1: developments add (uniqueness); at each grid
abscissa the g-digit either has w₀ strictly above the required ordinate (res
kills strict π-excess) or g has no digit there. Step: dev_m(f+g) =
dev_m(f) + dev_m(g) slotwise; at each grid slot of the λ-read, g's digit has
w_{m−1} strictly above the slot weight (else w_m(g) ≤ λ), and by IH at level
m−1 it contributes 0 to that slot's read. ∎

### S4.2 L6-(n) (ANCHOR-VAL = the ε-chain; all n, no engine, no DCX)

**Proposition L6-(n).** For every rung n ≥ 1 and γ ∈ W_{n+1}:

    val(φ_γ) = ε_n(β_n(γ)) · ε_{n−1}(β_{n−1}(γ)) · ⋯ · ε₁(β₁(γ)) ∈ K_{n+1}^×,
    and w_{n+1}(φ_γ) = γ.

*Proof.* Induction on n; base n = 1 = [IL S4 L6] (ACCEPTED; rule-8 package as
pasted in the header: harness tower class, standard-lift, strict superset of
DITER_RESTATE §S2's E₂ > 1 pin, on-the-nose at E₂ > 1, residuals
(ITER-LAW-LIFT) / gr(w₂)-wrapper / orders ≥ 3 in force). Step: by TRUNC,
φ_γ = a·Φ_n^{s_{n+1}} with a = φ⁽ⁿ⁾_{β_n(γ)}. Degree telescope: deg a =
Σ_{j<n} s_{j+1}·deg Φ_j ≤ Σ_{j<n} (e_j−1)·deg Φ_j ≤ Σ_{j<n} (deg Φ_{j+1} −
deg Φ_j) = deg Φ_n − d₀ < deg Φ_n (using deg Φ_{j+1} = e_jg_j·deg Φ_j ≥
e_j·deg Φ_j). So the Φ_n-development of φ_γ is the single digit a at slot
s_{n+1}. By the IH: w_n(a) = β_n(γ) and R_nv(a) = ∏_{j<n} ε_j(β_j) (TRUNC
matches all split data). Hence w_{n+1}(φ_γ) = e_nβ_n + s_{n+1}γ_{n+1} = γ;
the level-(n+1) grid at γ is s_{n+1} + je_n; the digit sits at j = 0 with
slot weight (γ − s_{n+1}γ_{n+1})/e_n = β_n = w_n(a): on-line; all other grid
slots carry zero digits. So val(φ_γ) = ε_n(β_n)·R_nv(a)·z̄⁰ = the ε-chain, a
unit (unit powers in the field K_{n+1}). ∎

(The L6 chain is therefore unconditional at EVERY rung given only [IL] — the
base as ACCEPTED on the harness tower class, standard-lift, strict superset
of §S2's E₂ > 1 pin, three residuals in force — and never consumes W-MULT,
(RM-m), or any DCX exclusion. The 5-read instance: B1_L6 56/56 exact, S8.)

**[ATTRIBUTION ERRATUM (errata round 4, 2026-08-03; PE4-F1): "given only
[IL]" undercounts — the proof above also consumes S4.1, at every rung ≥ 2.
The telescope input deg Φ_{j+1} = e_jg_j·deg Φ_j (with Φ_{j+1} monic, as
the division algorithm at the next level requires) holds only given
REALIZE-(m)'s (R1) degree bound deg C_k < deg Φ_m PLUS the existence of the
C_k — WELL-DEF(i) — applied to the S0.1 lift construction Φ_{m+1} =
Σ_{k ≤ g_m} C_kΦ_m^{e_mk}, C_{g_m} = 1 (the S0.1 class pin:
"well-definedness on the class: Lemma WELL-DEF-(n), S4.1"); TRUNC's
"identical … lifts" clause presupposes the same certification. And S4.1's
m = 1 base legs consume [IL3]† WELL-DEF(i)/(ii) (the S4.1 operative
bracket). Corrected attribution: unconditional at every rung given [IL] +
S4.1 (rung 1 — whose tower has no canonical lift — given [IL] alone). The
negative clause stands as written: no W-MULT, no (RM-m), no DCX exclusion.
No live grade change ([IL3] is ACCEPTED, the S7 r38 bracket); the
correction bites only in the S7 [IL3]-failure display branch, where row 4
now falls WITH row 3 at rungs ≥ 2 — the S7 round-4 erratum.]**

### S4.3 The induction package (ITER-PKG-r)

**[NOTATION ERRATUM (errata round 1, 2026-08-03 wallclock; campaign
2026-08-08; PC-C1 [CRITICAL — notation collision; NO mathematical error
found]): the letter S is overloaded in this note — the S0.3/S2/S3/S5
COMPOSED SHIFT S_j (the theorem recursion's object: S_n = δγ_{n+1},
S_{j−1} = D_jγ_j + S_j(ℓ_{j−1}′ − ℓ_{j−1}w_{j−1}Φ_{j−1})) versus the RAW
SLOT SUMS of the (P-DS)/case-analysis layer (S4.3–S4.7). The raw sums are
hereby RENAMED:

    T_j := s_j(β) + s_j(β′)    [the raw level-j slot sum of the pair;
                                in (P-DS): T_{r+1} = s_{r+1}(β)+s_{r+1}(β′)]

and EVERY capital-S pair-sum occurrence in S4.3–S4.7 is to be read as T:
the (P-DS) slot support {T_{r+1}, T_{r+1}+1}, the CII predicate's
"T_r = e_{r−1}−1", the base/link paragraphs' T₁/T₂, the S4.4–S4.6 uses
(T_n ≤ 2e_{n−1}−2, k := T_n − e_{n−1}, T_n = e_{n−1}−1, the Φ_n-exponents
T_{n+1}, s_{n+1}(Γ″) = T_{n+1} − δe_n), and S4.7's T_m/T₁ — dated brackets
sit at each affected display below. The composed shifts S_j NEVER appear in
S4.3–S4.7 (the two layers meet only through B_j and δ_j^{pl}); quoted
ledger pastes ([IL]†/[IL3]† blocks) are byte-frozen and retain [IL3]'s own
S₁ (= the raw sum = T₁). VERIFICATION THAT THIS IS NOTATION, NOT
MATHEMATICS: (a) the PE from-note reimplementation
(`verification/openmath/iterlawn_pe_reimpl.py` @ e7e9151, the semantics
referee) implemented BOTH objects separately — `ssum[j] = s_j + s_j′` for
every S4.3–S4.7 predicate (P-DS support/weight/read clauses, the CII chain,
CASE-SPLIT, k = ssum − e, the II-leg substitution identities, the DCX
outer/sharp predicates) and the composed `S` only in the S0.3 constants
recursion — and returned 22,369 family-checks / 0 violations on nine fresh
instances: the raw-sum reading is the consistent, law-producing semantics
of every renamed site; (b) an orchestrator site-by-site re-check (this
erratum) confirmed each renamed display is TRUE under the raw reading and
false or ill-typed under the composed one (e.g. the slot-T_n line
e_{n−1}B_{n−1} + T_nγ_n = B_n, from β_n = e_{n−1}β_{n−1} + s_nγ_n summed
over the pair), and that NO site consumes one reading while asserting the
other — no equivocation anywhere. The WRITTEN step was ambiguous; the
mathematics is not.]**

**(ITER-PKG-r)** — the exports of rung r, for (r+1)-read towers of the class
and pairs (β, β′) in the window, OFF DCX-(r); P := φ⁽ʳ⁺¹⁾_β·φ⁽ʳ⁺¹⁾_{β′};
plain carries δ_j^{pl} of the pair at each level; B_j the split sums:

* **(P-L6)**: val(φ_β) = ∏_{j=1}^{r} ε_j(β_j) ≠ 0; w(φ_β) = β.
  [No DCX exclusion needed for this clause — L6-(n), S4.2.]
* **(P-WM)**: val(P) = z_{r+1}^{δ_{r+1}^{pl}}·val(φ_β)·val(φ_{β′})
  = z_{r+1}^{δ_{r+1}^{pl}}·∏_{j=1}^{r} z_j^{δ_j^{pl}}·∏_{j=1}^{r} ε_j(B_j);
  the level-(r+1) read of P is a SINGLE nonzero coefficient, at slot
  δ_{r+1}^{pl}; w(P) = β+β′.
* **(P-DS)** (digit shapes of P, certified for the next level): the
  Φ_r-development of P has slot support ⊆ {S_{r+1}, S_{r+1}+1}
  (S_{r+1} := s_{r+1}(β)+s_{r+1}(β′)); the slot-S_{r+1} digit E₀ ≠ 0 has
  deg E₀ < deg Φ_r, w_r(E₀) = B_r, and CERTIFIED READ
  R_rv(E₀) = ∏_{j=1}^{r} z_j^{δ_j^{pl}}·∏_{j=1}^{r−1} ε_j(B_j); the
  slot-(S_{r+1}+1) digit E₁ (possibly 0) has deg E₁ < deg Φ_r and
  w_r(E₁) ≥ B_r − e_{r−1}γ_r; and E₁ ≠ 0 only in the CASE-II stratum:
  CII_r := (g_{r−1} = 1) ∧ (δ_r^{pl} = 1 ∨ (CII_{r−1} ∧ S_r = e_{r−1}−1)),
  with CII₁ := (g₀ = 1 ∧ δ₁^{pl} = 1).

**Base r = 1 (ACCEPTED).** (P-L6) = [IL S4 L6]; (P-WM) = [IL S4 L7 + COR-5];
(P-DS) = [IL L7]'s displayed developments: Case I — E₀ = π^UΦ₀^{S₁} at slot
S₂, w₁ = B₁, read z₁^{δ₁^{pl}} (the empty ε-product), E₁ = 0; Case II
(g₀ = 1 ∧ δ₁^{pl} = 1) — E₀ = A₀ = −ĉ₀π^{U+h₀}Φ₀^{S₁−e₀} at slot S₂, w₁ =
B₁, read −c₀ = z₁ = z₁^{δ₁^{pl}}, and E₁ = A₁ = π^UΦ₀^{S₁−e₀} at S₂+1 with
w₁(A₁) = B₁ − e₀h₀ = B₁ − e₀γ₁ (the γ₁ = h₀ convention makes the DS weight
clause uniform). DCX-(1) = ∅. All from [IL], ACCEPTED 2026-08-03 — rule-8
package pasted: "Scope as accepted: the harness tower class (superset of
DITER_RESTATE §S2's E₂ > 1 pin; on-the-nose coincidence restricted to
E₂ > 1); residuals: (ITER-LAW-LIFT) open exactly at g₀ = 1 ∧ δ₁ = 1; gr(w₂)
wrapper rides the accepted GRB retarget; orders ≥ 3 untouched" —
standard-lift, strict superset of the pinned E₂ = e₀e₁ > 1 setting.
**[Notation r1 (PC-C1): the S₁/S₂ of the base paragraph above are raw sums
— T₁ = s₁+s₁′ (Φ₀-exponents), T₂ = s₂+s₂′ (Φ₁-slots) — per the S4.3 head
erratum.]**

**Link r = 2 ([IL3], ATTEMPT GRADE 0/2; round 2 completed not-clean on
narrow display/naming findings, errata round 2 → round 3 owed).** (P-L6) = its
L6-(3); (P-WM) = its W-MULT-(3) (proved off DCX_2); (P-DS) = its S4.4–S4.6
digit constructions: Case I E₀ = A (single digit, w₂ = B₂, read
z₂^{δ₂}z₁^{δ₁}ε₁(B₁)), E₁ = 0; II-1 E₀ = −aĈΦ₁^k−QΦ₁^{k+1} (w₂ = B₂, read as
displayed), E₁ = aΦ₁^k with w₂(E₁) = B₂ − e₁γ₂; II-2/II-3 likewise (its S4.6
line tables give exactly the (P-DS) weight clauses — checked term by term in
S4.6 below at general level). CII₂ = (g₁ = 1) ∧ (δ₂^{pl} = 1 ∨ (CII₁ ∧
S₂ = e₁−1)) = its CASE-SPLIT Cases II-1∪II-2∪II-3 predicate on the nose.
**[Notation r1 (PC-C1): the CII₂ display's "S₂ = e₁−1" reads T₂ = e₁−1
(raw sum) — per the S4.3 head erratum.]**

**The inductive step (S4.4–S4.6): (ITER-PKG-r) off DCX-(r), plus (RM-r) ⟹
(ITER-PKG-(r+1)) off DCX-(r+1).** [DCX-(r+1) := DCX-(r)-pullback ∪ the new
top corner DCX_{r+1}; S4.7.]

### S4.4 CASE-SPLIT-(n) (the exact partition at the top of rung n)

**[Notation r1 (PC-C1): every S_n/S_{n+1} in this subsection is the raw
slot sum — T_n = s_n(β_n)+s_n(β_n′) = s_n(γ)+s_n(γ′) (TRUNC shares the
split data below the top), T_{n+1} = s_{n+1}(γ)+s_{n+1}(γ′). So: the slot
support {T_n, T_n+1}, the Φ_n-exponent T_{n+1}, "T_n+1 ≤ 2e_{n−1}−1"
(two s-values each ≤ e_{n−1}−1), "T_n ≤ e_{n−1}−2", II-3's
"T_n = e_{n−1}−1", the proof's overflow equivalences (T_n ≥ e_{n−1} ⟺
δ_n^{pl} = 1 given T_n ≤ 2e_{n−1}−2), and the weights display's slot
lines. The composed S0.3 shifts do not occur here — S4.3 head erratum.]**

Fix rung n = r+1 and γ, γ′ ∈ W_{n+1} with the inner pair (β_n, β_n′) off
DCX-(n−1) (else the pair is in DCX-(n) and excluded). By TRUNC, P :=
φ_γφ_{γ′} = A·Φ_n^{S_{n+1}} with A := φ⁽ⁿ⁾_{β_n}φ⁽ⁿ⁾_{β_n′}, and the IH
(P-DS) gives A's Φ_{n−1}-slot support ⊆ {S_n, S_n+1}, E₁ᴵᴴ ≠ 0 only on
CII_{n−1}. deg A < deg Φ_n ⟺ top slot of A ≤ e_{n−1}g_{n−1} − 1. Exactly one
of:

* **Case I** (deg A < deg Φ_n): automatic at g_{n−1} ≥ 2 (top slot ≤
  S_n+1 ≤ 2e_{n−1}−1 < e_{n−1}g_{n−1}); at g_{n−1} = 1: E₁ᴵᴴ = 0 ∧
  δ_n^{pl} = 0, or E₁ᴵᴴ ≠ 0 ∧ S_n ≤ e_{n−1}−2.
* **Case II-1**: g_{n−1} = 1, E₁ᴵᴴ = 0, δ_n^{pl} = 1 (forces e_{n−1} ≥ 2).
* **Case II-2**: g_{n−1} = 1, E₁ᴵᴴ ≠ 0, δ_n^{pl} = 1.
* **Case II-3**: g_{n−1} = 1, E₁ᴵᴴ ≠ 0, S_n = e_{n−1}−1 (δ_n^{pl} = 0).

*Proof.* Direct from the slot support (E₁ᴵᴴ = 0: overflow ⟺ S_n ≥
e_{n−1}g_{n−1}, possible only at g_{n−1} = 1 with S_n ≥ e_{n−1} ⟺
δ_n^{pl} = 1; E₁ᴵᴴ ≠ 0: overflow ⟺ S_n+1 ≥ e_{n−1}, i.e. δ_n^{pl} = 1 or
S_n = e_{n−1}−1). ∎ (At n = 2 this is [IL3] CASE-SPLIT verbatim with "inner
Case II" = CII₁; the union of Cases II-* is CII_n **[Precision note
(errata round 3, 2026-08-03; PE3-N1): ⊆, not = — the union of the II-*
case predicates is CONTAINED in CII_n, and only the containment is ever
consumed downstream; equality is neither claimed elsewhere nor needed]** —
the (P-DS) predicate
propagates by construction.)

Weights consumed below (IH (P-DS)): w_n(A) = B_n attained at slot S_n; the
E₁ᴵᴴ-slot line at level n is ≥ e_{n−1}(B_{n−1}−e_{n−2}γ_{n−1}) +
(S_n+1)γ_n = B_n + (γ_n − e_{n−1}e_{n−2}γ_{n−1}) ≥ B_n + h_{n−1} when CII
holds (g_{n−2} = 1, so w_{n−1}Φ_{n−1} = e_{n−2}γ_{n−1}) — strictly above.

### S4.5 W-MULT-(n), Case I (single top digit) — PROVED, generic

**[Notation r1 (PC-C1): S_{n+1} throughout this leg = T_{n+1} =
s_{n+1}(γ)+s_{n+1}(γ′) (raw sum): the digit slot, w_{n+1}(P) = e_nB_n +
T_{n+1}γ_{n+1} = Γ″, and s_{n+1}(Γ″) = T_{n+1} − δe_n (L1) — S4.3 head
erratum.]**

The Φ_n-development of P is the single digit A at slot S_{n+1}. w_{n+1}(P) =
e_nw_n(A) + S_{n+1}γ_{n+1} = e_nB_n + S_{n+1}γ_{n+1} = Γ″ (sum γ = e_nβ_n +
s_{n+1}γ_{n+1} over the pair). Grid at Γ″: s_{n+1}(Γ″) = S_{n+1} − δe_n
(L1), so A sits at slot j = δ with slot weight (Γ″ − S_{n+1}γ_{n+1})/e_n =
B_n = w_n(A): on-line; all other grid slots carry zero digits. The read of P
is the single coefficient

    C_δ = ε_n(B_n)·R_nv(A) = ε_n(B_n)·z_n^{δ_n^{pl}}·∏_{j<n} z_j^{δ_j^{pl}}·∏_{j<n} ε_j(B_j)

at slot δ (IH (P-WM) on the digit — the LEVEL-(n−1) LAW consumed exactly as
rung 2 consumed rung 1), whence

    val(P) = z̄^{δ}·∏_{j=1}^{n} z_j^{δ_j^{pl}}·∏_{j=1}^{n} ε_j(B_j)
           = z̄^{δ}·val(φ_γ)·val(φ_{γ′})

(the second equality: EPS-MULT at every level, EXP-KIT(b), + L6-(n)). The
(P-DS) export: E₀ⁿᵉʷ = A (w_n = B_n; certified read = the display above
without the ε_n(B_n)-factor — i.e. ∏_{j≤n}z_j^{δ_j^{pl}}∏_{j≤n−1}ε_j(B_j)),
E₁ⁿᵉʷ = 0. ∎

### S4.6 The engine (RM-m), and Cases II-1/II-2/II-3 — PROVED generic MODULO (RM-(n−1)), off DCX-(n)

**THE OPEN ENGINE FAMILY — the exact statement (the one resisting leg).**

┌────────────────────────────────────────────────────────────────────────┐
│ **(RM-m) [READ-MULT with the canonical correction, level m], 1 ≤ m.**  │
│ Setting: level m of a class tower with g_m = 1, Ĉ_m the canonical      │
│ correction digit of WELL-DEF-(n)(ii) (deg < deg Φ_m, w_m = γ_{m+1},   │
│ R_{m,γ_{m+1}}(Ĉ_m) = τ_m = ε_m(γ_{m+1})^{−1}c_m). Input: f ∈ O[x],    │
│ f ≠ 0, deg f < deg Φ_m, λ := w_m(f), v := R_{m,λ}(f) its read. Claim: │
│ there exist R, Q ∈ O[x] with deg R < deg Φ_m, deg Q < deg Φ_m, and    │
│                                                                        │
│   f·Ĉ_m = R + Q·Φ_m,   w_m(R) ≥ λ + γ_{m+1},                          │
│   w_m(Q) ≥ λ + γ_{m+1} − w_mΦ_m   (Q = 0 allowed),                    │
│   R_{m,λ+γ_{m+1}}(R) = z_m^{ε′}·v·τ_m,                                │
│   ε′ := ⌊(s_m(λ) + s_m(γ_{m+1}))/e_{m−1}⌋  [the level-m EPS-carry].   │
│                                                                        │
│ STATUS. m = 1: PROVED — [IL3] DIGIT-SPLIT + DEV-1 (attempt grade 0/2; │
│ round-2 arc not-clean on narrow findings, rounds owed) + the           │
│ double-sum factoring: writing f = Σ b_iΦ₀^i,                          │
│ Ĉ₁ = Σ c_jΦ₀^{k_j}, each coefficient product splits by DIGIT-SPLIT    │
│ and reduces by DEV-1 (children have Φ₀-exponent ≤ 2e₀g₀−2 < 2e₀g₀);   │
│ on-line contributions carry i ≡ s_m(λ), k_j ≡ s_m(γ₂) (mod e₀), and   │
│ (i+k_j−s_m(λ+γ₂))/e₀ = (i−s_m(λ))/e₀ + (k_j−s_m(γ₂))/e₀ + ε′, so the  │
│ read factors as z₁^{ε′}·v·τ₁ exactly. SCOPE inherited from the DEV-1  │
│ erratum: proved for e₀ ≥ 2, and at e₀ = 1 ∧ d₀ = 1 (junkΦ ≡ 0); at    │
│ {e₀ = 1 ∧ d₀ ≥ 2} general inputs enter [IL3]'s fenced DEV-1 corner —  │
│ open there ([IL3]'s own consumptions dodge it, PE-verified; rung ≥ 3  │
│ steps never consume RM-1).                                             │
│ m ≥ 2: **OPEN.** Expected resistances (stated so the unit is priced   │
│ honestly): (i) TERMINATION — the level-m reduction tree mixes the     │
│ standard-lift identity at level 1 with the canonical-correction        │
│ identities Φ_i^{e_ig_i}-shape at 2 ≤ i ≤ m, and the e-descent failure │
│ mode of the DEV-1 erratum (non-descending junk children at e = 1)     │
│ recurs at every level — a well-founded measure across levels is the   │
│ real content; (ii) READ-TRANSPARENCY across Ĉ-insertions:             │
│ 𝔯_m(XΦ_{m−1}^{e_{m−1}}) = 𝔯_m(XΦ_m) − 𝔯_m(XĈ_{m−1}) (the [IL3] S4.7   │
│ DEV-2 clause, per level); (iii) input-class closure (the IH (P-DS)    │
│ exports and their junk legs under the weight filtration). PRICE: one  │
│ dedicated unit per level-family (the wave-20 (W-MULT-3-DCX)/DEV-2     │
│ queue item is the m = 2 instance; a generic-engine unit would subsume │
│ the whole family). Instance evidence: every probed instance at rungs  │
│ 2, 3, 4 (S8) is consistent with (RM-m) — cited as evidence only.      │
└────────────────────────────────────────────────────────────────────────┘

**[ERRATUM (errata round 1, 2026-08-03; PC-G3 [GAP — display arithmetic]):
the box's m = 1 STATUS parenthesis "children have Φ₀-exponent ≤ 2e₀g₀−2 <
2e₀g₀" understates the DIGIT-SPLIT range by one. Corrected chain:
deg f < deg Φ₁ and deg Ĉ₁ < deg Φ₁ give i ≤ e₀g₀−1 and k_j ≤ e₀g₀−1, so
each parent product b_ic_jΦ₀^{i+k_j} has Φ₀-exponent i+k_j ≤ 2e₀g₀−2;
DIGIT-SPLIT on the coefficient product b_ic_j (= lift + πX + Φ₀Y) leaves
the lift- and πX-children at exponent i+k_j ≤ 2e₀g₀−2 but raises the Φ₀Y
child to i+k_j+1 ≤ 2e₀g₀−1. Every child therefore has Φ₀-exponent
≤ 2e₀g₀−1 < 2e₀g₀ — still strictly inside DEV-1's quantifier range
0 ≤ j < 2e₀g₀, so DEV-1 applies to every child and the factored read
z₁^{ε′}·v·τ₁ is unchanged. Conclusion and scope of (RM-1) unaffected; the
displayed bound alone was off by one.]**

**[Notation r1 (PC-C1), Cases II-1/II-2/II-3 below: every S_n is the raw
sum T_n = s_n+s_n′ and every S_{n+1} is T_{n+1} = s_{n+1}+s_{n+1}′ — the
Φ_{n−1}-exponents T_n, k := T_n − e_{n−1}, II-3's "T_n = e_{n−1}−1", the
line identity e_{n−1}B_{n−1} + T_nγ_n = B_n, the L1 grid reads s_n(B_n) =
T_n − δ_n^{pl}e_{n−1}, and all Φ_n-slot indices T_{n+1}/T_{n+1}+1 — S4.3
head erratum; per-leg brackets mark the load-bearing instances.]**

**Case II-1 (g_{n−1} = 1, E₁ᴵᴴ = 0, δ_n^{pl} = 1) — proved modulo
(RM-(n−1)).** A = E₀ᴵᴴ·Φ_{n−1}^{S_n}, k := S_n − e_{n−1} ∈ [0, e_{n−1}−2].
One substitution Φ_{n−1}^{e_{n−1}} = Φ_n − Ĉ_{n−1} (WELL-DEF-(n)(ii)):

    P = E₀ᴵᴴΦ_{n−1}^{k}·Φ_n^{S_{n+1}+1} − E₀ᴵᴴĈ_{n−1}Φ_{n−1}^{k}·Φ_n^{S_{n+1}},

both Φ_n-cofactors of degree < deg Φ_n (k+2 ≤ e_{n−1} = e_{n−1}g_{n−1}), so
this IS the development: E₁ⁿᵉʷ := E₀ᴵᴴΦ_{n−1}^k at slot S_{n+1}+1, E₀ⁿᵉʷ :=
−E₀ᴵᴴĈ_{n−1}Φ_{n−1}^k at slot S_{n+1}. Apply (RM-(n−1)) to f = E₀ᴵᴴ
(λ = B_{n−1}, v = the IH certified read): E₀ᴵᴴĈ_{n−1} = R + QΦ_{n−1}, so
E₀ⁿᵉʷ = −RΦ_{n−1}^k − QΦ_{n−1}^{k+1}, slots ⊆ {k, k+1} ⊆ [0, e_{n−1}−1]:

* slot k line: e_{n−1}(B_{n−1}+γ_n) + kγ_n = e_{n−1}B_{n−1} + S_nγ_n = B_n,
  attained (the R-read z_{n−1}^{ε′}vτ_{n−1} is a product of units ≠ 0);
* slot k+1 line: ≥ e_{n−1}(B_{n−1}+γ_n−w_{n−1}Φ_{n−1}) + (k+1)γ_n =
  B_n + h_{n−1} > B_n.

So w_n(E₀ⁿᵉʷ) = B_n. Level-n read at B_n: grid s_n(B_n) + je_{n−1} with
s_n(B_n) = S_n − e_{n−1} = k (L1, δ_n^{pl} = 1); j = 0 reads −R:

    C₀ = ε_{n−1}(B_{n−1}+γ_n)·(−z_{n−1}^{ε′}·v·τ_{n−1})
       = −c_{n−1}·v·ε_{n−1}(B_{n−1})        [EPS-MULT: ε_{n−1}(B_{n−1}+γ_n)
         = z_{n−1}^{−ε′}ε_{n−1}(B_{n−1})ε_{n−1}(γ_n); τ_{n−1} contributes
         ε_{n−1}(γ_n)^{−1}c_{n−1} — the ε′-carry/canonical-lift CANCELLATION]
       = z_n^{δ_n^{pl}}·v·ε_{n−1}(B_{n−1})   [z_n = −c_{n−1}, δ_n^{pl} = 1];

j ≥ 1: abscissa k+je_{n−1} ≥ k+2 carries zero digits; k+1 is off the grid
(e_{n−1} ≥ 2). So R_nv(E₀ⁿᵉʷ) = z_n^{δ_n^{pl}}vε_{n−1}(B_{n−1}) =
∏_{j≤n}z_j^{δ_j^{pl}}∏_{j≤n−1}ε_j(B_j) — the (P-DS) export. Top level:
slot S_{n+1} line = e_nB_n + S_{n+1}γ_{n+1} = Γ″; slot S_{n+1}+1 line =
e_n(B_n − e_{n−1}γ_n) + (S_{n+1}+1)γ_{n+1} = Γ″ + h_n (g_{n−1} = 1) — off
grid at e_n ≥ 2, on-grid-strictly-above at e_n = 1 (slot weight B_n −
γ_{n+1} < B_n − e_{n−1}γ_n = w_n(E₁ⁿᵉʷ)); read 0 either way. Grid:
s_{n+1}(Γ″) = S_{n+1} − δe_n, E₀ⁿᵉʷ at slot δ, on-line. Hence

    val(P) = z̄^{δ}·ε_n(B_n)·R_nv(E₀ⁿᵉʷ) = z̄^{δ}·val(φ_γ)·val(φ_{γ′}). ∎

**[Notation r1: in II-1 above, "A = E₀ᴵᴴ·Φ_{n−1}^{S_n}", "k := S_n −
e_{n−1}", "S_nγ_n = B_n − e_{n−1}B_{n−1}", "s_n(B_n) = S_n − e_{n−1}" all
carry T_n (raw sum); the Φ_n-slots carry T_{n+1} — S4.3 head erratum.]**

**Case II-2 (E₁ᴵᴴ ≠ 0, δ_n^{pl} = 1) — proved modulo (RM-(n−1)), off
DCX_n.** A = E₁ᴵᴴΦ_{n−1}^{S_n+1} + E₀ᴵᴴΦ_{n−1}^{S_n}; substitute on both
overflowing slots (k := S_n − e_{n−1}):

    P = [E₁ᴵᴴΦ^{k+1} + E₀ᴵᴴΦ^{k}]·Φ_n^{S_{n+1}+1}
        − [E₁ᴵᴴĈΦ^{k+1} + E₀ᴵᴴĈΦ^{k}]·Φ_n^{S_{n+1}}     (Φ := Φ_{n−1}).

E₁ⁿᵉʷ := E₁ᴵᴴΦ^{k+1} + E₀ᴵᴴΦ^k: slots ≤ k+1 ≤ e_{n−1}−1, a digit;
w_n(E₁ⁿᵉʷ) ≥ B_n − e_{n−1}γ_n (slot k line = B_n − e_{n−1}γ_n; slot k+1
line ≥ B_n − e_{n−1}γ_n + h_{n−1}, using w_{n−1}(E₁ᴵᴴ) ≥ B_{n−1} −
e_{n−2}γ_{n−1} and γ_n − e_{n−1}e_{n−2}γ_{n−1} = h_{n−1} at g_{n−2} = 1) —
the (P-DS) E₁-weight clause propagates. Two engine calls: E₀ᴵᴴĈ = R⁰+Q⁰Φ
(read clause consumed), E₁ᴵᴴĈ = R¹+Q¹Φ (WEIGHT clauses only). E₀ⁿᵉʷ =
−R⁰Φ^k − (Q⁰+R¹)Φ^{k+1} − Q¹Φ^{k+2}. **Off DCX_n (k+2 ≤ e_{n−1}−1 or
Q¹ = 0)** E₀ⁿᵉʷ is a digit, and: slot k on-line at B_n (R⁰-read ≠ 0); slot
k+1 lines ≥ B_n + h_{n−1} (Q⁰ as in II-1; R¹ ≥ e_{n−1}(B_{n−1} −
e_{n−2}γ_{n−1} + γ_n) + (k+1)γ_n = B_n + γ_n − e_{n−1}e_{n−2}γ_{n−1} =
B_n + h_{n−1}); slot k+2 (if Q¹ ≠ 0, which with k+2 ≤ e_{n−1}−1 forces
e_{n−1} ≥ 3 — the [IL3] N3 vacuity, generic) line ≥ B_n + 2h_{n−1}, off-grid
at e_{n−1} ≥ 3. So w_n(E₀ⁿᵉʷ) = B_n, grid s_n(B_n) = k, the j = 0
coefficient is C₀ = ε_{n−1}(B_{n−1}+γ_n)·(−R⁰-read) = z_n^{δ_n^{pl}}·v⁰·
ε_{n−1}(B_{n−1}) exactly as in II-1 (v⁰ := the E₀ᴵᴴ certified read); higher
grid slots zero/above-line (READ-ADD-(n−1) where components share a slot).
Top level verbatim as II-1 (E₁ⁿᵉʷ's weight bound gives slot-(S_{n+1}+1)
line ≥ Γ″ + h_n). val(P) = z̄^δ·val·val ∎, and (P-DS) exports as displayed.

**[Notation r1: in II-2 above, "Φ_{n−1}^{S_n+1}", "Φ_{n−1}^{S_n}",
"k := S_n − e_{n−1}" carry T_n (raw sum); the Φ_n-slots carry T_{n+1} —
S4.3 head erratum.]**

**Case II-3 (E₁ᴵᴴ ≠ 0, S_n = e_{n−1}−1, δ_n^{pl} = 0) — proved modulo
(RM-(n−1)), off DCX_n.** Only slot S_n+1 = e_{n−1} overflows:

    P = E₁ᴵᴴ·Φ_n^{S_{n+1}+1} + [E₀ᴵᴴΦ^{e_{n−1}−1} − E₁ᴵᴴĈ]·Φ_n^{S_{n+1}}.

E₁ⁿᵉʷ = E₁ᴵᴴ: slot S_{n+1}+1 line = e_ne_{n−1}w_{n−1}(E₁ᴵᴴ) +
(S_{n+1}+1)γ_{n+1} ≥ Γ″ + h_n + e_nh_{n−1} > Γ″ (the S4.4 weight display).
E₀ⁿᵉʷ = E₀ᴵᴴΦ^{e_{n−1}−1} − R¹ − Q¹Φ (engine on E₁ᴵᴴĈ, weight clauses
only). **Off DCX_n (e_{n−1} ≥ 2 or Q¹ = 0)** E₀ⁿᵉʷ is a digit: slot
e_{n−1}−1 carries E₀ᴵᴴ (w = B_{n−1}, line B_n, attained); R¹ at slot 0, line
≥ e_{n−1}(B_{n−1} − e_{n−2}γ_{n−1} + γ_n) = B_n − (e_{n−1}−1)γ_n + γ_n −
e_{n−1}e_{n−2}γ_{n−1}... = B_n + h_{n−1} + (e_{n−1}−1)(γ_n... [direct: line
− B_n = e_{n−1}(γ_n − e_{n−2}γ_{n−1}) − (e_{n−1}−1)γ_n = γ_n −
e_{n−1}e_{n−2}γ_{n−1} = h_{n−1} > 0]; Q¹ at slot 1, line ≥ B_n + 2h_{n−1} −
... > B_n similarly. Slot collisions (slot 1 = e_{n−1}−1 at e_{n−1} = 2;
slot 0 = e_{n−1}−1 at e_{n−1} = 1 with Q¹ = 0): benign — the colliding
summand sits strictly above the line inside the digit and READ-ADD-(n−1)
kills it. Grid s_n(B_n) = S_n = e_{n−1}−1 (δ_n^{pl} = 0): the j = 0
coefficient is C₀ = ε_{n−1}(B_{n−1})·v⁰ = z_n^0·v⁰·ε_{n−1}(B_{n−1}),
matching δ_n^{pl} = 0. Top level as before. val(P) = z̄^δ·val·val ∎.

**[Notation r1: in II-3 above, "S_n = e_{n−1}−1", "slot S_n+1 = e_{n−1}",
"s_n(B_n) = S_n" carry T_n (raw sum; s_n(B_n) = T_n by L1 at
δ_n^{pl} = 0); the Φ_n-slots carry T_{n+1} — S4.3 head erratum.]**

**[ERRATUM (errata round 1, 2026-08-03; PC-G4 [GAP — ellipses at a
load-bearing bound]): the Case II-3 Q¹ line bound, written out with no
ellipses (the R¹ bracket style). Engine weight clause on E₁ᴵᴴĈ:
w_{n−1}(Q¹) ≥ w_{n−1}(E₁ᴵᴴ) + γ_n − w_{n−1}Φ_{n−1}, with w_{n−1}(E₁ᴵᴴ) ≥
B_{n−1} − e_{n−2}γ_{n−1} (IH (P-DS)) and w_{n−1}Φ_{n−1} = e_{n−2}γ_{n−1}
(g_{n−2} = 1, forced by CII_{n−1}). Hence Q¹ at slot 1:

    line ≥ e_{n−1}(B_{n−1} − 2e_{n−2}γ_{n−1} + γ_n) + γ_n
    [direct: line − B_n = e_{n−1}(γ_n − 2e_{n−2}γ_{n−1}) + γ_n − (e_{n−1}−1)γ_n
                        = 2γ_n − 2e_{n−1}e_{n−2}γ_{n−1} = 2h_{n−1} > 0,
     using B_n = e_{n−1}B_{n−1} + T_nγ_n with T_n = e_{n−1}−1]

so the Q¹ line ≥ B_n + 2h_{n−1} > B_n exactly as asserted — "similarly" is
discharged; the collision legs and the READ-ADD kill stand as displayed.]**

**[Display supplement (errata round 1; PE-N1, one line): the II-3 (P-DS)
E₁-export weight clause, displayed: E₁ⁿᵉʷ = E₁ᴵᴴ has w_n(E₁ⁿᵉʷ) =
e_{n−1}w_{n−1}(E₁ᴵᴴ) ≥ e_{n−1}(B_{n−1} − e_{n−2}γ_{n−1}) = B_n −
(e_{n−1}−1)γ_n − e_{n−1}e_{n−2}γ_{n−1} = B_n − e_{n−1}γ_n + h_{n−1} ≥
B_n − e_{n−1}γ_n — the clause propagates, with h_{n−1} to spare.]**

(At n = 2 the three legs specialize clause-for-clause to [IL3] S4.5/S4.6
with (γ_n, e_{n−1}, z_n, ε_{n−1}, c_{n−1}, v) = (γ₂, e₁, z₂, ε₁, c₁,
z₁^{δ₁}) — a consistency display, not a second proof; [IL3]'s own grade
0/2 rides every rung-2 consumption.)

### S4.7 DCX-(n): the excluded locus, characterized per level

**Definition.** For 2 ≤ m ≤ n, DCX_m := the pairs whose level-m top analysis
(the S4.6 cases run on the truncated pair at level m, via TRUNC the split
data are literally shared) has a SECOND-ROUND OVERFLOW: the engine Q-part of
the E₁-product lands at Φ_{m−1}-slot ≥ e_{m−1} (II-2: slot k+2 = e_{m−1};
II-3: slot 1 ≥ e_{m−1}, i.e. e_{m−1} = 1) with Q ≠ 0 — a second substitution
Φ_{m−1}^{e_{m−1}} = Φ_m − Ĉ fires on Q and the one-round analysis above does
not close. **DCX-(n) := ∪_{m=2}^{n} DCX_m.**

**Outer bound (PROVED, generic — from the S4.6 slot arithmetic alone):**

**[ERRATUM (errata round 1, 2026-08-03; PE-G1 [GAP — grade qualifier]):
read "PROVED, generic (given IH package)". The proof below consumes the IH
(P-DS) clause (E₁ᴵᴴ ≠ 0 ⟹ CII_{m−1} ⟹ g_{m−2} = 1), and (P-DS) at rungs
≥ 3 sits inside the (RM)-conditional package chain — so the outer bound
(and the emptiness leg "DCX_m = ∅ whenever g_{m−2} ≥ 2 or g_{m−1} ≥ 2"
riding it) is conditional exactly as S7 rows 5/6, unless the short
engine-free-support lemma (that the (P-DS) support/E₁/CII sub-clauses never
call the engine) is proved — plausible, NOT claimed here. "From the S4.6
slot arithmetic alone" scopes the NEW ingredients, not the IH. The same
qualifier is bracketed at the two S0.3 display sites.]**

    DCX_m ⊆ DCX_m^out := { g_{m−1} = 1,  s_m(γ) = s_m(γ′) = e_{m−1}−1,
                            CII_{m−1} holds for the pair }
           ⊆ { g_{m−2} = g_{m−1} = 1, s_m-track extreme }.

*Proof.* Q-emission at slot ≥ e_{m−1} requires: from Q⁰ (the E₀-product) at
slot k+1: k+1 ≥ e_{m−1} ⟺ S_m ≥ 2e_{m−1}−1, impossible (S_m ≤ 2e_{m−1}−2);
so only the E₁-product's Q¹, needing E₁ᴵᴴ ≠ 0 (⟹ CII_{m−1}, ⟹ g_{m−2} = 1
by (P-DS)) and slot k+2 ≥ e_{m−1} (II-2: S_m = 2e_{m−1}−2 ⟺ both s_m-values
= e_{m−1}−1) or e_{m−1} = 1 (II-3: s_m ≡ 0 = e_{m−1}−1 automatically). ∎

**[Notation r1 (PC-C1): S_m in the outer-bound proof above = T_m =
s_m(γ)+s_m(γ′) (raw sum: "T_m ≤ 2e_{m−1}−2", "T_m ≥ 2e_{m−1}−1
impossible", "T_m = 2e_{m−1}−2 ⟺ both s_m-values = e_{m−1}−1", k = T_m −
e_{m−1}); in the m = 2 sharp form below, S₁ = T₁ = s₁+s₁′ ([IL3]'s own
notation, retained verbatim inside quoted byte-frozen ledger pastes such
as the S7 r38 block) — S4.3 head erratum.]**

**Sharp form and emptiness:**
* m = 2 (PROVED = [IL3] S4.7, attempt grade 0/2): DCX_2 = {g₀ = g₁ = 1,
  δ₁^{pl} = 1, s₂ = s₂′ = e₁−1, S₁ + s(γ₂) ≥ 2e₀} — EMPTY unless e₀ ≥ 3.
* m ≥ 3: the exact Q¹-emission predicate is (RM-(m−1))-internal — the sharp
  characterization is OPEN, priced with the engine. Proved emptiness NOW:
  DCX_m = ∅ whenever g_{m−2} ≥ 2 or g_{m−1} ≥ 2 (outer bound). CONJECTURED
  (the e_{j−1} ≤ 2 analogue, from the m = 2 mechanism + all instance data):
  DCX_m = ∅ unless e_{m−2} ≥ 3.
* Consequences: DCX-(n) = ∅ on the sub-class with no two consecutive
  degree-1 reads among ψ₀,…,ψ_{n−1}; and at n = 2, DCX-(2) = ∅ iff-covered
  by e₀ ≤ 2 (proved). The full-class emptiness statement for n ≥ 3 at
  e_j ≤ 2 is conjectural (engine-dependent).

**[REFUTATION BRACKET (2026-08-03, orchestrator-direct; the sealed RM-2
engine probe, seal 0b42885 → verdict d860a5a, note
`RM2_PROBE_2026-08-08.md`): the CONJECTURED clause above is FALSE at
m = 3 — 42 sealed witnesses, all measured ⊋ predicted, headline tuple
(RM2A/ℤ₃, γ = γ′ = 51, the all-(e = 2, g = 1) stack, every s-value 1),
cross-ring confirmed (F₂[[t]]): DCX₃ is LIVE at e₁ = 2 (Q¹ ≠ 0). The
"full-class emptiness for n ≥ 3 at e_j ≤ 2" clause settles NEGATIVELY.
UNAFFECTED: this note's theorem (proved OFF-DCX-(n), consuming only the
outer bound, which stayed GREEN 0/73); [IL3]'s proved m = 2 emptiness
(e₀ ≤ 2); the outer-bound consequence clauses above except the "full-class"
sentence. THE MEASURED SHARP LOCUS (post-run separation, 102/102 exact,
display-only, sealed confirmation owed): DCX₃ = TRACK ∧ [T₂ + s₂(γ₃) ≥
2e₁ ∨ (T₂ + s₂(γ₃) = 2e₁ − 1 ∧ s₁ + s₁′ + s₁(u₀) ≥ e₀)] with
u₀ = (γ₃ − s₂(γ₃)γ₂)/e₁ — a TWO-LEVEL CARRY CHAIN: the level-2 boundary
is decided by a level-1 overflow promoting into the Φ₁ slots, riding the
same E1-promotion edge that carries the engine's termination measure.
Also of record from the probe: the four (RM-m) box clauses ALL
instance-true at m = 2 (405/405 engine calls; weight/read/ε′/consistency
0 violations) — the m = 1 mechanism generalizes AS DISPLAYED; the
termination measure strictly descends on the inherited scope (e₁ = 1
flat-promotion edges need a lexicographic tiebreak, priced with the
engine compose); the fenced e₀ = 1 ∧ d₀ = 2 corner was UNTESTED (inputs
dodge it), not passed.]**

**On DCX-(n) the W-MULT display is the OPEN family (W-MULT-DCX-m)** —
instance-true at every probed instance (rung 2: [IL3] S4.7's roster-B
evidence + its diag 16 DCX pairs/0; rung 3: the sealed 4-read battery's
strata; rung 4: this unit's diagnostic — T5B realizes 16 sharp-DCX_2 pairs
per instance at 5 reads, law exact on ALL of them, S8). The DEV-2/engine
unit's price display ([IL3] S4.7) carries over; a proof of (RM-m) with a
second-round clause would close both II-legs and DCX at once.

## S5. The theorem (assembly; the induction closed on the stated perimeter)

*Proof of (ITER-LAW-n), by induction on the rung, off DCX-(n), modulo the S7
ledger.* Rung 1 = [IL] (ACCEPTED 2026-08-03; rule-8 package pasted: "Scope as
accepted: the harness tower class (superset of DITER_RESTATE §S2's E₂ > 1
pin; on-the-nose coincidence restricted to E₂ > 1); residuals:
(ITER-LAW-LIFT) open exactly at g₀ = 1 ∧ δ₁ = 1; gr(w₂) wrapper rides the
accepted GRB retarget; orders ≥ 3 untouched" — standard-lift, strict superset
of the pinned E₂ = e₀e₁ > 1 setting): its display IS the n = 1 recursion
(δ = δ₂, S₁ = γ₂δ₂, D₁ = (s₁-defect + ℓ₀γ₂δ₂)/e₀). Rung n ≥ 2: assume
(ITER-PKG-(n−1)) on the truncated tower (n = 2: the base; n = 3: [IL3] at
0/2; n ≥ 4: the S4 step iterated, consuming (RM-m) for 2 ≤ m ≤ n−1). Let
γ, γ′ ∈ W_{n+1}, pair off DCX-(n); Γ″ ∈ W_{n+1} (WINDOW-(n)). By L6-(n) at
Γ″ and W-MULT-(n) (S4.5/S4.6):

    c_{n+1}(γ,γ′) = z̄^{δ} · ∏_{j=1}^{n} [ z_j^{δ_j^{pl}}·ε_j(B_j)/ε_j(β_j″) ].

For each j, β_j″ = B_j + S_j (SHIFT-(n)), so EXP-KIT(c) at
(e_{j−1}, h_{j−1}, ℓ_{j−1}, z_j) with T = S_j turns the j-th bracket into
z_j^{D_j}, D_j = (s_j-defect + ℓ_{j−1}S_j)/e_{j−1} ∈ ℤ — the recursion's
exponents exactly; the internal carries δ_j^{pl} cancel inside the per-level
kernels and do not appear in the law. Hence c_{n+1} = z̄^δ·∏_j z_j^{D_j}. ∎

The (P-WM)/(P-DS) clauses proved in S4.5/S4.6 are exactly (ITER-PKG-n): the
induction climbs. The GRADE of each rung is the S7 ledger — no clause above
is claimed beyond it.

## S6. Corollaries (derived; the OFF-DCX qualifier baked into every

================================================================
=== PASTE B: RMENGINE_2026-08-08.md ([RMG], ACCEPTED @ a0e266c), lines 283-1170 @ 3c2ac60 ===
================================================================
## S0. Setting, the statement under proof, canonicity

### S0.1 The class pin

The (n+1)-read standard-lift harness class of [ILN]† S0.1, verbatim: O a
DVR with uniformizer π, residue field k₀, w₀ the Gauss valuation on O[x];
tower data Φ₀ (monic, deg d₀, reduction irreducible), reads (e_j, h_j, g_j)
with gcd(e_j,h_j) = 1, fields K_{j+1} = K_j[z_{j+1}]/(ψ_j), the uniformizing
convention γ₁ := h₀, γ_{j+1} := e_jw_jΦ_j + h_j, w_{j+1}Φ_{j+1} :=
e_jg_jγ_{j+1}; canonical ε-corrected lifts Φ_{j+1} (standard lift at j = 0),
certified on the class by [ILN]† S4.1 (REALIZE-(m)/WELL-DEF-(n)); normalized
Bézout pairs (ℓ_j, ℓ_j′). The calculus (w_m, reads R_{m,λ}, ε_m, eq-(12-int)
splits s_m(·)) is the [ILN]† S0.2 mirror recursion, consumed as definitions.
Abbreviations local to this note: P_l := e_lg_l (the level-(l+1) period),
E_m := e₀e₁⋯e_{m−1}, and the level-m line scales

    Γ_l := γ_{l+1} · ∏_{l<t<m} e_t   (l = 0, …, m−1;  Γ_{m−1} = γ_m).

(ITER-LAW-LIFT) propagates: nothing is claimed for non-canonical lifts. ⟦C21⟧

### S0.2 THE STATEMENT UNDER PROOF — the (RM-m) box, quoted verbatim (operative-site paste)

From [ILN]† S4.6 (byte-frozen there; the m = 1 STATUS lines and the m ≥ 2
resistance pricing are part of the box):

> **(RM-m) [READ-MULT with the canonical correction, level m], 1 ≤ m.**
> Setting: level m of a class tower with g_m = 1, Ĉ_m the canonical
> correction digit of WELL-DEF-(n)(ii) (deg < deg Φ_m, w_m = γ_{m+1},
> R_{m,γ_{m+1}}(Ĉ_m) = τ_m = ε_m(γ_{m+1})^{−1}c_m). Input: f ∈ O[x],
> f ≠ 0, deg f < deg Φ_m, λ := w_m(f), v := R_{m,λ}(f) its read. Claim:
> there exist R, Q ∈ O[x] with deg R < deg Φ_m, deg Q < deg Φ_m, and
>
>   f·Ĉ_m = R + Q·Φ_m,   w_m(R) ≥ λ + γ_{m+1},
>   w_m(Q) ≥ λ + γ_{m+1} − w_mΦ_m   (Q = 0 allowed),
>   R_{m,λ+γ_{m+1}}(R) = z_m^{ε′}·v·τ_m,
>   ε′ := ⌊(s_m(λ) + s_m(γ_{m+1}))/e_{m−1}⌋  [the level-m EPS-carry].

The four clauses named, for the ledger: **(C-W_R)** the R-weight clause;
**(C-W_Q)** the Q-weight clause; **(C-READ)** the R-read clause; **(C-EPS)**
the ε′-carry identity s_m(λ) + s_m(γ_{m+1}) − s_m(λ+γ_{m+1}) = e_{m−1}·ε′.
((C-EPS) is [IL]† L1 = EQ12-ADD at the level-(m−1) read, restated; it is
proved wherever cited below by that two-line lemma alone.) ⟦C1⟧

### S0.3 Canonicity (the RM2_PROBE reduction, now displayed in-note)

**Lemma L-CANON.** Since deg f < deg Φ_m and deg Ĉ_m < deg Φ_m, any
decomposition f·Ĉ_m = R + QΦ_m with deg R < deg Φ_m forces (Q, R) =
divmod(f·Ĉ_m, Φ_m) (uniqueness of division by the monic Φ_m;
deg Q < deg Φ_m is then automatic from deg(fĈ_m) < 2·deg Φ_m). So the ⟦C2⟧
box's existence claim is EQUIVALENT to: THE canonical division satisfies
(C-W_R)/(C-W_Q)/(C-READ). ∎ (Every leg below is about the canonical
division; the tree of S2 is the proof instrument that computes it.
[r1, PE-n4: the sentence is re-flowed so the C2 grep key sits on one
line; and the C2 claim row now records ALL the hypotheses this lemma
consumes — Φ_m MONIC, deg f < deg Φ_m, deg Ĉ_m < deg Φ_m — per PC-G3.])

## S1. Architecture: the depth calculus (what the data dictated, and how the induction runs)

The two sealed units feeding this compose fixed the architecture. The RM-2
probe measured: the m = 1 double-sum mechanism generalizes clause-for-clause
(405/405), the natural two-identity reduction tree terminates with strict
linear-measure descent exactly on the m = 1-analogue scope, and the ONLY ⟦C16⟧
bend is at the second-round overflow, where the boundary is decided by a
carry riding the E1-promotion edge. The DCX3 seal then made the mechanism
depth-exact at conjecture grade: a deficit-k overflow rides the promotion
edge exactly ≥ k deep; direct overflows are clean-visible at depth 0. So
the compose-facing directive of record — thread the termination measure and
the second-round boundary together along the promotion edge at every carry
depth — is executed here as follows.

**The promotion edge carries three exact structures at once:**

1. **The measure goes strictly DOWN** (S4): every promotion edge drops the
   generic measure μ_m (condition (A_i)); every correction/junk edge drops
   it too (condition (B_i), the DEV-1-scope e₀-edge) — this is termination.
2. **The line goes UP by an exact quantum** (S3, L-LINE): a promotion at
   level i+1 < m raises the cell's level-m line by EXACTLY
   h_{i+1}·∏_{i+1<t<m} e_t > 0. Hence the read skeleton at any fixed target
   is PROMOTION-FREE: carry depth 0. The entire four-clause theorem lives ⟦C24⟧
   at depth 0, and its proof (S5) is the m = 1 mechanism run once per
   level — the depth-0 base of the charter's induction, executed
   generically. This is why the box's clauses close without the sharp
   second-round analysis.
3. **The second-round boundary counts promotions** (S6): what re-enters the
   Φ_{m−1}-slots after the exit is exactly the depth-stratified part of Q;
   the level-l pool floor ⌊pool_l/e_{l−1}⌋ of LAW-DCX-(m+1) is the exact
   bookkeeping of how much exponent mass can ride the promotion edge from
   level l to level l+1 — at each depth the overflow either dies (the
   deficit accounting: the floor expression) or promotes exactly one edge
   deeper, carrying μ strictly down. The SHAPE of the law is MOTIVATED by
   the line/measure calculus [r1, PC-G2: regraded — S6.2 proves the
   crossing law and the carry cap c_l ≤ 2; the pool identification
   itself is heuristic, conjecture-supported]; the LIVENESS (the residue
   non-vanishing Q¹ ≠ 0 on the locus) is conjectural at every m ≥ 2 —
   proved at m = 1 ([IL3]†'s sharp form), sealed-confirmed at conjecture
   grade at m = 2 (THE UNIFORM CARRY LAW v2), first measured at m = 3 by
   this unit's diagnostic (S9).

**The kernel that makes the read layer one computation instead of m:** the
𝔷-multiplicativity lemma (L-ZMULT, S3) — the formal read-monomial of a cell
is multiplicative up to the single level-m carry z_m^{ε̃}, because the
λ-chain of S3 is LINEAR in (target, exponent vector) and every internal
carry cancels between its t-leg and its ε-leg (EXP-KIT(b) at each level).
With L-ZMULT, both telescope families (the standard-lift one and the
canonical-correction one) reduce to ψ_i(z_{i+1}) = 0 plus the
ε-carry/canonical-lift cancellation — the same two facts that closed
[ILN]† S4.6's Case II-1 top computation, now applied inside the engine.

**Consumption manifest (proof-grade):** [IL]† — L1 (EQ12-ADD) and the S0.2
base facts; EXP-KIT (a)/(b)/(c) as re-displayed self-contained in [ILN]† ⟦C7⟧
S3. [IL3]† — Lemma DIGIT-SPLIT (S4.1 there; the level-0 splitter, proved,
engine-free); the WELL-DEF m = 1 displays ride in through [ILN]† S4.1's ⟦C26⟧
base as before. [ILN]† — the S0.1/S0.2 class and calculus pins; S4.1
REALIZE-(m) (incl. (R1)–(R3) and the (I_m) descent invariant),
WELL-DEF-(n) (incl. the g_m = 1 form Φ_{m+1} = Φ_m^{e_m} + Ĉ_m and
(Ĉ1)–(Ĉ3)), READ-ADD-(m); the S4.4–S4.6 case partition and engine
call-sites (for S7's consequence display only); the S4.7 DCX_m definition ⟦C22⟧
and outer bound (S6/S7 display only). DEV-1 itself is NOT consumed: S5 ⟦C22⟧⟦C3⟧
re-derives its content generically (T-tele(1) + the S4 measure at m = 1).
Sealed artifacts (RM2 probe, DCX3 seal, the rung 3–5 batteries): instance
evidence and design input only, never proof steps. ⟦C15⟧

## S2. The cell calculus (cells, the three identity families, the tree, exactness) — PROVED, generic

### S2.1 Cells, lines, initial decomposition

A **cell** is C = b·Φ₀^{j₀}Φ₁^{j₁}⋯Φ_{m−1}^{j_{m−1}} with b ∈ O[x],
b ≠ 0, deg b < d₀, j_l ∈ ℤ_{≥0}. Its **line** (level-m weight lower bound,
exact on reduced cells by L-LINE(iv)):

    line(C) := E_m·w₀(b) + Σ_{l<m} j_l·Γ_l.

C is **reduced** iff j_l ≤ P_l − 1 for every l; then deg C < deg Φ_m by the
degree telescope deg Φ_{l+1} = P_l·deg Φ_l ([ILN]† S4.1 (R1) + WELL-DEF(i),
via the S0.1 pin): deg C ≤ (d₀−1) + Σ_l(P_l−1)deg Φ_l = deg Φ_m − 1.

**Lemma L-CELL (cell decomposition; B-READ).** (i) Every 0 ≠ x ∈ O[x] with
deg x < deg Φ_m is a finite sum of DISTINCT reduced cells (iterated
Φ_{m−1}-, …, Φ₀-development; uniqueness of division), with w_m(x) =
min line over its cells. (ii) [B-READ] For any target β* ≤ w_m(x),

    R_{m,β*}(x) = Σ_{cells C of x, line(C) = β*, C on-grid} res(b̄_C)·𝔷(C; β*),

with 𝔷 and on-grid as defined in S3.1, and b̄_C := res(b_C/π^{w₀(b_C)}).
*Proof.* (i) is the iterated division algorithm plus the min-formula for
single digits (developments add; w₀ a valuation — the S0.2 base facts of
[IL]†, consumed as in [ILN]†). (ii) Induction on m: the level-m read of x
at β* reads the Φ_{m−1}-digit at each grid abscissa s_m(β*) + te_{m−1} with
coefficient ε_{m−1}(slot weight)·R_{m−1,slot weight}(digit)·(z_m-power t);
each digit is the sum of the cells with that j_{m−1}; digits strictly above
their slot line contribute 0 (READ-ADD-(m−1), [ILN]† S4.1); off-grid
j_{m−1} are not read. Unwinding to level 1 gives exactly the 𝔷-product of
S3.1 per cell. ∎

**Initial decomposition of the engine input.** By L-CELL, f = Σ x-cells
(each with line ≥ λ, the on-line on-grid ones summing to v under B-READ)
and Ĉ_m = Σ D-cells. By the structure of realize_m ([ILN]† S4.1: children
placed at slots k_i = s + ie_{m−1} ≤ P_{m−1}−1, on their lines, nonzero τ's
only), EVERY cell of Ĉ_m is reduced, ON-LINE at γ_{m+1}, ON-GRID, with ⟦nc⟧
nonzero residue; B-READ on Ĉ_m is the (Ĉ3) certificate: Σ res(c̄_D)𝔷(D;
γ_{m+1}) = τ_m. Then

    f·Ĉ_m = Σ_{x-cell, D-cell} (b_x·c_D)·∏_l Φ_l^{j_l(x)+j_l(D)},

and each coefficient product is split by L-NORM below. **Initial
cells:** j_l ≤ 2P_l − 2 for all l, +1 at l = 0 on the Y-children.

**Lemma L-NORM (π-normalization of coefficient products) [r1 repair,
2026-08-04, PC-C1 (critical) as adjudicated TEXT-GAP by PE-F2 — THE
missing display].** Let b, c ∈ O[x] ∖ {0} with deg b, deg c < d₀. Write
b = π^{w₀(b)}·b̂ and c = π^{w₀(c)}·ĉ with b̂, ĉ PRIMITIVE (w₀ = 0; possible
since w₀ is the Gauss valuation), and set w := w₀(b) + w₀(c) = w₀(b·c)
(w₀ is a valuation — the S0.2 base facts). DIGIT-SPLIT ([IL3]† S4.1:
αβ = lift(q(α)q(β)) + πX + Φ₀Y, deg < d₀ each, q the reduction
homomorphism — stated and proved there for ALL α, β of degree < d₀ with
no primitivity hypothesis, engine-free [r2, PE2-3: the r1 paraphrase said ⟦C26⟧
"for PRIMITIVE inputs" — primitivity is where the L-child is guaranteed
nonzero, not a validity condition]) applies in particular to the
primitive product b̂ĉ; restoring the factor π^w gives THE NORMALIZED
SPLIT

    b·c = π^w·lift(b̄·c̄)  +  π^{w+1}·X̂  +  π^w·Φ₀·Ŷ,
    b̄ := res(b/π^{w₀(b)}) = res(b̂),   c̄ := res(ĉ)   ∈ K₀ = k₀[x]/(ψ̄₀),

with: (a) the **L-child** π^w·lift(b̄c̄) has w₀ EXACTLY w and normalized
residue b̄·c̄, which is NEVER zero (K₀ is a field and b̄, c̄ ≠ 0: a nonzero ⟦C18⟧
primitive polynomial of degree < d₀ has nonzero reduction of degree
< deg ψ̄₀) — so the L-child is never the zero polynomial; (b) the ⟦C18⟧
**π-junk child** π^{w+1}X̂ has w₀ ≥ w + 1 = w₀(b·c) + 1 (absent iff
X̂ = 0); (c) the **Φ₀Y-junk child** π^w·Ŷ at exponent j₀ + 1 has w₀ ≥ w
(absent identically at d₀ = 1: there deg(b̂ĉ) ≤ 2d₀ − 2 = 0 < d₀, no
Φ₀-overflow, Ŷ = 0). *Proof.* All
displayed: DIGIT-SPLIT on b̂ĉ is the [IL3]† lemma verbatim (its inputs
primitive by construction); multiplying through by π^w is exact;
(a)–(c) are the valuation bookkeeping just stated. ∎

**Convention [r1]:** EVERY invocation of DIGIT-SPLIT in this note — the
initial cells above, and the S2.2 correction children — is the L-NORM
normalized split. Without the normalization the displayed q would
annihilate any coefficient with w₀ > 0 (q(π·anything) = 0), the L-child
would be absent, and L-LINE(ii)'s Δline = 0 and (iii)'s π-child increase
would FAIL — PC-C1's counterexample (b = π, c_d = 1). With it, every
displayed law below holds; the implementation referee: PE's from-note
reimplementation was FORCED to insert exactly this step and then measured
97/97 engine calls with zero violations (`rmengine_pe_reimpl.py`,
"DIGIT-SPLIT, pi-normalized"), and the committed diagnostic's
`digit_split_norm` ("DIGIT-SPLIT on the unit part") is the same lemma.

### S2.2 The three identity families and the E1-priority tree

The identities, all EXACT in O[x] (the level-1 one from Φ₁ = the standard
lift of ψ₀; the level-(i+1) ones, 1 ≤ i ≤ m−1, from the canonical
ε-corrected lift, [ILN]† S0.1/S4.1, with C_k^{(i)} :=
realize_i(ε_i((g_i−k)γ_{i+1})^{−1}·ψ_{i,k}, (g_i−k)γ_{i+1}) for k < g_i,
ψ_{i,k} ≠ 0, and C_k^{(0)} := lift(ψ₀,k)·π^{(g₀−k)h₀}):

    (ID-(i+1))    Φ_i^{P_i} = Φ_{i+1} − Σ_{k<g_i, ψ_{i,k}≠0} C_k^{(i)}·Φ_i^{e_ik},
                  0 ≤ i ≤ m−1.

**The tree.** Nodes are cells. A cell with all j_l ≤ P_l − 1 is an
**R-leaf**. Otherwise let i := the LOWEST level with j_i ≥ P_i
(E1-priority) and apply (ID-(i+1)) to Φ_i^{P_i} inside C:

* **promotion child** (i ≤ m−2): j_i −= P_i, j_{i+1} += 1, b unchanged;
  **Φ_m-exit** (i = m−1): the cofactor b∏Φ^{j'}, j_{m−1}' = j_{m−1}−P_{m−1},
  joins Q (leaves the tree).
* **correction children** (one per k and per reduced cell d of C_k^{(i)},
  with c_d, a⃗_d its coefficient and exponent vector; a_{d,l} ≤ P_l − 1 for
  l < i, a_{d,l} = 0 for l ≥ i): coefficient product b·c_d L-NORM-split
  [r1, PC-C1] into the L/π-junk/Φ₀Y-junk children of S2.1 at exponents
  j_l + a_{d,l} (l < i, +1 at l = 0 on Y), j_i −= P_i − e_ik, sign −. For
  i = 0 the correction children are the L-NORM split of
  b·lift(ψ₀,k)·π^{(g₀−k)h₀} at j₀ − P₀ + e₀k — [IL3]† DEV-1's children,
  verbatim (there w₀(lift(ψ₀,k)) = 0 and the explicit π-depth (g₀−k)h₀
  rides outside the normalization).

**Lemma L-EXACT.** Each reduction step replaces a cell by the exact sum of
its children plus (at i = m−1) Φ_m times the exit cofactor. Hence, whenever
the tree on the S2.1 initial cells is finite (S4), with R₀ := Σ R-leaves
and Q := Σ exit cofactors: f·Ĉ_m = R₀ + Q·Φ_m; deg R₀ < deg Φ_m (R-leaves
are reduced), so by L-CANON (R₀, Q) = the canonical (R, Q), and
deg Q < deg Φ_m is automatic. ∎ (No window hypothesis is consumed here: ⟦C2⟧⟦C4⟧
the exit cofactors need not be reduced for this lemma.)

**Structural remark (exit cofactors are top-single digits, E1-priority).**
At a Φ_m-exit the reducing level is m−1, so by priority j_l ≤ P_l − 1 for
every l ≤ m−2: the sub-cell S := b∏_{l≤m−2}Φ_l^{j_l} is REDUCED AT LEVEL
m−1 (deg S < deg Φ_{m−1}), and the cofactor S·Φ_{m−1}^{j_{m−1}−P_{m−1}} is
a SINGLE Φ_{m−1}-digit whatever the top exponent. This is what makes the
Q-weight clause unconditional in S5.2.

**Display W-WIN (the window census — a structural display, NOT a lemma of
this note, consumed by NOTHING).** At m = 2 the RM-2 probe's L3-WINDOW leg ⟦C5⟧
measured every node inside {j₀ < 2P₀, j₁ < 2P₁} (0 violations, sealed).
At m = 2 the bound is provable by the path argument (a second consecutive
promotion into the top needs j₀ ≥ 2P₀, excluded; the only j₀-resupply — a ⟦nc⟧
level-2 correction — drops j₁ first). At m ≥ 3 the analogous ALL-LEVELS
bound j_l ≤ 2P_l − 1 is NOT proved here: a rebuild path through
intermediate-level corrections is not excluded by the m = 2 argument. Since
L-EXACT and every theorem clause below are window-free (the exit-cofactor ⟦C4⟧⟦C5⟧
remark above replaces the reduced-cofactor route), the bound stays a
display; the S9 diagnostic censuses it at m = 3 (leg D2). A measured
violation would refute only this display, no clause of the theorem. ⟦C5⟧

## S3. The line calculus and the 𝔷-kernel — PROVED, generic (the NEW displays)

### S3.1 The formal read monomial 𝔷

Fix a target β* ∈ ℤ. For a cell C = b∏Φ_l^{j_l} define the **λ-chain** and
**t-chain** top-down (λ_m := β*; l = m, …, 1):

    t_l := (j_{l−1} − s_l(λ_l)) / e_{l−1},      λ_{l−1} := (λ_l − j_{l−1}γ_l) / e_{l−1},

where s_l(·) is the eq-(12-int) s-value at the read (e_{l−1}, h_{l−1}). C
is **on-grid at β*** iff t_l ∈ ℤ for every l (equivalently j_{l−1} ≡
s_l(λ_l) mod e_{l−1}; then λ_{l−1} ∈ ℤ automatically, since γ_l ≡ h_{l−1} ⟦C30⟧
and ℓ_{l−1}h_{l−1} ≡ 1 mod e_{l−1}). For on-grid C set

    𝔷(C; β*) := ∏_{l=1}^{m} z_l^{t_l} · ∏_{l=1}^{m−1} ε_l(λ_l)   ∈ K_m^×

(z's are units; t_l of either sign is legal mid-tree; ε_l per the [ILN]†
S0.2 convention, applied to the level-l slot weight λ_l consumed by the
level-(l+1) read). The λ-chain is LINEAR in (β*, j⃗) — the load-bearing
triviality: λ_{l−1} is an affine function of λ_l and j_{l−1} with no floor
anywhere; all integrality sits in the t-chain. ⟦C6⟧

**First-failure convention [r1, PC-G5].** The chain is read top-down; if
t_l ∉ ℤ at some (largest) l, C is OFF-GRID at β* and the chain STOPS
there — s_{l−1} is never applied to a non-integer λ_{l−1} (which, past a ⟦C20⟧
failure, need not be integral), and 𝔷(C; β*) is defined only for on-grid ⟦C20⟧
C. This completes the definition PC-G5 charged as partial. By L-GRID
below, the convention is only ever exercised at foreign targets ⟦C20⟧
β* ≠ line(C), where the above-line case of every consuming lemma
governs; no clause of this note consults a partial chain.

**Lemma L-GRID (on-line ⟹ on-grid) [r1, PC-G5/PE — the vacuity lemma;
PE's two-line argument, displayed].** Every cell C = b∏Φ_l^{j_l} is
ON-GRID at its own line β* = line(C), and the λ-chain values are the
truncation lines: λ_l = line_l(b∏_{t<l}Φ_t^{j_t}) ∈ ℤ (the level-l line
with scales Γ_t^{(l)} := γ_{t+1}∏_{t<s<l}e_s). *Proof.* By the affine
recursion and Γ_t = e_{l−1}·Γ_t^{(l−1)} for t < l−1: λ_l =
e_{l−1}λ_{l−1} + j_{l−1}γ_l with λ_l, λ_{l−1} the displayed truncation
lines (integers — j's and w₀(b) are integers). Then mod e_{l−1}:
s_l(λ_l) ≡ ℓ_{l−1}λ_l ≡ ℓ_{l−1}·j_{l−1}γ_l ≡ ℓ_{l−1}j_{l−1}h_{l−1} ≡
j_{l−1} (γ_l ≡ h_{l−1} and ℓ_{l−1}h_{l−1} ≡ 1 mod e_{l−1} — the S0.1
conventions, already quoted in the on-grid display above), i.e. t_l ∈ ℤ
at every l. ∎ **Consequence:** "off-grid at β* = line(C)" is EMPTY — the
(D3) second case of DEV-(m) and RM-GEN's old "off-grid x-cells" branch
are vacuous, and the on-grid qualifiers in B-READ/L-LINE(iv) are
automatic at on-line targets (retained for emphasis). Machine shadow: ⟦C31⟧
PE's equal-line off-grid census 0 (counter C-GRID never fired, 97 engine ⟦nc⟧
calls, m = 2/3/4).

**Lemma L-ZMULT (𝔷-multiplicativity — the kernel).** Let C, C′ be on-grid
at targets β, β′ with exponent vectors j⃗, a⃗ and any coefficients. Then the
product-shape cell at exponents j⃗ + a⃗ is on-grid at β + β′, and

    𝔷((j⃗+a⃗); β+β′) = z_m^{ε̃} · 𝔷(j⃗; β) · 𝔷(a⃗; β′),
    ε̃ := ⌊(s_m(β) + s_m(β′)) / e_{m−1}⌋.

*Proof.* By linearity the λ-chains add: λ_l^Σ = λ_l + λ_l′ for every l. At
each level l, L1 ([IL]† S2, EQ12-ADD) gives s_l(λ_l + λ_l′) = s_l(λ_l) +
s_l(λ_l′) − δ_l e_{l−1} with δ_l := ⌊(s_l(λ_l)+s_l(λ_l′))/e_{l−1}⌋ ∈
{0,1}; hence t_l^Σ = t_l + t_l′ + δ_l ∈ ℤ (on-grid) and EXP-KIT(b) ([ILN]†
S3, self-contained modulo L1) gives ε_l(λ_l + λ_l′) = z_l^{−δ_l}· ⟦C7⟧
ε_l(λ_l)·ε_l(λ_l′). In the product defining 𝔷^Σ, the z_l^{+δ_l} from the
t-leg cancels the z_l^{−δ_l} from the ε-leg at every l = 1, …, m−1; the
single survivor is the TOP carry z_m^{δ_m} = z_m^{ε̃} (level m has a t-leg
and no ε-leg). ∎

(This one cancellation is why the engine's read layer needs no per-level
case analysis: every internal carry is self-curing, and exactly one
level-m carry survives — the box's ε′.)

### S3.2 Lemma L-LINE (the edge line laws — the exact quanta)

For every tree edge of S2.2, with lines as in S2.1:

* **(i) promotion at level i+1 ≤ m−1:** line(child) − line(parent) =
  Γ_{i+1}·1 − Γ_i·P_i = (γ_{i+2} − e_{i+1}P_iγ_{i+1})·∏_{i+1<t<m} e_t =
  **h_{i+1}·∏_{i+1<t<m} e_t > 0** — the exact promotion quantum. (For
  i+1 = m, the exit: line(cofactor) = line(parent) − P_{m−1}γ_m =
  line(parent) − w_mΦ_m.)
* **(ii) correction-main (L-)child, level i+1, digit d of C_k^{(i)}
  [display rewritten at r1: PE-n6 garble + PC-C1 threading].** The
  L-child of L-NORM has coefficient π^w·lift(b̄c̄_d) with w = w₀(b) +
  w₀(c_d) EXACTLY (L-NORM(a)); it is NEVER absent (L-NORM(a): residues ⟦C18⟧
  multiply in the field K₀ — the old "residue product vanishes"
  parenthetical was an artifact of the un-normalized display and is
  DELETED). Hence

      Δline = E_m·w₀(c_d) + Σ_{l<i} a_{d,l}·Γ_l − (P_i − e_ik)·Γ_i = 0,

  by scaling the d-cell's level-i on-line identity (realize structure,
  [ILN]† S4.1: E_i·w₀(c_d) + Σ_{l<i} a_{d,l}Γ_l^{(i)} = (g_i−k)γ_{i+1},
  with Γ_l^{(i)} := γ_{l+1}∏_{l<t<i}e_t) by ∏_{i≤t<m}e_t, using the three
  exact conversions E_i·∏_{i≤t<m}e_t = E_m; Γ_l^{(i)}·∏_{i≤t<m}e_t = Γ_l
  (l < i); and (g_i−k)γ_{i+1}·∏_{i≤t<m}e_t = e_i(g_i−k)·γ_{i+1}·
  ∏_{i<t<m}e_t = (P_i − e_ik)·Γ_i. Correction-main children PRESERVE the
  line exactly, always.
* **(iii) junk children [display rewritten at r1: PC-C1 through L-NORM +
  PE-n6].** The π-child has coefficient π^{w+1}X̂ with w₀ ≥ w + 1
  (L-NORM(b)), one more than the L-child's exact w, at the SAME
  exponents: Δline ≥ (ii)'s 0 + E_m·1 = E_m > 0. The Φ₀Y-child (d₀ ≥ 2
  only, L-NORM(c)) has coefficient w₀ ≥ w and exponent +1 at level 0: ⟦nc⟧
  Δline ≥ 0 + Γ₀ = Γ₀ > 0. For the level-1 identity (i = 0) the
  correction-main children carry the explicit depth π^{(g₀−k)h₀} and
  Δline = E_m·(g₀−k)h₀ − (P₀ − e₀k)·Γ₀ = 0, since Γ₀ = h₀·∏_{0<t<m}e_t =
  h₀E_m/e₀ and P₀ − e₀k = e₀(g₀−k); their junk siblings then gain ≥ E_m
  (π) resp. ≥ Γ₀ (Y) exactly as above.
* **(iv) reduced cells:** w_m(C) = line(C) EXACTLY (single nested digit:
  induction on m; a single digit's weight is exact at every level, no
  cancellation possible), and C is read at β* = line(C) as res(b̄)·𝔷(C;β*)
  if on-grid, 0 if off-grid; at β* < line(C) it reads 0 (above the line,
  READ-ADD-(m)). [This is L-CELL(ii) at a single cell.] [r1, PE: by
  L-GRID the off-grid branch at β* = line(C) is EMPTY; the clause stands
  as stated, vacuously true in that branch.]

*Proof of the arithmetic [r1: trimmed per PE-n6 — the (ii)/(iii)
computations now sit fully displayed inside their bullets].* (i): with
the convention Γ_l = γ_{l+1}∏_{l<t<m}e_t:
Γ_{i+1} − P_iΓ_i = ∏_{i+1<t<m}e_t·γ_{i+2} − P_iγ_{i+1}∏_{i<t<m}e_t =
∏_{i+1<t<m}e_t·(γ_{i+2} − e_{i+1}P_iγ_{i+1}) = ∏_{i+1<t<m}e_t·h_{i+1},
using γ_{i+2} = e_{i+1}w_{i+1}Φ_{i+1} + h_{i+1} and w_{i+1}Φ_{i+1} =
P_iγ_{i+1} (the S0.1 weight recursion). (ii)/(iii): displayed in the
bullets, through L-NORM. ∎

**Corollary L-MONO (line monotonicity).** Along every tree edge,
line(child) ≥ line(parent), with equality exactly on correction-main
children; promotions rise by the exact (i)-quantum. Hence every cell
reachable from an initial cell has line ≥ λ + γ_{m+1} (initial product
cells: line = line(x-cell) + line(D-cell) ≥ λ + γ_{m+1}, the line being
additive under S2.1's product formation since w₀ is a valuation), and
**the on-line skeleton at target λ + γ_{m+1} is PROMOTION-FREE and ⟦C24⟧
junk-free: carry depth 0.** ∎ ⟦C24⟧

## S4. Termination: the generic measure — PROVED on the T-scope [r1: amended, d₀ = 1 flats included]; the d₀ ≥ 2 flat corners fenced

### S4.1 Lemma L-MU (the generic linear measure) [statement STRENGTHENED at r1 per PE-F1; the composed statement required e_i ≥ 2 at all 1 ≤ i ≤ m−1 — the extension to d₀ = 1 flat shapes is argued in full below]

Let m ≥ 1 and suppose **(a) e_i ≥ 2 for all 1 ≤ i ≤ m−1 (any d₀), OR
(b) d₀ = 1 (any e_i ≥ 1)**. Then there exist positive rational weights
w₀, …, w_{m−1} with, for μ(C) := Σ_l w_l·j_l:

    (A_i)  w_i < w_{i−1}·P_{i−1}                    (1 ≤ i ≤ m−1)
    (B_i)  w_i·e_i > Σ_{l<i} w_l·ρ_l,   ρ₀ := P₀ − 1 + [d₀ ≥ 2],
                                        ρ_l := P_l − 1 (1 ≤ l < i)
                                                    (1 ≤ i ≤ m−1)

and μ then descends STRICTLY on every in-tree edge of S2.2 provided also
(e₀ ≥ 2 ∨ d₀ = 1) — the combined scope is exactly T-scope(m) as amended:

* promotion at level i+1 ≤ m−1: Δμ = w_{i+1} − w_iP_i < 0 by (A_{i+1});
  Φ_m-exit: Δμ ≤ −w_{m−1}P_{m−1} < 0;
* correction (all three L-NORM children) at level i+1, i ≥ 1 [display
  cleaned at r1, PE-n6]: the consumed level-i mass is P_i − e_ik =
  e_i(g_i−k) ≥ e_i (k ≤ g_i−1), and the dumped mass at level l < i is
  ≤ P_l − 1 (reduced C_k-digits), +1 at l = 0 on the Y-child (absent at
  d₀ = 1) — i.e. ≤ ρ_l per level; hence
  Δμ ≤ −w_i·e_i + Σ_{l<i} w_l·ρ_l < 0 by (B_i);
* correction at level 1 (i = 0): Δj₀ ≤ e₀(g₀−1) − P₀ + [d₀ ≥ 2] =
  −e₀ + [d₀ ≥ 2] ≤ −1 when e₀ ≥ 2 or d₀ = 1: Δμ ≤ −w₀ < 0. (At
  e₀ = 1 ∧ d₀ ≥ 2 the Y-child does not descend — the [IL3]† DEV-1 fenced
  corner, one level up, reproduced not repaired.) ⟦C8⟧

*Proof of existence [rewritten at r1: the (a)-branch display was garbled
(PE-n6); the (b)-branch is NEW (PE-F1)].* Choose w₀ := 1. (B_i)∧(A_i)
ask for w_i ∈ ( S_i/e_i , w_{i−1}P_{i−1} ), where S_i := Σ_{l<i} w_lρ_l
(so S_i = S_{i−1} + w_{i−1}ρ_{i−1}, S₁ = ρ₀).

**Branch (a): all e_i ≥ 2.** Induction with invariant (B_{i−1}). The
interval is nonempty iff w_{i−1}P_{i−1}·e_i > S_i. Since e_i ≥ 2 and
ρ_{i−1} ≤ P_{i−1}: w_{i−1}P_{i−1}e_i ≥ 2w_{i−1}P_{i−1} > S_{i−1} +
w_{i−1}P_{i−1} ≥ S_i, PROVIDED w_{i−1}P_{i−1} > S_{i−1} — which holds
because P_{i−1} ≥ e_{i−1} and (B_{i−1}) gives w_{i−1}e_{i−1} > S_{i−1}.
Base i = 1: (S₁/e₁, w₀P₀) = (ρ₀/e₁, P₀) is nonempty since e₁ ≥ 2 and
ρ₀ ≤ P₀ give ρ₀/e₁ ≤ P₀/2 < P₀. Pick any rational point (e.g. the
midpoint); (B_i) is then the invariant for the next step.

**Branch (b): d₀ = 1, arbitrary e_i ≥ 1 — THE FLAT EXTENSION.** At
d₀ = 1, ρ₀ = P₀ − 1 < P₀ — the base has SLACK, and the slack propagates
through near-ceiling choices. Use the smaller (uniform) interval

    w_i ∈ ( S_i , w_{i−1}P_{i−1} )      (note S_i ≥ S_i/e_i, so any such
                                         w_i satisfies (B_i): w_ie_i ≥
                                         w_i > S_i; and (A_i) directly),

with the invariant **w_i > S_i**. Nonemptiness: (S_i, w_{i−1}P_{i−1}) ≠ ∅
⟺ w_{i−1}P_{i−1} > S_i = S_{i−1} + w_{i−1}(P_{i−1} − 1) ⟺ w_{i−1} >
S_{i−1} — the invariant one step down. Base: at i = 1 the interval is
(S₁, w₀P₀) = (P₀ − 1, P₀), nonempty. Choosing w_i := the
midpoint of (S_i, w_{i−1}P_{i−1}) at every i maintains w_i > S_i, and NO
lower bound on any e_i was used — flat levels e_i = 1 are covered, and
promotions at flat levels descend STRICTLY (w_i < w_{i−1}P_{i−1} is
strict). ∎ In both branches μ takes values in (1/D)·ℤ_{≥0} for a common
denominator D, so strict descent is well-founded. ∎

**Necessity display (the corrected fence mechanism) [r1, PE-F1 — this
replaces the arithmetically FALSE unsatisfiability sentence that fenced
the d₀ = 1 flats: the composed box used ρ_{i−1} = P_{i−1} against the
note's own ρ = P − 1 and an undefined symbol W_{i−1}].** At d₀ ≥ 2 the
linear family genuinely dies at flat levels: if e_i = 1 for some
1 ≤ i ≤ m−1, then (B_i) reads w_i > S_i, which with (A_i) forces
w_{i−1}P_{i−1} > S_i = S_{i−1} + w_{i−1}(P_{i−1}−1), i.e. w_{i−1} >
S_{i−1}; with (A_{i−1}) this cascades down (w_{l−1}P_{l−1} > w_l > S_l
forces w_{l−1} > S_{l−1} by the same rearrangement) and bottoms out at
w₀P₀ > S₁ = w₀ρ₀, i.e. ρ₀ < P₀, i.e. **d₀ = 1**. So: the (A_i)/(B_i)
system with a flat level is satisfiable IFF d₀ = 1 — at d₀ ≥ 2 the
fence's conclusion stands (machine: PE's F2-e1flat-d2 and F3-e2flat-d2
probes INFEASIBLE on the full strict system); at d₀ = 1 the composed
fence was false, and branch (b) proves the corner instead.

**Consistency display (the sealed m = 2 instance + the r1 flat
witnesses).** w₀ = 2e₁, w₁ = P₀(e₁+1) satisfies (A₁): P₀(e₁+1) < 2e₁P₀
⟺ e₁ ≥ 2, and (B₁): P₀e₁(e₁+1) > 2e₁ρ₀ (ρ₀ ≤ P₀) ⟺ e₁+1 > 2 ⟺ e₁ ≥ 2 —
the RM-2 probe's sealed measure μ = e₀g₀(e₁+1)j₁ + 2e₁j₀ is a point of
the (a)-branch family, and its measured scope (strict exactly on e₀ ≥ 2
∧ e₁ ≥ 2, flat promotions at e₁ = 1) is the (a)-branch prediction on the
nose; at e₁ = 1 THAT point exits the family while the (b)-branch points
remain — PE's machine witnesses, all strict on the full system: (1, 3/2)
at m = 2, e₁ = 1, d₀ = 1 (M2C actual tree: 0 non-descending edges);
(1, 5/4, 19/8) at the fence box's own case m = 3, e₂ = 1, d₀ = 1 (M3C
actual tree: 88 nodes, 16 level-3 corrections, 0 non-descending);
(1, 3/2, 1) mid-flat; (1, 3/2, 5/4) double-flat; (1, 5/4, 149/64,
591/128) at m = 4 top-flat.

### S4.2 THEOREM T-TERM (termination) + the remaining flat corners [re-scoped at r1, PE-F1]

On **T-scope(m) = d₀ = 1 ∨ (e₀ ≥ 2 ∧ e_i ≥ 2, 1 ≤ i ≤ m−1)** [the r1
amended scope; strengthening argued in full at S4.1 branch (b)], the S2.2
tree on any finite set of window cells terminates (L-MU: strict descent
of a well-founded measure, branching finite). ∎

**Remark L-LEX (retained as a remark; SUBSUMED at r1).** The composed
note carried a lexicographic supplement for the single case m = 2,
e₁ = 1 ∧ d₀ = 1: σ(C) := Σ_{l<m}(m−l)·j_l drops on every promotion
(Δσ = −P_i(m−i) + (m−i−1) < 0) and on the exit, so (μ, σ) with a
promotion-flat μ terminates that shape. Under the r1-extended L-MU this
supplement is UNNECESSARY — branch (b) weights make every edge of every
d₀ = 1 flat shape strictly μ-descending directly (PE: even at M2C the
plain family is strict; witness (1, 3/2), 0 non-descending edges). The
σ-observation stays on record as a remark (a valid tiebreak mechanism,
consumed by nothing). ⟦C29⟧

**THE r1 EXTENSION OF THE THEOREM CLAUSES (DEV-(m)/RM-GEN on the d₀ = 1
flat corners) — the inspection census.** T-TERM above extends by S4.1(b).
The remaining question for the four box clauses is whether ANY other leg
of the proof chain consumes e_i ≥ 2. Census of the chain, displayed:
S2.1 (cells, lines, L-CELL, L-NORM: valuation + division arithmetic only, ⟦C27⟧
plus READ-ADD-(m−1) in L-CELL(ii)'s proof — an [ILN]† class-level fact,
e ≥ 1-safe, covered by the S5.1 bullet below [r3, PE3-N-2: the caveat
displayed so the gloss is literal] — every formula is e ≥ 1-safe); S2.2
(the identities and children: consumed
mass e_i(g_i−k), promotion/exit inventory — e ≥ 1-safe; L-EXACT is
scope-free); S3.1 (λ/t-chains, L-GRID, L-ZMULT: the congruences are mod ⟦C25⟧
e_{l−1} and trivialize at e = 1 — s ≡ 0, δ = 0, every carry vanishes);
S3.2 (L-LINE: the promotion quantum h_{i+1}∏e_t ≥ 1 needs only h ≥ 1, ⟦C27⟧
the class pin); S5.1 (DEV-(m): the induction carrier is T-TERM — now
extended; the consumed identities READ-ADD-(m), realize (R3),
ψ_i(z_{i+1}) = 0, EXP-KIT(b), L1 are [IL]†/[ILN]† CLASS-level facts, and
the S0.1 class pin imposes gcd(e_j, h_j) = 1 only — the [ILN]† head ⟦C27⟧
declaration grades REALIZE/WELL-DEF "unconditional-generic"; at a flat
level the level-i read degenerates, s_i ≡ 0, ε_i ≡ 1, and every displayed
identity holds in the degenerate form); S5.2 (RM-GEN: B-READ + L-ZMULT +
DEV-(m), nothing new). NO leg other than L-MU consumed e_i ≥ 2. Hence ⟦C23⟧⟦C27⟧
**DEV-(m) and THEOREM RM-GEN hold on the amended T-scope(m), same
consumption manifest** — graded PROVED, the same attempt grade as the
rest of the theorem. Instance shadow: PE ran the four clauses + tree +
LAW censuses on the actual flat towers M2C/M3C with 0 exceptions, and
this unit's r1 diagnostic leg D7 (S9) runs the full predicted-0 battery
on three d₀ = 1 flat shapes (flat-top, flat-mid, double-flat) at m = 3.

**FENCED OPEN CORNER (re-scoped at r1 per PE-F1: d₀ ≥ 2 ONLY — ⟦C33⟧
displayed, priced, not proved):**

    ┌────────────────────────────────────────────────────────────────────┐
    │ TERMINATION IS OPEN on:  d₀ ≥ 2  ∧  ( e₀ = 1  ∨                    │
    │   ∃ i with 1 ≤ i ≤ m−1: e_i = 1 ).                                 │
    │ Mechanism of the obstruction (corrected at r1 — the composed box   │
    │ misused ρ = P and an undefined W_{i−1}; PE-F1): at d₀ ≥ 2 with a   │
    │ flat level i, the (A_i)/(B_i) system is INFEASIBLE — the S4.1      │
    │ necessity cascade bottoms out at ρ₀ < P₀ ⟺ d₀ = 1 — so NO linear   │
    │ measure of the L-MU family terminates these shapes; the composed   │
    │ "a level-(i+1) correction can genuinely RAISE any linear μ"        │
    │ sentence is DELETED (false at d₀ = 1, where branch (b) weights     │
    │ make all edges strict; at d₀ ≥ 2 the correct statement is the      │
    │ family-infeasibility just displayed). At e₀ = 1 ∧ d₀ ≥ 2 the junkΦ │
    │ chain is the DEV-1 fenced corner verbatim, one level up.           │
    │ STATUS of the four CLAUSES there: RM2F e₀ = 1 ∧ d₀ = 2: UNTESTED — │
    │ inputs dodge the corner, on record; UNPROVED. PRICE: a             │
    │ non-linear/filtration measure (the [IL3]† repair-route genre:      │
    │ secondary induction on target-relative π-depth with a truncation   │
    │ argument for the division bookkeeping) — one dedicated unit; a     │
    │ collapse route (contracting a flat level out of the tower) remains │
    │ the plausible alternative at flat d₀ ≥ 2. Neither executed here.   │
    └────────────────────────────────────────────────────────────────────┘

(Every consumption of termination below carries the T-scope pin — now the
amended scope. The theorem's scope is exactly T-TERM's; no clause
silently widens it.)

## S5. The read layer and THE THEOREM — PROVED, generic, on the T-scope

### S5.1 Lemma DEV-(m) (the master reduction with read transparency)

On T-scope(m), for every cell G = b∏Φ_l^{j_l} with line(G) ≥ β*, define
(R₀(G), Q(G)) by the S2.2 tree recursion (well-founded by T-TERM):
R₀(leaf) := leaf, and at a reduction R₀(G) := Σ_children ±R₀(child),
Q(G) := Σ ±Q(child) + [exit cofactors]. Then:

(D1) G = R₀(G) + Q(G)·Φ_m (L-EXACT), deg R₀(G) < deg Φ_m;
(D2) w_m(R₀(G)) ≥ line(G), and w_m of every exit cofactor of Q(G) equals
     its line ≥ line(G) − w_mΦ_m;
(D3) [READ TRANSPARENCY]  R_{m,β*}(R₀(G)) =
       res(b̄)·𝔷(G; β*)   if line(G) = β* and G is on-grid at β*,
       0                  if line(G) > β* or G is off-grid at β*.
     [r1, PE/PC-G5: by L-GRID the on-grid qualifier in the first branch
     is AUTOMATIC and the off-grid disjunct in the second is VACUOUS — ⟦C31⟧
     off-grid occurs only at foreign targets β* ≠ line(G), where the ⟦C20⟧
     line(G) > β* branch governs every consultation below (targets only ⟦C20⟧
     ever sit ≤ the lines of the cells consulted). Statement unchanged;
     the proof's off-grid case is now the emptiness observation.]

*Proof.* Induction on μ (T-scope). **Leaf:** L-LINE(iv) is exactly
(D1)–(D3). **Reduction at level i+1, correction/junk/promotion children:**
(D1) is L-EXACT stepwise. (D2): children lines ≥ line(G) (L-MONO), leaves'
w_m = their lines, sums develop slotwise (w of a sum of digits at one
abscissa ≥ min — the S0.2 base facts); exit cofactors: the S2.2 structural
remark makes each a single Φ_{m−1}-digit, so w_m(cofactor) =
e_{m−1}·w_{m−1}(S) + j′γ_m with S reduced at level m−1, = its line
exactly, and the exit's line = line(parent) − w_mΦ_m (L-LINE(i)).
(D3): every child has w_m(R₀(child)) ≥ line(child) ≥ β* by (D2)+L-MONO, so ⟦C9⟧
reads add (READ-ADD-(m), [ILN]† S4.1) and R_{m,β*}(R₀(G)) =
Σ ±R_{m,β*}(R₀(child)) = Σ ± [IH read formulas]. Three cases:

* line(G) > β*: every child line > β*: all IH reads 0. ✓
* line(G) = β*, G off-grid: EMPTY by L-GRID (every cell is on-grid at
  its own line) — nothing to prove. [r1, PC-G5/PE: this replaces the ⟦nc⟧
  composed argument, which appealed to a converse-integrality direction
  of L-ZMULT that L-ZMULT does not state (PC-G5); that appeal is
  DELETED. Machine shadow: PE's equal-line off-grid census 0.] ✓
* line(G) = β* (on-grid automatic, L-GRID) — **the telescopes**: ⟦C31⟧

  **T-tele(1) (i = 0, the standard-lift identity).** The promotion child
  is above-line by exactly Γ-quantum h₁·∏_{1<t<m}e_t (L-LINE(i)): reads 0.
  Junk children: above-line: 0. Correction-main child (k): coefficient
  b·lift(ψ₀,k)·π^{(g₀−k)h₀}, residue b̄·ψ̄₀,k, exponents (j₀−P₀+e₀k,
  j₁, …), on-line (L-LINE(ii)); its λ-chain agrees with G's at every level
  ≥ 2 (only j₀ changed), and t₁ shifts by k − g₀. IH: ⟦nc⟧

      R_{m,β*}(R₀(G)) = − Σ_k ψ̄₀,k · res(b̄) · 𝔷(G)·z₁^{k−g₀}
                      = −res(b̄)𝔷(G)·z₁^{−g₀}·(ψ₀(z₁) − z₁^{g₀})
                      = res(b̄)·𝔷(G; β*)      [ψ₀(z₁) = 0]. ✓

  **T-tele(i+1), 1 ≤ i ≤ m−1 (the canonical identities — the box's
  resistance (ii), closed).** Promotion child (i+1 ≤ m−1): above-line by
  h_{i+1}·∏_{i+1<t<m}e_t: reads 0; at i+1 = m the exit contributes nothing ⟦nc⟧
  to R₀. Correction-(k, d) main child: exponents j⃗ + a⃗_d at levels < i,
  j_i − (P_i − e_ik) at level i; residue res(b̄)·res(c̄_d) [r1, PC-C1:
  this is L-NORM(a) — the L-child's normalized residue is EXACTLY the
  product of the parents' normalized residues, its coefficient's w₀ is
  exactly the sum, and it is never absent; the IH applies to it with the ⟦C18⟧
  same b̄-convention as B-READ's]. Its λ/t-chains:
  levels > i+1 unchanged; t_{i+1} shifts by k − g_i; λ_i rises by exactly
  β_k := (g_i−k)γ_{i+1} (linearity: Δλ_i = −Δj_i·γ_{i+1}/e_i =
  (P_i−e_ik)γ_{i+1}/e_i = β_k). So the child's level-≤i read data is the
  product-shape (G's level-≤i part at target λ_i) ⊕ (the d-cell at target
  β_k), and L-ZMULT AT LEVEL i gives

      𝔷(child; β*) = ε_i(λ_i + β_k)·z_{i+1}^{t_{i+1}+k−g_i}·[levels > i+1
                      factors of 𝔷(G)]·z_i^{ε̃_k}·𝔷_{≤i}(G-part; λ_i)·
                      𝔷_{≤i}(d-cell; β_k) / ε_i(λ_i)·…

  — organized: summing the IH reads over the d-cells of C_k^{(i)} first,
  B-READ on C_k^{(i)} (all cells on-line on-grid, L-CELL/S2.1) assembles
  Σ_d res(c̄_d)·𝔷_{≤i}(d; β_k) = R_{i,β_k}(C_k^{(i)}) = τ_k :=
  ε_i(β_k)^{−1}·ψ_{i,k} (realize (R3), [ILN]† S4.1). Hence

      R_{m,β*}(R₀(G)) = − Σ_k [upper factors]·ε_i(λ_i+β_k)·
                          z_i^{ε̃_k}·τ_k·z_{i+1}^{t_{i+1}+k−g_i}·
                          res(b̄)·𝔷_{≤i}(G-part; λ_i),
      ε̃_k := ⌊(s_i(λ_i) + s_i(β_k))/e_{i−1}⌋.

  EPS-MULT (EXP-KIT(b) at level i) gives ε_i(λ_i+β_k)·z_i^{ε̃_k} =
  ε_i(λ_i)·ε_i(β_k), and ε_i(β_k)·τ_k = ψ_{i,k} — **the ε-carry /
  canonical-lift cancellation, now at every level and every k**. The sum
  collapses exactly as at level 1:

      = −[upper]·ε_i(λ_i)·z_{i+1}^{t_{i+1}−g_i}·res(b̄)𝔷_{≤i}(λ_i)·
         Σ_k ψ_{i,k} z_{i+1}^{k}
      = −[…]·z_{i+1}^{t_{i+1}−g_i}·(ψ_i(z_{i+1}) − z_{i+1}^{g_i})·[…]
      = res(b̄)·𝔷(G; β*)              [ψ_i(z_{i+1}) = 0]. ✓ ∎

(At m = 1 there are no canonical identities, the promotion IS the exit,
and DEV-(1) with T-tele(1) is [IL3]† DEV-1's content re-derived — same
children, same ψ₀(z₁) = 0 collapse, the μ of S4 restricted to w₀ = 1
being j₀-descent. The e₀-scope agreement is exact: (e₀ ≥ 2 ∨ d₀ = 1).)

### S5.2 THEOREM RM-GEN (the generic engine)

**THEOREM.** For every m ≥ 1, on the standard-lift class with g_m = 1 and
T-scope(m) = d₀ = 1 ∨ (e₀ ≥ 2 ∧ e_i ≥ 2 for 1 ≤ i ≤ m−1) [r1, PE-F1:
the amended scope — the d₀ = 1 flat corners join by S4.1 branch (b) +
the S4.2 inspection census; a strengthening]: the canonical
division f·Ĉ_m = R + Q·Φ_m satisfies ALL FOUR box clauses of S0.2:
(C-W_R), (C-W_Q), (C-READ) with the exact factor z_m^{ε′}·v·τ_m, and
(C-EPS).

*Proof.* Decompose per S2.1: initial cells (x-cell, D-cell) with product
lines line(x) + γ_{m+1} ≥ λ + γ_{m+1} =: β* (D-cells all on-line at
γ_{m+1}). Run the tree; T-TERM terminates it; L-EXACT + L-CANON identify
(R₀, ΣQ) = (R, Q). **(C-W_R):** R = Σ over initial cells of R₀(cell),
each with w_m ≥ line ≥ β* (DEV-(m)(D2)); developments add. **(C-W_Q):**
every exit cofactor has w_m = its line ≥ (its tree-root initial cell's
line) − w_mΦ_m ≥ β* − w_mΦ_m (DEV-(m)(D2) + L-MONO); Q is their sum.
**(C-EPS):** L1 at the (e_{m−1}, h_{m−1}) read. **(C-READ):**
R_{m,β*}(R) = Σ_{initial cells} ±(DEV-(m)(D3) reads); the junk-split
initial children are above-line (0); above-line x-cells give above-line
products (0) [r1, PE: the composed "off-grid x-cells" branch is DELETED
— by L-GRID an x-cell with line = λ is on-grid at λ, so no such cells
exist; the L-ZMULT-integrality appeal it made is gone with it]; the
surviving initial cells are
(on-line x-cell, on-grid by L-GRID) × (D-cell), reading z_m^{ε̃}·res(b̄_x)𝔷(x; λ)·
res(c̄_D)𝔷(D; γ_{m+1}) by L-ZMULT with ε̃ = ⌊(s_m(λ)+s_m(γ_{m+1}))/
e_{m−1}⌋ = ε′ UNIFORMLY (all on-line x-cells share the target pair
(λ, γ_{m+1})). Summing (B-READ both factors):

    R_{m,β*}(R) = z_m^{ε′} · [Σ_x res(b̄_x)𝔷(x;λ)] · [Σ_D res(c̄_D)𝔷(D;γ_{m+1})]
                = z_m^{ε′} · v · τ_m.  ∎

**Scope remarks.** (i) At m = 1 the theorem reproduces the (RM-1) scope of
record exactly ([IL3]†'s "proved for e₀ ≥ 2, and at e₀ = 1 ∧ d₀ = 1"): an
independent generic re-derivation. (ii) [rewritten at r1 per PE-F3 — the
composed remark held the v = 0 case open as "legal"; it is in fact
EMPTY:]

  **Lemma L-VNZ (the read at the exact weight never vanishes).** For ⟦C19⟧
  0 ≠ f ∈ O[x] with deg f < deg Φ_m and λ := w_m(f): v := R_{m,λ}(f) ≠ 0.
  *Proof (induction on m; base m = 0: for 0 ≠ f of deg < d₀ the level-0
  read at w₀(f) is res(f/π^{w₀(f)}) ≠ 0 in the field K₀).* The level-m read
  at λ consults the Φ_{m−1}-digits f_j (deg f < deg Φ_m gives 0 ≤ j ≤
  P_{m−1} − 1) at the grid abscissae j = s_m(λ) + t·e_{m−1}; since
  0 ≤ s_m(λ) < e_{m−1} and j ≤ e_{m−1}g_{m−1} − 1, the slot index t
  ranges in 0 ≤ t ≤ g_{m−1} − 1. The read is Σ_t (unit)·
  R_{m−1,slot}(f_j)·z_m^t — a polynomial in z_m of degree < g_{m−1} =
  deg ψ_{m−1} = [K_m : K_{m−1}] with coefficients in K_{m−1}. At least
  one coefficient is nonzero: some digit ACHIEVES the min in λ = w_m(f)
  = min_j (e_{m−1}·w_{m−1}(f_j) + jγ_m) (L-CELL(i) / the S0.2 mirror
  recursion); for that j the slot congruence holds automatically (λ ≡ ⟦C31⟧
  jγ_m ≡ jh_{m−1} mod e_{m−1} — the L-GRID congruence) and its slot
  weight EQUALS w_{m−1}(f_j), so its coefficient is R_{m−1,w(f_j)}(f_j)
  ≠ 0 by the IH (deg f_j < deg Φ_{m−1}, f_j ≠ 0), times ε/z units ∈
  K_{m−1}^×. A nonzero K_{m−1}-polynomial in z_m of degree < [K_m :
  K_{m−1}] is nonzero in K_m. ∎ So the box NEVER receives v = 0 (residue ⟦C19⟧
  cancellation cannot reach the exact weight — deg f < deg Φ_m caps the
  read at ≤ g_{l−1} slots per level, PE-F3's mechanism, now displayed);
  the composed remark's "v = 0 is legal" clause was dead weight. The
  weight clauses still never consume residue survival (C10 — now ⟦C10⟧
  supported vacuously as well). Machine shadow: v = 0 realized 0/97 (PE)
  and 0/319 (S9 D1).

(iii) The g_m = 1 hypothesis enters only through Ĉ_m's existence ⟦C11⟧
(WELL-DEF-(n)(ii)); the tree and DEV-(m) are g_m-blind. (iv) OFF T-scope
— now d₀ ≥ 2 ∧ (e₀ = 1 ∨ ∃ flat level) [r1] — the four clauses are open
(the S4.2 fence), untested at RM2F on record.

## S6. The second-round clause: the level-m uniform carry law — STATED at every m, NOT PROVED (the exact conjecture perimeter)

### S6.1 LAW-DCX-(m+1) — the general-m form, displayed

Setting: the rung-(m+1) engine call sites ([ILN]† S4.4–S4.7): a g_m = 1
tower, pairs (γ, γ′) with iterated splits s_l, s_l′, the IH exports
E₁ᴵᴴ := quo(â·â′, Φ_m) (â the sub-Φ_m anchor part), and the second-round
overflow locus DCX_{m+1} := {E₁ᴵᴴ ≠ 0 ∧ Q¹ := quo(E₁ᴵᴴ·Ĉ_m, Φ_m) ≠ 0 ∧
the track clause} per the [ILN]† S4.7 definition. Define the **descending
weight chain of Ĉ_m's principal digit** (realize's own recursion, so each
u_l ∈ ℤ_{≥0}):

    u_m := γ_{m+1};    u_{l−1} := (u_l − s_l(u_l)·γ_l)/e_{l−1}   (l = m, …, 2),

the **pools and carries** (c₀ := 0):

    pool_1 := (s₁+s₁′) + s₁(u₁);           c₁ := ⌊pool_1/e₀⌋;
    pool_l := (s_l+s_l′) + s_l(u_l) + c_{l−1};  c_l := ⌊pool_l/e_{l−1}⌋
                                                    (2 ≤ l ≤ m−1),

and TRACK := (s_{m+1} = s_{m+1}′ = e_m − 1) ∧ E₁ᴵᴴ ≠ 0. **THE LAW:**

    (LAW-DCX-(m+1))    DCX_{m+1}  =  TRACK  ∧
                       (s_m+s_m′) + s_m(u_m) + c_{m−1}  ≥  2·e_{m−1}.

**Specialization audit (both anchors exact):** at m = 1 (c₀ = 0) the law
reads T₁ + s₁(γ₂) ≥ 2e₀ on TRACK — [IL3]†'s PROVED sharp DCX₂ form on the
nose. At m = 2 it reads (s₂+s₂′) + s₂(γ₃) + ⌊(s₁+s₁′+s₁(u₁))/e₀⌋ ≥ 2e₁
with u₁ = (γ₃ − s₂(γ₃)γ₂)/e₁ — THE UNIFORM CARRY LAW v2 verbatim
(u₁ = the DCX3 note's u₀; SEALED-CONFIRMED AT CONJECTURE GRADE,
0/11,808 + 0/3,320-minus-40-then-cured + the RM-2 102/102 refit; consumed
here as that sealed conjecture, its grade unchanged by this note). ⟦C12⟧

**STATUS, per m (the honesty split):** m = 1 PROVED ([IL3]† S4.7);
m = 2 SEALED-CONFIRMED, conjecture grade (the DCX3 unit; the head
declaration paste); m ≥ 3 CONJECTURE — first instance data ever produced
by this unit's S9 diagnostic (leg D5, m = 3). This note does NOT derive
the law generically; the seal does NOT upgrade.

### S6.2 What IS proved generically about the law (the depth mechanism)

* **Lemma L-CAP (carries cap at 2).** pool_1 ≤ 2(e₀−1) + (e₀−1) ⟹ c₁ ≤ 2;
  inductively pool_l ≤ 2(e_{l−1}−1) + (e_{l−1}−1) + 2 = 3e_{l−1} − 1 ⟹
  c_l ≤ 2 for every l (trivial at e_{l−1} = 1: s ≡ 0, pool = c_{l−1} ≤ 2).
  So the law's carry chain has depth ≤ 2 at EVERY level — the DCX3 "depth
  ≤ 2 structurally" display generalized. PROVED. ∎
* **The crossing law (PROVED) and the floor-form SHAPE (HEURISTIC) —
  regraded at r1 per PC-G2 (the composed bullet presented the whole as a
  derivation; the honest split follows).** PROVED (by the S2.2 child
  inventory alone): exponent mass crosses from level l−1 to level l ONLY ⟦C17⟧
  along the promotion edge, P_{l−1} units consumed per unit delivered —
  promotions are the only child type raising a higher-level exponent, ⟦C17⟧
  corrections dump strictly below their reduction level, exits leave the
  tree; and each promotion carries μ strictly down (L-MU — a T-scope-
  pinned citation: the composed bullet cited L-MU without the pin inside
  a generic-sounding derivation, PC-G2's leak, fixed here) and the line
  up by the exact quantum (L-LINE(i), scope-free). HEURISTIC (NOT ⟦C25⟧
  derived in this note): the identification of the on-line level-(l−1)
  mass with the pool (s_l + s_l′) + s_l(u_l) + c_{l−1} — no induction
  connecting the actual second-round quotient cells of Q¹ to these
  pools is displayed; that correction/rebuild paths contribute exactly
  the recursive c_{l−1} is unproved; and no support-containment argument
  separates the mass bookkeeping from residue survival. Hence "the
  maximal promotion count into level l is ⌊pool_l/e_{l−1}⌋" is the law's
  MOTIVATION at conjecture support: the DCX3 seal's depth-exact legs
  (C2/C3: deficit-k overflow carried entirely at depth ≥ k, 467/467
  rows; direct rows clean-visible 241/241), the S9 D5 first m = 3 data
  (51/51 chain-fed live rows), and PE's 2,208-pair fresh census incl.
  the first LAW-DCX-5 data — instance evidence, not proof. What is also
  NOT derived, as before: that the promoted mass ARRIVES with nonzero
  residue (Q¹ ≠ 0 exactly on the locus), the conjectural content at
  every m ≥ 2.

## S7. Consequences (the exact upgrades, and the one that does NOT fire)

### S7.1 COR-A: the ITERLAWN ledger upgrade on the T-subclass

**[LEDGER STATUS — inline at r1 per PC-G4/PE-n5: the ITERLAWN S7
row upgrade is applied AT ACCEPTANCE ONLY (S10/S11 convention; this ⟦nc⟧
note's acceptance counter is 0/2 at r1). What follows is the
MATHEMATICAL consequence display, not an applied ledger edit.]**

**[LEDGER APPLICATION — 2026-08-04, at acceptance (round 7, PC7 + PE7
both CLEAN on a0e266c). The counter is met; COR-A FIRES: on T-class(n),
the [ILN]† S7 rows 5/6/8 (RM-m) calls are DISCHARGED and (ITER-LAW-n)
holds on T-class(n) with the (RM-m) conditionality REMOVED — the
remaining conditionality of (ITER-LAW-n) on that sub-class is exactly
[ILN]†'s own non-(RM) residuals (its head declaration; nothing new).
Off T-class(n) — some called level in the d₀ ≥ 2 fenced corner — the
rows stay conditional, unchanged. The cross-note upgrade is recorded in
the ledger ([RMG] block) rather than by editing the accepted [ILN]
note, per the incorporation convention.]**

[ILN]†'s S7 rows 5/6/8 condition rung n ≥ 3 on (RM-m, 2 ≤ m ≤ n−1). By
THEOREM RM-GEN those calls are DISCHARGED whenever every called level
satisfies its T-scope, i.e. on the sub-class

    T-class(n) := d₀ = 1 ∨ (e₀ ≥ 2 ∧ e_i ≥ 2 for all 1 ≤ i ≤ n−2)

[r1: extended with the amended T-scope, PE-F1]. For n ≥ 3 this is the
EXACT union of the per-call pins over 2 ≤ m ≤ n−1 (the engine at level m
consumes reads 0..m−1 only). **The n = 1, 2 correction [r1, PC-G4]:** at ⟦C13⟧
n = 1, 2 the call family 2 ≤ m ≤ n−1 is EMPTY — no engine call exists to
discharge, the union of pins is no constraint at all, and the composed
claim that the display above was "the exact union" for EVERY n ≥ 1 was a
slip: as previously displayed the T-class still imposed (e₀ ≥ 2 ∨ d₀ =
1) at n = 1, 2, which the (empty) union does not. Corrected statement:
at n = 1, 2 the rows are the accepted [IL]†/[IL3]† statements on their
own scopes, with NO T-condition. CONSEQUENCE:
**(ITER-LAW-n) off DCX-(n) is UNCONDITIONAL on T-class(n) towers for
every n ≥ 3** (and at n = 1, 2 on the accepted [IL]†/[IL3]† scopes
verbatim) — modulo nothing but the accepted [IL]†/[IL3]†/[ILN]† packages ⟦C32⟧
riding through per their head declarations. Off T-class(n) (d₀ ≥ 2 with
a flat or e₀ = 1 level), rows 5/6/8 stay conditional on the fenced S4.2
corner. The consumption is by engine CALL, not by tower: II-3 call sites
at a flat top read (e_{n−1} = 1) call the engine at level n−1 whose OWN
T-scope concerns levels ≤ n−2 — the display above already accounts for
this (e_{n−1} does not appear in T-class(n)).

### S7.2 COND-B: (W-MULT-DCX-m) — the displayed conditional (NOT a corollary)

The chained-carry analysis did NOT close the second-round clause
generically (S6: the law is conjecture-grade at every m ≥ 2). Therefore
(W-MULT-DCX-m) STANDS OPEN, and the honest display is conditional:

    IF LAW-DCX-(m+1) is proved at level m together with a second-round
    read clause (the depth-≥1 analogue of (C-READ) for the re-entrant
    part of Q¹), THEN the [ILN]† S4.6 Case II legs close ON DCX_{m+1} as
    well, and (W-MULT-DCX-m) falls with it — the [ILN]† S4.7 carry-over
    ("a proof of (RM-m) with a second-round clause would close both
    II-legs and DCX at once"), now with the first half (RM-m, T-scope)
    in hand. The second half is the next dedicated unit's charge; its
    design input, on record: thread the S6.2 depth mechanism — the
    promotion edge carries μ (down, exactly), the line (up, exactly
    h·∏e), and the pool floors (the deficit accounting) SIMULTANEOUSLY.

## S8. THE CONDITIONALITY LEDGER + the dependency-claims table (machine-census-ready)

| # | leg | grade | rests on |
|---|---|---|---|
| 1 | L-CANON, L-CELL/B-READ, L-EXACT (cell calculus) | PROVED, generic, unconditional | division algorithm + S0.2 base facts + [ILN]† S4.1 (R1)/(R3) |
| 1a | L-NORM (π-normalization) [r1, PC-C1] | PROVED, generic, unconditional | w₀ a valuation + [IL3]† DIGIT-SPLIT on primitive inputs |
| 2 | L-ZMULT (the 𝔷-kernel) | PROVED, generic, unconditional | [IL]† L1 + EXP-KIT(b) ([ILN]† S3) |
| 2a | L-GRID (on-line ⟹ on-grid) [r1, PC-G5/PE] | PROVED, generic, unconditional | S0.1 congruences + λ-chain linearity |
| 2b | L-VNZ (v ≠ 0 forced) [r1, PE-F3] | PROVED, generic, unconditional | L-CELL(i) + field towers K_l + the L-GRID congruence |
| 3 | L-LINE / L-MONO (line quanta) | PROVED, generic, unconditional | S0.1 weight recursion + [ILN]† S4.1 realize structure + L-NORM |
| 4 | L-MU + T-TERM (termination) | PROVED on T-scope(m) [r1: AMENDED scope — d₀ = 1 ∨ (e₀ ≥ 2 ∧ all e_i ≥ 2); branch (b) NEW per PE-F1] | self-contained arithmetic + [IL3]† DIGIT-SPLIT via L-NORM (child inventory) |
| 5 | L-LEX supplement | REMARK at r1 (subsumed by row 4 branch (b); valid, consumed by nothing) | S4 |
| 6 | flat corners (termination + clauses) | **OPEN at d₀ ≥ 2 ∧ (e₀ = 1 ∨ ∃ flat) ONLY** [r1: the d₀ = 1 flats are PROVED — rows 4/7/8] | — |
| 7 | DEV-(m) (D1)–(D3) | PROVED, generic, on T-scope [r1: amended scope via row 4 + the S4.2 inspection census] | rows 1–4 + READ-ADD-(m) ([ILN]† S4.1) + ψ_i(z_{i+1}) = 0 |
| 8 | **THEOREM RM-GEN (all four clauses)** | **PROVED on T-scope(m), every m ≥ 1** [r1: amended scope] | rows 1–4, 7 |
| 9 | W-WIN window display | display only; m = 2 proved+sealed; m ≥ 3 measured (S9 D2) | nothing consumes it |
| 10 | LAW-DCX-(m+1) statement | m = 1 PROVED ([IL3]†) · m = 2 SEALED-CONFIRMED conjecture ([DCX3], a9dd275) · m ≥ 3 CONJECTURE (first data S9 D5) | — |
| 11a | L-CAP [split at r1, PC-G2] | PROVED, generic | self-contained arithmetic |
| 11b | the promotion-edge crossing law | PROVED (S2.2 inventory; the μ-descent citation carries the T-scope pin) | S2/S3/S4 |
| 11c | the floor-form pool identification | **HEURISTIC — conjecture-supported** (DCX3 C2/C3 seals, S9 D5, PE census); NOT derived [ERRATUM, post-acceptance 2026-08-04: REFUTED as literally worded — 72 instance-keyed divergences (physical promotion counts exceed the floor; the cap bounds the pool VARIABLE, not tree paths), stress seal d3950f2 / verdict d774ecb, independently reproduced set-exactly by two instruments; the CORRECTED identification PID† (cap on the full tree + attained max on the dump-free principal subtree under (STK) ∧ the law clause) is PROVED at attempt grade in `WMULTDCX_2026-08-08.md`, which supersedes this row's heuristic layer; this row was consumed by NOTHING proved (verified by all 12 arc passes), so the refutation touches no accepted clause] | — |
| 12 | COR-A (ITERLAWN upgrade on T-class(n)) | PROVED given row 8 + [ILN]†, n ≥ 3 [r1: n = 1, 2 corrected — no T-condition there]; LEDGER APPLICATION at acceptance only | S7.1 |
| 13 | COND-B ((W-MULT-DCX-m)) | **OPEN — conditional display** | S7.2 |

**Dependency-claims table — coverage arbitrated by THE MACHINE CENSUS
[r3, PC3-g2 = PE3-G-3: THE STRUCTURAL KILL of the recurring charter
defect].** Three rounds of hand censuses each claimed exhaustiveness and

================================================================
=== PASTE C: WMULTDCX_2026-08-08.md (attempt grade, W-arc 0/2 through W-r3), FULL @ 3c2ac60 ===
================================================================
# W-MULT-DCX (composer phase) — the POOL IDENTIFICATION layer of LAW-DCX-(m+1), DERIVED in its corrected one-sided form: the path ledger, the cellwise depth threshold, the dump-free cap, and the principal cascade — with the exact residual boxed as (SURV-(m+1))

Object: the pool identification of LAW-DCX-(m+1) (RMENGINE_2026-08-08.md
S6.1; S8 row 11c — HEURISTIC going in, REFUTED as literally worded by the
sealed stress unit, seal d3950f2 / verdict d774ecb: 72 instance-keyed
PID-MAX divergences).  This note states the CORRECTED identification and
proves it, layer by layer, on the two-stage E1-priority tree; the single
residual (residue survival/cancellation of the depth-graded parts) is
boxed as the open lemma (SURV-(m+1)).  Scope: the [ILN]† S0.1 class via
the RM-GEN S0.1 pin, level m ≥ 2, g_m = 1, on the standing hypotheses
(H1)–(H2) of S1 below.  Date: 2026-08-08 (composer unit of the W-MULT-DCX
arc; stress phase = WMULTDCX_STRESS_2026-08-08.md).

**Grade per layer (header summary):** W-D1–W-D7 and the assembled
corrected identification PID† — PROVED at attempt grade (composed this
unit, 0/2 CLEAN hostile passes — round 1 = PE1 NOT-CLEAN, repaired W-r1,
see the bracket below and S11; every displayed intermediate
machine-checked: `wmultdcx_derive_checks.py`, 412 traced rows = the
sealed battery's exact selection, 858 Q¹ cells, 11 predicted-0 families
ALL GREEN).  (SURV-(m+1)) — OPEN (boxed, S9).  LAW-DCX-(m+1) itself —
grade UNCHANGED (m = 1 proved, m ≥ 2 conjecture; nothing here upgrades
it).

> **[W-r1 2026-08-04 — round-1 verification + repair.]**  Pass PE1
> (fresh-context hostile; report `WMULTDCX_passPE1_report.md` +
> independent instrument `verification/openmath/wmultdcx_pe1_ext.py`,
> committed d63f8bb) returned NOT-CLEAN: 1 critical, 2 gaps, 2 notes.
> The mathematics SURVIVED: PE1 re-implemented the two-stage tree from
> scratch and reproduced the sealed 72-divergence set EXACTLY on the
> identical 412 rows, re-derived every W-D lemma, and hand-audited one
> full 28-edge trace.  The critical was PROSE, not a lemma: the S9
> obstruction paragraph asserted as derived a claim its own artifacts
> refute (all 126 traced dead rows carry ZERO Q¹ cells — observed
> deadness is path non-existence, not cancellation); it is withdrawn
> and rewritten in S9, with the flip side displayed there as the
> conjecture-grade (SURV)(ii) design lead.  Gaps repaired: W-D7(iii)'s
> two-sidedness now displays the law clause D + c_{m−1} ≥ 2e_{m−1}
> (pinned at W-D7(iii), S0, S8(b), F6, claims row); W-D2(0)'s u_l ≥ 0
> rebased to the (H3) any-d-cell argument.  Notes disclosed: the (STK)
> census denominator (302 = all-g = 1 rows only); LED_l machine
> coverage terminal-cells-only.  Nothing graded PROVED consumed the
> withdrawn claim (PE1 verified; re-confirmed at repair).  Full
> per-finding disposition: S11.

## S0. Authority, provenance, consumption, and the reconciliation verdict

Charge (RMENGINE S7.2 design input + the stress unit's S5 reading): derive
the pool identification — or, countermodel-aware, reconcile the stress
divergences, state the corrected form, and prove that.  The stress verdict
consumed: LAW lines 0/10,484 (no countermodel); PID-MAX 72 divergences
(nonzero Q¹ parts at pc_l = c_l+1, twice +2, physical depth up to 4 >
L-CAP's 2); PID-DEF GREEN part-level; PID-DIR 10/10; all 72 divergences on
LIVE rows.  **Reconciliation, proved below: the floor c_l =
⌊pool_l/e_{l−1}⌋ is the exact maximum of the promotion count on the
DUMP-FREE PRINCIPAL subtree (cap ≤ always; the max ATTAINED under
(H3) ∧ g₀ = 1 ∧ (STK) ∧ the law clause D + c_{m−1} ≥ 2e_{m−1} —
W-D6+W-D7, see S8(b)) [W-r1 2026-08-04: PE1-G1 — clause pin added]
[W-r3 2026-08-04: PE3-G-1 — the (H3) ∧ g₀ = 1 scope W-D7's attainment
consumes, added; at g₀ ≥ 2 only the P-divisor cap holds (fence F2)],
and only a
one-sided THRESHOLD on the full tree (W-D4: every Q¹ cell — not merely
every nonzero part — carries ≥ k promotions into level m−1); the
beyond-floor mass rides exactly the two channels the pools do not count —
correction dumps and non-principal d-cells (W-D6c, derived, 112/112
machine-keyed).**  Row 11c as worded is false on the full tree and true
verbatim on the dump-free principal subtree on the clause-satisfying
rows ((H3) ∧ g₀ = 1 ∧ (STK) ∧ D + c_{m−1} ≥ 2e_{m−1} — the same pins as
above [W-r3, PE3-G-1: scope completed]; on
clause-failing rows only the cap ≤ holds and the max may be over the
empty set) [W-r2 2026-08-04: PE2-G1 — this trailer sentence had escaped
the W-r1 pin].

Consumption, all read-only: RMENGINE_2026-08-08.md rows 1/1a (L-CELL,
L-NORM, L-EXACT, L-CANON), 2a (L-GRID), 3 (L-LINE/L-MONO), 4 (L-MU/T-TERM,
T-scope pin), 11a (L-CAP, display only), 11b (the crossing law — made
quantitative here); through RM-GEN S2.1: [ILN]† S4.1 REALIZE-(m) (R1),
WELL-DEF-(n)(ii), (Ĉ1)–(Ĉ3); [IL3]† DIGIT-SPLIT via L-NORM; the m = 1
anchor [IL3]† S4.6/S4.7.  Sealed artifacts (DCX3 a9dd275, stress
d3950f2/d774ecb) are instance evidence and design input only, never proof
steps.  Machine brackets cite MY runner: `verification/openmath/
wmultdcx_derive_checks.py` (md5 bea1a43d619a3c91a9a0b2f32c0920c0), output
`wmultdcx_derive_checks_output.txt` (2b5395adca632fa0c2e3564db12e668d),
results `wmultdcx_derive_checks_results.json`
(000ac228dc4aa4ea6309cf24d0a648ec); it consumes `wmultdcx_stress.py`
byte-untouched (roster, u-chain, anchors, pools, and the pid-key selection
— so the 412 rows here ARE the sealed battery's 412).

## S1. Setting, standing hypotheses, and the two-stage tree

Class and calculus: the RM-GEN S0.1 pin verbatim (O, π, w₀; reads
(e_j, h_j, g_j), gcd(e_j, h_j) = 1; γ₁ = h₀, γ_{j+1} = e_jw_jΦ_j + h_j;
P_l := e_lg_l, E_m := e₀⋯e_{m−1}, Γ_l := γ_{l+1}∏_{l<t<m}e_t; eq-(12-int)
splits s_l(·) with β = e_{l−1}·u + s·h_{l−1}, 0 ≤ s < e_{l−1}).  Level
m ≥ 2, g_m = 1, Ĉ_m the canonical correction digit (WELL-DEF-(n)(ii)).

Pair data ([ILN]† S4.4–S4.7 call sites): (γ, γ′) in the level-(m+1) window
with iterated splits s_l, s_l′ (1 ≤ l ≤ m+1) and tails u₁, u₁′; sub-Φ_m
anchors

    â := π^{u₁}·Φ₀^{s₁}Φ₁^{s₂}⋯Φ_{m−1}^{s_m},   â′ likewise;
    σ_l := s_l + s_l′;   E₁ᴵᴴ := quo(â·â′, Φ_m);
    Q¹ := quo(E₁ᴵᴴ·Ĉ_m, Φ_m);
    TRACK := (s_{m+1} = s_{m+1}′ = e_m − 1) ∧ E₁ᴵᴴ ≠ 0.

Law-side data (S6.1 verbatim): the u-chain u_m := γ_{m+1}, u_{l−1} :=
(u_l − s_l(u_l)γ_l)/e_{l−1}; pools pool_1 := σ₁ + s₁(u₁), pool_l :=
σ_l + s_l(u_l) + c_{l−1}; carries c_l := ⌊pool_l/e_{l−1}⌋; direct top mass
D := σ_m + s_m(u_m); **deficit k := 2e_{m−1} − D**.

**Standing hypotheses.** (H1) T-scope(m) (d₀ = 1 ∨ (e₀ ≥ 2 ∧ all interior
e_i ≥ 2)) — consumed ONLY for T-TERM, i.e. for the identification of the
tree outputs with the canonical divisions (W-D0); every per-path statement
below is scope-free.  (H2) g_{m−1} = 1 (with g_m = 1 from the box
setting).  Remark: on TRACK, E₁ᴵᴴ ≠ 0 forces CII_m and hence g_{m−1} = 1
through the [ILN]† (P-DS) clause — a discharge that rides the IH package
chain per the ITERLAWN S4.7 erratum (conditional at rungs ≥ 3), so (H2) is
kept EXPLICIT here; the machine class pins it outright.  (H3, only where
flagged) interior g ≡ 1: g_l = 1 for 1 ≤ l ≤ m−2 — the instrument's class
pin; g₀ is free ((H3) says nothing about g₀; the sub-case g₀ = 1 is flagged
separately).

**W-D0 (the instrument = the proof object).**  The two-stage tree
T²(γ,γ′): stage 1 is the S2.2 E1-priority tree on the single initial cell
X₀ (see W-D1); Φ_m-exits emit cofactor cells; by L-EXACT + L-CANON +
T-TERM (H1): Σ exit cofactors = E₁ᴵᴴ, Σ leaves = rem(ââ′, Φ_m).  Stage 2:
for each stage-1 exit cofactor Y and each d-cell D of Ĉ_m, the L-NORM
split of b_Y·c_D seeds children at exponents j(Y) + j(D) (+1 at level 0 on
the Y-junk child); the tree runs again; Σ second-exit cofactors = Q¹,
Σ leaves = rem(E₁ᴵᴴĈ_m, Φ_m) (L-NORM exactness + bilinearity + L-EXACT +
T-TERM).  Per-edge counters along every root-to-node path: π_l := number
of promotion edges into level l (l = 1..m−1); exits (2 on every Q¹ path,
one per stage); dmp_l := dump mass received at level l; cons_l := mass
consumed at level l; r := correction edges taken at reduction level m−1;
junk := number of π-junk/Φ₀Y-junk edges.  [Machine: DCK-CONS 0 violations
— both stage trees reassemble the canonical divisions on all 412 rows.]

**W-D1 (seed identification; the stress unit's S2 instrument caveat
discharged as a lemma).**  â is itself a reduced cell (exponents
s_{l+1} ≤ e_l − 1 ≤ P_l − 1), and the product â·â′ EQUALS the single
generalized-monomial cell

    X₀ := ( b₀, j⃗ ),   b₀ = π^{u₁+u₁′},  j_{l−1} = σ_l  (1 ≤ l ≤ m),

since the anchors are monomials in π and the Φ_l — no development, no
DIGIT-SPLIT, no re-expansion is involved.  Hence seeding stage 1 with the
FORMAL cell is not a convention but an identity, and lines add:
line(X₀) = λ + λ′ with λ := w_m(â) = E_m u₁ + Σ_l s_{l+1}Γ_l (L-LINE(iv)).
The alternative seeding (Φ-adic re-development of the product polynomial)
redistributes exponent mass by division arithmetic and fakes untracked
level crossings — exactly the smoke-phase artifact disclosed at stress S2.
∎  [DCK-SEED 0/412.]

## S2. W-D2: the u-chain and the d-cell inventory (what the pools count)

**Lemma W-D2.**  (0) The u-chain is integral: u_l ∈ ℤ for all l; indeed
eq-(12-int) gives u_l − s_l(u_l)h_{l−1} ∈ e_{l−1}ℤ, and γ_l − h_{l−1} =
e_{l−1}·w_{l−1}Φ_{l−1}, so u_l − s_l(u_l)γ_l ∈ e_{l−1}ℤ.  Moreover
u_l ≥ 0 for l ≥ 1, under (H3): by RM-GEN S2.1 every cell of Ĉ_m is
reduced, ON-LINE at γ_{m+1} and ON-GRID; under (H3) the λ-chain of ANY
d-cell equals the u-chain down to level 1 (level-0 t-freedom does not
touch λ_l for l ≥ 1 — the (ii)/(iv) forcing below), so u₁..u_m are
truncation lines of any d-cell — Ĉ_m ≠ 0 supplies one — hence ∈ ℤ_{≥0}
by L-GRID.  (u₀ enters only in (iii)/(iv), where nonnegativity of the
displayed coefficient weights is automatic.)  [W-r1 2026-08-04: PE1-G2 —
the previous argument read the u_l off THE PRINCIPAL cell, whose
existence at g₀ ≥ 2 is F3-CERTIFIED, not derived; the any-d-cell form
consumes only (H3), and every downstream consumer of u ≥ 0 (W-D6's
e-form, W-D7) already carries (H3) explicitly.]  (i) At g_{m−1} = 1 (H2): EVERY d-cell D of Ĉ_m has
j_{m−1}(D) = s_m(γ_{m+1}) = s_m(u_m) — reduced gives
j_{m−1} ≤ e_{m−1} − 1, on-grid gives j_{m−1} ≡ s_m(γ_{m+1}) mod e_{m−1}.
(ii) Under (H3): the on-grid recursion is forced level by level —
j_l(D) = s_{l+1}(λ_{l+1}) + t_{l+1}e_l with 0 ≤ t_{l+1} ≤ g_l − 1, so
t = 0 at every interior level, the cell λ-chain equals the u-chain down to
level 1, and j_l(D) = s_{l+1}(u_{l+1}) for every l ≥ 1.  (iii) If also
g₀ = 1: j₀(D) = s₁(u₁) and w₀(b_D) = u₀ := (u₁ − s₁(u₁)h₀)/e₀; at d₀ = 1,
Ĉ_m is then the SINGLE principal cell π^{u₀}∏Φ_l^{s_{l+1}(u_{l+1})}.
(iv) At g₀ ≥ 2 (under (H3)): every d-cell has j₀(D) = s₁(u₁) + t·e₀ with
0 ≤ t < g₀ and w₀(b_D) = u₀ − t·h₀; all higher exponents principal.
*Proof.*  All displayed: (0)–(iv) are the on-grid/reduced/on-line forcing
(L-GRID top-down) applied to RM-GEN S2.1's certified inventory of Ĉ_m's
cells ([ILN]† S4.1 (R1) + (Ĉ1)–(Ĉ3)).  ∎  [DCK-INV 0 violations, all 20
towers; at the four g₀ = 2 towers the (iv) inventory is exact with 2
d-cells each, principal PRESENT, ψ₀ nonzero-coefficient pattern (1,1).]

**So the pools count exactly: the two seed supplies.**  pool_l reads the
level-(l−1) exponent supplied by X₀ (= σ_l, stage 1) plus the PRINCIPAL
d-cell (= s_l(u_l), stage 2) plus the promoted-in carry c_{l−1}.  What
they do NOT count, by construction: correction dumps (a_{d,l}-mass
re-entering from reductions at higher levels, +1 at level 0 per Y-junk
edge) and the non-principal t ≥ 1 d-cells of (iv).  These two channels are
exactly the beyond-floor mechanism (W-D6c).

## S3. W-D3: the path ledger (the crossing law made quantitative)

**Lemma W-D3 (LED_l).**  Along any root-to-node path of T², for every
level 0 ≤ l ≤ m−1:

    j_l(node) = σ_{l+1} + j_l(D) + π_l + dmp_l − cons_l ,

where j_l(D) := the d-cell exponent joined at stage-2 seeding (0 in stage
1), π_l := promotion edges into level l on the path (π₀ := 0), dmp_l :=
Σ a_{d,l} over correction edges taken (+1 at l = 0 per Y-junk edge), and
cons_l := Σ consumptions at level l: P_l per promotion edge out of level l
and per Φ_m-exit (l = m−1), P_l − e_lk per correction edge taken at
reduction level l.  Every term is a non-negative integer.
*Proof.*  Edge-by-edge from the S2.2 child inventory: a promotion at level
i moves (j_i, j_{i+1}) by (−P_i, +1); an exit moves j_{m−1} by −P_{m−1};
a correction (k, d) at level i moves j_i by −(P_i − e_ik) and j_l by
+a_{d,l} (l < i; +1 at l = 0 on the Y-child); stage-2 seeding adds j(D).
Sum the moves.  ∎  [DCK-LEDGER 0 violations: 858 Q¹ cells × all levels —
TERMINAL cells only; interior-node states are covered by the edge-by-edge
proof above (and by PE1's interior hand trace on W3FMID), not by this
counter.  W-r1 2026-08-04: PE1-N2 disclosure.]

This is RM-GEN row 11b (the crossing law: mass enters level l only on
promotion edges, one unit per edge) with the OTHER inflows now displayed:
seeds and dumps.  The refutation of row 11c lives entirely in dmp_l and
the non-principal j_l(D).

## S4. W-D4: the top threshold — cellwise, exact

**Proposition W-D4.**  Assume (H2).  Then for EVERY Q¹ cell of T² (live or
dead row alike):

    π_{m−1} = k + e_{m−1}·r + j_{m−1}(cell)   ≥   k ,

r = its path's correction count at reduction level m−1.  In particular the
promotion-profile parts of Q¹ at depth < k are EMPTY — the sealed PID-DEF
leg (nonzero parts sit at depth ≥ k), strengthened from parts to cells.
*Proof.*  Specialize (LED_{m−1}): dmp_{m−1} = 0 (corrections at level i
dump strictly below i, and no reduction level exceeds m−1);
j_{m−1}(D) = s_m(u_m) by W-D2(i); cons_{m−1} = 2P_{m−1} + r·(P_{m−1} −
e_{m−1}·0) = (2 + r)e_{m−1} at g_{m−1} = 1 (two exits — one per stage —
and k = 0 is the only correction index).  Rearrange, using j_{m−1}(cell)
≥ 0.  ∎  [DCK-TOP 0/858 — both the exact identity and the inequality.]

Two consequences.  (a) The one-sided reading of the stress verdict is now
derived, at cell level: liveness at deficit k requires ≥ k promotions into
level m−1 ON EVERY contributing path; no cancellation argument is needed
for the shallow strata — they are empty.  (b) The minimal-depth stratum is
r = 0 ∧ j_{m−1}(cell) = 0, depth exactly k; battery census: on all 286
live traced rows the minimal NONZERO part depth equals k exactly
[counter minnz_eq_kdef: 286 yes / 0 no] — the survival, where it happens,
happens at the cascade depth (input to S9, not a proved claim).

At m = 2 this proposition is exactly the DCX3 seal's C2 leg (deficit-k
overflow carried entirely at depth ≥ k, 467/467 sealed rows), now proved;
at m = 3, 4 it covers the stress unit's PID-DEF GREEN.

## S5. W-D5: the line/weight floor and the depth-0 stratum

**Proposition W-D5.**  For every Q¹ cell, with q_l := h_l·∏_{l<t<m}e_t
(the L-LINE(i) promotion quantum into level l) and
λ̂ := λ + λ′ + γ_{m+1} − 2·w_mΦ_m:

    line(cell) = λ̂ + Σ_{l=1}^{m−1} π_l·q_l + (junk gains),

junk gains ≥ 0 with equality iff the path took no π-junk/Φ₀Y-junk edge.
Hence, with W-D4 and w_m(exit cofactor) = its line (the S2.2 structural
remark: cofactors are top-single digits), on every TRACK row:

    w_m(Q¹) ≥ λ̂ + max(k, 0)·h_{m−1}     (whenever Q¹ ≠ 0).

*Proof.*  Per-edge line laws, all proved in RM-GEN S3.2: promotions raise
the line by exactly q_l; correction-main (L-)children preserve it exactly
(L-NORM(a)); junk children raise it by ≥ E_m (π-child) resp. ≥ Γ₀
(Y-child); exits drop it by exactly w_mΦ_m (two exits on a Q¹ path);
stage-2 L-seeding adds line(D) = γ_{m+1} exactly (d-cells on-line, w₀
exactly additive by L-NORM(a)); line(X₀) = λ + λ′ (W-D1).  Sum; then
q_{m−1} = h_{m−1} and π_{m−1} ≥ k (W-D4); w_m of a sum of digits ≥ the
minimum of the lines.  ∎  [DCK-LINE 0/858; DCK-LINEEQ: all 801 junk-free
cells sit EXACTLY on the displayed line; DCK-WM 0/286 live rows.]

**Depth 0 = the λ̂-stratum.**  A Q¹ cell has line = λ̂ iff its path is
junk-free and promotion-free (each promotion costs a strictly positive
quantum), so the λ̂-graded piece of Q¹ is exactly (part of) the depth-0
part.  On DIRECT rows (k ≤ 0): D ≥ 2e_{m−1} with s_m(u_m) ≤ e_{m−1} − 1
forces σ_m ≥ e_{m−1} + 1, and the W-D7 cascade with zero pumping exits
twice with no promotion: depth-0 Q¹ cells EXIST — the structural half of
the sealed PID-DIR 10/10 (their residue survival stays with S9).

## S6. W-D6: the dump-free cap, and the derived mechanism of the 72

**Proposition W-D6 (the dump-free cap).**  Call a Q¹ cell *dump-free
principal* if its path received no dump mass (dmp_l = 0 for all l, no
Y-junk edge) and its d-cell is principal (t⃗ = 0).  Define the P-divisor
pool recursion

    ĉ_1 := ⌊(σ₁ + s₁(u₁))/P₀⌋,   ĉ_l := ⌊(σ_l + s_l(u_l) + ĉ_{l−1})/P_{l−1}⌋.

Then every dump-free principal Q¹ cell satisfies π_l ≤ ĉ_l for every
1 ≤ l ≤ m−1.  Under (H3) with g₀ = 1 (all P_l = e_l): **ĉ_l = c_l — the
S6.1 floors CAP the dump-free promotion counts (attainment of the max is
W-D7(iii)'s clause-pinned statement, (STK) ∧ D + c_{m−1} ≥ 2e_{m−1})
[W-r2 2026-08-04: PE2-G1 — the gloss formerly said "are exactly the
maximal … counts", an unpinned attainment].**
*Proof.*  In (LED_{l−1}) with dmp = 0 and principal j(D): each promotion
edge out of level l−1 consumes P_{l−1}, so cons_{l−1} ≥ π_l·P_{l−1}, and
j_{l−1} ≥ 0 gives π_l·P_{l−1} ≤ σ_l + s_l(u_l) + π_{l−1}.  Induction on l
with the monotonicity of x ↦ ⌊(a + x)/P⌋: π_l ≤ ĉ_l.  ∎
[DCK-DFREE 0 violations, 858 cells.]

**Corollary W-D6c (the excess channels — the sealed 72, derived).**  Every
Q¹ cell with π_l > c_l for some l rides at least one of the two channels
the pools do not count: dump mass (dmp ≠ 0) or a non-principal d-cell.
*Proof.*  Contrapositive of W-D6 (with ĉ_l ≤ c_l always, since P ≥ e and
floors are monotone).  ∎  [DCK-DIVERGE 0/112: every beyond-floor cell in
the battery — the population containing the 72 sealed nonzero-part
witnesses — carries a dump or a t ≥ 1 d-cell.]

**The g₀ ≥ 2 fence (corner E, displayed).**  At g₀ ≥ 2 the dump-free cap
has base divisor P₀ = e₀g₀, NOT the law's literal e₀: the literal-e₀
floors are not a dump-free object there.  Sharper, from the sealed m = 2
record (DCX3/RM2C, DS7): live deficit-1 rows exist at pool₁ = 2 = e₀ <
4 = P₀ — at g₀ ≥ 2 even the LAW'S OWN live rows are reachable only
through the excess channels — recycled dump mass (the C_k
partial-consumption children, consumption e₀(g₀−k), plus level-≥1
correction dumps) or non-principal d-cells [W-r3 2026-08-04, PE3-n:
"only through recycled dump mass" read class-wide would exclude the
d-cell channel; the support is instance-grade (the four g₀ = 2 towers)
either way].  So THE W-D
DUMP-FREE MACHINERY does not produce the e₀-divisor at g₀ ≥ 2 (its base
divisor there is P₀ = e₀g₀); the literal-e₀ floor is reachable only
through the recycled-mass economy, which this note does NOT derive —
fence F2 stands [W-r2 2026-08-04: PE2-G2 — the composed sentence
asserted "no mass-only argument can produce the e₀-divisor …
irreducibly … SURVIVES in residue", an unsupported ∀-over-arguments
claim of the withdrawn PE1-C1 genre, in tension with this note's own
(EMPTY-(m+1)) route; WITHDRAWN. The battery datum — path existence ⟺
the e₀-clause on all 412 traced rows including the four g₀ = 2 towers
(the derive battery + PE2's fresh instances) — is EVIDENCE that a
counting/emptiness argument may yet reach the divisor; whether it does
is part of the open (SURV)/(EMPTY) question, not settled either way
here].  The
battery's four g₀ = 2 towers have ψ₀ nonzero-pattern (1,1) (both
partial-consumption children present) — the pattern dependence of the
recycle economy at ψ₀,k = 0 corners is untested and stays inside S9.

## S7. W-D7: the principal cascade — the pool recursion derived as exact bookkeeping

**Lemma W-D7 (the two-stage greedy cascade).**  Assume (H3) and g₀ = 1
(so P_l = e_l for all l < m).  Define

    stage 1 (σ-pump):   a₀ := 0,  a_l := ⌊(σ_l + a_{l−1})/e_{l−1}⌋,
                        r_l := (σ_l + a_{l−1}) mod e_{l−1}   (1 ≤ l ≤ m−1);
    stage 2 (remainder pump):  b₀ := 0,
                        b_l := ⌊(r_l + s_l(u_l) + b_{l−1})/e_{l−1}⌋.

Then: (i) **a_l + b_l = c_l for every 1 ≤ l ≤ m−1** — the S6.1 pool
recursion is exactly the total of the two pumps.  (ii) If the stage-1
stock σ_m + a_{m−1} ≥ e_{m−1} (STK) holds, the greedy path — take the
promotion child at every firing, bottom-up, exit at the top, re-seed with
the principal d-cell, pump again, exit — is a legal path of T², and it
reaches a second Φ_m-exit IFF

    D + c_{m−1} ≥ 2e_{m−1}     (the law's clause, exactly),

in which case its terminal Q¹ cell is dump-free, junk-free, has promotion
profile π_l = c_l for every l, top exponent D + c_{m−1} − 2e_{m−1}, and
line exactly λ̂ + Σ_l c_l·q_l.  (iii) With W-D6: **π_l ≤ c_l for every
dump-free principal Q¹ cell ALWAYS, and under (STK) ∧ the law clause
D + c_{m−1} ≥ 2e_{m−1} the maximum of π_l over dump-free principal Q¹
cells EQUALS c_l — the corrected pool identification, two-sided exactly
on clause-satisfying rows.**  On clause-failing rows only the cap ≤ is
proved and the maximum may range over the EMPTY set (on all 126 traced
dead rows there are NO Q¹ cells at all — see S9); no attainment is
claimed there.  [W-r1 2026-08-04: PE1-G1 — the equality consumed (STK)
AND the law clause through (ii)'s greedy maximizer, but only (STK) was
displayed; the clause hypothesis is now pinned here and at S0, S8(b),
F6, and the claims row.]
*Proof.*  (i) Substituting r_l = σ_l + a_{l−1} − e_{l−1}a_l and pulling
the integer a_l into the floor: a_l + b_l = ⌊(σ_l + s_l(u_l) + (a_{l−1} +
b_{l−1}))/e_{l−1}⌋; induction on l with a₁ + b₁ = ⌊σ₁/e₀⌋ +
⌊((σ₁ mod e₀) + s₁(u₁))/e₀⌋ = ⌊(σ₁ + s₁(u₁))/e₀⌋ = c₁.  (ii) Legality:
E1-priority forces bottom-up reduction order; the promotion child exists
at every firing (i ≤ m−2); taking it leaves lower levels reduced, so the
pump at level l−1 fires exactly ⌊stock/e_{l−1}⌋ times before level l takes
over; the first exit needs the top stock σ_m + a_{m−1} ≥ P_{m−1} = e_{m−1}
= (STK); the stage-2 L-seed child is never absent (L-NORM(a)); the second
exit needs (σ_m + a_{m−1} − e_{m−1}) + s_m(u_m) + b_{m−1} ≥ e_{m−1},
which by (i) is D + c_{m−1} ≥ 2e_{m−1} verbatim.  Profile/line: the path
takes only promotion, exit, and one L-seed edge — dmp = 0, junk = 0 —
and W-D3/W-D5 give the displayed exponent and line.  (iii) is (ii) + W-D6.
∎  [DCK-GREEDY 0 violations: the identity (i) on all 302 all-g = 1 traced
rows; the greedy Q¹ cell FOUND on all 218 rows satisfying (STK) ∧ the
law's clause.  (STK) census: 302/302 — denominator = the traced TRACK
rows of the 16 all-g = 1 towers ONLY; the 110 rows on the four g₀ = 2
towers were never stock-censused (out of W-D7's (H3) ∧ g₀ = 1 scope
anyway) [W-r1 2026-08-04: PE1-N1] — but (STK) is NOT derived from TRACK
(E₁ᴵᴴ ≠ 0 only certifies SOME stage-1 exit, possibly dump-fed), so it
stays an explicit hypothesis, fence F6.]

**Reading.**  This derives the pool recursion from the child inventory in
the corrected sense the stress unit demanded: pool_l = σ_l + s_l(u_l) +
c_{l−1} is the exact two-stage supply ledger of ONE distinguished tree
object (the cascade: σ-mass in stage 1, principal u-mass in stage 2,
carries pooled across the stage boundary by the floor identity (i)), and
the LAW's top clause is exactly the cascade's second re-overflow
condition.  The law therefore asserts precisely: *the cascade decides
liveness* — its residue survives when it exits, and when it cannot exit,
Q¹ vanishes (on every probed instance by ABSENCE of cells — the 126/126
zero-cell census; whether by absence on the class is the (EMPTY) route)
[W-r3 2026-08-04, PE3-n: "everything it cannot fund cancels" retained
the withdrawn cancellation vocabulary at this one site].  That
deciding-ness is (SURV-(m+1)), S9.

## S8. PID† — the corrected pool identification (what replaces row 11c)

On the S1 hypotheses, for the two-stage tree computing Q¹:

* **(a) threshold, cellwise [W-D4]:** every Q¹ cell carries
  π_{m−1} = k + e_{m−1}r + j_{m−1} ≥ k; depth-(< k) parts are EMPTY.
* **(b) dump-free identification [W-D6 + W-D7; (H3) ∧ g₀ = 1]:**
  π_l ≤ c_l = ⌊pool_l/e_{l−1}⌋ for every dump-free principal Q¹ cell
  ALWAYS, with equality attained by the cascade cell whenever (STK) ∧
  D + c_{m−1} ≥ 2e_{m−1} — row 11c verbatim on exactly this subtree, on
  exactly the clause-satisfying rows.  On clause-failing rows the max
  may range over the empty set (no Q¹ cells at all on every probed dead
  row); no attainment is claimed there.  [W-r1 2026-08-04: PE1-G1 —
  law-clause hypothesis pinned; previously only (STK) was displayed.]
* **(c) excess channels [W-D6c]:** beyond-floor cells ride correction
  dumps or non-principal d-cells; nothing else.
* **(d) weight floor [W-D5]:** w_m(Q¹) ≥ λ̂ + max(k,0)·h_{m−1};
  the λ̂-stratum is depth-0 and junk-free.
* **(e) direct visibility, structural half [W-D5/W-D7]:** at k ≤ 0
  depth-0 Q¹ cells exist (the un-pumped cascade).

The literal row-11c wording — "the maximal promotion count into level l
equals ⌊pool_l/e_{l−1}⌋" (as a cap on the full tree) — stays REFUTED
(sealed: 72 nonzero-part witnesses; here: 112 beyond-floor cells, each
with its channel exhibited).  The identification is one-sided on the full
tree ((a), (d)) and two-sided on the dump-free principal subtree exactly
where (STK) ∧ the law clause hold ((b); cap ≤ unconditionally there)
[W-r1 2026-08-04: PE1-G1]; the pools count the two seed supplies and
nothing else (S2).

## S9. THE RESIDUAL, BOXED — the survival lemma (SURV-(m+1))

    ┌────────────────────────────────────────────────────────────────────┐
    │ (SURV-(m+1))  [OPEN].  On TRACK, hypotheses (H1)–(H2),             │
    │ k := 2e_{m−1} − D:                                                 │
    │   (i)  [survival]     c_{m−1} ≥ k   ⟹   Q¹ ≠ 0;                    │
    │   (ii) [cancellation] c_{m−1} < k   ⟹   Q¹ = 0.                    │
    │ Given W-D4, (i)+(ii) is EXACTLY LAW-DCX-(m+1) minus this note's    │
    │ proved layers: liveness = residue survival of the depth-≥ max(k,0) │
    │ promotion-profile parts; deadness = their absence or cancellation  │
    │ (on every probed instance: ABSENCE — 126/126 zero-cell dead rows;  │
    │ the (EMPTY-(m+1)) route targets exactly this) [W-r2, PE2-n:       │
    │ "total cancellation" alone was vacuous-compatible].                │
    └────────────────────────────────────────────────────────────────────┘

**Why the induction needs exactly this (the S7.2/COND-B thread).**  The
[ILN]† S4.6 Case-II legs close ON DCX_{m+1} given the law plus a
second-round read clause.  This note supplies the arithmetic skeleton the
S7.2 design input asked to thread — the line quanta (W-D5), the
exponent-mass ledger (W-D3), the deficit accounting (W-D4, W-D7) —
SIMULTANEOUSLY, per path.  What is not supplied is the residue layer: a
depth-graded read-transparency for T² ("DEV-at-depth": the minimal-line
piece of the depth-k part reads as an explicit unit residue times the
cascade family's sum, all other contributions cancelling through the
ψ_l(z_{l+1}) = 0 telescopes — the depth-≥1 analogue of (C-READ), i.e. the
second half of COND-B, the [IL3]† S4.7 "DEV-2" price one level up).  The
natural language for that layer is the class-level carry algebra
(F[u]/(u^e − z̄), the [ILN] kit); it is not consumed here.

**What the artifacts actually show about (i)/(ii).**  [W-r1 2026-08-04:
PE1-C1 — this paragraph previously asserted, as "derived, not just
measured", that (ii)-deadness "is a cancellation phenomenon, not path
non-existence" and that (SURV)(ii) is provably NOT closable by mass
arguments.  WITHDRAWN: the claim is refuted by this unit's own committed
artifacts.  Nothing graded PROVED consumed it — PE1 checked every W-D
proof and the PID† assembly, and the repair re-confirms it; the only
casualty is this paragraph's framing.]  The evidence: on EVERY probed
instance, deadness IS path non-existence — all 126 traced dead rows
carry ZERO Q¹ cells (126/126; the committed derive battery and PE1's
independent instrument `wmultdcx_pe1_ext.py` concur).  The 112
beyond-floor cells and the 72 sealed nonzero parts sit ENTIRELY on live
rows and say nothing about clause-failing rows.  (Consistently: on a
dead TRACK row any Q¹ cell would have π_{m−1} ≥ k > c_{m−1} by W-D4,
hence be beyond-floor and ride a W-D6c channel — and no such cell
occurs.)  What remains true: a mass-conservation UPPER bound at the
floor is unavailable on the full tree (the 72 live witnesses kill row
11c literally); and for (i) at g₀ ≥ 2 the S6 fence stands — live rows
exist whose entire top feed rides recycled dumps (pool₁ < P₀; the
sealed m = 2 record — MEASURED, instance-grade, not derived), so any
proof of (i) there must account for recycled-mass survival in residue.
At m = 1 both directions are [IL3]†-proved (Q¹ is a single cell).

**THE POSITIVE LEAD (design input for the (SURV) composer;
conjecture-grade, NOT a result)  [W-r1 2026-08-04: PE1-C1 flip side].**
W-D4 already PROVES every Q¹ cell carries π_{m−1} = k + e_{m−1}r +
j_{m−1} ≥ k, and the observed deadness is zero-CELL (path
non-existence), not cancellation.  So (SURV)(ii) may be provable by a
cell-emptiness/counting argument on the cascade, with no residue
arithmetic at all.  Target statement, sharpened through W-D4 + W-D6c:

    (EMPTY-(m+1))  [CONJECTURE-grade route]:  on TRACK, (H1)–(H2),
    c_{m−1} < k  ⟹  T² has NO two-exit path at all (Q¹ has no cells;
    a fortiori Q¹ = 0).  By W-D4 + W-D6c it suffices to show: when
    c_{m−1} < k, the two excess channels (correction dumps,
    non-principal d-cells) cannot fund π_{m−1} ≥ k on any legal path
    — a counting statement about the tree, not about residues.

Battery status: (EMPTY) holds on all 126 traced dead rows (that IS the
zero-cell census — measured, not derived on any class).  If (EMPTY) is
proved, (ii) follows with no cancellation lemma; (i) — survival — still
needs the residue layer (DEV-at-depth above) and, at g₀ ≥ 2, the
recycle economy.

**Instance evidence (never proof steps):** the stress battery 0/10,484
law-keyed (every hunted corner realized); the sharp census input to
the survival shape: on all 286 live traced rows the minimal nonzero part
depth is EXACTLY k — survival happens at the cascade depth and nowhere
shallower [counter minnz_eq_kdef 286/0; consistent with (b)+(d)]; and
the (EMPTY) census: all 126 traced dead rows carry ZERO Q¹ cells
(858/858 cells sit on the 286 live rows; PE1's independent instrument
concurs) [W-r1 2026-08-04].

## S10. Claims table, dependency line, fences

| # | claim | grade | consumed by / consumes |
|---|---|---|---|
| W-D0 | T² computes (E₁ᴵᴴ, Q¹) exactly | PROVED (T-scope pin, H1) | L-EXACT, L-CANON, T-TERM, L-NORM |
| W-D1 | ââ′ = the single formal cell X₀; formal seeding is an identity | PROVED | L-LINE(iv); discharges the stress S2 caveat |
| W-D2 | u-chain = principal weight chain, ∈ ℤ_{≥0}; d-cell inventory (top pin at g_{m−1} = 1; forced principality at interior g ≡ 1; the g₀ ≥ 2 t-inventory) | PROVED (nonneg leg of (0) under (H3), any-d-cell form — W-r1: PE1-G2) | L-GRID + RM-GEN S2.1 ([ILN]† (R1), (Ĉ1)–(Ĉ3)) |
| W-D3 | path ledger (LED_l), all levels | PROVED | S2.2 child inventory; quantifies row 11b |
| W-D4 | top threshold cellwise: π_{m−1} = k + e_{m−1}r + j_top ≥ k; shallow parts EMPTY | PROVED (H2) | (LED_{m−1}) + W-D2(i); strengthens sealed PID-DEF/DCX3-C2 |
| W-D5 | line law + weight floor w_m(Q¹) ≥ λ̂ + max(k,0)h_{m−1}; depth-0 = λ̂-stratum; direct-row depth-0 existence | PROVED | L-LINE, L-NORM(a), W-D4 |
| W-D6 | dump-free cap (P-divisor recursion; = c_l at interior g ≡ 1 ∧ g₀ = 1) | PROVED | (LED_l) |
| W-D6c | beyond-floor ⟹ dumps ∨ non-principal d-cell | PROVED | contrapositive of W-D6; derives the sealed 72's mechanism |
| W-D7 | cascade: a_l + b_l = c_l; greedy Q¹ cell at profile (c₁..c_{m−1}) under (STK) ∧ the law clause D + c_{m−1} ≥ 2e_{m−1}; law clause = second re-overflow; (iii) cap ≤ always, two-sided only under (STK) ∧ clause | PROVED ((H3) ∧ g₀ = 1; (STK) ∧ clause explicit — W-r1: PE1-G1) | W-D3/W-D5/W-D6 |
| PID† | the corrected identification (a)–(e) | PROVED at attempt grade (assembled) | W-D4–W-D7 |
| row 11c literal | full-tree cap reading | REFUTED (sealed) — stands | stress d774ecb; mechanism = W-D6c |
| (SURV-(m+1)) | survival/cancellation of the deep parts | **OPEN** (boxed, S9; the prior "not closable by mass arguments" obstruction claim WITHDRAWN at W-r1 — all 126 traced dead rows carry zero Q¹ cells, and the cell-emptiness route (EMPTY-(m+1)) is the conjecture-grade lead) | = LAW-DCX-(m+1)'s residual content |
| LAW-DCX-(m+1) | the law itself | UNCHANGED: m = 1 proved ([IL3]†), m ≥ 2 conjecture | not upgraded by this note |

**Dependency line.**  Consumes: RMENGINE_2026-08-08.md (RM-GEN)† rows
1/1a/2a/3/4/11a/11b on their recorded scopes (T-scope pin carried at
W-D0 only); [ILN]† S4.1 (R1)/WELL-DEF-(n)(ii)/(Ĉ1)–(Ĉ3) through RM-GEN
S2.1; [IL3]† DIGIT-SPLIT through L-NORM; [IL3]† S4.6/S4.7 as the m = 1
anchor of the (SURV) box.  Dagger convention: † = accepted per head
declaration, grades ride through.  Sealed artifacts consumed as instance
evidence only: DCX3 (a9dd275), WMULTDCX_STRESS (d3950f2/d774ecb).
Machine legs of THIS unit: `wmultdcx_derive_checks.py` (md5
bea1a43d619a3c91a9a0b2f32c0920c0; output 2b5395adca632fa0c2e3564db12e668d;
results 000ac228dc4aa4ea6309cf24d0a648ec) — 412 rows (= the sealed
selection), 858 Q¹ cells, verdict families DCK-SEED/INV/LEDGER/TOP/LINE/
LINEEQ/DFREE/DIVERGE/WM/GREEDY/CONS all GREEN [0 violations].

**Fences.**
F1. (H2) g_{m−1} = 1 is an explicit hypothesis; its discharge on TRACK
    rides the [ILN]† (P-DS) chain (conditional at rungs ≥ 3 per the
    ITERLAWN S4.7 erratum).  The general-g_{m−1} ledger exists (LED_l is
    g-free) but the displayed top identity and the law's 2e_{m−1}
    threshold are the g_{m−1} = 1 forms.
F2. At g₀ ≥ 2 the two-sided identification (b) FAILS structurally (base
    divisor P₀; the S6 fence): only the P-divisor cap and the one-sided
    layers hold there.  Nothing is claimed about the literal-e₀ floor's
    mechanism at g₀ ≥ 2 beyond W-D6c; its truth is instance-grade (sealed
    batteries) inside (SURV).
F3. Principal d-cell existence at g₀ ≥ 2 is a certified input (present on
    all battery towers), not derived; the ψ₀,k = 0 corners are untested.
F4. Interior g ≡ 1 (H3) is the instrument's class pin; W-D2(ii)–(iv),
    W-D6's e-form and W-D7 carry it explicitly.  The general-g inventory
    (t-vectors at every g_l ≥ 2 level) is displayed in the W-D2 proof
    shape but machine-checked only at g₀ ∈ {1, 2}.
F5. Nothing here upgrades LAW-DCX-(m+1): it stays conjecture at every
    m ≥ 2.  This note proves properties OF THE TREE, plus the exact
    reduction of the law to (SURV-(m+1)).
F6. (STK) (stage-1 stock) in W-D7 is an explicit hypothesis: census
    302/302 on the traced TRACK rows of the 16 all-g = 1 towers ONLY
    (the four g₀ = 2 towers' 110 rows were never stock-censused; out of
    W-D7's (H3) ∧ g₀ = 1 scope anyway) [W-r1 2026-08-04: PE1-N1], not
    derived from TRACK.  W-D7(iii)'s two-sidedness additionally consumes
    the law clause D + c_{m−1} ≥ 2e_{m−1} — displayed since W-r1
    (PE1-G1).
F7. All machine checks run on the m ∈ {3, 4} battery (plus m = 2 only
    through the sealed DCX3 legs cited); the displayed proofs are
    uniform in m ≥ 2.

## S11. W-r1 REPAIR RECORD (2026-08-04)

Round-1 verification: PE1 (fresh-context hostile pass; report
`lean/notes/openmath/WMULTDCX_passPE1_report.md`, independent instrument
`verification/openmath/wmultdcx_pe1_ext.py`, both committed at d63f8bb).
Verdict NOT-CLEAN: 1 critical, 2 gaps, 2 notes.  PE1's positive legs
first: the sealed 72-divergence set was reproduced EXACTLY by a fresh
instrument on the identical 412 rows; every W-D lemma re-derived sound;
8 witnesses re-derived including one full 28-edge hand audit; all md5
pins match; read-only re-run identical.  Per-finding disposition:

* **PE1-C1 (CRITICAL, the S9 obstruction paragraph) — REPAIRED by
  withdrawal + rewrite.**  The claims "(ii)-deadness is a cancellation
  phenomenon, not path non-existence", "derived, not just measured",
  and "provably NOT closable by mass arguments" are WITHDRAWN: all 126
  traced dead rows carry ZERO Q¹ cells (126/126, both instruments
  concur), so observed deadness IS path non-existence and the displayed
  inference (beyond-floor cells exist ⟹ deadness is cancellation) was
  invalid — the exhibited beyond-floor population sits entirely on live
  rows.  The paragraph now states the artifact facts and displays the
  cell-emptiness route (EMPTY-(m+1)) as the conjecture-grade positive
  lead for the (SURV) composer (W-D4's depth ≥ k + W-D6c's channel
  dichotomy reduce (ii) to a tree-counting statement).  Consumption
  re-graded: NOTHING graded PROVED consumed the withdrawn claim — PE1
  checked every W-D proof and the PID† assembly, and this repair
  re-confirms it; the claim fed only the S9 prose framing and the
  (SURV) row's difficulty gloss, both now honest.
* **PE1-G1 (GAP, W-D7(iii)/S0/S8(b) two-sidedness) — REPAIRED by
  hypothesis pin.**  The equality max π_l = c_l consumes (STK) AND the
  law clause D + c_{m−1} ≥ 2e_{m−1} (the greedy maximizer of (ii)
  exists only then); only (STK) was displayed.  Pinned at W-D7(iii),
  the S0 reconciliation sentence, S8(b), the S8 closing sentence, F6,
  and the W-D7 claims row; the clause-failing case stated honestly
  (cap ≤ only; the max may range over the empty set — on all 126
  traced dead rows there are no Q¹ cells at all).  No lemma body's
  mathematics changed.
* **PE1-G2 (GAP, W-D2(0) nonnegativity) — REPAIRED in-place.**  The
  old argument read the u_l off THE principal cell, whose existence at
  g₀ ≥ 2 is F3-certified, not derived.  Replaced by the (H3)
  any-d-cell form (any d-cell's λ-chain equals the u-chain down to
  level 1; Ĉ_m ≠ 0 supplies a cell); the (H3) pin is carried in the
  proof line and the W-D2 claims row, and every downstream consumer of
  u ≥ 0 (W-D6's e-form, W-D7) already carries (H3) explicitly.
* **PE1-N1 (note, (STK) census denominator) — DISCLOSED** at the W-D7
  machine bracket and F6: 302 = the traced TRACK rows of the 16
  all-g = 1 towers; the four g₀ = 2 towers' 110 rows were never
  stock-censused.
* **PE1-N2 (note, LED_l machine coverage) — DISCLOSED** at the W-D3
  machine bracket: DCK-LEDGER checks terminal cells only; interior
  nodes are covered by the edge-by-edge proof (and PE1's interior hand
  trace on W3FMID).

Statement fence respected: no other lemma's mathematics was touched;
every edit is the S9 rewrite, a hypothesis display, a disclosure, or a
record, each carrying a dated [W-r1] bracket.  Grade after W-r1:
W-D1–W-D7 + PID† remain PROVED at attempt grade with 0/2 CLEAN hostile
passes (round 1 = PE1 NOT-CLEAN, repaired here; round 2 runs on the
repaired text).  (SURV-(m+1)) remains OPEN; LAW-DCX-(m+1) remains
m ≥ 2 conjecture.

**REPAIR RECORD, W-r2 (2026-08-08 campaign, wallclock 2026-08-04).**
Round-2 verdict on the W-r1 text (03635d0): **PE2 (Fable,
`WMULTDCX_passPE2_report.md` + instrument `wmultdcx_pe2_ext.py`)
GAPS-ONLY — 0 critical, 2 gaps + 2 notes.** All five W-r1 remedies
substantively discharged under hostile re-derivation: the 126/126
zero-cell fact re-computed from both committed JSONs; **the
(EMPTY-(m+1)) reduction re-derived VALID at its displayed (H1)–(H2)
scope**; W-D7(iii) re-derived both directions under the pins; the
W-D2(0) any-d-cell argument correct with the F3 consumption genuinely
gone; both disclosures verified in runner source; md5 pins match; the
sandbox re-run identical; two fresh instances (W3NEST, W3FTOP) hand-
and machine-re-derived with 4 sealed divergence records confirmed.
Dispositions:

| finding | class | disposition |
|---|---|---|
| PE2-G-1 | GAP (unpinned attainment, 2 escaped sites) | CURED: the S0 trailer sentence and the W-D6 gloss now carry the (STK) ∧ law-clause pins [W-r2 brackets in place] |
| PE2-G-2 | GAP (withdrawn-genre ∀-claim) | CURED: the S6 fence's "no mass-only argument …" sentence WITHDRAWN and replaced by the honest scope statement (the W-D dump-free machinery does not produce the e₀-divisor at g₀ ≥ 2; fence F2 stands; the battery's path-existence ⟺ e₀-clause datum recorded as evidence FOR the counting/emptiness route, question open) |
| PE2-n (×2) | NOTES | The (SURV) box's "total cancellation" gloss fixed (absence-or-cancellation, with the 126/126 absence datum displayed); the cross-note RMENGINE row-11c refutation bracket [W-r3 2026-08-04: now APPLIED — RM-GEN was ACCEPTED at its round 7 (PC7+PE7 both CLEAN, [RMG] ledger block) and the erratum bracket sits at RMENGINE S8 row 11c citing d774ecb + this note's PID†] |

Grade after W-r2: unchanged — W-D1–W-D7 + PID† PROVED at attempt grade,
acceptance counter 0/2 CLEAN (PE2 = GAPS-ONLY, not CLEAN); (SURV-(m+1))
OPEN; LAW-DCX-(m+1) m ≥ 2 conjecture. Round 3 (PE-W3 Fable now; the
Codex leg on the post-reset budget) runs on THIS text.

**REPAIR RECORD, W-r3 (2026-08-08 campaign, wallclock 2026-08-04).**
Round-3 verdict on the W-r2 text (a0e266c): **PE3 (Fable,
`WMULTDCX_passPE3_report.md` + instrument `wmultdcx_pe3_ext.py`)
GAPS-ONLY — 0 critical, 1 gap + 3 notes.** All W-r2 remedies discharged
(withdrawn-genre grep clean; the (SURV) box exactly the law's residual);
machine legs identical; two never-audited sealed records (W3G2B, W4DEEP)
hand-then-machine re-derived and CONFIRMED. Dispositions: PE3-G-1
(the two S0 attainment sites omitted the (H3) ∧ g₀ = 1 scope W-D7's
attainment consumes) — CURED, scope pins added at both sites; PE3-n
(S7 cancellation vocabulary; the S6 recycled-mass channel wording; the
"tracked in the ledger" pointer) — all CURED in place. Grade unchanged:
W-D1–W-D7 + PID† PROVED at attempt grade, counter 0/2 CLEAN;
(SURV-(m+1)) OPEN; (EMPTY-(m+1)) the conjecture-grade route (its
reduction now independently re-derived VALID by two passes). Round 4 =
the Codex leg (post-reset budget) + PE-W4 — the acceptance attempt.

================================================================
=== PASTE D: EMPTY_PROOF_2026-08-08.md head + E-THM statement (attempt grade, 1/2 clean), lines 1-45 @ 3c2ac60 ===
================================================================
# (EMPTY-(m+1)) PROVED at attempt grade on (H1)–(H3), g₀ FREE — the weighted path-ledger assembly: the two excess channels cannot fund π_{m−1} ≥ k on any legal path when c_{m−1} < k

    [E-r1 REPAIR BRACKET, 2026-08-08 campaign (wallclock 2026-08-04).
    Hostile pass 1 (EMPTY-PE1, fresh-context Fable, on 4eb1719; report
    EMPTY_passPE1_report.md) — verdict NOT-CLEAN: 1 CRITICAL + 2 gaps +
    2 notes.  The CRITICAL was in the S7 ASSEMBLY step only: the master
    slack display was false as printed (instance-refuted on 290–437 of
    the batteries' own 492 two-exit cells, depending on the reading of
    its undefined ρ⁺_l symbol) and E-L7 was read off it in one line.
    PE1 re-derived E-L1–E-L6 VALID as displayed and the THEOREM E-THM
    TRUE on (H1)–(H3) end to end, supplying the full repair.  This
    revision applies that repair: S7 rewritten (false displays
    withdrawn in place, never deleted), the new displayed Lemma E-L8
    (tight-stratum remainder vanishing — PE1 Finding 2) stated and
    proved, the CTRL-PAY1 disclosure corrected and given teeth via the
    v2 machine leg (Finding 3), the EDC-CPLUS phantom guard and the
    header consumption range fixed (Findings 4–5).  E-THM's statement
    and E-L1..E-L6 are byte-unchanged.  Full disposition: the REPAIR
    RECORD at the tail.]

Object: the conjecture-grade route displayed at WMULTDCX_2026-08-08.md S9
(the W-r1 positive lead):

    (EMPTY-(m+1)):  on TRACK, (H1)–(H2), c_{m−1} < k := 2e_{m−1} − D
    ⟹  T²(γ,γ′) has NO two-exit path at all (Q¹ has no cells; a
    fortiori Q¹ = 0).

THIS NOTE PROVES IT, with one hypothesis added and displayed: (H3)
(interior g ≡ 1, the instrument class pin of the whole W arc), with **g₀
FREE and d₀ free inside T-scope** — i.e. on the full class where the W-D
layers themselves live.  The general-interior-g residual is boxed (S9,
(EMPTY-gen)); nothing else is left open.  Scope: the [ILN]† S0.1 class
via the RM-GEN S0.1 pin, level m ≥ 2, g_m = 1, uniform in m and in the
characteristic — the proof is class-level arithmetic of the tree, no
instance input.  Date: 2026-08-08 campaign (wallclock 2026-08-04;
composer unit of the W-MULT-DCX arc, the (EMPTY) leg).

**Grade (header summary) [E-r1 2026-08-08 (wallclock 2026-08-04),
amended after hostile pass 1].**  E-L1–E-L8 and the assembled theorem
E-THM — **PROVED at attempt grade, hostile-pass record 1/2 CLEAN**
[SD-r1 cross-file header cure, 2026-08-08 campaign (wallclock
2026-08-04), flagged by SURVDISJ-PE1 N-4: this header still read the
stale "0/1 CLEAN" after the E-r2 tail record logged PE2 CLEAN at
commit 7314525; the counter is cured here, nothing else touched]: pass
1 (EMPTY-PE1, fresh-context Fable, report

================================================================
=== PASTE E: SURV_PROOF_2026-08-08.md S6 (THEOREM S-THM) + S7 residual box (attempt grade), lines 403-470 @ 3c2ac60 ===
================================================================
## S6. S-THM: the assembled conditional theorem

**THEOREM S-THM (proved at attempt grade; the (SURV)(i) perimeter).**
On TRACK, (H1)–(H2), c_{m−1} ≥ k:

* **(a)** if the netted bottom stratum is a singleton, then Q¹ ≠ 0
  [S-L2];
* **(b)** if the netted bottom stratum is nonempty and (RW₀) holds at
  it, then Q¹ ≠ 0 [S-L3];
* **(c)** at the pins (H3) ∧ g₀ = 1 ∧ (STK), under the full top-reduced
  pin (RW) (every terminal cell has j_{m−1} ≤ P_{m−1} − 1): if the
  cascade slot has no equal-line partner path (the (NEP) hypothesis),
  then Q¹ ≠ 0 — i.e. **(SURV)(i) holds outright on the
  (NEP) ∧ (RW) sub-locus of the pinned class.**

*Proof.*  (a), (b) displayed above.  (c): by S-L4(ii)+(iii) and (NEP),
the cascade slot's equal-line subsum is the single term of S-L1, with
nonzero residue — the slot survives at line β̂.  Under (RW) every
terminal cell is reduced (interior automatic + top pinned), so Q¹'s
reduced development is exactly the list of surviving netted slots
(L-CELL(i) uniqueness), which is nonempty.  ∎

Instance status of the three hypotheses (S8 data): (a) held on 459/486
live rows; (b) on 484/486; (NEP) on 379/379 of the g₀ = 1 in-scope rows
(and FAILED on 55 g₀ ≥ 2 rows — it is a g₀ = 1 phenomenon on present
evidence, not a class fact); (RW) on 1,806/1,806 cells.  None of
(a)/(b)/(NEP) is derived from c_{m−1} ≥ k here — that derivation IS the
open residual.

## S7. THE RESIDUAL, BOXED — and the closest-call displays

    ┌────────────────────────────────────────────────────────────────────┐
    │ (SURV-RES-(m+1))  [OPEN — the exact blocking obstruction].         │
    │ On TRACK, (H1)–(H3), c_{m−1} ≥ k:  same-slot equal-line residue    │
    │ cancellation cannot kill EVERY slot — i.e. some slot of the        │
    │ two-exit multiset survives netting.  By S-THM this (with the (RW)  │
    │ pin where the surviving slot is not bottom-minimal) is ALL that    │
    │ separates the pinned class from (SURV-(m+1))(i).  Sub-forms, in    │
    │ decreasing strength, each sufficient over its own locus:           │
    │   (NEP)        [combinatorial, NO residue input]: at (H3)∧g₀ = 1   │
    │                the cascade slot has no equal-line partner path.    │
    │                Then S-THM(c) closes (i) there.  Instance record    │
    │                [S-r1]: held 379/379 committed + every fresh PE1    │
    │                g₀ = 1 row (0 partners of ANY valuation, ever);     │
    │                FALSE at g₀ ≥ 2 (55 committed + 39 fresh            │
    │                partnered rows).                                    │
    │   (SURV-DISJ)  [residue-level, the sharp falsifiable form]: the    │
    │                netted bottom stratum survives OR the cascade slot  │
    │                survives.  Instance record [S-r1]: 486/486          │
    │                committed + 545/545 fresh-traced (PE1), six unique  │
    │                kill rows in all — and on EVERY kill the survivor   │
    │                was the cascade slot AT the cascade line.           │
    │ Neither is proved here.  A proof of (NEP) is a quantum-resonance   │
    │ analysis of the S-L4(iii) line balance — and [S-r1, per PE1 G-1]   │
    │ at m ≥ 3 it must ALSO exclude junk-riding partners (junk gains     │
    │ compensating interior promotion deficits: the S-L4(iii) forcing    │
    │ is proved at m = 2 ONLY, and the S8.b mixed-junk near-miss         │
    │ EQ3G2A[2321,2321] shows junk-line compensation is instance-real)   │
    │ — still valuation-side, NO residue input needed.  A proof of       │
    │ (SURV-DISJ) needs the same-slot residue sums — the class-level     │
    │ carry algebra F[u]/(u^e − z̄), i.e. DEV-at-depth, the [IL3]†        │
    │ "DEV-2" price one level up (the WMULTDCX S9 display, unchanged     │
    │ by this note) — and [S-r1] it must handle BOTH kill genres of      │
    │ the PE1 census (S8.b): mod-p path-multiplicity kills (residue      │
    │ char 2, six instances) AND characteristic-BLIND opposite-sign      │
    │ equal-residue kills (37 instances at g₀ = 2).                      │
    └────────────────────────────────────────────────────────────────────┘


================================================================
=== PASTE F: SURVDISJ_PROOF_2026-08-08.md head (1-60) + THEOREM SD-THM-A (301-350) + consequences (590-615) @ 3c2ac60 ===
================================================================
# (SURV-DISJ) — the survival disjunction: PROVED at attempt grade on the g₀ = 1 ∧ d₀ = 1 locus via the E-weighted mass ledger ((NEP) discharged from hypothesis to THEOREM there — the cascade slot always survives); the g₀ ≥ 2 regime boxed as the signed-multiplicity-one law (SURV-K2) with its full proved perimeter; the d₀ ≥ 2 Φ₀Y-corner boxed as (SD-YJ)

    [SD-r1 REPAIR BRACKET, 2026-08-08 campaign (wallclock 2026-08-04).
    Hostile pass 1 (SURVDISJ-PE1, fresh-context Fable, on 4f7c3ff;
    report SURVDISJ_passPE1_report.md) — verdict GAPS-ONLY: 0 critical,
    1 gap, 4 notes.  PE1 re-derived SD-L2, the pool telescope, (★),
    SD-L1 (all three legs), SD-L3's consumption, SD-THM-A(i)–(v), and
    SD-L5/L6/L7 EXACT as displayed; re-ran both machine legs read-only
    (identical mod timings, all md5 pins matching, the two-commit seal
    verified genuine); hand-re-derived three instrument rows.  The one
    gap: SD-L4(ii)/(iii) displayed the "w₀ exactly ŵ / w₀ ≥ ŵ + 1"
    clauses without the principal-d-cell pin (false for t ≥ 1 seeds at
    g₀ ≥ 2) — repaired in place at S3, claims row + F-A rippled; both
    consumers verified undamaged by PE1 (SD-THM-A's locus pins the
    principal seed; SD-L5 consumes only the fork-counting).  The four
    notes cured at dated [SD-r1] brackets (one cross-file, in
    EMPTY_PROOF's header).  Statements of SD-THM-A and
    SD-L1/L2/L3/L5/L6/L7 byte-unchanged.  Full disposition: the REPAIR
    RECORD at the tail.  Arc counter: 0/1 CLEAN (GAPS-ONLY ≠ CLEAN).]

Object: the boxed conjecture (SURV-DISJ) of SURV_PROOF_2026-08-08.md S7
(at 51a8ac9), the sharp falsifiable sub-form of (SURV-RES-(m+1)):

    (SURV-DISJ):  on live rows (TRACK, (H1)–(H3), c_{m−1} ≥ k) the
    netted BOTTOM stratum survives netting OR the CASCADE slot survives
    at the cascade line.

**Verdict, stated first.**  (SURV-DISJ) is PROVED at attempt grade on
the locus (H1)–(H3) ∧ g₀ = 1 ∧ d₀ = 1 ∧ (STK) ∧ (c_{m−1} ≥ k) — by the
CASCADE leg, unconditionally on that locus (not merely on kill rows):
the cascade slot ĵ always survives netting at the cascade line β̂
(Theorem SD-THM-A).  The mechanism is the instrument's (K1) protection
identity, proved: an E-weighted mass ledger over the W-D3 path ledger
(SD-L2) forces every would-be cascade-slot companion's correction
economy to balance exactly, while every correction edge at g₀ = 1
strictly under-returns mass unless it rides a Φ₀Y-junk edge (SD-L1) —
and at d₀ = 1 the Φ₀Y-junk child is IDENTICALLY ABSENT (L-NORM(c),
accepted).  Hence (NEP) — until now an instance-only hypothesis
(379/379 + 693/693) — is a THEOREM on this locus, and S-THM(c) closes
(SURV-(m+1))(i) there outright under (RW).  With (EMPTY-(m+1)) and
W-D4 this puts LAW-DCX-(m+1) on the pinned class
(H1)–(H3) ∧ g₀ = 1 ∧ d₀ = 1 ∧ (STK) ∧ (RW) at the capped grade — the
first closure of the full law on any m ≥ 2 class (S8).

What is NOT proved: the g₀ ≥ 2 regime (K2).  Its perimeter is proved
here — correction-free rigidity SD-L5 (kills can never involve two
correction-free junk-free paths), the group survival criterion SD-L6,
and the protected-vector survival lemma SD-L7 (the instrument's
signed-multiplicity-one law reduced to field distinctness) — but the
existence of a protected bottom-line group at g₀ ≥ 2 is OPEN, boxed as
(SURV-K2) at S6 with the sign-reversing-pairing proof target and the
full instrument constraint census.  Also open: the g₀ = 1 ∧ d₀ ≥ 2
corner, boxed as (SD-YJ) — there the ledger admits Φ₀Y-junk-fed
companions with an exact deficit equation, never probed by any battery
(no g₀ = 1 ∧ d₀ ≥ 2 tower exists in any committed roster).

Scope: the [ILN]† S0.1 class via the RM-GEN S0.1 pin, level m ≥ 2,
g_m = 1.  Date: 2026-08-08 campaign (wallclock 2026-08-04; composer
unit of the W-MULT-DCX arc, the (SURV-DISJ) leg, on the two-commit
instrument seal 61e9e36/00e19b4).
[...]
## S4. SD-THM-A: the (K1) theorem — the cascade slot always survives

**THEOREM SD-THM-A.**  On TRACK, (H1)–(H3), g₀ = 1, d₀ = 1, (STK),
c_{m−1} ≥ k:

* **(i) [NEP-strong up to X-mates]**  Every two-exit terminal cell at
  the cascade slot ĵ is either the cascade cell Ĉ or an "X-mate": a
  correction-free cell descended from the X-child of the b₀-seeding,
  with the same exponent vector, junk ≥ 1, and w₀ ≥ ŵ + 1 (at most
  one such).
* **(ii) [(NEP) — now a THEOREM]**  The cascade slot's equal-line
  population at β̂ is the singleton {Ĉ}: the cascade has NO equal-line
  partner path.
* **(iii)** The cascade slot survives netting at the cascade line:
  B_ĵ ≠ 0 with w₀(B_ĵ) = ŵ and line(B_ĵ, ĵ) = β̂.
* **(iv) [(SURV-DISJ) on this locus]**  The disjunction holds — by
  the cascade leg, whatever the bottom stratum does.
* **(v) [(SURV)(i) on this locus]**  Under (RW) additionally:
  Q¹ ≠ 0.

*Proof.*  (i)  Let C be a two-exit terminal with j⃗(C) = ĵ.  By (★),
Σ_c defc_c = Y₂(C).  At d₀ = 1, SD-L3 kills every Φ₀Y-flag: Y₂ = 0
and Y_c = 0, so Σ_c defc_c = 0 with every term ≥ 1 by SD-L1 — hence
CE(C) = ∅.  A correction-free path has no stage-1 L-NORM event, so
its seeding is b₀·c_D with the unique principal d-cell (SD-L4); the
seeding's children are the L-child and (at most) the X-child — the
Y-child does not exist (SD-L4(i)).  Off the seeding, a
correction-free path has no forks at all: promotion and exit children
are unique per firing and E1-priority makes the firing sequence
deterministic ([RMG] S2.2; the S-L4(iii) uniqueness half is exactly
this display).  So C terminates the L-branch — and is Ĉ — or
terminates the X-branch: an X-mate, whose exponent moves after the
seeding are IDENTICAL to the L-branch's (the walk depends only on
exponents), whose junk is ≥ 1, ce = ∅, and whose w₀ ≥ ŵ + 1
(L-NORM(b)); there is at most one X-branch, hence at most one
X-mate.
(ii)  An X-mate sits at line β̂ + E_m·(w₀ − ŵ) > β̂ (line is affine in
w₀ at fixed slot — S-L4(i)); so the β̂-population at ĵ is {Ĉ}.
(iii)  B_ĵ = b_Ĉ ± b_X (the mate, if present).  w₀(b_X) > w₀(b_Ĉ) =
ŵ, so by S-L0(b) at w₀: B_ĵ ≠ 0 with w₀(B_ĵ) = ŵ, and its line at
slot ĵ is β̂.  (Equivalently: S-L4(ii) — the equal-line subsum at β̂
is the single S-L1 term with residue b̄₀c̄_D ≠ 0.)
(iv)  Immediate from (iii).
(v)  (ii) is (NEP); S-THM(c) applies verbatim with its (NEP)
hypothesis now supplied by theorem: under (RW) the reduced
development of Q¹ is the nonempty list of surviving netted slots
(L-CELL(i)), so Q¹ ≠ 0.  ∎

[Machine: DRV-NEPS — on all 693 g₀ = 1 ∧ d₀ = 1 ∧ (STK) live rows of
the union roster, the cascade slot's population is exactly {Ĉ} —
[...]

## S8. The honest consequence display

* **(SURV-DISJ)**: PROVED at attempt grade on
  TRACK ∧ (H1)–(H3) ∧ g₀ = 1 ∧ d₀ = 1 ∧ (STK) ∧ (c_{m−1} ≥ k)
  [SD-r1: TRACK restored to this display and the LAW-DCX one, PE1
  N-2 — SD-THM-A's head always carried it] — cascade leg,
  unconditional there (SD-THM-A(iv)).  OPEN at g₀ ≥ 2 ((SURV-K2))
  and at g₀ = 1 ∧ d₀ ≥ 2 ((SD-YJ)).  Class-wide: still open, now
  with the open set exactly = the two boxes.
* **(NEP)**: upgraded from instance-only hypothesis to THEOREM on
  the SD-THM-A locus (SD-THM-A(ii)).  S-THM(c)'s hypothesis list on
  that locus shrinks to (RW).
* **(SURV-(m+1))(i)**: CLOSED at attempt grade on
  (H1)–(H3) ∧ g₀ = 1 ∧ d₀ = 1 ∧ (STK) ∧ (RW) (SD-THM-A(v)) — the
  first closure not conditioned on (NEP).  OPEN beyond (the boxes;
  and (RW) itself at m ≥ 3).
* **LAW-DCX-(m+1) ON THE PINNED CLASS**: on
  TRACK ∧ (H1)–(H3) ∧ g₀ = 1 ∧ d₀ = 1 ∧ (STK) ∧ (RW) [SD-r1: TRACK
  restored, PE1 N-2], for every m ≥ 2:
  c_{m−1} ≥ k ⟹ Q¹ ≠ 0 (this note) and c_{m−1} < k ⟹ Q¹ = 0
  (E-THM†: zero two-exit paths; with W-D4† framing the deficit) —
  **the law holds on that class at the capped attempt grade**: the
  first full-law closure at any m ≥ 2.  Class-wide, LAW-DCX-(m+1)
  REMAINS A CONJECTURE.
* **What must still close before any acceptance-grade claim**, named:

================================================================
=== PASTE G: the [RMG] acceptance block (ledger BRIDGE_ADJUDICATIONS_2026-07-30.md lines 9328-9400 @ 3c2ac60) ===
================================================================
## [RMG] — THEOREM RM-GEN ACCEPTED — 2026-08-08 campaign (wallclock 2026-08-04)

**THE DEFINED TERM.** [RMG] := THEOREM RM-GEN as accepted: **the four
(RM-m) box clauses of [ILN]† S4.6 — (C-W_R), (C-W_Q), (C-READ) with
z_m^{ε′}vτ_m pinned and v ≠ 0 forced (L-VNZ), (C-EPS) — hold
level-generically for EVERY m ≥ 1 on**

    T-scope(m) := d₀ = 1  ∨  (e₀ ≥ 2 ∧ e_i ≥ 2 for all 1 ≤ i ≤ m−1),

proved by the depth-calculus (the S2.2 cell tree with L-NORM-normalized
children; L-GRID/L-ZMULT/L-LINE; the L-MU two-branch measure with the
d₀ = 1 flat extension; DEV-(m); the telescopes), in
`lean/notes/openmath/RMENGINE_2026-08-08.md`. **Accepted text: commit
a0e266c** (post-acceptance annotations at the head/S7.1/S8-row-11c/tail
only). Bar met at round 7: PC7 (Codex) CLEAN + PE7 (Fable) CLEAN, both
on a0e266c — 2-clean model-diverse, same round. Arc: 7 rounds, 12
hostile passes (6 Codex + 6 Fable), ZERO critical findings across the
arc; S2–S7 mathematical content byte-stable from 8835d20 (r1) except
one citation parenthetical. Grade: ACCEPTED (the corpus bar), attempt
lineage recorded in the note.

**Honest residuals (unchanged by acceptance):** the d₀ ≥ 2 ∧ (e₀ = 1 ∨
∃ interior flat) corners FENCED (the S4.1 necessity display: the linear
family is genuinely infeasible there — machine-confirmed); LAW-DCX-(m+1)
a sealed conjecture at m ≥ 2 (row 11c's literal floor identification
REFUTED per the W unit, superseded by PID† at attempt grade in
`WMULTDCX_2026-08-08.md`; the erratum bracket applied at S8 row 11c);
(H2)-class discharge via (P-DS) conditional at rungs ≥ 3 per the
ITERLAWN S4.7 erratum where consumed downstream.

**COR-A APPLIED (the at-acceptance ledger action, S7.1 bracket):** on

    T-class(n) := d₀ = 1 ∨ (e₀ ≥ 2 ∧ e_i ≥ 2 for all 1 ≤ i ≤ n−2),

the [ILN]† S7 rows 5/6/8 (RM-m) calls are DISCHARGED: **(ITER-LAW-n)
holds on T-class(n) with the (RM-m) conditionality REMOVED** — its
remaining conditionality there is exactly [ILN]†'s own non-(RM)
residuals. Off T-class(n) the rows stay conditional. The accepted [ILN]
note is not edited; this block is the ledger record of the upgrade
(incorporation convention).

**Arc artifacts:** reports RMENGINE_pass{PC,PE,PC2,PE2,PC3,PE3,PC4,PE4,
PC5,PE5,PC6,PE6,PC7,PE7}_report.md (all committed); the tag-grammar
machine census (`rmengine_claims_census.py` + committed CLOSED outputs,
zones 11/13/15/17 at ab2e471/d63f8bb/cfb3648/a0e266c); referees
`rmengine_pe_reimpl.py` (97/97) + `rmengine_pe2_ext.py` (67 calls, 8
fresh flat towers, 1080-shape sweep) + the S9 diag D1–D7. Standing
inventions minted in this arc, now corpus law: the TAG-GRAMMAR machine
census; supersede-at-refutation; as-of machine quotes (the committed
output file is the sole arbiter for current figures); enumerations
claim no completeness.

**Consequences queued:** the r40 ROOT fold (the RM-2 probe + DCX3 v2
seal + the full RM-GEN arc + [RMG] + the W-MULT-DCX arc state incl.
PID† and (EMPTY-(m+1)) + the S4.7 refutation bracket → REVISION 39 +
census + VC40 under the frozen-history trim); the standing public-map
refresh (major milestone); the W arc round 4 (Codex leg post-reset);
(SURV)/(EMPTY) = the remaining Q2 hard core; Q3 corners; Q4 GRT weld.

---

## ROOT r40 EXECUTED (REVISION 39) — the TWENTIETH re-anchoring made operative + the [RMG] fold + THE WAVE-21 CLOSURE-PROGRAM HARVEST; VC40 owed — 2026-08-08 campaign (wallclock 2026-08-04)

**The arc this fold harvests:** the VC39 cascade (VC39 0C/6G → r39b
2d3080f + the whitespace-normalization amendment → VC39b CONFIRMED
0C/0G, bbbd394 — the TWENTIETH re-anchoring, anchor now REVISION 38 +
r39b) · the sealed RM-2 probe (0b42885/d860a5a/9e3ad2c, adjudicated
ced7dbb) · the DCX3 v1→v2 cycle (1db30fa/9003821/a9dd275/2924ff6) ·
the full RM-GEN arc (compose bf18bfe → 7 rounds → acceptance b3ddada
on a0e266c = **[RMG], Q1 CLOSED**) with COR-A applied · the W-MULT-DCX
arc state (stress d3950f2/d774ecb; PID† at attempt grade through W-r3;
counter 0/2 CLEAN, NOT accepted, Codex leg post-reset; (SURV-(m+1))
boxed, (EMPTY-(m+1)) conjecture-grade route).

=== END OF BRIEF. Begin with the VERDICT line. ===
