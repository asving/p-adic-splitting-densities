# G-ADM-2 — the (ADM) adjudicating gate: realized-strata attainment scan

**Unit provenance.** Synthesis pass 6 queue #4 (F4: the gate NEVER RAN; the (ADM) row was
misdisplayed as blocked). Runner `verification/openmath/gadm2_scan.py`, predictions
sealed in the script header PRE-RUN (preregistration commit 4d7dba1). Battery log
`verification/logs/gadm2_attainment_20260802T232440Z.jsonl` (198 rows). Scripted
disposition `verification/logs/GADM2_DISPOSITION.md` — written by the unit's own
completion script. **Note authored at orchestrator harvest**: the wave-10 unit ended its
step mid-battery (the 3¹⁴ config); the battery completed unattended; the orchestrator
harvested the sealed disposition verbatim. No content below goes beyond the unit's own
sealed artifacts.

**Spec.** HDISCHARGE_H3.md §1.3 numerics gates + the ADM-U5 instrumentation spec
(`verification/h3_classd_scan.py`); router semantics per the ADM-U7 repair
(ADM_U7_TRACKA_ATTEMPT.md §6: `is_right_end` column; track B fires only on an OFF-END
partial).

## VERDICT (verbatim from the sealed disposition)

**RIGHT-END-ONLY PARTIAL (mixed-with-boundary): no track-B fire; track A live as
(ADM-REAL′); ADM-U7 dichotomy CONFIRMED at scan scope.**

Tallies: configs = 13, fibers = 99, chain_anomalies = 0, rows = 198, full = 113,
partial@right-end = 85, **partial@off-end = 0** (the track-B trigger never fired),
anomaly_empty = 0; subgroup_ok = False rows = 83, ALL right-end p ≥ 3 pin-explained;
CAP-truncated slots 14/432. Preregistered predictions P1–P7: **ALL PASS**.

First right-end partial row (the pin mechanism, q = 3, n_att = 1):
config = (3, 3, 5, None), pkey = (((0,3),(3,0)), (((1,1),(1,2)),)), slot = 2.

The right-end partials sit at the ONE slot CEN-J never charges (ρ-pinned, subtracted via
L′ — ADM-U7 §5). Standing discipline honored: a no-hit is never cited as proof
(finding 7) — this scan ADJUDICATES the track routing; it proves nothing.

## Routing consequence (the next-unit call)

The (ADM) row's dichotomy resolves to **track A**: the next (ADM) unit is the
**(ADM-REAL′) attempt** per ADM_U7_TRACKA_ATTEMPT.md, with the right-end pin mechanism
(ρ-pinned slot, L′ subtraction) as its displayed boundary hypothesis. Track B is NOT
chartered (its trigger — an off-end partial — has zero occurrences at scan scope). The
scan scope (13 configs through 3¹⁴, CAP truncation 14/432 slots displayed) is the honest
fence: a deeper battery is warranted only if the (ADM-REAL′) attempt hits a boundary
case outside the scanned strata.
