# DEPTH-3 STRATA PROBE — the order-3 all-e_j = 1 stratum measured: (IDENT-3) EXACT (0/1,656), no ǫ-revival on the all-unramified stratum, the level-2 Def-3.15 reading discriminated externally for the first time — COMPOSE UNIT CHARTERED

**Unit:** synthesis-6 queue #6 (charter = `docs/SYNTHESIS_PASS6_2026-08-08.md`
F3(c): deeper strata NOT yet finite-algebra; next candidate ORDER 3,
all-e_j = 1, PROBE-FIRST; a compose unit fires only on an exact measured
law).  **Harness:** `verification/openmath/strata3_probe.py`
(preregistration sealed pre-run at commit d8ef8d8; derivation of the
candidate law displayed in the header BEFORE the run — the strata_probe A3
discipline; output `strata3_probe_output.txt`, JSON
`strata3_probe_results.json`; seed 20260817; exact arithmetic; rerun
byte-identical mod timestamps).  **Parents:** `STRATA_PROBE_2026-08-08.md`
(the genre; order-2 twist law + ITER-LAW), `TR3S2_2026-08-08.md` (ORD-INV
matched-key discipline; §S7 scope box = the exact open this probes),
`GRB_ORDER2_2026-08-05.md` REV-12/REV-13 (the ǫ-revival narrowing),
grb_order2_check.py (N-1 Tower machinery, reused verbatim for levels 0–2).
**Author:** Fable depth-3 strata probe unit, campaign date 2026-08-08.

## S0. Verdict table (all preregistered rows; samples in parentheses)

| row | claim | status going in | observed | verdict |
|---|---|---|---|---|
| C1 | **(IDENT-3)** C^F3_j = c^G3_{s_j} — the composed TR3-S1 σ-shape | **THE PROBED LAW** (derived pre-run) | 0/1,656 | **GREEN** |
| P0 | GMN-3 integrity (t₂ ∈ ℤ, component shape, c ≠ 0 on-line) | sealed (GMN printed proofs, under (H-v3)) | 0/6,468 | GREEN |
| P1 | support agreement FGMN vs GMN at level 3 | conj component | 0/4,662 | GREEN |
| P2 | level-3 Lemma A-GEN z-shift (ǫ₂⁻¹C^F3 = z₂^{m₀}·R₂^GMN(A)(z₂), m₀ ∈ ℤ≥0) | conj component | 0/2,326 | GREEN |
| P3 | digit-level order-2 identity (TR3-S1 on the probed digits) | sealed (TR3-S1 theorem) | 0/2,326 | GREEN |
| P4 | key tie gmn3(Φ₃) = ψ₂ exactly (key classes coincide, θ₂ = 0) | sealed (ORD-INV display) | 0/24 | GREEN |
| P5 | matched-label ord transport (shared ψ₂ label both sides) | derived | 0/1,656 | GREEN |
| V1–V3 | VAL-3 mult · R₃ total mult · W-MULT-3 with δ ≡ 0 (no-carry control) | bug detectors / transcription validators | 0/720 | GREEN |
| V4–V5 | key lift R₃(Φ₃) = ψ₂ exact · two-form/support/j₀ shape | bug detectors | 0/1,680 | GREEN |
| CERT2 | order-2 tower re-certification (N-1's T1/T2/T5/T11+PARI) | sealed | 0/420 | GREEN |
| CERT3 | PARI order-3 external leg: Φ₃ irreducible/Q_p, unique prime, (e,f) = (1, d₀g₀g₁g₂) | sealed | 0/12 | GREEN |
| M1 | **reading-P level-2 ǫ refuted at every P-visible slot** | discrimination meter | **1,641/1,641** | MATCH |
| M2/M3 | inner z-shift m₀ > 0 / t₂ ≠ 0 genuinely exercised | teeth meters | 229 slots each | MATCH |
| M4 | towers with z₂^{γ₃} ≠ 1 (P-visibility carrier) | teeth meter | 22/24 | MATCH |
| P5-hist | ord histogram contains ord ≥ 1 | teeth meter | 0:1512 1:85 2:59 | MATCH |

Perimeter: 6 designed rows 3A–3F (all e_j = 1; g-vectors (2,2,2), (2,2,2)
h-varied, (2,2,3), (2,1,2), (1,2,2), (2,2,1) — mixed g_j incl. both
degenerate corners g₀ = 1 (K₁ = K₀) and g₁ = 1 (z₂ ∈ K₁) and the
refinement corner e₂g₂ = 1) over all four base rings
{ℤ₂, ℤ₃, F₂[[t]], F₃[[t]]} = 24 tower instances; deg Φ₃ up to 12; 21,950
samples; exit 0; 37.9 s.  No amendments: first full run ALL GREEN on the
sealed header.

## S1. The measured law

**Setting.** Order-3 tower, reads (e_j, h_j, ψ_j), j = 0,1,2, ALL e_j = 1;
FGMN level-3 weight-read coefficients C^F3_j (Def 3.16 one-step recursion
transcribed one level up from the N-1-certified R2w: C^F3_j =
ǫ₂(γ_j)·R_{2,γ_j}(A_{s_j})(z₂), with ǫ₂(β) = z₂^{ℓ′₁s−ℓ₁u} the corrected
Def-3.15 reading C at level 2); GMN order-3 residual coefficients c^G3_i
computed INDEPENDENTLY per the printed level-generic Defs 2.19–2.21
(c_i = z₂^{t₂(i)}·R₂(A_i)(z₂), t₂(i) = (s₂(A_i) − ℓ₁u_i)/e₁), fresh
implementation.  Matched keys at every level (ORD-INV discipline): both
sides consume the SAME Φ₀/Φ₁/Φ₂.

**The law, confirmed at every probed instance (C1, 0/1,656 on-line-slot
comparisons at 4,662 support checks):**

    (IDENT-3)   C^F3_j = c^G3_{s_j}     at every on-line slot, every f —

the level-3 discrepancy is IDENTICALLY 1 on the all-unramified stratum:
the TR3-S1 σ-compose shape composes through both levels with no surviving
twist.  Mechanism (the sealed derivation, now instance-confirmed): all
eq-(12) splits have s ≡ 0 and all ℓ_j = 0, so ǫ₁ ≡ ǫ₂ ≡ 1 (reading C); the
FGMN forced-abscissa evaluation contributes exactly z₂^{m₀}
(m₀ = s₂(A_j), row P2) and GMN's t₂ exponent absorbs it exactly
(t₂ = s₂(A_j) at ℓ₁ = 0); the inner coefficientwise agreement is TR3-S1 on
the digit (row P3).

**ǫ-revival adjudication (the F3(c) question).**  The ǫ-recurrence does
NOT revive at order 3 on the all-e_j = 1 stratum: no marched-ǫ residue, no
slot-keyed character, discrepancy ≡ 1 at all 1,656 measured slots with all
bug-detector and certification rows green.  GRB REV-12's revival mechanism
is therefore CONFINED (at probe scope) to strata with some e_j > 1 —
exactly its own narrowed statement ("e₀ = 1 does not force later e_j = 1");
the sharpest-case worry that a twist could appear even with EVERY e_j = 1
is measured dead.

**The level-2 Def-3.15 reading, discriminated externally for the first
time (M1).**  On this stratum reading C gives ǫ₂ ≡ 1 but the reading-P
(extraction-garbled) transcription gives ǫ₂(γ_j) = z₂^{−γ_j} ≠ 1 — a
slot-geometric z₂-character with step z₂^{γ₃}.  Measured: the reading-P
prediction FAILS at all 1,641 P-visible slots (22 of 24 towers carry
z₂^{γ₃} ≠ 1), on towers whose order-3 OM data are PARI-certified (CERT3:
factorpadic irreducibility + unique prime with (e,f) = (1, d₀g₀g₁g₂), 12/12
ℤ_p instances).  N-1's triple pin fixed reading C at level 1; this run
fixes it at level 2 — no prior unit had any level-2 ǫ instance at all.

## S2. What this settles and does not (the honest box)

* **Instances, not proofs.**  (IDENT-3) is confirmed at exact instances
  (order 3, all e_j = 1, g_j ≤ 3, d₀ ≤ 2, p ∈ {2,3}, four base rings, 24
  towers, deg Φ₃ ≤ 12) — a conjecture-level statement with the closed form
  AND its full derivation displayed (script header, sealed pre-run).  The
  theorem-backed inner legs: TR3-S1 (the digit-level identity, row P3) and
  the N-1/CERT2 certification of the level-0–2 machinery.
* **Displayed hypotheses.**  (H-v3): the GMN-3 ordinate consumes
  u_i = w₂(A_i) + i·w₂(Φ₂), the order-3 extension of the pinned order-2
  Def-2.5 identity v₂ = w₁ (shared-input scope: both sides consume the same
  w₂, as in the parent probes).  The level-3 FGMN transcription (ǫ₂ reading
  C + the one-step Def-3.16 recursion) is validated internally (V1–V5:
  multiplicativity, no-carry W-MULT, key lift) and externally (CERT3 PARI),
  not against the raw print (corrupted at Def 3.15; the recursion shape
  itself is clean in the extraction and quoted in the header).
* **The stratum is twist-degenerate BY DESIGN.**  θ = ℓ₀g₀h₀ = 0 and
  θ₂ = ℓ₁g₁γ₂ = 0 here, so the FGMN-normalized and GMN-lawful key classes
  coincide (P4, 24/24) and the TR3S2 key-convention bifurcation is
  invisible; nothing here tests the twist-visible regime at order 3.
* **NOT touched:** order-3 strata with any e_j > 1 (the revival regime —
  see S4); orders ≥ 4; the general σ_j family; D-ITER/composite digit
  algebra at order 3 (E₃ = 1 makes the composite rank 1 — empty here; the
  V3 δ ≡ 0 row is the degenerate no-carry control, all 240 green);
  TR-3-ORD-X's FGMN-internal Cor-5.15 leg (P5 is the transport leg only,
  matched labels).
* **Basis/convention scope:** normalized ℓ convention throughout (project
  standing hypothesis); reading C for ǫ at both levels (N-1 pins + this
  unit's M1/CERT3 discrimination at level 2).

## S3. COMPOSE-UNIT CHARTER (earned: the law returned exact)

Per the F3(c) protocol ("a compose unit fires only on an exact measured
law"), the following unit is CHARTERED:

**TR3-S3 (compose): TR-3′-GEN at ORDER 3 on the all-e_j = 1 stratum — the
finite-algebra proof note in TR3-S1/TR3-S2 format.**  Statement to prove:
under {σ₀, σ₁, σ₂} (the level-by-level identifications, built by matching
distinguished data as in TR3-S1 S2) and the normalized-ℓ convention,
σ₂(R^F₃(f)) = R_λ₃(f) IDENTICALLY (discrepancy ≡ 1) for every nonzero f at
every order-3 all-e_j = 1 state.  Proof route (= the sealed header
derivation, to be written as mathematics):
  (i) the ǫ-triviality kernel: s ≡ 0 and ℓ_j = 0 at every level under
      e_j = 1 (one line each from eq-(12));
  (ii) the level-3 Lemma A-GEN (row P2's identity): the forced-vs-actual
      abscissa z-shift m₀ = s₂(A_j), same proof shape as TR3-S2's Lemma
      A-GEN one level up;
  (iii) TR3-S1 (accepted) applied to each on-line digit;
  (iv) the GMN t₂-absorption: t₂(j) = s₂(A_j) at ℓ₁ = 0 (Def 2.19 verbatim).
Consumers: the (2′) R_λ leg of GD3-FULL/OL-B extends to order-3
all-unramified states ON THE NOSE (no character to thread); TR-3-ORD-X's
transport leg at these states via the P5 pattern (matched ψ₂ labels, both
key classes coinciding).  Falsifiers on file: this unit's 21,950/0.
Price: M (the pieces are all on record; no new mechanism needed).

**The NEXT stratum after that (queued behind the compose unit, NOT
chartered): order 3 with e₁ ≥ 2** — the genuine revival regime.  Sealed
candidate shape (preregistered in this unit's header, UNTESTED):
(TWIST-3) C^F3_j = z₂^{θ₂·s_j}·c^G3_{s_j}, θ₂ = ℓ₁g₁γ₂ — the exact level
shift of TR3-S2's θ = ℓ₀g₀γ₁.  A future probe there inherits this harness
(the general-e₂ code paths are written and only the stratum guard confines
them; realize2/gmn3 need the e₁ ≥ 2 abscissa strides exercised).

## S4. Run record

`python3 strata3_probe.py` → exit 0, 37.9 s, 21,950 samples, violations 0
on every preregistered row, all five meters MATCH; seed 20260817; rerun
identical mod timestamps.  PARI legs: CERT2 T11 (order-2 keys) + CERT3
(order-3 keys: factorpadic + idealprimedec, (e,f) = (1, d₀g₀g₁g₂) exact on
all 12 ℤ_p instances).  Preregistration sealed at commit d8ef8d8 BEFORE the
run; zero amendments; first full run ALL GREEN.
