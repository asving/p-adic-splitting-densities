PASS 14 — ACCEPTANCE DECIDER (Fable, fresh context) on B-M2 / H1_BM2 REVISION 14 + the three compiled H1 faces.

INDEPENDENT VERIFICATION PERFORMED (not a re-read of prior passes):
- All three gates re-run myself: `lake env lean` exit 0 on `Conformance.lean`, `RecParamE.lean`, `RCWitness.lean`; zero warnings, zero `sorry` (grep hits are doc phrases only); all 12 in-file `#print axioms` = {propext, Classical.choice, Quot.sound}.
- Load-bearing corpus fields verified at source, exactly as cited: `Node.hspecRec : species = ReadSpecies.recentering → e = 1 ∧ g = 1` (MovesC/Defs.lean:454), `hDwidth : Dwidth = σ.Φ.natDegree` (:418), `childWidth := ν.e * ν.g * ν.Dwidth` (:465), `Stage.hmonic`/`hdeg : 1 ≤ Φ.natDegree` (Moves/Defs.lean:124–125).

CORE STEPS RE-DERIVED (all confirmed):
1. Transport: `ν.hspecRec hspec` yields `he : ν.e = 1`, `hg : ν.g = 1`; `rw [he, hg]` turns `K0Conformant ν.σ ν.g ν.e Φhat` into `K0Conformant ν.σ 1 1 Φhat` = hK. The (σ, g, e′)-order with degree `e'·g·deg σ.Φ` is used consistently at every consumption site.
2. Width cancellation: `e·g·Dwidthᵢ = childWidthᵢ` (definitional, :465) `= Dwidthᵢ₊₁` (coherence width clause) `= deg Φᵢ₊₁` (hDwidth) `= deg Φᵢ` (kernel) `= Dwidthᵢ`; `d ≥ 1` by `Stage.hdeg`, so `Nat.eq_of_mul_eq_mul_right` gives `e·g = 1`, and `1 ≤ e, 1 ≤ g` force both = 1. `hi1 : i+1 < len` is exactly interiority; the e-half subsumption of `recParam_e_of_transition` clause (c) is index-exact (set its read index under `hi2`), clause (b) correctly displayed as remaining transition-case content.
3. `degree_sub` edge case: `degree_sub_eq_left_of_degree_lt hin` consumes precisely inC's `tt.degree < σ.Φ.degree`; sound at tt = 0 (⊥ < degree), no constant-parent hole; `natDegree_eq_of_degree_eq` is total. No `Node` in the kernel's type — the species fiat is genuinely unusable there.

S0 TABLE WALKED ROW-BY-ROW: every cell's citation resolves to a compiled declaration whose statement matches the cell's scope. [NR] actual-pair = the three carrier decls concluding at `(ν.g, ν.e)`. [RC] synthetic = the two `*_recentering_K0` conclusions `K0Conformant σ 1 1`, witnessed (rc_landingKey_gate on ν₁rc directly; rc_historyCoherent_gate on H₃rc, whose node-1 leg fires `recCore` — a genuine interior recentering, width chain 2 = 1·1·2); the `readsOf_landing_K0` recentering branch correctly stays UNWITNESSED (RCWitness constructs no `ReadsOf` run). [RC] actual-pair: the transport covers all species-recentering nodes; the wiring derivation's narrower interior-coherent-history scope is displayed in the cell itself. OL-1(a)/[RC] arithmetic (deg Φ′ = m = 1·1·m, f = g = 1 via `hψdeg`; at ν₁rc, ψ = X − C(center), deg 1) checks. The headline's compressed both-classes sentence carries its §S0 deferral in-sentence.

THE FOUR OPENS: each genuinely open — nothing compiled touches graded/initial-form structure (GRADED-READ); no `IsReadLift`-at-(1,1) or weight/twist law for the digit `−lift` (REC-SL proper); no site enumeration (SITE-EXH); no `ReadsOf` run containing a recentering read (the witness debt). §S4's consumer lists match §S0 and the headline's consolidated list verbatim; the compiled decls discharge only items already REMOVED from the list (REC-WIRE-G, e-half removal, node-level REC-PARAM) — no open is silently absorbed.

SEDIMENT SWEEP: the 14 records are dated and bracket-corrected; no live contradiction found between any record, the current body, and the Lean docstrings (the REV-9 "134" census figure is corrected in place at REV 10; C030's failure lesson is recorded at its own row).

ONE OBSERVATION, adjudicated NOT classifiable: the headline still reads *"Clean counter: 0/2; this unit's bar is the standard leaf bar"* (line 179) while the appended tail reads *"Clean counter: 1/2"* (line 2634). This is protocol-correct text freeze, not the stale-status genre: a CLEAN pass must not trigger edits (this pass had to run on the text pass 13 certified), the dated terminal record is unambiguous, and no mathematical status is mispriced. Update the counter at the acceptance fold.

NOTHING CLASSIFIABLE FOUND: no critical error, no justification gap. The [NR]/[RC] architecture + §S0 + the consolidated opens list price exactly what the three files prove — the compiled mathematics is correct, non-vacuous at the displayed witnesses, Lean-core, and every conditionality is named with its consumers.

VERDICT: CLEAN