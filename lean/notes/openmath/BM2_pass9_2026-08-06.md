1. **PASS — transport argument order is correct.**

> `def K0Conformant (σ ...) (g e' : ℕ) ... := ... natDegree = e' * g * ...`

The explicit argument order is `(σ, g, e')`, while the degree semantics are `e'·g·deg`. Therefore:

> `K0Conformant ν.σ ν.g ν.e Φhat`

correctly means degree `ν.e·ν.g·deg ν.σ.Φ`. In `k0_actual_of_recentering`, `he : ν.e = 1` and `hg : ν.g = 1`; `rw [he, hg]` correctly reduces the target to `K0Conformant ν.σ 1 1 Φhat`. There is no swapped display.

2. **PASS — the main REV-10 mathematical upgrades follow at the recorded [RC] sites.**

The derivation is exact:

- synthetic carrier theorem: `K0Conformant ν.σ 1 1 Φ'`;
- `Node.hspecRec`: `ν.e = 1 ∧ ν.g = 1`;
- transport: `K0Conformant ν.σ ν.g ν.e Φ'`;
- definition: `deg Φ' = ν.e·ν.g·m`;
- `Node.hψdeg`: `f_i = deg ψ = ν.g`;
- hence `deg Φ' = e_i·f_i·m_i`, with monicity already in K0 and integrality supplied by `Polynomial ℤ_[p]`.

This applies propositionally to all three compiled [RC] carrier implications, including the unwitnessed `readsOf_landing_K0` branch. The two `rc_*_gate_actual` declarations establish non-vacuity only for the two dedicated corollary paths. It does **not** establish an engine-wide “every [RC] firing” theorem; that still rides SITE-EXH. The DevBound degree-identification upgrade follows by the same calculation.

3. **CRITICAL — the current headline still asserts the superseded REV-2 status.**

> “what this unit PROVES is a D8-data/(K0)-CONFORMANCE theorem — … actual-pair (K0) on **[NR]** only; on **[RC]** synthetic (1, 1) only”

That is a present-tense current-unit claim, not merely a quotation inside the historical fold record. REV-10 proves actual-pair K0 on recorded [RC] carriers by transport. The note’s opening status therefore contradicts §S0 and the cited compiled lemma.

4. **CRITICAL — stale RG-2 claims directly contradict the transport upgrade.**

In the note:

> “an e ≥ 2 RECENTERING read is class [RC] — the compiled conclusion there is the SYNTHETIC `K0Conformant σ 1 1` only, so no actual-pair coverage exists at ANY stride”

In `Conformance.lean`’s live module header:

> “an e ≥ 2 RECENTERING read would receive only the SYNTHETIC (1, 1) conclusion.”

Both are stale after `k0_actual_of_recentering`. Moreover, `Node.hspecRec` makes a species-recentering node with `e ≥ 2` impossible. These sentences simultaneously ignore the actual-pair transport and discuss an uninhabitable node shape.

5. **GAP — one live REC-PARAM evidence paragraph retains the retired status.**

> “At-instance verification is EVIDENCE for the general claim; the obligation REMAINS OPEN”

Its immediate subject is the actual pair `(e,g)=(1,1)`, now supplied universally at species-recentering nodes by `Node.hspecRec`. Later REV-10 brackets partially correct the paragraph, but this sentence itself still states the old node-level REC-PARAM status. Historical displays elsewhere are expressly superseded; this one is not comparably fenced.

6. **GAP — REC-WIRE-G is named but not theorem-precise.**

> “the STAGE/WIRING-level g-half tie — non-fiat provenance of g = 1 (no Stage field records a g…)”

This does not specify the intended antecedents, indexed node/transition, wiring object, or formal conclusion. Unlike the e-half residual—which can be stated as `recParam_e_of_transition` with `hprev` removed—REC-WIRE-G is not yet a falsifiable proposition. No residual is obviously lost, and current consumer lists no longer require the retired node-level REC-PARAM, but this residual remains insufficiently specified for a later discharge audit.

7. **CRITICAL — the advertised 25 successor census rows are absent.**

REV-10 says:

> “25 successor sentences appended as rows **C135–C159**”

but the pasted census table ends at C134. Consequently the claimed classification “24 [TAGGED] + 1 [STATUS-FREE]” and arithmetic

> `132 − 21 + 25 = 136`

have no published row-level audit trail. The requested spot-audit cannot be performed against nonexistent rows, and the fresh sweep already finds live contradictory status sentences. Under the note’s own census rule, this is an automatic census finding.

I could not independently rerun `lake` or the census grep because the workspace command sandbox failed to launch, but the transport proof and declaration-level derivations above check directly from the pasted Lean definitions and theorem bodies.

VERDICT: CRITICAL