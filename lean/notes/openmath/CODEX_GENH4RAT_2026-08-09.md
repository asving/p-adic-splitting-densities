# GENH4 RATIFICATION — CODEX ADVERSARIAL PASS (decorrelated model, post-acceptance) + ADJUDICATION

## S0. Charter and provenance

**Pass:** the ratification sweep's SECOND leg (after HE7,
`CODEX_HE7RAT_2026-08-09.md`) on the FROZEN accepted text of
`GENH4_PROOF_2026-08-08.md` — ACCEPTED 2/2 (arc: seal 121d9ab → RED 3eb1667 →
GREEN 4b5c085 → PE1 263a86f/263a36f → r1 dff23f8 → PE2 2618e9b → r2
fd35416/5279a11 → PE3 a011533/3b88321 → r3 4b0d946 → PE4 CLEAN e15308d → PE5
CLEAN 11dbd23) plus ANNEX PE5 (baf05ed/e945734, ANNEX-LEMMA GENH4-7 + the A2
norm-units rider + the A3 "− extra…" closure). **Verifier:** Codex
`gpt-5.6-sol`, reasoning effort HIGH, fresh context, read-only sandbox,
charged quote-and-classify / fix-nothing. **The brief mandated the attack
order:** (1) THEOREM GENH4.A's census algebra at the E/F seam (the [r2]
comp-weighted correction's propagation); (2) THEOREM GENH4.C's σ-labelling
supply into W-12.D's (A1) [r4] clause, both characteristics, incl. the
F-SPLTAIL label the ANNEX derives; (3) the S9.3 level-0 assembly of the
eleven R_τ (double-count hunt at the shape boundaries); (4) ANNEX-LEMMA
GENH4-7(c)'s two-lift witness; (5) free hunt.

- **Brief:** `genh4rat_brief_2026-08-09.txt` (this directory, commit 57c9811),
  **128,625 chars, NO trims** (cap 1,048,576 not approached): PASTE A = the
  full GENH4 note byte-verbatim incl. ANNEX PE5; PASTE B = W-12.D + its
  (A0)/(A1)/(A2) with the [r4] σ-labelling clause + firing table + Step-4
  re-check, at the r5 text this note's S11 [r2] re-pin consumes
  (W12_PROOF lines 253–460); PASTE C = HM3.D statement + S5.5 assembly
  (HMENU3 lines 225–270, 469–496); PASTE D = GENHN.A(i)–(v)/GENHN.C/GENHN-T/
  GENHN.B head (GENHN lines 133–240, marked cross-check-only); PASTE E =
  QSCOUT22 Q22-L1/L2/L3 + grades + the orchestrator ANNEX (lines 245–268,
  331–346, 350–352).
- **Run of record:** session `019fe500-0ddc-7b52-9e32-c4dff01e560c`,
  gpt-5.6-sol, effort HIGH, **read-only** sandbox, launched 05:30:13 UTC
  2026-08-09 (stdin `-` prompt mode per the HE7 argv lesson), exited
  05:42:12 (**≈ 12.0 min, 86,127 tokens**), log
  `/tmp/codex_genh4rat_stdin_1786253413.log`; raw final message extracted to
  `/tmp/genh4rat_raw.txt` and reproduced verbatim in the appendix.
  codex-limits: 1% used before, 2% after. DISCLOSED: a first launch (session
  019fe4fd-eb34, 05:27 UTC) came up sandbox=workspace-write from the project
  trust config and was killed ~2 min in, before any finding output;
  relaunched with explicit `--sandbox read-only`.
- **Decorrelation discipline:** the orchestrator wrote a pre-verdict key for
  all four attack surfaces + free-hunt candidates BEFORE reading any Codex
  output (`genh4rat_scaffold_2026-08-09.md`, commit f96ba28, landed during
  the run) — with hand ledgers: full mass conservation at (q,N,k) = (2,7,1)
  genre F; the q = 2/3 level-0 census sums; the μ_E series; the (2,11,h5)
  slot strings. §S3 records the match.
- **Orchestrator machine legs (fold-side, this pass):** (i) the eleven S9.4
  R_τ forms re-typed fresh: Σ_τ R_τ = 1 exact at q = 2, 3; all 22 anchors
  match; E[#roots] = q/(q+1) at q = 2, 3, 5. (ii) a fresh transcription of
  the genre-F comp-weighted law: mass conservation EXACT at (2,7,1) and
  (2,8,1), reproducing every committed key the note quotes (RAM(7) = 128,
  2SIDED(3,4) = 384, SPLTAIL(3) = 384, UND = 128; SPLTAIL(3) = 1,536,
  2SIDED(3,5) = 1,536, UND = 1,024).

## S1. VERDICT (orchestrator adjudication, not Codex's self-grade)

**Codex returned 5 findings (2 CRITICAL-classified + 3 GAP). After
independent re-derivation of every claim AGAINST THE COMMITTED ARTIFACTS
(which the verifier by design did not have): 1 of the 5 REFUTED as classified
(F1 — the committed pre2 row partitions 2²⁰ exactly; the "missing" 393,216 is
the VTX + L1TAIL non-law-kind remainder, and "law keys" is the sealed
runner's LAW_KINDS term of art — surviving only as a MINOR display defect),
4 CONFIRMED as real defects of the text at their quoted sites, with 2
regraded in severity (F2 CRITICAL→GAP-with-flag; F5 GAP→MINOR). All four
charged attack surfaces came back CLEAN in substance — the two heavy
confirmed findings (F2/F4) land on the post-acceptance ANNEX's LEMMA GENH4-7
exactly where the charge pointed, and F3 (the stage-ring frame) came out of
the free hunt.**

**RATIFICATION OUTCOME: CHALLENGE** (confirmed GAP-or-worse exists — the
acceptance is annotated; Asvin decides). Scope, stated exactly:

- **F2 (CONFIRMED, GAP as stated; Codex graded CRITICAL — flagged both
  ways):** ANNEX-LEMMA GENH4-7's "hidden" gloss ("a₁-positions ≤ N−1 all
  vanish") is STRICTLY NARROWER than the F-SPLTAIL family the sealed laws
  count (law_F frees the A₀ boundary-band digits, `deep0` = q^{k−1}, carried
  by IN-WINDOW a₁ coefficients at positions N−k+1…N−1) — so GENH4-7(b) as
  stated labels only the band-zero slice. First live at k ≥ 2 AND N ≥ 4k+3;
  EVERY committed SPLTAIL row is k = 1 (band empty — the two definitions
  coincide there), and the fresh k = 2 row (2,9,2) has NO SPLTAIL leaf, so
  zero machine exercise. **The conclusion is TRUE for the full family by the
  annex's OWN displayed inequalities** (the proof of (a) uses v(a₀) ≥ N ∧
  v(a₁)+k ≥ N+1 — which is EXACTLY the true leaf's floor, and the omitted
  band-nonzero members satisfy it: u_lift ≥ N > 2w ⟹ 2SIDED at every lift
  ⟹ σ = {(1,2),(1,2)}); the defect is that the stated hypothesis
  contradicts the proof's own inequality (positions ≤ N−1 vanishing would
  give v(a₁)+k ≥ N+k, not N+1). One-line repair; but as written, THEOREM
  GENH4.C's F-SPLTAIL leg (whose F instance IS GENH4-7(b)) is gapped on the
  k ≥ 2 deep-window slice of a menu family that W-12.D's σ-labelling clause
  consumes — hence CHALLENGE, not annex-grade.
- **F3 (CONFIRMED, GAP as stated, display/frame scope):** S2.1's "R :=
  Ô[x]/(φ) is a complete DVR … residue field K" is literally FALSE at every
  genre-F entry (k ≥ 1) and at genre E with h ≥ 3 — R is the NON-MAXIMAL
  order O[θ] (Codex's instance checks: Z₂[x]/(x²+2x+4) mod 2 =
  F₂[ε]/(ε²), residue field F₂ ≠ F₄; my DVR-refutation at E h ≥ 3: m_R
  principal would force θ ∈ (π)·R, impossible at v(θ) = h/2 ∉ ℤ). Every
  CONSUMED fact survives: the note only ever uses v and res of EVALUATED
  elements A(θ), which live in L = Frac(R)/its valuation ring O_L — GENH4-2's
  ultrametric proof, S3's normalizer residues, S7's resolvent all go through
  in L verbatim; no count, census, or σ conclusion moves. Repair: rename the
  carrier (v := v_L restricted; residues in O_L). TRANSPORT OBLIGATION:
  GENHN.A(i) states the same false sentence at general (e₁,f₁) — owed to the
  live GENHN arc.
- **F4 (CONFIRMED, GAP, scoped to GENH4-7(c) — the cap-sharpness clause,
  NOT consumed by the W-12.D supply):** at k ≥ 2 band-pinned UND states
  (Codex's (k,N,w) = (2,9,5): band digit at height 10 fixed by the in-window
  a₁ digit at position 8) EVERY lift has u_lift ≤ 2w, so (c)'s exhibited
  u_lift > 2w witness does not exist; and (c)'s even-N leg ("different
  residual classes, hence different σ") is unjustified in char 2 (w > dμ
  forces S₁ = 0, T² + S₀ is always a square over F_{2^{2f}} — the read is
  REFINE, not a decided σ). The no-lift-sound-label CONCLUSION remains
  derivable (odd-N RAM(N) {(2,2)} vs u = 2w residual continuations) but is
  not displayed. The reader's UND exit at 2w ≥ N stays conservative-sound
  regardless — nothing consumed breaks.
- **F5 (CONFIRMED, regraded MINOR):** S7's unit-convention mix at genre F —
  the opening display v(A₁(θ)) = w/2, v(A₀(θ)) = u/2 and the
  2SIDED/INERT bullets ({w/2, (u−w)/2}; dμ/2) are E-halved forms flatly
  contradicting the bullet's own declaration "genre F: w, u in v₁ = v units"
  (true-unit values: w, u; {w, u−w}; dμ), while the RAM bullet's u/2 IS
  true-unit — and ANNEX A2 repaired only the norm tag, leaving its two
  neighbors. All σ conclusions are invariant under the rescaling (the INERT
  residue step is computed with n₂ = π^{dμ}, correct in true units); same
  defect species PE5 itself graded MINOR at A2.
- **F1 (REFUTED as CRITICAL; CONFIRMED as MINOR display):** the four P-3
  pre2 values sum to 655,360 ≠ 2²⁰ exactly as Codex computes — but the
  committed artifact row (`genh4_checks_results.json`, pin-verified md5)
  carries SIX keys summing to 1,048,576 = 2²⁰ EXACTLY: the remainder is
  VTX((0,10),(1,7),(4,0)) = 131,072 + L1TAIL = 262,144 — non-composite
  (vertex/order-1-tail) keys outside the runner's LAW_KINDS = ('RAM4', 'G2',
  'FULL', 'SUB', 'E', 'F') filter, whose law-kind keys are EXACTLY the four
  displayed with exactly the displayed counts. I re-derived both remainder
  strata by hand from the floored box (v(B₀) = 10 ∧ v(B₁) = 7 → the
  two-side vertex; v(B₀) ≥ 11 → the B₀-hidden tail) — counts forced, 2^17
  and 2^18. Residual defect: the note uses "law keys" (P-2, P-3) WITHOUT
  DEFINITION and displays no remainder, inviting exactly this partition
  misreading. No census contradiction, no machine number wrong.

No sealed number, no committed key count, no density, and no k = 1 σ-claim
moves. The eleven R_τ and the triple tie are UNTOUCHED (independently
re-verified this pass: Σ_τ R_τ = 1 exact at q = 2, 3; all 22 anchors; the
E[#roots] = q/(q+1) identity at q = 2, 3, 5).

## S2. The findings, transcribed + adjudicated (Codex's numbering; raw quotes in the appendix)

| # | Codex finding (compressed) | Codex class | adjudication |
|---|---|---|---|
| 1 | P-3's pre2 display "2²⁰ members, law keys EXACTLY {four}" sums to 655,360 ≠ 1,048,576 — deficit 393,216; exhaustive census contradiction, GH-PRE2's verification impugned | CRITICAL | **REFUTED as classified; CONFIRMED as MINOR (display).** The committed row (results JSON @ pin) partitions 2²⁰ EXACTLY with six keys: the four displayed law-kind keys at exactly the displayed counts PLUS VTX((0,10),(1,7),(4,0)) = 131,072 and L1TAIL = 262,144 — non-law kinds outside the runner's LAW_KINDS filter (line 299), which is what "law keys" means (P-2's "481 law-key checks" same usage). Both remainder strata re-derived by hand from the floored box: v(B₀)=10 ∧ v(B₁)=7 → the two-side vertex family, 2^17; v(B₀) ≥ 11 → the level-1 B₀-hidden tail, 2^18. The runner also volume-checks the FULL box (tot == (q−1)²q^{4N−10k₂−4}). Residual defect: "law keys" is never defined in the note and the remainder is never displayed — the sentence invites the partition misreading the verifier made. Display erratum owed, nothing else |
| 2 | ANNEX-LEMMA GENH4-7's "hidden" hypothesis (a₁-positions ≤ N−1 all vanish) is narrower than the counted F-SPLTAIL leaf (boundary band free, q^{k−1}); (b) proves the σ-label for a strict subfamily; W-12.D's every-member σ-labelling supply unproved as written | CRITICAL | **CONFIRMED — regraded GAP as stated, with the CRITICAL flag disclosed both ways (Asvin adjudicates).** Verified against the SEALED RUNNER: law_F prices SPLTAIL as (q^{comp(w)}−1)·wf(A1,w)·deep0 with deep0 = q^{Σcomp(s): s ∈ A₀, s>N} = q^{k−1} — the band digits (heights N+1…N+k−1, carried by IN-WINDOW a₁ positions N−k+1…N−1) are FREE in the counted family; GENH4-7's gloss pins them zero. The annex's proof-of-(a) inequality v(a₁)+k ≥ N+1 is EXACTLY the true leaf floor (heights ≤ N vanish), so the omitted members satisfy the whole (b) chain: u_lift ≥ N > 2w, 2SIDED at every lift, σ = {(1,2),(1,2)} — conclusion TRUE, hypothesis-gloss self-contradictory (positions ≤ N−1 would give v(a₁)+k ≥ N+k). First live k ≥ 2 ∧ N ≥ 4k+3; zero machine exercise (committed SPLTAIL rows all k = 1; (2,9,2) has no SPLTAIL — A₁ floor 2k+1 = 5 makes 2w < 9 empty). CONSUMED: S7's F-SPLTAIL bullet = GENH4-7(b) = the W-12.D σ-labelling supply row — a consumed clause is textually gapped on a nonempty menu slice, hence the CHALLENGE outcome. Repair: one-line hypothesis widening in a dated annex-erratum |
| 3 | S2.1's R = Ô[x]/(φ) "is a complete DVR … residue field K = F_{q²}" false: non-maximal order; R/2R = F₂[ε]/(ε²) at the annex's own key; E h = 3 analogue; associated-graded identification false literally | GAP | **CONFIRMED — GAP as stated, display/frame scope.** Codex's instance checks (θ² ≡ 0 mod 2). My independent leg: at E h ≥ 3, m_R = (π,θ) principal would force θ ∈ πR, impossible (v(θ) = h/2 ∉ ℤ); at h = 1 (Eisenstein) and only there R IS a DVR — genre F false at every k ≥ 1. Consumption audit: the note uses only v(A(θ)) and res of evaluated elements — all proofs (GENH4-2 ultrametric/parity, S3 normalizer residues, S7 resolvent, censuses as coordinate counts) run in L/O_L verbatim; S0's DULEMMA disclosure needs only v's multiplicativity (holds: v_L restricted). No count/σ/density conclusion moves. Repairs: carrier rename (v := v_L on O[θ] ⊂ O_L; residues in O_L; gr claim re-aimed at gr(O_L)). TRANSPORT: GENHN.A(i) carries the same sentence at general (e₁,f₁) — fold obligation to the live GENHN arc |
| 4 | GENH4-7(c)'s two-lift witness: at k = 2, N = 9, w = 5 with a fixed nonzero band digit, every lift has u_lift ≤ 2w — the u_lift > 2w lift does not exist; even-N leg's "different residual classes hence different σ" unproved in char 2 (S₁ = 0 forces '1sq'/REFINE) | GAP | **CONFIRMED — GAP, scoped to clause (c) only (cap sharpness), which the W-12.D supply does NOT consume ((b) is the supplied leg).** Both sub-claims verified: the band-pinned UND state has u_lift = min(v(a₀^lift), m) ≤ m ≤ 2w for every lift (m = the fixed band height); and at even N with w > N/2 the S₁ slot reads 0, so in char 2 the dμ = N/2 residual is always '1sq' — no decided σ at that read, and the annex does not carry the construction deeper. The (c) CONCLUSION (no single lift-sound label at 2w ≥ N) remains derivable — odd-N: RAM(N) {(2,2)} vs a u = 2w-even residual continuation; band-pinned states: u ∈ {N,…,m} spans decided legs with different σ — but is not displayed. The reader's UND exit stays conservative-sound; no consumer breaks |
| 5 | S7's v(Y) displays use E-halved units at genre F ({w/2,(u−w)/2}; INERT dμ/2) against the declared "genre F: w, u in v₁ = v units"; A2 fixed only the norm sentence; valuation-to-residue step false as written | GAP | **CONFIRMED — regraded MINOR (units display).** Verified: the opening display and the 2SIDED/INERT bullets are halved forms (true F-units: w, u; {w, u−w}; dμ); the RAM bullet's v(Y) = u/2 is TRUE-unit (v(A₀(θ))/2) — S7 mixes conventions inside one section, and the declaration contradicts the displays. All σ conclusions invariant (polygon shapes/denominators scale-blind; the INERT residue is computed against n₂ = π^{dμ}, right in true units; A2's own self-alarm analysis extends). Same defect species as A2's norm tag, which PE5 graded MINOR. Repair: one units-normalization rider covering all three sentences |

**CHECKED CLEAN (Codex, spot-verified against the pre-verdict key):** the
[r2] comp-weighted census propagation through S1/S6.1/S6.3 incl. the
(7,5,1) values 2,117,682/352,947 (= my P-1 hand ledger); the (2,5,1) and
(2,7,1) genre-F node closures (12,288+3,072+384+128+384+128 = 2^14 — matches
my machine transcription EXACTLY, incl. the u = 6 residual masses); the σ
dictionaries under correct rescaling incl. char-2 R₂′(s₁) = S₁ ≠ 0; the
level-0 censuses (q = 2: 16; q = 3: 81 — my hand sums); the
2·11/2·2/2̄² block assignments (no double count); R_{(2,1)²}(2) = 2/63 and
ALL 22 anchors with both Σ = 1 (= my sympy leg); the E/F drainage
telescoping incl. the even-N two-term law; the A3 closure (2SIDED mass
exactly 1/(q²+q+1)); GENH4-6's liveness inequalities.

## S3. Decorrelation record (pre-verdict key vs the delivered pass)

Key CONFIRMED by the pass: P-1 predicted CLEAN with the boundary-band
censuses as the weak corner — clean, and my (2,7,1)/(2,8,1) machine ledgers
match Codex's hand sums exactly; P-3 predicted CLEAN (census sums, shape
exhaustiveness, anchor spot) — clean, same numbers; P-5 candidates 1/2/3/5
(drainage ellipsis, two-term law, (2,11,h5) strings, liveness) — no finding,
consistent with my own closures. Codex DOMINATED the key on three fronts:
**F2/F4 (the annex band-digit mis-scope — my own P-4 key made the SAME error
as the annex, "positions ≥ N are all free", forgetting the in-window
a₁-carried band digits at k ≥ 2)**; **F3 (the non-maximal-order frame — my
key took S2.1's ring sentence at face value)**; and F1's arithmetic (my key
never audited the pre2 P-3 display — though here the artifact defeats the
classification). My key's pre-registered candidate findings that Codex did
NOT file: the GENH4-2-at-quartic-roots citation overreach (P-2's weak
corner; Codex folded it into its clean line "properly rescaled σ arguments"
— I keep it as a NOTED nit, no consumer breaks since S7 displays the two
transported facts its proof needs); the SPLITEQ-consumes-'1sq'-machinery nit
(disclosed inline by the note itself). Net: the decorrelated model earned
its cost on the annex's exact weak point (the charge's attack-2/attack-4
surfaces) and one frame sentence seven same-family passes read past — and
the committed-artifact check defeated its one would-be CRITICAL, which a
text-only verifier structurally cannot resolve.

## S4. Repair queue (owed as dated annexes/errata — the accepted body is FROZEN)

1. **R1 (F2, the heavy item): dated erratum to ANNEX-LEMMA GENH4-7** — widen
   the hidden-hypothesis to the true leaf floor ("a₀-positions ≤ N−1 vanish
   and a₁ heights ≤ N vanish, i.e. a₁-positions ≤ N−k; band positions
   N−k+1…N−1 free"), note the proof's inequalities already establish (a)/(b)
   for the widened family verbatim, and re-display (c)'s two-lift argument
   per-stratum (band-pinned states: odd-N RAM(N) vs residual continuation;
   plus the char-2 even-N leg carried one refine deeper or the claim scoped
   to what (c) needs). Until it lands, THEOREM GENH4.C's F-SPLTAIL leg
   carries an annotated grade on the k ≥ 2, N ≥ 4k+3 slice; k = 1 (every
   committed row) unaffected.
2. **R2 (F3): carrier-rename rider on S2.1** (+ the gr sentence) — v/res
   defined via L = Frac(R) and O_L; "DVR" claims re-aimed at O_L (true DVR
   statement) with the h = 1 Eisenstein case noted as the one where R = O_L;
   AND the GENHN.A(i) transport erratum filed to the live GENHN arc.
3. **R3 (F5): units-normalization rider on S7** — one sentence declaring all
   displayed valuation equations in E-halved units with the F true-unit
   table alongside (w, u; {w, u−w}; dμ), superseding A2's partial rider.
4. **R4 (F1): display erratum on S12 P-3/P-2** — define "law keys" (=
   LAW_KINDS, non-law remainder VTX/L1TAIL) and display the pre2 row's full
   six-key partition.
5. **R5 (noted, orchestrator's own): S7's "(GENH4-2)" cites at quartic roots**
   — a half-line "GENH4-2's proof applies verbatim at any θ with the two
   displayed v/res facts" would close the citation overreach.

## S5. Artifacts

- `genh4rat_brief_2026-08-09.txt` — the launched brief (this directory,
  commit 57c9811, 128,625 chars).
- `genh4rat_scaffold_2026-08-09.md` — the pre-verdict key (commit f96ba28,
  landed during the run, before any output was read).
- `/tmp/codex_genh4rat_stdin_1786253413.log` — full run log;
  `/tmp/genh4rat_raw.txt` — extracted canonical message.
- Orchestrator fold legs (this pass, not committed as scripts — displayed in
  S0/S1/S2): the sympy Σ/anchor/root-count check; the genre-F law
  transcription with node closures at (2,7,1)/(2,8,1); the committed-JSON
  pre2 row dump; the law_F/LAW_KINDS source reads.
- Appendix below — the raw Codex report, verbatim, first emission.

---

## APPENDIX: RAW CODEX REPORT (verbatim, first emission — session 019fe500-0ddc-7b52-9e32-c4dff01e560c)

## Findings

1. **Verbatim quote:** “pre2 (Zp,2,11,(1,2)): 2²⁰ members, law keys EXACTLY {E-RAM(21) = 32,768, E-UND = 32,768, G2(5,'2') = 65,536, RAM4(9) = 524,288}”

   **Classification:** CRITICAL ERROR

   **Failure:** The four allegedly exhaustive key counts sum to
   \[
   32768+32768+65536+524288=655360,
   \]
   whereas \(2^{20}=1,048,576\). The deficit is \(393,216\). Since the text says “law keys EXACTLY” and these reader keys are mutually exclusive terminal classifications, the displayed prehistory census cannot partition the stated member set. This directly contradicts GH-PRE2’s claimed exact verification and the asserted mass conservation/prehistory composition.

   Repair would need: identify the missing keys or correct one of the stated counts/total and re-establish the exhaustive sum.

2. **Verbatim quote:** “Hidden means every in-window position vanishes: \(v(a₀) \ge N\) … and \(v(a₁)+k \ge N+1\)” and “all data consumed are lift-stable … so S7's 2SIDED bullet applies to every lift”

   **Classification:** CRITICAL ERROR

   **Failure:** This narrows “A₀ hidden” beyond the leaf family counted in the body. S2.3 and S6.3 explicitly leave the genre-F boundary band free, contributing the factor \(q^{k-1}\). In particular, for \(k=2,N=12,w=5\), take all A₀ slots below \(N\) zero but the boundary slot \(m=N+1=13\) nonzero, supplied by the in-window \(a₁\)-coefficient at position \(11=N-1\). This is an actual hidden-band SPLTAIL member because \(2w=10<N\), and it is included by S6.1/S6.3, but it violates the annex hypothesis that all \(a₁\)-positions through \(N-1\) vanish.

   Thus GENH4-7(b) proves the F-SPLTAIL label only for a strict subfamily, not for “EVERY member” as required by W-12.D’s σ-labelling clause. The omitted members do satisfy the expected inequality \(u_{\rm lift}\ge N>2w\), but that argument is not given under their actual boundary-band conditions. Consequently the claimed W-12.D firing is unproved as written.

   Repair would need: formulate GENH4-7 for the actual free-boundary-band leaf and prove its every-lift inequality without killing those digits.

3. **Verbatim quote:** “\(R := \widehat O[x]/(\phi)=\widehat O[\theta]\)” and, for genre F, “\(R\) is an unramified-over-the-ladder DVR … residue field \(K=F_{q²}\).”

   **Classification:** JUSTIFICATION GAP

   **Failure:** The quotient order is generally not a DVR. In the annex’s own example
   \[
   \phi=x^2+2x+4,\qquad R=\mathbf Z_2[\theta],
   \]
   reduction modulo \(2\) gives
   \[
   R/2R\simeq\mathbf F_2[\varepsilon]/(\varepsilon^2),
   \]
   not \(\mathbf F_4\). Its maximal-ideal residue field is \(\mathbf F_2\), and \(\theta/2\), which generates the unramified integral closure, is not in \(R\). Likewise for genre E with \(h=3\), \(O[x]/(x^2-\pi^3)\) is the nonmaximal order \(O[\pi\sqrt\pi]\), not the valuation ring \(O[\sqrt\pi]\).

   The extension fields generated by \(\theta\) do have the stated ramification/residue degrees, and the later min-formulas may be recoverable in those fields. But the claimed DVR carrier, residue field of \(R\), and associated-graded identification are false literally; the advertised transport to “the stage ring \(O[x]/(\phi)\)” is therefore not justified as written.

   Repair would need: distinguish the nonmaximal order \(O[\theta]\) from the integral closure/valuation field and redo the lattice identification there.

4. **Verbatim quote:** “a lift with \(u_{\rm lift}>2w\) — realizable, the out-of-window digits are free” and “at even \(N\) … different deep lifts realize different residual classes, hence different σ”

   **Classification:** JUSTIFICATION GAP

   **Failure:** Free out-of-window digits do not override fixed in-window boundary-band digits. At \(k=2,N=9,w=5\), set the hidden boundary slot \(m=10=N+1\) of A₀ nonzero. It comes from the fixed in-window \(a₁\)-digit at position \(8\). Then every lift has
   \[
   u_{\rm lift}\le10=2w,
   \]
   so the exhibited \(u_{\rm lift}>2w\) lift does not exist for that hidden state.

   The even-\(N\) assertion is also not established in characteristic \(2\). For example, with \(q=2,k=1,N=6,w=4\), an \(u_{\rm lift}=6\) lift has \(S_1=0\), and over \(K=\mathbf F_4\), every \(T^2+S_0\) is a square. The first residual read is therefore REFINE, not a varying collection of decided σ-classes. Further digits might eventually furnish different σ, but the annex neither constructs them nor proves this.

   Repair would need: give case-specific disc-nonzero lifts accounting for fixed boundary digits and, at even \(N\), carry the construction through all required refinements.

5. **Verbatim quote:** “\(v(Y)\in\{w/2,(u-w)/2\}\)” and, under INERT, “\(v(Y)=dμ/2\).”

   **Classification:** JUSTIFICATION GAP

   **Failure:** These are correct only in genre E’s doubled \(dv\)-units. The setup explicitly says genre F uses \(w,u\) in ordinary valuation units, where
   \[
   v(A_1)=w,\qquad v(A_0)=u.
   \]
   Thus a genre-F 2SIDED leaf has actual root valuations
   \[
   v(Y)\in\{w,u-w\},
   \]
   and a genre-F INERT leaf has \(v(Y)=dμ\), not \(dμ/2\). At the annex’s \(w=3,u=7\) example the actual values are \(3,4\), not \(3/2,2\). Similarly, the genre-F normalizer is \(\pi^{dμ}\), so the claimed residue of \(Y/\pi^{dμ}\) requires \(v(Y)=dμ\).

   Annex A2 repairs the norm’s factor of two by calling the displayed quantities “halved root-values,” but no rescaled valuation is defined, and it does not correct the INERT statement. The σ conclusions are compatible with the correct valuations, but the valuation-to-residue step is false as written.

   Repair would need: separate E’s doubled units from F’s ordinary units throughout every S7 valuation equation.

## Checked clean

- The [r2] \(q^{\mathrm{comp}}-1\) correction is propagated consistently through S1, S6.1, S6.3, and the quoted \(q=7,N=5,k=1\) numbers:
  \[
  21(7-1)7^5=2,117,682,\qquad 21\cdot7^5=352,947.
  \]

- At \(q=2,k=1,N=5\), per \(\psi\), RAM \(=32\) and UND \(=32\), summing to \(2^6=64\).

- At \(q=2,k=1,N=7\), per \(\psi\):
  \[
  12288+3072+384+128+384+128=16384=2^{14},
  \]
  corresponding respectively to RAM(5), the \(u=6\) residual leaves, 2SIDED(3,4), RAM(7), SPLTAIL(3), and UND.

- The RAM, properly rescaled 2SIDED, SPLITEQ, INERT, and genre-E SPLTAIL σ arguments give the displayed dictionaries. In particular, in characteristic \(2\),
  \(R_2'(s_1)=s_1-s_2=S_1\ne0\) for a separable ‘11’ residual.

- The level-0 censuses sum correctly. At \(q=2\), the eleven counts are
  \[
  0,1,0,4,3;\;0,2,1,2,2,1,
  \]
  totaling \(16\). At \(q=3\), they are
  \[
  0,9,3,24,18;\;3,9,3,6,3,3,
  \]
  totaling \(81\).

- The distinction between \(2\cdot11\), \(2\cdot2\), and \(\bar2^2\) is exhaustive and nonoverlapping, and their block assignments to \(\nu_2\), \(\nu_2\otimes\nu_2\), and \(\nu_2^{(2)}\) are correct.

- \(R_{(2,1)^2}(2)=4/(2\cdot9\cdot7)=2/63\). All eleven displayed formulas reproduce both quoted \(q=2\) and \(q=3\) anchor lists, and each list sums exactly to \(1\).

- The E/F drainage exponents, including the even-\(N\) two-term genre-F expression, telescope to the stated powers. The A3 per-node masses also sum to \(1\), with 2SIDED mass exactly \(1/(q^2+q+1)\).

- GENH4-6’s displayed slot-liveness inequalities are arithmetically sound under the stated admissibility floors.

## Verdict

The document does **not** survive ratification as CLEAN. The prehistory test row contains an outright exhaustive-census contradiction, and the post-acceptance annex fails to prove the every-member genre-F SPLTAIL σ-label that the note says it supplies to W-12.D. The non-DVR stage-ring assertion, unrealized two-lift argument, and unresolved genre-F valuation scaling add substantial justification gaps. These findings materially challenge the acceptance.
