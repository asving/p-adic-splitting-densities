# BM2_TRACE — the recentering seam settled on a worked instance (B-M2 concrete-trace unit, 2026-08-02)

**Charge (Asvin's standing instruction).** The B-M2 (LIFT-CONF) unit took three
hostile CRITICALs on ONE seam — what is actually true at RECENTERING events
(`H1_BM2_2026-08-06.md` REV 3/4; `BM2_pass{1,2,3}_2026-08-06.md`). This unit
settles the seam numerically on a genuine order-≥1 tower with a real
recentering, traces every valuation/key/digit/graded computation, and returns
per-claim verdicts. Script: `verification/openmath/bm2_recentering_trace.py`
(exact arithmetic, deterministic, preregistered predictions in the header;
ALL CHECKS PASS, exit 0; PARI cross-check included).

**Headline.** All four mathematical claims are TRUE at the instance — none
refuted. REC-PARAM's content ((e,g) = (1,1) at a recentering) is not only true
but FORCED by three independent mechanisms, all exhibited numerically; the
corpus's synthetic (1,1) is the honest mathematics, merely not yet tied to the
recorded node fields (a wiring gap, not a math gap). The one genuinely negative
finding is at C-e/SITE: this REAL tower's recentering is UNRECORDABLE inside
any lawful full `ReadsOf` run (the RG-2 fence + the deg-1-base pin exclude it)
— a concrete exhibit that the disclosed fence excludes genuine histories.

## 1. The instance

    p = 3,   f(x) = x⁴ − 12x³ + 51x² − 90x + 198  ∈ ℤ₃[x]
           = (x² − 6x + 3)² + 9·(x² − 6x + 3) + 162.

PARI (gp, `factorpadic(f,3,30)` + `idealprimedec`): f is IRREDUCIBLE over ℚ₃,
one prime, (e,f) = (2,2). Root valuations, computed exactly two independent
ways (script §P7: resultants; graded trace): with θ a root,

    v(θ) = v₃(Res(f,x))/4    = 2/4 = 1/2,
    v(Φ₁(θ))  = v₃(Res(f,Φ₁))/4  = 6/4 = 3/2   (Φ₁  = x² + 3),
    v(Φ₁′(θ)) = v₃(Res(f,Φ₁′))/4 = 8/4 = 2     (Φ₁′ = x² − 6x + 3).

## 2. The tower, node by node (absolute v₃ scale, v(3) = 1)

| node | key Φ | side slope | (e,h)_stage | ψ(z) | g | μ | event |
|---|---|---|---|---|---|---|---|
| 0 | x | −1/2 | (2,1) | z+1 | 1 | 2 | AUGMENT → Φ₁ = x²+3 |
| 1 | x²+3 | −3/2 | (1,3) | z−2 | 1 | 2 | **RECENTER**: lift 6x, Φ₁′ = x²−6x+3 |
| 2 | x²−6x+3 | −2 | (1,4) | z²+z+2 | 2 | 1 | REGULAR, terminate |

E = 2·1·1 = 2, F = 1·1·2 = 2, E·F = 4 = deg f ✓ (= PARI's (2,2)). The tower
exercises e > 1 (node 0), a genuine ORDER-1 recentering with a NONTRIVIAL
digit c = 2 ≠ 1 (node 1), and g > 1 with BOTH lower digits present (node 2).

**Node 0** (base frame x). Polygon of f: points (i, v₃(aᵢ)) =
(0,2),(1,2),(2,1),(3,1),(4,0); single side slope −1/2, on-line points
i = 0,2,4 with digits 198/9 ≡ 1, 51/3 ≡ 2, 1: R₀(z) = z² + 2z + 1 = (z+1)².
ψ₀ = z+1 = z−2, g = 1, μ = 2, root 2. e·g = 2 > 1 ⟹ proper augmentation
(NOT recentering-eligible): standard lift Φ₁ = x^{e·g} + t₀ with t₀ = 3
(digit 1 = ψ₀.coeff 0 at weight h·(g−0) = 1). deg Φ₁ = 2·1·1.

**Node 1** (frame Φ₁ = x²+3; stage lattice Γ₁ = (1/2)ℤ; K = 𝔽₃; graded ring
gr = 𝔽₃[ū^{±1}][Ȳ₁], ū := in(x), ā := in(3), relation ū² = −ā valid since
w(Φ₁) = 3/2 > 1 = w(x²) = w(3); Ȳ₁ := in(Φ₁)). Development
f = Φ₁² + B₁Φ₁ + B₀, B₁ = −12x + 45, B₀ = −54x + 54. Slot weights
w₁(B₀) = 3, w₁(B₁) = 3/2, w₁(B₂ = 1) = 0: single side slope −3/2, all three
slots on-line. 3/2 ∈ Γ₁: **e_read = 1**; stage pair (1,3). Residual with the
one-slot anchor A := in(3x) = 2ū³ and slot anchors A^{2−j}:

    r₂ = 1;  r₁ = in(−12x)/A = (1·ū³)/(2ū³) = 2;  r₀ = in(54)/A² = (1·ū⁶)/(ū⁶) = 1
    R₁(z) = z² + 2z + 1 = (z − 2)².

ψ₁ = z − 2, **g = 1**, μ = 2, root c = 2 ≠ 0 ⟹ **the recentering fires**:
lift = 6x (in(6x) = 2·A, digit 2 = c at weight 3/2 = w(Φ₁), position 0),
Φ₁′ = Φ₁ − 6x = x² − 6x + 3. Root-side cross-check: Φ₁(θ)/(3θ) ≡ 2 and
v(Φ₁′(θ)) = 2 > 3/2 (P7) — strict improvement, as the last `HistoryCoherent`
conjunct demands. (Anchor covariance, noted: with anchors ū^{3(2−j)} instead,
R̃₁ = (z−1)² and digit(6x) = 1 — root and digit COVARY; every identity below
is checked inside one fixed convention, and holds in either.)

**Node 2** (frame Φ₁′; same lattice, same K — recentering changes neither).
f = Φ₁′² + 9Φ₁′ + 162; slot weights 4, 2+2, 0+4 → single side slope −2, all
slots on-line, stage pair (1,4). Anchor in(9) = ū⁴: R₂(z) = z² + z + 2,
irreducible over 𝔽₃ (no roots), g = 2, μ = 1 ⟹ regular, terminate.

## 3. Per-claim verdicts

### (C-a) REC-PARAM — TRUE-AT-INSTANCE (and forced; hunt found no counterexample)

Claim quoted (note §S2 case (b)): *"the recentering read is a genuine
(e, g) = (1, 1) read of the tower, not merely priced at (1, 1) by fiat."*

At the instance the recentering read's ACTUAL pair is (e_read, g) = (1, 1):
slope 3/2 lies IN Γ₁ (e = 1) and ψ₁ = z − 2 is linear (g = 1). Three
independent forcings, each numerically exhibited:

1. **Degree arithmetic**: a recentering replaces the key at the SAME degree;
   a read produces a key of degree e·g·m; m = e·g·m ⟺ e·g = 1.
2. **Lattice forcing (e = 1)**: the lift must be an in-window element
   (deg < m) with w(lift) = the side value; window weights lie in the parent
   lattice Γ; an e ≥ 2 side value is off-lattice. Script hunt leg (i): 459
   e > 1 sides checked across random towers (p ∈ {2,3}, order 0 and order 1
   over the instance's own ramified stage), every side value off-lattice.
3. **Residue forcing (g = 1)**: the recentering digit is a constant c ∈ K×;
   ψ(c) = 0 with ψ irreducible forces deg ψ = 1. Hunt leg (ii): 80
   irreducible residual factors of degree > 1 checked, all K-rootless.

Hunt totals: 905 reads, 387 recentering-eligible events, ALL at (1,1); zero
counterexamples. **Corpus face (the actual REC-PARAM gap):** `IsRecentering`
(Moves/Defs.lean:264ff) carries σ.e = 1 ∧ σ′.e = 1 as conjuncts — the STAGE
pair — and `HistoryCoherent`'s recentering leg (MovesC/Defs.lean:717ff)
asserts only `IsRecenteringCore σᵢ σᵢ₊₁ center lift`, never touching the
NODE's recorded (νᵢ.e, νᵢ.g). Partial in-corpus derivability, checked against
source: when the recentering node's stage was forged by a non-recentering
transition, `TransitionData.child_e : σ′.e = e′` (Moves/DefsT.lean:150) ties
σᵢ.e = νᵢ.e, so IsRecentering's σ.e = 1 DOES force νᵢ.e = 1 — the e-half of
REC-PARAM is derivable in that configuration. The g-half (νᵢ.g = 1) is tied
NOWHERE: a Lean history could record νᵢ.g = 7 at a recentering node and stay
coherent. So REC-PARAM is a true-of-the-mathematics identification that the
wiring genuinely does not yet enforce — the three-pass fight was about a real
(but purely wiring-side) gap, and the compiled synthetic (1,1) is the CORRECT
pricing of the mathematics. (HK25_recGate's node hand-picks e = g = 1,
consistent, chosen not derived.)

### (C-b) (K0) at the recentering, ACTUAL parameters — TRUE-AT-INSTANCE

deg Φ₁′ = 2 = 1·1·2 = e·g·m at the actual read pair (1,1), Φ₁′ monic. The
compiled `K0Conformant σ 1 1 Φ′` conclusion, evaluated at the instance's
actual parameters, is exact — no fiat.

### (C-c) the recentering digit datum vs the stage-law fields — TRUE-AT-INSTANCE

Claim quoted (note §S2 case (b), the OWED list): *"nothing compiled or
displayed here establishes that Φ′ is an `IsReadLift` at e = g = 1, or that
its sole lower digit −lift satisfies the (SL-dev) weight and twist-residual
obligations."* Checked numerically — they ALL hold, i.e. REC-SL's owed
content is TRUE here. Writing Φ₁′ = Φ₁^{1·1} + t̃₀ with t̃₀ := −lift = −6x,
against `IsNodeLift`'s fields (MovesC/Defs.lean:535) at (e,g,ψ) = (1,1,z−2):

* window: deg(−6x) = 1 < 2 = deg Φ₁ (`inC`) ✓;
* weight law: w(t̃₀) = 3/2 = h·(g−0) (stage units: 3 = h) ✓; and
  `IsRecentering`'s own clause w(lift) = w(Φ₁) = 3/2 ✓;
* residual law: digit(t̃₀) = 1 = ψ₁.coeff 0 (and digit(lift) = 2 = center c —
  the two faces are sign-consistent: ψ = z − c, coeff 0 = −c) ✓; twist
  position: canonical Bézout t = 0 at e = 1 (`hbezCanon`) gives T-exponent
  −t·wPrev = 0, matching `IsRecentering`'s recorded `T 0` ✓;
* (SL-top): D_g = D₁ = 1, literally Φ₁′ = 1·Φ₁ + t̃₀ ✓;
* the graded (SL-dev) identity at κ = 0: in(t̃₀) = 2ū³ = 1·ψ₁,₀·ϕ_{3/2}
  with the mixed anchor ϕ_{3/2} = in(3x) (γ̂ = 1·1·(3/2)), shared U₀ = 1 ✓.

So at this instance the recentered key IS an (e,g) = (1,1) standard lift with
marched data — the missing REC-SL theorem is a transcription obligation, not
a mathematical risk.

### (C-d) GRADED-READ, the (SL-dev) identity at non-recentering reads — TRUE-AT-INSTANCE

Claim quoted (note §S1): *"(SL-dev) in(D_κ·Φ_i^{eκ}) = U₀·ψ_κ·ϕ_{γ̂}·ȳ^κ
(κ with ψ_κ ≠ 0)"*. Checked at BOTH non-recentering reads, in the explicit
graded rings, all slots, one shared U₀:

* **Read 2** (γ̂ = 1·2·2 = 4, ϕ_γ̂ = in(3⁴) = ū⁸, ȳ = Ȳ·in(9)^{−1}):
  κ=0: in(162) = 2ū⁸ = U₀·ψ₀·ϕ_γ̂ with ψ₀ = 2 ⟹ U₀ = 1;
  κ=1: in(9Φ₁′) = ū⁴Ȳ = U₀·ψ₁·ϕ_γ̂·ȳ with ψ₁ = 1 ⟹ U₀ = 1;
  κ=2 (SL-top): in(Φ₁′²) = Ȳ² ⟹ U₀ = 1. ONE shared U₀ = 1 ✓; digits
  nontrivial (2 and 1). (K1): every slot value = 4 = γ̂ ✓. (K2):
  R^gr_γ̂(in f) = z² + z + 2 = ψ₂ with U₀ = 1 ✓. Absent-slot law: the κ with
  ψ_κ = 0 — none here; at node 0's lift the k=1 slot of ψ₀... (see read 0).
* **Read 0** (the e = 2, mixed-anchor case; γ̂ = 2·1·(1/2) = 1, ϕ₁ = ā,
  ȳ = x̄²ā^{−1}, free ring 𝔽₃[x̄, ā^{±}]): κ=0: in(3) = ā = U₀·1·ā ⟹ U₀ = 1;
  κ=1 (SL-top): in(x²) = x̄² = 1·ā·(x̄²ā^{−1}) ⟹ U₀ = 1 ✓. The e = 2
  TWIST position exhibited: base σ.t = 1 (canonical for (e,h) = (2,1)),
  wPrev(t₀) = 1 ⟹ position −1; graded content: 3 ≡ z̄₀^{−1}·x̄² with
  z̄₀ = 2, i.e. x̄² = 2ā — exactly the post-augmentation relation ū² = −ā.
  The digit-at-position-(−1) bookkeeping is the true statement of the
  corpus's `T^{−σ.t·σ.wPrev}` field at this read.

So the GRADED-READ seam's content — that the wired window/weight/twist fields
yield the graded identity — is TRUE at this instance in both the ramified
(e=2) and inert-extension (g=2) shapes, with the marched normalization and
U₀ = 1 exactly as B-M1 §S3.2 predicts.

### (C-e) firing sites — the RG-2/SITE-EXH exhibit (the negative finding)

At this instance, which carriers can lawfully fire? `ReadsOf` conjoins
`HistoryCoherent` (HC2/Defs.lean:320ff); `HistoryCoherent` pins node 0 to a
DEGREE-1 frame and conjoins νᵢ.e = 1 on every interior non-recentering leg
(the RG-2 fence). This tower's first read (e₀ = 2) is INTERIOR (two reads
follow). Consequently:

* the e = 2 read fires only as the FINAL read of a 1-node run (LandingKey,
  landing Φ₁) — representable in principle (U31/HK23 gate precedents);
* the order-1 RECENTERING (node 1) and the final read (node 2) are
  UNREACHABLE by any lawful full run on f: reaching frame Φ₁ requires the
  e = 2 interior step the fence forbids, and a history STARTING at Φ₁ is
  barred by the deg-1 node-0 pin. The engine mathematically fires a
  recentering that the wiring cannot record at all.

This is a DATA POINT for SITE-EXH/RG-2, not a refutation of any compiled
claim (the fence is disclosed; LIFT-CONF quantifies over fired-on-recorded
carriers): a genuine ℚ₃-quartic whose recentering — the very species this
unit fights over — falls outside the recordable carrier set. Pass-3
CRITICAL-4's complaint ("does not exclude an e ≥ 2 recentering; does not
prove fired e ≥ 2 lifts occur at final reads") is CONSISTENT with the trace:
here the e ≥ 2 lift is interior-in-nature and simply unrecordable. A
RECORDABLE order-1 recentering exists in nature (sketch: p = 3, first read
(e,g) = (1,2) at slope −1, Φ₁ = x² + 9, then a recentering over K₁ = 𝔽₉ —
fence-free since e₀ = 1); the corpus's carrier set is nonempty on real
towers, just not exhaustive of them. No FOURTH carrier type was found: the
only key-firing predicates wired into runs are `IsNodeLift` +
`IsRecenteringCore` (HistoryCoherent 4th conjunct) and `LandingKey`
(SideReads (iv), KeysLawful) — grep-confirmed; closure stays SITE-EXH's.

**Verdict: NOT-EXERCISED** (this instance cannot exercise the recentering
carriers inside a lawful run) **+ exhibited fence-exclusion data point.**

## 4. What this changes for B-M2

* REC-PARAM and REC-SL are **true obligations, not danger**: at the instance
  every owed clause holds with the synthetic (1,1) equal to the actual pair.
  The continuation work is transcription (tie νᵢ.g = 1 into the recentering
  leg, or derive it from a recorded-residual-root field), not repair. The
  e-half already has a derivation path via `child_e`.
* GRADED-READ's identity is numerically real at both e > 1 and g > 1 shapes
  with the marched twist bookkeeping — supporting (not proving) the
  GenuineStageModel discharge.
* The live structural debt this trace SHARPENS is RG-2/SITE-EXH: real towers
  with interior e ≥ 2 steps (generic among ramified quartics) are outside
  the recordable history class, recenterings-behind-them included.

— B-M2 concrete-trace unit, 2026-08-02. Script green (exit 0), PARI-confirmed;
no edits to `H1_BM2_2026-08-06.md` (concurrent revision in flight).
