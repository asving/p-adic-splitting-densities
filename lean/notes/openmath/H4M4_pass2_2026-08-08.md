Fold audit:

- [C1] cured. Branch (iii) no longer says both routes die and splits the escalation correctly between stable and unstable violating positions.
- [C2] cured. The precedence `(iii) → (i) → (ii)` is coherent and, with the branch conditions stated, exhaustive.
- [G3] cured. §5.4 is explicitly non-load-bearing, its missing consumer walk is assigned to H4-M7, and §5.5 bases the recommendation on §5.2 alone.

Findings:

1. **GAP — the imported Lean faces are opaque, preventing the required exactness audit.**

   > “Route A's target is EXACTLY `K3deltaStmt p RL` … the naive-(b) reading … is `UBXbStmt p RL`”

   and

   > “The corpus faces `UBXbStmt`/`K3deltaStmt`/`RealizedLedger`/`AlphabetData.Stable` are consumed by IMPORT, verbatim, never re-transcribed or weakened.”

   The quoted authoritative prose is sufficient to compare Routes A and B at the prose level, and the displayed proofs are consistent with the claimed outer logical shapes. But the note does not expose the imported definitions needed to verify that `RL.Pool` really means a realized δ > 1 pool, that `poolIdx` carries the intended absolute δ, or that `UBXbStmt` has exactly the claimed stability and realization quantifiers. Consequently, the claimed exact prose–Lean match for the declaration list cannot be fully verified from the supplied material.

2. **GAP — the all-\(n,p,\delta\) realization burden is asserted but not displayed.**

   > “(K3-δ) is consumed by clause (R) at EVERY degree n and every (p, δ) the assembled theorem touches.”

   This is load-bearing for pricing Route A as a theorem-strength realization problem rather than an isolated pool obligation. The quoted authoritative row says “at every δ” and restricts K3-δ to realized δ > 1 pools, but no quoted material displays the ambient quantification over every degree \(n\) and prime \(p\). Correspondingly, the Lean declarations fix `p` and contain no `n`. Assuming the external assembly quantifiers, the cheap-A analysis follows; they cannot be verified from this note alone.

3. **GAP — “branch-robust” overstates what the precedence table establishes.**

   > “**RECOMMENDATION: ROUTE B**, branch-robust in N-K3's pending verdict”

   versus branch (iii):

   > “Route B is REFUTED AS STATED, and the adjudication RE-OPENS”

   and §5.5:

   > “the call is robust … (modulo the dominating escalation branch)”

   The precedence itself is sound, and the branch-(iii) disposition is now honest. But the recommendation is not robust across the pending verdict: the highest-precedence outcome refutes Route B and cancels the adjudication. It is robust only across the non-escalating outcomes (i) and (ii). This does not invalidate the present §5.2-based recommendation, but it leaves the summary’s stronger robustness claim unsupported.

No countermodel is claimed: the realization question remains open, and composite δ is never said to force a realized noncoprime \(m\). No row is discharged; H4-F5 remains fenced, the ROOT authorization requirement is intact, and H4-M5/H4-M7 travel together. Subject to Finding 1’s opaque imported definitions, all seven displayed declarations match their local prose, and the dictionary semantics remain explicit hypotheses rather than being discharged by fiat.

VERDICT: GAPS