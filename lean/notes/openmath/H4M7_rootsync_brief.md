/-
# H4-M7 — ROOT-SYNC BRIEF DRAFTS for the ROOT owner (Fable prover H4-M7, 2026-08-01, Wave 5)

Unit H4-M7 of `lean/blueprints/HDISCHARGE_H4.md` (§M table row). This file is a
LITERATE LEAN MARKDOWN NOTE (the H4-M2/H4-M4 format): prose in comment blocks,
every Lean line compiles. Gate command (from `lean/`):
`lake env lean notes/openmath/H4M7_rootsync_brief.md` — green, zero sorry.

CHARGE (blueprint §M row, verbatim): "**H4-M7** | ROOT-sync brief (for the
ROOT owner, NOT a self-serve edit): retirement chain §1 executed — (H4a) row
retire + P(O-8b/M14) firing + (K3-δ) attribution return | after M1+M2 land
AND their passes; separately after M4/M5 | the landed units | MECH | ROOT
owner executes; this unit only drafts the delta."

STATEMENT-FENCE RECORD: the blueprint has NO §L Lean spec for H4-M7 (a §M
math-note unit, grade MECH); the charge is transcribed verbatim above; the
BP assigns no file path — this filename is synthesized per the dispatch. The
Lean content below is cross-reference PINS only (compile-checked pointers at
the corpus faces the briefs cite); no new mathematical declaration is made,
nothing is discharged, no file outside this note is edited.

DATING NOTE (inherited from H4-M5 pass-1 finding 10): swarm artifacts carry
inconsistent session labels (M1 "2026-08-06", M3's pass "launched 2026-08-08",
M4/M5/M6 "2026-08-01", the blueprint "2026-08-05"). Labels, not a chronology.
ON-FILE below means: the file exists in the repo and was read this session.

## §0. VERDICT SUMMARY (read this first)

1. **Three ROOT-owner briefs are DRAFTED below** (§2 the (H4a) retirement
   chain incl. the P(O-8b/M14) firing charge; §3 the §3.1/§3.2 display
   deltas; §4 the (K3-δ) attribution return via the SELECTED Route-B
   re-scope). Each brief carries its exact FIRING CONDITIONS.
2. **NONE of the three is fire-ready today.** The dispatch asserted
   "H4-M1..M5 landed + passed"; the repo record (§1 ledger, verified this
   session against the note files themselves) shows all five LANDED but the
   hostile-pass gates NOT all satisfied: M1's ×2 UNRUN (empty ledger),
   M2's ×1 UNRUN (no record), M3's ×1 IN FLIGHT (unit self-reports PARTIAL),
   M4's pass 2 announced but ABSENT from its file, M5 closed at ONE clean
   ACCEPT (a second consecutive clean is owed at the division's next touch).
   Per the blueprint's own timing rule ("draftable after M1+M2 land AND
   their passes") the briefs are DRAFTS WITH DISPLAYED GATES — exactly what
   a ROOT-sync brief must be, since "ROOT owner executes; this unit only
   drafts the delta". This unit reports PARTIAL, not BLOCKED: the drafting
   charge is executed in full; the firing conditions are not this unit's to
   discharge.
3. **Clause-(UB) sync content is FENCED OUT** (§5): H4-M3's own verdict
   holds (UB-X)(a) OPEN on Lemma RRE-δ (row 10) and states "H4-M6/H4-M7
   stay BLOCKED on it" — so NO Step-16 clause-(UB) display delta is drafted
   here beyond restating its honest conditionality. The (H4a)/(K3-δ) briefs
   are unaffected: neither consumes (UB-X)(a).

## §1. GATE-STATUS LEDGER (verified against the repo this session; the
firing conditions of §§2–4 point here)

| unit | landed | gate record (as found in the file itself) |
|---|---|---|
| H4-M1 `H4M1_rpack_attempt.md` | YES ((R1)–(R3) over W(F_q), 8 sections, consumption audit + δ-instance/EQ-1 walk §8) | N-T4 RUN, ALL PASS (in-file §N record). Hostile passes ×2: **UNRUN** — the file's ledger holds only the two placeholder lines ("see below this line after the run"; nothing below) |
| H4-M2 `H4M2_dict_attempt.md` | YES (dictionary slice + (R4) + Lemma 4; Lean layer compiled, Lean-core footprint) | Hostile pass ×1: **NO RECORD anywhere in the repo**. N-K3 gcd TABLE: compiled in-file (`nk3_gcd_table`); the empirical N-K3 RUN is separate and unrun |
| H4-M3 `H4M3_leafaudit.md` | YES (all 9 rows + added row 10) | Pass ×1 **IN FLIGHT** (launched, verdict not returned at report deadline; unit self-reports PARTIAL). VERDICT ON FILE: rows 1–9 CLOSED (i)/(iii); row 10 class (iv) — **Lemma RRE-δ OPEN** ⇒ **(UB-X)(a) OPEN**; "H4-M6/H4-M7 stay BLOCKED on it" |
| H4-M4 `H4M4_k3delta_adjudication.md` | YES (both routes stated; **RECOMMENDATION: ROUTE B**, branch-robust in N-K3's pending verdict) | Pass 1 RAN: REVISE 2C/1G, all folded. Pass 2: the file says "verdict recorded below" but **NO pass-2 verdict exists in the file** — a display defect the ROOT owner should have repaired at fold time; the M4 gate is NOT closed |
| H4-M5 `H4M5_k3delta_attempt.md` | YES ((RB) proved AS A DISPLAYED CONDITIONAL on the five §1.3 rows [RUN-PROD]/[LEDGER-READ]/[LET-DATA]/[SYM-ID]/[M1-STATUS]) | FOUR passes: REJECT(2C/8G) → REJECT(1C/4G) → REVISE(0C/4G) → **ACCEPT (clean)**, all folded. Ledger's own closing rule: ONE clean on record; **a second consecutive clean is owed**. N-K3: predictions SEALED §6; **run owed** |
| H4-M6 `H4M6_commute_attempt.md` | YES ((UB-X)(c) as the §3.3 displayed conditional) | Pass 1 REJECT(1C/5G) folded; pass 2 **clean ACCEPT**. Premise (i) ((UB-X)(a) audit CLOSED) **UNSATISFIED on file** — M6's own §0 says so |

N-K3 (blueprint §N; seals M4/M5 jointly): **NOT RUN** — no artifact on file;
M5 §6 holds the sealed predictions. A run violating the intrinsic law (RB)
refutes Route B (and escalates to (EQ-2) per the §N disposition); §4's brief
is DEAD on that branch and the §3.2 adjudication reopens at M4's branch table.

## §2. BRIEF 1 — the (H4a) retirement chain (blueprint §1, executed as a draft)

Chain steps verbatim from the blueprint, each with its firing condition and
what the ROOT owner does. Steps (iii)/(iv) are ROOT-owner moves.

- **(i) The (R1)–(R4) re-scoping pass lands as a leaf note + hostile
  passes.** Landing: DONE — H4-M1 ((R1)–(R3), one theory over W(F_q); base
  ℤ_p = the q = p instance, base O_δ = the q = p^δ instance; BASE-INDEX
  CONVENTION δ absolute) + H4-M2 ((R4) via the dictionary slice, `Stable` ⟺
  (R4) displayed, Lemma 4 proved). Passes: **OWED — M1 ×2 (one Codex), M2 ×1
  + N-K3's gcd table (table compiled; empirical run pending)**. FIRING
  CONDITION F-(i): both ledgers carry accepted verdicts (folded findings
  re-gated per the standing discipline). Step (i) is INCOMPLETE until F-(i).
- **(ii) M14's conditional proof then closes O-8b.** ON FILE already
  (`M14-eq2-alphabet_attempt.md`); nothing to rewrite. Fires automatically
  with F-(i).
- **(iii) The §3.2 row P(O-8b/M14) fires** — fresh hostile pass on the
  NOW-unconditional composite. DRAFTED FIRING CHARGE (for the ROOT owner to
  launch, fresh context, quote-and-classify, fix nothing):
  * Composite under audit: M14's O-8b proof + H4-M1's (R1)–(R3) package +
    H4-M2's (R4)/dictionary slice, read as ONE unconditional derivation of
    the alphabet law at δ-stable position classes over O_δ.
  * Attack targets, in order: (a) itemization fidelity — M14's consumption
    of (R1)–(R4) vs what M1/M2 actually PROVED (the blueprint §0 itemization
    is the checklist; any consumed clause not on M1/M2's file is a CRITICAL);
    (b) the arbitrary-q → q = p^δ instantiation (M1 step 8's δ-instance
    display; δ ABSOLUTE, `relExt m δ' = O (m·δ')`); (c) the mutatis-mutandis
    rule (laws verbatim, residue objects REBUILT — no cardinality
    substitution anywhere); (d) M14 Lemma 2's hypotheses supplied by M1's
    step-1 connective display (the retyped coordinates ∘ D.1(e) composition);
    (e) the counting layer (Theorem C(a)/(b), PIN-WELLDEF, (ZC)) NOT consumed
    (M1 §7's walk); (f) the base-clause onto-ness realizer (M1 step 5, the
    corrected x-dependent family — not "unit lifts").
  * Prompt inputs: M14 full; M1 full; M2 §§1–3 + DICT-1 + `stable_iff_R4`;
    blueprint §§0–2; ROOT §3.1 (R1)–(R4) row + §3.2 P(O-8b/M14) row texts.
  * FIRING CONDITION F-(iii): F-(i) on record first (the pass audits the
    composite as UNCONDITIONAL — premature firing audits a conditional and
    burns the fresh-context budget).
- **(iv) The §3.1 (R1)–(R4) row retires; Step 16/18 displays sync.** Delta
  text drafted at §3 below. FIRING CONDITION F-(iv): P(O-8b/M14) returns
  ACCEPT (per the standing discipline, after consecutive clean passes).

## §3. BRIEF 2 — the exact ROOT display deltas (apply only at F-(iv))

All targets in `docs/ROOT_ASSEMBLY_2026-08-02.md`; line refs at this
session's HEAD; the ROOT owner re-locates at apply time.

1. **§3.1 row "(R1)–(R4) (O-8b's base) = (ROOT-C)(H4a)"** (~line 1467):
   status moves from the [M] roster to DISCHARGED — replace "expected
   mechanical ('nobody has written it')" by: "WRITTEN AND PASSED:
   `H4M1_rpack_attempt.md` ((R1)–(R3), one theory over W(F_q)) +
   `H4M2_dict_attempt.md` ((R4) + dictionary); P(O-8b/M14) ACCEPT on
   record <pointer>". The row's (K3-δ) audit-note SURVIVES this retirement
   unchanged (its disposition is §4's separate brief; retire it only under
   §4's own firing conditions).
2. **§3.2 row "P(O-8b/M14)"** (~line 1492): append "RAN <date>, verdict
   <pointer to the pass record>".
3. **Step 16 / Step 18 (K3-c) displays + the O-8b tree node**: every
   "PROVED-UNVERIFIED conditional on (R1)–(R4)" reading upgrades to
   "PROVED-VERIFIED at the P(O-8b/M14) record" — the (R1)–(R4) leg only:
   the K3-c row's OTHER inherited conditionality ((K3-δ) until §4 fires,
   plus the standing (H1)–(H9) rows) is UNTOUCHED by this delta.
4. **Scaffold sync duty pointer (execution = BP_VI / fenced H4-F3, NOT this
   brief)**: `RootHyps.h4a_r1r4`'s eventual field refinement targets the
   H4-F6 four-law carriers — NEVER `H4aConsumptionFace` (blueprint L.3
   standing warning: a face-level refinement would replace the hypothesis
   being discharged).

## §4. BRIEF 3 — (K3-δ) attribution return, via the SELECTED Route-B re-scope

ROOT's standing disposition (§3.1 row, verbatim): "the match display (or a
re-scope) rides the next O-11/O-8b touch and would retire (K3-δ) from clause
(R)". H4-M4 adjudicated: Route A is NOT cheap (per-pool obligation = realized
coprimality, a realization property the dictionary does not control);
**RECOMMENDATION: ROUTE B** — so the return travels as a RE-SCOPE, and per
M4 §6 + the blueprint (finding 3): a Route-B adoption is a LEDGER STATEMENT
CHANGE, ROOT owner's touch, Asvin sign-off required (statement-change fence);
nothing is discharged by the Route-B proof until the re-scope lands; H4-F5
stays fenced until BOTH gates (recommendation ACCEPTED + re-scope LANDED).

DRAFTED DELTA (the re-scope, apply only at F-B below):

1. **(UB-X) row, member (b)** (~line 1469): replace "δ-stable position
   classes exhaust the realized ledger at every δ" by the intrinsic per-base
   ledger law — "**(RB)** for every prime p, every δ ≥ 1, and every position
   y the O_δ classifier realizes: a_δ(y) = δ · a_sym(letter_δ(y)), where
   letter_δ(y) is the classifier's OWN realized species letter and a_sym its
   p-free symbolic exponent (M5 §2 Def SYM; the ℤ_p-side per-letter law is
   Theorem C's accepted conformance at q = p)" — with provenance "(re-scoped
   from the stability form at <this brief + H4M4 §6 + H4M5>; the stability
   form is RETIRED as a row, not proved)".
2. **The (K3-δ) fragment**: its content becomes (RB) restricted to K3-c's
   realized δ > 1 pools (the per-pool exponent reads). Clause (R)'s
   attribution line and the §3.1 (H4a)-row audit-note then read: "clause (R)
   returns to (H4a)-only at the D-c interface; the former (K3-δ) stability
   fragment is superseded by the (RB) instance carried inside the re-scoped
   (UB-X)(b)". Scaffold face re-pointing (`UBXbStmt`/`K3deltaStmt` → the
   H4-F5 stability-free face) happens ONLY after this delta lands.
3. **Conditionality carried, displayed**: the re-scoped row is NOT proved
   unconditionally — M5's (RB) is a displayed conditional on its five §1.3
   rows, whose residue is REL.1's run-production/ledger slice + the
   catalogue-grammar slice (the corpus's STANDING conditionality for this
   chain); stability, (R4), and the cross-base dictionary are GONE from it.
   The row's grade after re-scope: PROVED-conditional at the M5 record.

FIRING CONDITIONS F-B (all five; none satisfied today — §1 ledger):
(1) F-(i) of §2 (M5's [M1-STATUS] row rides M1's gate); (2) M4's pass-2
verdict recorded and clean; (3) **N-K3 RUN clean against M5 §6's sealed
predictions** (a violation of (RB) kills this brief — escalation per §1);
(4) M5's second consecutive clean pass; (5) Asvin sign-off on the statement
change + the ROOT owner's acceptance pass on the CONSUMER-NEED WALK below.

CONSUMER-NEED WALK (M4 pass-1 fold [G3] displayed this as an H4-M7 open
item; drafted here at BRIEF grade, not theorem grade): the re-scope is
warranted only if the consumers need exactly (RB). (a) Step 18 K3-c's
per-pool reads: the β-legs β_μ(σ)(q^D) consume the realized letter's
EXPONENT at the pool's base — (RB)'s instance at that (y, δ); no stability
clause appears in the read (adjudication-level evidence: M4 §5.4 re-graded,
M5 Corollaries DIV/ROOT shapes). (b) Step 16's alphabet-law scope: **OPEN —
the walk is NOT executed here** (it needs the Step-16 text walked against
M5 §7's consumption map, an O-8b-interface read this MECH unit does not
own). NAMED RESIDUE OF THIS BRIEF: the Step-16 walk is condition (5)'s
substance; the ROOT owner's acceptance pass executes or commissions it. If
that walk finds a Step-16 consumption of stability BEYOND the exponent law,
the re-scope narrows to the clause-(R)/(K3-δ) interface only and member (b)
at full scope stays open in its current form.

## §5. FENCED OUT of this unit (displayed per the honesty invariant)

1. **No clause-(UB)/Step-16 discharge delta.** (UB-X)(a) is OPEN (Lemma
   RRE-δ, M3 row 10; M3's pass in flight); (UB-X)(c) = M6 is premise-(i)-
   unsatisfied by its own §0. Blueprint finding 9: "H4-M6/M7 stay BLOCKED on
   it" — read here at its consumption scope: no (UB)-side sync is drafted;
   §§2–4 consume nothing from (UB-X)(a)/(c).
2. **No self-serve edits**: ROOT_ASSEMBLY, the blueprint, `Hypotheses.lean`,
   the H4 Scaffold files are UNTOUCHED. No git.
3. **Post-wave note** (§W): H4-F1/F4/F6 unfence next — fenced §L.5 units,
   assigned to NO wave, NOT attempted here.

## §6. Lean cross-reference pins (compile-checked pointers; no new content)
-/

import Mathlib
import LeanUrat.Scaffold.Hypotheses
import LeanUrat.Scaffold.HDischarge.H4.Kernel

namespace LeanUrat.Scaffold.HDischarge.H4.RootSync

/-! Each `example` pins a name the briefs cite, so the cross-references
cannot silently rot. Nothing is proved beyond name resolution. -/

-- §3 item 4 / §4 item 2: the ROOT-carrier rows and the displayed fragment law.
example := @LeanUrat.Scaffold.RootHyps.h4a_r1r4
example := @LeanUrat.Scaffold.RootHyps.h4b_stable
example := @LeanUrat.Scaffold.RootHyps.h4b_k3delta
example := @LeanUrat.Scaffold.RootHyps.stable_implies_k3delta
example := @LeanUrat.Scaffold.clauseR_of_clauseUB

-- §4 item 2: the CURRENT (Route-A-shaped) typed faces that re-point to the
-- fenced H4-F5 face ONLY after the re-scope lands (blueprint finding 3).
example := @LeanUrat.Scaffold.HDischarge.H4.UBXbStmt
example := @LeanUrat.Scaffold.HDischarge.H4.K3deltaStmt
example := @LeanUrat.Scaffold.HDischarge.H4.k3delta_of_ubxb

-- §2/§3: the dimension-level conformance chain the retirement chain rides
-- (H4-L3/L4/L5 — the abstract layer; the instance content stays H4-F1/F6).
example := @LeanUrat.Scaffold.HDischarge.H4.SlotTree.dimOf_scale
example := @LeanUrat.Scaffold.HDischarge.H4.eq2_dim_of_skel
example := @LeanUrat.Scaffold.HDischarge.H4.eq2law_of_skel

end LeanUrat.Scaffold.HDischarge.H4.RootSync
