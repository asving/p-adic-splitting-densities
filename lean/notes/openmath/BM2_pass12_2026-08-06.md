The extended-scope census fails mechanically and the fresh semantic sweep finds surviving provenance wording defects.

1. Census re-grep — automatic finding

Applying the published keyword/segmentation rule to §SC–§S6 and every Lean doc comment gives **212 active hits**, not the provisional **151**.

Reconciliation:

| Newly included area | Hits | Already represented | Missing rows |
|---|---:|---:|---:|
| `Conformance.lean` declaration docstrings | 13 | C206 | 12 |
| `RecParamE.lean` module/declaration docs | 17 | C207 | 16 |
| `RCWitness.lean` module, section, and declaration docs | 33 | 0 | 33 |
| **Total** | **63** | **2** | **61** |

Thus the extended scope added 63 hits, but C205–C208 account for only two of the newly scoped hits. **Sixty-one keyword-hit sentences are absent from the census table**, which is an automatic finding under §S6 item 7.

Representative absent hits and their proper classifications:

- “**B-M2 core … every D8-shaped read lift … is (K0)-conformant.**” (`Conformance.lean`, `isReadLift_K0`) — **[TAGGED/status theorem]**, absent.
- “**Wired interior increments are conformant** …” (`historyCoherent_key_K0`) — **[TAGGED]**, absent.
- “**Every read of a `ReadsOf` run lands a (K0)-conformant key** …” — **[TAGGED]**, absent.
- “**THE GATES … both [RC] corollaries … FIRE** …” (`RCWitness.lean`) — **[TAGGED]**, absent.
- “**GATE C … REC-WIRE-G FIRES** …” — **[TAGGED]**, absent.
- Private construction descriptions such as “**The recentered gate stage** …” and “**The recorded lift** …” — **[STATUS-FREE]**, but still keyword hits and therefore still required census rows.
- The seven `RCWitness.lean` `/-! §… -/` headings mentioning recentering/lifts/carriers/firing are also newly scoped hits and have no rows.

Accordingly, the published claim “every doc comment” is not backed by the C001–C208 ledger.

2. Revision-13 sync verification

The three requested status syncs do correctly say that REC-WIRE-G is discharged and the e-half removal subsumed:

- `Conformance.lean` module header — discharge status correct.
- `K0Conformant` docstring — discharge status correct.
- `k0_actual_of_recentering` docstring — discharge status correct.

The compiled declarations support those status changes: `recentering_natDegree_eq`, `recParam_eg_of_recentering`, and `rc_wireG_gate` have the advertised statements and route.

However, two synced passages retain inaccurate provenance wording:

- `Conformance.lean`: “**the pair derived from `HistoryCoherent` alone**”
- `k0_actual_of_recentering` docstring: “**`recParam_eg_of_recentering` below (from `HistoryCoherent` alone)**”

Classification: **under-specified/false provenance wording**. The theorem also explicitly requires `hi1` and `hrec`; only the substantive recentering and width laws come from `HistoryCoherent`.

3. The two pedantic wording fixes were not propagated throughout the Lean docs

Two live `RecParamE.lean` statements preserve exactly the rejected wording:

- Module header: “**Every hypothesis comes from `HistoryCoherent`; `Node.hspecRec` is nowhere invoked.**”
- `recParam_eg_of_recentering` docstring: “**The route (every hypothesis supplied by `HistoryCoherent` itself, none by `Node.hspecRec`)**”

Classification: **stale overstatement**. The second is additionally an absent extended-census hit. The correct formulation is the one now present in §S2: `hi1` and `hrec` are explicit hypotheses; the substantive laws come from coherence; `Node.hspecRec` is unused.

A related current §SC sentence still says the pair is derived “**from `HistoryCoherent` alone**.” In context the interior/recentering qualifications are visible, but it remains inconsistent with Revision 13’s newly adopted hypothesis-precise wording.

4. Consolidated current-opens list

This part checks out. The three intended display sites agree substantively:

- GRADED-READ
- REC-SL proper
- SITE-EXH
- the `readsOf_landing_K0` recentering-branch witness debt

§S0 also preserves the distinction that S-1/[T] and TR-3-ORD-X are inherited blueprint riders rather than B-M2-face obligations. No omitted REC-WIRE-G or e-half residue remains in the canonical list.

5. Compiled-state sweep

I found no mathematical contradiction in the two Revision-12 theorems, their indices, the degree argument, the e-half subsumption claim, or the H₃rc gate. The decisive failures are documentation/census failures: 61 absent census hits and the surviving “HistoryCoherent alone/every hypothesis” assertions.

VERDICT: CRITICAL