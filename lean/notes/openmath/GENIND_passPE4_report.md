# GENIND pass PE4 — HOSTILE VERIFICATION REPORT (fresh context, fourth arc pass; the r3 text — the capped window formula — attacked for the first time)

**VERDICT: UNREFUTED, NOT CLEAN. 0 CRITICAL, 2 GAP, 0 MINOR (+2
remarks).** The 2-clean count does NOT start (stays 0/2; an r4 is
owed, then the count begins afresh). The capped window formula itself
— the r3's mathematical content — SURVIVED every attack, including
two configurations no leg had ever exercised (three children at three
distinct slopes with the mixed cap live inside one D_l; the corpus's
first n = 8 composite-bearing instance), and its "equivalently
D_l = min_j(v(a_j) + j·k_l)" clause is a genuine identity. Both GAPs
are residues of the r3 repair itself, at two faces no prior pass
walked: **(PE4-G1)** the r3's concordance/blast-radius sentences —
"every sealed genre sits in the agreeing regime", "(true at every
sealed instance)", "invisible at n ≤ 6 first-visible windows" — are
FALSE: the disagreeing regime (a co-block STEEPER than the extracted
child) is live INSIDE the sealed roster via steeper decided PIECES,
at the n = 3 control row (2,5) and the n = 4 row (2,6), in 544
exhaustively enumerated states (Zp side, machine-verified; the Fqt
rows carry the mirror genre; plus a 972-state odd-q companion leg
off-roster) that the sealed battery itself classified and
Hensel-extracted at the capped windows — the arc's
record-integrity defect class (PE2-G2/PE3-G1) for the third
consecutive round; and **(PE4-G2)** GENIND-3's proof step (1) still
displays the pre-cap uniform window "N − m·k_l", which contradicts
the [r3]-corrected statement (and the note's OWN r2-displayed M6/M7T
worked ledgers: N−6 vs N−4, N−7 vs N−5), degenerates to a negative
window at live instances (TWOCHILD child A at N = 7), and cannot
deliver the stated M_l at any genre with a shallower co-block —
while GENIND-6(a) consumes the step "VERBATIM". No count law, no
committed number, no theorem clause moved.

**Target:** `lean/notes/openmath/GENIND_PROOF_2026-08-08.md` at HEAD
(file last touched at r3 `d3ef66d`, 2026-08-09 03:19:50; arc: seal
`c3212cc` → PE1 `4259a93` → r1 `66200f2` → PE2 `50db400` → r2
`2abf419`/`ad6f31d`/`52fb6ff` → PE3 `f89b565` → r3 `d3ef66d`).
**Role:** hostile verifier, fresh context, zero prior stake; I fix
nothing; quote-and-classify only. All three prior reports read
first; none of their routes re-walked (prior routes: seal battery
n ≤ 5 / PE1 exact-eval n ≤ 5 / PE2 n = 6 census + first n = 6 PARI /
r2 supp M6 fibration 2,128,163 / PE3 M7T n = 7 + TWOCHILD + SS6 +
symbolic fibers 705,133).

---

## 1. FINDINGS

### PE4-G1 [GAP] — The r3's regime-inventory sentences are FALSE: the disagreeing regime sits INSIDE the sealed roster (n = 3 control row included) via steeper decided pieces; machine-refuted on 544 exhaustively enumerated sealed-window states (Zp side) that the sealed battery itself processed.

The r3 grounds its concordance in a factual claim about the sealed
data, at three sites:

* S4.1, inside the [r3]-corrected statement's erratum bracket:
  > "the sealed display charged every co-block its OWN slope-mass
  > (Σ_j μ_j k_j + Σ_pieces deg·slope-mass), which equals D_l iff
  > every co-block sits at slope ≤ k_l **(true at every sealed
  > instance)** and is FALSE for the shallower child at
  > distinct-slope multi-child genres"
* S4.1 honesty note:
  > "first visible N = 7 **(beyond every sealed window — why three
  > passes and the battery could not see it)** … the defect was the
  > display alone, silently corrected by the mechanics **at every
  > sealed instance (all in the agreeing regime: every co-block
  > shallower than or at the extracted child's slope)**"
* S13 [r3] record:
  > "the defect was display-only, **invisible at n ≤ 6 first-visible
  > windows**" and "The corrected formula changes NO committed
  > instance — **every sealed genre sits in the agreeing regime
  > (co-blocks shallower than or at the extracted child's slope)**,
  > and the runner always computed the capped value."

The corrected formula's own scope sentence says co-blocks are
"child, composite stage, **or decided piece**" (S4.1). A decided
PIECE steeper than the extracted child produces the disagreeing
regime at degrees and windows far below TWOCHILD's N = 7, because a
piece needs only ONE abscissa where a second child needs two.
Concretely, inside the sealed roster (runner ROWS: n = 3 at (2,5),
(3,4); n = 4 at … (2,6) …, both ring kinds):

* **PSTEEP3 at the (2,5) n = 3 CONTROL row**: hull (0,4)–(1,2)–(3,0)
  — decided piece on [0,1] at slope 2, (2,1)-child on [1,3] at slope
  1, residual (y+1)². Visibility v(a₀) = 4 ≤ N−1 = 4: LIVE at the
  sealed window. Capped D = 2·1 + 1·min(2,1) = 3 = min_j(v(a_j)+j)
  → child window 2; the pre-r3 display (own slope-mass) gives
  D = 2 + 2 = 4 → window 1.
* **PSTEEP4 at the (2,6) n = 4 row**: hull (0,5)–(1,3)–(4,0) —
  piece at slope 2, (3,1)-child at slope 1, residual (y+1)³.
  v(a₀) = 5 ≤ 5: LIVE. Capped D = 3·1 + 1·min(2,1) = 4 → window 2;
  uncapped D = 5 → window 1.

**Machine adjudication (fresh route FR-1, exhaustive; §7).** All
loci enumerated exhaustively and pushed through the SEALED reader
and the SEALED `extract_child` (md5-asserted read-only import):

* (2,5,n3): **32/32 states** classify to the sealed β key
  `('BOTHER', 3, (2,), ((1,1),), ((0,4,1,2),(1,2,3,0)))` and extract
  the child at **Wc = 2** (capped) — the locus fibers **ONTO all 4**
  window-2 child classes with **constant fiber 8** (impossible at
  the uncapped window 1: a single class), and the frame verdict
  splits **DEC 16 / DRAIN 16**, where the uncapped reading forces
  instant child-drain on ALL 32 (v(b₀) ≥ 1 = window). Capped-window
  content lift-CERTIFIED (16 members × 3 independent lifts, 0
  unstable; one-deeper unstable — fires 17×).
* (3,5,n3) — NOT a sealed row (ROWS[3] = [(2,5),(3,4)]; at (3,4)
  the genre is invisible, v(a₀) = 4 > N−1): the odd-q COMPANION of
  the same genre, run to show the odd-q pins live
  (digit₁(a₂) = −2z ≠ 0): **972 states** over 4 letter pairs — per
  pair 243 states, ONTO 9 × fiber 27, split DEC 162 / DRAIN 81.
  Exact. (Supports the capped formula; the sealed-inventory
  refutation rests on the two sealed cells above, 544 states per
  ring kind.)
* (2,6,n4): **512/512 states** at `('BOTHER', 4, (3,), …)`,
  Wc = 2, ONTO 8 × fiber 64, split DEC 256 / DRAIN 256. Exact.

And the sealed battery PROCESSED exactly these states: `run_row`
calls `beta_check` on **every** member with a β key
(`do_beta_pointwise=True` on all rows), so GT-BETA's 87,156
pointwise Hensel reconstructions include these loci, extracted at
the capped windows, since the seal. The disagreeing regime is not
"beyond every sealed window" — it is sealed data, and the old
uncapped display was refutable against the committed sweeps from
day one (had any leg compared the DISPLAY to the runner there).

**What survives (verified, not assumed):** the concordance
CONCLUSION. `extract_child` computes D = min_j(v(a_j)+j·k) — the
capped value — so no committed number moves; all three committed
machine legs re-ran bit-identically in my sandbox (§6). The false
sentences are the repair's blast-radius inventory, not its numbers.

**Failure scenario:** a reader auditing r3 trusts S13's "every
sealed genre sits in the agreeing regime", concludes the old display
agreed with the runner on all sealed data, and ports the uncapped
formula into a consumer — e.g. GENIND.C's β-term at an n = 3/n = 4
genre with a steeper decided piece: the child menu instantiated at
window 1 instead of 2 predicts 100% instant child-drain against the
true DEC/DRAIN split 16/16 (resp. 256/256), every family count wrong
by a q-power — while believing the sealed battery certifies the
agreement. This is the PE2-G2/PE3-G1 defect class (a repo-checkable
factual sentence in a repair record, false against the repo),
reproduced in the third consecutive repair; the check this time was
against the sealed ROSTER's genre inventory rather than the git log,
and it was not executed.

Class: **GAP** (record integrity; the corrected formula, the runner,
and every committed number are right; no theorem clause moves).

### PE4-G2 [GAP] — GENIND-3's proof step (1) still carries the pre-cap uniform window "N − m·k_l": it contradicts the [r3]-corrected statement AND the note's own M6/M7T worked ledgers, degenerates at live instances, and cannot deliver the stated M_l; GENIND-6(a) consumes it "VERBATIM".

S4.1, proof of LEMMA GENIND-3, step (1), verbatim (untouched by r3):

> "(1) RECENTER + SCALE: for the side S_l of slope k_l (integer,
> e = 1), recenter at the child's residual root (ẑ_l π^{k_l}) and
> scale x = π^{k_l}y: by GENIND-2(a)'s mechanics the frame becomes a
> **window-(N − m·k_l) level-0 frame** whose reduction is
> ȳ^{μ_l}·(**separable factors with the other letters as roots** —
> the other children sit at valuation 0 of this frame if on the same
> side, **at their own slopes otherwise**)."

and GENIND-6(a) (S4.3): "GENIND-3's steps (1)–(2) apply VERBATIM".

Three defects, all in the one sentence:

* **The window.** N − m·k_l equals the corrected M_l = N − D_l iff
  D_l = m·k_l iff the extracted child is the SHALLOWEST block (every
  root then has v ≥ k_l). At every genre with a shallower co-block
  the step undersells the statement — including the note's OWN
  worked instances, displayed two sections later since r2: at M6
  (child slope 1, stage slope 1/2) step (1) gives N−6 against the
  ledger's/runner's **N−4** (at N = 5: window **−1** against the
  ledger's 1 — the step cannot even license the extraction that the
  r2 supp battery performed on 8,192 states); at M7T, N−7 vs
  **N−5**. At the r3's own adjudicating instance TWOCHILD, child A
  at N = 7: N − m·k = 7 − 8 = **−1** while the extraction is live at
  window 1 ("the steeper child A extracts at N−6 exactly" — the r3's
  own control). At my THREECHILD (§7 FR-2), child B: step (1) gives
  window 1; the statement, the sealed extractor, and the fibration
  give **3** (onto 16 classes; a window-1 child space is one class).
  Charitable reading — N − m·k_l as a uniform lower bound on the
  scaled frame's coefficient precisions — is true (coefficient j is
  known mod π^{N + j·k_l − D_l} ≥ π^{N − m·k_l}) but strictly
  insufficient: the binding precision is N − D_l at j = 0, i.e. the
  content computation that the [r3] honesty note and GENIND-6(a)'s
  proof gloss carry and step (1) does not.
* **"separable factors with the other letters as roots".** At
  distinct-slope genres the reduction of the scaled frame is NOT
  that: steeper co-blocks all reduce to the SINGLE residue −ẑ_l
  (their letters collapse out of the reduction entirely — at
  TWOCHILD child B, child A's image is (ȳ+ẑ_B)², letter w gone);
  shallower co-blocks leave the reduction by degree drop (their
  letters gone too); and co-children on any side give REPEATED
  images ((ȳ−(z′−ẑ))^μ), not separable ones — inside GENIND-3's own
  multi-child scope, not just GENIND-6's.
* **"at their own slopes otherwise".** In the scaled frame no
  co-block sits at its own slope: steeper blocks sit at valuation 0,
  shallower at negative valuation (the degree drop).

**Consumption:** M_l is the window at which Step 1/2's menu assembly
instantiates the children's (IH) menus, and GENIND-6(a) re-asserts
the extraction "VERBATIM" from steps (1)–(2) for the mixed bucket —
so the proof step that must establish the corrected window is the
one place the cap was not propagated. **Failure scenario:** a reader
executes the proof as displayed at the note's flagship M6 at N = 5:
step (1) hands them a "window-(−1) frame" — vacuous — while the M6
ledger, the r2 supp battery (8,192 states ONTO 1 × 16), and the
statement all operate at window 1; at THREECHILD (2,13,6) the
displayed proof proves a strictly weaker lemma (child B at window 1,
one class) than the statement it sits under (window 3, 16 classes,
machine-onto). The statement is TRUE (my FR legs corroborate it at
every configuration); its displayed proof does not establish it at
any genre with a shallower co-block — a justification gap in the
repaired lemma's own proof, surviving three passes because PE1
walked the α-instance of the window (where D = mk and the display is
right), PE3 walked the STATEMENT's Σ-formula, and nobody walked the
proof step against the r2 ledgers.

Class: **GAP** (statement correct and machine-corroborated; the
displayed proof step cannot deliver it; repair is the one sentence —
the content form N − D_l with the ragged per-coefficient precisions
N + j·k_l − D_l, and the corrected reduction parenthetical).

### Remarks (not findings)

* **R1 (as-of statuses at verdict time).** The r3's pin sentences
  were TRUE at commit time — verified against the git log:
  GENH4 passPE4 CLEAN `e15308d` 02:59:25 < GENIND r3 `d3ef66d`
  03:19:50 < GENH4 passPE5 `11dbd23` 04:13:13; GENH4 note HEAD at r3
  time = `4b0d946`; GENHN pin `850e77e` = GENHN's note HEAD still.
  The commit-time pin rule was COMPLIED with this round — the first
  round of the arc where it was. Since then (this wave): GENH4 is
  **ACCEPTED 2/2** (`11dbd23`, PE5 CLEAN with 2 display MINORs) with
  the annex applied (`baf05ed`; accepted body byte-frozen = the r3
  pin `4b0d946`). The (CS-EXACT) n = 4 de facto discharge therefore
  now cites an ACCEPTED supplier — an honest strengthening for the
  next dated append; neither PE5 MINOR (the genre-F SPLTAIL cap
  display; the 2SIDED norm display) touches GENH4.A/B's UND/menu
  laws that GENIND consumes; no clause of GENIND upgrades or
  degrades (still "de facto at n = 4, hypothesis at general n").
* **R2 (the r3 mathematics held).** The capped formula and its
  "equivalently" clause were re-derived from scratch and survived a
  directed hunt (§2); the r3's TWOCHILD adjudication, concordance
  conclusion, and all [r3] MINOR records check out at their sites.

## 2. CHARGE 1 — THE CAPPED WINDOW FORMULA, RE-DERIVED AND HUNTED

**Re-derivation (root-sum route, independent of the note's).** Over
the splitting field of the monic frame f (valuation extended
uniquely; Gauss content is additive on products over a valuation
ring), the extraction at slope k_l recenters at ẑ_l π^{k_l} and
scales x = π^{k_l}y; each linear factor of f contributes content
min(v(ρ − ẑ_l π^{k_l}), k_l) to the scaled frame. Child roots
(v(ρ) = k_l, residue ẑ_l): recentering pushes them DEEPER, so each
contributes exactly k_l → μ_l·k_l. Same-side other blocks (v = k_l,
residue ≠ ẑ_l): difference has v = k_l → deg·k_l = deg·min(k_l,k_l).
Steeper blocks (v(ρ) = s_b > k_l): v(ρ − ẑπ^k) = k_l → deg·k_l =
deg·min(s_b, k_l) — THE CAP. Shallower (s_b < k_l): v = s_b →
deg·s_b. Total: **D_l = μ_l k_l + Σ_b deg_b·min(s_b, k_l)** ✓ the
r3 display, co-blocks of every type.

**The "equivalently" clause is an IDENTITY.** For monic f with the
convention v(0) = ∞: content(f(π^k y)) = min_j(v(a_j) + jk) (the
ultrametric coefficient formula, j = 0..m with a_m = 1), and
min_j(v(a_j) + jk) = Σ_roots min(v(ρ), k): the function
j ↦ H(j) + jk (H the hull) is convex with increments s_{m−j} − k, so
its min over integers is attained at a hull vertex, where
v(a_j) = H(j); evaluating at j = #{roots with v > k}… gives
Σ min(v(ρ), k). Recentering changes neither side (checked per block
above). The hull's convexity is automatic (it is a lower convex
hull), so "equivalently" needs NO extra hypothesis; a_j = 0 entries
drop out via v(0) = ∞; the j = m term caps the min at mk ≥ D_l. ✓

**The hunt (charge: find a configuration where even the CAPPED
formula fails).** Executed and came up empty:

* **Three children at three distinct slopes** — the mixed-cap
  regime, where one D_l contains BOTH a capped steeper co-block and
  an own-slope shallower one. Never live before (needs m ≥ 6,
  N ≥ 13). Constructed THREECHILD (§7 FR-2): hull
  (0,12)–(2,6)–(4,2)–(6,0), slopes 3 > 2 > 1. Hand values:
  D_A = 2·3+2·2+2·1 = 12 (Wc 1), D_B = 2·2+2·min(3,2)+2·min(1,2) =
  10 (Wc 3), D_C = 2·1+2·1+2·1 = 6 (Wc 7); uncapped would give
  (12, 12, 12) → (1,1,1). Machine: (1,3,7) exact on 400 members,
  both characteristics; child-C box BIJECTIVE onto all 4,096
  window-7 classes; PARI valuation partitions exact. **HELD.**
* **A decided piece BELOW (shallower than) the child's slope** —
  contributes its own slope-mass, degree-dropped cofactor; live at
  every M6/M7T-type ledger and at THREECHILD's child B; **HELD**
  (this is the regime the monic-factor Hensel form owns).
* **Slope-0 co-blocks** — impossible inside a cluster state: the
  frame has f̄ = P̄^m, every root valuation ≥ 1/e > 0; slope-0
  factors exist only at level 0, outside GENIND-3's scope. Vacuous.
* **d ≥ 2** — schema-grade Q-uniformity, disclosed at GENIND-BOX-3;
  no new exposure introduced by r3 (the formula is slope/degree
  arithmetic, d-free). Not further attacked.
* **n = 8, first composite-bearing instance at the tower-live
  degree** (§7 FR-3): the (4,1)-child + CS4-E stage genre — child
  window N−6 = 2 exact (agreeing regime), stage entry budgets exact.
  **HELD.**

## 3. CHARGE 2 — THE r3 PIN/DISCLOSURE SENTENCES AND S13 AT HEAD

* Timestamps verified against the git log (§1 R1): every r3 "as of
  this commit" claim TRUE at commit time. The rule adopted at r2 and
  violated at r2 (PE3-G1) was COMPLIED with at r3.
* S12's in-place correction: present, quotes its own false sentence,
  points to S13 ✓. S13's PE3-G1 bullet reproduces the PE3 table's
  timestamps correctly (4b0d946 00:50:10 before 01:00:35/01:21:44/
  01:35:30) ✓. Header + BOX-5 arc lines consistent with a 0/2 grade,
  clean count not started ✓.
* S13's PE3-G2 bullet: TWOCHILD numbers (2,048 + 177,147; 80×5 +
  60×4) match PE3's committed report ✓; the CONCORDANCE clause's
  first half (runner = capped D) verified ✓; its second half ("every
  sealed genre sits in the agreeing regime") is **PE4-G1**.
* One internal tension noted and resolved in the note's favor:
  S13 says "no machine leg was added at r3" while citing PE3's
  committed fresh leg — accurate (the leg is the verifier's, cited
  not owned; BOX-5 says the same).

## 4. CHARGE 3 — GENIND-6(a) WITH THE CORRECTED WINDOW IN HAND

Site-by-site: the GENIND-6(a) STATEMENT carries the capped formula
with the [r3] tag ✓; the proof of (a) carries the content
computation "D = min_j(v(a_j) + j·k_l) … CAPPED slope-mass …
regardless of type [r3]" ✓; the M6 worked ledger computes
D = min_j(v(a_j)+j) = 4 with the regime label "capped = uncapped at
M6 — the stage (slope 1/2) is SHALLOWER than the child" ✓ (checked:
min(4, 2+2, 6) = 4). The ONE remaining consumer of the uncapped era
is the "steps (1)–(2) VERBATIM" incorporation of GENIND-3's proof —
**PE4-G2**. No other site consumes the uncapped reading: grepped
every `M_l`/`slope-mass`/`window-(N` site at HEAD; S7.1's (CS-1)
rider and Step 2/4's mixed buckets reference GENIND-6/M_l
abstractly, inheriting whichever formula S4.1 displays.

## 5. CHARGE 4 — THE CONDITIONALITY DISPLAY (S9.3) AT HEAD

* GENHN bullet @ 850e77e: the pin is GENHN's current note HEAD
  (verified via git log — no GENHN commit since); the four consumed
  clauses (A(iv) node floor, A(v) ragged-corrected read, GENHN-3
  confinement, S6.2 reconciliation) named with sites ✓.
* GENH4 bullet @ r3 4b0d946 "+ arc since: PE4 CLEAN @ e15308d, its
  count 1/2": true as of d3ef66d; superseded since by ACCEPTED 2/2 +
  annex (R1) — dated-append material for the next rN, not a defect
  under the as-of rule.
* HE7/HE6: correctly ABSENT (GENIND consumes neither; re-checked
  that no [GENIND-H] member cites HE7 — the μ ≥ 3 surface is
  GENHN's [GENHN-HE], named at (CS-3)'s discharge status only).
* The one-line summary's "no axiom, no weld face inside any count
  law; grade = composition 0/2" — consistent with BOX-5 and the
  header.

## 6. MACHINE LEG — ALL THREE COMMITTED RUNNERS REPRODUCED; PINS INTACT

* **md5 pins:** sealed runner `genind_checks.py` = `e7ca150b…`
  FIVE-WAY identical (worktree = HEAD = r3 d3ef66d = r2 52fb6ff =
  seal c3212cc). r2 supp triple `7335fc16`/`63b7413a`/`6c93f29c` and
  PE3 fresh triple `fce9cb08`/`2075dd28`/`360eb239`: worktree = HEAD
  = their landing commits. GT-PIN supplier files 5/5 match the
  runner's PINS dict. S13's "byte-frozen" claims all CONFIRMED.
* **Isolated re-runs** (`/tmp/genind_pe4_run`; runner + supp + pe3
  fresh + the five pinned suppliers copied; committed artifacts
  never touched):
  * sealed battery: **GREEN — 163,536 checks, 0 violations,
    164.6 s**, teeth 12/12/4/24; output diff vs committed = 6 lines,
    all embedded timings; results JSON leaf-diff = `elapsed_s` only.
  * r2 supp: **GREEN — 2,128,163 / 0, 358.1 s** (committed 357.7);
    JSON content identical mod `elapsed_s`; all fibrations, teeth
    T1/T2/T4 reproduced exactly.
  * PE3 fresh: **GREEN — 705,133 / 0, 148.0 s** (committed 147.4);
    JSON content identical mod `elapsed_s` — the first re-run of
    PE3's leg by a different verifier.
* **Tautology audit (my angle, new).** The window adjudications in
  this pass use the sealed `extract_child`, so I checked the
  potential circularity explicitly: (i) the value under test is the
  STATEMENT's Σ-formula; the extractor computes min_j(v+jk) — tied
  to the Σ-form only through my independent root-sum derivation
  (§2), a third source; (ii) the consumption-level measurements
  (class counts ONTO 4/8/9/4,096 at the capped windows; DEC/DRAIN
  splits 16/16, 256/256, 162/81) distinguish capped from uncapped
  REGARDLESS of extractor internals — an uncapped (window-1)
  extraction has exactly one child class and 100% child-drain;
  (iii) the PARI valuation partitions are mechanism-free (PARI never
  sees the reader); (iv) lift-stability legs certify the capped
  windows as content, not extractor arithmetic. Non-tautological.

## 7. FRESH ROUTE — sealed-window regime adjudication + the mixed cap + n = 8 (disclosed; disjoint from all five prior routes)

Runner `verification/openmath/genind_pe4_fresh.py` (committed with
output + results JSON; md5 `aa2b9af8` / `1a29a9c1` / `e5493312`);
sealed runner imported read-only, md5-asserted; ran in the /tmp
sandbox. **ALL predictions hand-derived in the docstring BEFORE the
run. GREEN: 6,507 checks, 0 violations, 1.4 s.**

* **FR-1 PSTEEP (the PE4-G1 adjudication, exhaustive).** §1: the
  sealed-window disagreeing-regime genres. (2,5,n3): 32 states,
  Wc = 2 (uncapped display: 1), ONTO 4 × fiber 8, DEC 16 / DRAIN 16,
  lift-stable at Wc with one-deeper unstable. (3,5,n3) odd-q
  companion (off-roster): 4 × 243 states, ONTO 9 × 27, 162/81 per
  letter pair. (2,6,n4): 512 states, (3,1)-child, Wc = 2, ONTO 8 ×
  64, 256/256. Every number preregistered exactly.
* **FR-2 THREECHILD (the first three-child, three-distinct-slope
  genre; the MIXED cap).** (q,6,1), hull (0,12)–(2,6)–(4,2)–(6,0),
  N = 13, both characteristics (q = 3 with all six odd-q pins live
  and random letters). D_B = 2·2 + 2·min(3,2) + 2·min(1,2) = 10 is
  the first D_l ever exercised containing a capped steeper co-block
  AND an own-slope shallower one simultaneously. Windows
  (W_A, W_B, W_C) = **(1, 3, 7)** exact on 250 + 150 sampled members
  (uncapped predicts (1,1,1)); the sealed reader classifies every
  member to the single β key `('BOTHER', 6, (2,2,2), (), …)`;
  C-box (a₅{2..7} × a₄{3..8}) → **BIJECTION onto all 4,096**
  window-7 child-C classes (fibers {1}); B-box (a₃{5,6} × a₂{7,8})
  → onto all 16 B-classes (the per-height linearized images
  ȳ^j·S̄ mod (ȳ−z)² independent, preregistered from the triangular
  ledger); stability 20 × 3 + 12 × 3 lifts at (7, 3), 0 unstable,
  one-deeper-C fires 20×/17×; **PARI (mechanism-free): 50 disc ≠ 0
  lifts, root-valuation partition {3: 2, 2: 2, 1: 2} exact** — the
  three-block fracture read confirmed with no shared mechanism.
* **FR-3 N8MIX (the corpus's first n = 8 composite-bearing
  instance; the first μ = 4 child).** (2,8,8), hull
  (0,6)–(4,2)–(8,0): a (4,1)-child at slope 1 — the tower-live
  child degree (μ ≥ 4 ⟺ n ≥ 8) — coexisting with the CS4-E(1) stage
  at slope 1/2. 250 sampled members: every one CS at the single key
  `('CSOTHER', 8, (4,8), 2, 1, 2, 1)` with the child side present;
  child extraction Wc = 2 exact (D = 6, the agreeing regime — this
  leg is n = 8 existence, not the cap); quartic division ledger
  budgets v(u₀) = 2 with digit₂(u₀) = z², v(u₁) ≥ 2, v(u₂) ≥ 2,
  v(u₃) ≥ 1 exact on every member (the CS4-E entry, sibling-loss
  corrected); child-box (a₁{6}, a₂{5}, a₃{4}, a₄{3}) → onto all 16
  (4,1,2)-classes bijectively (images y..y⁴ mod (y+1)⁴ rank 4,
  preregistered); **PARI: 25 disc ≠ 0 lifts, valuation partition
  {1: 4, 1/2: 4} exact**; T-PIN tooth (corrupt digit₂(a₄)) fires
  10/10.

## 8. CLEAN CHARGES (executed, nothing found)

The capped-formula hunt (§2) — no counter-configuration, including
the two never-live ones; the "equivalently" clause — an identity,
no convexity hypothesis owed; GENIND-6(a)'s [r3] sites — capped
everywhere except the VERBATIM step import (= PE4-G2); the M6
ledger's regime label — correct; r3 pin sentences at commit time —
true (git-log timestamps); S12-in-place + S13 vs header/BOX-5 —
consistent; S9.3 at HEAD — GENHN pin current, GENH4 pin = the
accepted body at HEAD; cross-round contradiction hunt over r1/r2/r3
— the PE1-M1 window condition, PE2-m1 granularity propagation, and
the four [r3] MINOR records all verified in place; the one
cross-round inconsistency found is PE4-G2 (step (1) vs the r2 M6
ledger). The three committed machine legs re-ran green in isolation
(§6). GENIND-BOX-3's d ≥ 2 disclosure unchanged by r3.

## 9. FINDINGS TABLE + GRADE LINE

| id | class | site | one line |
|---|---|---|---|
| PE4-G1 | GAP | S4.1 statement erratum + honesty note / S13 | "every sealed genre sits in the agreeing regime" & "(true at every sealed instance)" & "invisible at n ≤ 6 first-visible windows" are FALSE: steeper-decided-piece genres live at the sealed (2,5,n3) control row (32 states) and (2,6,n4) (512 states), processed by the sealed battery's own GT-BETA at the capped windows; concordance CONCLUSION survives (runner capped, no number moves) |
| PE4-G2 | GAP | S4.1 proof step (1) (consumed VERBATIM by GENIND-6(a)) | the pre-cap uniform window "N − m·k_l" survived the r3: contradicts the corrected M_l at every genre with a shallower co-block incl. the note's own M6 (N−6 vs N−4) and M7T (N−7 vs N−5) ledgers, degenerates (−1) at TWOCHILD-A/M6's first windows, cannot deliver the stated window; reduction parenthetical ("separable factors with the other letters as roots… at their own slopes") false at distinct-slope genres |

**Machine leg:** sealed runner md5 five-way identical (worktree =
HEAD = r3 = r2 = seal); supp + PE3-fresh triples byte-frozen as
claimed; isolated re-runs of ALL THREE committed legs GREEN
(163,536/0 + 2,128,163/0 + 705,133/0; leaf-diffs = elapsed only —
the first pass to re-run all three); tautology audit on the
window-adjudication circularity executed (three-source structure +
consumption-level splits + mechanism-free PARI).

**Fresh route:** the sealed-window regime census (FR-1, exhaustive,
both characteristics, 544 sealed-row + 972 companion states with
preregistered
onto/fiber/verdict splits — adjudicating PE4-G1); the first
three-child three-slope genre with the mixed cap in one D_l (FR-2,
windows (1,3,7), C-box bijection 4,096, PARI {3:2,2:2,1:2}); the
first n = 8 composite-bearing evidence with the first μ = 4 child
(FR-3, PARI {1:4,1/2:4}). **GREEN: 6,507 checks, 0 violations.**
The r3's capped formula survived everything; both GAPs are prose of
the repair, not mathematics.

**GRADE LINE: NOT CLEAN — 0 CRITICAL + 2 GAP + 0 MINOR. The 2-clean
count does NOT start (0/2 stands; the counter stays at 0; GENIND
remains the last arc-program leader without a clean). r4 owed: one
regime-inventory correction (PE4-G1 — the honest sentence is
"agreeing at every sealed instance the note DISPLAYS; sealed sweep
rows do contain steeper-piece genres in the disagreeing regime,
where the runner's capped value is confirmed by [FR-1's numbers]")
and one proof-step display (PE4-G2 — the content form N − D_l with
ragged per-coefficient precisions N + j·k_l − D_l, plus the
corrected reduction parenthetical). The capped window formula, the
equivalence identity, LEMMA GENIND-6's mathematics, every committed
number, and the whole conditionality display survived — now through
the mixed-cap and n = 8 configurations.**

---
*Verifier: fresh-context hostile pass PE4, 2026-08-09. Fixed
nothing. All machine work in an isolated /tmp sandbox; committed
artifacts untouched (byte-verified before and after). Fresh-route
runner committed for audit as `verification/openmath/
genind_pe4_fresh.py` (+ output + results); every law it gates on is
derived in its docstring before the run; its two read-offs (the
BOTHER/CSOTHER key tuples) are disclosed as such and gated only on
UNIFORMITY across each locus.*
