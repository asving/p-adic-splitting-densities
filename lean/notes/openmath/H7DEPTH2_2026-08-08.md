# H7 DEPTH-2 — the HK23/HK25 transplant attempted: REFUTED at (2, ZMod 2), narrowed true layer landed

**Unit:** wave-18 U3 (synthesis-8 queue item: "H7 depth-2 transplant"; charter
`docs/SYNTHESIS_PASS8_2026-08-08.md` §S4 item 3), completed by the DEPTH-2 RECOVERY
unit after the 529 storm (predecessor's mid-work checkpointed AS-IS at d0e2b74,
completeness unverified). **Charge:** realize the depth-1 forge's sides-2 site
continuations at depth 2 by transplanting the HK23/HK25 two-node mechanisms
(`HC2/HK23_twoNodeGatePos.lean`, `HC2/HK25_recGate.lean`) onto
`Scaffold/HDischarge/H7/H7Forge.lean` (consumed READ-ONLY; H7FORGE §6 residual 1).
**Artifact:** `lean/LeanUrat/Scaffold/HDischarge/H7/H7ForgeDepth2.lean`.

## 0. Recovery audit (what the checkpoint contained, what was completed)

The d0e2b74 orphan (415 lines) was audited against its own header spec:

* PRESENT and (on recompile) GREEN: §O — the full obstruction chain
  (`zmod2_unit_val`, `node_g_eq_one`, `sa_leg_false`, `twoNode_coherent_false`,
  `no_depth2_member`); §C — both child nodes (`fChildS0`/`fChildS1`, all ~30 Node
  law fields discharged) + distinctness; §H2 — the snoc histories `fHjC0`/`fHjC1`
  + nodes/length/strFrame rfl-lemmas; §PN — the per-node coherence layer
  (`pernode_fHjC0/1`, the first three `HistoryCoherent` conjunct families verbatim).
* MISSING (header promised, file ended mid-plan): the hull keying
  (`child_slope_keyed`/`child_steepening`), the vertex transport + admissibility +
  realizability (`vtx_fNodeJ`, `transAdm_fChildS0/1`, `realizable_fHjC0/1`), the
  canonical presentation + box (`canonRoot_fChildS0/1`, `canonpres_fHjC0/1`,
  `inBox_fHjC0/1`), the §X transition-leg pinning (`transition_leg_data_fHjC0/1`,
  `sa_leg_fHjC0/1_false`, `coherent_fHjC0/1_false`, `fHj_snoc_never_coherent`),
  and the `#print axioms` census.
* Completion was ADDITIVE ONLY: zero checkpointed bytes changed except the header's
  §X paragraph, amended to name the symmetric side-1 negations
  (`sa_leg_fHjC1_false`, `coherent_fHjC1_false`) this completion adds.

## 1. THE TRANSPLANT VERDICT: REFUTED at the forge's ambient (the compiled wall)

The HK23/HK25 mechanism does NOT transplant to (p, F) = (2, ZMod 2). The
machine-checked chain (§O):

* `node_g_eq_one` — over the prime-field ambient, EVERY node has descend degree
  g = 1: `hzbarRoot` demands a root of the irreducible ψ in the ambient field, whose
  only unit is 1; an irreducible with a root is linear. HK23's `ν₀ram` evades this
  over F4 only because ψ₂ = z² + z + 1 has its root r₀ IN F4 — genuine residue
  growth, structurally unavailable at ZMod 2.
* `sa_leg_false` — the (S-a) transition leg is UNSATISFIABLE at ANY parent over
  (2, ZMod 2): the leg's own RG-2 fence gives e = 1; with g = 1 the recorded
  `IsNodeLift` keeps the key degree (Φ̂ = Φ + t₀, deg t₀ < deg Φ), but
  `TransitionCoreL.base.child_reps` puts the parent key on the child's rep list and
  `Stage.hreps` forces deg Φ < deg Φ̂ — the S9-corner genre fires at EVERY parent.
* `twoNode_coherent_false` — every `HistoryCoherent` history over (2, ZMod 2) has
  length 1 (the first transition's parent is the root read, species ≠ recentering,
  so the (S-a) leg fires and is false).
* `no_depth2_member` — NO `TreeModel` over (2, ZMod 2), at ANY box parameters,
  realizes any 2-node chain (`mem_realizable` forces coherence). The sides-2 site's
  continuations are UNREALIZABLE at this box; realizing them needs an ambient with
  residue growth — the F4 relocation, a NEW unit, out of this unit's additive scope.

H7FORGE §6 residual 1 therefore converts from "open transplant" to "REFUTED at
(2, ZMod 2); relocation to F4 required". This is the honest-obstruction landing
demanded by the charter's FALSE/type-blocked clause — no statement was weakened.

## 2. The narrowed true layer (everything short of the refuted leg)

Per hull-slope clause of the sides-2 site's split (`forgeSplitJ`, hull slopes
1 < 2), a LAWFUL child node continues the joint chain `fHj = [fNodeJ]`:

* `fChildS0`/`fChildS1` — recentering-species reads (the HK25 `ν₁rec` genre;
  increment is unlawful at e·g = 1 by `hspecInc`), slope-KEYED to the hull:
  `child_slope_keyed` proves slope(child of side j) = slope(fNodeJ) + hullSlope j
  (4 = 3 + 1, 5 = 3 + 2); `child_steepening` the strict slope chain. The S2
  CENTER-distinction genre is UNAVAILABLE (recentering over ZMod 2 forces
  center = z̄ = 1); the children are h/slope-distinguished (`fChildS0_ne_fChildS1`).
* `fHjC0`/`fHjC1` = `fHj.snoc fChild*` carry, PER NODE, exactly the depth-1 law
  layer: `pernode_fHjC0/1` (root frame degree, slope law, γ-tie — the three
  non-transition `HistoryCoherent` families), FULL realizability `realizable_fHjC0/1`
  via `transAdm_fChildS0/1` against the computed transported vertex
  `vtx_fNodeJ : fNodeJ.vtx = 1` ((NA) vacuous — both reads adjacent s0+wSide = 1 = μ₀;
  (HV) exact — pattern lead 1 = vtx; (SAE) strict 6 < 7/8 at the interior slot,
  vertex equality 3 = 3), `canonpres_fHjC0/1` (polTriv lifts + canonical residue
  roots), and `inBox_fHjC0/1 : InBox 2`.
* §X pins the failure surgically: `transition_leg_data_fHjC0/1` prove EVERY
  NON-(S-a) conjunct of the coherence transition leg at the instance (recentering
  leg vacuous, RG-2 e₀ = 1, both Bézout ties, window 0+1 ≤ 1 = μ₀, width chain
  D₁ = 1 = childWidth₀, strict steepening); `sa_leg_fHjC0/1_false` refute EXACTLY
  the (S-a) existential; `coherent_fHjC0/1_false` + `fHj_snoc_never_coherent` are
  the instance negations (the latter: NO snoc extension of `fHj` whatsoever is
  coherent).

## 3. Honesty displays

* The depth-2 analogues of the forge's three non-diagonality certificates are NOT
  stated: they would key on realized depth-2 sites, and `no_depth2_member` proves
  that class EMPTY over this box. Recorded as obstructed, not dodged.
* **Nothing here discharges (H7); (H7) stays [M].** H7-W3a (engine-tied semantics)
  remains the Tier-M obligation. The forge stays depth-1; H7FORGE §4's honesty
  display stands, with the depth-2 residual now CLOSED NEGATIVELY at this box.
* `H7Forge.lean` consumed read-only (its 15 committed decls untouched); new
  declarations only; no existing statement modified anywhere.

## 4. Build, census, repairs

* **Recompile round 1** (checkpointed §O–§PN + completion): the entire checkpointed
  part compiled CLEAN on first recompile — the predecessor's mid-work was sound, just
  truncated. Two repair sites in the completion, both proof-level (no statement
  weakened, both statements kept their promised content):
  1. `child_slope_keyed` — `forgeSplitJ.hullSlope 0` fails elaboration: the side
     type `Fin forgeSplitJ.k` has no OfNat numerals (`k` is not a syntactic
     literal). Repair: supply indices at `Fin 2` (defeq; the `forge_jcmulti` idiom).
  2. `vtx_fNodeJ` — a `show` at `↥toyStage.K` types for the vtxPoly computation
     leaves a MIXED-INSTANCE target: show's unification fills the pow instance
     from the `fNodeJ.σ.K` side of the original target instead of synthesizing it
     (full type ascription does NOT cure it — round 2 reproduced the frankenstein
     with the instance and explicit args swapped), and `rw [pow_one]` then cannot
     match ("target not type-correct under instances transparency"). WORKING
     PATTERN (recorded for future cross-defeq-type goals): elaborate the equality
     FRESH as a standalone `have key : ... := by ...` (homogeneous instances, no
     target unification), then `exact key` across the semireducible defeq. Repair
     notes are inline in the file.
* **Gates (all PASS, 2026-08-03):** `lake env lean
  LeanUrat/Scaffold/HDischarge/H7/H7ForgeDepth2.lean` — exit 0, zero errors, zero
  warnings (compile round 3; round 1 = the two repair sites above, round 2 =
  repair 1 landed + repair 2's first attempt refuted). In-file `#print axioms`
  census: **29/29 declarations `[propext, Classical.choice, Quot.sound]`, ZERO
  `sorryAx`** (the four §O obstruction theorems, the two children + distinctness,
  the two histories, pernode/slope-keying/steepening, vtx + admissibility ×2 +
  realizability ×2, canonpres ×2, inBox ×2, transition-leg data ×2, (S-a)
  negations ×2, coherence negations ×2, `fHj_snoc_never_coherent`).

## 5. Residuals (named)

1. **F4 relocation** — realize depth-2 continuations of a forge-genre sides-2 site
   over an ambient with residue growth (F4; the HK23 `ν₀ram` root-read genre as the
   parent, so g = 2 and the (S-a) lift genuinely grows the key). NEW unit; needs a
   new F4-ambient forge or a bridge from the HK23/HK25 gate histories to a ledger.
2. **H7-W3a** — unchanged Tier-M obligation (engine-tied `ITauSemantic`/
   `KLocSemantic`); this unit adds decision-point structure only.
3. **`ITauShadow forgeDatum`** — unchanged (H7FORGE residual 2).
