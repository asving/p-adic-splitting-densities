# D-11 (MASS-ID) — Phase-B attempt REV 4 (Unit A, 2026-08-04): the true-scope repairs + the r4 proof obligations, written out

Repair of the pass-3 hostile verification (`D11_owedpass_2026-08-03.md`, Codex
fresh context, VERDICT CRITICAL — 6 CRITICAL, 3 GAP) of the rev-3 attempt
(`D11_massid_phaseB_attempt_rev3.md`, now SUPERSEDED; pass-1 record =
`D11_pass1_verify.md`, pass-2 record = `V31_d11r2.jsonl`, their dispositions in
rev 2/rev 3's §0-pre, on file there). **Governing design:**
`R11_REPAIR_DESIGN_V5_2026-08-04.md` (execution-ready per its §9; this file is
its Unit A, deliverables L1–L11). Produced by COPYING rev 3 and applying ONLY
the design's edit list, so every pass-3-confirmed or unattacked passage —
M1(a)'s (i)/(ii)/(iii) bullets (modulo the two sanctioned pointer re-points of
L4), M1(c)'s booked-row/termination displays, M1(d), the verbatim L2/L3
quotes, Proposition M1′ (whose arithmetic pass 3 checked and confirmed), the
root layer (R-read)/(R-b/R-d)/(R-balance), §6's falsifier record — is carried
byte-stable (diff-checkable against rev 3: the delta is exactly the L1–L11
dispositions below). New sections: §2A ((ROW-Σ)), §4A ((CH-COV)), §4B ((NSF-INT)
leaf proof).

**REVISION 2 (2026-08-04, pass-1 fold):** the r4 pass-1 hostile verification
(`D11r4_pass1_2026-08-04.md`, Codex fresh context, VERDICT CRITICAL — 3
CRITICAL, 3 GAP) is repaired IN PLACE below; every edit is marked
"[REVISION 2, pass-1 …]" at its site and the full disposition table is the
"## REVISION 2 (pass-1 fold)" section at the end of this file. The pass-1
VALIDATED content ((ROW-Σ)'s arithmetic and solved normalization, the
termination argument modulo its stated premises, M2's (RE)/(PACK-iv)
scoping, (BOX-VOL), O-1..O-5/O-GLOB's structure) is byte-stable.

**The two NAMED DUTIES of this revision** (round-5 plan-review
`R11D5_review_2026-08-04.md`, criticals 2 and 3, folded into Unit A's charge by
the design §9): **(duty-2)** the (ROW-Σ) write-out must enumerate the COMPLETE
consumption edge set — no "untraced cover" clause survives (§2A.4); **(duty-3)**
the (O-GLOB) write-out must handle finite-time encounters with the null
remainder Z_μ — a trajectory may hit Z_μ after a finite loop history, and that
event is neither a displayed exit nor an eternal loop (§3, clause (O-2)(ii) +
(G-c)).

## 0-pre. Pass-3 findings → dispositions (no silent drops)

| # | pass-3 finding (gist) | class | disposition (design §; this file's §) |
|---|---|---|---|
| 1 | M1(a′) contradicts M1's announced conditioning scope: vol(C_e) = q₀^{−e}, vol(R_e) = q₀^{−E} are AMBIENT Haar masses, the singleton-residual readings PER-CELL — "M1 does not have one coherent meaning of 'mass'" | CRITICAL | **FIXED (design §2.1; here §2, L1)**: the preamble names the reference measure PER CLAUSE — clause (a) conditional on C_e; (a′) per occurrence as displayed (ambient boxes / per-cell singletons / NO mass reading at the (O3) scalings); (b)/(d) statements about the one-step strata of the C_e-conditional process; (c) per sub-clause. No formula changes — pass 3 found no arithmetic error, and every (a) bullet was already coherently conditional-on-C_e as displayed. Downstream unqualified-"mass" sweep executed (record at the end of §0-pre) |
| 2 | The family-(v) claim repeats the factor-as-mass error at the L6a cell factors q₀^{−N(P)}: the cell mass is (1−q₀^{−1})^k·q₀^{−N(P)} and no valuation-box EVENT was defined | CRITICAL | **FIXED (design §2.2; here §2, L2)**: the RELAXED BOX □(P) is DEFINED (the ≥-part of L6a's own verified event display) and Lemma **(BOX-VOL)** proves vol(□(P)) = q₀^{−N(P)} and P(cell | □(P)) = (1−q₀^{−1})^k in two lines over L6a's own per-coordinate displays; (a′)'s first sublist restated so each q^{−b} occurrence is the ambient mass of a NAMED box [rescoped at REVISION 2, pass-1 critical 1: MEASURE-SPECIFIC per occurrence — ambient for the named boxes C_e/R_e/□(P), CONDITIONAL for the ratio q₀^{−(E−e)} = P(R_e | C_e)]; the k-distinctness and N(P)-provenance duties executed in-lemma |
| 3 | §5/§7 billing contradiction: (a′) admits the (O3) scalings exceed 1, yet §5 bills "family (v) per entry per (a′)" and §7 bills the unit charge "covered exactly at (a)/(a′)/M1′" | CRITICAL | **FIXED (design §2.3; here §§5/7, L3)**: §5 clause 1's family-(v) fragment and §7's coverage sentence re-billed to match (a″) exactly — the (O3) scalings are NOT masses, their entire mass content is (a)'s (O3) bullet (scaling × exit mass = mass of the displayed resummed event), and they are billed ONLY so |
| 4 | M1(b)'s multi-step disjointness sentence is FALSE without a prefix-free restriction (E_{w′} ⊆ E_w when w is a proper prefix of w′) | CRITICAL | **FIXED (design §2.4; here §2, L4)**: the refuted sentence WITHDRAWN and replaced by **(b-PF)** (prefix-free multi-step disjointness, proved) + **(b-EX)** (grammar exhaustiveness: every multi-step event booked by the displayed grammar is stopping-word-indexed — stated as the r4 obligation and PROVED here by the seven-family induction + the three booking adjustments, §2(b) [rescoped at REVISION 2, pass-1 critical 2, route (a): the quantifier runs over the EXPLICITLY RESUMMED row terms; solved-value legs excluded as non-booked continuation values]) + **(LOOP-∞)** (the infinite loop-0 word: null + disjoint, displayed). Consumer re-points executed: (a)'s (O3) bullet, the (O3) exit-scaling line, (c)'s complement reread, M2's exhaustiveness note |
| 5 | Lemma M2.0's address induction omits the within-block loop history: a child is born after L ≥ 0 loop steps then exit; "{node a reached} = {node a′ reached} ∩ {x_{a′} ∈ F}" is false when loops precede the hand-off | GAP | **FIXED (design §3.4; here §3, L6)**: addresses LOOP-EXTENDED (per-level loop words in the address); the transport induction runs ONE LETTER at a time with two letter kinds (loop letters ← PROVED L2+L3; hand-off letters ← (S1-m)); the obligations **(O-1)–(O-5)** and the global **(O-GLOB)** ((G-a)–(G-d)) stated and PROVED here at the lemma's displayed (S1-m)+(DET) conditionality (loop legs on PROVED L2+L3 only), with duty-3's Z_μ clause ((O-2)(ii)) added to the null union |
| 6 | Theorem M2 is FALSE at its displayed universal scope: arbitrary allocation data satisfy (S1)/(S2) while the rows leave [0,1]; the proof mis-cites M1(a) at family-(iv) rows | CRITICAL | **FIXED (design §3.3/§3.5; here §3, L5)**: Theorem M2 RESTATED AT the hypothesis **(RE)** (re-entrant booking profile — a pure conjunction; arbitrary-α members EXCLUDED in-statement); the proof step re-derived from (RE) + the verified §2.3(iv) grammar display (quoted into §0) + M1(a) families (ii)/(iii) ONLY; at the CONSUMED member, (RE)+row-form is priced at ROOT as the NEW (H5) member **(PACK-iv)** — the leaf does NOT discharge it (the definitional discharge is WITHDRAWN, design §3.3) |
| 7 | M3(b) silently consumes the open clause-4 correspondence ("M1/M2 identify table values with masses" — but attaching to ACTUAL tables is the open pass) | CRITICAL | **FIXED (design §2.7; here §4, L8)**: the conditionality moved IN-STATEMENT — "Under the clause-4 correspondence (OPEN — §5 clause 4; ROOT prices it as (H5)/(PACK)) …"; the unconditional half restated at the stipulated-member scope; the GD/CU dictionary byte-stable |
| 8 | M3(a) drops the separability qualifier of its cited premise (Theorem E covers monic SEPARABLE lifts; "EVERY polynomial" is not displayed; ns-null justifies only a.e.) | GAP | **FIXED (design §2.5; here §4, L7)**: the conclusion restated at Theorem E's own scope (every monic SEPARABLE lift) + the mass-sufficiency note (ns-locus Haar-null, so mass/counting consumers unaffected); the pin → predicate → corpus chain PRICED as **(NSF-INT)** and the as-built keying as **(NSF-KEY)** — the two members of ROOT's NEW row (H9) = (NSF), displayed at §4 with the M02 divergence record; the (NSF-INT) leaf proof written out at §4B (artifact I-1, gate I-3 = this file's owed hostile pass); the channel-coverage audit **(CH-COV)** executed at §4A |
| 9 | The falsifier ledger's criterion is broader than the theorem's admitted scope ("every entry … in [0,1] (necessary for M1(a))" — but (a″) excludes families, and the (O3) scalings exceed 1) | GAP | **FIXED (design §2.6; here §6, L9)**: the criterion rescoped to families (i)/(ii)/(iii) (the (a″) scope); the (O3) scalings checked against their displayed > 1 form; no scanned quantity changes |

**The unqualified-"mass" sweep (L1's r4 duty, record).** Every occurrence of
"mass" in §§3–7 below now either (i) displays its conditioning event inline
(P(· | C_e), P(· | cell), per-block state law C_μ), (ii) names AMBIENT Haar
explicitly (vol(·), "Haar-null", the box masses of (a′)), or (iii) refers to a
named clause whose preamble fixes the measure ((a)/(a′)/M1′/(R-b)). The three
spots rev 3 left genuinely unqualified are pinned in place: §3's "P(F)" in the
M2 proof step (→ P(F | C_e), inside the L5 replacement), §5 clause 1's
family-(v) fragment (→ the L3 re-billing), §7's coverage sentence (→ the L3
re-billing). M1′ and the root layer already displayed their measures
(P(· | C_2); the top state Haar on monic degree-n f) and are byte-stable.

Pass-3 CONFIRMED or unattacked and carried byte-stable: M1(a)'s
(i)/(ii)/(iii) bullets (pass 3: "the verifier found no arithmetic error
anywhere"; the (O3)-bullet disjointness parenthetical is re-pointed per L4 —
a pointer edit, not a content change), M1(c)'s displays (+ one sanctioned
pointer re-point), M1(d), Proposition M1′ ("its displayed arithmetic survives
checking"), the L2/L3 verbatim quotes, the root layer, §6's falsifier record
(modulo L9's criterion sentences).

**Unit charge (tree §D1.2 D-11 node + DELTA-2 §§D2.2/D2.7 pricing + CU_cluster §6.1
audit).** Prove that the abstract solve family 𝔅_n's kernel/entry VALUES are the Haar
masses of the continuation events at the intended instance — the identification through
which O-12's pole-free theorem (VERIFIED, leaf #2) gets to speak about the real
densities — composed from the displayed pieces; or the honest partial with the exact
seam. Sub-charge: close OL-O12-2's mathematical half. This unit is written on the
CU_cluster §6.1 audit's pricing (per-box lift uniformity = O5triple Theorem E + D-15;
NO ∼_OM/OB2 consumption) and constructively re-confirms it: no step below pools
OM-equivalent polynomials.

## 0. Sources and the status of every consumed piece

* `O12_phaseB_verifybrief_rev4.md` — **VERIFIED at honest scope** (V19 + Fable pass 2;
  ledger commit 95a46d2). **The scope subtlety this unit lives on:** the O-12 CLAIM is
  zeros/poles only ("VALUE correctness is also not part of the claim", §1), but the
  brief's §3 is a set of fully-proved SEMANTIC MASS lemmas — L1 (coefficient/root
  valuations), L2 (rescale self-similarity, measure-preserving), L3 (re-cluster,
  measure-preserving + type-preserving), L4 (hand-off bounds μ < e), L6a (polygon cell
  = displayed event, exact Haar volume), L6b (affine cone exponents c_j ≥ 1), L6c/L6d
  (residue independence + the joint count-rationality (JC)), L6′ ((g4) closure), L6e
  (exhaustion off the displayed null set Z_e, disjoint cells), Fact F (universal
  pattern counts), Theorem 1 (kernels/determinants; its clause (3) displays the four
  bookings' balance equations — the re-entrant three are all algebraically
  (1 − κ₀ − κ₁)x = s), Theorem 3 ((r1)+(r2) at every
  prime power, margins 3/4 · 7/8 · 6/7), **Corollary D** (the det channel: the brief's
  margins-corollary of Theorem 3 — D_{Φ_e}(q₀) ≠ 0, N_{Φ_e}(q₀) ≠ 0, and the displayed
  per-booking margins Φ^{(O1)} ≥ 3/4, Φ^{(O2)} = Φ^{(O2′)} ≥ 7/8, Φ^{(O3)} ≥ 6/7 at
  every prime power q₀ ≥ 2), Theorem 4 (substochastic robustness) — all
  inside the verified perimeter (the rev-1..rev-3 audit arc confirmed exactly these).
  Also consumed as brief-internal DISPLAYED inputs (claim-scope text verified with the
  brief, named here per pass-1 F6(ii)): the §2.4 booking displays — the four kernels,
  the balance-equation forms `(O2′) x = κ₀x + (κ₁x + s)` ("the κ₁ strata booked as
  exit legs that RE-FEED block e's own entrance") and the (O3) resummed equivalence
  (`(1 − κ₁/(1−κ₀))x = s/(1−κ₀)`, same solve after multiplying by 1 − κ₀ ≠ 0).
  Because M1 consumes L2/L3 in their full conditional-pushforward strength (pass-1
  F5), the two verified statements are quoted VERBATIM from the brief's §3:

  > **Lemma L2** (rescale self-similarity). Conditioned on R_e = {a_i ∈ π^iO ∀i},
  > the coefficients b_i := a_i/π^i of h(y) := g(πy)/π^e are independent Haar-
  > uniform on O, and g, h have equal splitting types.

  > **Lemma L3** (re-cluster). For Haar-uniform monic h of degree e: the reduction
  > h̄ is uniform on the q₀^e monic degree-e polynomials over F_{q₀}; for each
  > c ∈ F_{q₀}, the event {h̄ = (y − c)^e} has volume q₀^{-e}, and conditioned on
  > it, h(c̃ + y) is Haar conditioned on C_e (the block state law), with splitting
  > type equal to that of h.

  These contain exactly the two consumptions M1 makes: (i) the uniform-reduction
  clause (h̄ uniform over the q₀^e monic residuals — what converts Fact F counts to
  masses), and (ii) the conditional-pushforward clause (conditioned on a loop stratum,
  the transformed state is AGAIN the state law with equal type — the Markov property
  driving the (O3) resummation and M2's loop-term handling).
  MASS-ID consumes the LEMMAS as mass statements, which is what they are; it does NOT
  widen the O-12 claim retroactively — the composition below is NEW material with its
  own conditionality display.

  **[NEW at r4, per design L5 — the verified grammar displays that Theorem M2 (r4),
  (b-EX), and (ROW-Σ) consume, quoted verbatim from the brief:]**

  > [§2.3(iv), the verdict-row grammar] The verdict row of F at verdict σ is
  > row_σ(F) := m(F) · Σ_{(σ_1,…,σ_r) : comp_F(σ_1,…,σ_r) = σ}
  > ∏_{i=1}^{r} β_{μ_i}(σ_i)(q^{D_i}), a finite sum of products with
  > coefficients 0 or 1 — no other scalars enter. r = 0 gives the terminal rows
  > (row_σ(F) = m(F) when comp_F() = σ, else the row is absent); r = 1 the split
  > rows; r ≥ 2 the junction rows. Per verdict, the aggregated row
  > t_σ := Σ_F row_σ(F) — again a 0/1-coefficient finite sum. Booking
  > adjustments (§2.4): under (O2) the table carries one further datum, the
  > *allocation datum* — a map α_e : {verdicts of block e} → ℚ[q] with
  > Σ_σ α_e(σ) = q − 1 — and the aggregated verdict row becomes
  > t_σ + α_e(σ)·q^{−E} …; under (O3) every exit row is multiplied by the
  > scaling q^E/(q^E−1) of family (v).

  > [§2.3, the E(e) definition] E(e) := the union of (i)–(vii): a finite set of
  > displayed rational functions PER MEMBER.

  > [§2.3(iv), the hand-off list — the FINITE-SLOT display (O-GLOB (G-a)'s
  > branching input)] each family F from (ii)/(iii) carries its *hand-off list*
  > H(F) = ((μ_1, D_1), …, (μ_r, D_r)), read off F's pattern data — the atoms
  > (D, m) with m ≥ 2 …, booked to block μ = m at base change q ↦ q^D (§2.2;
  > μ < e by Lemma L4) — and a *composition datum*: a map comp_F assigning to
  > each tuple (σ_1, …, σ_r) of verdicts of the hand-off blocks a verdict of
  > block e (verdicts = splitting types of degree ≤ n, a FINITE set; the m = 1
  > atoms terminate and are absorbed into comp_F as fixed data).

  > [§2.3(i)] *the kernel entry*, per booking (§2.4): q^{−E} or q^{1−E} …, or
  > (q−1)/(q^E−1) = Σ_{w≥0} q^{−Ew}·(q−1)q^{−E} [(g4) …].

  > [Theorem 2 (vii)] block e's own solved entries β_e(σ) := u^{−1}·t_σ, u the
  > booking's solve denominator (§2.4, Theorem 1(3)).

  Also consumed at (ROW-Σ): O-12 Theorem 1(3) (verbatim, already quoted above:
  the three re-entrant balance equations are all algebraically
  (1 − κ₀ − κ₁)x = s) and Corollary D (the pivot 1 − q₀^{1−E} ≠ 0 at every
  prime power q₀ ≥ 2).
* `O1thr_phaseB_verifybrief_rev4.md` §6 **Theorem 3 = D-15** (CYL + mass/count tie:
  decided ns-free verdict classes are exact finite unions of level-t cylinders;
  #Φ_N(T) = mass(T)·p^{nN}; per-box tree uniqueness/disjointness). CONDITIONAL-ON-PASS
  **P(O-1thr r4)** (rev-4 landed, fresh pass owed); caveat **(E-N)** OPEN (displayed
  where it bites). [NEW at r4: §1.4's state/halt/tree vocabulary is additionally
  consumed by the §4B (NSF-INT) leaf proof — the displays quoted there.]
* `O5triple_phaseB_verifybrief_rev3.md` §6 **Theorem E** (D-7c + O-5 ASSEMBLY: "any two
  monic separable lifts of x have equal splitting types"; the multiset is engine data,
  lift-independent). Conditional on interface (I-1)–(I-3) (served per CU-7's corrected
  attribution) + the five §2 flags; **P(O5t r3)**.
* `CU_cluster_phaseB.md` — CU-1/CU-2 (no-missing/no-phantom, per-f), CU-7 (interface
  discharge), §6.1 (the D2.7 audit: D-11 does NOT consume OB2), §0 pending-verdict
  registry (P(O-2a r5), P(O-1thr r4), P(O5t r3), P(CU-1 r2) = V29, P(GD23 r2) = V28).
* `docs/MATH_COMPLETION_TREE_2026-08-01.md` — the D-11 node text (§D1.2), T-3/T-6 node
  texts (§2.2), D2.2 GD table (GD-2/GD-3 rows), D2.6 path step 8.
* `O12_phaseB_attempt_rev4.md` §3 — OL-O12-2's own text ("dischargeable by a finite
  transcription-and-correspondence pass").
* [NEW at r4, consumed by §§4/4A/4B] `M04-series-tie_fable.md` (§§2–3, 5.5 —
  Theorems 1/2/4, Lemmas 3.1–3.3, the K1–K4 kernel statements);
  `M02-count-tie_fable.md` (headline + §2 — the exact-square poison V₀, the
  as-built carrier record); Lean HEAD quotes re-verified this session:
  `MovesU/BridgeD3_treeCarrier.lean` line 94–95 (`def bridgeTree (n p : ℕ)
  [Fact p.Prime] (σ : SplittingType n) : Type := {V : MovesT.VTree p (ZMod p) //
  V.typemult = σ.1 ∧ RealizedSelf n p V}` — NO ns-free conjunct),
  `MovesT/Defs.lean` line 1534 (`def NsFree (Tr : VTree p F) : Prop := ∀ H ∈
  Tr.chains, ¬ Tr.nsLeaf H` — a SEPARATE predicate, conjoined nowhere in the
  carrier), `MovesT/E11_treeN.lean` lines 48/118 (`hdet : ∀ H ∈ Tr.chains,
  ¬ Tr.nsLeaf H`, passed into `treeExp` at line 68). ROOT
  (`docs/ROOT_ASSEMBLY_2026-08-02.md`, REVISION 9) Steps 11/12/17/18/18b/19
  displays — the consumed lines for §2A.4's edge walk and §4A's closure walk.
* Numerics of record: N6-regp (25/25 gates, 95/95 primes, 0 failures, min margin 6/7);
  N2-density-tie (24/24 configs + exact n = 2 census, width ~1e-5); O-12 §8 rev-2/4
  (L6a cell-law and L6e-trichotomy exact enumerations). Falsifier extension
  N6-regp-ext2000: §6.

**Standing perimeter carried:** monic separable; ns loci Haar-null (T-6 (ns-null),
VERIFIED-DUAL); the intended instance = the O-12 §2.1 semantic model verbatim: prime p,
δ ≥ 1, q₀ = p^δ, K₀/ℚ_p unramified of degree δ, O its integers, Haar on monic degree-e
polynomials via coefficient tuples O^e; E := E_e := e(e+1)/2.

## 1. The objects identified (definitions, all displayed)

**The descent process 𝒟_e (the semantic reading of block e).** State space: the
aligned size-e cluster law — Haar conditioned on C_e = {v(a_i) ≥ 1 ∀i} (O-12 §2.2).
One step = the O-12 §2.2 stratification of C_e:

    (loop-0)   R_e ∩ {h̄ = y^e}                      [rescale, recenter at 0]
    (loop-c)   R_e ∩ {h̄ = (y−c)^e}, c ∈ F_{q₀}^×    [rescale, recenter at c]
    (patt-π)   R_e ∩ {h̄ has pattern π}, π ≠ {(1,e)} [rescale, split; hand-offs
                                                      = the atoms (D, m), m ≥ 2]
    (poly-κρ⃗)  (C_e \ R_e) ∩ {polygon P of face-kind κ, w ∈ cone} ∩ {shapes ρ⃗}
                                                     [polygon exit; hand-offs
                                                      = the atoms of the ρ_j, m ≥ 2]

up to the displayed Haar-null set Z_e = (C_e\R_e) ∩ {a_e = 0} (L6e; Z_2 = ∅). On loop
strata the step map is g ↦ ĥ := h(c̃ + y), h = g(πy)/π^e; L2 + L3 say: conditioned on
the stratum, ĥ is AGAIN the state law (Haar on C_e), with the same splitting type. On
hand-off strata the booked children go to blocks μ < e (L4) at base change q ↦ q^D.

**[NEW at r4 — notation for the loop alphabet and within-block trajectories,
consumed by §2(b) and §3.]** Write Λ_e := {(loop-0)} ∪ {(loop-c) : c ∈ F_{q₀}^×}
for block e's LOOP ALPHABET (a finite set: 1 + (q₀ − 1) = q₀ letters at pool q₀;
q₀^D letters at a base-changed pool q₀^D), and Exits(e) := the displayed
pattern strata {(patt-π) : π ≠ {(1,e)}} ∪ the displayed polygon strata
{(poly-κρ⃗)} — block e's EXIT STRATA (each either a HAND-OFF stratum, if its
pattern data carry an atom (D, m) with m ≥ 2, or a TERMINATING exit, if all its
atoms have m = 1 — no child slot). The one-step partition of §2.2 is then

    C_e = ⨆_{s ∈ Λ_e} s ⊔ ⨆_{F ∈ Exits(e)} F ⊔ Z_e.

The WITHIN-BLOCK TRAJECTORY of a state x ∈ C_e is x^{(0)} := x, and
x^{(t+1)} := the loop step map applied to x^{(t)}, DEFINED exactly when
x^{(t)} lies in a loop stratum (the step maps are stratum-wise; on exit strata
and on Z_e no within-block step is defined). For a finite word
w = (s_1, …, s_k) over Λ_e, "x EXECUTES w" := x^{(t−1)} ∈ s_t for t = 1..k
(so all intermediate states through step k−1 lie in loop strata by
definition); the post-w state is x^{(k)}. Note the step maps land in C_e
pointwise: on {h̄ = (y−c)^e} the recentered ĥ = h(c̃ + y) reduces to y^e, i.e.
every non-leading coefficient of ĥ has positive valuation — ĥ ∈ C_e.

**Per-row continuation events.** For a row of a 𝔅_n member (a §2.3 family member), its
*continuation event* is the sub-event of the state law that the row books, with
multi-step rows read through the step maps: e.g. the (O3) kernel books

    E_rec := ⨆_{L ≥ 1} E^{(L)},   E^{(L)} := {first L−1 steps are (loop-0),
                                              step L is some (loop-c), c ≠ 0},

and the (O3)-scaled exit rows book ⨆_{L ≥ 0} {L steps of (loop-0), then the exit
stratum}. These are genuine measurable events: finite intersections of preimages of
the displayed strata under the (measurable, measure-preserving) step maps.

**The abstract side.** A 𝔅_n member's kernel K_e, entries E(e), determinant
Φ_e = det(1 − K_e), solved values β_e(σ) = u^{−1}t_σ — fixed elements of ℚ(q),
evaluated at q₀ (defined there by O-12 Theorem 3).

## 2. Theorem M1 (one-step mass identification — block-level, unconditional over the verified O-12 lemma base; scope AND reference measures displayed IN-STATEMENT)

**Theorem M1.** Fix n ≥ 2, a member of 𝔅_n, a prime power q₀ = p^δ ≥ 2, a block
e ∈ {2, …, n} (block 1 is trivial: K_1 = 0, immediate termination). Then, with the
reference measure NAMED PER CLAUSE — clause (a): masses CONDITIONAL on the block
state law C_e (each bullet displays its conditional derivation); clause (a′): per
occurrence as displayed there (AMBIENT Haar on O^e for the valuation-box readings,
with the conditional forms used inside (a) displayed as ratios; the PER-CELL
conditional law for the singleton-residual readings; NO mass reading for the (O3)
scalings — they exceed 1); clauses (b)/(d): statements about the one-step strata of
the C_e-conditional process; clause (c): the per-visit margin sub-clauses
CONDITIONAL on the block state law C_e (the loop/exit masses and the (r1) margins
are C_e-conditional probabilities), and the per-block a.s.-exit sub-clause a
statement about the C_μ-conditional law of each block μ [the two sub-clauses'
reference measures named per round-1 advisory 7]:

**(a) [values are masses — families (i)/(ii)/(iii); pass-2 P2 rescope].** Every §2.3
family-(i)/(ii)/(iii) entry, evaluated at q₀, IS the Haar mass of its displayed
continuation event:
  * kernel, (O1): K_e(q₀) = q₀^{1−E} = P(R_e ∩ {h̄ = (y−c)^e for some c} | C_e) — the
    mass of "descend at a rational e-fold center" [L1 for R_e = the descent event; L2
    for the rescale; L3 for the reduction partition: q₀ centers × q₀^{−e} each ×
    P(R_e|C_e) = q₀^{−(E−e)}].
  * kernel, (O2)/(O2′): K_e(q₀) = q₀^{−E} = κ₀ = P(loop-0 | C_e) [same cites, c = 0].
  * kernel, (O3): K_e(q₀) = (q₀−1)/(q₀^E−1) = P(E_rec | C_e). Proof of the resummed
    identity: by induction on L, P(E^{(L)} | C_e) = κ₀^{L−1}κ₁ — the base is L2+L3;
    the step is the Markov property (conditioned on (loop-0) the transformed state is
    again the state law, L2+L3, so the next step's stratum probabilities are again
    κ₀, κ₁, …). The E^{(L)} are pairwise disjoint by (b-PF), the family
    {(loop-0)^{L−1}·(loop-c, c ≠ 0) : L ≥ 1} being stopping words per (b-EX), so
    countable additivity gives
    P(E_rec | C_e) = Σ_{L≥1} κ₀^{L−1}κ₁ = κ₁/(1−κ₀) = (q₀−1)/(q₀^E−1). The (O3) exit
    scaling likewise: the family {(loop-0)^L ∘ F : L ≥ 0} is a set of stopping words
    ((b-EX), family (iv) case), pairwise disjoint by (b-PF), so
    P(⨆_{L≥0} (loop-0)^L ∘ F | C_e) = P(F|C_e)/(1−κ₀) = P(F|C_e)·q₀^E/(q₀^E−1).
  * divisor-pattern masses: m(π)(q₀) = q₀^{−(E−e)}·N_π(q₀)·q₀^{−e} = P(patt-π | C_e)
    [L2 + L3's uniform reduction + Fact F(ii): N_π(q₀) counts the monic h̄ of pattern
    π].
  * polygon-family masses: m(κ, ρ⃗)(q₀) = P(poly-κρ⃗ summed over the depth cone | C_e).
    Per cell: P(cell) = (1−q₀^{−1})^k·q₀^{−N(P)} [L6a, exact]; P(shapes ρ⃗ | cell) =
    ∏_j P_{ρ_j}(q₀)·(q₀^{d_j}−q₀^{d_j−1})^{−1} — ONE value uniform over every cell of
    the face-kind [L6d (JC)]; the cells of the cone are pairwise disjoint [L6e(i)], so
    countable additivity along L6b's affine cone (exponents c_j ≥ 1 give convergent
    geometric sums Σ_w q₀^{−N_min−Σc_jw_j}) reproduces exactly the displayed closed
    form of §2.3(iii) [the L6′ algebra], times the conditional normalization q₀^{+e}
    (= 1/vol(C_e)).

**Definition (relaxed box) [NEW at r4, design §2.2].** For an L6a polygon cell —
by L6a's verified display, {g : v(a_{e−i}) ≥ ⌈h_i⌉ for every column i, with
equality at the k vertex columns} — its RELAXED BOX is
□(P) := {g : v(a_{e−i}) ≥ ⌈h_i⌉ for every column i} (the k equalities relaxed to
the inequalities L6a already displays; cell ⊆ □(P)).

**Lemma (BOX-VOL) [NEW at r4, design §2.2].** vol(□(P)) = q₀^{−N(P)} with
N(P) = Σ_i ⌈h_i⌉ (L6a's own exponent), and P(cell | □(P)) = (1 − q₀^{−1})^k;
hence L6a's cell mass factorizes as P(cell) = P(cell | □(P)) · vol(□(P)) with
BOTH factors masses/conditional masses of displayed events.
*Proof.* The coefficient coordinates are independent Haar with
P(v ≥ t) = q₀^{−t} and P(v = t | v ≥ t) = 1 − q₀^{−1} — the two per-coordinate
computations displayed inside L6a's verified proof; □(P) imposes one
≥-condition per column; the cell additionally one equality per vertex column;
multiply over the independent coordinates. ∎
*The two r4 duties, executed (design §2.2 / round-1 advisory 6).*
(k-distinctness) k counts DISTINCT imposed equalities: L6a's display imposes
the equalities "at the k vertex columns" x_1 < ⋯ < x_k — the vertex columns of
a polygon have strictly increasing abscissae, so the k equalities sit at k
DISTINCT coordinates, each refining that coordinate's ≥-condition (at a vertex
column the height h_{x_j} is a lattice value, so ⌈h_{x_j}⌉ = h_{x_j} and
{v = h_{x_j}} ⊆ {v ≥ ⌈h_{x_j}⌉} — the conditional factor is exactly
P(v = t | v ≥ t) per coordinate). (N(P) provenance) every q₀^{−N(P)}
occurrence this theorem references is (a)'s per-cell L6a factor: the polygon
bullet's "P(cell) = (1−q₀^{−1})^k·q₀^{−N(P)} [L6a, exact]", and the closed
form §2.3(iii)'s q^{−N_min(ε)} occurrences are those same L6a cell exponents
at L6b(ii)'s enumeration points, combined by the L6′ algebra — no other
q^{−N(P)} occurrence exists in the consumed displays.

**(a′) [family (v) — per-entry displayed readings; pass-2 P2/P5 + pass-3 findings
1/2: rev ≤ 2's blanket "each is a displayed factor of a mass" reading is WITHDRAWN,
and rev 3's per-occurrence sublist is repaired at its first member — a factor
occurring in a mass formula is not thereby itself the mass of a displayed event;
the reference measure of each reading is the one displayed for it here].** Family
(v)'s four sublists, each read at its occurrences in (a)'s closed forms and the
§2.3(iv)/(O3) adjustments:
  * q^{−b} (0 ≤ b ≤ E_n): each occurrence carries its displayed MEASURE-SPECIFIC
    reading — AMBIENT Haar for the three named boxes, CONDITIONAL for the ratio
    occurrence; NO uniform ambient claim is made [REVISION 2, pass-1 critical 1:
    the factor q₀^{−(E−e)} of the divisor-pattern formula is a C_e-conditional
    probability and the ambient Haar mass of NO displayed box (the displayed
    ambient boxes have masses q₀^{−e}, q₀^{−E}, q₀^{−N(P)}); the r4 sentence
    "each occurrence IS the AMBIENT Haar mass of a displayed valuation box" is
    WITHDRAWN]. The per-occurrence readings: vol(C_e) = q₀^{−e} (AMBIENT; box
    {v(a_i) ≥ 1 ∀i}) and vol(R_e) = q₀^{−E} (AMBIENT; box {v(a_i) ≥ i ∀i})
    [L1]; the divisor-pattern occurrence q₀^{−(E−e)} = P(R_e | C_e)
    (CONDITIONAL — the displayed ratio vol(R_e)/vol(C_e) of the two ambient box
    masses; this is the form used inside (a)); and each L6a cell factor
    q₀^{−N(P)} = vol(□(P)) (AMBIENT; the cell's relaxed box), with cofactor
    (1 − q₀^{−1})^k = P(cell | □(P)) [Definition + Lemma (BOX-VOL) above].
  * (q^d − q^{d−1})^{−1}: the conditional mass of a displayed SINGLETON-residual
    event — by L6c/L6d the face-j residual is uniform over its q₀^d − q₀^{d−1}
    admissible values, so each occurrence is P(residual = one fixed value | cell);
  * q^{E_m}/(q^{E_m}−1) (the (O3) exit scalings Σ_{L≥0} κ₀^L): NOT masses (they
    exceed 1); their mass reading is exactly and only (a)'s (O3) bullet — the PRODUCT
    with an exit-stratum mass is the mass of the displayed resummed event
    ⨆_{L≥0} (loop-0)^L ∘ F;
  * (q−1)/(q^{n+1}−1), the projective assembly weight: EXCLUDED from this theorem —
    quantified over NOWHERE in (a)/(a′). Its mass reading lives at the projective
    assembly layer and is seam **(S3)** in §3 (owner: annex node A-1 of the
    completion tree, the monic → projective bridge = June lemma M1); it is consumed
    only by the PROJECTIVE reading of M2's conclusion.

**(a″) [in-statement exclusions — the exact perimeter of the "values are masses"
claim, displayed here rather than in a downstream caveat (pass-2 P5)].** NOT claimed
by (a)/(a′): family-(iv) verdict rows under ARBITRARY allocation data α_e — they need
not be masses at all (they can leave [0, 1]; Proposition M1′ below covers exactly the
recorded concrete α₂ at n = 2, and nothing more); families (vi)/(vii) — the β-legs
and solved entries carry no per-entry mass identity, their density reading being
Theorem M2's CONDITIONAL conclusion; and the projective assembly weight ((S3)). The
unit charge's "kernel/entry VALUES" is covered exactly at (i)–(iii) + (a′)'s
readings + M1′'s concrete row, and not beyond.

**(b) [rows pairwise disjoint].** The one-step strata are pairwise disjoint and
exhaust C_e up to the null set Z_e: R_e vs C_e\R_e is the L1 dichotomy; within R_e the
reduction h̄ partitions [L3]; within C_e\R_e (off Z_e) the polygon cells partition
[L6e(i)] and, per cell, the joint-shape events partition [L6d's consistency clause:
the (JC) values sum to 1 over shape vectors].

> **(b-PF) [prefix-free multi-step disjointness; replaces the rev-3 sentence
> refuted at pass-3 finding 4].** For a finite word w = (s_1, …, s_k) over the
> one-step strata let E_w := {step j lies in s_j, j = 1..k} (composed through
> the step maps). If W is PREFIX-FREE — no member is a proper prefix of
> another — then {E_w : w ∈ W} is pairwise disjoint: two distinct members
> first differ at a position j that both words reach; on the common prefix the
> composed step-j state is the same point, and the one-step strata are
> pairwise disjoint at it by (b)'s one-step clause. [Without prefix-freeness
> the claim is FALSE — E_{w′} ⊆ E_w whenever w is a proper prefix of w′; the
> rev-3 sentence is WITHDRAWN at that generality.]
>
> **(b-EX) [booked-resummation exhaustiveness — stated as the design's OPEN
> LEMMA and PROVED below by the grammar induction; the seven-family enumeration
> does not by itself demonstrate the claim (round-1 finding 2) — the induction
> does. RESCOPED at REVISION 2 per pass-1 critical 2, ROUTE (a): the quantifier
> runs over the EXPLICITLY RESUMMED row terms; the recursive solved-value legs
> are excluded as non-booked continuation VALUES].** First the scope, displayed.
> A row term's BOOKED event content is the block-e event whose mass the term's
> displayed form itself carries — the row's immediate kernel/stratum leg, or
> the displayed resummation of such legs — read INTENSIONALLY off the display:
> a factor x_σ/β (families (vi)/(vii), the solved-vector coordinates — the
> (O2′) re-feed leg's x_σ in t_σ + κ₁·x_σ, every β-leg, and the solves'
> recursive x at every booking) is a solved continuation VALUE, NOT a booked
> event, and is EXCLUDED from (b-EX)'s scope. [Why the exclusion is FORCED,
> not cosmetic — the pass-1 counterexample: expanding κ₁·x_σ extensionally
> yields paths with arbitrary post-recentering letters, e.g. (loop-c) then
> (loop-0) then an exit, whose proper prefixes are NOT all-(loop-0) — no
> loop-0-prefix stopping-word set indexes them; the r4 universal-scope claim
> was false there. Why the exclusion is SAFE — the consumer check, the route
> decision's warrant: every consumer of (b-EX) partitions over BOOKED events
> only — (a)'s (O3) bullet's two countable-additivity computations (the
> kernel resummation E_rec and the scaled exit families), (ROW-Σ) step 3's
> (O3) row display, M2's exhaustiveness note (which names exactly "the (O3)
> resummations"), (b)/§5's scope sentences, AND — [REVISION 3, pass-2 gap 1:
> the omitted member, added to close the inventory] — M1(c)'s (O3)
> termination reread (the complement-of-E_rec decomposition, which consumes
> (LOOP-∞)'s exact display): it too partitions only the two booked (O3)
> resummations plus the null residue classes ({loop-0 forever} and the
> Z^{(L)} members), so it fits route (a) with no new booked class; NO consumer partitions an
> event over an expansion of a solved-value leg: M1 excludes families
> (vi)/(vii) from every mass claim ((a″)), and (ROW-Σ)/M2 consume those legs
> ONLY through the balance-equation algebra ((ROW-Σ) step 3; M2's (O2′)
> regrouping x = κ₀x + (κ₁x + s)), where x is eliminated by the pivot solve,
> never partitioned. The unions that DO range over general loop-word prefixes
> — (O-1)/(O-5)'s ⨆_{w ∈ Λ^{<ω}} at Lemma M2.0's layer — cite (b-PF) directly
> at their displayed mixed words w·F and consume (b-EX) nowhere. Hence route
> (b) (a general Λ_e^{<ω}-prefix grammar) is needed by no consumer, and route
> (a) is taken.] With that scope: every EXPLICITLY RESUMMED multi-step row
> term of the DISPLAYED 𝔅_n grammar (families (i)–(vii), E(e) their union —
> §0 quote; the three displayed booking adjustments of §2.3(iv)/§2.4: the
> (O2) α-fold, the (O2′) re-feed leg, the (O3) scaling) is indexed by a set
> of STOPPING WORDS over the one-step strata — words whose proper prefixes
> are all-(loop-0) and whose final letter is not (loop-0); the induction
> below finds exactly TWO such term classes (the (O3) kernel resummation and
> the (O3)-scaled exit rows). Any set of stopping words is prefix-free (a
> proper prefix of a stopping word is all-(loop-0), and no stopping word is
> all-(loop-0)), so (b-PF) applies to every such family. [Concrete packs'
> combined/adjusted rows remain clause-4 content — the scope sentence at the
> end of (b) stands unchanged.]

*Proof of (b-EX) — the grammar induction, one paragraph per family. The case
enumeration is exhaustive over the displayed grammar BY CONSTRUCTION of E(e):
O-12 §2.3 DEFINES E(e) as the union of families (i)–(vii) (the §0 quote "E(e)
:= the union of (i)–(vii)"), and §2.4 displays exactly three booking
adjustments — the (O2) α-fold, the (O2′) re-feed leg, the (O3) exit-row
scaling (the §0 quote's "Booking adjustments (§2.4)" sentence names the first
and third; the (O2′) re-feed leg is the §2.4 balance display "x = κ₀x +
(κ₁x + s)", quoted in §0). So a row of any member, in any booking, is one of
the seven families' displayed forms carrying at most these three adjustments,
and it suffices to walk the seven families and, inside families (i)/(iv)/(v),
the three adjustments.*

* **Family (i), kernels.** Under (O1)/(O2)/(O2′) the kernel entry books a
  ONE-STEP union of loop strata ((O1): all of Λ_e; (O2)/(O2′): {(loop-0)}) —
  no multi-step content; disjointness from other one-step strata is (b)'s
  one-step clause, and (b-EX) has nothing to certify. Under (O3) the kernel
  books E_rec = ⨆_{L≥1} E^{(L)} with word set
  {(loop-0)^{L−1}·(loop-c, c ≠ 0) : L ≥ 1, c ∈ F_{q₀}^×}: every proper prefix
  is all-(loop-0), and the final letter (loop-c), c ≠ 0, is not (loop-0) —
  stopping words. This is the ONLY family-(i) closure running along the TIME
  axis, and it is exactly the §2.3(i) display's (g4) resummation
  Σ_{w≥0} q^{−Ew}·(q−1)q^{−E} (quoted in §0), whose terms are the E^{(L)}
  masses of (a)'s (O3) bullet.
* **Family (ii), divisor-pattern masses.** m(π) books the ONE-STEP stratum
  (patt-π). Its (g1)/(g2)/(g6) generation (Fact F count × normalizations) is
  scalar arithmetic inside the one closed form — no event composition, no
  word structure. No multi-step content.
* **Family (iii), polygon-family masses.** m(κ, ρ⃗) books the ONE-STEP strata
  (poly-κρ⃗). The (g4) closures inside its closed form (exponents c_j ≥ 1) run
  along the DEPTH axis w of L6b's cone: they enumerate countably many CELLS of
  the SAME first step — O-12 §2.2's (S-exit) display lists the polygon
  partition among the one-step strata of C_e \ R_e, and L6b's cone indexes
  cells WITHIN that partition — never a second step of 𝒟_e. So the closure is
  a one-step countable union (disjoint by L6e(i)), not a multi-step event. No
  word structure.
* **Family (iv), verdict rows.** The block-e event a row row_σ(F) books is
  its exit stratum F — ONE-STEP at block e. The β-legs ∏_i β_{μ_i}(σ_i) in
  the row are LOWER-BLOCK objects: the children cross into blocks μ_i < e
  (L4), and their content is consumed at Theorem M2's layer through
  (S1)/(S2) — they are not block-e multi-step events and carry no M1(b)
  obligation (they are excluded from every mass claim by (a″)). The three
  adjustments: the (O2) α-fold folds the one-step recentering TOTAL κ₁ into
  the verdict rows (α_e(σ)·q^{−E} summing to κ₁ over σ, §0 quote) — [REVISION
  3, pass-2 gap 2: the booked event content is claimed only at the TOTAL: the
  σ-sum κ₁ is the mass of the one-step recentering strata. NO row-level
  stratum booking is claimed — under ARBITRARY rational allocation data α_e,
  an individual term α_e(σ)·q^{−E} may be negative or exceed every event mass
  and so is not generally an event mass at all, exactly (a″)'s exclusion; the
  prior sentence "the booked strata are the one-step recentering strata",
  read per-row, is WITHDRAWN. Either way the adjustment carries no MULTI-STEP
  content, which is all (b-EX) certifies]; the (O2′) re-feed leg
  t_σ + κ₁·x_σ: its BOOKED event content is the one-step recentering strata
  (the κ₁ factor — the same strata the (O2) α-fold books, here fed to block
  e's own entrance), while the x_σ FACTOR is a solved-value leg — family-
  (vii) content, EXCLUDED from (b-EX)'s scope by the statement's
  booked/solved-value distinction [REVISION 2, pass-1 critical 2: read
  extensionally, κ₁·x_σ is loop-c followed by the continuation x_σ, whose
  expansion contains non-loop-0-prefix paths such as (loop-c)·(loop-0)·exit
  that NO loop-0-prefix stopping-word set indexes — the r4 sentence declaring
  the leg's content "merely the one-step strata" at extensional scope is
  WITHDRAWN; the value is consumed only through the solve's algebra ((ROW-Σ)
  step 3; M2's (O2′) regrouping), never as a partitioned event — the
  statement's consumer check]; the (O3) adjustment scales each exit row to the booked family
  {(loop-0)^L ∘ F : L ≥ 0} — proper prefixes all-(loop-0), final letter F an
  exit stratum (an exit stratum is not (loop-0): the one-step strata are
  pairwise disjoint and F is not a loop stratum at all) — stopping words.
* **Family (v), scalar atoms.** The atoms book NO events except through
  their occurrences inside (a)/(a′)'s readings — the valuation boxes and
  per-cell singleton-residual events, which are one-step-internal objects
  (sub-events of single strata or ambient boxes, no word structure) — and
  the (O3) exit scalings q^{E_m}/(q^{E_m}−1), whose booked events are
  exactly family (iv)'s (O3) case above (stopping words). The projective
  weight books nothing here ((a′): quantified over nowhere).
* **Families (vi)/(vii), β-legs and solved entries.** These book NO block-e
  continuation events: (a″) excludes them from every mass claim; their
  density reading is Theorem M2's conditional conclusion. (Family (vi)'s
  σ′-aggregates Σ_{σ′} β_{e′}(σ′) are lower-block solved objects — same
  exclusion.) Nothing to certify.

*Every family is walked; every explicitly resummed multi-step term found
((i)'s (O3) kernel; (iv)/(v)'s (O3)-scaled exit rows — the statement's two
classes, exactly) is stopping-word-indexed; every other booked event is
one-step; every solved-value leg ((O2′)'s x_σ, the β-legs, the solves'
recursive x) is excluded from the booked class by the statement's displayed
distinction, and no consumer partitions over it (the statement's consumer
check). Stopping-word sets are prefix-free (displayed in (b-EX)'s
statement), so (b-PF) applies to every multi-step family. ∎*

> **(LOOP-∞) [the infinite loop-0 word — handled SEPARATELY: it is not a
> finite word and is outside (b-PF) as stated (round-1 finding 2)].** Set
> {loop-0 forever} := ⋂_{L≥1} {first L steps = (loop-0)} — measurable (a
> countable intersection of composed preimages of displayed strata under the
> step maps). (i) NULL: its C_e-conditional mass is
> ≤ P(first L steps = (loop-0) | C_e) = κ₀^L = q₀^{−EL} → 0 (the Markov
> property, L2+L3, as in M1(c)'s per-block clause — here at the single
> stratum (loop-0)). (ii) DISJOINT from every finite stopping-word event
> E_w: the word w's final letter, at position |w|, is NOT (loop-0), while on
> {loop-0 forever} step |w| lies in (loop-0); both events constrain step |w|
> through the same composed step maps (they agree on the all-(loop-0) prefix,
> so the composed step-|w| state is the same point on both), and the one-step
> strata at the composed state are pairwise disjoint by (b)'s one-step clause
> — so E_w ∩ {loop-0 forever} = ∅. (iii) THE FINITE-TIME Z_e MEMBERS
> [REVISION 2, pass-1 critical 3: the booked stopping words end in (loop-c)
> or an exit stratum, so a trajectory that executes finitely many (loop-0)
> steps and then lands in Z_e lies in NO E_w and not in {loop-0 forever} —
> the r4 "EXACT" identity omitted these members and is WITHDRAWN at that
> form]. Set Z^{(L)} := {first L steps = (loop-0), the post-L state ∈ Z_e},
> L ≥ 0 (the §2-layer instance of duty-3's clause — the same events
> (O-2)(ii) books into the bad set B_μ at Lemma M2.0's layer). Each Z^{(L)}
> is NULL: conditioned on executing (loop-0)^L the state is again the state
> law (the Markov property, L2+L3, as in (a)'s (O3) bullet) and Z_e is
> Haar-null [L6e(ii)], so P(Z^{(L)} | C_e) = κ₀^L · P(Z_e | C_e) = 0; and
> each is DISJOINT from every E_w, from every Z^{(L′)} (L′ ≠ L), and from
> {loop-0 forever} by the (ii) argument at the first position where the
> constraints differ — Z_e vs (loop-0) vs the word's final letter are
> pairwise disjoint one-step-partition members at the shared composed state
> ((b); on Z_e no further step is defined, consistently with §1). Hence
> (c)'s complement reread decomposes the loop region EXACTLY as
>
>     ⨆_{stopping words w} E_w ⊔ ⨆_{L≥0} Z^{(L)} ⊔ {loop-0 forever},
>
> the last TWO classes null — the countable-additivity computations of (a)'s
> (O3) bullet are unaffected (the added members carry zero mass).

[Scope (pass-2 P6): (b) is a statement about the DISPLAYED strata and the
multi-step events built from them per (b-PF)/(b-EX), plus — at n = 2 — M1′'s one
concrete adjusted row. That every combined/adjusted row of a CONCRETE pack books
such a disjoint union is actual-pack-correspondence content: §5 clause 4, OPEN
(OL-O12-2's data half). It is NOT claimed here. Prefix-freeness for the consumed
row-walks is discharged at (b-PF) + (b-EX) + (LOOP-∞) and NEVER as a
no-hypothesis grammar fact.]

**(c) [positive per-visit termination margin at the density-carrying bookings +
per-block a.s. exit].** The true per-visit PROCESS-continuation mass is the same at
every booking: the loop strata carry κ₀ + κ₁ = q₀^{1−E} [by (a); M1′ — the loop-c
strata continue (rescale, recenter, re-enter the state law) whether or not the
booking's kernel books them]. The (r1) margins (Corollary D) are genuine per-visit
TERMINATION probabilities exactly at the re-entrant density-carrying bookings:

    (O1)  1 − K_e(q₀) = 1 − q₀^{1−E} = Φ^{(O1)}(q₀) ≥ 3/4   [kernel = ALL loop
          strata, so booked exit = true exit],
    (O3)  Φ^{(O3)}(q₀) ≥ 6/7   [kernel = E_rec; up to the (LOOP-∞) null
          classes — {loop-0 forever} AND the finite-time Z_e members
          ⨆_{L≥0} Z^{(L)} (the REVISION-2 display: each null, each disjoint
          from every stopping-word event) — the complement of E_rec in the
          loop region is exactly the scaled exits, so the resummed step
          terminates with probability Φ^{(O3)}(q₀)],

— this restricted reread is the precise sense in which the pole-free theorem speaks
about the real process. At (O2)/(O2′) the booked exit mass 1 − q₀^{−E} ≥ 7/8 is a
BOOKED-ROW mass, NOT a termination probability: the loop-c strata (mass κ₁) sit
outside the κ₀ kernel yet the process demonstrably continues on them (M1′) — the
rev-1 reread at the 7/8 margin was wrong and is withdrawn (pass-1 F2).
Moreover, PER BLOCK the process a.s. exits: by the Markov property (L2+L3, as in
(a)), P(≥ L consecutive loops at a block-e state) = (q₀^{1−E})^L ≤ (1/4)^L → 0
(E = e(e+1)/2 ≥ 3 for e ≥ 2, q₀ ≥ 2), so under any single block's state law the
event "loop at this block forever" is Haar-null. [This is the UNCONDITIONAL half of
the rev-1 termination claim. A.s. termination of the FULL multi-block process needs,
in addition, the hand-off child law — verbatim seam-(S1) content — and is therefore
stated at M2's layer as Lemma M2.0 (§3), where (S1-m) is assumed: the pass-1
CRITICAL repair.]

**(d) [Σ = 1].** For every block state, the one-step strata masses sum to 1: (b)'s
partition + countable additivity + L6e(ii) (Z_e null). [O-12 §8 checked this
symbolically for e ≤ 5 as a declared non-proof corroboration; here it is a theorem of
the same lemma base — the composition O-12's scope fence deliberately did not take.
Scope, displayed: (d) is a Σ = 1 over the ONE-STEP STRATA; it is NOT a member-row
normalization — member verdict rows are family (iv), outside (a) by (a″). The
member-row normalization theorem is (ROW-Σ), §2A.]

*Proof.* Assembled above, clause by clause; every cite is to a proved, verified O-12
§3 lemma, plus ordinary countable additivity and the two measure-preservation lemmas
L2/L3 for the step maps. No engine object, no box, no lift, no ∼_OM class appears. ∎

**Proposition M1′ (the (O2) verdict-row caveat — a finding, not a defect).** Under the
(O2) booking with the concrete allocation datum α₂ = (0, q−1) (the machine-checked
degree-2 table), the adjusted verdict row values ARE masses of displayed events — at
e = 2, the α-adjusted row 1/q − q^{−3} evaluates to q₀^{−1} − q₀^{−3} =
P(descend and not (loop-0) | C_2), the disjoint union of the two pattern strata
(π = {(1,1),(1,1)} and {(2,1)}, total mass q₀^{−1} − q₀^{−2} by Fact F) with the
recentering stratum (κ₁ = q₀^{−2} − q₀^{−3}) — but the booked event is NOT
verdict-constant:
the recentering stratum (mass κ₁) re-enters the full state law (L3) and realizes EVERY
verdict with positive probability. Hence the (O2) booking satisfies M1(a–d) while its
solved values differ from the true conditional densities — exactly the divergence O-12
§2.4 records. Consequence for D-11: the instance pin's target must be (and is) the
re-entrant bookings — the general-n engine is (O1), the sealed degree-3 tables are
(O3) (O-12 §1); the (O2) member is the coarsened n = 2 Lean GATE instance (so
recorded in the N6 harness), not a density carrier. For arbitrary allocation data
α_e ∈ ℚ[q] the adjusted rows need not be masses at all (they can leave [0, 1]); O-12's
theorem quantifies over all of them because its claim is poles/zeros only — MASS-ID
covers exactly the recorded concrete choice.

## 2A. (ROW-Σ) — the member-row normalization theorem (L10; Step 17's layer, M2-free)

[Design §4.2 is the one authoritative home of this obligation's statement,
route, and consumer attribution; this section is its executed write-out, in
this leaf's notation, plus duty-2's consumption edge set (§2A.4). (ROW-Σ) is
proved HERE, at Step 17's layer: its inputs are M1 (this leaf, §2), the
verified O-12 solve displays (§0 quotes), and the member-profile hypothesis
(RE) — NOTHING from ROOT Steps 18/18b, hence non-circular wherever it is
applied at Steps 17/18.]

### 2A.1 Statement

**Theorem (ROW-Σ).** For every 𝔅_n member satisfying (RE) — Theorem M2 (r4)'s
re-entrant booking-profile hypothesis, displayed in full at §3; its three
conjuncts, restated for this section's self-containment: every block booked
(O1)/(O2′)/(O3), never (O2); no allocation datum at any block; family-(iv)
rows = the canonical 0/1-coefficient assembly with the only adjustments the
(O2′) re-feed leg κ₁·x and the (O3) exit-row scaling q^E/(q^E−1). At the
CONSUMED member, (RE) is supplied by ROOT's (H5) hypothesis member
(PACK-iv) — priced, not proved. Then for every prime power q₀ ≥ 2 and every
block e (with E := e(e+1)/2 the block's own exponent):

**(a) [solved-entry normalization]** Σ_σ β_e(σ)(q₀) = 1;

**(b) [per-booking verdict-row totals — three DIFFERENT values]** for
e ∈ {2, …, n}, the aggregated family-(iv) verdict-row total
S_e := Σ_σ row_σ(q₀) equals the block's booked termination margin
(Corollary D's Φ):

    at (O1):   S_e = 1 − κ₀ − κ₁ = 1 − q₀^{1−E}           = Φ^{(O1)};
    at (O2′):  S_e = 1 − κ₀       = 1 − q₀^{−E}            = Φ^{(O2′)};
    at (O3):   S_e = (1 − κ₀ − κ₁)/(1 − κ₀) = 1 − (q₀−1)/(q₀^E − 1) = Φ^{(O3)}.

In particular NO booking's displayed row total is 1; the object normalized to
1 is the SOLVED vector (a). [Honesty over elegance (round-4 finding 2): a
single-total display ("the row total is 1 − κ₀ − κ₁") would be true ONLY at
(O1); the uniform restatement is S_e = Φ^{(booking)}.]

### 2A.2 Proof (induction on e, uniform in q₀ over ALL prime powers — so the induction hypothesis applies at the child pools q₀^{D_i})

**Base e = 1.** Block 1 is the trivial solve: K_1 = 0, E(1) = {1}, a single
decided verdict with β_1 = 1 (O-12 §2.3's row-family preamble; §2's M1 header
"block 1 is trivial"). Σ_σ β_1(σ)(q₀) = 1 at every prime power.

**Step, at block e ∈ {2, …, n} of an (RE) member.** Write

    t_σ := Σ_{F ∈ Exits(e)} m(F) · Σ_{comp_F(σ⃗) = σ} ∏_{i=1}^{r(F)}
           β_{μ_i}(σ_i)(q₀^{D_i})

— the canonical UNADJUSTED assembly over the block's pattern/polygon exit
strata F (the §0 quote of O-12 §2.3(iv); r(F) = |H(F)|, finite; r(F) = 0
terms contribute m(F) at σ = comp_F() — the terminal rows). Two remarks fixing
the quantifier ground: (i) every 𝔅_n member carries the FULL family-(ii)/(iii)
roster by the E(e) definition ("one per pattern π ≠ {(1,e)}", "one per
face-kind and residual-shape vector" — §0 quotes), so Σ_F ranges over ALL of
Exits(e); (ii) by (RE), the member's displayed family-(iv) rows are exactly
this canonical assembly with the only adjustments the (O2′) re-feed leg and
the (O3) exit-row scaling — in every re-entrant booking t_σ ranges over
exactly the exit strata: the recentering strata are NEVER inside t (loop-side
at (O1) and (O3) — inside E_rec — and the separately displayed re-feed leg at
(O2′)). [This is where round-4 finding 2 bit: a route treating the DISPLAYED
row as t_σ in all three bookings is wrong; the three cases are separated at
step 3.]

**1. [telescoping]**

    Σ_σ t_σ = Σ_F m(F) · ∏_{i=1}^{r(F)} [ Σ_{σ_i} β_{μ_i}(σ_i)(q₀^{D_i}) ].

Justification: for fixed F, comp_F is a TOTAL map on verdict tuples (§0
quote: "a map comp_F assigning to EACH tuple (σ_1, …, σ_r) … a verdict of
block e"), so its fibers over σ partition the tuple set: summing the
0/1-selected products over σ recovers the sum over ALL tuples, which factors
as the product of the per-slot sums (finite verdict set, finite slot list —
finite distributivity, no convergence issue). Each slot sum
Σ_{σ_i} β_{μ_i}(σ_i)(q₀^{D_i}) = 1 by the INDUCTION HYPOTHESIS: μ_i < e (L4,
the §0 hand-off-list quote) and q₀^{D_i} is again a prime power ≥ 2 (D_i ≥ 1),
and the induction is uniform in q₀. Hence

    T := Σ_σ t_σ = Σ_F m(F).

**2. [mass input]** By M1(a) families (ii)/(iii) (repaired, §2 — conditional
on C_e), m(F)(q₀) = P(F | C_e) for every pattern/polygon exit stratum F. By
M1(d) (the one-step strata masses sum to 1, conditional on C_e) and the
one-step partition C_e = ⨆_{Λ_e} s ⊔ ⨆_{Exits(e)} F ⊔ Z_e (§1; Z_e null,
L6e(ii)), the exit total is 1 minus the two loop masses:

    T = Σ_F P(F | C_e) = 1 − κ₀ − κ₁.

**3. [the three booking cases, separated]**

* **(O1) [plain].** Displayed row: row_σ = t_σ. Row total:
  **S_e = T = 1 − κ₀ − κ₁ = 1 − q₀^{1−E} = Φ^{(O1)}**. Balance (the §2.4
  display): x_σ = (κ₀ + κ₁)·x_σ + t_σ, i.e. (1 − κ₀ − κ₁)·x_σ = t_σ.
* **(O2′) [the κ₁·x re-feed — the affine fixed point, solved for the
  totals].** Displayed row: row_σ = t_σ + κ₁·x_σ (the re-feed leg books the
  recentering strata; the coefficient is pinned = κ₁ — the §2.4 display
  "x = κ₀x + (κ₁x + s)", quoted in §0; x here is the member's solved vector
  β_e, family (vii)). Balance: x_σ = κ₀·x_σ + row_σ. Sum both displays over
  the finite verdict set (X := Σ_σ x_σ): the balance gives X = κ₀·X + S_e,
  i.e. S_e = (1 − κ₀)·X; the row display gives S_e = T + κ₁·X. Equate:
  (1 − κ₀)·X = T + κ₁·X ⟹ (1 − κ₀ − κ₁)·X = T = 1 − κ₀ − κ₁ [step 2] ⟹
  **X = 1** (the pivot 1 − κ₀ − κ₁ = 1 − q₀^{1−E} ≥ 3/4 > 0 at every prime
  power q₀ ≥ 2, E ≥ 3 — Corollary D), and
  **S_e = T + κ₁ = 1 − κ₀ = 1 − q₀^{−E} = Φ^{(O2′)}**. (The fixed-point solve
  is legitimate: per σ this is exactly O-12 Theorem 2(vii)'s display
  β_e(σ) = u^{−1}·t_σ, u = 1 − q^{1−E} — "NOT a division of a row containing
  β_e"; the row DISPLAY t_σ + κ₁·x_σ is evaluated AT the solved vector.)
* **(O3) [the exit-row scaling — how the factor enters].** Displayed row:
  row_σ = q₀^E/(q₀^E − 1) · t_σ = t_σ/(1 − κ₀) (the family-(v) scaling IS the
  loop-0 resummation Σ_{L≥0} κ₀^L; each exit row books
  {(loop-0)^L ∘ F : L ≥ 0} — §2(a)'s (O3) bullet with (b-PF)/(b-EX)); the
  booked kernel is the resummed E_rec with value κ₁/(1 − κ₀). Balance (the
  §2.4 display): x_σ = (κ₁/(1 − κ₀))·x_σ + t_σ/(1 − κ₀); multiplying by
  (1 − κ₀) ≠ 0: (1 − κ₀)·x_σ = κ₁·x_σ + t_σ ⟹ (1 − κ₀ − κ₁)·x_σ = t_σ. Row
  total: **S_e = T/(1 − κ₀) = (1 − κ₀ − κ₁)/(1 − κ₀) = 1 − κ₁/(1 − κ₀) =
  1 − (q₀−1)/(q₀^E − 1) = Φ^{(O3)}** (κ₁/(1 − κ₀) =
  (q₀−1)q₀^{−E}/(1 − q₀^{−E}) = (q₀−1)/(q₀^E − 1)).

**4. [close]** In all three cases the balance reduces to the SAME solve
(1 − κ₀ − κ₁)·x_σ = t_σ (O-12 Theorem 1(3), verbatim: "the re-entrant three
are all algebraically (1 − κ₀ − κ₁)x = s"), so β_e(σ)(q₀) =
t_σ(q₀)/(1 − κ₀ − κ₁) and

    Σ_σ β_e(σ)(q₀) = T/(1 − κ₀ − κ₁) = 1        [steps 1–2; pivot ≠ 0]

— clause (a); and the three S_e values are clause (b). Cross-check,
displayed: in each booking S_e = (1 − booked kernel)·X with X = Σ_σ β_e = 1
— (O1): (1 − κ₀ − κ₁)·1; (O2′): (1 − κ₀)·1; (O3): (1 − κ₁/(1−κ₀))·1 — the
row total IS the termination margin, per booking. ∎

### 2A.3 What (ROW-Σ) consumes (the non-circularity display)

(RE) — the quantifier's scope (the member's booking + row-form profile; at
the consumed member = (PACK-iv), ROOT (H5), hypothesis-grade); M1(a) families
(ii)/(iii) + M1(d) (repaired, §2 — proved in this leaf over the verified O-12
lemma base); the displayed grammar (finite slot lists, 0/1 comp-selection,
total comp_F — the §0 quotes of O-12 §2.3(iv)); O-12 Theorem 1(3) + Theorem
2(vii) + Corollary D (pivot ≠ 0). NOTHING from ROOT Steps 18/18b — no M2
content, no (S1)/(S2), no classifier object: the theorem is M2-free and
seam-free (its only hypothesis-grade input is (RE) itself).

### 2A.4 The consumption edge set — from the closure walk, COMPLETE RELATIVE TO THE WALKED CORPUS (duty-2; replaces every "untraced cover / possibly within 17/18" clause; completeness grade + residue (EDGE-RES) displayed below per REVISION 2, pass-1 gap 6)

**Method (what makes this walk a closure walk rather than a sample; its
completeness GRADE is the REVISION-2 display below).** A
consumption edge of (ROW-Σ) is a proof line, on the mandatory
Step-17/18/18b/19 path, that uses a MEMBER-ROW NORMALIZATION — a total
Σ_σ β_e(σ) = 1, a verdict-row total S_e, or any Σ over the verdict index of
member rows (as opposed to: per-entry values, kernel scalars, and the
ONE-STEP-strata Σ = 1 of M1(d), which are not member-row totals). The walk
starts at the four displayed consumption legs of ROOT REVISION 9's Steps
18/18b/19 plus Step 17's own delivery display, and expands EVERY invoked
statement and ITS hypotheses to the leaves, quoting each operative clause.
The corpus walked: ROOT Steps 17/18/18b/19 (REVISION 9, read this session);
M04 §§2–5 in full (Theorems 1/2/4, Corollary 1.1, Lemmas 3.1/3.2/3.3, the
K1–K4 kernel statements — read this session); O-11 rev 3 §3.3 (K2's displayed
discharge, as quoted at the design (†18)); O-1thr Theorem 3 (= D-15); this
leaf's §§2–4 (M1/M2/M3 and their proofs). The (†18) trace's three
non-exhaustiveness defects (round-3 finding 3: the K1 row's untraced Step-17
inputs; the Theorem-4 row's untraced Lemma-3.1/3.2/Bekić/blockSolve/Rsh/
rsh_interp hypotheses; ellipsed cells) are each closed below.

**Completeness grade [REVISION 2, pass-1 gap 6 — displayed, not
overclaimed].** The walk's completeness is CORPUS-RELATIVE: relative to the
named corpus above and to the operative clauses quoted per site. This leaf
does not reproduce the walked proof bodies in full and no mechanically
checkable dependency inventory exists on file, so "no further edge exists"
is NOT re-derivable from this leaf's quotations alone. The residue is ONE
displayed audit premise, **(EDGE-RES)**: no UNQUOTED line of the walked
corpus consumes a member-row normalization. Its discharge is
adjudication-by-inspection of the named corpus (the owed pass; and any
future revision touching those files re-opens it). Until then, the edge set
below is the walk's finding AT THE QUOTED OPERATIVE CLAUSES, each of which
is the site's displayed proof line for the consumption in question.

**The walk, site by site (operative clauses quoted; verdict per site).**

| # | site (consuming proof line) | operative clause(s), quoted | member-row-normalization content |
|---|---|---|---|
| 1 | Step 18 K1 (M04 §5.5 (K1)) + Lemma 3.3 + its CUT-WD hypothesis ((H8)) + the TREE-EXP/(SIB)/(JC-multi) kernels + the (I-τ)/K-LOC (H7) leg + CU leaf labels ((H6)/(FRESH)) | "each complete finite realizable canonical tree's fiber mass factors as the grammar weight of its derivation…; the type-σ corpus decomposes along shallow shapes as in Lemma 3.3"; Lemma 3.3: "Regroup along the bijection; for fixed Ŝ group tuples by their verdict tuple and factor (Tonelli in [0,∞])" | **NONE.** Every sum is over derivations, shapes, or verdict TUPLES in [0,∞] (Tonelli); no sum over the verdict index σ of member rows; the (H6)/(H7)/(H8) hypotheses are product/ownership/label laws, Σ_σ-free |
| 2 | Step 18 K2 (M04 §5.5 (K2)) + its displayed discharge (O-11 rev 3 §3.3) | "the active one-step matrix A(q₀) satisfies A(q₀)^k·𝟙 → 0"; discharge: "O-12 Theorem 1 + Corollary D …: every block kernel substochastic with positive termination margin at every prime power q₀ ≥ 2 …, hence ρ(A) < 1 and det(I−A) ≠ 0"; O-12 Theorem 4's note: "the row 'sum' is the scalar kernel value" | **NONE.** The only row-sum object is the KERNEL row sum — the scalar loop mass, bounded AWAY from 1 (SUB-stochasticity, Corollary D content — the opposite of a Σ = 1 law); not (ROW-Σ) |
| 3 | Step 18 K3 (M04 §5.5 (K3)) + (PACK-iii) wsh_ok + evalAt + D-6 | "the numeric grammar weights equal the evaluations at the pools of the symbolic ℚ(q) entries K_e, b^{term}, J, W_Ŝ"; "every g ∈ E(e) is defined at q₀" | **NONE.** Per-entry value correctness + definedness; no totals |
| 4 | Step 18 K4 (M04 §5.5 (K4)) + D-15 (O-1thr Theorem 3(b)/(c)) | "`bridgeMass` T … equals μ(fiber T) — level-exact cylinder reading; plus (ns-null)"; D-15: "#Φ_N(T) = mass(T)·p^{nN}"; "D_σ(N) = Σ_{T : type(T)=σ, thr(T)≤N} mass(T)·p^{nN}, a finite sum" | **NONE.** Per-tree count laws + a PER-σ count; nothing summed over σ, nothing totals 1 |
| 5 | the M04 Theorem-4 walk itself: Lemma 3.1 (lfp), Bekić, Lemma 3.2 (Neumann), `blockSolve`, `Rsh`, `rsh_interp` (PACK-i), `r_bdd` (PACK-ii) — the round-3 UNTRACED hypotheses, now walked | Lemma 3.1's proof: sums over DERIVATIONS (Kleene iterates; "regrouping the [0,∞]-valued sum along this bijection … is Tonelli"); Lemma 3.2's hypothesis: "Suppose all smaller-block values entering block e's split outcomes at q₀ are finite" — ENTRYWISE finiteness, discharged inductively by Lemma 3.2(ii) itself ((I − A)^{−1}c entrywise finite at smaller blocks; base blocks have finite exit vectors — finite rosters) + K2, never a total; Bekić: structural triangularity, no numeric hypothesis; Theorem 4's proof line: "K3 identifies this … with the evaluation at q₀ of `blockSolve` — the evaluation passes through the finite sums/products by the ring-hom property of `evalAt` …; by `rsh_interp` … that is Rval(vmap σ)(p) ∈ [0,1] (`r_bdd`)" — per-σ, per-entry | **NONE.** M04's own bookkeeping note: "the [0,∞] bookkeeping: no convergence hypothesis is consumed anywhere before K2 enters"; the solve needs ρ(A) < 1 (K2) and entrywise finiteness — never a row total; rsh_interp/r_bdd are per-entry laws |
| 6 | Step 18b (Theorem M2 (r4), §3 below — its FULL input inventory, this leaf's own proof) | inputs, each displayed at §3: (RE) row form; M1(a) families (ii)/(iii) ("each m(F)(q₀) = P(F | C_e)"); (S1)+(S2); the block induction; O-12 Theorem 1(3)/2(vii); Corollary D pivot; the exhaustiveness note ← M1(b)/(d) + (b-PF)/(b-EX)/(LOOP-∞) | **exactly ONE Σ = 1, and it is NOT a member-row total:** M1(d)'s ONE-STEP-STRATA theorem (proved, §2), consumed at the renewal-equation exhaustiveness note. The balance equation is a row IDENTITY (s_member = s_true), the pivot a nonvanishing; no line of M2's proof sums t_σ or β-legs over σ (M2 DERIVES the per-σ identity; the normalization Σ_σ β = 1 is a COROLLARY via (ROW-Σ), not an input) |
| 7 | Step 19's squeeze + the Σ_τ R_τ = 1 conclusion | ROOT REVISION 9 (verbatim, read this session): "Σ_τ R_τ = 1, concluded HERE [REVISION 3, finding B3]: Step 1 supplies Σ_τ α(n,τ;p) = 1 at every p plus the interpolation principle; Steps 18 + 18b supply α(n,τ;p) = R_τ(p) at every p …; hence Σ_τ R_τ agrees with 1 at infinitely many prime powers, and Σ_τ R_τ = 1 in ℚ(t)" | **NONE table-side.** The total is a CONCLUSION from Step 1 (the true densities partition) + interpolation — not a consumed table-side normalization |
| 8 | Step 17's OWN displays (this leaf; ROOT's Step 17 statement/delivery lines) | this leaf: M1 (a)-(d) (one-step scope displayed at (d)); M3 (labels — no totals); M1′ (one concrete row value); ROOT r9's Step 17 Statement fragment: "Σ = 1 across each row now a THEOREM" | **ONE display-level edge, the ONLY one found:** ROOT r9's Step-17 Statement fragment advertises a member-row Σ = 1 in the tranche. At r9 that fragment is UNSUPPORTED by rev 3 (M1(d) is one-step-only; family (iv) is excluded by (a″)) — (ROW-Σ) is exactly the theorem that makes the r11-rescoped fragment true at (RE)/(PACK-iv) members (Unit B edit E1 rewrites it: "member-row normalization at (PACK-iv) members = (ROW-Σ), a PROVED lemma …"). This is a DELIVERY display, not a consuming proof line; no downstream proof line reads it as an input (sites 1–7) |

**The complete edge set (the duty-2 deliverable).**

* **Consuming proof lines on the mandatory Step-17/18/18b/19 path: NONE at
  any quoted operative clause** (under (EDGE-RES) for the unquoted remainder
  of the walked bodies — the REVISION-2 completeness grade above). Every
  candidate site (1)–(7) is walked to its hypotheses and certified
  member-row-normalization-free by its quoted operative clause.
* **Display/delivery edges: exactly ONE** — ROOT Step 17's Statement fragment
  (site 8), whose r11 form (Unit B, E1) records (ROW-Σ) as the proved lemma
  behind it. Through this edge and ONLY this edge, (ROW-Σ)'s (RE) hypothesis
  — i.e. (PACK-iv) at the consumed member — enters Step 17's tranche; the
  other (PACK-iv) edge is Step 18b's DIRECT (RE) instantiation (site 6, §3).
  So (PACK-iv)'s consumption set is CLOSED: {Step 18b direct} ∪ {the Step-17
  E1 delivery display of (ROW-Σ)} — a known two-element set, replacing
  "possibly within 17/18".
* **Off-path adjacency, fenced (the one row-normalization-shaped object the
  walk found anywhere in the corpus):** M04 Theorem 2 — its proof sums over
  σ twice: the box partition "Σ_σ decided_σ(N) + undec(N) = p^{nN}" (a
  counting tautology of any Option-valued map) and the chain-side total
  "Σ_σ Rval(vmap σ)(p) = … = decidedTotal p (`rs1_equates`) = 1 (`x3_total`)"
  — the latter IS a solved-total normalization of (ROW-Σ)(a)'s genre at the
  top assembly. Theorem 2 is consumed by NO mandatory step: M04 Theorem 4's
  proof does not invoke it (site 5's quotes are its full proof line); ROOT
  Step 18's displayed statement is the per-τ tie only; Step 19 re-derives the
  total independently (site 7). FENCE: if any future revision routes M04
  Theorem 2 (or the chain law `x3_total`) onto the mandatory path, that edge
  consumes (ROW-Σ)(a) + (PACK-iv) and must be priced then — it is named here
  so it cannot enter silently.

**What survives of the design's cover language.** The former "any
member-row-normalization input the trace missed is covered by (ROW-Σ),
possibly within 17/18" is REPLACED by the enumeration above PLUS the single
displayed residue (EDGE-RES) — an adjudication-by-inspection premise against
a NAMED corpus, no longer an untraced cover clause [REVISION 2, pass-1 gap
6: "nothing conditional" overstated the leaf's own quotations]. (ROW-Σ)
stands PROVED (this section) and DELIVERED at Step 17 (the E1 display, Unit B
— executed only after this leaf's r4 pass accepts §2A.2 as a proof); its
consumption edge set is the set displayed, closed relative to (EDGE-RES);
the walk's corpus and method are on file for the hostile pass to audit at
exactly that grade.

## 3. Theorem M2 (the solve identification — the abstract solved values ARE the real conditional densities; the exact seams AND the (RE) member profile displayed)

**The three seams (the ONLY unproved inputs; owners named; per-consumer scope
displayed).** [Also consumed, NOT a seam: the O-12 §2.4 booking displays / Theorem
1(3) balance-equation forms — brief-internal displayed algebra, verified with the
brief, named in §0 per pass-1 F6(ii); they enter the (O2′) and (O3) legs below.]
[Also displayed, NOT counted a seam: **(DET)**, Lemma M2.0's structural reading of
§1's model (ONE Haar draw pushed through displayed measurable maps) — a definitional
stance on the semantic model, not an unproved mathematical statement; its
honest-widening fallback under a stochastic reading is displayed in-lemma (pass-2
P1).]
* **(S1) Hand-off faithfulness.** Conditioned on a hand-off stratum F with hand-off
  list ((μ_1, D_1), …, (μ_r, D_r)): the r handed-off sub-objects are INDEPENDENT, the
  i-th distributed per block μ_i's state law over the base-changed field (Haar-C_{μ_i}
  over the degree-D_i unramified extension, residue field F_{q₀^{D_i}}), and the
  parent's splitting type is a function of the children's types (+ the terminal m = 1
  atoms' fixed data). Status: PROVED for the within-block legs — the loop hand-off IS
  L2+L3, so (O1)/(O2′)/(O3)'s kernels carry no seam; OPEN for the cross-block legs:
  the pattern/polygon residual descent (the "standard Montes/Okutsu residual descent"
  O-12 §2.2 expressly fences out) and the root split (HEN-LIFT + joint law of the
  Hensel factors). Owner: **GD-3 FULL form** (the value side; O-9 OL-B — "one proof
  should serve both") + LIT-3/(HEN-LIFT) at the root. Proved partials on file: M08
  Theorem 2 (level-1 census, 46 strata machine-checked); B-4/M6 empirical through OM
  order 4 (GD-3 row, tree §D2.2).
* **(S2) Composition-datum correctness.** The member's comp_F equals the TRUE type
  composition (types of coprime factors aggregate as multiset union; a child type over
  the degree-D unramified extension contributes through the fixed base-change law).
  Status: OPEN as a general statement; table data per member. Owner: **O-11** (value
  correctness; T-7s carries the concrete tables and their obligations ledger).
* **(S3) Projective assembly weight.** The mass reading of the family-(v) entry
  (q−1)/(q^{n+1}−1) at the root/projective assembly layer — that it is the correct
  projective normalization tying the monic root read to the projective ensemble.
  Status: OPEN in this unit (nothing here derives it). Owner: **annex node A-1**
  (the monic → projective bridge, June lemma M1; PROVED-UNVERIFIED at the tree,
  out of the critical path by standing directive). Scope: consumed ONLY by the
  projective reading of M2's conclusion — M2 as stated (monic ensemble) does not
  consume it; the N2 falsifier's projective configurations exercise it numerically.

**Lemma M2.0 (a.s. termination of the full process — conditional exactly on the
displayed clause (S1-m), under the displayed structural reading (DET)).** [r4
rewrite per design §3.4, pass-3 finding 5: rev 3's addresses recorded only
(hand-off stratum, child slot) pairs, so its induction equation
"{node a reached} = {node a′ reached} ∩ {x_{a′} ∈ F}" was false whenever loop
steps precede the hand-off. The r4 addresses are LOOP-EXTENDED; the transport
runs one letter at a time; the local obligations (O-1)–(O-5) and the global
(O-GLOB) are stated and proved below, with duty-3's Z_μ clause.]

*The consumed inputs, displayed:*
* **(DET)** [the structural reading of §1's model — a premise, displayed, not
  hidden]: the semantic process is ONE Haar draw pushed through displayed measurable
  maps — the within-block step is the deterministic map g ↦ ĥ = h(c̃ + y) (§1), and
  each hand-off child is a measurable function T_{F,i} of the parent state on its
  stratum F (the child IS the displayed cluster/factor of the parent polynomial; the
  model carries no randomness beyond the initial draw). This is how §1 / O-12 §2.2
  define the model, and it is the reading under which L2/L3 are stated (they are
  distributional statements about exactly these deterministic maps).
* **(S1-m)** [of (S1), ONLY the child-marginal clause, read under the CANONICAL
  parent law]: for each block e′, each hand-off stratum F ⊆ C_{e′} with child list
  ((μ_1, D_1), …, (μ_r, D_r)), and each slot i, the pushforward under T_{F,i} of the
  normalized restriction of Haar-C_{e′} to F is Haar-C_{μ_i} over the degree-D_i base
  change — or merely ABSOLUTELY CONTINUOUS w.r.t. it, which is all the proof uses.
  [(S1)'s independence and type-composition clauses are NOT consumed. The ROOT leg
  of (S1-m) — the top-layer read spawns FINITELY many top blocks with entrance laws
  ≪ their blocks' state laws — is the ROOT-FINITENESS premise, consumed at (G-b)'s
  base and (G-d)'s close (round-5 finding 6: inventoried).]
* M1(c)'s per-block clause; M1(d) + L6e(ii) (the Z_μ nullity — duty-3's input); L4;
  measurability of the displayed strata and step maps (the L2/L3/L6 layer).

*Statement.* Under (DET) and (S1-m), the descent process from any block state (any
block, any prime power) terminates almost surely: for Haar-a.e. input, every
realized node of the block tree is exited after finitely many within-block steps,
the realized block tree has finitely many nodes, and the full multi-block
trajectory terminates.

*Proof — the loop-extended addresses, the five local obligations, and the global
induction, each displayed.*

**Addresses (loop-extended; replaces rev 3's step 1).** A node ADDRESS is a
finite word

    a = ((w_1, F_1, i_1), …, (w_k, F_k, i_k)),

where w_j is a finite word over the LOOP ALPHABET Λ (of the block reached
after j−1 hand-offs, at its composed pool), F_j a hand-off stratum of that
block, and i_j a child slot of F_j (a position in F_j's finite hand-off list
H(F_j)). The address records the FULL within-block loop history before each
hand-off — the rev-3 omission. R_a := {node a reached} (the root address ∅
per top block: R = the top-block entrance event); x_a := the node state
(a measurable function of the initial draw where defined, by (DET));
μ(a) := the node's block. For a node a and a loop word w over Λ_{μ(a)},
"{w then F at a}" := {x_a executes w and its post-w state lies in F} (§1's
trajectory notation; all pre-exit states in loop strata by definition).

**(O-1) [the child-reached identity — the disjoint/countable-union display].**
For a node a′ and a child slot (F, i), F ∈ Exits(μ(a′)) a hand-off stratum:

    {child (F, i) of a′ reached} = ⨆_{w ∈ Λ_{μ(a′)}^{<ω}} (R_{a′} ∩ {w then F at a′})

— a COUNTABLE union, PAIRWISE DISJOINT over the loop words w, the SAME event
for EVERY slot i of F.
*Proof.* (Exactness of the union.) Under (DET), the within-block trajectory
at a′ is the deterministic iteration of §1: it remains within-block exactly
while its states lie in loop strata, and the child (F, i) is born exactly
when the FIRST non-loop state exists and lies in F (§1 / O-12 §2.2: children
are born at hand-off exits; on Z_μ and at terminating exits no child is
born). So {child (F, i) reached} = {∃ finite t: x_{a′}^{(s)} ∈ loop strata
for s < t, x_{a′}^{(t)} ∈ F} ∩ R_{a′}; indexing by the executed loop word
w (= the sequence of loop strata visited, length t) gives the displayed
union EXACTLY — not up to null. Every slot of F indexes a factor-child of
the SAME exit event: the factor-children of one exit are simultaneous, not
alternatives — one event per F, shared by its slots. (Disjointness.) For
w ≠ w′, the words w·F and w′·F are prefix-free: if one were a proper prefix
of the other, the shorter word's final letter F (an exit stratum) would
equal a LOOP letter of the longer word's loop part — impossible, the strata
being distinct members of the one-step partition; (b-PF) (with (b)'s
one-step clause over the full mixed alphabet Λ ∪ Exits) gives
E_{w·F} ∩ E_{w′·F} = ∅. (Countability.) Λ_{μ(a′)} is finite (§1: q₀^D
letters at the node's pool), so there are countably many finite words. ∎
[This identity REPLACES the rev-3 false one-letter induction equation.]

**(O-2) [the never-exits event is null — eternal loops AND the finite-time
Z_μ encounters (duty-3)].** Fix a block μ and its pool q. Define the BAD SET
of the block's state space,

    B_μ := N_μ ⊔ ⨆_{w ∈ Λ_μ^{<ω}} Z_μ^{(w)},   where
    N_μ := {x ∈ C_μ : the within-block trajectory of x never leaves the loop
            strata} = ⋂_L {first L steps in loop strata},
    Z_μ^{(w)} := {x ∈ C_μ : x executes w and its post-w state lies in Z_μ}.

(i) **[eternal loop]** Haar-C_μ(N_μ) = 0: N_μ ⊆ {first L steps in loop
strata} for every L, whose Haar-C_μ mass is (κ₀ + κ₁)^L = (q^{1−E_μ})^L ≤
(1/4)^L → 0 by the Markov property (L2+L3, as in M1(c)'s per-block clause),
at every prime power q including the base-changed q₀^{D} pools; block 1
terminates immediately (§2: K_1 = 0), so N_1 = ∅.
(ii) **[Z_μ encounter after a finite loop history — the duty-3 clause; NEW,
absent from every earlier revision]** Haar-C_μ(Z_μ^{(w)}) = 0 for every
finite loop word w: by (O-4)'s canonical clause below, under Haar-C_μ
conditioned on {x executes w} the post-w state is AGAIN Haar-C_μ (|w|-fold
L2+L3); Z_μ is Haar-C_μ-null (L6e(ii) — M1(d)'s null remainder); so
Haar-C_μ(Z_μ^{(w)}) = Haar-C_μ({executes w}) · Haar-C_μ(Z_μ) = 0. [When
Haar-C_μ({executes w}) = 0 the product reading is unnecessary — the set is
null outright as a subset of a null event.]
(iii) **[assembly]** B_μ is a countable union of Haar-C_μ-null sets, hence
Haar-C_μ-NULL; and the disjoint decomposition displayed is EXHAUSTIVE of
"never exits": pointwise, for x ∈ C_μ, either some first non-loop time t
exists — and then x^{(t)} ∈ ⨆Exits(μ) ⊔ Z_μ (the one-step partition, §1),
i.e. x exits (∉ B_μ-content) or x ∈ Z_μ^{(w)} at w = the loop history — or
no such t exists and x ∈ N_μ. So

    {x ∈ C_μ : x never lands in an exit stratum} = B_μ,  Haar-C_μ(B_μ) = 0.

**(O-3) [measurability].** The displayed strata are Borel events of the
coefficient space (valuation conditions [L1], reduction conditions [L3],
polygon/shape conditions [L6a/L6c/L6d] — the verified lemma statements are
about exactly these events); Z_μ is displayed Borel ((C_μ\R_μ) ∩ {a_e = 0});
the loop step maps g ↦ ĥ = h(c̃ + y) are polynomial in the coefficients on
their strata, hence Borel measurable, and the hand-off maps T_{F,i} are
measurable on their strata ((DET)'s display); finite compositions of
measurable maps on measurable domains are measurable. Consequently every
event used in (O-1)/(O-2)/(O-5) — {executes w}, {w then F}, N_μ, Z_μ^{(w)},
R_a — is a finite or countable Boolean combination of composed preimages of
displayed strata under these maps: measurable. [One sentence per the design:
the composition argument is applied finitely often per word, countably many
words.]

**(O-4) [preservation of laws along loop words — the two-clause transport].**
Fix a block μ at pool q and a finite loop word w over Λ_μ.
(canonical clause) Under Haar-C_μ, conditioned on {x executes w} (mass
∏_{j=1}^{|w|} P(s_j | C_μ) = q^{−E_μ·|w|}, factored per letter by the Markov
property — each FIXED loop letter s_j has mass q^{−E_μ}; positive [REVISION 2,
pass-1 gap 4: the r4 parenthetical "(κ₀ + κ₁)^{|w|}" is the mass of "some loop
letter at each of the first |w| steps" — the sum over all q^{|w|} loop words —
not the fixed word w's mass, and is WITHDRAWN; only positivity is consumed
downstream, so the transport argument is unchanged]), the post-w state law IS
Haar-C_μ exactly. *Proof:* induction on |w|; empty word trivial. w = s·w′:
conditioned on {x^{(0)} ∈ s}, the law of x^{(1)} is Haar-C_μ — verbatim
L2+L3 ("conditioned on the stratum, ĥ is AGAIN the state law"). The residual
event {x^{(1)} executes w′} is a measurable function of x^{(1)} alone
((DET): deterministic iteration), so conditioning further on it and pushing
to the post-w′ state of x^{(1)} is the induction hypothesis at w′ under the
canonical law. ∎
(≪ clause) If a law ν on C_μ is ≪ Haar-C_μ, then for every letter s ∈ Λ_μ
and Haar-C_μ-null N ⊆ C_μ: the set s ∩ step^{−1}(N) is Haar-C_μ-null (by the
canonical clause at the one-letter word s, the pushforward of the normalized
restriction Haar-C_μ|_s puts mass Haar-C_μ(N) = 0 on N), hence ν-null; so
under ν conditioned on {x ∈ s} (when positive) the post-letter law is again
≪ Haar-C_μ. Letter-by-letter along w: the post-w law under ν (conditioned on
executing w, when positive) is ≪ Haar-C_μ. ∎ [This is the loop case of the
two-letter transport, consuming ONLY the PROVED L2+L3 — the exact reason the
billing does not widen beyond (S1-m)+(DET).]

**(O-5) [the corrected local exit partition — hand-off AND terminating
exits, plus the null member].** At every node a′ (block μ := μ(a′)):

    R_{a′} = ⨆_{w ∈ Λ_μ^{<ω}} ⨆_{F ∈ Exits(μ)} (R_{a′} ∩ {w then F at a′})
             ⊔ NE_{a′},
    NE_{a′} := R_{a′} ∩ {the within-block trajectory at a′ never lands in an
               exit stratum},

with F ranging over ALL displayed exit strata of the block — the HAND-OFF
strata (children born in blocks μ_i < μ; child slots (F, i)) AND the
TERMINATING exits (verdict/leaf rows with no child slot) — and NE_{a′} NULL
whenever P(R_{a′}) is defined (proved at (G-c) via (O-2) + (G-b); for the
partition itself no mass input is needed).
*Proof.* (Disjointness) across pairs (w, F): the words w·F are prefix-free
(the (O-1) argument, over the full exit roster: distinct F at equal w differ
at the final letter, distinct one-step strata; distinct w gives
prefix-freeness as in (O-1)) — (b-PF). (Exhaustion) pointwise, exactly
(O-2)(iii)'s trichotomy applied to x_{a′}: a first non-loop time in an exit
stratum (the (w, F) member with w = the loop history), or a Z_μ-encounter /
eternal loop (the NE member — NE_{a′} = R_{a′} ∩ {x_{a′} ∈ B_μ} by
(O-2)(iii) and (DET)). (Countability) as in (O-1). ∎
The induction step consumes EXACTLY this corrected form, in three displayed
uses: (i) the child-reached identity (O-1) is the sub-union at the HAND-OFF
pairs — for each hand-off F, the event ⨆_w (R_{a′} ∩ {w then F}) IS
{child (F, i) of a′ reached} for EVERY slot i of F (the factor-children of
one exit are simultaneous, not alternatives — ONE event per F, shared by its
slots); (ii) the TERMINATING-exit members are consumed as the per-node
terminal/leaf masses — they spawn no child and no further induction step;
(iii) the null member NE_{a′} is discarded — this is where M1(c)'s per-block
bound AND L6e(ii)'s Z_μ nullity, transported by ≪, are spent.

**(O-GLOB) [the global multi-block a.s. termination — the four-part
induction, with duty-3's enlarged null set].**

* **(G-a) [the address set is countable — with the branching input made
  explicit].** Depth: k < n by STRICT BLOCK DESCENT (children born in blocks
  μ_i < μ — L4; block sizes in {1, …, n}, and block 1 spawns no children).
  Per node: the loop alphabet is FINITE (§1), so the words w are countably
  many; the exit strata F are countably many (patterns finite; polygon
  strata = face-kinds × shape vectors × depth cells — finite × finite ×
  countable, L6b's cone); the slots i per F are FINITELY many — the slot
  list of each hand-off stratum is finite BY THE DISPLAYED GRAMMAR: H(F) =
  ((μ_1, D_1), …, (μ_r, D_r)) is a finite list and every family-(iv) row is
  "a finite sum of products" ∏_{i=1}^{r} over F's slots (the §0 quotes of
  O-12 §2.3(iv)). Countable unions of countable sets along depth < n: the
  address set 𝒜 is countable. [Descent alone bounds DEPTH, never node
  count — the finite-slot display is the branching bound the earlier route
  omitted (round-4 finding 5(α)).]
* **(G-b) [root-to-leaf transport — induction on address length,
  conditional on reached].** For every a ∈ 𝒜 with P(R_a) > 0: the
  R_a-conditioned law of the node state x_a is ≪ Haar-C_{μ(a)} (over the
  node's base-changed pool). Base: the ROOT nodes — the top-layer read
  spawns FINITELY many top blocks (finitely many patterns π, finitely many
  atoms per pattern) with entrance laws delivered at the root seam: (S1-m)'s
  root leg gives each top cluster's law ≪ Haar-C_{μ} — the ROOT-FINITENESS
  premise, displayed in (S1-m)'s bullet above. Step a′ → a = a′·(w, F, i):
  R_a = R_{a′} ∩ {w then F at a′} ((O-1) — the SAME conditioning event for
  EVERY slot i of F), and the child-state law is the HAND-OFF letter applied
  after the |w| LOOP letters: by the induction hypothesis the R_{a′}-law of
  x_{a′} is ≪ Haar-C_μ; by (O-4)'s ≪ clause, after conditioning on the loop
  word w the post-w law is ≪ Haar-C_μ; conditioning further on the post-w
  state lying in F (positive mass on R_a, else P(R_a) = 0 and nothing to
  prove) and pushing through T_{F,i} is the rev-3 kernel-null-set step,
  retained verbatim at the single hand-off letter: for Haar-null
  N ⊆ C_{μ(a)}, (S1-m) gives Haar-C_μ(F ∩ T_{F,i}^{−1}(N)) = 0; the ≪ law
  kills it; divide by P(post-w ∈ F | ·) > 0. Hence the R_a-law of
  x_a = T_{F,i}(post-w state) is ≪ Haar-C_{μ(a)}. ∎ [Sibling slots CONDITION
  ON ONE SHARED EVENT; nothing is multiplied and no independence is
  claimed.]
* **(G-c) [the null union runs PER ADDRESS — and, inside each address, PER
  TIME (duty-3)].** N_a := NE_a = R_a ∩ {x_a ∈ B_{μ(a)}} — the node-a
  never-exits event ((O-5)/(O-2)(iii)), which DECOMPOSES per time:
  N_a = (R_a ∩ {x_a ∈ N_μ}) ⊔ ⨆_w (R_a ∩ {x_a ∈ Z_μ^{(w)}}) — the eternal
  loop and the per-loop-word Z_μ encounters. P(N_a) = 0 for every a ∈ 𝒜:
  trivially when P(R_a) = 0 (N_a ⊆ R_a); else by (G-b) + (O-2): B_{μ(a)} is
  Haar-C_μ-null and null sets pull back through ≪. The global exceptional
  set is

      𝒩 := ⋃_{a ∈ 𝒜} N_a
         = ⋃_{a ∈ 𝒜} [ (R_a ∩ {x_a ∈ N_{μ(a)}}) ∪ ⋃_{w} (R_a ∩ {x_a ∈ Z_{μ(a)}^{(w)}}) ]

  — countable by (G-a) (+ countably many w per address), each member null,
  hence NULL. [Two displays, kept distinct (round-4 finding 5(β)): this
  union's index set is the ADDRESS SET (× times) — sibling slots contribute
  DISTINCT N_a's at their own addresses, every one included; the "one event
  per parent exit" convention applies to (O-5)'s MASS decomposition of
  R_{a′} (where sibling slots genuinely share one event per (w, F)), NEVER
  to this union. And duty-3's display: the per-address/per-time Z_μ events
  are INSIDE each N_a via B_μ's countable per-word union — a finite-history
  entry into Z_μ is in 𝒩, not outside it.]
* **(G-d) [off 𝒩 the realized tree is finite — strong induction on block
  size, pointwise].** Fix an input g ∉ 𝒩. At any node a that g realizes
  (g ∈ R_a): g ∉ N_a, so the within-block trajectory at a is NOT in
  B_{μ(a)} — pointwise, by (O-2)(iii)'s trichotomy, a FIRST non-loop time t
  exists and the state there lies in an EXIT stratum (the Z_μ branch and the
  eternal-loop branch are both excluded, g ∉ N_a — this is exactly where
  duty-3's enlarged 𝒩 is spent: without the Z_μ clause, a finite-history
  Z_μ-encounter would falsify the exit claim), through exactly ONE exit
  stratum F (the one-step strata are pairwise disjoint at the composed
  state, (b)). If F is a TERMINATING exit, a is a leaf of g's realized
  tree. If F is a HAND-OFF, it spawns the finitely many slot-children of F
  ((G-a)'s finite-slot display), each of block size μ_i < μ(a) (L4); by
  STRONG INDUCTION on block size, each child's realized subtree is finite;
  hence a's realized subtree is finite (one node + finitely many finite
  subtrees). Applied at the FINITELY many root nodes (the root-finiteness
  premise, (G-b) base): g's realized tree is finite, every realized node is
  exited after finitely many within-block steps, and the full multi-block
  trajectory terminates. ∎

**What (O-GLOB) consumes (inventory, complete):** (O-1)–(O-5); M1(c)'s
per-block clause + L6e(ii)/M1(d)'s Z_μ nullity (through (O-2)); the displayed
finite-slot grammar (O-12 §2.3(iv), §0 quotes); countable additivity; the
ROOT-FINITENESS premise ((S1-m)'s root leg — finitely many top blocks with
≪ entrance laws), consumed at (G-b)'s base and at (G-d)'s final application;
(DET) throughout (trajectories and children as measurable functions of the
one draw). NOTHING of M2. **Billing, borne out as claimed:** the loop legs
consume only the PROVED L2/L3 ((O-4)); (O-1)/(O-3) are structural;
(O-2)/(O-5) consume M1(c) + L6e(ii) + the ≪-induction; the hand-off letters
consume (S1-m). Lemma M2.0's ledger line remains (S1-m)+(DET) — the
design-level claim is now the displayed proof's actual input set. ∎ (Lemma
M2.0)

*Honest billing widening (displayed per the pass-2 charge: prove the minimal
dependence or widen).* The proof consumes (S1-m) AND the structural reading (DET).
If a future (S1) discharge realizes the hand-off as a genuinely STOCHASTIC kernel
(randomness beyond the parent state), (DET) fails, the composed-preimage
reading fails with it, and — as pass 2 observed — the bare marginal clause no longer
suffices: a parent law ≪ Haar does not transport through an arbitrary kernel pinned
only in Haar-average. The billing then widens to **(S1-m+)**: the child's law
conditioned on the FULL revealed path to its birth is ≪ its block's Haar-C law —
prefix-conditional (S1) content (the seam's intended Markov reading), NOT derivable
from (S1-m) alone. Either way M2.0 consumes strictly less than full (S1); the
minimal displayed form is (S1-m) + (DET).

**Theorem M2 (r4).** Let the member satisfy:

> **(RE) [re-entrant booking profile — a CONJUNCTION; no clause here is
> derived from another (the displays establish only (O2) ⟹ carries-α, so no
> "equivalently" is available); (RE) states everything the proof uses].**
> EVERY block e ∈ {2, …, n} of the member is booked (O1), (O2′), or (O3) —
> never (O2); AND the member carries NO allocation datum at any block; AND
> its family-(iv) verdict rows at every block are the canonical
> 0/1-coefficient assembly row_σ(F) = m(F)·Σ_{comp_F(σ⃗)=σ} ∏_i
> β_{μ_i}(σ_i)(q^{D_i}), aggregated t_σ = Σ_F row_σ(F), with the only booking
> adjustments the (O2′) re-feed leg κ₁·x (coefficient pinned = κ₁) and the
> (O3) exit-row scaling q^{E}/(q^{E}−1). [Arbitrary-α members are EXCLUDED —
> they are exactly M1(a″)/M1′'s exclusion, and for them only O-12's pole/zero
> theorem speaks; the (O2) member is a gate object, not a density carrier
> (M1′).]

Assume further (S1) and (S2) for the member. Then for every prime power
q₀ = p^δ and every block e:

    β_e(σ)(q₀) = P( splitting type = σ | the block-e state law at q₀ ),   ∀σ,

i.e. the abstract solved values ARE the real conditional densities; and the top-layer
assembly (root read + entrance weights) identifies the solved final densities with the
true unconditional splitting-type densities of Haar-random monic degree-n f over O.
[MONIC scope. The PROJECTIVE reading of this conclusion is additionally conditional
on (S3) — the projective assembly weight's mass reading, owner A-1.] [Top layer
(pass-2 P4): the identified object is the DISPLAYED root read (R-read) — proof, root
layer below. That a concrete pack's rows equal the member's — root/entrance rows
equal to (R-read) entry-for-entry, block rows equal to the member's — is clause-4
content (§5 clause 4, OPEN; ROOT prices it as (H5)) and is NOT claimed here.]

**At the CONSUMED instance (the r4 cross-reference, design L5; the rev-3
sentence pinning the intended member's bookings from O-12 §1 is REPLACED by
this display).** That the member ROOT actually consumes at Steps 18b/17
satisfies (RE) — its per-block booking profile and row forms — is NOT
discharged in this leaf and is NOT derivable from the O-12 §1 sentence naming
the project's intended bookings (O-12's own scope declaration makes the
sealed-table and general-n identifications interface assumptions, OL-O12-2;
and no-α does not imply the profile — the displays give only
(O2) ⟹ carries-α, never the converse). It is clause-4/(H5)-class content,
priced at ROOT as the NEW (H5) member **(PACK-iv)** (design §3.5), with its
retirement routes displayed there ((route-a): the OL-O12-2 transcription pass
extended by the per-row booking-profile duty at n = 3; (route-b): the O-14d
(O1)-booked construction at general n). Proposition M1′'s "instance pin"
sentence is a statement of this leaf's own chain — recorded as MOTIVATION for
targeting the re-entrant bookings, never as evidence of pack-to-member
conformance.

*Proof.* Induction on e (well-founded: hand-offs go to blocks μ < e by L4; base e = 1:
a linear factor, the type atom is forced, β_1 = 1 = the displayed E(1)). Let
β*_e(σ) := P(type = σ | C_e) — well-defined on the separable full-measure locus. One
step of 𝒟_e, using M1(b)'s partition and M1(d):

    β*_e(σ) = P(loop | C_e)·P(type σ | loop) + Σ_{exit strata F} P(F | C_e)·P(type σ | F).

On EVERY loop stratum — loop-0 AND each loop-c — the transformed state is again the
state law with the SAME type [L2+L3 — proved, no seam; L3's type-preservation is
what makes this hold at the recentering strata too], so P(type σ | loop-0) =
P(type σ | loop-c) = β*_e(σ) — the same unknown. On a genuine exit stratum F,
(S1)+(S2) give P(type σ | F) = Σ_{comp_F(σ⃗)=σ} ∏_i β*_{μ_i}(σ_i at
q₀^{D_i}); by the induction hypothesis (every lower block is also
re-entrant-booked under (RE)) each β*_{μ_i}(· at q₀^{D_i}) equals the
abstract β_{μ_i}(·)(q₀^{D_i}) — the (g5) β-legs of E(e). [The repaired step,
design §3.3 — replaces rev 3's "With M1(a) (each P(F) is the abstract mass
entry at q₀)":] By (RE) and the verified grammar display (O-12 §2.3(iv),
quoted in §0), the member's exit/verdict rows at block e are the canonical
assembly: each exit stratum F contributes m(F) times the 0/1
comp_F-selected products of β-legs, with no other scalars. By M1(a) families
(ii)/(iii) — in scope after the r3 rescope; family (iv) is NOT cited — each
m(F)(q₀) = P(F | C_e). Hence the member's abstract exit row equals the true
exit row, s_member(q₀) = s_true(q₀), and the semantic one-step identity is
the member's balance equation:

    x = (κ₀ + κ₁)·x + s(q₀),      x := β*_e(σ) coordinate-wise,

with s the true exit row (x-free in block e: every genuine exit hands off to blocks
μ < e by L4, values supplied by the IH). This IS the member's balance equation at
each re-entrant booking [pass-1 F3 repair — the (O2′) leg derived, not asserted]:
  * **(O1)**: kernel = κ₀ + κ₁, t = s — literally the display above.
  * **(O2′)**: kernel = κ₀ only, but the member's loop-c rows are BLOCK-e β-LEGS by
    construction [O-12 §2.4: "the κ₁ strata booked as exit legs that RE-FEED block
    e's own entrance", t = κ₁x + s], so the member's equation x = κ₀x + (κ₁x + s)
    is the same identity regrouped.
  * **(O3)**: the resummed form (1 − κ₁/(1−κ₀))x = s/(1−κ₀) [O-12 §2.4]; multiplying
    by 1 − κ₀ ≠ 0 recovers the display.
By O-12 Theorem 1(3), all three are algebraically (1 − κ₀ − κ₁)x = s, i.e.
(1 − q₀^{1−E})x = s(q₀); the folded pivot 1 − q₀^{1−E} = Φ^{(O1)}(q₀) ≥ 3/4 is
nonzero at every prime power [Corollary D / (r1)], so the equation has a UNIQUE
solution; the abstract common re-entrant solve β_e(q₀) = s(q₀)/(1 − q₀^{1−E}) is
one, hence β*_e = β_e(q₀). [Exhaustiveness of this conditioning: the one-step
strata partition C_e up to the displayed null sets — M1(b)/(d) — with the
multi-step booked families (the (O3) resummations) disjoint and exhaustive per
(b-PF)/(b-EX)/(LOOP-∞) (§2; proved in this leaf, adjudication at this file's
owed pass) — and NOT Lemma M2.0: pass-2 P8 corrected rev 2's misattribution;
the renewal equation and its unique solution consume no termination
statement. M2.0's role is the OPERATIONAL reading of the conclusion — a.s.
every draw's descent terminates, so the solved densities describe an
a.s.-decided process — billed on its own (S1-m)+(DET) line in §7.]

**Root layer (pass-2 P4: displayed, replacing rev 2's one-sentence assertion).** The
top state is Haar on monic degree-n f — NO C-conditioning; the root read precedes
any descent. Define the DISPLAYED root read: for each divisor pattern π of total
degree n with atoms (D_j, m_j) (the m_j = 1 atoms decided and absorbed into the root
composition datum comp^root_π as fixed data, exactly as in §2.3(iv)),

    (R-read)   d(σ)(q) := Σ_π N_π(q)·q^{−n} ·
                  Σ_{comp^root_π(σ⃗)=σ} ∏_{j : m_j ≥ 2} β_{m_j}(σ_j)(q^{D_j})

— the root analogue of the §2.3(iv) rows with pattern mass N_π(q)·q^{−n} in place of
m(π) (no q^{−(E−e)} factor: nothing is conditioned at the top).
* **(R-b/R-d)** [the root analogue of M1(b)/(d) — unconditional over the same lemma
  base]: the events {f̄ has pattern π} PARTITION the top state space exactly (unique
  factorization over F_{q₀}: every monic residual has exactly one divisor pattern —
  at this layer not even a null set is excluded), with P(f̄ has pattern π) =
  N_π(q₀)·q₀^{−n} [L3's uniform-reduction clause applied at degree n — its verbatim
  statement (§0) is for Haar-uniform monic of arbitrary degree e, and the top state
  is exactly that at e := n — plus Fact F(ii) for the count], and
  Σ_π N_π(q₀)·q₀^{−n} = 1 [the N_π count a partition of the q₀^n monic residuals].
* **(R-balance)** [conditional on (S1)'s ROOT leg + (S2)]: on {pattern π}, the
  m_j ≥ 2 atoms' clusters are the root hand-offs; (S1)'s root leg (HEN-LIFT + the
  joint law of the Hensel factors) gives cluster j the block-m_j state law over the
  degree-D_j unramified extension, and (S2)'s root composition datum aggregates the
  cluster types (+ the decided atoms) to the type of f, so P(type σ | pattern π) =
  Σ_{comp^root_π(σ⃗)=σ} ∏_{j : m_j ≥ 2} β*_{m_j}(σ_j at q₀^{D_j}); the block theorem above
  supplies β*_{m_j}(· at q₀^{D_j}) = β_{m_j}(·)(q₀^{D_j}). Total probability over
  (R-b)'s partition: the true unconditional splitting-type density of Haar-random
  monic degree-n f equals (R-read) at q₀. ∎

[What the root layer does NOT settle — displayed, not hidden (pass-2 P3/P4): that a
CONCRETE pack's root/entrance rows equal (R-read) entry-for-entry is the actual-pack
correspondence — §5 clause 4, OPEN, OL-O12-2's data half. M2's top layer identifies
the DISPLAYED assembly with the true densities; it certifies no sealed table.]

**Where the pole-free theorem is consumed — the displayed answer to the unit charge:**
O-12's (r1) is the UNIQUENESS PIVOT (the folded re-entrant pivot 1 − q₀^{1−E} =
Φ^{(O1)}(q₀) ≠ 0 — Corollary D — pins the semantic density vector as THE solution of
the abstract equation, at every prime power including wild); O-12's margins are the
TERMINATION supply (M1(c) per-visit at (O1)/(O3) + per-block; Lemma M2.0 for the
multi-block composition, on (S1-m)+(DET) via the r4 obligations (O-1)–(O-5) +
(O-GLOB)); O-12's (r2) is the definedness of every evaluated entry the induction
touches. That is the identification through which the pole-free theorem "speaks
about the real densities": without M1/M2 it is a theorem about rational functions;
with them it is a theorem about the descent process's decided masses.

## 4. Theorem M3 (row labels are true types; the T-3/T-6 event tie — the audit-priced clause)

**(a) [per-box σ-constancy — what D-11 needs where verdicts meet boxes; r4
restated at the consumed premise's own scope (pass-3 finding 8)].** Let T be a
decided ns-free tree with threshold t = thr(T) fibered by a box x. Then the verdict
class is an EXACT finite union of level-t cylinders with the fiber scaling law and
per-box tree uniqueness [D-15 = O-1thr Theorem 3(a)–(c)], and every monic separable
lift g of x has true splitting type equal to the tree's verdict multiset typemult(T)
[O5triple Theorem E via CU-7's corrected attribution]. So the verdict σ labeling a
decided continuation/termination event is the true splitting type of EVERY MONIC
SEPARABLE polynomial of the event's cylinders — lift uniformity per box at Theorem
E's own scope. Mass-sufficiency note: the nonseparable locus is Haar-null (T-6
(ns-null), VERIFIED-DUAL), so every mass/counting consumption of these labels (Step
18's K1/K4 interfaces) is unaffected; pointwise all-lifts separability at a counted
box is NOT claimed here — where a counting consumer needs it, it is supplied by that
consumer's own decided-ns-free certificate apparatus (O-1thr §1.4's fiber clauses;
D-15's vocabulary), not by this clause.
Conditionality (inherited, displayed): P(O5t r3) + (I-1)–(I-3) as served
by CU-7 (hence P(O-2a r5), P(CU-1 r2) = V29 through CU-4's leg) + the five O5triple
flags (GD-1/GD-7/GD-8 duties) + P(O-1thr r4) + TB-CAP's D-12r perimeter + (E-N)
displayed OPEN (it touches which N a box may be counted at, not the constancy) —
PLUS, for the routing of these labels into the COUNTED corpus, the (H9) = (NSF)
price displayed below.
**Constructive re-confirmation of the CU_cluster §6.1 audit:** the clause consumed is
single-cylinder lift uniformity; no ∼_OM pooling, no OB2, no BNS object appears — the
O3design row "D-11 ← OB2" stays unwarranted.

**The two consumption channels (what M3(a) does and does not supply at K1 —
displayed so the a.e./exact distinction cannot blur again; design §2.5):**
* **(ch-1) K1's EXACT combinatorics** — the cylinder classification, the
  finite-level counts, Lemma 3.3's weight-preserving regrouping — consumes
  D-15 + the K1 kernels AT decided ns-free trees and is
  LABEL-SEMANTICS-BLIND: both sides are keyed by the trees' RECORDED verdict
  multisets; no statement on this channel reads the true type of any
  polynomial. M3 is not consumed here, so its separable-lift scope cannot
  injure the exact content.
* **(ch-2) the SEMANTIC tie** (recorded labels = true types) — where M3(a)
  enters (via Steps 11/12 → Step 17): consumed MASS-MEDIATED. Each counted
  box's true-σ locus is the box minus its nonseparable locus (M3(a) at
  separable scope, applicable AT THE (H9) PRICE — (NSF-INT) for the
  pin → corpus chain, (NSF-KEY) for the as-built keying), of full box mass by
  T-6 (ns-null) — exactly what the Step-19 squeeze and Step 18b's density
  reading consume. No mandatory consumer reads a true type at an individual
  nonseparable lift. [The channel-coverage CLOSURE audit — that every
  mandatory M3(a) consumption factors through (ch-1) or (ch-2) — is executed
  at §4A; its outcome gates the (NS-ALL) contingency.]

**The (H9) = (NSF) price (displayed here because M3(a)'s labels enter the
counted corpus only through it; the ROW is ROOT's, added at Unit B edit E5 —
this leaf DISPLAYS it and proves what it can, §4B).** The TARGET statement:
*at any instantiation satisfying the interface pin (q-4), every K1/K4-labeled
box lies in the decided-ns-free counted corpus and D-15's exact-classification
package applies with no a.e. weakening.* The round-3 attempt to PROVE this by
chaining displayed quotes was refuted (the chain imports exactly the two
unquoted implications below) and stands WITHDRAWN; the two members:

* **(NSF-INT) — the interface implication chain (pin → predicate → corpus).**
  At every consumed site of Step 18's K1/K4 chain: (i) irr/hen-terminal
  completeness excludes ns-marks — a complete finite realizable tree with all
  leaves (τ-irr)/(τ-hen) carries no ns-marks (in particular the Hensel
  payload carries none) — so every box labeled under (q-4)'s pin fibers a
  tree that is decided ns-free in (q-1)'s sense; and (ii) M04's
  "complete-finite-realizable" scope coincides with the guarded TREE-N corpus
  ((q-3)'s all-leaves-(τ-irr)/(τ-hen), `hdet`-guarded trees) at every
  consumed site. The quotes (q-1)–(q-6) below are CONTEXT, not proof: none
  displays (i) (ns-marks are an INDEPENDENT conjunct of (q-1)'s "decided
  ns-free"; no quote says marks occur only at ns-halts) nor (ii) ((q-3)
  reports both scopes without equating them). NAMED RETIREMENT ROUTE — the
  ONLY one: THE LEAF PROOF AT §4B (artifact I-1, proved against the I-2 site
  list closed by §4A's walk; after REVISION 2 it carries TWO displayed
  premises — the marks-vocabulary premise for (i) and (K1-CANON) for (ii)'s
  K1 leg, pass-1 gap 5), RETIRED only at gate I-3 PLUS a separate discharge
  of (K1-CANON) [REVISION 3, pass-2 gap 3: I-3 = this file's owed hostile
  pass accepting §4B AS A PROOF — but §4B is a CONDITIONAL proof, so I-3's
  accept verifies only (K1-CANON) ⇒ the K1 scope conclusion and cannot
  itself establish (K1-CANON); full retirement additionally requires
  (K1-CANON)'s own discharge (the M04-side pin display or the
  interface-lemma form — external to this leaf), adjudicated at or after
  I-3]. Until BOTH, (NSF-INT) stands PRICED;
  riders unchanged: (E-N) — whether thr(T) ≤ N in D_σ(N) is redundant is
  OPEN at O-1thr rev 4; the numerical decided_σ(N) = D_σ(N) tie is the
  count_tie/finiteness_stack row's content, at its own home.
* **(NSF-KEY) — as-built keying conformance [ONE proposition].** At the wired
  instance: for every n, p, level N, box f, and σ — if the wired classifier
  labels f (`bridgeCanonical … N f = some σ`) then f fibers a carried tree of
  the σ-carrier satisfying `MovesT.NsFree` (no ns-marked chain) at threshold
  ≤ N. **KNOWN FALSE AT HEAD** — the M02 record, re-verified against Lean
  HEAD this session (§0): the carrier `bridgeTree n p σ = {V : MovesT.VTree p
  (ZMod p) // V.typemult = σ.1 ∧ RealizedSelf n p V}` carries NO ns-free
  conjunct (`BridgeD3_treeCarrier.lean` 94–95); `MovesT.NsFree` (`Defs.lean`
  1534) is conjoined nowhere in it; (†7) `bridgeCanonical` labels any box
  fibering a carried tree — and M02 §2 exhibits the poison: the exact-square
  family's V₀ (ns-marked, self-realized) "∈ bridgeTree n p σ₀ … and — this
  is the poison — the classifier decides exact squares, with verdict
  'totally split'"; c_{V₀}(N) = p^{N−2} against (SCALE)'s forced
  p²-per-level growth — `count_tie` as typed REFUTED, `canonical_stable`
  refuted at the intended instance, `VPSound` unsatisfiable. NAMED
  RETIREMENT ROUTE: the design's Unit C per (NSF-KEY-RET) — the sanctioned
  `MovesT.NsFree` conjunct on `bridgeTree` (M02's "Minimal repair", proposed
  NOT executed; statement-fence sign-off required), after which the pin
  holds BY CONSTRUCTION of (†7) and exact squares drain into the undecided
  envelope — exactly (q-6)'s intended drainage. NOT retired by anything in
  this leaf; the two members retire SEPARATELY.

**The displayed definitions (CONTEXT, not proof — verbatim; what the
interface displays, kept so the priced members are checkable against their
sources: O-1thr §1.4/§6, M04 §§2–3/5.5, M02, Lean HEAD).**
* **(q-1) [the bundled predicate — O-1thr §1.4]:** "**decided ns-free** :=
  every maximal chain is a (τ-irr) leaf and the Hensel payload covers the
  simple part — no ns-marks"; and the ns-halt is a MARKED halt: "(τ-ns) —
  write **NsHalts(o, f)** := o is a REALIZED non-saturated state whose window
  has no principal face on f — the ns-halt, MARKED". (Classification trees
  carry "leaf verdicts, and ns-marks" in the vocabulary.)
* **(q-2) [the counted corpus — O-1thr Theorem 3(c)]:** "D_σ(N) := #{x
  level-N : fiber(T, x) for some decided ns-free T with type(T) = σ and
  thr(T) ≤ N}" — decided-ns-free-quantified IN ITS DISPLAYED DEFINITION, and
  the whole package scoped there: "Throughout: T decided ns-free, t :=
  thr(T), fibers per §1.4" (Theorem 3's opening line).
* **(q-3) [the K1/K4 corpus quantifier — the TREE-N/TREE-EXP source scope,
  quoted on file at M02]:** "The note's TREE-N … is stated for trees
  'complete finite realizable with all leaves (τ-irr)/(τ-hen)' — the ns
  leaves are excluded; the machine-checked discharge candidate `treeN`
  (E11_treeN.lean) carries `hdet : ∀ H ∈ Tr.chains, ¬ Tr.nsLeaf H`" —
  re-verified at HEAD this session (lines 48/118; `treeN`'s body passes
  `hdet` into `treeExp`, line 68). M04's K1 quantifies over "each complete
  finite realizable canonical tree" (= this source scope) and its K4 reads
  "`bridgeMass` T … equals μ(fiber T) — level-exact cylinder reading; plus
  (ns-null) excluding infinite trees".
* **(q-4) [the interface pin — the ClassifierSpec docstring,
  `MovesU/Defs.lean`]:** "`canonical`: T_can^τ's verdict-type map … **`some
  σ` = complete finite realizable tree, all leaves (τ-irr)/(τ-hen),
  certified at level N (thr ≤ N)**"; and the counts are DEFINED from it
  ("decided_σ(N) — DEFINED from the canonical map (never a free field)").
* **(q-5) [what the partition identity actually is — M04 Theorem 2, proof
  step (a)]:** "At each level N the verdict map f ↦ canonical N f partitions
  the p^{nN}-element box into the σ-fibers and the none-fiber:
  Σ_σ decided_σ(N) + undec(N) = p^{nN}" — TRUE FOR ANY Option-valued map; it
  routes NOTHING. The routing content lives in (q-4), not in the identity.
* **(q-6) [the intended drainage — O-1thr Remark 4]:** "The ns-free
  hypothesis is NOT removable: an exact-square family (n = 2: classes of
  (X − b)², v_p(b) = 1 held to level N) fibers an ns-marked tree whose
  counts scale by p, not p² — the countermodel is PARI-verified in the
  source campaign. Undecided/ns loci drain into the undecided envelope
  instead; their treatment is the drainage node, not this package."

**(b) [the classifier tie — M1's events ARE the T-3/T-6 continuation events; r4
in-statement conditionality (pass-3 finding 7)].** Under the clause-4
correspondence (OPEN — §5 clause 4 = OL-O12-2's data half; ROOT prices it as
(H5)/(PACK)), M1/M2's identities attach to the ACTUAL tables' values;
unconditionally, M1/M2 identify entries of a stipulated 𝔅_n member and the
displayed (R-read) with masses of SEMANTIC strata (root valuations, reductions
of rescales, Newton polygons, residual shapes) — and this clause ties those
semantic strata to the classifier vocabulary:
D-11's node text speaks of "the T-3/T-6 continuation events" — the branch-tree
events of T_can(f) (T-3's per-shape-prefix mass law; T-6's assembly/TB-CAP). The
tie is the engine-read seam: the machine's window polygon = the Newton polygon
vertex-for-vertex and the emitted records are the GMN data [GD-2], the residual
reads are the residual shapes [GD-3-min], every emitted record is its chain's GMN
datum [CU-2 no-phantom] and every Cons_f chain is realized [CU-1 no-missing].
Under this dictionary the semantic strata of §1 are read by the engine as its
continuation events, and M1's identities become D-11's literal clause
("kernel entries are the Haar volumes of the T-3/T-6 continuation events") — at
the conditionality of this clause's opening sentence.
Conditionality: the GD-1..GD-6 residue per the CU §0 registry (heads GD23 = V28,
CU-1 = V29) + P(O-2a r5) + P(O-1thr r4). Nothing here is new mathematics — it is the
same composition CU-1/CU-2 already price; this clause only ATTACHES the mass
identities to the classifier vocabulary.

## 4A. (CH-COV) — the channel-coverage closure audit (L11)

**The obligation (design §2.5).** A DEPENDENCY-CLOSURE audit establishing that
every mandatory consumption of M3(a)'s conclusion on the Step-17/18/18b/19
path factors through (ch-1) or (ch-2) — walked to closure (each consuming
step's displayed proof line, its invoked leaf statements, and THEIR
hypotheses), not sampled. Outcome pricing, in advance: a consumer needing
all-lifts separability FROM M3(a) at boxes not definitionally ns-free — a
consumer OUTSIDE both channels — fires the contingency (NS-ALL) (priced as an
explicit open lemma per the design §6) and returns the design for
re-adjudication.

**Method / closure criterion.** M3(a)'s conclusion has TWO delivery edges at
the ROOT REVISION 9 lines quoted here (read this session): Step 12 Delivers —
"correct semantic leaf labels for O-11's K1 (Step 18) and D-11's M3 (Step
17); type(g) = typemult(T) for every lift g" — and Step 17 Delivers — "the
pack-side mass laws + TRUE ROW LABELS that Step 18's K1/K2 consume". [Two
REVISION-2 displays on these deliveries, pass-1 gap 6: (α) the Step-12
delivery phrase "for every lift g" is BROADER than M3(a)'s repaired
separable-lift conclusion — the deliverable is "every MONIC SEPARABLE lift
g" plus the ns-null mass-sufficiency note; the rescope is an owed ROOT/Unit-B
edit, recorded at REVISION 2 as **E-M3** (no ROOT edit rides this leaf), and
no mandatory consumer reads the unrescoped breadth (sites 1–6 below are
M3-free/label-blind; site 7 consumes mass-mediated at separable scope); (β)
that these two are the ONLY r9 delivery edges is part of the corpus-
inspection residue (CH-RES) below, not established by the quotations alone.]
The walk follows every consumption edge out of these deliveries on the
mandatory path (Steps 18 K1–K4, 18b, 19), expands each invoked statement's
displayed hypotheses to the leaves, and classifies each site: M3-FREE (the
line reads no true type of any polynomial), ch-1, ch-2, or OUTSIDE (fires
(NS-ALL)). Closure grade [REVISION 2, pass-1 gap 6 — the same grade as
§2A.4's]: every invoked statement in the table is expanded to a quoted
operative clause certified M3-free or channel-classified; but the leaf's
quotations do not reproduce the walked proof bodies in full, so "no further
consuming line" carries the displayed audit premise **(CH-RES)**: no
UNQUOTED line of the walked corpus reads M3(a)'s semantic content outside
(ch-1)/(ch-2), and no r9 line beyond the two quoted delivers it. Discharge =
adjudication-by-inspection against the named corpus (the owed pass), as the
Honesty bracket below records.

| # | site (consuming line) | invoked statements, expanded | classification |
|---|---|---|---|
| 1 | Step 18 K1 — "K1 ← O-10 (guarded) + (I-τ)→GD [(H7)] + CU leaf labels [(H6)/(FRESH), the A-5 route leg]" | M04 §5.5 (K1): fiber-mass factorization + Lemma 3.3 decomposition — keyed by "the type-σ corpus" = trees of RECORDED type σ (type(T) = the multiset of leaf + Hensel verdicts, §1.4 — recorded data); Lemma 3.3's hypothesis (the (H8) cut bijection) — shapes/derivations/weights, no polynomial's true type; the TREE-EXP/(SIB)/(JC-multi) kernels — product laws over recorded verdict data; the (H6)/(H7) legs — terminal-seam/interface conformance of RECORDS | **ch-1 (M3-free at every expanded line):** label-semantics-blind; no line reads a true type. M3's labels ride INTO K1's corpus as recorded data; their SEMANTIC correctness is not consumed here |
| 2 | Step 18 K2 — spectral escape | A(q₀)^k·𝟙 → 0; discharge O-12 Thm 1 + Cor D + D-6 — kernel masses and margins | **M3-FREE** (no labels at all) |
| 3 | Step 18 K3 — value correctness | symbolic-entry evaluation = numeric weights; wsh_ok/evalAt memberships; O-12 (r2) definedness | **M3-FREE** (no labels) |
| 4 | Step 18 K4 — "K4 ← Dfloor/D-15/CU mass semantics" | M04 §5.5 (K4): `bridgeMass` T = μ(fiber T) + (ns-null); D-15 = O-1thr Theorem 3(b)/(c): per-tree cylinder/count laws, "Throughout: T decided ns-free" — counts of BOXES fibering trees, keyed by recorded type; (ns-null) excludes infinite trees by MASS | **ch-1 (M3-free):** counting laws over recorded data at ns-free trees; no true-type reading. [The ns-free SCOPING of this site is (NSF-INT)(ii)'s content — priced at (H9), proved at §4B] |
| 5 | Step 18's Theorem-4 walk (Lemmas 3.1/3.2, Bekić, blockSolve, Rsh, rsh_interp, r_bdd) | expanded at §2A.4 site 5 — resummation algebra in [0,∞], per-entry value laws | **M3-FREE** (no labels) |
| 6 | Step 18b (Theorem M2 r4, §3) | M2's proof inputs (§2A.4 site 6): (RE), M1(a)(ii)/(iii), M1(d), (S1)/(S2), the solve displays. M2's β* := P(type = σ | C_e) is SEMANTIC-side (true types by definition); no classifier label appears in M2's proof — M3 is NOT invoked | **M3-FREE as a proof** (the semantic density is defined, not read off labels). The recorded-vs-true MEETING is deferred to Step 19's squeeze — site 7 |
| 7 | Step 19's squeeze — "decided_τ(k)/p^{nk} → R_τ(p) = α(n,τ;p)"; the bracket at every finite level (Step 1, SEMANTIC, unconditional) + drainage (Step 13) + the evaluation chain (Steps 18 + 18b) | the squeeze compares CLASSIFIER counts (decided_τ — recorded labels) with the TRUE density α: label soundness is consumed here, MASS-MEDIATED — each counted box's true-σ locus is the box minus its nonseparable locus (M3(a) at separable scope, at the (H9) price), of FULL box mass by T-6 (ns-null); Step 1's bracket legs and Step 13's drainage are mass/count statements; no leg reads a true type at an individual nonseparable lift | **ch-2 (the ONLY consumption of M3(a)'s semantic content on the path):** mass-mediated exactly as the channel display specifies |

**Outcome: CLEAN at the walked closure.** Every mandatory consumption of
M3(a) at the quoted operative clauses of the
Step-17/18/18b/19 path factors through (ch-1) (sites 1/4 — no semantic
consumption) or (ch-2) (site 7 — mass-mediated at the (H9) price); sites
2/3/5/6 are M3-free. NO consumer outside both channels was found at any
expanded line; **the contingency (NS-ALL) does NOT fire on this walk.**
[Honesty: this outcome is an executed audit artifact at the REVISION-2
closure grade — corpus-relative, residue (CH-RES) — adjudicated by this
file's owed hostile pass — until that pass
accepts it, N2's BLOCKED-PENDING clause carries "(CH-COV) closure audit"
unchanged.] **The I-2 site list, CLOSED by this walk (relative to
(CH-RES)):** the sites at which
(NSF-INT)(ii)'s scope equivalence must hold are exactly K1 (M04 §5.5 (K1) +
Lemma 3.3) and K4 (M04 §5.5 (K4) + D-15 = O-1thr Theorem 3(b)/(c)); the walk
surfaced NO further M3(a)/corpus-consuming site (site 7 consumes the corpus
through K1/K4's counts, adding no new corpus quantifier).

## 4B. (NSF-INT) leaf proof (artifact I-1, per the design's (NSF-INT-RET); proved against the I-2 site list closed at §4A)

[Retirement bookkeeping, displayed first: this section is (NSF-INT-RET)'s
artifact **I-1** — "the implication-chain write-out inside the Unit-A L7
deliverable, in a section titled '(NSF-INT) leaf proof'". The site list it is
proved against is **I-2** = {K1 (M04 §5.5 (K1) + Lemma 3.3), K4 (M04 §5.5
(K4) + D-15 = O-1thr Theorem 3(b)/(c))}, IDENTICAL to §4A's closed list (the
walk surfaced no further corpus-consuming site — closure relative to §4A's
displayed (CH-RES) residue, REVISION 2) — the identity display
(NSF-INT-RET) I-2 requires. Gate **I-3** = this file's owed hostile pass
accepting this section AS A PROOF; and [REVISION 3, pass-2 gap 3] since the
section is a CONDITIONAL proof, I-3's accept verifies only the implication
under the two displayed premises — retirement additionally requires a
separate discharge of (K1-CANON) (leaf-external; routes at the closing
paragraph). Until I-3 AND that discharge, (NSF-INT) stands PRICED at
(H9) and nothing below is billed as retired. REVISION 2: the section now
carries TWO displayed premises — the §1.4 marks-vocabulary premise (claim
(i)) and (K1-CANON) (claim (ii)'s K1 leg; pass-1 gap 5) — both of the priced
member's grade.]

**Claim (i) [irr/hen-terminal completeness excludes ns-marks].** Let T be a
classification tree in O-1thr §1.4's sense (a finite prefix-closed set of
chains per track, a Hensel payload, leaf verdicts, and ns-marks) that is
REALIZED by some f — its chains are all and only the realized pruned runs on
f, maximal-chain kinds and verdicts match its records, interior chains do not
halt on f, and its Hensel payload equals the simple-factor data of f̄ (the
fiber clauses (F1)–(F4) at any box realizing T) — and suppose every maximal
chain of T is a (τ-irr) leaf. Then no state of T realizes an ns-halt on f,
T's ns-mark field is empty, the Hensel payload carries no ns-marks, and —
with (F3) supplying "the payload covers the simple part" — T is **decided
ns-free** in (q-1)'s sense. Consequently every box labeled under (q-4)'s pin
("some σ = complete finite realizable tree, all leaves (τ-irr)/(τ-hen),
certified at level N") fibers a decided-ns-free tree — the pin routes into
(q-2)'s counted corpus.

*The one vocabulary premise, displayed (this is the reading the proof
consumes; it is §1.4's SOLE displayed generation rule for marks).* §1.4's
halt taxonomy introduces ns-marks in exactly one place: "(τ-ns) — write
NsHalts(o, f) := o is a REALIZED non-saturated state whose window has no
principal face on f — the ns-halt, MARKED." A mark is the RECORD of an
NsHalts event; no other display of §1.4 (states, windows, candidate children,
accumulated invariants, the (τ-irr)/(τ-hen) halts, the track partition, the
tree datum, the fiber clauses) generates or permits a mark. [On a purely
abstract-data reading — marks as an unconstrained field on the tree datum —
NO theorem could force the field empty; it is exactly this vocabulary tie
that the round-3 refutation identified as unquoted, which is why (i) is
(NSF-INT)'s priced content and why this proof retires only at gate I-3.]

*Proof of (i).* Marks record NsHalts events (the premise), and NsHalts(o, f)
requires o to be (α) REALIZED on f, (β) NON-SATURATED, and (γ) principal-face-
free (its window has no principal face on f). Walk every state of T — by
(F1) exactly the realized pruned runs on f:
1. **Interior states.** An interior chain o of T has a child in T. By §1.4's
   candidate-children display, children exist only as pairs "(principal face
   of the window polygon, monic irreducible factor ψ of that face's residual
   polynomial)" — so o's window HAS a principal face on f, and (γ) fails:
   ¬NsHalts(o, f). [(F4) — "interior chains do not halt on f" — states the
   same conclusion directly; both routes are displayed so the proof does not
   rest on the fiber clause alone.]
2. **Maximal chains.** By hypothesis every maximal chain H is a (τ-irr)
   leaf, i.e. IrrHalts(H) holds — by §1.4's key-degree identity this is
   EQUIVALENT to μ_{k−1} = 1 ("window width one"), the SATURATION condition
   ("the machine has NO children at saturated states, by definition"). So H
   is saturated and (β) fails: ¬NsHalts(H, f).
3. **The Hensel payload.** Its members are (1, g) verdicts "certified from
   the mod-p data" of the simple part ((τ-hen), §1.4) — they are not STATES
   of any track, and NsHalts is a predicate of states: no mark can attach
   there. [This is the "in particular the Hensel payload carries no
   ns-marks" sub-clause.]
So no state of T realizes an NsHalts event on f; by the vocabulary premise
the mark field records nothing — empty. The other two conjuncts of (q-1)'s
"decided ns-free" hold by hypothesis (all maximal chains (τ-irr) leaves) and
by (F3) (the payload equals the simple-factor data — it covers the simple
part). Hence T is decided ns-free. For the pin corollary: a box labeled
`some σ` under (q-4) fibers, by the pin's displayed clause, a complete
finite realizable tree with all leaves (τ-irr)/(τ-hen) certified at level N
— realizable = fibered by the box with the (F1)–(F4) clauses, so the claim
applies and the fibered tree is decided ns-free with thr ≤ N: the box lies
in (q-2)'s counted corpus D_σ(N). ∎

**Claim (ii) [scope coincidence at the consumed sites — site-by-site over
I-2].**
* **Site K1 (M04 §5.5 (K1) + Lemma 3.3) [REWRITTEN at REVISION 2 — pass-1
  gap 5].** K1's quantifier reads "each complete finite realizable canonical
  tree". The r4 argument that this bare phrase "carries the leaf clause FROM
  ITS SOURCES" (K1's bracket pins its content sources — "[= TREE-EXP + (SIB)
  + (JC-multi) + CUT-WD; MOVES note §T.4/§S.2; ledger rows `sibjc` and the
  [3t] kernels]" — and those sources are (q-3)-scoped, `hdet`-guarded) is
  WITHDRAWN as a scope inference: a theorem quantified over "each complete
  finite realizable canonical tree" does not acquire an all-leaves-
  (τ-irr)/(τ-hen) restriction merely because its cited source lemmas were
  proved under that restriction, and the quoted `hdet` guard establishes the
  machine-checked candidate's scope, not K1's quantifier scope [the pass-1
  display, verbatim in substance]. What the site-proof establishes instead,
  at the pin: (α) the trees at which the mandatory path INVOKES K1 are the
  labeled/type-σ corpus (§4A site 1: the fiber-mass factorization and Lemma
  3.3's regrouping are applied to "the type-σ corpus"), and under (q-4)'s
  pin every labeled box fibers a "complete finite realizable tree, all
  leaves (τ-irr)/(τ-hen), certified at level N" — so every CONSUMED K1
  instance is at a guarded tree, claim (i) lands it in (q-1)/(q-2)'s
  decided-ns-free corpus inside D-15's scope, and the (q-3)-scoped source
  lemmas' proved range covers the consumed instances even if K1's stated
  quantifier is broader; (β) trees outside the guarded corpus — if K1's
  stated scope admits any — are consumed NOWHERE on the mandatory path
  (§4A's walk: sites 1/4 quantify over the labeled corpus only). THE
  RESIDUAL TIE, displayed as this section's SECOND premise [the pass named
  three admissible routes — guard-in-definition, guard-in-statement, or a
  proved interface lemma — and none is displayed by the quotes on file]:
  **(K1-CANON)** — at every consumed Step-18 site, K1's "canonical tree"
  denotes (q-4)'s pinned notion; equivalently (the interface-lemma form),
  every tree at which the mandatory path invokes K1 satisfies the
  all-leaves-(τ-irr)/(τ-hen) guard. (K1-CANON) is interface-pin content of
  exactly (NSF-INT)'s priced grade: NOT proved by this leaf's quotations; it
  joins the marks-vocabulary premise as a displayed premise of this section,
  and claim (ii)'s K1 leg is proved UNDER it.
* **Site K4 (M04 §5.5 (K4) + D-15 = O-1thr Theorem 3(b)/(c)).** K4's per-tree
  law "`bridgeMass` T … equals μ(fiber T)" is invoked in Theorem 4's proof at
  the trees of the labeled corpus ("K4 turns the LHS into Σ_{T type σ}
  μ(fiber T)" — the sum ranges over the carrier's type-σ trees); its
  supplier D-15 is scoped "Throughout: T decided ns-free" ((q-2)). At the
  PINNED instantiation ((q-4)), the labeled corpus is the all-leaves-
  (τ-irr)/(τ-hen) complete finite realizable trees; by claim (i) every such
  tree is decided ns-free — inside D-15's scope, so every K4 invocation at
  the pinned instance is in-scope. [At the AS-BUILT HEAD wiring this
  coincidence FAILS — the carrier admits V₀ (ns-marked) and K4's sum ranges
  over it while D-15 does not cover it: that is (NSF-KEY)'s KNOWN-FALSE-AT-
  HEAD content, a SEPARATE member retiring by Unit C, not by this proof.
  Claim (ii) is proved AT (q-4)'s pin — exactly the (H9) target's stated
  scope.]
* **No further site.** §4A's closure walk surfaced no other M3(a)/corpus-
  consuming site on the mandatory path (its site 7 consumes the corpus only
  through K1/K4's counts) — the I-2 list is closed relative to §4A's
  displayed (CH-RES) residue [REVISION 2, pass-1 gap 6], and (ii) is proved
  at every member.

**What this section does and does not establish.** It proves (i) from the
§1.4 displays plus the DISPLAYED vocabulary premise (marks record NsHalts
events — §1.4's sole generation rule), and (ii) at the two closed sites AT
THE INTERFACE PIN — the K4 leg on (q-4) directly, the K1 leg UNDER the
second displayed premise (K1-CANON) [REVISION 2, pass-1 gap 5: the r4
source-inheritance sentence is withdrawn]. It does NOT: prove (K1-CANON)
(an open interface premise; its discharge routes are the M04-side pin
display or the interface-lemma form — a discharge SEPARATE from gate I-3,
which as the accept of a conditional proof cannot supply it [REVISION 3,
pass-2 gap 3]); prove
anything about the HEAD wiring ((NSF-KEY), false there, retires by Unit C);
remove (E-N); or retire (NSF-INT) by itself — retirement is I-1 (this
section) + I-2 (the closed list, displayed) + I-3 (the owed pass's ACCEPT
of the conditional argument) + the (K1-CANON) discharge,
per (NSF-INT-RET) as corrected at REVISION 3.

## 5. What this closes (the D-11 clause map + OL-O12-2's mathematical half)

The D-11 node's four clauses (tree §D1.2), with statuses after this unit:
1. *"kernel entries are the Haar volumes of the … continuation events at the intended
   instance"* — **PROVED unconditionally at the semantic events, AT M1's in-statement
   scope and per-clause reference measures (pass-3 findings 1–3)**: families
   (i)/(ii)/(iii) as masses conditional on C_e (M1(a)); family (v): the q^{−b}
   occurrences (MEASURE-SPECIFIC per occurrence — ambient box masses for
   C_e/R_e/□(P), incl. the relaxed-box reading of the L6a factor — (BOX-VOL);
   the CONDITIONAL ratio P(R_e | C_e) = q₀^{−(E−e)} for the divisor-pattern
   occurrence [REVISION 2, pass-1 critical 1]) and the (q^d−q^{d−1})^{−1} occurrences (per-cell
   singleton-residual conditional masses) per entry per (a′); the (O3) scalings
   are NOT masses — their entire mass content is (a)'s (O3) bullet (scaling ×
   exit mass = mass of the displayed resummed event), and they are billed ONLY
   so; M1′'s concrete adjusted row — with (a″)'s displayed exclusions
   (arbitrary-α verdict rows; (vi)/(vii), whose density reading is M2's
   conditional conclusion; the (S3) weight); at the T-3/T-6 (classifier) reading,
   conditional exactly on the M3(b) dictionary (GD/CU conditionality, displayed).
2. *"the events per row pairwise disjoint"* — **PROVED (M1(b) with
   (b-PF)/(b-EX)/(LOOP-∞), §2)**, up to the displayed null sets (Z_e; ns loci) —
   at the DISPLAYED strata and the explicitly resummed multi-step families of
   the displayed grammar (stopping-word-indexed per (b-EX) at its REVISION-2
   route-(a) scope — solved-value legs are not booked events; prefix-free-
   disjoint per (b-PF); the infinite loop-0 word AND the finite-time Z_e
   members null and disjoint per (LOOP-∞), REVISION-2 form); the per-row reading for
   a concrete pack's combined/adjusted rows is clause-4 content (the
   correspondence pass, OPEN — pass-2 P6).
3. *"with positive per-state termination margin"* — **PROVED (M1(c)) at the
   density-carrying bookings**: the margins ARE O-12's verified (r1) margins 3/4
   (O1) and 6/7 (O3), reread as per-visit termination probabilities; at (O2)/(O2′)
   the 7/8 margin is a booked-row mass, NOT a termination probability (M1′; pass-1
   F2), and the true per-visit loop mass is q₀^{1−E} ≤ 1/4 at every booking.
   A.s. termination: PER BLOCK unconditional (M1(c): L2+L3's Markov property + the
   loop-mass bound — pass-2 P7 cite fix; L4 plays no role there, its
   decreasing-index argument belonging to (O-GLOB)'s descent step); the FULL
   multi-block process conditional exactly on (S1-m)+(DET) (Lemma M2.0, r4
   rewrite: loop-extended addresses + (O-1)–(O-5) + (O-GLOB), incl. duty-3's Z_μ
   clause; billing widens to the displayed (S1-m+) under a stochastic hand-off
   reading). Additionally proved at this layer: **(ROW-Σ)** (§2A) — the
   member-row normalization at (RE) members (solved totals Σ_σ β_e = 1;
   per-booking verdict-row totals = the Corollary-D margins), M2-free, with its
   complete consumption edge set displayed (§2A.4).
4. *"the pack's block system IS 𝔅_n — or an organization covered by the robustness
   criterion"* — **OPEN (pass-2 P3: this clause is NOT closed by this unit, and D-11
   is therefore NOT a completed node)**. What stands: degree-2 = the (O2) member IN
   the O-12 claim (with M1′'s coarseness finding: it is a gate object, not a density
   carrier; its transcription is in the N6 harness of record); degree-3 sealed: the
   claim "is an (O3) member" is EXACTLY OL-O12-2, whose remaining DATA half — the
   finite transcription-and-correspondence pass — has NOT run (below); the GENERAL-n
   eventual pack = O-14d instance population, an engineering duty outside the math
   tree (D-10), guarded by the robustness backup (O-12 Theorem 4 + contingent
   OL-O12-1) if a future pack departs from 𝔅_n's organizations. Until the
   correspondence pass runs, M1/M2/M3 speak about 𝔅_n members and the displayed
   (R-read) assembly — not yet about the sealed tables. [r4 addition, design
   §3.3/§3.5: the (RE) booking-profile half of this clause at the consumed member
   is ROOT's NEW (H5) member (PACK-iv); its retirement routes — the extended
   OL-O12-2 pass at n = 3, the (O1)-booked O-14d construction at general n — are
   displayed at the design §3.3.]

**OL-O12-2, split and half-closed.** OL-O12-2 (O-12 attempt rev 4 §3): "the complete
sealed degree-3 tables — every stratum mass, kernel, junction row, and final density
on record — are, entry-for-entry, an (O3) member of 𝔅₃ with its recorded composition
datum. Dischargeable by a finite transcription-and-correspondence pass."
* Its MATHEMATICAL half — what the identification would MEAN, i.e. that matching an
  (O3) member makes the pole-free theorem's clauses statements about the true degree-3
  densities (masses, margins, uniqueness of the solved values) — is **CLOSED by M1 +
  M2 at n = 3, (O3) booking**, at the conditionality displayed there (M1 at its
  in-statement scope, unconditional; Lemma M2.0 on (S1-m)+(DET); M2 (r4) at its (RE)
  scope on (S1)/(S2), monic scope — projective reading additionally on (S3); M3 for
  the verdict/box reading at its displayed conditionality incl. (H9)). Nothing about
  the MEANING of the identification remains open.
* Its remaining half is the finite transcription-and-correspondence pass itself (map
  each sealed row to its §2.3 family (i)–(vii) member and check entry equality —
  r4: EXTENDED by the per-row booking-profile duty, design (route-a)) —
  finite displayed-data comparison, no new mechanism, same execution class as O-14d —
  but it IS the content of clause 4 (pass-2 P3: "finite" and "engineering" do not
  mean "proved"): until it runs, the sealed tables are not established to be an (O3)
  member, the per-row readings ((a″), (b)'s scope note, (R-read)) are not ATTACHED
  to them, and D-11 is not a completed node. Numerics already tie it tightly (§6) —
  a falsifier, not a discharge. The (S1)/(S2) seams are NOT OL-O12-2 residue: they
  are GD-3-full/O-11 property, priced there.

**Recommended DELTA lines (governance, sign-off owed — not executed here):**
(i) the CU §6.1 OB2 eviction, constructively re-confirmed by M3(a); (ii) D-11
status → **LAYERED, NOT COMPLETE**: clauses 1–3 at their displayed layers (M1 +
M1′ + (R-b/R-d) + (b-PF)/(b-EX)/(LOOP-∞) + (BOX-VOL) + (ROW-Σ) unconditional over
the verified O-12 lemma base at the in-statement scopes [(ROW-Σ) quantified at
(RE) members]; Lemma M2.0 on (S1-m)+(DET) via (O-1)–(O-5)/(O-GLOB); M2 (r4) at
(RE) on (S1)/(S2), monic scope, projective reading on (S3); M3 on the displayed
CU/O5triple/D-15 conditionality + (H9)); clause 4 **OPEN** = the actual-pack
correspondence, OL-O12-2's data half + the (PACK-iv) booking profile; (iii)
OL-O12-2 re-labeled "data-pass only — and that data pass = D-11 clause 4's open
content, extended by the booking-profile duty" per the split above.

## 6. Falsifier report (standing citations + the extension of record)

Standing (cited, not re-run):
* **N6-regp** (`verification/openmath/N6-regp_scan.py`, results JSON + tree §3 fleet
  table): the REAL tables — the sealed RESUM-n3 degree-3 solve transcribed verbatim +
  both n = 2 instances — pass all 25 symbolic/brute gates and the (r1)/(r2-def)/
  (r2-act) scan at ALL 95 primes < 500, faithful and superset pools, 0 failures; the
  minimum observed escape margin is 6/7 — EQUAL to Corollary D's (O3) bound at q₀ = 2,
  i.e. M1(c)'s margin is observed sharp at the real tables.
* **N2-density-tie** (exact + MC + report logs): the solved values equal the true Haar
  densities numerically — 24/24 (ensemble, n, p) configurations (n ≤ 5, p ∈ {2,3,5},
  all wild pairs, monic + projective), 0 cells beyond 4 SE, plus the float-free exact
  n = 2 census sandwich (width ~1.5e-5 / 5.7e-6 / 1.3e-5 at p = 2/3/5) — M2's
  conclusion observed exactly where it can be enumerated. [The projective
  configurations exercise the (S3)-conditional PROJECTIVE reading of M2's
  conclusion — numerical support for the seam, not a proof of it.]
* **O-12 §8 rev 2/4 exact enumerations**: L6a's cell law and L6e's trichotomy over
  (ℤ/p^M)^e lifts, five configs, zero exceptions — M1(a)'s per-cell mass identity
  checked exhaustively at small levels.

Extension of record (ran with the r3 unit; UNCHANGED by this revision — every
clause it tests survives r4 verbatim):
* **N6-regp-ext2000** (`verification/openmath/N6-regp_scan_ext2000.py`, results
  `N6-regp_results_ext2000.json`, log `logs/N6-regp_ext2000.log`): the same real
  tables, primes extended 500 → 2000 (303 primes) and pools deepened δ ≤ 3 → δ ≤ 4
  (q₀ up to ≈ 1.6·10¹³). MASS-ID-relevant clauses [criterion rescoped at r4,
  pass-3 finding 9 / design §2.6]: every family-(i)/(ii)/(iii) entry defined with
  value in [0, 1] — the (a″) scope, at which an out-of-[0,1] value WOULD have
  refuted M1(a); the (O3) scaling entries checked against their displayed > 1
  form q^{E_m}/(q^{E_m}−1) (they are NOT masses and [0,1] is not claimed for
  them); every count polynomial a nonnegative integer (Fact F at large q₀);
  every det margin positive (M1(c)). RESULT (2026-07-31, this machine):
  **303/303 primes PASS at n = 2 AND n = 3; failing sets EMPTY at faithful AND
  superset pools; all 25 gates + brute ACT anchors PASS; minimum escape margin
  again exactly 6/7** (at n = 2, q₀ = 2, the (O3) kernel) — the M1(c) bound stays
  sharp and is never approached anywhere else in the scanned range.

A single failure — a family-(i)/(ii)/(iii) entry outside [0, 1], an (O3) scaling
off its displayed form, a non-integer count, a vanishing determinant, or an N2
density mismatch — would have refuted M1(a)/(c) or M2 at the real tables, at the
clauses as scoped by (a″). [The scan already keys entries by family; no scanned
quantity changes at r4 — the rescope is the CRITERION display.]

## 7. Honesty ledger

**Proved here, unconditional over the VERIFIED O-12 §3 lemma base (+ ordinary measure
theory) — all BLOCK-LEVEL or root-partition statements; nothing in this class crosses
a hand-off (pass-2 P2 re-billing, per the verifier's own correctly-billed list):**
Theorem M1 at its REV-4 in-statement scope and per-clause reference measures — (a)
families (i)/(ii)/(iii) as masses conditional on C_e; (a′) the family-(v) per-entry
readings at their displayed measures (ambient boxes incl. (BOX-VOL)'s relaxed box;
the CONDITIONAL ratio P(R_e | C_e) = q₀^{−(E−e)} — measure-specific per occurrence,
REVISION 2; per-cell singletons; NO mass reading at the (O3) scalings; the (S3)
weight quantified over nowhere); (a″) the displayed exclusions; (b) at the displayed
strata with **(b-PF)** (proved), **(b-EX)** (proved by the seven-family grammar
induction + the three booking adjustments — quantified, per the REVISION-2
route-(a) rescope, over the EXPLICITLY RESUMMED row terms of the displayed
grammar only, solved-value legs excluded as non-booked continuation values),
and **(LOOP-∞)** (proved: null + disjoint, incl. the REVISION-2 finite-time
Z_e members); (c) per-visit margins at the
density-carrying bookings (O1)/(O3) + the PER-BLOCK a.s. exit; (d) at its
displayed ONE-STEP-strata scope — Proposition M1′, the root
partition/normalization (R-b/R-d), Lemma **(BOX-VOL)**, and **(ROW-Σ)** (§2A —
quantified at (RE) members; its (RE) hypothesis at the consumed member is ROOT's
(PACK-iv), hypothesis-grade). These are new COMPOSITIONS: O-12's claim
deliberately excluded value semantics; its lemmas do not — this unit is the place
where that composition is written down.
**Proved here, conditional exactly on the displayed seams — everything that crosses
a hand-off:** Lemma M2.0 (a.s. termination of the full process) [(S1-m), the
child-marginal clause under the canonical parent law incl. its ROOT-FINITENESS leg
— Haar absolute continuity suffices — PLUS the structural reading (DET); the r4
proof = the loop-extended addresses with **(O-1)** (child-reached identity, exact),
**(O-2)** (never-exits null: eternal loops + duty-3's per-word Z_μ encounters),
**(O-3)** (measurability), **(O-4)** (two-clause loop-word transport — loop legs on
PROVED L2+L3 only), **(O-5)** (the corrected local exit partition incl. terminating
exits), **(O-GLOB)** ((G-a) countable addresses with the finite-slot branching
bound; (G-b) root-to-leaf ≪ transport; (G-c) the per-address/per-time null union;
(G-d) pointwise strong induction on block size) — billing (S1-m)+(DET) borne out
by the displayed input inventory; widens to the displayed prefix-conditional
(S1-m+) under a stochastic hand-off reading — §3]; Theorem M2 (r4) at its **(RE)**
scope [(S1) GD-3 full / LIT-3 root leg; (S2) O-11 composition data; arbitrary-α
members EXCLUDED in-statement; at the CONSUMED member (RE) = (PACK-iv), ROOT (H5),
priced not proved]; M2's top layer at the DISPLAYED root read (R-read) [(S1) root
leg + (S2)] — its PROJECTIVE reading additionally on [(S3), owner A-1]; Theorem
M3(a) at separable-lift scope [P(O5t r3) + (I-1)–(I-3) via CU-7 + five flags +
P(O-1thr r4) + TB-CAP D-12r + (E-N) displayed + the (H9) price for the corpus
routing]; M3(b) [in-statement: the clause-4 correspondence, OPEN; the GD/CU
dictionary, per the CU §0 registry].
**Written out here, retirement GATED on this file's owed hostile pass:** the
**(NSF-INT)** leaf proof (§4B, artifact I-1 against the closed I-2 site list —
claim (i) from the §1.4 displays + the displayed vocabulary premise; claim (ii)
at K1/K4 at the interface pin, the K1 leg under the SECOND displayed premise
(K1-CANON) [REVISION 2, pass-1 gap 5]; gate I-3 = the pass's ACCEPT — until then
(NSF-INT) stands PRICED at (H9)); the **(CH-COV)** closure audit (§4A, outcome
CLEAN at the walked closure — (NS-ALL) does not fire; closure grade
CORPUS-RELATIVE with the displayed residue (CH-RES), REVISION 2 — adjudicated
at the same pass; the overbroad ROOT Step-12 delivery flagged, its rescope =
the owed Unit-B edit E-M3); the **(ROW-Σ)** consumption edge set
(§2A.4, duty-2 — consuming proof lines on the mandatory path: NONE at the
quoted operative clauses, residue (EDGE-RES) displayed, REVISION 2; delivery
displays: exactly ONE (ROOT Step 17's statement fragment, rescoped at Unit B E1);
off-path adjacency fenced: M04 Theorem 2 / `x3_total`).
**NOT proved here — the open content displayed:** D-11 clause 4, the ACTUAL-PACK
CORRESPONDENCE (that the sealed tables ARE an (O3) member — OL-O12-2's remaining
data half + the r4 booking-profile duty) — so D-11 is NOT a completed node; the
(RE) profile of the consumed member (= (PACK-iv), ROOT (H5) — hypothesis-grade,
retirement routes at the design §3.3); **(NSF-KEY)** (KNOWN FALSE AT HEAD — the
M02 V₀ poison, carrier quotes re-verified this session; retires ONLY by the
design's Unit C per (NSF-KEY-RET), never by this leaf); and the seams
(S1)/(S2)/(S3) themselves. The unit charge's "kernel/entry VALUES are masses" is
covered exactly at (a) (families (i)–(iii), per entry, conditional on C_e), at
(a′)'s box and singleton-residual readings, and at M1′'s concrete recorded row;
it is NOT covered per entry at the (O3) scalings (only their products with exit
masses are event masses), and families (iv)-arbitrary-α/(vi)/(vii) + the (S3)
weight are excluded ((a″)).
**Findings:** M1′ (the (O2) verdict-row coarseness pins the density-carrying packs to
the re-entrant bookings); the (r1)-as-uniqueness-pivot reading (§3) — the exact
mechanism by which O-12 speaks about real densities; the constructive OB2
re-confirmation (M3(a)); NEW at r4: the per-booking row-total identity
S_e = Φ^{(booking)} ((ROW-Σ)(b) — the verdict-row total IS the termination margin,
booking by booking); the empty consuming-edge finding for (ROW-Σ) on the walked
mandatory path (§2A.4, at its REVISION-2 corpus-relative grade); the CLEAN
(CH-COV) outcome at the walked closure (§4A, same grade).
**Not attempted:** discharging (S1)/(S2)/(S3) (owned by GD-3-full/O-9, O-11, and
annex A-1 respectively); the OL-O12-2 data pass (= clause 4's open content, above,
incl. the (route-a) booking-profile duty); (NSF-KEY)'s Unit-C execution (gated on
sign-off per (NSF-KEY-RET) R-0); the general-n pack population (O-14d); Lean
artifacts; any O5triple/O-1thr/CU pass.

**Files:** this record = `lean/notes/openmath/D11_rev4_2026-08-04.md`
(supersedes `D11_massid_phaseB_attempt_rev3.md`; pass-3 report =
`D11_owedpass_2026-08-03.md`; governing design =
`R11_REPAIR_DESIGN_V5_2026-08-04.md`, executed as its Unit A, L1–L11); falsifier
extension = `verification/openmath/N6-regp_scan_ext2000.py` + results JSON + log
(UNCHANGED by this revision AND by REVISION 2 — the rev-4/REVISION-2 edits are
statement-perimeter, billing, and proof-obligation write-outs, not scanned
quantities). The rev-3 file gains
its superseding header; no other file touched. Next gate [updated at
REVISION 2]: pass 2 of the r4 hostile verification (pass 1 =
`D11r4_pass1_2026-08-04.md`, VERDICT CRITICAL, folded at the REVISION 2
section below); fresh context, Codex; charge per the design §7 GATE — the
(b-EX)/(O-1)–(O-5)/(O-GLOB)/(ROW-Σ) write-outs adjudicated AS PROOFS ((b-EX)
at its route-(a) scope), (CH-COV)/(§2A.4) as traces AT their displayed
corpus-relative grades ((CH-RES)/(EDGE-RES)), §4B as a proof for gate I-3
under its TWO displayed premises. Unit C/B/D order and
the BLOCKED-PENDING standing per the design §0.3/§7 — no ROOT edit rides this
leaf (the new owed ROOT/Unit-B edit E-M3 is recorded, not executed).

## REVISION 2 (pass-1 fold)

Repair of the r4 pass-1 hostile verification (`D11r4_pass1_2026-08-04.md`,
Codex fresh context, 2026-08-04, VERDICT CRITICAL — 3 CRITICAL, 3 GAP),
executed IN PLACE per the campaign pattern: the verifier's own displayed
repair directions adopted verbatim where displayed (findings 1, 3, 4);
route-decision by consumer need where the verifier offered two routes
(finding 2); minimal honest fixes / explicit open premises where no
direction was displayed (findings 5, 6). Every edit is marked "[REVISION 2,
pass-1 …]" at its site and traces to exactly one finding below (churn
discipline); the pass-1 VALIDATED content — the ROW-Σ booking totals and
solved normalization X = 1, the termination argument modulo its stated
premises ((S1-m)+(DET)), M2's (RE)/(PACK-iv) scoping, (BOX-VOL)'s arithmetic
and vertex-coordinate independence, M1's (a″) exclusions, O-1's exact union,
O-2's enlarged bad set, O-3, O-4's transport clauses (apart from the
parenthetical), O-5's two exit kinds, O-GLOB (G-a)–(G-d) — is byte-stable.

### Disposition table (finding → repair → location)

| # | pass-1 finding (gist) | class | repair → location |
|---|---|---|---|
| 1 | M1(a′) still claims "each occurrence IS the AMBIENT Haar mass of a displayed valuation box" while q₀^{−(E−e)} — the divisor-pattern formula's factor — is a C_e-CONDITIONAL probability, the ambient mass of NO displayed box (the displayed boxes carry q₀^{−e}, q₀^{−E}, q₀^{−N(P)}) | CRITICAL | **FIXED, the verifier's fix verbatim → §2 (a′) first bullet:** the uniform ambient sentence WITHDRAWN; each occurrence now carries a displayed MEASURE-SPECIFIC reading — AMBIENT for C_e, R_e, □(P); CONDITIONAL for the R_e\|C_e occurrence, displayed as the ratio P(R_e \| C_e) = q₀^{−(E−e)} = vol(R_e)/vol(C_e). Echo sites re-pointed: §0-pre row-2 bracket, §5 clause 1, §7 ledger |
| 2 | (b-EX) is FALSE at its announced universal grammar scope: the (O2′) re-feed leg κ₁·x_σ is semantically loop-c FOLLOWED BY the continuation x_σ — its expansion contains paths (e.g. (loop-c)·(loop-0)·exit) whose proper prefixes are not all-(loop-0); same issue for the recursive solves read extensionally. Verifier's two routes: (a) restrict to explicitly resummed row terms, or (b) extend the grammar to Λ_e^{<ω} prefixes | CRITICAL | **FIXED, ROUTE (a) → §2 (b-EX) statement (rescoped, with the booked/solved-value distinction, the forcing counterexample, and the consumer check displayed in-statement) + family-(iv) walk ((O2′) sentence rewritten; the r4 "merely one-step at extensional scope" reading WITHDRAWN) + the walk's closing line.** Route warrant, from the consumers: (a)'s (O3) bullet, (ROW-Σ) step 3, M2's exhaustiveness note, and (b)/§5's scope sentences partition over BOOKED resummed terms only (exactly two classes: the (O3) kernel resummation; the (O3)-scaled exit rows); no consumer partitions over an expansion of a solved-value leg — M1 excludes (vi)/(vii) at (a″), (ROW-Σ)/M2 consume the legs only through the pivot-solve algebra; (O-1)/(O-5)'s general-loop-word unions cite (b-PF) directly at mixed words w·F and consume (b-EX) nowhere. So route (b) is needed by no consumer. Echo sites: §0-pre row-4 bracket, §5 clause 2, §7 ledger |
| 3 | (LOOP-∞)'s "EXACT" decomposition omits the finite-time Z_e encounters: booked stopping words end in (loop-c) or an exit, so "(loop-0)^L then Z_e" lies in no E_w and not in {loop-0 forever} — the identity is false as displayed (the O3 masses survive since the members are null) | CRITICAL | **FIXED, the verifier's fix verbatim → §2 (LOOP-∞), new clause (iii):** the members Z^{(L)} := {(loop-0)^L then Z_e}, L ≥ 0, ADDED to the displayed decomposition; each proved NULL (Markov property L2+L3 + L6e(ii)) and DISJOINT from every E_w, every Z^{(L′)}, and {loop-0 forever} (the (ii) composed-state argument); the countable-additivity computations of (a)'s (O3) bullet unaffected; noted as the §2-layer instance of duty-3's (O-2)(ii) clause. Consumer re-pointed: M1(c)'s (O3) sub-clause; echo: §5 clause 2 |
| 4 | (O-4)'s canonical clause gives the wrong fixed-word mass: (κ₀+κ₁)^{\|w\|} is the mass of "some loop letter at each of the first \|w\| steps" (summed over all q^{\|w\|} words), not of the fixed word w, whose mass is q^{−E_μ\|w\|} | GAP | **FIXED, the verifier's fix verbatim → §3 (O-4) canonical clause:** parenthetical replaced by the fixed-word mass ∏_j P(s_j \| C_μ) = q^{−E_μ·\|w\|} (each FIXED loop letter has mass q^{−E_μ}), the wrong form withdrawn in-place; only positivity is consumed downstream — the transport argument is unchanged |
| 5 | (NSF-INT) claim (ii)'s K1 leg rests on an unsupported scope inference ("K1's bare phrase carries the leaf clause FROM ITS SOURCES") — a theorem's quantifier does not inherit its source lemmas' restriction, and the quoted `hdet` guard scopes one machine-checked candidate, not K1; so the marks-vocabulary premise is not the only open (NSF-INT) gap | GAP | **FIXED, minimal honest fix (the verifier displayed three admissible routes, none provable from the quotes on file) → §4B claim (ii), K1 site REWRITTEN:** the source-inheritance sentence WITHDRAWN; the site rebuilt on the invocation-side route at (q-4)'s pin (consumed K1 instances = the labeled corpus = guarded trees; the guarded sources cover the consumed range; unguarded trees consumed nowhere on the mandatory path) PLUS the residual tie displayed as the section's SECOND premise **(K1-CANON)** — the verifier's route-(γ) interface lemma, stated OPEN, not proved; claim (ii)'s K1 leg proved UNDER it. Bookkeeping updated: §4B head bracket + closing paragraph, §4's (NSF-INT) bullet, §7 ledger. (NSF-INT) remains PRICED at (H9); CH-COV's inherited K1 scope issue routes to the same displayed premise |
| 6 | §2A.4's and §4A's "closure-complete" is asserted beyond what the leaf quotes (no full proof bodies or mechanically checkable dependency inventory on file; ditto CH-COV's "exactly TWO delivery edges"); and ROOT Step-12's delivery "type(g) = typemult(T) for every lift g" is broader than M3(a)'s repaired separable-lift conclusion | GAP | **FIXED, honest-strength rescope → §2A.4 (header + new "Completeness grade" display + edge-set bullet + closing paragraph) and §4A (method + outcome + I-2 references, incl. §4B's):** completeness restated CORPUS-RELATIVE with the residues displayed as named audit premises — **(EDGE-RES)** (no unquoted line of the walked corpus consumes a member-row normalization) and **(CH-RES)** (no unquoted line reads M3(a) outside (ch-1)/(ch-2); no r9 delivery edge beyond the two quoted) — discharged only by adjudication-by-inspection against the NAMED corpus; "NONE"/"CLEAN" verdicts restated at the quoted operative clauses under those residues. The Step-12 overbreadth DISPLAYED at §4A method with the required rescope ("every MONIC SEPARABLE lift" + the ns-null mass note) recorded as the owed ROOT/Unit-B edit **E-M3** (no ROOT edit rides this leaf); no mandatory consumer reads the unrescoped breadth (sites 1–6 M3-free/label-blind; site 7 mass-mediated at separable scope). Echoes: §7 ledger, Files/next-gate note |

### Net honesty movement (all narrowings or displays; nothing widened)

* (b-EX) is NARROWER (route (a) — matching its actual consumers exactly);
  its two booked resummed classes are now enumerated in-statement.
* (LOOP-∞)'s decomposition gains the null Z^{(L)} classes — the set identity
  is now exact; no mass value changes.
* §4B carries TWO displayed premises (was one): marks-vocabulary + (K1-CANON).
* §2A.4/§4A carry displayed corpus-relative residues (EDGE-RES)/(CH-RES)
  (was an unqualified closure-complete claim).
* One new owed governance edit recorded: E-M3 (ROOT Step-12 delivery rescope
  to separable-lift scope; Unit B, sign-off owed — not executed here).
* No formula, no validated proof step, and no scanned falsifier quantity
  changed; the N6/N2 falsifier reports stand as cited.

**Next gate:** r4 pass 2 (fresh context, Codex) — same design-§7 charge,
plus: adjudicate the route-(a) rescope's consumer check, the (LOOP-∞)
REVISION-2 decomposition, the (K1-CANON) premise display, and the
(EDGE-RES)/(CH-RES) residue grades.

## REVISION 3 (pass-2 gap fold)

Pass 2 (report: `D11r4_pass2_2026-08-04.md`) returned **GAPS-ONLY**: no
critical defect, all six pass-1 repairs adjudicated VALID (M1(a′) ambient
readings, (b-EX) route (a), (LOOP-∞), (O-4), the K1 invocation-side repair,
the closure rescope), no-churn confirmed on (ROW-Σ)/(G-a)–(G-d)/M2. Three
bookkeeping gaps, folded here as targeted edits; nothing adjudicated VALID
was touched.

| # | pass-2 gap (gist) | class | disposition → location |
|---|---|---|---|
| 1 | The (b-EX) "every consumer" audit inventory is incomplete: M1(c)'s (O3) termination reread (the complement-of-E_rec decomposition, consuming (LOOP-∞)) is a consumer but is not listed | GAP | **FIXED in-place → §2 (b-EX) statement, consumer-check bracket:** the omitted consumer ADDED to the inventory with the pass-2 observation displayed — it too partitions only the two booked (O3) resummations plus the null residue classes, so route (a) is unchanged (the verifier's own check, now on record) |
| 2 | The family-(iv) walk's (O2) α-fold sentence uses row-level event-booking language stronger than (a″) permits: only the σ-TOTAL κ₁ is an event mass; under arbitrary rational α_e an individual α_e(σ)·q^{−E} may be negative or exceed every event mass | GAP | **FIXED in-place → §2 (b-EX) proof, family-(iv) walk:** "books … into the verdict rows" weakened to "folds the TOTAL κ₁"; the per-row reading of "the booked strata are the one-step recentering strata" WITHDRAWN with the arbitrary-α reason displayed; noted that the adjustment carries no multi-step content either way, which is all (b-EX) certifies |
| 3 | The stated (NSF-INT) retirement gate does not discharge its own new premise: I-3 accepting §4B (now a CONDITIONAL proof) verifies only (K1-CANON) ⇒ the K1 scope conclusion and cannot establish (K1-CANON) | GAP | **FIXED as a gate correction → §4 (NSF-INT) bullet + §4B head bracket + §4B closing paragraph:** retirement restated as I-1 + I-2 + I-3 (accept of the conditional argument) **+ a separate (K1-CANON) discharge** (routes: the M04-side pin display or the interface-lemma form). The discharge itself is LEAF-EXTERNAL WORK (M04-side content), not a leaf edit — recorded here as the owed obligation; no ROOT-file edit rides this revision (E-M3 remains the only owed ROOT edit, unchanged from REVISION 2) |

Net movement: all three edits are honesty narrowings or audit-list
completions; no formula, mass value, proof step, or VALID-adjudicated
repair changed. (NSF-INT)'s priced status at (H9) is unchanged — only the
stated retirement condition was corrected (it now requires strictly more).
