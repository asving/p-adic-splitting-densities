1. **GAP — DICT-0 consumer audit misclassifies clause (b).**

   The note says:

   > “the (1) ⇒ (2) leg consumes (c) ONLY”

   but the proof itself says:

   > “under [IN-C](b)’s identification, whose range … is exactly what is consumed here”

   Clause (c)’s divisibility comparison is only meaningful after identifying the reconstructed residual field with \(F_{j_0}\!\cdot F_q\). Thus the leg consumes **(b) and (c)**, not (c) alone. This does not invalidate DICT-1—the four-clause hypothesis supplies both—but the advertised exact audit, and its repetition in §5/§6, is false.

2. **CONFIRMED — “lies over” is well-defined at this setting.**

   Let \(K=\mathbf Q_p\) and \(K_\delta=\operatorname{Frac}(O_\delta)\). Because \(O_\delta\) is a DVR, \(O_\delta[x]\) is a UFD; hence its irreducibles are prime. For a monic factorization \(f=\prod h_i^{e_i}\) over \(\mathbf Z_p\), any \(O_\delta[x]\)-irreducible factor \(G\mid f\) therefore divides some \(h_i\). Distinct \(h_i,h_j\) are relatively prime over \(K\), and remain relatively prime over \(K_\delta\), so \(G\) cannot divide two of them. Scalar extension preserving relative primality is the relevant field-level fact; see [Stacks, Lemma 9.11.2](https://stacks.math.columbia.edu/tag/09GY), while the UFD step is [Stacks, Lemma 10.120.10](https://stacks.math.columbia.edu/tag/0BC1).

   Qualification: “coprime in \(O_\delta[x]\)” must mean gcd/no common nonunit factor, not necessarily comaximal ideals. That is sufficient for the uniqueness argument actually used.

3. **CONFIRMED — no consumer silently requires a functional match outside the split-free locus.**

   The factor correspondence supplies the potentially one-to-many relation. DICT-0(d) supplies exactly the single reconstructed position required by DICT-1(3), `pdata`, and the \((2)\Rightarrow(3)\) leg. Outside the split-free locus, `posLetterD` collapses the branch ambiguity to `offLetter`; no audited consumer selects a particular reconstructed branch there.

4. **CONFIRMED — proof-leg replay, modulo finding 1.**

   - \((2)\Rightarrow(3)\): consumes DICT-0(a), (b), and (d), plus [IN-R1] and [IN-U].
   - \((3)\Rightarrow(1)\): consumes no additional DICT-0 clause.
   - \((1)\Rightarrow(2)\): consumes DICT-0(b) and (c), plus [IN-F1].
   - `l4_persist`: consumes no DICT-0 clause.

   Literal transported-key equality is indeed unused.

5. **CONFIRMED — disposition audit.**

   Both predecessor matching definitions are explicitly retired for the correct reasons. Pass-2’s missing next-read compatibility is supplied by clause (c); the terminal field range is explicitly \(j\le k+1\); split-free uniqueness is exposed as clause (d); and factor-blind Lean carriers remain honestly fenced as an H4-F1/H4-F6 obligation. No fresh critical circularity or missing premise was found.

Counter: **0/1**. This pass is not clean because the displayed “exact” consumer audit needs the clause-(b) correction.

VERDICT: GAPS-ONLY