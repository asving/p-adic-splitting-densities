# GENTOW2 passPE5 — HOSTILE VERIFIER REPORT (fresh context), 2026-08-10

**VERDICT: CLEAN — 0 CRITICAL, 0 GAP, 2 MINOR. The r3 repair HELD
under independent re-derivation and under the note's own suggested
hostile surfaces: LEMMA GENTOW2-B″ re-derived from the FGMN citation
surfaces and the accepted corpus (the Φ₂-adic expansion argument, the
j ≥ 1 y-degree kill, the γ-calculus — every step checked, the LD/LP1
numbers recomputed by hand and matched to the decode pins); the
restated LEMMA GENTOW2-B's multiplicative prescription is exact
algebra and ψ₃^{(w)} is verifiably admissible for every w ∈ K₂^×
(GENTOW2-A's quantifier accepts it); the flipped honesty box's
w-blindness is argued, not asserted. The fresh route — the FIRST
γ₂-live cocycle contact anywhere (new tower GB: e₂ = 2 AND f₂ = 2,
z₂ = η₂ ∈ F₄∖F₂ with z₁ = 1, ϑ a pure z₂-power) plus the first
e₃ = 2 × f₃ = 2 cocycle frame — came back GREEN 91/0
first-run-under-seal: the direct cocycle measurement
res(n̂₂(2u₃)/n̂₂(u₃)²) = z₂² (≠ 1, ≠ z₂) at all 7 primes, and the two
OPPOSITE-direction verdict teeth (GA1 split-where-per-height-says-
irreducible, GA2 irreducible-where-per-height-says-split) both bit
FOR B″'s law with the exact exponent. Machine leg: 13/13 pins, PE4
seal chains verified, 5/5 isolated re-runs bit-identical, both PE4
instruments tautology-audited negative. The two MINORs are r3-text
hygiene (inconsistent B′-consumption lists; two un-inventoried
descriptive cites). ACCEPTANCE COUNTER RESTARTS: 0/2 → 1/2.**

Target: lean/notes/openmath/GENTOW2_PROOF_2026-08-09.md at HEAD
(d798529; md5 399017ac32aac64e14c2bff5083c2c4d), the r3-repaired note
(arc: seal → PE1 → r1 → PE2 → r2 → PE3 CLEAN 1/2 → rider → PE4 NOT
CLEAN [B-2 machine-refuted letter-live, counter reset 0/2] → r3 =
commits 117b2c0/8d62554/acdf97a/d798529). PE4 report + all r3 commits
read first. Charge: quote-and-classify, fix nothing; CLEAN bar =
nothing GAP-or-worse; counter restarts only on CLEAN.

## A1. LEMMA GENTOW2-B″ RE-DERIVED (attack 1) — HOLDS

Re-derivation done independently this pass, then compared:

* **γ-calculus.** Value-0 Laurent monomials x^i π^a Φ′^b: value
  equation ih₁e₂ + ae₁e₂ + bu₂ = 0; mod e₂ forces bh₂ ≡ 0 (u₂ =
  e₂V₂ + h₂; gcd(h₂,e₂) = 1 — PDF p.16 top line re-read this pass)
  so e₂ | b; quotient by γ₂^{b/e₂} (an x,π,Φ′-monomial via π₂ =
  x^{ℓ₁}π^{ℓ′₁}) leaves value-0 x^{i′}π^{a′} = γ₁^{i′/e₁}
  (gcd(h₁,e₁) = 1). Residues z₁, z₂ via Def 3.12 + Prop 1.15 through
  the C-m1 bridge (PE4-vetted). ✓ Re-derived, matches S5.2.
* **Proof steps (i)–(iv).** (i) Cor 4.7(2) iteration: quote [Q8]
  carries no degree hypothesis; each peeled factor's grade (κ̄, 0)
  ∈ Γ₂ ✓; y-degree 0 by Cor 4.4(3) (s = s′ = 0 single-point
  polygon; layout L1088 re-read: "deg Ri,α(g) = ⌊s′(g)/ei⌋") ✓.
  (ii) Def 1.8 defines ν by the Φ₂-expansion minimum — verified
  VISUALLY on PDF p.5 this pass ✓; every term in P_{β_t}(µ₃), Cor
  4.7(1) additivity ✓. (iii) the dichotomy: e₃ ∤ j → jλ₃ ∉ Γ₂
  (gcd(h₃,e₃) = 1) while ν(Q_j) ∈ Γ₂ (deg < m₃ Φ₂-free, Prop 1.9(2)
  + µ-minimality — PDF p.6 visual ✓), value ∉ Γ₂ ∌ β_t; e₃ | j →
  eq (14) puts the term at y-degree j/e₃ ≥ 1 ✓; ν > β_t → Lemma
  3.17 kills ✓. (iv) G(x₀) = Q₀(x₀) (x₀ a Φ₂-root, Φ₂-adic
  expansion) → digit(Q₀) = res(ẑ)·res(N^d/M) = ϑ(t)·ϑ(t)^{−1} = 1
  ≠ 0; B-1 in scope (deg Q₀ < m₃) gives exactness; B′(3) gives
  R_{3,β_t}(Q₀) = u(β_t) ✓. Equating y-degree-0 parts in F₃[y] is
  valid (polynomial identity, graded by y-degree). **The proof
  goes through.**
* **LD/LP1 numbers re-derived by hand this pass.** LP1: γ₁ = x²/2,
  γ₂ = Φ′²/x⁹ (ℓ₁ = 1, ℓ′₁ = 0, u₂ = 9); N²/M = xΦ′²/32 ↦ lattice
  split (1,−5,2) = 1·(−9,0,2) + 5·(2,−1,0) → res = z₂z₁⁵ = z₁²
  (z₂ = 1, z₁³ = 1) = ϑ(0)^{−1}; ϑ(0) = z₁ ✓ = decode COC pin.
  Predicted P: LD1 y²+z₁y+z₁, LD2 y²+z₁y+1 ✓ = decode MP1/MP2.
  ϑ(1) = 1 (d = 1, M = N) ✓. LD3 single trivial slot ✓. Also
  checked: the decode division identity r3² = res(k̂₁/N)r3 +
  res(k̂₀/N²) is exact algebra at a Φ₃-root, and res(k̂₀/N²) =
  c₀·ϑ(0) — the decode frame measures ϑ with w quotiented, as the
  note says.

## A2. RESTATED LEMMA GENTOW2-B + ADMISSIBILITY (attack 2) — HOLDS

* ψ₃^{(w)} := w^{f₃}ψ₃(y/w) = y^{f₃} − Σ a_t w^{f₃−t} y^t: monic ✓,
  deg f₃ ✓, constant w^{f₃}ψ₃(0) ≠ 0 ✓, irreducible (y ↦ y/w is a
  K₂[y]-automorphism substitution; a factorization would pull back)
  ✓. THEOREM GENTOW2-A's hypothesis set is exactly {monic
  irreducible, deg f₃ ≥ 1, ψ(0) ≠ 0} + (i)/(ii) — it accepts
  ψ₃^{(w)} for EVERY w ∈ K₂^×; its quantifier is "ANY monic Φ₃ with
  (i)+(ii)" (PE4 walked this assembly end-to-end; spot-checked
  here). ✓
* Prescription algebra: c_t = ϑ(t)^{−1}a_t → u(β_t)c_t =
  ϑ(t)w^{f₃−t}ϑ(t)^{−1}a_t = w^{f₃−t}a_t → R = y^{f₃} −
  Σ a_t w^{f₃−t}y^t = ψ₃^{(w)} ✓ exact. c₀ ≠ 0 ⟺ a₀ ≠ 0 ✓; digit
  supply = T(b)′(iii) lifts ✓.
* Consumers: the S4 consumption note consumes clauses (c)+(d) only
  (level-2 data, w-free); residue field F₃[y]/(ψ₃^{(w)}) ≅
  F₃[y]/(ψ₃) (root scaling by w ∈ F₃) — factor patterns and degrees
  w-invariant. No downstream site found that consumes the literal
  digit-string spelling. ✓

## A3. FLIPPED HONESTY BOX / w-BLINDNESS (attack 3) — ARGUED, with
## one inventory-drift residue (m2)

The chain is argued, not asserted: B′(5) + B″(6) give realized =
w^{f₃}P(y/w); B″'s Consequently states P's irreducible-factor
pattern is attained for every w (substitution automorphism); and
#primes ↔ #distinct residual factors is the Lemma 5.3(2)/Thm 6.6
surface (PDF-verified in PE4's [F1] walk). The box's re-reading of
the 13-witness record ("supports ϑ = 1 at its own tested slots,
structurally w- and z₁-blind") is accurate — indeed conservative:
by the γ-calculus, ϑ ≡ 1 is FORCED on letter-dead geography (z₁ =
z₂ = 1 kills every letter monomial), and the two standing witnesses
with z₂ ≠ 1 potential (N1: f₂ = 2) have f₃ = 1 (trivial cocycle),
so the record could not have constrained a γ₂-leg either way — the
r3's own suggested fresh surface, taken below. Residue: the Thm 6.6
leg lives in the PE4 report and S8's T2 disclosure, not in the
note's S6.1 inventory — folded into finding m2.

## A4. NEW PDF ROWS 20–22 (attack 4) — ALL MATCH, both legs re-run

* Row 20, Lemma 3.13(1): layout L836–840 ✓ + PDF p.16 VISUAL ✓ —
  "µᵢ(πᵢ) = 1/e(µᵢ₋₁), µᵢ(πᵢ₊₁) = 1/e(µᵢ)"; eq (11) on the same
  page displays the stability µᵢ(πᵢ₊₁) = µⱼ(πᵢ₊₁) that C-m1's
  "EXACT stable value" clause rides. MATCH.
* Row 21, Def 1.8: layout L250–255 ✓ + PDF p.5 VISUAL ✓ — the
  augmented valuation IS defined by the φ-expansion minimum
  (µ′(g) = min µ′(gₛφˢ)), exactly B″ step (ii)'s consumption. MATCH.
* Row 22, Prop 1.9(2): layout L258–261 ✓ + PDF p.6 VISUAL ✓ — "µ(g)
  = µ′(g) if and only if φ ∤µ g". The note's instantiated gloss
  ("Φ₂-free ⟹ ν = µ₂") adds the one-step µ-minimality bridge
  (deg < deg φ ⟹ φ ∤µ g), which is FGMN Lemma 1.3(4)/p.5 territory
  — acceptable instantiation, noted in m2's species. MATCH.

## M. MACHINE-LEG INTEGRITY — ALL PINS EXACT, 5/5 RE-RUNS
## BIT-IDENTICAL, TAUTOLOGY AUDITS NEGATIVE

* **Pins at HEAD (13/13 exact):** PDF da846c82f1fcb619cefb3feb8164191e;
  checks.py 2603eb7c / run2 832aa838; pe1 a70dd986/e66e8ea5; pe2
  f2d6f1fa/d26cacf5; r2 probe d0c0ebeb/b79121a2; pe3 9755cce9/6bbb87f8;
  pe4_fresh ab0d3d5d/2b596b7e; pe4_decode 3f0cb151/50d7b17f.
* **Seal chains of the r3-load-bearing PE4 instruments verified:**
  pe4_fresh blob at seal 23def87 = HEAD blob (md5 ab0d3d5d);
  runner-only commit; output-only at 253ba13 (kept as-is, RED 62/9).
  pe4_decode blob at seal 90f50af = HEAD blob (3f0cb151); output-only
  at af9a19f (GREEN 14/0). First-run-under-seal on both.
* **Isolated re-runs (fresh /tmp copies), 5/5 BIT-IDENTICAL:**
  gentow2_checks 41/0 exit 0; r2_letter_probe 21/0 exit 0; pe3_fresh
  50/0 exit 0; pe4_fresh 62/9 RED exit 1 (the by-design nonzero exit;
  output byte-equal to the committed artifact); pe4_decode 14/0 exit 0.
* **Tautology audit, pe4_decode (the instrument B″ cites — never
  before audited):** GP leg computes r1/r3/m1/m0/coc via independent
  nfmodpr reads; expectations docstring-literal; DISCRIMINATING
  paired checks (MP1 vs MP2 and MULT0R1 vs MULT0ONE both computed
  per frame with opposite expected truth values). No circularity.
* **Tautology audit, pe4_fresh:** A-route = own exact integer
  arithmetic (expansions by monic division, dv-ladders, Bareiss
  resultants); B-route = PARI factorpadic + per-factor
  nfinit([·,[2]]) + idealprimedec/nfeltval/nfmodpr; predictions
  docstring-literal, sealed pre-run. The 9 violations are all
  B-2-derived predictions (self-implicating by design, disclosed).
  No circularity.
* r3 commits touch ONLY the note (.md) — sealed artifacts
  byte-untouched through r3, as S11 claims. ✓

## F. FINDINGS (2 MINOR; nothing GAP-or-worse survived)

**[m1] MINOR — three inconsistent B′-consumption lists in [r3] text.**
The corrected S5 proof step reads "By LEMMA GENTOW2-B′(3)+(4) (S5.1,
derived from the accepted corpus), with β = β_t the slot's grade:
R_{3,β_t}(k̂_t) = u(β_t)·digit(k̂_t)" — that step consumes B′(3)
only ((4) is the u = 1 equivalence, exactly what r3 stopped
consuming; its only remaining use-site is the restated lemma's final
per-height-iff sentence). The honesty box says "the restated lemma
consumes only B′(3)–(5) + B″"; the S5.1 [r3] bracket says "it
consumes (3)+(5) here and B″ (S5.2)"; S11 says "consumes B′(3)+(4) +
B″". Three different lists; an auditor tracking exact dependencies
from any single site gets a wrong (though harmless — every listed
clause IS derived) answer. No mathematical content at stake.
Failure scenario: a future repair round deletes or restates B′(4)
believing it unconsumed per S5.1's list while S5's proof text still
cites it — a dangling cite, not a soundness break.

**[m2] MINOR — two un-inventoried FGMN cites (PE4-F2 species,
descriptive-only uses).** (a) S5.1's [r3] re-scoped residue
paragraph: "by Thm 4.2's normal form H_µ(g) = x₃^s p₃^u
R_{3,β}(g)(y₃)" — content-accurate (checked this pass against layout
L1028–1030 + the eq (16) display: Hµ(g) = ϕ_r(α)R_{r,α}(g)(y_r),
ϕ_r(α) = x_r^{s(α)}p_r^{u(α)}) but absent from S6.1 and from the
supporting-pins paragraph; its use is descriptive only (what w IS —
no lemma conclusion consumes it, since w's unit-hood comes from
B′'s Cor 4.4(1) argument). (b) The [r3] honesty-box sentence
"Verdict-level machine data (prime-split patterns) is w-blind and
constrains ϑ only" implicitly rides "#primes = #distinct residual
factors" — the Lemma 5.3(2)/Thm 6.6 surface; Lemma 5.3 is row 4 but
Thm 6.6 appears only in S8's T2 disclosure prose, no S6.1 row.
Both uses are record-interpretation/description, not proof steps —
hence MINOR, not GAP. Failure scenario: a provenance audit walking
S6.1 alone certifies "every cited statement PDF-verified" and
over-claims by these two.

## E. FRESH ROUTE — FIRST γ₂-LIVE COCYCLE CONTACT ANYWHERE
## (runner gentow2_pe5_fresh.py SEALED at a4b4495 pre-run,
## py_compile only; output committed as-is at 0e816e8:
## GREEN 91 checks / 0 violations, first-run-under-seal)

Both r3-suggested surfaces, on a NEW tower disjoint from every prior
contact (battery, PE1/PE2/PE3 frames, LP1/LD1–LD3): every prior
ϑ-contact had the γ₂ leg silent (z₂ = 1 at LP1) or trivial (f₃ = 1,
or e₂ = 1 forcing Φ′-free normalizers, k = 0).

* **Tower GB** (hand-derived, no pre-seal machine contact): p = 2,
  λ₁ = 1/2, Φ′ = x² − 2, and — the new axis — e₂ = 2 AND f₂ = 2:
  Φ₂ = Φ′⁴ − 4xΦ′² − 32 (ψ₂ = y² + y + 1, λ₂ = 1/4, e(µ₂) = 4,
  u₂ = 5, floor dv₂(Φ₂) = 20). Here z₁ = 1 (f₁ = 1 over q = 2) and
  z₂ = η₂ ∈ F₄∖F₂ — machine: field (4,2), η₂-read a ψ₂-root ≠ 1
  (checks T EF/PSI2/LIVE). ϑ's letter monomial is a PURE z₂-power:
  the γ₂-component acting alone.
* **The cocycle, hand-derived then machine-measured:** n̂₂(21) =
  16Φ′, n̂₂(42) = 1024x (resp. n̂₂(41) = 512Φ′, n̂₂(82) = 2²⁰x at
  e₃ = 2); n̂₂(2u₃)/n̂₂(u₃)² = 4x/Φ′² = γ₂^{−1}γ₁^{−2} → ϑ(0) =
  res(n̂₂(2u₃)/n̂₂(u₃)²)... = z₂^{−1}·z₁^{−2} = z₂² = η₂², ϑ(1) = 1.
  Machine: COC == E2R² at ALL 7 primes across the four frames, with
  COC ≠ 1 (kills per-height/B-2) and COC ≠ E2R (kills the
  wrong-exponent alternative ϑ(0) = z₂). The first direct machine
  measurement of a live γ₂-cocycle leg.
* **Four stage-3 frames** (κ₃ = 21 at e₃ = 1; κ₃ = 41/2 at e₃ = 2;
  floor > 20 each; digits per-height strings):
  * GA1 (1, η₂): per-height ψ₃ = y²+y+η₂ IRREDUCIBLE → would be
    [(4,4)]; B″-realized P = y²+y+ϑ(0)η₂ = y²+y+1 SPLIT — machine:
    2 primes (4,2)×2 ✓, r3-reads hit the two DISTINCT roots
    {η₂, η₂²} ✓ (ROOTPAT).
  * GA2 (1, 1): per-height ψ₃ = y²+y+1 SPLIT → would be [(4,2)×2];
    B″ P = y²+y+η₂² IRREDUCIBLE — machine: single (4,4) ✓, leaf in
    F₁₆ (DEEP4 = 0) ✓, realized residual pinned by MPB: r3²+r3+η₂²
    = 0 ✓. Opposite-direction tooth to GA1: the pair is separated
    ONLY by ϑ(0) = z₂².
  * GA4 (η₂, 1): the top-slot tooth — B″ P = y²+η₂y+η₂² =
    (y+1)(y+η₂²) SPLIT, machine (4,2)×2 ✓ with root pattern
    {1, η₂²} ✓ (pins ϑ(1) = 1: either z₂^{±1} alternative at the
    top slot predicts a single prime).
  * GA3 (1, η₂) at e₃ = 2, f₃ = 2, deg 32 — the FIRST e₃ ≥ 2 ×
    cocycle-live frame anywhere: B″ P = y²+y+1 SPLIT, machine
    2 primes (8,2)×2 ✓, VALS (4,10,41) ✓, ψ₂⁴-signature polygon
    (on-line exactly {0,8,16}) ✓, v₂(Res(Φ₃,Φ₂)) = 164 exact ✓.
* **A-route 35/35 exact** (integer arithmetic only): ladder heights,
  Φ₂-adic exact shapes, one-sidedness at slope 5/2 with the char-2
  ψ₂^{e₃f₃}-square on-line signatures ({0,4,8} resp {0,8,16}),
  Bareiss resultant valuations (8/20/84, 16/40/164, tower 4/10).
  B-route: PARI factorpadic + per-factor nfinit([·,[2]]) +
  idealprimedec + nfeltval + nfmodpr.
* Unscored consistency bonus in the raw lines: GA1's η₂²-root prime
  shows MPC = 1 — the shared root of y²+y+1 and y²+η₂y+η₂², exactly
  as hand-derived; disclosed, not scored.
* Verifier-instrument honesty: all 91 predictions derive from
  B″/r3's law (the object under test) — a RED here would have been
  a refutation of B″, a GREEN is its confirmation at the suggested
  surfaces; the frames also carry law-independent checks (polygons,
  values, resultants) that certify the frames themselves are
  in-recipe.

## G. GRADE LINE

**passPE5 verdict: CLEAN (0 CRITICAL, 0 GAP, 2 MINOR: m1
consumption-list inconsistency, m2 two un-inventoried descriptive
cites — Thm 4.2 normal form, Thm 6.6). LEMMA GENTOW2-B″ survived
independent re-derivation AND its first γ₂-live + e₃ ≥ 2 machine
contacts (91/0 under seal, opposite-direction teeth, exact cocycle
exponent z₂²); the restated LEMMA GENTOW2-B and the ψ₃^{(w)}
admissibility argument are exact; the honesty box, the PDF rows
20–22, and the machine legs all held. ACCEPTANCE COUNTER: 0/2 → 1/2
(this pass CLEAN; one further clean pass on a fresh surface required
for acceptance). Repair guidance (MINOR-grade, no bar to the
counter): (i) unify the three B′-consumption lists (the true set for
the S5 proof step is B′(3) + B″, with (5) the summation display and
(4) consumed only by the per-height-iff sentence); (ii) fold Thm 4.2
(normal form, descriptive) and Thm 6.6 (verdict ↔ factor-pattern
surface) into S6.1 or the supporting-pins paragraph with the two-leg
method. Candidate fresh surfaces for the acceptance pass: a
mixed-letter frame (z₁ ≠ 1 AND z₂ ≠ 1 simultaneously — ϑ with BOTH
exponents live; every contact so far had at most one letter live),
f₃ ≥ 3 letter-live (three slots, ϑ(0) at d = 3), or p odd γ₂-live
(all cocycle contacts so far are p = 2). Fresh-route artifacts:
verification/openmath/gentow2_pe5_fresh.py (seal a4b4495) +
gentow2_pe5_fresh_output.txt (GREEN 91/0, first-run-under-seal,
committed as-is at 0e816e8).**
