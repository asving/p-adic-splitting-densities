# GD23 — the GD dictionary unit (GD-2 ⊕ GD-3): frame/valuation tie, development identity, residual dictionary, over an ARBITRARY complete DVR — verification brief REV 4

You are verifying a mathematics note. Quote each offending passage and
classify it as *critical error* (breaks the logical chain) or *justification
gap* (assume and continue); fix nothing. This is REV 4, repairing the
model-diverse pass-2 hostile verification of rev 3
(`GD23_pass2_fable_verify.md`; Claude Fable 5, fresh context:
SOUND-WITH-GAPS — 0 critical errors, 9 justification gaps F1–F9, none
breaking the chain at the displayed perimeters; dispositions in §0-pre-4).
Rev 3 had repaired the V28
verification of rev 2 (`V28_gd23r2.jsonl`; Codex, fresh context:
SOUND-WITH-GAPS — 2 justification gaps, 0 critical errors, both in the FGMN
citation layer of (GR-B); dispositions in §0-pre-3), which in turn had
repaired the
V26 hostile verification (`V26_gd23.jsonl`; Codex, fresh context: UNSOUND —
3 critical errors, 4 justification gaps) of the initial brief
(`GD23_phaseB_verifybrief.md`, self-labeled rev 0). V26 explicitly
CONFIRMED: the arbitrary-DVR order-≤1 layer ("Order-≤1 valuation/key/residual
layer: no critical error found"), DEVID ("DEVID as a conditional theorem from
a genuine valuation plus the stated minimality block: sound"), and the
generality audit ("Arbitrary-DVR/no-Teichmüller claim for those local proofs:
sound" — no proof uses completeness, perfectness, or a multiplicative
section). Everything confirmed is carried byte-stable: within §§3–4 the ONLY
edits are the POS-g repair (V26-4) and two appended rev-2 notes (after
KEY-g's proof and after DEVID's proof — consumption remarks, no statement or
proof text touched); DEV-g, GAUSS-g, VAL-g, KEY-g, RES-1, DEVID, UNIQ and
their statements and proofs are verbatim rev 0.

V28 additionally CONFIRMED, and rev 3 carries byte-stable: the (KPirr) +
BRIDGE recomposition against MacLane's printed Def 4.1 ("no missing
irreducibility premise remains"), the presence of the anchored-march
requirement at every statement and consumption site (GD-3 full/slot law,
(GR-B)(2′), GD3-FULL/OL-B, IFACE(c), the ledger), and the per-clause
interface scoping ("honestly restricted ... no surviving uniform-any-O
claim"). Rev 3 touches ONLY the V28 repair sites listed in §0-pre-3 — the
(GR-B) anchor/residual notation split with its new exact-match display (§2
pointer, §7.2, §7.3, §7.4(c)) and clause (4)'s properness proviso — plus
the §10/§11 ledger rows; everything else is byte-stable rev 2.

The pass-2 verification additionally CONFIRMED — recomputed, not merely
read — and rev 4 carries byte-stable: VAL-g steps (1)–(4); KEY-g(ii)
including the e = 1 corner and the primeness claim ("KEY-g(ii) does prove
primeness outright, i.e. (KPirr) at j = 0 as claimed"); KEY-g(iii)'s
ord-transport; DEVID ("no primeness is consumed anywhere in the proof");
BRIDGE including both ultrametric forcing steps and the representative-lift
step; the V1-tower recomposition and its index bookkeeping; the POS-g
division control; the (2′) two-form internal consistency, eq.-(16)
arithmetic, and the order-1 instance; the properness proviso's
automaticity/genuineness split; the absence of Route-A circularity through
(V2c); the arbitrary-DVR quantifier sweep (no completeness, perfectness, or
multiplicative section in any proof); the §1.2/§6 index-dictionary
constants; and the forall-by-example scan. Rev 4 touches ONLY the F1–F9
repair sites listed in §0-pre-4 — every repair is a definition display, a
bounding-datum display, a declared-difference or declared-import display,
an appended note, or a summary-layer ledger correction; NO confirmed
statement or proof text is altered except at the two displayed proof sites
named in §0-pre-4 (GD3-MIN Route B; the (GR-B)(4) proviso sentence) and
the (GR-B) operator bullet itself (the OPEN input's display, where F1's
definition now lives) — all three inside the declared-open (GR-B)
package/consumption path — and everything else is byte-stable rev 3.

Between the revisions the owed literature unit ran
(`LITUNIT_2026-08-01.md`, 2026-07-31): every citation flag of rev 0 (T-1,
T-2, ML-scope, the (V1)/(V2) attributions, the FGMN pin) resolved **MATCH**
against the printed sources, with verbatim quotes at named extraction lines,
and rev 0's attack items (5) and (6) were RESOLVED by the printed texts.
Rev 2 consumes those pins; re-checking any quoted statement against the PDFs
is a legitimate verification move, but the loci are no longer flags.

Standing declarations (not findings; an UNDECLARED dependence is): the
(GR-B) package at orders ≥ 2 and the (KP) clauses beyond order 1 (owned by
GD-1) are DECLARED open inputs; every engine-conformance statement (u = w
for the implemented engine) is a DECLARED non-claim. Status protocol:
PROVED-UNVERIFIED until hostile fresh-context passes accept it.

## 0-pre-4. Rev-4 changelog (pass-2 findings F1–F9 → dispositions → repair sites)

| finding | class | disposition | repair site |
|---|---|---|---|
| F1: Route B / GD3-FULL apply (GR-B)'s operators outside their displayed domain; "side-supported" undefined — vacuity risk if it means anything beyond homogeneous | gap | REPAIRED — "side-supported" is DEFINED (§7.2 operator bullet): a nonzero homogeneous δ ∈ gr_α whose monomial support lies on the weight-α lattice line — which EVERY nonzero homogeneous element satisfies (the adjective names an automatic property; the intended reading was the vacuous one, matching the printed operators' totality in g ≠ 0); the operators are declared TOTAL on nonzero homogeneous elements; domain membership is PROVED at every application: in(f) homogeneous by construction, and β, ψ̂^kβ homogeneous by the displayed Lemma HOM-FACT (divisors of nonzero homogeneous elements in a ℤ-graded domain are homogeneous), GD3-FULL's division is by an invertible monomial in gr, no operator fired | §7.2 operator bullet (definition + totality); §7.3 Route B (HOM-FACT + membership display); §7.3 GD3-FULL (domain note); §10 attack item (9) |
| F2: the exact-match dictionary row R^{gr}_α ↔ R_{r,α} silently consumes that the printed R_{r,α}(g) depends on g only through H_µ(g) — a fourth undeclared difference beyond (a)/(b) | gap | REPAIRED — declared as intentional difference **(c)** in the §7.2 match display: the printed operators take the POLYNOMIAL g, (2′)'s take the graded element in f; the factor-through-H_µ(g) property is a lemma-shaped fact about the printed Def 3.16 (its coefficients are residues of the attained digits, exactly the data of H_µ(g)), carried inside (GR-B)'s transcription residue, named in §10/§11 | §7.2 match display ("only intentional differences" list); §10 Open paragraph; §11 FGMN row; §10 attack item (10) |
| F3: TIE's policy-pin hypothesis (engine keys = GMN representatives) dropped at the summary layer — §0 paragraph, status-table row, §8 OL-2(a) row advertise the three transcriptions only; REC-c1/GD3-MIN Route A inherit it | gap | REPAIRED — the policy pin is re-advertised at every summary site: §0 paragraph, status-table TIE row (REC-c1/GD3-MIN rows read "as TIE" and inherit), §8 OL-2(a) row, §10 "proved modulo" paragraph; §6's statement was already honest (unchanged) | §0; status table; §8; §10 |
| F4: Route B's "m := ord, well-defined: gr a domain, §1.3 (g3)" never displays what bounds ψ̂-chains at general order (a domain alone does not bound divisibility chains) | gap | REPAIRED — the bounding datum is DISPLAYED as (BOUND) inside Route B: ψ̂^k \| in(f) ⟹ k·deg Φ_{i+1} ≤ deg f, proved by re-running DEVID's pattern ((V1) + (KPa)-block at the state — available inside (GR-B)'s setting); at order 1 the bound is VAL-g's explicit ring | §7.3 Route B; §10 attack item (9) |
| F5: KEY-g(iii)/RES-1 consume (R1)/(R2), ℓ(·), the side tuple, and R_λ as external definitions with only a gloss — "PROVED" conditional on the O-1thr/GMN definitions matching the gloss | gap | REPAIRED — the consumed vocabulary is DISPLAYED (§2, "Imported side vocabulary"): development points, side data (e, h, s0, d), line height ℓ, (R1)/(R2), R_λ, exactly as the §3 proofs read them; the identification of the display with O-1thr §2.5's (R1)/(R2) and GMN (L3)'s R_λ is a DECLARED transcription import (recheckable, same status as the LITUNIT pins) | §2 (new display after the opening paragraph); §10 |
| F6: VAL-g step (1) applies DEV-g to dividends a_t b_s with possibly NEGATIVE contents (A, B ∈ K[x]), outside DEV-g's O[x] hypotheses | gap | REPAIRED — the one-line π-power clearing is displayed as an APPENDED NOTE after VAL-g's proof (statement and proof text untouched, preserving the V26/pass-2 byte-stability of §3): rescale A, B into O[x], every quantity shifts by the exact displayed constants, conclusions descend | §3 (appended rev-4 note after VAL-g) |
| F7: the properness proviso fires FGMN's Lemma-5.3 remark on a chain in which Φ_{i+1} is not yet a chain key — the augmentation/chain-completion step is consumed silently | gap | REPAIRED — the completion step is DISPLAYED inside (GR-B)(4): first augment w_{i+1} at Φ_{i+1} (an augmented valuation exists at any admissible value by MacLane Thm 4.2, whose hypotheses the proviso's chain condition + (KP) at the state supply), making (Φ₀, …, Φ_{i+1}) a MacLane chain of the augmented valuation with Φ_{i+1} the key at position i+2; THEN the remark applies | §7.2 (GR-B)(4) proviso sentence |
| F8: §1.3 asserts π̄ invertible at bare-semivaluation generality (w(π^{−1}) = −w(π) is an extra property, not a consequence of superadditivity) | gap | REPAIRED — the vocabulary display is RESCOPED: invertibility of π̄ is claimed exactly where exact π-scaling holds (every valuation; every w_i via the definition/POS-g(ii)), with the over-claim retracted and the consumer audit displayed (every gr consumer fires at a valuation or at a w_i) | §1.3 |
| F9: §5's Def 4.1(i) "⟺" chain skips homogeneous-prime ⟹ ring-prime in the decorative direction | gap | REPAIRED — an APPENDED NOTE after V1-tower displays the standard graded-domain lemma (a homogeneous element prime on homogeneous elements is prime: top-component + induction) closing the decorative direction; the consumed direction ((KPirr) ⟹ ring-prime ⟹ homogeneous-prime ⟹ Def 4.1(i)) was already sound as displayed | §5 (appended rev-4 note) |

Churn classification: NO statement of the mathematical chain changes; F1,
F4, F7 add displayed definitions/lemmas at the (GR-B) statement bullet
(the open input's own display) and at the two proof sites inside the
declared-open (GR-B) consumption path (Route B; the proviso); F2, F5 are
declared-difference/declared-import displays; F3 is summary-layer ledger
accuracy; F6, F9 are appended notes; F8 is a vocabulary-display rescope.
No confirmed proof is touched.

## 0-pre-3. Rev-3 changelog (V28 findings → dispositions → repair sites)

| V28 finding | class | disposition | repair site |
|---|---|---|---|
| 1: "modeled verbatim" conflated FGMN's two printed normalizations — Thm 4.2 pairs the WEIGHT-indexed anchor ϕ_r(α) with R_{r,α} and the SIDE-anchored ϕ_r(g) with the ordinary R_r; rev 2's (2′) paired ϕ_{w(f)} with the GMN-matching residual, and the mixed-pairing discrepancy is a ȳ-power (ȳ^{j₀}, j₀ = ⌊s₀(f)/e_i⌋), not a scalar | gap | REPAIRED + MATCH VERIFIED — (2′) restated with BOTH printed pairings and their exact relation ϕ(f) = ϕ_{w(f)}·ȳ^{j₀} displayed; the anchor/residual systems are split into the ϕ_α/R^{gr}_α and ϕ(f)/R^{gr} pairs; an exact-match check against the printed Thm 4.2 (verbatim, with Lemma 4.1 / Defs 3.16, 3.18 / Cor 4.4(3) and eq. (16) of the printed proof) is executed and displayed in §7.2; the "modeled verbatim"/"only the three caveats" claims are re-made OF THE CORRECTED two-form statement, not of rev 2's mixed one | §2 GD-3(full) pointer; §7.2 anchor + operator bullets, (2′), NEW match display; §7.3 GD3-FULL/OL-B anchor; §7.2 order-1 instance; §7.4(c); §10 items (3)/(8); §11 |
| 2: properness required by Prop 5.14/Cor 5.15 asserted from the degree law alone; Def 5.10's parameters are the intrinsic e_r(µ), m_r(µ) and the printed characterization is Cor 5.12's chain condition (φ ≁_µ φ_r), which neither (KP) nor the degree law contains | gap | REPAIRED — properness is now a DISPLAYED PROVISO inside (GR-B)(4): the extended tower chain (Φ₀, …, Φ_{i+1}) is a MacLane chain (Def 3.1's Φ_{j+1} ≁_{w_{j+1}} Φ_j — automatic at degree-increasing reads, a GENUINE side condition at e_j·g_j = 1 refinement reads), whence FGMN's Lemma-5.3 remark gives properness; proviso owned with (KP) by GD-1; LITUNIT §5's properness-check sentence SUPERSEDED (appended note there) | §7.2 (GR-B)(4); §10 Open paragraph + attack item (8); §11; LITUNIT §8 (appended) |

Note: rev-2 changelog rows 2 and 7 below retain their original wording
("modeled verbatim on FGMN Thm 4.2", "restated in the shape FGMN actually
proves") as HISTORY of the rev-2 dispositions; V28-1 found that wording
inaccurate for rev 2's mixed pairing, and it is superseded by row 1 above —
the shape now displayed is the two-form identity as printed.

## 0-pre. Rev-2 changelog (V26 findings → dispositions → repair sites)

| V26 finding | class | disposition | repair site |
|---|---|---|---|
| 1: (KPa) as displayed gives minimality but never primeness of ψ̂_hom; the composition (KP)+MacLane Thm 4.2 ⟹ (V1) does not meet the imported theorem's hypotheses; "No other input" was false | critical | REPAIRED — the missing premise is DECLARED as a new (KP) clause | §2: **(KPirr)** added to the (KP_j) package (proved at j = 0 by KEY-g(ii)); §5: Lemma BRIDGE (proved) + the recomposed deduction against MacLane's printed Def 4.1(i)/(ii)/(iii); §4 rev-2 note: DEVID consumes only the block, NOT (KPirr) |
| 2: "one anchor division absorbs ALL slot twists" does not follow from per-slot twist data — τ_κ = a^κ sends 1+z to 1+az, no scalar; FGMN proves an initial-form identity, not the absorption | critical | REPAIRED — the deduction is DELETED; the one-anchor form is moved INTO the open input as the anchored-march clause (2′), modeled verbatim on FGMN Thm 4.2 | §2: GD-3(full)/(slot law) statements now carry the anchored-march normalization; §7.2: (GR-B)(2′) + both countermodels displayed (V26's 1+z ↦ 1+az; CU-1's 1,818× slot-fresh refutation); §7.3: GD3-FULL proof rewritten as an unwinding |
| 3: IFACE advertised "any O" while its polygon clause rides REC-c1, which is scoped to TIE = GMN's scope | critical | REPAIRED — rescoped per clause (rescope authority per the standing protocol; no consumer needed the composite at "any O") | §0 status row; §7.4 per-clause scope split |
| 4: POS-g's all-order sub-multiplicativity points at VAL-g's "≥" paragraph, which is the order-1 Φ₀-calculation only | gap | REPAIRED — the unconditional claim is RESCOPED to orders ≤ 1; the general-order step is now DISPLAYED, conditional on exactly the tower's own package | §3: Lemma POS-g rev 2 (clause (iv) + the division-control display) |
| 5: T-1 plausible but an unverified transcription; exact GMN normalization needed | gap | DISCHARGED to pins — GMN Prop 2.7(1)+(3)+(4) with Lemma 2.2(2), quoted verbatim (incl. the e_{r−1}-rescaled displayed form, GMN's own normalization) | §6 (T-1 bracket); §11; LITUNIT §1 |
| 6: T-2's consumed FIRST-attainment form not pinned by a length identity alone | gap | DISCHARGED to pins — ω-definition (§2.1) + Lemma 2.17(2)(3) + Def 1.8; first attainment displayed in the printed proof of 2.17 via Prop 2.8 ("the least abscissa with u_{i₀} = u") | §7.3 Route A; §11; LITUNIT §2 |
| 7: "MacLane §§9–12 / FGMN 2015" does not pin the bespoke four-clause (GR-B) interface | gap | REPAIRED + DISCHARGED — (2′) is restated in the shape FGMN actually proves (Thm 4.2's identity), and every clause now has a printed home; the residual conditionality (the GMN Defs 2.20–2.21 normalization dictionary) stays declared INSIDE the open package | §7.2 per-clause citations; §11; LITUNIT §5 |

Churn classification: findings 1, 2 are statement-level repairs of the
note's own displayed inputs (the theorems conditional on them are unchanged);
finding 3 is a pure rescope; finding 4 is a rescope plus a new displayed
conditional proof; findings 5–7 are citation-status upgrades (no text of the
mathematical chain changes). No confirmed proof is touched.

## 0. What is claimed, in one paragraph

Over an arbitrary complete DVR O (fraction field K, normalized valuation v,
residue field F — perfectness of F carried as a standing hypothesis for the
consumers, USED NOWHERE in this brief's proofs, §1.1), the engine/GMN seam's
mathematical middle term is the MacLane augmented-valuation chain w_i (§1.2).
The brief proves, outright and self-contained at the displayed perimeters:
the full order-≤1 layer at EVERY root-key degree d₀ (Lemmas DEV-g/POS-g/
GAUSS-g, Theorems VAL-g/KEY-g/RES-1 — generalizing O-1thr's VAL1/KEY1/DIV1
from d₀ = 1 over ℤ_p to any d₀ over any O, and closing the order-1 residual
dictionary in full, twists absent); the **development identity at EVERY order**
(Theorem DEVID — the (V2b) family member, proved from (V1) + the key-minimality
block by a graded argument that replaces DIV1's slot-chase and is degree- and
ring-free); the uniqueness half of the frame tie (Lemma UNIQ) so that the
MacLane↔GMN identification w_i = v_{i+1} (Theorem TIE) rests on exactly three
GMN transcriptions — ALL THREE now pinned to verbatim printed statements
(LITUNIT §§1, 4) — PLUS the §6 policy pin (the engine/O-1thr keys ARE the
GMN representatives of the truncated types; GD-1/OL-1(b) territory, a
displayed hypothesis of TIE inherited by REC-c1 and GD3-MIN Route A —
re-advertised here at rev 4, F3) — and the stage-polygon/record dictionary (Corollary REC-c1)
as a corollary shape. GD-3's general-order residual dictionary is split: the
MIN CORE (all the recut cluster consumes, per V25) gets TWO independent
routes — one through a pinned GMN lemma (T-2), one through the displayed
residue-structure input (GR-B); the FULL form and the OL-B slot-bijection law
are proved FROM (GR-B), whose order-1 instance is proved here (RES-1), whose
statement now CARRIES the anchored-march normalization (the load-bearing
clause (2′), §7.2 — slot-fresh readings are refuted by two displayed
countermodels), and whose general form is the note's one genuinely open
input, cited clause-by-clause (MacLane Thm 10.2/Lemma 10.1/Lemmas 11.1–11.2/
Thm 12.1/Cor 12.2; FGMN Thm 4.2/Cor 4.4/Thm 4.8/Cor 4.9/Cor 5.6(2)/
Lemma 5.13/Prop 5.14/Cor 5.15). The tower composition (V1) beyond order 1 now
consumes the RESTRUCTURED key package (KPa)+(KPirr)+monicity — rev 0's claim
that (KPa) alone supplied MacLane irreducibility was V26-1's critical error —
through the proved graded↔equivalence-divisibility bridge (Lemma BRIDGE, §5)
and MacLane Thm 4.2 (printed scope: any field, any value; ML-scope flag
CLEARED, LITUNIT §3). The engine-conformance half of every carrier statement
(that the ENGINE's emitted records are computed by these formulas) is NOT
claimed — it is displayed as the GD-4/GD-6/S-1 residue, and the brief instead
proves the interface-instantiation clause AT ITS PER-CLAUSE SCOPES
(Corollary IFACE, §7.4 — rev 0's uniform "any O" advertisement was V26-3's
scope error).

**Status table.**

| claim | scope | status |
|---|---|---|
| DEV-g, GAUSS-g | any O, any d₀ | PROVED (§3) |
| POS-g (i)–(iii) | any O, any order | PROVED (§3) |
| POS-g (iv) sub-multiplicativity | any O | PROVED order ≤ 1; beyond: given (V1)_i + (KP_{i−1}) via the displayed division control (§3) — subsumed by (V1)_{i+1} |
| VAL-g: w₁ valuation, gr = K₀[π̄^{±1}][Φ̄₀] UFD | any O, any d₀ | PROVED (§3) |
| KEY-g: standard lift key laws + min core at order 1 | any O, any d₀ | PROVED (§3) — incl. PRIMENESS, i.e. (KPirr) at j = 0 |
| RES-1: order-1 residual dictionary, full form, OL-B order-1 | any O, any d₀ | PROVED (§3) |
| DEVID: development identity, every order | any O | PROVED given (V1)+(KPa-block) at that order (§4; consumes NO primeness) |
| Lemma BRIDGE: equivalence-divisibility = graded divisibility | any O | PROVED (§5) |
| V1-tower: every w_i a valuation | any O | PROVED order ≤ 1; beyond: (KPa)+(KPirr) [GD-1] + BRIDGE + MacLane Thm 4.2 [pinned; printed scope CLEARED] (§5) |
| UNIQ (tie uniqueness) | any O | PROVED (§6) |
| TIE: w_i = v_{i+1} (GMN) | GMN's scope (K/ℚ_p finite) | PROVED modulo pinned transcriptions (L2)/(L4-P2.7)/T-1 [ALL PINNED — LITUNIT §§1,4] AND modulo the §6 policy pin (engine keys = GMN representatives; GD-1/OL-1(b)) — rev 4, F3; rows reading "as TIE" inherit BOTH (§6) |
| REC-c1: window polygon = N_{i+2}(f), record verbatim (c1) | as TIE | corollary of TIE + DEVID (§6) |
| GD3-MIN (general order) | route A: as TIE; route B: any O | route A: TIE + T-2 [PINNED]; route B: (GR-B) (§7) |
| GD3-FULL + OL-B (general order) | any O | PROVED from (GR-B) — whose anchored-march clause (2′) carries the content (§7; displayed open/cited input) |
| IFACE (a): u ℤ-valued/mult/ultrametric + (S-K1) | any O, given (V1)+(KPa-block) | PROVED (§7.4) |
| IFACE (b): stage window polygon = N_{i+2}(f) | GMN's scope (rides REC-c1) | as REC-c1 (§7.4) — rev 0's "any O" row was V26-3 |
| IFACE (c): dig/residual laws ([F.1]/[F.2], anchored march) | any O | = (GR-B); order 1 PROVED (§7.4) |
| engine conformance (u = w for the implemented engine) | — | NOT CLAIMED (GD-4/GD-6/S-1 residue; §8) |

## 1. Setting

### 1.1 The base ring, and where each standing hypothesis is used

O: a complete discrete valuation ring, π a uniformizer, v the normalized
valuation (v(π) = 1), K := Frac O, F := O/πO. Standing hypotheses per the unit
spec: O complete, F perfect. **Usage display (attackable):**

* **Completeness is used in NO proof of this brief.** Its consumers are the
  GMN factorization theorems (Thms 1.15/1.19/3.1/3.7 — splitting f into
  factors needs Hensel-type completeness), which this brief cites only inside
  the route-A input T-2 and nowhere else. Every PROVED statement below
  holds over any DVR.
* **Perfectness of F is used in NO proof of this brief.** The residue tower
  K_{i+1} = K_i[z]/(ψ_i) is a tower of fields for any F. Perfectness is
  carried because the project's consumers (separability of residual counts,
  the BB3/L3 finite-field layer) assume it, and because GMN's own scope is
  F finite. It costs nothing to carry.
* Consequently the statements INSTANTIATE at O = ℤ_p, at O = O_δ = W(F_{p^δ})
  (the unramified re-base of D-3/O-8b — **this is the D-3 kill**: T-1-style
  stage laws over O_δ are instances, not a transfer theorem), and at
  equal-characteristic O = F_q[[t]] (exercised by the falsifier, §9).
* GMN's own theorems remain scoped to K/ℚ_p finite (their standing hypothesis,
  `GMN_citations.md` audit-fix); the TIE/T-2 clauses are therefore claimed at
  GMN's scope, and their arbitrary-DVR generalization rides the (GR-B)/FGMN
  pin (FGMN 2015 works over an arbitrary discretely valued field — scope
  PINNED verbatim, §11 and LITUNIT §5).

### 1.2 The tower: policy, frames, the MacLane chain, and the three-term seam

Transcribed from O-1thr rev 4 §1.1 with ℤ_p → O throughout (the transcription
is verbatim except v_p → v, p → π, 𝔽_p → F).

* **Policy 𝒫**: (i) to each monic irreducible φ̄ ∈ F[X], a monic lift
  Φ₀ ∈ O[X] with deg Φ₀ = deg φ̄ =: d₀; (ii) to each lawful node ν over a
  frame of order i, a monic key Φ_{i+1} ∈ O[X] with
  deg Φ_{i+1} = e_i·g_i·deg Φ_i. All statements are relative to one fixed 𝒫.
* **Order 0**: w₀(B) := min_j v-content(c_j) over the Φ₀-adic development
  B = Σ_j c_j Φ₀^j (v-content := min of coefficient valuations);
  K₀ := F[z]/(φ̄). (Lemma GAUSS-g: w₀ = the plain Gauss valuation, and a
  valuation.)
* **Order i+1**: given the frame (Φ_i, w_i, K_i) and a read ν_i with data
  (e_i, h_i) coprime ≥ 1 and ψ_i ∈ K_i[z] monic irreducible of degree g_i with
  ψ_i(0) ≠ 0: set γ_{i+1} := e_i·w_i(Φ_i) + h_i and
  w_{i+1}(B) := min_j ( e_i·w_i(c_j) + j·γ_{i+1} ) over the Φ_i-adic
  development; K_{i+1} := K_i[z]/(ψ_i). E_i := w_i(π) = e₀e₁⋯e_{i−1};
  γ̂_i := w_{i+1}(Φ_{i+1}).
* **The MacLane display.** w_{i+1} is EXACTLY MacLane's augmented value
  [e_i·w_i ; w_{i+1}(Φ_i) = γ_{i+1}]: the augmentation of the (rescaled)
  order-i valuation at its own key Φ_i, assigning Φ_i the strictly larger
  value γ_{i+1} > e_i·w_i(Φ_i) (h_i ≥ 1). This display is what closes the
  V24-G6 "owed correspondence" for (V1): the §1.1 chain of O-1thr IS the
  MacLane composite, now said with the augmentation exhibited (§5).
* **The three-term seam.** Three valuation-like objects appear at the
  carriers: **u** (the engine's [F.0] side valuation — an axiom of the stage
  interface, O-2a §4), **w** (the MacLane chain above — a mathematical
  object), **v_r** (GMN's order-r valuation, (L2)). This brief proves
  statements about w and about w = v (TIE); the u = w half is engine
  conformance (the intended proof routes of OL-2/OL-5/OL-6, owned by
  GD-4/GD-6 and the S-1 transcription audit) and is NOT claimed here. What
  IS proved here about u's axioms: they are SATISFIED by w (Corollary IFACE,
  §7.4, at the per-clause scopes displayed there) — "the engine's axiom
  holds at the GMN valuation" (SYNTH4 §1, GD-2 note).
* **Index dictionary (a standing transcription hazard, displayed).** O-1thr's
  0-based reads vs GMN's 1-based orders: Φ_i ↔ φ_{i+1}, w_i ↔ v_{i+1},
  (e_i, h_i) ↔ (e_{i+1}, h_{i+1}), K_i ↔ F_{i+1}, ψ_i ∈ K_i[z] ↔
  ψ_{i+1} ∈ F_{i+1}[y]. Constants check: w_i(π) = E_i = e₀⋯e_{i−1} =
  e₁^{GMN}⋯e_i^{GMN} = v_{i+1}(π) — matching (L2)'s index e₁⋯e_{r−1} at
  r = i+1. O-2a's stage at level i+1 speaks GMN indexing (its constant test
  u_{i+1}(π) = E_i is the same number).

### 1.3 Graded vocabulary

For a function w : K[x]∖{0} → ℤ with w(AB) ≥ w(A) + w(B) and
w(A+B) ≥ min(w(A), w(B)) (a semivaluation): P_γ := {A : w(A) ≥ γ} ∪ {0},
P_γ⁺ := {A : w(A) > γ} ∪ {0}, gr(w) := ⊕_{γ∈ℤ} P_γ/P_γ⁺ (graded pieces over
K[x]). in(A) := the class of A in gr_{w(A)}. π̄ := in(π) is invertible
WHENEVER w scales π-multiples exactly — w(π^k B) = k·w(π) + w(B) for all
k ∈ ℤ — since then w(π^{−1}) = −w(π) and in(π)·in(π^{−1}) = in(1) = 1.
[REV 4, F8: for a BARE semivaluation this exact scaling is an extra
property, not a consequence (superadditivity gives only
w(1) ≥ w(π) + w(π^{−1})); rev 3's parenthetical over-claimed it at
semivaluation generality. It holds for every VALUATION and for every w_i
of §1.2 (the development definition gives w_i(π^k B) = k·E_i + w_i(B)
directly — DEV-g slot bookkeeping/POS-g(ii), valid for k ∈ ℤ by clearing).
Consumer audit, displayed: every consumer of gr below fires it either at
a valuation (DEVID, BRIDGE, GAUSS-g/VAL-g) or at a w_i (POS-g's division
control), so no statement rested on the over-claim.]
Facts used (displayed, elementary): (g1) if each w(x_j) = γ and
w(Σ x_j) > γ then Σ in(x_j) = 0 in gr_γ; (g2) w is a VALUATION
(w(AB) = w(A)+w(B) always) iff in(A)·in(B) = in(AB) ≠ 0 for all A, B, iff
gr(w) is a domain; (g3) ord_δ(α) := max{k : δ^k | α} is well-defined in a
domain for homogeneous δ of positive weight-or-degree data bounding the chain.

## 2. The merged statements (what GD-2 ⊕ GD-3 assert)

Fix a policy 𝒫 and a lawful chain of reads ν₀, …, ν_i building frames through
order i+1 as in §1.2, and let f ∈ O[x] be monic with the creating read ν_i
satisfying, on f, (R1) at all slots and (R2) at its strides (the standing
WGEO hypothesis of O-1thr §2.5 — side support with equality only at strides,
and exact digit pins at the strides; at a root state, ROOT-M's
ord_{φ̄}(f̄) = m ≥ 2 setting).

**Imported side vocabulary, displayed (rev 4 — F5).** The (R1)/(R2)/ℓ/side
vocabulary the §3 proofs read is displayed here at the ROOT-read instance
— the only instance any proof of this brief reads DEFINITIONALLY
(KEY-g(iii), RES-1). At general order the vocabulary appears inside
statement hypotheses only (GD-3, (GR-B)(2′) — read one level up against
the §1.2 sheared window, transcription status riding the (GR-B) package),
and general-order residuals enter proofs only as GMN's PINNED Def-2.21
objects through the T-2/(2′) pins, never through this display:

* *Development points.* f = Σ_t b_t Φ₀^t the Φ₀-adic development
  (b_t ∈ O[x], deg b_t < d₀); the level-0 cloud is {(t, w₀(b_t)) : b_t ≠ 0}.
* *The λ-side data (e, h, s0, d).* λ = −h/e in lowest terms (e, h ≥ 1
  coprime); s0 = the declared initial abscissa, u₀ = the declared initial
  height, d = the stride count: the declared side runs from (s0, u₀) to
  (s0 + ed, u₀ − dh). ((R1)+(R2) below are what make the declared side an
  ACTUAL side of the lower hull of the cloud, endpoints vertices — the
  data are the read's declaration, the conditions are on f.)
* *Line height ℓ(t).* The affine function of the side's line:
  ℓ(s0 + eκ) = u₀ − κh at the strides t = s0 + eκ, 0 ≤ κ ≤ d (off-strides
  ℓ(t) ∉ ℤ, since gcd(e, h) = 1); the side's right-end height is
  ℓ(s0 + ed) = u₀ − dh. Note e·ℓ(t) + t·h = e·u₀ + s0·h, constant along
  the line.
* *(R1) (side support).* w₀(b_t) ≥ ℓ(t) for EVERY t — whence
  e·w₀(b_t) + t·h ≥ e·u₀ + s0·h always, with equality forcing
  w₀(b_t) = ℓ(t) ∈ ℤ, i.e. t an on-line stride ("equality only at
  strides" is integrality, not an extra axiom).
* *(R2) (stride digit pins).* Both endpoint digits attain the declared
  heights: w₀(b_{s0}) = u₀ and w₀(b_{s0+ed}) = u₀ − dh.
* *Digit classes and the residual.* c_κ := [b_{s0+eκ}/π^{ℓ(s0+eκ)}] ∈ K₀
  (residue of the rescaled stride digit mod (π, Φ₀); c_κ = 0 exactly when
  the κ-th stride digit sits strictly above the line, possible only at
  0 < κ < d); R_λ(f) := Σ_{κ=0}^{d} c_κ z^κ ∈ K₀[z], with c₀ ≠ 0 ≠ c_d —
  so deg R_λ(f) = d and R_λ(f)(0) ≠ 0.

KEY-g(iii)'s first proof sentence ("the w₁-minimizing slots are exactly the
on-line strides, with digit classes the residual coefficients") and RES-1
read EXACTLY this display (with g′ = d in KEY-g's hom notation; β = the
right-end height u₀ − dh). DECLARED IMPORT (the F5 residue, same status as
the LITUNIT pins — recheckable, not a flag): that this display IS O-1thr
§2.5's (R1)/(R2) (the standing WGEO hypothesis) and GMN (L3)'s order-1
residual R_λ. No proof below consumes more of those sources than the
display.

**GD-2 (frame/valuation tie + development identity).**
(a) each w_j (j ≤ i+1) is a valuation on K[x], ℕ-valued on O[x]∖{0}
    ((V1)); and w_j = v_{j+1}, GMN's order-(j+1) valuation over the same
    (policy-pinned) representatives — the engine ℤ-normalization being the
    SAME normalization (no rescaling; §1.2 index dictionary);
(b) the state key's development computes the state valuation from below:
    for every nonzero B ∈ K[x], with B = Σ_k C_k Φ_{i+1}^k the
    Φ_{i+1}-development, w_{i+1}(B) = min_k ( w_{i+1}(C_k) + k·γ̂_i ) ((V2b));
(c) hence the state window polygon — the lower hull of the sheared points
    (k, u_k), u_k := w_{i+1}(C_k) + k·γ̂_i, of f's Φ_{i+1}-development — IS
    GMN's N_{i+2}(f), vertex for vertex, and the (c1) side data
    (e, h, ℓ, s, u) of any principal side agree verbatim in the order-(i+2)
    integer frame.

**GD-3 (residual dictionary; full form and slot law RESTATED at rev 2 to
carry the anchored-march normalization — V26-2).** With ψ̂_hom :=
in(Φ_{i+1})·(unit monomial)^{−1} the key initial form ((KPa)):
(min core) ord_{ψ̂_hom}(in(f)) = ord_{ψ_i}(R_λ(f)) — the level-i residual
    polynomial computes the ψ-order of the initial form ((V2c); all the recut
    cluster consumes, per V25's split);
(full form) in the ANCHORED-MARCH normalization — [F.2]'s anchor-unit
    division with the slot-κ normalizer marching as anchor·(step)^κ through
    the (GR-B)(2′) monomial system, the anchor being the SIDE-anchored
    ϕ(f) (keyed to the λ-side's initial vertex), NOT the weight-only
    ϕ_{w(f)}: the two differ by the ȳ-power ȳ^{⌊s₀/e_i⌋} (§7.2 — V28-1);
    slot-FRESH per-slot normalizations are
    REFUTED (§7.2) — the stage residual satisfies R^{nrm} = c·R_λ(f),
    c ∈ K_i^×: the z-power twists of GMN Def 2.20 are carried by the march
    itself, NOT absorbed from free per-slot data;
(slot law, = O-9's OL-B) each stride's residual-coefficient read THROUGH THE
    MARCHED NORMALIZER is (fixed unit twist)·(residue), a BIJECTION of K_i
    per slot; and the slot-to-exponent coupling is κ ↦ z^κ (the march is
    linear in κ): no un-cancelled z ↦ z^a, a ≥ 2, survives.

**Displayed inputs** (each with owner and proved perimeter):

* **(KP_j), j ≤ i (from GD-1, cumulative; RESTRUCTURED at rev 2 — V26-1).**
  Φ_{j+1} is monic of degree e_j·g_j·deg Φ_j and:
  **(KPa)** in_{w_{j+1}}(Φ_{j+1}) = u·ψ̂_hom^{(j)} with u an invertible
  homogeneous element of gr(w_{j+1}), and ψ̂_hom^{(j)} ∤ in(B) for EVERY
  nonzero B ∈ K[x] with deg B < deg Φ_{j+1} (the minimality/degree block)
  [verbatim rev 0];
  **(KPirr)** [NEW clause, DECLARED at rev 2] ψ̂_hom^{(j)} is PRIME in
  gr(w_{j+1}) — equivalently (Lemma BRIDGE, §5) Φ_{j+1} is
  w_{j+1}-equivalence-irreducible in MacLane's printed sense (Def 4.1(i)).
  Rev 0 asserted this came WITH (KPa) ("primeness of ψ̂_hom is
  irreducibility") — V26-1: false as displayed; it is a genuinely additional
  input clause;
  **(KPb)** R_j(Φ_{j+1}) ∼ ψ_j (the Def-2.12 residual normalization)
  [verbatim rev 0].
  Proved perimeter: at j = 0 for the STANDARD lift at any d₀, ALL clauses —
  Theorem KEY-g (§3): (KPa) AND (KPirr) are KEY-g(ii) (which proves
  PRIMENESS outright, not just the block), (KPb) is KEY-g's construction. At
  the order-0 key itself the analogous block AND primeness are FREE for any
  monic lift (Lemma GAUSS-g(iii)). Beyond: GD-1's business — GMN Thm 2.11/
  Def 2.12 (the representatives are key polynomials); MacLane Def 4.1
  (printed clauses (i) irreducibility + (ii) minimal degree + (iii) monic,
  quoted verbatim at LITUNIT §3); FGMN's "monic, µ-minimal and µ-irreducible"
  key-polynomial definition is the modern printed form (Def 1.2 of
  arXiv:1305.0775, locus per the V26 report, same paper as the LITUNIT
  extraction; key-polynomial theory: FGMN §5, Prop 5.7/Thm 5.8).
* **(ML) MacLane Thm 4.2** (augmented values are valuations) — §5, pinned;
  printed scope CLEARED (LITUNIT §3).
* **(T-1), (T-2)** — two GMN transcriptions, §6/§7 — both PINNED (LITUNIT
  §§1–2).
* **(GR-B)** — the residue-structure package at orders ≥ 2, §7.2, OPEN;
  cited clause-by-clause (§7.2, §11) and proved at order 1 here.

## 3. The order-≤1 layer, proved outright over an arbitrary DVR at every d₀

**Lemma DEV-g (development congruence).** Φ ∈ O[x] monic of degree d ≥ 1;
B, B′ ∈ O[x] with B ≡ B′ (mod π^L). Then the Φ-adic developments have
c_j ≡ c′_j (mod π^L) for all j, and all c_j ∈ O[x].

*Proof.* Verbatim O-1thr Lemma DEV with ℤ_p → O: division with remainder by a
monic polynomial keeps O-coefficients, is O-linear in the dividend, and
commutes with reduction mod π^L (uniqueness of monic division over O/π^L);
induct along the development. ∎

**Lemma POS-g (REV 2 — the semivaluation clause honestly scoped; V26-4).**
Each w_i of §1.2 satisfies on O[x]: (i) w_i(B) ≥ 0;
(ii) w_i(πB) = E_i + w_i(B); (iii) w_i(B + B″) ≥ min(w_i(B), w_i(B″)).
Consequently B ≡ B′ (mod π^L) ⟹ w_i(B − B′) ≥ L·E_i. Further,
(iv) sub-multiplicativity w_i(AB) ≥ w_i(A) + w_i(B) holds: OUTRIGHT at
orders ≤ 1 (order 0: Gauss's content bound, GAUSS-g; order 1: VAL-g's step
(1)); at order i+1 ≥ 2 it is claimed ONLY GIVEN (V1)_i + (KP_{i−1})'s
(KPa)-block, by the division-control display below — and is in any case
subsumed by (V1)_{i+1} once the full tower package is granted (§5).

*Proof.* (i)–(iii) verbatim O-1thr Lemma POS (induction on i; DEV-g gives
integral development coefficients; γ_{i+1} > 0). The consequence: B − B′ =
π^L·C, C integral — uses (i)+(ii) only. (iv) at order 0 is Gauss's content
bound; at order 1 it is step (1) of Theorem VAL-g below (which proves it
with the strictly-heavier-carry refinement). Rev 0 claimed the general-order
step was "displayed inside the proof of Theorem VAL-g, whose '≥' paragraph
is stated for arbitrary order i" — a MISDESCRIPTION (V26-4): VAL-g's
displayed computation is the order-1 Φ₀-calculation with the plain Gauss
valuation. The general-order step, displayed now:

*Division control at level i (given (V1)_i + (KP_{i−1})'s (KPa)-block).*
These are exactly the hypotheses of Theorem DEVID (§4) at (w_i, Φ_i); DEVID
gives, for any nonzero P of degree ≤ 2·deg Φ_i − 2 with division
P = c + d·Φ_i (deg c, deg d < deg Φ_i — the full Φ_i-development of such P):
w_i(P) = min( w_i(c), w_i(d) + w_i(Φ_i) ), hence
    (α) w_i(c) ≥ w_i(P)  and  (β) w_i(d) + w_i(Φ_i) ≥ w_i(P).
*Sub-multiplicativity of w_{i+1}, given the above.* For A = Σ_t a_t Φ_i^t,
B = Σ_s b_s Φ_i^s (Φ_i-developments, so deg a_t, deg b_s < deg Φ_i):
AB = Σ_{t,s} a_t b_s Φ_i^{t+s}, and each P_{ts} := a_t b_s divides as
c_{ts} + d_{ts}Φ_i; by (V1)_i, w_i(P_{ts}) = w_i(a_t) + w_i(b_s), so (α)/(β)
bound c_{ts} and d_{ts}. The slot-r coefficient of AB's Φ_i-development is
Σ_{t+s=r} c_{ts} + Σ_{t+s=r−1} d_{ts} (degrees < deg Φ_i, and the
development is unique), so by (iii) at order i:
e_i·w_i(coeff_r) + r·γ_{i+1} ≥ min over contributions of
  [ e_i(w_i(a_t) + w_i(b_s)) + (t+s)·γ_{i+1} ]                    (c-terms)
  and [ e_i(w_i(a_t) + w_i(b_s)) − e_i·w_i(Φ_i) + (t+s+1)·γ_{i+1} ]
    = (c-term bound) + h_i                                        (d-terms),
each ≥ w_{i+1}(A) + w_{i+1}(B) (since w_{i+1}(A) ≤ e_i·w_i(a_t) + t·γ_{i+1}
definitionally, likewise B), the carry terms strictly heavier by h_i ≥ 1 —
the general-order shadow of VAL-g's step (1). Minimizing over r:
w_{i+1}(AB) ≥ w_{i+1}(A) + w_{i+1}(B). ∎ (conditional as stated)

*Consumption note (attackable).* No consumer of this brief fires POS-g(iv)
at an order where (V1) one level down is unavailable: §1.3's gr(w) is
consumed at orders where (V1) holds AT the order (DEVID's hypothesis);
the POS-g consequence clause uses (i)+(ii) only; VAL-g proves its own
order-1 instance. If a consumer is found that needs (iv) unconditionally at
order ≥ 2, that is a finding.

**Lemma GAUSS-g (order-0 layer).** (i) w₀(B) = the plain Gauss valuation
(min coefficient valuation of B itself), independent of Φ₀. (ii) w₀ is a
valuation; gr_K(w₀) = F[X̄][π̄^{±1}] (X̄ := in(X) at weight 0, π̄ := in(π) at
weight 1), a Laurent-polynomial domain, indeed a UFD. (iii) For any monic
lift Φ₀ of an irreducible φ̄ of degree d₀: in(Φ₀) = φ̄(X̄), a prime of
gr_K(w₀), and φ̄(X̄) ∤ in(B) for every nonzero B ∈ K[x] with deg B < d₀.

*Proof.* (i) B ∈ π^L O[x] iff every development coefficient is in π^L O[x]
(⟸ clear; ⟹ DEV-g with B′ = 0), so the two minima agree. (ii) Gauss's
lemma over the domain F[X̄]: the weight-L piece of gr is π̄^L·F[X̄]
(clear denominators by units π̄^{−L}), and in(A)in(B) = in(AB) is the
statement that the product of the content-normalized reductions is the
reduction of the content-normalized product — nonzero since F[X̄] is a
domain. UFD: localization of the UFD F[X̄][π̄]. (iii) Φ₀ monic with
reduction φ̄ has w₀(Φ₀) = 0 and weight-0 class φ̄(X̄); φ̄(X̄) is prime
(irreducible in F[X̄], and π̄ is a unit); a nonzero multiple φ̄·δ has
X̄-degree ≥ d₀ in every graded piece, while in(B) has X̄-degree ≤ deg B < d₀.
∎

Fix now a root read ν₀ over Φ₀ with data (e, h) coprime ≥ 1, and write
w₁(B) = min_t ( e·w₀(b_t) + t·h ) over the Φ₀-development (γ₁ = h since
w₀(Φ₀) = 0). Write K₀ = F[X]/(φ̄) (= F_1^{GMN}, a field, q₀ := |K₀| when F
finite).

**Theorem VAL-g (order-1 valuation, any d₀, any O).** w₁ is a valuation on
K[x]; gr_K(w₁) ≅ K₀[π̄^{±1}][Φ̄₀] with π̄ := in(π) invertible of weight e,
Φ̄₀ := in(Φ₀) of weight h, and K₀ sitting in weight 0 as the classes of
coefficient polynomials (deg < d₀) of w₀-content 0; a UFD. Explicitly, for
A = Σ_t a_t Φ₀^t with S := {t : e·w₀(a_t) + t·h = w₁(A)}:
in(A) = Σ_{t∈S} [a_t]·π̄^{w₀(a_t)}·Φ̄₀^t, where [a_t] ∈ K₀ is the class of
a_t/π^{w₀(a_t)} mod (π, Φ₀) — nonzero for each t ∈ S.

*Proof.* (1) *Sub-multiplicativity with strictly heavier carries.* For
A = Σ a_t Φ₀^t, B = Σ b_s Φ₀^s: AB = Σ_{t,s} a_t b_s Φ₀^{t+s}, and each
a_t b_s (degree ≤ 2d₀ − 2) reduces as a_t b_s = c_{ts} + d_{ts}Φ₀ with
deg c_{ts} < d₀ and deg d_{ts} ≤ d₀ − 2 < d₀. By DEV-g (applied
to the dividend a_t b_s ∈ π^{w₀(a_t)+w₀(b_s)}O[x], using GAUSS-g(ii)'s
multiplicativity of w₀): w₀(c_{ts}), w₀(d_{ts}) ≥ w₀(a_t) + w₀(b_s). The
development of AB therefore has slot-r coefficient
Σ_{t+s=r} c_{ts} + Σ_{t+s=r−1} d_{ts}, every summand of w₁-weight
≥ e(w₀(a_t)+w₀(b_s)) + (t+s)h ≥ w₁(A) + w₁(B), and every CARRY summand
d_{ts} of weight ≥ w₁(A) + w₁(B) + h, strictly heavier. So
w₁(AB) ≥ w₁(A) + w₁(B).
(2) *The graded module.* Weight-γ classes: by slot uniqueness of the
development, a sum Σ_t π^{a_t}u_tΦ₀^t (u_t of content 0, ea_t + th = γ) has
w₁ > γ iff every [u_t] = 0; so gr_γ is the free K₀-module on the monomials
π̄^aΦ̄₀^t with ea + th = γ (a ∈ ℤ after clearing denominators by π-units,
t ≥ 0), and the displayed in(A) formula holds.
(3) *Ring structure.* Products of weight-0 coefficient classes multiply as
in K₀: a_tb_s = c_{ts} + d_{ts}Φ₀ gives, modulo (π, Φ₀),
[c_{ts}] = [a_t][b_s] — the field product — while the carry term d_{ts}Φ̄₀
lands in strictly higher weight by (1) and so vanishes in the graded piece.
Hence gr(w₁) ≅ K₀[π̄^{±1}][Φ̄₀] as a graded ring: a domain, a UFD
(localization of a polynomial ring over a field).
(4) *Multiplicativity.* By (2)/(3), in(A) and in(B) are nonzero elements of
a domain; the weight-(w₁A + w₁B) part of AB's development is, by (1)'s
bookkeeping, exactly the polynomial product in(A)·in(B) ≠ 0. So
w₁(AB) = w₁(A) + w₁(B). ∎

*Rev-4 note on step (1) (F6; statement and proof text untouched).* For
A, B ∈ K[x] the contents w₀(a_t), w₀(b_s) can be negative, and DEV-g's
hypotheses read dividends in O[x]. The displayed one-line clearing: pick
M, N ≥ 0 with π^M A, π^N B ∈ O[x] and run step (1) on them — every content
shifts exactly (w₀(π^M a_t) = M + w₀(a_t): GAUSS-g(i)), division by the
monic Φ₀ is O-linear so c_{ts}, d_{ts} scale by π^{M+N}, and each displayed
inequality shifts on BOTH sides by e(M + N); w₁(π^M A) = eM + w₁(A) from
the definition. Subtracting e(M + N) recovers step (1)'s conclusion for
A, B themselves. The same clearing serves every later application of
step-(1)-style bookkeeping to K[x] operands (steps (2)–(4) already clear
denominators by π-units explicitly).

**Theorem KEY-g (standard first key, any d₀, any O).** Let ψ ∈ K₀[z] be
monic irreducible of degree g with ψ(0) ≠ 0, ψ̂_κ ∈ O[x] (deg < d₀) fixed
lifts of its coefficients (ψ̂_g = 1), and
Φ₁ := Σ_κ ψ̂_κ · π^{(g−κ)h} · Φ₀^{eκ} — the standard key lift. Then:
(i) Φ₁ is monic of degree e·g·d₀, every nonzero monomial of w₁-weight
exactly egh, and w₁(Φ₁) = egh with
in(Φ₁) = ψ_hom := Σ_κ [ψ_κ]·π̄^{(g−κ)h}·Φ̄₀^{eκ}, the (e,h)-homogenization
of ψ; (ii) ψ_hom is PRIME in gr_K(w₁), and ψ_hom ∤ in(B) for every nonzero
B ∈ K[x] with deg B < egd₀ — i.e. (KPa) holds at j = 0, u = 1; (iii) for f
satisfying the read's (R1)/(R2) at a side (e, h, s0, d) with residual
R_λ(f) ∈ K₀[z]: ord_{ψ_hom}(in(f)) = ord_ψ(R_λ(f)) — GD-3's min core at
order 1.

*Proof.* (i) Weight of the κ-monomial: e·((g−κ)h + w₀(ψ̂_κ)) + eκ·h = egh
when ψ_κ ≠ 0 (w₀(ψ̂_κ) = 0). Degrees: the κ = g term is Φ₀^{eg}, degree
egd₀, monic; lower terms have degree < egd₀. So in(Φ₁) is the displayed sum
(all monomials at one weight; classes [ψ_κ]).
(ii) The homogenization map hom_{e,h} : c(z) = Σ c_κ z^κ (deg = g′, c ∈
K₀[z]) ↦ Σ_κ c_κ π̄^{(g′−κ)h}Φ̄₀^{eκ} is multiplicative
(hom(c)hom(c′) = hom(cc′): exponents add slot-wise) and injective. Every
homogeneous δ ∈ gr_K(w₁) factors as π̄^a·Φ̄₀^b·hom(c) with c(0) ≠ 0:
collect the least Φ̄₀-exponent b; the remaining exponents lie on one
(e,h)-lattice line (gcd(e,h) = 1: from ea + th = const, t is determined
mod e), giving the hom-shape. Now suppose ψ_hom = δδ′: since Φ̄₀ ∤ ψ_hom
(the κ = 0 term [ψ₀]π̄^{gh} ≠ 0, as ψ(0) ≠ 0) and ψ_hom has a
π̄-unit-free top term Φ̄₀^{eg} (monic), the monomial prefactors of δ, δ′
are units π̄^a, and ψ_hom = π̄^{a+a′}hom(cc′) forces cc′ = ψ up to a
K₀^×-scalar (injectivity), so c or c′ is a scalar: δ or δ′ is a unit.
Irreducible + UFD ⟹ prime. Degree block: deg B < egd₀ ⟹ the
Φ₀-development of B has top slot t ≤ ⌊deg B/d₀⌋ < eg, so in(B) has
Φ̄₀-degree < eg; any nonzero multiple ψ_hom·δ has Φ̄₀-degree ≥ eg (top
Φ̄₀-degrees add in the domain).
(iii) By (R1)/(R2), the w₁-minimizing slots of f's development are exactly
the on-line strides of the side, with digit classes the residual
coefficients c_κ = [b_{s0+eκ}/π^{ℓ(s0+eκ)}] (GMN (L3)'s order-1
coefficients, K₀-valued, 0 above the line): so by VAL-g's in-formula,
in(f) = Φ̄₀^{s0}·π̄^{β}·hom_{e,h}(R_λ(f)), β := the side's right-end
height. π̄ is a unit; Φ̄₀ ∤ ψ_hom and ψ_hom ∤ Φ̄₀; and
ord_{ψ_hom}(hom R) = ord_ψ(R): hom is multiplicative, and if
ψ_hom·δ = hom(R′) with R′(0) ≠ 0 then δ's monomial prefactor is trivial
(match Φ̄₀-floors and top terms as above), so δ = π̄^a·hom(c) and
ψ·c = R′ up to scalar. Hence ord_{ψ_hom}(in f) = ord_ψ(R_λ(f)). ∎

*Rev-2 note on (ii).* KEY-g(ii) proves PRIMENESS of ψ_hom outright — i.e.
it discharges BOTH (KPa) and the rev-2 clause (KPirr) at j = 0, any d₀,
any O. Rev 0's clause label "(KPa) holds at j = 0" understated what the
displayed proof delivers; the statement and proof are unchanged.

**Theorem RES-1 (order-1 residual dictionary, full form; OL-B at order 1).**
In the setting of KEY-g(iii): (a) the FULL residual identity holds at order
1: reading the stride digits of f through the graded monomials —
c_κ = [b_{s0+eκ}/π^{ℓ(s0+eκ)}] — gives R(z) := Σ_κ c_κ z^κ = R_λ(f)
EXACTLY (GMN's (L3) order-1 residual; no twists: "the z-power normalizations
of Def 2.20 enter at orders ≥ 2 only"), and any anchor-unit normalization
([F.2] with e_b = 1, 𝒟 = K₀) changes it by a c ∈ K₀^× only; (b) per slot,
the digit read b ↦ [b/π^ℓ] is (unit twist = 1)·(residue map), a bijection
from the on-line digit classes onto K₀ ∖ {0} extended by 0 above the line —
OL-B's order-1 instance, with NO z ↦ z^a map anywhere in the chain.

*Proof.* (a) is KEY-g(iii)'s display read off (the identification is
definitional once the in-formula is proved; scalar freedom is GMN's own "∼",
scope remark (4) of O-2a). (b) the slot map is x ↦ x·π^{−ℓ} followed by
reduction mod (π, Φ₀): a composition of a unit scaling and the residue
map onto the field K₀ — bijective on classes; the exponent map on digits is
the identity. ∎

*Remarks.* (1) VAL-g/KEY-g at d₀ = 1 over ℤ_p are verbatim O-1thr's
VAL1/KEY1 (𝔽_p[x̄, p̄] localized); the falsifier (§9) exercises exactly the
NEW perimeter (d₀ = 2; equal characteristic). (2) SYNTH4's GD-5 note
predicted "the weighted-Gauss ring becomes F_{p^g}[x̄, p̄] via the unramified
track extension"; the proof above shows one does not even need the
unramified-extension detour — the coefficient field K₀ emerges inside the
graded ring directly, over any O. (3) KEY-g(iii) + RES-1 discharge the
MATHEMATICAL content of O-2a's OL-5 (b′)+(c′) (the order-1 base of GD-5's
composition note): a base read performed by the displayed formulas emits
verbatim (c1)/(c2) — the remaining OL-5 content is engine conformance, per
§8.

## 4. Theorem DEVID — the development identity at every order

**Theorem DEVID.** Let w be a valuation on K[x] ((V1) at the state order),
Φ ∈ O[x] monic such that in(Φ) = u·ψ̂ with u invertible homogeneous and
ψ̂ ∤ in(B) for every nonzero B ∈ K[x] with deg B < deg Φ ((KPa)). Then for
every nonzero B ∈ K[x], with B = Σ_k C_k Φ^k the Φ-adic development:

    w(B) = min_k ( w(C_k) + k·w(Φ) ),

i.e. (V2b) holds at the state: the state key's development computes the
state valuation from below, with γ̂ = w(Φ).

*Proof.* "≥": ultrametric + multiplicativity: w(B) ≥ min_k w(C_kΦ^k) =
min_k (w(C_k) + k·w(Φ)) =: u₀. Suppose w(B) > u₀ and let
S := {k : w(C_k) + k·w(Φ) = u₀} (nonempty; each C_k ≠ 0 for k ∈ S),
m := min S. By (g1)+(g2) of §1.3, Σ_{k∈S} in(C_k)·in(Φ)^k = 0 in gr_{u₀}.
Substitute in(Φ) = u·ψ̂ and factor (gr is a domain, u invertible):

    0 = ψ̂^m · ( Σ_{k∈S} in(C_k)·u^k·ψ̂^{k−m} ),

so the bracket vanishes; isolating its k = m term,
in(C_m)·u^m = −ψ̂·( Σ_{k∈S, k>m} in(C_k)·u^k·ψ̂^{k−m−1} ), whence
ψ̂ | in(C_m) — contradicting (KPa)'s block at C_m (nonzero, of degree
< deg Φ). ∎

*Rev-2 note (minimality at the V26-1 seam; attackable).* DEVID consumes
(V1) + (KPa)'s BLOCK only — NOT the rev-2 clause (KPirr): the proof's last
step reads the divisibility ψ̂ | in(C_m) off the displayed equation
directly; no primeness of ψ̂ is used anywhere. This matches V26's own
verdict ("DEVID as a conditional theorem from a genuine valuation plus the
stated minimality block: sound"). (KPirr) is consumed by the TOWER (§5) and
by (GR-B)'s formulation (§7.2), not here.

**Corollaries.**

* **(S-K1) instantiated.** The stage interface's axiom
  u(f) = min_j (u(B_j) + j·u(Φ̂)) (O-2a §4 [F.0]) HOLDS at u := w_{i+1},
  Φ̂ := Φ_{i+1}, at every order where (V1)+(KPa) hold — one of the two
  IFACE clauses (§7.4).
* **DIV1 retired; the V24-G4 caveat moot.** O-1thr's DIV1 proved this at
  order 1, d₀ = 1, by a Φ₀-adic slot-chase whose general-lift form was a
  pass-3 repair site (V24 G4: the reduction step is "not weight-graded when
  the policy's root key is Φ₀ = X − a"). The graded proof above never sees
  the shape of any lower key — only (V1) and (KPa) — so it covers every
  order, every d₀, every O at once, and DIV1 survives only as the order-1
  corroboration of a special case.
* **Corollary VTX's deeper leg supplied.** O-1thr's Corollary VTX needed, at
  deeper harvests, exactly "e_i·w_i(C_{μ′}) ≤ w_{i+1}(C_{μ′}), i.e. that
  C_{μ′}'s Φ_i-development computes w_i from below — the development
  identity (V2b) at level i". DEVID at level i (given (V1)_i + (KPa)_{i−1})
  is that statement. (Note the level-i instance consumed there is DEVID at
  the PARENT key: w_i with Φ_i, i.e. (KPa) one level down — the cumulative
  reading of (KP), matching OL-1's joint induction.)
* **WGEO re-founded.** O-1thr's Lemma WGEO consumes (V1), (V2a) = (KPa),
  (V2b) = DEVID, (V2c) = GD3-MIN. With this brief, WGEO's status at every
  order becomes: PROVED given (KP) at the state plus GD3-MIN's route pin —
  the (V1)/(V2) bundle is no longer an undifferentiated citation; its owed
  normalization dictionary (V24-G6, seam S-4) is §§4–7 of this brief.

## 5. (V1) at every order: the MacLane composition, recomposed (V26-1)

**Lemma BRIDGE (equivalence-divisibility = graded divisibility; proved).**
Let W be a valuation on K[x] and a, b ∈ K[x] nonzero. MacLane's
equivalence-divisibility "a |_W b" (∃c : W(b − ca) > W(b)) holds iff
in(a) | in(b) in gr(W).

*Proof.* (⟹) W(b − ca) > W(b) forces W(ca) = W(b): if W(ca) > W(b) then
b = (b − ca) + ca would have W(b) ≥ min(W(b − ca), W(ca)) > W(b); if
W(ca) < W(b) then W(b − ca) = W(ca) < W(b) (ultrametric equality at
distinct values), contradiction. Then (g1) at weight W(b):
in(b) = in(ca) = in(c)·in(a) ((g2); W is a valuation). (⟸) If
in(b) = δ·in(a) with δ homogeneous of weight W(b) − W(a): every nonzero
homogeneous element of gr(W) is in(c) for any representative
c ∈ P_γ ∖ P_γ⁺ of its class (the graded pieces are P_γ/P_γ⁺, §1.3), so pick
c with in(c) = δ; then in(b − ca): b − ca has in(b) − in(ca) = 0 at weight
W(b), i.e. W(b − ca) > W(b). ∎

**Proposition V1-tower (REV 2).** (a) w₀ and w₁ are valuations over any O,
any d₀ (GAUSS-g, VAL-g — proved). (b) For i ≥ 1, GIVEN (V1)_i (the
induction hypothesis: e_i·w_i is a valuation with the same graded ring
re-graded) and the FULL key package (KP_{i−1}) — (KPa) + (KPirr) +
monicity — Φ_i is a MacLane key polynomial for e_i·w_i in the PRINTED sense
of Def 4.1, clause by clause:

* Def 4.1(i) (*Irreducibility*: "If a product is equivalence-divisible in W
  by φ(x), then one of the factors is") ⟺ (by Lemma BRIDGE, twice)
  in(Φ_i) is prime in gr ⟺ (u invertible, (KPa)) ψ̂_hom^{(i−1)} is prime
  = **(KPirr)**. Rev 0 attributed this clause to (KPa)'s block — V26-1's
  critical error; the block does not state or imply primeness.
* Def 4.1(ii) (*Minimal degree*: "Any non-zero polynomial
  equivalence-divisible in W by φ(x) has a degree in x not less than the
  degree of φ(x)") ⟺ (Lemma BRIDGE) ψ̂_hom ∤ in(B) for nonzero B of
  deg B < deg Φ_i = **(KPa)'s block** (contrapositive).
* Def 4.1(iii) (leading coefficient 1) = (KP)'s monicity.

And γ_{i+1} > e_i·w_i(Φ_i) (true: h_i ≥ 1) is Def 4.1's value condition
(1). THEREFORE MacLane Thm 4.2 applies with its printed hypotheses met:
w_{i+1} = [e_i·w_i ; w(Φ_i) = γ_{i+1}] is a valuation. [MacLane, *A
construction for absolute values in polynomial rings*, Trans. AMS 40 (1936),
363–395: Def 4.1 and Thm 4.2 quoted VERBATIM at LITUNIT §3
(maclane1936.txt:261–270, 213–216). Scaling remark: e_i·w_i is a value in
MacLane's sense with the same key polynomials (Def 4.1's three conditions
are scaling-invariant — equivalence-divisibility depends only on the
ordering data W(b − ca) > W(b), preserved under positive scaling; LITUNIT §3
records this as a one-line observation, not a quote), so augmenting the
rescaled valuation is inside Thm 4.2's scope. Corroborating modern printed
form of "key = minimal + irreducible": FGMN Def 1.2; and FGMN Prop 1.9
cites the augmentation theorem back to "[10, Thms. 4.2, 5.1]" = MacLane —
an independent locus confirmation (LITUNIT §5).]

*Rev-4 note on the Def 4.1(i) "⟺" (F9; the displayed chain is untouched).*
BRIDGE translates Def 4.1(i) into primeness of in(Φ_i) for divisibility of
HOMOGENEOUS elements (all initial forms are homogeneous); (KPirr) asserts
primeness in the ring gr. The direction the tower CONSUMES — (KPirr) ⟹
ring-prime ⟹ homogeneous-prime ⟹ Def 4.1(i) — needs nothing further.
The decorative converse of the "⟺" uses the standard graded-domain lemma,
displayed: *in a ℤ-graded domain, a homogeneous element δ that is prime on
homogeneous elements is prime.* [Proof: let δ | xy, x, y ≠ 0, and induct
on the total number of homogeneous components of x and y. The top
components satisfy x_top·y_top = (xy)_top (gr a domain), and (xy)_top is a
multiple of δ times the top component of the cofactor, so δ | x_top·y_top,
both homogeneous; say δ | x_top. Then δ | xy − x_top·y = (x − x_top)·y
with fewer components; induction gives δ | x − x_top (whence δ | x, with
δ | x_top) or δ | y.] No consumer fires the converse; it is recorded so
the "⟺" is honest as displayed.

**ML-scope: CLEARED (was rev 0's flag; LITUNIT §3).** MacLane's printed
standing hypotheses are WIDER than rev 0 claimed: Part I defines the
values "for polynomials with coefficients in **any field K**"; discreteness
is assumed ONLY in §§7, 8, 16 (limit values/completeness/normal forms),
none of which Thm 4.2 needs. So the arbitrary-DVR instantiation (mixed or
equal characteristic, F arbitrary, no completeness) is inside printed
scope, and the Vaquié fallback of rev 0 is UNNECESSARY (dropped).

Status: (V1) is thus PROVED at orders ≤ 1 (any O, any d₀), and at every
higher order is the composite (KP_{i−1}) = (KPa)+(KPirr)+monicity [GD-1's
input, restructured at rev 2] + Lemma BRIDGE [proved here] + MacLane
Thm 4.2 [pinned; scope cleared]. The honest input ledger: (KPirr) is a
genuinely additional clause relative to rev 0's display — proved at j = 0
by KEY-g(ii), owned by GD-1 beyond (its citation-grade home: GMN Thm 2.11's
representatives; FGMN §5's key-polynomial theory).

## 6. The tie w = v (GD-2(a) second half) and the record dictionary (GD-2(c))

**Lemma UNIQ (proved).** Let ν, ν′ : K[x]∖{0} → ℤ both satisfy, for a fixed
monic Φ of degree D ≥ 1: (u1) ν = ν′ on {B ≠ 0 : deg B < D} and
ν(Φ) = ν′(Φ); (u2) both are computed by the Φ-development min formula
ν(B) = min_j ( ν(b_j) + j·ν(Φ) ). Then ν = ν′.

*Proof.* Immediate: the development is unique and both sides are the same
min of identical numbers. ∎

**Theorem TIE.** At GMN's scope (K/ℚ_p finite; F finite) and under the
policy pin (the engine/O-1thr keys Φ₀, …, Φ_{i+1} ARE the GMN
representatives φ₁, …, φ_{i+2} of the truncated types — GD-1/OL-1(b)
territory, a displayed hypothesis here): w_j = v_{j+1} on K[x]∖{0} for
every j ≤ i+1, in the SAME ℤ-normalization (no rescaling; constants check
in §1.2). Consumed GMN properties, each displayed:

* (P-base) v₁ = the Gauss valuation on developments — pinned, O-2a (L2)
  [Def 2.5 / Prop 2.6 transcription, verified through O-2a's five rounds];
* (P-key) v_{j+2}(φ_{j+1}) = h + e·v_{j+1}(φ_{j+1}) — pinned; now the
  sharper locus: it is displayed as **Prop 2.7(4) itself** (LITUNIT §1;
  rev 0 cited it "as used in the proof of Prop 2.15"); equals γ_{j+1}
  under the induction hypothesis;
* **(T-1) [PINNED at LITUNIT §1 — was rev 0's flag]:**
  v_{j+2}(b) = e_{j+1}^{GMN}·v_{j+1}(b)
  for deg b < deg φ_{j+1}, and v_{j+2}(P) = min over the φ_{j+1}-development
  of v_{j+2}(b_k φ_{j+1}^k) — i.e., GMN's Def-2.5 valuation is computed by
  the current-key development with the rescaled coefficient values. Printed
  home, quoted verbatim at LITUNIT §1 (gmn.txt:1204): the development
  clause is **GMN Prop 2.7(3)** — BOTH displayed forms, including the
  e_{r−1}-rescaled coefficient form in GMN's OWN integer normalization
  (v_r(P) = e_{r−1}·min{v_{r−1}(a_i) + i(v_{r−1}(φ_{r−1}) + |λ_{r−1}|)}),
  which is what V26-5 asked to see pinned; the rescaling clause on low
  degrees is **Prop 2.7(1)'s equality case entered through Lemma 2.2(2)**
  (deg b < m_{r−1} ⟹ ω_{r−1}(b) = 0 ⟹ equality). The transcription is
  discharged to these loci; TIE's conditionality is now "modulo the
  LITUNIT-verified transcriptions", each a verbatim quote a verifier can
  re-check against the PDF.

*Proof (given the pins).* Induction on j. Base: w₀ = Gauss = v₁ (GAUSS-g(i)
+ (P-base)). Step: by §1.2's computation, w_{j+1} satisfies UNIQ's (u2) at
Φ_j with coefficient values e_j·w_j(b) (= w_{j+1}(b) on deg < deg Φ_j: the
slot-0 development) and key value γ_{j+1} = w_{j+1}(Φ_j). By (T-1) and
(P-key), v_{j+2} satisfies (u2) at φ_{j+1} = Φ_j with coefficient values
e·v_{j+1}(b) = e_j·w_j(b) (induction hypothesis + index dictionary) and the
same key value. UNIQ finishes. ∎

**Corollary REC-c1 (GD-2(c)).** In the TIE's setting, at the order-(i+1)
state with key Φ_{i+1}: the sheared window points of f,
(k, u_k) with u_k := w_{i+1}(C_k) + k·γ̂_i over the Φ_{i+1}-development,
are EXACTLY GMN's N_{i+2}(f) points (k, v_{i+2}(C_k φ_{i+2}^k)) — the same
numbers, term by term (v_{i+2} = w_{i+1} by TIE one level up;
v_{i+2}(φ_{i+2}) = γ̂_i). Hence: the window polygon IS N_{i+2}(f) as a
subset of ℝ², vertex for vertex and side for side; every principal side's
(e, h, ℓ) are the lowest-terms slope pair and degree of the corresponding
side of N_{i+2}^−(f); and (s, u) — its initial point — is the pair pinned
by (c1)'s positional clause in the order-(i+2) integer frame. At a root
state the same holds with i+1 = 0 against N₁(f) (w₀ = v₁, γ̂ = 0). By
DEVID, moreover, these points compute w_{i+1}(f) from below — the polygon
is drawn at the honest heights.

*Two displays riding REC-c1.* (1) **The mathematical content of GD-2(c) is
exhausted by TIE + DEVID** — no residual geometry statement remains open on
the math side. What OL-2(b) additionally asserts (the ENGINE's emitted
record equals this data, any internal offset inverted before emission) is
engine conformance: exactly where the corpus's compiled stretch
countermodel lives (HK52: a naive engine-internal vertex transport fails by
a factor-e′ stretch at e′ ≥ 2). The merged GD-2 keeps OL-2(b)'s typing AT
THE EMITTED RECORD for precisely this reason; the HK52 gate remains the
standing GD-2(c) trap for the engine-side units (§8, §9). (2) The
V-recursion clause of SYNTH4's GD-1(c), V_{i+1} = e_i f_i (e_i V_i + h_i),
is the (P-key) pin composed with (KP)'s degree law — recorded here so the
GD-1 unit can consume the tie without re-deriving it. (3) [rev 2] REC-c1 is
scoped AS TIE — GMN's scope — because its right-hand objects (N_{i+2},
v_{i+2}) are GMN's; this scoping is load-bearing for IFACE's clause (b)
(§7.4), where rev 0's "any O" advertisement was V26-3's critical error.

## 7. GD-3 at general order

### 7.1 The consumption split (per V25)

The recut cluster (K-RUN-C/K-RUN-BOX; O-2a Theorem 2(D); WGEO(b)) consumes
ONLY the min core. The value side (O-9's census → O-11's tables) consumes
the full form and OL-B. The two get different treatment below — min core:
two independent routes, one pinned lemma each; full form: one route, through
the displayed (GR-B).

### 7.2 The displayed input (GR-B) — REV 2: the anchored-march form (V26-2); REV 3: the two printed pairings disambiguated + exact match verified (V28-1), properness proviso displayed (V28-2)

**(GR-B) [side-residual structure at state order i+1 ≥ 2; OPEN here; cited
clause-by-clause].** Given (V1)/(KP) through the state: write w := w_{i+1},
ψ̂ := ψ̂_hom^{(i)} (the creating read's key initial form — prime by
(KPirr)), λ := −h_i/e_i. The package asserts the existence of:

* a distinguished DEGREE-0 generator ȳ of the degree-0 subring Δ of the
  localized graded ring, with Δ = K_i[ȳ] a polynomial ring — the structure
  clause [MacLane Thm 12.1 (with Thm 10.2, Lemma 10.1, Cor 12.2);
  FGMN Thm 4.8 "∆ = F_r[y_r]" + Cor 4.9];
* a distinguished PAIR of invertible homogeneous ANCHOR-monomial systems
  [FGMN Lemma 4.1's ϕ_r(α)/ϕ_r(g) pair — REV 3: rev 2's locus "Def 3.15's
  monomial system" was a slip (Def 3.15 defines the twist units ε);
  V28-1]: the WEIGHT-indexed anchors ϕ_α, one per attained weight α
  (built on the least-abscissa datum 0 ≤ s(α) < e_i of FGMN eq. (12)),
  and the SIDE-anchored ϕ(f) := ϕ_{w(f)}·ȳ^{j₀(f)},
  j₀(f) := ⌊s₀(f)/e_i⌋, keyed to the initial vertex (s₀(f), ·) of f's
  λ-side; the system's degree-0 sub-step structure realizes the carry
  algebra 𝒟 = F[u]/(u^{e_b} − ζ) of [F.0] (e_b the birth index, ζ the
  distinguished unit: the u_ρ are the fractional sub-steps of the march,
  ζ the e_b-fold wrap);
* a matching PAIR of residual operators, TOTAL on the nonzero HOMOGENEOUS
  elements of gr_K(w), valued in K_i[z]∖{0} [REV 3, V28-1; REV 4, F1:
  "side-supported" is hereby DEFINED — δ ∈ gr_α ∖ {0} is *side-supported*
  iff its monomial support in the anchor frame lies on the single weight-α
  lattice line — and shown to be NO restriction: every nonzero homogeneous
  element satisfies it (homogeneity IS one-line support; the adjective
  named an automatic property, and rev 3's phrasing invited a stronger
  reading with a vacuity risk in Route B). The operators' domain is
  therefore ALL nonzero homogeneous elements, matching the printed
  operators' totality in g ≠ 0 (every nonzero homogeneous class is an
  in(g) — BRIDGE's representative-lift step); domain membership is
  displayed at each firing site (§7.3)]:
  the WEIGHT-indexed R^{gr}_α (slot κ read at abscissa s(α) + κ·e_i —
  FGMN Def 3.16's parameterization; z-order of R^{gr}_{w(f)}(in f) equal
  to j₀(f), FGMN Cor 4.4(3)) and the SIDE-read
  R^{gr} := z^{−j₀(f)}·R^{gr}_{w(f)} (FGMN Def 3.18; nonzero constant
  term, slot κ read at abscissa s₀(f) + κ·e_i);

with:
(1) R^{gr}(α·β) = θ_{α,β}·R^{gr}(α)·R^{gr}(β), θ_{α,β} ∈ K_i^× [FGMN
    Thm 4.2 (H_µ and ϕ_r multiplicative) + Lemma 5.13];
(2′) **the anchored-march identity (REV 3: stated in BOTH printed
    normalizations, correctly paired — V28-1)**: for f with the creating
    read's (R1)/(R2), writing α := w(f) and j₀ := j₀(f) = ⌊s₀(f)/e_i⌋,
        in(f) = ϕ_α · R^{gr}_α(in f)(ȳ) = ϕ(f) · R^{gr}(in f)(ȳ),
    ONE invertible anchor monomial times the ȳ-evaluation of the MATCHING
    residual polynomial: the weight-indexed anchor ϕ_α pairs with the
    weight-indexed R^{gr}_α (ȳ-order j₀), the side-anchored ϕ(f) pairs
    with the side-read R^{gr} (nonzero constant term). The two pairings
    agree through ϕ(f) = ϕ_α·ȳ^{j₀} exactly; the discrepancy between the
    MIXED pairings is the ȳ-power ȳ^{j₀}, NOT a scalar — rev 2 displayed
    the mixed pair ϕ_{w(f)} with R^{gr}, V28-1's conflation, retired
    here. So the slot-κ stride monomial is ϕ(f)·ȳ^κ = ϕ_α·ȳ^{j₀+κ} (one
    fixed step, LINEAR in κ, anchored once), and the κ-th coefficient of
    R^{gr}(in f) is the K_i-residue of the stride digit at abscissa
    s₀(f) + κ·e_i READ THROUGH THE MARCHED NORMALIZER [= FGMN Thm 4.2,
    quoted verbatim and matched clause-for-clause in the display below,
    with the slot granularity of Cor 4.4(2)-(3): the j-th coefficient of
    R_{r,α} is nonzero iff the abscissa-s_j digit attains,
    s_j = s(α) + j·e_r, and ord_y(R_{r,α}(g)) = ⌊s(g)/e_r⌋];
    and R^{gr}(in f) ∼ R_λ(f), GMN's Def-2.21 residual (level-i read;
    index dictionary §1.2) — the SIDE-read operator is the GMN-matching
    one (both read from the side's initial abscissa with nonzero constant
    term; the "∼" is the declared twist dictionary, caveat (ii));
(3) R^{gr}(ψ̂) ∼ ψ_i ((KPb) transported) [FGMN Cor 5.6(2), verbatim:
    "R_i(φ_{i+1}) = ψ_i, the minimal polynomial of z_i over F_i"];
(4) faithfulness at ψ: ψ̂ | α ⟺ ψ_i | R^{gr}(α), indeed
    ord_{ψ_i}(R^{gr}(α)) = ord_{ψ̂}(α) [FGMN Prop 5.14 / Cor 5.15 for
    PROPER keys — REV 3 (V28-2): the state key's properness is now a
    DISPLAYED PROVISO, no longer inferred from the degree law alone.
    The printed definition (Def 5.10, fgmn.txt:1831, verbatim: "We say
    that φ is a proper key polynomial for µ if deg φ is a multiple of
    e_r(µ)m_r(µ)") is stated in the INTRINSIC data e_r(µ), m_r(µ),
    defined through OPTIMAL MacLane chains (FGMN §3.2: "If the MacLane
    chain is optimal ... all these rational numbers are intrinsic data
    of µ"), and the printed characterization is Cor 5.12 ("A key
    polynomial φ for µ is proper if and only if there exists a MacLane
    chain of µ such that φ ≁_µ φ_r"). Rev 2's "deg Φ_{i+1} =
    e_i g_i deg Φ_i, a multiple of e·m — proper" read Def 5.10 at the
    TOWER-chain data (e_i, deg Φ_i) where the printed hypothesis reads
    the intrinsic (e_{i+1}(µ), m_{i+1}(µ)) — V28-2's gap. PROVISO (owned
    with (KP) by GD-1): the tower chain extended by the state key,
    (Φ₀, …, Φ_{i+1}) with valuations w_j, is a MACLANE CHAIN — Def 3.1's
    condition Φ_{j+1} ≁_{w_{j+1}} Φ_j at every step. This is AUTOMATIC
    at degree-increasing steps (equivalence forces equal degree:
    Φ_{j+1} ∼ Φ_j ⟹ Φ_{j+1} |_{w_{j+1}} Φ_j ⟹ deg Φ_{j+1} = deg Φ_j
    by w-minimality, the printed argument at fgmn.txt:782–784) and a
    GENUINE side condition at degree-preserving refinement reads
    (e_j·g_j = 1). Under the proviso, the remark fires in two displayed
    steps [REV 4, F7 — the completion step was consumed silently at
    rev 3]: FIRST, complete the tuple to a chain OF a valuation — the
    remark speaks of MacLane chains of a valuation µ, and Φ_{i+1} is not
    yet a chain key: augment w_{i+1} at Φ_{i+1}, w′ :=
    [w_{i+1}; w′(Φ_{i+1}) = γ] for any admissible γ > w_{i+1}(Φ_{i+1})
    (such an augmented VALUATION exists by MacLane Thm 4.2, whose
    hypotheses — Φ_{i+1} a key for w_{i+1} in the printed Def-4.1 sense —
    are exactly what (KP) at the state supplies through the §5
    recomposition, and the proviso's chain condition makes the extended
    tuple a MacLane chain of w′ per Def 3.1), so that (Φ₀, …, Φ_{i+1})
    is a MacLane chain of w′ with Φ_{i+1} the key at position i+2 and
    the remark's µ_{i−1} reading w_{i+1}; THEN FGMN's displayed remark
    "In every
    MacLane chain, all φ_i are proper key polynomials for µ_{i−1}, by
    Lemma 5.3" (fgmn.txt:1838) applies to the completed chain and gives
    Φ_{i+1} proper for w_{i+1}, so Prop 5.14/Cor 5.15 apply as printed;
    in particular the proviso is automatic when every read has
    e_j·g_j ≥ 2 (the extended chain is then OPTIMAL, and the intrinsic
    identification (e_i, deg Φ_i) = (e_{i+1}(µ), m_{i+1}(µ)) also holds
    directly), and unneeded when e_{i+1}(µ) = 1 ("If e_r(µ) = 1, all key
    polynomials are proper", fgmn.txt:1835). Note clause (3)'s cite
    (Cor 5.6(2), a chain-internal statement) reads the SAME extended
    chain, so the proviso serves both. rev-0 attack item (5) stays
    RESOLVED (FGMN does provide clause (4)); LITUNIT §5's
    properness-check sentence is SUPERSEDED by this proviso (appended
    note there, V28-2)].

Corroborating GMN pin (VERIFIED, `GMN_citations.md`): Thm 2.26 (Theorem of
the product): N_r^−(fg) = N_r^−(f) + N_r^−(g) and
R_λ(fg) ∼ R_λ(f)·R_λ(g) — clause (1)+(2′)'s composite at GMN's own
normalization.

**Why (2′) must carry the march (the rev-0 defect; V26-2).** Rev 0 stated
clause (2) with per-slot data only ("the κ-th coefficient is (fixed
invertible twist τ_κ)·(residue)") and then DEDUCED the one-constant full
identity by asserting "the τ-system's carry law makes the single anchor
division absorb ALL slot twists up to one global K_i^×-constant". That
deduction is FALSE, by two independent countermodels, both displayed:

* *V26's formal countermodel.* τ_κ := a^κ (a ∈ K_i^×, a ≠ 1) is fixed,
  invertible, and multiplicative, yet sends the residual digit vector
  (1, 1) to 1 + az, and no anchor division (τ₀ = 1) makes 1 + az a scalar
  multiple of 1 + z: per-slot twisting contains the variable rescaling
  z ↦ az, which is NOT multiplication of the polynomial by a scalar.
* *CU-1's machine countermodel (the coherent-normalization finding).* The
  depth-3 gate's first version read order-2 residuals slot-FRESH and FIRED
  (60 vertex-law violations at (3,3,3)); under the coherent ANCHORED-MARCH
  convention — slot normalizers marching by the fixed weight-γ₂ step
  monomial, class wraps folded into K₁ through x̄^e = ȳ·p̄^h — every law
  holds, and the slot-fresh reading is refuted **1,818 times** in the
  production run (`verification/openmath/o1thr_depth3_check.py`, 470,736
  order-2 instances; `CU1_phaseB_attempt.md` §1). The naive reading is
  invisible at p = 2, g = 1 — exactly why no depth-1/2 suite saw it.

So the one-anchor form cannot be DERIVED from per-slot bijection data; it
must be ASSERTED, and it is what the literature actually proves (FGMN
Thm 4.2's identity has one coefficient-dependent monomial ϕ_r(g), not free
per-slot twists). Rev 2 therefore moves the march INTO the statements: of
(GR-B) (this clause), of GD-3(full)/(slot law) (§2), and of [F.2]'s
normalization reading (§7.4(c)). The residual open content of (GR-B) is the
TRANSCRIPTION of the pinned FGMN statements into the project's objects —
the ℤ-normalized w vs FGMN's ℚ-normalized µ, the sign-mirrored slopes, and
the FGMN-R vs GMN-Def-2.20/2.21 twist conventions (LITUNIT §5 caveats
(i)–(iii)) — declared HERE, inside the package, not hidden.

**Exact-match check of (2′) against the printed Thm 4.2 (V28-1; executed
and displayed at rev 3).** The printed statement (fgmn.txt:1414; the
LITUNIT §5 quote), verbatim:

> **Theorem 4.2.** Let g ∈ K[x] be a non-zero polynomial and let
> α = µ(g). Then, H_µ(g) = ϕ_r(α)R_{r,α}(g)(y_r) = ϕ_r(g)R_r(g)(y_r).
> In particular, P_α(µ)/P_α^+(µ) = ϕ_r(α)∆ is a free ∆-module of rank
> one.

Clause-for-clause against (2′), through the §1.2 index dictionary
(depth r = i+1; e_r = e_i, h_r = h_i):

* in(f) ↔ H_µ(g); w(f) ↔ α = µ(g); ȳ ↔ y_r; K_i ↔ F_r.
* ϕ_α ↔ ϕ_r(α) = x_r^{s(α)}p_r^{u(α)} and ϕ(f) ↔ ϕ_r(g) =
  x_r^{s(g)}p_r^{u(g)} [FGMN Lemma 4.1: s(α), u(α) from eq. (12) with
  0 ≤ s(α) < e_r; "(s(g), u(g)/e(µ_{r−1})) is the left end point of
  S_{λ_r}(g)"] — so s₀(f) ↔ s(g), the side's initial abscissa.
* R^{gr}_α ↔ R_{r,α} [Def 3.16: coefficient j at abscissa
  s_j = s(α) + je_r] and R^{gr} ↔ R_r [Def 3.18:
  R_r(g) := R_{r,α}(g)/y^{j₀}, j₀ = ⌊s(g)/e_r⌋].
* The two-form consistency ϕ(f) = ϕ_α·ȳ^{j₀} ↔ the equality of the two
  printed sides: eq. (16) of the printed proof gives ϕ_r(α)·y_r^j =
  x_r^{s(α)+je_r}p_r^{u(α)−jh_r}; at j = j₀ the exponent pair is
  (s(g), u(g)), because the left end point of the side lies on the
  weight-α lattice line at parameter j₀ (Def 3.16's parameterization
  P_j) — so ϕ_r(α)·y_r^{j₀} = ϕ_r(g); and ord_y(R_{r,α}(g)) = j₀ is
  Cor 4.4(3).

The match is EXACT as a two-form statement. The only intentional
differences, all three declared [REV 4: (c) added — F2]: (a) (2′) claims
the identity only for f
satisfying the creating read's (R1)/(R2) — a scope RESTRICTION (the
printed theorem is unconditional in g ≠ 0); (b) the three normalization
caveats (LITUNIT §5 (i)–(iii): λ-sign mirror, ℚ- vs ℤ-normalized values,
FGMN-R vs GMN-Def-2.20/2.21 twists), which remain (GR-B)'s declared open
transcription content; (c) the ARGUMENT TYPE of the residual operators:
the printed R_{r,α}(g), R_r(g) take the POLYNOMIAL g, while (2′)'s
R^{gr}_α, R^{gr} take the graded element in f — so the dictionary rows
"R^{gr}_α ↔ R_{r,α}" and "R^{gr} ↔ R_r" consume the lemma-shaped fact
that *the printed operators factor through the initial form*: R_{r,α}(g)
depends on g only through H_µ(g) (true of the printed Def 3.16 — its
coefficients are the residues of the attained digits of g, which are
exactly the data of H_µ(g) — but a FACT about the printed definition, not
a notation change; the pass-2 verification recomputed it, F2). Difference
(c) is carried inside (GR-B)'s declared transcription residue alongside
(b) — nothing else is consumed beyond (a)/(b)/(c). Rev 2's bracket quoted
only the SECOND printed form while its displayed identity paired the
FIRST form's anchor with the second form's residual; that mixed pairing
is retired (V28-1), and with it rev 2's claim that the statement was
already "modeled verbatim": the verbatim modeling holds of THIS two-form
restatement.

**Order-1 instance (proved; the march made explicit).** By KEY-g(iii),
in(f) = Φ̄₀^{s0}·π̄^{β}·hom_{e,h}(R_λ(f)), and
hom_{e,h}(R)= Σ_κ c_κ π̄^{(g′−κ)h}Φ̄₀^{eκ} = π̄^{g′h}·Σ_κ c_κ·(Φ̄₀^{e}π̄^{−h})^κ,
so with ȳ := Φ̄₀^{e}π̄^{−h} (weight e·h − h·e = 0) and anchor
ϕ := Φ̄₀^{s0}π̄^{β+g′h}:
    in(f) = ϕ · R_λ(f)(ȳ)
— exactly (2′)'s SIDE-anchored form: the displayed ϕ IS ϕ(f), keyed to
the side's initial abscissa s₀ (the weight-only anchor ϕ_{w(f)} differs
from it by ȳ^{⌊s₀/e⌋} — V28-1's distinction, visible already at order
1), with e_b = 1, 𝒟 = K₀, every marched twist = 1, and
clauses (1)/(3)/(4) read off KEY-g/RES-1 (rev 0's order-1 claim, now
displayed in the march's own shape).

### 7.3 The theorems

**Theorem GD3-MIN (min core, general order).**
ord_{ψ̂}(in f) = ord_{ψ_i}(R_λ(f)), under EITHER of:
* **Route A (no GR-B):** TIE + the pin **(T-2) [PINNED at LITUNIT §2 — was
  rev 0's flag]**: for P of type t (order i+1, GMN indexing) with
  representative φ_{i+2}, the principal part of N_{i+2}(P) — including the
  slope-−∞ side — has length ω_{i+2}(P) = ord_{ψ_{i+1}^{GMN}}(R_{i+1}(P)).
  Printed home: the ω-DEFINITION (GMN §2.1 Notation: ω_r(P) :=
  ord_{ψ_{r−1}}(R_{r−1}(P))) + **Lemma 2.17(2)** (length of N_r^−(f) is
  ω_r(f)) + **Lemma 2.17(3)** with **Def 1.8** (the slope-−∞ side and its
  length, "including the side of slope −∞" verbatim). The consumed
  FIRST-ATTAINMENT form (V26-6; rev-0 attack item (6)) is displayed in the
  printed PROOF of Lemma 2.17 via Prop 2.8: "ω_r(g) = i₀, the least
  abscissa with u_{i₀} = u" (gmn.txt:1808) — ω is the least development
  abscissa attaining min u_k. Given (T-2), the first attainment of
  min_k u_k is at k = ω = ord_ψ(R_λ f) (REC-c1: the u_k ARE the
  N-heights); and by DEVID + the WGEO(b) factorization (O-1thr §2.5,
  verbatim: gr a domain + (KPa) block), the first attainment is at
  ord_{ψ̂}(in f). Equate.
* **Route B (no completeness, any O):** (GR-B). Two preliminary displays
  [REV 4 — F1, F4]:
  *Domain membership (F1).* Every element the operators are fired at below
  is nonzero homogeneous, hence in their §7.2 domain: in(f) is homogeneous
  by construction, and its divisors β, ψ̂^kβ are homogeneous by
  **Lemma HOM-FACT** — *in a ℤ-graded domain, every divisor of a nonzero
  homogeneous element is homogeneous* (proof: let δ = x·y with δ
  homogeneous, x, y ≠ 0; the top and bottom components multiply without
  cancellation in a domain, so x_top·y_top and x_bot·y_bot are both
  nonzero components of δ, which has only one; hence
  deg x_top + deg y_top = deg x_bot + deg y_bot, forcing x_top = x_bot
  and y_top = y_bot: both factors are homogeneous) — and nonzero since gr
  is a domain.
  *The (g3) bounding datum, displayed (F4).* A domain alone does not bound
  divisibility chains; what bounds ψ̂-chains at the state order is
  **(BOUND)**: ψ̂^k | in(f) ⟹ k·deg Φ_{i+1} ≤ deg f. [Proof — DEVID's
  pattern re-run from (V1) + (KPa)-block at the state, both granted in
  (GR-B)'s setting: let f = Σ_j C_j Φ_{i+1}^j be the development, top slot
  j_max = ⌊deg f/deg Φ_{i+1}⌋, and suppose k > j_max. By DEVID,
  w(f) = min_j(w(C_j) + j·γ̂) =: u₀ with attaining set S; the sum
  Σ_{j∈S} in(C_j)·in(Φ_{i+1})^j is NONZERO (else w(f) > u₀, against
  DEVID), so it equals in(f) = Σ_{j∈S} in(C_j)·u^j·ψ̂^j, every j ≤
  j_max < k. Set m₀ := min S and factor: in(f) = ψ̂^{m₀}·B,
  B := Σ_{j∈S} in(C_j)·u^j·ψ̂^{j−m₀}. From ψ̂^k | in(f) and gr a domain,
  ψ̂^{k−m₀} | B with k − m₀ ≥ 1; so ψ̂ divides B's j = m₀ term
  in(C_{m₀})·u^{m₀} = B − ψ̂·(Σ_{j∈S, j>m₀} in(C_j)u^jψ̂^{j−m₀−1}), and
  u invertible gives ψ̂ | in(C_{m₀}) — contradicting (KPa)'s block
  (C_{m₀} ≠ 0 of degree < deg Φ_{i+1}). So k ≤ j_max.] At order 1 the
  bound is VAL-g's explicit ring (Φ̄₀-degrees add). Note (BOUND) consumes
  no (GR-B) clause — it is proved, not assumed.
  *The route.* Write in(f) = ψ̂^m·β with
  ψ̂ ∤ β (m := ord_{ψ̂}(in f), well-defined: gr a domain + (BOUND), the
  §1.3 (g3) data now both displayed). Apply R^{gr} — fired at the nonzero
  homogeneous in(f), and clauses (1)+(3) fired at the nonzero homogeneous
  pairs (ψ̂, ψ̂^{k−1}β), k = m, …, 1, all in-domain by the display above:
  R^{gr}(in f) ∼ ψ_i^m·R^{gr}(β), and ψ_i ∤ R^{gr}(β) by (4).
  So ord_{ψ_i}(R^{gr}(in f)) = m; by (2′), R^{gr}(in f) ∼ R_λ(f). ∎
Route A inherits GMN's scope and completeness; Route B inherits (GR-B)'s.
Either alone suffices for the cluster.

**Theorem GD3-FULL + OL-B (general order, from (GR-B); REV 2 — the proof
is now an unwinding, not an absorption; V26-2. REV 3 — the divisor is the
SIDE-anchored ϕ(f); V28-1).** Given (GR-B):
(full) dividing in(f) by the SINGLE anchor monomial ϕ(f) — [F.2]'s anchor
normalization is the side's initial-vertex monomial, read in the
anchored-march convention; dividing by the weight-only ϕ_{w(f)} instead
would leave the extra factor ȳ^{j₀}, i.e. the weight-indexed vector
R^{gr}_{w(f)}(in f), V28-1 — yields
R^{gr}(in f)(ȳ) ∈ Δ; through the structure clause Δ = K_i[ȳ] its
coefficient vector IS R^{gr}(in f); and R^{gr}(in f) ∼ R_λ(f) by (2′). So
R^{nrm} = R^{gr}(in f) = c·R_λ(f) for one global c ∈ K_i^×. Nothing is
"absorbed": the one-anchor form is (2′) itself. [Domain note, REV 4 —
F1: the only operator application here is R^{gr} at in(f), nonzero
homogeneous by construction, inside the §7.2 domain; the "division" by
ϕ(f) is multiplication by the inverse of an INVERTIBLE monomial inside
the graded ring, not an operator firing — no domain question arises.]
(OL-B) by (2′)'s slot clause, the κ-th coefficient read is (the marched
normalizer's fixed degree-0 unit)·(K_i-residue of the κ-th stride digit) —
a unit scaling composed with the residue map: a bijection of K_i per slot;
and the slot-to-exponent coupling is κ ↦ ȳ^κ from the ϕ(f) anchor
(κ ↦ ȳ^{j₀+κ} in the weight-indexed frame), linear in κ (Cor 4.4(2)'s
slot granularity), so no un-cancelled z ↦ z^a (a ≥ 2) survives anywhere in
the evaluation chain — a skip-march coupling κ ↦ ȳ^{aκ} is excluded by
(2′)'s displayed form.
*Proof.* Both clauses are re-readings of (2′) + the structure clause; at
order 1 they are RES-1 (proved), see the §7.2 order-1 display. At orders
≥ 2 the content sits entirely inside (GR-B) — the full form is priced AT
(GR-B), and the statement now carries exactly what (GR-B) asserts. Rev 0's
per-slot-to-one-anchor deduction is DELETED (V26-2). ∎

### 7.4 Corollary IFACE (the stage-interface instantiation) — REV 2: per-clause scope (V26-3)

The engine's stage-interface axiom package (O-2a §4, [F.0]–[F.2]) is
SATISFIED by the MacLane/GMN valuation — with each clause at ITS OWN scope
(rev 0 advertised the composite at "any O": V26-3's critical error, since
the polygon clause rides REC-c1, which is scoped to TIE = GMN's scope):

* **(a) [any O; given (V1) + (KPa-block) through the state — PROVED].**
  u := w_{i+1} is ℤ-valued, multiplicative, ultrametric ((V1)); and (S-K1)
  holds at Φ̂_{i+1} := Lift_i(c) = Φ_{i+1} (DEVID).
* **(b) [GMN's scope: K/ℚ_p finite, F finite; modulo TIE's pinned
  transcriptions].** The stage window polygon of f is N_{i+2}(f), record
  verbatim (REC-c1). Outside GMN's scope the sheared window polygon of
  §1.2 still EXISTS and computes w (DEVID) — but the identification with
  GMN's N_{i+2} is neither defined nor claimed there.
* **(c) [any O; = (GR-B), OPEN at orders ≥ 2; order 1 PROVED (RES-1)].**
  [F.1]'s stage residual is R_λ(f) up to the [F.2] anchor normalization,
  READ IN THE ANCHORED-MARCH CONVENTION (dig := the graded digit read
  through the marched monomial system, anchored at the SIDE-anchored
  ϕ(f) — §7.2, V28-1; 𝒟 := the (GR-B) carry algebra;
  GD3-FULL). At order 1, e_b = 1 and 𝒟 = K₀ is trivial.

So every engine lemma quantified over the interface (OL-2-min/OL-3-min/
OL-5's mathematical halves) is INSTANTIABLE at w at the scope of the
clauses it consumes — "the engine's axiom holds at the GMN valuation"
(SYNTH4 GD-2 note) — and the entire remaining gap between the carriers' u
and this w is engine conformance, priced in §8.

## 8. Consumption map (every carrier of the two families, served or re-priced)

| carrier (exact label) | what this brief supplies | residue left at the carrier |
|---|---|---|
| O-1thr (V1) | proved order ≤ 1 (any d₀, any O); BRIDGE + MacLane composition beyond (§5) | (KP) incl. (KPirr) [GD-1]; ML-scope CLEARED |
| O-1thr (V2)(a) | = (KPa): proved at order 1/standard lift, any d₀ (KEY-g, incl. primeness) | GD-1 beyond |
| O-1thr (V2)(b) | **PROVED at every order** given (V1)+(KPa-block) (DEVID) — the owed dictionary closed | — |
| O-1thr (V2)(c) | order 1 proved (KEY-g(iii)); general: Route A (T-2 PINNED) or Route B (GR-B) | T-2 transcription-recheck or (GR-B) |
| O-1thr Cor VTX deeper leg ("(V2b) at level i") | DEVID at level i | (KP) cumulative |
| O-1thr WGEO/(M6c) beyond the proved slice | its four named inputs now individually priced (§4 corollary) | (KP), GD3-MIN pin |
| O-2a OL-2(a) [u_{i+1} = v_{i+1}] | the w = v half (TIE, mod PINNED transcriptions AND the §6 policy pin — rev 4, F3); u = w re-priced as engine conformance via IFACE | engine seam (GD-4/6/S-1) + the policy pin [GD-1/OL-1(b)] |
| O-2a OL-2(b)–(d), OL-2-min | REC-c1 (math half, verbatim (c1)); emitted-record typing kept, HK52 trap displayed | engine seam |
| O-2a OL-3 (full) / OL-3-min | GD3-FULL from (GR-B) / GD3-MIN two routes; order 1 outright (RES-1) | (GR-B) resp. T-2-or-(GR-B); engine seam |
| O-2a OL-5 (b′)+(c′) | mathematical half proved (KEY-g(iii)+RES-1 = the (D3) formulas' semantics, over any O) — GD-5's composition-note input | engine seam (S-1 audit) |
| O5triple (I-1)(a) legs "through OL-2a-2/3" | supplied at the dictionary level by REC-c1 + GD3-MIN | as OL-2/OL-3 above |
| O3design JC-2 / JC-3 | = GD-2 / GD-3 as restated here (the units' targets) | the units consume this brief |
| O-9 OL-B | general form from (GR-B), STATED with the anchored march; order-1 instance proved | (GR-B) |
| D-3 / UNRAM-TRANSFER; O-8b (R1)–(R4) scope | KILLED as a separate node: every proved statement holds over O_δ = W(F_{p^δ}) as an instance (§1.1) | O-8b's engine-side clauses only |
| CU-1 handoff note 1 (GD-3 must carry the coherent slot normalization) | EXECUTED at rev 2: the march is in GD-3(full)/(slot law), (GR-B)(2′), IFACE(c) | the depth-3 harness is the standing falsifier |
| (V1)/(V2) "owed normalization dictionary" (V24-G6, seam S-4) | §§1.2, 4–7 ARE the dictionary (MacLane display, index dictionary, TIE, residual routes) | LITUNIT pins (recheckable quotes) |

**Not claimed anywhere in this brief:** any statement about the implemented
engine's emitted records (u = w); OL-1/(KP) beyond order 1; OL-4/OL-6
(bookkeeping families GD-4/GD-6); exhaustiveness/drainage; anything about
boxes or runs. The brief is a statement about valuations, keys, polygons,
and residuals only.

## 9. Falsifier record (falsifier-first)

`verification/openmath/gd23_check.py` (this unit; exact arithmetic, seed
pinned; run BEFORE the rev-0 brief was written). Perimeter chosen to be
exactly what T-E/T-F did NOT cover: root keys of degree d₀ = 2, and the
equal-characteristic base O = F_p[[t]] (the arbitrary-DVR claim), with
lifts carrying genuine t-noise (Φ₀ = X² + (φ̄₁ + t)X + φ̄₀).
Configurations: (ℤ₂, X²+X+1), (ℤ₃, X²+1), (F₂[[t]], X²+X+1), (F₃[[t]],
X²+1). Per sampled monic f realizing a root read (ord_{φ̄}(f̄) = m ∈
{2,3,4}, random π-power digits, beyond-window slots included):

* GAUSS-g: development-min = plain content (asserted on every sample);
* VAL-g: w₁(AB) = w₁(A) + w₁(B) — **2,622 random pairs, 0 violations**;
* A-law (WGEO(a) display): w₁(f) = e·u₀ + s₀·h at every harvested side —
  **1,910 sides, 0 violations**;
* KEY-g: standard lift monic of degree e·g·d₀, every monomial of w₁-weight
  egh, w₁(Φ₁) = egh — **2,115 keys** (every monic irreducible factor ψ of
  every side residual over F_{p²}, complete factorization), **0 violations**;
* DEVID + GD3-MIN: min_k (w₁(C_k) + k·egh) over the Φ₁-development equals
  w₁(f), AND first attainment at k = ord_ψ(R_λ(f)) — **2,115 instances, 0
  violations**.

**Falsifier-of-record for the rev-2 march clause (the one NEW mechanism).**
The depth-3 harness rev 0 queued now EXISTS (CU-1 unit, 2026-07-31):
`verification/openmath/o1thr_depth3_check.py` — **470,736 order-2
instances, 0 violations** of the nine per-site laws under the COHERENT
anchored-march convention (the (GR-B)(2′) shape, pinned in the checker's
docstring), while the naive slot-fresh residual reading is REFUTED 1,818
times in the same run (§7.2). That run exercises exactly the convention
rev 2 moves into the statements — before rev 2 was written. Its perimeter
is d₀ = 1 root keys; the d₀ ≥ 2 depth-2 harness (order-2 states over
degree-2 root keys — (GR-B)'s order-2 instance at g > 1 parents)
remains QUEUED.

Standing traps carried from the corpus (not re-run here, cited as gates):
T-E (33,043 depth-1) and T-F (41,720 depth-2, d₀ = 1) — T-F's "7,472
violations of the UNsheared reading" is the live countermodel showing the
§1.2 convention is load-bearing; the HK52 compiled stretch gate
(`HC2/HK52_stretchGate.lean`) is the standing engine-side GD-2(c) trap.

## 10. Honesty ledger (exact conditionality; where to attack)

**Proved outright here (any O; perfectness/completeness unused):** DEV-g,
POS-g(i)–(iii) (+ (iv) at orders ≤ 1), GAUSS-g, VAL-g, KEY-g (incl.
(KPirr) at j = 0), RES-1, DEVID, UNIQ, BRIDGE, and the reductions
(GD3-MIN Route B given (GR-B); GD3-FULL/OL-B given (GR-B); POS-g(iv) at
higher orders given (V1)+(KP) one level down; IFACE(a)).

**Proved modulo pinned transcriptions (all loci in hand — LITUNIT; a
verifier may re-check any quote against the PDFs):** TIE (T-1 = GMN
Prop 2.7(1)(3) + Lemma 2.2(2); (P-key) = Prop 2.7(4)), REC-c1 (rides TIE),
GD3-MIN Route A (T-2 = GMN §2.1 ω-def + Lemma 2.17(2)(3) + Def 1.8;
first attainment per the printed proof), V1-tower beyond order 1 (MacLane
Def 4.1 + Thm 4.2, printed scope cleared). [REV 4 — F3:] TIE additionally
carries the §6 POLICY PIN (the engine/O-1thr keys ARE the GMN
representatives — GD-1/OL-1(b) territory, a displayed hypothesis), and
REC-c1 and GD3-MIN Route A INHERIT it; pass 2 found this hypothesis
honest in §6 but dropped at every summary layer — it is now advertised
at §0, the status table, §8 OL-2(a), and here.

**Open (the unit's honest residue):** (GR-B) at orders ≥ 2 — ONE package,
now cited clause-by-clause (§7.2, §11), proved here at order 1, its
anchored-march clause (2′) carrying the load; the declared open content is
the transcription of the pinned FGMN statements into the project's
normalization (LITUNIT §5 caveats (i)–(iii), PLUS [rev 4, F2] the declared
difference (c): the printed operators' factor-through-H_µ property, a
lemma-shaped fact about Def 3.16 consumed by the dictionary rows) — the
two-form anchor
PAIRING itself is no longer open content: it is verified exact against
the printed Thm 4.2 (§7.2 match display, V28-1) — plus [rev 3, V28-2]
clause (4)'s properness PROVISO (the extended tower chain is a MacLane
chain; genuine only at e_j·g_j = 1 refinement reads), owned with (KP) by
GD-1. Plus the inputs owned
elsewhere: (KP) = (KPa)+(KPirr)+(KPb) [GD-1] beyond order 1; every
engine-conformance statement (GD-4/GD-6/S-1).

**Attack here first (rev-4 list; (1)–(8) carried from rev 3, (9)–(11)
new):** (1) Lemma BRIDGE — the homogeneous-lift
step (every homogeneous class is some in(c)) and the two ultrametric
forcing steps; and whether §5 applies it only where W is already a
valuation (it is applied at e_i·w_i under the (V1)_i induction
hypothesis). (2) The POS-g division-control display — the index
bookkeeping ((KP_{i−1}) at level i, i.e. (KPa) for Φ_i AT w_i), and the
consumption note's claim that no consumer needs (iv) unconditionally at
order ≥ 2. (3) [updated at rev 3] The §7.2 exact-match display — re-check
the two-form pairing, the ȳ^{j₀} relation, and every dictionary row
against the printed Thm 4.2 / Lemma 4.1 / Defs 3.16, 3.18 / Cor 4.4 /
eq. (16); the three LITUNIT §5 caveats remain the open transcription
content; check nothing ELSE is silently consumed.
(4) IFACE's per-clause scope split against every §8 consumer (does any
consumer read clause (b) outside GMN's scope?). (5) Whether GD-3(full)'s
statement carries the march at every consumption site (O-9/OL-B; the CU-1
handoff row of §8). (6) TIE's index dictionary (§1.2) — the off-by-one
seam. (7) KEY-g(ii)'s hom-descent at gcd(e,h) = 1 — the lattice-line
argument at e = 1 corners (carried from rev 0's list; V26 found no error
there but probed order 1 only). (8) [new at rev 3, V28-2] The properness
PROVISO in (GR-B)(4): does any consumer instantiate (GR-B) at a tower
with degree-preserving reads (e_j·g_j = 1) without discharging
Φ_{j+1} ≁_{w_{j+1}} Φ_j; and the intrinsic-data identification
(e_i, deg Φ_i) = (e_{i+1}(µ), m_{i+1}(µ)) at non-optimal chains.
(9) [new at rev 4, F1/F4] The Route-B preliminary displays: Lemma
HOM-FACT's top/bottom-component argument, and (BOUND)'s DEVID-pattern
re-run — especially whether every hypothesis it consumes ((V1) +
(KPa)-block at the state) is genuinely granted inside (GR-B)'s setting,
and the §7.2 domain definition's claim that "side-supported" is automatic
for homogeneous elements. (10) [new at rev 4, F2] The declared difference
(c): re-check against the printed Def 3.16 that R_{r,α}(g) depends on g
only through H_µ(g). (11) [new at rev 4, F7] The proviso's completion
step: that MacLane Thm 4.2's hypotheses at the augmentation of w_{i+1} by
Φ_{i+1} are supplied by (KP) at the state through the §5 recomposition,
and that Def 3.1's chain condition on the completed tuple is exactly the
proviso's displayed condition.

## 11. Literature pins (single list; status per pin — post-LITUNIT)

* MacLane, *A construction for absolute values in polynomial rings*, Trans.
  AMS 40 (1936) 363–395. Def 4.1 (key polynomials: irreducibility +
  minimal degree + monic) and Thm 4.2 (augmented values are values) —
  QUOTED VERBATIM, LITUNIT §3; printed scope: "any field K", discreteness
  only in §§7/8/16 — ML-scope flag CLEARED, Vaquié fallback dropped.
  (GR-B)'s MacLane half: Thm 10.2, Lemma 10.1, Lemmas 11.1–11.2 (the
  equivalence-divisibility ⟷ residue-ring dictionary corroborating
  BRIDGE), Thm 12.1 (residue ring = F_t[y]; commensurable hypothesis,
  automatic for ℤ-valued chains), Cor 12.2 — numbers PINNED (were rev 0's
  "FLAGGED"). Source caveat: read from D. Ford's re-typeset PDF; the §.n
  numbering is the standard one (used by FGMN's own citations), original
  TAMS page numbers not checkable from that copy.
* GMN, Trans. AMS 364 (2012) 361–416 (arXiv:0807.2620v2 numbering; the
  `docs/GMN_citations.md` divergence found by LITUNIT §6 is corrected by
  append there). Def 2.5/Prop 2.6 (v_r) [transcribed, O-2a (L2)];
  **Prop 2.7(1)+(3)+(4) with Lemma 2.2(2)** = T-1 + (P-key) [PINNED,
  verbatim at LITUNIT §1]; Thm 2.11/Def 2.12 (representatives) [pinned;
  GD-1's input]; Defs 2.19–2.21 (residuals) [pinned]; **§2.1 ω-definition +
  Lemma 2.17(2)(3) + Def 1.8** = T-2 [PINNED, verbatim at LITUNIT §2;
  first attainment in the printed proof via Prop 2.8]; Thm 2.26 (Theorem
  of the product) [VERIFIED verbatim].
* Fernández–Guàrdia–Montes–Nart, *Residual ideals of MacLane valuations*,
  J. Algebra 427 (2015) 30–75 = arXiv:1305.0775. Scope: an ARBITRARY
  discretely valued field, no completeness at the pinned statements
  [verbatim, LITUNIT §5] — carries (GR-B)'s arbitrary-DVR claim at orders
  ≥ 2. Per-clause homes for (GR-B): Thm 4.2 (the anchored-march identity,
  BOTH printed forms: H_µ(g) = ϕ_r(α)R_{r,α}(g)(y_r) = ϕ_r(g)R_r(g)(y_r),
  α = µ(g) — clause (2′)'s model, matched two-form at rev 3 (§7.2 match
  display, V28-1; rev 2 quoted only the second form while pairing across
  them), and with Lemma 5.13 clause (1)); Lemma 4.1 (the ϕ_r(α)/ϕ_r(g)
  anchor pair — rev 2's locus "Def 3.15" was a slip); Defs 3.15/3.16/3.18
  (twist units ε_i, weight-indexed R_{r,α}, side-read R_r — the §7.2
  dictionary additionally consumes Def 3.16's factor-through-H_µ
  property, declared as difference (c) at rev 4, F2); Cor 4.4(2)(3)
  (slot granularity + ord_y(R_{r,α}(g)) = ⌊s(g)/e_r⌋); Thm 4.8 + Cor 4.9
  (structure clause ∆ = F_r[y_r]); Cor 5.6(2) (clause (3)); Prop 5.14 +
  Cor 5.15 (clause (4), PROPER keys — rev-0 attack item (5) resolved);
  Def 5.10 (properness = degree a multiple of the INTRINSIC e_r(µ)m_r(µ))
  + Cor 5.12 (the chain characterization) + the Lemma-5.3 remark (chain
  keys proper) — consumed at rev 3 through the displayed PROVISO of
  §7.2(4) (V28-2), not through the degree law alone; Prop 5.7/Thm 5.8
  (key dictionary, for
  GD-1); Def 1.2 (key = monic + µ-minimal + µ-irreducible — (KPirr)'s
  modern printed form); Prop 1.9 (cites the augmentation theorem to
  MacLane Thm 4.2 — independent locus confirmation). Declared
  normalization caveats (INSIDE (GR-B)): FGMN's λ-sign mirror, the
  FGMN-R vs GMN-Def-2.20/2.21 twist conventions, ℚ- vs ℤ-normalized
  values (LITUNIT §5 (i)–(iii)).

— GD23 REV 4 (2026-08-02, recorded under the campaign date; the pass-2
report it repairs is `GD23_pass2_fable_verify.md`, 2026-07-31).
Verification
protocol: gap-check pass (per the ledger's status reset: GD23 =
ZERO-CRITICAL-PASS-2 WITH GAP DEBTS; this rev discharges the debts, and
the acceptance bar is the gap-check plus the standing two-pass protocol)
with the §10 rev-4 attack list + the
standard charge (quote-and-classify; check every "proved" against its
displayed perimeter; check the byte-stability claims of the preamble — the
V26-confirmed §3/§4 statements and proofs against rev 0, everything
outside the §0-pre-3 repair sites against rev 2, and everything outside
the §0-pre-4 repair sites against rev 3; re-check the §7.2
exact-match display, the declared difference (c), and the (GR-B)(4)
proviso (now with the F7 completion step) against the FGMN PDF; check
no consumer of §8's table is promised more than the status table of §0
grants).



