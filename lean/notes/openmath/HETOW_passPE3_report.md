# HETOW passPE3 — hostile verification report (fresh context) — THE ACCEPTANCE ATTEMPT

**VERDICT: CLEAN — 0 CRITICAL, 0 GAP, 1 MINOR.** The four owned
surfaces held under independent re-derivation: LEMMA HETOW-3's three
clauses re-proved end-to-end (the full-side endpoint pin, the
ϖ-normalized residual transport r̃^{μ₂} via the geometric coherent-
normalizer discrepancy, the peel via HE7-12(c) item (11) — the item
numbering verified against HE7_PROOF's own (c)-audit list); the §S1
termination row re-derived AT the corrected key (the wrap correction
moves unit factors only — heights, degrees, multiplicities, and hence
HE6R1-1's descent factor ℓd_r = e₂f₂ and both displayed bounds, are
gauge-invariant; the two μ₂'s agree at the full side, tight); the
letter clause's own derivation re-built at multi-wrap (the division
0 = Φ₂(ξ)/ϖ^{f₂u₂} is per-t — each slot's η^{W(t)} cancels its own
telescope defect Q((f₂−t)u₂) = (f₂−t)Q + W(t), no cross-t wrap
interaction — hand-instantiated at frame V's data, reproducing the
committed 42x/1372/2401x key and Z³+5Z²+4Z+5 exactly); the rider
C-A/C-B/C-C verified quote-by-quote against the PE2 artifacts and
re-derived (C-A's one-digit exact-height claim proved; C-B's
W(f₂−1) = 0 monic pin forced by 0 ≤ i(u₂) < e₁). Machine leg: pins
exact, body byte-frozen, three isolated re-runs content-identical mod
timing, letter-job code audited genuine. Fresh route GREEN 0
violations at three frames disjoint from the 10 prior contacts (first
ord(η) = 12, first f₂ = 4 triple-live wrap, first e₁ = 4), 9/9
dual-route σ jobs at sealed predictions, the σ-BLIND face reproduced
at f₂ = 4 with the letter tooth firing. Nothing at GAP-or-worse
survives this pass: per the arc rule the tower σ-closure counter
moves 1/2 → **2/2 — HETOW ACCEPTED**.

Target: `lean/notes/openmath/HETOW_RECON_2026-08-09.md` @ HEAD
(5b879e0 — the post-PE2 dated-corrections rider C-A/C-B/C-C).
Arc: seal (3e19eba) → PE1 NOT CLEAN 1C+2G+2m (5f821dc) → r1 (5335ae3)
→ PE2 CLEAN 0C/0G/3m (67cbab2, counter 0/2 → 1/2) → rider (5b879e0).
Protocol: quote-and-classify, fix nothing. CLEAN bar: nothing at
GAP-or-worse survives this pass's own verification. Prior routes NOT
re-walked (battery η = 1 frames; PE1's X + f₁ = 2 Y; r1's Z/W; PE2's
V/U/S/R/T): consumed as read, integrity re-checked at the freeze
level only.

---

## RIDER VERIFICATION (C-A / C-B / C-C) — HELD

**Byte-freeze.** HEAD file = the 5335ae3 state (34,475 bytes) + the
appended rider; prefix `cmp`-identical — append-only as claimed, and
the [r1] "body BYTE-FROZEN" sentence is machine-true.

**C-A (F-2 exemplar withdrawal + "either way" re-scope).**
(i) Re-derived: a plain degree-< D′ lift ĉ = Σ_j lift(d_j)x^{e₁j}
times n̂(k) puts digit j at dv-height k + j·e₁h (the (LIFT) form's
compensating π^{−jh} is exactly what holds every digit at height k),
so the exact-height-k part is the single j = 0 digit with residue
d_0·η^{−Q(k)} ≠ c·η^{−Q(k)} in general — the withdrawn exemplar
indeed breaks the paragraph's own residue premise at f₁ ≥ 2, and the
rider's "differ by e₁h-multiples" is the correct arithmetic. (ii) The
corner: at e₂ = 1, t = f₂−1 a moved piece x^{r′}π^{·}Φ′^{f₂} sits at
the leading slot with x-degree r′ + f₂D′ ≥ D″, so HE7-12's "deg w <
D″" (verified against HE7_PROOF §S5.8's statement) fails there — the
re-scope to the (LIFT) w = 0 branch is the honest fence. (iii) The
consumer sentence checked: HETOW.A(i) reads at the §S2 (LIFT) display,
and the GENHN dated erratum's corrected display pins "per digit at
f₁ ≥ 2: the exact-height (LIFT) output" verbatim — both consumers on
the w = 0 branch; the frame-R machine cite (r′ = e₁−2 = 1 maximal)
matches PE2's committed G5 leg.

**C-B (the W(f₂−1) = 0 pin + the σ-BLIND face).** W(f₂−1) =
⌊i(u₂)/e₁⌋ = 0 is forced by the range pin 0 ≤ i(u₂) < e₁ — re-derived.
The rescale argument re-derived: matching naive residues needs
η^{−W(t)} = λ^{t−f₂} at each live t; t = f₂−1 (c_{f₂−1} ≠ 0) forces
λ = 1, killing the rescale unless the wrap is trivial; when
c_{f₂−1} = 0 the escape λ² = η^{W(0)} exists exactly when η^{W(0)} is
a square — the rider's "e.g. ψ₂ = Z² − c₀ with η a square" is the
right instance. Frames X/Z/W all have c_{f₂−1} ≠ 0 (r̃'s Z-coefficient
nonzero at each), so "the [r1] records stand" is correct. The σ-BLIND
record checked against the committed PE2 artifacts: G2-naive-V-
SIGMABLIND σ = {(2,3)} on both routes with letter row [0,1] — and
re-derived by hand: frame V's naive β-form polynomial Z³+5Z²+6Z+4 is
root-free over F₇ (values at Z = 0…6 computed: 4,2,2,3,4,4,2), hence
irreducible, hence the naive key is itself an HE6-1 member at the
datum (3, N) with carrier-shaped σ = {(2,3)} and the wrong letter —
the letter clause is the only detector, as the rider records. This
pass's fresh frame M2 reproduces the face at f₂ = 4 (below).

**C-C (BOX-3 gains the multi-wrap axis).** The cited frame-V values
re-verified against `hetow_pe2_fresh_output.txt` @ 67cbab2 line by
line: corrected σ {(2,3)} (G1-coh-V), naive {(2,3)} (σ-blind), both
telescope identities (E1: Q(9)−3Q(3) = 1, Q(6)−2Q(3) = 1 — heights 9
and 6), slot residues = minpoly(β) = (5,4,5) = Z³+5Z²+4Z+5 (also
re-derived by hand from ψ₂ = Z³+Z²+Z+2, η = 3, Q = 1), trichotomy
8/8 (2 RAM + 2 2SIDED + 4 INERT-family), 3RAM {(6,3)}. W = (1,1,0)
is genuinely non-affine (successive differences 0, 1) ✓; ord(3 mod 7)
= 6 ✓. The axis row is honestly a disclosed-with-contact row now.

## OWNED SURFACE 1 — LEMMA HETOW-3 (re-derived end-to-end, HELD)

(a) FULL side ⟺ the side's residual degree f₂μ₂ = L/e₂ with L = μ₁
forces the hull to span [0, μ₁]; a hull spanning to J = 0 needs the
point (0, dv(A_0)) to exist, i.e. A_0 ≠ 0 (points with dv = ∞ are
absent from the hull, and a missing left endpoint caps L at μ₁ − J₀ <
μ₁); the endpoint is then the attained pin (0, κ₂μ₁). A_0 = f mod Φ′
gives Φ′ ∤ f. Also consumed upstream (HETOW-1(a)'s r̃(0) ≠ 0): the
attained endpoint makes R_{κ₂}(0) = ψ₂(0)^{μ₂} ≠ 0 ✓.
(b) The one-slope polygon: full side ⟹ the (J, dv(A_J))-hull IS one
side, so COROLLARY HE6-2″'s slope membership pins dv(Φ′(x₀)) = κ₂ at
each root. The residual transport re-derived: the side residual read
against side-COHERENT normalizers n̂(u₂)^m is ψ₂^{μ₂}; the ϖ-read
differs per coefficient by res(n̂(u₂)^m/ϖ^{mu₂}) = (η^{−Q})^m — a
GENUINE geometric power (an m-th power of one fixed unit, no wrap:
contrast Q(mu₂) = mQ + ⌊mi(u₂)/e₁⌋ for the single monomial n̂(mu₂)) —
so the ϖ-residual is η^{−Qf₂μ₂}ψ₂(η^QZ)^{μ₂} = r̃^{μ₂}, whose only
irreducible factor is r̃; β_{x₀} = η^{−Q}η₂(x₀) kills it. With (T1)/
(T2) from the outer genre, every locus root is a DEFINITION HE7-1
level-2 point of (κ₂, r̃); S_{κ₂,r̃} = roots(f), f_S = f (HE7-6
uniqueness), and HE7.A's μ₂ := deg f_S/D″ = μ₁/(e₂f₂) = the entry
multiplicity — the two μ₂'s agree exactly at the full side.
(c) The citation "HE7-12(c) item (11)" verified against HE7_PROOF
§S5.8's (c)-proof list: item (11) IS "the §S1 CONVENTION and the peel
at Ψ^{(w)}" (roots of the perturbed key are level-2 points by (b)'s
disk criterion; the sandwich forces (e, f) = (e₁ℓ, f₁d_r)); at
(ℓ, d_r) = (e₂, f₂) the peel emits (e₁e₂, f₁f₂) and drops μ₂ by 1;
disc f ≠ 0 is a level-1 frame row. Under HETOW-2's w = 0 branch the
transport is vacuous (Φ₂ IS a literal HE6-1 member). HELD.

## OWNED SURFACE 2 — the termination row at the CORRECTED key (HELD)

The charge: does the wrap correction preserve HE6R1-1's descent
arithmetic? Re-derivation: the correction multiplies slot coefficients
by the units η^{W(t)} — it moves residue LETTERS only. Everything
HE6R1-1's two displays consume is letter-blind: L_{κ₂} (hull width),
deg R_{κ₂} = L/e₂, deg r̃ = deg ψ₂ = f₂ (r̃ = η^{−Qf₂}ψ₂(η^QZ) is a
K-affine substitute — degree, irreducibility, multiplicity μ₂ all
preserved; the GAUGE-NAIVE assembled polynomial can be reducible, but
it is not the residual of any instantiated datum — the entry residual
is computed from f's own development, key-free), deg Φ₂ = D″ (monic
top slot untouched; corrected B_t x-degrees ≤ D′−1), and HE6-3(b)'s
n_λ = D′L_λ (level-1). So μ₁ ≥ L ≥ μ₂·m·... ≥ μ₂e₂f₂ with equality
at the full side — "HE7.C's bound, tight" ✓ — and μ₂ ≤ μ₁/(e₂f₂),
J ≤ log₂μ₁ − 1 ≤ log₂n − 2 (n = D′μ₁, D′ ≥ 2). At n = 12: μ₁ = 6 ≥ 4
admits the entry jump, μ₂ = 3 < 4 admits no second — HETOW.A(iv)'s
display ✓. The citation "LEMMA HE6R1-1 = THEOREM HE7.C [r1]" checked
against both sources: HE7.C [r1] states the widened condition with
HE6R1-1's bounds, as HE6R1 §S3.3 records. The level-3 floor row
(μ₂ ≥ 4) re-derived from HE7.A(3)'s own display m₂(ℓ₂d_{r₂}) ≥ 4 ≤
Σ L_{λ₂} = μ₂ — available at the corrected key since HETOW-2's w = 0
branch makes Φ₂ a literal member (no HE7-12 transport needed; where a
presentation does perturb, HE7-12(a)–(d) is transported by HETOW-2's
statement). Machine corroboration at descent factor e₂f₂ = 4: the
fresh M2 members (below) run the level-2 dictionary AT the corrected
key with μ₁ = 8 → μ₂ = 2 exactly = 8/4, PARI-exact on 3/3 rows.

## OWNED SURFACE 3 — the letter clause's own derivation at multi-wrap (HELD)

The clause (T(b)′(i)/HETOW-1(b)-(c): ψ₂(η₂(ξ)) = 0 ⟺ r̃(β_ξ) = 0 at
key roots) is, per the rider, THE load-bearing seam detector — so its
own soundness at multi-wrap was owned here. Re-derivation from
scratch: at a root ξ of the corrected key, each of the f₂ + 1 terms
of Φ₂(ξ) sits at dv = f₂u₂ EXACTLY ((LIFT) exact height + dv(Φ′(ξ))
= κ₂ exact + (f₂−t)u₂ + e₂t·κ₂ = f₂u₂), so the ϖ^{f₂u₂}-residues sum
to zero: β^{f₂} − Σ_t (c_tη^{W(t)})·η^{−(f₂−t)Q−W(t)}·β^t = r̃(β) = 0.
The wrap enters ONE term at a time — slot t's planted η^{W(t)}
cancels its own telescope defect (Q((f₂−t)u₂) = (f₂−t)Q + W(t), the
range-pin identity re-derived) — so the derivation is uniform in the
number of live wrap units; nothing couples distinct t's. The leading
term is wrap-free pointwise (β^{f₂} is a genuine f₂-th power of
Φ′(ξ)^{e₂}/ϖ(ξ)^{u₂} — no normalizer product occurs). Hand-
instantiated at frame V (f₂ = 3, two live units): the (LIFT) algebra
reproduces the committed key coefficients −42x/−1372/−2401x and
r̃ = Z³+5Z²+4Z+5 from ψ₂ = Z³+Z²+Z+2 exactly; the η₂-form naive
polynomial η^{3Q}N(η^{−Q}Z) = Z³+Z²+5Z+3 = the artifact's letter-tooth
polynomial. Machine leg at THREE live units: fresh frame M (f₂ = 4,
W = (2,1,1,0) — the first W = 2 anywhere): the G2C coset block reads
res(n̂(k)(ξ)/ϖ(ξ)^k) = η^{−Q(k)} in the key's own field at the four
slot heights k = 3, 6, 9, 12 (Q = 1, 3, 4, 6 — each the telescoped
(f₂−t)Q + W(t)), 4/4 exact; the G2L letter jobs fire the tooth at the
σ-blind frame M2 ([0,1] vs [1,0]). HELD — and the detector's
delicacy is real: at M2 the σ read alone accepts the broken key.

## FREE HUNT (seal + r1 + rider) — one MINOR

Checked and held (spot list, no re-walk of prior-pass routes): the Q/
n̂/ϖ integrality and positivity pins (e₁ | i₀k − i(k) via gcd(h,e₁)=1;
Q(k) ≥ 0; n̂(k)/ϖ^k = (π^h/x^{e₁})^{Q(k)}); the cocycle and telescope
displays (heights add; wrap ∈ {0,1}; Q additivity); the (LIFT)
π-exponent availability (a(k) ≥ (f₁−1)h ⟸ k > (D′−1)h ⟸ the node
floor u₂ > e₂D′h > (D′−1)h); HETOW-1(a)'s expansion (t-coefficient
−c_tη^{−(f₂−t)Q}; constant term via the full-side pin); the (SLOT₂) =
GENHN-2′ substitution arithmetic; the n_min = 12 derivation and both
§S3 menus; HETOW.A(iii)'s 4 > 3 exclusion display; the n₂-vs-n̂
normalizer row's twist-invariance warrant (HE7 §S1 CONVENTION's
displayed changing-choice law). The finding:

### F-α — MINOR — HETOW-BOX-3's "e₂f₂ ≥ 4" NOT-exercised row is not folded against PE2's frame S, the neighboring axis to the one C-C folded

**Quote (BOX-3 [r1], byte-frozen body):** "NOT exercised: μ₂ ≥ 4
(level-3-capable towers), e₂f₂ ≥ 4 (both ℓ ≥ 2 AND d_r ≥ 2 — first
live n = 24), …" **Quote (rider C-C):** appends only the f₂ ≥ 3
multi-wrap axis, citing frame V's machine contact.
**The defect.** PE2's committed artifacts contain frame S ((e₂,f₂) =
(2,2), both ≥ 2, e₂f₂ = 4) with first-e₂f₂ = 4 members ({(8,2)} RAM
forced, {(4,4)} integer-λ₂) at μ₂ = 2, n = 16 — machine contact on
the e₂f₂ ≥ 4 axis at trichotomy grade. The rider folded frame V's
f₂ = 3 contact (also μ₂ ≤ 3 trichotomy grade) into BOX-3 but left the
neighboring row uncited, so at HEAD the standing NOT-exercised list
undersells the record on one axis by the rider's own citation
standard. **Why MINOR:** the row's parenthetical "first live n = 24"
pins its reading to the μ₂ ≥ 3 box family, under which the row is
literally accurate (frame S's members are μ₂ = 2; no μ₂ ≥ 3 member at
e₂ ≥ 2 exists on record); no record sentence is false, and the fix is
one appended citation. **Failure scenario:** an axis-by-axis coverage
reader treats e₂f₂ ≥ 4 as machine-untouched and re-runs a redundant
first-contact instrument.

## CLEAN CHARGES (attacked and held)

1. Rider byte-discipline: body prefix cmp-identical to 5335ae3;
   counter left at 1/2 by the rider (not a scoring round) — correct
   per the arc rule.
2. C-A's three claims (exemplar's one-digit exact-height part; the
   leading-slot deg-w corner vs HE7-12's hypothesis as stated in
   HE7_PROOF §S5.8; the two-consumer trace) — re-derived/verified.
3. C-B's pin argument and its scope sentence (X/Z/W have c_{f₂−1} ≠ 0);
   the σ-blind record vs artifacts; the f₂ ≥ 3 non-affinity instance.
4. C-C's seven cited frame-V values vs the committed artifacts, plus
   hand re-derivation of key coefficients and r̃.
5. HETOW-3(a)(b)(c) as re-proved above, incl. the geometric-vs-
   telescope distinction that makes the side-residual transport
   wrap-immune while the per-height key display was not.
6. The termination row at the corrected key: letters-only invariance;
   both HE6R1-1 displays; tightness at the full side; the HE7.C [r1]
   equation of citations; n = 12 one-jump arithmetic.
7. The letter clause's derivation at any number of live wrap units
   (per-t cancellation; pointwise wrap-free leading term), plus its
   frame-V hand instantiation and frame-M machine legs.
8. HE7-12(c) "item (11)" numbering against the source list.
9. The letter-job code of `hetow_pe2_fresh.py` (nfmodpr in the key's
   own field via nfinit + idealprimedec; predictions derived in the
   docstring, values read from the field) — genuine, non-tautological.

## MACHINE LEG (pins, isolated re-runs, tautology audit)

* **Pins exact.** Body freeze verified (prefix cmp, 34,475 bytes).
  Runner/artifact md5s at HEAD match the §S7 and PE1/PE2 records:
  `hetowr1_supp.py` dcb8452f… / b52eb226… / 58f1ce0c…;
  `hetow_pe2_fresh.py` 6600588f… / c568444d… / d9e0e835…;
  `hetow_pe2_freshT.py` d2344a65… / 4568a280… / 55fd8137…;
  `hetow_checks.py` 31935fe8….
* **Freeze sweep.** git diff empty for the battery + PE1 + supp + PE2
  instrument/artifact files against their landing commits (3e19eba /
  5f821dc / 5335ae3 / 67cbab2 → HEAD).
* **Isolated re-runs (staged from their commits into /tmp, fresh
  dirs):** `hetowr1_supp.py` GREEN 29 jobs / 0 violations (21.4s);
  `hetow_pe2_fresh.py` GREEN 20 σ + 4 letter + 1 val-block / 0
  violations (131.4s); `hetow_pe2_freshT.py` GREEN (52.0s) — each
  stdout content-identical to its committed artifact mod the timing
  line, results JSONs equal mod timing fields.
* **Tautology audit.** The rider's load-bearing evidence (frame V
  letter tooth) re-audited at code level: η₂bar is computed by
  nfmodpr((Φ′(X))/(7X)) inside the actual key's field and tested
  against ψ₂ and the hand-derived η₂-form polynomial — the letter is
  read OFF the field, not assumed. This pass's own instrument shares
  no code with the program's readers or the prior fresh instruments
  (written from scratch; PARI built-ins are the shared oracle only).

## FRESH ROUTE (`hetow_pe3_fresh.py`, sealed 9a0bc5e; artifacts @ ddd59c6) — GREEN, 0 violations

Frames disjoint from the 10 prior contacts (battery ×2, X, Y, Z, W,
V, U, S, R, T). 11 exact checks + 9 dual-route σ jobs (routes agreeing
9/9) + 4 letter jobs + 1 four-height coset block.

* **FRAME M (p = 13, Φ′ = x² − 26, η = 2 primitive — first
  ord(η) = 12 anywhere; (e₂,f₂) = (1,4), u₂ = 3 — first f₂ = 4 wrap,
  W = (2,1,1,0): THREE live units, first W(t) = 2).** Two ψ₂
  sub-frames (design-phase search disclosed, predictions sealed):
  **M1** (σ-visible): corrected key σ {(2,4)} = carrier; naive tooth
  fired σ {(2,1),(2,3)} = the derived 1+3 split of N. **M2**
  (σ-BLIND at f₂ = 4): N irreducible ⟹ naive σ {(2,4)} = carrier σ,
  scored as EQUAL — and the letter tooth is the only detector:
  G2L [0,1] at naive vs [1,0] at corrected (nfmodpr). Slot residues
  = r̃ exact both sub-frames (E2); telescope identities 4/4 incl.
  W = 2 (E1); coset factors η^{−Q(k)} at k = 3,6,9,12 in-field 4/4
  (G2C). **First f₂ = 4 tower members** (μ₂ = 2, n = 16, descent
  factor e₂f₂ = 4): RAM {(4,4)}, SPLITEQ {(2,4)}×2, INERT {(2,8)}
  via the β-nonsquare digit (Euler criterion, design-pinned) — 3/3
  forced rows, dual routes.
* **FRAME N (p = 5, Φ′ = x⁴ − 10 — first e₁ = 4 wrap contact
  anywhere; (e₂,f₂) = (1,2), u₂ = 6, i(u₂) = 2, W = (1,0)).**
  Corrected σ {(4,2)} = carrier, slot residues = r̃ = Z²+3Z+4; naive
  tooth fired σ {(4,1),(4,1)} (N = (Z+1)(Z+2)); letter jobs [1,0] /
  [0,1] at EACH of the naive key's two primes.
* **Instrument errata (disclosed in the docstring, plumbing-only):**
  two gp input-line defects (default(parisize) eating its line; two
  brace definitions sharing a line) killed/degraded the σ-job
  plumbing on runs 1–2; no prediction or derived value was touched
  (run 2's readable values — sig2 route + letters + cosets — were
  each already at the sealed predictions), and the committed run is
  the first with both routes live.

## GRADE LINE

**CLEAN (0C / 0G / 1m).** F-α (BOX-3's e₂f₂ ≥ 4 row not folded
against PE2's frame S — literally accurate under its n = 24 reading;
one-line citation fix at the next fold). The owned surfaces (HETOW-3;
the termination row at the corrected key; the letter clause as
detector; the rider) held under this pass's own re-derivations; the
machine legs are intact (pins exact, three isolated re-runs
content-identical mod timing, freeze sweep empty); the fresh route ran
GREEN with 0 violations and 3/3 preregistered teeth fired (M1, N σ
teeth; M2 letter tooth at the first f₂ = 4 σ-blind face). Nothing at
GAP-or-worse survives this pass. Per the arc: the tower σ-closure
counter moves **1/2 → 2/2 — HETOW ACCEPTED**; the n ≥ 8 tower-stage
σ-story stands complete at the acceptance grade, inheriting min over
{GENHN, HE6, HE7, HE3} per the note's own status header. Suggested
one-liner for the next fold (fix nothing here): append frame S's
citation to BOX-3's e₂f₂ ≥ 4 row, scoped "μ₂ ≤ 2 trichotomy grade".
