# H7 FORGE — the first non-vacuous compiled (H7) instance (the OL-2 inhabitant)

**Unit:** synthesis-7 queue #8 (`docs/SYNTHESIS_PASS7_2026-08-08.md` — pass-7 F4: "the
(H7) restatement forge is now real (two pins)"). **Charge:** forge the first
NON-VACUOUS compiled (H7) instance — a hand-built `TreeModel` meeting OL-2's sharpened
inhabitant spec (`H7PROBE_2026-08-08.md` P-4: sides ≥ 2 at a branching site AND
non-diagonal `branchCellOf` on a sorry-free `TreeModel`), then state the PIN-CORRECTED
K-LOC/(I-τ) faces on it (N-H7-1's two measured pins: slope-keyed ε; D-8-guarded
decide-small gates) and decide them at the instance. **Artifact:**
`lean/LeanUrat/Scaffold/HDischarge/H7/H7Forge.lean` (`lake env lean` green, exit 0;
in-file `#print axioms` census: every forge declaration Lean-core
`[propext, Classical.choice, Quot.sound]`, zero `sorryAx`).

## 1. What was built (the instance)

A five-site depth-1 forge model at (p, F, n, N, m) = (2, ZMod 2, 2, 2, 4) —
`forgeModel : TreeModel` + `forgeCA : CellData` + `forgeTree : VTree` +
`forgeLedger : SiteLedger forgeTree forgeModel forgeCA fχ4` — the S6 shape of N-H7-1
(m = 2 roster, GENUINE side-separation: recorded slopes 1 vs 2, mirroring S6's
(x−3)(x²−3) slopes 1 vs 1/2) at the cheapest compiled scale. Box coordinates:
x0, x1 = the reduction block (entrance event = `rootCell` = 4 points), x2 = branch a's
read digit, x3 = branch b's read digit. The five realized sites (one-node chains, all
sharing the `.red` window entrance `esF`):

| site | node (slope) | read map | state event | fresh system |
|---|---|---|---|---|
| `t1` (reused from S2) | toyHead (1) | x2-projection `aRead` | W ∩ {x2=0} (2 pts) | [x2=0], mstar 1 |
| `t2` (reused) | sibNode2 (1) | `aRead` | W ∩ {x2=1} | [x2=1], mstar 1 |
| `fHb0` (new) | fNodeB0 (2) | x3-projection `bRead` | W ∩ {x3=0} | [x3=0], mstar 1 |
| `fHb1` (new) | fNodeB1 (2) | `bRead` | W ∩ {x3=1} | [x3=1], mstar 1 |
| `fHj` (new) | fNodeJ (3) | JOINT read `jointRead` | W ∩ {x2=0, x3=0} (1 pt) | [x2=0],[x3=0], mstar 2, **sides = 2** |

W = the window event {x0 = 0 ∧ x1 = 0}. The G1 wall (`mem_realizable` demanding
2-node `HistoryCoherent` transitions — the toys' BLOCKED-FALSE sorries) is avoided
STRUCTURALLY: every realized chain is one-node, so only the per-node coherence clauses
+ `Realizable` (vacuous at length 1) + `polTriv.IsCanonPres` fire — all proved
(`laws_fHb0/fHb1/fHj`, reusing S2's `laws_t1/laws_t2` and `canonRoot_toyHead`).
New nodes are root-species clones of `toyLeafA`/`toyLeafB` (species-law fields
re-supplied; within-branch read values center-distinguished, the S2 `sibNode2` genre).

## 2. Both P-2 rfl-degeneracies BROKEN (the OL-2 inhabitant spec, met)

* **(a) non-diagonal `branchCellOf`** — `forgeCA.branchCellOf := fRead`: at the SAME
  entrance, a-branch nodes key `aRead`, b-branch nodes `bRead`, the joint site
  `jointRead` (`branch_cell_joint` proved: all three factor through the joint read).
  Certified three ways: `forge_read_nondiagonal` (the sibling read maps differ at a
  point); `forge_states_crosscut` (the sibling STATE EVENTS overlap non-trivially AND
  differ — fibers of one shared map are equal-or-disjoint, so this configuration is
  diagonal-unrepresentable); **`forge_no_diagonal_read`** (the structural breaker: NO
  single-map read over ANY cell type presents both sibling states over the shared
  entrance — quantified over `(γ : Type) (Ψ : Box → γ)`).
* **(b) sides = 2 with the split legs DISCHARGED** — `forgeLedger.sides fHj = 2` with
  the genuine `SideSplit` (`forgeSplitGen`): 2-clause fresh system partitioned over
  disjoint side spans {x2}/{x3}, strictly monotone hull slopes 1 < 2, `hside_read`
  tying the roster nodes' RECORDED `line.slope` (toyHead: 1, fNodeB0: 2) to the hull —
  and `splitAt`/`hsplit_k`/`hsplit_exp` all proved (the toys' omega-absurd dodge gone).
  The (JC-multi) price is DECIDED TRUE: `forge_jcmulti` — #(site cell event)·p^{Σ side
  exponents} = #(entrance event), i.e. 1·2² = 4.

## 3. The pin-corrected (H7) faces, decided at the instance

* **PIN 1 (slope-keyed ε; pooled REFUTED).** `forge_kloc_slope_keyed` PROVES the
  K-LOC §1.1(ii)-shaped transport at the sides-2 site's roster: every branch read IS
  the slope-keyed normalization `slopeEps ν.line.slope` of the JOINT read, uniformly
  in the box point. `forge_kloc_pooled_refuted` is the COMPILED COUNTERMODEL to the
  slope-pooled strengthening (one ε₀ for all roster branches) at this very instance —
  at the window point (x2, x3) = (0, 1) the same joint cell would need ε₀-images
  `cA true` AND `cB false`. The compiled twin of N-H7-1's S5 pooled-u refutation:
  any `KLocSemantic` row must key ε per realized slope, never pool.
* **PIN 2 (gates inside the D-8 guard).** `forgeDatum : SiteDatum 2 2 2` (m = 2,
  thr = 1, ρ = 0, Σ-count 4, branch counts 2·2 — tied to the ACTUAL box cardinalities
  by `forge_stratum_count`/`forge_branch_counts`, not free pins); `forge_guard :
  InGuard forgeDatum` is the compiled guard fact (1 + 2·0 ≤ 2; the gate site sits at
  (N, τ, e_max) = (2, 1, 0), N − e_max = 2 ≥ 1 = τ). The decide-small semantic gate
  **`forge_klocCounts`** is the FIRST contentful `KLocCounts` instantiation in the
  corpus (H7PROBE P-3 recorded zero): Events := the site's four realized joint read
  words (Bool × Bool), joint/single counts := actual box cardinalities; the
  (SIB)-shaped identity #joint·#Σ^{m−1} = ∏#single holds EXACTLY at every word
  (1·4 = 2·2), decided by kernel evaluation over the 16-point box. Per pin 2 no count
  gate is stated below the guard (the out-of-guard extraction failure is
  numeric-tier — N-H7-1 P-E — displayed, not compiled).

## 4. Honesty display (what this instance is NOT)

* **Depth-1.** The multi-side site's roster nodes are realized as SIBLING chains of
  the same window entrance, but its own deeper CONTINUATIONS are not realized:
  that requires coherent 2-node histories — the HK23/HK25 transplant (those gates
  prove such histories exist over F4) — the named residual, out of this unit's scope.
  `NsHalts` holds at every site; `forgeTree` leaf verdicts are decoration (no
  `fiberAt`/`Realizes` claim is made or needed by the ledger).
* **Synthetic-but-lawful.** The node literals are lawful `Node` instances of the
  G1/S2 toy genre; the box-digit read tables are the forge's own keying, NOT derived
  from an engine run. Engine-tied semantics (`ITauSemantic`/`KLocSemantic` at
  `siteCellEvent` over the pinned canonical models) remain Tier-M obligation H7-W3a.
  `ITauShadow forgeDatum` (Tier-S interface fit, H7-D1b genre, needs an `AdaptedCell`
  carrier) was NOT attempted — per the blueprint's own warning the shadow row is
  never evidence; the decided faces above carry this unit's semantic content.
* **Nothing here discharges (H7).** The forge supplies the first non-vacuous compiled
  DECISION POINT for the (H7) statement shapes: the pin-corrected faces now have a
  compiled site where they are decided (all TRUE as pin-corrected; the pooled
  strengthening compiled-FALSE), replacing H7PROBE's "no compiled decision point of
  its own" state.

## 5. Build & census

`lake env lean LeanUrat/Scaffold/HDischarge/H7/H7Forge.lean` — green, exit 0 (green
at compile 4 after 3 repair rounds; findings worth recording: structure-update clones must
re-supply every proof field mentioning the updated field (`fNodeB1.hspecRecCenter`);
`FCell`-vs-`forgeCA.Cell` Eq-typing blocks `rw`/`simp` across the CellData projection —
the working pattern is `change` to the FCell-typed statement first, then rewrite).
In-file `#print axioms`: `forgeModel`, `forgeCA`, `forgeTree`, `forgeLedger`,
`forgeSplitJ`, all six certificates/faces — Lean core only, zero `sorryAx`. New
declarations only; imports reuse `MovesT.S2_sibFalse` (sorry-free layer) +
`Scaffold.HDischarge.H7.Rows`; no sorried declaration (toyModel/toyCA/toyLedgerA/…)
is consumed; `MovesT.SibJcRows`/`CellData` fences + parked items 22–29 untouched;
no existing statement modified.

## 6. Residuals (named)

1. **Depth-2 realization** — coherent 2-node continuation chains under the forge
   ledger (HK23/HK25 transplant; would upgrade the sides-2 site from ledger-data hull
   to realized continuations).
2. **`ITauShadow forgeDatum` / H7-D1b** — the Tier-S interface-fit gate at the forge
   integers (needs an `AdaptedCell` carrier; MED, prover-wave genre).
3. **H7-W3a** (engine-tied `ITauSemantic`/`KLocSemantic` at `siteCellEvent` over the
   pinned canonical models) — unchanged Tier-M obligation; the forge is its surrogate
   decision point, not its discharge.
