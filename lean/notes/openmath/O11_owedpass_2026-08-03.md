# O11 owed hostile pass (2026-08-03) — Codex fresh-context verification

**Campaign**: the OWED-PASS QUEUE of the ROOT r10 ledger entry
(`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`). **Target document**:
`lean/notes/openmath/O11_phaseB_verifybrief_rev3.md` (pasted in full into the prompt; Codex
sandbox cannot read the repo). **Charge**: hostile fresh-context proof verifier,
quote-and-classify CRITICAL vs GAP, falsifier-first, fix nothing, single-line verdict.
**Prompt/raw output**: `/tmp/openmath/O11_PROMPT.txt` / `/tmp/openmath/O11_OUT.jsonl`.
**Codex budget at launch**: 7-day window 52% used.

## VERDICT: CRITICAL

## Adjudication (campaign manager, from the report text only)

NOT BANKED. 1 CRITICAL + 2 GAP. The critical: Theorem O11 proves only the n>=2 restriction of the advertised for-every-n target row (evading cases n=0,1 untreated). Gaps: CUT-WD consumed-but-undemonstrated premise not folded into the 'EXACTLY four rows' accounting; the '65/65 end-to-end' RESUM-n3 rederivation claim unsupported (only the five displayed identities are proved). No repair performed. O-11's r3 counter does NOT advance toward its bar on this pass.

## Verifier report (verbatim, unedited)

I found one critical scope failure and two justification gaps. The displayed external dependencies are generally billed honestly; I found no additional silent dependency in the §5 sweep.

1. **CRITICAL — The theorem proved is narrower than the stated target.**

> “Mathematics … **for every splitting type σ of degree n and every prime p**”

but:

> “**Theorem O11 (rev 2). Fix n ≥ 2** and a prime p.”

No hypothesis \(n\ge2\) appears in the displayed normative Lean row or target statement. The proof therefore establishes only the \(n\ge2\) restriction of the advertised row. The evading cases \(n=0\) and \(n=1\) are neither treated nor reduced to trivial base cases. Nothing displayed shows that `SplittingType n` is empty there or that an ambient constraint supplies \(n\ge2\). This breaks the chain from Theorem O11 to the full displayed target.

2. **GAP — CUT-WD, needed to invoke Lemma 3.3, is assumed rather than supplied.**

> “**CUT-WD** … ← O-1thr Theorem 1 (= D-14, read locality …) + CU-3 (= F3′ coherent nonempty locus…)”

followed by the note’s own correct diagnosis:

> “for CUT-WD the map is a **correspondence CLAIM, not a demonstrated implication** — read locality (D-14) plus a coherent nonempty read locus (CU-3) are NOT displayed to imply the exhaustive-exclusive read ownership or the shallow-decomposition bijection…”

and nevertheless:

> “Given (K1-a)–(K1-e) … the type-σ corpus decomposes along shallow shapes — **Lemma 3.3 applies**.”

Falsifier-first evasion: a coherent nonempty read locus can still have overlapping ownership, an unowned read, or two different shallow decompositions of one tree. D-14 and CU-3, as described, exclude none of these. Any such case destroys the required bijection and invalidates the regrouping used in §3.5.

The note honestly exposes this gap, but its simultaneous accounting claim is too strong:

> “sweeping every §5 status/residue cell … finds **EXACTLY** the four rows above beyond tier 1 + D-a/D-b/D-c.”

Pending adjudication does not prove CUT-WD. If the conclusion is assumed so downstream verification can continue, it is a provisional consumed premise outside that asserted exact union. Thus the gap is disclosed, but not incorporated consistently into the claimed complete accounting.

Assuming CUT-WD, I found no further internal defect in the Lemma 3.3 regrouping.

3. **GAP — The V2 interpolation result does not internally rederive the larger RESUM-n3 record.**

> “This **re-derives the RESUM-n3 65/65 anchor symbolically end-to-end**.”

The displayed argument proves the five rational-function identities
`alpha_full(3) == monic_cubic_pred`: the degree calculations are valid, the stated bounds \(12,12,12,11,11<44\) are sufficient, and \(q=2,\ldots,45\) avoids the displayed oracle poles. But nothing displayed identifies the 65 RESUM-n3 checks with those five identities or proves that every one of the 65 checks follows from them. The broader “65/65 … end-to-end” claim is therefore unsupported. This is evidentiary and not used by the assembly proof.

Falsifier/ledger sweep:

- M04 Theorem 1/Corollary 1.1: the obvious evasion—level counts not representing the claimed fibers—is explicitly conditional on K4-CYL.
- M04 Lemmas 3.1–3.2: escape, invertibility, pole avoidance, and regular evaluation are explicitly supplied by K2/D-6.
- M04 Lemma 3.3: CUT-WD is the unresolved counterexample channel above.
- M04 Theorem 2 is orientation-only and is not illicitly used in the per-\(\sigma\) walk.
- D-a, D-b, D-c, D-11, H-1 through H-4, and the unramified-pool route are explicitly conditional; their external provenance is not silently promoted to an internal proof.
- The §7 execution, byte-identity, transcription, and log-count claims are explicitly marked artifact-external. The displayed V2 degree and root-count argument is internally sound given those artifact premises.
- The \(R_{\sigma}(p)\to\mathrm{ENNReal.ofReal}\) step is covered conditionally by the billed `r_bdd` pack law.
- Drainage, \((E\!-\!N)\), OB1/D-16, and the density tie are not internally consumed by the displayed §3.5 walk.

VERDICT: CRITICAL
