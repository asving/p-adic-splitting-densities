# VC36b — Codex narrow confirming pass on REVISION 35 (pinned db298d3): NOT-CONFIRMED, 0C/2G

Charge: /tmp/openmath/VC36b_charge.txt (181,045 bytes; 11 remedy-site slices + all-70-row pinned census transcripts + full process disclosures). RESULT: the four VC36 remedies CONFIRMED cured; census 70/70 and sorry-census reproduced by the auditor; the two gaps are qualifier-propagation defects at acceptance-citation sites NOT among the four findings — (g1) the second ROOT §0 bracket cited the acceptance bare; (g2) the DITERSUP J3b second bracket omitted 'orders ≥ 3' and mislabeled gr(w₂) as the third residual.

## Orchestrator adjudication + errata (2026-08-03, orchestrator-direct per the qualifier-defect-class precedent)

- g1: r36b ERRATUM bracket appended after the flawed ROOT §0 bracket (full qualifier package: class scope + all three residuals; census note per the d79f295 post-census-edit precedent).
- g2: r36b ERRATUM bracket appended after the DITERSUP J3b bracket (the three-item residual list of record spelled out; mislabel corrected; everything else stands).
- Flawed brackets NOT edited in place (VC36b quoted them; the audit trail stays true). VC36c = tiny confirming pass on the two erratum brackets only.

## Codex output (verbatim)

I’ll perform the narrow quote-and-classify pass only. I’ll read the pinned working tree without editing, verify the charged brackets and record preservation from git, re-grep a representative census set, and independently reproduce the sorry breakdown.
Sandboxed execution is unavailable because bubblewrap is absent, exactly as the standing disclosure anticipates. I’m therefore treating the pinned verbatim slices and raw transcripts as the audit input; this is a NOTE, not a finding. The remedy sites themselves cure F1–F4, but I found one qualifier-propagation issue in the charged acceptance sweep and am checking whether any second site has the same defect.
The four VC36 remedies themselves check out: F1/F2’s acceptance-upgrade bracket gives the complete arc, counter 2/2, supplement breakdown (which sums to 18,396), F2c actuals, and corrected scope; F3 supplies all three residuals; F4 supplies depth-1 honesty, synthetic-but-lawful nodes, and “NOTHING discharges (H7).” The broader acceptance-sweep claim is not fully confirmed.

1. GAP

   QUOTE: “**[r36 (REVISION 35, §41): VC36 RAN and returned NOT-CONFIRMED 1C/3G … F1 (critical) + F2 (gap) = FOLD-LAG on the then-in-flight ITER-LAW arc, cured by the acceptance (33373cb) … The acceptance citation STAYS at the REVISION-33 text (VC35+VC35b)**”

   DIAGNOSIS: This charged second ROOT §0 bracket cites the ITER-LAW acceptance as the F1/F2 cure but supplies neither the accepted harness-class scope nor any of the three residuals. That contradicts §41.(ii)’s categorical claim that “NO site cites the acceptance without the class scope and all three residuals.” Assuming the qualified acceptance for downstream verification, this is a qualifier-propagation gap rather than a failure of the F1/F2 remedy itself.

2. GAP

   QUOTE: “J3b’s CONDITIONAL-ON-ITER-LAW leg MAY now cite **ITER-LAW ACCEPTED-on-class** … Clauses (a) and (c) STAND: (a) the instance must be standard-lift … and (c) the (ITER-LAW-LIFT) residual …; SCOPE AS ACCEPTED: the harness tower class …; the third residual (the gr(w₂) semantics wrapper rides the accepted GRB retarget consumer-side) is exactly this note’s §S3 ‘missing tie’ — still MISSING.”

   DIAGNOSIS: The charged DITERSUP §S4/J3b second bracket gives the corrected class scope, the `(ITER-LAW-LIFT)` residual, and the gr(w₂) tie residual, but omits “orders ≥ 3 untouched.” It even labels gr(w₂) the “third residual,” although the three-item list of record is lift, gr(w₂), and orders ≥3. Thus this acceptance citation does not carry all three residuals, contradicting the acceptance-sweep claim. Assuming the omitted order restriction, downstream checking continues.

Census spot-checks from the pinned raw transcripts reproduce more than the required five rows: `(R1)–(R4)` 125, `TransDeep` + `TRANS-DEEP` 36 + 21 = 57, `ITER-LAW` 129, `DITERSUP` 29, `VC36` 56, `ITER-LAW-LIFT` 18, and `VC36b` 15. The sorry breakdown also reproduces: 10 + 7 + 6 + 5 + 4 + 2 = 34 in `LeanUrat/`, plus one scratch probe = 35 repo-wide; B2D 0 and OM tree 0.

NOTE: sandbox execution was unavailable because bubblewrap is absent; the pinned transcripts were therefore the audit input, not a finding. The orchestrator’s F1/F2 fold-lag adjudication was not re-adjudicated. Campaign labels use the 2026-08-08 window while the authoritative git chain records wallclock 2026-08-03. The frozen-history trim and disclosed 7598491 API-529 checkpoint/recovery process were treated as process facts, not findings.

VC36B FINDINGS: 0 critical, 2 gaps
VERDICT: NOT-CONFIRMED
