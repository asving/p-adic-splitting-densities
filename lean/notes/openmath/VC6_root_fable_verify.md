# VC6 — Phase C end-to-end pass 6 (Fable, fresh context, model-diverse)

**Unit:** VC6 · 2026-08-03 window · verifier = Claude Fable 5, fresh context, no prior
VC reports read, no leaf records read. **Input: `docs/ROOT_ASSEMBLY_2026-08-02.md` at
REVISION 5, in full (1761 lines), and nothing else.** Charge = the document's own §11
"What VC6 should check first" list + the standard end-to-end charge: (1) the
composition proves (ROOT-C) — (H1)–(H6), NINE §3.1 [M] rows/groups, per-clause
attribution as displayed — quantifier-for-quantifier from the declared leaf statements
+ ledger rows; (2) the (H6) group's routes (Step 11 §9.5 / Step 18 D-a/K4-CYL / Step
18 K3-a); (3) a document-wide re-audit of EVERY remaining §3.3 [T] row against the
ledger's own classification rule; (4) acyclicity; (5) the leaf #8/#9 scope interlocks.
Findings numbered, BLOCKER vs ADVISORY. Nothing is fixed here.

**Verdict: COMPOSITION-GAPS** — 3 blockers + 6 advisories. All three blockers are the
VC5 genre walked to completion: the [T]→[M] genre check ("a [T] row must never carry a
consumed open proposition") was applied at REVISION 5 to D-a's terminal content and
K3-a's item (vii), but NOT to the sibling declared row D-b, not to the O-2a open-lemma
residue's GD-1 face, and not to the GD-6 instances Step 10's own conditionality line
displays. The composition's mathematics is again NOT the problem: acyclicity,
the displayed nine-row bijection, both unconditional clauses, the Σ_τ R_τ = 1
derivation, (SL≥2), and the leaf-#8 interlock all PASS as displayed.

---

## 1. What PASSED (checked, not presumed)

- **Acyclicity (charge 4): PASS, edge-by-edge over all 20 steps.** Every Consumes
  line points strictly earlier: Step 1 (literature only; the Appendix-A ns-null
  re-point stands, no T-6 edge); Steps 2–5 symbolic/regularity (B-1..B-7 grades
  displayed at Step 2); Steps 6–13 the DELTA-2 recut (C.1.5 ← T-2, D-5 internal to
  O-2a, (O6) ← O3design, Step 9's re-keyed coherence ← governance record); Step 14 ←
  Step 8; Step 15 ← literature pins; Step 16 ← Steps 2/8/14 + M14 + §3.1 rows; Step
  17 ← Steps 5/6/12 + T-3/T-6, with "NOTHING from Steps 18/18b" displayed; Step 18 ←
  Steps 1, 5, 6, 10–12, 14, 15, 16, 17 only; Step 18b ← Steps 14/17/18; Step 19 ←
  1–18b. The 17 ≺ 18 ≺ 18b stratification holds; REVISION 5's edits introduce no new
  forward edge (the (H6) texts reference Steps 11/18 from Step 19 and ledger rows
  only).
- **The NINE-row bijection (charge 1, syntactic): EXACT.** §3.1 has exactly nine
  rows/groups: (GR-B), (FRESH), (R1)–(R4), (ADM), (UB-X), (PACK), the (H6) group,
  D-12r, (E-N). Mapping: H1 = (GR-B); H2 = (FRESH); H3 = D-12r + (E-N) + (ADM);
  H4a = (R1)–(R4); H4b = (UB-X); H5 = (PACK); H6 = the terminal-seam group. Each row
  in exactly one hypothesis; no hypothesis names a non-§3.1 row. Counts reconcile
  (7 at R3 + PACK at R4 + H6 group at R5 = 9).
- **Per-clause attribution (charge 1, internal consistency): PASS at every displayed
  site.** (0) ← Step 1 alone, unconditional. (SQ) ← Step 1's BRACKET/SEM-DRAIN +
  Step 13's semantic layer (Lemma A/C/D + Theorem E), both displayed unconditional at
  every prime under §1's SEMANTIC definitions of decided_τ(k)/undec(k) — no §3.1 row
  consumed; the classifier-transfer layer ((AGR), a §3.2 inherited-[V] row, not [M])
  rides the evaluation squeeze inside clause (R), consistently stated at §1, Step 13's
  Delivers, Step 19, and the D-12r/(FRESH) rows. (R) ← (H1)–(H3) + (H4a) + (H5) +
  (K3-δ) + (H6): traced row-by-row ((GR-B) via Steps 10/14/18/18b; (FRESH) via Steps
  10/14 → 18; D-12r via TB-CAP at Steps 2/3/11/12/19; (E-N) via CU-4 → the transfer
  layer inside (R); (ADM) via Step 14 → K3-b and (S1); (H4a) via K3-c's base-changed
  β-legs; (K3-δ) displayed as ⊆ (H4b) at both touch points; (H5) via Steps 17 →
  18(K2)/18b; (H6) per §2 below). (UB) ← (H1)–(H3) + (H4a) + (H4b) + (H5) + (H6),
  matching Step 16's honest-scope display leg-for-leg; only (H4b)-minus-(K3-δ) is
  (UB)-exclusive. Step 19's closer names exactly (H1)–(H6).
- **Σ_τ R_τ = 1 (Step 19): PASS.** Σ_τ α = 1 at every p + interpolation (Step 1's
  declared perimeter) + α = R_τ(p) at every p (Steps 18 + 18b, inside (R)'s displayed
  conditionality) ⇒ agreement at infinitely many arguments ⇒ identity in ℚ(t). No
  clause-(UB) content consumed (primes suffice); no new hypothesis.
- **(SL≥2) (Step 5): PASS as displayed** — strict inter-block descent (L4) + block-1
  emptiness (K_1 = (0), Φ_1 = 1) + the consumer audit (Steps 5/17/18 consume only
  through O-12's e ≥ 2-scoped theorems); §1's exponent bound e(e+1)/2 − 1 ≥ 2
  consistent.
- **Leaf #8 (O-9) interlock (charge 5): PASS.** Step 14, Step 18 (K3-b), Step 18b
  ((S1), the VC4-5 repair), Step 16, §1 (H3), and the §3.1 (ADM) row all consume the
  census at exactly the adjudicated scope — CEN-W/CEN-J conditional on (GR-B) +
  (FRESH) + (ADM)-FULL; the single operative (ADM) definition is the r4
  FULL-attainment form everywhere operative; "automatic at r = 0 ONLY" is consistent
  at §1/Step 14/§3.1; every "orders ≤ 1" automaticity phrasing found (Step 14 rev-2
  text; §8/§10 appendix tables) is inside explicitly superseded/quarantined history.
  Nothing upgrades the leaf in transit.
- **The (H6) promotion mechanics: correctly executed at its displayed sites.** §1's
  (H6) text, the §3.1 row group, Step 11's status, Step 18's retraction-in-place of
  the former fence routing, Step 19's consumption line, the §3.3 GD-4/GD-6 and
  widened-charge rows (keeping (v)/(vi) resp. the bookkeeping remainder), and the
  (PACK)-vs-(vii) distinctness note all agree; (H6) enters (R)/(UB) only; the
  escalation clause (fence kernels become ADDITIONAL open mathematics if a pack needs
  fenced territory) survives inside the row. Row-for-row (UB) inheritance is coherent:
  all four members are δ-transportable content ((T-*) ride the transported CU seam;
  (vii)'s tables are p- and δ-free), none (UB)-exempt.

---

## 2. Findings

### F-1 (BLOCKER) — declared row D-b ((I-τ)/K-LOC) is a consumed open interface still sitting in [T]: the LAST unpromoted member of O-11 r2's D-a/D-b/D-c triple

O-11 r2 declared THREE interface rows with ledger homes (Step 18): D-a =
(M6b)(iii)+(ii−) → the GD-4/GD-6 row; **D-b = (I-τ)/K-LOC → the §3.3 interface row**;
D-c = (R1)–(R4) → §3.1. The document has since adjudicated D-c hypothesis-grade
((H4a) + (K3-δ), findings V2-2/VC4-1) and D-a's terminal content hypothesis-grade
((H6), finding VC5-2) — but D-b still sits in §3.3 [T], and it has the same shape:

- **Open:** K-LOC "= M07 OL-3" — named by the document itself as an OPEN LEMMA
  ("continuation reads of branch j = OM reads of f_j up to cell-constant
  normalization"); no proof on file is claimed anywhere for it or for (I-τ).
- **Consumed on clause (R)'s mandatory path, triple-displayed:** Step 15
  ("Application to the engine's realized site strata is conditional on the two
  displayed interfaces K-LOC (= M07 OL-3) and (I-τ)"); Step 18's K1 line ("K1 ← O-10
  (guarded) + **(I-τ)→GD** + CU leaf labels"); the §3.3 row's own owner column
  ("O-10 consumers (Steps 15, 18)").
- **No supplying row:** "both priced into the GD families" (Step 15) has no displayed
  [M] landing — (GR-B)'s row is scoped "GD-2/GD-3's one open input" and carries no
  read-locality/normalization content; no GD-family [T] row lists K-LOC/(I-τ); no
  (ROOT-C) hypothesis names them.

Under the ledger's own rule ([T] = content pinned to printed sources or existing
proofs) and the VC5 genre check verbatim, D-b cannot sit in [T]: without it, Step
18's K1 does not receive O-10's product law at the engine's strata, so clause (R)'s
displayed hypothesis set is incomplete. Repair shape (not executed here): promote to
§3.1 (its own row or an (H6)-style interface group), or display the supplying row +
pin that makes it transcription.

### F-2 (BLOCKER) — GD-1's "(KP)/(KPirr) beyond order 1" is an exactly-stated open lemma of a consumed leaf, classified [T] while the same-shape GD-2/GD-3 residue is (H1)

The document's own displays establish, jointly:

- Step 7: O-2a's residue is "the SIX exactly-stated open lemmas OL-2a-1..6 = the
  GD-1..GD-6 families", with "**OL-2a-1 = GD-1 (representative admissibility — §3.3
  (KP)/(KPirr) row)**". An exactly-stated open lemma has no proof on file — [M]-shaped
  by the ledger's own definitions.
- Step 8 (VERIFIED leaf #4) fences it OUT of the verified scope and OUT of (GR-B):
  "(GR-B) at orders ≥ 2 is a DECLARED open input …; the (KP)/(KPirr) clauses beyond
  order 1 and the properness proviso at e·g = 1 refinement reads **are owned by
  GD-1**." The §3.1 (GR-B) row is likewise scoped to "GD-2/GD-3's one open input".
- O-2a is consumed at Steps 10/11/12 (mandatory path), including its
  admissible-representative-family clause (Θ's typing; Theorem 2(B)/(D) feeding
  CU-2/CU-2t); the deep-wild orders ≥ 2 are the theorem's content; Step 12's five
  flags are expressly "GD-1/GD-7/GD-8 duties" ("representative lookup").
- The (KP)/(KPirr) §3.3 row displays a pin ONLY for the properness leg (FGMN Lemma
  5.3); the beyond-order-1 admissibility clauses carry no pin, no proof-on-file
  claim, and — unlike LC-COH or the O-14a/b/c row — no "consumed by nothing
  mandatory" scoping line.

So the three dictionary families with the proved-≤1/open-≥2 shape are classified
asymmetrically: GD-2/GD-3's ≥2 face = (H1), hypothesis-grade; GD-1's ≥2 face = [T].
The (GR-B) row's own precedent closes the escape: even when "every target statement
is pinned to a printed theorem", the unwritten composed layer is "honestly [M] until
written and passed". Repair shape: promote GD-1-beyond-1 to §3.1 (alongside or into
(H1)'s group), or display the consumption-scoping under which no mandatory step
consumes O-2a's admissibility clause at orders ≥ 2 (none is displayed now, and Steps
10–12's consumption suggests the opposite).

### F-3 (BLOCKER) — Step 10's consumed "GD-6 instances" have no supplying row: (GR-B)'s row excludes GD-6, and GD-6's [T] home holds routes-on-record — the same evidentiary status as the promoted (T-DEC)/(T-VERD)

Step 10 (CU-1, VERIFIED leaf #5) displays: "Conditionality carried: the step consumes
GD-2/GD-3(min)/**GD-6 instances** at orders ≥ 2 **through (GR-B)**, plus the declared
pins…". But:

- The §3.1 (GR-B) row is scoped "the anchored-march residue-structure package
  (**GD-2/GD-3's one open input**)" — it carries no GD-6 (run-decomposition
  conformance) content, so (H1) as displayed does not supply what Step 10 routes
  through it. (Note GD-6 = OL-2a-6, run-decomposition CONFORMANCE, is distinct from
  the proved OL-6 binding in Step 10's Consumes list — the document itself keeps
  these separate at Step 7's mapping.)
- GD-6's ledger home is the §3.3 GD-4/GD-6 row, whose post-promotion remainder is
  held on "proof-by-construction routes on record" — routes, not proofs: exactly the
  evidentiary status under which (T-DEC)/(T-VERD) were PROMOTED at VC5-2 (the (H6)
  row itself says "(T-DEC)/(T-VERD) GD-4 (proof-by-construction routes on record)").
- After the promotion, the GD-4/GD-6 row's consumption display is ambiguous: "its
  Step-18 D-a entry line now points at the (H6) row for the promoted content" leaves
  undisplayed whether the KEPT content (W1–W5, (c0), AUX band, run-decomposition
  conformance) is still consumed at Step 18's D-a — and Step 10's GD-6-instance
  consumption line stands regardless.

Either (H1)'s row understates its own content (then re-scope it to include the
consumed GD-6 instances, and the (H1) hypothesis grows), or a consumed open
proposition sits in [T] (then it joins (H6)); as displayed, clause (R)'s
"(H1)–(H3) + (H4a) + (H5) + (K3-δ) + (H6)" attribution is not supplied
quantifier-for-quantifier at the Step-10 interface. This finding interlocks with F-2
(both are O-2a OL-residue faces) and with A-4 below (CU-2t's "GD residue" wording).

### A-4 (ADVISORY) — leaf #9 interlock: Step 11 renders CU-2t's conditionality twice, unreconciled

The R3-era sentence: "CU-2t is PROVED at R3 … conditional on **P(O-2a r5) + TB-CAP's
D-12r perimeter + the GD residue including the THREE NEW GD-4 terminal rows**". The
adjudication sentence (same step, REVISION 5 sync, echoed at §11): "VERIFIED at scope
(leaf #9) … conditional per §9.5 (**the three terminal rows + the pinned
literature**)", and §11 sharpens to "exactly the three terminal rows". These are
reconcilable (inherited-vs-new: P(O-2a r5) is a [V] debt, D-12r rides (H3), "the GD
residue" = the inherited O-2a/GD conditionality with its own homes) — but the text
never says so, the earlier sentence is not marked superseded, and "including" invites
a ⊇ reading under which un-enumerated GD residue (the F-3 remainder) is consumed via
CU-2t without a price. One reconciling sentence at Step 11 (new-vs-inherited split,
with the inherited items' homes named) closes it. Modulo this display, the leaf-#9
interlock is sound: nothing upgrades CU-2t in transit, and the (H6) promotion is
correctly what the VERIFIED-at-scope grade forces the composition to carry.

### A-5 (ADVISORY) — (H6) route display omits the Step-12 → Step-18-K1 "CU leaf labels" leg

The displayed (H6) routes are Step 11 (§9.5) → Step 18 D-a/K4-CYL, plus K3-a for item
(vii). But Step 10/11's terminal seam also feeds O5triple ("terminals routed to
O5triple's counting", CU-7), whose leaf labels Step 18's K1 consumes ("K1 ← … + CU
leaf labels") and Step 17's M3 consumes (Thm E) — and the document itself uses
exactly this K1/CU-leaf-labels route for (FRESH)'s clause-(R) inheritance (the §3.1
(FRESH) row). No clause attribution changes ((R)/(UB) already carry (H6)); the route
display is incomplete relative to the document's own treatment of the same interface.

### A-6 (ADVISORY) — four [T] sites display neither proof-on-file status nor consumption scope, so their class is unadjudicable under the ledger's rule

Each needs the item-(vii)-style adjudication line (proof on file? consumed where? if
open-and-consumed → [M]; if unconsumed → say so, LC-COH-style):
(i) the S-1 row's "**degree-g > 1 KEY1 generalization** (same method)" — owed proof
content (a generalization, not an audit of an existing proof) inside an audit row;
(ii) widened-charge item (v), the pack's carried laws **rsh_interp/r_bdd/wsh_ok** —
consumed by Step 18's restated D-11 entry, with "acceptance of the widened charge
still owed" and no proof-on-file display (if unproved at general n, (PACK)-adjacent);
(iii) the **GD-8 order ≥ 2** row — "expected verbatim over GD-2's frame tie" is the
(R1)–(R4) shape that was kept [M] for honesty; the row displays no consumption scope
(the quarantined CU-2t-deep corollary is displayed non-mandatory, but Step 12's GD-8
duty is not scoped);
(iv) **(FR≡)**'s "refutable order-2 content" (CU-1's carried pin) — supported by a
gate finding (a flag, not a proof); why convention-coherence with refutable content
is [T] rather than an open claim should be displayed.

### A-7 (ADVISORY) — GD-7 flags: five consumed statements, pins at section granularity, expressly NOT covered by the executed LITUNIT

O5triple (consumed at Steps 17/18) carries the five flags
(GMN-COH)/(R-0)/(R-1)/(R-2)/(R-3) as "GD-1/GD-7/GD-8 duties"; the row's discharge
story ("one GMN §2.1/§3/§4.1 reading session covers them") is a claimed pin at
section range, unexecuted and outside the LITUNIT's MATCH-verified set. [T] holds
only under a generous reading of "pinned"; given this project's two-false-axioms
history, execute the reading session (it is one session) or display per-flag target
loci before the clean-pass bar.

### A-8 (ADVISORY) — stale "seven leaves VERIFIED" inside the (ROOT-C) display

§1, "Proof status of the implication itself: it is the §2 composition — **seven
leaves VERIFIED**, the rest PROVED-UNVERIFIED…" — a REVISION 5 sync miss (the
headline, Step rosters, §3.2 and §11 all say NINE: O-13, O-12, Dfloor, GD23, CU-1,
O-10, O4T, O-9, CU-2t). Not load-bearing (§3.2 governs), but it sits inside the
theorem display itself.

### A-9 (ADVISORY, standing — third consecutive recording) — the T-U §U.SQ.6 row-by-row correspondence remains asserted, not displayed

Step 19 still consumes THEOREM U against the ASSERTED coincidence of its §U.SQ.6
kernel ledger with the Steps 1–18b roster + evictions/annex; the §3.3 audit row
(A8 → VC5-3 → here) is openly recorded, so not a hidden edge, and it correctly gates
acceptance through §3.3's emptying. But this is now the third consecutive pass
recording it on the CLOSING step: a fresh verifier structurally cannot certify "no
§U.SQ.6 item falls outside the steps" from this document, so the correspondence table
should be EXECUTED before VC7 — it is cheap (a per-item pointer table) and it is the
one remaining self-inflicted opacity in an otherwise checkable walk.

---

## 3. Charge-item verdicts, compressed

| charge | verdict |
|---|---|
| (1) (ROOT-C) from leaves + rows, 9-row bijection, per-clause attribution | Displayed bijection and attribution INTERNALLY EXACT and consistent at every site; Σ/interpolation, (SL≥2), unconditional (0)/(SQ) all pass. But the completeness claim "every consumed OPEN proposition is now a §3.1 row" FAILS at F-1/F-2/F-3 — the hypothesis list is not yet complete under the ledger's own rule |
| (2) (H6) routes | Internally consistent at all displayed sites (§1/§3.1/Steps 11/18/19/§3.3); (UB) inheritance coherent row-for-row; one route under-display (A-5). Faithfulness of the D-a/K4-CYL routing to O-11 r2's declaration is outside this document's four corners — it rides the owed O-11 pass 3 |
| (3) [T] re-audit, every row | 24 rows walked. Clean: D-5, (O6), sloped/sheared pin, (S1)/(S2) seams, OL-O12-1, OL-O12-2 (n=3 residue), BASE-1(f), LC-COH, (REP-IRR), W3 note, LIT-1/LIT-3/LIT-8, widened-charge item (vi), O-14a/b/c, item-27/28. Violations: F-1 ((I-τ)/K-LOC), F-2 ((KP)/(KPirr) ≥ 2), F-3 (GD-4/GD-6 remainder incl. Step-10's GD-6 instances). Unadjudicable-as-displayed: A-6's four sites; A-7 (GD-7 flags). Standing: A-9 (T-U) |
| (4) acyclicity | PASS, edge-by-edge; no new forward edge from the REVISION 5 edits |
| (5) leaf #8/#9 interlocks | #8 (O-9): PASS exactly at the adjudicated scope. #9 (CU-2t): sound modulo A-4's unreconciled double rendering of the §9.5 conditionality |

**On the consecutive-clean counter:** this pass is COMPOSITION-GAPS, so the ≥ 2
consecutive-clean counter stays at ZERO. The three blockers are one repair genre
(classification promotions/scopings, no new mathematics beyond what the rows already
name), the same shape VC5's repairs took. After they land, the residual known opacity
is A-9's owed table — recommend executing it in the same fold so VC7 runs on a
document with no asserted-not-displayed edge on the closing step.

— VC6 complete. Fix nothing was the charge; nothing was fixed.
