<!-- GRB pass 4 (clean-bar attempt 1) — Codex hostile sweep on GRB_ORDER2_2026-08-05.md REVISION 4.
Run: 2026-08-06 campaign date; thread 019fc06f-8349-7640-9098-0919cdd58946; ~4.5 min; prompt /tmp/openmath/GRB_pass4_prompt.txt (73,353 bytes = charge + full leaf).
Adjudication: GAPS-ONLY -> clean counter stays 0/2. -->

The REV-4 repairs are present and mostly sufficient. Fresh sweep found three remaining gaps, no critical break.

1. GAP — one stale order-2 scope overclaim remains.

> “the order-2 law is computed (ratio geometric, step \(z_1^{\gamma_2}\))”

This live sentence in S8.2 escaped R4.3. Only one specified depth-2 setup was algebraically examined and sampled over 500 developments. It should not describe “the order-2 law” generally. This is the same proof-strength issue R4.3 repaired elsewhere.

A related live sentence in S8.3 repeats the ambiguity:

> “now with the dictionary’s exact shape (S3.2’s law)”

S3.2 establishes that shape only in the specified depth-2 setup; the general shape remains part of open TR-3′-GEN.

2. GAP — H-CHAIN omits the first internal chain condition.

> “the chain conditions \(\Phi_{j+1}\not\sim_{\mu_{j+1}}\Phi_j\) at the INTERNAL steps \(1\le j\le i-1\)”

For FGMN depth \(r=i+1\), Definition 3.1 requires
\(\phi_{k+1}\not\sim_{\mu_k}\phi_k\) for every \(1\le k<r\), which translates to

\[
\Phi_{j+1}\not\sim_{w_{j+1}/E_{j+1}}\Phi_j,\qquad 0\le j\le i-1.
\]

The displayed range must begin at \(j=0\). At state order two (\(i=1,r=2\)), the leaf’s range is empty even though the condition \(\phi_2\not\sim_{\mu_1}\phi_1\) is required. Because the preceding phrase assumes a chain “in FGMN’s Def 3.1 sense,” I can assume the missing condition and continue; hence GAP rather than CRITICAL. The primary definition confirms the required range and valuation level in [FGMN, §3.1](https://upcommons.upc.edu/bitstream/handle/2117/28203/ResidualIdealsArxiv.pdf).

3. GAP — S3.1 incorrectly says the inner residual operators agree at depth two.

> “the inner level-\((r-1)\) residuals agree at \(r-1\le1\) (both are the classical untwisted digit residues…)”

In S3.2’s own monomial computation at \(r=2\),

\[
R^{FGMN}_{1,\alpha_j}(a)(z_1)=\bar c\,z_1^k,
\qquad
R^{GMN}_1(a)(z_1)=\bar c,
\]

with GMN’s missing \(z_1^k\) supplied externally by \(z_1^{t_1(s_j)}\), since \(t_1(s_j)=k\). Thus the inner operators do not literally agree; the assembled pre-\(\epsilon_1\) coefficient expressions agree. GMN’s placement of the \(z_{r-1}^{t_{r-1}(i)}\) factor is explicit in Definitions 2.19–2.20 of the [primary GMN paper](https://arxiv.org/pdf/0807.2620). The subsequent ratio calculation remains correct, so this is not critical.

Independent checks otherwise pass:

- Sign/rescaling: \(\lambda^{GMN}=-h_i/e_i=-E_i\lambda_r^{FGMN}\).
- RESCALE follows directly from equality of filtration cuts under multiplication by \(E>0\).
- The index dictionary \(r=i+1\), \(\phi_k\leftrightarrow\Phi_{k-1}\), and \(F_r\leftrightarrow K_i\) is consistent.
- CARRY-1 and \(x^{e_i}=\bar y\,p^{h_i}\) follow from the integral Bézout equation.
- D-REAL has rank \(e_i\); full-stage rank is \(e_iE_i\), so its corrected full coverage iff \(E_i=1\) is sound.
- The weight-read carry exponent has the stated positive sign.
- Total side-read multiplicativity is printed without properness; the primary paper states \(R_r(gh)=R_r(g)R_r(h)\) for all \(g,h\) ([FGMN, Corollary 4.12(3) in the accessible version](https://upcommons.upc.edu/bitstream/handle/2117/28203/ResidualIdealsArxiv.pdf)).
- The REV-4 carrier wording now claims only availability of \(x_r,p_r^{\pm1},y_r\), and the G2 evidence is consistently described as support except for the stale S8.2/S8.3 language above.
- No live sentence routes project-facing ord consumers through proved TR-3-ORD.

VERDICT: GAPS-ONLY
