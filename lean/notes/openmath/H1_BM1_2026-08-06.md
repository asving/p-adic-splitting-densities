# H1_BM1 — Theorem KP-STEP: the key-polynomial block at OM state orders ≥ 2 (the door-1 core of (H1)(b), serving (H1)(d))

**Unit:** HDISCHARGE_H1 §2.2 member (b) door 1, wave-2 unit B-M1
(`lean/blueprints/HDISCHARGE_H1.md` REV 2). **Charge:** state and prove Theorem
KP-STEP — the standard key lift's initial-form block (KPa)+(KPirr)+(KPb) at
(KP_i), proved IN gr(w_{i+1}), from the proved order-≤ 1 layer (GD23 Theorem
KEY-g = the j = 0 instance) transported through the FGMN-retargeted
(GR-B-STR)_{i+1} package; dispose OP-B1 (the degree block) as PROVED or as an
explicit open lemma. **Sources of truth:** (GR-B-STR) = the A-M2 leaf
`GRB_ORDER2_2026-08-05.md` REVISION 4 (FGMN-retargeted clause-by-clause; the
CORRECTED conventions: (TR-1-corr) λ^GMN = −h_i/e_i = −E_i·λ_r^FGMN, §R2.1;
D-REAL scope E_i = 1, §R2.2; ord-consumers on OPEN TR-3-ORD-X, §R2.3); the
order-1 layer = `GD23_phaseB_verifybrief_rev4.md` §3 (GAUSS-g/VAL-g/KEY-g/RES-1,
verified leaf #4); FGMN pins re-read THIS SESSION from `/tmp/litunit/fgmn.txt`
at the stated lines (J. Fernández, J. Guàrdia, J. Montes, E. Nart, *Residual
ideals of MacLane valuations*, J. Algebra 427 (2015) 30–75 = arXiv:1305.0775);
GMN Defs 2.19–2.21 re-read from `/tmp/litunit/gmn.txt:1823–1906`. **B-M2 (the
OL-1 family clause, §2.2 s7) is NOT this unit** — wave 3, blocked on this note;
its statement and inputs are displayed at §S7. **REVISION 2 (pass-1 fold).**
Pass 1 (`BM1_pass1_2026-08-06.md`) returned CRITICAL: the rev-0 theorem
quantified over (K1)+(K2) alone, and that class does NOT support the
minimality block — high-valuation high-degree padding preserves every
initial-form datum while breaking minimality (findings 1–3), and the gate
language over-claimed (finding 4). ALL FOLDED: the theorem is RESTATED at the
degree-conformant class (K0)+(K1)+(K2) (§S1.3), the (SL-dev) companion gains
the (SL-top) clause, the e·g = 1 corner is re-run at the class (§S5), the
gate is re-scoped (supports, never proves — §S0), and the CONSUMER ALIGNMENT
is displayed (§S8 + the §REVISION-2 record): every consumer consumes only the
restricted class, by specification. **REVISION 3 (pass-2 fold):** pass 2
(`BM1_pass2_2026-08-06.md`) returned GAPS-ONLY (no critical); both gaps
folded — the parent-key monicity invariant DISPLAYED as Lemma PAR-MON (§S3.2;
corpus carrier `Stage.hmonic`, source-confirmed this session) with the four
wiring-claim sites softened to "shape + parent invariant", and the Cor 4.12
proof quote completed with its printed "Denote α := …" binding (§S3.3; the
statement's inert "for any α" quantifier displayed). Counter 0/2; hostile
pass 3 runs on THIS text.

## S0. Gate record and the verdict in one table

**Falsifier-first status.** The blueprint names no dedicated numeric gate for
B-M1 (N-1 guards A-M2 and remains the standing wave-0 debt). This unit built
and ran its own leaf-local gate BEFORE §§S2–S6 were composed:
`verification/openmath/kpstep_gate.py` (exact integer arithmetic over ℤ_p,
p ∈ {2,3,5}; order-2 towers at d₀ = 1, g₀ = 1, e₀ ∈ {2,3} — so the level-2
twist units are NONTRIVIAL — state reads (e₁, h₁, ψ₁) with e₁g₁ from 1
(refinement corner) to 9; 336 instances; all four predictions preregistered in
the docstring):

* **P1 (the (K1) clause):** every stride monomial of the marched standard lift
  at one w₂-weight γ̂₂ = e₁g₁γ₂, and w₂(Φ₂) = γ̂₂ — **0 violations** (predicted 0).
* **P2 (DEVID canary):** the development identity at the state key
  (w₂(P) = min_k(w₂(C_k) + kγ̂₂)) on random P — **0 violations** (predicted 0),
  for BOTH the twist-aware and the naked lift (the degree block is
  twist-INsensitive — predicted and confirmed).
* **P3 ((KPb), GMN face):** the level-2 GMN-convention residual (Def 2.19–2.21,
  the t₁-twists) of the twist-aware lift equals τ_g·ψ₁ exactly, τ_g = the
  top-stride twist — **0 violations** (predicted 0 after one builder repair;
  see the finding below). [REV 2, finding 4 — the circularity DISPLAYED: P3's
  BUILDER and CHECKER share the twist formula (declared in the docstring), so
  P3 certifies one implementation's self-consistency, NOT the formula's
  correctness against an independent oracle; the formula-independent teeth
  are P1/P2 (twist-free predictions) and P4 (a refutation).]
* **P4 (twist load-bearing):** the NAKED lift (coefficients lifted without
  twist inversion) has residual ≁ ψ₁ at **120 of 151** nontrivially-twisted
  instances, and at **50 instances the naked residual is REDUCIBLE over the
  residue field** — e.g. (p, e₀, h₀, z₁, e₁, h₁) = (3, 2, 1, 2, 1, 1),
  ψ₁ = y² + 2y + 2 (irreducible over 𝔽₃), naked residual y² + 2y + 1 = (y+1)².
  A naked lift at such an instance has in(Φ₂) = unit·hom of a REDUCIBLE
  polynomial: **(KPa)-with-irreducible-ψ̂ and (KPirr) FAIL for the naked
  reading of "standard lift".** The marched/twist-aware clause in the lift
  specification below is therefore load-bearing, not bookkeeping.

**Gate scope (REV 2, finding 4 — what the runs establish, no more).** The gate
establishes exactly its reported finite computations: order EXACTLY 2,
p ∈ {2, 3, 5}, d₀ = g₀ = 1, e₀ ∈ {2, 3}, e₁g₁ ∈ 1..9, random test polynomials
for P2. It does not establish DEVID universally, OP-B1, arbitrary residue
fields, higher orders — and it CANNOT see the REV-2 (K0) hypothesis at all
(every gate instance is constructed conformant; the pass-1 padding
counterexamples are invisible to it by design). P4 validly REFUTES the
universal claim "the naked construction always preserves the residual" (and
refutes primeness outright at the 50 reducible instances); it does NOT show
that every naked lift fails, nor that the engine uses the naked construction.
Standing rule (§S9(v), re-affirmed): the gate SUPPORTS statement choices and
REFUTES alternatives; it proves nothing — no claim of this note rests on it.
"GATE PASS" below is a record that the four preregistered predictions held at
the tested perimeter, nothing else.

**Two findings the gate forced into the statement (both folded below, neither
a repair of any source leaf):** (i) the P3/P4 twist arithmetic — the naive
"lift each residual coefficient over a naked Φ₀^a π^b normalizer" construction
is REFUTED as a standard lift at E_i > 1 (the V26-2/V28-1-family lesson
recurring one level up; at order 1, E₀ = 1 and the twists are invisible, which
is why KEY-g's classical lift is naked AND correct); (ii) the TOP-stride twist
τ_g cannot be inverted away (D_g = 1 is forced by monicity), which is exactly
why (KPb) is a "∼"-statement (GMN Def 2.12 normalization), never an equality.

**Per-section verdict.**

| section | claim | verdict |
|---|---|---|
| S1 | Theorem KP-STEP stated at the DEGREE-CONFORMANT class ((K0)+(K1)+(K2) intrinsic form + (SL-dev)+(SL-top) construction form + existence) [REV 2, findings 1–2: (K1)+(K2) alone is FALSE for minimality — padding; the class-scope display + counterexample now IN the statement] | stated; hypotheses displayed |
| S2 (s1) | marched homogenization hom^m multiplicative + injective | **PROVED** (from (GR-B-STR)(0)/(0′)) |
| S3 (s2) | in(Φ_{i+1}) = U₀·ψ̂_hom ((KPa) initial-form identity) | **PROVED** from (K1)+(K2) via (2′) (degree-free); (SL-dev) ⟹ (K1)+(K2) proved; (SL-dev)+(SL-top) ⟹ (K0), U₀ = 1 [REV 2]; DEVID-consumption note displayed |
| S4 (s3/s4) | graded presentation + (KPirr) primeness | **PROVED** — pinned at FGMN Thm 4.13 + Cor 4.14 (both re-read this session); transported order-1 argument displayed as corroboration; (s4a) localization-avoidance VOID (no localization exists to avoid — Thm 4.13 is about Gr(µ) itself) |
| S5 (s5) | **OP-B1, the degree block at the FIXED bound e·g·deg Φ_i — PROVED** (Lemma LOW-CONST + Lemma IN-FORM + x-degree bookkeeping; the statement is Φ_{i+1}-free); its KEY-MINIMALITY reading (bound = deg Φ_{i+1}) holds exactly on the (K0) class and is FALSE beyond it [REV 2, findings 1/3]; the blueprint's candidate pin FGMN Prop 5.7 checked and REJECTED (it is key-equivalence, not the low-degree block — the direct route of the blueprint's fallback is executed) | **PROVED** (fixed bound; minimality (K0)-gated) |
| S6 (s6) | (KPb): R^gr(ψ̂_hom) = ψ (FGMN-convention weight- and side-read), residual-ideal form; project/GMN-convention face | FGMN form **PROVED**; GMN face rides **open TR-3-ORD-X's label tie** (displayed, NOT claimed) — with the gate's τ_g display |
| S7 | B-M2 (s7, OL-1 family clause) statement + inputs | DISPLAYED, not proved (wave 3) |
| S8 | consumers/residue | see S8 |

**What this note does NOT claim:** that the ENGINE's pinned Lift_i emits
(K0)+(K1)+(K2)-conformant lifts at orders ≥ 2 (the conformance package —
OL-1's discharge chain through B-M2 and member (c): (K1)+(K2) is §S7's
obligation (i), gate-P4-certified as real; (K0) is B-M2's own clause (a),
D8-hard-wired on the Lean face); the GMN-convention (KPb) face; anything
about (GR-B-KEY) or properness (TR-6 untouched); B-M2.

## S1. Setting, inputs, and the statement

### S1.1 Standing setting and the induction state

O an arbitrary complete DVR, π, v, K, F as in GD23 §1.1 (completeness and
perfectness carried, used nowhere below). Policy 𝒫 fixed; frames
(Φ_j, w_j, K_j) built by lawful reads per GD23 §1.2:
w_{j+1}(B) := min_t(e_j·w_j(c_t) + t·γ_{j+1}) over the Φ_j-adic development,
γ_{j+1} = e_j·w_j(Φ_j) + h_j, K_{j+1} = K_j[z]/(ψ_j), E_j = w_j(π) = e₀⋯e_{j−1}.
State order i+1 ≥ 2 fixed; w := w_{i+1}; gr := gr_K(w) over K[x] with in(·)
(GD23 §1.3). The creating read ν_i carries (e, h) := (e_i, h_i) coprime ≥ 1 and
ψ := ψ_i ∈ K_i[z] monic irreducible of degree g := g_i ≥ 1 with ψ(0) ≠ 0.
V := w_i(Φ_i) (so γ_{i+1} = e·V + h).

**Induction state (consumed, never proved here — the §0 ladder of the
blueprint):** (V1)_{≤ i+1} — each w_j, j ≤ i+1, is a valuation on K[x] (so gr
is a domain and in(·) multiplicative, GD23 (g2)); (KP)_{≤ i−1} — the key blocks
strictly below the state. Both are exactly what the ladder supplies at this
rung (base: GD23 GAUSS-g/VAL-g/KEY-g; step: BRIDGE + MacLane Thm 4.2 per GD23
§5, consuming (KPa)+(KPirr)+monicity one level down).

**(GR-B-STR)_{i+1} (consumed AS THE A-M2 LEAF EMITS IT — GRB_ORDER2 rev 4
§S1.4, FGMN-convention instantiation).** The clauses fired below, with their
GRB discharge sites:
* **(0)** Δ = K_i[ȳ], a polynomial ring on the distinguished degree-0
  generator ȳ [GRB S5.1; FGMN Thm 4.8 + Cor 4.9].
* **(0′)** the anchor system ϕ_γ = x^{s(γ)}p^{u(γ)}, γ ∈ ℤ, exponents pinned
  by (12-int): u(γ)·e + s(γ)·h = γ, 0 ≤ s(γ) < e [GRB S5.2; FGMN Lemma 4.1 /
  eq (12)], with Lemma CARRY-1 (ϕ_γϕ_{γ′} = ϕ_{γ+γ′}·ȳ^δ, δ = ⌊(s+s′)/e⌋)
  [GRB S5.2, proved, G1-gated]. Distinguished classes (GRB S2.2's REV-4
  realization claim): p := the class of the level-r uniformizer π_r (weight e,
  INVERTIBLE), x := the class of Φ_i·π_r^{−V} (weight h; so
  **in(Φ_i) = x·p^V** definitionally), ȳ = x^e·p^{−h}. The lower-key classes
  and π̄ are UNITS of gr itself (FGMN fgmn.txt:1140 through GRB S2.2).
* **(0″)** the total residual-operator pair R^gr_γ / R^gr on nonzero
  homogeneous elements, valued in K_i[z] ∖ {0}: weight-read slot κ at
  ABSCISSA s(γ) + κe of the argument's Φ_i-development, nonzero κ-coefficient
  iff that abscissa's digit attains the weight (FGMN Cor 4.4(1)(2) through
  GRB S3.1/S5.4); side-read R^gr = R^gr_γ/y^{j₀}, j₀ = the weight-read's
  z-order, nonzero constant term (Cor 4.4(3)(4)).
* **(1)** total multiplicativity of the side-read, θ = 1 in the FGMN
  instantiation [GRB S6.1; FGMN Cor 4.7(3)], and the weight-pair law (W-MULT)
  with explicit carry [GRB S6a].
* **(2′)** the anchored-march identity, both pairings:
  in(f) = ϕ_{w(f)}·R^gr_{w(f)}(in f)(ȳ) = ϕ(f)·R^gr(in f)(ȳ), tied by
  ϕ(f) = ϕ_{w(f)}·ȳ^{j₀} [GRB S6.2; FGMN Thm 4.2 + eq (16)]. Its "∼ R_λ" leg
  (project/GMN convention) is NOT consumed anywhere in this note's proofs —
  the S6 convention display is where it would enter, and it is left open
  there exactly as GRB S3 prices it.

Conditionality inherited: (GR-B-STR) itself is conditional on the ladder
inputs ((V1)_{≤ i+1}, (KP)_{≤ i−1}, (H-CHAIN)) and rides the GRB leaf's own
verification state (0/2 clean passes; N-1 owed). Nothing below consumes
(GR-B-KEY), the 𝒟-clause (D-REAL/D-ITER), TR-3′-GEN, TR-3-ORD-X, or the
properness pin — checked line-by-line at §S9(iii).

**TR-5 index line (displayed once, the GD23 §6 discipline).** FGMN depth
r = i+1: φ_r ↔ Φ_i (FGMN's Φ_r := φ_rπ_r^{−V_r} ↔ our x-class), µ_r ↔
w_{i+1}/E_{i+1} (Lemma RESCALE, GRB S2), (e_r, h_r) ↔ (e_i, h_i), F_r ↔ K_i,
ψ_r ↔ ψ_i, y_r ↔ ȳ, p_r ↔ p, V_r ↔ V = w_i(Φ_i); slopes per (TR-1-corr):
λ^GMN = −h_i/e_i = −E_i·λ_r^FGMN. All FGMN pins below are consumed through
Lemma RESCALE's verbatim transport (weights relabeled γ = E_{i+1}α), as at
every GRB S5/S6 line.

### S1.2 The marched homogenization and the state key form (the objects)

**Definition (hom^m).** For nonzero c ∈ K_i[z] of degree g′, the marched
(e, h)-homogenization at the state is

    (HOM)   hom^m(c) := p^{g′·γ_{i+1}} · c(ȳ)  ∈ gr,   homogeneous of
            weight e·g′·γ_{i+1},

and the state key form of the read's ψ is **ψ̂_hom := hom^m(ψ) =
p^{g·γ_{i+1}}·ψ(ȳ)**, of weight γ̂ := e·g·γ_{i+1}. Anchor form: since
γ̂ ∈ eℤ, (12-int) gives s(γ̂) = 0, u(γ̂) = g·γ_{i+1}, so ϕ_{γ̂} = p^{g·γ_{i+1}}
and hom^m(c) = ϕ_{e g′ γ_{i+1}}·c(ȳ) — the anchor-times-degree-0 shape, and
monomial-wise hom^m(c) = Σ_κ c_κ·ϕ_{γ(c)}·ȳ^κ: exactly the "Σ [ĉ_κ]·ϕ·ȳ^κ-
shaped sums" of the blueprint's (s1), the marched monomial system
M̄_κ := ϕ_{γ̂}·ȳ^κ = x^{eκ}p^{gγ_{i+1}−hκ} (all at ONE weight γ̂).

**Order-1 reproduction (the non-vacuity anchor, checked now).** At i = 0:
p = π̄, x = Φ̄₀ (V₀ = 0), γ₁ = h, so hom^m(c) = π̄^{g′h}·c(Φ̄₀^eπ̄^{−h}) =
Σ_κ c_κ π̄^{(g′−κ)h}Φ̄₀^{eκ} — VERBATIM the hom_{e,h} of GD23 Theorem KEY-g,
and ψ̂_hom = KEY-g's ψ_hom. Every construction below instantiates at i = 0 to
the KEY-g object it transports.

### S1.3 Theorem KP-STEP (the B-M1 statement)

**Theorem KP-STEP (RESTATED at REVISION 2 — the degree-conformant class;
pass-1 findings 1–3).** Assume the induction state and (GR-B-STR)_{i+1} of
S1.1, with read data (e, h, ψ, g) as displayed; write m := deg Φ_i ≥ 1. Let
Φ_{i+1} ∈ K[x] be a **conformant key lift at the read**, i.e.

    (K0)  Φ_{i+1} is MONIC with deg Φ_{i+1} = e·g·m   (degree conformance —
          exactly the normalization FGMN Cor 4.12 supplies (§S3.3) and
          OL-1(a) specifies for the standard lift (§S8 alignment)),
    (K1)  w_{i+1}(Φ_{i+1}) = γ̂ = e·g·γ_{i+1},   and
    (K2)  R^gr_{γ̂}(in Φ_{i+1}) = U₀·ψ  for some U₀ ∈ K_i^×.

Then the full (KP_i) block holds for Φ_{i+1} in gr(w_{i+1}):

* **(KPa)** in(Φ_{i+1}) = u·ψ̂_hom with u := U₀ invertible homogeneous
  (indeed u ∈ K_i^×; on this class u = U₀ = 1 — the remark below), AND the
  minimality block: ψ̂_hom ∤ in(B) for every nonzero B ∈ K[x] with
  deg B < deg Φ_{i+1} — the bound is the key's OWN degree, and by (K0) it
  equals e·g·m, the fixed bound at which §S5's OP-B1 is proved.
* **(KPirr)** ψ̂_hom is PRIME in gr(w_{i+1}).
* **(KPb-F)** (FGMN-convention/convention-free form) the weight-read and the
  side-read of the state key return the residual factor:
  R^gr_{γ̂}(in Φ_{i+1}) = U₀·ψ ∼ ψ, with z-order j₀ = 0 so the side-read
  agrees; equivalently, convention-free: the residual IDEAL
  (in(Φ_{i+1})·gr) ∩ Δ equals ψ(ȳ)·Δ, the Max(Δ)-point labeled by ψ.
  [The project/GMN-convention face R_i(Φ_{i+1}) ∼ ψ_i is NOT part of the
  theorem: §S6.3 displays it riding the open TR-3 dictionary.]

**Class-scope display (REV 2 — what needs (K0) and what does not; the pass-1
counterexample IN the statement).** §§S3.1, S4, S6 never touch deg Φ_{i+1}:
the (KPa) IDENTITY, (KPirr), and (KPb-F) hold verbatim on the wider class
(K1)+(K2) alone. The MINIMALITY block does NOT — on the bare (K1)+(K2) class
it is FALSE: take Cor 4.12's conformant representative Φ and any T ∈ K[x]
with w_{i+1}(T) > γ̂ and deg T > e·g·m; then Φ + T still satisfies (K1)+(K2)
(in(Φ + T) = in(Φ): padding of valuation strictly above γ̂ is invisible to
EVERY initial-form datum — weight, weight-read, side-read), while B := Φ has
deg B = e·g·m < deg(Φ + T) and ψ̂_hom ∣ in(B). The same padding breaks
monicity at will. No initial-form hypothesis can exclude it; only the degree/
monicity hypothesis (K0) does. [Pass-1 findings 1/2/3, ACCEPTED — rev 0's
bracketed "[= deg Φ_{i+1} whenever …]" acknowledged the gap without imposing
the hypothesis.] The restriction costs the consumers NOTHING: every consumer
consumes only (K0)-conformant lifts, by specification — the §S8 alignment
display.

**Remark (unit normalization on the class).** (K0)+(K1)+(K2) force U₀ = 1:
Φ_{i+1} monic of degree e·g·m has Φ_i-adic top slot eg with digit C_{eg} = 1
(monic division), that slot attains γ̂ (value e·w_i(1) + eg·γ_{i+1} = γ̂,
which (K1) says is the min), so IN-FORM(b)/(c) (§S5) give in(Φ_{i+1}) the
x^{eg}-coefficient 1·p^{w_i(1)+egV} = p^{egV}; matching against U₀·ψ̂_hom's
top monomial U₀·ψ_g·x^{eg}·p^{gγ_{i+1}−hg} = U₀·x^{eg}·p^{egV} (ψ_g = 1, ψ
monic; exponent count γ_{i+1} = eV + h) forces U₀ = 1. So in(Φ_{i+1}) =
ψ̂_hom EXACTLY on the class — consistent with Cor 4.12 (R_r(g) = ψ on the
nose, §S3.3) and with the S0 finding (ii) (D_g = 1 forced by monicity).

**Companion clauses (proved with the theorem):**

* **(SL-dev + SL-top ⟹ K0+K1+K2), the construction-facing form [REV 2,
  finding 2 — the top-normalization clause ADDED; (SL-dev) alone does NOT
  suffice].** If Φ_{i+1} = Σ_{κ: ψ_κ ≠ 0} D_κ·Φ_i^{eκ} with each D_κ ∈ K[x]
  nonzero of deg D_κ < deg Φ_i and MARCHED initial data — for one shared
  U₀ ∈ K_i^×:

      (SL-dev)   in(D_κ · Φ_i^{eκ}) = U₀ · ψ_κ · ϕ_{γ̂} · ȳ^κ   for every κ
                 with ψ_κ ≠ 0   (equivalently in(D_κ) = U₀·ψ_κ·p^{(g−κ)γ_{i+1}}),

  AND the monic top stride

      (SL-top)   D_g = 1,

  then (K0)+(K1)+(K2) hold, with U₀ = 1 (§S3.2). (SL-dev) alone does NOT
  force (K0): ψ is monic (ψ_g = 1), so at κ = g (SL-dev) reads in(D_g) = U₀
  — a constraint on D_g's INITIAL FORM only (weight 0, constant read). Two
  failures survive it: (a) at ANY m, take D_g = a constant c ≠ 1 (with
  U₀ = its class) — monicity fails; (b) at m ≥ 2, replace D_g by D_g + J
  with deg D_g < deg J < m and w_{i+1}(J) > w_{i+1}(D_g) = 0 — every
  (SL-dev) identity survives (in(D_g + J) = in(D_g)) while
  deg Φ_{i+1} = deg J + e·g·m > e·g·m and the top coefficient becomes J's:
  the pass-1 finding-2 example. (SL-top) excludes both, and it is no extra
  demand on the standard lift: it IS
  the standard lift's displayed top term (blueprint §2.2 (s7)(a): "the κ = g
  monomial is Φ_i^{eg}-shaped with unit normalizer, lower κ strictly smaller
  degree"; corpus face: the D8 predicate `HC2.IsReadLift` HARD-WIRES
  Φhat = σ.Φ^{e·g} + Σ_{k<g} t_k·σ.Φ^{e·k}, top coefficient literally 1
  [REV 3, pass-2 GAP 1: the shape yields monic-of-exact-degree only GIVEN
  σ.Φ monic — the PARENT invariant, Lemma PAR-MON §S3.2; corpus carrier
  `Stage.hmonic`, not the lift formula]) —
  the §S8 alignment. [The blueprint's Σ_κ ψ̂_κ·M_κ display, with M_κ THE
  MARCH monomials' representatives: the gate's P4 shows that reading M_κ as
  naked Φ₀-π monomials with naked coefficient lifts VIOLATES (K2) — and
  (KPirr) itself — at E_i > 1; "marched" is a genuine constraint on the
  construction, invisible at order 1 where E₀ = 1.]
* **(SL-EX), existence.** There EXISTS a monic residual-conformant lift of
  degree e·g·deg Φ_i — FGMN Cor 4.12 retargeted (§S3.3), landing INSIDE the
  (K0) class with U₀ = 1: so KP-STEP is non-vacuous at every order and every
  read.

Proof: (K1)+(K2) ⟹ (KPa)-identity at §S3.1 (degree-free); (SL-dev) ⟹
(K1)+(K2) and (SL-top) ⟹ (K0) at §S3.2; (SL-EX) at §S3.3; (KPirr) at §S4
(degree-free); the minimality block at §S5 (OP-B1 at the fixed bound e·g·m +
the (K0) identification); (KPb-F) at §S6 (degree-free). Each section opens
with its exact inputs; the per-section class re-verification is recorded at
§REVISION-2(iii).

## S2. (s1) The marched homogenization: multiplicative and injective

*Inputs: (GR-B-STR)(0) (Δ = K_i[ȳ] a polynomial ring; z ↦ ȳ an isomorphism
K_i[z] ≅ Δ — FGMN Thm 4.8's R_{r,0}-inverse through the GRB S5.1 discharge),
(0′) (the anchors; CARRY-1 for the display only), (V1)_{i+1} (gr a domain),
γ_{i+1} ≥ 1 (γ_{i+1} = eV + h, V = w_i(Φ_i) ≥ 0, h ≥ 1).*

**Lemma HOM-M.** hom^m : K_i[z] ∖ {0} → gr ∖ {0} of (HOM) satisfies:
(a) hom^m(c·c′) = hom^m(c)·hom^m(c′) for all nonzero c, c′;
(b) hom^m is injective;
(c) hom^m(c) is homogeneous of weight e·(deg c)·γ_{i+1}, with
    R^gr-weight-read equal to c (§S3.1's mechanism), and x-degree e·deg c.

*Proof.* (a) deg(cc′) = deg c + deg c′ (K_i[z] a domain), so
hom^m(c)hom^m(c′) = p^{(g′+g″)γ_{i+1}}·c(ȳ)c′(ȳ) = p^{(g′+g″)γ_{i+1}}·(cc′)(ȳ)
= hom^m(cc′): the substitution z ↦ ȳ is a ring homomorphism into Δ (clause
(0)), and the p-powers add freely. *Carry display (the blueprint's "exponents
add slot-wise as at order 1"):* the anchors fired are exactly the s = 0
family ϕ_{eγ′} = p^{γ′} (e | weight ⟹ s = 0 by (12-int) uniqueness), and
CARRY-1's wrap δ = ⌊(s + s′)/e⌋ = ⌊0/e⌋ = 0 on this family — hom^m is
carry-free BY THE CHOICE of the e-divisible weight frame, which is why it is
defined there and not on all weights (on general weights the wrap ȳ^δ of
(W-MULT) appears; GRB S6a).
(b) Suppose hom^m(c) = hom^m(c′). Weights: e·(deg c)·γ_{i+1} =
e·(deg c′)·γ_{i+1} with e, γ_{i+1} ≥ 1, so deg c = deg c′; cancel the
invertible p-power: c(ȳ) = c′(ȳ) in Δ; z ↦ ȳ injective (clause (0)) gives
c = c′.
(c) Weight: w(p) = e (p = the π_r-class, GRB S5.2's weight table), so
p^{g′γ_{i+1}} has weight eg′γ_{i+1} and c(ȳ) has weight 0. x-degree: monomial-
wise hom^m(c) = Σ_κ c_κ x^{eκ}p^{g′γ_{i+1}−hκ} (substitute ȳ = x^ep^{−h});
distinct κ give distinct x-exponents eκ; top κ = deg c has c_κ ≠ 0. The
weight-read clause is proved at §S3.1 (it needs (2′)). ∎

At i = 0 (order 1) HOM-M(a)(b) are verbatim KEY-g(ii)'s "hom_{e,h} is
multiplicative and injective" — the transported statement, nothing more.

## S3. (s2) The initial form of the standard lift, and existence

### S3.1 (K1)+(K2) ⟹ the (KPa) initial-form identity

*Inputs: (2′) weight form, (0), (0′); domain discipline: in(Φ_{i+1}) is
nonzero homogeneous of weight γ̂ (definitional — w(Φ_{i+1}) = γ̂ is (K1), and
a nonzero polynomial's class at its own weight is nonzero), so the operators
fire on a legal argument (the GD23 F1 discipline).*

By (2′) at f := Φ_{i+1}, weight pairing:
in(Φ_{i+1}) = ϕ_{γ̂}·R^gr_{γ̂}(in Φ_{i+1})(ȳ) = ϕ_{γ̂}·(U₀·ψ)(ȳ)
= U₀·p^{g·γ_{i+1}}·ψ(ȳ) = U₀·ψ̂_hom,
using (K2), ϕ_{γ̂} = p^{gγ_{i+1}} (s(γ̂) = 0: e | γ̂), and K_i-linearity of
evaluation. Since U₀ ∈ K_i^× is an invertible homogeneous element of gr
(weight 0), this is (KPa)'s identity with u = U₀. ∎

*Conversely* if in(Φ_{i+1}) = u·ψ̂_hom with u ∈ K_i^×, then (K1) holds by
weight count and (K2) with U₀ = u by the same display read backwards ((2′)'s
read is determined: ϕ_{γ̂} invertible and z ↦ ȳ injective force
R^gr_{γ̂}(in Φ_{i+1})(ȳ) = ϕ_{γ̂}^{−1}·in(Φ_{i+1})). So (K1)+(K2) is exactly
the (KPa)-identity, stated operator-side.

### S3.2 (SL-dev) ⟹ (K1)+(K2)

*Inputs: the development definition of w_{i+1} (GD23 §1.2 — definitional, NO
DEVID consumed; see the note below), (V1)_{i+1}, in(Φ_i) = x·p^V ((0′)),
POS-g(iii) (superadditivity, GD23 §3, unconditional).*

Let Φ_{i+1} = Σ_{κ:ψ_κ≠0} D_κΦ_i^{eκ} be (SL-dev)-conformant. Since
deg D_κ < deg Φ_i, this IS the Φ_i-adic development (uniqueness of monic
division): slot eκ ↦ D_κ, all other slots zero. The two (SL-dev) displays
agree: in(D_κΦ_i^{eκ}) = in(D_κ)·(x p^V)^{eκ} by (V1)_{i+1} and the x-class
pin, and ϕ_{γ̂}ȳ^κ = p^{gγ_{i+1}}x^{eκ}p^{−hκ}, so
in(D_κ) = U₀ψ_κ·p^{gγ_{i+1}−hκ−eκV} = U₀ψ_κ·p^{(g−κ)γ_{i+1}}
(γ_{i+1} = eV + h) — each stride coefficient's class is the marched
p-power times the residual coefficient, ONE shared U₀.

(K1): w(Φ_{i+1}) := min over the development of e·w_i(D_κ) + eκ·γ_{i+1}
(the §1.2 definition), and each stride term has this value = γ̂ (the
(SL-dev) class lives at weight γ̂). So w(Φ_{i+1}) = γ̂ — definitionally, all
slots minimizing.

(K2): B′ := Φ_{i+1} is literally the sum of its stride terms, all at weight
γ̂, so its class at weight γ̂ is the sum of their classes (the quotient map
P_γ̂ → P_γ̂/P_γ̂⁺ is additive):
in(Φ_{i+1}) = Σ_κ U₀ψ_κ·ϕ_{γ̂}·ȳ^κ = U₀·ϕ_{γ̂}·ψ(ȳ) ≠ 0
(Δ = K_i[ȳ] a polynomial ring and ψ ≠ 0; nonzeroness is also definitional).
Then as in S3.1's converse, R^gr_{γ̂}(in Φ_{i+1}) = U₀·ψ. ∎

**(K0) from (SL-top) [REV 2, finding 2].** Add (SL-top): D_g = 1. The κ = g
stride is then Φ_i^{eg} — monic (Φ_i monic) of degree e·g·m, m := deg Φ_i.
Every lower stride is strictly smaller: for κ ≤ g − 1,
deg(D_κ·Φ_i^{eκ}) ≤ (m − 1) + e(g − 1)·m < e·g·m (the gap 1 + e(g−1) ≤ eg is
1 ≤ e). So Φ_{i+1} is monic of degree exactly e·g·m — (K0) holds. Moreover
(SL-dev) at κ = g reads in(D_g) = in(1) = 1 = U₀·ψ_g·p⁰ = U₀ (ψ monic): the
shared unit is 1, as the S1.3 remark forces on the whole (K0) class. ∎

**Lemma PAR-MON (the parent-key monicity invariant and its tower induction
— the (SL-top) ⟹ (K0) count's silent input, DISPLAYED [REV 3, pass-2
GAP 1]).** Every frame key Φ_j, 0 ≤ j ≤ i, is monic (with deg Φ_j ≥ 1).

*Proof (induction up the tower).* Base: Φ₀ is the seed key — monic of
degree ≥ 1 by the frame construction (GD23 §1.2's lawful-read base; in the
compiled corpus it is literally X). Step: each Φ_{j+1} is a standard lift
at the level-j read, i.e. of the (SL-dev)+(SL-top) stride shape
Φ_j^{e_j g_j} + Σ_{κ<g_j} D_κ Φ_j^{e_j κ} with deg D_κ < deg Φ_j; GIVEN
Φ_j monic (the induction hypothesis), the top stride Φ_j^{e_j g_j} is monic
of degree e_j g_j deg Φ_j and every lower stride has strictly smaller
degree (this section's count, run one level down), so Φ_{j+1} is monic of
exact degree — monicity propagates. ∎

*Corpus face (source-confirmed this session — pass 2 could not read the
corpus).* Parent monicity is a FIELD of the state, not a consequence of the
lift formula: `Stage.hmonic : Φ.Monic` (`lean/LeanUrat/Moves/Defs.lean:124`,
alongside `hdeg : 1 ≤ Φ.natDegree`), discharged at the base by
`Polynomial.monic_X` (e.g. `HC1/T1_baseStage.lean:1547`) and RE-SUPPLIED at
every stage transition as the forged stage's own `hmonic` obligation (e.g.
`MovesD/R7_ramifiedForge.lean:118` via `HK47.transHyp_gate.hmonic`) — the
Lean mirror of the induction above. Consequently every wiring claim of this
note ("`IsReadLift` hard-wires (SL-top), hence (K0)") reads: the D8 shape
PLUS `σ.Φ`'s `Stage.hmonic` supply (K0); the shape alone fixes D_g = 1 and
the degree count but yields monicity only through the parent invariant.

**DEVID-consumption note (a simplification vs the blueprint, displayed, not
a gap).** The blueprint's (s2) priced "DEVID at the PARENT level" as the
input for the development weights. In development form the weights are
DEFINITIONAL — w_{i+1} is defined by the Φ_i-development min (GD23 §1.2) —
so that consumption evaporates; no appeal to GD23 §4 occurs anywhere in this
note's proofs. DEVID reappears only DOWNSTREAM: once (KPa) lands, GD23
Theorem DEVID applies at the NEW pair (w_{i+1}, Φ_{i+1}) — that is the
gate's P2 observable and the (V2b) clause the ladder's next rung consumes.

### S3.3 (SL-EX): existence of residual-conformant monic lifts — FGMN Cor 4.12 retargeted

*Pin (NEW in this note; re-read this session at fgmn.txt:1621–1634),
verbatim:* "**Corollary 4.12.** Let ψ ∈ F_r[y] be a monic polynomial of
degre f such that ψ(0) 6= 0. Then, for any α ∈ Γ(µ) there exists g ∈ K[x]
monic such that deg g = e_r f m_r, µ(g) = e_r f (w_r + λ_r) and R_r(g) = ψ."
Their proof (quoted for the two facts we consume; the OPENING BINDING now
included [REV 3]): "Denote α := e_r f (w_r + λ_r). By Corollary 4.11, there
exists g₀ ∈ K[x] with µ(g₀) = α and R_{r,α}(g₀) = ψ − y^f. By dropping all
terms with abscissa s ≥ e_r f from the φ_r-expansion of g₀, we may assume
that deg g₀ < e_r f m_r. Then, g = φ_r^{e_r f} + g₀ satisfies what we want.
… Since R_{r,α}(g)(0) = ψ(0) ≠ 0, we have R_{r,α}(g) = R_r(g)."

**Quotation repair display [REV 3, pass-2 GAP 2].** The printed statement's
"for any α ∈ Γ(µ)" quantifier is INERT: the displayed conclusion never
mentions α (both quoted lines above ARE verbatim to fgmn.txt:1621–1634 —
the source itself carries the vacuous quantifier, presumably a typo for
"take α := e_r f (w_r + λ_r)"), and the printed proof immediately REBINDS α
by the "Denote" line — which rev ≤ 2's proof quote OMITTED, leaving its α
apparently bound by the statement's spurious quantifier: that was the
malformation. What this note consumes is exactly the α-free existence
claim: one monic g of degree e_r f m_r with µ(g) = e_r f (w_r + λ_r) and
R_r(g) = ψ. Nothing consumes the quantifier.

Retarget through TR-2/TR-5 (f ↔ g, m_r ↔ deg Φ_i, e_r ↔ e): there exists a
MONIC Φ_{i+1} ∈ K[x] of degree e·g·deg Φ_i with
w(Φ_{i+1}) = E_{i+1}·eg(w_r + λ_r) = e·g·γ_{i+1} = γ̂ (the GRB S2.1
augmentation-value identity E_{i+1}(µ_{r−1}(φ_r) + λ_r) = γ_{i+1}, G3(d)-
gated there) — that is (K1) — and with WEIGHT-read equal to ψ exactly (their
last displayed sentence: R_{r,α}(g) = R_r(g) = ψ, i.e. j₀ = 0 and U₀ = 1) —
that is (K2). So residual-conformant monic lifts of the conformant DEGREE
exist at every state and every read: KP-STEP is non-vacuous, and B-M2's
clause (a) has its existence supply. ∎

**(K0)-membership display [REV 2].** Cor 4.12's representative is monic of
degree e_r·f·m_r = e·g·deg Φ_i — it satisfies (K0)+(K1)+(K2) VERBATIM, with
U₀ = 1: the existence supply lands INSIDE the REV-2 restricted class. This is
the "exactly what Cor 4.12 supplies" alignment: (K0) was chosen AT the
printed normalization, not invented — the theorem's class is the literature's
representative class.

*Honesty line:* Cor 4.12 supplies EXISTENCE; it does not say the ENGINE's
pinned Lift_i is such a lift (lift conformance — §S7/§S8). Note their g is
monic in K[x]; integrality (O[x]) is B-M2's construction-side business.

## S4. (s3)+(s4) The graded presentation and (KPirr)

### S4.1 The presentation (the factor-collection section, done once, printed)

*Pin (NEW in this note; re-read this session at fgmn.txt:1636–1660),
verbatim core:* "**Theorem 4.13.** We get an isomorphism of graded
F_r-algebras Gr(µ) = ⊕_{α∈Γ(µ)} ϕ_r(α)∆ ≃ F_r[y, p, p^{−1}][x], where y, p
are indeterminates and x is an algebraic element satisfying x^{e_r} =
p^{h_r} y." (Their proof: the monomials y^m p^n are F_r-linearly independent
— gr a domain with y_r transcendental — and the relation x^{e_r} = p^{h_r}y_r
is minimal since gcd(h_r, e_r) = 1.)

Through TR-2/TR-5: gr(w_{i+1}) = ⊕_{γ∈ℤ} ϕ_γ·Δ ≅
K_i[y, p^{±1}][x]/(x^e − p^h y). Eliminating y (the relation solves y =
x^e p^{−h} — substitution is an isomorphism onto):

    (PRES)   gr(w_{i+1}) ≅ K_i[p^{±1}][x],  free K_i-basis {x^a p^b :
             a ∈ ℤ_{≥0}, b ∈ ℤ},  weight(x^a p^b) = a·h + b·e,

a UFD (localization of the polynomial UFD K_i[p][x] at the prime p), carrying
a well-defined x-DEGREE on nonzero elements (top x-exponent), additive under
products (domain), with ALL x-exponents ≥ 0. At i = 0 this is VAL-g's
K₀[π̄^{±1}][Φ̄₀] verbatim. *Corroboration from the package alone (so the
input does not silently widen):* (2′) gives the rank-one decomposition
gr = ⊕ ϕ_γΔ; CARRY-1 makes the monomial dictionary ϕ_γ·ȳ^j ↦
x^{s(γ)+ej} p^{u(γ)−hj} a ring isomorphism onto the free monomial algebra
(exponent addition = CARRY-1's arithmetic, checked both ways by the (12-int)
uniqueness); the image basis is exactly (PRES)'s.

**(s4a), the REV-2 finding-14 display: VOID.** No localization occurs
anywhere in this note: Thm 4.13 presents Gr(µ) ITSELF over the K[x]-carrier;
p^{±1} and the lower-key classes are units of gr (fgmn.txt:1140 through GRB
S2.2 REV 4), Δ ⊂ gr, and the anchors live in gr. There is no inverted
non-unit for ψ̂_hom to be divisible by, and no pullback step to display. The
order-1 shadow: π̄ was already invertible in gr(w₁) because π^{−1} ∈ K.

### S4.2 (KPirr): primeness of ψ̂_hom — printed at FGMN Cor 4.14

*Pin (NEW in this note; re-read this session at fgmn.txt:1663–1676),
verbatim:* "**Corollary 4.14.** Let ψ ∈ F_r[y] such that ψ(0) 6= 0. Then,
ψ(y_r) ∈ ∆ is a prime element in Gr(µ) if and only if ψ is irreducible in
F_r[y]." (Their proof of the direction we consume: Gr(µ)/ψ(y_r)Gr(µ) ≅
F′[p, p^{−1}][x] with F′ = F_r[y]/(ψ) and x^{e_r} = p^{h_r}z̄, z̄ ≠ 0 the
class of y — an integral domain, so the ideal is prime.)

Retarget: ψ ∈ K_i[z] monic irreducible with ψ(0) ≠ 0 (the read data), so
ψ(ȳ) is prime in gr(w_{i+1}); and ψ̂_hom = p^{gγ_{i+1}}·ψ(ȳ) is an ASSOCIATE
of ψ(ȳ) (p invertible), hence prime. **(KPirr) holds.** ∎

*Corroboration — the order-1 argument transported verbatim in (PRES) (KEY-g
(ii)'s proof shape, so the retarget visibly REPRODUCES the proved layer):*
suppose ψ̂_hom = δδ′ in gr. Collect x-floors: by (PRES) each of δ, δ′ is
(x^{a}p^{b})·(unit-free part) with x-floor a ≥ 0; x-floors add, and ψ̂_hom
has x-floor 0 (its κ = 0 monomial ψ₀·p^{gγ_{i+1}} ≠ 0 since ψ(0) ≠ 0), so
a = a′ = 0 and δ = p^{b}·hom^m(c), δ′ = p^{b′}·hom^m(c′) with c(0), c′(0) ≠ 0
(collect the least ȳ-power of the Δ-parts; a nonzero ȳ-floor would be a
positive x-floor). Then hom^m(ψ) = p^{b+b″+…}hom^m(cc′) forces, by matching
top ȳ-coefficients and HOM-M(b), cc′ = ψ up to K_i^×; irreducibility of ψ
makes c or c′ constant, i.e. δ or δ′ a unit K_i^×·p^ℤ. Irreducible in the
UFD (PRES) ⟹ prime. (Same skeleton as KEY-g(ii): "Φ̄₀ ∤ ψ_hom, π̄-unit
prefactors, injectivity of hom"; the general-order novelty is only that the
top term carries the p-power p^{geV} — absorbed into the unit group.)

*Honesty:* Cor 4.14's converse (prime ⟹ irreducible) is not consumed.

## S5. (s5) = OP-B1: the degree block at the fixed bound — PROVED; the minimality reading is (K0)-gated [REV 2]

**Pin-check finding (the LITUNIT discipline, executed).** The blueprint's
candidate printed supply for OP-B1 was "FGMN §5's key-polynomial minimality
characterizations (Prop 5.7's equivalent forms) — pin TO BE VERIFIED".
Verified this session (fgmn.txt:1769–1780): **Prop 5.7 does NOT match** — it
characterizes when two KEY POLYNOMIALS are µ-equivalent (equal residual
ideals), not the low-degree divisibility block for arbitrary B. REJECTED as
a pin. (FGMN Lemma 5.3, fgmn.txt:1714, characterizes KP(µ)-membership and is
adjacent but also not the block; not consumed.) The blueprint's fallback —
"(s5) is proved directly from the development structure … the same shape as
order 1" — is executed below, from the (GR-B-STR) package alone.

*Inputs: (0), (0′) (incl. the x-class pin in(Φ_i) = x·p^V), (0″)'s slot-
abscissa and support clauses (FGMN Cor 4.4(1)(2) through GRB S3.1/S5.4),
(2′), (V1)_{i+1}, POS-g(iii), (PRES).*

**Lemma LOW-CONST (the low window is x-degree 0 — the graded shadow of the
PARENT key's minimal-degree clause).** For nonzero C ∈ K[x] with
deg C < deg Φ_i: w(C) = e·w_i(C), and

    in(C) = c · p^{w_i(C)}   for some c ∈ K_i^×;

in particular R^gr_{w(C)}(in C) is a nonzero CONSTANT of K_i and in(C) has
x-degree 0.

*Proof.* The Φ_i-development of C is the single slot 0 (deg C < deg Φ_i), so
w(C) = e·w_i(C) + 0·γ_{i+1} definitionally, and e | w(C), whence
s(w(C)) = 0 and ϕ_{w(C)} = p^{w_i(C)} by (12-int) uniqueness. The weight-
read: by (0″)'s slot clause, the κ-th coefficient of R^gr_{w(C)}(in C) is
supported on the abscissa s(w(C)) + κe = κe of the argument's Φ_i-
development, and is nonzero only if that abscissa's digit attains the weight
(Cor 4.4(2); the operator factors through the class — TR-4 — so the support
is read off the representative C). For κ ≥ 1 the abscissa κe ≥ e ≥ 1
exceeds the only populated slot 0: coefficient zero. The read of a nonzero
class is nonzero (Cor 4.4(1)), so it is a nonzero constant c ∈ K_i. Then
(2′): in(C) = ϕ_{w(C)}·c(ȳ) = c·p^{w_i(C)}, of x-degree 0 in (PRES). ∎

*Remark.* At i = 0 this is VAL-g's weight-0 display ("K₀ sits in weight 0 as
classes of coefficient polynomials of content 0", shifted by π̄-powers). At
e = 1 the same proof runs with s ≡ 0 throughout. This lemma is where the
e·g = 1 refinement corner's entire difficulty lives — see the corner display
below.

**Lemma IN-FORM (the general-order VAL-g in-formula).** Let B ∈ K[x] be
nonzero with Φ_i-development B = Σ_t C_tΦ_i^t and minimizing slot set
S := {t : e·w_i(C_t) + tγ_{i+1} = w(B)}. Then:
(a) the slots of S are pairwise congruent mod e (the stride structure);
(b) in(B) = Σ_{t∈S} c_t · p^{w_i(C_t)} · (x p^V)^t, with c_t ∈ K_i^× the
    LOW-CONST constants of the C_t;
(c) the x-degree of in(B) is exactly max S.

*Proof.* (a) For t, t′ ∈ S: e(w_i(C_t) − w_i(C_{t′})) = (t′ − t)γ_{i+1};
mod e this reads 0 ≡ (t′ − t)h (γ_{i+1} = eV + h), and gcd(e, h) = 1 forces
e | t′ − t. (b) Set B′ := Σ_{t∈S} C_tΦ_i^t. Its development is {C_t}_{t∈S},
so w(B′) = w(B) definitionally; and w(B − B′) > w(B): every slot of B − B′
is an off-S slot of B's development, each of development weight > w(B), and
POS-g(iii) bounds the sum from below by their min. Hence in(B) = the class
of B′ at weight w(B) = Σ_{t∈S} (class of C_tΦ_i^t) — the quotient map
P_{w(B)} → P_{w(B)}/P⁺ is additive and each summand lies at weight w(B) —
= Σ_{t∈S} in(C_t)·in(Φ_i)^t by (V1)_{i+1} (initial forms multiply), which is
the display by LOW-CONST and in(Φ_i) = x·p^V. (c) Each summand is the single
(PRES)-basis monomial c_t·x^t·p^{w_i(C_t)+tV}; distinct t ∈ S give distinct
x-exponents, so no cancellation: the top x-exponent is max S. (in(B) ≠ 0 is
definitional anyway.) ∎

**Theorem OP-B1 (the degree block at the fixed bound — PROVED).** For every
nonzero B ∈ K[x] with deg B < e·g·deg Φ_i:  ψ̂_hom ∤ in(B) in gr(w_{i+1}).

*Proof.* The top development slot of B is t_max ≤ ⌊deg B / deg Φ_i⌋ ≤
eg − 1 (monic division degree count), so by IN-FORM(c), the x-degree of
in(B) is ≤ eg − 1 < eg. ψ̂_hom has x-degree exactly eg (HOM-M(c) at the
monic ψ). In the domain (PRES), x-degrees add and every nonzero element has
x-degree ≥ 0, so every nonzero multiple ψ̂_hom·δ has x-degree ≥ eg. Hence
in(B) is not a multiple. ∎

**The minimality identification (REV 2, finding 1).** OP-B1 is stated and
proved at the FIXED bound e·g·deg Φ_i — a statement about ψ̂_hom and B alone,
never mentioning Φ_{i+1}; its proof needed no repair. On the (K0) class the
bound EQUALS deg Φ_{i+1}, so there OP-B1 IS (KPa)'s key-polynomial minimality
block. On the bare (K1)+(K2) class the minimality reading (bound =
deg Φ_{i+1}) is FALSE — §S1.3's padding counterexample: deg Φ_{i+1} can
exceed e·g·deg Φ_i while the discarded conformant representative itself
becomes a low-degree B with ψ̂_hom ∣ in(B). Rev 0's bracket "[= deg Φ_{i+1}
whenever Φ_{i+1} is a degree-conformant lift]" named the needed hypothesis
without imposing it; (K0) now imposes it, and the §S8 alignment shows every
consumer already lives on that class.

**The e·g = 1 corner (the blueprint §4.2(iii) "hardest corner"), displayed
[REV 2, finding 3 — restated AT THE (K0) CLASS].** At a refinement read
(e = g = 1, ψ = z − c̄), (K0) pins deg Φ_{i+1} = deg Φ_i, so the minimality
block quantifies over deg B < deg Φ_i — the LOW WINDOW itself — and
LOW-CONST alone answers: x-degree 0 < 1 = eg. The same proof, no case split;
the corner's difficulty sits entirely inside LOW-CONST's operator inputs,
which is where the order-1 analogue ("top Φ₀-slot < eg = 1 means slot 0
only") also lived. WITHOUT (K0) the corner claim fails exactly like the
general case: pad a conformant refinement key above γ̂ — (K1)+(K2) survive,
the degree exceeds deg Φ_i, and the discarded representative violates
minimality (the pass-1 finding-3 example; same root cause as findings 1–2,
same repair). Rev 0's corner display proved only the fixed-window inequality
and silently read it as the quantified corner claim — the (K0) restatement
closes that gap. Gate instances at e₁g₁ = 1 are included in P1/P2 (clean;
all gate instances are constructed conformant, so the gate never probed the
padding failure).

**Consequences (displayed for the consumers) [REV 2 — (ii)/(iii) recomputed
under the restriction].** (i) With (KPa) (§S3) and (V1)_{i+1}, GD23 Theorem
DEVID now applies at the STATE pair (w_{i+1}, Φ_{i+1}) — (V2b) at the state,
the gate's P2 observable, and the input the §0 ladder's next rung reads.
(ii) The Lean hypothesis row `DevBound` (H1-U8, blueprint §5.3) bounds at
`σ'.Φ.natDegree` — the wired lift's OWN degree; its math derivation is OP-B1
(the fixed bound) PLUS the wired lift's (K0), and the latter is supplied by
the `IsReadLift` shape TOGETHER WITH the parent-key monicity invariant
[REV 3, pass-2 GAP 1]: `kp_step`'s `hΦ : IsNodeLift ν σ'.Φ` pins σ'.Φ to the
D8 stride shape σ.Φ^{e·g} + Σ_{k<g} t_k·σ.Φ^{e·k} (top coefficient literally
1, digits in the `inC` window), monic of degree e·g·deg σ.Φ by this
section's count (§S3.2's (K0) leg) fired at the monic σ.Φ — parent monicity
is the state field `Stage.hmonic`, Lemma PAR-MON's corpus face, not a
consequence of the shape — the two bounds coincide on every `IsNodeLift`
witness at a lawful stage,
so the row's derivation is complete at the wired class; per blueprint §6(b)
the row retires into a proved lemma when this leaf passes its bar. (iii)
OP-B1's own scope is the FIXED bound e·g·deg Φ_i; the identification with
deg Φ_{i+1} is the (K0) HYPOTHESIS on KP-STEP's class, and is B-M2's clause
(a) for the pinned engine lift — a consumer-side supply, never assumed here.

## S6. (s6) (KPb): the key residual — the direct computation, and the convention split

### S6.1 (KPb-F), the FGMN-convention form (PROVED)

*Inputs: (K2) (or (KPa) via S3.1's converse), (2′), (0″)'s j₀ and constant-
term clauses, K_i-linearity of the read. NO properness anywhere: TR-6 is
untouched — the properness proviso enters only (GR-B-KEY)(3)/(4)'s extended-
chain reads, which this note never fires. NO (GR-B-KEY) clause is consumed.*

By (K2), the weight-read of the state key is R^gr_{γ̂}(in Φ_{i+1}) = U₀·ψ.
Its z-order is 0 (ψ(0) ≠ 0, U₀ ≠ 0), so by (0″)'s normalization clause the
side-read agrees: j₀ = 0 and R^gr(in Φ_{i+1}) = U₀·ψ ∼ ψ. At the normalized
key form itself: R^gr_{γ̂}(ψ̂_hom) = ψ EXACTLY (S3.1's converse at u = 1 —
ψ̂_hom = ϕ_{γ̂}·ψ(ȳ) and the read is determined). The K_i^×-tolerance is
K_i-linearity: for c ∈ K_i^×, in both (2′) pairings the read of c·α is
c·(read of α) by uniqueness of the Δ-part against the invertible anchor.
This is exactly "(KPb) is KEY-g's construction" transported: at order 1 the
displayed KEY-g(iii) proof sentence "in(f) = Φ̄₀^{s₀}π̄^{β}hom_{e,h}(R_λ(f))"
at f = Φ₁ reads the residual off the homogenization — here the march
identity does it at every order. ∎

### S6.2 The convention-free (residual-ideal) form

For the GRB S6.3 consumer ((GR-B-KEY)(3) reads (KP_i) at the Max(Δ)-label),
display the ideal form: with R(Φ_{i+1}) := (in(Φ_{i+1})·gr) ∩ Δ,

    R(Φ_{i+1}) = ψ(ȳ)·Δ  —  the maximal ideal of Δ = K_i[ȳ] labeled by ψ.

*Proof.* in(Φ_{i+1})·gr = ψ̂_hom·gr = ψ(ȳ)·gr (associates: U₀ and the
p-power are units). By (PRES)/Thm 4.13's freeness gr = ⊕_γ ϕ_γΔ and ψ(ȳ)
degree-0: ψ(ȳ)·gr = ⊕_γ ϕ_γ·ψ(ȳ)Δ, whose weight-0 piece (ϕ₀ = 1) is
ψ(ȳ)Δ. ∎

### S6.3 The project/GMN-convention face — OPEN, displayed with the gate's τ_g finding

What OL-1(b) literally asserts is R_i(Lift_i(c)) ∼ ψ̂_i with R_i = the
GMN Def-2.21 operator (the z^{t_{r−1}}-twisted reads — gmn.txt:1823–1906,
re-read this session). The tie between the FGMN-convention R^gr (what §S6.1
proves) and the GMN-convention R_i at the state key is EXACTLY the label-tie
clause of the GRB leaf's open lemma **TR-3-ORD-X** (GRB §R2.3: "the labels
tied by each operator's own key residual — clause (3) on each side"). This
note does NOT close it; per GRB rev 4 the naive one-scalar identification of
the two operator systems is REFUTED (GRB S3.2 + gate G2), so the tie is a
genuine open dictionary, priced ONCE at the GRB leaf, never re-priced here.

What the leaf-local gate ADDS at the tested perimeter (order-2, d₀ = g₀ = 1,
336 instances): the GMN-face residual of the twist-aware standard lift is
**τ_g·ψ₁ exactly** (P3, 0 violations), τ_g = the top-stride twist
z₁^{t₁(top)} — the top coefficient CANNOT be twist-inverted because
monicity forces D_g = 1, which is precisely why (KPb) and GMN Def 2.12 carry
"∼" and not equality. Support, not proof — and P3-internal support only
[REV 2, finding 4]: the P3 builder and checker SHARE the twist formula, so
P3 certifies one implementation's self-consistency, not the formula's
correctness against an independent oracle; the formula-independent evidence
at this face is P4's refutation of the naked alternative. The general tie
stays open.

**Who needs which face (the consumption split, displayed):**
* the §0 LADDER needs only (KPa)+(KPirr)+monicity for (V1)_{j+2} (GD23 §5
  V1-tower consumes exactly these) and the Max(Δ)-label form for
  (GR-B-KEY)(3) (GRB S6.3's own display) — BOTH supplied by this note
  ((KPa)/(KPirr)/§S6.2; monicity is a CLAUSE of (K0), so on KP-STEP's class
  it is a hypothesis carried, not re-derived [REV 2]); the ladder does NOT
  wait on the GMN face;
* OL-1(b)'s VERBATIM GMN wording ("Def-2.12 representative") rides the open
  label tie — a residue of member (b), recorded at §S8, distinct from the
  ladder's needs.

## S7. (s7) = B-M2, the OL-1 family clause: statement and inputs (DISPLAYED, wave 3 — NOT proved here)

**B-M2's statement (blueprint §2.2 (s7), REV-2 finding-15 form).** By strong
induction on i (cumulative, the P2-4 reading): for every well-formed chain
datum c of order i ≥ 1: (a) Lift_i(c) is monic in O[x] of degree
m_{i+1} = m_i e_i f_i; (b) Lift_i(c) is a Def-2.12 representative of its
type, R_i(Lift_i(c)) ∼ ψ̂_i; (c) v_{i+1}(Lift_i(c)) = e_i f_i·
v_{i+1}(Lift_{i−1}(c′)), equivalently V_{i+1} = e_i f_i(e_i V_i + h_i).

**What this note supplies to B-M2:** the per-level block (KPa)+(KPirr)+
(KPb-F) for conformant key lifts ((K0)+(K1)+(K2) — KP-STEP, REV 2);
existence at the right degree and monic (SL-EX = Cor 4.12, §S3.3, inside the
class); the weight arithmetic for (c) (γ̂ = e·g·γ_{i+1} at the top monomial;
the K_i-degree f_i enters through deg ψ = g and the residue tower — FGMN
Cor 4.9, already discharged at GRB S5.1); the (SL-dev)+(SL-top) sufficient
condition that a CONSTRUCTION can be checked against, with (SL-top) ⟹ (K0)
proved (§S3.2) — so B-M2's clause (a) reduces to exhibiting the pinned
lift's top stride.

**What B-M2 must add (the honest gap between "some lift" and "THE lift"):**
(i) LIFT CONFORMANCE — the ENGINE's pinned Lift_i (O-2a (C2): "an arbitrary
but fixed function … pinned to the project engine's standard lift") emits
(SL-dev)-conformant data at every order. The gate's P4 makes this a REAL
claim: the NAKED construction (naive coefficient lifts over naked monomial
normalizers) fails (K2) at 120/151 nontrivially-twisted instances and fails
(KPirr) outright at 50 of them — at E_i > 1 the marched normalizers differ
from naked monomials by the level-crossing twist units, invisible at order 1
(E₀ = 1) where KEY-g's classical naked lift is correct. Whatever the
engine's actual construction is, B-M2 must prove IT lands on the marched
side of this fork (or exhibit its twist bookkeeping). (ii) **(K0)-conformance
of the pinned lift** — monicity AND deg = m_{i+1} = e_i·g_i·m_i (= OL-1(a)'s
normalization clause verbatim) — plus integrality (O[x]-coefficients):
construction-side. [REV 2: KP-STEP's (K0) hypothesis is discharged for THE
lift exactly here; NO new obligation was created by the restriction — this
clause was already B-M2(a), and the corpus D8 `IsReadLift` shape hard-wires
the (SL-top) top stride, so on the Lean face it is supplied by the shape
together with the parent invariant `Stage.hmonic` (Lemma PAR-MON [REV 3]).]
(iii) The cumulative "type over F_rep" well-definedness thread (P2-4).
(iv) OL-1(b)'s verbatim GMN face — rides §S6.3's open label tie even after
(i)–(iii).

## S8. Consumers served and the residue statement

**(H1)(b), the door-1 core — where it now stands.**

> **Theorem KP-STEP is PROVED at the degree-conformant class** (this note,
> REV 2): at every state order i+1 ≥ 2, over an arbitrary complete DVR,
> GIVEN the induction state ((V1)_{≤ i+1}, (KP)_{≤ i−1}) and
> (GR-B-STR)_{i+1} in the GRB rev-4 FGMN-convention instantiation, every
> CONFORMANT key lift — (K0) monic of degree e·g·deg Φ_i, plus (K1)+(K2);
> the class Cor 4.12's representatives inhabit and every (SL-dev)+(SL-top)
> standard lift lands in — satisfies the FULL (KP_i) block: (KPa) WITH the
> minimality block at the key's own degree — **OP-B1 is PROVED at the fixed
> bound e·g·deg Φ_i, which (K0) makes deg Φ_{i+1}** — (KPirr), and (KPb) in
> the FGMN/ideal form, with U₀ = 1. On the bare (K1)+(K2) class the
> identity/(KPirr)/(KPb-F) clauses still hold, but the minimality block is
> FALSE there (pass-1 findings 1–3; §S1.3's padding display): the
> restriction is load-bearing — and costs the consumers nothing (the
> alignment below). The §0 ladder's member-(b) rung is thereby supplied in
> exactly the form the ladder consumes ((KPa)+(KPirr)+monicity for
> (V1)_{j+2}, monicity now a CLAUSE of the class; the Max(Δ) label for
> (GR-B-KEY)(3)) — conditional on the GRB leaf's own state.

**The consumer-alignment display (REV 2 — the decisive check: does the
restriction cost anything?).** The consumed classes, each source re-read
this session:

* **ROOT (H1)(d)** (`docs/ROOT_ASSEMBLY_2026-08-02.md` §3.1, the VC8-1
  requantified form): the block is consumed "for the STANDARD key lift
  Φ_{i+1} assigned at such a site" — a PINNED construction, not an arbitrary
  (K1)+(K2) polynomial — and its (d2) is stated at deg B < deg Φ_{i+1}, the
  key's own degree.
* **The standard lift's specification** (OL-1(a) = B-M2(a), O-2a rev 4 §4
  verbatim, displayed at blueprint §2.1): "Lift_i(c) is monic in O[x] of
  degree m_{i+1} = m_i e_i f_i" — (K0)-conformance IS the spec; GMN
  Thm 2.11/Def 2.12's normalization makes every Def-2.12 representative
  monic of degree m_{i+1} BY DEFINITION.
* **The construction's displayed top term** (blueprint §2.2 (s7)(a)):
  "monicity + deg Φ_{i+1} = e_i g_i deg Φ_i are the lift CONSTRUCTION's top
  term — the κ = g monomial is Φ_i^{eg}-shaped with unit normalizer, lower
  κ strictly smaller degree" — (SL-top) verbatim, §S3.2's count the proof
  shape.
* **The engine's compiled lifts** (Lean corpus): `OM/Classifier.liftKey` and
  `OM/LiftKeyExt.liftKeyExt` are literally X^d + (tail of degree < d) with
  monicity THEOREMS on file (`liftKey_monic`, `liftKeyExt_monic`); and the
  D8 predicate `HC2.IsReadLift` (= `IsNodeLift` at the node's read pair,
  `HC2/Defs.lean`) HARD-WIRES Φhat = σ.Φ^{e·g} + Σ_{k<g} t_k·σ.Φ^{e·k} —
  top coefficient literally 1, digits in the `inC` window — so `kp_step`'s
  hypothesis `hΦ : IsNodeLift ν σ'.Φ` already quantifies over
  (SL-top)-shaped lifts ONLY. [REV 3: the `IsReadLift` formula
  SOURCE-CONFIRMED this session at `HC2/Defs.lean:181–191` (pass 2 could
  not read the corpus) — `Φhat = σ.Φ ^ (e' * g) + ∑ k ∈ Finset.range g,
  tt k * σ.Φ ^ (e' * k)`, verbatim the quoted shape; and the shape supplies
  (K0) only together with `Stage.hmonic` (`Moves/Defs.lean:124`), the
  parent invariant of Lemma PAR-MON — carried by every lawful stage, so
  the alignment verdict is unchanged.]
* **The existence supply** (FGMN Cor 4.12, §S3.3): monic of degree
  e_r·f·m_r — inside the class, U₀ = 1.

VERDICT: **ALIGNED.** Every consumer consumes only (K0)-conformant lifts —
by specification (OL-1(a)/Def 2.12), by displayed construction ((s7)(a)), or
by hard-wired shape (D8) — so the restricted theorem serves (H1)(b) and
(H1)(d) at FULL strength, and the pass-1 counterexamples (valuation-above-γ̂
padding; D_g-padding) live OUTSIDE every consumed class. Cost of the
restriction: ZERO NEW obligations — that the pinned engine Lift_i MEETS its
spec is B-M2(a), which was ALREADY §S7's obligation (ii); the restriction
re-files the pass-1 defect into that priced ledger line instead of adding
one.

**Conditionality chain (nothing hidden):** (GR-B-STR) rides the GRB leaf
(rev 4: 0/2 clean hostile passes; N-1 owed; its own inputs (H-CHAIN) +
(KP)_{≤ i−1} — the latter is the LADDER's well-founded recursion, base
KEY-g, not a circularity: this note proves (KP)_i from strictly-lower
inputs). [Freshness check at compose time: GRB pass 4
(`GRB_pass4_2026-08-06.md`, GAPS-ONLY, counter stays 0/2) landed in
parallel; its three wording gaps — the stale S8.2/S8.3 order-2 line, the
(H-CHAIN) j = 0 range start, the S3.1 inner-residual wording — touch no
clause this note consumes ((H-CHAIN) is inherited conditionality only, and
the TR-3 rows are never fired here).] The induction state is the ladder's, proved at the base (GD23) and
recomposed per rung (BRIDGE + MacLane Thm 4.2).

**Residue of member (b) after B-M1** (what remains before the §3.1 (b)-face
flips, per blueprint §6(b)):
1. **B-M2** (wave 3): the OL-1 family assembly — with its four added
   obligations of §S7, of which the CONFORMANCE PACKAGE of the pinned engine
   Lift_i is the substantive one: (K1)+(K2) = obligation (i) (gate-certified
   as a real claim at E_i > 1) and (K0) = obligation (ii) = B-M2's own
   clause (a) [REV 2 — KP-STEP transfers from the class to THE lift exactly
   through this package; the restriction added no line to it];
2. **the hostile-pass bar**: this note has 0/2 clean passes (attack list at
   §S9);
3. **OL-1(b) verbatim** (GMN convention): rides the GRB leaf's open
   TR-3-ORD-X label tie (§S6.3) — a residue of the (a)-leaf, shared, not
   re-priced here;
4. the GRB leaf's own bar + N-1 (inherited conditionality).

**(H1)(d) face served (KEY1-SITE's inputs, blueprint §4.2) [REV 2 —
recomputed under the restricted theorem + the alignment]:** (d1)+(d2) at a
site = (KPa)+(KPirr) at the site's creating read FOR THE SITE'S STANDARD
LIFT. KP-STEP (REV 2) supplies them GIVEN the standard lift's conformance
package — (K0) (= OL-1(a)/B-M2(a): monic of degree m_{i+1}) and (K1)+(K2)
(§S7 obligation (i)) — and under (K0) the ROOT row's (d2) bound
deg B < deg Φ_{i+1} coincides with OP-B1's proved fixed bound e·g·deg Φ_i:
(d2) is served at full strength on the consumed class, INCLUDING the
e·g = 1 refinement corner (§S5's REV-2 corner display discharges the
(s5)-half of obligation (iii); the properness half of that obligation stays
at its §3.3 [T] home, untouched). The conditionality is the SAME conformance
package B-M2 already owed — the recomputation changes the display, not the
ledger. (d3) is NOT served here (GD3-MIN route B — sits on TR-3-ORD-X per
GRB R2.3, as recorded there).

**Lean face (blueprint §5.3) [REV 2]:** H1-U8 `kp_step`'s hypothesis row
`DevBound` bounds at `σ'.Φ.natDegree` — the wired lift's own degree; its
mathematical derivation is §S5 OP-B1 (the fixed bound) PLUS the wired lift's
(K0), supplied by the wiring itself (`hΦ : IsNodeLift` pins the D8 stride
shape, monic of degree e·g·deg σ.Φ — §S5 consequence (ii)); the derivation
is thus complete at the wired class — per §6(b) the row retires
into a proved lemma when this leaf lands its bar (a DevBound-conditional U8b
remains STAGING until then, the finding-11 rule); U8a's displayed
localization-avoidance duty is discharged by §S4.1's VOID note (no
localization exists in the corrected carrier picture); U8's `M`-instantiation
chain (non-vacuity at depth ≥ 2) continues to ride A-M2, unchanged.

## S9. Honesty ledger and attack list

**(i) Proved vs consumed vs open — one list.** PROVED here: HOM-M, S3.1,
S3.2 (incl. the REV-2 (SL-top) ⟹ (K0) count and U₀ = 1), SL-EX-retarget,
(KPirr)-retarget, LOW-CONST, IN-FORM, OP-B1 (at the FIXED bound e·g·deg Φ_i;
its minimality reading holds exactly on the (K0) class — REV 2), (KPb-F),
the ideal form. CONSUMED (never proved here): (V1)_{≤ i+1}, (KP)_{≤ i−1},
(GR-B-STR)_{i+1} clauses (0)/(0′)/(0″)/(1)/(2′) as GRB rev 4 emits them,
POS-g(iii), the §1.2 definitions. OPEN (displayed, owned elsewhere or at
B-M2): lift conformance of the pinned Lift_i; the GMN-convention (KPb) face
(TR-3-ORD-X); B-M2's clauses; the GRB leaf's own residue (TR-3′-GEN,
TR-3-ORD-X, D-ITER, N-1).

**(ii) New pins added by THIS note** (each re-read this session at the
stated extraction lines; a hostile pass should re-check them against the
PDF): FGMN **Cor 4.12** (fgmn.txt:1621), **Thm 4.13** (fgmn.txt:1636),
**Cor 4.14** (fgmn.txt:1663) — plus re-firings of the GRB leaf's standing
pins (Thm 4.2, Thm 4.8, Cor 4.4, Cor 4.9, Lemma 4.1/eq (12), fgmn:1140) and
GMN Defs 2.19–2.21 (gmn.txt:1823–1906, gate implementation only). The three
new pins carry the FGMN scope ("a field equipped with a discrete valuation")
— the arbitrary-DVR claim is inside printed scope, as at every GRB line.

**(iii) Non-consumption audit (checked line-by-line while composing):**
(GR-B-KEY)(3)/(4) — never fired (S6 computes the key residual DIRECTLY from
(2′), the REV-2 finding-2 discipline; properness/TR-6 never enters);
the 𝒟-clause / D-REAL / D-ITER — never fired (no carry-algebra object
appears; CARRY-1 is fired only as (0′) exponent arithmetic);
TR-3′-GEN / TR-3-ORD-X — never fired in any PROOF (S6.3 displays them as the
open GMN-face, consuming nothing);
(2′)'s "∼ R_λ" leg — never fired.
If a hostile pass finds any of these silently consumed, that is a critical
finding against this note.

**(iv) Where to attack (ranked):**
1. **LOW-CONST's operator step**: the claim "the weight-read's κ-th
   coefficient is supported on abscissa s(γ) + κe of the ARGUMENT'S
   Φ_i-development, read off any representative" — check this against the
   retargeted (0″) (FGMN Def 3.16's s_j = s(α) + je_i + Cor 4.4(1)(2) and
   TR-4's factor-through-the-class) — the note's sharpest genuinely-new
   inference; a mis-reading here kills OP-B1.
2. **IN-FORM(b)'s additivity step** (class of a same-weight sum = sum of
   classes; the off-S tail strictly heavier via POS-g(iii)) — elementary but
   load-bearing; check the K[x]-content clearing is not needed (POS-g(iii)
   holds on K[x] by the §1.3 display).
3. **(PRES)'s elimination** K_i[y, p^{±1}][x]/(x^e − p^h y) ≅ K_i[p^{±1}][x]
   — the relation is linear in y, so substitution is an isomorphism;
   check no grading subtlety (weights: a·h + b·e reproduces both sides).
4. **Cor 4.12's retarget constants** (S3.3): E_{i+1}·eg(w_r + λ_r) = eg·
   γ_{i+1} — rides the GRB S2.1/G3(d) identity; check the e_r f m_r ↔
   e·g·deg Φ_i degree line (TR-5's m_r ↔ deg Φ_i).
5. **The gate's partial circularity** (P3 builder/checker share the twist
   formula — declared in the docstring; the non-circular teeth are
   P1/P2/P4) and its perimeter (d₀ = g₀ = 1 only; no equal-characteristic
   towers; N-1 remains the real harness).
6. **hom^m's weight-frame choice**: injectivity used γ_{i+1} ≥ 1 — check
   V = w_i(Φ_i) ≥ 0 at every state (V = γ_i > 0 at i ≥ 1; V = 0 at i = 0).
7. The (SL-dev) shared-unit U₀: one U₀ across κ is load-bearing for (K2)
   (per-κ units would homogenize a DIFFERENT polynomial — exactly the naked-
   lift failure P4 exhibits); check §S3.2 nowhere weakens it.
8. **[REV 2] The restriction's completeness**: hunt for any consumer firing
   KP-STEP on a lift NOT known (K0)-conformant — the §S8 alignment display
   names the consumed classes (ROOT (H1)(d)'s standard lift, OL-1(a)'s spec,
   the D8 `IsReadLift` shape, Cor 4.12's supply); a consumer outside them is
   a critical finding. Check the D8/(s7)(a) shape quotes against
   `lean/LeanUrat/HC2/Defs.lean` (the `IsReadLift` formula) and blueprint
   §2.2; and check the S1.3 remark's U₀ = 1 derivation (slot-eg attainment +
   the x^{eg}-coefficient match) — a wrong U₀ claim would contaminate (KPb-F)
   displays, though nothing downstream consumes U₀ = 1 yet.

**(v) Perimeter honesty.** The gate exercises ℤ_p only, d₀ = g₀ = 1, orders
exactly 2; the THEOREM's claims are ring-free and order-general, riding the
GRB package's own scope. The equal-characteristic and d₀ ≥ 2 exercise is
N-1's (owed at wave 0, A-M2's bar). No claim in this note is supported ONLY
by the gate; every proof step cites its package clause or pin.

---

**Gate record.** `verification/openmath/kpstep_gate.py` (this session;
preregistered predictions in the docstring; exact integer arithmetic):
336 order-2 instances over ℤ₂/ℤ₃/ℤ₅ (d₀ = g₀ = 1; e₀ ∈ {2,3}; e₁g₁ ∈ 1..9
incl. the refinement corner): **P1 0 violations; P2 0 violations (both lift
variants); P3 0 violations (after the τ_g builder repair — the finding is
folded at S0/S6.3); P4: 120/151 nontrivially-twisted naked lifts fail ∼ψ₁,
50 with REDUCIBLE naked residual (first: p = 3, e₀ = 2, h₀ = 1, z₁ = 2,
e₁ = h₁ = 1, ψ₁ = y² + 2y + 2 ↦ (y + 1)²).** GATE PASS — support-only scope
per S0's REV-2 gate-scope paragraph (finite perimeter; P3 internally
circular; no claim of this note rests on the gate).

## REVISION 2 (pass-1 fold) — the degree-conformant restriction + the consumer alignment

Pass 1 (`BM1_pass1_2026-08-06.md`, Codex, hostile, fresh context): **CRITICAL**
— 3 critical findings + 1 justification gap. Root cause in one sentence: the
rev-0 theorem quantified over (K1)+(K2) alone, and initial-form data CANNOT
see high-valuation high-degree padding, so that class can never support a
minimality claim keyed to the lift's own degree — the theorem had to move to
the class the consumers actually consume. All four findings ACCEPTED and
folded IN PLACE (the sites carry [REV 2] tags); nothing was repaired by
weakening a conclusion — the hypotheses were corrected to the consumed class.

**(i) Per-finding disposition.**

| # | pass-1 finding | class | disposition |
|---|---|---|---|
| 1 | (K1)+(K2) does not force deg Φ_{i+1} = e·g·deg Φ_i: pad a Cor-4.12 representative above γ̂ — same initial form, weight-read, side-read, so (K1)+(K2) survive, while the discarded representative is a smaller-degree B whose initial form ψ̂_hom divides; the rev-0 bracket acknowledged the missing hypothesis without imposing it — the full minimality block is FALSE on the quantified class | CRITICAL | ACCEPTED. Theorem RESTATED at the degree-conformant class: hypothesis **(K0)** (monic + deg = e·g·deg Φ_i — exactly Cor 4.12's normalization) ADDED (§S1.3); the padding counterexample DISPLAYED inside the statement's class-scope note; OP-B1 kept verbatim as the FIXED-BOUND block (its statement and proof are Φ_{i+1}-free and needed no repair), with the minimality identification now explicitly (K0)-gated (§S5); U₀ = 1 on the class displayed as a remark |
| 2 | (SL-dev) also does not impose degree or monicity: ψ monic gives ψ_g = 1, but (SL-dev) fixes only in(D_g) — replace D_g by U₀-lift + positive-degree high-valuation junk of degree < deg Φ_i: all (SL-dev) identities survive while deg Φ_{i+1} > e·g·deg Φ_i; S3.2 proves only (K1)+(K2), not the full block claimed for every (SL-dev) lift | CRITICAL | ACCEPTED. The top-normalization clause **(SL-top)** (D_g = 1) ADDED to the companion (§S1.3); (SL-dev)+(SL-top) ⟹ (K0)+(K1)+(K2) PROVED with the explicit degree count (§S3.2); the D_g-padding example displayed as exactly what (SL-top) excludes; alignment — (SL-top) is the blueprint (s7)(a) displayed top term AND hard-wired in the corpus D8 `IsReadLift` shape |
| 3 | the e·g = 1 corner display proves the fixed-window inequality (deg B < deg Φ_i) but not the corner of the theorem AS QUANTIFIED — the same padding breaks refinement-corner minimality without degree conformance | CRITICAL | ACCEPTED. Same root cause, same repair: the corner display RESTATED at the (K0) class, where deg Φ_{i+1} = deg Φ_i makes the low window the FULL minimality window; the without-(K0) failure and rev 0's silent reading both displayed (§S5) |
| 4 | the gate establishes only the reported finite computations (order 2, p ∈ {2,3,5}, d₀ = g₀ = 1, selected reads, random P); P3's builder and checker share the twist formula; P4 refutes the tested universal only; "GATE PASS" cannot evidence the full theorem | GAP | ACCEPTED. S0 gains the gate-scope paragraph (exact perimeter; the gate cannot see (K0) at all — every instance constructed conformant; what P4 does and does not refute); the P3 builder/checker circularity DISPLAYED at S0 and §S6.3 (internal-consistency canary; independent teeth = P1/P2/P4); standing rule re-affirmed: the gate SUPPORTS, never proves — no proof step cites it (§S9(v)) |

**(ii) The consumer-alignment verdict (displayed in full at §S8): ALIGNED —
the restriction costs nothing.** Every consumer consumes only
(K0)-conformant lifts: ROOT (H1)(d) consumes the block for "the STANDARD key
lift" with (d2) at deg B < deg Φ_{i+1}; the standard lift's spec IS monic of
degree m_{i+1} = m_i e_i f_i (OL-1(a) = B-M2(a); GMN Def-2.12
representatives are such by definition); the blueprint (s7)(a) displays
monicity + degree as the construction's top term; the corpus D8 `IsReadLift`
hard-wires the top stride with coefficient 1 (`HC2/Defs.lean`), and the
engine's compiled lifts are X^d + lower with monicity theorems
(`OM/Classifier.liftKey`, `OM/LiftKeyExt.liftKeyExt`); Cor 4.12's supply is
monic of the exact degree. The pass-1 counterexamples live OUTSIDE every
consumed class, and the (K0) discharge for THE pinned lift is B-M2(a) —
already §S7 obligation (ii) before this revision: ZERO new obligations.

**(iii) Per-section class re-verification (each argument re-checked against
the restricted class while folding).** S2 HOM-M: Φ_{i+1}-free — unaffected.
S3.1: consumes (K1)+(K2) only, degree never appears — valid verbatim (it
proves the IDENTITY clause, which survives on the wider class). S3.2: the
(SL-dev) ⟹ (K1)+(K2) computation unchanged; the (K0) leg is new. S3.3:
Cor 4.12's representative is monic of the exact degree — inside the class,
U₀ = 1. S4: about ψ̂_hom alone — unaffected. S5: LOW-CONST/IN-FORM/OP-B1
quantify over B alone, proofs unchanged; only the minimality IDENTIFICATION
and the corner READING were repaired. S6.1/S6.2: consume (K2)/the identity —
degree-free, valid verbatim. NO proof in this note consumes deg Φ_{i+1}
anywhere except the minimality reading: the defect was statement-level (a
quantifier over too wide a class), not proof-level — which is why the
restricted content survives unchanged, as the pass itself noted.

**(iv) Residue delta.** NONE added: the (b)/(d) residue lists (§S8) are
unchanged in content — the conformance package ((K0) = B-M2(a); (K1)+(K2) =
§S7(i)) now carries explicitly what it always owed. NEW small strengthening:
U₀ = 1 on the class (in(Φ_{i+1}) = ψ̂_hom exactly). The Lean `DevBound` row's
derivation is now complete AT THE WIRED CLASS (OP-B1 + the D8 shape's (K0),
§S5 consequence (ii)).

## REVISION 3 (pass-2 fold) — the parent-monicity invariant displayed + the Cor 4.12 quotation repaired

Pass 2 (`BM1_pass2_2026-08-06.md`, Codex, hostile, fresh context):
**GAPS-ONLY** — no critical defect; the pass-1 repairs (minimality at (K0),
(SL-top), the corner, the gate rescope) all judged GENUINE, and (KPa)/(KPirr)
re-derived clean. Two gaps, both documentation/dependency-level, both folded:

**GAP 1 (parent monicity — the wiring claims' silent input).** The
`IsReadLift` shape hard-wires D_g = 1 and the degree count, but yields
MONICITY of Φhat only if σ.Φ is already monic — the note's own §S3.2 count
says "monic (Φ_i monic)" while the Lean-facing conclusions said "supplied by
the wiring". FOLDED: new **Lemma PAR-MON** (§S3.2) displays the invariant
and its tower induction (base seed monic — literally X in the compiled
corpus; each stage's standard lift monic-of-exact-degree at the monic
parent by the stride count, so monicity propagates), with the corpus
carrier SOURCE-CONFIRMED this session (pass 2's sandbox could not read the
corpus): `Stage.hmonic : Φ.Monic` at `lean/LeanUrat/Moves/Defs.lean:124`
(with `hdeg`), base discharge `Polynomial.monic_X`
(`HC1/T1_baseStage.lean:1547`), per-transition re-supply
(`MovesD/R7_ramifiedForge.lean:118` via `HK47.transHyp_gate.hmonic`); and
the `IsReadLift` formula itself re-read verbatim at `HC2/Defs.lean:181–191`
(the note's quote was accurate). All four wiring-claim sites softened to
"the shape TOGETHER WITH `Stage.hmonic`" (§S1.3, §S5 consequence (ii), §S7
obligation (ii), §S8 D8 bullet). No consumer change: every consumed lift
sits at a lawful stage, which carries `hmonic` by construction.

**GAP 2 (the Cor 4.12 quotation).** The rev ≤ 2 proof quote omitted the
printed opening line "Denote α := e_r f (w_r + λ_r).", leaving its α
apparently bound by the statement's "for any α ∈ Γ(µ)" — a quantifier that
is INERT (the displayed conclusion is α-free; the source text itself
carries it, presumably a typo). FOLDED at §S3.3: the proof quote now
includes the binding line, and a quotation-repair display records that both
quotes are verbatim to fgmn.txt:1621–1634, that the statement's quantifier
is vacuous, and that only the α-free existence claim is consumed.

No other findings: the pass's consumer-alignment section confirms the
restriction prices nothing new (B-M2(a) unchanged). Residue delta: NONE —
PAR-MON consumes only the induction state (frames built by lawful reads)
and records where the corpus holds the invariant; no new obligation, pin,
or axiom.

— H1_BM1 rev 0 (unit B-M1), composed 2026-08-06 against GRB_ORDER2 rev 4 and
GD23 rev 4. **REVISION 2, 2026-08-06: pass 1 = `BM1_pass1_2026-08-06.md`,
CRITICAL — all four findings folded per the §REVISION-2 record; the theorem
now lives at the degree-conformant class (K0)+(K1)+(K2) with the consumer
alignment displayed. REVISION 3 (this session): pass 2 =
`BM1_pass2_2026-08-06.md`, GAPS-ONLY — both gaps folded per the §REVISION-3
record (Lemma PAR-MON + the Cor 4.12 quotation repair).**
Consecutive-clean hostile passes: 0/2 (GAPS-ONLY is not clean). Next:
hostile pass 3 on THIS text (attack list §S9(iv); add: check PAR-MON's
base/step against GD23 §1.2's lawful-read construction and the quoted
corpus lines); B-M2 (wave 3) consumes §S7's display.
