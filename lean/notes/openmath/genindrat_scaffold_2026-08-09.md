# GENIND RATIFICATION — PRE-VERDICT SCAFFOLD (orchestrator key, committed BEFORE reading any Codex output)

**Run of record:** session 019fe5fe-39b0-7ff3-806d-4f578bcdb292, gpt-5.6-sol,
effort HIGH, sandbox read-only, launched 10:07 UTC 2026-08-09, log
`/tmp/codex_genindrat_stdin_1786270070.log`, brief
`genindrat_brief_2026-08-09.txt` (192,223 chars; trims disclosed in-brief:
S12–S15 to header+arc+grade, S16 + dated corrections in full). At scaffold
commit time the only Codex output read is the session header + opening
sentence (no findings emitted yet).

## Predictions per charged attack surface

**P-1 (grammar disjointness+completeness; PE8 §4.3 audited).** CLEAN in
substance. My own walk: DRAIN-first is deterministic; hull exactness under
window-hidden points is the chord argument (heights on hull ≤ N−1 < N);
residual unique factorization keys the trichotomy; α (one side, INTEGER
slope, residual (y−z)^m, z ≠ 0 forced by side-endpoint nonvanishing) vs CS
((y−z)^μ on e ≥ 2 side has NON-integer slope h/e, e·deg ψ = e ≥ 2) are
disjoint via the slope; β's "and not α" clause + CS's "some" quantifier
close the rest. Candidate residual nits I could not fully discharge:
(a) e = 1 side with repeated NON-linear factor is CS (e·deg ψ = deg ψ ≥ 2) —
covered; (b) the DEC bucket at multi-side polygons where some side has
e ≥ 2 but separable residual — decided by Ore, in DEC — covered by W-12
S1's taxonomy cite. Weak corner: none found by hand.

**P-2 (GENIND-6(c) fibration proof).** EXPECT A GAP FILING here — the (c)
proof is three lines ("triangular with exact budget floors; surjectivity
with constant q-power fiber is the same S2.2 slot bookkeeping...; J-D0
lifts the count"), and the onto-with-constant-fiber claim at RAGGED stage
windows is machine-exercised only at M6/SS6/M7T. The note's own honesty box
prices exactly this compression (S4.3 honesty box (i)–(iv); GENIND-BOX-2).
Adjudication hinge prepared: a finding that re-states the disclosed
schema-grade compression = GAP-as-disclosed (annex-grade), not CRITICAL;
a finding of a concrete genre where the budget floors are NOT exactly
complementary (locus exponent ≠ fiber+child+stage) would be CRITICAL — my
hand check at M6: 6N−17 = 9 + 2(N−5) + [4(N−5)+4] exactly (and 7 at the
sharp windows), so no such instance exists at the displayed ledger.

**P-3 (well-foundedness).** CLEAN on the order itself: β-children have
μ_l < m strictly (nontrivial partition), so child degree μ_l·d < n; CS
stages are consumed via the [GENIND-H] HYPOTHESIS, not via P(k) — no
circularity; within fixed degree α strictly reduces the window
(N → N−mk, k ≥ 1) and M_l = N − D_l ≤ N − 2 at every β (D_l ≥ μ_l·k_l ≥ 2);
lex (degree, window) is well-founded. Candidate nits preregistered:
(i) M_l can equal 1 (M6 at N = 5: child window 1, instant drain) while the
S1 convention states "window N ≥ 2" — the (IH) package is consumed one
window below its stated convention; trivially repairable (window-1 system
= 1 state, drains, u = total = 1) but textually a convention breach.
(ii) P(k)'s first member is specified as "history-resolved exact menu
(HM3.A-form)" — definition by exemplar; a hostile reader can file a
definitional gap on what "HM3.A-form" binds at general k.

**P-4 (the (A2)/I-E chain).** The I-E identity itself is trivial algebra
(Π T_i − Π(T_i − u_i) expanded) and its consumption at Step 4 is CLEAN.
PREREGISTERED CANDIDATE FINDING (on the PE8 artifact pasted as PASTE G
§4.1, and adjacently on the r1 rationale's gloss): PE8's displayed
non-monotonicity instance is ARITHMETICALLY FALSE — for
f(u₁) = u₁T₂ + T₁u₂ − u₁u₂, ∂f/∂u₁ = T₂ − u₂ ≥ 0 always (u₂ ≤ T₂), so
"not monotone in u₁ once u₂ > T₂ − u₂" is wrong; indeed ΠT − Π(T−u) is
weakly INCREASING in each u_i, so substituting upper bounds û_i ≥ u_i
yields an UPPER BOUND — the "neither a bound nor a value" rationale is
overdrawn as stated. What actually makes (CS-EXACT) load-bearing is the
IDENTITY/partition requirement: (A1) demands EXACT class counts and Step
3's Σ_τ D_τ(N) + r(N) = q^{nN} is an exact partition — bounds propagate
bounds but not the exact-complement member (A2) as DEFINED (an exact
closed form), so the induction's fourth member still fails under
bound-only stage laws. Net: the note's own Step-4 sentence ("an identity
only if those u's are exact") is TRUE; the PE8 §4.1 elaboration's
monotonicity claim is false; conclusion unaffected. If Codex files this:
CONFIRM at the artifact, severity MINOR-to-GAP (verifier-artifact scope).

**P-5 (free hunt).** Exponent arithmetic all re-derived clean by hand this
round: α-locus m(N−1) − k·m(m+1)/2 (slot sum); ghost inequality
j(N−1−mk) + m > 0; bracket GF A₁/(1−A₁) = (Q−1)Q^c x/(1 − Q^{c+1}x);
GENIND-4 budget total 4N−5h−3 = the opening law; M6 fiber exponents 9/7
(6N−17 vs child 2(N−5) + stage 4(N−5)+4 resp. +6); (CS-2) reconciliation
192 = 48+144 = 16·12 at (q,μ″) = (2,2); CS5-V1E2's 4-coordinate exponent
4N−5h−3 and headroom factor q^{N−(5h+1)/2} − 1; criterion necessity
(e·g ≥ e·μ·deg ψ ≥ 4 both cases) and F₂-padding sufficiency. Candidate
Codex filings I expect from disclosed-compression territory: GENIND.C's
β-term vanishing bound (BOX-4 discloses the bookkeeping is displayed, not
expanded per genre); d ≥ 2 machine-uncorroborated (BOX-3); the stage-ring
DVR sentence class (the GENH4-leg F3 species) — GENIND itself never states
the DVR sentence (S7.1 gives the stage data abstractly; GENHN A(i)'s
corrected carrier is pasted with E3 applied), so I predict NO carrier
finding lands ON THIS NOTE.

## Fold discipline prepared

Verdict vocabulary: CONFIRMED / REFUTED / DUPLICATE (of a disclosed box or
a prior adjudicated finding). Severity regrades disclosed both ways.
Outcome space: RATIFIED / RATIFIED-WITH-ANNEX / CHALLENGE (confirmed
GAP-or-worse on a consumed clause ⟹ CHALLENGE). Machine legs planned for
the fold: sympy re-check of any Codex arithmetic; the m = 2 drain closed
forms u(2ℓ) = ℓQ^{2ℓ−1} − (ℓ−1)Q^{2ℓ−2} against the recursion; the I-E
identity symbolic at r = 2, 3; any concrete counter-instance Codex
exhibits re-derived from the note's own formulas before accepting it.
