# GENH4 ANNEX-PASS #3 — HOSTILE VERIFIER REPORT (the STACK ACCEPTANCE attempt; scoped: PE5 + R + R2 + the dated micro-annex)

**VERDICT: CLEAN at annex-stack scope — 0 CRITICAL, 0 GAP, 1 MINOR
(a terminology collision inside ANNEX R whose sentence is true under
its local reading and disambiguated by its own numbers; unconsumed)
+ 3 remarks. Nothing GAP-or-worse survived verification. The annex
stack takes its SECOND clean mark: the annex-acceptance count moves
1/2 → 2/2 — THE STACK IS ACCEPTED. GENH4 (body + annexes) is the
second fully-frozen note, and with the Codex CHALLENGE's repair
queue now hostile-verified end-to-end, the ratification program's
second complete challenge-discharge cycle CLOSES.** The never-walked
surfaces were walked: ANNEX PE5's A1 derivation chain (the S6.3
complement route + original GENH4-7(a)/(b)) held; the cross-note
supply seam (GENH4.C + GENH4-7′(b) → W-12.D r5's σ-LABELING clause)
was read end-to-end in one sitting and MATCHES clause-for-clause,
both characteristics; the three annex machine legs' two genuinely
shared components (one GP factorpadic σ-oracle; one char-p Panayi
module) were identified as the stack's only systematic blind spots
and BOTH were killed by decorrelated fresh instruments (dual-oracle
agreement 86/86 across three σ values at q ∈ {2,5}; a freshly
written GF-tower counter reproducing the consumed clause at q = 4
equal characteristic 27/27 + teeth). All four committed legs re-ran
isolated GREEN with artifacts BIT-IDENTICAL (4/4 — the strongest
machine-integrity result of the arc); every pin, seal, and 3-way
byte-freeze verified, including the micro-rider's corrected pin.

Scope and provenance: target = the FULL ANNEX STACK of
`GENH4_PROOF_2026-08-08.md` at HEAD 6568114 — ANNEX PE5 (lines
1209–1477), ANNEX R (1478–1885), ANNEX R2 (1886–2140), and the
dated micro-annex (2141–2173, commit 6568114, the three pass-2
MINORs). Stack arc: pass #1 NOT CLEAN (a8dec16, 0C+1G+4m) → ANNEX
R2 (760d20f) → pass #2 CLEAN (d6a9ac8, 0C+0G+3m; 1/2) → micro-annex
(6568114). The frozen accepted body's 2/2 is NOT at stake and
nothing here touches it. Fresh context, zero prior stake;
quote-and-classify, fix nothing. Both prior reports read; their
routes NOT re-walked. Body sections read for consumption: S1
(GENH4.C + dictionary), S6.1/S6.2/S6.3, S7, S11; cross-note:
`W12_PROOF_2026-08-08.md` at r5 (be4a1dc/ee17210 = HEAD for that
file) — the (A1) σ-LABELING clause, W12-BOX-7, and the n = 4 firing
row.

## S1. Findings (1 MINOR + 3 remarks; quoted and classified, nothing fixed)

### MINOR-1 — ANNEX R R1.2's "(band-pinned)" label means band-pinned-TO-ZERO, colliding with the same annex's later "band-pinned" = lowest NONZERO band digit; under the later (dominant) usage the sentence is false

**Quoted (R1.2, C3):** "the OLD-gloss (band-pinned) family = 12,288
= HALF — count-refuted as a description of the leaf."
**Quoted (R2, the dominant usage):** "at a band-pinned state (k ≥ 2,
lowest nonzero band digit at height m)" — and identically in (c′),
R2.G1, R2.M1, and both pass reports.

**Failure scenario:** an auditor who reads R1.2 with the annex's own
later definition takes "the (band-pinned) family" = the band-NONZERO
slice and files a contradiction against C4's adjacent sentence ("the
band-nonzero half (12,288) VIOLATES the old hypothesis") — the
old-gloss family is the band-ZERO slice (the old GENH4-7 hypothesis
pins the band digits to zero), which at (2,11,2)'s single band digit
happens to be numerically HALF as well, so the two opposite readings
even share the number 12,288 and the collision is silent.
Classification: MINOR (display) — the local reading ("pinned [to
zero] by the old gloss") is true, C4's own text disambiguates, no
count and no consumer moves, and the machine record (C3/C4,
re-verified bit-identical this pass) is unambiguous. Owed at the
next dated opportunity: one parenthetical, e.g. "(band-pinned to
ZERO — not R2's band-pinned ≠ 0 sense)".

### Remarks (no grade)

* **Remark 1 (surface 1; superseded text).** Original GENH4-7(a)'s
  realizability parenthetical — "u_lift = N is realizable (the
  a₀-component digit at position N …; for k ≥ 2 also the free
  boundary-band a₁ digits)" — carries a FALSE sub-claim beyond the
  two defects already on record (R1.0's hypothesis contradiction;
  pass-1 MINOR-1's N ≤ 4k budget): the band digits sit at heights
  N+1 … N+k−1 and can never realize u_lift = N (the height-N band
  slot is pinned zero — deep₀ sums s > N strictly, R1.0's own
  display); under 7's strict hypothesis the "free" band digits do
  not even exist. No grade: the ENTIRE parenthetical is replaced by
  GENH4-7′(a) (R1.0) and the replacement is inventoried (R2.M4);
  zero consumers of the old form (pass-1 charge 4's grep audit,
  re-confirmed). Recorded so the defect species is on the arc
  record.
* **Remark 2 (the seam's consumption framing).** The stack's
  "consumed clause" framing (7′(b) = THE W-12.D σ-labelling supply
  row) is the CONSERVATIVE reading: W-12.D's own statement routes
  "window-boundary (tail) families" through (A2) drainage (as it
  did for n = 2's SPLIT-TAIL), and GENH4's A3 itself notes SPLTAIL
  vanishes in the per-node limit — if the fired n = 4 menu
  (A2)-routes SPLTAIL, the label is consumed only through GENH4.C's
  wholesale citation. Either way the supply is TRUE and SUFFICIENT
  (verified below); over-claiming consumption raises the hostile
  bar on (b), which is where the machine exercise went. No edit
  owed.
* **Remark 3 (FL3 machine color).** At the fresh (5,6,1) 2w = N
  cell, exactly 3 of 8 pins carried one degenerate '1sq' u = 2w
  digit each (reads {(2,2)} after refine, both oracles) — ≤ 1 per
  pin, exactly R2.M3's corrected geography at odd q; recorded
  ungated, no clause claims these slots.

## S2. Clean charges (each attacked, none broke)

1. **Surface 1a — the S6.3 complement route (PE5 A1's in-note
   derivation, never walked).** The quoted main factor exists
   verbatim in S6.3 ("main factor q^{Σ comp(s): s ∈ A₁, 2s ≥ N}");
   the complement reading is a definitional partition (UND frees A₁
   exactly at slots 2s ≥ N ⟹ the pinned-w SPLTAIL range is
   2w < N); the arithmetic re-derived exactly: A₁ slot heights with
   comp = 2 on [k, N−1], comp = 1 on [N, N+k−1] give
   Σ_{2s≥N} comp(s) = 2(N − ⌈N/2⌉) + k = 2⌊N/2⌋ + k, matching
   S6.3's displayed exponent; the (2,8,1) F → 1024 = 512 + 512
   even-N two-term box prices the partition. Riders 1–3 checked
   against S6.1/S7: at E, 2w < u_floor = 2N ⟺ w < N (matches the
   display); at F, 2w < N; rider 3's F instance u ≥ N > 2w is
   7(b)/7′(b). CLEAN.
2. **Surface 1b — original GENH4-7(b) + the A₀ = 0 corner.** Under
   the (superseded) band-zero hypothesis the proof is exact:
   u ≥ N > 2w at every lift (no letter below N to cancel into; the
   F_{q²}-letter d₁ȳ + d₀ vanishes iff both components do); w
   lift-stable (w ≤ (N−1)/2 < N); S7's 2SIDED F-leg consumes only
   (v(A₁(θ)) = w, v(A₀(θ)) = u > 2w). Corner: φ + A₁ preserves the
   S2.1 genre-F invariants — v(a₀-comp) ≥ w ≥ 2k+1 > 2k keeps
   v(p₀ + a₀) = 2k exact with residue, v(a₁-comp) ≥ w−k > k keeps
   the ψ-pair (char-2 S₁ ≠ 0 kept), both quadratics f = 2, disc ≠ 0
   gives coprimality. CLEAN (7′(b) extends this to the full leaf —
   pass-1 charge 1, not re-walked; machine-extended here at k = 4
   and q = 4 equal char).
3. **Surface 1c — A1's committed refutation tables.** Internal
   conservation exact ((2,8,1): 384+96+24+6 = 510, 510+514 = 1024;
   (2,7,1): 126+2 = 128; the q^{−2}-per-w-step ratios consistent
   with comp-weighted slot loss); machine-tied by the PE5 leg's
   C1–C3 including the disclosed refined-history propagation —
   re-run this pass, bit-identical. CLEAN.
4. **Surface 2 — the cross-note seam, end-to-end in one sitting.**
   W-12.D r5 (A1) σ-LABELING clause (quoted from W12_PROOF at
   ee17210): "each with a splitting type σ_F [r4:] and such that
   EVERY member of every class (F, x) is σ-DECIDED in the S1
   Conventions' pinned lift-certification sense, with forced
   σ = σ_F"; W12-BOX-7 pins "Decided = certified on window digits
   for all disc ≠ 0 lifts". The n = 4 firing row consumes THEOREM
   GENH4.C verbatim + the explicit dictionary ("F: {(2,2)}/
   {(1,2),(1,2)}/{(1,4)}" — the middle entry covering
   2SIDED/SPLITEQ/SPLTAIL) and rules SUPPLIED. Match verified
   clause-for-clause: (i) the quantifier — GENH4.C's "EVERY lift
   with disc ≠ 0" ≡ BOX-7's pinned disc ≠ 0 certification; (ii) the
   label — F-SPLTAIL ↦ {(1,2),(1,2)} identical in S1's dictionary,
   S7's bullet, 7(b), 7′(b), and the firing row's quotation; (iii)
   member-universality — 7′(b)'s "EVERY disc ≠ 0 lift of EVERY
   member — all q^{k−1} band completions" is exactly the clause's
   "EVERY member of every class"; (iv) BOTH characteristics —
   7′(b) states it, (A1) requires characteristic-independence, and
   the machine record now spans char 0 (PARI: q = 2,3,4,5 incl.
   k = 3,4) and char p (F₂[[t]] 96/96 + 4/4; F₄[[t]] 27/27 THIS
   pass); (v) the supply chain GENH4.C F-SPLTAIL leg = S7 bullet =
   7′(b) after R1.1's re-point, whose conclusion is byte-identical
   across 7 → 7′. W-12 is at r5 (ee17210 = that file's HEAD; the
   S11 [r2] re-pin names exactly be4a1dc/ee17210). NO mismatch
   anywhere on the seam. CLEAN (Remark 2 on the framing).
5. **The micro-rider's three lines (charge: verify each).**
   (1) MINOR-1 pin: committed
   `genh4annexpass_fresh_results.json` md5 =
   `7153578c0948d773830f58c193dcb62d` ✓; blob at fed3cff == HEAD ✓;
   single-commit history ✓ — transcription slip confirmed, record
   intact. (2) MINOR-2 disc route: in equal characteristic 2,
   4A₀ = 0 so disc = A₁² with v = 2w < ∞ and A₁ ≠ 0 from the pinned
   finite dv(A₁) = w — one-line-sound; the cited FAeq 4/4 exists in
   the committed pass-2 artifacts (checks.FAeq = 4) at exactly
   (2,7,2)/(2,12,2) ✓. (3) MINOR-3 count: the committed fresh leg
   builds contrastC = 3 band-zero pins at (2,8,2) + 3 at (2,10,2)
   (source lines 426–434) — "6/6 TOTAL, 3 per cell" exact ✓.
   Freeze: the pre-annex prefix (128,564 bytes, md5 `22b766d3…`) is
   byte-identical at 6568114~1 == 6568114 == HEAD, and
   760d20f's FULL file md5 == that prefix md5 — NOTHING changed
   between ANNEX R2 and the micro-annex, and the append is exactly
   the 33 disclosed lines (git stat: +33/−0). CLEAN.
6. **Withdrawal hygiene re-check at HEAD.** Both R2.G1 withdrawn
   displays still occur exactly TWICE (frozen ANNEX R original +
   the quotation), whitespace-normalized ✓; provenance commits
   6cb147b/fed3cff/a8dec16/ca3754d/760d20f/d6a9ac8 all exist ✓.
7. **(c′) spot re-derivation (not a re-walk of pass 2's route).**
   The u₀ trichotomy inequalities re-checked (N odd ≥ 4k+1; N even
   ≥ 4k+2 via both-even; N ≤ 4k via w ≥ 2k+1 — S2.3's stage-initial
   floor); 2w+1 ≥ 4k+3 in-budget; (ii)'s u = 2w realizability at
   N = 2w ≥ 4k+2; R2.M1's band-pinned sweep {max(N,4k+1), …, m}
   consistent with GAP-1's (2,8,2) singleton {9}. All held; the
   fresh FL3 cell machine-instantiates (ii) at a never-touched odd
   q with 15 inert witnesses.

## S3. Machine legs — pins, isolated re-runs, the SYSTEM audit (surface 3)

**Pins at HEAD (2026-08-09).** Scripts/artifacts:
`genh4annex_supp.py` bcb87498 (seal e945734 ✓), output 713a0a3b,
results 37d0650e; `genh4annexr_supp.py` d1d5288f (seal 6272cd4 ✓),
output c1ed4a7f, results f26186fd (== R1.2's displayed pins ✓);
`genh4annexpass_fresh.py` f044c491 (fed3cff ✓), output bf8f94f5,
results 7153578c (the rider-corrected pin ✓);
`genh4annexpass2_fresh.py` ec9706e4 (seal 510c0cf ✓). The 5 md5
pins inside the PE5/R legs' PINS dicts re-verified against committed
files (C0 legs ran green in-sandbox). Byte-freezes 3-way: ANNEX R's
`a584cb3e…` (89,530 B), R2's `863c2160…` (113,738 B, == 760d20f ==
ca3754d), the micro-annex's `22b766d3…` (128,564 B, pre == post ==
HEAD); body r3 `09af7f78…` at 4b0d946 ✓.

**Isolated re-runs** (fresh /tmp sandbox, committed artifacts + the
traced transitive import closure copied in; nothing sealed touched):
ALL FOUR stack legs GREEN, exit 0, and artifacts **BIT-IDENTICAL
4/4** — `genh4annex_supp` ✓, `genh4annexr_supp` ✓,
`genh4annexpass_fresh` ✓, `genh4annexpass2_fresh` ✓ (the first pass
of this arc in which every leg reproduced to the byte).

**The system audit (the charged hunt for shared blind spots).**
Two genuinely shared components found, then killed:
* *Shared char-0 σ-oracle:* every committed leg scores char-0 σ by
  the SAME GP snippet (factorpadic + per-factor
  nfinit([g,[p]])/idealprimedec). Killed: this pass's fresh leg ran
  every char-0 member through TWO oracles — the committed snippet
  verbatim AND a global route (factor over ℚ + idealprimedec per
  global factor; NO factorpadic call — the HE7 precedent) — with
  agreement **86/86** across q = 2 (k = 4 cell) and q = 5 (2w = N
  cell), spanning all three consumed σ values {(1,2),(1,2)} /
  {(2,2)} / {(1,4)}.
* *Shared char-p oracle:* `genh4annexpass2_fresh` IMPORTS
  `genh4annexr_supp`'s count_roots/fp_disc_nonzero (disclosed in
  the pass-2 report as an import) — so every committed
  equal-characteristic read traces to ONE implementation (audited
  line-by-line at pass 1, but single). Killed: FL2's counter is a
  from-scratch generic GF(2^m)-tower Panayi + fraction-free
  prem-gcd separability sharing no code with it, gated by
  known-answer self-tests (3/3), and it reproduces the consumed
  clause's read at a NEVER-EXERCISED ring type (F₄[[t]]) 27/27 with
  teeth 2/2.
* *law_F reader:* NOT a shared blind spot — three independent
  typings already exist across the legs (PE5's C2, R's law_F_fresh,
  pass-1's my_law_F), each tying the sealed reader; member
  construction conventions differ per leg. No shared enumerator.

## S4. Fresh route — `genh4annexpass3_fresh.py`, sealed a118aa6 BEFORE run, GREEN 0 violations exit 0, FIRST run (no smoke; artifacts 95984a9)

Cells chosen to differ from every prior leg:
* **FL1 — (2,19,4): the first k = 4 cell EVER (deepest band: 3 free
  digits at heights 20/21/22).** SPLTAIL(9), 2w = 18 < 19; 3 pins ×
  4 band values × 3 lifts (u = N = 19, deep, A₀ = 0 corner):
  36/36 σ = {(1,2),(1,2)} on BOTH oracles; GENH4-7′(a)'s floor
  (u ≥ 19) and band cap (u ≤ m) clean on the valuation layer;
  wrong-floor teeth (b₀@17 → RAM(17)) 2/2 → {(2,2)} both oracles.
* **FL2 — (4,11,2) over F₄[[t]]: the first equal-characteristic
  non-prime-q exercise of the consumed clause anywhere** (pass 2's
  q = 4 was mixed-char W(F₄)). φ = x² + t²x + ωt⁴, ψ = y² + y + ω
  irreducible over F₄ (Tr = 1), F₁₆ = F₄(Y). 27/27 members
  separable (exact prem-gcd) with (F₁₆[[t]], F₄[[t]]) root counts
  (4, 0) = two conjugate unramified quadratic factors = relative
  σ = {(1,2),(1,2)} over F₄((t)); RAM(9) teeth 2/2 → (0,0)
  (ramified). Fresh instrument, self-tests 3/3 (split quartic /
  unramified-quadratic pair / ramified x² + t).
* **FL3 — (5,6,1): 2w = N at a fresh odd q, dual-oracle.** 8 pin
  letters × 6 lifts = 48 members, oracle agreement 48/48; u = 7
  lifts 8/8 {(1,2),(1,2)}; RAM(5) teeth 8/8 {(2,2)}; **15 INERT
  {(1,4)} witnesses at the separable u = 2w read** — R2.M2's coset
  count machine-witnessed at q = 5 (its first non-ternary odd-q
  contact) — and every non-degenerate u = 2w read in
  {2SIDED, INERT} with ≤ 1 degenerate digit per pin (Remark 3),
  R2.M3's exact geography.

## S5. Grade line

**CLEAN at annex-stack scope: 0 CRITICAL + 0 GAP + 1 MINOR
(MINOR-1, the R1.2 "(band-pinned)" terminology collision —
display-layer, locally true, unconsumed) + 3 remarks. Nothing
GAP-or-worse survived verification: the annex-acceptance count
moves 1/2 → 2/2 — THE ANNEX STACK IS ACCEPTED. GENH4 is FULLY
FROZEN (body ACCEPTED 2/2 at PE5 + annex stack ACCEPTED 2/2 at this
pass), the second note through both bars after HE7; the Codex
CHALLENGE (CODEX_GENH4RAT_2026-08-09.md, 5 findings) is fully
discharged through hostile verification — the ratification
program's second complete challenge-discharge cycle CLOSES.**
Conditionality unchanged: exactly the S11 arc-grade stack plus the
GENHN.A(i) transport erratum QUEUED to the live GENHN arc (R3).
Owed at the next dated opportunity (does NOT block acceptance):
MINOR-1's one-parenthetical disambiguation. Report artifacts:
`verification/openmath/genh4annexpass3_fresh.py` + output/results
(sealed a118aa6 before run; artifacts 95984a9); isolated re-run
sandbox in /tmp (session-local).
