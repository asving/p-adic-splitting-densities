# GENIND pass PE5 — HOSTILE VERIFICATION REPORT (fresh context, fifth arc pass; the r4 full-sweep — S14 — attacked for the first time)

**VERDICT: UNREFUTED, NOT CLEAN. 0 CRITICAL, 1 GAP, 2 MINOR.** The
2-clean count does NOT start (stays 0/2; an r5 is owed, then the
count begins afresh). No count law, no theorem clause, no sealed or
committed number, and no clause of the r4's mathematics was refuted —
my fresh route (§7; disjoint from all six prior routes) confirmed the
capped-window machinery at **n = 9** (the corpus's first n ≥ 9
evidence, a genre carrying ALL FOUR of LEMMA GENIND-6's block types
at once) and made the r4 trichotomy's never-realized SAME-SIDE
co-children leg machine-real (59,049 exhaustive), plus a systematic
steeper-decided-piece scan (double-piece and piece-sandwich genres,
exhaustive). The one GAP is the arc's record-integrity defect class
(PE2-G2 → PE3-G1 → PE4-G1 → **PE5-G1**), for the FOURTH consecutive
round, this time inside the full-sweep record that was built as the
structural answer to that class: S14's universal-coverage sentences
("a claim-by-claim audit of EVERY record/regime/inventory sentence…",
"every inventory sentence now says only what the repo can verify")
are refuted by a concrete witness — an inventory sentence in S4.3
that carries a FALSE entry-law formula, matches NONE of S14's
disclosed grep patterns (the hyphen in "first-visible" evades the
"first visible" target), and was never audited.

**Target:** `lean/notes/openmath/GENIND_PROOF_2026-08-08.md` at HEAD
`0003153` (file last touched at r4 `4a482b5`/`a2000fb`, 2026-08-09
05:34:07/05:38:10; arc: seal `c3212cc` → PE1 `4259a93` → r1 `66200f2`
→ PE2 `50db400` → r2 `2abf419`/`ad6f31d`/`52fb6ff` → PE3 `f89b565` →
r3 `d3ef66d` → PE4 `51bd1bc`/`ade9bac` → r4). **Role:** hostile
verifier, fresh context, zero prior stake; I fix nothing;
quote-and-classify only. All four prior reports read first; none of
their routes re-walked (priors: seal battery n ≤ 5 / PE1
symbolic+exact n ≤ 5 / PE2 n = 6 census + first n = 6 PARI / r2 supp
M6 fibration 2,128,163 / PE3 M7T + TWOCHILD + SS6 705,133 / PE4
PSTEEP + THREECHILD + N8MIX 6,507).

---

## 1. FINDINGS

### PE5-G1 [GAP] — S14's universal-coverage claims are FALSE by a concrete witness: an S4.3 inventory sentence with a false formula sits outside every disclosed grep pattern and was never audited — the record-integrity defect class reproduced inside the full-sweep record built to end it.

S14 makes two universal claims about sweep leg 2:

> "(2) a claim-by-claim audit of **every record/regime/inventory
> sentence, each machine- or artifact-checked before being kept**"
> (S14, the Arc paragraph)

> "this round changed PROSE: one proof step now derives what its
> statement says, and **every inventory sentence now says only what
> the repo can verify**." (S14, closing paragraph)

The witness, S4.3 (the mixed-genre first-visibility inventory,
r2-era text, line ~528):

> "The **first-visible** genre (all others sit at deeper visibility
> floors — k ≥ 2, stage-steeper arrangements, and the f-first
> ψ²-block variants **enter at v(a₀) = 2k + 2h ≥ 6, i.e. N ≥ 7**) is
> PE2's [M6]"

Three defects compound:

* **The formula is FALSE for its last-listed subject.** For the two
  E-stage variants the formula is exactly right (child 2k + E-side
  drop 4·(h/2) = 2h; k ≥ 2, h = 1 gives 6; k = 1, h ≥ 3 gives 8 —
  both ≥ 6 ✓). But an f-first ψ²-block (e = 1, deg ψ = 2, μ = 2)
  spans 4 abscissas at integer slope h, so its drop is **4h**, not
  2h: the same-side variant (k = h forced; residual
  (y−w)²ψ(y)² on one slope-k side) enters at v(a₀) = **6k** (= 6 at
  k = 1 — the N ≥ 7 minimum), and the distinct-side variants at
  **2k + 4h** (≥ 8). At the f-first minimum the displayed chain
  "= 2k + 2h ≥ 6" evaluates to 4 ≥ 6 — false. (The CONCLUSION — all
  non-M6 mixed genres enter at v(a₀) ≥ 6, i.e. N ≥ 7, M6 uniquely
  first-visible at N = 5 — is TRUE: I re-enumerated the complete
  m = 6 mixed grammar: child+E distinct-side 2k+2h with (1,1) = M6
  the unique value < 6; child+F same-side 6k ≥ 6; child+F
  distinct-side 2k+4h ≥ 8; stage-steeper 2k+2h ≥ 8; no other
  arrangement fits the 2+4 = 6 abscissa budget.)
* **The sweep could not see it.** S14 leg 2's disclosed grep targets
  are "every sealed", "all sealed", "true at every", "invisible",
  "first live", "first visible", "beyond every", "never", "no
  prior". The witness sentence spells the key phrase
  **"first-visible"** (hyphenated) and its claim verb is "enter at":
  no disclosed pattern matches it, it appears in none of C1–C14, and
  its formula is refuted by the repo (my FR-route enumeration; also
  by hand from S5.3's own CS4-F heights (4k, 3k, 2k, k) — drop 4k).
* **Therefore both quoted S14 sentences are false as written** — the
  first because a record/inventory sentence went unchecked, the
  second because an inventory sentence says something the repo
  refutes. This is the arc's established GAP class (a repo-checkable
  factual sentence in a repair record, false against the repo:
  PE2-G2, PE3-G1, PE4-G1), reproduced in its FOURTH consecutive
  repair — and this time the repair WAS the full sweep, so the
  false sentence is the sweep's own coverage claim.

**What survives (verified, not assumed):** sweep leg 1 (the
window-site inventory) — my independent grep sweep over the whole
note found NO window-formula site outside S14's 15 classes and no
uncapped survivor; leg 3 (tag-set consistency) — S11 byte-identical
since the verdict commit 4984004 (extraction diff = the appended S12
header only), grade lines monotone 0/2, every [rN] tag names its
finding; the 14 audited claim groups C1–C14 themselves (my spot
checks, §5). The defect is the JUMP from "every grep hit checked"
(true) to "every sentence checked / every sentence repo-verified"
(false at the witness). **Failure scenario:** a reader executing
S14's own program — trusting "every inventory sentence now says only
what the repo can verify" — consumes the f-first entry law 2k + 2h
to enumerate mixed-genre visibility floors at n = 6 (e.g. to design
a battery window or an (A2) boundary inventory) and places the
same-side f-first genre at v(a₀) = 4, N = 5 — inside windows where
it does not exist (its true floor is 6/N = 7); the M6-uniqueness
conclusion they would ALSO derive contradicts it, surfacing the
inconsistency only if they cross-check. Class: **GAP** (record
integrity; repair = one formula + one scope sentence on S14's two
universal claims, or the sweep re-run at sentence granularity).

### PE5-m1 [MINOR] — The witness display itself: "the f-first ψ²-block variants enter at v(a₀) = 2k + 2h ≥ 6" (S4.3).

The false formula quoted and adjudicated inside PE5-G1, filed
separately as the in-place display defect: the f-first variants
enter at v(a₀) = 6k (same-side, k = h) resp. 2k + 4h
(distinct-side), never 2k + 2h; the bound ≥ 6 and the conclusion
N ≥ 7 are correct. Consequence-free for every count law and every
machine leg (M6 is the only mixed genre any leg prices; the S5.3
CS4-F table and the criterion are untouched). Class: MINOR (display
arithmetic in a parenthetical; conclusion true).

### PE5-m2 [MINOR] — The r4-corrected step (1) trichotomy's same-side enumeration, consumed VERBATIM by GENIND-6(a), omits same-side COMPOSITE blocks (live at n = 6, N ≥ 7).

S4.1 step (1) [r4], verbatim:

> "same-side co-blocks appear at their SHIFTED residues z′ − ẑ_l ≠ 0
> (**decided pieces as separable factors, same-side co-CHILDREN as
> repeated factors (ȳ − (z′ − ẑ_l))^{μ}**)"

Within LEMMA GENIND-3's stated scope ("repeated factors all linear
on e = 1 sides") this two-case enumeration is complete — pieces and
co-children are the only same-side co-block types. But GENIND-6(a)
consumes steps (1)–(2) "VERBATIM" for mixed frames, where a
composite block can share the extracted child's side: exactly the
same-side f-first variant of PE5-m1 (residual (y−w)²ψ(y)² on one
side, first visible N = 7). There the co-block's image in the scaled
recentered reduction is **ψ(ȳ + ẑ)^{μ_b}** — a repeated NON-linear
factor at shifted residues — neither of the parenthetical's two
cases. The main clause ("shifted residues z′ − ẑ_l ≠ 0") and the
coprimality CONCLUSION remain true (ψ irreducible of degree ≥ 2 over
F_Q has no F_Q-root, so ψ(ẑ) ≠ 0), and GENIND-6(a)'s own proof
carries the generic form ("co-blocks on the same side have distinct
residual roots/factors"), so nothing false is asserted and no
consumer breaks; the enumeration is merely under-inclusive at the
consumption site — the same shape as PE4-G2's parenthetical finding,
one severity class down because here the covering gloss exists in
the consumer. My fresh route machine-realizes the same-side
mechanics one type over (SAMESIDE: same-side co-CHILDREN, §7 FR-B,
59,049 states — the (ȳ − (z′ − ẑ))^μ case exact on every member);
the same-side composite case remains machine-unrealized corpus-wide.
Class: MINOR (display completeness under verbatim consumption;
conclusion true and separately proved in the consumer).

### Remarks (not findings; as-of observations at verdict time)

* **R1 (the post-r4 GENH4 state).** GENIND r4 landed 05:34/05:38;
  the Codex GENH4 ratification VERDICT (CHALLENGE, `5c8d2fd`) landed
  06:00:22 — AFTER r4, so S9.3's [r4] sentence "no adjudicated
  unrepaired finding at the pin" and S14's C14 were TRUE at commit
  time (the commit-time rule COMPLIED with, second round running).
  As of MY verdict time GENH4 carries the CHALLENGE's adjudicated,
  not-yet-repaired findings (F2 GAP on ANNEX-LEMMA GENH4-7; F3
  display-scope with a GENHN.A(i) transport erratum owed; F4 scoped
  to a sharpness clause; F1/F5 MINOR). NONE touch what GENIND
  consumes: the (CS-EXACT) discharge cites THEOREM GENH4.A/B +
  GENH4-1F + the comp-weighted census (all untouched per the
  adjudication — "eleven densities untouched"; GENH4-7 is
  post-acceptance annex material GENIND never cites), and GENIND
  consumes GENHN.A(iv)(v)/GENHN-3/S6.2, not A(i). The NEXT dated
  append owes the CHALLENGE disclosure under the note's own standing
  rule; not a defect today.
* **R2 (other suppliers).** GENHN's note HEAD is still the pin
  850e77e (git-log checked at verdict time). HE7/HE6 (ACCEPTED,
  ANNEX R landing this wave): consumed NOWHERE in GENIND — grep
  finds zero HE7/HE6 references in the note; nothing to disclose.
* **R3 (the r4 mathematics held).** The rewritten step (1) was
  re-derived from scratch and survived (§3); the trichotomy's three
  legs are each now machine-real (steeper-collapse: PSTEEP/THREECHILD
  since PE4 + my STEEPSCAN; shallower-degree-drop: every mixed
  ledger; same-side-shifted: my SAMESIDE — corpus-first).

## 2. CHARGE 1 — S14 ITSELF (the never-attacked full sweep)

### 2a. Window-site spot checks (7 of 15, against the artifacts)

* **Site 2** (S1 GENIND.A(II) "the SAME (m, d) system at window
  N − mk"): α-CORRECT confirmed — at an α event the WHOLE cluster
  recenters (no co-blocks), D = m·k exactly, the cap degenerate. ✓
* **Site 3** (S3 GENIND-2: σ_k mod π^{N−mk}, ghost zones
  {≥ N − jk}): α-CORRECT confirmed, same reason; the ghost/consulted
  pairing re-checked (j(N−1−mk) + m > 0). ✓
* **Site 9** (M6 ledger D = min(4, 2+2, 6) = 4): hand-recheck — hull
  (0,4)–(2,2)–(6,0), j = 0: 4, j = 2: 2+2, j = 6: 0+6; min 4;
  regime label (stage 1/2 SHALLOWER than child 1) correct. ✓
* **Site 10** (SS6 D = 2·1 + 4·min(3/2, 1) = 6, uncapped 8): ✓.
* **Site 11** (S5.2 u_{m,d}(N−mk) in the α-term): α-CORRECT ✓.
* **Site 14** (S10 runner description vs code): `extract_child` in
  the sealed runner (md5 e7ca150b) computes
  `D = min over j ≤ m of (v(a_j) + j·k)` with the monic j = m term —
  literally the capped value; S10's "(D = weighted min) + monic
  Hensel extraction at window W − D" matches the code. ✓
* **Site 7** ("checked: the (a) statement, the (a) proof gloss, and
  step (1) display the same D_l"): verified — all three sites carry
  the identical capped Σ/min_j formula. ✓

**Miss-hunt (leg 1):** my own independent grep sweep ("N − m·k",
"N−mk", "slope-mass", "window-(N", "min_j", "weighted min", "W − D"
over the whole note) surfaced NO window-formula site outside S14's 15
classes; S2 (LEMMA GENIND-1) displays no window formula (checked —
its (ii) is the node D_k, heights not windows). **"Uncapped survivors:
NONE" is confirmed.** Leg 1 CLEAN.

### 2b. Record-claim spot checks (7 of 14, against the artifacts)

* **C1**: sealed ROWS = {3: [(2,5),(3,4)], 4: […,(2,6),…]} read from
  the runner source ✓; PSTEEP3 visibility v(a₀) = 4 ≤ N−1 = 4 ✓.
* **C6** (mixed first live n = 6): degree budget 2 + 4 re-derived ✓.
* **C7** (SS6 N ≥ 9; ≥ 2 stage blocks ≥ 8 abscissas): v(a₀) =
  4·(3/2) + 2·1 = 8 ⟹ N ≥ 9 ✓; 2 × 4 abscissas ✓.
* **C8** (V4E2 first visible N = 8): v₀ = v₄ + 2h, v₄ ≥ 1,
  h ≥ 2v₄+1 ⟹ v₀ ≥ 7 ⟹ N ≥ 8 ✓; corroborated by P-3's
  predicted-absent at (·,2,5,n5), reproduced in my isolated re-run. ✓
* **C10** (n ≤ 3 never consulted [W12-H]): the m ≤ 3 multiplicity-
  monotonicity argument re-checked + my re-run's four n = 3 control
  rows print 0 CS (GT-CRIT 22/0). ✓
* **C12** (S11 = the artifacts): my isolated re-run of the sealed
  battery reproduced 163,536/0 with teeth 12/12/4/24; committed
  output/results md5s match S11's citations (c157dc31… /
  366f2143…). ✓
* **C14** (as-of pins): git-log timestamps verified — r4 commits
  05:34:07/05:38:10 PRECEDE the Codex GENH4 CHALLENGE 06:00:22, so
  "no adjudicated unrepaired finding at the pin" was true at commit
  time; GENHN HEAD = 850e77e still. ✓ (Post-r4 state: Remark R1.)

**Miss-hunt (leg 2):** executed the disclosed nine grep patterns
myself, then swept for inventory sentences OUTSIDE them ("first-
visible", "enter(s) at", "always", "the only", "corpus's first").
ONE hit at variance with the repo: the S4.3 first-visible sentence =
**PE5-G1/PE5-m1**. Everything else at variance-risk resolved TRUE
(M6 box "visible iff 4 ≤ N−1" ✓; "q = 3 slice is the only odd-q
machine point of this leg" ✓ — PE3/PE4/PE5 legs are cited as
verifier artifacts, not this note's; S4.3 "PE2 constructed the first
live instance" ✓).

**Leg 3 (tag-set):** S11 extracted at the verdict commit 4984004 and
at HEAD — byte-identical (diff = the later-appended S12 header only):
"S11 stays frozen" CONFIRMED. Grade lines S12/S13/S14 all 0/2,
monotone; each [rN] record names its findings and its successor's
catches; the eight md5 pins listed in S14's Arc paragraph all
re-verified (§6). CLEAN except as charged in PE5-G1.

## 3. CHARGE 2 — THE r4 REWRITE OF STEP (1), RE-DERIVED

* **Per-factor content.** f(π^k(y + ẑ)) = Π(π^k y + (ẑπ^k − ρ));
  content of a linear π^k y + c is min(k, v(c)) — the displayed
  min(v(ρ − ẑ_l π^{k_l}), k_l) ✓. Summing: child roots v ≥ k+1 → k
  each (μ_l k_l); same-side co-blocks v = k → k = min(s_b, k_l);
  steeper v(ρ) = s_b > k → v(ρ − ẑπ^k) = k = min(s_b, k_l) — the
  cap; shallower → s_b. Total D_l = μ_l k_l + Σ deg_b·min(s_b, k_l)
  ✓. Recentering is a unimodular triangular substitution over O, so
  content(f(π^k(y+ẑ))) = content(f(π^k y)) = min_j(v(a_j) + jk) —
  the "equivalently" clause ✓ (independent of PE4's root-sum route).
* **Ragged precisions.** Coefficient j of f(π^k(y+ẑ)) =
  Σ_{i≥j} C(i,j) ẑ^{i−j} π^{ik} a_i; the loosest term is i = j at
  precision N + jk ✓; dividing by π^{D_l} gives N + jk − D_l,
  minimized at j = 0 (k > 0) ✓ "binding at j = 0"; all coefficients
  known at least mod π^{N−D_l} ⟹ a window-M_l frame ✓; M_l ≥ 1 on
  any visible genre (D_l ≤ v(a₀) ≤ N−1) ✓.
* **The floor framing.** D_l ≤ m·k_l via the monic j = m term ✓;
  equality iff no shallower co-block ✓ ("undersells strictly at
  every genre with a shallower co-block" ✓). Consumer sweep: no site
  consumes N − m·k_l as a β/mixed window (my grep, §2a); every
  "N − mk" site is α-side where D = mk exactly. CONSISTENT.
* **The trichotomy, leg by leg on constructed instances:**
  (i) same-side SHIFTED residues, co-children repeated
  (ȳ − (z′−ẑ))^μ — machine-real for the first time anywhere at my
  SAMESIDE (§7 FR-B): on all 59,049 states, extracting the z = 1
  child leaves cofactor reduction (ȳ−2)² and its ẑ-shift is
  (ȳ−1)² — the displayed form exactly, both letters; (ii) steeper
  co-blocks collapse (their letters leave the reduction) — the
  runner's unrecentered mirror (collapse to ȳ, residue 0) verified
  at STEEPSCAN/NINEMIX via the forced child-multiplicity and the
  capped D on 49,600+ exhaustive states; (iii) shallower co-blocks
  leave by degree drop — NINEMIX child-B extraction (stage drops
  reduction degree 9 → 5) and every M6/M7T-era ledger. One
  under-inclusion at the consumption site filed as **PE5-m2**
  (same-side COMPOSITE blocks under GENIND-6(a)'s VERBATIM import).

## 4. CHARGE 3 — GENIND-6(a) END-TO-END WITH THE CORRECTED STEP

The (a) STATEMENT, the (a) proof gloss, and step (1) now display the
same capped D_l (site-7 check ✓). The (a) proof's coprimality chain
re-read against the corrected reduction picture: child ȳ^{μ_l}
coprime to [same-side blocks at shifted residues ≠ 0] × [steeper
blocks at −ẑ_l ≠ 0] × [shallower blocks gone] ✓ — including the
same-side composite case via its own "distinct residual
roots/factors" clause (the step-(1) parenthetical's omission is
PE5-m2, display only). The iterated extraction ("iterating over the
children") is corroborated at NINEMIX: both children of a 4-block
frame extract at their own capped windows (2, 5) with the child-B
box bijective onto the FULL window-5 space — impossible if iteration
degraded the second window. Clause (d)'s conditionality sentence
unchanged and accurate (stage clauses consume [GENIND-H] only).

## 5. CHARGE 4 — CONDITIONALITY/RECORD STATE AT HEAD (as-of rule)

S9.3 checked bullet-by-bullet at verdict time: GENHN @ 850e77e = its
note HEAD ✓ (the Codex-GENH4 F3 note "GENHN.A(i) transport erratum
owed" touches a clause GENIND does not consume — A(iv)/(v)/GENHN-3/
S6.2 only); GENH4 @ 4b0d946 + annex baf05ed — the [r4] dated append
accurate at commit time, superseded 22 minutes later by the Codex
CHALLENGE (Remark R1: dated-append material for r5, no consumed
clause touched); HE7/HE6 correctly absent (grep: zero references);
W-11/W-12/HEX3/HMENU3/J-D0/GRTJC pins unmoved. The one-line summary
("no axiom, no weld face inside any count law; grade = composition
0/2") consistent with BOX-5 + the header, which correctly say the
clean count has NOT started and PE5 attempts the first clean.

## 6. MACHINE LEG — ALL FOUR COMMITTED RUNNERS REPRODUCED; PINS INTACT

* **md5 pins (all 8 + 5 suppliers):** sealed runner
  `genind_checks.py` = e7ca150b SIX-WAY identical (worktree = HEAD
  0003153 = r4 a2000fb = r4 4a482b5 = r3 d3ef66d = r2 52fb6ff = seal
  c3212cc); r2 supp triple 7335fc16/63b7413a/6c93f29c ✓; PE3 fresh
  triple fce9cb08/2075dd28/360eb239 ✓; PE4 fresh triple
  aa2b9af8/1a29a9c1/e5493312 ✓; GT-PIN suppliers 5/5 match the
  runner's PINS dict; committed sealed artifacts match S11's md5s.
* **Isolated re-runs** (`/tmp/genind_pe5_run`; runners + the five
  pinned suppliers copied; committed artifacts never touched):
  sealed battery **GREEN 163,536 / 0** (164.3 s; results JSON
  identical mod `elapsed_s`; output diff = timing lines only); r2
  supp **GREEN 2,128,163 / 0** (JSON identical mod elapsed); PE3
  fresh **GREEN 705,133 / 0** (leaf diff = elapsed only: 147.1 vs
  147.4); PE4 fresh **GREEN 6,507 / 0** (JSON identical mod
  elapsed). The second pass ever to re-run all four committed legs.
* **Tautology audit (my angle).** My fresh legs consume the sealed
  `extract_child`, so the window adjudications keep PE4's
  three-source structure and add a fourth: (i) the hand value = the
  statement's Σ-formula (derived in §3 and in the leg's docstring
  before the run); (ii) the extractor computes min_j(v + jk) — tied
  to (i) only through the content identity I re-derived
  independently; (iii) consumption-level counts distinguish capped
  from uncapped regardless of extractor internals (a window-1 child
  space is ONE class: my onto counts 256/16/64/4/81 and DEC/DRAIN
  splits are impossible under the uncapped reading); (iv) PARI
  factorpadic valuation partitions and σ-shapes never see the
  reader. The exhaustive-locus legs also pin the LOCUS SIZES
  (59,049/128/16,384/32,768) from S2.2 slot counts derived by hand —
  a generator-free cross-check on the pinned-state enumerations.
  Non-tautological.

## 7. FRESH ROUTE — n = 9 + the same-side leg + the steeper-piece scan (disclosed; disjoint from all six prior routes)

Runner `verification/openmath/genind_pe5_fresh.py` (committed with
output + results JSON; md5 6ef3132e / a0452521 / bdc04c4d); sealed
runner imported read-only, md5-asserted; ran in the /tmp sandbox;
single run, GREEN first time, no smoke. **ALL predictions
hand-derived in the docstring BEFORE the run. GREEN: 109,146 checks,
0 violations, 28.5 s.**

* **FR-A NINEMIX — the corpus's first n ≥ 9 evidence, and the first
  genre with ALL FOUR GENIND-6 block types at once.** (q, 9, 1),
  hull (0,11)–(1,8)–(3,4)–(5,2)–(9,0), N = 12, both characteristics:
  decided piece @ slope 3 + child A (2,1) @ slope 2 + child B (2,1)
  @ slope 1 + CS4-E(1) stage @ slope 1/2. D_B = 2 + min(3,1) +
  2·min(2,1) + 4·min(1/2,1) = 7 is the first D_l containing a capped
  steeper PIECE, a capped steeper CO-CHILD, and an own-slope
  shallower STAGE simultaneously; D_A = 10. Windows (W_A, W_B) =
  **(2, 5)** exact on 200 (q=2) + 120 (q=3) product-constructed
  members (uncapped predicts (1,1)); every member classifies CS at
  the single reader key ('CSOTHER', 9, (5,9), 2, 1, 2, 1) (read-off
  disclosed, gated on uniformity only); driving boxes
  (a₃ h5–8 × a₄ h4–7) → **BIJECTION onto all 256** window-5 child-B
  classes at q = 2 and (a₃ h5,6 × a₄ h4,5) → onto all 81 truncated
  classes at q = 3 (per-height image pairs (ȳ³, ȳ⁴) mod (ȳ−z)² =
  (1,1),(1,0) at char 2 vs (1,0),(1,1) at char 3 — the J-D0
  degeneration swapping the drivers, det ≠ 0 both); **PARI
  (mechanism-free): 15 + 10 disc ≠ 0 lifts, root-valuation partition
  {3:1, 2:2, 1:2, 1/2:4} exact** (wild p = 2 at degree 9); TOOTH:
  corrupting the (3,4) vertex pin exits the genre 10/10.
* **FR-B SAMESIDE — the r4 trichotomy's same-side co-children leg,
  corpus-first.** (3, 4-cluster, 6): one side (0,4)–(4,0) slope 1,
  residual (y−1)²(y−2)² (q = 3 FORCED — q = 2 cannot carry two
  distinct unit residues, and every sealed q ≥ 3 row sits below the
  v(a₀) = 4 floor: the genre was processable by NO sealed row).
  Exhaustive **59,049** pinned states: both children extract at
  Wc = 2 (capped = uncapped — the same-slope leg); joint child-class
  map **ONTO 9 × 9 with constant fiber 729**; on EVERY member the
  cofactor of the z = 1 extraction reduces to (ȳ−2)² and shifts to
  (ȳ−1)² — the displayed (ȳ − (z′ − ẑ))^μ made literal; verdict
  split **DEC 26,244 / DRAIN 32,805** exactly as derived from the
  product of the (2,1,2) child menus (fractions (6/9)² vs
  complement); **PARI: 25 fully-DEC members → exactly two ramified
  quadratics each (odd disc valuation), σ = {(2,1),(2,1)}**.
* **FR-C STEEPSCAN — systematic steeper-decided-piece coverage
  beyond PE4's two cells.** C1 (2,6,n3), s = 3 piece over (2,1)
  child: exhaustive 128, Wc = 3 (uncapped 1), ONTO 16 fiber 8, DEC
  80 / DRAIN 48 (= u₂(3) = 6/16 via the drain+α recursion). C2
  (2,8,n4), **TWO steeper pieces at distinct slopes (3, 2)** — never
  constructed: exhaustive 16,384, Wc = 4 (uncapped 1), ONTO 64 fiber
  256, DEC 13,312 / DRAIN 3,072 (u₂(4) = 12/64). C3 (2,9,n4), the
  **piece SANDWICH** (steeper s = 3 capped + shallower s = 1
  own-slope around a k = 2 child — the decided-piece analogue of
  THREECHILD's mixed cap): exhaustive 32,768, Wc = 2 (uncapped 1),
  ONTO 4 fiber 8,192, DEC/DRAIN 16,384/16,384 (u₂(2) = 2/4). C1q3
  (3,6,n3): 100 product samples, odd-q pin digit₁(a₂) = −2z ≠ 0 live
  (v(a₂) = 1 ON the side; D unchanged at 3), Wc = 3 exact.

## 8. CLEAN CHARGES (executed, nothing found)

Step (1) content route — re-derived, exact (§3); the floor-framing
consumer sweep — no consumer of the uncapped window survives (§2a,
independently of S14 leg 1); GENIND-6(a) end-to-end — consistent
(§4); S14 leg 1 — confirmed complete by my own sweep; S14 leg 3 —
S11 frozen byte-exact, tags coherent; C1–C14 spot checks — 7/7
verified against artifacts; S9.3 at verdict time — accurate under
the as-of rule (Remarks R1/R2); the four committed machine legs —
all reproduced in isolation; u₂(W) recursion vs my exhaustive
verdict splits — four independent confirmations (C1/C2/C3/SAMESIDE);
the m = 6 mixed-genre grammar — re-enumerated (feeding PE5-G1's
"conclusion true" clause); cross-round contradiction hunt over
r1–r4 — none found beyond the filed findings.

## 9. FINDINGS TABLE + GRADE LINE

| id | class | site | one line |
|---|---|---|---|
| PE5-G1 | GAP | S14 Arc ¶ + closing ¶ / S4.3 line ~528 | the full-sweep's universal-coverage sentences are FALSE by concrete witness: the hyphenated "first-visible" inventory sentence evades all nine disclosed greps, was never audited, and displays a false f-first entry law — the arc's record-integrity class, fourth consecutive round, now inside the sweep built to end it |
| PE5-m1 | MINOR | S4.3 first-visible parenthetical | "f-first ψ²-block variants enter at v(a₀) = 2k + 2h ≥ 6" — false formula (true: 6k same-side / 2k + 4h distinct-side; the chain reads 4 ≥ 6 at the f-first minimum); conclusion N ≥ 7 and M6-first-visibility TRUE (re-enumerated) |
| PE5-m2 | MINOR | S4.1 step (1) [r4] parenthetical, consumed VERBATIM by GENIND-6(a) | same-side co-block enumeration (pieces separable / co-children repeated) omits same-side COMPOSITE blocks (image ψ(ȳ+ẑ)^μ, live at n = 6 N ≥ 7); complete within GENIND-3's own scope; coprimality conclusion true and covered by 6(a)'s gloss |

**Machine leg:** all 8 frozen md5 pins + 5 supplier pins verified
(sealed runner six-way identical worktree = HEAD = r4×2 = r3 = r2 =
seal); isolated re-runs of ALL FOUR committed legs GREEN (163,536/0 +
2,128,163/0 + 705,133/0 + 6,507/0; every diff = elapsed-time only);
committed artifacts byte-untouched by this pass; tautology audit
executed on a fresh angle (hand-derived locus sizes as a
generator-free leg).

**Fresh route:** first n ≥ 9 evidence (NINEMIX: all four GENIND-6
block types, triple-mixed cap in one D_l, boxes bijective 256/81,
PARI partitions exact both characteristics); the r4 trichotomy's
same-side co-children leg machine-realized corpus-first (SAMESIDE,
59,049 exhaustive, σ = {(2,1),(2,1)} ×25); systematic steeper-piece
scan (double-piece + sandwich, 49,280 exhaustive states, capped
windows 3/4/2 vs uncapped 1/1/1). **GREEN: 109,146 checks, 0
violations, single run.** Every count matched its hand-derived
prediction exactly.

**GRADE LINE: NOT CLEAN — 0 CRITICAL + 1 GAP + 2 MINOR. The 2-clean
count does NOT start (0/2 stands; the consecutive-clean counter
stays at 0; GENIND remains the last arc-program leader without a
clean). r5 owed: one formula (PE5-m1), one scope-or-resweep repair on
S14's two universal sentences (PE5-G1), one enumeration clause
(PE5-m2) — all display/record-level; the capped window formula, the
rewritten step (1), LEMMA GENIND-6, every count law, every committed
number, and the conditionality display survived every attack, now
through n = 9, the same-side leg, and the systematic steeper-piece
regime.**

---
*Verifier: fresh-context hostile pass PE5, 2026-08-09. Fixed
nothing. All machine work in an isolated /tmp sandbox; committed
artifacts untouched (md5-verified before and after). Fresh-route
runner committed for audit as
`verification/openmath/genind_pe5_fresh.py` (+ output + results);
every law it gates on is derived in its docstring before the run;
its one read-off (the NINEMIX reader key tuple) is disclosed as such
and gated only on UNIFORMITY across the locus.*
