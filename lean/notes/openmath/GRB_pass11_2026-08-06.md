REVISION 9 is correct and complete.

- S1.4: “blanket invertibility would even make \(\bar y=x^{e_i}p^{-h_i}\) invertible [scoped to \(e_i\ge2\)]” — sound. For \(e_i\ge2\), \(\phi_{h_i}=x\), so blanket anchor invertibility makes both \(x\) and \(p\) units, hence \(\bar y\) a unit, contradicting \(\Delta=K_i[\bar y]\). For \(e_i=1\), every anchor has \(s(\gamma)=0\), while \(x\) is not an anchor; excluding that case is necessary.
- R7.1 twin: “blanket invertibility would even force \(\bar y=\cdots\) invertible (FOR \(e_i\ge2\))” — identical reasoning, correctly propagated.
- The operative per-case statement “\(\phi_\gamma\) is a unit iff \(s(\gamma)=0\)” remains valid in both cases. This agrees with FGMN’s structure theorem machinery: \(p_r\) is a unit, while the \(x_r\)-factor controls the nonunit contribution; \(\Delta\) is polynomial in \(y_r\). [FGMN, §§4–5](https://arxiv.org/abs/1305.0775)

Fresh hostile sweep: no quotation classifies as a critical error or justification gap. In particular, no remaining \(e_i=1\) sentence identifies \(x\) with a nonexistent nontrivial anchor, and W-MULT uses domain cancellation rather than anchor invertibility.

Residue audit is complete and consumption-honest:

- TR-3′-GEN remains open for the project-convention march dictionary; GD3-FULL/OL-B remain routed to it.
- TR-3-ORD-X remains open for the project-facing order bridge; Route B and project-facing clause-(4) consumers remain routed to it.
- TR-3-ORD is claimed proved only FGMN-internally.
- D-ITER remains open whenever \(E_i>1\), including order-2 \(e_0>1\), and is consumed only by IFACE(c); D-REAL’s full-stage claim remains restricted to \(E_i=1\).
- KP/H-CHAIN/[T] remain displayed external inputs, with the corrected range \(0\le j\le i-1\).
- N-1 remains explicitly owed.

Nothing in the discharged-clause summaries silently promotes any of these residues to proved status. No fixes made. This earns clean pass 1 of 2.

VERDICT: CLEAN