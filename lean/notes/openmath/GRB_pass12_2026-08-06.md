# GRB pass 12 (Fable, fresh context, max effort — the deciding pass that wasn't)

VERDICT: GAPS-ONLY (3 findings, none critical). Deep verification: CARRY-1/wrap
laws rederived from scratch; D-REAL rederived both cases incl. an independent
presentation check (F_r[y,p^±1][x]/(x^{e_i}−p^{h_i}y) mod (p−1, ψ_i(y)) =
K_{i+1}[x]/(x^{e_i}−z̄)); W-MULT domain cancellation validated; (TR-1-corr)
λ^GMN = −E_i·λ^FGMN rederived from Def 1.8 vs GMN Def 2.21; EVERY quoted
FGMN/GMN pin re-read verbatim in the extractions and confirmed supporting its
use; G1/G2/G3 gates RE-RUN reproducing the note's counts to the digit (G2
internals recomputed by hand: ψ₁ irreducible via disc 3 non-QR, V₂ = 2,
γ₂ = 5, ord(z₁) = 24); residue ledger consumption-honest (grep sweep, no
unflagged survivor); HDISCHARGE_H1 REV-3 range sync verified on disk.

F1 (gap): Lemma TR-3-ORD's display omitted the properness hypothesis its proof
consumes (Prop 5.14 improper branch: ⌈ord/e_r⌉ ≠ ord) + the (KP_i) membership
for L ∈ Max(∆) + a bound-variable garble (f vs g). Consumption site S6.4
already conditional on exactly these — display-alignment only.
F2 (cosmetic): "(12) of S5.1" dead pointer — (12-int) lives in S5.2.
F3 (cosmetic): G1 e-fold loop range is 2..12 × 1..39, not "all e ≤ 12, h ≤ 40";
e = 1 degenerate-true and covered by the main wrap loop.

All nine prior revision folds re-verified sound. Full report text archived from
workflow wf_059d37c2-e8d.
