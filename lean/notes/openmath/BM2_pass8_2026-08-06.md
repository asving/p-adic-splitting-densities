Findings:

1. **CRITICAL — `hspecRec` changes the target instead of stratifying the original REC-PARAM obligation.**

   Original REC-PARAM is explicitly:

   > “the identification of the node's actual (e_i, g_i) with … (1, 1)”

   But the note also correctly records:

   > “`Node.hspecRec` … forcing species = recentering → e = 1 ∧ g = 1”

   Therefore the original node-level REC-PARAM proposition is already supplied by a compiled structure field at every species-recentering node. The remaining “stage/wiring-level g-half tie” and non-fiat provenance are stronger, different obligations. They may remain open, but they cannot coherently remain the reason that §S0 says actual-pair (K0) on [RC] is “NOT proved — awaits REC-PARAM.” Synthetic `K0Conformant σ 1 1 Φ'` can be transported to the node’s actual pair using `hspecRec`.

   The note avoids smuggling node fiat into the wiring residual, but instead smuggles the stronger wiring requirement back into the weaker node-level status row. This breaks S0/body/Lean consistency.

2. **CRITICAL — the [RC] witness debt is called discharged too broadly.**

   The precise per-declaration display is accurate:

   - `landingKey_recentering_K0` is witnessed by `rc_landingKey_gate`.
   - `historyCoherent_recentering_K0` is witnessed by `rc_historyCoherent_gate`.
   - The recentering branch of `readsOf_landing_K0` remains without a compiled `ReadsOf` antecedent.

   But the surrounding claims say:

   > “the REV-8 UNWITNESSED-ON-FILE debt is DISCHARGED”

   and:

   > “The [RC] legs of Theorem LIFT-CONF's compiled perimeter are hereby NON-VACUOUS”

   Those umbrella statements include a still-unwitnessed compiled branch. The honest residual is displayed, but it contradicts the global discharge/non-vacuity wording. The defensible statement is that the two dedicated recentering corollaries are now non-vacuous, not the entire [RC] perimeter.

3. **AUTOMATIC CENSUS FINDING — the refreshed table cannot represent 134 current hits.**

   The table says C001–C123 stand, while C060/C061 are superseded, and C124–C134 are appended. Thus it contains only:

   `123 − 2 + 11 = 132`

   active current-text rows, not 134. Consequently either two current hits are absent or the reported 134-hit total is wrong. Under §S6 item 7, either outcome is an automatic finding.

4. **Witness-citation audit otherwise passes.**

   - `landingKey_ν₁rc`, `H₃rc`, `coherent_H₃rc`, `rc_landingKey_gate`, and `rc_historyCoherent_gate` all exist and have the stated synthetic `K0Conformant σ₁rec 1 1 σ₂rc.Φ` scope.
   - `RCWitness.lean` constructs no `ReadsOf` run, so the displayed `readsOf_landing_K0` residual is substantively correct.
   - `recParam_e_of_transition` has exactly the displayed hypotheses: `HistoryCoherent H`, `i + 2 < length`, predecessor species non-recentering, and child species recentering. Its three-part conclusion is exactly stage `e = 1`, stage `e =` node `e`, and node `e = 1`. It claims no g-half.
   - Minor precision: `rc_landingKey_gate` fires directly on `ν₁rc` and `landingKey_ν₁rc`, not on `H₃rc`; `ν₁rc` is merely also a node of `H₃rc`.

Clean counter remains 0/2.

VERDICT: CRITICAL