# N-H7-1 — the (H7) (I-τ)/K-LOC numerics probe: real m ≥ 2 decision points

**Unit:** numerics-fleet charge N-H7-1 (charter: `H7PROBE_2026-08-08.md` P-4 — the
near-term (H7) decision point, since every compiled instance is vacuous-by-type and the
one compiled m ≥ 2 roster object is triply degenerate; spec:
`lean/blueprints/HDISCHARGE_H7.md` §5). **Artifact:**
`verification/openmath/h7_kloc_probe.py` (pure Python, exact integers, sealed
predictions in the module docstring BEFORE first run, seed 20260808) + output
`h7_kloc_probe_output.txt` + `h7_kloc_probe_results.json`. cypari2/sympy unavailable in
env → the optional cross-check leg was skipped (blueprint marks it env-conditional).

## 1. What was built (the first real decision points)

Six sites with GENUINE m ≥ 2 rosters across (p, n) ∈ {(5,4), (2,4), (3,3), (3,5),
(3,4), (3,3)}, in the O-10 K-FACT frame (site = pinned coprime factorization mod
p^τ, τ = ρ+1; roster = the factors; branch reads = frame developments):

| site | data | separation type | ρ | sealed Smith |
|---|---|---|---|---|
| S1 | p=5: (x²−2x+6)(x²−4x+9) | residue-split, both RAM e=2 (F1-shape) | 0 | (0,0,0,0) ✓ |
| S2 | p=2: (x²+2)(x²+2x+2) | order-2 digit separation (blueprint's named wild pair) | 3 | (0,0,1,2) ✓ |
| S3 | p=3: (x−1)(x−4)(x−7) | m=3 HEN roster, depth-1 residual | 3 | (0,1,2) ✓ |
| S3b | S3 + f₀ = x²+1 | the genuine-f₀/C₀ face | 3 | Σe=3 ✓ |
| S5 | p=3: (x²+3)(x²+3x+3) | wild pair, F₃^× units live | 3 | (0,0,1,2) ✓ |
| S6 | p=3: (x−3)(x²−3) | SIDE-separation (KL-1b face), mixed degree | 1 | (0,0,1) ✓ |

All hand-sealed Smith profiles, ρ values, δ_j (λ-side translations), u_j (unit
scalars) confirmed by the harness integrity layer. Battery totals: **139 PASS, 8 FAIL,
26 INFO** (23s); ~180k polydisc members swept + 4 full enumerations (65,536 and
531,441-tuple tables).

## 2. Per-face results (sealed → measured)

* **P-A (K-JAC within-cell constancy)** — v_p(det Φ(g)) = ρ at every sampled member,
  all 6 sites: **GREEN**.
* **P-B (K-LOC (i), λ-side translation)** — branch side of F in the t_j frame = side
  of g_j translated by the sealed cell-constant δ_j (both endpoints), all branches all
  sites incl. the m=3 and mixed-degree rosters: **GREEN** (singleton observed-δ sets).
* **P-C (K-LOC (ii), unit scalar)** — **GREEN at the fixed-λ vertex everywhere**
  (incl. the nontrivial u = 2 predictions at S3, S5-branch-2, S6-HEN). **RED as
  slope-POOLED at S5 (both branches)**: the moving-endpoint unit ratio takes {1,2} —
  the flip fires exactly on x-dominated (odd-w) deviations, where the read crosses the
  frame's x²-reduction; measured flip factor = unit of the reduction constant (−1 for
  x²+3; +1 for x²−3 at S6; invisible at p=2). Supplementary per-slope-parity test:
  **constant in every parity class, both branches** (S5: even {1}/odd {2} and even
  {2}/odd {1}). **Verdict: §1.1(ii) as drafted (per-slope N_r^−/R_λ, ℓ-shift
  covariant — the §1.3 risk-(c) clause) SURVIVES; the slope-pooled strengthening is
  REFUTED.** Restatement pin for any transcription: key u_{j,r} by the realized slope
  (or vertex parity), never pool.
* **P-D (K-LOC (iii′) core)** — at every branch of every site: (d1)
  cofactor-independence of the branch read word computed from F alone (incl. f₀
  variation at S3b — the C₀ face rides free), (d2) word-graph bijection factor-word ↔
  F-word, (d3) measured K within the sealed ceiling τ+ρ+1 (tight at S1: K=2=ceiling;
  S6: K=4=ceiling): **GREEN**. Displayed boundary held as predicted: S1's raw depth-2
  B₀ digit (NOT an order-1 OM read) leaks cofactor dependence in 500/625 classes —
  the order-2-refinement requirement measured.
* **P-E ((I-τ) product identity)** — counts/fibers/product law #Σ_c·p^{sM} = ∏#C_j
  (·#C₀ at S3b) exact at ALL scans, all sites: **GREEN**. Constructive Lemma-A
  extraction (Newton–Cramer from the pins, decision from F alone): **GREEN wherever
  the blind zone covers the pins (N − e_max ≥ τ)** — S1, S6 (in-guard scans), S2/S3/S5
  at N=6, and 4×200 random in-guard samples at N = τ+2ρ = 10. **RED exactly at the
  three pins-uncovered scans (N=5: S2 2/4, S3b 10/27, S5 6/9 false-negatives, zero
  false-positives anywhere)**: traced mechanism — Newton converges to the exact
  factorization of the integer lift, which lawfully wanders off the pinned classes in
  the unblinded zone [N−e_max, τ). **Verdict: the guard-scoped IT-2 claim survives;
  the D-8 guard is measured to be exactly the extraction round-trip boundary** (its
  designed role). No countermodel to (I-τ) as drafted (a guard-scoped claim).
* **P-F (IT-4b probe)** — full-table sub-cell refinement (S2: 16 sub-cells × 4096;
  S3: 27 × 19,683): read-fiber constancy + alphabet stability for depth-τ′+1 digit
  words: **GREEN**. Species (merging, valuation-type) words: within-sub-cell fiber
  constancy held (0 nonuniform at both sites) — the sharpest IT-4b risk did not fire;
  species alphabets varied across sub-cells (9 resp. 27 distinct), but the species
  word as computed overlaps the sub-pin level, so this is FORCED by construction, not
  an IT-4b refutation — the correctly-scoped merging-word probe (anchored past τ′) is
  the remaining open falsifier.
* **P-G (route-B mini, H7-C1a/C1b arithmetic)** — aggregation uniformity + the (SIB)
  product identity on joint words (raw AND species; m=2 and m=3): **GREEN**, zero
  violations over all attained joint words.

## 3. Verdict

**The (H7) faces SURVIVE first contact at real m ≥ 2 sites** — with two measured
boundary pins, both of which the blueprint's REV-2 fine print already carries:

1. **(ii) is per-slope or false.** The pooled-u countermodel at S5 (exact shape: flip
   = unit of the frame's x²-reduction constant on x-dominated deviations) is the
   compiled-instance content of §1.3 risk (c). Any Lean `KLocSemantic` row must key
   ε_j(c) per level AND per realized slope — a slope-pooled ε field would be
   refutable by the S5 integers above.
2. **IT-2 extraction is guard-scoped or false.** Below N−e_max < τ the constructive
   Lemma-A round-trip fails at measured rates (2/4, 10/27, 6/9) while all count/product
   laws stay exact. `gate_order0_semantic`-style decide-small gates must sit in the
   D-8 guard range or they will manufacture false REDs.

Upgrades for the [M] row: (I-τ)+(K-LOC) now have a NON-degenerate numeric decision
point (H7PROBE P-1/P-2's degeneracy is broken at the numerics tier); the exact
instance shapes a future Lean forge needs are the six SiteDatum-ready integer sets in
§1 (m, thr-proxy τ, ρ, Smith, branchCounts p^{d_j(N−τ)}, stratumCount, δ_j, u_j per
slope-parity, K_j).

## 4. Honesty display

Numerics are falsifiers, never proofs: nothing here discharges KL-0 (frame kernel),
KL-3 (precision analysis), IT-2 ⊇ (run-replay), or the order ≥ 2 legs — those remain
the blueprint's open kernels. The sealed FAILs are recorded as-is in the JSON (8 FAIL
standing); the two supplementary tests ((ii′) per-parity, (e1g) in-guard sample) are
labeled post-hoc in-file and were added only after the run-2 countermodels fixed their
shapes. The site notion here is the O-10 K-FACT frame surrogate for the engine's
realized sites; engine-tied semantics (ITauSemantic at `siteCellEvent`) remain the
Tier-M obligation (H7-W3a), untouched.
