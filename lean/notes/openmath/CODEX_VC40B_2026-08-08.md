# CODEX VC40B — micro confirming pass on the r40b cure batch (orchestrator-direct launch during the 529 stand-down)

**Run**: orchestrator-assembled charge (VC40B_charge_2026-08-08.txt,
113,288 chars, built mechanically from committed sources: the CODEX_VC40
S3 findings list, the CORRECTED PROJECT_STATE slice @24ef527:1328–1348
per the B.4 cure spec, the full e92d5b4 diff), launched via nohup at
14:26 UTC; raw output VC40B_raw_2026-08-08.txt. Codex disclosed its
sandbox limitation (bwrap absent; no direct git) — verdict rendered on
the supplied diff only, which the charge was designed to be sufficient
for; the disclaimer is quoted in the raw log's final line.

**VERDICT: NOT-CONFIRMED — 1 finding (check 2); checks 1/3/4/5 PASS.**

- Check 1 (Class A, 3 sites): PASS — all three brackets carry the
  required 14-pass / 7-round / 2-round-1-critical / 12-consecutive-clean
  correction with flawed text preserved.
- Check 2 (Class B + self-check consistency): **FINDING** — the r40b EOF
  classification leaves "use-mention of the token or defect class" bare
  tokens un-daggered as LEGITIMATE class (b), but the STANDING
  SELF-CHECK the same cure installed at the §0 convention tail states
  ONLY a full-paste exception. The cure's two new texts contradict each
  other on the exception scope; the charge's "no new un-daggered bare
  tokens" is not met under the self-check's own rule.
- Check 3 (item (v) on the corrected slice): PASS — the Q1-closed state
  substantiated; the B.4 charge defect is cured.
- Check 4 (EOF revision record): PASS.
- Check 5 (nothing else moved): PASS as far as the diff shows
  (insertion-only in content terms).

**Orchestrator adjudication**: the finding is CONFIRMED as a
rule-consistency gap (both texts are r40b's own; they must agree). CURE
(r40c, one dated bracket): harmonize — either extend the self-check's
exception list to name the use-mention class explicitly (matching the
EOF classification), or dagger the use-mention tokens; the first is the
faithful reading of r40b's intent. **r40c + VC40c owed; the acceptance
citation REMAINS at REVISION 38 + r39b.**
