# GENTOW2 passPE4 — HOSTILE VERIFIER REPORT (fresh context), 2026-08-10
# THE ACCEPTANCE ATTEMPT

**VERDICT: NOT CLEAN — 0 CRITICAL + 1 GAP + 1 MINOR. The acceptance
attempt's fresh route (the first LETTERS × DEPTH contact anywhere:
depth-3 GENTOW2-A witnesses on the letter-live LP1 tower, f₁ = 2,
z₁ ∈ F₄∖F₂) MACHINE-REFUTED B-2 at letter-live grades: the recipe
frame LD2, built with per-height K₂-digit string (z₁, z₁²) whose
prescribed ψ₃ = y² + z₁y + z₁² SPLITS over F₄, came back a SINGLE
prime (4,4) — the realized residual is y² + z₁y + 1, i.e. exactly
LEMMA GENTOW2-B′(5)'s transformation law with a grade-varying,
z₁-valued unit family (u = 1 at the grade-19 slot, u = z₁ at the
grade-38 slot in the Φ₂/n̂₂(19)-read frame; the discrepancy is the
per-height/multiplicative normalizer cocycle res(n̂₂(19)²/n̂₂(38)) =
z₁², machine-exact). LEMMA GENTOW2-B's displayed conclusion is
therefore FALSE at letter-live frames as stated, and the honesty
box's "ON THE NOSE" assertion for B-2 is now machine-false — while
the box's [r2] scope disclosure ("the record supports u ≡ 1 on
letter-dead geography only") is proved PRESCIENT: the cocycle is
z₁-powered, hence identically 1 at every one of the 13 standing
letter-dead witnesses. THEOREM GENTOW2-A itself is UNTOUCHED and
took its first letter-live depth-3 contact 100% green: every
A-clause machine check passed (27/27 A-route incl. the clause-(c)
letter-twist certificates; polygons, (e,f), value triples, η₂-read
all exact at LD1/LD2/LD3). The rider (C-m1/C-m2) verified. Counter:
the consecutive-clean streak breaks — 1/2 → 0/2.**

Target: lean/notes/openmath/GENTOW2_PROOF_2026-08-09.md at HEAD
(7532acb = byte-frozen 92ac16b body + 61-line dated-corrections
rider; arc seal → PE1 61daa69 → r1 → PE2 aff457c → r2 → PE3 92ac16b
CLEAN 1/2 → rider). All three prior reports read; their routes (PE1
faithfulness audit, PE2 f₃ = 3, PE3 z₂ ≠ 1 letter contacts) not
re-walked. Charge: quote-and-classify, fix nothing; CLEAN bar =
nothing GAP-or-worse.

## F. FINDINGS

**[F1] GAP — B-2 is machine-REFUTED at letter-live grades: the
per-height repo K₂-digit does NOT equal the ε₂-normalized
F₃-coordinate there, so LEMMA GENTOW2-B's conclusion is false as
stated at letter-live frames (S5 honesty box "(B-2) ... ON THE
NOSE"; S5.1 B′(4) consumed form "u(β_t) = 1"; LEMMA GENTOW2-B's
"Then Φ₃ ... satisfies R_ν(Φ₃) = ψ₃"; the S4 in-particular clause
"the T(b)″-recipe polynomial ... satisfies ... (ii) by LEMMA
GENTOW2-B").** Exhibit (dual-sealed, dual-oracle): on the LP1 tower
(p = 2, Φ′ = x⁴+2x²+4, Φ₂ = Φ′²−8x³, f₁ = 2, z₁ ∈ F₄∖F₂, u₂ = 9,
e(µ₂) = 4, dv₂(Φ₂) = 18) take the stage-3 recipe at e₃ = 1, f₃ = 2,
κ₃ = 19: k̂₁ = (x²/2)·n̂₂(19) = 2x³Φ′ (per-height digit z₁) and
  * LD1: k̂₀ = n̂₂(38) = 512x (digit 1), prescribed ψ₃ = y²+z₁y+1
    IRREDUCIBLE — machine: single prime (4,4) ✓;
  * LD2: k̂₀ = (x²/2+1)·512x = 256x³+512x (digit z₁²), prescribed
    ψ₃ = y²+z₁y+z₁² = (y+1)(y+z₁²) SPLIT — machine: single prime
    (4,4) ✗ (predicted-under-B-2 [(4,2),(4,2)]).
Both k̂₀ are Φ₂-free, exact-grade (two monomials of classes summing
to z₁² ≠ 0), on-line, gcd-normalized — every recipe hypothesis
holds. Since FGMN additivity + eq (14) + Lemma 5.3(2)/Thm 6.6 (all
PDF-verified citation surfaces) force #primes = #distinct
irreducible residual factors, R₃(Φ₃_LD2) is irreducible ≠ the
B-2-predicted split ψ₃: B-2 fails at these grades. The decode probe
(sealed post-hoc, disclosed; GREEN 14/0 first-run-under-its-seal)
pins the realized residuals in the y = Φ₂/n̂₂(19)-read frame: LD1 →
y²+z₁y+z₁, LD2 → y²+z₁y+1 (LD2 realized LD1's prescription!),
via the division identity r3² = res(k̂₁/N)·r3 + res(k̂₀/N²) with
N = n̂₂(19)(ξ): the grade-38 slot picks up the normalizer cocycle
res(n̂₂(19)²/n̂₂(38)) = z₁² (machine-exact), so realized =
y² + c₁y + z₁·c₀ = B′(5) with (u₁, u₀) = (1, z₁) in this frame —
grade-varying, z₁-valued, IDENTICALLY 1 on letter-dead geography
(z₁ = 1), which is exactly why the standing 13-witness record was
green and exactly the ϑ-cocycle mode the fallback clause names.
Equivalently: the realized residual equals the MULTIPLICATIVE-
convention digit string (LD1 (z₁, z₁), LD2 (z₁, 1) — decode checks
MULT0/MULT1), not the per-height string the (R-repo) display and
the recipe grammar prescribe. What survives, per the note's own
machinery (and machine-confirmed here): THEOREM GENTOW2-A (consumes
only hypothesis (ii) — all clause checks green at LD1/LD2/LD3);
B′(3)/(5) (the measured law IS B′(5)); realizability of every
admissible ψ₃ via c_t ↦ u(β_t)⁻¹c_t; and the (α)-consumption inputs
(c)+(d) (η₂-read = z₁ and value triples machine-green letter-live).
What is false as stated: B-2, GENTOW2-B's conclusion for the
unmodified recipe at letter-live frames, and the S4 "(ii) by LEMMA
GENTOW2-B" leg there. Failure scenario (realized): a letter-live
tower consumer prescribes ψ₃ through the recipe's digit string and
gets a different — possibly split-vs-irreducible-flipped —
extension; LD2 is the exhibited instance. Note also: u's measured
coincidence with the repo-internal per-height/multiplicative
cocycle at both tested grades sits in tension with S5.1's "GENTOW5-
A1 ... cannot produce the cross-frame u(β)" (derivability claim;
the VALUES coincided here) — repair should adjudicate whether
u = ϑ-cocycle generally, which would make B-2's residue repo-
derivable and GENTOW2-B repairable to a theorem in the
multiplicative prescription.

**[F2] MINOR — the rider C-m1 makes Lemma 3.13(1) load-bearing
(the exact stable value v(π₂(x₀)) = 1/e(µ₁) carries the
denominator-clearing bridge) but the S6 "Statements consumed" list
(byte-frozen body) does not include Lemma 3.13; it sits only in
S6.1's supporting-pins paragraph.** Provenance is covered (L836–
840, p.16, PDF-verified in r2 and re-verified by PE3), so this is
inventory drift only: a reader auditing consumed statements from
the S6 list alone would miss one.

## R. RIDER VERIFICATION (C-m1 + C-m2) — CLEAN

C-m1 re-derived line-by-line: H_{µ₂}(b) = y₂ ⟺ µ₂(b − γ₂) > 0;
multiplying by π₂^{u₂} (µ₂-value u₂/e(µ₁) = e₂(w₂+λ₂) by Lemma
3.13(1) + §3.2 data e(µ₂) = e₂e(µ₁)) gives the displayed inequality
µ₂(bπ₂^{u₂} − Φ′^{e₂}) > e₂(w₂+λ₂) = µ₂(Φ′^{e₂}) ✓; v ≥ µ₂ on
polynomials at the Φ₂-root (µ₂ < µ₃ ≤ v_{x₀}, PE3's own vetted
bridge line) ✓; the denominator's EXACT stable value v(π₂(x₀)^{u₂})
= u₂/e(µ₁) = e₂(w₂+λ₂) ✓; division gives v(b(x₀) − γ₂(x₀)) > 0
hence res(b(x₀)) = res(γ₂(x₀)) ✓; Prop 1.15(1) then applies to the
POLYNOMIAL b ✓. The γ₁-step gloss ("no stability input needed
there") checks: γ₁'s cleared denominator is a pure π-power (v(π) =
1 trivially exact), and the negative powers γ₁^k, k < 0 are k-th
powers of a nonzero residue. The two other identification use-sites
correctly declared polynomial-class applications. C-m2's three-arc
bracket cross-checked against S9/S10 and the PE3 report @92ac16b —
accurate, including "HOLDS AT 1/2" (dated appends, not a scoring
round). Byte-freeze verified in §M.

## S. OWNED SURFACES — CLEAN CHARGES

* **THEOREM GENTOW2-A assembly (the four r3-hole inputs as ONE
  theorem; quantifier structure walked end-to-end this pass).**
  The Φ₃-quantifier is genuinely "ANY monic with (i)+(ii)": (a)'s
  KP-certificate consumes only R₃(Φ₃) = ψ₃ ≠ y irreducible +
  deg = e₃f₃m₃ (Q6 schema + Cor 4.4 + Lemma 5.3(2)); the
  non-equivalence leg has BOTH Prop-5.7 arguments certified in
  KP(ν) (Φ₃ by (a), Φ₂ = φ₃ by Lemma 5.3(1)); Cor 6.4 then fires
  with the S2 length-3 chain (H-b: the tower's proper stages give
  strict degree growth; Cor 6.4 needs only a chain); (d)'s
  per-root quantifier is sound (Thm 6.3/Cor 6.4 stated for a fixed
  root θ, every root of irreducible F qualifies); ℓ-bookkeeping
  ℓ₃ = e₃f₃ from (23) at i = 3, ℓ₂ = e₂f₂ℓ₃ from the "in
  particular", deg R₂ = ℓ₂/e₂ consistency via Cor 4.4(4); e₃ is
  correctly read as ν's augmentation data, not a free parameter.
  No quantifier slip found — and the assembly took its first
  letter-live depth-3 machine contact 100% green (section E).
* **S2 dictionary + shear as a WHOLE (all 13 rows + the affine
  map, walked once).** Each row re-derived; witness column
  recomputed by hand (λ₁ = 1/2; u₂ = 5 = 4·(1+1/4); µ(φ₂) = 5/4;
  κ₂ = 5/2; κ₃ = 21/2 = 4·(21/8); e₂ = denom(e(µ₁)λ₂) = 2; e₃ = 2
  from ν(Φ₂) = 21/8 with e(µ₃) = 8 over e(µ₂) = 4; dv-ladders 2v,
  4v; the [r2] ψ₂-row twist hedge exact per the letter formula).
  Shear: (j,y) ↦ (j, y/e(µ₁) + jw₂) affine invertible abscissa-
  preserving; witness pins (4,0)/(2,7)/(0,10) ↦ (4,4)/(2,11/2)/
  (0,5) exact; slope correspondence κ₂ = e(µ₁)(w₂+λ₂) ↔ −λ₂; the
  µ₁ = dv₁/e(µ₁) leg is B-1-territory disclosed inline. No defect.
* **Honesty-box inventory at HEAD.** B-1 / B-2 / B-2-fallback /
  B′ / the T(b)′ + GENHN-2′ corpus anchors / the (β)-floor rider:
  each [r1]/[r2] addition's conditionality is boxed or corpus-
  anchored; the single drift found is [F2] (the rider's Lemma-3.13
  elevation). The box's B-2 machine-support sentences are accurate
  AS DATED ("13 witnesses standing at PE2", "letter-dead geography
  only") — and [F1] proves the scope warning was load-bearing, not
  boilerplate. The "ON THE NOSE" truth-assertion itself falls under
  [F1].
* **Free hunt.** GENTOW2-B's pin arithmetic (α = u₃f₃/e(µ₂);
  on-line heights; κ₃ > e₂f₂u₂ ⟺ λ₃ > 0) re-derived exact; the
  s_j = s(α) + je₃ abscissa bookkeeping consistent with eq (12);
  (H-f)'s improper-stage collapse cite fits; S7's assembly
  consequence accurately scoped (depth ≥ 4 NOT claimed; (β) cited
  as in-flight). Nothing further at GAP level.

## M. MACHINE-LEG INTEGRITY

* **Body byte-frozen under the rider**: md5(head-898 at HEAD) =
  1f38fd7e5d2d860de1bcf3c30c754af1 = the full 92ac16b blob; rider
  append-only (959 = 898+61).
* **Pins at HEAD, 12/12 EXACT** (all PE3 §M pins re-taken): PDF
  da846c82f1fcb619cefb3feb8164191e / 518,522 bytes; checks.py
  2603eb7c; run2 832aa838; pe1_fresh a70dd986/e66e8ea5; pe2_fresh
  f2d6f1fa/d26cacf5; r2 probe d0c0ebeb/b79121a2; pe3_fresh
  9755cce9/6bbb87f8.
* **pe3_fresh seal chain**: blob at 0e80968 = HEAD blob; runner-
  only commit; output only at ce43fe0 — first-run-under-seal.
* **Isolated re-runs, 5/5 BIT-IDENTICAL** (fresh /tmp copies, exit
  0): battery 41/0; pe1_fresh 20/0; pe2_fresh 22/0; r2 probe 21/0;
  pe3_fresh 50/0.
* **Tautology audit of pe3_fresh** (the committed instrument no
  verifier had audited): A-route resultant-only certification with
  docstring-literal predictions; B-route nfmodpr values computed
  independently then compared. No circularity.

## E. FRESH ROUTE (LETTERS × DEPTH — runner gentow2_pe4_fresh.py
## SEALED at 23def87 pre-run; first-run-under-seal output committed
## as-is; decode probe gentow2_pe4_decode.py sealed then run,
## GREEN 14/0)

Frames LD1/LD2/LD3 on the LP1 tower as in [F1] (+ LD3 = Φ₂² −
64x²Φ′: e₃ = 2, f₃ = 1, κ₃ = 37/2, letter digit z₁ — the first
e₃ ≥ 2 letter-live stage). Sealed verdict: **62 checks, 9
violations — RED, and the 9 misses ARE the finding**: every one is
a prediction I derived assuming B-2-on-the-nose (LD2's split
verdict + its downstream prime-count/val/residue tallies + the
ψ₃-root reads keyed to the prescribed strings). What passed (53):
the ENTIRE A-route 27/27 — construction-identity expansions,
dv₁/dv₂ pin tuples, one-sidedness κ₂ = 9/2 with s = 0 and support
{0,4} (middle pin strictly above: the char-2 ψ₂-power signature),
the exact resultant certificates (8/36/76 or 74 value triples ×3
frames) INCLUDING the five constant-digit certificates
{152,148,144,144,144} that pin clause (c)'s letter twist in pure
integer arithmetic (per-height constant digit 1 = z₂-side vs
multiplicative digit z₁² = η₂², ratio = the S5.1 LETTER FORMULA
raised to ℓ₃ = 2) — plus, on the B-route: LD1 (4,4) single prime
with nfeltval (2,9,19), LD3 (8,2) with (4,18,37), ℚ-irreducibility
3/3, z₁ live in every residue field, η₂(ξ) = z₁ (the (α)-
consumption ψ₂-root read) at 4/4 primes, LD1's leaf letter
generating F₁₆ (f₃ = 2 live), LD1's DPH/DMU digit reads, LD3's
e₃ = 2 slot read = z₁, and the level-2 tooth-face identity LD1 ≡
LD2. So THEOREM GENTOW2-A's surfaces went green at the first
letters × depth contact; LEMMA GENTOW2-B's digit-prescription
clause did not — see [F1]. Verifier-instrument honesty: my own
sealed predictions inherited the note's B-2, so the instrument's
RED is self-implicating by design and disclosed; the decode probe
(14/0, predictions hand-derived from the division identity and
sealed before its run) uniquely resolves the failure pattern —
realized residuals y²+z₁y+z₁ (LD1) / y²+z₁y+1 (LD2), cocycle
res(n̂₂(19)²/n̂₂(38)) = z₁², u₀ = z₁.

## G. GRADE LINE

**passPE4 verdict: NOT CLEAN (1 GAP: F1 — B-2 machine-refuted at
letter-live grades, LEMMA GENTOW2-B's conclusion false as stated at
letter-live frames, honesty-box "ON THE NOSE" now machine-false;
1 MINOR: F2 rider/consumed-list inventory drift). Zero CRITICAL:
the FGMN citation surfaces, THEOREM GENTOW2-A (with its quantifier
assembly walked end-to-end this pass), the S2 dictionary + shear,
the rider, and all sealed machine legs held — and the note's own
disclosed fallback law B′(5) is exactly what the machine measured,
so the chain repairs inside the note's own machinery. GENTOW2 is
NOT ACCEPTED this round: the consecutive-clean counter breaks,
1/2 → 0/2. Repair guidance: (i) flip B-2 to its measured status
(refuted at letter-live grades; u = grade-varying z₁-cocycle,
identically 1 letter-dead) and restate GENTOW2-B via the
re-parameterized or multiplicative-convention prescription
(machine-supported at LD1/LD2); (ii) adjudicate u ↔ ϑ-cocycle
identity (values coincided at both tested grades — if general,
B-2's residue is repo-derivable and B becomes a theorem; re-scope
the S5.1 "GENTOW5-A1 cannot supply u(β)" sentence accordingly);
(iii) propagate to S4's "(ii) by LEMMA GENTOW2-B" leg and the
consumption note (GENTOW2-A itself and the (α) inputs (c)+(d) are
machine-green letter-live and need no change); (iv) fold Lemma
3.13(1) into the consumed-statements inventory (F2). Fresh-route
artifacts committed: verification/openmath/gentow2_pe4_fresh.py
(sealed 23def87) + gentow2_pe4_fresh_output.txt (RED 62/9,
first-run-under-seal, kept as-is) + gentow2_pe4_decode.py (sealed)
+ gentow2_pe4_decode_output.txt (GREEN 14/0).**
