1. CONFIRMED — base-case arithmetic closes:

   > “By [IN-F1] … gcd(d, δ) > 1 … factors of equal degree d/gcd < d”

   Since the base residual is irreducible of degree \(d\) over \(\mathbf F_p\), LN 3.46 gives exactly those factor degrees over \(\mathbf F_q\). DICT-0(c)-base makes every reconstructed irreducible base residual one of those divisors, contradicting DICT-1(1)’s required base degree \(d\).

2. CONFIRMED — no DICT-0(b) consumption in that base case:

   > “both reads live over F_q by construction”

   The reconstructed base read is over the residue field of \(O_\delta\), namely \(\mathbf F_q\), while the transported read is over \(\mathbf F_p\mathbf F_q=\mathbf F_q\). No separately supplied embedded-field identification is needed.

3. CONFIRMED — indexing is coherent at all live DICT-0 uses:

   > “through stage k” = base read plus stages 1, …, k accepted

   Thus \(k=0\) means the base read is accepted and \(\psi_1\) is pending; the empty prefix is correctly excluded from the \(k\)-indexed family and handled separately. In the stage case, \(j_0\ge1\) and \(k=j_0-1\), so clause (b)’s range through \(k+1=j_0\) supplies exactly the field needed for clause (c).

4. CONFIRMED — final dependency tally:

   - \((2)\Rightarrow(3)\): DICT-0(a), (b), (d), plus [IN-R1] and [IN-U].
   - \((3)\Rightarrow(1)\): no additional DICT-0 clause.
   - \((1)\Rightarrow(2)\), base failure: DICT-0(c)-base plus [IN-F1]; no (b).
   - \((1)\Rightarrow(2)\), stage failure: DICT-0(b), (c), plus [IN-F1].
   - `l4_persist`: no DICT-0 clause.
   - Literal transported-key or residual-polynomial equality: not consumed.

5. CONFIRMED — revision sediment is clean. Retired ledger/key-based matching language appears only in explicitly historical records. The live §3 sites consistently describe DICT-1 as ledger/data coincidence, reserving actual residual persistence for `l4_persist`.

6. CONFIRMED — fences remain explicit and intact:

   > “DICT-0 … DISPLAYED OPEN”

   > “DICT-1 is NOT a Lean theorem”

   > “Both are NAMED HYPOTHESIS rows”

   > “NO `DictModel`, `GcdSlice` … or `AlphabetData` instance is constructed”

   No prose-to-Lean strength leak or hidden instance claim remains.

VERDICT: CLEAN