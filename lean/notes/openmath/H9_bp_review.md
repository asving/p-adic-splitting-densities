# H9 blueprint — Codex adversarial review (archived verbatim)

Run 2026-08-01 machine clock (the 2026-08-05-dispatch session), fresh-context
Codex, detached exec; prompt = charge + full HDISCHARGE_H9.md + verbatim
ground-truth appendix (/tmp/openmath/H9_bp_prompt.txt, 49,615 bytes; raw stream
H9_bp_review.jsonl). Tokens: 28,431 in / 5,405 out. VERDICT: REVISE
(7 CRITICAL + 3 GAP). ALL TEN FINDINGS ADJUDICATED VALID by the H9 lead and
FOLDED into the blueprint same-session (disposition table: HDISCHARGE_H9.md §8).

1. **CRITICAL** — “**Two independent grounds:** … **(b) truth at HEAD, machine-checkable**”

   Ground (a) is sufficient because (NSF-KEY-RET) expressly makes the six artifacts necessary and sufficient, and the appendix confirms all six. Ground (b) is not independently sufficient: the authority says (NSF-KEY) retires “ONLY” through Unit C and “NEVER by any leaf text,” so a compiled theorem can corroborate the result but cannot constitute a second retirement route.

2. **CRITICAL** — “**Two displays now exist that did not then:** … **(b) intended — (q-4)'s docstring clause carries the guard verbatim.**”

   This directly misstates the chronology and the pass-1 ruling. The withdrawn K1 site argument already used (q-4) to establish that labeled boxes fiber guarded trees, yet the authority still concluded that no discharge of (K1-CANON) was on file. Re-presenting the same pin as newly available evidence silently re-runs evidence already adjudicated insufficient.

3. **CRITICAL** — “decided_σ is ‘**DEFINED from the canonical map (never a free field)**’ … **so the consumed range = {trees fibered by `some σ`-labeled boxes within threshold} = the σ-carrier's trees.**”

   The conclusion does not follow from the quoted definition. Being defined from a map does not establish that K4’s index set `T type σ` is exactly the map’s fiber image, nor that every summand is a `bridgeTree`; the supplied K4 excerpt only says that K4 produces a sum over trees of type σ. This unsupported equality is the central range lemma that (K1-CANON) requires.

4. **CRITICAL** — “**(x-3) the intended-layer leg reads (q-4)'s text as the definition of ‘labeled’ — legitimate HERE because the claim being proved IS the denotation statement (no circularity…).**”

   Calling the target a denotation statement does not remove the circularity. The note must first prove that M04’s K1 invocations range over the objects denoted by the (q-4) pin; quoting what the pin means only describes the destination and supplies no bridge from K1’s bare “canonical tree” quantifier. This is precisely the unresolved premise identified in the authority’s pass-1 gap 5.

5. **CRITICAL** — “**hence the (q-3)-scoped source lemmas' proved range covers every consumed K1 instance, and K1's ‘canonical tree’ AT THE CONSUMED SITES denotes the pinned notion**”

   This conclusion depends on the unproved range identity in C1-2 and conflates the as-built Lean classifier with M04’s intended paper-level K1 quantifier. Unit C proves properties of `bridgeTree`; it does not prove that every tree indexed by the post-K4 paper sum is a `bridgeTree` or carries the same denotation. Thus the design effectively re-runs the withdrawn source/scope inheritance inference under new terminology.

6. **CRITICAL** — “**at realized trees the two guard vocabularies COINCIDE**” and “**every CARRIED tree satisfies the (q-3) guard in the LEAF vocabulary**”

   The Lean theorems themselves are valid and their displayed bodies are confirmed by the appendix, but their billing is stronger than their statements. U3 proves, under a `fiberAt` witness, only `NsFree Tr ↔` every maximal chain `IrrHalts`; U5 proves only the maximal-chain conclusion. Neither theorem establishes the entire (q-3) package—complete, finite, canonical, certified, with all leaves classified as τ-irr/τ-hen—or a semantic identification of `henV` payload with every τ-hen leaf, so they cannot by themselves supply the advertised K1 guard.

7. **CRITICAL** — “**exact squares and every window-degenerate locus now fiber NO carried tree and drain to `none`/env(N)**”

   The authority supports expulsion of the specific M02 witness V₀ and records the intended-semantics caveat involving designer-sorried `canPolicy`. Nothing quoted proves the universal claim about every exact square or every window-degenerate locus. The stated bounds do not qualify this overclaim, so the adjudication exceeds its documented consequences.

8. **GAP** — “**compiled-proposition record H9-U1**” in E-r14-1, versus “**compiled record H9-U1 once landed**” in E-r14-2.

   Section 7 leaves r14 externally queued but does not make it depend on Wave 1 landing H9-U1. Consequently, the r14 batch could be executed after the Unit-D bar but before the real module exists, leaving E-r14-1 with a nonexistent corpus pointer. The probe is evidence, but it is not the promised landed declaration.

9. **GAP** — “**WAVE 2 … parallel with Wave 1 … cites U3/U5 as ‘probe-proved, landing as Wave 1’**” followed by “**WAVE 3 … C2 Codex hostile pass on C1**”

   The intended join is not explicitly stated: C2 must wait for both the paper note and the actual Lean module, not merely for C1’s draft. Otherwise the hostile pass could accept a discharge whose compiled dependencies are still only temporary probes. This is a dependency/bookkeeping omission, not a defect in the Lean proofs themselves.

10. **GAP** — “**the ELEVEN remaining rows ↔ (H1)–(H8) bijection must be re-displayed**”

   Eleven rows cannot be in bijection with eight labeled hypotheses without an additional member-level counting convention. No such convention is stated here. The r15 edit specification therefore contains internally inconsistent enumeration bookkeeping.

**VERDICT: REVISE**
