NOTE — repository execution was unavailable because the sandbox launcher lacked `bubblewrap`. Under the standing division-of-labor rule, this is not a finding: the Fable-executed battery, F2c implementation, mutation controls, and fixed-point grep are already on record. No files were changed.

Findings:

1. GAP — one current verdict-tier summary abbreviates the third component as:

   > “conditional on the displayed standing rows”

   The operative §0, §39 disposition, acceptance record, and provenance entry correctly spell out `(H-ℓ) + (KP_i)+[T] at S6.2 + PIN-GEN-i`, so there is no unconditionality leak. Nevertheless, the REVISION-33 closing summary does not meet the charge’s “exact conditionalities at every acceptance site” standard.

2. GAP — the provenance appendix announces:

   > “the TWIST3 hygiene bracket (verbatim, complete, with context)”

   but the following slice is the unrelated embedded N-T4 record. The actual dated TWIST3 supersession bracket is therefore absent from the supplied appendix. Its substance is consistently described elsewhere, but the claimed verbatim-and-complete provenance slice is not present.

Checks otherwise passed:

- Anchor: consistently REVISION 32/VC34 (`f3b1ff8`); REVISION 33 correctly remains VC35-owed and never self-confirms.
- TR3-S4: the 4-pass/2-revision arc, both pass-3 execution legs, F2c’s 1,108/0 result, and three RED mutation controls are faithfully folded.
- Strata: component two remains print-unconditional; component three remains conditional. Member (a) is not discharged and (H1) remains `[M]`. Deeper strata remain OPEN, probe-first, with no candidate sealed.
- Hygiene substance: one additive orientation supersession; measured data and sealed harness explicitly remain untouched.
- Queue: all five wave-15 remainder items are present; synthesis pass 8 remains owed.
- Asvin batch: exactly four items, with the verified FGMN proposal at TOP.
- Roster: independently enumerates nine rows.
- Clauses: no attribution change; `(0)` and `(SQ)` remain unconditional, while `(R)` and `(UB)` retain the inherited 8/8 recount.
- Census reconciliation: §39 contains 60 table rows but 61 grep patterns because the single row `TransDeep + TRANS-DEEP` represents two separately counted patterns. Thus “59 + VC35 + KEY0-LIFT = 61” is arithmetically consistent.
- Falsifiers: the sealed 13,679/0 run, F2c 1,108/0 run, and mutation teeth are consistently recorded.
- Embedded-record convention: N-T4 is correctly treated as embedded in H4M1 rather than as a separate note file.
- Chronology and disclosures are internally consistent with the supplied authoritative git chain.

VC35 FINDINGS: 0 critical, 2 gaps
VERDICT: NOT-CONFIRMED