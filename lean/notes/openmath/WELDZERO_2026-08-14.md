# WELD-ZERO — the last weld face inside a count law, retired

## LEMMA GENHN-4 layer 1 proved in gr(O_L), the level-1 convention pinned, and COROLLARY W-9 replaced clause by clause

**Unit:** WELD-ZERO (the unit `HYP.149` names; Asvin-ordered 2026-08-14,
"let's also try the candidate route synthesized by the audit right now")
**Composition pass:** attempt **0/2**, 2026-08-14 — **hostile arc owed**
**Charge:** derive or refute the audit's synthesis (`runs/qgen/WELD_FACE_AUDIT.md`
§4): that GENHN's own `[r2]` gr(O_L) re-aim composed with the GENTOW5-A0/A1 laws
supplies what COROLLARY W-9 supplies at LEMMA GENHN-4 layer 1, **without the weld
layer**.
**Verdict:** **DERIVED**, with one item the audit did not anticipate — a
convention pin that is load-bearing, not cosmetic (§3.3, §5).
**Sources read at HEAD `f5841548`** (md5s in §9), except
`spec/HYPOTHESIS_LEDGER.md`, whose **v5** fold landed from a concurrent session
mid-composition and is read at `1d787a7d` (§7 header). This note edits nothing.

---

## S0. VERDICT UP FRONT

**The route runs.** LEMMA GENHN-4's layer 1 at general (e₁, f₁, μ) is proved
below (THEOREM WZ-A) inside gr(O_L) — the graded ring of the stage leaf FIELD —
from four inputs, none of which is a weld face:

| input | supplier | grade at HEAD |
|---|---|---|
| gr of a valued field is a graded field | LEMMA WZ-1 (= GENTOW5-A0 one level down) | GENTOW5 **CERTIFIED 2026-08-12**; re-proved in-note in five lines |
| the level-1 ladder cocycle and the two residual reads | LEMMA WZ-2 (= T1 (C2-wrap)/(C2-level-1)/(C3-dictionary) at base height κ) | T1 **ACCEPTED 2/2**; re-derived in-note |
| one K-slot per dv-height, slot-min = valuation | LEMMA GENHN-2 | GENHN **CERTIFIED 2026-08-12** |
| the exact-height K-lift of the letter at height κ > D′h | LEMMA GENHN-LIFT / T1 (C6) | GENHN, T1 |

**The one thing the audit's synthesis missed.** The audit priced the fold as
CARRY — "one fold unit that rewrites GENHN-4 layer 1 in the A0/A1 idiom … no new
mathematics is required" (`WELD_FACE_AUDIT.md` L266–268). That is **not quite
right**. The A0/A1 idiom carries a *convention obligation* that GENTOW5 itself
flags one level up — "**fixing the multiplicative convention is part of the box's
content**, not a cosmetic choice" (`GENTOW5_PROOF_2026-08-09.md` L257–258) — and
at level 1 the obligation has teeth: the side residual read against the coherent
normalizers n(tκ) (GENHN's **"twisted residual"**, L1006) and the side residual
read against n(κ)^t (the **multiplicative** residual) differ by a diagonal unit
rescaling ϑ_t = η^{⌊t·i(κ)/e₁⌋}, and

> **a twisted-residual perfect μ-th power is NOT a multiplicative-residual
> perfect μ-th power whenever ϑ is live.**

GENHN-4's stated hypothesis "residual (T − s)^μ, s ∈ K^×" (L868) is true in
exactly one of the two readings. §3.3 pins the reading (multiplicative), §3.6
shows the pin costs the count laws nothing, and §5's battery **fires the tooth**:
at seven of eleven frames, actual polynomial re-division kills all μ pins under
the multiplicative hypothesis and leaves pins standing under the twisted one, at
exactly the predicted heights with exactly the predicted digits.

**Consumption impact if the arc accepts** (§7): `HYP.148` (GENHN-BOX-2, the one
weld face inside a count law) and `HYP.139` (the W-1 transport riding under it)
both retire from the cone; `HYP.149` closes as discharged-by-this-unit; the
preview's weld carry drops from two conjuncts to one frame-grade conjunct
(`JD0-BOX-2`), which this note does **not** touch and does **not** add to.

**What this note does NOT claim.** It does not re-prove GENHN-4's layers 2 or 3
(§6, WZ-BOX-1). It does not touch `[GENHN-HE(μ ≥ 3)]`/`HYP.81`, the σ-decision
box, which is a different obligation at the same threshold. It does not edit
GENHN, GENTOW5, T1 or GRTJC — all four are frozen accepted text and the fold's
convention pin is a **reading** of GENHN's frozen words, boxed as such
(WZ-BOX-3).

---

## S1. SETTING (one-pass restate — GENHN S3.1 as corrected by its own `[e2]` E3)

Base: a complete discretely valued field with valuation ring Ô, uniformizer π,
residue field F_Q, v(π) = 1. Stage genre data (Q; e₁, f₁, μ; h) with
gcd(h, e₁) = 1, D′ := e₁f₁, key Φ′ ∈ Ô[x] monic of degree D′.

Fix a root θ of Φ′ and put **L := Frac(Ô)(θ)**. GENHN's dated erratum
`[e2] ERRATUM E3 2026-08-09` restates the carrier at the FIELD, because
R = Ô[θ] is in general a non-maximal order:

> "Let θ be a root of Φ′, L := Frac(Ô)(θ), **O_L its valuation ring — a complete
> DVR** … S3.1's forcing chain runs verbatim AT THE FIELD: v(θ) = h/e₁ with
> gcd(h, e₁) = 1 gives e(L/base) ≥ e₁; η = res(θ^{e₁}π^{−h}) a root of (a unit
> multiple of) ψ gives f(L/base) ≥ f₁; then e·f = [L : base] ≤ deg Φ′ = D′ = e₁f₁
> forces e = e₁, f = f₁ … and O_L/m_L = F_Q(η) = K = F_{Q^{f₁}}"
> (`GENHN_PROOF_2026-08-08.md` L500–509)

and — the sentence this whole unit turns on —

> "**S6.1 layer 1's graded frame re-aims at gr(O_L)** (one K-line per dv-height,
> W-9's twisted algebra; R ⊆ O_L and every graded read of an evaluated element
> happens in gr(O_L))." (`GENHN_PROOF` L515–517)

Write **dv := e₁·v**, so dv(L^×) = ℤ, dv(θ) = h, dv(π) = e₁. Because
[L : base] = D′ with e(L) = e₁ and f(L) = f₁, the residue map is an
**isomorphism** ι : K ≅ k_L; we suppress ι where it is harmless and write it
where a transport is being asserted.

Normalizers (GENHN S4): for each integer height m,

    n(m) := x^{i(m)}·π^{a(m)},   0 ≤ i(m) < e₁,   i(m)·h + e₁·a(m) = m.

**Occupied domain.** GENHN's dated corrigendum 2026-08-12 tightens the full
K-slot to heights m > D′h, and records that this is exactly the consumed domain:

> "Every height m > D′h is occupied, with the explicit lift supplied by LEMMA
> GENHN-LIFT …; **every pin the note's laws consume sits at dv > D′h** (the
> stage-initial node floor {dv(A_j) ≥ (μ−j)D′h + 1} and the refine floors), so no
> consumer moves." (`GENHN_PROOF` L2306–2310)

Consequently at every consumed stage-α event the slope satisfies **κ > D′h** (take
j = μ−1 in the node floor: (μ−j)κ ≥ (μ−j)D′h + 1 gives κ > D′h), and all pin
heights tκ, 1 ≤ t ≤ μ, are occupied. Everything below is stated on that domain.

**The event.** A *stage-α event* at slope κ: the stage polygon is one side from
(0, μκ) to (μ, 0) in the (j, dv(A_j))-plane, where f = Φ′^μ + Σ_{j<μ} A_j Φ′^j is
the Φ′-adic development (deg A_j < D′; C ≡ 1 — the full-side genre, embedded
genres having been split off by GENIND-3's Hensel step, GENHN-1's own
convention), with dv(A_j) ≥ (μ−j)κ for every j and the residual a perfect μ-th
power of a linear form. GENHN-4's recentering is Φ′ ↦ Φ′ + w, w := −ŝ·n(κ)-lift.

---

## S2. THE INTERFACE — what GENHN-4 layer 1 consumes, restated

GENHN's frozen layer-1 text, verbatim:

> "1. **Leading graded layer (the kills).** Pass to gr R = the twisted algebra
> K^c[ℤ/e₁-graded lines] (**COROLLARY W-9 @ accepted pin, consumed HERE as the
> organizing frame** — disclosed; GENHN-2 makes the passage pointwise-faithful:
> one K-slot per height, slot-min = valuation). In gr, the development transforms
> by the substitution Φ̄′ ↦ Φ̄′ + s̄ on the residual polynomial: the new residual
> at the node heights is R(T + s) where R(T) = (T − s)^μ, ALL of whose
> coefficients below degree μ vanish — the μ pins die. The twist powers z^{fl}
> (S4's cocycle: n(κ)^t = ẑ^{fl_t}n(tκ)) **enter both the residual assembly and
> the carry products IDENTICALLY**, so the graded identity holds coefficient for
> coefficient over K …" (`GENHN_PROOF` L873–884)

and the box that discloses it:

> "**GENHN-BOX-2 (the graded-frame consumption).** LEMMA GENHN-4's layer 1 at
> μ ≥ 3 consumes COROLLARY W-9's twisted algebra as the carry bookkeeping (unlike
> GENH4, which re-proved its μ = 2 instances elementarily). … An elementary
> general-μ carry display (the GENH4-S5 computation at general (e₁, f₁, μ)) would
> retire this box; until then the count laws at μ ≥ 3 stand on one accepted weld
> face." (`GENHN_PROOF` L1331–1338)

**Grep footprint.** `W-9` occurs in GENHN S6.1 at exactly one line (L874, quoted
above); layers 2 and 3 (L890–904) cite no weld label. GENHN-BOX-2's own wording
agrees ("layer 1"). So the interface to be discharged is **layer 1 and nothing
else**.

**What W-9 supplies, clause by clause** (`GRTJC_PROOF_2026-08-08.md` L1777–1788):

> "(i) c_{m+1} descends to a map ℤ/E × ℤ/E → K^* and **is a 2-cocycle** …; (ii)
> its y-degree part δ satisfies the integer cocycle identity … (the {0,1}-valued
> carry cocycle); (iii) 𝒜(T) is the **twisted group algebra** K^{c}[ℤ/E] of the
> abscissa-coset group over the carry cocycle; (iv) COR-4's monodromy is the
> **gauge period** …"

**And the two independent readings already on record.** From the weld notes
alone the discharge is impossible — `SG2_PROBE_2026-08-10.md` L25–29 returns
NEEDS-X, "GENHN-4 is NOT re-derivable from WELDMASTER (M0)–(M4) + WM-COB + J-D0
as those clauses are stated" — with the exact missing content

> "**X (the exact missing content, as returned):** … (ii) the recentering
> Φ′ ↦ Φ′ + w induces, in coherent normalized graded coordinates,
> res_{(μ−j)κ}(A_j^new) = [T^j] R(T+s) for 0 ≤ j ≤ μ" (`SG2_PROBE` L37–43).

This note delivers X(ii) — **and corrects it**: as literally stated, in *coherent*
normalized coordinates, X(ii) is FALSE at live ϑ (COROLLARY WZ-A′, §5 tooth).

---

## S3. THE FOLD

### S3.1 LEMMA WZ-1 (gr of the stage leaf field is a graded field)

> **LEMMA WZ-1.** For each m ∈ ℤ set
> gr(L)_m := {a ∈ L : dv(a) ≥ m}/{a : dv(a) > m} and gr(L) := ⊕_m gr(L)_m, with
> [a] := the class of a ∈ L^× in degree dv(a). Then:
> (a) gr(L) is a commutative graded ring, and [a][b] = [ab] ≠ 0 for all
>     a, b ∈ L^×;
> (b) every nonzero homogeneous element is invertible; in particular gr(L) has no
>     homogeneous zero divisors;
> (c) gr(L)_0 = k_L = ι(K), and **gr(L)_m is a 1-dimensional K-vector space for
>     every m ∈ ℤ** — "one K-line per dv-height".

*Proof.* (a) Well-definedness and distributivity are the ultrametric axioms;
[a][b] = [ab] is dv(ab) = dv(a) + dv(b), a valuation identity **on a field** —
there are no zero divisors to break it. (b) dv(a^{−1}) = −dv(a) and
[a][a^{−1}] = [1] ≠ 0 since 1 ∉ {dv > 0}. (c) gr(L)_0 = O_L/m_L = k_L; for
m ∈ ℤ pick any a with dv(a) = m (possible: dv(L^×) = ℤ), then for any b with
dv(b) ≥ m we have [b] = [b/a]·[a] with [b/a] ∈ gr(L)_0, so gr(L)_m = k_L·[a],
a K-line via ι. ∎

**Provenance.** This is **LEMMA GENTOW5-A0** (`GENTOW5_PROOF_2026-08-09.md`
L159–177, CERTIFIED 2026-08-12) run one level down, in exactly the frame GENHN's
own `[e2]` E3 erratum already re-aims at. GENTOW5's own point transposes
verbatim: "**pointwise, at a root, there is no carry problem.** Carries are a
phenomenon of the slot GRID … evaluation at x0 is blind to them" (L179–183). It
is re-proved above rather than cited because the tower note states it for L2 and
this note needs it for L; the proof is the same five lines and consumes nothing
tower-specific.

### S3.2 LEMMA WZ-2 (the level-1 ladder cocycle and the telescope unit)

> **LEMMA WZ-2.** With n(·) as in S1 and any integers a, b:
> (a) i(a) + i(b) = i(a+b) + e₁·wrap(a,b) and a(a) + a(b) = a(a+b) − h·wrap(a,b),
>     where wrap(a,b) := ⌊(i(a)+i(b))/e₁⌋ ∈ {0,1}; hence
>     **n(a)·n(b) = n(a+b)·(x^{e₁}/π^{h})^{wrap(a,b)}**, and
>     τ(a,b) := n(a)n(b)/n(a+b) satisfies the normalized 2-cocycle law
>     τ(0,a) = τ(a,0) = 1, τ(a,b)τ(a+b,c) = τ(b,c)τ(a,b+c).
> (b) dv(n(m)(θ)) = m exactly, so N := [n(κ)(θ)] is a K-basis of gr(L)_κ and is
>     invertible (WZ-1(b)).
> (c) Put **ϑ_t := res( n(κ)(θ)^t / n(tκ)(θ) ) ∈ K^×**. Then ϑ_0 = ϑ_1 = 1,
>     ϑ_{t+1} = ϑ_t·res(τ(tκ, κ)(θ)), and in **closed form at level 1**
>
>         ϑ_t = η^{W_κ(t)},        W_κ(t) := ⌊ t·i(κ) / e₁ ⌋ .
>
> (d) [n(tκ)(θ)] = N^{t}·ι(ϑ_t)^{−1} in gr(L)_{tκ}.

*Proof.* (a) i(a+b) is the reduction of i(a) + i(b) mod e₁ (both sides satisfy
·h ≡ a+b mod e₁ and gcd(h, e₁) = 1 makes the class determine i), which is the
first identity; the second follows from height additivity
i(m)h + e₁a(m) = m. The monomial identity is then immediate, and the cocycle law
is the two parenthesizations of n(a)n(b)n(c). (b) dv(n(m)(θ)) =
i(m)·dv(θ) + a(m)·dv(π) = i(m)h + e₁a(m) = m; WZ-1(c) makes gr(L)_m a K-line.
(c) Iterating (a), n(κ)^t = n(tκ)·(x^{e₁}/π^{h})^{W} with
e₁W = t·i(κ) − i(tκ), i.e. W = ⌊t·i(κ)/e₁⌋ because
i(tκ) ≡ t·i(κ) (mod e₁) and 0 ≤ i(tκ) < e₁. Evaluating at θ and taking residues,
res(θ^{e₁}π^{−h}) = η by definition, so ϑ_t = η^{W_κ(t)}; ϑ_0 = ϑ_1 = 1 since
W_κ(0) = W_κ(1) = 0. (d) is (c) rearranged in gr(L). ∎

**Provenance and non-novelty.** (a) and (c) are **T1 (C2-wrap)**,
**(C2-cocycle)**, **(C2-recursion)** and **(C2-level-1)**
(`T1_GAUGE_COCYCLE_2026-08-12.md` L160–250, ACCEPTED 2/2), instantiated at base
height **κ** in place of the tower's u₂. T1's proof of the closed form is uniform
in the base height — "reducing the x-exponent s·i(u₂) modulo e₁ gives exactly
⌊s·i(u₂)/e₁⌋ copies of x^{e₁}/π^h" (T1 L856–862) — a computation that reads only
i(base height); the instantiation is therefore verbatim, and it is re-derived
above so the fold is self-contained. The same wrap function W is already in
GENHN's own frozen text at a *different* site, the composed-key lift coefficients:

> "The normalizer monomials are not multiplicative —
> n̂(u₂)^{f₂−t} = n̂((f₂−t)u₂)·(x^{e₁}/π^{h})^{W(t)} with
> W(t) = ⌊(f₂−t)·i(u₂)/e₁⌋ — so the displayed coefficient's residue against S4's
> coherent normalizers (the ones (i)'s proof computes the side residual with) is
> **c_t·η^{−W(t)}, not c_t** … At any frame where each c_t·(η^{W(t)} − 1) = 0 —
> in particular at η = 1 — corrected = naive."
> (`GENHN_PROOF` dated erratum 2026-08-09 from HETOW PE1 F-1, L1988–2001)

That erratum is machine-confirmed at three η ≠ 1 frames. WZ-2 is that identity,
at the event slope instead of the stage height.

### S3.3 DEFINITION WZ-R (the two side reads) and THE CONVENTION PIN

> **DEFINITION WZ-R.** At a stage-α event (S1) define, for 0 ≤ j ≤ μ:
> * the **coherent digit** Γ_j := res( A_j(θ) / n((μ−j)κ)(θ) ) ∈ K when
>   dv(A_j(θ)) = (μ−j)κ, and Γ_j := 0 when dv(A_j(θ)) > (μ−j)κ (GENHN-2's slot
>   read; A_μ = 1 gives Γ_μ = 1);
> * the **multiplicative coefficient** c_j, read against n(κ)^{μ−j} instead:
>   [A_j(θ)] = ι(c_j)·N^{μ−j} when dv(A_j(θ)) = (μ−j)κ, and c_j := 0 otherwise;
> * the **twisted residual** R_Γ(T) := Σ_j Γ_j T^j and the **multiplicative side
>   residual** R_N(T) := Σ_j c_j T^j, both monic of degree μ over K.
>
> Then, by WZ-2(d),
>
>     **Γ_j = c_j · ϑ_{μ−j},   c_j = Γ_j · ϑ_{μ−j}^{−1}.**

This is **T1 (C3-dictionary)** (`T1` L273–286) at base height κ; T1 states the
three-read distinction as part of its carrier — "for a homogeneous slot of height
su_{i+1}, distinguish: 1. its **coherent digit** Γ, read against n̂_i(su_{i+1});
2. its **multiplicative coefficient**, read against n̂_i(u_{i+1})^s" (T1 L105–108).

> **THE CONVENTION PIN (WZ-PIN).** GENHN-4's hypothesis "residual (T − s)^μ,
> s ∈ K^×" (L868) is read as **R_N(T) = (T − s)^μ** — the MULTIPLICATIVE side
> residual.

**Why this reading and not the other.** Four corpus facts, none of them this
note's invention:

1. GENHN S4 says the side residual's coefficients are *twists of* the slot
   digits, not the slot digits: "Side residuals assembled with coherent
   normalizers are therefore **z-power-twisted slot digits** (the S6 computation
   and the battery's readers implement exactly this)" (L733–735). Under
   DEFINITION WZ-R that sentence is exactly c_j = Γ_j·ϑ_{μ−j}^{−1}.
2. GENHN's own HETOW erratum makes the same distinction explicit at the composed
   key and *chooses the multiplicative side*: the lifts are corrected precisely so
   that the residual's coefficients come out right — "for the slot residues to
   equal the residual's coefficients … the lifts need the correction absorbing
   that wrap factor" (L1992–1995).
3. GENTOW5 fixes the same convention one level up and says why it must be fixed:
   "This is the convention under which residual-of-product = product-of-residuals
   holds by A0 alone, and it is **the corpus's operative convention**"
   (`GENTOW5_PROOF` L200–207); "At mu2 >= 3 with a live cocycle the
   per-height-digit coefficient vector of a perfect power is NOT itself a
   perfect-power vector unless the vartheta_t are carried: **fixing the
   multiplicative convention is part of the box's content**" (L254–258).
4. **Only under this reading is GENHN-4 layer 1's conclusion true.** THEOREM WZ-A
   proves it; COROLLARY WZ-A′ and §5's battery show the twisted reading fails at
   live ϑ, at μ = 2 already in odd characteristic.

WZ-PIN is a *reading of frozen accepted text*, not an edit and not a theorem; it
is boxed at WZ-BOX-3 and is the hostile arc's first target.

### S3.4 THEOREM WZ-A (the level-1 substitution theorem — all μ pins die)

> **THEOREM WZ-A.** Stage-α event as in S1, with κ > D′h and
> **R_N(T) = (T − s)^μ**, s ∈ K^×. Let w := −lift(s; κ) be the exact-height lift
> at height κ (LEMMA GENHN-LIFT / T1 (C6): integral on the live domain κ > D′h,
> dv(w(θ)) = κ, res(w(θ)/n(κ)(θ)) = −s), put Φ′₊ := Φ′ + w, and let B_k be the
> coefficients of the polynomial-ring expansion f = Σ_k B_k Φ′₊^k (no reduction).
> Then for every k < μ
>
>     **dv( B_k(θ) ) > (μ − k)·κ** ,
>
> i.e. the height-(μ−k)κ class of B_k(θ) vanishes: **all μ pins die at the graded
> level**, and the new side's multiplicative residual is T^μ.

*Proof.* Since Φ′ = Φ′₊ − w,

    f = Σ_j A_j (Φ′₊ − w)^j = Σ_k B_k Φ′₊^k,
    B_k = Σ_{j ≥ k} binom(j,k)·(−w)^{j−k}·A_j .

Evaluate at θ. This is a sum of products **in the field L**, so WZ-1 applies
term by term. By hypothesis on the lift, [−w(θ)] = ι(s)·N. By DEFINITION WZ-R,
[A_j(θ)] = ι(c_j)·N^{μ−j} when c_j ≠ 0 and dv(A_j(θ)) > (μ−j)κ when c_j = 0.
Hence every term of B_k(θ) has dv ≥ (j−k)κ + (μ−j)κ = (μ−k)κ, and the class of
B_k(θ) in gr(L)_{(μ−k)κ} is

    [B_k(θ)]_{(μ−k)κ} = Σ_j binom(j,k)·ι(s)^{j−k}·N^{j−k}·ι(c_j)·N^{μ−j}
                      = N^{μ−k} · ι( Σ_j binom(j,k) s^{j−k} c_j )
                      = N^{μ−k} · ι( [T^k] R_N(T + s) ),

the last step the Taylor/substitution identity: if R(T) = Σ_j c_j T^j then
R(T + s) = Σ_k ( Σ_j binom(j,k) s^{j−k} c_j ) T^k, an identity of polynomials
valid over any commutative ring, **both characteristics**. The factorization of
N^{μ−k} out of the sum is legitimate because gr(L) is commutative with N
invertible (WZ-1(b)). By hypothesis R_N(T) = (T − s)^μ, so
R_N(T + s) = (T + s − s)^μ = T^μ and [T^k]R_N(T+s) = 0 for every k < μ. A
vanishing class at height (μ−k)κ is exactly dv(B_k(θ)) > (μ−k)κ. At k = μ the
same display gives coefficient 1, so the new side's multiplicative residual is
T^μ. ∎

**Two honesty riders, both inherited verbatim from GENTOW5-A's proof.**
(i) The identity is a **substitution** identity — the binomials appear
transiently, but the conclusion (T − s)^μ|_{T↦T+s} = T^μ is binomial-free; where
char q divides binom(μ, k) the corresponding coefficients vanish on **both**
sides, so nothing is lost (battery frame F4 exercises char 3 at μ = 3, and F8/F11
exercise char 2). (ii) WZ-A is a statement about the **polynomial-ring**
coefficients B_k. Passing to the canonical Φ′₊-development coefficients A_k^new
(degree < D′) is **GENHN-4's own layer 2**, unchanged and not re-proved here
(WZ-BOX-1). Note that WZ-A is *stronger* than GENHN's layer 1 at the seam: layer
1 handles only the exact-height leading parts and hands "the products
w^{i−j}A_i" to layer 2, whereas WZ-A's conclusion is about the full B_k, so the
residual obligation on layer 2 is only the mod-Φ′₊ reduction overflow (GENHN
L890–897, priced by GENHN-3(c)(d)).

### S3.5 COROLLARY WZ-A′ (sharpness — and the corrected form of SG-2's X(ii))

> **COROLLARY WZ-A′.** Drop the perfect-power hypothesis and put
> ρ_k := [T^k] R_N(T + s). Then for every k < μ:
> * ρ_k = 0 ⟹ dv(B_k(θ)) > (μ − k)κ (the pin dies);
> * ρ_k ≠ 0 ⟹ dv(B_k(θ)) = (μ − k)κ exactly, with coherent digit
>   **Γ_k^new = ϑ_{μ−k}·ρ_k** and multiplicative coefficient c_k^new = ρ_k.
>
> Equivalently, in the **coherent-digit** coordinates the recentering acts as the
> ϑ-CONJUGATED shift
>
>     R_Γ  ↦  D^{−1} ∘ shift_s ∘ D (R_Γ),      D := diag(ϑ_{μ−j}^{−1})_j ,
>
> **not** as shift_s. The two agree iff ϑ_t = 1 for every t with a live
> coefficient.

*Proof.* The display in WZ-A's proof, without the hypothesis; the digit statement
is DEFINITION WZ-R applied to B_k, using WZ-2(d). ∎

**Against SG-2's X(ii).** SG-2 asked for
"res_{(μ−j)κ}(A_j^new) = [T^j] R(T+s) … in coherent normalized graded
coordinates" (`SG2_PROBE` L40–43). In *coherent* coordinates that display is
**false at live ϑ**: the truth is Γ_k^new = ϑ_{μ−k}·[T^k]R_N(T+s), and
R_N = D(R_Γ) is not R_Γ. WZ-A′ is the corrected X(ii); it is delivered here, and
§5's battery fires precisely on the difference. This is why the fold is **not**
pure transcription and why the audit's CARRY pricing is one item short.

### S3.6 LEMMA WZ-3 (the convention change is a slot-wise unit character; counts are blind)

> **LEMMA WZ-3.** Fix a node and a slope κ. The dictionary of DEFINITION WZ-R is
> the diagonal map D_κ : (Γ_j)_j ↦ (c_j)_j = (Γ_j·ϑ_{μ−j}^{−1})_j, which
> multiplies the height-tκ slot digit by the fixed unit ϑ_t^{−1} = η^{−W_κ(t)}.
> Then:
> (i) ϑ depends only on (e₁, h, η, κ) — node and slope data — and **not on the
>     event**: within a fixed (node, κ) it is a constant;
> (ii) D_κ is a bijection of digit vectors fixing the top coefficient; it is a
>     **type-preserving** bijection in J-D0's sense, being a slot-wise **unit
>     character** (the multiplicative mode, not the additive one);
> (iii) hence for every s the fibre correspondence is exact:
>     #{Γ : R_Γ = (T−s')^μ for some s' ∈ K^×} = |K| − 1 =
>     #{Γ : R_N = D_κ(Γ) is (T−s')^μ for some s' ∈ K^×},
>     and more generally every count stratified by slot heights transports along
>     D_κ.

*Proof.* (i) is WZ-2(c): W_κ(t) = ⌊t·i(κ)/e₁⌋ reads only the slope's class and η
is genre data. (ii) D_κ is invertible with inverse diag(ϑ_{μ−j}); it acts within
each height's K-line, so it preserves which slots are occupied and each slot's
type; multiplication by a fixed unit is a character, which is exactly the mode
J-D0's blindness covers, and is exactly *not* the NONCHAR mode — "J-D0's proof
covers unit-character gauges; any value layer NOT of that form is outside the
perimeter — and the NONCHAR tooth displays the failure mode (**an additive slot
shift is a bijection that breaks type preservation, hence counts**)"
(`JD0_PROOF_2026-08-08.md` L222–225), with the general principle at L140–142:
"any slot-wise bijection preserving the condition preserves the count; **the unit
character does both**". (iii) both sets are the D_κ-images of {(T−s′)^μ : s′ ∈ K^×}, of
cardinality |K| − 1, and any count over a union of slot-height strata is
invariant under a bijection that fixes every stratum. ∎

**Consequence for GENHN's count laws.** The α-letter factor of GENHN-4 layer 3
("Letters: |K| − 1 choices of s per step", L903) and the bracket
b_{μ,K}(κ) = (|K|−1)|K|^{(c(μ)+1)κ−1} (L930) are **convention-blind**; only the
letter's *coordinate* changes, by s ↦ (the D_κ-reparameterization). This is
GENHN's own sentence — "counts are twist-blind (J-D0: (digits) ↦ (twisted digits)
is a type-preserving bijection), LETTERS are twisted" (L736–737) — made exact at
μ ≥ 3, and it is the reason WZ-PIN costs nothing downstream even though it is a
real content decision.

### S3.7 COROLLARY WZ-B (GENHN-4 layer 1 without COROLLARY W-9)

> **COROLLARY WZ-B.** Under WZ-PIN, LEMMA GENHN-4's layer-1 conclusion holds at
> general (e₁, f₁, μ) with gr(O_L) as the organizing frame and **no clause of
> COROLLARY W-9 consumed**. Explicitly:

| W-9 clause | what layer 1 would take from it | in-cone replacement | class |
|---|---|---|---|
| (i) the K^×-valued 2-cocycle c, "on the gr side … associativity of gr(w)'s multiplication" | a graded ring in which the residual assembly is associative/multiplicative | **WZ-1**: gr(L) is a commutative graded ring with all nonzero homogeneous elements invertible; associativity is free. The concrete cocycle is τ(a,b) with its normalized law, **WZ-2(a)** — a monomial identity | elementary |
| (ii) the {0,1}-valued integer carry cocycle δ | the integer wrap bookkeeping | **WZ-2(a)**: wrap(a,b) = ⌊(i(a)+i(b))/e₁⌋ ∈ {0,1} with the additive identity, from i(a)+i(b) = i(a+b) + e₁·wrap | elementary |
| (iii) 𝒜(T) = K^{c}[ℤ/E] with dim_K 𝒜(T) = E | "one K-line per dv-height" | **WZ-1(c)** + **LEMMA GENHN-2**. GENHN already records the implication in this direction: "W-9's dim_K 𝒜(T) = E = e₁ is **this statement's graded shadow** (cited as frame)" (L716–717) | already in-cone |
| (iv) monodromy = gauge period, ζ_T = ∏ c(1,k) | — | **not consumed**: no occurrence in S6.1. The wrap letter layer 1 actually uses is ϑ_t, **derived** at WZ-2(c) from the monomials | n/a |

*Proof.* THEOREM WZ-A proves the layer-1 conclusion outright from WZ-1, WZ-2,
GENHN-2, GENHN-LIFT and DEFINITION WZ-R; none of these is a weld object. ∎

**Method note, stated because it matters for the audit.** WZ-B does **not**
re-derive GENHN's layer-1 *prose* from weld-free inputs — that is what SG-2 tried
and found impossible. It proves layer 1's *conclusion* from scratch. Any unstated
content hiding inside "the organizing frame" is therefore **bypassed**, not
audited. That is the honest description of what a replacement proof does, and it
is also why SG-2's obstruction 1 (perimeter re-instantiation into WELD-M's domain)
is **dissolved rather than discharged**: this route never enters WELD-M's domain,
so there is nothing to instantiate. Dissolution is legitimate here only because
the conclusion is independently proved; it would be illegitimate as a way of
skipping an input the proof still needs.

---

## S4. THE JD0-LEAK SITE, TREATED

The audit's LEAK-1 (`WELD_FACE_AUDIT.md` L143–164) is this note's site:

> "**LEAK-1 (LIVE, disclosed) — LEMMA GENHN-4 layer 1 at μ ≥ 3.** GENHN-4 is a
> **count** lemma … Its layer 1 does not route through J-D0; it imports the W-9
> graded frame and a letter-layer cocycle identity as premises … The recentering
> it studies (Φ′ ↦ Φ′ + w) is an **additive** shift — precisely the NONCHAR
> failure mode, so J-D0's perimeter cannot cover it even in principle."

**The fold splits the site in two and covers both halves.**

**(a) The additive half — COVERED by proof, not by J-D0.** The content SG-2 says
J-D0 cannot supply — "J-D0 is count-layer blindness and cannot substitute for the
event-level coefficient identity R(T+s) = T^μ" (`SG2_PROBE` L64–66) — is
**THEOREM WZ-A**, proved in gr(L) from the ultrametric axioms and a polynomial
substitution identity. J-D0 is not invoked for it, so its NONCHAR perimeter is
not being stretched. The leak closes because the premise is supplied, not because
the perimeter is widened.

**(b) The multiplicative half — INSIDE J-D0's perimeter, with the bijection
exhibited.** The residual-convention change is the diagonal unit rescaling D_κ,
i.e. a **slot-wise unit character** — the very mode `JD0-BOX-2` describes ("the
weld's ENTIRE conditional value dictionary acts by slot-wise unit characters",
`JD0_PROOF` L219–221) and the opposite of the NONCHAR mode. LEMMA WZ-3
**exhibits** the bijection (multiply the height-tκ digit by η^{−W_κ(t)}) rather
than citing it, so this note joins the corpus's ten CLEAN routing sites
(`WELD_FACE_AUDIT.md` L193–202) rather than the leak list, and **adds nothing to
`JD0-BOX-2`'s inherited perimeter** (`HYP.74`), which two spine notes carry for
the *general framing* only.

**Net.** LEAK-1 is retired at its own site: half proved, half exhibited. LEAK-2
was already retired by GENTOW5 at the tower level ("no W-9-style weld face is
consumed", `GENTOW5_PROOF` L157). LATENT-1 (GENIND-5's stage recentering) and
LATENT-2 (GENIND (OB-b)) sit inside GENIND's boxed obstruction and are
**untouched** by this note — but note for the record that a future discharge of
GENIND-5 can now inherit WZ-A rather than W-9, which is a strictly better
geography than the audit priced.

---

## S5. TEETH — the battery

**Instrument.** `verification/openmath/weldzero_checks.py` (md5 in §9), output of
record `verification/openmath/weldzero_checks_output.txt`. Pure exact integer /
`Fraction` arithmetic, no external dependencies, exit 0 iff every check passes.

**Design (the point is decorrelation).** The *prediction* engine and the
*measurement* engine share nothing:

* **prediction** — F_p[T] arithmetic: compute ϑ_t, form R_N from the requested
  convention via WZ-R, compute ρ_k = [T^k]R_N(T+s), and preregister for each
  k < μ either "pin dies" or "pin survives at height (μ−k)κ with coherent digit
  ϑ_{μ−k}ρ_k" (COROLLARY WZ-A′);
* **measurement** — build f = Φ′^μ + Σ A_j Φ′^j in ℤ[x] with the prescribed
  pins, recentre by Φ′₊ = Φ′ + w, and obtain the *canonical* development
  coefficients by **actual repeated polynomial division** (the GN-REFINE3
  pattern: no formula), then read dv and the digit off the coefficients;
* **a third, decorrelated valuation leg** — every dv used is cross-checked
  against e₁·v_p(Res(Φ, A))/D′ computed from the Sylvester determinant, so the
  GENHN-2 slot-min formula is never trusted on its own;
* ϑ itself is computed twice: from the raw monomials, and from T1's closed form
  η^{⌊t·i(κ)/e₁⌋}.

Because the measurement passes through the canonical development, the battery
tests **layers 1 and 2 jointly** — WZ-A plus GENHN-4's own layer 2 — at μ ≥ 3.

**Frames.** f₁ = 1 (as GENHN's own sealed battery, GENHN-BOX-3), e₁ ∈ {2,3},
h ∈ {1,3}, μ ∈ {2,3,4}, p ∈ {2,3,5,7}, κ > D′h at every frame. Each frame is run
**twice**: once with the side built so its *multiplicative* residual is (T−s)^μ,
once with the side built so its *twisted* residual is (T−s)^μ.

| frame | p | e₁ | h | η | μ | κ | ϑ_{0..μ} | multiplicative | twisted |
|---|---|---|---|---|---|---|---|---|---|
| F1 live-wrap | 5 | 2 | 1 | 2 | 3 | 3 | 1,1,2,2 | **all pins die** | pins k=0,1 survive |
| F2 η = 1 control | 5 | 2 | 1 | 1 | 3 | 3 | 1,1,1,1 | all die | all die |
| F3 i(κ) = 0 control | 5 | 2 | 1 | 2 | 3 | 4 | 1,1,1,1 | all die | all die |
| F4 char \| binom | 3 | 2 | 1 | 2 | 3 | 3 | 1,1,2,2 | **all die** | pin k=0 survives |
| F5 e₁ = 3 | 7 | 3 | 1 | 3 | 3 | 4 | 1,1,1,3 | **all die** | pin k=0 survives |
| F6 μ = 4 | 5 | 2 | 1 | 2 | 4 | 3 | 1,1,2,2,4 | **all die** | pins k=0,1,2 survive |
| F7 h = 3 | 5 | 2 | 3 | 2 | 3 | 7 | 1,1,2,2 | **all die** | pins k=0,1 survive |
| F8 wild p = 2, μ = 4 | 2 | 2 | 1 | 1 | 4 | 3 | 1,1,1,1,1 | all die | all die |
| F9 e₁ = 2, p = 7 | 7 | 2 | 1 | 3 | 3 | 5 | 1,1,3,3 | **all die** | pins k=0,1 survive |
| F10 **μ = 2, odd char** | 5 | 2 | 1 | 2 | 2 | 3 | 1,1,2 | **all die** | pin k=0 survives |
| F11 μ = 2, char 2 | 2 | 2 | 1 | 1 | 2 | 3 | 1,1,1 | all die | all die |

Plus **negative control N1**: multiplicative residual (T−1)²(T−2) at μ = 3 (not a
perfect power) — pins must NOT all die, and indeed ρ = (0,0,4), pin k = 2
survives with the predicted digit. Plus **sweep C**: ϑ_t = η^{⌊t·i(κ)/e₁⌋} against
the monomial computation over p ∈ {2,3,5,7}, e₁ ≤ 4, h ∈ {1,2,3,5} coprime to e₁,
all z, twelve slopes each, t ≤ 4 — **9,295** points.

**Result: 9,877 checks, 0 violations, exit 0.** Every preregistered kill and
every preregistered survivor (height *and* digit) matched.

**The three findings the battery delivers.**

1. **THEOREM WZ-A survives contact.** Under WZ-PIN all μ pins die at every frame,
   by actual re-division, including char q | binom(μ,k) (F4), wild p = 2 (F8,
   F11), e₁ = 3 (F5), h > 1 (F7), μ = 4 (F6).
2. **The conventions genuinely separate, and exactly when ϑ is live.** Separation
   at F1, F4, F5, F6, F7, F9, F10; agreement at F2 (η = 1), F3 (i(κ) = 0), F8 and
   F11 (q = 2 at f₁ = 1 forces η = 1). The preregistered biconditional
   "separation ⟺ ϑ live" is checked as its own assertion and holds at 11/11.
3. **The separation is already live at μ = 2 in odd characteristic** (F10) — it is
   not a μ ≥ 3 novelty. In char 2 at μ = 2 it is provably absent (F11), because
   every K-element is a square and the root re-parameterizes — GENTOW5's own
   remark, "which is why the mu2 = 2 battery rows never saw the convention"
   (`GENTOW5_PROOF` L252–254), verified one level down.

**Coverage disclosure (and a finding about the corpus's own coverage).** Every
sealed GH-REFINE row of GENH4 — the accepted μ = 2 pointwise verification, 192,000
refine events — is either genre E at q = 2 ("consE (Zp,2,6,h1) + (Fqt,2,6,h1)",
`GENH4_PROOF` L985–988), where η = 1 forcibly, or genre F ("consF (Zp,2,7,k1)"),
where e₁ = 1 and hence i(κ) = 0. GENHN's tower rows likewise sit "at q = 2
(Φ′ = x² − 2, K = F₂), **where η = 1 forcibly**" (`GENHN_PROOF` L2027–2029).
**So no sealed row in the accepted corpus exercises a live level-1 wrap at a
refine event**, and F1/F4/F5/F6/F7/F9/F10 above are the corpus's first machine
contact with one. That is a fact about coverage, not a defect claim: the frozen
notes' *conclusions* are the ones WZ-A proves, and WZ-3 shows the counts they
carry are convention-blind either way.

---

## S6. HONEST BOXES

* **WZ-BOX-1 (scope: layers 2 and 3).** This note proves layer 1 only. GENHN-4's
  layer 2 (corrections land strictly deeper; the mod-Φ′₊ overflow priced by
  GENHN-3(c)(d)) and layer 3 (triangularity ⟹ injectivity; cardinality ⟹
  surjectivity) are **unchanged, not re-proved here**, and carry GENHN's own
  grade. Their weld-freedom is established two ways, neither of them this note's
  work: by inspection of the frozen text (no weld label occurs in GENHN L890–904)
  and by SG-2's independent CHECKED-CLEAN list — "layer 2 from GENHN-2 +
  GENHN-3(c)(d) + classical filtered arithmetic; layer 3 from GENHN-2 +
  GENHN-3(d) + J-D0 + triangular-map cardinality, CONDITIONAL on layer 1"
  (`SG2_PROBE` L28–31). The conditionality of layer 3 on layer 1 is discharged by
  WZ-A.
* **WZ-BOX-2 (machine coverage).** The battery is f₁ = 1 only. At f₁ ≥ 2 the
  coherent digit is a K-digit assembling f₁ O-digits (GENHN-2), so realizing a
  prescribed Γ_j needs the exact-height (LIFT) operator rather than one monomial;
  the proof is uniform in f₁ (WZ-1 and WZ-2 never split by f₁, and GENHN-LIFT/T1
  (C6) supplies the lift on κ > D′h), but no f₁ ≥ 2 row is run. Mixed
  (e₁ ≥ 2, f₁ ≥ 2) is first live at n = 8 — machine-unreachable here, GENHN-BOX-3's
  own gap, inherited. **A live-wrap f₁ = 2 frame is the first fresh leg the
  hostile arc should demand.**
* **WZ-BOX-3 (WZ-PIN is a reading).** The convention pin is a reading of GENHN's
  frozen sentence at L868, supported by four corpus facts (§3.3) but asserted by
  no accepted note *in those words*. If the arc rejects the reading, WZ-A stands
  unchanged as a theorem, but the fold becomes a **correction** to GENHN-4's
  displayed hypothesis rather than a re-proof of it, which is a statement-fence
  matter and not this unit's authority to settle.
* **WZ-BOX-4 (the bypass, restated as a box).** WZ-B replaces layer 1's
  conclusion, not layer 1's prose. If GENHN's phrase "the organizing frame"
  carries content beyond W-9(i)–(iv) that some *other* consumer reads back out of
  layer 1, this note does not see it. Grep evidence that there is no such
  consumer: HE3 declines GENHN-4 outright ("GENHN.A(iv)/LEMMA GENHN-4 is NOT
  consumed, hence the W-9 graded weld face (GENHN-BOX-2) is NOT inherited",
  `HE3_PROOF_2026-08-08.md` L79–88), HE6/HE7 likewise, and GENTOW5 retires the
  tower analogue.
* **WZ-BOX-5 (not the σ box).** `[GENHN-HE(μ ≥ 3)]` / `HYP.81` — the σ-decision at
  stage leaves of multiplicity μ ≥ 3, "THE one load-bearing open lemma of the
  whole composite program" (`GENHN_PROOF` L1452–1453) — is **untouched**. It is
  live at the same threshold (n = 6) and keys on the same μ ≥ 3, which makes it
  easy to conflate with `HYP.148`; the ledger already fences the two
  (ledger v5 L1216: "`[GENHN-HE(μ≥3)]` is a σ-decision obligation on stage leaves
  and `GENHN-BOX-2` is a weld-face consumption inside a count law"). Retiring a
  weld face is not progress on the σ box.
* **WZ-BOX-6 (grade).** 0/2. GENTOW5, T1, GENHN and GRTJC are all accepted or
  certified at HEAD, so unlike the audit's caveat (b) this fold inherits **no**
  0/2 arc from its suppliers (the ledger already corrects that caveat at
  `HYP.149`); the 0/2 is this note's own.

---

## S7. CONSUMPTION IMPACT (exact ledger effect, IF the arc accepts)

**Ledger state read for this section:** `spec/HYPOTHESIS_LEDGER.md` at **v5**,
commit `1d787a7d`, md5 `92827c7c81e264fa379950b94690d0d0`, 1,692 lines (the v5
fold landed from a concurrent session *during* this composition; §7 is written
against v5, not the v4 the WELD-FACE-AUDIT folded). v5's frozen Display A already
records this unit as running: "`WELD-ZERO unit launched to retire both
(HYP.149, IN-PROGRESS)`" (L1334–1336).

**Rows that retire.**

* **`HYP.148` (`GENHN-BOX-2` / the W-9 graded face)** — **DISCHARGED**. Its
  statement is "LEMMA GENHN-4's layer 1 at μ≥3 consumes COROLLARY W-9's twisted
  algebra as the carry bookkeeping rather than re-deriving it". COROLLARY WZ-B
  re-derives it, clause by clause, from gr(O_L) + GENHN-2 + T1's level-1 cocycle
  + GENHN-LIFT. The box's own retirement condition — "an elementary general-μ
  carry display (the GENH4-S5 computation at general (e₁, f₁, μ))" — is met by
  THEOREM WZ-A plus the layer-2/3 disposition of WZ-BOX-1.
* **`HYP.139` (the W-1 transport, harness w and 𝑅 → FGMN objects, ATTEMPT)** —
  **RETIRES FROM THE CONE** with it, exactly as the ledger predicts
  ("the replacement frame is gr(O_L), not an FGMN object, so the W-1 transport
  would no longer be consumed", ledger v5 row `HYP.139` @ L1079–1084). Verified against
  this fold: **no step of WZ-1, WZ-2, WZ-R, WZ-A, WZ-A′, WZ-3 or WZ-B names an
  FGMN object**; the only rings that occur are L, O_L, gr(L) and Ô[x]. The row
  stays live *inside GRTJC's own note* — this fold retires its **cone
  consumption**, not the obligation.
* **`HYP.149` (the proposed route, PROPOSED-NODE)** — **CLOSED**: this unit is
  its unit. Note for the fold record that HYP.149's own summary of the route is
  one item short: it prices the work as "one fold unit rewrites GENHN-4 layer 1 in
  the A0/A1 idiom", with no mention of the convention obligation, which §3.3/§5
  show is load-bearing.

**Rows that do NOT move.** `HYP.74` (`JD0-BOX-2`) — unchanged; this note consumes
J-D0 at FRAME grade with the bijection exhibited inline (§4b), so it neither
inherits nor enlarges BOX-2's perimeter. `HYP.81` (`[GENHN-HE(μ≥3)]`) — unchanged
(WZ-BOX-5). `HYP.142` (`GENHN-ERR-1`) — unchanged and **not** brushed: WZ-A needs
only κ > D′h, which is the *corrected* node floor GENHN already carries
(L2306–2310), so no S-for-D′h substitution enters this fold. `HYP.144`
(`GENTOW4-BOX-2`) — unchanged.

**Conditionality-display effect.** The v5 frozen Display A carries the weld block
as two conjuncts (L1332–1336), both now undaggered:

```
  ∧ JD0-BOX-2             -- unit-character gauge-form perimeter, inherited FRAME-grade;
                          --   undaggered at v5: machine grade SIGNED as the bar (Q6, HYP.39)
  ∧ (GENHN-BOX-2 if n≥6)  -- the W-9 graded face in GENHN-4 layer 1 at μ≥3;
                          --   carries HYP.139 (W-1 transport, ATTEMPT).
                          --   WELD-ZERO unit launched to retire both (HYP.149, IN-PROGRESS)
```

On acceptance the **second conjunct is DELETED** (with its HYP.139 rider), and the
weld block becomes the single line

```
  ∧ JD0-BOX-2             -- FRAME-grade only; every consuming site exhibits its own bijection
```

i.e. **the weld layer's contribution to the capstone's conditionality goes from
one face inside a count law to zero**, leaving one frame-grade perimeter that no
count law consumes. That is the whole point of the unit, and it is exactly what
the audit forecast ("one fold unit would take the weld layer's contribution to the
capstone's conditionality from one face down to zero, and would retire HYP.139
from the cone with it", `WELD_FACE_AUDIT.md` L488–490).

---

## S8. NON-CIRCULARITY AUDIT

The fold must not re-consume, through a back door, the face it replaces.

1. **The cocycle.** WZ-2(c) uses the wrap letter η and the identity
   n(κ)^t = n(tκ)·(x^{e₁}/π^h)^{W}. This is derived here from the monomials
   (WZ-2(a)) and is independently GENHN's own derivation: "Products wrap through
   θ^{e₁} = (unit lift)·π^h: n(κ)^t = ẑ^{fl}·n(tκ) with the explicit integer
   fl = the wrap count — W-9's cocycle, **literal**" (`GENHN_PROOF` L731–733).
   GENHN **derives** it and only **identifies** it with W-9's; the identification
   is a remark, not an input.
2. **T1.** T1 (C2)/(C3) is cited as an accepted supplier. T1's own weld footprint
   is fenced: "the weld is used only at the proved scope of the five-field package
   WELD-M-PKG; **no cross-face coherence or uniqueness is asserted**" (T1 L13),
   and the audit's own verdict on WELD-M-PKG is that it is consumed on the spine
   as **naming only** (`WELD_FACE_AUDIT.md` L124). Moreover WZ-2 is re-derived in
   full above, so T1 is corroboration, not load.
3. **GENTOW5.** Cited as provenance for WZ-1; WZ-1 is re-proved in five lines from
   the ultrametric axioms. GENTOW5's own disposition confirms the direction: "No
   W-9-analogue weld face is consumed; the graded frame is gr(L2) itself"
   (`GENTOW5_PROOF` L358–359).
4. **GENHN-2 and GENHN-LIFT.** Both are GENHN-internal and elementary; GENHN-2
   states its own weld independence — "this is JC-LOAD's no-cancellation holding
   BY CLASS SEPARATION + RESIDUE-FIELD INDEPENDENCE — **elementary, hence not
   consumed**" (L714–716) — with W-9's dimension count as its *shadow*, not its
   source (L716–717).
5. **J-D0.** Consumed at FRAME grade only, with the bijection exhibited (§4b).

**No step of the fold cites GRTJC, WELDMASTER, GRTJA, GRTJB, GRTW2, SIGMALAW,
LIFTCORNER or DULEMMA.**

---

## S9. STATUS, PINS, AND THE HOSTILE ARC

**STATUS: composed, attempt 0/2, hostile arc owed** (two clean passes; per the
standing RCT protocol at least one model-diverse). No note edited; no grade moved;
nothing in the ledger changed by this note — §7 is a *proposal* for the next fold.

**Pins (all read at HEAD `f5841548853fe2e07b4149a109b7968d6c6b6cd7`).**

| source | md5 | what is cited |
|---|---|---|
| `lean/notes/openmath/GENHN_PROOF_2026-08-08.md` | `98a0bb5bf470556eb3b1e1e51248ff8a` | S6.1 L865–921; `[e2]` E3 L470–527 (esp. L500–517); S4 L699–737; GENHN-BOX-2 L1331–1338; HETOW erratum L1980–2032; occupied-height corrigendum L2306–2310; GENHN-LIFT L2103 |
| `lean/notes/openmath/GENTOW5_PROOF_2026-08-09.md` | `30f0f889df69908441ea9da04038d8ed` | S1.1 A0 L159–183; S1.2 A1 + convention consequence L185–261; S1.3 A L263–325; S1.4(ii) level-1 rider L341–349; S1.5 L351–365 |
| `lean/notes/openmath/T1_GAUGE_COCYCLE_2026-08-12.md` | `ebc2b21a274d12fa117959b093237405` | §1.1 three reads L105–111; (C2) L155–269; (C3-dictionary) L271–286; (C6) L621–688; proof §2.2 L816–862, §2.3 L864–886 |
| `lean/notes/openmath/GRTJC_PROOF_2026-08-08.md` | `2669aeaeb5b2bd2654f23c1faa4f09bf` | COROLLARY W-9 L1777–1799 |
| `lean/notes/openmath/SG2_PROBE_2026-08-10.md` | `59ce5b81f9635a5d71da08ed693b9029` | verdict L23–35; X L37–46; obstructions L48–66; checked-clean L68–75 |
| `runs/qgen/WELD_FACE_AUDIT.md` | `8a43b27b25c10f53363d08592fd584d9` | §3 LEAK-1 L143–164; §4 L219–283; §6 L460–490 |
| `lean/notes/openmath/JD0_PROOF_2026-08-08.md` | (read at HEAD) | S2.2 L140–142; `JD0-BOX-2` + the NONCHAR failure mode L219–225 |
| `lean/notes/openmath/HE3_PROOF_2026-08-08.md` | (read at HEAD) | "NOT consumed (deliberate)" L79–88 |
| `lean/notes/openmath/GENH4_PROOF_2026-08-08.md` | (read at HEAD) | GH-REFINE row list L985–988 |
| `spec/HYPOTHESIS_LEDGER.md` **v5** @ `1d787a7d` | `92827c7c81e264fa379950b94690d0d0` | HYP.139 L1079–1084; HYP.148 L1146; HYP.149 L1153; the 148/81 fence L1216; frozen Display A L1322–1346 |
| `verification/openmath/weldzero_checks.py` | `220388206f70dda9a87456e52da3fe5d` | the battery |
| `verification/openmath/weldzero_checks_output.txt` | `271a784bb315afd557ebe902820738d5` | output of record, 9,877/0, exit 0 |

Grades at HEAD used above: T1 ACCEPTED 2/2 (its own header, L6); GENHN, GENTOW5
CERTIFIED 2026-08-12 (`docs/SOL_CERTIFICATION_LEDGER_2026-08-12.md` rows 13, 10);
GRTJC W-6..W-9 ACCEPTED 2/2 @ 83f47f4.

**The hostile arc's target list, ranked (written for the attacker, not for the
defender).**

1. **WZ-PIN (WZ-BOX-3).** Attack the reading. If GENHN-4's residual is the
   twisted one, WZ-A is a correction and not a re-proof. The strongest attack is
   to find a spine site that reads GENHN-4's residual in coherent digits and
   propagates it.
2. **The f₁ ≥ 2 live-wrap frame (WZ-BOX-2).** Demand a battery row at
   (e₁, f₁) = (2,2) or a genre-F variant with a live wrap. If the (LIFT)-realized
   side behaves differently, the fold's uniformity claim in f₁ fails.
3. **WZ-A's Step-A/layer-2 seam.** WZ-A subsumes part of GENHN's layer 2 (§3.4
   rider ii). Check that the remaining layer-2 obligation (mod-Φ′₊ overflow) is
   genuinely covered by GENHN-3(c)(d) as GENHN claims, at μ ≥ 3 and mixed
   (e₁, f₁ ≥ 2) — GENHN's own grade note flags exactly this: "A hostile pass
   should attack layer 2's strictness at mixed (e₁, f₁ ≥ 2) genres (first live
   n = 8 …)" (L913–915).
4. **WZ-3(iii)'s count claim.** Verify that *every* count GENHN-4 feeds is
   stratified by slot heights (so D_κ-invariant), not by a residual-coefficient
   predicate that is convention-sensitive in some other way.
5. **The bypass (WZ-BOX-4).** Hunt for a consumer that reads something back out
   of layer 1's frame beyond its stated conclusion.
6. **Battery independence.** Re-run `weldzero_checks.py` from a fresh
   implementation of the development (the current one shares `pdivmod_monic`
   between construction and measurement, which is a genuine, disclosed
   correlation, mitigated but not removed by the resultant leg).

---

**Record rule.** Every quotation above is verbatim at the pinned md5 and was
grep-verified at composition. The one synthesis this note contributes beyond the
audit's — the convention pin WZ-PIN and its count-blindness LEMMA WZ-3 — is
labelled as this note's own, and the fold is offered at attempt grade: a
**candidate discharge** of `HYP.148`/`HYP.139`, not a discharge, until two clean
hostile passes land.
