# RMENGINE-PC — Codex round-1 content pass (note @bf18bfe): NOT-CLEAN, 1C/4G

THE FIRST GENUINE MATHEMATICAL FINDINGS IN THE CLOSURE ARC (adjudication): C1 [CRITICAL] DIGIT-SPLIT without coefficient-valuation extraction — the pi-child increase holds only for primitive inputs; the missing step is normalize (factor pi^{w0(b)+w0(c_d)}) -> split primitives -> restore; load-bearing for L-LINE(iii)/DEV-(m). G2: the floor-form derivation graded PROVED-generic is heuristic (missing quotient-cell-to-pool induction; L-MU T-scope leak into a generic grading). G3: claims table misses >= 3 rows + C2 under-records monicity/degree hypotheses. G4: COR-A stated unconditional in S7.1 vs at-acceptance in S10/S11 + the n=1,2 exact-union slip. G5: the lambda/t-chain undefined off-grid — needs the partial-chain/first-failure definition + a converse-integrality lemma L-ZMULT does not provide. DISPOSITION: repair unit (math work, not brackets) dispatches AFTER the PE leg lands — PE's from-note reimplementation adjudicates whether C1 is text-gap (code normalizes) or mechanism-gap (counterexample).

## Codex output (verbatim)

I’m auditing the proof text as written, including every S11 charge. I’ll treat the concurrent PE execution as out of scope and classify only content defects or unsupported proof steps.
1. **CRITICAL — DIGIT-SPLIT is applied without extracting coefficient valuations.** S2.1 says coefficient products are split directly by
   > “DIGIT-SPLIT … splits each coefficient product into the L-child … the π-junk child”

   and L-LINE(iii) consequently claims
   > “the π-child raises \(w_0\) by ≥ 1.”

   DIGIT-SPLIT only gives that increase relative to primitive inputs. For example, with \(b=\pi\) and \(c_d=1\), the displayed split has zero L-child and π-child \(\pi X=\pi\), whose valuation equals \(w_0(bc_d)=1\), not one more. Cells permit arbitrary \(w_0(b)\), and no step factors \(\pi^{w_0(b)+w_0(c_d)}\), applies DIGIT-SPLIT to the normalized coefficients, and restores that factor. Thus line monotonicity, the designation of π-children as junk, and DEV-(m) read transparency do not follow for the stated tree. This is load-bearing for RM-GEN.

2. **GAP — the advertised generic floor-form derivation is only a heuristic.** S6.2 asserts:
   > “the maximal promotion count into level \(l\) is the floor \(\lfloor pool/e_{l-1}\rfloor\)-form”

   but supplies no induction connecting the actual second-round quotient cells to the stated pools, no proof that correction/rebuild paths contribute exactly the recursive \(c_{l-1}\), and no support-level containment argument before residue non-vanishing is separated out. Moreover, it invokes “carrying \(\mu\) strictly down (L-MU)” while L-MU is proved only on T-scope, whereas S8 row 11 grades the floor-form derivation “PROVED, generic.” L-CAP itself is correct, including \(e=1\); the claimed exact floor-form derivation is not established.

3. **GAP — the dependency-claims table is not exhaustive as chartered.** It says:
   > “every ‘only/never/nothing/free/self-contained/automatic’ claim … enumerated”

   but omits, among others, S1’s
   > “Sealed artifacts … instance evidence and design input only, never proof steps,”

   S1’s “the ONLY bend,” and S6.2’s “exponent mass crosses … ONLY along the promotion edge.” In the requested six-row spot check, C5, C7, C9, and C11 agree with the displayed proof; C2 underrecords monicity and the input-degree hypotheses needed by division; C13 is correct for a single engine call but does not justify the claimed low-rung “exact union” discussed below. The table is therefore not machine-census-ready.

4. **GAP — COR-A’s acceptance status and low-rung scope accounting are inconsistent.** S7.1 presently declares
   > “CONSEQUENCE: (ITER-LAW-n) … is UNCONDITIONAL on T-class(n) … for every \(n\ge1\),”

   while S10/S11 say the ledger upgrade is
   > “at acceptance only”

   and the note still has acceptance counter \(0/2\). The “exact union” assertion is also false literally for \(n=1,2\): the family \(2\le m\le n-1\) is empty there, whereas the displayed T-class still imposes \(e_0\ge2\lor d_0=1\). The restricted mathematical implication remains valid, but the promised acceptance conditionality and exact scope characterization are not displayed honestly.

5. **GAP — the λ/t-chain is undefined on precisely the off-grid objects later quantified over.** S3.1 recursively defines
   > “\(\lambda_{l-1}:=(\lambda_l-j_{l-1}\gamma_l)/e_{l-1}\)”

   and then applies the integer-valued \(s_{l-1}(\lambda_{l-1})\) at the next step. If a cell first fails the grid congruence, \(\lambda_{l-1}\) need not be integral, so the remaining chain and the condition “\(t_l\in\mathbb Z\) for every \(l\)” are not defined. DEV-(m)’s off-grid case nevertheless reasons about the level where failure occurs and applies L-ZMULT’s converse integrality direction, which L-ZMULT does not state or prove. A partial-chain/first-failure definition and converse lemma are missing.

RMENGINE-PC FINDINGS: 1 critical, 4 gaps
VERDICT: NOT-CLEAN
