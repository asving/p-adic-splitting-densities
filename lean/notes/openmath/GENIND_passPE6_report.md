# GENIND pass PE6 — HOSTILE VERIFICATION REPORT (fresh context, sixth arc pass; the r5 text — the f-first entry law + the standing-rule sweep — attacked for the first time)

**VERDICT: UNREFUTED, NOT CLEAN. 0 CRITICAL, 1 GAP, 1 MINOR.** The
2-clean count does NOT start (stays 0/2; an r6 is owed, then the
count begins afresh). The r5's MATHEMATICS survived everything I
could throw at it — my fresh route (§7; disjoint from all seven
prior routes) executed the charge the r5 itself disclosed as open:
the SAME-SIDE F-FIRST genre (the 6k entry law's own genre, admitted
at S15 as "machine-unrealized corpus-wide") is now MACHINE-REAL,
exhaustively (2,097,152 states at its k = 1 cell, plus its
first-visible window, its N = 6 floor, its k = 2 cell, both
characteristics) — and the 6k law, the 2k + 4h distinct-side law,
the three-family m = 6 grammar (which I re-derived independently
before running), and a NEVER-REALIZED cap regime (a composite
f-first co-block STEEPER than the extracted child) all came out
exactly on the note's numbers: **GREEN, 2,132,082 checks, 0
violations, single run, no smoke.** The one GAP is the arc's
record-integrity defect class for the FIFTH consecutive round
(PE2-G2 → PE3-G1 → PE4-G1 → PE5-G1 → **PE6-G1**), and this time the
false sentence went THROUGH the r5's standing-rule sweep and came
out still false: S14 C14's [r5 sweep] rewrite claims the S9.3
supplier stack was "each git-log-checked at commit time, none moved
from its pin" — refuted by the git log at FIVE of the enumerated
suppliers, including W-12 (the reduction supplier: THREE repair
rounds beyond the pin, one of them repairing an adjudicated
CRITICAL) — a claim that also survived, uncaught, through the PE4
and PE5 reports' own §5 lines.

**Target:** `lean/notes/openmath/GENIND_PROOF_2026-08-08.md` at HEAD
(file last touched at r5 `f30e0fa`/`9f2da0f`, 2026-08-09
07:01:12/07:06:56; arc: seal `c3212cc` → PE1 `4259a93` → r1 `66200f2`
→ PE2 `50db400` → r2 `2abf419`/`ad6f31d`/`52fb6ff` → PE3 `f89b565` →
r3 `d3ef66d` → PE4 `51bd1bc`/`ade9bac` → r4 `4a482b5`/`a2000fb` →
PE5 `bd9bce1` → r5). **Role:** hostile verifier, fresh context, zero
prior stake; I fix nothing; quote-and-classify only. All five prior
reports read first; none of their routes re-walked (priors: seal
battery n ≤ 5 / PE1 symbolic+exact n ≤ 5 / PE2 n = 6 census + first
n = 6 PARI / r2 supp M6 fibration 2,128,163 / PE3 M7T + TWOCHILD +
SS6 705,133 / PE4 PSTEEP + THREECHILD + N8MIX 6,507 / PE5 NINEMIX
n = 9 + SAMESIDE + STEEPSCAN 109,146).

---

## 1. FINDINGS

### PE6-G1 [GAP] — S14 C14's [r5 sweep] rewrite "each git-log-checked at commit time, none moved from its pin" is FALSE at five of the enumerated suppliers; W-12's pinned state carries three subsequently-adjudicated findings (one CRITICAL), none disclosed — the record-integrity class, FIFTH consecutive round, this time surviving the standing-rule sweep itself.

S14 C14 at HEAD, verbatim:

> "Of the remaining pinned suppliers enumerated in S9.3's stack
> (W-12, W-11, HEX3, HMENU3, J-D0, GRTJC, WELDMASTER / DULEMMA /
> [W12-H], QSCOUT22): **each git-log-checked at commit time, none
> moved from its pin** [r5 sweep: was "No other supplier note moved
> since its pin" — scoped to the enumerated stack]."

The git log refutes both the r4 original and the r5 rewrite. At
BOTH r4 commit time (05:34:07) and r5 commit time (07:06:56,
2026-08-09), the enumerated suppliers' note files stood as follows
(all movement timestamps 2026-08-08):

| supplier | S9.3 pin | note commits AFTER the pin, BEFORE r4 | moved? |
|---|---|---|---|
| **W-12** | r2 `99f1813` (09:02) | r3 `b17238a` 17:19 (PE3 GAP repaired); r4 `d2d91b7`/`5728258` 19:28/19:32 (**PE4 F1 CRITICAL repaired** — S3.2's sealed B = 0 witness pair); r5 `be4a1dc`/`ee17210` 20:23/20:24 (PE5 GAP + 3m) | YES — 3 repair rounds, 591-line diff |
| **HEX3** | r1 `2580f12` (09:36) | r2 `a707430` 12:13 (never-measured census 11 → 12 pairs; S8.2 U₃^conv k = 1 mass regains a (1 − q^{−(N−3)}) factor); r3 `b6a1d69` 17:53 | YES — 2 repair rounds |
| **HMENU3** | verdict `625fa49` ("r1 owed") | r2 `e1280be` 11:05; **ACCEPTED 2/2** `050425d` 12:44; ANNEX A `19c0285` 20:27 (HM3.C's (A0) quantifier corrected) | YES — accepted + quantifier-correcting annex |
| **W-11** | narrative ends "PE3 CLEAN 839b735 (counter 1/2)" | **ACCEPTED 2/2** `35e6545` 10:53 (append-only annotation) | YES — honest strengthening, undisclosed |
| **QSCOUT22** | `240a56c` (skeleton, 09:34) | VERDICT `dd67cda` 10:00; **L3-correction annex** `fcaf268` 14:23 | YES |
| J-D0 | `1642275`/`9387eb1` | none | no |
| DULEMMA | `f819f26` | none | no |
| GRTJC / WELDMASTER | accepted pins | accepted-body convention (post-acceptance annexes only; GRTJC's annexes predate GENIND's seal) | body unmoved |

Three defects compound:

* **The claim is false as written.** Five of the enumerated notes
  moved from their pins — four of them (all but W-11's annotation)
  with CONTENT commits: repair rounds, an acceptance, a corrected
  census, a corrected mass formula, a corrected (A0) quantifier, an
  L3-correction annex. Had the git-log check the sentence asserts
  actually been executed per supplier, every one of these surfaces
  in one command. HEX3's r2 (12:13) predates even GENIND's r1
  (23:18), so the staleness spans the WHOLE repair arc.
* **The note's own standing rule (adopted at r2, S12: "pin
  suppliers at their repair HEAD and disclose any adjudicated,
  not-yet-repaired findings at the pin") is violated at W-12 in
  every round from r1 on, while being conscientiously executed for
  GENHN/GENH4 (three dated appends).** At the pinned W-12 r2 text
  there are, as of r4/r5 commit time, THREE adjudicated findings
  (W12-PE3's GAP, W12-PE4's F1 **CRITICAL**, W12-PE5's GAP) — all
  repaired at the supplier's HEAD, none repaired AT THE PIN, none
  disclosed in GENIND. W-12 is not a peripheral supplier: it is THE
  reduction (W-12.D), the top of GENIND's conditionality chain.
* **The sentence went through the r5 sweep and came out false.**
  It contains "none " — one of the five disclosed sweep patterns —
  and was indeed rewrite #3 of the seven [r5 sweep] rewrites. The
  rewrite replaced one universal ("No other supplier note moved")
  with an enumerated claim asserting an executed check ("each
  git-log-checked at commit time") that the git log refutes. The
  standing rule's enumerate-with-evidence discipline was executed
  with false evidence — one severity notch subtler than PE5-G1's
  pattern-evasion, because here the pattern MATCHED and the
  adjudication itself was wrong.

**What survives (verified, not assumed).** Every consumption I
could trace survives the suppliers' movement: (i) W12's own r5
supply table records "the n = 3 firing SURVIVES unchanged" and
re-derives the (A0) clause at the disc ≠ 0 form — which is the form
GENIND's citations consume (S4.1's "W-12 S2.3 + GENIND-2(a)" and
Step 4's HM3.C cite remain valid at the corrected quantifier); (ii)
the W12-PE4 CRITICAL was in W12 S3.2 — GENIND consumes S3.4 (the
2SIDED transport, three sites: S4.2, S6) and S2.4/S2.5/L0/L1/W-12.D
/W-12.A, not S3.2; (iii) HEX3's corrected items (a census record;
the S8.2 Θ-mass) are not the consumed H-1..H-4/u₃(M)/species-table
clauses, and the species-table tie is at the md5-frozen
`hex3_checks_results.json`, which did NOT move (GT-PIN 5/5, my
re-verification); (iv) HMENU3's ANNEX A corrects HM3.C's (A0)
quantifier exactly TO the form GENIND's induction needs; (v) BOX-5's
"every supplier consumed at the pins of S10's PIN family" is an
md5-runner-level sentence and TRUE. So no count law, no theorem
clause, no committed number moves — the defect is the ledger, the
same shape as PE3-G1 ("factually right about the LAWS, factually
wrong about the LEDGER").

**Failure scenario:** a reader executing the conditionality audit
trusts C14's rewrite, concludes the pinned W-12 r2 text is current,
and consumes its S2.3-era (A0) sentence ("Ore certifies EVERY
lift") — false in equal characteristic 3 at the in-window
b₁ = b₂ = 0 members of a TRP-RAM3(h) family with 3∤h, where the
lift F = y³ + b₀ has F′ ≡ 0, disc = 0, and no σ at all (W12 r4's
MINOR 2 / r5's PE5-F1 analysis) — and ports "σ certified at every
lift" into a density argument at exactly those disc = 0 lifts,
believing the GENIND ledger certifies the pin's currency. Class:
**GAP** (record integrity; repair = re-pin the moved suppliers at
their repair HEADs (or dated-append the arcs) + disclose the
at-pin adjudicated findings, W-12's CRITICAL first; one table).

**Context (not chargeable to the note):** the same false claim
appears in the PE4 report §5 ("W-11/W-12/HEX3/HMENU3/J-D0/GRTJC
pins unmoved" — PE5 §5 repeats it). Both prior passes verified the
GENHN/GENH4 halves of C14 and inherited the rest — the miss
survived TWO hostile passes, which is precisely why this pass
walked the eight bullets one by one.

### PE6-m1 [MINOR] — Three S9.3 stack bullets' arc parentheticals are stale by multiple supplier rounds with no dated append, against the note's own dated-append convention.

Verbatim at HEAD: W-12 "@ … r2 99f1813 **(arc 0/2, PE2 adjudicated
+ r2, PE3 owed)**" — W12 is post-r5 with PE3, PE4 (CRITICAL), PE5
all adjudicated and repaired; HMENU3 "@ seal 64d3ff9 / verdict
625fa49 **(0/2, PE1 = 0C+1G+1m @ f4f7b03, r1 owed)**" — HMENU3 is
ACCEPTED 2/2 with a post-acceptance annex; W-11 "@ … PE3 CLEAN
839b735 **(counter 1/2 — the first clean pass)**" — W-11 is
ACCEPTED 2/2. Each sentence was true when written (composition,
08-08 10:16, when every pin WAS its supplier's repair HEAD) and is
a dated record; but the note's own convention (executed three times
on the GENH4 bullet: [r3]/[r4]/[r5] dated appends) is that arc
movement earns a dated append, and these three bullets never got
one across five repair rounds. Distinct from PE6-G1 (which charges
the false C14 claim); this is the bookkeeping face — the same shape
as PE3-m4, one class down. Class: MINOR (three dated appends owed).

### Remarks (not findings; as-of observations at verdict time)

* **R1 (GENH4 state).** GENIND r5 landed 07:06:56; the GENH4
  annexpass GAP-1 (ANNEX R R2's N-even band-pinned leg
  machine-refuted) landed `fed3cff` 07:23:01 and its repair ANNEX
  R2 `760d20f` 07:41 — both AFTER r5, so the [r5] dated append's
  claims were TRUE at commit time (rule complied). As of MY verdict
  time nothing GENIND consumes is touched (GAP-1 sat on ANNEX R R2,
  which GENIND never cites; "eleven densities untouched"; the
  GENH4.A/B + GENH4-1F + comp-weighted census cites stand). The
  next dated append owes the disclosure; not a defect today.
* **R2 (other suppliers at verdict time).** GENHN note HEAD is
  still the pin `850e77e` (git-log checked). HE7/HE6: zero
  references in GENIND (grep = 0) — correctly absent; the HE7
  annex-stack acceptance and its post-acceptance micro-annex are
  not GENIND's to disclose.
* **R3 (the r5 mathematics held everywhere).** The f-first entry
  laws, the m = 6 grammar, and the [r5] step-(1) same-side
  composite parenthetical were each re-derived from scratch and
  then machine-realized (§3, §7) — including the law's OWN genre,
  which no machine leg had ever exercised.

## 2. CHARGE 1 — THE r5 f-FIRST ENTRY LAWS, RE-DERIVED THEN REALIZED

**Re-derivation (before any machine work; independent of S15's).**
An m = 6 mixed genre = one (2, d) child on an e = 1 side at integer
slope k + one composite block with e·μ·deg ψ = 4. The composite
solutions of e·μ·deg ψ = 4 with μ ≥ 2, e·deg ψ ≥ 2 are exactly
(e, deg ψ, μ) ∈ {(2, 1, 2), (1, 2, 2)} (μ = 4 would force
e·deg ψ = 1 < 2); 2 + 4 = 6 abscissas leave no room for pieces; a
same-side child on an e = 2 side is impossible (a repeated linear
factor there is itself CS). Hence exactly THREE arrangement
families, with entry heights by Gauss additivity (v(a₀) = Σ length
× slope): child+E distinct-side 2k + 2h (E-slope h/2, h odd —
never the child's side), unique sub-6 cell (k, h) = (1, 1) = M6 at
4; f-first SAME-side (slope shared, k = h forced, one length-6
side, residual (y−w)²ψ(y)²) at **6k ≥ 6**; f-first DISTINCT-side at
**2k + 4h ≥ 8** (min at (2, 1); the reverse order (1, 2) gives 10).
Boundary sub-checks: child+E at k ≥ 2: ≥ 6 ✓ (= 6 at (2,1));
stage-steeper h ≥ 2k + 1: ≥ 8 ✓ (= 8 at (1,3) — exactly SS6's
committed v(a₀) = 8, a cross-tie the note's grammar gets right).
**The r5 display matches this derivation clause for clause,
including the "4 ≥ 6" refutation of the old 2k + 2h chain at the
f-first minimum.**

**Realization (§7 FR-A/B): the same-side genre's first machine
instance anywhere.** All five predicted invariants held on
2,097,152 exhaustive states + 33,088 more exhaustive at N = 7 +
every sampled cell: entry v(a₀) = 6k at k = 1 AND k = 2; visibility
floor N = 6k + 1 (N = 6 members DRAIN 20/20); single-key
classification through the sealed reader (both its p = 2 Frobenius
branch and its odd-q gcd branch); extraction window N − 6k; the
same-side composite co-block visible as the cofactor reduction
ψ(ȳ)² and coprime to ȳ after the child shift (ψ(w) ≠ 0) — the [r5,
PE5-m2] parenthetical's ψ(ȳ + ẑ)^μ case made literal on every
member. THE 6k LAW IS CONFIRMED BY CONSTRUCTION; the disclosure
"machine-unrealized corpus-wide" was accurate and is now dischargeable.

## 3. CHARGE 2 — THE SEVEN [r5 sweep] REWRITES AT THEIR SCOPES

1. **S14 leg-1 header** ("every site matched by the six targets"):
   scoped ✓; my spot-checks of sites 4/5/7/8/9/10/14 against the
   text and the sealed `extract_child` agree; PE5's independent
   outside-pattern sweep is cited as corroboration, correctly
   labeled a verifier artifact. TRUE in scope.
2. **S14 leg-1 count** ("Uncapped survivors among these 15 site
   classes: NONE"): scoped to the enumerated classes ✓; consistent
   with my reading of every [r3]-tagged site. TRUE in scope.
3. **S14 C14** ("each git-log-checked at commit time, none moved
   from its pin"): **FALSE in its own declared scope — PE6-G1.**
4. **S14 leg-3** ("each tag surfaced by grepping the four patterns
   [r1] [r2] [r3] [r4] … cites the finding it repairs"): scoped ✓;
   spot-checked eight tags (S4.1 ×3, S4.3 ×3, S12 ×1, S13 ×1) —
   each cites its finding. TRUE in scope.
5. **S12 PE2-m1** ("checked by grep for the one-term phrasing: its
   consumer sites were the S1 box, Step 4, and S7.1, all corrected
   here"): the three sites carry the two-term granularity gloss at
   HEAD ✓. TRUE in scope.
6./7. **The two PE5-G1-charged S14 coverage sentences**: both now
   name their executed scope (six leg-1 targets; nine leg-2 targets
   as C1–C14) and display PE5's witness as the residual, quoting
   the originals ✓. TRUE in scope.

**Score: 6 of 7 rewrites accurate; the seventh is PE6-G1.** The
S15 sweep's kept-claims adjudication (artifact-pinned quantifiers,
S10/S11 roster quantifiers, C13's lemma-content "never"s) checked
and fair; the S15 as-of paragraph (GENHN 850e77e unmoved; GENH4
HEAD b10a5a7) TRUE at its commit time against the git log.

## 4. CHARGE 3 — S15 + THE PIN-TYPO CORRECTION

* **e549331a verified three ways:** `git show
  51bd1bc:verification/openmath/genind_pe4_fresh_results.json |
  md5sum` = `git show HEAD:...` = worktree =
  e549331a2b07c7e2205733551de5940a — the artifact byte-identical
  since its landing commit, exactly as the [r5] brackets claim; the
  corrupt "e5493312" does originate in the PE4 report's own §7
  listing (grep) and survives in the note only inside the three
  quoting brackets (S4.1, S14, S15) ✓.
* **S15's 13-md5 byte-freeze list:** re-verified by md5sum — 13/13
  match at HEAD (sealed runner + 4 committed triples).
* **S15's minimality contract** ("no machine leg added — every
  fresh number cited is PE5's committed leg"): consistent — no new
  runner at r5 (git diff f30e0fa^..9f2da0f touches the note only).
* The S15 Arc/record paragraphs are enumerated or pattern-scoped
  throughout — rule-compliant in FORM; the one false content item
  is C14's rewrite (PE6-G1), which lives in S14.

## 5. CHARGE 4 — THE CONDITIONALITY DISPLAY AT HEAD (as-of rule)

S9.3 walked bullet-by-bullet at verdict time: GENHN @ 850e77e = its
note HEAD ✓ (four consumed clauses named with sites; the queued
GENHN.A(i) transport erratum touches an unconsumed clause); GENH4
bullet with THREE dated appends ([r3]/[r4]/[r5]) — accurate at each
commit time; post-r5 GENH4 movement (annexpass GAP-1 + ANNEX R2) is
next-append material (Remark R1); HE7/HE6 correctly absent (grep
0); J-D0/DULEMMA pins = their notes' HEADs ✓; GRTJC/WELDMASTER
accepted bodies unmoved ✓; **W-12/W-11/HEX3/HMENU3/QSCOUT22 = the
PE6-G1/PE6-m1 findings**. The one-line summary ("no axiom, no weld
face inside any count law; grade = composition 0/2") and BOX-5's
arc line are consistent with the header: clean count NOT started,
PE6 attempts the first clean. The BOX-5 sentence "every supplier
consumed at the pins of S10's PIN family" is true at its md5 level
(GT-PIN 5/5 re-verified).

## 6. MACHINE LEG — ALL FIVE COMMITTED RUNNERS REPRODUCED; PINS INTACT

* **md5 pins:** sealed runner `genind_checks.py` = e7ca150b
  EIGHT-WAY identical (worktree = HEAD = r5 f30e0fa/9f2da0f = r4
  4a482b5/a2000fb = r3 d3ef66d = r2 52fb6ff = seal c3212cc); r2
  supp triple 7335fc16/63b7413a/6c93f29c ✓; PE3 fresh triple
  fce9cb08/2075dd28/360eb239 ✓; PE4 fresh triple
  aa2b9af8/1a29a9c1/**e549331a** (the r5-corrected pin — verified
  at 51bd1bc AND HEAD by git-show) ✓; PE5 fresh triple
  6ef3132e/a0452521/bdc04c4d ✓; GT-PIN suppliers 5/5 match the
  runner's PINS dict; committed sealed artifacts match S11's md5s
  (c157dc31/366f2143) ✓. 13/13.
* **Isolated re-runs** (`/tmp/genind_pe6_run`; runners + the five
  pinned suppliers copied; committed artifacts never touched):
  sealed battery **GREEN 163,536 / 0** (164.6 s; teeth 12/12/4/24;
  results JSON leaf-diff = `elapsed_s` only); r2 supp **GREEN
  2,128,163 / 0** (leaf-diff = elapsed only); PE3 fresh **GREEN
  705,133 / 0** (leaf-diff = elapsed only); PE4 fresh **GREEN
  6,507 / 0** (leaf-diff = NONE, bit-identical); PE5 fresh **GREEN
  109,146 / 0** (leaf-diff = elapsed only). The second pass ever to
  re-run all committed legs, and the first to re-run all FIVE.
* **Tautology audit (my angle).** My fresh legs consume the sealed
  `extract_child` and `read_cluster`, so: (i) every gated value
  (entry heights, windows, keys, cofactor reductions, class/fiber
  counts, verdict splits) was hand-derived in the runner's
  docstring BEFORE the run from the note's laws + my own
  re-derivation of the grammar; (ii) the locus SIZES (2^21, 2^15)
  are generator-free slot counts; (iii) consumption-level counts
  (ONTO 4 × constant 2^19; child-DEC = 2^20; box bijections
  256/81; DS-B's 182/119 distinct window-5 classes) distinguish the
  capped/true windows from the uncapped reading regardless of
  extractor internals (an uncapped window-1 extraction admits ONE
  class); (iv) the PARI legs are mechanism-free (global
  nfinit/idealprimedec + factorpadic on disc ≠ 0 irreducible
  lifts — PARI never sees the reader), with the (2,1)-odd-f /
  even-f-mass-4 prediction certified by the hull + residue-field
  argument, not by the reader. Non-tautological.

## 7. FRESH ROUTE — the same-side f-first realization + the f-first cap regime (disclosed; disjoint from all seven prior routes)

Runner `verification/openmath/genind_pe6_fresh.py` (committed with
output + results JSON @ ad1bd98; md5 87b0f27e / 1c2f8eed /
fb473a57); sealed runner imported read-only, md5-asserted; ran in
the /tmp sandbox. **ALL predictions hand-derived in the docstring
BEFORE the run (grammar re-derived independently in §2). GREEN:
2,132,082 checks, 0 violations, 244.1 s — single run, first time,
no smoke, no instrument arc.**

* **FR-A SSF — the SAME-SIDE F-FIRST genre, first machine instance
  anywhere** (the r5 6k law's own genre; S15's disclosed honest
  limit discharged). (2, 6, 1, N = 8), one side (0,6)–(6,0) slope
  1, residual (y−w)²ψ(y)² = y⁶ + 1 over F₂ (w = 1, ψ = y²+y+1 both
  forced; every interior pin the J-D0-degenerate 0): **EXHAUSTIVE
  2,097,152** = the full 2^21 locus — every member classifies to
  the single key ('CSOTHER', 6, (0,6), 1, 2, 2, 1) (derived from
  the reader source, not read off), extracts the child at Wc = 2 =
  N − 6k, and shows cofactor reduction ψ(ȳ)² with child-shift
  constant ψ(1)² ≠ 0 — the [r5, PE5-m2] same-side COMPOSITE
  co-block literal on every member; child-class map **ONTO 4 with
  constant fiber 2^19**; child-DEC exactly 2^20. Plus: **N = 7
  first-visible window exhaustive (32,768**, Wc = 1); **N = 6 floor
  DRAIN 20/20** (the 6k law's N ≥ 7 clause); **k = 2 cell at
  N = 14** (v(a₀) = 12 = 6k, Wc = 2, 200 samples); char-p mirrors
  Fqt(2)/Fqt(3) (200 + 100); **q = 3 all six letter slices**
  (w, ψ) × 60 (odd-q pins live; the reader's gcd branch, vs the
  p = 2 Frobenius branch; ψ(w) ≠ 0 on every member).
* **FR-C/D — BOTH distinct-side f-first cells, including the
  never-realized f-first-STEEPER cap regime.** DS-A (child steeper:
  k = 2, h = 1 — the 2k+4h MINIMUM cell): v(a₀) = 8 exact, key
  ('CSOTHER',6,(2,6),1,2,2,1), D = 8, Wc = 2, DEGREE-DROP cofactor
  (B̄ = constant ψ(0)²) — 200 q=2 + 120 q=3. DS-B (ψ²-block
  steeper: k = 1, h = 2): v(a₀) = 10 = 2k+4h, key
  ('CSOTHER',6,(0,4),1,2,2,2), **capped D = 2·1 + 4·min(2,1) = 6 →
  Wc = 5 where the uncapped reading says 10 → Wc = 1** — the first
  realization of a COMPOSITE f-first co-block steeper than the
  extracted child (SS6/PE5 realized only E-type steeper stages);
  COLLAPSE cofactor B̄ = ȳ⁴ with shift constant w⁴ ≠ 0 (the
  steeper-collapse leg of the r4 trichotomy at a new block type);
  300 q=2 + 120 q=3; driving boxes **BIJECTIVE onto 256 / 81**
  child classes (per-order images (z⁴, 4z³), (z⁵, 5z⁴), det z⁸ ≠ 0
  both characteristics).
* **FR-E — the child+E family's k = 2 boundary cell** (the r5
  grammar's "k ≥ 2 gives ≥ 6", never realized — M6 is k = 1):
  v(a₀) = 6 = 2k + 2h, key ('CSOTHER',6,(2,6),2,1,2,1), Wc = 2;
  100 q=2 + 60 q=3.
* **FR-F PARI (mechanism-free).** SSF child-DEC members, 15 q=2 +
  10 q=3 irreducible disc ≠ 0 lifts (nfinit + idealprimedec):
  root-valuation partition {1: 6} and σ = the certified child
  ramified quadratic as **the unique odd-f prime (2,1)** + even-f
  prime mass exactly 4 (the ψ-block's residue field contains
  F_{q²}) — 25/25. DS-B, 10 lifts: partition **{2: 4, 1: 2}** exact
  (the cap-regime fracture read confirmed with no shared
  mechanism).
* **FR-G TOOTH (preregistered fire count).** Corrupting the SSF
  genre's single nonzero pin (digit₆(a₀) → 0, digit₇ := 1) forces
  the one-side gcd(6,7) = 1 read: top event ('DEC',) with
  σ = ((6,1),) — **fired 10/10**, exactly as derived.

**Consequence for the r5 text:** the f-first entry laws (6k /
2k + 4h), the three-family grammar, its boundary sub-claims (k ≥ 2
cell = 6; stage-steeper ≥ 8 = SS6's committed 8), the [r5]
step-(1) same-side composite parenthetical, and the capped-window
formula at a NEW co-block type are all now machine-real — the r5's
mathematics is not merely unrefuted but realized at its own genre.

## 8. CLEAN CHARGES (executed, nothing found)

The 6k / 2k+4h laws — re-derived then realized (§2, §7); the m = 6
grammar completeness — re-derived independently (three families,
no fourth arrangement fits the abscissa budget); the seven [r5
sweep] rewrites — 6/7 true in scope (§3); the e549331a pin
correction — verified at the landing commit and HEAD (§4); S15's
13-md5 byte-freeze — 13/13; S15's as-of paragraph — true at commit
time; S11 — frozen at its seal (byte-checked against the committed
artifacts); header/BOX-5/S15 grade lines — monotone 0/2, PE6 named
as the first-clean attempt; the five committed machine legs — all
reproduced in isolation (§6); HE7/HE6 — correctly absent; GENHN
pin — current at verdict time; the [r5] dated append on GENH4 —
true at commit time with the post-r5 movement correctly outside
its scope (Remark R1); my own sweep for universal-force record
sentences outside the S15 patterns ("each ", "always", "the only",
"both ", "exactly", capitalized "Every") — every hit resolved
either lemma-content, artifact-pinned, or already-charged (the one
false "each" sentence is PE6-G1's, which the S15 sweep did match
via "none ").

## 9. FINDINGS TABLE + GRADE LINE

| id | class | site | one line |
|---|---|---|---|
| PE6-G1 | GAP | S14 C14 [r5 sweep] (+ the r4 original) / S9.3 stack | "each git-log-checked at commit time, none moved from its pin" FALSE at 5 of the enumerated suppliers (W-12: 3 repair rounds incl. an adjudicated-CRITICAL repair, 591-line diff; HEX3: 2 rounds; HMENU3: accepted + (A0)-quantifier annex; W-11: accepted; QSCOUT22: verdict + L3-correction annex); the standing pin rule executed for GENHN/GENH4 but never for these; consumption survives at every traced clause (verified) — the arc's record-integrity class, FIFTH consecutive round, this time through the sweep itself and through two prior verifier reports |
| PE6-m1 | MINOR | S9.3 W-12 / HMENU3 / W-11 bullets | arc parentheticals stale by multiple supplier rounds ("PE3 owed" vs post-r5; "r1 owed" vs ACCEPTED 2/2 + annex; "counter 1/2" vs ACCEPTED 2/2) with no dated append, against the note's own convention (executed thrice on GENH4) |

**Machine leg:** 13/13 md5 pins verified (sealed runner eight-way
identical; the r5-corrected e549331a git-show-verified at 51bd1bc
and HEAD); isolated re-runs of ALL FIVE committed legs GREEN
(163,536/0 + 2,128,163/0 + 705,133/0 + 6,507/0 + 109,146/0; every
results-JSON leaf-diff = elapsed-time only, PE4's = NONE);
committed artifacts byte-untouched by this pass; tautology audit
executed on a fresh angle (hand-derived loci + consumption-level
capped-vs-uncapped discriminators + mechanism-free global PARI).

**Fresh route:** the same-side f-first genre machine-realized
corpus-first — 2,097,152 exhaustive at its first two visible
windows + floor + k = 2 + both characteristics + both ring kinds —
confirming the r5's 6k law by construction; both distinct-side
cells including the new f-first-steeper cap regime (Wc = 5 vs
uncapped 1; boxes bijective 256/81; PARI {2:4, 1:2}); the child+E
k = 2 boundary cell; 25 + 10 mechanism-free σ lifts; tooth 10/10.
**GREEN: 2,132,082 checks, 0 violations, single run.** Every count
matched its hand-derived prediction exactly.

**GRADE LINE: NOT CLEAN — 0 CRITICAL + 1 GAP + 1 MINOR. The 2-clean
count does NOT start (0/2 stands; the consecutive-clean counter
stays at 0; GENIND remains the last arc-program leader without a
clean). r6 owed: re-pin or dated-append the five moved suppliers
with the at-pin adjudicated findings disclosed (W-12's CRITICAL
first) and correct C14's rewrite to what the git log supports
(PE6-G1); three dated appends on the stale arc parentheticals
(PE6-m1) — all record/display-level; the f-first entry laws, the
m = 6 grammar, the capped window formula (now at a new co-block
type), LEMMA GENIND-6, every count law, every committed number,
and the conditionality display's consumed-clause content survived
every attack, now including the first machine realization of the
same-side f-first genre and the f-first-steeper cap regime.**

---
*Verifier: fresh-context hostile pass PE6, 2026-08-09. Fixed
nothing. All machine work in an isolated /tmp sandbox; committed
artifacts untouched (md5-verified before and after). Fresh-route
runner committed for audit as
`verification/openmath/genind_pe6_fresh.py` (+ output + results);
every law it gates on is derived in its docstring before the run;
its genre keys are derived from the reader's source (cs_genre_key),
not read off; the run was single-shot GREEN with no smoke and no
re-runs.*
