I have completed the full read of the operative sections (§§0–6 + §3.4) and cross-checked every attribution display against its consumption sites. Findings below, most severe first.

---

## FINDINGS

**1. BLOCKER — §1's (ROOT-C) hypothesis display of (H5) omits (PACK-iv): the displayed theorem assumes a WEAKER member than Steps 17/18b consume, and the Step-19 closer's hypothesis content is NOT identical to §1's display.**

§1's (ROOT-C) block displays:

> "**(H5)** (PACK), the actual-pack correspondence PLUS the pack row-laws (D-11's open clause 4: … AND, ENLARGED at REVISION 7, finding VC7-2, the three named row-laws at the 𝔅_n rows as explicit clauses: **(PACK-i)** rsh_interp, **(PACK-ii)** r_bdd, **(PACK-iii)** wsh_ok, displayed at the §3.1 row — consumed at Step 18 with the sub-attribution exact …)"

— three row-laws, Step-18-only consumption. But the §3.1 (PACK) row carries "**AND (PACK-iv) sem_alloc — semantic verdict-row form at the intended member** [NEW at REVISION 10, r11]", consumed at "**Step 18b DIRECT** (Theorem M2's (RE) instantiation) AND **Step 17's E1 delivery display of (ROW-Σ)**"; Step 18b's proof rests on it ("At the CONSUMED member, (RE)+row-form is PRICED as **(PACK-iv)**"); and Step 19's ∎ closes on "(H5) (incl. PACK-i/ii/iii since REVISION 7 AND PACK-iv since REVISION 10, r11)". The REVISION-11 sync ran Steps 16/19 and the final ∎ "to the twelve-row (H1)–(H9) scope with (H5) at PACK-i/ii/iii/iv" (§17 record) but did **not** carry §1's own (H5) member display — the exact VC11 ripple-sync genre (a stale enumeration presenting as the operative hypothesis), surviving in the single most load-bearing location: the theorem statement. A reader instantiating (ROOT-C) from §1's display assumes correspondence + three laws; Step 18b's mandatory (RE) instantiation is then unpriced at the displayed statement. (Mitigations: §1's later "Proof status" line and the headline do name (PACK-iv), and the display points at "the §3.1 row" — but the enumerated display reads as exhaustive, and VC11 classified the identical stopped-at-PACK-i-iii defect in the ∎ as a blocker.) Charge-(1)'s identity test — Step-19 closer ≡ §1 display — **fails at member granularity**.

**2. BLOCKER — Step 18's δ-stable interface note displays a STALE clause-(R) attribution while citing §1: "(H1)–(H3) + (H4a) + (H5) + (K3-δ) — §1", omitting (H6)–(H9); plus a stale hypothesis count.**

Step 18 (the O-11 step), operative text:

> "the fragment is NAMED **(K3-δ)** = (UB-X)(b) restricted to K3-c's realized pools, and clause (R)'s displayed attribution now CONSUMES it ((H1)–(H3) + (H4a) + (H5) + (K3-δ) — §1; the joint five-hypothesis theorem is unchanged since (K3-δ) ⊆ (H4b))."

§1's actual clause-(R) display is "(H1)–(H3) + (H4a) + (H5) + the (K3-δ) fragment of (H4b) + **(H6) + (H7) + (H8) + (H9)**". The note's parenthetical presents as a current quote of §1 ("now CONSUMES … — §1") and understates the clause-(R) hypothesis set by four groups; "the joint five-hypothesis theorem" is a REVISION-4-era count against the current twelve-rows-↔-(H1)–(H9) roster. This is the same defect class the document's own bar treats as blocker-grade (VC11-2: "Step 16's claimed exact (UB) scope was stale against §1"), surviving REVISION 11 in Step 18. The mathematics is unchanged (§1 and Step 19 carry the full list), but a display inside the composition contradicting §1's attribution fails the internal-consistency invariant the acceptance bar enforces.

**3. ADVISORY — Step 17's Consumes summary omits the §3.1 rows the step's own Statement/Delivers displays it consuming ((H9), (PACK-iv)).**

Step 17: "Consumes: Step 5's lemma base (as the mass statements they are), Step 6 (D-15), Step 12 (Thm E), T-3/T-6 mass laws. NOTHING from Steps 18/18b." Yet the same step's Statement prices "the routing of these labels into the COUNTED corpus … at **(H9) = (NSF)**" and its Delivers states "THROUGH this edge (ROW-Σ)'s (RE) hypothesis at the consumed member = **(PACK-iv)** ((H5)) enters this tranche". The post-VC11-5 convention (Step 18b's repaired Consumes now names "(GR-B)/(FRESH)/(ADM) … (PACK-iv) ((H5)) directly") makes Consumes summaries carry consumed [M] rows; Step 17's does not. Display asymmetry only — both prices are displayed inside the step body, so no hypothesis is hidden.

**4. ADVISORY — (H8)'s provenance label ("the O-11 owed-pass G-2") is irreconcilable, within the document's four corners, with the §3.2 O-11 row's standing "pass 3 owed".**

The (H8) row (and §1/Step 18) sources the cut kernel to "[NEW row at REVISION 10, r11, from the O-11 owed-pass G-2]" and "(O-11 pass-2 G-1; owed-pass G-2: 'overlapping ownership, an unowned read, or two different shallow decompositions' are unexcluded)". But the §3.2 O-11 cycle row records "pass 2 (V32): billing precision → r3 (7aef1b9 …); **pass 3 owed**", and the §16 edit table states "O-11's row stays owed, unchanged". Either "owed-pass" here misnames the D-11 owed pass of 2026-08-03 (whose report the suspension record credits), or an O-11 pass ran whose outcome the §3.2 row does not record. No consumption depends on the resolution — (H8) is priced [M] and consumed as hypothesis either way — but a fresh reader cannot reconstruct the finding's provenance, and one of the two displays is inaccurate.

**5. ADVISORY — the (AGR) inherited-[V] row's residue claim "nothing else" elides CU-4's displayed §3.1 conditionality.**

§3.2: "(AGR) … ASSEMBLED from CU-2 + CU-4 + GMN Cor 4.19 (all on file); its residue = exactly the pass debts of CU-2/CU-4 (this table) + the Cor 4.19 literature row (§4) — **nothing else**." Step 11 displays CU-4 as "TB-CAP (τ-irr) perimeter-conditional" (= D-12r), and the (E-N) row records its CU-4 touch. Those [M] conditionalities are correctly displayed elsewhere and reach clause (R) through (H3), so no hypothesis is hidden — but as a self-contained residue enumeration the row is imprecise (pass debts are not the assembly's whole residue).

---

## WHAT SURVIVED THE HUNT (verified clean)

- **The composition walk (charge 1):** Steps 1–19 walked; every consumption is a proved-on-record pointer (nine VERIFIED leaves consistently identified: O-13, O-12, Dfloor, GD23, CU-1, O-10, O4T, O-9, CU-2t), a PROVED-UNVERIFIED leaf with a named §3.2 pass row (P(O-2a r5)/P(O-1thr r4)/P(O5t r3)/P(O-8b/M14)/P(O-14a′ r3)/D-11/O-11/B-layer), an explicit §3.1 member, or a §4 pinned cite. No silent unconditional upgrade found: O-12's value-correctness non-claim, O-9's (GR-B)/(FRESH)/(ADM) scope, CU-2t's §9.5 rows, D-11's r4 true scope, and Theorem E's separable-lift rescope (E-M3) are all consumed at displayed scope. Σ_τ R_τ = 1 is correctly concluded at Step 19 from Step 1's Σ_τ α = 1 + interpolation + the conditional α = R_τ. At hypothesis-**group** level the Step-19 ∎ list matches §1 ((H1)–(H9), (0)/(SQ) unconditional); the member-level failure is Finding 1.
- **The ledger (charge 2):** the twelve §3.1 rows ↔ (H1)–(H9) bijection checks ((H3) = {(ADM), D-12r, (E-N)}; (H4) split (H4a)/(H4b); one row each otherwise). Each member's statement is precise and at least as strong as its consumption site ((H1)(d) read-order-quantified, coverage arithmetic displayed; (K3-δ) names the formerly presumed pool-stability match; (PACK-i/ii/iii) explicitly not inferred from the correspondence; (H8) stated exactly as Lemma 3.3's hypothesis; (K1-CANON) and the marks-vocabulary premise housed in (NSF-INT)). **(NSF-KEY) is nowhere consumed as true**: every occurrence carries KNOWN FALSE AT HEAD with the Unit-C-gated retirement, and Step 18/Table B route every M02/M03/M06/M07/M11 refutation to the repaired scoped statement, never consuming the refuted typing.
- **The seams (charge 3):** Step 17's statement is internally coherent with the displayed r4 scope — per-clause reference measures named, (ROW-Σ) totals (solved = 1; per-booking verdict rows = the Φ^{(O1)}/Φ^{(O2′)}/Φ^{(O3)} margins, matching Step 5's Corollary-D numbers, "NO booking's displayed row total is 1"), (b-PF) proved / (b-EX) at route-(a) scope with the quantified exclusion displayed; Step 18b consumes M1(a) at families (ii)/(iii) only. Step 18b's (RE) is **assumed, not derived** — priced as (PACK-iv) with the former definitional discharge explicitly WITHDRAWN, and the two-site closed edge set (18b direct; 17's (ROW-Σ) display) keeps (ROW-Σ) M2-free.
- **Invariants (charge 4):** (0)/(SQ) unconditional consistently at §1/Step 13/Step 19; per-clause attribution consistent across §1/Step 16/Step 19 (the one contradiction is Finding 2, inside Step 18); the step DAG is acyclic edge-by-edge, including 17 ≺ 18 ≺ 18b ((ROW-Σ) M2-free; (S2) ← Step 18 only in 18b; Step 1's ns-null re-pointed to its own Appendix A).
- **Consistency (charge 5):** the suspension supersession is coherent (acceptance language everywhere marked historical; consecutive-clean counter ZERO of 2 at headline, §3.2, §6); stale row counts (NINE/TEN-row) occur only inside explicitly historical narrative; §3.4's verdict paragraph now correctly carries the (H8)/(H9) surfacings; the item-6 residue list closes under the table's reading rule.

---

Findings 1 and 2 are display/attribution defects of exactly the class the document's own acceptance bar (VC11 precedent) counts as blockers — the underlying mathematics and pricing are present elsewhere in the document, but the displayed theorem statement (§1 (H5)) and a Step-18 attribution note claiming §1's scope are stale against the twelve-row roster, so the composition text does not yet satisfy its own identity invariants.

VERDICT: COMPOSITION-GAPS