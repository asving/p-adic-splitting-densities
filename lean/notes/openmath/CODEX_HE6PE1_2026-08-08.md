# HE6 pass PE1 — CODEX ADVERSARIAL VERIFICATION (independent model, fresh context) + ADJUDICATION

## S0. Charter and provenance

**Pass:** PE1 (first hostile pass) on `HE6_PROOF_2026-08-08.md` (seal a77c376, close
1f5a417) — the note that proved HE6.A (HE3-BOX-6 closed: label class sizes at every side
via the enlarged test family at fractional heights, base-change-free) and stated
COROLLARY HE6.B with the surviving box HE6-BOX-1. **Verifier:** Codex `gpt-5.6-sol`,
reasoning effort HIGH, fresh context, charged quote-and-classify / fix-nothing.
**The unit itself specified the attack order** and the brief mandated it: (1) LEMMA
HE6-1 Step 1's two ultrametric cases; (2) (LIFT) from GENHN-2's residue clause;
(3) HE6-3(a)'s c₀ = 0 elimination; (4) whether HE6-BOX-1's scope in COROLLARY HE6.B is
exactly the complement of the separable-residual hypothesis; then free hunt.

- **Brief:** `he6pe1_brief_2026-08-08.txt` (this directory), 91,055 chars,
  self-contained: PASTE A = the full HE6 note; PASTE B = GENHN S3.1/S3.2 (GENHN-1) +
  S4 (GENHN-2, THE SLOT LEMMA, statement + proof + twist paragraph) + S5 (GENHN-3);
  PASTE C = HE3 @ 7a95449 S1 (DEFINITION 1/2, THEOREM HE3.A, COROLLARY HE3.B) +
  S3–S6.4 (HE3-1/1′, HE3-0/0′, HE3-2, HE3-3 + (SEP), HE3-4 + BOXED branches, the HE3.A
  assembly, the HE3.B proof), all verbatim.
- **Run of record:** session `019fe327-6d2a-75e0-8961-4a16a6c20d07`, gpt-5.6-sol,
  effort HIGH, launched 20:53 UTC, exited ~21:04 (≈11 min), 59,609 tokens, log
  `/tmp/codex_he6pe1_1786222439.log` (streamed and reprinted final messages
  diff-IDENTICAL). Raw final message: appendix below, verbatim.
- **Decorrelation discipline:** the orchestrator wrote a pre-verdict key for all four
  attack points + free-hunt candidates BEFORE reading any Codex output
  (`he6pe1_scaffold_2026-08-08.md`, this directory; written during the run, committed
  as-was). S3 below records the match.

## S1. VERDICT (orchestrator adjudication, not Codex's self-grade)

**Codex returned 9 findings (4 CRITICAL + 5 JUSTIFICATION GAPS). After independent
re-derivation of every counterexample: 8 findings UPHELD as classified or near, 1
(finding 3) UPHELD AS A REAL SUPPLIER SEAM but its direct counterexample REGRADED (it
misses the ⁺-pin clause of GENHN-1's budget; the display-level inconsistency it exposes
is real and lands on GENHN, not HE6).**

Bottom line, statement by statement:

- **THEOREM HE6.A**: the conclusion is NOT refuted — no finding touches the bite
  configuration (f₁ = 1, h = 1, A₀ ≠ 0, disc ≠ 0, ℓ ≥ 2 separable) — but the PROOF as
  written has five named repairable defects (findings 2, 4, 5, 6, 7) and the STATEMENT
  is missing two hypotheses it silently uses (disc(f) ≠ 0; Φ′ ∤ f). Status: claim
  stands at attempt grade WITH A REPAIR QUEUE (S4); not survivable as-written.
- **COROLLARY HE6.B**: the scope sentence is **FALSE AS WORDED** (finding 1, = the
  unit's own attack point 4, = the orchestrator's pre-verdict key): the excluded set
  must also contain ℓ = 1 sides carrying a repeated irreducible residual factor of
  degree ≥ 2. **HE6-BOX-1 WIDENS from "ℓ ≥ 2 + repeated" to "repeated non-K-rational-
  linear residual factor at ANY ℓ"** — same first-live degree n = 8 (that crack needs
  L_λ = d ≥ 4, μ ≥ 4, D′ ≥ 2), so the program's **n ≤ 7 unconditional claim is
  unaffected** (μ ≤ 3 is composite-stage-free, HE3.B).
- **Suppliers**: finding 3 exposes a genuine display inconsistency at f₁ ≥ 2 between
  GENHN-1's stage-initial-node line and the D′h disk threshold — an erratum owed on
  GENHN (S2, F3), invisible to every f₁ = 1 battery row.

**GRADE: HE6-BOX-2 stays attempt 0/2.** The pass ran and is adjudicated, but a pass
"survived" only when the repairs land with statements re-checked; the repair unit (S4
queue) is owed first. The machine verdict (§S9 of the note: 72,315/0, 4,232 strict
certificates, 742 split octics all {(4,1),(4,1)}) is untouched — every finding lives
off the exercised strata (f₁ ≥ 2, h ≥ 2, A₀ = 0, small k, ℓ = 1 repeated-nonlinear).

## S2. The findings, transcribed + adjudicated (Codex's numbering; quotes in the appendix)

| # | Codex finding (compressed) | Codex class | adjudication |
|---|---|---|---|
| 1 | HE6.B's excluded set is not the complement: ℓ = 1 repeated irreducible residual of degree ≥ 2 routes to the stage-CS recursion, whose counting half §S7.1 itself declares missing (e₁′ = 1, f₁′ ≥ 2); not excluded by "ℓ ≥ 2 ... REPEATED" | CRITICAL | **UPHELD, CRITICAL.** Exactly the orchestrator's pre-verdict key for attack 4. Enumeration (i)–(iv) verified: (i) ℓ=1 repeated linear = stage-α ✓ covered; (ii) ℓ=1 repeated deg ≥ 2 = the crack; (iii) ℓ≥2 separable = HE6.A ✓; (iv) ℓ≥2 repeated = excluded ✓. Repair: HE6.B excludes ALL stage-CS events (any repeated non-K-rational-linear residual); HE6-BOX-1 widens, §S7.2's (LIFT₂)/(SLOT₂) program already covers the ℓ = 1 instance (Ψ_{λ,r} at integer height) |
| 2 | Roots with Φ′(ρ) = 0 (⟺ A₀ = 0 ⟺ Φ′ \| f) carry no finite label; HE3-0(iii), (★), (†), and the Σ = D′μ consistency check are false on that stratum. Counterexample f = Φ′(Φ′ + π²), Φ′ = x − π: (†) reads κ = 2κ on 1 < κ < 2 | CRITICAL | **UPHELD, CRITICAL as stated — conclusions survive on a clean repair.** Counterexample re-derived: member IS on the opening locus (ambient residual (T−1)² = ψ² ✓, entry bounds ✓, disc ≠ 0 ✓); ρ = π has dv(Φ′(ρ)) = ∞; displayed (†) false exactly as claimed, and the TRUE (RES) identity still holds (both sides 2ℓd₀κ) — the error is the transcription dropping the ∞-slope roots. Repair routes (either works, checked): (a) hypothesis Φ′ ∤ f (peel Φ′-factors first: each is an irreducible degree-D′ factor, σ-block (e₁,f₁)); or (b) an ∞-label column n_∞ = D′L_∞, L_∞ := μ − Σ_λ L_λ — min(∞,κ) = κ stays independent in the top gap and the triangular elimination returns n_λ = D′L_λ unchanged. Degenerate stratum; machine-invisible (all battery members have finite prescribed pins, A₀ ≠ 0) |
| 3 | At f₁ ≥ 2 the pasted suppliers conflict: GENHN-1's node {dv(A_j) ≥ (μ−j)S + 1} admits a member (Φ′ = x²+2π², A₁ = π², A₀ = π⁴ over ℤ₅) whose P(F) has slope 2 = D′h and whose roots violate (T2) — "GENHN-1's bijection and HE3-0's characterization cannot both be true" | CRITICAL | **UPHELD AS A REAL SEAM; the member-level contradiction REGRADED.** Codex's floor check ("2 ≥ 2, 4 ≥ 3") uses the UN-pinned budget; under the ⁺-pin clause ("+1 at the entry-pinned lattice slots") the slot (j,i) = (1,0) sits ON the ambient ψ²-line, is pinned (the key's own Φ′² contributes exactly ψ²'s T²-coefficient 4), so the true floor is v(a_{1,0}) ≥ 3 and the member is OUTSIDE the budget — verified by direct computation (f = x⁴ + π³x² + 7π⁴ has ambient residual T⁴ + 2 ≠ ψ², off-locus). But then GENHN-1's NODE display, derived from that same budget claiming "equality realized", is WRONG at f₁ ≥ 2 (pins force dv(A_j) ≥ (μ−j)D′h + 1 = (μ−j)f₁S + 1). Codex's disjunction is therefore correct: one of the two GENHN-1 display lines fails at f₁ ≥ 2. Blame lands UPSTREAM: **erratum owed on GENHN-1's stage-initial-node display at f₁ ≥ 2** (invisible to the f₁ = 1 battery rows; the f₁ ≥ 2 stage genres F3/E13 are grammar-only, "beyond this battery's sweep windows"). For HE6: JUSTIFICATION GAP only — HE6 consumes the opening locus + HE3-0, never the node display, and its own §S3.1 Remark + HE6-3(a) PROVE the D′h threshold on the locus |
| 4 | (LIFT) is false as boxed: at small k not all f₁ slots admit O-coefficients (counterexample e₁ = 1, f₁ = 2, h = 1, k = 0: res(C(θ)) ∈ F_Q only, c = η_θ unreachable); plus the omitted normalization ϖ(θ)^k/n(k)(θ) = (θ^{e₁}π^{−h})^q twisting the residue by η^{−q} | CRITICAL | **UPHELD, CRITICAL as stated — all use sites survive.** Matches the pre-verdict key's attack-2 verdict; Codex's k = 0/f₁ ≥ 2 counterexample is sharper than the key's (e₁ ≥ 2, h ≥ 2, k < i(k)h) — both real; general failure condition k < (i(k) + e₁(f₁−1))h. The twist identity ϖ^k/n(k) = (x^{e₁}π^{−h})^q re-derived exactly (i₀k = i + qe₁, a₀k − a = −qh). Use sites: every invocation has k = (d−t)u > D′h > (D′−1)h ≥ (i(k) + e₁(f₁−1))h, so all f₁ slots are available; and the twist is a FIXED power of η — universal, hence ι_ξ-transportable — so surjectivity onto K^× survives. Repair: restate (LIFT) for k > (D′−1)h with the η-twist rider |
| 5 | HE6-0 third case: "η_ξ = η_{θ_i} ⟹ v(ξ−θ_i) > h/e₁ for that i" is false (ξ = −θ at Φ′ = x² − π: η equal, distance h/e₁) | GAP | **UPHELD, GAP** — the pre-verdict key's F-A, same defect (key's counterexample ξ = ζ₃θ(1+w) at e₁ = 3 refutes it at a non-root of Φ′ too). Conclusion of HE6-0 is TRUE; clean re-proof route: GENHN-2's tie clause at Φ′'s own lattice slots (the key's residual is ψ; (T2) makes ψ(η_ξ) = 0 the tie-residue ⟹ cancellation ⟹ dv > D′h). LOAD-BEARING position: HE6-3(a)'s "n_λ = 0 for λ ≤ D′h" consumes exactly this direction — the gap must be closed for the (†)-elimination's premise, as both the key and Codex's CHECKED-CLEAN wording note |
| 6 | GENHN-2 is stated only "for every root θ of Φ′"; HE6 applies it at arbitrary (T1)/(T2) points ξ — overconsumption; the ϖ-normalization factor is also outside the stated residue clause | GAP | **UPHELD, GAP.** Verified against PASTE B: the statement is at roots of Φ′; the PROOF uses only (T1) (class separation) + (T2) (η generates F_{Q^{f₁}}), so it generalizes verbatim — and HE3-0's pasted proof already asserts the generalization. Repair: restate GENHN-2 at (T1)/(T2) points (one line, proof unchanged) + the η-twist rider of F4 |
| 7 | HE6.A does not state disc(f) ≠ 0, though the proof mixes (RES)-with-multiplicity, plain sets S_{λ,r}, and Galois orbits of distinct roots | GAP | **UPHELD, GAP** (statement hygiene). The consuming site THEOREM HE3.A carries disc(f) ≠ 0 explicitly, so the composed program is unaffected; HE6.A's box must inherit the hypothesis |
| 8 | "All of Φ′, ϖ ∈ O[x]" is false: a₀ < 0 possible (e₁ = 2, h = 3 ⟹ ϖ = xπ^{−1}) | GAP | **UPHELD, GAP** — the key's F-F verbatim (same example). Harmless: coefficients are Galois-fixed in K₀, which is all HE6-5 needs, as Codex itself notes |
| 9 | c_t = 0 ⟹ B_t = 0 ⟹ dv(B_t) = ∞, so the term-by-term "dv exactly (d−t)u + ℓtδ" displays are false for zero terms | GAP | **UPHELD, GAP** (display imprecision). Endpoints t = 0 (r(0) ≠ 0) and t = d (monic) are nonzero, so every unique-minimum argument and the tie-residue sum survive with "nonzero terms" inserted |

**CHECKED CLEAN (Codex, spot-verified against the pre-verdict key):** attack 1 (HE6-1
Step 1, both ultrametric cases — the key's identical algebra, incl. (ℓ−1)D′ + 1 > 0 and
the t = d exactness from (T1)/(T2)-failure excluding Φ′(ξ) = 0); attack 3's elimination
(conditional on F2's exhaustiveness + F5's positive direction — correctly flagged as
conditional); r₀ of degree μ+1 never divides a residual; HE6-0′; HE6-2/2′ (incl. the
j ≡ j₁ mod ℓ congruence); HE6-4's strictness transfer INCLUDING shared/repeated roots
in the extended-value reading (this disposes of the key's F-B); HE6-5's composition
ι_{gρ} = ḡ∘ι_ρ; HE6.A's four-step sandwich conditional on its inputs; the Bézout
normalizer incl. a₀ < 0; DEFINITION HE6-1's degree bookkeeping.

## S3. Decorrelation record (pre-verdict key vs the delivered pass)

Key predictions CONFIRMED by the pass: F1 (attack 4's exact crack, configuration
enumeration matching item for item), F4's k-range half (different counterexample
region: key had e₁ ≥ 2 ∧ h ≥ 2; Codex's f₁ ≥ 2 ∧ k = 0 is sharper — the union is the
true failure set), F5 (same defect, different counterexample), F8 (same example).
Codex EXCEEDED the key with: **F2 (the Φ′ | f stratum — the key missed it entirely)**,
**F3 (the f₁ ≥ 2 supplier seam — missed)**, F6 (the key had judged the transport sound
"modulo convention"; Codex is right that the PASTED statement does not cover the use),
F7, F9. The key's F-B ((RES) finiteness) was answered by Codex's clean-list
extended-value reading plus, for HE6-3, the degree-μ+1 argument (no shared root
possible). Net: the hostile pass dominated the same-side key — the decorrelation
discipline earned its cost on F2/F3.

## S4. Repair queue (owed before any 1/2 grade; statement changes flagged per policy)

1. **R1 (CRITICAL, F1): restate COROLLARY HE6.B** — exclusion = "histories containing
   ANY side whose residual has a repeated irreducible factor of degree ≥ 2, at any ℓ,
   or a repeated factor at ℓ ≥ 2" (equivalently: any stage-CS event); widen HE6-BOX-1
   accordingly (its §S7.2 (LIFT₂)/(SLOT₂) program already covers ℓ = 1 via Ψ_{λ,r} at
   integer height). n ≤ 7 claims unaffected.
2. **R2 (CRITICAL, F2): add the Φ′ ∤ f convention** (peel Φ′-factors first, σ-block
   (e₁,f₁) each) or the ∞-label column to HE6-3/HE6.A and to the consumed HE3-0(iii)
   display; fix the Σ = D′μ consistency check.
3. **R3 (CRITICAL, F4): restate (LIFT)** for k > (D′−1)h + η-twist rider; DEFINITION
   HE6-1 unaffected (its k are > D′h).
4. **R4 (GAP, F5): re-prove HE6-0's third case** via the slot/ψ-cancellation route
   (this also feeds HE6-3(a)'s premise).
5. **R5 (GAP, F6): restate GENHN-2 at (T1)/(T2) points** (proof unchanged) — GENHN-side
   one-liner, or an HE6-local lemma.
6. **R6 (GAP, F7): add disc(f) ≠ 0 to THEOREM HE6.A.**
7. **R7/R8 (GAPS, F8/F9): display fixes** (ϖ ∈ K₀[x]; "nonzero terms").
8. **R9 (UPSTREAM, F3): GENHN erratum unit** — adjudicate GENHN-1's stage-initial-node
   display at f₁ ≥ 2 against the ⁺-pinned budget (expected correction: node =
   {dv(A_j) ≥ (μ−j)D′h + 1} at f₁ ≥ 2); check what, if anything, consumed the S-version
   at f₁ ≥ 2 (HE6 did not; the f₁ ≥ 2 σ-prediction {(2,2),(2,2)} of HE6 §S2 is
   proof-only and survives via HE6's own D′h remark).

## S5. Artifacts

- `he6pe1_brief_2026-08-08.txt` — the launched brief (this directory).
- `he6pe1_scaffold_2026-08-08.md` — the orchestrator's pre-verdict key (this directory).
- `/tmp/codex_he6pe1_1786222439.log` — full run log (brief echo + message streamed and
  reprinted, diff-identical); `/tmp/he6pe1_raw.txt` — extracted canonical message.
- Appendix below — the raw Codex report, verbatim, first emission.

---

## APPENDIX: RAW CODEX REPORT (verbatim)

1. Verbatim quote:

   “COROLLARY HE6.B (the program consequence, stated honestly). THEOREM HE3.A and [GENHN-HE(μ)] hold at every μ and at every decided leaf whose history contains no side with ℓ ≥ 2 carrying a REPEATED residual factor”

   and:

   “as a stage-CS event with inner data (e₁′,f₁′,μ′) = (ℓ, 1, m) — HE3 then applies THEOREM HE3.A recursively over the base ring R. That step supplies the FORCING half ((T1)/(T2) stack ⟹ divisibility of e and f) but NOT the counting half”

   Classification: CRITICAL ERROR.

   The excluded histories are not exactly the complement of the cases unsupported by the HE3+HE6 assembly. The node-by-node enumeration is:

   (i) ℓ = 1, repeated linear residual factor: HE3’s stage-α recentering is intended to handle this through HE3-2(b), followed by slope counting. The recentered key exists at the integer slope. This configuration is not the crack identified in S7.1.

   (ii) ℓ = 1, repeated irreducible residual factor of degree at least 2: HE3 routes this to the stage-CS recursion, not stage-α. HE6.A is unavailable because the residual is inseparable. The criticism quoted from S7.1 applies unchanged: the recursive theorem factors the resolvent over the stage ring and supplies divisibility for roots of f, but no argument transports the inner label class sizes to roots of f. Here e₁′ = 1 and f₁′ ≥ 2, so this defective configuration is not excluded by HE6.B.

   (iii) ℓ ≥ 2, separable residual: this is the intended domain of HE6.A.

   (iv) ℓ ≥ 2, repeated residual factor: HE6.B excludes it, and S7 declares the required deeper count unavailable.

   Thus configuration (ii) falls squarely into the crack. HE6.B’s word “exactly” and its claimed scope are false.

2. Verbatim quote:

   “COROLLARY HE6-2″ (HE3-0(iii), re-proved in this convention). Taking ξ = ρ a root of f: 0 = f(ρ) forces the excess to be infinite, hence ι_ρ(R_{λ_ρ})(β_ρ) = 0 — every root's dv(Φ′(ρ)) is a slope”

   and:

   “ρ-side: by LEMMA HE6-2 (with r₀ never a label's factor)
   dv(Ψ_κ(ρ)) = ℓd₀·min(λ_ρ, κ) for every root ρ of f”

   Classification: CRITICAL ERROR.

   The note never excludes Φ′(ρ)=0, hence λρ=∞. Such a root has no finite side label and is omitted from every displayed sum over finite slopes.

   Concrete counterexample: take e₁=f₁=h=D′=1, Φ′=x−π, ψ=T−1, μ=2, and

   f=Φ′²+π²Φ′=Φ′(Φ′+π²).

   This satisfies the stated entry bounds:
   dv(A₁)=2 and A₀=0 has dv(A₀)=∞. It also has disc(f)=π⁴≠0 and its ambient residual is ψ². Its roots are ρ₁=π and ρ₂=π−π². For ρ₁,

   Φ′(ρ₁)=0, so dv(Φ′(ρ₁))=∞.

   The finite Newton polygon only has the side from (1,2) to (2,0), of length 1. The missing root contributes

   min(∞,κ)=κ

   to the root-side resultant calculation. Omitting it makes (†) false: for 1<κ<2, the finite-slope contribution is κ, whereas h_F(κ)=2κ. Consequently the proof of HE6-3, its c₀ elimination, the identity ΣLλ=μ, and the later partition/counting sandwich do not cover all polynomials allowed by the stated locus.

3. Verbatim quote:

   “At f₁ ≥ 2 the correct disk threshold is D′h = f₁S, and LEMMA HE6-3(a) proves that P(F) has no slope ≤ D′h”

   contrasted with the consumed node:

   “the stage-initial node is
   {dv(A_j) ≥ (μ−j)S + 1}, S := e₁h.”

   Classification: CRITICAL ERROR.

   For f₁≥2 these two pasted claims are incompatible. The GENHN-1 budget permits coefficients that change the supposed entry residual and produce slopes at or below D′h, while HE3-0 and HE6-3 say that cannot happen.

   Concrete counter-configuration over O=Z₅:

   e₁=1, f₁=2, h=1, D′=2,
   Φ′=x²+2π²,
   ψ(T)=T²+2,
   μ=2,
   A₁=π²,
   A₀=π⁴.

   Here Φ′ is irreducible, its roots have valuation 1, and res(θ/π) has degree 2. The displayed GENHN node bounds hold: 2≥2 and 4≥3. But P(F) consists of a side of slope 2=D′h, with residual Z²+Z+1. A root ρ of f satisfies, after writing w=res(ρ/π),

   (w²+2)²+(w²+2)+1=0,

   so w²+2 is nonzero and w is not a root of ψ. Thus HE3-0’s asserted (T2) fails. Indeed the ambient residual becomes T⁴+2, rather than ψ². GENHN-1’s asserted bijection onto the displayed budget and HE3-0’s characterization of the opening locus therefore cannot both be true. HE6 consumes both.

4. Verbatim quote:

   “(LIFT) (a corollary of the SLOT LEMMA's residue clause). For every c ∈ K^× and every k ∈ ℤ_{≥0} there is C ∈ O[x] with deg C < D′, dv(C) = k, and res(C(θ)/ϖ(θ)^k) = c.”

   and:

   “the f₁ slots i, i+e₁, …, i+e₁(f₁−1) all being < D′ = e₁f₁.”

   Classification: CRITICAL ERROR.

   Being below D′ is not enough: the coefficients must lie in O. At height k, if i is the unique residue class with ih≡k mod e₁, making the slot i+e₁t attain k requires

   v(a_{i+e₁t})=(k−ih)/e₁−th.

   This is negative for sufficiently large t at small k.

   Concrete counterexample: let e₁=1, f₁=2, h=1, so D′=2 and ϖ=π. At k=0, any C=a₀+a₁x∈O[x] with dv(C)=0 must attain its minimum at a₀; the x-slot has value v(a₁)+1≥1. Hence

   res(C(θ))=res(a₀)∈F_Q,

   so c=ηθ∈K∖F_Q cannot be realized. Thus the boxed assertion “every c” at “every k” is false.

   There is also an omitted normalization factor. If n(k)=x^iπ^a is the canonical height-k normalizer and ki₀=i+qe₁, then

   ϖ(θ)^k/n(k)(θ)=(θ^{e₁}π^{-h})^q,

   so division by ϖ^k multiplies the slot residue by ηθ^{-q}. The literal claim that the GENHN-2 polynomial is unchanged under the ϖ-power normalization is false. DEFINITION HE6-1 explicitly invokes this false universal corollary.

5. Verbatim quote:

   “Finally if (T1)/(T2) hold, η_ξ is a root of ψ; the roots of ψ are exactly the η_{θ_i} … so η_ξ = η_{θ_i} for some i, whence v(ξ−θ_i) > h/e₁ for that i (the converse computation: v(ξ−θ_i) = h/e₁ would force … η_ξ ≠ η_{θ_i})”

   Classification: JUSTIFICATION GAP.

   Equality of e₁-th-power residues does not imply equality of the underlying leading residues, so it does not imply proximity to that particular θ_i. For example, over odd residue characteristic with e₁=2, take Φ′=x²−π, a root θ, and ξ=−θ. Then

   ηξ=res(ξ²/π)=res(θ²/π)=ηθ,

   but

   v(ξ−θ)=v(−2θ)=h/e₁,

   not strictly greater. The quoted “converse computation” is false. A separate argument would be required to show that ξ is close to some appropriate conjugate root. The negative-direction exactness used in HE6-1 Step 1 is not affected: when (T2) fails, no factor can have distance greater than h/e₁.

6. Verbatim quote:

   “SLOT LEMMA applies at ξ: dv(B_t(ξ)) = (d−t)u exactly”

   and PASTE B’s actual statement:

   “dv(A(θ)) = min_i (e₁·v(a_i) + i·h) EXACTLY, for every root θ of Φ′”

   Classification: JUSTIFICATION GAP.

   GENHN-2, verbatim, is stated only at roots θ of Φ′. HE6 repeatedly applies it to arbitrary (T1)/(T2) points ξ, including roots of f and roots of the generally reducible test polynomial Ψ. The universal-polynomial proof strongly suggests a related statement, but the consumed lemma does not state it. Moreover, transport after division by the fixed ϖ^k involves the η-power normalization factor described in finding 4, which is not part of GENHN-2’s stated residue clause. This is an explicit overconsumption of the pasted supplier.

7. Verbatim quote:

   “Throughout, for monic f, g ∈ O[x] the resultant symmetry gives … sums over roots with multiplicity in K̄₀ (disc f ≠ 0 makes f's roots simple).”

   and:

   “THEOREM HE6.A … each S_{λ,r} is the root set of a single monic irreducible factor of f”

   Classification: JUSTIFICATION GAP.

   HE6.A does not state disc(f)≠0, although its proof changes between roots counted with multiplicity in (RES), ordinary sets Sλ,r, and Galois orbits of distinct roots. The assertion Σr|Sλ,r|=nλ and the conclusion that a class is exactly one irreducible factor require consistent multiplicity bookkeeping. PASTE C’s HE3.A does impose disc(f)≠0, but HE6.A as independently stated does not.

8. Verbatim quote:

   “All of Φ′, ϖ ∈ O[x] have g-fixed coefficients”

   Classification: JUSTIFICATION GAP.

   In general ϖ is not in O[x]. For e₁=2 and h=3, the prescribed solution is i₀=1, a₀=−1, so

   ϖ=xπ^{-1}.

   It lies in K₀[x], not O[x]. Its coefficients are still Galois-fixed, so the equivariance calculation does not fail for this reason, but the quoted membership assertion is false and the sign of a₀ was not audited in the note.

9. Verbatim quote:

   “dv(B_t) = (d−t)u for t < d”

   and:

   “(and B_t := 0 if c_t = 0)”

   and later:

   “the term t … has dv exactly (d−t)u + ℓtδ”

   Classification: JUSTIFICATION GAP.

   If c_t=0, then B_t=0 and dv(B_t)=∞, not (d−t)u; the corresponding summand likewise has infinite valuation, not the displayed finite value. The endpoint coefficients c₀ and c_d are nonzero, so the unique-minimum arguments away from δ=κ remain available, and zero terms contribute zero to the residual sum. Nevertheless the repeated term-by-term exactness claims are false as written.

CHECKED CLEAN

- Attack point (1): In HE6-1 Step 1, conditional on HE6-0’s negative-direction equality, both ultrametric cases are arithmetically correct. For dv(ξ)≥h, the t=d term has exact value ℓdD′h and every nonzero t<d term is strictly higher. For dv(ξ)<h, substituting dv(ξ)=h−ε gives exactly

  (d−t)ℓ[(κ−D′h)+D′ε]−(D′−1)ε,

  and d−t≥1 yields

  ℓ(κ−D′h)+(ℓD′−D′+1)ε>0.

  The coefficient ℓD′−D′+1=(ℓ−1)D′+1 is positive. The t=d exactness is valid because failure of (T1)/(T2) excludes Φ′(ξ)=0.

- Attack point (3): Once finite labels are exhaustive, at least one slope exceeds D′h, and all finite slopes are positive, the affine argument on (D′h,λ₁) is correct: vanishing at every rational point forces both the κ-coefficient and constant term to vanish. The subsequent triangular elimination between rational breakpoints is also correct. Finding 2 shows that those prerequisites were not established for A₀=0.

- The generic test r₀ of degree μ+1 cannot divide a residual polynomial of degree at most μ, and r₀(0)≠0.

- HE6-0′’s ramification divisibility e₁ℓ|e(Lρ), residue-degree divisibility f₁deg(r)|f(Lρ), and resulting degree lower bound are correct.

- HE6-2’s unique-minimum analysis for δ<κ and δ>κ, and its cancellation criterion at δ=κ, are correct for nonzero summands, conditional on the extended SLOT statement and residue transport.

- HE6-2′’s lattice congruence j≡j₁ mod ℓ is correct because gcd(u,ℓ)=1 and the pin ordinates are integral.

- HE6-4’s strictness transfer survives repeated roots of Ψ and shared roots of f and Ψ when valuations are interpreted in the extended sense: a common root makes both resultant sides infinite, and still forces a nonempty exceptional class. Multiplicities in (RES) are handled consistently there.

- HE6-5’s composition ιgρ=ḡ∘ιρ and the consequent preservation of the fixed K-polynomial r are correct; the false assertion ϖ∈O[x] is unnecessary because ϖ∈K₀[x] suffices.

- Conditional on nonempty disjoint Galois-stable classes of the asserted total size, the four-step sandwich in HE6.A is correct. The orbit-size lower bound, equality of the sums, and e·f=degree force the displayed invariants.

- The Bézout normalizer exists and satisfies dv(ϖ(ξ))=i₀h+e₁a₀=1 exactly, including cases a₀<0.

- DEFINITION HE6-1’s degree bookkeeping and monicity are correct: every lower term has degree strictly below D′ℓd, including ℓ=1 and d=1.

VERDICT

THEOREM HE6.A does not survive as proved or as stated: its chain uses the false universal (LIFT), overconsumes GENHN-2, omits roots with Φ′(ρ)=0 from the slope/resultant inversion, and does not state the squarefreeness hypothesis used in its class/orbit bookkeeping. More fundamentally, at f₁≥2 the consumed GENHN node budget conflicts with HE3-0 and HE6’s D′h disk threshold. COROLLARY HE6.B is false as stated even after assuming HE6.A: histories with an ℓ=1 repeated irreducible residual factor of degree at least 2 enter exactly the stage-CS counting gap admitted in S7.1, but are not excluded by HE6.B.
