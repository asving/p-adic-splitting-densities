# TAMEKQX — exact rational ties for the unramified-K moments: letter-blindness (TKQX-0), the menu×recursion double computation at every certified degree, and the q-adic-expansion protocol with a live Kac–Rice instance

**Unit:** TAMEK-QX (Asvin's methodological directive, 2026-08-09, verbatim: "numerical
archimedean coincidences are rarely very good evidence because the errors are often
O(1/q^something large). Rational equality is very good of course... Could a q-adic
expansion work? (this is regarding verifying our higher order moments for the
unramified case)").
**Predecessors:** TAMEK_2026-08-08.md (S1 TK-0; S4.R corrected unramified mean law;
S5 Euler-product reduction), MOMENTS_2026-08-08.md (the O(n²) PGF recursion),
HMENU3/GENH4/BCFGTIE (the certified menus).
**Status:** COMPLETE at commit 2. **VERDICT: GREEN — 48 checks, 0 fails, 3/3 teeth
bite, exit 0** (`tamekqx_results.json`; run arc: run 1 aborted on a symbol-identity
instrument bug; run 2 exit 1 with 24/24 n≤4 cells EQUAL + three instrument errors
r2a/r2b/r2c repaired with disclosure; run 3 = the clean scoring run — full record in
the runner docstring and S6/S7).
**Grade:** attempt 0/2 (this note is itself attempt 0/2; no hostile pass).

---

## S0. Overview for Asvin

Your directive — archimedean/MC coincidences are weak evidence (errors O(1/q^big));
rational equality is what counts; could a q-adic expansion work for the unramified
higher moments? — answered in three tiers, all EXACT, no Monte Carlo anywhere.

(1) **For unramified K the letter machinery is irrelevant — X_K is SHAPE-ONLY**
(LEMMA TKQX-0, one line: unramified extensions are unique per degree, so there is
no twist class to see). Consequently every unramified-K moment at a menu-certified
degree is an exact ℚ(q) computation.

(2) **Tier 1 (rational equality): ALL 32 CELLS EQUAL.** E[X_K^k](n) computed
symbolically in q two independent ways — (a) the certified menus (TK-Q n=2, HMENU3
n=3, GENH4 n=4, BCFGTIE n=5) weighted by X_K(τ)^k, and (b) the MOMENTS PGF
recursion generalized over the divisor lattice (TAMEK S5; consumes only P_m(j) +
level-0 independence, no menu, no engine) — for f ∈ {2,3}, k ∈ {1,2,3,4},
n ∈ {2,3,4,5}: identical rational functions of q, every cell (S3). The routes
share no computation; the menus' upstream suppliers and the recursion's suppliers
are different theorem stacks. Corollary: the S4.R mean law (f = 2, n ≥ 4 closed
form) is re-derived twice over. Your 17 MC ties in the sealed TAMEK battery are
hereby RETIRED to consistency-grade evidence (dated append to TAMEK). One catch
worth knowing: the committed quintic table is the PROJECTIVE model (its K = F mean
is exactly 1) — the n = 5 ties run through the projective conversion, which the
runner also validates (S2).

(3) **Tier 2 (your q-adic expansion): yes, it works — protocol in S4, live
instance executed.** The instance: E[X_K] for K unramified quadratic, computed by
direct configuration counting (a p-adic Kac–Rice integral over O_K graded by the
conjugate-collision codimension w = v(z − z̄)) — independent of the cluster
recursion AND the menus. It reproduces TK-Q at n = 2, the n = 3 anchor 357/242
(at q = 3), and the S4.R stabilized closed form for n ≥ 4 — both as an exact
rational identity and, per your protocol, as a 1/q-expansion from finitely many
strata matched to the determining depth (num deg + den deg + 1 = 15). Bonus
insight the route surfaces: stabilization at n = 2f is "min-poly degree f + two
free coefficients", and the n = 3 dip is "one free coefficient short of covering
O[z]". The stabilized SECOND moment by this third route resists at one explicit
integral (decomposed and boxed, TKQX-BOX-1) — but note Tier 1 already certifies
k ≤ 4 exactly, so the box concerns the third route's reach, not the moments.

## S1. LEMMA TKQX-0 (unramified K is letter-blind)

**LEMMA TKQX-0.** Let K/F be UNRAMIFIED of degree f, and L = (e′, f′) any finite
extension of F (tame or wild, any letter). Then

      #Hom_F(L, K) = f′ · [f′ | f] · [e′ = 1] .

*Proof (one line).* Any subfield of an unramified extension is unramified, so
Hom = ∅ unless e′ = 1; and there is EXACTLY ONE unramified extension of each degree,
Galois cyclic, so L = F_{f′} embeds iff f′ | f, in exactly #Aut(L) = f′ ways. ∎
(Equivalently: TAMEK's TK-0 at K = (1, f, 0): e′ | 1 forces e′ = 1, then
g = gcd(1, q^f − 1) = 1 and the letter congruence is mod 1 — vacuous.)

**Conceptual reason:** unramified extensions are unique per degree; there is no twist
class to see. **Consequence:** for random monic f of degree n,

      X_K = Σ_{irreducible factors g of type (1, f′), f′ | f} f′

— a function of the SHAPE (the (e,f)-multiset of the factorization type) alone.
The letter layer (TK-0's congruence, TK-BOX-2's joint equidistribution) never enters:
every unramified-K moment at a menu-certified degree is decided by the committed
shape menus. This holds at EVERY residue characteristic (wild factors have e′ > 1
and contribute 0), so the symbolic-in-q comparison below is meaningful at all q.

## S2. The two routes and their sources (grades disclosed, as-of 760d20f)

**Route (a), menus:** E_menu[X_K^k](n) = Σ_τ R_τ(q) · X_K(τ)^k, X_K(τ) =
Σ_{(1,f′) ∈ τ, f′|f} f′.
- n = 2: THEOREM TK-Q's three-type menu {q/(2(q+1)), q/(2(q+1)), 1/(q+1)}
  (TAMEK, attempt 0/2; proved there for q odd via discriminant square classes —
  consumed symbolically at all q with that disclosure).
- n = 3: COROLLARY HM3.D's five rows (HMENU3 — **ACCEPTED 2/2**, PE3+PE4 clean;
  conditional on its displayed stack).
- n = 4: GENH4's eleven rows (`genh4_checks_results.json:Rtau`, verdict GREEN;
  arc through PE5).
- n = 5: BCFGTIE S9's seventeen quintic rows (`bcfgtie_results.json:
  quintic_densities`, verdict GREEN; attempt 0/2; HE-free per BCFGTIE S9).
  **MODEL KEY (r2a):** this table is the PROJECTIVE model (machine-verified:
  its K = F mean is exactly 1, not q/(q+1)); the n = 5 comparisons therefore run
  against the recursion's projective conversion b_n F^p_n = Σ_j q^{n−j} F_{n−j} φ_j
  (MOMENTS Cor 2.2, the ∞ point as one more rational residue point). The n = 2, 3,
  4 menus are MONIC (HM3.D "MONIC level-0 assembly"; GENH4 empirically confirmed —
  its 8 cells tie the monic recursion exactly).
- n = 6, 7: **NO committed menu tables exist in-repo** (only the type COUNTS
  34/52 in `menu_growth`); Tier 1 therefore stops at n = 5. Disclosed.

**Route (b), recursion:** the MOMENTS master identity run over the divisor lattice
(TAMEK S5, unramified case):

      Σ_n F_n^{(K)}(t) u^n = (1/(1−u)) · Π_{δ|f} [ (1−(u/q)^δ) · W_δ((u/q)^δ, t) ]^{N_δ(q)}
      W_δ(v, t) = Σ_m φ_m^{(f/δ)}(t^δ; q^δ) v^m ,   φ_m^{(f′)}(s; Q) = Σ_j P_m(j; Q) F_j^{(f′)}(s; Q)

with P_m(j) the BCFGTIE Lemma C law and N_δ(q) the closed-point count of degree δ
on 𝔸¹. Consumes: P_m(j) (re-derived independently in MOMENTS C1), (J-D0-PROD)
level-0 independence, the α-re-entry cell law — the same supplier stack as MOMENTS,
NOT the menus and NOT the engine. The two routes share no computation.

## S3. TIER-1 VERDICT TABLE  (commit 2, verbatim from `tamekqx_results.json`)

Per (f, k, n): menu route == recursion route as rational functions of q
(sympy exact cancellation; model per column — n = 5 projective, r2a):

      f = 2:            n=2      n=3      n=4      n=5(proj)
        k = 1           EQUAL    EQUAL    EQUAL    EQUAL
        k = 2           EQUAL    EQUAL    EQUAL    EQUAL
        k = 3           EQUAL    EQUAL    EQUAL    EQUAL
        k = 4           EQUAL    EQUAL    EQUAL    EQUAL
      f = 3:            (same 16 cells)   ALL EQUAL

**32/32 EQUAL — zero mismatches.** Corollary rows: the recursion's f = 2 mean at
n = 4 and n = 5 both equal the S4.R closed form
q(2q⁶+2q⁵+3q⁴+2q³+3q²+q+1)/((q+1)(q²+1)Φ₅) symbolically (P3), so the menus
certify S4.R independently at n = 4 (monic) and n = 5 (projective conversion).
Menu cross-foot Σ_τ R_τ = 1 at all four degrees (P1); sanity gates: MOMENTS' F₂
and E_n = q/(q+1) reproduced (f = 1), projective K = F mean = 1 at n = 5.

## S4. SECTION TKQX-2 — the q-adic expansion protocol, and the live instance

### The protocol (precise statement)

Let A(q), B(q) be the two sides: B = the recursion side, an explicit rational
function with numerator/denominator degrees (a_B, b_B) READ OFF its symbolic
output; A = the truth side, E[X_K^k](n) as a function of the residue size q.
- **Degree bound on A (the honest step):** both sides are bounded as q → ∞
  (E[X_K^k] ≤ n^k uniformly), so the reduced form of A has num deg ≤ den deg.
  The denominator bound b_A is supplied by the structure theorem (the uniformity
  theorem's rational-form clause: A ∈ ℚ(q) with denominator dividing the
  recursion side's, up to the displayed cyclotomic factors). Where this step is
  consumed it is BOXED as an input (TKQX-BOX-2), not proved here.
- **Determination:** if A − B = P/(Q_A Q_B) with deg(Q_A Q_B) ≤ b_A + b_B, and the
  1/q-Laurent expansions of A and B agree through the coefficient of q^{−(b_A+b_B)}
  (i.e. b_A + b_B + 1 leading coefficients, both sides O(1)), then
  P = (A−B)·Q_A Q_B is a polynomial that is O(q^{−1}) at infinity, hence ZERO:
  **A = B exactly.**
- **The truth-side coefficients are codimension-graded configuration counts:**
  the falling moment E[(X_K)_r] is the expected number of ordered r-tuples of
  distinct K-roots; grading the Kac–Rice integral by collision codimension
  (valuations of root differences/conjugate differences), the coefficient of
  q^{−i} receives contributions from FINITELY many strata, each a q-uniform count.
  So each depth-D verification is a finite exact computation.

### The live instance: E[X_K] for K unramified quadratic, by configuration counting

The p-adic Kac–Rice identity (exact pathwise for separable f, standard change of
variables): X_K = ∫_{O_K} δ_K(f(z)) |f′(z)|_K dz, with |·|_K the module of K
(|π|_K = q^{−2}) and δ_K the Haar-dual delta. Write z = x + yθ (θ the Teichmüller
basis of O_K = O ⊕ Oθ; θ − θ̄ is a UNIT for every q, even q included) and grade by
the **conjugate-collision codimension w := v(y)** (the F-locus y = 0 is measure
zero in O_K; roots in F are counted by the separate base integral, which gives
E[X_F] = q/(q+1) for n ≥ 2 in one line — density 1 × E|h(z)|_F, h(z) Haar on O).
For z strictly quadratic (w < ∞):

* f(z) = 0 ⟺ m_z | f (m_z the degree-2 minimal polynomial); writing f = m_z·h + r
  with (h, r) independent Haar (division is a triangular bijection), the remainder
  value r(z) is uniform on O[z] = O ⊕ Oyθ, of index [O_K : O[z]] = q^w — so the
  δ-density at 0 is **q^w** (the collision-codimension weight);
* conditional on m_z | f: f′(z) = (z − z̄)·h(z) with |z − z̄|_K = **q^{−2w}**, and
  for n ≥ 4 the value h(z) is uniform on O[z] (monic h of degree n−2 has ≥ 2 free
  low coefficients; ALL powers z^j lie in O + Oz = O[z]) —
  so |h(z)|_K has mean J(w) := E[q^{−2 min(A, w+B)}], A, B iid with
  P(· = a) = (1−1/q)q^{−a}. **The 2f = 4 threshold is conceptual here: "min-poly
  degree 2 + two free coefficients" — that is WHY the mean stabilizes at n = 2f.**

Assembling (vol{v(y) = w} = (1−1/q)q^{−w}):

      E[X_K](n ≥ 4) = q/(q+1) + (1−1/q) Σ_{w≥0} q^{−2w} J(w) ,
      J(w) = 1 − (1−q^{−2})·[ q^{−1}(1−q^{−3w})/(1−q^{−3}) + q^{−3w}q^{−2}/(1−q^{−4}) ]
      (so J(0) = q²/(q²+1), J(∞) = E[q^{−2A}] — the sealed runner's transcription
      of the second bracket carried q^{−4} for q^{−2}; repaired r2b, disclosed)

(the Abel form E[t^M] = 1 − (1−t)Σ_{m≥1}t^{m−1}P(M ≥ m); ARC RECORD: the first
derivation used a WRONG Abel formula — (1−t)Σt^mP(M≥m) — and missed by exactly
q²/((q+1)Φ₅); the bug was caught pre-seal by an independent exact O_K root counter
with per-w stratum readout, the corrected per-w densities re-verified against it,
and only then sealed. The stratum counter also confirmed the per-w decomposition
itself: w = 0 carries J(0) = q²/(q²+1), the collision strata the rest.)
The same integral at n = 2 (h ≡ 1, J ≡ 1) gives 2q/(q+1) = TK-Q, and at n = 3
(h = x + c₀: h(z) uniform on a coset of O only — J₃(w) = E[q^{−2min(A,w)}]) gives
TAMEK S3's unramified-quadratic anchor q/(q+1) + q³(q²+q+1)/((q+1)Φ₅) (= 357/242
at q = 3) — the unramified mean's own dip (2q/(q+1) → this → the n ≥ 4 plateau),
seen by the configuration count as "one free coefficient short of covering O[z]".
(r2c, disclosed: the sealed runner's TARGET for this leg was the RAMIFIED-quad
dip anchor 1 − Φ₆/Φ₅, pasted in error.) Machine legs: P4/P5 (symbolic identity
with S4.R + the depth-15 truncated-strata expansion tie).

## S5. What resists (boxed honestly)

* **TKQX-BOX-1 (the second moment's configuration count — decomposed, remaining
  integral boxed).** The charged Tier-2 instance was the stabilized second moment;
  it RESISTS at one specific integral and the mean was executed instead (the
  fallback the charge sanctions). What is settled: E[(X_K)_2] = (E[X_K] − E[X_F])
  + A. The first term is the conjugate-pair contribution — (z, z̄) is an ordered
  pair of DISTINCT roots carried on the singular antidiagonal {z₂ = z̄₁}, which the
  absolutely-continuous double integral cannot see; its expected count is exactly
  2·E[#conjugate pairs] = E[X_K] − E[X_F], already computed. A = the
  off-conjugate-diagonal double Kac–Rice integral ∫∫ρ₂(z₁,z₂)dz₁dz₂ with
  ρ₂ = (density of m_{z₁}m_{z₂} | f) × E[|f′(z₁)f′(z₂)|_K | ·]. The exact
  obstruction: ρ₂ needs a FOUR-parameter collision grading (w₁, w₂,
  v_K(z₁ − z₂), v_K(z₁ − z̄₂)) with species boundaries (each root F-rational or
  quadratic), and the colliding strata (z₂ ≡ z₁ deep) re-enter pair-correlation
  structure that this unit did not derive. Nothing here is conjectured; the boxed
  object is one explicit integral. Note Tier 1 already certifies the k = 2, 3, 4
  moments by exact rational identity at n ≤ 5 — the box is about extending the
  menu-free THIRD route, not about the moments' truth.
* **TKQX-BOX-2 (the truth-side denominator bound).** The protocol's degree-bound
  step for the truth side rides the uniformity theorem's rational-form clause;
  consumed as a boxed input where the expansion-only (not exact-tie) form of the
  protocol is invoked.

## S6. Preregistered predictions (sealed at commit 1) and machine verdict

Runner: `verification/openmath/tamekqx_checks.py` (all symbolic sympy; NO Monte
Carlo anywhere in this unit). Disclosure: the Tier-2 closed-form identities
(P4/P5) were scratch-checked at composition time before sealing [proto], as were
no Tier-1 cells (P2/P3 first evaluated at the scoring run).

* **P1 (MENU-FOOT).** Σ_τ R_τ = 1 symbolically at n = 2, 3, 4, 5 for the consumed
  menu transcriptions.
* **P2 (EQ, the heart).** For every cell (f, k, n) ∈ {2,3} × {1,2,3,4} ×
  {2,3,4,5}: menu route == recursion route as rational functions of q
  (sympy `simplify(difference) == 0`). 32 cells, each reported EQUAL/MISMATCH;
  any MISMATCH is a MAJOR finding reported exactly, not patched.
* **P3 (S4.R corollary).** Recursion route, f = 2: E(4) = E(5) =
  q(2q⁶+2q⁵+3q⁴+2q³+3q²+q+1)/((q+1)(q²+1)Φ₅) symbolically — the S4.R mean law
  re-derived; with P2 this makes the menu route certify it too at n = 4, 5.
* **P4 (TIER-2 instance).** The Kac–Rice configuration-count closed form
  (S4, independent of P_m(j)/menus/engine) equals the same S4.R value
  symbolically, AND its 1/q-expansion from truncated finite strata matches to
  depth b_A + b_B + 1 with the truncation tail bounded explicitly.
* **P5 (TIER-2 small-n).** The same route at n = 2 gives 2q/(q+1) (TK-Q) and at
  n = 3 gives 1 − Φ₆/Φ₅ (the TAMEK S3 dip anchor), symbolically.
* **P6 (TEETH, 3).** T1 wrong-weight mutant (drop the f′ | f divisibility in
  X_K(τ)) must break ≥ 1 P2 cell; T2 wrong-menu-row mutant (swap the two n = 3
  rows R_{(1,2)(1,1)} ↔ R_{(2,1)(1,1)}) must break ≥ 1 P2 cell; T3 block-drop
  mutant (recursion without its δ = f block) must break ≥ 1 P2 cell. All three
  must bite; a tooth not biting voids the battery's evidential value.

**Falsifiers.** A P2 MISMATCH indicts (in order): the menu transcription consumed
here, TKQX-0's consequence display, the S5 master-identity generalization, then
the upstream menu itself — reported exactly, NOT patched. P3 failing kills the
S4.R closed form as displayed. P4/P5 failing kills the Kac–Rice leg (the Tier-2
instance) while leaving Tier 1 intact.

## S7. VERDICT  (commit 2, from the committed artifacts `tamekqx_results.json` /
`tamekqx_output.txt`; scoring run 65.0 s, exit 0)

**GREEN: 48 checks, 0 fails, 3/3 teeth bite.**

* **P1 ✓** menu cross-foots Σ_τ R_τ = 1, n = 2, 3, 4, 5 symbolic.
* **P2 ✓ 32/32 EQUAL** (the S3 table) — the certified menus and the P_m(j)-only
  divisor-lattice recursion agree on every unramified moment cell, as identical
  rational functions of q.
* **P3 ✓** S4.R re-derived: recursion E(4) = E(5) = the closed form, symbolic.
* **P4 ✓** the Kac–Rice configuration count equals S4.R exactly, AND its
  truncated-strata 1/q-expansion (w ≤ 8, valuations ≤ 16) matches to the
  determining depth 15 — the q-adic protocol executed end to end.
* **P5 ✓** the same route reproduces TK-Q (n = 2) and the unramified n = 3 anchor.
* **P6 ✓ TEETH 3/3:** T1 wrong-weight (drop f′|f) breaks 6 cells; T2 swapped n=3
  menu rows breaks the f=2 mean cell; T3 δ=f block dropped breaks f=2 n=2.

**Run arc (full disclosure; also in the runner docstring):** run 1 aborted — the
JSON menus sympified to a plain Symbol('q') distinct from the module's positive q,
so true identities failed to cancel (byte-identical displays; repaired, one line).
Run 2 exit 1 — 24/24 n ≤ 4 cells EQUAL + teeth green; 10 FAILs = three instrument
errors: r2a the quintic menu's PROJECTIVE model key (consumption error, mine),
r2b the piece2 exponent transcription slip in P4's sealed closed form, r2c the
wrong sealed target (ramified-quad anchor) for P5-n3. Run 3 (repairs committed
with disclosure at 24a79e6) is the clean scoring run. No menu row, recursion
coefficient, P_m(j), or tooth was modified at any point in the arc.

## S8. Grade box and conditionality

- Attempt 0/2 (no hostile pass; PE passes owed if adopted). The three-run
  instrument arc (S7) is disclosed in full; every repair is tagged r2a/r2b/r2c at
  its site and none touched a predicate, menu row, or recursion coefficient.
- Proved here, self-contained: TKQX-0; the Kac–Rice mean legs (n = 2, 3, ≥ 4, all
  q including even), machine-tied at P4/P5 and cross-tied to S4.R/TK-Q/the S3
  anchor.
- Consumed: menus at their displayed grades (S2); P_m(j) + (J-D0-PROD) +
  α-re-entry (MOMENTS' stack) for the recursion route; TAMEK S5's unramified
  reduction (structure only — the code re-implements it).
- Novelty: all (c)-labelled, unsearched (TAMEK TK-BOX-5 still open).
