1. **CRITICAL — (K1-NSF′) still does not prove pin → predicate → corpus.** The decisive implication is asserted between (q-4) and (q-1): “all leaves (τ-irr)/(τ-hen)” is treated as implying “no ns-marks.” But (q-1) makes absence of ns-marks an independent part of `decided ns-free`; none of (q-1)–(q-4) says ns-marks occur only on ns-leaves or cannot coexist with an irr/hen terminal leaf. The proof imports the unquoted claim that the Hensel payload “carries no ns-marks.” There is a second gap between M04’s phrase “complete finite realizable canonical tree” and the guarded TREE-N corpus: (q-3) reports both scopes but does not quote an equivalence. Quotes (q-5) and (q-6) provide no missing implication—the design correctly admits that (q-5) routes nothing, while (q-6) is intended drainage rather than a theorem about the interface. Thus the displayed evidence does not establish the advertised chain.

2. **CRITICAL — pricing (NSF-KEY) without a new [M] row is not justified.** The existing K4/[3t] seam prices `count_tie`, but (NSF-KEY) is also a K1/N2 label-routing proposition: the classifier must refuse ns-marked witnesses. The design itself says the current carrier violates this proposition and that the same defect refutes `canonical_stable` and makes `VPSound` unsatisfiable. Those are not merely the numerical K4 tie under a narrower corpus. Citing an already-owed transcription audit does not demonstrate that this distinct mandatory proposition shares the same ledger home. Under the design’s own rule that a mandatory open proposition needs an [M] home, “named execution obligation riding an existing seam” remains an assertion.

3. **CRITICAL — (†18) is not an exhaustive dependency trace.** The design says Step 17 supplies mass laws and true labels to “K1/K2,” but the K1 row’s ROOT consumption line contains neither Step 17 nor D-11 M1/M3, and its leaf column does not trace that missing input. Conversely, the K2 row traces only part of the Step-17 tranche. The Theorem-4 row invokes Lemmas 3.1 and 3.2, Bekić, `blockSolve`, `Rsh`, and `rsh_interp`, but only one Lemma-3.2 hypothesis is displayed; the remaining invoked statements and hypotheses are not traced. Therefore the table cannot support the universal conclusion that no omitted input contains normalization. Several entries are excerpts with ellipses or editorial bracket insertions, so the claim that the table supplies exhaustive “verbatim lines” is also overstated. The requested row names are present, but the dependency closure behind those rows is not.

4. **CRITICAL — corrected (O-5) closes the local exit partition, not N4’s multi-block route.** Adding terminating exits repairs the specific omitted-stratum defect. It does not display the global argument required for multi-block almost-sure termination: well-founded descent across every simultaneously spawned child, countability of all realized node addresses, and removal of the union of their reached-conditioned null exceptional sets. Moreover, child slots of one hand-off share the same parent event and are explicitly “simultaneous, not alternatives”; they therefore cannot themselves be used as disjoint accounting pieces. The design promises per-node terminal masses and further induction but never displays how shared hand-off events are aggregated without duplication. N4 remains route-incomplete.

5. **CRITICAL — N2’s stratification is not honest relative to the actual composition.** “MET AT THE INTERFACE PIN” describes a conditional interface lemma, while the design concedes that the capstone’s HEAD wiring violates that interface and that sign-off for changing it is still owed. A need consumed through the actual wiring is not met merely because a different, pinned instantiation would satisfy it. This also conflicts with the design’s own statements that the end-to-end bar cannot pass through the current wiring and that refusal of sign-off “re-opens” N2. N1/N4 as pending-r4-proof and N5/N7/N8 as priced are honestly qualified; N2 is not.

6. **CRITICAL — Unit C is not genuinely order-independent of Unit B.** E9 must record whether (NSF-KEY) is executed or sign-off-pending. If Unit B writes E9 before Unit C completes, C-4 necessarily changes ROOT afterward; that produces new ROOT text after Unit B and invalidates any identical-text acceptance sequence begun on the earlier status. The Order clause does not state that the end-to-end counter restarts after this synchronization. Unit C also changes the carrier and `count_tie` scope across the `canonical_stable`/`VPSound` cone, yet its listed checks have no explicit hostile-pass or successful-build gate before the end-to-end bar. Unit B’s r4-only gate is sound, but the B/C gate is not.

7. **ADVISORY — the design’s “BLOCKED strictly EMPTY” vocabulary obscures rather than clarifies status.** N1 and N4 lack accepted proofs; N2 is known false at HEAD pending a gated definition change. Calling all three unblocked because each has a proposed route makes “blocked” mean only “no imaginable route,” which is not operationally useful and clashes with the acceptance standing. The finer tiers are substantially more informative than the asserted empty blocked list.

VERDICT: DESIGN-REVISE