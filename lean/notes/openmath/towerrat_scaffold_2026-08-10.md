# TOWER-RAT pre-verdict scaffold (written BEFORE reading any Codex output)

Orchestrator's own pre-verdict key for the three FROZEN accepted tower notes,
per the RAT decorrelation discipline (cf. `he7rat_scaffold_2026-08-09.md`).
Committed before the runs return.

## HETOW (HETOW_RECON_2026-08-09.md, accepted 2/2 at PE3)

Charged surfaces and my expectations:
1. **Literal-instance identification (S1 table + THEOREM HETOW.A(i)).** Expect
   HOLD — each row re-checks against the pasted DEFINITION HE7-1; the three
   non-verbatim rows are carried by HETOW-1/2/3. Residual risk: the n₂ vs n̂₂
   normalizer row is discharged by "σ-content twist-invariant (J-D0)" — a
   cite, not a computation; a hostile read could grade the row's reliance on
   twist-invariance as a GAP if some consumed clause is letter-sensitive.
   My read: the letter-sensitive clause (r̃(β_ξ) = 0) is proved pointwise in
   HETOW-1(b), so the row survives; expect at most MINOR.
2. **Gauge-invariant termination arithmetic (HETOW.A(iv) + n_min = 12).**
   Expect HOLD — μ₁ ≥ μ₂e₂f₂ with equality at full side is elementary; the
   J ≤ log₂μ₁ − 1 count re-uses HE7.C's display. Risk: "counts the tower
   entry as one jump" — whether HE7.C's jump condition (repeated r with
   ℓ·deg r ≥ 2) is verified AT the tower node needs ψ₂ repeated (μ₂ ≥ 2),
   which is the standing hypothesis; expect CLEAN.
3. **Letter clause at multi-wrap (HETOW-1(c) + the W(t) cocycle).** The r1
   repair's arithmetic (Q(a+b) = Q(a)+Q(b)+wrap, telescoped to W(t)) was
   PE2/PE3-audited; the known soft spot is the PE2 F-B mechanism (W(f₂−1)=0
   pin at f₂ = 2 vs the non-linearity claim) — already a recorded MINOR.
   Multi-wrap f₂ ≥ 3 is machine-unexercised (HETOW-BOX-3 omission, PE2 F-C /
   PE3 F-α MINORs). Expect Codex to re-find the f₂ ≥ 3 blindness as MINOR;
   a new GAP would have to be in the telescoping identity itself — I
   re-derived (f₂−t)i(u₂) = i((f₂−t)u₂) + e₁W(t) and it is exact.

Predicted outcome: RATIFIED or RATIFIED-WITH-ANNEX (MINORs on the f₂ ≥ 3
axis and exemplar hygiene); CHALLENGE only if the HETOW-2 carry accounting
(F-2 leg) hides a value-T₂ residue error I did not see.

## GENTOW1 (GENTOW1_PROOF_2026-08-09.md, accepted 2/2 at PE3)

1. **ONTO cardinality (GENTOW-2 layer 3).** The onto-by-cardinality step
   consumes "slot strings are key-independent (GENHN-2′ at Φ2+ via (i))".
   This is the thinnest step in the note: bijectivity of a triangular map
   between equal finite budget boxes needs the two boxes to have the SAME
   floors, which is (i)'s "GENTOW-1 holds verbatim at Φ2+". Expect Codex to
   probe whether the event slice's cardinality equals the λ-floored node
   box's; my read: triangular + injective + equal finite counts is sound,
   but the equal-counts leg is asserted via key-independence — possible GAP
   grade from a hostile reader; I'd adjudicate REAL only if a concrete
   count mismatch is exhibited.
2. **Windows (5)(c)(d) (LEMMA GENTOW-5).** (c) is one congruence; (d) cites
   layer 2's carry monotonicity. Expect CLEAN; risk is (a)'s first-band
   clause at f₁f₂ ≥ 2 (transported "verbatim" from the level-1 band annex —
   a transfer claim Codex may test at e₂ ≥ 2 mixed flavors).
3. **Budget composition (GENTOW-1(a)(b), Step 0 weight monotonicity).**
   Expect HOLD — the two carry moves are priced by the two sides; the [pin]
   (+1 on-lattice) display matches the r2-F2 level-1 display. Risk: the
   ceil/pin arithmetic at slots where (μ₂−j)E₂ − w(a,b) < 0 (the pin
   condition's "≥ 0" guard) — check whether the floor formula degrades
   gracefully; a corner slip here would be MINOR-to-GAP.

Predicted outcome: RATIFIED-WITH-ANNEX (the layer-1 graded-frame consumption
at general μ₂ is a disclosed box — GENHN-BOX-2 pattern — which a hostile
reader may re-grade but which is disclosed, hence annex not challenge).

## GENTOW2 (GENTOW2_PROOF_2026-08-09.md, accepted 2/2 at PE6)

1. **B″ statement + proof (S5.2).** The four-step proof (peel Cor 4.7(2);
   Def 1.8 expansion minimum; the h-iii dichotomy e₃ ∤ j vs e₃ | j;
   the j = 0 digit computation) is the note's hardest new mathematics.
   Soft spots I see: (iv)'s "Φ₂(x₀) = 0 gives G(x₀) = Q₀(x₀)" needs x₀ a
   root of Φ₂ (the evaluation identification's base point) — fine; and
   (i)'s iterated Cor 4.7(2) at grade-0 factor ẑ needs 0 ∈ Γ₂ (true).
   The dichotomy step's "jλ₃ ∉ Γ₂ for e₃ ∤ j" is standard (λ₃ has exact
   denominator e₃ over Γ₂). Expect HOLD; a GAP grade could land on the
   γ-calculus residue leg (res γ₁ = z₁, res γ₂ = z₂ "through the rider C-m1
   denominator-clearing line" — a repo-internal cite Codex cannot see; must
   treat as GIVEN or flag overconsumption).
2. **FGMN cite fidelity (S3 quotes; Thm 6.3 [Q9] + Cor 6.4 [Q10] use in A(b)-(e)).**
   Codex checks internal fidelity only (the PDF is not pasted); the S6.1
   provenance table says visual re-verification happened. Expect CLEAN on
   use-sites; risk: Cor 6.4's hypothesis "φ ≁_µ φ_r" — does the application
   in GENTOW2-A(b)(c) verify it? (Φ₃ vs Φ₂ = φ_r at ν: deg Φ₃ > deg Φ₂
   forces the non-equivalence... the note should display this; if it
   doesn't, that is a legitimate GAP candidate — my main predicted finding.)
3. **ψ^{(w)} admissibility (restated B, S5).** y ↦ wy conjugation preserves
   monicity-after-renormalization, irreducibility, degree, constant ≠ 0 —
   elementary and correct. Expect CLEAN; risk only in the claim
   "consumer-invisible" (w-blindness of verdicts), which is an honesty-box
   claim, not a consumed step.

Predicted outcome: RATIFIED-WITH-ANNEX; CHALLENGE if the Cor 6.4
hypothesis-verification (φ ≁_µ φ_r) is genuinely absent at a use-site.
