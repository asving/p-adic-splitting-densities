# K2-CLOSE — (SURV-K2) AT g₀ = 2 BEYOND THE REGULAR LOCUS: the glued verdict table, corner coverage (K2-THM-B), and the same-sign multiplicity residual

STATUS: ASSEMBLED (unit B complete: legs 2+3 = K2CLOSE_LEGS23_2026-08-08.md at
11e1743/7bb28a6; leg 1a = the shape enumerator at 13a2179/8579216; leg 1b = the
verdict column at 816c75e; this note is the note of record for the unit).

Object: extend K2-THM-A (`SURVK2_PROOF_2026-08-08.md` at a8914944 — (SURV-K2)
proved at attempt grade on g₀ = 2 ∧ (K2-REG)) toward EVERY live g₀ = 2 row, by
closing the (K2-RES) strata that live at g₀ = 2: (a) the GLUED systems, the
A = {0} corner, and the budget reach on (b)/(e).

**Verdict, stated first.**  Four results, all g₀ = 2, all capped at ATTEMPT
grade by the same consumption chain as K2-THM-A (the W arc, 0/2 CLEAN):

1. **K2-L11 (ID-CLOSURE).**  The glued identity algebra is CLOSED: every
   tower-level digit identity forcible by any glued-group coincidence lies in
   ⟨I₁, I₃⟩; the only new parameters are one free glue ratio per glue edge.
   No novel digit identity exists at g₀ = 2.  (Machine shadow: 0 violations
   over every state of every shape.)
2. **K2-L12 + the VERDICT COLUMN.**  Coincidence patterns of a glued shape =
   closed partitions of its cell monomials (patterns-are-subgroups); the
   machine enumerates them exactly and grades every shape:
   **44 CONDITIONAL** (35 main + 9 annex), **9 NOVEL** (8 main + 1 annex),
   **0 PROTECTED**.  The NOVEL set = the imbalance genre: the seven |T| = 2
   even-dup main shapes + GS-40 (T = 0, killed by exhaustion) + AS-10 (T = 3).
3. **K2-THM-B (corner coverage).**  On junk-free g₀ = 2 rows whose β_min
   groups are all THM-A forms or enumerated glued shapes: if the system
   covers all four I₁/I₃ corners (S anywhere; P1 on ¬I₁; P2 on ¬I₃; T1 on
   I₁∨I₃; corner-safe glued shapes per the verdict column), then (SURV-K2)
   holds under EVERY residue assignment.  This strictly generalizes (R4).
4. **The residual, unified.**  What remains open is ONE genre: same-sign
   multiplicity — char | (a forced net count) — plus the corner-uncovered
   systems; boxed exactly at S5.  Instance leg: 20/29 realized glued rows are
   class-closed outright; the other 9 are I₃-corner-covered and protected in
   vivo; NO realized row sits in the residual unprotected.

## S0. Authority, consumption, grade caps, machine leg

Consumes read-only, at the grades of the SURVK2_PROOF header cap sentence:
SURVK2_PROOF_2026-08-08.md (K2-L1..L8, K2-THM-A, the four forms S/P1/P2/T1,
I₁/I₃, the (K2-RES) box) at attempt grade; K2CLOSE_LEGS23_2026-08-08.md
(K2-L9 the A = {0} safety lemma, K2-L10 the exact budget reach) at attempt
grade; SURVDISJ_PROOF† SD-L6 (G survives at β ⟺ Σ sgn·res ≠ 0 in K₀) and
SD-L7 (protected vector ⟹ survives, char-free); WMULTDCX† W-D2(iv); [RMG]
(accepted) S2.2/L-NORM/L-LINE; LEDGERLAMBDA_2026-08-08.md as bookkeeping
frame only.  Everything here is capped at ATTEMPT grade by the weakest
consumed arc (the W arc, 0/2 CLEAN); no hostile pass has run on THIS note.

Machine leg (all committed, deterministic, exact integer algebra):
`verification/openmath/k2close_enum.py` (the 43+10 shape table; GLUE-COVER
38/38, CTRL-MUT 190/190) and `k2close_verdicts.py` (the verdict column +
corner safety + the 29-row system census; laws GREEN 0 violations; sealed
PV-1/2/3/5 green, PV-4 MISS on record — the S6 GLUE-DEGEN finding; one
shakedown code repair disclosed in its docstring).  Scope: the [ILN]† S0.1 class, TRACK,
(H1)–(H3), c_{m−1} ≥ k, g₀ = 2; β_min junk-free/Y-free ((R0); automatic at
d₀ = 1); glued systems within cap B4 (ONE dumping interior correction — the
enumerator's declared budget; beyond-B4 is boxed, not enumerated).

## S1. The enlarged identity set, and why it does not enlarge (K2-L11)

Fix a glued bottom group G with fibers f = 1..r (fiber = one interior book;
GLUE-EDGE: the books share their interior edge skeleton and differ only in
level-0 dump vectors).  By K2-L4 the residue class of a junk-free cell with
seed t, stripped level-0 multiset K, fiber f is the MONOMIAL

    μ(cell)  =  x_t · ∏_{k∈K} ψ̄_k · ρ_f        (times a group-common factor)

in the free nonzero parameters x₀, x₁ (seed digit residues), ψ̄₀, ψ̄₁
(level-0 digit residues), ρ_f (the fiber's dump-variant residue product —
one per fiber, the (K2-RES)(a) "one new identity per glue edge").  The
tower-level identities are I₁: x₀ = x₁ψ̄₁ and I₃: ψ̄₀ = ψ̄₁²; note
I₂ = I₁ − I₃ in exponent vectors, and box (b)'s kill identity
x₀ψ̄₀ = x₁ψ̄₁³ is exactly I₁ + I₃.

**Lemma K2-L11 (ID-CLOSURE).**  Let H be any subgroup of the exponent
lattice Z^{4+r} generated by differences of cell monomials of one glued
group (any coincidence configuration).  Then H ∩ L_digit ⊆ ⟨I₁, I₃⟩, where
L_digit is the ρ-free sublattice.  Hence the enlarged identity set of the
entire glued case table is ⟨I₁, I₃⟩ plus the r − 1 free glue ratios per
group — coincidences can never force a NEW tower-level digit identity.

*Proof.*  Two integer functionals on Z^{4+r}: the x-degree
deg(v) = v_{x0} + v_{x1}, and the mass-balance functional
φ(v) = 2v_{ψ0} + v_{ψ1} − v_{x1}.  On a cell monomial, deg = 1 and
φ(μ) = M − t = −ν̂_f, the K2-L6 invariant — CONSTANT on each fiber.  Any
w ∈ H is an integer combination of differences, w = Σ_μ c_μ·μ with
Σ_{μ∈f} c_μ =: s_f summing to zero over each difference; the ρ_f-coordinate
of w is s_f, so w ∈ L_digit forces s_f = 0 for EVERY f.  Then
deg(w) = Σ_f s_f = 0 and φ(w) = Σ_f (−ν̂_f)·s_f = 0.  Finally
{v ∈ L_digit : deg v = 0, φ(v) = 0} = {(α, −α, β, −α−2β)} = ⟨I₁, I₃⟩
exactly (α·I₁ + β·I₃; integrally, so the containment is saturated).  ∎
[Machine: the ID-CLOSURE family — the digit part of every visited state H
of every shape decomposes in ⟨I₁, I₃⟩; 0 violations.]

**Lemma K2-L12 (patterns are subgroups).**  For any residue assignment
(nonzero values for x_t, ψ̄_k, ρ_f in K₀), the induced coincidence pattern
P on G's cell monomials is CLOSED: with H_P := ⟨μ_i − μ_j : i ~_P j⟩,
i ~_P j ⟺ μ_i − μ_j ∈ H_P.  Conversely every closed partition is realized
by an assignment: an integer weight functional vanishing on H_P and nonzero
on the cross-block ratios outside span_Q(H_P) gives x = z^w with z of large
multiplicative order (any characteristic); cross ratios inside
span_Q(H_P) \ H_P force roots of unity — such states are TORSION-flagged
(realizable only where K₀* carries the torsion) and are KEPT in every
universal (safety) quantification, a conservative reading.  The BFS of
`k2close_verdicts.py` (merge two blocks, close under H-membership)
enumerates exactly the closed partitions.  ∎ (attempt grade; the abstract
residue model's faithfulness to the engine is K2-L4 + GLUE-EDGE, verified
cellwise on all realized groups; on unrealized shapes it is the model.)

## S2. Legs 2 and 3 (note of record: K2CLOSE_LEGS23_2026-08-08.md)

**K2-L9 (A = {0} safety).**  On the only realizable proper availability
corner (ψ₀_1 = 0, odd characteristic, K2-L8), every SINGLE-BOOK bottom
group is one residue class of one sign; the sign-reversing involution has
no moves; the (SURV-K2) opposite-sign failure mode is absent.  Residual
(K2-L9-RES): promotion-placement multiplicity mod the odd characteristic —
a same-sign question, boxed.  NOTE THE SCOPE, sharpened by this unit's
annex: K2-L9's single-sign conclusion is a single-book statement.  GLUED
A = {0} groups evade both of its mechanisms (dump offsets absorb the
parked-parity argument and mix seeds — e.g. AS-02 — and fibers of unequal
level-0 mass mix signs — e.g. AS-01), which is exactly why the annex table
(10 shapes) exists and is graded separately at S4.

**K2-L10 (exact budget reach).**  The window budget n ≤ 3 kills only the
SINGLE-WINDOW sectors of the residual boxes (b) and (e): box (b)'s
{C₁,C₁,C₁} cell needs opening budget ≥ 4; box (e)'s placement pair is
intrinsically ≥ 2-window and budget-LEGAL (the explicit net −2 chain =
K2-CAND).  Neither box closes by budget; their survival analysis is taken
up at S4/S5 in the verdict-column language.

## S3. The verdict column (leg 1b, machine): definitions and tallies

Per shape, over the K2-L12 state space, with the SD-L6/SD-L7 reading: a
state PROTECTS iff its nonzero per-class nets are exactly {±1} or {+1,−1}
(SD-L7 — survives netting char-free); it is DEAD iff all nets vanish
(Σ sgn·res ≡ 0); the ANNEX EXTENSION grants survival (not SD-L7 protection
— fence F-2) to a single class of net ±2^k, since A = {0} forces odd
characteristic (K2-L8).  The column:

    PROTECTED    every state protects (survival unconditional);
    CONDITIONAL  some state protects, some does not;
    NOVEL        no state protects — SD-L7 can NEVER certify the group.

The TOTAL INVARIANT drives everything: T := Σ sgn·mult is pattern-blind
(coincidences move mass between classes, never create or destroy it), and a
protecting state needs T ∈ {0, ±1} (annex: also ±2^k).  Machine tallies
(`k2close_verdict_table.md`, laws GREEN):

    main  (43):  35 CONDITIONAL, 8 NOVEL, 0 PROTECTED;
    annex (10):   9 CONDITIONAL, 1 NOVEL, 0 PROTECTED;
    NOVEL = {GS-11, GS-19, GS-20, GS-21, GS-30, GS-34, GS-35} (|T| = 2,
    ALL even-n dup shapes — the parity datum's live even dup groups),
    + GS-40 (T = 0, exhaustive kill over its 53 states), + AS-10 (T = 3).

Corner safety (main): I₁-corners safe {GS-04, GS-06, GS-15, GS-24};
I₃-corners safe {GS-05, GS-08, GS-16, GS-25}; c11 (I₁∧I₃) only {GS-38,
GS-41}; every other shape safe on NO corner.  No shape is c00-safe: the
generic-corner glue kill is universal.

## S4. The survival derivations, BY CLASS-VECTOR PATTERN

**(PC-1) The glue pair, pattern (−1, +1)** [GS-01/02/03; AS-01/02 — the
realized workhorse: 28 of the 38 realized glued groups are GS-02].  Two
cells, opposite sign, one per fiber.  Dichotomy over the single glue
identity g: ρ₂/ρ₁ = (the displayed digit ratio): if ¬g the two classes are
distinct — the protected pair (+1, −1) (SD-L7(b), opposite signs by K2-L4
parity as in THM-A); if g the group is DEAD (a +1/−1 pair in ONE class)
and simply drops out of the netting — the row must be carried elsewhere.
g is available in every I-corner (glue ratios are corner-independent), so
glue pairs can never anchor a row: verdict CONDITIONAL, corners nnnn.

**(PC-2) The anchored chains, |T| = 1** [main GS-04–08, 15–18, 24–27, 33,
38, 39, 41; annex AS-04–07].  THE FULL-MERGE ANCHOR: since every state's
nets sum to T = ±1, the all-coincident state is one class of net ±1 —
protected outright (SD-L7(a)).  Partial-coincidence states can fail
(machine kill witnesses; e.g. the generic state whenever ≥ 3 monomials).
The load-bearing subfamily is the CORNER-SAFE anchors: on their safe
corners EVERY glue configuration protects — e.g. GS-08 under I₃: the
in-fiber I₃ pair cancels to 0 and the lone remainder cell gives (0, −1),
and every further glue merge only re-routes the zero; symmetrically
GS-04/06 under I₁.  These are the NEW dichotomy partners for K2-THM-B:
{P1 + I₁-safe glued} and {P2 + I₃-safe glued} close rows exactly as
{P1 + T1} does in THM-A.

**(PC-3) The balanced forms, T = 0, ≥ 3 monomials** [GS-09–14, 22, 23, 28,
29, 31, 32, 36, 37, 42, 43; AS-09].  Full merge is DEAD (T = 0); the
generic state is unprotected (≥ 3 nonzero nets); protection lives on
intermediate identity loci — each machine-witnessed, e.g. GS-22's
protecting state sits on the RATIO identity I₂ = I₁ − I₃ (digit readout
I1^1*I3^-1), the first in-vivo role for I₂ outside T1's corner.
CONDITIONAL, no safe corner (the glue kill survives every corner):
passengers, never anchors.

**(PC-4) The imbalance genre, |T| ≥ 2 — the even dup groups** [NOVEL:
GS-11, 19, 20, 21, 30, 34, 35, all even-n dup shapes; T = ±2 each].
SD-L7 certification is IMPOSSIBLE: protected vectors net to 0 or ±1, but
every state nets to T.  This settles the parity datum's question: the
even dup groups are not merely unforced by enumeration — they are exactly
the shapes the protection calculus can never save.  Survival is NOT
excluded: the full merge nets T·v ≠ 0 iff char ∤ T — with T = ±2
everywhere here, a CHAR-2-ONLY death, and a SAME-SIGN one: no
opposite-sign mechanism is involved.  Boxed at (K2-RES-B)(ii).

**(PC-5) GS-40, T = 0, NOVEL by exhaustion** (53 states, 12 cells, 7
monomials).  The first non-invariant novel.  Every candidate {+1, −1}
split dies by I-linkage cascade: its zero-blocks must absorb the ±2/±4
dup cells, any block holding two same-fiber cells forces an element of
⟨I₁, I₃⟩ into H (K2-L11 — there is nothing else to force), and the
closure of ANY such element merges the candidate ±1 blocks with their
mates (the three I₁-pairs and three I₃-pairs of GS-40 chain every cell to
every other) into dead or unbalanced configurations.  Machine-certified
exhaustively; the display case: X = {b}, Y = {d} forces I₁ ∈ H via any
zero-block ⊇ {a, c}, and I₁ merges b~d — X and Y annihilate.

**(PC-6) The annex same-sign pairs, T = −2^k** [AS-03 (−1,−1), AS-08
(−2,−2)].  Beyond K2-L9's single-book reach (S2).  The 2^k ANCHOR: the
full merge is one class of net −2^k, and the A = {0} characteristic is
odd (K2-L8), so the netted sum −2^k·v never vanishes — survival
unconditional AT the anchor state; the partial states lose SD-L7 (the
(−1,−1) split can meet the order-2 torsion assignment w = −v, realizable
at odd char) — CONDITIONAL, survival-graded (fence F-2).

**(PC-7) AS-10, T = 3** [annex NOVEL].  No state nets to 0/±1/±2^k;
the full merge dies exactly in characteristic 3.  The single annex
residual beyond K2-L9-RES — same genre: char | multiplicity.

**(PC-8) The (e)/K2-CAND placement pair** [single-book, NOT a table shape;
from K2-L10 it is budget-legal].  Vector: one class, net −2 — the
single-book limit of PC-4.  It can NEVER witness (SURV-K2) (no SD-L7
state), at any characteristic; at odd char it survives netting (−2v ≠ 0),
at char 2 it is identically dead — and a dead group cannot BLOCK
(SURV-K2) either, since netting is per-group and the box is existential.
So (e) threatens only rows where it is the ENTIRE β_min population, and
its β_min occurrence at all still awaits the line-quanta exclusion
(K2-L10's honest cap).  Boxed at (K2-RES-B)(ii) with its kin.

## S5. K2-THM-B: corner coverage, and the residual boxed

**Definition (corner coverage; (R4⁺)).**  For a live g₀ = 2 row with β_min
system Σ, and a corner c ∈ {I₁, ¬I₁} × {I₃, ¬I₃}: Σ COVERS c iff it
contains an S; or a P1 and c ⊆ ¬I₁; or a P2 and c ⊆ ¬I₃; or a T1 and
c ⊆ I₁ ∨ I₃; or a glued group whose shape is corner-safe at c (S3).
(R4⁺) := Σ covers all four corners.  (R4⁺) ⊋ (R4): {S} and {P1, T1} cover
all four, and NEW minimal covers exist — {P1, X} for X ∈ {GS-04, GS-06,
GS-15, GS-24} (I₁-safe), {P2, Y} for Y ∈ {GS-05, GS-08, GS-16, GS-25}
(I₃-safe), and {P1, P2, Z} for Z any c11-cover (T1, GS-38, GS-41, or any
safe-corner shape containing c11).

**THEOREM K2-THM-B.**  On TRACK ∧ (H1)–(H3) ∧ g₀ = 2 ∧ c_{m−1} ≥ k, with
A = {0,1} ((R2)), the β_min population junk-free/Y-free ((R0)), every
β_min group either single-book of normalized form in {S, P1, P2, T1} or
glued with shape in the enumerated table ((R1⁺/R3⁺) — the B4 scope), and
(R4⁺): some β_min group carries an SD-L7-protected residue-class vector
UNDER EVERY residue assignment — (SURV-K2) holds on the row.

*Proof.*  A residue assignment φ fixes the truth values of I₁ and I₃ —
tower-level data, common to every group of the row (K2-THM-A's coherence
point) — hence a corner c(φ).  Take a group G covering c(φ).  If G is
single-book, THM-A's form evaluations protect it on exactly its covering
corners (S always; P1 ⟺ ¬I₁; P2 ⟺ ¬I₃; T1 ⟺ I₁ ∨ I₃ — the S6 display of
SURVK2_PROOF).  If G is glued: by K2-L12, φ's coincidence pattern on G is
a closed state whose H cannot contain a digit identity φ falsifies, so the
state is consistent with c(φ); corner-safety is the machine-verified
universal that EVERY such state (torsion states included, conservatively)
is SD-L7-protected.  Either way G's class vector at φ is protected.  ∎
(Attempt grade; consumes K2-L4/L7/THM-A†, K2-L11/L12, SD-L6/SD-L7†, and
the machine corner column.)

**Instance leg (the 29 glued rows; machine census).**  20/29 rows contain
an S — (R4⁺) holds, K2-THM-B closes them CLASS-WIDE.  The remaining 9
([GS-02, GS-08] ×8 on SDG2BF/SDG2BZ; [GS-08, GS-16] ×1 on SDW4G2F) cover
exactly {c01, c11} — the I₃ corners — via their I₃-safe anchors; their
¬I₃ corners are uncovered, so K2-THM-B is silent there.  In vivo all nine
are protected (GLUE-DEGEN: every realized glued group sits at the
full-merge state, where the |T| = 1 anchors net ±1).  No realized row is
residual-unprotected.

    ┌──────────────────────────────────────────────────────────────────────┐
    │ (K2-RES-B)  [OPEN — (SURV-K2) at g₀ = 2 beyond K2-THM-B].            │
    │ (i) CORNER-UNCOVERED systems ((R4⁺) fails).  Exact candidates: the   │
    │   realized nine [GS-02/GS-08-type] systems on their ¬I₃ corners —    │
    │   there the anchors have unprotected states and the glue pairs die   │
    │   under their glue coincidences; whether the netted bottom still     │
    │   survives is then ADDITIVE arithmetic ((−1,+1,−1)-type sums),       │
    │   outside the multiplicative calculus.  Inherits the (R4) failures   │
    │   (all-bare-P1 systems) from THM-A.  Instance record: zero realized  │
    │   failures (all nine at the full-merge state, protected).            │
    │ (ii) K2-MULT-RES — the SAME-SIGN MULTIPLICITY genre, now unified:    │
    │   every remaining single-group threat at g₀ = 2 is "char divides a   │
    │   forced net count", never a sign cancellation: the NOVEL shapes     │
    │   (seven even-dup |T| = 2 mains: char-2 events at the merge states;  │
    │   GS-40: T = 0 with every ±1 split I-cascaded dead; AS-10: char 3),  │
    │   the (e)/K2-CAND single-book pair (net −2; char 2; β_min occurrence │
    │   still awaiting the line-quanta exclusion, K2-L10), and K2-L9-RES   │
    │   (A = {0} promotion multiplicity mod odd char).  The involution     │
    │   program is COMPLETE at g₀ = 2: no opposite-sign failure mode       │
    │   remains anywhere; what is left is multiplicity arithmetic.         │
    │ (iii) BEYOND-B4 glued strata (≥ 2 dumping interior corrections,      │
    │   W ≥ 3 windows) — not enumerated; and box (b) multi-window          │
    │   out-of-list single-book forms — open, but by K2-L11 their kill     │
    │   identities still lie in ⟨I₁, I₃⟩ (the displayed one is I₁ + I₃):   │
    │   the identity ALGEBRA is closed even where the shape list is not.   │
    │ (iv) g₀ ≥ 3 — machine-dark; the two K2-CAND inventory failures       │
    │   (the −2 window; order-multiplicity) stand as sealed predictions    │
    │   for the next instrument.                                           │
    └──────────────────────────────────────────────────────────────────────┘

## S6. Machine brackets and the GLUE-DEGEN finding

Leg 1a (`k2close_enum.py --xcheck`, committed 8579216): GLUE-COVER 38/38,
GLUE-EDGE, GLUE-SGN, GLUE-NU, GLUE-CENSUS (29/38/14+14+1), GLUE-DUP (the
single dup fiber → GS-16), CTRL-MUT 190/190 — 0 violations.  Leg 1b
(`k2close_verdicts.py`, committed 816c75e): GUARD (table re-derived
in-process, 43/10, realized matches {GS-02:28, GS-08:9, GS-16:1}),
ID-CLOSURE (every visited state, all shapes, all corner runs — 0
violations), SYS-CENSUS/SYS-SHAPE (29 rows, every glued group matched),
GLUE-DEGEN, PIN (md5) — laws GREEN, 0 violations.  Sealed predictions:
PV-1 (no PROTECTED shape), PV-2 (the invariant core of NOVEL; extras
GS-40), PV-3 (GS-02/08/16 grades and corners), PV-5 (ID-CLOSURE) — GREEN;
**PV-4 — MISS, the unit's substantive finding**: the realized glue
assignments are not generic but fully DEGENERATE — all 38 realized glued
groups carry a SINGLE in-vivo residue class (the full-merge state; the
roster's digit residues all coincide), so the 28 GS-02 groups net 0 and
are DEAD in vivo (their rows carried by co-located S groups), and the ten
|T| = 1 groups net exactly ±1.  Consequences: (a) the instance roster
probes ONLY the full-merge corner of the state space and is NO evidence
about generic glue ratios; (b) K2-PROT's "all 29 glued rows protected"
was carried by S partners and full-merge anchors, never by a generic
(+1, −1) glue pair; (c) a residue-generic tower read (nontrivial digit
residues) is the natural falsifier target for the next instrument.

## S7. Claims, consequence, fences

| # | claim | grade |
|---|---|---|
| K2-L11 | ID-CLOSURE: glued digit identities ⊆ ⟨I₁, I₃⟩; glue ratios free | PROVED (attempt; machine shadow 0 viol) |
| K2-L12 | patterns = closed partitions; BFS exhaustive; torsion flagged | PROVED (attempt; abstract model, F-3) |
| column | 35+9 CONDITIONAL / 8+1 NOVEL / 0 PROTECTED; corner safety | MACHINE (laws green, committed) |
| K2-THM-B | (SURV-K2) on (R0) ∧ (R1⁺/R3⁺) ∧ (R2) ∧ (R4⁺) | PROVED (attempt) |
| instance | 20/29 realized glued rows class-closed; 9 I₃-covered, in-vivo protected | MACHINE |
| K2-L9/L10 | A = {0} single-book safety; exact budget reach | cited (LEGS23, attempt) |
| (K2-RES-B) | corner-uncovered systems; K2-MULT-RES; beyond-B4; g₀ ≥ 3 | OPEN (boxed) |

**Consequence (the LAW-DCX class after this unit).**  At g₀ = 2 the
(SURV-K2) input to the LAW-DCX chain (K2-COR: protected ⟹ netted bottom
survives ⟹ (SURV-DISJ) bottom leg ⟹ under (RW₀), Q¹ ≠ 0) now holds on
(K2-REG) ∧ (R4) [THM-A] **∪ corner-covered systems** [THM-B] — the pinned
class widens from "regular" to "corner-covered", absorbing 20 of the 29
glued rows class-wide and giving the other nine their exact open corner
(¬I₃).  What g₀ ≥ 3 still needs: the budget-5 window inventory, rulings
on the two K2-CAND combinatorial failures, and an instrument past the
engine cost wall — none of it touched here.

**Fences.**  (F-1) K2-THM-B does NOT cover the nine ¬I₃-open realized
rows class-wide; their in-vivo protection is instance fact (GLUE-DEGEN),
not theorem.  (F-2) PROT2K (annex 2^k anchor) certifies netted SURVIVAL,
not an SD-L7 protected vector; (SURV-K2)-literal consumers must not cite
it — the consequence chain (SD-L6 survival) may.  (F-3) the state space
is the ABSTRACT residue model; engine faithfulness rests on K2-L4 +
GLUE-EDGE, cellwise-verified on realized groups only.  (F-4) B4 scope
throughout the glued analysis (one dumping interior correction); the
beyond-B4 strata are boxed, not covered.  (F-5) everything at ATTEMPT
grade under the W-arc cap; no hostile pass has run on this note.
