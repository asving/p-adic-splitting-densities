I’ll audit this as a falsifier, treating prose claims as untrusted until the Lean definitions and all constructor/call paths support them. I’ll first map every key-installation/lift predicate and transition constructor, then trace `hmonic` and compare the exact residual exponent with the claimed marched normalization.

---

The local command runner is failing before execution because its sandbox helper (`bwrap`) is absent, so I’m switching to the available workspace-reading interfaces. This is an environment failure, not evidence for or against the note.

---

Even before the unavailable source sweep, the note contains a self-falsifying seam: its stated KP-STEP class is `(K0)+(K1)+(K2)`, while the corpus face compiles only `(K0)` and expressly leaves the implication to `(K1)+(K2)` open. I’m now separating that logical defect from narrower coverage and monicity evidence issues.

---

# Hostile pass 1 verdict: **CRITICAL**

The note does not establish its headline theorem. Two internal contradictions are already decisive, independently of any undiscovered firing site.

## CRITICAL 1 — “marched data” is confused with the actual `(SL-dev)` condition

The note defines KP-STEP’s class as requiring all three of `(K0)+(K1)+(K2)`, and defines `(SL-dev)` as the graded identity

> `in(D_κ·Φ_i^{eκ}) = U₀·ψ_κ·ϕ_{γ̂}·ȳ^κ`.

But the corpus predicate only carries window, weight, and residual-coordinate fields. Passing from those fields to the displayed graded identity is a semantic model theorem, not definitional reduction. The note itself admits this:

> “the graded (SL-dev) READING — OPEN”

and says this open seam is precisely what yields `(K1)+(K2)`.

Therefore these claims are false as stated:

> “order-≥ 2 instances inhabit KP-STEP's class — PROVED”

> “the wiring therefore fires only marched-data lifts”

> “every lift … lies in the … class the verified KP-STEP quantifies over”

What is proved is only: D8-shaped data are carried, and `(K0)` follows. Calling those fields “stage-law data intended to realize marched `(SL-dev)`” would be accurate; calling full class membership proved is not.

## CRITICAL 2 — recenterings are included in the theorem but omitted from clauses 1–2

The theorem quantifies over:

> “every fired lift”

including case (b), recenterings. Yet the `(SL-top)` and marched-data argument is explicitly restricted to:

> “cases (a)/(c, non-recentering)”

The recentering proof establishes only that `Φ − lift` remains monic of degree `m`. Nothing quoted establishes that it is an `IsReadLift` at `e = g = 1`, or that its sole lower digit `−lift` satisfies the required weight and twist-residual fields.

Thus the universal “shape + marched data + K0” conjunction is unproved for one of its three named carriers. A compiled `*_recentering_K0` lemma cannot repair the missing `(SL-top)/(SL-dev)` obligations.

## GAP 1 — firing-site completeness is asserted by prose, not proved

> “‘The engine fires a lift’ means … one of the three carriers”

This defines away the coverage obligation. The listed Lean corollaries show facts about selected predicates; they do not prove that every engine operation installing or consuming a new key factors through one of them.

There is also a counting inconsistency:

> “the carriers as the five site corollaries”

whereas the charge and surrounding claim speak of six firing sites. The S5 table mixes generic lemmas and firing-site corollaries without identifying which exact six constitute the supposedly exhaustive list.

A genuine coverage proof needs either:

- a closed `FiredLift` datatype used by every engine path, followed by exhaustive elimination; or
- a call/constructor audit covering every `Stage` creation, key replacement, classifier lift, recentering, `LandingKey`, and final-read path.

No seventh path is demonstrated here, but completeness is not established.

## CRITICAL 3 — the base-face paragraph supplies another scope contradiction

The note calls `liftKey` and `liftKeyExt`:

> “the classifier's own lift functions”

but proves only:

> “Shape + monicity only: their digit residual laws … outside this unit's claim.”

It later says that at order ≤ 1:

> “the same statement reproduces the proved KEY-g lift class.”

That “same statement” includes marched `(SL-dev)` data. The paragraph expressly declines to establish those data for the two base lift functions. Either exclude these functions from LIFT-CONF’s scope or supply the missing residual/data theorem.

## GAP 2 — monicity is plausible but the claimed supply audit is incomplete

`Stage.hmonic : Φ.Monic` genuinely gives parent monicity whenever a site receives a lawful `Stage`. A newly constructed `Stage` must also fill that field, so the type enforces monicity at forge boundaries.

However, the note gives only one example:

> “e.g. … `HK47.transHyp_gate.hmonic`”

That does not audit every forge constructor or prove that every firing path obtains its parent through a `Stage`. Consequently the monicity thread is sound conditional on the asserted carrier enumeration, but the enumeration gap prevents the universal conclusion.

## GAP 3 — OL-1(b) is locally honest, but the summary table is not

The detailed OL-1(b) paragraph correctly marks both seams:

- graded reading/class membership is required;
- `TR-3-ORD-X` remains open and is not consumed.

That part is honestly conditional.

But S0 simultaneously labels order-≥2 class membership “PROVED.” Since `(K1)+(K2)` remain open, KP-STEP cannot yet be applied to the fired lift. The top-level status must be downgraded to conditional. Likewise OL-1(a) for the abstract blueprint engine still rides the explicitly open S-1 identification, even if its corpus analogue is proved.

## Fresh sweep

Two additional wording defects should be repaired:

- “the `(K0) class the verified KP-STEP quantifies over” is inaccurate: KP-STEP quantifies over the `(K0)+(K1)+(K2)` class.
- A clean compilation of several implication lemmas proves those implications, not the closed-world premise that all engine firings satisfy their antecedents.

I could not complete an independent repository-wide `rg`/Lean sweep because the workspace command runner fails before execution due to a missing `bwrap` sandbox helper. That limitation does not affect the **CRITICAL** verdict: the note’s own statements already expose the unresolved semantic seam and the missing recentering leg.

Minimum repair: rename the proved result to a D8-data/`K0` conformance theorem, remove KP-STEP class-membership language, make recenterings a separate conditional case, and add an exhaustive engine-site theorem before restoring “every lift.”