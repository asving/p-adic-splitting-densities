# H1_BM2 — B-M2: engine-lift D8-data/(K0)-conformance (Theorem LIFT-CONF) + the OL-1 family clause assembly

**Unit:** HDISCHARGE_H1 §2.2 (s7), wave-3 unit B-M2 (`lean/blueprints/HDISCHARGE_H1.md`
REV 3). **Re-headline (REVISION 2; SYNCED at REVISION 11 — the pass-9
CRITICAL-3 fold):** what this unit PROVES is a
**D8-data/(K0)-CONFORMANCE theorem** — status per claim class per the §S0
table: actual-pair (K0) PROVED on **BOTH** classes ([NR] compiled directly;
[RC] via the compiled transport `k0_actual_of_recentering`,
`H1/RecParamE.lean` [REV 10], AND independently via the compiled WIRING
derivation `recParam_eg_of_recentering` [REV 12 — REC-WIRE-G
DISCHARGED]); the [NR]/[RC] distinction is RETAINED for
the claims that still differ per class — on [RC] the `IsReadLift`/
(SL-top)/(SL-dev) legs ride REC-SL (§S0 table) —
NOT KP-STEP class membership. KP-STEP's class is (K0)+(K1)+(K2); B-M2 supplies (K0)
compiled and the stage-law DATA definitionally on [NR], while (K1)+(K2) for the fired
lift ride the OPEN graded (SL-dev) reading (**GRADED-READ**, §S4), so KP-STEP's
application to the fired lifts is CONDITIONAL, not established. **Current opens
(REV 13 — the canonical list is §S4's "CONSOLIDATED CURRENT-OPENS LIST",
matching §S0; cited here, not restated):** GRADED-READ; REC-SL proper
(`IsReadLift`/(SL-top)/(SL-dev), KP-STEP route); SITE-EXH; and the
`readsOf_landing_K0` recentering-branch witness debt — after REC-WIRE-G + the
e-half transition-hypothesis removal DISCHARGED at REV 12. **Sources of
truth:** the KP-STEP leaf `H1_BM1_2026-08-06.md` REVISION 3 —
**2-clean bar MET** (pass 3 Codex CLEAN + pass 4 Fable CLEAN, model-diverse;
`BM1_pass3/4_*.md`) — §S1.3 (the class), §S3.2 (Lemma PAR-MON + (SL-dev)+(SL-top) ⟹
(K0)+(K1)+(K2)), §S7 (B-M2's statement and obligations), §S8 (the consumer-alignment
display + the corpus pins, pass-4 SOURCE-VERIFIED: `HC2/Defs.lean:181–191`
`IsReadLift`; `Moves/Defs.lean:124` `Stage.hmonic`; `HC1/T1_baseStage.lean:1547` base
`monic_X`; `MovesD/R7_ramifiedForge.lean:118` per-transition re-supply;
`OM/Classifier.lean:315` `liftKey_monic`; `OM/LiftKeyExt.lean:95` `liftKeyExt_monic`);
blueprint §2.1/§2.2(s7) (OL-1 verbatim, the REV-2 finding-15 form). **Corpus face
landed with this note:** `lean/LeanUrat/Scaffold/HDischarge/H1/Conformance.lean`
(sorry-free; per-file `lake env lean` clean, zero warnings; the compiled
declarations STAND untouched through this revision — comment/docstring edits
only at REV 2, REV 4, and REV 6). **REVISION 2 (pass-1 fold, 2026-08-02)** — hostile
pass 1 (Codex, `BM2_pass1_2026-08-06.md`) returned CRITICAL against the NOTE's
claim structure (3 critical, 3 gaps, 2 wording defects; the compiled artifacts
were not challenged); folded as the honest re-pricing. **REVISION 3 (pass-2
fold, 2026-08-02)** — hostile pass 2 (Codex, `BM2_pass2_2026-08-06.md`)
confirmed 6/8 pass-1 findings cured and the 9-declaration authority list
exact, but returned CRITICAL on recentering scope leaking back into universal
claims; folded as [REV 3] tags + the new sub-obligation **REC-PARAM**.
**REVISION 4 (pass-3 fold, 2026-08-02) — STRUCTURAL.** Hostile pass 3 (Codex,
`BM2_pass3_2026-08-06.md`) found FIVE more instances of the SAME genre all
three passes hit: sentences whose grammatical scope includes the recentering
carriers in claims the compiled Lean gives only for non-recentering carriers.
Two piecemeal folds failed to kill the genre, so per the bounded-repair rule
this revision rewrites the claim-scope STRUCTURE instead of patching sites:
two named claim classes **[NR]**/**[RC]** defined ONCE (§SC), a single
status-by-claim-class table (§S0) as the SOLE source of status truth, and a
whole-note sweep — every status sentence about lifts/carriers/firing either
carries an explicit [NR]/[RC] tag or defers to the table; no untagged
universal survives. Lean-face edits at REV 4: COMMENTS/DOCSTRINGS ONLY (the
retired RG-2 module-comment line; the `K0Conformant` docstring's OL-1(a)
scope); zero statement changes; gate re-run green.
**REVISION 6 (pass-4 fold, 2026-08-02)** — hostile pass 4 (Codex,
`BM2_pass4_2026-08-06.md`) returned CRITICAL (2 critical + 2 gaps; the
compiled statements re-confirmed unchallenged, evidence/status separation
CLEAN): BOTH criticals were surviving untagged scope sentences in the LEAN
MODULE HEADER; folded comment-only (header split to [NR]-actual/
[RC]-synthetic; the "FIRED lifts" universal retired in both files) + the
two gap display repairs (OL-1(a)-on-[RC] = REC-PARAM only, everywhere; the
abstract-engine universal added to SITE-EXH's consumers). Full record at
the tail. **REVISION 7 (pass-5 fold, 2026-08-02) — THE CENSUS.** Hostile
pass 5 (Codex, `BM2_pass5_2026-08-06.md`) returned CRITICAL (3 criticals +
3 gaps, ALL the same untagged-universal genre passes 1–4 hit); five folds
of quoted-site fixes + judgment sweeps failed to kill the genre, so this
revision replaces judgment with ENUMERATION: a mechanical sentence CENSUS
(grep-defined keywords; method + the full hit table in the REVISION 7
record at the tail) of every status sentence in §SC–§S6 and the Lean
module doc block — every hit classified [TAGGED]/[STATUS-FREE]/
[UNTAGGED-STATUS], every [UNTAGGED-STATUS] hit fixed, and §S6 attack
item 7 makes the census mechanically re-runnable by pass 6. Lean edits:
COMMENT-ONLY (the carrier-bullet read-off split [NR]-actual/
[RC]-synthetic), gate re-run green. **REVISION 8 (pass-6 fold, 2026-08-02)**
— hostile pass 6 (Codex, `BM2_pass6_2026-08-06.md`) checked THE CENSUS and
returned CRITICAL (2 census misclassifications incl. one recurring-genre
Lean-header leak at C112, 1 bookkeeping reclass at C057; the seven
REVISION-7 repairs verified SOUND, the REC-PARAM-only adjudication
independently re-derived, the three consumer lists confirmed aligned): all
folded — C112 per-class ([NR] GRADED-READ / [RC] + REC-SL, Lean
COMMENT-ONLY, gate green); the C060/C061 non-vacuity paragraph re-scoped to
the [NR]-witnessed carriers with the [RC] inhabitation displayed
UNWITNESSED-ON-FILE (substantively grep-checked; BM2_TRACE = evidence only);
C057 reclassified [STATUS-FREE]. Full record at the tail. **REVISION 9
(pass-7 + witness-unit fold, 2026-08-02)** — pass 7 (Codex,
`BM2_pass7_2026-08-06.md`) returned GAPS-ONLY (no critical defect: the
[RC]-debt display should enumerate the affected decls; the census stale at
123 vs 127 post-REV-8); folded together with the [RC]-WITNESS UNIT (commit
3fd9caf): the REV-8 UNWITNESSED-ON-FILE debt is DISCHARGED — both
recentering corollaries fire on the compiled interior-recentering history
H₃rc (`H1/RCWitness.lean`, Lean-core) — the REC-PARAM e-half is compiled at
its displayed came-from-a-non-recentering-transition scope
(`H1/RecParamE.lean`), REC-PARAM is stratified by the `Node.hspecRec`
node-level finding (the trace's "g tied NOWHERE" bracket-corrected to
wiring-level-only), and the census is refreshed over the current text. No
Lean edit at this revision (the witness unit's files landed at 3fd9caf;
`Conformance.lean` untouched). Full record at the tail. **REVISION 10
(pass-8 fold, 2026-08-02)** — hostile pass 8 (Codex,
`BM2_pass8_2026-08-06.md`) returned CRITICAL, for the first time in the
arc in BOTH directions: the note TOO CONSERVATIVE at one row (finding 1:
`Node.hspecRec` supplies the ORIGINAL node-level REC-PARAM proposition,
so the compiled synthetic conclusion transports to the actual pair —
the stronger wiring obligations were being smuggled back into the weaker
node-level status row) and too broad at two sentences (finding 2: the
[RC] non-vacuity umbrellas outran the still-unwitnessed
`readsOf_landing_K0` recentering branch), plus the census arithmetic
(finding 3). Folded three ways: (1) THE TRANSPORT LEMMA compiled —
`k0_actual_of_recentering` (`H1/RecParamE.lean`) + the actual-pair gates
`rc_landingKey_gate_actual`/`rc_historyCoherent_gate_actual`
(`H1/RCWitness.lean`), upgrading the §S0 actual-pair [RC] row and OL-1(a)
on [RC] to PROVED-VIA-TRANSPORT; (2) REC-PARAM RESTRUCTURED — the original
node-level proposition SUPPLIED, the remaining STRONGER obligations
renamed **REC-WIRE-G** (wiring g-half) / the e-half transition-hypothesis
removal / REC-SL proper; (3) the umbrella sentences scoped to the two
dedicated recentering corollaries, and the census recounted with the
pass-8 arithmetic reconciled. Full record at the tail. **REVISION 11
(pass-9 fold, 2026-08-02)** — hostile pass 9 (Codex,
`BM2_pass9_2026-08-06.md`) returned CRITICAL with an INVERTED profile:
its findings 1–2 VALIDATE the REVISION-10 mathematics in full (the
transport's argument order and every [RC] upgrade derivation re-derived
and confirmed exact), and all five defects are STALE TEXT UNDER-CLAIMING
relative to the compiled transport **[REV 12 bracket-correction — the
pass-10 GAP-2 fold: that profile summary OVERSTATED; it fits CRITICAL 3/
CRITICAL 4/GAP 5, but pass-9 GAP 6 was UNDER-SPECIFICATION (REC-WIRE-G
not theorem-precise) and CRITICAL 7 was a MISSING AUDIT TRAIL (the census
rows unpublished) — the individual dispositions in the REVISION 11 record
were and remain faithful]**. Folded: the opening headline SYNCED
(actual-pair (K0) PROVED on BOTH classes; the class split retained for
the claims that still differ); the two stale RG-2 e ≥ 2-recentering
sentences replaced by the sharpened VACUITY fact — `Node.hspecRec` makes
an e ≥ 2 species-recentering node UNCONSTRUCTIBLE — in the note AND the
Lean header (COMMENT-ONLY, gate green); the live [REV 5] evidence
sentence fenced (its obligation = the retired node-level REC-PARAM, now
supplied; evidence re-attached to the renamed residuals); REC-WIRE-G
STATED THEOREM-PRECISELY (corpus determination: no Stage/TransitionData
g-carrier, but the coherence WIDTH LAW + `Node.hDwidth` carry g at the
wiring layer — falsifiable proposition + candidate route displayed, NOT
compiled, obligation stays open); and THE CENSUS ROW LEDGER PUBLISHED —
rows C135–C184 enumerated over the current text under a validated
implementation, active rows 145, ZERO untagged. Full record at the tail.
**REVISION 12 (pass-10 fold, 2026-08-02) — REC-WIRE-G DISCHARGED.**
Hostile pass 10 (Codex, `BM2_pass10_2026-08-06.md`) returned CRITICAL
(2 criticals — both surviving stale synthetic-only/"tied NOWHERE"
sentences; 2 gaps — the REC-WIRE-G top-level proposition not
provenance-sensitive, and the REVISION-11 profile summary inexact; the
uninhabitability replacement and the census arithmetic CLEAN). Folded
FOUR ways: (1) THE WIRING PAIR COMPILED — the stage kernel
`recentering_natDegree_eq` (`IsRecentering σ σ' cc tt →
σ'.Φ.natDegree = σ.Φ.natDegree`; no `Node` in the type, so `hspecRec`
is unusable BY TYPE — pass 10's routing gap answered at the type level)
+ the wiring corollary `recParam_eg_of_recentering` (coherent history +
interior recentering read ⟹ νᵢ.e = 1 ∧ νᵢ.g = 1, from the coherence
width law + `hDwidth` + `Stage.hdeg` alone), `H1/RecParamE.lean`,
Lean-core, gate `rc_wireG_gate` fired at H₃rc's node 1 — **REC-WIRE-G
DISCHARGED, and the e-half transition-hypothesis removal SUBSUMED**
(νᵢ.e = 1 now hypothesis-free at interior recentering reads);
(2) CRITICAL 1's §S2 theorem-header synthetic-only sentence SYNCED;
(3) CRITICAL 2's "never TIES"/"tied NOWHERE" sentences (note + Lean
header) replaced by the accurate narrow form (no Stage FIELD records g;
the tie runs through the coherence width law, now compiled);
(4) GAP 2's REVISION-11 profile summary bracket-corrected (above and in
the record). Census upkeep over the changed rows in the REVISION 12
record. Full record at the tail.
Clean counter: 0/2; this unit's bar is the standard leaf bar.

## SC. The two claim classes — the ONLY scope vocabulary of this note (REV 4)

Every status claim in this note about lifts, carriers, or firing is a claim
about exactly one of two named classes (or is explicitly tagged with both).

**[NR] — the NON-RECENTERING recorded carriers.** The sites read off by the
compiled legs `landingKey_K0`, `historyCoherent_key_K0`, and
`readsOf_landing_K0` (non-recentering branch): interior non-recentering
transitions of a coherent recorded history, and non-recentering
`LandingKey`/`ReadsOf` landings (final read included). On [NR]:
**actual-pair (K0) is PROVED, compiled** — the conclusion is
`K0Conformant ν.σ ν.g ν.e Φ'`, i.e. (K0) at the node's REAL (e, g) — and the
D8 stride shape/(SL-top) + the stage-law (SL-dev) DATA
(window/weight/twist-residual fields) are DEFINITIONAL fields of the firing
predicate. Still open on [NR]: (K1)+(K2) (**GRADED-READ**), and every
universal "every fired lift" quantifier (**SITE-EXH**).

**[RC] — the RECENTERING carriers.** The sites read off by
`landingKey_recentering_K0` and `historyCoherent_recentering_K0` (and
`readsOf_landing_K0`'s recentering branch). On [RC] what is proved is
monicity + the SYNTHETIC conclusion `K0Conformant σ 1 1 Φ'` — AND [REV 10]
(K0) at the node's ACTUAL pair, BY TRANSPORT: `Node.hspecRec` (structure
field, `MovesC/Defs.lean`) forces (ν.e, ν.g) = (1, 1) at every
species-recentering node, and the compiled `k0_actual_of_recentering`
(`H1/RecParamE.lean`) carries the synthetic conclusion to
`K0Conformant ν.σ ν.g ν.e Φ'` (§S2 case (b)) — AND [REV 12] the pair's
WIRING provenance is COMPILED TOO: `recParam_eg_of_recentering` +
`recentering_natDegree_eq` (`H1/RecParamE.lean`) derive
(ν.e, ν.g) = (1, 1) at interior recentering reads from `HistoryCoherent`
alone, no `hspecRec` (REC-WIRE-G + the e-half hypothesis removal,
DISCHARGED — §S2 case (b)). What remains open on [RC]:
the `IsReadLift` form and the (SL-top)/(SL-dev) digit obligations await
**REC-SL** (§S4 (i-c)); no stage-law data is forced on the digit `−lift`.

**Standing rule (REV 4).** No untagged universal survives in this note: a
sentence asserting a status for "fired lifts" / "the recorded carriers"
without an [NR]/[RC] tag is a defect. The §S0 table is the single source of
status truth; prose defers to it.

## S0. Status by claim class — THE table (single source of status truth)

| claim | [NR] (non-recentering carriers) | [RC] (recentering carriers) |
|---|---|---|
| (K0) at the ACTUAL read pair (ν.e, ν.g) | **PROVED, compiled** (`landingKey_K0`, `historyCoherent_key_K0`, `readsOf_landing_K0`) | **PROVED VIA TRANSPORT [REV 10]** (`k0_actual_of_recentering`, `H1/RecParamE.lean`: the node-level pair IS (1, 1) by the `Node.hspecRec` structure field, so the compiled synthetic conclusion transports; witnessed at the actual pair by `rc_landingKey_gate_actual`/`rc_historyCoherent_gate_actual`, `H1/RCWitness.lean`) — **AND PROVED VIA WIRING [REV 12]** (`recParam_eg_of_recentering` + the stage kernel `recentering_natDegree_eq`, `H1/RecParamE.lean`: at interior recentering reads of a coherent history, (ν.e, ν.g) = (1, 1) from the coherence width law, no `hspecRec`; gate `rc_wireG_gate`, `H1/RCWitness.lean`). REC-WIRE-G + the e-half transition-hypothesis removal: DISCHARGED (§S2 case (b)); no wiring residue remains on this row |
| (K0) at the SYNTHETIC pair (1, 1) | (not the relevant form here) | **PROVED, compiled, NON-VACUOUS [REV 9]** (`*_recentering_K0`: monic, degree-preserving, `K0Conformant σ 1 1 Φ'`; the two dedicated corollaries fire — `rc_landingKey_gate` on ν₁rc/`landingKey_ν₁rc` directly, `rc_historyCoherent_gate` on the compiled interior-recentering history H₃rc [REV 10 precision] — `H1/RCWitness.lean`, commit 3fd9caf; `readsOf_landing_K0`'s recentering branch stays UNWITNESSED [REV 10 scope]) |
| D8 stride shape / (SL-top) | DEFINITIONAL (`IsReadLift`'s displayed formula; D_g = 1 hard-wired) | **NOT established** — the `IsReadLift`-at-(1,1) form awaits **REC-SL** † |
| stage-law (SL-dev) DATA (window/weight/twist-residual fields) | DEFINITIONAL fields of the firing predicate — a naked-data lift cannot fire at these sites | **NOT forced** — no weight/twist-residual data on the digit `−lift`; awaits **REC-SL** † |
| (K1)+(K2) — hence KP-STEP class membership & any KP-STEP application | OPEN — rides **GRADED-READ** † | OPEN — rides **GRADED-READ** + **REC-SL** (the `IsReadLift`/(SL-top)/(SL-dev) legs; the node-level parameter identification is SUPPLIED [REV 10] via `k0_actual_of_recentering`) † |
| OL-1(a) corpus face (monic, deg = e_i·f_i·m_i, integral) | **PROVED at these sites** (f_i = g via `Node.hψdeg`; integrality = the `ℤ_[p]` carrier) | **PROVED [REV 10]** — the compiled transport `k0_actual_of_recentering` gives (K0) at the actual pair, + `Node.hψdeg` (f_i = g): deg Φ' = m = e_i·f_i·m_i, exactly the pass-3/6-adjudicated arithmetic, now with its formerly-missing input compiled |
| OL-1(c) — V_{i+1} = e_i·f_i·(e_i·V_i + h_i) | CONDITIONAL on **GRADED-READ** (supplied on the class by B-M1 §S3.2, verified) | CONDITIONAL on **GRADED-READ** + **REC-SL** |
| OL-1(b) — Def-2.12 representative | CONDITIONAL on **GRADED-READ** (+ open **TR-3-ORD-X** for the GMN-verbatim face) | CONDITIONAL on **GRADED-READ** + **REC-SL** (+ **TR-3-ORD-X**) |
| DevBound degree identification (§S4 (vi)) | compiled at these sites (natDegree σ'.Φ = e·g·deg σ.Φ) | **SUPPLIED [REV 10]** — the ACTUAL-pair degree identification follows by the same transport (`k0_actual_of_recentering`; same arithmetic as the OL-1(a) adjudication, §S2); REC-SL's other legs matter for the row's graded route (§S4 (vi)), which stays OPEN on GRADED-READ |
| the UNIVERSAL "every fired lift" quantifier (either class) | rides **SITE-EXH** ‡ | rides **SITE-EXH** ‡ |
| abstract-engine OL-1(a) reading ("every Lift_i(c)" monic/degree/integral, blueprint) [REV 7 — row split] | rides **S-1** [T] + **SITE-EXH** | rides **S-1** [T] + **SITE-EXH** [REV 10: the former REC-PARAM dependency is SUPPLIED by the transport `k0_actual_of_recentering`; the §S2 adjudication caveat — S-1/SITE-EXH not replaced — stands] |
| abstract-engine OL-1(b)/(c) readings (blueprint) [REV 7 — row split] | ride **S-1** [T] + **SITE-EXH** + **GRADED-READ** (+ **TR-3-ORD-X** for (b)'s GMN-verbatim face) | ride **S-1** [T] + **SITE-EXH** + **GRADED-READ** + **REC-SL** (+ **TR-3-ORD-X** for (b)) |

**[REV 5] at-instance evidence marks.** † = verified TRUE at the worked
instance (BM2_TRACE_2026-08-02.md, commit de7f706; details at the claim's
statement site). ‡ = the trace's §C-e fence exhibit bears on this
obligation (§S2 SITE-EXH display). EVIDENCE ONLY: every status in this
table is UNCHANGED — at-instance verification never discharges a general
claim (the GRB pass-2 lesson).

**What this note does NOT claim:** KP-STEP class membership of any fired lift (the
(K1)+(K2) legs are open — GRADED-READ, and REC-SL on [RC]); that engine
recentering FIRINGS have the recorded species/coherence shape (the [REV 12]
wiring derivation `recParam_eg_of_recentering` covers RECORDED coherent
histories — S-1/SITE-EXH-side stays open, §S2 case (b)); that the three
recorded carriers exhaust the engine's firing sites (SITE-EXH); the graded
(K1)+(K2) identity for the wired data (the model seam); any repair or
reinterpretation of the VERIFIED KP-STEP text; the GMN-convention residual face;
(GR-B-KEY)/properness (untouched); retirement of any §3.1 row; any location or
existence claim for fired e ≥ 2 lifts (§S2 RG-2 display). Compilation of the
implication lemmas proves THOSE IMPLICATIONS, never the closed-world premise that
all engine firings satisfy their antecedents — that premise is SITE-EXH.

## S1. The conformant class, recalled (no re-derivation)

KP-STEP (B-M1 §S1.3, REV 2 — verified) quantifies over key lifts Φ_{i+1} ∈ K[x]
satisfying, at a read (e, h, ψ, g) with parent key Φ_i of degree m:

    (K0)  Φ_{i+1} MONIC with deg Φ_{i+1} = e·g·m,
    (K1)  w_{i+1}(Φ_{i+1}) = γ̂ = e·g·γ_{i+1},
    (K2)  R^gr_{γ̂}(in Φ_{i+1}) = U₀·ψ,  U₀ ∈ K_i^×,

and its companion (§S3.2, verified) proves that the CONSTRUCTION-facing form

    (SL-dev)  in(D_κ·Φ_i^{eκ}) = U₀·ψ_κ·ϕ_{γ̂}·ȳ^κ  (κ with ψ_κ ≠ 0)   +
    (SL-top)  D_g = 1

implies (K0)+(K1)+(K2) with U₀ = 1. The B-M1 gate's P4 showed the marched/naked
fork in (SL-dev) is LOAD-BEARING at E_i > 1 (120/151 naked failures, 50 with
reducible residual): "the engine's lifts are marched" is a real claim, not
bookkeeping. B-M2's §S7 obligations: (i) the engine emits (SL-dev)-conformant
data; (ii) (K0)-conformance of the pinned lift (= OL-1(a)); (iii) the cumulative
type thread; (iv) OL-1(b)'s GMN-verbatim face.

**What KP-STEP assumes vs what B-M2 supplies (REVISION 2 — aligned verbatim
with the VERIFIED B-M1 REVISION 3 §S1.3; scopes per the §S0 table).**
KP-STEP's hypothesis on the lift
is ALL THREE of (K0)+(K1)+(K2) — "the degree-conformant class"; its REV-2
class-scope display proves the minimality block FALSE on the bare (K1)+(K2)
class, so (K0) is load-bearing, and its §S3.2 companion proves
(SL-dev)+(SL-top) ⟹ (K0)+(K1)+(K2) with U₀ = 1. Against that statement,
B-M2 supplies — on **[NR]** — : **(K0) — PROVED, compiled at the actual
pair** (§S2 clause 3; §S5); **the stage-law (SL-dev) DATA — definitional**
(§S2 clause 2: window/weight/twist-residual FIELDS of the firing predicate);
**(K1)+(K2) — NOT supplied**:
passing from those fields to the graded identity in(D_κ·Φ_i^{eκ}) =
U₀·ψ_κ·ϕ_{γ̂}·ȳ^κ is a semantic model theorem, not a definitional reduction —
the open seam GRADED-READ (§S4 (i-b)). On **[RC]** B-M2 supplies: monicity
+ synthetic-(1, 1) (K0), compiled — and [REV 10] (K0) at the ACTUAL read
pair too, by the `k0_actual_of_recentering` transport (`Node.hspecRec`
makes the pair (1, 1)) and [REV 12] independently by the compiled WIRING
derivation `recParam_eg_of_recentering` (§S2 case (b) — REC-WIRE-G
discharged); the (SL-top)/(SL-dev)/`IsReadLift` legs ride
REC-SL (§S4 (i-c)). So of KP-STEP's
three hypotheses this unit discharges exactly one — (K0), now on BOTH
recorded classes ([RC] via the transport [REV 10]). No KP-STEP
application is established by this unit on EITHER recorded class — on [NR]
the (K1)+(K2) hypotheses ride GRADED-READ, on [RC] additionally REC-SL's
remaining legs — and beyond the recorded carriers this note asserts no
engine-wide firing status at all, positive or negative (SITE-EXH; §S0
table) [REV 7 — the pass-5 CRITICAL-1 fold: the former untagged "any
engine lift" universal is retired].

## S2. Theorem LIFT-CONF (D8-data/(K0)-conformance on [NR], corpus face) — PROVED as re-headlined; quantifier scope owned by SITE-EXH; [RC] the displayed case (b)

**Setting.** The three RECORDED carriers on which the run wiring ASSERTS a key
are:

* (a) an interior NON-RECENTERING transition i → i+1 of a coherent recorded
  history (`HistoryCoherent H`, 4th conjunct, non-recentering leg): the child
  frame key `σᵢ₊₁.Φ` is asserted `IsNodeLift νᵢ σᵢ₊₁.Φ`; — class **[NR]**
* (b) an interior RECENTERING transition: `IsRecenteringCore` asserts
  `σᵢ₊₁.Φ = σᵢ.Φ − liftᵢ` with the recorded lift in the coefficient window;
  — class **[RC]**
* (c) the DESIGNATED LANDING KEY of ANY read of a classifier run
  (`ReadsOf p F n f H`, per-read clause bundle `SideReads`, clause (iv)
  `LandingKey νᵢ Φnext`) — the FINAL read included, pinned to `σᵢ₊₁.Φ` at
  interior reads; **[NR]** at non-recentering species, **[RC]** at
  recentering species.

**Open obligation SITE-EXH (REVISION 2 — the pass-1 GAP-1 fold; owner: B-M2
continuation; consumer list at §S4 (i-d), EXTENDED at REV 4).**
Rev 0 DEFINED "the engine fires a lift" as the disjunction of (a)/(b)/(c) —
defining away the coverage obligation. That the three recorded carriers
EXHAUST the engine's key-firing sites is NOT proved. The two honest routes:
(1) a closed `FiredLift` datatype threaded through every engine path,
followed by exhaustive elimination; or (2) a full constructor/call audit
covering every `Stage` creation, key replacement, classifier lift,
recentering, `LandingKey`, and final-read path. Until one lands, every
"fired lift" below means "lift fired ON A RECORDED CARRIER". No fourth
carrier is currently known (the §S6(iii) attack stands un-cashed), but
absence of a counterexample is not the theorem.

**[REV 5] Data point — the C-e fence exhibit** (BM2_TRACE_2026-08-02.md
§C-e, commit de7f706): the traced REAL p = 3 quartic tower's interior
e = 2 step is UNRECORDABLE in any lawful full `ReadsOf` run (the RG-2
interior fence), so its genuine recentering can fire only in TRUNCATED
runs — the sharpest SITE-EXH/RG-2 data point on record. It strengthens
the case that SITE-EXH closure (either route above) must handle
truncated-run firing, not just full-run enumeration.

**Theorem LIFT-CONF (REVISION 2 form; scope: clauses 1–3 are **[NR]**
claims ONLY — case (b)/**[RC]** is the separate displayed case below;
its compiled conclusions hold at the synthetic pair (1, 1) AND at the
node's actual pair (νᵢ.e, νᵢ.g) — via the [REV 10] transport
`k0_actual_of_recentering` and, at interior recentering reads, the
[REV 12] wiring derivation `recParam_eg_of_recentering`, which proves
(νᵢ.e, νᵢ.g) = (1, 1) from the coherence wiring itself).** At every lift
fired on an [NR]
carrier, with (e, g, m) := (νᵢ.e, νᵢ.g, deg σᵢ.Φ):

1. **[(SL-top) + stride shape — DEFINITIONAL, [NR]]** In cases (a)/(c, non-recentering)
   the fired key has the D8 stride form
   `Φ' = σ.Φ^(e·g) + Σ_{k<g} t_k·σ.Φ^(e·k)` with top coefficient LITERALLY 1 —
   `IsReadLift`'s displayed formula (`HC2/Defs.lean:181–191`, pass-4
   verbatim-verified; `IsNodeLift ν Φ' ↔ IsReadLift ν.σ ν.ψ ν.g ν.e ν.h Φ'` is
   `Iff.rfl`). D_g = 1 is hard-wired: the sum ranges over k < g.
2. **[stage-law (SL-dev) DATA — DEFINITIONAL, [NR]; the graded READING stays open]**
   Each present digit t_k is constrained by FIELDS of the same predicate: the
   coefficient window `deg t_k < deg σ.Φ` (`inC`), the marched weight law
   `σ.w(t_k) = h·(g−k)`, and the twist-aware residual law
   `σ.R(t_k) = C(ψ_k)·T^(−σ.t·σ.wPrev(t_k))` — the stage-law data INTENDED TO
   REALIZE marched (SL-dev), with the twist bookkeeping (forced (S5) positions
   `−t·wPrev`) EXHIBITED in the predicate itself. At the DATA level the P4
   fork's naked side is excluded at every [NR] site. What this clause does
   NOT say (REVISION 2): that these fields yield the GRADED identity
   in(D_κ·Φ_i^(eκ)) = U₀·ψ_κ·ϕ_{γ̂}·ȳ^κ — that passage is a semantic model
   theorem, the open seam **GRADED-READ** (§S4 (i-b)); so "the wiring fires
   only (SL-dev)-conformant lifts" is NOT established, only "fires only
   lifts carrying the stage-law data" (and that only on [NR] — §S0 table).
3. **[(K0) — THEOREM, [NR]]** In cases (a)/(c, non-recentering) the fired key is
   monic of the conformant degree `deg Φ' = e·g·m`. Integrality is the carrier
   type (`Polynomial ℤ_[p]`).

**Case (b), recenterings — class [RC]: a separate, DISPLAYED conditional case
(REVISION 2; the pass-1 CRITICAL-2 fold).** PROVED (compiled): the recentered key
`Φ' = σ.Φ − lift` is monic of degree m = 1·1·m — (K0) at the SYNTHETIC pair
e = g = 1 (`sub_inC_K0`, consumed by `landingKey_recentering_K0` and
`historyCoherent_recentering_K0`; **NON-VACUOUS, witnessed [REV 9]**: the two
dedicated corollaries fire — `rc_landingKey_gate` on ν₁rc/`landingKey_ν₁rc`
directly, `rc_historyCoherent_gate` on the compiled interior-recentering
history H₃rc [REV 10 precision] — `H1/RCWitness.lean`, commit 3fd9caf).
PROVED (compiled) [REV 10]: (K0) at the node's ACTUAL pair, BY TRANSPORT —
`k0_actual_of_recentering` (`H1/RecParamE.lean`) rewrites the synthetic
conclusion along `Node.hspecRec`'s two equalities (ν.e = 1, ν.g = 1);
witnessed at the actual pair by `rc_landingKey_gate_actual` /
`rc_historyCoherent_gate_actual` (`H1/RCWitness.lean`).
OWED — **REC-SL** (open sub-member;
owner: B-M2 continuation; consumer list at §S4 (i-c)): nothing compiled or
displayed here
establishes that Φ' is an `IsReadLift` at e = g = 1, or that its sole lower
digit `−lift` satisfies the (SL-dev) weight and twist-residual obligations.
**[REV 5]** verified at the worked instance (BM2_TRACE_2026-08-02.md,
commit de7f706): the recentering digit `−lift` satisfies EVERY owed REC-SL
field — window, weight 3/2 = h, digit = ψ.coeff 0, twist position 0,
(SL-top) form, graded identity with U₀ = 1. At-instance verification is
EVIDENCE for the general claim; the obligation REMAINS OPEN (the GRB
pass-2 lesson: instance testing never discharges a general claim).
**[REV 3 — the pass-2 CRITICAL fold: the parameter identification is a
DISTINCT owed leg.]** The compiled conclusions at [RC] sites are
`K0Conformant σ 1 1 Φ'` — (K0) at the SYNTHETIC pair (1, 1) — and [REV 10
bracket-correction: the following clause, true at REV 3, is now FALSE —
`Node.hspecRec` + `k0_actual_of_recentering` are exactly such an
identification, compiled] NOTHING compiled or displayed identifies the
node's actual read parameters (νᵢ.e, νᵢ.g) with (1, 1). Displayed as a
sub-obligation inside REC-SL:

    REC-PARAM (sub-obligation of REC-SL; historical display, stratified
    at REV 9 and RESTRUCTURED at REV 10 — see below): at every recentering
    read, the parameters (e_i, g_i) under which KP-STEP/OL-1 price the
    fired key are IDENTIFIED with the synthetic pair (1, 1) of the
    compiled `*_recentering_K0` conclusions — i.e. the recentering read is
    a genuine (e, g) = (1, 1) read of the tower, not merely priced at
    (1, 1) by fiat.

(`Node.hψdeg` supplies f_i = g only; it supplies neither e_i = 1 nor g = 1.)

**[REV 10] REC-PARAM RESTRUCTURED — the pass-8 finding-1 fold (supersedes
the "open" label on the display above and the [REV 9] "remaining open
content" bullet below).** The display above conflates two propositions of
different strength; pass 8 adjudicated that the smuggling now ran in the
CONSERVATIVE direction (the stronger wiring requirement gating the weaker
node-level status row). They are now separated and named:

* **The ORIGINAL node-level REC-PARAM proposition — SUPPLIED, compiled.**
  "The identification of the node's actual (e_i, g_i) with (1, 1)" holds
  at every species-recentering node: `Node.hspecRec` (`MovesC/Defs.lean`,
  a STRUCTURE FIELD) forces species = recentering → e = 1 ∧ g = 1, and the
  transport lemma `k0_actual_of_recentering` (`H1/RecParamE.lean`,
  Lean-core, sorry-free) carries the compiled synthetic conclusion
  `K0Conformant σ 1 1 Φ'` to the actual pair `K0Conformant ν.σ ν.g ν.e Φ'`
  by rewriting along the field's two equalities. Instantiated at both
  witnesses: `rc_landingKey_gate_actual`/`rc_historyCoherent_gate_actual`
  (`H1/RCWitness.lean`).
* **The STRONGER, DIFFERENT obligations — [REV 12: the two WIRING
  residuals are now DISCHARGED, compiled; REC-SL proper stays open]:**
  * **REC-WIRE-G — DISCHARGED at REV 12 (the pass-10 GAP-1 route,
    compiled; `H1/RecParamE.lean`, Lean-core, sorry-free).** The REV-11
    corpus determination stands recorded (NO `Stage` field and NO
    `TransitionData` clause records a g — `Moves/Defs.lean:106–166`;
    `Moves/DefsT.lean:147–156` carries `child_e`/`child_h` only; the
    wiring carries g through the WIDTH LAW `Dwidthᵢ₊₁ = νᵢ.childWidth =
    νᵢ.e·νᵢ.g·νᵢ.Dwidth` + the frame link `Node.hDwidth`). The REV-11
    proposition is now the compiled pair:

        THE STAGE KERNEL — `recentering_natDegree_eq`:
        `IsRecentering σ σ' cc tt → σ'.Φ.natDegree = σ.Φ.natDegree`
        (no `Node` in the type, so `hspecRec` is unusable BY TYPE —
        exactly the REV-11 routing requirement; proof: `σ'.Φ = σ.Φ − tt`
        + the window `inC σ.Φ tt`, lower-degree subtraction).

        THE WIRING COROLLARY — `recParam_eg_of_recentering`: for every
        H with `HistoryCoherent H` and every i with
        i + 1 < H.nodes.length and `(H.nodes[i]).species =
        ReadSpecies.recentering` (an INTERIOR recentering read):
        `(H.nodes[i]).e = 1 ∧ (H.nodes[i]).g = 1` — proof: coherence's
        recentering leg fires `IsRecenteringCore`, whose `base` feeds
        the kernel (`deg σᵢ₊₁.Φ = deg σᵢ.Φ`); the width clause + both
        `hDwidth` frame links give `νᵢ.e·νᵢ.g·d = d` at
        `d = deg σᵢ.Φ ≥ 1` (`Stage.hdeg`), forcing `νᵢ.e·νᵢ.g = 1`.
        The index bound `hi1` and the recentering read `hrec :
        species = recentering` are explicit hypotheses; the substantive
        width/recentering laws are all obtained from `HistoryCoherent`,
        and `Node.hspecRec` (the species-pair fiat) is nowhere invoked.

    Non-vacuity gate: `rc_wireG_gate` (`H1/RCWitness.lean`) fires the
    corollary at H₃rc's interior recentering node 1. In-file
    `#print axioms` = Lean-core on all three declarations. Scope note:
    the derivation covers RECORDED coherent histories; whether engine
    recentering FIRINGS have that shape stays S-1/SITE-EXH-side.
  * **The e-half's transition-hypothesis removal — DISCHARGED at REV 12,
    by the same corollary** (as the REV-11 display predicted: the two
    wiring residuals shared the route): `recParam_eg_of_recentering`
    derives νᵢ.e = 1 at EVERY interior recentering read of a coherent
    history, with NO came-from-a-non-recentering-transition hypothesis —
    subsuming `recParam_e_of_transition`'s clause (c). That lemma's
    clause (b) stage-node tie (σᵢ₊₁.e = νᵢ₊₁.e via `child_e`) remains
    transition-case content as displayed; it gates nothing.
  * **REC-SL proper (open):** the `IsReadLift`-at-(1, 1) form and the
    (SL-top)/(SL-dev) digit obligations — these gate the KP-STEP route
    ONLY (§S4 (i-c)), never OL-1(a) or the DevBound degree identification.
* The "not merely priced at (1, 1) by fiat" clause of the display is
  now SATISFIED [REV 12]: the pair is compiled BOTH by fiat-layer
  transport (`k0_actual_of_recentering` riding `Node.hspecRec`) AND by
  the non-fiat wiring derivation (`recParam_eg_of_recentering`, no use
  of `Node.hspecRec` — the species-pair fiat; it does take `hrec :
  species = recentering` as an index hypothesis). Whether engine
  recentering firings HAVE the recorded
  shape stays S-1/SITE-EXH-side, unchanged.

**[REV 5]** verified at the worked instance (BM2_TRACE_2026-08-02.md,
commit de7f706): the traced recentering read's ACTUAL pair is (e, g) =
(1, 1), forced three independent ways (degree arithmetic; e = 1 by
window-lattice; g = 1 by residue-root), with a 387-event refutation hunt
finding zero counterexamples. At-instance verification is EVIDENCE for the
general claim; the obligation REMAINS OPEN (the GRB pass-2 lesson:
instance testing never discharges a general claim) **[REV 11 fence — the
pass-9 GAP-5 fold: "the obligation" of the preceding sentence is the
RETIRED node-level REC-PARAM, which is now SUPPLIED (`Node.hspecRec`
structure field + the compiled transport `k0_actual_of_recentering`
[REV 10]); [REV 12]: the residuals this paragraph's at-instance evidence
was re-attached to — REC-WIRE-G + the e-half transition-hypothesis
removal — are now themselves DISCHARGED by the compiled general
derivation (`recParam_eg_of_recentering`, RESTRUCTURE display above), so
the trace's forced (e, g) = (1, 1) is no longer evidence for an open
claim but an instance of a compiled theorem]**. Wiring note (same unit;
REWRITTEN at REV 12 — the pass-10 CRITICAL-2 fold; the trace's original
wording was too broad): the accurate narrow form of the trace's finding
is that NO `Stage` FIELD directly records a g and the e-half's `child_e`
route (`Moves/DefsT.lean:150`, via a non-recentering-transition
hypothesis) has no direct g-analogue — but the tie was never "nowhere at
the stage/wiring level": it runs through the coherence WIDTH LAW
(`Node.childWidth` + `HistoryCoherent`'s Dwidth clause + `Node.hDwidth`),
now compiled as `recentering_natDegree_eq` + `recParam_eg_of_recentering`
(`H1/RecParamE.lean`). BM2_TRACE_2026-08-02.md itself is NOT edited: it
is a dated record; its correction lives here and in the ledger. The
former closing sentence "REC-PARAM is wiring debt, not mathematical
risk" is RETIRED [REV 12]: the node-level identification is supplied
[REV 10] and the wiring debt is paid (REC-WIRE-G + the e-half removal,
DISCHARGED — RESTRUCTURE display above).

**[REV 9] REC-PARAM stratified — the compiled e-half + the node-level fiat
layer** (the [RC]-witness unit, commit 3fd9caf; ledger entry in
`BRIDGE_ADJUDICATIONS_2026-07-30.md`):

* **e-half — COMPILED at its derivable scope.** `recParam_e_of_transition`
  (`H1/RecParamE.lean`, Lean-core, sorry-free): in a coherent history, at an
  INTERIOR recentering read whose frame stage CAME FROM A NON-RECENTERING
  TRANSITION (both hypotheses DISPLAYED — do not read this as the
  unconditional identification), σᵢ₊₁.e = 1 (`IsRecentering`, via the
  recentering coherence leg) ∧ σᵢ₊₁.e = νᵢ₊₁.e (`TransitionData.child_e`,
  `Moves/DefsT.lean:150`, via the non-recentering leg's `TransitionCoreL`)
  ∧ νᵢ₊₁.e = 1 — the BM2_TRACE §3 (C-a) wiring derivation, compiled. The
  g-half is NOT claimed by that lemma.
* **Node-level stratification (the hspecRec finding).** `Node.hspecRec`
  (`MovesC/Defs.lean`) is a STRUCTURE FIELD forcing
  species = recentering → e = 1 ∧ g = 1: the node-level pair is (1, 1) BY
  STRUCTURE FIELD (the fiat layer — the predicate BUILDS the pair in). So
  BM2_TRACE's claim that a coherent history could record ν.g = 7 at a
  recentering node is FALSE at the node level; the "tied NOWHERE" verdict
  survives ONLY at the stage/wiring level for the g-half. Whether engine
  recentering firings HAVE the species shape stays S-1/SITE-EXH-side.
* **REC-PARAM's remaining open content is therefore PRECISELY:** the
  STAGE/WIRING-level tie for the g-half, plus removing the
  came-from-a-non-recentering-transition hypothesis on the e-half. Nothing
  else of REC-PARAM is open; nothing of REC-SL's other legs is touched.
  **[REV 10: these two residuals are now NAMED — REC-WIRE-G and the e-half
  transition-hypothesis removal (the RESTRUCTURE display above) — and the
  node-level identification itself is SUPPLIED by the compiled transport
  `k0_actual_of_recentering`, so no status row gates on "REC-PARAM"
  any more; rows cite the transport (supplied) or the named wiring
  residuals (open).]** **[REV 12: both named wiring residuals are now
  DISCHARGED — `recentering_natDegree_eq` + `recParam_eg_of_recentering`
  (the RESTRUCTURE display above); nothing of former REC-PARAM remains
  open at the recorded-history perimeter.]**

**REC-PARAM sufficiency — ADJUDICATED at pass 3 (finding 9, 2026-08-02;
recorded at the REVISION 4 fold).** Read concretely as the equalities
νᵢ.e = 1 AND νᵢ.g = 1, REC-PARAM together with the compiled
`*_recentering_K0` conclusions and `Node.hψdeg` (f_i = g) is **SUFFICIENT
for the corpus OL-1(a) clause at [RC] sites**: deg Φ' = 1·1·m = e_i·g_i·m
= e_i·f_i·m_i. NO `IsReadLift` theorem is additionally needed for that
clause. The `IsReadLift`-at-(1, 1) form remains SEPARATELY necessary for
the stage-law/KP-STEP route — it stays a distinct REC-SL leg. Caveat (same
adjudication): for the blueprint's ABSTRACT "every Lift_i(c)" clause,
REC-PARAM does NOT replace S-1's identification of the abstract lift and
degrees with the corpus objects, nor SITE-EXH's universal coverage —
REC-PARAM alone settles the corpus [RC] leg of OL-1(a), not the whole
abstract clause. **[REV 10 — the adjudication CASHED:** the equalities are
now compiled (`Node.hspecRec` via `k0_actual_of_recentering`), so the
corpus OL-1(a) clause at [RC] sites is PROVED outright — §S0 table, §S3
bullet (a); the caveat's S-1/SITE-EXH scope is unchanged.**]**

Clauses 1–2 above do NOT cover case (b); rev 0's "every fired lift"
conjunction was overdrawn exactly here.

Consequently: **every lift fired on an [NR] carrier at an
OM read order ≥ 2 site satisfies the (K0) CLAUSE of the class the verified
KP-STEP quantifies over** (on [RC] the compiled (K0) now holds at KP-STEP's
recorded read pair TOO [REV 10] — the synthetic conclusion transported via
`k0_actual_of_recentering`; the pair's wiring provenance compiled [REV 12] —
`recParam_eg_of_recentering`; §S0 table) — the class
itself is (K0)+(K1)+(K2), and CLASS
MEMBERSHIP of the fired lifts is NOT proved: (K1)+(K2) ride GRADED-READ, and
on [RC] the (SL-top)/(SL-dev) obligations
ride REC-SL [REV 10: the (1, 1) parameter identification no longer rides
there — supplied by the transport]. KP-STEP's application to any
RECORDED-CARRIER lift is therefore CONDITIONAL on those named seams per
class ([NR]: GRADED-READ; [RC]: GRADED-READ + REC-SL's remaining legs — §S0
table), and its application to "any fired lift" ENGINE-WIDE additionally
rides SITE-EXH (§S4 (i-d)) [REV 7 — the pass-5 CRITICAL-2 fold: SITE-EXH
added to the universal reading]. At order ≤ 1 the proved
**[NR]** (K0)+shape face is consistent with the proved KEY-g lift class
(E₀ = 1: marched = naked — B-M1 S0 finding (i)), the non-vacuity anchor
**[REV 4: this consistency sentence is an [NR] claim ONLY — at order ≤ 1
[RC] sites the compiled face is the synthetic (1, 1) leg + its [REV 10]
actual-pair transport, with the `IsReadLift` shape awaiting
REC-SL]**; rev 0's
stronger "reproduces the proved KEY-g lift class" is retired with the
re-headline.

**Proof.** (1), (2) are the definitions (no content to prove; the pass-4
verification already source-confirmed the quoted formulas). (3) is the displayed
two-input chain:

* *Parent monicity is a carried invariant* (Lemma PAR-MON's corpus carrier,
  B-M1 §S3.2 — verified): `Stage.hmonic : Φ.Monic` is a REQUIRED FIELD of
  every lawful stage (`Moves/Defs.lean:124`, with `hdeg : 1 ≤ Φ.natDegree`),
  so every `Stage` literal must discharge it at its forge boundary. **Supply
  census (REVISION 2 — the pass-1 GAP-2 fold; `rg "hmonic :=" lean/LeanUrat/`,
  2026-08-02).** Base/gate supplies via `monic_X`: `HC1/T1_baseStage.lean:1547`
  (THE base stage), `HC1/S1_transHypGate.lean:691`, `HC1/T2_baseGate.lean:91`
  (`by monicity!`), `HC2/U28_gateSep.lean:569`, `HC2/U30_gatePol.lean:464`,
  `HC2/U31_gateReadsOf.lean:633`, `MovesT/G1_toyGate.lean:169`/`:2573` (toy
  gates). Forge/transition re-supplies: `HC1/S9_builder.lean:587` (`hmonΦ`),
  `HC1/S10_recStage.lean:454` (`hmonic'`), `HC2/HK15_gateChildCarrier.lean:152`
  and `MovesD/R7_ramifiedForge.lean:118` (both via
  `HK47.transHyp_gate.hmonic`), `HC2/HK47.lean:156`. (`OM/Discriminant.lean:411`
  is a local `have`, not a constructor site.) This is the CURRENT CENSUS, not
  a completeness proof: positional/`where`-style constructions and future
  sites are invisible to this grep — closure is owned by **SITE-EXH**. The
  PAR-MON thread is therefore sound CONDITIONAL on SITE-EXH's enumeration:
  wherever a firing path obtains its parent through a lawful `Stage`, the
  invariant is enforced by the type.
* *The stride count* (PAR-MON's step): at a monic parent of degree m ≥ 1
  (`hdeg`), the top stride `σ.Φ^(eg)` is monic of degree e·g·m, and every lower
  stride obeys `deg(t_k·σ.Φ^(ek)) ≤ (m−1) + e·k·m < e·g·m` for k ≤ g−1 — the
  strict step is `m−1 < e·(g−k)·m`, true since `e·(g−k)·m ≥ e·m ≥ m` at
  `1 ≤ e` (supplied by `Node.he`), `1 ≤ g−k`, `1 ≤ m` (B-M1 §S3.2's count, run
  verbatim) — so the sum is monic of exact degree e·g·m. Recenterings ([RC];
  the synthetic leg only):
  `deg lift < deg Φ` (window) makes `Φ − lift` monic of degree m.

Both legs are COMPILED sorry-free in `H1/Conformance.lean` (§S5): the count as
`isReadLift_K0` — the read-pair transport of the on-file frame-pair lemma
`L3_liftMonic` (`Moves/L3_liftMonic.lean`; stride freed from `σ.e` to the read's
`e'`, `σ.he` replaced by the explicit `1 ≤ e'` binder) — and the carriers read
off by EXACTLY these declarations (REVISION 2, the pass-1 five-vs-six count
fixed by enumeration; this list is the authority): 1 def (`K0Conformant`), the
core theorem (`isReadLift_K0`), 2 bridge lemmas (`isNodeLift_K0`,
`sub_inC_K0`), and 5 carrier-site corollaries (`landingKey_K0`,
`landingKey_recentering_K0`, `historyCoherent_key_K0`,
`historyCoherent_recentering_K0`, `readsOf_landing_K0`) — 9 declarations in
all. What their compilation proves is those implications and per-carrier
read-offs; NOT the closed-world premise that every engine firing satisfies
their antecedents (SITE-EXH). ∎

**Engine's compiled base-face lifts (the §S8 bullet, cited not re-proved).** The
classifier's own lift functions are literally `X^d + (tail of degree < d)` with
monicity THEOREMS on file: `OM.Classifier.liftKey_monic` (`OM/Classifier.lean:315`)
and `OM.LiftKeyExt.liftKeyExt_monic` (`OM/LiftKeyExt.lean:95`) — the (SL-top)
shape at the base frame (Φ₀ = X, e′ = 1, digits constant). **Honest scope
(REVISION 2 — the pass-1 CRITICAL-3 fold): shape + monicity ONLY.** Their
marched (SL-dev) data obligations (digit weight/twist-residual laws, box-ring
face) are EXCLUDED from Theorem LIFT-CONF's claim: no clause of this note
asserts class membership, or even stage-law-data carriage, for `liftKey`/
`liftKeyExt`. Rev 0's suggestion that the order ≤ 1 statement "reproduces the
proved KEY-g lift class" through these functions is retired (§S2 conclusion);
if a consumer needs their (SL-dev) data, that is a NEW obligation, not covered
here.

**Non-vacuity of the carriers (displayed; scoped per class at REV 8;
[RC] debt DISCHARGED at REV 9 — the pass-7 fold via the [RC]-witness
unit).** On **[NR]** the quantified predicates are inhabited on file:
`ReadsOf`'s anchor is U31's gate (`gate_readsOf_inert2` — an inert,
non-recentering run); coherent 2-node histories: `HK23_twoNodeGatePos`
(root → increment, both non-recentering; HK-06 wave, Lean-core). So the [NR]
carrier corollaries are not statements over empty carriers. On **[RC]** the
REV-8 UNWITNESSED-ON-FILE debt is **DISCHARGED FOR THE TWO DEDICATED
RECENTERING COROLLARIES [REV 9; scope precised at REV 10 — the pass-8
finding-2 fold: the former umbrella wording spanned the still-unwitnessed
`readsOf_landing_K0` branch]**:
`H1/RCWitness.lean` (commit 3fd9caf; Lean-core, sorry-free, in-file
`#print axioms`) compiles `landingKey_ν₁rc : LandingKey ν₁rc σ₂rc.Φ` — the
FIRST recentering-species `LandingKey` witness — and `H₃rc` with
`coherent_H₃rc` — the FIRST coherent history with an INTERIOR recentering
transition (built through `S10_recStage` off HK25's gate child σ₁rec) —
and both dedicated recentering corollaries FIRE: `rc_landingKey_gate` on
ν₁rc/`landingKey_ν₁rc` DIRECTLY (ν₁rc is also H₃rc's node 1 — [REV 10
precision, pass-8 finding 4]) and `rc_historyCoherent_gate` on
H₃rc/`coherent_H₃rc`, each concluding `K0Conformant σ₁rec 1 1 σ₂rc.Φ`;
[REV 10] both transported to ν₁rc's ACTUAL pair at
`rc_landingKey_gate_actual`/`rc_historyCoherent_gate_actual`.
Per the pass-7 enumeration of the affected compiled claims:
`landingKey_recentering_K0` — witnessed (fired by `rc_landingKey_gate`);
`historyCoherent_recentering_K0` — witnessed (fired by
`rc_historyCoherent_gate`); the recentering branch of `readsOf_landing_K0`
— its antecedent bundle is still without a compiled `ReadsOf` run
containing a recentering read: that branch's compiled inhabitation REMAINS
UNWITNESSED-ON-FILE (displayed, not absorbed — [REV 10]). The
NON-VACUITY claim therefore scopes to the TWO DEDICATED recentering
corollaries (now at both the synthetic and [REV 10] the transported actual
pair), NOT to the entire [RC] perimeter; REC-SL's remaining legs,
GRADED-READ, and SITE-EXH stay open as displayed ([REV 12]: REC-WIRE-G and
the e-half removal are discharged — §S2 case (b); the wiring gate
`rc_wireG_gate` adds a third fired corollary at H₃rc's node 1). The BM2_TRACE worked example
(BM2_TRACE_2026-08-02.md) remains at-instance EVIDENCE; the compiled
witness is now on file. No claim of this note consumes [RC] inhabitation.

**RG-2 fence display (honesty; REWRITTEN at REV 4 — the pass-3 finding-4
fold: the LOCATION claim is dropped).** The same coherence leg that asserts
case (a)'s
lift also records `νᵢ.e = 1` (the HK-06 recording fence — non-recentering
interior reads with regrade stride e ≥ 2 are UNRECORDABLE, a disclosed
wiring-scope restriction with its own ledger home, `QUEUE_EXECUTION_2026-07-31.md`
§Item 3). So recorded INTERIOR [NR] increments instantiate LIFT-CONF at stride 1
(degree still grows by g ≥ 1). For e ≥ 2 lifts this note makes NO location
claim: the previous display's "e ≥ 2 lifts, WHEN fired, fire at
`LandingKey`/`ReadsOf` FINAL reads" is RETIRED — locating ALL fired e ≥ 2
lifts at final reads would require SITE-EXH (or a separate firing-path
theorem) and is not compiled. What IS compiled is per-declaration and
conditional: `readsOf_landing_K0` covers every read of an ASSUMED `ReadsOf`
run (final read included, where e ≥ 2 strides are recordable) — and no
compiled declaration proves EXISTENCE of a final read with e ≥ 2 (checked
2026-08-02: the displayed non-vacuity anchor `gate_readsOf_inert2` is an
inert gate, e = 1; no e ≥ 2 `ReadsOf` existence lemma found in the corpus).
Caveat (REV 4; REWRITTEN at REV 11 — the pass-9 CRITICAL-4 fold): the
former "an e ≥ 2 RECENTERING read gets the SYNTHETIC (1, 1) only, no
actual-pair coverage at ANY stride" sentence is RETIRED twice over — (i)
the compiled [RC] conclusion now TRANSPORTS to the actual pair
(`k0_actual_of_recentering` [REV 10]), and (ii) the discussed shape is
UNINHABITABLE at the node level: `Node.hspecRec` (`MovesC/Defs.lean:454`,
a structure field) forces e = 1 ∧ g = 1 at every species-recentering
node, so an e ≥ 2 species-recentering node CANNOT BE CONSTRUCTED — the
e ≥ 2 concern at recentering reads is VACUOUS at the node level. Where
e ≥ 2 ACTUALLY lives: NON-RECENTERING interior reads — fenced by RG-2's
recording conjunct νᵢ.e = 1, exactly the C-e exhibit above (the traced
tower's interior e = 2 step is a non-recentering read, unrecordable in
lawful full runs) — and `ReadsOf` FINAL reads (recordable; no compiled
e ≥ 2 existence lemma, as displayed).
**[REVISION 6 — the pass-4 CRITICAL-2 fold: the former "conformance
quantifies over FIRED lifts" universal is RETIRED.]** The compiled
declarations quantify over their carrier PREDICATES (the
`IsNodeLift`/`IsRecenteringCore`/`LandingKey`/`HistoryCoherent`/`ReadsOf`
antecedents); whether every engine firing satisfies one of those predicates
is exactly SITE-EXH, open (§S4 (i-d)). No claim of this note leans on
the fence.

## S3. The OL-1 family clause (B-M2's blueprint statement) — assembly and per-clause status

**OL-1 (blueprint §2.1, verbatim core; strong induction on i, cumulative — the
P2-4 reading).** For every well-formed chain datum c of order i ≥ 1:
(a) Lift_i(c) is monic in O[x] of degree m_{i+1} = m_i·e_i·f_i;
(b) Lift_i(c) is a Def-2.12 representative of its type, R_i(Lift_i(c)) ∼ ψ̂_i;
(c) V_{i+1} = e_i·f_i·(e_i·V_i + h_i).

* **(a) — status per the §S0 table: PROVED on [NR]; PROVED on [RC] VIA
  TRANSPORT [REV 10]; universal reading rides SITE-EXH.** Corpus face:
  Theorem LIFT-CONF clause 3 (this unit, compiled), with f_i = g via
  `Node.hψdeg : ψ.natDegree = g`; integrality = the `ℤ_[p]` carrier.
  On **[RC]** OL-1(a) is now PROVED [REV 10 — a REAL discharge, the pass-8
  finding-1 consequence]: the compiled case-(b) conclusion
  `K0Conformant σ 1 1 Φ'` transports to the node's ACTUAL pair by
  `k0_actual_of_recentering` (`Node.hspecRec` forces (e_i, g_i) = (1, 1)),
  and with `Node.hψdeg` (f_i = g) the clause's arithmetic closes:
  deg Φ' = m = 1·1·m = e_i·f_i·m_i — exactly the pass-3-adjudicated,
  pass-6-re-derived sufficiency arithmetic (§S2 case (b)), its formerly
  missing input now compiled. `IsReadLift` and the remaining REC-SL legs
  were adjudicated NOT needed for this clause; the pair's WIRING
  provenance was a provenance strengthening, not a gate on this clause —
  and is compiled anyway [REV 12] (`recParam_eg_of_recentering`, §S2
  case (b)). The
  UNIVERSAL reading ("every Lift_i(c)") rides SITE-EXH. Math face: Lemma PAR-MON (base + step tower induction),
  ALREADY VERIFIED as B-M1 §S3.2 content at the 2-clean bar. The abstract
  engine's O[x]-integrality is its construction's digit lifts (the (s7)(a)
  displayed top term) — rides the S-1 identification (residue (ii) of S4).
* **(c) — supplied on the class; conditional for THE lift (both classes —
  §S0 table).** On (SL-dev)+(SL-top)
  lifts, B-M1 §S3.2 (verified) gives (K1): w_{i+1}(Φ_{i+1}) = γ̂ = e·g·γ_{i+1};
  with the constants dictionary γ_{i+1} = e·V_i + h_i (B-M1 §S1.3 remark) this
  IS (c): V_{i+1} = e·g·(e·V_i + h_i), f_i = g. For the FIRED lift, (c) therefore
  rides exactly the class-membership seam GRADED-READ (S4 residue (i-b); plus
  REC-SL on [RC]); its universal fired-lift reading additionally rides
  SITE-EXH (§S4 (i-d), REV 4). Corpus rendering displayed: the stage normalization
  `Stage.hwΦ : w Φ = h` + coherence's γ-tie/stretch (`strFrame`) carry the
  recorded stage-scale face; the stage↔absolute scale dictionary is GRB TR-2
  territory (discharged there; that leaf's bar is inherited conditionality).
* **(b) — CONDITIONAL, seams displayed by name (§S0 table).** GIVEN class membership —
  (K0) from this unit (both classes; [RC] via the [REV 10] transport) + (K1)+(K2) from
  GRADED-READ (open) + REC-SL on [RC] (open) — the verified KP-STEP delivers
  (KPa)+(KPirr)+(KPb-F) — the FGMN/ideal-form block; through GMN Def 2.12's
  ∼-insensitive normalization (TR-B3) that is "Def-2.12 representative of its
  type" in the convention-free reading. Its universal fired-lift reading
  rides SITE-EXH (§S4 (i-d), REV 4). The GMN-VERBATIM face R_i(Lift_i) ∼ ψ̂_i
  rides the open TR-3-ORD-X label tie (B-M1 §S6.3's display — a GRB-leaf
  residue, shared, not re-priced here). The ladder inputs ((GR-B-STR), the
  (V1)/(KP) recursion) carry the GRB leaf's own state: counter 0/2 at record
  time — pass 8 returned gaps, folded at its rev 7 (commit 83c4a26); no
  clause this unit consumes was touched by that fold (checked: the rev-7
  edits are (0′) anchor-unit wording + S5.2/S5.3 e_i ≥ 2 qualifiers — B-M2
  fires no GRB clause directly).
* **The cumulative thread (§S7 obligation (iii)) — displayed skeleton.** The
  induction is per-level-conformance + ladder recomposition: level i's block
  (KP-STEP at the fired lift) feeds (V1)_{i+2} via BRIDGE + MacLane Thm 4.2 (the
  §0 ladder, on file), and "type over F_rep" stays well-defined level-by-level
  through the residue tower (FGMN Cor 4.9 — discharged at GRB S5.1). Nothing
  new is proved here; the skeleton names its inputs so the assembly can be
  audited. PAR-MON's own tower induction (the (a)-thread) is the verified
  instance of this skeleton.

**Member-(b) retirement arithmetic (blueprint §6(b)).** KP-STEP: VERIFIED
(2-clean). OP-B1: PROVED inside B-M1 (fixed bound; minimality (K0)-gated —
verified). The family clause: THIS note, clean counter 0/2. So member (b)'s
residue after B-M2 = this note's hostile bar + the GRB leaf's bar (inherited)
+ the S4 seams below. REVISION 2 honesty: the pass-1 fold NAMES two
obligations rev 0's prose had absorbed silently — SITE-EXH (site
completeness) and REC-SL (the recentering (SL) legs). They are not NEW
mathematics created by the fold, but they ARE B-M2 residue that rev 0
failed to display; they now count.

## S4. Residue (what B-M2 leaves open — displayed, priced, owned)

* **(i-a) the marched fork choice at the DATA level — CLOSED at the corpus
  face on [NR] ONLY** (Theorem LIFT-CONF
  clause 2: the twist bookkeeping is exhibited, hard-wired). This is §S7
  obligation (i)'s fork half AT THE DATA LEVEL: on the **[NR]**
  carriers the recorded wiring cannot fire a naked-data lift — the digit
  weight/twist-residual fields are forced by the firing predicate itself.
  On **[RC]** the compiled decls force
  shape + monicity ONLY (window subtraction); no weight or twist-residual
  data is forced on the digit `−lift` there, so NO naked-data-impossibility
  claim is made on [RC] — that leg is REC-SL. Any UNIVERSAL naked-data
  claim ("the engine cannot fire a naked-data lift anywhere") additionally
  rides SITE-EXH [REV 4 — on (i-d)'s consumer list]. (The (SL-dev) CLAIM
  itself rides (i-b); the [RC] legs ride REC-SL (i-c).)
* **(i-b) GRADED-READ — the graded (SL-dev) READING, OPEN.** That the
  hard-wired stage-law data (window/weight/twist-residual fields) yields the
  GRADED identity in(D_κΦ^(eκ)) = U₀ψ_κϕ_{γ̂}ȳ^κ — i.e. (K1)+(K2) for the
  fired lift, hence KP-STEP class membership — is the GenuineStageModel seam
  (Θ/isLoc/discharge), riding A-M2's non-vacuity chain. Owned there (and at
  `kp_step`'s M-binder); NOT discharged by this unit. **[REV 5]** verified
  at the worked instance (BM2_TRACE_2026-08-02.md, commit de7f706): the
  graded identity holds EXACTLY at both non-recentering reads of the traced
  tower — (K1)+(K2) exact at the e = 2 mixed-anchor and g = 2
  two-present-digits shapes, ONE shared U₀ = 1. At-instance verification is
  EVIDENCE for the general claim; the obligation REMAINS OPEN (the GRB
  pass-2 lesson: instance testing never discharges a general claim).
  **Consumers blocked on
  it:** KP-STEP's application to any fired lift; OL-1(b) and OL-1(c) at THE
  lift (§S3); the DevBound Lean row ((vi) below); and [REV 7 — the pass-5
  GAP-3 fold] the abstract-engine OL-1(b)/(c) readings (the §S0
  abstract-engine rows — each also riding S-1 [T] + SITE-EXH, +
  TR-3-ORD-X for (b)'s GMN-verbatim face, + REC-SL on [RC]).
* **(i-c) REC-SL — the recentering (SL) legs, OPEN (new at REVISION 2;
  EXTENDED at REVISION 3; RESTRUCTURED at REVISION 10 — the pass-8
  finding-1 fold).** At [RC] sites what is proved is monic-degree-m (K0)
  at the synthetic pair AND [REV 10] at the node's ACTUAL pair (the
  `k0_actual_of_recentering` transport; §S2 case (b) RESTRUCTURE display).
  Owed under this namespace is EXACTLY, by name [REV 12 — the former
  items (2)/(3) DISCHARGED, see below]:
  (1) **REC-SL proper** — the `IsReadLift`-at-(1, 1) form and the
  (SL-top)/(SL-dev) obligations for the digit `−lift`; these gate the
  KP-STEP route ONLY, never OL-1(a) or the DevBound degree identification
  (the §S2 sufficiency adjudication, pass-3/6-confirmed).
  **DISCHARGED at REV 12** (compiled, `H1/RecParamE.lean`, Lean-core;
  §S2 case (b) RESTRUCTURE display): (2) **REC-WIRE-G** — the
  stage/wiring-level g-half tie, now the compiled kernel + corollary pair
  `recentering_natDegree_eq` (stage layer, no `Node` in the type) +
  `recParam_eg_of_recentering` (interior recentering read in a coherent
  history ⟹ νᵢ.e = 1 ∧ νᵢ.g = 1 from the coherence width clause +
  `hDwidth` + `IsRecenteringCore`, no `hspecRec`); gate `rc_wireG_gate`
  (`H1/RCWitness.lean`); and (3) **the e-half's transition-hypothesis
  removal** — subsumed by the same corollary (νᵢ.e = 1 with no `hprev`;
  `recParam_e_of_transition`'s clause (b) stage-node tie remains
  transition-case content, gating nothing).
  The FORMER sub-obligation REC-PARAM ([REV 3] — the node-level
  (e_i, g_i) = (1, 1) identification) is **SUPPLIED at REV 10** by
  `Node.hspecRec` + the compiled transport and no longer gates any
  consumer; its historical display and stratification live at §S2
  case (b). Owner: B-M2 continuation.
  **Consumers blocked on the REMAINING legs:** KP-STEP application at
  recentering reads (leg (1) + GRADED-READ); OL-1(b)/(c) at those sites;
  the DevBound Lean row's graded route ((vi) below); and the
  abstract-engine [RC] OL-1(b)/(c) readings (each also riding S-1 [T] +
  SITE-EXH, and GRADED-READ).
  **DISCHARGED from this consumer list at REV 10 (each by the compiled
  transport, citation `k0_actual_of_recentering`):** OL-1(a) on [RC]
  (§S3 (a) — proves outright with `Node.hψdeg`); the DevBound [RC]
  degree identification ((vi) below); the abstract-engine [RC] OL-1(a)
  reading's parameter component (its S-1 [T] + SITE-EXH riders stand,
  §S0 row).
* **(i-d) SITE-EXH — firing-site completeness, OPEN (new at REVISION 2;
  consumer list EXTENDED at REV 4 — the pass-3 finding-7 fold).**
  That the three recorded carriers (§S2) exhaust the engine's key-firing
  sites; the two honest routes are displayed at §S2. Owner: B-M2
  continuation. **Consumers blocked on it:** every universal ("every fired
  lift") reading of LIFT-CONF; the PAR-MON supply census's closure (§S2
  proof); OL-1(a)'s universal reading; the universal fired-lift readings of
  OL-1(b) and OL-1(c) (§S3); the §S0 KP-STEP class/application row's
  universal reading; any universal naked-data claim extending (i-a) beyond
  the [NR] carriers; any location claim for fired e ≥ 2 lifts (the
  retired RG-2 sentence — §S2); and **[REVISION 6 — the pass-4 GAP-2
  fold]** the blueprint's ABSTRACT-ENGINE universal readings ("every
  Lift_i(c)", the OL-1 blueprint clauses — riding S-1 [T] + SITE-EXH, the
  §S0 abstract-engine rows [REV 7: row split — the (b)/(c) readings
  additionally ride GRADED-READ (+ TR-3-ORD-X for (b)) and REC-SL on
  [RC]]; the §S2 REC-PARAM sufficiency caveat already
  said REC-PARAM does not replace this coverage). GRADED-READ or REC-SL can establish
  properties AT an eligible site; neither proves that every engine lift
  REACHES one of those sites.
* **(ii) the abstract-engine identification — OPEN, [T].** O-2a's pinned Lift_i
  ("arbitrary but fixed, pinned to the project engine's standard lift") vs THIS
  corpus's wiring = the S-1 transcription/composition audit — the standing §3.3
  [T] row. Blueprint §3.3's honest-scope split applies verbatim.
* **(iii) OL-1(b) GMN-verbatim** — TR-3-ORD-X (open; GRB residue, shared).
* **(iv) inherited conditionality** — the GRB leaf's own bar (1/2 clean) + N-1
  (wave-0 numerics debt, A-M2's harness).
* **(v) this note's hostile bar** — 0/2.
* **(vi) DevBound (Lean row) does NOT retire yet.** `DevBound.block` is a GRADED
  divisibility statement; this unit compiles the DEGREE identification on
  **[NR]** (natDegree σ'.Φ = e·g·deg σ.Φ) and [REV 10] on **[RC]**: the
  compiled degree-preservation at the synthetic (1, 1) transports to the
  ACTUAL-pair identification via `k0_actual_of_recentering` (the same
  arithmetic as the §S2 OL-1(a) adjudication — e = g = 1 + the compiled
  degree preservation IS the identification, its input now compiled;
  REC-SL's other legs matter for the row's graded/KP-STEP route, not for
  the degree identification), which together with the VERIFIED OP-B1
  completes the row's MATH derivation at the wired [NR] class (B-M1 §S8's
  Lean-face
  paragraph), but the row's Lean derivation still needs GRADED-READ ((i-b)).
  The row stays an explicit binder per the finding-11/12 discipline.

* **THE CONSOLIDATED CURRENT-OPENS LIST (REV 13 — the pass-11 GAP fold; the
  single canonical enumeration, matching the §S0 table; the headline cites
  this list verbatim).** After REC-WIRE-G + the e-half removal DISCHARGED at
  REV 12, exactly these obligations remain open on B-M2's face:
  1. **GRADED-READ** — the graded (SL-dev) reading (K1)+(K2) ((i-b) above).
  2. **REC-SL proper** — the `IsReadLift`-at-(1, 1) form + the (SL-top)/
     (SL-dev) digit obligations, gating the KP-STEP route only ((i-c) leg (1)).
  3. **SITE-EXH** — firing-site completeness ((i-d) above).
  4. **the `readsOf_landing_K0` recentering-branch witness debt** — that
     branch stays UNWITNESSED-ON-FILE [REV 10 scope; §S0 synthetic-pair row].
  (The abstract-engine [T]/S-1 rows and TR-3-ORD-X are the inherited/blueprint
  riders of (ii)/(iii), not B-M2-face obligations.)

## S5. Lean face — `lean/LeanUrat/Scaffold/HDischarge/H1/Conformance.lean` (landed with this note)

| decl | content | status |
|---|---|---|
| `K0Conformant` | (K0) at a read: monic of degree e′·g·deg σ.Φ | def |
| `isReadLift_K0` | D8 shape + `Stage.hmonic` + `1 ≤ e′` ⟹ (K0) — PAR-MON's step compiled (read-pair transport of `L3_liftMonic`) | **PROVED** |
| `isNodeLift_K0` | (K0) at the node's own read pair (`Node.he` supplies 1 ≤ e) | **PROVED** |
| `sub_inC_K0` | window subtraction is degree-preserving monic — (K0) at e = g = 1 | **PROVED** |
| `landingKey_K0` / `landingKey_recentering_K0` | the fired landing key, both species ([NR] actual pair / [RC] synthetic (1, 1)) | **PROVED** |
| `historyCoherent_key_K0` / `historyCoherent_recentering_K0` | wired interior transitions, both legs ([NR] / [RC]) | **PROVED** |
| `readsOf_landing_K0` | EVERY read of a `ReadsOf` run fires a conformant key (final read included; [NR] branch actual pair, [RC] branch synthetic) | **PROVED** |

Declaration census (exact; REVISION 2; re-confirmed exact at pass 3): 9
declarations — 1 def
(`K0Conformant`), the core theorem (`isReadLift_K0`), 2 bridge lemmas
(`isNodeLift_K0`, `sub_inC_K0`), 5 carrier-site corollaries (`landingKey_K0`,
`landingKey_recentering_K0`, `historyCoherent_key_K0`,
`historyCoherent_recentering_K0`, `readsOf_landing_K0`). Rev 0's "five site
corollaries" vs the ledger's "six" both undercounted/miscounted by mixing
bridges with sites; the list above is the authority. Note the two
`*_recentering_K0` corollaries prove, IN THIS FILE, (K0) at the SYNTHETIC
pair — conclusion `K0Conformant σ 1 1 Φ'`; **[REV 10]** the identification
of the node's actual (e, g) with (1, 1) is now compiled in the sibling
unit — `k0_actual_of_recentering` (`H1/RecParamE.lean`, via
`Node.hspecRec`), instantiated at
`rc_landingKey_gate_actual`/`rc_historyCoherent_gate_actual`
(`H1/RCWitness.lean`); [REV 12] the wiring residuals REC-WIRE-G + the
e-half removal are DISCHARGED — `recentering_natDegree_eq` +
`recParam_eg_of_recentering` (`H1/RecParamE.lean`), gate `rc_wireG_gate`
(§S2 case (b)) (**[REV 9]
both corollaries NON-VACUOUS — `rc_landingKey_gate` fires on
ν₁rc/`landingKey_ν₁rc` directly, `rc_historyCoherent_gate` on the compiled
interior-recentering history H₃rc [REV 10 precision],
`H1/RCWitness.lean`, commit 3fd9caf; the
REC-PARAM e-half is compiled at its came-from-a-non-recentering-transition
scope, `H1/RecParamE.lean`**);
`readsOf_landing_K0`'s "EVERY read" quantifier is internal to `ReadsOf` —
that every engine firing is a `ReadsOf`/`HistoryCoherent` site is SITE-EXH.

Gate: `lake env lean LeanUrat/Scaffold/HDischarge/H1/Conformance.lean` — clean,
zero warnings, zero `sorry` (2026-08-06; re-run at REVISION 2, 2026-08-02,
after docstring-only honesty edits; re-run at REVISION 4, 2026-08-02, after
comment-only edits: the retired RG-2 module-comment line removed, the
`K0Conformant` docstring's OL-1(a) sentence [NR]-scoped with the REC-PARAM
caveat — no statement touched, diff verified comment-only; re-run at
REVISION 6, 2026-08-02, after comment-only edits: the module header's
re-headline sentence split to [NR]-actual/[RC]-synthetic scope and the
"conformance quantifies over FIRED lifts" universal replaced by the
per-predicate form — no statement touched, diff verified comment-only;
re-run at REVISION 7, 2026-08-02, after the comment-only carrier-bullet
split — the six-corollary read-off bullet now states [NR]-actual/
[RC]-synthetic per class — no statement touched, diff verified
comment-only). No new axioms;
imports = `H1/Defs` only (corpus Defs transitively). The file header carries
the same honesty ledger as §S4 (updated to the current names). **[REV 10]
gates:** `Conformance.lean` re-run green after COMMENT-ONLY edits (the
header's and `K0Conformant` docstring's "awaits REC-PARAM" sentences
re-pointed at the compiled sibling transport; diff verified comment-only,
zero statement changes); `lake env lean`
clean on `H1/RecParamE.lean` (+ the new `k0_actual_of_recentering` and its
in-file `#print axioms` = Lean-core) and on `H1/RCWitness.lean` (+ the two
`*_gate_actual` corollaries, in-file `#print axioms` = Lean-core; header
comments re-scoped per the pass-8 finding-2 fold); zero `sorry` in all
three. **[REV 11] gate:** `Conformance.lean` re-run green after the
COMMENT-ONLY RG-2 header edit (the retired e ≥ 2-recentering sentence
replaced by the `Node.hspecRec` vacuity fact — the pass-9 CRITICAL-4
fold; diff verified comment-only, zero statement changes, zero `sorry`).
**[REV 12] gates (2026-08-02):** `lake env lean` clean on
`H1/RecParamE.lean` with the two NEW declarations
`recentering_natDegree_eq` + `recParam_eg_of_recentering` (header's
stale "tied NOWHERE" scope note replaced by the accurate narrow form —
the pass-10 CRITICAL-2 fold) and on `H1/RCWitness.lean` with the new
gate `rc_wireG_gate` (header updated); in-file `#print axioms` =
Lean-core `{propext, Classical.choice, Quot.sound}` on all new
declarations; zero `sorry`; no existing statement touched
(`Conformance.lean` untouched at this revision).

## S6. Honesty ledger and attack list

**(i) Proved vs consumed vs open (status per the §S0 table).** PROVED here:
Theorem LIFT-CONF as re-headlined — the D8-data display (clauses 1–2,
definitional, on [NR]) + (K0)-conformance at the actual pair
(clause 3, compiled, on [NR]) + the case-(b)/[RC] monic-degree-m leg at the
SYNTHETIC pair
(1, 1) (compiled) + [REV 10] the transport of that leg to the node's ACTUAL
pair (`k0_actual_of_recentering`, via `Node.hspecRec`) + [REV 12] the
WIRING provenance of the pair (`recentering_natDegree_eq` +
`recParam_eg_of_recentering` — REC-WIRE-G + the e-half removal,
DISCHARGED). CONSUMED (verified elsewhere): KP-STEP + Lemma
PAR-MON + OP-B1 + §S3.2's (SL) computations (B-M1, 2-clean); the corpus pins
(pass-4 source-verified). NOT PROVED here (conditional, named): KP-STEP class
membership of the fired lifts, and hence any application of KP-STEP to them.
OPEN: S4's list — GRADED-READ (i-b), REC-SL proper (i-c; its former
wiring items (2)/(3) discharged at REV 12), SITE-EXH
(i-d), (ii), (iii), (iv), (v), (vi)'s graded route.

**(ii) Non-consumption audit.** This note fires NO graded-model field, no
(GR-B-KEY) clause, no properness, no TR-3 row, no gate result as evidence (the
B-M1 gate is cited only as the reason (SL-dev)'s marched clause is load-bearing
— a refutation of the alternative, per the standing support-only rule).

**(iii) Where to attack (for the hostile pass).** [REVISION 2 note: pass 1
attacked the CLAIM STRUCTURE instead and hit — see §REVISION 2. Item 1 below
is now the displayed open SITE-EXH rather than a mere attack surface.
REVISION 4 note: passes 2 and 3 hit the same structure again — the [NR]/[RC]
class discipline of §SC/§S0 is the standing repair; attack IT first.]
1. The carrier ENUMERATION: is there a corpus site that fires a key OUTSIDE the
   three carriers of §S2 (a builder that installs a stage key without
   `IsNodeLift`/`IsRecenteringCore`/`LandingKey`)? A fourth carrier would be a
   critical finding against "every lift the engine fires". [= SITE-EXH.]
2. The claim "clause 2 excludes the naked side BY DEFINITION" — check the D8
   residual field's exponent (−σ.t·σ.wPrev(t_k)) against the forced (S5)
   positions and the B-M1 gate's twist formula (declared circular there at P3):
   is the FIELD the marched normalization, or merely A normalization?
3. The (c)-clause constants: γ_{i+1} = e·V_i + h_i is quoted from B-M1 §S1.3's
   remark — re-derive it; a mismatch would break the (c) display (not the
   theorem).
4. `isReadLift_K0`'s statement vs the note's clause 3: the Lean lemma binds
   `1 ≤ e'` explicitly — check every consumption site supplies it from a
   carried field (`Node.he`), never from an ambient assumption.
5. The RG-2 display: confirm no claim of this note quantifies over the
   unrecordable e ≥ 2 interior (the fence's content must stay at its own row),
   and that no location claim for fired e ≥ 2 lifts has crept back in.
6. The [NR]/[RC] sweep itself (REV 4): find ONE status sentence about
   lifts/carriers/firing with no [NR]/[RC] tag and no table deferral whose
   grammatical scope includes [RC] in an [NR]-only claim.
7. THE CENSUS (REV 7): re-run the REVISION 7 census grep (the exact
   keyword list, segmentation rule, and line ranges are recorded in the
   REVISION 7 record) over §SC–§S6 + the Lean module doc block; any status
   sentence about lifts/carriers/firing that is ABSENT from the REVISION 7
   census table, or MISCLASSIFIED there, is an AUTOMATIC finding.

## REVISION 2 — the pass-1 fold record (2026-08-02)

**[REV 4 note: HISTORICAL record. Any status language below reflects the
REVISION-2 state of the note and is superseded by the §S0 claim-class table;
the [NR]/[RC] vocabulary postdates it — read "at the recorded carriers" as
untagged pre-REV-4 prose, and "(K0) at e = g = 1" as the [RC] synthetic
leg.]**

Pass 1 (Codex, hostile; archived `BM2_pass1_2026-08-06.md`) returned
**CRITICAL** against the note's claim structure; the compiled Lean artifacts
were not challenged and STAND unchanged (statements untouched; docstrings/
header re-worded to the K0-conformance language only; gate re-run green).
Finding-by-finding disposition:

* **CRITICAL 1** — *"'order-≥2 instances inhabit KP-STEP's class — PROVED' is
  false as stated: only (K0) is definitional/compiled; (K1)+(K2) ride the
  note's own admitted OPEN graded (SL-dev) reading."* **ACCEPTED, folded.**
  The note is re-headlined as a D8-data/(K0)-conformance theorem (title, S0,
  S1's assumes/supplies split, S2's conclusion); class membership and every
  KP-STEP application are now displayed CONDITIONAL on GRADED-READ (named,
  §S4 (i-b), with its consumer list). The offending "PROVED", "the wiring
  therefore fires only marched-data lifts", and "every lift … lies in the
  class" sentences are rewritten.
* **CRITICAL 2** — *"the theorem quantifies over 'every fired lift' incl.
  recenterings (case b), but the (SL-top)/marched-data argument covers only
  (a)/(c); the recentering leg proves monic-degree-m only."* **ACCEPTED,
  folded.** Case (b) is split out as a separate DISPLAYED conditional case
  (§S2): proved = (K0) at e = g = 1 (compiled `*_recentering_K0`); owed =
  the (SL-top)/(SL-dev) obligations, the named open **REC-SL** (§S4 (i-c)).
* **CRITICAL 3** — *"liftKey/liftKeyExt are called covered by 'the same
  statement' while their marched (SL-dev) data are expressly declined."*
  **ACCEPTED, folded.** The base-face paragraph now EXCLUDES their (SL-dev)
  data obligations from LIFT-CONF's claim outright, and the order-≤1
  "reproduces the KEY-g lift class" sentence is retired (§S2 conclusion).
* **GAP 1** — *"firing-site completeness asserted by prose definition, not
  proved; five-vs-six carrier counting inconsistency."* **ACCEPTED, folded.**
  The prose definition is replaced by the displayed open obligation
  **SITE-EXH** (§S2, §S4 (i-d)) stating both honest routes (closed
  `FiredLift` datatype + exhaustive elimination, or the full constructor/call
  audit); the count is fixed by exact enumeration of the 9 declarations
  (§S2 proof + §S5): 1 def + 1 core theorem + 2 bridges + 5 carrier-site
  corollaries.
* **GAP 2** — *"one monicity example is not a supply audit; sound only
  conditional on the carrier enumeration."* **ACCEPTED, folded.** The §S2
  proof now carries the current `rg "hmonic :="` census (13 constructor-site
  hits, classified base/gate vs forge re-supply; `OM/Discriminant.lean:411`
  excluded as a local `have`), presented explicitly as a census, NOT a
  completeness proof — closure owned by SITE-EXH; the PAR-MON thread is
  stated sound CONDITIONAL on SITE-EXH.
* **GAP 3** — *"S0's table overclaims relative to the honest OL-1(b)
  paragraph."* **ACCEPTED, folded.** S0 rewritten: both seams displayed
  (GRADED-READ class membership required; TR-3-ORD-X open and not consumed);
  every "PROVED" label re-priced; the conditional rows name their seams.
* **Wording defect 1** — *"'the (K0) class the verified KP-STEP quantifies
  over' — KP-STEP's class is (K0)+(K1)+(K2)."* **ACCEPTED, folded** (§S2
  conclusion: "satisfies the (K0) CLAUSE of the class"; §S1's split states
  which hypotheses KP-STEP assumes vs which B-M2 supplies, aligned verbatim
  with B-M1 REVISION 3 §S1.3).
* **Wording defect 2** — *"compiled implication lemmas do not prove the
  closed-world premise."* **ACCEPTED, folded** (S0 tail + §S2 proof tail:
  compilation proves the implications, never that all firings satisfy the
  antecedents — that premise is SITE-EXH).

Net effect: the note emerges STRICTLY more conditional. The unit's surviving
value: the compiled (K0)-conformance theorem + the definitional D8-data
display at the recorded carriers + a complete named map (GRADED-READ, REC-SL,
SITE-EXH) of what full KP-STEP class membership still needs. Lean-face edits:
COMMENTS/DOCSTRINGS ONLY (module header, `K0Conformant` and
`readsOf_landing_K0` docstrings); zero statement changes; gate re-run clean.

## REVISION 3 — the pass-2 fold record (2026-08-02)

**[REV 4 note: HISTORICAL record. Any status language below is superseded by
the §S0 claim-class table; "non-recentering carriers" = [NR], "recentering
sites" / "(K0) at the synthetic pair" = [RC].]**

Pass 2 (Codex, hostile; archived `BM2_pass2_2026-08-06.md`) returned
**CRITICAL**: 6/8 pass-1 findings confirmed cured, the 9-declaration
authority list confirmed exact against the pasted file, the Lean content
audited accurate — but recentering scope was found leaking back into
universal claims, plus one fresh gap. The compiled Lean artifacts were not
challenged and STAND unchanged (zero Lean-file edits at this revision).
Finding-by-finding disposition:

* **CRITICAL (finding 2 survival, part 1)** — *"'at every lift fired on the
  three RECORDED carriers … the D8 stride shape + the stage-law digit DATA
  are definitional' (S0 row) grammatically includes recenterings, despite
  the split."* **ACCEPTED, folded.** The S0 headline row is re-scoped to the
  NON-RECENTERING recorded carriers with an explicit [REV 3] tag; the
  recentering row now points at REC-SL/REC-PARAM. The LIFT-CONF theorem
  header and the S1 assumes/supplies split carry the same scope.
* **CRITICAL (finding 2 survival, part 2)** — *"S4(i-a)'s 'the recorded
  wiring cannot fire a naked-data lift' is stated at the recorded carriers
  with a parenthetical naming SITE-EXH and GRADED-READ but not REC-SL —
  silently closing part of REC-SL."* **ACCEPTED, folded.** (i-a) is
  re-scoped to the NON-RECENTERING carriers; the naked-data-impossibility
  claim is restricted to what the compiled decls give per carrier class —
  at recenterings, shape + monicity ONLY, no digit data forced — and REC-SL
  is named in the scope note.
* **CRITICAL (the sharpest) — OL-1(a) at recentering sites.** *"OL-1(a) is
  claimed PROVED AT THE RECORDED CARRIERS with f_i = g via `Node.hψdeg`; at
  a recentering the compiled conclusion is `K0Conformant σ 1 1 Φ'` — the
  SYNTHETIC pair (1, 1) — and nothing identifies the node's actual e_i, g_i
  with 1; `Node.hψdeg` supplies f_i = g only."* **ACCEPTED, folded, four
  ways:** (a) OL-1(a)'s PROVED verdict re-scoped to the non-recentering
  carrier sites (§S0 row, §S3 bullet); (b) REC-SL EXTENDED with the new
  displayed sub-obligation **REC-PARAM** (stated in full at §S2 case (b)):
  the identification of the node's actual (e_i, g_i) with the synthetic
  (1, 1) of the compiled conclusions; (c) OL-1(a)-at-recentering-sites added
  to REC-SL's consumer list (§S4 (i-c)); (d) the §S2 conclusion "every lift
  fired … satisfies the (K0) CLAUSE of the class" re-scoped to the
  non-recentering carriers, with the recentering leg displayed as pending
  REC-SL/REC-PARAM.
* **GAP — RG-2 existence overclaim.** *"'e ≥ 2 lifts are fired — and
  covered — at LandingKey/ReadsOf FINAL reads' overclaims:
  `readsOf_landing_K0` gives conditional coverage of an assumed run's
  reads, not existence of a final read with e ≥ 2."* **ACCEPTED, folded.**
  Reworded to conditional coverage ("when fired, are covered"); a corpus
  grep for a compiled e ≥ 2 `ReadsOf` existence lemma (2026-08-02) found
  none — the displayed non-vacuity anchor `gate_readsOf_inert2` is an inert
  gate (e = 1) — so no existence citation is available and none is claimed.
  **[REV 4 note: the "when fired, are covered AT FINAL READS" rewording
  itself survived as a LOCATION overclaim — retired at REV 4 (pass-3
  finding 4; §S2 RG-2 display).]**
* **Sweep (the pass-2 genre, applied to the whole note).** Two further
  sentences whose grammatical scope included recenterings in claims the
  compiled decls give only at non-recentering carriers were re-scoped: the
  header's re-headline sentence ("at the recorded carriers"), and §S4 (vi)'s
  DevBound "DEGREE identification at wired sites" (at recenterings the
  compiled identity is degree-preservation at the synthetic (1, 1)). The S5
  census note now displays the `K0Conformant σ 1 1` conclusion form
  verbatim.

Net effect: the note emerges STRICTLY more conditional again. No compiled
claim was weakened; every re-scope narrows a PROSE claim to what the Lean
face proves, and the newly displayed residue (REC-PARAM) was always owed —
it is now priced. Clean counter stays 0/2.

## REVISION 4 — the pass-3 fold record (2026-08-02): the STRUCTURAL rewrite

Pass 3 (Codex, hostile; archived `BM2_pass3_2026-08-06.md`) returned
**CRITICAL**: five critical findings + three gaps — ALL of the same genre
passes 1 and 2 hit (grammatical scope including the recentering carriers in
claims the compiled Lean gives only for non-recentering carriers) — plus two
POSITIVE adjudications. The structural rationale for this revision: three
consecutive hostile passes found the same defect genre, and two piecemeal
folds (REV 2's case split, REV 3's [REV 3] tags) failed to kill it. Per the
bounded-repair rule (≈3 attempts, then re-sketch), REVISION 4 stops patching
sites and rewrites the claim-scope STRUCTURE: the claim classes **[NR]**/
**[RC]** are defined ONCE (§SC), the §S0 status-by-claim-class table is the
SINGLE source of status truth, and the whole note is swept — every status
sentence tagged or deferred to the table; historical revision records carry
blanket supersession notes. The compiled Lean STATEMENTS were not challenged
(finding 10) and STAND unchanged; Lean edits at this revision are
comment/docstring-only. Finding-by-finding disposition:

* **CRITICAL 1** — *"S0 again claims universal (K0): 'order-≥2 fired lifts
  inhabit KP-STEP's class … (K0) is proved; … at recenterings additionally
  REC-SL' — 'additionally REC-SL' does not grammatically restrict the
  preceding assertion; at recenterings (K0) at the actual read parameters is
  not proved."* **ACCEPTED, folded structurally.** The prose S0 verdict rows
  are REPLACED by the status-by-claim-class table: (K0) has two rows —
  actual-pair (PROVED on [NR]; NOT proved on [RC], awaits REC-PARAM) and
  synthetic-(1,1) (PROVED on [RC]). No row asserts a status across both
  classes without displaying both statuses.
* **CRITICAL 2** — *"S1: 'this unit discharges exactly one; KP-STEP does not
  yet fire on any engine lift' — for a recentering at the actual recorded
  pair, the unit does not yet discharge even (K0); it discharges synthetic
  (1,1)-(K0)."* **ACCEPTED, folded.** §S1 now reads: discharges exactly one,
  "and that ONLY on [NR] — on [RC] not even (K0) at the actual read pair is
  discharged (synthetic (1, 1) only; REC-PARAM)".
* **CRITICAL 3** — *"the order-≤1 conclusion is unscoped: 'At order ≤ 1 the
  proved (K0)+shape face is consistent with the proved KEY-g lift class' —
  at recenterings, actual-pair (K0) awaits REC-PARAM, the full IsReadLift
  shape awaits REC-SL."* **ACCEPTED, folded.** The sentence is now an
  explicit [NR]-only claim with the [RC] residue displayed inline (§S2
  conclusion).
* **CRITICAL 4** — *"RG-2 still overclaims both location and coverage:
  'e ≥ 2 lifts, WHEN fired, fire at LandingKey/ReadsOf FINAL reads' — the
  compiled declarations do not prove that every fired e ≥ 2 lift occurs at a
  final ReadsOf landing (SITE-EXH territory); and an e ≥ 2 recentering gets
  only synthetic (1,1) conformance."* **ACCEPTED, folded.** The LOCATION
  claim is DROPPED entirely (§S2 RG-2 display, rewritten): the note now
  states only that the compiled coverage is per-declaration and conditional
  (`readsOf_landing_K0` over an ASSUMED run; no e ≥ 2 existence lemma), adds
  the e ≥ 2-recentering caveat (synthetic (1, 1) only, class [RC]), and adds
  the retired location claim to SITE-EXH's consumer list (§S4 (i-d)).
* **CRITICAL 5** — *"the Lean file's module comment still contains the
  retired RG-2 claim: 'e ≥ 2 lifts are fired (and covered) at
  LandingKey/ReadsOf final reads' — the note/source claim ledger is
  inconsistent."* **ACCEPTED, folded.** `H1/Conformance.lean`'s module
  comment is edited (COMMENT ONLY, zero statement changes): the retired
  sentence replaced by the per-declaration conditional-coverage wording +
  the [RC] caveat; gate re-run green, diff verified comment-only (§S5).
* **GAP 6** — *"the `K0Conformant` docstring overprices: 'and OL-1(a)'s
  normalization … at the site' — applied through either recentering
  corollary, `K0Conformant σ 1 1 Φ'` does not yield OL-1(a) at the actual
  (e_i, f_i) without REC-PARAM."* **ACCEPTED, folded.** The docstring's
  OL-1(a) sentence is [NR]-scoped with the explicit REC-PARAM caveat
  (comment-only edit, same gate).
* **GAP 7** — *"SITE-EXH's consumer list is incomplete: must also include the
  universal fired-lift readings of OL-1(b)/(c), the S0 order-≥2 class row,
  any universal naked-data claim in S4(i-a), and the RG-2 location claim."*
  **ACCEPTED, folded** (§S4 (i-d) extended verbatim with all four; §S3's
  (b)/(c) bullets and §S4 (i-a) now name SITE-EXH for their universal
  readings; pass 3's closing observation displayed there: GRADED-READ or
  REC-SL establish properties AT an eligible site, neither proves every
  lift REACHES one).
* **GAP 8** — *"REC-SL's consumer list omits DevBound; S4(vi) says the
  recentering degree identification rides REC-SL/REC-PARAM but S4(i-c)'s
  list disagrees."* **ACCEPTED, folded** (§S4 (i-c) consumer list +=
  the DevBound Lean row's [RC] leg; the two sections now agree).
* **POSITIVE 9 — REC-PARAM sufficiency ADJUDICATED.** Pass 3 confirmed:
  read as νᵢ.e = 1 AND νᵢ.g = 1, REC-PARAM + `*_recentering_K0` +
  `Node.hψdeg` is SUFFICIENT for corpus OL-1(a) at [RC] sites
  (deg Φ' = 1·1·m = e_i·f_i·m_i), with NO `IsReadLift` theorem needed for
  that clause; `IsReadLift` remains a separate REC-SL leg for the
  stage-law/KP-STEP route; and REC-PARAM does NOT settle the blueprint's
  abstract "every Lift_i(c)" clause (S-1 + SITE-EXH still needed).
  **RECORDED** as the sufficiency display at REC-PARAM's statement (§S2
  case (b)).
* **POSITIVE 10 — Lean face re-confirmed.** Pass 3 re-audited all nine
  compiled declarations sound ("I found no defect in the pasted proof
  bodies"), the census exact, and the statements' [NR]-actual/[RC]-synthetic
  /internal-quantifier distinctions consistently drawn. **RECORDED**; the
  compiled face has now survived three hostile passes untouched.

Net effect: the note emerges STRICTLY more conditional a third time (the
RG-2 location claim retired; two consumer lists widened; the Lean comments
brought into agreement with the note), and structurally hardened: the
[NR]/[RC] discipline makes the pass-1/2/3 genre a mechanically checkable
defect (§S6 (iii) item 6). No compiled claim weakened; the two comment-only
Lean edits only NARROW documentation claims. Clean counter stays 0/2.

— H1_BM2 REVISION 4 (unit B-M2), pass-3 fold 2026-08-02, against H1_BM1
REVISION 3 (VERIFIED, 2-clean) and blueprint REV 3. Hostile passes: 3 run
(all CRITICAL, all folded; pass 3 folded STRUCTURALLY — §SC/§S0 claim-class
discipline); clean counter 0/2. Next: fresh hostile pass on THIS text; on
2-clean, member (b)'s residue re-prices per §S3's retirement arithmetic.

**REVISION 5 (evidence fold, 2026-08-02) — at-instance evidence lines
ONLY; NO claim status changes, NO new obligations.** Source: the B-M2
concrete-trace unit (BM2_TRACE_2026-08-02.md +
`verification/openmath/bm2_recentering_trace.py`, commit de7f706; script
re-run exit 0) — a real p = 3 quartic tower with a genuine order-1
recentering, PARI-confirmed. Folded as [REV 5] lines at: REC-PARAM's
statement (§S2 case (b): actual (e, g) = (1, 1) forced three independent
ways, 387-event hunt clean; + the wiring note — e-half derivable from
`IsRecentering`'s σ.e = 1 + `TransitionData.child_e`
(`Moves/DefsT.lean:150`), g-half tied NOWHERE [REV 9 bracket-correction:
OVERSTATED — nowhere at the STAGE/WIRING level only; `Node.hspecRec`
(`MovesC/Defs.lean`) ties the node-level pair (1, 1) by structure field —
see the §S2 [REV 9] stratification; BM2_TRACE_2026-08-02.md is NOT edited,
being a dated record]); the REC-SL owed list (§S2
case (b): every owed field TRUE at the instance, incl. (SL-top) and
U₀ = 1); GRADED-READ (§S4 (i-b): (K1)+(K2) exact at both non-recentering
reads, one shared U₀ = 1); the SITE-EXH display (§S2: the C-e fence
exhibit — the RG-2 interior fence makes this real tower's recentering
UNRECORDABLE in lawful full runs, so closure must handle truncated-run
firing); + †/‡ evidence marks under the §S0 table. Wording discipline
throughout: at-instance verification is EVIDENCE for the general claims;
every obligation REMAINS OPEN (the GRB pass-2 lesson). Clean counter
UNCHANGED 0/2 — evidence folds reset nothing; hostile pass 4 runs on this
text.

## REVISION 6 — the pass-4 fold record (2026-08-02)

Pass 4 (Codex, hostile; archived `BM2_pass4_2026-08-06.md`) returned
**CRITICAL**: two critical scope failures + two gaps — BOTH criticals
located in the LEAN MODULE HEADER (the REV-4 [NR]/[RC] sweep covered the
note but left the Lean header's two oldest sentences untouched); the
compiled statements were not challenged (the pass re-confirmed the
nine-declaration table exact against the pasted Lean, and returned CLEAN on
evidence/status separation — no [REV 5] evidence line discharges an
obligation). Lean edits at this revision: COMMENT-ONLY (module header),
gate re-run green, diff verified comment-only (§S5). Finding-by-finding
disposition:

* **CRITICAL 1** — *"'what is compiled is (K0)-CONFORMANCE at the recorded
  carriers' — 'recorded carriers' includes [RC], while the compiled [RC]
  result is only `K0Conformant σ 1 1 Φ'`, not (K0) at the recorded node's
  actual pair … It also contradicts §S5's claim that the file header
  carries the current honesty ledger."* **ACCEPTED, folded.** The header's
  re-headline sentence is REWRITTEN to the split form: at the
  non-recentering carriers, (K0) at the recorded node's ACTUAL pair; at
  the recentering carriers, monicity + the synthetic `K0Conformant σ 1 1`
  ONLY, the actual pair awaiting the displayed open REC-PARAM. §S5's
  header-ledger claim is thereby made true again.
* **CRITICAL 2** — *"'Conformance quantifies over FIRED lifts; the fence
  restricts what fires, not the conformance' — an untagged status sentence
  … the compiled declarations quantify over carrier predicates; they do
  not quantify over a closed `FiredLift` universe. That passage is
  precisely what SITE-EXH leaves open. The identical sentence appears in
  the Lean module comment."* **ACCEPTED, folded in BOTH files.** The
  sentence is RETIRED in the note (§S2 RG-2 display) and the Lean header,
  replaced by the honest per-predicate form: the compiled declarations
  quantify over their carrier PREDICATES; whether every engine firing
  satisfies one of those predicates is exactly SITE-EXH, open.
* **GAP 1** — *"§S0 and the body disagree on OL-1(a)'s [RC] dependency …
  'rides REC-SL/REC-PARAM' and the REC-SL consumer list overstate the
  dependency unless 'REC-SL' is being used merely as a namespace for its
  REC-PARAM sub-obligation. That qualification is not stated."*
  **ACCEPTED, folded.** Disambiguated at all three sites per the §S2
  adjudication (pass-3 finding 9, trace-confirmed): the §S0 OL-1(a) [RC]
  cell now reads "awaits REC-PARAM (only; REC-SL's other legs not needed
  for this clause — §S2 adjudication)"; the §S3 prose sentence now reads
  "awaits REC-PARAM ONLY" with the not-needed legs named; the §S4 (i-c)
  consumer entry is marked "via the REC-PARAM sub-obligation only"; and
  the namespace convention (REC-SL cited as PARENT NAMESPACE for
  REC-PARAM-only dependencies, always with an explicit mark) is stated
  once at REC-SL's definition.
* **GAP 2** — *"§S4(i-d)'s declared complete consumer list does not include
  the abstract-engine universal reading."* **ACCEPTED, folded.** The
  blueprint's ABSTRACT-ENGINE universal reading ("every Lift_i(c)", riding
  S-1 [T] + SITE-EXH — the §S0 abstract-engine row) is added to
  SITE-EXH's consumer list at §S4 (i-d).

Header sweep (the last two survivors were both in the Lean header): the
full module comment was re-swept after the two folds for any remaining
untagged status sentence about lifts/carriers/firing — none found; every
remaining status line is per-declaration, [NR]/[RC]-split, or carries its
open-obligation name (SITE-EXH / GRADED-READ / REC-SL / REC-PARAM /
TR-3-ORD-X).

Net effect: the two files' claim ledgers are brought back into agreement
with the §S0 table; no compiled claim weakened; no new obligation created
(GAP 1/GAP 2 are display repairs of already-priced residue). Clean counter
stays 0/2; hostile pass 5 runs on this text.

## REVISION 7 — the pass-5 fold record (2026-08-02): THE CENSUS

Pass 5 (Codex, hostile; archived `BM2_pass5_2026-08-06.md`) returned
**CRITICAL**: three critical findings + three gaps — the SAME mechanical
genre all five passes hit (untagged universal status sentences whose
grammatical scope outruns their [NR]/[RC] tag or omits a named open their
universal reading needs). Pass 5 also VERIFIED all four REVISION-6 folds,
re-confirmed the nine compiled declarations internally consistent ("no
proof-body defect is visible"), and confirmed the REVISION-5
evidence/status discipline clean. Diagnosis of the recurrence: every fold
fixed the QUOTED sites and then judgment-swept for siblings — and judgment
misses. This revision replaces judgment with ENUMERATION (the repo's
ROOT-audit discipline applied to this note): a mechanical sentence census,
recorded in full below so pass 6 CHECKS the census instead of re-hunting.

### Finding-by-finding disposition

* **CRITICAL 1** — *"'KP-STEP does not yet fire on any engine lift' (§S1):
  the prohibited grammatical form — a status assertion about 'any engine
  lift', no [NR]/[RC] split, no S0 deferral, ranging beyond the recorded
  carriers despite SITE-EXH being open."* **ACCEPTED, folded** (census row
  C020/C021): replaced by the per-class form — no KP-STEP application
  established on EITHER recorded class ([NR]: GRADED-READ; [RC]:
  additionally REC-SL incl. REC-PARAM) — plus the explicit disclaimer that
  beyond the recorded carriers the note asserts no engine-wide firing
  status at all, positive or negative (SITE-EXH).
* **CRITICAL 2** — *"'KP-STEP's application to any fired lift is therefore
  CONDITIONAL on those named seams' (§S2): 'those named seams' are
  GRADED-READ and REC-SL/REC-PARAM; an engine-wide 'any fired lift'
  application also depends on SITE-EXH — untagged AND understating the
  conditions."* **ACCEPTED, folded** (census row C048/C049): re-scoped to
  RECORDED-CARRIER lifts with the per-class seam list, and the ENGINE-WIDE
  reading now explicitly rides SITE-EXH (§S4 (i-d)).
* **CRITICAL 3** — *"the Lean header retains the original generic-carrier
  grammar locally: 'the (K0)-conformance read off at each of the three
  RECORDED carriers' asserts undifferentiated (K0)-conformance across all
  recorded carriers."* **ACCEPTED, folded** (census row C116; Lean
  COMMENT-ONLY): the six-corollary bullet now states the read-offs per
  class — [NR] carriers at the node's ACTUAL pair, [RC] carriers the
  SYNTHETIC `K0Conformant σ 1 1` ONLY (§S0 table). Gate re-run green; diff
  verified comment-only (§S5 updated).
* **GAP 1** — *"DevBound dependency remains over-broad: the S0 [RC] cell
  says 'awaits REC-SL/REC-PARAM' though actual-pair degree identification
  requires REC-PARAM only, just as OL-1(a) does; §S4(vi) repeats the
  ambiguity."* **ACCEPTED, folded** (census rows C010, C091, C096) after
  checking the §S2 adjudication: the arithmetic is identical to the
  OL-1(a) case — REC-PARAM's e = g = 1 + the compiled degree preservation
  IS the identification (natDegree Φ' = m = e·g·m); REC-SL's other legs
  matter for the row's graded/KP-STEP route only. The §S0 cell, §S4 (vi),
  and the §S4 (i-c) consumer entry now all read REC-PARAM ONLY (parent
  namespace REC-SL, per the REVISION-6 convention) and agree.
* **GAP 2** — *"the S0 abstract-engine row understates dependencies: if it
  covers the whole OL-1 abstract clause, OL-1(b)/(c) also require
  GRADED-READ, and (b)'s GMN-verbatim face TR-3-ORD-X; if only OL-1(a),
  the label is too broad."* **ACCEPTED, folded** (census row C010): the
  row is SPLIT — an OL-1(a) row (S-1 [T] + SITE-EXH; + REC-PARAM only on
  [RC]) and an OL-1(b)/(c) row (adding GRADED-READ, + TR-3-ORD-X for (b),
  + full REC-SL on [RC]). §S4 (i-d)'s abstract-engine entry now says
  "rows" with the split displayed.
* **GAP 3** — *"named-open consumer lists inherit the omission: REC-SL's
  list does not name the abstract-engine [RC] reading; GRADED-READ's list
  does not include the abstract-engine OL-1(b)/(c) readings."* **ACCEPTED,
  folded** (census rows C089, C091): both lists extended; SITE-EXH's list
  was confirmed complete by the pass and is unchanged (modulo the
  row-split bracket).

### The census method (re-runnable; §S6 attack item 7)

**Scope.** The note body from the `## SC.` heading through the end of §S6
(THIS revision's text: lines 66–687 at record time; the historical
REVISION records and this record are excluded) plus the Lean module doc
block (`H1/Conformance.lean` lines 1–75, through the closing `-/`).
**[REV 13 — scope-hole closure, the pass-11 CRITICAL/GAP fold]:** the Lean
scope is EXTENDED to ALL doc comments (module doc block AND every `/-- … -/`
declaration docstring) of the THREE H1 face files —
`H1/Conformance.lean`, `H1/RecParamE.lean`, `H1/RCWitness.lean` — not the
module doc block of `Conformance.lean` alone. The pass-11 defect was that
two stale docstrings (`K0Conformant`, `k0_actual_of_recentering`) said
REC-WIRE-G "stay open"/"remain open" while sitting OUTSIDE the old
module-doc-block scope; this extension makes such docstrings first-class
census hits permanently. New rows added at REV 13 (C205–C210 below).
**Segmentation.** Text split into sentences at `.`/`?`/`!` followed by
whitespace, suppressing splits after `e.g`/`i.e`/`vs`/`§S<n>`/`<d>.<d>`/
`Def-2`/`Thm 4`/`Cor 4`; tables and displayed lists ride with their
introducing sentence. **Keywords (case-insensitive; a sentence is a HIT if
either matches):** the word-boundary regex
`\blift(s)?\b|carrier(s)?|\bfire(d|s)?\b|firing|engine|KP-STEP|conforman(ce|t)|\(K0\)|K0Conformant|recenter`
PLUS the substring supplement (catches `Lift_i`, `IsReadLift`, `liftKey`,
…): `lift|carrier|fire|firing|engine|KP-STEP|conforman|\(K0\)|K0Conformant|recenter`.
**Result at record time:** 182 sentences in scope, **123 hits**.
**Classification:** [TAGGED] = carries an explicit [NR]/[RC] tag, an
explicit deferral to the §S0 table, or an explicit named-open conditional
(SITE-EXH/REC-*/GRADED-READ/TR-3-ORD-X/S-1) covering its full scope;
[STATUS-FREE] = definitional, historical/verbatim quote, attack-list item,
external-unit cite, or makes no proved/awaits claim; [UNTAGGED-STATUS] =
the genre — all such hits FIXED at this revision. **Counts: 123 hits =
81 [TAGGED] standing + 33 [STATUS-FREE] + 9 rows FIXED at REV 7** (the 9
rows = 7 defect sites: two sentences span two rows each because the
segmenter splits at "incl."; at census start on the REVISION-6 text the
same method gave 119 hits with 6 [UNTAGGED-STATUS] hits = 7 defect sites
counting the two S0 cells inside one table hit, plus the keyword-free
§S4 (vi) agreement partner fixed per the pass-5 GAP-1 charge and now
in-census because its fix added a keyword). Line numbers below are those
of the REVISION 7 text.

### THE CENSUS TABLE (every hit; pass 6 checks THIS, not a re-hunt)

| id | loc (line) | sentence (head) | class | disposition |
|---|---|---|---|---|
| C001 | NOTE 66 | The two claim classes — the ONLY scope vocabulary of this… | [STATUS-FREE] | — |
| C002 | NOTE 69 | **[NR] — the NON-RECENTERING recorded carriers.** The site… | [STATUS-FREE] | — |
| C003 | NOTE 75 | On [NR]: **actual-pair (K0) is PROVED, compiled** — the co… | [TAGGED] | stands |
| C004 | NOTE 80 | Still open on [NR]: (K1)+(K2) (**GRADED-READ**), and every… | [TAGGED] | stands |
| C005 | NOTE 81 | **[RC] — the RECENTERING carriers.** The sites read off by… | [STATUS-FREE] | — |
| C006 | NOTE 85 | On [RC] what is proved is monicity + the SYNTHETIC conclus… | [TAGGED] | stands |
| C007 | NOTE 88 | Actual-pair (K0) awaits **REC-PARAM** (§S2 case (b)); the… | [TAGGED] | stands |
| C008 | NOTE 90 | **Standing rule (REV 4).** No untagged universal survives… | [STATUS-FREE] | — |
| C009 | NOTE 97 | Status by claim class — THE table (single source of status… | [TAGGED] | stands |
| C010 | NOTE 105 | REC-PARAM) † ¦ ¦ OL-1(a) corpus face (monic, deg = e_i·f_i… | **[UNTAGGED→FIXED]** | FIXED REV 7 (GAP-1: DevBound [RC] cell → REC-PARAM only; GAP-2: abstract-engine row SPLIT into OL-1(a) / OL-1(b),(c) rows with GRADED-READ + TR-3-ORD-X displayed) |
| C011 | NOTE 119 | **What this note does NOT claim:** KP-STEP class membershi… | [TAGGED] | stands |
| C012 | NOTE 128 | Compilation of the implication lemmas proves THOSE IMPLICA… | [TAGGED] | stands |
| C013 | NOTE 132 | The conformant class, recalled (no re-derivation) KP-STEP… | [STATUS-FREE] | — |
| C014 | NOTE 146 | The B-M1 gate's P4 showed the marched/naked fork in (SL-de… | [STATUS-FREE] | — |
| C015 | NOTE 149 | B-M2's §S7 obligations: (i) the engine emits (SL-dev)-conf… | [STATUS-FREE] | — |
| C016 | NOTE 151 | **What KP-STEP assumes vs what B-M2 supplies (REVISION 2 —… | [STATUS-FREE] | — |
| C017 | NOTE 159 | Against that statement, B-M2 supplies — on **[NR]** — : **… | [TAGGED] | stands |
| C018 | NOTE 166 | On **[RC]** B-M2 supplies LESS: only monicity + synthetic-… | [TAGGED] | stands |
| C019 | NOTE 168 | So of KP-STEP's three hypotheses this unit discharges exac… | [TAGGED] | stands |
| C020 | NOTE 170 | No KP-STEP application is established by this unit on EITH… | **[UNTAGGED→FIXED]** | FIXED REV 7 (CRITICAL-1: the former "KP-STEP does not yet fire on any engine lift" untagged universal replaced by the per-class + SITE-EXH form; sentence spans this row and the next) |
| C021 | NOTE 173 | REC-PARAM) — and beyond the recorded carriers this note as… | (cont.) | (second half of the CRITICAL-1 fix sentence above) |
| C022 | NOTE 178 | Theorem LIFT-CONF (D8-data/(K0)-conformance on [NR], corpu… | [STATUS-FREE] | — |
| C023 | NOTE 193 | **Open obligation SITE-EXH (REVISION 2 — the pass-1 GAP-1… | [TAGGED] | stands |
| C024 | NOTE 198 | That the three recorded carriers EXHAUST the engine's key-… | [TAGGED] | stands |
| C025 | NOTE 199 | The two honest routes: (1) a closed `FiredLift` datatype t… | [STATUS-FREE] | — |
| C026 | NOTE 203 | Until one lands, every "fired lift" below means "lift fire… | [STATUS-FREE] | — |
| C027 | NOTE 204 | No fourth carrier is currently known (the §S6(iii) attack… | [TAGGED] | stands |
| C028 | NOTE 206 | **[REV 5] Data point — the C-e fence exhibit** (BM2_TRACE_… | [TAGGED] | stands |
| C029 | NOTE 212 | It strengthens the case that SITE-EXH closure (either rout… | [TAGGED] | stands |
| C030 | NOTE 214 | **Theorem LIFT-CONF (REVISION 2 form; scope: clauses 1–3 a… | [TAGGED] | superseded REV 12 (the pass-10 CRITICAL-1 stale synthetic-only scope clause — the sentence the census failed to expose, per the pass-10 audit; successor C185) |
| C031 | NOTE 222 | **[(SL-top) + stride shape — DEFINITIONAL, [NR]]** In case… | [TAGGED] | stands |
| C032 | NOTE 235 | What this clause does NOT say (REVISION 2): that these fie… | [TAGGED] | stands |
| C033 | NOTE 241 | **[(K0) — THEOREM, [NR]]** In cases (a)/(c, non-recenterin… | [TAGGED] | stands |
| C034 | NOTE 242 | Integrality is the carrier type (`Polynomial ℤ_[p]`). | [STATUS-FREE] | — |
| C035 | NOTE 243 | **Case (b), recenterings — class [RC]: a separate, DISPLAY… | [TAGGED] | stands |
| C036 | NOTE 249 | OWED — **REC-SL** (open sub-member; owner: B-M2 continuati… | [TAGGED] | stands |
| C037 | NOTE 253 | **[REV 5]** verified at the worked instance (BM2_TRACE_202… | [TAGGED] | stands |
| C038 | NOTE 259 | **[REV 3 — the pass-2 CRITICAL fold: the parameter identif… | [TAGGED] | stands |
| C039 | NOTE 264 | Displayed as a sub-obligation inside REC-SL: REC-PARAM (su… | [TAGGED] | stands |
| C040 | NOTE 270 | (`Node.hψdeg` supplies f_i = g only; it supplies neither e… | [TAGGED] | stands |
| C041 | NOTE 280 | Wiring note (same unit): the corpus never TIES νᵢ.e/νᵢ.g a… | [TAGGED] | superseded REV 12 (the pass-10 CRITICAL-2 fold: the REV-9 bracket was itself still too broad — "tied NOWHERE at the STAGE/WIRING level" is false against the coherence width law; rewritten to the accurate narrow form, successor C186) |
| C042 | NOTE 285 | **REC-PARAM sufficiency — ADJUDICATED at pass 3 (finding 9… | [TAGGED] | stands |
| C043 | NOTE 292 | NO `IsReadLift` theorem is additionally needed for that cl… | [STATUS-FREE] | — |
| C044 | NOTE 293 | The `IsReadLift`-at-(1, 1) form remains SEPARATELY necessa… | [TAGGED] | stands |
| C045 | NOTE 294 | Caveat (same adjudication): for the blueprint's ABSTRACT "… | [TAGGED] | stands |
| C046 | NOTE 299 | Clauses 1–2 above do NOT cover case (b); rev 0's "every fi… | [TAGGED] | stands |
| C047 | NOTE 302 | Consequently: **every lift fired on an [NR] carrier at an… | [TAGGED] | stands |
| C048 | NOTE 312 | KP-STEP's application to any RECORDED-CARRIER lift is ther… | **[UNTAGGED→FIXED]** | FIXED REV 7 (CRITICAL-2: "any fired lift" re-scoped to RECORDED-CARRIER per class; ENGINE-WIDE reading now names SITE-EXH; sentence spans this row and the next) |
| C049 | NOTE 314 | REC-PARAM — §S0 table), and its application to "any fired… | (cont.) | (second half of the CRITICAL-2 fix sentence above) |
| C050 | NOTE 317 | At order ≤ 1 the proved **[NR]** (K0)+shape face is consis… | [TAGGED] | stands |
| C051 | NOTE 328 | (3) is the displayed two-input chain: * *Parent monicity i… | [STATUS-FREE] | — |
| C052 | NOTE 340 | Forge/transition re-supplies: `HC1/S9_builder.lean:587` (`… | [STATUS-FREE] | — |
| C053 | NOTE 346 | The PAR-MON thread is therefore sound CONDITIONAL on SITE-… | [TAGGED] | stands |
| C054 | NOTE 355 | Recenterings ([RC]; the synthetic leg only): `deg lift < d… | [TAGGED] | stands |
| C055 | NOTE 357 | Both legs are COMPILED sorry-free in `H1/Conformance.lean`… | [TAGGED] | stands |
| C056 | NOTE 369 | What their compilation proves is those implications and pe… | [TAGGED] | stands |
| C057 | NOTE 371 | ∎ **Engine's compiled base-face lifts (the §S8 bullet, cit… | [STATUS-FREE] | RECLASSIFIED REV 8 (pass-6 finding 3: an external cited fact, "cited not re-proved" — the census rules give [STATUS-FREE], not [TAGGED]; no text change) |
| C058 | NOTE 377 | **Honest scope (REVISION 2 — the pass-1 CRITICAL-3 fold):… | [TAGGED] | stands |
| C059 | NOTE 382 | Rev 0's suggestion that the order ≤ 1 statement "reproduce… | [TAGGED] | stands |
| C060 | NOTE 385 | **Non-vacuity of the carriers (displayed).** The quantifie… | **[UNTAGGED→FIXED]** | FIXED REV 8 (pass-6 finding 2: the plural "the quantified predicates are inhabited" carried no [NR]/[RC] tag and outran its evidence — the cited witnesses are [NR]-only. Rewritten per class: [NR] witnessed by name; [RC] displayed UNWITNESSED-ON-FILE after a corpus grep, BM2_TRACE = evidence only, compiled witness = one-lemma follow-up); paragraph REWRITTEN REV 9 — successor rows C124–C128 (the [RC] debt DISCHARGED, witness `H1/RCWitness.lean` commit 3fd9caf) |
| C061 | NOTE 389 | The theorem is not a statement over empty carriers. | **[UNTAGGED→FIXED]** | FIXED REV 8 (pass-6 finding 2, same paragraph: replaced by the [NR]-scoped form "the [NR] carrier corollaries are not statements over empty carriers"); paragraph REWRITTEN REV 9 — successor rows C124–C128 |
| C062 | NOTE 390 | **RG-2 fence display (honesty; REWRITTEN at REV 4 — the pa… | [TAGGED] | stands |
| C063 | NOTE 398 | So recorded INTERIOR [NR] increments instantiate LIFT-CONF… | [TAGGED] | stands |
| C064 | NOTE 399 | For e ≥ 2 lifts this note makes NO location claim: the pre… | [TAGGED] | stands |
| C065 | NOTE 408 | Caveat (REV 4): an e ≥ 2 RECENTERING read is class [RC] —… | [TAGGED] | stands |
| C066 | NOTE 411 | **[REVISION 6 — the pass-4 CRITICAL-2 fold: the former "co… | [TAGGED] | stands |
| C067 | NOTE 420 | The OL-1 family clause (B-M2's blueprint statement) — asse… | [STATUS-FREE] | — |
| C068 | NOTE 426 | * **(a) — status per the §S0 table: PROVED on [NR]; NOT pr… | [TAGGED] | stands |
| C069 | NOTE 431 | On **[RC]** OL-1(a) is NOT proved: the compiled case-(b) c… | [TAGGED] | stands |
| C070 | NOTE 437 | OL-1(a)-on-[RC] therefore awaits **REC-PARAM ONLY** [REVIS… | [TAGGED] | stands |
| C071 | NOTE 442 | The UNIVERSAL reading ("every Lift_i(c)") rides SITE-EXH. | [TAGGED] | stands |
| C072 | NOTE 444 | The abstract engine's O[x]-integrality is its construction… | [TAGGED] | stands |
| C073 | NOTE 446 | * **(c) — supplied on the class; conditional for THE lift… | [TAGGED] | stands |
| C074 | NOTE 451 | For the FIRED lift, (c) therefore rides exactly the class-… | [TAGGED] | stands |
| C075 | NOTE 457 | * **(b) — CONDITIONAL, seams displayed by name (§S0 table)… | [TAGGED] | stands |
| C076 | NOTE 463 | Its universal fired-lift reading rides SITE-EXH (§S4 (i-d)… | [TAGGED] | stands |
| C077 | NOTE 464 | The GMN-VERBATIM face R_i(Lift_i) ∼ ψ̂_i rides the open TR… | [TAGGED] | stands |
| C078 | NOTE 466 | The ladder inputs ((GR-B-STR), the (V1)/(KP) recursion) ca… | [STATUS-FREE] | — |
| C079 | NOTE 471 | * **The cumulative thread (§S7 obligation (iii)) — display… | [STATUS-FREE] | — |
| C080 | NOTE 479 | **Member-(b) retirement arithmetic (blueprint §6(b)).** KP… | [STATUS-FREE] | — |
| C081 | NOTE 482 | OP-B1: PROVED inside B-M1 (fixed bound; minimality (K0)-ga… | [STATUS-FREE] | — |
| C082 | NOTE 485 | REVISION 2 honesty: the pass-1 fold NAMES two obligations… | [STATUS-FREE] | — |
| C083 | NOTE 491 | Residue (what B-M2 leaves open — displayed, priced, owned)… | [TAGGED] | stands |
| C084 | NOTE 495 | This is §S7 obligation (i)'s fork half AT THE DATA LEVEL:… | [TAGGED] | stands |
| C085 | NOTE 498 | On **[RC]** the compiled decls force shape + monicity ONLY… | [TAGGED] | stands |
| C086 | NOTE 502 | Any UNIVERSAL naked-data claim ("the engine cannot fire a… | [TAGGED] | stands |
| C087 | NOTE 504 | (The (SL-dev) CLAIM itself rides (i-b); the [RC] legs ride… | [TAGGED] | stands |
| C088 | NOTE 511 | **[REV 5]** verified at the worked instance (BM2_TRACE_202… | [TAGGED] | stands |
| C089 | NOTE 517 | **Consumers blocked on it:** KP-STEP's application to any… | **[UNTAGGED→FIXED]** | FIXED REV 7 (GAP-3: abstract-engine OL-1(b)/(c) readings added to GRADED-READ consumers) |
| C090 | NOTE 523 | * **(i-c) REC-SL — the recentering (SL) legs, OPEN (new at… | [TAGGED] | stands |
| C091 | NOTE 537 | Consumers blocked on it:** KP-STEP application at recenter… | **[UNTAGGED→FIXED]** | FIXED REV 7 (GAP-1: DevBound entry gains the REC-PARAM-only mark; GAP-3: abstract-engine [RC] readings added) |
| C092 | NOTE 551 | * **(i-d) SITE-EXH — firing-site completeness, OPEN (new a… | [TAGGED] | stands |
| C093 | NOTE 556 | **Consumers blocked on it:** every universal ("every fired… | [TAGGED] | stands |
| C094 | NOTE 568 | GRADED-READ or REC-SL can establish properties AT an eligi… | [TAGGED] | stands |
| C095 | NOTE 570 | * **(ii) the abstract-engine identification — OPEN, [T].**… | [TAGGED] | stands |
| C096 | NOTE 578 | * **(vi) DevBound (Lean row) does NOT retire yet.** `DevBo… | **[UNTAGGED→FIXED]** | FIXED REV 7 (GAP-1 agreement partner: (vi) now says ACTUAL-pair degree identification awaits REC-PARAM ONLY — keyword-free at census start, in census now because the fix added a keyword) |
| C097 | NOTE 594 | Lean face — `lean/LeanUrat/Scaffold/HDischarge/H1/Conforma… | [TAGGED] | stands |
| C098 | NOTE 613 | Note the two `*_recentering_K0` corollaries prove (K0) at… | [TAGGED] | stands; EXTENDED REV 9 (same sentence gains the [REV 9] non-vacuity + RecParamE annotation — still [TAGGED]) |
| C099 | NOTE 616 | REC-PARAM [REV 3]); `readsOf_landing_K0`'s "EVERY read" qu… | [TAGGED] | stands |
| C100 | NOTE 618 | Gate: `lake env lean LeanUrat/Scaffold/HDischarge/H1/Confo… | [STATUS-FREE] | — |
| C101 | NOTE 637 | Honesty ledger and attack list **(i) Proved vs consumed vs… | [TAGGED] | stands |
| C102 | NOTE 645 | CONSUMED (verified elsewhere): KP-STEP + Lemma PAR-MON + O… | [TAGGED] | stands |
| C103 | NOTE 647 | NOT PROVED here (conditional, named): KP-STEP class member… | [TAGGED] | stands |
| C104 | NOTE 650 | **(ii) Non-consumption audit.** This note fires NO graded-… | [STATUS-FREE] | — |
| C105 | NOTE 662 | The carrier ENUMERATION: is there a corpus site that fires… | [STATUS-FREE] | — |
| C106 | NOTE 664 | A fourth carrier would be a critical finding against "ever… | [STATUS-FREE] | — |
| C107 | NOTE 673 | `isReadLift_K0`'s statement vs the note's clause 3: the Le… | [STATUS-FREE] | — |
| C108 | NOTE 676 | The RG-2 display: confirm no claim of this note quantifies… | [STATUS-FREE] | — |
| C109 | NOTE 679 | The [NR]/[RC] sweep itself (REV 4): find ONE status senten… | [STATUS-FREE] | — |
| C110 | NOTE 682 | THE CENSUS (REV 7): re-run the REVISION 7 census grep (the… | [STATUS-FREE] | — |
| C111 | LEAN 1 | /- HDISCHARGE_H1 — `H1/Conformance.lean`: B-M2's Lean face… | [STATUS-FREE] | — |
| C112 | LEAN 3 | Unit note: `lean/notes/openmath/H1_BM2_2026-08-06.md` (The… | **[UNTAGGED→FIXED]** | FIXED REV 8 (pass-6 finding 1, the recurring genre: "(K1)+(K2) for the fired lifts" spans BOTH classes but only GRADED-READ was attached — per-class form now: on [NR] GRADED-READ; on [RC] GRADED-READ + REC-SL. Lean COMMENT-ONLY; gate re-run green) |
| C113 | LEAN 13 | WHAT IS COMPILED HERE (all sorry-free; no new axioms; no s… | [TAGGED] | stands |
| C114 | LEAN 20 | * `isReadLift_K0` — THE core: every D8-shaped read lift at… | [TAGGED] | stands |
| C115 | LEAN 22 | This is Lemma PAR-MON's induction STEP (KP-STEP §S3.2, ver… | [STATUS-FREE] | — |
| C116 | LEAN 26 | * `isNodeLift_K0` / `landingKey_K0` / `landingKey_recenter… | **[UNTAGGED→FIXED]** | FIXED REV 7 (CRITICAL-3: the carrier-bullet read-off split per class — [NR] actual pair / [RC] synthetic (1,1)) |
| C117 | LEAN 38 | That these three carriers exhaust the engine's key-firing… | [TAGGED] | stands |
| C118 | LEAN 41 | WHAT IS NOT CLAIMED (the unit note's §S4 residue — display… | [TAGGED] | stands |
| C119 | LEAN 51 | The (SL-top) stride shape and the marched (SL-dev) DATA (w… | [STATUS-FREE] | — |
| C120 | LEAN 55 | Parent-key monicity is the carried invariant `Stage.hmonic… | [STATUS-FREE] | — |
| C121 | LEAN 58 | RG-2 fence display (honesty; REV 4 — the retired location… | [TAGGED] | stands |
| C122 | LEAN 64 | No existence or LOCATION claim is made for e ≥ 2 lifts: th… | [TAGGED] | stands |
| C123 | LEAN 69 | The compiled declarations quantify over their carrier PRED… | [TAGGED] | stands |
| C124 | NOTE 442 [REV 9] | **Non-vacuity of the carriers (… REV 8; [RC] debt DISCHARGED at REV 9…).** On [NR] the quantified predicates are inhabited on file… | [TAGGED] | NEW REV 9 (per-class, witnesses named; successor of C060) |
| C125 | NOTE 447 [REV 9] | So the [NR] carrier corollaries are not statements over empty carriers. | [TAGGED] | NEW REV 9 (successor of C061; [NR]-scoped) |
| C126 | NOTE 448 [REV 9] | On [RC] the REV-8 UNWITNESSED-ON-FILE debt is DISCHARGED [REV 9]: `H1/RCWitness.lean`… both recentering corollaries FIRE… | [TAGGED] | NEW REV 9 (the witness citation: H₃rc, commit 3fd9caf, Lean-core) |
| C127 | NOTE 457 [REV 9] | Per the pass-7 enumeration of the affected compiled claims: `landingKey_recentering_K0` — witnessed… `readsOf_landing_K0`… residual… | [TAGGED] | NEW REV 9 (per-decl display; the readsOf recentering-branch residual displayed, not papered over) |
| C128 | NOTE 464 [REV 9] | The [RC] legs of Theorem LIFT-CONF's compiled perimeter are hereby NON-VACUOUS; the witnessed scope is unchanged… | [TAGGED] | NEW REV 9 (scope fence: synthetic (1,1) only; REC-SL/REC-PARAM/GRADED-READ/SITE-EXH open) |
| C129 | NOTE 316 [REV 9] | **[REV 9] REC-PARAM stratified — the compiled e-half + the node-level fiat layer**… * **e-half — COMPILED at its derivable scope.**… | [TAGGED] | NEW REV 9 (both hypotheses of `recParam_e_of_transition` displayed; g-half explicitly not claimed) |
| C130 | NOTE 329 [REV 9] | * **Node-level stratification (the hspecRec finding).** `Node.hspecRec`… is a STRUCTURE FIELD… | [STATUS-FREE] | NEW REV 9 (a definitional fact about the corpus structure field — cited, not a lift-status claim) |
| C131 | NOTE 332 [REV 9] | So BM2_TRACE's claim that a coherent history could record ν.g = 7 at a recentering node is FALSE at the node level… | [TAGGED] | NEW REV 9 (the correction display; scope split node-level vs stage/wiring-level explicit) |
| C132 | NOTE 335 [REV 9] | Whether engine recentering firings HAVE the species shape stays S-1/SITE-EXH-side. | [TAGGED] | NEW REV 9 (named-open conditional covering its full scope) |
| C133 | NOTE 337 [REV 9] | * **REC-PARAM's remaining open content is therefore PRECISELY:** the STAGE/WIRING-level g-half tie + removing the came-from-transition hypothesis on the e-half… | [TAGGED] | NEW REV 9 (the residual statement; nothing else of REC-PARAM open, REC-SL's other legs untouched) |
| C134 | NOTE 612 [REV 9] | **[REV 9] REC-PARAM stratified (see the §S2 [REV 9] display): the e-half is COMPILED at its came-from-a-non-recentering-transition scope…** | [TAGGED] | NEW REV 9 (the §S4 (i-c) pointer to the §S2 stratification; residual displayed in the same sentence) |
Net effect: the note emerges STRICTLY more conditional a fifth time (an
engine-wide universal retired; a universal application re-scoped with
SITE-EXH displayed; a dependency cell disambiguated to REC-PARAM-only; an
over-broad table row split with GRADED-READ/TR-3-ORD-X displayed; two
consumer lists widened; the Lean header's last undifferentiated
carrier sentence split per class). No compiled claim weakened; no new
obligation created (every fold is a display repair of already-priced
residue). Lean edits: COMMENT-ONLY; gate re-run green; diff verified
comment-only. The genre defense is now ENUMERATIVE, not judgment-based:
§S6 attack item 7 charges pass 6 to re-run the census grep above — any
status sentence absent from the table or misclassified there is an
automatic finding.

— H1_BM2 REVISION 7 (unit B-M2), pass-5 fold 2026-08-02, against H1_BM1
REVISION 3 (VERIFIED, 2-clean) and blueprint REV 3. Hostile passes: 5 run
(all CRITICAL, all folded; pass 5 folded by CENSUS). Clean counter stays
0/2; hostile pass 6 runs on this text, checking the census table first.

## REVISION 8 — the pass-6 fold record (2026-08-02): the census checked

Pass 6 (Codex, hostile; archived `BM2_pass6_2026-08-06.md`) ran the §S6
attack-item-7 charge — CHECK the REVISION-7 census table, don't re-hunt —
and returned **CRITICAL**: two census misclassifications (one of them the
recurring untagged-universal genre, in the LEAN HEADER again) plus one
bookkeeping reclass. Its POSITIVE verdicts, on record: (a) all seven
REVISION-7 repairs (C020/C021, C048/C049, C116, C010, C091, C096, the
consumer-list extensions) are SOUND; (b) the REC-PARAM-only adjudication
was independently RE-DERIVED (from `K0Conformant σ 1 1 Φ'`:
deg Φ' = m = e_i·f_i·m_i = e_i·g_i·m_i with REC-PARAM's e = g = 1 and
`Node.hψdeg`'s f = g — `IsReadLift` and the remaining REC-SL legs matter
only for the graded/KP-STEP route); (c) the three principal consumer lists
(GRADED-READ / REC-SL / SITE-EXH) are internally ALIGNED after the row
split. No keyword hit absent from the table was found.

### Finding-by-finding disposition

* **CRITICAL 1 (census row C112)** — *"'(K1)+(K2) for the fired lifts ride
  the OPEN graded … GRADED-READ' — 'the fired lifts' ranges across both
  classes while only GRADED-READ is attached locally. On [RC], obtaining
  (K1)+(K2) also requires REC-SL. Classification: [UNTAGGED-STATUS] /
  underconditioned universal. The same recurrent genre."* **ACCEPTED,
  folded** (Lean COMMENT-ONLY): the header clause is now per-class — on
  [NR] the open GRADED-READ; on [RC] GRADED-READ **and additionally the
  open recentering legs REC-SL**. Gate re-run green; diff comment-only;
  census row C112 reclassified [UNTAGGED→FIXED].
* **CRITICAL 2 (census rows C060/C061)** — *"'The quantified predicates
  are inhabited on file' / 'The theorem is not a statement over empty
  carriers' — marked [TAGGED] but neither carries [NR]/[RC], a §S0
  deferral, or a named-open conditional; the plural carrier claim is
  broader than its displayed evidence (the cited witnesses do not
  separately demonstrate a recentering [RC] carrier)."* **ACCEPTED, folded
  after the substantive check** (the check FIRST, per the fold charge): a
  corpus grep (2026-08-02) confirmed the cited witnesses are [NR]-only —
  `gate_readsOf_inert2` is an inert non-recentering `ReadsOf` run;
  `HK23_twoNodeGatePos`'s H₂ram is root → increment, both non-recentering.
  NO compiled [RC] antecedent witness exists on file: the only compiled
  `LandingKey` witnesses (`U31.landingKey_ν₀`, `HK57.sideReads0_landing`)
  are non-recentering; no coherent history has an INTERIOR recentering
  transition (`HK25_recGate.H₂rec` is recentering-ENDED — its recentering
  node is TERMINAL, so no [RC] antecedent of the compiled corollaries
  fires on it); no `ReadsOf` run contains a recentering read;
  `S10_recStage` gives `IsRecenteringCore` existence only under
  undischarged hypotheses. The §S3 paragraph is rewritten per class:
  **[NR] inhabited, witnesses named; [RC] inhabitation displayed
  UNWITNESSED-ON-FILE** — a small displayed debt. The BM2_TRACE worked
  example (BM2_TRACE_2026-08-02.md) proves the mathematical [RC] instance
  exists — EVIDENCE only; a compiled [RC] carrier witness is a candidate
  ONE-LEMMA follow-up unit. No claim of this note consumes [RC]
  inhabitation. Census rows C060/C061 reclassified [UNTAGGED→FIXED].
* **Finding 3 (census row C057)** — *"explicitly an external cited fact
  ('cited not re-proved'); the published rules support [STATUS-FREE], not
  [TAGGED]. Not itself an overclaim — the table classification is
  wrong."* **ACCEPTED**: row C057 reclassified [STATUS-FREE]; no text
  change.

Net effect: the note emerges STRICTLY more conditional a sixth time (the
Lean header's last underconditioned universal split per class; the
non-vacuity display re-scoped to its actual witnesses, with a NEW small
displayed debt — the [RC] inhabitation — surfaced rather than papered
over; one bookkeeping reclass). No compiled claim weakened; no compiled
statement touched (Lean edit COMMENT-ONLY, gate re-run green). Census
counts after REV 8: the 123 hits stand, with C112/C060/C061 moved to
[UNTAGGED→FIXED] (12 FIXED rows total) and C057 moved to [STATUS-FREE]
(34); the REV-8 §S3 rewrite adds sentences that are [TAGGED] by
construction (every one carries [NR]/[RC] or the UNWITNESSED-ON-FILE
display).

— H1_BM2 REVISION 8 (unit B-M2), pass-6 fold 2026-08-02, against H1_BM1
REVISION 3 (VERIFIED, 2-clean) and blueprint REV 3. Hostile passes: 6 run
(all CRITICAL, all folded). Clean counter stays 0/2; hostile pass 7 runs
on this text (census re-check + the REV-8 paragraphs).

## REVISION 9 — the pass-7 + witness-unit fold record (2026-08-02): three streams folded

This revision folds THREE input streams into one text. Stream-by-stream
disposition:

**STREAM 1 — hostile pass 7** (Codex, archived `BM2_pass7_2026-08-06.md`;
verdict **GAPS-ONLY**, no critical defect — the first non-CRITICAL pass of
the arc; the REVISION-8 fixes verified: *"C112: correctly split… C060/C061:
correctly split… C057: correctly reclassified"*). Two gaps:

* **GAP 1** — *"the debt does not enumerate which compiled claims currently
  lack an on-file [RC] instance: `landingKey_recentering_K0`,
  `historyCoherent_recentering_K0`, the recentering branch of
  `readsOf_landing_K0`. Thus the [RC] synthetic-(1,1) claims in §S0, §S2,
  and §S5 are valid implication theorems but presently unwitnessed over
  compiled carrier instances."* **ACCEPTED — and DISCHARGED rather than
  displayed:** stream 2's compiled witness landed between pass 7 and this
  fold, so the enumeration demand is satisfied BY CITATION (§S2 non-vacuity
  display, rewritten): the three affected decls are named per pass 7's
  list, the first two now FIRE on the compiled interior-recentering
  history H₃rc (commit 3fd9caf), and the `readsOf_landing_K0` recentering
  branch's remaining antecedent gap (no compiled `ReadsOf` run containing
  a recentering read) is displayed as the honest residual. The §S0
  synthetic-(1,1) row, the §S2 case-(b) compiled leg, and the §S5 census
  note now carry the NON-VACUOUS/witnessed annotation.
* **GAP 2** — *"the census is stale after the Revision 8 rewrite… the
  updated total is therefore 127 hits, not 123. Four new hits are absent
  from the table."* **ACCEPTED, folded by refresh** (below): the REV-8
  paragraph (rewritten again at this revision) and all REV-9 additions are
  enumerated as table rows C124–C134; the totals paragraph below is the
  current authority.

**STREAM 2 — the [RC]-witness unit** (commit 3fd9caf; ledger entry in
`BRIDGE_ADJUDICATIONS_2026-07-30.md`; docstrings in the two new files).
FOLDED as status changes in the HONEST-UPGRADE direction only:
`lean/LeanUrat/Scaffold/HDischarge/H1/RCWitness.lean` compiles the first
coherent history with an INTERIOR recentering transition (`H₃rc`,
`coherent_H₃rc`) and the first recentering-species `LandingKey`
(`landingKey_ν₁rc`); BOTH recentering corollaries FIRE
(`rc_landingKey_gate`, `rc_historyCoherent_gate`, each concluding
`K0Conformant σ₁rec 1 1 σ₂rc.Φ`) — the [RC] legs are NON-VACUOUS,
Lean-core axioms. `lean/LeanUrat/Scaffold/HDischarge/H1/RecParamE.lean`
proves `recParam_e_of_transition` — the REC-PARAM e-half at its derivable
scope (interior recentering read whose stage came from a NON-RECENTERING
transition ⊢ ν.e = 1; the g-half not claimed). Folded at: the §S2
non-vacuity display (the debt discharge), the §S2 [REV 9] REC-PARAM
stratification block, §S4 (i-c), §S0, §S5. NO obligation is closed beyond
what the compiled statements give: REC-PARAM's residual is displayed
exactly (the wiring g-half + the e-half's came-from-transition
hypothesis); REC-SL's other legs, GRADED-READ, and SITE-EXH are untouched.

**STREAM 3 — the hspecRec finding** (the witness unit's ledger entry).
FOLDED as a bracket-correction + stratification: BM2_TRACE's claim that a
coherent history could record ν.g = 7 at a recentering node is FALSE —
`Node.hspecRec` (`MovesC/Defs.lean`) is a structure field forcing
species = recentering → e = 1 ∧ g = 1 at the NODE level, so the "tied
NOWHERE" verdict survives ONLY at the stage/wiring level for the g-half;
the node-level pair is (1, 1) BY STRUCTURE FIELD (the fiat layer), and the
compiled `recParam_e_of_transition` is the non-fiat wiring derivation of
the e-half. Both quotes of the trace's "g-half tied NOWHERE" (the §S2
[REV 5] wiring note; the REVISION 5 record) are bracket-corrected in
place. BM2_TRACE_2026-08-02.md itself is NOT edited — it is a dated
record; its correction is recorded here and in the ledger.

### Census refresh (the pass-7 GAP-2 fold)

Method: the REVISION-7 keyword rule re-implemented and re-run over the
CURRENT text (§SC–§S6 + the Lean module doc block, `H1/Conformance.lean`
lines 1–75; implementation fresh at this fold). **Result: 121 NOTE hits +
13 LEAN hits = 134 hits, ZERO [UNTAGGED-STATUS].** Reconciliation with the
published figures: pass 7 certified 127 on the REV-8 text; this fold's
implementation gives 128 on that same text (a ±1 segmentation variance at
the REV-8 paragraph — sentence-boundary placement, not coverage; every
variant boundary falls inside already-classified table regions). The
REV-9 delta over REV-8 under ONE fixed implementation: the REV-8
non-vacuity paragraph's hits are REPLACED count-neutrally by rows
C124–C128, and the REC-PARAM stratification + §S4 pointer add rows
C129–C134 (+6). Table state: rows C001–C123 stand (C041/C098 annotated —
same sentences edited, still [TAGGED]; C060/C061 marked superseded by
C124–C128); rows C124–C134 appended, line numbers in REVISION-9 text,
classifications: 10 [TAGGED] + 1 [STATUS-FREE] (C130, a definitional
corpus fact). Every REV-9 sentence passes the standing rule (each carries
[NR]/[RC], a §S0 deferral, or a named-open conditional covering its full
scope).

Net effect: the note moves in the HONEST-UPGRADE direction for the first
time in the arc — a displayed debt is discharged by a compiled witness,
a sub-obligation gains a compiled half at displayed scope, and an
EVIDENCE-layer overstatement (the trace's g-half quote) is corrected —
while every remaining conditionality (REC-PARAM residual, REC-SL,
GRADED-READ, SITE-EXH, S-1, TR-3-ORD-X) stays displayed. No Lean file is
edited at this revision (`Conformance.lean` untouched; the witness unit's
files landed at commit 3fd9caf with their own green gates and in-file
`#print axioms` = Lean-core).

— H1_BM2 REVISION 9 (unit B-M2), pass-7 + witness-unit fold 2026-08-02,
against H1_BM1 REVISION 3 (VERIFIED, 2-clean) and blueprint REV 3. Hostile
passes: 7 run (6 CRITICAL folded; pass 7 GAPS-ONLY, folded here). Clean
counter stays 0/2 — a GAPS-ONLY pass with folds resets nothing; hostile
pass 8 runs on this text (census re-check per §S6 item 7 + the REV-9
paragraphs + the witness citations against the compiled files).

## REVISION 10 — the pass-8 fold record (2026-08-02): the transport lemma

Pass 8 (Codex, hostile; archived `BM2_pass8_2026-08-06.md`) returned
**CRITICAL** (2 critical + 1 automatic census finding + 1 positive audit)
— for the first time in the arc, one critical ran in the CONSERVATIVE
direction: the note was under-claiming a row its own cited structure field
already supplies. Asymmetry rule honored throughout this fold: the
upgrades cite compiled declarations; the scopings/downgrades need none.

### Finding-by-finding disposition

* **CRITICAL 1** — *"`hspecRec` changes the target instead of stratifying
  the original REC-PARAM obligation. Original REC-PARAM is explicitly:
  'the identification of the node's actual (e_i, g_i) with … (1, 1)'. But
  the note also correctly records: '`Node.hspecRec` … forcing
  species = recentering → e = 1 ∧ g = 1'. Therefore the original
  node-level REC-PARAM proposition is already supplied by a compiled
  structure field at every species-recentering node. The remaining
  'stage/wiring-level g-half tie' and non-fiat provenance are stronger,
  different obligations. They may remain open, but they cannot coherently
  remain the reason that §S0 says actual-pair (K0) on [RC] is 'NOT proved
  — awaits REC-PARAM.' Synthetic `K0Conformant σ 1 1 Φ'` can be
  transported to the node's actual pair using `hspecRec`. … The note
  avoids smuggling node fiat into the wiring residual, but instead
  smuggles the stronger wiring requirement back into the weaker node-level
  status row. This breaks S0/body/Lean consistency."* **ACCEPTED, folded
  as a COMPILED upgrade + a restructure.** (1) THE TRANSPORT LEMMA is
  compiled — `k0_actual_of_recentering` (`H1/RecParamE.lean`, Lean-core,
  sorry-free, statement verbatim:
  `ν.species = ReadSpecies.recentering → K0Conformant ν.σ 1 1 Φhat →
  K0Conformant ν.σ ν.g ν.e Φhat` — a 3-line rewrite along `hspecRec`'s
  two equalities), instantiated at both witnesses
  (`rc_landingKey_gate_actual`/`rc_historyCoherent_gate_actual`,
  `H1/RCWitness.lean`, in-file `#print axioms` = Lean-core). (2) The §S0
  actual-pair [RC] row upgraded to PROVED-VIA-TRANSPORT; OL-1(a) on [RC]
  PROVED outright (transport + `Node.hψdeg` — the §S2 sufficiency
  adjudication cashed); the DevBound [RC] degree identification SUPPLIED;
  the abstract-engine [RC] OL-1(a) row's REC-PARAM rider dropped (S-1 +
  SITE-EXH stand). (3) REC-PARAM RESTRUCTURED at §S2 case (b): the
  ORIGINAL node-level proposition SUPPLIED; the remaining STRONGER
  obligations renamed so no row gates on the wrong one — **REC-WIRE-G**
  (the stage/wiring-level g-half tie), **the e-half's
  transition-hypothesis removal** (compiled `recParam_e_of_transition`
  covers the transition case), and **REC-SL proper** (the
  `IsReadLift`/(SL-top)/(SL-dev) legs, gating the KP-STEP route only).
  §S4 (i-c) rewritten to the same structure; §SC, §S1, §S6 (i), and the
  Lean header/docstring of `Conformance.lean` (COMMENT-ONLY, gate re-run
  green, diff verified comment-only) brought into agreement.
* **CRITICAL 2** — *"the [RC] witness debt is called discharged too
  broadly. … the surrounding claims say 'the REV-8 UNWITNESSED-ON-FILE
  debt is DISCHARGED' and 'The [RC] legs of Theorem LIFT-CONF's compiled
  perimeter are hereby NON-VACUOUS'. Those umbrella statements include a
  still-unwitnessed compiled branch. … The defensible statement is that
  the two dedicated recentering corollaries are now non-vacuous, not the
  entire [RC] perimeter."* **ACCEPTED, folded (a scoping — no citation
  needed).** Both umbrella sentences re-scoped to the TWO DEDICATED
  recentering corollaries, in the §S2 non-vacuity display AND the
  `RCWitness.lean` header (comment-only); the `readsOf_landing_K0`
  recentering branch is displayed UNWITNESSED-ON-FILE (no compiled
  `ReadsOf` run contains a recentering read), not absorbed.
* **AUTOMATIC CENSUS FINDING 3** — *"the refreshed table cannot represent
  134 current hits. The table … contains only `123 − 2 + 11 = 132` active
  current-text rows, not 134."* **ACCEPTED — the TABLE was right, the
  published total was wrong.** Reconciliation under ONE fixed fresh
  implementation of the REVISION-7 method (re-run at this fold): the
  REV-9 text gives **119 NOTE + 13 LEAN = 132 hits** — exactly the
  pass-8 row arithmetic; the REV-9 record's "121 NOTE + 13 LEAN = 134"
  was a +2 NOTE-side overcount in that fold's implementation, not two
  missing rows. The census refresh below is the current authority.
* **Finding 4 (positive audit)** — *"Witness-citation audit otherwise
  passes"*: `landingKey_ν₁rc`, `H₃rc`, `coherent_H₃rc`,
  `rc_landingKey_gate`, `rc_historyCoherent_gate` verified on file at the
  stated synthetic scope; `RCWitness.lean` constructs no `ReadsOf` run
  (the displayed residual substantively correct);
  `recParam_e_of_transition`'s hypotheses and 3-part conclusion exactly
  as displayed, no g-half claimed. **RECORDED.** Its minor precision note
  — *"`rc_landingKey_gate` fires directly on `ν₁rc` and
  `landingKey_ν₁rc`, not on `H₃rc`; `ν₁rc` is merely also a node of
  `H₃rc`"* — **folded** at the §S0 synthetic row, the §S2 case-(b) and
  non-vacuity displays, §S5, and the `RCWitness.lean` header.

### Census refresh (the finding-3 fold; current authority)

Method: the REVISION-7 keyword rule, fresh implementation at this fold,
run over BOTH texts. REV-9 text: 119 NOTE + 13 LEAN = **132** (matches
the REV-9 table's 123 − 2 + 11 = 132 active rows; finding 3 resolved as
above). CURRENT (REV-10) text: **123 NOTE + 13 LEAN = 136 hits, ZERO
[UNTAGGED-STATUS].** Delta under the same implementation: 21 REV-9 hit
sentences retired/edited at this revision — rows C006, C007, C010 (the
§S0 actual-pair cell), C018, C019, C021, C039, C040, C042, C046, C049,
C068, C069, C070, C090, C091, C098, C099, C126, C128, C134 marked
SUPERSEDED-AT-REV-10 — and 25 successor sentences appended as rows
**C135–C159** (locations: §SC [RC] paragraph; §S0 rows 1/2, (K1)+(K2),
OL-1(a), DevBound, abstract-engine-OL-1(a); §S1 supplies split; §S2
case (b) transport display + RESTRUCTURE block + sufficiency-cashed
bracket + non-vacuity display; §S3 (a); §S4 (i-c) + (vi); §S5 census
note + [REV 10] gate sentence; §S6 (i)). Classifications: 24 [TAGGED]
(every one carries a [REV 10] mark with its compiled citation, an
[NR]/[RC] tag, or a named-open — REC-WIRE-G / REC-SL / GRADED-READ /
SITE-EXH / S-1 — covering its full scope) + 1 [STATUS-FREE] (the §S5
[REV 10] gate-record sentence, per the C100 precedent). Active row
arithmetic: 132 − 21 + 25 = **136** = the implementation's count.

Net effect: the note moves in the HONEST-UPGRADE direction a second
time, and for the first time on a VERIFIER's initiative — a compiled
3-line transport converts an over-conservative row into a real
discharge (OL-1(a) on [RC] now PROVED at the corpus face) — while the
two umbrella sentences NARROW to their witnessed scope and the census
arithmetic is repaired at its root (implementation totals now published
against the table arithmetic). Lean edits at this revision:
`H1/RecParamE.lean` (+ the transport lemma + `Conformance` import;
header updated), `H1/RCWitness.lean` (+ the two `*_gate_actual`
corollaries + `RecParamE` import; header re-scoped),
`H1/Conformance.lean` COMMENT-ONLY; all three `lake env lean` green,
zero `sorry`, in-file `#print axioms` = Lean-core; no existing statement
touched.

— H1_BM2 REVISION 10 (unit B-M2), pass-8 fold 2026-08-02, against H1_BM1
REVISION 3 (VERIFIED, 2-clean) and blueprint REV 3. Hostile passes: 8 run
(7 CRITICAL folded; pass 7 GAPS-ONLY). Clean counter stays 0/2; hostile
pass 9 runs on this text (census re-check per §S6 item 7 — the REVISION
10 totals paragraph is the authority — + the REV-10 upgrade citations
against the compiled files: every upgraded row must trace to
`k0_actual_of_recentering`/`rc_*_gate_actual`/`Node.hspecRec`).

## REVISION 11 — the pass-9 fold record (2026-08-02): the inverse-genre sync + the census row ledger

Pass 9 (Codex, hostile; archived `BM2_pass9_2026-08-06.md`) returned
**CRITICAL** with an INVERTED profile — for the first time in the arc,
EVERY defect is of the INVERSE genre: stale text UNDER-claiming relative
to the compiled REV-10 transport upgrade **[REV 12 bracket-correction —
the pass-10 GAP-2 fold: this profile summary OVERSTATED; GAP 6 was
UNDER-SPECIFICATION and CRITICAL 7 a MISSING AUDIT TRAIL, not stale
under-claiming — the finding-by-finding dispositions below stand]**. Its
findings 1–2 are POSITIVE
VALIDATIONS of the REVISION-10 mathematics, on record first:

* **Finding 1 (PASS) — transport argument order.** *"The explicit
  argument order is `(σ, g, e')`, while the degree semantics are
  `e'·g·deg` … `rw [he, hg]` correctly reduces the target to
  `K0Conformant ν.σ 1 1 Φhat`. There is no swapped display."*
  **RECORDED** — the `k0_actual_of_recentering` display order verified
  correct against the compiled definition.
* **Finding 2 (PASS) — the REV-10 upgrades follow.** *"The derivation is
  exact: … synthetic carrier theorem … `Node.hspecRec` … transport …
  `Node.hψdeg` … hence deg Φ' = e_i·f_i·m_i … applies propositionally to
  all three compiled [RC] carrier implications … It does not establish an
  engine-wide 'every [RC] firing' theorem; that still rides SITE-EXH. The
  DevBound degree-identification upgrade follows by the same
  calculation."* **RECORDED** — every REV-10 status upgrade independently
  re-derived and confirmed, at exactly the scoped perimeter the note
  displays (SITE-EXH untouched). The compiled face has now survived nine
  hostile passes untouched.

### Finding-by-finding disposition (the five defects)

* **CRITICAL 3** — *"the current headline still asserts the superseded
  REV-2 status: 'actual-pair (K0) on [NR] only; on [RC] synthetic (1, 1)
  only' … a present-tense current-unit claim … contradicts §S0 and the
  cited compiled lemma."* **ACCEPTED, folded.** The opening re-headline
  is SYNCED to the §S0 table: actual-pair (K0) PROVED on BOTH classes
  ([NR] compiled directly; [RC] via `k0_actual_of_recentering`,
  `H1/RecParamE.lean`), with the [NR]/[RC] distinction retained for the
  claims that still differ per class (REC-SL's `IsReadLift`/(SL) legs;
  the REC-WIRE-G + e-half wiring residuals).
* **CRITICAL 4** — *"stale RG-2 claims directly contradict the transport
  upgrade … Both are stale after `k0_actual_of_recentering`. Moreover,
  `Node.hspecRec` makes a species-recentering node with e ≥ 2
  impossible. These sentences simultaneously ignore the actual-pair
  transport and discuss an uninhabitable node shape."* **ACCEPTED,
  folded in BOTH files.** The note's §S2 RG-2 caveat and the Lean
  header's closing RG-2 sentence are REPLACED by the sharpened fact:
  `Node.hspecRec` (`MovesC/Defs.lean:454`, a structure field) forces
  e = 1 ∧ g = 1 at every species-recentering node, so an e ≥ 2
  species-recentering node CANNOT BE CONSTRUCTED — the e ≥ 2 concern at
  recentering reads is VACUOUS at the node level; the fence discussion
  re-points to where e ≥ 2 actually lives (non-recentering interior
  reads — cross-checked: the §S2 C-e exhibit already displays exactly
  that, the traced tower's interior e = 2 step being a NON-recentering
  read, and STANDS unchanged — and `ReadsOf` final reads). Lean edit
  COMMENT-ONLY; gate re-run green; diff verified comment-only (§S5
  [REV 11] gate sentence).
* **GAP 5** — *"one live REC-PARAM evidence paragraph retains the retired
  status: 'At-instance verification is EVIDENCE for the general claim;
  the obligation REMAINS OPEN' … its immediate subject is the actual
  pair (e, g) = (1, 1), now supplied universally … this one is not
  comparably fenced."* **ACCEPTED, folded.** The sentence now carries a
  [REV 11] fence: its "the obligation" is the RETIRED node-level
  REC-PARAM (supplied — structure field + compiled transport); the
  paragraph's at-instance evidence re-attaches to the RENAMED open
  residuals REC-WIRE-G + the e-half transition-hypothesis removal.
* **GAP 6** — *"REC-WIRE-G is named but not theorem-precise … not yet a
  falsifiable proposition."* **ACCEPTED, folded — the corpus
  determination is option (a) of the charge (a wiring carrier for g DOES
  exist).** Grep evidence (2026-08-02, this fold):
  * `Stage` (`Moves/Defs.lean:106–166`): fields e, h, s, t, Φ, w, wPrev,
    K, FQ, R + laws — **NO g field** (pass 9's observation confirmed);
  * `TransitionData` (`Moves/DefsT.lean:147–156`): `child_key`,
    `child_e : σ'.e = e'`, `child_h : σ'.h = h'`, `child_field`,
    `field_grow`, `child_base`, `child_wPrev`, `threshold` — **NO g
    clause**; the transition layer transports (e', h', z̄) only;
  * `OM/Classifier.lean`'s residual-degree data (`dr`,
    `rootResidual_natDegree_le`) is the CLASSIFIER-RUN face, not the
    Stage/History wiring this obligation lives on — not the carrier;
  * BUT the wiring carries g through the WIDTH LAW: `HistoryCoherent`'s
    per-step Dwidth conjunct pins `Dwidthᵢ₊₁ = νᵢ.childWidth`
    (`MovesC/Defs.lean`, `HistoryCoherent`; `Node.childWidth :=
    ν.e·ν.g·ν.Dwidth`, line 465), and the frame link
    `Node.hDwidth : Dwidth = σ.Φ.natDegree` (line 418) grounds both
    sides in STAGE data; `IsRecentering` (`Moves/Defs.lean:271`) gives
    `σ'.Φ = σ.Φ − tt` with the window `inC σ.Φ tt`, i.e. key degree
    preserved.
  REC-WIRE-G is therefore STATED as a falsifiable proposition with
  displayed antecedents (coherent history + interior species-recentering
  read) and conclusion (νᵢ.e·νᵢ.g = 1, hence g = 1, derived from
  transition data), the non-fiat requirement expressed by ROUTING (the
  degree content factors through a stage-layer kernel where `hspecRec`
  is unavailable by type), and a candidate discharge route DISPLAYED —
  full display at §S2 case (b)'s RESTRUCTURE bullet; §S4 (i-c) entry
  synced. NOT compiled; the obligation stays OPEN; no status row
  upgrades on the display. (Bonus, displayed: the same route, if
  compiled, would remove the e-half's `hprev` hypothesis.)
* **CRITICAL 7** — *"the advertised 25 successor census rows are absent
  … the claimed classification '24 [TAGGED] + 1 [STATUS-FREE]' and
  arithmetic '132 − 21 + 25 = 136' have no published row-level audit
  trail … an automatic census finding."* **ACCEPTED, folded — THE ROW
  LEDGER IS PUBLISHED BELOW,** enumerated over the CURRENT (REV-11)
  text, REVISION-11 sentences included.

### Census refresh (the CRITICAL-7 fold; current authority)

**Implementation validation.** The REVISION-7 method was re-implemented
fresh at this fold and VALIDATED against the record: on the REV-9 text it
reproduces **119 NOTE + 13 LEAN = 132** and on the REV-10 text
**123 NOTE + 13 LEAN = 136** — both published totals EXACT. This
implementation is the authority for the rows below.

**The REV-10 sentence delta, published (rows C135–C173).** Under the
fixed implementation the REV-9 → REV-10 delta is **35 hit sentences
retired/edited + 39 successors** (finer than the REV-10 record's row
bookkeeping of "21 + 25": the extra 14 retirements are sentences of rows
that fold treated as standing-annotated — their heads changed, so under
head-level accounting they too are superseded-with-successor). The 14
additionally-superseded rows, now marked: **C009, C011, C020, C035,
C038, C047, C048, C050, C075, C096, C101, C112, C116, C127** (joining
the 21 listed in the REVISION 10 record). Same total either way:
132 − 21 + 25 = 132 − 35 + 39 = **136**. The REV-10 record's
"24 [TAGGED] + 1 [STATUS-FREE]" is superseded by the per-row
classifications below (38 [TAGGED] + 1 [STATUS-FREE] over the 39).

**The REV-11 delta (rows C174–C184).** Retired/edited: **C065** (the §S2
RG-2 caveat, rewritten per CRITICAL 4 — two successors) and **C122**
(the Lean header's e ≥ 2 sentence, edited per CRITICAL 4 — successor +
two new sentences); the REC-WIRE-G display, the [REV 5] fence, and the
§S5 [REV 11] gate sentence add fresh hits. Delta: −2 rows + 11 rows.

**Totals (CURRENT text): 130 NOTE + 15 LEAN = 145 hits, ZERO
[UNTAGGED-STATUS].** Active-row arithmetic: 134 table rows C001–C134,
minus 39 superseded (C060/C061 at REV 9; the 21 + the 14 at REV 10;
C065/C122 at REV 11), plus the 50 rows below = 95 + 50 = **145** = the
implementation's count, exact. Classifications over the 50 new rows:
**46 [TAGGED] + 4 [STATUS-FREE]** (C170 the [REV 10] gate record and
C181 the [REV 11] gate record, per the C100 precedent; C175 the
REC-WIRE-G routing spec and C184 the e ≥ 2-location corpus fact, per the
C130 definitional precedent).

### THE ROW LEDGER (rows C135–C184; line numbers = REVISION-11 text)

| id | loc (line) | sentence (head) | class | disposition |
|---|---|---|---|---|
| C135 | NOTE 152 | On [RC] what is proved is monicity + the SYNTHETIC conclus… | [TAGGED] | REV-10 successor |
| C136 | NOTE 158 | What remains open on [RC]: the WIRING-provenance obligatio… | [TAGGED] | REV-10 successor; superseded REV 12 |
| C137 | NOTE 169 | Status by claim class — THE table (single source of status… | [TAGGED] | REV-10 successor |
| C138 | NOTE 173 | Open residue: WIRING provenance only — **REC-WIRE-G** + th… | [TAGGED] | REV-10 successor; superseded REV 12 |
| C139 | NOTE 191 | **What this note does NOT claim:** KP-STEP class membershi… | [TAGGED] | REV-10 successor; superseded REV 12 |
| C140 | NOTE 240 | On **[RC]** B-M2 supplies: monicity + synthetic-(1, 1) (K0… | [TAGGED] | REV-10 successor; superseded REV 12 |
| C141 | NOTE 245 | So of KP-STEP's three hypotheses this unit discharges exac… | [TAGGED] | REV-10 successor |
| C142 | NOTE 247 | No KP-STEP application is established by this unit on EITH… | [TAGGED] | REV-10 successor |
| C143 | NOTE 320 | **Case (b), recenterings — class [RC]: a separate, DISPLAY… | [TAGGED] | REV-10 successor |
| C144 | NOTE 329 | PROVED (compiled) [REV 10]: (K0) at the node's ACTUAL pair… | [TAGGED] | REV-10 successor |
| C145 | NOTE 339 | **[REV 5]** verified at the worked instance (BM2_TRACE_202… | [TAGGED] | REV-10 successor |
| C146 | NOTE 345 | **[REV 3 — the pass-2 CRITICAL fold: the parameter identif… | [TAGGED] | REV-10 successor |
| C147 | NOTE 352 | Displayed as a sub-obligation inside REC-SL: REC-PARAM (su… | [TAGGED] | REV-10 successor |
| C148 | NOTE 370 | They are now separated and named: * **The ORIGINAL node-le… | [TAGGED] | REV-10 successor |
| C149 | NOTE 422 | * **The e-half's transition-hypothesis removal (open):** t… | [TAGGED] | REV-10 successor; superseded REV 12 |
| C150 | NOTE 427 | * **REC-SL proper (open):** the `IsReadLift`-at-(1, 1) for… | [TAGGED] | REV-10 successor |
| C151 | NOTE 433 | Whether engine recentering firings HAVE the species shape … | [TAGGED] | REV-10 successor |
| C152 | NOTE 486 | **[REV 10: these two residuals are now NAMED — REC-WIRE-G … | [TAGGED] | REV-10 successor |
| C153 | NOTE 506 | **[REV 10 — the adjudication CASHED:** the equalities are … | [TAGGED] | REV-10 successor |
| C154 | NOTE 512 | Consequently: **every lift fired on an [NR] carrier at an … | [TAGGED] | REV-10 successor; superseded REV 12 |
| C155 | NOTE 524 | KP-STEP's application to any RECORDED-CARRIER lift is ther… | [TAGGED] | REV-10 successor |
| C156 | NOTE 529 | At order ≤ 1 the proved **[NR]** (K0)+shape face is consis… | [TAGGED] | REV-10 successor |
| C157 | NOTE 605 | On **[RC]** the REV-8 UNWITNESSED-ON-FILE debt is **DISCHA… | [TAGGED] | REV-10 successor |
| C158 | NOTE 620 | Per the pass-7 enumeration of the affected compiled claims… | [TAGGED] | REV-10 successor |
| C159 | NOTE 627 | The NON-VACUITY claim therefore scopes to the TWO DEDICATE… | [TAGGED] | REV-10 successor; superseded REV 12 |
| C160 | NOTE 681 | * **(a) — status per the §S0 table: PROVED on [NR]; PROVED… | [TAGGED] | REV-10 successor |
| C161 | NOTE 686 | On **[RC]** OL-1(a) is now PROVED [REV 10 — a REAL dischar… | [TAGGED] | REV-10 successor |
| C162 | NOTE 694 | `IsReadLift` and the remaining REC-SL legs were adjudicate… | [TAGGED] | REV-10 successor; superseded REV 12 |
| C163 | NOTE 712 | * **(b) — CONDITIONAL, seams displayed by name (§S0 table)… | [TAGGED] | REV-10 successor |
| C164 | NOTE 778 | * **(i-c) REC-SL — the recentering (SL) legs, OPEN (new at… | [TAGGED] | REV-10 successor |
| C165 | NOTE 783 | Owed under this namespace are EXACTLY, by name: (1) **REC-… | [TAGGED] | REV-10 successor; superseded REV 12 |
| C166 | NOTE 803 | **Consumers blocked on the REMAINING legs:** KP-STEP appli… | [TAGGED] | REV-10 successor |
| C167 | NOTE 808 | **DISCHARGED from this consumer list at REV 10 (each by th… | [TAGGED] | REV-10 successor |
| C168 | NOTE 841 | * **(vi) DevBound (Lean row) does NOT retire yet.** `DevBo… | [TAGGED] | REV-10 successor |
| C169 | NOTE 875 | Note the two `*_recentering_K0` corollaries prove, IN THIS… | [TAGGED] | REV-10 successor; superseded REV 12 |
| C170 | NOTE 908 | **[REV 10] gates:** `Conformance.lean` re-run green after … | [STATUS-FREE] | REV-10 successor |
| C171 | NOTE 922 | Honesty ledger and attack list **(i) Proved vs consumed vs… | [TAGGED] | REV-10 successor; superseded REV 12 |
| C172 | LEAN 3 | Unit note: `lean/notes/openmath/H1_BM2_2026-08-06.md` (The… | [TAGGED] | REV-10 successor; superseded REV 13 (its header sentence said REC-WIRE-G + the e-half removal "stay open" — the stale-doc leak the REV-12 census missed; successor C205) |
| C173 | LEAN 32 | * `isNodeLift_K0` / `landingKey_K0` / `landingKey_recenter… | [TAGGED] | REV-10 successor |
| C174 | NOTE 398 | For every history H with `HistoryCoherent H` and every i w… | [TAGGED] | NEW REV 11; superseded REV 12 |
| C175 | NOTE 402 | The non-fiat requirement is expressed by ROUTING, not as a… | [STATUS-FREE] | NEW REV 11 |
| C176 | NOTE 409 | Candidate discharge route (DISPLAYED, not compiled — the o… | [TAGGED] | NEW REV 11; superseded REV 12 |
| C177 | NOTE 418 | If compiled, this route would ALSO deliver the e-half at i… | [TAGGED] | NEW REV 11; superseded REV 12 |
| C178 | NOTE 440 | At-instance verification is EVIDENCE for the general claim… | [TAGGED] | NEW REV 11; superseded REV 12 |
| C179 | NOTE 652 | Caveat (REV 4; REWRITTEN at REV 11 — the pass-9 CRITICAL-4… | [TAGGED] | NEW REV 11 |
| C180 | NOTE 661 | Where e ≥ 2 ACTUALLY lives: NON-RECENTERING interior reads… | [TAGGED] | NEW REV 11 |
| C181 | NOTE 917 | **[REV 11] gate:** `Conformance.lean` re-run green after t… | [STATUS-FREE] | NEW REV 11 |
| C182 | LEAN 72 | No existence or LOCATION claim is made for e ≥ 2 lifts: th… | [TAGGED] | NEW REV 11 |
| C183 | LEAN 76 | An e ≥ 2 RECENTERING read is VACUOUS at the node level [RE… | [TAGGED] | NEW REV 11 |
| C184 | LEAN 82 | e ≥ 2 lives only at NON-RECENTERING reads: interior ones a… | [STATUS-FREE] | NEW REV 11 |

Net effect: the note moves in the HONEST-UPGRADE direction a third time,
this time purely as TEXT SYNC — no compiled object changed, no status
row moved except toward what the compiled corpus already gives; the one
remaining under-specified residual (REC-WIRE-G) is now a falsifiable
proposition with a displayed candidate route; and the census audit trail
demanded by pass 9 is published in full. Lean edits at this revision:
`Conformance.lean` COMMENT-ONLY (gate re-run green, diff verified
comment-only, zero `sorry`); `RecParamE.lean`/`RCWitness.lean` untouched.

— H1_BM2 REVISION 11 (unit B-M2), pass-9 fold 2026-08-02, against H1_BM1
REVISION 3 (VERIFIED, 2-clean) and blueprint REV 3. Hostile passes: 9 run
(8 CRITICAL folded; pass 7 GAPS-ONLY; pass 9's findings 1–2 = the first
full mathematical validation of the REV-10 upgrades). Clean counter stays
0/2; hostile pass 10 runs on this text (census re-check per §S6 item 7 —
the REVISION 11 totals paragraph and ROW LEDGER above are the authority —
+ the REC-WIRE-G statement against the quoted corpus decls
`Node.childWidth`/`Node.hDwidth`/`IsRecentering`/`HistoryCoherent`).

## REVISION 12 — the pass-10 fold record (2026-08-02): REC-WIRE-G COMPILED

Pass 10 (Codex, hostile; archived `BM2_pass10_2026-08-06.md`) returned
**CRITICAL** (2 criticals + 2 gaps + 2 explicit CLEAN verdicts). The fold
is the first in the arc whose center of mass is NEW COMPILED MATHEMATICS
rather than text sync: the pass-10 GAP-1 analysis certified the REC-WIRE-G
candidate route sound ("the candidate route is not false, and the
stage-layer kernel genuinely cannot invoke `Node.hspecRec` because no
`Node` occurs in its type") while faulting the REVISION-11 display for not
ENCODING the routing; this revision COMPILES the route, making the routing
question moot.

### The compiled upgrade (`H1/RecParamE.lean` + `H1/RCWitness.lean`)

* **`recentering_natDegree_eq`** — THE STAGE KERNEL:
  `IsRecentering σ σ' cc tt → σ'.Φ.natDegree = σ.Φ.natDegree`, proved from
  `IsRecentering`'s own conjuncts `σ'.Φ = σ.Φ − tt` + `inC σ.Φ tt` by
  lower-degree subtraction (`Polynomial.degree_sub_eq_left_of_degree_lt`).
  No `Node` occurs in the type: `hspecRec` is unusable BY TYPE — the
  pass-10 provenance requirement, enforced at the only layer Lean can
  check it.
* **`recParam_eg_of_recentering`** — THE WIRING COROLLARY (the REC-WIRE-G
  proposition, verbatim from the REVISION-11 display, strengthened to the
  conjunction): coherent history + interior recentering read
  (`i + 1 < len`, species recentering at i) ⟹ `νᵢ.e = 1 ∧ νᵢ.g = 1`.
  Route exactly as displayed at REV 11: the recentering coherence leg
  fires `IsRecenteringCore`, its `base` feeds the kernel; the width
  clause `Dwidthᵢ₊₁ = νᵢ.childWidth` + both `hDwidth` frame links +
  `Stage.hdeg` give `νᵢ.e·νᵢ.g·d = d`, `d ≥ 1`, hence `νᵢ.e·νᵢ.g = 1`,
  hence both = 1. Every hypothesis from `HistoryCoherent`; `hspecRec`
  nowhere invoked.
* **`rc_wireG_gate`** (`H1/RCWitness.lean`) — the corollary FIRES at
  H₃rc's interior recentering node 1 (non-vacuity).
* Gates: `lake env lean` clean on both files; in-file `#print axioms` =
  Lean-core on all three new declarations (+ the two pre-existing ones);
  zero `sorry`; NO existing statement touched (`Conformance.lean`
  untouched at this revision).
* Consequences: **REC-WIRE-G DISCHARGED**; **the e-half
  transition-hypothesis removal DISCHARGED BY SUBSUMPTION** (the corollary
  yields `νᵢ.e = 1` with no `hprev` — `recParam_e_of_transition`'s
  clause (c) at every interior recentering read; its clause (b) stage-node
  tie stays transition-case content, gating nothing). §S4 (i-c)'s owed
  list shrinks to REC-SL proper. What the corollary does NOT touch:
  REC-SL proper, GRADED-READ, SITE-EXH/S-1 (whether engine firings have
  the recorded coherent-history shape), all open as displayed.

### Finding-by-finding disposition

* **CRITICAL 1** — *"The current §S2 theorem header still says … 'its
  compiled conclusions are at the synthetic pair (1, 1), not at
  (νᵢ.e, νᵢ.g)' … neither historical nor fenced … exactly the pass-9
  inverse genre the REVISION-11 sweep claimed to eliminate."*
  **ACCEPTED, folded.** The header clause is REPLACED: case (b)'s
  compiled conclusions hold at the synthetic pair AND at the actual pair
  — via the [REV 10] transport and the [REV 12] wiring derivation
  (successor row C185; the census-audit lesson that C030 was the row the
  REV-11 ledger failed to expose is recorded at C030's disposition).
* **CRITICAL 2** — *"the §S2 evidence paragraph still says 'the corpus
  never TIES νᵢ.e/νᵢ.g at recentering legs' … 'the g-half is tied
  NOWHERE … nowhere at the STAGE/WIRING level' … conflicts with
  REVISION 11's new determination … The same stale assertion survives in
  `RecParamE.lean` … The narrower statement … is sound."*
  **ACCEPTED, folded in BOTH files.** The wiring note is REWRITTEN to
  the accurate narrow form (no `Stage` FIELD directly records g; the
  e-half's `child_e` route has no direct g-analogue; the tie runs
  through the coherence width law — now COMPILED); the `RecParamE.lean`
  header's "tied NOWHERE … stands AT THE STAGE/WIRING LEVEL" bullet is
  replaced by the same narrow form + the compiled citations (successor
  C186; C041 superseded).
* **GAP 1** — *"the displayed top-level proposition … has an immediate
  fiat proof from `ν.hspecRec` … 'must factor through' remain[s] prose
  constraints on the proof, not propositions Lean can check … Moreover,
  'transition data' is imprecise."* **ACCEPTED, folded by COMPILATION**
  (the display above): the kernel now EXISTS as a compiled declaration
  whose type excludes `hspecRec`, and the corollary's compiled proof
  term consumes the kernel + the coherence width clause — the routing is
  no longer a prose constraint but the actual on-disk derivation; the
  imprecise "transition data" phrasing is replaced everywhere by the
  exact carriers (`IsRecenteringCore.base` + the width clause +
  `hDwidth` + `Stage.hdeg`).
* **GAP 2** — *"'all five defects are STALE TEXT UNDER-CLAIMING' … not
  accurate for pass-9 GAP 6 (under-specification of REC-WIRE-G) or
  Critical 7 (missing audit trail)."* **ACCEPTED, folded.** Both the
  headline's REVISION-11 summary and the REVISION 11 record's profile
  paragraph carry the [REV 12] bracket-correction; the
  finding-by-finding dispositions stand as written.
* **CLEAN verdict (uninhabitability replacement)** — *"The narrow
  assertion is sound … The replacement generally preserves the layer
  boundary … Classification: CLEAN, with the stated layer-sensitive
  reading."* **RECORDED** — the REV-11 CRITICAL-4 replacement (the
  `Node.hspecRec` vacuity fact) is verified sound; its layer-sensitive
  reading ("reads are represented by `Node`s"; no claim about raw stage
  transitions; SITE-EXH open and displayed) is exactly the note's.
* **CLEAN verdict (census audit)** — *"The published arithmetic is
  exact … 134 rows … 39 … 95 … 50 … 145 … Spot audit found no missing ID
  or arithmetic discrepancy."* **RECORDED**, with the audit's honest
  sting kept on record: *"The census nevertheless failed its larger
  purpose to expose the active stale assertion in the old C030
  sentence; [TAGGED] does not imply semantically current."* C030's
  disposition now records this. C175/C184's debatable [STATUS-FREE]
  classes were adjudicated non-failures by the pass; both stand.

### Census upkeep (head-level delta; the implementation re-run is pass 11's §S6-item-7 check)

Superseded at REV 12 (17 rows, each annotated at its table):
**C030, C041** (the two criticals) + **C136, C138, C139, C140, C149,
C154, C159, C162, C165, C169, C171** (REV-10-successor rows whose heads
asserted the wiring residuals open) + **C174, C176, C177, C178**
(the REV-11 REC-WIRE-G display rows, superseded by the compiled form).
Successor/new rows **C185–C204** (20; line numbers = REVISION-12 text):

| id | loc | sentence (head) | class |
|---|---|---|---|
| C185 | §S2 theorem header | its compiled conclusions hold at the synthetic pair (1, 1) AND at the node's actual pair… | [TAGGED] |
| C186 | §S2 wiring note | Wiring note (same unit; REWRITTEN at REV 12 — the pass-10 CRITICAL-2 fold…): the accurate narrow form… | [TAGGED] |
| C187 | §S2 RESTRUCTURE | REC-WIRE-G — DISCHARGED at REV 12… THE STAGE KERNEL — `recentering_natDegree_eq`… | [TAGGED] |
| C188 | §S2 RESTRUCTURE | THE WIRING COROLLARY — `recParam_eg_of_recentering`… | [TAGGED] |
| C189 | §S2 RESTRUCTURE | Non-vacuity gate: `rc_wireG_gate`… Scope note: the derivation covers RECORDED coherent histories… | [TAGGED] |
| C190 | §S2 RESTRUCTURE | The e-half's transition-hypothesis removal — DISCHARGED at REV 12, by the same corollary… | [TAGGED] |
| C191 | §S2 RESTRUCTURE | The "not merely priced at (1, 1) by fiat" clause of the display is now SATISFIED [REV 12]… | [TAGGED] |
| C192 | §SC [RC] | AND [REV 12] the pair's WIRING provenance is COMPILED TOO… | [TAGGED] |
| C193 | §S0 table | AND PROVED VIA WIRING [REV 12]… REC-WIRE-G + the e-half transition-hypothesis removal: DISCHARGED… | [TAGGED] |
| C194 | §S0 does-NOT-claim | that engine recentering FIRINGS have the recorded species/coherence shape… | [TAGGED] |
| C195 | §S1 | and [REV 12] independently by the compiled WIRING derivation… | [TAGGED] |
| C196 | §S2 [REV 5] fence | [REV 12]: the residuals this paragraph's at-instance evidence was re-attached to … are now themselves DISCHARGED… | [TAGGED] |
| C197 | §S2 stratification | [REV 12: both named wiring residuals are now DISCHARGED…] | [TAGGED] |
| C198 | §S2 consequently | the pair's wiring provenance compiled [REV 12] — `recParam_eg_of_recentering`… | [TAGGED] |
| C199 | §S2 non-vacuity | [REV 12]: REC-WIRE-G and the e-half removal are discharged … the wiring gate `rc_wireG_gate` adds a third fired corollary… | [TAGGED] |
| C200 | §S3 (a) | the pair's WIRING provenance was a provenance strengthening … and is compiled anyway [REV 12]… | [TAGGED] |
| C201 | §S4 (i-c) | DISCHARGED at REV 12 (compiled, `H1/RecParamE.lean`, Lean-core…): (2) REC-WIRE-G … (3) the e-half's transition-hypothesis removal… | [TAGGED] |
| C202 | §S5 | [REV 12] the wiring residuals REC-WIRE-G + the e-half removal are DISCHARGED… | [TAGGED] |
| C203 | §S5 | **[REV 12] gates (2026-08-02):** `lake env lean` clean on `H1/RecParamE.lean`… | [STATUS-FREE] |
| C204 | headline | **REVISION 12 (pass-10 fold, 2026-08-02) — REC-WIRE-G DISCHARGED.**… | [TAGGED] |

Provisional totals (head-level): 145 − 17 + 20 = **148 active rows,
ZERO untagged known**; the validated grep implementation re-run over the
REV-12 text is pass 11's standing §S6-item-7 charge (this fold's
bookkeeping is hand-enumerated over exactly the sentences it changed —
the C030 lesson says the re-run must also re-check UNCHANGED [TAGGED]
rows for semantic currency against the new compiled state).

Net effect: the note moves in the HONEST-UPGRADE direction a fourth
time, and for the first time by NEW COMPILED MATHEMATICS on the unit's
own obligation list: REC-WIRE-G (named at REV 10, stated theorem-precisely
at REV 11) is DISCHARGED as the kernel + corollary pair, the e-half
hypothesis-removal residual is subsumed, and §S4 (i-c) shrinks to REC-SL
proper. Lean edits at this revision: `RecParamE.lean` (2 new theorems +
header sync — the CRITICAL-2 echo removed), `RCWitness.lean` (1 new gate
+ header sync); `Conformance.lean` UNTOUCHED; no existing statement
changed anywhere; all gates green, Lean-core.

— H1_BM2 REVISION 12 (unit B-M2), pass-10 fold 2026-08-02, against H1_BM1
REVISION 3 (VERIFIED, 2-clean) and blueprint REV 3. Hostile passes: 10 run
(9 CRITICAL folded; pass 7 GAPS-ONLY; passes 9–10 progressively validated
the REV-10/11 mathematics before this fold compiled the REV-11 route).
Clean counter stays 0/2; hostile pass 11 runs on this text (census re-run
per §S6 item 7 with the C030 semantic-currency lesson; the new compiled
pair checked against the quoted corpus decls `IsRecentering`/
`IsRecenteringCore`/`HistoryCoherent`/`Node.childWidth`/`Node.hDwidth`/
`Stage.hdeg`; the REC-SL-proper display as the sole surviving (i-c) leg).

## REVISION 13 — the pass-11 fold record (2026-08-02): the stale-doc sweep + the consolidated-opens list + the census scope-hole closure

Hostile pass 11 (Codex, `BM2_pass11_2026-08-06.md`) returned **CRITICAL**,
but with the same INVERSE profile as passes 9–10: the mathematics VALIDATED
in full, all defects DOCUMENTATION-CURRENCY. Findings, quoted heads +
dispositions:

* **Finding 1 (CLEAN — the two new theorems)** — *"`recentering_natDegree_eq`
  uses the correct route … Constant edge cases are safe … does not introduce
  a constant-polynomial hole"* and *"`recParam_eg_of_recentering` has the
  correct indices … No off-by-one or vacuity defect is present"*, and the
  subsumption claim *"correct, with the stated limited meaning"* (clause (c),
  not (b)). **NO CHANGE NEEDED — both REV-12 theorems re-confirmed CLEAN
  mathematically.** This is the second model-diverse math-clean read of the
  REC-WIRE-G pair (after the pass-10 arithmetic-clean audit).
* **Finding 2 (CRITICAL — stale Lean doc sentences)** — *"REC-WIRE-G was not
  removed from every active consumer … Three live Lean documentation
  statements still call the discharged residual open"*: the `Conformance.lean`
  module header (*"the WIRING provenance REC-WIRE-G + the e-half
  transition-hypothesis removal stay open"*), the `K0Conformant` docstring
  (*"the wiring-provenance residuals stay open"*), and the
  `k0_actual_of_recentering` docstring in `RecParamE.lean`
  (*"(REC-WIRE-G; the e-half's transition-hypothesis removal) remain open"*).
  **ACCEPTED, folded — all three SYNCED to DISCHARGED** (citing
  `recentering_natDegree_eq` + `recParam_eg_of_recentering`; the e-half
  removal recorded as SUBSUMED). Both files re-run `lake env lean` clean,
  diffs COMMENT-ONLY, zero `sorry`, `#print axioms` Lean-core on all four
  RecParamE declarations.
* **Finding 3 (GAP — provenance wording)** — *"`recParam_eg_of_recentering`,
  no species field"* is inaccurate (*"the theorem explicitly assumes and uses
  `hrec : node.species = ReadSpecies.recentering`"*) and *"Every hypothesis
  comes from `HistoryCoherent`"* is too strong (*"`hi1` and `hrec` are
  separate explicit hypotheses"*). **ACCEPTED, folded:** §S2's "no species
  field" → "no use of `Node.hspecRec` (the species-pair fiat); it does take
  `hrec : species = recentering`"; the "Every hypothesis" sentence → the
  accurate form (`hi1`/`hrec` explicit; the substantive width/recentering
  laws from coherence).
* **Finding 4 (GAP — the remaining-open display)** — *"the remaining-open
  display is not exact everywhere requested … S4 does not include the
  remaining `readsOf` recentering-branch witness debt in a consolidated
  current list … The headline does not give the requested exact consolidated
  list either."* **ACCEPTED, folded:** ONE canonical **CONSOLIDATED
  CURRENT-OPENS LIST** added at the §S4 summary (GRADED-READ; REC-SL proper;
  SITE-EXH; the `readsOf_landing_K0` recentering-branch witness debt),
  matching §S0, and CITED verbatim from the headline.
* **Finding 5 (CLEAN — the REV-12 replacements)** — *"The §S2 wiring note and
  the `RecParamE.lean` header now accurately say that no `Stage` field
  directly records `g` … The defect is in additional stale siblings, not
  those two replacements."* **NO CHANGE NEEDED — the two REV-12 stale-sentence
  replacements re-confirmed CLEAN locally.**
* **Finding 6 (the census, semantic)** — *"C172 remains active even though its
  `Conformance.lean` header sentence still says REC-WIRE-G and the e-half
  removal 'stay open' … the 17-row delta is semantically incomplete"*, and
  *"the stale `K0Conformant` and `k0_actual_of_recentering` docstrings also
  escape the published module-doc-block census scope."* **ACCEPTED, folded:**
  C172 SUPERSEDED (successor C205); the census scope EXTENDED to ALL doc
  comments of the three H1 face files (scope-hole closed permanently); rows
  C205–C208 added below.

### Census upkeep (REV 13; head-level delta)

Superseded at REV 13 (1 row): **C172** (the Conformance module-header
sentence, its "stay open" head synced by Finding 2). Sub-sentence edits
WITHIN existing rows whose heads are unchanged (no row split, per head-level
accounting): **C188** (the `recParam_eg_of_recentering` corollary display —
the "Every hypothesis" sentence reworded, Finding 3) and **C191** (the "not
merely priced at (1, 1) by fiat" display — the "no species field" phrase
reworded, Finding 3). Successor/new rows **C205–C208**:

| id | loc | sentence (head) | class |
|---|---|---|---|
| C205 | `Conformance.lean` module header | …the WIRING provenance REC-WIRE-G is DISCHARGED [REV 12] by `recentering_natDegree_eq` + `recParam_eg_of_recentering` … which SUBSUMES the e-half transition-hypothesis removal… | [TAGGED] |
| C206 | `Conformance.lean` `K0Conformant` docstring [NEW SCOPE] | …the wiring provenance REC-WIRE-G is DISCHARGED [REV 12] via `recParam_eg_of_recentering` (subsuming the e-half removal); REC-SL proper alone stays open… | [TAGGED] |
| C207 | `RecParamE.lean` `k0_actual_of_recentering` docstring [NEW SCOPE] | …REC-WIRE-G (which SUBSUMES the e-half's transition-hypothesis removal) is not claimed by THIS lemma, but is DISCHARGED [REV 12] by the companion `recParam_eg_of_recentering`… | [TAGGED] |
| C208 | §S4 | **THE CONSOLIDATED CURRENT-OPENS LIST (REV 13 …)** … GRADED-READ; REC-SL proper; SITE-EXH; the `readsOf_landing_K0` recentering-branch witness debt… | [TAGGED] |

Provisional head-level totals: 148 − 1 (C172) + 4 (C205–C208) = **151 active
rows, ZERO untagged known**. The headline's REV-13 current-opens sentence is
OUT of census scope (pre-`## SC.`), so it takes no row. The full validated
grep re-implementation over the EXTENDED Lean scope (now including the
`RecParamE.lean`/`RCWitness.lean` unchanged docstrings, which become
first-class hits) is **pass 12's standing §S6-item-7 charge**; this fold's
bookkeeping is hand-enumerated over exactly the sentences it changed plus the
two newly-in-scope stale docstrings it repaired.

Net effect: the note moves in the HONEST-UPGRADE direction a FIFTH time —
this fold is pure documentation-currency (no compiled object changed, no
status row moved), syncing three stale Lean doc sentences to the REV-12
compiled DISCHARGE, publishing the single canonical current-opens list, and
closing the census scope hole that let docstrings escape. Lean edits at this
revision: `Conformance.lean` COMMENT-ONLY (module header + `K0Conformant`
docstring; `lake env lean` clean, diff comment-only, zero `sorry`);
`RecParamE.lean` COMMENT-ONLY (`k0_actual_of_recentering` docstring; `lake env
lean` clean, `#print axioms` Lean-core on all four declarations, zero
`sorry`); `RCWitness.lean` untouched; no existing statement changed anywhere.

— H1_BM2 REVISION 13 (unit B-M2), pass-11 fold 2026-08-02, against H1_BM1
REVISION 3 (VERIFIED, 2-clean) and blueprint REV 3. Hostile passes: 11 run
(9 CRITICAL folded; pass 7 GAPS-ONLY; passes 9–11 progressively validated the
REV-10/11/12 mathematics — pass 11 re-confirmed the REC-WIRE-G pair CLEAN,
finding only documentation-currency defects). Clean counter stays 0/2;
hostile pass 12 runs on this text (census re-run per §S6 item 7 over the
EXTENDED all-H1-docstring scope; the consolidated current-opens list checked
against §S0; the three synced Lean doc sentences checked against the compiled
DISCHARGE).
