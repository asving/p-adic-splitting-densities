# RECSL_F2 — the REC-SL supplier question (synthesis pass 5 F2, queue #5)

Attempt-grade unit, 2026-08-08. Charter: `docs/SYNTHESIS_PASS5_2026-08-08.md` F2 —
does the GRADED-READ discharge pattern (the compiled GR0b supplier layer + vacuity
scoping) transfer to **REC-SL proper**, B-M2's one untouched open (the
`IsReadLift`-at-(1,1)/(SL-top)/(SL-dev) digit legs at recentering reads,
`lean/notes/openmath/H1_BM2_2026-08-06.md` §S4 (i-c) leg (1))? F2's own warning was
the null hypothesis: digit laws are NOT graded identities — only the supplier layer
is shared.

Compiled artifact: `lean/LeanUrat/B2D/RECSL_suppliers.lean` — 9 theorems, builds
green under `lake env lean`, `#print axioms` = Lean-core
(`propext, Classical.choice, Quot.sound`) on all seven capstone declarations, zero
`sorry`, no existing file touched.

## HEADLINE VERDICT

**The supplier layer reaches 2.75 of the 3 legs; the residue is ONE sign bit,
compiled sharp.** (SL-top) is PROVED unconditionally from the recentering record.
(SL-dev)'s window/weight/nonzero/twist-position data are PROVED from GR0b's
`ReadHyps` pack. The (SL-dev) residual-DIGIT law is proved EQUIVALENT (at every
recentering record with `ReadHyps`) to the frame sign law `σ.R(−1) = −1` — which is
`StageCore.R_neg` content, corpus-recorded as UNDERIVABLE from the round-2 `Stage`
fields, and carried by NEITHER `ReadHyps` NOR `HistoryCoherent`'s transition
records. With that one pin as a displayed hypothesis (discharged by `StageCore`
wherever the frame has engine provenance), the full `IsReadLift`-at-(1,1) leg is
PROVED, single-node and history-level. So: **SUPPLIERS, with one compiled
obstruction pin** — the F2 warning materialized exactly once, as a sign.

## THE THREE LEGS, VERBATIM (the B-M2 note's displays)

From the §S0 table (`H1_BM2_2026-08-06.md:236–237`):

> | D8 stride shape / (SL-top) | DEFINITIONAL (`IsReadLift`'s displayed formula; D_g = 1 hard-wired) | **NOT established** — the `IsReadLift`-at-(1,1) form awaits **REC-SL** † |
> | stage-law (SL-dev) DATA (window/weight/twist-residual fields) | DEFINITIONAL fields of the firing predicate — a naked-data lift cannot fire at these sites | **NOT forced** — no weight/twist-residual data on the digit `−lift`; awaits **REC-SL** † |

From §S2 case (b), the OWED display (`:424–428`):

> OWED — **REC-SL** (open sub-member; owner: B-M2 continuation; consumer list at
> §S4 (i-c)): nothing compiled or displayed here establishes that Φ' is an
> `IsReadLift` at e = g = 1, or that its sole lower digit `−lift` satisfies the
> (SL-dev) weight and twist-residual obligations.

From the consolidated opens list, item 2 (`:964–965`):

> **REC-SL proper** — the `IsReadLift`-at-(1, 1) form + the (SL-top)/(SL-dev)
> digit obligations, gating the KP-STEP route only ((i-c) leg (1)).

## PER-LEG VERDICTS

| leg | verdict | Lean artifact |
|---|---|---|
| (SL-top): `Φ' = σ.Φ − lift` is the D8 stride form, top coeff 1, D_g = 1 | **PROVED-COMPILED, unconditional** (record-only: `hspecRec` + `IsRecentering` clause 8) | `recsl_slTop` |
| (SL-dev) DATA: digit `−lift` window / weight `h·(g−k)` / nonzero / twist position | **PROVED-COMPILED** from `ReadHyps` (GR0b-supplied at history level) | `recsl_slDev_data` |
| (SL-dev) residual DIGIT law: `σ.R(−lift) = C(ψ₀)·T^(−σ.t·wPrev(−lift))` | **OBSTRUCTED-WITH-DISPLAYED-LEMMA, sharp**: holds ⟺ `σ.R(−1) = −1` (the RECSL-SIGN pin below); everything but the sign is supplied | `recsl_residual_iff_sign` |
| `IsReadLift`-at-(1,1) (assembled) | **PROVED-COMPILED conditional on the ONE pin**; pin discharged by `StageCore` | `recsl_isNodeLift`, `recsl_isReadLift_11`, `recsl_isNodeLift_of_history`(`_stageCore`) |

## THE DERIVATIONS (informal; each step names its supplier)

Setting: ν a recentering read node (`ν.species = recentering`) with recorded
transition `IsRecentering ν.σ σ' ν.center ν.lift` (`HistoryCoherent`'s recentering
leg records exactly this via `IsRecenteringCore.base`). Write σ := ν.σ, c̃ := ν.center,
t := ν.lift. The record's clauses (Moves/Defs.lean:271): σ.e = 1 (clause 1 — CL5's
legality discovery, per the charter), σ'.e = 1, c̃ ≠ 0, `inC σ.Φ t`, t ≠ 0,
σ.w t = σ.w σ.Φ, σ.R t = C(c̃)·T⁰, σ'.Φ = σ.Φ − t, w/wPrev/K/FQ/weightSet/digPrime
unchanged. Node species laws: `hspecRec` (e = g = 1), `hspecRecCenter`
(ψ = z − c̃). ReadHyps at ν is GR0b-supplied by `readHyps_of_history_rec` at the
covered configuration (frame from a non-recentering landing; the GR0b
recentering-CHAIN fence stands unchanged here).

**(SL-top).** e = g = 1 (`hspecRec`), so the stride form reads
Φ' = σ.Φ^(1·1) + t₀·σ.Φ^(1·0) with the single digit t₀ := −t; the record's
σ'.Φ = σ.Φ − t IS that form. Top coefficient literally 1; D_g = 1 is the range
k < g = 1. Pure algebra — no ReadHyps, no history, no sign.

**(SL-dev) data, digit t₀ = −t at the sole slot k = 0.**
* nonzero: t ≠ 0 (clause 5), negation.
* window: `inC σ.Φ t` (clause 4) + `degree_neg`.
* marched weight: w(−t) = w(t) (`ResVal.w_neg`, a pack theorem from `hwmul`)
  = w(σ.Φ) (clause 6) = σ.h (`Stage.hwΦ`) = ν.h (`readHyps_h_eq` — the
  GR0b frame-to-read tie via `hscale`, i.e. `child_e`/`child_h` at history level).
  And h·(g−0) = ν.h at g = 1. ✓
* twist position: the (S5) forced position is −σ.t·wPrev(−t); the route-(a)
  frame pin `ReadHyps.hσt` (σ.t = 0 — GR34_DISPOSITION; history supplier
  `frame_level_of_history`) kills it: position 0, exactly the worked-instance
  value (BM2_TRACE "twist position 0"). ✓
* ψ-vanishing clause (∀k, ψ.coeff k = 0 → t_k = 0): at k = 0, `node_hψ0` — the
  GR0b anchored-residual pin, which at a recentering IS c̃ ≠ 0 (charter's
  "node_hψ0 at a recentering IS the c̃ ≠ 0 pin"); at k ≥ 1 the witness digit is 0. ✓

**(SL-dev) residual digit law — where the supplier layer STOPS.** Needed:
σ.R(−t) = C(ψ.coeff 0)·T^(−σ.t·wPrev(−t)) = C(−c̃)·T⁰ (position killed by `hσt`;
ψ.coeff 0 = −c̃ from `hspecRecCenter`). Supplied: σ.R(t) = C(c̃)·T⁰ (clause 7) and
σ.R(−t) = σ.R(−1)·σ.R(t) (`ResVal.R_neg`, from `hRmul`). So the law holds iff
σ.R(−1)·C(c̃) = C(−c̃), iff (cancel C(c̃), c̃ ≠ 0 — clause 3) **σ.R(−1) = −1**.
Both directions compiled: `recsl_residual_iff_sign`.

**Why the sign is genuinely outside the supplier layer** (not merely unproved
here): the round-2 `Stage` R-laws (`hRmul`/`hRadd`/`hRlt` — exactly what `ReadHyps`
re-exports) determine only σ.R(−1)² = 1 (`ResVal.R_neg_one_sq`, whose docstring
says "only THIS is needed downstream — never the sign"). The additive laws are
silent precisely in the CANCELLATION case (w(f+g) > w f = w g), and every
sign-pinning identity (e.g. (Φ−1) + 1 = Φ) is of that shape. The corpus already
adjudicated this: `R_neg : ∀ f, σ.R(−f) = −σ.R f` is one of "the THREE MISSING
D.1(b) TIE LAWS … true in every genuine graded model, UNDERIVABLE from the round-2
`Stage` fields — the root of the audits' pathological-stage findings"
(`Moves/DefsCore.lean`, `StageCore` docstring; the audit record is
`notes/MOVES_LEAN_SEMAUDIT1_2026-07-26.md` §"pathological non-mathematical
stages"). `TransitionCoreL` and `IsRecenteringCore` (all `HistoryCoherent`
records) bundle no `StageCore`, so the history layer cannot hand it over either.

**THE DISPLAYED OPEN LEMMA (the one owed pin).**

    RECSL-SIGN: at every recentering read frame σ of a coherent history,
    σ.R(−1) = −1.
    (Lean shape: the displayed hypothesis `hsign` of `recsl_isNodeLift*`;
    equivalent to the (SL-dev) residual leg by `recsl_residual_iff_sign`.
    Supplied by `StageCore.R_neg` at f = 1 — `signLaw_of_stageCore` — wherever
    the frame has engine provenance: the base CONSTRUCTS `StageCore`
    (`L1.baseStage_exists`), both transitions TRANSPORT it (`L4.TRANSstage`,
    `L5.recTRANSRS`). On the abstract recorded-history carrier it is a genuine
    hypothesis, same status class as the GR-3/GR-4 frame pins pre-route-(a).)

Countermodel shape (why no cheaper supplier exists): a sign-twisted stage
R₁ := χ·R₀ with χ a {±1}-character trivial on additive-guard triples and χ(−1) = −1
satisfies every round-2 R-law, `hRΦ`, `hS5`, and the recentering record (with
recorded center −c̃) while breaking the residual leg. The `hRadd` guard constrains χ
only on triples whose SUM keeps the common weight; all bridges from −1 to 1 pass
through a weight jump, which the round-2 laws never see. (Informal display; the
corpus's own StageCore adjudication is the standing authority, so no fresh compiled
countermodel was built.)

## THE CONSUMPTION SHAPE FOR B-M2

`recsl_isNodeLift_of_history (hcoh) (hi2 : i+1+1 < len) (hprev : nodes[i] ≠ rec)
(hspec : nodes[i+1] = rec) (hsign) : IsNodeLift (nodes[i+1]) (nodes[i+2].σ.Φ)` —
i.e. the [RC] fired key acquires EXACTLY the `IsNodeLift` interface that
`HistoryCoherent` already records at [NR] reads (its non-recentering leg carries
`IsNodeLift (nodes[i]) (nodes[i+1].σ.Φ)` as a clause). Consequences for the B-M2
ledger (`H1_BM2` §S0/§S2/§S4):

* The §S0 rows "D8 stride shape / (SL-top)" and "stage-law (SL-dev) DATA" at [RC]
  move from "NOT established / NOT forced — awaits REC-SL" to ESTABLISHED at the
  `readHyps_of_history_rec` configuration, MODULO the single RECSL-SIGN pin
  (StageCore-discharged on engine-built towers; the (K1)+(K2) row is untouched —
  it still rides GRADED-READ, as F2 predicted).
* KP-STEP's [RC]-specific residue collapses: at recentering reads the (SL) legs
  now cost GRADED-READ + frame `StageCore` provenance — no recentering-specific
  digit obligation remains beyond the sign pin.
* Scope fences inherited, both stated in the artifact: (i) the frame-from-a-
  RECENTERING chain (GR0b's standing recentering-chain future-work fence) — the
  supplier route needs a non-recentering landing one step back; (ii) terminal
  recenterings have no recorded fired key (the transition needs node i+2), so the
  legs are stated at INTERIOR recentering reads — matching the `rc_historyCoherent_gate`
  witness family (H₃rc has exactly this shape).
* Whether engine recentering FIRINGS have the recorded shape stays S-1/SITE-EXH
  side, unchanged (the standing REV-12 scope note).

## FILE MAP

* `lean/LeanUrat/B2D/RECSL_suppliers.lean` — the compiled unit (this note's §s in
  Lean order): `recsl_psi_coeff_zero`, `recsl_center_ne_zero` (single-node pins);
  `recsl_slTop`; `recsl_slDev_data`; `recsl_residual_iff_sign` (the sharp
  obstruction pin); `recsl_isNodeLift` / `recsl_isReadLift_11` (the assembled leg,
  `hsign` displayed); `signLaw_of_stageCore`, `recsl_isNodeLift_of_stageCore`;
  `recsl_isNodeLift_of_history`, `recsl_isNodeLift_of_history_stageCore` (the
  consumption shape). All Lean-core.
* Untouched: every existing file (imports FROM the new file only:
  `B2D.GR0b_suppliers`, `Moves.ResVal`).

— REC-SL F2 attempt unit, 2026-08-08.
