# TR3-S4 — TR-3′-GEN at ORDER 3 on the e₀ = 1, e₁ ≥ 2, e₂ = 1 stratum: the ǫ₂-revival composed as finite algebra — σ₂(R^F₃(f)) = z̄₂^{θ₂s₀(f)}·R_λ₃(f)(z̄₂^{θ₂}y), θ₂ = ℓ₁g₁γ₂

Unit chartered by the TWIST-3 probe (`lean/notes/openmath/TWIST3_PROBE_2026-08-08.md`
S3: "TR3-S4 charters ONLY on an exact law"; the measured law = (TWIST-3-MEASURED),
0/1,656 on the sealed candidate) — wave-15 lead per ROOT r32's displayed queue.
Parents: `TR3S1_2026-08-08.md` (ACCEPTED: order 2, e₀ = 1, e₁ FREE — the inner
theorem consumed on every digit), `TR3S2_2026-08-08.md` (ACCEPTED: order 2, all
e₀ — the Lemma EXP compose kernel and the KEY0/ORD-INV discipline, both lifted
one level here), `TR3S3_2026-08-08.md` (ACCEPTED, PRINT-UNCONDITIONAL per
HV3-THM: order 3, all e_j = 1 — the θ₂ = 0 instance of this note's display),
`HV3_DISCHARGE_2026-08-08.md` (HV3-THM: v₃ = w₂ at e₀ = e₁ = 1 — NOT covering
this stratum; its pinned-source displays ARE re-consumed here at the adjusted
instantiation, S3 Lemma HV3-E2), GRB_ORDER2_2026-08-05.md REV 14 (corrected ǫ;
the REV-12 revival narrowing this unit converts to a theorem on its stratum).
Genre: attempt-grade leaf proof note; finite algebra executed in full; numerics
are FALSIFIERS only (the probe's 23,733 recorded samples + the NEW preregistered
`tr3s4_check.py`, S8; two-commit seal). Every formula pin carries a non-textual
leg or an explicit attribution display ((PIN-GEN-i) inherited; the corrupted
shared fgmn.txt extraction is never consulted). HARD FENCES honored: no e₀ ≥ 2
(stacked) state, no e₂ ≥ 2 (live level-3 carry) state, no order ≥ 4, no D-ITER
object is touched (S7).

## S0. Statement and verdict

**Theorem TR3-S4 (proved below at attempt grade; falsifiers green through the
S8 run; acceptance counter 0/2 — the F2c verifier-owned implementation is a
DISPLAYED ACCEPTANCE OBLIGATION, S8.4).** Standing setting of the GRB leaf S1.1
(GD23 §1.2), state of ORDER 3 on the isolated-middle-ramification stratum:
reads ν₀ = (e₀, h₀, ψ₀), ν₁ = (e₁, h₁, ψ₁), ν₂ = (e₂, h₂, ψ₂) with **e₀ = 1,
e₁ ≥ 2, e₂ = 1** (general g_j, d₀, residue characteristic, base DVR per the
standing engine convention); (e_j, h_j) coprime, ψ_j monic irreducible with
ψ_j(0) ≠ 0; Φ₀ a monic root lift (w₀(Φ₀) = 0, deg Φ₀ = d₀); Φ₁ a lawful monic
lift of ν₀ (deg Φ₁ = g₀d₀, w₁(Φ₁) = g₀h₀, R_{λ₀}(Φ₁) ∼ ψ₀); Φ₂ a lawful monic
lift of ν₁ (deg Φ₂ = e₁g₁·deg Φ₁, w₂(Φ₂) = e₁g₁γ₂, R_{λ₂}(Φ₂) ∼ ψ₁); w₃ the
order-3 state valuation; γ₂ = e₁w₁(Φ₁) + h₁ = e₁g₀h₀ + h₁ and
γ₃ = e₂w₂(Φ₂) + h₂ = e₁g₁γ₂ + h₂. DISPLAYED HYPOTHESIS **(H-ℓ)**: the
normalized Bézout convention 0 ≤ ℓ_j < e_j at every level (the standing engine
convention; here it forces (ℓ₀, ℓ₀′) = (ℓ₂, ℓ₂′) = (0, 1) and makes
**(ℓ₁, ℓ₁′) genuinely nontrivial**: ℓ₁ = h₁⁻¹ mod e₁ ≠ 0). Let σ₀ : F₁ → K₀
and σ₁ : F₂ → K₁ be the ACCEPTED TR3-S1 identifications (its stratum is
e₀ = 1 with e₁ FREE — this state's order-2 substate lies inside it, S2) and
σ₂ : F₃ → K₂ the extension constructed in S2 (z₂ ↦ z̄₂, σ₂|_{F₂} = σ₁). Write
**θ₂ := ℓ₁g₁γ₂** (the level-3 character exponent; the exact level shift of
TR3-S2's θ = ℓ₀g₀γ₁, γ₁ = h₀). Then for EVERY nonzero f ∈ O[x], with
γ := w₃(f), s := s₀(f) the initial abscissa of the λ₃-component of N₃⁻(f),
j₀ := ⌊s/e₂⌋ = s:

    (TR3-S4-coeff)   σ₂-coefficientwise, at every slot j (abscissa s_j = j
                     at e₂ = 1):
                     σ₂( C^F3_j ) = z̄₂^{θ₂·s_j} · c^G3_{s_j},
                     with support EQUAL on both sides (nonzero exactly on-line);

    (TR3-S4-form)    σ₂( R^F₃(f) )(y) = z̄₂^{θ₂·s₀(f)} · R_λ₃(f)( z̄₂^{θ₂e₂} y ),
                     the rescaling factor being z̄₂^{θ₂} at e₂ = 1,

where C^F3_j are the coefficients of FGMN's marched level-3 weight read
R^F_{3,γ}(f) (Def 3.16 at i = 3 with the NONTRIVIAL corrected ǫ₂, Def 3.18 at
i = 3, integer weights via RESCALE with E₂ = E₃ = e₁) and c^G3_i, R_λ₃(f) are
the GMN-convention order-3 residual data (Defs 2.19–2.21 at r = 3 with the (18)
ordinate, normalized ℓ) — the ordinate u_i = w₂(A_i) + i·w₂(Φ₂) consumed
through **Lemma HV3-E2 (S3): v₃ = w₂ DERIVED at e₀ = 1, e₁ ≥ 2 from the same
sha256-pinned generic displays as accepted HV3-THM, at the adjusted
instantiation |λ₂| = h₁/e₁** — the charter's "attempt the derivation" leg,
which GOES THROUGH; no bespoke (H-v3-e2) hypothesis remains displayed, but
Lemma HV3-E2 is THIS note's own proof obligation (accepted HV3-THM's scope is
e₁ = 1 and does NOT cover it). The discrepancy is a per-argument SCALAR times
the FIXED rescaling y ↦ z̄₂^{θ₂}y — the (TWIST-3-MEASURED) law of the probe,
now an identity; it is EXACTLY TR3-S2's two-form one level up, and TR3-S3's
identity is its θ₂ = 0 instance. No (R1)/(R2) hypothesis is consumed: the
identity is total in nonzero f ∈ O[x].

| clause | status | where |
|---|---|---|
| σ₂ constructed, well-defined, march-compatible (family {σ₀, σ₁, σ₂}) | PROVED (σ₀, σ₁ consumed from accepted TR3-S1) | S2 |
| Lemma KER-13 (ǫ-triviality at FGMN levels 1 and 3; the LIVE level-2 data displayed) | PROVED (one line per dead level) | S3 |
| Lemma HV3-E2 (v₃ = w₂ at e₀ = 1, e₁ ≥ 2; ordinate identity) | PROVED at attempt grade (the HV3-THM derivation re-run at |λ₂| = h₁/e₁; e₁-sites tallied) | S3 |
| Lemma A-GEN-3 (level-3 inner identity at stride e₁ = accepted TR3-S1 on the digit; m₀-shift) | PROVED (citation discipline displayed) | S4 |
| Lemma Z2-e2 (off-line vanishing of the stride-e₁ level-2 read below its weight) | PROVED (two-level dichotomy) | S4 |
| Lemma EXP-3 (the compose kernel — TR3-S2's two-line Bézout identity one level up) | PROVED (two lines) | S4 |
| the assembled per-slot compose + support + two-form (TR3-S4) | PROVED (the unit's content) | S5 |
| TR-3′-GEN at order 3, e₀ = 1, e₂ = 1, ALL e₁ (with TR3-S3 as the θ₂ = 0 stratum) | THEOREM at attempt grade (probe conjecture → proved) | S6.1 |
| units-only phrasing on this stratum | REFUTED at every state with c := z̄₂^{θ₂} ≠ 1 (Lemma WIT-3) | S5 Rem. 2 |
| Lemma KEY0-LIFT (the FGMN-normalized key lift's GMN residual class = the θ₂-twisted ψ₂ class; the GMN-lawful key's class = the untwisted ψ₂ class; divergence criterion) + Lemma ORD-INV-LIFT (matched-label ord transport) | PROVED (the display lemmas with measured cover) | S6.2 |
| TR-3-ORD-X at these states (GMN-lawful key) | PROVED conditional on (KP_i) + [T]-properness (inherited, displayed) | S6.2 |
| consumers (GD3-FULL/OL-B (2′) leg WITH the character threaded; GD3-MIN Route B + clause-(4); B-M2 OL-1(b)) | served at these states (exact forms displayed) | S6.3 |
| e₀ ≥ 2 stacked, e₂ ≥ 2 carry, orders ≥ 4, D-ITER | NOT PROVED / NOT TOUCHED — scope box | S7 |
| falsifiers: recorded TWIST-3 probe + NEW preregistered tr3s4_check (composed E2E, fresh off-roster rows) | see S8 | S8 |

## S1. Setting, dictionary at depth 3, and the operator pins

Consumed from the GRB leaf and TR3-S1/TR3-S2/TR3-S3 (not re-derived): TR-5
(index dictionary at r = 3: FGMN φ_k ↔ project Φ_{k−1}; FGMN level data
(e₁, h₁)^F = (1, h₀), (e₂, h₂)^F = (e₁, h₁), (e₃, h₃)^F = (1, h₂) on this
stratum); Lemma RESCALE (w_i = E_iµ_i; here E₁ = 1, **E₂ = E₃ = e₁** — the
level-2 dilation is genuinely nontrivial, the level-1 and level-3 strides are
1 and the level-2 stride is e₁); TR-1-corr; **(H-CHAIN) at depth 3** (the
tower is a MacLane chain of µ₃ = w₃/E₃); and the GD23 standing GMN rows
(w_i ↔ v_{i+1}, K_i ↔ F^{GMN}_{i+1}, engine residuals = GMN-convention
residuals), extended at the top by Lemma HV3-E2 (S3).

**Formula pins.**

* **FGMN eq (12)** at level i: u(α)e_i + s(α)h_i = e(µ_i)α, 0 ≤ s(α) < e_i;
  Bézout ℓ_ih_i + ℓ′_ie_i = 1, 0 ≤ ℓ_i < e_i (FGMN's own print normalizes,
  fgmn.txt:1073). Non-textual legs inherited (N-1 T0, 0/23,111 symbolic).
  Levels 1 and 3 are DEAD here (e₁^F = e₃^F = 1, Lemma KER-13); the LIVE
  level-2 integer form: with β ∈ Γ₂ = ℤ (E₂-dilated), (12-int₂):
  u·e₁ + s·h₁ = β, 0 ≤ s < e₁ — write (s(β), u(β)) (= eq12(β, e₁, h₁) in the
  harnesses). Level-2 Bézout pair = the (H-ℓ) pair (ℓ₁, ℓ₁′).
* **FGMN Def 3.15 (CORRECTED, reading C)**: ǫ_i(α) = (z_i)^{ℓ′_i s(α) − ℓ_i u(α)}.
  Here ǫ₁ ≡ 1 (level-1 kernel), and **ǫ₂(β) = z₂^{ℓ₁′s(β) − ℓ₁u(β)} is
  genuinely nontrivial for the first time in an accepted-genre compose at
  depth 3** — the revival regime. Reading legs: N-1's triple pin at level 1;
  the TWIST-3 probe's M1 row (reading P refuted at ALL 1,277 P-visible slots
  with BOTH readings nontrivial) at level 2 — the strongest level-2
  discrimination on record, on PARI-certified RAMIFIED order-3 keys (CERT3).
  The corrupted raw extraction is never consulted.
* **(PIN-GEN-i)** — the standing FGMN attribution pin, inherited VERBATIM
  from TR3-S3 S1 (REV 2–4 scope): the identification of corrected Def 3.15
  at level 2, Def 3.16 at i = 3, and Def 3.18 at i = 3 with the printed FGMN
  operators rests on the print's level-GENERIC displays plus computational
  discrimination; the owed fresh-extraction leg was discharged separately at
  303b11e (the FGMN re-derivation unit's §S7 rider) and the pin STAYS
  DISPLAYED as the attribution record. Unlike TR3-S3 (where the algebra
  consumed only ǫ ≡ 1), THIS unit consumes the corrected ǫ₂ display
  load-bearingly — the M1-row discrimination and the S8 falsifiers are the
  non-textual cover, and a reading error would break the C1/E2A law rows,
  not hide.
* **FGMN Def 3.16** (generic in i; fgmn.txt:1246 provenance under the pin).
  At i = 3 with the stratum data (e₃^F = e₂ = 1): for f = Σ_i A_iΦ₂^i (the
  Φ₂-development, deg A_i < deg Φ₂; both operators read this SAME
  development, φ₃ ↔ Φ₂ under TR-5),
  R_{3,γ}(f) = Σ_j ǫ₂(γ_j)·R_{2,γ_j}(A_{s_j})(z₂)·y^j, slot abscissas
  s_j = s(γ) + je₂ = j (level-3 kernel: s(γ) = 0), slot weights

      (SLOT-3′)   γ_j = (γ − s_j·γ₃)/e₂ = γ − j·γ₃ ∈ ℤ    (µ₃(φ₃) = γ₃/E₃,
                  RESCALE at level 3; trivial integrality at e₂ = 1).

  At i = 2 (the inner read, NOW AT STRIDE e₁): for A = Σ_m a_mΦ₁^m,
  R_{2,β}(A) = Σ_{m′≥0} ǫ₁(β_{m′})·R_{1,β_{m′}}(a_{s(β)+m′e₁})(z₁)·y^{m′}
  (ǫ₁ ≡ 1), slot abscissas s(β) + m′e₁, slot weights

      (SLOT-2′)   β_{m′} = (β − (s(β)+m′e₁)·γ₂)/e₁ ∈ ℤ
                  (integrality: β ≡ s(β)h₁ and (s(β)+m′e₁)γ₂ ≡ s(β)h₁
                  (mod e₁), since γ₂ ≡ h₁ (mod e₁)) —

  this is TR3-S2's (SLOT-gen) verbatim ONE LEVEL UP (there: level-2 slots at
  stride e₁ fed level-1 weights; here the same display feeds the stride-e₁
  level-2 read inside the level-3 slots). At i = 1 and the i = 0 line: as in
  TR3-S1 S1 at stride 1 (level-0 read = reduction of b/π^δ, zero whenever
  w₀(b) > δ, including δ < 0).
* **FGMN Def 3.18**: R^F_i(g) = R_{i,α}(g)/y^{j₀}, j₀ = ⌊s(g)/e_i^F⌋; at
  e₃^F = e₂ = 1, j₀ = s(g) = the initial abscissa of the λ₃-component.
* **Valuations (project side, standing).** w₁(a) = min_m(w₀(b_m) + m·h₀)
  over the Φ₀-development (e₀ = 1); w₂(A) = min_m(e₁·w₁(a_m) + m·γ₂) over
  the Φ₁-development (the TR3-S2 S1 pin, E₂ = e₁);
  w₃(f) = min_i(e₂·w₂(A_i) + i·γ₃) = min_i(w₂(A_i) + i·γ₃) over the
  Φ₂-development ((H-CHAIN) at depth 3 + RESCALE).
* **GMN Defs 2.19–2.21 at r = 3** (the level-generic recursion,
  gmn.txt:1823–1906 provenance; the r = 2 instance pinned at TR3-S1/S2):
  ordinates u_i := v₃(A_iφ₃^i) = v₃(A_i) + i·v₃(φ₃), consumed through Lemma
  HV3-E2 as **u_i = w₂(A_i) + i·w₂(Φ₂)**; twist exponent
  **t₂(i) = (s₂(A_i) − ℓ₁u_i)/e₁** where s₂(A_i) = the initial abscissa of
  S₂(A_i) (the λ₂-component of N₂⁻(A_i), the tower's second-read slope
  λ₂ = −h₁/e₁ FIXED BY THE TYPE) and (ℓ₁, e₁) are the GMN level-2 = project
  read-ν₁ data — at ℓ₁ ≠ 0 the ℓ₁u_i term is LOAD-BEARING for the first
  time at depth 3 (TR3-S1 S9.7's trap, live one level up); coefficients
  c^G3_i = z₂^{t₂(i)}·R₂(A_i)(z₂) for (i, u_i) on the λ₃-component of
  N₃⁻(f), c^G3_i = 0 strictly above; R_λ₃(f) = c^G3_s + c^G3_{s+e₂}y + ⋯ +
  c^G3_{s+de₂}y^d. The inner R₂(A) is GMN's order-2 residual polynomial
  w.r.t. λ₂ — under the GD23 rows and ACCEPTED TR3-S1 (which covers e₀ = 1
  with e₁ ≥ 2 free) this is the project operator R_λ₂(A); evaluation
  R₂(A)(z₂) is Def 2.21's substitution from the ACTUAL initial abscissa
  s₂(A) (constant term = c^G2_{s₂(A)}).
* **GMN normalization note (display duty, verbatim TR3-S2 S1).** The print
  fixes ℓ only up to representative/sign; replacing ℓ₁ by ℓ₁ + qe₁ changes
  t₂(i) by −q·u_i and transforms R_λ₃ by the standing law
  R′_λ(y) = z₂^{−q·u(f)}·R_λ(z₂^{qh₂}y) — scalar × rescaling, the same genre
  as (TR3-S4-form) itself. The on-the-nose identity is scoped to (H-ℓ); the
  S6.2 ord conclusions survive any fixed choice.

## S2. The σ-family at depth 3: σ₂ constructed at the e₁-stride key

Towers: FGMN F₀ = F, F₁ = F₀[z₀], F₂ = F₁[z₁], F₃ = F₂[z₂] (ψ_i^F := the
minimal polynomial of z_i over F_i); project K₀ = F[X]/(φ̄₀),
K₁ = K₀[z]/(ψ₀) with z̄ := the class, K₂ = K₁[z]/(ψ₁) with z̄₂ := the class.

**σ₀ and σ₁ — CONSUMED from accepted TR3-S1 S2** (z₀ ↦ X̄, z₁ ↦ z̄,
σ₁|_{F₁} = σ₀, the evaluation square (SQ)). TR3-S1's stratum is {order 2,
e₀ = 1} with ν₁ = (e₁, h₁, ψ₁) FREE — its σ-construction and theorem apply to
this state's order-2 substate (reads ν₀, ν₁, keys Φ₀, Φ₁, the same K₀, K₁)
VERBATIM. The substate qualifies exactly as in TR3-S3 S2: a MacLane chain of
µ₃ truncates to a MacLane chain of µ₂ (Def 3.1 is nested), so (H-CHAIN) at
depth 3 supplies (H-CHAIN) at depth 2, and the substate's read data (lawful
Φ₁, R_{λ₀}(Φ₁) ∼ ψ₀) are the state's own first-read data.

**σ₂ : F₃ → K₂ extending σ₁ with z₂ ↦ z̄₂.** Well-defined iso iff
σ₁[y](ψ₂^F) = ψ₁. Supply:
(i) ψ₂^F = R₂^F(Φ₂) — Cor 5.6(2) chain-internal at index 2 < r = 3
((H-CHAIN) at depth 3 consumed; NO (KP_i) needed here — the extended-chain
reading enters only at the state key Φ₃, S6.2, exactly as in TR3-S3);
(ii) accepted TR3-S1 at f := Φ₂ (total in nonzero f): σ₁(R₂^F(Φ₂)) = R_λ₂(Φ₂)
— on the e₀ = 1 substate the order-2 discrepancy is ≡ 1 (θ = ℓ₀g₀h₀ = 0);
(iii) monicity upgrade of the read datum R_λ₂(Φ₂) ∼ ψ₁ to EQUALITY, at
stride e₁: Φ₂ monic of exact degree e₁g₁·deg Φ₁ has top Φ₁-digit
a_{e₁g₁} = 1 with w₁ = 0, ordinate u = e₁g₁·w₁(Φ₁), on-line check
e₁u + e₁g₁h₁ = e₁g₁(e₁w₁(Φ₁) + h₁) = e₁g₁γ₂ = w₂(Φ₂); the digit 1 has inner
data R₁(1) = 1, s₁(1) = 0, so t₁ = (0 − ℓ₀u)/e₀ = 0 (ℓ₀ = 0 at e₀ = 1) and
c^G2_{e₁g₁} = 1 — the leading coefficient is 1 (the order-2 character is
trivial on this stratum; contrast S6.2's level-3 key, where it is not);
lawfulness (deg R_λ₂(Φ₂) = g₁ = deg ψ₁) forces the window [s₂(Φ₂), e₁g₁] to
have stride-e₁ length g₁, i.e. s₂(Φ₂) = e₁g₁ − e₁·g₁ = 0; a monic associate
of the monic ψ₁ equals ψ₁. Hence σ₁(ψ₂^F) = R_λ₂(Φ₂) = ψ₁. ∎

**March-compatibility.** σ₂|_{F₂} = σ₁, all tower squares commute, and the
evaluation square

    (SQ₂)   σ₂( P(z₂) ) = ( σ₁[y]P )( z̄₂ )   for every P ∈ F₂[y]

(σ₂ = the quotient of σ₁[y] by (ψ₂^F) ↦ (ψ₁)). {σ₀, σ₁, σ₂} is the
level-by-level compatible family of TR-3′-GEN at order 3 — the same family
shape as TR3-S3's, constructed at the e₁-stride key.

## S3. Lemma KER-13, the live ǫ₂, and Lemma HV3-E2 (route steps (i) and the ordinate identity)

**Lemma KER-13 (the dead levels).** At FGMN levels i = 1 and i = 3 of this
stratum (e₁^F = e₃^F = 1): every eq-(12) split has (s(β), u(β)) = (0, β),
the normalized Bézout pair is (0, 1), and ǫ₁(β) = z₁^{1·0 − 0·β} = 1 for
every β (ǫ₀ ≡ 1 as always). Consequently the level-3 forced progression
starts at s(γ) = 0 with stride 1 (every abscissa is a slot), and the ONLY
live twist system is ǫ₂.

*Proof.* u·1 + s·h = β with 0 ≤ s < 1 forces (s, u) = (0, β); ℓh + ℓ′ = 1
with 0 ≤ ℓ < 1 forces (ℓ, ℓ′) = (0, 1); substitute into corrected Def 3.15. ∎

**The live level-2 data (route step (i)'s complement).** ǫ₂(β) =
z₂^{ℓ₁′s(β) − ℓ₁u(β)} with (s(β), u(β)) the (12-int₂) split — a genuinely
nontrivial character of β (ℓ₁ ≠ 0). This is the object whose threading is
this unit's charge; it cancels in S5 through Lemma EXP-3, never by dying.

**Lemma HV3-E2 (the ordinate identity at e₁ ≥ 2; the charter's attempted
derivation — it goes through).** On this stratum, for every nonzero
P ∈ O[x]: v₃(P) = w₂(P); consequently for f = Σ_i A_iΦ₂^i the GMN order-3
ordinates satisfy u_i := v₃(A_iφ₃^i) = w₂(A_i) + i·w₂(Φ₂).

*Derivation* (the HV3-THM §D4 five steps re-run at the adjusted
instantiation; source = the sha256-pinned `docs/references/HigherNewton.tex`
(`8253d4d6…bd18`, re-verified this unit), displays (E1) Def @ TeX 979–983,
(E2) Def 2.5 @ TeX 985–991, (E3) Prop 2.6 @ TeX 1012–1014, (E4) Prop
2.7(3)(4) @ TeX 1028–1038 — all GENERIC in r, extracted verbatim at HV3 §D2
and re-read against the pinned file for this unit). Dictionary at r = 3:
φ₂^{GMN} ↔ Φ₁, φ₃^{GMN} ↔ Φ₂, (e₂, h₂)^{GMN} = (e₁, h₁)^{proj}, so
**λ₂^{GMN} = −h₁/e₁ and |λ₂| = h₁/e₁** (the adjusted site) and
e₂^{GMN} = e₁ ≥ 2 (the second adjusted site). Let P = Σ_i a_iΦ₁^i be the
Φ₁-development (= the φ₂^{GMN}-adic development; both sides read the SAME
development).

1. **[Print, (E2)+(E1)]** v₃(P) = e₂^{GMN}·H₂(S₂(P)) — consumed only through
   its print-evaluated form (E4), exactly as in HV3 §D4 step 1 (the (E1)
   intercept is rational in general at e₁ ≥ 2; nothing below touches it
   directly).
2. **[Print, (E4) at r = 3]**
   v₃(P) = e₂^{GMN}·min_i{ v₂(a_i) + i(v₂(φ₂) + |λ₂|) }
         = e₁·min_i{ v₂(a_i) + i·v₂(φ₂) + i·h₁/e₁ }.
3. **[Accepted TR3-S1]** v₂ = w₁ verbatim and v₂(φ₂) = w₁(Φ₁) at e₀ = 1 —
   the r = 2 tie reads ONLY order-1 data (Def 2.5 at r = 2 consumes the
   λ₁-component of the Φ₀-development), so it is e₁-FREE and applies on this
   stratum unchanged. Substituting and distributing the positive factor e₁
   through the min (min commutes with multiplication by e₁ > 0; every term
   e₁·(w₁(a_i) + i·w₁(Φ₁)) + i·h₁ is an INTEGER — no rounding anywhere):
   v₃(P) = min_i{ e₁·w₁(a_i) + i·(e₁w₁(Φ₁) + h₁) } = min_i{ e₁w₁(a_i) + iγ₂ }.
4. **[Project side, standing]** Under (H-CHAIN) at depth 3 + Lemma RESCALE
   with the NONTRIVIAL dilation E₂ = e₁ (the third adjusted site; at HV3-THM
   this was the trivial E₂ = 1): w₂(P) = min_i{ e₁w₁(a_i) + iγ₂ } over the
   Φ₁-development — the TR3-S2 S1 standing pin, verbatim.
5. **[Conclude]** v₃(P) = w₂(P). Ordinates: u_i = v₃(A_i) + i·v₃(φ₃) (v₃ is
   a discrete valuation, (E3); GMN's (18)-note expansion), and
   v₃(A_i) = w₂(A_i), v₃(φ₃) = w₂(Φ₂) by the all-P identity at φ₃ ↔ Φ₂. ∎

Consistency display (not consumed): (E4) item 4 gives v₃(φ₂) =
e₂^{GMN}v₂(φ₂) + h₂^{GMN} = e₁w₁(Φ₁) + h₁ = γ₂ = w₂(Φ₁) — the augmentation
value, matching the project chain.

**Status box for Lemma HV3-E2 (honesty).** The e₁-adjusted sites, tallied:
step 2 (e₂^{GMN} = e₁ twice: the outer factor and |λ₂| = h₁/e₁), step 3 (the
e₁-distribution across the min — the only new arithmetic; exact, integer
output), step 4 (E₂ = e₁ dilation). Conditionality = HV3-THM's four standing
conditions VERBATIM ((i) pinned-print baseline for (E1)–(E4) read at r = 3;
(ii) accepted TR3-S1; (iii) (H-CHAIN)+RESCALE; (iv) the projective
dictionary/type-hood consumptions — the r = 3 type here is the order-2 type
assembled from ν₀, ν₁ and Φ₀, Φ₁, Φ₂, same consumption shape). What is NEW
and therefore THIS note's hostile-pass obligation: the instantiation at
e₁ ≥ 2 (accepted HV3-THM proved exactly this arc at e₁ = 1 and its
acceptance does NOT transfer). e₂ is never used — but this note consumes
the lemma only at e₂ = 1 anyway. No claim at e₀ ≥ 2 (there v₂ = w₁ itself
needs TR3-S2's pin rather than TR3-S1's, and the (E4) instantiation changes
again — fenced, S7). Falsifier face: the S8 HV row (two-way v₃: geometric
(E4)-min with exact fractions vs the MacLane operator, plus the ordinate
expansion on Φ₂-developments).

## S4. Lemma A-GEN-3, Lemma Z2-e2, Lemma EXP-3 (route steps (ii)+(iii))

**Lemma A-GEN-3 (the level-3 inner identity at stride e₁ — accepted TR3-S1
consumed as one theorem).** Let A ∈ O[x] be nonzero, β := w₂(A), and
(s(β), u(β)) the (12-int₂) data. Then, in K₁[y]:

    (A-GEN-3)   σ₁[y]( R^F_{2,β}(A) ) = y^{m₀} · R_λ₂(A),
                m₀ := (s₂(A) − s(β))/e₁ ∈ ℤ_{≥0},

where s₂(A) is the initial abscissa of the λ₂-component of N₂⁻(A) and
R_λ₂(A) the GMN-convention order-2 residual polynomial of the substate.

*Proof.* Accepted TR3-S1's display (TR3-S1) applied to the order-2 substate
(S2) at f := A, γ := β = w₂(A): σ₁-coefficientwise
R^F_{2,β}(A) = y^{j₀}·R_λ₂(A) with j₀ = ⌊s₀(A)/e₁⌋. TR3-S1 is total in
nonzero f ∈ O[x] with no degree bound (its scope note), so the digit
qualifies whatever its degree; its S5(i) gives that every on-line abscissa
of N₂⁻(A) is ≡ s(β) (mod e₁) and identifies s₀(A) = s₂(A) (the same
initial-abscissa datum), so s₂(A) = s(β) + m₀e₁ with m₀ ∈ ℤ_{≥0} and
j₀ = ⌊(s(β) + m₀e₁)/e₁⌋ = m₀ (0 ≤ s(β) < e₁). The forced-vs-actual
bookkeeping m₀ = (s₂(A) − s(β))/e₁ is the probe's row-P2 identity — here it
is the y^{m₀} factor. ∎

**Citation discipline (the S9 attack point, displayed).** The inner operator
match: FGMN's R_{2,γ_j} inside Def 3.16-at-i = 3 is the SAME operator
TR3-S1's theorem reads — Def 3.16 at i = 2 on the same Φ₁-development at the
same integer weight (S1's (SLOT-2′) = TR3-S1's slot display at e₀ = 1,
e₁ free), and GMN's inner R₂(A) at the type-fixed λ₂ is the operator
TR3-S1's c^G2 data assemble (S1 pins). TR3-S1's stratum hypothesis is
e₀ = 1 ONLY — every consumption here is inside it.

**Lemma Z2-e2 (off-line vanishing; the slot-domain input of S5(ii)).** Let
A ∈ O[x] be nonzero and β ∈ ℤ with β < w₂(A). Then R^F_{2,β}(A) = 0 — every
coefficient vanishes.

*Proof.* By S1's Def-3.16 pin at i = 2, the m′-th coefficient reads the
digit a at abscissa s_{m′} = s(β) + m′e₁ at level-1 weight β_{m′} =
(β − s_{m′}γ₂)/e₁ ((SLOT-2′); integral by the displayed congruence). Each
nonzero digit has e₁w₁(a) + s_{m′}γ₂ ≥ w₂(A) > β (the min defining w₂), so
w₁(a) > β_{m′}. In turn each Φ₀-digit b of a has (e₀ = 1, level-1 stride 1)
w₀(b) ≥ w₁(a) − m″h₀ > β_{m′} − m″h₀ = the level-0 read weight δ, so every
coefficient of b·π^{−δ} has positive valuation and its reduction is 0 —
including δ < 0 (TR3-S1 (A3)'s corner, verbatim). Every level-1 coefficient
of every slot vanishes, hence so does every level-2 coefficient. ∎
(Domain: β < w₂(A) means A ∈ P_β(µ₂) — Def 3.16 applied inside its printed
domain; A = 0 reads 0 by the printed convention.)

**Lemma EXP-3 (the compose kernel — TR3-S2's Lemma EXP one level up; route
step (iii)).** Let (ℓ₁, ℓ₁′) satisfy ℓ₁h₁ + ℓ₁′e₁ = 1. Let β ∈ ℤ with
(12-int₂) data (s, u′) (u′e₁ + sh₁ = β), and let s₂ = s + m₀e₁ for some
m₀ ∈ ℤ. Then for EVERY abscissa S ∈ ℤ:

    (EXP-3)   [ ℓ₁′s − ℓ₁u′ ] + m₀  =  ( s₂ − ℓ₁(β + S·e₁g₁γ₂) )/e₁  +  θ₂·S,

both sides being the integer (s₂ − ℓ₁β)/e₁. In words: FGMN's composed
per-slot exponent (the corrected-ǫ₂ exponent + Lemma A-GEN-3's shift m₀)
and GMN's composed per-slot exponent (t₂ at ordinate u_S = β + S·w₂(Φ₂),
plus the character θ₂S) are BOTH the S-free integer (s₂ − ℓ₁β)/e₁.

*Proof.* Multiply by e₁ and substitute e₁u′ = β − sh₁:
LHS·e₁ = e₁ℓ₁′s − ℓ₁(β − sh₁) + (s₂ − s) = s₂ − ℓ₁β + s(ℓ₁′e₁ + ℓ₁h₁ − 1)
       = s₂ − ℓ₁β.
RHS·e₁ = s₂ − ℓ₁β − ℓ₁S·e₁g₁γ₂ + e₁·θ₂·S = s₂ − ℓ₁β  (θ₂ = ℓ₁g₁γ₂). ∎

Integrality remark: s₂ ≡ s ≡ ℓ₁β (mod e₁) (the on-line congruence + (12-int₂))
makes (s₂ − ℓ₁β)/e₁ ∈ ℤ; GMN's printed t₂-integrality follows for free
(s₂ − ℓ₁u_S ≡ s₂ − ℓ₁β ≡ 0 (mod e₁)) — the probe's P0 row, re-derived. At
e₁ = 1 every ℓ₁-term dies and (EXP-3) reads m₀ = s₂ — TR3-S3's t₂-collapse
exactly. The level shift against TR3-S2's Lemma EXP: (e₀, h₀, ℓ₀, w₁(Φ₁)
datum e₀g₀h₀) ↦ (e₁, h₁, ℓ₁, w₂(Φ₂) datum e₁g₁γ₂), θ = ℓ₀g₀h₀ ↦ θ₂ = ℓ₁g₁γ₂.

## S5. The theorem: the assembled per-slot compose

Fix nonzero f ∈ O[x] with Φ₂-development f = Σ_i A_iΦ₂^i and
γ := w₃(f) = min_i(w₂(A_i) + iγ₃) (e₂ = 1); s(γ) = 0 (Lemma KER-13), so
FGMN's slot j sits at abscissa s_j = j for every j ≥ 0, with slot weight
γ_j = γ − jγ₃ ((SLOT-3′)).

**(i) Outer support is the same set on both sides.** Under Lemma HV3-E2,
GMN's order-3 ordinate is u_i = w₂(A_i) + i·w₂(Φ₂), so

    e₂·u_i + i·h₂ = w₂(A_i) + i·(w₂(Φ₂) + h₂) = w₂(A_i) + i·γ₃ ≥ γ,

with equality iff (i, u_i) lies on the λ₃-component S of N₃⁻(f). Call these
i ON-LINE. At stride e₂ = 1 no congruence survives: every on-line abscissa i
is the slot j = i, and conversely FGMN's slot j is on-line iff
w₂(A_j) = γ_j — the same equality. The initial/final on-line abscissas
s := s₀(f), s′ give GMN's component data (s, d), d = s′ − s, and FGMN's
j₀ = ⌊s/e₂⌋ = s. [TR3-S3 S5(i) verbatim; the only change is which lemma
supplies the ordinate.]

**(ii) Per-slot identity.** Slot j, digit A := A_j, slot weight γ_j. Three
cases.
* A = 0, or A ≠ 0 with w₂(A) > γ_j (off-line): FGMN's coefficient is
  ǫ₂(γ_j)·R^F_{2,γ_j}(A)(z₂) = 0 by Lemma Z2-e2 (domain: every nonzero
  digit has w₂(A) ≥ γ_j from γ = min; this covers γ_j < 0, where no digit
  can be on-line since w₂ ≥ 0; the unit ǫ₂(γ_j) changes nothing). GMN:
  (j, u_j) strictly above N (or A = 0), so c^G3_j := 0; outside the
  component window GMN reads nothing and FGMN's coefficient is 0 by the
  same lemma. Both sides 0 — support agrees at every abscissa.
* on-line: w₂(A) = γ_j, A ≠ 0. Write (s, u′) := (s(γ_j), u(γ_j)) the
  (12-int₂) data, s₂ := s₂(A), m₀ := (s₂ − s)/e₁ ∈ ℤ_{≥0} (Lemma A-GEN-3),
  u_j = γ_j + j·e₁g₁γ₂ (the on-line ordinate, Lemma HV3-E2). Then

      σ₂( C^F3_j ) = σ₂( ǫ₂(γ_j) ) · σ₂( R^F_{2,γ_j}(A)(z₂) )
                   = z̄₂^{ℓ₁′s − ℓ₁u′} · ( σ₁[y] R^F_{2,γ_j}(A) )( z̄₂ )
                                              [(SQ₂); σ₂z₂ = z̄₂; corrected ǫ₂]
                   = z̄₂^{ℓ₁′s − ℓ₁u′} · z̄₂^{m₀} · R_λ₂(A)( z̄₂ )
                                              [Lemma A-GEN-3 at β = γ_j = w₂(A)]
                   = z̄₂^{t₂(j)} · z̄₂^{θ₂·j} · R₂(A)( z̄₂ )
                                              [Lemma EXP-3 at β = γ_j, S = j:
                                               both exponents = (s₂ − ℓ₁γ_j)/e₁;
                                               GMN's inner R₂ = R_λ₂ at the
                                               type-fixed λ₂, S1 pins]
                   = z̄₂^{θ₂·j} · c^G3_j .    [GMN Def 2.20]

  **Nonvanishing (both sides).** R_λ₂(A) is a NONZERO polynomial (its
  endpoint coefficients are ≠ 0 — TR3-S1 S5's support/nonvanishing at the
  substate) of y-degree ≤ g₁ − 1: deg A < deg Φ₂ = e₁g₁·deg Φ₁ confines the
  Φ₁-development to abscissas ≤ e₁g₁ − 1, the on-line window [s₂, s₂′] has
  s₂′ ≤ e₁g₁ − 1 and stride e₁, so deg R_λ₂(A) = (s₂′ − s₂)/e₁ ≤
  (e₁g₁ − 1)/e₁ < g₁ — THIS is where the digit's degree bound is consumed.
  Since deg R_λ₂(A) < g₁ = deg ψ₁ = the degree of z̄₂'s minimal polynomial
  over K₁, the evaluation at z̄₂ is nonzero ({1, z̄₂, …, z̄₂^{g₁−1}} is a
  K₁-basis of K₂); multiplying by the units z̄₂^{…} keeps it nonzero. So
  σ₂(C^F3_j) ≠ 0 and (reading the chain backwards through the field iso)
  c^G3_j ≠ 0 — GMN's printed on-line nonvanishing re-derived; with the
  off-line case, support is EXACTLY the on-line set on both sides.

**(iii) Assembly and the two-form.** By (i)+(ii): σ₂-coefficientwise,
R^F_{3,γ}(f) = Σ_j C^F3_j y^j has support = the on-line set ⊆ [s, s′], with
σ₂(C^F3_j) = z̄₂^{θ₂j}·c^G3_j at EVERY j ≥ 0 (both sides 0 off-line).
Re-index j = s + k:

    σ₂( R^F_{3,γ}(f) ) = Σ_{k=0}^{d} z̄₂^{θ₂(s+k)} · c^G3_{s+k} · y^{s+k},

and dividing by y^{j₀} (Def 3.18; j₀ = s by (i), and the k = 0 term is
nonzero, so the observed initial slot IS j₀):

    σ₂( R^F₃(f) )(y) = z̄₂^{θ₂s} · Σ_{k=0}^{d} c^G3_{s+k} (z̄₂^{θ₂}y)^k
                     = z̄₂^{θ₂·s₀(f)} · R_λ₃(f)( z̄₂^{θ₂} y ).            ∎

**Remark 1 (what the compose bought).** Route steps (i)–(iv) of the probe's
sealed derivation are exactly: Lemma KER-13 + the live ǫ₂ display (i), Lemma
A-GEN-3 (ii)+(iii)'s inner citation, Lemma EXP-3 (iii)'s cancellation, and
the Def-2.19 t₂ at general ℓ₁ (iv) — the ONLY genuinely new algebra at this
stratum is Lemma EXP-3 (two lines) and Lemma HV3-E2 (five steps of pinned
displays). The measured (TWIST-3-MEASURED) is the S = j instance of (EXP-3)
read through the compose chain; the probe's sealed header derivation is this
proof's skeleton, now with support, σ-construction, domain lemma, assembly,
and normalization displayed.

**Remark 2 (the units-only phrasing is REFUTED on this stratum too).** The
argument is TR3-S2 S5 Remark 2 VERBATIM one level up: whenever
c := z̄₂^{θ₂e₂} = z̄₂^{θ₂} ≠ 1, no family of per-argument units u_f and no
relabeling σ₂′ = τ∘σ₂ can replace (TR3-S4-form) — coefficient ratios across
two consecutive on-line slots differ between the two sides by the FIXED
factor c, and ratios are invariant under per-argument scalars and move
coherently under any field automorphism τ (τ(x)/τ(y) = τ(x/y); at witnesses
with ratio 1 on one side, τ(1) = 1 ≠ c⁻¹). The witness supply is Lemma
WIT-3: for every pair x⁰, x¹ ∈ K₂* there is an f whose λ₃-component is
exactly the two consecutive slots 0, 1 with FGMN slot values x⁰, x¹
(equivalently GMN coefficients z̄₂^{−θ₂·j}x^j — prescribe x⁰ = x¹ = 1 for
the refutation witness). *Construction*: set W₀ := e₁(g₁−1)γ₂ +
e₁(g₀−1)h₀ + (e₁−1)γ₂, β⁰ := γ₃ + W₀, β¹ := W₀; realize each target value
x ∈ K₂* at level-2 weight β ≥ W₀ as A := Σ_{k: x_k ≠ 0} C_k·Φ₁^{s(β)+ke₁}
where x = Σ_{k<g₁} x_k z̄₂^k in the K₁-basis and C_k is TR3-S2's (W2)
level-1 realization (e₀ = 1 instance: C_k = Σ_{k′: (x_k)_{k′} ≠ 0}
π^{β_k − k′h₀}·B_{k′}·Φ₀^{k′} at level-1 weight β_k := (β − (s(β)+ke₁)γ₂)/e₁
≥ (g₀−1)h₀ by the choice of W₀ — all π-exponents nonnegative, all digits
on-line, deg A < deg Φ₂); then w₂(A) = β, R^F_{2,β}(A)(z₂) realizes any
prescribed value as x ranges over K₂* (the ǫ₂(β) unit is absorbed into the
prescription), and f := A₀ + A₁Φ₂ has ordinates u₀ = γ₃ + W₀,
u₁ = W₀ + w₂(Φ₂) with equal values u₀ + 0 = u₁ + h₂ = γ₃ + W₀ — exactly the
two on-line slots 0, 1. (Falsifier face: the S8 W row constructs prescribed
two-sided slot values with 0 failures predicted; the probe's M6 meter
refuted the naive identity at all 710 twist-visible slots.)

## S6. Consumers: what is now a theorem, and for whom

### S6.1 TR-3′-GEN at order 3 with e₀ = 1, e₂ = 1 closes at ALL e₁ (attempt grade this stratum)

The probe's (TWIST-3-MEASURED) (instance-confirmed 0/1,656, conjecture
grade) is now the identity clause of Theorem TR3-S4. Combined with accepted
TR3-S3 (the e₁ = 1 stratum, where θ₂ = ℓ₁g₁γ₂ = 0 and the display below
degenerates to the identity) and the accepted order-2 trilogy legs:

> **TR-3′-GEN now has the display** σ₂(R^F₃(f)) = z̄₂^{θ₂·s₀(f)}·
> R_λ₃(f)(z̄₂^{θ₂e₂}y), θ₂ = ℓ₁g₁γ₂, **proved on {all order-2 states} ∪
> {order-3 states with e₀ = 1, e₂ = 1, ANY e₁}** — accepted at order 2 and
> at order-3 e₁ = 1; ATTEMPT GRADE (this unit, pending hostile passes) at
> order-3 e₁ ≥ 2. The ǫ-recurrence revival on this stratum is exactly the
> slot-keyed character z̄₂^{θ₂s_j} — the leaf's REV-12 narrowing converted
> to a theorem here. At order 3 with e₀ ≥ 2 or e₂ ≥ 2, and at all orders
> ≥ 4, TR-3′-GEN stays OPEN (S7); the units-only record phrasing is REFUTED
> on this stratum wherever z̄₂^{θ₂} ≠ 1 (S5 Remark 2) — the correct general
> statement is the scalar × fixed-rescaling two-form, as at order 2.

Through TR-4 (leaf S4), the (2′) final leg at these states holds in the
exact form σ₂(R^{gr}(in f)) = z̄₂^{θ₂s₀(f)}·R_λ₃(f)(z̄₂^{θ₂}y) — the
character MUST be threaded (not on-the-nose at twist-visible states).
Conditionality of the attribution: Lemma HV3-E2 (this note, S3 status box)
+ (PIN-GEN-i) (S1) + (H-ℓ); no bespoke hypothesis.

### S6.2 The display lemmas: KEY0-LIFT and ORD-INV-LIFT; TR-3-ORD-X at these states

0. **Lemma KEY0-LIFT (the key-class bifurcation one level up; the probe's
   P4′ primary row 24/24 and V4 row 0/24 are its faces).** Let Φ₃^h ∈ O[x]
   be monic with deg Φ₃^h = e₂g₂·deg Φ₂ = g₂·deg Φ₂ and
   σ₂(R₃^F(Φ₃^h)) = ψ₂ (the FGMN-NORMALIZED key datum — the harness's
   ǫ₂-corrected canonical lift realizes it, probe V4). Then s₀(Φ₃^h) = 0,
   the λ₃-window is exactly [0, g₂], and

       R_λ₃(Φ₃^h) = T_{c⁻¹}ψ₂,   c := z̄₂^{θ₂e₂} = z̄₂^{θ₂},

   i.e. coefficientwise (R_λ₃(Φ₃^h))_k = z̄₂^{−θ₂k}·ψ₂_k — the GMN residual
   class of the FGMN-NORMALIZED key lift is the θ₂-TWISTED ψ₂ class (the
   GMN-LAWFUL key's own class is the untwisted ψ₂ class — step 1; REV 1
   orientation fix).
   *Proof* (TR3-S2 Lemma KEY0 verbatim one level up): (1) the degree bounds
   the Φ₂-development abscissas by g₂, so s′ ≤ g₂; (2) by S5(ii)+(iii) the
   side read has degree exactly d = s′ − s₀ with nonzero ends; σ₂ is a
   coefficientwise iso, so d = deg ψ₂ = g₂; (3) hence s₀ = s′ − g₂ ≤ 0 and
   abscissas are ≥ 0, so s₀(Φ₃^h) = 0, s′ = g₂; (4) (TR3-S4-form) at
   f = Φ₃^h with s₀ = 0 reads ψ₂(y) = R_λ₃(Φ₃^h)(z̄₂^{θ₂}y), i.e.
   R_λ₃(Φ₃^h) = T_{c⁻¹}ψ₂. ∎
   **Divergence criterion (CLASS form — the P4′-secondary lesson applied).**
   The two key classes, both read on the GMN side (the FGMN-normalized
   lift's class = the class of T_{c⁻¹}ψ₂, this lemma; the GMN-lawful key's
   class = the class of ψ₂, step 1), diverge iff the MONICIZED
   T_{c⁻¹}ψ₂ ≠ ψ₂, i.e. iff
   some k < g₂ has ψ₂_k ≠ 0 and z̄₂^{θ₂(g₂−k)} ≠ 1 — the TWIST-VISIBLE
   states (measured: exactly the 22/24 visible towers diverge, the 2 blind
   coincide). The LITERAL polynomial T_{c⁻¹}ψ₂ additionally carries the
   non-monic leading coefficient z̄₂^{−θ₂g₂}; the probe's sole RED was a
   sealed display clause conflating the two comparisons — both forms are
   displayed here and both are S8 falsifier rows (K).
1. **Label tie (GMN-lawful key).** ψ₃^F := R₃^F(Φ₃) is the minimal
   polynomial of z₃ by Cor 5.6(2) read in the EXTENDED chain
   (Φ₀, Φ₁, Φ₂, Φ₃) — consuming **(KP_i)**, exactly as the parents at their
   state keys. The Theorem at f = Φ₃ (monic, deg Φ₃ = g₂·deg Φ₂,
   w₃(Φ₃) = g₂γ₃): top Φ₂-digit A_{g₂} = 1 with w₂ = 0, ordinate
   u = g₂·w₂(Φ₂), on-line (u + g₂h₂ = g₂γ₃); the digit 1 has s₂(1) = 0,
   R₂(1) = 1, so t₂(g₂) = (0 − ℓ₁g₂·e₁g₁γ₂)/e₁ = −θ₂g₂ and
   c^G3_{g₂} = z̄₂^{−θ₂g₂} — at e₁ ≥ 2 the leading coefficient is the
   explicit character value, NOT 1 (the TR3-S2 step-1 scalar pin one level
   up); lawfulness forces s₀(Φ₃) = 0 and

       R_λ₃(Φ₃) = z̄₂^{−θ₂g₂} · ψ₂     (lawfulness R_λ₃(Φ₃) ∼ ψ₂ + ψ₂ monic
                                       + the pinned leading coefficient
                                       z̄₂^{−θ₂g₂} force EQUALITY with the
                                       scalar times the UNTWISTED ψ₂ — NOT
                                       step 0's twisted class; REV 1
                                       orientation fix),
       σ₂( ψ₃^F )(y) = z̄₂^{−θ₂g₂} · ψ₂( z̄₂^{θ₂} y ) = z̄₂^{−θ₂g₂}·(T_cψ₂)(y).

2. **Lemma ORD-INV-LIFT (matched-label transport).** σ₂ extends to a ring
   iso F₃[y] → K₂[y]; T_c is a K₂[y]-automorphism carrying (ψ₂) to (T_cψ₂)
   (irreducibility preserved); nonzero scalars change no ψ-adic order.
   Chain, for every nonzero f:

       ord_{ψ₂}( R_λ₃(f) )
         = ord_{T_cψ₂}( T_cR_λ₃(f) )                       [T_c automorphism]
         = ord_{z̄₂^{−θ₂g₂}T_cψ₂}( z̄₂^{θ₂s₀(f)}T_cR_λ₃(f) ) [scalars]
         = ord_{σ₂ψ₃^F}( σ₂R^F₃(f) )                       [step 1 + (TR3-S4-form)]
         = ord_{ψ₃^F}( R^F₃(f) ).                          [σ₂ iso]

   Consumers must key each side by its OWN key read (matched labels): the
   naive shared-ψ₂ pairing CAN FAIL exactly where the classes diverge (the
   twist-visible states; the probe's M8 row measured 142 naive failures,
   report-only, against 0/1,656 matched-label failures P5′).
3. **FGMN-internal leg.** ord_{ψ₃^F}(R₃^F(f)) = ord_L(R(f)) is Cor 5.15
   with the **[T]-pinned properness** of the state key — NOT discharged
   here, exactly as in all three parents.

**Hence TR-3-ORD-X holds at order-3 e₀ = 1, e₁ ≥ 2, e₂ = 1 states — with
the GMN-lawful key convention displayed at steps 0–1 — conditional on
(KP_i) + [T]-properness (+ this note's own attribution stack, S6.1). With
the parents: TR-3-ORD-X now covers {all order-2} ∪ {order-3, e₀ = 1,
e₂ = 1, all e₁} under the same inherited conditionality.** Consumers GD3-MIN
Route B and ALL clause-(4) ord-consumers: served at these states with the
matched-label discipline (ORD-INV-LIFT); elsewhere they continue to sit on
OPEN TR-3-ORD-X.

### S6.3 B-M2 OL-1(b), GMN-verbatim face

The TR-3-ORD-X seam of B-M2's OL-1(b) extends to order-3 e₀ = 1, e₁ ≥ 2,
e₂ = 1 states (with (KP_i) + [T] + this note's attribution stack inherited).
OL-1(b) remains conditional on its OTHER displayed seams everywhere, and on
TR-3-ORD-X beyond {order ≤ 2} ∪ {order 3, e₀ = 1, e₂ = 1}. No other B-M2
clause is touched.

## S7. Scope box — what this unit does NOT prove (the charter's fences)

* **e₀ ≥ 2 stacked (E₃ = e₀e₁) — HARD FENCE.** Lemma A-GEN-3's citation
  requires the substate's e₀ = 1 (TR3-S1); at e₀ ≥ 2 the inner digit
  identity becomes TR3-S2's TWISTED law and the z₁-character rides INSIDE
  the R₂ evaluation — the probe note's NOT-touched display predicts the
  discrepancy is then NOT a pure z₂-character. NO candidate is sealed here;
  that stratum is probe-first (the [M]-row protocol).
* **e₂ ≥ 2 (live level-3 carry) — HARD FENCE.** Lemma KER-13's level-3
  clause dies (s(γ) ≠ 0, stride e₂, live ℓ₂), the level-3 W-MULT carry
  δ becomes nonzero (the probe's V3 row was the degenerate control), and
  Lemma HV3-E2 would additionally need... nothing (v₃ reads level-≤ 2 data
  only) — but the ASSEMBLY (S5) is e₂ = 1-specific throughout. Probe-first,
  no candidate sealed.
* **Orders ≥ 4**: the recursion is not written; TR3-S1's acceptance covers
  order 2 and the trilogy + this unit stop at depth 3.
* **D-ITER / the composite digit algebra — UNTOUCHED.** On this stratum the
  composite algebra has rank e₁ > 1, but nothing here consumes ITER-LAW,
  composite anchors, or cocycle tables; the whole argument lives at the
  residual-OPERATOR level (the TR3-S2 hard-fence discipline).
* **Lemma HV3-E2's status**: proved HERE at attempt grade (S3 status box);
  accepted HV3-THM does NOT cover e₁ ≥ 2 and nothing of its acceptance is
  borrowed beyond the pinned displays it shares. A failed HV3-E2 would
  strip the GMN-attribution of R_λ₃ (the identity would survive as the
  project-facing statement), exactly the TR3-S3 (H-v3) exposure shape.
* **(H-ℓ) normalization scope** — as in all parents (S1 note); ord
  conclusions survive any fixed choice.
* **(KP_i) and [T]-properness** — consumed at S6.2 steps 1/3, not
  discharged.
* **(PIN-GEN-i)** — inherited attribution display (S1); its extraction leg
  discharged at 303b11e; on THIS stratum the corrected ǫ₂ is consumed
  load-bearingly, with the probe M1 row + S8 falsifiers as cover.
* **GMN's printed base scope** (finite extensions of ℚ_p) vs the project
  DVR engine convention: as in the parents; the falsifiers cover equal
  characteristic against exactly that reading.

## S8. Falsifier record (2026-08-08 campaign window)

### S8.1 Recorded: the TWIST-3 probe (prior to this unit; sealed 76513f8, run 48c07b4)

`verification/openmath/strata3e2_probe.py` (seed 20260818): C1 (TWIST-3)
0/1,656 (2,274 on-line comparisons); P2 (level-3 A-GEN, live ǫ₂) 0/2,274;
P3 (TR3-S1 on digits) 0/2,274; P4′ primary (twisted key class) 0/24; P5′
(matched-label ord) 0/1,656; M6 naive-identity refuted 710/710 visible
slots; M7 marched increment 504/504; M1 reading-P refuted 1,277/1,277;
CERT2/CERT3 (first RAMIFIED order-3 PARI keys) 0 violations; 23,733 samples.
Honest RED on record: the P4′ SECONDARY display clause (10/24), classified
as a transcription slip in the probe note §S2 — this note's S6.2 step 0
displays both corrected forms, and row K below seals them as predictions.

### S8.2 NEW: the preregistered composed-identity family (two-commit seal)

`verification/openmath/tr3s4_check.py` (seed 20260819, fresh; predictions
sealed in the script header BEFORE the run; independence perimeter stated
there). FRESH beyond the probe: a fresh FGMN pipeline (R2w_at at stride e₁
reading ARBITRARY weights — off-line slots computed HONESTLY, so Lemma
Z2-e2 is a tested prediction; fresh_R3 at i = 3), the route decomposition
as rows, the HV two-way v₃ row (Lemma HV3-E2's face: geometric (E4)-min
with exact fractions vs the MacLane operator + ordinate expansion), the
KEY0/ORD-INV rows in BOTH forms, a prescribed-value two-sided W family, and
a FRESH OFF-ROSTER tower roster (4 rows × 4 rings, none equal to a probe
row; design constants disclosed in the header, tower constants only).
Rows: E2A (the law per-abscissa + support), E2B (two-form as lists:
side[k] = z₂^{θ₂(s+k)}·gco[k], j₀ = s), RI (dead-level kernels + live-ǫ₂
teeth), RII (A-GEN-3: inner TR3-S1 + m₀ law), RIII (EXP-3 as an INTEGER
identity + t₂ integrality + the field form), RIV (recomposition from
level-2 GMN data only), Z2 (constructed off-line vanishing + own-weight
cross-validation), HV (two-way v₃ + ordinates), XV (fresh vs probe FGMN),
W (prescribed two-sided values, noise/holes), K (V4 key lift; P4′ primary;
class + literal divergence criteria), O (matched-label ord transport +
naive-pairing report), CERT (G.T1_T2/T5/T11 + CERT3 PARI per tower). All
predicted 0; meters (m0pos, t2nz, epsnz, vis, identkill = vis, j0pos, noff,
noisy, holes, ordhi) must fire.

**Provenance.** Preregistration seal commit and first-run record commit are
cited in S8.3 below (appended at the run commit per the two-commit
protocol; the TR3-S3 pass-1 gap-4 lesson).

### S8.3 First-run record (the two-commit protocol, executed)

**Provenance.** Preregistration seal = commit **7fa8825** (this note + the
sealed `tr3s4_check.py`, committed BEFORE the first execution); first-run
record = commit **79eaee6** (the commit of even date carrying this block +
the outputs `tr3s4_check_output.txt`/`tr3s4_check_results.json`; hash
displayed at REV 1 — the S8.2 promise now discharged in text). ZERO
harness amendments; rerun output identical mod the four timing lines, JSON
byte-identical.

RESULTS (first full run on the sealed predictions, seed 20260819, **exit 0,
4.3 s**; log `tr3s4_check_output.txt`, JSON `tr3s4_check_results.json`):

    E2A  per-abscissa TWISTED identity + support     0/900     GREEN
    E2B  two-form as lists (twisted tie) + j0 law    0/400     GREEN
    RI   kernels: lvl-1/3 dead, (SLOT-2') integral   0/9,267   GREEN
    RII  A-GEN-3: inner TR3-S1 + m0 law              0/536     GREEN
    RIII EXP-3 integer identity + t2 + field form    0/536     GREEN
    RIV  recomposition from level-2 GMN data         0/536     GREEN
    Z2   off-line vanishing + own-weight cross-val   0/384     GREEN
    HV   two-way v3 (geo Fractions vs w2) + ordinates 0/160    GREEN
    XV   fresh vs probe FGMN pipeline                0/400     GREEN
    W    prescribed two-sided values (noise/holes)   0/80      GREEN
    K    key rows: V4 + P4' + BOTH divergence forms  0/64      GREEN
    O    matched-label ord transport (kG vs psi2)    0/400     GREEN
    CERT external/integrity legs (incl. PARI)        0/16      GREEN
    METERS all fire: m0pos 87, t2nz 481, epsnz 340 (the revival live),
      vis 130, identkill 130 == vis (naive IDENT-3 refuted at EVERY visible
      slot), j0pos 72, noff 269 (honestly-computed off-line slots — Lemma
      Z2-e2's face), noisy 48, holes 34, wfail 0, ord histogram
      0:366 1:19 2:15 (ord >= 1 present), naive shared-psi2 ord mismatches
      27 (REPORT row — the matched-label discipline's teeth), design check
      12/16 visible == the disclosed table.
    TOTAL 13,679 samples, 0 violations — ALL GREEN, first run as sealed.

A red on E2A/E2B falsifies S5; RI falsifies Lemma KER-13/(SLOT-2′); RII
Lemma A-GEN-3 (and accepted TR3-S1 at the substate); RIII Lemma EXP-3 (both
as integers and in the field); RIV the S5(ii) recomposition; Z2 Lemma
Z2-e2; HV Lemma HV3-E2 (its geometric leg is the falsifier's only
v₃-reading decorrelated from gmn3's hard-coded ordinate); W the theorem
against a two-sided a-priori prescription; K the S6.2 step-0/1 displays
(both divergence forms — the P4′-secondary lesson sealed GREEN this time);
O the ORD-INV-LIFT transport. Note row-O's independent falsification power
is limited by E2B (equal twisted lists force the ord tie once labels are
matched) — its live content is the kG-keying itself and the histogram; the
transport's falsification lives in E2B, as in TR3-S3's REV-2 display.

### S8.4 The F2c acceptance obligation (displayed, standing rule)

Per the pass-7 F2c standing rule (three verifier-owned implementations on
the TR3-S3 arc; promoted to the acceptance floor): **the acceptance arc of
THIS note must include at least one VERIFIER-OWNED from-note implementation
on off-roster instances** — implement the two sides from the note's
displays alone (fresh field models, own pipelines), preferably at off-roster
primes (p ∉ {2, 3}), and test (TR3-S4-coeff)/(TR3-S4-form) plus the HV
two-way v₃ leg. The prover-side harness of S8.2 does NOT satisfy this
obligation (shared blind spots); it is displayed here as the acceptance
gate, not claimed.

## S9. Honesty ledger and attack surface for the hostile passes

1. **Lemma HV3-E2 is the largest exposure** — the only new print
   instantiation. Attack: re-read (E1)–(E4) from the pinned TeX at r = 3
   and check the three tallied e₁-sites (step 2's e₂^{GMN} = e₁ and
   |λ₂| = h₁/e₁; step 3's e₁-distribution through the min; step 4's E₂ = e₁
   dilation); check the (E1) intercept's ℤ_{≥0} codomain typing is not
   load-bearing at e₁ ≥ 2 (it is consumed only through (E4), whose output
   here is an integer min — display in S3 step 1); check no other consumer
   of v₃ hides in Defs 2.19–2.21's r = 3 instance.
2. **The Lemma A-GEN-3 citation discipline**: TR3-S1 applied to the
   substate — verify its stratum is e₀ = 1 with e₁ genuinely FREE, its
   totality (no degree bound), j₀ = m₀ at 0 ≤ s(β) < e₁, and the inner
   operator match (S4's displayed paragraph).
3. **The S6.2 display bookkeeping** — the probe's only RED lived exactly
   here (leading-coefficient vs class comparisons). Verify Lemma KEY0-LIFT's
   two forms against row K's implementation and the t₂(g₂) = −θ₂g₂ scalar
   pin.
4. Lemma Z2-e2's negative-weight and zero-digit corners; the domain
   direction (β < w₂(A) is INSIDE P_β(µ₂)).
5. Index dictionary at depth 3 (γ₃ = e₂w₂(Φ₂) + h₂; GMN's t₂ uses
   GMN-level-2 = project read-ν₁ data); an off-by-one shifts every slot
   weight — cross-checked by XV/RII/RIII and the probe's V-rows.
6. Everything consumed from the parents is at their ACCEPTED states
   (TR3-S1 REV 2; TR3-S2 REVISION 1; TR3-S3 REVISION 4 + HV3 bracket;
   HV3-THM at fa2180e); nothing from the superseded laws or the retired
   artifact ǫ. The θ₂ display uses the normalized ℓ₁ only.

— TR3-S4 composed 2026-08-08 (wave 15); the falsifier run record follows in
S8.3 at the run commit; hostile passes NOT yet run — the 2-clean
model-diverse acceptance bar starts under the orchestrator, with the S8.4
F2c obligation displayed as part of the bar.

---

## REVISION 1 (2026-08-08) — response to hostile pass 1 (GAPS-ONLY); acceptance counter stays 0/2

Pass 1 (Codex; `lean/notes/openmath/TR3S4_pass1_report.md`): VERDICT
GAPS-ONLY — 0 critical / 2 gaps; "The coefficient theorem survives hostile
checking … Neither flaw breaks the main TR3-S4 chain." Verified in full by
the same pass and BYTE-UNTOUCHED here: Lemma KER-13, Lemma A-GEN-3 (incl.
the citation discipline and totality), Lemma Z2-e2 (negative weights, zero
digits, domain direction), Lemma EXP-3, the per-slot t₂-absorption /
support equality / evaluation nonvanishing / y^s normalization / two-form
(S5), Lemma HV3-E2 (all three e₁-sites confirmed against the primary GMN
print), the σ₂ construction and monicity upgrade, all fences, the (H-ℓ)/
PIN-GEN-i/(KP_i)+[T] conditionality placement, and the 13,679-row
reconciliation. The two gaps and their fixes:

1. **GAP — "S6.2 conflates the two opposite key-normalization
   orientations"** (quoted: "R_λ₃(Φ₃) = z̄₂^{−θ₂g₂}·(monicized twisted
   class)" — substituting step 0's definition of the monicized twisted
   class (= z̄₂^{θ₂g₂}T_{c⁻¹}ψ₂) would make the right side T_{c⁻¹}ψ₂,
   generally NOT associate to ψ₂, contradicting Φ₃'s GMN lawfulness; and
   the reversed summary "the GMN-lawful level-3 key class is the
   θ₂-TWISTED ψ₂ class", which is what step 0 proves of the
   FGMN-NORMALIZED lift). FIXED at five sites, orientation kept distinct
   throughout: step 1's display now reads the verifier's correct
   calculation **R_λ₃(Φ₃) = z̄₂^{−θ₂g₂}·ψ₂** (lawfulness + monic ψ₂ + the
   verified scalar pin t₂(g₂) = −θ₂g₂ force the scalar times the UNTWISTED
   ψ₂); step 0's conclusion sentence and the divergence-criterion
   parenthetical now attribute the T_{c⁻¹}ψ₂ class to the FGMN-normalized
   lift and the ψ₂ class to the GMN-lawful key; the S0 table row and the
   S6.2 conclusion's step pointer follow. UNCHANGED because already
   correct (per the same finding): step 1's σ₂(ψ₃^F) = z̄₂^{−θ₂g₂}·T_cψ₂
   display, the entire ORD-INV-LIFT chain (it consumed the correct T_cψ₂
   label — "the order conclusion remains valid"), the class/literal
   divergence criteria, and row K/O of the harness (row K tested step 0's
   form and the scalar pin; row O's k_G matches the FGMN-side literal
   label, its own correct orientation). NO falsifier change, hence no
   ADDENDUM and no rerun: the error lived only in the note's step-1
   display line and its echoes.
2. **GAP — "the first-run seal hash is absent from S8.3"** (S8.2 promises
   both commits "cited … below", but only 7fa8825 appeared). FIXED: S8.3's
   provenance paragraph now displays first-run record = commit
   **79eaee6** alongside preregistration seal **7fa8825** (verified
   against the git log: 79eaee6 carries this note's S8.3 block +
   `tr3s4_check_output.txt` + `tr3s4_check_results.json`).

The verified algebraic perimeter (S2–S5 lemma proofs, KER-13, A-GEN-3,
Z2-e2, EXP-3, the t₂-absorption chain, Lemma HV3-E2, the fences) and the
falsifier record (13,679/0; seals 7fa8825/79eaee6) are textually untouched
by this revision beyond the two pinned sites. Counter stays 0/2; passes
2+3 decide under the standing division-of-labor discipline; the S8.4 F2c
verifier-owned implementation REMAINS the acceptance obligation.
