# H8 blueprint adversarial review (2026-08-05) — Codex fresh-context verification

**Campaign**: the hypothesis-discharge swarm (Asvin directive 2026-08-05,
ledger 5a624c3). **Target document**: `lean/blueprints/HDISCHARGE_H8.md`
(pasted in full into the prompt; Codex sandbox cannot read the repo).
**Charge**: hostile fresh-context reviewer — faithfulness to the quoted (H8)
row / M04 Lemma 3.3, mathematics (granularity, first-entrance combinatorics,
(BDY) shift, ENNReal regrouping provability), Lean-statements-vs-prose,
residual pricing, process; quote-and-classify CRITICAL vs GAP vs ADVISORY;
falsifier-first; fix nothing. **Prompt/raw output**:
`/tmp/openmath/H8_bp_prompt.txt` / `/tmp/openmath/H8_bp_review.jsonl`.
**Codex budget at launch**: 7-day window 2% used.

## VERDICT: REVISE (5 CRITICAL / 3 GAP / 4 ADVISORY)

## Adjudication (H8 lead, from the report text only)

FOLDED IN FULL — the revised blueprint is the unit of record; disposition
table at its §7. The five criticals share one root cause: the blueprint's
uniqueness/reduction claims outran what its waves CONSTRUCT (no extraction
unit for (Ŝ,(D_i)); bare Prop seam fields cannot supply the CutData Equiv).
Cure: (K-CUT-1) split into ownership-partition (1a) vs decomposition-data
(1b) levels; NEW HARD unit H8-B3 `CutExtract.lean` (concrete shallow-layer +
block-derivation carriers, cut map, injectivity, conformance, countability;
compiled (a)/(b) carrier design gate); H8-C1 bridge re-scoped through B3 +
typed seam re-keys; §1.3/§6 residual re-displayed (3 hypothesis fields + the
B3 construction layer); the G-6 strictness sentence and G-7 countability note
corrected; G-8 present-tense "PROVES" demoted to probe-only status. C-3
partially REBUTTED: bare-Prop seam fields are the corpus's deliberate
theoremU/BP_VI convention — folded as a binding display rule (UNPINNED-until-
typed + named re-key targets) rather than typing fields ahead of their
carriers. The design core SURVIVED: A-10 (first-entrance calculus, partition,
outermost-block rule, ProperPrefix (BDY) shift — "no off-by-one defect"),
A-11 (fiber_count_cut derivation), A-12 (the §2.5 pre-registered disposition
consistent with G-2) are explicit confirmations; A-9 confirms the granularity
adjudication with a proviso, folded. No probed Lean statement changed.

## Verifier report (verbatim, unedited)

1. **“Choice-freeness: `minEnts`/`blockReads`/`shallowReads` are `Finset.filter`s of decidable-from-E predicates — FUNCTIONS of (T, E), so the decomposition is unique by construction, and ‘two different shallow decompositions of one tree’ … is structurally dead.”** — **CRITICAL** — Unique read ownership does not establish uniqueness of the shallow-layer data or of the associated grammar derivations; two encodings can induce the same filtered partition.

2. **“After §4’s provable units land, (H8)’s open content is EXACTLY three fields (the Lean surface `VTreeCutSeam`, unit H8-B2).”** — **CRITICAL** — The units never construct the full-data shallow family, extract grammar derivations from trees, prove interface/verdict conformance, or establish countability for that concrete family, so the claimed three-field remainder omits essential forward-cut content.

3. **“`supply_cell : Prop` … `graft_complete : Prop` … `mass_tie : Prop`”** — **CRITICAL** — These unconstrained proposition-valued fields contain no typed predicate, graft equivalence, or mass equation and can all be instantiated as `True`; their names and comments impose no Lean-level semantics.

4. **“Lean-side it is the `cut` Equiv’s surjectivity inside `CutData` (unit H8-A3) — supplied at instantiation time by the `VTreeCutSeam.graft_complete` field.”** — **CRITICAL** — A proof of the opaque proposition `S.graft_complete` cannot supply an `Equiv`; moreover, `CutData.cut` includes the entire forward map and injectivity, not merely graft surjectivity.

5. **“GIVEN a `VTreeCutSeam` whose fields hold and the wave-1/2 theorems, a `CutData` instance exists for the intended carrier.”** — **CRITICAL** — This reduction is not derivable from the displayed seam, and its statement is expressly left unpinned; consequently there is no formal bridge from the proved combinatorics to the M04 consumer.

6. **“The cut direction (K-CUT-1/2/3) alone gives … ≤ …; missing tuples would make the inequality strict.”** — **GAP** — Missing tuples need not make an ENNReal inequality strict: they may have zero weight, or both sides may already equal `∞`; both directions are needed for the claimed bijection and uniform equality theorem, not for strictness in every instance.

7. **“NOTE (probed): … countability instances ride `ctblS`/`ctblD`.”** — **GAP** — `ctblD` does not produce `Countable V` or countability of the verdict-tuple index; `seriesSum_cut_conv` is mathematically valid, but its Lean proof must explicitly handle the countable support of attained verdicts or use unrestricted ENNReal fiberwise Tonelli.

8. **“Every Lean statement in §4 was compile-probed green … sorry-warnings only, zero errors”** together with **“(i) PROVES in Lean the pure-combinatorics legs.”** — **GAP** — A compile probe containing `sorry` establishes only well-typedness, not that any theorem has a proof artifact; the later conditional landing language is honest, but the present-tense “PROVES” claim is unsupported.

9. **“At this granularity (‡) is FALSE as a bijection … and (†) is FALSE per-T … [whereas at full-data granularity they are] literally true-formable.”** — **ADVISORY** — The adjudication is mathematically sound provided forgotten heights are absent from all remaining tuple data; “true-formable” should not be read as “proved,” since factorization still depends on the residual mass and graft semantics.

10. **“The proper prefixes of a chain H are linearly ordered … existence and uniqueness of the first entrance.”** — **ADVISORY** — The first-entrance arguments, ownership partition, outermost-block rule, and ProperPrefix-based boundary shift are correct; no off-by-one defect appears in the displayed definitions.

11. **“`fiber_count_cut`: `treeExp` … + `siteExp_cut` + `pow_add`/`Finset.prod_pow_eq_pow_sum`.”** — **ADVISORY** — The displayed count identity does follow from `treeExp` and the partition by ordinary finite exponent algebra, assuming the quoted `treeExp` theorem has the asserted total-site exponent form.

12. **“only outcome (vi-a)=PASS retires the row … the expected outcome ((vi-a) FAIL, (vi-b) split) yields the §6 RE-SCOPE display instead.”** — **ADVISORY** — This pre-registered disposition agrees with G-2’s quoted conclusion that D-14 plus CU-3 does not establish CUT-WD or graft completeness.

VERDICT: REVISE
