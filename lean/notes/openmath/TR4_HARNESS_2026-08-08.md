# TR-4 HARNESS (2026-08-08) — the factorization/bijectivity falsifier family; the GRB leaf's displayed empirical debt discharged

**Unit charter.** SYNTHESIS_PASS4 item 8 / SYNTHESIS_PASS5 F6 (the small-units
dispatch): "TR-4 harness family (Cor 4.5/4.11 factorization/bijectivity —
currently NO family) — cheap numerics-fleet item, closes the leaf's displayed
evidence gap." The debt of record: GRB_ORDER2_2026-08-05.md REV-12 GAP 1 /
REV-13 GAP 4 and the R11.5/R13.4 residue taxonomy — "TR-4's Cor-4.5/4.11
factorization/bijectivity content ↔ NO listed family (paper-secured only)":
the N-1 battery's T2 checks two-form/support, NOT factorization or
bijectivity.

## 1. The TR-4 statement tested (GRB S4, retargeted per Lemma RESCALE)

For the order-2 residual operator of record R_{2,γ} (FGMN Def 3.16 with the
corrected Def 3.15 twist, reading C — the operator pinned by N-1's
T0/P3/P12), in integer weights:

* **(Cor 4.4(1))** vanishing detects exactly the coset: for f ≠ 0 with
  w₂(f) ≥ γ: R_{2,γ}(f) = 0 ⟺ w₂(f) > γ (f ∈ P_γ⁺).
* **(Cor 4.5)** the operator factors through the initial form H_µ(f): for
  nonzero g, h with w₂(g) = w₂(h) = γ: w₂(g−h) > γ ⟺ R_{2,γ}(g) = R_{2,γ}(h)
  (both directions — well-definedness AND injectivity of the induced map).
* **(Cor 4.11)** the induced map R^gr : P_γ(µ)/P_γ⁺(µ) → K₁[y] is bijective
  (total, injective, onto).

**Convention note (ORD-INV interaction: none).** TR-4 is convention-internal:
one tower, one state key Φ₂, one operator — no cross-convention
(FGMN-normalized vs GMN-lawful key) comparison occurs, so every check is
automatically in the matched-key form; the ORD-INV state-key bifurcation law
(TR3-S2 arc) is not engaged. Likewise the family does NOT discriminate the
C/P twist readings — a per-slot UNIT rescaling ε₁(γ_j) preserves
factorization/bijectivity content; C-vs-P discrimination is N-1's
T0/P3/P11/P12, already on record. What this family covers, for the first
time, is the factorization/bijectivity CONTENT itself, on the operator of
record.

## 2. The harness

`verification/openmath/tr4_check.py` (output log `tr4_check_output.txt`),
the TR3-S1/S2 genre: seed, families, and violation-count predictions
committed in the script header BEFORE running. Seed 20260817 (fresh; N-1
20260808, TS1 20260812, strata 20260815, TS2 20260816). Perimeter: the FULL
N-1 roster A–J (no stratum restriction — TR-4 has none) over all four base
rings {ℤ₂, ℤ₃, F₂[[t]], F₃[[t]]} = 40 exact order-2 towers, reading C.

Machinery: grb_order2_check's Tower objects reused (w₁/w₂, R1w_at_z1, eps1,
R2w, realize1). New and independent legs: `R2gamma(f, γ)` — Def 3.16
transcribed at a FIXED weight γ (the N-1 operator `Tower.R2w` evaluates only
at γ = w₂(f), so the coset-vanishing half of Cor 4.4(1) was untestable with
it); `realize_target` — the build_Phi2 anchor recipe generalized to an
arbitrary target q ∈ K₁[y] (the surjectivity witness constructor); and the
pairwise-difference class tests, which consume ONLY w₂ (no residual-operator
reuse — the non-circular injectivity leg).

## 3. Preregistered predictions vs observed (the run of record)

All predictions: 0 violations. Run: 2026-08-08, exit 0.

| family | content | pred | obs (samples) | verdict |
|---|---|---|---|---|
| TR4a | fixed-γ transcription tie + R_{2,γ}(f) ≠ 0 at γ = w₂(f) (Cor 4.4(1) "only if") | 0 | 0 (560) | GREEN |
| TR4b | coset vanishing: w₂(f) > γ ⟹ R_{2,γ}(f) = 0 (Cor 4.4(1) "if") | 0 | 0 (940) | GREEN |
| TR4c | R2w(g + above-line noise) == R2w(g), weight preserved (Cor 4.5 ⟹) | 0 | 0 (560) | GREEN |
| TR4d | the Cor 4.5 equivalence on same-weight pairs, both branches | 0 | 0 (800) | GREEN |
| TR4e | surjectivity: arbitrary targets deg ≤ 4, arbitrary support (incl. q₀ = 0), random s(γ), realized and read back through BOTH operator legs (Cor 4.11 onto) | 0 | 0 (480) | GREEN |
| TR4f | exhaustive finite-window bijectivity: ALL nonzero q, deg ≤ 2, on the 26 towers with |K₁| ≤ 4; read-back + pairwise w₂(g_q − g_q′) == γ | 0 | 0 (15,886) | GREEN |

**TOTAL: 19,226 samples, 0 violations, ALL GREEN.**

Non-vacuity/consistency checks inside the run: TR4d branch counts — R-equal
403 (240 constructed same-class + 163 genuine random collisions, so the
injectivity branch fired on independent samples too), R-unequal 397. TR4f
window count 26 with N ∈ {7, 26, 63} = |K₁|³ − 1, and the printed sample
total 15,886 equals Σ_windows (N + N(N−1)/2) exactly (2·[5·28 + 3·2016] +
2·[5·351]) — the enumeration is complete, not truncated.

## 4. What this changes (and what it does not)

* **DISCHARGED: the GRB leaf's TR-4 EMPIRICAL debt** (the R11.5/R13.4
  open-lemma-list rider "no harness family, paper-secured only"). The
  Cor-4.5/4.11 factorization/bijectivity content now has a listed,
  preregistered family: TR-4 ↔ TR4a–f (this unit), extending the N-1 family
  ledger mapping (TR-7 ↔ T3; TR-1/TR-2 ↔ T1 + G3; CARRY-1/D-REAL ↔ T4/T10;
  (GR-B) clauses ↔ T2/T5–T8).
* **UNCHANGED: TR-4's mathematical status.** It stays PAPER-SECURED — pinned
  to the printed Cor 4.4(1) (fgmn.txt:1516), Cor 4.5 (fgmn.txt:1527),
  Cor 4.11 (fgmn.txt:1616) — exactly as the S4 discharge records; a GREEN
  falsifier run is instance evidence, not a proof, and no proof claim is
  made or needed (the printed theorems carry it).
* **UNCHANGED: everything else in the GRB residue** — TR-3′-GEN closed at
  order 2 only (orders ≥ 3 open), TR-3-ORD-X conditional, D-ITER open at
  E_i > 1 with its measured ITER-LAW, (KP_i)/(H-CHAIN)/[T] external.
* GRB note sync: a dated one-line bracket at the R11.5 OPEN-LEMMAS bullet
  and the final residue-of-record display, pointing here; acceptance
  counters NOT reset (post-acceptance annotation, the TR3-S1 precedent —
  no accepted claim weakened, one debt line updated to its discharge).

— TR-4 harness unit, 2026-08-08.
