# GENTOW3 passPE2 — SECOND HOSTILE PASS (2026-08-10)

**Target:** `lean/notes/openmath/GENTOW3_PROOF_2026-08-09.md` at HEAD
(r1 round complete at e1a8dfe; [GT3-r1] gauge annex S7.1 + MINOR
repairs S7.2–S7.4 + manifest S7.5).
**Arc:** seal → passPE1 NOT CLEAN 1 GAP + 3 MINOR (3101cf5) → r1
(cda85ee / 5396bbe / e1a8dfe) → this pass. Counter 0/2 entering.
**Protocol:** quote-and-classify, fix nothing. Fresh context, zero
prior stake. PE1's route (the Θ_j re-derivation, the three-frame
485/0 instrument, the supplier census) is NOT re-walked; this pass
stressed (1) the r1 gauge annex by independent re-derivation,
(2) every attainment sentence against the PE1-measured FR-M3 slack,
(3) the supplier stack as of today's read, (4) a free hunt, with a
fresh machine route disjoint from PE1's frames.

## VERDICT: NOT CLEAN — 1 GAP (F-1). Counter stays 0/2.

The r1 gauge annex SURVIVES independent re-derivation (the x-free
equivalence's exact scope confirmed from scratch, including a leg
the annex only cites: π-integrality of the (LIFT) form on 𝒯 follows
from the [r1] node floor itself). The sealed and PE1 machine legs
re-ran bit-identical in isolation. The fresh route (compound frame
μ₂ = 3 AND η ≠ 1 — the first such frame anywhere — plus the j = μ₂
top, the δ = 0 corner, and a constructed count-law member) is GREEN
149/0 with all preregistered exact values confirmed. The GAP:
three summary/record sentences (S0 geography, S4 δ = 1 bullet,
S4 count-law) assert UNCONDITIONAL per-coordinate attainment /
mislabeling that THEOREM GENTOW-3(iv) only proves under its
per-coordinate certificate — and the PE1-measured FR-M3 slack plus
a constructed, PARI-checked member REFUTE the unconditional
readings. The theorem statements, COR 3.C, and the honesty boxes
carry the correct content; the defect is confined to the summary
sentences and one undefined term ("touched coordinate(s)").

---

## FINDINGS

### F-1 (GAP — attainment stated certificate-free in three record
### sentences; refuted by the measured FR-M3 slack + a constructed
### member)

QUOTES:
(a) S0 (THE GEOGRAPHY, L48–53): "at overflow genres it fails AT
THETA_j (attained by the key power's own shadow)" — announced as
"(proved in S2)"; "overflow genre" is defined nowhere.
(b) S4 δ = 1 bullet (L288–289): "Θ_j = the node floor: the FIRST
leaf height is already unfaithful." and (L292) "no faithful leaf
band exists."
(c) S4 count-law (L304–308): "strata priced through shadow pins
are mislabeled exactly on the members whose deciding heights reach
Θ_j — at δ = 1 that is the entire leaf population".
(d) S2 (iv) statement (L148–155): "carries a nonzero digit at
exactly THETA_j at the touched coordinate(s)" — "touched
coordinate(s)" undefined in the statement.

CLASSIFICATION: GAP (one cure: certificate-scope the three
sentences and pin "touched coordinate(s)" to the per-coordinate
certificate — the honest content already exists in the (iv) proof
("NONZERO whenever the assembled K2-digit does not cancel") and in
GENTOW3-BOX-1 ("the key power's height-Θ K2-digit ≠ 0")).

THE REFUTING DATA (both committed):
* PE1's kept measurement (gentow3_pe1_fresh output, 94160b22):
  FR-M3 (p = 2, Phi2 = x²−2x−2, μ₂ = 3, δ = 1, Θ = [10, 7, 4])
  key-power self-shadow pins [12, 7, 4]: at j = 0 the key power's
  own shadow attains 12 > Θ₀ = 10 — quote (a)'s parenthetical
  ("attained by the key power's own shadow") is false at that
  coordinate, and no witness of failure AT Θ₀ exists in any
  committed leg.
* This pass's constructed member (P2-3, sealed prediction, GREEN,
  dual-oracle): f = Phi2³ + 32 at FR-M3 — 32 = π⁵ sits at slot
  height 10 = Θ₀ = the node floor, x-free monomial, so its own
  division discrepancy is zero and the member's total discrepancy
  is exactly the self-shadow ([12, 7, 4], all ≥ Θ). Honest read:
  pins [10, ∞, ∞], polygon = single edge (0,10)–(3,0), decided
  (slope 10/3), PARI σ {(6,1)}. Shadow read: pins [10, 7, 4], the
  fake pins (1,7)/(2,4) lie strictly ABOVE the edge (line values
  20/3, 10/3) — hulls IDENTICAL, vertex digit at (0,10) IDENTICAL
  (96 = 2⁵·3 → residue 1 = 32 → residue 1). A δ = 1 LEAF member
  whose deciding height REACHES Θ₀, read identically by both
  frames on every consulted datum: NOT mislabeled at any stratum
  granularity. This refutes (c)'s biconditional ("mislabeled
  exactly on… the entire leaf population") and (b)'s "the FIRST
  leaf height is already unfaithful" as δ = 1-regime claims.
  ((c) is also refuted at σ-granularity inside the anchor family
  by the note's OWN S1 row: "PE1-0 … σ both {(8,1)} = parity
  luck".)
* Quote (d)'s live ambiguity: at FR-M3 j = 0 the shadow COORDINATE
  is nonzero (ShC₀ = 64, pin 12) — "touched" in the plain sense —
  but its digit is NOT at Θ₀: under the plain reading (iv) itself
  is false at FR-M3; under the certificate reading (touched = the
  height-Θ digit does not cancel) it is correct. A measured
  instance now separates the two readings, so the statement needs
  the definition pinned.

FAILURE SCENARIO: a consumer prices δ = 1 σ-strata from S4's
count-law sentence and corrects "the entire leaf population" for
shadow mislabeling; on genres with FR-M3-shaped certificate slack
(measured, committed) the correction over-counts — members decided
at heights in [node floor, first attained divergence) are read
FAITHFULLY (P2-3's member: identical hulls, identical digits,
PARI-exact σ) yet get reclassified as mislabeled: exactly the
count-law corruption the theorem exists to prevent, in the
opposite direction.

SCOPE OF DAMAGE (verified): THEOREM GENTOW-3 (i)/(ii)/(iii) and
COR GENTOW-3.C are untouched (they consume floors and bands, not
attainment; my fresh floor sweep and the entry-margin checks are
all GREEN). (iv) is TRUE under the certificate reading of
"touched", which its proof text and GENTOW3-BOX-1 support. The
battery families and the compound fresh frame FR2-C1 attain Θ at
their touched-in-the-certificate-sense coordinates (machine rows),
so quotes (a)–(c) are correct ON those frames; what is false is
their unconditional/biconditional form. No downstream note
consumed here rests on the overclaims (COR 3.C consumes (i)/(ii)).

---

## CLEAN CHARGES (nothing at GAP-or-worse found)

1. **The r1 gauge annex (S7.1) re-derived independently —
   CONFIRMED, including its honest-status paragraph.** With
   n̂(m) = π^{(m−i(m)h)/e1}x^{i(m)} and K = F_Q(η), the height-m_t
   lift of c = Σ_r λ_r η^r has x-support {i(m_t) + e1·r : λ_r ≠ 0},
   pairwise distinct, all < D′ — so "x-free monomial ⟺ i(m_t) = 0
   AND c_t·η^{W(t)} ∈ F_Q" is exact, the i-test alone is necessary
   (not sufficient) at f1 ≥ 2, and automatic-second-clause at
   f1 = 1 recovers the displayed test: the annex's scope is the
   correct one. Verified leg the annex only cites (HETOW [r1]
   F-4): on 𝒯 the (LIFT) form is π-integral — the [r1] node floor
   u2 > e2D′h gives exponent (m_t − i(m_t)h)/e1 − hr ≥ h/e1 > 0
   at each r ≤ f1−1, t < f2. The η = 1-does-not-restore remark
   (c_t ∈ F_Q not automatic when K ⊋ F_Q) checks. BOX-2's honest
   status is accurate: (i)/(ii) weight-level and gauge-immune,
   (iii)'s proof walked at f1 ≥ 2 under the primary definition
   (x-free monomial K2-coefficients keep the division's x-degrees
   ≤ deg A_J < D′; reduction mod Φ′ = identity) — no f1
   restriction needed; the two-axis machine blindness (f1 = 1 AND
   η = 1) is stated. No frame with f1 ≥ 2, η ≠ 1 breaks the
   primary-definition reading — the classification test, not the
   proof, was the f1 = 1-scoped object, and the annex says so.
   FR-GL paragraph numbers re-checked (W(0) = 1, i(6) = 0,
   naive 54 ↦ corrected 108 = lift(c₀η) with c₀η = 4 ≡ 1 mod 3;
   slot heights 2·5+3 = 2·6+1 = 13, 2·2+3 = 2·3+1 = 7); the
   instrument line-cites L17–21 / L368–369 / L511–546 all land on
   the cited objects.
2. **The attainment walk (the F-1 complement).** Sentences that
   survive: S1 READ OFF (i) is scoped to "every non-x-free FAMILY"
   (the five machine-checked families — pins attained there);
   S2 (iv) under the certificate reading; the S4 δ ≥ 2 band
   sentence ((ii) ∩ leaf range, general); GENTOW3-BOX-1 (already
   names the certificate as per-genre and undischarged); PE1's
   clean-charge-1 tightness sentence ("attained where the theorem
   says"). The failing sentences are exactly F-1's three quotes.
3. **Supplier stack as of this read** (states read TODAY:
   GENHN erratum 59c1966 verified in-tree; GENTOW1_PROOF @
   4e81a57 — ACCEPTED 2/2 + frozen, dated corrections through the
   S3 fixed-base K2-digit lift display; HETOW_RECON @ e38df9f —
   accepted, [r1] F-4 (LIFT) form present; GENTOW5_PROOF @
   d0631cb — r3 (c) re-index landed). GENTOW3 consumes NO
   GENTOW5-unit clause (grep: its "GENTOW-5" cites are LEMMA
   GENTOW-5 in GENTOW1's note — S2 scope remark (3) + the S6
   stack), so the GENTOW5-B(c) stage-conflation repair does not
   touch this note. GENTOW3 mentions neither LIFT-STABLE nor the
   free-count display; BUT LEMMA GENTOW-5(a) (GENTOW1 L414) IS the
   LIFT-STABLE clause, and GENTOW3's remark (3) consumes its CAP
   ("consulted heights also < e1e2·N"). The three-band restatement
   LANDED MID-PASS (TOWERRAT-R2, 4caf0fb: BAND 1 below-cap
   stable / BAND 2 at-cap value-exact-residue-undetermined at
   f1f2 >= 2 / BAND 3 above-cap unstable; the sealed "iff <
   e1e2*N" only-if direction refuted at FAM-B value 8 = cap).
   ADJUDICATED at the restated text (GENTOW1 L919-955): GENTOW3's
   consumption is the strict-below-cap direction ONLY (Band 1 +
   clause (c)'s consultation congruence, both of which R2c's
   consumer sentence explicitly preserves: "consumers ((c)'s
   congruence, (d)'s carries, the readable-event consultation)
   ride heights and band-1/2 residue-determinacy as before");
   GENTOW3 nowhere uses the refuted only-if direction, and the
   R2a free-count clip touches nothing GENTOW3 cites. NOT a
   finding -- the consumption survives the repair verbatim.
4. **r1 manifest verified.** Pre-round body md5 8664269e
   re-computed from d90db6d = the manifest's value; the r1 diff's
   touched lines are the nine listed sites; the THEOREM (i)–(iv) /
   COR 3.C statement displays appear in no deleted line of
   `git diff d90db6d..e1a8dfe` (the (iv) certificate parenthetical
   that gained the attribution bracket sits in the *Proof*, not
   the statement). All six S7.5 md5s re-verified (runner 7cf3a7b8,
   output 926cd05f, fresh 35ad32aa / 94160b22 / RED 485946fc,
   import base 6df4b213). m-1/m-2/m-3 repair texts check against
   the code and tallies (L398–399 literal `pass` confirmed
   byte-for-byte; 137 = 5×(14+12+1)+2, ×2 = 274; FR-P5 digit
   polynomials [−32,0,16] = 16Φ′ etc. all expand correctly).
5. **Free hunt.** S1 table constants re-derived for the five
   families (E2, δ, Θ₀, Θ₁, entry top — from the genre data alone;
   all match); the 582 and 137×2 tallies recompute; COR 3.C's
   margin display (mu2−j)E2 = Θ_j − δ checks; S6's "at GENTOW-1's
   0/2 grade" is stale-but-conservative (GENTOW1 now 2/2; PE1
   already adjudicated accurate-at-composition). Nothing further.

## MACHINE LEG (seals + isolated re-runs)

All six sealed artifacts byte-match the S7.5 manifest at HEAD.
ISOLATED RE-RUNS (fresh scratch dir /tmp/gentow3_pe2_iso_20260810,
runner + import base only): sealed battery exit 0, output
BIT-IDENTICAL (926cd05f), GREEN 582/0 with 3/3 teeth; PE1 fresh
instrument exit 0, output BIT-IDENTICAL (94160b22), GREEN 485/0
with 3/3 teeth. The PE1 tautology audit was not re-walked (per
charge); its dead-code disclosure (S7.4) was re-confirmed at the
quoted lines.

## FRESH ROUTE (gentow3_pe2_fresh.py — own primitives, sealed
## pre-run at 783129e; disjoint from PE1's three frames)

Frames/probes: **FR2-C1** (the COMPOUND corner PE1 never ran:
μ₂ = 3 AND η ≠ 1 together, at the corrected key Φ′² + 3xΦ′ + 108
over Q3, Θ = [19, 13, 7]) with the full self-shadow hand-derived
on paper BEFORE the run (three-step K2-division, every x² → 6
reduction tracked); **P2-3** the count-law counter-member at
FR-M3 (F-1's witness); **P2-8** the j = μ₂ top boundary (both
routes carry the monic 1; the note's j < μ₂ scoping is correct);
**P2-9** the δ = 0 corner (δ = 0 forces u2 = e2D′h ≡ 0 mod e1, so
i(m_t) = 0 at every t on the probe grid: at f1 = 1 every δ = 0
genre is x-free and the geography trivializes — the corner is
fenced by [r1] AND carries no attainment content at f1 = 1).

RESULT: run 1 RED 149/4 KEPT (`gentow3_pe2_fresh_output_run1_RED
.txt`, md5 3ca64bd4; all four misses = MY gp harness literal —
top-level `my()` is invalid in GP — zero math predictions touched;
one-function repair disclosed). Run 2 **GREEN — 149 checks, 0
violations, 3/3 teeth** (runner md5 55208baf, output md5 06b4bddf):
tally SELF 6, REP 2, CL 6, GP 4, BAND 3, DIV 2, FLOOR 120, TOP 2,
D0 1, TEETH 3. Headlines: the compound self-shadow lands EXACTLY
on the hand-derived values (ShC₀ = −157464Φ′ − 314928x,
ShC₁ = 486Φ′ + 17496x, ShC₂ = 27Φ′ − 135x; pins [19, 13, 7] = Θ
ATTAINED at all three coordinates — the compound corner does not
break the theorem, and FR-M3's j = 0 slack is a per-frame
certificate phenomenon, not a μ₂ = 3 artifact); my primitives
replicate PE1's committed FR-GL values at μ₂ = 2 (decorrelation
row); P2-4/P2-5 bracket Θ₀ at the compound frame (faithful at 18,
mislabeled-by-undercut at 20 with shadow fake pin 19 = Θ₀, PARI
{(6,2)} on the honest truth); 120 floor + entry-margin coordinate
checks clean; PARI: corrected key σ {(2,2)} = (e1e2, f1f2) carrier
vs gauge-naive key σ {(4,1)} not a carrier (erratum 59c1966 live
at the compound frame). Teeth: T2-UP (Θ+1 mutant killed at all
three attained pins), T2-DOWN (Θ−1 mutant refuted at the faithful
height-18 member), T2-CL (the count-law's unconditional reading
killed by P2-3 — F-1's machine face).

## GRADE LINE

**passPE2: NOT CLEAN — 1 GAP (F-1); acceptance counter stays
0/2.** The r1 gauge annex is CLEAN under independent re-derivation
and first-contact machine stress at the compound frame; the sealed
+ PE1 legs are bit-stable; the theorem's floor/band/x-free core
and COR 3.C survived a second adversarial route. The owed repair
is one certificate-scoping pass: fix or fence the unconditional
attainment sentences at S0 and S4 (δ = 1 bullet + count-law) and
pin "touched coordinate(s)" in (iv) to the per-coordinate
certificate — re-derived from the note's own (iv) proof and
BOX-1, not from this report's wording.
