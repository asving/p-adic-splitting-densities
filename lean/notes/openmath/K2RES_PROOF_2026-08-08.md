# K2-RESIDUE — Q2's residue attacked: order-multiplicity IS same-sign multiplicity (the genre unified across g₀); the g₀ ∈ {3,4} single-window window calculus DERIVED with its verdict column (K2-THM-C); the any-g₀ A = {0} lemma; the promotion-swap reduction (one gate for the whole single-book same-sign genre); the residual re-boxed exactly

STATUS: VERDICT IN (commit 2 of 2).  Sealed with the runner at commit 1
(caa7c50, machine leg UNRUN at seal); the verdict displays marked
[VERDICT] are filled at commit 2 from the committed artifacts, appended
never edited.  Battery verdict: ALL 12 FAMILIES GREEN (0 violations),
teeth 3/3, P-1..P-5/P-7 CONFIRMED, P-6 measured (67 promo rows — all
REGULAR), P-8 confirmed on the disclosed-skip branch; runner
byte-verified at the seal; full verdict + run provenance at S8-V.
Unit: K2-RESIDUE, wave wmyhw298v, 2026-08-08 campaign (wallclock
2026-08-07).

Object: the ledgered residue of the Q2/(SURV-K2) program — the ledger's
standing line (BRIDGE_ADJUDICATIONS, the J-B/NR-1 entry): "NR-3 = Q2,
closed at g₀ ≤ 2 except K2-MULT-RES + g₀ ≥ 3."  This unit closes what is
closable of that residue at intermediate grade and boxes the rest exactly.

**Verdict, stated first.**  Six results, all capped at ATTEMPT grade by the
same consumption chain as K2-THM-A/B (the W arc, 0/2 CLEAN); no hostile
pass has run on THIS note (arc 0/0):

1. **K2-L13 (the any-g₀ drain calculus + the order-multiplicity law).**
   The legal-drain inventory of a level-0 window is derived for EVERY
   g₀ ≥ 2 and every availability set A ∋ 0; the number of legal orders of
   a drain multiset is the prefix-mass count (K2-L13(b)); multi-order
   multisets exist iff g₀ ≥ 3, and each one contributes a SAME-SIGN
   multiplicity ≥ 2 to a single residue class.  **The (K2-RES)(d)
   order-multiplicity candidate is thereby ABSORBED into the K2-MULT-RES
   genre: at every g₀ the only in-perimeter unconditional single-group
   deaths are char | (forced net count).  Q2's two residue legs are ONE
   genre.**
2. **K2-L14 (single-window bottom groups, any g₀).**  A single-book
   single-window β_min group is EXACTLY the t-graded union of complete
   per-budget drain censuses — the g₀-general form table, with integer
   multiplicities given by K2-L13(b).  (The g₀ = 2 case reproduces
   S/P1/P2/T1 as a machine calibration.)
3. **K2-L15 (the identity lattice at every g₀).**  The tower-level digit
   identity lattice forcible by single-book coincidences is exactly
   L(g₀) = {deg = 0, φ = 0}, rank 2(g₀−1), integrally generated by the
   quantum-1 seed identities A_1..A_{g₀−1} and the elementary exchange
   identities E_0..E_{g₀−2}.  (K2-L11's single-book content at every g₀;
   at g₀ = 2 this is ⟨I₁, I₃⟩ verbatim.)
4. **K2-THM-C (corner coverage at g₀ ∈ {3,4}, single-window).**  On
   junk-free rows whose β_min groups are single-book single-window with
   full availability, corner coverage over the 2(g₀−1)-generator corner
   space forces a protected group under EVERY residue assignment —
   (SURV-K2)'s existence half extends to g₀ ∈ {3,4} on this locus.  The
   machine verdict column (KR-COL) grades every form at every corner; the
   exact obstruction beyond the covered locus is DISPLAYED: the budget-5
   (units 3, p 0) sector at g₀ = 3 has generic class vector (+2, −1, −1)
   — protection-impossible off the identity loci, the first
   full-availability sector where the g₀ = 2 mechanism provably fails.
5. **K2-L16 (the A = {0} corner at every g₀, single-book).**  K2-L9
   generalizes verbatim: all events cost g₀ units, seeds separate mod g₀,
   the placement analogue is budget-dead ({C₀, promo} = 2g₀ > 2g₀ − 1) —
   every single-book bottom group is one class of one sign at EVERY
   g₀ ≥ 2.  A positive g₀ ≥ 3 closure, not just a g₀ = 2 fact.
6. **K2-L17 (the promotion-swap reduction).**  Replacing any level-0
   promotion by C₀ (always available, K2-L8; same j₀ consumption;
   line-neutral where the promotion pays a positive quantum) undercuts
   any β_min cell carrying a level-0 promotion, UNLESS the swap starves
   the second exit.  Hence (e)-at-β_min, K2-L9-RES, and the g₀ ≥ 3
   in-window {C_{g₀−1}, promo} placement genre all live inside ONE gate:
   the EXIT-CRITICAL corner, displayed and boxed.  **The corner is
   REALIZED**: the pre-seal shakedown (disclosed) already found
   promotion-carrying junk-free β_min cells on 2 rows of SDW4G2F (the
   glued witness tower) — by K2-L17(c) those rows are exit-critical at
   those cells, so the corner is non-vacuous in vivo; the full 332-row
   census (KR-PFREE) reports the exact count, the towers, and whether
   any REGULAR row carries one [VERDICT, filled at commit 2: 67
   promotion-carrying rows / 77 promo cells on 14 towers (census at
   S8-V), and the disclosed post-seal extension leg re-traced all 67 —
   EVERY one is a REGULAR row (single-fiber β_min groups, full
   availability); the corner is realized on regular rows, not only on
   the glued witnesses].

What is NOT closed (boxed at S7 as (K2-RES-C)): the even-dup |T| = 2
glued NOVEL shapes and GS-40/AS-10 (K2-CLOSE's char-divides-count merge
deaths — untouched here, restated); the exit-critical corner; g₀ ≥ 3
multi-window, glued, and in-vivo strata (the engine cost wall
re-measured and disclosed); availability corners A ∉ {full, {0}} at
g₀ ≥ 3 beyond the displayed −2-window arithmetic; g₀ ≥ 5 columns.

## S0. Charge, authority, consumption, machine leg

Charge (the orchestrator's K2-RESIDUE charge): close Q2's remaining
residue — (a) the K2-MULT-RES genre, (b) g₀ ≥ 3 — or box it exactly;
examples-first where the mathematics resists; intermediate grade,
disclosure-grade honesty, 0/2, honest boxes over reach.

Consumption, all read-only, at the grades of the SURVK2_PROOF header cap
sentence (the binding cap = the W arc, 0/2 CLEAN): SURVK2_PROOF_2026-08-08.md
(K2-L1(a)–(d), K2-L2, K2-L3, K2-L4, K2-L5(a)(b)(c), K2-L6, K2-L7, K2-L8,
K2-THM-A, the S6 forms table, the (K2-RES) box) at attempt grade —
**K2-L1/L2/L4/L5/L6 are any-g₀ ≥ 2 by their own headers (F-A of that
note), and this unit leans on exactly that**; K2CLOSE_2026-08-08.md
(K2-L11, K2-L12, K2-THM-B, the verdict-column discipline, the (K2-RES-B)
box) at attempt grade; K2CLOSE_LEGS23_2026-08-08.md (K2-L9 single-book,
K2-L10, the (K2-L9-RES) box) at attempt grade; WMULTDCX† W-D2(iv) (seed
arrival j₀(D_t) = s₁(u₁) + t·e₀, 0 ≤ t < g₀, w₀ = u₀ − t·h₀ — any g₀ ≥ 2
under (H3)); [RMG] (ACCEPTED, no cap): S2.2 child inventory (correction
child present iff digit ≠ 0; C_k = lift(ψ₀_k)·π^{(g₀−k)h₀} consuming
e₀(g₀−k), no dump; promotion consumes P₀ = e₀g₀, +1 into level 1),
L-NORM(a)(b)(c), S3.2 L-LINE(i)–(iv) (promotion quanta > 0;
correction-main children line-neutral EXACTLY at every level; the
identity Γ₀ = h₀E_m/e₀), S2.1 line kit; SURVDISJ_PROOF† SD-L6/SD-L7;
[ILN]† S0.1 (ψ_j(0) ≠ 0 — the class pin behind K2-L8, quoted through
that lemma).  Scope: the [ILN]† S0.1 class, level m ≥ 2, g_m = 1, TRACK,
(H1)–(H3), c_{m−1} ≥ k; g₀ as stated per lemma.  Dagger convention as in
SURVK2_PROOF S0.

**Machine leg of THIS unit:** `verification/openmath/k2res_checks.py`
(md5 pinned at commit 1), sealed UNRUN with the preregistered predictions
P-1..P-8 and THREE planted teeth (TOOTH-1 firing-threshold mutant,
TOOTH-2 sign-parity mutant, TOOTH-3 seed-quantum mutant) in its
docstring; verdict families KR-PIN / KR-INV / KR-ORD / KR-TOT / KR-FORM
/ KR-IDC / KR-COL / KR-CAL / KR-SWAP / KR-PFREE / KR-VIVO / HARNESS.
It reuses the committed machinery by md5-pinned read-only import:
`survk2_derive_checks.py` (the g₀ = 2 drain enumerator, the recording
walk, the window decomposition), `survk2_instrument.py` (roster specs),
`k2close_verdicts.py` (the exact-lattice kit Lat/closure/protection and
the S/P1/P2/T1 corner table FORM_CORNERS as calibration target),
plus the engine stack pins inherited verbatim.  Smoke record, disclosed:
an abstract-enumerator smoke (g₀ ∈ {2,3,4} inventories + availability
corners + a 49-combo g₀ = 3 form count) ran in the composer's context
before sealing and shaped predictions P-2..P-5; no engine row was
traced and no verdict-column state was explored before the seal.

## S1. THE RESIDUE, RECONSTRUCTED EXACTLY (the open statements this unit attacks)

Quoted from the notes of record; these are the unit's targets and the
box (K2-RES-C) at S7 must be exactly commensurable with them.

**R-A (K2-MULT-RES; K2CLOSE_2026-08-08.md S5 box (K2-RES-B)(ii), KC-r1
text).**  "K2-MULT-RES — the SAME-SIGN MULTIPLICITY genre, now unified:
every remaining single-group threat WITHIN THE B4-ENUMERATED PERIMETER
is 'char divides a forced net count': the NOVEL shapes (seven even-dup
|T| = 2 mains: char-2 events at the merge states; GS-40: T = 0 with
every ±1 split I-cascaded dead; AS-10: char 3), the (e)/K2-CAND
single-book pair (net −2; char 2; β_min occurrence still awaiting the
line-quanta exclusion, K2-L10), and K2-L9-RES (A = {0} promotion
multiplicity mod odd char)."

**R-A′ (the A = {0} sub-box; K2CLOSE_LEGS23_2026-08-08.md S5,
(K2-L9-RES)).**  "promotions ... are NOT placement-rigid ..., so two
promotion timings can share a group when level 1 never overflows — a
group of cell count #cells > 1, single class, single sign.  Then
Σ sgn·res = ±(#cells)·res vanishes iff the (odd) characteristic divides
#cells. ... The natural closer is a promotion-placement-rigidity lemma
(does (I, π⃗, slot) pin the per-window promo profile?)".

**R-A″ (the (e) sub-box; K2CLOSE_LEGS23_2026-08-08.md S3, K2-L10 honest
status).**  "box (e) is budget-LEGAL ...; its exclusion at β_min is a
LINE-QUANTA question — the structuring promotion raises the line
(L-LINE(i)), so a competing promotion-free cell generically sits at a
strictly lower line and the pair is not at β_min, but on 'forced-carry'
rows the promotion may be unavoidable and the pair could reach β_min."

**R-B (g₀ ≥ 3; SURVK2_PROOF_2026-08-08.md S7 box (K2-RES)(d), still the
authority via the (K2-RES-B)(iv) pointer).**  "g₀ ≥ 3 [machine-dark
program-wide — the instrument's engine cost wall].  The budget bound
(K2-L2) reaches n ≤ 5, where BOTH pillars of the g₀ = 2 inventory
PROVABLY fail combinatorially (K2-CAND arithmetic, verified): • the −2
window: n = 5, masses {1,3} available (ψ₀_0, ψ₀_2 ≠ 0), drains {C₀} and
{C₂,C₂,C₂} both sign −, per-window count −2 — K2-L3(iv) fails; •
order-multiplicity: {C₂,C₁} legal in both orders at n = 5 — K2-L3(i)
fails; identical books, same sign: a −2 class.  Whether pool arithmetic
realizes an n = 5 window AT β_min is open — a sealed falsifiable
prediction for the next g₀ ≥ 3 instrument (needs t = 2 seeds or
near-full dumps)."

Ledger wording of record (Wave-21 closure program, K2-CLOSE COMPLETE
entry): "**Boxed residual (K2-RES-B), unified:** ... (ii) K2-MULT-RES —
the single same-sign multiplicity genre (the novel imbalance shapes at
char | T; K2-CAND at char 2; K2-L9-RES) ... (iv) g₀ ≥ 3."

Status of each after this unit: S7's box.

## S2. K2-L13: the any-g₀ drain calculus, and the genre unification

Unit bookkeeping as in SURVK2_PROOF S1: j₀ = n·e₀ + r (0 ≤ r < e₀); a
window's budget is its opening n.  Availability A := {k : ψ₀_k ≠ 0} —
always 0 ∈ A (K2-L8; its two-leg proof — the [ILN] S0.1 pin ψ₀(0) ≠ 0
and irreducibility at degree g₀ ≥ 2 — is g₀-uniform, so the lemma holds
verbatim at every g₀ ≥ 2).

**Lemma K2-L13 (any g₀ ≥ 2, any A ∋ 0, junk-free drains).**
(a) [inventory]  The legal drains of a window of budget n are EXACTLY
the finite sequences over the event alphabet {C_k (k ∈ A, mass g₀ − k),
P (promotion, mass g₀)} in which every event fires at running unit count
≥ g₀, continued until the running count drops below g₀.  [Proof: K2-L1(a)
(any-g₀: level-0 events fire only at j₀ ≥ P₀ = e₀g₀ and drain
completely) + the [RMG] S2.2 consumption quanta (C_k consumes e₀(g₀−k),
promotion consumes e₀g₀) + child presence iff digit ≠ 0.  Junk-free
excludes the Y-variants, so no +1 returns occur and the running count is
strictly decreasing — the drain is a bare mass-consumption sequence.]
(b) [the order count]  The number of legal orders of a fixed multiset
(K, p) at budget n equals the number of arrangements of the multiset
{masses of K} ∪ {g₀}^p whose every PROPER prefix has mass sum
≤ n − g₀.  [Immediate from (a): the event at position i fires iff
n − (prefix mass before it) ≥ g₀.]  Distinct legal orders are realized
by DISTINCT terminal cells with IDENTICAL (t, K, I, π⃗, slot, line,
residue class, sign): distinct paths by [RMG] S2.2 branching (K2-L1(d));
identical invariants by K2-L4 (class/sign/w₀ see only (t, K, I)),
equal parked j₀ and equal π⃗ (same units, same p), K2-L5(b) (line a
function of π⃗ on junk-free cells).
(c) [g₀ = 2 uniqueness, rederived]  At g₀ = 2 every (K, p) has exactly
one legal order at each budget — K2-L3(i), reproved by the machine from
(a)–(b) (KR-INV calibrates the fresh enumerator against the pinned
`legal_drains_g2` and the displayed K2-L3 table; exhaustion over n ≤ 3).
(d) [the multiplicity threshold]  Multi-order multisets exist iff
g₀ ≥ 3.  ⇐: at budget n = 2g₀ − 1 the multiset {C_{g₀−1}, C_{g₀−2}}
(masses 1 and 2) is legal in both orders iff both proper prefixes
(masses 1 and 2) are ≤ n − g₀ = g₀ − 1, i.e. iff g₀ ≥ 3.  ⇒: at g₀ = 2
this is (c).  By (b) each multi-order multiset deposits a signed count
of absolute value ≥ 2 into ONE residue class of ONE sign.
(e) [finite tables]  The complete inventories at g₀ ∈ {2, 3, 4}, all
availabilities A ∋ 0, all budgets n ≤ 2g₀ − 1, with per-(units, p)
class censuses, class totals, and order counts, are machine-exhausted
(KR-INV/KR-ORD/KR-TOT) and the g₀ = 3 full-availability table is
displayed below.  ∎

**The g₀ = 3 full-availability table (A = {0,1,2}; C₂/C₁/C₀/P masses
1/2/3/3; sign = (−1)^{|K|}).**  Per (units, p) class at each budget:

    n=3: (0,1):{P}+1 | (1,0):{C₂}−1 | (2,0):{C₁}−1 | (3,0):{C₀}−1
    n=4: (0,1):{P}+1 | (1,1):{C₂}−1 (P first is illegal: order forced)
         (2,0):{C₁}−1,{C₂C₂}+1 → total 0   [exchange pair, identity E₁]
         (3,0):{C₀}−1,{C₁C₂}+1 → total 0   [exchange pair, identity E₀]
         (4,0):{C₀C₂}+1
    n=5: (0,1):{P}+1 | (1,1):{C₂}−1
         (2,1):{C₁}−1,{C₂C₂}+1 → total 0
         (3,0):{C₀}−1, {C₁C₂}+2 (BOTH orders), {C₂C₂C₂}−1 → total 0
               — THE OBSTRUCTION SECTOR: generic class vector (+2,−1,−1)
         (4,0):{C₀C₂}+1,{C₁C₁}+1,{C₁C₂C₂}−1 → total +1
         (5,0):{C₀C₁}+1,{C₀C₂C₂}−1 → total 0 [pair, identity E₁]

Readings.  (i) Every (units, p) class TOTAL lies in {−1, 0, +1}
(prediction P-2; the K2CLOSE-PE1 bonus census, now a displayed lemma
consequence) — the T-invariant calculus of K2-CLOSE survives at g₀ = 3
full availability.  (ii) The (n=5, units=3, p=0) sector is the FIRST
full-availability class anywhere whose generic vector has an entry of
absolute value 2: (+2, −1, −1).  SD-L7 protection there requires an
identity (E₀ merges {C₀} into the +2 class: (+1, −1); E₁ merges
{C₂C₂C₂}: (+1, −1)); at ¬E₀ ∧ ¬E₁ no state protects (the only further
coincidences are lattice elements like E₀+E₁ merging {C₀} with
{C₂C₂C₂}: (+2, −2)).  This DERIVES the exact failure of the g₀ = 2
mechanism that (K2-RES)(d) predicted combinatorially: at g₀ = 2 every
multi-member class was one OPPOSITE-sign exchange pair; at g₀ = 3 a
multi-member class can carry a same-sign double.  (iii) The −2 window:
at A = {0, 2} (ψ₀_1 = 0) the (n=5, units=3, p=0) census is {C₀}−1,
{C₂C₂C₂}−1 — total −2, same sign, matching R-B's display digit for
digit; machine-verified to be the UNIQUE |total| ≥ 2 sector at g₀ = 3
over all availabilities (prediction P-4).  At g₀ = 4, availability
corners realize |total| = 2 sectors in several ways INCLUDING a pure
order-multiplicity single-class +2 (A = {0,1,2}, n = 7, units 5:
{C₁C₂} × 2 orders and nothing else) — the multiplicity genre needs only
ONE missing digit at g₀ = 4 (machine census KR-TOT).

**The genre unification (the R-A ∪ R-B collapse).**  By K2-L13(b)+(d),
order-multiplicity contributes same-sign integer multiplicities to
single classes — exactly the "char divides a forced net count" genre of
(K2-RES-B)(ii).  By the table, the −2 window is the availability-corner
face of the same genre.  So R-B's two displayed combinatorial failures
are not a new failure mode: **at every g₀, within the single-book
window-calculus perimeter, every unconditional single-group death is
char | count — K2-MULT-RES is THE residual genre of the whole Q2
program, uniformly in g₀** (the glued/beyond-perimeter fences of
(K2-RES-B)(ii) unchanged; KR-COL verifies the classification at
g₀ ∈ {3,4} state-by-state).

## S3. K2-L14 + K2-L15: single-window bottom groups and the identity lattice at every g₀

**Lemma K2-L14 (single-window single-book β_min groups; any g₀ ≥ 2,
full availability or any fixed A ∋ 0).**  Let G be a β_min group on a
live row with the β_min population junk-free/Y-free ((R0); automatic at
d₀ = 1 — K2-L5(a), any-g₀), all of whose cells carry one fixed interior
book I and promotion totals π⃗ (single-book), and drain all their
level-0 events in the SEEDING window (single-window).  Then there are
constants (n₀, u₀, p) and a set T ⊆ {0, …, g₀−1} of available seeds
with:
(i) [structure]  G's cells are EXACTLY ⋃_{t∈T} {legal drains of budget
n₀ + t with unit mass u₀ + t and promotion count p} — every legal drain
realized, in every legal order;
(ii) [classes]  the cell of seed t and multiset K has residue class
x_t · ∏_{k∈K} ψ̄_k · (a factor common to all of G) and sign
(−1)^{|K|+|I|} (global across G up to the common (−1)^{|I|});
(iii) [multiplicities]  the class vector of G is the t-graded sum of
the K2-L13(e) sector censuses, with per-(K, p) multiplicities the
K2-L13(b) order counts.
*Proof.*  Seeds arrive at j₀(D_t) = s₁(u₁) + t·e₀ (W-D2(iv)†, any g₀
under (H3)), so the seed window's budget steps by exactly t units
across seeds: n_t = n₀ + t with n₀ the t-free part (parked-before-seed
+ s₁(u₁) units).  Fixed slot forces equal parked j₀; fixed line with
fixed π⃗ forces (K2-L5(b), K2-L6) equal w₀, i.e. mass u_t = u₀ + t
(ν = t − M constant — K2-L6, any-g₀); equal π⃗ forces equal p.  Cells
with these totals and ANY legal drain order/content are interchangeable
along junk-free continuations and all realized (K2-L1(c)(d), any-g₀),
and land in G (equal parked, π⃗, w₀, line, slot; K2-L4/L5(b)) — so G
contains the full union and nothing else (any cell of G has SOME legal
drain, whose totals are forced as above).  (ii) is K2-L4 (any-g₀; the
common factor is res(κ-part)·ρ_I·x-normalization as in the S6
normalization of SURVK2_PROOF).  (iii) is K2-L13(b) + distinctness of
order-realizations (K2-L13(b)'s cell-distinctness clause).  ∎
[Machine: KR-FORM enumerates the (n₀, u₀, p, T) form space at
g₀ ∈ {2, 3, 4}; KR-CAL verifies the g₀ = 2 slice reproduces the
S/P1/P2/T1 table EXACTLY — statement (i) instantiated at g₀ = 2 is
K2-THM-A's forms display.]

**Lemma K2-L15 (the single-book identity lattice at every g₀ ≥ 2).**
On the free nonzero digit generators (x₀, …, x_{g₀−1}, ψ̄₀, …,
ψ̄_{g₀−1}) write deg(v) = Σ_t v_{x_t} and φ(v) = Σ_k (g₀−k)·v_{ψ_k} −
Σ_t t·v_{x_t}.  Then:
(a) every difference of two cell monomials of one single-book group
lies in L(g₀) := {v : deg(v) = 0, φ(v) = 0};
(b) L(g₀) has rank 2(g₀−1) and is INTEGRALLY generated by
    A_t : x_{t−1} − x_t − ψ_{g₀−1}          (t = 1, …, g₀−1)
    E_j : ψ_j − ψ_{j+1} − ψ_{g₀−1}          (j = 0, …, g₀−2)
(the quantum-1 Q-A0 seed identities x_{t−1} = x_t·ψ̄_{g₀−1}, and the
elementary exchange identities ψ̄_j = ψ̄_{j+1}·ψ̄_{g₀−1}).  At g₀ = 2:
A₁ = I₁ and E₀ = I₃ — K2-L11's ⟨I₁, I₃⟩ verbatim.
*Proof.*  (a) Each cell monomial has exactly one x-generator (deg 1)
and φ(cell) = M − t = −ν, constant on the group's book (K2-L6, any-g₀);
differences kill both.  (b) φ(A_t) = −1·1 − ((t−1) − t) = 0 and
deg(A_t) = 0; likewise E_j.  Integral generation by echelon descent:
given v ∈ L(g₀), subtract Σ_{t} (Σ_{s<t} v_{x_s})·A_t-combinations to
clear the x-block (possible exactly because deg v = 0: the x-part is an
integer combination of consecutive differences e_{t−1} − e_t, each of
which is the x-part of A_t); the remainder w is ψ-only with φ(w) = 0;
subtract w_{ψ_0}·E_0, then w_{ψ_1}·E_1, …, clearing ψ_0 … ψ_{g₀−2};
the residue is c·ψ_{g₀−1} with φ = c = 0.  Every subtraction is
integral, so the generation is saturated; rank = (g₀−1) + (g₀−1).  ∎
[Machine: KR-IDC — lattice equality by echelon comparison at
g₀ ∈ {2,…,6}, and membership of every coincidence generator of every
visited verdict-column state at g₀ ∈ {3,4}.]

Reading.  K2-L15 extends K2-L11's closure to every g₀ on the
single-book stratum: coincidences can never force a digit identity
outside ⟨A_•, E_•⟩, so the corner space of the verdict column below is
COMPLETE for single-book groups.  (The glued ρ-coordinate argument of
K2-L11 is not redone here; glued g₀ ≥ 3 strata stay boxed.)

## S4. The g₀ ∈ {3,4} verdict column and K2-THM-C (corner coverage, single-window)

**The column (machine, KR-COL; the K2-L12 state discipline).**  For each
single-window form F (a K2-L14 datum (n₀, u₀, p, T) at full
availability) build the monomial model of K2-L14(ii)–(iii): one
monomial x_t·∏ψ̄_k per (t, K), net = sign × order count.  States =
closed partitions of the monomials (K2-L12, consumed at attempt grade:
patterns are subgroups, BFS exhaustive, torsion states flagged and KEPT
— conservative for every safety verdict); the corner space = the
2(g₀−1) sign patterns on K2-L15's generators {A_•, E_•} (complete for
single-book groups by K2-L15(a)); a state is consistent with corner c
iff its H contains every c-imposed generator and no c-negated one;
PROTECTED per state = nonzero per-class nets exactly {±1} or {+1, −1}
(SD-L7†).  Per form and corner: SAFE (every consistent state protected)
/ CONDITIONAL / NOVEL (no consistent state protected); plus the
full-merge total T (the SD-L6 survival display: the merged group nets
T·v, dead exactly at char | T).  Calibration (KR-CAL): the identical
pipeline at g₀ = 2 must reproduce the S/P1/P2/T1 verdicts and the
K2CLOSE FORM_CORNERS table {S: all corners; P1: ¬I₁; P2: ¬I₃; T1:
I₁ ∨ I₃} EXACTLY, reading I₁ = A₁, I₃ = E₀.

[VERDICT, filled at commit 2 from the committed artifacts:
g₀ = 3 — 26 forms × 16 corners = 416 pairs: 254 SAFE / 52 CONDITIONAL /
110 NOVEL, 0 capped; every NOVEL death is T = 0 (72) or char | T (38),
the K2-MULT-RES genre law with no third genre.  g₀ = 4 — 72 forms × 64
corners = 4608 pairs: 2030 SAFE / 1140 CONDITIONAL / 1438 NOVEL,
0 capped; NOVEL deaths 787 T = 0 + 651 char | T.  Obstruction sector
(n = 5, units 3, p 0): corner-safe EXACTLY on the 8 {E₀ xor E₁}
corners, machine-confirmed (the P-7 law).  Singleton-census single-seed
forms: safe at all 16 corners.  Calibration (KR-CAL): S/P1/P2/T1 +
FORM_CORNERS reproduced exactly.  Full detail in S8-V.]

**THEOREM K2-THM-C (corner coverage at g₀ ∈ {3, 4}, single-window).**
On TRACK ∧ (H1)–(H3) ∧ g₀ ∈ {3, 4} ∧ c_{m−1} ≥ k, suppose
(C0) the β_min population is junk-free/Y-free ((R0); automatic d₀ = 1);
(C1) every β_min group is single-book and SINGLE-WINDOW (all level-0
     events in the seeding window);
(C2) full availability: A = {0, …, g₀−1} (every ψ₀_k ≠ 0);
(C3) corner coverage: for every corner c of the 2(g₀−1)-generator
     corner space, some β_min group's form is corner-safe at c
     (machine column).
Then some β_min group carries an SD-L7-protected residue-class vector
UNDER EVERY residue assignment — (SURV-K2) holds on the row.
*Proof.*  A residue assignment φ fixes the truth values of the 2(g₀−1)
generators — tower-level data (the x_t and ψ̄_k are tower-level residues,
K2-L4), common to every group of the row — hence a corner c(φ).  Take a
group G whose form is corner-safe at c(φ) (C3).  By K2-L14, G's class
vector is its form's monomial model evaluated at φ; by K2-L12
(quantification discipline) φ's coincidence pattern on G is a closed
state whose H contains every c(φ)-imposed generator (they vanish under
φ) and cannot contain a c(φ)-negated one (φ does not kill it); corner
safety says every such state — torsion included — is protected.  ∎
(Attempt grade; consumes K2-L1..L6/L8†, K2-L12†, K2-L13/L14/L15,
SD-L6/SD-L7†, and the machine column.)

**Corollary K2-COR-C.**  On the K2-THM-C locus the consequence chain of
K2-COR runs verbatim: the protected group survives netting (SD-L6 +
SD-L7†), the netted bottom stratum survives (S-L3†), and under (RW₀)
Q¹ ≠ 0 (S-THM(b)†) — the FIRST g₀ ≥ 3 closure of any (SURV) form, on
its locus.  ∎

**The exact obstruction beyond the locus, displayed.**  (i) Off the
corner-covered systems the dichotomy fails exactly as at g₀ = 2 (the
corner-uncovered box), but with a NEW floor: the obstruction sector
(n = 5, units 3, p 0) is generically unprotected EVEN ALONE, so g₀ = 3
rows whose β_min system realizes only obstruction-genre sectors at
generic digits have NO protected group — whether pool arithmetic
realizes such a system is exactly R-B's sealed falsifiable prediction,
untouched by this unit (the engine wall, S8).  (ii) Multi-window
g₀ ≥ 3 chains inherit everything open from g₀ = 2's (b)/(e) PLUS the
budget-{4,5} placement freedom; not enumerated here.  (iii) Availability
corners: A = {0} is CLOSED single-book at every g₀ (K2-L16); strict
intermediate corners (e.g. A = {0, 2}: the −2 window) are boxed with
their displayed arithmetic.

## S5. K2-L16: the A = {0} corner at every g₀ (K2-L9 generalized, single-book)

**Lemma K2-L16 (any g₀ ≥ 2; A = {0}; single-book).**  On TRACK ∧
(H1)–(H3) ∧ c_{m−1} ≥ k with ψ₀_1 = … = ψ₀_{g₀−1} = 0 (A = {0}; ψ₀_0 ≠ 0
by K2-L8) and the β_min population junk-free/Y-free (R0): every
SINGLE-BOOK bottom-line group is a single residue class carrying a
single sign; the sign-reversing involution has no moves among
single-book groups.
*Proof, K2-L9's five steps generalized.*
(1) The only events are C₀ (mass g₀) and P (mass g₀); each active
window (budget n with g₀ ≤ n ≤ 2g₀ − 1, K2-L2 any-g₀) fires EXACTLY
once: after one event n − g₀ ≤ g₀ − 1 < g₀ closes the window; two
events cost 2g₀ > 2g₀ − 1.
(2) The two drains {C₀} and {P} of any budget differ in p — every
(units, p) class is a singleton; no in-window exchange exists (every
exchange needs a second available digit).
(3) All consumption is ≡ 0 (mod g₀); seed t's arrival adds t units
(W-D2(iv)†), so within one fiber the parked unit count is ≡ base + t
(mod g₀).  The seeds t ∈ {0, …, g₀−1} are pairwise distinct mod g₀ —
per-fiber pairwise DISJOINT slots; no single-book group mixes seeds
(the K2-L9(3) argument verbatim, with parity replaced by residue
mod g₀).
(4) A C₀ cannot be re-placed at a fixed per-window promotion profile:
{C₀, P} in one window costs 2g₀ > 2g₀ − 1 (K2-L13(a)); K2-L1(b)
(any-g₀) pins the interior to the promo profile exactly as in K2-L9(4).
(5) Fixed (I, π⃗, slot, line) + a single seed t (step 3) force the
correction count a (parked = arrival(t) − g₀·a − g₀·p, all else fixed)
and hence K = {C₀}^a; K2-L4 (any-g₀) makes all cells of the group one
class res(c_{D_t})·ψ̄₀^a·∏res(c_d) of one sign (−1)^{a+|I|}.  ∎
[Machine: KR-SWAP/KR-INV — the A = {0} inventory at g₀ ∈ {2,…,6}:
every (units, p) class a singleton, every drain a single event; the
2g₀ > 2g₀ − 1 arithmetic displayed.  Instance leg: A = {0} remains
instance-dark at every g₀ (no such read on any committed roster) —
enumeration facts, never proof steps.]

Residual (unchanged in kind): the promotion-placement multiplicity
(K2-L9-RES) — now known to be the SAME question at every g₀, and by
K2-L17 below confined to the exit-critical corner; glued A = {0} groups
(the K2-CLOSE annex's in-budget shapes AS-01/02/03) stay outside, as at
g₀ = 2.

## S6. K2-L17: the promotion-swap reduction — one gate for the single-book same-sign genre

**Lemma K2-L17 (any g₀ ≥ 2).**
(a) [the placement law]  In one window, {C_{g₀−1}, P} costs
(g₀ + 1) ≤ 2g₀ − 1 ⟺ g₀ ≥ 2: the in-window correction+promotion pair
is budget-LEGAL at every g₀ (at g₀ = 2 this is K2-L10(b)'s chain
arithmetic); {C₀, P} costs 2g₀ > 2g₀ − 1: budget-DEAD at every g₀
(= K2-L9(4)/K2-L16(4)).
(b) [the swap]  Let C be a junk-free two-exit cell whose path fires a
level-0 promotion at a state s (j₀ ≥ P₀).  At s the C₀ child is also
present (ψ₀_0 ≠ 0 — K2-L8; [RMG] S2.2 child presence) with the SAME j₀
consumption e₀g₀ = P₀, and its correction-main L-child is never absent
(L-NORM(a)).  The swapped prefix reaches a state s′ differing from the
post-promotion state of C exactly by: j₁ lower by 1; one extra level-0
correction edge (sign flipped, coefficient × lift(ψ₀_0)·π^{g₀h₀}); and
LINE lower by the level-0 promotion quantum q₀ > 0 — the promotion pays
+q₀ (L-LINE(i)) while C₀ is line-neutral (L-LINE(iii): E_m·g₀h₀ =
e₀g₀·Γ₀, the identity Γ₀ = h₀E_m/e₀).
(c) [the undercut]  If from s′ SOME junk-free two-exit continuation
exists whose promotion-quanta sum is ≤ the promotion-quanta sum of C's
own continuation after s, then the resulting cell C″ satisfies
line(C″) ≤ line(C) − q₀ < line(C) (the K2-L5 line assembly, any-g₀:
for junk-free cells of one row, line differences = promotion-quanta
differences; corrections line-neutral at every level, exits and seed
common).  Hence a β_min cell can carry a level-0 promotion ONLY if the
row is EXIT-CRITICAL at it: every junk-free continuation of every such
swap either never reaches its second exit or is forced into strictly
more promotion quanta than C's own continuation.
(d) [the reduction]  Each of the three single-book same-sign threats —
(e)/K2-CAND at g₀ = 2 (R-A″: its cells carry the structuring level-0
promotion), K2-L9-RES (R-A′: promotion-placement multiplicity — its
cells carry the re-timed level-0 promotions), and the g₀ ≥ 3 in-window
{C_{g₀−1}, P} pair of (a) — requires a level-0 promotion in a β_min
book.  By (c) ALL of them are confined to the exit-critical corner.
On exit-slack rows (every swap undercut succeeds) the β_min books are
level-0-promotion-free outright and the whole genre is ABSENT — no
promotion-placement-rigidity lemma is needed there.  ∎
[Machine: KR-SWAP (the (a) arithmetic at g₀ ∈ {2,…,6}); KR-PFREE (the
in-vivo census: level-0-promotion content of every β_min book on the
332 traced rows, the instrument's selection replicated — MEASURED, see
P-6.  The pre-seal shakedown already found 2 promotion-carrying rows
on SDW4G2F: by (c) these are the FIRST REALIZED exit-critical rows —
at the lemma's attempt grade — so (d)'s exit-slack closure is NOT
class-wide; the corner it boxes is live.  Note both shakedown rows sit
on the glued tower and were protected in vivo (K2-PROT 332/332 of the
committed battery): the finding de-vacuifies the corner, it does not
threaten any traced row.)]
(Filled at commit 2 — the full-census verdict for this bracket is the
S8-V PFREE block: 67 rows / 77 cells on 14 towers, every one REGULAR
per the disclosed post-seal extension leg.)

What K2-L17 does NOT do: it does not decide exit-criticality (that is a
mass-balance question about the exit budget — two exits consume
2P_{m−1} at level m−1, and the swap starves the supply by one unit at
level 1; on rows with slack in that balance the undercut goes through,
on tight rows it need not) and it does not touch the GLUED same-sign
shapes.  The exit-critical corner is the box's (K2-RES-C)(b) below.

## S7. THE RESIDUAL, RE-BOXED

    ┌────────────────────────────────────────────────────────────────────┐
    │ (K2-RES-C)  [OPEN — Q2's residue after this unit].  Supersedes    │
    │ NOTHING: (K2-RES-B) stays the g₀ = 2 authority; this box REFINES  │
    │ its items (ii)/(iv) and displays what moved.                      │
    │                                                                   │
    │ (a) THE GLUED/MERGE SAME-SIGN DEATHS [(K2-RES-B)(ii), untouched]. │
    │   The seven even-dup |T| = 2 glued NOVEL shapes (char-2 merge     │
    │   deaths), GS-40 (T = 0), AS-10 (char 3): OPEN exactly as boxed   │
    │   at K2-CLOSE; nothing here touches the glued stratum.           │
    │ (b) THE EXIT-CRITICAL CORNER [NEW NAME for the residual gate of   │
    │   (e) + K2-L9-RES + the g₀ ≥ 3 in-window pair — K2-L17(d)].      │
    │   The corner is REALIZED: promotion-carrying β_min cells exist    │
    │   in vivo (shakedown: 2 SDW4G2F rows; full census, commit 2: 67   │
    │   rows / 77 cells on 14 towers, EVERY row regular — S8-V), so     │
    │   by K2-L17(c) exit-critical rows exist and the genre CANNOT be   │
    │   closed by emptying the corner.  OPEN, exactly: on exit-critical │
    │   rows, can a same-sign multiplicity object ((e)'s −2 pair, a     │
    │   K2-L9-RES multi-cell class, the g₀ ≥ 3 in-window pair) BE the   │
    │   entire β_min system with char | every count?  The realized      │
    │   instances so far are glued-tower rows, protected in vivo        │
    │   [commit-2 update: the full census WIDENED this — 67 rows on 14  │
    │   towers, every one row-REGULAR per the extension leg (S8-V);     │
    │   all 332 rows protected (the instrument's K2-PROT), so the       │
    │   corner is realized broadly yet still threatens no traced row].  │
    │   The natural closer is the level-(m−1) exit-budget balance (the  │
    │   mass-accounting sketched at S6), NOT attempted here.            │
    │ (c) g₀ ≥ 3 BEYOND K2-THM-C's locus:                               │
    │   (c1) corner-uncovered single-window systems (the exact          │
    │        obstruction sector (+2, −1, −1) and its kin — S4);         │
    │   (c2) multi-window chains (the (b)/(e) analogues at budgets      │
    │        {4, 5} and beyond — un-enumerated);                        │
    │   (c3) glued g₀ ≥ 3 groups (no shape table exists);               │
    │   (c4) strict availability corners {0} ⊊ A ⊊ full (the −2 window  │
    │        at A = {0, 2} displayed at S2; only its window arithmetic  │
    │        is settled, not its β_min realizability);                  │
    │   (c5) IN-VIVO: g₀ ≥ 3 stays machine-dark — the engine cost wall  │
    │        RE-MEASURED this unit (S8: the minimal m = 2, g₀ = 3       │
    │        tower's prep exceeded the disclosed budgets), so the       │
    │        KR-VIVO family's outcome is SKIPPED (verdict: KRG3E1F     │
    │        hit the 3,000 s phase budget at 2,999 s prep, 0 rows      │
    │        traced; KRG3M2F/KRG4E1F phase-budget-exhausted disclosed  │
    │        skips — S8-V P-8).                                        │
    │        R-B's sealed prediction (an n = 5 window at β_min needs    │
    │        t = 2 seeds or near-full dumps) stands for the next        │
    │        instrument with engine work (an engine_ext-genre unit) as  │
    │        the stated prerequisite.                                   │
    │ (d) g₀ ≥ 5 columns: K2-L13/L14/L15/L16/L17 are any-g₀; the        │
    │   verdict COLUMN is machine-run at g₀ ∈ {3, 4} only (finite per   │
    │   g₀; the machinery extends without new mathematics).             │
    └────────────────────────────────────────────────────────────────────┘

## S8. Machine bracket: preregistered predictions, teeth, families — and the verdict

Sealed BEFORE the battery (commit 1; the runner's docstring carries the
same list).  Smoke + shakedown disclosure (= the runner docstring's,
verbatim in content): the abstract-enumerator smoke of S0; a FULL
shakedown of the abstract families (all green, teeth 3/3, one harness
repair: the P-7 obstruction lookup re-keyed to the census key); a
2-tower KR-PFREE shakedown (SDDPG2F + SDW4G2F, 13 rows, 0 violations)
whose finding RESHAPED P-6 as displayed.  The full 332-row census, the
sealed teeth run, and KR-VIVO run only at the verdict.

**Predictions.**
P-1  [KR-INV/KR-CAL]  The fresh enumerator (unit level AND raw-j₀
     level, two implementations cross-agreeing) reproduces the K2-L3
     g₀ = 2 inventory and the pinned `legal_drains_g2` EXACTLY; the
     g₀ = 2 column pipeline reproduces S/P1/P2/T1 with the K2CLOSE
     FORM_CORNERS corner table EXACTLY (I₁ = A₁, I₃ = E₀).
P-2  [KR-ORD/KR-TOT]  g₀ = 3 full availability: the displayed S2 table
     byte-exact; {C₁, C₂} at n = 5 is the UNIQUE multi-order multiset
     (2 orders); every (units, p) class total ∈ {−1, 0, +1};
     multi-member class totals ∈ {0, +1}.
P-3  [KR-ORD]  g₀ = 4 full availability: class totals all ∈ {−1, 0, +1};
     multi-order multisets exist with order counts up to 3 (including
     {C₂, C₃, C₃} ×3 at n = 7).
P-4  [KR-TOT]  Over ALL availabilities A ∋ 0 at g₀ = 3: the −2 window
     (A = {0, 2}, n = 5, units 3, p 0; census {C₀} −1, {C₂C₂C₂} −1) is
     the UNIQUE sector with |total| ≥ 2.  At g₀ = 4 such sectors exist
     at several corners INCLUDING the pure order-multiplicity +2
     (A = {0,1,2}, n = 7, units 5: {C₁C₂} ×2 alone).
P-5  [KR-SWAP/KR-INV]  At every g₀ ∈ {2,…,6}: A = {0} inventories are
     all-singleton one-event drains (K2-L16 steps 1–2 shadow);
     {C_{g₀−1}, P} fits at budget 2g₀ − 1, {C₀, P} never fits.
P-6  [KR-PFREE, MEASURED census — RESHAPED at the disclosed shakedown]
     Promotion-carrying junk-free β_min cells EXIST (2 SDW4G2F rows
     found pre-seal).  The full census (selection replicated from the
     pinned SD runner; rowset equality a law) measures: the exact
     row count (≥ 2 predicted), their towers (glued towers predicted;
     whether any REGULAR row carries one is OPEN — the census
     decides), and the single-/multi-window split of the β_min
     population.  All FINDINGS, never violations.
P-7  [KR-COL]  g₀ = 3 column: every single-seed singleton-census form
     is safe at every corner; every exchange-pair form is conditional
     exactly on its displayed identity; the obstruction sector's
     single-seed form is corner-safe exactly on {E₀ xor E₁}-type
     corners and NOVEL at fully-generic corners; every unconditional
     death across the column is char | T at a merge state or a T = 0
     dead state (the K2-MULT-RES genre law at g₀ ∈ {3, 4}).
P-8  [KR-VIVO, conditional]  If any g₀ ≥ 3 tower preps within the
     phase budget: all realized window budgets ≤ 2g₀ − 1 and the
     g₀-generic SD law families hold (XWALK/CONS/WIN-budget/FACT/NU/
     JBOT/COLOC laws); K2-PROT-form violations, if any, are reported as
     FINDINGS (the R-B counter-instance), not harness errors.  If no
     tower preps in budget: the phase reports the DISCLOSED SKIP and
     the measured prep times.

**Teeth (planted; each must FIRE or the battery is toothless).**
TOOTH-1  a firing-threshold mutant (events fire at ≥ g₀ − 1) must
         produce out-of-inventory drains vs K2-L3 at g₀ = 2.
TOOTH-2  a sign-parity mutant (sgn = (−1)^{|K|+1}) must break the
         g₀ = 2 FORM_CORNERS calibration.
TOOTH-3  a seed-quantum mutant (budgets n₀ + 2t) must break the g₀ = 2
         P1/T1 form reproduction.

**Families.**  KR-PIN (md5 pins) · KR-INV (fresh double enumerator +
calibration vs `legal_drains_g2`) · KR-ORD (order-multiplicity law) ·
KR-TOT (class totals, all availabilities) · KR-FORM (the K2-L14 form
space) · KR-IDC (K2-L15 lattice equality g₀ ≤ 6 + state-membership
shadow at g₀ ∈ {3,4}) · KR-COL (the verdict column g₀ ∈ {3,4}) ·
KR-CAL (the g₀ = 2 calibration column) · KR-SWAP (K2-L16/L17
arithmetic) · KR-PFREE (the 332-row in-vivo promotion census) ·
KR-VIVO (the g₀ ≥ 3 engine attempt, budget-boxed, skip = disclosed) ·
HARNESS.

<!-- VERDICT-ANCHOR -->

## S8-V. THE VERDICT (filled at commit 2 from the committed artifacts)

**Run provenance (disclosed in full).**  The sealed battery ran TWICE.
Run 1 died partway (its finisher's session ended); its artifacts are
archived at /tmp/k2res_partial_archive with `final: false` and NO
KR-VIVO block — it is NOT the run of record.  The committed artifacts
(`k2res_checks_output.txt`, `k2res_checks_results.json`) are the FRESH
full re-run, ended 23:26 UTC 2026-08-07, elapsed 3134.8 s, runner
completion marker printed, `final: true`, **0 violations** (exit 0).
The runner at this commit is byte-identical to the seal caa7c50
(`git diff` empty; md5 62b335241e39499ff5392a19e2f0928f).
Cross-check at the fold: every block the two runs share (violations,
findings, column, pfree, multiorder, bigT_sectors) is IDENTICAL —
the partial run agreed exactly as far as it got.

**Families: ALL 12 GREEN, 0 violations total** (KR-PIN / KR-INV /
KR-ORD / KR-TOT / KR-FORM / KR-IDC / KR-COL / KR-CAL / KR-SWAP /
KR-PFREE / KR-VIVO / HARNESS: each 0).  **Teeth 3/3 FIRED**: TOOTH-1
(threshold mutant) produced out-of-inventory extras at g₀ = 2;
TOOTH-2 (sign-blind mutant) collapsed P1's safe corner set to ∅;
TOOTH-3 (seed-quantum mutant) declassified P1 to OTHER.

**Predictions (P-1..P-8, judged against the committed artifacts).**
P-1  CONFIRMED — the fresh double enumerator (unit level + raw-j₀
     level) cross-agreed on 1,308 inventory pairs and reproduced the
     pinned `legal_drains_g2` exactly; the g₀ = 2 column reproduced
     S/P1/P2/T1 (4 forms) + FORM_CORNERS exactly (8 calibration
     checks; I₁ = A₁, I₃ = E₀).  [KR-INV/KR-CAL: 0 violations]
P-2  CONFIRMED — S2's g₀ = 3 full-A table byte-exact
     (`g3_table_exact`); {C₁, C₂} at n = 5 (units 3, p 0) is the
     UNIQUE g₀ = 3 multi-order multiset (2 orders — the committed
     multiorder block has exactly one g₀ = 3 record); class totals all
     ∈ {−1, 0, +1}, multi-member totals ∈ {0, +1}.  [0 violations]
P-3  CONFIRMED — g₀ = 4 full-A class totals all ∈ {−1, 0, +1};
     15 multi-order records with order counts up to 3, including
     {C₂, C₃, C₃} ×3 at n = 7 (realized at A = {0,2,3} AND at full A).
P-4  CONFIRMED — over all A ∋ 0 at g₀ = 3 the |total| ≥ 2 sector
     census is EXACTLY the −2 window (A = {0,2}, n = 5, units 3, p 0;
     {C₀} −1 + {C₂C₂C₂} −1); at g₀ = 4 there are 7 such sectors
     including the pure order-multiplicity +2 at (A = {0,1,2}, n = 7,
     units 5: {C₁C₂} ×2 alone).
P-5  CONFIRMED — at every g₀ ∈ {2,…,6}: A = {0} inventories are
     all-singleton one-event drains; {C_{g₀−1}, P} fits at budget
     2g₀ − 1; {C₀, P} never fits.  [KR-SWAP/KR-INV: 0 violations]
P-6  MEASURED (census delivered; the open clause DECIDED) — **67
     promotion-carrying rows / 77 promotion-carrying junk-free β_min
     cells on 14 of the 16 census towers** (332 rows; only the two
     SDDP duplicate-pair towers are promo-free); the junk-free β_min
     population is 689 cells = 664 single-window + 25 multi-window.
     The prediction's floor (≥ 2 rows, glued towers) was low by 30×
     and the tower prediction TOO NARROW: promotions appear on every
     tower genre (W3G2A 13 rows, EQ3G2B 16, SDW3G2F 13, …).  The open
     REGULAR-row clause is decided by the disclosed extension leg
     (below): ALL 67 rows are REGULAR.
P-7  CONFIRMED — g₀ = 3 column: 26 forms × 16 corners = 416 pairs →
     254 SAFE / 52 CONDITIONAL / 110 NOVEL / 0 capped; g₀ = 4 column:
     72 forms × 64 corners = 4,608 pairs → 2,030 / 1,140 / 1,438 / 0.
     EVERY NOVEL death is T = 0 (72 resp. 787) or char | T (38 resp.
     651) — the K2-MULT-RES genre law with NO third genre.  The
     obstruction sector (n = 5, units 3, p 0, T = [2]) is corner-safe
     EXACTLY on the 8 E₀-xor-E₁ corners (machine law
     `obstruction_corner_law`); singleton-census single-seed forms
     safe at all corners.  **KR-COL is K2-THM-C's verdict column and
     it CONFIRMS the theorem on its locus — 0 capped pairs, no
     contradiction.**
P-8  CONFIRMED on the SKIP branch — **KR-VIVO outcome: the budget box
     was HIT; the skip is disclosed.**  NO tower prepped within the
     3,000 s phase budget: KRG3E1F consumed 2,999 s in prep before the
     alarm fired; the TimeoutError was absorbed by the walk's
     engine-unsupported catch, so the tower appears under `traced`
     with 0 rows — an accounting artifact disclosed here: it is a
     SKIP, not a trace (0 rows entered any census; pfree rows stayed
     332; `tower_skipped` = 1).  KRG3M2F and KRG4E1F: phase budget
     exhausted, disclosed skips.  Consequence: fence F-2 stands
     VERBATIM (no g₀ ≥ 3 row has ever been engine-traced) and
     (K2-RES-C)(c5) stays machine-dark with R-B's sealed prediction
     untested.

**The PFREE census, group-resolved (the exit-critical corner's in-vivo
geography).**  Of the 77 promotion-carrying cells: 67 sit in singleton
β_min groups of class total +1, 2 in singleton groups of total −1, and
8 in size-2 groups of total 0 — so EVERY realized multi-cell β_min
group carrying a promotion is an opposite-sign net-0 pair, and NO
same-sign |total| ≥ 2 group carries one (the (K2-RES-C)(b) threat
object remains UNREALIZED in vivo).  All 67 rows have a protected
group (`rows_without_protected_group` = 0); all 332 rows protected
(the instrument's K2-PROT).

**Disclosed post-seal EXPLORATORY extension (NOT part of the sealed
battery): `k2res_regx_ext.py` + `k2res_regx_ext_results.json`,
retained.**  Written and run at the verdict stage because the sealed
runner records promo rows without their K2-REG flag — exactly the
clause P-6 left "the census decides".  It re-traces EXACTLY the 67
promo rows named in the committed findings (walk copied verbatim from
the sealed `pfree_row`, `ce` retained; read-only over the pinned
stack).  Verdict: **67/67 rows REGULAR** (full availability + every
β_min group single-fiber); recount matched the committed cell counts
77/77; 77/77 promo cells in single-fiber groups; 0 rows unprotected.
Timing disclosure: this leg ran against the FIRST (partial) run's
findings block, which is byte-identical to the committed fresh run's
findings block (verified at the fold), so its verdict applies to the
committed artifacts verbatim.

**Grade impact: NONE beyond F-5.**  No family RED; KR-COL does not
contradict K2-THM-C; every prediction CONFIRMED (P-6 measured as
preregistered, its point predictions low but framed as measurement).
The grades stand as written: everything ATTEMPT, capped by the W arc
(0/2 CLEAN) through the consumption chain; this note's own hostile
arc 0/0.

## S9. Claims, consequence, fences, annex proposals

| # | claim | grade | consumes |
|---|---|---|---|
| K2-L13 | any-g₀ drain calculus; order count law; multiplicity iff g₀ ≥ 3; genre unification | PROVED (attempt; any g₀ ≥ 2) | K2-L1(a)(d)†, K2-L2†, K2-L4†, K2-L5(b)†, K2-L8†, [RMG] S2.2 |
| K2-L14 | single-window single-book β_min groups = t-graded drain censuses, complete, with order-count multiplicities | PROVED (attempt; any g₀ ≥ 2; (R0)) | K2-L1(c)(d)†, K2-L4/L5/L6†, W-D2(iv)†, K2-L13 |
| K2-L15 | the identity lattice L(g₀) = ⟨A_•, E_•⟩, rank 2(g₀−1), saturated; single-book coincidences ⊆ L(g₀) | PROVED (attempt; any g₀ ≥ 2) | K2-L4†, K2-L6† |
| K2-THM-C | (SURV-K2) at g₀ ∈ {3,4} on (C0)–(C3) (single-window corner coverage) | PROVED (attempt, capped; machine column) | K2-L12..L15, SD-L6/L7† |
| K2-COR-C | the consequence chain at g₀ ∈ {3,4} on the locus; first g₀ ≥ 3 (SURV) closure | PROVED (attempt, capped) | S-L3†, S-THM(b)† |
| K2-L16 | A = {0} single-book single-class single-sign at EVERY g₀ | PROVED (attempt; any g₀ ≥ 2) | K2-L9†'s mechanism, K2-L13, W-D2(iv)† |
| K2-L17 | the promotion-swap reduction: the single-book same-sign genre ⊆ the exit-critical corner, every g₀ | PROVED (attempt; any g₀ ≥ 2) | K2-L5†, K2-L8†, L-LINE†, L-NORM(a)† |
| (K2-RES-C) | the residual: glued deaths, exit-criticality, g₀ ≥ 3 beyond-locus, g₀ ≥ 5 columns | **OPEN** (boxed, S7) | — |

**Consequence display.**  (i) Q2's residue line ("K2-MULT-RES +
g₀ ≥ 3") is now ONE genre with three fronts: glued merge deaths
((K2-RES-C)(a), untouched), the exit-critical corner ((b), NEW — the
single gate for every single-book same-sign threat at every g₀), and
the g₀ ≥ 3 beyond-locus strata ((c)).  (ii) (SURV-K2)'s existence half
now holds additionally on the K2-THM-C locus at g₀ ∈ {3, 4} — the
first g₀ ≥ 3 coverage of any kind — and on the A = {0} single-book
stratum at every g₀ (K2-L16).  (iii) The LAW-DCX pinned class gains the
K2-THM-C locus at the capped grade via K2-COR-C, exactly as K2-THM-B
widened it at g₀ = 2 (the fold is the orchestrator's).

**Fences.**
F-1  K2-THM-C is SINGLE-WINDOW and single-book with FULL availability;
     do not cite it on multi-window, glued, or partial-A rows.  (C1) is
     a row predicate, machine-decidable given a trace, NOT a tower
     predicate.
F-2  The column's SAFE verdicts quantify over closed states consistent
     with a corner, torsion kept — K2-L12's conservative reading; the
     abstract model's engine faithfulness rests on K2-L4 (+ its machine
     shadow on the g₀ = 2 traced set); NO g₀ ≥ 3 row has ever been
     engine-traced (KR-VIVO skipped all three roster towers — S8-V
     P-8; this fence stands verbatim).
F-3  K2-L16 is single-book (glued A = {0} evades it — AS-01/02/03,
     exactly as at g₀ = 2); A = {0} is instance-dark at every g₀.
F-4  K2-L17(c) is a CONDITIONAL undercut: it reduces, never decides;
     exit-criticality is open ((K2-RES-C)(b)).  KR-PFREE measures the
     traced g₀ = 2 world only.
F-5  Grade: everything ATTEMPT, capped by the W arc (0/2 CLEAN) through
     the consumption chain; this note's own arc 0/0 — no hostile pass
     has run; the Codex leg queues with the other K2 legs.
F-6  Nothing here upgrades (SURV-K2) class-wide, touches (SD-YJ), the
     EMPTY arc, or any glued statement; (K2-RES-B) remains the g₀ = 2
     authority with its (ii) refined per S7.

**Annex proposals (text proposed, NOT applied — the notes of record
are not edited by this unit).**
* SURVK2_PROOF S7 box (d), proposed dated annex line: "[K2-RES annex,
  2026-08-08 campaign (wallclock 2026-08-07), K2RES_PROOF: the
  order-multiplicity candidate is ABSORBED into the same-sign
  multiplicity genre (K2-L13(d)); the −2 window's arithmetic is
  confirmed the unique |total| ≥ 2 g₀ = 3 sector (KR-TOT); the
  single-window slice of g₀ ≥ 3 is now graded — K2-THM-C — and the
  A = {0} single-book slice closed at every g₀ (K2-L16); the residual
  is re-boxed at (K2-RES-C)]".
* K2CLOSE (K2-RES-B)(ii), proposed dated annex line: "[K2RES annex,
  same stamp: the (e)/K2-CAND pair and K2-L9-RES are both confined to
  the EXIT-CRITICAL corner (K2-L17(d)); in-vivo census KR-PFREE:
  67 promotion-carrying rows / 77 cells on 14 towers, ALL row-regular,
  every realized multiplicity an opposite-sign net-0 pair, every promo
  row protected — the corner is realized and benign-so-far; the genre
  statement extends to g₀ ∈ {3,4} single-window by the KR-COL
  column]".
* K2CLOSE_LEGS23 (K2-L9-RES) box, proposed annex: "[K2RES annex, same
  stamp: the natural closer is now K2-L17's swap reduction — the
  rigidity question is needed only on exit-critical rows]".

**Dependency line.**  Consumes: SURVK2_PROOF_2026-08-08.md† (K2-L1..L8,
THM-A, forms) · K2CLOSE_2026-08-08.md† (K2-L11/L12, THM-B, column
discipline) · K2CLOSE_LEGS23_2026-08-08.md† (K2-L9/L10) ·
WMULTDCX_2026-08-08.md† (W-D2(iv) — the binding W-arc cap rides here) ·
[RMG] (accepted) · SURVDISJ_PROOF† (SD-L6/L7) · SURV_PROOF† (S-L3,
S-THM(b)) · [ILN]† S0.1 through K2-L8.  Machine leg:
`verification/openmath/k2res_checks.py` + artifacts (md5s at the
commits).  Instrument artifacts consumed as design input + instance
evidence only.



