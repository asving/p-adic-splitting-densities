# RMENGINE — the generic (RM-m) correction-product engine: the four box clauses PROVED level-generically on the T-scope (either d₀ = 1 — degenerate flat reads admitted — or all reads below the top non-degenerate [r3, PC3-g1: the gloss now names both branches of the amended scope]) by the depth-calculus — the cell tree, the exact line quanta on the promotion edge, the 𝔷-multiplicativity kernel, and the generic termination measure — with the level-m uniform carry law stated at every m (the second-round clause, conjecture-grade), the d₀ ≥ 2 flat corners fenced (the d₀ = 1 flat corners PROVED at r1), and the first level-3 engine data

**Unit:** the GENERIC (RM-m) ENGINE COMPOSE — THE CLOSURE PROGRAM's central
unit (ROOT §44.(ii) Q1; opened by the RM-2 probe ced7dbb and gated by the
DCX3 sharp-locus seal). Genre: attempt-grade theorem note in the
ITERLAW/ITERLAW3/ITERLAWN genre — finite algebra executed in full where the
level-generic proof closes, explicit displayed open boxes WITH PRICES where
it does not. Acceptance counter 0 — no hostile pass has run on THIS note.
Author: Fable, campaign date 2026-08-08 (wallclock 2026-08-04).

**[ACCEPTED — 2026-08-08 campaign (wallclock 2026-08-04). THE BAR MET AT
ROUND 7.** The accepted text = the r6 text, commit **a0e266c** (this
bracket, the S7.1 ledger-application bracket, the row-11c erratum
bracket, and the tail ACCEPTANCE RECORD are the only post-acceptance
additions; they alter no accepted content). Round-7 verdicts, both on
a0e266c, model-diverse, the two verdict lines verbatim from the archived
reports (`RMENGINE_passPC7_report.md` 5021a3b;
`RMENGINE_passPE7_report.md`):

    RMENGINE-PC7 FINDINGS: 0 critical, 0 gaps / VERDICT: CLEAN
    RMENGINE-PE7 FINDINGS: 0 critical, 0 gaps / VERDICT: CLEAN

Arc totals [CORRECTED 2026-08-04, same day — the r40 fold unit flagged
this bracket's original figures ("12 passes, zero critical across the
arc") against the ledger's own 14 verdict legs; the original conflated
the post-repair streak with the whole arc]: **7 rounds, 14 hostile
passes (7 Codex + 7 Fable). Round 1, on the composed r0 text, found TWO
criticals — PC-C1 (adjudicated TEXT-GAP by PE1: the missing L-NORM
display) and PE1-F1 (display-local: the arithmetically false fence
mechanism, cured at r1 by the branch-(b) STRENGTHENING). Rounds 2–7:
TWELVE consecutive passes with zero critical findings and zero
mathematical gaps.** No critical ever touched a theorem clause; the
S2–S7 mathematical content is byte-stable from the
r1 text (8835d20) except the L-NORM citation parenthetical (r2, PE2-3).
**THEOREM RM-GEN — the four (RM-m) box clauses, level-generically for
every m ≥ 1 on T-scope(m) = d₀ = 1 ∨ (e₀ ≥ 2 ∧ eᵢ ≥ 2) — is ACCEPTED**
(the corpus's hostile 2-clean model-diverse bar). Honest residuals,
unchanged by acceptance: the d₀ ≥ 2 ∧ (e₀ = 1 ∨ ∃ flat) corners remain
FENCED; LAW-DCX-(m+1) remains a sealed conjecture at m ≥ 2 (row 11c's
literal floor identification is REFUTED — see the erratum bracket at S8
— with the corrected PID† proved at attempt grade in
`WMULTDCX_2026-08-08.md`); COR-A's ledger application now FIRES (S7.1
bracket). Ledger entry: BRIDGE_ADJUDICATIONS_2026-07-30.md, the [RMG]
block.]**

**[REPAIR ROUND 1 — 2026-08-08 campaign (wallclock 2026-08-04).** Applied
against RMENGINE-PC (report 2b29712: 1C/4G) and RMENGINE-PE (report
363a051: 1C/2G/3n, with machine witnesses; `rmengine_pe_reimpl.py` = the
implementation-forced referee). Every edit below carries a dated bracket
naming its finding. The dispositions: **R1** (PC-C1, critical, adjudicated
TEXT-GAP by PE) — Lemma L-NORM displayed in S2.1, L-LINE(ii)/(iii) and
DEV-(m) re-derived through it. **R2** (PE-F1, critical display-local) —
the S4.2 fence-box mechanism corrected (ρ = P − 1), the fence re-scoped to
d₀ ≥ 2, and L-MU/T-TERM/RM-GEN EXTENDED to the d₀ = 1 flat corners with
the full displayed derivation (a strengthening, argued in full; L-LEX
subsumed, retained as a remark). **R3** (PC-G2) — the floor-form
derivation REGRADED: crossing law proved, pool identification heuristic;
S8 row 11 split; the L-MU T-scope leak pinned. **R4** (PC-G5 + PE-F3) —
Lemma L-GRID (on-line ⟹ on-grid): the (D3) off-grid case is EMPTY; the
converse-integrality appeal deleted; Lemma L-VNZ (v ≠ 0 forced) replaces
scope remark (ii)'s dead clause. **R5** (PC-G4 + PE-n5) — COR-A inline
at-acceptance qualifier + the n = 1, 2 exact-union correction. **R6**
(PC-G3 + PE-n4/n6) — claims table completed (C15–C22 [r2, PE2-2: this
bracket formerly said C15–C19, a metadata slip — the S8 table is the
authoritative surface] [r5, PC5-g1 supersession: "completed" was this
round's claim; refuted at rounds 2–3 (rows C23–C33 followed) — the tag
census is the arbiter]), C2 hypotheses
corrected and its key unwrapped, the garbled displays rewritten.]**

**[REPAIR ROUND 2 — 2026-08-08 campaign (wallclock 2026-08-04).** Round-2
verdicts on the r1 text (8835d20): **PC2** (Codex, report 9466efe)
GAPS-ONLY, 0 critical / 2 gaps; **PE2** (Fable, report
`RMENGINE_passPE2_report.md`, with the extension referee
`rmengine_pe2_ext.py`: 67 zero-violation engine calls on 8 fresh d₀ = 1
flat towers + a 1080-shape branch-(b) sweep + 225 d₀ ≥ 2-flat
infeasibility confirmations) GAPS-ONLY, 0 critical / 1 gap + 2 notes.
Every round-1 remedy DISCHARGED under both hostile re-derivations; the
no-weakening claim was verified against the actual git diff by PE2
(re-verified by PE3), while PC2 — execution-less in its sandbox
(bubblewrap unavailable, its report item 7) — verified it CONTENT-ONLY
against the supplied text [r3, PE3-G-2: this bracket formerly credited
"both" with the git-diff verification — false as to PC2]. All
surviving findings are documentation-layer, repaired in place at [r2]
brackets: the claims-table completeness residue (PC2-g2 = PE2-1,
convergent: rows C23–C28 added), the repair-record hygiene wording
(PC2-g1: regrade ≠ weakening), this head bracket's C15–C19 slip (PE2-2),
and the L-NORM citation paraphrase (PE2-3). Acceptance counter 0/2 —
GAPS-ONLY is not CLEAN; round 3 (PC3 + PE3, the acceptance attempt) runs
on THIS text.]**

**[REPAIR ROUND 3 — 2026-08-08 campaign (wallclock 2026-08-04).** Round-3
verdicts on the r2 text (423b399): **PC3** (Codex, report
`RMENGINE_passPC3_report.md`) GAPS-ONLY, 0 critical / 3 gaps; **PE3**
(Fable, report `RMENGINE_passPE3_report.md`, full spine re-derivation +
byte-exact re-run of the committed referee `rmengine_pe2_ext.py`)
GAPS-ONLY, 0 critical / 3 gaps + 2 notes. FOUR consecutive hostile
passes with ZERO critical findings and ZERO mathematical gaps — every
finding of round 3 is record-layer. Repaired in place at [r3] brackets:
the stale headline gloss (PC3-g1); the under-enumerated r1
statement-diff sentence + row C28 (PC3-g3); the PC2 git-diff
over-attribution (PE3-G-2); the r2-broken grep keys re-quoted
(PE3-G-1: C26, C28; sweep also re-keyed C7, C10); the byte-identity
exception clause completed (PE3-N-1) and the S2.1 census-gloss caveat
added (PE3-N-2). THE STRUCTURAL KILL of the recurring census defect
(PC-G3 → PC2-g2/PE2-1 → PC3-g2/PE3-G-3, three rounds of hand censuses
each refuted by the next): the S8 charter is re-arbitrated to the TAG
GRAMMAR — every charter-keyword line of the body now carries an explicit
row tag ⟦C<nn>⟧ or the challengeable non-claim marker ⟦nc⟧, rows C29–C33
added (incl. PE3's L-LEX witness), machine-checked CLOSED by
`verification/openmath/rmengine_claims_census.py` (committed output).
The r3 edit adds trailing tags throughout the body (the pre-S0 head
matter and S0–S7, S9, S10 lines) [r4, PE4-N-1: this sentence formerly
said "ONLY … on S2–S7 lines" — under-scoped; the tail record had the
correct scope] [r5, PE5-n: pre-S0 head-matter tags now named] plus the
charter/rows/records edits and the two queue-named wordings (title
gloss; the S4.2 census-gloss caveat): no S2–S7 display content changed.]**

**[REPAIR ROUND 4 — 2026-08-08 campaign (wallclock 2026-08-04).**
Round-4 verdicts on the r3 text (ab2e471): **PC4** (Codex, archived
998e599) **CLEAN — 0 critical, 0 gaps** (2 notes: the one-tag-per-line
loophole confirmed hypothetical with no instance; execution
unavailable). **PE4** (Fable, `RMENGINE_passPE4_report.md` + referee
`rmengine_pe4_ext.py`) GAPS-ONLY — 0 critical, 1 gap: the r3 "SIX
changes, complete" r1 statement-diff census missed a seventh of its own
genre (L-LINE(ii)'s vanishing clause → its NEGATION at L-NORM(a));
PE4's machine legs all green (census reproduces CLOSED; the pe2
referee byte-identical; the r3 statement fence verified against the
actual diff; all 44 row keys exact; all 12 ⟦nc⟧ survive challenge).
FIFTH AND SIXTH consecutive 0-critical passes; the math surface has
never moved. Repaired at [r4] brackets: change (7) added and THE
COMPLETENESS CLAIM WITHDRAWN — the diff is the authoritative surface,
the enumeration a reading aid (the fourth hand-census recurrence ends
the pattern: enumerations in this note no longer claim completeness;
machine-checkable surfaces — the tag census, the diff — are the
arbiters); the round-3 bracket's fence sentence re-scoped (PE4-N-1).
Acceptance counter: PC4 CLEAN = the Codex leg met on ab2e471; the r4
edits are record-layer only (no S2–S7 content touched), so round 5 =
PC5 + PE5 on THIS text is the acceptance attempt with bar BOTH CLEAN.]**

**[REPAIR ROUND 5 — 2026-08-08 campaign (wallclock 2026-08-04).**
Round-5 verdicts on the r4 text (d63f8bb): PC5 (Codex) GAPS-ONLY 0C/1G;
PE5 (Fable, `RMENGINE_passPE5_report.md`) GAPS-ONLY 0C/1G + 4n —
seventh and eighth consecutive 0-critical passes, both spine sweeps
clean, PE5's machine legs green. Both gaps are record ripples of the r4
repair itself: three prior-round record cells still asserted complete
enumerations un-superseded (PC5-g1), and the round-3 record's census
quote went stale when r4's own record surfaces changed the zone count
(PE5-G-1). Repaired at [r5] brackets with TWO STANDING RULES displayed
in the round-5 record: supersede-at-refutation (a later round's
refutation must bracket the earlier record in the same repair) and
machine-output quotes carry their as-of commit (only the committed
output file speaks for the current text). The census re-ran CLOSED
after the r5 edit (output re-committed with this round). Round 6
(PC6 + PE6, on THIS text) is the acceptance attempt, bar BOTH CLEAN.]**

**[REPAIR ROUND 6 — 2026-08-08 campaign (wallclock 2026-08-04).**
Round-6 verdicts on the r5 text (cfb3648): **PE6 (Fable,
`RMENGINE_passPE6_report.md`) CLEAN — 0 critical, 0 gaps** (2 notes;
machine legs: census CLOSED byte-identical, referee + diag re-runs
byte-identical, the r5 fence verified, the historical zone figures
machine-verified, the dagger pastes content-token identical, the spine
re-derived from scratch — the ninth consecutive 0-critical pass);
**PC6 (Codex, archived 0182e1b) GAPS-ONLY — 0 critical, 1 gap**: the r5
records asserted TWO machine-quote rules (as-of/file-arbiter AND
"re-runs are re-quoted") while the note satisfies only the first.
Repaired at [r6] brackets: the rule UNIFIED to the file-arbiter form at
both asserting sites (the second rule superseded in place per the
supersede-at-refutation rule), and PE6-N-1's inline as-of added to the
round-4 record's zone figures. The r6 edit is record-layer only; the
census re-ran CLOSED (output re-committed with this round). Both legs
have now returned CLEAN on this stable text (PC4 on ab2e471; PE6 on
cfb3648), but never in the same round — **round 7 (PC7 + PE7, on THIS
text) is the acceptance attempt, bar BOTH CLEAN.**]**

**HEADLINE (the exact grade, stated before anything else).** The (RM-m) box
of [ILN]† S4.6 — the one resisting leg of the accepted all-orders theorem —
is PROVED in this note, level-generically for every m ≥ 1, ON THE T-SCOPE

    T-scope(m) := d₀ = 1  ∨  (e₀ ≥ 2 ∧ e_i ≥ 2 for all 1 ≤ i ≤ m−1)

[r1, PE-F1: the definition is AMENDED at repair round 1. The composed
(pre-repair) scope was T-scope₀(m) := (e₀ ≥ 2 ∨ d₀ = 1) ∧ (e_i ≥ 2 for
all 1 ≤ i ≤ m−1) ⊂ T-scope(m); the d₀ = 1 flat corners were then fenced
by an arithmetically false mechanism display (ρ misread as P instead of
the note's own ρ = P − 1). The extension is a STRENGTHENING, proved in
full in S4.1/S4.2 + the S5 inspection census; every downstream occurrence
of "T-scope(m)" in this note now denotes the amended scope],

all four displayed clauses (weight-R, weight-Q, R-read = z_m^{ε′}·v·τ_m,
ε′-carry identity), with the division tree terminating under the generic
measure of S4. At m = 1 the T-scope reproduces exactly the known RM-1 scope
(e₀ ≥ 2, plus e₀ = 1 ∧ d₀ = 1; the two definitions coincide at m = 1) — an
independent generic re-derivation, not a consumption, of [IL3]†'s DEV-1
mechanism. OFF the T-scope, i.e. on d₀ ≥ 2 ∧ (e₀ = 1 ∨ ∃ i ∈ [1, m−1]:
e_i = 1), the theorem is OPEN — the remaining flat corners, fenced and
priced in S4/S10 [r1: re-scoped per PE-F1; the RM-2 probe's e₁ = 1 ∧
d₀ = 1 shape (405/405 instance-true) now sits INSIDE the proved scope,
and RM2F (e₀ = 1 ∧ d₀ = 2) stays outside, untested on record]. The
SECOND-ROUND
clause (the sharp DCX_{m+1} locus) is NOT proved: it is stated generically
as LAW-DCX-(m+1) (S6, the nested-floor form specializing to [IL3]'s proved
m = 1 sharp form and to THE UNIFORM CARRY LAW v2 at m = 2, the latter
consumed as CONJECTURE-GRADE SEALED throughout); consequently
**(W-MULT-DCX-m) STANDS** — the S7 conditional display, not a corollary.

**The consequence for the model class (S7):** with (RM-m) proved on the
T-scope, ITERLAWN S7 rows 5/6/8 upgrade on the T-subclass (at acceptance
only — S7.1): (ITER-LAW-n) off DCX-(n) is UNCONDITIONAL (modulo the ⟦C32⟧
accepted [IL]†/[IL3]†/[ILN]† packages) on every tower with d₀ = 1 ∨
(e₀ ≥ 2 ∧ e_i ≥ 2 for 1 ≤ i ≤ n−2) [r1: extended per PE-F1; n ≥ 3 —
see S7.1 for the n = 1, 2 correction]. The remaining mathematical
distance on the model class = the d₀ ≥ 2 flat corners + (W-MULT-DCX-m) +
the fenced corners of record + (TAIL-STAB).

**[HEAD DECLARATION — THE INCORPORATION CONVENTION.** Per the convention of
record (ledger 9f9910c, with the VC39b whitespace-normalization amendment:
paste fidelity = content-token identity), this note consumes exactly three
accepted defined-term blocks, DECLARED HERE ONCE with full pastes; the
dagger forms **[IL]†**, **[IL3]†**, **[ILN]†** incorporate these blocks by
reference at every other mention; full pastes recur only at operative ⟦nc⟧
consumption sites (marked). No bare acceptance token appears anywhere in
this note, headings and table cells included.

**[IL]† = the ledger block "ITER-LAW ACCEPTED — 2026-08-03"
(`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`):** [IL] =
`ITERLAW_2026-08-08.md`, ACCEPTED 2026-08-03 at the 2-clean model-diverse
bar — "Scope as accepted: the harness tower class (superset of
DITER_RESTATE §S2's E₂ > 1 pin; on-the-nose coincidence restricted to
E₂ > 1); residuals: (ITER-LAW-LIFT) open exactly at g₀ = 1 ∧ δ₁ = 1; gr(w₂)
wrapper rides the accepted GRB retarget; orders ≥ 3 untouched."

**[IL3]† = the ledger block "[IL3] ACCEPTED — 2026-08-03" (same ledger):**
"[IL3] = the (ITER-LAW-3) theorem, ACCEPTED 2026-08-03: the display c₃ =
z̄^{δ₃}·z₂^{D₂}·z₁^{D₁} (δ₃ = ⌊(s₃+s₃′)/e₂⌋; D₂ = (s₂-defect + ℓ₁γ₃δ₃)/e₁;
Σ = D₂γ₂ + δ₃γ₃(ℓ₁′−ℓ₁w₁Φ₁); D₁ = (s₁-defect + ℓ₀Σ)/e₀) PROVED on the
3-read standard-lift harness class OFF the DCX corner {g₀ = g₁ = 1, δ₁ = 1,
s₂(γ) = s₂(γ′) = e₁−1, S₁+s(γ₂) ≥ 2e₀} (EMPTY at e₀ ≤ 2) — RUNG n = 2 of
(ITER-LAW-n). RESIDUALS: (W-MULT-3-DCX) OPEN (instance-true 132/132); the
fenced non-consumed DEV-1 corner {e₀=1 ∧ d₀≥2 ∧ j≥g₀}; the consumed [IL]
base's package rides through." (Inside this quoted block S₁ is [IL3]'s own
notation for the raw slot sum s₁+s₁′; byte-frozen, not renamed.)

**[ILN]† = the ledger block "[ILN] ACCEPTED — 2026-08-03" (same ledger):**
"[ILN] = the (ITER-LAW-n) theorem note, ACCEPTED 2026-08-03: the uniform
recursion c_{n+1}(γ,γ′) = z̄^δ·∏ⱼ z_j^{D_j} (S_n = δγ_{n+1}; D_j =
(s_j-defect + ℓ_{j−1}S_j)/e_{j−1}; S_{j−1} = D_jγ_j + S_j(ℓ_{j−1}′ −
ℓ_{j−1}w_{j−1}Φ_{j−1})) for ALL n ≥ 1 on the (n+1)-read standard-lift
harness class, PROVED with the structure:
integer/positivity/window/cocycle/descent + REALIZE/WELL-DEF
unconditional-generic; L6-(n) every rung from [IL]† (+ S4.1 at rungs ≥ 2);
W-MULT-(n) Case I generic given the IH package; Cases II generic MODULO
(RM-m, m ≥ 2) (m = 1 proved inside [IL3]†), off DCX-(n) (outer bound proved
given the IH package). RESIDUALS: (RM-m, m ≥ 2); (W-MULT-DCX-m); the fenced
corners; the consumed [IL]†/[IL3]† packages ride through. Rung ledger:
n = 1 [IL] ACCEPTED · n = 2 [IL3] ACCEPTED-OFF-DCX · n = 3 MEASURED GREEN
(13b026b) · n = 4/5 measured (5fe901e diag) — instance evidence only."

**Sealed measured artifacts consumed, with their exact grades (stated here
and at every use):** the RM-2 ENGINE PROBE (`RM2_PROBE_2026-08-08.md`, seal
0b42885 → verdict d860a5a): the four box clauses instance-true 405/405 at
m = 2; the m = 2 termination measure μ = e₀g₀(e₁+1)j₁ + 2e₁j₀ strict on
e₀ ≥ 2 ∧ e₁ ≥ 2, flat promotions at e₁ = 1; the [ILN]† S4.7 conjectured
emptiness transport REFUTED (DCX₃ live at e₁ = 2) — INSTANCE EVIDENCE ONLY, ⟦C15⟧
never a proof step. THE UNIFORM CARRY LAW v2 (`DCX3_SHARP_2026-08-08.md`, ⟦C15⟧
reseal a9dd275, verdict GREEN 0/11,808 on the second fresh roster; union
class 15 shapes): DCX₃ = TRACK ∧ (s₂+s₂′) + s₂(γ₃) + ⌊(s₁+s₁′+s₁(u₀))/e₀⌋
≥ 2e₁ — **SEALED-CONFIRMED at CONJECTURE GRADE**; consumed in S6 as the
displayed m = 2 instance of LAW-DCX-(m+1) and NEVER as a proof step; this ⟦C12⟧
note does NOT derive it generically, so its grade is unchanged here. The
depth-exact mechanism legs (C2/C3: deficit-k overflow carried entirely at
promotion depth ≥ k; direct rows clean-visible) — sealed instance evidence
consumed as DESIGN INPUT for the S1 architecture only.]** ⟦C15⟧

**PRINT-CONSUMPTION DISPLAY.** As in [IL]†/[IL3]†/[ILN]†: no printed
formula and no text extraction is consumed. Every object is consumed as a
DEFINITION — the harness calculus (`verification/openmath/`
`grb_order2_check.py` levels 0–2, `strata3_probe.py` level 3,
`iterlaw4_probe.py` level 4, `iterlawn_diag.py` level 5, and this unit's
own post-seal diagnostic `rmengine_diag.py`, built by the same
one-level-up mirror discipline). Print faithfulness lives upstream in the
accepted GRB/TR3 arcs and is not consumed.

## S0. Setting, the statement under proof, canonicity

### S0.1 The class pin

The (n+1)-read standard-lift harness class of [ILN]† S0.1, verbatim: O a
DVR with uniformizer π, residue field k₀, w₀ the Gauss valuation on O[x];
tower data Φ₀ (monic, deg d₀, reduction irreducible), reads (e_j, h_j, g_j)
with gcd(e_j,h_j) = 1, fields K_{j+1} = K_j[z_{j+1}]/(ψ_j), the uniformizing
convention γ₁ := h₀, γ_{j+1} := e_jw_jΦ_j + h_j, w_{j+1}Φ_{j+1} :=
e_jg_jγ_{j+1}; canonical ε-corrected lifts Φ_{j+1} (standard lift at j = 0),
certified on the class by [ILN]† S4.1 (REALIZE-(m)/WELL-DEF-(n)); normalized
Bézout pairs (ℓ_j, ℓ_j′). The calculus (w_m, reads R_{m,λ}, ε_m, eq-(12-int)
splits s_m(·)) is the [ILN]† S0.2 mirror recursion, consumed as definitions.
Abbreviations local to this note: P_l := e_lg_l (the level-(l+1) period),
E_m := e₀e₁⋯e_{m−1}, and the level-m line scales

    Γ_l := γ_{l+1} · ∏_{l<t<m} e_t   (l = 0, …, m−1;  Γ_{m−1} = γ_m).

(ITER-LAW-LIFT) propagates: nothing is claimed for non-canonical lifts. ⟦C21⟧

### S0.2 THE STATEMENT UNDER PROOF — the (RM-m) box, quoted verbatim (operative-site paste)

From [ILN]† S4.6 (byte-frozen there; the m = 1 STATUS lines and the m ≥ 2
resistance pricing are part of the box):

> **(RM-m) [READ-MULT with the canonical correction, level m], 1 ≤ m.**
> Setting: level m of a class tower with g_m = 1, Ĉ_m the canonical
> correction digit of WELL-DEF-(n)(ii) (deg < deg Φ_m, w_m = γ_{m+1},
> R_{m,γ_{m+1}}(Ĉ_m) = τ_m = ε_m(γ_{m+1})^{−1}c_m). Input: f ∈ O[x],
> f ≠ 0, deg f < deg Φ_m, λ := w_m(f), v := R_{m,λ}(f) its read. Claim:
> there exist R, Q ∈ O[x] with deg R < deg Φ_m, deg Q < deg Φ_m, and
>
>   f·Ĉ_m = R + Q·Φ_m,   w_m(R) ≥ λ + γ_{m+1},
>   w_m(Q) ≥ λ + γ_{m+1} − w_mΦ_m   (Q = 0 allowed),
>   R_{m,λ+γ_{m+1}}(R) = z_m^{ε′}·v·τ_m,
>   ε′ := ⌊(s_m(λ) + s_m(γ_{m+1}))/e_{m−1}⌋  [the level-m EPS-carry].

The four clauses named, for the ledger: **(C-W_R)** the R-weight clause;
**(C-W_Q)** the Q-weight clause; **(C-READ)** the R-read clause; **(C-EPS)**
the ε′-carry identity s_m(λ) + s_m(γ_{m+1}) − s_m(λ+γ_{m+1}) = e_{m−1}·ε′.
((C-EPS) is [IL]† L1 = EQ12-ADD at the level-(m−1) read, restated; it is
proved wherever cited below by that two-line lemma alone.) ⟦C1⟧

### S0.3 Canonicity (the RM2_PROBE reduction, now displayed in-note)

**Lemma L-CANON.** Since deg f < deg Φ_m and deg Ĉ_m < deg Φ_m, any
decomposition f·Ĉ_m = R + QΦ_m with deg R < deg Φ_m forces (Q, R) =
divmod(f·Ĉ_m, Φ_m) (uniqueness of division by the monic Φ_m;
deg Q < deg Φ_m is then automatic from deg(fĈ_m) < 2·deg Φ_m). So the ⟦C2⟧
box's existence claim is EQUIVALENT to: THE canonical division satisfies
(C-W_R)/(C-W_Q)/(C-READ). ∎ (Every leg below is about the canonical
division; the tree of S2 is the proof instrument that computes it.
[r1, PE-n4: the sentence is re-flowed so the C2 grep key sits on one
line; and the C2 claim row now records ALL the hypotheses this lemma
consumes — Φ_m MONIC, deg f < deg Φ_m, deg Ĉ_m < deg Φ_m — per PC-G3.])

## S1. Architecture: the depth calculus (what the data dictated, and how the induction runs)

The two sealed units feeding this compose fixed the architecture. The RM-2
probe measured: the m = 1 double-sum mechanism generalizes clause-for-clause
(405/405), the natural two-identity reduction tree terminates with strict
linear-measure descent exactly on the m = 1-analogue scope, and the ONLY ⟦C16⟧
bend is at the second-round overflow, where the boundary is decided by a
carry riding the E1-promotion edge. The DCX3 seal then made the mechanism
depth-exact at conjecture grade: a deficit-k overflow rides the promotion
edge exactly ≥ k deep; direct overflows are clean-visible at depth 0. So
the compose-facing directive of record — thread the termination measure and
the second-round boundary together along the promotion edge at every carry
depth — is executed here as follows.

**The promotion edge carries three exact structures at once:**

1. **The measure goes strictly DOWN** (S4): every promotion edge drops the
   generic measure μ_m (condition (A_i)); every correction/junk edge drops
   it too (condition (B_i), the DEV-1-scope e₀-edge) — this is termination.
2. **The line goes UP by an exact quantum** (S3, L-LINE): a promotion at
   level i+1 < m raises the cell's level-m line by EXACTLY
   h_{i+1}·∏_{i+1<t<m} e_t > 0. Hence the read skeleton at any fixed target
   is PROMOTION-FREE: carry depth 0. The entire four-clause theorem lives ⟦C24⟧
   at depth 0, and its proof (S5) is the m = 1 mechanism run once per
   level — the depth-0 base of the charter's induction, executed
   generically. This is why the box's clauses close without the sharp
   second-round analysis.
3. **The second-round boundary counts promotions** (S6): what re-enters the
   Φ_{m−1}-slots after the exit is exactly the depth-stratified part of Q;
   the level-l pool floor ⌊pool_l/e_{l−1}⌋ of LAW-DCX-(m+1) is the exact
   bookkeeping of how much exponent mass can ride the promotion edge from
   level l to level l+1 — at each depth the overflow either dies (the
   deficit accounting: the floor expression) or promotes exactly one edge
   deeper, carrying μ strictly down. The SHAPE of the law is MOTIVATED by
   the line/measure calculus [r1, PC-G2: regraded — S6.2 proves the
   crossing law and the carry cap c_l ≤ 2; the pool identification
   itself is heuristic, conjecture-supported]; the LIVENESS (the residue
   non-vanishing Q¹ ≠ 0 on the locus) is conjectural at every m ≥ 2 —
   proved at m = 1 ([IL3]†'s sharp form), sealed-confirmed at conjecture
   grade at m = 2 (THE UNIFORM CARRY LAW v2), first measured at m = 3 by
   this unit's diagnostic (S9).

**The kernel that makes the read layer one computation instead of m:** the
𝔷-multiplicativity lemma (L-ZMULT, S3) — the formal read-monomial of a cell
is multiplicative up to the single level-m carry z_m^{ε̃}, because the
λ-chain of S3 is LINEAR in (target, exponent vector) and every internal
carry cancels between its t-leg and its ε-leg (EXP-KIT(b) at each level).
With L-ZMULT, both telescope families (the standard-lift one and the
canonical-correction one) reduce to ψ_i(z_{i+1}) = 0 plus the
ε-carry/canonical-lift cancellation — the same two facts that closed
[ILN]† S4.6's Case II-1 top computation, now applied inside the engine.

**Consumption manifest (proof-grade):** [IL]† — L1 (EQ12-ADD) and the S0.2
base facts; EXP-KIT (a)/(b)/(c) as re-displayed self-contained in [ILN]† ⟦C7⟧
S3. [IL3]† — Lemma DIGIT-SPLIT (S4.1 there; the level-0 splitter, proved,
engine-free); the WELL-DEF m = 1 displays ride in through [ILN]† S4.1's ⟦C26⟧
base as before. [ILN]† — the S0.1/S0.2 class and calculus pins; S4.1
REALIZE-(m) (incl. (R1)–(R3) and the (I_m) descent invariant),
WELL-DEF-(n) (incl. the g_m = 1 form Φ_{m+1} = Φ_m^{e_m} + Ĉ_m and
(Ĉ1)–(Ĉ3)), READ-ADD-(m); the S4.4–S4.6 case partition and engine
call-sites (for S7's consequence display only); the S4.7 DCX_m definition ⟦C22⟧
and outer bound (S6/S7 display only). DEV-1 itself is NOT consumed: S5 ⟦C22⟧⟦C3⟧
re-derives its content generically (T-tele(1) + the S4 measure at m = 1).
Sealed artifacts (RM2 probe, DCX3 seal, the rung 3–5 batteries): instance
evidence and design input only, never proof steps. ⟦C15⟧

## S2. The cell calculus (cells, the three identity families, the tree, exactness) — PROVED, generic

### S2.1 Cells, lines, initial decomposition

A **cell** is C = b·Φ₀^{j₀}Φ₁^{j₁}⋯Φ_{m−1}^{j_{m−1}} with b ∈ O[x],
b ≠ 0, deg b < d₀, j_l ∈ ℤ_{≥0}. Its **line** (level-m weight lower bound,
exact on reduced cells by L-LINE(iv)):

    line(C) := E_m·w₀(b) + Σ_{l<m} j_l·Γ_l.

C is **reduced** iff j_l ≤ P_l − 1 for every l; then deg C < deg Φ_m by the
degree telescope deg Φ_{l+1} = P_l·deg Φ_l ([ILN]† S4.1 (R1) + WELL-DEF(i),
via the S0.1 pin): deg C ≤ (d₀−1) + Σ_l(P_l−1)deg Φ_l = deg Φ_m − 1.

**Lemma L-CELL (cell decomposition; B-READ).** (i) Every 0 ≠ x ∈ O[x] with
deg x < deg Φ_m is a finite sum of DISTINCT reduced cells (iterated
Φ_{m−1}-, …, Φ₀-development; uniqueness of division), with w_m(x) =
min line over its cells. (ii) [B-READ] For any target β* ≤ w_m(x),

    R_{m,β*}(x) = Σ_{cells C of x, line(C) = β*, C on-grid} res(b̄_C)·𝔷(C; β*),

with 𝔷 and on-grid as defined in S3.1, and b̄_C := res(b_C/π^{w₀(b_C)}).
*Proof.* (i) is the iterated division algorithm plus the min-formula for
single digits (developments add; w₀ a valuation — the S0.2 base facts of
[IL]†, consumed as in [ILN]†). (ii) Induction on m: the level-m read of x
at β* reads the Φ_{m−1}-digit at each grid abscissa s_m(β*) + te_{m−1} with
coefficient ε_{m−1}(slot weight)·R_{m−1,slot weight}(digit)·(z_m-power t);
each digit is the sum of the cells with that j_{m−1}; digits strictly above
their slot line contribute 0 (READ-ADD-(m−1), [ILN]† S4.1); off-grid
j_{m−1} are not read. Unwinding to level 1 gives exactly the 𝔷-product of
S3.1 per cell. ∎

**Initial decomposition of the engine input.** By L-CELL, f = Σ x-cells
(each with line ≥ λ, the on-line on-grid ones summing to v under B-READ)
and Ĉ_m = Σ D-cells. By the structure of realize_m ([ILN]† S4.1: children
placed at slots k_i = s + ie_{m−1} ≤ P_{m−1}−1, on their lines, nonzero τ's
only), EVERY cell of Ĉ_m is reduced, ON-LINE at γ_{m+1}, ON-GRID, with ⟦nc⟧
nonzero residue; B-READ on Ĉ_m is the (Ĉ3) certificate: Σ res(c̄_D)𝔷(D;
γ_{m+1}) = τ_m. Then

    f·Ĉ_m = Σ_{x-cell, D-cell} (b_x·c_D)·∏_l Φ_l^{j_l(x)+j_l(D)},

and each coefficient product is split by L-NORM below. **Initial
cells:** j_l ≤ 2P_l − 2 for all l, +1 at l = 0 on the Y-children.

**Lemma L-NORM (π-normalization of coefficient products) [r1 repair,
2026-08-04, PC-C1 (critical) as adjudicated TEXT-GAP by PE-F2 — THE
missing display].** Let b, c ∈ O[x] ∖ {0} with deg b, deg c < d₀. Write
b = π^{w₀(b)}·b̂ and c = π^{w₀(c)}·ĉ with b̂, ĉ PRIMITIVE (w₀ = 0; possible
since w₀ is the Gauss valuation), and set w := w₀(b) + w₀(c) = w₀(b·c)
(w₀ is a valuation — the S0.2 base facts). DIGIT-SPLIT ([IL3]† S4.1:
αβ = lift(q(α)q(β)) + πX + Φ₀Y, deg < d₀ each, q the reduction
homomorphism — stated and proved there for ALL α, β of degree < d₀ with
no primitivity hypothesis, engine-free [r2, PE2-3: the r1 paraphrase said ⟦C26⟧
"for PRIMITIVE inputs" — primitivity is where the L-child is guaranteed
nonzero, not a validity condition]) applies in particular to the
primitive product b̂ĉ; restoring the factor π^w gives THE NORMALIZED
SPLIT

    b·c = π^w·lift(b̄·c̄)  +  π^{w+1}·X̂  +  π^w·Φ₀·Ŷ,
    b̄ := res(b/π^{w₀(b)}) = res(b̂),   c̄ := res(ĉ)   ∈ K₀ = k₀[x]/(ψ̄₀),

with: (a) the **L-child** π^w·lift(b̄c̄) has w₀ EXACTLY w and normalized
residue b̄·c̄, which is NEVER zero (K₀ is a field and b̄, c̄ ≠ 0: a nonzero ⟦C18⟧
primitive polynomial of degree < d₀ has nonzero reduction of degree
< deg ψ̄₀) — so the L-child is never the zero polynomial; (b) the ⟦C18⟧
**π-junk child** π^{w+1}X̂ has w₀ ≥ w + 1 = w₀(b·c) + 1 (absent iff
X̂ = 0); (c) the **Φ₀Y-junk child** π^w·Ŷ at exponent j₀ + 1 has w₀ ≥ w
(absent identically at d₀ = 1: there deg(b̂ĉ) ≤ 2d₀ − 2 = 0 < d₀, no
Φ₀-overflow, Ŷ = 0). *Proof.* All
displayed: DIGIT-SPLIT on b̂ĉ is the [IL3]† lemma verbatim (its inputs
primitive by construction); multiplying through by π^w is exact;
(a)–(c) are the valuation bookkeeping just stated. ∎

**Convention [r1]:** EVERY invocation of DIGIT-SPLIT in this note — the
initial cells above, and the S2.2 correction children — is the L-NORM
normalized split. Without the normalization the displayed q would
annihilate any coefficient with w₀ > 0 (q(π·anything) = 0), the L-child
would be absent, and L-LINE(ii)'s Δline = 0 and (iii)'s π-child increase
would FAIL — PC-C1's counterexample (b = π, c_d = 1). With it, every
displayed law below holds; the implementation referee: PE's from-note
reimplementation was FORCED to insert exactly this step and then measured
97/97 engine calls with zero violations (`rmengine_pe_reimpl.py`,
"DIGIT-SPLIT, pi-normalized"), and the committed diagnostic's
`digit_split_norm` ("DIGIT-SPLIT on the unit part") is the same lemma.

### S2.2 The three identity families and the E1-priority tree

The identities, all EXACT in O[x] (the level-1 one from Φ₁ = the standard
lift of ψ₀; the level-(i+1) ones, 1 ≤ i ≤ m−1, from the canonical
ε-corrected lift, [ILN]† S0.1/S4.1, with C_k^{(i)} :=
realize_i(ε_i((g_i−k)γ_{i+1})^{−1}·ψ_{i,k}, (g_i−k)γ_{i+1}) for k < g_i,
ψ_{i,k} ≠ 0, and C_k^{(0)} := lift(ψ₀,k)·π^{(g₀−k)h₀}):

    (ID-(i+1))    Φ_i^{P_i} = Φ_{i+1} − Σ_{k<g_i, ψ_{i,k}≠0} C_k^{(i)}·Φ_i^{e_ik},
                  0 ≤ i ≤ m−1.

**The tree.** Nodes are cells. A cell with all j_l ≤ P_l − 1 is an
**R-leaf**. Otherwise let i := the LOWEST level with j_i ≥ P_i
(E1-priority) and apply (ID-(i+1)) to Φ_i^{P_i} inside C:

* **promotion child** (i ≤ m−2): j_i −= P_i, j_{i+1} += 1, b unchanged;
  **Φ_m-exit** (i = m−1): the cofactor b∏Φ^{j'}, j_{m−1}' = j_{m−1}−P_{m−1},
  joins Q (leaves the tree).
* **correction children** (one per k and per reduced cell d of C_k^{(i)},
  with c_d, a⃗_d its coefficient and exponent vector; a_{d,l} ≤ P_l − 1 for
  l < i, a_{d,l} = 0 for l ≥ i): coefficient product b·c_d L-NORM-split
  [r1, PC-C1] into the L/π-junk/Φ₀Y-junk children of S2.1 at exponents
  j_l + a_{d,l} (l < i, +1 at l = 0 on Y), j_i −= P_i − e_ik, sign −. For
  i = 0 the correction children are the L-NORM split of
  b·lift(ψ₀,k)·π^{(g₀−k)h₀} at j₀ − P₀ + e₀k — [IL3]† DEV-1's children,
  verbatim (there w₀(lift(ψ₀,k)) = 0 and the explicit π-depth (g₀−k)h₀
  rides outside the normalization).

**Lemma L-EXACT.** Each reduction step replaces a cell by the exact sum of
its children plus (at i = m−1) Φ_m times the exit cofactor. Hence, whenever
the tree on the S2.1 initial cells is finite (S4), with R₀ := Σ R-leaves
and Q := Σ exit cofactors: f·Ĉ_m = R₀ + Q·Φ_m; deg R₀ < deg Φ_m (R-leaves
are reduced), so by L-CANON (R₀, Q) = the canonical (R, Q), and
deg Q < deg Φ_m is automatic. ∎ (No window hypothesis is consumed here: ⟦C2⟧⟦C4⟧
the exit cofactors need not be reduced for this lemma.)

**Structural remark (exit cofactors are top-single digits, E1-priority).**
At a Φ_m-exit the reducing level is m−1, so by priority j_l ≤ P_l − 1 for
every l ≤ m−2: the sub-cell S := b∏_{l≤m−2}Φ_l^{j_l} is REDUCED AT LEVEL
m−1 (deg S < deg Φ_{m−1}), and the cofactor S·Φ_{m−1}^{j_{m−1}−P_{m−1}} is
a SINGLE Φ_{m−1}-digit whatever the top exponent. This is what makes the
Q-weight clause unconditional in S5.2.

**Display W-WIN (the window census — a structural display, NOT a lemma of
this note, consumed by NOTHING).** At m = 2 the RM-2 probe's L3-WINDOW leg ⟦C5⟧
measured every node inside {j₀ < 2P₀, j₁ < 2P₁} (0 violations, sealed).
At m = 2 the bound is provable by the path argument (a second consecutive
promotion into the top needs j₀ ≥ 2P₀, excluded; the only j₀-resupply — a ⟦nc⟧
level-2 correction — drops j₁ first). At m ≥ 3 the analogous ALL-LEVELS
bound j_l ≤ 2P_l − 1 is NOT proved here: a rebuild path through
intermediate-level corrections is not excluded by the m = 2 argument. Since
L-EXACT and every theorem clause below are window-free (the exit-cofactor ⟦C4⟧⟦C5⟧
remark above replaces the reduced-cofactor route), the bound stays a
display; the S9 diagnostic censuses it at m = 3 (leg D2). A measured
violation would refute only this display, no clause of the theorem. ⟦C5⟧

## S3. The line calculus and the 𝔷-kernel — PROVED, generic (the NEW displays)

### S3.1 The formal read monomial 𝔷

Fix a target β* ∈ ℤ. For a cell C = b∏Φ_l^{j_l} define the **λ-chain** and
**t-chain** top-down (λ_m := β*; l = m, …, 1):

    t_l := (j_{l−1} − s_l(λ_l)) / e_{l−1},      λ_{l−1} := (λ_l − j_{l−1}γ_l) / e_{l−1},

where s_l(·) is the eq-(12-int) s-value at the read (e_{l−1}, h_{l−1}). C
is **on-grid at β*** iff t_l ∈ ℤ for every l (equivalently j_{l−1} ≡
s_l(λ_l) mod e_{l−1}; then λ_{l−1} ∈ ℤ automatically, since γ_l ≡ h_{l−1} ⟦C30⟧
and ℓ_{l−1}h_{l−1} ≡ 1 mod e_{l−1}). For on-grid C set

    𝔷(C; β*) := ∏_{l=1}^{m} z_l^{t_l} · ∏_{l=1}^{m−1} ε_l(λ_l)   ∈ K_m^×

(z's are units; t_l of either sign is legal mid-tree; ε_l per the [ILN]†
S0.2 convention, applied to the level-l slot weight λ_l consumed by the
level-(l+1) read). The λ-chain is LINEAR in (β*, j⃗) — the load-bearing
triviality: λ_{l−1} is an affine function of λ_l and j_{l−1} with no floor
anywhere; all integrality sits in the t-chain. ⟦C6⟧

**First-failure convention [r1, PC-G5].** The chain is read top-down; if
t_l ∉ ℤ at some (largest) l, C is OFF-GRID at β* and the chain STOPS
there — s_{l−1} is never applied to a non-integer λ_{l−1} (which, past a ⟦C20⟧
failure, need not be integral), and 𝔷(C; β*) is defined only for on-grid ⟦C20⟧
C. This completes the definition PC-G5 charged as partial. By L-GRID
below, the convention is only ever exercised at foreign targets ⟦C20⟧
β* ≠ line(C), where the above-line case of every consuming lemma
governs; no clause of this note consults a partial chain.

**Lemma L-GRID (on-line ⟹ on-grid) [r1, PC-G5/PE — the vacuity lemma;
PE's two-line argument, displayed].** Every cell C = b∏Φ_l^{j_l} is
ON-GRID at its own line β* = line(C), and the λ-chain values are the
truncation lines: λ_l = line_l(b∏_{t<l}Φ_t^{j_t}) ∈ ℤ (the level-l line
with scales Γ_t^{(l)} := γ_{t+1}∏_{t<s<l}e_s). *Proof.* By the affine
recursion and Γ_t = e_{l−1}·Γ_t^{(l−1)} for t < l−1: λ_l =
e_{l−1}λ_{l−1} + j_{l−1}γ_l with λ_l, λ_{l−1} the displayed truncation
lines (integers — j's and w₀(b) are integers). Then mod e_{l−1}:
s_l(λ_l) ≡ ℓ_{l−1}λ_l ≡ ℓ_{l−1}·j_{l−1}γ_l ≡ ℓ_{l−1}j_{l−1}h_{l−1} ≡
j_{l−1} (γ_l ≡ h_{l−1} and ℓ_{l−1}h_{l−1} ≡ 1 mod e_{l−1} — the S0.1
conventions, already quoted in the on-grid display above), i.e. t_l ∈ ℤ
at every l. ∎ **Consequence:** "off-grid at β* = line(C)" is EMPTY — the
(D3) second case of DEV-(m) and RM-GEN's old "off-grid x-cells" branch
are vacuous, and the on-grid qualifiers in B-READ/L-LINE(iv) are
automatic at on-line targets (retained for emphasis). Machine shadow: ⟦C31⟧
PE's equal-line off-grid census 0 (counter C-GRID never fired, 97 engine ⟦nc⟧
calls, m = 2/3/4).

**Lemma L-ZMULT (𝔷-multiplicativity — the kernel).** Let C, C′ be on-grid
at targets β, β′ with exponent vectors j⃗, a⃗ and any coefficients. Then the
product-shape cell at exponents j⃗ + a⃗ is on-grid at β + β′, and

    𝔷((j⃗+a⃗); β+β′) = z_m^{ε̃} · 𝔷(j⃗; β) · 𝔷(a⃗; β′),
    ε̃ := ⌊(s_m(β) + s_m(β′)) / e_{m−1}⌋.

*Proof.* By linearity the λ-chains add: λ_l^Σ = λ_l + λ_l′ for every l. At
each level l, L1 ([IL]† S2, EQ12-ADD) gives s_l(λ_l + λ_l′) = s_l(λ_l) +
s_l(λ_l′) − δ_l e_{l−1} with δ_l := ⌊(s_l(λ_l)+s_l(λ_l′))/e_{l−1}⌋ ∈
{0,1}; hence t_l^Σ = t_l + t_l′ + δ_l ∈ ℤ (on-grid) and EXP-KIT(b) ([ILN]†
S3, self-contained modulo L1) gives ε_l(λ_l + λ_l′) = z_l^{−δ_l}· ⟦C7⟧
ε_l(λ_l)·ε_l(λ_l′). In the product defining 𝔷^Σ, the z_l^{+δ_l} from the
t-leg cancels the z_l^{−δ_l} from the ε-leg at every l = 1, …, m−1; the
single survivor is the TOP carry z_m^{δ_m} = z_m^{ε̃} (level m has a t-leg
and no ε-leg). ∎

(This one cancellation is why the engine's read layer needs no per-level
case analysis: every internal carry is self-curing, and exactly one
level-m carry survives — the box's ε′.)

### S3.2 Lemma L-LINE (the edge line laws — the exact quanta)

For every tree edge of S2.2, with lines as in S2.1:

* **(i) promotion at level i+1 ≤ m−1:** line(child) − line(parent) =
  Γ_{i+1}·1 − Γ_i·P_i = (γ_{i+2} − e_{i+1}P_iγ_{i+1})·∏_{i+1<t<m} e_t =
  **h_{i+1}·∏_{i+1<t<m} e_t > 0** — the exact promotion quantum. (For
  i+1 = m, the exit: line(cofactor) = line(parent) − P_{m−1}γ_m =
  line(parent) − w_mΦ_m.)
* **(ii) correction-main (L-)child, level i+1, digit d of C_k^{(i)}
  [display rewritten at r1: PE-n6 garble + PC-C1 threading].** The
  L-child of L-NORM has coefficient π^w·lift(b̄c̄_d) with w = w₀(b) +
  w₀(c_d) EXACTLY (L-NORM(a)); it is NEVER absent (L-NORM(a): residues ⟦C18⟧
  multiply in the field K₀ — the old "residue product vanishes"
  parenthetical was an artifact of the un-normalized display and is
  DELETED). Hence

      Δline = E_m·w₀(c_d) + Σ_{l<i} a_{d,l}·Γ_l − (P_i − e_ik)·Γ_i = 0,

  by scaling the d-cell's level-i on-line identity (realize structure,
  [ILN]† S4.1: E_i·w₀(c_d) + Σ_{l<i} a_{d,l}Γ_l^{(i)} = (g_i−k)γ_{i+1},
  with Γ_l^{(i)} := γ_{l+1}∏_{l<t<i}e_t) by ∏_{i≤t<m}e_t, using the three
  exact conversions E_i·∏_{i≤t<m}e_t = E_m; Γ_l^{(i)}·∏_{i≤t<m}e_t = Γ_l
  (l < i); and (g_i−k)γ_{i+1}·∏_{i≤t<m}e_t = e_i(g_i−k)·γ_{i+1}·
  ∏_{i<t<m}e_t = (P_i − e_ik)·Γ_i. Correction-main children PRESERVE the
  line exactly, always.
* **(iii) junk children [display rewritten at r1: PC-C1 through L-NORM +
  PE-n6].** The π-child has coefficient π^{w+1}X̂ with w₀ ≥ w + 1
  (L-NORM(b)), one more than the L-child's exact w, at the SAME
  exponents: Δline ≥ (ii)'s 0 + E_m·1 = E_m > 0. The Φ₀Y-child (d₀ ≥ 2
  only, L-NORM(c)) has coefficient w₀ ≥ w and exponent +1 at level 0: ⟦nc⟧
  Δline ≥ 0 + Γ₀ = Γ₀ > 0. For the level-1 identity (i = 0) the
  correction-main children carry the explicit depth π^{(g₀−k)h₀} and
  Δline = E_m·(g₀−k)h₀ − (P₀ − e₀k)·Γ₀ = 0, since Γ₀ = h₀·∏_{0<t<m}e_t =
  h₀E_m/e₀ and P₀ − e₀k = e₀(g₀−k); their junk siblings then gain ≥ E_m
  (π) resp. ≥ Γ₀ (Y) exactly as above.
* **(iv) reduced cells:** w_m(C) = line(C) EXACTLY (single nested digit:
  induction on m; a single digit's weight is exact at every level, no
  cancellation possible), and C is read at β* = line(C) as res(b̄)·𝔷(C;β*)
  if on-grid, 0 if off-grid; at β* < line(C) it reads 0 (above the line,
  READ-ADD-(m)). [This is L-CELL(ii) at a single cell.] [r1, PE: by
  L-GRID the off-grid branch at β* = line(C) is EMPTY; the clause stands
  as stated, vacuously true in that branch.]

*Proof of the arithmetic [r1: trimmed per PE-n6 — the (ii)/(iii)
computations now sit fully displayed inside their bullets].* (i): with
the convention Γ_l = γ_{l+1}∏_{l<t<m}e_t:
Γ_{i+1} − P_iΓ_i = ∏_{i+1<t<m}e_t·γ_{i+2} − P_iγ_{i+1}∏_{i<t<m}e_t =
∏_{i+1<t<m}e_t·(γ_{i+2} − e_{i+1}P_iγ_{i+1}) = ∏_{i+1<t<m}e_t·h_{i+1},
using γ_{i+2} = e_{i+1}w_{i+1}Φ_{i+1} + h_{i+1} and w_{i+1}Φ_{i+1} =
P_iγ_{i+1} (the S0.1 weight recursion). (ii)/(iii): displayed in the
bullets, through L-NORM. ∎

**Corollary L-MONO (line monotonicity).** Along every tree edge,
line(child) ≥ line(parent), with equality exactly on correction-main
children; promotions rise by the exact (i)-quantum. Hence every cell
reachable from an initial cell has line ≥ λ + γ_{m+1} (initial product
cells: line = line(x-cell) + line(D-cell) ≥ λ + γ_{m+1}, the line being
additive under S2.1's product formation since w₀ is a valuation), and
**the on-line skeleton at target λ + γ_{m+1} is PROMOTION-FREE and ⟦C24⟧
junk-free: carry depth 0.** ∎ ⟦C24⟧

## S4. Termination: the generic measure — PROVED on the T-scope [r1: amended, d₀ = 1 flats included]; the d₀ ≥ 2 flat corners fenced

### S4.1 Lemma L-MU (the generic linear measure) [statement STRENGTHENED at r1 per PE-F1; the composed statement required e_i ≥ 2 at all 1 ≤ i ≤ m−1 — the extension to d₀ = 1 flat shapes is argued in full below]

Let m ≥ 1 and suppose **(a) e_i ≥ 2 for all 1 ≤ i ≤ m−1 (any d₀), OR
(b) d₀ = 1 (any e_i ≥ 1)**. Then there exist positive rational weights
w₀, …, w_{m−1} with, for μ(C) := Σ_l w_l·j_l:

    (A_i)  w_i < w_{i−1}·P_{i−1}                    (1 ≤ i ≤ m−1)
    (B_i)  w_i·e_i > Σ_{l<i} w_l·ρ_l,   ρ₀ := P₀ − 1 + [d₀ ≥ 2],
                                        ρ_l := P_l − 1 (1 ≤ l < i)
                                                    (1 ≤ i ≤ m−1)

and μ then descends STRICTLY on every in-tree edge of S2.2 provided also
(e₀ ≥ 2 ∨ d₀ = 1) — the combined scope is exactly T-scope(m) as amended:

* promotion at level i+1 ≤ m−1: Δμ = w_{i+1} − w_iP_i < 0 by (A_{i+1});
  Φ_m-exit: Δμ ≤ −w_{m−1}P_{m−1} < 0;
* correction (all three L-NORM children) at level i+1, i ≥ 1 [display
  cleaned at r1, PE-n6]: the consumed level-i mass is P_i − e_ik =
  e_i(g_i−k) ≥ e_i (k ≤ g_i−1), and the dumped mass at level l < i is
  ≤ P_l − 1 (reduced C_k-digits), +1 at l = 0 on the Y-child (absent at
  d₀ = 1) — i.e. ≤ ρ_l per level; hence
  Δμ ≤ −w_i·e_i + Σ_{l<i} w_l·ρ_l < 0 by (B_i);
* correction at level 1 (i = 0): Δj₀ ≤ e₀(g₀−1) − P₀ + [d₀ ≥ 2] =
  −e₀ + [d₀ ≥ 2] ≤ −1 when e₀ ≥ 2 or d₀ = 1: Δμ ≤ −w₀ < 0. (At
  e₀ = 1 ∧ d₀ ≥ 2 the Y-child does not descend — the [IL3]† DEV-1 fenced
  corner, one level up, reproduced not repaired.) ⟦C8⟧

*Proof of existence [rewritten at r1: the (a)-branch display was garbled
(PE-n6); the (b)-branch is NEW (PE-F1)].* Choose w₀ := 1. (B_i)∧(A_i)
ask for w_i ∈ ( S_i/e_i , w_{i−1}P_{i−1} ), where S_i := Σ_{l<i} w_lρ_l
(so S_i = S_{i−1} + w_{i−1}ρ_{i−1}, S₁ = ρ₀).

**Branch (a): all e_i ≥ 2.** Induction with invariant (B_{i−1}). The
interval is nonempty iff w_{i−1}P_{i−1}·e_i > S_i. Since e_i ≥ 2 and
ρ_{i−1} ≤ P_{i−1}: w_{i−1}P_{i−1}e_i ≥ 2w_{i−1}P_{i−1} > S_{i−1} +
w_{i−1}P_{i−1} ≥ S_i, PROVIDED w_{i−1}P_{i−1} > S_{i−1} — which holds
because P_{i−1} ≥ e_{i−1} and (B_{i−1}) gives w_{i−1}e_{i−1} > S_{i−1}.
Base i = 1: (S₁/e₁, w₀P₀) = (ρ₀/e₁, P₀) is nonempty since e₁ ≥ 2 and
ρ₀ ≤ P₀ give ρ₀/e₁ ≤ P₀/2 < P₀. Pick any rational point (e.g. the
midpoint); (B_i) is then the invariant for the next step.

**Branch (b): d₀ = 1, arbitrary e_i ≥ 1 — THE FLAT EXTENSION.** At
d₀ = 1, ρ₀ = P₀ − 1 < P₀ — the base has SLACK, and the slack propagates
through near-ceiling choices. Use the smaller (uniform) interval

    w_i ∈ ( S_i , w_{i−1}P_{i−1} )      (note S_i ≥ S_i/e_i, so any such
                                         w_i satisfies (B_i): w_ie_i ≥
                                         w_i > S_i; and (A_i) directly),

with the invariant **w_i > S_i**. Nonemptiness: (S_i, w_{i−1}P_{i−1}) ≠ ∅
⟺ w_{i−1}P_{i−1} > S_i = S_{i−1} + w_{i−1}(P_{i−1} − 1) ⟺ w_{i−1} >
S_{i−1} — the invariant one step down. Base: at i = 1 the interval is
(S₁, w₀P₀) = (P₀ − 1, P₀), nonempty. Choosing w_i := the
midpoint of (S_i, w_{i−1}P_{i−1}) at every i maintains w_i > S_i, and NO
lower bound on any e_i was used — flat levels e_i = 1 are covered, and
promotions at flat levels descend STRICTLY (w_i < w_{i−1}P_{i−1} is
strict). ∎ In both branches μ takes values in (1/D)·ℤ_{≥0} for a common
denominator D, so strict descent is well-founded. ∎

**Necessity display (the corrected fence mechanism) [r1, PE-F1 — this
replaces the arithmetically FALSE unsatisfiability sentence that fenced
the d₀ = 1 flats: the composed box used ρ_{i−1} = P_{i−1} against the
note's own ρ = P − 1 and an undefined symbol W_{i−1}].** At d₀ ≥ 2 the
linear family genuinely dies at flat levels: if e_i = 1 for some
1 ≤ i ≤ m−1, then (B_i) reads w_i > S_i, which with (A_i) forces
w_{i−1}P_{i−1} > S_i = S_{i−1} + w_{i−1}(P_{i−1}−1), i.e. w_{i−1} >
S_{i−1}; with (A_{i−1}) this cascades down (w_{l−1}P_{l−1} > w_l > S_l
forces w_{l−1} > S_{l−1} by the same rearrangement) and bottoms out at
w₀P₀ > S₁ = w₀ρ₀, i.e. ρ₀ < P₀, i.e. **d₀ = 1**. So: the (A_i)/(B_i)
system with a flat level is satisfiable IFF d₀ = 1 — at d₀ ≥ 2 the
fence's conclusion stands (machine: PE's F2-e1flat-d2 and F3-e2flat-d2
probes INFEASIBLE on the full strict system); at d₀ = 1 the composed
fence was false, and branch (b) proves the corner instead.

**Consistency display (the sealed m = 2 instance + the r1 flat
witnesses).** w₀ = 2e₁, w₁ = P₀(e₁+1) satisfies (A₁): P₀(e₁+1) < 2e₁P₀
⟺ e₁ ≥ 2, and (B₁): P₀e₁(e₁+1) > 2e₁ρ₀ (ρ₀ ≤ P₀) ⟺ e₁+1 > 2 ⟺ e₁ ≥ 2 —
the RM-2 probe's sealed measure μ = e₀g₀(e₁+1)j₁ + 2e₁j₀ is a point of
the (a)-branch family, and its measured scope (strict exactly on e₀ ≥ 2
∧ e₁ ≥ 2, flat promotions at e₁ = 1) is the (a)-branch prediction on the
nose; at e₁ = 1 THAT point exits the family while the (b)-branch points
remain — PE's machine witnesses, all strict on the full system: (1, 3/2)
at m = 2, e₁ = 1, d₀ = 1 (M2C actual tree: 0 non-descending edges);
(1, 5/4, 19/8) at the fence box's own case m = 3, e₂ = 1, d₀ = 1 (M3C
actual tree: 88 nodes, 16 level-3 corrections, 0 non-descending);
(1, 3/2, 1) mid-flat; (1, 3/2, 5/4) double-flat; (1, 5/4, 149/64,
591/128) at m = 4 top-flat.

### S4.2 THEOREM T-TERM (termination) + the remaining flat corners [re-scoped at r1, PE-F1]

On **T-scope(m) = d₀ = 1 ∨ (e₀ ≥ 2 ∧ e_i ≥ 2, 1 ≤ i ≤ m−1)** [the r1
amended scope; strengthening argued in full at S4.1 branch (b)], the S2.2
tree on any finite set of window cells terminates (L-MU: strict descent
of a well-founded measure, branching finite). ∎

**Remark L-LEX (retained as a remark; SUBSUMED at r1).** The composed
note carried a lexicographic supplement for the single case m = 2,
e₁ = 1 ∧ d₀ = 1: σ(C) := Σ_{l<m}(m−l)·j_l drops on every promotion
(Δσ = −P_i(m−i) + (m−i−1) < 0) and on the exit, so (μ, σ) with a
promotion-flat μ terminates that shape. Under the r1-extended L-MU this
supplement is UNNECESSARY — branch (b) weights make every edge of every
d₀ = 1 flat shape strictly μ-descending directly (PE: even at M2C the
plain family is strict; witness (1, 3/2), 0 non-descending edges). The
σ-observation stays on record as a remark (a valid tiebreak mechanism,
consumed by nothing). ⟦C29⟧

**THE r1 EXTENSION OF THE THEOREM CLAUSES (DEV-(m)/RM-GEN on the d₀ = 1
flat corners) — the inspection census.** T-TERM above extends by S4.1(b).
The remaining question for the four box clauses is whether ANY other leg
of the proof chain consumes e_i ≥ 2. Census of the chain, displayed:
S2.1 (cells, lines, L-CELL, L-NORM: valuation + division arithmetic only, ⟦C27⟧
plus READ-ADD-(m−1) in L-CELL(ii)'s proof — an [ILN]† class-level fact,
e ≥ 1-safe, covered by the S5.1 bullet below [r3, PE3-N-2: the caveat
displayed so the gloss is literal] — every formula is e ≥ 1-safe); S2.2
(the identities and children: consumed
mass e_i(g_i−k), promotion/exit inventory — e ≥ 1-safe; L-EXACT is
scope-free); S3.1 (λ/t-chains, L-GRID, L-ZMULT: the congruences are mod ⟦C25⟧
e_{l−1} and trivialize at e = 1 — s ≡ 0, δ = 0, every carry vanishes);
S3.2 (L-LINE: the promotion quantum h_{i+1}∏e_t ≥ 1 needs only h ≥ 1, ⟦C27⟧
the class pin); S5.1 (DEV-(m): the induction carrier is T-TERM — now
extended; the consumed identities READ-ADD-(m), realize (R3),
ψ_i(z_{i+1}) = 0, EXP-KIT(b), L1 are [IL]†/[ILN]† CLASS-level facts, and
the S0.1 class pin imposes gcd(e_j, h_j) = 1 only — the [ILN]† head ⟦C27⟧
declaration grades REALIZE/WELL-DEF "unconditional-generic"; at a flat
level the level-i read degenerates, s_i ≡ 0, ε_i ≡ 1, and every displayed
identity holds in the degenerate form); S5.2 (RM-GEN: B-READ + L-ZMULT +
DEV-(m), nothing new). NO leg other than L-MU consumed e_i ≥ 2. Hence ⟦C23⟧⟦C27⟧
**DEV-(m) and THEOREM RM-GEN hold on the amended T-scope(m), same
consumption manifest** — graded PROVED, the same attempt grade as the
rest of the theorem. Instance shadow: PE ran the four clauses + tree +
LAW censuses on the actual flat towers M2C/M3C with 0 exceptions, and
this unit's r1 diagnostic leg D7 (S9) runs the full predicted-0 battery
on three d₀ = 1 flat shapes (flat-top, flat-mid, double-flat) at m = 3.

**FENCED OPEN CORNER (re-scoped at r1 per PE-F1: d₀ ≥ 2 ONLY — ⟦C33⟧
displayed, priced, not proved):**

    ┌────────────────────────────────────────────────────────────────────┐
    │ TERMINATION IS OPEN on:  d₀ ≥ 2  ∧  ( e₀ = 1  ∨                    │
    │   ∃ i with 1 ≤ i ≤ m−1: e_i = 1 ).                                 │
    │ Mechanism of the obstruction (corrected at r1 — the composed box   │
    │ misused ρ = P and an undefined W_{i−1}; PE-F1): at d₀ ≥ 2 with a   │
    │ flat level i, the (A_i)/(B_i) system is INFEASIBLE — the S4.1      │
    │ necessity cascade bottoms out at ρ₀ < P₀ ⟺ d₀ = 1 — so NO linear   │
    │ measure of the L-MU family terminates these shapes; the composed   │
    │ "a level-(i+1) correction can genuinely RAISE any linear μ"        │
    │ sentence is DELETED (false at d₀ = 1, where branch (b) weights     │
    │ make all edges strict; at d₀ ≥ 2 the correct statement is the      │
    │ family-infeasibility just displayed). At e₀ = 1 ∧ d₀ ≥ 2 the junkΦ │
    │ chain is the DEV-1 fenced corner verbatim, one level up.           │
    │ STATUS of the four CLAUSES there: RM2F e₀ = 1 ∧ d₀ = 2: UNTESTED — │
    │ inputs dodge the corner, on record; UNPROVED. PRICE: a             │
    │ non-linear/filtration measure (the [IL3]† repair-route genre:      │
    │ secondary induction on target-relative π-depth with a truncation   │
    │ argument for the division bookkeeping) — one dedicated unit; a     │
    │ collapse route (contracting a flat level out of the tower) remains │
    │ the plausible alternative at flat d₀ ≥ 2. Neither executed here.   │
    └────────────────────────────────────────────────────────────────────┘

(Every consumption of termination below carries the T-scope pin — now the
amended scope. The theorem's scope is exactly T-TERM's; no clause
silently widens it.)

## S5. The read layer and THE THEOREM — PROVED, generic, on the T-scope

### S5.1 Lemma DEV-(m) (the master reduction with read transparency)

On T-scope(m), for every cell G = b∏Φ_l^{j_l} with line(G) ≥ β*, define
(R₀(G), Q(G)) by the S2.2 tree recursion (well-founded by T-TERM):
R₀(leaf) := leaf, and at a reduction R₀(G) := Σ_children ±R₀(child),
Q(G) := Σ ±Q(child) + [exit cofactors]. Then:

(D1) G = R₀(G) + Q(G)·Φ_m (L-EXACT), deg R₀(G) < deg Φ_m;
(D2) w_m(R₀(G)) ≥ line(G), and w_m of every exit cofactor of Q(G) equals
     its line ≥ line(G) − w_mΦ_m;
(D3) [READ TRANSPARENCY]  R_{m,β*}(R₀(G)) =
       res(b̄)·𝔷(G; β*)   if line(G) = β* and G is on-grid at β*,
       0                  if line(G) > β* or G is off-grid at β*.
     [r1, PE/PC-G5: by L-GRID the on-grid qualifier in the first branch
     is AUTOMATIC and the off-grid disjunct in the second is VACUOUS — ⟦C31⟧
     off-grid occurs only at foreign targets β* ≠ line(G), where the ⟦C20⟧
     line(G) > β* branch governs every consultation below (targets only ⟦C20⟧
     ever sit ≤ the lines of the cells consulted). Statement unchanged;
     the proof's off-grid case is now the emptiness observation.]

*Proof.* Induction on μ (T-scope). **Leaf:** L-LINE(iv) is exactly
(D1)–(D3). **Reduction at level i+1, correction/junk/promotion children:**
(D1) is L-EXACT stepwise. (D2): children lines ≥ line(G) (L-MONO), leaves'
w_m = their lines, sums develop slotwise (w of a sum of digits at one
abscissa ≥ min — the S0.2 base facts); exit cofactors: the S2.2 structural
remark makes each a single Φ_{m−1}-digit, so w_m(cofactor) =
e_{m−1}·w_{m−1}(S) + j′γ_m with S reduced at level m−1, = its line
exactly, and the exit's line = line(parent) − w_mΦ_m (L-LINE(i)).
(D3): every child has w_m(R₀(child)) ≥ line(child) ≥ β* by (D2)+L-MONO, so ⟦C9⟧
reads add (READ-ADD-(m), [ILN]† S4.1) and R_{m,β*}(R₀(G)) =
Σ ±R_{m,β*}(R₀(child)) = Σ ± [IH read formulas]. Three cases:

* line(G) > β*: every child line > β*: all IH reads 0. ✓
* line(G) = β*, G off-grid: EMPTY by L-GRID (every cell is on-grid at
  its own line) — nothing to prove. [r1, PC-G5/PE: this replaces the ⟦nc⟧
  composed argument, which appealed to a converse-integrality direction
  of L-ZMULT that L-ZMULT does not state (PC-G5); that appeal is
  DELETED. Machine shadow: PE's equal-line off-grid census 0.] ✓
* line(G) = β* (on-grid automatic, L-GRID) — **the telescopes**: ⟦C31⟧

  **T-tele(1) (i = 0, the standard-lift identity).** The promotion child
  is above-line by exactly Γ-quantum h₁·∏_{1<t<m}e_t (L-LINE(i)): reads 0.
  Junk children: above-line: 0. Correction-main child (k): coefficient
  b·lift(ψ₀,k)·π^{(g₀−k)h₀}, residue b̄·ψ̄₀,k, exponents (j₀−P₀+e₀k,
  j₁, …), on-line (L-LINE(ii)); its λ-chain agrees with G's at every level
  ≥ 2 (only j₀ changed), and t₁ shifts by k − g₀. IH: ⟦nc⟧

      R_{m,β*}(R₀(G)) = − Σ_k ψ̄₀,k · res(b̄) · 𝔷(G)·z₁^{k−g₀}
                      = −res(b̄)𝔷(G)·z₁^{−g₀}·(ψ₀(z₁) − z₁^{g₀})
                      = res(b̄)·𝔷(G; β*)      [ψ₀(z₁) = 0]. ✓

  **T-tele(i+1), 1 ≤ i ≤ m−1 (the canonical identities — the box's
  resistance (ii), closed).** Promotion child (i+1 ≤ m−1): above-line by
  h_{i+1}·∏_{i+1<t<m}e_t: reads 0; at i+1 = m the exit contributes nothing ⟦nc⟧
  to R₀. Correction-(k, d) main child: exponents j⃗ + a⃗_d at levels < i,
  j_i − (P_i − e_ik) at level i; residue res(b̄)·res(c̄_d) [r1, PC-C1:
  this is L-NORM(a) — the L-child's normalized residue is EXACTLY the
  product of the parents' normalized residues, its coefficient's w₀ is
  exactly the sum, and it is never absent; the IH applies to it with the ⟦C18⟧
  same b̄-convention as B-READ's]. Its λ/t-chains:
  levels > i+1 unchanged; t_{i+1} shifts by k − g_i; λ_i rises by exactly
  β_k := (g_i−k)γ_{i+1} (linearity: Δλ_i = −Δj_i·γ_{i+1}/e_i =
  (P_i−e_ik)γ_{i+1}/e_i = β_k). So the child's level-≤i read data is the
  product-shape (G's level-≤i part at target λ_i) ⊕ (the d-cell at target
  β_k), and L-ZMULT AT LEVEL i gives

      𝔷(child; β*) = ε_i(λ_i + β_k)·z_{i+1}^{t_{i+1}+k−g_i}·[levels > i+1
                      factors of 𝔷(G)]·z_i^{ε̃_k}·𝔷_{≤i}(G-part; λ_i)·
                      𝔷_{≤i}(d-cell; β_k) / ε_i(λ_i)·…

  — organized: summing the IH reads over the d-cells of C_k^{(i)} first,
  B-READ on C_k^{(i)} (all cells on-line on-grid, L-CELL/S2.1) assembles
  Σ_d res(c̄_d)·𝔷_{≤i}(d; β_k) = R_{i,β_k}(C_k^{(i)}) = τ_k :=
  ε_i(β_k)^{−1}·ψ_{i,k} (realize (R3), [ILN]† S4.1). Hence

      R_{m,β*}(R₀(G)) = − Σ_k [upper factors]·ε_i(λ_i+β_k)·
                          z_i^{ε̃_k}·τ_k·z_{i+1}^{t_{i+1}+k−g_i}·
                          res(b̄)·𝔷_{≤i}(G-part; λ_i),
      ε̃_k := ⌊(s_i(λ_i) + s_i(β_k))/e_{i−1}⌋.

  EPS-MULT (EXP-KIT(b) at level i) gives ε_i(λ_i+β_k)·z_i^{ε̃_k} =
  ε_i(λ_i)·ε_i(β_k), and ε_i(β_k)·τ_k = ψ_{i,k} — **the ε-carry /
  canonical-lift cancellation, now at every level and every k**. The sum
  collapses exactly as at level 1:

      = −[upper]·ε_i(λ_i)·z_{i+1}^{t_{i+1}−g_i}·res(b̄)𝔷_{≤i}(λ_i)·
         Σ_k ψ_{i,k} z_{i+1}^{k}
      = −[…]·z_{i+1}^{t_{i+1}−g_i}·(ψ_i(z_{i+1}) − z_{i+1}^{g_i})·[…]
      = res(b̄)·𝔷(G; β*)              [ψ_i(z_{i+1}) = 0]. ✓ ∎

(At m = 1 there are no canonical identities, the promotion IS the exit,
and DEV-(1) with T-tele(1) is [IL3]† DEV-1's content re-derived — same
children, same ψ₀(z₁) = 0 collapse, the μ of S4 restricted to w₀ = 1
being j₀-descent. The e₀-scope agreement is exact: (e₀ ≥ 2 ∨ d₀ = 1).)

### S5.2 THEOREM RM-GEN (the generic engine)

**THEOREM.** For every m ≥ 1, on the standard-lift class with g_m = 1 and
T-scope(m) = d₀ = 1 ∨ (e₀ ≥ 2 ∧ e_i ≥ 2 for 1 ≤ i ≤ m−1) [r1, PE-F1:
the amended scope — the d₀ = 1 flat corners join by S4.1 branch (b) +
the S4.2 inspection census; a strengthening]: the canonical
division f·Ĉ_m = R + Q·Φ_m satisfies ALL FOUR box clauses of S0.2:
(C-W_R), (C-W_Q), (C-READ) with the exact factor z_m^{ε′}·v·τ_m, and
(C-EPS).

*Proof.* Decompose per S2.1: initial cells (x-cell, D-cell) with product
lines line(x) + γ_{m+1} ≥ λ + γ_{m+1} =: β* (D-cells all on-line at
γ_{m+1}). Run the tree; T-TERM terminates it; L-EXACT + L-CANON identify
(R₀, ΣQ) = (R, Q). **(C-W_R):** R = Σ over initial cells of R₀(cell),
each with w_m ≥ line ≥ β* (DEV-(m)(D2)); developments add. **(C-W_Q):**
every exit cofactor has w_m = its line ≥ (its tree-root initial cell's
line) − w_mΦ_m ≥ β* − w_mΦ_m (DEV-(m)(D2) + L-MONO); Q is their sum.
**(C-EPS):** L1 at the (e_{m−1}, h_{m−1}) read. **(C-READ):**
R_{m,β*}(R) = Σ_{initial cells} ±(DEV-(m)(D3) reads); the junk-split
initial children are above-line (0); above-line x-cells give above-line
products (0) [r1, PE: the composed "off-grid x-cells" branch is DELETED
— by L-GRID an x-cell with line = λ is on-grid at λ, so no such cells
exist; the L-ZMULT-integrality appeal it made is gone with it]; the
surviving initial cells are
(on-line x-cell, on-grid by L-GRID) × (D-cell), reading z_m^{ε̃}·res(b̄_x)𝔷(x; λ)·
res(c̄_D)𝔷(D; γ_{m+1}) by L-ZMULT with ε̃ = ⌊(s_m(λ)+s_m(γ_{m+1}))/
e_{m−1}⌋ = ε′ UNIFORMLY (all on-line x-cells share the target pair
(λ, γ_{m+1})). Summing (B-READ both factors):

    R_{m,β*}(R) = z_m^{ε′} · [Σ_x res(b̄_x)𝔷(x;λ)] · [Σ_D res(c̄_D)𝔷(D;γ_{m+1})]
                = z_m^{ε′} · v · τ_m.  ∎

**Scope remarks.** (i) At m = 1 the theorem reproduces the (RM-1) scope of
record exactly ([IL3]†'s "proved for e₀ ≥ 2, and at e₀ = 1 ∧ d₀ = 1"): an
independent generic re-derivation. (ii) [rewritten at r1 per PE-F3 — the
composed remark held the v = 0 case open as "legal"; it is in fact
EMPTY:]

  **Lemma L-VNZ (the read at the exact weight never vanishes).** For ⟦C19⟧
  0 ≠ f ∈ O[x] with deg f < deg Φ_m and λ := w_m(f): v := R_{m,λ}(f) ≠ 0.
  *Proof (induction on m; base m = 0: for 0 ≠ f of deg < d₀ the level-0
  read at w₀(f) is res(f/π^{w₀(f)}) ≠ 0 in the field K₀).* The level-m read
  at λ consults the Φ_{m−1}-digits f_j (deg f < deg Φ_m gives 0 ≤ j ≤
  P_{m−1} − 1) at the grid abscissae j = s_m(λ) + t·e_{m−1}; since
  0 ≤ s_m(λ) < e_{m−1} and j ≤ e_{m−1}g_{m−1} − 1, the slot index t
  ranges in 0 ≤ t ≤ g_{m−1} − 1. The read is Σ_t (unit)·
  R_{m−1,slot}(f_j)·z_m^t — a polynomial in z_m of degree < g_{m−1} =
  deg ψ_{m−1} = [K_m : K_{m−1}] with coefficients in K_{m−1}. At least
  one coefficient is nonzero: some digit ACHIEVES the min in λ = w_m(f)
  = min_j (e_{m−1}·w_{m−1}(f_j) + jγ_m) (L-CELL(i) / the S0.2 mirror
  recursion); for that j the slot congruence holds automatically (λ ≡ ⟦C31⟧
  jγ_m ≡ jh_{m−1} mod e_{m−1} — the L-GRID congruence) and its slot
  weight EQUALS w_{m−1}(f_j), so its coefficient is R_{m−1,w(f_j)}(f_j)
  ≠ 0 by the IH (deg f_j < deg Φ_{m−1}, f_j ≠ 0), times ε/z units ∈
  K_{m−1}^×. A nonzero K_{m−1}-polynomial in z_m of degree < [K_m :
  K_{m−1}] is nonzero in K_m. ∎ So the box NEVER receives v = 0 (residue ⟦C19⟧
  cancellation cannot reach the exact weight — deg f < deg Φ_m caps the
  read at ≤ g_{l−1} slots per level, PE-F3's mechanism, now displayed);
  the composed remark's "v = 0 is legal" clause was dead weight. The
  weight clauses still never consume residue survival (C10 — now ⟦C10⟧
  supported vacuously as well). Machine shadow: v = 0 realized 0/97 (PE)
  and 0/319 (S9 D1).

(iii) The g_m = 1 hypothesis enters only through Ĉ_m's existence ⟦C11⟧
(WELL-DEF-(n)(ii)); the tree and DEV-(m) are g_m-blind. (iv) OFF T-scope
— now d₀ ≥ 2 ∧ (e₀ = 1 ∨ ∃ flat level) [r1] — the four clauses are open
(the S4.2 fence), untested at RM2F on record.

## S6. The second-round clause: the level-m uniform carry law — STATED at every m, NOT PROVED (the exact conjecture perimeter)

### S6.1 LAW-DCX-(m+1) — the general-m form, displayed

Setting: the rung-(m+1) engine call sites ([ILN]† S4.4–S4.7): a g_m = 1
tower, pairs (γ, γ′) with iterated splits s_l, s_l′, the IH exports
E₁ᴵᴴ := quo(â·â′, Φ_m) (â the sub-Φ_m anchor part), and the second-round
overflow locus DCX_{m+1} := {E₁ᴵᴴ ≠ 0 ∧ Q¹ := quo(E₁ᴵᴴ·Ĉ_m, Φ_m) ≠ 0 ∧
the track clause} per the [ILN]† S4.7 definition. Define the **descending
weight chain of Ĉ_m's principal digit** (realize's own recursion, so each
u_l ∈ ℤ_{≥0}):

    u_m := γ_{m+1};    u_{l−1} := (u_l − s_l(u_l)·γ_l)/e_{l−1}   (l = m, …, 2),

the **pools and carries** (c₀ := 0):

    pool_1 := (s₁+s₁′) + s₁(u₁);           c₁ := ⌊pool_1/e₀⌋;
    pool_l := (s_l+s_l′) + s_l(u_l) + c_{l−1};  c_l := ⌊pool_l/e_{l−1}⌋
                                                    (2 ≤ l ≤ m−1),

and TRACK := (s_{m+1} = s_{m+1}′ = e_m − 1) ∧ E₁ᴵᴴ ≠ 0. **THE LAW:**

    (LAW-DCX-(m+1))    DCX_{m+1}  =  TRACK  ∧
                       (s_m+s_m′) + s_m(u_m) + c_{m−1}  ≥  2·e_{m−1}.

**Specialization audit (both anchors exact):** at m = 1 (c₀ = 0) the law
reads T₁ + s₁(γ₂) ≥ 2e₀ on TRACK — [IL3]†'s PROVED sharp DCX₂ form on the
nose. At m = 2 it reads (s₂+s₂′) + s₂(γ₃) + ⌊(s₁+s₁′+s₁(u₁))/e₀⌋ ≥ 2e₁
with u₁ = (γ₃ − s₂(γ₃)γ₂)/e₁ — THE UNIFORM CARRY LAW v2 verbatim
(u₁ = the DCX3 note's u₀; SEALED-CONFIRMED AT CONJECTURE GRADE,
0/11,808 + 0/3,320-minus-40-then-cured + the RM-2 102/102 refit; consumed
here as that sealed conjecture, its grade unchanged by this note). ⟦C12⟧

**STATUS, per m (the honesty split):** m = 1 PROVED ([IL3]† S4.7);
m = 2 SEALED-CONFIRMED, conjecture grade (the DCX3 unit; the head
declaration paste); m ≥ 3 CONJECTURE — first instance data ever produced
by this unit's S9 diagnostic (leg D5, m = 3). This note does NOT derive
the law generically; the seal does NOT upgrade.

### S6.2 What IS proved generically about the law (the depth mechanism)

* **Lemma L-CAP (carries cap at 2).** pool_1 ≤ 2(e₀−1) + (e₀−1) ⟹ c₁ ≤ 2;
  inductively pool_l ≤ 2(e_{l−1}−1) + (e_{l−1}−1) + 2 = 3e_{l−1} − 1 ⟹
  c_l ≤ 2 for every l (trivial at e_{l−1} = 1: s ≡ 0, pool = c_{l−1} ≤ 2).
  So the law's carry chain has depth ≤ 2 at EVERY level — the DCX3 "depth
  ≤ 2 structurally" display generalized. PROVED. ∎
* **The crossing law (PROVED) and the floor-form SHAPE (HEURISTIC) —
  regraded at r1 per PC-G2 (the composed bullet presented the whole as a
  derivation; the honest split follows).** PROVED (by the S2.2 child
  inventory alone): exponent mass crosses from level l−1 to level l ONLY ⟦C17⟧
  along the promotion edge, P_{l−1} units consumed per unit delivered —
  promotions are the only child type raising a higher-level exponent, ⟦C17⟧
  corrections dump strictly below their reduction level, exits leave the
  tree; and each promotion carries μ strictly down (L-MU — a T-scope-
  pinned citation: the composed bullet cited L-MU without the pin inside
  a generic-sounding derivation, PC-G2's leak, fixed here) and the line
  up by the exact quantum (L-LINE(i), scope-free). HEURISTIC (NOT ⟦C25⟧
  derived in this note): the identification of the on-line level-(l−1)
  mass with the pool (s_l + s_l′) + s_l(u_l) + c_{l−1} — no induction
  connecting the actual second-round quotient cells of Q¹ to these
  pools is displayed; that correction/rebuild paths contribute exactly
  the recursive c_{l−1} is unproved; and no support-containment argument
  separates the mass bookkeeping from residue survival. Hence "the
  maximal promotion count into level l is ⌊pool_l/e_{l−1}⌋" is the law's
  MOTIVATION at conjecture support: the DCX3 seal's depth-exact legs
  (C2/C3: deficit-k overflow carried entirely at depth ≥ k, 467/467
  rows; direct rows clean-visible 241/241), the S9 D5 first m = 3 data
  (51/51 chain-fed live rows), and PE's 2,208-pair fresh census incl.
  the first LAW-DCX-5 data — instance evidence, not proof. What is also
  NOT derived, as before: that the promoted mass ARRIVES with nonzero
  residue (Q¹ ≠ 0 exactly on the locus), the conjectural content at
  every m ≥ 2.

## S7. Consequences (the exact upgrades, and the one that does NOT fire)

### S7.1 COR-A: the ITERLAWN ledger upgrade on the T-subclass

**[LEDGER STATUS — inline at r1 per PC-G4/PE-n5: the ITERLAWN S7
row upgrade is applied AT ACCEPTANCE ONLY (S10/S11 convention; this ⟦nc⟧
note's acceptance counter is 0/2 at r1). What follows is the
MATHEMATICAL consequence display, not an applied ledger edit.]**

**[LEDGER APPLICATION — 2026-08-04, at acceptance (round 7, PC7 + PE7
both CLEAN on a0e266c). The counter is met; COR-A FIRES: on T-class(n),
the [ILN]† S7 rows 5/6/8 (RM-m) calls are DISCHARGED and (ITER-LAW-n)
holds on T-class(n) with the (RM-m) conditionality REMOVED — the
remaining conditionality of (ITER-LAW-n) on that sub-class is exactly
[ILN]†'s own non-(RM) residuals (its head declaration; nothing new).
Off T-class(n) — some called level in the d₀ ≥ 2 fenced corner — the
rows stay conditional, unchanged. The cross-note upgrade is recorded in
the ledger ([RMG] block) rather than by editing the accepted [ILN]
note, per the incorporation convention.]**

[ILN]†'s S7 rows 5/6/8 condition rung n ≥ 3 on (RM-m, 2 ≤ m ≤ n−1). By
THEOREM RM-GEN those calls are DISCHARGED whenever every called level
satisfies its T-scope, i.e. on the sub-class

    T-class(n) := d₀ = 1 ∨ (e₀ ≥ 2 ∧ e_i ≥ 2 for all 1 ≤ i ≤ n−2)

[r1: extended with the amended T-scope, PE-F1]. For n ≥ 3 this is the
EXACT union of the per-call pins over 2 ≤ m ≤ n−1 (the engine at level m
consumes reads 0..m−1 only). **The n = 1, 2 correction [r1, PC-G4]:** at ⟦C13⟧
n = 1, 2 the call family 2 ≤ m ≤ n−1 is EMPTY — no engine call exists to
discharge, the union of pins is no constraint at all, and the composed
claim that the display above was "the exact union" for EVERY n ≥ 1 was a
slip: as previously displayed the T-class still imposed (e₀ ≥ 2 ∨ d₀ =
1) at n = 1, 2, which the (empty) union does not. Corrected statement:
at n = 1, 2 the rows are the accepted [IL]†/[IL3]† statements on their
own scopes, with NO T-condition. CONSEQUENCE:
**(ITER-LAW-n) off DCX-(n) is UNCONDITIONAL on T-class(n) towers for
every n ≥ 3** (and at n = 1, 2 on the accepted [IL]†/[IL3]† scopes
verbatim) — modulo nothing but the accepted [IL]†/[IL3]†/[ILN]† packages ⟦C32⟧
riding through per their head declarations. Off T-class(n) (d₀ ≥ 2 with
a flat or e₀ = 1 level), rows 5/6/8 stay conditional on the fenced S4.2
corner. The consumption is by engine CALL, not by tower: II-3 call sites
at a flat top read (e_{n−1} = 1) call the engine at level n−1 whose OWN
T-scope concerns levels ≤ n−2 — the display above already accounts for
this (e_{n−1} does not appear in T-class(n)).

### S7.2 COND-B: (W-MULT-DCX-m) — the displayed conditional (NOT a corollary)

The chained-carry analysis did NOT close the second-round clause
generically (S6: the law is conjecture-grade at every m ≥ 2). Therefore
(W-MULT-DCX-m) STANDS OPEN, and the honest display is conditional:

    IF LAW-DCX-(m+1) is proved at level m together with a second-round
    read clause (the depth-≥1 analogue of (C-READ) for the re-entrant
    part of Q¹), THEN the [ILN]† S4.6 Case II legs close ON DCX_{m+1} as
    well, and (W-MULT-DCX-m) falls with it — the [ILN]† S4.7 carry-over
    ("a proof of (RM-m) with a second-round clause would close both
    II-legs and DCX at once"), now with the first half (RM-m, T-scope)
    in hand. The second half is the next dedicated unit's charge; its
    design input, on record: thread the S6.2 depth mechanism — the
    promotion edge carries μ (down, exactly), the line (up, exactly
    h·∏e), and the pool floors (the deficit accounting) SIMULTANEOUSLY.

## S8. THE CONDITIONALITY LEDGER + the dependency-claims table (machine-census-ready)

| # | leg | grade | rests on |
|---|---|---|---|
| 1 | L-CANON, L-CELL/B-READ, L-EXACT (cell calculus) | PROVED, generic, unconditional | division algorithm + S0.2 base facts + [ILN]† S4.1 (R1)/(R3) |
| 1a | L-NORM (π-normalization) [r1, PC-C1] | PROVED, generic, unconditional | w₀ a valuation + [IL3]† DIGIT-SPLIT on primitive inputs |
| 2 | L-ZMULT (the 𝔷-kernel) | PROVED, generic, unconditional | [IL]† L1 + EXP-KIT(b) ([ILN]† S3) |
| 2a | L-GRID (on-line ⟹ on-grid) [r1, PC-G5/PE] | PROVED, generic, unconditional | S0.1 congruences + λ-chain linearity |
| 2b | L-VNZ (v ≠ 0 forced) [r1, PE-F3] | PROVED, generic, unconditional | L-CELL(i) + field towers K_l + the L-GRID congruence |
| 3 | L-LINE / L-MONO (line quanta) | PROVED, generic, unconditional | S0.1 weight recursion + [ILN]† S4.1 realize structure + L-NORM |
| 4 | L-MU + T-TERM (termination) | PROVED on T-scope(m) [r1: AMENDED scope — d₀ = 1 ∨ (e₀ ≥ 2 ∧ all e_i ≥ 2); branch (b) NEW per PE-F1] | self-contained arithmetic + [IL3]† DIGIT-SPLIT via L-NORM (child inventory) |
| 5 | L-LEX supplement | REMARK at r1 (subsumed by row 4 branch (b); valid, consumed by nothing) | S4 |
| 6 | flat corners (termination + clauses) | **OPEN at d₀ ≥ 2 ∧ (e₀ = 1 ∨ ∃ flat) ONLY** [r1: the d₀ = 1 flats are PROVED — rows 4/7/8] | — |
| 7 | DEV-(m) (D1)–(D3) | PROVED, generic, on T-scope [r1: amended scope via row 4 + the S4.2 inspection census] | rows 1–4 + READ-ADD-(m) ([ILN]† S4.1) + ψ_i(z_{i+1}) = 0 |
| 8 | **THEOREM RM-GEN (all four clauses)** | **PROVED on T-scope(m), every m ≥ 1** [r1: amended scope] | rows 1–4, 7 |
| 9 | W-WIN window display | display only; m = 2 proved+sealed; m ≥ 3 measured (S9 D2) | nothing consumes it |
| 10 | LAW-DCX-(m+1) statement | m = 1 PROVED ([IL3]†) · m = 2 SEALED-CONFIRMED conjecture ([DCX3], a9dd275) · m ≥ 3 CONJECTURE (first data S9 D5) | — |
| 11a | L-CAP [split at r1, PC-G2] | PROVED, generic | self-contained arithmetic |
| 11b | the promotion-edge crossing law | PROVED (S2.2 inventory; the μ-descent citation carries the T-scope pin) | S2/S3/S4 |
| 11c | the floor-form pool identification | **HEURISTIC — conjecture-supported** (DCX3 C2/C3 seals, S9 D5, PE census); NOT derived [ERRATUM, post-acceptance 2026-08-04: REFUTED as literally worded — 72 instance-keyed divergences (physical promotion counts exceed the floor; the cap bounds the pool VARIABLE, not tree paths), stress seal d3950f2 / verdict d774ecb, independently reproduced set-exactly by two instruments; the CORRECTED identification PID† (cap on the full tree + attained max on the dump-free principal subtree under (STK) ∧ the law clause) is PROVED at attempt grade in `WMULTDCX_2026-08-08.md`, which supersedes this row's heuristic layer; this row was consumed by NOTHING proved (verified by all 12 arc passes), so the refutation touches no accepted clause] | — |
| 12 | COR-A (ITERLAWN upgrade on T-class(n)) | PROVED given row 8 + [ILN]†, n ≥ 3 [r1: n = 1, 2 corrected — no T-condition there]; LEDGER APPLICATION at acceptance only | S7.1 |
| 13 | COND-B ((W-MULT-DCX-m)) | **OPEN — conditional display** | S7.2 |

**Dependency-claims table — coverage arbitrated by THE MACHINE CENSUS
[r3, PC3-g2 = PE3-G-3: THE STRUCTURAL KILL of the recurring charter
defect].** Three rounds of hand censuses each claimed exhaustiveness and
each was refuted by the next pass (PC-G3 → PC2-g2/PE2-1 → PC3-g2/
PE3-G-3; the r2 bracket's "complete in-scope absentee list is exactly
C23–C27" was refuted by PE3's independent 136-hit census — the L-LEX
witness, now row C29), because "in-scope dependency claim" is fuzzy and
no fuzzy hand pass can CLOSE it. The coverage arbiter is therefore now
syntactic, per the ITERLAWN machine-census precedent:
`verification/openmath/rmengine_claims_census.py` (run from the repo
root: `python3 verification/openmath/rmengine_claims_census.py`;
closure output committed as `rmengine_claims_census_output.txt`). THE
TAG GRAMMAR it enforces on this note's body (outside the excluded
record surfaces — this S8 section, the head repair brackets, the three
byte-frozen †-ledger-quote paragraphs, the tail repair records, S11):
every physical line carrying a charter keyword (only / never / nothing
/ -free / self-contained / automatic) must end with an explicit row tag
⟦C<nn>⟧ naming the table row that covers its claim, or the non-claim
marker ⟦nc⟧ (= this keyword occurrence asserts no dependency/scope fact
about this note's proof chain: rhetoric, arithmetic narration, or an
upstream note's own claim); every ⟦C<nn>⟧ must name an existing row;
every row must be body-tagged or carry "(table-only: <reason>)" in its
site column. VERIFIER AFFORDANCE: each ⟦nc⟧ is individually
challengeable — a focused local judgment replacing the unclosable
global census; the table's exhaustiveness claim is now exactly the
script's "CENSUS CLOSED — zero violations" line, nothing more. Row KEYS
stay grep-stable as a secondary property (keys quote the committed
text; whitespace-normalized — join wrapped lines AND strip the ⟦…⟧ tags
before matching, since a tag can sit mid-key where a key wraps); the
r2-broken keys C26/C28 and the stale C7/C10 keys were re-quoted at r3
per PE3-G-1. History: [r1, PC-G3 + PE-n4: rows C15–C22 added] [r2,
PC2-g2 = PE2-1: rows C23–C28 added from PE2's 112-hit census] [r3: rows
C29–C33 added; every keyword line tagged; grammar machine-checked].**

| # | site (grep key) | claim | basis |
|---|---|---|---|
| C1 | S0.2 "(C-EPS) is [IL]† L1" | (C-EPS) proved by EQ12-ADD alone | two-line congruence, no other input |
| C2 | S0.3 "deg Q < deg Φ_m is then automatic" | canonicity needs deg R < deg Φ_m only — GIVEN the box setting's hypotheses: Φ_m MONIC, deg f < deg Φ_m, deg Ĉ_m < deg Φ_m [r1, PC-G3: hypotheses now recorded] | uniqueness of division by a monic + deg(fĈ_m) < 2 deg Φ_m |
| C3 | S1 "DEV-1 itself is NOT consumed" | S5 re-derives DEV-1 content generically | T-tele(1) + L-MU at m = 1; DIGIT-SPLIT IS consumed (distinct lemma) |
| C4 | S2.2 L-EXACT "No window hypothesis is consumed here" | L-EXACT is window-free | deg-route through L-CANON |
| C5 | S2.2 W-WIN "consumed by NOTHING" | no theorem clause consumes the window bound | exit-cofactor structural remark carries (C-W_Q); L-EXACT row C4 |
| C6 | S3.1 "all integrality sits in the t-chain" | λ-chain needs no floor | affine recursion displayed |
| C7 | S3.1 L-ZMULT proof "EXP-KIT(b) ([ILN]† S3, self-contained modulo L1)" + S1 manifest "as re-displayed self-contained in [ILN]† S3" [r3, PE3-G-1 sweep: the old key "self-contained modulo L1 + EXP-KIT(b)" was a paraphrase, not a quote] | no other consumption in L-ZMULT | proof displays both cites |
| C8 | S4.1 "(At e₀ = 1 ∧ d₀ ≥ 2 the Y-child does not descend" | exact locus of the level-1 descent failure | DEV-1 erratum mechanism, one level up |
| C9 | S5.1 "(D3): every child has w_m(R₀(child)) ≥ line(child) ≥ β*" | READ-ADD applicability at every node | (D2) + L-MONO |
| C10 | S5.2 "still never consume residue survival" [r3, PE3-G-1 sweep: the old key dropped the committed text's "still"] | (C-W_R)/(C-W_Q) hold at v = 0 | line calculus only |
| C11 | S5.2 "the tree and DEV-(m) are g_m-blind" | g_m = 1 enters via Ĉ_m only | statement inspection: g_m absent from S2–S5.1 |
| C12 | S6.1 "consumed here as that sealed conjecture, its grade unchanged" | v2 never used as a proof step | S6/S9 display-only uses |
| C13 | S7.1 "consumes reads 0..m−1 only" | engine level-m scope pin | S0.2 box setting + S2 inventory |
| C14 | S8 row 9 "nothing consumes it" (table-only: its site is grade row 9's own cell in this excluded S8 surface; the body sites of the same claim carry ⟦C5⟧) | W-WIN consumer census | = C5 |
| C15 | S1 "instance evidence and design input only, never proof steps" [r1, PC-G3 absentee] | sealed artifacts (RM2 probe, DCX3 seal, rung batteries) are never proof steps; echoes the head-declaration "INSTANCE EVIDENCE ONLY" riders | consumption manifest; every use site carries the grade (S6.1 audit, S9 standing line); PE's 10+13-site LAW-grade census CLEAN |
| C16 | S1 "the ONLY bend" [r1, PC-G3 absentee] | an RM-2 SEALED-MEASUREMENT narrative claim (design input), not a proof claim of this note | RM2 probe seal 0b42885 → d860a5a — instance grade |
| C17 | S6.2 "ONLY along the promotion edge" [r1, PC-G3 absentee] | exponent mass enters level l on promotion edges alone — PROVED by child inventory (row 11b); its pool APPLICATION stays heuristic (row 11c) | S2.2 child inventory |
| C18 | S2.1 "the L-child is never the zero polynomial" [r1, new at L-NORM] | normalized residue products cannot vanish | K₀ a field + primitive parts nonzero |
| C19 | S5.2 "the box NEVER receives v = 0" [r1, new at L-VNZ] | v ≠ 0 forced on the whole legal class | L-VNZ displayed induction; measured 0/97 (PE) + 0/319 (S9) |
| C20 | S3.1 "only ever exercised at foreign targets" [r1, new at L-GRID] | partial λ-chains are never consulted by the theorem chain | L-GRID + the (D3)/B-READ routing |
| C21 | S0.1 "nothing is claimed for non-canonical lifts" [r1 — tag restored at r2, PE2-2] | (ITER-LAW-LIFT) rider propagates | class pin; scope box S10 |
| C22 | S1 "(for S7's consequence display only)" + "(S6/S7 display only)" [r1 — tag restored at r2, PE2-2] | [ILN]† S4.4–S4.7 call sites and the DCX_m outer bound are never RM-GEN proof steps [r2, PC2-g2: read narrowly — the call-site STRUCTURE is consumed by the proved COR-A consequence, which is not RM-GEN] | S7 text consumes them only in the conditional/consequence displays; DAG routes |
| C23 | S4.2 "NO leg other than L-MU consumed e_i ≥ 2" [r2, PC2-g2/PE2-1] | the scope-extension census: every proof-chain leg except L-MU is e ≥ 1-safe or class-level | PC2 re-derivation; PE2 grep of all 50 "≥ 2" sites; 67 zero-violation flat-tower engine calls (`rmengine_pe2_ext.py`) |
| C24 | S3.2 "PROMOTION-FREE and junk-free: carry depth 0" (echoed S1 pt 2) [r2, PE2-1(i)] | the on-line skeleton at target λ + γ_{m+1} has carry depth 0 (L-MONO) | S3.2 displayed derivation; PE2 verified TRUE (report §5) |
| C25 | S4.2 "L-EXACT is scope-free" + S6.2 "(L-LINE(i), scope-free)" [r2, PE2-1(ii)] | neither lemma consumes the T-scope | C4/C7 genre; the proofs display no scope input; PE2 verified |
| C26 | S1 "proved, engine-free" + S2.1 "no primitivity hypothesis, engine-free" [r2, PE2-1(iii)] [r3, PE3-G-1: re-keyed — the r2 key "proved there, engine-free" was landed and broken by the same r2 commit (the PE2-3 citation rewrite) and matched no committed body line] | DIGIT-SPLIT's anti-circularity rider: [IL3]† S4.1 is proved without this note's engine | [IL3]† S4.1 proof text; PE2 verified; PE3 re-verified at source (report §1.2) |
| C27 | S4.2 census-internal onlys: "valuation + division arithmetic only" · "needs only h ≥ 1" · "gcd(e_j, h_j) = 1 only" · "nothing new" [r2, PE2-1(iv)] [r3: "nothing new" added to the key list per PE3 §2] | the inspection census's own per-leg dependency claims | each verified by PE2 against the cited sections; PE3 re-derived per-leg (report §3.5) |
| C28 | repair record "The r1 statement-change census" (table-only: the claim's site is the round-1 repair record, an excluded record surface) [r2, PC2-g2] [r3, PC3-g3 + PE3-G-1/G-2: re-keyed, re-enumerated, basis corrected] [r4, PE4-G-1: the r3 "SIX changes" completeness claim was itself under-enumerated (the L-LINE(ii) NEVER-absent negation = change 7); completeness claim WITHDRAWN — the diff is the authoritative surface, the enumeration a reading aid] | the r1 statement-diff census, seven enumerated changes per the actual diff (see the round-1 repair record), no completeness claim: all repairs or strengthenings, none weakening an established clause | the none-weakening rider verified against the actual `git diff bf18bfe 8835d20` by PE2, PE3 (report §1.5), and PE4 (its G-1 re-read found change 7 and re-confirmed the rider); PC2/PC3/PC4 verified content-only against supplied texts (no execution) |
| C29 | S4.2 L-LEX remark "a valid tiebreak mechanism, consumed by nothing" [r3, PE3-G-3 — the round-3 census absentee] | L-LEX's consumer census: no lemma or theorem clause of this note consumes L-LEX (T-TERM's proof cites L-MU strict descent alone); the W-WIN genre (C5/C14), third instance | PE3's 136-hit census + consumer grep (report §2), claim verified TRUE; S11 DAG; grade row 5 |
| C30 | S3.1 "then λ_{l−1} ∈ ℤ automatically" [r3, PC3-g2] | λ-chain integrality rides t-chain integrality with no further input: γ_l ≡ h_{l−1} and ℓ_{l−1}h_{l−1} ≡ 1 mod e_{l−1} (the S0.1 conventions) | the congruence displayed in-line; the same arithmetic as L-GRID's proof |
| C31 | the automatic on-grid qualifiers: S3.1 "automatic at on-line targets" · S5.1 (D3) "is AUTOMATIC" · T-tele "(on-grid automatic, L-GRID)" · L-VNZ "the slot congruence holds automatically" [r3, PC3-g2] | every on-grid qualifier at an on-line target is discharged by L-GRID alone — no call site consumes a separate on-grid hypothesis | L-GRID (grade row 2a); each call site cites it inline |
| C32 | S7.1 "modulo nothing but the accepted [IL]†/[IL3]†/[ILN]† packages" + the headline echo "UNCONDITIONAL (modulo the accepted … packages)" [r3, PE3 §2 residue (i)] | COR-A's conditionality census: the T-class(n) consequence carries no conditionality beyond the three accepted packages, given THEOREM RM-GEN (grade row 8) | grade row 12 ("PROVED given row 8 + [ILN]†"); the S1 consumption manifest |
| C33 | S4.2 fence heading "re-scoped at r1 per PE-F1: d₀ ≥ 2 ONLY" [r3, census] | the fenced open-termination region is exactly d₀ ≥ 2 ∧ (e₀ = 1 ∨ ∃ flat level) — the complement of the amended T-scope; the d₀ = 1 flats sit INSIDE the proved scope | grade rows 4/6; the S4.1 necessity cascade (an iff) |

## S9. POST-SEAL-LABELED DIAGNOSTIC (run at composition; NOT a proof step, NOT a seal — the iterlawn_diag/[IL3]-diag precedent)

`verification/openmath/rmengine_diag.py` + `rmengine_diag_output.txt` —
exact arithmetic, deterministic, sealed harness consumed READ-ONLY ⟦nc⟧
(imports byte-untouched). Legs (all law-keyed to THIS note's NEW generic
displays; predicted 0 violations on D1–D4, D6; D5 is census-grade):

* **D1** the four box clauses at m = 3 — THE FIRST LEVEL-3 ENGINE DATA
  EVER: canonical division of f·Ĉ₃ by Φ₃ on fired (rung-4 Case-II) and
  breadth inputs; (C-W_R)/(C-W_Q)/(C-READ) with z₃^{ε′}vτ₃ pinned,
  (C-EPS).
* **D2** the S2/S4 instruments at m = 3: the 3-level tree reassembles the
  canonical division (CONS); L-MU weights constructed by the S4.1 interval
  rule, strict μ-descent on every in-tree edge (T-scope shapes); W-WIN
  window census (display-grade).
* **D3** the S3 line calculus: leaf w₃ = line exact; promotion Δline =
  the exact quantum (e₂h₁ at level-1 edges scaled, h₂ at level-2 edges);
  correction-main Δline = 0; junk Δline > 0.
* **D4** the 𝔷-kernel: R's read at target == Σ over on-line on-grid
  R-leaves of res(b̄)·𝔷 (the S3.1 formula implemented from this note) ==
  the box prediction — B-READ and L-ZMULT measured end-to-end.
* **D5** LAW-DCX-4 first data (census-grade, m = 3): on 5-read window
  pairs, measured DCX₄ (TRACK ∧ Q¹ ≠ 0) vs the S6.1 nested-floor law;
  strata report (deficits realized, carries fired).
* **D6** consistency vs the committed artifacts: the RM-2 probe's RM2A
  engine legs re-executed (expect 0 violations, counts matching
  `rm2_probe_results.json`); the DCX3 v2 law re-implemented independently
  in this diagnostic and re-run on a committed-roster shape, live sets
  compared to `dcx3_sharp_v2_results.json`.
* **D7 [ADDED at r1, post-seal-labeled — the flat-corner leg, PE-F1]:**
  the d₀ = 1 FLAT shapes join the tested scope (they are now INSIDE the
  amended T-scope): at m = 3, flat-top (2,2,1), flat-mid (2,1,2) and
  double-flat (2,1,1) e-shapes, d₀ = 1, ℤ₃ + one F₂[[t]] cross-ring —
  the four box clauses on breadth inputs, tree == canonical division,
  STRICT μ-descent under the S4.1 BRANCH-(b) weights (the near-ceiling
  rule w_i := midpoint of (S_i, w_{i−1}P_{i−1}), (A_i)/(B_i) asserted
  strictly), the line quanta, and the 𝔷-kernel — all predicted-0.
  D1–D6 are UNTOUCHED (byte-exact re-run required on their output).

**ACTUALS (first run, exit 0, 0.6 s wallclock; runner md5
985795090987caa495f99d068026e797, output 874557bba69e5726bf884626e4f055f9,
results JSON 7e9177d15a2f04dfe5a1b6dd396a3b18). ZERO violations on every
predicted-0 leg — the six verdict lines, verbatim:**

    D1 VERDICT (four box clauses at m=3, predicted-0): GREEN [0 violations]
    D2 VERDICT (CONS + mu strict descent, predicted-0): GREEN [0 violations]
    D3 VERDICT (line calculus exact, predicted-0): GREEN [0 violations]
    D4 VERDICT (zh-kernel == harness reads == box pred, predicted-0): GREEN [0 violations]
    D5 CENSUS (LAW-DCX-4 vs measured, census-grade): mismatches=0 over 896 pairs
    D6 VERDICT (RM2A + DCX3-v2 artifact consistency, predicted-0): GREEN [0 violations]

Roster: RME3A = the all-(e = 2, g = 1) 5-read stack, d₀ = 1, r₄ = (1,1,2)
@ ℤ₃ AND F₂[[t]] (cross-ring); RME3B = (2,1,1)(2,1,1)(2,3,1)(1,1,1)(2,1,1),
e₃ = 1 track-everywhere; RME3C = d₀ = 2 junk-alive,
(2,1,1)(2,1,1)(2,1,1)(1,1,1)(1,1,2). All T-scope (e₀ = e₁ = e₂ = 2).
Detail: **D1** 319 engine calls (E0-consumed 142 / E1-extra 93 / breadth
84), all four clauses exact — the first (RM-3) data ever. **D2** tree ==
canonical division 319/319; interval-rule weights (w₀,w₁,w₂) =
(1, 5/4, 29/16) and (1, 3/2, 19/8) (Fractions), strict μ-descent on all
670 reduction steps, 0 flat/ascending edges. **D3** every leaf w₃ = line
exact; every promotion Δline = the exact quantum (e₂h₁ at level-1 edges,
h₂ at level-2), every correction-L Δline = 0, junk > 0, every Φ₃-exit
cofactor line = parent − w₃Φ₃. **D4** 319 𝔷-sums == the harness R-read ==
z₃^{ε′}·v·τ₃; per-leaf 𝔷-formula == harness single-cell read at every
on-line leaf; the ε₂-drop mutant differed on 46/42/27 leaves at the three
ℤ₃ runs (tooth confirmed; 0 at RME3A/F₂[[t]] where ε₂ ≡ 1 on that small
field — disclosed). **D5 — the first m = 3 carry-law data:** LAW-DCX-4
matched the measured second-round locus EXACTLY on all four towers
(live/pred 6/6, 6/6, 24/24, 21/21; 0 mismatches on 896 pairs), and every
live row was CHAIN-FED: direct rows 0, c₂ > 0 on 51/51 live rows, the
DOUBLE-nested carry c₁ > 0 feeding pool₂ on 3/12/18 of them — the nested
floor genuinely fired, at conjecture grade, first data. **W-WIN census:**
0 over-window cells; maxj hit 2P_l − 1 at levels 1 and 2 (bound tight,
held — display-grade). **D6** RM2A/ℤ₃ census == the committed
`rm2_probe_results.json` on all eight keys + n_inputs = 20, with 0
engine/tree violations on re-execution; the independently re-implemented
v2 law on EX1/ℤ₃ reproduced live = meas = 220, mism = 0 ==
`dcx3_sharp_v2_results.json`. Standing: instance evidence only; nothing ⟦C15⟧
here upgrades any proof grade (LAW-DCX-(m+1) stays conjecture at m ≥ 2).

**D7 ACTUALS [r1 repair run, wallclock 2026-08-04 — runner md5 now
c50c08e3350c2f3f1e9aa50df7914463, output d9118f5a52a5ad8fc87beca4ce00b4fe,
results JSON 1ce263df774f89043aec41874995b1bc; the D1–D6 legs re-ran
BYTE-EXACT against the committed first-run output (the only non-inserted ⟦nc⟧
delta is the volatile trailing `elapsed` float, 0.6s → 0.7s — the same
volatility class PE disclosed for the results JSON); the six committed
D1–D6 verdict lines and all six per-tower lines reproduced verbatim; the
committed results JSON's tower entries, counts, and violation list are
byte-identical inside the new file.]** The verdict line, first run,
exit 0:

    D7 VERDICT [r1 ADDENDUM] (box clauses + tree + branch-(b) mu + lines + zh on d0=1 FLAT shapes, predicted-0): GREEN [0 violations]

Roster + detail (all d₀ = 1, g₃ = 1, breadth inputs): FLT-TOP e=(2,2,1)
— the fence box's own case — at ℤ₃ AND F₂[[t]] (cross-ring): 9 engine
calls each, branch-(b) weights (1, 3/2, 11/4), 31 pops, maxj (2,2,1),
all four clauses exact, tree == division, 0 non-strict μ-edges, 9/9
𝔷-leaf ties; FLT-MID e=(2,1,2): 8 calls, weights (1, 3/2, 5/4), maxj
hit 2P₂ − 1 = 3 (window tight, held); FLT-DBL e=(2,1,1) double-flat: 4
calls, weights (1, 3/2, 5/4). v = 0 realized 0/30 (L-VNZ shadow).
30 engine calls, ZERO violations — the first engine data ever taken
INSIDE the amended T-scope's flat region, consistent with PE's M2C/M3C
exploratory censuses and predicted-0 by the r1 extension. Standing
unchanged: post-seal-labeled diagnostic, not a proof step.

## S10. Scope box — fences (what this note does NOT do)

* No proof OFF the (amended) T-scope: the d₀ ≥ 2 flat corners of S4.2
  are OPEN (four clauses + termination), priced there [r1: the d₀ = 1
  flat corners are no longer fenced — proved, S4.1(b)/S4.2]. No repair
  of the DEV-1 e₀ = 1 ∧ d₀ ≥ 2 corner — reproduced one level up, fenced.
* No proof of LAW-DCX-(m+1) at any m ≥ 2 and NO upgrade to the DCX3
  seal's conjecture grade; no second-round read clause; (W-MULT-DCX-m)
  stays open (S7.2 conditional display).
* No sharp DCX_{m+1} characterization beyond the S6 statement; the
  [ILN]† S4.7 outer bound is consumed for display only, its (given IH ⟦C22⟧
  package) qualifier riding along.
* No edit to [IL]/[IL3]/[ILN] or any sealed artifact — all read-only; ⟦nc⟧
  ledger/display upgrades (ITERLAWN S7 rows on T-class, L3CONVERGENCE,
  ROOT §44 Q1) ride the next fold, NOT applied here.
* No general lawful lifts ((ITER-LAW-LIFT) propagates); no gr(w) wrapper,
  no GMN transport, no Lean work, no density-side claim.
* W-WIN at m ≥ 3: display + census only. ⟦C5⟧

## S11. Records: claim DAG + suggested hostile charges

**Claim DAG (arrows = consumed-by):**

    [IL]† L1 ─→ L-ZMULT, (C-EPS), L-MU(display)          EXP-KIT(b) ([ILN]† S3) ─→ L-ZMULT
    [IL3]† DIGIT-SPLIT ─→ L-NORM [r1] ─→ S2.1 initial cells, S2.2 correction children,
                                          L-LINE(ii)/(iii)
    [ILN]† S4.1 REALIZE/WELL-DEF/READ-ADD ─→ L-CELL, L-LINE(ii)/(iv), DEV-(m)(D3), Ĉ_m data
    L-CANON ─→ L-EXACT ─→ RM-GEN                          L-CELL/B-READ ─→ DEV-(m), RM-GEN
    L-ZMULT ─→ DEV-(m) T-tele(i+1), RM-GEN (C-READ)
    L-GRID [r1] ─→ DEV-(m)(D3) [the empty off-grid case], RM-GEN (C-READ), L-VNZ
    L-VNZ [r1] ─→ scope remark (ii) [v = 0 impossible]    (consumed by no theorem clause)
    L-LINE/L-MONO ─→ DEV-(m)(D2)/(D3), (C-W_R)/(C-W_Q), S6.2 crossing law
    L-MU [r1: branches (a)+(b)] ─→ T-TERM ─→ DEV-(m) [the induction carrier]
    L-LEX ─→ (remark at r1; consumed by nothing)
    DEV-(m) ─→ THEOREM RM-GEN ─→ COR-A (with [ILN]† S7; at-acceptance ledger rider)
    LAW-DCX-(m+1) [CONJ m ≥ 2; [IL3]† at m = 1; DCX3-sealed at m = 2] ─→ COND-B (conditional only)
    W-WIN ─→ (nothing)

**Suggested hostile charges (2-clean model-diverse bar):**
(i) CONTENT pass on the two load-bearing computations: L-ZMULT's
carry-cancellation (is the ε-leg's z^{−δ} exactly EXP-KIT(b)'s, at every
level, with the top level's missing ε-leg handled right?) and
T-tele(i+1)'s assembly (the B-READ-on-C_k step: may the d-cell sums be
taken inside the k-sum with the claimed uniform upper factors? the λ_i
rise β_k exact? sign discipline of (ID-(i+1))?). Also the S2.2
structural remark (exit cofactor single-digit at ANY top exponent) and
the (C-W_Q) chain through it. (ii) SCOPE pass: does any S5 step silently
consume the window display (charge C5), residue survival (C10), or an
e_i ≥ 2 not in the T-scope pin? Is the T-class(n) display of S7.1 the
exact union of the per-call pins? (iii) EXECUTION pass: from-note
reimplementation of 𝔷, L-ZMULT, and the D1/D4 legs at m = 3 on
OFF-diagnostic shapes; a DCX₄ meter on fresh 5-read shapes vs LAW-DCX-4.
(iv) RULE-8/citation audit per the head declaration (content-token
fidelity of the three pastes; the v2 law's conjecture grade stated at
every use — grep "v2" and "UNIFORM CARRY LAW").

**Consumers and owed displays (ride the next fold; NOT applied here):**
ITERLAWN S7 rows 5/6/8 + the S4.6 box STATUS line (m ≥ 2 OPEN → PROVED on
T-scope as amended at r1, d₀ ≥ 2 flat corners open) — at acceptance only; ROOT §44.(ii) Q1 status;
the (W-MULT-DCX) unit inherits S6.2/S7.2 as its design input; the Q3
corner queue inherits the S4.2 fence (the flat-level termination unit).

**REPAIR RECORD, round 1 (per-finding disposition — the head bracket's
long form):**

| finding | class | disposition |
|---|---|---|
| PC-C1 (= PE-F2) | CRITICAL, adjudicated TEXT-GAP | CURED: Lemma L-NORM displayed (S2.1); S2.2 children, L-LINE(ii)/(iii), DEV-(m)/T-tele residues re-derived through it; referee = PE 97/97 + diag digit_split_norm |
| PE-F1 | CRITICAL, display-local | CURED + STRENGTHENED: fence mechanism corrected (ρ = P−1 cascade, W_{i−1} removed); fence re-scoped to d₀ ≥ 2; L-MU branch (b) + T-TERM + RM-GEN extended to the d₀ = 1 flat corners, PROVED (full derivation + inspection census); L-LEX subsumed → remark; D7 diag leg added |
| PC-G2 | GAP (over-grading) | REGRADED: S6.2 split into crossing law (proved) / pool identification (heuristic, conjecture-supported); row 11 → 11a/11b/11c; the L-MU T-scope pin restored at the citation |
| PC-G5 (+ PE-F3 dissolution) | GAP (definedness) | CURED: first-failure convention (S3.1) + Lemma L-GRID; (D3) off-grid case = EMPTY; converse-integrality appeal DELETED; RM-GEN branch simplified |
| PE-F3 | GAP (dead clause) | CURED: Lemma L-VNZ (v ≠ 0 forced, full induction displayed); scope remark (ii) rewritten; C10 supported vacuously |
| PC-G4 (= PE-n5) | GAP (status display) | CURED: S7.1 inline at-acceptance qualifier; n = 1, 2 exact-union correction displayed; headline consequence line carries the rider |
| PC-G3 (+ PE-n4) | GAP (charter) | CURED: rows C15–C22 added from a fresh census; C2 hypotheses recorded + key unwrapped to one line; whitespace-normalization caveat displayed |
| PE-n6 | nit (garbled displays) | CURED: L-LINE(ii)/(iii) rewritten, S4.1 existence proof rewritten, fence-box W_{i−1} eliminated |

No established clause was weakened and no proof content was lost; one
PROVED-labeled row was honestly REGRADED (row 11 → 11a/11b/11c: the pool
identification to heuristic, per PC-G2 — an over-grade correction, and
nothing RM-GEN's proof consumes) [r2, PC2-g1: this sentence formerly said
"no PROVED clause was weakened", conflating regrading a mislabeled row
with weakening an established one]. The r1 statement-change census, per
the actual `git diff bf18bfe 8835d20` [r3, PC3-g3: the r2 sentence here
read "The only statement changes are the T-scope/T-class STRENGTHENING
… and the L-LEX regrade to remark" — under-enumerated; the changes
below were disclosed by name in the R1–R6 record above, but the "only"
sentence named two] [r4, PE4-G-1: the r3 census claimed completeness
("SIX changes") and was itself under-enumerated — change (7) below is
PE4's witness, of the same genre; the fourth recurrence of the
hand-census completeness pattern. THE DIFF ITSELF IS THE AUTHORITATIVE
SURFACE; the enumeration below is a reading aid, adjudicated across
rounds 3–4, with no completeness claim]: (1) the T-scope/T-class
STRENGTHENING (argued in full); (2) the L-LEX regrade PROVED → REMARK
(its content retained); (3) scope remark (ii)'s "v = 0 is legal" clause
restated as Lemma L-VNZ (v ≠ 0 forced — the composed clause was dead
weight, PE-F3); (4) COR-A's n = 1, 2 exact-union correction (the
composed claim imposed a spurious T-condition; its removal is a
strengthening); (5) L-GRID's empty-case conclusion replacing (D3)'s
converse-integrality appeal (the appealed direction was never stated by
L-ZMULT); (6) S8 row 11 split into 11a/11b/11c (the PC-G2 regrade);
(7) L-LINE(ii)'s composed "when the residue product vanishes the
L-child … is absent" clause replaced by its NEGATION "it is NEVER
absent" — the vanishing-residue possibility exposed as an artifact of
the un-normalized display and eliminated by L-NORM(a) (the R1 repair's
core; secondary witness of the genre: the fence box's false "can
genuinely RAISE any linear μ" sentence deleted at R2). All are repairs
or strengthenings; none weakened an established clause. LAW-DCX-(m+1)
grades untouched.

**REPAIR RECORD, round 2 (2026-08-08 campaign, wallclock 2026-08-04).**
Verdicts on the r1 text (8835d20): PC2 (Codex, archived 9466efe)
GAPS-ONLY 0C/2G; PE2 (Fable, `RMENGINE_passPE2_report.md` + referee
`rmengine_pe2_ext.py`, 355 lines, machine legs: 67 zero-violation calls
on 8 fresh flat towers, 1080-shape branch-(b) sweep, 225 infeasibility
confirmations, committed-diag byte-exact re-run) GAPS-ONLY 0C/1G+2n.
Zero critical findings in either pass; every round-1 remedy discharged
under hostile re-derivation. Dispositions, all documentation-layer:

| finding | class | disposition |
|---|---|---|
| PC2-g1 | GAP (record wording) | CURED: the hygiene sentence above rewritten — regrade ≠ weakening, row-11 regrade named explicitly |
| PC2-g2 = PE2-1 | GAP (charter completeness, convergent) | CURED: rows C23–C28 added (the PE2 census absentee list + the repair-record claim [r5, PC5-g1 supersession: "complete … list" was this round's claim; refuted at round 3 by PE3's L-LEX witness — rows C29–C33 followed]); charter bracket now names its census basis; C22 narrowed per PC2 |
| PE2-2 | NOTE (metadata) | CURED: head bracket C15–C19 → C15–C22; C21/C22 [r1] tags restored |
| PE2-3 | NOTE (citation paraphrase) | CURED: L-NORM's DIGIT-SPLIT cite corrected — no primitivity hypothesis upstream; primitivity is where the L-child is nonzero |

No math content changed at r2 (wording, table rows, and brackets only —
every S2–S7 display is byte-identical to 8835d20 except the L-NORM
citation sentence: the rewritten parenthetical AND, outside it, the two
connective words "applies to" → "applies in particular to" in the same
sentence [r3, PE3-N-1: the r2 exception clause under-described the
changed region by those two words]).

**REPAIR RECORD, round 3 (2026-08-08 campaign, wallclock 2026-08-04).**
Verdicts on the r2 text (423b399): PC3 (Codex, the first acceptance
attempt, archived `RMENGINE_passPC3_report.md`) GAPS-ONLY 0C/3G; PE3
(Fable, `RMENGINE_passPE3_report.md`, full spine re-derivation — both
L-MU branches from scratch, all telescopes, the necessity iff, the
inspection census re-grepped — plus byte-exact read-only re-run of the
committed referee `rmengine_pe2_ext.py`, all 12 verdict lines GREEN)
GAPS-ONLY 0C/3G + 2 notes. FOUR consecutive hostile passes with zero
critical findings; ZERO mathematical findings at round 3 — every
finding is record-layer. Dispositions:

| finding | class | disposition |
|---|---|---|
| PC3-g1 | GAP (stale gloss) | CURED: the title parenthetical now names both branches of the amended T-scope (d₀ = 1 flat reads admitted) |
| PC3-g3 (+ C28) | GAP (under-enumeration) | CURED: the round-1 record's statement-diff sentence and row C28 re-enumerated per the actual diff [r5, PC5-g1 supersession: this cell's "all SIX" was itself refuted at round 4 (PE4-G-1, change 7) — the completeness claim is withdrawn note-wide; the diff is the arbiter]; the truthful core (all repairs/strengthenings, none weakening) retained |
| PC3-g2 = PE3-G-3 | GAP (charter completeness, third recurrence — convergent) | KILLED STRUCTURALLY: the S8 charter re-arbitrated to the TAG GRAMMAR — every charter-keyword body line carries ⟦C<nn>⟧ or a challengeable ⟦nc⟧; rows C29–C33 added (C29 = PE3's L-LEX witness, verified TRUE); machine-checked by `rmengine_claims_census.py` |
| PE3-G-1 | GAP (dead grep keys) | CURED: C26/C28 re-keyed to the committed text; full key sweep also re-quoted the stale C7/C10 keys; normalization rule now strips tags before matching |
| PE3-G-2 | GAP (false provenance) | CURED: the round-2 bracket and C28's basis now state PC2's content-only (execution-less) basis vs PE2/PE3's actual-diff verification |
| PE3-N-1 | NOTE (exception clause) | CURED: the r2 byte-identity clause now also names the "applies in particular to" two-word change |
| PE3-N-2 | NOTE (census gloss) | CURED: the S4.2 S2.1-leg gloss displays the READ-ADD-(m−1) caveat |

**The census closure (the exit gate of this repair), machine output
quoted from the committed `rmengine_claims_census_output.txt`:**

    MACHINE CENSUS (tag grammar): 67 keyword lines in body (55 row-tagged, 12 nc-tagged); 33 table rows (2 table-only); excluded zones: 11

    CENSUS CLOSED — zero violations.

[r5, PE5-G-1: the quote above is AS OF the r3 run (ab2e471). Each
repair round adds record surfaces (auto-excluded zones), so the zone
count in later re-runs grows while every other figure is unchanged and
the verdict stays CLOSED (r4 run, at d63f8bb: zones 13). The
AUTHORITATIVE current figures are always the committed
`rmengine_claims_census_output.txt` at the same commit as the note —
that file, not any quote in this note, is the arbiter. Standing rule
from this finding: machine-output quotes carry their as-of commit, and
only the committed output file speaks for the current text.]

The r3 edit touched no mathematical display: S2–S7 changes are trailing
⟦…⟧ tags only, plus the two queue-named wordings (the title gloss,
PC3-g1; the S4.2 census-gloss caveat, PE3-N-2).

**REPAIR RECORD, round 4 (2026-08-08 campaign, wallclock 2026-08-04).**
Verdicts on the r3 text (ab2e471): **PC4 (Codex, archived 998e599)
CLEAN 0C/0G** + 2 notes (the one-tag-per-line loophole: hypothetical,
no concrete instance in this text; execution unavailable — content-only
basis, disclosed); PE4 (Fable, `RMENGINE_passPE4_report.md`, referee
`rmengine_pe4_ext.py`) GAPS-ONLY 0C/1G + 1 note. PE4's machine legs:
the claims census reproduces CLOSED byte-identically; `rmengine_pe2_ext.py`
all-GREEN byte-identical (md5 untouched); the r3 statement fence HOLDS
against the actual `git diff 423b399 ab2e471`; all 44 row-key strings
exact quotes; all 12 ⟦nc⟧ tags survive individual challenge; the zone
fix legitimate; all attributions accurate at source; C29–C33 true.
Dispositions:

| finding | class | disposition |
|---|---|---|
| PE4-G-1 | GAP (census completeness, 4th recurrence — statement-diff surface) | CURED: change (7) added (L-LINE(ii) vanishing clause → NEVER-absent negation, the R1 core; secondary witness named); THE COMPLETENESS CLAIM WITHDRAWN — the diff is the authoritative surface, the enumeration a reading aid; C28 rewritten to match, basis re-attributed per-pass |
| PE4-N-1 | NOTE (bracket scope) | CURED: the round-3 head bracket's fence sentence re-scoped (tags throughout the body, not only S2–S7); the tail record was already correct |

The r4 edit is record-layer only: the round-1 record's census sentence,
row C28, the round-3 head bracket, this record, the head round-4
bracket, and the tail acceptance-counter lines [r5, PE5-n: the counter
lines now named]. NO S2–S7 content touched; no tags moved; the census
re-run CLOSED after the edit (output re-committed; zones 11 → 13 as of
d63f8bb [r6, PE6-N-1: as-of commit now inline] — the two new record
surfaces; disclosed per PE5-G-1's standing rule).

**The standing lesson, now policy in this note: enumerations claim no
completeness. Machine-checkable surfaces (the tag census; the git diff)
are the arbiters; enumerations are reading aids.**

**REPAIR RECORD, round 5 (2026-08-08 campaign, wallclock 2026-08-04).**
Verdicts on the r4 text (d63f8bb): PC5 (Codex, archived with this
round) GAPS-ONLY 0C/1G + 1 note; PE5 (Fable,
`RMENGINE_passPE5_report.md`) GAPS-ONLY 0C/1G + 4 notes. SEVENTH AND
EIGHTH consecutive 0-critical passes; both spine sweeps clean; PE5's
machine legs green (census CLOSED byte-identical; pe2 referee
byte-identical; the r4 fence verified against the actual diff; change
(7) verified against the actual r1 diff). Dispositions:

| finding | class | disposition |
|---|---|---|
| PC5-g1 | GAP (supersession) | CURED: dated supersession brackets added at the three surviving complete-enumeration record assertions (the round-1 bracket's "completed", the round-2 record's "complete … absentee list", the round-3 record's "all SIX") — history retained, claims superseded in place |
| PE5-G-1 | GAP (stale machine quote) | CURED: the round-3 record's census quote annotated AS-OF ab2e471, the r4-run figure displayed commit-pinned (zones 13 at d63f8bb); STANDING RULE [unified at r6, PC6-g1: this cell formerly ALSO asserted "re-runs that change any figure are re-quoted" — a second rule the note does not follow; superseded]: machine-output quotes carry their as-of commit, and only the committed output file speaks for the current text |
| PE5-n (×4) | NOTES | CURED: the "all SIX" cell and "complete … list" wording (= PC5-g1's sites); the r3 bracket's gloss now names the pre-S0 head-matter tags; the r4 edited-surface list now names the tail counter lines |

**The r5 standing rule (the class fix): supersede-at-refutation —
whenever a later round refutes a record claim, the earlier record gets
a dated supersession bracket in the SAME repair; and machine-output
quotes carry their as-of commit, with only the committed output file
speaking for the current text [r6, PC6-g1: this sentence formerly ended
"…are re-quoted whenever a re-run changes any figure" — a second,
unfollowed rule in tension with the file-arbiter rule; unified to the
latter, which the note satisfies by construction].** The r5
edit is record-layer only (supersession brackets, the as-of annotation,
two gloss completions, this record, the head round-5 bracket).

**REPAIR RECORD, round 6 (2026-08-08 campaign, wallclock 2026-08-04).**
Verdicts on the r5 text (cfb3648): **PE6 (Fable,
`RMENGINE_passPE6_report.md`) CLEAN 0C/0G** + 2 notes (N-1 the zone
figures' as-of placement — cured below; N-2 the S4.1 (B₁) ⟺-chain
majorization reading — a display note, all operative claims verified);
machine legs byte-identical across the census, the pe2 referee, AND the
S9 diag; the historical zone figures machine-verified; the dagger
pastes content-token identical; the spine re-derived from scratch. PC6
(Codex, archived 0182e1b) GAPS-ONLY 0C/1G: the two-rules inconsistency.
Dispositions:

| finding | class | disposition |
|---|---|---|
| PC6-g1 | GAP (rule inconsistency) | CURED: the machine-quote rule UNIFIED to the file-arbiter form at both asserting sites (the "re-runs are re-quoted" clause superseded in place); the note now asserts exactly the rule it satisfies by construction |
| PE6-N-1 | NOTE (as-of placement) | CURED: the round-4 record's "zones 11 → 13" now carries its as-of commit inline |
| PE6-N-2 | NOTE (display) | STANDS as a note: the (B₁) consistency display's ⟺-chain reads exactly under the stated ρ₀ ≤ P₀ majorization; all operative claims verified true by PE6 — no edit needed |

The r6 edit is record-layer only (the two rule-site brackets, the
inline as-of, this record, the head round-6 bracket); the census re-ran
CLOSED (output re-committed with this round).

**ACCEPTANCE RECORD (round 7, 2026-08-08 campaign, wallclock
2026-08-04).** On the r6 text (a0e266c): **PC7 (Codex, archived 5021a3b)
CLEAN 0C/0G** (2 notes: the hypothetical one-tag-per-line loophole, no
instance; execution unavailable, content basis disclosed) and **PE7
(Fable, `RMENGINE_passPE7_report.md`) CLEAN 0C/0G** (3 notes + 1
informational, none blocking; machine legs: census byte-identical at
zones 17, referee 12/12, the r6 fence and the CUMULATIVE ab2e471 →
a0e266c diff verified record-layer only, the historical zone figures
11/13/15/17 machine-verified at their four commits, twelve archived
verdict lines and nineteen cited commits all checked, the spine
re-derived standalone end to end). **The bar — two CLEAN model-diverse
passes, same round, same text — is MET. THEOREM RM-GEN is ACCEPTED.**
Counter 2/2. The acceptance edits (this record, the head ACCEPTED
bracket, the S7.1 ledger application, the row-11c erratum) are dated
post-acceptance annotations; the accepted content is a0e266c's.

— GENERIC (RM-m) ENGINE COMPOSE unit, campaign date 2026-08-08 (wallclock
2026-08-04). ACCEPTED at round 7; the ledger block is [RMG].
