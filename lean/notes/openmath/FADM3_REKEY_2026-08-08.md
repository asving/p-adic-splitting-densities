# F-ADM-3 CARRIER RE-KEY — `wphi` to the O-9 §4 top-normalized ledger + `classCount_eq_d`

Unit: F-ADM-3 CARRIER RE-KEY (synthesis-7 queue #4, pass-7 F2a; charter
`docs/SYNTHESIS_PASS7_2026-08-08.md` queue item 4 + §F2(a)).
Finding of record: F-ADM-3 (`lean/notes/openmath/ADMREAL_2026-08-08.md` §5b);
repair design: ADMREAL §8 ripple items (2)–(4).
Authority: the STANDING DEFINITION-CHANGE AUTHORITY (Asvin 2026-08-05 memory
entry; ADMREAL §5b named this edit SIGN-OFF GATED and pass-7 F2a resolved the
gate to that authority, with Codex confirmation of the applied diff as the
on-record mechanism — §6 below).
Window-label dating per the standing DATING CONVENTION disclosure
(`lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md`): chronology = git commits.

## 0. Verdict up front

* The Lean carrier `CensusData.wphi` (`lean/LeanUrat/Scaffold/ValueSide/CensusCore.lean`)
  is RE-KEYED from the birth values `e_i·V_i + h_i` to the O-9 §4
  top-normalized ledger `E_i·(e_i·V_i + h_i)`, `E_i = e_{i+1}⋯e_r`
  (new defs `Emul`, `wbirth`; old body displayed in the `wphi` docstring
  with the F-ADM-3 cite).
* **CLASS-LAT is now MACHINE-CHECKED at every order**:
  `CensusData.classCard_eq_d` (CensusCore.lean) via the new abstract kernel
  `ClassLat.digitSum_inj`/`card_digitBox_class` (pure `Nat.ModEq`
  triangular-system induction, head-peeling form of ADMREAL §5's proof).
* **ADM-U2's `hclass` hypothesis is RETIRED**: `HD3.classCount_eq_d`
  (AdmCarrier.lean) is the proved law; `admFull_iff_maxClassWeight` is
  restated hypothesis-free (a strengthening — statement change under the
  standing authority, old form displayed in its docstring).  Grep at HEAD:
  no consumer passed `hclass`, so no downstream statement moved.
* Full ripple GREEN (`lake build` at the re-keyed head; the 33-module
  transitive-importer cone incl. all six audit files rebuilt); footprints
  Lean-core (§4); capstone path untouched (§1 fence).
* Falsifier: NEW sealed leg R7 in `verification/openmath/admreal_check.py`
  (REVISION 2 block).  Predictions sealed in the PREREGISTRATION COMMIT
  carrying this note; verdict in the follow-up commit (§5).

**DEFINED TERM — REKEY-CLEAN** (the scoped verdict this note claims; all
restrictions live HERE and nowhere else): *on the full r ≤ 3 abstract type
grid of the ADMREAL falsifier (12,348 types: stages (e_i, h_i, f_i) with
e_i ∈ {1,2,3}, h_i ∈ {1..4}, gcd(h_i, e_i) = 1, f_i ∈ {1,2}, f₀ ∈ {1,2}),
the re-keyed carrier's weight enumeration equals the O-9 §4 top-normalized
ledger enumeration ELEMENTWISE (same digit order), and every residue class
mod e carries exactly d slots at every grid type.*  The Lean theorem
`classCard_eq_d` proves the class-count clause at ALL orders and ALL carrier
data (no grid restriction); REKEY-CLEAN is the numerics-side twin claim the
R7 leg measures.

## 1. Spec gate + capstone fence (both PASSED before any edit)

* Charter located: `docs/SYNTHESIS_PASS7_2026-08-08.md` queue #4 ("F-ADM-3
  carrier re-key + `classCount_eq_d` (M Lean, F2a): rescale `wphi` by E_i per
  ADMREAL §8; re-run the F-ADM-2 scan (prediction: clean = CLASS-LAT);
  replace ADM-U2's `hclass` by the proved lemma; executed under the standing
  definition-change authority with Codex diff confirmation") + §F2(a).
  Unambiguous, not previously executed (pre-edit `wphi` still read
  `e_i·V_i + h_i`).
* Capstone fence: computed the full import closure of
  `OM/RealInstanceV2.lean` + `OM/RealInstanceV2Gates.lean` (87 modules) —
  contains NO ValueSide module; intersection with the CensusCore
  reverse-import closure (33 modules) is EMPTY.  B2D reaches CensusCore
  transitively (TDDefs → H2/TransDeep → … → Hyps → CensusCore) but B2D is
  not on the capstone path; B2D statements untouched (one B2D file needed
  zero edits — §3).

## 2. The applied definition change (CensusCore.lean)

OLD (the F-ADM-2-era coded lattice, landed from O-9 §2's r = 1 worked data
where E₁ = 1 hides the factor):

    def wphi (D : CensusData) (i : Fin (D.r + 1)) : ℕ :=
      if i.1 = 0 then 0 else D.e i * D.Vrec i.1 + D.h i

NEW (this unit; O-9 §4 ledger wt, `w = v_{r+1}`):

    def Emul (D : CensusData) (i : Fin (D.r + 1)) : ℕ :=
      ∏ j ∈ Finset.Ioi i, D.ledgerE j
    def wbirth (D : CensusData) (i : Fin (D.r + 1)) : ℕ :=
      if i.1 = 0 then 0 else D.e i * D.Vrec i.1 + D.h i
    def wphi (D : CensusData) (i : Fin (D.r + 1)) : ℕ :=
      if i.1 = 0 then 0 else D.Emul i * (D.e i * D.Vrec i.1 + D.h i)

Support lemmas: `Emul_pos`, `Emul_last` (E_r = 1: empty product),
`wbirth_coprime_ledgerE` (gcd(e_iV_i + h_i, e_i) = gcd(h_i, e_i) = 1;
stage 0: gcd(0,1) = 1), `wphi_eq_wbirth_mul_Emul` (the kernel shape
`wphi = wbirth · E`, true at stage 0 as 0 = 0·E₀), `wphi_last` (top-stage
agreement — the evaluation every r ≤ 1 ripple proof reads).
`Emul` uses `ledgerE` (≡ `e` at stages ≥ 1, = 1 at stage 0): every `j > i`
has `j.1 ≥ 1`, so the product is literally `e_{i+1}⋯e_r`.

`wt`/`Gset`/`attainDim`/`s`/`onLineSlots`/`ADMFull` are textually UNCHANGED —
they read the re-keyed `wphi` through the same sums.

## 3. The Lean CLASS-LAT + the consumer ripple (all green)

**Kernel (`ClassLat` namespace, CensusCore.lean; no census dependence):**

* `wsys m u i := u i * ∏_{j ∈ Ioi i} m j` — the abstract triangular weight.
* `digitSum_inj`: for radices `m i ≥ 1` and units `gcd(u i, m i) = 1`, the
  map `a ↦ Σ (a i)·wsys i` on `Π i, Fin (m i)` is injective mod `∏ m i`.
  Proof = ADMREAL §5's induction in HEAD-peeling form (the note peels the
  top stage; peeling the head is the list-`cons` direction and matches
  `Fin.sum_univ_succ`/`Fin.prod_Ioi_succ`): mod the tail product the head
  term dies and the tail is EXACTLY the truncated system (the (F3)
  identification is definitional here); then
  `Nat.ModEq.add_right_cancel'` + `mul_right_cancel'` (cancel `e'` from
  value AND modulus) + `cancel_right_of_coprime` (the (F2) unit) pin the
  head digit.
* `card_digitBox_class`: each class mod `∏ m i` is hit EXACTLY once, any
  constant offset (injectivity + card(box) = ∏ m i = card of the residue
  range → image = range).

**Carrier theorem (`CensusData.classCard_eq_d`):** #{j ∈ J : wt j ≡ c mod e}
= d for EVERY c, EVERY carrier datum, EVERY order.  Assembly: split each
digit pair (class digit a_i < ledgerE_i, t-digit b_i < f_i); for fixed b the
weight is (triangular a-sum, in kernel shape via `wphi_eq_wbirth_mul_Emul`)
+ (b-offset); fiberwise count over b (`card_eq_sum_card_fiberwise` +
explicit `card_bij'`) gives 1 per fiber × ∏ f_i fibers = d.  NOTE the
simplification vs ADMREAL §5's printed proof: the f-digits decouple as a
constant offset per fiber, so only the a-lattice needs the induction — the
class count per fixed b is exactly 1.

**Consumer ripple (the §5b six-file audit, re-grepped at HEAD; `lake build`
green over the full 33-module cone):**

| consumer | fate under the re-key |
|---|---|
| `CensusCore.lean` (def site) | re-keyed + kernel + `classCard_eq_d` added |
| `HDischarge/H3/AdmCarrier.lean` | `classCount_eq_d` ADDED (:= `classCard_eq_d`); `admFull_iff_maxClassWeight` RESTATED hypothesis-free (old `hclass` form in docstring); ADM-U3's r ≤ 1 proof repaired at ONE line (`wphi 1 = h 1` now reads `Emul_last`: E₁ = 1) — statement verbatim |
| `HDischarge/H3/AdmGates.lean` | ZERO edits — all three gates (K7/K7⁺/K6) are r = 1 `decide`/computation legs where the re-key is value-invariant (E₁ = 1); rebuilt green |
| `ValueSide/Census.lean` | ONE proof repair (same `wphi 1` line in `attainDim_eq_d_iff_r1Bound_le`); r = 0 automaticity + `gate_wt` + `attained_always` unaffected (stage-0/if-branch or digit-zero routes) |
| `B2D/TD0_ledgerInstance.lean` | ZERO edits — `tdD_wphi_one : tdD.wphi 1 = 1 := rfl` survives (kernel reduces the empty `Emul` product; all e ≡ 1) |
| `B2D/TD3_countermodel.lean` | ZERO edits — `cmD` is r = 0 (if-branch route) |

Derived-hypothesis consumers (`ADMFull` rows in `DensityTie.lean`,
`SeriesTie.lean`, `Hyps.lean`, `H1/Defs.lean`): rebuilt green, no edits (they
price `ADMFull`, no weight arithmetic — as the §5b audit recorded).

## 4. Footprints (all Lean-core; census re-run)

`lake env lean LeanUrat/AxChk_baseline.lean` re-run at the re-keyed head:
capstone `OM.RealInstanceV2.montes_unconditional` = [propext,
Classical.choice, Quot.sound] — unchanged; no footprint regression anywhere
in the census.  New decls checked directly: `ClassLat.digitSum_inj`,
`ClassLat.card_digitBox_class`, `CensusData.classCard_eq_d`,
`wphi_eq_wbirth_mul_Emul`, `wbirth_coprime_ledgerE`, `HD3.classCount_eq_d`,
`HD3.admFull_iff_maxClassWeight`, `HD3.classCount_eq_d_of_r_le_one`,
`HD3.k7_not_admFull`, `HD3.k7Pos_admFull` — all Lean-core.  Zero new
`sorry`; the repo's single banked sorry is untouched.

## 5. Falsifier leg R7 (sealed) + verdict slot

Contract (sealed in the REVISION 2 header block of
`verification/openmath/admreal_check.py`, committed in this unit's
PREREGISTRATION COMMIT before first execution; the two-commit seal protocol
of ADMREAL REVISION 1):

* R7(a): REKEY-CLEAN, elementwise-equality clause — `rekey_type_data`
  (transcribed from the NEW Lean defs, independent of `type_data`'s
  note-side generators) vs `type_data` on all 12,348 grid types.
  Prediction: 0 mismatches.
* R7(b): **the charter's ACCEPTANCE TEST** — the r = 2 grid on which the
  coded (pre-re-key) lattice fails (CLASS-d) at EXACTLY 88 types (leg R3′,
  contract kept as the historical reproduction) has ZERO failing types
  under re-keyed carrier semantics.  Prediction: 0.
* R7(c): the F-ADM-2 quoted type (2,1,1),(2,1,1),f₀=1 → weights {0,2,5,7},
  uniform mod 4.  Prediction: both.
* Exit contract REVISION 2: R7 joins the live all-pass condition.

**VERDICT (run 1 = the first execution of the REVISION 2 contract; sealed at
preregistration commit ba02d60; log
`verification/logs/fadm3_rekey_run1_20260803T042912Z.log`):** **ALL LIVE
PREDICTIONS PASS, exit 0.**  R7(a) 12,348 grid types, 0 carrier-vs-ledger
mismatches (REKEY-CLEAN, elementwise clause); R7(b) THE ACCEPTANCE TEST:
re-keyed r = 2 grid 0 class-count failing types — the F-ADM-2 88 → 0 — while
the historical coded twin R3′ stays == 88 EXACTLY; R7(c) quoted type weights
[0, 2, 5, 7], every class mod 4 hit once.  All prior legs unchanged PASS
(R1 0/12,348; R2 incl. theorem-grade (c) 0 hits, neg-pool (e) 243 as before;
R3(a) 0; R3(b)-STATE clean; R4/R5 PASS; R3″ 0/11,664; R6 0 fails with the
43,536/0 premise-control split).

**Verifier-implementation contract (pass-7 F2c standing expectation):** a
verifier-owned from-note implementation of R7 needs only §2's three
displayed Lean definitions + the digit ranges (stage 0: `j₀ < f₀`; stage
i ≥ 1: `j_i < e_i·f_i`) + `wt = Σ j_i·wphi_i`; the acceptance arc should
reproduce 88 (birth lattice) → 0 (re-keyed) on the §0-defined grid and is
expected to run it independently at acceptance.

## 6. Honesty summary + Codex diff confirmation slot

* PROVED (this unit, machine-checked, Lean-core): CLASS-LAT at every order
  on the re-keyed carrier (`classCard_eq_d`/`classCount_eq_d`);
  `admFull_iff_maxClassWeight` hypothesis-free; the kernel lemmas.
* CHANGED (definitions/statements, standing authority): `wphi` (definition;
  old form displayed at the def site); `admFull_iff_maxClassWeight`
  (hypothesis REMOVED = strengthened; old form displayed).  No statement
  was weakened anywhere; no consumer statement moved.
* NOT CLAIMED: anything about the (ADM) [M]-row beyond what ADMREAL claims
  ((B-PIN)/(FRESH)/value-attainment still price it); realized r ≥ 2 numeric
  coverage (STILL none exists — R7 is lattice-level, exactly like R3′/R3″;
  the discrimination between the two weight conventions at a REALIZED read
  remains pinned by TRI + (P1), ADMREAL §2); any change to O-9's text.
* Numerics are falsifiers, never proofs: R7 measures REKEY-CLEAN on the
  grid; the theorem is the Lean proof.
* **Codex diff confirmation (the standing authority's mechanism):** the
  applied diff (commit ba02d60, definition-layer files inline) passed to
  Codex in a fresh context with the F-ADM-3 charge (confirm: exact repair,
  nothing else at the definition layer; no statement weakened; old forms
  displayed; `classCount_eq_d` = CLASS-LAT).  CODEX VERDICT: **CONFIRM**,
  all four points affirmed verbatim ("Removing `hclass` … strictly
  strengthens it; the two r=1 changes are proof-only"; "former `wphi` body
  and former `hclass` theorem form … explicitly displayed").  Disclosed
  caveat: Codex's local sandbox could not launch commands (bwrap), so the
  confirmation is diff-based — which is the mechanism's charge ("Codex
  confirms the applied diff"); the compile/footprint legs are this note's
  §3/§4, machine-checked here.

## 7. Ripple duties handed on (owners unchanged, per ADMREAL §8)

* Blueprint `lean/blueprints/HDISCHARGE_H3.md`: execution record appended
  (F-ADM-2's re-run duty RESOLVES into this re-key; ADM-U2 row: `hclass` →
  proved `classCount_eq_d`).
* (ADM) row display: the ROOT fold (next r-fold owns it; this note is the
  citable artifact).  (C-JCT) fan-out audit proceeds on the now-honest
  carrier (queue path unchanged).
* G-ADM-2 / F-ADM-2 "re-run duty" list: discharged by R7 at the lattice
  level; any future REALIZED r ≥ 2 harness still owes its own gate.
