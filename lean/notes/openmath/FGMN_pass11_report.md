Quote-and-classify audit:

1. **CRITICAL — the “ONLY” acceptance list omits part of the required standing perimeter.**

   REVISION 9 accepts fixed points only as:

   > “the S1 two-scale law; `juncforge_clauseIII_derivable`; the DITER §S3 supplier obligations”

   That enumeration excludes the parity-wall/JuncForge refutation semantics and the D0 fixed point (`juncforge_no_e2_junction`, `juncforge_D0_instanceFalse`). Item (e), “the pinned sources and the battery record,” accepts R2/R5 observations but does not accept the separate Lean-semantic claim that those existing theorems retain their exact compiled meanings. Because acceptance binds “ONLY” the enumerated content, the requested JuncForge/D0 honesty perimeter is not fully inside acceptance.

2. **GAP — the scope-cut rationale makes an unjustified completeness claim.**

   > “no pre-application textual or build-error procedure can totally and checkably specify this change-set”

   Five failed perimeter procedures establish that those five procedures failed, not that every possible pre-application procedure must fail. Likewise:

   > “the only total oracle: review of the actual diff + the rebuilt tree + a hostile pass”

   Review and hostile verification are strong adjudication methods, but not “total” completeness oracles in the formal sense. This does not damage the mathematical re-derivation, but overstates what the scope cut established.

3. **GAP — fresh artifact execution could not be completed in this pass.**

   The local runner failed before command execution because `bubblewrap` is unavailable. Therefore I could not independently recompute the hashes, inspect the actual Lean declarations, or rerun the 1,093-check battery. The note honestly records earlier independent executions, but this pass cannot itself certify item (e).

Everything else survives the textual hostile check:

- REVISION 9 explicitly and effectively demotes every REVISION 4–8 perimeter formulation, including their formerly “NORMATIVE” language.
- The main-body S3/S6 application prescriptions are clearly labeled application-time material and are excluded by REVISION 9(2).
- The row-A arithmetic is correct: corrected \([6,6]\), compiled \([12,6]\).
- The proposal contains three `σ.w → σ.wPrev` replacements; earlier “four-token” language is expressly superseded.
- R2’s repaired any-\(\gamma'\) branch is logically correct.
- The support meter totals 336, and the battery rows total 1,093.
- R1 and R4 are properly rescoped; the edited-render/raw-order distinction is disclosed.
- PIN-GEN-i states its single-extractor and shared-state limitations.
- No mathematical contradiction was found in the diagnosis, source-fires verdict, branch argument, or D0 source degeneracy.

FGMN-P11 FINDINGS: 1 critical, 2 gaps  
VERDICT: CRITICAL