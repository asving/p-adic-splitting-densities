# DCX3_SHARP — sealed preregistration: the TWO-LEVEL CARRY CHAIN as the PRIMARY sharp-DCX₃ candidate, confirmed or refuted on FRESH data (closure program; the gate between the RM-2 probe's measured discovery and the engine compose's consumption of it)

Runner of record: `verification/openmath/dcx3_sharp_seal.py` (sealed with
this note at commit 1, run AFTER the seal; verdict = commit 2 from
artifacts). This is the standard measured-law-to-sealed-law promotion:
the RM-2 probe's post-run separation produced a DISPLAY-ONLY candidate
(fit 102/102 on the probe's own roster, separated AFTER the run, so it
has never faced a sealed prediction on data it has not seen); this unit
seals it as the PRIMARY candidate on a fresh roster. A violation of any
sealed leg is a probe SUCCESS (the violating tuple is the unit's
product, reported prominently). One resealed candidate cycle is allowed
within the unit if a sealed RED demands a corrected candidate.

## S0. Authority, provenance, and consumption

Charter: THE CLOSURE PROGRAM (ROOT §44.(ii)) Q2-adjacent gate opened by
the RM-2 adjudication (ced7dbb, BRIDGE_ADJUDICATIONS append): "the DCX₃
two-level carry chain needs its own SEALED CONFIRMATION before the
compose consumes it." Parent artifacts, consumed read-only and
byte-untouched: `RM2_PROBE_2026-08-08.md` (the discovery + the S4.7
refutation bracket at ITERLAWN), `rm2_probe.py` (Engine2/anchor2/
digit_split/monomials consumed VERBATIM as the harness face),
`rm2_postrun_separation.py` (the candidate's exact implementation,
whose formulas are transcribed 1:1 into the sealed runner), plus the
underlying sealed harness chain (`grb_order2_check.Tower`,
`strata3_probe.Tower3`, `iterlawr_probe` pick_irr3 shim,
`iterlaw4_probe.Tower4/Composite4`). Class pin: the 4-read
standard-lift harness class restricted to g₂ = 1 (the RM-2 setting).

## S1. THE CANDIDATE UNDER SEAL (primary; sealed verbatim)

On a g₂ = 1 tower with splits (s₄,s₃,s₂,s₁,b₃,b₂,b₁,u₁) =
Composite4.split(γ), anchor parts â = π^{u₁}Φ₀^{s₁}Φ₁^{s₂}, IH exports
E₁ᴵᴴ := quo(â·â′, Φ₂), and Ĉ₂ := Φ₃ − Φ₂^{e₂}:

    TRACK(γ,γ′) := s₃ = s₃′ = e₂−1  ∧  E₁ᴵᴴ ≠ 0
    lvl2 := (s₂+s₂′) + s₂(γ₃),   pool := s₁+s₁′+s₁(u₀),
    u₀ := (γ₃ − s₂(γ₃)·γ₂)/e₁    [the level-1 weight of Ĉ₂'s Φ₁-digit;
                                  s₁(u₀) := eq12(u₀, e₀, h₀).s]

    DCX₃ᶜᵃⁿᵈ := TRACK ∧ [ lvl2 ≥ 2e₁                    (direct level-2)
                          ∨ (lvl2 = 2e₁−1 ∧ pool ≥ e₀)  (chained level-1) ]

The MEASURED locus it must match, operationalized VERBATIM as in the
RM-2 probe (the [ILN]† S4.7 DCX_m definition at m = 3):

    DCX₃ᵐᵉᵃˢ := E₁ᴵᴴ ≠ 0 ∧ Q¹ := quo(E₁ᴵᴴĈ₂, Φ₂) ≠ 0
                ∧ [ (δ₃ᵖˡ = 1 ∧ s₃ = s₃′ = e₂−1) ∨ (T₃ = e₂−1 ∧ e₂ = 1) ].

Equivalence on record (sealed reasoning): for every e₂ the locus clause
of DCX₃ᵐᵉᵃˢ is exactly TRACK's s₃-clause (at e₂ ≥ 2, s₃ = s₃′ = e₂−1
forces δ₃ᵖˡ = ⌊(2e₂−2)/e₂⌋ = 1 and conversely; at e₂ = 1 both clauses
are vacuous-true), so DCX₃ᵐᵉᵃˢ = TRACK ∧ Q¹ ≠ 0 and F-SHARP is a
genuine two-sided test of the bracketed carry-chain condition on TRACK
plus emptiness off TRACK. u₀-integrality is a design cert (γ₂ ≡ h₁
mod e₁ makes γ₃ − s₂(γ₃)γ₂ ≡ 0 mod e₁ by eq12).

**Candidate provenance and its evidentiary hole (disclosed).** The
102/102 fit covered ONLY g₀ = 1 ∧ d₀ = 1 towers (RM2A/B/G). RM2C
(g₀ = 2, d₀ = 2) was DCX₃-live in the RM-2 run but never fitted.
Design-phase intelligence, on record: replaying the OLD RM2C artifacts
(previous unit's data, already unsealed) against the candidate gives
4/4 on its track rows — the literal e₀ threshold (NOT e₀g₀) fits the
one live row (75,75) at pool = 2 = e₀ < e₀g₀ = 4. On that ground the
primary is sealed AS STATED with no g-normalized secondary; fresh
g₀ = 2 rows (DS7) are in-scope stress, and F-SHARP is reported split
CORE (g₀ = 1 ∧ d₀ = 1) / EXT (g₀ ≥ 2 ∨ d₀ = 2) under one verdict.

## S2. Fresh roster, design disclosures

**Freshness:** all nine (d₀,r₀,r₁,r₂,r₃) tuples are disjoint from the
RM-2 probe's seven; new e-values e₁ = 4, e₂ = 3, e₂ = 4, e₀ = 4, e₀ = 1
(with d₀ = 1: outside the fenced DEV-1 corner), e₁ = 1; fresh h-tracks
(h₀ = 3, h₂ = 3, h₁ = 2/e₁ = 3); fresh prime p = 5 on DS1; both ring
families (Zp on all, Fpt2 on DS1/DS4/DS5). Deterministic full W₁×W₁
blocks; no sampling, no seed (the unit's "own seed" = its own disjoint
roster + windows; the harness class is deterministic).

| tag | d₀ | r0 | r1 | r2 | r3 | rings | why |
|---|---|---|---|---|---|---|---|
| DS1 | 1 | (2,3,1) | (2,1,1) | (2,3,1) | (1,1,1) | Zp5, Fpt2 | all-e=2 fresh h-track + fresh p; at e₁=2 every live row is chained (boundary-only shape) |
| DS2 | 1 | (3,1,1) | (4,1,1) | (2,3,1) | (1,1,1) | Zp3 | e₁=4 NEW; s₂(γ₃)=3: direct AND boundary rows; E4=24 |
| DS3 | 2 | (2,1,1) | (3,2,1) | (4,1,1) | (1,1,1) | Zp3 | d₀=2 junk-alive + ℓ₁=2 carry geometry + e₂=4 NEW; E4=24; EXT row |
| DS4 | 1 | (4,1,1) | (2,1,1) | (3,1,1) | (1,1,1) | Zp3, Fpt2 | e₀=4 pool threshold; e₂=3 NEW; boundary-rich; E4=24 |
| DS5 | 1 | (3,2,1) | (3,1,1) | (1,2,1) | (2,1,1) | Zp3, Fpt2 | e₂=1 track-everywhere + e₃=2; s₂(γ₃)=2: direct+boundary; E4=18 |
| DS6 | 1 | (1,1,1) | (2,1,1) | (2,1,1) | (1,1,1) | Zp3 | e₀=1 degenerate pool: chain clause CANNOT fire; boundary rows predicted all dead |
| DS7 | 1 | (2,1,2) | (2,1,1) | (2,3,1) | (1,1,1) | Zp3 | g₀=2 with d₀=1: the unfitted g-pattern, d₀-channel isolated; EXT row |
| DS8 | 2 | (2,1,1) | (2,1,2) | (3,1,1) | (1,1,1) | Zp3 | g₁=2 negative control (E₁ᴵᴴ structurally 0) with the d₀=2 junk channel free to break that; EXT row |
| DS9 | 1 | (2,1,1) | (1,1,1) | (2,3,1) | (1,1,1) | Zp3 | e₁=1 degenerate: lvl2 = 0 < 2e₁−1, predicate dead everywhere; TRACK reachable only by promotion |

**Design disclosures (pre-seal `--design` run, `dcx3_sharp_design.json`;
IH-side censuses/meters ONLY — no Q¹, no DCX₃ᵐᵉᵃˢ, no sealed leg, no
trace).** (a) All 12 tower-runs pass every design cert: g₂ = 1, ψ₂
monic deg 1, Ĉ₂ (Ĉ1) deg/(Ĉ2) w₂ = γ₃/(Ĉ3) read = τ₂, level-1+level-2
identity replays byte-exact, u₀ integral ≥ 0. (b) Coverage meters
(F-BOUNDARY, design-verified): M1 (track rows at lvl2 = 2e₁−1 exactly)
= 224 ≥ 20; M2 (boundary rows at pool = e₀ exactly) = 52 ≥ 20; direct
rows (lvl2 ≥ 2e₁) = 103 (DS2 27, DS3 4, DS5 36×2 — the direct clause
faces fresh data too). (c) Track censuses: DS8 track = 0 (E₁ᴵᴴ ≡ 0 at
g₁ = 2, as structurally expected — the row stays as a both-sides-empty
control), DS9 track = 1 (a promotion-reached E₁ᴵᴴ ≠ 0 row at e₁ = 1 —
the predicate says dead; the run decides), DS6 boundary = 1 with
pool_eq_e0 = 0 (chain degenerate). (d) Mutation visibility
(mut ≠ primary on track rows, design-computable): MC-1 94, MC-2 313,
MC-3 52, MC-4 4 — ALL visible; MC-4's visibility rides DS3 (s₁(u₀) = 1
vs mutant 0; on all other shapes the mutant coincides, disclosed).
(e) s₁(u₀) values roster-wide: 1,2,1,3,2,0,1,0,1 (DS1..DS9) — the u₀
term is load-bearing on seven shapes (vs 0/0/1 on the RM-2 roster:
fresh leverage on the chain's third summand). (f) Predicted-live counts
(prediction side only): 3/3/51/10/15/15/100/100/0/3/0/0 per run in
roster order. (g) Cost: design 0.2 s; sealed battery estimate < 60 s.

## S3. Preregistered falsifier legs, predictions, controls

All legs LAW-KEYED to the candidate; predicted violation count 0 on
every leg unless marked; RED on any leg = probe SUCCESS.

**F-SHARP** (every pair of every W₁×W₁ block, 3320 pairs/12 runs):
DCX₃ᶜᵃⁿᵈ == DCX₃ᵐᵉᵃˢ pairwise — BOTH inclusions. Reported split
CORE/EXT per S1; one verdict line. Predicted 0.

**F-BOUNDARY** (coverage, design-verified): M1 ≥ 20 and M2 ≥ 20
realized in the sealed run (deterministic, so = design values 224/52).
Meter line, MET/NOT-MET.

**F-OUTER**: every DCX₃ᵐᵉᵃˢ row satisfies the PROVED outer bound
{g₁ = 1 ∧ s₃ = s₃′ = e₂−1}. Predicted 0.

**F-CHAIN** (the u₀ mechanism, the unit's trace charge): instrument =
the clean/dirty two-stage reduction tree (`TraceTree`): stage 1 reduces
â·â′ by the E1-priority tree (E1: Φ₀^{e₀g₀} → promotion child + lifted
DIGIT-SPLIT children; E2: Φ₂-exit + C_k children), stage 2 reduces
E₁ᴵᴴ·Ĉ₂ with the stage-1 quotient's clean/dirty parts inherited; DIRTY
= descended through ≥ 1 E1-promotion edge (the split is exact linear
bookkeeping, so clean+dirty reassembles the canonical division). On
every DCX₃ᵐᵉᵃˢ-live pair (dedup by (u₁+u₁′, s₁+s₁′, s₂+s₂′); e₀ ≥ 2
scope — no live row is predicted at e₀ = 1, and a live row there is
already an F-SHARP RED):
  (C1) both stages complete under the pop cap and reassemble the
       canonical divisions exactly (tree == divmod at both stages).
       Predicted 0.
  (C2) on every BOUNDARY-live pair (lvl2 = 2e₁−1): Q¹_clean = 0 ∧
       Q¹_dirty ≠ 0 ∧ (promo₁+promo₂) ≥ 1 — i.e. the ENTIRE overflow
       is promotion-fed: the missing +1 Φ₁-exponent genuinely rides the
       E1-promotion edge. Predicted 0.
  (C3, census only, NO prediction): the clean/dirty split of Q¹ on
       DIRECT-live pairs (how often the direct route is clean-visible).

**Mutation controls (teeth; verdict SEPARATE from the law; one control
per constant of the candidate; design visibility per S2(d)).** Trip :=
mutated predicate ≠ DCX₃ᵐᵉᵃˢ on ≥ 1 pair. MC-1: direct threshold
2e₁ → 2e₁+1. MC-2: the boundary −1 → −2 (lvl2 = 2e₁−2). MC-3: pool
threshold e₀ → e₀+1. MC-4: the u₀ formula → u₀ᵐᵘᵗ := ⌊γ₃/e₁⌋ (the
γ₂-correction dropped). A control NOT tripping = CONTROL FAILURE,
reported separately; never colors the law verdict.

**Verdict lines (pre-agreed wording):** SHARP-LOCUS VERDICT (F-SHARP
both-inclusions, sealed primary, predicted-0) · OUTER VERDICT · CHAIN-
MECHANISM VERDICT (C1+C2) · BOUNDARY COVERAGE · CONTROL VERDICT.

## S4. What this unit does NOT claim (fences)

1. GREEN everywhere promotes the candidate to SEALED-CONFIRMED ON THIS
   CLASS — instance evidence at conjecture grade, consumable by the Q1
   engine compose as the DISPLAYED second-round clause, but NOT a
   proof; the (W-MULT-DCX-m) family stays open until a hostile-arc
   proof lands.
2. F-CHAIN measures the CANDIDATE TREE's mechanism; C-leg RED refutes
   the promotion-edge story (the compose design input), not the
   predicate itself (F-SHARP owns the predicate).
3. No claim at g₂ ≥ 2, at m ≥ 3 levels, or beyond the probed window
   blocks; the fenced e₀ = 1 ∧ d₀ = 2 DEV-1 corner stays fenced (DS6
   is e₀ = 1 ∧ d₀ = 1, outside it).
4. The harness is sealed ground truth; any harness-level assertion
   failure is a HARNESS EVENT (reported, run aborted), not a law
   verdict.

## S5. VERDICT (commit 2, from the artifacts; seal = 1db30fa; run
`dcx3_sharp_output.txt` + `dcx3_sharp_results.json`, 0.3 s, deterministic)

**THE VERDICT LINES (transcript tail, verbatim):**

    SHARP-LOCUS VERDICT (F-SHARP both-inclusions, sealed primary, predicted-0): RED (probe SUCCESS) [40 violations]
    OUTER VERDICT (F-OUTER vs the proved bound, predicted-0): GREEN [0 violations]
    CHAIN-MECHANISM VERDICT (F-CHAIN C1+C2, predicted-0): GREEN [0 violations]
    BOUNDARY COVERAGE (F-BOUNDARY, M1>=20 & M2>=20): MET [M1=224, M2=52]
    CONTROL VERDICT (mutation teeth MC-1..4, SEPARATE): MET

**THE RED — THE UNIT'S FINDING (40 F-SHARP violations = the ENTIRE
deficit-2 stratum; every mismatch is meas = True, pred = False — the
RM-2 candidate is again too SMALL, same failure direction as its own
discovery).** The violations are EXACTLY the track rows with

    lvl2 = 2e₁ − 2   ∧   pool ≥ 2e₀      (40/40; zero rows outside it)

— DS2/Zp3 4 rows (pool = 6 = 2e₀, e₀ = 3), DS4 6 rows × BOTH rings
(pool ∈ {8,9} ≥ 8 = 2e₀, e₀ = 4), DS5 12 rows × BOTH rings (pool =
6 = 2e₀, e₀ = 3). Headline tuple: (DS4/ℤ₃, γ = 248, γ′ = 251):
s₃ = s₃′ = 2, s₂ = (0,1), s₁ = (3,3), pool = 9, lvl2 = 2 = 2e₁−2 —
LIVE with TWO carries, cross-ring confirmed at F₂[[t]]. The RM-2
candidate's chained clause caps the level-1 carry at ONE promotion;
the fresh deep-pool shapes (s₁(u₀) ∈ {2,3}, e₀ ∈ {3,4} — unreachable
on every RM-2 roster shape, where pool < 2e₀ held identically) expose
the second carry. GREEN detail: F-SHARP held on 3280/3320 pairs incl.
ALL deficit-1 boundary rows (197 live, DS7's g₀ = 2 rows at literal
e₀ included — the g-pattern extrapolation HELD), all EXT rows, both
degenerate shapes (DS6 e₀ = 1 boundary row dead as predicted; DS9's
promotion-reached e₁ = 1 TRACK row dead as predicted); F-OUTER 0/340
live rows; F-CHAIN C1 0/340 traces, C2 0/197 boundary-live (Q¹_clean
= 0 ∧ Q¹_dirty ≠ 0 ∧ promo ≥ 1 on every one). The C3 census is the
mechanism smoking gun: the 143 non-boundary live rows split into
103 = ALL genuinely-direct rows (lvl2 ≥ 2e₁) with Q¹_clean ≠ 0
(clean-visible) and 40 = EXACTLY the deficit-2 violations with
Q¹_clean = 0 (promotion-fed only). Controls: MC-1 134, MC-2 273,
MC-3 92, MC-4 44 — all tripped.

**POST-RUN SEPARATION (display-only, NOT sealed; run-1 data).** The
measured locus on ALL 3320 fresh pairs is EXACTLY (zero misfits,
verified stratum-by-stratum against the census + violation lists) the
UNIFORM CARRY LAW

    DCX₃ᵛ² := TRACK ∧ lvl2 + ⌊pool/e₀⌋ ≥ 2e₁

(v1 is v2 with the carry capped at 1; they differ only on deficit-2
rows, absent from every earlier roster; deficit ≥ 3 is arithmetically
unreachable — pool ≤ 3(e₀−1) < 3e₀ — so v2 predicts ALL lvl2 ≤ 2e₁−3
rows dead, a falsifiable fresh prediction). v2 also refits the RM-2
102/102 and the RM2C replay unchanged. Reading: the DCX₃ boundary is
decided by the TOTAL level-1 carry budget ⌊(s₁+s₁′+s₁(u₀))/e₀⌋ — the
same Φ₀-pool promotion edge, iterated. THE RESEAL CYCLE (S6) seals v2
as the corrected primary on a second fresh roster.
