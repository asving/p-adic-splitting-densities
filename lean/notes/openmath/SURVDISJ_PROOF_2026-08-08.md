# (SURV-DISJ) — the survival disjunction: PROVED at attempt grade on the g₀ = 1 ∧ d₀ = 1 locus via the E-weighted mass ledger ((NEP) discharged from hypothesis to THEOREM there — the cascade slot always survives); the g₀ ≥ 2 regime boxed as the signed-multiplicity-one law (SURV-K2) with its full proved perimeter; the d₀ ≥ 2 Φ₀Y-corner boxed as (SD-YJ)

Object: the boxed conjecture (SURV-DISJ) of SURV_PROOF_2026-08-08.md S7
(at 51a8ac9), the sharp falsifiable sub-form of (SURV-RES-(m+1)):

    (SURV-DISJ):  on live rows (TRACK, (H1)–(H3), c_{m−1} ≥ k) the
    netted BOTTOM stratum survives netting OR the CASCADE slot survives
    at the cascade line.

**Verdict, stated first.**  (SURV-DISJ) is PROVED at attempt grade on
the locus (H1)–(H3) ∧ g₀ = 1 ∧ d₀ = 1 ∧ (STK) ∧ (c_{m−1} ≥ k) — by the
CASCADE leg, unconditionally on that locus (not merely on kill rows):
the cascade slot ĵ always survives netting at the cascade line β̂
(Theorem SD-THM-A).  The mechanism is the instrument's (K1) protection
identity, proved: an E-weighted mass ledger over the W-D3 path ledger
(SD-L2) forces every would-be cascade-slot companion's correction
economy to balance exactly, while every correction edge at g₀ = 1
strictly under-returns mass unless it rides a Φ₀Y-junk edge (SD-L1) —
and at d₀ = 1 the Φ₀Y-junk child is IDENTICALLY ABSENT (L-NORM(c),
accepted).  Hence (NEP) — until now an instance-only hypothesis
(379/379 + 693/693) — is a THEOREM on this locus, and S-THM(c) closes
(SURV-(m+1))(i) there outright under (RW).  With (EMPTY-(m+1)) and
W-D4 this puts LAW-DCX-(m+1) on the pinned class
(H1)–(H3) ∧ g₀ = 1 ∧ d₀ = 1 ∧ (STK) ∧ (RW) at the capped grade — the
first closure of the full law on any m ≥ 2 class (S8).

What is NOT proved: the g₀ ≥ 2 regime (K2).  Its perimeter is proved
here — correction-free rigidity SD-L5 (kills can never involve two
correction-free junk-free paths), the group survival criterion SD-L6,
and the protected-vector survival lemma SD-L7 (the instrument's
signed-multiplicity-one law reduced to field distinctness) — but the
existence of a protected bottom-line group at g₀ ≥ 2 is OPEN, boxed as
(SURV-K2) at S6 with the sign-reversing-pairing proof target and the
full instrument constraint census.  Also open: the g₀ = 1 ∧ d₀ ≥ 2
corner, boxed as (SD-YJ) — there the ledger admits Φ₀Y-junk-fed
companions with an exact deficit equation, never probed by any battery
(no g₀ = 1 ∧ d₀ ≥ 2 tower exists in any committed roster).

Scope: the [ILN]† S0.1 class via the RM-GEN S0.1 pin, level m ≥ 2,
g_m = 1.  Date: 2026-08-08 campaign (wallclock 2026-08-04; composer
unit of the W-MULT-DCX arc, the (SURV-DISJ) leg, on the two-commit
instrument seal 61e9e36/00e19b4).

**Grade (header summary) and THE GRADE-CAP SENTENCE.**  SD-L1–SD-L7
and SD-THM-A — PROVED at attempt grade (composed this unit; 0 hostile
passes yet).  **Grade cap carried from consumption [dagger
discipline]: this note consumes WMULTDCX_2026-08-08.md† (W-D1, W-D2,
W-D3, W-D4, W-D5, W-D7) at ATTEMPT grade — that arc stands at 0/2
CLEAN, not accepted — and SURV_PROOF_2026-08-08.md† (S-L0, S-L1,
S-L3, S-L4(i)(ii), the S-L4(iii) uniqueness half, S-THM) at ATTEMPT
grade (round 1 GAPS-ONLY repaired, 0 CLEAN passes) — and
EMPTY_PROOF_2026-08-08.md† (E-L6, LOAD-BEARING this time, unlike the
notation-only consumption at SURV_PROOF; plus E-THM in the S8
consequence display only) at ATTEMPT grade (1/2 CLEAN); so every
result here is capped at attempt grade by the weakest of these (the W
arc, 0/2) until those arcs close.  RMENGINE [RMG] is ACCEPTED
(round-7 double-clean): L-NORM(a)(b)(c), the S2.2 child inventory
(correction children: dump exponents a_{d,l} ≤ P_l − 1 displayed
there), L-CELL(i), and the S0.2 weight kit carry no cap.  L-ZMULT,
L-VNZ, L-LINE (named in the charge's pin list) are NOT load-bearing
here: L-LINE enters only through the already-consumed S-L0/W-D5;
L-VNZ only as S-L1's cited mechanism template; L-ZMULT not at all.
E-L8 likewise: consulted, not consumed.**  (SURV-DISJ) class-wide —
OPEN (the two boxes).  LAW-DCX-(m+1) class-wide — UNCHANGED
conjecture; on the S8 pinned class — holds at the capped grade.

## S0. Authority, provenance, consumption, machine leg

Charge (the orchestrator's composer charge): prove (SURV-DISJ) — on
live rows at the pins, the bottom or the cascade slot survives — or
box the exact obstruction with everything proved around it.  Design
input: the (SURV-DISJ) INSTRUMENT verdict (two-commit seal
61e9e36/00e19b4; note SURVDISJ_INSTRUMENT_2026-08-08.md): 979 live
rows, 0 violations in nine families; protection identities FOUND —
(K1) = combinatorial one-path protection at g₀ = 1 (P-A 693/693, no
same-slot cascade companion of ANY valuation), (K2) = the
signed-multiplicity-one law at g₀ ≥ 2 (PB-mult1-some 286/286; on all
200 kill rows every minimal survivor's class-count vector in
{(+1), (−1), (−1,0), (0,+1), (−1,+1)}).  This unit proves (K1)'s
identity and the reduction half of (K2)'s, and boxes (K2)'s existence
half.  Instrument and composer are decorrelated (separate contexts;
the instrument's preregistered PRED-1..7 sealed before its battery).

Consumption, all read-only, per-block grades as in the header cap
sentence: **WMULTDCX†**: W-D1 (b₀ = π^{u₁+u₁′}, the formal-cell seed
identity), W-D2(iii)/(iv) (d-cell inventory: unique principal at
(H3) ∧ g₀ = 1; the t-inventory j₀(D) = s₁(u₁) + t·e₀,
w₀(b_D) = u₀ − t·h₀ at g₀ ≥ 2), W-D3 (LED_l — THE parent of SD-L2),
W-D4 (top identity), W-D5 (line law; junk strictly above), W-D7
(cascade existence/profile under (STK) ∧ clause).  **SURV_PROOF†**:
the slot/netting language and S-L0 (weight kit), S-L1 (cascade slot
content: slot ĵ = (ρ₁, …, ρ_{m−1}, c_{m−1}−k), line β̂, coefficient
π^ŵ·lift(b̄₀c̄_D), residue ≠ 0, unique d-cell), S-L3 (cross-slot
independence), S-L4(i) (equal slot+line ⟺ equal w₀; line =
E_m·w₀ + Σ j_lΓ_l), S-L4(ii) (higher-w₀ slot-mates never disturb),
S-L4(iii)'s uniqueness half (the cascade is the unique
correction/junk/Y₂-free two-exit path at (H3) ∧ g₀ = 1), S-THM(c).
**EMPTY_PROOF†**: E-L6 (the Φ₀Y-junk child of a monomial-coefficient
seeding is identically absent — deg(b̂ĉ) = deg ĉ < d₀ — plus its
"why b stays monomial without corrections" leg); E-THM only in S8.
**RMENGINE [RMG], accepted**: L-NORM(a) (L-child exact valuation +
residue product, never zero), L-NORM(b) (π-junk child w₀ ≥ w + 1),
L-NORM(c) (Φ₀Y-child ABSENT IDENTICALLY at d₀ = 1: deg(b̂ĉ) ≤
2d₀ − 2 = 0 < d₀), the S2.2 child inventory (promotion j_i −= P_i /
j_{i+1} += 1 with b unchanged; exit consumes P_{m−1}; correction
(k, d) at level i: consumes P_i − e_ik, dumps the REDUCED digit-cell
exponents a_{d,l} ≤ P_l − 1 at l < i, +1 at level 0 on the Y-child,
sign −; the i = 0 form with no dump vector), L-CELL(i).  [IL3]†/
[ILN]† ride through these blocks as at SURV_PROOF S0.  Sealed
artifacts and the instrument's censuses are instance evidence and
design input only, never proof steps.

**Machine leg of THIS unit (the derivation shadow):**
`verification/openmath/survdisj_derive_checks.py`
(md5 3eaf69c08f9a5b278826102610d4aa22), output
`survdisj_derive_checks_output.txt`
(be088ed43e57569cbc508082dc2d15b8), results
`survdisj_derive_checks_results.json`
(868f71903d53955418435c2377026723).  Single deterministic 24.5 s run,
exact arithmetic, engines md5-pinned byte-untouched (the instrument's
seven pins PLUS the instrument runner itself, 3df33c7406913510…,
whose union-roster selection is replicated verbatim — the same 979
live rows).  Ten verdict families, **0 violations**; every displayed
lemma below carries its family.  Summary at S7.  It is a
derivation shadow (checks THIS note's displayed steps), not a re-run
of the instrument's candidate battery.

## S1. Setting and the two regimes

Notation: WMULTDCX S1 / SURV_PROOF S1 verbatim (T², counters π_l,
r, dmp_l, cons_l, junk, Y₂; pools, carries c_l, remainders
ρ_l = pool_l − e_{l−1}c_l; D, k := 2e_{m−1} − D; slots, netting,
B_{j⃗}, β_min; the cascade slot ĵ := (ρ₁, …, ρ_{m−1}, c_{m−1} − k) at
line β̂ := λ̂ + Σ c_l·q_l with single-cell coefficient
π^ŵ·lift(b̄₀c̄_D), ŵ := u₁ + u₁′ + u₀ — S-L1).  Standing hypotheses
(H1)–(H3) exactly as there; c_{m−1} ≥ k ⟺ the law clause
D + c_{m−1} ≥ 2e_{m−1}.  Define throughout

    E_0 := 1,   E_l := e_0·e_1⋯e_{l−1}   (so E_m = e_0⋯e_{m−1}),
    M(x⃗)  :=  Σ_{l=0}^{m−1} x_l·E_l      (the E-weighted mass of an
                                          exponent vector).

The kill regimes on record (instrument D3): (K1) mod-2
path-multiplicity pair-kills of the bottom at g₀ = 1 (six rows, all
residue characteristic 2; cascade survived every one); (K2)
characteristic-blind opposite-sign pair-kills of bottom-line groups
at g₀ ≥ 2 (97 bottom SIGN-PAIR kills through p = 5; the bottom
survived elsewhere on every one).  This note closes (K1)'s locus by
proving the cascade leg and boxes (K2)'s.

## S2. SD-L1 + SD-L2: the mass ledger (the (K1) protection identity)

**Lemma SD-L2 (the E-weighted mass ledger; any g₀).**  For every
two-exit terminal cell C of T², with seed d-cell exponent vector
j⃗(D), Y₂(C) ∈ {0,1} the stage-2 seeding Φ₀Y-flag, CE(C) its
correction-edge multiset (each c at reduction level i_c, index k_c,
dump vector a⃗_c, Φ₀Y-flag Y_c ∈ {0,1}):

    M(j⃗(C)) = M(σ⃗) + M(j⃗(D)) + Y₂(C)
               − 2·P_{m−1}E_{m−1}
               − Σ_{l=1}^{m−1} π_l·(P_{l−1}E_{l−1} − E_l)
               − Σ_{c ∈ CE(C)} defc_c ,

    defc_c := (P_{i_c} − e_{i_c}k_c)·E_{i_c} − Σ_{l<i_c} a_{c,l}E_l − Y_c .

*Proof.*  Multiply LED_l (W-D3) by E_l and sum over l = 0..m−1.  The
promotion terms telescope into the displayed loss sum (each promotion
into level l gains E_l and consumes P_{l−1}E_{l−1} at level l−1); the
two exits consume P_{m−1}E_{m−1} each; each correction consumes
(P_i − e_ik)E_i and returns its dump mass Σ_{l<i}a_lE_l plus 1 at
level 0 if taken via the Y-child; the stage-2 seeding contributes
M(j⃗(D)) plus 1 at level 0 on its Y-child; X-junk (π-junk) edges move
no exponents.  All edge moves are the [RMG] S2.2 inventory verbatim.
∎  [Machine: DRV-MASS — 3,606/3,606 two-exit cells across all 979
live rows, both g₀ classes, 0 violations.]

**Lemma SD-L1 (strict dump deficit at g₀ = 1).**  Assume (H2), (H3),
g₀ = 1 (so P_l = e_l for all 0 ≤ l ≤ m−1 and every correction index
k_c = 0, g_{i_c} = 1 having no other).  Then every correction edge
has

    defc_c ≥ E_{i_c+1} − E_{i_c} + 1 − Y_c ≥ 0 ;

in particular defc_c ≥ 1 whenever Y_c = 0, and defc_c = 0 forces
e_{i_c} = 1 ∧ Y_c = 1 ∧ full principal dump (a_{c,l} = P_l − 1 for
every l < i_c).
*Proof.*  Consumption is P_{i}E_{i} = e_iE_i = E_{i+1} exactly.  The
dump exponents are reduced ([RMG] S2.2: a_{d,l} ≤ P_l − 1 for l < i),
so Σ_{l<i} a_{c,l}E_l ≤ Σ_{l<i}(e_l − 1)E_l = Σ(E_{l+1} − E_l) =
E_i − 1 (mixed-radix telescope; at i = 0 the empty sum is 0 = E_0 − 1
consistently — the i = 0 correction children carry no dump vector at
all, S2.2).  Hence defc ≥ E_{i+1} − (E_i − 1) − Y_c.  ∎  [Machine:
DRV-DEF — 2,167 correction edges on g₀ = 1 rows, 0 violations; the
bound is SHARP: minimal observed deficits 1, 2, 3, 4 at
e_{i} = 1, 2, 3, 4 respectively, all with Y_c = 0.]

**The slot-mass identity (★).**  At (H2) ∧ (H3) ∧ g₀ = 1 ∧ (STK) ∧
(c_{m−1} ≥ k) the promotion-loss sum vanishes (all P = e) and the
cascade slot's mass obeys the pool telescope

    M(ĵ) = Σ_{l=1}^{m−1} ρ_l E_{l−1} + (c_{m−1}−k)E_{m−1}
         = Σ_{l=1}^{m−1} (σ_l + s_l(u_l))E_{l−1} − c_{m−1}E_{m−1}
           + (c_{m−1}−k)E_{m−1}
           [ρ_l = σ_l + s_l(u_l) + c_{l−1} − e_{l−1}c_l with c_0 := 0;
            the carry sums Σ c_{l−1}E_{l−1} − Σ c_lE_l telescope to
            −c_{m−1}E_{m−1}]
         = Σ_{l=1}^{m} (σ_l + s_l(u_l))E_{l−1} − D·E_{m−1} − k·E_{m−1}
           [added and removed the top term; D = σ_m + s_m(u_m)]
         = M(σ⃗) + M(j⃗_prin) − 2E_m
           [k = 2e_{m−1} − D and e_{m−1}E_{m−1} = E_m].

Since the seed is the UNIQUE principal
d-cell at (H3) ∧ g₀ = 1 (W-D2(iii)/S-L1: j⃗(D) = j⃗_prin for every
path), SD-L2 gives, for ANY two-exit terminal C with j⃗(C) = ĵ:

    (★)      Σ_{c ∈ CE(C)} defc_c  =  Y₂(C)  ∈ {0, 1}.

A cascade-slot companion's whole correction economy must balance to
at most one unit of Φ₀Y-seed mass.

## S3. SD-L3 + SD-L4: where Φ₀Y-junk can and cannot exist

**SD-L3 (Φ₀Y-vanishing at d₀ = 1) — consumed, not composed: this is
L-NORM(c) verbatim ([RMG], accepted).**  At d₀ = 1 every cell
coefficient has x-degree 0, every coefficient product has
deg(b̂ĉ) ≤ 2d₀ − 2 = 0 < d₀, and the Φ₀Y-part of EVERY L-NORM split
in T² is identically zero: no Φ₀Y-junk edge exists anywhere — at any
correction and at any seeding, any g₀.  Hence Y₂ ≡ 0 and Y_c ≡ 0 on
every path.  [Machine: DRV-Y0 — 3,274 cells on d₀ = 1 towers, zero
Φ₀Y-flags, 0 violations; teeth: CTRL-Y fired 60 on the two d₀ = 2
towers — Φ₀Y-edges are real where the degree argument does not
apply.]

**Lemma SD-L4 (the b₀-seeding: Y absent at any d₀; the X-mate
shape).**  On a two-exit path whose stage-1 prefix takes no
correction edge, the stage-1 exit coefficient is b₀ = π^{u₁+u₁′}
verbatim (W-D1 + [RMG] S2.2 b-unchanged — the E-L6 "why b stays
monomial" leg), and the stage-2 seeding split of b₀·c_D has:
(i) Φ₀Y-child IDENTICALLY ZERO at ANY d₀ (E-L6: deg b̂₀ = 0 forces
deg(b̂₀ĉ_D) = deg ĉ_D < d₀);
(ii) L-child never absent, with w₀ exactly ŵ and residue
b̄₀c̄_D ≠ 0 (L-NORM(a) — the S-L1 content);
(iii) π-junk X-child, IF nonzero, with w₀ ≥ ŵ + 1 (L-NORM(b)) and
the SAME exponent vector as the L-child.
*Proof.*  All three are the cited displays applied to b = b₀, c = c_D.
∎  [Machine: DRV-SEEDY — 1,355 b₀-seeding splits recomputed directly
(every d-cell on every live row), Y-child zero on ALL, 0 violations.
Measured, beyond the proof's needs: the X-child was ALSO zero on all
1,355 (counter seedx_zero; seedx_nonzero = 0) — instances realize
the stronger exactness, which is why the instrument saw NO cascade
companion of any valuation; the proof below does not consume it.
Teeth for the X-genre: CTRL-XSEED fired 388 (junk-carrying cells are
real elsewhere — X-junk arises at correction products).]

## S4. SD-THM-A: the (K1) theorem — the cascade slot always survives

**THEOREM SD-THM-A.**  On TRACK, (H1)–(H3), g₀ = 1, d₀ = 1, (STK),
c_{m−1} ≥ k:

* **(i) [NEP-strong up to X-mates]**  Every two-exit terminal cell at
  the cascade slot ĵ is either the cascade cell Ĉ or an "X-mate": a
  correction-free cell descended from the X-child of the b₀-seeding,
  with the same exponent vector, junk ≥ 1, and w₀ ≥ ŵ + 1 (at most
  one such).
* **(ii) [(NEP) — now a THEOREM]**  The cascade slot's equal-line
  population at β̂ is the singleton {Ĉ}: the cascade has NO equal-line
  partner path.
* **(iii)** The cascade slot survives netting at the cascade line:
  B_ĵ ≠ 0 with w₀(B_ĵ) = ŵ and line(B_ĵ, ĵ) = β̂.
* **(iv) [(SURV-DISJ) on this locus]**  The disjunction holds — by
  the cascade leg, whatever the bottom stratum does.
* **(v) [(SURV)(i) on this locus]**  Under (RW) additionally:
  Q¹ ≠ 0.

*Proof.*  (i)  Let C be a two-exit terminal with j⃗(C) = ĵ.  By (★),
Σ_c defc_c = Y₂(C).  At d₀ = 1, SD-L3 kills every Φ₀Y-flag: Y₂ = 0
and Y_c = 0, so Σ_c defc_c = 0 with every term ≥ 1 by SD-L1 — hence
CE(C) = ∅.  A correction-free path has no stage-1 L-NORM event, so
its seeding is b₀·c_D with the unique principal d-cell (SD-L4); the
seeding's children are the L-child and (at most) the X-child — the
Y-child does not exist (SD-L4(i)).  Off the seeding, a
correction-free path has no forks at all: promotion and exit children
are unique per firing and E1-priority makes the firing sequence
deterministic ([RMG] S2.2; the S-L4(iii) uniqueness half is exactly
this display).  So C terminates the L-branch — and is Ĉ — or
terminates the X-branch: an X-mate, whose exponent moves after the
seeding are IDENTICAL to the L-branch's (the walk depends only on
exponents), whose junk is ≥ 1, ce = ∅, and whose w₀ ≥ ŵ + 1
(L-NORM(b)); there is at most one X-branch, hence at most one
X-mate.
(ii)  An X-mate sits at line β̂ + E_m·(w₀ − ŵ) > β̂ (line is affine in
w₀ at fixed slot — S-L4(i)); so the β̂-population at ĵ is {Ĉ}.
(iii)  B_ĵ = b_Ĉ ± b_X (the mate, if present).  w₀(b_X) > w₀(b_Ĉ) =
ŵ, so by S-L0(b) at w₀: B_ĵ ≠ 0 with w₀(B_ĵ) = ŵ, and its line at
slot ĵ is β̂.  (Equivalently: S-L4(ii) — the equal-line subsum at β̂
is the single S-L1 term with residue b̄₀c̄_D ≠ 0.)
(iv)  Immediate from (iii).
(v)  (ii) is (NEP); S-THM(c) applies verbatim with its (NEP)
hypothesis now supplied by theorem: under (RW) the reduced
development of Q¹ is the nonempty list of surviving netted slots
(L-CELL(i)), so Q¹ ≠ 0.  ∎

[Machine: DRV-NEPS — on all 693 g₀ = 1 ∧ d₀ = 1 ∧ (STK) live rows of
the union roster, the cascade slot's population is exactly {Ĉ} —
zero X-mates observed (the sharper instance fact, matching
seedx_zero 1,355/1,355 and reproducing the instrument's P-A
693/693); any companion outside the theorem's X-mate shape would
have been a violation; 0 violations.]

**Scope remarks.**  (a) The d₀ = 1 pin covers the ENTIRE probed
g₀ = 1 class: no battery has ever carried a g₀ = 1 ∧ d₀ ≥ 2 tower
(SURV_PROOF F-C; instrument disclosures) — so the theorem's locus
contains every g₀ = 1 row ever traced, and the excluded corner is
machine-dark, boxed at S6 as (SD-YJ), not contradicted.  (b) (STK)
enters only through the cascade's existence (W-D7(ii)); it held on
every probed row and stays an explicit pin (WMULTDCX F6).  (c) The
six K1 kill rows are consistent instances: the bottom died, the
theorem's cascade leg carried the disjunction — exactly what the
instrument recorded (survivor = cascade slot at cascade line on all
six).  (d) At m = 1 the frame degenerates to the [IL3]† anchor (Q¹ a
single cell); nothing here touches it.

## S5. The g₀ ≥ 2 perimeter: what is PROVED around the (K2) box

**Lemma SD-L5 (correction-free rigidity; any g₀).**  On TRACK,
(H1)–(H3): among two-exit terminal cells taking NO correction edge
and NO junk edge (in particular Y₂ = 0), there is AT MOST ONE per
d-cell seed, and distinct seeds land at pairwise DISTINCT slots.
Hence every same-slot equal-line group of size ≥ 2 contains at most
one correction-free junk-free cell: **a netting kill can never
consume two correction-free junk-free paths** — every collision
partner rides a correction or junk edge.
*Proof.*  Fix the seed D_t (at (H3), j₀(D_t) = s₁(u₁) + t·e₀ with
0 ≤ t < g₀ and all higher exponents principal — W-D2(iv); at g₀ = 1,
t = 0).  A correction-free junk-free path has no forks given its
seed (as in SD-THM-A(i): no L-NORM forks — it takes the L-child of
its seeding — and promotion/exit children are unique, E1-priority
deterministic), so at most one such terminal per seed.  Distinct
slots: by LED_0 with no corrections, no dumps, no Y-edges,
j₀(term) = σ₁ + s₁(u₁) + t·e₀ − P₀·π₁; two such terminals at the
same slot force (t − t′)e₀ = e₀g₀(π₁ − π₁′), i.e. t ≡ t′ (mod g₀),
i.e. t = t′ (both in [0, g₀)), then the same seed hence the same
path.  ∎  [Machine: DRV-RIG — all 979 rows, 0 violations (≤ 1 per
seed AND distinct slots); teeth: CTRL-RIGSEEDS fired 123 (rows with
≥ 2 correction-free junk-free terminals exist — the distinct-slot
clause is exercised); towers with the principal d-cell absent
(θ₀ = 0: SDW3G2F, SDW4G2F, disclosed by the engine census) are
covered — the argument uses only distinctness of t·e₀ mod e₀g₀.]

Reading: with S-L3 (cross-slot cells never cancel), SD-L5 proves the
correction-free junk-free population — the "t-family core" — is
structurally kill-immune: those cells can only die inside a group
against correction-riders.  This derives the anatomy every probed
kill displayed (every collision pair rode ≥ 1 correction).

**Lemma SD-L6 (group survival criterion).**  On TRACK, (H1)–(H2):
let G be a same-slot equal-line group (slot j⃗, line β, size ≥ 1).
All its cells share one w₀ =: w (S-L4(i)).  Then the group's netted
contribution B_G := Σ_{C∈G} sgn_C·b_C satisfies:

    G survives at β  (B_G ≠ 0 with line β)   ⟺
    Σ_{C∈G} sgn_C·res(b̄_C)  ≠  0   in K₀ ,

and when it survives, w₀(B_G) = w exactly; when it dies, B_G = 0 or
w₀(B_G) > w (raised).
*Proof.*  b_C = π^w·b̂_C with res b̂_C = res(b̄_C); Σ± b_C =
π^w·Σ± b̂_C, and reduction mod π is additive, so res(Σ± b̂_C) =
Σ± res(b̄_C).  If ≠ 0: w₀ exact at w, and line = E_m·w + Σ j_lΓ_l = β
(S-L4(i) affinity).  If = 0: w₀ ≥ w + 1 or the sum vanishes, so the
line rises by E_m per unit of w₀ or the group nets to zero — killed
either way.  ∎  [Machine: DRV-CRIT — all 632 groups of size ≥ 2
(280 surviving, 352 killed — CTRL-KILL fired): killed-by-netting ⟺
w₀-raised-or-zero, and the line/w₀ affinity line(B_G) − β =
E_m·(w₀(B_G) − w) exact on every surviving group; 0 violations.]

**Lemma SD-L7 (protected-vector survival — the reduction half of the
signed-multiplicity-one law; characteristic-free).**  Let G be a
group as in SD-L6 and partition it into residue classes (distinct
K₀-residues r₁, r₂, … of its cells) with signed counts n_i :=
(#positive) − (#negative) cells in class i.  Call the vector (n_i)
**protected** if either (a) exactly one n_i ≠ 0 and |n_i| = 1, or
(b) exactly two n_i ≠ 0 with values +1 and −1.  Then a protected
group SURVIVES at its line, in every characteristic:
Σ sgn·res = ±r_a (case a) or r_a − r_b with r_a ≠ r_b DISTINCT field
elements (case b) — nonzero by field distinctness alone; conclude by
SD-L6.  ∎  **Warning display (why (b) demands OPPOSITE signs):** a
(+1, +1) vector is NOT protected — r_a + r_b = 0 is a real field
relation (r_b = −r_a is a distinct class whenever char ≠ 2 and
r_a ≠ 0); the instrument's observed two-class survivor vectors were
exclusively (−1, +1), the protected form.  [Machine: DRV-M1 — 56
protected groups across the roster, every one netted nonzero at its
line, 0 violations; teeth: CTRL-M1 fired 576 (unprotected groups
are the majority — the predicate bites).]

**What SD-L7 does to the (K2) question.**  The instrument's P-M1
census says: on ALL 200 kill rows, every minimal survivor's vector
lay in {(+1), (−1), (−1,0), (0,+1), (−1,+1)} — i.e. protected (the
0-entries are dead classes).  SD-L7 proves protected ⟹ survives,
char-free, with NO carry-algebra input — so the ONLY open content of
(K2) is the EXISTENCE statement: some bottom-line group is protected.
That is the box.

## S6. THE RESIDUALS, BOXED

    ┌────────────────────────────────────────────────────────────────────┐
    │ (SURV-K2)  [OPEN — the g₀ ≥ 2 existence half].  On TRACK,          │
    │ (H1)–(H3), g₀ ≥ 2, c_{m−1} ≥ k: some slot group AT THE BOTTOM      │
    │ LINE β_min carries a PROTECTED residue-class vector (SD-L7         │
    │ sense).  By SD-L6 + SD-L7 + S-L3 this yields: the netted bottom    │
    │ survives — (SURV-DISJ)'s bottom leg at g₀ ≥ 2 — and with (RW₀),    │
    │ Q¹ ≠ 0 (S-THM(b)).                                                 │
    │   Weaker sufficient class-wide form (the off-kill scope caveat     │
    │   P-M1 carries): some bottom-line group has a class with           │
    │   n_i ≢ 0 (mod p) AND no cross-class relation at β_min (the 12     │
    │   observed (−2)-vector survivors need p ∤ 2; CROSS relations are   │
    │   instance-absent at every bottom/survivor line, 979/979, but      │
    │   instance-REAL off-bottom: 9 CROSS kills — 8 junk-free p = 3      │
    │   with 2·res_a = res_b in F₃, 1 mixed-junk char-2 with             │
    │   res_a + res_b = res_c).                                          │
    │   PROOF TARGET (from the instrument): a sign-reversing pairing on  │
    │   the bottom-line path family — pair correction-riders by a local  │
    │   toggle (correction ↔ t-seed shift, the two routes to the same    │
    │   slot mass), residue-preserving and sign-flipping, leaving        │
    │   exactly one unpaired path (or one +1/−1 pair of distinct         │
    │   classes).  CONSTRAINTS any candidate must reproduce: the exact   │
    │   survivor vector census {(+1)×85, (−1)×115, (−1,0)×5, (0,+1)×4,   │
    │   (−1,+1)×2}; the exceptionless complementarity (no non-CROSS      │
    │   killed group EVER carries a ±1 class — 0 instances in 979        │
    │   rows); M-B1 = 39 killed groups with ODD count (parity is NOT     │
    │   the mechanism); P-C refuted (103/200 kill rows have kills AT     │
    │   β_min — valuation separation protects nothing).                  │
    │   Ledger status at g₀ ≥ 2 (why SD-L1 does not close this): the     │
    │   dump budget rises to E_i − 1 + e₀(g₀−1) (level-0 window          │
    │   P₀ − 1 = e₀g₀ − 1) and level-0 corrections consume as little as  │
    │   e₀ (the C_k partial-consumption children, k ≥ 1) — corrections   │
    │   can be mass-cheap or free, promotions out of level 0 are LOSSY   │
    │   (e₀(g₀−1) per edge), and non-principal seeds inject t·e₀: the    │
    │   recycle economy (WMULTDCX fence F2).  On THIS roster every       │
    │   observed correction still had defc ≥ 1 (CTRL-DEFNEG silent,     │
    │   disclosed at S7) — the economy's mass-profitable corner is      │
    │   arithmetic headroom, not yet an instance.                        │
    ├────────────────────────────────────────────────────────────────────┤
    │ (SD-YJ)  [OPEN — the g₀ = 1 ∧ d₀ ≥ 2 corner].  At g₀ = 1,          │
    │ d₀ ≥ 2: exclude Φ₀Y-fed cascade-slot companions.  By (★) + SD-L1   │
    │ any companion C ≠ Ĉ (or X-mate) carries ≥ 1 Φ₀Y-junk edge and its  │
    │ correction economy solves  Σ defc_c = Y₂(C) ∈ {0,1}  with every    │
    │ defc_c ∈ {0, 1} pinned by SD-L1's equality analysis:               │
    │   Y₂ = 0: every correction has defc = 0 — each at a level with     │
    │           e_i = 1, taken via its Y-child, with FULL principal      │
    │           dump (a_l = P_l − 1 at every l < i);                     │
    │   Y₂ = 1: exactly one deficit-1 correction (e.g. e_i = 1           │
    │           full-dump L/X-child, or i = 0 with e₀ = 2 via Y-child),  │
    │           the rest defc = 0 as above.                              │
    │ Both shapes need a Φ₀-degree overflow (deg(b̂ĉ) ≥ d₀) at a real    │
    │ product — impossible at d₀ = 1 (L-NORM(c)), open at d₀ ≥ 2.        │
    │ MACHINE-DARK: no committed battery carries a g₀ = 1 ∧ d₀ ≥ 2       │
    │ tower (SURV_PROOF F-C); the corner has no instance record either   │
    │ way.  If (SD-YJ) is proved, SD-THM-A extends to all d₀ and the     │
    │ g₀ = 1 leg of (SURV-DISJ) is class-complete.                       │
    └────────────────────────────────────────────────────────────────────┘

Neither box is claimed minimal: for (SURV)(i) a bare "some slot
survives" suffices (SURV_PROOF S7); (SURV-K2) is displayed in the
protected-vector form because it is exactly what every instance
realized (maximal falsifiability), with the mod-p/no-CROSS weakening
displayed alongside.

## S7. Machine brackets (summary) and disclosures

`survdisj_derive_checks.py` (md5 3eaf69c…), single deterministic run,
24.5 s, exact arithmetic; the instrument's union roster replicated
verbatim (979 live rows: 286 sealed + 330 EQ + 212 PX + 151 SD).
**Ten verdict families, 0 violations:**

    DRV-PIN    8 md5 pins green (the 5 engines, 2 SURV runners, the
               committed instrument runner).
    DRV-MASS   the SD-L2 ledger, cellwise: 3,606/3,606 two-exit cells.
    DRV-DEF    per-correction inventory: dumps reduced, k = 0 at
               g_i = 1, SD-L1 deficit bound on 2,167 g₀ = 1
               correction edges — sharp (min 1, 2, 3, 4 at
               e_i = 1, 2, 3, 4, all Y_c = 0).
    DRV-Y0     no Φ₀Y-edge on d₀ = 1 towers: 3,274 cells (SD-L3
               shadow); CTRL-Y fired 60 on d₀ = 2 towers (teeth).
    DRV-SEEDY  1,355 b₀-seeding splits: Y-child ZERO on all
               (SD-L4(i)); measured: X-child also zero on all 1,355
               (instances realize exact splits; the proof does not
               consume this).  CTRL-XSEED 388 (X-junk real at
               correction products).
    DRV-NEPS   693/693 g₀ = 1 ∧ d₀ = 1 ∧ (STK) rows: cascade-slot
               population = {Ĉ} (no companion at all — stronger than
               the theorem's X-mate allowance; = the instrument's
               P-A).
    DRV-RIG    SD-L5 on all 979 rows: ≤ 1 correction-free junk-free
               terminal per seed, pairwise distinct slots;
               CTRL-RIGSEEDS 123.
    DRV-CRIT   SD-L6 on all 632 groups (n ≥ 2): kill ⟺ residue-sum
               zero ⟺ w₀ raised/zero; line/w₀ affinity exact.
               CTRL-KILL 352.
    DRV-M1     SD-L7 on 56 protected groups: all survive at their
               line; CTRL-M1 576 (unprotected groups exist).
    HARNESS    0 engine violations.

Group census (g₀-class | at-bottom? | verdict): g1 bottom kills 6
(the six K1 rows), g1 bottom survivals 12, g1 off-bottom 140 kill /
160 surv; g2+ bottom kills 97 (the sign-pair census), g2+ bottom
survivals 31, g2+ off-bottom 109 kill / 77 surv.

**Disclosures.**  (i) CTRL-DEFNEG never fired: no correction edge
with defc ≤ 0 exists on this roster even at g₀ ≥ 2 — the (SURV-K2)
box's "mass-cheap corrections" headroom is arithmetic, not
instance-realized here; the g₀ ≥ 2 deficit claim in the box is
therefore NOT machine-teethed and is displayed as budget arithmetic
only.  (ii) Coverage: m ∈ {3, 4}; d₀ = 2 only at g₀ = 2 (so
DRV-SEEDY's d₀ ≥ 2 coverage is g₀ = 2 only, and SD-THM-A's locus is
proof-complete but its d₀ = 1 boundary is exactly the probed
boundary); interior g ≥ 2 never probed (program-wide).  (iii) The
roster replication copies the committed instrument's selection
byte-logic; a selection divergence would show as a row-count
mismatch (979 = the instrument's count, matched).  (iv) All counts
are measured instance evidence, never proof steps.

## S8. The honest consequence display

* **(SURV-DISJ)**: PROVED at attempt grade on
  (H1)–(H3) ∧ g₀ = 1 ∧ d₀ = 1 ∧ (STK) ∧ (c_{m−1} ≥ k) — cascade leg,
  unconditional there (SD-THM-A(iv)).  OPEN at g₀ ≥ 2 ((SURV-K2))
  and at g₀ = 1 ∧ d₀ ≥ 2 ((SD-YJ)).  Class-wide: still open, now
  with the open set exactly = the two boxes.
* **(NEP)**: upgraded from instance-only hypothesis to THEOREM on
  the SD-THM-A locus (SD-THM-A(ii)).  S-THM(c)'s hypothesis list on
  that locus shrinks to (RW).
* **(SURV-(m+1))(i)**: CLOSED at attempt grade on
  (H1)–(H3) ∧ g₀ = 1 ∧ d₀ = 1 ∧ (STK) ∧ (RW) (SD-THM-A(v)) — the
  first closure not conditioned on (NEP).  OPEN beyond (the boxes;
  and (RW) itself at m ≥ 3).
* **LAW-DCX-(m+1) ON THE PINNED CLASS**: on
  (H1)–(H3) ∧ g₀ = 1 ∧ d₀ = 1 ∧ (STK) ∧ (RW), for every m ≥ 2:
  c_{m−1} ≥ k ⟹ Q¹ ≠ 0 (this note) and c_{m−1} < k ⟹ Q¹ = 0
  (E-THM†: zero two-exit paths; with W-D4† framing the deficit) —
  **the law holds on that class at the capped attempt grade**: the
  first full-law closure at any m ≥ 2.  Class-wide, LAW-DCX-(m+1)
  REMAINS A CONJECTURE.
* **What must still close before any acceptance-grade claim**, named:
  (1) the W arc's acceptance (WMULTDCX 0/2 CLEAN — the binding cap on
  every lemma here); (2) the EMPTY arc's acceptance (1/2 CLEAN);
  (3) the SURV_PROOF arc's acceptance (0 CLEAN passes); (4) THIS
  note's own hostile arc (0 passes); (5) the (H2) on-TRACK discharge
  rider (ITERLAWN S4.7 erratum chain, as at WMULTDCX F1); (6) the
  (RW) window at m ≥ 3 (instance-clean everywhere, unproved); (7)
  (STK) as an explicit pin (not derived from TRACK); (8) the two S6
  boxes for the loci beyond; (9) (EMPTY-gen)/interior g ≥ 2
  (program-wide out of scope); (10) the T-scope (H1) rider.

## S9. Claims table, dependency line, fences

| # | claim | grade | consumes |
|---|---|---|---|
| SD-L2 | E-weighted mass ledger, cellwise, any g₀ | PROVED (attempt; (H1)–(H2)) | W-D3†, [RMG] S2.2 inventory |
| SD-L1 | correction deficit ≥ E_{i+1} − E_i + 1 − Y_c at g₀ = 1; = 0 only at (e_i = 1 ∧ Y ∧ full dump) | PROVED (attempt; (H2)–(H3), g₀ = 1) | [RMG] S2.2 (a_{d,l} ≤ P_l − 1) |
| (★) | cascade-slot companions: Σ defc = Y₂ ∈ {0,1} | PROVED (attempt; + (STK), clause) | SD-L2, W-D2(iii)†, W-D7†, S-L1† |
| SD-L3 | no Φ₀Y-edge anywhere at d₀ = 1 | consumed VERBATIM (accepted) | L-NORM(c) [RMG] |
| SD-L4 | b₀-seeding: Y-child zero any d₀; X-mate has same slot, w₀ ≥ ŵ+1 | PROVED (attempt) | E-L6†, W-D1†, L-NORM(a)(b) |
| SD-THM-A | g₀=1 ∧ d₀=1 ∧ (STK) ∧ clause: cascade slot survives at β̂; (NEP) holds; (SURV-DISJ) holds; +(RW): Q¹ ≠ 0 | PROVED (attempt, capped) | SD-L1–L4, S-L0†, S-L4(i)(ii)†, S-L4(iii)-uniq†, S-THM(c)†, L-CELL(i) |
| SD-L5 | correction-free junk-free rigidity: ≤1 per seed, distinct slots, any g₀ | PROVED (attempt; (H1)–(H3)) | W-D2(iv)†, W-D3†, S2.2 determinism |
| SD-L6 | group survives at line ⟺ signed residue sum ≠ 0 in K₀ | PROVED (attempt; (H1)–(H2)) | S-L4(i)†, S-L0†, L-NORM(a) |
| SD-L7 | protected vector (±1 single, or +1/−1 pair) ⟹ survives, char-free | PROVED (attempt) | SD-L6, K₀ a field |
| (SURV-K2) | g₀ ≥ 2: some bottom-line group protected | **OPEN** (boxed, S6; existence half of P-M1) | — |
| (SD-YJ) | g₀ = 1 ∧ d₀ ≥ 2: no Φ₀Y-fed cascade companion | **OPEN** (boxed, S6; machine-dark corner) | — |
| (SURV-DISJ) | class-wide | **OPEN** = exactly the two boxes; PROVED on the SD-THM-A locus | — |
| LAW-DCX-(m+1) | the law | class-wide UNCHANGED conjecture; **holds on the S8 pinned class at capped attempt grade** | SD-THM-A + E-THM† + W-D4† |

**Dependency line.**  Consumes: WMULTDCX_2026-08-08.md† (W-D1, W-D2,
W-D3, W-D4, W-D5, W-D7 — **ATTEMPT grade, 0/2 CLEAN: the binding cap
on every result above**); SURV_PROOF_2026-08-08.md† (S-L0, S-L1,
S-L3, S-L4(i)(ii)+(iii)-uniqueness, S-THM(c) — attempt grade, 0
CLEAN passes); EMPTY_PROOF_2026-08-08.md† (E-L6 LOAD-BEARING;
E-THM in S8 only — attempt grade, 1/2 CLEAN); RMENGINE_2026-08-08.md
[RMG]† ACCEPTED (L-NORM(a)(b)(c), the S2.2 child inventory incl. the
reduced-dump display, L-CELL(i), S0.2 weight kit; L-VNZ/L-LINE
indirect only; L-ZMULT, E-L8 not consumed); [IL3]†/[ILN]† through
those blocks.  Dagger convention: † = consumed at the block's own
standing grade; grades ride through.  Instrument artifacts (seal
61e9e36, verdict 00e19b4) consumed as design input + instance
evidence only.  Machine leg of THIS unit:
`survdisj_derive_checks.py` 3eaf69c08f9a5b278826102610d4aa22 ·
output be088ed43e57569cbc508082dc2d15b8 · results
868f71903d53955418435c2377026723 (consumed runners pinned inside:
the instrument's seven + survdisj_instrument.py
3df33c7406913510ed7ebaa60d480b57).

**Fences.**
F-A. Pins are per-lemma and displayed: SD-THM-A consumes
     (H1)–(H3) ∧ g₀ = 1 ∧ d₀ = 1 ∧ (STK) ∧ clause (+ (RW) for (v)
     only); SD-L5/L6/L7 are g₀-free on (H1)–(H3)/(H1)–(H2); nothing
     is claimed at interior g ≥ 2.
F-B. Machine coverage: m ∈ {3, 4}; the displayed proofs are uniform
     in m ≥ 2; d₀ ≥ 2 probed only at g₀ = 2.
F-C. CTRL-DEFNEG silent: the (SURV-K2) box's g₀ ≥ 2 deficit-headroom
     display is budget arithmetic, not instance-backed.
F-D. No claim that X-mates exist (never observed in 1,355 splits);
     SD-THM-A merely TOLERATES them — do not cite this note as
     proving their absence (that would need the digit-exactness of
     canonical d-cell coefficients, deliberately not consumed).
F-E. Grade cap: the header sentence; this note's own hostile-pass
     record: 0 passes — the arc opens with the owed round 1.
F-F. Nothing here upgrades LAW-DCX-(m+1) class-wide, touches
     (SURV)(ii)/(EMPTY), or narrows the WMULTDCX S9 / SURV_PROOF S7
     boxes beyond the displayed closures; (SURV-RES-(m+1))'s weaker
     "some slot survives" form remains available to future units.
F-G. The DRV counters are instance evidence, never proof steps; the
     P-M1 vector census cited in S5/S6 is the INSTRUMENT's
     (committed artifacts), quoted as constraint, not re-derived.
