# HMENU3 passPE3 report — hostile verification of HMENU3_PROOF_2026-08-08.md (post-r2)

**Pass:** PE3 (third hostile pass on the (hMenu-3) discharge note; fresh
context; verifier fixes nothing, quotes and classifies; fences/boxes/annex
drafts are part of the statement). **Target:**
`lean/notes/openmath/HMENU3_PROOF_2026-08-08.md` at HEAD after r2 (e1280be;
seal 64d3ff9, verdict 625fa49, PE1 f4f7b03, r1 3235e1b, PE2 af988ee).
**Date:** 2026-08-08.

## VERDICT LINE

**CLEAN — 0 CRITICAL + 0 GAPS + 0 MINOR.** Every charge on this pass
CONFIRMS the note, including all five orchestrator-designated unmined veins.
The r2 carry rescope is verified on the closed forms, on the committed pin,
and by a full finite-difference carry sweep over my own transcriptions of all
family laws (B1/B2/B3-RAM aggregates carry u with integer coefficient exactly
1; the 3/2 raw carry is real exactly where r2 scopes it). The r2 fired-clause
record for HEX3 is accurate against the HEX3 PE1 report and the HEX3 r1 diff.
LEMMA HM3-3's β3 route is re-derived in full (locus, fibration, j-entry
transport, dictionary, aggregation) and all 18 committed B3 composite keys
plus all four A^{(j)}_L displays reproduce from my own transcription. The
α-bracket is verified AT m = 3 — a stratum the committed roster cannot
realize (max committed α-depth = 2, verified) — by my own enumeration of the
never-measured N = 10 α-slice in BOTH ring kinds: all four compositions of 3
at exactly their product laws, total 2,048 = b(3) = (q−1)q^{11}. The B2/B3
tier boundary admits no double-claimed history (constructed candidates all
excluded structurally; grammar scan over all 1,031 committed keys clean).
Supplier arcs are CURRENT at my read time (no W12 PE3, no HEX3 PE2, W-11
acceptance fold verified as a dated append with the consumed body
byte-frozen). Machine leg bit-clean: runner md5 = seal, fresh full run GREEN
183,387/0 in 445.1 s, teeth 21/15/10/4, artifacts identical ex-timing. The
fresh route (differs from both priors: an independent DEEP reader exercised
at α-depth ≥ 2, where no non-runner reader had ever run) validates
key-for-key on 10 committed rows including both full (2,9) rows (16.7M
states each), then extends to never-measured territory at N = 10. **The
2-clean acceptance counter moves to 1/2; PE4 is the accepting attempt.**

Three verified NON-findings are recorded as remarks (wording/evidence-scope
observations, no repair owed; see REMARKS).

---

## FINDINGS

None.

---

## CHARGE 1 — the r2 edits (the PE2-unmined vein): carry rescope + fired-clause record

### 1a. The aggregate coefficient-1 claim, re-derived from the closed forms

From my own transcription of the displayed laws (W12-L0 instance laws +
LEMMA HM3-4 + the route prefactors — no runner code): the W-11 interior
bracket at a RAM(u) leaf is q^{s_max} = q^{(u−1)/2}, and

    A_RAM(u) = (q−1)q^{2M−1−(3u+1)/2} · q^{(u−1)/2} = (q−1)q^{2M−u−2},

i.e. the bracket cancels the half exactly as the r2 bracket says. Composing:
ℓ(B2-RAM(m,k,w;u)) = 5m+6k+w+u+4 for m ≥ 1 and 6k+w+u+3 at m = 0 — u
coefficient 1, matching r2's displayed ℓ = 6k+w+u+3. Same for B1-RAM
(ℓ = 6k+u+3 at m = 0) and B3-RAM (ℓ = 6t+j+u+3 at m = 0 — u coefficient 1,
j coefficient 1). Full
finite-difference carry sweep over my own closed forms (q = 5, N = 30):
m → 5, k/t → 6, w → 1, j → 1, block-refine k′ → 2, VERT2-leaf k′ (AP
index) → 3, I-RAM3 h → 2, I-VERT1 u₀ → 1, I-LINRAM2 u₀ → 1;
**B1/B2/B3-RAM u → 1 exactly**; and 3/2 appears exactly where r2 scopes it:
I-LINRAM2 u₁ → 3/2, I-RAM2LIN u₀ → 3/2, and the HM3.A INSTANCE law
B2-RAM@hist u → 3/2.

### 1b. The committed pin, re-verified at raw-key granularity

At (Zp,2,9) the B2-RAM(0,1,1;u=3) aggregate fiber consists of EXACTLY the
two keys the r2 commit names — (('VERT1',4,1),('RAM2LIN',6,1)) = 8,192 and
(('VERT1',4,1),('VERT2',5,1),('RAM2LIN',6,1)) = 8,192 (verified by
enumerating the whole committed tally for VERT1(4,1)-first, RAM2LIN(6,·)
leaf keys) — sum 16,384 = 2^{27−13} with ℓ = 13 = 6+1+3+3 (u carry 1). Each
instance key individually sits at instance ℓ = 6k+w+(3u+1)/2+2 = 14
(2^{27−14} = 8,192 ✓, my own product: prefactor 2^7 × L0-instance 2^6,
second key × (q−1)^1) — "the two instance keys carry the halves, their SUM
does not" ✓. The "(3u+1)/2 aggregate reading gives 2^{27−16} = 2,048"
matches PE1's displayed (refuted) formula ℓ = 6k+w+(3u+1)/2+4 = 16 ✓ (see
REMARK 3 for a referent pedantry).

### 1c. The tier-I/II 3/2 claim from the closed forms

From W12 S2.4 as stated: RAM2LIN(u₀,t) has ℓ = 2+u₀+t+(u₀+t+1)/2 → u₀
carries 3/2 (equivalently ℓ = 2+6t+(3u+1)/2 in dictionary coordinates
u = u₀−3t); LINRAM2(u₀,u₁) has ℓ = 2+u₀+u₁+(u₁+1)/2 → u₁ carries 3/2, u₀
carries 1; tier II adds +5m and preserves both. HM3.A B-RAM instance laws:
route prefactor × (q−1)^{|hist|}(q−1)q^{2M−1−(3u+1)/2} → u carries 3/2. So
the 3/2 raw carry lives exactly in {tier-I/II RAM2LIN/LINRAM2, HM3.A
instance laws} among the odd-RAM-type parameters — the r2 scope is exact
(REMARK 2 records the one adjacent nuance, outside the sentence's scope).
The Step-1 guard m_i·b_i ∈ ℤ_{>0} holds at every family: step-2
progressions carry (3/2)·2 = 3 resp. 1·2 = 2.

### 1d. The fired-clause record (r2 m2), verified against the suppliers

* **HEX3 gap 1 is what r2 says it is:** HEX3_passPE1 F1 = H-2(i)'s
  displayed ghost-zone inequality "(3−j)(N−1)/3 < N−(3−j)k", pairing
  backwards, FALSE at j = 1, N = 7, k = 2 (reads 4 < 3); corrected pairing
  N−jk holds on the whole range (HEX3 PE1 re-derived it; j = 1: 4 < 5,
  j = 2: 2 < 3) ✓ quoted from the report. §S3.1's LEMMA HM3-2 proof fires
  H-2(i) BY NAME ("window coherence") ✓. The display was corrected at HEX3
  r1 2580f12 ✓ (the r1 diff replaces the pairing; the corrected text is at
  HEX3 HEAD lines 229–245 with the [r1, PE1 F1] tag).
* **HEX3's gap 2 + minors don't touch fired clauses** ✓: gap 2 is P-1
  never-measured-census bookkeeping (HMENU3 consumes H-1/H-2/H-3 + the
  u(q,N) law, not HEX3's P-1 list); m1 is the u(2,7) anchor's form tag —
  and u(2,7) = 5,664 in both forms is verified inside HEX3-PE1's own m1
  (1440+128 telescoped = 1504+64 first-step, both + 4096) ✓; m2 is an
  S8.2 aside.
* **W-11 "since ACCEPTED at 2/2"** ✓: PE3 CLEAN @ 839b735, PE4 CLEAN @
  16d12e1, fold @ 35e6545 — all exist with exactly those verdicts. The
  fold's diff on W11_PROOF is a DATED APPEND ONLY (the ★ACCEPTANCE RECORD★
  at the tail; body byte-frozen since 31506b3), so the r2 bracket's "the
  pins consumed here are unchanged" is verified at diff granularity.
* **W-12 "unchanged since r2 99f1813, no PE3"** ✓ at r2 time AND at my
  read time: no W12_passPE3 report exists; the last commit touching
  W12_PROOF is 99f1813. HEX3 likewise: last touch 2580f12 (r1), no
  HEX3_passPE2. The only commits after e1280be at my read (HEAD a049d6c)
  are GENH4/SIGMALAW items that touch neither this note nor any supplier
  clause.
* **Nothing edited the note after r2** ✓ (`git log` on the note: e1280be
  is the last commit).

## CHARGE 2 — LEMMA HM3-3, the β3 route re-derived in full (the priors checked B1/B2 harder)

**Locus (own count, from the W12 S2.4 frame conventions).** β3 =
VERT2(u₀,t)-⟨1²⟩: vertex (2,t) exact with unit lead λ = digit_t(B₂) (q−1
letters), v(B₀) = u₀ exact, u₀ ≡ t (mod 2), u₀ > 3t, j := (u₀−3t)/2 ≥ 1;
left side slope t+j through the interior lattice point (1,(u₀+t)/2);
residual λ(y−z)² pins digit_{(u₀+t)/2}(B₁) = −2zλ and digit_{u₀}(B₀) = z²λ
(determined, NOT a letter). Free digits q^{(W−1−t)+(W−1−(u₀+t)/2)+(W−1−u₀)}
⟹ locus = (q−1)²q^{3W−3−6t−3j} after substituting u₀ = 3t+2j. Matches
HEX3's displayed locus and PE2's independent count.

**Root geometry / why the entry depth is j.** The split root sits at depth
t (the right side (2,t)–(3,0), length 1); the cluster pair sits on the left
side at depth t+j each, total cluster valuation u₀−t = 2(t+j). Scaling by
π^t (split depth m = t), the block window is M = W−3t and the block state
(A₁,A₀) has v(A₀-block) = (u₀−t)−2t = 2j EXACT with repeated residual root —
so the block's polygon is a single slope-j side with a repeated root: its
FIRST refine event is at block depth j, neither earlier (v(A₀) = 2j exact
forbids a shallower node) nor later (the repeated residual forces the
event). The β3 event and the block's first refine are the SAME event: the
β3 z-letter = W-11's refine letter at depth j, which is why the note writes
ONE (q−1) in the prefactor and leaves the other inside L0's (j)⌢hist′
bracket (the (q−1)^{|hist|} weight with |hist| ≥ 1).

**Fibration exactness (dimension check).** The note's law:
(q−1)·q^{3t+M−1} × L0((j)⌢hist′, leaf; M). Summed over the block system:
block states entering first refine at depth j = (q−1)q^{2M−2−3j} (own
count: v(A₀) = 2j exact + letter, v(A₁) ≥ j with pinned digit), and
(q−1)q^{3t+M−1}·(q−1)q^{2M−2−3j} = (q−1)²q^{3W−6t−3j−3} = the locus,
EXACTLY — ghost fiber q^{3t} (2t B₂-digits + t B₁-digits) × split-root
fiber q^{M−1} forced. Consistent with HEX3 H-3's β3 drain bullet:
(q−1)_λ q^{3t+M−1} × (q−1)_z q^j R(M−2j) = (q−1)²q^{3t}q^{M−1}q^jR(M−2j) ✓.

**Dictionary at split depth m = t (raw-key transport).** A later block
event at block depth k′ > j has raw B₀-valuation t + 2(t+k′) = 3t+2k′ with
the persistent vertex (2,t) ⟹ raw record ('VERT2', 3t+2k′, t) ✓; block
RAM(u) ⟹ raw ('RAM2LIN', 3t+u, t) with (3t+u)−t = 2t+u odd ⟺ u odd ✓;
2SIDED(w, u−w) ⟹ ('3LIN', t+(u−w), t+w, t) ✓; SPLITEQ/INERTDEEP(k′) ⟹
('VERT2', 3t+2k′, t, λ) ✓ — each from root-valuation sums plus HM3-1(iii)'s
persistent-vertex argument (v(recentering shifts) > t keeps
v(center−r_split) = t; rightmost side length 1 ⟹ g = 1 ⟹ no repeat there;
residuals unit-scaled by W12-L1(b)).

**The j-entry aggregation (all four A^{(j)}_L displays).** Histories with
k₁ = j are (j)⌢(subsets of {j+1..s_max}), so Σ(q−1)^{1+t′}C(s_max−j,t′) =
(q−1)q^{s_max−j}, giving A^{(j)}_RAM = (q−1)²q^{2M−u−2−j} (u ≥ 2j+1),
A^{(j)}_2SIDED = (q−1)³q^{2M−3−u−j} (w₁ ≥ j+1), A^{(j)}_SPLITEQ =
((q−1)²(q−2)/2)q^{2M−2k′−3−j}, A^{(j)}_INERTDEEP = (q(q−1)²/2)
q^{2M−2k′−3−j} (k′ ≥ j+1) — all four re-derived, ranges from the W12-L0
node constraints at s ≥ j (u ≥ 2s+1, w ≥ s+1, k ≥ s+1) ✓ = the displays.

**Machine tie.** All 18 committed B3 composite keys across the 41 rows
reproduce from my own transcription (part of the 1,031-key check below),
and every realized B3 aggregate cell at the 6 parser rows matches my own
b(m)·(q−1)q^{3t+M−1}·A^{(j)}_L law.

## CHARGE 3 — the α-bracket at m ≥ 3 (both priors stopped at m ≤ 2)

**Roster fact (established first):** the maximum α-depth realized on the
ENTIRE committed 41-row roster is m = 2 (survey over all committed keys;
rows (2,7),(2,8),(2,9) both kinds carry it). Reason: a depth-m α-prefix
needs reduced window N−3m ≥ 1, and the largest roster N is 9 ⟹ m ≤ 2. So
"verify m = 3 against the committed deep rows" is unsatisfiable from
committed data — the sealed battery machine-exercises LEMMA HM3-4 only at
m ≤ 2 (see REMARK 1; the lemma itself is proved by generating function,
which I re-verified: 1−A₁ = (1−q⁴x)/(1−q³x), coefficient (q−1)q^{4m−1},
and by hand at m = 3: (q−1)³q⁹ + 2(q−1)²q⁹ + (q−1)q⁹ = (q−1)q⁹·q² ✓).

**m = 3 verified by my own enumeration on never-measured territory.** The
α-slice {v(B₁) ≥ 2, v(B₀) ≥ 3} at window N = 10 contains EVERY state whose
key begins with an α-step (char-2 proof: the α residual (y−z)³ ≡
y³+zy²+z²y+z³ mod 2 has all coefficients nonzero, pinning v(B₂) = k ≥ 1,
v(B₁) = 2k ≥ 2, v(B₀) = 3k ≥ 3), so slice tallies are EXACT full-row counts
for all α-prefixed keys. Enumerating all 2^24 = 16,777,216 slice states per
ring kind with MY OWN reader (below): the m = 3 stratum exists at N = 10
exactly as the recursion predicts — the four composition keys
(('ALPHA',1),('ALPHA',2),('ALPHA',3),DRAIN), (('ALPHA',1),('ALPHA',3),DRAIN),
(('ALPHA',2),('ALPHA',3),DRAIN), (('ALPHA',3),DRAIN), EACH measured at
exactly its product law Π(q−1)q^{3k_i} = 512, total 2,048 = b(3)·drain(W=1)
= (q−1)q^{11} — in BOTH ring kinds. Every other α-first key (64 keys per
kind) also matches: 57 decided/pure-drain keys vs my closed forms 0
mismatches, 7 pure-α drain keys each at its composition law, and the
leaf-resolved self-similarity checked against COMMITTED data — all 55
(('ALPHA',1),)⌢K keys = (q−1)q³ × committed (2,7)[unshear(K)] and all 8
(('ALPHA',2),)⌢K keys = (q−1)q⁶ × committed (2,4)[unshear(K)], masses
2,097,152 and 32,768 on the nose (both kinds).

**A discriminating bonus:** the deep-3LIN key (('ALPHA',1),('3LIN',4,3,2))
first becomes visible at N = 10 (never on the roster). My first transcription
sheared 3LIN's slopes by 3m; the measurement (2,048) refuted MY reading and
confirmed the note's HM3-2 shear table ("slope/depth params +k": 8 × 256 =
2,048 with slopes sheared by m). The instrument distinguishes the two
readings — the note's is the true one.

## CHARGE 4 — B2/B3 tier-boundary disjointness (constructed candidates)

Candidate double-claimed histories, each excluded:

* **B2 vs B3 head-on:** B2 keys have first non-ALPHA symbol
  ('VERT1',·,·)-arity-3; B3 keys ('VERT2',·,·)-arity-3 — no tuple can be
  both. State-side, β2 needs the hull vertex at 1 ONLY, β3 at 2 ONLY; a
  hull with vertices at both 1 and 2 is 3LIN (all sides g = 1, decided) —
  no state satisfies both loci.
* **B3 entry step vs B2's interior block-refine steps** (same tuple shape
  ('VERT2', a, b)): interior VERT2 steps occur only strictly AFTER a
  VERT1/FULL11SQ event; a key's first non-ALPHA symbol being VERT2 forces
  the β3 parse. Committed-corpus scan: no key mixes a VERT2-first with a
  later VERT1/FULL11SQ/ALPHA step (phase grammar HM3-1(iii)), and within
  every key ALL VERT2-step/VERT1-step third fields agree (the constant
  split depth) — 1,031/1,031 keys clean.
* **B3 parameter re-parse** (same key, different (t,j)): t is the step's
  own third field and j = (u₀−3t)/2 — recovery is unique; no arithmetic
  freedom.
* **B3-with-immediate-SPLITEQ vs tier-II DEEP-VERT2(λ):** both are
  length-2 keys ending in an arity-4 VERT2 leaf, but the first symbols are
  ('VERT2',a,t) arity-3 vs ('ALPHA',m) arity-2 — disjoint; both patterns
  realized in the corpus ((('VERT2',5,1),('VERT2',7,1,'2')) at (Zp,2,8) vs
  (('ALPHA',1),('VERT2',8,2,'2')) at (Zp,2,9)) and counted apart.
* **Bare ('VERT2',u₀,t) with no continuation** (the would-be ambiguous
  incomplete key): does not occur in the corpus — a β3 step is always
  followed by its block chain's record (the read continues).
* **Parser partition:** my own key parser (α-prefix → route → dictionary
  inverse) assigns every one of the 1,031 committed keys exactly one
  (tier, family, params) cell with no unparseable or ambiguous key, and
  per-cell sums equal my own aggregate laws (58 B-cells + 18 II-cells at
  each (2,9) row, 0 mismatches).

## CHARGE 5 — supplier-arc currency at MY read time (HEAD a049d6c)

* W-12: post-r2 99f1813, counter 0/2, **no PE3 has run** — the HM3.D
  header and S9 bullet 1 [r2 m2] displays are CURRENT.
* HEX3: post-r1 2580f12, counter 0/2, **no PE2 has run** — BOX-2/S9/S10
  displays CURRENT.
* W-11: ACCEPTED 2/2 (839b735 + 16d12e1 + fold 35e6545, dated-append-only
  on the note) — displays CURRENT; consumed body frozen since 31506b3.
* This note: untouched since e1280be; runner + artifacts untouched since
  625fa49/64d3ff9 (md5s at HEAD = the S10/seal values).
* Commits landed after r2 (ea5978d GENH4, 6e57e03/a049d6c SIGMALAW) touch
  no supplier clause consumed here.

## MACHINE LEG

* **Runner vs seal:** md5 65326f85c218087261f434f8b8cc8dda = the sealed
  value; `git diff 64d3ff9 HEAD -- verification/openmath/hmenu3_checks.py`
  EMPTY. Artifacts at HEAD: output txt md5 7f754887…, results JSON md5
  daf077d0… = the S10-displayed values.
* **Fresh full run** (isolated `git archive` of seal 64d3ff9 into /tmp,
  single run): **GREEN, exit 0, 183,387 checks / 0 violations, 445.1 s**
  (committed 443.8 s) — PIN 7, MENU 1,031, DRAIN 82, AGG 707, XREAD
  171,444, HEX3TIE 38, W12TIE 69, DBLTIE 23, ORACLE 9,956, RTAU 30; teeth
  21/15/10/4 all exactly preregistered; 41/41 rows; never-measured rows
  reproduced ((Fqt,2,9): 187 keys, deep 523,008, drain 103,168; (Fqt,3,6);
  (Fqt,27,2)). Results JSON == committed except `elapsed_s`; output txt
  ZERO non-timing diffs.
* **Verdict-section arithmetic:** family totals sum to 183,387 ✓; ORACLE
  9,956 = 9,952 σ + 4 member-count rows (48/624/6,688/2,592) ✓.

## FRESH ROUTE (differs from both priors: an independent DEEP reader)

PE1's fresh reader enumerated rows N ≤ 6 (max α-depth 1); PE2's route had
no state enumeration; the sealed XREAD tie also only covers rows ≤ 65,536
states (N ≤ 6 at q = 2). So before this pass, NO non-runner reader had ever
read a depth-≥ 2 chain. My reader (`/tmp/hmenu3_pe3/fresh_deep_reader.py`,
own hull/residual/recentering arithmetic for ℤ/2^N and F₂[t]/t^N, written
from the S1 spec + W12 S2.4 + HEX3 S2 loci, no runner imports):

* **Validation, 10 committed rows key-for-key:** (Fqt,2,N) N = 4–8,
  (Zp,2,N) N = 5,7,8, then BOTH full (2,9) rows (16,777,216 states each,
  187 keys each) — **0 mismatches anywhere**, including every m = 2 key and
  the never-measured (Fqt,2,9) row's full tally (second independent
  reproduction, first at depth ≥ 2).
* **Never-measured N = 10 α-slices, both kinds** (2 × 16,777,216 states):
  the CHARGE-3 results (m = 3 exact; 64 α-first keys all at their laws;
  self-similarity ties to committed (2,7)/(2,4) exact).
* **Menu-vs-complement identity at 12 (q,N):** my own transcription of ALL
  26 TRP aggregate families summed (with my own A_L/A^{(j)}/b(m)) equals
  q^{3(N−1)} − u(q,N) (my own HEX3.A transcription) EXACTLY at (2,5),
  (2,7), (2,9), (3,5), (3,6), (4,5), (5,3), and at fresh never-tested
  parameters (4,18), (5,14), (2,20), (7,12), (27,4); and equals the
  committed states−drain at (Fqt,2,9), (Fqt,3,6), (Zp,2,9), (Fqt,27,2),
  (Zp,13,3). This is an independent exactness+exhaustiveness check of
  THEOREM HM3.B jointly with (A2)'s complement form.
* **All 1,031 committed keys re-derived:** every decided key on all 41
  rows equals my own closed-form transcription (0 mismatches; 158
  β-route drain-history keys checked at aggregate level: per-row drain
  sums == my own u(q,N) on 41/41 rows).
* **R_τ displays:** Σ_τ R_τ = 1 symbolically, the q = 2, 3 anchor vectors
  and E[#roots] = q/(q+1) reproduced (light leg; both priors did full
  independent assemblies).

## REMARKS (verified non-findings, for the record)

1. **The bracket's machine evidence on the roster stops at m = 2.** The
   committed 41 rows cannot realize α-depth 3 (max N = 9), so P-4's "the
   bracket LEMMA HM3-4 … machine-checked against enumeration" is exercised
   at m ≤ 2 only — accurate as stated (the note claims the lemma by proof,
   not enumeration), recorded here because this pass EXTENDS the machine
   evidence to m = 3 on N = 10 territory (both kinds, exact).
2. **An adjacent 3/2 outside the r2 sentence's scope:** the tier-I/II
   VERT2(u₀,t;λ) LEAF also carries u₀ with raw coefficient 3/2 (the
   (u₀+t)/2 term; even-parity family). The r2 clause is scoped to "the odd
   RAM-type parameters u/u₁", which VERT2's parameters are not, and the
   main text prices VERT2 in AP-index k′ coordinates (carry 3, integer;
   step-2 parity progression passes the Step-1 guard: (3/2)·2 = 3). No
   text is false; recorded so no future pass re-derives this as a surprise.
3. **Referent pedantry in the r2 pin parenthetical:** "a (3u+1)/2
   aggregate reading would give 2^{27−16} = 2,048" is computed against
   PE1's displayed formula ℓ = 6k+w+(3u+1)/2+4 (the bracket cites PE1
   FINDING 2, so the referent is recoverable); a reader instead
   substituting (3u+1)/2 into the r2's own ℓ = 6k+w+u+3 gets 2^{27−15} =
   4,096. Either way ≠ 16,384 — the pin discriminates against every
   half-carry reading; nothing false.
4. (Wording, inherited from r1 and PE2-era text, not r2-new:) the arc
   lines' "runner + artifacts byte-frozen at seal 64d3ff9" — the artifacts
   enter the history at verdict 625fa49 (the seal commit contains note +
   runner only, per S7 and PE2's charge 5); the content claim (nothing
   moved since) is true and md5-verified. No reader of S7/S10 can be
   misled about which commit holds what.

## SCORE AGAINST P-10

No falsifier fired on any leg: no MENU/AGG-equivalent discrepancy in my
deep-reader or closed-form routes (so HM3-4 — now through m = 3 — and every
A_L/A^{(j)} aggregation stand), no XREAD-analogue divergence (my own reader
agrees with the committed tallies everywhere it ran), no σ anomaly, no
tooth anomaly, engine/series identities hold.

## COUNTER

PE3: **0 critical / 0 gaps / 0 minor — CLEAN.** 2-clean acceptance counter
for this note: **1/2**. PE4 (fresh context) is the accepting attempt.
