Full hostile pass completed; no files changed.

1. **GAP — D0 conditionality list is not literally exhaustive.** The note says HV3-THM is given “ONLY” conditions (i)–(iii), but the theorem setting and D4 additionally consume the standing GD23-row/TR-5 dictionary and associated r=3 type-hood. Those identifications are essential to translate
   \[
   \phi_2^{GMN}\leftrightarrow\Phi_1,\quad
   \phi_3^{GMN}\leftrightarrow\Phi_2,\quad
   (e_2,h_2)^{GMN}=(e_1,h_1)^{proj}.
   \]
   They are disclosed earlier in D0, so this is not a hidden mathematical failure, but “ONLY (i)–(iii)” is not closed under the proof’s stated consumptions.

2. **GAP — D3 does not actually reproduce the entire TR3-S1 pin “verbatim.”** D3 derives
   \[
   v_2(P)=\min_m\{w_0(b_m)+mh_0\}=w_1(P),
   \]
   but merely includes \(v_2(\phi_2)=w_1(\Phi_1)\) in the parenthetical description of the accepted pin. That second equality follows immediately by applying the all-\(P\) identity to \(P=\phi_2\) and using \(\phi_2^{GMN}\leftrightarrow\Phi_1\), but D3 does not perform that check explicitly. This is presentational, not critical.

Priority classifications:

- **A — PASS.** The quoted \(H_{r-1}\) display is genuinely generic in \(r\), inside the paper’s standing scope fixing a type of order \(r-1\). Its intercept formula and Def. 2.5 normalization agree with the primary GMN text; no r-specific premise is suppressed. The generic valuation formulas are also corroborated by the [GMN paper](https://upcommons.upc.edu/server/api/core/bitstreams/d0cbc387-9886-4c36-96e7-43a7add44dcd/content) and [arXiv v2 record](https://arxiv.org/abs/0807.2620v2).

- **B — PASS.** All five steps are valid under the displayed standing inputs. Step 2 specializes Prop. 2.7 correctly; step 3 substitutes accepted TR3-S1 correctly; step 4 matches the same \(\Phi_1\)-development and augmentation value; step 5 validly uses multiplicativity and the dictionary. The consumed ordinate identity follows:
  \[
  u_i=w_2(A_i)+i\,w_2(\Phi_2).
  \]
  Zero coefficients are harmless under the usual \(v(0)=\infty\) convention or by omitting them.

- **Scope — PASS.** Only \(e_0=1\) and \(e_1=1\) enter: \(e_0\) through TR3-S1 and \(e_1\) through \(e_2^{GMN}=1\), \(|\lambda_2|=h_1\), and identity rescaling. Project \(e_2\) is never used.

- **C — GAP as above, noncritical.** The missing displayed substitution is immediate but prevents the subsection’s “verbatim” claim from being literally complete.

- **D — GAP as above, noncritical.** The dictionary/type-hood consumption is disclosed but lies outside the supposedly exhaustive three-item list. H-CHAIN/RESCALE and accepted TR3-S1 are otherwise accounted for.

- **E — PASS.** D7 expressly says the sites are “DELIBERATELY NOT EDITED.” D5’s “removes ONE bracket” is semantic discharge language, bounded by “NOT touched” and D7’s fold-duty reservation; it does not claim textual edits.

The environment’s local shell was unavailable because its sandbox launcher lacked `bubblewrap`, so I could not independently recompute the repository hash, line count, or bracket-site census. That operational limitation does not alter the two content findings above.

HV3-P1 FINDINGS: 0 critical, 2 gaps  
VERDICT: GAPS-ONLY