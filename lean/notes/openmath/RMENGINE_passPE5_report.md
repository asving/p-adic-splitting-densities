# RMENGINE pass PE5 report (Fable, round 5 — THE ACCEPTANCE ATTEMPT) — 2026-08-08 campaign (wallclock 2026-08-04)

**Pass:** RMENGINE-PE5, hostile content verification, fresh context. Verify only; fix nothing.
**Object:** `lean/notes/openmath/RMENGINE_2026-08-08.md` at commit **d63f8bb** (confirmed: `git log -1 -- <file>` → d63f8bb; working tree byte-identical to d63f8bb, `git diff d63f8bb -- <file>` empty; 1453 lines). The r4 text.
**Bar:** acceptance = this pass AND the concurrent Codex PC5 both CLEAN (0 critical, 0 gaps; notes do not block).
**Charge:** (1) r4-remedy discharge, hostile — change (7) vs the actual `git diff bf18bfe 8835d20`; the completeness withdrawal at every ripple site; C28's per-pass attribution vs each archived report; the PE4-N-1 re-scope vs the actual `git diff 423b399 ab2e471`; (2) machine legs — census re-run vs committed output, `rmengine_pe2_ext.py` re-run read-only, the r4 statement fence vs `git diff ab2e471 d63f8bb`; (3) full spine sweep re-derived standalone; (4) fresh eyes over the whole note.

---

## 1. R4-remedy discharge (charge 1)

### 1.1 Change (7) vs the actual `git diff bf18bfe 8835d20` — FAITHFUL
I ran the diff myself and extracted the L-LINE(ii) hunk. Composed (bf18bfe), verbatim: "correction-main children PRESERVE the line exactly whenever w₀(b·c_d) = w₀(b) + w₀(c_d), i.e. always (w₀ is a valuation; the residue product sits in the L-child, whose displayed line uses exactly this w₀-sum — when the residue product res(b̄)res(c̄_d) vanishes the L-child is the zero polynomial and is absent, the [IL3]† DEV-1 bookkeeping)". r1 (8835d20), verbatim: "it is NEVER absent (L-NORM(a): residues multiply in the field K₀ — the old \"residue product vanishes\" parenthetical was an artifact of the un-normalized display and is DELETED)". The r4 census entry (7) — "L-LINE(ii)'s composed 'when the residue product vanishes the L-child … is absent' clause replaced by its NEGATION 'it is NEVER absent' … eliminated by L-NORM(a)" — matches both sides of the hunk. Secondary witness verified: the composed fence box (bf18bfe lines 502–503, wrapped) contains verbatim "a level-(i+1) correction can genuinely RAISE any linear μ", deleted at r1; "deleted at R2" correctly names the round-1 disposition R2 (PE-F1), where the fence-box deletion is recorded. Genre-match to PE4's G-1 exact. DISCHARGED.

### 1.2 The completeness withdrawal at the ripple sites — DISCHARGED at every operative site; two archived-cell survivals (notes) + one stale committed-output quote (GAP — §5 G-1)
Grep of the full text for complete/all six/all SIX/exhaustive: the operative census sentence (round-1 record) now reads "The r1 statement-change census, per the actual `git diff bf18bfe 8835d20` … with no completeness claim]: (1)…(7)"; C28 reads "seven enumerated changes per the actual diff … no completeness claim"; the standing-lesson paragraph declares the policy; the S8 charter's own exhaustiveness claim was already machine-arbitrated at r3 ("exactly the script's 'CENSUS CLOSED — zero violations' line, nothing more"). Survivals, all in archived per-round surfaces: (a) round-3 disposition cell "now enumerate all SIX r1 statement changes per the actual diff" — stale at HEAD (see note N-1); (b) round-2 disposition cell "the complete PE2 census absentee list" (see note N-2). Separately, the r4 edit itself left the round-3 record's quote of the committed census output stale — that is finding G-1 (§5), the one gap of this pass.

### 1.3 C28's per-pass attribution — ACCURATE at all six sources
C28 basis, verbatim: "the none-weakening rider verified against the actual `git diff bf18bfe 8835d20` by PE2, PE3 (report §1.5), and PE4 (its G-1 re-read found change 7 and re-confirmed the rider); PC2/PC3/PC4 verified content-only against supplied texts (no execution)". Checked against each archived report:
* PE2 §4, verbatim: "`git diff bf18bfe 8835d20` on the note (511+/202−), all grade-bearing lines extracted" — ran the actual diff. ✓
* PE3 §1.5, verbatim: "My own extraction of every grade-bearing/statement-bearing line from `git diff bf18bfe 8835d20`" — ran the actual diff. ✓
* PE4 §1.2 + §5 G-1: "I ran the diff myself (715 ± lines)… the census's substantive rider … remains TRUE — verified independently against the diff", and G-1 is the change-7 witness. ✓
* PC2 report item 7 (quoted in PE3-G-2): "the local command launcher failed because `bubblewrap` is unavailable … treated solely as stated evidence" — no execution. ✓
* PC3 report, verbatim: "Exact repository/diff verification was unavailable because the command launcher failed for lack of `bubblewrap`." — no execution. ✓
* PC4 report note 2, verbatim: "independent repository byte-diff execution was unavailable because the command launcher lacks `bubblewrap`." — no execution. ✓
Head round-4 bracket cross-checked against the PC4/PE4 reports: PC4 "CLEAN — 0 critical, 0 gaps" + 2 notes ✓; PE4 "GAPS-ONLY — 0 critical, 1 gap" (+1 note, stated in the tail record) ✓; PE4's machine-leg list (census CLOSED byte-identical; pe2 referee byte-identical; r3 fence vs actual diff; 44 keys; 12 ⟦nc⟧) matches PE4 §§1.3, 2, 4 ✓; "FIFTH AND SIXTH consecutive 0-critical passes" = PC2/PE2/PC3/PE3/PC4/PE4 ✓. DISCHARGED.

### 1.4 The PE4-N-1 re-scope vs the actual `git diff 423b399 ab2e471` — CORRECT in its operative content; sectional gloss still under-inclusive (note N-3)
The re-scoped sentence: "The r3 edit adds trailing tags throughout the body (S0–S7, S9, S10 lines) … plus the charter/rows/records edits and the two queue-named wordings … : no S2–S7 display content changed." Against the actual r3 diff (81 added lines carrying ⟦…⟧): tags were indeed added throughout, including on pre-S0 HEAD-matter lines — the headline-consequence line ("only — S7.1): (ITER-LAW-n) off DCX-(n) is UNCONDITIONAL (modulo the ⟦C32⟧") and the head-declaration sealed-artifacts paragraph (⟦C15⟧ ×3, ⟦C12⟧ ×2) — which are census body but not inside S0–S7/S9/S10. "Throughout the body" is correct; the parenthetical section list omits the head-matter lines; the operative fence (no S2–S7 display content changed) is TRUE against the diff. Residue of the same genre as PE4-N-1 itself → note-grade (N-3).

## 2. Machine section (charge 2)

* `python3 verification/openmath/rmengine_claims_census.py` → exit 0; output BYTE-IDENTICAL to the committed `rmengine_claims_census_output.txt` (diff exit 0): "MACHINE CENSUS (tag grammar): 67 keyword lines in body (55 row-tagged, 12 nc-tagged); 33 table rows (2 table-only); excluded zones: 13 / CENSUS CLOSED — zero violations." CLOSED REPRODUCES. Zone arithmetic verified against the script's zone classes (S8→S9; S11→EOF; the 3 ledger-paste paragraphs; head brackets `**[REPAIR ROUND`; tail records `**REPAIR RECORD`): 13 = 1+1+3+4+4; the +2 over r3's 11 = the round-4 head bracket + the round-4 tail record, both legitimately record surfaces under the pre-existing zone classes — no grammar widening. Body counts (67/55/12/33/2) UNCHANGED from r3 — machine confirmation that the r4 edit touched no body keyword/tag line.
* `python3 verification/openmath/rmengine_pe2_ext.py` (md5 574f87d89fae5794bede7e0b38121877, matching PE4's pin — untouched) → exit 0; output BYTE-IDENTICAL to the committed `rmengine_pe2_ext_output.txt` (diff exit 0): all 12 XV verdict lines GREEN, 67 flat-tower calls, 1080/0 + 225/0 + 252/0 sweeps. ALL-GREEN REPRODUCES.
* **The r4 statement fence:** `git diff ab2e471 d63f8bb -- lean/notes/openmath/RMENGINE_2026-08-08.md` (132 diff lines, 79+/18−) read IN FULL. Hunks: (1) the round-3 head bracket re-scope + the new round-4 head bracket; (2) row C28; (3) the round-1 record's census sentence + change (7); (4) the round-4 repair record + the standing-lesson paragraph; (5) the tail signature/counter line. ALL record surfaces (head brackets, S8 table row, tail records/signature); ZERO S0–S10 body content, zero displayed equations/lemmas/proofs, no ⟦…⟧ tag added, moved, or removed in the body. THE FENCE HOLDS as chartered ("record-layer only; no S2–S7 content touched; no tags moved" — all TRUE; the enumerated surface list omits the tail counter line, note N-4).
* S9 md5 pins re-verified at HEAD: `rmengine_diag.py` c50c08e3350c2f3f1e9aa50df7914463, output d9118f5a52a5ad8fc87beca4ce00b4fe, results JSON 1ce263df774f89043aec41874995b1bc — all three match the note's pinned values.
* Provenance of the one gap: `git show ab2e471:verification/openmath/rmengine_claims_census_output.txt` = "excluded zones: 11"; `git diff ab2e471 d63f8bb -- …output.txt` = exactly the 11→13 line change. The mismatch quoted in §5 G-1 was CREATED at d63f8bb itself.

## 3. Spine sweep (charge 3 — re-derived standalone; NO mathematical finding)

* **Headline/T-scope:** T-scope(m) = d₀=1 ∨ (e₀≥2 ∧ ∀1≤i≤m−1: e_i≥2); complement d₀≥2 ∧ (e₀=1 ∨ ∃ flat) re-derived (= the fence box and C33 exactly); m=1 coincidence with the RM-1 scope of record re-derived ({e₀≥2}∪{e₀=1∧d₀=1} = {d₀=1}∪{e₀≥2}); the L-MU combined-scope identity [(a)∨(b)] ∧ (e₀≥2∨d₀=1) = T-scope re-derived. Title gloss names both branches. ✓
* **L-NORM (a)–(c):** primitive parts, w = w₀(b)+w₀(c) = w₀(bc) (Gauss valuation), DIGIT-SPLIT on b̂ĉ, restore π^w. (a) w₀(lift(b̄c̄)) = 0 since b̄c̄ ≠ 0 in the field K₀ (nonzero primitive of deg < d₀ = deg ψ̄₀ has nonzero reduction of deg < deg ψ̄₀) — L-child w₀ EXACTLY w, residue b̄c̄, never absent; (b) π^{w+1}X̂ ≥ w+1; (c) Ŷ = 0 at d₀=1 forced by degree (deg b̂ĉ ≤ 2d₀−2 = 0 < d₀ and Φ₀Y-uniqueness). ✓
* **Tree + L-EXACT:** (ID-(i+1)) rearranges the standard/canonical lift displays; E1-priority child inventory re-checked (promotion/exit; correction children at j_i − (P_i−e_ik), dumps a_{d,l} ≤ P_l−1 below, L-NORM triple); L-EXACT = exact stepwise rewriting + L-CANON (monic uniqueness; deg Q < deg Φ_m from deg(fĈ_m) < 2 deg Φ_m) — window-free ✓; exit-cofactor single-digit structural remark via priority (all j_l ≤ P_l−1 for l ≤ m−2 at a level-(m−1) reduction). ✓
* **L-GRID:** λ_l = the truncation lines by the affine recursion (Γ_t^{(l)} = e_{l−1}Γ_t^{(l−1)}, E_l = e_{l−1}E_{l−1}, the j_{l−1} terms cancel — re-derived); integrality; the congruence s_l(λ_l) ≡ ℓ_{l−1}j_{l−1}γ_l ≡ j_{l−1} mod e_{l−1} via γ_l ≡ h_{l−1}, ℓh ≡ 1. Off-grid at own line EMPTY. ✓
* **L-ZMULT:** chains add by linearity; L1 gives δ_l ∈ {0,1}; t_l^Σ = t_l + t_l′ + δ_l ∈ ℤ; EXP-KIT(b) ε-leg z^{−δ_l} cancels the t-leg z^{+δ_l} at l = 1…m−1; sole survivor z_m^{δ_m} (no ε_m in the 𝔷-product — the ε-product runs to m−1) = z_m^{ε̃} with ε̃ = ⌊(s_m(β)+s_m(β′))/e_{m−1}⌋. ✓
* **L-LINE quanta:** (i) Γ_{i+1} − P_iΓ_i = h_{i+1}∏_{i+1<t<m}e_t via γ_{i+2} = e_{i+1}P_iγ_{i+1} + h_{i+1} (w_{i+1}Φ_{i+1} = P_iγ_{i+1}) ✓; exit −w_mΦ_m = −P_{m−1}γ_m ✓; (ii) Δline = 0 by scaling the realize on-line identity — the three conversions E_i·∏_{i≤t<m}e_t = E_m, Γ_l^{(i)}·∏ = Γ_l, (g_i−k)γ_{i+1}·∏ = (P_i−e_ik)Γ_i all re-checked ✓; (iii) π-child ≥ +E_m, Y-child ≥ +Γ₀; the i=0 leg Δline = E_m(g₀−k)h₀ − (P₀−e₀k)Γ₀ = 0 via Γ₀ = h₀E_m/e₀ ✓; (iv) single-digit weight exactness by induction ✓; L-MONO + product-line additivity ⟹ the depth-0 on-line skeleton (C24) ✓.
* **S4.1 both branches + the necessity iff:** (a) interval (S_i/e_i, w_{i−1}P_{i−1}) nonempty from e_i ≥ 2, ρ ≤ P, P ≥ e, invariant (B_{i−1}); base (ρ₀/e₁, P₀) ✓. **(b) re-derived from scratch:** at d₀=1, ρ₀ = P₀−1; interval (S_i, w_{i−1}P_{i−1}); any point gives (B_i) from w_ie_i ≥ w_i > S_i using ONLY e_i ≥ 1, and (A_i) directly; nonemptiness ⟺ w_{i−1}(P_{i−1} − ρ_{i−1}) > S_{i−1} ⟺ w_{i−1} > S_{i−1} (ρ_{i−1} = P_{i−1}−1 for i−1 ≥ 1, and for i−1 = 0 exactly because d₀=1); base (P₀−1, P₀) ≠ ∅; midpoint maintains the invariant; no e-bound consumed. Descent legs re-checked: promotion (A_{i+1}); exit; correction i ≥ 1 (consumed e_i(g_i−k) ≥ e_i, dumped ≤ ρ_l incl. the Y-accounting at l=0); level-1 Δj₀ ≤ −e₀ + [d₀≥2] ≤ −1 iff e₀ ≥ 2 ∨ d₀ = 1 — the fenced e₀=1∧d₀≥2 leg exact. Well-founded on (1/D)ℤ≥0. **Necessity:** flat (B_i) + (A_i) ⟹ w_{i−1} > S_{i−1}; the cascade rearrangement identical at each step; bottoms at w₀P₀ > w₀ρ₀ ⟺ ρ₀ < P₀ ⟺ d₀ = 1; converse = branch (b): an IFF ✓. Consistency display: the sealed m=2 point satisfies (A₁)/(B₁) ⟺ e₁ ≥ 2 and exits via (A₁) at e₁ = 1 ✓.
* **S4.2 census (C23/C27):** my own leg-by-leg pass concurs — division/valuation arithmetic, class-level identities, congruences degenerating cleanly at e = 1 (s ≡ 0, δ = 0, ε ≡ 1), h ≥ 1 for the quantum, T-TERM the only carrier; NO leg but L-MU consumes e_i ≥ 2. ✓
* **DEV-(m):** induction on μ; leaf = L-LINE(iv); (D2) via L-MONO + the single-digit exit remark (w_m(cofactor) = its line = parent line − w_mΦ_m); (D3) READ-ADD applicable from (D2) (C9); above-line ⟹ 0; off-grid-at-own-line EMPTY (L-GRID); T-tele(1) re-derived including the sign and the collapse −Σψ̄₀,k z₁^{k−g₀} = 1 from ψ₀(z₁) = 0; T-tele(i+1) re-derived: λ_{l>i+1} unchanged, t_{i+1} shift k−g_i, Δλ_i = (P_i−e_ik)γ_{i+1}/e_i = β_k exact, L-ZMULT at level i, B-READ inside the k-sum (finite sums; C_k cells on-line on-grid by realize), Σ_d res(c̄_d)𝔷(d;β_k) = τ_k = ε_i(β_k)^{−1}ψ_{i,k} by (R3), EPS-MULT cancellation ε_i(λ_i+β_k)z_i^{ε̃_k} = ε_i(λ_i)ε_i(β_k), then ψ_i(z_{i+1}) = 0 collapse. ✓
* **L-VNZ:** re-walked in full — slot cap t ≤ g_{m−1}−1 from deg f < deg Φ_m; the min-achieving digit is automatically on-grid (λ = e_{m−1}w_{m−1}(f_j) + jγ_m ⟹ λ ≡ jh_{m−1}); its slot weight equals w_{m−1}(f_j) so the IH applies; on-grid non-achievers read 0 (above slot line); distinct achievers occupy distinct z_m-powers; a nonzero K_{m−1}-polynomial of deg < [K_m:K_{m−1}] is nonzero in K_m. ✓
* **RM-GEN, all four clauses:** (C-W_R) from (D2) + line ≥ β* on every initial cell; (C-W_Q) from the exit-cofactor exactness + L-MONO; (C-EPS) = L1 at the (e_{m−1}, h_{m−1}) read; (C-READ): junk initial children and above-line x-cells read 0, surviving pairs share the target pair (λ, γ_{m+1}) so ε̃ = ε′ UNIFORM, L-ZMULT + B-READ factor the double sum into z_m^{ε′}·v·τ_m. ✓
* **S6 grades:** LAW-DCX-(m+1) m=1 specialization = [IL3]'s sharp form (u₁ = γ₂, c₀ = 0) ✓; m=2 = the v2 law verbatim (u₁ = (γ₃ − s₂(γ₃)γ₂)/e₁) ✓; conjecture grade at every m ≥ 2 use site (C12 re-checked) ✓; L-CAP re-proved (pool₁ ≤ 3e₀−3 ⟹ c₁ ≤ 2; pool_l ≤ 3e_{l−1}−1 ⟹ c_l ≤ 2; e = 1 leg pool = c_{l−1}) ✓; 11b crossing law from the child inventory alone, T-scope pin present at the μ-citation ✓; 11c honestly HEURISTIC ✓.
* **COR-A:** ∧_{m=2}^{n−1}[d₀=1 ∨ A_m] = d₀=1 ∨ A_{n−1} = T-class(n) (A_m decreasing; distributivity re-checked) ✓; n = 1,2 empty call family, no T-condition ✓; e_{n−1} absent ✓; at-acceptance rider present ✓. COND-B stands conditional ✓.
* **Records:** S8 grade rows match body grades; S10 fences match the proved perimeter; S11 DAG consistent (L-VNZ consumed by no theorem clause; L-LEX by nothing; W-WIN by nothing); the tail counter line's premise ("the r4 edits are record-layer only") verified against the actual r4 diff ✓.

## 4. Fresh eyes (charge 4)

Beyond §§1–3: the Unit-paragraph composition header ("Acceptance counter 0 — no hostile pass has run on THIS note") read as frozen composition metadata per the genre convention, counter current in the brackets/tail (six passes let it stand; no finding); the round-1 head bracket's R1–R6 dispositions consistent with the tail record's seven-change census (the R-list is a disposition-by-finding record, not a statement census); the LEX_SKIP carve-outs still match nothing; the WMULTDCX files added in the same commit are outside this note's perimeter. The one substantive fresh-eyes finding is G-1 below — new at d63f8bb, not missable by any earlier pass (the mismatch did not exist before the r4 commit).

## 5. Findings (quote-and-classify)

**G-1 [JUSTIFICATION GAP — the round-3 record's census-closure quote no longer matches the committed arbiter surface it cites; introduced by the r4 commit itself; zero chain impact].** Round-3 repair record, verbatim: "**The census closure (the exit gate of this repair), machine output quoted from the committed `rmengine_claims_census_output.txt`:** MACHINE CENSUS (tag grammar): 67 keyword lines in body (55 row-tagged, 12 nc-tagged); 33 table rows (2 table-only); excluded zones: 11 / CENSUS CLOSED — zero violations." At HEAD the committed `rmengine_claims_census_output.txt` reads "excluded zones: **13**": the r4 commit (d63f8bb) added two record surfaces (the round-4 head bracket and the round-4 tail record), re-ran the census, and re-committed the output with the changed zone count — `git diff ab2e471 d63f8bb -- …output.txt` is exactly the 11→13 line. The quote of "the committed" file is therefore false as stated at the committed HEAD, and the change is disclosed NOWHERE in the note: the round-4 record says only "the census re-run CLOSED after the edit (output re-committed)", which asserts reproduction-of-closure and is silent on the changed numbers; no surface states 13 or annotates the stale 11. This fails the note's own r4 policy on its own arbiter surface ("machine-checkable surfaces — the tag census, the diff — are the arbiters") and the note's own S9 standard (when the committed diag artifacts changed at r1, the D7 record disclosed the new md5s and even a 0.1 s elapsed delta). Grading per this arc's consistent precedent: PE3-G-1 (quote-of-committed-text keys broken by a later commit of the same note = GAP), PE3-G-2/PC3-g3/PE4-G-1 ("every false-as-stated record claim graded a gap"; disclosure elsewhere — here not even present — does not cure). Counter-reading acknowledged: the block is framed as "the exit gate of this repair" (round 3), and the quote was accurate at ab2e471 — but the citation is to "the committed" file, unpinned, and the note's standing practice is to annotate stale record sentences in place (it did so for two-word deltas). Substantively harmless: the census CLOSES on re-run, the body counts (67/55/12/33/2) are unchanged, and the +2 zones are the round-4 record surfaces under pre-existing zone classes. Record-layer; zero chain impact; the cure is a one-line annotation or re-quote.

**N-1 [NOTE — archived round-3 disposition cell stale at HEAD; refutation disclosed adjacently].** Round-3 record disposition row, verbatim: "CURED: the round-1 record's statement-diff sentence and row C28 now enumerate all SIX r1 statement changes per the actual diff". At HEAD they enumerate SEVEN with the completeness claim withdrawn. Unlike G-1, the staleness is explicitly re-adjudicated by name in the same note (round-4 record row PE4-G-1: "the r3 'SIX changes' completeness claim was itself under-enumerated"; the [r4] brackets at the round-1 record and C28) — an archived per-round verdict cell superseded on the page. PE4-N-1 precedent (disclosed-adjacent staleness = note).

**N-2 [NOTE — a surviving completeness word in an archived round-2 disposition cell].** Round-2 record disposition row, verbatim: "CURED: rows C23–C28 added (the complete PE2 census absentee list + the repair-record claim)". The S8 charter itself records that the r2 completeness claim was refuted by PE3's 136-hit census (the L-LEX witness, row C29). The cell survives readable as "the complete list of absentees PE2's census found" (true of PE2's report) rather than "the complete absentee list" (refuted); both PE3 and PE4 read this cell without finding. Archived, ambiguous-but-defensible, refutation documented in-note.

**N-3 [NOTE — the PE4-N-1 re-scope's sectional gloss omits the pre-S0 head-matter tag lines].** Round-3 head bracket as re-scoped at r4, verbatim: "The r3 edit adds trailing tags throughout the body (S0–S7, S9, S10 lines)". The r3 diff also added tags on head-matter lines BEFORE S0: the headline-consequence line (⟦C32⟧) and the sealed-artifacts head-declaration paragraph (⟦C15⟧, ⟦C12⟧) — census body, outside every named section. "Throughout the body" is correct; the fence clause is TRUE; the parenthetical inventory is under-inclusive by the head-matter lines. Wording precision, the PE4-N-1 genre.

**N-4 [NOTE — the round-4 record's edited-surface list omits the tail counter line].** Round-4 record, verbatim: "The r4 edit is record-layer only: the round-1 record's census sentence, row C28, the round-3 head bracket, this record, and the head round-4 bracket." The actual r4 diff also rewrote the tail signature/counter line ("Acceptance counter 0/2 …" → "Acceptance counter: the Codex leg CLEAN on ab2e471 (PC4) …"). The operative claim ("record-layer only") is TRUE — the signature is a record surface — and under the note's own new policy the enumeration claims no completeness; inventory precision only.

## 6. Verdict block

The mathematics survives this pass whole: every spine item of the charge re-derived standalone (both L-MU branches, the necessity iff, all telescopes, all four clauses, L-VNZ in full) with zero findings; both machine referees byte-identical on re-run; the r4 statement fence verified against the actual diff (record surfaces only, zero body content, no tags moved); change (7) faithful to the actual r1 diff including both verbatim clauses and the secondary witness; C28's per-pass attribution accurate against all six archived reports; the completeness withdrawal discharged at every operative site. What does not survive is one record-layer accuracy defect NEW at the r4 commit itself: the r4 edit changed the committed census output (excluded zones 11 → 13, the two new round-4 record surfaces) and left the round-3 record's verbatim quote of "the committed `rmengine_claims_census_output.txt`" stale, with the changed count disclosed nowhere in the note. By this arc's own consistently applied rule (false-as-stated record claims about machine-checkable surfaces are gaps: PE3-G-1/G-2, PC3-g3, PE4-G-1) and the note's own arbiter-surface policy, that is a JUSTIFICATION GAP, not a note — zero chain impact, one-line cure. Four notes besides, none blocking. The bar is CLEAN; this is not CLEAN.

RMENGINE-PE5 FINDINGS: 0 critical, 1 gaps
VERDICT: GAPS-ONLY
