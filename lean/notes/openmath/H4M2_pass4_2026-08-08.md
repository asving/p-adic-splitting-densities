1. **CONFIRMED — REV-3 fold complete.** The former “consumes (c) ONLY” tally is retired at all live audit sites. The note consistently says `(1) ⇒ (2)` consumes “**(b) AND (c), plus [IN-F1]**”; §5 and §6 agree. The old REV-2 wording survives only inside an explicitly superseded historical record.

2. **CONFIRMED — four-leg dependency tally.**

   - `(2) ⇒ (3)`: `[IN-C](a)/(b)/(d) + [IN-R1] + [IN-U]`.
   - `(3) ⇒ (1)`: no additional named input; “residuals are irreducible by construction” was already displayed as part of object (i-a).
   - `(1) ⇒ (2)`: `[IN-C](b)/(c) + [IN-F1]`.
   - `l4_persist`: no DICT-0 clause; its F1/F2 content is explicitly packaged in that assumed row.
   - Literal transported-key equality is unused.

3. **CONFIRMED — factor-correspondence well-definedness.** The cited results support the displayed argument: polynomial rings over UFDs are UFDs ([Stacks 0BC1](https://stacks.math.columbia.edu/tag/0BC1)), and relative primality is preserved after field extension ([Stacks 09GY](https://stacks.math.columbia.edu/tag/09GY)). The gcd-sense/non-comaximal qualification is correct.

4. **GAP — unresolved base-stage indexing in `(1) ⇒ (2)`.** The proof says:

   > “let j₀ be the FIRST failing stage (base stage included…)”

   and then:

   > “The sub-prefix through j₀ − 1 is split-free, so DICT-0 applies to it with k = j₀ − 1”

   before concluding that realized residuals have degree `g_{j₀}/gcd`. But the note separately assigns the base residual degree `d`, while `g_j` denotes later stage residual degrees. Clause (c) likewise calls the `k = 0` read both “the base residual” and “ψ_{k+1} = ψ₁”, although earlier `ψ₁` has degree `g₁`. Thus the base-failure case is intended but not coherently indexed. It needs a separate `k = 0`, degree-`d` argument or a declared shifted convention. No new mathematical input appears necessary, so this is a gap rather than a critical failure.

5. **GAP — stale §3 overstatement of DICT-1’s conclusion.** Section §3 says DICT-1 states:

   > “at split-free prefixes the reconstructed run realizes the verbatim-transported residuals”

   and calls this its “transport identity.” But DICT-1(3) states equality of numerical tuples, valuations, and embedded fields—not literal residual-polynomial equality—and its `(2) ⇒ (3)` tally deliberately does not consume clause (c). The Lean row `coincide` is only `pdata` equality. Actual persistence is separately assumed through `l4_persist`. This is inter-revision sediment: either “transport identity” must be weakened to ledger/data coincidence, or residual association must be derived using clause (c), changing the advertised tally.

6. **CONFIRMED — honesty fences remain intact.** DICT-0’s four clauses remain explicitly OPEN; `[IN-R1]`, `[IN-U]`, `l4_persist`, and `coincide` remain named assumptions. No faithful `DictModel`, `GcdSlice`, factor-aware `Pos`, reconstructed tower, or F₉ dictionary instance is constructed. The Lean face remains hypothesis-row-only.

VERDICT: GAPS-ONLY