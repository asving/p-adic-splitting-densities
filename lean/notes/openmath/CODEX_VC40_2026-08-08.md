# CODEX LEG 2 (wave-21 queue item (2)) — VC40: THE END-TO-END CONFIRMING PASS ON ROOT REVISION 39 — 2026-08-08 campaign

## S0. Charter and provenance

Unit: the second Codex leg of the wave-21 closure program's post-reset queue
(ledger `BRIDGE_ADJUDICATIONS_2026-07-30.md`, "CODEX WINDOW RESET" ordering;
queue item (2)): **VC40**, the confirming end-to-end pass owed on the
REVISION-39 ROOT text (charge of record: ROOT §45, "VC40 charge" block;
frozen-history trim MANDATORY per checklist rule 7). Precedent legs: VC38
(0C/9G → r38b → VC38b CONFIRMED), VC39 (0C/6G → r39b + the
whitespace-normalization amendment → VC39b CONFIRMED) — the TWENTIETH
re-anchoring. Until VC40 lands, the acceptance citation points at the
REVISION 38 + r39b text.

**PIN RETARGETING (the queue item's intent, retargeted to the current
corpus — disclosed in the charge header).** The charge as written is owed "on
this REVISION-39 text" (= the r40 fold, commit c357bf0). The audit was run on
the ROOT file at commit **24ef527** = REVISION 39 **plus the same-day
[RMG-c1] arc-count correction** (the fold's own flag, adjudicated at the
ledger: 14 hostile passes / 7 rounds, not the block's as-dated 12) — the two
commits are the ONLY ROOT-touching commits in the window 2d3080f..24ef527,
and the ROOT file at repository HEAD is byte-identical to the 24ef527
version (verified at charge build; the 282 post-pin commits touch other
files and are OUT OF PERIMETER per the charge's standing disclosure (d) —
the W-arc/weld/HEX3/SIGMALAW/SURV-K2 progression claims nothing into
REVISION 39 and REVISION 39 claims nothing from it). Adjudication rule given
to the auditor: a pasted [RMG] block faithfully carrying the block-as-dated
12-pass sentence is NOT a finding (paste fidelity is the requirement; the
supersession is [RMG-c1]); a ROOT site asserting 12 passes in its OWN voice
outside a faithful paste IS one.

## S1. The charge (built per the VC39 executed pattern + checklist rules 1-8)

- Charge file: `/tmp/openmath/VC40_charge.txt`, **834,906 bytes** (cap
  1,048,576; stderr checked clean at launch — no silent cap death).
  Builder scripts + parts: `/tmp/openmath/vc40_parts/` (build_vc40.py,
  build_fix.py, build_assemble.py). Committed copy of the charge:
  `VC40_charge_2026-08-08.txt` (this directory).
- FROZEN-HISTORY TRIM geometry: BLOCK 1 = ROOT lines 1-5248 (§§0-6 incl.
  §3.4, byte-exact); BLOCK 2 = ROOT lines 10618-12375 (§43 + §44 + §45
  through EOF, byte-exact — carries the VC38/VC39 charge blocks of the
  inheritance chain AND the complete §45); all 11 window hunks of
  `git diff --unified=0 2d3080f..24ef527` fall inside the two blocks
  (verified programmatically); omitted middle = §§7-42 (lines 5249-10617),
  proved byte-identical to the VC39b-certified 2d3080f text — **md5
  91cab65da3508ad21d2c939a0c289ad5, the SAME middle md5 as the VC39
  charge's own trim proof** (provenance continuity through VC38/VC37).
- Appendices: B = 32/32 commit resolutions + both fold-unit `--stat`
  footprints (c357bf0 = ROOT+ledger ONLY; 24ef527 = the four disclosed
  [RMG-c1] sites) + seven verbatim ledger slices ([RMG] acceptance entry
  w/ the 154-token block; r40 entry; [RMG-c1]; bbbd394 re-anchor; VC39
  adjudication + whitespace amendment; 9f9910c convention; a46578e [ILN]
  block) + the PROJECT_STATE Q1-closed slice at the pin + note-file
  listings; C = token/byte transcripts; D = pattern census; E = sorry
  census.
- Orchestrator pre-verification (run at charge build, BEFORE launch —
  transcripts pinned into the charge):
  - **[RMG] content-token check: 154/154 tokens, all THREE ROOT pastes
    identical to the ledger defined-term span** (whitespace-normalized per
    the VC39b amendment) — reproduces the fold's own 154/154 ×3 claim.
  - **[ILN] byte-identity: all FOUR r39 pastes byte-UNCHANGED** between
    2d3080f and 24ef527 (pairwise raw-span comparison).
  - **Pattern census: ALL 114 r39 rows + the joint TransDeep/TRANS-DEEP row
    re-grepped** over the full pinned text (exceeds the charge's ≥ 8 floor);
    **all FOURTEEN new r40 markers reproduce their recorded values exactly
    at c357bf0 (14/14)** — the fold's fixed-point claim confirmed; the
    24ef527 column isolates the [RMG-c1] bracket's own additions. Per
    §45.(vii) this transcript becomes the new baseline of record.
  - **Sorry census: `git diff 3980471..24ef527 -- '*.lean'` EMPTY**
    (the fold's diff-invariance claim reproduced) **+ independent
    block-comment-aware scan at the pin: TOTAL 34 = 33 LeanUrat + 1
    scratch** — the census of record reproduced exactly.
- Launch: session/thread `019fe04b-ff19-79b3-9d01-27a92c41dd5f`, model
  gpt-5.6-sol, reasoning effort HIGH, `codex exec --json` via stdin,
  nohup+disown; log `/tmp/openmath/vc40_out.jsonl`, stderr
  `/tmp/openmath/vc40_err.log` (empty at launch+60s). Codex window 0% used
  at launch (post-reset, resets Aug 15).

## S2. VERDICT

(TO BE FILLED FROM THE RUN ARTIFACTS — never self-confirm; this section is
written only from the Codex output + orchestrator adjudication.)

## S3. Findings and adjudication

(TO BE FILLED.)

## S4. Consequences

(TO BE FILLED — on CONFIRMED: the acceptance anchor moves to REVISION 39 +
[RMG-c1] / VC40 (the TWENTY-FIRST re-anchoring is the orchestrator's fold,
not this unit's claim); on NOT-CONFIRMED: the cure batch + VC40b per the
VC38/VC39 precedent.)

## S5. Artifacts

- `/tmp/openmath/VC40_charge.txt` + `/tmp/openmath/vc40_parts/` — the charge
  and its builder (committed copy `VC40_charge_2026-08-08.txt`).
- `/tmp/openmath/vc40_out.jsonl` — the raw event stream (to be archived in
  the report file on completion).
- `VC40_report.md` (this directory, on completion) — the raw report +
  adjudication, per the VC series format.
