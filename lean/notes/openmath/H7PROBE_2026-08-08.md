# H7PROBE — the (H7) first probe: (I-τ)/K-LOC decided at the compiled instance bin

**Unit:** synthesis-6 queue #7 (`docs/SYNTHESIS_PASS6_2026-08-08.md` — pass-6 F4 finding
"(H7) LIVE-BUT-NEVER-PROBED — the pair has no compiled or numeric decision point of its
own"). **Protocol:** countermodel-first [M]-row entry — pin the exact statements, decide
each at the cheapest compiled instance; HOLDS / FAILS (→ honest restatement) / VACUOUS
(→ cheapest non-degenerate extension or why none is cheap). **Artifact:**
`lean/LeanUrat/Scaffold/HDischarge/H7/H7Probe.lean` (12 new theorems, `lake env lean`
green, all Lean-core — census in-file §E). **Authority for the statements:**
`lean/blueprints/HDISCHARGE_H7.md` REV 2 (§1.1 K-LOC, §2.1 IT-1/IT-2) + ROOT §3.1 (H7).

## 1. The statements pinned (compressed; verbatim forms in the blueprint)

* **(I-τ):** at every realized site s with continuing branch roster m ≥ 2 and K-FACT
  tuple dec = f₀·∏f_j: Σ_c = μ_N(C₀ × ∏_j C_j) — C₀ the pinned f₀ cell (τ(c)-keyed, no
  event), C_j the K_j-keyed per-branch factor strata, K_j ≤ thr(c)+ρ(c).
* **(K-LOC)** (= M07 OL-3): given KL-0, ∃ cell-constant ε_j(c) = (δ_{j,r}, u_{j,r})
  with (i) λ_{j,r}-side translation of N_r^−, (ii) R_λ unit-scalar transport,
  (iii′) event pullback to E_j mod p^{K_j} on the polydisc coordinate.

No restatement is pinned by this probe: no countermodel exists at the bin because the
statements cannot be INSTANTIATED there (below) — the REV-2 forms stand as drafted.

## 2. The instance walk (bin: toyStage3, bStageP, td2V4, H₃rc; + the walk's find)

| instance | type | (I-τ) | (K-LOC) full | KL-1a count face |
|---|---|---|---|---|
| `toyStage3` (G1_toyGate:2568) | `Stage 3 (ZMod 3)` | VACUOUS (type) | VACUOUS | **HOLDS** at ℤ/3 (`kl1a_decided_zmod3`) |
| `bStageP` (HK13:553) | `Stage 2 F4` | VACUOUS (type) | VACUOUS | **HOLDS** at F4 (`kl1a_decided_F4`) |
| `H₃rc` (H1/RCWitness:401) | `History 2 F4` | VACUOUS (type) | VACUOUS | **HOLDS** at F4 (same) |
| `td2V4` (B2D/HuniV4Probe:68) | `LedgerStratumDataV4` | VACUOUS (type) | VACUOUS | **HOLDS** at ℤ/2 (`kl1a_decided_zmod2`) |
| `toyTreeA`+`toyLedgerA` (walk find) | `VTree`/`SiteLedger` | STATABLE-BUT-DEGENERATE | DEGENERATE (diagonal) | — |

VACUOUS (type): `Stage` = one OM read of one branch (single key Φ; `reps` = the DEF-3
U-representative list, NOT a roster — anchors `toyStage3_anchor`, `bStageP_anchor`);
`History` = one chain (`h3rc_anchor`: 3 nodes; roster vocabulary starts at
`VTree.chains`/`CellData.branchSetOf` and no compiled VTree contains H₃rc); the ledger
carrier has window slots on ONE line and no factorization tuple in the type
(`td2V4_anchor`; its `slotUniform_td2V4` is (FRESH)/huni content, not (H7)).

## 3. Findings

* **P-1.** The charter bin is roster-free: neither face can be stated at any of the four
  instances; no countermodel and no support is obtainable there. Pass-6 F4's
  "never-probed = no decision point of its own" is confirmed AT COMPILE LEVEL.
* **P-2.** The corpus's ONLY compiled m ≥ 2 sibling object is `toyTreeA` at the tA1 site
  — now certified Lean-core (`toy_roster`/`toy_roster_card` card-2 roster
  {toyLeafA, toyLeafB}; `toy_sibling_pair` + `toy_sibling_parent`: tA2a ≠ tA2b both
  extend tA1). But it is TRIPLY DEGENERATE as an (H7) decision point:
  (a) diagonal site read — `branchCellOf := fun es _ν => toyCellA es` (G1_toyGate:723),
  sibling reads definitionally equal, so any K-LOC check compares a function to itself
  (a degenerate HOLDS, review-finding-16 genre); (b) `sides := fun _ => 1`
  (G1_toyGate:1496) with the split legs discharged by `omega`-absurd — no compiled leg
  exercises an m ≥ 2 presentation; (c) the carrier chain `toyModel`/`toyCA`/`toyLedgerA`
  depends on `sorryAx` (the two `mem_realizable` sorries, G1_toyGate:561/:794; measured
  by scratch `#print axioms` 2026-08-08 — kept OUT of the probe's compiled perimeter to
  hold it Lean-core).
* **P-3.** The one (H7) fragment with compiled decision content anywhere: KL-1a's
  order-1 count face (H7-C2, landed proved) — DECIDED HOLDS at every bin field (ℤ/3,
  F4, ℤ/2) by explicit ψ = X, g = X+1 instantiation. KL-0/KL-1b/1c/KL-2/KL-3/KL-4,
  (iii′), IT-2⊇, IT-4b: no compiled decision point exists.
* **P-4 (route).** Cheapest non-degenerate Lean extension: NONE cheap. The named
  cheapest is H7-W3a's `gate_order0_semantic` (decide-small p=2,n=2,N=2), blocked on
  OL-2 — which this probe SHARPENS: an OL-2 inhabitant must break BOTH P-2
  rfl-degeneracies (sides ≥ 2 at a branching site AND non-diagonal `branchCellOf`) on a
  sorry-free TreeModel. Near-term decision point is therefore the NUMERICS harness
  N-H7-1 (`verification/openmath/h7_kloc_probe.py`, blueprint §5 — the read-conformance
  falsifier b), i.e. exactly the pass-6 F4 charter artifact; the Lean side has nothing
  cheaper to decide until OL-2 or the readsOf debt moves.

## 4. Build & fences

`lake env lean LeanUrat/Scaffold/HDischarge/H7/H7Probe.lean` — green, exit 0; §E census:
all 12 declarations `[propext, Classical.choice, Quot.sound]`. New decls only; no
existing statement touched; no Tier-S Prop shells added (Tier-M rows stay §3.7-owned);
`MovesT.SibJcRows`/`CellData` + parked items 22–29 untouched.
