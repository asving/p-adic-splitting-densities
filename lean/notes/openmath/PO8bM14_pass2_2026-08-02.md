# P(O-8b/M14) composite pass 2 — Codex hostile verify (ROOT r22 unit, wallclock 2026-08-02; campaign label 2026-08-08)

Charge: H4M7_rootsync_brief.md §2 step (iii), targets (a)-(f) + the added currency target (g).
Composite: M14-eq2-alphabet_attempt.md (full) + H4M1_rpack_attempt.md (full, ACCEPTED text) +
H4M2_dict_attempt.md §§0-3 (REVISION 4) + blueprint §§0-2 + ROOT §3.1/§3.2 row texts.
Model: gpt-5.6-sol, reasoning effort HIGH, fresh context, read-only. Prompt: 177,703 bytes (self-contained).
Session: 019fc304-23f0-72c2-940f-e3bde2d46693.

## Verbatim verdict

PASS-2: 0 CRITICAL / 1 GAP

1. JUSTIFICATION GAP — M14 Lemma 1 omits the top-space scale case.

   M14 states:

   > “(b) γ* = γ/e_{j+1} (γ at the top) uses only e_{j+1}”

   But current H4-M1 explicitly distinguishes:

   > “γ* := γ/e_{k+1} for k+1 ≤ K …;  
   > γ* := γ at the TOP SPACE (k = K): … no read K+1 exists, hence no e_{K+1}”

   M14 applies Lemma 1 to positions “including x itself at the top space,” where its displayed proof refers to a nonexistent final stretch/index. The lemma’s top-space conclusion is therefore not justified as written. Assuming the separately supplied H4-M1 top-space clause permits the induction to continue, so this is a gap rather than a critical break.

Walk-record:

- (a) One itemization seam: M14’s top-space use of the numerical skeleton is stale relative to M1’s current two-case scale display; otherwise (R1)–(R4) match.
- (b) CLEAN — arbitrary \(F_q\) specializes to \(q=p^\delta\), with \(\delta\) consistently absolute.
- (c) CLEAN — residue fields, residuals, bases, and alphabets are rebuilt over \(F_q\); no cardinality substitution was used as construction.
- (d) CLEAN — M1 §1.3 supplies every hypothesis used by M14 Lemma 2, including D.1(c)/(e), coefficient stretch, positivity of \(w(p)\), and \((L^{\mathrm{coeff}})_0\).
- (e) CLEAN — Theorem C(a)/(b), PIN-WELLDEF, and (ZC) are not consumed by the alphabet-law derivation.
- (f) CLEAN — the base alphabet uses the \(x\)-dependent \(p^vC_1\)-lift family onto \(F_1\), while unit constants are correctly rejected for \(d>1\).
- (g) CLEAN apart from finding 1 — M2 revision 4 composes through the factor-correspondence match, all four displayed DICT-0 clauses, and the key-free model. M14’s same-keys Lemma 4 remains explicitly unconsumed.
