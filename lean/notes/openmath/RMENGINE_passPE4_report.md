# RMENGINE pass PE4 report (Fable, round 4 — THE SECOND ACCEPTANCE ATTEMPT) — 2026-08-08 campaign (wallclock 2026-08-04)

**Pass:** RMENGINE-PE4, hostile content verification, fresh context. Verify only; fix nothing.
**Object:** `lean/notes/openmath/RMENGINE_2026-08-08.md` at commit **ab2e471** (confirmed: `git log -1 --format=%h -- <file>` → ab2e471; 1392 lines). The r3 text.
**Bar:** acceptance = this pass AND the concurrent Codex PC4 both CLEAN (0 critical, 0 gaps; notes do not block).
**Charge:** (1) r3-remedy discharge, each hostile — headline gloss, six-change census vs the actual `git diff bf18bfe..8835d20`, all 33 row keys as exact quotes, attribution accuracy per each pass's report, rows C29–C33; (2) THE TAG GRAMMAR — census re-run, excluded-zone scan, all 12 ⟦nc⟧ tags challenged individually, one-tag-per-line rule, the script's ledger-quote zoning fix; (3) full spine sweep, re-derived standalone; (4) machine legs — `rmengine_pe2_ext.py` re-run read-only, the r3 statement fence vs `git diff 423b399 ab2e471`.

---

(Findings appended incrementally below.)

## 1. R3-remedy discharge (charge 1)

### 1.1 Headline gloss (PC3-g1 remedy) — ACCURATE
Title, verbatim: "on the T-scope (either d₀ = 1 — degenerate flat reads admitted — or all reads below the top non-degenerate [r3, PC3-g1 ...])". T-scope(m) = d₀ = 1 ∨ (e₀ ≥ 2 ∧ e_i ≥ 2, 1 ≤ i ≤ m−1); "all reads below the top non-degenerate" = e₀..e_{m−1} ≥ 2 exactly; both branches named. DISCHARGED.

### 1.2 The six-change census vs the actual `git diff bf18bfe 8835d20` — UNDER-ENUMERATED (finding G-1)
I ran the diff myself (715 ± lines) and read every statement-bearing hunk. The six enumerated changes are all real and correctly described, and the census's substantive rider — "all repairs or strengthenings, none weakening an established clause" — is TRUE (independently confirmed: T-scope/T-class moved strictly outward; L-LEX content retained; v = 0 flip is a strengthening; COR-A's spurious condition removed; the (D3) appeal replaced by an emptiness proof; row 11c honestly downgraded a never-proved identification). But the completeness claim fails on a seventh claim-content change of exactly the enumerated genre — see finding G-1 (§5).

### 1.3 All 33 row keys as exact quotes — GREEN
Machine leg K1 (`rmengine_pe4_ext.py`, new, read-only): all 44 key strings across the 33 rows (C7/C22/C25/C26 two keys; C27 four; C31 four; C32 two incl. the ellipsis echo) match the committed text verbatim after the charter's normalization (whitespace-normalize + strip ⟦…⟧ tags; C32's "…" key checked piecewise in order). Table-only rows: C14's key sits in grade row 9's cell, C28's in the round-1 repair record — both verified at their stated surfaces. The re-quoted C7/C10/C26/C28 keys all live: n ≥ 2 occurrences each (body + table). K1 VERDICT GREEN, zero dead keys.

### 1.4 Attribution accuracy — VERIFIED at all four sources
* PC2 report item 7 quoted correctly: "the local command launcher failed because `bubblewrap` is unavailable ... treated solely as stated evidence" — the r3 bracket's "execution-less in its sandbox (bubblewrap unavailable, its report item 7) — verified it CONTENT-ONLY" and C28's basis ("PC2 verified content-only against the supplied text (no execution — its report item 7)") are now accurate.
* PE2 report §4 confirmed: "git diff bf18bfe 8835d20 on the note (511+/202−), all grade-bearing lines extracted" — PE2 did run the actual diff; PE3 §1.5 re-verified. The bracket's "verified against the actual git diff by PE2 (re-verified by PE3)" is accurate.
* Round-3 verdicts: PC3 report = 0 critical / 3 gaps, GAPS-ONLY ✓; PE3 report = 0 critical / 3 gaps + 2 notes, GAPS-ONLY ✓; finding names PC3-g1/g2/g3, PE3-G-1/G-2/G-3/N-1/N-2 all match the archived reports; "FOUR consecutive hostile passes with ZERO critical findings" = PC2/PE2/PC3/PE3 ✓, every round-3 finding record-layer ✓.

### 1.5 Rows C29–C33 — ALL TRUE (each re-derived)
* C29: my own grep of every L-LEX site (remark 680, grade row 5, DAG 1269, records) — no consumer; T-TERM's proof (675–678) cites L-MU strict descent alone. TRUE.
* C30: s_l(λ)·γ_l ≡ ℓ_{l−1}λ·h_{l−1} ≡ λ mod e_{l−1} (γ_l ≡ h_{l−1}, ℓh ≡ 1), so j_{l−1} ≡ s_l(λ_l) ⟹ e_{l−1} | (λ_l − j_{l−1}γ_l): λ-integrality rides t-integrality with no further input. TRUE.
* C31: all four "automatic" sites re-derived — the S3.1 consequence, (D3) first branch, and the T-tele header are literal L-GRID applications; L-VNZ's slot congruence is λ = e_{m−1}w_{m−1}(f_j) + jγ_m ⟹ λ ≡ jh_{m−1} mod e_{m−1}, the same congruence. No call site consumes a separate on-grid hypothesis. TRUE.
* C32: COR-A given row 8 = RM-GEN on T-class(n) (pins inside the class) + the three accepted packages with their own residuals riding per their head declarations; off-DCX excludes the DCX conditionality; the [IL3]† fenced corner (e₀ = 1 ∧ d₀ ≥ 2) is disjoint from T-class(n). No further conditionality found. TRUE.
* C33: ¬[d₀ = 1 ∨ (e₀ ≥ 2 ∧ ∀e_i ≥ 2)] = d₀ ≥ 2 ∧ (e₀ = 1 ∨ ∃ flat) exactly (e ≥ 1 class pin); the S4.1 cascade is an iff (re-derived §3). TRUE.

## 2. THE TAG GRAMMAR (charge 2)

**(a) Census re-run.** `python3 verification/openmath/rmengine_claims_census.py`: exit 0, output byte-identical to the committed `rmengine_claims_census_output.txt` (diff empty): "67 keyword lines in body (55 row-tagged, 12 nc-tagged); 33 table rows (2 table-only); excluded zones: 11 / CENSUS CLOSED — zero violations." REPRODUCES.

**(b) Excluded zones scanned** (K4 inventory: 3 head repair brackets, 3 †-ledger pastes, S8 1038–1129, S11 1255–1393 with the 3 repair records nested). Contents verified record/aggregate/paste only: the brackets record repairs (one wording imprecision — note N-1); the pastes are the byte-frozen ledger blocks (their internal keywords are upstream claims, untaggable without breaking VC39b paste fidelity); S8 = the two tables + the charter (the aggregating surface itself); S11 = DAG/charges/records. S9 and S10 are correctly NOT excluded and carry tags. No hidden body-claim found in any zone.

**(c) All 12 ⟦nc⟧ tags challenged individually** (K2 inventory, lines 124, 345, 442, 497, 551, 784, 795, 803, 991, 1134, 1214, 1248):
124 "full pastes recur only at operative consumption sites" — citation-formatting convention (out-of-scope by all prior passes; the operative paste is marked at S0.2) ✓nc. 345 "nonzero τ's only" — [ILN]† S4.1 realize structure, an upstream note's own claim ✓nc. 442 "the only j₀-resupply" — arithmetic narration inside W-WIN's m = 2 path argument (a display consumed by nothing, C5; I re-checked the narration: at m = 2 only a level-2 correction raises j₀, and it drops j₁ by ≥ e₁ first) ✓nc. 497 "counter C-GRID never fired" — measurement prose ✓nc. 551 "d₀ ≥ 2 only" — narration of L-NORM(c)'s proved clause, cited inline; mathematical content graded at row 1a, not a chain-dependency claim ✓nc. 784 "nothing to prove" — rhetoric (the case is empty) ✓nc. 795 "(only j₀ changed)" — arithmetic narration ✓nc. 803 "the exit contributes nothing to R₀" — arithmetic narration (exits join Q by construction) ✓nc. 991 "AT ACCEPTANCE ONLY" — ledger procedure ✓nc. 1134 "READ-ONLY" — procedure ✓nc. 1214 "the only non-inserted delta" — re-run measurement prose ✓nc. 1248 "all read-only" — procedural fence ✓nc. NO nc tag is a disguised dependency claim.

**(d) One-tag-per-line rule.** All 12 nc keyword lines carry exactly one keyword hit. Row-tagged lines with ≥ 2 keyword hits (K3): only 307 and 1208, both hits inside C15's single claim (1208's second clause "nothing here upgrades any proof grade" is the S9 standing line, which C15's basis column names as a site of the C15 claim). Genuinely multi-claim lines carry multiple tags (428 ⟦C2⟧⟦C4⟧, 446 ⟦C4⟧⟦C5⟧, 304 ⟦C22⟧⟦C3⟧, 711 ⟦C23⟧⟦C27⟧). NO concrete instance of a covered claim sharing a line with a distinct uncovered claim.

**(e) The script's ledger-quote zoning fix.** `git diff 2ef7685 ab2e471 -- rmengine_claims_census.py` = exactly the one zone loop for paragraphs starting "**[IL...† = the ledger block". The docstring at the script's FIRST commit (2ef7685) already listed "the ledger-quote blocks" among excluded surfaces — verified by `git show` — so the fix implements a pre-declared clause rather than widening the grammar post hoc; the pastes are byte-frozen (tagging them would break paste fidelity), and the zone pattern matches exactly the three paste paragraphs (no other line starts "**[IL"). LEGITIMATE. Also probed: the LEX_SKIP carve-outs ("only if"/"not only"/"if and only if") mask nothing — zero occurrences anywhere in the note.

## 3. Spine sweep (charge 3 — re-derived standalone; NO mathematical finding)

* **T-scope algebra**: amended scope, its complement (fence box, S5.2(iv), C33), the S4.1 combined-scope identity [(a) ∧ (e₀ ≥ 2 ∨ d₀ = 1)] ∪ (b) = T-scope, and the m = 1 coincidence with the RM-1 scope — all re-derived exactly.
* **L-NORM (a)–(c)**: primitive parts + DIGIT-SPLIT (for all α, β of deg < d₀, per the upstream statement PE3 read at source) + restore π^w; (a) L-child w₀ exactly w with residue b̄c̄ ≠ 0 (K₀ = k₀[x]/(ψ̄₀) a field; primitive nonzero of deg < deg ψ̄₀ has nonzero reduction); (b) π-child ≥ w+1; (c) Y-child ≥ w, Ŷ ≡ 0 at d₀ = 1 (deg b̂ĉ = 0 < 1). ✓
* **S2**: reduced-cell degree telescope (Σ(P_l−1)deg Φ_l = deg Φ_m − d₀); L-CELL/B-READ induction; L-EXACT (exact rewriting + L-CANON pins the canonical pair; window-free); exit-cofactor single-digit remark via E1-priority (all j_l ≤ P_l − 1 below the reducing level m−1). ✓
* **S3**: L-GRID (λ_l = truncation lines, integrality, the congruence s_l(λ_l) ≡ j_{l−1}); L-ZMULT (chains add by linearity; L1 gives δ_l ∈ {0,1}; the t-leg z^{+δ} cancels the ε-leg z^{−δ} at l = 1..m−1; sole survivor z_m^{δ_m}, ε̃ as displayed); L-LINE(i) quantum h_{i+1}∏e_t via γ_{i+2} = e_{i+1}P_iγ_{i+1} + h_{i+1}; (ii) Δline = 0 by scaling the realize on-line identity ((g_i−k)e_i = P_i − e_ik; the three conversions all re-checked); (iii) junk gains ≥ E_m / ≥ Γ₀; i = 0 leg Γ₀ = h₀E_m/e₀; (iv) single-digit exactness; L-MONO + product-line additivity ⟹ the depth-0 skeleton (C24). ✓
* **S4.1 both branches**: (a) interval nonemptiness e_i·w_{i−1}P_{i−1} ≥ 2w_{i−1}P_{i−1} > S_{i−1} + w_{i−1}ρ_{i−1} = S_i with invariant (B_{i−1}) and P ≥ e; base (ρ₀/e₁, P₀) ≠ ∅. (b) at d₀ = 1: ρ_l = P_l − 1 all l; interval (S_i, w_{i−1}P_{i−1}); (B_i) from w_ie_i ≥ w_i > S_i needs only e_i ≥ 1; nonemptiness ⟺ w_{i−1} > S_{i−1} (the invariant); base (P₀−1, P₀) ≠ ∅; no e-bound consumed. Descent legs: promotion (A), exit, correction i ≥ 1 (consumed ≥ e_i, dumped ≤ ρ_l incl. the Y-accounting in ρ₀), level-1 Δj₀ ≤ −e₀ + [d₀ ≥ 2]. **Necessity as an iff**: flat (B_i) + (A_i) ⟹ w_{i−1} > S_{i−1}, cascades via the identical rearrangement, bottoms at ρ₀ < P₀ ⟺ d₀ = 1; converse = branch (b). All re-derived; the consistency display's point exits the family at e₁ = 1 via (A₁) (exact iff). ✓
* **S4.2 inspection census (C23/C27)**: my own pass over every leg — L-CANON/L-CELL/L-NORM (division + valuation; READ-ADD-(m−1) caveat now displayed per PE3-N-2), S2.2 inventory (mass e_i(g_i−k) ≥ 1), S3 congruences degenerate cleanly at e = 1 (s ≡ 0, δ = 0, ε ≡ 1), L-LINE needs h ≥ 1, DEV-(m) carrier = extended T-TERM, RM-GEN adds nothing. NO leg but L-MU consumes e_i ≥ 2. ✓
* **S5**: DEV-(m) (D1)–(D3) induction on μ — leaf = L-LINE(iv); (D2) exit leg via the single-digit remark; (D3) READ-ADD applicability from (D2)+L-MONO; above-line ⟹ 0; off-grid-at-own-line EMPTY (L-GRID); T-tele(1) collapse via ψ₀(z₁) = 0 with the (ID-1) sign; T-tele(i+1): Δλ_i = β_k exactly, t_{i+1} shift k − g_i, L-ZMULT at level i, B-READ-inside-the-k-sum legitimate (finite sums, C_k cells on-line on-grid by realize), EPS-MULT + (R3) give ε_i(β_k)τ_k = ψ_{i,k}, collapse via ψ_i(z_{i+1}) = 0. L-VNZ: full induction re-walked (slot cap t ≤ g_{m−1}−1 from deg f < deg Φ_m; min-achieving digit on-grid by the equality λ = e_{m−1}w_{m−1}(f_j) + jγ_m; distinct min-achievers land at distinct z_m-powers, no cross-slot cancellation; nonzero poly of deg < [K_m:K_{m−1}]). RM-GEN all four clauses assembled; ε̃ = ε′ uniform over the shared target pair; (C-EPS) = L1. ✓
* **S6 grades honest**: LAW-DCX-(m+1) conjecture-grade at every use site (all re-checked incl. C12's); m = 1 and m = 2 specializations re-derived (u₁ = (γ₃ − s₂(γ₃)γ₂)/e₁ = the DCX3 u₀); L-CAP re-proved (pool₁ ≤ 3e₀−3; pool_l ≤ 3e_{l−1}−1; e = 1 leg pool = c_{l−1}); 11b crossing law from the child inventory alone with the T-scope-pinned μ-citation; 11c pool identification honestly HEURISTIC. ✓
* **S7 COR-A**: ∧_{m=2}^{n−1}[d₀ = 1 ∨ A_m] = d₀ = 1 ∨ A_{n−1} = T-class(n) (distribution re-checked); n = 1, 2 empty call family; e_{n−1} correctly absent. COND-B stands conditional. ✓
* **Records**: S9 md5 pins re-verified against the committed artifacts (runner c50c08e..., output d9118f5..., results JSON 1ce263df...); S8 grade rows match body; S10 fences match the proved perimeter; counter 0/2 consistent.

## 4. Machine section (charge 4)

* `python3 verification/openmath/rmengine_claims_census.py` → exit 0; output BYTE-IDENTICAL to committed `rmengine_claims_census_output.txt` (diff exit 0).
* `python3 verification/openmath/rmengine_pe2_ext.py` (md5 574f87d89fae5794bede7e0b38121877, untouched) → exit 0, 0.5 s; output BYTE-IDENTICAL to committed `rmengine_pe2_ext_output.txt` (diff exit 0, elapsed line included): XA 67 flat-tower calls / v = 0 realized 0; XB 1080/0, 225/0, 252/0; all 12 XV verdict lines GREEN.
* `git diff 423b399 ab2e471 -- lean/notes/openmath/RMENGINE_2026-08-08.md` (704 lines) read in full: every S2–S7 hunk is a trailing ⟦…⟧ tag append or one of the two queue-named wordings (the title gloss; the S4.2 READ-ADD-(m−1) census-gloss caveat). No displayed equation, lemma statement, or proof step changed. The r3 STATEMENT FENCE HOLDS as chartered.
* `git diff 2ef7685 ab2e471 -- rmengine_claims_census.py` = the ledger-quote zone loop only; docstring clause pre-existing (git show 2ef7685).
* New referee: `verification/openmath/rmengine_pe4_ext.py` (this pass's, read-only) — K1 all 44 keys GREEN; K2 12 nc lines; K3 no covered+uncovered co-tenancy; K4 zone inventory. No committed file mutated.

## 5. Findings (quote-and-classify)

**G-1 [JUSTIFICATION GAP — the six-change statement census is still not complete; no chain impact].** Round-1 repair record, verbatim: "The complete r1 statement-change census, per the actual `git diff bf18bfe 8835d20` [...]: (1) the T-scope/T-class STRENGTHENING ...; (2) the L-LEX regrade PROVED → REMARK ...; (3) scope remark (ii)'s 'v = 0 is legal' clause restated as Lemma L-VNZ ...; (4) COR-A's n = 1, 2 exact-union correction ...; (5) L-GRID's empty-case conclusion replacing (D3)'s converse-integrality appeal ...; (6) S8 row 11 split into 11a/11b/11c"; row C28: "the r1 statement-diff census, SIX changes per the actual diff". I ran the diff. A SEVENTH claim-content change of exactly the enumerated genre is missing: **Lemma L-LINE(ii)'s statement clause flipped**. Composed (bf18bfe), verbatim: "correction-main children PRESERVE the line exactly whenever w₀(b·c_d) = w₀(b) + w₀(c_d), i.e. always (... — when the residue product res(b̄)res(c̄_d) vanishes the L-child is the zero polynomial and is absent, the [IL3]† DEV-1 bookkeeping)". r1 (8835d20), verbatim: "it is NEVER absent (L-NORM(a): residues multiply in the field K₀ — the old 'residue product vanishes' parenthetical was an artifact of the un-normalized display and is DELETED)". A case-possibility clause inside a lemma's statement bullet was replaced by its negation — genre-identical to enumerated items (3) (a live case proved empty and restated) and (5) (a flagged appeal replaced); with (5) — a proof-layer repair — inside the list, no principled boundary excludes this one. Secondary witness, weaker (arguably subsumable under (1)): the fence box's composed sentence "a level-(i+1) correction can genuinely RAISE any linear μ" (false at d₀ = 1) was DELETED and replaced by the family-infeasibility display. Counter-reading acknowledged: the note frames the whole PC-C1 complex as display repair ("[display rewritten at r1]"; R1 adjudicated TEXT-GAP), and both PE2 (§4: L-LINE treated as "Row 3 basis gains L-NORM") and PE3 (§1.5: "additions not changes") drew the boundary the other way — but the committed census itself adopted the broader genre at r3, and under its own composition the claim "complete ... per the actual diff" is not literally true. The missed change is a disclosed-at-site STRENGTHENING; the census's substantive rider ("all repairs or strengthenings, none weakening an established clause") remains TRUE — verified independently against the diff. Doc-layer; zero chain impact. This is the fourth recurrence of the hand-census-claims-completeness defect, now on the statement-diff surface (the tag grammar closed the keyword surface only).

**N-1 [NOTE — the head bracket's r3-fence sentence is under-scoped].** Round-3 head bracket, verbatim: "The r3 edit adds ONLY trailing tags on S2–S7 lines plus the two queue-named wordings (title gloss; the S4.2 census-gloss caveat): no display content changed." Under any parse the sentence's inventory is incomplete: the r3 edit also adds trailing tags on S0/S1/S9/S10 lines (≈17 tags, e.g. ⟦C21⟧ at S0.1, ⟦C15⟧/⟦C16⟧ at S1, ⟦C5⟧ at S10), the S8 charter rewrite, rows C29–C33 + the four re-keys, and the round-3 records — and its own exception (the title gloss) is itself outside S2–S7. The facts are all disclosed in the same bracket's preceding sentences ("every charter-keyword line of the body now carries an explicit row tag ... rows C29–C33 added ..."), and the tail record states the correctly-scoped version ("S2–S7 changes are trailing ⟦…⟧ tags only, plus the two queue-named wordings"). The fence itself — no mathematical display changed — is TRUE against the actual diff (§4). Wording precision only; PE3-N-1 precedent.

## 6. Verdict block

The mathematics survives this pass in full: every spine item re-derived standalone with zero findings, both referees byte-identical on re-run, the r3 statement fence verified against the actual diff, all 33 row keys exact quotes, the tag grammar CLOSED and its 12 ⟦nc⟧ tags individually unbroken, the excluded zones clean, the script fix legitimate, every attribution now accurate at its source, and rows C29–C33 all true. What does not survive is — once again — a record-layer completeness claim: the r3 repair, curing PC3-g3's under-enumeration, re-asserted "The complete r1 statement-change census ... per the actual diff" as a hand list, and the diff contains a seventh claim-content change of the list's own genre (L-LINE(ii)'s "residue product vanishes ⟹ L-child absent" clause replaced by "NEVER absent"). By this arc's consistent precedent (PC3-g3: disclosure elsewhere does not cure an exhaustiveness claim's under-enumeration; every false-as-stated record claim graded a gap), that is a JUSTIFICATION GAP, not a note — doc-layer, zero chain impact, none-weakening rider intact. One wording note besides. The bar is CLEAN; this is not CLEAN.

RMENGINE-PE4 FINDINGS: 0 critical, 1 gaps
VERDICT: GAPS-ONLY
