# LEAKFREE TYPED — the general-instance face at the v4 carrier (pass-5 F6 unit)

Date: 2026-08-08 (wallclock 2026-08-02). Unit: synthesis pass 5 queue item 8
(F6 small-units dispatch), "leakFree general-instance face" — the typed-carrier
debt left open by H2-U9's statement-gate fold and carried at the ROOT (FRESH)
row residue ("general-instance supplier + Step-10/K1 classifier face still
owed") and the PROJECT_STATE frontier queue.

## Spec gate (executed first)

* Spec LOCATED and LIVE: `docs/SYNTHESIS_PASS5_2026-08-08.md` queue item 8;
  `docs/PROJECT_STATE.md` :1782; ROOT r23-3 fold row + §(FRESH) residue.
* NOT already executed: TD-5's `LeakFreeCarrier` is the v1-read (ii.3)/(ii.4)
  pair, and the v4 chain plugs it only via the projection
  (`leakFree := LeakFreeCarrier L.toLedgerStratumData`); no typed law read the
  AS-BUILT v4 (`slotRead`, d-leaf) carrier, and NOTHING discharged the plugged
  Prop (the landed gates are `rfl` INSPECTIONS of the field's value, not
  proofs of it).

## What landed (`lean/LeanUrat/B2D/LeakFaceV4.lean`, all NEW declarations;
## zero existing statements touched)

THE TYPED STATEMENT (the general-instance face):

```
def LeakFreeCarrierV4 (L : LedgerStratumDataV4 D W P) : Prop :=
  LeakFreeCarrier L.toLedgerStratumData ∧
  (∀ k : ↥W.onLine, (k : Fin (W.ℓ + 1)) ≠ W.ks →
    ∀ t < D.d, L.slotCoords k t ∉ L.parentBlock)
```

= TD-5's accepted (ii.3)/(ii.4) pair PLUS "every read leaf of every interior
on-line slot avoids the parent block" — clause (L)'s referent at the d-leaf
READ (the height-β_k digit IS `slotRead`; the parent's realized conditions
read only the parent block by `parent_support`). Faithfulness lineage: a
consequence-of-clauses carrier (`parent_interior_disjoint` +
`slot_coords_mem`), the TD-5 species; NO slotDigits/count law is stated, so
the H2-U9 R2-C1 counting-shadow bar is respected. Display face
`LeakFreeCarrierV4.read_parentBlind`: parent-block-confined rewrites cannot
move any interior read code.

RESULTS (all `lake env lean` green, footprints = Lean-core
[propext, Classical.choice, Quot.sound]):

1. `ledger_leakFreeV4` — every v4-LAWFUL datum satisfies the typed law
   (the general-instance supplier).
2. Wiring where the TransDeep chain displays `leakFree` (upgrade of the rfl
   inspection gates to DISCHARGE):
   * `transDeep_of_lawful_v4_leakFree_holds` / `transDeep_of_lawful_leakFree_holds`
     — the presenting-datum builders' plugged Prop is TRUE (v4 + v3 chains);
   * `leakFreeCarrierV4_plug` — the typed v4 law subsumes the plugged value;
   * `transDeep_of_ledger_v4_leakFree_holds` / `transDeep_of_ledger_leakFree_holds`
     — the FAMILY suppliers' rows carry a TRUE `leakFree`, proved THROUGH the
     Exists.choose transport (helper `TransDeep.leakFree_of_heq`; the cast is
     eliminated by `eq_mpr_eq_cast` + `cast_heq` + proof irrelevance);
   * `ledgerStrataV4_leakFreeV4` — family existential form.
3. Instance gates: `tdL_leakFree`, `td2_leakFree` (v1-carrier law;
   `LeakFreeCarrierV4` not applicable — v1 data have no `slotCoords`),
   `td2V4_leakFreeV4` (the v4 law at the transported huni countermodel), and
   the fired-row discharges `td_transDeep_leakFree_holds` /
   `td2_transDeep_leakFree_holds` / `td2V4_transDeep_leakFree_holds` — the
   LIVE chain's compiled rows now carry a PROVED (L) field.
4. De-vacuity fence `td2V4bad_not_leakFreeV4`: a raw v4 datum with the parent
   block moved onto a read leaf REFUTES the typed law — the definition is
   falsifiable, lawfulness is load-bearing. (Countermodel-searched first per
   the F1 protocol; no countermodel exists at lawful data — the supplier
   proof is two pack-clause projections, so the law is a theorem there.)

## Honesty display (what this does NOT close)

* All compiled instances have `parentBlock = ∅`, so at THESE instances the
  carrier holds trivially; the content lives in the general supplier + the
  de-vacuity fence. A nonempty-parent LAWFUL instance = F5 forge material.
* The H2 `TransDeep.leakFree : Prop` FIELD stays bare (H2 statements
  byte-unchanged by design — the U9 fold of record stands: no faithful typed
  counting shadow exists at the skeletal (W,P,J) carrier; the typed content
  lives at the ledger carrier and is now discharged there).
* The Step-10/K1 classifier face (the other U10 twin, the leakFree CONSUMER)
  remains owed — outside this unit's charter.
* ROOT (FRESH) row: the "general-instance supplier" residue for the FULL row
  is still the engine's (the whole §B2-DEF at non-ledger instances); this
  unit retires exactly the leakFree TYPED-FACE component of that residue.
