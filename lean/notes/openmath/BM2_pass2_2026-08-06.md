The revision remains **CRITICAL**. Six of the eight pass-1 findings are genuinely cured, but recentering scope leaks back into two universal claims, and OL-1(a) is still overclaimed at recentering sites.

## Eight-finding audit

1. **KP-STEP class membership overclaim — cured.**  
   The note repeatedly and correctly says that only (K0) is proved and that (K1)+(K2), class membership, and KP-STEP application depend on GRADED-READ.

2. **Recenterings quantified without (SL-top)/(SL-dev) — not fully cured.**  
   The dedicated case split is correct, but two later sentences erase it:

   > “at every lift fired on the three RECORDED carriers … the D8 stride shape + the stage-law digit DATA are definitional”

   This S0 row grammatically includes recenterings, despite “recenterings split out below.”

   More sharply:

   > “the recorded wiring cannot fire a naked-data lift.”

   S4(i-a) states this “at the recorded carriers,” although REC-SL explicitly says the digit data for recenterings are not established. The parenthetical mentions SITE-EXH and GRADED-READ, but not REC-SL.

   **Classification: CRITICAL.** This silently closes part of REC-SL and contradicts the explicit recentering split.

3. **`liftKey`/`liftKeyExt` scope contradiction — cured.**  
   Their coverage is now expressly limited to shape and monicity, with stage-law data excluded.

4. **SITE-EXH/five-vs-six inconsistency — cured.**  
   SITE-EXH is named and open, both closure routes are given, and the declaration arithmetic is consistently 1+1+2+5=9.

5. **One-example monicity audit — cured.**  
   The 13-hit list is presented as a census rather than a completeness proof, and closure is explicitly assigned to SITE-EXH.

6. **S0 overclaim relative to OL-1(b) — cured.**  
   OL-1(b) is conditional on GRADED-READ, REC-SL where applicable, and TR-3-ORD-X.

7. **“(K0) class” wording defect — cured.**  
   The note now consistently says “the (K0) clause of the class.”

8. **Compiled implications presented as closed-world coverage — cured.**  
   The distinction between per-carrier implications and SITE-EXH is explicit.

## Nine-declaration authority audit

The pasted file contains exactly these nine declarations:

1. `K0Conformant`
2. `isReadLift_K0`
3. `isNodeLift_K0`
4. `sub_inC_K0`
5. `landingKey_K0`
6. `landingKey_recentering_K0`
7. `historyCoherent_key_K0`
8. `historyCoherent_recentering_K0`
9. `readsOf_landing_K0`

Thus the note’s count—one definition, one core theorem, two bridges, five carrier corollaries—is exact. No declaration in the pasted file is missing from the authority list, and none listed is absent.

Their descriptions are accurate with one important boundary: the recentering corollaries conclude `K0Conformant σ 1 1 Φ'`; they do not identify the node’s actual `ν.e` and `ν.g` with 1.

## Lean-content audit

- `isReadLift_K0` proves exactly the non-recentering (K0) result claimed in S2: monicity and
  `natDegree Φhat = e' * g * natDegree σ.Φ`.
- It requires `1 ≤ e'` explicitly. `isNodeLift_K0` supplies this through `ν.he`; both non-recentering landing/history corollaries pass through `isNodeLift_K0`.
- The five carrier corollaries establish the advertised per-carrier K0 implications.
- The recentering leg is indeed only monic-degree-`m` at the synthetic pair `e=g=1`. It proves no `IsReadLift`, weight law, residual law, or graded identity.

That last fact exposes another surviving critical claim:

> “(a) — PROVED AT THE RECORDED CARRIERS … with `f_i = g` via `Node.hψdeg`”

OL-1(a) requires

`deg Lift_i = e_i · f_i · m_i`

using the chain/read’s actual `e_i,f_i`. At a recentering, the Lean results provide only

`deg Φ' = 1 · 1 · m`.

`Node.hψdeg` supplies `f_i=g`; it does not supply `e_i=1` or `g=1`. No pasted declaration establishes those equalities. Consequently, OL-1(a) is not proved at recentering carriers unless an additional parameter-identification premise is supplied. The same defect affects:

> “every lift fired … satisfies the (K0) CLAUSE of the class the verified KP-STEP quantifies over”

For recenterings, the compiled K0 statement is at `(1,1)`, not demonstrably at KP-STEP’s recorded read pair.

**Classification: CRITICAL.** REC-SL—or a separate recentering parameter-identification obligation—is silently consumed by OL-1(a), despite REC-SL’s consumer list naming only OL-1(b)/(c).

## Fresh lesser gap

The RG-2 sentence

> “e ≥ 2 lifts are fired — and covered — at `LandingKey`/`ReadsOf` FINAL reads”

is stronger than the pasted file. `readsOf_landing_K0` conditionally covers every read of an assumed `ReadsOf` run, but does not establish existence of a final read having `e ≥ 2`.

**Classification: justification gap**, unless the cited external corpus result explicitly proves that existence claim.

GRADED-READ and SITE-EXH otherwise remain displayed and conditional. REC-SL does not: S4(i-a) and OL-1(a)’s recentering claim silently consume parts of it.

VERDICT: CRITICAL