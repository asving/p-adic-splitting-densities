VERDICT: **GAPS-ONLY**

No critical defect found in the revised theorem or OP-B1/minimality argument. The pass-1 counterexamples are genuinely excluded by (K0), and no residual quantifier hole remains.

## Pass-1 repairs

1. **Minimality repair — GENUINE**

Quoted chain:

> “For every nonzero \(B\) … with \(\deg B<e g\deg\Phi_i\): \(\widehat\psi_{\rm hom}\nmid\operatorname{in}(B)\).”

and then:

> “On the (K0) class the bound EQUALS \(\deg\Phi_{i+1}\).”

This is now logically complete:

\[
\deg B<\deg\Phi_{i+1}
\overset{\mathrm{K0}}{\Longleftrightarrow}
\deg B<eg\deg\Phi_i
\overset{\mathrm{OP\!-\!B1}}{\Longrightarrow}
\widehat\psi_{\rm hom}\nmid\operatorname{in}(B).
\]

(K0) is used exactly where required: translating the theorem’s own-degree quantifier to OP-B1’s fixed bound. OP-B1 itself remains independent of \(\Phi_{i+1}\). No “whenever conformant” qualification survives inside the proof.

Monicity is unnecessary for that translation but is legitimate as part of the standard representative normalization.

2. **(SL-top) repair — GENUINE**

With \(D_g=1\), the top term is \(\Phi_i^{eg}\). For \(\kappa<g\),

\[
\deg(D_\kappa\Phi_i^{e\kappa})
\le (m-1)+e(g-1)m
<egm.
\]

Thus, assuming the standing parent-key monicity, the sum is monic of exact degree \(egm\). The shared-unit calculation at \(\kappa=g\) also gives \(U_0=1\).

The displayed \(D_g\)-padding counterexample correctly demonstrates why (SL-dev) alone cannot give (K0).

3. **Refinement corner — GENUINE**

When \(eg=1\), (K0) gives \(\deg\Phi_{i+1}=\deg\Phi_i\). Therefore LOW-CONST covers the entire minimality range rather than merely a smaller fixed window. The original corner defect is closed.

4. **Gate rescoping — GENUINE**

The revised text now accurately labels P3 as self-consistency evidence, restricts all finite claims to the tested perimeter, and uses P4 only as a counterexample to the naked universal construction. No proof step relies on the gate.

## Re-derivations

### (KPa)

From (2′), (K1), and (K2),

\[
\begin{aligned}
\operatorname{in}(\Phi_{i+1})
 &=\varphi_{\widehat\gamma}\,
   R^{\mathrm{gr}}_{\widehat\gamma}
      (\operatorname{in}\Phi_{i+1})(\bar y)\\
 &=\varphi_{\widehat\gamma}\,U_0\psi(\bar y).
\end{aligned}
\]

Since \(\widehat\gamma=eg\gamma_{i+1}\) is divisible by \(e\), its anchor has \(s(\widehat\gamma)=0\) and

\[
\varphi_{\widehat\gamma}=p^{g\gamma_{i+1}}.
\]

Hence

\[
\operatorname{in}(\Phi_{i+1})
=U_0p^{g\gamma_{i+1}}\psi(\bar y)
=U_0\widehat\psi_{\rm hom}.
\]

On (K0), the monic top \(\Phi_i\)-slot is \(eg\) with coefficient \(1\), and comparison of the \(x^{eg}\)-coefficients forces \(U_0=1\). That argument is sound.

### (KPirr)

The presentation eliminates \(y\) legitimately because \(p\) is already invertible:

\[
K_i[y,p^{\pm1},x]/(x^e-p^hy)
\simeq K_i[p^{\pm1},x],
\qquad y\mapsto x^ep^{-h}.
\]

FGMN’s prime-element result applies to \(\psi(\bar y)\), using both irreducibility and \(\psi(0)\ne0\). Since

\[
\widehat\psi_{\rm hom}
=p^{g\gamma_{i+1}}\psi(\bar y)
\]

and the \(p\)-power is a unit, \(\widehat\psi_{\rm hom}\) is prime. The cited primary paper contains the corresponding presentation and prime-element argument, although theorem numbering differs between versions: [FGMN preprint](https://upcommons.upc.edu/bitstream/handle/2117/28203/ResidualIdealsArxiv.pdf).

## Findings

### GAP 1 — the Lean alignment quote needs its missing parent invariant

Quoted claim:

> “`HC2.IsReadLift` HARD-WIRES \(\widehat\Phi=\sigma.\Phi^{e g}+\sum_{k<g}t_k\sigma.\Phi^{ek}\) … so `hΦ : IsNodeLift` already quantifies over (SL-top)-shaped lifts ONLY.”

The quoted equation does hard-wire \(D_g=1\) and, with the digit degree bounds, exact degree. But it yields **monicity only if \(\sigma.\Phi\) is already monic**. S3.2 itself recognizes this:

> “the \(\kappa=g\) stride is then \(\Phi_i^{eg}\) — monic (\(\Phi_i\) monic)”

Thus the Lean-facing conclusion “wiring itself supplies (K0)” should cite the node/state invariant establishing `σ.Φ.Monic`, not merely `hΦ : IsNodeLift`. This is likely already available from the lawful-state invariant, so it is a documentation/dependency gap rather than a theorem failure.

The local corpus could not be independently read in this pass because the workspace sandbox launcher failed before any file access. Consequently, the exact `IsReadLift` quotation remains source-unconfirmed here.

### GAP 2 — Corollary 4.12’s alleged verbatim quotation is malformed

Quoted text:

> “Then, for any \(\alpha\in\Gamma(\mu)\) there exists \(g\)… \(\mu(g)=e_rf(w_r+\lambda_r)\)…”

The displayed conclusion is independent of the quantified \(\alpha\). The construction later correctly chooses

\[
\alpha=e_rf(w_r+\lambda_r),
\]

which is also how the accessible preprint proof proceeds. Therefore SL-EX’s mathematical use is sound, but the claimed “verbatim” quotation should be corrected—probably to “take \(\alpha=\cdots\)” or by removing “for any \(\alpha\).”

## Consumer alignment and B-M2(a)

The pricing claim is sound in the ledger sense:

- KP-STEP proves the block for a (K0)-conformant lift.
- B-M2(a) already requires the pinned lift to be monic of degree \(m_ie_if_i\).
- Establishing that the engine’s chosen lift satisfies this specification remains B-M2’s construction-side obligation.
- S3.2 proves the top-stride shape implies (K0) independently of KP-STEP, so there is no circular use of minimality to obtain (K0).

Accordingly, the restriction introduces no new mathematical obligation. The note should only soften “supplied by the wiring itself” to “supplied by the `IsReadLift` shape together with the parent-key monicity invariant.”

No fresh critical defect was found in LOW-CONST, IN-FORM, OP-B1, the residual-ideal contraction, the (KPa) identity, or the (KPirr) pin usage.