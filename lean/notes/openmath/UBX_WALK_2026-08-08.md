# (UB-X) CONSUMER-NEED WALK — the Route-B re-scope's consumption-site map and the exact Asvin question (unit UB-X WALK, synthesis-7 queue #7, NAMED DISPATCH, 2026-08-08)

CHARTER: docs/SYNTHESIS_PASS7_2026-08-08.md queue #7 ("the walk that tees the
Route-B Asvin decision"; F5 item 8: dropped at pass-5 and pass-6 — this is the
named dispatch) + docs/SYNTHESIS_PASS6_2026-08-08.md queue #8 ("tees up the
Route-B adoption; the adoption itself Asvin-gated") + the pass-7 F4 row
("(UB-X)/(K3-δ) BLOCKED-ON-ASVIN for adoption; OUR half (the consumer-need walk)
still not on file"). GENRE: DOC WALK — no Lean edits, no re-scope applied, no
discharge claimed. This note is condition (5)'s substance per
`lean/notes/openmath/H4M7_rootsync_brief.md` §4 ("the Step-16 walk is condition
(5)'s substance; the ROOT owner's acceptance pass executes or commissions it") —
here it is EXECUTED at walk grade: every claim below is a citation-anchored read
of on-file text; nothing is proved, and the one place where the needed text does
not exist (the unwritten clause-(UB) composition) is displayed as the walk's
honest limit (§4.3).

## §0. Verdict summary

1. The consumers of (UB-X) content are EXACTLY three site families (completeness
   warrant §2): clause (R) at Step 18's K3-c (through the (K3-δ) fragment),
   clause (UB) at Step 16 (the full row), and the Scaffold typed faces (the
   Lean-side transcription of both).
2. **RB-DELTA is CONSUMER-VISIBLE at all three families — it is NOT a
   display-only edit** (terms defined in §1.3/§1.4). It changes clause (R)'s
   displayed obligation roster, changes what member (b) supplies to Step 16's
   clause-(UB) derivation, and changes the Prop content of three `RootHyps`
   fields. This is exactly why it is Asvin-gated (the statement-change fence):
   the gate is substantive, not procedural.
3. Per-consumer need, at exact strength: clause (R)/K3-c needs precisely the
   (RB) instance at its realized δ > 1 pools — need-exact, nothing stability-
   shaped in the consuming text (§3 site 1). Step 16 consumes member (b) at
   exactly ONE textual site — the coverage step extending the proved δ-stable-
   scoped alphabet law to the realized ledger — and there RB-DELTA NARROWS the
   supply: (RB) delivers the exponent law + the letter identification at every
   realized position but NOT F_{p^δ}-linearity at realized-not-Stable positions
   (§4.2). The walk found NO on-file consumer, prose or compiled, that reads
   stability or linearity beyond `EQ2law`'s internal `Stable` guard (§4.2 items
   W3–W5) — so H4M7 §4's fallback trigger ("a Step-16 consumption of stability
   BEYOND the exponent law") did NOT fire on the written corpus; the residual
   caveat is only that the clause-(UB) composition is itself unwritten (§4.3).
4. The F-B firing-condition ledger reads (1)–(4) SATISFIED on the current
   record; condition (5) = Asvin sign-off + the ROOT owner's acceptance pass on
   THIS walk is the only open gate (§5). The exact decision paragraph is §6.

## §1. The row, the fragment, and the re-scope, pinned

### 1.1 The row (UB-X) = (H4b)

ROOT (`docs/ROOT_ASSEMBLY_2026-08-02.md`, §3.1 row at line 2494; members
restated at Step 16's honest-scope clause, lines 1984–1986): clause (UB) of
(ROOT-C) is missing exactly

- **(a)** the per-leaf O_δ re-base audit for the ℚ_p-stated leaves (the
  dictionary/CU/drainage layers — "per-leaf scope hygiene" of the D-3 collapse);
  open as Lemma RRE-δ (H4-M3 row 10; H4M7 §5.1);
- **(b)** "δ-stable position classes exhaust the realized ledger at every δ"
  (the stability/exhaustion form — the member RB-DELTA rewrites);
- **(c)** full solve + type aggregation commute with unramified base change
  (H4-M6; premise-(i)-unsatisfied by its own §0; H4M7 §5.1).

**(K3-δ)** := member (b) RESTRICTED to Step 18 K3-c's realized δ > 1 pools
(named at ROOT REVISION 4, finding VC4-1; the audit note moved onto the (UB-X)
row at REVISION 22 — the row's second bracket, ROOT:2494: "clause (R)'s
attribution consumes it EXPLICITLY; the match display (or the Route-B re-scope
above) rides the next O-11/O-8b touch and would retire (K3-δ) from clause (R)").

### 1.2 (RB), the Route-B law

`H4M5_k3delta_attempt.md` §1.1 (blueprint text, verbatim there): for every prime
p, every δ ≥ 1, and every position y the O_δ = W(F_{p^δ}) classifier REALIZES,

  a_δ(y) = δ · a_sym(letter_δ(y)),

letter_δ(y) the classifier's OWN realized species letter, a_sym its p-free
symbolic exponent (M5 §2 Def SYM). M5 proved (RB) as a displayed conditional on
its five §1.3 rows ([RUN-PROD], [LEDGER-READ], [LET-DATA], [SYM-ID],
[M1-STATUS]) — textual bar MET (passes 6+7 CLEAN, model-diverse); the sealed
falsifier N-K3 ran ALL GREEN (376/376, commit 5fdf350; ROOT:2494 REVISION 21
bracket). Binding non-implication (M5 §1.1/§5.3, blueprint finding 3): **(RB)
does not imply that realized positions are δ-stable** — the F₉-shape instance
(M5 §5.2) is intrinsic-exact and stability-broken.

### 1.3 DEFINITION (RB-DELTA)

**RB-DELTA** := the H4M7 §4 drafted delta, exactly as drafted there and nothing
more: (i) (UB-X) member (b)'s row text is REPLACED by (RB), with the displayed
provenance "re-scoped from the stability form; the stability form is RETIRED as
a row, not proved" (the withdrawal display of M4 §6 item 1 — mandatory, so no
consumer silently keeps reading stability); (ii) the (K3-δ) fragment's content
becomes (RB) restricted to K3-c's realized δ > 1 pools, and clause (R)'s
attribution line re-points to that (RB) instance; (iii) members (a)/(c) and
every other §3.1 row are UNTOUCHED; (iv) the re-scoped row's grade is
PROVED-conditional at the M5 record (residue = M5's five §1.3 rows, i.e. REL.1's
run-production/ledger slice + the catalogue-grammar slice + [M1-STATUS]); (v)
the Lean faces re-point (`UBXbStmt`/`K3deltaStmt` → the fenced H4-F5 shape) only
AFTER the delta lands. All subsequent uses of "RB-DELTA" in this note mean
exactly this five-clause package.

### 1.4 DEFINITION (CONSUMER-VISIBLE)

**CONSUMER-VISIBLE** := RB-DELTA changes the displayed obligation set or the
supplied content at one or more consumption sites OUTSIDE the §3.1 (UB-X) row's
own text — the alternative verdict being DISPLAY-ONLY (only the row's internal
wording changes; every consumer's obligations and supplies are extensionally
unchanged). The charter's question "consumer-visible / display-only" is decided
between these two defined terms.

## §2. The consumer enumeration and its completeness warrant

ROOT's clause-attribution display is exhaustive by construction and states the
scoping line verbatim (ROOT:1232): "**NO clause other than (UB) consumes (H4b)
BEYOND its (K3-δ) fragment**; clause (UB) consumes (H1)–(H3) + (H4b) + (H5) +
(H6) + (H7) + (H8′)". With clause (R)'s roster (ROOT:1214–1216: "(H1)–(H3) +
(H5) + the (K3-δ) fragment of (H4b) + (H6) + (H7) + (H8′)"), the complete
consumer set of (UB-X) content is:

| # | consumer | what it consumes | where |
|---|----------|------------------|-------|
| 1 | clause (R), through Step 18's K3-c | the (K3-δ) fragment only | ROOT:1070–1075 (the (H4a)-split record: "clause (R)'s value chain consumed it too, through Step 18's K3-c — the base-changed β-legs at q ↦ q^δ, δ > 1; the O-11 r2 declared row D-c"); ROOT:2023–2025 (Step 16's cross-reference: "(R1)–(R4) is NOT (UB)-only — it also enters clause (R) through Step 18's K3-c; the split (H4a)/(H4b) records this") |
| 2 | clause (UB), through Step 16 | all of (a)+(b)+(c) | ROOT:1952–2031 (Step 16; the honest-scope clause 1972–1986; Consumes list 2026–2031 "and the §3.1 row (UB-X)") |
| 3 | the Scaffold typed faces | (b) and (K3-δ) as Props + the fragment law | `lean/LeanUrat/Scaffold/Hypotheses.lean`:60–62 (table), 167–172 (`h4b_stable`, `h4b_k3delta`, `stable_implies_k3delta`), 253–276 (`ClauseRHyps`/`ClauseUBHyps`/`clauseR_of_clauseUB`); `lean/LeanUrat/Scaffold/HDischarge/H4/Kernel.lean`:75–88 (`UBXbStmt`, `K3deltaStmt`, `k3delta_of_ubxb`) |

No fourth family exists on the record: a corpus grep for the typed faces finds
consumers only in `Scaffold/Hypotheses.lean`, `Scaffold/HDischarge/H4/Kernel.lean`,
and `Scaffold/HDischarge/H4/AxChk.lean` (the census file), and ROOT's §1/(ROOT-C)
display quantifies the prose attribution exhaustively (the line quoted above).
Members (a)/(c) have Step 16 as their sole consumer (the same scoping line);
RB-DELTA clause (iii) leaves them untouched, so they enter the map below only as
the displayed no-change rows.

## §3. The consumption-site map, at exact strength

### Site 1 — clause (R) / Step 18 K3-c, through (K3-δ)

**What the consuming text needs.** O-11 §3.4's K3-c row, quoted verbatim in M4
§1 and re-quoted at M4 §5.4: "the re-based classifier obeys the same
digit/alphabet laws with a_δ = δ·a₁" — the β-legs β_μ(σ)(q^D) read the realized
letter's EXPONENT at the pool's base (H4M7 §4, walk item (a)). **There is no
stability quantifier in the consuming row text** (M4 §5.4, first bullet):
stability entered only because the retired (H4a) package's proof is DECLARED at
δ-stable classes — (K3-δ) exists to bridge that scope mismatch, not because the
consumer reads stability (M4 §5.4: "(K3-δ) exists to bridge a SCOPE MISMATCH
between the law's declared hypothesis and its consumption site").

**Exact strength needed:** the (RB) instance at (y, δ) for K3-c's realized
δ > 1 pools — the per-pool exponent read. Evidence grade: adjudication-level
(M4 §5.4's own status header), corroborated by M5's Corollaries DIV/ROOT shapes
and by N-K3 ALL GREEN (376/376: intrinsic law exact at every probed n = 3 pool
including per-branch at split pools).

**RB-DELTA at this site: CONSUMER-VISIBLE, need-exact, and strictly
improving.** Clause (R)'s displayed obligation converts from the OPEN stability
fragment (K3-δ) — the [M]-row content — to the (RB) instance, whose content is
PROVED-conditional at the M5 record (residue: the five §1.3 rows). Clause (R)'s
conditionality ledger materially changes; this is the discharge H4M7 §4 item 2
drafts ("clause (R) returns to (H4a)-only at the D-c interface; the former
(K3-δ) stability fragment is superseded by the (RB) instance").

### Site 2 — clause (UB) / Step 16, through the full row

Walked clause-by-clause in §4 (the leg H4M7 §4 marked OPEN). Summary: member (b)
is consumed at exactly ONE textual site — the coverage step — and RB-DELTA is
CONSUMER-VISIBLE there as a supply NARROWING (exponent + letter supplied at
every realized position; F_{p^δ}-linearity at realized-not-Stable positions no
longer supplied), with no on-file consumer found that needs the dropped part.

### Site 3 — the Scaffold typed faces

`RootHyps.h4b_stable` ("δ-stable position classes exhaust the realized ledger
at every δ", Hypotheses.lean:60/167) and `RootHyps.h4b_k3delta` (:61/170) are
bare Props whose CONTENT is the row texts; `stable_implies_k3delta :
h4b_stable → h4b_k3delta` (:172) is the displayed restriction law, consumed by
`clauseR_of_clauseUB` (:273–276). Kernel.lean's `UBXbStmt`/`K3deltaStmt`
(:75–82) are the Route-A-SHAPED carrier forms (both quantify
`AlphabetData.Stable`), with `k3delta_of_ubxb` (:87) the same restriction law.

**RB-DELTA at this site: CONSUMER-VISIBLE** — the Prop content of `h4b_stable`/
`h4b_k3delta` changes to (RB)/(RB)-at-pools (a Lean statement change: the fence
that makes this Asvin-gated), and `UBXbStmt`/`K3deltaStmt` re-point to the
fenced H4-F5 stability-free face (RB-DELTA clause (v); H4M7 §6's pins). The
DERIVATION SHAPE is preserved: (RB)-full → (RB)-at-pools is the same elementary
restriction, so `stable_implies_k3delta`/`clauseR_of_clauseUB`/`k3delta_of_ubxb`
survive with re-bound content — no consumer of these theorems changes shape.

### Sites for members (a) and (c) — displayed no-change rows

Member (a): consumed by Step 16 only (the D-3 "per-leaf scope hygiene" clause,
ROOT:1956–1957 + missing-leg (a) at ROOT:1982–1984; it is also the TRANSPORT
CARRIER through which (E-N) and the D-12r/TB-CAP perimeter enter clause (UB)'s
attribution, ROOT:1249–1251 — a carrier role, not a separate consumer of (b)).
Member (c): consumed by Step 16 only (missing-leg (c), ROOT:1985–1986). RB-DELTA
touches neither (clause (iii); H4M7 §5.1 fences both out: "(UB-X)(a) is OPEN
(Lemma RRE-δ, M3 row 10); (UB-X)(c) = M6 is premise-(i)-unsatisfied").

## §4. The Step-16 walk proper (the OPEN leg, executed at walk grade)

H4M7 §4 walk item (b), verbatim: "Step 16's alphabet-law scope: OPEN — the walk
is NOT executed here (it needs the Step-16 text walked against M5 §7's
consumption map...)". Executed here, clause by clause against ROOT:1952–2031.

### 4.1 Where member (b) enters Step 16's text — the census

- W1 (ROOT:1952–1956, the statement + route): "the GD families and O-9 are
  stated over an ARBITRARY absolutely-unramified complete DVR from the start
  (Steps 8/14...)" — no (b) content (arbitrary-DVR layers, proved).
  "...collapsing D-3 to per-leaf scope hygiene" — member (a), not (b).
- W2 (ROOT:1956–1959, the alphabet slice): "PLUS the alphabet slice O-8b: at
  δ-stable position classes the re-based slot map is F_{p^δ}-linear and
  a_δ = δ·a₁" — this is the PROVED leg (the retired (H4a) package at the
  P(O-8b/M14) ACCEPT record), NOT member (b): the law AT δ-stable classes.
- W3 (ROOT:1972–1986, the honest-scope clause): "missing are exactly (a) ...,
  (b) that δ-stable position classes exhaust the classifier's realized ledger
  at every δ, and (c) ...". **This is member (b)'s ONLY consumption site in
  Step 16's text: the coverage step** — the bridge from the δ-stable-scoped
  proved law (W2) to the classifier's full realized ledger, which is what
  clause (UB)'s "same R_τ at q = p^δ" needs the law to cover.
- W4 (ROOT:2026–2031, the Consumes list): "...and the §3.1 row (UB-X)" — the
  row as a whole; no additional (b)-specific strength beyond W3.

### 4.2 What the coverage step needs vs what RB-DELTA supplies

The proved law at W2 has TWO conjuncts: F_{p^δ}-linearity of the re-based slot
map, and the exponent law a_δ = δ·a₁. Under the CURRENT row, member (b)
(exhaustion) would extend BOTH conjuncts to every realized position. Under
RB-DELTA, member (b) = (RB) supplies, at every realized position directly and
with no bridge: the exponent law (the (RB) equation) AND the letter
identification (M5 Lemma S, "the realized skeleton IS the symbolic skeleton" —
the qualitative half the aggregation needs) — but NOT the linearity conjunct at
realized-not-Stable positions (M5 §1.1's binding non-implication; the F₉ shape
of M5 §5.2 is the witness that such positions can exist). So the walk's exact
question: does any consumer read the LINEARITY conjunct (or stability-as-such)
at realized positions? The census:

- W5 (compiled corpus): the alphabet law's only compiled consumer face is
  `MovesU.DefsLedger.UpstreamTyped.cl8_eq2 : ∀ p, EQ2law p (KC.AD p)`
  (DefsLedger.lean:162/179 — CL-8's typed [2r] face), plus the H4 side
  (`Scaffold/HDischarge/H4/TypedFaces.lean`:43, `eq2law_of_skel` at
  Kernel.lean:68). `EQ2law` (MovesRBase/Defs.lean:119–122) is
  ∀ δ x, `Stable p AD δ x` → (linearity ∧ dim-scale): **both conjuncts sit
  BEHIND the Stable guard, inside the law**. Every compiled consumer consumes
  the guarded law as a whole; NO compiled site applies either conjunct at a
  realized position through `UBXbStmt` (whose only consumer is
  `k3delta_of_ubxb` + the AxChk census — §2's grep).
- W6 (prose corpus): Step 16's own text reads the linearity conjunct only
  inside W2 (the proved δ-stable-scoped package). The K3-c consumer reads
  exponents only (Site 1). M5 §7's consumption map confirms the Route-B chain
  itself consumes no stability (`AlphabetData.Stable` "appears in NO hypothesis
  or proof — only in §5's non-implication discussion"). No other prose
  consumer exists by §2's completeness warrant.

**Finding: H4M7 §4's fallback trigger — "a Step-16 consumption of stability
BEYOND the exponent law" — did NOT fire on the written corpus.** No on-file
text, prose or compiled, consumes stability-as-such or linearity at realized
positions.

### 4.3 The walk's honest limit

The clause-(UB) COMPOSITION — the derivation that would assemble (a)+(b)+(c)
into "same R_τ at q = p^δ" — is unwritten (members (a) and (c) are open). The
finding of §4.2 is therefore a CENSUS OF EXISTING TEXT, not a theorem that no
future composition will need the dropped conjunct. RB-DELTA's clause (i)
withdrawal display is the safety property: with the stability form RETIRED as a
row (displayed, not silently), any future composition that needs
linearity-at-realized-positions or stability-as-such CANNOT silently presume it
— it re-enters as NEW displayed open content at composition time. This is the
honest-`sorry` analogue at ledger level.

## §5. The consumer-need verdict + the F-B ledger read

**VERDICT: RB-DELTA is CONSUMER-VISIBLE (per the §1.4 definition), at all three
site families — NOT DISPLAY-ONLY.** Per site: need-exact and strictly improving
at clause (R)/K3-c (Site 1); a supply narrowing at clause (UB)/Step 16 whose
dropped part has no on-file consumer (Site 2, §4.2), guarded by the §4.3 limit;
a Prop-content statement change with preserved derivation shape at the Lean
faces (Site 3). Because the verdict is CONSUMER-VISIBLE, the adoption is a
genuine ledger statement change and the Asvin gate is substantive — the
DISPLAY-ONLY branch, under which the gate could have been discharged as
housekeeping, is REFUTED by Sites 1 and 3 alone.

F-B firing conditions (H4M7 §4), read on the current record — a walk-grade
read; the ROOT owner confirms at adjudication: (1) F-(i) of H4M7 §2 — M1 is
ACCEPTED and M5's [M1-STATUS] row was refreshed at the pass-5 fold (M5 §7);
READS SATISFIED. (2) M4's verdict recorded and clean — M4 at rev 4 after three
folds, adjudication on record (ROOT:2494 REVISION 21 bracket); READS SATISFIED.
(3) N-K3 clean against M5 §6's sealed predictions — ALL GREEN 376/376, commit
5fdf350, no M4 branch fired; SATISFIED. (4) M5's second consecutive clean pass
— passes 6+7 CLEAN, model-diverse (textual bar MET); SATISFIED. (5) Asvin
sign-off + the ROOT owner's acceptance pass on this walk — **THE ONLY OPEN
GATE**.

## §6. The exact question Asvin's gate decides (decision-ready)

Signing off adopts RB-DELTA (§1.3: (UB-X) member (b) and its (K3-δ) fragment
rewritten from the stability/exhaustion form to the intrinsic law (RB), with
the stability form displayed as WITHDRAWN, members (a)/(c) untouched, executed
by the ROOT owner at the next O-11/O-8b touch). What it buys: clause (R)'s last
(H4)-family obligation converts from an open [M] fragment to content
PROVED-conditional at the M5 record (residue: M5's five perimeter rows — the
corpus's standing REL.1 run-production/ledger + catalogue-grammar
conditionality), and this is need-exact for that consumer (§3 Site 1, N-K3
376/376 green). What it costs: member (b) at Step 16 stops promising
F_{p^δ}-linearity at realized-not-Stable positions — a conjunct this walk found
NO on-file consumer for, prose or compiled (§4.2), but whose absence-of-need
rests on a census of written text while the clause-(UB) composition remains
unwritten (§4.3); the mandatory withdrawal display guarantees any future need
re-enters as new displayed open content rather than a silent presumption. Note
also that the current wording is not a safe default: M4 adjudicated member (b)
NOT WELL-POSED as worded, and M5 §5.2's F₉ shape witnesses that the stability
form can fail where (RB) holds. **The question: adopt RB-DELTA at full scope on
the strength of this census (the walk found H4M7's fallback trigger unfired),
or decline/narrow — firing the re-scope at the clause-(R)/(K3-δ) interface only
and leaving member (b) at Step 16 in its current stability form as OPEN (H4M7
§4's displayed fallback branch)?** The walk's finding supports the full scope
as drafted; the fallback remains available at zero additional proof cost.

## §7. What this note consumed; fenced out

CONSUMED (read-only, each at its displayed status): ROOT_ASSEMBLY_2026-08-02.md
§3.1 (UB-X) row (:2494, both revision brackets incl. the (K3-δ) audit note),
Step 16 (:1952–2031), the clause-attribution display (:1214–1251), the
(H4a)-split record (:1070–1075); H4M7_rootsync_brief.md §4 (drafted delta, F-B,
walk items (a)/(b)), §5, §6; H4M4_k3delta_adjudication.md §1, §5.2, §5.4, §5.5,
§6; H4M5_k3delta_attempt.md §1.1–1.3, §2.2–2.3, §5.1–5.3, §7, §8 (N-K3
results); SYNTHESIS_PASS6/7 (charter texts); Lean read-only:
Scaffold/Hypotheses.lean, Scaffold/HDischarge/H4/Kernel.lean + TypedFaces.lean,
MovesRBase/Defs.lean, MovesU/DefsLedger.lean.

FENCED OUT (honesty invariant): no re-scope applied; no Lean edit; no discharge
of (K3-δ), member (b), or any row; no claim about members (a)/(c) beyond their
untouched display; no theorem that the (UB) composition avoids the linearity
conjunct (§4.3 is the displayed limit). ADOPTION stays Asvin-gated; this note
is the gate's input, not its exercise. Verifier-implementation note (pass-7
F2c): this unit is a doc walk with no numerics of its own — the anticipated
verifier action is CITE-CHECKING (every quote above carries file:line or file/§
anchors), not a from-note implementation.

