# L3CONVERGENCE — the convergence judgment on the level-3 pattern hunt: five decorrelated methods, ZERO splits — the sealed (ITER-LAW-3) IS the measured law (the briefed "RED" is contradicted by the artifacts on six independent reads), the mechanism is the ε-chain gauge with a single inflated top carry, and the next sealed cycle is the n = 4 (ITER-LAW-n) probe

**POST-SEAL DIAGNOSTIC — NON-PREREG. CONVERGENCE-JUDGE UNIT of the level-3
pattern-hunt fleet** (Asvin's 2026-08-03 directive: "run small numerics to see
what the pattern is too! use a multitude of methods and checks to converge on
the correct proof strategy"). The sealed prereg (`ITERLAWR_PROBE_2026-08-08.md`
S0–S4 + `verification/openmath/iterlawr_probe.py`, seal **911431c**) is
READ-ONLY and byte-untouched. This unit ran NO new battery: it is the
adversarial cross-reading of the five method notes + the sealed artifacts, with
every judgment cited to a committed count. Everything pattern-shaped below is
CONJECTURE grade until a sealed confirmation cycle; the only ACCEPTED object in
scope is the order-2 ITER-LAW (ledger block "ITER-LAW ACCEPTED — 2026-08-03").
Wallclock 2026-08-03, campaign date 2026-08-08.

**Inputs judged** (all committed): the sealed run artifacts
(`iterlawr_probe_output.txt`, `iterlawr_probe_results.json`, m4 supplement
pair, separation analysis) + the recovery-filled S5/S6 of the prereg note ·
M5 = `L3XCHECK_2026-08-08.md` (cross-implementation) · M1 =
`L3FIT_M1_2026-08-08.md` (from-data exact fit) · M2 = `L3COHOM_M2_2026-08-08.md`
(coboundary/gauge) · M3 = `L3RAMPS_M3_2026-08-08.md` (degenerations/ramps) ·
M4 = `L3FINGER_M4_2026-08-08.md` (fingerprints) · ground truth =
`ITERLAW_2026-08-08.md` S5–S6 (the accepted order-2 law).

## S0. The judge's own RED adjudication (formed from raw artifacts first)

Read directly from `iterlawr_probe_results.json` + `iterlawr_probe_output.txt`
before weighing any note's opinion: the `violations` array is **EMPTY**; all 13
predicted-0 families printed 0 observed (F0a 0/896 · F0b 0/64 · G1 0/1344 ·
G2 0/13440 · G3 0/13440 · G4 0/13440 · G5 0/9408 · G6 0/576 · G7 0/26880 ·
G8 0/20000 · CERT2 0/702 · CERT3 0/52 · CERT3P 0/26); all coverage meters MET;
harness events none. The printed "VERDICT (preregistered rows): RED" traces to
exactly one line: `MUTATION m4: detected 0 (pred >= 1) NOT DETECTED — battery
lacks teeth` — the strict exit rule, which the seal's own S3 wording classifies
as PARTIAL (teeth), not RED (law). The sealed m4 supplement
(`iterlawr_m4_supplement_output.txt`, own seal, rows M4A/M4B with g₂ = 2 so
z̄ ≠ 1): S1 sealed battery on the new rows 0/19,792 GREEN, m4 detected 8/8
towers on every divergence pair, "SUPPLEMENT VERDICT: ALL GREEN".

**JUDGMENT: the briefing premise "the sealed level-3 cocycle candidate came
back RED" is CONTRADICTED by the artifacts.** The sealed primary (ITER-LAW-3)
was violated NOWHERE (123,564 samples across run + supplement). Six
decorrelated readers of the same raw artifacts — the recovery unit's S5 fill,
M5, M1, M2, M3, M4 — reached this identically, each before reading the others
(decorrelation stamps in each note's header). The d0e2b74 checkpoint phrase
"verdict RED on preregistered rows" quotes the strict exit line only. This is
the single most-confirmed fact of the whole hunt.

## S1. THE AGREEMENT MATRIX (methods × claims)

Columns: **SEAL** = the sealed prereg run + m4 supplement + its S6 separation
analysis; **M5/M1/M2/M3/M4** = the method notes. Cell: ✓ = supports with an
exact committed count; ( ✓ ) = supports via an equivalent/adjacent formulation;
— = not probed by that method. Verdict: CONVERGED (≥ 2 decorrelated methods,
none contradicting) / SINGLE-SOURCE / SPLIT. **There are NO SPLIT rows.**

| # | claim | SEAL | M5 | M1 | M2 | M3 | M4 | verdict |
|---|---|---|---|---|---|---|---|---|
| 1 | harness validity (apparatus measures the true cocycle) | ✓ F0a 0/896, F0b 0/64 | ✓ HARNESS-CONFIRMED: matched-instance 4,224/4,224 + anchors 416/416 (S6) | ✓ M5-xharness 20/20 (S4) | ✓ XCONST 0 mism. + M5 leg 20/20 (S1,S7) | ✓ F0a all subtowers + M5 ties 116/116 (S1) | ✓ SPOT-A 4/4, SPOT-B 248/248 (S2) | **CONVERGED 6/6** |
| 2 | "RED" = m4-teeth-only; no predicted-0 violation | ✓ JSON violations [] | ✓ S6 autopsy | ✓ header read | ✓ header read | ✓ S0 autopsy | ✓ S0 autopsy | **CONVERGED 6/6** (+ this unit's own read, S0) |
| 3 | (ITER-LAW-3), composed form = the measured law | ✓ G3 0/13,440 + 0/19,792 suppl. | ✓ 248/248 fresh exhaustive (S5) | ✓ UNIQUE minimal fit; field 1,244/1,244 (S2,S3) | ✓ 1,616/1,616 incl. 4 fresh E₃=6 shapes (S2ii) | ✓ 0/6,320 field-exact (S1) | ✓ FP0 0/1,600 (S2) | **CONVERGED 6/6** |
| 4 | naive per-level template REFUTED as the level-3 law | ✓ 1,752/13,440 viol.; 520 NONINT + 64 field-diff census | — | ✓ 234/1,244 (186 NONINT + 48 value), distractors never needed | ✓ fails 2-COCYCLE IDENTITY on every separating e₁ ≥ 2 tower (96/512 triples × 16) — not repairable by ANY gauge | ✓ 816 NONINT + 576 ID over 97 towers | ✓ warrant fails 164/1,600 field + 5,653/20k symbolic | **CONVERGED 5/5 probed** — M2's associativity kill is the strongest form |
| 5a | first-deviation locus: EVERY naive-vs-composed separation at δ₃ = 1; none at δ₃ = 0 | ✓ (L1) 584/584 | — | ✓ 234/234; 929/929 clean at δ₃=0 | ✓ 252/252 | ✓ e₂=1 kills all (S2-i) | ✓ both censuses 0 at δ₃=0 (S8) | **CONVERGED 5/5** |
| 5b | discrepancy factor = pure z₁^{D₁−D₁ⁿᵃⁱᵛᵉ} (never z̄/z₂) | ✓ (L2) 64/64 | — | ✓ (c-slot only) | ✓ tables S5 | ✓ 0 bad/192 field-diff | ( ✓ ) shadow-deviation z̄-free 0/224 (different reference object, same z̄-freeness) | **CONVERGED 4/4** |
| 6 | gauge/coboundary reading: c₃ = z̄^{δ₃}·∂F, F = ε-chain ε₂(β₂)·ε₁(β₁) = val₃(φ_γ) | ( ✓ ) G1 (L6-3 anchor = ε-chain) + G2b (W-MULT-3) ⟹ same identity | ( ✓ ) anchor = ε-chain in own harness | — | ✓ 0 viol/44 towers, F closed-form + monodromy ρ (S2iii) | — | ✓ B0a genre 0/1,600, disjoint roster & code (S5) | **CONVERGED 4/4** (M2+M4 direct, SEAL+M5 by the equivalent L6/W-MULT pair) |
| 7 | correction-term factorization Δ = Σ−γ₂δ₂ = γ₂J + δ₃γ₃κ, κ = ℓ₁′−ℓ₁w₁Φ₁; master switch δ₃, e₁=1 kills γ₂J & pins κ=+1, e₀=1 kills visibility not the term, g₂ absent | ✓ S6 identity display + G8 | — | ✓ unique size-3 refit = exactly this composition (S2) | ( ✓ ) defect identity defect(numN)=ℓ₀γ₂·defect(δ₂) (S4a) | ✓ 0/6,320 residual; vanishing table + 8 ramps exact (S2,S4) | — | **CONVERGED 4/4** |
| 8 | denominators per level: e₁ (middle, cured by ℓ₁γ₃δ₃), e₀ (inner, cured by ℓ₀Σ), NEVER e₀e₁ | ✓ G4 0/13,440 + G8 | — | ✓ all fits integral; alternatives NONINT | ✓ integer layer 44/44 | ✓ NI census | ✓ FP7 field+20k symbolic, e₀e₁-phantom counter = 0 (S8) | **CONVERGED 5/5** |
| 9a | e₂ = 1 degeneration: level 3 = embedded measured level 2 exactly | ✓ F0b + G6 0/576 | ✓ e₂=1 rows in 248/248 | ✓ T04 control | ✓ CX/S-rows | ✓ TWO ways: split-chain verbatim + value tie 0/560+0/560 (S3) | ✓ Q4D/Q6D controls | **CONVERGED 6/6** |
| 9b | subwindow restriction INSIDE e₂ ≥ 2 towers: c₃ on (e₂ℤ)² = embed(measured c₂) ON THE NOSE + inflation-defect identity (deviation only at δ₃=1, z̄-free) | — | — | — | — | — | ✓ R1 0/516; R2 0/1,600 (S6) | **SINGLE-SOURCE (M4)** — consistent with rows 5a/6; prereg gate for n=4 |
| 10 | measured c₃ (and δ₃, D₂, D₁ at integer level) satisfy the 2-cocycle identity; swap-symmetric; ℤ/E₃ descent per-argument | ✓ G5 0/9,408 + G7 0/26,880 | — | — | ✓ all E₃³ triples ×44, window AND quotient; integer layer 0 defects | — | ✓ 0/4,384 triples + FP2 0/1,600 + FP3 0/6,208 | **CONVERGED 3/3** |
| 11 | H²-class invariant: class(c₃) = [z̄^{(E₃/e₂)ℓ₂}·z₂^{e₀ℓ₁}·z₁^{ℓ₀}] ∈ M/M^{E₃}; κ_ℤ(δ₃) = (E₃/e₂)ℓ₂ | — | — | — | ✓ 42/42 field-visible; κ_ℤ 44/44 (S3) | — | ( ✓ ) top slot = inflated ℤ/e₂ carry table (FP6) supports the z̄-component only | **top component CONVERGED 2/2; full letter-tail class SINGLE-SOURCE (M2)** |
| 12 | J-window fine structure: J = D₂−δ₂ ∈ {⌊ℓ₁γ₃/e₁⌋, ⌈ℓ₁γ₃/e₁⌉} on δ₃=1 pairs, selection = b₂-phase | — | — | — | — | ✓ 72/72 towers, 0 exceptions (S6-J) | — | **SINGLE-SOURCE (M3)** — prereg target for n=4 |
| 13 | (LIFT-DEV-3): c₃^plain = c₃^canon·ε₁(γ₂)^{δ₂} at g₁ = 1; invariant at g₁ ≥ 2 — the law is pinned to the canonical ε₁-corrected Φ₂ | — | ✓ census 192/192 + 4,032 agreeing (S7) | ✓ confirmed on fresh off-roster T07, 2 forced pairs (S4) | — | — | — | **CONVERGED 2/2** (conjecture grade; sealed cycle owed; both field legs ride M5's plain-lift implementation — a third implementation would fully decorrelate) |
| 14 | integer data ring- & characteristic-blind (ℤ_p vs F_p[[t]] at fixed p) | — | — | — | — | ✓ 17/17 rungs × 4 configs identical | ✓ 22/22 fingerprint pairs identical | **CONVERGED 2/2** |
| 15 | value-set multiplicity spectrum = product of per-level carry counts | — | — | — | — | — | ✓ (V2), all 44 towers | **SINGLE-SOURCE (M4)** |

**Hostility check (what a split would have looked like, and why none is one):**
(i) the per-method naive-failure COUNTS differ (584 / 234 / 252 / 816+576 /
164) — different rosters and blocks; the invariant content (row 5a locus, row
5b factor, row 8 mode) is identical everywhere. (ii) M4's shadow-deviation
carries z₂ components where the seal's S6 says "pure z₁" — different reference
objects (measured level-2 shadow vs naive template); both are z̄-free, no
contradiction. (iii) M3 writes Δ = δ₃(γ₂J + γ₃κ) vs the seal's
Δ = γ₂(D₂−δ₂) + δ₃γ₃κ — equal because J ≡ 0 at δ₃ = 0 (M3 verified 0
exceptions). (iv) M2's per-tower "is c₃ fully a coboundary over M/K₃^×"
verdicts VARY across towers — that is per-tower letter-order arithmetic in a
fixed class, not a law-level disagreement. (v) The S4(b) trap (naive ≡
measured on ALL 36 pairs of ℤ₃-S2 despite the wrong ℤ-class) is M2's finding
and nobody else's counter-evidence: it is precisely why row 4's verdict rests
on associativity + warrant legs, not on value counts alone.

## S2. THE LEVEL-3 LAW(S) OF RECORD (at most two, per the charge)

**There is no "corrected" law: the sealed candidate needed no correction.**
The charge's premise (a RED demanding a corrected candidate) is disposed of by
S0/row 2. The two displays below are therefore the CONFIRMED law and its
FORWARD generalization — both conjecture-grade (measured law; no proof unit
has run).

> **[r38 STATUS BRACKET 2026-08-03 (the acceptance fold; the display upgrade
> owed at acceptance, applied) — CANDIDATE 1 IS NOW ACCEPTED-OFF-DCX.** The
> "(measured law; no proof unit has run)" gloss above and every
> "conjecture-grade" status for Candidate 1 in this section are superseded as
> dated composition-time history. The ledger defined-term block, pasted per
> checklist rule 8: "[IL3] = the (ITER-LAW-3) theorem, ACCEPTED 2026-08-03:
> the display c₃ = z̄^{δ₃}·z₂^{D₂}·z₁^{D₁} (δ₃ = ⌊(s₃+s₃′)/e₂⌋; D₂ =
> (s₂-defect + ℓ₁γ₃δ₃)/e₁; Σ = D₂γ₂ + δ₃γ₃(ℓ₁′−ℓ₁w₁Φ₁); D₁ = (s₁-defect +
> ℓ₀Σ)/e₀) PROVED on the 3-read standard-lift harness class OFF the DCX
> corner {g₀ = g₁ = 1, δ₁ = 1, s₂(γ) = s₂(γ′) = e₁−1, S₁+s(γ₂) ≥ 2e₀} (EMPTY
> at e₀ ≤ 2) — RUNG n = 2 of (ITER-LAW-n). RESIDUALS: (W-MULT-3-DCX) OPEN
> (instance-true 132/132); the fenced non-consumed DEV-1 corner
> {e₀=1 ∧ d₀≥2 ∧ j≥g₀}; the consumed [IL] base's package rides through."
> Arc: proof compose `ITERLAW3_PROOF_2026-08-08.md` 8ba1820 → two convergent
> NOT-CLEAN rounds → errata f9daca0/a687b80 → round 3 DOUBLE-CLEAN (PC3
> 5bdca01 + PE3 f288b73); acceptance f726b36. CANDIDATE 2 ((ITER-LAW-n))
> REMAINS A CANDIDATE — the general-n compose (`ITERLAWN_PROOF_2026-08-08.md`,
> 5fe901e) is composed-with-displayed-opens with its own hostile arc OWED;
> its owed Candidate-2 status upgrade fires only AT THAT NOTE'S ACCEPTANCE,
> not here. Candidate 2's "Confirmed instances" line below upgrades to the
> rung ledger of record: n = 1 = [IL] ACCEPTED ("Scope as accepted: the
> harness tower class (superset of DITER_RESTATE §S2's E₂ > 1 pin;
> on-the-nose coincidence restricted to E₂ > 1); residuals: (ITER-LAW-LIFT)
> open exactly at g₀ = 1 ∧ δ₁ = 1; gr(w₂) wrapper rides the accepted GRB
> retarget; orders ≥ 3 untouched" — standard-lift, strict superset of the
> pinned E₂ = e₀e₁ > 1 setting) · n = 2 = [IL3] ACCEPTED-OFF-DCX (block
> above) · n = 3 = MEASURED GREEN only (ITERLAW4_PROBE verdict 13b026b:
> 0/362,624 on 72 fresh towers × 4 rings; F-SUB subwindow gate 2,896/0).
> Candidate 1's known-conditionality paragraph stands, with the acceptance's
> own scope note: on the DCX corner the display remains an OPEN lemma
> ((W-MULT-3-DCX)), instance-true everywhere probed.]**

### Candidate 1 (the law of record at level 3) — (ITER-LAW-3), verbatim the sealed S1 primary

For standard-lift 3-read towers (reads (e₀,h₀,ψ₀), (e₁,h₁,ψ₁), (e₂,h₂,ψ₂),
gcd(eᵢ,hᵢ) = 1, canonical ε₁-corrected Φ₂, E₃ = e₀e₁e₂), with the iterated
eq-(12-int) splits s₃, s₂, s₁ and window W₃ (u₁ ≥ 0):

    c₃(γ,γ′) = z̄^{δ₃} · z₂^{D₂} · z₁^{D₁},   where

    δ₃ = ⌊(s₃(γ)+s₃(γ′))/e₂⌋ ∈ {0,1}                          [top carry]
    D₂ = (s₂(γ)+s₂(γ′)−s₂(γ+γ′) + ℓ₁·γ₃·δ₃)/e₁ ∈ ℤ            [middle exponent]
    Σ  = D₂·γ₂ + δ₃·γ₃·(ℓ₁′ − ℓ₁·w₁Φ₁) ∈ ℤ                    [composed inner shift]
    D₁ = (s₁(γ)+s₁(γ′)−s₁(γ+γ′) + ℓ₀·Σ)/e₀ ∈ ℤ                [inner exponent]

with z̄/z₂/z₁ the level-3/2/1 letters, γ₂ = e₁w₁Φ₁+h₁, γ₃ = e₂w₂Φ₂+h₂,
w₁Φ₁ = e₀g₀h₀, w₂Φ₂ = e₁g₁γ₂, (ℓ₀,ℓ₀′), (ℓ₁,ℓ₁′) the normalized Bézout pairs.
Equivalent forms, proven-equal-on-instance: the realized-shift form
D₁ = (s₁-defect + ℓ₀·(β₁″−β₁−β₁′))/e₀ (M5/M1: Σ = the realized shift on every
probed pair), and the gauge form c₃ = z̄^{δ₃}·∂F, F = ε₂(β₂)·ε₁(β₁) (M2/M4).

**Evidence trail:** sealed battery 0 violations/103,772 + supplement 0/19,792
(SEAL) · independent reimplementation 248/248 + matched-instance 4,224/4,224
(M5) · from-data unique minimal fit, train 656/656 + holdout 588/588 (M1) ·
1,616/1,616 incl. four never-probed E₃ = 6 shapes (M2) · 6,320/6,320 across a
17-rung degeneration ladder + 8 ramps to E₃ = 20 (M3) · 1,600/1,600 + shape
fingerprints (M4). Total ≈ 133k field-exact samples, six methods, zero
violations anywhere.

**What would refute it:** any single pair on the class violating the display
(the seal's S1 falsifier taxonomy 1–8 stands verbatim); at the structural
level — a failure of the subwindow restriction (M4 R1), of the 2-cocycle
identity (M2/M4), of a per-level warrant (M4 S8), or of the H²-class (M2 S3)
on any new tower. **Known conditionality:** pinned to the canonical
ε₁-corrected Φ₂ (row 13, LIFT-DEV-3: the plain lift differs by ε₁(γ₂)^{δ₂}
exactly on g₁ = 1 ∧ δ₂ = 1); (ITER-LAW-LIFT) open; orders ≥ 4 untouched.

### Candidate 2 (the forward, all-orders form) — (ITER-LAW-n), the uniform recursion

For an (n+1)-read standard-lift tower with canonical ε-corrected lifts, splits
s_{n+1}, …, s₁, top carry δ = ⌊(s_{n+1}(γ)+s_{n+1}(γ′))/e_n⌋:

    S_n := δ·γ_{n+1};    for j = n, …, 1:
      D_j     := (s_j(γ)+s_j(γ′)−s_j(γ+γ′) + ℓ_{j−1}·S_j)/e_{j−1} ∈ ℤ,
      S_{j−1} := D_j·γ_j + S_j·(ℓ_{j−1}′ − ℓ_{j−1}·w_{j−1}Φ_{j−1});

    c_{n+1}(γ,γ′) = z̄^{δ} · z_n^{D_n} · ⋯ · z₁^{D_1}
                  = z̄^{δ} · ∂F_n,   F_n(γ) = Π_{j=1}^{n} ε_j(β_j(γ))   [gauge form]

with the gauge-independent invariant (INV-n, M2's display):
class(c_{n+1}) = [Π_{j=0}^{n} z_{j+1}^{(e₀e₁⋯e_{j−1})·ℓ_j}] ∈ M/M^{E_{n+1}}.
Confirmed instances: n = 1 (the ACCEPTED order-2 theorem, ITERLAW S5),
n = 2 (this hunt, Candidate 1). **What would refute it:** the n = 4 probe
(S4 below) — a deviation from the recursion, a subwindow-restriction failure
against measured level 3, an INV-4 class mismatch, a cross-level denominator
event, or a g_top-dependence (M3: g_top provably absent at n = 3).

**Explicitly NOT a candidate:** the naive per-level template
(D₁ⁿᵃⁱᵛᵉ = (s₁-defect + ℓ₀γ₂δ₂)/e₀) is REFUTED (row 4) — dead beyond appeal:
it fails associativity wherever it separates (M2), fails its own integrality
on the majority of separating pairs (all methods), and its instance-agreement
episodes (ℤ₃-S2) carry the wrong ℤ-class.

## S3. THE PROOF-STRATEGY READING (the deliverable of the 2026-08-03 directive)

The converged pattern says the all-orders mechanism is **M2's outcome (a),
strengthened, and NOT a new cohomology class per level**: modulo the single
top carry, the entire level-(n+1) cocycle is an EXPLICIT COBOUNDARY of the
ε-chain gauge of the anchor lifts — c_{n+1} = z̄^{δ_top}·∂F_n, measured exact
at n+1 = 2 (accepted proof, ITERLAW S6.2), n+1 = 3 (M2 44/44 towers; M4
0/1,600 on a disjoint roster and code path). All the intricate constants
(D_j, the composed shifts S_j) are the exponent bookkeeping of ∂F through
per-level EXP; the only irreducible 2-cocycle content is the top carry,
inflated from ℤ/e_top (M4 FP6: the quotient carry table verbatim), and the
only gauge-independent invariant is the single class (INV-n).

**The strategy this converges on (per-level gauge normalization; general-r
conjecture uniform in r and p):**

1. **Induction carrier = the ε-chain gauge, not the exponents.** State the
   induction on val_n of anchor products, as two lemma families per level:
   **L6-(n)** (anchor value = ε-chain: val_n(φ_γ) = Π_j ε_j(β_j(γ)) — one
   slot, one digit per level; routine) and **W-MULT-(n)**
   (val_n(φ_γφ_{γ′}) = z̄^{δ_top}·val_n(φ_γ)·val_n(φ_{γ′}) — the one hard
   step: top-level Case-I/Case-II development analysis with level-(n−1)'s L7
   consumed verbatim on the inner digit A = the level-(n−1) anchor pair
   product, exactly the seal's "L7 stacked on L7" provenance). The cocycle
   identity, closed form, integrality, and the (ITER-LAW-n) recursion are
   then DERIVED corollaries — never guessed or hostile-passed
   constant-by-constant again (M2 S6.1; M1's "cheapest proof route" concurs
   from the fit side: prove the realized-shift form, then the closed
   Σ-composition is the already-G8-verified integer identity).
2. **Integrality architecture is per-level.** One Bézout kernel (EXP lemma)
   per level, denominators e_{j−1} each cured by its own level's carry
   correction, NO cross-level denominators (M4 S8: e₀e₁-phantom counter 0 on
   field + 20k symbolic). The EXP-(n) lemma bank can be stated and proved
   level-locally.
3. **The correction over the naive lift is a product of one datum per level**
   (M3 S6): top carry δ_top (master switch; e_top = 1 kills everything) →
   transported through each middle level's Bézout phase (the J-window + κ) →
   landed by ℓ₀/e₀ (visibility switch e₀ = 1). g_top enters nothing. This is
   the same one-EXP-per-level shape as item 1 — the degeneration ladder is
   the vanishing-pattern shadow of the gauge mechanism.
4. **Consumer/semantics interfaces should key to the class + the gauge, not
   the raw constants** (M2 S6.3): the invariant content is
   K[u]/(u^{E} − σ), σ = the (INV-n) monomial — the Eisenstein-type
   presentation echoing the MOVES carry algebra D = F[u]/(u^e − z̄)
   (conjecture-grade identification). Cocycle-constant-keyed interfaces
   overfit to a gauge choice.
5. **Lift normalization is part of the statement.** The law is pinned to the
   canonical ε-corrected lift class; LIFT-DEV-3 (row 13) is the measured
   failure of gauge-invariance under lift change at the cascade stratum, and
   any (ITER-LAW-LIFT) unit inherits ε₁(γ₂)^{δ₂} as its first quantitative
   target.

**Honest SPLIT scenarios: none arose.** No method contradicts another on any
probed claim. The residual risks are single-source cells, not splits: the
full H²-class letter-tail (M2 only — and M2's own S8 flags that the top-carry
letter image lay in M^{E₃} on every roster tower by small-letter-order
happenstance; an E₃ = 16, g₂ = 2 row is needed), the J-window law (M3 only),
the subwindow/inflation-defect identities (M4 only), the multiplicity
spectrum (M4 only). Each is a cheap prereg leg below rather than a reason to
withhold the strategy.

## S4. THE NEXT SEALED CYCLE SPEC (design only; the run is the next unit's)

**Unit: ITERLAW4_PROBE — the n = 4 confirmation of (ITER-LAW-n)** (Candidate
2 at its first unconfirmed order; level 3 needs no correction cycle, so the
confirmation budget moves up an order). Two-commit seal per the house
pattern: prereg S0–S4 + runner committed pre-run; fresh seed never used in
the genre; `--design` disclosure mode; exact arithmetic; JSON + transcript.

* **Class/calculus:** standard-lift 4-read towers, canonical ε-corrected
  lifts at EVERY level (Φ₂ ε₁-corrected, Φ₃ ε₂-corrected — the LIFT-DEV
  lesson made a design pin); val₄ via K₄ = K₃[z̄₄]/(ψ₃); anchors by the
  4-fold iterated eq-(12-int) split; E₄ = e₀e₁e₂e₃.
* **Primary candidate (sealed):** the S2 Candidate-2 recursion at n = 4,
  every constant named in the prereg. **Distinguished alternative:** the
  "level-3-naive" template (S₂ replaced by γ₃δ₃-naive composition at the new
  level) — the exact analogue of this cycle's separation pair, with
  separation meters MSEPi/MSEPf preregistered.
* **Roster:** tiny exhaustive first — E₄ ∈ {8,12,16} full W1×W1 blocks, all
  four rings, p ∈ {2,3}; per-level controls e_j = 1 (each recovering the
  MEASURED level-3 law two ways, M3-S3 style); stacked e₀ ≥ 2 rows; mixed-e
  rows; an E₄ = 16 row with g₃ = 2 AND letters at every level (M2's S8 flag:
  test whether the top-carry image escapes M^{E₄}); one E₄ ≥ 24 stretch row;
  MIXED translate blocks in the FIRST battery (COR-4 lesson, carried).
* **Falsifier legs (predicted 0 unless marked):** F0 = level-3 fidelity
  (measured c₃ == (ITER-LAW-3)) on every 3-subtower; **F-SUB (new, the M4
  gate, run FIRST): c₄ restricted to (e₃ℤ)² == embed(MEASURED c₃ of the
  subtower) on the nose**; F-INF: the R2-style inflation-defect identity
  against the measured level-3 shadow (deviation only at δ₄ = 1, z̄₄-free);
  G-legs: anchor = ε-chain, W-MULT-4 both forms + single-top-slot shape,
  the candidate closed form, per-level warrants e_j | (s_j-defect +
  ℓ_{j−1}S_j) with a NO-cross-level-denominator counter (predicted 0
  phantom events), cocycle identity on triples (measured AND closed form,
  window AND quotient), swap symmetry, ℤ/E₄ descent per-argument, symbolic
  integer layer ≥ 20k draws; F-CLASS: κ_ℤ(δ₄-table) = (E₄/e₃)ℓ₃ and
  class(c₄) = [z̄₄^{(E₄/e₃)ℓ₃}·z₃^{e₀e₁ℓ₂}·z₂^{e₀ℓ₁}·z₁^{ℓ₀}] (INV-4);
  F-J: the M3 J-window at each middle level (D_j − carry ∈
  {⌊ℓ_{j−1}γ_{j+1}…⌋, ⌈…⌉} — exact display per level in the prereg);
  M3-fences: master switch δ₄, per-level e_j = 1 collapse pattern,
  g_top-invariance (frozen-row prediction); CERT legs incl. PARI on ℤ_p.
* **Mutation controls — the repaired m4 genre WITH TEETH:** every mutation
  row has ALL letters of genuine order (g_j = 2 at every level; letter
  nontriviality asserted per mutation AT DESIGN TIME — the M4A/M4B lesson
  institutionalized) AND every mutation carries a WARRANT-LEVEL (integer)
  detection leg in addition to field detection (M3/M4: warrant beats value —
  a wrong exponent is detected structurally even where the field is blind).
  Mutations: m1 drop the new-level correction ℓ₂γ₄δ₄ from D₃; m2 naive
  composed shift at the new level; m3 D₃ denominator e₂ → e₁ (teeth rows
  e₁ ≠ e₂); **m4′ δ₄-modulus e₃ → e₂ on rows with e₂ ≠ e₃ ∧ g₃ = 2, PLUS the
  structural top-slot leg (read slot index ≠ δ₄) that fires even at
  z̄₄ = 1**; m5 drop ε₃ from the anchor prediction; m6 letter swap. Each
  predicted detected ≥ 1 with per-row divergence-pair counts; any
  undetected mutation ⟹ verdict PARTIAL (teeth), wording pre-agreed in the
  seal (this cycle's lesson: the exit line must distinguish law-RED from
  teeth-PARTIAL by construction).
* **Secondary sealed leg (small, same unit or its own file):** LIFT-DEV
  confirmation — fresh g₁ = 1 rows + a THIRD lift implementation,
  preregistered factor ε₁(γ₂)^{δ₂} (and its level-3 analogue ε₂(γ₃)^{δ₃} at
  g₂ = 1, currently unmeasured — a genuine new prediction of the gauge
  mechanism, cheap to test).
* **Decision default (for the wave-19 charter, not binding):** the compose/
  proof unit for (ITER-LAW-3) via L6-(n)+W-MULT-(n) can proceed IN PARALLEL
  with the n = 4 probe — S3's strategy does not wait on n = 4; the probe's
  role is to catch a wrong uniformization before the induction is drafted at
  general r.

## S5. Fences

No new battery was run by this unit; no seal, no Lean, no edits to any
sealed/accepted record (seal 911431c byte-untouched; ITERLAW / DITER_RESTATE
/ TWIST3 / the five method notes untouched). All S2/S3 content is
conjecture-grade measured law except where explicitly cited to the ACCEPTED
order-2 theorem. Orders ≥ 4: everything is prereg design, nothing measured.
(ITER-LAW-LIFT) propagates. The S4 spec is design-only — the next unit owns
the seal, the run, and the verdict.

— Convergence judge, level-3 pattern-hunt fleet, campaign date 2026-08-08
(wallclock 2026-08-03).
