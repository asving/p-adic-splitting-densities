# GRTJA — J-A, THE DICTIONARY JUNCTION: type data → tower data, the four operator equalities, and the covariance-corrected residual tie

**Unit:** J-A COMPOSER (weld dependency order: consumes W-2 clause 1 at attempt
grade; per `docs/in-progress/GRT_WELD_BLUEPRINT_2026-08-08.md` §S2.1/S3.1
rows W-0/W-1/W-4). **Genre: PROOF NOTE at attempt grade (acceptance counter
0/2; no gate fires; no acceptance arc opened), with sealed-runner machine
brackets.** SKELETON COMMIT — sections filled incrementally below; nothing in
this file is citable until the section is marked composed.

## PLAN OF RECORD (filled in this order)

- §1 Setting + the P-index dictionary (restated from W-2 §1.2, one screen).
- §2 THEOREM J-A-PIN (= W-0): T(t) in the [ILN]† class pin clause by clause,
  each clause from the print's own hypotheses (tex anchors); lift clause
  = W-2 clause 1, consumed at attempt grade with its exact conditionality.
- §3 The four operator equalities (= W-1 + W-4), each with derivation +
  GMN theorem-number citations + hypothesis lists:
  (i) w = v; (ii) the residual dictionary (per-slot character-gauge law);
  (iii) ε = the print twist; (iv) eq-(12-int) = abscissa/side coordinates
  + the carry/lattice-crossing clause.
- §4 Covariance transport: how each equality of §3 rides the (ξ,w) orbit /
  the W-2 clause-3 transported chamber.
- §5 Base cases consumed: N-1 (order 2, family-ledger, ACCEPTED) and GD-5
  (order 1, nearly closed) with their exact grades.
- §6 Honest boxes (JA-BOX-*): everything the print leaves underdetermined
  + the one blueprint-statement correction found while composing.
- §7 Records: machine table (from `verification/openmath/grt_ja_checks.py`,
  two-commit seal), claims/grades table, dependency line.

— J-A COMPOSER unit, campaign date 2026-08-08. SKELETON (commit 1 of N).
