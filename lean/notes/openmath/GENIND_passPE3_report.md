# GENIND pass PE3 — HOSTILE VERIFICATION REPORT (fresh context, third arc pass; the r2 text — LEMMA GENIND-6 and the re-pin — attacked for the first time)

**VERDICT: UNREFUTED, NOT CLEAN. 0 CRITICAL, 2 GAP, 4 MINOR.** The
2-clean count does NOT start (stays 0/2; an r3 is owed, then the count
begins afresh). No count law, no clause of THEOREM GENIND.A / GENIND.C,
no clause of LEMMA GENIND-6's mathematics, and no sealed number was
refuted — my fresh route (§7; disjoint from all four prior routes)
confirmed the transport machinery at **n = 7** (the corpus's first
n ≥ 7 composite evidence, a constructed mixed genre with a (3,1)-child)
and at the honesty box's own named blind spot (a stage STEEPER than the
child). The two GAPs are (i) a false factual sentence in the r2's own
(CS-EXACT) re-pin — "no GENH4 r3 existed at this commit — checked" is
refuted by the git log, and the standing disclosure rule the r2 adopts
is violated in the very repair that adopts it — and (ii) the M_l window
display of GENIND-3 (inherited verbatim by GENIND-6(a)), whose literal
reading is FALSE at multi-child genres with distinct slopes: I
constructed the counter-genre, and the sealed machinery itself computes
the capped value my derivation demands, refuting the display's literal
reading on 2,048 + 177,147 states.

**Target:** `lean/notes/openmath/GENIND_PROOF_2026-08-08.md` at HEAD
`b3c05be` (file last touched at r2 `52fb6ff`; arc: seal `c3212cc` →
PE1 `4259a93` → r1 `66200f2` → PE2 `50db400` → r2
`2abf419`/`ad6f31d`/`52fb6ff`). **Role:** hostile verifier, fresh
context, zero prior stake; I fix nothing; quote-and-classify only.
Both prior reports read first; none of their routes re-walked.

---

## 1. FINDINGS

### PE3-G1 [GAP] — The r2's (CS-EXACT) re-pin repeats the defect class it was repairing: "no GENH4 r3 existed at this commit — checked" is FALSE (r3 predates every r2 commit by 10–45 minutes), and the standing rule adopted in the same repair is violated at adoption.

S12, verbatim: *"The (CS-EXACT) n = 4 de facto discharge now cites
GENH4 @ r2 (fd35416 + 5279a11; **no GENH4 r3 existed at this commit —
checked**)"*; and the same S12 bullet adopts: *"Standing rule adopted:
pin suppliers at their repair HEAD and disclose any adjudicated,
not-yet-repaired findings at the pin."*

The git log refutes the checked claim and convicts the rule violation:

| event | commit | timestamp (2026-08-09) |
|---|---|---|
| GENH4 passPE3 report (adjudicates 1 MINOR + 2 remarks vs r2) | `3b88321` | 00:24:06 |
| **GENH4 r3** (repairs that MINOR) | `4b0d946` | **00:50:10** |
| GENIND r2 commit 1 (the re-pin itself) | `2abf419` | 01:00:35 |
| GENIND r2 commit 2 (GENIND-6 seal) | `ad6f31d` | 01:21:44 |
| GENIND r2 commit 3 (S12 carries the "checked" sentence) | `52fb6ff` | 01:35:30 |

So at EVERY r2 commit, GENH4's repair HEAD was r3, not the pinned r2;
and at the pinned r2 (`fd35416` + `5279a11`) there WAS an adjudicated,
not-yet-repaired finding — GENH4 passPE3's MINOR 1 (the dv0 = N
trichotomy at GENH4-CAP(F) [r2] + S4 [r2] with overlapping antecedents
and conflicting conclusions, adjudicated 00:24:06, 36 minutes before
the first r2 commit) — sitting in exactly the CAP(F) neighborhood whose
two-term display GENIND's corrected (CS-EXACT) gloss cites. Neither the
supersession nor the adjudicated finding is disclosed; instead the note
affirms it checked and found nothing. This is the same defect shape as
PE2-G2 (stale pin + undisclosed adjudicated finding at the pin),
reproduced inside the repair OF PE2-G2, one round later.

**What survives (verified, not assumed):** I diffed the GENH4 note
`fd35416 → 4b0d946` in full. r3 is display-only for everything GENIND
consumes: THEOREM GENH4.A and GENH4.B statements untouched; the
two-term CAP(F) even-N law untouched; the comp-weighted census [r2]
clause untouched; GENH4.D's package untouched. r3 restates the dv0 = N
trichotomy with pairwise-disjoint antecedents ((a) N > 2w either parity
→ 2SIDED; (b) N ≤ 2w odd → RAM(N); (c) N ≤ 2w even → UND), scopes the
W-12.A mnemonic, and refreshes GENH4's own stack box. The UND count —
the only clause GENH4.B's two-term law and hence (CS-EXACT)'s gloss
rest on — is identical before and after. So no conclusion of GENIND.B
moves; the discharge geography is factually right about the LAWS and
factually wrong about the LEDGER. Class: **GAP** (citation/disclosure
integrity, the arc's own precedent class for exactly this shape;
repair is one sentence — re-pin at r3 or disclose the race and the
adjudicated MINOR).

### PE3-G2 [GAP] — The M_l window display in LEMMA GENIND-3's statement, inherited verbatim by GENIND-6(a), is false under its literal reading at multi-child genres with DISTINCT slopes; constructed counter-genre machine-refuted at (2,4,7) and (3,4,7).

GENIND-3 (S4.1), verbatim: *"M_l = N − (Σ_j μ_j k_j + Σ_pieces
deg·slope-mass of the frame at S_l's scale) — the explicit affine
window of the scale-then-Hensel extraction at the child's frame"*.
GENIND-6(a) (S4.3), verbatim: *"extracts each as a (μ_l, d) cluster
state at the SAME explicit window M_l = N − (μ_l k_l + Σ_{other
blocks} deg·slope-mass at S_l's scale) as in GENIND-3"*. The M6 worked
ledger computes the instance as *"M = N − (2·1 + 4·(1/2)) = N − 4, the
GENIND-3 formula with the stage block contributing slope-mass 2"* —
i.e. co-blocks enter at their OWN slope-mass (uncapped).

The correct loss (the content of the scale-then-Hensel extraction at
slope k_l) is min-CAPPED: D_l = μ_l k_l + Σ_{other blocks}
deg·min(own slope, k_l). The two agree exactly when every co-block is
SHALLOWER than (or at) the extracted child's slope — which holds at M6,
at M7T, at every S5.3 genre, and at every state of every sealed sweep
(single-child or equal-slope genres only; distinct-slope multi-child
genres are first VISIBLE at n = 4, N = 7, beyond every sealed window,
which is why three passes and the battery could not see this). They
disagree as soon as a co-block is STEEPER than the extracted child.

**Constructed counter-genre TWOCHILD** at (q, 4, 1): hull
(0,6)–(2,2)–(4,0), child A on [0,2] at slope 2 (residual (y−w)²),
child B on [2,4] at slope 1 (residual (y−z)², unit-twisted); pins
digit₆(a₀) = w²z², digit₄(a₁) = −2wz², digit₂(a₂) = z²,
digit₁(a₃) = −2z; locus (q−1)²q^{4N−17}, first visible N = 7. For the
shallower child B the literal display gives M_B = N − (Σμ_jk_j) =
N − (2·2 + 2·1) = N−6; the capped value is N − (2·1 + 2·min(2,1)) =
N−4. Machine (§7, FR-B): on ALL 2,048 states at (2, 7) and ALL 177,147
states at (3, 7), the sealed `extract_child` (whose D is the weighted
min — the RUNNER always implemented the capped value) returns
W_c = N−4, and the locus fibers ONTO the FULL (2,1,3) child-B space
(16 = q^{2·2} classes at q = 2; 81 at q = 3) × (child-A space at
N−6 = 1) with CONSTANT fiber q⁷ — impossible if the true window were
N−6 (the child space would be a single class). Lift-stability of the
child-B class at the capped window (80 × 5 and 60 × 4 exact-factor
lifts, 0 unstable) confirms N−4 is certified content, not extractor
arithmetic. The steeper child A extracts at N−6 exactly (both readings
agree there — the control).

**Consumption:** M_l is not a side remark — it is the window at which
Step 1/2's menu assembly instantiates the children's (IH)
history-resolved menus, and GENIND-6(a) re-asserts it ("the SAME
explicit window") for the mixed bucket. A reader instantiating the
degree-2 menu at window N−6 instead of N−4 at this genre gets every
child count law wrong by q^{±(...)} per family. **Failure scenario:**
at (q, N) = (2, 7), genre TWOCHILD, the child-B (IH) menu at the
display's window 1 says the child instantly drains (1 class); the true
window 3 carries the full 16-class menu with its own α/DEC/DRAIN
split — Step 2's menu for this genre, assembled from the display,
loses the entire child-B menu structure. No committed number is
touched (every committed instance is in the agreeing regime); the
proof's own operational definition ("the explicit affine window of the
scale-then-Hensel extraction") and the sealed runner are both correct.
Class: **GAP** (a false display in a lemma STATEMENT on genres inside
its stated scope, silently corrected by the mechanics; repair = one
formula, μ_j·min(k_j, k_l), at S4.1 + the S4.3 echo).

### PE3-m1 [MINOR] — S4.3's machine record overstates the q = 3 fibration block: "Every member classifies to the M6 key" is machine-checked at B1/B2 only; the (3,6,5) letter slice runs with the reader OFF.

S4.3 machine record, verbatim: *"(3, 6, 5) full (z, w) = (1,1) letter
slice: 1,594,323 ONTO 1 × 81 / fibers {19,683} = q⁹ AND 1 × 729 /
{2,187} = q⁷ — the odd-q pins live … **Every member classifies to the
M6 key with the child side present**; every stage cofactor hits the
entry budgets exactly."* In `genindr2_supp.py` the B3 block is invoked
as `fib_block('B3-fib-q3N5', 3, 5, 1, 1, do_read=False)` — the
`read_cluster` classification (the M6 KEY check) is skipped for all
1,594,323 members; only the side-signature half ("child side present")
and the budget half are checked there. The blanket sentence is
machine-supported at (2,5) and (2,6) only. (My fresh route closes the
q = 3 reader hole at the HARDER genre: 300 M7T members at (3,6) with
the reader ON, all CS with the stable key — §7 FR-A.) Class: MINOR
(machine-record wording vs instrument; the mathematical claim is not
refuted, merely not checked where stated).

### PE3-m2 [MINOR] — The GENIND-6 honesty box's attack-surface line "first live n ≥ 7 arrangements" is false for the stage-steeper-than-child case: it is live at n = 6 (window-deep, not degree-deep) — and I machine-realized it.

S4.3 honesty box, verbatim: *"(i) the certified-window ledger at
genres with ≥ 2 stage blocks or a stage block STEEPER than a child
(first live n ≥ 7 arrangements — no machine instance here)"*. The
genre SS6 at (q, 6, 1) — hull (0,8)–(4,2)–(6,0), stage [0,4] at slope
3/2 (e = 2, h = 3, residual (y−z)²), child [4,6] at slope 1 (residual
(y−w)²) — is a valid mixed opening at n = 6, first visible at N = 9
(deeper WINDOW, same degree; the box's own earlier sentence "the
first-visible genre (all others sit at deeper visibility floors — …
stage-steeper arrangements …)" already knew this). Machine: §7 FR-C
realizes it at (2,9) and (3,9) — 370 members, all CS, child extraction
at the capped window N−6, stage ledger budgets exact, certified
windows lift-stable. Class: MINOR (a scope line inside an honesty box;
the box's claim "no machine instance here" was true of the note's own
leg).

### PE3-m3 [MINOR] — The sharp-window sharpness claim is only machine-supported as a DISJUNCTION, and at p = 2 the u₀ half does not fire on its own: u₀-only-one-deeper is lift-STABLE at (2,6) in my split test.

S4.3, verbatim: *"and these are sharp: one further digit (u₀ → π^N or
u₂ → π^{N−1}) is lift-UNSTABLE (machine: the T1 tooth)"*. The T1 tooth
deepens BOTH windows jointly (`W_DEEPER = (N, N−1, N−1, N−2)`), so its
firing supports only the disjunction. My per-coefficient split (§7
FR-D; derived first: the Bezout first-order bound puts every Δu₀ term
at ≥ N when p = 2 — Δg₀·u₂ ≥ (N−2)+2, g₀Δu₂ ≥ 2+(N−2), Δg₁·u₁ ≥
(N−2)+2, g₁Δu₁ ≥ 2+(N−1) — while at odd q Δg₀·u₂ = (N−2)+1 = N−1
bites): **u₂-only-deeper fires everywhere (39/40, 39/40, 40/40 at
(2,6)/(2,5)/(3,5)); u₀-only-deeper fires at odd q (40/40 at (3,5))
but is STABLE at p = 2: 0/40 at (2,6) AND 0/40 at (2,5)** — the
derived directions exactly (my ungated (2,5) marginal case resolved
to stable). So at (2,6) the displayed sharp
window for u₀ is certified-but-possibly-not-sharp (the same defect
genre the leg's own instrument arc disclosed for u₃, one coefficient
over). Counts consume only the certified direction (the box says so),
and the disjunction as literally worded is true — but "these are
sharp" per-coefficient is unsupported at u₀, p = 2 — at BOTH tested
windows ((2,6) AND (2,5), 0/40 each), so even the marginal
second-order term never reaches u₀ there. Class: MINOR.

### PE3-m4 [MINOR] — S9.3's "Conditionality stack (consumed suppliers, as-of pins)" was not updated at r1/r2: GENHN and GENH4 are consumed by the repaired text but absent from the stack.

The r1/r2 text consumes: GENHN.A(iv) @ 850e77e as a FACT inside LEMMA
GENIND-6(b)'s proof (*"the stage node floor, GENHN.A(iv) @ 850e77e"* —
I verified the clause at the pin: stated, GRADE "proved via the graded
computation, the W-9 frame disclosed", machine-checked at μ = 2, 3;
the consumption is accurate and within grade); GENHN.A(v) + LEMMA
GENHN-3 at (CS-1)/Step 4; GENHN S6.2 at (CS-2); GENH4 r2 at the
(CS-EXACT) geography. S9.3 lists none of them — its supplier roster
still ends at {W-12, W-11, HEX3, HMENU3, J-D0, GRTJC, WELDMASTER/
DULEMMA/[W12-H], QSCOUT22, classical}. The pins are given inline at
each use site (so nothing is untraceable), but the section whose title
promises the consumed-supplier inventory is two repair rounds stale.
Also un-wired: S6 Step 3's product-σ sentence still cites only
"(W12-S2.1, GENIND-3)" — at mixed frames the σ-composition is
GENIND-6(d), wired at Steps 2/4 but not 3 (the pointer chain exists
via S4.1's scope note). Class: MINOR (bookkeeping, two sentences).

## 2. THE ATTACK ON LEMMA GENIND-6 ITSELF (charge 1) — the mathematics HELD

I re-derived the lemma's four clauses from the note's own machinery
before running anything; everything below was checked independently
and came out on the nose except the two display defects already filed
(PE3-G2 touches (a)'s formula display; PE3-m3 touches the ledger's
sharpness gloss).

* **(a) SPLIT, factor-genericity:** re-read GENIND-3's steps (1)–(2)
  against the monic-factor Hensel form — the split consumes only
  coprimality of the reduction blocks (child root z ≠ 0 ≠ co-block
  roots after scaling), never linearity/separability of the repeated
  factors; the degree-dropped cofactor (co-blocks shallower than the
  child ⟹ ghat's lead has positive valuation) is handled by exactly
  the non-unit-lead form the note names. My fresh instances exercise
  BOTH cofactor shapes: degree-dropped (M7T child; M6-like) AND
  unit-lead with a steeper co-block reducing into ȳ-powers (TWOCHILD
  child B, SS6 child). The window formula is exact per genre (the
  pins force v(a₀) exactly, so D is constant on the locus) — but see
  PE3-G2 for the display.
* **(b) PERMANENCE at the boundary:** the separation valuation between
  blocks is min(slopes) (distinct sides) or the common slope (same
  side) — for the SHALLOWER block the separation EQUALS its own slope,
  the boundary case. Child-internal moves sit at v ≥ k_l + 1 > k_l
  (strict, GENIND-1(iii)); stage-internal moves at ambient depth
  κ/e_b ≥ (S_b+1)/e_b = h_b + 1/e_b > h_b ≥ h_b/e_b (strict in both
  e_b = 1 and e_b ≥ 2 cases). No boundary case closes the gap:
  re-merge needs depth ≤ separation = slope, and every move is
  strictly deeper. The GENHN.A(iv) consumption (κ ≥ S_b + 1, the
  stage node floor) is accurate at the pin (§1 PE3-m4). HELD.
* **(c) FIBRATION/PRODUCT, the never-attacked clause:** I hunted the
  charge's suggested counter-genre — a fiber depending on child state
  through a shared coefficient budget. The triangular division ledger
  makes the stage-class map a function with FIXED (genre-level)
  certified windows; deeper child valuations only push ledger error
  terms deeper (they never consult the windows), so the fiber is
  constant. Tested at THREE new genres beyond M6: M7T (cubic divisor —
  a (3,1)-child, first ever in a mixed frame): 524,288 states ONTO
  1 × 32 with constant fiber 2¹⁴ exactly as my hand ledger demands;
  TWOCHILD (two co-evolving children): ONTO 16 × 1 / 81 × 1, constant
  q⁷ both characteristics; SS6 (stage steeper): certified windows
  stable on every sample. NO non-constant fiber found; the
  stage-window count never saw the child state. HELD (and now
  machine-real one child-degree and one arrangement beyond the note's
  own instances).
* **(d) conditionality inventory:** (d) consumes — children: (IH)
  member 1 (history-resolved, β3 entry) + member 4 (exact u's);
  stages: (CS-1)+(CS-2)+(CS-3)+(CS-EXACT) = all four [GENIND-H]
  members; pieces: Ore; plus clause (b) (hence GENHN.A(iv) @ pin).
  Since (IH) and [GENIND-H(n)] are GENIND.B's own antecedents and
  GENHN.A(iv) is a proved supplier clause, the sentence "consumes
  [GENIND-H] exactly as (CS-1) does — no new conditionality" is
  ACCURATE for the theorem's conditional structure (the un-listed
  GENHN pin is PE3-m4's bookkeeping, not new conditionality). The
  (CS-EXACT) closure inventory with the mixed bucket routed through
  GENIND-6: Step 2's mixed menu = GENIND-0 × (IH) menus × stage menus
  — every factor either re-established at n or hypothesized; Step 4's
  mixed drain inclusion–exclusion consumes children's u's (IH member
  4, exact) × stage drains ((CS-EXACT), exact) — the strong induction
  CLOSES on all four P(n) members with mixed genres included. HELD.
* **The M6 worked ledger re-derived by hand:** entry budgets from the
  product structure (v(u₀) = 2 with digit z², v(u₁) ≥ 2, digit₁(u₂) =
  −2z, v(u₃) ≥ 1) ✓; coarse windows (N−1, N−2, N−2, N−3) from g₁ mod
  π^{N−3}, g₀ mod π^{N−2} through the four division lines ✓; the
  Bezout refinement: r = u mod g has r₁ = u₁ − u₂g₁ − u₃(g₀ − g₁²) −
  g₁³ + 2g₀g₁, every term ≥ 2 given v(u₁) ≥ 2 and v(u₂g₁) ≥ 2 (the
  note's stated budget inputs — verbatim what my expansion needs), so
  v(r₁) ≥ 2 in both characteristics; v(r₀) = 2 exactly (u₀ binding);
  Res(g, r) = r₀² − r₁r₀g₁ + r₁²g₀ has v = 4 exactly; Δg = δ·adj(r)/
  Res ≥ N + 2 − 4 = N−2 ✓ the claimed bound, and a weaker u₁-budget
  would lose exactly the refinement (the box's own caveat) ✓; pushing
  Δg through the ledger reproduces the sharp set (N−1, N−1, N−2, N−2)
  ✓; fiber exponents 9/7 re-derived as N-independent identities
  (locus (6N−17) − child 2(N−5) − stage (4(N−5)+4 resp. +6)) ✓.
  The ONE overstatement found is PE3-m3 (u₀'s sharpness at p = 2).

## 3. THE r2 SUPP LEG (charge 2) — NON-TAUTOLOGICAL, with one record defect

* **Product side vs locus side:** genuinely two-source. The locus is
  enumerated by the generator (the hand parametrization); each member
  is then INDEPENDENTLY processed by the sealed reader (`sides_of` +
  `read_cluster`, hull/residual mechanics shared with nothing in the
  generator) and by the extraction map (`extract_child` Hensel + the
  division ledger). The histograms measure distinct child classes,
  distinct stage classes, joint support, and fiber multiset from the
  MAP — the closed-form predictions (child space q^{2(N−5)}, stage
  space q^{4(N−5)+4}/q^{4(N−5)+6}, fibers q⁹/q⁷) are the note's; onto
  + constancy follow by measurement + pigeonhole, not by arithmetic
  identity. The identity 8,192 = 16 × 512 is FORCED once constancy
  and support are measured — the measured content is constancy and
  support, and those are real. NOT a tautology.
* **The one instrument defect:** B3's reader is off (PE3-m1 above).
* **Exact-factor stability design:** the redesigned instrument
  (factors Hensel-lifted to each independent lift's full precision,
  truncations compared) is the correct fix of the run-1
  determinism-vs-certification artifact, and the instrument arc is
  disclosed in BOTH the docstring and S4.3 — honest. My FR-D split
  test reuses the design one coefficient at a time and it resolves
  sharpness per-coefficient exactly as the derivation predicts, so
  the instrument measures what it claims.
* **Bezout-sharp bonus finding:** verified (§2, the M6 ledger bullet)
  — v(r₁) ≥ 2 is genuinely forced by v(u₁) ≥ 2 + v(u₂g₁) ≥ 2, both
  characteristics, and the refinement genuinely dies at weaker
  budgets.
* **T2/T4 teeth:** T2 corrupts the vertex pin and demands the genre
  signature break (real negative control); T4's counter-prediction
  (σ_child unramified (1,2)) is a genuine mechanism-free PARI leg
  (360/360 refuted). The drain-honesty diagnostic is correctly
  UNGATED and correctly interpreted (conservative drain ≠ decided).

## 4. MACHINE LEG — REPRODUCED; PINS INTACT

* **md5 pins:** sealed runner `genind_checks.py` =
  `e7ca150b…` — FOUR-WAY identical (worktree = HEAD b3c05be = r2
  52fb6ff/ad6f31d = seal c3212cc). Supp leg `genindr2_supp.py`
  `7335fc16…`, output `63b7413a…`, results `6c93f29c…` — worktree =
  HEAD = r2 commit 3. Sealed-battery artifacts untouched. Supplier
  GT-PIN files 5/5 match the runner's PINS dict at the worktree.
* **Fresh isolated re-run** (`/tmp/genind_pe3_run`; runner + supp +
  the five pinned suppliers copied; committed artifacts never
  touched): `genindr2_supp.py` **GREEN — 2,128,163 checks, 0
  violations, 358.7 s** (committed: 2,128,163 / 0 / 357.7 s). Output
  diff vs committed = 5 embedded per-block timing values + the total
  line, nothing else; results JSON leaf-diff = `elapsed_s` only
  (357.7 vs 358.7). Every fibration
  count (8,192 ONTO 1×16 fib 512 / 1×64 fib 128; 524,288 ONTO 4×256
  fib 512 / 4×1024 fib 128; 1,594,323 ONTO 1×81 fib 19,683 / 1×729
  fib 2,187), the stability blocks, PARI 360/360 + T4, and teeth T1
  (38/40, 40/40) + T2 (20/20) reproduced exactly.

## 7. FRESH ROUTE — n = 7 + the named blind spots (all predictions hand-derived first; ran in the /tmp sandbox; committed for audit as `verification/openmath/genind_pe3_fresh.py` + output + results JSON — md5 fce9cb08 / 2075dd28 / 360eb239; the sealed runner imported read-only, md5-asserted)

Prior routes: (a) seal battery (n ≤ 5 rosters, 163,536); (b) PE1
exact-evaluation legs (n ≤ 5); (c) PE2's n = 6 census + first n = 6
oracle + m = 6,7 symbolic brackets; (d) r2 supp (M6 fibration
2,128,163). **Mine: constructed genres at n = 7 and at the two n = 6
arrangements no leg ever touched, every law derived by hand in this
report's terms before the run. GREEN: 705,133 checks, 0 violations,
147.4 s.**

* **FR-A (M7T — the first n ≥ 7 composite evidence).** Genre M7T at
  (q, 7, 1): hull (0,5)–(3,2)–(7,0); a (3,1)-child at slope 1 (the
  first child of degree > 2 in any mixed frame — GENIND-6 quantifies
  over (μ_l, d) children, machine-real before only at μ = 2) +
  the CS4-E stage at slope 1/2. Hand ledger: pins digit₅(a₀) =
  −w³z², digit₄(a₁) = 3w²z², digit₃(a₂) = −3wz², digit₂(a₃) = z²,
  v(a₄) ≥ 2, digit₁(a₅) = −2z, v(a₆) ≥ 1; locus (q−1)²q^{7N−23};
  child window N−5 (D = 3·1 + 4·(1/2) = 5); CUBIC division ledger
  u₃ = a₆−g₂, u₂ = a₅−g₁−g₂u₃, u₁ = a₄−g₀−g₁u₃−g₂u₂,
  u₀ = a₃−g₀u₃−g₁u₂−g₂u₁ with entry budgets = M6's stage budgets and
  certified windows (N−2, N−2, N−3, N−4) at p = 2 / (N−2, N−3, N−3,
  N−4) at p = 3 (they differ because v(g₂) = 1 at odd p); fiber
  exponent 14 (p = 2) / 15 (p = 3), N-independent. **Machine: (2,6)
  EXHAUSTIVE — 524,288 states, every one CS at the single reader key
  ('CSOTHER', 7, (3,7), 2, 1, 2, 1) with the (3,1)-child side
  present, locus exact, ONTO 1 × 32 with
  constant fiber 2¹⁴ = 16,384; (2,7) 400 sampled members + 120 × 5
  exact-factor lifts stable at the coarse windows; (3,6) — THE WILD
  p = 3 CHILD (pins 3w², −3w ≡ 0: the first equal-characteristic
  J-D0 degeneration of a CHILD inside a mixed frame) — 300 members
  with the reader ON (closing B3's reader hole at a harder genre) +
  80 × 4 lifts stable. 0 violations.**
* **FR-B (TWOCHILD — the PE3-G2 adjudication).** §1: (2,7) exhaustive
  2,048 + (3,7) exhaustive 177,147; capped windows confirmed, literal
  display refuted; ONTO 16 × 1 / 81 × 1, constant fiber q⁷; stability
  80 × 5 + 60 × 4 at the capped windows, 0 unstable; steeper-child
  control at N−6 exact.
* **FR-C (SS6 — stage STEEPER than child, the honesty box's named
  no-instance case).** (2,9): 250 members — all CS, side signature
  (0,4,2,3)+(4,6,1,1), child extraction at capped D = 6 (uncapped
  reading would say 8 — refuted again, independently of FR-B), stage
  ledger budgets v(u₀) = 6 & digit₆ = z², v(u₁) ≥ 5, v(u₂) ≥ 4,
  v(u₃) ≥ 2 exact on every member, certified windows (N−2, N−3, N−4,
  N−5) lift-stable × 4 lifts. (3,9): 120 members, odd-q pins live,
  same. 0 violations. GENIND-6's ledger machinery survives its own
  named blind spot.
* **FR-D (M6 per-coefficient sharpness split).** §1 PE3-m3: u₂-only
  39/40, 39/40, 40/40 unstable at (2,6)/(2,5)/(3,5); u₀-only 0/40 at
  (2,6), 0/40 at (2,5), 40/40 at (3,5) — the derived per-coefficient
  resolution (u₂ driven by Δg₀ = N−2 exactly; u₀ reached at odd q by
  Δg₀·u₂ at height N−1, out of reach at p = 2 where v(u₂) ≥ 2 pushes
  every first-order term to ≥ N).
* **FR-E (symbolic).** The five fiber exponents (M6 9/7; M7T 14/15;
  TWOCHILD 7; SS6 15) verified as N-independent affine identities;
  the letter censuses factor out of every fibration display.

## 8. CLEAN CHARGES (executed, nothing found)

GENIND-6(b) boundary hunt (separation = slope) — clean, strict
inequalities everywhere; GENIND-6(c) fiber-constancy hunt at three
new genres — no counter-instance; GENIND-6(d)/(CS-EXACT) closure
inventory with the mixed bucket — closes; the GENHN.A(iv) @ 850e77e
consumption — verbatim-accurate, clause proved at the pin; GENH4 r3
consumption check — display-only for GENIND (full diff); r1-tag
collateral re-check at the r2 sites (the granularity gloss PE2-m1 is
correctly propagated at S1 + Step 4 + S7.1; S7.4/S9.1 already
correct); the PE2-G1 wiring (S4.1 scope note, Step 2 mixed bucket,
Step 4 mixed drain, S7.1 rider) — mutually consistent, single
four-member box at all four sites; supplier pins 18 + ledger hash
resolve; S11 stays frozen and consistent with BOX-5's [r2] arc line.

## 9. FINDINGS TABLE + GRADE LINE

| id | class | site | one line |
|---|---|---|---|
| PE3-G1 | GAP | S12 re-pin bullet / Step 4 [r2] | "no GENH4 r3 existed at this commit — checked" is FALSE (r3 4b0d946 00:50:10 predates all three r2 commits); the just-adopted disclosure rule violated at adoption; adjudicated GENH4-PE3 MINOR at the pin undisclosed; consumption survives (r3 display-only — verified by diff) |
| PE3-G2 | GAP | S4.1 GENIND-3 statement / S4.3 GENIND-6(a) | the M_l display's literal reading (Σ_j μ_j k_j, co-blocks at OWN slope-mass) is false at distinct-slope multi-child genres; constructed TWOCHILD refutes it on 2,048 + 177,147 states (true window N−4, display N−6); correct capped formula is what the runner and proof mechanics implement |
| PE3-m1 | MINOR | S4.3 machine record | "Every member classifies to the M6 key" not machine-checked at the (3,6,5) block (`do_read=False`); reader-ON support is (2,5)/(2,6) only |
| PE3-m2 | MINOR | S4.3 honesty box (i) | "first live n ≥ 7 arrangements" false for stage-steeper-than-child (live at n = 6, N ≥ 9 — realized at FR-C) |
| PE3-m3 | MINOR | S4.3 M6 ledger, STAGE SHARP bullet | sharpness machine-supported only as a disjunction; u₀-only-one-deeper is lift-STABLE 0/40 at (2,6) p = 2 (per the Bezout derivation), so "sharp" is unsupported per-coefficient at u₀ |
| PE3-m4 | MINOR | S9.3 / S6 Step 3 | conditionality stack omits the GENHN/GENH4 consumptions added at r1/r2; Step 3's product-σ line still cites GENIND-3 only (mixed σ = GENIND-6(d), wired at Steps 2/4 only) |

**Machine leg:** runner md5 four-way identical; supp-leg triple
(runner/output/JSON) identical worktree = HEAD = r2; isolated re-run
GREEN 2,128,163/0 (leaf-diff = elapsed only); instrument audited
non-tautological on a fresh angle (locus-vs-product two-source check;
B3 reader exception filed as PE3-m1).

**Fresh route:** first n ≥ 7 composite evidence (M7T, (3,1)-child,
524,288 exhaustive + wild p = 3 child); the TWOCHILD window
adjudication (2,048 + 177,147 exhaustive, both characteristics); the
stage-steeper blind spot realized (SS6, 370 members); per-coefficient
sharpness split; five symbolic fiber identities. **GREEN: 709,113
checks, 0 violations.** Every GENIND-6 mathematical clause survived;
both GAPs are display/record-level with all mechanics correct.

**GRADE LINE: NOT CLEAN — 0 CRITICAL + 2 GAP + 4 MINOR. The 2-clean
count does NOT start (0/2 stands; consecutive-clean counter stays at
0). r3 owed: one disclosure sentence (PE3-G1), one formula (PE3-G2),
plus the four MINOR sentences. The mathematics of LEMMA GENIND-6, the
criterion, the brackets, the r1/r2 seam closures, and every sealed
number survived every attack, now through n = 7 and the honesty box's
own named blind spots.**

---
*Verifier: fresh-context hostile pass PE3, 2026-08-09. Fixed nothing.
Machine leg re-run in an isolated /tmp sandbox; committed artifacts
untouched (byte-verified before and after). Fresh-route runner ran in
the same sandbox and is committed for audit as
`verification/openmath/genind_pe3_fresh.py` (+ output + results);
every law it gates on is derived in this report before the run; its
one exploratory read-off (the M7T reader key tuple) is disclosed as
such and was gated only on UNIFORMITY across the locus.*
