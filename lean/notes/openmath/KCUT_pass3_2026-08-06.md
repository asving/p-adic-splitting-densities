VERDICT: **DISCHARGES**

This discharges the Round-3 review gate—not the remaining semantic owner rows and not the unconditional ROOT theorem.

### Falsifier-first result

1. **Round-2 C2/C3 staleness — CURED**

The governing §D now says:

> “unit H8-C3 … `kcut_k1_form` PROVES the exact-form K1 convolution”

and:

> “unit H8-C2 … `massFactor_mass_eq` PROVES factorwise ⇒ the aggregate `mass_eq` row”

It also explicitly classifies both structures as:

> “UNPINNED assumption packages, not semantic discharges”

The former false “OPEN H8-C2/H8-C3” status no longer appears in the governing replacement row.

2. **Round-2 unit-count defect — CURED**

The live status consistently reports:

> “all ELEVEN Lean units + 2 documentary units LANDED”

The “NINE Lean units” occurrence in §F is part of the dated Round-1 repair history and is explicitly superseded by §G; it is not presented as current state.

3. **Exactly-one shallow decomposition — CONDITIONALLY PROVED; RESIDUE HONEST**

Ownership, forward extraction, and source-side injectivity are proved outright. Consumer-level equivalence depends on:

> `graft_cut`  
> `cut_graft`

The display expressly says:

> “full decomposition bijectivity/realizability stays open through the graft rows”

and identifies the graft owner arc. No unconditional bijection is claimed.

4. **Exhaustive-exclusive ownership — PROVED**

The display accurately attributes this to:

> `read_cut_exhaustive`, `shallow_block_disjoint`, `block_block_disjoint`, `bdy_display`

and carefully limits the conclusion to:

> “the ownership-partition level”

That avoids conflating ownership with graft realizability.

5. **Weight-preserving product, including factorwise law — CONDITIONALLY PROVED; RESIDUE HONEST**

`KCutMassFactor` supplies genuine slot-independent factorization through `ShallowData` and `BlockDeriv`, and `massFactor_mass_eq` derives C1’s aggregate `mass_eq`. The display correctly retains the semantic residue:

> “the MEASURED identification of `shMass`/`blkMass` stays with the owners”

Thus the factorwise implication is proved, while possession of the intended measured instance is not falsely claimed.

6. **Step-18 K1 exact-form tie — CONDITIONALLY PROVED; RESIDUE HONEST**

`kcut_k1_form` proves the displayed transported-pool convolution from `kcut_reduce`, the tie/support/injectivity fields, graft inverses, and mass equality. `kcut_k1_form_of_massFactor` correctly supplies the latter from C2.

The remaining semantic interface is conspicuously displayed:

> “C3’s τ/δ pool rows have NO typed node-level read-off”

> “`vBridge` is constrained only by keying/injectivity/support”

> “`blockTotal_tie`’s instantiation needs the REL.2 pool dictionary … + continuation memorylessness”

The absent W-resummation leg is also explicitly assigned to CL-17(ii)/`W17ii`.

### New-defect hunt

No new mathematical or chain-breaking display defect was found.

Two phrases are mildly awkward but not false in context:

- §F’s “proofs NOT attempted … since LANDED + PROVED” compresses a historical transition; §G unambiguously supplies the current status.
- “discharge awaits ONLY the clean re-pass” means this adjudication/display gate, not retirement of the mathematical residues. The surrounding text repeatedly says “NO retirement,” “row PERSISTS,” and “UNPINNED assumption packages,” preventing substantive ambiguity.

Accordingly, the package proves the claimed conditional decomposition/ownership/product/K1 composition at the consumption site, and every material unproved semantic interface—especially graft, measured mass identification, and τ/δ dictionary—is displayed as residue rather than consumed or claimed.