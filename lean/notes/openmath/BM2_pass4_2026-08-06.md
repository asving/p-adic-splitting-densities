Pass 4 finds two critical scope failures and two gaps. Clean counter remains 0/2.

### Critical 1 — Lean header retains the recurring scope leak

> “what is compiled is (K0)-CONFORMANCE at the recorded carriers”

“Recorded carriers” includes [RC], while the compiled [RC] result is only `K0Conformant σ 1 1 Φ'`, not (K0) at the recorded node’s actual pair. Later clarification does not repair this sentence’s grammatical scope. It also contradicts §S5’s claim that the file header carries the current honesty ledger.

Classification: **CRITICAL — surviving [NR]-actual/[RC]-synthetic scope collapse in the Lean documentation.**

### Critical 2 — untagged fired-lift universal survives in both note and Lean header

> “Conformance quantifies over FIRED lifts; the fence restricts what fires, not the conformance.”

This violates the standing rule directly: it is an untagged status sentence about “FIRED lifts.” More substantively, the compiled declarations quantify over carrier predicates; they do not quantify over a closed `FiredLift` universe. That passage is precisely what SITE-EXH leaves open. The identical sentence appears in the Lean module comment.

Classification: **CRITICAL — untagged universal and closed-world implication contrary to SITE-EXH.**

### Gap 1 — §S0 and the body disagree on OL-1(a)’s [RC] dependency

The table says:

> “OL-1(a) … [RC] **NOT proved — awaits REC-PARAM**”

and §S2 adjudicates:

> “REC-PARAM … is SUFFICIENT for the corpus OL-1(a) clause at [RC] sites”

But later prose says:

> “OL-1(a)-on-[RC] therefore rides REC-SL/REC-PARAM”

and the REC-SL consumer list includes:

> “OL-1(a) on [RC]”

Because `IsReadLift` and the remaining REC-SL fields are expressly unnecessary for corpus OL-1(a), these sentences overstate its dependency unless “REC-SL” is being used merely as a namespace for its REC-PARAM sub-obligation. That qualification is not stated, so the table is not the unambiguous single source of truth claimed.

Classification: **GAP — dependency/status inconsistency, not a false compiled theorem.**

### Gap 2 — SITE-EXH’s consumer list remains incomplete

The §S0 table says:

> “abstract-engine readings (‘every Lift_i(c)’, blueprint) | ride S-1 [T] + SITE-EXH …”

and §S2 repeats that the abstract clause still needs SITE-EXH. Yet §S4(i-d)’s declared complete consumer list does not include the abstract-engine universal reading.

Classification: **GAP — missing named consumer.**

### Evidence-line audit

The REC-SL, REC-PARAM, and GRADED-READ instance reports all explicitly say:

> “At-instance verification is EVIDENCE …; the obligation REMAINS OPEN”

The §S0 legend likewise says statuses are unchanged. The C-e fence exhibit asserts an instance-specific restriction and draws a requirement for eventual SITE-EXH closure; it does not claim closure. No evidence line itself discharges a general obligation.

Classification: **CLEAN on evidence/status separation.**

The nine-declaration table otherwise matches the pasted Lean statements: [NR] corollaries conclude at the actual pair, [RC] corollaries conclude at `(1,1)`, and `readsOf_landing_K0` has only an internal `ReadsOf` quantifier.

VERDICT: CRITICAL