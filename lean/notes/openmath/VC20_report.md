1. **BLOCKER — stale ten-row operative enumeration in §1.** Inside the live `(ROOT-C)` hypothesis display, the retired `(H8)` bracket still says: “the live roster is … **TEN §3.1 rows**.” After adding `(H8′)`, the live roster is eleven rows. This directly contradicts §1’s surrounding `(H8′)` hypothesis, Step 16, Step 19, §3.1, and §23. Thus the claimed eleven-row enumeration invariant is not literally satisfied at §1.

2. **BLOCKER — §3.2 retirement rationale retains VC19’s rejected owner-home claim.** The operative K-CUT retirement row says the package is proved conditional on “typed, **owner-displayed interfaces**” and that the remaining content is “priced at its owners.” Later in the same row, the REVISION-17 correction correctly says those owners are only retirement routes and `(H8′)` is the actual priced home. These statements are incompatible. The retirement record therefore is not fully consistent with `(H8′)` and still contains the precise owner-housing claim VC19 rejected.

Apart from those defects:

- `(H8′)`’s three members exactly cover VC19’s missing K1 premises: GRAFT, MASS-ID, and POOL-DICT including `blockTotal_tie`. No fourth VC19 residue is apparent, and the members are stated at the consumed interface strength without silently reinstating the full retired cut kernel.
- The live clause attributions at §1, Step 16, Step 18, and Step 19 otherwise agree: `(R)` and `(UB)` gain exactly `(H8′)`; `(0)` and `(SQ)` remain unconditional.
- The displayed-scope walk closes at `(ROOT-C)`.
- Acyclicity remains intact, including `17 ≺ 18 ≺ 18b`; `(H8′)` is a hypothesis row and introduces no backward edge.
- No independent regression was found in D-12r, NSF-KEY, PACK, `(SL≥2)`, the ΣτRτ identity, or the K-CUT machine-proved core.

VERDICT: COMPOSITION-GAPS