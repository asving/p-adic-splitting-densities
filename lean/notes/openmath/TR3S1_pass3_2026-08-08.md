All checks complete. Composing the verification report.

---

# TR3-S1 pass 3 — ACCEPTANCE DECIDER (Fable, fresh context, hostile)

**Object:** `lean/notes/openmath/TR3S1_2026-08-08.md` REV 2 + pass-2 record, read in full.

## What I re-derived independently (charge 1)

**Lemma A, (SLOT), support, assembly:** re-derived from the paper pins alone. Level-1 (12) at (e₁,h₁)^F=(1,h₀), e(µ₁)=1 forces (s,u)=(0,β), (ℓ₁,ℓ′₁)=(0,1); α_j = α − s_jµ₂(φ₂) with E₂µ₂(φ₂)=γ₂ gives (SLOT) with the displayed mod-e₁ integrality; on-line ⟺ component membership (a point on the touching line is on the lower envelope); j₀ = (s−s(γ))/e₁ = ⌊s/e₁⌋ is FGMN's own print (fgmn.txt:1349). The S5 per-slot chain, both monicity upgrades (S2(iii), S6.2 — leading coefficient 1 forces the unit u=1 in R = uψ), and the REV-2 transformation law R′_λ(y) = z^{−qu(f)}R_λ(z^{qh₁}y) (from t₁ ↦ t₁ − qu_i and the on-line march u_{s+je₁} = u − jh₁) all check; gmn.txt:487's π-change law has the identical scalar×rescaling shape, corroborating the gap-2 fold. **ǫ₁ ≡ 1 one-liner:** exponent ℓ′₁s − ℓ₁u = 1·0 − 0·β = 0 — confirmed. **(SQ):** tautological from σ₁ := quotient of σ₀[y]; σ₀(ψ₁^F)=ψ₀ verified via Lemma A at Φ₁ + Cor 5.6(2) (print "0 ≤ i < r" — chain-internal, (H-CHAIN) only). **Full instance with the j₀ wrap:** ℤ₂, Φ₀=x, h₀=1, Φ₁=x²+2x+4, ψ₀=y²+y+1, (e₁,h₁)=(2,1), γ₂=5; f = 2⁹x·Φ₁² + 2⁸Φ₁³ + 2⁶Φ₁⁴ + (x+1)Φ₁⁶. Hand walk: γ=30, s(γ)=0, on-line {2,6}, off-line congruent slot 4, non-congruent abscissa 3 ignored both sides, negative β_m corner, inner shift s₁(a₂)=1 → σ₁(R^F₂(f)) = z̄+y² = R_λ(f), j₀=1. Confirmed by a from-scratch script sharing zero code with the harness (own division, own 𝔽₄ tables); second corner Φ₁⁵(x+1) exercises s(γ)=1, j₀=2 — exact. My first script run *disagreed* — my own bug: I omitted i·v₂(φ₂) from u_i, precisely the trap S9.7 flags; the harness has it right.

## Falsifier + independence (charge 2)

Ran `tr3s1_check.py`: **ALL GREEN — TS1a 0/2080, TS1b 0/2080, TS1c 0/20, TS1d 0/2080, total 6,260** — leaf table reproduced verbatim; counts derived from script structure (104 calls × 20 towers + 20 ties) reconcile. Code-read: `gmn_order2/gmn_order1` build their **own** polygons, on-line sets, s₁, t₁ (hard-coding ℓ₁=0), and assembly; shared: Tower object, dev, w₀/w₁-as-v₁/v₂, res_digit (the level-0 reduction both papers print identically — (RES0)), K₁ ring ops — **exactly the REV-2 perimeter as declared, nothing broader claimed**. TS1b's no-trailing-zeros property is by fp_norm construction, but TS1a's union-range per-abscissa+support check closes any hole. Fresh N-1 log reconciled row-by-row (T0 0/23111, T9 0/500 + iii-C 300/300, T2 0/1200, T5 0/40, T11 0/20, P12 [False,True]×2, P3 145/0/13; 33,547 ALL GREEN).

## Pins and consumers (charges 3–4)

Def 3.15 legs redone: pdftotext p.18 — prime col 51 directly above FIRST ℓ (col 50), second ℓ col 58; i=0 semantic check derived independently (misreading gives z₀^{−α}, contradicting the print). FGMN Bézout + normalization printed (fgmn.txt:1076-78); GMN prints ℓh−ℓ′e=1 with **no range** (gmn.txt:1086) — the S0 normalization hypothesis is honestly a project convention. Defs 2.19–2.21, Def 2.5, §2.1 type-fixes-λ, Cor 5.15 properness — all verbatim. **GRB cross-checks:** R2.3's TR-3-ORD-X display (ord tie, labels by clause (3) each side, ψ_i ∼ R_λ(Φ_{i+1})) matches S6.2's delivery restricted to the stratum with (KP_i)+[T] inherited; S3.4 REV-13's TR-3′-GEN(i) statement (σ_j family + per-argument units + side-read correspondence) matches S6.1; the S3.3 consumer split matches; R11.5's three-taxon residue keeps (KP_i)/(H-CHAIN)/[T] external-consumed — never discharged here; Cor 5.6 use is chain-internal (indices 0,1 < r=2), the extended-chain use at S6.2 explicitly priced under (KP_i). BM2: H1_BM2 line 232 displays OL-1(b) conditional on GRADED-READ (+ TR-3-ORD-X for the GMN-verbatim face) — S6.3 discharges only that seam on the stratum, GRADED-READ/REC-SL untouched. S7 scope box complete for all claims made.

## Findings

**No classifiable findings.** I found nothing to quote as a critical error and nothing to quote as a justification gap. Three non-classifiable observations, for the record: (a) the S0 table row label "TR-3′-GEN(i) restricted to order 2" compresses "order 2 ∧ e₀ = 1" — the S0 statement and S6.1 body are precise; (b) the leaf's byte-column phrasing mixes byte/codepoint metrics — the semantic claim (prime above the FIRST ℓ) is robust under both (1 vs 7 columns); (c) TS1b's "carries no trailing zeros" is enforced by construction rather than asserted-and-checked — net still tight via TS1a. All four pass-1 folds verified sound by independent re-derivation.

This is the second consecutive clean pass on REV 2, model-diverse (pass 2 Codex, pass 3 Fable). TR3-S1 is **ACCEPTED**; the GRB residue re-prices at the next fold.

VERDICT: CLEAN