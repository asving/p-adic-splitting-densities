REVISION 10 passes.

- F1: TR-3-ORD now displays every additional hypothesis its proof consumes:
  - `(KP_i)` supplies that `Φ_{i+1}` is a key polynomial, makes `L = R(Φ_{i+1})` maximal, and includes the KPa association used to compare graded orders.
  - `[T]`-pinned properness selects the equality branch; without it, FGMN gives the ceiling correction.
  - `g ≠ 0` is explicitly bound.
  - The ambient inductive valuation, `(H-CHAIN)`, rescaling, and carrier data remain globally displayed standing context, not hidden lemma-specific inputs.

  S6.4 prices the result under exactly `(KP_i)` plus `[T]` properness. It obtains only the FGMN-internal integer equality; the project-facing comparison remains openly routed through TR-3-ORD-X. FGMN independently confirms that a key polynomial gives a maximal residual ideal and that the equality branch requires properness. [FGMN paper](https://upcommons.upc.edu/bitstream/handle/2117/28203/ResidualIdealsArxiv.pdf)

- F2: `(12-int) of S5.2` is the correct source of the anchor exponents.
- F3: the stated gate range now matches the claimed loop: `2 ≤ e ≤ 12`, `1 ≤ h ≤ 39`; `e = 1` is correctly separated as the degenerate Bézout case and covered by the main wrap test.

Fresh hostile sweep produced no quotation classifiable as critical or a justification gap. In particular, there is no surviving ord-service leak, improper-key leakage, `e_i = 1` overstatement, anchor-invertibility assumption, or full-order-2 D-REAL claim.

The residue ledger is complete and consumption-honest:

- TR-3′-GEN → GD3-FULL/OL-B.
- TR-3-ORD-X → project-facing Route B and clause-(4) ord consumers.
- D-ITER for `E_i > 1` → IFACE(c) only.
- N-1 remains owed.
- `(KP)`/`(H-CHAIN)`/`[T]` remain displayed external inputs.
- The two-clean acceptance bar remains; this pass is clean 1/2.

No residue item is used to discharge a conclusion elsewhere in the leaf.

VERDICT: CLEAN