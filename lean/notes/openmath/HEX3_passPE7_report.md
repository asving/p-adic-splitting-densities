# HEX3 passPE7 report — THE ACCEPTANCE ATTEMPT: hostile verification of HEX3_PROOF_2026-08-08.md at HEAD (1fa7034, post-PE6 rider)

**VERDICT: CLEAN under the charge bar (0 CRITICAL + 0 JUSTIFICATION
GAPS + 2 MINOR — nothing GAP-or-worse survived verification). Counter
1/2 → 2/2: HEX3 is ACCEPTED at the standing convention (the charge
bar, ledger-adjudicated at the wave-16 fold 91ec856). Text frozen;
dated appends only. The two MINORs are display/cartography defects in
non-theorem prose (one in the sealed S0/BOX-1 program-cartography, one
in the rider's own gloss), with verified zero blast radius on THEOREM
HEX3.A/B/C, the transport lemmas, the species table, the (hExhaust-3)
discharge, and every consumer — repair shape: one dated append each.**

**Pass:** PE7, the acceptance attempt (arc: seal e3f3459 → PE1
0C+2G+2m @ d177fd8 → r1 → PE2 0C+1G+1m @ f60348b → r2 → PE3 CLEAN @
fca1a97 (1/2) → PE4 0C+0G+1m @ 34a946b (reset) → r3 @ b6a1d69 → PE5
0C+0G+2m @ 5919e2f → r4 @ fa7b70d → PE6 CLEAN under the charge bar @
d9c5ef8 (1/2, adjudicated 91ec856) → r5 rider @ 1fa7034 = the target).
**Verifier:** fresh-context Fable, 2026-08-09, zero prior stake; all
six prior reports read first, their routes not re-walked. **Target
byte-state:** sealed body lines 1–832 byte-identical to fa7b70d
(diff empty); rider = the sole append; runner md5 83faf7ed… == seal
e3f3459 (`git diff e3f3459..HEAD -- hex3_checks.py` empty); artifact
md5s 6e4b3207… / ce6f4116… == §S10's display; PE6 report md5
42d86c7a… == the rider's freeze pin.

**Never-owned surfaces attacked this pass:** (1) THE R(M)
CERTIFIED-TAIL LAW (S4/LEMMA H-4 — the closed form's own derivation,
never previously the main target; now GENIND ANNEX R1's rate ground);
(2) the drain typology (the rider's corrected gloss, re-derived from
S2 + checked against the committed histograms); (3) the HMENU3
interface at its frozen text incl. ANNEX A's disc ≠ 0 quantifier;
(4) the rider itself; (5) free hunt. **Fresh route (disjoint from all
six prior):** the R(M) law by DIRECT ENUMERATION with an own W-11
block reader at never-measured window depths — q = 2 to M = 16 (2 ×
1,073,741,824 states, both ring types), plus first-ever R rows at
q = 7, 11, 13, 25 — details below.

---

## m1 [MINOR] — the "tail half of (hMenu-3)" attribution (S0 L105, HEX3-BOX-1 L448–449, echoed in S8.2-1's ingredient list) is contradicted by W12-BOX-2's frozen obligation text and refuted by the ACCEPTED HMENU3 (S5.4: the lemma is the missing piece of the exact U₃^σ law "ONLY")

Quotes (sealed body):

> that residue is HEX3-BOX-1, priced with its exact mechanism (it is
> the tail half of (hMenu-3), i.e. W-13's floor, not this unit's
> charge). [S0, L104–105]

> Deliberately not attempted here (it is the tail half of (hMenu-3),
> W-13's floor; the w12 instrument was DESIGNED not to need it).
> [HEX3-BOX-1, L447–449]

> so the depth-≥1 TRP family laws should fall to the same three routes
> + the certification lemma [S8.2 item 1, L652–654]

Checked against the two suppliers' frozen texts this pass: (a)
W12-BOX-2 (W12_PROOF L1218–1224) defines (hMenu-3) as exactly the two
node-species transfer laws for the depth-≥1 TRP families — it contains
NO tail-certification content; the tail-σ-certification lemma lives in
W12-BOX-3's obstruction, named there for the exact σ-side law. (b) The
ACCEPTED HMENU3 discharged (hMenu-3) in full with the certification
lemma untouched, and says so explicitly (HMENU3 S5.4 L465–467: "The
tail-σ-certification lemma remains what it was: the missing piece of
the exact U₃^σ law ONLY (HEX3-BOX-1, inherited open, price
unchanged)"); its THEOREM HM3.A proved the depth-≥1 family laws via
HEX3's own transports WITHOUT the lemma, so S8.2-1's forecast
over-listed an ingredient. FAILURE SCENARIO: a W-13 planner pricing
the remaining frontier from HEX3's frozen text budgets the
certification lemma as a required (hMenu-3)/menu-rung ingredient — or
doubts HMENU3's completeness for lacking it — mispricing an
already-accepted supplier; the lemma's true residence is the exact
U₃^σ law only. BLAST RADIUS inside HEX3: zero — no theorem, count, or
consumer consumes the attribution; the §S8.1 annex text (applied at
W-12 as ANNEX 2) does NOT carry it ("remains open exactly at the
tail-σ-certification lemma (HEX3-BOX-1)", residence-neutral). The
disc ≠ 0 quantifier discipline is ALIGNED across the two notes (HEX3's
[r4] S0 bracket == HMENU3 ANNEX A's corrected form; same W12-BOX-7
pinned notion). Repair shape: one dated line re-pointing the residence
(BOX-1's lemma = the U₃^σ-law-only missing piece per HMENU3 S5.4;
(hMenu-3) is discharged without it; S8.2-1's ingredient list drops the
lemma for the DECIDED-family laws). Seal-era text, unflagged by
PE1–PE6 — HMENU3's acceptance is what made it checkable.

## m2 [MINOR] — the rider's genre-(i) gloss "the DOMINANT drained mass" (L854) is an unscoped superlative: true at every machine-instantiated row, FALSE in the N → ∞ regime THEOREM HEX3.B itself is about

Quote (rider, Correction 1):

> (i) **B0ZERO drains** — the reader exits UNDECIDED at the TRP frame
> itself (drained column q^{2N−2}, the DOMINANT drained mass; no block
> frame);

Verified true at every committed row (the deepest, (Zp,2,9):
B0ZERO 65,536 of u = 103,168 = 63.5%; genre split from the committed
JSON: B0ZERO/α/β = 65,536/11,904/25,728) and at every fresh-route row
any pass has touched (PE5's deepest, (Fqt,2,12): 4,194,304 of
7,936,000 = 52.8%; (16,4): 94.8%). But u(q,N) = q^{2N−2}(1 + Θ(N/q))
(S5.3's own bound, S8.2's own N-linear display), so B0ZERO's fraction
→ 0 as N → ∞ at fixed q: by q = 2, N ≈ 45 even the α genre alone
exceeds it, and the R-carrying β rows carry the Θ(N) mass — the exact
inversion of the OLD gloss this rider replaced. The superlative also
breaches the standing record rule (enumerated/scoped claims only).
FAILURE SCENARIO: a W-13 prover weighing the certification-lemma work
order from the rider concludes the B0ZERO-column split is the dominant
work at every window and under-prices the R-split rows that carry the
mass in the large-N regime where (A2) lives. BLAST RADIUS: zero on
the typology itself (the three genres and their columns are correct,
re-derived this pass), on the witness row, and on every count. Repair
shape: scope the clause ("dominant at every committed row; the
R-carrying rows overtake it as N grows — S8.2's N-linear family").

---

## Charge-by-charge record (all CLEAN except as found above)

**(1) THE R(M) LAW (S4, LEMMA H-4) — the never-owned surface:
derivation VERIFIED FAITHFUL at the suppliers' frozen bytes, exact at
every arithmetic leg, and machine-true at never-measured depths.**
* *Exit dichotomy re-derived from first principles:* at any node, a
  state with A₀ = 0 is a leaf (SPLIT-TAIL(w) if w ≤ ⌊(M−1)/2⌋, else
  UNDECIDED — W12-L0's loci, W12_PROOF L715–717, cover all A₀ = 0
  states at each node with no gap and no overlap), so a chain exits at
  its FIRST A₀ = 0 state and "reaches A₀ = 0" ⟺ exit ∈ {SPLIT-TAIL,
  UNDECIDED}; the other four leaves (RAM/2SIDED/SPLITEQ/INERTDEEP)
  have v(A₀) exact ≤ M−1, hence A₀ ≠ 0, and their chains never passed
  through A₀ = 0. Independently confirmed at M = 2, 3 by hand
  (R(2) = q, R(3) = 2q² − q from the raw digit loci, including the
  refine-to-seed route at M = 3).
* *Consumption faithful:* S4's summands are W12-L0's rows verbatim
  (UNDECIDED aggregate q^{M−1} == W12-L0's consistency display == W-11
  (iii)'s exact drainage law per center; SPLIT-TAIL(w)@H =
  (q−1)q^{M−1−w} on range s+1 ≤ w ≤ ⌊(M−1)/2⌋; the instance bracket
  Σ_{H: k_t ≤ w−1}(q−1)^t = q^{w−1} re-derived by EXPLICIT history
  enumeration, q ∈ {2,3,5}, w ≤ 11, exact). Each w-term is
  (q−1)q^{M−2}, w-independent; the sum is the closed form, == W-11's
  proved SPLIT-TAIL aggregate quoted in W-12 S3.4 — two independent
  supplier routes to the same display.
* *Arithmetic legs, exact integer checks (this pass's own code):* the
  R-recursion R(M) = q^{M−1} + (q−1)Σ_{2j≤M−1} q^j R(M−2j) equals the
  closed form at 13 q-values (2..49 incl. non-prime 4, 8, 9, 16, 25,
  27) for all M ≤ 60; the β3 identity (q−1)Σ q^j R(M−2j) = R(M) −
  q^{M−1} same range; S5.3's R(M) ≤ q^{M−1}(M+1)/2 for M ≤ 59;
  R(1) = 1 pinned (the closed-form display's q^{M−2} never fires at
  M = 1: floor = 0 — the seed convention consistent across S1/S4/S3.2).
* *Downstream tie:* the u-law's telescoped and first-step forms agree
  exactly (9 q-values, N ≤ 44, own evaluators); anchors u(2,7) = 5664,
  u(3,4) = 837, u(2,4) = 72 reproduced; GENIND ANNEX R1 consumes
  exactly this closed form (== the rate ground it cites).

**(2) The drain typology — CORRECT; the committed histograms verified
key-by-key with an OWN transcription of S5.1.** Re-derivation from
S2's definitions: the depth-0 partition is complete (W-12 S2.4), DEC
never drains, B0ZERO exits UNDECIDED at the TRP frame at once, α
recenters onto D_k ≅ the scaled TRP frame (H-2(iii)), β1/β2/β3
continue into transported W-11 block frames (H-3) — the rider's three
genres are exhaustive and correctly aligned with the S5.1 rows.
Machine: my own closed-form transcription of all six row types checked
against the COMMITTED hex3_checks_results.json — 46 rows, 172 realized
keys, locus AND drained both directions, partition sums, drain-sum ==
u_measured, DECIDED drained == 0 on every row: 0 violations. The
rider's witness quote verified exactly: (Zp,2,4) species_tally
B0ZERO [64,64] + ALPHA(1) [8,8] + DECIDED [440,0], no β key realized,
64 + 8 = all 72 drains at TRP frames (u(1) = 1 is the seed — the α
chains at that row cannot enter a block frame; β keys are
range-empty at N = 4, q = 2: FULL11SQ needs q ≥ 3, VERT1/VERT2 need
u₀ ≤ N−1 with u₀ > 3k — empty). The corrected shorthand (per-column
certified/uncertified splits; R-split only for the three R(M) rows;
B0ZERO's q^{2N−2} split ALSO needed; α recursive) is coherent with
the table. The one defect is m2's unscoped superlative.

**(3) The HMENU3 interface — the quantifier seam is ALIGNED; the one
defect is m1's residence mis-attribution.** HEX3 consumes no HMENU3
content (S9 stack: W-11, W-12 pins, J-D0 — checked; the flow is
HMENU3 → HEX3). What the acceptance pass owed: (a) HEX3's post-r4 Ore
warrant ("every disc ≠ 0 lift", S0 [r4]) == HMENU3 ANNEX A's corrected
(A0) form == W12-BOX-7's pinned notion — one convention across the
three notes, byte-checked; (b) the S8.1 annex text as applied at W-12
(ANNEX 2) is residence-neutral about BOX-1 and stays faithful; (c)
HEX3-BOX-4's "No claim that W-12.D fires at n = 3 yet" remains a
conservative under-claim now that COROLLARY HM3.D fires it (PE6
already dispositioned: harmless staleness, it under-claims); (d) BOX-1's
open-lemma PRICE is unchanged by HMENU3 (HM3-BOX-1 inherits it
verbatim). Only the "tail half of (hMenu-3)" attribution fails — m1.

**(4) The rider — VERIFIED, both corrections, all pins.** Append-only
confirmed (body lines 1–832 byte-identical to fa7b70d; the commit
touches exactly the one file, +75 lines). Correction 1's typology:
re-derived correct (charge 2); its JSON quote byte-faithful; its
"100% of that committed row's 72 drains, zero W-11 block frames"
scoped and true. Correction 2's arc line: every pin checked — 5919e2f
IS the PE5 FINAL report commit (0C+0G+2m, ~149B states / 27 rows,
(2,12), (16,4) @ 17,698,816 — all as the line says); fa7b70d = r4
(note-only, [r4] tags at S0+S6); d9c5ef8 = PE6 FINAL (CLEAN under the
charge bar, 58,158 oracle lifts, 27/27 ambiguous — matches); the
wave-16 adjudication is ledgered at BRIDGE_ADJUDICATIONS_2026-07-30.md
("HEX3 PE6 CLEAN → 1/2 (ORCHESTRATOR ADJUDICATION: the charge bar
stands)", fold 91ec856); the three superseded r3-era endings are where
the rider says they are (header L11–17, BOX-2 L466–481, §S10
L811–829); PE6-report freeze md5 42d86c7a…, runner 83faf7ed…, JSON
ce6f4116… all re-hashed == displayed. "Counter 1/2; PE7 next" was the
correct pre-pass state.

**(5) Free hunt.** The four species-locus exponents re-derived from
the digit-slot census (α: 3N−3−6k with 3 pinned digits; β1 letters
(q−1)(q−2) with all three digits pinned by (z,z′); β2: 3N−3−u₀−3k
with digit_k(B₂), digit_{2k}(B₁) determined and two (q−1) letters;
β3: 3N−3−u₀−t−(u₀+t)/2 with λ and z letters, B₁/B₀ digits determined
— parity u₀ ≡ t (mod 2) forced) — all == S5.1. HEX3.B's chain
re-derived: (q−1+N)q^{2N−1} ≤ (1+N)q^{2N} ⟺ N−1 ≤ Nq; N·q^{2N−1} ≤
q^{3N−2} ⟺ N ≤ q^{N−1}; u ≤ q^{2N−2}(1+N/(2(q+1))) machine-checked
exact for q ≤ 5, N ≤ 39. u(q,2) = q², u(q,3) = q⁴ (empty k-sum)
consistent with the committed q³/q⁵ whole-space columns. No further
finding.

## Machine leg (fresh this session; MACHINE-LEG INTEGRITY charge)

* PINS: the four md5s in the runner's pin block (w12_checks.py 7dc0…,
  w11_checks.py 500a…, w10_checks.py a9c3…, w12 JSON 0577…) re-hashed
  == pinned at copy time; runner md5 83faf7ed… == seal e3f3459; repo
  artifacts byte-untouched by this pass (run isolated).
* ISOLATED full re-run: import closure (21 .py + the w12 JSON,
  computed by own AST walk) copied to /tmp/hex3_pe7_run, run OUTSIDE
  the repo: **GREEN, 158,512 checks / 0 violations** — PIN 4, LAW 92,
  SPECIES 172, XREAD 158,204, W12TIE 23, CHAR 17; teeth fired
  10/20/13 == preregistered; 46/46 rows, 334.3 s, exit 0. Recursive
  JSON diff vs the committed artifact: 25 leaves, ALL elapsed-timing,
  ZERO non-timing.
* TAUTOLOGY AUDIT (own read, this pass): u_measured comes only from
  `read_fresh`'s enumeration (hull walk + residual multiple roots +
  shift3 recentering — an actual reader, L248–306); `u_law` /
  `species_pred` are separate closed-form evaluators; the teeth
  compare CORRUPTED evaluators to MEASUREMENT and go RED if a
  corruption matches (L388–411); XREAD ties the fresh reader's
  drained/depth-0 bits pointwise to the sealed `W12.read_trp`
  (L330–335) — which also machine-grounds the note's identification
  "drained ⟺ conv-UNDECIDED" on 158,204 states; W12TIE compares
  q·u_measured (not the law) to the committed w12 JSON. Non-tautological
  in every family.

## Fresh route — R(M) by direct enumeration at never-measured depths (own W-11 block reader; disjoint from all six prior routes, and aimed at THE acceptance surface)

Own conservative block reader written from W-11's S2 spec (polygon on
{(0,u),(1,w),(2,0)}; 2w < u ⟹ split; u odd ⟹ RAM; u = 2k with
separable residual ⟹ decided; repeated residual root z ⟹ recenter
y ↦ y + ẑπ^k; drain = reach A₀ = 0), NOT from w11 code — two
implementations: C for q = 2 (ℤ₂ ints-with-carries AND 𝔽₂[[t]]
XOR-masks) and Python for odd/prime-power q (ℤ_p ints; 𝔽_q[[t]] with
own field tables built from brute-force irreducibles, char-2 Frobenius
sqrt and odd-char −c₁/2 residual roots). Protocol: validate on
PE2-measured depths FIRST (PE2's coverage: q = 2 M ≤ 8, q = 3 M ≤ 6,
q = 4 M ≤ 5, q = 5 M ≤ 4, q = 8, 9 M ≤ 3, q = 27 M = 2), then extend.
* **Validation: 23 (ring, q, M) cases at PE2-covered depths — every
  value == R(M) = q^{M−1} + ⌊(M−1)/2⌋(q−1)q^{M−2}, 0 violations**
  (both ring types at q = 2 M = 2..8 and q = 5 M = 4; ℤ₃ M = 5, 6 +
  𝔽₃[[t]] M = 6; q = 4 M = 5; q = 8, 9 M = 3; q = 27 M = 2).
* **Never-measured depths, all EXACT, all == the law, 0 violations:**
  - **q = 2, M = 9..16, BOTH ring types** (C): the deepest,
    M = 16, is 2 × 1,073,741,824 states — R = 147,456 on the nose in
    ℤ₂ (carries) AND 𝔽₂[[t]] (no carries); every M in between exact
    (768, 1536, 3584, 7168, 16384, 32768, 73728). The certified-tail
    law now stands measured at DOUBLE the window depth of any prior
    R measurement (8 → 16).
  - **q = 3: M = 7 (both types) and M = 8 (ℤ₃, 43M states)** — 2187,
    6561 exact; **q = 4: M = 6** (2560); **q = 5: M = 5 both types**
    (1625); **q = 8: M = 4** (960); **q = 9: M = 4** (1377); **q = 25:
    M = 3** (1225); **q = 27: M = 3** (1431).
  - **First-ever R rows at four q values: q = 7 (M = 4: 637), q = 11
    (M = 4: 2541), q = 13 (M = 3: 325; M = 4: 4225, 4.8M states),
    q = 25 (M = 3)** — no prior pass or battery ever enumerated the
    block system at these q.
* FRESH-ROUTE VERDICT: GREEN — 53 enumerated (ring, q, M) cells
  (23 validation + 30 never-measured), ~2.9B states total,
  0 violations; both parities of M throughout (the ⌊(M−1)/2⌋ floor
  exercised on both sides at every q).
* Own-instrument disclosure (caught by own validation, note
  untouched): none needed — all validation cells hit on first run;
  the C and Python implementations agree at their overlap (q = 2
  M ≤ 8 vs the law).

## Grade line

**PE7 = CLEAN under the charge bar (0C + 0G + 2m; nothing
GAP-or-worse): counter 1/2 → 2/2 — HEX3 ACCEPTED, the fifth chain
note through the bar. Text frozen; dated appends only** (the two
MINORs are one-line dated-append repairs: m1 the BOX-1/S0 residence
re-point at HMENU3 S5.4's "ONLY", m2 the rider's "DOMINANT" scoped to
the enumerated rows). The theorems, the transports, the species
table, R(M)'s own derivation, the rider, the (A2) handoff, the
machine seal, and the deepest block-window enumeration yet all held.
The chain's conditionality on the (hExhaust-3) leg now rests on
accepted text end to end; what remains open in this note is exactly
HEX3-BOX-1 (the σ-residue law — the U₃^σ-only missing piece), as
priced.

*(Machine-leg artifacts: /tmp/hex3_pe7_run/{full_run.log,
hex3_checks_results.json}; fresh-route artifacts:
/tmp/hex3_pe7_fresh/{rblock.py, rblock2.c, c_deep.log, py_runs.log}.)*
