1. **CRITICAL — the halt convention still fails at \(n=1\).**  
   (G6) explicitly includes the exceptional case “\(W=n=1\) at the root,” where the root letter is confirming and has selection \(\bot\). But SP.4 universally defines the halt as “the first read whose selection is \((g,1)\), i.e. the confirming \(W=1\) read’s PARENT,” and SP-COMP likewise places the verdict at that parent. For \(n=1\), there is no parent and no preceding \((g,1)\)-selection read. Consequently the stated location of the halt, the operation “catalogue word minus that final letter,” and the verdict attachment are undefined or false in a theorem quantified over every \(n\). The rev-4 endpoint is consistent for non-root confirming reads, but not globally.

2. **GAP — the purported pasted VP clause is not the VP clause, so the claimed verbatim agreement cannot be checked.**  
   The block headed “THE VP CLAUSE” begins with “Out = \(V_{\rm term}\) … per SP.4’s HALT CONVENTION, rev 4” and then reproduces the R3/\(n=3\) catalogue. It does not contain the cited VP saturation statement, its definition of the latest read, or the purported “\(\mu=1\)” equivalence. It is therefore circular evidence from §M-SPECIES rather than the promised independent §T-ASSEMBLY clause.

The coordinate repair itself checks out:

- From \(es+th=1\) and \(\gamma=e u_0+s'_0h\),
  \[
  t\gamma\equiv ths'_0\equiv(1-es)s'_0\equiv s'_0\pmod e,
  \]
  so \(\sigma=\lfloor t\gamma/e\rfloor=(t\gamma-(s'_0\bmod e))/e\in\mathbb Z\).
- \(\sigma\) is **not constant on realizations of a species**: heights are forgotten fields, and even with \(e=2,s'_0=1,t=1\), one has \(\sigma=u_0+(h-1)/2\). This does not invalidate the census species because its fields \(a=\lfloor s'_0/e\rfloor\) and \(\{\hat p_k\}\) are functions of retained letter data alone; the note correctly uses a per-read family of shifts rather than treating \(\sigma\) as a species field.
- The shifts transport (G2)/(G3), while anchoring cancels the monomial \(z^\sigma\), leaving \(R_{\rm anch}\), \(\lambda\), selection, and \(\ell\) unchanged.
- The example gives geometric slots \(\{-1,0\}\), census slots \(\{0,1\}\), and \(\sigma=1\), as claimed.
- Under \((s,t)=(0,1)\mapsto(1,-1)\), geometric positions move by \(+3\) and \(\sigma\) moves from \(1\) to \(-2\), so their census images remain unchanged. The Bézout-choice check is correct.
- Apart from the \(n=1\) endpoint, SP.1, SP-COMP, SP.4, and SP.6 consistently place the non-root \((\tau\text{-irr})\) halt at the \(\mu=1\)-selection parent while retaining the confirming \(W=1\) read only in the catalogue word.

**Counts: 1 critical, 1 gap.**

**Verdict: NOT ACCEPTED.**
