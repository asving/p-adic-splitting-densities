Clean acceptance verdict. I performed the full sweep and a confirming resweep of the identical pasted text; both were clean.

1. **CLEAN — normalized residual length.** L11 now correctly states  
   “`ℓ_r := w'_r/e_r`” and identifies this with `deg R_anch,r`, matching §C DOM’s width-confinement formula “degree `w'/e_{i+1}`.” The D4-n3 calculations are genuinely unchanged: both reads have `e₀=e₁=1`, hence degrees `3/1=3` and `1/1=1`, so  
   `M(P̂*) = ⌊3/(2·1)⌋·⌊1/(1·1)⌋ = 1`.

2. **CLEAN — L6 empty-history repairs.** L6 dispatches `η=∅` before both inclusions using  
   “`S(∅,⊤) = the FULL BOX`” and `T(∅,⊤):=⊤`, not Theorem C(a). Its corollary likewise proves positive mass of the empty fiber from the normalized full-box display, reserving Theorem C(b) for nonempty histories. This is consistent with the identical dispatches in D4R.1 and L12.

3. **CLEAN — whole-section empty-history citation scan.** Every location where `∅` can enter is correctly handled:

   - L5: §C’s explicit empty-history definition.
   - L6 cylinder identity: direct full-box argument.
   - L6 positivity corollary: direct mass-one argument.
   - D4R.1: empty summand separated before C(b).
   - L12: empty shape/fiber separated before C(b).

   L9’s empty product is vacuous, while the remaining §C references are structural definitions or concern nonempty node histories. No other invocation of Theorem C(a) or C(b) has `∅` in scope.

4. **CLEAN — L1–L12 and D4R.0 wiring.** The twelve-item ledger is coherent: L3 is intentionally definitional and item 10 is D4R.1. L1 and L5 remain explicitly OPEN as the two parts of `D4R.0-K`; L2, L4, L6, and L7 consume exactly the dependencies they name. Prefix cylinders include infinite runs, while only L7 identifies complete terminating histories with run fibers. No completion-union or termination assumption reappears.

5. **CLEAN — mass, pool, and finite-level displays.** D4R.1 follows from L6 disjointness, L9’s shape-constant mass, and L8 finiteness. L11’s enlarged encoding includes factor-selection indices and proves the stated sharper factor  
   `M(P̂)=∏m̄_r≤n^{|P̂|}`. D4R.2 consistently carries `M`; the literal coefficient-one `(3i)` is expressly not claimed except when `M=1`. L12 separately handles `Pref(P̂)=∅`, so no maximum over an empty set is formed.

6. **CLEAN — status and perimeter.** The note consistently classifies D4R.0 as OPEN and D4R.1/D4R.2/D4R.4 as CONDITIONAL on accepted §C, accepted §B2-DEF, and `D4R.0-K`. Polynomiality, menu finiteness, resummation, exhaustion, and termination remain deferred. The passed numerical gate is not misrepresented as closing the textual kernel.

7. **CLEAN — sealed-gate arithmetic and claims.** The displayed values recompute correctly:

   - `A=9+2=11`, `W=1·3+2·2=7`, `A_net=4`, `M=1`, `N=5`.
   - `C(2)=1·1·3=3`; `C(3)=3·2·8=48`.
   - Counts: `384`, `3072`, `3888`, `104976`.
   - Box sizes and the aggregate `404,128,692` are correct.
   - FC8 gives `4·3·2−4=20` valuation lumps.
   - FC9’s `p²−1` nonzero cells plus `DEEP` is exhaustive and disjoint at the stated resolution.
   - The gate’s limited scope and its failure-to-close-the-kernel warning are preserved.

8. **CONFIRMING CLEAN PASS.** A second end-to-end scan of the identical text found no changed dependency, citation, display, status tag, bound, or gate number requiring classification.

**Counts:** 0 critical errors; 0 justification/display gaps.

**Verdict: ACCEPTED (0/0).**
