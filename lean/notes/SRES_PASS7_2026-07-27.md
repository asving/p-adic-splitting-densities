1. CLEAN — The displayed “(m, c) CLASSIFICATION” is exhaustive and exclusive:  
   “(c = 0, any m)”, “(c ≥ 1, m ≥ 2)”, and “(c = 1, m = 1)” partition every pair with \(m\ge1\) and \(0\le c\le m\). The routing agrees with the S.0 column typings, V.6.1/V.6.3 rows, and DEG-CONS. TERMINAL/CONTINUING supplies the \(c\)-axis; the full member tuples supply the \(m\)-axis; neither roster label is improperly treated as the routing rule.

2. CLEAN — The K-column agreement is valid. A K-entry is typed block-\(e\to\)block-\(e\), hence its continuing target is a member of size \(e\). The full-roster degree law gives, for \(m\ge2\), \(e_j\ge1\) and \(\sum_j e_j\le e\), so every \(e_j<e\). Therefore no \(m\ge2\) outcome can supply a K-column. Since a K-column is continuing, it consequently has \(m=1\) and \(c=1\). Conversely, (SCS) places every \((1,1)\) outcome back in block \(e\).

3. CLEAN — (SCS) now runs definitionally. With \(m=1\), every flank side and every non-selected residual factor is excluded, forcing full span \(e\ell=W\) and \(\lambda=\{(g,\mu)\}\), hence \(g\mu=\ell\). The stage laws give
   \[
   W'D'=\mu(egD)=(g\mu)eD=\ell eD=WD,
   \]
   so the sole continuing child preserves cluster size. The window-shrinking species edge therefore cannot occur in the \(m=1\) bucket.

4. CLEAN — Multi-member all-halted outcomes are correctly routed. In particular,
   “\(m_{H3}\)” has \((m,c)=(3,0)\) and lands in \(b_e^{\mathrm{term,fin}}\) with verdict multiset \((1,1)^3\); “\(m_{H12}\)” similarly lands there with its two-member multiset. This is licit because \(\sigma'\) is explicitly multiset-valued and the terminal-column definition imposes no \(m=1\) restriction.

5. CLEAN — Mixed outcomes such as \(m_{2+1}=(2,1)\) route to \(b_e^{\mathrm{split}}\), with the halted sibling contributing factor \(1\). All-halted multi-member outcomes are no longer mistaken for splits, and no cell is shared between K and \(b_e^{\mathrm{split}}\).

6. CLEAN — End-to-end acceptance-depth sweep found no new inconsistency in (K-TRI), (K-SUB), the S.4 branch-probability interpretation, RS.1 routing, rationality/ACT typing, the consumption DAG, or the honest conditionality perimeter. The existing open kernels and unrun acceptance gates remain explicitly fenced and do not invalidate the [3]-COND note-level result.

Counts: 0 critical, 0 gaps.

Verdict: **ACCEPTED (0/0)**
