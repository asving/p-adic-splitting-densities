1. **CRITICAL — §C rev-14’s side-height field is missing from the shape/encoding.**  
   L3 defines a shape-prefix using “`(e, h, g, μ, a)`” plus horizontal side data, but omits §C’s explicit side-height field `u*_r`. Yet §C rev 14 makes `u*` part of the node datum determining the read line, `(NA)/(SAE)`, and the fresh-clause inventory. Consequently:
   - L9 has not shown that `A(η)` is constant on the stated `Pref(P̂)`;
   - L11’s claim that shape plus digits and selection indices reconstructs the node datum omits `u*`, so `enc′` is not proved injective;
   - therefore D4R.1-SUM and D4R.2 do not follow for the live definition of `P̂` unless `u*` is retained or proved recoverable from the listed fields. No such recovery is displayed.

2. **GAP — the live dependency/status statements still cite the punctured §C version.**  
   The opening says “`accepted §C (Theorem C, rev 11b, passes 12+13 CLEAN)`,” while D4-R.5 still says “`§C's own queued (SAE) repair`” and is “`conditional on that repair landing`.” The supplied record says those passes were punctured and §C became dual-accepted only at rev 14. The mathematical citations generally match rev 14, but the live source/version and perimeter statements are false or stale.

3. **VERIFIED — rev-8 heading repair.**  
   “`the digit-cell partition and its branch sets`” correctly describes L5 and Theorem D4R.0. It no longer suggests the withdrawn per-prefix-fiber partition.

4. **VERIFIED — rev-8 finite-level qualifier.**  
   “`at every level N ≥ N(P̂)`” is exactly the needed range: L8 gives finitely many fibers, L12 defines a common resolving level, and Theorem C(b) supplies each nonempty-history count there. The empty-prefix and empty-`Pref(P̂)` cases are separately covered.

**Counts:** 1 critical error, 1 justification/display gap.

**Verdict: NOT ACCEPTED.**
