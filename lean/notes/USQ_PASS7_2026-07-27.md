1. **GAP — stale identification language in the pass-1 record.** The record still says:
   > “PER-POOL REGULARITY AT p (= per-pool ESCAPE(E0)/CL-1 + CTS-M(ii) ACT (ii-c)/CL-5)”

   This explicitly identifies `(REG-p)` with E0+ACT, contradicting the live rev-5/rev-6 formulation that they are related but separate and neither implies the other. Historical records are within the requested sweep.

The two intended rev-6 edits are correct:

- SQ.6 item 6(vii) now states the proper related-but-separate relationship.
- The overview now qualifies the conclusion by primes satisfying `(REG-p)`.

The end-to-end mathematical and conditionality sweep found no additional substantive defect or unqualified prime claim; Theorem U’s initially broad wording is explicitly governed by its subsequent “every p satisfying `(REG-p)`” interpretation and failure semantics.

Counts: **0 criticals, 1 gap.**

**VERDICT: NOT ACCEPTED (0/1).**
