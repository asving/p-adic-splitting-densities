# MOVEST round-7 Fable verdict (2026-07-29)

{
  "criticals": 2,
  "gaps": 2,
  "verdict": "REV 7 NOT ACCEPTED — 2 crit / 2 gap",
  "findings": "CRIT-1 (NsBridgeStmt, §2.10 ~2024-2036; disposition row #5). The typed seam omits the note's per-cap DECAY leg and is FALSE as stated. The note's ∎ (7216-7233) closes via (ii) C(b) pricing μ(Σ∩L_M)=μ(Σ)p^{−|Z_M|} + (iii) |Z_M|→∞ ⇒ μ(Σ∩L_M)→0, THEN continuity from above. NsBridgeStmt's only premises are antitone + Tendsto μcap→μclass + finite-level read-off — no decay, no ScaleFaithful/ZCPack/hMgrow/JetTower (all present on its cousins NsAmendedPair-leg-2/TreeExpNs). Refutation from the corpus's OWN accepted counter-model (§0 record 10, rev-4 text: \"a jet CHOSEN to have Mlev=M₀, interiorFreshIdx=∅, L M=univ\"): childless-tower Tat (mem none=univ, mem some=∅, child=∅ — all TreeModel fields hold; tie vacuous), junk H with last-node μ·Dwidth=0 (Node's only proof fields are he/hh/hcop — μ=0 legal) ⇒ interior roster empty ⇒ Lat.L≡univ satisfies vanish/interior_mono/slot0; ratios≡1, μcap≡1 satisfies (i)-(iii) with μclass=1; conclusion demands 1=0. The owner (MovesS w5/[2b]) is handed an undischargeable statement; xrbOfMovesT.ns_null uninhabitable. Fix: state the note's CONDITIONAL (thread the lawfulness/gap-growth premise row or a per-cap-decay premise).\n\nCRIT-2 (T-F4 belowHalt_decomp, ~3406-3431; row #8). The ∅-shape expectation is wrong against the AS-BUILT corpus the row keys to: blueprint asserts \"the index sum runs over the ∅-shape's single root class … must compute 1 = 1 + 0\". But W4_SYNC delta 8 + built Defs.lean: at reads=[], mult x = 1 (dispatch, line 517) while PrefIdx is EMPTY — delta 8's own words: \"the incidence identity is false at reads=[] … RHS sums over empty PrefIdx; D11a mult_sum gained an hne premise\". So belowHalt_decomp computes 1 = 0 + 0 at every empty-shape P — false as displayed. The semantics are LANDED, not in-flight; the check is decidable now and fails. Fix: the same hne premise D11a took.\n\nGAP-1 (row #3 / §0 record 19, OmSat). The realized-membership conjunct CANNOT close the witness-free vacuity channel: any junk trackOf (constant monic irreducible of degree n+1, or any non-factor value) still fails a conjunct ⇒ ClusterFactorOf witness-free ⇒ OmSat vacuously TRUE — identical polarity to Codex-6 #3's first half. What actually kills the escape at the consumed seam is ruling 2's SHARED trackOf: KBTotTower.root_total (∀ψ repeated, ∃ν trackOf ν=ψ ∧ child) makes VPPinned uninhabitable under junk trackOf. Record 19's \"protection is the realized-membership conjunct in ClusterFactorOf's type\" is the wrong regression pointer; the conjunct's real gain is the wrong-reduction/over-strength direction. Re-attribute (record + row) to the VPPinned-level trackOf tie.\n\nGAP-2 (row #6, T-G1 full-roster table ~4187). The pinned γ values violate the γ-tie of BOTH HistLawful (§2.2) and built HistoryCoherent (MovesC ~641-644: gam = e·(strFrame·ustar) + (s0+wSide)·h). Heads (s0,wSide)=(0,2), e=STR=u*=h=1: law gives γ=3, table pins γ=1; leaf a: 1·2+1·2=4≠2; leaf b: 3+3=6≠3. The table's cited formula \"γ = e·u*+j*·h at j*=0\" treats j* as free; the displayed law pins the coefficient to s0+wSide. No gate/census consumes gam (integers unchanged, verified), but the claimed consistency fails the blueprint's own REV-6 rule (walk pins against the table's own consistency clause) — same class as Fable-5 GAP-1's slopes. Lawful re-pins exist (γ=3/4/6, or adjust u*).",
  "dispositionCheck": "All 19 rows checked vs text + built corpora. REAL (15): #1 per-track root_total w/ trackOf in KBTotTower+VPPinned (childless tower dies at x=0, f̄=xⁿ — rechecked); #2 ChildCover + constFalse_childCover_false — sound, fence-free, kills the constant-false model AT the interface, faithful to 7112-7119, dodges record-13 (∃ν varies with x), delta-10 recorded; #4 windowBlockHts/hScale/hRes closed over built htH (MovesC 605)/floorH (583)/μ·Dwidth; #7 bandFresh closed over built bandS/Nshape (sigs match); g1 downgrade real; g2 NPIDPin/NthrSup/noteNP/D4R4_all_noteRange(hpin) verified in NPv_npid.lean; g3 W4-5 row + OffsetPPin/CD_canonical verified in PolPin.lean, census 20 recounts; g4 wsh17_pin projection-def (Interfaces 187); g5 legs_read (141); g6 recorded; C-1 recomputed: 24→0 (32−8), 192→0 (256−64), cellEventE 8/64 unchanged, census 8=2^{9−6}, toyA=6, 2¹²=2⁹·2³, 2⁴·2²=2⁶ all exact; C-2 recomputed: levelIdx c<4 ⟺ c<6 covers all read coords; G-1=#1, G-2=g2, G-3 cite fix 7216-17 present. DEFECTIVE: #5 (NsBridgeStmt false — CRIT-1), #8 (∅-shape leg false at built PrefIdx-empty semantics — CRIT-2), #3 (protection misattributed — GAP-1), #6 (γ pins unlawful — GAP-2). 19F tally arithmetic ✓; event/mult dispatch + eventO/multO deletion verified built."
}