1. **Pass-1 finding 1 — GENUINE.**  
   Repair: “`b_e^split(τ)(σ′) := Σ_{o branching} J_{τ,o}(q) · …`”, with \(J_{\tau,o}\) defined as the branching-cell mass “EACH CELL ONCE” and explicitly distinguished from marked \(T\!\cdot G\).

   The dimensional analysis now works. For one source row \(\tau\):

   \[
   \beta_\tau
   =
   \underbrace{\sum_{\tau'}K_e(\tau,\tau')\beta_{\tau'}}_{\text{one same-size continuing cell}}
   +
   \underbrace{\sum_{\text{terminal cells}}\Pr(c)\,\mathbf 1_{\mathrm{type}(c)}}_{\text{terminal exit}}
   +
   \underbrace{\sum_{\text{branching cells }c}\Pr(c)
      \prod_j\beta_{\tau_j(c)}}_{\text{split exit}}.
   \]

   A branching cell is selected once, after which `(SIB)` supplies the conditional product of child laws. `(K-TRI)` ensures those children have strictly smaller block size. Correspondingly,

   \[
   \sum_{\tau'}K_e(\tau,\tau')+\text{terminal mass}+\sum_oJ_{\tau,o}=1.
   \]

   Thus \(K_e\), \(b_e\), and the recursion now have compatible probability dimensions. `(J-RAT)` correctly remains a separate open rationality burden.

2. **Pass-1 finding 2 — GENUINE.**  
   Repair:  
   > “`(JC-INV) per read, the JOINT digit-cell conditional law … branching cells included … is ONE function of the retained state — invariant across representatives and admissible histories, h_ent included.`”

   This is a refutable lumpability hypothesis: compare the complete probability vector of fully labelled digit cells from two histories representing the same retained state. It supplies branching-cell selection probabilities, which are exactly what the former proof lacked.

   The displayed non-implication is correct in substance. `(u-R)` controls single-branch/per-assignment or marked marginal data; it need not determine the probability of a full branching cell carrying a particular joint branch set. `(SIB)` begins after conditioning on that cell and therefore cannot supply its selection probability. The phrase “different correlations” is slightly informal because cells themselves form alternatives, but the accompanying “different branching-cell probabilities” states the decisive obstruction accurately.

3. **Pass-1 finding 3 — GENUINE.**  
   Repair:  
   > “`the list (i)–(v) is the seal PROTOCOL. A seal EXISTS only when a referee-readable artifact … records the CONCRETE contents … committed BEFORE any census runs.`”

   Every status line now calls RESUM-n3 `SPECIFIED`, not sealed. The governance claim is honest.

4. **Pass-1 finding 4 — GENUINE.**  
   `(ORC-1)`–`(ORC-4)` now separate certified cylinders from `UNRESOLVED`, require a displayed PARI-to-\(\sigma\) map, and type the comparison against the classifier-decided set. The exact Krasner/discriminant inequality remains a seal-time duty, but the gate is expressly forbidden to run before it is supplied; that is consistent with the current `SPECIFIED` status.

5. **Pass-1 finding 5 — PARTIAL.**  
   The mathematical convention is repaired:

   > “`the ENTERING READ … belongs to the ENTRANCE SIDE`”  
   > “`β_{e,τ} is CONDITIONED on that entrance event and charges NO mass or height for the entering read`”.

   This matches §V’s actual interface: a concrete entrance shape \(\varepsilon\) contains the prefix and designated branch landing in \(\beta_0\), while `(COMP-h)` starts \(\gamma\) at \(\beta_0\). Hence \(\iota/W_{\widehat S}\) owns the landing read and \(\beta\) begins afterward.

   The claimed independent “verbatim §T RS.1-SH pin” is not supported by the pasted §T text: `RS.1-SH` is a clause of §S-RESUM, not a named §T clause. §T supplies TREE-EXP, root `(SIB)`, and the marked-to-per-\(f\) conversion. This is a citation/record gap, not a remaining off-by-one mathematical error.

6. **Pass-1 finding 6 — GENUINE.**  
   RS.4 now explicitly inherits `(iv)-POLY`, XHD-s, INIT-RAT, rational \(W_{\widehat S}\), and `(J-RAT)`, with the correct explanation that primewise mass equality alone does not produce a fixed rational-function identity.

7. **Pass-1 finding 7 — GENUINE.**  
   The closed-class test now requires total in-class row mass \(1\), equivalently no terminal exit, no split deficit, and no \(A\)-edge leaving the class. This is the correct finite stochastic obstruction to E0.

8. **CRITICAL — E0/ACT is not quantified over the base-changed evaluations used by the recursion.**  
   S.3 and both convolution sites consume children as

   > “`\(\beta_{e_j,\tau_j}(q^{\delta_j})\)`”

   representing continuation over \(O_{\delta_j}\). But E0 is stated only as

   > “`For each fixed p … A := … K_e(p)`”,

   and the subsequent active-value discussion is framed around wild primes \(p\), especially \(p=2,3\). End-to-end probabilistic solvability requires the analogue for every relevant prime-power pool \(q_0=p^\delta\): the active matrix \(K_e(q_0)\), its escape property, and removable-pole/active-value agreement there. Symbolic nonvanishing of \(\det(I-K_e)\) does not prevent a zero or uncancelled pole at an individual \(p^\delta\).

   REL.2(e3) cannot silently replace this missing scope: it is an identification of the evaluated \(\beta\) with an \(O_\delta\)-mass and therefore itself presupposes that the evaluated entry is well-defined. The E0/ACT statement and its probe/active-value perimeter must cover the base-changed systems that RS.1 actually consumes.

9. **GAP — the asserted internal proof order contradicts the displayed section order.**  
   The note repeatedly says S.1 is

   > “`BEFORE both PCI β-substitution sites (b_e^split at S.0 …)`”

   and the S.0 site says XRB is

   > “`PROVED ABOVE THIS SITE at S.1`”.

   S.1 is textually after S.0. A definition in S.0 may legitimately forward-reference a later proof, so this is not itself a mathematical cycle; however, the claimed binding proof-order audit is false as written. The note must distinguish definitional presentation from logical consumption or reorder the sites.

10. **GAP — `(J-RAT)` and `(JC-INV)` are assigned retroactively to external owners without a matching owner-side interface record.**  
    Both are honestly marked open, so no theorem is overclaimed. Nevertheless, `[1v]/[2b]` are already separately reviewed artifacts, and the pasted §V/§H interfaces do not state these new deliverables. In particular, PART-1 supplies total cell-partition mass, not the individual polynomial/rational cell tables needed for `(J-RAT)`. The new obligations need an explicit owner-side acceptance/interface entry before they can be discharged rather than merely cited from §S.

Repair classifications: **6 GENUINE / 1 PARTIAL / 0 NOT FIXED**.

New/outstanding findings: **1 critical / 2 gaps**.

**Verdict: NOT ACCEPTED.**
