1. **CORRECT — rev-6 propagation is complete.** X.2 restricts its family intrinsically to histories satisfying “H is (τ-ns)-FREE.” The live (X2-BRIDGE) statement likewise requires “SOME (τ-ns)-FREE branch,” and its fourth decomposition piece uses “∃ (τ-ns)-FREE finite branch H.” No live cap-bearing display relies on membership in an unordered union.

2. **CORRECT — all other cap quantifiers are safely typed.** (X2-CAP) initially quantifies over CAP-DETECTABLE branches, which necessarily terminate through (τ-irr), and extends only to the already defined fourth-piece family. Subsequent envelope applications use the bridge’s witnessing branch. X.3(b) invokes TB-CAP only for complete trees whose leaves are explicitly restricted to (τ-irr)/(τ-hen). No display evaluates cap on a (τ-ns) branch.

3. **CORRECT — the exclusion predicate is decidable from finite cell data.** VP-2 makes τ a function of cell data; in particular, (τ-ns) is the cell-data condition that the current window has no eligible side, \(J(f)=\varnothing\). Therefore, checking a finite history for absence of a node/cell carrying that status is a finite per-node/cell-data test, not a measure-theoretic or union-membership test.

4. **CORRECT — no live union-routing argument survives.** References to the former “routing” argument occur only in revision-history passages identifying it as invalid, “STRUCK,” or superseded. The one historical pass-3 description using “routes them” is not consumed by any current definition, quantifier, inclusion, or proof step. The operative rev-6 text uses intrinsic membership predicates throughout.

5. **CORRECT — full end-to-end sweep.** The certified/uncertified depth split, four-population termination argument, both X.3(a) routes, König bridge, qualitative/quantitative tag separation, square-root-envelope leg tags, TB-CAP domains, NS-ROUTE typing, consumer map, and toothless-gate declaration remain consistent with their displayed conditionalities. No new critical error or gap was found.

Counts: **0 critical, 0 gaps.**

**VERDICT: ACCEPTED (0/0)**
