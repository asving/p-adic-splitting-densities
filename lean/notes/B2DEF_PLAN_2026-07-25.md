# §B2-DEF PLAN v4 — the tower induction via the trivialized graded localization
*(2026-07-25. REV 4 — the drafting rev. History: rev 1 drafted + self-reviewed (4
amendments pre-verdict); structural pass 1 (Codex, fresh context): 5 crit + 3
concerns — findings 1/6 coincided with self-amendments B/A (independent
convergence). Rev 2: pass 2 judged 6/8 RESOLVED; new findings 9–14, all in the
stage-transition cluster. Rev 3 displayed TRANS; pass 3 judged 9/10/11/14 RESOLVED
and returned five criticals of a categorically SHALLOWER kind — a base-index typing
slip (15), a weight-formula symbol error (16), construction-order circularity (17),
a localization notation bug (18), the missing recentered-stage package (19) — plus
DAG sync (20) and the residues of 12/13. Rev 4 folds all of these in (see the
per-finding notes inline, tagged [P3.n]) and — decisively — PROVES Lemma REAL
(§3-REAL below: the per-stage domination induction h_r > e_r·f_r), removing the
last open vertex. DECISION RECORDED: after three structural rounds with all
post-pass-2 repairs local, further plan-level passes hit diminishing returns
(pass-3's 12/13 residues are 'not proven here' complaints only a draft can
discharge); drafting proceeds from this rev, and the draft carries the full
acceptance burden under its own adversarial passes. No claim below is citable
until §B2-DEF itself passes verification.)*

## 0. Diagnosis (unchanged from rev 1)

Revs 4–7 of §B2 worked with FLATTENED residuals — anchored digit polynomials over a
field — and re-axiomatized the discarded monomial bookkeeping ("carries",
"alignments", "coset typing"), which each pass correctly found unproved. The repair
is structural: never flatten. Work inside gr_w(A) for the current side valuation w
and its monomial localization, where ONE fixed degree-1 homogeneous unit T makes

  R(f) := [f] · T^{−w(f)}

simultaneously (a) F-linear on each homogeneous piece and (b) globally
multiplicative. Both ring operations exact; no cocycle, no alignment, no typing
axiom. The pass-7 carry algebra 𝒟 = F[u]/(u^e − z̄) is CONSTRUCTED (a concrete
subring of a localized quotient ring), not postulated.

## 1. Level 1, fully explicit (the template — unchanged from rev 1)

A = ℤ_p[x]; key φ monic deg d, φ̄ irreducible, F := F_Q; side slope h/e;
w(Σaᵢφ^i) := min(e·v(aᵢ) + i·h), value group ℤ. 𝒢 := gr_w(A) ≅ F[π, y] (accepted
B1.1); L := F[π^{±1}, y^{±1}] (§A.2's accepted Laurent localization); L₀ =
F[z^{±1}] where z := THE POSITIVE GENERATOR of the rank-1 degree-0 monomial group
(= y^e π^{−h}; definition non-circular: monomial group first, then z, then T, then
R). T := π^s y^t, one FIXED Bézout pair es + ht = 1, pinned per §4-P below.
Dictionary: R_anch(f) = z^{−ord_z R(f)}·R(f); §A's absolute form = z^{m(f)}·R_anch,
m(f) = (i₀(f) − t·w(f))/e ∈ ℤ. Coefficient digits are MONOMIALS: dig(B) := R(B) =
c·z^{k(B)}, c ∈ Fˣ.

## 2. The stage invariant (v2 — placements corrected per findings 3, 4)

A *stage* = the data of one read: side valuation w (value group ℤ), key Φ,
coefficient space C = {deg_x < deg_x Φ}, with:

- (S1) w is a valuation.
- (S2) K1(Φ/w): Φ-developments achieve the slot minimum.
- (S3) Slot decomposition (P6): gr_γ(A) ≅ ⊕_j gr^C_{γ−j·w(Φ)} as GROUPS (from K1;
       the accepted part of rev-4's P6). NO subring claim at this stage's grading
       [repair of finding 4 — the coefficient product rule enters only WITH the
       augmented grading of the NEXT stage, where the gap makes division quotients
       strictly deeper].
- (S4) Localization data: a declared finite set U of homogeneous units of 𝒢 :=
       gr_w(A) (at the base: {π, y}); L := 𝒢[U^{−1}]; the degree-0 monomial group
       is rank 1 with positive generator z; (L^coeff)₀ = F a finite field;
       L₀ = F[z^{±1}]; a degree-1 unit T (pinned per §4-P).
- (S5) Monomial digits + stride rule: for B ∈ C∖0, dig(B) = c·z^{k(B)}, c ∈ Fˣ;
       for a minimizing slot j at total weight γ, the slot's z-position is
       p_j = (j − tγ)/e — an integer (anchor congruence + ht ≡ 1 mod e), affine in
       j with step 1 per e-stride, depending only on (j, γ). Consequences proved
       once, level-free: the WIDTH BOUND (R of a coefficient-space element for the
       next key has z-width < g), and Codex-finding-1-of-pass-8's demanded affine
       rule, now a two-line monomial computation.
- (S6) CONSTRUCTIVE SLOT BASIS [v4 form; repairs P1.3, P2.12, AND P3.15 — the
       base was mistyped in v3: level-1 coefficients occupy slot 0 ONLY; the
       x^i-structure inside C feeds the DIGIT (an F_Q-element), not the weight]:
       𝒜_base := {(e·v, −t·v) : v ≥ 0} — weight e·v, position −t·v, FORCED by
       weight (single-slot space); realizers p^v·û, û any unit lift, giving every
       scalar in F_Qˣ. Recursion [P3.12-residue: no placeholders]:
       𝒜_{r+1} := {(e'·(γ + j·h_r), pos) : (γ, κ) ∈ 𝒜_r, 0 ≤ j < e_r·g_r}, where
       h_r := w_r(Φ_r) (the CURRENT key's weight — [P3.16's symbol fix]), with
       pos = 0 at e_read = 1 stages (T coefficient-pure ⟹ all coefficient digits
       position-0) and pos = (j − t·(γ + jh_r))/e_read at fractional stages (the
       §2-S5 formula). Realizers: t_{γ,j} := (stage-r realizer of γ)·Φ_r^j —
       explicit, degree-bounded. Scalars: free at every index (base: unit lifts;
       step: the P-LIFT = the B1.3 standard-lift formula applied to arbitrary
       P ∈ F[z], deg P < g, realizing any c' = P(z̄) ∈ F'ˣ). What the recentering
       needs from S6 is ONLY (γ, 0) at e_read = 1 stages — supplied by Lemma REAL
       (§3, now PROVED).

## 3. The two moves (v2 — repairs of findings 1, 2, 4, 5 inline)

**The increment (fractional or unramified side; e·g > 1).** ψ ∈ F[z] irreducible,
ψ ≠ z, deg ψ = g; Φ̂ := the standard lift (weights all equal on its slots, R(Φ̂) =
z^{m̂}·ψ with m̂ explicit). Chain of lemmas, each with its one proof mechanism:

- K1(Φ̂/w): minimizing-slot terms R(B_j)·z^{jm̂}ψ^j have ψ-order EXACTLY j (width
  bound + ψ ≠ z prime in F[z^{±1}]); distinct orders cannot sum to zero. NO
  coset-splitting needed — the absolute residual carries the pass-5 offsets
  natively. [Amendment C.]
- The ψ-DIVISION IDENTITY, tie-robust [repair of finding 4]: BB' = QΦ̂ + R_dev ⟹
  in 𝒢: [B][B'] = δ₁[Q]·inΦ̂ + δ₂[R_dev], δᵢ ∈ {0,1}, at least one fires (K1 gives
  ≥, ultrametric gives the min). Apply the graded embedding R and reduce mod ψ:
  the Q-term dies (R(inΦ̂) = z^{m̂}ψ); dig'(B)·dig'(B') ≠ 0 in the FIELD F' :=
  F[z]/(ψ) (width bound); hence δ₂ = 1 — the remainder achieves the weight tie
  EXACTLY — and dig'(R_dev) = dig'(B)dig'(B'). One displayed computation delivers:
  multiplicativity, nonvanishing, exact remainder weight. [This is pass-7 item 2
  and pass-8 crit 4, discharged.]
- **Proposition TRANS, DISPLAYED [pass-2 findings 9, 11, 13, 14 + rev-1 finding 7].**
  The augmented weight w'(Σ_j B_j Φ̂^j) := min_j (e'·w(B_j) + j·h'), on the side
  (e', h') to be read next; K1(Φ̂/w') is DEFINITIONAL (the accepted pass-4
  observation). Construction of the full next graded algebra gr_{w'}(A), each
  piece with its named mechanism:
  (i) SLOT DECOMPOSITION at w' (P6'): gr_γ(A) ≅ ⊕_j gr^{C'}_{(γ−jh')/e'} as
      groups — definitional K1 + additivity of monic division. Y' := in_{w'}(Φ̂),
      with w'(Φ̂) = h' (its own development is the slot-1 unit).
  (ii) SUBMULTIPLICATIVITY FIRST, at the level of A — no gr-multiplication is
      used before it exists [P3.17's order fix]: for coefficients, BB' = QΦ̂ +
      R_dev with w(R_dev) ≥ w(B) + w(B') and w(Q) ≥ w(B) + w(B') − w(Φ̂), both
      from K1(Φ̂/w) applied to the development of BB'; for general f, g the
      slotwise product estimate then gives w'(fg) ≥ w'(f) + w'(g), the Q-terms
      landing one slot up with the strictly positive (I-aug) gap h' − e'·w(Φ̂).
      All statements are inequalities about elements of A. THEN gr_{w'}(A) is a
      well-defined graded ring, and 𝒞' := its coefficient subring is closed under
      multiplication with [B]·[B'] = [R_dev] (the Q-term strictly deeper). [P2.14
      stays dodged: no quotient presentation is consumed.]
  (iii) DOMAIN/valuation, in this order: the tie-robust division identity +
      dig'(B)dig'(B') ≠ 0 in the FIELD F' give that leading slot data of nonzero
      elements multiply to nonzero leading slot data; with Y' transcendental over
      𝒞' (slot decomposition (i)), gr_{w'}(A) is a graded domain, i.e. w' is a
      valuation (Ore'). [P2.9 resolved at pass 3; order now per P3.17.]
  (iv) LOCALIZATION [P3.18's notation fix + P3.13's representatives fix]: the old
      units are carried by their explicit REPRESSENTATIVE ELEMENTS ũ ∈ A (p, φ,
      the previous keys — all concrete monic/monomial elements of degree <
      deg Φ̂, hence in the new coefficient space); U' := {in_{w'}(ũ)} ∪ {Y'}.
      Nonvanishing is automatic (nonzero elements have nonzero initial forms);
      what needs proof is dig'(ũ) ∈ F'ˣ: R(ũ) is a MONOMIAL c·z^k by S5 (ũ IS a
      coefficient-space element of the new stage), and ψ ∤ c·z^k (ψ ≠ z, deg ψ =
      g ≥ 1). The "(inΦ̂-analog)" phrase of v3 is DELETED — nothing needs it.
      L' := the localization of the graded ring gr_{w'}(A) at the multiplicative
      set generated by U' (one localization of one ring; 𝒞'^loc := the image of
      𝒞' localized at U'∖{Y'}).
  (v) THE DEGREE-0 FIELD: define the graded embedding D := (per-piece
      linearization of R) mod ψ: linear on each gr-piece (R_δ(x) = x·T^{−δ} is
      well-defined and injective on A_{≥δ}/A_{>δ}), multiplicative across pieces,
      valued in F' := F[z]/(ψ). Injective on coefficient pieces (width < g ⟹
      nonzero mod ψ); surjective onto F' (z̄ = D of a ratio of old U-monomials).
      Hence (L'^coeff)₀ ≅ F'. [Replaces rev-2's quotient route; finding 14 moot.]
  (vi) RANK-ONE, correctly stated [finding 11]: what S5 consumes is NOT the rank
      of the unit-monomial group but: degree-0 elements of L' with FIXED
      Y'-exponent k form a single F'-line. Proof: coefficient-side localized
      pieces (𝒞'^loc)_δ are F'·(one U'-monomial) when e' | δ and ZERO otherwise
      (coefficient weights lie in e'ℤ); a Y'-exponent-k degree-0 element needs
      −k·h' ∈ e'ℤ ⟹ e' | k (gcd(e',h') = 1). Define z' := u₀·Y'^{e'} for one
      fixed U'-monomial u₀ of degree −e'h'; then the k-line is F'·z'^{k/e'}. Any
      excess in the unit-monomial group's degree-kernel lands in F'ˣ — a finite
      group, absorbed into scalars; its rank is irrelevant.
  (vii) T' per §4-P; S5 (monomial digits + stride rule) re-derived by the same
      two-line monomial computation in the new frame; S6 re-derived per §2-S6.
- INCREMENT LANDING, two-sided, with the pass-2-gap-5 iff now two lines: every
  minimizing slot contributes ψ-order exactly j, so ord_ψ R(f) = least minimizing
  slot; "stratum: ord_ψ R(f) = μ" ⟺ "no minimizing slot < μ; slot μ pinned" —
  the landing box IS the distinct-orders statement. Transported data: R(B_j) for
  the higher minimizing slots, deterministic via P6-linearity.

**The recentering (e_read = 1 only; the species lemma restated).** [Repairs of
findings 1, 2.] At an e_read = 1 stage with T pinned as the previous stage's image
(§4-P): EVERY coefficient digit has position 0 (dig(B) = [B]T^{−w(B)} has
Y-exponent 0, hence lies in (L^coeff)₀ = F) — so the classifier's recentering
lift t (any lift: w(t) = the side value, dig(t) = c̃ ∈ Fˣ; realizability by S6)
automatically matches positions; NO restriction of the move class occurs, and
conclusions depend on t only through [t] — LIFT INDEPENDENCE is a lemma (empirics:
the sealed G2 gate). Then with Φ' := Φ̂ − t: v = v' (the accepted B2⁗.2 argument),
z' := R_v(Φ') = z − c̃, and the substitution identity is stated IN THE COMMON
LOCALIZATION F[z^{±1}, (z − c̃)^{−1}] [amendment B = Codex finding 1]:

  R'(f) = R(f)|_{z = z' + c̃}   (both residual maps land in the common ring;
  the parent's landing data is z-polynomial, the child's z'-polynomial, and the
  substitution converts one to the other; the anchored display (a', R'_anch) =
  (ν, z^{−ν}(z+c̃)^a R_anch(z+c̃)) is the derived corollary — the pass-7-confirmed
  target).

  RS-LANDING two-sided via the symmetric inverse move (again position-free).

**(I-aug), DISPLAYED [pass-2 finding 10 — no longer a promise].** The invariant:
*at every read, the side slope (per-slot, integer-normalized) strictly exceeds the
current key's weight: λ > w(Φ).* Re-established per species:
- ROOT: on the initial cluster (all non-lead coefficients ≡ 0 mod p; key x with
  weight 0 after normalization), every polygon side has slope ≥ 1 > 0 = w(x).
- INCREMENT: the landing box gives w'(B_j) − w'(B_μ) > (μ−j)·w'(Φ̂) for j < μ, so
  every chord of the child polygon over [0, μ] to the pinned vertex has slope
  > w'(Φ̂); lower-polygon convexity WITHIN THIS ONE FRAME extends it to every side
  over the continuing window. (The pass-6-accepted B2⁗.1 arithmetic, restated.)
- RECENTERING: after RS-landing the recentered key's weight equals the consumed
  side's value, and the child polygon's sides over the continuing window [0, μ]
  satisfy the same chord inequality from the RS-landing box.
Every non-root read is immediately preceded by a landing of one of the two species
(the read process's definition), so (I-aug) holds at every read by induction along
the history. Consumed EXACTLY ONCE: TRANS(ii)'s strict-depth step.

**Lemma REAL (recentering-weight realizability) — PROVED at v4 [closes P2.12's
residue; corrects P3.16's weight formula; answers P3's 'eventual ≠ every'
objection: the bound is PER-STAGE].** Claim: at every e_read = 1 read, the side
value λ″ lies in the coefficient weight set (position-0 stratum), so the S6
realizer t exists. Proof:
(a) COEFFICIENT WEIGHT RECURSION (corrected symbols): in the stage-r scale, the
    weight set of C_{r+1} is 𝒲_{r+1}^{unnorm} = ⋃_{0≤j<e_r g_r}(𝒲_r + j·h_r),
    h_r := w_r(Φ_r) — single-slot realizers B_j·Φ_r^j; minima of achievable
    values are achievable. (P3.16 fixed: h_r is the CURRENT key's weight, and at
    level 1→2 only, this coincides with the increment slope — the degeneracy that
    made the v3 depth-2 check come out right.)
(b) COFINITENESS with explicit bound: 𝒲_r ⊆ e_r·ℤ with gcd(e_r, h_r) = 1 and j
    ranging over ≥ e_r consecutive values ⟹ the union covers every group element
    ≥ e_r·f_r + (e_r − 1)·h_r =: f_{r+1}-bound, where f_r is the stage-r bound
    (f₁ = 0: 𝒲₁ = e₁ℤ≥0 exactly).
(c) DOMINATION INDUCTION, per-stage: claim h_r > e_r·f_r for every r. Base:
    h₁ ≥ 1 > 0. Step: f_{r+1} = e_r f_r + (e_r−1)h_r < h_r + (e_r−1)h_r = e_r h_r
    ≤ e_r h_r g_r = w_r(Φ_{r+1}) (the standard lift's weight under the PARENT
    stage weight); every read of the child polygon has slope > w_r(Φ_{r+1}) by
    (I-aug); in particular the next stage's h_{r+1} = e_{r+1}·(that slope) >
    e_{r+1}·f_{r+1}. Induction closes.
(d) CONCLUSION: at any e_read = 1 read, λ″ > w(current key) ≥ f-bound of the
    current coefficient set [by (c)], λ″ in the coefficient value group [e_read =
    1 definition], hence λ″ ∈ the weight set by (b); position 0 is automatic at
    e_read = 1 stages (T coefficient-pure), scalars free by S6. For reads inside
    a RECENTERED frame: the coefficient space and weight are UNCHANGED (v = v'),
    and subsequent side values only grow (convexity within the frame + (I-aug)
    re-established after RS), so (d) persists. ∎
(The draft re-proves this with full scale bookkeeping; the mechanism — (I-aug)'s
multiplicative growth dominating the linear Frobenius recursion — is fixed here.)

**TRANS-RS (the recentered stage package) [new at v4; repairs P3.19 — the RS move
must also close the induction].** After Φ' := Φ̂ − t at an e_read = 1 stage: the
child stage's package is inherited nearly verbatim, and the draft SAYS so item by
item: S1 (same valuation, v = v'); S2 = K1(Φ'/v) (the g = 1 instance, displayed);
S3 (slot decomposition at Φ', from K1); S4: units = the SAME representatives ũ
plus Φ' itself; T unchanged (coefficient-pure, untouched by the move); L'₀ =
F[z'^{±1}] by the same rank-one argument at Φ'; S5: coefficient digits LITERALLY
unchanged (same C, same v, same T — position 0 all); S6: the same 𝒜 and
realizers. F does not grow at a recentering. Nothing else is needed for the next
read in the primed frame.

**The per-move ledger corollary [repair of finding 5 — the missing endpoint].**
Each move is a bijection of the WHOLE coefficient space commuting with reduction
mod p^N (Fact B — accepted); the two-sided landing statements give
Θ(stratum) = landing cylinder EXACTLY; P6-linear isos translate both loci into
digit-cylinder descriptions at any level above the constrained threshold; hence
#(stratum mod p^N) = #(cylinder mod p^N) = the nominal product × the transported
point mass. Stated ONCE at stage level (the §A.2-corollary pattern verbatim);
this — not just the two containments — is the section's endpoint per move.
Joint histories remain §C (deferred explicitly; pass-8 gap 7).

## 4-P. Pinned conventions (new; several findings traced to unpinned choices)

- Integer normalization of every stage's value group (ℤ, onto).
- z := positive generator of the degree-0 monomial group [amendment D].
- T: at e_read = 1 stages, T := image of the previous stage's T (coefficient-pure;
  exists since U' contains the old units and the old key class); at fractional
  stages, any fixed Bézout monomial T := (unit)^s·Y^t, es + ht = 1. Positions and
  strides are relative to THIS pinned choice; the convention dictionary translates
  §A/§B1 statements once.
- All residual identities are displayed in the named ambient ring (L₀, F[z^{±1}],
  the common localization, or F') — never "in coordinates".

## 5. The lemma DAG v4 (bottom-up write-up order) [P3.20: synced with §3]

0. Conventions + the pinned choices (§4-P) + the three-convention dictionary.
1. Lemma GR (graded generalities: filtration-compatibility makes gr a ring;
   domain ⟺ w valuation; initial forms add on ties or drop; localization at
   homogeneous elements; rank-1 freeness of fixed-degree pieces given a unit of
   that degree).
2. Base stage (§1; §A/§B1 accepted content re-expressed; S1–S6 verified —
   S6 base per the CORRECTED 𝒜_base).
3. Anchor congruence + stride rule + width bound (the S5 mechanism, level-free).
4. (I-aug), per-species, self-contained (displayed in §3).
5. K1 at the new key (under the current w).
6. The tie-robust ψ-division identity; dig' and its multiplicativity.
7. Proposition TRANS, FULL: (i) slot decomposition/Y'; (ii) submultiplicativity
   at the A-level FIRST, then gr ring, then 𝒞' subring; (iii) domain/valuation;
   (iv) localization via representatives, unit digits; (v) the degree-0 field via
   the graded embedding D; (vi) fixed-Y'-exponent F'-lines, z'; (vii) T', S5, S6
   re-derived. [Order per P3.17.]
8. Increment landing (two-sided; the two-line iff).
9. Lemma REAL (proved; the §3 display with full scale bookkeeping).
10. The species lemma; lift independence; the recentering: v = v', z' = z − c̃,
    the common-localization substitution identity, the anchored display,
    RS-landing two-sided; TRANS-RS (the recentered stage package, item by item).
11. The per-move ledger corollary (counts; the endpoint statement; NO composition
    clause).
12. Evidence perimeter (exact); the Case-K obligation.

## 6. The Case-K gate (role corrected per finding 8)

The gate VALIDATES FORMULAS in finite configurations; it is not evidence for the
general algebra (which stands or falls with the proofs). Configuration: a level-2
e_read = 1 recentering over a genuine extension residue field (deg ψ = 2 at level
1 ⟹ F' = F₄-type), anchor a ≥ 1 if reachable, with at least one transported digit
whose §B2-DEF-predicted value requires a z̄-reduction (z̄² = z̄ + 1-type — the
pass-6 counterexample as a census); preregister the full child table AND a
deliberately position/reduction-naive alternative as the discriminating control;
seal; run p ∈ {2, 3}. If no reachable configuration separates the tables, record
the invariant that forces agreement as a lemma.

## 7. Honest relation to the literature (unchanged)

This is the graded-algebra formulation of MacLane–Vaquié/GMN residual-polynomial
theory (GMN's theorem of the product ↔ per-level Ore; Vaquié's graded algebra ↔
𝒢). §B2-DEF proves self-contained (the ledger content has no published digit-level
counterpart) and CITES the correspondence for the algebra layer, flagging which
lemmas are known-shaped (GR, TRANS-shape, product) vs new (landing cylinders,
stride bookkeeping, S6, the gates).

## 8. Finding-resolution table (cumulative through structural pass 2)

| finding | class | status after v3 |
|---|---|---|
| P1.1 (Laurent rings differ) | CRIT | RESOLVED at pass 2 (common localization + T-pinning) |
| P1.2 (position-0 restricts the move) | CRIT | RESOLVED at pass 2 (position 0 = theorem; lift independence) |
| P1.3 (Lemma E circular) | CRIT | v3: S6 = constructive slot basis with DEFINED 𝒜; residue → Lemma REAL (open, named) |
| P1.4 (product rule circular) | CRIT | RESOLVED at pass 2 (tie-robust identity; subring only in TRANS(ii)) |
| P1.5 (endpoint lacks counting) | CRIT | RESOLVED at pass 2 (per-move ledger corollary) |
| P1.6 (Lemma H overgeneral) | CONCERN | RESOLVED at pass 2 (deleted) |
| P1.7 (transition asserted) | CONCERN | v3: TRANS displayed (i)–(vii) |
| P1.8 (gate epistemics) | CONCERN | RESOLVED at pass 2 (§6 reworded) |
| P2.9 (full next algebra absent) | CRIT | v3: TRANS(i)+(iii) — gr_{w'}(A) constructed, Y' in U' |
| P2.10 ((I-aug) not restated) | CRIT | v3: displayed per-species in §3 |
| P2.11 (rank-1 from wrong datum) | CRIT | v3: TRANS(vi) — the F'-line statement, excess kernel absorbed in F'ˣ |
| P2.12 (S6 uncheckable) | CRIT | v3: 𝒜 defined recursively = slot-basis indices; residue → Lemma REAL (open, named) |
| P2.13 (unit survival) | CONCERN | v3: TRANS(iv) — monomial digits + ψ ∤ monomials |
| P2.14 (kernel theorem too strong) | CONCERN | v3: DODGED — 𝒞' defined as subring; quotient iso demoted to remark |

| P3.15 (𝒜_base mistyped) | CRIT | v4: 𝒜_base = {(ev, −tv)}; recursion retyped with h_r = w_r(Φ_r) |
| P3.16 (wrong weight in REAL's set) | CRIT | v4: recursion corrected; feeds the REAL proof |
| P3.17 (valuation order circular) | CRIT | v4: TRANS(ii) = A-level inequalities first; gr ring second; domain third |
| P3.18 (L' not a defined ring) | CRIT | v4: one localization of gr_{w'}(A) at ⟨U'⟩; 𝒞'^loc separately |
| P3.19 (RS doesn't close induction) | CRIT | v4: TRANS-RS displayed item by item |
| P3.20 (DAG out of sync) | CONCERN | v4: DAG v4 lists TRANS(i)–(vii), (I-aug), REAL, TRANS-RS |
| P3.12-residue (𝒜 placeholders) | — | v4: recursion displayed with explicit pos-formula and realizers |
| P3.13-residue (unit images undefined) | — | v4: representatives ũ ∈ A; initial forms in the new gr; "analog" deleted |
| Lemma REAL | was OPEN | v4: PROVED (per-stage domination h_r > e_r·f_r; §3) |

Open vertices after v4: NONE at plan level. The draft carries the full acceptance
burden (complete proofs with scale bookkeeping) under its own adversarial passes.
