1. **CRITICAL — Lemma W’s hull grammar repair is PARTIAL, not sufficient.**

   Repair quoted:

   > “(G3) CONTIGUITY (shared vertices, no gaps/overlaps) … BOTH lines passing through ONE shared point (equal height there)”

   This repairs geometric contiguity, but not equality of the two endpoint digit values at a shared vertex. Each adjacent side’s digit tuple includes that endpoint. G2 merely requires both values to be nonzero and alphabet-admissible. Thus two adjacent sides may prescribe different nonzero digits for the same coefficient at the same height while satisfying G0–G7.

   The proof then incorrectly says:

   > “one covering side, or a shared vertex where the two lines AGREE — so … demands ONE height profile per slot, never two”

   Equal lines give one height, not one digit equation. Such a datum is grammatical under the written G0–G7 but empty. The compactness/freshness argument therefore fails at Step 1, before compactness can be invoked.

   Two smaller type omissions reinforce this:

   - G0 does not explicitly require `w′ > 0` or, for case R, `s_min < μ`. Unless “side” is already a formal nondegenerate type inherited from §B2-DEF, a zero-width “side” passes the displayed arithmetic conditions.
   - G0–G7 otherwise do cover the concerns named in the charge: endpoint heights are integral because widths are multiples of `e′` and the chain is anchored at the integral old vertex; G2 explicitly checks attainable height lattices and alphabets, including interior on-lattice digits; off-lattice slots are correctly handled by zeros below the rational line.

   Classification of pass-1 finding 1: **PARTIAL**.

2. **CRITICAL — Lemma E is logically exhaustive only relative to the defective grammar.**

   Repair quoted:

   > “NON-GRAMMATICAL := ¬(G0 ∧ … ∧ G7) = ¬G0 ∨ … ∨ ¬G7”

   The De Morgan enumeration is formally exhaustive for failure of the written predicates. It is not exhaustive for unrealizable abstract data because the shared-vertex digit conflict satisfies all eight predicates.

   The treatment of G7 itself is sound provided `(a_S,u_S,factor set)` remain stored fields of an abstract datum:

   > “¬G7 (derived-field mismatch)”

   “Forced” means forced for an actual datum, not definitionally erased from the abstract input, so a mismatched redundant field can meaningfully violate G7. If those fields were instead implemented as computed fields, ¬G7 would be uninhabited, but harmlessly so.

   G6 is redundant but logically legitimate. The statement that its negation is “vacuous relative to ¬G3/¬G4/¬G5” is imprecise: ¬G6 is impossible when G3–G5 hold, but it can coexist with failures among G3–G5. Redundant conjuncts do not invalidate De Morgan complementation.

   Classification of pass-1 finding 2: **PARTIAL**.

3. **GAP — the no-side leaf is not vacuously an instance of Lemma W.**

   The derivation says:

   > “apply Lemma W to the EMPTY side list (vacuously grammatical — G0–G7 quantify over the side list)”

   That is false as written. G3 refers to `[s_min,μ]`, with `s_min` undefined for an empty list, and G5 asserts:

   > “the rightmost side ends at `(μ,old(μ))`”

   An empty list has no rightmost side. Also, Lemma W is explicitly stated for “an abstract case-R datum,” whereas leaf T is case T.

   The parenthetical compactness argument likely proves T directly—set all slots below μ identically zero while preserving the pinned μ-slot—but it is not an application of the stated lemma. This is a proof gap, not evidence that T is actually empty.

4. **GAP — §D4-R rev 7’s additional `(SAE)` requirement is not wired into Theorem B’s branch-realizability conclusion.**

   Rev 7 says a realized prefix requires:

   > “(NA) + (HV) + (SAE)”

   and expressly says `(NA)+(HV)` alone is incomplete. Theorem B nevertheless concludes:

   > “every emitted branch satisfies §C realizability ((NA)/(HV))”

   and its proof derives only named `(HV)` and `(NA)` clauses.

   The needed inequality is effectively already present: R-a and Lemma W Step 2 show every chain line at a span slot below μ lies strictly above the old line. That appears to imply SAE. But the theorem’s stated interface and named consumption omit it, contradicting the pasted current consumer. Hence the structural result is not yet stated strongly enough to discharge rev-7 branch realizability verbatim.

5. **Pass-1 terminal-verdict finding: the narrowing is GENUINE; the residual interface is only PARTIAL.**

   Genuine repair quoted:

   > “NO general terminal-verdict function is defined or claimed in this theorem”

   and:

   > “the FULL-verdict form of L5 is NOT discharged”

   This correctly removes the old overclaim. The narrowed Theorem B supplies what rev-7 L5 consumes on its structural side: polygon/digit cells and branch sets. Including factor multisets does not over-refine those cells because G7 makes the factorization a forced function of the side digits. The no-side status is also structurally present.

   But D4R.0-K(c) is not yet a fully dischargeable specification. In particular:

   > “(c3) compatibility — automatic given (c1)+(c2)”

   is not automatic. A function τ of the complete, possibly infinite-height cell datum need not be determined at any finite cap. Theorem B(iii) makes exact cells measurable as countable intersections, but it does not imply that arbitrary τ-preimages are finite digit cylinders or that a verdict is detectable at a finite stopping height. The interface needs, at minimum, a finite-detectability/stopping-time condition if “measurable at every cap” is required.

   There are two further ambiguities:

   - c1 does not cleanly separate a cell-level terminal outcome from a verdict attached to an individual branch.
   - c2 leaves root Hensel leaves conditional on “if in scope — O4,” so the domain of configurations requiring verdicts is not closed.

   Classification of pass-1 finding 3:

   - Restatement of Theorem B: **GENUINE**.
   - D4R.0-K(c) as an assembly-side pin specification: **PARTIAL**.
   - Full-verdict L5: correctly **OPEN/BLOCKED**.

6. **GAP — the empty-prefix/multi-cluster root case is asserted but not incorporated into Theorem B’s displayed case tree.**

   S2.2 describes a level-0 factorization and potentially several opened tracks. But Theorem B’s proof subsequently uses one current key, one window, one `J(f)`, and one T/R dichotomy. At the full-box empty prefix, different root factors can simultaneously produce mixed read/no-side outcomes.

   O4 says this is “no gap for the kernel,” but rev-7 L5 explicitly takes the empty-prefix cylinder to be the full coefficient box. Unique reduction factorization makes a joint root construction plausible, yet the theorem does not display the product case tree or explain how mixed track statuses form its single structural datum. The normalized one-cluster theorem and the full-box root theorem are currently conflated.

7. **Pass-1 B5 finding: GENUINE repair.**

   The single-recentering chain is correct:

   \[
   W_k\ge\frac{W-(eg-1)h}{e}
   >\frac{\lambda^{(1)}-(eg-1)h}{e}
   >\frac{ehg-(eg-1)h}{e}
   =\frac he
   >w_{r-2}(\Phi_{r-1}).
   \]

   Every strict comparison has the claimed source:

   - `W > λ⁽¹⁾`: updated clean threshold;
   - `λ⁽¹⁾ > ehg`: I-aug at the first recentering;
   - `h/e > w_{r−2}(Φ_{r−1})`: I-aug at the increment that created the coefficient space.

   For consecutive recenterings, `λ⁽ℓ⁾ > λ⁽¹⁾` follows from the strict I-aug ladder, so the same lower-stage induction hypothesis applies. D.10’s “same coefficient space, weight, 𝒜 and realizers” supports retaining the original `(e,h,g)` machinery. The false equal-weights shortcut is genuinely gone.

   Classification of pass-1 finding 4: **GENUINE**.

8. **Pass-1 FC8 finding: GENUINE at the expressly weakened strength.**

   On `{V₀=3,V₁≥2}`, the points are:

   - slot 0 exactly at height 3 with nonzero digit;
   - slot 1 at or above height 2;
   - slot 2 at or above height 1;
   - slot 3 at height 0.

   Hence the lower hull is the full-span slope-1 side, and `(d₂,d₁,d₀)` are exactly its on-line digits. The factor multiset is a deterministic function of those digits, so adding it does not split a fixed digit tuple further.

   The derivation correctly weakens the off-target assertion: the 20 valuation lumps are only capped unions and are not claimed to be exact full-𝒟 fibers; NC is outside the normalized cluster cylinder.

   Classification of pass-1 finding 5: **GENUINE**.

9. **GAP — Theorem B(iii) sometimes calls valuation lumps “digit cylinders” too literally.**

   An exact capped valuation such as “first nonzero digit occurs at height h” is generally a finite union over the possible nonzero digit values, not one pure-equation cylinder unless the nonzero value is also fixed. This is consistent with FC8’s valuation “lumps,” but not with:

   > “the capped outcome … induces a partition … into FINITE digit cylinders”

   followed by calling FC8’s lumps literal instances. The safe structural statement is finite unions of pure digit cylinders. This does not damage totality or exclusivity, but it matters to the claimed resolution wording and to the non-automatic c3 assertion.

10. **The six former CITE-UNCHECKED items are textually resolved.**

   Against the pasted sources:

   - `[c-multiside]`: **VERIFIED** for `(α)/(β)/(γ)`, `(NA)`, and `(HV)`. It does not cover rev-7 SAE; that is the separate gap above.
   - `[c-noineq]`: **VERIFIED**. It supports fixed-pattern strata being pure equations.
   - `[c-cut]`: **VERIFIED**. The quoted C.1/C.1.5/LST statements support free above-floor coordinates and the stated alphabet-sized cuts.
   - `[c-free]`: **VERIFIED**. It identifies determined coordinates as the floor downset plus rim leads.
   - `[c-floor]`: **VERIFIED**. It supplies `F_i=old_i` on the factor interior.
   - `[c-term]`: **VERIFIED**, with the limited conclusion that §C is parameterized by an admissible terminal system. It does not itself define admissibility or prove c3.
   - `[c-perpath]`: **VERIFIED**. Both §C’s single-history formulation and rev-7’s interpretation are aligned.
   - `[c-hull]` and `[c-rs]`, added at rev 2: **VERIFIED** against the pasted D.4, D.9, and D.10 text.

   Thus the former count of six CITE-UNCHECKED falls to **zero**. Line-number stability cannot be assessed beyond the pasted register, but the quoted mathematical sentences match the supplied source text.

11. **Fresh sweep: Theorem A survives.**

   I found no new defect in the offset-P recursion, its scale conversion, sparse-slot handling, minimal weight, `g=1`, `e=1`, anchors, or recentered-stage arithmetic. The local reuse of `t` both for the Bézout integer and later for the constructed realizer is poor notation but not a mathematical ambiguity once read by scope.

### Counts

- Critical: **2**
- Gaps: **4** substantive gaps, plus **1** resolution-wording gap
- Former CITE-UNCHECKED: **0**
- Genuine pass-1 repairs: B5, FC8, Theorem-B narrowing, cite register
- Partial pass-1 repairs: Lemma W, Lemma E, D4R.0-K(c)

## Verdict: UNSOUND

The structural strategy is substantially improved, and Theorem A is adoptable. But rev 2 is not yet adoptable as the D4R.0-K structural kernel: G0–G7 still admit an empty shared-vertex digit conflict, so Lemmas W and E do not establish the promised realized-or-empty classification. Separately, rev-7 SAE and the full-box root case are not fully wired. D4R.0-K(c) remains legitimately open, but its current `(c1)–(c3)` formulation is not yet precise enough to make finite-cap compatibility automatic.
