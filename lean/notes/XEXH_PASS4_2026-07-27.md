1. **CRITICAL — the rev-4 cap-domain repair remains ill-typed in the displayed four-way decomposition.** The fourth set is written
   > “\(\{f:\exists\) finite branch \(H,\ \mathrm{threshold}(H)+\mathrm{cap}(H)>N\}\),”
   
   while the domain conventions explicitly say
   > “\((\tau\text{-ns})\) branches take NO convention”
   
   and merely claim that the preceding \((\mathcal T^{ns}\text{ fibers})\) union piece “routes” them away. An ordinary union is not ordered or short-circuiting: membership in the fourth set still quantifies over finite \((\tau\text{-ns})\) branches and therefore can evaluate the undefined \(\mathrm{cap}(H)\). The same defect undermines the assertion that X.2 is quantified over a well-defined “FOURTH-PIECE FAMILY.” Thus the first of the three rev-4 repairs is not complete. The added \(\max h(\varnothing):=0\) convention itself is sound, and the hen/no-window \(0\)-convention is consistent with TB-CAP(hen), but the ns-domain exclusion is not encoded in the actual display.

The remaining acceptance sweep is clean: the certified-depth table and reduction preserve the NOT-CERT semantics; the four-population decomposition and both X.3 routes carry the necessary T4/full WEIGHT-CHARGE burden; (X2-CAP) no longer contradicts TB-CAP’s nonuniformity; the \(\sqrt N\) line carries both mass-leg tags and agrees with the conditionality summary; the CL-4 consumer map is coherent and excludes SQ.1; and X-n3 honestly identifies its assembled finite-range envelope as toothless while retaining independently falsifiable layers.

**Counts:** 1 critical, 0 gaps.

**Verdict: NOT ACCEPTED.**
