Numbered findings:

1. **VERIFIED — D4R.1-EV pointwise upgrade.**  
   The passage beginning “by L8 the event … is a FINITE union of fibers” is correct. Each fiber is determined at some finite level; after taking a common level, any nonempty overlap contains a full residue class and therefore has positive mass. Thus equality in the finite union bound implies all pairwise overlaps are empty, not merely null, and hence  
   `mult_P̂ ≡ 1` pointwise on the event.

2. **VERIFIED — D4R.4-EV pointwise upgrade.**  
   For `N ≥ N(P̂)`, every fiber is exactly saturated by level-`N` residue classes. Therefore equality of the event count with the branch-weighted sum is equivalent to residue-disjointness, which is equivalent to `mult_P̂ ≡ 1` pointwise. The display correctly removes all a.e. slack.

3. **VERIFIED — corrected D4-n3 degree exclusion.**  
   The statement  
   `deg R_anch,0 = 3 < 2g₀μ₀ = 4`  
   is correct. Two distinct eligible irreducible quadratic factors, each of order `μ₀ = 1`, would contribute total degree at least `2+2=4`. Since the pinned cubic already has the displayed factorization `R₀=ψ(z−r)`, existence plus the degree exclusion gives exactly `m₀=1`. The depth-one linear residual likewise has `m₁=1`.

4. **VERIFIED — foundational architecture L1–L7.**  
   L1 and L5 are honestly marked as the two open kernel components. L2’s choice-free branch tree correctly replaces the refuted single-run ontology. L3 consistently retains the side data required later and uses `(NA)+(HV)+(SAE)` only as a data-level realizability predicate. L4’s common-frame induction is correctly conditional on L1. D4R.0 is accurately stated as an open digit-cell partition, not a node-stratum partition. L6 and L7 correctly distinguish prefix membership from terminal maximal branches and consume no termination assertion.

5. **VERIFIED — mass and pool chain L8–L11.**  
   L8 establishes finiteness using digit tuples plus factor-selection indices. L9 correctly makes the fresh-volume exponent shape-constant. D4R.1-SUM follows by summing Theorem C(b) over branch-indexed histories, with the empty prefix separately and correctly handled. D4R.1-EV is exactly the corresponding union bound. L11’s enlarged encoding is injective, its sharper bound  
   `m̄_r = ⌊deg R_anch,r/(g_rμ_r)⌋`  
   is correct, and D4R.2 consistently carries the necessary p-free factor `M(P̂)`.

6. **VERIFIED — finite-level law L12.**  
   The empty-`Pref(P̂)` case avoids `max ∅`; the empty shape is separately handled outside Theorem C(b); and the nonempty case gives the exact branch-weighted count and event bound at every common sufficient level. No quotient, representative choice, disjointness, or terminal-completion assumption re-enters.

7. **VERIFIED — perimeter, gate, and records.**  
   D4-R.5 consistently distinguishes the open kernel, conditional results, withdrawn rev-5 claims, and deferred aggregation questions. The `(SAE)` forward reference is used consistently as an additional node-data predicate and is not represented as proved inside this note. All D4-n3 ledger values, counts, levels, `M(P̂*)=1`, and multiplicity-one conclusions recompute correctly. The historical pass records accurately distinguish the former rev-5 acceptance, its puncture, the rev-6 rebuild, pass 7’s two display gaps, and the two rev-7 repairs now present in the live text.

Counts:

- Critical errors: **0**
- Justification/display gaps: **0**

Verdict: **ACCEPTED (0/0)**
