# FABLE fresh-instance verification — D4R0K_DERIVATION rev 3 (2026-07-27)

Verifier: fresh Fable instance (dual bar; Codex pass 3 read but not deferred to).
Object: D4R0K_DERIVATION_2026-07-26.md rev 3 (1262 lines) — Theorem A (offset-P
lift totality, 8 boundary cases) + Structural Theorem B (joint read datum:
totality/exclusivity via grammar G0–G7+G3′, nine-clause complement; verdicts
excluded = D4R.0-K(c)). Sources checked live: MOVES_2026-07-24.md,
ASSEMBLY_PLAN_2026-07-26.md (VP at plan lines 1391–1434, REV 10).

## D1. Quote-and-classify sweep (duty a)
(appended below)

## D2. Adversarial counterexample attempt vs Lemma W (duty a, hardest)
(appended below)

## D3. Theorem A boundary B5, independently re-derived (duty b)
(appended below)

## D4. S5 cite register vs live MOVES (duty c)
(appended below)

## D5. (c1)–(c3)/VP alignment vs plan [3t] (duty d)
(appended below)

## D6. Fresh sweep at own emphasis (duty e) + counts + verdict
(appended below)

---
## D1 (appended). Quote-and-classify

F1. GAP (declared residual, owed at [3t]) — S2.6 (c3-b): "τ is a STOPPING
  RULE ... determined by the capped cell datum at some finite height" is
  POINTWISE, but the conclusion "at every cap at or beyond its detection
  height, a finite UNION of pure digit cylinders" presumes a PER-CELL
  (uniform) detection height. Same as Codex pass-3 finding 7; the plan's
  REV 10 VP-3 now states the uniform cap and owns the proof at [3t]. Not a
  defect of Theorems A/B (verdicts are outside B's value set).
F2. GAP-minor — S5 [c-free]: the register quote ("determined coordinates
  are exactly its floor downset {ht ≤ F_k} plus DIG_k's lead level sets")
  is NO LONGER the live text: MOVES ~4083 now has rev-13 SPECIES-CORRECT
  TOPS (strict "< F_k" at value-carrying rim indices). The claim Lemma W
  consumes — factor-interior coordinates strictly above the floor are
  FREE — SURVIVES the edit (only rim indices changed); register stale.
F3. NOTE — S5 [c-term]: rev 12 inserted a clause mid-quote (live 4135–40:
  "...any interleaving and depth) — all REALIZABLE in C.0's
  (NA)+(HV)+(SAE) sense: ... [rev 12] — any admissible terminal system").
  Quote no longer contiguous-greppable; supported claim (verdicts a
  parameter of §C) intact — indeed reinforced.
F4. NOTE — S7's "DISCREPANCY FLAGGED: the plan's VP-3 still glosses (c3)
  as 'automatic'" is now STALE: plan REV 10 fixed the gloss (split c3-a/
  c3-b, uniform cap, credited to D4R0K). The flag did its job; harmless.
F5. NOTE — S2.5 G2 does not spell out interior on-lattice slots whose
  line height is UNATTAINABLE at depth ≥ 2 (proper value sets). The cited
  D.3(e) rev-D¹¹c supplies it: 𝔸(γ) = {0} there ("automatic at
  unattainable heights, where no slot ever minimizes"), so d_j = 0 is the
  only grammatical entry and its value clause is automatic; nonzero
  entries die at ¬G2 with rev-D¹¹d's "an INADMISSIBLE pin makes the locus
  EMPTY" (live ~2698) as the emptiness witness. Consistent both
  directions; wording could name the {0}-alphabet case explicitly.
F6. NOTE — B(iii)'s "verdict cell" must be read conditionally on a
  supplied τ (pass-3 caveat 9 concurred; verdicts excluded from B).
No CRITICALs found. Theorem A chain, Theorem B case tree, Lemmas W/E,
S2.1 refutation arithmetic, S2.3/S2.3b displays: all verified (D2–D6).

## D2 (appended). Counterexample attempt: grammatical ⟹ realized

Charge: build an abstract G0–G7+G3′ datum with empty stratum. Vectors:
V1 (shared-vertex digit conflict — pass-2's class, re-probed with THREE
  sides / two junctions and with off-alphabet common values): dies at
  G3′ + G2 — one coefficient, one height (G3), one digit (G3′), one
  alphabet membership; no residual freedom found.
V2 (attainable-alphabet/integrality, my main attempt): depth-≥ 2 side
  whose INTERIOR on-lattice slot has line height OFF the slot's value
  set 𝒲 (possible: D.9's value sets are proper at depth ≥ 2, cf. B1's
  parenthetical). Nonzero d_j there → ¬G2 (𝔸 = {0}, D.3(e) rev-D¹¹c) —
  correctly EMPTY via rev-D¹¹d. Zero d_j there → grammatical, and the
  value clause is AUTOMATIC (no weight ever equals that height), so Lemma
  W's truncations keep positive mass: REALIZED. Endpoints need attainable
  heights (nonzero digits), forced by G2. No empty grammatical datum.
  Vertex-height integrality: forced by G0's e′|w′ + G3 chaining from
  old(μ) ∈ ℤ — a non-integral shared height cannot pass G3. Dead end.
V3 ((HV) lead conflict): prescribe rightmost lead ≠ vtx(ν_k), or vtx ∉
  alphabet at (μ, old(μ)): first is ¬G5; second impossible on a REALIZED
  Σ_k (the (VERTEX) pin is attained by membership). Dead end.
OUTCOME: attempt FAILED — every candidate dies at a named clause or
realizes via Step 2's positive-mass nested truncations. Lemma W's
grammar, as tightened at rev 3, appears exactly right; concur with
pass 3 finding 1.

## D3 (appended). B5 re-derived independently

Setting re-built from D.5/D.10/D.9(d) (all verified live, [c-rs]): depth r
born by an increment (e,h,g), w(Φ_r) = ehg; recentered ℓ ≥ 1 times, key
weight updating to the consumed side value; TRANS-RS keeps C_r, the read
index (e,h,g), and the realizers UNCHANGED (live 2676: "S6: the same 𝒜
and realizers"); one scale (v = v′, e_read = 1).
Ladder: λ⁽¹⁾ > ehg by D.9(d) at the first recentering (current key Φ_r);
λ⁽ⁱ⁾ > λ⁽ⁱ⁻¹⁾ by D.9(d) at the i-th (current key weight = λ⁽ⁱ⁻¹⁾). ✓
Chain, my own run: eligibility gives W > λ⁽ℓ⁾. j₀+ek ≤ (e−1)+e(g−1) =
eg−1, so W_k = (W−(j₀+ek)h)/e ≥ (W−(eg−1)h)/e > (λ⁽ℓ⁾−(eg−1)h)/e ≥
(λ⁽¹⁾−(eg−1)h)/e > (ehg−(eg−1)h)/e = h/e (arithmetic: ehg−egh+h = h ✓)
> w_{r−2}(Φ_{r−1}) by (I-aug) at the creating increment read. MATCHES
S1.4 (B5) link for link (its "λ⁽ℓ⁾ > λ⁽¹⁾" strict at ℓ > 1; mine keeps ≥
to cover ℓ = 1 in one line — same content). Final-link fine print
checked: if Φ_{r−1} was itself recentered before the increment read,
(I-aug) compares against the CURRENT key's weight, which IS C_{r−1}'s
clean threshold per S1.1's definition — the induction hypothesis is the
literal one, no unit mixing (v = v′; W_k's /e is the D.0 scale step,
untouched by recenterings). Integrality of W_k: (F-1)/(F-2) run with the
ORIGINAL (e,h) — unchanged by TRANS-RS, so j₀ ≡ h⁻¹W (mod e) still gives
e | (W−(j₀+ek)h). Recursion depth: recenterings add none (same C_r,
same Φ_{r−1}-slots). VERDICT: B5 CONFIRMED, no gap found.

## D4 (appended). Cite register, all 11 cites vs live MOVES

Verbatim + claim OK (line drift only, as the register itself caveats):
[c-noineq] live 3420–22 (numbers even match) · [c-cut] all three quotes
found (3911–13; 4036–39; 4044–47 — the "additive ... kernel cosets"
sentence line-wraps, grep needs a fragment) · [c-floor] 3750–52 ·
[c-perpath] 3436 exact; "applies to each lift-carrying history
separately" 3494–95; §D4-R reading 4634–40 · [c-hull] 2240–43 exact ·
[c-rs] 2245–48, 2668–77 (S6 at 2676), 2586–88 all exact · [c-sae]
3523–34, 3544–46, 4715 — near-exact numbers · [c-multiside] hull-side
read 3616 ff.; (NA)/(HV) 3519–22 with one trivial live insertion
("j* :=") · S2.3b's FC8 quote verbatim at 5324–30 (register said
5271–80; drift) — and FC8's own scope sentence ("Deeper structure of
non-target lumps is other prefixes' business") confirms S2.3b's lump
reading. DEFECTS: [c-free] materially edited at rev 12/13 → F2
(GAP-minor, claim survives); [c-term] mid-quote insertion → F3 (NOTE).
Each cite's MATHEMATICAL use re-checked at its consumption site: the
(NA)/(HV) re-derivation in S2.1, Lemma W Step 2's free/cut/no-ineq
chain, B5's package facts, G4's ordering convention — all supported by
the live text as it now stands.

## D5 (appended). (c1)–(c3)/VP vs plan [3t]

Plan VP (lines 1391–1434, REV 10) vs S2.6: (c1) ↔ VP-1 (alphabet:
multisets of (E,F), Σ E·F = d) + VP-2 (τ = (τ-irr)/(τ-ns)/(τ-hen), cell
data only, branch-level attachment) — MATCH, including the two
attachment levels ((τ-irr) branches; (τ-ns) track halt) and the derived
cell outcome. (c2) closed domain ↔ VP-2 pins root Hensel leaves IN
scope ((τ-hen)) + VP-3 duty "mutually exclusive AND covering" — MATCH;
the leaf-rule refutation resolved by T_can^τ pruning, consistent with
S2.6's D4-n3 record. (c3-a) automatic — both sides agree. (c3-b): plan
REV 10 carries the pass-3 UNIFORM sharpening verbatim ("admits a
UNIFORM detection cap N_V(cell data) ... pointwise stopping alone
insufficient"), owner [3t], census layers named as consumers — the
residual is correctly owed there; the derivation's own (c3-b) sentence
remains pointwise-worded (F1) and S7's discrepancy flag is stale (F4).
ALIGNMENT: CONFIRMED modulo F1/F4 (no contradiction between the two
documents as they now stand; the plan is the stronger, current side).

## D6 (appended). Fresh sweep + counts + verdict

Own-emphasis checks, all PASS:
- S2.1 refutation arithmetic re-run: v₅(20,75,375,2500) = (1,2,3,4) ✓;
  (z−1)²(z−2)² ≡ z⁴+4z³+3z²+3z+4 (mod 5) ✓; R₀(1) = 15 ≡ 0, R₀(2) = 70
  ≡ 0 ✓; digits (4,3,3,4) read at heights (1,2,3,4) ✓.
- S2.3b target region: {V₀ = 3, V₁ ≥ 2} + cluster forces the single
  full-span slope-1 hull ✓; FC8 cell equations = capped-𝒟 fiber
  equations ✓; the 20-lump count (24 − 4) ✓ against live 5330–34.
- Two-sided instance: (1,1) is a hull vertex (chord height 8/3 > 1) ✓;
  it lives in the ROOT f̄ = x³ track (old ≡ 0), where v(c₁) = 1 is in
  the box — consistent with S2.2's ROOT INSTANCE, not the deeper
  normalized cell.
- Theorem A internals: (F-2) integrality (t ≡ h⁻¹ mod e from Bézout) ✓;
  Step 0 species (ii): h(g−k) ≥ h > e·w(Φ_r) ≥ w(Φ_r) ✓; B1's h+1
  margin ✓; B6's Bézout normalization (s,t) = (1,0) ✓; well-foundedness
  (stage index strictly decreases, recenterings add no depth) ✓.
- Theorem B(ii): (NA) via convexity ✓; (SAE) via affineness of
  new_S − old on the span (old is a LINE on the window — D.8 box; ≡ 0 at
  the root) with (BOX)/(VERTEX) endpoint signs ✓ — genuinely derived,
  not assumed. (HV) via (VERTEX)+(TRANSPORT) ✓.
- Lemma E's emptiness semantics: ¬G0/¬G3/¬G4 clauses are
  "value-never-attained ⟹ fiber empty" — sound for fibers of a total
  function; ¬G2/¬G3′/¬G5/¬G7 additionally have direct condition-set
  emptiness ✓. Nine-conjunct De Morgan bijection re-counted ✓.
- Leaf T: direct witness sound (fresh zeros at free coordinates, p^{−1}
  cuts, nested clopen truncations in a compact box) ✓; R0: per-track
  claims only, joint witness honestly external (MONTES L1, O4) ✓.
- mass(Σ_k) > 0 for realized prefixes: consumed from accepted §C
  (Theorem C's positive per-path mass) — legitimate given the dual-
  accepted §C; noted, not a gap.

COUNTS: 0 CRITICAL · 2 GAP (F1 declared/owed at [3t]; F2 minor register
staleness) · 4 NOTES (F3–F6). Codex pass-3 comparison: independently
reproduced its finding 7 (= F1) before reading its details; no finding
of mine contradicts its verdict; my F2/F3 (live-text drift of [c-free]/
[c-term]) are new but minor.

VERDICT: CLEAN — Theorems A and B adoptable at their stated structural
strength, with the declared open residuals (D4R.0-K(c)/VP-3 at [3t],
O2–O4) and the register touch-ups F2/F3 recommended at the next rev.
