# JOINT-D1 — the class-3 descend bijection (J1-step) and the joint ledger, with proofs
*(2026-07-23. Closes the note's OPEN-D1 for class-3 descend towers at deg-2 keys, order-1
reads, both parities — the mechanism the whole general-n program was gated on. Empirical
pins: Case E (`case_e_gate.py`, D1-marginal), Case F (`case_f_gate.py`, p = 2, N = 5,
exact joint uniformity), Case F-odd (`case_f_oddp.py`, p = 3, m = 1 and 2, sign −, exact
joint uniformity, 81/81 buckets equal at every center). Status: DRAFT pending Codex
adversarial passes. Consumed by: the general-n formalization blueprint (W9, forthcoming).)*

## 0. Setting and conventions

p any prime; Q := p². φ = x² + sx + t ∈ ℤ_p[x] monic with φ̄ irreducible over F_p
(irreducibility used only to make F_Q := F_p[x]/(φ̄) a field; the bijection algebra never
uses it). Every monic quartic f has the unique φ-adic development f = φ² + a₁φ + a₀ with
deg aᵢ ≤ 1; identify a deg-≤1 polynomial u₁x + u₀ with the coordinate pair (u₁, u₀) ∈ ℤ_p².
The **height-h digit** of a pair is its coefficientwise p-adic digit pair, read as the
element of F_Q via the basis (x̄, 1); the **Gauss valuation** v(a) = min(v_p(u₁), v_p(u₀)).
D1m (proved: blueprint W8 §1 + Lean `OM/DevBijection.lean`): (c₃,…,c₀) ↦ (a₁, a₀) is
affine ℤ[s,t]-unitriangular, hence exact-count on every digit cylinder.

**The descend stratum** at slope m ≥ 1 with center c̃ ∈ F_Qˣ, inside the root-cluster
ambient A := {digit₀(a₁) = digit₀(a₀) = 0}:

  S_{m,c̃} := { (a₁, a₀) : v(a₀) ≥ 2m, digit_{2m}(a₀) = c̃²,
                v(a₁) ≥ m,  digit_m(a₁)  = −2c̃ }.

(At p = 2, −2c̃ = 0, recovering the char-2 "square residual ⟺ v(a₁) ≥ m+1" form.) The
residual polynomial of the slope-−m side is y² + digit_m(a₁)·y + digit_{2m}(a₀) =
(y − c̃)² exactly on S_{m,c̃} — the μ = 2 descend read.

**The child key** (the corrected sign, §5.1 of blueprint W8): ĉ := the minimal-support
lift of c̃ (deg ≤ 1, coefficients Teichmüller/minimal — any FIXED lift with ĉ̄ = c̃ works
for everything below), and

  φ' := φ − p^m·ĉ .

φ' is again monic quadratic, φ̄' = φ̄. Re-develop: f = φ'² + a₁'φ' + a₀', deg aᵢ' ≤ 1.

## 1. Lemma J1a (the descend re-development map, explicitly)

Write ĉ = ĉ₁x + ĉ₀, a₁ = a₁₁x + a₁₀, φ' = x² + s'x + t' with s' = s − p^mĉ₁,
t' = t − p^mĉ₀. Substituting φ = φ' + p^mĉ into f = φ² + a₁φ + a₀ and reducing the two
deg-2 products by the monic φ' (single division steps: for deg-≤1 u, w one has
u·w = (u₁w₁)·φ' + [(u₁w₀ + u₀w₁ − u₁w₁s')x + (u₀w₀ − u₁w₁t')]):

  Θ_ĉ(a₁, a₀) = (a₁', a₀') with
  a₁'₁ = a₁₁ + 2p^mĉ₁
  a₁'₀ = a₁₀ + 2p^mĉ₀ + p^m·ĉ₁a₁₁ + p^{2m}·ĉ₁²
  a₀'₁ = a₀₁ + p^m[ĉ₁a₁₀ + ĉ₀a₁₁ − ĉ₁a₁₁s'] + p^{2m}[2ĉ₁ĉ₀ − ĉ₁²s']
  a₀'₀ = a₀₀ + p^m[ĉ₀a₁₀ − ĉ₁a₁₁t'] + p^{2m}[ĉ₀² − ĉ₁²t']

Θ_ĉ is affine over ℤ[s,t,ĉ₁,ĉ₀] with lower-unitriangular linear part in the order
(a₁₁, a₁₀, a₀₁, a₀₀), and **Θ_ĉ ≡ id mod p^m** (every non-identity term carries p^m).
Hence Θ_ĉ is a bijection of ℤ_p⁴ and of (ℤ/p^N)⁴ for every N, commuting with reduction.

*Proof.* The substitution identity and the two reduction steps are the displayed algebra;
(φ' + p^mĉ)² = φ'² + 2p^mĉφ' + p^{2m}ĉ², a₁(φ' + p^mĉ) = a₁φ' + p^mĉa₁, and the two
products ĉ², ĉa₁ reduce by the displayed single-step formula. Triangularity and the p^m
factor are visible; unitriangular ⟹ determinant 1 ⟹ bijectivity at every level;
ℤ-polynomial coefficients ⟹ reduction-compatibility. ∎

*(Machine anchor: the four formulas satisfy the defining identity f = φ'² + a₁'φ' + a₀'
identically — checkable by `ring`; empirically exercised by the three census gates.)*

## 2. Lemma J1b (staircase inclusion: the child lands in the child cluster box)

On S_{m,c̃}: v(a₁') ≥ m + 1 and v(a₀') ≥ 2m + 1.

*Proof.* Heights < m of a₁' equal those of a₁ (Θ ≡ id mod p^m), all zero. Height m:
digit_m(a₁') = digit_m(a₁) + 2c̃ + [digit₀(ĉ₁a₁₁-term)·p^{m}-shift: v(a₁₁) ≥ m gives that
term v ≥ 2m ≥ m+1, invisible at height m] = −2c̃ + 2c̃ = 0. For a₀': heights < 2m get
contributions only from a₀ (zero: v(a₀) ≥ 2m) and from p^m·(ĉa₁-remainder) whose Gauss
valuation is ≥ m + v(a₁) ≥ 2m, and p^{2m}-terms — all invisible below 2m. Height 2m, read
in F_Q (the remainder-mod-φ' reduction at the residue level is multiplication in F_Q since
φ̄' = φ̄): digit_{2m}(a₀') = digit_{2m}(a₀) + c̃·digit_m(a₁) + c̃² = c̃² − 2c̃² + c̃² = 0. ∎

## 3. Theorem J1-step (the descend bijection)

Θ_ĉ restricts to a BIJECTION

  S_{m,c̃}  ⟶  B' := { (a₁', a₀') : v(a₁') ≥ m+1, v(a₀') ≥ 2m+1 }

at every finite level N ≥ 2m + 2 (and hence over ℤ_p).

*Proof.* Inclusion is J1b. Injectivity is J1a. Cardinalities at level N: S_{m,c̃} pins
heights 0..m of a₁ (m+1 digits) and 0..2m of a₀ (2m+1 digits), leaving
Q^{N−1−m}·Q^{N−1−2m}; B' pins heights 0..m of a₁' and 0..2m of a₀' to zero — the same
count. A cardinality-preserving injection between equinumerous finite sets is onto. ∎

**Corollary J1-uniform.** The child digit vector (the heights ≥ m+1 of a₁' and ≥ 2m+1 of
a₀', any window) is EXACTLY uniform on S_{m,c̃}, and any child digit condition C' has
joint count #(S_{m,c̃} ∩ Θ⁻¹C') = vol(S_{m,c̃})·vol(C')·(normalizations as displayed) —
the depth-1 joint ledger. *(This is the statement Cases F / F-odd verify bucket-by-bucket.)*

## 4. Theorem JOINT-D1 (constant-deg-2-key descend towers)

Fix a history shape: slopes (m₁, …, m_k), all ≥ 1, and a terminating leaf condition; fix
history values (c̃₁, …, c̃_k) ∈ (F_Qˣ)^k. Define keys φ⁽⁰⁾ = φ, φ⁽ʲ⁾ = φ⁽ʲ⁻¹⁾ − p^{M_j}ĉ_j
with M_j := m₁ + ⋯ + m_j, and strata S⁽ʲ⁾ recursively: S⁽ʲ⁾ = the slope-m_j center-c̃_j
descend stratum in the j-th child frame (heights shifted by (M_{j−1} + …) as produced by
J1-step at step j−1 — precisely: after step j−1, the coordinates (a₁⁽ʲ⁻¹⁾, a₀⁽ʲ⁻¹⁾) range
over the box {v ≥ M_{j−1}+j−1-type bounds} onto which the previous Θ mapped, wait — state
it cleanly by INDUCTION: at each step, J1-step applies verbatim to the current pair
RELATIVE to its cluster frame, because B' of step j−1 is an affine digit box of exactly
the ambient form A requires after the height shift (v(a₁) ≥ m+1, v(a₀) ≥ 2m+1 play the
role of the next read's "cluster" bounds, with the next read's polygon computed at
heights ≥ these; the slope-m_j read of step j imposes digit conditions at heights
m_j-relative to the frame, all ABOVE the pinned block). Then:

  #{ f mod p^N in the joint stratum } = ∏_{j} (nominal digit-volume of read j)
                                        × (nominal volume of the leaf condition),

for every N above the total budget — the class-3 joint ledger, with D1-marginal (D1m)
supplying the first coordinate change from f to (a₁, a₀).

*Proof.* Induction on k. Base: D1m. Step: the composite of the first j−1 coordinate
changes is an affine unitriangular bijection (J1a composed; compositions of unitriangular
affine maps with ℤ_p coefficients are such) under which the first j−1 reads' conditions
correspond to a pinned digit block and the remaining freedom is exactly the box B'⁽ʲ⁻¹⁾
(J1-step at step j−1, onto). Read j's conditions and the eventual leaf conditions are
digit conditions on the CURRENT pair at heights inside the fresh block; J1-step at step j
converts them to the next box. Multiplying the per-step pinned-digit volumes gives the
product formula. ∎

**Remark (what "frame" means; no rescale).** Unlike the deg-1-key staircase (Lemma 2.3),
Θ_ĉ involves NO rescale — heights are preserved, the pinned blocks accumulate upward, and
the "frame shift" is pure bookkeeping of which heights are already pinned. This is why the
deg-2-key tower is, if anything, CLEANER than the n = 2 case already formalized.

## 5. Scope, and what remains open after this note

PROVED here: D1-marginal (imported from W8 §1) + J1-step + JOINT-D1 for class-3 towers
whose every descend has deg ψ = 1 (residual a square of a linear factor) at deg-2 keys —
i.e. the note's class-3 "self-loop-like" histories, both parities, all slopes, all depths.
By the note §3's reduction, D4 (the class-3 prefix bound) follows from this joint form
(the (R1)/(R2) route). Base-change remark: everything is verbatim for deg-f keys with
Q = p^{2}→p^{f·(…)} where the digit alphabet is F_Q — the algebra of J1a/J1b uses only
"monic key, residue field F_Q, minimal lifts", not deg φ = 2; the Lean phase-1 target
stays at deg 2 for concreteness with the general statement recorded.

STILL OPEN (named, next):
- **Order-r ≥ 2 reads** (deg ψ ≥ 2 residual factors: the child key jumps degree, deg φ' =
  m_φ·e·f_ψ, and subsequent reads use φ'-adic developments whose DIGITS are themselves
  φ-adic developments — the biadic ledger). Mechanism expectation: the SAME two move
  types (monic-division development = D1m-type; key recentering = J1-type) generate the
  order-r apparatus, so the ledger should follow by the same induction one level up. To
  be proved next, with a Case-G census (n = 4, the φ' = deg-4 key order-2 read) as the
  gate BEFORE the proof is trusted.
- **D3/D5** (deg-1-key partial-side and mid-polygon descents, n ≥ 3): these involve the
  RESCALE mechanism (x ↦ p^m(y + ĉ)) at sides that do not span the whole window — a
  genuinely different failure surface from J1 (see the Remark in §4). Attack with n = 3
  censuses at the exact configurations (partial side: polygon with a vertex splitting the
  window; mid-polygon: a full side flanked by others), diagnose, then prove.
- D2: Lean vocabulary only (extension-field pool counts).
