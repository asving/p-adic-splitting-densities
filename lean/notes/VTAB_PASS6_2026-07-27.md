1. **Status sweep — clean.** Current-status lines consistently report: §T-ASSEMBLY “DUAL-ACCEPTED”; §M-SPECIES “rev 4, acceptance pending”; V-n3 “PASSED … 178 exact checks, 0 failures”; the plan remains SOUND with its stated conditional perimeter; and the HMC probe is “specced-and-pinned … NOT yet run, no result claimed.” Earlier “AWAITS pass” and “launched” statements occur only inside dated, append-only historical pass records and are clearly superseded by later records.

2. **V.6.5 must-name audit — complete.**

   - “the XHD tag ([1v]-FULL pending [2b])” appears verbatim.
   - XHD-w/-d/-s, XHD-u, and ENT-U are classified OPEN with owner [2b].
   - “(U-A) PROVED here” is named.
   - HMC is an OPEN kernel; its probe is correctly recorded as not run.
   - ENT-COUNT and INIT-RAT are named burdens; INIT-RAT is explicitly conditional on XHD + ENT-U.
   - Computed coverage is correctly limited to “THREE cells’ lists”: s₀/α₀, s₁/α₁, and s_blk2/β₀.
   - The remaining full (D1)–(D4) coverage is correctly owned by this note’s pending [1v]-COND/CL-13 duty, resting on pending [2a] SP-FIN/SP-OUT.
   - V-n3’s A-side and B-side are both named as PASSED, support only.
   - VALUE-Fq/VALUE-p remain standing.
   - The probe roster is evidence-only.
   - The [3t] marked→per-f ownership fence is retained.

3. **Hypothesis-ledger sweep — clean.** The text consistently separates XHD-free syntax/count duties, XHD-conditional measured and resummed duties, the open HMC kernel, ENT-COUNT/ENT-U/INIT-RAT burdens, per-instance ACTIVE-VALUE checks, and ESCAPE(E0). VAL(a) is XHD-free while VAL(b)’s chain identification carries the XHD-u tag. COMP-h, COMP-hΣ, COMP-Σ, and COMP-AGG have the correct progressively stronger hypotheses.

4. **Typing and composition sweep — clean.** Entrance heights occur only as the projection h_ent(ε) of a concrete ε; the template-instantiation bijection ranges them exactly once. Path heights are separately summed exactly once. Marked-space measurability and multiplicity are preserved throughout. The terminal-final HMC probe step is correctly typed by the absorbing exit object [o₂], without an illicit continuing target-cell map.

5. **V.6 numerical audit — clean.**

   - A1’s five cubic-factorization counts sum identically to q³, and all nine displayed evaluation columns are correct.
   - A2’s domain is correctly parameterized by  
     \(h_2=t\ge1,\ h_0=3t+2u+1\), giving \(L((4,1);\{(3,1),(2,0)\})\).  
     Its table is \((q-1)^2\), and  
     \(E=(3(h_0+h_2)-1)/2\), hence \(E(4,1)=7\).
   - The size-2 block counts
     \((q-1)(q-2)/2,\ q(q-1)/2,\ q-1\)
     sum to \(q(q-1)\), including characteristic 2; all displayed evaluations and the auxiliary \((q-1)\), \((q-1)^2\) rows are correct.
   - Gate B has \(I^{ent}(2)=2\), \(\iota=2\cdot2^{-3}=1/4\), \(T=1\), \(g=2^{-3}\), hence \(\widehat\mu=2^{-5}=1/32\). The residue counts are \(2^{9}/32=16\) at \(N=3\) and \(2^{12}/32=128\) at \(N=4\), with count shadow 2.
   - The two-history attribution is explicit and consumes only the separately checked fixed-history (U-A) face, not XHD-u.

6. **End-to-end acceptance sweep — no defect found.** The disjoint XHD-d convention, marked-space MEAS typing, ε-unification, countable PART-1 route, VAL(b) conditionality, exit-object probe typing, ownership fences, gate governance, and consolidated perimeter remain mutually consistent.

**Counts:** 0 critical findings; 0 record gaps.

**Verdict: ACCEPTED (0/0).**
