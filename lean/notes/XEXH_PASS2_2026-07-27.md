1. Pass-1 repair audit

   1. Finding 1 — GENUINE. Rev 2 now says T3/T4 are “NOT-CERT” and that zero means “no positive per-side bound certified,” never actual zero gain. The unused `d_gen^sem` causes no leakage.

   2. Finding 2 — GENUINE. The definition
      > “d_cert(H) := #{continuing nodes … rows T1, T2 … whose ALIGN sub-clause … is DISCHARGED}”
      
      is coherent. With neither ALIGN clause discharged, `d_cert ≡ 0`; every theorem-level display respects this. The raw `CUM` gate is explicitly a conjectural ALIGN test, not a use of positive certified depth.

   3. Finding 3 — GENUINE. From the displayed classifications,
      \[
      \#\mathrm{increments}\le v_p(\disc f)/2+\lfloor\log_2n\rfloor,
      \]
      while WEIGHT-CHARGE gives
      \[
      \#\mathrm{recenterings}\le s(n)v_p(\disc f).
      \]
      Thus
      \[
      d_{\rm total}\le (s(n)+1/2)v_p(\disc f)+\lfloor\log_2n\rfloor,
      \]
      yielding exactly
      \[
      v_p(\disc f)\ge
      \frac{d_{\rm total}-\lfloor\log_2n\rfloor}{s(n)+1/2}.
      \]
      The additive T3 term is no longer improperly absorbed.

   4. Finding 4 — GENUINE. For T2,
      \[
      \operatorname{ind}(S)=\frac{eh-e-h+1}{2}
      =\frac{(e-1)(h-1)}2.
      \]
      The point \((s+1,u+1)\) is strictly below the side precisely when
      \((e-1)(h-1)>1\); the sole equality obstruction \((e,h)=(2,2)\) is excluded by coprimality.

   5. Finding 5 — GENUINE. T0 is now an opening preceding the root read; ROOT-tag letters remain genuine reads classified by T1–T5.

   6. Finding 6 — GENUINE. The corrected bound
      \[
      \sum h_r\le(d_{\rm total}+1)\max h
      \]
      accounts for the possible terminal read and handles `d_total = 0`.

   7. Finding 7 — GENUINE. `(X2-AFF)` is explicitly OPEN; neither its constant nor its existence is claimed derived.

   8. Finding 8 — PARTIAL. The missing inclusion is now honestly fenced behind `(X2-BRIDGE)`, with the existential conversion and cap duty displayed. But the uniform-cap component conflicts with the supplied TB-CAP record; see Finding 2 below.

   9. Findings 9–10 — CORRECT as retained. The formulas have been consistently rebased on the affine depth reduction and corrected read count.

   10. Finding 11 — GENUINE as to the pass-1 defect. Every relevant tag now uses
       > “(X1a-ALIGN) ∨ ((ALIGN-inc) ∧ X.1b)”
       
       and no longer claims X.1b alone controls increments. A separate defect remains in the first disjunct; see Finding 3 below.

   11. Finding 12 — GENUINE. Finite child menus plus absence of an infinite branch permit the stated König contrapositive, hence tree-finiteness.

   12. Finding 13 — GENUINE. The series identity now inherits the precise `(HEN-LIFT)/(OM-SAT)` VP-SOUND obligations.

   13. Findings 14, 15 and 17 — CORRECT as retained. The elementary tail, NS typing, and probe scope are consistently stated.

   14. Finding 16 — GENUINE. The gate now defines a legitimate prefix quantity:
       \[
       \mathrm{CUM}(j)=\sum_{i\le j}\operatorname{ind}(S_{\nu_i}).
       \]
       The row values recompute correctly:
       \[
       \mathrm{T1}=\tfrac12\ell(eh\ell-e-h+1),\qquad
       \mathrm{T2}=\tfrac12(e-1)(h-1),\qquad
       \mathrm{T3/T4}=0.
       \]
       The numerical `(3b)` legs and the displayed `env*` substitution are arithmetically consistent.

2. CRITICAL — `(X2-BRIDGE)` contains a cap duty contradicted by its cited perimeter

   Rev 2 requires
   > “a UNIFORM cap bound cap(H) ≤ c_cap(n)”

   and says only that τ-irr uniformity “is not displayed.” But the pasted TB-CAP record states more strongly:

   > “No cap uniform over ALL cells of a verdict value exists or is claimed — heights grow along deeper shapes.”

   Therefore this is not merely an omitted proof of an otherwise perimeter-supported assertion. The proposed absolute uniform cap conflicts with the source record. The existential conversion is well-posed, and the four-way decomposition is a meaningful open duty, but the bridge as displayed cannot currently be the claimed route to
   \[
   \threshold(H)>N-c_{\rm cap}(n).
   \]
   Finding 8 is consequently only partially repaired.

3. CRITICAL — the first X.3(a) termination route exceeds `(X1a-ALIGN)`

   X.3(a1) claims:

   > “(X1a-ALIGN) … event/step correspondence is exactly (X1a-ALIGN)’s content”

   and concludes that every branch is finite. But `(X1a-ALIGN)` only transports the table’s certified per-order index contributions and prevents double-counting. Even after both subclauses are discharged, `(CD)` counts T1/T2 nodes; T3 is separately degree-bounded, while every T4 node remains permanently NOT-CERT with zero certified side contribution.

   Consequently `(X1a-ALIGN)` as actually stated supplies no bound on the number of T4 recenterings. The note itself assigns that population to WEIGHT-CHARGE. Invoking GMN’s step bound requires an additional correspondence showing that these zero-side-bound MOVES recenterings are controlled by GMN’s index-step accounting. That assertion is neither part of the displayed ALIGN duties nor derived elsewhere.

   Thus the disjunct
   \[
   (X1a\text{-ALIGN})\Longrightarrow\text{no infinite branches}
   \]
   is unsupported. The alternative `(ALIGN-inc) ∧ X.1b` route is internally coherent, but it does not validate the advertised disjunction.

4. Gate assessment

   The TOOTHLESS declaration is honest. Since `env*(N)>1` throughout the census range, layer (iii) cannot test the assembled quantitative envelope there. Nevertheless the gate is not vacuous:

   - layer (i) can falsify the raw ALIGN transport;
   - the recentering/discriminant ratio can falsify the sealed `s(3)=2`;
   - the per-letter tests can falsify the sealed tail candidates;
   - the `(3b)` census checks can detect a theorem transcription or implementation failure.

   It is therefore a meaningful multi-layer gate, but not an empirical validation of the quantitative envelope at \(N\le8\).

5. End-to-end verdict

   The certified-depth repair, index arithmetic, affine depth-to-discriminant reduction, T0 typing, König bridge, VP-SOUND typing, and rebuilt gate survive the sweep. The quantitative route remains honestly conditional on open kernels, but `(X2-BRIDGE)` presently asks for a cap contradicted by TB-CAP, and qualitative X.3 still overclaims an ALIGN-only termination route.

Counts: 2 criticals / 0 gaps.

Verdict: NOT ACCEPTED.
