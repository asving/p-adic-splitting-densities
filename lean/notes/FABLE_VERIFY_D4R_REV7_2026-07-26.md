# FABLE FRESH-INSTANCE VERIFICATION of §D4-R rev 7 (2026-07-26)

Verifier: fresh Fable instance (dual bar, second Fable pass — successor
to FABLE_VERIFY_D4R_2026-07-26.md, whose F1 punctured rev 5). Target:
live §D4-R rev 7, MOVES_2026-07-24.md lines 4548–5363. Codex passes 8+9
CLEAN on this text — no deference. Gate D4-m2 seal read
(verification/CASE_D4M2_SEALED_PREDICTIONS.md, 33/33 PASS).

## Duty (a): does the rebuild answer F1/F2/F3? (quotes + verdicts)

### (a) F1 — GENUINELY ANSWERED. The rebuild meets the refutation head-on.
Damage-chain coverage, item by item (predecessor's (i)-(v)):
 (i) run undefined -> L2 rewritten: "the rev-5 unique maximal RUN
   H_can^max does not exist — at F1's instance two same-shape factors
   are simultaneously eligible"; replaced by "a UNIQUE maximal canonical
   BRANCH TREE T_can(f)", children = ALL eligible (side, psi) — a
   genuine function of f (polygon+digits f-computed; eligibility a
   condition on the FIXED cell data). VERIFIED sound.
 (ii) L6 disjointness -> corollary: "The rev-5 disjointness claim ... is
   REFUTED — F1's instance exhibits two distinct same-shape eta with
   LITERALLY EQUAL strata"; replaced by mult_P(f), realizable <-> realized
   now per-branch ("the tree carries all branches"). VERIFIED.
 (iii) event identity -> D4R.1 split: exact SUM law (D4R.1-SUM, each
   summand Theorem C(b), "No disjointness is consumed anywhere — the sum
   is over the INDEX SET") + event BOUND (D4R.1-EV, equality iff mult=1,
   pointwise upgrade displayed). VERIFIED.
 (iv) L7/L12/D4R.4 -> L7 tree-restated, no disjointness over H; L12
   rewritten to D4R.4-SUM ("each level-N residue counted with its
   multiplicity") + D4R.4-EV; "the fibers are disjoint" struck. VERIFIED.
 (v) kernel statement false -> Theorem D4R.0 RESTATED at the cell level
   ("CELL PARTITION + BRANCH SETS"), header kernel part (b) now
   "DIGIT-OUTCOME enumeration (value cells; branch data are cell data,
   never f-data)". The rev-5 exclusivity sentence "struck as REFUTED,
   not open" — the exact honesty gradation the predecessor demanded.
 Root-cause face: L5 now displays "BRANCH SETS ARE CELL DATA, NOT
   f-DATA"; D4-R.5 carries the cross-shape consumer duty ("shape events
   are a multiplicity-weighted COVER ... may exceed 1") and "The plan
   S2's 'patterns are FUNCTIONS of f' is REFUTED (F1); plan rev 8 must
   carry this repair" — the inheritance is on record, queued.
 Architecture decision: multiset prefixes REJECTED for the correct
   reason (accepted SC proves Theorem C for SINGLE-BRANCH histories
   only; a multiset locus constrains f in several frames — no SC mass
   theorem). The adopted branch-indexed form is the predecessor's own
   recovery direction, executed with the mult bookkeeping added.
 REFUTATION INSTANCE, displayed handling checked: the REV-6 CHECK at
   D4R.1 works n=4/p=5, cell (4,3,3,4), R0=(z-1)^2(z-2)^2, "the sum
   (D4R.1-SUM) counts the cell TWICE (mult = 2 ...) and the event
   (D4R.1-EV) counts it ONCE", overcount "exactly 6*5^-14". My
   independent census (fresh code, integer-binomial Taylor method):
   exactly 6 mult-2 cells at p=5, the six tuples verbatim incl.
   (4,3,3,4)->{1,2}; gap = 6. A(P0)=14 re-derived (4+6+4). EXACT MATCH.

### (a) F2 — ANSWERED. L3's shape-prefix bullet now retains "endpoints
s0'_r (hence right endpoint s0'_r + w'_r), raw width w'_r, the
stride-position/slot sets p_j (hence the digit count d_r), anchor a_r".
L9 (fresh inventory), L11 (deg R_anch,r = w'_r/e_r), W(P) (d_r) all now
consume shape-resident data. Side HEIGHTS are not listed but are derived
data (slope + endpoints + frame recursion via L4/prior shape — the same
derivation both gate ledgers used, twice census-confirmed); CLOSED.

### (a) F3 — ANSWERED. L4's bracket now "[the plan's BURDEN label (c),
common frame — NOT a kernel part: D4R.0-K has parts (a), (b) only]";
D4R.0's burden ledger annotated to match. CLOSED.


## Duty (b): quote-and-classify sweep of the rebuilt note

Full line-check of §D4-R rev 7 (lines shifted by concurrent §C edits;
grep-anchored). Findings, quote-and-classify (fix nothing):

B1. GAP (display) — STALE SECTION HEADING: "### D4-R.1 D4R.0 — the
  partition into prefix fibers (three burdens, displayed)" (grep
  anchor: 'partition into prefix fibers'). The rebuilt content beneath
  it withdraws exactly that: Theorem D4R.0 is now "CELL PARTITION +
  BRANCH SETS", and D4-R.5 lists "per-node-outcome
  exclusivity/partition" among REFUTED AND WITHDRAWN items. A reader
  navigating by headings re-imports the refuted prefix-fiber-partition
  ontology. Same genre as predecessor F3 (stale label); heading
  survives from rev 5 unedited. One-line fix (e.g. "the cell partition
  and the branch fibers").

B2. GAP (display, minor) — UNQUALIFIED LEVEL-N PARENTHETICAL: D4R.1's
  "(equivalently: the level-N average of mult_P is C_P(p)*p^-A(P) —
  finitely many indicators, summed)". Fiber membership of a level-N
  residue is well-defined (and the count exact) only for N >= N(P)
  (D4R.4/L12; below N(P) a fiber is not a union of level-N classes, so
  "the level-N average of mult" is not defined). The qualifier N >=
  N(P) — displayed at D4R.4 — is missing here.

CHECKS THAT PASS (each line-verified in the rev-7 text):
- L2 tree: choice-freeness argument sound (polygon + ALL side digits
  are f-computed; eligibility is a predicate on the fixed residual);
  prefix-closure and finite/infinite branches carried; no termination
  consumed. Children quantify over ALL node data, so each psi's
  derived (g, mu) shape is self-consistent.
- L6 (D4R-CYL) both inclusions: the ⊆ induction routes through L5 CELL
  exclusivity + branch-set membership + L1's deterministic lift —
  valid on the tree; empty prefix dispatched by display (not C(a)).
- D4R.1-EV pointwise upgrade: overlaps of finite-level cylinders are
  unions of level-max classes, so null => empty. Sound. Same at
  D4R.4-EV (residue-disjointness form, no measure-zero slack).
- L8 finiteness via enc' with the m-bar_r bound: distinct eligible psi
  give coprime prime-power factors, degrees add — m_r <= m-bar_r =
  floor(deg R_anch,r/(g_r mu_r)) is correct; deg R_anch,r = w'_r/e_r
  shape-determined (SC DOM width-confinement), F2-repaired data in P.
- L11 injectivity induction (digits + s_r reconstruct node data and
  frame via L4/L1); (NA)/(HV)/(SAE) only-remove argument correct
  (data predicates on the encoded tuple).
- D4R.2/D4R.2' chain: first step is now the EV union bound; M(P)
  carried p-free; plan reconciliation displayed, plan not edited.
- Rev-7 repairs both genuine: the pointwise upgrades (F1) displayed at
  BOTH bounds; the exclusion threshold now "deg R_anch,0 = 3 < 2*g0*mu0
  = 4" — correct (two ord-1 irreducible quadratics need degree >= 4).
- No completion-union anywhere (pass-4 mandate): checked L2, L6, L7,
  D4R.1, L12, D4-R.5.
- Kernel honesty: L1 and L5 both CONSUMED AS OPEN; D4R.0 "displayed
  here for its consumers' wiring, not established"; D4-R.5 status
  paragraph consistent (nothing unconditional; (SAE) conditionality on
  SC rev 12 declared at L3/L11/D4R.1 and now landed as stated).
- D4-R.6 gate re-derivation: mult == 1 argument correct per cell; the
  sealed numbers' meaning-preservation argument valid; (SAE)-cleanness
  evidence argument valid (empty stratum would dent exact counts).

COUNT: 0 CRITICAL, 2 GAP (both display-grade).


## Duty (c): own end-to-end trace — mult = 3 instance, n = 6, p = 7
(not in any record: all prior records have mult ≤ 2)

Configuration (mine; in NO record — every prior instance has mult <= 2
and n <= 4 at the multiplicity face): n = 6, p = 7, root-only shape
P6: node (e,h,g,mu,a) = (1,1,1,2,0), full-span side (0,6)-(6,0), slope
1, monic-sextic box, cluster c5..c0 = 0 mod 7.
- Ledger (derived from the note alone): A(P6) = 6 cluster + 15
  below-line (c0:5, c1:4, c2:3, c3:2, c4:1) + 6 on-line = 27;
  cross-check by direct pin count: c_j pinned at levels 0..(6-j),
  sum_{j} (7-j) = 2+3+4+5+6+7 = 27. W = 1*6 = 6; A_net = 21;
  m-bar_0 = floor((w'/e)/(g mu)) = floor(6/2) = 3 = M(P6);
  N(P6) = 1 + 6 = 7.
- Target cell: R0 = (z-1)^2(z-2)^2(z-3)^2 = z^6+2z^5+2z^4+3z^3+4z^2+z+1
  over F7 — digits (d5..d0) = (2,2,3,4,1,1), branch set {z-1,z-2,z-3}:
  mult 3, m-bar_0 TIGHT at 3. Spot member f = (x-7)^2(x-14)^2(x-21)^2:
  coefficients (-84, 2842, -49392, 463393, -2218524, 4235364) verified
  v(c_j) >= 6-j (equality at c0, v = 6), digits exactly (2,2,3,4,1,1).
- T_can(f): root chain has ONE eligible side, THREE children (psi =
  z-1, z-2, z-3, each deg 1 = g, ord 2 = mu) — three chains eta_1,
  eta_2, eta_3 in Pref(P6) with LITERALLY EQUAL strata (digit
  conditions only, r-independent). enc' separates them: s_0 = lex rank
  of coefficient vector (1, 7-r): z-3 -> 1, z-2 -> 2, z-1 -> 3.
- L6 (D4R-CYL) at eta_i: S(eta_i,T) = the digit cylinder = {f : eta_i
  in T_can(f)} — forward: cylinder membership => the read computes the
  cell => all three psi eligible => each eta_i is a chain; converse:
  chain => digits are f's. mult_P6 = 3 on the whole cylinder.
- SUM and EVENT laws, whole shape (my own census over F7, fresh code):
  E = 10139 cells, C = 10584 branches, histogram {1: 9714, 2: 405,
  3: 20}, max mult 3; #mult-3 cells = 20 = C(6,3) (choose 3 distinct
  roots in F7^x) — closed-form crosscheck. Gap C - E = 445 = 405*1 +
  20*2 — the histogram IS the sum/event gap decomposition: D4R.1-SUM
  gives Sum_eta mu(S(eta,T)) = 10584*7^-27; D4R.1-EV gives mu(event) =
  10139*7^-27 STRICTLY below; per-cell overcount carried honestly.
- Pool bound (D4R.2'): C = 10584 <= M*p^W = 3*7^6 = 352947. OK.
- D4R.4 form: each stratum pins all 27 essential digits (one essential
  box per eta — the same structure the D4-m2 seal enumerated); level-N
  counts = C resp. E times 7^{6N-27} for N >= 7 by the multiplier.
TRACE VERDICT: every rev-7 display consumed as written; no step needed
a partition or disjointness assumption; the first mult-3 exercise of
the laws PASSES. (Numbers: /tmp/fable_d4r_rev7_checks.py, this session.)


## Duty (d): independent D4-m2 ledger recomputation (p = 3 family)

Family recomputed: p = 3 (the prime that realizes mult >= 2; the full
sealed family P1-P6 at p = 3, plus the p = 5 headline). Method
INDEPENDENT of both the seal's pre-seal synthetic division and the
census's in-script Taylor shift: (1) analysis — per-cell root
multiplicity via integer-binomial Taylor coefficients (own code); (2)
synthesis — direct construction of the factorization types
(z-a)^2*q and (z-a)^2(z-b)^2 from the 3 irreducible quadratics over
F3. Both agree with each other and with the seal:
- E(3) = 7, C(3) = 8, gap = 1, histogram {1: 6, 2: 1}, per-branch
  totals 4/4 (P1, P2, P3, P4: match).
- Seven-cell registry VERBATIM, incl. mult-2 cell (0,1,0,1) -> {1,2}
  [R0 = z^4+z^2+1 = (z-1)^2(z-2)^2] and all six mult-1 cells (P5: match).
- Ledger re-derived: A = 4+6+4 = 14, W = 1*4 = 4, A_net = 10,
  M = floor(4/2) = 2, N(P0) = 1+4 = 5 (S3: match).
- Level counts: N=5: SUM 5832, EVENT 5103, GAP 729 = 3^6, hist
  {4374, 729}; N=6: 472392 / 413343 / 59049, ratio 3^4 exact (P6: match).
- p = 5 headline crosscheck: E = 58, C = 64, gap = 6, hist {52, 6},
  per-branch 16 x 4, six mult-2 cells verbatim (P1-P5 p=5: match).
LEDGER VERDICT: the D4-m2 p = 3 family reproduces EXACTLY from the
note + seal text by an independent implementation. 0 discrepancies.


## Duty (e): fresh sweep — silent single-branch semantics; weight
consumption downstream

Emphasis 1 — SILENT SINGLE-BRANCH / PARTITION SEMANTICS. Every
occurrence of "disjoint"/"partition"/"unique"/"fiber" in the live
section line-checked: each is (a) withdrawn-language documentation,
(b) the CELL level (where partition is correct and census-tested), or
(c) the mult == 1 gate regime with the argument displayed. The ONLY
survivor is the B1 heading (duty (b)). L4, L7, L9, L12 bodies are
multiplicity-clean; L9's fresh volume is branch-independent by
construction (per-digit factors; confirmed by D4-m2 P7: both fibers
over the mult-2 cell have 729 members on the SAME boxes).

Emphasis 2 — WEIGHT CONSUMPTION BY EVERY DOWNSTREAM DISPLAY. Traced
C_P/mult through all consumers: header display (SUM exact, EV bound,
"equality iff branch mult. == 1") -> D4R.1 (SUM over index set; EV by
subadditivity; pointwise upgrade) -> D4R.2/D4R.2' (first step is EV —
a bound suffices for the (3i) consumer; M(P) p-free) -> D4R.4 (SUM
with multiplicity; EV at finite level) -> D4-R.5 (cross-shape COVER
duty; Sum_P mu(P) may exceed 1 — the aggregation hazard named for
[2a]/[3]/[5]) -> D4-R.6 (mult == 1 argument, rev-7 threshold correct).
CONSISTENT throughout: no display re-reads C_P as a cell count or the
event as exact outside mult == 1. The RENAMING RULE's phrase "realized
VALUE PREFIXES" is mildly loose (an eta is values + branch labels) but
L3's eta definition and L11's bracket ("enc' counts BRANCHES") disambiguate.

Emphasis 3 — NEW REGIME PROBE (mult >= 3). The laws' displays nowhere
assume mult <= 2; my duty-(c) instance (mult 3, m-bar tight at 3, gap
decomposition 445 = 405 + 2*20) consumed them without strain. The
queued multiplicity gate could adopt n = 6/p = 7 to also pin the
mult-3 face (suggestion only, not an obligation).

## VERDICT

(a) F1 GENUINELY ANSWERED (all five damage-chain items repaired, the
    refutation instance displayed and census-confirmed); F2, F3 CLOSED.
(b) Sweep: 0 CRITICAL, 2 GAP — both display-grade (B1 stale heading
    "partition into prefix fibers"; B2 unqualified level-N average).
(c) Own mult-3 trace (n=6, p=7, no record): PASSES all displays;
    ledger A=27/W=6/M=3 tight; sum 10584 vs event 10139 per 7^-27.
(d) D4-m2 p=3 family: EXACT independent reproduction, 0 discrepancies
    (plus p=5 headline + six mult-2 cells verbatim).
(e) No silent single-branch semantics beyond B1; weights consumed
    consistently by every downstream display.

NOT CLEAN — 0 critical, 2 display-grade gaps (B1, B2). The
mathematics of the rebuild survives this pass intact; both findings
are one-line display repairs, after which a clean confirming pass is
expected. Conditionality unchanged and honestly displayed (SC rev 12 +
SB2-DEF + open kernel D4R.0-K).

