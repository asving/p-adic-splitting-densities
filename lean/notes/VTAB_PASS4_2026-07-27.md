The mathematical repairs themselves survive the acceptance-depth sweep: ε is consistently the single concrete entrance object; every `h_ent(ε)` is a projection, not an independent index; ENT-AGG/INIT-RAT use the template-instantiation bijection exactly once; and `W_ent` remains height-independent. The terminal absorbing-column extension also type-checks, including the pinned HMC terminal step.

The V.6 arithmetic rechecks cleanly: all displayed A1, A2, and `s_blk2` polynomials and evaluations are correct; the partition sums are correct; and Gate B gives \(I^{ent}=2\), \(\iota=1/4\), \(T=1\), \(g=1/8\), hence \(\widehat\mu=1/32\), with counts \(16\) and \(128\). The recorded 178/178 PASS is numerically consistent.

Numbered findings:

1. **GAP — stale terminal revision/status record.** The note ends with:

   > “*(End of §V-TABLES, rev 3 … Verification continues … the seal file … before any census code runs.)*”

   This contradicts the rev-5 identity and V.6’s completed census:

   > “**GATE STATUS: PASSED … 178 exact checks, 0 failures.**”

   At this campaign’s acceptance bar, stale revision and pre-census status language has previously required repair.

2. **GAP — the mandated acceptance-record perimeter is incomplete.** V.6’s support-not-closure sentence names XHD, XHD-u, ENT-U, HMC, VALUE-Fq, and VALUE-p, but V.6.5 explicitly requires the acceptance record also to name ENT-COUNT/INIT-RAT, computed-list coverage, the two gate components, inherited `[2a]` pending status, and the evidence-only probe roster including the HMC-probe result. No consolidated rev-5 acceptance record supplies all of these, and no HMC-probe result is reported.

Counts: **0 critical, 2 gaps.**

Verdict: **NOT ACCEPTED (0/2).**
