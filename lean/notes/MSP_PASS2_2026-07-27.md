1. **CRITICAL — C1 is NOT FIXED.** Rev 3 defines  
   > “(G2) \(a=\lfloor s'_0/e\rfloor\)”  
   > “(G3) \(\{p_j\}=\{a,\ldots ,a+\ell\}\)”  
   
   but the cited D.3(c) formula is \(p_j=(j-t\gamma)/e\), not \(\lfloor j/e\rfloor\). The discrepancy occurs in the sealed \(n=3\) list itself. Consider a root side from \((1,1)\) to \((3,0)\), so \(e=2,h=1,\gamma=3\), with Bézout coefficient \(t=1\). Then
   \[
   p_1=(1-3)/2=-1,\qquad p_3=(3-3)/2=0.
   \]
   Thus the retained slot set is \(\{-1,0\}\) and the D.8 anchor \(\operatorname{ord}_zR=-1\). Rev 3 instead records the R7 variant \(s'_0=1\) with \(a=0\) and slots \(\{0,1\}\). Such a side is realizable, for example by a cubic whose points include \((1,1),(3,0)\), with the other coefficients strictly above that side.

   The appeal to “the census convention FC4” silently replaces the D4-R/B2 anchor and positions by shifted, non-geometric fields. D.10 proves nonnegativity and \(a=s'_0\) only at \(e=1\); it does not justify the \(e\ge2\) replacement. Consequently the corrected anchor bound is valid for R3 and other \(e=1\) rows but not for the actual general anchor.

   Internally, if G1–G6 are accepted as new axioms, their closure does generate exactly the displayed 53 synthetic tuples: the root has 21, its reachable stages are exactly Q3, Q2, Q1 and the four W=1 increment stages, and no additional coherent tuple appears. Walking R4\(\triangleright(1,2)\to\)Q2, Q2.3\(\triangleright(1,2)\to\)Q2, and an \(e=2\) row \(\to I(2,1)\) confirms the stage calculation. But that internal count no longer catalogues the retained D4-R read data. SP-COMP and the claimed census-to-theory identification therefore fail.

2. **CRITICAL — C2 is only PARTIAL.** The verdict alphabet retyping itself is clean:
   > “\(V_{\rm term}:=\{(z),(ep),(ns)\}\), disjoint from \(\mathcal S_n^{raw}\)”  
   > “The five \(W=1\) letters … stay species with selection \(\bot\).”
   
   SP.1, SP.4 and the first part of SP.6 consistently treat the confirming \(W=1\) read as a species followed by a verdict edge. But SP-COMP simultaneously says the catalogue word includes
   > “the reads … up to and including its first \(W=1\) read”
   
   while explaining this by saying
   > “\(T_{\rm can}^{\tau}\), whose \((\tau\text{-irr})\) halt sits at the confirming read’s parent.”
   
   If the VP halt sits at the parent, the confirming child read is not a read of the truncated tree; if the confirming read is included, the halt/verdict attaches after that read, not at its parent. The word endpoint, leaf location, and SP-COMP quantifier therefore remain mutually inconsistent. VP must choose one convention before the retype is complete.

3. **GAP — C4 is PARTIAL.** Rev 3 says
   > “\(N_p^*(\text{letter}) :=\) the least \(N\) … — well-defined exactly”  
   > for every letter passing the field-size exclusions.
   
   Passing the field-size test does not prove eventual realization: SP.4 itself lists value/collision and height-lattice emptiness as independent mechanisms. The note proves or censuses existence only for \(p=2,3\) in the stated \(n=3\) cases. For other primes, “well-defined exactly there” and the all-prime threshold iff are predictions, not derived statements. \(N_p^*\) is presently defined only conditionally on existence.

4. **GAP — the revised threshold gate remains unsealed.** The record explicitly says
   > “its RE-SEAL … is QUEUED as the required follow-up gate.”
   
   The old sealed prediction failed. The diagnostics support the revised \(p=3\) thresholds, but the note’s own required replacement gate has not passed. The corrected “five, not six, at \(p=2\)” statement is genuine and agrees with the recorded exclusion, but it does not complete the queued re-seal.

Pass-1 repair classification:

- C1, reachable geometric catalogue: **NOT FIXED**.
- C2, verdict/species typing: **PARTIAL**.
- C3, entrance finiteness: **GENUINE**. SP-DAG’s rank handles every edge type; \(D\) never decreases, genuine increments raise \(D\), recenterings either raise `tagord`, decrease \(W\), or move from the unique full letter to a non-full letter. The \(6n^2\) bound is derived from the rank range, not merely asserted.
- C4, per-prime \(N_p^*\): **PARTIAL**.
- Gap 1, encoding ranges and bound: **GENUINE**.
- Gap 2, D4R.0-K ledger: **GENUINE**.
- Gap 3, false progress claim: **GENUINE**, replaced correctly by SP-DAG.

**Counts: 2 criticals / 2 gaps.**

**Verdict: NOT ACCEPTED.**
