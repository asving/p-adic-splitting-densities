Pass-3 acceptance review completed from the pasted text only.

## Rev-3 repair classification

1. **F1 — disjoint semilinear components: GENUINE.**

   Repair quoted: “the listed linear sets are required PAIRWISE DISJOINT, and [2b] proves XHD-d in exactly that form (an explicitly listed disjoint decomposition).”

   This has the right quantifier. XHD-d does not require an arbitrary pre-existing overlapping presentation to become disjoint magically; it requires [2b] to provide the presentation consumed here, with that listed presentation a partition. Semilinear sets admit such finite disjoint decompositions in principle. XHD-s, \(G\), INIT-RAT, and the V.6.2 example all consistently sum those given components without inclusion–exclusion.

2. **F2 — marked-space MEAS: GENUINE.**

   Repair quoted: “the summand ITSELF is that union with the continuation mark ADJOINED … and the summands are pairwise disjoint IN THE MARKED SPACE; as f-events only the DIGIT CELLS are disjoint.”

   This matches §D4-R’s branch-tree ontology. A cell with two eligible continuations produces two different marked pairs \((f,\eta)\), even when their projections to the \(f\)-space are equal. PART-2, \(\widehat\mu\), COMP-h, COMP-hΣ, COMP-Σ, and COMP-AGG all consume precisely that multiplicity-counting measure.

   “Height-class” must mean a fixed exact-height stratum here. That is the interpretation used by COMP-h and the subsequent countable height sums. Under that reading, the finite-level assertion is consistent.

3. **F3 — VAL(b) conditionality: GENUINE.**

   Repair quoted: “CHAIN — GIVEN (XHD-u)’s COUNT FACE.”

   The note now distinguishes unconditional pool/degree bounds from the identification of an actual linked count with \(\prod_iT_i\). The latter genuinely requires representative-uniform successor rows and height rectangularity. The tag is propagated through \(\widehat C_{\varepsilon,\gamma}\), COMP’s count shadow, the epistemic census language, V.5, and COMPOSE-probe.

4. **F4 — PART-1 countable partition: GENUINE.**

   Repair quoted: “TOTALITY … COUNTABLE ADDITIVITY … NULL REMAINDER … each fixed-height block sums by C.1.5(1)’s conditional exactness.”

   This replaces the false finite-pool argument with the correct countable-partition route. The no-finite-height locus is placed in a decreasing family of strip cylinders whose masses tend to zero. PART-1 no longer depends on continuation-mark exclusivity.

5. **F5 — pinned HMC probe: PARTIAL.**

   The missing data are now pinned: source \(\beta_1\), the recentered depth-2 window, \(m_2^{(2)}\), \(o_2=o_{\rm inert}\), \(h_2=2\lambda_2\), the expected law \(2^{-3\lambda_2}\), and the determination-level rule.

   But the chosen second outcome is terminal and has “no target-cell map needed,” whereas the formal HMC and COMP template is written entirely as
   \[
   \beta_{i-1}\xrightarrow{m_i,o_i}\beta_i
   \]
   with a target CTS cell at every step. Thus the probe is fully numerically pinned but is not literally an instance of the displayed HMC quantifier. It tests the natural terminal-final-step extension of HMC, not HMC exactly as typed.

6. **F6 — Gate B’s two histories: GENUINE.**

   Repair quoted: “(U-A) is applied PER HISTORY, separately to each of the two marked entrance histories.”

   The two histories are explicitly identified, both landing at \(t=1\) in characteristic two. The common inventory is derived from the identical recentered window shape, after which the two per-history contributions are added. No representative-uniformity claim from XHD-u is silently used.

## End-to-end sweep

The main measured chain is now coherent:

\[
\text{fixed marked history}
 \xrightarrow{\text{U-A + XHD-u}}
 T(h)g(h)
 \xrightarrow{\text{XHD-d/s}}
 \sum_{h\in H(\gamma)}\prod T_ig_i(h_i)
 \xrightarrow{\text{HMC}}
 \prod T_iG_i.
\]

The note correctly keeps the unfactored COMP-hΣ available without HMC, keeps \(h_{\rm ent}\) only in \(\iota\), and sums entrance heights exactly once in ENT-AGG/COMP-AGG. No source-mass division or ordinary-event interpretation of \(\widehat\mu\) reappears.

One substantive typing defect remains in INIT. The pasted §D4-R definition says a shape-prefix \(\widehat P\) already retains every side height \(u_r^*\). V-TABLES nevertheless defines
\[
\varepsilon=(\widehat P,b,\sigma_{\rm sel})
\quad\text{and then independently}\quad
h_{\rm ent}\in H(\varepsilon),
\]
while also saying that \(\widehat P\) is a “§D4-R shape-prefix” and that the height is “shape data via \(u^*\in\widehat P\).” These cannot all be literal simultaneously:

- If \(\widehat P\) is a concrete §D4-R shape-prefix, its entrance heights are already fixed, so \(h_{\rm ent}\) is duplicated and \(H(\varepsilon)\) should be a singleton.
- If \(\widehat P\) is a height-forgetting symbolic template later instantiated by \(h_{\rm ent}\), it is not yet a §D4-R shape-prefix, and D4R.1-SUM cannot be cited until the instantiation map is supplied.

This affects the indexing and exact-once summation underlying ENT-AGG and INIT-RAT, not merely terminology.

## V.6 numerical recheck

All displayed arithmetic remains correct.

- A1:
  \[
  \binom q3,\quad \frac{q^2(q-1)}2,\quad \frac{q^3-q}3,\quad q(q-1),\quad q,
  \]
  summing to \(q^3\). The complete nine-\(q\) grids, including \(m_{H3}(2)=0\), are correct.

- A2:
  \[
  H=L((4,1);\{(3,1),(2,0)\}),\qquad T=(q-1)^2,
  \]
  and
  \[
  E(h_0,h_2)=\frac{3(h_0+h_2)-1}{2};
  \]
  at \((4,1)\), \(E=7\). The evaluation row
  \(1,4,9,16,36,49,64,100,144\) is correct.

- Size-2 block:
  \[
  T_{\rm split}=\frac{(q-1)(q-2)}2,\quad
  T_{\rm inert}=\frac{q(q-1)}2,\quad
  T_{\rm double}=q-1,
  \]
  summing to \(q(q-1)\). Also \(T_{m_{w2}}=q-1\) and
  \(T_{m_{vv}}=(q-1)^2\). Every displayed evaluation is correct in odd and characteristic-two conventions.

- Gate B:
  \[
  I^{\rm ent}(2)=2,\qquad
  \iota=2\cdot2^{-3}=\frac14,\qquad
  T=1,\qquad g=2^{-3},
  \]
  hence
  \[
  \widehat\mu=\frac1{32},\qquad
  \text{count shadow}=2.
  \]
  At \(N=3\), \(2^{9}/32=16\); at \(N=4\), \(2^{12}/32=128\). The threshold derivation is consistent.

The predictions are correct, but the note explicitly says “V-n3 census not yet run,” while its governance says “[1v]-COND may NOT be accepted before V-n3 passes.” That alone prevents acceptance now.

## Numbered findings

1. **CRITICAL — entrance-height typing is inconsistent.** A concrete §D4-R shape-prefix already contains the \(u^*\) heights, while V-TABLES independently parameterizes the same entrance by \(h_{\rm ent}\). ENT-AGG and INIT-RAT therefore lack an unambiguous indexing object.

2. **CRITICAL — the mandatory V-n3 acceptance gate has not run.** Correct hand predictions do not satisfy the note’s explicit seal-and-census precondition.

3. **GAP — the pinned HMC probe ends in a terminal outcome without a target cell.** It is not literally a path template under the displayed HMC/COMP signature, although it is a valid probe of the intended terminal-step extension.

**Counts: 2 critical / 1 gap.**

**VERDICT: NOT ACCEPTED.**
