1. **CRITICAL — the fixed-height inventory is miscounted.** H.1.1 says fresh zeros occupy “`(old(j), new(j)) at (α)/(β) slots`.” But §C C.1(ii) explicitly makes the β left-slot strip `(old(j), new(j)]`. The top level is a zero pin for β, not a value digit. Omitting it changes \(E(h)\), so the displayed alphabet product is not proved equal to the C.1.5 conditional mass.

2. **CRITICAL — the level-scale conversion is reversed or at least incompatible with the cited lemma.** H.1.1/H.1.2 count strip levels as “`str·(new(j) − old(j))`.” C.1.0(c) states \(\ell=(h-\mathrm{off})/\mathrm{str}\), so at a fixed label the number of base levels between two stage heights is \((new-old)/\mathrm{str}\), subject to endpoint conventions—not `str·(new-old)`. No reconciliation of the two scales is supplied. This independently invalidates the claimed exponent.

3. **CRITICAL — positivity is asserted, not proved, and the quantifiers change mid-proof.** XHD-w requires each step exponent \(E\) to have positive weights. H.1.2 first admits source coordinates enter the current step with negative sign, then changes the claim to positivity of the *total template exponent*: “`On each listed component the total template exponent Σ_i E_i(h) has positive weight`.” The proposed domination—“`strictly dominates ... because new − old telescopes`”—has no inequality or coefficient computation behind it. A “`standing per-template duty`” is still open work, not a derived theorem for every component.

4. **CRITICAL — Lemma H-LIST’s construction does not prove its stated lemma.** Residue partitioning does not make the maximum of several affine lower bounds into one affine form. For example, a coordinate constrained by
   \[
   y\ge x+1,\qquad y\ge 2z+1
   \]
   has least value \(\max(x,2z)+1\); fixing all variables modulo \(M\) does not determine which branch dominates. Step (3), which substitutes a single \(\phi_r^\uparrow\), therefore fails. Step (4)’s two-period argument treats one very special under-staircase and does not handle several earlier variables, several upper-bounded coordinates, or interacting affine slacks. A correct result would require a genuine rational-cone/Hilbert-basis decomposition and a separate disjointification proof.

5. **CRITICAL — H-LIST also fails to establish disjoint, uniquely parameterized linear sets.** Refining by residue and “slack residue” does not show that the resulting period monoids are free or that their parameterizations are injective. General affine cones can require dependent Hilbert generators. The worked identity for \(0\le t\le2x\) verifies only that example. Consequently the claimed listed partition, its disjointness, and its “one orbit” interpretation are not established.

6. **CRITICAL — XHD-s depends on the failed free-monoid conclusion.** H.3 replaces the sum over a linear set by a free \(\mathbb N^P\)-sum and factors it as one geometric factor per listed period. That formula is valid only when
   \[
   (n_v)_{v\in P}\longmapsto h_0+\sum n_vv
   \]
   is injective. Injectivity was proved only for the lower-bound triangular substitution, not for the general slack/cone components supposedly produced in H-LIST. With dependent periods, the displayed product overcounts points.

7. **CRITICAL — the per-component \(u\)-T derivation does not give V.0’s count statement.** M4b-T, even granted, transports chains while twisting representatives/targets. Triviality of the cell-label permutation \(\pi_v\) does not by itself show
   \(T_\gamma(x,h)=T_\gamma(x,h+v)\) for the *same representative* \(x\); that requires the fenced U-R-COUNT comparison or an explicit source-fixing clause in the bijection. Nor does height constancy of the total template count prove V.0’s factorization
   \[
   T_\gamma(x,h)=\prod_iT_{m_i,o_i}^{\beta_{i-1},\beta_i}(q).
   \]
   Thus the “PER COMPONENT — DERIVED” label is too strong even under M4b-T-AUD.

8. **CRITICAL — the consumer specification is not matched.** V.0 asks for one affine \(E(h)\) for each step datum, whereas H.1 explicitly supplies “`ONE affine form PER LISTED COMPONENT`.” Calling this a “declared reading” does not make it verbatim. V.0 also requires global \(u\)-T, the count face of \(u\)-R, and exact two-sided XHD-d; H leaves these under XHD-u-X, U-R-COUNT, and XHD-d-EX(∂). Those are honest fences, but they mean the V.0 package is not delivered and `[1v]-FULL` cannot be recorded. In addition, V.0 demands bases and periods explicitly listed per template; H supplies only a defective generic recipe and one gate-family list.

9. **CRITICAL — H-n3 is not actually sealed or referee-runnable.** It contains no explicit affine \(E\), no predicted per-point counts or conditional masses, no exact determination-level formula, and no fixed caps. Instead it says the cap will be “`displayed in the seal`” and gives only “`target N ≤ 8`” and “`target N ≤ 5`.” That is a future seal, not a sealed specification. The cross-component kernel’s promised second family is also not specified. The primary domain identity
   \[
   L((1,3);\{(1,2),(0,2)\})
   =\{h_0\ge1,\ h_1\text{ odd},\ h_1>2h_0\}
   \]
   is correct, and the single-component limitation is honestly acknowledged, but the gate cannot presently be independently executed.

10. **GAP — AUD-E’s narrow conclusion is plausible, but its displacement claim is overstated.** Once one conditions on an admissible fixed digit value, TYP(b)’s additive equal-fiber argument does exclude multiplicative \(\gcd(a,q^g-1)\) factors from that digit’s mass. However, H.1.3 says such congruences “`instead surface in XHD-d ... as the (HV)-hinge congruence family`.” Its own preceding sentence also places norms, ratios, and collision strata in cell-membership/count data. It has not shown that every height-dependent multiplicative admissibility condition is an HV hinge; that is precisely part of the still-open M4b-T all-coefficient/collision audit. AUD-E should therefore be credited only for exclusion from the additive volume factors, not for the claimed complete relocation.

11. **GAP — M4b-T’s status language is internally inconsistent.** H.5.1 calls the normal-form conclusion “`proved, never premised`,” then immediately declares the ALL-quantifier establishing that conclusion open as M4b-T-AUD. The fence itself is genuine and nonvacuous—likewise XHD-d-EX(∂), U-R-COUNT, and XHD-u-X—but the sentence incorrectly labels the fenced conclusion as proved.

Counts: **9 critical / 2 gaps**

**Verdict: NOT ACCEPTED.**
