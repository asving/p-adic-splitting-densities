1. **CRITICAL — the claimed “MEMBER-COUNT TRICHOTOMY” is contradicted by the cited roster.** Rev 5 states:

   > “o terminal = the m = 1 halted case … o branching = m ≥ 2 … o continuing = m = 1”

   But V.6.1 explicitly has terminal outcomes with multiple individuated members:

   > “m_H3: 3 children (1,1,1)”  
   > “m_H12: (1,2)”

   Thus member count alone does not classify terminal versus branching outcomes. Likewise, the final claimed exhaustive dispatch

   > “m = 1 halted / m = 1 continuing / m ≥ 2”

   incorrectly sends multi-member, all-halted outcomes to `b_e^split`, whereas the displayed system sends them to `b_e^{term,fin}`. More importantly, the cited DEG-CONS statement proves only `branching ⇒ m ≥ 2`; it does not prove the converse or independently establish that every global `o continuing` has exactly one member. Consequently the first step used to derive (SCS), and hence the stated proof of `(K-SUB)` exhaustiveness, remains unsupported.

   Conditional on the missing implication “a K-column continuing outcome has exactly one total member,” the rest of (SCS) is correct: no flank and no non-selected cofactor force `e_read·ℓ = W` and `λ = {(g,μ)}`; then
   `W′D′ = μ·e_read·g·D = ℓ·e_read·D = WD`.
   The `e_read = g = 1, μ < W` species edge consequently must have another member and is compatible with a branching outcome satisfying DEG-CONS. The arithmetic and window-shrinking fence themselves are sound.

2. **End-to-end sweep:** no additional criticals or gaps found. The block orientation, marked/per-f fences, split coefficient `J`, height summation, XRB consumption DAG, per-pool E0/ACT scope, rationality tags, checksum dependencies, S.6 perimeter, and S.7 specified-but-unsealed status remain coherent.

Counts: **1 critical, 0 gaps.**

**VERDICT: NOT ACCEPTED.**
