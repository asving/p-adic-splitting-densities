# FABLE FRESH-INSTANCE VERIFICATION of §D4-R (2026-07-26)

Verifier: fresh Fable instance (dual bar, Fable half; Codex accepted at
passes 5+6 CLEAN — no deference). Target: live §D4-R,
MOVES_2026-07-24.md lines 4464–5052 (+ pass records to 5202).
Inputs read: §C REV 2 (line 3398 ff., rev 11b as amended in place),
§B2-DEF offset-P paragraph, ASSEMBLY_PLAN §2 where cited.

## Duty (a): quote-and-classify findings

F1. CRITICAL — the single-run ontology is refutable, not merely open: the
next-node datum is NOT a function of f. L5 EXCLUSIVITY: "the outcome data
are FUNCTIONS of f (...), so distinct outcome data have DISJOINT strata"
— presented as an argument, NOT flagged into the open kernel (header:
"D4R.0-K := (a) L1's totality ... (b) L5's exhaustive next-outcome
enumeration"; acceptance record likewise lists only totality +
exhaustiveness). But the full node datum includes the descended factor ψ
(L11: "A §C node datum carries MORE than shape + digits: the residual
factor ψᵢ") and, at multi-side polygons, the side choice. Neither is a
function of f: the D4-R.0 canonical policy fixes ONLY the lift rule.
  Counterexample (worked, not in any record): n=4, p=5, cluster
  c₃..c₀ ≡ 0 mod p, root shape (e,h,g,μ,a) = (1,1,1,2,0), full-span side
  (0,4)–(4,0). Digit tuple (d₃,d₂,d₁,d₀) = (4,3,3,4): R₀ = z⁴+4z³+3z²
  +3z+4 = (z−1)²(z−2)² over F₅. Both ψ = z−1 and ψ = z−2 have deg 1,
  ord 2: two DISTINCT realized prefixes η₁ ≠ η₂ ∈ Pref(P̂₀) (ψ is node
  data) with LITERALLY EQUAL strata (the stratum equations are cluster +
  side pattern only; ord_ψ R_anch = μ is a data condition on the FIXED
  R_anch, not an f-condition). Concrete member: f = x⁴+20x³+75x²+375x
  +2500.
  Damage chain, granted everything else: (i) L2's "UNIQUE maximal
  canonical run" is undefined at such f (which ψ?) — even at the GATE's
  own target cell R₀ = ψ·(z−r), TWO branches (quadratic and linear)
  exist and no rule picks one; (ii) L6 (D4R-CYL) cannot hold for both
  η₁, η₂ (equal loci, but a single run has one length-1 segment);
  its corollary's disjointness (S(η₁,⊤)∩S(η₂,⊤) = ∅) is FALSE by
  display, and "realizable ⟹ realized" fails for the unchosen branch;
  (iii) D4R.1's "⨆" is not disjoint and the event identity fails:
  at P̂₀, event mass = (#tuples)·p^{−14} but C_P̂₀·p^{−14} counts
  ψ-labels — at p=5 the C(4,2) = 6 two-double-root tuples each
  contribute an overcount of p^{−14}; (iv) L7, L12's "fibers are
  disjoint", and D4R.4's exact level-N count inherit (iii); (v) Theorem
  D4R.0's PARTITION statement — the kernel itself — is FALSE as stated
  (outcomes = full node data), so every "CONDITIONAL on D4R.0-K" clause
  conditions on a refuted statement. What survives: Σ_η μ(S(η,⊤)) =
  C_P̂·p^{−A} as a SUM of §C masses (each summand is Thm C(b)); hence
  μ(⋃) ≤ C·p^{−A} ≤ M·p^{−A_net} by subadditivity — D4R.2′ is
  recoverable as a union bound; the exact law and the run-map/event
  language are not. Supporting internal inconsistency: L5(i) describes
  the outcome as "a polygon side ..., its side-digit tuple, and (at a
  recentering) the canonical lift" — OMITTING ψ, contradicting L11's
  node-datum sentence; the omission is exactly what makes the
  exclusivity sentence look plausible. Root cause sits in the plan §2
  ("polygon and side patterns are FUNCTIONS of f, so ... disjoint") —
  inherited, and invisible to the D4-n3 gate (m_r = 1; FC8/FC9 strata
  are digit-VALUE cells, which do partition; the run map itself was
  never computed).

F2. GAP — L3's binding dictionary under-specifies the shape-prefix:
"species, (e, h, g, μ, a) per node, strides, residue degrees w_r
retained" omits the per-read side data (endpoints s₀', raw width w'_r /
slot set), yet L9 needs the fresh inventory shape-determined, W(P̂)
needs d_r, and L11 says "the side endpoints/strides (shape data in P̂)"
and calls w'_r "a shape datum". w'_r is NOT derivable from (e,h,g,μ,a)
(the residual cofactor h̃ can have any degree). "Strides" must be read
as the full stride-position sets to close this; the plan §2 has the
same omission. Display-grade: state the retained side data explicitly.

F3. GAP (display) — L4's bracket "[kernel part (c); pass-4 lemma 4]"
collides with the header's kernel definition (parts (a),(b) only) and
with the burden ledger's "(c) COMMON FRAME = L4: DISCHARGED CONDITIONAL
ON L1". The "(c)" is the PLAN's burden label, not a kernel part; as
placed it reads as if L4 were open kernel.

## Duty (b): instance trace (own construction, not in any record)

P̂† (n = 4, two reads, species mix increment→increment with e₁ = 2 —
no record instance has e_r > 1 or μ > 1):
- Box: monic quartics; cluster c₃,c₂,c₁,c₀ ≡ 0 mod p (4 pins).
- ν₀: side (0,4)–(4,0), slope 1, stride 1, (e₀,h₀,g₀,μ₀,a₀) =
  (1,1,1,2,0): R₀ deg 4, descend at linear ψ₀ with ord 2. Fresh pins:
  4 cluster + 6 below-line (c₀:3, c₁:2, c₂:1, c₃:0) + 4 on-line digits
  (monic lead excluded) = 14; F₁ = F_p, w₁ = 1. Key φ = x − S lift,
  deg e₀g₀ = 1, w(φ) = e₀h₀g₀ = 1.
- ν₁: window [0,μ₀] = [0,2]; box line old(j) = 4−j (w(f) = 4);
  pin w(B₀) = 7, w(B₁) ≥ 5: side (0,7)–(2,2), (e₁,h₁,g₁,μ₁,a₁) =
  (2,5,1,1,0); I-aug 5/2 > w(φ) = 1 ✓; w' = 2, ℓ₁ = w'/e₁ = 1, R₁
  linear. Fresh: slot 0 strip levels 5,6 (2 pins) + value at 7 (1) +
  slot 1 off-lattice forced zero at level 4 (1; levels ≤ old(1) = 3
  implied) + slot 2 adjacent (HV) hinge (0 pins) = 4 pins, pool F_p.
- Ledger: A(P̂†) = 14+4 = 18; W = 4·1 + 2·1 = 6; A_net = 12;
  M(P̂†) = m̄₀·m̄₁ = ⌊4/(1·2)⌋·⌊1/(1·1)⌋ = 2·1 = 2 — first instance
  with M > 1: the plan's literal (3i) is NOT recovered here, exactly as
  the PLAN RECONCILIATION predicts. N(P̂†) = 1 + 7 = 8 (deepest pin:
  the slot-0 value digit at ht 7 = base level 7; B_j constants, level =
  height; root pins reach only level 4).
- L2/L5 trace: empty prefix ∅ ⪯ every run, Σ_{−1} = full box, S(∅,⊤)
  = box, μ = 1 = C_∅·p⁰ (L5/L12 displays check out ✓). Step-0 outcome
  strata at my digit cell (4,3,3,4), p = 5: TWO eligible ψ — the L2/L5
  step is where the trace ABORTS (finding F1): no rule selects
  H_can^max(f)'s ν₀ for f = x⁴+20x³+75x²+375x+2500.
- L6/D4R.1 on the ψ-labeled reading: per-η masses via Thm C(b) are
  p^{−18} each; Σ over Pref = C_P̂†(p)·p^{−18} — consistent as a SUM;
  the event/⨆ form double-counts two-double-root cells (F1(iii)).
- Maximal-run consideration: inputs like f = (x²−p·u)² (persistently
  recentering branch) lie in finite prefix cylinders of P̂†-like chains
  with INFINITE canonical continuations; L6's (D4R-CYL) form handles
  them without completion unions ✓ — the pass-4 mandate is genuinely
  honored in the text (checked at L2, L6, L7, D4R.1: no completion
  union anywhere) ✓.

## Duty (c): independent D4-n3 ledger recomputation

All independently recomputed from the note text alone; every number
matches the seal:
- E(ν₀): 3 cluster + 3 below-line (c₀ lv 1,2; c₁ lv 1) + 3 on-line = 9
  pins ✓; division by φ = x²+p[s₁]x+p²[s₀]: b₁ = c₂−p[s₁], β₁ =
  c₁−p²[s₀]−p[s₁]b₁, β₀ = c₀−p²[s₀]b₁ ✓; forced height-3 pair =
  (d₀−s₀(d₂−s₁), d₁−s₀−s₁(d₂−s₁)) = (0,0) at (d₂,d₁,d₀) =
  (s₁−r, s₀−rs₁, −rs₀) ✓; vtx(ν₀) = z̄−r = d'₁ ((HV) ✓, m̂₀ = 0).
- A = 9+2 = 11 ✓ (cross-check c₂ mod p², c₁ mod p³, c₀ mod p⁴ + 2
  ν₁-pins = 11 ✓). W = 1·3 + 2·2 = 7 ✓. A_net = 4 ✓. N = 1+4 = 5 ✓.
- M via normalized ℓ_r: ℓ₀ = w'₀/e₀ = 3/1, ℓ₁ = 1/1; m̄₀ = ⌊3/2⌋ = 1,
  m̄₁ = ⌊1/1⌋ = 1, M = 1 ✓ (old coarse ⌊n/gμ⌋ would give 3 ✓ flagged).
- C: N₂(2) = 1, r ∈ {1}, p²−1 = 3 ⟹ C(2) = 3 ✓ (unique tuple (0,0,1),
  z³+1 = (z+1)(z²+z+1) ✓). N₂(3) = 3 {z²+1, z²+z+2, z²+2z+2 — all
  checked irreducible}, r ∈ {1,2}, 8 ⟹ C(3) = 3·2·8 = 48 ✓ (spot:
  ψ = z²+1, r=1 ⟹ (2,1,2), (z²+1)(z+2) = z³+2z²+z+2 ✓).
- Four counts: 3·2⁷ = 384 /2¹⁸ = 262144; 3·2¹⁰ = 3072 /2²¹ = 2097152;
  48·3⁴ = 3888 /3¹⁵ = 14348907; 48·3⁷ = 104976 /3¹⁸ = 387420489 ✓ all.
- Net bound: 3/2048 ≈ .00146 ≤ 1/16; 48/177147 ≈ .000271 ≤ 1/81 ✓.
- FC8 count: 4·3·2 − 4 = 20 ✓; digit cells p²(p−1) cover exactly the
  4 removed lumps {V₀=3, V₁≥2, V₂ any} ✓; FC9: w(B₀) > 3 forced, so
  (p²−1 δ-cells) ⊔ DEEP is exhaustive ✓.

## Duty (c): independent D4-n3 ledger recomputation

(to follow)

## Duty (d): fresh sweep — L11 enlarged-encoding collision test; L6
maximal-run finite/infinite handling

L11 collision test (constructed): P̂₀/P̂† above, digit tuple with R₀ =
(z−1)²(z−2)² (p ≥ 3, e.g. (4,3,3,4) at p = 5). Old digits-only encoding:
η_{z−1} and η_{z−2} COLLIDE (same digits). Enlarged enc′: eligible set
{ψ : deg 1, ord 2} = {z−1, z−2}, lex ranks by coefficient vector give
s₀ = 1 vs 2 — SEPARATED ✓; m₀ = 2 = m̄₀ = ⌊ℓ₀/(g₀μ₀)⌋ = ⌊4/2⌋ (bound
TIGHT here, so no slack to spare — the sharper residual-degree m̄ is
necessary and sufficient at this instance). L11's injection argument
itself checks: frame reconstruction by induction via L4 needs the side
VALUES (heights), which are shape-determined only once F2's side data
are in P̂ — folded into F2. Verdict: L11's mathematics is sound; the
irony recorded: the very multiplicity the rev-2 enlargement handles in
the COUNT is what refutes the run-map ontology upstream (F1) — the two
η's that enc′ carefully separates have the same underlying f-locus.

L6 finite/infinite maximal-run handling: the pass-4 mandate is honored
— no completion union anywhere (checked L2, L6, L7, D4R.1, L12);
(D4R-CYL) is stated for every f including infinite-run inputs; L7
restricts run-map fibers to TERMINATING H correctly; "no termination
statement is consumed" is accurate throughout. The finite/infinite
distinction itself is NOT the broken part; the single-path (vs branch
TREE) assumption is (F1).

Empty-prefix edge: L5's Σ_{−1}, L6's empty dispatch, D4R.1's C_∅ = 1 /
A(∅) = 0, L12's Pref(∅) = {∅}, N(∅,⊤) = 1 — all check out ✓, correctly
routed around Theorem C's root-read quantifier in all four places.

## VERDICT

Counts: 1 CRITICAL (F1), 2 GAP (F2, F3). NOT CLEAN.

F1 is structural, shared with the plan §2, and invisible to both the
Codex passes (which verified the note's internal logic granted its
kernel statement, and the kernel's falsity-as-stated is a
counterexample question, not a step-check question) and the D4-n3 gate
(single-branch-per-shape instance; FC8/FC9 test digit-VALUE cells,
which genuinely partition — the run map was never computed). Repair
direction (not applied): either extend the canonical policy to a
deterministic side+factor selection and redefine Pref as run-realized
prefixes (C_P̂ then counts per-cell, enc′'s selection index becomes
policy-forced), or replace the run by the branch tree, restate D4R.1 as
the SUM law Σ_{η∈Pref(P̂)} μ(S(η,⊤)) = C_P̂·p^{−A(P̂)} (true per §C),
and demote the event form to the union bound μ(⋃) ≤ C_P̂·p^{−A} —
which is all the class-3 (3i) consumer needs. D4R.2′ survives either
way; the D4-n3 numbers all reverify (duty (c)).
