# FABLE FRESH-INSTANCE VERIFICATION — §T-ASSEMBLY rev 5 (2026-07-27)

Verifier: fresh Fable instance (dual bar, Fable half; Codex accepted at passes 5+6).
Charge: (a) quote-and-classify sweep; (b) independent (ns-null) re-derivation + break
attempts; (c) own-instance TREE-EXP census (fresh prime/shape, depth 2); (d) independent
recompute of one T-n3 ledger layer; (e) fresh-emphasis sweep (pre-halt marks, (SIB-STEP)
sufficiency, (JC-multi) propagation). Text: MOVES_2026-07-24.md lines 6449–7229 (rev 5).

## S1 — Sweep findings (quote-and-classify; fix nothing)

Full-text sweep of §T.0–§T.6 + the four pass-record blocks. ONE finding; the rest clean.

FINDING S1-1 — JUSTIFICATION GAP (mis-attribution of an accepted result; the chain
survives on the (SIB) tag). §T.3: "the accepted JOINT-D1 (the joint refinement | law
across distinct root clusters) IS (SIB) at η = ∅" and §T.4 ROOT CLAUSE: "the
cross-track factorization is (SIB) at | η = ∅ = the ACCEPTED JOINT-D1". The accepted
JOINT-D1 note (JOINT_D1_2026-07-23.md §5, its own scope display) proves "JOINT-D1 for
class-3 towers | whose every descend has deg ψ = 1 ... at deg-2 keys" — a SINGLE-cluster
joint ledger ALONG one tower (J_k = the chain of descend strata, Ψ_k : J_k → B_k), not a
law ACROSS distinct root clusters. The perimeter's own root-level attribution is a CITE,
not an accepted proof: D4R0K S2.5 ROOT LEAVES, "the JOINT witness — all | tracks' data
realized SIMULTANEOUSLY — is the cross-cluster product, | MONTES L1's territory (O4):
consumed as a cite, NOT re-derived here". The identification originates in
ASSEMBLY_PLAN REV 10 (line 1735) and is inherited here, but as worded it reads as an
UNCONDITIONAL discharge of the root cross-track factorization; a consumer (RS.1-SH's
"outermost sibling law") could drop the (SIB) tag at the root on its strength.
Mitigations already in the text: §T.5(1) keeps the (SIB) tag on TREE-EXP globally, and
the depth-0 product is census-supported (SIB-gate L3 m=3; my own two-track census, S3
below). Adjudication: gap, not critical — but the word ACCEPTED should not survive into
[3]'s consumption without either the Montes-L1 faithful cite or a written depth-0 proof.

Checked clean (each line-verified against its cited source): (TGT) partition logic +
Tonelli licenses; VP-1/VP-2 typing and the (c1)/(c2)/(c3-a) discharges ((c2)'s domain
stratification and covering case-split verified against D4R0K Theorem B's case list);
TB-CAP(hen) (level-1 cylinders); TB-CAP(irr) — I re-derived the exactness of the class
= S(η·ν_{k+1},⊤): pre-halt-ness of every proper prefix is pinned by η's OWN values
(μ ≥ 2 at intermediate cells is cell data), so no over/under-count — clean; the (ns)
refutation logic (nonempty by S2.5 + null ⟹ never a finite cylinder union: a mass-0
finite union is empty); the struck raw-count claim and its normalized replacement (the
p^{(n−1)N} growth remark is correct: one fresh zero clause per level vs n digits per
level); (SIB) statement + gate-scope honesty; PCI as interface-only; SIBLING INDEXING
and the demoted multinomial (exchangeability a [1v] burden — honest); TREE-EXP's
(JC-root)/(JC-single)/(JC-multi) trichotomy with (JC-multi) honestly OPEN; the F1
ledger split 5^{−4}·5^{−10} = 5^{−14} and censused instance 5^{−16} (recomputed);
first-split induction well-founded on node count, [2r] base-change dependency flagged;
per-shape law disjointness (one f, one tree); TREE-N's thr(T) composition; ONE-F +
the Pref^τ dictionary (re-derived: for pre-halt η, "η is a chain of the pruned T" is
T-determined, so the swap μ(S(η,⊤)) = Σ_T [η chain of T]·μ(fiber T) is exact on the
decided set); §T.5's condition list complete vs. the text (SIB, TB-CAP/(ns) pair,
VP-SOUND cites, D4R.0-K(c) residual, X.3, [2r], JC-multi); §T.6 layer specs = the seal
(FC1/FC3/FC4/FC5 cross-checked), arithmetic 5^{−19}, B26 = 5¹², 6·390,625 = 2,343,750,
78,125·5¹² = 5¹⁹ all recomputed. Minor remark (no classification): (TGT)'s "decided ⟹
finite tree" uses König (finitely-branching + all branches halt), not displayed.

## S2 — (ns-null) re-derivation and break attempts

INDEPENDENT RE-DERIVATION (against the accepted §C rev-14 text, lines 4001–4131, and
D4R0K rev 3 (z-2)/(z-3)/B(iii)/S2.5 — all read fresh). The five load points:
 1. Σ ∩ L_M = S(H, Z_M) as loci: L_M's clauses are window-INTERIOR only (the (ns)
    object is the Φ̂-development remainder a₀..a_{μ−1}, base indices [0, μD); the
    vertex/transport data are rim, outside the lump), and at interior indices Σ's
    determined coordinates are the zero downset with INCLUSIVE top ht ≤ F(b)
    ((ZC-a) species-correct tops, quoted: "at indices whose | governing clause is a
    strict bound ((BOX)-type — every factor-interior | index) the top is INCLUSIVE").
    So the fresh content is exactly Z_M = {y_(b,l) = 0 : interior, F(b) < ht ≤ M}. ✓
 2. Admissibility of Z_M (C.2 rev 9: each equation solves for a coordinate FREE on
    Σ_k = not in the determined set = downset + DIG_k leads): interior coordinates at
    ht > F(b) are outside the downset, and DIG_k's leads are at rim ("LEADS AT RIM",
    (ZC-b): base indices ≥ μ_i·D_{i+1}) — never interior. Coordinates pairwise
    distinct, each a literal zero clause solving for itself: admissible. ✓
 3. C(b) factor: Theorem C(b) with Z = Z_M gives mass = μ(S(H,⊤))·vol(Z_M) and each
    zero clause cuts exactly p^{−1} (TYP(b) equal-fiber cut), so vol(Z_M) = p^{−|Z_M|}. ✓
 4. Ladder: slot 0's block has base index 0 < μD (μ ≥ 2 on a continuing track, D ≥ 1:
    interior nonempty), one digit coordinate per level, F fixed by the finite realized
    history — |Z_M| ≥ ⌊M − M₀⌋ → ∞. Even if a weighted frame spaced heights by 1/e or
    e, |Z_M| → ∞ survives (only the displayed ⌊M − M₀⌋ constant would change). ✓
 5. Continuity from above on the nested L_M (B(iii)'s capped format; (z-3) = zero at
    EVERY height ⟹ class = ∩_M(Σ ∩ L_M)). Probability measure, so the limit stands. ✓
Conclusion: μ((τ-ns) class │ Σ) = 0. The rev-2 proof is CORRECT as displayed, and its
single-state design genuinely avoids the pass-1 all-depth-freeness quantifier: no
deepened state is ever consulted; Z_M lives on the ONE entrance state.

BREAK ATTEMPTS (both failed; this is the strongest pressure I could construct):
 B1 — a lump with NON-admissible zero clauses. Needed: an interior coordinate above
   the floor that is DETERMINED on Σ — i.e. a DIG_k lead at an interior index, or a
   determined non-downset interior coordinate. (ZC-b) excludes the first structurally
   (leads at rim is the carried invariant, re-established by C.1.5(2) at every step:
   "have all leads at future-rim | indices, and Θ_{i+1}-pushforward preserves leads");
   PIN-WELLDEF's determined-set characterization (C.2 rev 12/13 text) excludes the
   second (determined = species-correct downset + rim lead level sets, nothing else).
   A break here would be a counterexample to accepted (ZC), outside this note's text.
 B2 — a state where the ladder stalls. Needed: a window-interior block with only
   finitely many free heights, F(b₀) infinite, or empty interior. F is a max of
   finitely many realized staircases (finite); coefficient slots carry digits at all
   levels; interior = [0, μD) nonempty since μ ≥ 2 on any continuing track (μ = 1
   would have τ-irr-halted, so an (ns) status at entrance forces μ ≥ 2). No stall.
 Toy anchor (independent, p = 3, n = 2, root window): (ns) class = {f = x² exactly};
   Z_M = both slots' digits at 1 ≤ ht ≤ M, |Z_M| = 2M, μ(Σ ∩ L_M) = 3^{−2}·3^{−2M} → 0;
   class = single point, mass 0. Matches the general proof's every step.
VERDICT on (ns-null): PROVED, no gap found; the two prior clean passes are confirmed
by an independent third derivation.

## S3 — Own-instance TREE-EXP census (p=3; not in any record)

Two fresh instances at p = 3 (no record touches p = 3 trees: F1/T-n3 are p = 5 n = 4,
SIB L2 is p = 2 n = 3 below-halt, L3 is p = 5 n = 3 Hensel; D4-m2/D4-n3 are p = 5).
Script /tmp/fable_tasm_census.py — my own walker (Taylor development at the recentre,
lower-hull window read, residual roots, τ-pruning), written from scratch, no reuse of
verification/ code. Zero tolerance; all counts exact.

INSTANCE 1 (n = 3, f̄ = x²(x−1): Hensel co-track + DEPTH-2 CHAIN on the x-track).
 Value-tree T(s, a′) = [root cell (3 pins, jvol 3^{−3}); track x−1: τ-hen leaf (1,1);
 track x: read 1 = single side (2,0)–(0,2), residual double root s (3 pins: 1 strip
 zero + 2 on-line digits, jvol 3^{−3}); read 2 after recentre x = 3s: RAM leaf
 v(C₀) = 3, digit₃(C₀) = a′ (one fresh digit, 3^{−1})]. TREE-EXP predicts
 μ(fiber) = 3^{−7}, i.e. 9 members on the 3⁹ box (c₂ mod 3², c₁ mod 3³, c₀ mod 3⁴).
 CENSUS: all four value-trees (s, a′) ∈ {1,2}² have EXACTLY 9 members; the split tree
 (residual roots {1,2}, two depth-1 (1,1) leaves; predicted 3^{−6}) has EXACTLY 27;
 the C.1.5 chain step is exactly uniform (per s: 27 double-root members split 9/9/9
 over digit₃(C₀) ∈ {0,1,2} — the 'deeper' bucket 9 confirms the shared prefix charged
 once through two levels, the §T.6 Layer-B mechanism at a fresh prime).

INSTANCE 2 (n = 4, f̄ = x²(x−1)²: TWO READING TRACKS — a genuinely joint tree with a
 depth-2 continuation on one track; no sealed family has this configuration).
 T = [root cell (4 pins, jvol 3^{−4}); track 0: read 1 double root s₀ = 1 (3^{−3}),
 read 2 RAM leaf a′ = 2 (3^{−1}); track 1: read 1 residual split {1,2}, two (1,1)
 leaves (3^{−3})]. TREE-EXP predicts μ(fiber) = 3^{−11} = 9 members on the 3¹³ box
 (c₃, c₂, c₁ mod 3³, c₀ mod 3⁴). CENSUS: N(root cell) = 19,683 = 3⁹ ✓;
 N(S₀ ∩ rc) = 243 = 3⁵ ✓ (per-track TREE-EXP); N(S₁ ∩ rc) = 729 = 3⁶ ✓;
 N(S₀ ∩ S₁ ∩ rc) = 9 ✓ EXACT — and the depth-0 (SIB) integer identity
 N(joint)·N(rc) = 9·19,683 = 177,147 = 243·729 = N(S₀)·N(S₁) EXACT. This is fresh
 census support for exactly the object of finding S1-1 (the root cross-track product)
 at a new prime with both tracks reading and unequal-depth subtrees.
 STABILITY FALSIFIER: 100,000 random one-digit-deeper extensions of instance-2 box
 members left every tree decision unchanged (0 changes) — the events are box-determined
 at the displayed levels, as TREE-N's thr(T) requires.

RESULT: TREE-EXP confirmed on my own instances — 6 pinned value-trees, 3 marginal
events, the joint identity, all exact.

## S4 — T-n3 ledger layer recompute

LAYER A recomputed INDEPENDENTLY (same cell spec from the seal — the shared input —
but my own development/digit code, /tmp/fable_tasm_census.py part C): enumerated all
390,625 = 5⁸ members of Σ_c ∩ B22 from the 14-pin definition (4 cluster + 6 below-line
+ 4 on-line digits (4,3,3,4)); developed f at Φ̂_r = x − 5r, r = 1, 2, exactly over ℤ.
Results: the P6 landing state held on ALL members (v(B₂) = 2 with unit digit 1,
v(B₁) ≥ 4, v(B₀) ≥ 5 — asserted per member, zero violations); RAM marginals
N(v_r = a │ Σ_c) = 78,125 for every a ∈ F₅ˣ, both branches; ALL 16 joint fibers
(a₁, a₂) EXACTLY 15,625. Matches the gate ledger verbatim (case_tn3_results.json A2 =
15,625, A3 identity 15,625·390,625 = 78,125²) and §T.4's F1 displayed instance
(5^{−14}·5^{−1}·5^{−1} = 5^{−16} = 15,625/5²²). Layer-A ledger: CONFIRMED.

## S5 — Fresh-emphasis checks

(i) PRE-HALT / BELOW-HALT MARK ACCOUNTING (ONE-F). Re-derived the dictionary: for
pre-halt η, "η realized by f" ⟺ "η is a chain of the PRUNED tree" is a function of
T alone (pre-halt-ness is η's own cell data; intermediate μ ≥ 2 pinned by η's values),
so the weighted-sum identity is exact on the decided set; the below-halt excess is
correctly quarantined (SIB-gate Layer-2 events and D4-n3's opened μ = 1 branches are
below-halt, and the note routes them out via Pref^τ). Layer C's "root-only marks ⟹
pre-halt restriction vacuous" checks: a root-window mark's only proper prefix is the
level-0 datum, and τ-hen cannot have fired on an m ≥ 2 track. Clean.
(ii) (SIB-STEP) SUFFICIENCY. TREE-EXP's induction consumes tree-indexed (SIB) directly
(prescribed-finite-subtree events at every realized branching prefix — the FIRST SPLIT
display is literally (SIB)'s quantifier, with exact-realization events: for complete
finite T_j with cell-data τ-halts, "realizes T_j" = an exact event, no containment
slack). (SIB-STEP) is support only; the (SIB-STEP) ⟹ (SIB) filtration lemma is listed
OPEN in both §T.3(iv)(c) and §T.5(1). No hidden dependence found. Clean, honestly
statused. One residual note: the stacked form is necessary as stated — the pass-1
counterexample (two contemporaneous uniform-but-identical reads) is real, and the
gate's L1 P2/P13 tests the stacked law only through word depth 3 at one cell.
(iii) (JC-multi) TAG PROPAGATION. The tag appears at TREE-EXP's statement, TREE-N,
the per-shape law, and §T.5(6); unbranched segments provably never consume it (k ≥ 2
sides ⟹ ≥ k branches — displayed); all gate cells single-side (checked: F1's root
window is full-span ONE side; my p = 3 windows likewise). No consumer in §T.4–§T.6
uses a multi-side jvol without the tag. Clean.
(iv) VP-SOUND: (HEN-LIFT)/(OM-SAT) are complete precise statements (checked: both
quantify correctly, (OM-SAT) consumed as a GMN cite, not derived from §B2-DEF —
consistent with §B2-DEF's accepted scope which supplies accumulation only). Citation
obligations correctly carried in §T.5(2), D4R.0-K(c) remainder = {VP-SOUND cites}.

## S6 — VERDICT

Sweep: 0 critical, 1 justification gap (S1-1: "the ACCEPTED JOINT-D1" mis-attributes
the root cross-cluster product — the accepted note is the single-cluster deg-2-key
tower ledger; the perimeter's actual root-product authority is the Montes-L1 cite
(D4R0K O4); the (SIB) tag saves the chain). (ns-null): PROVED — independent third
derivation clean; both break attempts failed against accepted (ZC)/(C.2) structure.
Own-instance TREE-EXP (p = 3, two instances incl. a two-reading-track joint tree):
ALL EXACT. T-n3 Layer A independently recomputed: CONFIRMED (16 × 15,625; P6 on all
members). Fresh-emphasis checks (i)–(iv): clean.

FABLE HALF-VERDICT: NOT CLEAN at the letter (the S1-1 gap is real and should be
repaired — one wording/citation fix: replace "ACCEPTED JOINT-D1" by the Montes-L1
cite or keep the (SIB) tag explicit at the root clause); CLEAN on all mathematics
this note owns. No critical error; no finding touches (ns-null), TREE-EXP's
mechanism, TB-CAP, ONE-F, or the gate ledgers.
