# THE RE-POINTED HK-11a KERNEL — σV-vertex law LANDED (post-wave unit 4, 2026-07-31)

Executing agent: post-wave-4 (BP2 sequencing HK-12 → HK-52 → **HK-11a**, per the HK-12
re-adjudication `notes/HK12_RECHECK_2026-07-31.md`).  Charge: build the honest closure
route for the V9 e′ = 1 sub-perimeter — the σV-VERTEX LAW = V4-(VERTEX) at the recorded
(S-a) STEP-1 regrade + the R3a anchor transport (V9 REV-2's ~300-line spec) — and
adjudicate each of the four reopened V9 sorries against it.  Statements BYTE-UNTOUCHED
throughout (verified: the V9 diff is comment-only).

## Artifact

`lean/LeanUrat/HC2/HK11a_vertexTransport.lean` (~650 lines, 0 sorry) — five public
theorems, ALL verified Lean-core `{propext, Classical.choice, Quot.sound}`
(in-file `#print axioms`):

* `HK11a.anchor_transport` — **R3a, PROVED** (the substantive new mathematics).  From
  SideReads(i)+(ii)-shaped data alone — support line ≥ gam with ties exactly the stride
  digits, recorded digit monomials — plus the node laws `hAnchor`/`hRanch`/`hpat0`:
  `wV(f) = gam` attained AND `HasAnchorK (Rv f) a Ranch`, i.e. `Rv f = T(a)·Ranch`.
  Mechanism exactly per the REV-2 spec: the min-sum decomposition of the ReadFrame
  residual over f's frame-i development (a fresh K1At-driven min-sum core: masked
  sub-developments + Fact B nonzeroness + `hRlt`/`hRadd` summed), hS5's shared unit
  matched against SideReads(ii)'s recorded scalar, and the stride-position arithmetic
  `(j − t★·gam)/e★ = a + k` (hAnchor + Bézout, e★-cleared) making the tie positions
  consecutive from the anchor — the tie sum IS `T(a)·Ranch`.
  **SideReads(iii) NOT consumed** (the ⚠ U31 fence holds); `ord_ψ` is the node field.
* `HK11a.regrade_w_eq_childW` — HK-12 F1's compiled half: `RegradeOf`'s
  `IsSlotMinWeight` clause pins `σV.w = childW σ σ.Φ e★ h★` on every nonzero
  polynomial (attained-slot-min uniqueness against `S2_childW`, via `L0_FactA_exists`).
* `HK11a.sigmaV_vertexLaw` — **THE KERNEL (†)**: at a coherent `ReadsOf` record,
  non-recentering read i, for f's development in the RECORDED child key and EVERY
  recorded regrade σV of the read pair:
  `B μ ≠ 0 ∧ σV.w (B μ) = gam − μ·e★·h★·g ∧ (∀ m < μ, B m ≠ 0 → gam − m·e★h★g < σV.w (B m))`
  — (VERTEX) + strict (BOX).  Supply: R3a fires `V4_readLanding` at `V2_readFrame`'s
  choice-built frame; the regrade lemma transports the landing.
* `HK11a.readVertex_e1` / `HK11a.readBox_e1` — V9's two displayed ℚ-forms
  (`V9_K1nonrec`/`V9_E1box_nonrec` conclusions, binder-mirrored), derived from (†)
  through `HK52.hk52_qform_stretch` (the ν_{i+1}.e-stretch — the IDENTITY at the
  carried `he'1 : ν_{i+1}.e = 1`) + the γ-tie/slope-law/hLineU scale arithmetic
  (`lineAt_arith`: `STR_{i+1}·lineᵢ.at(j·cwᵢ) = gam − j·e★h★g`, division-free
  linear_combination).

## The carried hypotheses (the kernel's honest conditionality, all NAMED)

1. `he'1 : ν_{i+1}.e = 1` — the HK-12 F4 ADJUDICATION ITEM (unruled; the RG-2 fence
   covers the PARENT stride only).  This is where ruling option (i)'s guard would land.
2. `hsteep : νᵢ.e·σᵢ.h < νᵢ.h` — STRICT read steepness (`ReadPair`).  Per F5 genuine
   h-steep reads live at i = 0 / immediately post-recentering ONLY (at interior
   non-recentering-predecessor reads `child_h` pins σᵢ.h = νᵢ.h — not steep, regrade
   weight-idempotent, HK-08 restatement territory, NOT this kernel).  The old
   derivation suppliers (`V9_transSteepness` etc.) were wave-refuted; root-read
   steepness is (I-aug) classifier-semantics content — hypothesis class.
3. `hσL : StageCoreL σᵢ` — tower provenance of the recorded frame (the V2/V4 engine's
   standing hypothesis; `ReadsOf`/`HistoryCoherent` do not record it).

## V9 sorry-by-sorry adjudication (charge item 2 — NO fills landed, by design)

Re-read of all four reopened V9 statements confirms: NONE genuinely restricts to
ν_{i+1}.e = 1 — each quantifies over histories with arbitrary child stride (F4's
FALSE-SUSPECT face), and none carries hσL/hsteep.  Therefore NO sorry was filled:

* `V9_K1nonrec` STEEP leg (e·g ≥ 2 ⇒ e★ = 1, g ≥ 2): waiting supplier =
  `HK11a.readVertex_e1` (named in the leg's UNIT-4 addendum).  Blocked on the F4
  ruling + the hσL/hsteep supplies.
* `V9_E1box_nonrec` STEEP leg: waiting supplier = `HK11a.readBox_e1` (same blocks).
* BOTH CORNERS (i = 0 ∧ e·g = 1): the kernel does NOT cover them — `ReadPair` needs
  strict steepness, underivable at e·g = 1 (the R3c record: root data consistent with
  h ≤ e·σ₀.h).  The corner e′ = 1 route stays the v9c-toolkit re-plumb
  (`RegradeOf.w_coeff` folds σV.w to σ₀.w), blocked on the same ν₁.e ruling.
  Addenda updated naming this disposition.

## What this changes in the BP2 map

* HK-11a — **LANDED as re-pointed** (this unit): the σV-vertex law is now a PROVED
  Lean-core asset; the "~300-line V4-shaped kernel" of the REV-2 record exists on disk
  (`anchor_transport` + `sigmaV_vertexLaw`).  BP2 HK-11a's original displayed
  bottom-window law stays REFUTED verbatim (HK-12 F3) — never proved, never consumed.
* HK-11b — its e′ = 1 plan now has its hard supplier; still blocked on the ν_{i+1}.e
  adjudication (HK12_RECHECK §ADJUDICATION, dispositions (i)/(ii)/(iii)) AND the new
  named supplies hσL/hsteep (surface with the same ruling: candidate disposition =
  named hypotheses on the V9 legs, the R-tie guard precedent).
* HK-11c — unchanged (waits on HK-11b).
* The four V9 sorries STAND (census unchanged: 4 bare sorries in
  `HC1/V9_K1nonrec.lean`), each with a UNIT-4 addendum naming its waiting supplier.

## Verification

* `lake env lean LeanUrat/HC2/HK11a_vertexTransport.lean` — green, 0 sorry; in-file
  `#print axioms` on all five theorems = Lean-core exactly.
* `lake env lean LeanUrat/HC1/V9_K1nonrec.lean` — green after the comment-only addenda
  (diff insertion-only).
* Fence audit: the new module consumes SideReads conjuncts (i)+(ii) and node fields
  only; grep confirms no reference to SideReads(iii)'s `σ.R f` anchor clause.
