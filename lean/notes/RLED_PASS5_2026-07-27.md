1. **CLEAN — (EQ-2) is now refutation-stable.** Its comparison domain is independently pinned: “at a fixed shape position whose species is δ-STABLE … matched per (a6)/(EQ-1)’s owed dictionary.”  
   **Falsifier:** after that dictionary identifies a genuinely δ-stable position class, exhibit any δ for which either the re-based piece maps are not \(F_\delta\)-linear or
   \[
   a_\delta\ne \delta a.
   \]
   A residual-factor split is not a falsifier because the original position class does not persist; its alphabet is redistributed among new branch realizations. The displayed \(F_3\to F_9\) example correctly distinguishes the confirming shallow position from the out-of-domain full position. REL-n4 explicitly inherits this same domain restriction.

2. **CLEAN — (REL.2a2) is correctly typed and fenced.**  
   “\(E:=\prod_r e_r\) and \(F:=\prod_r g_r\)” binds both quantities to C.0’s per-read SHAPE fields \((e,h,g,\mu)\) accumulated along \(b_j\). The saturation identity and consequent divisibility are stated as **EXPECTED**, walked in the future (a)-pass, not assumed. Since \(\delta_j^{\mathrm{rel}}\) is the accumulated product of the same residual degrees \(g_r\), this is the correct route to the integrality of \(d_j^{\mathrm{rel}}\).

3. **CLEAN — R.0 and the honesty banner agree.** The unconditional R.0 sentence is limited to the ambient \(v_p\)-scale, uniformizer, and value group. MOVES heights, stretches, and \(\kappa_r\) remain explicitly inside the PREDICTED (R0-tower) bracket. No obligation is discharged, and no formerly open invariance is used as an input. The worked split example diagnoses the comparison domain; it does not establish an obligation.

4. **CLEAN — end-to-end ledger sweep.** REL.1 remains a new open re-scoping theorem; REL.2(a)/(b)/(d)/(e) remain separately typed obligations; (c) has one owner at §T.3; (SIB-Oδ) is properly fenced; absolute indices compose consistently; β-substitution requires all of (e1)–(e5); REL.3 remains a deferred-statement obligation; and REL-n4 remains an outlined, non-discharging gate with its runnable specification owed. W1–W2 are correctly recorded as cross-note duties and do not create an internal defect in §R-LEDGER.

**Counts:** 0 critical findings / 0 gaps.

**Verdict: ACCEPTED (0/0).**
