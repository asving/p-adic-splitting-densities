# FABLE CONFIRMING PASS — §R-LEDGER rev 5 (2026-07-27, round 2)

Verifier: fresh Fable instance, confirming half of the dual-acceptance
shot, round 2 (Codex parallel on identical text). Text:
MOVES_2026-07-24.md §R-LEDGER (lines ~9297–9769, rev 5). Predecessor:
FABLE_CONFIRM_RLED_REV4_2026-07-27.md (rev 4: 0 crit / 1 gap G1 + notes
N1/N2). Charge: (a) verify the two rev-5 edits, incl. the
relabel-vs-N2 honesty question; (b) confirming sweep at fresh emphasis;
(c) one own-construction check (/tmp/rled_confirm_rev5.py).

## Verdict summary

CLEAN: 0 critical / 0 gaps. Both rev-5 edits genuine and mutually
consistent; the relabel's forward reference is honest BECAUSE the (g2)
fold strengthens N2 from a warning into a spec mandate (§B). Residue:
1 NOTE (N3, §C — a quantifier-alignment duty for REL.1's future pass,
no present assertion wrong) + 1 cosmetic remark. Predecessor's list is
exhausted.

## A. Rev-5 edit verification (G1 relabel; N1/N2 fold)

EDIT 1 [PASS] — the (EQ-2) relabel (G1). The display now reads "the
shallow position a = 1 gives a_δ = 2 = δ·a NUMERICALLY — but ψ's split
changes the species at BOTH positions, so even the shallow comparison is
OUT-OF-DOMAIN by this pin's own clause". Grep over the live section:
ZERO instances of "(law holds)" remain. The shallow half is now a
numeric observation, not an adjudication; the pin's own domain clause is
applied symmetrically to both positions (exactly G1's complaint); R.5's
"LITERALLY true of every display" banner is no longer punctured — the
display asserts arithmetic facts and domain exclusions only.

EDIT 2 [PASS] — the (g2) fold. New bracket: "N1 — μ is unpinned and
μ = 1 gives a trivial degree-1 box, pin μ ≥ 2 or justify; N2 — z²+z+1
splits over F₄, so the exponent leg may have an EMPTY in-domain instance
set at this family: (g2) must exhibit at least one δ-stable-species
position or pick a different family". N1 faithful (trivial μ=1 box =
d_j^rel = 1, no refinement — predecessor's content). N2 STRENGTHENED:
the predecessor allowed a declared masses-only gate; rev 5 mandates a
non-vacuous exponent leg as a (g2) spec requirement. Strengthening is
legitimate and load-bearing for Edit 1 (§B).

## B. The honesty question: relabel's forward reference vs N2's possible vacuity

The charge: "the law's first in-domain test is the gate's" vs N2's
warning that the gate family may have NO in-domain instance. VERDICT:
CONSISTENT AND HONEST, by composition of the two edits. (i) The
relabel does NOT assert the gate HAS an in-domain instance; it points
the law's first test at the gate. (ii) The (g2) mandate makes a
non-vacuous exponent leg a precondition of the seal: either (g2)
exhibits a δ-stable-species position, or the family is re-picked, or
the gate SPEC-FAILS before sealing (the (g7)-style default) — in no
world does a sealed gate run a vacuous exponent leg while (EQ-2)
points at it. (iii) The gate is typed OUTLINED — RUNNABLE SPEC OWED at
its own site and in R.5, so the forward reference is to an owed test,
fenced as such. Residual reading risk (not a gap): a maximally careful
clause would say "owed to the gate per (g2)'s requirement"; the
two-paragraph separation between (EQ-2)'s pointer and (g2)'s mandate is
acceptable since both sit inside the same note and cross-cite the same
Fable-confirm source. If the mandate ever proves unsatisfiable at
p = 2, n = 4 (no family works), the gate dies at spec time and (EQ-2)'s
pointer must be re-aimed — the correct failure mode, on the record.

## C. Fresh sweep (unprobed by predecessors)

Probed at fresh emphasis: BASE-INDEX composition, R.2's (e1)–(e5)
glosses, (R0-ledger)'s back-cite, gate-outline inheritance, WATCH ledger,
R.5 dating. Results:
 (i) BASE-INDEX / (SIB-Oδ) recursion arithmetic [PASS]: δ_j = δ·δ_j^rel,
   q_{δ_j} = p^{δ_j} = q_δ^{δ_j^rel}; recursion targets O_{δ_j·δ″};
   subring chains valid (δ_j ∣ δ_j·δ″ in W(F̄_p)); indices absolute
   throughout, including the R.2 consumer sentence.
 (ii) R.2 closing paragraph vs (REL.2e) [PASS]: the five glosses (state
   index / exponent / mass=entry / first index / typing) match (e1)–(e5)
   one-for-one; PCI bookkeeping and CL-8 line carry the same five-count.
 (iii) (N3) NOTE — LINEARITY QUANTIFIER ALIGNMENT (fenced-side, not a
   defect). (EQ-2)'s pinned law states F_δ-linearity of re-based piece
   maps ONLY inside the δ-stable-species quantifier; but (R0-ledger)'s
   "a q_δ-power only under (EQ-2)'s OPEN prediction" and walk item
   (iv)'s "whether the re-based maps are F_δ-linear ... is (EQ-2)'s OPEN
   prediction" need linearity INTRINSICALLY — at every re-based
   position, including new branch positions born from splits, which have
   no matched ℤ_p counterpart and sit outside the pinned domain. No
   present assertion is wrong (everything is OPEN/fenced, and "only
   under" is necessary-condition wording); the duty: REL.1's pass should
   predict linearity intrinsically (per re-based position) and the
   exponent law per matched class. One clause, owed at the REL.1 draft,
   not now.
 (iv) Gate-outline inheritance sentence vs the (g2) fold [PASS]: "enters
   through its own re-based families in (g2), never as an
   exponent-comparison instance" composes exactly with N2's mandate.
 (v) COSMETIC: R.5's banner still opens "and at rev 4 that is LITERALLY
   true" — true at rev 5 too (the edits derive nothing); reads as a
   historical date. No action needed; optionally re-date at dual
   acceptance.

## D. Own-construction check

/tmp/rled_confirm_rev5.py (exact arithmetic, pure python), ALL PASS:
z²+z+1 the unique irreducible monic quadratic over F₂ and splits over F₄
(N2's witness re-derived); F₄ ⊗_{F₂} F₄ decomposes (2 distinct roots —
the (EQ-2) gcd caveat's smallest case); SIX irreducible monic quadratics
over F₄ — the re-based W(F₄) stage-2 ledger has genuine inert letters,
so (g2)'s mandate is plausibly SATISFIABLE (supports, does not prove,
the forward reference's dischargability); the (EQ-2) display's numbers
re-derived a third time (shallow F₃-span of {(1,1),(i,−i)} in F₉×F₉ has
dim 2 = δ·a; full per-branch 𝔸_δ = F₉ gives a_δ = 2 ≠ δ·a = 4).

## Final verdict

CLEAN: 0 critical / 0 gaps. Edits verified: the G1 relabel is complete
(no "(law holds)" anywhere; both positions adjudicated by the pin's own
clause) and the N1/N2 fold is faithful-and-strengthened; the relabel's
forward reference is honest precisely because (g2) now mandates a
non-vacuous exponent leg before any seal. Residue: N3 (linearity
quantifier stated per-δ-stable-position, consumed intrinsically —
one clause owed to REL.1's future pass) + the cosmetic rev-4 banner
date. This verifier's list is exhausted; the Fable half of the rev-5
dual-acceptance shot is a pass.
