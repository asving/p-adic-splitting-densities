# FABLE VERIFIER — §S-RESUM rev 3 (fresh instance, 2026-07-27)

Charge: quote-and-classify, fix nothing. Dual bar: Codex passes 3+4 CLEAN
on identical rev-3 text; this is the independent Fable half.
Text verified: /tmp/MOVES_snapshot.md lines 11557–12467 (snapshot taken
05:30Z — the live file is under concurrent edit; all quotes from snapshot).

## A. XRB delivery — (β-1)/(β-2a) re-derivation + hypothesis-attribution break attempts
(pending)

## B. (K-SUB) vs §V PART clauses — independent audit
(pending)

## C. (BDY) edge convention — §V/§T quote check + boundary instance
(pending)

## D. Per-pool E0 quantifier vs RS.1's base-changed legs
(pending)

## E. Fresh sweep (own emphasis) + own-construction check
(pending)

## Verdict
(pending)

## A. XRB delivery — re-derivations + break attempts: ATTRIBUTIONS HOLD
(β-1) re-derived from the snapshot's §V/§H displays. Move (i) = §V (U-A)
(7960-69: mass a function of the nominal inventory, never of pinned values);
moves (ii)/(iii) = (u-R)/(u-T) exactly as §V displays them (7936-41), and
§V 7971-76 confirms verbatim the note's claim that these are the NOT
§C-derivable lumpability clauses — the [2b] fence line is faithful. Move
(iv): XHD-d's two-sided exactness (7947-49) is representative-quantified;
history-dependence of T_γ is killed by (u-R), which is IN the hypothesis
set. Break attempt on (β-2a) (smuggled (iv)-REP or HMC): FAILS — kernel and
terminal entries are k=1 objects (§V 8144-46), where §V 8153-56 licenses
T·G from (U-A)+(u-R)+(u-T) alone, no HMC ("no HMC at k=1" matches); the
multi-step clause claims only h_ent-INVARIANCE of the unfactored (COMP-hΣ)
sum and defers factorization to HMC (CL-5 item 6) — checked against §V's
(COMP-hΣ)/(COMP-Σ) split (8239-45), which carries exactly that division.
Representative-independence enters only through (u-R) (inside XHD-u), not
through CTS-M(iv)-REP. Break attempt on (β-2b)'s (SIB) placement: FAILS —
§T.3's display (7292) conditions on Σ_c, i.e. AFTER the branching cell, so
the note's "supplies no selection law" is exact; the selection law rides
(JC-INV), which is genuinely new (checked: (u-R) is template/per-assignment
indexed and does not quantify joint branching cells — even single-side
branching sites are only conservatively covered, so the extra hypothesis is
honest, not redundant). Leaf/halted children: §T.4 "a leaf child has
S_{T_j} = the sure event given c, factor 1" — consumed, see finding F2.

## B. (K-SUB) vs §V PART clauses: ONE JUSTIFICATION GAP (F1)
PART-1/PART-2 retyped forms re-read at §V 8165-83. (K-SUB)'s at-most-once
count for same-size marks is sound (2e>e; e+1>e — verified). PART-2's
surplus correctly excluded (J is cell-level, per cell once; the row deficit
reading in probe (a) is consistent). F1 (GAP): the exhaustion behind
"= 1" needs every digit cell to be terminal, same-size-single-child, or
branching — i.e. "m=1 continuing ⟹ e₁ = e". The DISPLAYED DEG-CONS clause
constrains only BRANCHING outcomes ("m ≥ 2 children ... Σ_j e_j ≤ e"); a
size-dropping single-child cell (m=1, e₁<e) is not excluded by any display
and would fall in NO summand of (K-SUB). Plausibly M1's degree identity
forces the deficit into halted siblings (making the cell branching, m≥2),
and CL-11's full-roster check would catch violations — but that inference
is not displayed. Classify: justification gap, repair = one sentence
extending DEG-CONS's display to the single-child case.

## C. (BDY) — quotes verified; boundary instance PASSES
§V (iii) (8082-92): ε retains the designated continuing branch b entering
the block landing in β₀ — so the entering read is entrance-side, matching
(BDY); (COMP-h) (8234-38) starts the block product at β₀'s first in-block
read with ι_{e,ε,β₀} as the sole entrance factor — no read omitted or
double-charged, exactly as (BDY) states. (CUT-2)'s node/edge rule agrees
(first entrance node = state index; edge into it = entrance mass), and the
entrance predicate's SHALLOW P̂-clause correctly fences deep-split children
out of re-classification. Constructed instance (/tmp/sres_check.py, p=5,
n=2, exhaustive mod 25): entrance cell (x+t)², t=0: ι = 5⁻²; depth-1
ram-halt conditional 20/25; joint mass 0.032 = ι·(T·g) EXACTLY (no double
charge of the root pins). Same row (20/25, 5/25) from the t=1 entrance
representative — a passing (β-1)/(u-R) instance across two entrances.

## D. Per-pool E0 vs the base-changed legs: QUANTIFIER ADEQUATE (one WATCH)
Walked leg: n=3, RS.1-SH shape with a block entrance e_i=1, δ_i=2 (the
unramified-quadratic subfield leg): its value is β_{1,τ}(q²) read at
q₀ = p². What the leg needs: (numeric well-definedness of the evaluated
entry at q₀) = active-submatrix solve + ii-c agreement AT q₀ = p² — and
the rev-3 kernel supplies exactly this: δ ranges "over 1 AND every
base-change index a β_{e_j,τ_j}(q^{δ_j}) leg of b_e^split or RS.1-SH
consumes", with A(q₀) per pool and ii-c's agreement burden stated PER POOL.
The (e3)-FENCE correctly blocks the circular escape (REL.2(e3) presupposes,
never supplies, well-definedness). Gate scope (q₀=2,3, +4 as feasible)
narrows no quantifier — stated twice (S.4, S.7(iv)). F5 (WATCH): closure
under NESTED base change (a δ-leg inside an already-base-changed block →
pool p^{δδ′}) rests on reading "every leg ... consumes" across all
recursive instantiations of b_e^split; the ABSOLUTE-δ convention ([2r]
(e2)) makes that the natural reading and the pool set is finite (sizes
strictly shrink), but the seal artifact should display the full
relevant-pool list for n=3 to pin it.

## E. Fresh sweep (own emphasis) + remaining findings
F2 (GAP, b_e^split leaf-member typing): the display "∏_{j=1}^m
β_{e_j,τ_j(o)}(σ_j)(q^{δ_j})" with j over ALL individuated branch-set
members leaves halted members untyped — β_{e,τ} is defined (S.0 TYPING)
only for block states τ of 𝒞_n; a τ-halted member has a verdict, not a
block state. §T.4's convention (leaf child = sure event, factor 1, verdict
feeding the type multiset) is plainly intended and consistent, but is not
restated or cited at the display, and mixed cells (halt + continue
children) are exactly the b^split case at n ≥ 3. Repair = one parenthesis.
F3 (WATCH, wording): (β-1) says mass "of one continuation realizing
(γ, h)" while m_γ(h) includes the count factors ∏T — the EVENT mass over
all assignment chains; the derivation (moves ii/iii) makes the intent
unambiguous, but the noun phrase under-describes the displayed object.
F4 (WATCH): S.4(ii)'s "at all-active primes ... K_e(p) = A" presumes every
listed state's cell-size polynomial ≢ 0 (a state empty at EVERY prime
would make full-matrix det ≠ active det at all p); realizability of the
[2b]-owed typed list 𝒯_n presumably supplies this — not stated.
Swept clean at my emphasis: (R_e-lump)/(R_e-exact) vs (NS-ROUTE) (no
additive ns column anywhere; residual outside the recursion); (K-TRI)'s
triangularity; (CUT-1/2/3) logic incl. deep-split non-reclassification;
RS.2's Cramer route and q ↦ q^δ ℚ(q)-stability; RS.3(ii)'s
infinitely-many-primes argument (modulo F4); RS.4's inheritance of the
FULL RS.2 chain (correctly widened at rev 2); S.6 perimeter = body;
S.7 ORC-1..4 typing and seal semantics; the consumption-DAG acyclicity
claim (S.1 consumes neither PCI site — verified against S.1's actual
hypothesis usage); the MONTES-L1/JOINT-D1 authority split (matches §T.3).

## Verdict
Break attempts: (β-1) hypothesis attribution HOLDS; (β-2a) REP/HMC
smuggling attempt FAILS (no sneak); (β-2b) (SIB) tag placement HOLDS and
(JC-INV) is genuinely load-bearing; (K-SUB) audit yields F1; (BDY)
instance PASSES numerically; per-pool E0 quantifier ADEQUATE (F5 watch).
Counts: 0 critical, 2 justification gaps (F1 DEG-CONS single-child
exhaustion display; F2 b_e^split halted-member typing), 3 WATCH (F3-F5).
Both gaps are one-line display repairs; neither breaks a logical chain.
NOT CLEAN (2 gaps) — rev-4 display repairs indicated; no structural
objection to the [3]-COND architecture.
