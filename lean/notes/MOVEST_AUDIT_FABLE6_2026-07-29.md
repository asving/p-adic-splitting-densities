# MOVEST round-6 Fable verdict (2026-07-29)

{
  "verdict": "REV 6 NOT ACCEPTED: 2 crit (toy tables), 3 gaps",
  "criticals": 2,
  "gaps": 3,
  "findings": "CRIT-1 (blueprint §T-G1 ~3969-4027 + §0 record 13 claim \"hred is instantiable\"): the displayed toy `cellOf .red` guards read NEITHER g NOR the reduction coords x0/x1, so the CellAssign field `child_cell_red` (∀ χ g) is FALSE on BOTH carriers — decide REFUTES it. Witness (carrier A, χ=toyχ, g=(1,0), x=(1,0,0,…,0)): x ∈ rootCell χ g; toyCellA (.red (1,0) X) x = winC (only ψ=X ∧ x2..x5=0 checked) → tAν₁ ∈ branchSet, but toyChildA none tAν₁ x demands x0=0 — conclusion false. Same witness kills carrier B and `RedCellPartition`'s second conjunct, so `toyCA`/`toyCAB` are uninstantiable as displayed and the C-1 row's \"(+ ψ-guards … so RedCellPartition's uniqueness conjunct decides)\" / \"hred is instantiable ✓\" walk-through is unreal at any g ≠ toyG (the walk-through checked only g = toyG). Fix shape: add x0=0 ∧ x1=0 to both winC guards — cellEventE at the true entrance and every census integer unchanged (verified: A 2³, B 2⁶). The mem_single recomputation itself IS real (see disposition check).\nCRIT-2 (§T-G1 3985/4027, `cellLevel := fun _ => 3`): `cell_local` is FALSE on both carriers by decide. levelIdx (n=2) gives coords 4,5 level 3, so the premise constrains only c∈{0..3}; toyCellA .st reads x4,x5 (toyCellB .st reads x4): x = 0⃗ → splitC vs x' = 0⃗ except x'4=1 → junk, premise satisfied. Needs cellLevel ≥ 4 (then levelIdx c < 4 ⟺ c < 6 covers every read coordinate; all integers unchanged). Both criticals falsify the blueprint's \"every DATA field is displayed; only proofs sit under the `…` — G1a decide obligations\" (proofs that cannot exist), the exact class Fable-5 C-1 established as rejection-grade.\nGAP-1 (§2.4 `KBTotTower.root_total`, lines 651-658, vs note 7112-7115): the quoted covering display is PER LIVE TRACK (\"fix f and a live track … m_i ≥ 2 opens the window — total\"), but the Lean leg concludes only ∃ SOME root child from ∃ SOME repeated factor. A model with f̄ = ψ₁²ψ₂² realizing only ψ₁'s track child inhabits the closure while the note rejects it — a silent weakening of the transcribed display (the faithful form ties the child to its track via the available `trackOf`: ∀ ψ repeated, ∃ ν, trackOf ν = ψ ∧ child none ν x). The record-14 childless tower does die, so the claimed repair is real but under-quantified; undisclosed.\nGAP-2 (stale seam refs, W4_SYNC delta 6): W4-4/T-D7 were written against pre-repair MovesD. MovesD now SHIPS typed `NPIDPin`+`NthrSup`+`noteNP`+`D4R4_all_noteRange`; T-D7's E-phase-bracketed `transportedLevels`/`bandLevels` and the W4-4 row's \"landing deletes D4R4_all's conditionality pointer\" violate the blueprint's own REV-4 rule (a value existing in a built corpus must be imported by name, never re-bracketed) and the DISCHARGED row must be re-judged against the built types.\nGAP-3 (stale refs, deltas 7-8): the CONSUMES roster (line 98: Presented `fiber/event/mult`) and T-F4's `S.mult` predate `Presented.eventO/multO` + the D14c empty-shape dispatch (\"uniform-event consumers should key on eventO/multO\") — T-F4/T-C2's empty-shape leg needs the multO/eventO keying or a recorded deviation; and the §5 count \"4 (MovesD W4-1…4)\" predates the now-existing W4-5 (`OffsetPPin`/`CD_canonical`) — whether wave 4 owes a W4-5 row (polTriv vs `lift_ne_zero`) is unadjudicated. Minor: hRes cite \"(7213)\" sits at note 7216-7217.",
  "dispositionCheck": "All 15 §7 rows verified against text: #1 root_total+χat present, consumed by VPPinned.covering — real (but GAP-1); #3 Tsub.Finite threads to all 6 named sites, note quantifier 7287-89 verbatim; #4 track-lawfulness conjuncts displayed; #6 flip reversed, direction verified against the note (f̄=x² has no simple factor → henPayload=∅ on the fiber, clause (iii) empties the flipped fiber; pair coexists); #7 hScale/hRes displayed with note clauses; #9 belowEnt/SubtreeFiber/TreeScaffold take trackOf; #10 ReprInj on all 3 fibOf sites; #12 shapeExp displayed + E10a/b split; Fable G-1 slopes 1/2/3 lawful (tie/hh/chains/StrictMono check); G-2 roster counts 12. Tally 11F/2F*/2BDY = 15 ✓. RECOMPUTED: rev-5 mem_single violations 128−8=120 (A), 128−64=64 (B); rev-6 = 0 both; x* fails both sides; 8=2^{9−6}; toyA=2+4=6; 2³·2⁷·2²=2¹²=2⁹·2³; 2⁴·2²=2⁶; F1: 5^{22−14}·5^{10}=5^{18}=card(rootCell); clash 0<5⁸<5^{18} ✓. HELD items on quotes: hdict (7537-40) — note states per-site shape-determination with external warrants; pointwise premise ≠ conclusion; licensed. W4-1 BDY — degenerate model dies inside consumed vp; boundary disclosure accurate; licensed."
}