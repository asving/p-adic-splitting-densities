# MOVESV RATIFICATION ROUND 5 (2026-08-01): RATIFY 0M/1N — all three round-4 materials verified (TablePins real ties + honest measured inventories; the Tgam-keyed VAL(b) IN the capstone via hRC; the five-class census complete with exhaustiveness). The fenced inventory confirmed exactly: the 3 Phase-B instantiation duties + the banked w17ii. ALL THREE CORPORA NOW RATIFIED.

I’ll audit the round-5 tree directly under the stated record-accuracy rule. I’m checking the three repaired surfaces against their exact types and comments, then scanning the proof-hole inventory and compiling the relevant MovesV target if the workspace supports it.

# Verdict: RATIFY (0M/1N)

All three round-4 material findings are repaired under the record-accuracy rule.

- **M1 — verified.** `StateOf` now includes the real `CellIdx`; `tableShape_inst` transports the actual `C.Wloc` and block `Wstate`; `TablePins` ties `VType`, states, outcomes, bounds, and `odata` inventories including target skeletons. The three seam records now explicitly admit everything still untied—including measured cells, events, masses, rows, entrances, and target cell indices. The former overclaim is gone.

- **M2 — verified.** `ValB_Stmt` is keyed to `Tgam`; `CtsmLedger` contains `hRC : RealizationComplete`; `ctsM.val_b_chain` invokes `val_b` with `L.hRC`. The table-side `chainCount` result is correctly demoted to `val_b_table`. The ownership record also correctly says no-stray/positivity does not prove realization completeness.

- **M3 — verified.** `rootTable_census_TH12` and `rootTable_census_TH3c` prove the two missing classes over arbitrary finite fields. `profile_mem_five` and `census_partition` establish exhaustiveness, and `rootTable_census_complete` assembles all five semantic counts. The weaker block-2 theorem and the external “178 checks” are now accurately scoped as such rather than promoted to kernel results.

The accepted fenced proof-hole inventory is exactly:

1. `ledgerIV_inst`
2. `ratBurdens_exists`
3. `scs_data_supply`
4. `w17ii_wave4`

The first three are accurately recorded Phase-B instantiation duties. The sole narrow reservation is the banked W17ii clause, which remains excluded from proved content and is candidly documented.

A fresh `rg`/Lean rebuild could not run because the execution sandbox lacks its `bubblewrap` launcher. This ratification is therefore a statement-and-dependency audit of the complete supplied corpus.