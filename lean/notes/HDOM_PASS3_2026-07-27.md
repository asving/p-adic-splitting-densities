1. **CRITICAL — H-LIST is only PARTIALLY fixed.** The refined-stride claim

   > “KEY PROPERTY: \(a_{ij}S_j\in S_i\mathbb Z\)”

   does correctly imply that, inside a fixed refined residue cell, \(\phi_i\) changes by multiples of \(S_i\). Thus the ceiling’s fractional contribution is cell-constant, and the displayed \(y\ge x/2+1\) example is correct.

   But the stated class (TRI) does not require its affine lower bound to dominate the ambient constraint \(h_i\in\mathbb N\), nor does it restrict slopes/constants sufficiently. For example,
   \[
   x,y\in\mathbb N,\qquad y\ge x-2
   \]
   is a (TRI) system with one affine lower bound, no upper bound, and no congruences. Its least admissible value is
   \[
   y_{\min}(x)=\max(0,x-2)
   \]
   (or \(\max(1,x-2)\) under the positive-\(\mathbb N\) convention), which is not affine on the sole refined cell. The even sharper negative-slope example \(y\ge1-x\) also lies in the class as written. Correspondingly, the claimed triangular periods can acquire negative coordinates, contrary to \(P\subseteq\mathbb N^D\).

   Therefore

   > “\(S\cap\operatorname{cell}(r)=L(h(r,0);\{v_j(r)\})\)”

   is false for the full stated (TRI) class. It is valid for the actually used adjacent-only systems once their positive lower bounds are known to dominate the ambient floor, but that restriction is absent from the lemma. Pass-2 H-LIST finding: **PARTIAL**.

2. **PASS — the refuting example and gate-family calculation are correct.** For \(y\ge x/2+1\) with both variables even, \(S_y=2\) and \(S_x=4\), giving exactly
   \[
   y_{\min}=x/2+2\quad(x\equiv0\bmod4),\qquad
   y_{\min}=x/2+1\quad(x\equiv2\bmod4).
   \]
   For the gate, the only cross-coordinate slope is \(2\), so \(S_1=2\) and \(S_0=\operatorname{lcm}(1,2)=2\). The two listed parity components and all sealed numerical data are unchanged.

3. **PASS — pass-2 coefficient finding is GENUINELY fixed.** H.1.2(ii-a) now distinguishes an integer increment of at least one along an admissible lattice step from the raw-coordinate coefficient, for which only strict positivity is asserted. Convergence continues to depend solely on the fenced per-period E-POS condition.

4. **PASS — L4 re-attribution is GENUINELY fixed.** The \((0,2)\) leg remains within one listed component. The coarse \((1,2)\) leg alternates the two \(h_0\)-parity components:
   \[
   (1,3)\to(2,5)\to(3,7),
   \]
   so it is correctly classified as the first, mild XHD-u-X count-face test. The remaining fence is honest: factorization is still untested, and no heterogeneous pair of components differing in twist or alphabet data has been exercised.

5. **GAP — stale revision label.** H.5.2 still labels its current delivery column

   > “THIS NOTE, REV 2”

   although the audited text is rev 3. The table’s substance reflects rev 3, so this is a record defect rather than a mathematical one.

Pass-2 repair classification: **2 GENUINE / 1 PARTIAL / 0 NOT FIXED**.

Counts: **1 critical / 1 gap**.

**Verdict: NOT ACCEPTED.**
