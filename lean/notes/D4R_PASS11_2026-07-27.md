1. **Rev-9 repair — CLEAN.** The retained-height decision is mathematically sound. For \(e_r\ge2\), Bézout gives \(t_r^B\ne0\), and
   \[
   p_j=(j-t_r^B\gamma_r)/e_r
   \]
   indeed recovers \(\gamma_r=(j-e_rp_j)/t_r^B\), hence \(u_r^*\). For \(e_r=1\), the prescribed Bézout pair has \(t_r^B=0\), so \(p_j=j\); the stride data and anchor contain no \(\gamma_r\)-information. Non-adjacent transition inequalities constrain rather than determine the side’s vertical translate, so distinct integral heights can give the same remaining shape fields and different strata/fresh depths. At the root there is even no preceding-line condition; thus “the root included” is slightly loose terminology, but strengthens rather than damages the non-recoverability conclusion.

2. **Retained-\(u^*\) propagation — CLEAN.** L3 includes \(u_r^*\) and \(\gamma_r\); the A-ledger explicitly consumes it; L9 uses it to make the fresh-volume exponent constant on \(\operatorname{Pref}(\widehat P)\); L11 includes it in the shape component needed for injectivity. \(W\) correctly remains independent of \(u^*\), since it uses only pool degrees and stride-set cardinalities. The displayed D4-n3 and D4-m2 sides pin the relevant right-endpoint heights, so their shapes were already \(u^*\)-complete and no sealed quantity changes.

3. **Citation repair — CLEAN.** The opening, L3 realizability discussion, D4-R.5 status paragraph, and D4-R.6 SAE bracket consistently cite the live §C rev 14 and correctly state that the rev-12 \((\mathrm{SAE})\) repair landed and was dual-accepted. These statements agree with the supplied §C rev-14 text.

4. **End-to-end branch-tree architecture — CLEAN.** The tree is a well-defined function of \(f\), while side and factor choices remain branch data. Digit cells, branch sets, single-history §C cylinders, multiplicity-weighted sums, and event unions are kept distinct throughout. No withdrawn single-run, per-node-partition, or disjoint-fiber claim is silently reused.

5. **L6–L7 identification — CLEAN, conditional as advertised.** Empty histories are handled separately; nonempty histories use Theorem C only inside its stated quantifier. The reverse induction uses common frames, digit-cell exclusivity, branch-set eligibility, and canonical lifts without assuming node-stratum disjointness. Prefix membership and terminal maximal-branch membership are not conflated.

6. **D4R.1 and D4R.4 — CLEAN.** The exact statements are sums over the finite branch index set; event statements are union bounds. The equality criterion is correctly upgraded from almost-everywhere to pointwise because all relevant fibers and their intersections are finite-level cylinders, so a nonempty overlap has positive mass. Empty-prefix and empty-\(\operatorname{Pref}\) cases are correctly separated.

7. **L8/L11/D4R.2 — CLEAN.** Side digits plus factor-selection indices give an injective encoding once the complete shape data, including \(u^*\), are supplied. The normalized residual degree \(w'_r/e_r\), sharper multiplicity bound \(\bar m_r\), p-free factor \(M(\widehat P)\), and distinction between D4R.2′ and the plan’s coefficient-one bound are internally consistent.

8. **Honest perimeter and gates — CLEAN.** The open kernel remains explicitly open; all downstream claims remain conditional on it and the accepted source material. Cross-shape overlap, lack of termination input, deferred value aggregation, and the limitations of the D4-n3 gate are stated accurately. The sealed arithmetic, multiplicity-one justification, heights, \(A=11\), \(W=7\), \(M=1\), \(N=5\), and counts \(3,48\) remain consistent.

**Counts:** 0 critical errors; 0 justification/display gaps.

**Verdict: ACCEPTED (0/0).**
