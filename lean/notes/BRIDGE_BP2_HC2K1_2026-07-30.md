# BRIDGE BP2 — HC2 CONSTRUCTION + THE K1 DE-VACUIFICATION (blueprint, 2026-07-30)

AREA: the nine standing HC2 sorries; the HistoryCoherent child-keying repair (standing
task #44 — sign-off granted by the bridge-campaign charge); the 2-node ReadsOf gate that
de-vacuifies the K1 kernel cone (U20a/U20b/U22-E2 at the steep perimeter); the U1b
hdvd/hthr sign-off. Unit ids: HK-01 … HK-57 (HK-47…HK-57 added at REVISION 2).

Binding disciplines (campaign charge): statement fence (named warranted hypotheses only,
orchestrator-ratified); countermodel-first (every at-risk universal gets a countermodel
unit sequenced BEFORE its prover); honest sorries (unprovable-as-stated ⇒ repair
adjudication); Lean 4.31.0 + Mathlib; green = `lake build` (never `lake env lean`).

─────────────────────────────────────────────────────────────────────────────────────────
## 1. GOAL

What this area delivers to `LeanUrat.MovesU.theoremU` when complete:

(G1) **HC2 sorry-free**: the nine bare sorries (U10×5, U4×1, U17a×1, U21×1, U26×1)
     closed or converted to ratified named hypotheses. This makes the HC2/JetSetup
     bridge cone (U13 → U15/U16 → the MovesD PrefSet/Threshold seam that theoremU's
     UInstance rows consume) sorryAx-free, and moves U17c's `presentNorm_total` to
     Lean-core-conditional-on-`StageTransHyp` only.

(G2) **The K1 kernel cone NON-VACUOUS**: today `K1_readVertexPin_nonrec`,
     U20a/U20b/U22-E2, and every ReadsOf-keyed transport theorem close through
     `HC1.V10_readTransition_incompatible` — the recorded non-recentering transition is
     CONTRADICTORY at `ν.e·ν.g ≥ 2`, so `ReadsOf` admits no steep instances and the
     theorems are vacuous at their intended perimeter (flagged in U20a/U20b/U22-E2
     docstrings; MANIFEST K1 note). Deliverable: the ratified `HistoryCoherent` child
     re-keying + a compiled 2-node `ReadsOf` gate (`gate_readsOf_2node`, Lean-core
     footprint) witnessing a genuine `e·g = 2` root→child transition, plus the honest
     (non-contradiction) re-proof of the kernel's `e·g ≥ 2` legs.

(G3) **Cross-area unblocks**: MovesT/G1's `twoNodeKcardH` carrier (the card-4
     `History 2 F4`), the U1_negWitness inhabitation leg, and the input records for
     MovesT's G1b re-adjudication (owner: the MovesT area, not BP2).

Success criterion per unit: `lake build` green, `#print axioms` = Lean-core (or the
named-hypothesis-conditional footprint stated in the unit), statements byte-unchanged
except where an HK-ratification unit explicitly authorizes the diff.

─────────────────────────────────────────────────────────────────────────────────────────
## 2. OBJECT INVENTORY (all verified by reading, 2026-07-30)

Owner file → object → status.

**Frozen defs (statement-fence territory; edits ONLY via HK-01/HK-06 ratification):**
- `lean/LeanUrat/MovesC/Defs.lean` : `Node` (l.361; fields e h s t g μ a s0 wSide Dwidth
  line ustar gam zbar center lift ψ pat Ranch + law fields), `IsNodeLift` (l.535 — read-
  pair lift: weights `σ.w (tt k) = ν.h·(g−k)`), `LandingKey` (l.552), `History` (l.565),
  `HistoryCoherent` (l.634 — THE DEFECT SITE: non-recentering leg keys
  `TransitionCoreL σᵢ σᵢ₊₁ Φᵢ₊₁ νᵢ.e νᵢ.h νᵢ.zbar` plus ties `σᵢ₊₁.s = νᵢ.s`,
  `σᵢ₊₁.t = νᵢ.t`), `TransitionAdmissible` (l.677), `JetSetup` (l.808 carries
  `coherent : HistoryCoherent H` — this is why the repair must be IN PLACE, not a
  parallel definition).
- `lean/LeanUrat/Moves/DefsL.lean` : `TransitionCoreL` (l.198; `base : TransitionData` +
  `child_Tvec` + `child_dig_frame` with the S8-twist-free `mfun` existential),
  `LandingCylinderL` (l.247).
- `lean/LeanUrat/Moves/DefsT.lean` : `TransitionData` (l.147; `child_key/child_e/child_h/
  child_field/field_grow/child_base/child_wPrev (σ'.wPrev f = σ.w f)/threshold/
  child_slotmin (IsSlotMinWeight σ'.w Φ̂ e' h' σ.w)/child_reps`).
- `lean/LeanUrat/Moves/Defs.lean` : `IAug` (l.241: `h' > e'·σ.w Φ̂` — D.7's NEXT-side
  steepness), `IsSlotMinWeight` (l.247), `HasAnchorK`, `OrdPsiPoly`, `IsStandardLift`.

**HC2 (owned by this area; adds files freely, own statements repairable via honest
adjudication):**
- `HC2/Defs.lean` : `StageTransHyp` (l.200 — conflates the creating pair and the D.7
  next pair; see §3.1(d): CONDITIONALLY FALSE as typed), `OmUniqHyp` (l.216),
  `SideReads` (l.245, clauses (i)–(vi)), `ReadsOf` (l.285), `KeysLawful`, `ZCSeedLaws`
  (incl. `theta_norm` l.360 — covers ALL i < len — and `root_shape` l.373 — ROOT-ONLY),
  `PresentSeed`, `mkFresh/mkSigma/pinTransportSystem`, `D10Eligible`, `RootD4`,
  `SeedFreshUnpinned`, `AdjacentReads` (consumer-less/dead).
- The nine sorries (grep-verified): `U10_zcStep.lean` ×5 (`zc_step_transport`,
  `zc_step_freshExact`, `zc_step_interiorZero`, `zc_step_confine`, `zc_step`);
  `U4_freshCover.lean` ×1 (`levelSet_no_straddle`; `mkFresh_cover` itself is complete
  modulo it); `U17a_liftSwap.lean` ×1 (`lift_swap`, carries `StageTransHyp`);
  `U21_HV.lean` ×1 (`readsOf_HV`; the reduction `readsOf_HV_of_pin` is PROVED
  Lean-core; residue = `RunVertexPin` at the recorded transition);
  `U26_fiberWelldef.lean` ×1 (`class_fiber_welldef`).
- PROVED assets consumed here: `U31_gateReadsOf.lean` (concrete H₀/ν₀/fq/bStage +
  de-privatized helpers; Lean-core), `U27_gateInert.lean` (concrete seed; N := 1
  coverage floor), `K1_vertexPin.lean` (kernel + verbatim consumers; obstruction
  record items 1–6), `U7_sigmaRec.lean` (`freshAttach_exists`,
  `mkSigma_recursion(_of_unpinned/_zero)`), `U9_zcRoot.lean` (U9a/b/c + `zc_root`
  CLOSED — the root (ZC) case, the template for U10), `U17b_galois.lean`
  (`recenterLiftSpec_nodeMap`/`polOM_liftOf_nodeMap`/`isCanon_historyMap`),
  `U18_unique.lean` (`presentNorm_unique`, transport-keyed, Lean-core),
  `SharedRecenter.lean` (`recentering_scaffold`).

**HC1 (consumed; V9/V10 are this area's seam):**
- `HC1/V9_K1nonrec.lean` : `V9_K1nonrec` (SORRY-FREE, Lean-core — but its `e·g ≥ 2`
  legs close BY the V10 contradiction: vacuous perimeter; the R3c corner
  `i = 0 ∧ e·g = 1` is honestly proved via the `v9c_corner` toolkit). Reduction lemmas
  `V9_readPair`, `V9_transSteepness`, `V9_wvGeStretch`,
  `V9_wvEqStretch_of_bottomSlot`, `V9_bottomSlot_of_wvEqStretch`,
  `V9_steepSide_frameDescent` — all PROVED, keying-independent supply for HK-11.
- `HC1/V10_transportWindow.lean` : `V10_forcedKeyWeight` (child laws force
  `h★ = e★²·g·σ.h`), `V10_readTransition_incompatible` (adds the s/t ties → False at
  `e★·g ≥ 2`), `V10_transportWindow` (PROVED VACUOUSLY — disclosed). Lean-core.
- `HC1/S9*` chain: `S9_transStage`/`S9a`/`S9b` (guarded `e·g > 1`)/`S9c_coreAssembly`/
  `S9d_pinVerify`/`S9w`/`S9r` — the BUILT child stage of a lift (sorry-free per HC1
  MANIFEST; the V10 header records S9's transitions as consistent because their lift is
  frame-pair/equal-weight). `S2_childW` (`childW` = the read functional),
  `V1/V2_readFrame` (the read-regrade carrier, PROVED), `V4_readLanding` (D.8 at the
  V-frame). `DefsChild.ChildResData` (`hS5'` — the constructed-child vertex pin,
  realized by `V8_childResidual.v8Rc_S5'`).
- `HC1/K1_vertexPin.lean` (`VertexPin` formula — `RunVertexPin`'s HC1 original).

**MovesT (cross-area consumers, NOT edited here):** `G1_toyGate.lean` —
`twoNodeKcardH : History 2 F4` (sorry, l.2248), `toy_v8_wchain` (sorry),
`toyModel.mem_realizable`/`toyModelB.mem_realizable` (BLOCKED-FALSE at pinned tables,
awaiting the G1b re-adjudication against the repaired keying).

**Numeric mirrors:** `verification/r3b_countermodel_check.py`,
`verification/v10_transition_check.py` (both cited by the V9/V10 records).

─────────────────────────────────────────────────────────────────────────────────────────
## 3. THE MATHEMATICS

### 3.1 The keying defect and its resolution (the task-#44 core)

**(a) The defect, as compiled.** `HistoryCoherent`'s non-recentering leg asserts, at
every interior transition i → i+1:

    IsNodeLift νᵢ Φᵢ₊₁  ∧  TransitionCoreL σᵢ σᵢ₊₁ Φᵢ₊₁ (νᵢ.e) (νᵢ.h) (νᵢ.zbar)
    ∧  σᵢ₊₁.s = νᵢ.s  ∧  σᵢ₊₁.t = νᵢ.t                                        (OLD)

i.e. the child stage is keyed at the pair of the read that CREATED its key (the "read
pair"), with `child_wPrev : σᵢ₊₁.wPrev f = σᵢ.w f` and
`child_slotmin : IsSlotMinWeight σᵢ₊₁.w Φᵢ₊₁ νᵢ.e νᵢ.h σᵢ.w`. V10 proved
(Lean-core, on disk): these clauses + the child's own Stage laws are CONTRADICTORY
whenever `νᵢ.e·νᵢ.g ≥ 2` (finding 1 forces `νᵢ.h = νᵢ.e²·νᵢ.g·σᵢ.h`, finding 2 then
derives a Laurent-position clash through the s/t ties). Hence `ReadsOf` records no
steep non-recentering transition, and the whole K1/U20/U22-E2 transport cone is vacuous
there.

**(b) The note's own keying is DIFFERENT — the off-by-one.** §B2-DEF D.7 head
(MOVES_2026-07-24.md, the display at "D.7 Proposition TRANS", quoted):

> Fix the NEXT side to read on the child polygon (D.8 landing or the root cluster),
> with slope h'/e' (lowest terms) OVER w, satisfying (I-aug): h'/e' > w(Φ̂). Define
>     w'( Σ_j B_j Φ̂^j ) := min_j ( e'·w(B_j) + j·h' ).

So D.7's (e', h') is the pair of the read AT node i+1 (the next side on the CHILD
polygon), not node i's pair; `Moves.IAug` (l.241) transcribes exactly this next-side
steepness `h' > e'·σ.w Φ̂`. Under (OLD), `h' := νᵢ.h` is forced by V10-finding-1 to
equal `νᵢ.e²·νᵢ.g·σᵢ.h = νᵢ.e·σᵢ.w(Φᵢ₊₁)` — the exact BOUNDARY that (I-aug)'s strict
inequality excludes. The corpus keying and the note's keying are off by one read.

**(c) The two-step faithful transition (candidate S-a, RECOMMENDED).** Classical
MacLane/Montes and the note's D.5→D.7 pipeline decompose a non-recentering read i into:

  STEP 1 (READ REGRADE, same degree): the intermediate stage σV — key UNCHANGED
  (`σV.Φ = σᵢ.Φ`), pair = the read pair (`σV.e = νᵢ.e`, `σV.h = νᵢ.h`,
  `gcd = 1` by `νᵢ.hcop` ✓), `σV.wPrev = σᵢ.w`, `σV.w` = the read functional
  (`hStretch`-consistent on coefficients: `σV.w x = νᵢ.e·σᵢ.w x` for `x ∈ C_{σᵢ.Φ}`;
  `σV.w σᵢ.Φ = νᵢ.h = σV.h` ✓ `hwΦ`). This is HC1's PROVED `V2_readFrame` carrier
  given Stage clothing. Against σV the recorded lift `IsNodeLift νᵢ Φᵢ₊₁` (weights
  `σᵢ.w(t_k) = νᵢ.h(g−k)`) IS the frame-pair standard-lift shape: σV-equal-weight,
  `σV.R(Φ̂)` ψ-divisible — exactly the hypothesis class of Lemma DIV/D.7 and of HC1's
  S9-built children ("S9's genuine transitions are untouched", V10 header).

  STEP 2 (KEY AUGMENTATION at the NEXT pair): `TransitionCoreL σV σᵢ₊₁ Φᵢ₊₁
  (νᵢ₊₁.e) (νᵢ₊₁.h) (νᵢ.zbar)` — child_wPrev = σV.w (ℤ-valued ✓), child_h = νᵢ₊₁.h
  (= `σᵢ₊₁.w Φᵢ₊₁`, D.7's `w'(Φ̂) = h'` with `gcd(e', h') = 1` ✓ `hcop`), slot-min at
  the next pair over σV.w, ties `σᵢ₊₁.s = νᵢ₊₁.s`, `σᵢ₊₁.t = νᵢ₊₁.t` (the Bézout pair
  of the child's OWN (e, h) — Stage.hbez forces this typing; the (OLD) tie to νᵢ's
  pair is part of the compiled contradiction). IAug = the recorded strict steepening
  clause of coherence.

  The REPAIRED leg (the HK-01 ratification target, displayed for the diff):

    ((νᵢ.species ≠ recentering) →
      ∃ σV : Stage p F, RegradeOf σᵢ νᵢ σV ∧
        IsNodeLift νᵢ Φᵢ₊₁ ∧
        TransitionCoreL σV σᵢ₊₁ Φᵢ₊₁ (νᵢ₊₁.e) (νᵢ₊₁.h) (νᵢ.zbar)) ∧
    σᵢ₊₁.s = νᵢ₊₁.s ∧ σᵢ₊₁.t = νᵢ₊₁.t                                        (NEW)

  where `RegradeOf σ ν σV : Prop` pins:
  `σV.Φ = σ.Φ ∧ σV.e = ν.e ∧ σV.h = ν.h ∧ σV.K = σ.K ∧ σV.FQ = σ.FQ ∧
  σV.reps = σ.reps ∧ (∀ f, σV.wPrev f = σ.w f) ∧ IsSlotMinWeight σV.w σ.Φ ν.e ν.h σ.w`
  (+ the residual-law clauses HK-01 fixes from D.7(i)–(v): σV.R ties to σ.R on
  coefficients at the (S5) positions of the READ's Bézout pair). At the ROOT read with
  `(ν₀.e, ν₀.h) = (σ₀.e, σ₀.h)` the regrade is the identity (`σV = σ₀` satisfies every
  clause) — the U27/U31 gates are unaffected.

  LAYERING (REVISION 2, Codex finding 6): `RegradeOf` mentions `Node`, which lives in
  MovesC/Defs, and `HistoryCoherent` (same file, later) references `RegradeOf` — so a
  separate importable home is IMPOSSIBLE (cycle). The compiled def goes INTO MovesC/Defs
  (before `HistoryCoherent`) via the HK-06 diff, full stop. HK-04's role is STAGING:
  draft + compile the definition text and the identity-regrade instance in a scratch
  file `HC2/StagingRegrade.lean` against a byte-identical local copy (marked STAGING,
  deleted by the HK-06 wave). Every pre-wave gate (HK-05, HK-21) runs against the
  staged copy; the wave replaces copy by original with proof text unchanged.

  COMPILED e ≥ 2 BRANCH (REVISION 2, Codex finding 12): a compiled `Prop` cannot leave
  the σV residual-law list "open" at `ν.e ≥ 2`. The candidate compiled forms, decision
  ESCALATED (Q2-extension, orchestrator ruling required before HK-06):
    (RG-1) residual-law clause group GUARDED by `ν.e = 1`; core clauses (the display
      above) unconditional. e ≥ 2 transitions are recordable but carry a weaker record
      — no BP2 theorem may consume residual clauses at e ≥ 2 (matches the §3.3 fence);
      consequence: V9's e ≥ 2 legs lose their supply (see the §3.3 disposition —
      the REAL cost, surfaced honestly).
    (RG-2) whole non-recentering leg guarded by `ν.e = 1`: e ≥ 2 non-recentering reads
      become UNRECORDABLE — a disclosed definitional scope restriction (vacuity at
      e ≥ 2 only, visible in the def, unlike the OLD silent contradiction); V9's e ≥ 2
      legs then close by disclosed emptiness, statement byte-unchanged.
    (RG-3) unguarded residual clauses transcribing §B2-FINAL's carry-algebra laws at
      e ≥ 2 — REJECTED by BP2 (pass-8: B2-FINAL is NOT accepted; recording unverified
      mathematics as coherence clauses is an axiom-smell).
  BP2 recommends RG-1 + the named-hypothesis disposition for V9's e ≥ 2 legs, but does
  NOT decide (statement-fence event either way).

  IDENTITY-DISCHARGE TABLE (REVISION 2, Codex finding 11): HK-01's ratified clause list
  MUST come with a per-clause discharge column for the identity regrade — for each
  residual-law clause, the Stage law or named lemma proving it at `σV = σ` when
  `(ν.e, ν.h) = (σ.e, σ.h)` (the display's core clauses are reflexivity/`child_wPrev`-
  style; the D.7(i)–(v) residual clauses are NOT automatically Stage laws for arbitrary
  σ). A clause with no identity discharge blocks HK-04 and REOPENS HK-01.

**(d) Consequences already provable, and the falsity of `StageTransHyp` as typed.**
`HC2.StageTransHyp` (Defs l.200) binds `IsReadLift σ ψ g e' h' Φ̂` (creating-pair lift
shape) AND `IAug σ Φ̂ e' h'` (next-pair steepness) at the SAME (e', h') and concludes
`TransitionCoreL σ σ' Φ̂ e' h'` + s/t ties — the (OLD) configuration. By
`V10_forcedKeyWeight`, any such σ' forces `h' = e'·σ.w Φ̂ · e'`-collapsed
`= e'²·g·σ.h`; since for the read lift `σ.w Φ̂ = e'·g·σ.h` (steepness makes the key
slot the unique minimum), IAug reads `h' > e'²·g·σ.h` — strict — contradiction. So:
`StageTransHyp p F` implies NO `StageCoreL` stage admits any lawful
(ψ, g, e', h', lift, IAug) instance with `e'·g ≥ 2` (unit HK-03). U17a/U17c consume
`StageTransHyp` — their hypothesis is (conditionally) FALSE, so the repair MUST also
re-key `StageTransHyp` to the two-step form (HK-06; HC2 owns this def — honest-sorry
rule, not a silent weakening: the new form takes the read data + the NEXT pair +
IAug and concludes `RegradeOf ∧ TransitionCoreL σV σ' Φ̂ e' h'`-shaped data).
ARCHIVAL (REVISION 2, Codex finding 7): the HK-06 diff RENAMES the old predicate to
`StageTransHypOld` (kept on disk, docstring "refuted-shape historical record; HK-03's
subject") and introduces the re-keyed def under the standing name. HK-03's
`stageTransHyp_steep_empty` is proved pre-wave against the current name; the wave
mechanically re-points it to `StageTransHypOld` — the emptiness theorem survives as
the permanent record of WHY the re-key happened, never re-interpreted against the new
definition. NOTE the re-key itself changes U17a's binder type = Q9, unadjudicated —
HK-06 is BLOCKED on the orchestrator's Q9 ruling (see HK-01's re-scope).

**(e) The constraint table (K-gates) — every candidate diff must pass ALL of these;
they are machine gates, not prose:**
- (K-1) CONSISTENCY: a concrete instance of the repaired leg exists (the 2-node gate,
  Block G). The V10/V11 refutation machinery applied to the NEW clause set must fail
  to re-derive False (HK-05 runs it).
- (K-2) SCALE: on coefficients `B ∈ C_{Φ̂}` the repaired record still yields
  `σᵢ₊₁.w B = νᵢ₊₁.e·σV.w B` with `σV.w B = νᵢ.e·σᵢ.w B` on `C_{σᵢ.Φ}`-single-slot
  B — C.1.0(c)'s stretch chain. NOTE the consumer-visible change: the old
  `child_wPrev : σ'.wPrev = σᵢ.w` becomes `σ'.wPrev = σV.w`; K1-kernel item (v)'s
  "σ'.w(B μ) = ν.e·σ.w(B μ)" is NO LONGER a definitional unfold — it becomes the
  bottom-window LAW (D.8-(TRANSPORT)), provable (HK-11) instead of assumed.
- (K-3) hcop/hbez/hwΦ of σᵢ₊₁ and σV are satisfiable simultaneously with their ties
  (checked at the gate; the (OLD) ties fail this — that is V10 finding 2).
- (K-4) S9 COMPATIBILITY: HC1's built children (S9c output) satisfy the STEP-2 clause
  set verbatim (their lift is already frame-pair against the regraded stage) — the
  Wall A↔B seam closes definitionally, no S9 re-proof.
- (K-5) The root read (identity regrade) keeps U27/U31 BYTE-COMPATIBLE (1-node gates
  never instantiate the leg; 2-node gate uses σV = σ₀).
- (K-6) Downstream re-derivations exist for every current consumer of the leg
  (inventory in §3.2). No consumer silently loses its supply.

### 3.2 Fallout inventory of the repair (grep-verified consumers of the leg)

Direct consumers of `HistoryCoherent`'s transition legs / `TransitionCoreL` fields:
1. `HC1/V9_K1nonrec.v9_readSteep_all` (l.233, PRIVATE lemma): states
   `νᵢ.e·σᵢ.h < νᵢ.h` for all i ≥ 1. CORRECTED AT REVISION 2 (the original sketch here
   said the chain "simplifies" — WRONG): under (NEW), `child_h : σᵢ.h = νᵢ.h` (the
   hosted pair IS the stage pair), so the OLD statement collapses to
   `νᵢ.e·νᵢ.h < νᵢ.h` — FALSE at every recorded non-recentering-predecessor node
   (Node.he/hh: e, h ≥ 1); the HK-22 gate instance H₂ is a compiled countermodel at
   i = 1. The lemma is `private` (proof-internal supply, freely restatable — no
   statement fence). RESTATEMENT TARGET: the steepness content the V9 route consumes,
   re-keyed to the PRE-augmentation scale — candidate
   `(νᵢ.e : ℤ)·σᵢ.wPrev σᵢ.Φ < νᵢ.h` (IAug at the creating transition, transported
   through `threshold : σᵢ.wPrev σᵢ.Φ = σV.w σᵢ.Φ`), derived from coherence's strict
   slope-steepening clause + the slope law + the width chain (the (NEW) leg carries no
   literal IAug clause; if this route under-supplies, HK-01's package adds the IAug
   clause to the leg — SURFACE, not improvise). Restatement + re-derivation unit HK-08,
   countermodel-record unit HK-48 first.
2. `HC2/U21_HV.readsOf_HV_of_pin`: uses `hncleg` + `child_wPrev` for the frame link at
   the top stride. Re-route through `σV` (`σ'.wPrev = σV.w`, and on the single
   coefficient `B' μᵢ` the position arithmetic gains one `νᵢ.e`-factor, absorbed by the
   (S5) position being stated at `σ'.t·σ'.wPrev`). Unit HK-09.
3. `HC2/U25_polTotal` (l.113): consumes only the RECENTERING leg (`(htrans i).1`) —
   expected untouched; re-elaboration check in HK-07.
4. `HC2/U19/U23/U24` (projections), `HC2/K1_vertexPin.K1_SAE_vertexEq_endpoint`
   (γ-tie + SideReads only): expected untouched (HK-07).
5. `HC2/U17b_galois` (l.638 rebuilds `TransitionCoreL` fields under nodeMap): mechanical
   re-thread (HK-10).
6. `HC1/V9_K1nonrec` main theorem: its `e·g ≥ 2` legs currently close by V10
   contradiction — the repair REOPENS them as honest obligations (HK-11, the area's
   mathematical hard core). `HC1/V10_transportWindow`: statement keeps compiling (its
   hypotheses just become uninstantiable-by-runs); its docstring gains a
   "historical witness" note (HK-06).
7. `HC2/U1_negWitness` (imports the leg via coherence-preservation of the zeroLast
   surgery — surgery touches only `lift`, which the (NEW) leg still ignores at
   non-recenterings): re-elaboration in HK-07.
8. `MovesT/G1_toyGate` mem_realizable A/B: BLOCKED-FALSE records naming the repaired
   keying as their re-adjudication basis — hand-off, NOT re-proved here (Q7).

### 3.3 The honest transport (what the vacuity was masking) — HK-11's mathematics

After the repair, `K1_readVertexPin_nonrec` at `e·g ≥ 2` must be PROVED. Statement
(K1_vertexPin.lean l.176, byte-unchanged): at consecutive reads (i, i+1) of a run with
non-recentering parent, for any development B of f in the child key with `μ < Nd`:

    B νᵢ.μ ≠ 0  ∧  (σᵢ₊₁.w (B νᵢ.μ) : ℚ) = STR_{i+1} · lineᵢ.at (νᵢ.μ·νᵢ.childWidth)

Proof plan (the V9 route (i)–(vi), now with real supply):
- (i) read steepness: HK-08's re-derived `v9_readSteep_all`; ROOT read steepness from
  the recorded side data (`νᵢ.hh`, slope law at STR₀ = 1).
- (ii)–(iii) the ReadFrame at (νᵢ.e, νᵢ.h) on σᵢ: V1/V2 (PROVED); the recorded
  `IsNodeLift = IsReadLift` (`isNodeLift_iff`, definitional).
- (iv) f's ReadFrame residual from SideReads (i)+(ii): the side supports the
  development with equality exactly at stride digits, so `wV(f) = νᵢ.gam` attained and
  the pattern anchors `RV(f)` (V9's derived pattern-anchor route — does NOT consume the
  U31-fenced SideReads(iii) `σ.R f` rendering).
- (v) V4_readLanding gives (VERTEX) at the V-frame: `B μ ≠ 0`,
  `wV(B μ) = gam − μ·νᵢ.e·νᵢ.g·νᵢ.h + μ·(...)` (V4's normal form). The remaining gap is
  the BOTTOM-WINDOW LAW `wV(B μ) = νᵢ.e·σᵢ.w (B μ)` (R3b; refuted-underivable from the
  ⚠-clean pool by the sympy countermodel). NEW SUPPLY: the repaired record carries
  `σᵢ₊₁.wPrev = σV.w = wV` as ACTUAL DATA, so the window law becomes a statement about
  the recorded child: `σᵢ₊₁.wPrev (B μ) = νᵢ.e·σᵢ.w (B μ)`. Route: the (TRANSPORT)
  upward forced-window recursion (MOVES 2521–2528) transcribed against the CONSISTENT
  carrier — strong induction on j: carry_j is a function of d_μ…d_{j−1}; d_j ≡ C_j −
  carry_j mod ψ; the window base is forced by the (S5′) position; `z^q·{deg < g}` is a
  full system of representatives mod ψ. V10_forcedKeyWeight's computation pattern
  (2-slot development + slot-min + equal-weight τ bound) redone at σV yields the
  per-slot equations instead of False. Sequenced AFTER the HK-12 countermodel re-check.
- (vi) the ℚ-conversion: γ-tie + slope law, the PROVED `K1_SAE_vertexEq_endpoint`
  arithmetic pattern.

Wild-prime caveat (SCOPE FENCE, Q2): at `νᵢ.e ≥ 2` the read regrade extends the value
group and the residue representation (the pass-7 carry algebra `D = F[u]/(u^e − z̄)`);
the note's consolidated tower induction for this case (§B2-FINAL) is NOT ACCEPTED
(pass-8 record: 5 crit/2 gap). BP2 therefore targets the `νᵢ.e = 1, νᵢ.g ≥ 2` steep
perimeter (which includes the entire refuted-vacuous zone probed so far — V10 finding 2
and the r3b countermodel both live at e★ = 1), and FENCES `νᵢ.e ≥ 2` interior reads as
a named open boundary (`RegradeOf` still definable; the σV residual-law clause list at
e ≥ 2 is the open part). This is a scope statement, not a hidden hypothesis: the gate,
the kernel re-proof, and the de-vacuity claims are all stated and delivered at e = 1.

REVISION 2 (Codex finding 1 — CONFIRMED, and it is a BUILD-BLOCKER, not just an
overclaim): `V9_K1nonrec` (and the kernel theorems above it) quantify over ALL recorded
`ReadsOf` runs, INCLUDING `νᵢ.e ≥ 2` non-recentering reads. Today those legs close BY
the V10 contradiction; the HK-06 diff DELETES the contradictory clauses, so the
existing proof breaks and — under compiled form RG-1 (§3.1(c)) — the e ≥ 2 instances
are recordable with NO residual-law supply. The whole-tree build after HK-06 therefore
CANNOT go green with V9's statement byte-unchanged and its e ≥ 2 legs honest: the
choices are exactly (E-a) named-hypothesis / statement change on the e ≥ 2 legs
(statement-fence event), (E-b) = RG-3, rejected, (E-c) open the carry-algebra area
now, (E-d) = RG-2 (explicit `ν.e = 1` guard in the leg; disclosed definitional
restriction; V9 byte-unchanged, e ≥ 2 legs close by DISCLOSED emptiness). HK-11a is
now stated at `νᵢ.e = 1` explicitly; HK-11b splits into the e = 1 closure + the
escalated e ≥ 2 disposition; HK-11c retires vacuity caveats ONLY at the delivered
e = 1 perimeter (the e ≥ 2 caveat text is REWRITTEN to the ratified disposition, never
deleted wholesale). ESCALATED as the Q2-extension: HK-06 is blocked on this ruling.

### 3.4 U4's `levelSet_no_straddle` (the (SAE)/C.1.5 line-dominance geometry)

Statement (U4_freshCover.lean l.72, byte-unchanged): under `HistoryCoherent H` +
`Realizable H`, if `c, c' ∈ levelSet H n N i νᵢ j` (same fine slot j, same exact height
`νᵢ.slotVal j`) and c is in-band (`inFreshBand`: base < `prevRim n i` AND
`floorH i (base) < slotVal j`), then c' is in-band.

The two legs for c′:
- FLOOR LEG. `floorH i b = max_{r<i} staircase_r b` where `staircase_r b =
  line_r.at((b/childWidth_r)·childWidth_r)` on node r's factor interior. Claim (the
  C.1.5 line-dominance): for every r < i and every base b in slot j's window with j a
  span slot of νᵢ at exact value, `staircase_r b < νᵢ.slotVal j`. Mechanism: lines
  strictly steepen (coherence's slope clause) and (SAE) (via `Realizable`'s
  `TransitionAdmissible` at each consecutive pair) gives strict span-entry
  `new(j) > old(j)` below the vertex + vertex equality at adjacency; chase the chain
  r → r+1 → … → i: each staircase is dominated by the next line on the relevant window,
  because the next side enters strictly above the standing box line and staircases are
  left-edge evaluations of concave-decreasing lines. The root case i = 0 is trivial
  (`floorH 0 = ⊥`).
- RIM LEG. `(boxChart c').2 < prevRim n i = νᵢ₋₁.μ·νᵢ₋₁.childWidth` (i ≥ 1): c' shares
  fine slot j with c; slot j is a span slot (from `IsValueCoord`), and coherence's
  window containment `s0ᵢ + wSideᵢ ≤ νᵢ₋₁.μ` + `Dwidthᵢ = νᵢ₋₁.childWidth` bound the
  whole slot-j window: `(j+1)·Dwidthᵢ ≤ (s0ᵢ+wSideᵢ+1)·Dwidthᵢ ≤ ...` — pure
  arithmetic once `j ≤ s0ᵢ + wSideᵢ` is extracted. CAUTION: the vertex slot j = μ case
  needs `j ≤ νᵢ₋₁.μ` with the c'-base still `< μ·childWidth` — check the boundary
  block; if the vertex slot's window CROSSES the rim, the rim leg may need the level
  set intersected with the box (the in-band c gives base < rim for ONE member only).
  This is R-2's countermodel target.
Both legs are conditional on the HK-29 countermodel attempt failing (the N-6 sign-off
ASSUMED the geometry; it was never machine-probed).
REVISION 2 (Codex finding 2): the displayed rim chain was ARITHMETICALLY INVALID —
`s0ᵢ + wSideᵢ ≤ νᵢ₋₁.μ` does NOT give `s0ᵢ + wSideᵢ + 1 ≤ νᵢ₋₁.μ`, and the strict form
fails exactly at ADJACENT reads (`s0 + wSide = μ`, e.g. the HK-18 gate read). The
CORRECTED route does not use window containment at all: `Node.fineSlot` is Euclidean
division (`fineSlot b = b / Dwidth`, MovesC/Defs l.514), so `c' ∈ levelSet … j` pins
`(boxChart c').2 ∈ [j·Dᵢ, (j+1)·Dᵢ)` — the SAME aligned window that contains
`(boxChart c).2`. With `Dᵢ > 0` (from `hDwidth : Dwidth = σ.Φ.natDegree` + the Stage
degree law `σ.hdeg`), c's OWN in-band rim bound `(boxChart c).2 < νᵢ₋₁.μ·Dᵢ` (coherence:
`Dwidthᵢ = νᵢ₋₁.childWidth`, so `prevRim n i = νᵢ₋₁.μ·Dᵢ`) forces `j·Dᵢ < μ·Dᵢ`, hence
`j < μ`, hence `j + 1 ≤ μ`, hence `(boxChart c').2 < (j+1)·Dᵢ ≤ μ·Dᵢ`. Aligned windows
NEVER straddle the rim (integers: `j < μ` gives `(j+1)·D ≤ μ·D`), so the vertex-slot
CAUTION above DISSOLVES for the rim leg — the reviewer's straddle scenario (`j = μ`)
is impossible for a level set containing an in-band member, since slot μ's window
starts AT the rim. The RISKY leg is therefore the FLOOR leg (the C.1.5 line-dominance
chain) alone; HK-29's countermodel search re-targets it.

### 3.5 The U10 (ZC) inductive step

`zc_step` (U10, statement byte-unchanged): ZCData at prefix i+1 → ZCData at prefix i+2,
for the choice-built `mkSigma`. The four legs mirror the CLOSED root case (U9a/b/c +
zc_root in U9_zcRoot.lean — read it first; it is the template):
- U10a (transport): pin of `mkSigma (i+1)` splits via `pinTransportSystem_spec` clause
  (2) into (old pin) ∨ (fresh cut); recursion supply = `mkSigma_recursion_of_unpinned`
  whose `hunpinned` leg derives from ih + U3 `mkFresh_band` + `hreal` (HK-32); the new
  floor is the old max'd with νᵢ₊₁'s staircase (`C2_floorH_succ`, PROVED); the band's
  upper edge transports through the general-step floor bridge (replaces U9a's
  `STR₀ = 1, Dwidth = 1` collapse — the bridge is `kappaH`/`innerslotH` arithmetic).
- U10b (fresh exactness): with `freshAttach_exists` (Θ-composed clauses, post-DC-3) the
  witness exists for every seed under `hunpinned`; ⊇ closes by the U10a band machinery
  run backwards.
- U10c (interior zeros): the STOP record predates the D5-fence sign-off. The landed
  `theta_norm` field (Defs l.360) is stated at ALL i < len and at-or-below-floor
  coordinates — exactly the strip-solve residue `solve = −corr = 0` needs (the U9c
  closure used it at i = 0; the file's own docstring names U10c as consumer at i > 0).
  The landed `root_shape` (l.373) is ROOT-ONLY: the value-interior exclusion at STEP
  reads (U10c's second gap: nothing yet forces span slots at/beyond the rim at i ≥ 1)
  is NOT covered by it. Expected discharge: at i ≥ 1 the interior-value exclusion is a
  THEOREM, not a seed law — coherence's window containment `s0'+w' ≤ μ` puts every span
  slot of read i+1 at fine slots ≤ νᵢ.μ, i.e. at bases ≥ the new rim... verify the
  inequality ORIENTATION against `prevRim (i+2) = νᵢ₊₁.μ·νᵢ₊₁.childWidth` (HK-36's
  audit; if it fails, the residue is a new ZCSeedLaws STEP-shape field = D5-fence event,
  Q8). Sequenced behind the HK-37 countermodel (lift the badSeed one read up AGAINST
  theta_norm — the U9c badSeed had `corr ≡ 1`, killed by theta_norm at i = 0; verify
  the step analogue dies too).
- U10d (confinement): (old pin confined by `ih.downset_exact`'s rim guard) ∨ (fresh cut
  confined by U3's `prevRim` clause).
- Assembly packages a–d into `ZCData` (downset_exact := ⟨U10a-derived →, U10b⟩,
  interior_zero := U10c, confine := U10d).

### 3.6 U17a `lift_swap`

Statement (byte-unchanged, carries `hst : StageTransHyp` — after HK-06 the RE-KEYED
StageTransHyp'; U17a's binder name is preserved, its type changes with the ratified
re-key — a statement-fence event covered by HK-01's package): every `H ∈ PrefSetAny n P`
has a class-mate H' matching P, with equal `etaData`, `(polOM p F).IsCanon H'`, coherent,
realizable, in-box. Route:
- Walk the nodes last-to-first; at each recentering node ν with `ν.lift ≠
  polOM.liftOf ν`, replace the lift (D.10 lift-independence: `L5_recLiftIndep_R4`,
  PROVED — same child key value class, digits transported by `RecenterSubstLaw`), then
  REBUILD the downstream stages with StageTransHyp' (each downstream key shifts
  `Φ' = Φ − t'`; the rebuilt σ's exist by the hypothesis; coherence legs are the
  hypothesis's conclusion clauses re-packaged).
- `etaData` reads shape + side digits + ψ-coefficients per read — all invariant under
  lift choice (D.10's content); `InBox` reads heights, invariant likewise.
- IsCanon of the result: `isCanon_historyMap` (U17b, PROVED) + the per-node swap gives
  all-nodes `IsCanon` ∧ `canonRoot` (SideReads(v)'s shape, but U17a needs it only as
  polOM's predicate, no ReadsOf needed).
- Non-recentering nodes: lifts are junk data off recenterings (the policy is blind) —
  the swap is `{ν with lift := polOM.liftOf ν}` with every coherence clause untouched.
  EVIDENCE (REVISION 2, vs Codex finding 3, verified on disk): `IsNodeLift`
  (MovesC/Defs l.535) is `∃ tt, …` reading ONLY the projections `ν.ψ/ν.g/ν.e/ν.h/ν.σ`
  — it NEVER projects `ν.lift`; structure-update projections are definitional
  (`{ν with lift := l}.ψ ≡ ν.ψ` etc.), so
  `isNodeLift_liftSwap : IsNodeLift {ν with lift := l} Φ ↔ IsNodeLift ν Φ := Iff.rfl`
  is rfl-grade — HK-40 delivers it as the NAMED lift-independence lemma the review
  asked for (same for `TransitionCoreL`, which reads no node fields beyond e/h/zbar,
  and for `LandingKey`'s recentering conjunct, vacuous at species ≠ recentering).
- REBUILD INVARIANT (REVISION 2, Codex finding 10): `StageTransHyp'`'s conclusion
  supplies the CURRENT step's `RegradeOf ∧ TransitionCoreL` — it does NOT by itself
  re-type the next recorded node's `IsNodeLift`/`LandingKey` against the REBUILT stage.
  The suffix induction needs the named transport lemma (unit HK-49): the rebuilt stage
  agrees with the recorded one on every field `IsNodeLift`/`LandingKey` read (Φ, w on
  the coefficient carrier, R at the (S5) positions, t/wPrev) — either provable from
  the hypothesis's conclusion clauses (uniqueness of the rebuilt data over fixed
  inputs), or HK-49 reports the exact residue and R-8's fallback fires (U17a carries
  the leg-shaped named hypothesis).

### 3.7 U21's residue and U26

U21: `readsOf_HV_of_pin` (PROVED) reduces (HV) to `RunVertexPin` at the recorded
transition, non-recentering parents, single element `B' μᵢ`. The S8 dual-audited twist
(`mfun ↦ mfun + λe'·id` preserves every recorded law) certifies underivability FROM THE
OLD RECORD. The repair round is the natural (and only fenced) fix point: when HK-01
re-keys the leg, it ALSO rules on pinning the child digit map (D.7(v): "D(x) := R(x)
mod ψ" — reduction mod ψ IS digPrime; realized by construction on HC1's built child,
`v8Rc_S5'`). Recommended ruling: the (NEW) leg's `TransitionCoreL σV σ' …` carries
`child_dig_frame` AT σV, where the constructed-child pin `hS5'` lives — then
`RunVertexPin` at the recorded transition follows with `mfun ≡ 0` at the one needed
weight, and `readsOf_HV` closes by `exact readsOf_HV_of_pin … (fun _ => pin)` (modulo
the σV re-route of HK-09). If the ruling declines the pin, U21's sorry converts to the
named hypothesis route (Wall A↔B: histories built by HC1 carry the pin) — recorded
non-discharge, not a forced proof.

U26: `ClassFiberWelldef p F n N m (polOM p F) P` — one η-class, one classifier locus,
over all representatives and all jets. Route: U18's `presentNorm_unique` (PROVED,
transport-keyed: `∃ φ, HistGalTransport φ H H'`) turns EtaGalEq class-mates into
transport-related histories; C6's `thmC_b` (MovesC, PROVED) + the MovesD
`ClassFiberWelldef` unfolding tie both loci to the same transported T(H,⊤) system.
Honest risk: the transport-keyed U18 needs the ∃φ witness from EtaGalEq — exactly the
step `OmUniqHyp`-as-typed was conditionally refuted on (scratch_U18_unique_false: a
non-adjacent last recentering makes (HV) vacuous and canonRoot trivial). DEFERRABLE by
design (D15 carries it as `hwd`); deferral is a recorded non-discharge.

─────────────────────────────────────────────────────────────────────────────────────────
## 4. UNIT SPLIT (57 units after REVISION 2; HK-47…HK-57 are the new ids)

Format: id · statement · deps · sketch · difficulty · est. size (proof lines).
"adjudication" units produce ratified diffs/rulings + records, not theorems.

### Block K — the task-#44 repair (HK-01 … HK-12)

HK-01 · PREPARE + SURFACE (re-scoped at REVISION 2, Codex finding 5: this unit RATIFIES
  NOTHING beyond what the orchestrator already ruled — candidate (S-a) is ratified,
  adjudications file BP2/Q1; every other ruling is PREPARED here and SURFACED). Read:
  MOVES D.7 head display (§3.1(b) quote), D.5 lift display, C.1.0(c) scale declaration,
  the B2-FINAL pass-7/8 records (git c308d26/e3cd124), V9/V10 headers. Deliverables —
  a PROPOSAL PACKAGE, orchestrator sign-off required before HK-06: (i) byte-exact diff
  text for MovesC/Defs `HistoryCoherent` leg (NEW display §3.1(c)) + Node/mhat
  docstring corrections + the in-file `RegradeOf` def placement (§3.1(c) LAYERING);
  (ii) HC2/Defs `StageTransHyp` re-key text + the `StageTransHypOld` archival rename
  (§3.1(d)); (iii) the `RegradeOf` clause list incl. the σV residual laws from
  D.7(i)–(v) at e = 1 WITH the per-clause identity-discharge table (§3.1(c));
  (iv) the OPTIONS BRIEF for the JOINT `child_dig_frame`/mfun-pin ruling (§3.7) — Q5,
  DECIDED BY ORCHESTRATOR; (v) the OPTIONS BRIEF for the compiled e ≥ 2 branch
  (RG-1/RG-2/RG-3, §3.1(c)) + the V9 e ≥ 2 leg disposition (E-a/…/E-d, §3.3) — the
  Q2-extension, DECIDED BY ORCHESTRATOR; (vi) the Q9 brief (U17a binder re-type).
  Codex adversarial pass on the package MANDATORY (memory: plan-level adversarial
  passes) BEFORE surfacing. · deps: — · adjudication-prep · n/a.
HK-02 · `V11_minimalIncompat` (new file HC1/V11_minimalCore.lean): the 5-clause core —
  `child_h` + `child_wPrev` + σ'.hStretch/hwmul/hwult + read-lift τ-bound → False at
  `h★ > e★²·g·σ.h` (truly-steep). Sketch: σ'.w(Φ^{e★g}) = e★²gσ.h by hwmul+hStretch;
  ultrametric on Φ̂ − τ gives ≥ min(h★, e★·σ.w τ) = h★; contradiction. Extends V10's
  refuted perimeter (no slot-min, no ties needed); pins WHICH fields must change —
  fences the (S-c)-lite "drop ties only" non-candidate. · deps: — (V10 helper patterns
  copy) · routine-opus · ~50.
HK-03 · `stageTransHyp_steep_empty`: `StageTransHyp p F → ∀ σ, StageCoreL σ → ¬∃ (ψ g
  e' h' s' t' Φ̂), (binders) ∧ IsReadLift ∧ IAug ∧ 2 ≤ e'·g`. Sketch: apply the
  hypothesis, get σ' + TransitionCoreL at (e',h'); `V10_forcedKeyWeight` forces
  h' = e'²gσ.h; `σ.w Φ̂ = e'gσ.h` for the read lift (steepness computation, V10's
  hτbound pattern); IAug reads h' > e'²gσ.h — strict — False. ARCHIVAL (REVISION 2,
  finding 7): proved pre-wave against the current `StageTransHyp`; the HK-06 wave
  renames the subject to `StageTransHypOld` and mechanically re-points this file —
  the theorem is the permanent refuted-shape record, never re-interpreted against the
  re-keyed def. · deps: HK-02 patterns · routine-opus · ~60.
HK-04 · `RegradeOf` STAGING (re-scoped at REVISION 2, finding 6 — the def's compiled
  home is INSIDE MovesC/Defs via the HK-06 diff, §3.1(c) LAYERING; no separate
  importable file exists without a cycle): draft the definition text per HK-01's
  package; compile a byte-identical local copy in `HC2/StagingRegrade.lean` (marked
  STAGING, deleted by the wave); prove the identity-regrade instance `RegradeOf σ ν σ`
  when `(ν.e, ν.h) = (σ.e, σ.h)` against the staged copy, discharging EVERY residual
  clause through HK-01's per-clause identity-discharge table (finding 11) — a clause
  with no discharge REOPENS HK-01. · deps: HK-01 (ratified package) · routine-opus ·
  ~40.
HK-05 · COUNTERMODEL GATE on (NEW): run the V10/V11 refutation machinery against the
  ratified clause set at a steep instance (e = 1, g = 2, h★ = 3 over a (1,1)-Gauss
  shape), against the STAGED copy: the derivation must STOP (report the exact blocking
  clause), NOT close. Deliverable: a scratch file + record; if False closes, HK-01
  REOPENS (stop-the-line). Runs BEFORE HK-06. · deps: HK-01, HK-02, HK-04 (staged
  copy) · hard-fable · ~80 (exploratory).
HK-06 · EXECUTE the ratified diff — THE MIGRATION WAVE (re-scoped at REVISION 2,
  finding 4: gates + wave contents made explicit). PRE-GATES, all green first:
  (g1) orchestrator ratification of HK-01's package incl. the Q2-extension, Q5, Q9
  rulings; (g2) HK-05 refutation stall; (g3) HK-21 — the STAGED 2-node instance of the
  (NEW) leg clause set (the adjudicated HK-22 gate in its only pre-diff-satisfiable
  form; see HK-21/HK-22). WAVE CONTENTS (one atomic build-green change-set — the
  fallout re-derivations CANNOT be sequenced after the diff, the tree would be red):
  the MovesC/Defs edit (RegradeOf def + HistoryCoherent leg + docstrings), HC2/Defs
  (StageTransHyp re-key + StageTransHypOld archival rename + HK-03 re-point), V10
  docstring (historical-witness note), + landing the pre-drafted HK-07/HK-08/HK-09/
  HK-10 re-derivations (each DRAFTED against the staged copy beforehand) + the
  ratified V9 e ≥ 2 disposition (§3.3). POST-WAVE, immediately: whole-tree
  `lake build`; re-run `LeanUrat/AxChk_baseline.lean` (footprint regression =
  stop-the-line); HK-22 (re-bind the staged gate — the second adjudicated gate in
  final form) BEFORE any prover consumes (NEW); MANIFEST/PROJECT_STATE updates; Codex
  confirmation of the applied diff (memory: definition-change authority). ·
  deps: HK-01 + orchestrator rulings, HK-04, HK-05, HK-21; drafts of HK-07…HK-10 ·
  adjudication-execution · diff + mechanical.
HK-07 · Fallout sweep A (expected-untouched consumers): U19/U23/U24 projections,
  K1_SAE_vertexEq_endpoint, U25's recentering leg, U1_negWitness surgery lemmas,
  U17c/U18 — re-elaborate, fix index plumbing only; verdict table per file. DRAFTED
  against the staged copy; LANDS inside the HK-06 wave. · deps: HK-04 (staged copy);
  lands in HK-06 · routine-opus · plumbing.
HK-08 · RESTATE + re-derive `v9_readSteep_all` (CORRECTED at REVISION 2 — the original
  sketch here claimed the old statement "re-derives": FALSE; see §3.2 item 1: under
  (NEW), `child_h : σᵢ.h = νᵢ.h` collapses the old statement to `νᵢ.e·νᵢ.h < νᵢ.h`,
  refuted by Node.he/hh). The lemma is PRIVATE — restatement is proof-internal, no
  fence. New statement target: `(νᵢ.e : ℤ)·σᵢ.wPrev σᵢ.Φ < νᵢ.h` (pre-augmentation
  scale; §3.2 item 1's route: strict slope-steepening + slope law + width chain; the
  recentering leg keeps `v9_recenter_h_eq`); every V9-internal consumer re-checked
  against the new form. If the slope route under-supplies, STOP and surface (HK-01
  package addendum: literal IAug clause in the leg). Gated by HK-48's countermodel
  record. DRAFTED against the staged copy; LANDS inside the HK-06 wave. ·
  deps: HK-04 (staged), HK-48; lands in HK-06 · hard-fable · ~80.
HK-09 · Re-derive U21's `readsOf_HV_of_pin` under (NEW): the non-recentering branch's
  frame link `child_wPrev` now lands on σV.w; re-route the position arithmetic (the
  (S5) position `−σ'.t·σ'.wPrev (B' μ)` gains the regrade factor; the Laurent
  scalar-extraction CT calculus is unchanged). Statement byte-unchanged. MANDATORY
  split at the named seam if > 100 lines: (a) the σV frame-link lemma, (b) the CT
  re-assembly. DRAFTED against the staged copy; LANDS inside the HK-06 wave. · deps:
  HK-04 (staged); lands in HK-06 · hard-fable · ~120 (rewrite of the 90-line proof).
HK-10 · Fallout C: U17b's `TransitionCoreL` nodeMap re-thread (l.638) + U25's
  `D10Eligible` legs under (NEW); statements byte-unchanged. DRAFTED against the
  staged copy; LANDS inside the HK-06 wave. · deps: HK-04 (staged); lands in HK-06 ·
  routine-opus · ~60.
HK-52 · (NEW unit, REVISION 2 — split out of HK-11a, finding 16) The per-slot
  forced-window recursion at σV: the (TRANSPORT) upward recursion (MOVES 2521–2528,
  §3.3(v)) transcribed at the CONSISTENT carrier — strong induction on j; carry_j a
  function of d_μ…d_{j−1}; d_j ≡ C_j − carry_j mod ψ; window base forced by (S5′);
  `z^q·{deg < g}` a full system of representatives mod ψ. V10_forcedKeyWeight's 2-slot
  computation pattern redone at σV, yielding per-slot window equations instead of
  False. Scoped `νᵢ.e = 1` (the §3.3 fence). · deps: HK-06, HK-12, HK-22 ·
  hard-fable · ~100.
HK-11a · The bottom-window law at the repaired record (the R3b honest transport),
  SCOPED `νᵢ.e = 1` EXPLICITLY IN THE STATEMENT (REVISION 2, finding 1 — no law is
  claimed at an unrestricted recorded transition): at a recorded (NEW) transition with
  non-recentering parent and `νᵢ.e = 1`, `σᵢ₊₁.wPrev (B νᵢ.μ) = νᵢ.e·σᵢ.w (B νᵢ.μ)`
  for f's child-key development B (SideReads at both reads supplied). Sketch:
  σ'.wPrev = σV.w is recorded slot-min data; V4_readLanding gives the vertex value in
  the V-frame; HK-52's per-slot equations; close with `V9_wvEqStretch_of_bottomSlot`.
  THE hard core of the area. · deps: HK-52 · hard-fable · ~80 (post-split assembly).
HK-11b · Re-close `V9_K1nonrec`'s steep legs honestly AT THE DELIVERED PERIMETER
  (re-scoped at REVISION 2, finding 1): the `νᵢ.e = 1, e·g ≥ 2` legs replace the
  contradiction-closure by (i)–(vi) of §3.3 with HK-11a's law; the `νᵢ.e ≥ 2` legs
  follow the ORCHESTRATOR-RATIFIED disposition from the Q2-extension (E-a named
  hypothesis / E-d disclosed-emptiness guard — executed in the HK-06 wave, NOT decided
  here); statement byte-unchanged only if the ruling is E-d, else per the ratified
  fence text; downstream footprints re-verified (K1_readVertexPin/_nonrec, U20a/b/c,
  U22-E2, readsOf_NA). · deps: HK-11a, the Q2-extension ruling · hard-fable · ~120
  (MANDATORY split at legs (i)–(iii) / (iv)–(vi) if > 100 lines).
HK-11c · Footprint + vacuity-caveat retirement sweep AT e = 1 ONLY (re-scoped at
  REVISION 2, finding 1): rewrite the ⚠ VACUITY CAVEAT blocks in
  U20a/U20b/U22-E2/K1_vertexPin/MANIFEST — the e = 1 de-vacuified record + gate
  pointer REPLACES the vacuity text; the e ≥ 2 caveat is REWRITTEN to the ratified
  disposition wording, NEVER deleted wholesale; `#print axioms` table. ·
  deps: HK-11b, HK-22 · routine-opus · doc + checks.
HK-12 · COUNTERMODEL re-check (runs BEFORE HK-52/HK-11a): the r3b sympy countermodel
  (f = (X+8)(Φ̂²+8X³), (1,1)-Gauss, read (1,2)) against the REPAIRED semantics — can
  its data extend to a (NEW)-coherent 2-node history? Expected: NO (its would-be σ'
  fails the σV residual laws); if YES, the bottom-window law is REFUTED as a theorem
  and HK-11a converts to a SideReads/Defs addendum adjudication (Q8-analogue,
  stop-the-line). Extend `verification/r3b_countermodel_check.py` + a Lean scratch. ·
  deps: HK-06 · hard-fable · ~80 exploratory.
HK-48 · (NEW unit, REVISION 2 — findings 13 + the §3.2 item-1 derivation) Countermodel
  RECORD for the old `v9_readSteep_all` + probe of the restated form: (a) compile the
  refutation of the OLD private statement under the staged (NEW) clause set (the H₂
  gate data at i = 1 is the witness; ~10 lines once HK-21 exists); (b) probe the
  RESTATED form (§3.2 item 1) against the same instance and against a recentering-
  containing variant — the restated inequality must HOLD there; report any violation
  (→ HK-08 statement re-design, surface). · deps: HK-04 (staged), HK-21 · routine-opus
  · ~40 exploratory.

### Block G — the 2-node ReadsOf gate (HK-13 … HK-26)

HK-13 · `StageCoreL bStage` part 1a — wPrev valuation laws (v_p facts), genuine reps
  ([p]), tie laws, prevIaug at the diagonal (1,1) Gauss stage over F4. Concrete
  arithmetic; the U31 600-line build is the precedent and supplies the carrier
  lemmas. (SlotDecomp + CoeffFieldLawCore split to HK-56 at REVISION 2, finding 16.) ·
  deps: — · hard-fable · ~90.
HK-56 · (NEW unit, REVISION 2 — split out of HK-13, finding 16) `StageCoreL bStage`
  part 1b — SlotDecomp + CoeffFieldLawCore at bStage (the two law groups with their
  own decomposition machinery). · deps: HK-13 (carrier lemmas) · hard-fable · ~70.
HK-14 · `StageCoreL bStage` part 2 — `TvecUnitLaw` + `CoeffLocLaw` at bStage: the
  degree-0 subring argument at a concrete 1-variable stage (K = prime subfield;
  closure computations go through `Subring.closure` induction). · deps: HK-13 ·
  hard-fable · ~120.
HK-15 · The gate child carrier: Φ̂ := U31's landing key (the recorded lift of
  ψ₂ = z²+z+1 at the (1,1) read, g = 2); instantiate HC1's S9 constructor data
  (ChildResData at bStage/ψ₂) to DEFINE σ₁ : Stage 2 F4 with `σ₁.K` card 4 (the
  `Nat.card ↥σ₁.K = 4` check — G1's carrier). · deps: HK-13 · routine-opus · ~60.
HK-16 · σ₁'s Stage laws via the guarded S9a/S9b/S9c chain at the gate instance
  (e·g = 2 > 1 — the guards pass); consume HK-13/HK-56/HK-14 for the StageCoreL
  inputs. · deps: HK-13, HK-56, HK-14, HK-15 · hard-fable · ~100 (mostly
  instantiation).
HK-47 · (NEW unit, REVISION 2 — finding 9) The S9→(NEW) interface, NAMED (the K-4
  obligation made compilable; signatures verified on disk this revision):
  `S9c_coreAssembly (σ) (hσ : StageCoreL σ) (th : TransHyp σ ψ g Φhat e' h' zbar)
  (hEG : 1 < σ.e·g) : ∃ σ', TransitionCoreL σ σ' Φhat e' h' zbar ∧ VertexPin … ∧
  (∀ f ≠ 0, σ'.w f = childW …) ∧ StageCore σ'` (HC1/S9c_coreAssembly.lean l.452),
  with `TransHyp` (HC1/DefsTower.lean l.40) carrying `hlift : IsStandardLift σ ψ g
  Φhat` (FRAME-pair lift) while the pair (e', h') is a FREE parameter (enters via
  hcop/hiaug only) — so S9c is ALREADY parameterized by the next pair and no
  "re-dressing" of a fixed-pair output exists or is needed. Deliverables: (a) the
  frame-pair lift conversion, named — `IsReadLift σ ψ g σ.e σ.h Φ̂ ↔ IsStandardLift
  σ ψ g Φ̂` (the recorded `IsNodeLift ν₀` is `IsReadLift` at the root read pair =
  frame pair, `isNodeLift_iff`); (b) the assembled `TransHyp bStage ψ₂ 2 Φ̂ (ν₁.e)
  (ν₁.h) zbar₀`, hiaug checked concretely (`ν₁.h > ν₁.e·bStage.w Φ̂` — HK-18 chooses
  ν₁'s data to satisfy it). If (a)'s shapes fail to match, STOP and report the exact
  mismatch (a K-4 seam residue, surfaced — never papered over). · deps: HK-13, HK-15,
  HK-18 · routine-opus · ~50.
HK-17 · The (NEW) transition record at the gate: `RegradeOf bStage ν₀ bStage`
  (identity — root read at the frame pair, HK-04's staged instance) +
  `TransitionCoreL bStage σ₁ Φ̂ (ν₁.e) (ν₁.h) zbar₀` by instantiating
  `S9c_coreAssembly` at HK-47's TransHyp (hEG: 1 < 1·2 ✓); σ₁ is pinned to the S9c
  witness through HK-15's ChildResData construction (definitional mismatch → HK-47
  reports). Deps corrected at REVISION 2 (finding 8: ν₁ is HK-18's data). ·
  deps: HK-04, HK-16, HK-18, HK-47 · routine-opus · ~50.
HK-18 · The quartic f + node ν₁ data: f := Φ̂² + 2·Φ̂ (or the minimal variant making
  read 1 a clean ADJACENT side: s0₁ + wSide₁ = μ₀ = 1, e₁ = 1, g₁ = 1); define ν₁'s
  fields (species increment, line/gam/ustar per the γ-tie at STR₁ = 1, pat from the
  side digits of B₀ = 2·Φ̂... note B's Φ̂-development of f is literally (0, 2, 1));
  prove ν₁'s Node law fields. · deps: HK-15 · routine-opus · ~80.
HK-19 · SideReads at read 0 for f, clauses (i)/(ii) — the base development of f
  (concrete octic-in-X coefficients; U31's `sideReads_ν₀` machinery de-privatized).
  (Clauses (iii)–(vi) split to HK-57 at REVISION 2, finding 16.) · deps: HK-18 ·
  hard-fable · ~80.
HK-57 · (NEW unit, REVISION 2 — split out of HK-19, finding 16) SideReads at read 0,
  clauses (iii)–(vi): (iii) anchor/ord at ν₀ (fq-adic development, hOrd μ=1);
  (iv) LandingKey ν₀ Φ̂ (the recorded lift); (v) polOM/canonRoot (policy values,
  rfl-grade); (vi) Fact-B uniqueness. · deps: HK-19 · hard-fable · ~70.
HK-20 · SideReads at read 1 (terminal): the Φ̂-development (0, 2, 1) — clause (i)/(ii)
  at the recorded side; (iii) anchor of σ₁.R f (single-side read — the ⚠ (iii) seam is
  FAITHFUL here); (iv) terminal `LandingKey ν₁ Φtop` (existential Φnext — the last-read
  freedom); (v)/(vi) at ν₁ (vi's development is in Φtop; concrete). · deps: HK-16,
  HK-18 · hard-fable · ~120.
HK-21 · The STAGED coherence certificate for H₂ := ⟨[ν₀, ν₁]⟩ (re-scoped at
  REVISION 2, finding 4: this is the compiled-instance gate in its only
  pre-diff-satisfiable form — pre-gate (g3) of HK-06): against the staged copy
  (`HistoryCoherentStaged`, byte-identical (NEW) clause text in
  HC2/StagingRegrade.lean), prove root degree, slope laws, γ-ties, window containment
  (adjacency), width chain, strict steepening, the staged (NEW) leg := HK-17. ·
  deps: HK-04 (staged defs), HK-17, HK-18 · routine-opus · ~60.
HK-22 · POST-WAVE re-bind + assembly: `gate_readsOf_2node : ReadsOf 2 F4 4 f H₂`
  against the REAL (post-HK-06) definitions — mechanical re-bind of HK-21's proof text
  (staged copy deleted) + full ReadsOf assembly + `#print axioms` = Lean-core. THE
  de-vacuification anchor (the first ReadsOf instance with a length-2 history and an
  e·g = 2 root read), and the adjudicated second gate in FINAL form: NO prover
  consuming (NEW) starts before this is green (REVISION 2, finding 4). ·
  deps: HK-06, HK-19, HK-57, HK-20, HK-21 · routine-opus · ~40.
HK-23 · De-vacuity corollaries at the gate: instantiate `K1_readVertexPin` at
  (H₂, i = 0) and CHECK both conclusions by concrete arithmetic (B μ₀ = 2·1-slot ≠ 0;
  the ℚ-equation numerically); U20a's root-parent transport witnessed; U22-E2's
  adjacency equation witnessed. Each as a compiled `example`/named gate theorem. ·
  deps: HK-22, HK-11b · routine-opus · ~60.
HK-24 · G1 seam hand-off: `twoNodeKcardH := H₂` shape-check (History 2 F4, card-4 K₁
  via HK-15) + the hand-off record for `toy_v8_wchain` (owner MovesT; BP2 delivers the
  carrier + the repaired-keying record, NOT the ¬HistLawful proof). · deps: HK-22 ·
  routine-opus · ~30 + record.
HK-25 · U1_negWitness inhabitation: the recentering-ended variant H₂ʳ (ν₁ re-dressed
  as a recentering read: e=g=1, ψ = z − c, recorded center/lift from bStage data;
  coherence's recentering leg via `IsRecenteringCore` at a concrete recentering — the
  SharedRecenter scaffold applies); discharges the recorded honesty perimeter of
  `retiredKeysExist_false_of_witness`. · deps: HK-21 patterns · hard-fable · ~100.
HK-26 · ADJUDICATION-PREP (re-worded at REVISION 2, finding 5 — BP2 decides nothing):
  U20b non-vacuity scope = Q4. Prepare the brief: defer-with-fence (record: U20b's
  non-vacuity witnessed only at the kernel level through HK-23's species-generic
  instantiation) vs commission a 3-node gate (est. +5 units, reuse HK-13…HK-22
  machinery at depth 2); SURFACE to the orchestrator, execute the ruling. ·
  deps: HK-22 · adjudication-prep · brief + record.

### Block U — U1b sign-off (HK-27, HK-28)

HK-27 · ADJUDICATION-PREP (re-worded at REVISION 2, finding 5): U1b's `hdvd`
  (`e ∣ h'·(g−k)`) / `hthr` (`e·wPrev Φ < h'·(g−k)`) side conditions = Q3. Read D.5's
  realizer display + (S6b). Prepare the brief and SURFACE: (i) RATIFY as named
  side conditions (consumers thread them; note `hdvd` at e ≥ 2 constrains ψ's support
  to `k ≡ g mod e/gcd` — record the perimeter); or (ii) restate the realizer call at
  CURRENT-weight (S6b at σ.w — drops hdvd, needs the current-scale realizer lemma).
  At the e = 1 scope fence (Q2) both are trivially satisfied — BP2 RECOMMENDS (i)
  with the e = 1 note; the orchestrator rules. · deps: HK-01 (same note read) ·
  adjudication-prep · brief.
HK-28 · Execute HK-27: flip MANIFEST "sign-off pending" → ratified (option (i): doc
  only) or restate + re-prove nodeLift_exists (option (ii): ~90 lines, the existing
  proof's realizer-weight conversion drops the hediv step). · deps: HK-27 ·
  routine-opus · 0–90.

### Block Z — the (ZC) induction: U4 + U10 (HK-29 … HK-39, + HK-55)

BLOCK SEQUENCING (REVISION 2, Codex finding 15 — the original "launch IMMEDIATELY in
parallel" is RETRACTED): every Block Z statement quantifies over `HistoryCoherent`,
whose meaning HK-06 changes; a countermodel search under OLD — where steep
non-recentering histories are contradictory — is NOT a gate for the post-repair
theorems (the search space is spuriously empty). All Block Z units except the
read-only audit HK-36 therefore run POST-WAVE (dep HK-06); HK-36's audit may draft
early but its verdict is re-validated post-wave.

HK-29 · COUNTERMODEL attempt (BEFORE HK-30/HK-31, against the POST-REPAIR semantics):
  a coherent+realizable straddle — search for H (i ≥ 1) with a span slot whose level
  set straddles the floor. RE-TARGETED at REVISION 2 (finding 2 + the corrected §3.4
  arithmetic): the rim leg is pure interval arithmetic (aligned windows never straddle
  the rim) — the search targets the FLOOR leg (the C.1.5 line-dominance chain:
  staircase-vs-slotVal at deep prefixes, multi-node r-chains, recentering
  interleavings). Deliverable: compiled witness (→ HK-31 becomes a repair
  adjudication: band-aware levelSet = option (ii) of the N-6 record, re-opening
  U3/U5/U6 — orchestrator escalation) or a written failure analysis naming the
  blocking coherence/admissibility clause. · deps: HK-06 · hard-fable · exploratory.
HK-30 · Line-dominance single step: under coherence+`TransitionAdmissible νᵣ νᵣ₊₁`,
  node-(r+1)'s span-slot values strictly dominate node-r's staircase on the shared
  window below the vertex ((SAE) strict entry + slope law + left-edge evaluation
  monotonicity; vertex case via the adjacency equality + hLineU). Countermodel-first
  (REVISION 2, finding 15): sequenced AFTER HK-29's verdict — it is the universal
  prover for exactly HK-29's risky geometry. · deps: HK-06, HK-29 (must fail) ·
  hard-fable · ~100.
HK-31 · `levelSet_no_straddle` assembled: chain HK-30 over r < i (foldr-max induction
  on floorH) + the rim leg per the CORRECTED §3.4 route (REVISION 2, finding 2:
  same-slot interval arithmetic anchored at the in-band member — `fineSlot` is
  division by `Dwidth`, c's rim bound forces `j < μ`, so `(j+1)·D ≤ μ·D` bounds c';
  supply: `hDwidth` + `σ.hdeg` for `D > 0`; NO window-containment step, NO `+1`
  slip) + root case (floorH 0 = ⊥). CLOSES U4 (mkFresh_cover's roster goal already
  consumes it). · deps: HK-29 (must fail), HK-30 · hard-fable · ~120 (MANDATORY split
  floor-leg / rim-leg+assembly if > 100 lines).
HK-32 · `seedFreshUnpinned_of_zc`: `SeedFreshUnpinned` at step i+1 from `ZCData` (ih) +
  U3 `mkFresh_band` + `hreal` (the K2-diagnosis derivation: fresh supports sit in the
  band, band coordinates are unpinned on the prior state by ih's exactness ↔ +
  floor comparison). · deps: HK-06 · routine-opus · ~60.
HK-55 · (NEW unit, REVISION 2 — the HK-33 conditional split made real, finding 16)
  The general-step floor bridge: the `kappaH`/`innerslotH` arithmetic replacing U9a's
  `STR₀ = 1, Dwidth = 1` root collapse — the band's upper edge transported through
  the step floor (`C2_floorH_succ` composition). Standalone lemma. · deps: HK-06 ·
  hard-fable · ~50.
HK-33 · U10a `zc_step_transport` per the in-file route: recursion via
  `mkSigma_recursion_of_unpinned` (HK-32's supply); pin reads off
  `pinTransportSystem_spec` clause (2); `C2_floorH_succ` + HK-55's floor bridge.
  Statement byte-unchanged. · deps: HK-32, HK-55 · hard-fable · ~70 (post-split
  assembly).
HK-34 · U10b `zc_step_freshExact`: witness via `freshAttach_exists` + HK-32; ⊇ by the
  HK-33 band machinery backwards (band membership from the floor-bridge identity —
  U9b's proof shape one level up). · deps: HK-33 · hard-fable · ~100.
HK-35 · U10d `zc_step_confine`: (old pin → ih.downset_exact's rim guard) ∨ (fresh cut →
  U3's prevRim clause). · deps: HK-32 · routine-opus · ~40.
HK-36 · ADJUDICATION-lite: U10c coverage audit — verify (a) `theta_norm` (ALL-i form,
  Defs l.360) discharges the strip-solve `−corr` obstruction at step reads (the U9c
  closure is the i = 0 precedent — read U9_zcRoot's as-built proof first); (b) the
  step value-interior exclusion: derive from coherence's window containment + rim
  orientation (§3.5) or report the exact residue (→ Q8: a STEP-shape ZCSeedLaws field,
  D5-fence event). Deliverable: derivation plan or residue statement. · deps: — ·
  adjudication-lite (fresh-context audit) · report.
HK-37 · COUNTERMODEL attempt (BEFORE HK-38): the badSeed lifted one read up AGAINST
  the landed laws — construct a lawful seed (all ZCSeedLaws fields incl. theta_norm)
  with a nonzero strip correction at read i+1, or prove the construction blocked by
  theta_norm (expected: blocked — the shifted coordinate sits at-or-below the step
  floor, exactly theta_norm's guard). · deps: HK-06, HK-36 · hard-fable · exploratory.
HK-38 · U10c `zc_step_interiorZero`: strip solves via theta_norm (i > 0 instance);
  value-interior exclusion per HK-36's plan (or the ratified Q8 field); the U9c
  as-built route one level up. Statement byte-unchanged. · deps: HK-33, HK-36, HK-37 ·
  hard-fable · ~120 (MANDATORY split strip-solve / interior-exclusion if > 100 lines).
HK-39 · `zc_step` assembly (package U10a–d into ZCData; the `downset_exact` iff pairs
  HK-33's → with HK-34) + U13's re-verification (it threads hunp/hcoh/hreal already).
  CLOSES U10. · deps: HK-33, HK-34, HK-35, HK-38 · routine-opus · ~40.

### Block L — U17a lift_swap (HK-40 … HK-43, + HK-49/HK-50/HK-53/HK-54)

HK-40 · Single-swap kernel: for a recentering node ν, `{ν with lift := polOM.liftOf ν}`
  preserves Node laws + the LandingKey clause + coherence's recentering leg
  (`L5_recLiftIndep_R4` for the key value class; `recenterLiftSpec_of_eligible`/U25
  machinery for the polOM lift's own legality). ADDED at REVISION 2 (finding 3
  disposition): also deliver the NAMED lift-independence lemmas for the
  non-recentering swap — `isNodeLift_liftSwap : IsNodeLift {ν with lift := l} Φ ↔
  IsNodeLift ν Φ := Iff.rfl` and companions for `TransitionCoreL`/the vacuous
  `LandingKey` recentering conjunct (all rfl-grade; §3.6 EVIDENCE — `IsNodeLift`
  never projects `.lift`). · deps: HK-06 (post-repair semantics) · routine-opus · ~60.
HK-49 · (NEW unit, REVISION 2 — finding 10) The REBUILD INVARIANT, named (§3.6): the
  stage rebuilt by StageTransHyp' at step k agrees with the recorded stage on every
  field the NEXT node's `IsNodeLift`/`LandingKey` read (Φ, w on the coefficient
  carrier, R at the (S5) positions, t/wPrev) — the transport theorem HK-54's suffix
  induction threads. Route: uniqueness of the rebuilt data over fixed inputs from the
  hypothesis's conclusion clauses (`child_key/child_e/child_h/child_wPrev/threshold` +
  RegradeOf's ties pin the read-relevant fields). On block: report the exact residue →
  R-8 fallback (U17a carries the leg-shaped named hypothesis, ratified via HK-01's
  package). · deps: HK-06, HK-40 · hard-fable · ~80.
HK-50 · (NEW unit, REVISION 2 — finding 13) COUNTERMODEL attempt (BEFORE HK-53/54/41):
  at the compiled gate data (H₂ʳ / H₂ variants), attempt a swapped-node instance whose
  rebuilt downstream stage BREAKS the next node's recorded `IsNodeLift`/`LandingKey`
  (i.e. probe HK-49's invariant concretely before the universal is attempted).
  Deliverable: compiled witness (→ HK-49 residue report + R-8 fallback fires) or
  failure analysis naming the pinning clause. · deps: HK-22, HK-40 · hard-fable ·
  exploratory.
HK-53 · (NEW unit, REVISION 2 — split out of HK-41, finding 16) Rebuild-one-step: from
  a stage σₖ' agreeing with σₖ per HK-49's invariant + the recorded node data,
  StageTransHyp' produces σₖ₊₁' with the (NEW) leg's clauses at step k. ·
  deps: HK-06, HK-40, HK-49, HK-50 (must fail) · hard-fable · ~60.
HK-54 · (NEW unit, REVISION 2 — split out of HK-41, finding 16) Rebuild-suffix
  induction: iterate HK-53 over k+1…len, threading HK-49's invariant as the inductive
  hypothesis (this is the transport finding 10 showed was missing — the next recorded
  key's `IsReadLift`/`LandingKey` re-typed against the rebuilt stage AT EVERY STEP,
  not just the first). · deps: HK-53 · hard-fable · ~70.
HK-41 · Coherence-of-rebuilt + assembly (re-scoped at REVISION 2 — the induction body
  moved to HK-53/HK-54): package the rebuilt suffix into `HistoryCoherent` for the
  swapped history (coherence legs from the hypothesis's TransitionCoreL conclusion;
  realizability legs are node-data predicates — untouched by stage rebuild). ·
  deps: HK-54 · hard-fable · ~60.
HK-42 · etaData/InBox preservation: etaData reads ShapePrefix matching + side digits +
  ψ-coefficients (lift-independent by D.10); InBox reads htH (node data). ·
  deps: HK-40 · routine-opus · ~50.
HK-43 · `lift_swap` assembled: last-to-first induction over recentering nodes +
  `isCanon_historyMap`; CLOSES U17a (statement byte-unchanged modulo the HK-06
  StageTransHyp re-key ratified in HK-01's package); U17c footprint re-verified. ·
  deps: HK-40, HK-41, HK-42 · hard-fable · ~100.

### Block H — U21 + U26 (HK-44 … HK-46, + HK-51)

HK-44 · Execute the ORCHESTRATOR's Q5 ruling for U21 (REVISION 2, finding 5: both
  branches below are fence-relevant — the pin lands in a frozen-def leg, the
  alternative converts a statement to named-hypothesis form; NEITHER executes without
  the Q5 ruling from HK-01's surfaced package): if the pin landed in (NEW)'s
  `child_dig_frame` at σV, derive `RunVertexPin` at recorded transitions (mfun ≡ 0 at
  the single weight via the recorded hS5'-class clause); else convert `readsOf_HV`'s
  sorry to the ratified named-hypothesis form (honest sorries rule). · deps: HK-01 +
  the Q5 ruling, HK-06, HK-09, HK-22 · routine-opus · ~50.
HK-45 · Close `readsOf_HV` by `readsOf_HV_of_pin` + HK-44; re-verify U23's (HV) leg
  footprint. CLOSES U21. · deps: HK-44 · routine-opus · ~20.
HK-51 · (NEW unit, REVISION 2 — finding 14: R-7's "no countermodel unit needed" is
  RETRACTED; the campaign's countermodel-first discipline admits no safe-fallback
  exemption for a risky universal) COUNTERMODEL attempt BEFORE HK-46: lift
  `scratch_U18_unique_false`'s construction (non-adjacent last recentering → (HV)
  vacuous, canonRoot trivial) toward a compiled `ClassFiberWelldef` violation — two
  EtaGalEq class-mates at distinct classifier loci. Deliverable: compiled witness
  (→ HK-46 goes STRAIGHT to the deferral record, no proof attempt) or failure
  analysis naming what blocks the lift (e.g. the K3 adjacency hypothesis). ·
  deps: HK-06 (post-repair semantics) · hard-fable · exploratory.
HK-46 · U26 route attempt: `class_fiber_welldef` from U18 + thmC_b locus equality
  (§3.7); on block, report the exact residue and record the DEFERRAL (D15's hwd) —
  never a forced proof; any attempted proof must NOT thread OmUniqHyp silently (K3
  scope record). · deps: HK-51 (verdict first) · hard-fable · ~120 or deferral record.

─────────────────────────────────────────────────────────────────────────────────────────
## 5. RISKS (statements that could be FALSE as sketched → their countermodel units)

R-1 · The (S-a) repaired leg could itself be inconsistent (a wrong σV residual-law
  clause reintroduces the V10 clash). Gate: HK-05 (refutation machinery must stall) +
  HK-22 (a compiled instance must exist). Both sequenced before any prover consumes
  (NEW).
R-2 · `levelSet_no_straddle` may be FALSE even under coherence+realizability (the N-6
  option-(i) ruling assumed the geometry). REVISED at REVISION 2 (finding 2): the rim
  leg is now KNOWN pure arithmetic (§3.4 corrected route); the residual risk is the
  FLOOR leg (line dominance) only. Countermodel: HK-29 (re-targeted, post-wave),
  sequenced before HK-30/HK-31. Fallback on witness: band-aware levelSet redesign
  (N-6 option (ii)) — an orchestrator escalation, reopens U3/U5/U6.
R-3 · U10c may STILL be false at step reads if the value-interior exclusion fails at
  i ≥ 1 (root_shape is root-only by design). Countermodel: HK-37; audit HK-36. Fallback:
  the Q8 step-shape ZCSeedLaws field (D5-fence event, pre-declared here).
R-4 · The bottom-window law (HK-11a) may be underivable even from the repaired record
  (the r3b countermodel's ghost). Gate: HK-12 re-check first; fallback: the (TRANSPORT)
  clause enters SideReads/the leg as a ratified addendum (statement-fence event).
R-5 · The e ≥ 2 interior-read semantics is OPEN MATHEMATICS (B2-FINAL not accepted).
  BP2's deliverables are scoped at e = 1 (§3.3 fence); any unit that finds itself
  needing e ≥ 2 content STOPS and reports (no silent generalization).
R-6 · `StageCoreL bStage` may hit an unprovable core law at the concrete stage
  (CoeffLocLaw's closure argument is untested on concrete carriers). If HK-13/14 block,
  the gate falls back to hypothesis-carried StageCoreL (the gate then witnesses
  conditional non-vacuity — recorded, weaker) and the blocker is reported with the
  exact law.
R-7 · U26 as stated may be false without a uniqueness supplement (OmUniqHyp-as-typed is
  conditionally refuted). REVISED at REVISION 2 (finding 14): the "no countermodel
  unit needed" exemption is RETRACTED — HK-51 (countermodel attempt) is sequenced
  before HK-46's proof attempt, per the campaign's binding countermodel-first
  discipline; any attempted proof must NOT thread OmUniqHyp silently (K3 scope record).
R-8 · U17a's rebuild (now HK-49/HK-50/HK-53/HK-54/HK-41) depends on StageTransHyp'
  being STRONG enough to rebuild coherence legs verbatim AND on the HK-49 rebuild
  invariant (finding 10 — the next node's IsReadLift/LandingKey re-typed against the
  rebuilt stage, per step). HK-50 probes it concretely first. If either fails, U17a
  converts to carrying the leg-shaped hypothesis (named, ratified via HK-01's
  package — flagged now).
R-9 · (NEW at REVISION 2) The restated `v9_readSteep_all` (HK-08) may under-derive
  from the slope-clause route if the (NEW) leg's recorded clauses don't recover IAug's
  inequality at the pre-augmentation scale (§3.2 item 1). Gate: HK-48's probe.
  Fallback: HK-01 package addendum (literal IAug clause in the leg) — surfaced, not
  improvised.

─────────────────────────────────────────────────────────────────────────────────────────
## 6. ORCHESTRATOR QUESTIONS (decisions BP2 cannot make)

Q1 · Ratify candidate (S-a) (two-step regrade keying, §3.1(c)) vs (S-c) (record-less
  fallback) for the HistoryCoherent leg — HK-01's central ruling. BP2 recommends (S-a):
  it uniquely satisfies the K-gate table (§3.1(e)) and makes HC1's S9 assets the
  discharge.
Q2 · The e ≥ 2 scope fence wording (§3.3): confirm that de-vacuification at
  `e = 1, g ≥ 2` satisfies the campaign's non-vacuity bar, with `e ≥ 2` a named open
  boundary (B2-FINAL's status), or order the carry-algebra track opened as its own
  area. EXTENDED at REVISION 2 (findings 1 + 12 — this now GATES HK-06): (a) choose
  the compiled `RegradeOf` e ≥ 2 branch (RG-1/RG-2/RG-3, §3.1(c)); (b) rule the V9
  e ≥ 2 leg disposition (E-a/E-c/E-d, §3.3) — E-a is a statement-fence event on
  V9_K1nonrec's cone; E-d is a definitional scope restriction; the build cannot go
  green post-diff without one of them.
Q3 · U1b: option (i) ratify hdvd/hthr as named side conditions vs option (ii) restate
  at current-weight realizers (HK-27).
Q4 · U20b non-vacuity: accept the kernel-level witness (HK-23, species-generic) or
  commission the 3-node gate (+~5 units) (HK-26).
Q5 · The JOINT mfun/child_dig_frame pin in the repaired leg (§3.7) — U21's discharge
  hangs on this; declining it converts U21 to a named hypothesis permanently.
  REVISION 2: this GATES HK-06 (the pin is a clause choice in the diff itself);
  HK-01 surfaces the options brief, the orchestrator rules, HK-44 executes.
Q6 · U26: authorize deferral-as-endstate (D15's hwd) if HK-46 blocks, or keep it on
  the open queue for a dedicated later wave.
Q7 · Cross-area: the repaired keying obligates MovesT's G1b re-adjudication
  (mem_realizable A/B BLOCKED-FALSE records) and possibly the wave-4 seam registry
  (SideReads copies in MovesT's `eligible`). BP2 delivers the records; who owns the
  re-adjudication wave and when?
Q8 · Pre-authorization for the U10c STEP-shape ZCSeedLaws field (D5-fence event) IF
  HK-36/HK-37 report the residue — field text would be drafted by HK-36 from C.1.5's
  display, ratification by you (the D5 fence names you as the stuck-rule authority).
Q9 · U17a's binder type changes with the StageTransHyp re-key (same name, re-keyed
  Prop). Confirm this rides on HK-01's package (statement-fence: hypothesis re-keying
  on a conditionally-FALSE hypothesis is a repair, not a weakening). REVISION 2: this
  GATES HK-06 (finding 5 — the re-key executes inside the diff).
Q10 · (NEW at REVISION 2, finding 4) The two-gate order ("HK-05 + HK-22 before any
  consumer") is PHYSICALLY unsatisfiable in its strict reading: the real HK-22 needs
  the compiled (NEW) def, which needs the HK-06 wave, which needs the fallout
  re-derivations (HK-07…HK-10) for build-greenness. BP2's proposed interpretation —
  confirm or amend: (a) HK-05 + HK-21 (the STAGED compiled instance, byte-identical
  clause text) gate the wave; (b) re-derivations land INSIDE the wave, drafted against
  the staged copy; (c) HK-22 (real form) gates every post-wave prover consuming (NEW)
  (HK-52/11a/11b/23/24/25, Blocks L/H/Z provers).

─────────────────────────────────────────────────────────────────────────────────────────
END OF BLUEPRINT. Hardest units: HK-52/HK-11a (bottom-window law), HK-01/HK-06 (the
repair package), HK-49/HK-53/HK-54 (U17a rebuild), HK-30/HK-31 (no-straddle), HK-38
(U10c), HK-13/14/56 (StageCoreL bStage). Sequencing spine (REVISION 2, findings 4/15):
HK-01 (prepare) → ORCHESTRATOR rulings (Q2-ext, Q5, Q9, Q10) → HK-02/03 ∥ HK-04
(staging) → HK-05 ∥ Block G carriers (HK-13…HK-20, HK-56/57, HK-47, keying-independent)
→ HK-21 (staged gate) + HK-48 + pre-drafted HK-07…HK-10 → HK-06 WAVE (atomic: diff +
fallout + ratified V9 e ≥ 2 disposition) → HK-22 (real gate) → ALL (NEW)-consuming
provers: {HK-12 → HK-52 → HK-11a → HK-11b → HK-11c ∥ HK-23…HK-26} ∥ Block L
(HK-40 → HK-49/HK-50 → HK-53 → HK-54 → HK-41 → HK-42/43) ∥ Block H (HK-44/45; HK-51 →
HK-46) ∥ Block Z (post-wave: HK-29 → HK-30 → HK-31; HK-32/HK-55 → HK-33…HK-39; HK-36
audit may draft early). Blocks U (HK-27/28) is wave-independent.

─────────────────────────────────────────────────────────────────────────────────────────
## REVISION 2 (2026-07-30, post-Codex) — finding dispositions

Codex adversarial review: REVISE (5 CRITICAL / 10 GAP / 1 NOTE), findings at
/tmp/bridge/reviews/findings_bp2.txt. Reviewer had NO repo access (sandbox launcher
down) — findings verified against disk before disposition. Binding context: the
adjudications file ratifies BP2/Q1 (S-a) only, gated by HK-05 + HK-22 before any
consumer; everything else surfaced, never improvised.

1 (CRITICAL, HK-11 scope exceeds ratified mathematics) → APPLIED, and CONFIRMED WORSE
  than stated: post-diff the V9 e ≥ 2 legs are a BUILD-BLOCKER, not just an overclaim
  (§3.3 REVISION 2 paragraph). HK-11a now scoped `νᵢ.e = 1` in-statement; HK-11b split
  into the e = 1 closure + the orchestrator-ruled e ≥ 2 disposition; HK-11c rewrites
  (never wholesale-deletes) the e ≥ 2 caveats. ESCALATED as the Q2-extension
  (E-a/…/E-d options; E-a is a NEW potential statement-fence event).
2 (CRITICAL, invalid rim arithmetic in levelSet_no_straddle) → APPLIED. The displayed
  `(s0+wSide+1)·D ≤ …` chain was indeed invalid. Verified on disk that
  `Node.fineSlot b = b / Dwidth` (MovesC/Defs l.514): the CORRECT route is same-slot
  interval arithmetic anchored at the in-band member (§3.4 REVISION 2) — it needs NO
  window containment, and the reviewer's j = μ straddle scenario is impossible for a
  level set containing an in-band member (slot μ's window starts AT the rim). Rim leg
  downgraded to pure arithmetic; HK-29 re-targeted at the floor leg.
3 (CRITICAL, lift_swap non-recentering branch consumes the modified node) → REBUTTED
  with disk evidence: `IsNodeLift` (MovesC/Defs l.535–543) is `∃ tt, …` over
  `ν.ψ/ν.g/ν.e/ν.h/ν.σ` and NEVER projects `ν.lift`; structure-update projections are
  definitional, so the witness survives the swap (`Iff.rfl`). The reviewer, without
  repo access, inferred lift-dependence from the inventory's "read-pair lift" gloss.
  Applied-in-form: HK-40 now delivers the NAMED rfl-grade independence lemmas
  (`isNodeLift_liftSwap` + companions), which is the checkable artifact the finding
  asked for.
4 (CRITICAL, unit order violates the two-gate adjudication) → APPLIED via the
  staged-gate design: HK-21 = staged compiled instance (byte-identical clause text)
  gates the wave together with HK-05; fallout re-derivations are drafted against the
  staged copy and land INSIDE the atomic HK-06 wave; HK-22 (real form) gates every
  post-wave prover. The strict reading is physically unsatisfiable (the real gate
  needs the compiled def); the interpretation is ESCALATED as Q10 for confirmation.
5 (CRITICAL, HK-01/HK-06 execute unadjudicated fence decisions) → APPLIED: HK-01
  re-scoped to PREPARE + SURFACE (ratifies nothing; produces options briefs for the
  Q2-extension, Q5, Q9); HK-06 blocked on the orchestrator rulings; HK-26/HK-27
  re-worded to adjudication-prep; HK-44 explicitly executes the orchestrator's Q5
  ruling, either branch.
6 (GAP, RegradeOf file/dependency cycle) → APPLIED: layering resolved — the compiled
  def lives INSIDE MovesC/Defs via the HK-06 diff (no importable external home
  exists); HK-04 re-scoped to STAGING (HC2/StagingRegrade.lean, byte-identical copy,
  deleted by the wave). Every intermediate state compiles.
7 (GAP, HK-03 stated against a def HK-06 replaces) → APPLIED: the wave renames the old
  predicate to `StageTransHypOld` (archival, kept on disk) and re-points HK-03's file
  mechanically; the emptiness theorem survives as the permanent refuted-shape record.
8 (GAP, HK-17 uses ν₁ before it exists) → APPLIED: HK-17 deps now include HK-18
  (+ HK-47).
9 (GAP, S9-to-NEW interface asserted not supplied) → APPLIED: new unit HK-47.
  Verified on disk: `S9c_coreAssembly` (HC1/S9c_coreAssembly.lean l.452) takes
  `TransHyp σ ψ g Φhat e' h' zbar` (HC1/DefsTower.lean l.40) whose (e',h') is a FREE
  parameter and whose lift clause is the FRAME-pair `IsStandardLift` — so "re-dressed"
  reduces to (a) the named frame-pair lift conversion (IsReadLift at the root pair ↔
  IsStandardLift) + (b) assembling TransHyp at (ν₁.e, ν₁.h); both are HK-47
  deliverables with a stop-and-report clause on shape mismatch.
10 (GAP, StageTransHyp' lacks the suffix-induction invariant) → APPLIED: the rebuild
  invariant is now the NAMED unit HK-49 (§3.6 REVISION 2 bullet), probed concretely by
  HK-50 and threaded by HK-54's induction; R-8 extended with the residue fallback.
11 (GAP, identity-regrade clauses not shown to be Stage laws) → APPLIED: HK-01's
  package must include the per-clause identity-discharge table (§3.1(c)); HK-04
  discharges against it; an undischargeable clause REOPENS HK-01.
12 (GAP, compiled meaning of RegradeOf at e ≥ 2 unspecified) → APPLIED: §3.1(c) now
  displays the three compiled forms (RG-1 guarded residual clauses / RG-2 guarded leg /
  RG-3 rejected); choice ESCALATED (Q2-extension) — BP2 recommends RG-1 and does not
  decide.
13 (GAP, no countermodel units before HK-08/HK-41) → APPLIED: HK-48 before HK-08,
  HK-50 before the rebuild provers. Verification went further than the finding: the
  OLD `v9_readSteep_all` statement is provably FALSE under (NEW) (child_h : σᵢ.h =
  νᵢ.h + Node.he/hh collapse it — §3.2 item 1 corrected; the original blueprint sketch
  claimed it "re-derives", which was wrong). It is a PRIVATE lemma → restatement is
  proof-internal (no fence); HK-08 re-scoped to restate + re-derive; risk R-9 added.
14 (GAP, U26 exempts itself from countermodel-first) → APPLIED: R-7's exemption
  retracted; new countermodel unit HK-51 sequenced before HK-46.
15 (GAP, Block Z ordering unsound) → APPLIED: Block Z re-sequenced post-wave (dep
  HK-06; "launch immediately" retracted — a countermodel search under OLD's
  contradictory steep zone is spuriously empty); HK-30 now depends on HK-29's verdict;
  HK-36 (read-only audit) may draft early, verdict re-validated post-wave.
16 (NOTE, oversized units) → PARTIALLY APPLIED: real splits where a clean seam exists —
  HK-52 (out of HK-11a), HK-53/HK-54 (out of HK-41), HK-55 (out of HK-33), HK-56 (out
  of HK-13), HK-57 (out of HK-19); the remaining flagged units (HK-09, HK-11b, HK-14,
  HK-20, HK-31, HK-38) carry MANDATORY named-seam splits triggered at 100 proof lines
  (upgraded from the vague "split if over"). Not applied further: blueprint-level ids
  for every possible fragment would freeze seams better chosen at proof time.

ESCALATIONS (orchestrator, do-not-proceed items): the Q2-extension (compiled RegradeOf
e ≥ 2 branch + V9 e ≥ 2 leg disposition — potential NEW statement-fence event on the
V9_K1nonrec cone; HK-06 is blocked on it); Q5 and Q9 confirmed as HK-06 gates; Q10 (the
two-gate wave interpretation). Unit count 46 → 57; all pre-existing ids stable, scopes
revised as logged above.
