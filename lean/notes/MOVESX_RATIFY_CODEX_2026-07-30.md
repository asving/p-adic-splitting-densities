# MovesX AS-BUILT RATIFICATION (Codex, 2026-07-30): RATIFIED — "faithful in both directions"; the authorized eventual-envelope restatements confirmed; no forbidden X.2 dependencies.

I’ll audit the 40 declaration statements against the note and blueprint, with special attention to the restated XE.2/XE.3 binder order, non-vacuity, and both implication directions. I’ll quote each statement’s operative form and classify it, then give a strict RATIFY/REJECT count.

Verdict: RATIFY (40/40; N=40, M=0)

I find no statement-level mismatch with §X-EXHAUST. The corpus preserves the note’s conditionality, populations, domain restrictions, null tags, and n-only/p-free quantifier discipline.

### Quote-and-classify

| # | Unit | Operative statement | Classification |
|---:|---|---|---|
| 1 | XA.1 | “`rowOf ν = T5/T1/T2/T3/T4 ↔` the five stated cases” | Exact row partition |
| 2 | XA.2 | “`μ ≥ 2 ∨ g ≥ 2 → 2 ≤ ell ∧ rowOf ν = T1`” | Exact selection forcing |
| 3 | XA.3 | “T1 witness `(s₀+e,u₀+(ell−1)h) ∈ region`” | Exact P1 on-or-below witness |
| 4 | XA.4 | “`h(ell−1) ≤ eh ell+1−e−h` and doubled index positive” | Exact T1 arithmetic |
| 5 | XA.5 | “T2 witness `(s₀+1,u₀+1)`; doubled index ≥2 and even” | Exact T2 witness/ruling |
| 6 | XA.6 | “`2·card(p1Region … 1)=(e−1)(h−1)`” | Exact ℓ=1 lattice count |
| 7 | XA.7a | Region decomposes into stride-column slices | Faithful IND decomposition |
| 8 | XA.7b | “`2·card region = ell·(eh ell+1−e−h)`” | Exact general IND formula |
| 9 | XA.8 | T3/T4: “`region.card=0 ∧ ind2=0`” | Correct no-positive-table-bound semantics |
| 10 | XA.9 | “`ind2 ν=2·region.card`”; T1/T2 imply `ind2≥1` | Exact raw table |
| 11 | XB.1 | “`dCert ⟨false,false⟩ H=0`” and `dCert≤dTotal` | Exact current-discharge convention |
| 12 | XB.2 | Inc-only certification = incT12; full = incT12 + recT1 | Exact `(CD)` populations |
| 13 | XB.3 | “`dTotal=incT12+recT1+t3+t4`” | Exact four-population partition |
| 14 | XB.4 | “`#T3≤Nat.log 2 n`” under degree-product budget | Exact T3 degree charge |
| 15 | XC.1 | Stratum → `DescX`; `DescX d → 2d≤vdisc` | Exact conditional X.1a chain |
| 16 | XC.2 | Same chain at zero discharge without ALIGN | Exact honest trivial face |
| 17 | XC.3 | “`2 dTotal≤(2s+1)vdisc+2 log₂n`” under ALIGN-inc + full WC | Exact cleared X.1b reduction |
| 18 | XC.4 | Same bound via full ALIGN + T4 WC | Exact X.3(a1) route |
| 19 | XD.1 | NsFree decidable; cap is own cap/0; `maxH []=sumH []=0` | Exact domain conventions |
| 20 | XD.2 | “`sumH≤(dTotal+1)·maxH`” | Exact terminal-read correction |
| 21 | XD.3 | Product bound implies squared dichotomy | Exact √-shape arithmetic |
| 22 | XD.4 | `N−1−ccap < (c0+ccap)(d+1)²` or corresponding height square | Exact X2-HYP assembly |
| 23 | XE.2 | `∃N₀,c3,c4>0`, before `∀X,p,N`, exponential bound for `N≥N₀` | Faithful restated exponential envelope |
| 24 | XE.3 | `∃N₀,c3′,c4′>0`, before `∀X,p,N`, √N bound for `N≥N₀` | Faithful restated fallback envelope |
| 25 | XF.1 | `p^D ∣ discr f ↔ reduced discriminant=0` | Exact cylinder bridge |
| 26 | XF.2 | “`‖discr f‖=‖Res(f,f′)‖`” for monic positive degree | Exact resultant bridge |
| 27 | XF.3 | Constructs a splitting multiplicative norm extension | Internal faithful apparatus |
| 28 | XF.4 | Resultant norm = `‖n‖^n` times root-value product | Exact root-product/offset form |
| 29 | XF.5 | Sum bound gives one term with `K≤card·v` | Exact sign-free pigeonhole |
| 30 | XF.6 | Event points in the same selected ball are congruent modulo `p^tailExp.toNat` | Exact ball-cover statement |
| 31 | XF.7 | `tailCount·p^tailExp ≤ (n−1)p^(nD)` | Exact cleared (3b) |
| 32 | XF.8 | Normalized tail count tends to zero | Exact (3b) limit |
| 33 | XF.10 | “`frac(discZero)=0`” | Exact discriminant-zero corollary |
| 34 | XG.1 | Branch-length bound from either X.3(a) route | Exact route glue |
| 35 | XG.2 | No infinite rooted path implies finite branch type | Exact König bridge |
| 36 | XG.2b | Finite tree with ns-free leaves is detected at some level | Exact X.3(b) |
| 37 | XG.2c | “`⋂Undec ⊆ InfTree ∪ ⋃nsFiber`” | Exact trichotomy |
| 38 | XG.2d | Off `discZero`, X.3 route implies finite tree | Exact X.3(a) |
| 39 | XG.3 | “`frac(Undec N) → 0`” under routes + ns-null | Exact X.3 density form |
| 40 | XG.4 | Per-σ fiber series has sum `densityOf S σ` | Exact Tonelli/VP-SOUND identity |

### Kernel statement fence

The eight open/named hypotheses are also accurately represented:

- `X1aAlignP`: `dCert st H ≤ ind f` on the relevant stratum, off `discZero`.
- `WeightChargeT4P`: `#T4 ≤ s(n)·vdisc`.
- `WeightChargeFullP`: `#recT1 + #T4 ≤ s(n)·vdisc`.
- `X2AffP`: `threshold ≤ 1+c₀Σh`.
- `X2CapP`: the cap bound on CAP-DETECTABLE fourth-piece branches.
- `X2BridgeP`: both the displayed covering inclusion and the a.e. witness clause.
- `X2TailsP`: the defined global `TallEvent` tail.
- `X2ProgressP`: the linear deep-or-tall target with positive n-only slopes.

These are stated as hypotheses, not falsely proved.

### XE.2/XE.3 adjudication

The post-refutation repairs are correct:

- `N₀`, decay constants, and slopes are chosen before `∀ X`, `∀ p`, and `∀ N`.
- They therefore depend only on `(n,K)`, where `K : XConsts n` is itself p-free.
- The conclusions apply only for `N ≥ N₀`.
- This is the proper eventual interpretation of the note’s envelope displays; the deliberately toothless/small-N regime creates no contradiction.
- The recorded root-only countermodel can satisfy the repaired statement by choosing `N₀ ≥ 2`, while it refuted the former `∀N` statement.

### Non-vacuity and conditionality

The statements are non-vacuous at the intended statement-fence level:

- All kernels concern the one shared `XFamily`, rather than independently selectable mock objects.
- `XCtx.root` makes each instantiated branch presentation rooted.
- The actual `XFamily`/series realization remains the explicitly ledger-tracked MovesD/MovesT synchronization obligation; this corpus does not pretend to discharge it.
- X.3 retains `NsNullP`, both permitted X.3(a) routes, and VP-SOUND/additivity exactly where consumed.
- No forbidden dependency on X.2 is introduced into qualitative X.3, RS.1, SQ.1, or RS.3.

Accordingly, the as-built statement corpus is faithful in both directions: it neither claims an open kernel unconditionally nor weakens a proved/derived note conclusion beyond the expressly authorized eventual-envelope restatement.