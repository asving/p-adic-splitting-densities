# FABLE CONFIRM — §X-EXHAUST rev 8 (fresh-instance dual half; 2026-07-27)

Scope: MOVES_2026-07-24.md lines 10947–11555 (§X-EXHAUST rev 8) + the rev-8
repair record (lines 12679–12689). Predecessor: FABLE_VERIFY_XEXH_2026-07-27.md
(rev 7: 0 crit, 1 record gap, 2 cosmetics). Charge: (a) verify the three rev-8
fixes; (b) confirming sweep at own emphasis; (c) one own-construction check.

## A. The three rev-8 fixes — ALL APPLIED, each verified against its finding

1. STATUS stamp (predecessor GAP F.1) — APPLIED. Line 11555 now ends:
   "STATUS [rev 8]: ACCEPTED at the Codex bar (passes 8+9 on rev 7)" — matches
   the acceptance record (line 12655); the contradicting "Awaits pass 4" tail is
   STRUCK (0 grep hits file-wide); "census layers of gate X-n3 not yet run"
   preserves the honest unrun state. Residual cosmetic WATCH: the block still
   OPENS "STATUS: rev 4, 2026-07-27 —" as a historical layer; non-contradictory
   now (the repair narrative it stamps is true as history), but dual stamps.
2. Slope display (F.2) — APPLIED, EXACT. Line 11531:
   "c_d/((s+1/2)(n−1)) = 1/10" — recomputed (1/2)/(2.5·2) = 1/10; the struck
   factor 2 would give 1/20; value and toothless conclusion unchanged, as noted.
3. T1 gloss (F.3) — APPLIED and now COMPLETE against the μ·g/e·g semantics.
   New clause names "the e ≥ 2, (g,μ) = (1,1) increments and the e = 1,
   (g,μ) = (1,1) recenterings". Case-exhaustion re-derived: g ≥ 2 ⟹ increment
   with μ·g ≥ 2 (clause 1); g = 1, μ ≥ 2: e ≥ 2 increment (clause 1) / e = 1
   recentering (clause 2); g = 1, μ = 1: split by e·g per the line-6228
   dichotomy + D.10 Species lemma — the two NEW sub-cases. All (g,μ,e) covered,
   no overlap; "the row's ind(S) formula always covered them" is right (row
   key is ℓ ≥ 2 alone; the witness point argument never uses the selection).

## B. Confirming sweep, own emphasis (rev-7-unprobed spots) — NO FINDINGS

1. X.3 route arithmetic AT DEPTH, both disjuncts: (a1) #T1/T2(incl.
   recenterings) = d_cert ≤ v/2 via (X.1a) with BOTH sub-clauses ((CD)'s
   "given both" clause supplies exactly this population); #T3 ≤ ⌊log₂n⌋;
   #T4 ≤ s(n)·v via X.1b(T4). (a2) #increments ≤ v/2 + ⌊log₂n⌋ (inc-only
   d_cert + T3); #recenterings ≤ s(n)·v (full burden). Off disc = 0, v < ∞
   ⟹ every branch finite in EITHER route; König bridge needs only finite
   menus. Tags match the CONDITIONALITY SUMMARY verbatim. COHERENT.
2. (X2-HYP)'s rebased constant (unprobed at rev 7): N < thr + cap ≤
   (1 + c₀Σh) + c_cap(1 + Σh) = (1 + c_cap) + c₀⁺·Σh ⟹ Σh >
   (N−1−c_cap)/c₀⁺ — the displayed constant EXACT; the √ step and the sealed
   c_cap = 0 face ⌈√((N−1)/4)⌉ at c₀(3) = 4 both recompute. EXACT.
3. (X2-CAP) openness: level-relative by construction (cap grows along deeper
   shapes; only the RATE vs Σ h_r conjectured) — consistent with TB-CAP's
   no-uniform-cap record; trivially true on convention-0 branches; listed
   OPEN under PROGRESS in X.4; X.3(a)–(d) cites neither (X2-BRIDGE) nor
   (X2-CAP). COHERENT.
4. X.1b reduction + deep leg re-derived: v ≥ (d_total − ⌊log₂n⌋)/(s + 1/2);
   (3b) at that D matches the displayed leg; env*(8) recomputed > 1 both
   primes (p=2: 1 + 6.625; p=3: 6 + ~1.96) — toothlessness honest. EXACT.
5. Gate layers: CUM(j) is per-prefix, values in ν's own frame — the raw-table
   transport independent of (CD) discharge, as claimed; the s(3) ratio test
   well-posed (a recentering forces v ≥ 1); ℓ = 1 ⟹ (g,μ) = (1,1) from
   μ·g ≤ ℓ (table header) checks. OK.

## C. Own-construction check — PASS (fresh degree n = 5 + fix-3 instance)

Extension of the preserved checker's layer 1 to QUINTICS over ℤ₂ (a degree
neither prior verifier censused; ⌊log₂ 5⌋ = 2): box 4⁵ (760 f, rows T3/T4
only) + a tall-polygon slice (a₀ ∈ {4,8,12}, even a₁..a₄: 768 f, rows
T1=96/T2=160/T3=720/T4=416) — v₂(disc f) ≥ 2·Σ ind(S): 0 violations in
1528, min slack 0 (first box) / 4 (tall slice). Targeted instance of fix 3's
NEW T1 case (e = 2, (g,μ) = (1,1) increment on an ℓ = 2 side):
f = (x²−3)(x²−6)/ℤ₃, side (e,h,ℓ) = (2,1,2), residual y²+2 = (y−1)(y+1)
over F₃ (two distinct g = 1, μ = 1 selections — exactly the added gloss
case); ind(S) = 2, v₃(disc) = 6 ≥ 4. EXACT.

## VERDICT

The three rev-8 fixes: APPLIED / APPLIED / APPLIED-and-complete. Confirming
sweep at fresh emphasis: 0 critical, 0 gaps. Own construction: 1528-f quintic
census 0 violations + the new-T1-case instance exact. One cosmetic WATCH
(the retained historical "rev 4" stamp head, non-contradictory). CLEAN.
