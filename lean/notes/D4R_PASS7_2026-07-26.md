1. **GAP — the event-bound equality criterion is asserted at pointwise strength, not derived.**  
   The proof says: “equality iff no \(f\) is double-counted, i.e. \(\mathrm{mult}_{\widehat P}\equiv1\) on the event.” Measure theory directly yields only \(\mathrm{mult}_{\widehat P}=1\) almost everywhere. Here the stronger pointwise statement is recoverable because L8 gives finitely many fibers and each is a finite-level digit cylinder, so any nonempty overlap has positive mass; however, that cylinder argument is not displayed. The same omission propagates to D4R.4-EV.

2. **GAP — one displayed gate justification uses the wrong degree threshold.**  
   D4-R.6 says “\(\deg 3<2g_0\mu_0+g_0\) forbids a second” eligible quadratic factor. With \(g_0=2,\mu_0=1\), this merely says \(3<6\); the relevant exclusion is
   \[
   \deg R_{\rm anch,0}<2g_0\mu_0=4.
   \]
   The gate conclusion remains correct: the pinned factorization \(R_0=\psi(z-r)\) and \(\deg R_0=3\) prove that the quadratic factor is unique. Thus this is a defective displayed rationale, not a failure of the gate.

3. **Verified — rebuilt tree and fibers.**  
   \(T_{\rm can}(f)\) is genuinely choice-free: it retains all eligible \((s,\psi)\) continuations and makes no factor or side selection. Canonical lifts are determined by branch data, not by \(f\) alone. The tree is finitely branching.  
   The branch-indexed fibers are well-defined through L6. L8 makes \(\mathrm{Pref}(\widehat P)\) finite, so
   \[
   \mathrm{mult}_{\widehat P}(f)=\sum_{\eta\in\mathrm{Pref}(\widehat P)}
   1_{S(\eta,\top)}(f)
   \]
   is finite and measurable. The exact SUM law legitimately sums individual nonempty-history applications of Theorem C(b), with the empty history handled separately; it consumes no disjointness.  
   The rejected multiset alternative is rejected for a valid reason: Theorem C is single-chain and supplies no exact mass theorem for simultaneous constraints in several descendant frames.

4. **Verified — the \(n=4,p=5\) refutation instance.**  
   The displayed polynomial has the stated digits and residual
   \[
   R_0=(z-1)^2(z-2)^2.
   \]
   One deterministic tree carries both branches, their branch strata coincide, the sum counts multiplicity two, and the event counts once. The six cells are the \(\binom{4}{2}=6\) unordered pairs of distinct nonzero roots in \(\mathbf F_5\); hence the excess \(6\cdot5^{-14}\) is correct.

5. **Verified — downstream propagation, subject to Finding 1.**  
   L11 correctly adds the factor-selection coordinate and counts branches even when two labels have equal strata. D4R.2 follows by the union bound and the enlarged injection:
   \[
   \mu(\widehat P)\le M(\widehat P)p^{-A_{\rm net}(\widehat P)}.
   \]
   The branch rebuild preserves this \(M\)-carrying consumer chain. The literal coefficient-one (3i) does **not** survive generally; the note states this honestly and queues the plan-interface change.  
   L12 correctly gives branch-weighted finite-level sums and event bounds, including the empty-\(\mathrm{Pref}\) case. D4-R.5 accurately records the refuted claims and warns downstream consumers that cross-shape events form a cover, not a partition.

6. **Verified — D4-n3 gate values and meanings.**  
   Each root cell has exactly one quadratic branch and each \(R_1\) exactly one linear branch, so \(\mathrm{mult}_{\widehat P^*}\equiv1\). Consequently the SUM and event quantities coincide on this instance. The sealed arithmetic is consistent:
   \[
   A=11,\quad W=7,\quad A_{\rm net}=4,\quad M=1,\quad N=5,
   \]
   with \(C(2)=3\), \(C(3)=48\), and all four displayed finite-level counts correct.  
   The pinned depth-one side also directly satisfies SAE: at the only span slot \(j<\mu_0\), its height is \(4\), strictly above the old box-line height \(3\). The \(m\ge2\) regime is not tested and is explicitly left to a queued multiplicity gate.

7. **Full-sweep result.**  
   No renewed use of a single-run ontology, node-stratum exclusivity, fiber disjointness, termination, or cross-shape partitioning was found. The open kernel and the forward dependency on §C’s queued SAE repair are consistently advertised. Apart from Findings 1–2, the rev-6 architecture is internally coherent.

**Counts: 0 critical / 2 gaps.**

**Verdict: NOT ACCEPTED.**
