1. **Rev-6 citation edits — CLEAN.** The §T.3 statement, “the root product's citation authority is the Montes-L1 cite (D4R0K O4's scope note),” correctly replaces JOINT-D1 as authority while retaining the root clause under `(SIB)`. The matching §T.4 root clause is consistent.

2. **JOINT-D1 scope sweep — CLEAN.** Every remaining occurrence describes JOINT-D1 as “single-cluster evidence” or records the historical repair. No live argument relies on it for a cross-cluster product.

3. **End-to-end acceptance sweep — CLEAN.** TB-CAP including `(ns-null)`, TREE-EXP/TREE-N, root and joint-cell accounting, `(SIB-STEP)`, sibling indexing, ONE-F/pre-halt conversion, gate records, and the honest perimeter remain internally consistent. The open `(SIB)`, conditional `(JC-multi)`, VP-SOUND citations, X.3/CL-4, and `[2r]` dependency are explicitly carried rather than silently assumed.

**Counts:** 0 critical findings; 0 citation gaps.

**Verdict: ACCEPTED (0/0)**
