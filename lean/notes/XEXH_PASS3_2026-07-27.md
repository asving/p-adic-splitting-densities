1. **CRITICAL — pass-2 bridge repair: PARTIAL.** The contradiction with TB-CAP is removed: an H-dependent cap may grow with height, and TB-CAP does not refute the proposed rate
   `cap(H) ≤ c_cap(n)·(1 + Σ h_r)`. That rate is genuinely open; nothing pasted proves either linear growth or superlinear countergrowth.

   However, `cap(H)` is not defined on the stated domain. X.0 lets H range over arbitrary finite branch histories, while `cap(H)` is “the max … of H’s OWN leaf-detection cells.” A continuing prefix has no leaf-detection cell; a root `(τ-hen)` branch has no window read; and a `(τ-ns)` leaf has no TB-CAP cap. Nevertheless X.2 and X2-CAP quantify over “every branch history H.” The maximum can therefore be over an empty/non-cap-detectable family, making the repaired X.2 proposition ill-typed. The four-way decomposition isolates the intended cap-detectable finite branches but does not repair those universal statements. The parallel definition `max h(H)` also lacks an empty-history convention.

2. **CORRECT — pass-2 termination repair: GENUINE.** The four populations
   `{T1/T2 increments} ⊔ {T1 recenterings} ⊔ {T3} ⊔ {T4}`
   partition the continuing nodes. In route (a1), full ALIGN bounds T1/T2 by `v_p(disc f)/2`, the degree charge bounds T3, and the restricted burden
   `#T4 ≤ s(n)·v_p(disc f)`
   supplies exactly the missing population. In route (a2), ALIGN-inc bounds non-T3 increments, the degree charge handles T3, and full WEIGHT-CHARGE handles both recentering classes. The corrected disjunction is arithmetically sound, and the T4-restricted X.1b burden is the honest minimal form for (a1).

3. **CRITICAL — the √N envelope is locally overclaimed.** X.2 says:
   > “Given only (X2-HYP) (+ (X2-AFF), (X2-CAP), (X2-BRIDGE)): env(N) ≤ …”

   This does not follow “given only” those hypotheses. The deep estimate additionally needs `X.1b ∧ ALIGN-inc` and the tall estimate needs X2-TAILS. The later CONDITIONALITY SUMMARY correctly restores those tags, so the note contains two incompatible dependency statements. This violates its stated rule that every conditional display carry its tag.

4. **CRITICAL — the consumer map contradicts X.3.** X.3 explicitly says it is:
   > “the ONE cite point … for [4] (SQ.1’s clause (b)),”

   while X.4 says:
   > “NOTHING here feeds … SQ.1 … any such citation is a drafting error.”

   Both cannot be true. The acceptance dependency ledger is therefore not coherent end to end.

5. **CORRECT — remaining sweep.** The index-table arithmetic and endpoint conventions check; (CD) is consistently used; the affine X.1b reduction retains the additive T3 term; the rebased `c₀⁺ = c₀ + c_cap` calculation is algebraically correct; X2-TAILS remains fenced; the König, finite-level detection, ns-null and monotone-assembly steps are properly separated; and the sealed numerical faces are accurately labelled as candidates or toothless observations.

**Counts: 3 critical, 0 gaps.**

**Verdict: NOT ACCEPTED.**
