## §H-DOMAINS pass 6 — rev 5 fresh acceptance-depth audit

### 1. Rev-5 repair, independently rechecked

The pass-4 defect is closed.  For a bound on coordinate \(h_i\),
\[
  h_i\ge \phi_i(h_{<i})+1,
\]
the reverse-triangular stride choice gives
\[
  a_{ij}S_j\in S_i\mathbf Z
\]
whenever \(\phi_i\) cites \(h_j\).  Hence on a fixed stride cell,
\(\phi_i\) changes by \(S_i\)-multiples and
\(\lceil\phi_i\rceil\) is an integer-valued affine function.  The two
branches
\[
  \lceil\phi_i\rceil+1\ge f_i,\qquad
  \lceil\phi_i\rceil\le f_i-2
\]
are therefore exact complements.  This is the needed comparison: it is
made after integerization, not on the raw rational affine form.

The pass-4 witness
\[
  y\ge x/2-1/2
\]
was recomputed.  Refining by \(x\bmod 2\) gives least-value maps
\(x/2\) on the even cell and \((x-1)/2\) on the odd cell.  Thus the
least values are \(0,0,1,1,2,2,\ldots\), including the formerly omitted
\(x=0\) tie.  The two displayed linear sets are exact, disjoint, and
freely parameterized.

### 2. Break attempts against the class-(TRI) construction

No new counterexample was found.

- Several positively cited earlier coordinates do not break the
  domination recursion.  On a floor-dominated branch, every coordinate
  with positive coefficient is bounded and can be enumerated; zero-
  coefficient coordinates remain live harmlessly.  On the complementary
  branch, the first-coordinate slab decomposition is finite: above its
  solo threshold it contributes one constant lower bound, and below the
  threshold its stride values form a finite list before recursion on the
  remaining cited coordinates.
- Splits and substitutions alter constants and may fix or drop variables,
  but introduce no new slope denominator or congruence modulus.  The
  precomputed strides therefore remain valid on every descendant piece.
- Re-splitting caused by a merged constant floor moves strictly to an
  earlier coordinate.  Together with finite enumeration at each slab,
  this supplies the stated well-foundedness.
- On a terminal piece the class lift of the single dominating bound is
  affine.  Nonnegative slopes make the triangular substitution periods
  coordinatewise nonnegative, and the positive diagonal strides make the
  parameterization injective.  Residue cells and the complementary
  domination branches supply disjointness.

The text does not extend this proof to multiple lower bounds, mixed or
negative slopes, or the lower-and-upper-bound non-adjacent slack.  Those
cases remain explicitly under `H-LIST-GEN`, so none of these break
attempts is being silently absorbed into the proved class.

### 3. Gate and consumer audit

The gate family remains unchanged by rev 5.  Its only cross-coordinate
bound has integral slope \(2\), so integerization and the floor split are
vacuous.  The two listed components
\[
  L((1,3);\{(2,4),(0,2)\})\;\sqcup\;
  L((2,5);\{(2,4),(0,2)\})
\]
are disjoint by \(h_0\)-parity and have the stated coarse union.  For
\(E=(3h_1-1)/2\), the margins are \(3\) on \((1,2)\) and \((0,2)\), and
\(6\) on \((2,4)\); the zero \(h_0\)-weight continues to refute
coordinatewise positivity without threatening the per-period
convergence claim.

The proof-order and status sweep is also clean:

- no G-bearing CTS-M clause is consumed before H.5;
- `[1v]-FULL` is explicitly not recorded;
- the mass face is separated from `U-R-COUNT`;
- deep exactness is separated from `XHD-d-EX(∂)`;
- class-(TRI) listing and summation are separated from `H-LIST-GEN`;
- `M4b-T-AUD`, `XHD-u-X`, and the per-family `E-POS` displays remain
  visible;
- the per-component-\(E\) and per-period-positivity spec divergences
  remain flagged rather than reported as verbatim V.0 delivery;
- H-n3 is described as a sealed, not-yet-run refuter and does not close
  any kernel.

Counts: **0 critical / 0 gaps**

Verdict: **CLEAN (0/0).**

This is the second consecutive clean Codex audit on the identical rev-5
mathematics (passes 5 and 6).  It establishes the Codex text-review bar
only.  It is not acceptance of §H-DOMAINS, does not discharge
`[1v]-FULL`, and does not alter the recorded blockers: the named kernels,
H-n3 census, STATE-probe-2, the heterogeneous cross-component re-seal,
and the required independent confirmation remain outstanding.
