## A. Pass-12 finding dispositions

| Pass-12 finding | REV 13 disposition | Classification |
|---|---|---|
| C1. TREE-N counted an exact ns fiber modulo \(p^N\), and \(b_e^{ns}\) double-counted a tail already propagated through \(K_e\) | “the residue set … is recorded **UNDEFINED**”; TREE-N now counts only the “**LUMPED FIBER \(T[N]\)**”; the “**unconditional additive insertion of \(b_e^{ns}\) is STRUCK**”; \(b_e=b_e^{\mathrm{term,fin}}+b_e^{\mathrm{split}}\) | **GENUINE.** Both ill-typed sites are repaired, and the repair is propagated through the live consumers. |
| C2. PCI created a hidden \([3t]\leftrightarrow[3]\) cycle | RS.0\((\beta)\) is repackaged as the displayed hypothesis **XRB** at \([3t]\)-COND; \([3]\) proves it; \([3]\to[3t]\)-FULL is discharge-only; PCI is struck from TREE-EXP inputs | **GENUINE.** The conditional-package pattern actually removes the cycle, provided RS.0\((\beta)\) is proved before the two PCI substitution sites inside \([3]\). |
| K1. `(ns-null)` lacked an honest refutation scope | finite-lump masses/decay rates have a named census falsifier; nullity itself is explicitly proof-only; the claimed proof remains pending dual passes | **GENUINE.** The epistemic boundary is now honest. |
| K2. Countability of \(\mathcal T^{ns}\) was unsupported | finite pruned symbolic trees are indexed by finite words, integer parameters, and finite residue decorations | **GENUINE.** This excludes arbitrary infinite value-decorated trees and supplies a countable indexing. |
| K3. TREE-EXP-ns lump factors could be read unconditionally | every lump factor is explicitly conditional in the ns leaf’s transported entrance frame | **GENUINE.** The shared prefix is no longer at risk of being charged twice. |
| K4. \([3t]\)-FULL sounded globally unconditional | it is pinned as discharge of X.3 and XRB only, while every remaining CL-10 tag persists | **GENUINE.** The acceptance semantics are now explicit and noncontradictory. |

## B. End-to-end ns trace

The route now uses one coherent convention.

1. **TGT.** Exact ns trees are legitimate measurable tree fibers, but their contribution is retained as a separate \(\mathcal T^{ns}\) sum. That sum is dropped only with the displayed `(ns-null)` qualifier. No finite-level detection is inferred.

2. **TREE-EXP.**
   - \(\mathcal T^{fin}\): ordinary finite product of node fresh-volume factors.
   - \(\mathcal T^{ns}\): exact fiber \(=\bigcap_N\) capped/lumped fibers; every capped mass is a finite product whose ns-leaf factor is conditional in the leaf frame; the exact mass is the displayed limit. Zero is concluded only under `(ns-null)`.

3. **TREE-N.**
   - Exact ns residue sets modulo \(p^N\) are explicitly undefined.
   - The counted object is only \(T[N]\), with each ns leaf replaced by its level-\(N\) lump.
   - The exact fiber returns solely through
     \[
     \operatorname{fiber}(T)=\bigcap_N\operatorname{fiber}(T[N])
     \]
     and continuity from above.

4. **RS.PRE/\(K_e\).** Finite ns shadows remain in the continuing vanishing-digit transitions of \(K_e\). They are not terminal columns.

5. **\(b_e\).**
   \[
   b_e=b_e^{\mathrm{term,fin}}+b_e^{\mathrm{split}}.
   \]
   There is no unconditional \(b_e^{ns}\) insertion. The eventual-ns object is only a tail residual outside the recursion.

6. **Recursion.**
   - `(R_e-lump)` unconditionally computes the all-finite-leaf completion value: paths that continue forever toward ns contribute to no finite exit in the Neumann series.
   - `(R_e-exact)` identifies that value with the full subtree density only under `(ns-null)`.

7. **SQ.1 and `(†-AGG)`.** Decided finite-level objects range only over \(\mathcal T^{fin}\). Exact ns fibers stay in \(\operatorname{Undec}(N)\) at every finite \(N\).

Thus no live site treats an exact ns event as an \(N\)-stable cell, a per-node terminal factor, or an unconditional exit. The previous double-counting channel is gone.

The only intentionally schematic object is the non-null “ns residual terms” in `(R_e-exact)`. That is harmless for the present architecture because no unconditional formula consumes those terms and the theorem proceeds only after `(ns-null)` makes them zero.

## C. XRB/PCI dependency audit

The relevant path is now:

\[
[3t]\text{-COND}\xrightarrow{\text{conditional TREE package}}[3]
\xrightarrow{\text{prove RS.0}(\beta)=XRB}[3t]\text{-FULL}.
\]

This is not a cycle because:

- TREE-EXP/TREE-N no longer consume PCI or XRB.
- RS.0\((\beta)\) is a CTS/type-closure theorem and can be proved without the PCI substitutions.
- PCI is consumed only later, at \(b_e^{\mathrm{split}}\) and RS.1-SH.
- \([3t]\)-FULL is a discharge event and is consumed by nothing.

No other bidirectional hypothesis package remains:

- `[1v]-COND → [2b] → [1v]-FULL` is the already explicit XHD conditional/discharge pattern.
- `[5] → [3t]-FULL` discharges X.3 only.
- `(ns-null)` is an open internal CL-10 condition, not a backwards dependency on a downstream note.
- REL, CTS-M(iv)-REP, and XHD all precede the relevant \([3t]\)/\([3]\) events.

The one drafting safeguard still needed is an explicit internal order in \([3]\): prove RS.0\((\beta)\)/XRB before invoking PCI in RS.PRE or RS.1-SH.

## D. Ledger and kernel roster

The CL-1…CL-17 ledger is now structurally complete. Its status is:

- **CL-1:** ESCAPE(E0), open.
- **CL-2:** X.1b/WEIGHT-CHARGE, open.
- **CL-3:** X.2 PROGRESS and quantitative constants, open.
- **CL-4:** X.3 termination/a.e. exit package, pending \([5]\).
- **CL-5:** nine RS.PRE inputs; major open components include XHD/M4a-X, HMC, CTS measured-row duties, ACT/ACTIVE-VALUE, INIT/ENT-COUNT/INIT-RAT, and COMP typing.
- **CL-6:** CTS-M(iv)-POLY, open.
- **CL-7:** TRACK-COUNT, \(D(n)\), and positive-genuine-increment companions, open.
- **CL-8:** REL.1, REL.2(a)/(b)/(d), REL.3, open.
- **CL-9:** RS.0 lumpability and height-shift bisimulation/XRB, open.
- **CL-10:** TREE package: `(SIB-STEP)`, VP duties, `(ns-null)`, TREE-EXP/TREE-N derivations and T-n3. `(ns-null)` has a claimed proof pending passes; SIB-gate has passed but does not close `(SIB-STEP)`.
- **CL-11:** SQ.2 no-equal-\(e\)-feedback plus DEG-CONS roster audit, open.
- **CL-12:** closed perimeter; O3 is only a capstone display duty.
- **CL-13:** CTS-M(i) fixed syntax/state partition, open.
- **CL-14:** M1/M5 species catalogue and completeness, open.
- **CL-15:** X.1a GMN index dictionary, open.
- **CL-16:** M4b/M4b-T and the \(\pi_v\) equivariance alternative, open.
- **CL-17:** RS.1-DEEP/SH, unique shallow cut, split routing, and \(W_{\widehat S}\) regrouping, open.

The kernel/gate roster remains honest:

- Passed support gates: D4-m2 and SIB-gate.
- Required but unrun: T-n3, including its independent direct-walk side.
- Blocking/open gates and probes remain attached to VALUE, XHD/MENU, REL, STATE-probe-2, EXHAUST/STALL, ESCAPE/RESUM, TRACK, and SQUEEZE.
- No passed finite gate is represented as proving an all-depth kernel.

## E. DAG audit

The displayed order

\[
\S C,\ \mathrm{decomp}\ \S3,\ [1],\ [2a],\ [1v]\text{-COND},\ [2b],
[1v]\text{-FULL},\ [2r],\ [5],\ [3t]\text{-COND},\ [3],
[3t]\text{-FULL},\ [4]
\]

is a valid topological order for every displayed edge. Some independent nodes could be scheduled earlier—particularly \([5]\) relative to \([2r]\)—but a topological order need not expose all available parallelism.

The statement that \([3t]\)-FULL is a sink is also consistent: \([4]\) consumes \([3t]\)-COND plus the canonical CL ledger, not \([3t]\)-FULL as a mathematical input.

## Fresh sweep

The ordinary first-split recursion remains coherent: shared prefixes are charged once, branching is member-indexed, deep and shallow splits have distinct multilinear sites, and linear marked aggregation occurs only after splitting.

Three drafting-level issues remain:

1. The internal proof order of \([3]\) should be binding: RS.0\((\beta)\)/XRB first, then PCI-dependent \(b_e^{\mathrm{split}}\) and RS.1-SH. Without that written order, a future draft could accidentally recreate a local proof cycle despite the correct event DAG.

2. TREE-N’s lumped identity should explicitly say “for \(N\) at least the thresholds of all ordinary nodes, and with the chosen lump readable at level \(N\).” “\(N\)-stable by construction” conveys this intention, but the quantified display should prevent small-\(N\) misreading.

3. The non-null residual in `(R_e-exact)` is deliberately schematic. It must never be promoted into an additive root-level \(b_e^{ns}\) formula without a branching-aware derivation. For this campaign it is safe because `(ns-null)` is required before any full-density reading.

## CRITICAL OBSTRUCTIONS

None.

- **CRITICAL OBSTRUCTIONS: 0**

## CONCERNS

1. Pin the internal \([3]\) proof order: RS.0\((\beta)\)/XRB before either PCI substitution site.

2. Add the ordinary-threshold/readability qualifier to the TREE-N formula for \(T[N]\).

3. Keep the non-null ns residual schematic and outside the recursion; it has no licensed additive \(b_e^{ns}\) formula.

- **CONCERNS: 3**
- **Verdict: SOUND**

This is architectural soundness conditional on the honestly listed kernels. No claim is made that the theorem has been proved; `(SIB-STEP)`, XHD/HMC, RS.0, `(ns-null)` verification, EXHAUST, ESCAPE, TRACK-COUNT, RS.1, and the remaining ledger items still have to close.

**Acceptance clearance:** §C, §B2-DEF, and [1] D4-R retain their dual-accepted status. No new note is cleared for acceptance merely by REV 13. `[3t]`-COND may be accepted only after its own derivations, \([2r]\), and T-n3 close, with all CL-10/XRB/X.3 tags recorded; `[3t]`-FULL remains only the later named-hypothesis discharge event.

**Drafting clearance:** [2a] SPECIES and [1v]-COND retain drafting clearance. `[3t]`, including its `(SIB-STEP)`, TREE-EXP/TREE-N, VP, and `(ns-null)` work, is architecturally cleared to continue drafting and verification. `[2b]`, [1v]-FULL, [2r], [5], [3], and [4] remain subject to their displayed dependencies, kernels, and gates.
