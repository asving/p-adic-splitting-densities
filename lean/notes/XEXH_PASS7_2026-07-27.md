1. **CRITICAL — stale union-routing claim survives in the pass-3 record.** The record states: “**the four-way decomposition routes them to the 𝒯^ns piece … never the cap-detectable piece**.” An unordered union provides no such routing. Although rev 6 correctly restricts every operative bridge quantifier to intrinsically `(τ-ns)`-FREE branches, this surviving assertion violates the explicit requirement that no union-routing language remain anywhere, including the records.

The operative rev-6 predicates themselves are correctly propagated, cap is total on their quantified families, and `(τ-ns)`-FREE membership is decidable from per-node cell data under VP’s certificate clauses. No further end-to-end defect found.

Counts: **1 critical, 0 gaps.**

**Verdict: NOT ACCEPTED.**
