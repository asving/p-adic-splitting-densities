# MOVESGR — the graded-ring campaign blueprint (2026-07-26)

*Blueprint architect deliverable for the GRADED-RING Lean campaign. Turns the R5.3 spec
(`MOVES_LEAN_BLUEPRINT_2026-07-26.md` §R5.3) into an executable construction, discharging the
"definitional façade" STOP-THE-LINE of `MOVES_LEAN_SEMAUDIT5_2026-07-26.md` and the R5.3 boundary
that left five units unsafe. Read with `lean/LeanUrat/MovesGr/Defs.lean` (the shared vocabulary,
COMPILES green — `lake env lean`, no `sorry`/axiom) and `lean/LeanUrat/MovesGr/MANIFEST.json` (the
26 machine-checked unit statements).*

## 0. The objection this discharges

Audit #5's verdict, verbatim: round 5's `Stage.grRes f := C (σ.R f) * T (σ.w f)` "defines the
purported initial form FROM the residual output. It does not construct `gr_w(A)`, the genuine
initial form `[f]`, the homogeneous localization, the degree-one unit, an isomorphism carrying
`[f]` to `grRes f`." Consequently every `StageCoreL` certificate "silently consumes — in its
formal interface — the fact the blueprint defers." The five units left FAITHLESS were
`L1.baseStage_exists`, `L1.base_nonvacuity_gate`, `L4.TRANSvi`, `L4.TRANSstage`, `L5.recTRANSRS`.

This campaign builds the real objects and makes `grRes` a THEOREM about them.

## 1. The construction (D.1(a)-(e) taken literally; all in `Defs.lean`, all compiling)

The design decision, stated once: **build `gr_w(A)` from the side FILTRATION of `A = ℤ_[p][X]`,
induce its multiplication from `A`'s multiplication, and recover `σ.R` as `Θ ∘ [·]` — never the
reverse.** The concrete objects (each an anchor; `S : SideVal p`, `σ : Stage p F`):

* **(0) `SideVal p`** — the D.1 hypothesis package: `w : A → WithTop ℤ` (value `⊤` at `0`) with
  `w_zero`, `w_ne_top`, `w_neg`, ultrametric `w_add`, scalar law `w_smul` (`w f ≤ w (C c · f)`),
  submultiplicative `w_mul` (`w f + w g ≤ w (f·g)`), `w_one`. A genuine stage's `σ.w` extended by
  `0 ↦ ⊤` is an inhabitant (built by the L1 units).

* **(1a) The filtration** `ge γ := {f | γ ≤ w f}`, `gt γ := {f | γ < w f}` as honest
  `Submodule ℤ_[p] A` (membership discharged from the `SideVal` laws — COMPILES). `gtIn γ` is
  `gt γ` viewed inside `ge γ`. **`grPiece γ := (ge γ) ⧸ (gtIn γ) = A_{≥γ}/A_{>γ}`** (D.1) and
  **`Gr := DirectSum ℤ grPiece = gr_w(A)`**.

* **(1b) The genuine piece multiplication** — the anchor of the whole discharge:
  ```
  (†)  pmul γ δ : grPiece γ →ₗ[ℤ_[p]] grPiece δ →ₗ[ℤ_[p]] grPiece (γ+δ),   [a]·[b] = [a·b]
  ```
  built by descending `A`'s multiplication `(a,b) ↦ a·b` (`mulToGe`, a bilinear map into
  `ge (γ+δ)` via `w_mul`) through BOTH quotients with `LinearMap.liftQ₂`. Well-definedness is the
  submultiplicative strictness `WithTop.add_lt_add_of_{le_of_lt,lt_of_le}`: raising either factor's
  weight raises the product's. **`pmul` is INDUCED FROM `A`, not read off `σ.R`** — the exact point
  of audit #5. (Compiles in `Defs.lean`.)

* **(1c) `initialForm f := [f]`** `:= DirectSum.of _ (deg f) (mkPiece f)`, the genuine
  initial-form map (`deg f := (w f).untopD 0`, so `↑(deg f) = w f` for `f ≠ 0`, `coe_deg_le`).

* **(1d) `GradedRingStr S`** — the commutative graded RING structure on `Gr` whose product
  RESTRICTS to `pmul` (`mul_of`), with `1 = [1]` (`one_def`), `[·]` multiplicative (`if_mul`,
  D.1(c)) and the D.1(b) additive tie (`if_add_lt`). Constructed by the L1 units.

* **(1e) `GenuineStageModel σ`** — the R5.3 (1)-(4) bundle, the object audit #5 §7 said the
  signatures must carry. Fields in R5.3 order: `S`,`hSw` (gr built from `σ.w`); `Rg` (the graded
  ring); `homMon`,`Lo`,`Loring`,`alg`,`isLoc` (the homogeneous **localization** `L = gr[⟨U∪{Y}⟩⁻¹]`
  via mathlib `IsLocalization`, `homMon` generated by `initialForm` of `σ.reps` and `σ.Φ`);
  `Tunit`,`hT` (the degree-1 unit `T`, `Θ T = locT`); `Θ` (the D.1(e) ring iso
  `L ≅ L₀[T^{±1}] = LTwo ↥σ.K`); and the FIAT DISCHARGE
  ```
  (‡)  discharge : ∀ f ≠ 0,  Θ (algebraMap Gr Lo ([f])) = σ.grRes f
  ```
  Since `σ.grRes f = C(σ.R f)·T^{w f}` (DefsL) and `Θ T = locT`, (‡) is exactly D.1(e)'s
  `Θ([f]·T^{−w f}) = σ.R f`. So `(σ.w, σ.R)` ARE `x ↦ ([x], deg)` composed with the genuine
  structure; nothing reads a residual backwards.

## 2. Mathlib-reuse report (investigated)

* **Associated graded of a filtration/valuation — NOT in mathlib.** `Mathlib/RingTheory/
  Filtration.lean` has `Ideal.Filtration` (I-ADIC only) whose `submodule` is `⊕ Nᵢ` over the Rees
  algebra — the Rees module, NOT the graded quotient ring `⊕ Nᵢ/Nᵢ₊₁`. A grep for
  `associatedGraded`/`associated_graded` over all of mathlib returns nothing. **Conclusion:
  `gr_w(A)` must be built by hand** (this file's core), which is also why R5.3 flagged it "a large,
  self-contained formalization unit … not off-the-shelf."
* **Genuinely lighter reuse we DID take** (validated in `Defs.lean`): `Submodule` for `ge`/`gt`
  (ℤ_[p]-submodules — the `w_smul` law makes them submodules, giving the richer quotient API);
  `Submodule.Quotient` for the pieces; **`LinearMap.liftQ₂`** (`Mathlib/LinearAlgebra/Quotient/
  Bilinear.lean`, D. Loeffler 2026) + `LinearMap.compr₂` for the bilinear descent of `pmul` — this
  is the single biggest labor-saver, the whole well-definedness of the graded product;
  `DirectSum ℤ` for `⊕_γ`; `WithTop.add_lt_add_of_{le_of_lt,lt_of_le}` for submult strictness.
* **Localization — reuse `Localization` / `IsLocalization`.** For the FULL graded localization `L`
  (all degrees) we localize the CommRing `Gr` at the submonoid `homMon`: mathlib
  `Localization homMon` + `Localization.isLocalization` is the canonical witness (`L2_loc_exists`
  is a one-line reuse). For the DEGREE-0 part, `Mathlib/RingTheory/GradedAlgebra/
  HomogeneousLocalization.lean` (`HomogeneousLocalization`, `NumDenSameDeg`, `val`, `Away`) is
  directly applicable to `(L^coeff)₀` and `L₀` — recommended for `L2_degZero_subring`/`L2_coeffLoc`.
* **Graded-ring assembly — reuse `DirectSum.GCommRing`/`GradedRing`.** `L1_gradedRingStr_exists`
  should route the `CommRing Gr` through mathlib's `DirectSum.GSemiring`/`GCommRing` graded-monoid
  machinery, feeding `pmul` as the `GMul` and `[1]` as the `GOne` — this discharges assoc/distrib
  from the graded-algebra API rather than by hand.

## 3. The DAG (6 layers, 26 units; each ≤ ~40 expected proof lines)

Every unit is one theorem in `MANIFEST.json` (verbatim, elaboration-tested §6). Difficulty:
7 easy/medium-easy, 9 medium, 10 hard.

* **L0 — filtration/piece generalities (from `SideVal`; pure order/quotient).** `gt_le_ge`,
  `ge_antitone`, **`pmul_mk`** (`(†)`: `pmul [a] [b] = [a·b]`, via `liftQ₂_mk`), `initialForm_zero`,
  `initialForm_ne_zero` (D.1(a): `[f]≠0` for `f≠0`), `deg_add` (deg additive under valuation).
* **L1 — the graded RING D.1(a)-(c).** `pmul_assoc`, `initialForm_mul` (D.1(c)),
  `initialForm_add_lt` (D.1(b) tie), **`gradedRingStr_exists`** (the `CommRing Gr` capstone),
  `gr_domain_iff_val` (D.1(c): domain ⟺ valuation).
* **L2 — the homogeneous localization D.1(d).** `loc_exists` (mathlib `Localization`),
  `degZero_subring` (`L₀`), `homMon_homogeneous`, `coeffLoc` (S4 = DefsL `CoeffLocLaw`, now with
  provenance — discharges audit #5 §2).
* **L3 — D.1(e) rank-1 + iso.** `Tunit_deg1` (`Θ T = locT`), **`iso_exists`**
  (`Θ : L ≅ L₀[T^{±1}]`, the hard localization unit), `rank1_residual`
  (`Θ([f])·T^{−w f} = C(R f)` — the R5.3(4) display at the genuine object), `residual_mul`
  (recovers `σ.hRmul` as a THEOREM).
* **L4 — fiat discharge + bridge + the deferred units.** **`grRes_eq_genuine`** (`(‡)`: `grRes` IS
  the transported genuine initial form — kills the façade), **`genuine_imp_stageCoreL`** (the
  BRIDGE `GenuineStageModel σ → StageCoreL σ`, discharging audit #5 §7), **`baseStage_exists`**,
  `base_nonvacuity_gate` (concrete `GaloisField 2 2`), `TRANSvi`, **`TRANSstage`**.
* **L5 — recentering.** `recTRANSRS`.

**Load-bearing cross-layer edges (acyclic — DFS-checked):** `L0_pmul_mk → L1_{pmul_assoc,
initialForm_mul}`; `L1_gradedRingStr_exists → L2_*`; `L2_{loc_exists,degZero_subring} → L3_iso_exists`;
`L3_iso_exists → L3_{rank1_residual,residual_mul}`; `L3_rank1_residual → L4_grRes_eq_genuine →
L4_genuine_imp_stageCoreL → L4_baseStage_exists → {L4_base_nonvacuity_gate, L4_TRANSstage} →
L5_recTRANSRS`. The tower induction is: base genuine model (L4_baseStage) → child genuine model
(L4_TRANSstage, consuming D.7(i)-(vii) at the graded level) → recentered model (L5_recTRANSRS);
each closes `StageCoreL` through the single bridge `L4_genuine_imp_stageCoreL`.

## 4. The five R5.3-deferred units, mapped (all now non-vacuous)

| deferred unit | MOVESGR unit | how discharged |
|---|---|---|
| `L1.baseStage_exists` | `L4_baseStage_exists` | CONSTRUCT `GenuineStageModel` for the base (`gr = F[π,y]`, `L = F[π^{±1},y^{±1}]`, `T = π^s y^t`), then bridge to `StageCoreL` |
| `L1.base_nonvacuity_gate` | `L4_base_nonvacuity_gate` | concrete `p=2`, `GaloisField 2 2`, `φ = X²+X+1` inhabitant of `GenuineStageModel` |
| `L4.TRANSvi` | `L4_TRANSvi` | child S4/T-vector from the CHILD's genuine model, not from records that don't entail them |
| `L4.TRANSstage` | `L4_TRANSstage` | build the child graded model (D.7(i)-(vii)); `TransitionCoreL` records transport |
| `L5.recTRANSRS` | `L5_recTRANSRS` | transport the model through `z' = z−c̃`; child normalizing unit = parent's (actual objects) |

## 5. Honest gaps (explicit — NOT silent weakenings)

* **G-L0 (the one residue of the fiat).** `L₀ ≅ ↥σ.K[z^{±1}]` is carried as the TARGET type of
  `Θ` (`LExt ↥σ.K = LTwo ↥σ.K`), i.e. as the codomain of the D.1(e) iso — an explicit part of
  `GenuineStageModel`, not derived from a construction of `L₀` as `(L^coeff)₀`. We no longer read
  `grRes` backwards (that IS discharged), but the identification of the abstract `L₀` with the
  concrete Laurent ring is the last standing D2 fiat. Discharging it fully = building `(L^coeff)₀`
  via `HomogeneousLocalization` and proving `(L^coeff)₀ ≅ K` (the S4 field part), then
  `L₀ = (L^coeff)₀[z^{±1}]`. Flagged for the auditor.
* **G-Rg (bundle vs. per-stage construction).** `GenuineStageModel` bundles `Lo`/`alg`/`isLoc`/`Θ`
  as fields; the L2/L3 units construct them for base/child, and `L4_base_nonvacuity_gate` exhibits
  a concrete inhabitant (so it is not vacuous), but the bundle is not a single global functor
  `SideVal → GenuineStageModel`. Building that functor is the natural follow-on refactor.
* **G-hval.** `L1_gradedRingStr_exists` takes the valuation equality `hval` (D.1(c)) as hypothesis;
  a genuine stage supplies it from `σ.hwmul`. The base/child units discharge it at their sites.
* **`Defs.lean` scope.** `Defs.lean` provides `pmul` (the piece product, DATA) but NOT the assembled
  `CommRing Gr` — that is `L1_gradedRingStr_exists` (routes through `DirectSum.GCommRing`). This is
  the standard blueprint split (data in `Defs`, ring-axiom proofs in units).
* **§C composition** along read histories remains out of scope (D.11 defers it; pass-8 gap 7).

## 6. Elaboration-test record

`Defs.lean` compiles green (`lake env lean LeanUrat/MovesGr/Defs.lean`, exit 0, no `sorry`, no
axiom; olean built via `lake env lean -o`). Validated CONSTRUCTIONS (not just signatures): the
filtration submodules, the bilinear `mulToGe`, and the genuine piece multiplication `pmul` via
`liftQ₂` all elaborate as real terms. **All 26 `MANIFEST.json` statements** were elaborated TWICE:
hand-written, then REGENERATED verbatim from the manifest `statement` fields (`:= sorry` against the
`Defs`+`Moves` preamble with the `GaloisField 2 2` gate; `lake env lean`, exit 0, exactly 26 `sorry`
warnings, zero errors, both runs) — no transcription drift. Riskiest checks that passed: the
`letI := Rg.ring/Loring/alg` threading, mathlib `IsLocalization`/`Algebra`/`≃+*` in field position,
`DirectSum.of`, `HEq` in `pmul_assoc`, and the discharge equation `(‡)` landing in the same
`LTwo ↥σ.K` type as `σ.grRes`.

## 7. How the fan-out consumes this

**Preamble (every unit file):** the §MANIFEST `preamble` string, then the unit's `deps`. Set the
repo options (`linter.style.longLine/header false`, `maxHeartbeats 800000`). End with
`#print axioms <thm>` (Lean-core only; `sorry` shows as `sorryAx`, acceptable pre-proof).
**Sorry policy:** NO new axioms; unprovable stays `sorry`. **Search-before-prove:** the graded-ring
units reuse `DirectSum`/`GradedRing`/`HomogeneousLocalization`/`Localization` (§2) — grep those
namespaces before hand-rolling. **Build/verify:** `lake env lean LeanUrat/MovesGr/<file>.lean`
per file (NOT `lake build` — NFS hang). `Defs.lean` is green and its olean is built.
