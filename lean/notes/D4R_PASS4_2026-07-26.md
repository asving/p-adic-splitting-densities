1. **GAP — L11 misidentifies the residual degree with the raw side length.**  
   Quoted claim: “`deg R_anch,r equals that side's horizontal length ℓ_r`.” Under §C, exponents are the stride positions \(p_j(\gamma)-a\); for a side of raw horizontal width \(w'\) and stride \(e_r\), the residual degree is \(w'/e_r\), as §C’s DOM proof itself states (“the residual factor, of degree \(w'/e_{i+1}\)”). Thus the degree is shape-determined, but it is not generally the polygon’s raw horizontal length unless \(e_r=1\) or \(\ell_r\) is explicitly defined as normalized length. The degree-based definition
   \[
   \bar m_r=\left\lfloor\frac{\deg R_{\rm anch,r}}{g_r\mu_r}\right\rfloor
   \]
   is otherwise propagated consistently through L8, L11, D4R.2′, the reconciliation, FACTOR SELECTION, and sealed prediction (4).

2. **GAP — L6 still applies Theorem C(a) to the empty history.**  
   Quoted text: “For every \(\eta\in\mathrm{Pref}(\widehat P)\) (any \(\widehat P\)) … by Theorem C(a), \(S(\eta,\top)\) is exactly the locus…” This includes \(\eta=\varnothing\), while Theorem C’s history quantifier begins with the root read. Unlike D4R.1 and L12, L6 does not separately dispatch the empty history before this citation.

3. **GAP — Theorem C(b) is still cited without excluding the empty history.**  
   Quoted text in L6’s corollary: “Moreover Theorem C(b) gives \(S(\eta,\top)\) the positive mass \(p^{-A(\widehat P)}>0\).” This is quantified over every \(\eta\in\mathrm{Pref}(\widehat P)\), hence includes \(\eta=\varnothing\) when \(\widehat P=\varnothing\). Therefore the rev-4 claim that Theorem C(b) is now cited only for nonempty histories is false globally. D4R.1 and L12 themselves do correctly split off the empty prefix.

The D4-n3 computation is correct:
\[
\left\lfloor\frac3{2\cdot1}\right\rfloor
\left\lfloor\frac1{1\cdot1}\right\rfloor=1.
\]
The displayed values \(A=11\), \(W=7\), \(A_{\rm net}=4\), \(N=5\), \(C(2)=3\), \(C(3)=48\), all four finite-level counts, box totals, mass inequalities, and the aggregate \(404{,}128{,}692\) are arithmetically consistent. The OPEN/CONDITIONAL status perimeter and gate limitations are also consistent. No additional critical error was found.

**Counts: 0 critical errors, 3 justification/display gaps.**

**Verdict: NOT ACCEPTED (0 critical / 3 gaps).**
