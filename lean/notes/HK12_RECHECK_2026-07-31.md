# HK-12 RE-CHECK + THE ν_{i+1}.e STRETCH RE-ADJUDICATION (post-wave unit 1, 2026-07-31)

Executing agent: post-wave-1 (BP2 sequencing HK-12 → HK-52 → HK-11a/b, per
QUEUE_EXECUTION_2026-07-31.md §ITEM 3's post-wave queue).  Charge: the honest
steep-leg transport for the four reopened V9 sorries; corners flagged
re-adjudication-suspect — if FALSE under the new keying, compile the countermodel
mechanism, do NOT force.  Statements BYTE-UNTOUCHED throughout.

## Artifacts

* `verification/hk12_recheck.py` — the mandated countermodel re-check, five
  asserted findings (F1–F5), all green (sympy; `uv run --with sympy`).
* `lean/LeanUrat/HC2/HK52_stretchGate.lean` — the compiled mechanism leaf,
  4 theorems, 0 sorry, all Lean-core {propext, Classical.choice, Quot.sound}:
  `hk52_qform_stretch` (child-pair stretch at any STEP-2 record),
  `hk52_history_child_stretch` (read off a coherent history),
  `hk52_v9_qform_reduction` (V9's displayed ℚ-form ⟺ the ν_{i+1}.e-scaled
  σV-form), `hk52_gate_stretch_two` (factor 2 compiled at HK23's coherent H₂ram
  carrier).

## The findings (definitive state of the HK-52/HK-11 area)

F1 — the r3b countermodel data are NOT excluded at the `RegradeOf` layer.
`σV.w` is slot-min-FORCED by `RegradeOf`'s `IsSlotMinWeight` clause (its value on
every nonzero polynomial is determined by `σ.w` and the read pair), and the
D.7(i)–(v) σV residual laws were never ratified (B2-FINAL pass-8) and are NOT in
the definition.  BP2's "Expected: NO (fails the σV residual laws)" is answered
NOT-EXCLUDED at this layer; any exclusion would have to come from
TransitionCoreL/SideReads-at-read-1 inhabitation.

F2 — THE CURE (the wave did what it was designed to do, at e′ = 1).  Under (NEW),
`σ'.wPrev = σV.w` lands on the REGRADE (= wV), so on the r3b data
`σ'.w(B₂) = 1·wV(B₂) = 2 =` the displayed target.  The r3b countermodel NO LONGER
violates the kernel conclusion at unramified child pairs: R3b's bottom-window law
DISSOLVES for V9's steep legs — the route is now
V4-(VERTEX)-at-the-recorded-regrade, not a σᵢ.w transport.

F3 — BP2 HK-11a AS WRITTEN is dead and must be RE-POINTED.  Its displayed law
`σ'.wPrev (B μ) = νᵢ.e·σᵢ.w (B μ)` stays refuted verbatim on the r3b data under
(NEW) (2 ≠ 1) — and V9 never consumes it.  The honest HK-11a target is the
σV-VERTEX LAW `σV.w (B μ) = gam − μ·e★·g·h★`, whose supply is V4-(VERTEX) at the
regrade + the R3a ANCHOR TRANSPORT (the ~300-line V4-kernel-shaped derivation
spec'd in V9's REV-2 obstruction record — unchanged, still the substantive
mathematics at root/post-recentering steep reads).

F4 — THE NEW OBSTRUCTION: the ν_{i+1}.e stretch (compiled).  On EVERY
(NEW)-coherent non-recentering step, `σ_{i+1}.w (B μ) = ν_{i+1}.e · σV.w (B μ)`
(`hk52_qform_stretch`; Stage `hStretch` + `child_e` + `child_wPrev`), while
`strFrame(i+1)` carries NO ν_{i+1}.e factor (parent-side strides only, all 1 on
the RG-2 perimeter).  So (`hk52_v9_qform_reduction`) V9's displayed ℚ-form is
EQUIVALENT to `ν_{i+1}.e · σV.w(B μ) = strFrame(i+1)·lineᵢ.at(μ·cwᵢ)`:

* TRUE-perimeter: `ν_{i+1}.e = 1` — the displayed form IS the σV-vertex law.
* FALSE-SUSPECT: `ν_{i+1}.e ≥ 2` with nonzero vertex value.  Recordable — a
  TERMINAL child's own leg never fires, and the coherent carrier is compiled
  (HK23's H₂ram, child pair (2,5); `hk52_gate_stretch_two` shows the literal
  factor 2 there).  This covers ALL FOUR reopened V9 sorries — the two steep legs
  AND both corners (at the corner, `σV.w = σ₀.w` on the coefficient space by
  `RegradeOf.w_coeff`, so the ℚ-form reduces to `ν₁.e·(gam₀ − μ₀h₀) = gam₀ − μ₀h₀`).
* Residual to a FULL refutation: ReadsOf inhabitation at one
  ramified-terminal-child record — a Block-G-gate-scale build (HK-18/19/57/20/22
  shaped, ~1000+ lines of concrete side arithmetic).  NOT built this unit.
* Residual to a FULL proof at ν_{i+1}.e = 1: the σV-vertex law (F3's re-pointed
  HK-11a = R3a + scale conversion at root/post-recentering reads; F5's
  frame-pair story at interior reads).

F5 — INTERIOR DEGENERATION (changes HK-52's content).  At i ≥ 1 with a
non-recentering predecessor, `child_e`/`child_h` pin the FRAME pair to the READ
pair (σᵢ.e = νᵢ.e = 1 by the fence, σᵢ.h = νᵢ.h), and the e★ = 1 regrade slot-min
is IDEMPOTENT over a stage's own weight (`hStretch` folds w into wPrev):
σV.w = σᵢ.w pointwise.  So interior non-recentering regrades carry NO weight
content, genuinely h-steep reads (h★ > σ.h) occur only at i = 0 or immediately
after recenterings, and HK-52's "per-slot forced-window recursion at σV" is the
IDENTITY at interior reads.  The interior e·g ≥ 2 legs' honest content is the
frame-pair side structure — HK-08's restatement territory, NOT a σV recursion.

## THE ADJUDICATION ITEM SURFACED (blocks filling the V9 sorries)

The Q2-extension ruling (RG-2, executed in the wave) fenced the PARENT read's
stride only.  The ν_{i+1}.e ≥ 2 disposition on V9's four legs is UNRULED.
Candidate dispositions (mirroring the wave's menu):

  (i)  named-hypothesis / guard `σ_{i+1}.e = 1` on the four legs — a
       statement-fence event (needs Asvin sign-off; the R-tie guard precedent);
  (ii) extend the recording fence to TERMINAL reads (a MovesC/Defs change —
       heavier, breaks HK23's positive gate as stated);
  (iii) commission the ReadsOf ramified-terminal countermodel first (gate-scale)
       and let the compiled refutation force (i).

DO NOT fill or force the V9 sorries before this ruling.  The sorries stand as
honest HK-11 obligations with the perimeter now EXACTLY mapped.

## What survives of the BP2 unit names

* HK-12 — DONE (this unit; outcome above, richer than the expected binary).
* HK-52 — RE-SCOPED by F5: no σV recursion needed; the residual per-slot content
  lives at root/post-recentering reads inside the re-pointed HK-11a.
* HK-11a — RE-POINTED at the σV-vertex law (R3a + V4 at the regrade); still the
  hard mathematical core, still open.
* HK-11b — BLOCKED on the ν_{i+1}.e adjudication above; at ν_{i+1}.e = 1 its
  plan (route (i)–(vi) with the re-pointed HK-11a) stands.
* HK-11c — unchanged (waits on HK-11b).
