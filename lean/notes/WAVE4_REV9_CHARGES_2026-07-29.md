# WAVE-4 REV-9 CHARGES (2026-07-29) — the CLOSING blueprint round (both corpora)

Round-8 verdicts: T Codex 6/4 ("the principal REV-8 repairs are mathematically sound";
criticals = elaboration/closure + IsLumpLocus) + T Fable 0/2 (all rows real; ~80 manifest
entries verified; fresh: NsBridgeStmt = discharge-by-tautology); V Codex 6/3 (counting
tie real; the zc-based toy illegal) + V Fable PASS 0/1. ORCHESTRATOR SYNTHESIS: the
mathematical content is verified on both; this round executes the enumerated point fixes
and the TWO adjudicated designs, then both corpora go to E-PHASE (the compiler is the
closure arbiter; the toys become decide-checkable Lean; the dual ratification runs on the
AS-BUILT statements per the MovesS/MovesD precedent). No further full markdown audit
rounds. Produce REV 9 in place, full disposition table, updated closure manifest. No git.

## PART T (MovesT blueprint)

T1 [ADJUDICATED — the Fable GAP-1 tautology inversion; binds]. NsBridgeStmt currently
PROVES its conclusion from its own premises by pure real analysis — the owner is bound
to a tautology, and the measure content (that the REAL conditional masses instantiate
μcap/μclass/Z) is typed nowhere. RESTRUCTURE into the honest split:
  - NsPricing (OPEN, owner MovesS-w5/[2b]): the C(b) pricing equality typed AGAINST THE
    REAL CARRIERS — μcap M is DEFINED (not a free field) as the finite-level conditional
    cap mass of the (τ-ns) class within Σ at cap M (use the corpus's own cap/locus
    carriers from T-C5..C7), and the pricing law says μcap M = μ(Σ)·p^(−Z M) with Z the
    REAL pinned-coordinate count |Z_M| (typed from interiorFreshIdx/the lump locus).
  - NsGrowth (OPEN, owner same): Tendsto Z atTop atTop for the real Z.
  - nsNull_of_pricing_growth (PROVED in-corpus — the pure-analysis leg, exactly what
    the current NsBridgeStmt proof would be).
  The identifications ARE the fields; a `True`-style or constant instantiation must be
  impossible because μcap/Z are no longer free. Re-run BOTH prior counter-models (the
  μcap ≡ 1 model and the rev-6 True-channel) — display their deaths.
T2 [Codex-8 crit 5 — semantic]. IsLumpLocus gains the literal-zero VALUE clause: the
  locus's solve is ZERO on the pinned coordinates (Z.solve c = 0 for c ∈
  interiorFreshIdx …) — without it the T-C5→C6→C7 identification of the priced locus
  with L_M fails. One clause; re-check the chain.
T3 [mechanical, from both legs]: (a) UnramifiedOfDeg gets a real displayed decl (one
  line: ramIdx g = 1 ∧ resDeg g = d). (b) FreshData.clauses is a LIST — fix the Finset
  usages (List.toFinset with the note on duplicates, or .foldr union). (c) declare the
  CellAssign→CellData coercion (instance or uniform .toCellData). (d) the
  toyBelowHaltExcess roster/gloss mismatch (Fable GAP-2 — fix the roster to match the
  gloss: the below-halt extension set is {tA2a.snoc tAνa-style} per the gloss; recompute
  the gate). (e) the withHenV arity slip; (f) the §7 tally prose "three findings" → four;
  (g) TreeExpFin's stale "nothing dropped" sentence; (h) the emptyFiber manifest pointer
  (point at the real decl in MovesD/Defs.lean, not the D11a note). (i) ramIdx/resDeg:
  keep the Mathlib names (they exist — Fable verified) but move the INSTANCE PLUMBING
  (IsLocalRing (AdjoinRoot g) etc.) to a displayed E-phase obligation row (the def as
  displayed may need a junk-total dispatch; design honestly, no silent totality).
T4: refresh the closure manifest for every touched item; the E-PHASE HANDOFF section:
  list the units, the decide obligations (toy gates), the instance-plumbing obligations,
  and the disclosed open boundaries (consumer non-vacuity/F1 witness, G1b, hRes-Q1).

## PART V (MovesV blueprint)

V1 [ADJUDICATED DESIGN — the census-level coupling; binds; supersedes the zc mechanism].
  Codex-8 N-1 is CORRECT: the zc-based toy violates ext_zc (the flag-0 history is an
  extension of the root's zc witness, hence zc) and the refined flag-0 cell violates
  zc_ne. THE LEGAL MECHANISM (orchestrator walk, verified against the laws): set
  zc ≡ true on ALL cells (both systems); encode the coupling in the CENSUS/EVENT FIELDS:
  tinst/tevt at the COMPOSITE template and (h₁,h₂) = (0,0) are EMPTY while the per-step
  censuses are full — legal precisely because marks do not compose (the blueprint's own
  "combinatorial chains need not be f-realized" comment; chainCount(0,0) = 2 > 0 = Tgam
  is the marks_chains-consistent coupling signature). All StepSys laws then hold
  trivially (ext_zc/zc_ne/out_ne with zc ≡ true); XHDd holds per-step and at the
  composite with dom γ = the coupled set; ¬HMC = dom γ ⊊ the product. The h₁-flag
  refinement splits the censuses per refined state; HMCAug holds; zc' ≡ true so the
  refined zc_ne is fine. REWRITE the V4-11 toy this way; re-walk the four-point check
  AND the cover laws; also FIX the unbound (hb p) in the cover display (bind it).
  Record the mechanism adjudication in §0 (the zc route is BARRED BY ext_zc — a
  structural fact worth recording: zc cannot carry branch memory).
V2 [Codex-8 N-2]. CutRealization must tie the census to the REALIZED fibers: add the
  field encode_fiber : ∀ i, encode i ∈ Pr.fiber i (or the realization law the note's
  marked-entrance mass demands — each counted tag IS a realized continuation point of
  its prefix). A tagged-census adversary must fail by type; display the kill.
V3 [converged closure items]: (a) spWord/IsBranchingLetter/letterMenuCard/SelfLoopLetter/
  speciesTagOf/monicTopOf get displayed Defs-layer decls (or the consuming defs move
  after their homes — no forward refs). (b) gcellDenom: ONE home (a displayed §2.H decl),
  body included. (c) V7-9: a displayed Classical-choice wrapper def for the pool
  representative (M.rep does not exist; use rep_ne) + the total continuing/terminal
  dispatch displayed (the +T twin route) + a §7(A) manifest row. (d) the C.Outc slip →
  (C.bd s).Outc m. (e) V1-3 exceeds the ≤4-lemma unit rule — split (V1-3a/b/…) and
  update the census arithmetic. (f) the manifest fixes: consumedDeltas' home
  (MovesS/Defs.lean), real path:line for the D4R1_SUM/D0b/D4R4_all rows, rows for every
  identifier from (a)-(c). (g) M1: state the Finite instance obligation for the
  EligibleImage subtype as a typed row (hcard stays a premise; the docstring claim
  becomes a displayed obligation).
V4: the E-PHASE HANDOFF section (units, decide obligations — the census-level toy is
  FINITE: list the decide checks that will machine-verify ¬HMC and the refinement laws —
  instance-plumbing obligations, disclosed boundaries incl. the V7-8 W17ii sorry-gate
  and the tCount identification via ValA).

Return {units, dispositions, summary} per corpus (the T agent returns T's, the V agent V's).
