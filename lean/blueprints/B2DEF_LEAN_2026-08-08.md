# B2DEF_LEAN — the §B2-DEF carry-algebra apparatus as the ONE shared Lean supplier (blueprint, 2026-08-08)

CHARTER: synthesis pass 4 finding **F4** (`docs/SYNTHESIS_PASS4_2026-08-08.md`, queue
item 4). GRADED-READ ((H1)/B-M2's open seam) and TRANS-DEEP ((H2)'s open kernel row)
are NOT the same obligation, but both discharge routes run through the SAME unbuilt
Lean layer: the compiled §B2-DEF carry-algebra graded-read apparatus (𝒟 = F[u]/(u^e −
z̄), the aligned DIG, the anchor-monomial transport). This blueprint charters that
layer ONCE, with the TWO consumer interfaces displayed verbatim (§3, §4), so the
corpus never grows two carry-algebra formalizations.

MATH SOURCE OF RECORD: the ACCEPTED §B2-DEF (`lean/notes/MOVES_2026-07-24.md`
:1849–2745; accepted 2026-07-26, passes 20+21 consecutive CLEAN; DUAL-ACCEPTED at rev
D¹¹d, MOVES :5606). Consumer sources: `HDISCHARGE_H2.md` §5 (TRANS-DEEP) + the LANDED
`Scaffold/HDischarge/H2/TransDeep.lean` (H2-U9, gate PASSED 2026-08-02);
`lean/notes/openmath/H1_BM2_2026-08-06.md` §S1/§S2/§S4 (i-b) (B-M2 ACCEPTED,
14-pass arc) + the LANDED `Scaffold/HDischarge/H1/{Defs,TowerStep}.lean`.

STATUS: BLUEPRINT (design, no proofs). E-phase transcribes §5's units verbatim;
deviations are statement changes (E-phase sign-off discipline; standing
statement-change authority applies to honest changes only).

---

## §0. The design decision, stated once

**§B2-DEF is already substantially formalized.** The HC1 campaign
(`lean/notes/HC1_LEAN_BLUEPRINT_2026-07-28.md`, executed; `LeanUrat/HC1/MANIFEST.json`:
"the §B2-DEF tower induction hard core") carries the carry algebra (D-layer), the tower
spine (S-layer = D.2/D.4–D.11), the digit/block/alphabet layer (T/C/CL = D.3(e)), and
the V′ read-functional kernel (V-layer). **This campaign builds NO new tower theory.**
What is missing — and what BOTH consumers are blocked on — is exactly two *projection
layers* out of the compiled apparatus:

* **(GR)** the fired-lift graded reading: from `IsReadLift`'s hard-wired stage-law data
  fields to the graded (SL-dev) identity, i.e. (K1)+(K2) with U₀ = 1 at fired lifts —
  §B2-DEF D.5's EQUAL WEIGHTS + RESIDUAL displays at the corpus `Stage` carrier,
  transported through the PARENT stage's `GenuineStageModel.Θ/discharge` [R1-C3 fold:
  the (i-b) identity is a parent-graded statement (B-M1's R^gr_{γ̂} at the parent
  read); the CHILD-model `KPBlock`/`kp_step` face is landed and NOT touched here].
  Consumer: H1/B-M2 (§4).
* **(TD)** the counting shadow: from digit-cylinder facts (D.3(e)(ii.1)–(ii.4), D.8
  (BOX)/(VERTEX)/(TRANSPORT), D.11 one-move ledger) to the census-keyed counting laws of
  the `TransDeep` row — an INSTANCE of the H2 skeletal carriers whose counting fields
  ARE level-N cylinder counts, plus proofs of the (F)/(J) laws and the typed (L)
  carrier. Consumer: H2 (§3).

New units live in `lean/LeanUrat/B2D/` (namespace `LeanUrat.B2D`), importing HC1/HC2/
Moves/MovesGr/Scaffold — never duplicating them. Anything provable by citing an
existing decl is REUSED (§1 census); a unit that finds itself re-proving a cited decl
is mis-scoped and stops.

---

## §1. Reuse census (the definitional layer already exists — cited, not rebuilt)

The carry algebra as a Lean structure over the existing corpus carriers — the survey
the F4 charter demanded. Each row: decl (file) — §B2-DEF display it carries.

**1.1 The carry algebra 𝒟 = F[u]/(u^e − z̄)** (pass-7 spec MOVES :1502–1530):

| decl | file | carries |
|---|---|---|
| `CarryAlg F e := ZMod e → F`, `carryExp`, `cmul`, `dOf`, `IsHomog`, `CarryRingPack` | `HC1/DefsD.lean` | 𝒟 as the concrete graded function object with the cocycle product; z̄ enters through multiplication only |
| `D1_carryRing` … `D6_carryHomogUnits` | `HC1/D1..D6_*.lean` | CommRing (cocycle identity), u_ρ·u_σ law, graded-domain, `𝒟 ≃+* F[u]/(u^e − z̄)` (AdjoinRoot), e = 1 field case, homogeneous units |
| `D7_digD_mul` | `HC1/D7_digD_mul.lean` | **the aligned DIG**: DIG(B) = (ρ(B), z̄^{q(B)}·[R_anch(B)]) ∈ (ℤ/e) ⋉_{z̄} Fˣ, the twisted hom law (§B2⁵.1 MOVES :1385–1400 + Lemma DIV(iii) via Moves `L3_DIV`) |
| `D8_digD_recenter` | `HC1/D8_digD_recenter.lean` | recentering preserves 𝒟-typing (D.10 S5-unchanged + e_birth/e_read) |

**1.2 The anchor-monomial transport** (D.8/D.10/D.11):

| decl | file | carries |
|---|---|---|
| `S14_wellTypedAnchor`, `S15_anchoredSubst` | `HC1/S14,S15_*.lean` | D.10 well-typedness (a ≥ 0) + the substitution identity R′(f) = R(f)(z′+c̃), the (z′+c̃)^a·z′^μ display |
| `L5_landBox`, `L5_landVertex`, `L5_landVertexDigit`, `L5_landTransport_R4`, `L5_landTwoSided` (repair file), `L6_moveAffineBij`, `L6_ledgerCount` | `Moves/L5*,L6*.lean` (clean) | D.8 (BOX)/(VERTEX)/(TRANSPORT) both directions; the move bijection; the ledger count |
| `S16_levelLanding`, `S17_levelMeasureExact` | `HC1/S16,S17_*.lean` | D.8/D.10 at level k (hexact threaded per stratum); D.11 ONE move, measure-exact, attainable product |
| `K1_vertexPin` (`VertexPin`, twist-class bridges) | `HC1/K1_vertexPin.lean` | the read-indexed D.8 vertex pin, D.7(vi) normalized exponent `mfun ≡ 0` |

**1.3 Tower spine + child residual (D.2, D.4–D.7, D.9)**: `T1/T2` (base stage),
`S1–S13` incl. `S9a/b/c/d` builders, `S9w_residualLaws` (the lawful child-residual law
pack hR0/hRne/hRadd/hRlt/hRmul — PROVED), `S9r_childR`, `V8_childResidual` (PROVED in
full, Lean-core; the descent carrier `Rc f := Σ_{j min} C(digPrime z̄ (B_j))·T(js′ −
t′σ.w B_j)`), `V34_readKernel` (ReadFrame `ResValPack`), `V10_transportWindow`,
`S12_iaugPersist` (D.4), `S13_realPersist` (D.9), `HC2/SharedRecenter`, `HC2/SharedZC`.

**1.4 Digit/block/alphabet layer (D.3(e))**: `T3–T12` (height chains, slot-min
heights, alphabet span/step = (ii.2), **`T9_blockDisjoint` = (ii.3)**, floors
staircase T10, **`T11_attainDichotomy`** (off-alphabet pin ⟹ empty), **`T12_coordFin`**
(the global-order coordinate system behind (ii.1)/(ii.4))), `C1–C6` consumer bridges,
`CL01a/b/c`, `CL07–CL09`, `CL11–CL14` (positional uniqueness, packE).

**1.5 Graded model layer**: `MovesGr/Defs.lean` (`GradedRingStr`,
**`GenuineStageModel`** — fields S/hSw/Rg/homMon/Lo/isLoc/Tunit/**Θ**/hT/**discharge**),
`L0_*`/`L1_initialForm_add_lt`/`L1_initialForm_mul`/`L2_*`/`L3_Tunit_deg1`/
`L3_iso_exists`/`L3_rank1_residual`/`L3_residual_mul`/`L4_grRes_eq_genuine`; H1 helper
lemmas `stage_w_pow`, `stage_R_pow`, `initialForm_pow`, `initialForm_add_of_w_eq`,
`nodeLift_key_res` (`Scaffold/HDischarge/H1/Order1.lean`).

**1.6 Fired-lift carriers**: `IsReadLift` / `IsNodeLift` / `isNodeLift_iff`
(`HC2/Defs.lean` :150–199 — the window/weight/twist-residual FIELDS that are
GRADED-READ's hypotheses), `HK52_stretchGate` (`HC2/HK52_stretchGate.lean` — the
compiled child-pair stretch `σ'.w B = e′·σV.w B`; a *child-side* mechanism: cited as
the scale-bug precedent, NOT consumed — GR lives at the parent weight).

**1.7 Consumers, compiled and waiting**: H2 — `CensusData`
(`Scaffold/ValueSide/CensusCore.lean`), `WindowDatum`/`ParentShape`/`JointStratum`
(`H2/Carriers.lean`), `FloorsChargeLaw`/`DigitsProdLaw`/`FiberSumLaw`/`FreshReadsLaw`/
`SlotUniformLaw`/`JunctionPinLaw`/`FreshClauses` (`H2/FreshLaws.lean`),
`freshReads_of_digitsProd` (U5, `H2/FreshReduction.lean`), **`TransDeep`**,
`fresh_of_transDeep`, `freshRowOn_of_transDeep` (`H2/TransDeep.lean`), `R0Instance.lean`
(the r = 0 de-vacuifier precedent for TD-0). H1 — `KPBlock`, `GrBKeyLaws`,
`Key1SiteBlock`, `DevBound` (`H1/Defs.lean`), **`kp_step`** (`H1/TowerStep.lean`),
`RCWitness`/`RecParamE` ([RC] side, not touched here).

**1.8 Honest sorry inventory at HEAD (grep 2026-08-08)** — HC1: `C6_alphabetCard.lean:129`
(the OPEN **CL-05** fullness-from-clean-threshold identity — ON this campaign's path,
see unit CL5), `CL04_addrDichotomy.lean:190` (open countermodel PROBE — adjacent, not a
dependency), `V9_K1nonrec.lean` ×4 (steep+corner legs at e′ ≥ 2, FALSE-SUSPECT per the
HK52 ruling — **adjudicated OFF-PATH here**, §6 fence (v)). HC2: `U17a:57`, `U26:39`
(unrelated). Everything else cited above is sorry-free at HEAD.

---

## §2. The theorem layer, minimized to what the TWO consumers need

From the accepted §B2-DEF's D.0–D.12, the consumers need ONLY:

**(GR) needs D.5's two displays at the parent stage** — EQUAL WEIGHTS (each present
slot has w(t_k) + ek·h = ehg = w(Φ̂); MOVES :2266–2270) and RESIDUAL (R(t_k·Φ^{ek}) =
ψ_k·z^{k−thg}, distinct exponents, no cancellation, R(Φ̂) = z^{m̂}·ψ(z), m̂ = −thg;
MOVES :2270–2277) — plus their transport through the PARENT model's
`GenuineStageModel.discharge` into the graded ring. HONESTY [R1-C2 fold]:
`IsReadLift` alone does NOT imply equal weights — the slot total h′(g−k) + e′k·σ.w(σ.Φ)
is k-free iff the SCALE LAW `(h′ : ℤ) = e′·σ.w σ.Φ` holds (D.5's own bookkeeping:
h′ = e·h with h = w(Φ)); that law and the Bézout/twist pins behind m̂ = −t·h·g are
NOT `IsReadLift` fields and ride in the displayed `ReadHyps` pack (§4, unit GR-0 —
supplier-or-hypothesis per field). NOT needed: Lemma K1(Φ̂/w) itself (that is the NEXT
stage's (S2), already HC1 S3's business), D.7 stage transport, D.9, D.10 (the [RC]
legs are REC-SL, owned by B-M2's continuation — §6 fence (ii)), and the child-model
`KPBlock` assembly (landed `kp_step`, verbatim — §6 fence (viii)).

**(TD) needs D.3(e)(ii.1)–(ii.4) + D.8's three clauses + D.11 at ONE level, projected
to counts** — all carried by T/S/C/CL units (§1.2–1.4); the NEW content is (a) the
census-carrier instantiation (counting fields defined as level-N cylinder counts),
(b) the per-slot product law across a ρ-fiber (D.3(e)(ii)'s "each equation pins the
fresh leaves of its own block", counted), (c) the CL-05 counting identity (the one
place genuinely new mathematics enters: C6:129's honest sorry — full alphabet card at
clean thresholds, spec CI-1–CI-4 in the C6 proof comment), and (d) the typed (L)
carrier from (ii.3)+(ii.4). NOT needed: composition of moves (§C-side; D.11's own
fence, MOVES :2716–2718), exhaustiveness (`hExhaust`), drainage.

---

## §3. Consumer interface A — the TransDeep supplier (VERBATIM, from the landed `Scaffold/HDischarge/H2/TransDeep.lean`)

The row this campaign must FILL (an [M]-row today: "DISPLAYED, consumed by
`fresh_of_transDeep`, NOT proved in this corpus. SUPPLIER OF RECORD: the ACCEPTED
§B2-DEF"). Verbatim:

```lean
structure TransDeep (W : WindowDatum D) (P : ParentShape D W)
    (J : JointStratum D W P) where
  /-- (F), floors face: per-slot height counts at the joint condition
      max(β_k, g_k), shape-only (§B2-DEF D.8 (BOX) + D.11 through the proved
      A1/TRI ledger). -/
  floorsCharge : FloorsChargeLaw W P J
  /-- (F), product-box face: fiberwise per-slot factorization of the
      prescribed-digit counts (§B2-DEF D.3(e)(ii) digit cylinders + D.11
      attainable product). -/
  digitsProd : DigitsProdLaw W P J
  /-- (F), aggregation seam: digit-level counts aggregate to the height-only
      fiber count (§B2-DEF D.3(e)(ii) bottom-up display: w ≥ γ ⟺ all lower
      digits vanish). -/
  fiberSum : FiberSumLaw W P J
  /-- (L) [leak], §5 verbatim referent: "at every interior on-line slot
      k ≠ k_s, the parent's realized conditions impose NO constraint on the
      height-β_k digit of a_k beyond (F)'s floor." UNPINNED bare-`Prop`
      field (RootHyps E-phase convention) — see the structure docstring's
      fold record; True-instantiable; not consumed by `fresh_of_transDeep`. -/
  leakFree : Prop
  /-- (J): the junction read is a ρ-determined nonzero constant (§B2-DEF
      D.8 (VERTEX) + D.6 + D.10). -/
  junctionPin : JunctionPinLaw W P J
```

with the waiting consumers (verbatim signatures):

```lean
theorem fresh_of_transDeep (W : WindowDatum D) (P : ParentShape D W)
    (J : JointStratum D W P) (hTD : TransDeep W P J)
    (huni : SlotUniformLaw W P J) : FreshClauses W P J

theorem freshRowOn_of_transDeep (D : CensusData)
    (Strata : ∀ (W : WindowDatum D) (P : ParentShape D W),
      Set (JointStratum D W P))
    (hTD : ∀ (W : WindowDatum D) (P : ParentShape D W),
      ∀ J ∈ Strata W P, TransDeep W P J)
    (huni : ∀ (W : WindowDatum D) (P : ParentShape D W),
      ∀ J ∈ Strata W P, SlotUniformLaw W P J) :
    FreshRowOn D Strata
```

**What "fill the row" means here** (the H2 carriers are SKELETAL — `Carriers.lean`
docstring: "the engine's constructed strata instantiate them at the BP_IV wave-4
seam"): deliver a designated strata family `LedgerStrata` whose `JointStratum` counting
fields (`rhoCount`/`slotCount`/`slotDigits`/`fiber`/`fiberDigits`) are DEFINED as
level-N digit-cylinder counts of §B2-DEF ledger strata (TD-0), and prove

```lean
theorem transDeep_of_ledger … (hJ : J ∈ LedgerStrata …) (hscope : …)
    (hclean : …) : TransDeep W P J        -- TD-6, the row supplier
```

with `leakFree` instantiated by the TYPED carrier of TD-5 (per the TransDeep docstring:
"keyed to §B2-DEF D.3(e)(ii.3)/(ii.4)"; its H2-face wiring is the gated H2-U10 —
TD-5 lands the carrier standalone so the gate, when it opens, consumes not builds).
Pricing fences carried over verbatim: `SlotUniformLaw` is NOT supplied (it is (ADM) +
(GR-B)-line content — TransDeep fence (i)); the junction VALUE is NOT identified
(fence (ii): only nonvanishing + ρ-fixedness).

---

## §4. Consumer interface B — the GRADED-READ discharger (VERBATIM, from H1_BM2 §S4 (i-b) + the landed H1 carriers)

**The open seam, verbatim** (`H1_BM2_2026-08-06.md` §S4): "**(i-b) GRADED-READ — the
graded (SL-dev) READING, OPEN.** That the hard-wired stage-law data
(window/weight/twist-residual fields) yields the GRADED identity
in(D_κΦ^(eκ)) = U₀ψ_κϕ_{γ̂}ȳ^κ — i.e. (K1)+(K2) for the fired lift, hence KP-STEP
class membership — is the GenuineStageModel seam (Θ/isLoc/discharge) … Consumers
blocked on it: KP-STEP's application to any fired lift; OL-1(b) and OL-1(c) at THE
lift (§S3); the DevBound Lean row ((vi)); the abstract-engine OL-1(b)/(c) readings."

The display it must produce (B-M1 §S1.3/§S3.2, verified; U₀ ∈ K_iˣ):

```
(SL-dev)  in(D_κ · Φ_i^{eκ}) = U₀ · ψ_κ · ϕ_{γ̂} · ȳ^κ   (κ with ψ_κ ≠ 0)
(SL-top)  D_g = 1
⟹ (K0) + (K1) + (K2) with U₀ = 1,  where
(K1)  w_{i+1}(Φ_{i+1}) = γ̂ = e·g·γ_{i+1},
(K2)  R^gr_{γ̂}(in Φ_{i+1}) = U₀·ψ.
```

**The hypothesis side is already hard-wired** (LIFT-CONF clause 2, [NR]): the fired
lift's digits carry `IsReadLift`'s fields verbatim (`HC2/Defs.lean`) — window
`inC σ.Φ (tt k)`, marched weight `σ.w (tt k) = h'·(g−k)`, twist residual
`σ.R (tt k) = LaurentPolynomial.C (ψ.coeff k) * LaurentPolynomial.T (−σ.t * σ.wPrev (tt k))`,
and (SL-top) hard-wired (`Φhat = σ.Φ^(e'·g) + Σ_{k<g} tt k · σ.Φ^(e'·k)`).

**The corpus carriers the discharger fills** (verbatim):

```lean
-- GrBKeyLaws.key_res (H1/Defs.lean) — the σ.R-face of (K2):
key_res : ∃ (c : (↥ν.σ.K)ˣ) (k : ℤ),
  ν.σ.R σ'.Φ = LaurentPolynomial.C (c : ↥ν.σ.K) * LaurentPolynomial.T k *
    Polynomial.toLaurent ν.ψ

-- KPBlock.kpa_init (H1/Defs.lean) — the graded face, with ψhom now the
-- TRANSPORTED residual (today kp_step self-keys ψhom := in(σ'.Φ), unit 1):
kpa_init : letI := M.Rg.ring; ∃ u : (M.S.Gr)ˣ,
  M.S.initialForm σ'.Φ = (u : M.S.Gr) * ψhom

-- kp_step (H1/TowerStep.lean) — the M-binder the seam is "owned at":
theorem kp_step (ν : Node p F) (σ' : Stage p F) (M : GenuineStageModel σ')
    (hΦ : IsNodeLift ν σ'.Φ) (he : σ'.e = ν.e) (hh : σ'.h = ν.h)
    (hprev : σ'.wPrev = ν.σ.w) (D : DevBound ν σ' M) :
    Nonempty (KPBlock σ' M)
```

**Proposed discharger target** (GR-4; E-PHASE PROBE REQUIRED — this signature is a
design display, not frozen). RESTRUCTURED at the round-1 design review [R1-C1/C2/C3/C4
folds]: the model binder sits at the PARENT stage ν.σ (the (i-b) identity is B-M1's
parent-read R^gr_{γ̂} statement; the child-model `KPBlock` is `kp_step`'s landed
business), the GRADED clause is IN the conclusion (not prose), and the wiring
hypotheses that `IsReadLift` does not carry ride the DISPLAYED pack `ReadHyps`:

```lean
-- GR-0 (E-phase): the displayed wiring pack + its supplier map.  Fields, each
-- tagged SUPPLIED-BY-<wiring decl> or DISPLAYED-HYPOTHESIS after the probe:
structure ReadHyps (ν : Node p F) : Prop where
  hscale : (ν.h : ℤ) = ν.e * ν.σ.w ν.σ.Φ     -- THE equal-weights scale law
                                              -- (probe: coherence width clause /
                                              --  hDwidth per RecParamE precedent)
  hbez   : …                                  -- the Stage T-vector/Bézout pins
                                              -- behind m̂ = −t·h·g (probe: Stage laws)
  hψ     : …                                  -- ψ monic irr., ψ.coeff 0 ≠ 0,
                                              -- ν.g = ν.ψ.natDegree (Node.hψdeg …)
  hRlaws : …                                  -- the parent σ.R product/tie laws the
                                              -- computation consumes (probe: V34
                                              --  ResValPack / S9w at the parent)

theorem readHyps_of_node (ν : Node p F) (h… : wiring) : ReadHyps ν   -- GR-0
-- residue fields that no wiring decl supplies STAY displayed hypotheses of
-- graded_read (the kp_step unconsumed-binder precedent; listed, never silent).

theorem graded_read (ν : Node p F) (Mpar : GenuineStageModel ν.σ)
    (Φ' : Polynomial ℤ_[p]) (hlift : IsNodeLift ν Φ') (R : ReadHyps ν) :
    -- (K1), parent weight:
    (ν.σ.w Φ' = (ν.e : ℤ) * ν.g * ν.σ.w ν.σ.Φ)
    -- (K2), Laurent face (fills GrBKeyLaws.key_res with c = 1, k = m̂):
    ∧ (∃ m̂ : ℤ, ν.σ.R Φ' = LaurentPolynomial.T m̂ * Polynomial.toLaurent ν.ψ)
    -- (K2), GRADED face — the (i-b) identity through the parent model
    -- (via Mpar.discharge: Θ(ι in(Φ')) = σ.grRes Φ' = C(σ.R Φ')·T^{w Φ'}):
    ∧ (letI := Mpar.Rg.ring; ∃ m̂ : ℤ,
        Mpar.Θ (algebraMap _ _ (Mpar.S.initialForm Φ'))
          = LaurentPolynomial.T m̂ * Polynomial.toLaurent ν.ψ
            * (algebra/T-embed of T^{ν.σ.w Φ'}))   -- exact form: E-phase probe
```

with the per-κ (SL-dev) identity as GR-2's displayed intermediate, at BOTH faces:
σ.R (tt κ · σ.Φ^{e′κ}) = C(ψ_κ)·T^{pos(κ)} and its Θ-image — under the (P4)
dictionary this IS in(D_κΦ^{eκ}) = U₀ψ_κϕ_{γ̂}ȳ^κ (ϕ_{γ̂}ȳ^κ ↔ the T-monomial at
the stride position; B-M1's ϕ_{γ̂} = p^{gγ_{i+1}}, ȳ = x^e p^{−h} are its order-1
frame instances). CONSUMPTION CHAIN, displayed (nothing rebuilt) [R1-C5 fold — the
former corollary "GR-4b `kpblock_of_read`" is DELETED: it re-assembled `kp_step`'s
own fields]: (i-b) discharged on [NR] = `graded_read` + B-M1 §S3.2's VERIFIED
arithmetic ((SL-dev)+(SL-top) ⟹ (K0)+(K1)+(K2), U₀ = 1); a KP-STEP application
additionally consumes `kp_step` AS LANDED (child model M, `DevBound` row — untouched,
their conditionality displayed there). SCOPE, per B-M2's own display: this discharges
(i-b) — lift-level (K1)+(K2) on the [NR] carriers, GIVEN the parent model Mpar. It
does NOT touch SITE-EXH, REC-SL, TR-3-ORD-X, or A-M2 (model inhabitation at depth
≥ 2) — §6. THE E-GATE (GR-0) IS A NAMED RISK: if the probe finds the corpus wiring
cannot supply `hscale` (the HK52 lesson: scale identities are the recurring defect
class — probe, never assume), GR escalates with a compiled obstruction report before
any GR proof effort is spent.

---

## §5. Unit decomposition (E-phase-first; parallelizable waves; hardest hole FIRST)

**THE HARDEST-HOLE CALL, named first: the hardest WAVE is TWO INDEPENDENT HARD FRONTS
[R2-G2 fold] — front 1 = TD-0 + TD-3 (`DigitsProdLaw` at the ledger instance: the
skeletal-carrier instantiation + the fiberwise product count); front 2 = CL5 (the
CL-05 counting identity, which feeds TD-1's full-alphabet regime, NOT TD-3
directly).** Rationale: (a) it
is the one unit whose STATEMENT might fail at the skeletal carrier — the H2-U9
statement gate already killed two proposed (L)-adjacent forms, and D¹¹c proved the
uniform field-alphabet display FALSE at shallow heights, exactly the regime this law
crosses; (b) it has no near-verbatim supplier (S17 counts ONE stratum's mass; the
per-slot factorization ACROSS a ρ-fiber is new counting); (c) CL5 is on record as
"real new mathematics, not in the corpus at HEAD" (C6:129) and TD-1's q-power count
consumes it at full-alphabet regimes; (d) everything downstream (TD-6, U5's
`freshReads_of_digitsProd`, CEN-J) routes through it. GR's hardest leg (GR-3
no-cancellation) is, by contrast, D.5's two-line distinct-exponent argument with clean
Moves/MovesGr suppliers. CONDITIONALITY OF THE CALL [R1-C6 fold]: the round-1 review
correctly ruled that GR's INTERFACE soundness (the `hscale` scale law, the parent-model
binder, the `ReadHyps` supplier map) precedes any proof-difficulty ranking — so the
GR-0 E-gate (wave E, below) runs FIRST and the ranking is trusted only on its PASS;
on a GR-0 obstruction the campaign re-adjudicates the ranking before staffing wave 1.
Per the repo discipline the fleet staffs TD-0/TD-3/CL5 first (after wave E);
a failure there re-scopes the campaign before further effort is spent.

Waves (units within a wave are parallel; `deps:` are cross-wave):

**Wave E (skeleton + THE INTERFACE RISK GATE; 3 units).**
* **E1** `B2D/GRDefs.lean` + statement fan-out `GR1..GR4` with sorry bodies —
  `ReadHyps` (§4: `hscale`, `hbez`, `hψ`, `hRlaws` + the D.5 (S6b)/(I-aug) range
  fields), `m̂`; compile-probe the §4 target shapes against
  `IsNodeLift`/`GenuineStageModel`. moves_ref: D.0 (P1)–(P4), D.5.
  **LANDED 2026-08-02 (wave E)**: `GRDefs.lean` (ReadHyps flattened per-fact,
  FLAGGED; readPos; m̂ = the EXISTING `MovesC.Node.mhat`, cited not rebuilt;
  (S6b)/(I-aug) probe: no field added, flagged) + `GR1..GR4` files, 8 E-phase
  sorries, all statements compiling; §4 target shapes elaborate (third
  graded_read clause probe record in GR4's docstring).
* **GR-0** `B2D/GR0_readHyps.lean` — THE E-GATE [R1-C2/C4 fold]: `readHyps_of_node`
  — prove each `ReadHyps` field from the recorded wiring (probe order: coherence
  width clause / `hDwidth`, `Node.hψdeg`, Stage T-vector laws, V34/S9w parent
  instances — the `RecParamE`/REC-WIRE-G derivation precedent) or tag it
  DISPLAYED-HYPOTHESIS with no silent residue. Output: the supplier map table in
  the module docstring + either PASS or a compiled obstruction report (HK52
  precedent). deps: E1. difficulty: medium; HIGH information value.
  **LANDED 2026-08-02 (wave E) — E-GATE VERDICT: PASS-WITH-DISPLAYED-RESIDUE**:
  `GR0_readHyps.lean`, NO sorry, Lean-core. Supplier map (docstring table):
  9/11 fields SUPPLIED (`Node.hbez/hbezCanon/hψmonic/hψirr/hψdeg`,
  `Stage.hRmul/hRlt/hRadd/hRΦ` at the parent); `hscale` + `hψ0` =
  DISPLAYED-HYPOTHESES (no single-node wiring decl; `HistoryCoherent`'s slope
  law named as the history-level wave-2 supplier candidate; kp_step
  displayed-binder precedent). Non-vacuity: `readHyps_of_node_level` discharges
  `hscale` at the level/inert configuration via `Stage.hwΦ`. No interface
  obstruction fired ⟹ per the §5 conditionality clause the hardest-hole ranking
  STANDS; wave 2a runs in residue-displayed mode.
* **E2** `B2D/TDDefs.lean` + fan-out `TD1..TD6` — `LedgerStratumData` (the §B2-DEF
  stratum datum at level N: window chain, per-slot heights β_k, junction k_s, parent
  realization ρ, the hexact anchor pin per S16/S17), the counting-field definitions
  [R1-G1 fold — TWO LAYERS]: the fields are q-GENERIC formulas (the O-9 census's
  q-polynomial world, all q : ℕ), and a SEPARATE named semantics lemma ties them at
  q₀ = the corpus prime power to actual level-N ℤ_p cylinder counts (Fintype card —
  the `R0Instance` pattern at r ≥ 1; the Carriers docstring's own "q = p^δ
  prime-power semantics" conformance duty), including the `Fin (q^D.d)` digit-value
  coding [R1-G2 fold] that `FiberSumLaw`/`JunctionPinLaw` presuppose. **SUPPORT
  CONSTRAINT [R2-C1 fold — load-bearing]**: because every H2 law is `∀ q : ℕ` while
  the ledger content lives only at the corpus prime power q₀, `rhoCount` MUST be
  defined to VANISH off the support — in particular `rhoCount q = 0` for `q < 2`
  (at q ∈ {0, 1}, `q^D.d ≤ 1`, so `JunctionPinLaw`'s `∃ c ≠ 0, c < q^D.d` is
  UNSATISFIABLE; the law can hold there only through a vacuous `ρ < rhoCount q`
  premise). This makes all `∀ q` laws hold vacuously off q₀ and substantively at q₀
  (the R0Instance skeletal-carrier pattern), and confines the fixed-field digit
  transport to q = q₀ where the digit exists — resolving the all-q-vs-fixed-q₀
  junction mismatch. moves_ref: D.3(e)(ii), D.8, D.11.
  **LANDED 2026-08-02 (wave E)**: `TDDefs.lean` (`LedgerStratumData`,
  `ledgerJoint` with the SUPPORT CONSTRAINT **PROVED** Lean-core
  (`ledger_rhoCount_off_support`/`ledger_rhoCount_lt_two` — the [R2-C1] fold
  discharged), `blockCount`, `LedgerSemantics`, `LedgerLawful`, `LedgerStrata`,
  `LedgerScope`, `LedgerClean`; NO sorry) + fan-out `TD1..TD6` files (7 E-phase
  sorries) incl. TD-5's typed `LeakFreeCarrier` DEF landed. E-PHASE FINDING,
  FLAGGED (TDDefs docstring flag 2): the count-VALUE content priced at TD-1
  necessarily rides `LedgerLawful.slot_count_val` (an abstract datum has no ℤ_p
  content), so the LED/CL5 substance moves into TD-0's clause discharge —
  wave-1 staffing re-adjudicates pricing; no statement deviates.
  **STATEMENT ROUND 2 (v2), LANDED 2026-08-02** — the wave-1 hard-hole run
  (commit 5371139) REFUTED the v1 semantics with a compiled countermodel
  (`TD3_countermodel.lean`: `LedgerSemantics`'s `Nat.card` over ALL of
  `Coord → ℕ` is empty-or-infinite under box-supported membership ⟹ fiber
  counts ≡ 0 against the positive `slot_count_val` product; design flag 1
  turned CONTRADICTORY with lawfulness). The v2 layer is `TDDefsV2.lean`:
  `supportUnion` (parentBlock ∪ boxSlots.biUnion slotBlock — box slots ONLY,
  so the erased monic-top block contributes no free factor), finite-box
  `LedgerSemanticsV2` (fiber counts = `blockCount`s over `supportUnion`,
  `Fin q₀` values — MOVES D.11's `#{f mod p^N}` at the constrained
  coordinates; the N-TD1 trace's `members_on_window(U, q0)` count IS the
  spec, [LHS-V2] 3/3 product-match), `LedgerLawfulV2` (semantics clause
  swapped, all eleven other clauses byte-verbatim v1), `LedgerStrataV2` +
  helper lemmas (`blockCount_congr`/`blockCount_of_not`, proved Lean-core).
  MANDATORY COUNTERMODEL CHECK compiled in `TD3v2_control.lean` (no sorry,
  Lean-core): `cmL2` (the countermodel witness's shape with corrected
  finite-box counts; `supportUnion = {(0,0)}`, count 2¹ = 2 computed) with
  `cmL2_lawfulV2`, `cm2_digitsProd` (**DigitsProdLaw HOLDS, PROVED, at the
  v2 control** — the same shape refuted v1 as 0 ≠ 2),
  `v2_mechanism_inapplicable` (¬∀ of `not_digitsProd_of_lawful`'s v2
  transposition), `v2_devacuify_possible` (TD-0 duty (c) restored; contrast
  `lawful_fiber_devacuify_impossible`), `cmL_not_lawfulV2` (the refutation
  witness does not port). v1 decls STAY byte-unchanged in TDDefs
  (superseded-marked) as the refutation record; TD-1..TD-6 restated on the
  v2 carrier (same names/laws, `LedgerLawfulV2`/`LedgerStrataV2`
  hypotheses, 7 E-phase sorries re-tagged "round 2").

**Wave 1 (the hardest hole; 3 units; gated on GR-0's outcome only for RANKING,
not for content).**
* **TD-0** `B2D/TD0_ledgerInstance.lean` — the instance is LAWFUL, which means
  exactly [R1-G3 fold]: (a) the `WindowDatum`/`ParentShape` LAW FIELDS
  (`hℓ/hn/hn2/hmono/hbox/honLine_box/hks_line/hks_top`; `hN0`; `hN` — `JointStratum`
  itself carries only `hN`) hold at the built instance; (b) the E2 q₀-semantics
  lemma (formula fields = cylinder counts at q₀); (c) non-vacuity at a designated
  family (de-vacuifier duty: one compiled r ≥ 1 witness; precedent
  `H2/R0Instance.lean`). deps: E2. difficulty: hard. REUSE: S16/S17, T12, C1–C5.
  **ROUND-2 STATUS (2026-08-02)**: wave-1 run verdict BLOCKED-REFUTED at v1
  (duty (c) impossible — `lawful_fiber_devacuify_impossible`); UNBLOCKED by
  the E2 v2 round: duties (a)-(c) now target `LedgerLawfulV2`, and (c) is
  compiled-possible (`TD3v2_control.v2_devacuify_possible`). Open, staffable.
  **LANDED 2026-08-02 (wave-2 run)**: `TD0_ledgerInstance.lean`, NO sorry,
  all Lean-core. The designated instance = N-TD1 trace i2 lifted to census
  depth r = 1 (`tdD`: f = ![2,1], d = 2, s ≡ 2; `tdW`: monicTop = false so
  the junction is ON-LINE; `tdL`: junction slotCond pins the read leaf).
  Duties: (a) `td_lawfulV2` (junction/digit_range/slotDigits_sem
  NON-vacuous — the control had them vacuous); (b) `td_semanticsV2`
  (finite-box counts computed: fiber 16 = 2⁴, per-slot 4 = 2^(mN−s) exact);
  (c) `td_devacuify` + `td_scope`/`td_clean` + `td_mem_ledgerStrataV2` at
  r = 1. BONUS positive control: `td_digitsProd` — DigitsProdLaw PROVED at
  the instance with genuine on-line digit content. NEW helper (per the TD-3
  add-if-needed provision): `blockCount_pin` (+ `pinnedRestrictEquiv`) —
  pinned-leaf block count `q^(|B|−1)`. TRANSCRIPTION CAVEAT recorded: trace
  i2's declared slot-1 exponent (3) violates `slot_count_val`'s formula
  (mN − s = 2) — i2 as data is NOT v2-lawful; `tdL` resizes slot 1's block
  to 2 leaves so the formula holds exactly.
* **TD-3** `B2D/TD3_digitsProd.lean` — `DigitsProdLaw` at the instance: per-slot
  factorization of `fiberDigits` (on-line digit counts × off-line height counts).
  Proof skeleton: (ii.3) `T9_blockDisjoint` (distinct pins hit disjoint leaf sets) +
  (ii.4)/`T12_coordFin` (unit-diagonal: each pinned leaf forced given predecessors,
  whatever the predecessors) ⟹ the level-N box splits as a product over slots ⟹
  card = ∏. The shallow-height regime rides the ATTAINABLE product (T11 dichotomy),
  never full alphabets. deps: TD-0. difficulty: **hardest**.
  **ROUND-2 STATUS (2026-08-02)**: v1 statement REFUTED at the wave-1 run
  (`TD3_countermodel.ledger_digitsProd_refuted`, the ¬∀ of the v1 closure —
  kept on file verbatim); RESTATED on `LedgerLawfulV2` (E2 v2 round). Gate
  state for round 2: N-TD1 [LHS-V2] 3/3 MATCH + the compiled positive
  control `cm2_digitsProd` (the law PROVED at the v2 control instance).
  Honest open goal again; proof skeleton above unchanged (the box now
  splits FINITELY over slot blocks).
  **ROUND 2 REFUTED (2026-08-02, wave-2 run)** — a SECOND, lawfulness-pack
  seam class (the v2 SEMANTICS repair stands; the PACK is too weak for the
  D.11 product): `TD3v2_countermodel.ledger_digitsProd_v2_refuted` is the
  compiled ¬∀ of the round-2 statement. Seam A (parent satisfiability): no
  clause asserts per-realization member existence — `inStratum ≡ False` is
  lawful, LHS 0 vs positive `slot_count_val` product (`cmL4`). Seam B
  (overlap): `parent_interior_disjoint` = (ii.3) fences interior ON-LINE
  slots only, so a parent condition may cut inside an OFF-LINE slot's block
  — NONEMPTY stratum, `pinned_forced` substantive, joint 1 vs product 2
  (`cmL3`); not an emptiness pathology. The pack yields D.11's injectivity
  half only. WHY THE GATES MISSED IT: every N-TD1 instance has
  `parentBlock = []` (both seams silently closed there; on that subfamily
  the law is TRUE — `cm2_digitsProd`, `td_digitsProd`). V3 STATEMENT ROUND
  OWED (statement fence: not executed at this run): add (i) per-realization
  member existence, (ii) parent-block/box-slot disjointness (trace-faithful:
  the junction pin rides the junction's own `slotCond`); TD-0's `tdL`
  already satisfies both. The E-phase sorry STANDS refuted-open,
  byte-unchanged.
* **CL5** `HC1/C6_alphabetCard.lean:129` (in place — HC1 property) — the CI-1..CI-4
  identity: equidistribution `#aligned = ∏g per achieved class`, digit-line card =
  |F_Q|, joint independence `aDim = N·[F_Q:F_p]`, `card F_{K+1} = |F_Q|^{∏g}`;
  assembly closes C6's sorry. Consumed by TD-1's full-alphabet q-power counts.
  difficulty: hard (genuinely new). Falsifier N-TD1 runs BEFORE proving.

**Wave 2a (GR; 4 units; runs only on GR-0 PASS or with GR-0's residue displayed).**
* **GR-1** `B2D/GR1_equalWeights.lean` — (K1): ν.σ.w Φ′ = e′·g·σ.w(σ.Φ) from
  `IsReadLift` weight fields + `ReadHyps.hscale` [R1-C2: NOT derivable without it]
  + the slot-min law. REUSE: `stage_w_pow`, S9w weight laws,
  `initialForm_add_of_w_eq`. deps: E1, GR-0. difficulty: medium.
* **GR-2** `B2D/GR2_slotResidual.lean` — per-κ (SL-dev), σ.R-face:
  σ.R (tt κ · σ.Φ^{e′κ}) = C(ψ_κ)·T^{pos(κ)}, pos affine in κ with unit κ-step
  (D.3(c) stride at the read; `ReadHyps.hbez` supplies the exponent pins), + the
  Θ-image corollary (the per-κ (i-b) display, §4). REUSE: `stage_R_pow`,
  `L3_residual_mul`/S9w `hRmul`, `nodeLift_key_res` pattern. deps: E1, GR-0.
  difficulty: medium.
* **GR-3** `B2D/GR3_keyResidual.lean` — the sum: distinct T-exponents (pos strictly
  monotone in κ) ⟹ no cancellation (D.1(b)) ⟹ σ.R Φ′ = T^{m̂}·toLaurent ψ, m̂ = −t·h·g
  — fills `GrBKeyLaws.key_res` with c = 1. REUSE: `L1_initialForm_add_lt` (Laurent
  face), D.5's MOVES :2270–2277 arithmetic. deps: GR-1, GR-2. difficulty: medium-hard.
* **GR-4** `B2D/GR4_gradedRead.lean` — the (i-b) seam theorem `graded_read` (§4:
  the three-clause conclusion INCLUDING the parent-graded face through
  `Mpar.discharge`) + the consumption-chain docstring (B-M1 §S3.2 + landed `kp_step`;
  NO `KPBlock` assembly here — R1-C5). REUSE: `GenuineStageModel.discharge`,
  `L4_grRes_eq_genuine`. deps: GR-3. difficulty: medium.

**Wave 2b (TD remainder; 4 units).**
* **TD-1** `B2D/TD1_floorsCharge.lean` — `FloorsChargeLaw`: per-slot height count =
  q^{mN − s(max(β_k, g_k))} under `hscope` + `hclean`. REUSE: T10, C6 (via CL5), the
  A1/TRI ledger count (`L6_ledgerCount`). deps: TD-0, CL5. difficulty: medium-hard.
* **TD-2** `B2D/TD2_fiberSum.lean` — `FiberSumLaw`: sum over the digit alphabet box
  recovers the height-only count (the (ii.2) bottom-up display "w ≥ γ ⟺ all lower
  digits vanish"). REUSE: T3/T5, D.3(e)(ii) bottom-up (MOVES :2170–2173). deps: TD-0.
  difficulty: medium.
* **TD-4** `B2D/TD4_junctionPin.lean` — `JunctionPinLaw`: the k_s-read constant ∃c ≠ 0
  from D.8 (VERTEX) `dig′(B_μ) = z̄^{−μm̂}·((R(f)/ψ^μ) mod ψ) ≠ 0`, ENCODED [R1-G2
  fold]: only at q = q₀ (E2's SUPPORT CONSTRAINT gives `rhoCount q = 0` for all other
  q, so the `∃ c` clause is reached ONLY where the field digit exists) the field digit
  passes through E2's `Fin (q^D.d)` coding to the ℕ-code c < q^D.d, and the off-c
  `fiberDigits = 0` leg rides T11's forced-value/off-alphabet dichotomy at the coded
  coordinates; off q₀ the law is vacuous [R2-C1 fold — this is what makes the all-q
  `JunctionPinLaw` well-founded, q ∈ {0,1} included]. REUSE: `L5_landVertexDigit`,
  `K1_vertexPin`, S16, T11; D.6 (dig′ ∈ F′ˣ). Route ADJUDICATED: rides S16/L5 +
  K1_vertexPin — **never V9** (§6 fence (v)). deps: TD-0. difficulty: medium.
* **TD-5** `B2D/TD5_leakCarrier.lean` — the TYPED (L) carrier `LeakFreeCarrier` (a
  named Prop: parent-pinned blocks are DISJOINT from every interior on-line slot's
  height-β_k block, and each pinned leaf's value is forced given predecessors —
  (ii.3)+(ii.4) verbatim, per the TransDeep docstring's key). PRIORITY HONESTY
  [R1-G4 fold]: neither compiled consumer INSPECTS `leakFree` today
  (`fresh_of_transDeep` ignores it), so this unit is CHARTER-DEMANDED (the F4
  (F)/(L)/(J) row + the TransDeep fold record's own "typed carrier lands with the
  engine instantiation" key), not consumer-blocking; its real consumer is the gated
  H2-U10 classifier face, which CONSUMES, never builds, this carrier when un-gated.
  It is a REQUIRED campaign deliverable ([R2-G1 fold]: a charter-demanded artifact is
  not optional), plugged into TD-6's `leakFree`. REUSE: T9, T12. deps: TD-0.
  difficulty: medium.

**Wave 3 (assembly; 1 unit).**
* **TD-6** `B2D/TD6_assembly.lean` — `transDeep_of_ledger` (§3) + the
  `freshRowOn_of_transDeep` demonstration corollary at the designated family (with
  `huni` as the displayed (ADM) hypothesis, never supplied); `leakFree` is
  instantiated by TD-5's `LeakFreeCarrier` (the charter-demanded typed carrier — NOT
  the `True` fallback; [R2-G1 fold]). deps: TD-1..TD-5. difficulty: easy (glue).

**Unit count: 15** (3 wave-E incl. the GR-0 gate + 3 hardest-hole + 4 GR + 4 TD
wave-2b + 1 wave-3), plus 3 numeric families (§7). All units obey the HC1
preamble/build conventions (per-file `lake env lean`, `#print axioms` footer, no new
axioms, hypothesis-never-axiom).

---

## §6. Fences (what this campaign does NOT do)

(i) **A-M2**: no `GenuineStageModel` instances are built; GR stays M-parametric
(kp_step's M-binder discipline — B-M2 §S4 (i-b) "owned there").
(ii) **REC-SL** ([RC] (SL) legs) and **SITE-EXH** (firing-site completeness): B-M2
continuation property; GR is [NR]-carrier-scoped.
(iii) **TR-3-ORD-X**, OL-1(b) GMN-verbatim face: GRB residue, untouched.
(iv) **SlotUniformLaw/(ADM)** and the junction VALUE ((c-GR)): TransDeep fences
(i)/(ii) carried verbatim.
(v) **V9_K1nonrec's e′ ≥ 2 legs**: OFF-PATH by adjudication — (J) rides
S16/L5_landVertexDigit + K1_vertexPin; no TD/GR unit may import V9. A unit finding
itself needing V9's steep leg stops and reports (the HK52 ruling stands).
(vi) **hExhaust / drainage / §C composition**: out of scope (D.11's one-move fence).
(vii) Statement fence: consumer signatures (§3, §4 carriers) are UNTOUCHABLE; any
mismatch is repaired on the B2D side or escalated.
(viii) [R1-C5] The child-model `KPBlock` assembly (`kp_step`, `u8_key_prime`,
`DevBound` staging) is LANDED and out of scope — no B2D unit re-assembles or
re-proves it; GR ends at the parent-graded (i-b) clauses + the displayed
consumption chain.

---

## §7. Falsifier plan

**Existing families → units covered** (all sealed/green at HEAD):
* HC1 decide gates `G1_divGate` (DIV over F₄), `G2_carryGate`, `G3_caseJGate`,
  `G4_caseKGate`, `G5_spanGate` → D-layer, DIG, anchor arithmetic (GR-2/GR-3's
  exponent algebra at instances).
* Sealed census: Cases E/F/F-odd/G/H/I (level-1 landings) → TD-4 (VERTEX pin);
  G2-level-2 (lift independence) → GR scope clause; **Case J** (a > 0 anchor-monomial,
  12 strata exact) → S15/GR-3 m̂ bookkeeping; **Case K v2** (p = 3, F₉ recentering) →
  the D.10 face TD-4 cites; **Case L** (attainable-product masses: β-cells 1/16, joint
  cells 1/64, 10⁶ members) → TD-3's attainable product exactly.
* `BM2_TRACE_2026-08-02.md` (p = 3 quartic tower): the graded identity EXACT at both
  [NR] reads, one shared U₀ = 1 → GR-4 end-to-end.
* N8-eq2 alphabet-law harness (EQ-2) → T7/T8-fed alphabets (TD-2).
* H2-N1/N2 (H2 blueprint §7) → the standing TransDeep numeric fence (TD-6).

**NEW families needed (build BEFORE proving the unit they gate):**
* **N-GR1** — per-κ (SL-dev) harness at e′ ≥ 2 AND g ≥ 2 with nontrivial twist
  (σ.t ≠ 0) positions: predicted pos(κ) affine, unit step, m̂ = −t·h·g, AND the
  GR-0 scale law h′ = e′·w(Φ) checked at every recorded read; extend the BM2 trace
  driver. Gates GR-0/GR-2/GR-3 (the scale/exponent defect class).
* **N-TD1** — shallow-height `DigitsProdLaw` harness: a stratum family with some slot
  height BELOW the clean threshold (attainable alphabet a PROPER F_p-subspace);
  predicted: factorization holds with |𝔸(γ_i)| factors, FAILS with |F| factors.
  Gates TD-3's statement + CL5's scope BEFORE proofs are attempted.
  **RUN RECORD**: built + run 2026-08-02
  (`verification/openmath/ntd1_td3_ledger_semantics_trace.py`); round 1 —
  v1 statement FAILS 3/3 vs intended math MATCH 3/3 (fired the TD-3
  refutation, commit 5371139); round 2 — the [LHS-V2] column (v2 finite-box
  semantics) MATCHES 3/3; gate GREEN for the v2 statements.
  **SCOPE CAVEAT (recorded at the wave-2 run)**: the trace family probes
  the SEMANTICS tie only on the `parentBlock = []` subfamily — it never
  probes parent-satisfiability or parent/slot overlap (the two lawfulness
  seams that refuted TD-3 round 2, `TD3v2_countermodel.lean`), and its
  `slot_val_exp` are instance DATA checked for blockCount consistency, not
  against `slot_count_val`'s formula (i2's slot-1 exponent 3 ≠ mN − s = 2,
  so i2 as data is not v2-lawful). Any v3 gate run must add parent-pinned
  instances and check the formula.
* **N-TD2** — junction-count harness at `monicTop = false` windows: ∃c ≠ 0 with
  off-c `fiberDigits = 0`, c ρ-fixed across the fiber. Gates TD-4's count-level form.

---

## §8. Records + acceptance

DESIGN REVIEW OF RECORD: Codex round 1 (2026-08-08, fresh context, transcript
`lean/notes/openmath/B2DEF_LEAN_codex_design_r1_2026-08-08.txt`): REJECT, 6 critical
/ 4 gaps — ALL folded in place ([R1-…] tags throughout: the GR interface restructure
C1–C4, the GR-4b double-build deletion C5, the ranking conditionality C6, the
q-semantics/coding/lawfulness/TD-5-priority gaps G1–G4). Round 2
(`…_codex_design_r2_2026-08-08.txt`): confirmed 8/10 round-1 folds GENUINE, returned
REJECT (2 critical / 2 gaps) on LOCAL residues — R2-C1 (the all-q `JunctionPinLaw`
needs `rhoCount` to vanish at q ∈ {0,1}, else `∃ c ≠ 0, c < q^D.d` is unsatisfiable —
folded into E2's SUPPORT CONSTRAINT + TD-4), R2-C2 (§8 acceptance still named the
deleted GR-4b — fixed in (B) below), R2-G1 (TD-5 must be REQUIRED, not optional —
fixed), R2-G2 (hardest-hole = two independent fronts, CL5 feeds TD-1 not TD-3 —
fixed). ALL FOUR folded in place ([R2-…] tags); per the max-2-rounds discipline no
round 3 is run: the residues are a support-constraint on one Defs field, a record
typo, and two scoping clarifications — not architecture. DESIGN FROZEN for E-phase.

Per-unit: HC1 conventions (module docstring with moves_ref, difficulty, deps;
`#print axioms` footer). Campaign: MANIFEST addendum in `B2D/MANIFEST.json`; ledger
line in `lean/notes/BRIDGE_ADJUDICATIONS_2026-07-30.md` at charter and at landing.
Acceptance per consumer: (A) `transDeep_of_ledger` compiled, Lean-core axioms, the
designated family non-vacuous, the TD-5 typed (L) carrier `LeakFreeCarrier` LANDED and
plugged into TD-6's `leakFree` (NOT the `True` fallback — the charter demands the (L)
carrier as a deliverable; [R2-G1 fold]), N-TD1/N-TD2 green — then the H2 row's
[M]-status updates to SUPPLIED-at-the-family (never "discharged" beyond the family's
scope); (B) `graded_read` compiled with its three-clause conclusion (the parent-graded
face included), Lean-core, N-GR1 green [R2-C2 fold — the deleted GR-4b
`kpblock_of_read` is NOT an acceptance target; fence (viii) prohibits it] — then B-M2's
§S4 (i-b) row updates to DISCHARGED-on-[NR]-at-the-M-binder (SITE-EXH/REC-SL/A-M2
riders stand, displayed). Both updates are ROOT-owner moves, not this campaign's.
